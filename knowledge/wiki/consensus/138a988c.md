---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 138a988cbf05e031c0a17c6ab02fff7dadb3375db47a7c22e1a6ef3042493545
sha8: 138a988c
ts: 2026-06-10T02:54:14.054Z
task_type: auto-userprompt
source_session: 19dff632-e5c0-49cd-ad5a-524091f39df0
mode: compare
recommendation: review
agreement_score: 0.5
success_count: 1
total_latency_ms: 2498
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-userprompt, review]
---

# Consensus Run `138a988c`

**Recommendation:** `review` · **Agreement:** `0.5` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
<task-notification>
<task-id>wbbpdtioh</task-id>
<tool-use-id>toolu_017iCCd4Q2t2A3Ghwkwcqpeq</tool-use-id>
<output-file>C:\Users\wompu\AppData\Local\Temp\claude\H--prism\19dff632-e5c0-49cd-ad5a-524091f39df0\tasks\wbbpdtioh.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Brainstorm + plan a dependency-ordered forge roadmap to finalize the hotel (ERP/business) slot, grounded in the mined 19-session open-threads + live-code dedup verification" completed</summary>
<result>{"synthesis":"All load-bearing facts are now verified against live code. The adversarial lens is correct: the route exists with an allowlist, the hotel WRITE actions + ~120 financial-write actions are documented-403-pending-review, business engines have zero persistence, CustomerPortalEngine holds 4 in-memory Maps, and the Q2S DFM path can pass a non-array `ctx.features`. Here is the synthesis deliverable.\n\n---\n\n# HOTEL FORGE ROADMAP - finalize the ERP/business slot\n\n**Synthesis basis:** Only the ADVERSARIAL lens was populated; ROI/DEPS/RISK were empty. I treated the adversarial verdict as the spine and **re-verified every load-bearing claim against live code** (citations below) rather than trusting the consolidated gather lists. Result: the gather roadmap was ~60% false gaps. This roadmap is the trimmed, dependency-ordered truth.\n\n**Ground-truth verified this session (file:line):**\n- `routes/business.ts` EXISTS (5022 B), mounted `routes/index.ts:144` `/api/v1/business` with `verifyToken` + `isBusinessActionAllowed` deny-by-default allowlist (`business.ts:34-35,62,77`). **The \"404 dispatch route\" gap is FALSE.**\n- `routes/hotel-portal.ts` EXISTS (9918 B), mounted `index.ts:136`. `routes/realtime.ts` EXISTS, mounted `index.ts:179`.\n- `businessDispatcher.ts:2024` wires `engine.remitLiability(params.amount, params.date)` — **payroll-filing gap is FALSE.**\n- `engines/business/` has **0 files** with sqlite/WAL/Database — persistence gap is **REAL but narrow**.\n- `CustomerPortalEngine.ts:153-156` holds 4 in-memory `Map`s (tokens, messages, qualityDocs, serviceCases) — **in-memory portal state is REAL.**\n- `business.ts:27-29` self-documents: hotel WRITE (`handoff_counterparty_respond`) + ~120 financial-write actions remain `403` \"pending per-action security review\" — **write-path gap is REAL.**\n- `QuoteToShipOrchestratorEngine.ts:1471` sets `ctx.features = resolved?.features ?? resolved ?? []` — when `resolved` is a non-array object lacking `.features`, downstream DFM receives a non-iterable. **Q2S contract bug is REAL but lives in a charlie/quoting-owned engine, not a hotel engine.**\n\n---\n\n## 1. DEFINITION OF DONE\n\n\"Finalized hotel\" means the ERP/business slot is a **trustworthy, durable, write-capable back end behind an authenticated HTTP surface** — concretely, ALL of:\n\n1. **Integrity** — every allowlisted `prism_business` action returns *real engine output*, not a dispatcher placeholder. A standing regression guard proves it and catches re-introduction of the 341-false-wire bug class.\n2. **Durability** — no business state that must survive a restart lives only in a process `Map`. The 4 `CustomerPortalEngine` Maps (and any peer in-memory surfaces) persist and pass a kill-restart-readback test. (Scoped to what is *genuinely* in-memory; do NOT re-persist what juliett's 33 stores already own.)\n3. **Write front door** — a manager can perform at least the core write actions (PTO approve, PO cut, counterparty respond) through the portal behind per-action auth; they are no longer blanket-`403`.\n4. **Constants honesty** — every tax/PTO/CoA constant the engines depend on is confirmed present in `src/data/` (audit, not assumed-missing extraction).\n5. **No false debt** — the Q2S DFM contract bug is either fixed or filed to its real owner (charlie/quoting) on the chat bus; mobile/catalog/slot-migrate items are explicitly out-of-scope and tagged.\n\nDoD **excludes** (adversarial trim): mobile-engine wiring (no consumer app — `WIRE-EXEMPT` tag instead), Docustrata catalog rebuild (shipped: JM-DOC-POPULATION-MS0), slot-worktree migration (hygiene), and any \"rebuild the live route\" work.\n\n---\n\n## 2. PHASED UNITS (dependency order)\n\n### Phase 0 — Unblock + dedup-verify (prove what's already done)\n\n| U-id | WHAT | WHY | DEPENDS-ON | MODE | WIRE/TEST/VALIDATE | ACCEPTANCE |\n|---|---|---|---|---|---|---|\n| **U-HOTEL-P0-VERIFY-SHIPPED** | Confirm-and-close the 6 false gaps: business route, hotel-portal route, realtime route, payroll filing wiring, DocuStrata population, JM data fill. Post a \"verified-shipped, no build\" note to chat bus + close-out ledger. | Stops the fleet from re-building ~6 of the \"10 blockers\" that are live code. R8 read-before-write. | none | **VERIFY-FIRST** | VALIDATE: curl `/api/v1/business/dispatch` with an allowlisted read action → real JSON, not 404/placeholder. Confirm `index.ts:136,144,179` mounts. | All 6 confirmed live; close-out entry written; zero build performed. |\n| **U-HOTEL-P0-CONSTANTS-AUDIT** | Grep `src/data/` for tax/PTO/CoA/payroll constants the business engines import. Produce a confirmed-missing list (likely near-empty). | The `(verify)` tags in CLAUDE.md are NOT evidence of absence. Gatherer-2 admitted it never read these. | none | **VERIFY-FIRST** | VALIDATE: for each engine import of a constants file, prove the file exists + exports the symbol. | Audit report lists only *confirmed-missing* files (0 or a tiny set). No speculative extraction. |\n\n### Phase 1 — Durable foundation (persistence + auth front door)\n\n| U-id | WHAT | WHY | DEPENDS-ON | MODE | WIRE/TEST/VALIDATE | ACCEPTANCE |\n|---|---|---|---|---|---|---|\n| **U-HOTEL-PORTAL-PERSISTENCE** | Persist the genuinely-in-memory portal surfaces — `CustomerPortalEngine.ts:153-156` (tokens, messages, qualityDocs, serviceCases) + any peer in-memory Maps found in P0 — via juliett's SQLite-WAL store pattern. | Verified REAL: `engines/business/` has 0 persistence; portal state dies on restart → lost PTO drafts, NCRs, sessions. | U-HOTEL-P0-VERIFY-SHIPPED (scope-gate against juliett stores) | **BUILD (scope-gated)** | WIRE: route writes through juliett's WAL store, not a new bespoke DB. TEST: kill-restart-readback E2E. VALIDATE: write a PTO draft → kill process → restart → read it back live. | Kill-restart-readback green for all 4 Maps; `git grep \"new Map\" engines/business` for *durable* state returns 0 (transient caches exempt + tagged). NO duplication of juliett-owned stores. |\n| **U-HOTEL-ALLOWLIST-WRITE-REVIEW** | Per-action security review of the `403` write set (`business.ts:27-29`): `handoff_counterparty_respond` + the core manager-write actions (PTO approve, PO cut). Add reviewed actions to `business-dispatch-allowlist.ts` behind per-action auth (`verifyToken` + role check). | Verified REAL: today the portal is read-only through the front door; an ERP a manager can't *write* through isn't finalized. The real \"no front door for writes\" gap. | U-HOTEL-PORTAL-PERSISTENCE (writes must be durable before exposed) | **BUILD** | WIRE: extend allowlist + role-gate middleware. TEST: authed manager role → write succeeds + persists; unauthed/wrong-role → 403. VALIDATE: round-trip a real PTO approve through `/api/v1/business/dispatch`. | Core manager-write actions reachable behind auth; financial-write actions still gated until individually reviewed (documented, not blanket-opened — financial-invariant safety). |\n\n### Phase 2 — Close the real ERP integrity gap\n\n| U-id | WHAT | WHY | DEPENDS-ON | MODE | WIRE/TEST/VALIDATE | ACCEPTANCE |\n|---|---|---|---|---|---|---|\n| **U-HOTEL-FALSE-WIRE-REGRESSION-GUARD** | Standing test: every allowlisted `prism_business` action, round-tripped through `/api/v1/business/dispatch`, returns real engine output — NOT a placeholder/echo. | The BUSINESS-CLEANUP arc (`701210abf2`, `919e40e395`, `c9874f0623`) fixed **341 false-wires**. Zero standing guard exists → silent regression risk. This is the bug-class hotel fought all month; higher ROI than any feature. | U-HOTEL-P0-VERIFY-SHIPPED | **BUILD** | TEST: parametric over the allowlist; assert response shape ≠ known placeholder signatures + ≥1...[truncated]
```

## Consensus answer

```
The final answer: The dynamic workflow has been completed, resulting in a dependency-ordered roadmap to finalize the hotel ERP/business slot by verifying facts, addressing gaps, and ensuring system integrity, durability, and write capabilities.
```

## Per-model responses

### gpt-5.5 (openai) — error

- latency: 4ms · tokens: n/a
- error: `process error: spawn codex ENOENT`

```
(no answer — process error: spawn codex ENOENT)
```

### qwen2.5-coder:32b (ollama) — ok

- latency: 2465ms · tokens: n/a

```
The final answer: The dynamic workflow has been completed, resulting in a dependency-ordered roadmap to finalize the hotel ERP/business slot by verifying facts, addressing gaps, and ensuring system integrity, durability, and write capabilities.
```

## Backlinks

- task_type: [[task-type-auto-userprompt]]
- recommendation: [[consensus-review]]
- voter: [[model-qwen2-5-coder-32b]]
