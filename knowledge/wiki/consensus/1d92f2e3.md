---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 1d92f2e35846c00649b88e46c98e5adad8b5f7aca894585e44ff9e94595ba77f
sha8: 1d92f2e3
ts: 2026-06-24T14:29:28.521Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.053
success_count: 2
total_latency_ms: 41018
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `1d92f2e3`

**Recommendation:** `escalate` · **Agreement:** `0.053` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\resourceExtractionDispatcher.ts via Edit

OLD:
            const ex = params.extraction ?? params.extractedData ?? params.result ?? params.office;
            if (ex == null || typeof ex !== "object") {
              return { error: "document_extract_contract requires extraction (an OfficeDocumentPipelineEngine ExtractionResult or its extractedData object)" };
            }
            const mod = await import("../../schemas/DocumentExtractionContract.js");
            const opts: any = {};
            if (typeof params.confirmFloor === "number") opts.confirmFloor = params.confirmFloor;
            if (typeof params.entryConfidence === "number") opts.entryConfidence = params.entryConfidence;
            if (typeof params.source === "string") opts.source = params.source;
            if (typeof params.docType === "string") opts.docType =

NEW:
            const ex = params.extraction ?? params.extractedData ?? params.result ?? params.office ?? params.ocr;
            if (ex == null || typeof ex !== "object") {
              return { error: "document_extract_contract requires extraction (an OfficeDocumentPipelineEngine ExtractionResult OR an ImageOCRPipelineEngine OCRResult, or its extractedData object)" };
            }
            const mod = await import("../../schemas/DocumentExtractionContract.js");
            const opts: any = {};
            if (typeof params.confirmFloor === "number") opts.confirmFloor = params.confirmFloor;
            if (typeof params.entryConfidence === "number") opts.entryConfidence = params.entryConfidence;
            if (typeof params.source === "string") opts.source = params.source;
            
```

## Consensus answer

```
Final Answer: The changes are appropriate and align with the described updates in the system's capabilities, ensuring that both document extraction and OCR results are considered valid inputs.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 29253ms · tokens: n/a

```
Final Answer: The changes are appropriate and align with the described updates in the system's capabilities, ensuring that both document extraction and OCR results are considered valid inputs.
```

### gpt-oss:20b (ollama) — ok

- latency: 8069ms · tokens: n/a

```
**Critical‑file edit review**

- **Added `params.ocr` to the extraction lookup** – this expands the accepted input types, but make sure that downstream code can handle an OCRResult (e.g., property names, shape). If it currently expects only OfficeDocumentPipelineEngine data, you’ll need to add type guards or adjust the logic that consumes `ex`.

- **Updated error message** – now correctly references both extraction and OCR. Ensure any callers that parse this string for UI or logs are updated accordingly.

- **Syntax check** – the snippet ends abruptly after `opts.source = params.source;`. Verify that the rest of the file (especially the missing `if (typeof params.docType === "string") opts.docType = …` block) is intact and that all braces/parentheses close properly. A missing closing brace could break compilation.

- **Import path** – unchanged (`../../schemas/DocumentExtractionContract.js`). No issue here unless the file structure changed.

- **Guarded critical‑file** – this change is low risk, but double‑check that no other critical modules rely on `ex` being strictly an Office extraction result. If they do, add a runtime check or adjust those modules accordingly.

**Recommendation**

1. Run unit tests covering both extraction and OCR paths to confirm downstream handling.
2. Verify that any UI/error‑handling code consuming the error string is updated for the new wording.
3. Ensure the file compiles with all braces/parentheses balanced after this change.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
