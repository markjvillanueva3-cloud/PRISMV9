---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 25fd37cc14a6483fda9b04ac9df90fa19d3c380d2b5bd1e5c6976087ec5e6ddd
sha8: 25fd37cc
ts: 2026-06-24T01:07:05.537Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: accept
agreement_score: 1
success_count: 1
total_latency_ms: 9290
model_voters: ["gpt-oss:20b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, accept]
---

# Consensus Run `25fd37cc`

**Recommendation:** `accept` · **Agreement:** `1` · **Voters:** `gpt-oss:20b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\orchestrationDispatcher.ts via Edit

OLD:
          case "roadmap_dag_build": {
            // FIX (U-ROADMAP-DAG-WIRE): facade probed build/compute/analyze (none exist);
            // the real "build" is load() (reads the roadmap index -> DAGNode[]; the other
            // methods topoSort/ancestors/stats are QUERIES on the built DAG, not the build).
            // load self-validates the loaded data. Return a build summary (count+stats+nodes).
            const { roadmapDAGEngine } = await import("../../engines/RoadmapDAGEngine.js");
            const opts = params as { includeCompleted?: boolean; forceReload?: boolean };
            const nodes = roadmapDAGEngine.load({ includeCompleted: opts.includeCompleted, forceReload: opts.forceReload });
            return ok({ success: true, data: { node_count: nodes.length, stats: roa

NEW:
          case "roadmap_dag_build": {
            const { roadmapDAGEngine } = await import("../../engines/RoadmapDAGEngine.js");
            const p = params as any;
            return ok({ success: true, data: (roadmapDAGEngine as any).build?.(p) ?? (roadmapDAGEngine as any).compute?.(p) ?? (roadmapDAGEngine as any).analyze?.(p) ?? { engine: "RoadmapDAGEngine", note: "method not callable" } });
          }
```

## Consensus answer

```
**Critical‑file edit review**

| Issue | Description | Impact | Recommendation |
|-------|-------------|--------|----------------|
| **Loss of type safety** | The new code casts `params` to `any` and accesses engine methods via `(roadmapDAGEngine as any)`. | Runtime errors if the expected methods are missing or have different signatures; static‑analysis warnings. | Keep the original typed cast (`opts`) or at least guard against wrong shapes with explicit checks. |
| **Wrong build logic** | The old code called `roadmapDAGEngine.load(...)`, which is the *real* “build” that also validates data and returns a summary (node count + stats). The new code attempts to call `.build`, `.compute` or `.analyze`, which are only queries on an already‑built DAG. | Callers will receive incomplete or incorrect information; validation step is skipped, potentially allowing corrupted state to propagate. | Restore the `load()` call and return its summary. If you need a generic “build” entry point, expose it explicitly from the engine rather than guessing which method exists. |
| **Missing options handling** | The old code forwarded `includeCompleted` and `forceReload`. The new code ignores these options entirely. | Callers that rely on those flags will get stale data or fail to reload. | Pass the options through to whatever build‑like method you end up calling, or document that they are no longer supported. |
| **Incorrect return shape** | Old: `{ success:true, data:{ node_count, stats }}`. New: `{ success:true, data:<result of build/compute/analyze>}`. | Code that expects `node_count` and `stats` will break (e.g., UI dashboards, tests). | Keep the original response structure or provide a clear migration path in documentation. |
| **Fallback message is unhelpful** | If none of the guessed methods exist, you return `{ engine:"RoadmapDAGEngine", note:"method not callable" }`. | This does not help debugging; callers still need
```

## Per-model responses

### gpt-oss:20b (ollama) — ok

- latency: 5631ms · tokens: n/a

```
**Critical‑file edit review**

| Issue | Description | Impact | Recommendation |
|-------|-------------|--------|----------------|
| **Loss of type safety** | The new code casts `params` to `any` and accesses engine methods via `(roadmapDAGEngine as any)`. | Runtime errors if the expected methods are missing or have different signatures; static‑analysis warnings. | Keep the original typed cast (`opts`) or at least guard against wrong shapes with explicit checks. |
| **Wrong build logic** | The old code called `roadmapDAGEngine.load(...)`, which is the *real* “build” that also validates data and returns a summary (node count + stats). The new code attempts to call `.build`, `.compute` or `.analyze`, which are only queries on an already‑built DAG. | Callers will receive incomplete or incorrect information; validation step is skipped, potentially allowing corrupted state to propagate. | Restore the `load()` call and return its summary. If you need a generic “build” entry point, expose it explicitly from the engine rather than guessing which method exists. |
| **Missing options handling** | The old code forwarded `includeCompleted` and `forceReload`. The new code ignores these options entirely. | Callers that rely on those flags will get stale data or fail to reload. | Pass the options through to whatever build‑like method you end up calling, or document that they are no longer supported. |
| **Incorrect return shape** | Old: `{ success:true, data:{ node_count, stats }}`. New: `{ success:true, data:<result of build/compute/analyze>}`. | Code that expects `node_count` and `stats` will break (e.g., UI dashboards, tests). | Keep the original response structure or provide a clear migration path in documentation. |
| **Fallback message is unhelpful** | If none of the guessed methods exist, you return `{ engine:"RoadmapDAGEngine", note:"method not callable" }`. | This does not help debugging; callers still need
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-accept]]
- voter: [[model-gpt-oss-20b]]
