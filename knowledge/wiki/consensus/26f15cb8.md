---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 26f15cb853e20183685674db3d0f183779e106bb6d6bcb4f68770a1dee689edc
sha8: 26f15cb8
ts: 2026-06-24T19:51:01.674Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.118
success_count: 2
total_latency_ms: 10352
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `26f15cb8`

**Recommendation:** `escalate` · **Agreement:** `0.118` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\resourceExtractionDispatcher.ts via Edit

OLD:
            // producer selects the normalizer: "ocr" (ImageOCRPipelineEngine) vs "office" (default).
            const producer = params.producer === "ocr" || params.ocr != null ? "ocr" : "office";
            const contract = producer === "ocr" ? mod.normalizeOcrExtractToContract(ex, opts) : mod.normalizeOfficeExtractToContract(ex, opts);

NEW:
            // producer selects the normalizer: "ocr" (ImageOCRPipelineEngine), "doclearn"
            // (documentLearning IngestionResult), vs "office" (default).
            const producer = params.producer === "ocr" || params.ocr != null ? "ocr"
              : params.producer === "doclearn" || params.ingestion != null ? "doclearn"
              : "office";
            const contract = producer === "ocr" ? mod.normalizeOcrExtractToContract(ex, opts)
              : producer === "doclearn" ? mod.normalizeDocLearningToContract(ex, opts)
              : mod.normalizeOfficeExtractToContract(ex, opts);
```

## Consensus answer

```
**Answer:** The edit is correct and properly handles the addition of the "doclearn" producer type, updating both the selection logic and the associated contract normalization function calls.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 2797ms · tokens: n/a

```
**Answer:** The edit is correct and properly handles the addition of the "doclearn" producer type, updating both the selection logic and the associated contract normalization function calls.
```

### gpt-oss:20b (ollama) — ok

- latency: 2623ms · tokens: n/a

```
The change correctly extends the dispatcher to handle a new “doclearn” producer:

- Adds a third branch in the `producer` selection logic.
- Uses `mod.normalizeDocLearningToContract` for the contract when `producer === "doclearn"`.

**Things to verify:**

1. **Function existence:** Ensure that `normalizeDocLearningToContract` is exported from `mod` and imported where this file resides.
2. **Parameter naming:** Confirm that `params.ingestion` (or whatever flag indicates doc‑learning) is the intended property; otherwise adjust accordingly.
3. **Linting/formatting:** The nested ternary is syntactically correct, but double‑check spacing for consistency with the project style.

No other critical-file changes are required.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
