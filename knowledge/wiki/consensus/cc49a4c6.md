---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: cc49a4c68957366a4869ac9fba97ad522928161625532c32645cbfb886b300d1
sha8: cc49a4c6
ts: 2026-06-25T03:32:47.665Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.128
success_count: 2
total_latency_ms: 23874
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `cc49a4c6`

**Recommendation:** `escalate` · **Agreement:** `0.128` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\sessionDispatcher.ts via Edit

OLD:
  "node_card",
  // CHEAP-NODE-ACCESS-MS0/U-VBL-DISPATCHER (sierra 2026-06-09): REVERSE of node_card — given a vault doc (wiki path or memory slug) list the graph node(s) that document it, via the single-source CLI `doc-nodes` over vault-backlinks.json (no 644MB graph load). params.doc (string) + aliases query/q/key/path/slug.
  "doc_nodes",

NEW:
  "node_card",
  // SYSTEM-VIZ/U-VIZ-NEAR (sierra 2026-06-25): semantic nearest-neighbor node search -- top-K by 768d cosine via the single-source CLI `near` subcommand (streams the embedding pool, no 884MB graph load). params.id (string) + params.k (optional int, default 10).
  "node_near",
  // CHEAP-NODE-ACCESS-MS0/U-VBL-DISPATCHER (sierra 2026-06-09): REVERSE of node_card — given a vault doc (wiki path or memory slug) list the graph node(s) that document it, via the single-source CLI `doc-nodes` over vault-backlinks.json (no 644MB graph load). params.doc (string) + aliases query/q/key/path/slug.
  "doc_nodes",
```

## Consensus answer

```
**Answer:** The edit adds a new critical file entry for `node_near`, which is a semantic nearest-neighbor node search function, while keeping the existing `doc_nodes` function intact.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 5820ms · tokens: n/a

```
**Answer:** The edit adds a new critical file entry for `node_near`, which is a semantic nearest-neighbor node search function, while keeping the existing `doc_nodes` function intact.
```

### gpt-oss:20b (ollama) — ok

- latency: 13223ms · tokens: n/a

```
**Review of `session.ts` edit**

- **New entry added correctly**  
  ```ts
  "node_near",
  // SYSTEM-VIZ/U-VIZ-NEAR (sierra 2026‑06‑25): semantic nearest‑neighbor node search -- top‑K by 768d cosine via the single‑source CLI `near` subcommand (streams the embedding pool, no 884MB graph load). params.id (string) + params.k (optional int, default 10).
  ```
  The comment is clear and follows the same style as existing entries. No syntax errors.

- **Existing entries preserved**  
  - `"node_card"` remains unchanged.  
  - `"doc_nodes"` keeps its original comment and alias list (`query/q/key/path/slug`).

- **Comma placement**  
  Each string literal ends with a comma, matching the surrounding array/list. Ensure that if `doc_nodes` is not the last element in the array, it also has a trailing comma.

- **No duplicate keys** – all identifiers are unique.

Overall, the edit is syntactically correct and semantically consistent with the rest of the file. No further changes needed.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
