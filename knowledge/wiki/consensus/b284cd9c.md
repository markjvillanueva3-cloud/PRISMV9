---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: b284cd9c35647ccaf9ee317e137ebf57a24acf72d8f3ad6d1dc0a36b1032997a
sha8: b284cd9c
ts: 2026-06-24T00:47:00.332Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.068
success_count: 2
total_latency_ms: 13537
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `b284cd9c`

**Recommendation:** `escalate` · **Agreement:** `0.068` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\orchestrationDispatcher.ts via Edit

OLD:
          case "wet_run_pilot_orchestrate": {
            const { wetRunPilotOrchestratorEngine } = await import("../../engines/WetRunPilotOrchestratorEngine.js");
            const p = params as any;
            return ok({ success: true, data: await ((wetRunPilotOrchestratorEngine as any).run?.(p) ?? (wetRunPilotOrchestratorEngine as any).orchestrate?.(p) ?? (wetRunPilotOrchestratorEngine as any).execute?.(p)) ?? { engine: "WetRunPilotOrchestratorEngine", note: "method not callable" } });
          }

NEW:
          case "wet_run_pilot_orchestrate": {
            // FIX (U-WETRUN-PILOT-WIRE): facade probed run/orchestrate/execute (none
            // exist); the real method is pilotPromotionReadiness(pilotId, nowTs) --
            // POSITIONAL args, self-validates (pilot_id non-empty string + nowTs finite),
            // so no schema is needed. Default nowTs to the current time when omitted
            // (the gate answers "is this pilot ready as of now").
            const { wetRunPilotOrchestratorEngine } = await import("../../engines/WetRunPilotOrchestratorEngine.js");
            const p = params as { pilot_id?: string; now_ts?: number };
            const nowTs = typeof p.now_ts === "number" ? p.now_ts : Date.now();
            return ok({ success: true, data: wetRunPilotOrchestratorE
```

## Consensus answer

```
The critical-file edit review for `H:\prism\mcp-server\src\tools\dispatchers\orchestrationDispatcher.ts` via Edit appears to be a valid fix, updating the method call and handling default values correctly.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 2723ms · tokens: n/a

```
The critical-file edit review for `H:\prism\mcp-server\src\tools\dispatchers\orchestrationDispatcher.ts` via Edit appears to be a valid fix, updating the method call and handling default values correctly.
```

### gpt-oss:20b (ollama) — ok

- latency: 6293ms · tokens: n/a

```
**Quick review**

| Issue | Comment |
|-------|---------|
| **Missing `await`** | The engine method is likely async (`pilotPromotionReadiness`). Wrap the call in `await`. |
| **Incomplete return statement** | The snippet ends at `data: wetRunPilotOrchestratorE…`. It should be something like: <br>`data: await wetRunPilotOrchestratorEngine.pilotPromotionReadiness(pilot_id, nowTs)` |
| **Parameter naming consistency** | You use `now_ts` in the type but later refer to it as `nowTs`. Keep one style (e.g., `now_ts`) or rename consistently. |
| **Default timestamp unit** | `Date.now()` returns milliseconds. Verify that `pilotPromotionReadiness` expects ms; if it expects seconds, divide by 1000. |
| **Optional `pilot_id` handling** | The engine’s self‑validation will reject an empty string. If you want to allow omission, consider passing `undefined` explicitly or letting the validation throw – just document that behavior. |
| **Type safety** | Instead of casting `params as any`, destructure: <br>`const { pilot_id, now_ts } = params as { pilot_id?: string; now_ts?: number };` |
| **Comment clarity** | The comment is fine but could be shortened: “Use the real method `pilotPromotionReadiness`; it validates inputs.” |

**Suggested minimal patch**

```ts
case "wet_run_pilot_orchestrate": {
  // Use the real engine method – it self‑validates pilot_id and nowTs.
  const { wetRunPilotOrchestratorEngine } = await import(
    "../../engines/WetRunPilotOrchestratorEngine.js"
  );
  const { pilot_id, now_ts } = params as {
    pilot_id?: string;
    now_ts?: number;
  };
  const nowTs =
    typeof now_ts === "number" ? now_ts : Date.now(); // adjust if seconds needed
  return ok({
    success: true,
    data: await wetRunPilotOrchestratorEngine.pilotPromotionReadiness(
      pilot_id,
      nowTs
    ),
  });
}
```

This addresses the missing `await`, completes the return, and keeps the logic clear.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
