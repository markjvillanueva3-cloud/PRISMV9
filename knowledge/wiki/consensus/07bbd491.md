---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 07bbd491d52da815d95488619ec9876b65a7784e123370272ec82b113724a2b0
sha8: 07bbd491
ts: 2026-06-24T14:18:48.164Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.054
success_count: 2
total_latency_ms: 13329
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `07bbd491`

**Recommendation:** `escalate` · **Agreement:** `0.054` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\resourceExtractionDispatcher.ts via Edit

OLD:
            } else {
              return { error: "keyword or partNumber is required for office_search" };
            }
            break;
          }

          // ── Machine Log Actions ───────────────────────────────

NEW:
            } else {
              return { error: "keyword or partNumber is required for office_search" };
            }
            break;
          }

          case "document_extract_contract": {
            // U-XRAY-DOCUMENT-EXTRACT-CONTRACT -- normalize an office/document extraction into the
            // versioned DocumentExtractionContract the document-feature consumers bind to (sibling of
            // blueprint_extract_contract). Pure + in-process: the caller obtains the extraction via
            // office_process first, then calls this to get the stable contract. No producer run, no I/O.
            const ex = params.extraction ?? params.extractedData ?? params.result ?? params.office;
            if (ex == null || typeof ex !== "object") {
              return { error: "doc
```

## Consensus answer

```
Final answer: The edit appears to be a safe addition of new functionality for handling document extraction contracts, with appropriate error checking.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 2087ms · tokens: n/a

```
Final answer: The edit appears to be a safe addition of new functionality for handling document extraction contracts, with appropriate error checking.
```

### gpt-oss:20b (ollama) — ok

- latency: 6706ms · tokens: n/a

```
**Critical‑file review – `resourceExtractionDispatcher.ts`**

| Issue | Explanation | Suggested fix |
|-------|-------------|---------------|
| **Missing closing brace for the `case "document_extract_contract"` block** | The snippet ends with an opening `{` after the case label but we don’t see a matching `}`. If it’s omitted, the switch will be syntactically broken. | Add a `}` right before the next `break;` (or at the end of the file if this is the last case). |
| **No explicit `break;` after the block** | Although all paths return, TypeScript/JS will still fall through to the next case unless you exit the function. If the code after the block doesn’t `return`, execution would continue into the following case. | Keep the `return` statements inside the block. If there’s any path that could reach the end of the block without returning, add a `break;`. |
| **Potential type mismatch for `params`** | The new code accesses `params.extraction`, `params.extractedData`, `params.result`, and `params.office`. Ensure these properties exist on the `Params` interface used by this dispatcher. | Extend or update the `Params` type (or create a new one) to include those fields, e.g.:

```ts
interface ExtractionParams {
  extraction?: unknown;
  extractedData?: unknown;
  result?: unknown;
  office?: unknown;
}
```

and use it in the function signature. |
| **Missing import for `DocumentExtractionContract`** | The comment mentions “normalize … into the versioned DocumentExtractionContract”. If you return that type later, make sure it’s imported. | Add:

```ts
import { DocumentExtractionContract } from "./contracts/DocumentExtractionContract";
```

or wherever it lives. |
| **`typeof ex !== "object"` also matches `null`** | You already guard against `ex == null`, so this is fine, but you might prefer `!ex || typeof ex !== "object"`. | No change needed unless you want a more concise check. |
| **Comment style consistency** | The comment uses a mix of single‑line and multi‑line styles. Ensure it doesn’t break any linter rules (e.g., no trailing spaces, max line length). | Keep the comments as is or adjust to your project’s linting guidelines. |

### Quick sanity check

```ts
switch (action) {
  ...
  case "
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
