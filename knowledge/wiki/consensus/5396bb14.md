---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 5396bb14cc72b200b921d1373107562067b00d75b548a226188aeaa3413243dd
sha8: 5396bb14
ts: 2026-06-24T14:51:41.110Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.111
success_count: 2
total_latency_ms: 19165
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `5396bb14`

**Recommendation:** `escalate` · **Agreement:** `0.111` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\resourceExtractionDispatcher.ts via Edit

OLD:
          case "ocr_stats": {
            const engine = await getEngine("ocr");
            result = engine.getStatistics();
            break;
          }

NEW:
          case "ocr_stats": {
            const engine = await getEngine("ocr");
            result = engine.getQueueStats();
            break;
          }
```

## Consensus answer

```
Final answer: The edit changes the method call from `getStatistics()` to `getQueueStats()`, which should be reviewed for correctness and impact on system behavior.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 9758ms · tokens: n/a

```
Final answer: The edit changes the method call from `getStatistics()` to `getQueueStats()`, which should be reviewed for correctness and impact on system behavior.
```

### gpt-oss:20b (ollama) — ok

- latency: 6083ms · tokens: n/a

```
**Review Summary**

The change simply swaps the call from `engine.getStatistics()` to `engine.getQueueStats()`.  
Key points to verify:

| Item | Check |
|------|-------|
| **Method existence** | Confirm that `getQueueStats` is defined on the OCR engine returned by `getEngine("ocr")`. |
| **Return type compatibility** | Ensure the value returned matches what callers of `"ocr_stats"` expect (e.g., same shape as the old statistics object). |
| **Async behavior** | If `getQueueStats()` is asynchronous, add `await` (`result = await engine.getQueueStats();`). |
| **Side‑effects / caching** | Verify that calling the new method does not alter internal state in an unintended way (e.g., it shouldn't clear a queue). |
| **Documentation & tests** | Update any docs or unit tests that reference `getStatistics` to reflect the new method name. |

If all of the above hold, the edit is safe and improves clarity by using the more appropriate “queue stats” terminology. No other code changes appear necessary.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
