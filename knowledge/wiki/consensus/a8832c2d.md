---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: a8832c2d30219e451a4adf67fb4b02d57596894c213794ec9119b9b380e0903c
sha8: a8832c2d
ts: 2026-06-24T14:38:46.116Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: review
agreement_score: 0.5
success_count: 1
total_latency_ms: 102487
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, review]
---

# Consensus Run `a8832c2d`

**Recommendation:** `review` · **Agreement:** `0.5` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\cadDispatcher.ts via Edit

OLD:
            const routerMod = await import("../../engines/blueprint-vision/blueprintExtractionRouter.js");
            const rOpts = params.includeIneligible === false ? { includeIneligible: false } : {};
            const plan = routerMod.routeExtractionToConsumers(validation.data!, rOpts);
            result = { success: true, data: { plan } };
            break;
          }
          case "blueprint_coverage_audit": {

NEW:
            const routerMod = await import("../../engines/blueprint-vision/blueprintExtractionRouter.js");
            const rOpts = params.includeIneligible === false ? { includeIneligible: false } : {};
            const plan = routerMod.routeExtractionToConsumers(validation.data!, rOpts);
            result = { success: true, data: { plan } };
            break;
          }
          case "blueprint_extract_and_route": {
            // U-XRAY-EXTRACT-AND-ROUTE -- one-call convenience composing blueprint_extract_contract +
            // blueprint_extract_route: a producer extraction (VLM `fused` OR `drawing`) -> the versioned
            // contract -> the confirm-gated fan-out plan, in a SINGLE dispatcher call (the app's
            // upload->extract->route flow doesn't have to chain 
```

## Consensus answer

```
The edit appears to be a deliberate enhancement, adding functionality and documentation for a new feature. No critical issues are apparent from the provided context.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 7473ms · tokens: n/a

```
The edit appears to be a deliberate enhancement, adding functionality and documentation for a new feature. No critical issues are apparent from the provided context.
```

### gpt-oss:20b (ollama) — error

- latency: 90005ms · tokens: n/a
- error: `timeout`

```
(no answer — timeout)
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-review]]
- voter: [[model-qwen2-5-coder-32b]]
