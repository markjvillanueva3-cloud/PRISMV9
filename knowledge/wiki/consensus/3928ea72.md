---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 3928ea72d87bd5ff60a256c885f5ce9dffd9c7562a3e139c36f2abfa1310ee66
sha8: 3928ea72
ts: 2026-06-24T15:12:23.630Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.117
success_count: 2
total_latency_ms: 9882
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `3928ea72`

**Recommendation:** `escalate` · **Agreement:** `0.117` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\resourceExtractionDispatcher.ts via Edit

OLD:
            result = engine.getSummary(filePath);
            if (!result) {
              return { error: "Drawing not found — extract it first" };
            }
            break;

NEW:
            result = engine.getResult(filePath);
            if (!result) {
              return { error: "Drawing not found -- extract it first" };
            }
            break;
```

## Consensus answer

```
**Review Needed**: Confirm if `engine.getResult(filePath)` is correct and verify the change does not introduce any functional issues.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 1741ms · tokens: n/a

```
**Review Needed**: Confirm if `engine.getResult(filePath)` is correct and verify the change does not introduce any functional issues.
```

### gpt-oss:20b (ollama) — ok

- latency: 4235ms · tokens: n/a

```
**Critical‑file review**

| Issue | Details |
|-------|---------|
| **Method name change** | The code switches from `engine.getSummary(filePath)` to `engine.getResult(filePath)`. Verify that `getResult` is a valid method on the engine and returns the same type/value expected by callers. If it does not exist or behaves differently, this will cause a compile‑time error or runtime logic bug. |
| **Error‑message formatting** | The original message used an em‑dash (`—`). It was replaced with two hyphens (`--`). This subtle change can break string‑comparison tests or documentation that relies on the exact wording. Confirm whether the new format is intentional and update any tests or consumers accordingly. |
| **EOL consistency** | No direct impact, but remember that PRISM will flip the file’s line endings to LF during commit. Ensure no stray CRLF characters remain after editing. |

**Recommendation**

1. **Confirm API** – Check the engine implementation for `getResult`. If it doesn’t exist or differs from `getSummary`, revert the change or update the call accordingly.
2. **Update tests/consumers** – Adjust any unit tests, error‑handling logic, or documentation that expects the original em‑dash message.
3. **Run a full build/test cycle** to catch any downstream effects of these changes.

If both points are validated, the edit can be accepted; otherwise, revert or correct as needed.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
