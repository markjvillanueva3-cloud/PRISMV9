---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 5a76b305da82fb13d0a4e13e1feb604d6ea641a764aab032b649fe03009af2b2
sha8: 5a76b305
ts: 2026-06-24T01:30:48.276Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.084
success_count: 2
total_latency_ms: 33322
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `5a76b305`

**Recommendation:** `escalate` · **Agreement:** `0.084` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\cadDispatcher.ts via Edit

OLD:
            if (hasRegions) {
              data.regions = redact.redactionRegions(params.regions);
            }
            result = { success: true, data };
            break;
          }
          case "blueprint_coverage_audit": {

NEW:
            if (hasRegions) {
              data.regions = redact.redactionRegions(params.regions);
            }
            result = { success: true, data };
            break;
          }
          case "blueprint_extract_contract": {
            // U-XRAY-EXTRACT-CONTRACT-WIRE -- make the tested BlueprintExtractionContract normalizers
            // reachable as an app surface. Given a PRE-OBTAINED producer extraction (the VLM ensemble
            // `fused` shape OR a Drawing2DExtractionEngine `drawing` result), return the versioned,
            // mm-canonical contract the app consumers bind to + a schema-validation verdict. Pure +
            // in-process: like blueprint_redact, the caller obtains the extraction via the producer
            // action first -- this action only NORMA
```

## Consensus answer

```
The new case "blueprint_extract_contract" should be reviewed by a critical-file guard process due to its significant functional change.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 18888ms · tokens: n/a

```
The new case "blueprint_extract_contract" should be reviewed by a critical-file guard process due to its significant functional change.
```

### gpt-oss:20b (ollama) — ok

- latency: 9510ms · tokens: n/a

```
**Critical‑file review – `cadDispatcher.ts`**

| Issue | Observation | Recommendation |
|-------|-------------|----------------|
| **Incomplete comment / code** | The new case block ends with “this action only NORMA” – the sentence is cut off and no closing brace or `break;` is shown. | Verify that the full implementation follows, including a proper closing `}` for the `case`, a `break;` (or `return`) statement, and any necessary logic after the comment. |
| **Syntax consistency** | The snippet mirrors the existing pattern (`if (…) { … } result = …; break;`). No obvious syntax errors are visible in the excerpt. | Ensure that the new case follows the same brace‑style as the rest of the file (single‑line `break` on its own line). |
| **Imports / references** | The comment mentions “BlueprintExtractionContract normalizers” but no imports are shown. | Confirm that any referenced functions or types (`blueprint_extract_contract`, etc.) are imported at the top of the file and that their names match the implementation. |
| **EOL handling** | PRISM’s build tools write CRLF, but repo files use LF → whole‑file EOL flip on commit. | Make sure the file ends with a single LF (no trailing CR) before committing. |
| **Critical‑file guard / consensus** | No violation of `critical-file-guard` or `auto-consensus-critical-edit` is apparent from this snippet. | Still run the repository’s automated checks (`mcp-server-reality-check`, etc.) after the change to catch any hidden issues. |

**Bottom line:** The added case appears syntactically sound, but the comment and missing closing parts need to be verified in the full file. Ensure all required imports are present, that the block ends with a `break;` (or appropriate return), and that the file’s EOLs remain LF before committing.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
