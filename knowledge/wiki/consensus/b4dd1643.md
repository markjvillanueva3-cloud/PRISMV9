---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: b4dd1643cd9b793b56c8f714e65831066ced079419018b821013d0c21dc01e76
sha8: b4dd1643
ts: 2026-06-17T13:30:10.520Z
task_type: auto-userprompt
source_session: dc3f020e-9a94-4310-adf2-a60d9533cfec
mode: compare
recommendation: escalate
agreement_score: 0
success_count: 2
total_latency_ms: 9375
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-userprompt, escalate]
---

# Consensus Run `b4dd1643`

**Recommendation:** `escalate` · **Agreement:** `0` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Sierra overnight autonomous vault-ops loop tick (operator asleep; slot:sierra, session intent claude-dc3f020e, worktree H:/prism-slot-sierra, commits to cad-fusion-live-ms0 via [MAIN-FORCE]). Degraded: prism MCP bridge down — use direct `node H:/prism/scripts/*.mjs` and set `PRISM_MCP_ENFORCE_DISABLE=1 PRISM_GIT_ADD_LANE_DISABLE=1` inline on every bash. Read state first: `node H:/prism/.claude/helpers/per-agent-handoff.mjs read --terminal claude-dc3f020e` and recall [[reference_sierra_vault_promote_gate_4class_2026_06_16]].

PRIMARY UNIT (R15 apply-to-all, verified real 2026-06-16): `scripts/promote-tribal-to-wiki.mjs` is a 2nd wiki-promotion path with ZERO junk filter; `scripts/promote-memory-to-wiki.mjs` has the 4-class `nonPromotableReason` (node-pointer / unverified-advisory / run-log / test-fixture). VERIFY-THEN-BUILD: (1) read promote-tribal-to-wiki.mjs end-to-end — what it promotes, its gate, and whether the real tribal corpus actually contains junk those classes catch (grep real tribal frontmatter for node_kind/run_log/deadbeef/advisoryOnly); add ONLY relevant filtering. (2) R8/dedup: extract nonPromotableReason → shared `scripts/lib/wiki-promotion-filter.mjs`; refactor promote-memory-to-wiki.mjs to import it (zero behavior change — re-run its 35/35 tests to prove); wire the tribal promoter to it. (3) Real reference-value tests for the lib + both consumers; live dry-run proof on both; per-file 2-arm scrutiny (code-analyzer + reviewer); fix every P0/P1. (4) Commit ONLY via `git commit <explicit file paths>` (NOT `git add` — the shared cad-fusion index gets peer-contaminated); verify `git show --stat HEAD` shows ONLY your files; if a peer file got swept, `git reset --soft HEAD~1` + recommit by-pathspec. Persist a reference_*.md memory + update the handoff (--source live-chat).

IF that unit is already shipped (check git log for a shared-filter commit): re-run `node scripts/promote-memory-to-wiki.mjs` dry-run — promote genuine new candidates with `--apply --backlink` to keep the vault compounding (freeze-safe; gate is clean); if a NEW junk class appears, harden the gate. Offload mechanical recon/summarize to ollama (`node scripts/ask-ollama.mjs <mode> <input>`). Stay strictly in-lane (vault-ops/system-viz); do NOT spiral (cap anomaly chases at 1 tick); SKIP the operator-gated cron arming (MIGRATION-FREEZE-ACTIVE.flag still present — do not delete it). If the committable build queue is genuinely exhausted (all candidates verified non-actionable), STOP HONESTLY (R12): report "queue exhausted, maintenance-only" rather than fabricate busy-work.
```

## Consensus answer

```
Proceed as outlined above.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 3841ms · tokens: n/a

```
Proceed as outlined above.
```

### gpt-oss:20b (ollama) — ok

- latency: 4721ms · tokens: n/a

```
**Action Plan (no actual script execution)**  

1. **Read current handoff state**  
   ```bash
   node H:/prism/.claude/helpers/per-agent-handoff.mjs read --terminal claude-dc3f020e
   ```
   – Capture the output for later reference.

2. **Check if `scripts/promote-tribal-to-wiki.mjs` is already shipped**  
   ```bash
   git log -1 --pretty=format:"%h %s" | grep "shared-filter"
   ```
   – If a commit containing the shared‑filter exists, skip to step 4.

3. **If not shipped → build & test**  

   *a.* Run end‑to‑end on `promote-tribal-to-wiki.mjs`  
   ```bash
   node scripts/promote-tribal-to-wiki.mjs --dry-run
   ```
   – Inspect what it promotes, its gate logic, and whether the real tribal corpus contains any of the junk classes (`node_kind/run_log/deadbeef/advisoryOnly`).  
   – Add filtering only for those classes that actually appear.

   *b.* Extract `nonPromotableReason` into shared lib  
   ```bash
   # create scripts/lib/wiki-promotion-filter.mjs
   ```
   – Refactor `promote-memory-to-wiki.mjs` to import the new lib.  
   – Run its 35/35 tests (`npm test promote-memory`) to confirm zero behavior change.

   *c.* Wire the tribal promoter to use the shared lib and run a dry‑run on both scripts.  
   – Verify `node scripts/promote-tribal-to-wiki.mjs --dry-run` and  
     `node scripts/promote-memory-to-wiki.mjs --dry-run`.

   *d.* Run reference‑value tests for the lib and both consumers.  
   – Ensure all P0/P1 issues are fixed.

4. **Commit only the changed files**  
   ```bash
   git commit <explicit file paths>
   ```
   – Verify `git show --stat HEAD` shows only those files.  
   – If any peer file was mistakenly staged, reset and recommit by‑pathspec.

5. **Persist reference markdown & update handoff**  
   - Create/append to `reference_*.md`.  
   - Update the handoff via:  
     ```bash
     node H:/prism/.claude/helpers/per-agent-handoff.mjs write --source live-chat
     ```

6. **If already shipped → dry‑run promotion**  
   ```bash
   node scripts/promote-memory-to-wiki.mjs --dry-run --apply --backlink
   ```
   – Promote only genuinely new candidates; keep vault compounding.  
   – If a new junk class appears, tighten the gate.

7. **Summarize mechanically**  
   - Run:  
     ```bash
     node scripts/ask-ollama.mjs summarize <output>
     ```

8. **Final check**  
   - Ensure `MIGRATION-FREEZE-ACTIVE.flag` remains untouched.  
   - If the queue is exhausted, log: “queue exhausted, maintenance‑only”.

*All actions are confined to the vault‑ops/system‑viz lane; no operator‑gated cron arming or flag deletion occurs.*
```

## Backlinks

- task_type: [[task-type-auto-userprompt]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
