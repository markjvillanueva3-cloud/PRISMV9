---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 8826b268f2d1def7910fae360e12ba8f9d09b720b61acd32ea94044cea395728
sha8: 8826b268
ts: 2026-06-25T22:18:16.547Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: review
agreement_score: 0.5
success_count: 1
total_latency_ms: 117087
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, review]
---

# Consensus Run `8826b268`

**Recommendation:** `review` · **Agreement:** `0.5` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\businessDispatcher.ts via Edit

OLD:
          // ── Lathe ERP Orchestrator (U-LTH57) ──
          case "lathe_erp_full": {
            const engine = await getEngine("latheERPOrchestrator");
            result = engine.erpFull(params as any);
            break;
          }

          // ── Lathe AGI Substrate (U-LTH58..U-LTH61) ──

NEW:
          // ── Lathe ERP Orchestrator (U-LTH57) ──
          case "lathe_erp_full": {
            const engine = await getEngine("latheERPOrchestrator");
            result = engine.erpFull(params as any);
            break;
          }

          // ── Value Stream Mapping (U-Q-VSM -- backs FE ValueStreamPage) ──
          case "value_stream_map": {
            const { valueStreamMapEngine } = await import("../../engines/ValueStreamMapEngine.js");
            result = valueStreamMapEngine.build({ job_id: (params as any).job_id });
            break;
          }

          // ── Lathe AGI Substrate (U-LTH58..U-LTH61) ──
```

## Consensus answer

```
**Answer:** The edit adds a new case for handling "value_stream_map" requests, which involves importing the `ValueStreamMapEngine` and using it to generate a value stream map with the provided job ID. This change does not affect existing functionality but extends the dispatcher's capabilities.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 23491ms · tokens: n/a

```
**Answer:** The edit adds a new case for handling "value_stream_map" requests, which involves importing the `ValueStreamMapEngine` and using it to generate a value stream map with the provided job ID. This change does not affect existing functionality but extends the dispatcher's capabilities.
```

### gpt-oss:20b (ollama) — error

- latency: 90011ms · tokens: n/a
- error: `timeout`

```
(no answer — timeout)
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-review]]
- voter: [[model-qwen2-5-coder-32b]]
