---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: ad4c537928b4a9466871ae7a8de7da96cb21a1f0b2824c5d99e4bac26696f124
sha8: ad4c5379
ts: 2026-06-23T00:40:41.795Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: accept
agreement_score: 1
success_count: 1
total_latency_ms: 6875
model_voters: ["gpt-oss:20b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, accept]
---

# Consensus Run `ad4c5379`

**Recommendation:** `accept` · **Agreement:** `1` · **Voters:** `gpt-oss:20b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\sessionDispatcher.ts via Edit

OLD:
          case "workflow_advance": {
            const { DurableWorkflowEngine } = await import("../../engines/DurableWorkflowEngine.js");
            const p = params as { prior: never; step_ids: string[] };

NEW:
          case "workflow_durable_advance": {
            const { DurableWorkflowEngine } = await import("../../engines/DurableWorkflowEngine.js");
            const p = params as { prior: never; step_ids: string[] };
```

## Consensus answer

```
**Review of `sessionDispatcher.ts` – case rename**

| Issue | Detail | Recommendation |
|-------|--------|----------------|
| **Action name mismatch** | The dispatcher now uses `"workflow_durable_advance"` instead of the previous `"workflow_advance"`. If any client code, tests, or other dispatchers still send/expect `"workflow_advance"`, they will fail. | Search the entire repo for `"workflow_advance"` and update all occurrences to `"workflow_durable_advance"`. Verify that unit/integration tests reflect the new name. |
| **Consistency with naming convention** | The new label follows the pattern of other durable‑workflow actions (e.g., `workflow_start`, `workflow_complete`). Ensure no other action names conflict or duplicate this string. | Run a quick grep for `"workflow_durable_advance"` to confirm uniqueness. |
| **Switch‑case termination** | The snippet ends with `{ ... }` but the surrounding switch logic isn’t shown. Make sure there’s a `break;` (or return) after the block so control doesn’t fall through to subsequent cases. | Verify that the closing brace is followed by `break;` or an explicit return. |
| **Import path** | The import remains unchanged (`../../engines/DurableWorkflowEngine.js`). No issues here. | None. |

**Action items**

1. Update all references from `"workflow_advance"` to `"workflow_durable_advance"`.  
2. Run tests and linting to catch any remaining mismatches.  
3. Confirm that the switch case ends correctly with a `break` or return.

If those steps are satisfied, the change should be safe.
```

## Per-model responses

### gpt-oss:20b (ollama) — ok

- latency: 4122ms · tokens: n/a

```
**Review of `sessionDispatcher.ts` – case rename**

| Issue | Detail | Recommendation |
|-------|--------|----------------|
| **Action name mismatch** | The dispatcher now uses `"workflow_durable_advance"` instead of the previous `"workflow_advance"`. If any client code, tests, or other dispatchers still send/expect `"workflow_advance"`, they will fail. | Search the entire repo for `"workflow_advance"` and update all occurrences to `"workflow_durable_advance"`. Verify that unit/integration tests reflect the new name. |
| **Consistency with naming convention** | The new label follows the pattern of other durable‑workflow actions (e.g., `workflow_start`, `workflow_complete`). Ensure no other action names conflict or duplicate this string. | Run a quick grep for `"workflow_durable_advance"` to confirm uniqueness. |
| **Switch‑case termination** | The snippet ends with `{ ... }` but the surrounding switch logic isn’t shown. Make sure there’s a `break;` (or return) after the block so control doesn’t fall through to subsequent cases. | Verify that the closing brace is followed by `break;` or an explicit return. |
| **Import path** | The import remains unchanged (`../../engines/DurableWorkflowEngine.js`). No issues here. | None. |

**Action items**

1. Update all references from `"workflow_advance"` to `"workflow_durable_advance"`.  
2. Run tests and linting to catch any remaining mismatches.  
3. Confirm that the switch case ends correctly with a `break` or return.

If those steps are satisfied, the change should be safe.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-accept]]
- voter: [[model-gpt-oss-20b]]
