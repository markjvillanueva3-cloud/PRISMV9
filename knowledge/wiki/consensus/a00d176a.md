---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: a00d176a37b7f6d7061b133b2d43ad0f8be2b775060ea8f97e060d3c1c1a30b5
sha8: a00d176a
ts: 2026-06-23T01:41:38.640Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.14
success_count: 2
total_latency_ms: 39736
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `a00d176a`

**Recommendation:** `escalate` · **Agreement:** `0.14` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\turningDispatcher.ts via Edit

OLD:
            const breakdown = jobCostingEngine.calculateJobCost(jobSpec);
            result = { ...breakdown, batch_size: batch };
            break;

NEW:
            const breakdown = jobCostingEngine.calculateJobCost(jobSpec);
            // The engine rounds perPart to 2 decimals (round2(total/qty)), so perPart*qty drifts
            // from total for large batches. Re-derive perPart at full precision off the engine's
            // authoritative total so the documented invariant total === perPart * batch_size holds
            // exactly at any batch size; echo batch_size (CostBreakdown omits it).
            result = { ...breakdown, perPart: batch > 0 ? breakdown.total / batch : breakdown.total, batch_size: batch };
            break;
```

## Consensus answer

```
**Answer:** The edit clarifies the logic for recalculating `perPart` to maintain precision and ensure the documented invariant holds, which is a valid and necessary change.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 15020ms · tokens: n/a

```
**Answer:** The edit clarifies the logic for recalculating `perPart` to maintain precision and ensure the documented invariant holds, which is a valid and necessary change.
```

### gpt-oss:20b (ollama) — ok

- latency: 21262ms · tokens: n/a

```
The change is acceptable. It preserves the original spread of `breakdown`, adds a precise recalculation of `perPart` (handling batch = 0 safely), and includes `batch_size`. No type or runtime errors are introduced, and the comment clarifies the intent. This edit passes the critical‑file guard.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
