---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 2cf32e84206c44dce6b30685a9a85a143dfd7513cb09810c9a7dda30942fe510
sha8: 2cf32e84
ts: 2026-06-23T02:36:56.269Z
task_type: auto-userprompt
source_session: 6a8a0fc5-4275-43b0-b847-449c590c706b
mode: compare
recommendation: accept
agreement_score: 1
success_count: 1
total_latency_ms: 7705
model_voters: ["gpt-oss:20b"]
mean_factuality: null
tags: [consensus, auto-userprompt, accept]
---

# Consensus Run `2cf32e84`

**Recommendation:** `accept` · **Agreement:** `1` · **Voters:** `gpt-oss:20b`

## Prompt

```
[ZULU AUTONOMOUS BUILD LOOP — operator-armed 2026-06-18: "continue building autonomously, harnessed loops/crons, utilize ollama+obsidian+hermes+octopus"] One build unit this iteration, full substrate ladder (cheapest rung first, Claude last):

1. PICK (never idle, hunt ladder): finish any in-flight unit → own-domain (hermes-zulu orchestration backlog: state/shared/specs/BRAVO-HERMES-ZULU-OPEN-TASKS-LEDGER.md) → FIXES (failing tests / tsc / `## Recent regressions`) → WIRINGS (`node scripts/audit-unwired-engines.mjs`) → GHOST builds (/system-viz roosts) → MISC-TASKS backlog. Zulu ANY-DOMAIN override applies when own queue is dry.
2. RECALL before building: `/dedup` (DuplicationGuard THROWS on dup) → master-index → wiki (`/wiki-query`) → Obsidian vault. Don't re-derive.
3. BUILD with the stack: mechanical code (transforms/lint/docstrings/explain) → Ollama coder ensemble (`ask-ollama` qwen3-coder:30b + qwen2.5-coder, combine outputs); independent parallel modules → hermes/forge-team Agents (model:sonnet); contested/high-stakes verdict → octopus consensus (`prism_ai:consensus_decide`); deep architecture/safety design ONLY → Opus. Real reference-value tests (happy + ≥3 failure + ≥2 adversarial), no stubs, physics constants from src/physics/constants.ts.
4. EVAL-GATE per unit: run the tests + per-file 2-arm scrutiny (reviewer + code-analyzer). Fix every P0/P1.
5. SHIP: commit `[MAIN-FORCE] [SCOPE]/U-ID (slot:zulu): title` on cad-fusion-live-ms0, staging ONLY your files; update the per-agent handoff; feed a lesson memory if a bug was found.
6. RUNWAY: check `node scripts/fleet-survival-status.mjs`; if zone=critical or context YELLOW, self-compact (`node scripts/self-compact.mjs`) — the loop continues fresh. Never bypass the account-switch RED-gate. If Ollama is DOWN, fall back Ollama→Sonnet-agent→Opus (never silently promote mechanical work to Opus).
```

## Consensus answer

```
**Zulu Autonomous Build Loop – 2026‑06‑18**

1. **PICK**  
   - `hermes-zulu backlog --state shared/specs/BRAVO-HERMES-ZULU-OPEN-TASKS-LEDGER.md`  
   - Resolve: failing tests / tsc (`## Recent regressions`) → `node scripts/audit-unwired-engines.mjs` → GHOST builds → MISC‑TASKS.  
   - If own queue empty, apply Zulu ANY‑DOMAIN override.

2. **RECALL**  
   - `/dedup` (DuplicationGuard) → master-index → `/wiki-query` → Obsidian vault.  
   - Skip re‑derivation; use cached data.

3. **BUILD**  
   - Mechanical code: `transforms/lint/docstrings/explain`.  
   - Ollama coder ensemble:  
     ```bash
     ask-ollama qwen3-coder:30b + qwen2.5-coder | combine
     ```  
   - Parallel modules → `hermes/forge-team` (model:sonnet).  
   - Contested verdict → `prism_ai:consensus_decide`.  
   - Deep architecture/safety → Opus.  
   - Run real reference‑value tests (happy + ≥3 failures + ≥2 adversarial); use constants from `src/physics/constants.ts`.

4. **EVAL‑GATE**  
   - Execute unit tests.  
   - Two‑arm scrutiny: reviewer + code‑analyzer.  
   - Fix all P0/P1 issues.

5. **SHIP**  
   - Commit to `[MAIN-FORCE] [SCOPE]/U-ID (slot:zulu): title` on `cad-fusion-live-ms0`.  
   - Stage only your files; update per‑agent handoff.  
   - Log lesson memory if a bug was found.

6. **RUNWAY**  
   - `node scripts/fleet-survival-status.mjs` → check zone status.  
   - If critical/YELLOW, run `node scripts/self-compact.mjs`.  
   - Never bypass RED‑gate account switch.  
   - If Ollama is DOWN: fallback to Sonnet‑agent → Opus (do not promote mechanical work to Opus silently).
```

## Per-model responses

### gpt-oss:20b (ollama) — ok

- latency: 3645ms · tokens: n/a

```
**Zulu Autonomous Build Loop – 2026‑06‑18**

1. **PICK**  
   - `hermes-zulu backlog --state shared/specs/BRAVO-HERMES-ZULU-OPEN-TASKS-LEDGER.md`  
   - Resolve: failing tests / tsc (`## Recent regressions`) → `node scripts/audit-unwired-engines.mjs` → GHOST builds → MISC‑TASKS.  
   - If own queue empty, apply Zulu ANY‑DOMAIN override.

2. **RECALL**  
   - `/dedup` (DuplicationGuard) → master-index → `/wiki-query` → Obsidian vault.  
   - Skip re‑derivation; use cached data.

3. **BUILD**  
   - Mechanical code: `transforms/lint/docstrings/explain`.  
   - Ollama coder ensemble:  
     ```bash
     ask-ollama qwen3-coder:30b + qwen2.5-coder | combine
     ```  
   - Parallel modules → `hermes/forge-team` (model:sonnet).  
   - Contested verdict → `prism_ai:consensus_decide`.  
   - Deep architecture/safety → Opus.  
   - Run real reference‑value tests (happy + ≥3 failures + ≥2 adversarial); use constants from `src/physics/constants.ts`.

4. **EVAL‑GATE**  
   - Execute unit tests.  
   - Two‑arm scrutiny: reviewer + code‑analyzer.  
   - Fix all P0/P1 issues.

5. **SHIP**  
   - Commit to `[MAIN-FORCE] [SCOPE]/U-ID (slot:zulu): title` on `cad-fusion-live-ms0`.  
   - Stage only your files; update per‑agent handoff.  
   - Log lesson memory if a bug was found.

6. **RUNWAY**  
   - `node scripts/fleet-survival-status.mjs` → check zone status.  
   - If critical/YELLOW, run `node scripts/self-compact.mjs`.  
   - Never bypass RED‑gate account switch.  
   - If Ollama is DOWN: fallback to Sonnet‑agent → Opus (do not promote mechanical work to Opus silently).
```

## Backlinks

- task_type: [[task-type-auto-userprompt]]
- recommendation: [[consensus-accept]]
- voter: [[model-gpt-oss-20b]]
