---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: aef1c865664d0d045616d7eabb05e7a762dacdf62e9efcd7101aac9879efecae
sha8: aef1c865
ts: 2026-06-25T01:23:18.664Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.048
success_count: 2
total_latency_ms: 118409
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `aef1c865`

**Recommendation:** `escalate` · **Agreement:** `0.048` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\camDispatcher.ts via Edit

OLD:
            } else if (
              model.includes("OKUMA") || model.includes("LB250") ||
              // U-PPGW12 — Okuma lathe alias-expand: LB-family compact
              // lathes + explicit OSP-PxxxL controllers all route through
              // OkumaB250LatheMasterPostEngine. The engine is hardwired to
              // LB250II-M tribal knowledge; non-LB250 lathes may emit
              // slightly off codes (acknowledged risk per user direction).
              model.includes("LB200") || model.includes("LB300") ||
              model.includes("OSP-P300L") || model.includes("OSP_P300L") ||
              model.includes("OSP-P500L") || model.includes("OSP_P500L")
            ) {
              const { okumaB250LatheMasterPostEngine } = await import("../../engines/OkumaB250LatheMaster

NEW:
            } else if (
              model.includes("OKUMA") || model.includes("LB250") ||
              model.includes("LB3000") || model.includes("MULTUS") ||
              // U-PPGW12 + U-PP-LATHE-MACHINE-AWARE -- Okuma lathe / mill-turn
              // alias-expand: LB-family compact lathes, the LB3000, the MULTUS
              // B250II mill-turn (turning side), and explicit OSP-PxxxL
              // controllers all route through OkumaB250LatheMasterPostEngine.
              // The engine is now machine-aware (latheMachineId below injects
              // machine_id) so LB3000 / MULTUS B250II emit a correct
              // (MACHINE: ...) header instead of the prior hardwired LB250II-M.
              model.includes("LB200") || model.includes("LB300") ||
              model.includes
```

## Consensus answer

```
The edit requires review to confirm the additions and completion of the condition.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 78941ms · tokens: n/a

```
The edit requires review to confirm the additions and completion of the condition.
```

### gpt-oss:20b (ollama) — ok

- latency: 35077ms · tokens: n/a

```
**Critical‑file review – `camDispatcher.ts`**

| Issue | Details | Suggested fix |
|-------|---------|---------------|
| **Incomplete conditional** | The new block ends with `model.includes` and no closing `)` or `{`. This will cause a syntax error on compile. | Finish the condition: e.g. `model.includes("LB300") || model.includes("OSP-P300L") ... ) {`. |
| **Missing imports** | The import line is truncated (`await import("../../engines/OkumaB250LatheMaster`). Ensure it ends with a closing quote and parenthesis, e.g. `await import("../../engines/OkumaB250LatheMaster");`. | Add the missing part of the import statement. |
| **Comment formatting** | The comment block is long but fine; just ensure no stray tabs or line‑breaks that could affect readability. | Keep comments as they are – they accurately describe the new machine‑aware logic. |
| **Model list consistency** | Old code handled `OSP-P300L`/`OSP_P300L` and `OSP-P500L`/`OSP_P500L`. The new snippet only shows `LB200`, `LB300` (and truncated). If those should still be included, add them back. | Add the missing `model.includes("OSP-P300L") || model.includes("OSP_P300L") || model.includes("OSP-P500L") || model.includes("OSP_P500L")` if required. |
| **EOL consistency** | The file uses LF; ensure the new lines are also LF (no CRLF). | Verify with a diff tool or editor settings. |

**Bottom line:**  
The intent to add `LB3000` and `MULTUS` is clear, but the snippet as posted will not compile. Complete the conditional, finish the import, and double‑check that all intended model checks are present before merging.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
