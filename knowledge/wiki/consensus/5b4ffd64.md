---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 5b4ffd64557af36bfbba2c88b87b157833f16bd2d81099e763726467bf60a1eb
sha8: 5b4ffd64
ts: 2026-06-25T03:36:32.768Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: accept
agreement_score: 1
success_count: 1
total_latency_ms: 31428
model_voters: ["gpt-oss:20b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, accept]
---

# Consensus Run `5b4ffd64`

**Recommendation:** `accept` · **Agreement:** `1` · **Voters:** `gpt-oss:20b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\sessionNodeNearAction.test.ts via Edit

OLD:
  it("JSON missing neighbors[] -> fail-soft error", () => {
    const res = runNodeNearAction({ id: "a" }, { runCli: () => JSON.stringify({ id: "a", k: 10 }) });
    expect(res.success).toBe(false);
    expect(res.error).toMatch(/missing neighbors/);
  });
});

NEW:
  it("JSON missing neighbors[] -> fail-soft error", () => {
    const res = runNodeNearAction({ id: "a" }, { runCli: () => JSON.stringify({ id: "a", k: 10 }) });
    expect(res.success).toBe(false);
    expect(res.error).toMatch(/missing neighbors/);
  });

  it("ignores params.ids (single-id action; only id/nodeId/node are read)", () => {
    // node_card takes ids[]; node_near is single-id -> an ids[] alone yields no id.
    const res = runNodeNearAction({ ids: ["a", "b"] }, { runCli: () => GOOD });
    expect(res.success).toBe(false);
    expect(res.error).toMatch(/requires params.id/);
  });

  it("caps k at MAX_NEAR_K before forwarding to runCli (argv cannot be inflated)", () => {
    let seenK = -1;
    const res = runNodeNearAction({ id: "a", k: 9999 }, { runCli: (_id, k) => { seenK
```

## Consensus answer

```
**Review Summary**

The added tests are a good idea – they cover two edge‑cases that were previously untested:

1. **`ids[]` is ignored for single‑ID actions**  
2. **`k` is capped at `MAX_NEAR_K` before the CLI is invoked**

However, there are a few practical issues that need to be addressed before this file can be merged.

| Issue | Why it matters | Fix |
|-------|----------------|-----|
| **Missing import / definition of `GOOD`** | The second test uses `GOOD
```

## Per-model responses

### gpt-oss:20b (ollama) — ok

- latency: 28079ms · tokens: n/a

```
**Review Summary**

The added tests are a good idea – they cover two edge‑cases that were previously untested:

1. **`ids[]` is ignored for single‑ID actions**  
2. **`k` is capped at `MAX_NEAR_K` before the CLI is invoked**

However, there are a few practical issues that need to be addressed before this file can be merged.

| Issue | Why it matters | Fix |
|-------|----------------|-----|
| **Missing import / definition of `GOOD`** | The second test uses `GOOD
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-accept]]
- voter: [[model-gpt-oss-20b]]
