---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 923681967e0438034c4a468c2690f46a5ddbedee851b662b8ccefafde2223452
sha8: 92368196
ts: 2026-06-24T14:24:17.272Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.071
success_count: 2
total_latency_ms: 17844
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `92368196`

**Recommendation:** `escalate` · **Agreement:** `0.071` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\resourceExtractionDispatcher.ts via Edit

OLD:
            const contract = mod.normalizeOfficeExtractToContract(ex, opts);
            const validation = mod.validateDocumentExtractionContract(contract);
            result = { contract, valid: validation.ok, errors: validation.errors ?? [] };
            break;
          }

          // ── Machine Log Actions ───────────────────────────────

NEW:
            const contract = mod.normalizeOfficeExtractToContract(ex, opts);
            const validation = mod.validateDocumentExtractionContract(contract);
            result = { contract, valid: validation.ok, errors: validation.errors ?? [] };
            break;
          }

          case "document_extract_route": {
            // U-XRAY-DOCUMENT-EXTRACT-ROUTER -- given a VALIDATED DocumentExtractionContract (chain
            // document_extract_contract -> this), return the fan-out plan: which document-knowledge
            // consumers (tool-crib lookup / speeds-feeds / tribal capture) the entries can drive, with
            // per-consumer payloads + the tribal confirm-gate. Pure + in-process.
            const docContract = params.contract;
            if (docContract == null || 
```

## Consensus answer

```
The change introduces a new case "document_extract_route" which needs careful review due to its impact on document extraction routing logic.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 4660ms · tokens: n/a

```
The change introduces a new case "document_extract_route" which needs careful review due to its impact on document extraction routing logic.
```

### gpt-oss:20b (ollama) — ok

- latency: 7681ms · tokens: n/a

```
**Issue**

The `case "document_extract_route":` block is inserted *after* the `break;` that ends the previous case in the same `switch`.  
That makes the switch syntactically invalid – the compiler will report “a ‘case’ label is not allowed here” and the file won’t compile.

**Fix**

Move the new case into the `switch` before the preceding `break`, and close all opened blocks (`if`, `case`, `switch`).  
Below is a minimal patch that keeps the rest of the file unchanged:

```ts
// ... previous cases ...

case "document_extract_route": {
  // U-XRAY-DOCUMENT-EXTRACT-ROUTER – given a VALIDATED DocumentExtractionContract (chain
  // document_extract_contract -> this), return the fan‑out plan: which document‑knowledge
  // consumers (tool‑crib lookup / speeds‑feeds / tribal capture) the entries can drive,
  // with per‑consumer payloads + the tribal confirm‑gate. Pure + in‑process.
  const docContract = params.contract;
  if (docContract == null || !docContract.valid) {
    result = { error: "Missing or invalid contract" };
    break;   // exit this case
  }

  /* …rest of the logic… */
}   // ← close the case block

break;   // ← keep the original break for the previous case
```

**Key points**

1. **Place the new `case` inside the `switch`.**  
2. **Close every opened block** (`if`, `case`, and ultimately the `switch`).  
3. **Keep the existing `break;`** that terminates the prior case.

After applying this change, the file should compile without EOL‑flip or syntax errors.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
