---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: bf306e56282cb2c1283891ff54213e972a6f5766c1e9265e01a13e11cd15f303
sha8: bf306e56
ts: 2026-06-24T15:02:47.483Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.163
success_count: 2
total_latency_ms: 24036
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `bf306e56`

**Recommendation:** `escalate` · **Agreement:** `0.163` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\resourceExtractionDispatcher.ts via Edit

OLD:
            const options: any = {};
            if (params.dimensions) {
              options.simulatedDimensions = params.dimensions;
            }
            if (params.titleBlock || params.title_block) {
              options.simulatedTitleBlock = params.titleBlock || params.title_block;
            }
            result = engine.extractDrawing(filePath, options);
            break;

NEW:
            // extractDrawing(path, { entities, dimensions, annotations, layers }) -- match the
            // engine's simulatedData keys (the prior simulated* keys were silently dropped).
            const options: any = {};
            if (params.dimensions) options.dimensions = params.dimensions;
            if (params.entities) options.entities = params.entities;
            if (params.layers) options.layers = params.layers;
            if (params.annotations) options.annotations = params.annotations;
            // title-block fields feed partInfo extraction, which scans annotations.
            const tb = params.titleBlock || params.title_block;
            if (tb && typeof tb === "object") {
              options.annotations = [
                ...(options.annotations || []),
     
```

## Consensus answer

```
The changes are appropriate and align with the comment indicating that the engine's `simulatedData` keys should be matched. The previous `simulated*` keys have been replaced with direct mappings, which is a necessary update. The handling of `titleBlock` or `title_block` to feed into `annotations` also seems correct based on the provided context.

**Final Answer:** The changes are valid and improve the code by correctly mapping parameters to the expected keys in the engine's method and properly handling title block data.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 11021ms · tokens: n/a

```
The changes are appropriate and align with the comment indicating that the engine's `simulatedData` keys should be matched. The previous `simulated*` keys have been replaced with direct mappings, which is a necessary update. The handling of `titleBlock` or `title_block` to feed into `annotations` also seems correct based on the provided context.

**Final Answer:** The changes are valid and improve the code by correctly mapping parameters to the expected keys in the engine's method and properly handling title block data.
```

### gpt-oss:20b (ollama) — ok

- latency: 8773ms · tokens: n/a

```
**Review**

- **Alignment with Engine API** – The patch switches from the old `simulated*` keys to the exact keys (`dimensions`, `entities`, `layers`, `annotations`) that the engine expects, so no data is silently dropped.
- **Title‑Block handling** – By pushing the title‑block object into `options.annotations`, you correctly expose those fields for partInfo extraction (which scans annotations).  
  *Tip:* guard against `null` (`typeof null === "object"`), e.g. `if (tb && typeof tb === "object" && tb !== null)`.
- **Code style** – Inline assignments keep the block compact; comments explain intent clearly.
- **EOL & formatting** – No CRLF/LF issues introduced; the file remains LF‑only.

Overall, the change is sound and improves correctness. Just add a `null` check for `tb` if you want to avoid accidental `null` entries in the annotations array.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
