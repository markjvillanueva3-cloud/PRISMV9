---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: c772a3b0a8212904c22f0fa4c7b7cfc486857b223a7f91bd347043a387d7195f
sha8: c772a3b0
ts: 2026-06-24T18:13:15.152Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.224
success_count: 2
total_latency_ms: 20838
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `c772a3b0`

**Recommendation:** `escalate` · **Agreement:** `0.224` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\feasibilityDispatcher.ts via Edit

OLD:
          case "predictive_failure": {
            const fa = await getEngine("feasAnalysis");
            result = fa.analyze ? fa.analyze(params) : { error: "analyze not available" };
            break;
          }

NEW:
          case "predictive_failure": {
            const fa = await getEngine("feasAnalysis");
            // FeasibilityAnalysisEngine has no analyze() method. The unified entry point
            // is calculate(input: FeasibilityInput) which routes by input.action discriminator
            // ("analyzeAccessibility" | "analyzeWorkholding" | "analyzeRigidity").
            // The auditor found analyze() missing; this guard prevented a runtime crash
            // but silently returned { error: "analyze not available" } on every call.
            result = fa.calculate ? fa.calculate(params) : { error: "FeasibilityAnalysisEngine.calculate not available" };
            break;
          }
```

## Consensus answer

```
**Final Answer:** The edit correctly updates the method call and error message to match the actual API of `FeasibilityAnalysisEngine`. This change should prevent silent errors and ensure that the correct method is invoked.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 9644ms · tokens: n/a

```
**Final Answer:** The edit correctly updates the method call and error message to match the actual API of `FeasibilityAnalysisEngine`. This change should prevent silent errors and ensure that the correct method is invoked.
```

### gpt-oss:20b (ollama) — ok

- latency: 6101ms · tokens: n/a

```
The edit correctly replaces the obsolete `analyze` call with the proper `calculate` entry point for `FeasibilityAnalysisEngine`. The added comment explains the change and why the guard is necessary, and the error message has been updated to reflect the new method name. No syntax or linting issues are introduced. This review approves the edit.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
