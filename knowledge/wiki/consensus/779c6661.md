---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 779c666125b4b5dd72c309fd8108d91bc50055d601183698bd12e47fa1037e00
sha8: 779c6661
ts: 2026-06-24T19:35:17.385Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.222
success_count: 2
total_latency_ms: 9632
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `779c6661`

**Recommendation:** `escalate` · **Agreement:** `0.222` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\resourceExtractionDispatcher.ts via Edit

OLD:
            const ex = params.extraction ?? params.extractedData ?? params.result ?? params.office ?? params.ocr;
            if (ex == null || typeof ex !== "object") {
              return { error: "document_extract_contract requires extraction (an OfficeDocumentPipelineEngine ExtractionResult OR an ImageOCRPipelineEngine OCRResult, or its extractedData object)" };
            }

NEW:
            const ex = params.extraction ?? params.extractedData ?? params.result ?? params.office ?? params.ocr ?? params.ingestion;
            if (ex == null || typeof ex !== "object") {
              return { error: "document_extract_contract requires extraction (an OfficeDocumentPipelineEngine ExtractionResult, an ImageOCRPipelineEngine OCRResult, or a documentLearning IngestionResult -- or its extractedData/items)" };
            }
```

## Consensus answer

```
The change appears valid if `params.ingestion` is a legitimate source of extraction data and the updated error message accurately reflects the new requirement. However, it's crucial to ensure that `documentLearning IngestionResult` is compatible with the expected structure and type checks in the subsequent code.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 2681ms · tokens: n/a

```
The change appears valid if `params.ingestion` is a legitimate source of extraction data and the updated error message accurately reflects the new requirement. However, it's crucial to ensure that `documentLearning IngestionResult` is compatible with the expected structure and type checks in the subsequent code.
```

### gpt-oss:20b (ollama) — ok

- latency: 2122ms · tokens: n/a

```
The change is valid and improves flexibility by allowing an `ingestion` payload as a fallback extraction source.  
- The added `params.ingestion` fits naturally into the existing nullish‑coalescing chain.  
- The updated error message correctly reflects the new accepted types.  

No syntax or type errors are introduced, assuming `ingestion` is part of the `params` interface. This edit should pass compilation and tests.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
