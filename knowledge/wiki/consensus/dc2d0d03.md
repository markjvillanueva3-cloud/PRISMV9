---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: dc2d0d031a391ed42a4436cefc9c51c89a2aa2ad7c75ec8b4a987da8e180b3a7
sha8: dc2d0d03
ts: 2026-06-23T15:43:08.643Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.063
success_count: 2
total_latency_ms: 9885
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `dc2d0d03`

**Recommendation:** `escalate` · **Agreement:** `0.063` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\cadDispatcher.ts via Edit

OLD:
          case "blueprint_coverage_audit": {
            if (!params.rootDir || !params.indexPath || !Array.isArray(params.precomputedRecords)) {

NEW:
          case "blueprint_redact": {
            // U-APP-REDACT-WIRE -- make the tested blueprintRedaction lib reachable as an app surface.
            // Pure + in-process (no I/O): redacts customer identity from a structured extraction (the SAFE
            // field-mask path), free text (distinctive-tier scrub), and/or returns image mask regions from
            // the region-classifier output. Privacy-critical -- a false negative leaks a JM customer identity.
            const hasText = typeof params.text === "string";
            const hasExtraction = params.extraction != null && typeof params.extraction === "object";
            const hasRegions = params.regions != null;
            if (!hasText && !hasExtraction && !hasRegions) {
              return dispatcherError(
              
```

## Consensus answer

```
The edit appears to be a critical change affecting how blueprint redaction is handled. It should undergo a thorough review due to its privacy implications.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 1956ms · tokens: n/a

```
The edit appears to be a critical change affecting how blueprint redaction is handled. It should undergo a thorough review due to its privacy implications.
```

### gpt-oss:20b (ollama) — ok

- latency: 4886ms · tokens: n/a

```
**Critical‑file edit review – `cadDispatcher.ts`**

| Item | Observation | Recommendation |
|------|-------------|----------------|
| **Case name change** | Switched from `"blueprint_coverage_audit"` to `"blueprint_redact"`. | Verify that all callers (e.g., dispatcher registry, tests) reference the new string. If any hard‑coded references remain, update them or add a deprecation shim. |
| **Parameter checks** | Added `hasText`, `hasExtraction`, `hasRegions` and guard against empty payloads. | Ensure `params` type includes `text?: string; extraction?: object; regions?: any;`. If not, extend the interface or cast safely. |
| **Error handling** | Calls `dispatcherError(...)` when no data is supplied. | Confirm that `dispatcherError` is in scope (imported or defined in the same file). Provide a clear error message inside the call – the snippet truncates it. |
| **Syntax & formatting** | The snippet ends abruptly (`return dispatcherError(`) and missing closing braces/parentheses. | Complete the statement: e.g., `return dispatcherError('Missing redact data');` and close all `{}`. Ensure consistent indentation; PRISM will flip EOLs on commit. |
| **Dependencies** | Comment references “blueprintRedaction lib” but no import shown. | If the lib is used later in this case, add an appropriate import (`import { redact } from '...';`). |
| **Privacy‑critical comment** | Good documentation of risk. | Consider adding a unit test that verifies a false negative triggers `dispatcherError`. |
| **Compliance with guards** | No console logs or TODOs; no obvious anti‑pattern. | Passes critical‑file‑guard and auto‑consensus checks. |

**Bottom line:** The intent is clear, but the code fragment is incomplete. Finish the error call, close all blocks, update any external references to the new case string, and ensure type safety for `params`. Once those are addressed, the edit should satisfy the PRISM critical‑file policies.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
