---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: a36c971c433b317ac567153070312a54c2dacb93cdece9a1585e737c3dc31bc3
sha8: a36c971c
ts: 2026-06-24T02:41:11.970Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.054
success_count: 2
total_latency_ms: 14033
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `a36c971c`

**Recommendation:** `escalate` · **Agreement:** `0.054` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\cadDispatcher.ts via Edit

OLD:
          case "per_app_incad_infer": {
            const mod = await import("../../engines/PerAppInCADInferenceAdapter.js");
            const eng: any = new (mod as any).PerAppInCADInferenceAdapter();
            const p = params as any;
            result = { success: true, data: await eng.runInference?.(p?.modelId ?? p?.model_id ?? "", p?.input ?? p) ?? eng.extractFromGeometry?.(p?.cadApp ?? p?.cad_app ?? "generic", p) ?? { engine: "PerAppInCADInferenceAdapter", note: "method not callable" } };
            break;
          }

NEW:
          case "per_app_incad_infer": {
            // FIX (U-INCAD-INFER-FAILLOUD): the case did `new PerAppInCADInferenceAdapter()`
            // with NO args, but the constructor REQUIRES an injected InferenceRuntime +
            // FeatureExtractor -> a TypeError crash on construct (it never even reached the
            // dark runInference/extractFromGeometry probe). This adapter runs model inference
            // INSIDE a CAD plugin process; those backends cannot be supplied through a JSON
            // dispatcher call. Fail LOUD + honest ([[stub-fallback-must-signal-mode-not-pose-as-real]]):
            // report the backend-required mode + the real (pure, no-construct) capability
            // contract so a caller can DISCOVER what is supported and what a host must inject.
   
```

## Consensus answer

```
**Answer:** The edit is appropriate as it provides necessary context and corrects the misunderstanding of how `PerAppInCADInferenceAdapter` should be instantiated, enhancing error reporting and maintainability.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 1731ms · tokens: n/a

```
**Answer:** The edit is appropriate as it provides necessary context and corrects the misunderstanding of how `PerAppInCADInferenceAdapter` should be instantiated, enhancing error reporting and maintainability.
```

### gpt-oss:20b (ollama) — ok

- latency: 9790ms · tokens: n/a

```
**Review**

The new snippet only adds explanatory comments – it no longer creates the adapter instance or returns any result.  
That means the dispatcher will fall through to the next case (or throw a syntax error if the block isn’t closed), and callers will receive `undefined` instead of a clear failure message.

**What needs to be added**

1. **Close the block** – add the missing `}` for the `case`.
2. **Return an explicit error/fallback** – e.g.:

```ts
case "per_app_incad_infer": {
  // The adapter requires a runtime & feature extractor that cannot be supplied via JSON.
  // Return a clear failure indicating the required mode and available pure‑capabilities.
  result = {
    success: false,
    error: {
      code: "BACKEND_REQUIRED",
      message:
        "PerAppInCADInferenceAdapter requires an injected InferenceRuntime and FeatureExtractor. " +
        "This dispatcher cannot provide them; please supply a host backend.",
      supportedModes: ["pure"] // or whatever modes the adapter supports without construction
    }
  };
  break;
}
```

3. **Optional** – If you still want to attempt a fallback (e.g., use a stub that only exposes pure methods), instantiate that instead of throwing.

With those changes the dispatcher will behave predictably and inform callers of the missing backend requirement.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
