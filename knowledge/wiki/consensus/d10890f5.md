---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: d10890f53554f75943b60a237dbe4ff9b173a46b961c8845b55f92236d199273
sha8: d10890f5
ts: 2026-06-24T04:12:22.424Z
task_type: auto-userprompt
source_session: ca9b9050-43d7-4390-93f4-e31f19be4b82
mode: compare
recommendation: escalate
agreement_score: 0.188
success_count: 2
total_latency_ms: 52967
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-userprompt, escalate]
---

# Consensus Run `d10890f5`

**Recommendation:** `escalate` · **Agreement:** `0.188` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
i'm going to bed soon, please run crons, harnesses and engineered loops to run autonomously. make sure auto compaction is fully on so you can build autonomously while I sleep. utilize hermes cli / hermes agents and ollama offloading to their fullest capabilities, optimally. if you exhaust all quoting and charlie work. check into hotel and do the same thing for hotel and the business erp part of the app that you did for the quoting system
```

## Consensus answer

```
Final command sequence:
```
hermes run cascade-run auto-dry-run payroll-run cad-pipeline-run macro-fanout-dry-run
hermes set auto-compaction on
# Monitor and offload tasks as needed using Hermes CLI and Ollama
# Switch to hotel and ERP once quoting and Charlie work are complete
```
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 30592ms · tokens: n/a

```
Final command sequence:
```
hermes run cascade-run auto-dry-run payroll-run cad-pipeline-run macro-fanout-dry-run
hermes set auto-compaction on
# Monitor and offload tasks as needed using Hermes CLI and Ollama
# Switch to hotel and ERP once quoting and Charlie work are complete
```
```

### gpt-oss:20b (ollama) — ok

- latency: 18720ms · tokens: n/a

```
**Quick‑start guide for nightly autonomous runs**

| Task | Command / Script | Notes |
|------|------------------|-------|
| **Enable auto‑compaction** | `hermes config set compaction.auto true`<br>`hermes restart` | Compacts all PRISM data stores on each run. |
| **Schedule cron jobs** | Create `/etc/cron.d/prism_autorun` with: <pre># Run every hour at minute 5<br>5 * * * * root hermes run --context L10/built/cascade-run\n5 * * * * root hermes run --context L10/built/auto-dry-run\n5 * * * * root hermes run --context L10/built/payroll-run\n5 * * * * root hermes run --context L10/built/cad-pipeline-run\n5 * * * * root hermes run --context L10/built/macro-fanout-dry-run</pre> | Adjust hour/minute as needed. |
| **Automate engineered loops** | Add to the same cron file: <pre># Nightly loop for quoting system<br>0 2 * * * root hermes loop start quoting-system --auto‑exit\n# Nightly loop for hotel & ERP modules<br>30 3 * * * root hermes loop start hotel-erp --auto‑exit</pre> | `--auto‑exit` ensures loops terminate after completion. |
| **Use Hermes CLI / agents** | In each script, prefix with: <br>`hermes agent use default`<br>`hermes cli exec "..."` | Ensures all commands run under the correct agent context. |
| **Ollama offloading** | Add to cron jobs: <pre># Offload heavy ML tasks to Ollama<br>15 4 * * * root ollama serve --model large‑lang-model &</pre> | Keep the server running; Hermes can call it via `hermes cli exec "ollama run large-lang-model ..."`. |
| **Verify nightly runs** | Add a log check: <br>`/usr/bin/logger -t PRISM_AUTORUN "$(date): All jobs completed successfully"` | Review `/var/log/syslog` or `/var/log/prism_autorun.log`. |
```

## Backlinks

- task_type: [[task-type-auto-userprompt]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
