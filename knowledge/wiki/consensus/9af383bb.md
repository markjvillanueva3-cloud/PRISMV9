---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 9af383bb23a5ad45cefe39335649f2cd1ebee9ba9383feeded3e78b13ebdd321
sha8: 9af383bb
ts: 2026-06-24T14:53:46.425Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: escalate
agreement_score: 0.122
success_count: 2
total_latency_ms: 43380
model_voters: ["qwen2.5-coder:32b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, escalate]
---

# Consensus Run `9af383bb`

**Recommendation:** `escalate` · **Agreement:** `0.122` · **Voters:** `qwen2.5-coder:32b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\resourceExtractionDispatcher.ts via Edit

OLD:
            const options: any = {};
            if (params.dimensions) {
              options.simulatedDimensions = params.dimensions;
            }
            if (params.titleBlock || params.title_block) {
              options.simulatedTitleBlock = params.titleBlock || params.title_block;
            }
            result = engine.extractDrawing(filePath, options);
            break;

NEW:
            // extractDrawing(path, { entities, dimensions, annotations, layers }) — match the
            // engine's simulatedData keys (the prior simulated* keys were silently dropped).
            const options: any = {};
            if (params.dimensions) options.dimensions = params.dimensions;
            if (params.entities) options.entities = params.entities;
            if (params.layers) options.layers = params.layers;
            if (params.annotations) options.annotations = params.annotations;
            // title-block fields feed partInfo extraction, which scans annotations.
            const tb = params.titleBlock || params.title_block;
            if (tb && typeof tb === "object") {
              options.annotations = [
                ...(options.annotations || []),
      
```

## Consensus answer

```
Final answer: The edit updates the parameter mapping in `resourceExtractionDispatcher.ts` to match the expected keys in `engine.extractDrawing`, ensuring all necessary data (`dimensions`, `entities`, `layers`, `annotations`) is correctly passed. It also properly handles `titleBlock` or `title_block` by adding its contents to `annotations`.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — ok

- latency: 29911ms · tokens: n/a

```
Final answer: The edit updates the parameter mapping in `resourceExtractionDispatcher.ts` to match the expected keys in `engine.extractDrawing`, ensuring all necessary data (`dimensions`, `entities`, `layers`, `annotations`) is correctly passed. It also properly handles `titleBlock` or `title_block` by adding its contents to `annotations`.
```

### gpt-oss:20b (ollama) — ok

- latency: 9715ms · tokens: n/a

```
**Review**

- **Key change** – The dispatcher now passes the raw `dimensions`, `entities`, `layers` and `annotations` keys to the engine instead of the old `simulated*` names.  
  - This matches the engine’s current `simulatedData` contract, so the drop‑of‑silently‑ignored fields is intentional.

- **Title‑block handling** – The comment explains that title‑block data should be fed into the part‑info extraction via annotations.  
  - The code correctly extracts `tb = params.titleBlock || params.title_block`.  
  - However, the snippet ends abruptly (`options.annotations = [ ...(options.annotations || []),`).  
    * Ensure the array is closed and that whatever value you intend to push (likely the title‑block object or a transformed annotation) is added before the closing `]` and semicolon.

- **Safety** – Using `any` for `options` keeps the existing signature but loses type safety. If possible, replace it with a proper interface matching the engine’s expected shape.

- **Lint / EOL** – No CRLF/LF issues are introduced; the edit follows the repo’s LF convention.

**Action items**

1. Complete the missing array literal and confirm what is being pushed from `tb`.  
2. Replace `any` with a typed interface if the project allows.  
3. Run the unit tests for resource extraction to verify that annotations now include title‑block data correctly.
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-escalate]]
- voter: [[model-qwen2-5-coder-32b]]
