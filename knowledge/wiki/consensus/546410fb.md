---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 546410fb6322b45729812c0fdaaf01d863ba02701d3452ad695f65b4abcdd519
sha8: 546410fb
ts: 2026-06-26T15:55:02.558Z
task_type: auto-userprompt
source_session: 23b529e7-7bb8-465e-b4d5-d938982b54b8
mode: compare
recommendation: review
agreement_score: 0.5
success_count: 1
total_latency_ms: 98301
model_voters: ["gpt-oss:20b"]
mean_factuality: null
tags: [consensus, auto-userprompt, review]
---

# Consensus Run `546410fb`

**Recommendation:** `review` · **Agreement:** `0.5` · **Voters:** `gpt-oss:20b`

## Prompt

```
please fix the hermes app install, it doesnt load properly and the hermes cli doesn't launch anymore in powershell
```

## Consensus answer

```
**Quick fix checklist**

| Step | What to do | Command (PowerShell) |
|------|------------|---------------------|
| 1 | **Uninstall old copy** – remove any broken install first. | `Remove-Item -Recurse -Force $env:LOCALAPPDATA\Hermes`<br>`npm uninstall -g hermes-cli` |
| 2 | **Clear cache & config** – delete leftover files that may corrupt the launch. | `rm -r ~/.hermes*` |
| 3 | **Re‑install Hermes CLI** – use the latest release from npm or pip. | `npm install -g hermes-cli@latest`<br>or `<pip install --upgrade hermes-cli>` |
| 4 | **Verify PATH** – ensure the global bin folder is in `$env:Path`. | `echo $env:Path | Select-String "hermes"` |
| 5 | **Test CLI** – check version and basic command. | `hermes --version`<br>`hermes help` |
| 6 | **Launch Desktop App** – run the executable directly to see any error messages. | `& "$env:LOCALAPPDATA\Hermes\hermes.exe"` |
| 7 | **Check logs** – if it still fails, look at `C:\Users\<user>\AppData\Local\Hermes\logs\app.log` for stack traces. |

If after these steps the app still won’t start:

1. **Node version conflict** – make sure you’re on Node ≥ 18 (or the version specified in the docs).  
2. **Permissions** – run PowerShell as Administrator when installing/uninstalling.  
3. **Reboot** – sometimes a fresh session clears stale environment variables.

Let me know what error appears after step 5 or 6, and we can dig deeper.
```

## Per-model responses

### qwen2.5-coder:32b (ollama) — error

- latency: 90016ms · tokens: n/a
- error: `timeout`

```
(no answer — timeout)
```

### gpt-oss:20b (ollama) — ok

- latency: 5307ms · tokens: n/a

```
**Quick fix checklist**

| Step | What to do | Command (PowerShell) |
|------|------------|---------------------|
| 1 | **Uninstall old copy** – remove any broken install first. | `Remove-Item -Recurse -Force $env:LOCALAPPDATA\Hermes`<br>`npm uninstall -g hermes-cli` |
| 2 | **Clear cache & config** – delete leftover files that may corrupt the launch. | `rm -r ~/.hermes*` |
| 3 | **Re‑install Hermes CLI** – use the latest release from npm or pip. | `npm install -g hermes-cli@latest`<br>or `<pip install --upgrade hermes-cli>` |
| 4 | **Verify PATH** – ensure the global bin folder is in `$env:Path`. | `echo $env:Path | Select-String "hermes"` |
| 5 | **Test CLI** – check version and basic command. | `hermes --version`<br>`hermes help` |
| 6 | **Launch Desktop App** – run the executable directly to see any error messages. | `& "$env:LOCALAPPDATA\Hermes\hermes.exe"` |
| 7 | **Check logs** – if it still fails, look at `C:\Users\<user>\AppData\Local\Hermes\logs\app.log` for stack traces. |

If after these steps the app still won’t start:

1. **Node version conflict** – make sure you’re on Node ≥ 18 (or the version specified in the docs).  
2. **Permissions** – run PowerShell as Administrator when installing/uninstalling.  
3. **Reboot** – sometimes a fresh session clears stale environment variables.

Let me know what error appears after step 5 or 6, and we can dig deeper.
```

## Backlinks

- task_type: [[task-type-auto-userprompt]]
- recommendation: [[consensus-review]]
- voter: [[model-gpt-oss-20b]]
