---
schema_version: 1.0.0
kind: consensus_run
prompt_hash: 6058ed8c233bf917a032a9f51d8fe023f62a788ab1ad8f03c491917ea9c91c60
sha8: 6058ed8c
ts: 2026-06-24T01:37:37.611Z
task_type: auto-critical-edit
source_session: unknown
mode: compare
recommendation: accept
agreement_score: 1
success_count: 1
total_latency_ms: 15948
model_voters: ["gpt-oss:20b"]
mean_factuality: null
tags: [consensus, auto-critical-edit, accept]
---

# Consensus Run `6058ed8c`

**Recommendation:** `accept` · **Agreement:** `1` · **Voters:** `gpt-oss:20b`

## Prompt

```
Critical-file edit review for H:\prism\mcp-server\src\tools\dispatchers\orchestrationDispatcher.ts via Edit

OLD:
          case "rollback_plan_build": {
            const { rollbackPlannerEngine } = await import("../../engines/RollbackPlannerEngine.js");
            const p = params as any;
            return ok({ success: true, data: (rollbackPlannerEngine as any).plan?.(p) ?? (rollbackPlannerEngine as any).generate?.(p) ?? (rollbackPlannerEngine as any).run?.(p) ?? { engine: "RollbackPlannerEngine", note: "method not callable" } });
          }

NEW:
          case "rollback_plan_build": {
            // FIX (U-ROLLBACK-PLAN-WIRE): facade probed plan/generate/run (none
            // exist on RollbackPlannerEngine) -> always "method not callable".
            // The real method is planRollback(unitId, steps) -- POSITIONAL args.
            // It self-validates (unitId non-empty string + steps array, else
            // throws -> dispatcher catch -> success:false). `verify:true` opt-in
            // also dry-runs each rollback through the CounterfactualBuildSimulator
            // (planAndVerify); default is plan-only (fast + deterministic, no sim).
            const { rollbackPlannerEngine } = await import("../../engines/RollbackPlannerEngine.js");
            const p = params as { unitId?: string; steps?: unknown; verify?: boolean }
```

## Consensus answer

```
**Critical‑file edit review – `orchestrationDispatcher.ts`**

| Issue | Details |
|-------|---------|
| **Missing return statement** | The new case block ends after declaring `p`. It never returns a value (the old code returned an `ok(...)`). Add the appropriate `return ok({ … })` logic. |
| **Unclosed braces** | The snippet shows only one opening `{` for the case; you need a matching `}` before the next case or at the end of the function. |
| **Incomplete implementation** | The comment explains that `planRollback(unitId, steps)` is the real method and that `verify:true` triggers a dry‑run via `CounterfactualBuildSimulator`. The code must call this method (e.g., `rollbackPlannerEngine.planRollback(p.unitId!, p.steps as any[])`) and handle the optional `verify` flag. |
| **Type safety** | `steps?: unknown` should be typed more precisely (likely `any[]`). If you cast to `unknown`, remember to validate before passing to `planRollback`. |
| **Error handling** | The old code relied on a fallback object `{ engine: "RollbackPlannerEngine", note: "method not callable" }`. Decide whether you still need that fallback or if you’ll rely on the method’s own validation and exception handling. |
| **Import path** | `../../engines/RollbackPlannerEngine.js` matches the old code, so no change needed there. |

**Recommendation**

1. Add a proper return statement that calls `rollbackPlannerEngine.planRollback(...)`.  
2. Close all braces.  
3. Validate `unitId` and `steps` before calling the method; if invalid, return `{ success: false }`.  
4. If `verify:true`, wrap the call in
```

## Per-model responses

### gpt-oss:20b (ollama) — ok

- latency: 11719ms · tokens: n/a

```
**Critical‑file edit review – `orchestrationDispatcher.ts`**

| Issue | Details |
|-------|---------|
| **Missing return statement** | The new case block ends after declaring `p`. It never returns a value (the old code returned an `ok(...)`). Add the appropriate `return ok({ … })` logic. |
| **Unclosed braces** | The snippet shows only one opening `{` for the case; you need a matching `}` before the next case or at the end of the function. |
| **Incomplete implementation** | The comment explains that `planRollback(unitId, steps)` is the real method and that `verify:true` triggers a dry‑run via `CounterfactualBuildSimulator`. The code must call this method (e.g., `rollbackPlannerEngine.planRollback(p.unitId!, p.steps as any[])`) and handle the optional `verify` flag. |
| **Type safety** | `steps?: unknown` should be typed more precisely (likely `any[]`). If you cast to `unknown`, remember to validate before passing to `planRollback`. |
| **Error handling** | The old code relied on a fallback object `{ engine: "RollbackPlannerEngine", note: "method not callable" }`. Decide whether you still need that fallback or if you’ll rely on the method’s own validation and exception handling. |
| **Import path** | `../../engines/RollbackPlannerEngine.js` matches the old code, so no change needed there. |

**Recommendation**

1. Add a proper return statement that calls `rollbackPlannerEngine.planRollback(...)`.  
2. Close all braces.  
3. Validate `unitId` and `steps` before calling the method; if invalid, return `{ success: false }`.  
4. If `verify:true`, wrap the call in
```

## Backlinks

- task_type: [[task-type-auto-critical-edit]]
- recommendation: [[consensus-accept]]
- voter: [[model-gpt-oss-20b]]
