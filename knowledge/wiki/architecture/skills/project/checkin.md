---
title: Skill — checkin
type: skill
skill_name: checkin
scope: project
source_path: H:/prism/.claude/commands/checkin.md
generated_by: scripts/generate-skill-wiki.mjs
last_verified: 2026-05-16
tags: [skill, scope-project]
related:
  - knowledge/wiki/architecture/layer-l8.md
---

# Skill — `checkin`

> Check this chat into the 12-slot PRISM fleet (claim alpha..foxtrot / hotel..lima work slots OR golf hygiene slot), bind the handoff filename to the slot, reap crashed slots + stale file-claims. With a loop keyword it engages an autonomous continuous-work loop that survives /compact.

**Scope:** project

<!-- AUTO-START — regenerated by generate-skill-wiki.mjs -->

| Field | Value |
|-------|-------|
| Name | `checkin` |
| Scope | project |
| Source | `H:/prism/.claude/commands/checkin.md` |
| Model | default |
| Tags | _(none)_ |

## Description (first paragraph)

Run this in any chat that's one of the ~7 concurrent PRISM chats (6 work slots + 1 hygiene slot). It (a) claims this chat a stable human-readable slot name, (b) makes the per-chat handoff save under that slot, (c) cleans up crashed-slot / stale-claim debris, (d) surfaces anything that would cause a silent overwrite, a roadmap-drift surprise, or a commit collision, and (e) — if you pass `--roadmap`

<!-- AUTO-END -->

## Autonomous continuous-work loop (2026-05-16)

`/checkin` is keyword-gated: a bare check-in claims the slot + prints the §Report and stops. When `$ARGUMENTS` carries a loop keyword (`/loop`, `autopilot`, `continuous`, `/run-continuous`, "until complete/done", "keep going/working", "as long as possible") **Step 12 — Autonomous Loop** engages: the `/autopilot-full` + `/yolo-mode` doctrine rolled into the slot system. The loop picks the next pending unit, builds it (mandatory per-file scrutiny gate), auto-fixes 3×, commits atomically, ticks `loop-state.mjs`, and repeats — zero questions, no implicit unit caps.

**Cross-`/compact` continuity.** `Step 2b` (loop-resume detection) runs `loop-state.mjs reap` then `read`; a `running` loop-state file makes the chat *resume* the loop after `/compact` even though the auto-fired `/checkin --topic <slot>-<topic>` carries no loop keyword. Rule: the keyword gate engages a *fresh* loop; an active `running` loop-state *resumes* one. `--no-loop` is the operator off-switch (also ends a running loop). Runaway-guarded by `loop-state` 2×target abandon + `autonomous-loop-watchdog` + `autonomous-loop-defer`.

The 12 `checkin-<slot>` NATO wrappers inherit this — they delegate to the canonical `checkin.md` body.

## See also

- Skills root: `H:\prism\.claude\commands`
- Invoke via Skill tool with name `checkin` (no leading slash)
