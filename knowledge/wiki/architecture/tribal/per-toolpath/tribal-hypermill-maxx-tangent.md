---
name: tribal-hypermill-maxx-tangent
description: Per-toolpath tribal — hypermill maxx-tangent
software: hypermill
toolpath: maxx-tangent
source: youtube-transcript
tipCount: 2
---

# hypermill — maxx-tangent

_Per-toolpath tribal MD synthesized from YouTube transcripts. Each tip cites video URL + timestamp deeplink per kilo soul refuse-list._

### Building a Complete Custom Feature (FeatureScript) @649s

**Source:** [Onshape](https://www.youtube.com/watch?v=yi06ZVDoevs&t=619s) · video `yi06ZVDoevs` · 2026-05-27T03:21:31.477Z

```
face there are a few different ways to face there are a few different ways to find the center of a face looking find the center of a face looking find the center of a face looking through the standard Library through the standard Library through the standard Library documentation you may be tempted to use documentation you may be tempted to use documentation you may be tempted to use a function like EV approximate centroid a function like EV approximate centroid a function like EV approximate centroid but it is not recommended to use any but it is not recommended to use any but it is not recommended to use any functions with approximate in the name functions with approximate in the name functions with approximate in the name to build geometry a valid option would to build geometry a valid option would to build geometry a valid option would be to use the evv plane function we used be to use the evv plane function we used be to use the evv plane function we used this in a previous video the origin is this in a previous video the origin is this in a previous video the origin is automatically placed at the center of automatically placed at the center of automatically placed at the center of the Face's bounding box a more direct the Face's bounding box a more direct the Face's bounding box a more direct way to find a location on a face is to way to find a location on a face is to way to find a location on a face is to use the EV face tangent plane function use the EV face tangent 
```

### Building a Complete Custom Feature (FeatureScript) @651s

**Source:** [Onshape](https://www.youtube.com/watch?v=yi06ZVDoevs&t=621s) · video `yi06ZVDoevs` · 2026-05-27T03:21:31.477Z

```
find the center of a face looking find the center of a face looking through the standard Library through the standard Library through the standard Library documentation you may be tempted to use documentation you may be tempted to use documentation you may be tempted to use a function like EV approximate centroid a function like EV approximate centroid a function like EV approximate centroid but it is not recommended to use any but it is not recommended to use any but it is not recommended to use any functions with approximate in the name functions with approximate in the name functions with approximate in the name to build geometry a valid option would to build geometry a valid option would to build geometry a valid option would be to use the evv plane function we used be to use the evv plane function we used be to use the evv plane function we used this in a previous video the origin is this in a previous video the origin is this in a previous video the origin is automatically placed at the center of automatically placed at the center of automatically placed at the center of the Face's bounding box a more direct the Face's bounding box a more direct the Face's bounding box a more direct way to find a location on a face is to way to find a location on a face is to way to find a location on a face is to use the EV face tangent plane function use the EV face tangent plane function use the EV face tangent plane function to create a plane on the selected face to create a plane o
```
