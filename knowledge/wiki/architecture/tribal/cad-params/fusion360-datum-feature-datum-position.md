---
name: "tribal-cad-param-fusion360-datum-feature-datum-position"
domain: "cad"
system: "fusion360"
function: "DATUM_FEATURE"
parameter: "Datum Position"
---

# Tribal — `fusion360` parameter `Datum Position` of `DATUM_FEATURE`

Symbol placement on sheet

When set outside [—], expect API-side type rejection.