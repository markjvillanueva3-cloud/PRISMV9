---
name: "tribal-cad-param-fusion360-joint-rotation-axis"
domain: "cad"
system: "fusion360"
function: "JOINT"
parameter: "Rotation Axis"
---

# Tribal — `fusion360` parameter `Rotation Axis` of `JOINT`

Local axis (revolute / cylindrical)

When set outside [—], expect API-side type rejection.