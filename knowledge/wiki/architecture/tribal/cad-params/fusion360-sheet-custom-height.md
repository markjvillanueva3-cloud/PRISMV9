---
name: "tribal-cad-param-fusion360-sheet-custom-height"
domain: "cad"
system: "fusion360"
function: "SHEET"
parameter: "Custom Height"
---

# Tribal — `fusion360` parameter `Custom Height` of `SHEET`

Used with format=custom

When set outside [—], expect API-side type rejection.