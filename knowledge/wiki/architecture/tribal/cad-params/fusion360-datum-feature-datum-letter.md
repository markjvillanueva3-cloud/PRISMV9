---
name: "tribal-cad-param-fusion360-datum-feature-datum-letter"
domain: "cad"
system: "fusion360"
function: "DATUM_FEATURE"
parameter: "Datum Letter"
---

# Tribal — `fusion360` parameter `Datum Letter` of `DATUM_FEATURE`

Single letter (A, B, C…)

When set outside [—], expect API-side type rejection.