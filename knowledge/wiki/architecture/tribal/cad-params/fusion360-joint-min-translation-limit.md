---
name: "tribal-cad-param-fusion360-joint-min-translation-limit"
domain: "cad"
system: "fusion360"
function: "JOINT"
parameter: "Min Translation Limit"
---

# Tribal — `fusion360` parameter `Min Translation Limit` of `JOINT`

Lower bound on translational DoF (slider/cyl/pin_slot/planar)

When set outside [—], expect API-side type rejection.