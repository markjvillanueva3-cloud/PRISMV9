---
name: "tribal-cad-param-fusion360-joint-plane-normal"
domain: "cad"
system: "fusion360"
function: "JOINT"
parameter: "Plane Normal"
---

# Tribal — `fusion360` parameter `Plane Normal` of `JOINT`

Plane normal (planar joint)

When set outside [—], expect API-side type rejection.