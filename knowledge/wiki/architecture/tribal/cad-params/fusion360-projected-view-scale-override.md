---
name: "tribal-cad-param-fusion360-projected-view-scale-override"
domain: "cad"
system: "fusion360"
function: "PROJECTED_VIEW"
parameter: "Scale Override"
---

# Tribal — `fusion360` parameter `Scale Override` of `PROJECTED_VIEW`

Override source view scale (rare)

When set outside [—], expect API-side type rejection.