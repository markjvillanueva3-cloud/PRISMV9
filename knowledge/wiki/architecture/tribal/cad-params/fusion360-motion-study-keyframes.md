---
name: "tribal-cad-param-fusion360-motion-study-keyframes"
domain: "cad"
system: "fusion360"
function: "MOTION_STUDY"
parameter: "Keyframes"
---

# Tribal — `fusion360` parameter `Keyframes` of `MOTION_STUDY`

[(time_s, joint_position_state)] keyframe at each timestamp

When set outside [—], expect API-side type rejection.