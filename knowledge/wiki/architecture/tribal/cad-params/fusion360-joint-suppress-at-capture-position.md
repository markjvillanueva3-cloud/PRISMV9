---
name: "tribal-cad-param-fusion360-joint-suppress-at-capture-position"
domain: "cad"
system: "fusion360"
function: "JOINT"
parameter: "Suppress at Capture Position"
---

# Tribal — `fusion360` parameter `Suppress at Capture Position` of `JOINT`

Auto-suppress joint when in capture position (rare — for kinematic puzzles)

When set outside [—], expect API-side type rejection.