---
name: "tribal-cad-param-fusion360-joint-component-2-joint-origin"
domain: "cad"
system: "fusion360"
function: "JOINT"
parameter: "Component 2 Joint Origin"
---

# Tribal — `fusion360` parameter `Component 2 Joint Origin` of `JOINT`

Joint origin on second (target) component

When set outside [—], expect API-side type rejection.