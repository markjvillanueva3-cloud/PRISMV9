---
name: "tribal-cad-param-fusion360-joint-component-1-joint-origin"
domain: "cad"
system: "fusion360"
function: "JOINT"
parameter: "Component 1 Joint Origin"
---

# Tribal — `fusion360` parameter `Component 1 Joint Origin` of `JOINT`

Joint origin on first (mobile) component

When set outside [—], expect API-side type rejection.