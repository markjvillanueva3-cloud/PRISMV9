---
name: "tribal-cad-param-fusion360-as-built-joint-min-limit"
domain: "cad"
system: "fusion360"
function: "AS_BUILT_JOINT"
parameter: "Min Limit"
---

# Tribal — `fusion360` parameter `Min Limit` of `AS_BUILT_JOINT`

Lower motion bound (units depend on motion type)

When set outside [—], expect API-side type rejection.