---
name: "tribal-cad-param-fusion360-joint-snap-mode"
domain: "cad"
system: "fusion360"
function: "JOINT"
parameter: "Snap Mode"
---

# Tribal — `fusion360` parameter `Snap Mode` of `JOINT`

How origins are aligned during preview

When set outside [—], expect API-side type rejection.