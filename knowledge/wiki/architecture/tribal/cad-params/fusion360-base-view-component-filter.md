---
name: "tribal-cad-param-fusion360-base-view-component-filter"
domain: "cad"
system: "fusion360"
function: "BASE_VIEW"
parameter: "Component Filter"
---

# Tribal — `fusion360` parameter `Component Filter` of `BASE_VIEW`

Comma-separated component visibility list

When set outside [—], expect API-side type rejection.