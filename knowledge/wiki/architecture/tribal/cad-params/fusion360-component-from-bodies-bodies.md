---
name: "tribal-cad-param-fusion360-component-from-bodies-bodies"
domain: "cad"
system: "fusion360"
function: "COMPONENT_FROM_BODIES"
parameter: "Bodies"
---

# Tribal — `fusion360` parameter `Bodies` of `COMPONENT_FROM_BODIES`

Bodies to wrap in a component

When set outside [—], expect API-side type rejection.