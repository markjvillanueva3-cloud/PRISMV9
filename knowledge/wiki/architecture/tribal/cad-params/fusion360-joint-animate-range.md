---
name: "tribal-cad-param-fusion360-joint-animate-range"
domain: "cad"
system: "fusion360"
function: "JOINT"
parameter: "Animate Range"
---

# Tribal — `fusion360` parameter `Animate Range` of `JOINT`

Show range-of-motion preview in Animation workspace

When set outside [—], expect API-side type rejection.