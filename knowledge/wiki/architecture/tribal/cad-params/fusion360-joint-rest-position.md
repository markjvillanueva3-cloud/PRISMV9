---
name: "tribal-cad-param-fusion360-joint-rest-position"
domain: "cad"
system: "fusion360"
function: "JOINT"
parameter: "Rest Position"
---

# Tribal — `fusion360` parameter `Rest Position` of `JOINT`

Joint position at design rest state (units depend on motion type)

When set outside [—], expect API-side type rejection.