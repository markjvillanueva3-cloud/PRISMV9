---
name: "tribal-cad-param-fusion360-centerline-extension"
domain: "cad"
system: "fusion360"
function: "CENTERLINE"
parameter: "Extension"
---

# Tribal — `fusion360` parameter `Extension` of `CENTERLINE`

Extension beyond endpoints

When set outside [—], expect API-side type rejection.