---
name: "tribal-cad-param-fusion360-revision-table-sheet-reference"
domain: "cad"
system: "fusion360"
function: "REVISION_TABLE"
parameter: "Sheet Reference"
---

# Tribal — `fusion360` parameter `Sheet Reference` of `REVISION_TABLE`

List which sheets each revision affects

When set outside [—], expect API-side type rejection.