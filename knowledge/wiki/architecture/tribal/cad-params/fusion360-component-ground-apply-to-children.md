---
name: "tribal-cad-param-fusion360-component-ground-apply-to-children"
domain: "cad"
system: "fusion360"
function: "COMPONENT_GROUND"
parameter: "Apply to Children"
---

# Tribal — `fusion360` parameter `Apply to Children` of `COMPONENT_GROUND`

Recurse into sub-components

When set outside [—], expect API-side type rejection.