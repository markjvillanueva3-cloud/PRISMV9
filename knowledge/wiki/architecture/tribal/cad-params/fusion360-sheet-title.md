---
name: "tribal-cad-param-fusion360-sheet-title"
domain: "cad"
system: "fusion360"
function: "SHEET"
parameter: "Title"
---

# Tribal — `fusion360` parameter `Title` of `SHEET`

Sheet title (defaults to source model name)

When set outside [—], expect API-side type rejection.