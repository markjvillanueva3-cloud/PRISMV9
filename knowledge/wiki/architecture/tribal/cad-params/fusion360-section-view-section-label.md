---
name: "tribal-cad-param-fusion360-section-view-section-label"
domain: "cad"
system: "fusion360"
function: "SECTION_VIEW"
parameter: "Section Label"
---

# Tribal — `fusion360` parameter `Section Label` of `SECTION_VIEW`

Letter (A, B, C…) for the section identifier

When set outside [—], expect API-side type rejection.