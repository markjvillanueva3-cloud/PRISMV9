---
name: "tribal-cad-param-fusion360-sheet-custom-width"
domain: "cad"
system: "fusion360"
function: "SHEET"
parameter: "Custom Width"
---

# Tribal — `fusion360` parameter `Custom Width` of `SHEET`

Used with format=custom

When set outside [—], expect API-side type rejection.