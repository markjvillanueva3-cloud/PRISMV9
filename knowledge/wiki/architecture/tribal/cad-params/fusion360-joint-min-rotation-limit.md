---
name: "tribal-cad-param-fusion360-joint-min-rotation-limit"
domain: "cad"
system: "fusion360"
function: "JOINT"
parameter: "Min Rotation Limit"
---

# Tribal — `fusion360` parameter `Min Rotation Limit` of `JOINT`

Lower bound on rotational DoF (revolute/cyl/pin_slot/ball)

When set outside [—], expect API-side type rejection.