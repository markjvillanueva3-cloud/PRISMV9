---
name: "tribal-cad-param-fusion360-base-view-scale"
domain: "cad"
system: "fusion360"
function: "BASE_VIEW"
parameter: "Scale"
---

# Tribal — `fusion360` parameter `Scale` of `BASE_VIEW`

View scale (overrides sheet scale)

When set outside [—], expect API-side type rejection.