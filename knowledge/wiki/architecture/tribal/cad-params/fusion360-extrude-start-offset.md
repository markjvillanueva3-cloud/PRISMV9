---
name: "tribal-cad-param-fusion360-extrude-start-offset"
domain: "cad"
system: "fusion360"
function: "EXTRUDE"
parameter: "Start Offset"
---

# Tribal — `fusion360` parameter `Start Offset` of `EXTRUDE`

Distance from profile plane when start = offset

When set outside [—], expect API-side type rejection.