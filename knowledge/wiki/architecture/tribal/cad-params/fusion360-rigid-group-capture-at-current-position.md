---
name: "tribal-cad-param-fusion360-rigid-group-capture-at-current-position"
domain: "cad"
system: "fusion360"
function: "RIGID_GROUP"
parameter: "Capture at Current Position"
---

# Tribal — `fusion360` parameter `Capture at Current Position` of `RIGID_GROUP`

Lock at current pose (vs design rest)

When set outside [—], expect API-side type rejection.