---
name: "tribal-cad-param-fusion360-export-include-sheets"
domain: "cad"
system: "fusion360"
function: "EXPORT"
parameter: "Include Sheets"
---

# Tribal — `fusion360` parameter `Include Sheets` of `EXPORT`

Comma-separated sheet ids ('all' for full doc)

When set outside [—], expect API-side type rejection.