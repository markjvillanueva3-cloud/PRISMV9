---
name: "tribal-cad-param-fusion360-joint-joint-name"
domain: "cad"
system: "fusion360"
function: "JOINT"
parameter: "Joint Name"
---

# Tribal — `fusion360` parameter `Joint Name` of `JOINT`

User-facing label in browser tree

When set outside [—], expect API-side type rejection.