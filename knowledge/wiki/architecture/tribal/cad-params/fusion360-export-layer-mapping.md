---
name: "tribal-cad-param-fusion360-export-layer-mapping"
domain: "cad"
system: "fusion360"
function: "EXPORT"
parameter: "Layer Mapping"
---

# Tribal — `fusion360` parameter `Layer Mapping` of `EXPORT`

Custom layer-name override map (DWG/DXF)

When set outside [—], expect API-side type rejection.