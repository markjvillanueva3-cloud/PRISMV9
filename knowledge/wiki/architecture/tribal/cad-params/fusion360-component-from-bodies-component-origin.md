---
name: "tribal-cad-param-fusion360-component-from-bodies-component-origin"
domain: "cad"
system: "fusion360"
function: "COMPONENT_FROM_BODIES"
parameter: "Component Origin"
---

# Tribal — `fusion360` parameter `Component Origin` of `COMPONENT_FROM_BODIES`

Optional point to use as new component's origin

When set outside [—], expect API-side type rejection.