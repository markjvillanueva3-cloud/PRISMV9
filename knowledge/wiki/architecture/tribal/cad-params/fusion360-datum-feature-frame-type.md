---
name: "tribal-cad-param-fusion360-datum-feature-frame-type"
domain: "cad"
system: "fusion360"
function: "DATUM_FEATURE"
parameter: "Frame Type"
---

# Tribal — `fusion360` parameter `Frame Type` of `DATUM_FEATURE`

_(no operator guidance yet — fill from shop-floor knowledge or vendor docs)_

When set outside [—], expect API-side type rejection.