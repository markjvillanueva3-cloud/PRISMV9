---
name: "tribal-cad-param-fusion360-extrude-target-bodies"
domain: "cad"
system: "fusion360"
function: "EXTRUDE"
parameter: "Target Bodies"
---

# Tribal — `fusion360` parameter `Target Bodies` of `EXTRUDE`

Bodies to combine with (join/cut/intersect)

When set outside [—], expect API-side type rejection.