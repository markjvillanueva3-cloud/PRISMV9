---
name: "tribal-cad-param-fusion360-centerline-hash-length"
domain: "cad"
system: "fusion360"
function: "CENTERLINE"
parameter: "Hash Length"
---

# Tribal — `fusion360` parameter `Hash Length` of `CENTERLINE`

Long-dash length

When set outside [—], expect API-side type rejection.