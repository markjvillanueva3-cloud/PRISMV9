---
name: "tribal-cad-param-fusion360-joint-custom-rotation-vector"
domain: "cad"
system: "fusion360"
function: "JOINT"
parameter: "Custom Rotation Vector"
---

# Tribal — `fusion360` parameter `Custom Rotation Vector` of `JOINT`

Custom axis when rotation_axis = custom

When set outside [—], expect API-side type rejection.