---
name: "tribal-cad-param-fusion360-drive-joints-target-positions"
domain: "cad"
system: "fusion360"
function: "DRIVE_JOINTS"
parameter: "Target Positions"
---

# Tribal — `fusion360` parameter `Target Positions` of `DRIVE_JOINTS`

[(joint_index, position_value)] target value per joint

When set outside [—], expect API-side type rejection.