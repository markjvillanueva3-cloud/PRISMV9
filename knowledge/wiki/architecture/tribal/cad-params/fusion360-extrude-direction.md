---
name: "tribal-cad-param-fusion360-extrude-direction"
domain: "cad"
system: "fusion360"
function: "EXTRUDE"
parameter: "Direction"
---

# Tribal — `fusion360` parameter `Direction` of `EXTRUDE`

Sweep direction relative to profile plane

When set outside [—], expect API-side type rejection.