---
name: "tribal-cad-param-fusion360-parts-list-custom-fields"
domain: "cad"
system: "fusion360"
function: "PARTS_LIST"
parameter: "Custom Fields"
---

# Tribal — `fusion360` parameter `Custom Fields` of `PARTS_LIST`

Additional iProperty fields

When set outside [—], expect API-side type rejection.