---
name: "tribal-cad-param-fusion360-extrude-direction-vector-override"
domain: "cad"
system: "fusion360"
function: "EXTRUDE"
parameter: "Direction Vector Override"
---

# Tribal — `fusion360` parameter `Direction Vector Override` of `EXTRUDE`

Optional non-normal direction (edge/axis)

When set outside [—], expect API-side type rejection.