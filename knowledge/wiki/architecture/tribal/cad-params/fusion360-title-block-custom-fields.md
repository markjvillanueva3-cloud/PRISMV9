---
name: "tribal-cad-param-fusion360-title-block-custom-fields"
domain: "cad"
system: "fusion360"
function: "TITLE_BLOCK"
parameter: "Custom Fields"
---

# Tribal — `fusion360` parameter `Custom Fields` of `TITLE_BLOCK`

Comma-separated key=value overrides

When set outside [—], expect API-side type rejection.