---
name: "tribal-cad-param-fusion360-hole-table-hole-selection"
domain: "cad"
system: "fusion360"
function: "HOLE_TABLE"
parameter: "Hole Selection"
---

# Tribal — `fusion360` parameter `Hole Selection` of `HOLE_TABLE`

Specific holes (default: all on view)

When set outside [—], expect API-side type rejection.