---
name: "tribal-cad-param-fusion360-break-view-break-lines"
domain: "cad"
system: "fusion360"
function: "BREAK_VIEW"
parameter: "Break Lines"
---

# Tribal — `fusion360` parameter `Break Lines` of `BREAK_VIEW`

Two parallel sketch lines defining break range

When set outside [—], expect API-side type rejection.