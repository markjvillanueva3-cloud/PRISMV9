---
name: "tribal-cad-param-fusion360-export-line-weight-override"
domain: "cad"
system: "fusion360"
function: "EXPORT"
parameter: "Line Weight Override"
---

# Tribal — `fusion360` parameter `Line Weight Override` of `EXPORT`

Force line weight (default = inherit)

When set outside [—], expect API-side type rejection.