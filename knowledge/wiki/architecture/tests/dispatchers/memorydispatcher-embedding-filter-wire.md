---
title: Test — memoryDispatcher.embedding-filter-wire.test.ts
type: test
kind: test
test_path: mcp-server/src/__tests__/memoryDispatcher.embedding-filter-wire.test.ts
group: dispatchers
it_count: 5
expect_count: 20
line_count: 99
generated_by: scripts/generate-test-wiki.mjs
last_verified: 2026-06-02
tags: [architecture, system-viz, test, dispatchers]
related:
  - knowledge/wiki/architecture/tests-index.md
---

# Test — `memoryDispatcher.embedding-filter-wire.test.ts`

> EMBEDDING-FILTER-WIRE — wire the orphaned EmbeddingFilterEngine (0 dispatcher refs, 0 · consumers — a true stop_on_unwired_assets orphan) into memoryDispatcher (prism_memory) as · `embedding_filter`. 

<!-- AUTO-START — regenerated by generate-test-wiki.mjs -->

| Field | Value |
|-------|-------|
| Path | `mcp-server/src/__tests__/memoryDispatcher.embedding-filter-wire.test.ts` |
| Group | dispatchers |
| Tests (`it`/`test`) | 5 |
| Assertions (`expect`) | 20 |
| Lines | 99 |

## What it checks (sampled describe/it titles)

- prism_memory::embedding_filter (EmbeddingFilterEngine round-trip + DI logic)
- dispatcher round-trip: filters directive lines, honors maxLines, preserves source order
- dispatcher round-trip: rejects non-string directive/prompt (fail-loud)
- direct engine (injected fake embedder): cosine path ranks the prompt-aligned line first
- direct engine (failing embedder): falls back to Jaccard, token-overlap line outscores trivia
- direct engine: empty directive yields an empty, well-formed result

## How to run

```bash
cd mcp-server && npx vitest run src/__tests__/memoryDispatcher.embedding-filter-wire.test.ts
```

(Or `rtk vitest run …` for token-reduced output.) After an engine edit, the post-edit hook suggests the matching test; this entry is the wiki index of it.

<!-- AUTO-END -->

## See also

- Test catalog: [[tests-index]]
- Run all: `cd mcp-server && npx vitest run` · coverage: `/test-coverage`
