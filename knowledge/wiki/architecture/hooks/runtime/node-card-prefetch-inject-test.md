---
title: Hook — node-card-prefetch-inject.test
type: hook
hook_name: node-card-prefetch-inject.test
hook_source: runtime
source_path: H:/prism/.claude/hooks/node-card-prefetch-inject.test.mjs
events: []
generated_by: scripts/generate-hook-wiki.mjs
last_verified: 2026-06-04
tags: [hook, source-runtime, ]
related:
  - knowledge/wiki/architecture/layer-l6.md
---

# Hook — `node-card-prefetch-inject.test`

**Source:** runtime  ·  **File:** `H:/prism/.claude/hooks/node-card-prefetch-inject.test.mjs`
**Triggers on:** _(events not inferable from source)_

<!-- AUTO-START — regenerated by generate-hook-wiki.mjs -->

## Description (from file header)

node-card-prefetch-inject.test.mjs — verifies the prefetch hook's INTENT:
  1. detection fires ONLY on whitelisted node-id prefixes and IGNORES noisy
     code/prose dotted tokens (fs.readFileSync, version 2.5, schema.org) —
     so the common prompt never even triggers a seek,
  2. trailing punctuation / dedup / cap handled,
  3. buildPrefetchContext injects cards ONLY for ids that resolve to REAL
     nodes in the (fixture) offset index; a whitelisted-but-nonexistent id
     injects nothing; n

## Events

_(no event signatures found — may be a helper or imported by another hook)_

## Doctrine

- Hooks are enforced by the Claude Code harness on every applicable event
- Source-of-truth lives in `H:/.claude/settings.json` (project + user merged)
- Source files in `.claude/hooks/` (runtime) and `mcp-server/src/hooks/` (engine)
- Hook output convention: print to stdout, return JSON `{ continue: true }` to proceed

<!-- AUTO-END -->

## See also

- Engine layer: [[layer-l6]]
- Hook bus settings: `H:/.claude/settings.json`
- Verify hook refs: `node H:/prism/.claude/helpers/verify-hook-refs.mjs`
