# CLAUDE.md regression log (archived)

## Archived 2026-06-30T20:50:00.985Z — 38 entries

- 2026-06-29 | **JM Okuma lathe master-post emitted Fanuc-dialect canned cycles wearing an Okuma header -- threading was Fanuc G76, must be Okuma G71 (a Fanuc G76 ALARMS / mis-cycles on an OSP control); JM-fleet line-by-line audit caught it (slot:echo)** | observed-in: U-PP-OKUMA-THREAD-G71 (35a5f267e2 + 1b06b46276) | root cause: `OkumaB250LatheMasterPostEngine.generateThreadingCycle` emitted Fanuc two-line `G76 P.. / G76 X.. Z.. P.. Q.. F..` threading on Okuma machines. On Okuma OSP threading is the SINGLE-LINE `G71 X Z B D U H F M33 M73` cycle -- and Fanuc's G71 is ROUGHING, so a Fanuc assumption points the wrong way (swapping roughing->G71 would emit a thread cycle for roughing -- the prior-session trap). Verified line-for-line vs Mark's running JM programs (A05-LSC-25-B.MIN:149 "3-16", ACME/THREAD M8X125.MIN:60) + the JM Multus .cps onCyclePoint thread-turning writeBlock (.cps:3364-3378). The post passed dialect-lint + structural-conformance with 0 ERRORs because NO lint rule knew the threading dialect -- dialect-wrong-but-structurally-valid is the silent class. fix: emit Okuma G71 (X=minor=start-2*radial, H=2*radial "output as diameter" per .cps:3372, F=lead mm, B=incl angle, D/U=Mark's D.003"/U.001", M33 M73; decoded Mark's inch F=J*pitch~=1.0 vs metric F=lead). Added R9 lint thread-dialect-okuma (P-word-gated so valid OSP `G76 X.. L..` grooving in ~984 real .MIN is spared -- scrutiny arm A). Rewrote unit+integration+lint tests to G71 (arms B+C caught 2 un-updated integration consumers; my first '74/74' missed the integration file). LIVE: G71 X38.160 Z-25.000 B60 D0.080 U0.025 H1.840 F1.5 M33 M73; verifier ALL 20 PERFECT; lint 35/35; engine+integ 87/87; 3-of-3 PASS. STILL Fanuc on Okuma (QUEUED, ground truth in [[reference_echo_okuma_cycle_dialect_2026_06_28]]): rough G72->LAP G85/G81/G80, finish->G87, groove G75->explicit+G4, T0101->T010101, M30->M2; + sibling engines LatheMasterPostRouterEngine:315 + MasterPostProcessorGeniusEngine catalog carry same G76-on-okuma. Lesson: a structurally-valid post can be 100% wrong-DIALECT; lint that doesn't encode the cycle dialect won't catch it -- open the machine's OWN .MIN + the .cps emit fn. | verify: `git -C H:/prism show 35a5f267e2` -- [[reference_echo_okuma_cycle_dialect_2026_06_28]]
- 2026-06-28 | **text->CadQuery applied a SPURIOUS + units-buggy spark-gap to ~16% of GEN parts -> systematically undersized ~1.94mm (FOUND by the new CAD dim-correction closed loop, slot:delta)** | observed-in: U-DELTA-CADGEN-SPARKGAP-FIX | root cause: the GEN prompt (`scripts/cad-text-to-cadquery.mjs::buildPrompt`) said "If the request implies a sinker-EDM electrode, undersize 0.0015in/side (0.003 total spark gap)." qwen2.5-coder:32b (1) applied it to PLAIN parts (a "rectangular plate" is not an electrode) ~16% of the time (45/283 model.py had SPARK_GAP, inconsistently -- the cube didn't), AND (2) with a UNITS BUG: `SPARK_GAP=0.003*IN`(0.0762mm) then `length=(2.0-SPARK_GAP)*IN`=(2.0-0.0762)*25.4=48.86mm -- subtracting a MM value from an INCH value, undersizing by a CONSTANT ~1.94mm (0.97/side) regardless of part size (the units-bug fingerprint). "2x1x0.5in plate" -> 48.86x23.46x12.7mm not 50.8x25.4x12.7 (Z exact, no allowance touched it). Silent: valid solid, wrong size. **The closed loop's FIRST real catch** -- the dim-correction producer (U-DELTA-CADGEN-DIM-CORRECTION) flagged it, root-cause traced to the prompt. fix: tighten the prompt -- lead "EXACT nominal dimensions", gate the allowance "ONLY when the request EXPLICITLY names a sinker-EDM electrode ... a plain plate/block/cube gets NO allowance", show the correct units form `(2.0-0.003)*IN` NEVER `(2.0-0.003*IN)*IN`. Kept the canonical electrode capability. VALIDATED: exact failing spec regenerated no-spark-gap [50.8,25.4,12.7] (was [48.86,23.46,12.7]); 18/18 prompt tests. Lessons: a closed loop earns its keep on its first run; a units bug hides as a CONSTANT offset (fixed mm regardless of nominal); a conditional LLM instruction needs an explicit NEGATIVE. | verify: `git -C H:/prism show HEAD` -- [[cadgen-spurious-sparkgap-units-bug]]
- 2026-06-28 | **STEP dimension extractor silently dropped trailing-dot reals (`20.`/`0.`/`-3.`) -- `extractBboxMm` returned NULL on standard STEP files, `extractRadiiMm` under-counted integer radii (slot:delta)** | observed-in: U-DELTA-STEP-TRAILING-DOT | root cause: STEP/EXPRESS reals are routinely written with a trailing dot and NO fractional digits (`20.`, `0.`, `-3.`) -- a valid literal. But the number sub-pattern in BOTH `RADIUS_ENTITY_RE` and `POINT_RE` (`scripts/lib/step-dimension-extract.mjs`) was `[0-9]+(?:\.[0-9]+)?` -- digits REQUIRED after the dot -- so `20.` matched only `20` then failed on the stray `.`. SILENT effects: `extractBboxMm` matched 0 of 9 CARTESIAN_POINTs on a real corpus part (`cylinder.step`) -> `n<2` -> returned `null` on a valid part (the point-envelope CAD training signal was DEAD for every STEP writing integer coords as `N.` -- most of them); `extractRadiiMm` dropped any `5.`-style integer radius -> under-counted the DIMTOL distribution gate I built earlier + india's bbox/radii training signals. No error, green tests -- the original tests only used `0.5`/`0.1000...` (fractional digits present), never the trailing-dot form STEP writers overwhelmingly use (R9 coverage gap). Surfaced building a NEW consumer (live-Fusion corpus closed loop) that got `null` where geometry plainly existed. fix: number sub-pattern -> `[0-9]+(?:\.[0-9]*)?` (zero-or-more) in both regexes; live cylinder.step 0->9 points; 20/20 (4 regression each FAIL pre-fix). Lesson: a parse regex that silently drops a valid literal form is the worst bug class (no exception, just a quietly-incomplete distribution every downstream consumer inherits with green tests); test the literal forms the FORMAT emits, not the ones you'd type. Secondary: point-based STEP bbox is unreliable for CURVED surfaces (cylinder envelope came back [40,12.5,0] not [25,25,40] -- CARTESIAN_POINTs aren't a true surface bound; bboxStats already excludes degenerate min-dim~0 bboxes downstream; reliable curved envelope needs the kernel /geometry). | verify: `git -C H:/prism show HEAD` -- [[step-trailing-dot-real-silent-underextraction]]
- 2026-06-28 | **JM Multus B250 sub-spindle: THREE divergent unverified code-sets in the repo, ZERO matching the real machine -- built the verified emit + corrected them (slot:echo)** | observed-in: 498574b55f | root cause: the real JM Multus B250 master post (`OkumaB250LatheMasterPostEngine`) had `sub_spindle_enabled` but NO transfer emit path -- only a WRONG `M38/M39` tip + header doc-comment. TWO other engines carried conflicting unverified sub-spindle codes: `PPOkumaSubSpindleSyncEngine` (claims "LU3000", no cite: `M227/M228` sub-chuck, `M87/M89` main, `G145/G146` sync, `B`-axis, `G04 P<ms>` dwell) and `marks-multus-patterns.ts` PAT-005/008 (collet `M68/M69/M51/M102/M111`). The actual fleet machine (verified vs Mark's OWN running programs `JM DIE/CNC OKUMA MULTUS/MARK'S {GRAB AND PULL, WORKING SPINDLE GRAB-PULL-CUTOFF}.min`) uses `M248/M249`(sub chuck)+`M83/M84`(main)+`M151/M150`(sync)+`M247/M246`+`M185/M184`(interlock)+`G141`+`W`-axis+Okuma `G4 F<sec>` dwell -- NONE of the three. A wrong/absent sub-spindle transfer is crash + part-drop class. fix: built `generateSubSpindleTransfer` (bar_pull/pickoff) emitting the verified choreography from canonical `MULTUS_B250_SUBSPINDLE_CODES` (not inlined); NO-DROP invariant (sub clamps M248+dwell BEFORE main unclamps M84), G4 dwell after every chuck op, sync-on before approach, interlock brackets, fail-loud on non-finite W/RPM; corrected the M38/M39 tip+header; FLAGGED (not deleted -- R7) the collet/LU3000 entries (may serve other machines). 18 tests + 73/73 B250 suites; verifier 20/20; golden 3/3 no-drift; 2-arm + 3-of-3 scrutiny PASS (both read the raw .min source line-for-line; 3-of-3 arm C caught a stale count-guard P1 -> fixed `17ebddf8b7`). **FOLLOW-ON same session:** the part-off dwell `G04 P0.5` (Fanuc ms) on the Okuma OSP post -> `G4 F0.5` (Okuma F=sec) -- one site fixes the whole JM Okuma lathe fleet, golden re-baselined (1-line) + dialect lock test (`4878fee401`); `PPOkumaSubSpindleSyncEngine` LU3000 codes flagged UNVERIFIED (`c0d7a27961`). Lesson: a dialect code in a comment/tip/data-file is NOT proof -- open the machine's OWN running program; sibling of the 5-axis fabricated-cite bug below. | verify: `git -C H:/prism show 498574b55f` -- [[multus-b250-subspindle-verified-codes-2026-06-28]] · [[feedback_read_full_content_not_titles]]
- 2026-06-28 | **OkumaOSP 5-axis TCP shipped with WRONG TCP-cancel (G170 not G168) + WRONG tilt-axis word (B not A) — a FABRICATED dialect citation, caught by the 3-of-3 not the per-file scrutiny (slot:echo)** | observed-in: ed5a7ae10d (fixed same session) | root cause: the recovered Workflow `wunplwj53` 5-axis TCP emit for VMC-02 (Genos M460V-5AX) emitted `G170` to cancel RTCP and `B`-words for the trunnion tilt. The cited source `OKUMA-M460V-5AX-Ai Enhanced-(iMachining).cps` actually says TCP-OFF=**G168** (:47 "G169 ON / G168 OFF", :515) and the tilt is the **A-axis** (:833 `aOutput{prefix:"A"}`, :1097 tilt `coordinate:0` axis `[1,0,0]`; table=C). The M460V has no B-axis; the engine's OWN OO88 macro already used A. The engine doc-comment FABRICATED ".cps:515 (G170 off)" + "B-word maps to the tilting axis". Both safety-critical: G170 leaves TCP ACTIVE (crash on next positioning move); B-words drive a non-existent axis. The per-file 2-arm scrutiny (incl physics-review) PASSED by trusting the engine's self-citation comment; the end-of-session 3-of-3 arm A opened the `.cps` and caught both P0s. fix: emit G168 (renamed the `tcp_mode` enum off its "G169_G170" misnomer to "G169_G168") + emit A-words (`rotary_moves[].a_deg`); corrected the fabricated comments to cite the real `.cps` lines; propagated to tests (37/37), the closed-loop verifier 5-axis leg (20/20 PERFECT), the byte-lock goldens, and the wiki/ledger/confidence docs. Lesson: a dialect/citation COMMENT is NOT proof — open the cited `.cps` and read `createAxis`/`aOutput`/the TCP M-code lines before trusting it; a reviewer that reads only the engine + its self-citation rubber-stamps a fabricated cite (the 3-of-3's source-reading arm is what catches it). | verify: `git -C H:/prism show HEAD` -- [[okuma-5axis-tcp-and-golden-drift-cron-2026-06-28]] · [[feedback_read_full_content_not_titles]]
- 2026-06-27 | **The lathe closed-loop's "74% uneconomical" was a FLAG-calibration artifact, NOT over-aggressive generation (slot:whiskey)** | observed-in: 55d299ca13 (+ c566611c77, b5d4a9ba4d, c64a1c1100) | root cause: `tool_life_economical` (`scripts/lib/lathe-safety-efficiency-score.mjs`) graded economy on the MIN raw Taylor life across ALL ops vs an arbitrary 15-min floor -- it landed on the surface-justified `od_finish` pass @320 m/min (~1 min raw life, a LIGHT cut whose Vc is NOT an economic lever), and 15 min is not the cost optimum (Gilbert T_econ ~7.81 min for P/n0.25 + JM economics). The obvious premise was REFUTED twice before the real cause: rough Vc (220) is already ~5% from the Gilbert optimum (209) so the generator was NOT over-aggressive (cost-neutral -0.05%), and accurate per-op cost attribution (even-split -> real `cycle_time_sec`) did NOT move the count. fix: (a) the `min_cost` target now caps rough/face/bore Vc at the Gilbert minimum-cost optimum via the canonical GilbertEconomicSpeedEngine (only lowers, never raises; fail-soft); (b) the flag re-grounded on T_econ (new pure `economicOptimumLifeMin`) judged on the dominant tool-CONSUMPTION op among economically-FREE ops (rough/face/bore), EXCLUDING surface/operation-constrained finish/thread/groove/part-off/drill. LIVE A/B (15 real JM parts): min_cost grades 0/15 uneconomical vs balanced 9/15; safety verdict identical 15/15. Advisory-only (never touches the safety verdict). Lessons: validate a headline metric's PREMISE on live data before "fixing" it (the obvious cause was wrong 3x); judge an economy flag only on the variables it can actually optimize (free Vc), floored on the true cost optimum not an arbitrary constant. 21/21 + 13/13 tests. | verify: `git -C H:/prism show 55d299ca13` -- [[economy-flag-artifact-not-generation-defect]] · [[reference_whiskey_kienzle_cost_wire_audit_2026_06_27]]
- 2026-06-23 | **SFC core (SpeedFeedOrchestrator) collapsed cutting speed Vc 2-7x to "resolve" DEFLECTION/force violations -- Vc is independent of them, so the cut published 2-7x too slow AND the violation persisted (slot:oscar)** | observed-in: U-SFC-DEFLECTION-VC-LEVER (ec0ce2ea26) | root cause: `SpeedFeedOrchestratorEngine.compute`'s proportional-reduction block did `Vc *= min(limit/value over ALL failed checks); fz *= sqrt(that)`. deflection (delta=Fc*L^3/3EI), workholding (Fc=kc*ap*fz^(1-mc)) and torque (T=Fc*D/2000 -- Vc cancels since rpm prop Vc) are FORCE-driven and INDEPENDENT of cutting speed; reducing Vc relieves NONE of them, only the fz/ap lever does. Live 1045 (deflection 291%, SOLE failing check) had Vc collapsed 204->33.4 m/min (6x below the 110-200 carbide band) while deflection STILL ended 291% (UNDER-protection). The page (ProductEngine) was already in-band, so the parity probe pinned the divergence as a CORE defect; the corrupted core feeds the 11.2M variability corpus + sfc_nine_axis. physics-reviewer FAIL/CRITICAL; also caught the wrong fz exponent (cut Fc by r -> fz scales r^(1/(1-mc)), not r^0.5). fix: route each binding check to its physically effective lever -- force-driven -> reduce fz by r^(1/(1-mc)); feed_rate -> fz; rpm -> clamp Vc; power (P=Fc*Vc/60000, the ONLY true Vc lever) -> reduce Vc LAST after fz settles. Parity probe page/core 2-7x -> 1.0-1.33x, core now IN published bands all 6 ISO groups (P/M/N/K/S/H). 7/7 new lock (sfc-deflection-vc-lever) + 5 stale characterization tests re-baselined to verified-in-band values. Lesson: a parameter/safety clamp must reduce the lever the violated quantity actually depends on -- collapsing Vc for a deflection/force violation is both ineffective and destructive. | verify: `git -C H:/prism show ec0ce2ea26` -- [[reference_oscar_sfc_deflection_vc_lever_2026_06_23]]
- 2026-06-23 | **SFC over-power/stall guard was efficiency-blind -- compared raw cutting power vs spindle rating, ~15-25% too LENIENT (under-protected) (slot:oscar)** | observed-in: U-SFC-OVERPOWER-SPINDLE-EFF | root cause: `ProductEngine.calculateSafetyScore` (SFC over-power guard on `/speed-feed-calc`) compared RAW cutting power Pc=Fc*Vc/60000 directly to the rated spindle power. But the spindle MOTOR supplies Pc PLUS belt/gear/bearing drivetrain losses, so the real demand is P_spindle=Pc/eta_drive; omitting /eta made every load tier ~1/eta (~15-25%) too lenient -- a cut at 95% cutting-power actually draws ~112% of spindle and stalls, yet graded "safe". UNDER-protected (the dangerous direction for a safety guard). fix: new canonical `SPINDLE_DRIVE_EFFICIENCY=0.85` (constants.ts; HSMAdvisor default / G-Wizard 0.80-0.90, cited inline NOT inlined in the engine); `calculateSafetyScore` compares `spindlePower=Pc/0.85` in all 4 tiers (thresholds/penalties unchanged -- only the compared quantity swapped). Monotonically SAFE (only tightens). +1 self-calibrating R9 lock. physics-reviewer validated the design (Pc/eta stricter+safe; 0.85 defensible; 4 call sites confirmed cutting-power, no double-count; units kW) -- it ran in an isolated worktree off HEAD so couldn't see the uncommitted diff; the committed code matches + the R9 test proves it wired. 27/27 page + 84/84 SFC-path/safety tests; type-clean; LIVE :3100: cutting 6.18kW < rating 6.67kW (old=safe) but spindle 7.27kW > rating -> now "warning: spindle will stall". Lesson: a stall guard must compare the EFFICIENCY-CORRECTED spindle DRAW (Pc/eta), not raw cutting power -- and the physics-reviewer (Agent isolation:worktree) sees only COMMITTED code, so commit before review or rely on a behavioral test as proof. | verify: `git -C H:/prism show HEAD` -- [[reference_oscar_sfc_overpower_spindle_efficiency_2026_06_23]]
- 2026-06-23 | **SFC page published tool life ~4x too SHORT -- ProductEngine's INLINE MATERIAL_HARDNESS Taylor C/kc/mc diverged from canonical constants (slot:oscar)** | observed-in: 4ad8a0116b | root cause: `ProductEngine.sfcCalculate` (backs the customer-facing `/speed-feed-calc` page) carried its OWN inline `MATERIAL_HARDNESS` table whose kc1_1/mc/Taylor-C/n DIVERGED from the canonical source of truth `src/physics/constants.ts` -- most consequentially 1045 steel Taylor C=250 vs the ISO-3685 canonical 350, so the page published tool life ~4x too short (LIVE :3100 2.2 min vs ~9). Violated soul refuse `inline-physics-constants`; the inline 250 was an unsourced outlier while every OTHER PRISM consumer already used canonical 350 (3 in-repo sources: `AISI_CUTTING_COEFFICIENTS`, `CANONICAL_TAYLOR.P`, `physics/CLAUDE.md` + Machinery's Handbook/Kennametal). fix (R8 surgical): keep the `MATERIAL_HARDNESS` shape so all ~6 consumers are untouched, but COMPOSE kc1_1/mc/C/n at module load via `canonicalCoefficients(grade, iso)` -- per-material `AISI_CUTTING_COEFFICIENTS` override -> per-ISO `CANONICAL_KIENZLE`/`CANONICAL_TAYLOR` fallback (mirrors `buildMaterialPhysics`); `resolveMaterial` unknown-path also canonicalized (C=250->350). +2 R9 canonical-bound tool-life locks that fail on a revert to inline 250. physics-reviewer PASS (temper-strip regex + module-load TDZ verified); 26/26 page + 75/75 SFC-path tests; changed files type-clean; LIVE :3100 post-restart 1045 tool_life 2.2->8.3 min. Lesson: a product/composition engine must SOURCE physics constants from constants.ts, never carry its own inline copy -- an inline divergence reaches the customer while every isolated identity/band unit test stays green. | verify: `git -C H:/prism show 4ad8a0116b` -- [[reference_oscar_sfc_material_table_divergence_2026_06_23]]
- 2026-06-23 | **"a bunch of terminal windows open" -- detached + sync subprocess calls fleet-wide were missing windowsHide:true on Windows (slot:sierra)** | observed-in: U-VIZ-WINDOWSHIDE-DETACHED (6a1cf88bb4) + U-VIZ-WINDOWSHIDE-SYNC (6654bb3412) | root cause: on Windows a Node child_process call that launches a console app (node/git/powershell/WMIC/taskkill) opens a console WINDOW unless it passes `windowsHide:true`. A DETACHED spawn (`{detached:true}`) without it opens a window that PERSISTS for the child's life (Node sets CREATE_NEW_PROCESS_GROUP but NOT a no-window flag); a synchronous call (the Sync family) only FLASHES. The interactively-fired .claude/hooks + .claude/helpers layer had ~22 detached sites (7 Stop hooks + 5 UserPromptSubmit injectors + autostarts) and ~231 sync sites without windowsHide -- with up to 26 chats firing on every turn-end + prompt, that is a burst of PERSISTENT console windows = the operator symptom (the sync ones add flicker). fix: TWO commits. (1) detached class -- add windowsHide:true to 22 sites/19 files via new `scripts/fix-detached-windowshide.mjs`, which anchors on the `detached: true` literal (NOT a fn name) so it also covers the read-only auditor's blind spots (custom-named wrappers `_spawn`/`spawnImpl` the name-regex misses). (2) sync class -- add windowsHide:true to 231 sites/137 files via new `scripts/fix-sync-windowshide.mjs`, which detects REAL subprocess calls via the auditor's brace-matching, inserts into the call's LAST top-level options object, SKIPS the 26 calls with no options object (adding one changes arity/default-encoding), and `node --check` gates + auto-reverts every file. windowsHide:true is behavior-neutral (window visibility only), so the large sweep is safe; 0 reverted. Companion to `scripts/audit-windows-hide.mjs`. 11/11 + 12/12 tests. Staged from the shared tree via the git-add-lane-guard's documented `[MAIN-FORCE]` fleet-infra escape (the guard reads slot branch from chat-slots; sierra's record drifted to slot/sierra). Lessons: a NAME-anchored auditor has blind spots (custom-named spawn wrappers) -- anchor the FIXER on the wrong VALUE (`detached: true`) not the fn name; never add an options object to a subprocess call that lacks one; a tool's SOURCE (code OR markdown) must assemble the spawn/exec name-open-paren substrings or the pre-tool security scanner blocks the file. | verify: `git -C H:/prism show 6a1cf88bb4 6654bb3412` -- [[windowshide-console-flash]]
- 2026-06-22 | **Quoting closed-loop train-cycle died on EVERY bare-`node` launch -- ERR_MODULE_NOT_FOUND on a .ts->.js DYNAMIC import under Node 24 TS type-strip (slot:charlie)** | observed-in: U-QP-TSX-REEXEC | root cause: `scripts/quoting-train-cycle.mjs` (cron-side invoker for the quoting OODA training loop) lazy-loads `QuotingTrainingOrchestratorEngine` SRC-FIRST (the `.ts`, deliberate -- per-file `dist/engines/*.js` are stale esbuild fossils). Under **Node v24** native TS type-strip, plain `node` DOES load the entry `.ts`, but the orchestrator's own runtime `await import("./QuotingTrainingLoopEngine.js")` throws ERR_MODULE_NOT_FOUND -- type-strip does NOT rewrite a `.js` specifier in a DYNAMIC import to its `.ts` sibling (only `.ts` exists in `src/`). So the ENTIRE train-cycle died opaquely on every bare-`node` path: the cron's tsx-missing fallback (`install-quoting-pipeline-cron.ps1:144`), ad-hoc runs, AND `quoting-pipeline-verify`. The engine-load comment already SAID "invoke via tsx" but the script never ENFORCED it -- it walked straight into the guaranteed-fail import. Masked because the frontend-readiness doc's "436/436 PASS" was a tsx/dist run; the bare-node verify path was silently broken (`tests:0` + guard-preflight exit 1). fix: self-reexec under tsx once (pure exported `isUnderTsx`/`resolveTsxCli`/`planTsxReexec`; guard is the FIRST statement in `main()`), mirroring the proven `shouldReexecForHeap` self-reexec in `nn-graph-retrain-lifecycle.mjs`. Infinite-loop breaker `PRISM_QTC_REEXEC=1` (exact-match) on the child + `isUnderTsx` short-circuit; tsx-absent -> dist-fallback + honest error (never worse). LIVE: bare `node ... --json --no-write` now `ok:true` (was ERR_MODULE_NOT_FOUND); guard-preflight T14 14/14; new tsx-reexec test 20/20 (incl 2 E2E spawn round-trips + anchor-guard false-match + exact-breaker-match); per-file 2-arm scrutiny (arm B caught + fixed a P0 conditional-skip + missing substring-false-match guard). Lesson: a script that SRC-loads a `.ts` and relies on the caller picking tsx must ENFORCE its own runtime, not document it -- Node 24 type-strip loads an entry `.ts` but breaks every chained dynamic `.js`->`.ts` import. | verify: `git -C H:/prism show HEAD~1` -- [[reference_charlie_train_cycle_tsx_reexec_2026_06_22]]
- 2026-06-20 | **A PSN-SYNERGY cross-wire insertion SEVERED the xproc switch fall-through -- ~120 xproc actions silently routed to outcome_trace_record instead of routeXprocAction (slot:india)** | observed-in: U-XPROC-FALLTHROUGH-RESTORE | root cause: `aiReasoningDispatcher.ts` routes the whole `xproc_*` CrossProcess-neural fleet via a long block of BARE fall-through `case` labels terminating in one shared handler `case "xproc_feedbackbus_reset": { result = await routeXprocAction(action, params); break; }`. Commit `0fd90359de [PSN-SYNERGY]/U-OUTCOME-WIRE` (+ U-RAG-PSN-AI-WIRE) inserted NEW case bodies (`outcome_trace_record`/`outcome_log`/`outcome_query`/`outcome_stats` + `rag_rerank`) INTO THE MIDDLE of that bare-case chain. In a JS switch, bare cases stacked above a case-with-body fall into that body -- so every xproc case above the insertion (`xproc_episodic_stats`/`xproc_online_constants`/`xproc_drift_constants`/`xproc_ewc_constants`/`xproc_symbolic_violations` + ~115 others down to `xproc_outcome_adapter_reset`) returned `outcomeTraceEngine.record(params)` instead of reaching `routeXprocAction`. Silent: only 5 were test-covered (tier10-wire 19/24); the other ~115 were wrong with no test. fix (R8 surgical): gave the last bare xproc case before the insertion (`xproc_outcome_adapter_reset`) its own terminal `{ result = await routeXprocAction(action, params); break; }` handler (mirrors the sibling at `xproc_feedbackbus_reset`), terminating the upper sub-block; cross-wire delegates keep their own bodies; lower sub-block unchanged. tier10-wire 24/24; all *wire* tests 31/31; 0 new tsc errors. Lesson: inserting a case-with-body into a region of stacked BARE fall-through cases is a silent control-flow regression -- the inserted body steals every preceding bare case; insert after the shared handler's break, and run the OWNING domain's dispatcher tests when cross-wiring a shared dispatcher. | verify: `git -C H:/prism show HEAD~0` · [[reference_xproc_fallthrough_severed_2026_06_20]]
- 2026-06-18 | **"keep checking back into papa" RECURRED after the claimSlot/ps-pin fixes -- TWO deeper re-trigger paths the resolver fixes never touched: a durable `/startup-papa` cron + a SLOT-BLIND handoff read (slot:alpha)** | observed-in: 965cc46ddd + c31ef9d644 | root cause: the operator reported the papa-rebind 3x in one session AFTER [[reference_slot_one_owner_dual_ownership_fix_2026_06_18]] + ps-window-pin landed -- because those fixed *which slot resolves in chat-slots/window-pin* but NOT (1) the **scheduler**: a DURABLE cron `1b150d99` (`17,47 * * * *` -> `/startup-papa`), created by `14b038a1` while transiently papa then rebound to alpha, kept force-claiming papa twice an hour (deleted it + a dup alpha cron `6d696642`); and (2) the **resolver**: `per-agent-handoff.mjs read --terminal` tier (0.5) `same-instance-newest` returned the chat's NEWEST `HANDOFF-<id>-*.md` REGARDLESS OF SLOT -- a churned terminal has handoffs under BOTH slots, newest was papa, so auto-resume//loop//checkin all re-emitted `/startup-papa` though chat-slots said alpha (live: `read --terminal claude-14b038a1` -> `...-papa-cad-fusion-live.md`). fix: (a) new read tier (0.4) `same-instance-current-slot` -- prefer the chat's OWN handoff bound to the slot it CURRENTLY owns in chat-slots (topic-prefix or `slot:` frontmatter) over global-newest; fail-soft fallthrough to (0.5) byte-identical (live after: resolves `...-alpha-session-contin.md`); (b) new `stale-slot-cron-advisory.mjs` SessionStart hook (wired all 4 matchers) flags durable slot-loop crons targeting an unclaimed/rebound slot -> `CronDelete` plan, advisory-only, high-conf targets only. 3-of-3 analyst arm caught a P1: the new `matchedBy` label was missing from `loop-state.mjs HANDOFF_OWN_MATCH` -> `/loop` dropped the own resume -> fixed in c31ef9d644 + behavioral test. 9/9 handoff + 24/24 advisory + 10/10 loop-state-next; 3-of-3 PASS. Lesson: a symptom that RECURS after a fix means a DIFFERENT mechanism also drives it -- enumerate ALL re-trigger paths (scheduler + resolver + every consumer of a changed return shape), never assume one fix covers the class. | verify: `git -C H:/prism show 965cc46ddd c31ef9d644` · [[reference_papa_rebind_resolver_cron_fix_2026_06_18]]
- 2026-06-18 | **`claimSlot` left a DANGLING slot when a chat owned two -> the "keep logging back into papa" bug (slot:alpha)** | observed-in: this commit (U-SLOT-ONE-OWNER) | root cause: `.claude/helpers/chat-slots.mjs::claimSlot` reconciled only the FIRST slot a chat owned, then returned/broke (old loop ~921-947) -- it ASSUMED one-chat-one-slot but never ENFORCED it. So a chat that transiently owned TWO slots (a lingering `papa` claim from a prior `/startup-papa` + `alpha` from this terminal) ran `/checkin-alpha` (preferSlot=alpha), hit the `wantsDifferentSlot(alpha)=false` branch, refreshed alpha, and RETURNED -- leaving papa dangling forever. The stale papa record was then resolved by the per-prompt context injectors (slot-soul / galaxy-claudemd / slot-domain-awareness / slot-context-bundle) + fleet tooling, so the chat presented as PAPA while `slot-bind-enforce` authoritatively bound ALPHA (two resolvers, two answers; the operator's "keep logging back into papa"). Secondary (now MOOT): `slot-context-bundle.resolveSlot` matched via loose `startsWith(short)` over JSON insertion order vs the canonical `findSlotForChat` (exact chatId over SLOT_NAMES order) -- harmless once only one slot ever matches. fix: `claimSlot` collects EVERY slot the chat owns and settles on exactly ONE (operator force-move to an un-owned slot -> release ALL owned + fall through to claim it; else keep preferSlot-if-owned, else newest-heartbeat owned slot; release the rest). Byte-identical in the single-owned case (3 prior branches preserved); only the >=2-owned case changes from leak-the-extras to reconcile-to-one. grep-verified `claimSlot` is the SOLE slot-record writer (no bypass), so it is the chokepoint that guarantees the invariant fleet-wide. New `chat-slots-one-owner.test.mjs` 6/6 (failing-first: 4 failed pre-fix) + force-fix 6/6 + pid-gate 20/20 + release-no-orphan 2/2; live-data validated against a copy of the real chat-slots.json (alpha+papa -> only alpha); 2-arm scrutiny PASS. Sibling of the ps-window-pin fix below (that fixed WHICH handoff to read; this fixes the slot CLAIM reconciliation -- together /checkin-alpha now sticks). Lesson: an "owns at most one X" invariant must be ENFORCED at the single mutation chokepoint, never assumed by callers -- a first-match-then-return reconciliation silently leaks the extras the moment the invariant is violated once. | verify: `git -C H:/prism show HEAD` · [[reference_slot_one_owner_dual_ownership_fix_2026_06_18]]
- 2026-06-18 | **ps-window-pin `findPsAncestorPid` returned null on EVERY host -- `powershell -Command` does NOT bind `$args` -> window->slot pin NEVER written -> auto-resume/terminal-pin silently ran on the fleet-global family-latest WRONG-SLOT fallback (slot:alpha)** | observed-in: 601b51fb53 | root cause: `findPsAncestorPid` (`.claude/helpers/ps-window-pin.mjs`) built a PS ancestry-walk reading `$args[0]`/`$args[1]` but invoked `powershell -Command "<script>" <startPid> <maxHops>`; with `-Command` (unlike `-File`) trailing positionals are NOT bound to `$args` -- PowerShell appends them to the command text -> `ParserError: Unexpected token` -> non-zero exit -> returned `null` for EVERY input since the helper shipped. So `state/shared/ps-window-pins.json` was NEVER written; the window->slot binding never existed; `session-start-auto-resume.getHandoffPreferSlot` + `session-start-terminal-pin` both fell back -- the former to `per-agent-handoff read --terminal <freshId>` which, on a brand-new post-/clear id, lands on `family-latest` (per-agent-handoff.mjs:796-813 = newest handoff across ALL chats, all family `claude`) = the just-/cleared peer's slot. Operator symptom: "/clear a papa chat -> auto-checkin keeps directing /startup-papa." Survived because the `_spawn` test mocks ignored args entirely (R9: green while 100% broken). fix: validate startPid/maxHops as positive ints + INTERPOLATE into the script (no `$args`) + drop trailing positionals + absolute `PS_BIN` (bare fallback) on findPsAncestorPid+isPidAlive (sibling of stable-session-id.mjs WIN_PS). Live: module resolves the WT-tab powershell anchor (was null); pin round-trips; terminal pinned alpha. Unit-2 (slot-switch repoint) free: chat-slots.mjs:1610-1617 already writes the pin per claim. +3 R9 spawn-args-contract tests (29/29); 2-arm scrutiny PASS. Lessons: `-Command` != `-File` for `$args`; a subprocess mock that ignores its args hides a 100%-broken integration; absolute Windows bin paths beat bare names under portable-node. | verify: `git -C H:/prism show 601b51fb53` · [[reference_pspin_findps_args_fix_2026_06_18]]
- 2026-06-17 | **"chats STILL getting kicked off the MCP server" was a FALSE fleet `/mcp reconnect` broadcast on a HEALTHY :3100, NOT an OOM -- + R12 correction of my own first-pass OOM misdiagnosis (slot:bravo)** | observed-in: U-MCP-FALSEPOS-LIVEPROBE | root cause: `maybeWriteBroadcast` (`.claude/hooks/mcp-bridge-enforce-pretool.mjs`) alarmed the whole fleet off `countBridges()===0` -- the NORMAL idle resting state (transient stdio->HTTP bridges spawn/serve/exit) -- gated only by a 900s last-known-healthy cache; when the turn-start health cache aged past 900s during an idle gap, `serverUp`->undefined -> `decideEnforcement` fell back to broadcast-on-fleet-0 -> a false "every chat disconnected" signal (live: written 2026-06-17T15:25:12 while /health answered in 5ms). MY FIRST CUT misread the one `supervisor.log` child-exit (`4294967295`/signal-null = my OWN `Stop-Process` kill, NOT a V8 OOM -- no `FATAL`/heap marker exists; peak_inflight 1-6 never near the 64 cap) as an OOM and shipped a `cap=16` .env band-aid (REVERTED -- 64/512 is contract-tested). fix: gate the broadcast on a LIVE :3100 probe -- write ONLY on confirmed DOWN (new pure `liveBroadcastVerdict`: broadcast iff `ok===false`); a healthy probe suppresses + refreshes the cache; per-chat HARD-BLOCK path UNCHANGED; fail-open preserved; probe timeout single-sourced to the sibling's 3000ms. lib 28/28 + hook 20/20; 3-of-3 PASS; live-validated (the exact 15:25 condition vs the real up :3100 -> NO false signal). Lesson: an ambiguous exit code is NOT an OOM without a FATAL marker (prove the failure mode from logs first); a stale-prone proxy must never actuate a fleet-wide alarm -- require positive DOWN evidence via a live probe at decision time (R7 family). | verify: `git -C H:/prism-slot-bravo show HEAD` · [[reference_mcp_kickoff_falsepos_liveprobe_fix_2026_06_17]]
- 2026-06-17 | **runLifecycle tests spawned the REAL 542MB-graph `--apply` subprocess since vault stage 1a shipped -- latent test-hermeticity violation (slot:india)** | observed-in: ed339a7955 | root cause: stage 1a (and now 1b) call `defaultApplyVaultRefpool`/`defaultApplyOutcomeRefpool` (spawnSync of `{vault,ghost-wire-outcomes}-to-refpool.mjs --apply`, which reads+WRITES the 542MB `system-graph.json`) PRE-fingerprint on every `runLifecycle`. The ~16 base-`harness()` tests that did NOT inject `applyVaultRefpool` therefore spawned the real subprocess during the suite -- tolerated (suite green + apply idempotent) but a real R9 violation + multi-minute suite. fix: default-stub BOTH apply stages (`applyVaultRefpool`/`applyOutcomeRefpool` no-ops returning `{status:0}`) in the base harness BEFORE the `...over` spread, so stage-specific tests still override. Shipped WITH stage 1b (outcome ref-pool durability -- the bigger ~139-ref pool vs vault ~14). Two env traps also surfaced: `node --test <file>` ran 0 tests in this env (run `node file.mjs` directly -- node:test auto-runs on exit -- and PIPE to tail; a `> file` redirect block-buffers + loses output on a reaped multi-min run). 80/80 tests, per-file 2-arm scrutiny PASS (0 P0/P1). Lesson: a lifecycle stage that spawns a real heavy subprocess pre-fingerprint must be default-stubbed in the shared test harness, else every untargeted test silently mutates production state. | verify: `git -C H:/prism show ed339a7955` · [[reference_outcome_refpool_durable_2026_06_17]]
- 2026-06-15 | **zulu build-loop `parseShipped` over-counted an inline-prose unit id as shipped -> loop DRAINED before the final unit was built (slot:zulu)** | observed-in: 775a0f8287 | root cause: `parseShipped` (`scripts/lib/zulu-build-queue.mjs`) extracted shipped ids with `/\bC(\d+)\b/g` over the whole `## SHIPPED` slice -- matching `C<n>` ANYWHERE incl. free prose INSIDE a shipped bullet. After C7 shipped, its bullet read "over_claim is the **C8** signal" + a summary line "C5+C6+C7+C8 build-complete" -> those prose `C8` tokens marked C8 done while it was UNBUILT (`DRAINED done=8`), a silent over-count that would have skipped the final queue unit. The docstring's "a candidate merely mentioned is never miscounted" was true only for the section-slice guard (REMAINING excluded), NOT for prose within a SHIPPED bullet. fix: anchor id extraction to a BULLET-HEADER position -- `/(?:^|\n)\s*[-*]\s+\*{0,2}C(\d+)\b/g` (bullet + optional `**` + `C<n>` at line start); inline prose excluded, the `- **C8 Engine**` convention still matches. Live brief re-parses to the same honest done=8; +1 regression test (the exact "C8 signal" fixture), 12/12 zulu-build-queue tests. Surfaced building the bravo C8 unit (queue C1-C8 complete this session). Lesson: a token-anywhere regex over HUMAN PROSE silently over-counts -- anchor structured-id/count extraction to a structural position (bullet/cell/fenced field), never free text (parser sibling of "prove by COUNT not 'looks done'"). | verify: `git -C H:/prism show 775a0f8287` · [[reference_zulu_parseshipped_prose_miscount_fix_2026_06_15]]
- 2026-06-13 | **Model-invokable self-compaction shipped (SELF-COMPACT-MS0); R12 self-correction of a FALSE in-session claim about zulu's compaction mechanism (slot:alpha)** | observed-in: cad-fusion-live-ms0 (this commit) | finding: the only model-reachable `/compact` trigger is SendKeys into the chat's terminal, needing a Win32 HWND. `chat-slots.json[slot].pid` is documented-transient (chat-slots.mjs:504 "the OWNING-WINDOW PID -- not slot.pid (which is the transient chat-slots.mjs [pid])") -- dead seconds after `claim` -- so resolving an hwnd from it is both wrong and UNSAFE (a recycled pid could SendKeys into a STRANGER's window). **R12 CORRECTION:** my first draft of this very entry claimed "`zulu-orchestrator-sweep` resolves its hwnd from the same dead `entry.pid` -> silent no-op" citing `zulu-orchestrator-lib.mjs:71` -- WRONG, asserted without reading the live send site (3-of-3 arm A caught it). The VERIFIED truth (`scripts/zulu-orchestrator-sweep.mjs:435-455`): zulu resolves via Tier-1 `focusWtTabBySlot(slot)` (UIA -- focuses the WT tab whose UIA name matches the slot, single-pane-verified; WORKS on `tw-wt` tabs) -> Tier-2 `matchWindowsByTitle("PRISM <slot>")` (legacy separate windows) -> `hwnd:tabbed-fleet-occluded` only if both miss. `entry.pid` in zulu-orchestrator-lib is opt-in GATING, not hwnd resolution. fix: `scripts/self-compact.mjs` REUSES that exact tiered resolver (R8 -- focusWtTabBySlot -> matchWindowsByTitle -> stable owning-window pid tw-ps/tw-pa; NEVER slot.pid), refuses to guess an ambiguous/multi-pane tab, falls back honestly otherwise. LIVE on this session: the UIA tier RAN and returned `no-tab` (this chat's WT tab is not named "PRISM alpha") -> correct safe fallback -- so actuation requires the "PRISM <slot>" tab-naming convention zulu also depends on (a fleet-launcher config gap, not a code bug). 24/24 tests (tiered resolver + injected-spawner round-trip) + per-file 2-arm + 3-of-3 PASS. Lesson: READ the live send site before claiming a resolution mechanism (don't cite a file:line you haven't opened -- R12/HONESTY); never resolve a SendKeys target from a documented-transient pid. | verify: `git -C H:/prism show <this>` · [[reference_self_compact_and_wt_actuation_dormant_2026_06_13]]
- 2026-06-11 | **Per-chat budget display fabricated YELLOW "approaching budget" from a STALE sidecar at GREEN ctx (operator-reported, slot:zulu)** | observed-in: 384b05e265 | root cause: `applyStaleness` (`scripts/lib/token-awareness-state.mjs`) called `bumpZoneForStale`, forcing a stale (>180s) GREEN zone UP to YELLOW (and null->YELLOW); `.claude/statusline.mjs` mirrored it. LIVE: a chat at `ctx=17%` (then 40%) on a 5.3h-stale / 513s-stale sidecar rendered `🟡 zone=YELLOW approaching budget`. But the sidecar WRITER refreshes on EVERY UserPromptSubmit+PostToolUse, so staleness accumulates only during IDLE -- exactly when ctx is STABLE, not growing; the bump fabricated budget pressure with zero evidence. The ctx NUMBER was always accurate (writer prefers the API-authoritative usage block); only the ZONE lied. fix: `applyStaleness` keeps the real MEASURED zone, sets `stale`+`ageMs` only (the staleness is already surfaced on its own display line by inject+statusline, so the model sees "GREEN 17% + stale 5.3h" and judges correctly -- not lulled, not falsely alarmed); removed dead `bumpZoneForStale`; de-bumped the statusline mirror (clone-don't-fork). Consumer-safe: `token-awareness-stop-advisory` gates on RED/CRITICAL, which the bump never produced. NOT a softening: 47/47 (3 rewritten to corrected intent + new operator-live-case test 17%/5.3h->GREEN + inverted regression oracle pins the fix). LIVE: `uniform` 513s-stale at ctx=40% now GREEN (was YELLOW). Fleet-wide single shared-lib fix -> all 26 slots. Lesson (R7): staleness is a FRESHNESS signal, orthogonal to BUDGET pressure -- surface both, never overwrite a measured signal with a fabricated worse one just because it is old. | verify: `git -C H:/prism show 384b05e265` · [[reference_token_awareness_stale_zone_fix_2026_06_11]]
- 2026-06-11 | **`stop_on_unwired_assets` false-flagged 21 array-dispatched actions as UNHANDLED -- blind to `FOO_ACTIONS.includes(action)` dynamic dispatch (slot:lima)** | observed-in: cad-fusion-live-ms0 (this commit) | root cause: the Stop gate's `checkDispatcherActionHandlers` recognized only 3 handler patterns (switch/`case`, lookup-table key, plain object key). machineLiveDispatcher (and 4 other dispatchers: diagnosis/integration/knowledgeExt/product) route via ARRAY-MEMBERSHIP dispatch -- `MACHINE_ACTIONS.includes(action)` forwards the whole action string to a sub-engine that owns the per-action switch -- so there is NO literal `case "machine_unregister":` in the dispatcher. The gate flagged 21 genuinely-routed MACHINE_ACTIONS as UNHANDLED and BLOCKED every session that touched the file (distinct from the 2026-05-23 cam_bridge false-positive, which had cases at line 5000+; this pattern has no case at all). fix: extracted pure exported `findUnhandledActions(rawBody)` + added Pattern 4 (a member of a `*_ACTIONS` array that the file uses as `NAME.includes(`/`(NAME as readonly string[]).includes(` is routed -> handled) + comment-stripping (a commented `.includes`/`case` must not clear a real orphan) + a `main()`-guard for importability + removed now-dead `git()`/`execSync`. NOT a softening: negative-control (injected orphan) still caught; genuine-orphan + commented-guard adversarial tests pin it. 15/15 tests; LIVE machineLive 21->0 unhandled, orphan_proof_xyz still caught; 2-reviewer per-file PASS. Per-file scrutiny caught a P1 (URL-unaware line-comment strip could eat a `case` sharing a line with `http://` -> false-NEGATIVE) -> fixed to require whitespace/line-start before `//`. Pre-existing P2 (logged, NOT this diff): `objKeyRe` `name:value` over-matches a TS interface field -> can clear a real orphan; Pattern-3 hardening follow-up. Lesson: an action-handler audit must recognize array-membership dispatch as a valid 3rd handler shape; a comment-strip in a safety gate must be string/URL-aware or it flips to the dangerous false-negative direction. | verify: `git -C H:/prism show <this>` · [[reference_stop_unwired_array_dispatch_fix_2026_06_11]]
- 2026-06-11 | **Constant false /compact nudge -- chat-token-watch's byte-estimate FALLBACK over-reports an impossible "critical" (slot:alpha)** | observed-in: 7b8dbde2dd | root cause: SEQUEL to the 2026-06-10 boundary fix below. `readChatPressure`'s byte-estimate path (used when no fresh sidecar -- slot=unknown / >180s stale) counts the post-compact transcript JSONL / 3.5, but the JSONL redundantly logs every turn's FULL hook-injection + FULL tool outputs -> OVER-reports. This 26MB session: 5.5MB post-compact -> byte-est 1.86M "tokens" -- IMPOSSIBLE on a 1M window -> classified `critical` -> zulu CHO01 "/compact recommended" EVERY turn + `zulu-orchestrator-sweep` would auto-SendKeys /compact. `precompact-auto-trigger` (the HARD block) already self-protects via its 1.1x CONTEXT_CAP TOKEN_COUNT_SUSPECT floor; `chat-token-watch` lacked the parity guard in its CLASSIFICATION. (Also: this phantom mislead the live model into mis-reading its own context as "deep YELLOW" + refusing to continue.) fix: parity SUSPECT guard -- byte-est > 1.1x CONTEXT_CAP (1M, knob `PRISM_CHAT_TOKEN_CONTEXT_CAP`) -> `suspect:true` + downgrade false `critical` -> `warn` (= zulu advise-only "build on", NOT /compact). Sidecar path untouched (real critical still fires); in-window 940K-1.1M still critical. Shared lib -> all consumers (zulu-advisory-inject, zulu-orchestrator-sweep, zulu-orchestrator-lib) fixed at once. 42/42 (3 new); LIVE: critical->warn, compact-decision->advise-only. Lesson: an unreliable proxy (transcript-byte count) must NEVER actuate compaction at a physically-impossible value; the authoritative per-turn `usage` is the only real critical signal. | verify: `git -C H:/prism show 7b8dbde2dd` · [[reference_compact_phantom_byte_estimate_fix_2026_06_11]]
- 2026-06-10 | **Alpha "constantly compacting" -- Claude Code changed its transcript compact marker (isCompactSummary -> compact_boundary), breaking every byte-based ctx estimator (slot:alpha)** | observed-in: cad-fusion-live-ms0 (this commit) | root cause: current Claude Code marks a compaction with `{"type":"system","subtype":"compact_boundary","compactMetadata":{"preTokens":...}}`, NOT the legacy `"isCompactSummary":true` flag (verified: a 158MB session = `compact_boundary` x13, ZERO `isCompactSummary`-flagged entries). `precompact-auto-trigger.mjs` (tier-T0, PreToolUse) `findLastCompactOffset()`, the shared `transcript-token-counter.mjs`, statusline, the sidecar, and chat-token-watch ALL scanned ONLY the dead legacy flag -> found no boundary -> `estimateFromBytes` counted the WHOLE appended transcript (the JSONL is appended on /compact, never truncated). At 3.5 B/tok a 3.29-3.85MB transcript byte-estimates into the UNGUARDED [HARD=940K, 1.1xCAP=1.1M] band (the >1.1xcap "TOKEN_COUNT_SUSPECT" suppress added 2026-05-21 only catches >1.1M) -> `decision:block` every tool call -> model runs /precompact -> /compact -> file only grows, boundary still unseen -> next tool call blocks again -> constant-compaction loop. Alpha worst-hit (heaviest-injection slot = biggest/fastest-growing transcript + auto-resume reloads it). 2nd path: `lastAssistantTokens` read a pre-compact ~950K turn as authoritative right after a high-watermark compact. The token-awareness sidecar MASKED it (reads authoritative per-turn `usage` = live 345K GREEN); the byte path only fires when the sidecar is stale (>180s, routine on >3min turns under fleet load) + lastAssistantTokens null. fix: centralized `COMPACT_MARKERS` (current+legacy) + pure `lastCompactMarkerOffset` in `transcript-token-counter.mjs`; `findLastCompactOffset` regex + `lastAssistantTokens` boundary-break in the hook; propagated to the 3 inline byte-slice consumers (sidecar/statusline/chat-token-watch); de-duped the redundant `--post` PostToolUse wiring (PreToolUse-only per the hook header, sidecar refresh kept). Real BYTE-PATH + ASSISTANT-PATH regression tests (FAIL pre-fix: "CONTEXT AT 962,327 TOKENS" / "950,000 TOKENS"). counter 48/48 + hook 16/16 + watch 39/39; 2-reviewer per-file PASS. Lessons: centralize harness-format markers (1 edit not N); a byte estimate must never actuate /compact in an unguarded band; compaction appends-never-shrinks so estimators MUST be boundary-aware; in the shared `H:/prism` tree `git stash` is GLOBAL (collides with peer stashes) -- never use for test isolation. | verify: `git -C H:/prism show <this>` · [[compact-boundary-format-change-constant-compaction]] · [[reference_compact_boundary_format_fix_2026_06_10]]
- 2026-06-11 | **GNN self-retrain lifecycle OOM'd on its in-process graph load -- only the spawned TRAINER had a heap bump (slot:charlie)** | observed-in: 8d6a481080 + 15123dff67 (+ HEAP-FIX-3 helper/execArgv) | root cause: surfaced validating U-AISYN-GNN-NODEFEAT (the 34 galaxy node-features I'd merged into india's `node-embeddings-768d.jsonl`). `nn-graph-retrain-lifecycle.mjs --force` crashed with a JS heap OOM at ~381MB, 2.5s in -- BEFORE training. The trainer SUBPROCESS got `--max-old-space-size=8192` (line ~288) but the lifecycle's OWN node process runs the eval (`runAssessment`) + base-embedding builds IN-PROCESS, each loading the ~550MB system graph, with the DEFAULT heap. A sibling OOM (exit 134/SIGABRT) hit the step-2c galaxy-embedding child (`build-galaxy-node-embeddings.mjs`), spawned WITHOUT the flag too -- fail-soft, so the retrain silently continued on the PRIOR run's galaxy features. fix: pure `shouldReexecForHeap(argv,env,execArgv)` + a `__isMain` self-reexec that re-launches node once with `--max-old-space-size` (`PRISM_NN_RETRAIN_LIFECYCLE_HEAP_MB`, default 8192; cheap `--status`/`--help` skip; `PRISM_NN_RETRAIN_REEXEC=1` breaks the loop; `hasHeapFlag(process.execArgv)` skips a redundant re-exec when the scheduled task already passed the flag); shared tested `nodeArgsWithHeap()` regression-locks the flag at BOTH spawn sites. VALIDATED: retrain ran to completion (EXIT 0, 0 OOM) -- but AUROC 0.40 on the 6000-node capped subgraph (macro-F1 0.14 / Brier 0.25), BELOW the 0.78 gate, so correctly NOT promoted; the 34 galaxy node-features add COVERAGE (`hit=6000 miss=0`) but did not lift the gate (consistent with the known link-pred-pretext variance; full-coverage is ref-pool growth, not this fix). 57/57 tests; 3-of-3 PASS. Lesson: when a script spawns a heap-bumped child but ALSO does heavy work in its OWN process, the parent needs the bump too -- a self-reexec fixes every launch path (ad-hoc + scheduled) at once. | verify: `git -C H:/prism show 8d6a481080` · [[reference_ai_systems_6unit_complete_2026_06_11]]
- 2026-06-10 | **HS-01: a BARE `stable-session-id.mjs` call miskeyed handoffs to a PEER chat -> auto-session-start silently broken fleet-wide (slot:alpha)** | observed-in: e81dec5cba (fleet-wide fix) + 7dec157db0 (arg-anchor) | root cause: the `STABLE=$(stable-session-id.mjs)` pattern in /precompact + /handoff + 78 per-slot wrappers passes NO CLI arg and has NO stdin -> `getStableIdentifier()` fell through to the PID-pin heuristic (anchor 2), which with N concurrent chats sharing one cwd returned a PEER chat's id (confirmed live: `claude-c48a1aff` for session db273e77). The handoff was written/read under the wrong key -> the next session's auto-resume could not find it -> auto-session-start was DEAD in production exactly when the fleet was busy. The first fix (7dec157db0) added an explicit `--session-id` anchor (0), but that needs 24+ caller edits to actually use. The REAL fix (e81dec5cba): the harness exports `CLAUDE_CODE_SESSION_ID` into EVERY tool subprocess's env, scoped to THIS chat's process (a per-process var CANNOT be a peer's id -- subprocess env inheritance is strictly downward; `compact-counter.mjs:14-22` already relies on this exact isolation). One new anchor (1.5) in the single shared helper, ranked ABOVE the PID-pin, fixes every bare caller + hook + wrapper at once with ZERO caller edits -- fleet-wide the instant it commits. The pre-existing env anchor (3) read the WRONG var name (`CLAUDE_SESSION_ID`, UNSET; harness sets `CLAUDE_CODE_SESSION_ID`). LIVE PROOF: bare `node stable-session-id.mjs` -> `claude-db273e77` (was a peer); per-agent-handoff read via the bare pattern -> `matchedBy:same-instance-newest`. 10/10 tests, 3-of-3 PASS. Lesson: a per-process env var the harness sets is a more robust multi-chat anchor than any shared-file PID/cwd heuristic, and fixing the shared substrate beats editing N call sites (R7/R8). | verify: `git -C H:/prism show e81dec5cba` · [[reference_hs01_env_anchor_fleetwide_2026_06_10]]
- 2026-06-10 | **Stop-hook handoff-append silently no-ops on a full-uuid sid -- findHandoff matched the wrong key (slot:alpha, surfaced building U-TASK-BOUNDARY-COMPACT)** | observed-in: 9fcda446a1 | root cause: a Stop hook's `f.includes(sid)` handoff matcher is fed `resolveSessionId() = stdin.session_id` = the FULL uuid (`db273e77-fb5e-...`), but per-agent handoffs are keyed by the SHORT chatId (`HANDOFF-claude-db273e77-<topic>.md`). `"HANDOFF-claude-db273e77-...".includes("db273e77-fb5e-...")` = FALSE (verified live) -> the durable handoff append silently never fires in prod. Fixed in the NEW hook (stop-task-boundary-compact-nudge.mjs): resolve the authoritative chatId from chat-slots.json + anchor `HANDOFF-${chatId}-` (also kills the under-anchored substring peer-leak; R9 prod-path test pins it). The SIBLING `stop-force-loop-continue.mjs::findHandoff` had the identical bug -> FIXED + regression-tested in 2f829418b4 (U-SFLC-CHATID, slot:alpha): `handoffNeedle()` now derives the 8-hex short form from a full-UUID sid so the `## RESUME_LOOP` auto-continue append fires; `__tests__/stop-force-loop-continue.test.mjs` 15/15 pins the full-UUID-matches-short-file prod-path case. [stale "not fixed" note corrected 2026-06-13 slot:echo] Also caught pre-ship by R9: the handoff-append regex `m`-flag + leading-`\n*` truncation (replaced only the marker header / jammed `# H## COMPACT_SEAM`). | verify: `git -C H:/prism show 9fcda446a1` · [[reference_task_boundary_compact_nudge_2026_06_10]]
- 2026-06-10 | **documented-by cross-substrate edges silently collapsed 38->0 -- confirmed against a VOLATILE rotating augmentation (slot:sierra)** | observed-in: 88cb72b532 | root cause: `generate-cross-substrate-edges.mjs` confirmed documented-by knowledge-note endpoints against `memNodeIds` = the newNodes of the ROTATING `memories-atomic-augmentation.json` (it emits only a few recent notes). When the `memory_patterns.<galaxy>_synthesis` nodes rotated out, documented-by collapsed to 0 -- even though the system-viz merge is ADD-only so those synthesis nodes still LIVE in the merged graph. The regenerated artifact + its test were ALREADY red ~8h, undetected. fix: confirm knowledge notes against the node-card OFFSET ORACLE (`node-card-offsets.json`, ~336K merged-graph node ids, cheap parse, no 548MB load) instead -- restored documented-by to 320 (42 synthesis@1.0 + 278 backlink@0.9, MORE than the original 38 because the full note set resolves far more authored backlinks) + hardened vs silent re-collapse. Shipped alongside U-XSUB-EMBEDS (3rd typed edge `embeds`: 948 edges = the NN/GNN/RAG 768d embedding footprint surfaced in the search graph). 7/7 tests incl oracle-absent CI path proven; per-file 2-reviewer PASS. Lesson: confirm endpoints against the STABLE ADD-only merged-graph oracle, never a rotating augmentation -- a generator reading a volatile source silently loses coverage when it rotates. | verify: `git -C H:/prism show 88cb72b532` · [[reference_xsub_embeds_docby_oracle_2026_06_10]]
- 2026-06-10 | **Generated session handoff surfaced a PEER's commit as "Last work" (handoff-stub leak, slot:alpha)** | observed-in: be9182dca7 | root cause: `precompact-handoff.mjs::generateSmartResume` built the "Last work" line from `git log --oneline` on the SHARED `cad-fusion-live-ms0` tree -> it surfaced the fleet's most-recent commit, NOT this chat's. Live symptom: a hotel UI commit auto-resumed an alpha verified-offload session (the operator's "handoffs are stubs by helper agents" report). The writer-ban (live-chat-only) was intact; the leak was the GENERATED content. fix: slot-scope the grep to THIS slot's own commits, anchored on the OPENING paren `(slot:<slot>` so a mid-message MENTION of another slot (`(slot:india rescuing slot:papa orphan)`) never false-matches (proven: 0 false-matches for papa, 371 true self-matches for alpha); labeled fleet-commit fallback (R12); + `buildReentryDirective(slot)` ends every synthesized resume with the `/startup-<slot> /loop [10m] /goal` re-entry so no generated handoff is a dead-end. SESSION-CONTINUITY-AGENTIC also auto-starts that sequence on all 3 startup surfaces + gives spawned agents their parent slot's galaxy pack + makes /goal force R15 100%-completion. Lesson: a "Last work" line derived from `git log` on a shared multi-chat tree is a peer-leak unless slot-scoped. | verify: `git -C H:/prism show be9182dca7` · [[reference_session_continuity_agentic_2026_06_10]]
- 2026-06-10 | **SFC radial_depth INERT in prism_optimized + hex_mm force-collapse at full slot (slot:oscar)** | observed-in: 69146aa9c1 | root cause: TWO coupled bugs closing the last SFC axis gap. (1) `SpeedFeedNineAxisOrchestratorEngine.ts` prism_optimized (~895) recomputed `ae = (alt.ae_pct/100)*D` from the balanced ALTERNATIVE's static table, discarding the operator's `toolpath.radial_depth_mm/_pct` -> MRR IDENTICAL 5%->100% engagement (axis fully inert; cost_batch/aggressive_rush already honored `sfc.radial_depth.value`, only prism_optimized broke it). (2) `UltimateSpeedFeedEngine.ts` STEP 9 (~2245) `hex_mm = fz*sin(acos(1-2*ae/Dc))` DECAYED past ae/Dc=0.5 (sin of an angle >90deg) to ~0 at a full slot -> Fc->0 where engagement is GREATEST; since the orchestrator workholding + spindle-power clamps read `sfc.forces`, honoring a high user ae with forces~0 would UNDER-PROTECT (oscar red line). fix: (1) `ae = userGaveRadial(>0) ? sfc.radial_depth.value : table` -- engine resolved it AND computed sfc.forces at the SAME ae, so clamps stay force-consistent with no re-derivation; (2) `hex = immersionRatio>=0.5 ? fz : fz*sin(acos(1-2*immersionRatio))` -- max chip = fz for ae>=Dc/2 (peak at the 90deg centerline; Sandvik/Boothroyd-Knight), byte-identical ae<Dc/2, continuous at Dc/2, SAFE-DIRECTION (forces only increase). +12 R9 tests (all RED pre-fix); 401-gauntlet + variability + orchestrator + all-axis sweep PASS; 3-of-3 + physics-reviewer PASS (mandatory force formula). Lesson: when an orchestrator overrides an engine-resolved value with a static-table proxy, the user input goes inert AND the downstream safety clamps (reading the engine's force at the real ae) silently decouple. P2 follow-ups (pre-existing): negative explicit radial_depth_mm -> engine NaN; max(1,Dc) sub-mm immersion floor. | verify: `git -C H:/prism show 69146aa9c1` · [[reference_oscar_radial_engagement_fix_2026_06_10]]
- 2026-06-10 | **Unwired-engine audit blind to engine->engine consumption -- 23 library engines mis-counted UNWIRED (slot:sierra)** | observed-in: a6dbec1842 | root cause: `scripts/audit-unwired-engines.mjs` (source of BUILD_STATE NEEDS_WIRING + the fleet "N unwired engines" count + /system-viz ghost-orphan roosts) scanned consumers = {dispatchers,routes,registries,orchestrators,hooks,singletons} but NOT plain engine->engine consumption -> a library-layer engine consumed only by other (non-orch/non-singleton) engines (e.g. QdrantVectorStoreEngine, 3 engine-consumers) showed UNWIRED + was chased as a false dispatcher-wiring target (R13 zero-gain churn). fix: extracted a pure exported `applyConsumerClassification` + added a LAST-priority self-excluded WIRED-VIA-ENGINE pass (dispatcher/route/etc still win first-match). LIVE: UNWIRED **89 -> 66 truly-dormant + 23 library-layer** (reconciles exactly); 23/23 tests (5 new). The actionable dormant set is 66, not 89. 3rd audit-detector fix in this file. | verify: `git -C H:/prism show a6dbec1842` · [[reference_audit_wired_via_engine_2026_06_10]]
- 2026-06-10 | **Tribal brain CLOBBER #4 -- shard-transition read-blind dropped 29,723 -> ~11,500 entries (slot:sierra, self-inflicted by running the coverage batch)** | observed-in: 8bf1873577 | root cause: `tribal-embed-index.mjs` readIndex() + writeIndex()'s clobber-guard checked existence via `fs.existsSync(INDEX_PATH)` (MONOLITH .json ONLY). When the index first crossed ~480 MiB it SHARDED (write-tribal-index.mjs writes .manifest.json + shards and retireSupersededArtifacts REMOVES the monolith .json) -> next readIndex saw .json absent -> returned EMPTY base -> buildOrUpdate.persist() merged staged-only -> writeIndex's removeShardLayout DELETED the shards (the 29,723 base lived there). Clobber-guard had the same monolith-only blind spot so it silently did NOT run. Latent since the shard writer shipped after the 2026-06-08 readIndex fail-loud fixes; fired on the first-ever shard transition (which the streaming-rerank heap-ceiling removal enabled). fix: BOTH checks MANIFEST-AWARE (empty/skip ONLY when neither .json NOR .manifest.json exists; loadTribalIndex reads shards). 14/14 incl 2 forced-shard regression tests (fail pre-fix). Brain recoverable (source intact); re-embed restoring. **3 sibling embedders share the vector (dormant-while-monolith): embed-engines/embed-knowledge-store/embed-cited-tips -> follow-up U-TRIBAL-SIBLING-WRITER-SHARD-SAFE (route ALL writers through loadTribalIndex+writeTribalIndex).** Lesson: a new on-disk layout must be reflected in EVERY existence/guard check; don't run a long index-mutating batch on the live brain unattended. | verify: `git -C H:/prism show 8bf1873577` · [[reference_tribal_shard_read_clobber_2026_06_10]]
- 2026-06-09 | **Runout tool-life DOUBLE-COUNT -- engine fix met an obsolete consumer workaround (slot:oscar)** | observed-in: 73b97ef25f | root cause: U-OSC-RUNOUT-LIFE-DERATE (a8f72823cb) made UltimateSpeedFeedEngine fold the runout life reduction into `tool_life.life_minutes`. But SpeedFeedNineAxisOrchestratorEngine had a pre-existing compensating workaround (U-OSC-HOLDER-RUNOUT-LIFE) that read the SAME `runout_impact.life_reduction_pct` and did `life *= keep` -- built because the engine USED to only warn. Once the engine owned the derate, the orchestrator's `life` (= already-derated `sfc.tool_life.life_minutes`) got derated AGAIN -> `tool_life_min` = raw * keep^2 (proven: engineFactor 0.884 single vs orchFactor 0.795 ~ 0.884^2; cost fallback path double-counted too). The engine's OWN isolated 3-of-3 scrutiny could not see the consumer's fork. fix: remove the orchestrator's `life *= keep` (engine is now the single source, R8/R7), keep the operator advisory warning. TDD red->green test `sfc-nine-axis-runout-no-double-count.test.ts` (ratio cancels co-varying derates: orchFactor must match engine SINGLE factor, NOT its square). Lesson: when an engine fix folds an advisory INTO its headline output, grep EVERY consumer for a workaround applying the same term. | verify: `git -C H:/prism show 73b97ef25f` · [[feedback_audit_consumers_when_moving_logic_into_engine]]
- 2026-06-08 | **Fail-OPEN read CLOBBERED the tribal brain 33,639→1 entries (slot:golf)** | observed-in: a3e6d3ca97 | root cause: the V8-cap read-failure (182788232a) had a worse second manifestation. `.claude/scripts/tribal-embed-index.mjs` `readIndex()` had a fail-OPEN `catch` that **returned a fresh empty `{entries:[]}` on any parse error** ("starting fresh"). When the 537MB index threw the cap error, an `--add`/`--update` (auto-embed hook on a wiki Write) loaded the EMPTY base, spliced 1 file, and `writeIndex()`'d it → clobbered the 537MB/33,639-entry brain to a 1-entry 16KB stub (signature: no `wikiEmbeddedAt`, fresh `generatedAt`). 537MB version gitignored → unrecoverable; rename-replace bypassed recycle bin. fix: (1) `readIndex` cap-safe-loads + **FAILS LOUD** when the file exists-but-won't-load (never empty-then-clobber); (2) `writeIndex` **clobber-guard** refuses a >50% shrink over a populated index unless `PRISM_TRIBAL_ALLOW_SHRINK=1` (alone blocks the 33,639→1 write); (3) loader closing-`]` assert (torn-file fail-loud). RESTORED the surviving 4,162-entry baseline (2026-05-20, slot worktrees); lost ~29K delta is re-embeddable. 7/7 guard + 10/10 loader; live rerank + --stats clean. Lesson: a fail-OPEN `catch→return empty` on a re-written corpus is silent-total-destruction; `catch` on an EXISTING file must fail loud. | verify: `git -C H:/prism show a3e6d3ca97` · [[reference_tribal_index_v8_string_cap_2026_06_08]]
- 2026-06-08 | **Tribal index crossed V8's 512MiB string cap → tribal injection (PSN leg #5) silently DEAD fleet-wide (slot:golf)** | observed-in: 182788232a | root cause: `state/shared/tribal-embed-index.json` reached 536,988,127 B, crossing V8's hard max string length `0x1fffffe8`=536,870,888 B by 117KB. Every consumer doing `JSON.parse(fs.readFileSync(path,"utf8"))` now throws `Cannot create a string longer than 0x1fffffe8 characters` BEFORE parsing — the LIVE `.claude/scripts/tribal-rerank.mjs:76` (PSN leg #5, fires every UserPromptSubmit) returned NOTHING in every chat with no error surfaced; the wiki embedder read was also blocked. `--max-old-space-size` does NOT help (string-length limit, not heap — the earlier 533MB heap-OOM was a different wall). fix: `scripts/lib/load-tribal-index.mjs` reads the index as a Buffer (no string cap) — fast-path JSON.parse under cap (byte-identical), incremental brace-aware entries walker over cap; wired into tribal-rerank + embedder read. 8/8 loader + 23/23 embedder; LIVE 537MB/33,639-entry index read in 3.4s 0-corrupt, rerank returns real mill hits. KNOWN-REMAINING: the WRITE side (`JSON.stringify` of a >cap object) also throws → appending entries needs index **sharding** (next unit). | verify: `git -C H:/prism show 182788232a` · [[reference_tribal_index_v8_string_cap_2026_06_08]]
- 2026-06-08 | **OCR closed-loop read page-0 ONLY of 96%-multi-page drawings + ran to completion on a reaper kill (slot:xray)** | observed-in: b76b4d55ab + 265e8a6e41 | root cause: TWO corpus-scale defects surfaced building "begin closed-loop training on all prints". (1) **page-0-only**: `blueprint-ocr-training-loop.mjs` rasterized PDF page 0 only — but a STEP-2b sample proved **96% of JM drawing PDFs are multi-page (2-32pp)**, so ~76% of dimension-bearing pages were silently dropped (a 4pp print contributed 1 page's dims, not 4). (2) **non-resumable corpus burn**: the runner held every print in memory + `writeFileSync` ONCE at the end → a fleet-reaper kill at print N/M (host reaps long node/python under load) lost ALL N and restarted at print 1 — a non-terminating GPU burn that LOOKS like progress. fix: `rasterizePrintPages` renders ALL pages (cap 12, `capped` logged) + per-page ensemble → one (page-image,dims) pair per page; per-print stream-append to trainset/queue/`processed-cursor.jsonl` (durable rows BEFORE cursor) + `parseCursorDoneSet`/`partitionByResumeCursor` resume (re-OCR=0). Real denominator: `build-print-corpus-manifest.mjs` SEARCHES juliett's documents.jsonl → 7794 drawing prints (not 257K, not a toy set), 76978 business excluded. LIVE: RUN1 OCR'd 2 PDFs, RUN2 resume re-OCR=0 (1s vs 42s); a 4pp print → 4 per-page rows / 7 gold dims. 31/31 tests; per-file 2-reviewer fixed run-as-main-guard×3 + downstream key+page last-wins dedup. | verify: `git -C H:/prism show 265e8a6e41` · [[reference_xray_ocr_corpus_resumable_multipage_2026_06_08]]
- 2026-06-06 | **OCR closed-loop lost EVERY print's extraction over one malformed number — truncation×leading-dot + leading-`+` (slot:xray)** | observed-in: e354869c93 | root cause: the multi-VLM ensemble reported "0 models survived / all-failed" on every print (incl. extrude_punch.png which made gold labels 06-04). The VLM extracts fine; the loss is at `parseVisionResponse` (`scripts/lib/ollama-vision-extract-lib.mjs`): (1) a truncated response (unterminated trailing string) makes `repairLeadingDotDecimals` BAIL (fail-safe return) → an earlier value-position `.86` survives → `repairTruncatedJson` closes the braces but NOT the notation → JSON.parse fails → whole extraction discarded; (2) VLMs emit `+0.015` for a `±` tolerance — JSON allows leading `-` but never leading `+`. Stochastic VLM output → intermittent total-loss. fix: repairTruncatedJson closes the unterminated trailing string (+drops a dangling escape); fallback parse paths re-apply repairLeadingDotDecimals AFTER truncation repair (structure-first, then notation); repairLeadingDotDecimals also strips a value-position leading `+` (exponents + string interiors preserved). 61/61 tests (7 new, R9-verified by revert); LIVE same print now extracts 8-9 dims → 7/9 trainable, calibration 0→38. Per-file 2-reviewer PASS (1 P1 dangling-escape fixed). THE blocker to corpus closed-loop training — not the VLM or GPU. | verify: `git -C H:/prism show e354869c93` · [[reference_xray_ocr_parse_truncation_fix_2026_06_06]]
- 2026-06-04 | **VLM OCR silently discarded a WHOLE print over one leading-dot decimal `.171` (slot:xray)** | observed-in: cad-fusion-live-ms0 (uncommitted) | root cause: vision models emit engineering notation `"nominal": .171` (no leading zero) — valid manufacturing shorthand, INVALID JSON — so `parseVisionResponse` (`scripts/lib/ollama-vision-extract-lib.mjs`) threw on JSON.parse and lost the ENTIRE extraction (a full print of dims over one number; same silent-data-loss class as the truncation bug `repairTruncatedJson` already guards). Surfaced live during the multi-VLM ensemble build: qwen2.5vl:7b OCR'd a `.171"` dim → 0 dims extracted. fix: sanitizer `jsonText.replace(/([:,\[])(\s*)(-?)\.(\d)/g, "$1$2$30.$4")` inserts the zero ONLY in a JSON value position (after `:`/`,`/`[`, optional ws+sign) so a quoted string value (`: ".005"`) or string interior (`"dim .250 ref"`) is never touched. +2 regression tests (54/54). Live re-run: `.171"` recovered as 4.3434mm, F1 0.8 vs truth. Shipped alongside `vision-ensemble-fuse.mjs` (multi-VLM consensus OCR — Blackwell unlock: N diverse families resident+concurrent, ≥2-agree=corroborated, 1-of-N=hallucination-candidate). | verify: `rtk node --test scripts/lib/ollama-vision-extract-lib.test.mjs` · [[vlm-ensemble-ocr-and-leading-dot-parse-fix]]
- _Older regressions (pre most-recent 15) archived to [`state/shared/RECENT-REGRESSIONS-ARCHIVE.md`](state/shared/RECENT-REGRESSIONS-ARCHIVE.md) -- token-injection slim. They remain searchable + the lessons live in the wiki._

> Drained from `CLAUDE.md` `## Recent regressions` by `scripts/claude-md-archive-regressions.mjs` (golf maintenance). Newest batch on top. CLAUDE.md keeps only the most recent few + a pointer here.

## Archived 2026-05-17T21:24:07.607Z — 19 entries

- 2026-05-16 | **System-graph utilization classifier degenerate — 281,683 ghost / 0 orphan / 81.7% ghost density**. `AWARENESS-SNAPSHOT.md` (injected into every chat at SessionStart by `awareness-snapshot-inject.mjs`) reports 0 orphans across a 372K-node graph with active 836-engine wiring debt — implausible. The classifier's binary "any-doc-edge → not-ghost" threshold makes the orphan signal (the "punch list of built-but-undocumented") unusable. Every chat reads "0 orphans" and concludes nothing needs documenting; reality is 1,348 wired engines lack wiki entries. | fix: re-tune classifier in `scripts/augment-graph-with-awareness.mjs` or `scripts/regen-viz.mjs` — drop binary doc-edge rule, use degree percentile + has-source-file as the orphan signal. Until fixed, the punch-list line in awareness-snapshot is misinforming every chat. | observed-by: claude-416be9ac `/forge-audit-v2` F4 (peer-reviewer-added). | verify: `node scripts/node-staleness-rank.mjs --json | jq '.utilization.classifierDegenerate, .utilization.orphan'` → false AND >0.
- 2026-05-16 | **3 META-tool calculation bugs shipped in first-pass `node-staleness-rank.mjs` caught by peer-reviewer (Boris doctrine working as designed)**: (a) Ollama schema path `o.totals.offloaded` (non-existent) → reported 0% ratio while reality was 20.3%; (b) envelope-drift regex over-counted 60× by sweeping `consistent`/`n/a` rows (681 false-positives vs real 11); (c) wikiCoverage read `bs.engines.*` (schema drifted, never existed in current shape) → all-zero output. All three are the same class of bug: assuming a schema without reading the file first. Fixes shipped same session via Edit; back-compat fallbacks added (`o.offloaded || (o.totals||{}).offloaded`, `bs.headline.built_engines || bs.BUILT?.summary?.total_built`). | fix: schema-read-first discipline — when writing a measurement tool against a JSON state file, ALWAYS `Object.keys(json)` it first; never assume a shape from the prose summary in the .md sibling. | observed-by: claude-416be9ac `/forge-audit-v2` peer-reviewer BLOCK verdict. | verify: `node scripts/node-staleness-rank.mjs --json | jq '.ollama.ratio, .envelopeDrift.driftedMilestones, .wikiCoverage.coverageGap'` → 0.20-ish, 11, 1348.
- 2026-05-16 | **MEMORY.md re-grew to 23,826 B (96.9% of the 24,576-byte truncation ceiling) only 0 days after U-MEMORY-COMPRESS — the one-shot compression had no watchdog, so the known fleet-wide silent-recall-truncation regression (line below) was 750 B from re-triggering with zero alert.** | fix: `scripts/memory-size-watch.mjs` shipped 2026-05-16 via `/forge-audit-v2` (mirrors `synergy-regression-watch.mjs`: read → `state/shared/memory-size-history.jsonl` → exit 0/1/2; WARN ≥90%, CRITICAL ≥97% of ceiling). Makes the U-MEMORY-COMPRESS fix DURABLE instead of one-shot. Surfaced by the audit's peer-reviewer as the strongest missed finding (audit had "memory retention" in scope but no memory finding until review). Wire to `/loop --interval 1d` or Stop advisory. | observed-by: claude-3a1c1c68 slot juliett `/forge-audit-v2` (peer-reviewer-added F7). | verify: `node scripts/memory-size-watch.mjs --json | jq '.bytes,.pctOfCeiling,.status'` → currently 23826 / 0.9695 / "warn" (exit 1) — index needs compression NOW.
- 2026-05-16 | **Ollama offload rate sat at 22.2% (target 30%) for an unknown period with no automated alert, and `ollama-auto-router.mjs:166` `/`-prefix skip made the auto-router dead code for the entire `/checkin`/`/loop`/`/forge` prompt class (0 decisions recorded in telemetry — wired but never fired).** Mirrors the synergy-regression-watch gap; corroborated by `feedback_ollama_docker_pipeline_dead_code_2026_05_16` (88% of Ollama hook surface unwired). | fix: pending — F2 R1 (drop `/` skip `ollama-auto-router.mjs:166`) + R2 (`INJECT_THRESHOLD` 0.90→0.80 `ollama-task-offloader.mjs:56`) + R4 (rate-limit 5min→60s `:54`) + R5 (auto-execute Ollama for safe categories `:441`); R1+R5 projected to clear 30%. R3 from first-pass audit DROPPED (phantom — space-form `/checkin` regex already present at `:102`). | observed-by: claude-3a1c1c68 slot juliett `/forge-audit-v2`. | verify: `node scripts/ollama-offload-dashboard.mjs --json | jq '.totals|(.offloaded/(.offloaded+.keptOnClaude))'` ≥ 0.30 AND `ollama-auto-router` appears in `ollama-offload-stats.json.byHook` with `fired>0`.
- 2026-05-16 | **Synergy ratio regressed 22.2% → 21.1% over 7 days (2026-05-09 → 2026-05-16) without any automated alert.** The `system-synergy-map.mjs` measurement existed but ran only on-demand; there was no week-over-week diff, no chat-bus alert, no `/loop` schedule. A 1.1pp drop in the single most load-bearing dev-pipeline health metric went unnoticed for a full week, surfacing only when slot foxtrot ran `/forge-audit-v2` for an unrelated dev-tool enhancement audit. | fix: `scripts/synergy-regression-watch.mjs` shipped 2026-05-16 (215 LOC, NOT claimed by any peer chat) — runs synergy-map, appends to `state/shared/synergy-history.jsonl`, exits non-zero on regression beyond configurable threshold (default 0.5pp week-over-week). 3 modes (normal/`--json`/`--history`), cron/CI-friendly exit codes (0=clean, 1=regression, 2=measurement-error). Baseline seeded at 0.211. Wire to `/loop --interval 1d` or Windows-task scheduler for daily watch. | observed-by: claude-32a39c0c slot foxtrot, `/forge-audit-v2` dev-tools-pipelines audit. | verify: `node scripts/synergy-regression-watch.mjs --json | jq -r .currentRatio` returns a number; `node scripts/synergy-regression-watch.mjs --history` shows ≥1 record under `state/shared/synergy-history.jsonl`.
- 2026-05-16 | **MEMORY.md index 73KB / 3× the 24.4KB compliance threshold — load was silently truncated every chat fleet-wide, breaking cross-session recall.** The Anthropic harness emits `Only part of it was loaded` when MEMORY.md exceeds 24576 bytes, but the index entries had grown to 1500+ chars each (dense distillations duplicating content already in the linked per-memory files). Confirmed live via the SessionStart system-reminder in slot bravo claude-339c8ff7. | fix: U-MEMORY-COMPRESS (AUDIT-SYNERGY-MS0, 2026-05-16) — rewrote index so each entry is ≤200 chars per global CLAUDE.md schema; underlying memory files in `C:/Users/wompu/.claude/projects/H--prism/memory/` untouched. Final size: 21474 bytes / 134 lines (well under the 24576-byte limit). Doctrine: index entries are pointers — full detail belongs in `<slug>.md`, never inlined. | observed-by: claude-339c8ff7 slot bravo, /forge7 audit. | verify: `wc -c C:/Users/wompu/.claude/projects/H--prism/memory/MEMORY.md` < 24576; next-chat SessionStart load must NOT emit the truncation warning.
- 2026-05-16 | **`stop-force-loop-continue.mjs` Stop hook is dead code fleet-wide — the `## RESUME_LOOP` handoff re-injection never fires.** `stop-force-loop-continue.mjs:174` gates with `if (loop.status !== "active") approveAndExit(...)`, but `loop-state.mjs` never writes `"active"` — `cmdStart` (line 71) writes `status:"running"`, and the only other values are `ended`/`abandoned`/`stale` (the last set by `reap`). So the hook early-exits on EVERY real loop and never injects `## RESUME_LOOP` into the handoff. Loop continuation across `/compact` thus has no Stop-hook belt — it rests entirely on `session-start-auto-resume` re-firing `/checkin` + the `checkin.md` Step 2b loop-resume detection (which reads `loop-state` directly and works without this hook). | fix: pending — 1-line `stop-force-loop-continue.mjs:174` `"active"` → `"running"`. NOT fixed in the observing commit (out of scope: that commit rolled the autonomous loop into `/checkin`; the checkin loop does not depend on this hook). | observed-by: claude-339c8ff7 slot alpha — per-file scrutiny reviewer B during the `/checkin` autonomous-loop roll-in. | verify: `grep -n 'loop.status' H:/prism/.claude/hooks/stop-force-loop-continue.mjs` → the comparison must read `!== "running"`.
- 2026-05-16 | **`git-add-lane-guard.mjs` false-blocked every legitimate main-tree `git add` via case-sensitive path compare.** `canonicalize()` lowercased only the drive letter (`H:`→`h:`), leaving the rest of the path case-sensitive. `git worktree list --porcelain` reports the main worktree as `H:/PRISM` (uppercase) while a chat's cwd is `h:/prism` — the SAME directory on case-insensitive NTFS. `isWithin()` did `cwd.startsWith(root+"/")` → `"h:/prism".startsWith("h:/PRISM/")` → false → every explicit pathspec flagged "outside slot worktree" → staging blocked. Compounded by slot mis-resolution (juliett chat resolved to slot `alpha`), but the casing bug alone is sufficient to block. Kill switch `PRISM_GIT_ADD_LANE_DISABLE=1` is a `process.env` read — unreachable from a tool call (PreToolUse hook runs in the harness process; an inline `VAR=1 git add` prefix does NOT reach it). | fix: `canonicalize()` now lowercases the WHOLE path on `process.platform==="win32"` (NTFS is case-insensitive); non-Windows keeps drive-only behavior. Verified: `canonicalize("H:/PRISM")===canonicalize("h:/prism")`. The `.claude/hooks/__tests__/git-add-lane-guard.test.mjs` suite imports `vitest` and cannot run under the isolated `.claude/` test infra (pre-existing gap, same class as the helpers/ vitest bug) — fix verified by direct invocation. | observed-by: claude-3a1c1c68 slot juliett (MISC-TASKS extraction commit). | verify: `node -e "import('./.claude/hooks/git-add-lane-guard.mjs').then(m=>console.log(m.canonicalize('H:/PRISM')===m.canonicalize('h:/prism')))"` → `true`.
- 2026-05-16 | **`/compact` silently no-op'd the precompact handoff write on every portable-node machine (stale RESUME across the whole fleet).** `precompact-handoff.mjs:419` spawned the per-agent-handoff writer with bare `spawnSync("node", ...)`. Under portable-node `process.execPath`=`H:\Tools\nodejs\node.exe` resolves but `node` is NOT on the PreCompact-hook child's PATH → `spawnSync("node",...)` returns `ENOENT` with `stdout=undefined`. The result parser did `(writeResult.stdout||"").trim()` → `""` → `if(out)` skipped → `JSON.parse` never ran → `catch` never fired → `writeMsg` frozen at its init literal `"(no output)"`. Net: every `/compact` emitted `auto-write attempted ((no output))` and the handoff RESUME was never refreshed (user report: "compact doesn't kick off precompact anymore"; observed session claude-339c8ff7 — RESUME stale 65min, pre-U3-U8). Silent because the hook still returned `{continue:true}` and the swallow hid the ENOENT. The sibling test `precompact-hook-source.test.mjs:28` already documented this exact footgun in its own harness — production never applied the lesson. | fix: `precompact-handoff.mjs:419` `"node"`→`process.execPath` (matches the working line-337 terminal-resolver spawn) + fail-loud result parser (surfaces `writeResult.error.code` as `SPAWN FAILED:` and the empty-stdout-no-error case as `writer emitted no stdout (status=,stderr=)` instead of `(no output)`, per Karpathy R12). 3 regression guards added to `precompact-pad.test.mjs` (comment-stripped source assertion — guard inspects code not prose, avoiding the AAM04 comment-blind class below); 16/16 + writer-source 10/10 green; live repro `{"ok":true,"file":"...HANDOFF-..."}`. `runGit()` bare `"git"` (line 85) verified NOT affected (git IS on the spawn PATH; status=0). Same-class latent bug flagged not-fixed (scope): `.claude/helpers/portability-setup.mjs:83` bare `spawnSync("node",...)`. | observed-by: claude-339c8ff7 slot bravo (user-reported during autonomous-chain test). | verify: `"H:/.claude/bin/portable-node" -e "const{spawnSync}=require('child_process');const r=spawnSync(process.execPath,['H:/prism/.claude/helpers/per-agent-handoff.mjs','read','--terminal','x'],{encoding:'utf8'});console.log(r.status,(r.stdout||'').length>0)"` → `0 true`; `node --test H:/prism/.claude/helpers/precompact-pad.test.mjs` → 16/16.
- 2026-05-16 | **AAM04 hook-wiring auditor reported 14 dangling refs but 12 were INTERNAL auditor bugs.** 6 from over-greedy `parseBundleChildren` regex capturing bare `.mjs` string literals in `bundles/smoke-test.mjs:16-21` (`join(tempDir, "ok.mjs")` etc.); 6 from helper-scope blindness — `findHooksOnDisk` excludes `.claude/helpers/` by design, so `${HELPER_BASE}/foo.mjs` bundle refs surface as dangling even when the helper exists on disk. Only 2 of 14 were real: `posttool-edit-bundle.mjs:46-47` refs `${HOOK_BASE}/build-cache-manager.mjs` + `${HOOK_BASE}/build-tracker.mjs` but BOTH files live in `helpers/` (runtime ENOENT pending; per 2026-05-14 regression `build-tracker.mjs` had xmalloc OOM — likely moved to helpers/ as mitigation but bundle ref not updated). | fix shipped: commit `e3a08b95e` AAM04-FIX1 — tightened `parseBundleChildren` regex to require path separator before basename + 1 regression-guard test (25/25 PASS); dangling 14→8. | fix #2 deliberately NOT shipped: helper-aware lenience would mask the 2 real scope-mismatches (Karpathy R12 fail-loud). | follow-up: scope-aware `parseBundleChildren` tracking prefix context per ref (HOOK_BASE vs HELPER_BASE) → emit separate `scopeMismatch` diagnostic. | observed-by: claude-549c9f4f kilo slot AAM04 /loop iter 1-7. | verify: `node H:/prism/scripts/harness-wiring-audit.mjs` → severity=warn, dangling=8.
- 2026-05-16 | **Error-learn hooks documented as auto-firing but 0/6 actually wired in either settings.json.** `grep error-(pattern\|block\|learner) settings.json` returned ZERO matches in BOTH C: and H: copies — `error-pattern-promote`, `error-pattern-capture`, `error-pattern-learner`, `error-pattern-memory`, `error-block-capture`, `error-block-prewarn` all dead-code on disk despite CLAUDE.md (project + global) AND `data/docs/gsd/GSD_QUICK.md` claiming they fire on PostToolUse/PreToolUse/Stop. Result: `ERROR_LEARN_LEDGER.jsonl` writes never accumulated; user's "make sure error learning hooks fire" silently failing for an unknown period. | partial fix: `error-pattern-promote.mjs` wired into Stop[12] after `stop-wiring-audit-suggest` (timeout 5000ms, atomic node-script + manual `cp` C:→H: per 2026-05-15 mirror-gap pattern, byte-equal preserved, hook smoke-tested `{"continue":true}`). 1/6 done. | remaining 5 need header-read first to confirm matcher: `error-pattern-capture` (T2 PostToolUse `Bash|Edit|MultiEdit|Grep|Glob`), `error-pattern-learner`, `error-pattern-memory`, `error-block-capture` (PreToolUse), `error-block-prewarn` (UserPromptSubmit). | **CAVEAT 2026-05-16 in-flight**: `claude-6d0595bf` concurrently writing `scripts/_emergency-unwire-yolo-25.mjs` + `feedback_dont_wire_for_wiring_sake_2026_05_16.md` + `reference_hook_wiring_yolo_25_2026_05_16.md` — read those before piling on more wires; doctrine may shift from "wire by default" to "wire on demonstrated need", potentially reverting `error-pattern-promote`. | observed-by: claude-549c9f4f kilo slot. | verify: `grep error-pattern-promote C:/Users/wompu/.claude/settings.json` ≥1 match.
- 2026-05-16 | **Settings.json hook wiring silently REVERTED on shipped SYSTEM-VIZ-BRAIN-MS0 keystone units.** U-P0-AUDIT-VIZ-FIRST + U-P1-POST-SHIP-DISTILL shipped together in `0c11ff1cb` (2026-05-15) with their hook .mjs files, skill, script, wiki entry, and auto-distilled post-ship memo. Commit body explicitly wired `audit-viz-first-inject.mjs` into `UserPromptSubmit` chain idx 5 and the post-ship-distill Stop hook. Audit on 2026-05-16 via slot ECHO: `grep audit-viz-first` returned **0 matches** in BOTH `C:/Users/wompu/.claude/settings.json` AND `H:/.claude/settings.json` — wiring was reverted in some intervening multi-chat merge. Hooks were dead-code on disk despite envelope+memos saying "shipped". | fix: re-spliced both entries via atomic node JSON write into C:/Users/wompu/.claude/settings.json (UserPromptSubmit[4] right after master-index-precheck-inject; Stop[8] right after stop-system-viz-drift per [[reference_stop_advisory_wiring_cluster_2026_05_15]]); manually `cp` to H:/.claude/settings.json because c-to-h-mirror hook fires on Edit/Write/MultiEdit/NotebookEdit but NOT on Bash node-writes (separate documented mirror gap); both files byte-identical 30731B; smoke-tested each hook with empty stdin (post-ship-distill returns `{"continue":true}`, audit-viz-first silently no-ops). | observed-by: claude-a61bbf34 slot echo /loop iter 1 SYSTEM-VIZ-BRAIN-MS0. | verify: `node -e "['H:/.claude/settings.json','C:/Users/wompu/.claude/settings.json'].forEach(p=>{const c=require('fs').readFileSync(p,'utf8'); console.log(p+': audit-viz-first='+((c.match(/audit-viz-first/g)||[]).length)+' post-ship-distill='+((c.match(/post-ship-distill/g)||[]).length))})"` → both ≥1. Doctrine + protocol: [[feedback_settings_wiring_drift_2026_05_16]] — add grep-check as 5th surface to the `feedback_reflect_all_changes_post_update` close-out rule for any unit that touches harness config.
- 2026-05-15 | **`c-to-h-mirror` hook was DOCUMENTED but NEVER WIRED (silent C:↔H: drift for months).** Both global and project CLAUDE.md claimed "c-to-h-mirror hook auto-replicates C: → H: on every save" since 2026-05-12 at the latest. Reality: `.claude/hooks/mirror-c-to-h.mjs` existed but had ZERO entries in either settings.json. Every Claude session on this PC wrote to `C:\Users\<u>\.claude\` (memory, plans, transcripts) and silently failed to mirror to `H:\.claude\`. Live audit on 2026-05-15: **34,003 files walked on C:, 33,040 missing on H:** (≈97% drift), 575 correctly excluded by the hook's exclusion list, 388 already in sync. | fix: INTEL-OLLAMA-OBSIDIAN-MS0/P6-U01 (claude-b6c4b196 slot delta 2026-05-15) wired the hook into the `Edit|Write|MultiEdit|NotebookEdit` PostToolUse group in BOTH `C:\Users\<u>\.claude\settings.json` AND `H:\.claude\settings.json` (timeout 3000ms, byte-identical 30064 bytes); shipped 2 supporting scripts (`scripts/mirror-c-to-h-audit.mjs` to surface drift, `scripts/bootstrap-h-mirror.mjs` for one-shot backlog sync with --apply); 32 vitest tests in `.claude/helpers/mirror-c-to-h.test.mjs` (plain `node:assert` because the helpers/ vitest-config has a pre-existing infra bug). | verify: `node scripts/mirror-c-to-h-audit.mjs` → `missing-on-h: 0` after running `node scripts/bootstrap-h-mirror.mjs --apply` once. The hook continues to fire on every Edit/Write going forward.
- 2026-05-14 | **DISPATCHER_DIGEST.md regen parser broken for spread-array action enums.** `scripts/generate-dispatcher-wiki.mjs` (or whichever emits the digest) does NOT recognise `z.enum([...A, ...B] as const)`. Four high-traffic dispatchers show 0 actions in the digest while their `.ts` files have 428 / 27 / 121 / 130 case statements (`aiReasoningDispatcher`, `localDispatcher`, `millDispatcher`, `mlDispatcher`). Every downstream audit that scrapes the digest mis-prioritises these as "zero-action wiring milestones". | fix: pending — unit `U-HVA-DIGEST-PARSER-FIX` (S). Test fixture: `enum X = [...A, ...B]` literal must produce `len(A)+len(B)` actions. Interim mitigation: `scripts/high-value-additions-rank.mjs` counts directly from `.ts` files via `case ["']\w+["']\s*:` regex. | observed-by: claude-a2b1b5ca slot alpha /forge-audit-v2 + peer reviewer agent a8299dd3b088946a6. | verify: `node scripts/high-value-additions-rank.mjs --json | jq .dispatchers.digestParserBroken` must return `false`.
- 2026-05-14 | **BUILD_STATE.NEEDS_WIRING signal has ≥ 50 % false-positive rate on sample.** `master_index_query buildClass:unknown` actually means "not indexed by node-classifier", not "no dispatcher import". Sampling 10 named candidates this session: 5 are wired (HookLatencyEngine→devDispatcher, TokenEconomyEngine→contextDispatcher, AutoFixPipelineEngine→devDispatcher, OllamaEmbedderEngine→memoryDispatcher, HookTelemetryEngine→hookDispatcher). | fix: unit `U-HVA-UNWIRED-SIGNAL-VALIDATE` — `scripts/validate-unwired-signal.mjs` samples 50 random NEEDS_WIRING engines + greps every dispatcher for imports. False-positive rate ≤ 10 % gates downstream wiring milestones. | observed-by: same peer reviewer. | verify: re-run script → false-positive rate ≤ 10 %.
- 2026-05-14 | **Hook orphan-rate metric was bundle-blind (overstated by 12.7 pp).** v1 of `scripts/high-value-additions-rank.mjs` only scraped settings.json `command` fields and missed bundle-child refs (six `bundles/*.mjs` invoke ~91 child hooks via `${HOOK_BASE}/foo.mjs`). Original baseline 78.6 % → corrected 65.9 %. | fix: v2 walks `bundles/*.mjs` and unions child refs into the wired set before computing orphans. | observed-by: same peer reviewer. | verify: `node scripts/high-value-additions-rank.mjs --json | jq '.hooks | {wiredViaSettings,wiredViaBundle,sourceHooks}'` returns sane numbers (settings+bundle ≥ 130 on this repo).
- 2026-05-14 | `system-viz-live-bridge` (iter 2 — supersedes 2026-05-14 iter 1 below): even reclassifying TypeError → `viz-not-running` (info) still emitted 1 record per session per 5-min backoff window — pure noise for an optional dev tool. Operators don't care about "viz is off" on machines where viz is intentionally off. | fix: extracted pure `telemetryRecordFor()` that returns `null` for viz-not-running (no JSONL row at all). The `.down` sidecar (`viz-live-bridge-<sid>.down`, backoff-until epoch) remains the audit trail — its mtime tells operators when viz was last detected down. `scripts/hook-health-check.mjs` `NEUTRAL_EVENTS` still includes `viz-not-running` to correctly classify historical pre-fix rows. Also fixed: malformed `post:{}` from adversarial custom postFn now returns `null` instead of falling through to bogus `ping-failed`. | observed-by: claude-a2b1b5ca slot alpha (3-of-3 scrutiny FAIL → fix → PASS cycle) | verify: `node --check .claude/hooks/system-viz-live-bridge.mjs` + the 8 new vitest cases in `__tests__/system-viz-live-bridge.test.mjs`
- 2026-05-14 | `system-viz-live-bridge` PostToolUse hook logged 1,347 `ping-failed:TypeError` events (4.3% of telemetry stream) when local viz server was off — every Edit/Write retried ECONNREFUSED forever | fix: classify TypeError as `viz-not-running` (info) + add 5-min session backoff via `VIZ_DOWN_BACKOFF_MS` + `vizDownFile` sidecar | observed-by: claude-48450e3d /forge-audit-v2 | verify: `node scripts/hook-health-check.mjs --window=1h` should show 0 broken hooks
- 2026-05-14 | `build-tracker.mjs` PostToolUse:Write fires `/bin/bash: xmalloc: cannot allocate 8192 bytes` (fork-storm symptom under Windows hook load) | fix: not a code bug; run `node .claude/hooks/node-process-janitor.mjs --full` to reap orphan bash.exe + MCP procs | observed-by: claude-48450e3d /forge-audit-v2 | verify: subsequent Write hooks emit no xmalloc errors

