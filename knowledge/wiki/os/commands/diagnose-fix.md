---
kind: command
slug: diagnose-fix
status: stub
generated_at: 2026-05-23T00:34:38.924Z
generator: scripts/backfill-wiki-os-commands-entities.mjs
source: .claude/commands/diagnose-fix.md
description: "Operator front-end for forensic-classify → suggest → apply → test → trace on a broken artifact (engine / test / hook / config). Composes /forge-debug + error-pattern memory + regression-hunter agent + test-runner. Generates a fix proposal, applies, runs tests, traces verdict. R8: adds 0 logic."
---

# /diagnose-fix

Operator front-end for forensic-classify → suggest → apply → test → trace on a broken artifact (engine / test / hook / config). Composes /forge-debug + error-pattern memory + regression-hunter agent + test-runner. Generates a fix proposal, applies, runs tests, traces verdict. R8: adds 0 logic.

## Source command

See `.claude/commands/diagnose-fix.md` for the live executable surface.

## Status

This entity is a U-CK11 Phase 2 backfill stub. Richer content (purpose,
composes_with chain, invocation examples, related skills) lands in a
later phase per the U-CK11 verdicts doc remediation queue.
