---
kind: command
slug: wire-unwired
status: stub
generated_at: 2026-05-23T00:34:38.924Z
generator: scripts/backfill-wiki-os-commands-entities.mjs
source: .claude/commands/wire-unwired.md
description: "One command that orchestrates the full wiring sprint pipeline — `/dispatcher-coverage` picks the dispatcher, `/forge-wiring` proposes the engine→dispatcher hooks, `/wiring-batch` applies the batch, `/unwired-review` confirms post-state. Replaces the 4-skill manual chain with a single suggest-then-apply flow. Companion to (not replacement for) the underlying skills."
---

# /wire-unwired

One command that orchestrates the full wiring sprint pipeline — `/dispatcher-coverage` picks the dispatcher, `/forge-wiring` proposes the engine→dispatcher hooks, `/wiring-batch` applies the batch, `/unwired-review` confirms post-state. Replaces the 4-skill manual chain with a single suggest-then-apply flow. Companion to (not replacement for) the underlying skills.

## Source command

See `.claude/commands/wire-unwired.md` for the live executable surface.

## Status

This entity is a U-CK11 Phase 2 backfill stub. Richer content (purpose,
composes_with chain, invocation examples, related skills) lands in a
later phase per the U-CK11 verdicts doc remediation queue.
