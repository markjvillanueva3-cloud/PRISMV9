---
kind: command
slug: fix-hook-schemas
status: stub
generated_at: 2026-05-23T00:34:38.924Z
generator: scripts/backfill-wiki-os-commands-entities.mjs
source: .claude/commands/fix-hook-schemas.md
description: "Runs the two-stage hook audit-and-fix pipeline that keeps the `.claude/hooks/` and `.claude/helpers/` scripts emitting JSON that matches Claude Code's hook schema. Catches bugs that surface in the UI as `Hook JSON output validation failed —"
---

# /fix-hook-schemas

Runs the two-stage hook audit-and-fix pipeline that keeps the `.claude/hooks/` and `.claude/helpers/` scripts emitting JSON that matches Claude Code's hook schema. Catches bugs that surface in the UI as `Hook JSON output validation failed —

## Source command

See `.claude/commands/fix-hook-schemas.md` for the live executable surface.

## Status

This entity is a U-CK11 Phase 2 backfill stub. Richer content (purpose,
composes_with chain, invocation examples, related skills) lands in a
later phase per the U-CK11 verdicts doc remediation queue.
