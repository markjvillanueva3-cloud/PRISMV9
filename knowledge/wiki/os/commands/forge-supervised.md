---
kind: command
slug: forge-supervised
status: stub
generated_at: 2026-05-23T00:34:38.924Z
generator: scripts/backfill-wiki-os-commands-entities.mjs
source: .claude/commands/forge-supervised.md
description: "Supervised /forge-triple wrapper — runs /forge-triple (engine+skill+hook trio) then /scrutiny-gate end-of-task automatically, then writes a close-out record. The \"no rogue forge\" front-end — every forged trio passes the 3-of-3 gate before commit."
---

# /forge-supervised

Supervised /forge-triple wrapper — runs /forge-triple (engine+skill+hook trio) then /scrutiny-gate end-of-task automatically, then writes a close-out record. The "no rogue forge" front-end — every forged trio passes the 3-of-3 gate before commit.

## Source command

See `.claude/commands/forge-supervised.md` for the live executable surface.

## Status

This entity is a U-CK11 Phase 2 backfill stub. Richer content (purpose,
composes_with chain, invocation examples, related skills) lands in a
later phase per the U-CK11 verdicts doc remediation queue.
