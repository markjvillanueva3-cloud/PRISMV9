---
kind: command
slug: macro-program
status: stub
generated_at: 2026-05-23T00:34:38.924Z
generator: scripts/backfill-wiki-os-commands-entities.mjs
source: .claude/commands/macro-program.md
description: "Macro-program pipeline — the operator-facing front door for MACRO-PROGRAM-PIPELINE-MS0. Catalog the 4 JM Okuma-OSP macros, match parts to families, place labelled reference templates, fill VC vars from print dims, run the S(x) ≥ 0.70 safety gate, emit per-machine labelled .MIN files, and orchestrate the gated, operator-confirmed bulk fan-out. NEVER bulk-auto-emit. Operator-in-the-loop is unconditional."
---

# /macro-program

Macro-program pipeline — the operator-facing front door for MACRO-PROGRAM-PIPELINE-MS0. Catalog the 4 JM Okuma-OSP macros, match parts to families, place labelled reference templates, fill VC vars from print dims, run the S(x) ≥ 0.70 safety gate, emit per-machine labelled .MIN files, and orchestrate the gated, operator-confirmed bulk fan-out. NEVER bulk-auto-emit. Operator-in-the-loop is unconditional.

## Source command

See `.claude/commands/macro-program.md` for the live executable surface.

## Status

This entity is a U-CK11 Phase 2 backfill stub. Richer content (purpose,
composes_with chain, invocation examples, related skills) lands in a
later phase per the U-CK11 verdicts doc remediation queue.
