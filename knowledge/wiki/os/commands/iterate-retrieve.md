---
kind: command
slug: iterate-retrieve
status: stub
generated_at: 2026-05-23T00:34:38.924Z
generator: scripts/backfill-wiki-os-commands-entities.mjs
source: .claude/commands/iterate-retrieve.md
description: "Progressive context refinement for codebase search. Runs DISPATCH→EVALUATE→REFINE→LOOP (max 3 cycles) and returns ranked, scored files. Use instead of one-shot Grep/Glob when initial query is broad or terminology is unknown."
---

# /iterate-retrieve

Progressive context refinement for codebase search. Runs DISPATCH→EVALUATE→REFINE→LOOP (max 3 cycles) and returns ranked, scored files. Use instead of one-shot Grep/Glob when initial query is broad or terminology is unknown.

## Source command

See `.claude/commands/iterate-retrieve.md` for the live executable surface.

## Status

This entity is a U-CK11 Phase 2 backfill stub. Richer content (purpose,
composes_with chain, invocation examples, related skills) lands in a
later phase per the U-CK11 verdicts doc remediation queue.
