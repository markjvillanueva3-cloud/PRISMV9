---
kind: command
slug: session-cycle
status: stub
generated_at: 2026-05-23T00:34:38.924Z
generator: scripts/backfill-wiki-os-commands-entities.mjs
source: .claude/commands/session-cycle.md
description: "Composed lifecycle pipeline — runs the canonical chat session as a single chain: /checkin → /pick-unit → /research → build (per-file scrutiny) → /scrutiny-gate → close-out → /handoff. Replaces \"remember each step\" with one operator-typed entry. Each stage is a registered surface; this skill threads them."
---

# /session-cycle

Composed lifecycle pipeline — runs the canonical chat session as a single chain: /checkin → /pick-unit → /research → build (per-file scrutiny) → /scrutiny-gate → close-out → /handoff. Replaces "remember each step" with one operator-typed entry. Each stage is a registered surface; this skill threads them.

## Source command

See `.claude/commands/session-cycle.md` for the live executable surface.

## Status

This entity is a U-CK11 Phase 2 backfill stub. Richer content (purpose,
composes_with chain, invocation examples, related skills) lands in a
later phase per the U-CK11 verdicts doc remediation queue.
