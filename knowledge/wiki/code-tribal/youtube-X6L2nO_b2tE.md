---
title: "Understanding and using Voronoi Process Rhino and Grasshopper Parametric Architecture and Design"
domain: cad
source: youtube
videoId: X6L2nO_b2tE
url: https://www.youtube.com/watch?v=X6L2nO_b2tE
channel: "DCO Parametric"
duration_s: 565
tribal_entries: 3
chunks_scanned: 11
extracted_at: 2026-05-27
extraction_method: youtube-free-extract+regex-notability
---

# Understanding and using Voronoi Process Rhino and Grasshopper Parametric Architecture and Design

**Channel:** [DCO Parametric](https://www.youtube.com/watch?v=X6L2nO_b2tE)
**Duration:** 9m 25s
**Domain:** `cad` (auto-classified from title/channel keywords)
**Tribal entries surfaced:** 3 of 11 scanned chunks (notability ≥ 0.4)

## Toolpath tribal tips

These chunks scored above the notability floor for toolpath/CAM relevance. Each is searchable via [[tribal-by-domain-inject]] when a chat slot's `domain_filter` includes `cad`.

### Tip 1 — confidence 0.42

> halfway between this one and this one at this point at this point there is a perpendicular created this way and this way

halfway between this one and this one at this point at this point there is a perpendicular created this way and this way so there is a 90 degree intersection here degree intersection here and that's what it does so the process is connects the points is connects the points is connects the points creates 90 degrees but since we only have two points have two points have two points it would only subdivide this entire if this is the frame it subsides it into two two two if we wanted to add more points that's where it starts getting interesting and let's start with let's create just two more points

_Signals: params:2 · howto:1_

### Tip 2 — confidence 0.41

> just randomly so one here here here and one here so what we're going to do is connect them using where these two interse

just randomly so one here here here and one here so what we're going to do is connect them using where these two intersect we do 90 degrees so where are these two in the midpoint where they intercept where they intercept where they intercept we do 90 degrees same thing here same thing here same thing here now once we extend those that's where we start getting the pattern start getting the pattern start getting the pattern so if we extend this one we extend this one we extend this one and between these two we have this one if we had another point then there would be a perpendicular here

_Signals: params:2_

### Tip 3 — confidence 0.4

> connecting these two connecting these two connecting these two so let's add another point here creating now that this be

connecting these two connecting these two connecting these two so let's add another point here creating now that this becomes the boring cell and this is the i forget the name exactly uh but that's the process of using warning so whenever you have a bunch of random points it self divides it and the reason why it gets a bad wrap is because it's a really simple way of subviting stuff so a lot of people tend to use it to use it to use it and also you can create random points on any any any on any surface fairly quickly so a lot of people use it because you can take let's say a surface that looks

_Signals: safety:1 · howto:1_

---
## Wiring

- Tribal entries are embedded in `state/shared/tribal-embed-index.json` (id prefix `tribal-jsonl:youtube-toolpath-tribal#youtube-toolpath-X6L2nO_b2tE-c*`)
- Surfaced via `.claude/hooks/tribal-by-domain-inject.mjs` (UserPromptSubmit) on prompts matching domain `cad`
- Source artifact: `state/shared/youtube-extraction/X6L2nO_b2tE.json`

Acquired via [[reference_youtube_free_extraction_pipeline_2026_05_26]] (victor's $0 yt-dlp + auto-captions pipeline) — see also [[reference_youtube_toolpath_tribal_extraction_2026_05_26]].