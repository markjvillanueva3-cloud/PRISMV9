---
title: "CAD function template — onshape / simulation-fea"
software: onshape
function: simulation-fea
source: video-tribal-aggregation
tip_count: 1
videos_covered: 1
generated_at: 2026-05-27
---

# CAD function template — onshape / simulation-fea

**Software:** `onshape` · **Function category:** `simulation-fea`
**Source:** aggregated from 1 video tribal tips across 1 unique tutorial videos.

## Canonical prompt pattern

When a chat asks "do <simulation-fea> in <onshape>" or invokes [[tribal-by-domain-inject]] with domain=cad matching, this template surfaces.

## Aggregated tribal tips (top 1 by confidence)

### Tip 1 (confidence 0.4)

> enough or should we just uh try it and see how it turns out let's try and see where early in the stages here all right s

enough or should we just uh try it and see how it turns out let's try and see where early in the stages here all right stages here all right stages here all right as soon as Chris commits his change from perhaps he's in Michigan right um um um I can see my simulation which is here in Boston Boston Boston um changing so and and the the results are automatically refreshing are automatically refreshing are automatically refreshing so this is truly collaborative real-time simulation uh the likes of which you probably well you would never have seen before because this isn't possible in any other

_Signals: safety:1 · howto:1_

_Source: [Onshape Simulation Overview and Demo](https://www.youtube.com/watch?v=71WdbUIsRRM) — channel `Onshape`_

## Self-learning hook

This template is consumed by the assembly-generation AI when it needs to synthesize `simulation-fea` operations in `onshape`. The aggregated tips act as few-shot priors. Closed-loop flow:

1. New tribal entries land in `mcp-server/data/tribal/youtube-toolpath-tribal.jsonl`
2. `embed-tribal-jsonl-into-index.mjs` adds them to the recall index
3. Re-running `generate-cad-function-templates.mjs` rebuilds this template with new tips folded in
4. The AI sees the richer template on its next invocation