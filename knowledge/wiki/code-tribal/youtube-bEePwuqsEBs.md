---
title: "Solidworks Tutorial | Stress Simulation and Factor of Safety in Solidworks"
domain: cad
source: youtube
videoId: bEePwuqsEBs
url: https://www.youtube.com/watch?v=bEePwuqsEBs
channel: "How it's Designed "
duration_s: 408
tribal_entries: 2
chunks_scanned: 7
extracted_at: 2026-05-27
extraction_method: youtube-free-extract+regex-notability
---

# Solidworks Tutorial | Stress Simulation and Factor of Safety in Solidworks

**Channel:** [How it's Designed ](https://www.youtube.com/watch?v=bEePwuqsEBs)
**Duration:** 6m 48s
**Domain:** `cad` (auto-classified from title/channel keywords)
**Tribal entries surfaced:** 2 of 7 scanned chunks (notability ≥ 0.4)

## Toolpath tribal tips

These chunks scored above the notability floor for toolpath/CAM relevance. Each is searchable via [[tribal-by-domain-inject]] when a chat slot's `domain_filter` includes `cad`.

### Tip 1 — confidence 0.4

> this split line we just did the split line feature so we can select that top face in a selected direction we're going to

this split line we just did the split line feature so we can select that top face in a selected direction we're going to click this edge you can see the arrows are going in the y direction change the units to english and let's just do 500 pounds to start click ok click ok [Music] [Music] [Music] now we've got our now we've got our now we've got our fixtures fixtures fixtures and input setup and input setup and input setup now we can run a mesh we're just gonna do a basic mesh a little bit on the finer side that's going to look something like that got multiple nodes got multiple nodes got

_Signals: howto:5_

### Tip 2 — confidence 0.46

> multiple nodes on the thickness on the thickness on the thickness which is good so this is what it's going to look like 

multiple nodes on the thickness on the thickness on the thickness which is good so this is what it's going to look like in the deformed in the deformed in the deformed result result result we can always turn that off if we want it exaggerates how it's going to react to the force react to the force react to the force [Music] [Music] [Music] and this is our on mycss plot here we've got some we've got some we've got some things to modify to make it more clear we're going to change this well first we're going to change the units [Music] we're going to have our max at 25 because that's the tensile

_Signals: camOps:1 · safety:1 · howto:4_

---
## Wiring

- Tribal entries are embedded in `state/shared/tribal-embed-index.json` (id prefix `tribal-jsonl:youtube-toolpath-tribal#youtube-toolpath-bEePwuqsEBs-c*`)
- Surfaced via `.claude/hooks/tribal-by-domain-inject.mjs` (UserPromptSubmit) on prompts matching domain `cad`
- Source artifact: `state/shared/youtube-extraction/bEePwuqsEBs.json`

Acquired via [[reference_youtube_free_extraction_pipeline_2026_05_26]] (victor's $0 yt-dlp + auto-captions pipeline) — see also [[reference_youtube_toolpath_tribal_extraction_2026_05_26]].