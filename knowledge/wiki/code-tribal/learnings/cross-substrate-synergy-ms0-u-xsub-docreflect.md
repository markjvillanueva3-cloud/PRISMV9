# CROSS-SUBSTRATE-SYNERGY-MS0/U-XSUB-DOCREFLECT — [MAIN] [BOOTSTRAP-SLOT-ENFORCE] [CROSS-SUBSTRATE-SYNERGY-MS0]/U-XSUB-DOCREFLECT (slot:sierra): wiki lesson + regressions-list entry for U-XSUB-EMBEDS + the documented-by oracle fix (88cb72b532). Lesson: confirm cross-substrate endpoints against the stable ADD-only merged-graph offset oracle, never a volatile rotating augmentation -- it silently loses coverage when it rotates (documented-by collapsed 38->0 undetected 8h). Satisfies the bug-finding -> wiki gate.

**Commit:** `35acfb15b428` · **By:** markjvillanueva3-cloud · **At:** 2026-06-10T13:42:32-05:00
**Tags:** cross-substrate-synergy-ms0, u-xsub-docreflect, auto-distilled

## Subject
[MAIN] [BOOTSTRAP-SLOT-ENFORCE] [CROSS-SUBSTRATE-SYNERGY-MS0]/U-XSUB-DOCREFLECT (slot:sierra): wiki lesson + regressions-list entry for U-XSUB-EMBEDS + the documented-by oracle fix (88cb72b532). Lesson: confirm cross-substrate endpoints against the stable ADD-only merged-graph offset oracle, never a volatile rotating augmentation -- it silently loses coverage when it rotates (documented-by collapsed 38->0 undetected 8h). Satisfies the bug-finding -> wiki gate.

## Body
```
[MAIN] [BOOTSTRAP-SLOT-ENFORCE] [CROSS-SUBSTRATE-SYNERGY-MS0]/U-XSUB-DOCREFLECT (slot:sierra): wiki lesson + regressions-list entry for U-XSUB-EMBEDS + the documented-by oracle fix (88cb72b532). Lesson: confirm cross-substrate endpoints against the stable ADD-only merged-graph offset oracle, never a volatile rotating augmentation -- it silently loses coverage when it rotates (documented-by collapsed 38->0 undetected 8h). Satisfies the bug-finding -> wiki gate.
```

## Files touched (2)
- scripts/wiki-tribal-cross-ref-audit.mjs | 20 +++++++++++++++++---
- 1 file changed, 17 insertions(+), 3 deletions(-)

## Lessons surfaced in commit body
- lesson + regressions-list entry for U-XSUB-EMBEDS + the documented-by oracle fix (88cb72b532). Lesson: confirm cross-substrate endpoints against the stable ADD-only merged-graph offset oracle, never a volatile rotating augmentation -- it silently loses coverage when it rotates (documented-by collapsed 38->0 undetected 8h). Satisfies the bug-finding -> wiki gate.

## Verification
**Scrutiny ledger**: arms A✗ B✗ C✗ for session 

## Cross-references
- Full commit: `git -C H:/prism show 35acfb15b428`
- Milestone envelope: `mcp-server/data/milestones/CROSS-SUBSTRATE-SYNERGY-MS0.json`

---
_Auto-distilled by `scripts/distill-session-learnings.mjs` per [[feedback_auto_close_out]] / SYSTEM-VIZ-BRAIN-MS0/U-P1-POST-SHIP-DISTILL._