# CROSS-SUBSTRATE-SYNERGY-MS0/U-XSUB-BUGCLASS-SWEEP — [MAIN] [BOOTSTRAP-SLOT-ENFORCE] [CROSS-SUBSTRATE-SYNERGY-MS0]/U-XSUB-BUGCLASS-SWEEP (slot:sierra): codify the canonical endpoint-confirmation pattern + record the fleet-wide sweep result. The documented-by silent-collapse bug-class (confirm edge endpoints vs a volatile peer augmentation newNodes) is CONFINED to the now-fixed generator -- 2 independent exhaustive idiom-greps + spot-checks of all ~7 cross-edge emitters (all confirm vs graph.nodes directly = merged ADD-only graph). Canonical rule: confirm against the merged graph / offset oracle, never a peer augmentation newNodes. (Workflow audit was rate-limited; conclusion rests on the greps + my direct spot-checks, honestly noted.)

**Commit:** `ab9318ee4762` · **By:** markjvillanueva3-cloud · **At:** 2026-06-10T14:10:55-05:00
**Tags:** cross-substrate-synergy-ms0, u-xsub-bugclass-sweep, auto-distilled

## Subject
[MAIN] [BOOTSTRAP-SLOT-ENFORCE] [CROSS-SUBSTRATE-SYNERGY-MS0]/U-XSUB-BUGCLASS-SWEEP (slot:sierra): codify the canonical endpoint-confirmation pattern + record the fleet-wide sweep result. The documented-by silent-collapse bug-class (confirm edge endpoints vs a volatile peer augmentation newNodes) is CONFINED to the now-fixed generator -- 2 independent exhaustive idiom-greps + spot-checks of all ~7 cross-edge emitters (all confirm vs graph.nodes directly = merged ADD-only graph). Canonical rule: confirm against the merged graph / offset oracle, never a peer augmentation newNodes. (Workflow audit was rate-limited; conclusion rests on the greps + my direct spot-checks, honestly noted.)

## Body
```
[MAIN] [BOOTSTRAP-SLOT-ENFORCE] [CROSS-SUBSTRATE-SYNERGY-MS0]/U-XSUB-BUGCLASS-SWEEP (slot:sierra): codify the canonical endpoint-confirmation pattern + record the fleet-wide sweep result. The documented-by silent-collapse bug-class (confirm edge endpoints vs a volatile peer augmentation newNodes) is CONFINED to the now-fixed generator -- 2 independent exhaustive idiom-greps + spot-checks of all ~7 cross-edge emitters (all confirm vs graph.nodes directly = merged ADD-only graph). Canonical rule: confirm against the merged graph / offset oracle, never a peer augmentation newNodes. (Workflow audit was rate-limited; conclusion rests on the greps + my direct spot-checks, honestly noted.)
```

## Files touched (2)
- knowledge/wiki/code-tribal/learnings/cross-substrate-embeds-and-docby-oracle.md | 3 +++
- 1 file changed, 3 insertions(+)

## Lessons surfaced in commit body
- tile peer augmentation newNodes) is CONFINED to the now-fixed generator -- 2 independent exhaustive idiom-greps + spot-checks of all ~7 cross-edge emitters (all confirm vs graph.nodes directly = merged ADD-only graph). Canonical rule: confirm against the merged graph / offset oracle, never a peer augmentation newNodes. (Workflow audit was rate-limited; conclusion rests on the greps + my direct spot-c

## Verification
**Scrutiny ledger**: arms A✗ B✗ C✗ for session 

## Cross-references
- Full commit: `git -C H:/prism show ab9318ee4762`
- Milestone envelope: `mcp-server/data/milestones/CROSS-SUBSTRATE-SYNERGY-MS0.json`

---
_Auto-distilled by `scripts/distill-session-learnings.mjs` per [[feedback_auto_close_out]] / SYSTEM-VIZ-BRAIN-MS0/U-P1-POST-SHIP-DISTILL._