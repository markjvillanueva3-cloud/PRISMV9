# HOTEL/U-ERP-PHASE1-P0 — [MAIN] [BOOTSTRAP-SLOT-ENFORCE] [HOTEL]/U-ERP-PHASE1-P0 (slot:hotel /goal iter6): G1 DepartmentEngine + G2 ManagerRegistryEngine + G5 AIProposalApprovalQueueEngine — Phase 1 P0 foundation closure

**Commit:** `814406820905` · **By:** markjvillanueva3-cloud · **At:** 2026-05-26T14:58:32-05:00
**Tags:** hotel, u-erp-phase1-p0, auto-distilled

## Subject
[MAIN] [BOOTSTRAP-SLOT-ENFORCE] [HOTEL]/U-ERP-PHASE1-P0 (slot:hotel /goal iter6): G1 DepartmentEngine + G2 ManagerRegistryEngine + G5 AIProposalApprovalQueueEngine — Phase 1 P0 foundation closure

## Body
```
[MAIN] [BOOTSTRAP-SLOT-ENFORCE] [HOTEL]/U-ERP-PHASE1-P0 (slot:hotel /goal iter6): G1 DepartmentEngine + G2 ManagerRegistryEngine + G5 AIProposalApprovalQueueEngine — Phase 1 P0 foundation closure

3 cohesive engines + 36 businessDispatcher actions + 78/78 tests + wiring round-trip.

— G1 DepartmentEngine (~440 LOC, 21/21 PASS): org-chart entity (code/name/manager_employee_id/members/parent_code/machine_domain), 18 canonical JM-Die dept codes (eng/programming/mill/lathe/swiss/millturn/wedm/sinker/grinder/honing/polishing/qa/shipping/receiving/office/sales/accounting/maintenance), KPI rollup (OEE/on_time/waste_count/open_kaizen/open_ncrs per window — Nakajima 1988), ancestor-chain walk, cycle gate on setParent, audit-trail on every mutation. R12: code regex, missing manager, unknown parent, duplicate, self-parent, cycle, oee>100, NaN/Infinity boundaries.

— G2 ManagerRegistryEngine (~470 LOC, 27/27 PASS): 22-rank ladder (apprentice..admin), admin = NEW rank above owner per operator directive 2026-05-26. employee_id → rank + dept + reports_to chain. canManagerApprove(approver, subject) is the CANONICAL gate consumed by every approval engine downstream. Self-promote rejected (SoD), promote requires approver to outrank target rank, demote requires reason (no silent demotion), deactivate requires reason. R12: invalid rank, self-report, unranked manager, duplicate registration, unregistered reports_to, non-higher promote, lower demote, cycle in setReportsTo, inactive employee gate.

— G5 AIProposalApprovalQueueEngine (~520 LOC, 24/24 PASS): generic admin-approval queue for ALL AI outputs (7 kinds: ai_summary, auto_schedule, auto_delegation, auto_quote, auto_remediation, auto_extraction, auto_recommendation). Fail-CLOSED auth — reviewer must be (a) registered, (b) active, (c) rank >= adminMinRank (default 'admin'). Lifecycle: submitted -> under_review -> approved | rejected | edited_and_approved | expired | withdrawn. editAndApprove preserves ORIGINAL AI payload + records edited_payload diff (the human correction signal for downstream fine-tuning). Stats: by_status, by_kind, oldest_pending_age_minutes, approval_rate_pct. Composes ManagerRegistryEngine (auth) + ApprovalWorkflowEngine (multi-step via approval_workflow_id). R12: invalid kind/payload/explanation_ref/TTL, non-admin approver, missing rationale (no silent admin approval — Hotel AI invariant), withdraw after review, expired ledger, invalid now_iso.

— businessDispatcher.ts: +36 actions (dept_* 12, mgr_* 13, aiprop_* 11) in z.enum + matching case handlers with lazy imports (existing dispatcher convention).

— Wiring round-trip test (6/6 PASS): static-source verification that every new action is in z.enum AND has a case branch, the engines lazy-import path is correct, and singletons are loadable. Catches dispatcher wiring drift at test-time (businessDispatcher is MCP-server-bound via server.tool() and not directly callable from Node tests).

— tsc clean. Anti-regression preserved (only additive z.enum entries).

— Phase 1 P0 deliverable per HOTEL-ERP-SCOPE-ASSESSMENT-2026-05-26.md §4. Unblocks downstream: G3 (AutomatedJobSchedulerEngine — uses canManagerApprove + aiprop_submit), G4 (AutomatedTaskDelegatorEngine — uses department roster + manager rank gate + aiprop_submit), G6 (AISummaryWriterEngine — submits narrative via aiprop_submit per cadence daily/weekly/monthly), G7 (DepartmentAuditDashboardEngine — uses dept_get_rollup), G8 (multi-step approval extension), G9 (RFQToOrderPipelineOrchestratorEngine — quote draft via aiprop_submit).

— Operator green-lights captured: admin=NEW rank above owner; AI cadence=daily+weekly+monthly; auto-delegation=4 triggers (nightly, shift-gap, stalled-handoff, sick-day); logistics=internal-first.

Composes with today's hotel ship (commits 67178f76d6 + c96228f5ed + d7eeabefe4): EmployeeTaskHandoffEngine handoff_propose can be auto-generated by G4 via aiprop_submit; KaizenLeanSigmaEngine wasteSummary feeds G7 dept rollup; EmployeeMachineDomainAcademyEngine reportPath feeds G4 skill-match.

Total this hotel /goal session: 6 engines + 70 dispatcher actions + 169 tests + scope spec + 18 dept codes + 22-rank ladder + 8 lean wastes + 5 DMAIC phases + 10 machine domains + 5 specialist tiers.
```

## Files touched (9)
- .../AIProposalApprovalQueueEngine.test.ts          | 413 ++++++++++++++++
- mcp-server/src/__tests__/DepartmentEngine.test.ts  | 365 ++++++++++++++
- .../src/__tests__/ManagerRegistryEngine.test.ts    | 355 ++++++++++++++
- .../hotel-phase1-dispatcher-roundtrip.test.ts      |  92 ++++
- .../src/engines/AIProposalApprovalQueueEngine.ts   | 536 +++++++++++++++++++++
- mcp-server/src/engines/DepartmentEngine.ts         | 429 +++++++++++++++++
- mcp-server/src/engines/ManagerRegistryEngine.ts    | 477 ++++++++++++++++++
- .../src/tools/dispatchers/businessDispatcher.ts    | 232 +++++++++
- 8 files changed, 2899 insertions(+)


## Verification
**Scrutiny ledger**: arms A✗ B✗ C✗ for session 

## Cross-references
- Full commit: `git -C H:/prism show 814406820905`
- Milestone envelope: `mcp-server/data/milestones/HOTEL.json`

---
_Auto-distilled by `scripts/distill-session-learnings.mjs` per [[feedback_auto_close_out]] / SYSTEM-VIZ-BRAIN-MS0/U-P1-POST-SHIP-DISTILL._