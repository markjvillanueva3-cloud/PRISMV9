# HERMES-UTIL/U-GLM-CONSENSUS-WIRE — [MAIN-FORCE] [HERMES-UTIL]/U-GLM-CONSENSUS-WIRE (slot:zulu, operator 'get it active'): wire GLMClientEngine as the octopus consensus 'zhipu' voice -- 9-spot mirror of the DeepSeek voice (import, vendor union, includeGLM option, key-gate GLM_API_KEY||ZHIPU_API_KEY, available.push, diverse-panel keep-filter, dualOllama guard, calls.push, callGLM branch via glmClientEngine.run). KEY-GATED: includeGLM false without a key so back-compat holds (ConsensusDecideE2E 6/6 green on this keyless host). tsc clean on changed files. GLM-5.2 = set PRISM_GLM_MODEL=glm-5.2. Now ACTIVE-ON-KEY: set GLM_API_KEY -> the voice auto-joins consensus. Probe-banner GLM voice + dedicated includeGLM round-trip test = fast-follow (alpha brief).

**Commit:** `122831a2ac75` · **By:** markjvillanueva3-cloud · **At:** 2026-06-18T12:59:26-05:00
**Tags:** hermes-util, u-glm-consensus-wire, auto-distilled

## Subject
[MAIN-FORCE] [HERMES-UTIL]/U-GLM-CONSENSUS-WIRE (slot:zulu, operator 'get it active'): wire GLMClientEngine as the octopus consensus 'zhipu' voice -- 9-spot mirror of the DeepSeek voice (import, vendor union, includeGLM option, key-gate GLM_API_KEY||ZHIPU_API_KEY, available.push, diverse-panel keep-filter, dualOllama guard, calls.push, callGLM branch via glmClientEngine.run). KEY-GATED: includeGLM false without a key so back-compat holds (ConsensusDecideE2E 6/6 green on this keyless host). tsc clean on changed files. GLM-5.2 = set PRISM_GLM_MODEL=glm-5.2. Now ACTIVE-ON-KEY: set GLM_API_KEY -> the voice auto-joins consensus. Probe-banner GLM voice + dedicated includeGLM round-trip test = fast-follow (alpha brief).

## Body
```
[MAIN-FORCE] [HERMES-UTIL]/U-GLM-CONSENSUS-WIRE (slot:zulu, operator 'get it active'): wire GLMClientEngine as the octopus consensus 'zhipu' voice -- 9-spot mirror of the DeepSeek voice (import, vendor union, includeGLM option, key-gate GLM_API_KEY||ZHIPU_API_KEY, available.push, diverse-panel keep-filter, dualOllama guard, calls.push, callGLM branch via glmClientEngine.run). KEY-GATED: includeGLM false without a key so back-compat holds (ConsensusDecideE2E 6/6 green on this keyless host). tsc clean on changed files. GLM-5.2 = set PRISM_GLM_MODEL=glm-5.2. Now ACTIVE-ON-KEY: set GLM_API_KEY -> the voice auto-joins consensus. Probe-banner GLM voice + dedicated includeGLM round-trip test = fast-follow (alpha brief).
```

## Files touched (2)
- mcp-server/src/engines/MultiModelConsensusEngine.ts | 45 +++++++++++++++++++++++++++++++++++++++++++--
- 1 file changed, 43 insertions(+), 2 deletions(-)

## Lessons surfaced in commit body
- TIL]/U-GLM-CONSENSUS-WIRE (slot:zulu, operator 'get it active'): wire GLMClientEngine as the octopus consensus 'zhipu' voice -- 9-spot mirror of the DeepSeek voice (import, vendor union, includeGLM option, key-gate GLM_API_KEY||ZHIPU_API_KEY, available.push, diverse-panel keep-filter, dualOllama guard, calls.push, callGLM branch via glmClientEngine.run). KEY-GATED: includeGLM false without a key so b

## Verification
**Scrutiny ledger**: arms A✗ B✗ C✗ for session 

## Cross-references
- Full commit: `git -C H:/prism show 122831a2ac75`
- Milestone envelope: `mcp-server/data/milestones/HERMES-UTIL.json`

---
_Auto-distilled by `scripts/distill-session-learnings.mjs` per [[feedback_auto_close_out]] / SYSTEM-VIZ-BRAIN-MS0/U-P1-POST-SHIP-DISTILL._