---
type: "chat-session"
source: "claude-code-cli"
session_id: "908e5108-a8fe-44df-b811-fbe9d85144f9"
title: "You are reviewing a Node.js script in PRISM for completeness and correctness. PA"
date: "2026-05-13"
first_ts: "2026-05-13T22:57:32.816Z"
last_ts: "2026-05-13T22:58:26.066Z"
cwd: "H:\\prism"
messages: 18
user_msgs: 8
assistant_msgs: 10
raw_file: "H:/.claude/projects/H--prism/908e5108-a8fe-44df-b811-fbe9d85144f9/subagents/agent-aafddef044c74f1cf.jsonl"
tags:
  - "chat-archive"
  - "claude-code-cli"
ingested: "2026-06-25T20:40:18"
---

# You are reviewing a Node.js script in PRISM for completeness and correctness. PA

> **claude-code-cli** | 2026-05-13 | 18 msgs (8 user / 10 assistant) | cwd: H:\prism
> Raw: `H:/.claude/projects/H--prism/908e5108-a8fe-44df-b811-fbe9d85144f9/subagents/agent-aafddef044c74f1cf.jsonl`

## Transcript

### User | 2026-05-13T22:57:32.816Z

You are reviewing a Node.js script in PRISM for completeness and correctness. PASS/FAIL grade required.

**File to review (WHOLE file end-to-end, no skimming):**
`H:/prism/scripts/build-system-viz-livediff.mjs` (~488 lines)

**Spec this implements:** CLEANUP-MS0 / U-CLEANUP-G19 — "System-Viz Live Diff". Hourly diff of `state/shared/system-viz/system-graph.json` vs its `.previous.json` sibling. Surfaces per-hour delta: nodes added/removed/changed (with status/tier/businessValue field tracking), edges added/removed, headline counter deltas. Read-only / advisory-only. Outputs `state/shared/SYSTEM_VIZ_LIVEDIFF.{md,json}`. CLI flags: `--json`, `--frozen-time <iso>`, `--current <path>`, `--previous <path>`. Exit always 0.

**Operational context the script lives in:**
- The current production system-graph.json is ~65 MB (auto-regenerated by another pipeline)
- A `system-graph.previous.json` sibling is maintained by that pipeline as the prior snapshot
- This script is intended to run from a cron or one-off CLI — must be robust to missing/oversized/corrupt files (never throw)
- Atomic writes via tmp+rename idiom (matches sibling scripts in this dir)
- Output consumed by operators reading the markdown — quality of the markdown matters

**What I want you to evaluate, file-wide, end-to-end:**
1. Correctness — pure-function diff logic (diffNodes/diffEdges/diffHeadline/buildLayerBreakdown/composeDiffReport). Walk every branch. Could any input produce a wrong answer? Off-by-one? Missing edge case?
2. Edge cases — null/undefined/empty/oversize/duplicate/non-string ids. Does the code degrade gracefully? Are there silent-failure paths where bad input produces a misleading "ok:true" report?
3. Resource bounds — MAX_GRAPH_BYTES=250MB, SAMPLE_NODES=20, SAMPLE_EDGES=10. With a 65MB×2 graph pair, does the function complete in reasonable time + memory? Anything quadratic?
4. CLI ergonomics — argv parsing, `--frozen-time` without value, unknown flags, Windows path handling (the `opts.curren
... [+1271 chars truncated]

### Assistant | 2026-05-13T22:57:37.277Z

I'll review the file end-to-end. Let me start by reading it and comparing against sibling scripts.

### Assistant | 2026-05-13T22:57:37.469Z

[tool_use: Read]

### User | 2026-05-13T22:57:38.803Z

[tool_result] 1	#!/usr/bin/env node
2	/**
3	 * build-system-viz-livediff.mjs — System-Viz Live Diff (CLEANUP-MS0/U-CLEANUP-G19)
4	 *
5	 * Hourly comparison of `state/shared/system-viz/system-graph.json` vs its
6	 * `system-graph.previous.json` sibling (already maintained by the regen
7	 * pipeline). Surfaces the per-hour delta — nodes added/removed/changed,
8	 * edges added/removed, and headline-counter deltas 
... [+19232 chars truncated]

### Assistant | 2026-05-13T22:57:42.151Z

Now let me compare against sibling scripts in the same dir for convention parity.

### Assistant | 2026-05-13T22:57:42.688Z

[tool_use: Glob]

### User | 2026-05-13T22:57:43.041Z

[tool_result] scripts\index\build-shared-index-surfaces.mjs
scripts\audit\build-hook-skill-context-audit.mjs
scripts\build-ppg-catalog.mjs
scripts\index\build-command-bridge.mjs
scripts\build-lathe-engine-registry.mjs
scripts\build-lathe-wiring-audit.mjs
scripts\build-lathe-test-gap.mjs
scripts\build-lathe-physics-inline-scan.mjs
scripts\build-lathe-knowledge-coverage.mjs
scripts\build-engine-index.mjs
scripts\
... [+397 chars truncated]

### Assistant | 2026-05-13T22:57:43.655Z

[tool_use: Grep]

### User | 2026-05-13T22:57:51.178Z

[tool_result] Found 9 files
scripts\build-milestone-progress.mjs
scripts\__tests__\build-system-viz-livediff.test.mjs
scripts\__tests__\jsonl-orphan-scan.test.mjs
scripts\jsonl-orphan-scan.mjs
scripts\inventory-hook-definitions.mjs
scripts\inventory-slash-commands-by-workflow.mjs
scripts\build-state-snapshot.mjs
scripts\close-out-milestone.mjs
scripts\settings-dedup-audit.mjs

### Assistant | 2026-05-13T22:57:54.689Z

[tool_use: Grep]

### User | 2026-05-13T22:57:55.280Z

[tool_result] 291-// Atomic write — tmp + rename prevents byte-interleaved partial reads when
292-// 6 concurrent Claude chats all trigger build-state-inject on SessionStart.
293-function atomicWriteFileSync(targetPath, contents) {
294:  const tmp = `${targetPath}.tmp-${process.pid}-${Date.now()}`;
295-  try {
296-    writeFileSync(tmp, contents, "utf8");
297-    renameSync(tmp, targetPath);

### Assistant | 2026-05-13T22:57:56.030Z

[tool_use: Grep]

### User | 2026-05-13T22:57:56.708Z

[tool_result] 42-// and /checkin fire this script concurrently across 6 chat sessions.
43-function atomicWriteFileSync(targetPath, contents) {
44:  const tmp = `${targetPath}.tmp-${process.pid}-${Date.now()}`;
45-  try {
46-    writeFileSync(tmp, contents, "utf8");

### Assistant | 2026-05-13T22:58:00.012Z

[tool_use: Read]

### Assistant | 2026-05-13T22:58:00.528Z

[tool_use: Read]

### User | 2026-05-13T22:58:01.069Z

[tool_result] 290	
291	// Atomic write — tmp + rename prevents byte-interleaved partial reads when
292	// 6 concurrent Claude chats all trigger build-state-inject on SessionStart.
293	function atomicWriteFileSync(targetPath, contents) {
294	  const tmp = `${targetPath}.tmp-${process.pid}-${Date.now()}`;
295	  try {
296	    writeFileSync(tmp, contents, "utf8");
297	    renameSync(tmp, targetPath);
298	  } catch 
... [+914 chars truncated]

### User | 2026-05-13T22:58:01.497Z

[tool_result] 1	/**
2	 * build-system-viz-livediff.test.mjs — verification of CLEANUP-MS0/U-CLEANUP-G19.
3	 *
4	 * Coverage floor:
5	 *   - happy path
6	 *   - >= 3 failure modes
7	 *   - >= 2 adversarial inputs
8	 *   - >= 3 spanning variability configs
9	 *   - round-trip through CLI entry
10	 *
11	 * Real reference values — no toBeDefined() stubs.
12	 */
13	
14	import { describe, it, expect, beforeEach, afte
... [+1209 chars truncated]

### Assistant | 2026-05-13T22:58:26.066Z

I have enough. Final review below.

```
GRADE: PASS

P0 findings: (none)

P1 findings:
1. Line 430-433 — writeAtomic lacks tmp-cleanup-on-failure (sibling atomicWriteFileSync at build-state-snapshot.mjs:298-301 wraps writeFileSync+renameSync in try/catch with unlinkSync(tmp) on failure). Concurrent cron + manual run leaves stranded `.tmp-<pid>-<ts>` files in `state/shared/` that no one ever cleans. Fix: copy the sibling pattern verbatim — try { write; rename } catch { try{unlink(tmp)}catch{}; throw }.

2. Line 295,298 — `opts.current.includes(":")` absolute-path heuristic is unsound on Windows. A relative path containing a colon (e.g. `notes:draft/graph.json`, an NTFS alternate-data-stream reference, or a drive-relative `C:graph.json` without slash) is misclassified as absolute and skips the `join(repo, ...)` prefix. Fix: use `path.isAbsolute(opts.current)` from `node:path` instead — that's what other scripts (build-headline-history, close-out-milestone) use.

3. Line 461,485 — `process.exitCode = 0` in the write-failure catch block silently swallows write failures. The spec says "Exit always 0", but a cron whose write step failed should at minimum signal something the operator can grep. Currently stderr gets the message and exit is 0 — the message is the only signal. Acceptable per spec, but the redundant assignment (exitCode already defaults to 0) reads like the author intended non-zero and forgot. Fix: drop the `process.exitCode = 0` line or add a comment `// keep exit 0 per spec — stderr is the operator signal`.

4. Line 119 — `nodeKey(n)` assumes `n` is non-null. `diffNodes` filters by Array.isArray but doesn't filter null elements inside the array (`[null, {id:"x"}]` would crash on `n.id`). Same for `edgeKey` (line 122-127) and `layerBucket` (line 212). Fix: add `if (!n || typeof n !== "object") continue;` in the buildIndex/layerBucket loop, or guard at key-fn entry.

5. Line 187-204 — `diffHeadline` silently skips numeric-on-one-side-but-the-other-side-is-a-s
... [+1779 chars truncated]
