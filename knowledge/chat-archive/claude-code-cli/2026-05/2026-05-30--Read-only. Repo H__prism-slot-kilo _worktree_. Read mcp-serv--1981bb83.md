---
type: "chat-session"
source: "claude-code-cli"
session_id: "1981bb83-0e76-4058-af8a-a99dd99987be"
title: "Read-only. Repo H:/prism-slot-kilo (worktree). Read mcp-server/src/engines/Fusio"
date: "2026-05-30"
first_ts: "2026-05-30T03:22:34.678Z"
last_ts: "2026-05-30T03:25:36.474Z"
cwd: "H:\\prism-slot-kilo"
messages: 13
user_msgs: 5
assistant_msgs: 8
raw_file: "H:/.claude/projects/H--prism-slot-kilo/1981bb83-0e76-4058-af8a-a99dd99987be/subagents/workflows/wf_ebb36f84-adf/agent-a45df94c1c7062eeb.jsonl"
tags:
  - "chat-archive"
  - "claude-code-cli"
ingested: "2026-06-25T20:41:00"
---

# Read-only. Repo H:/prism-slot-kilo (worktree). Read mcp-server/src/engines/Fusio

> **claude-code-cli** | 2026-05-30 | 13 msgs (5 user / 8 assistant) | cwd: H:\prism-slot-kilo
> Raw: `H:/.claude/projects/H--prism-slot-kilo/1981bb83-0e76-4058-af8a-a99dd99987be/subagents/workflows/wf_ebb36f84-adf/agent-a45df94c1c7062eeb.jsonl`

## Transcript

### User | 2026-05-30T03:22:34.678Z

Read-only. Repo H:/prism-slot-kilo (worktree). Read mcp-server/src/engines/Fusion360LiveBridgeEngine.ts.
Return EXACT contracts for the CAM WRITE methods: createCamSetup, createCamOperation, assignTool, generateToolpaths, getToolpathStatus, postProcess — full TS signatures (arg types + return types), the request body shape each POSTs to the :18360 add-in (the /cam/* route + JSON payload), and the singleton export name + how it is imported elsewhere.
Also: how the bridge passes operation PARAMETERS today — does createCamOperation take an arbitrary param map or a fixed set? Is there a CAM_PARAM_MAP on the TS side? Quote the exact param-handling code.
Also: the READ methods already wired (listCamOperations/getToolpathValidity/getCycleTime/getCamMaterials) signatures, so I match the pattern.

### Assistant | 2026-05-30T03:22:47.443Z

I'll read the Fusion360LiveBridgeEngine to extract the exact contracts.

### Assistant | 2026-05-30T03:22:47.455Z

[tool_use: Glob]

### User | 2026-05-30T03:22:56.544Z

[tool_result] mcp-server\src\engines\Fusion360LiveBridgeEngine.ts

### Assistant | 2026-05-30T03:22:59.261Z

[tool_use: Read]

### User | 2026-05-30T03:23:09.557Z

[tool_result] 1	/**
2	 * Fusion360LiveBridgeEngine — PRISM-side client for the Fusion 360 API Server Add-In.
3	 * Communicates with the add-in's HTTP server running inside Fusion 360 on localhost:18360.
4	 *
5	 * Provides typed methods for all CAD operations: sketch, extrude, fillet, chamfer,
6	 * revolve, hole, pattern, combine, shell, export, undo, parameters, and raw code execution.
7	 *
8	 * Also supports e
... [+61117 chars truncated]

### Assistant | 2026-05-30T03:23:12.089Z

Now let me verify how the singleton is imported elsewhere to give you the exact import pattern.

### Assistant | 2026-05-30T03:23:13.531Z

[tool_use: Grep]

### User | 2026-05-30T03:23:38.051Z

[tool_result] mcp-server\src\engines\AutoProgramOrchestratorEngine.ts:282:  const m = await import("./Fusion360LiveBridgeEngine.js");
mcp-server\src\engines\AutoProgramOrchestratorEngine.ts:283:  return m.Fusion360LiveBridgeEngine;
mcp-server\src\engines\AutoProgramOrchestratorEngine.ts:977:    const engines: string[] = ["Fusion360LiveBridgeEngine"];
mcp-server\src\engines\AutoProgramOrchestratorEngine.ts:1057:
... [+7847 chars truncated]

### Assistant | 2026-05-30T03:23:40.636Z

I have everything needed. The file was already read; I'm using my existing knowledge of it plus the import-pattern grep.

### Assistant | 2026-05-30T03:24:39.907Z

[tool_use: StructuredOutput]

### User | 2026-05-30T03:25:26.112Z

[tool_result] Structured output provided successfully

### Assistant | 2026-05-30T03:25:36.474Z

I've extracted the exact contracts. Summary of the key answers:

**CAM WRITE methods** (all POST to `:18360`, all use the same `this._post<T>(path, input as unknown as Record<string,unknown>)` pattern):
- `createCamSetup(input: CamSetupInput): Promise<CamSetupResult>` → `POST /cam/setup`
- `createCamOperation(input: CamOperationInput): Promise<CamOperationResult>` → `POST /cam/operation`
- `assignTool(input: AssignToolInput): Promise<AssignToolResult>` → `POST /cam/assign-tool`
- `generateToolpaths(input: ToolpathInput): Promise<ToolpathJobResult>` → `POST /cam/toolpath` (async job, returns `job_id`)
- `getToolpathStatus(jobId: string): Promise<ToolpathStatusResult>` → `GET /cam/toolpath/status?job_id=<enc>` (this one is a GET/poll, not a POST)
- `postProcess(input: PostProcessInput): Promise<PostProcessResult>` → `POST /cam/post`

**Parameter handling — the key finding:** `createCamOperation` takes an **arbitrary param map**, not a fixed set. The field is `parameters?: Record<string, number | string | boolean | undefined>` (line 176). The method passes the whole `input` straight through with zero transformation — the entire object (raw `parameters` map included) is JSON-stringified and POSTed verbatim. **There is NO `CAM_PARAM_MAP` on the TS side** — no CAM param-name translation table exists in this engine. All param-key validation/mapping happens inside the Fusion add-in.

**READ methods (the pattern to match):** `listCamOperations`/`getToolpathValidity`/`getCycleTime` all use `setupName?` → conditional `?name=<enc>` query → `this._get<T>(path)`; `getCamMaterials()` is the zero-arg form `this._get<T>("/cam/materials")`.

**Singleton:** `export const fusion360LiveBridgeEngine = new Fusion360LiveBridgeEngine();` (line 1607), imported everywhere via dynamic `const { fusion360LiveBridgeEngine } = await import("../../engines/Fusion360LiveBridgeEngine.js")` (note the `.js` extension; cadDispatcher memoizes it via `_f360Bridge ??=` at line 40).

Full signatures, the `ST
... [+103 chars truncated]
