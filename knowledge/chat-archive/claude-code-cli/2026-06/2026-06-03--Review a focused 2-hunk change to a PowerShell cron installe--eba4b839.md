---
type: "chat-session"
source: "claude-code-cli"
session_id: "eba4b839-de67-42ff-bc0e-3836c8de27d5"
title: "Review a focused 2-hunk change to a PowerShell cron installer that generates a n"
date: "2026-06-03"
first_ts: "2026-06-03T01:15:02.539Z"
last_ts: "2026-06-03T01:17:40.929Z"
cwd: "H:\\prism"
messages: 26
user_msgs: 9
assistant_msgs: 17
raw_file: "H:/.claude/projects/H--prism/eba4b839-de67-42ff-bc0e-3836c8de27d5/subagents/agent-a9e1812e19099842d.jsonl"
tags:
  - "chat-archive"
  - "claude-code-cli"
ingested: "2026-06-25T20:40:31"
---

# Review a focused 2-hunk change to a PowerShell cron installer that generates a n

> **claude-code-cli** | 2026-06-03 | 26 msgs (9 user / 17 assistant) | cwd: H:\prism
> Raw: `H:/.claude/projects/H--prism/eba4b839-de67-42ff-bc0e-3836c8de27d5/subagents/agent-a9e1812e19099842d.jsonl`

## Transcript

### User | 2026-06-03T01:15:02.539Z

Review a focused 2-hunk change to a PowerShell cron installer that generates a nightly quoting-pipeline wrapper. Read the file end-to-end: H:/prism/scripts/install-quoting-pipeline-cron.ps1 (focus ~line 100-120, the $WrapperContent here-string).

THE CHANGE: the nightly wrapper's Stage2 (quoting-train-cycle) previously trained on `baseline-records-with-synth.json` (a 69-record synthetic merge, guard-admitted but tiny). It now trains on `baseline-records-corpus-with-real.json` (the real 47,905-record JM corpus) with the synth merge passed as `--fallback-corpus` (used only if the real corpus is missing; the train-cycle re-validates either path through the poison-guard). Goal: "keep the closed loop training utilizing ALL documents."

CONTEXT: the here-string uses `@"..."@` with backtick-escaped wrapper-local vars (`` `$BaselineReal ``). The installer AST-parses cleanly; the train-cycle's exact new invocation was live-verified to train on 47,905 records (baseline_source=real corpus, fallback not used).

VERIFY: (1) the here-string escaping is correct — `` `$BaselineReal `` / `` `$BaselineFallback `` become wrapper-local vars, while `$PrismRoot`/`$Stage2` expand at install time; (2) both the tsx and node branches were updated consistently (no stale `$BaselineMerged`); (3) is bypassing the Stage0→Stage1 synth-merge chain for TRAINING sound, or does Stage1 (docustrata) feed something the real corpus lacks? (the real corpus is generated by a separate overlay process, not Stage0/Stage1); (4) any R12 concern — the cron does NOT pass --no-write, so it can activate calibration factors; does training on 47,905 real (but synthetic-revenue-dominant, MAPE 71%) records vs 69 synth records change activation risk? (5) the operator must RE-RUN the installer (elevated) for the live scheduled task to pick this up — is that caveat worth surfacing?

Report P0/P1/P2 with line numbers, grade PASS/FAIL. Concise.

### Assistant | 2026-06-03T01:15:16.209Z

I'll review this change. Let me read the file end-to-end first.

### Assistant | 2026-06-03T01:15:16.261Z

[tool_use: Read]

### User | 2026-06-03T01:15:20.959Z

[tool_result] 1	<#
2	.SYNOPSIS
3	    Register a Windows Scheduled Task that runs the quoting calibration pipeline nightly.
4	
5	.DESCRIPTION
6	    iter26 — operator-facing installer for the iter9-25 quoting calibration chain.
7	    Registers "PRISM Quoting Pipeline" task that runs the full 4-stage chain
8	    (bootstrap -> docustrata-overlay -> train-cycle -> drift-alert) at the
9	    configured time, captures 
... [+7233 chars truncated]

### Assistant | 2026-06-03T01:15:24.268Z

Now let me verify the surrounding facts: the train-cycle's argument handling for `--baseline` / `--fallback-corpus`, whether the synth-merge / docustrata chain feeds anything the real corpus lacks, and the `--no-write` / activation behavior.

### Assistant | 2026-06-03T01:15:24.608Z

[tool_use: Read]

### User | 2026-06-03T01:15:32.505Z

[tool_result] 1	#!/usr/bin/env node
2	/**
3	 * quoting-train-cycle — invoke QuotingTrainingOrchestratorEngine.runOnce() from cron/scheduler.
4	 *
5	 * Operator overnight directive: "keep training the system with quoting".
6	 * yolo-iter1 shipped the orchestrator engine; yolo-iter3 ships the cron-side
7	 * invoker so the loop can actually fire every N minutes via Windows Task Scheduler.
8	 *
9	 * Reads JM Die ba
... [+25083 chars truncated]

### Assistant | 2026-06-03T01:15:35.920Z

Now I understand the train-cycle argument contract. Let me check the resolver to confirm the `--baseline` + `--fallback-corpus` interaction matches what the wrapper passes, and check the CoV/activation gate (R12 concern #4).

### Assistant | 2026-06-03T01:15:36.019Z

[tool_use: Read]

### User | 2026-06-03T01:15:39.996Z

[tool_result] read-once-guard: 'H:\prism\scripts\lib\quoting-baseline-resolve.mjs' was already read 21m ago in this session and is unchanged (mtime 1780447616860). Its content is in your context — use it, don't re-read. For a different part of the file use Grep, or Read with a different offset/limit. If you just edited it, the Edit/Write tools already track the new state for you. (disable: PRISM_READ_CACHE=0)

### Assistant | 2026-06-03T01:15:43.170Z

The resolver content is from a prior context window, not this conversation. Let me read it with explicit override via Grep for the key behaviors.

### Assistant | 2026-06-03T01:15:44.344Z

[tool_use: Grep]

### User | 2026-06-03T01:15:51.759Z

[tool_result] 26- *
27- * Pure + injectable (fs is parameterized) so the resolver is unit-testable without
28- * touching disk. Exports:
29: *   - FALLBACK_CORPORA          canonical real-data corpora, descending preference
30- *   - normalizePath(p)          slash-normalize for dedup/compare
31: *   - buildCandidateList(...)   configured + fallbacks, deduped, order-preserving
32- *   - resolveTrainableBaseline
... [+4231 chars truncated]

### Assistant | 2026-06-03T01:15:55.281Z

I have the resolver contract confirmed. Note one thing I caught in the resolver: line 174 shows `\ ran)` — a stray backslash inside a comment block. Let me verify that's just my Grep context rendering and not an actual syntax error, plus confirm the activation/CoV gate behavior in the orchestrator (R12 concern #4).

### Assistant | 2026-06-03T01:15:56.260Z

[tool_use: Read]

### User | 2026-06-03T01:16:08.812Z

[tool_result] 168	        // `configuredRefused` means specifically "the poison-guard ran on the
169	        // configured baseline and REFUSED it" — NOT "the configured file was
170	        // missing/unreadable/empty". Both return paths use this identical
171	        // definition so a caller can trust the field across either shape (the
172	        // no-admit return below computes it the same way). A missing
... [+376 chars truncated]

### Assistant | 2026-06-03T01:16:13.072Z

Line 174 is clean (`// ran)`) — the `\` was a Grep rendering artifact. Good. Now let me confirm the activation gate (CoV-based) so I can assess R12 concern #4 properly — whether MAPE 71% real corpus can activate a calibration factor that the 69-record synth could not.

### Assistant | 2026-06-03T01:16:14.624Z

[tool_use: Grep]

### User | 2026-06-03T01:16:23.597Z

[tool_result] 7- * the system can self-train on every fresh batch of quote outcomes:
8- *   1. QuotingTrainingLoopEngine.run(records)  → AccuracyReport
9: *   2. QuotingCalibrationEngine.deriveWithCoV(report)
10: *        → factors + Chain-of-Verification verdict + safe_to_activate flag
11: *   3. If safe_to_activate AND writeIfSafe:true → write active-factor JSON
12- *      (atomic temp+rename) so QuotingActiv
... [+2409 chars truncated]

### Assistant | 2026-06-03T01:16:29.837Z

The activation gate is CoV-based (`deriveWithCoV` → `safe_to_activate`), not MAPE-gated directly. The gate keys on coefficient-of-variation across per-customer factors and posterior confidence, independent of absolute MAPE. This is the crux of R12 concern #4 — let me check how CoV is computed to determine whether a 47,905-record synthetic-revenue-dominant corpus is more likely to pass the gate than 69 synth records.

### Assistant | 2026-06-03T01:16:31.869Z

[tool_use: Grep]

### User | 2026-06-03T01:16:38.441Z

[tool_result] 56-export interface DeriveOptions {
57-  /** Minimum records per customer to derive a per-customer factor (below → global). */
58:  minRecordsForCustomer?: number;
59-  /** Lower clamp on factor — protects against negative/zero from extreme outliers. */
60-  minFactor?: number;
--
62-  maxFactor?: number;
63-  /** Treat |signed_pct_error| ≤ this as no-bias (factor=1.0). */
64:  balancedBandPct?: n
... [+2492 chars truncated]

### Assistant | 2026-06-03T01:16:42.019Z

This is the key R12 finding. The activation gate (`safe_to_activate`) flips on `factors.ok && !cov.shouldEscalate && verdict ∈ {confirmed, confirmed_with_caveat}`. Let me see the CoV verification questions and the verifier to understand whether large sample size makes escalation *less* likely (i.e., whether the real corpus is MORE likely to activate than the 69-record synth).

### Assistant | 2026-06-03T01:16:43.007Z

[tool_use: Read]

### User | 2026-06-03T01:16:44.407Z

[tool_result] 285	
286	  /** Build the CoV question set from derived factors. */
287	  private buildCovQuestions(factors: CalibrationFactors, opts: DeriveOptions): VerificationQuestion[] {
288	    const minF = opts.minFactor ?? DEFAULT_MIN_FACTOR;
289	    const maxF = opts.maxFactor ?? DEFAULT_MAX_FACTOR;
290	    const band = opts.balancedBandPct ?? DEFAULT_BALANCED_BAND_PCT;
291	
292	    const q: VerificationQ
... [+4213 chars truncated]

### Assistant | 2026-06-03T01:17:40.929Z

I now have everything needed. The activation gate is purely CoV-based on factor sanity (clamp, math consistency, runaway, sample size ≥10) — it does NOT gate on absolute MAPE. The `record-count-sufficient` question (severity: medium, min 10) is trivially satisfied by both 69 and 47,905 records. This is the substance of the R12 concern. Let me confirm one detail — whether the synthetic-revenue-dominant warning escalates CoV or is merely advisory.

The warning path is clear from train-cycle lines 234-246: `synthetic_revenue_dominant` is a guard *warning* (advisory, training proceeds), not a CoV escalation. CoV operates on derived factors, independent of the synth-revenue warning. So the activation gate is blind to whether revenue is synthetic.

I have enough to render the verdict.

---

## Code Review: install-quoting-pipeline-cron.ps1 (2-hunk Stage2 corpus swap)

**Grade: PASS** (with one P1 the operator must action, two P2 advisories)

### Verification results

**(1) Here-string escaping — CORRECT.** `H:/prism/scripts/install-quoting-pipeline-cron.ps1:107-108,119,121`
The `@"..."@` is a double-quoted (expanding) here-string. The two new wrapper-local vars are backtick-escaped so they survive to the generated file as literal `$BaselineReal` / `$BaselineFallback`:
- L107 `` `$BaselineReal = '$PrismRoot\state\...\baseline-records-corpus-with-real.json' `` — backtick keeps `$BaselineReal` literal; `$PrismRoot` (no backtick) expands at install time to the assignment RHS string. Correct on both counts.
- L119/121 consume `` `$BaselineReal `` / `` `$BaselineFallback `` (literal, wrapper-local) while `$TsxBin`/`$Stage2`/`$NodeExe` expand at install time. Correct. This matches the established pattern of `` `$LogFile ``/`` `$LASTEXITCODE ``/`` `$alertExit `` already in the file (L100,101,110,113 etc.), so it is convention-conformant (R11).

**(2) tsx and node branches consistent — CORRECT.** L118-122
Both branches of the `if (Test-Path '$TsxBin')` carry the identical `--basel
... [+7132 chars truncated]
