---
type: "chat-session"
source: "claude-code-cli"
session_id: "a30723cc-3de1-4276-99a4-4ea33617eedb"
title: "Review a surgical change to `H:/prism/scripts/domain-corpus-to-lora-dataset.mjs`"
date: "2026-06-25"
first_ts: "2026-06-25T02:52:41.747Z"
last_ts: "2026-06-25T02:54:36.136Z"
cwd: "H:\\prism"
messages: 25
user_msgs: 10
assistant_msgs: 15
raw_file: "H:/.claude/projects/H--prism/a30723cc-3de1-4276-99a4-4ea33617eedb/subagents/agent-a8f54c1454cfabfed.jsonl"
tags:
  - "chat-archive"
  - "claude-code-cli"
ingested: "2026-06-25T20:40:20"
---

# Review a surgical change to `H:/prism/scripts/domain-corpus-to-lora-dataset.mjs`

> **claude-code-cli** | 2026-06-25 | 25 msgs (10 user / 15 assistant) | cwd: H:\prism
> Raw: `H:/.claude/projects/H--prism/a30723cc-3de1-4276-99a4-4ea33617eedb/subagents/agent-a8f54c1454cfabfed.jsonl`

## Transcript

### User | 2026-06-25T02:52:41.747Z

Review a surgical change to `H:/prism/scripts/domain-corpus-to-lora-dataset.mjs` (slot:papa). Read the WHOLE file end-to-end.

CONTEXT: This script turns 65 Ollama-rescued resource-PDF specs into domain-tagged Alpaca LoRA pairs. The `--distill` mode calls local Ollama (qwen2.5-coder:14b) once per (PDF,domain) for a grounded Q&A pair. A full `--distill --out` run is ~65 Ollama calls (~10 min), which the harness auto-backgrounds and the fleet-reaper KILLS mid-run — and the original code wrote the output file ONCE at the end, so a kill lost everything (the OCR-loop "burn" class).

THE CHANGE adds reap-resumable streaming: two new pure exported functions `parseCursorDoneSet(cursorText)` and `partitionByResumeCursor(rescued, doneSet)`, plus `main()` now: (a) computes `resume = write && (distill || --resume)`; (b) loads a sibling cursor `<outPath>.cursor.jsonl`; (c) on a FRESH run (empty cursor) truncates outPath; (d) partitions rescued into todo (skipping cursor-done slugs); (e) per entry, appends pairs to outPath AND a cursor line; (f) keeps the non-resume raw path byte-identical (single end-write).

VERIFY (cite file:line, grade PASS/FAIL, flag P0/P1):
1. CORRECTNESS of resume: does a killed+resumed run produce the correct UNION of rows with no double-processing? Is the fresh-run truncate (`doneSet.size===0`) safe — could it ever wipe a partially-complete cursor'd run? (i.e., is there a window where cursor exists but is empty?)
2. BACK-COMPAT: is the non-distill, non-resume path truly byte-identical to before (same single writeFileSync, same summary lines)?
3. CLOBBER SAFETY: a fresh `--distill --out <canonical>` truncates the existing canonical 102-row raw file to rebuild distilled — is that the right call, and is it reversible/intended? Any path where resume mode clobbers a good file on a benign re-invocation?
4. TORN-ROW / GIGO: per-entry append writes complete jsonl lines — can a kill mid-append tear a row? Does parseCursorDoneSet tolerate a torn final cursor line?
... [+310 chars truncated]

### Assistant | 2026-06-25T02:52:47.295Z

I'll review this file end-to-end. Let me start by reading it completely.

### Assistant | 2026-06-25T02:52:47.715Z

[tool_use: Read]

### User | 2026-06-25T02:52:50.848Z

[tool_result] 1	#!/usr/bin/env node
2	/**
3	 * domain-corpus-to-lora-dataset.mjs  (slot:papa 2026-06-24)
4	 *
5	 * THE MISSING PLUMBING: turn the rescued per-domain knowledge (the resource-pdf specs
6	 * that reclassify-domain-feeders-ollama.mjs lifted out of the keyword-unclassified
7	 * residual) into ACTUAL LoRA training data.
8	 *
9	 * getDomainCorpus + the per-domain tribal corpora are a POINTER/orchestrat
... [+17077 chars truncated]

### Assistant | 2026-06-25T02:52:53.765Z

I have the full file. Now let me trace the consumers and the imported contracts to verify the change doesn't silently break anything. Let me check the key dependencies and consumers in parallel.

### Assistant | 2026-06-25T02:52:55.647Z

[tool_use: Bash]

### Assistant | 2026-06-25T02:52:56.351Z

[tool_use: Grep]

### User | 2026-06-25T02:53:11.560Z

[tool_result] knowledge\wiki\consensus\9f573423.md:26:[Omitted long matching line]
knowledge\wiki\consensus\9f573423.md:53:1. **Modify script** – In `scripts/domain-corpus-to-lora-dataset.mjs` replace the raw `pdftotext` output with Q&A pairs generated by `qwen2.5-coder-distilled --distill`.  
knowledge\memories\reference\reference_session_papa_2026-06-25.md:21:- `7c8c734985` [MAIN-FORCE] [DOMAIN-KNOWLEDGE]/U-P
... [+6267 chars truncated]

### User | 2026-06-25T02:53:11.570Z

[tool_result] 7c8c734985 [MAIN-FORCE] [DOMAIN-KNOWLEDGE]/U-PAPA-LORA-DISTILL-MODE (slot:papa): add --distill mode to domain-corpus-to-lora-dataset.mjs -- local Ollama (qwen2.5-coder:14b, R5) synthesizes ONE grounded Q&A pair per (PDF,domain) from the extracted text, MUCH better LoRA than raw text dumps. GIGO-safe: control-byte sanitize + a distill-floor + RAW FALLBACK on any Ollama failure (never blocks). Injec
... [+7140 chars truncated]

### Assistant | 2026-06-25T02:53:15.882Z

Now let me verify the downstream consumer contract (`assemble-fleet-lora-corpus.mjs`) and check whether it would pick up the new `.cursor.jsonl` sibling file or be confused by it, plus check the test file and the inventory registration path.

### Assistant | 2026-06-25T02:53:16.808Z

[tool_use: Read]

### Assistant | 2026-06-25T02:53:17.900Z

[tool_use: Grep]

### User | 2026-06-25T02:53:19.909Z

[tool_result] 90	    id: 'cam-master-training-set',
91	    kind: 'lora-training-jsonl',
92	    path: 'H:/prism/state/shared/corpus/cam-master-training-set.jsonl',
93	    fallbackPath: 'H:/prism/mcp-server/data/state/cam-master-training-set.jsonl',
94	    description: 'CAM-AI-TRAINING-MS0 3766-tuple LoRA training set, 8 tracks (template/physics/param/cross-system/iso286/finish/coolant/operator-gate).',
95	    do
... [+1761 chars truncated]

### User | 2026-06-25T02:53:20.004Z

[tool_result] 8:  parseCursorDoneSet, partitionByResumeCursor,
206:test("parseCursorDoneSet collects every recorded slug, tolerates a torn final line + blanks (R12)", () => {
213:  const done = parseCursorDoneSet(txt);
220:test("parseCursorDoneSet returns an empty Set for empty/non-string/garbage (fresh run + adversarial)", () => {
221:  assert.equal(parseCursorDoneSet("").size, 0);
222:  assert.equal(parseCurs
... [+1037 chars truncated]

### Assistant | 2026-06-25T02:53:24.492Z

Now let me check how `assemble-fleet-lora-corpus.mjs` consumes the path — specifically whether it could glob in the sibling `.cursor.jsonl` and whether the inventory `statPath` would be confused. Let me also confirm the test file's coverage of the streaming path and check the consumer reads exactly the registered path.

### Assistant | 2026-06-25T02:53:26.372Z

[tool_use: Bash]

### Assistant | 2026-06-25T02:53:27.087Z

[tool_use: Read]

### User | 2026-06-25T02:53:29.938Z

[tool_result] 200	  const fail = async () => { throw new Error("down"); };
201	  const fallback = await buildPairsForEntry(entry, sig, REAL_TEXT, { distill: true, fetchImpl: fail });
202	  assert.equal(fallback[0].distilled, undefined, "Ollama failure -> raw fallback (never blocks)");
203	});
204	
205	// ------------------------------- reap-resumable streaming (--distill survives the fleet-reaper kill)
206	test
... [+2661 chars truncated]

### User | 2026-06-25T02:53:32.472Z

[tool_result] 45:const DEFAULT_OUT = path.join(ROOT, "state", "shared", "lora", "fleet-lora-combined.jsonl");
49: * <name>.jsonl -> <name>.stats.json. Deriving it from the (possibly custom) --out
54:  return String(outPath || DEFAULT_OUT).replace(/\.jsonl$/i, "") + ".stats.json";
271:      console.error("usage: assemble-fleet-lora-corpus [--json] [--out [<path.jsonl>]] [--cap-per-galaxy=N]");

### Assistant | 2026-06-25T02:53:37.121Z

The consumer `assemble-fleet-lora-corpus.mjs` reads the inventory-registered exact path (`domain-knowledge-dataset.jsonl`), not a glob — so the sibling `.cursor.jsonl` won't be ingested. Let me confirm that the inventory `statPath`/reader uses the exact registered path and not a directory scan, and verify there's no torn-row test for the streaming append itself. Let me look at how the inventory resolves and how assemble reads each source.

### Assistant | 2026-06-25T02:53:39.029Z

[tool_use: Bash]

### Assistant | 2026-06-25T02:53:40.088Z

[tool_use: Bash]

### User | 2026-06-25T02:53:44.753Z

[tool_result] 9: * kind:'lora-training-jsonl' sources -- but NOTHING read the manifest to actually
64: * Trust weight for a manifest source. Advisory/LLM-distilled corpora (the
90:export function loadInventory(p = INVENTORY_PATH, readImpl = fs.readFileSync) {
102:/** Select the PRESENT lora-training-jsonl sources from the inventory. */
104:  const sources = (inventory && inventory.sources) || [];
105:  return s
... [+1149 chars truncated]

### User | 2026-06-25T02:53:47.869Z

[tool_result] -rw-r--r-- 1 wompu 197611  315101 Jun 24 21:12 domain-knowledge-dataset.jsonl
---wc---
102 H:/prism/state/shared/lora/domain-knowledge-dataset.jsonl

### Assistant | 2026-06-25T02:54:36.136Z

Confirmed: the consumer (`assembleCorpus`, line 174) reads `src.resolvedPath || src.path` — the exact inventory-registered path. The sibling `.cursor.jsonl` is never ingested. The current canonical file is the 102-row raw set (315KB, 102 lines).

Now I have everything needed to answer the 6 verification points. Let me confirm one final concern — the partial-write window on truncate. The fresh-run code does `writeFileSync(outPath, "")` then `writeFileSync(cursorPath, "")`. Let me reason about the "cursor exists but empty" window precisely, and check the `arg()` semantics for `--out` with a value vs `--resume`.

I have all the evidence I need. Let me write up the findings.

## VERDICT: PASS

**summary:** The reap-resumable streaming change is correct and well-bounded: the union-of-rows invariant holds, the non-resume path is byte-identical, the consumer reads the exact registered path (so the sibling cursor is never ingested), and torn rows are tolerated on both the read (`parseCursorDoneSet` catches per-line) and the write (complete-line appends). I found no P0/P1 silent-breakage. There are two genuine behavioral notes worth recording (one design tradeoff the operator already accepted, one true-but-low-severity P2 around transient pdftotext failures) plus one cosmetic P2.

**findings:**

- **[PASS] Resume correctness / union (`domain-corpus-to-lora-dataset.mjs:240-275`)** — On a kill+resume, `parseCursorDoneSet` (`:245`) rebuilds the done-set from the cursor, `partitionByResumeCursor` (`:249`) yields only un-done slugs, and each surviving entry's pairs are append-only (`:272`). Because the cursor line is written *after* the data append (`:273`), a kill between the two re-processes that one slug on resume — at worst a duplicate-slug block, never a missing one. The downstream `assembleCorpus` dedupes across all sources by `(instruction,output)` (`assemble-fleet-lora-corpus.mjs:162-190`), so a re-processed deterministic-raw slug collapses to identical rows; a re-process
... [+6641 chars truncated]
