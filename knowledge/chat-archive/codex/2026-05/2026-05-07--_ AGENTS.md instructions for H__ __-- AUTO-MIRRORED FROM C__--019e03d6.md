---
type: "chat-session"
source: "codex"
session_id: "019e03d6-8c8e-7472-9c70-548ea87ed5d6"
title: "# AGENTS.md instructions for H:\\ <!-- AUTO-MIRRORED FROM C:/Users/Mark Villanuev"
date: "2026-05-07"
first_ts: "2026-05-07T19:08:05.950Z"
last_ts: "2026-05-07T19:56:54.958Z"
cwd: "H:\\"
messages: 25
user_msgs: 5
assistant_msgs: 20
raw_file: "C:/Users/wompu/.codex/sessions/2026/05/07/rollout-2026-05-07T14-07-33-019e03d6-8c8e-7472-9c70-548ea87ed5d6.jsonl"
tags:
  - "chat-archive"
  - "codex"
ingested: "2026-06-25T20:43:34"
---

# # AGENTS.md instructions for H:\ <!-- AUTO-MIRRORED FROM C:/Users/Mark Villanuev

> **codex** | 2026-05-07 | 25 msgs (5 user / 20 assistant) | cwd: H:\
> Raw: `C:/Users/wompu/.codex/sessions/2026/05/07/rollout-2026-05-07T14-07-33-019e03d6-8c8e-7472-9c70-548ea87ed5d6.jsonl`

## Transcript

### User | 2026-05-07T19:08:05.954Z

# AGENTS.md instructions for H:\

<INSTRUCTIONS>
<!-- AUTO-MIRRORED FROM C:/Users/Mark Villanueva/.claude/CLAUDE.md by .claude/helpers/sync-cli-context-files.mjs -->
<!-- Mirror sync: 2026-05-04T19:20:36.750Z · Target: Codex CLI (global) · DO NOT EDIT — edits will be lost on next sync -->
<!-- To update: edit C:/Users/Mark Villanueva/.claude/CLAUDE.md, then run `node H:/PRISM/.claude/helpers/sync-cli-context-files.mjs` -->

# PRISM Global CLAUDE.md — Operational Playbook v2.5

> **Edit `C:\Users\wompu\.claude\settings.json` ONLY** — `c-to-h-mirror` hook auto-replicates C: → H: on every save. Editing `H:\.claude\settings.json` directly gets clobbered on the next mirror fire. (Junction `~/.claude` → `H:\.claude` is NOT active; both dirs are real and the mirror hook keeps them aligned.) Reference tables auto-injected by `reference-inject.mjs` hook when relevant keywords detected.

---

## EXPERT ROLE (ALWAYS ACTIVE)
PhDs across every math/science/eng/CS field. Deep thinker: exhaust obvious + non-obvious paths, edge cases, failure modes, second-order effects. Never "good enough" — optimal with justification.

---

## TOKEN ECONOMY
- **RTK prefix on bash** — `rtk vitest run` (99%), `rtk git/gh/npm/tsc/docker` (60-90% savings). Use in `&&` chains. Skip only if output <500 chars. `/rtk-setup` to install.
- **Ollama offload** — code explain/summarize/docstring/classify/lint/diff-summary/error-triage routed to local qwen2.5-coder:7b via `/ollama-*` skills (9 of them) and `OllamaHookBridgeEngine`. Reserve Claude for deep reasoning + safety. See `feedback_ollama_token_routing.md`.
- **Tool selection** — Glob/Grep over Bash find/grep · `Read offset=X limit=Y` for partial · Parallel independent tool calls in one message · Don't re-read after Edit/Write (hooks track).
- **Context extension** — per-agent `HANDOFF-<id>-<topic>.md` (6 chats), `MEMORY.md` index (<200 lines), digests over exploration, load-on-demand skills, keyword-gated hook injections.

---

## KARPATHY DISCIPLINE (
... [+12150 chars truncated]

### User | 2026-05-07T19:08:05.959Z

PRISM spawned-agent context unavailable. Fall back to CURRENT_POSITION, HANDOFF, SVI-compact, AGENT_WORKBOARD, AGENT_CHAT, ROADMAP_COLLABORATION_STATE, and CLAUDE-CODEX-SPAWNED-AGENT-DIRECTIVE.

Task: read-only support for PRISM Calculator Studio mill validation. The user wants exhaustive variability testing across every logical function/input, with incompatible machine/spindle/holder/tool/toolpath/material choices excluded or explicitly negative-tested, and mathematical cutting result outputs verified as 100% correct relative to inputs and cutting conditions.

Please inspect the served app tree only: H:/PRISM/mcp-server/web and relevant backend/shared math code. Do not edit files. Produce a concise test-matrix proposal and point to existing functions/tests/audit scripts that should be used or extended. Focus on:
- Where mill cutting result math is computed and what fields are generated for CAM/program use.
- Compatibility gates: machine/spindle holder interface, holder bore/tool diameter/reach, tool/toolpath support, material/tool limitations, coolant/workholding, license/toolpath types.
- Existing tests/audit scripts that already cover shards, and gaps for exhaustive logical variability.
- Any obvious invariant formulas or independent oracle strategy for verifying results.
Return file paths and line/function names where possible.

### User | 2026-05-07T19:08:05.960Z

PRISM spawned-agent context unavailable. Fall back to CURRENT_POSITION, HANDOFF, SVI-compact, AGENT_WORKBOARD, AGENT_CHAT, ROADMAP_COLLABORATION_STATE, and CLAUDE-CODEX-SPAWNED-AGENT-DIRECTIVE.

Task: read-only support for PRISM Calculator Studio mill validation. The user wants exhaustive variability testing across every logical function/input, with incompatible machine/spindle/holder/tool/toolpath/material choices excluded or explicitly negative-tested, and mathematical cutting result outputs verified as 100% correct relative to inputs and cutting conditions.

Please inspect the served app tree only: H:/PRISM/mcp-server/web and relevant backend/shared math code. Do not edit files. Produce a concise test-matrix proposal and point to existing functions/tests/audit scripts that should be used or extended. Focus on:
- Where mill cutting result math is computed and what fields are generated for CAM/program use.
- Compatibility gates: machine/spindle holder interface, holder bore/tool diameter/reach, tool/toolpath support, material/tool limitations, coolant/workholding, license/toolpath types.
- Existing tests/audit scripts that already cover shards, and gaps for exhaustive logical variability.
- Any obvious invariant formulas or independent oracle strategy for verifying results.
Return file paths and line/function names where possible.

### Assistant | 2026-05-07T19:08:08.707Z

I’ll do this read-only and stay inside the served app/backend math surface you named. First I’m loading the codebase-memory exploration guidance so I can use the project’s preferred orientation path before drilling into files.

### Assistant | 2026-05-07T19:08:08.708Z

I’ll do this read-only and stay inside the served app/backend math surface you named. First I’m loading the codebase-memory exploration guidance so I can use the project’s preferred orientation path before drilling into files.

### Assistant | 2026-05-07T19:08:17.610Z

The explicit graph tools aren’t exposed in this session, so I’m falling back exactly as requested: current position/handoffs/state files first, then the served app and shared math code. After that I’ll tie findings back to functions and existing test shards.

### Assistant | 2026-05-07T19:08:17.610Z

The explicit graph tools aren’t exposed in this session, so I’m falling back exactly as requested: current position/handoffs/state files first, then the served app and shared math code. After that I’ll tie findings back to functions and existing test shards.

### Assistant | 2026-05-07T19:08:25.385Z

`rtk` isn’t installed on this shell path, so I’m switching to native PowerShell/`rg` for the read-only pass. I’ll keep the output tight and avoid wandering outside the requested app/math area.

### Assistant | 2026-05-07T19:08:25.385Z

`rtk` isn’t installed on this shell path, so I’m switching to native PowerShell/`rg` for the read-only pass. I’ll keep the output tight and avoid wandering outside the requested app/math area.

### Assistant | 2026-05-07T19:09:19.091Z

The state confirms this is the same milling-calculator-validation lane, with prior strict shards through `157-162/255`; there are active claims around `calcDispatcher`, so I’ll stay read-only and avoid touching dispatcher files. Now I’m locating the calculator studio surface and the math/validation engines behind it.

### Assistant | 2026-05-07T19:09:19.092Z

The state confirms this is the same milling-calculator-validation lane, with prior strict shards through `157-162/255`; there are active claims around `calcDispatcher`, so I’ll stay read-only and avoid touching dispatcher files. Now I’m locating the calculator studio surface and the math/validation engines behind it.

### Assistant | 2026-05-07T19:09:27.371Z

`rg` is blocked by the app package path on this machine, so I’m using PowerShell search over scoped directories. The first pass already shows I need targeted searches to keep this sane, so I’m narrowing to calculator/mill/CAM names and likely test scripts.

### Assistant | 2026-05-07T19:09:27.372Z

`rg` is blocked by the app package path on this machine, so I’m using PowerShell search over scoped directories. The first pass already shows I need targeted searches to keep this sane, so I’m narrowing to calculator/mill/CAM names and likely test scripts.

### Assistant | 2026-05-07T19:10:25.989Z

I found the main result engine and the exact output contract: the app builds a speed/feed request, calls `/api/v1/speed-feed/orchestrate|quick`, then normalizes the `SpeedFeedOrchestratorEngine.compute()` result into the calculator view. I’m checking existing shards now so the proposal builds on what’s already there instead of inventing a second test universe.

### Assistant | 2026-05-07T19:10:25.989Z

I found the main result engine and the exact output contract: the app builds a speed/feed request, calls `/api/v1/speed-feed/orchestrate|quick`, then normalizes the `SpeedFeedOrchestratorEngine.compute()` result into the calculator view. I’m checking existing shards now so the proposal builds on what’s already there instead of inventing a second test universe.

### Assistant | 2026-05-07T19:13:00.258Z

I inspected only `H:/PRISM/mcp-server/web` plus the relevant shared/backend math. No edits made.

**Math Surface**
Primary mill cutting math is in [SpeedFeedOrchestratorEngine.ts](H:/PRISM/mcp-server/src/engines/SpeedFeedOrchestratorEngine.ts:2201), `SpeedFeedOrchestratorEngine.compute()`.

Generated CAM/program-use fields are declared in `OrchestratorResult` at [SpeedFeedOrchestratorEngine.ts](H:/PRISM/mcp-server/src/engines/SpeedFeedOrchestratorEngine.ts:234):

- Speed/feed: `cutting_speed_mpm`, `spindle_rpm`, `feed_per_tooth_mm`, `feed_rate_mmmin`, `axial_depth_mm`, `radial_depth_mm`
- Cutting results: `mrr_cm3min`, `power_kw`, `torque_Nm`, `tangential_force_N`, `tool_life_min`, `surface_finish_Ra_um`, `deflection_um`
- Program context: `resolved_machine`, `resolved_tool`, `resolved_material`, `resolved_holder`, `resolved_coolant`, `resolved_workholding`, `resolved_cam_strategy`, `resolved_geometry`
- Validation metadata: `safety_checks`, `limiting_factors`, `stability_assessment`, `formulas_used`, `recommendations`, `alternatives`

Frontend normalization/contract is in [calculatorSpeedFeedContract.ts](H:/PRISM/mcp-server/web/src/utils/calculatorSpeedFeedContract.ts:31), especially `buildCalculatorSpeedFeedParams()` at line 762 and `normalizeCalculatorSpeedFeedResult()` at line 940. The normalized frontend output shape is `CalculatorNormalizedSpeedFeedResult` at line 79.

Core invariant formulas in `compute()`:

```text
RPM = 1000 * Vc / (pi * D)
Vf = fz * flutes * RPM
MRR = ap * ae * Vf / 1000
Power = Fc * Vc / 60000
Torque = Power * 30000 / (pi * RPM)
Ra = fz^2 * 1000 / (32 * corner_radius)
```

Independent oracle helpers already exist in [PowerTorqueCalc.ts](H:/PRISM/mcp-server/src/algorithms/PowerTorqueCalc.ts:117), `PowerTorqueCalcImpl.calculate()`, and [AdvancedCalculations.ts](H:/PRISM/mcp-server/src/engines/AdvancedCalculations.ts:649).

**Compatibility Gates**
Key existing gates to reuse or extend:

- Machine/spindle/holder metadata: [calculatorWorkspace
... [+4821 chars truncated]

### Assistant | 2026-05-07T19:13:00.260Z

I inspected only `H:/PRISM/mcp-server/web` plus the relevant shared/backend math. No edits made.

**Math Surface**
Primary mill cutting math is in [SpeedFeedOrchestratorEngine.ts](H:/PRISM/mcp-server/src/engines/SpeedFeedOrchestratorEngine.ts:2201), `SpeedFeedOrchestratorEngine.compute()`.

Generated CAM/program-use fields are declared in `OrchestratorResult` at [SpeedFeedOrchestratorEngine.ts](H:/PRISM/mcp-server/src/engines/SpeedFeedOrchestratorEngine.ts:234):

- Speed/feed: `cutting_speed_mpm`, `spindle_rpm`, `feed_per_tooth_mm`, `feed_rate_mmmin`, `axial_depth_mm`, `radial_depth_mm`
- Cutting results: `mrr_cm3min`, `power_kw`, `torque_Nm`, `tangential_force_N`, `tool_life_min`, `surface_finish_Ra_um`, `deflection_um`
- Program context: `resolved_machine`, `resolved_tool`, `resolved_material`, `resolved_holder`, `resolved_coolant`, `resolved_workholding`, `resolved_cam_strategy`, `resolved_geometry`
- Validation metadata: `safety_checks`, `limiting_factors`, `stability_assessment`, `formulas_used`, `recommendations`, `alternatives`

Frontend normalization/contract is in [calculatorSpeedFeedContract.ts](H:/PRISM/mcp-server/web/src/utils/calculatorSpeedFeedContract.ts:31), especially `buildCalculatorSpeedFeedParams()` at line 762 and `normalizeCalculatorSpeedFeedResult()` at line 940. The normalized frontend output shape is `CalculatorNormalizedSpeedFeedResult` at line 79.

Core invariant formulas in `compute()`:

```text
RPM = 1000 * Vc / (pi * D)
Vf = fz * flutes * RPM
MRR = ap * ae * Vf / 1000
Power = Fc * Vc / 60000
Torque = Power * 30000 / (pi * RPM)
Ra = fz^2 * 1000 / (32 * corner_radius)
```

Independent oracle helpers already exist in [PowerTorqueCalc.ts](H:/PRISM/mcp-server/src/algorithms/PowerTorqueCalc.ts:117), `PowerTorqueCalcImpl.calculate()`, and [AdvancedCalculations.ts](H:/PRISM/mcp-server/src/engines/AdvancedCalculations.ts:649).

**Compatibility Gates**
Key existing gates to reuse or extend:

- Machine/spindle/holder metadata: [calculatorWorkspace
... [+4821 chars truncated]

### User | 2026-05-07T19:56:33.213Z

Sidecar exploration for the PRISM calculator testing task. Please do not edit files. Inspect local PRISM/JM Die sources for: (1) whether JM Die has a Haas VF-3 machine record anywhere beyond the web test fixture, (2) exact canonical names/IDs/specs for Hurco VM30i vs VMX30i, Haas VF-2/VF-3, Okuma M460V-5AX, and (3) the JM Die lathe machine list we should move to after milling. Return concise findings with absolute file paths and line numbers where possible. I am working locally on the web test/oracle coverage, so avoid duplicate implementation work.

### User | 2026-05-07T19:56:33.213Z

Sidecar exploration for the PRISM calculator testing task. Please do not edit files. Inspect local PRISM/JM Die sources for: (1) whether JM Die has a Haas VF-3 machine record anywhere beyond the web test fixture, (2) exact canonical names/IDs/specs for Hurco VM30i vs VMX30i, Haas VF-2/VF-3, Okuma M460V-5AX, and (3) the JM Die lathe machine list we should move to after milling. Return concise findings with absolute file paths and line numbers where possible. I am working locally on the web test/oracle coverage, so avoid duplicate implementation work.

### Assistant | 2026-05-07T19:56:38.395Z

I’ll keep this read-only and stay out of the web test/oracle lane. I’m going to load the codebase-memory exploration guidance first, then search the local PRISM/JM Die sources for canonical machine records and names.

### Assistant | 2026-05-07T19:56:38.400Z

I’ll keep this read-only and stay out of the web test/oracle lane. I’m going to load the codebase-memory exploration guidance first, then search the local PRISM/JM Die sources for canonical machine records and names.

### Assistant | 2026-05-07T19:56:45.593Z

The dedicated codebase-memory graph tools are not exposed in this session, so I’ll use the closest PRISM memory/session tools if helpful and then fall back to fast local read-only searches. I’m checking `rtk`/`rg` availability before scanning so the output stays tight.

### Assistant | 2026-05-07T19:56:45.594Z

The dedicated codebase-memory graph tools are not exposed in this session, so I’ll use the closest PRISM memory/session tools if helpful and then fall back to fast local read-only searches. I’m checking `rtk`/`rg` availability before scanning so the output stays tight.

### Assistant | 2026-05-07T19:56:52.776Z

`rtk` and `rg` don’t appear to be installed on this machine/session, so I’m switching to scoped PowerShell searches. I’ll start with exact model strings and skip generated/vendor directories.

### Assistant | 2026-05-07T19:56:52.778Z

`rtk` and `rg` don’t appear to be installed on this machine/session, so I’m switching to scoped PowerShell searches. I’ll start with exact model strings and skip generated/vendor directories.
