---
type: "chat-session"
source: "codex"
session_id: "019e196c-8fde-7803-bd50-405001a710ab"
title: "# AGENTS.md instructions for H:\\ <!-- AUTO-MIRRORED FROM C:/Users/Mark Villanuev"
date: "2026-05-11"
first_ts: "2026-05-11T23:44:55.868Z"
last_ts: "2026-05-16T00:22:39.912Z"
cwd: "H:\\"
messages: 108
user_msgs: 6
assistant_msgs: 102
raw_file: "C:/Users/wompu/.codex/sessions/2026/05/11/rollout-2026-05-11T18-43-25-019e196c-8fde-7803-bd50-405001a710ab.jsonl"
tags:
  - "chat-archive"
  - "codex"
ingested: "2026-06-25T20:43:34"
---

# # AGENTS.md instructions for H:\ <!-- AUTO-MIRRORED FROM C:/Users/Mark Villanuev

> **codex** | 2026-05-11 | 108 msgs (6 user / 102 assistant) | cwd: H:\
> Raw: `C:/Users/wompu/.codex/sessions/2026/05/11/rollout-2026-05-11T18-43-25-019e196c-8fde-7803-bd50-405001a710ab.jsonl`

## Transcript

### User | 2026-05-11T23:44:55.873Z

# AGENTS.md instructions for H:\

<INSTRUCTIONS>
<!-- AUTO-MIRRORED FROM C:/Users/Mark Villanueva/.claude/CLAUDE.md by .claude/helpers/sync-cli-context-files.mjs -->
<!-- Mirror sync: 2026-05-04T19:20:36.750Z · Target: Codex CLI (global) · DO NOT EDIT — edits will be lost on next sync -->
<!-- To update: edit C:/Users/Mark Villanueva/.claude/CLAUDE.md, then run `node H:/PRISM/.claude/helpers/sync-cli-context-files.mjs` -->

# PRISM Global CLAUDE.md — Operational Playbook v2.5

> **Edit `C:\Users\wompu\.claude\settings.json` ONLY** — `c-to-h-mirror` hook auto-replicates C: → H: on every save. Editing `H:\.claude\settings.json` directly gets clobbered on the next mirror fire. (Junction `~/.claude` → `H:\.claude` is NOT active; both dirs are real and the mirror hook keeps them aligned.) Reference tables auto-injected by `reference-inject.mjs` hook when relevant keywords detected.

---

## EXPERT ROLE (ALWAYS ACTIVE)
PhDs across every math/science/eng/CS field. Deep thinker: exhaust obvious + non-obvious paths, edge cases, failure modes, second-order effects. Never "good enough" — optimal with justification.

---

## TOKEN ECONOMY
- **RTK prefix on bash** — `rtk vitest run` (99%), `rtk git/gh/npm/tsc/docker` (60-90% savings). Use in `&&` chains. Skip only if output <500 chars. `/rtk-setup` to install.
- **Ollama offload** — code explain/summarize/docstring/classify/lint/diff-summary/error-triage routed to local qwen2.5-coder:7b via `/ollama-*` skills (9 of them) and `OllamaHookBridgeEngine`. Reserve Claude for deep reasoning + safety. See `feedback_ollama_token_routing.md`.
- **Tool selection** — Glob/Grep over Bash find/grep · `Read offset=X limit=Y` for partial · Parallel independent tool calls in one message · Don't re-read after Edit/Write (hooks track).
- **Context extension** — per-agent `HANDOFF-<id>-<topic>.md` (6 chats), `MEMORY.md` index (<200 lines), digests over exploration, load-on-demand skills, keyword-gated hook injections.

---

## KARPATHY DISCIPLINE (
... [+12150 chars truncated]

### User | 2026-05-11T23:44:55.877Z

can you utilize obsidian 2nd brain system, prism awareness and /system-viz that claude built for the prism mcp server. the system-viz is a visual representation of the prism brain galaxy where we quantified everything as nodes that wire into each other like a neural network


### User | 2026-05-11T23:44:55.878Z

can you utilize obsidian 2nd brain system, prism awareness and /system-viz that claude built for the prism mcp server. the system-viz is a visual representation of the prism brain galaxy where we quantified everything as nodes that wire into each other like a neural network


### Assistant | 2026-05-11T23:45:05.262Z

Yes, I can work with those PRISM systems from this workspace. I’m going to do a quick local orientation pass so I can answer from the actual files and affordances here instead of hand-waving.

### Assistant | 2026-05-11T23:45:05.262Z

Yes, I can work with those PRISM systems from this workspace. I’m going to do a quick local orientation pass so I can answer from the actual files and affordances here instead of hand-waving.

### Assistant | 2026-05-11T23:45:13.792Z

RTK isn’t on this shell’s PATH, so I’ll keep the commands compact manually. I’m checking the PRISM root and the Codex skill instructions that describe the codebase-memory path.

### Assistant | 2026-05-11T23:45:13.792Z

RTK isn’t on this shell’s PATH, so I’ll keep the commands compact manually. I’m checking the PRISM root and the Codex skill instructions that describe the codebase-memory path.

### Assistant | 2026-05-11T23:45:24.954Z

The PRISM MCP tools are available in this session now, including self-awareness, session, memory, and knowledge graph entry points. I’m going to ping those directly for `/system-viz` and the “second brain” vocabulary.

### Assistant | 2026-05-11T23:45:24.955Z

The PRISM MCP tools are available in this session now, including self-awareness, session, memory, and knowledge graph entry points. I’m going to ping those directly for `/system-viz` and the “second brain” vocabulary.

### Assistant | 2026-05-11T23:45:29.896Z

A couple of PRISM endpoints responded imperfectly: dispatcher action search returned no `/system-viz` match, semantic memory says Qdrant is not connected, and two self-awareness calls returned an MCP response-type error. That still tells us something useful: the tools are present, but I should verify the on-disk implementation and any command aliases too.

### Assistant | 2026-05-11T23:45:29.896Z

A couple of PRISM endpoints responded imperfectly: dispatcher action search returned no `/system-viz` match, semantic memory says Qdrant is not connected, and two self-awareness calls returned an MCP response-type error. That still tells us something useful: the tools are present, but I should verify the on-disk implementation and any command aliases too.

### Assistant | 2026-05-11T23:45:41.935Z

PRISM session health is green, but the knowledge graph search is currently gated by missing machine spindle fields, and the unified awareness search is answering from tribal machining knowledge rather than the visualizer docs. I’ll switch to the repo’s indexed files and command definitions for the exact `/system-viz` surface.

### Assistant | 2026-05-11T23:45:41.935Z

PRISM session health is green, but the knowledge graph search is currently gated by missing machine spindle fields, and the unified awareness search is answering from tribal machining knowledge rather than the visualizer docs. I’ll switch to the repo’s indexed files and command definitions for the exact `/system-viz` surface.

### Assistant | 2026-05-11T23:46:25.012Z

`rg` is not installed either, so I’ll use Git’s index for fast lookup instead of walking the entire tree. That should keep this tidy even in a large PRISM checkout.

### Assistant | 2026-05-11T23:46:25.012Z

`rg` is not installed either, so I’ll use Git’s index for fast lookup instead of walking the entire tree. That should keep this tidy even in a large PRISM checkout.

### Assistant | 2026-05-11T23:46:57.446Z

Tool discovery exposed the dedicated `prism_knowledge` actions for Obsidian sync and Obsidian-viz. That’s the clean MCP path for the second-brain side; I’ll query read-only statuses now.

### Assistant | 2026-05-11T23:46:57.446Z

Tool discovery exposed the dedicated `prism_knowledge` actions for Obsidian sync and Obsidian-viz. That’s the clean MCP path for the second-brain side; I’ll query read-only statuses now.

### Assistant | 2026-05-11T23:47:02.820Z

The Obsidian sync status returned cleanly: it’s not configured through MCP right now, and the vault path is empty. The `obsidian_viz_*` calls then closed the PRISM transport, so I’ll rely on the local `state/shared/system-viz` artifacts for the visual brain graph until that endpoint is restarted/fixed.

### Assistant | 2026-05-11T23:47:02.820Z

The Obsidian sync status returned cleanly: it’s not configured through MCP right now, and the vault path is empty. The `obsidian_viz_*` calls then closed the PRISM transport, so I’ll rely on the local `state/shared/system-viz` artifacts for the visual brain graph until that endpoint is restarted/fixed.

### User | 2026-05-15T23:43:31.105Z

<environment_context>
  <current_date>2026-05-15</current_date>
  <timezone>America/Chicago</timezone>
</environment_context>

### User | 2026-05-15T23:43:31.110Z

get resynced. pick where you left off in the other chat with full variability calculation testing on the milling page of the prism calculator studio


### User | 2026-05-15T23:43:31.110Z

get resynced. pick where you left off in the other chat with full variability calculation testing on the milling page of the prism calculator studio


### Assistant | 2026-05-15T23:43:43.218Z

I’ll resync from the PRISM handoff/awareness surfaces first, then I’ll locate the Calculator Studio milling page and its current tests before touching code. Since this is frontend testing work, I’m also going to follow the Build Web Apps frontend testing flow once I know how the app runs.

### Assistant | 2026-05-15T23:43:43.218Z

I’ll resync from the PRISM handoff/awareness surfaces first, then I’ll locate the Calculator Studio milling page and its current tests before touching code. Since this is frontend testing work, I’m also going to follow the Build Web Apps frontend testing flow once I know how the app runs.

### Assistant | 2026-05-15T23:43:56.606Z

The worktree is very dirty from other PRISM activity, so I’ll stay scoped and avoid touching unrelated files. I’m looking specifically for the prior handoff/thread notes around “milling”, “Calculator Studio”, and “variability” now.

### Assistant | 2026-05-15T23:43:56.606Z

The worktree is very dirty from other PRISM activity, so I’ll stay scoped and avoid touching unrelated files. I’m looking specifically for the prior handoff/thread notes around “milling”, “Calculator Studio”, and “variability” now.

### Assistant | 2026-05-15T23:44:07.420Z

I found the relevant calculator-restore handoff: the prior lane had just codified the “tests must plan for variability/adaptability” rule and was finishing calculator panel tests. Your new target is milling, so I’ll use that rule but shift from the lathe handoff to the milling page/test surface.

### Assistant | 2026-05-15T23:44:07.420Z

I found the relevant calculator-restore handoff: the prior lane had just codified the “tests must plan for variability/adaptability” rule and was finishing calculator panel tests. Your new target is milling, so I’ll use that rule but shift from the lathe handoff to the milling page/test surface.

### Assistant | 2026-05-15T23:44:58.251Z

There are two milling surfaces: the standalone `MillStudioPage` is still a mocked/orphan-style test, while the routed Calculator Studio mill mode has the richer variability oracle. The older Codex sync says the exact previous checkpoint was strict milling validation shards `157-162/255`; I’m tracing the shard runner and failure logs now.

### Assistant | 2026-05-15T23:44:58.252Z

There are two milling surfaces: the standalone `MillStudioPage` is still a mocked/orphan-style test, while the routed Calculator Studio mill mode has the richer variability oracle. The older Codex sync says the exact previous checkpoint was strict milling validation shards `157-162/255`; I’m tracing the shard runner and failure logs now.

### Assistant | 2026-05-15T23:45:50.205Z

The prior checkpoint points at the routed `/calculator` mill mode, not the standalone orphan `MillStudioPage`: the active coverage is `calculatorJmDieMillVariabilityOracle.test.ts` plus the full-page solve workflow. I’m going to run those focused suites first so the next edit is driven by actual failures.

### Assistant | 2026-05-15T23:45:50.205Z

The prior checkpoint points at the routed `/calculator` mill mode, not the standalone orphan `MillStudioPage`: the active coverage is `calculatorJmDieMillVariabilityOracle.test.ts` plus the full-page solve workflow. I’m going to run those focused suites first so the next edit is driven by actual failures.

### Assistant | 2026-05-15T23:47:07.994Z

The backend-facing variability oracle is green. The full page workflow is failing because the test drives holder choices before the page has switched away from the default face-mill/tooling state, so the UI correctly filters out roughing/finishing holders; the rest are duplicate-confidence assertions. I’m going to adjust the workflow test to follow the actual Calculator Studio dependency order and keep the variability assertions intact.

### Assistant | 2026-05-15T23:47:07.994Z

The backend-facing variability oracle is green. The full page workflow is failing because the test drives holder choices before the page has switched away from the default face-mill/tooling state, so the UI correctly filters out roughing/finishing holders; the rest are duplicate-confidence assertions. I’m going to adjust the workflow test to follow the actual Calculator Studio dependency order and keep the variability assertions intact.

### Assistant | 2026-05-15T23:47:21.828Z

I’m going to edit only the focused workflow test. The change is test-driver correctness: select the cutter family before holder package on mill cases, and assert confidence with a multi-match-safe helper because the UI now shows confidence in more than one result card.

### Assistant | 2026-05-15T23:47:21.828Z

I’m going to edit only the focused workflow test. The change is test-driver correctness: select the cutter family before holder package on mill cases, and assert confidence with a multi-match-safe helper because the UI now shows confidence in more than one result card.

### Assistant | 2026-05-15T23:47:37.655Z

The workflow test is patched. I’m rerunning the same two focused suites now; if the page still rejects a lane, I’ll read the exact option set instead of loosening assertions blindly.

### Assistant | 2026-05-15T23:47:37.655Z

The workflow test is patched. I’m rerunning the same two focused suites now; if the page still rejects a lane, I’ll read the exact option set instead of loosening assertions blindly.

### Assistant | 2026-05-15T23:49:20.537Z

The remaining failures are still test-driver drift: one mill lane is not selecting the exact adaptive rougher, the preflight label now appears in multiple places, and the WEDM holder style is being overwritten by a later tool-family change. I’m tightening those interactions now rather than weakening the calculation checks.

### Assistant | 2026-05-15T23:49:20.538Z

The remaining failures are still test-driver drift: one mill lane is not selecting the exact adaptive rougher, the preflight label now appears in multiple places, and the WEDM holder style is being overwritten by a later tool-family change. I’m tightening those interactions now rather than weakening the calculation checks.

### system

... [28 middle messages omitted -- see raw_file] ...

### Assistant | 2026-05-16T00:04:22.241Z

I added a focused regression test for the exact shape of the live-sweep bug: a fine milling finish with calculated Ra below 0.01 um must remain positive, not round down to zero. Now I’m running the orchestrator test before rebuilding the HTTP bundle.

### Assistant | 2026-05-16T00:04:22.241Z

I added a focused regression test for the exact shape of the live-sweep bug: a fine milling finish with calculated Ra below 0.01 um must remain positive, not round down to zero. Now I’m running the orchestrator test before rebuilding the HTTP bundle.

### Assistant | 2026-05-16T00:04:35.571Z

The new regression assertion passes, but that older dedicated file has unrelated existing failures around `clearCache` and CAM name expectations. I’m moving the new assertion into the broader orchestrator test file that already uses the current import style, so the verification signal stays clean and doesn’t get tangled with stale tests.

### Assistant | 2026-05-16T00:04:35.571Z

The new regression assertion passes, but that older dedicated file has unrelated existing failures around `clearCache` and CAM name expectations. I’m moving the new assertion into the broader orchestrator test file that already uses the current import style, so the verification signal stays clean and doesn’t get tangled with stale tests.

### Assistant | 2026-05-16T00:05:08.034Z

The focused source-level regression is green: 21 orchestrator tests passed, including the new sub-hundredth Ra case. I’m stopping the old HTTP process, rebuilding the server bundle so the live API picks up the source patch, then I’ll rerun the live variability sweep.

### Assistant | 2026-05-16T00:05:08.034Z

The focused source-level regression is green: 21 orchestrator tests passed, including the new sub-hundredth Ra case. I’m stopping the old HTTP process, rebuilding the server bundle so the live API picks up the source patch, then I’ll rerun the live variability sweep.

### Assistant | 2026-05-16T00:05:41.725Z

The server bundle rebuilt successfully with one pre-existing esbuild warning about direct `eval` in `OkumaParametricProgramEngine`. I’m starting the rebuilt API and rerunning the 4,074-scenario sweep to see whether the live anomaly count drops to zero.

### Assistant | 2026-05-16T00:05:41.725Z

The server bundle rebuilt successfully with one pre-existing esbuild warning about direct `eval` in `OkumaParametricProgramEngine`. I’m starting the rebuilt API and rerunning the 4,074-scenario sweep to see whether the live anomaly count drops to zero.

### Assistant | 2026-05-16T00:06:41.519Z

Second live sweep is running against the rebuilt API now. It is at 600/4,074 scenarios; same breadth as before, but now with the sub-hundredth Ra fix in the live dist bundle.

### Assistant | 2026-05-16T00:06:41.519Z

Second live sweep is running against the rebuilt API now. It is at 600/4,074 scenarios; same breadth as before, but now with the sub-hundredth Ra fix in the live dist bundle.

### Assistant | 2026-05-16T00:07:46.208Z

The rebuilt sweep is past 1,900 scenarios, so it’s about halfway. I’m watching for the final report rather than drawing conclusions from progress output, since anomalies are only summarized at the end.

### Assistant | 2026-05-16T00:07:46.208Z

The rebuilt sweep is past 1,900 scenarios, so it’s about halfway. I’m watching for the final report rather than drawing conclusions from progress output, since anomalies are only summarized at the end.

### Assistant | 2026-05-16T00:08:40.092Z

The rerun made it to 2,600/4,074 scenarios and then the client hit `ECONNRESET`. I’m checking whether the server crashed or reset one request under load, then I’ll choose the least noisy verification path rather than pretending that partial sweep is a pass.

### Assistant | 2026-05-16T00:08:40.092Z

The rerun made it to 2,600/4,074 scenarios and then the client hit `ECONNRESET`. I’m checking whether the server crashed or reset one request under load, then I’ll choose the least noisy verification path rather than pretending that partial sweep is a pass.

### Assistant | 2026-05-16T00:08:54.261Z

That reset was not a calculation anomaly: the server process itself exited during the long live sweep, likely from the extremely chatty stdout pipe under thousands of route logs. I’m going to run the rebuilt HTTP API detached with logs redirected to disk, then repeat the sweep so the API process isn’t fighting the terminal output channel.

### Assistant | 2026-05-16T00:08:54.261Z

That reset was not a calculation anomaly: the server process itself exited during the long live sweep, likely from the extremely chatty stdout pipe under thousands of route logs. I’m going to run the rebuilt HTTP API detached with logs redirected to disk, then repeat the sweep so the API process isn’t fighting the terminal output channel.

### Assistant | 2026-05-16T00:10:19.663Z

The detached run is progressing cleanly so far, now past 750/4,074 scenarios. Redirecting API logs is keeping the sweep channel much calmer.

### Assistant | 2026-05-16T00:10:19.663Z

The detached run is progressing cleanly so far, now past 750/4,074 scenarios. Redirecting API logs is keeping the sweep channel much calmer.

### Assistant | 2026-05-16T00:11:40.868Z

I’m resyncing against the PRISM context and the current workspace state first, then I’ll close the loop on the milling variability verification rather than just trusting the old thread’s momentum.

### Assistant | 2026-05-16T00:11:40.868Z

I’m resyncing against the PRISM context and the current workspace state first, then I’ll close the loop on the milling variability verification rather than just trusting the old thread’s momentum.

### Assistant | 2026-05-16T00:12:14.903Z

PRISM MCP is up and green; the awareness query pulled the same milling/surface-finish neighborhood the other chat was circling. I don’t see a direct `/system-viz` tool exposed in this Codex session yet, so I’m using the PRISM awareness/graph surface that is available and checking the local visual/system files next.

### Assistant | 2026-05-16T00:12:14.903Z

PRISM MCP is up and green; the awareness query pulled the same milling/surface-finish neighborhood the other chat was circling. I don’t see a direct `/system-viz` tool exposed in this Codex session yet, so I’m using the PRISM awareness/graph surface that is available and checking the local visual/system files next.

### Assistant | 2026-05-16T00:12:44.814Z

The deeper PRISM knowledge graph endpoint is currently blocked by its own machine-completeness gate, so I’m treating that as a resync signal rather than forcing it. The local changes are narrowly in the orchestrator and calculator workflow tests, with no server still listening on 3001.

### Assistant | 2026-05-16T00:12:44.815Z

The deeper PRISM knowledge graph endpoint is currently blocked by its own machine-completeness gate, so I’m treating that as a resync signal rather than forcing it. The local changes are narrowly in the orchestrator and calculator workflow tests, with no server still listening on 3001.

### Assistant | 2026-05-16T00:15:09.165Z

I found the `/system-viz` assets and used the graph-first query path: `SpeedFeedOrchestratorEngine` is in the graph, and the drift report is fresh enough to trust for this work. I’m going to do one more targeted live endpoint sweep over the exact micro/ball milling cases that produced the surface-finish anomalies, without the optimizer loop that was killing the long server run.

### Assistant | 2026-05-16T00:15:09.165Z

I found the `/system-viz` assets and used the graph-first query path: `SpeedFeedOrchestratorEngine` is in the graph, and the drift report is fresh enough to trust for this work. I’m going to do one more targeted live endpoint sweep over the exact micro/ball milling cases that produced the surface-finish anomalies, without the optimizer loop that was killing the long server run.

### Assistant | 2026-05-16T00:16:21.009Z

The inline probe was too large for Windows’ command-line limit. I’m switching that focused live sweep into a temporary local script, running it, then removing the script so the source tree only keeps the intended fixes.

### Assistant | 2026-05-16T00:16:21.009Z

The inline probe was too large for Windows’ command-line limit. I’m switching that focused live sweep into a temporary local script, running it, then removing the script so the source tree only keeps the intended fixes.

### Assistant | 2026-05-16T00:17:41.926Z

The focused live sweep is moving: 2,304 quick/orchestrate calls over the micro-endmill and ball-endmill milling lanes. That is broader than the original anomaly count, but still avoids the optimizer path that caused the server reset.

### Assistant | 2026-05-16T00:17:41.926Z

The focused live sweep is moving: 2,304 quick/orchestrate calls over the micro-endmill and ball-endmill milling lanes. That is broader than the original anomaly count, but still avoids the optimizer path that caused the server reset.

### Assistant | 2026-05-16T00:18:46.635Z

Still cleanly progressing through the live API, now past 1,700 of 2,304 route calls. If the old Ra rounding bug were still present, these are exactly the lanes that would be lighting up `invalid_surface_finish`.

### Assistant | 2026-05-16T00:18:46.635Z

Still cleanly progressing through the live API, now past 1,700 of 2,304 route calls. If the old Ra rounding bug were still present, these are exactly the lanes that would be lighting up `invalid_surface_finish`.

### Assistant | 2026-05-16T00:19:11.449Z

The focused live sweep passed: 1,152 scenarios, 2,304 live API calls, zero anomalies. The sub-hundredth Ra values are now surviving as real positive numbers instead of being rounded to zero.

### Assistant | 2026-05-16T00:19:11.449Z

The focused live sweep passed: 1,152 scenarios, 2,304 live API calls, zero anomalies. The sub-hundredth Ra values are now surviving as real positive numbers instead of being rounded to zero.

### Assistant | 2026-05-16T00:19:37.591Z

I cleaned up the temporary script and stopped the local API listener. I’m doing a final quick status and regression pass now so the close-out is based on the current tree, not just the previous chat’s memory.

### Assistant | 2026-05-16T00:19:37.592Z

I cleaned up the temporary script and stopped the local API listener. I’m doing a final quick status and regression pass now so the close-out is based on the current tree, not just the previous chat’s memory.

### Assistant | 2026-05-16T00:21:39.958Z

The PRISM checkpoint endpoint accepted the event shape but skipped writing because it still considers the zone green; semantic memory couldn’t persist because Qdrant is disconnected. I’ve got the result locally in the generated sweep report and the changed tests.

### Assistant | 2026-05-16T00:21:39.958Z

The PRISM checkpoint endpoint accepted the event shape but skipped writing because it still considers the zone green; semantic memory couldn’t persist because Qdrant is disconnected. I’ve got the result locally in the generated sweep report and the changed tests.

### Assistant | 2026-05-16T00:22:39.804Z

Resynced and carried it forward.

I used PRISM awareness plus the `/system-viz` graph/query path. The live system-viz server log still points at a missing `_server.cjs`, so I used the graph files/query adapter and MCP awareness instead.

Changed:
- [SpeedFeedOrchestratorEngine.ts](H:/PRISM/mcp-server/src/engines/SpeedFeedOrchestratorEngine.ts:50): added `roundSurfaceFinishRa()` so fine milling Ra values under `0.01 um` keep significant digits instead of rounding to `0`.
- [speed-feed-orchestrator.test.ts](H:/PRISM/mcp-server/src/__tests__/speed-feed-orchestrator.test.ts:182): added a regression for sub-hundredth Ra.
- [CalculatorPage.solveWorkflows.test.tsx](H:/PRISM/mcp-server/web/src/__tests__/CalculatorPage.solveWorkflows.test.tsx:140): hardened the full Calculator Studio solve workflow selectors and EDM expectation.

Verification:
- `speed-feed-orchestrator.test.ts`: 21 passed
- `CalculatorPage.solveWorkflows.test.tsx`: 6 passed
- `npm run build:fast`: passed, with the existing Okuma direct-`eval` warning
- Focused live milling sweep: [calculator-live-mill-surface-focused-2026-05-15.json](H:/PRISM/output/calculator-live-mill-surface-focused-2026-05-15.json)
  - 1,152 micro/ball milling scenarios
  - 2,304 live quick/orchestrate API calls
  - `anomalyCount: 0`
  - micro-endmill Ra range: `0.00145` to `0.00573`
  - ball-endmill Ra range: `0.00127` to `0.03`

One honest caveat: the full long live sweep including `sf_optimize` still exposes a separate server stability/reset issue under load. The milling surface-finish variability bug itself is fixed and verified across the anomaly class.

### Assistant | 2026-05-16T00:22:39.804Z

Resynced and carried it forward.

I used PRISM awareness plus the `/system-viz` graph/query path. The live system-viz server log still points at a missing `_server.cjs`, so I used the graph files/query adapter and MCP awareness instead.

Changed:
- [SpeedFeedOrchestratorEngine.ts](H:/PRISM/mcp-server/src/engines/SpeedFeedOrchestratorEngine.ts:50): added `roundSurfaceFinishRa()` so fine milling Ra values under `0.01 um` keep significant digits instead of rounding to `0`.
- [speed-feed-orchestrator.test.ts](H:/PRISM/mcp-server/src/__tests__/speed-feed-orchestrator.test.ts:182): added a regression for sub-hundredth Ra.
- [CalculatorPage.solveWorkflows.test.tsx](H:/PRISM/mcp-server/web/src/__tests__/CalculatorPage.solveWorkflows.test.tsx:140): hardened the full Calculator Studio solve workflow selectors and EDM expectation.

Verification:
- `speed-feed-orchestrator.test.ts`: 21 passed
- `CalculatorPage.solveWorkflows.test.tsx`: 6 passed
- `npm run build:fast`: passed, with the existing Okuma direct-`eval` warning
- Focused live milling sweep: [calculator-live-mill-surface-focused-2026-05-15.json](H:/PRISM/output/calculator-live-mill-surface-focused-2026-05-15.json)
  - 1,152 micro/ball milling scenarios
  - 2,304 live quick/orchestrate API calls
  - `anomalyCount: 0`
  - micro-endmill Ra range: `0.00145` to `0.00573`
  - ball-endmill Ra range: `0.00127` to `0.03`

One honest caveat: the full long live sweep including `sf_optimize` still exposes a separate server stability/reset issue under load. The milling surface-finish variability bug itself is fixed and verified across the anomaly class.
