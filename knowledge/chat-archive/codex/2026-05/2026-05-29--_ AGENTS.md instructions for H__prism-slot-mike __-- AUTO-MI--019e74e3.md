---
type: "chat-session"
source: "codex"
session_id: "019e74e3-4de0-77a1-ad59-441b74a7c561"
title: "# AGENTS.md instructions for H:\\prism-slot-mike <!-- AUTO-MIRRORED FROM C:/Users"
date: "2026-05-29"
first_ts: "2026-05-29T17:59:12.914Z"
last_ts: "2026-05-29T18:02:29.242Z"
cwd: "H:\\prism-slot-mike"
messages: 11
user_msgs: 3
assistant_msgs: 8
raw_file: "C:/Users/wompu/.codex/sessions/2026/05/29/rollout-2026-05-29T12-58-34-019e74e3-4de0-77a1-ad59-441b74a7c561.jsonl"
tags:
  - "chat-archive"
  - "codex"
ingested: "2026-06-25T20:43:35"
---

# # AGENTS.md instructions for H:\prism-slot-mike <!-- AUTO-MIRRORED FROM C:/Users

> **codex** | 2026-05-29 | 11 msgs (3 user / 8 assistant) | cwd: H:\prism-slot-mike
> Raw: `C:/Users/wompu/.codex/sessions/2026/05/29/rollout-2026-05-29T12-58-34-019e74e3-4de0-77a1-ad59-441b74a7c561.jsonl`

## Transcript

### User | 2026-05-29T17:59:12.917Z

# AGENTS.md instructions for H:\prism-slot-mike

<INSTRUCTIONS>
<!-- AUTO-MIRRORED FROM C:/Users/Mark Villanueva/.claude/CLAUDE.md by .claude/helpers/sync-cli-context-files.mjs -->
<!-- Mirror sync: 2026-05-12T16:44:31.599Z · Target: Codex CLI (global) · DO NOT EDIT — edits will be lost on next sync -->
<!-- To update: edit C:/Users/Mark Villanueva/.claude/CLAUDE.md, then run `node H:/PRISM/.claude/helpers/sync-cli-context-files.mjs` -->

# PRISM Global CLAUDE.md — Operational Playbook v2.5

> **Edit `C:\Users\wompu\.claude\settings.json` ONLY** — `c-to-h-mirror` hook auto-replicates C: → H: on every save. Editing `H:\.claude\settings.json` directly gets clobbered on the next mirror fire. (Junction `~/.claude` → `H:\.claude` is NOT active; both dirs are real and the mirror hook keeps them aligned.) Reference tables auto-injected by `reference-inject.mjs` hook when relevant keywords detected.

---

## EXPERT ROLE (ALWAYS ACTIVE)
PhDs across every math/science/eng/CS field. Deep thinker: exhaust obvious + non-obvious paths, edge cases, failure modes, second-order effects. Never "good enough" — optimal with justification.

---

## TOKEN ECONOMY
- **RTK prefix on bash** — `rtk vitest run` (99%), `rtk git/gh/npm/tsc/docker` (60-90% savings). Use in `&&` chains. Skip only if output <500 chars. `/rtk-setup` to install.
- **Ollama offload** — code explain/summarize/docstring/classify/lint/diff-summary/error-triage routed to local qwen2.5-coder:7b via `/ollama-*` skills (9 of them) and `OllamaHookBridgeEngine`. Reserve Claude for deep reasoning + safety. See `feedback_ollama_token_routing.md`.
- **Tool selection** — Glob/Grep over Bash find/grep · `Read offset=X limit=Y` for partial · Parallel independent tool calls in one message · Don't re-read after Edit/Write (hooks track).
- **Context extension** — per-agent `HANDOFF-<id>-<topic>.md` (6 chats), `MEMORY.md` index (<200 lines), digests over exploration, load-on-demand skills, keyword-gated hook injections.

---

## KARPAT
... [+37821 chars truncated]

### User | 2026-05-29T17:59:12.918Z

READ-ONLY final assessment of the PRISM WEDM (wire-EDM) galaxy for slot:mike. Working dir: H:/prism-slot-mike. DO NOT MODIFY OR CREATE ANY FILE — this is a review only; if you cannot read a file, say so rather than guessing.

(The prior attempt failed because the read-only sandbox could not access files on this Windows host; you now have filesystem read access. Use `cat`/`sed`/`rg`/`type` — whatever works — but make NO edits.)

Read + critically assess:
- mcp-server/src/engines/wedm/{CLAUDE.md,MEMORY.md,PATHS.md,TOOLBELT.md}
- mcp-server/src/engines/WEDMKnowledgeIndexEngine.ts + wedm-knowledge-index-loader.ts
- scripts/build-wedm-knowledge-index.mjs + scripts/build-wedm-file-index.mjs
- dispatcher wiring: rg "wedm_knowledge_index" mcp-server/src/tools/dispatchers/edmDispatcher.ts
- tests: mcp-server/src/__tests__/WEDMKnowledgeIndexEngine.test.ts + wedm-knowledge-index-loader.test.ts

Answer in under ~400 words. One-line VERDICT first (SOLID / NEEDS-WORK / BROKEN), then:
1. Is WEDMKnowledgeIndexEngine correct+complete? Any real bug/edge case the tests miss (ranking, confidence 0..100-vs-0..1 normalization, dedupe, no-match-vs-no-criteria branch, source weighting)?
2. Dispatcher wiring complete+consistent (enum + getEngine + 2 case bodies) or half-wired?
3. Galaxy brain genuinely connected/useful or cosmetic?
4. Top 3 concrete ranked gaps/improvements.

### User | 2026-05-29T17:59:12.918Z

READ-ONLY final assessment of the PRISM WEDM (wire-EDM) galaxy for slot:mike. Working dir: H:/prism-slot-mike. DO NOT MODIFY OR CREATE ANY FILE — this is a review only; if you cannot read a file, say so rather than guessing.

(The prior attempt failed because the read-only sandbox could not access files on this Windows host; you now have filesystem read access. Use `cat`/`sed`/`rg`/`type` — whatever works — but make NO edits.)

Read + critically assess:
- mcp-server/src/engines/wedm/{CLAUDE.md,MEMORY.md,PATHS.md,TOOLBELT.md}
- mcp-server/src/engines/WEDMKnowledgeIndexEngine.ts + wedm-knowledge-index-loader.ts
- scripts/build-wedm-knowledge-index.mjs + scripts/build-wedm-file-index.mjs
- dispatcher wiring: rg "wedm_knowledge_index" mcp-server/src/tools/dispatchers/edmDispatcher.ts
- tests: mcp-server/src/__tests__/WEDMKnowledgeIndexEngine.test.ts + wedm-knowledge-index-loader.test.ts

Answer in under ~400 words. One-line VERDICT first (SOLID / NEEDS-WORK / BROKEN), then:
1. Is WEDMKnowledgeIndexEngine correct+complete? Any real bug/edge case the tests miss (ranking, confidence 0..100-vs-0..1 normalization, dedupe, no-match-vs-no-criteria branch, source weighting)?
2. Dispatcher wiring complete+consistent (enum + getEngine + 2 case bodies) or half-wired?
3. Galaxy brain genuinely connected/useful or cosmetic?
4. Top 3 concrete ranked gaps/improvements.

### Assistant | 2026-05-29T17:59:26.985Z

Using the codebase-memory exploration guidance because this is a targeted code assessment. I’ll keep this strictly read-only and fall back to direct file reads if the graph tooling is not available.

### Assistant | 2026-05-29T17:59:26.985Z

Using the codebase-memory exploration guidance because this is a targeted code assessment. I’ll keep this strictly read-only and fall back to direct file reads if the graph tooling is not available.

### Assistant | 2026-05-29T17:59:50.511Z

The graph-specific MCP from that skill is not exposed here; the available PRISM tools are session/memory surfaces, not `search_graph`/`get_architecture`. I’m moving to the exact direct reads the review calls for.

### Assistant | 2026-05-29T17:59:50.511Z

The graph-specific MCP from that skill is not exposed here; the available PRISM tools are session/memory surfaces, not `search_graph`/`get_architecture`. I’m moving to the exact direct reads the review calls for.

### Assistant | 2026-05-29T18:00:34.392Z

I’ve got the core engine and generators. Next I’m checking the dispatcher with line numbers and the tests against the edge cases the prompt names, especially query semantics and confidence/ranking.

### Assistant | 2026-05-29T18:00:34.393Z

I’ve got the core engine and generators. Next I’m checking the dispatcher with line numbers and the tests against the edge cases the prompt names, especially query semantics and confidence/ranking.

### Assistant | 2026-05-29T18:02:29.239Z

NEEDS-WORK — core query path is mostly solid and dispatcher-wired, but completeness is overclaimed and tests allow the wiki half to vanish silently.

1. `WEDMKnowledgeIndexEngine` is basically correct: confidence normalization, no-criteria list-all vs no-match empty, dedupe-by-id, and source weighting are implemented. Missed edge cases: blank criteria like `keywords:[" "]` becomes a criteria query and returns empty; wiki dedupe uses only basename slug, so same filename in different wiki dirs collides; tags/topics are lowercased but not trimmed/deduped; ranking is simple substring scoring, and the “wiki ranks above tribal” test does not actually assert ordering. Loader fail-soft also hides missing/bad wiki JSON, and tests explicitly accept `wiki >= 0`, so tribal-only can pass.

2. Dispatcher wiring is complete inside `edmDispatcher.ts`: cached field, `getEngine("knowledgeIndex")`, action enum entries, and both query/stats cases are present. I found no `wedm_knowledge_index` refs in `edmActionSchemas.ts`, so any schema-driven validation surface may still be unwired.

3. The galaxy brain is useful, not purely cosmetic: docs point to paths, dispatcher actions, generated indexes, and WEDM doctrine. But it overstates completeness. Generated `WEDM_WIKI_KNOWLEDGE.json` currently has 5 wiki docs, not the broad “206/43” wiki surface implied elsewhere; `build-wedm-file-index.mjs` is a shallow known-dir manifest, not truly “ALL” H-wide WEDM files.

4. Top gaps:
   1. Make loader/generator E2E fail if curated wiki JSON is missing, malformed, or unexpectedly tiny.
   2. Add tests for blank criteria, duplicate wiki basenames, actual ranking order, and dispatcher query/stats.
   3. Either broaden the wiki/file indexers or rewrite the docs to call them curated/narrow indexes instead of complete galaxy coverage.

### Assistant | 2026-05-29T18:02:29.239Z

NEEDS-WORK — core query path is mostly solid and dispatcher-wired, but completeness is overclaimed and tests allow the wiki half to vanish silently.

1. `WEDMKnowledgeIndexEngine` is basically correct: confidence normalization, no-criteria list-all vs no-match empty, dedupe-by-id, and source weighting are implemented. Missed edge cases: blank criteria like `keywords:[" "]` becomes a criteria query and returns empty; wiki dedupe uses only basename slug, so same filename in different wiki dirs collides; tags/topics are lowercased but not trimmed/deduped; ranking is simple substring scoring, and the “wiki ranks above tribal” test does not actually assert ordering. Loader fail-soft also hides missing/bad wiki JSON, and tests explicitly accept `wiki >= 0`, so tribal-only can pass.

2. Dispatcher wiring is complete inside `edmDispatcher.ts`: cached field, `getEngine("knowledgeIndex")`, action enum entries, and both query/stats cases are present. I found no `wedm_knowledge_index` refs in `edmActionSchemas.ts`, so any schema-driven validation surface may still be unwired.

3. The galaxy brain is useful, not purely cosmetic: docs point to paths, dispatcher actions, generated indexes, and WEDM doctrine. But it overstates completeness. Generated `WEDM_WIKI_KNOWLEDGE.json` currently has 5 wiki docs, not the broad “206/43” wiki surface implied elsewhere; `build-wedm-file-index.mjs` is a shallow known-dir manifest, not truly “ALL” H-wide WEDM files.

4. Top gaps:
   1. Make loader/generator E2E fail if curated wiki JSON is missing, malformed, or unexpectedly tiny.
   2. Add tests for blank criteria, duplicate wiki basenames, actual ranking order, and dispatcher query/stats.
   3. Either broaden the wiki/file indexers or rewrite the docs to call them curated/narrow indexes instead of complete galaxy coverage.
