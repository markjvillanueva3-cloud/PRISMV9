---
type: "chat-session"
source: "codex"
session_id: "019df37e-c414-7551-92cc-90db8365c43c"
title: "# AGENTS.md instructions for H:\\PRISM # Codex Global PRISM Rules Canonical repo "
date: "2026-05-04"
first_ts: "2026-05-04T14:58:58.968Z"
last_ts: "2026-05-04T15:02:12.381Z"
cwd: "H:\\PRISM"
messages: 13
user_msgs: 3
assistant_msgs: 10
raw_file: "C:/Users/wompu/.codex/sessions/2026/05/04/rollout-2026-05-04T09-57-44-019df37e-c414-7551-92cc-90db8365c43c.jsonl"
tags:
  - "chat-archive"
  - "codex"
ingested: "2026-06-25T20:43:33"
---

# # AGENTS.md instructions for H:\PRISM # Codex Global PRISM Rules Canonical repo 

> **codex** | 2026-05-04 | 13 msgs (3 user / 10 assistant) | cwd: H:\PRISM
> Raw: `C:/Users/wompu/.codex/sessions/2026/05/04/rollout-2026-05-04T09-57-44-019df37e-c414-7551-92cc-90db8365c43c.jsonl`

## Transcript

### User | 2026-05-04T14:58:58.979Z

# AGENTS.md instructions for H:\PRISM

<INSTRUCTIONS>
# Codex Global PRISM Rules

Canonical repo copy: `H:/PRISM/.codex/AGENTS.md`
Keep any profile-side mirror pointed at this repo copy.

## CRITICAL COMMANDS — MUST AUTO-SUGGEST

When ANY of these triggers are detected, IMMEDIATELY suggest the corresponding command:

| Command | Triggers | Purpose |
|---------|----------|---------|
| `/pdf-learn` | pdf, document, manual, catalog, paper, extract | AI-powered PDF extraction into tribal tips |
| `/video-learn` | video, youtube, tutorial, training, watch | AI-powered video extraction into procedures |
| `/forge-triple` | create engine, new engine, build engine, forge | Create engines+skills+hooks (EXHAUSTIVE) |
| `/dedup` | duplicate, before creating, check existing | MANDATORY before creating ANY asset |
| `/wire-edm-studio` | wire edm, wedm, edm program, wire cut | Full Wire EDM programming studio |
| `/lathe-studio` | lathe, turning, okuma, turning center | Full lathe programming studio |
| `/quote-to-ship` | quote, estimate, job cost, order | Quote-to-ship pipeline orchestration |
| `/auto-speed-feed` | speed, feed, sfm, ipm, rpm | Auto speed/feed calculation |
| `/shop-knowledge` | tribal, shop floor, operator wisdom | Tribal knowledge extraction |

**MANDATORY RULES:**
1. ALWAYS suggest `/pdf-learn` when ANY PDF or document is mentioned
2. ALWAYS suggest `/video-learn` when ANY video or tutorial is mentioned  
3. ALWAYS run `/dedup` BEFORE creating ANY new engine/hook/skill
4. ALWAYS suggest `/forge-triple` (not `/forge-engine`) for new capabilities

**Cross-Session Coordination:**
- Read `H:/prism/state/shared/UNIFIED_COMMAND_BROADCAST.json` for command awareness
- Check `H:/prism/state/shared/ACTIVE_WORK_REGISTRY.json` before starting work
- Post to `H:/prism/state/shared/AGENT_CHAT.jsonl` when doing significant work

**On Codex Session Start, run:**
```powershell
node H:\prism\.claude\helpers\codex-command-awareness.mjs
```
This gives Codex the same command awa
... [+9602 chars truncated]

### User | 2026-05-04T14:58:58.984Z

You are reviewing a multi-model consensus orchestration pipeline running in a 6-simultaneous-Claude-terminals environment. Find concrete defects in the code below.

Look for:
1. Race conditions / concurrency bugs (file-based inflight registry, cache, budget — read/write races between terminals)
2. Edge cases the tests miss (what happens if a hash collides? clock skew? partial JSONL writes? full-disk?)
3. Dead code, half-finished patterns, unreachable branches
4. Over-engineering — features that aren't actually used
5. Sub-optimal algorithms — O(N) where O(log N) is feasible, redundant work
6. Resource leaks — unclosed handles, unbounded files

For each finding give: SEVERITY (critical|high|medium|low), the FILE+LINE if locatable, the BUG, and the FIX. If the code is good, say so explicitly — don't manufacture findings.

Be terse. Skip preamble. Top 5 findings only.

=== FILES ===
### H:/prism-iooms0/mcp-server/src/engines/ConsensusCoordinatorEngine.ts
```typescript
/**
 * ConsensusCoordinatorEngine — concurrency-aware wrapper around MultiModelConsensusEngine.
 *
 * Milestone: INTEL-OLLAMA-OBSIDIAN-MS0 / AUTO-CONSENSUS.
 *
 * Designed for the 6-simultaneous-Claude-terminals reality: every consensus
 * call hits a shared external resource (Codex API, Ollama daemon, Claude
 * subprocess), so naive auto-fan-out from every terminal would saturate the
 * fleet and exhaust the daily Codex token budget within minutes.
 *
 * Coordinator responsibilities:
 *   1. **Cache** — prompt+taskType hash → result (1h TTL, file-backed JSONL).
 *      A terminal that already consensused on prompt P returns cached result
 *      instantly to the next terminal that asks the same.
 *   2. **In-flight tracking** — file-locked registry at
 *      data/state/consensus-inflight.json. Each entry: {terminalId, hash,
 *      startedAt}. Max 1 per terminal, max 3 globally.
 *   3. **Token budget** — daily Codex token cap (default 500k via
 *      PRISM_CONSENSUS_DAILY_BUDGET env). Refuses fan-out 
... [+33606 chars truncated]

### User | 2026-05-04T14:58:58.984Z

You are reviewing a multi-model consensus orchestration pipeline running in a 6-simultaneous-Claude-terminals environment. Find concrete defects in the code below.

Look for:
1. Race conditions / concurrency bugs (file-based inflight registry, cache, budget — read/write races between terminals)
2. Edge cases the tests miss (what happens if a hash collides? clock skew? partial JSONL writes? full-disk?)
3. Dead code, half-finished patterns, unreachable branches
4. Over-engineering — features that aren't actually used
5. Sub-optimal algorithms — O(N) where O(log N) is feasible, redundant work
6. Resource leaks — unclosed handles, unbounded files

For each finding give: SEVERITY (critical|high|medium|low), the FILE+LINE if locatable, the BUG, and the FIX. If the code is good, say so explicitly — don't manufacture findings.

Be terse. Skip preamble. Top 5 findings only.

=== FILES ===
### H:/prism-iooms0/mcp-server/src/engines/ConsensusCoordinatorEngine.ts
```typescript
/**
 * ConsensusCoordinatorEngine — concurrency-aware wrapper around MultiModelConsensusEngine.
 *
 * Milestone: INTEL-OLLAMA-OBSIDIAN-MS0 / AUTO-CONSENSUS.
 *
 * Designed for the 6-simultaneous-Claude-terminals reality: every consensus
 * call hits a shared external resource (Codex API, Ollama daemon, Claude
 * subprocess), so naive auto-fan-out from every terminal would saturate the
 * fleet and exhaust the daily Codex token budget within minutes.
 *
 * Coordinator responsibilities:
 *   1. **Cache** — prompt+taskType hash → result (1h TTL, file-backed JSONL).
 *      A terminal that already consensused on prompt P returns cached result
 *      instantly to the next terminal that asks the same.
 *   2. **In-flight tracking** — file-locked registry at
 *      data/state/consensus-inflight.json. Each entry: {terminalId, hash,
 *      startedAt}. Max 1 per terminal, max 3 globally.
 *   3. **Token budget** — daily Codex token cap (default 500k via
 *      PRISM_CONSENSUS_DAILY_BUDGET env). Refuses fan-out 
... [+33606 chars truncated]

### Assistant | 2026-05-04T14:59:24.818Z

Using `codebase-memory-quality` because this is a defect/dead-code review. I’ll use the provided snippets as the source of truth and only touch the filesystem if needed for line numbers.

### Assistant | 2026-05-04T14:59:24.819Z

Using `codebase-memory-quality` because this is a defect/dead-code review. I’ll use the provided snippets as the source of truth and only touch the filesystem if needed for line numbers.

### Assistant | 2026-05-04T14:59:53.498Z

I’m checking the actual files for stable line numbers now. The code-memory MCP tools referenced by the skill aren’t exposed in this session, so I’m falling back to local line-number lookup against the supplied paths.

### Assistant | 2026-05-04T14:59:53.499Z

I’m checking the actual files for stable line numbers now. The code-memory MCP tools referenced by the skill aren’t exposed in this session, so I’m falling back to local line-number lookup against the supplied paths.

### Assistant | 2026-05-04T15:00:24.878Z

`rg` isn’t installed here, so I’m switching to PowerShell’s native `Select-String` for the line lookup. The command-awareness helper was blocked by policy; that doesn’t affect the code review.

### Assistant | 2026-05-04T15:00:24.879Z

`rg` isn’t installed here, so I’m switching to PowerShell’s native `Select-String` for the line lookup. The command-awareness helper was blocked by policy; that doesn’t affect the code review.

### Assistant | 2026-05-04T15:00:31.350Z

The no-profile shell wrapper is blocked in this environment, so I’m relying on the snippet plus manual line counting where the file lookup can’t run cleanly.

### Assistant | 2026-05-04T15:00:31.350Z

The no-profile shell wrapper is blocked in this environment, so I’m relying on the snippet plus manual line counting where the file lookup can’t run cleanly.

### Assistant | 2026-05-04T15:02:12.317Z

1. **CRITICAL** `ConsensusCoordinatorEngine.ts:122-141, 241-254`  
   **BUG:** In-flight limit is check-then-write with no interprocess lock. Six terminals can all read `[]`, pass the cap, then overwrite each other’s entries. `removeInflight` can also erase peer updates from a stale read.  
   **FIX:** Use a real atomic coordinator: SQLite transaction, OS lock file with exclusive create + retry, or append-only claims with atomic CAS. Check+insert+remove must be one locked transaction.

2. **HIGH** `ConsensusCoordinatorEngine.ts:111-118, 270-275`  
   **BUG:** Budget is also check-then-write. Terminals can all see budget available, fan out, then clobber increments. Charging happens after the expensive call, so overspend is guaranteed under burst.  
   **FIX:** Reserve estimated tokens under lock before fan-out, fail closed if reservation cannot be persisted, then reconcile/refund after actual usage.

3. **HIGH** `ConsensusCoordinatorEngine.ts:224-238, 258-267, 246, 275`  
   **BUG:** Corrupt/partial JSON or full-disk errors fail open. `writeFile` truncates before writing; a concurrent reader can parse partial JSON and `loadInflight` returns `[]`, `loadBudget` returns `0`. Write errors are swallowed.  
   **FIX:** Write temp file + fsync + atomic rename, validate checksums/version, and treat corrupt budget/inflight state as infrastructure failure or conservative rate-limit, not empty state.

4. **MEDIUM** `ConsensusCoordinatorEngine.ts:203-220`  
   **BUG:** Cache is unbounded JSONL and `loadCache` reads/splits/scans the whole file on every request. Long-running multi-terminal use becomes O(file size) memory and CPU per prompt.  
   **FIX:** Use SQLite/LevelDB keyed by hash with TTL index, or at least rotate/compact JSONL and maintain a bounded per-hash index.

5. **MEDIUM** `MultiModelConsensusEngine.ts:251-261`  
   **BUG:** Ollama timeout only races a timer; it does not cancel `ollamaClientEngine.generate`. On timeout the daemon work continues in the background, an
... [+161 chars truncated]

### Assistant | 2026-05-04T15:02:12.321Z

1. **CRITICAL** `ConsensusCoordinatorEngine.ts:122-141, 241-254`  
   **BUG:** In-flight limit is check-then-write with no interprocess lock. Six terminals can all read `[]`, pass the cap, then overwrite each other’s entries. `removeInflight` can also erase peer updates from a stale read.  
   **FIX:** Use a real atomic coordinator: SQLite transaction, OS lock file with exclusive create + retry, or append-only claims with atomic CAS. Check+insert+remove must be one locked transaction.

2. **HIGH** `ConsensusCoordinatorEngine.ts:111-118, 270-275`  
   **BUG:** Budget is also check-then-write. Terminals can all see budget available, fan out, then clobber increments. Charging happens after the expensive call, so overspend is guaranteed under burst.  
   **FIX:** Reserve estimated tokens under lock before fan-out, fail closed if reservation cannot be persisted, then reconcile/refund after actual usage.

3. **HIGH** `ConsensusCoordinatorEngine.ts:224-238, 258-267, 246, 275`  
   **BUG:** Corrupt/partial JSON or full-disk errors fail open. `writeFile` truncates before writing; a concurrent reader can parse partial JSON and `loadInflight` returns `[]`, `loadBudget` returns `0`. Write errors are swallowed.  
   **FIX:** Write temp file + fsync + atomic rename, validate checksums/version, and treat corrupt budget/inflight state as infrastructure failure or conservative rate-limit, not empty state.

4. **MEDIUM** `ConsensusCoordinatorEngine.ts:203-220`  
   **BUG:** Cache is unbounded JSONL and `loadCache` reads/splits/scans the whole file on every request. Long-running multi-terminal use becomes O(file size) memory and CPU per prompt.  
   **FIX:** Use SQLite/LevelDB keyed by hash with TTL index, or at least rotate/compact JSONL and maintain a bounded per-hash index.

5. **MEDIUM** `MultiModelConsensusEngine.ts:251-261`  
   **BUG:** Ollama timeout only races a timer; it does not cancel `ollamaClientEngine.generate`. On timeout the daemon work continues in the background, an
... [+161 chars truncated]
