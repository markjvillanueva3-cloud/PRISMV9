---
type: "chat-session"
source: "codex"
session_id: "019dfa6a-c90f-72a0-a083-3a205160a96c"
title: "# AGENTS.md instructions for H:\\PRISM # Codex Global PRISM Rules ## Spawned Agen"
date: "2026-05-05"
first_ts: "2026-05-05T23:13:24.429Z"
last_ts: "2026-05-05T23:13:33.078Z"
cwd: "H:\\PRISM"
messages: 5
user_msgs: 3
assistant_msgs: 2
raw_file: "C:/Users/wompu/.codex/sessions/2026/05/05/rollout-2026-05-05T18-13-15-019dfa6a-c90f-72a0-a083-3a205160a96c.jsonl"
tags:
  - "chat-archive"
  - "codex"
ingested: "2026-06-25T20:43:33"
---

# # AGENTS.md instructions for H:\PRISM # Codex Global PRISM Rules ## Spawned Agen

> **codex** | 2026-05-05 | 5 msgs (3 user / 2 assistant) | cwd: H:\PRISM
> Raw: `C:/Users/wompu/.codex/sessions/2026/05/05/rollout-2026-05-05T18-13-15-019dfa6a-c90f-72a0-a083-3a205160a96c.jsonl`

## Transcript

### User | 2026-05-05T23:13:24.430Z

# AGENTS.md instructions for H:\PRISM

<INSTRUCTIONS>
# Codex Global PRISM Rules

## Spawned Agents In PRISM

When working in `H:\PRISM` and calling `spawn_agent`, treat spawned-agent awareness as mandatory.

Before every `spawn_agent` call in PRISM:

1. use the `prism-spawn-awareness` skill
2. run:

```powershell
node C:\Users\Mark Villanueva\.codex\skills\prism-spawn-awareness\scripts\emit_spawn_context.mjs --type "<agent-type>"
```

3. prepend the returned `additionalContext` to the spawned agent prompt
4. prefer `fork_context: true` unless there is a specific reason not to

This keeps Codex spawned agents aligned with the same shared bundle Claude subagents receive through hooks:

- current position and handoff
- SVI / Psi / coverage alerts
- shared coordination and workboard state
- roadmap collaboration gate
- MCP-development preference rules
- search/index-first guidance
- command-bridge awareness

Canonical shared directive:

- `H:/PRISM/state/shared/CLAUDE-CODEX-SPAWNED-AGENT-DIRECTIVE.md`

## `/rgs-sync` In PRISM

When the user types `/rgs-sync` while working in `H:\PRISM`, mirror the shared roadmap-sync protocol instead of treating it as plain text.

1. Read:
   - `H:/PRISM/state/shared/CLAUDE-CODEX-RGS-SYNC-PROTOCOL.md`
   - `H:/PRISM/state/shared/CLAUDE-CODEX-ROADMAP-EXECUTION-DIRECTIVE.md`
   - `H:/PRISM/state/shared/ROADMAP_COLLABORATION_STATE.md`
2. Run the canonical helper:

```powershell
node H:\PRISM\.claude\helpers\roadmap-sync.mjs status
```

3. If the user supplies a structured sync payload, mirror it with:

```powershell
node H:\PRISM\.claude\helpers\roadmap-sync.mjs sync --lane "<lane>" --status "<status>" --current "<current>" --next "<next>" --done "<done>" --blockers "<blocker 1 || blocker 2>" --needs "<need 1 || need 2>" --convergence-target "<target>"
```

4. Summarize the result in natural language, including:
   - current collaboration mode
   - whether the current backend/frontend finish-first gate is active
   - whether a new large r
... [+33221 chars truncated]

### User | 2026-05-05T23:13:24.431Z

You are a strict code reviewer for the PRISM manufacturing-intelligence platform. You will be given a unified diff of recent changes and asked for a code review verdict.

Acceptance criteria (per PRISM CLAUDE.md):
  1. No stub engines, TODOs, or placeholder returns
  2. Tests use concrete assertions (no toBeDefined() blanket stubs, no synthetic threshold loops)
  3. ≥3 failure modes covered for any new engine
  4. Physics constants imported from src/physics/constants.ts (never inlined)
  5. New engines wired to every dispatcher that would naturally consume them
  6. New tests use vitest, exact values via toBe()/toEqual()/toBeCloseTo()
  7. No floating promises, no any-spread anti-patterns introduced

Respond with EXACTLY this format on the first line:
  VERDICT: PASS    (if all criteria met)
  VERDICT: FAIL    (if ANY criterion violated)

After the verdict line, list specific blockers (1 per line, prefix BLOCKER:).
Then optionally add up to 5 lines of additional notes.

Be strict. If unsure between PASS and FAIL, choose FAIL.

--- DIFF (target: commit HEAD) ---
commit 276d4e0ba1017c719b5a003ce5f0e11857c7ab9f
Author: markjvillanueva3-cloud <markjvillanueva3@gmail.com>
Date:   Tue May 5 18:12:54 2026 -0500

    [MAIN] [INFRA-CLEANUP]/U-SWEEPER-EXPAND-FIX1: address 3-way scrutiny blockers
    
    Codex + Opus flagged 4 issues against 0117ecb3f. Fixed in this commit:
    
    1. WMI CreationDate timezone bug (HIGH, codex+opus):
       Old: Date.UTC(+m[1]...) treated WMI's local-time as UTC, mis-aging
       procs by ±tz_offset minutes (5h on CDT). Fresh hooks would compute
       as 5h+ old and exceed any threshold.
       Fix: new Date(y, m-1, d, h, min, s).getTime() interprets in host TZ
       matching what WMI emits.
    
    2. sweepClaudeTaskOutputs deleted *.json (codex BLOCKER):
       Old regex /\.(output|json)$/ would have removed task control JSON
       that the harness owns. Tightened to /\.output$/ — stdout only.
    
    3. Race in sweepChatBusClaims (op
... [+7083 chars truncated]

### User | 2026-05-05T23:13:24.431Z

You are a strict code reviewer for the PRISM manufacturing-intelligence platform. You will be given a unified diff of recent changes and asked for a code review verdict.

Acceptance criteria (per PRISM CLAUDE.md):
  1. No stub engines, TODOs, or placeholder returns
  2. Tests use concrete assertions (no toBeDefined() blanket stubs, no synthetic threshold loops)
  3. ≥3 failure modes covered for any new engine
  4. Physics constants imported from src/physics/constants.ts (never inlined)
  5. New engines wired to every dispatcher that would naturally consume them
  6. New tests use vitest, exact values via toBe()/toEqual()/toBeCloseTo()
  7. No floating promises, no any-spread anti-patterns introduced

Respond with EXACTLY this format on the first line:
  VERDICT: PASS    (if all criteria met)
  VERDICT: FAIL    (if ANY criterion violated)

After the verdict line, list specific blockers (1 per line, prefix BLOCKER:).
Then optionally add up to 5 lines of additional notes.

Be strict. If unsure between PASS and FAIL, choose FAIL.

--- DIFF (target: commit HEAD) ---
commit 276d4e0ba1017c719b5a003ce5f0e11857c7ab9f
Author: markjvillanueva3-cloud <markjvillanueva3@gmail.com>
Date:   Tue May 5 18:12:54 2026 -0500

    [MAIN] [INFRA-CLEANUP]/U-SWEEPER-EXPAND-FIX1: address 3-way scrutiny blockers
    
    Codex + Opus flagged 4 issues against 0117ecb3f. Fixed in this commit:
    
    1. WMI CreationDate timezone bug (HIGH, codex+opus):
       Old: Date.UTC(+m[1]...) treated WMI's local-time as UTC, mis-aging
       procs by ±tz_offset minutes (5h on CDT). Fresh hooks would compute
       as 5h+ old and exceed any threshold.
       Fix: new Date(y, m-1, d, h, min, s).getTime() interprets in host TZ
       matching what WMI emits.
    
    2. sweepClaudeTaskOutputs deleted *.json (codex BLOCKER):
       Old regex /\.(output|json)$/ would have removed task control JSON
       that the harness owns. Tightened to /\.output$/ — stdout only.
    
    3. Race in sweepChatBusClaims (op
... [+7083 chars truncated]

### Assistant | 2026-05-05T23:13:33.065Z

VERDICT: FAIL
BLOCKER: No tests were added or updated for the changed sweep/WMIC/race logic, so there are no concrete `vitest` assertions (`toBe`/`toEqual`/`toBeCloseTo`) proving these fixes.
BLOCKER: The commit explicitly leaves the prior test-coverage blocker unresolved, which fails the review bar for verified behavioral fixes in this codepath.

Note: Criteria 1, 4, 5, and 7 are not newly violated in this diff.
Note: No new engine was introduced, so engine-specific wiring/failure-mode criteria are not directly applicable here.


### Assistant | 2026-05-05T23:13:33.065Z

VERDICT: FAIL
BLOCKER: No tests were added or updated for the changed sweep/WMIC/race logic, so there are no concrete `vitest` assertions (`toBe`/`toEqual`/`toBeCloseTo`) proving these fixes.
BLOCKER: The commit explicitly leaves the prior test-coverage blocker unresolved, which fails the review bar for verified behavioral fixes in this codepath.

Note: Criteria 1, 4, 5, and 7 are not newly violated in this diff.
Note: No new engine was introduced, so engine-specific wiring/failure-mode criteria are not directly applicable here.

