---
type: "chat-session"
source: "codex"
session_id: "019dffe3-c451-7c52-b926-31e381b81071"
title: "# AGENTS.md instructions for H:\\prism-ppgh05 <!-- AUTO-MIRRORED FROM C:/Users/Ma"
date: "2026-05-07"
first_ts: "2026-05-07T00:43:34.336Z"
last_ts: "2026-05-07T00:43:45.096Z"
cwd: "H:\\prism-ppgh05"
messages: 5
user_msgs: 3
assistant_msgs: 2
raw_file: "C:/Users/wompu/.codex/sessions/2026/05/06/rollout-2026-05-06T19-43-30-019dffe3-c451-7c52-b926-31e381b81071.jsonl"
tags:
  - "chat-archive"
  - "codex"
ingested: "2026-06-25T20:43:34"
---

# # AGENTS.md instructions for H:\prism-ppgh05 <!-- AUTO-MIRRORED FROM C:/Users/Ma

> **codex** | 2026-05-07 | 5 msgs (3 user / 2 assistant) | cwd: H:\prism-ppgh05
> Raw: `C:/Users/wompu/.codex/sessions/2026/05/06/rollout-2026-05-06T19-43-30-019dffe3-c451-7c52-b926-31e381b81071.jsonl`

## Transcript

### User | 2026-05-07T00:43:34.338Z

# AGENTS.md instructions for H:\prism-ppgh05

<INSTRUCTIONS>
<!-- AUTO-MIRRORED FROM C:/Users/Mark Villanueva/.claude/CLAUDE.md by .claude/helpers/sync-cli-context-files.mjs -->
<!-- Mirror sync: 2026-05-04T19:20:36.750Z · Target: Codex CLI (global) · DO NOT EDIT — edits will be lost on next sync -->
<!-- To update: edit C:/Users/Mark Villanueva/.claude/CLAUDE.md, then run `node H:/PRISM/.claude/helpers/sync-cli-context-files.mjs` -->

# PRISM Global CLAUDE.md — Operational Playbook v2.5

> **Edit `C:\Users\wompu\.claude\settings.json` ONLY** — `c-to-h-mirror` hook auto-replicates C: → H: on every save. Editing `H:\.claude\settings.json` directly gets clobbered on the next mirror fire. (Junction `~/.claude` → `H:\.claude` is NOT active; both dirs are real and the mirror hook keeps them aligned.) Reference tables auto-injected by `reference-inject.mjs` hook when relevant keywords detected.

---

## EXPERT ROLE (ALWAYS ACTIVE)
PhDs across every math/science/eng/CS field. Deep thinker: exhaust obvious + non-obvious paths, edge cases, failure modes, second-order effects. Never "good enough" — optimal with justification.

---

## TOKEN ECONOMY
- **RTK prefix on bash** — `rtk vitest run` (99%), `rtk git/gh/npm/tsc/docker` (60-90% savings). Use in `&&` chains. Skip only if output <500 chars. `/rtk-setup` to install.
- **Ollama offload** — code explain/summarize/docstring/classify/lint/diff-summary/error-triage routed to local qwen2.5-coder:7b via `/ollama-*` skills (9 of them) and `OllamaHookBridgeEngine`. Reserve Claude for deep reasoning + safety. See `feedback_ollama_token_routing.md`.
- **Tool selection** — Glob/Grep over Bash find/grep · `Read offset=X limit=Y` for partial · Parallel independent tool calls in one message · Don't re-read after Edit/Write (hooks track).
- **Context extension** — per-agent `HANDOFF-<id>-<topic>.md` (6 chats), `MEMORY.md` index (<200 lines), digests over exploration, load-on-demand skills, keyword-gated hook injections.

---

## KARPATHY 
... [+12174 chars truncated]

### User | 2026-05-07T00:43:34.338Z

You are a strict code reviewer for the PRISM manufacturing-intelligence platform. You will be given a unified diff of recent changes and asked for a code review verdict.

Acceptance criteria (per PRISM CLAUDE.md):
  1. No stub engines, TODOs, or placeholder returns
  2. Tests use concrete assertions (no toBeDefined() blanket stubs, no synthetic threshold loops)
  3. ≥3 failure modes covered for any new engine
  4. Physics constants imported from src/physics/constants.ts (never inlined)
  5. New engines wired to every dispatcher that would naturally consume them
  6. New tests use vitest, exact values via toBe()/toEqual()/toBeCloseTo()
  7. No floating promises, no any-spread anti-patterns introduced

Respond with EXACTLY this format on the first line:
  VERDICT: PASS    (if all criteria met)
  VERDICT: FAIL    (if ANY criterion violated)

After the verdict line, list specific blockers (1 per line, prefix BLOCKER:).
Then optionally add up to 5 lines of additional notes.

Be strict. If unsure between PASS and FAIL, choose FAIL.

--- DIFF (target: uncommitted changes) ---
diff --git a/mcp-server/src/__tests__/OkumaMultusB250IIMillTurnMasterPostEngine.ToolBreakCrossCheck.test.ts b/mcp-server/src/__tests__/OkumaMultusB250IIMillTurnMasterPostEngine.ToolBreakCrossCheck.test.ts
new file mode 100644
index 000000000..a4c879ee7
--- /dev/null
+++ b/mcp-server/src/__tests__/OkumaMultusB250IIMillTurnMasterPostEngine.ToolBreakCrossCheck.test.ts
@@ -0,0 +1,470 @@
+/**
+ * Tool-break-detect setup cross-check tests for the Multus facade (U-PPGMU14).
+ *
+ * The v5.2.7 .cps's writePRISMToolBreakSetup function (line 2278):
+ *
+ *   function writePRISMToolBreakSetup(toolNumber, toolLength, tolerance) {
+ *     if (!getProperty("usePRISMToolBreakDetect")) return;
+ *     if (!tolerance) tolerance = 0.01; // Default 0.01" or 0.25mm
+ *     writeln("VTBRK[" + toolNumber + "]=" + toolLength.toFixed(4));
+ *     writeln("VTBRT[" + toolNumber + "]=" + tolerance.toFixed(4));
+ *     writeComm
... [+46913 chars truncated]

### User | 2026-05-07T00:43:34.339Z

You are a strict code reviewer for the PRISM manufacturing-intelligence platform. You will be given a unified diff of recent changes and asked for a code review verdict.

Acceptance criteria (per PRISM CLAUDE.md):
  1. No stub engines, TODOs, or placeholder returns
  2. Tests use concrete assertions (no toBeDefined() blanket stubs, no synthetic threshold loops)
  3. ≥3 failure modes covered for any new engine
  4. Physics constants imported from src/physics/constants.ts (never inlined)
  5. New engines wired to every dispatcher that would naturally consume them
  6. New tests use vitest, exact values via toBe()/toEqual()/toBeCloseTo()
  7. No floating promises, no any-spread anti-patterns introduced

Respond with EXACTLY this format on the first line:
  VERDICT: PASS    (if all criteria met)
  VERDICT: FAIL    (if ANY criterion violated)

After the verdict line, list specific blockers (1 per line, prefix BLOCKER:).
Then optionally add up to 5 lines of additional notes.

Be strict. If unsure between PASS and FAIL, choose FAIL.

--- DIFF (target: uncommitted changes) ---
diff --git a/mcp-server/src/__tests__/OkumaMultusB250IIMillTurnMasterPostEngine.ToolBreakCrossCheck.test.ts b/mcp-server/src/__tests__/OkumaMultusB250IIMillTurnMasterPostEngine.ToolBreakCrossCheck.test.ts
new file mode 100644
index 000000000..a4c879ee7
--- /dev/null
+++ b/mcp-server/src/__tests__/OkumaMultusB250IIMillTurnMasterPostEngine.ToolBreakCrossCheck.test.ts
@@ -0,0 +1,470 @@
+/**
+ * Tool-break-detect setup cross-check tests for the Multus facade (U-PPGMU14).
+ *
+ * The v5.2.7 .cps's writePRISMToolBreakSetup function (line 2278):
+ *
+ *   function writePRISMToolBreakSetup(toolNumber, toolLength, tolerance) {
+ *     if (!getProperty("usePRISMToolBreakDetect")) return;
+ *     if (!tolerance) tolerance = 0.01; // Default 0.01" or 0.25mm
+ *     writeln("VTBRK[" + toolNumber + "]=" + toolLength.toFixed(4));
+ *     writeln("VTBRT[" + toolNumber + "]=" + tolerance.toFixed(4));
+ *     writeComm
... [+46913 chars truncated]

### Assistant | 2026-05-07T00:43:45.067Z

VERDICT: FAIL
BLOCKER: Acceptance criterion 6 is violated: new tests use non-exact matchers (`toHaveLength`, `toContain`, and `some(...).toBe(true)`) instead of consistently using exact-value assertions via `toBe()` / `toEqual()` / `toBeCloseTo()`.
BLOCKER: `camActionSchemas.master_post_okuma_multus_b250_tool_break` does not enforce the engine’s required bounds (`tool_number > 0`, `tool_length > 0`, `tolerance >= 0`), so invalid inputs can pass schema validation and fail later at engine runtime rather than at dispatcher boundary.

Notes:
The new action is wired in `ACTIONS` and the dispatcher switch, and no stub/TODO/placeholder returns were introduced.
Failure-mode coverage in tests is strong (well over 3 cases), and no floating promises or `any`-spread patterns were introduced.

### Assistant | 2026-05-07T00:43:45.068Z

VERDICT: FAIL
BLOCKER: Acceptance criterion 6 is violated: new tests use non-exact matchers (`toHaveLength`, `toContain`, and `some(...).toBe(true)`) instead of consistently using exact-value assertions via `toBe()` / `toEqual()` / `toBeCloseTo()`.
BLOCKER: `camActionSchemas.master_post_okuma_multus_b250_tool_break` does not enforce the engine’s required bounds (`tool_number > 0`, `tool_length > 0`, `tolerance >= 0`), so invalid inputs can pass schema validation and fail later at engine runtime rather than at dispatcher boundary.

Notes:
The new action is wired in `ACTIONS` and the dispatcher switch, and no stub/TODO/placeholder returns were introduced.
Failure-mode coverage in tests is strong (well over 3 cases), and no floating promises or `any`-spread patterns were introduced.
