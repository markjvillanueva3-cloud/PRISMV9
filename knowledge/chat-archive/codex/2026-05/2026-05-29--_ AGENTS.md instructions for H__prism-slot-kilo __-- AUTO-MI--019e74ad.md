---
type: "chat-session"
source: "codex"
session_id: "019e74ad-ea00-7803-81fb-2436b3e32b80"
title: "# AGENTS.md instructions for H:\\prism-slot-kilo <!-- AUTO-MIRRORED FROM C:/Users"
date: "2026-05-29"
first_ts: "2026-05-29T17:00:45.823Z"
last_ts: "2026-05-29T17:02:31.632Z"
cwd: "H:\\prism-slot-kilo"
messages: 13
user_msgs: 3
assistant_msgs: 10
raw_file: "C:/Users/wompu/.codex/sessions/2026/05/29/rollout-2026-05-29T12-00-15-019e74ad-ea00-7803-81fb-2436b3e32b80.jsonl"
tags:
  - "chat-archive"
  - "codex"
ingested: "2026-06-25T20:43:35"
---

# # AGENTS.md instructions for H:\prism-slot-kilo <!-- AUTO-MIRRORED FROM C:/Users

> **codex** | 2026-05-29 | 13 msgs (3 user / 10 assistant) | cwd: H:\prism-slot-kilo
> Raw: `C:/Users/wompu/.codex/sessions/2026/05/29/rollout-2026-05-29T12-00-15-019e74ad-ea00-7803-81fb-2436b3e32b80.jsonl`

## Transcript

### User | 2026-05-29T17:00:45.824Z

# AGENTS.md instructions for H:\prism-slot-kilo

<INSTRUCTIONS>
<!-- AUTO-MIRRORED FROM C:/Users/Mark Villanueva/.claude/CLAUDE.md by .claude/helpers/sync-cli-context-files.mjs -->
<!-- Mirror sync: 2026-05-12T16:44:31.599Z · Target: Codex CLI (global) · DO NOT EDIT — edits will be lost on next sync -->
<!-- To update: edit C:/Users/Mark Villanueva/.claude/CLAUDE.md, then run `node H:/PRISM/.claude/helpers/sync-cli-context-files.mjs` -->

# PRISM Global CLAUDE.md — Operational Playbook v2.5

> **Edit `C:\Users\wompu\.claude\settings.json` ONLY** — `c-to-h-mirror` hook auto-replicates C: → H: on every save. Editing `H:\.claude\settings.json` directly gets clobbered on the next mirror fire. (Junction `~/.claude` → `H:\.claude` is NOT active; both dirs are real and the mirror hook keeps them aligned.) Reference tables auto-injected by `reference-inject.mjs` hook when relevant keywords detected.

---

## EXPERT ROLE (ALWAYS ACTIVE)
PhDs across every math/science/eng/CS field. Deep thinker: exhaust obvious + non-obvious paths, edge cases, failure modes, second-order effects. Never "good enough" — optimal with justification.

---

## TOKEN ECONOMY
- **RTK prefix on bash** — `rtk vitest run` (99%), `rtk git/gh/npm/tsc/docker` (60-90% savings). Use in `&&` chains. Skip only if output <500 chars. `/rtk-setup` to install.
- **Ollama offload** — code explain/summarize/docstring/classify/lint/diff-summary/error-triage routed to local qwen2.5-coder:7b via `/ollama-*` skills (9 of them) and `OllamaHookBridgeEngine`. Reserve Claude for deep reasoning + safety. See `feedback_ollama_token_routing.md`.
- **Tool selection** — Glob/Grep over Bash find/grep · `Read offset=X limit=Y` for partial · Parallel independent tool calls in one message · Don't re-read after Edit/Write (hooks track).
- **Context extension** — per-agent `HANDOFF-<id>-<topic>.md` (6 chats), `MEMORY.md` index (<200 lines), digests over exploration, load-on-demand skills, keyword-gated hook injections.

---

## KARPAT
... [+37821 chars truncated]

### User | 2026-05-29T17:00:45.824Z

Read-only, BE FAST (avoid broad scans — run only the targeted commands below, then judge). CWD = slot worktree. Audit CAM-galaxy completeness. Do EXACTLY this, then report:

1. `ls mcp-server/src/engines/cam/` — confirm CLAUDE.md MEMORY.md PATHS.md TOOLBELT.md GSD.md all present.
2. `sed -n '1,5p' mcp-server/src/engines/cam/CLAUDE.md` — does line 1 still say "HONEST STUB"? (header/body contradiction = P1)
3. `node scripts/cam-galaxy-verify.mjs --json` — report worst + any WARN/FAIL.
4. `grep -nE "cam_p2p_orchestrate|cam_kienzle_force|cam_taylor_tool_life|cam_feedrate_chipload|cam_tool_deflection|cam_coolant_strategy|cam_omega_score|cam_p2p_explain" mcp-server/src/tools/dispatchers/camDispatcher.ts | wc -l` — expect each action twice (enum + switch); report count.
5. `grep -cE "blueprint-vision|charlie|hotel|juliett|xray" mcp-server/src/engines/cam/MEMORY.md` and same for cam/CLAUDE.md — confirm the new PSN edges are in BOTH (symmetry).
6. `grep -rEl "kc1\.1\s*=|kc11_mpa\s*=\s*[0-9]" mcp-server/src/engines/CAM*.ts | head` — any inlined Kienzle constants (should be ZERO; must import from src/physics/constants.ts).

Output: OVERALL VERDICT (COMPLETE/MOSTLY_COMPLETE/GAPS/INCOMPLETE) + P0/P1/P2 gap lists + 3-5 recommendations. Concise. Do not write files.

### User | 2026-05-29T17:00:45.824Z

Read-only, BE FAST (avoid broad scans — run only the targeted commands below, then judge). CWD = slot worktree. Audit CAM-galaxy completeness. Do EXACTLY this, then report:

1. `ls mcp-server/src/engines/cam/` — confirm CLAUDE.md MEMORY.md PATHS.md TOOLBELT.md GSD.md all present.
2. `sed -n '1,5p' mcp-server/src/engines/cam/CLAUDE.md` — does line 1 still say "HONEST STUB"? (header/body contradiction = P1)
3. `node scripts/cam-galaxy-verify.mjs --json` — report worst + any WARN/FAIL.
4. `grep -nE "cam_p2p_orchestrate|cam_kienzle_force|cam_taylor_tool_life|cam_feedrate_chipload|cam_tool_deflection|cam_coolant_strategy|cam_omega_score|cam_p2p_explain" mcp-server/src/tools/dispatchers/camDispatcher.ts | wc -l` — expect each action twice (enum + switch); report count.
5. `grep -cE "blueprint-vision|charlie|hotel|juliett|xray" mcp-server/src/engines/cam/MEMORY.md` and same for cam/CLAUDE.md — confirm the new PSN edges are in BOTH (symmetry).
6. `grep -rEl "kc1\.1\s*=|kc11_mpa\s*=\s*[0-9]" mcp-server/src/engines/CAM*.ts | head` — any inlined Kienzle constants (should be ZERO; must import from src/physics/constants.ts).

Output: OVERALL VERDICT (COMPLETE/MOSTLY_COMPLETE/GAPS/INCOMPLETE) + P0/P1/P2 gap lists + 3-5 recommendations. Concise. Do not write files.

### Assistant | 2026-05-29T17:01:05.548Z

I’ll run only the six requested read-only checks from the slot worktree and report the gaps by severity.

### Assistant | 2026-05-29T17:01:05.549Z

I’ll run only the six requested read-only checks from the slot worktree and report the gaps by severity.

### Assistant | 2026-05-29T17:01:14.614Z

The first parallel attempt was blocked by the Windows sandbox before producing usable output. I’m retrying the same requested checks individually with shell profile loading disabled, still read-only.

### Assistant | 2026-05-29T17:01:14.614Z

The first parallel attempt was blocked by the Windows sandbox before producing usable output. I’m retrying the same requested checks individually with shell profile loading disabled, still read-only.

### Assistant | 2026-05-29T17:01:33.001Z

The standard shell path is failing at sandbox setup, not at the command level. I’m checking whether the local Node-backed tool is available so I can perform the same six targeted read-only checks without widening scope.

### Assistant | 2026-05-29T17:01:33.001Z

The standard shell path is failing at sandbox setup, not at the command level. I’m checking whether the local Node-backed tool is available so I can perform the same six targeted read-only checks without widening scope.

### Assistant | 2026-05-29T17:01:54.908Z

Launching subprocesses from the Node tool hits the same sandbox failure. I’m falling back to direct filesystem reads for the same exact paths and regexes, with no broad discovery beyond the requested glob check.

### Assistant | 2026-05-29T17:01:54.908Z

Launching subprocesses from the Node tool hits the same sandbox failure. I’m falling back to direct filesystem reads for the same exact paths and regexes, with no broad discovery beyond the requested glob check.

### Assistant | 2026-05-29T17:02:31.629Z

**OVERALL VERDICT: INCOMPLETE AUDIT**

I could not produce a CAM-galaxy completeness verdict because every read path was blocked by the Windows sandbox before command output was available. Both `shell_command` and the Node-backed fallback failed with:

`windows sandbox failed: CryptUnprotectData failed: 2148073483`

**P0**
- Audit blocked: none of the requested command outputs could be trusted or completed.
- `node scripts/cam-galaxy-verify.mjs --json` could not run, so worst/WARN/FAIL status is unknown.

**P1**
- Could not verify whether `mcp-server/src/engines/cam/CLAUDE.md` still says `HONEST STUB` on line 1.
- Could not verify dispatcher action count for the 8 CAM actions.
- Could not verify Kienzle constant inlining.

**P2**
- Could not confirm CAM doc presence or PSN edge symmetry in `MEMORY.md` and `CLAUDE.md`.

**Recommendations**
1. Run the six commands directly in `H:\prism-slot-kilo` outside this blocked sandbox.
2. Treat this as no-pass/no-fail, not evidence of completeness.
3. If rerunning through Codex is needed, fix the Windows sandbox `CryptUnprotectData` issue first.
4. Prioritize the `CLAUDE.md` header check and `cam-galaxy-verify.mjs --json`; those determine the likely P1/P0 surface fastest.

### Assistant | 2026-05-29T17:02:31.629Z

**OVERALL VERDICT: INCOMPLETE AUDIT**

I could not produce a CAM-galaxy completeness verdict because every read path was blocked by the Windows sandbox before command output was available. Both `shell_command` and the Node-backed fallback failed with:

`windows sandbox failed: CryptUnprotectData failed: 2148073483`

**P0**
- Audit blocked: none of the requested command outputs could be trusted or completed.
- `node scripts/cam-galaxy-verify.mjs --json` could not run, so worst/WARN/FAIL status is unknown.

**P1**
- Could not verify whether `mcp-server/src/engines/cam/CLAUDE.md` still says `HONEST STUB` on line 1.
- Could not verify dispatcher action count for the 8 CAM actions.
- Could not verify Kienzle constant inlining.

**P2**
- Could not confirm CAM doc presence or PSN edge symmetry in `MEMORY.md` and `CLAUDE.md`.

**Recommendations**
1. Run the six commands directly in `H:\prism-slot-kilo` outside this blocked sandbox.
2. Treat this as no-pass/no-fail, not evidence of completeness.
3. If rerunning through Codex is needed, fix the Windows sandbox `CryptUnprotectData` issue first.
4. Prioritize the `CLAUDE.md` header check and `cam-galaxy-verify.mjs --json`; those determine the likely P1/P0 surface fastest.
