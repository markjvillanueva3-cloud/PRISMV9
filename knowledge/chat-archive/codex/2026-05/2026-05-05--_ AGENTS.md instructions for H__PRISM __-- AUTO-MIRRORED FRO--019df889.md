---
type: "chat-session"
source: "codex"
session_id: "019df889-b095-7052-b20a-a8f41ec4f90c"
title: "# AGENTS.md instructions for H:\\PRISM <!-- AUTO-MIRRORED FROM C:/Users/Mark Vill"
date: "2026-05-05"
first_ts: "2026-05-05T14:28:53.543Z"
last_ts: "2026-05-05T14:36:23.638Z"
cwd: "H:\\PRISM"
messages: 5
user_msgs: 3
assistant_msgs: 2
raw_file: "C:/Users/wompu/.codex/sessions/2026/05/05/rollout-2026-05-05T09-27-46-019df889-b095-7052-b20a-a8f41ec4f90c.jsonl"
tags:
  - "chat-archive"
  - "codex"
ingested: "2026-06-25T20:43:33"
---

# # AGENTS.md instructions for H:\PRISM <!-- AUTO-MIRRORED FROM C:/Users/Mark Vill

> **codex** | 2026-05-05 | 5 msgs (3 user / 2 assistant) | cwd: H:\PRISM
> Raw: `C:/Users/wompu/.codex/sessions/2026/05/05/rollout-2026-05-05T09-27-46-019df889-b095-7052-b20a-a8f41ec4f90c.jsonl`

## Transcript

### User | 2026-05-05T14:28:53.545Z

# AGENTS.md instructions for H:\PRISM

<INSTRUCTIONS>
<!-- AUTO-MIRRORED FROM C:/Users/Mark Villanueva/.claude/CLAUDE.md by .claude/helpers/sync-cli-context-files.mjs -->
<!-- Mirror sync: 2026-05-04T19:20:36.750Z · Target: Codex CLI (global) · DO NOT EDIT — edits will be lost on next sync -->
<!-- To update: edit C:/Users/Mark Villanueva/.claude/CLAUDE.md, then run `node H:/PRISM/.claude/helpers/sync-cli-context-files.mjs` -->

# PRISM Global CLAUDE.md — Operational Playbook v2.5

> **Edit `C:\Users\wompu\.claude\settings.json` ONLY** — `c-to-h-mirror` hook auto-replicates C: → H: on every save. Editing `H:\.claude\settings.json` directly gets clobbered on the next mirror fire. (Junction `~/.claude` → `H:\.claude` is NOT active; both dirs are real and the mirror hook keeps them aligned.) Reference tables auto-injected by `reference-inject.mjs` hook when relevant keywords detected.

---

## EXPERT ROLE (ALWAYS ACTIVE)
PhDs across every math/science/eng/CS field. Deep thinker: exhaust obvious + non-obvious paths, edge cases, failure modes, second-order effects. Never "good enough" — optimal with justification.

---

## TOKEN ECONOMY
- **RTK prefix on bash** — `rtk vitest run` (99%), `rtk git/gh/npm/tsc/docker` (60-90% savings). Use in `&&` chains. Skip only if output <500 chars. `/rtk-setup` to install.
- **Ollama offload** — code explain/summarize/docstring/classify/lint/diff-summary/error-triage routed to local qwen2.5-coder:7b via `/ollama-*` skills (9 of them) and `OllamaHookBridgeEngine`. Reserve Claude for deep reasoning + safety. See `feedback_ollama_token_routing.md`.
- **Tool selection** — Glob/Grep over Bash find/grep · `Read offset=X limit=Y` for partial · Parallel independent tool calls in one message · Don't re-read after Edit/Write (hooks track).
- **Context extension** — per-agent `HANDOFF-<id>-<topic>.md` (6 chats), `MEMORY.md` index (<200 lines), digests over exploration, load-on-demand skills, keyword-gated hook injections.

---

## KARPATHY DISCIPL
... [+12160 chars truncated]

### User | 2026-05-05T14:28:53.546Z

You are an ACCEPTANCE-TEST PSEUDOCODE writer. Read `H:/prism/mcp-server/data/milestones/comprehensive-roadmap-2026-05-04-v2.json` (V3.2, 36 milestones).

Pick the THREE highest-value milestones from V3.2's critical path (currently MS0, MS3, MS9 are likely top — verify against the JSON's `criticalPath` field).

For each of the 3, write **executable acceptance-test pseudocode** in TypeScript-like syntax that a developer could turn into a real Vitest test. The test must:
- Be deterministic (no flaky network/timing)
- Cover happy path + 3 failure modes + 2 adversarial inputs
- Reference real engine names from the V3.2 roadmap acceptance text
- Include physics-constant imports where applicable
- Show ledger-write assertions where applicable

Output strictly JSON:
{
  "milestone_1": {
    "id": "...",
    "test_pseudocode": "// TypeScript-like pseudocode here\n..."
  },
  "milestone_2": { ... },
  "milestone_3": { ... }
}
No prose outside the JSON. Pseudocode strings are JSON-escaped.


### User | 2026-05-05T14:28:53.547Z

You are an ACCEPTANCE-TEST PSEUDOCODE writer. Read `H:/prism/mcp-server/data/milestones/comprehensive-roadmap-2026-05-04-v2.json` (V3.2, 36 milestones).

Pick the THREE highest-value milestones from V3.2's critical path (currently MS0, MS3, MS9 are likely top — verify against the JSON's `criticalPath` field).

For each of the 3, write **executable acceptance-test pseudocode** in TypeScript-like syntax that a developer could turn into a real Vitest test. The test must:
- Be deterministic (no flaky network/timing)
- Cover happy path + 3 failure modes + 2 adversarial inputs
- Reference real engine names from the V3.2 roadmap acceptance text
- Include physics-constant imports where applicable
- Show ledger-write assertions where applicable

Output strictly JSON:
{
  "milestone_1": {
    "id": "...",
    "test_pseudocode": "// TypeScript-like pseudocode here\n..."
  },
  "milestone_2": { ... },
  "milestone_3": { ... }
}
No prose outside the JSON. Pseudocode strings are JSON-escaped.


### Assistant | 2026-05-05T14:36:23.634Z

{
  "milestone_1": {
    "id": "INFRA-CONSENSUS-WIRE-MS0",
    "test_pseudocode": "import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';\nimport { SwarmExecutor } from '../../engines/SwarmExecutor.js';\nimport { CoordinationLedgerEngine } from '../../engines/CoordinationLedgerEngine.js';\nimport { createPrismAiDispatcherHarness, deterministicModelAdapters, denyNetwork } from '../helpers/acceptanceHarness.js';\n\ndescribe('INFRA-CONSENSUS-WIRE-MS0 acceptance', () => {\n  let ledger: CoordinationLedgerEngine;\n  let dispatcher: ReturnType<typeof createPrismAiDispatcherHarness>;\n\n  beforeEach(() => {\n    vi.useFakeTimers();\n    vi.setSystemTime(new Date('2026-05-04T00:00:00.000Z'));\n    ledger = new CoordinationLedgerEngine(1000);\n    dispatcher = createPrismAiDispatcherHarness({\n      actionName: 'prism_ai:consensus_decide',\n      consensusEngine: new SwarmExecutor(),\n      ledger,\n      network: denyNetwork(),\n      modelAdapters: deterministicModelAdapters({\n        Claude: { choice: 'OKUMA_GENOS_M560V', confidence: 0.94, provenance: ['fixture:ms0/claude-machine.json'] },\n        Codex: { choice: 'OKUMA_GENOS_M560V', confidence: 0.93, provenance: ['fixture:ms0/codex-machine.json'] },\n        Gemini: { choice: 'OKUMA_GENOS_M560V', confidence: 0.91, provenance: ['fixture:ms0/gemini-machine.json'] },\n        Ollama: { choice: 'HURCO_VM10i', confidence: 0.62, provenance: ['fixture:ms0/ollama-machine.json'] }\n      })\n    });\n  });\n\n  afterEach(() => vi.useRealTimers());\n\n  it('happy path: prism_ai:consensus_decide returns 3-of-4 vote with provenance and production dispatcher path calls it', async () => {\n    const request = { pick: 'machine', highStakes: true, partId: 'JM-MS0-001', allowedMachines: ['OKUMA_GENOS_M560V', 'HURCO_VM10i'] };\n    const result = await dispatcher.handle('consensus_decide', request);\n\n    expect(result.status).toBe('APPROVE');\n    expect(result.decision).toBe('OKUMA_GENOS_M560V');\n    expect(resul
... [+14842 chars truncated]

### Assistant | 2026-05-05T14:36:23.635Z

{
  "milestone_1": {
    "id": "INFRA-CONSENSUS-WIRE-MS0",
    "test_pseudocode": "import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';\nimport { SwarmExecutor } from '../../engines/SwarmExecutor.js';\nimport { CoordinationLedgerEngine } from '../../engines/CoordinationLedgerEngine.js';\nimport { createPrismAiDispatcherHarness, deterministicModelAdapters, denyNetwork } from '../helpers/acceptanceHarness.js';\n\ndescribe('INFRA-CONSENSUS-WIRE-MS0 acceptance', () => {\n  let ledger: CoordinationLedgerEngine;\n  let dispatcher: ReturnType<typeof createPrismAiDispatcherHarness>;\n\n  beforeEach(() => {\n    vi.useFakeTimers();\n    vi.setSystemTime(new Date('2026-05-04T00:00:00.000Z'));\n    ledger = new CoordinationLedgerEngine(1000);\n    dispatcher = createPrismAiDispatcherHarness({\n      actionName: 'prism_ai:consensus_decide',\n      consensusEngine: new SwarmExecutor(),\n      ledger,\n      network: denyNetwork(),\n      modelAdapters: deterministicModelAdapters({\n        Claude: { choice: 'OKUMA_GENOS_M560V', confidence: 0.94, provenance: ['fixture:ms0/claude-machine.json'] },\n        Codex: { choice: 'OKUMA_GENOS_M560V', confidence: 0.93, provenance: ['fixture:ms0/codex-machine.json'] },\n        Gemini: { choice: 'OKUMA_GENOS_M560V', confidence: 0.91, provenance: ['fixture:ms0/gemini-machine.json'] },\n        Ollama: { choice: 'HURCO_VM10i', confidence: 0.62, provenance: ['fixture:ms0/ollama-machine.json'] }\n      })\n    });\n  });\n\n  afterEach(() => vi.useRealTimers());\n\n  it('happy path: prism_ai:consensus_decide returns 3-of-4 vote with provenance and production dispatcher path calls it', async () => {\n    const request = { pick: 'machine', highStakes: true, partId: 'JM-MS0-001', allowedMachines: ['OKUMA_GENOS_M560V', 'HURCO_VM10i'] };\n    const result = await dispatcher.handle('consensus_decide', request);\n\n    expect(result.status).toBe('APPROVE');\n    expect(result.decision).toBe('OKUMA_GENOS_M560V');\n    expect(resul
... [+14842 chars truncated]
