---
type: "chat-session"
source: "codex"
session_id: "019e0034-cef7-7b61-a2ec-d89d558d46b4"
title: "# AGENTS.md instructions for H:\\PRISM <!-- AUTO-MIRRORED FROM C:/Users/Mark Vill"
date: "2026-05-07"
first_ts: "2026-05-07T02:12:32.092Z"
last_ts: "2026-05-07T02:13:04.128Z"
cwd: "H:\\PRISM"
messages: 11
user_msgs: 3
assistant_msgs: 8
raw_file: "C:/Users/wompu/.codex/sessions/2026/05/06/rollout-2026-05-06T21-12-01-019e0034-cef7-7b61-a2ec-d89d558d46b4.jsonl"
tags:
  - "chat-archive"
  - "codex"
ingested: "2026-06-25T20:43:34"
---

# # AGENTS.md instructions for H:\PRISM <!-- AUTO-MIRRORED FROM C:/Users/Mark Vill

> **codex** | 2026-05-07 | 11 msgs (3 user / 8 assistant) | cwd: H:\PRISM
> Raw: `C:/Users/wompu/.codex/sessions/2026/05/06/rollout-2026-05-06T21-12-01-019e0034-cef7-7b61-a2ec-d89d558d46b4.jsonl`

## Transcript

### User | 2026-05-07T02:12:32.093Z

# AGENTS.md instructions for H:\PRISM

<INSTRUCTIONS>
<!-- AUTO-MIRRORED FROM C:/Users/Mark Villanueva/.claude/CLAUDE.md by .claude/helpers/sync-cli-context-files.mjs -->
<!-- Mirror sync: 2026-05-04T19:20:36.750Z · Target: Codex CLI (global) · DO NOT EDIT — edits will be lost on next sync -->
<!-- To update: edit C:/Users/Mark Villanueva/.claude/CLAUDE.md, then run `node H:/PRISM/.claude/helpers/sync-cli-context-files.mjs` -->

# PRISM Global CLAUDE.md — Operational Playbook v2.5

> **Edit `C:\Users\wompu\.claude\settings.json` ONLY** — `c-to-h-mirror` hook auto-replicates C: → H: on every save. Editing `H:\.claude\settings.json` directly gets clobbered on the next mirror fire. (Junction `~/.claude` → `H:\.claude` is NOT active; both dirs are real and the mirror hook keeps them aligned.) Reference tables auto-injected by `reference-inject.mjs` hook when relevant keywords detected.

---

## EXPERT ROLE (ALWAYS ACTIVE)
PhDs across every math/science/eng/CS field. Deep thinker: exhaust obvious + non-obvious paths, edge cases, failure modes, second-order effects. Never "good enough" — optimal with justification.

---

## TOKEN ECONOMY
- **RTK prefix on bash** — `rtk vitest run` (99%), `rtk git/gh/npm/tsc/docker` (60-90% savings). Use in `&&` chains. Skip only if output <500 chars. `/rtk-setup` to install.
- **Ollama offload** — code explain/summarize/docstring/classify/lint/diff-summary/error-triage routed to local qwen2.5-coder:7b via `/ollama-*` skills (9 of them) and `OllamaHookBridgeEngine`. Reserve Claude for deep reasoning + safety. See `feedback_ollama_token_routing.md`.
- **Tool selection** — Glob/Grep over Bash find/grep · `Read offset=X limit=Y` for partial · Parallel independent tool calls in one message · Don't re-read after Edit/Write (hooks track).
- **Context extension** — per-agent `HANDOFF-<id>-<topic>.md` (6 chats), `MEMORY.md` index (<200 lines), digests over exploration, load-on-demand skills, keyword-gated hook injections.

---

## KARPATHY DISCIPL
... [+39020 chars truncated]

### User | 2026-05-07T02:12:32.093Z

You are CODEX REVIEWER 4 of 5 in a parallel optimization assessment of PRISM's XPROC-NEURAL stack.

CONTEXT: Read H:/prism/state/shared/NEURAL-ASSESSMENT-CONTEXT.md for the briefing. Key facts: 47 engines, T1-02 is a pure-JS MLP 32->16->3 with SGD-momentum + Xavier init. NO Adam, NO BatchNorm, NO dropout, NO residual, NO attention. Trains on synthetic data; convergence test passes 11/11 but doesnt prove real-shop generalization.

YOUR ANGLE: STRICT ML CORRECTNESS

Audit ML correctness of T1-02 in mcp-server/src/engines/CrossProcessNeuralLearningEngine.ts. Find:

1. ALGORITHMIC BUGS: backprop sign errors, momentum direction, softmax numerical instability (no log-sum-exp trick?), gradient clipping absent, weight init scale wrong for tanh vs ReLU, off-by-one in batch indexing, missing epoch shuffle, mini-batch boundary handling.
2. LOSS FUNCTION: cross-entropy correctness + numerical stability. Is reported finalLoss the actual converged loss?
3. FEATURIZATION HYGIENE: numeric normalization, categorical hash collisions, NaN handling, missing-feature handling, train/test feature-space symmetry.
4. TRAIN/PREDICT SYMMETRY: forward pass identical at train and inference time?
5. CONVERGENCE GUARANTEES: is loss reduction >=30% statistically significant for a 3-class softmax (random ~ln(3)=1.099)?
6. HYPERPARAMETER SANITY: lr=0.01, momentum=0.9, batch=32, 240-sample training set.
7. REPRODUCIBILITY: RNG seeded? Shuffle uses same RNG?
8. SAVE/LOAD: saveTo + loadFrom preserve weights bit-exactly?

OUTPUT: Plain text, <=600 words, lead with most severe findings, cite line numbers, end with VERDICT line: PASS / CONCERNS / FAIL.


### User | 2026-05-07T02:12:32.093Z

You are CODEX REVIEWER 4 of 5 in a parallel optimization assessment of PRISM's XPROC-NEURAL stack.

CONTEXT: Read H:/prism/state/shared/NEURAL-ASSESSMENT-CONTEXT.md for the briefing. Key facts: 47 engines, T1-02 is a pure-JS MLP 32->16->3 with SGD-momentum + Xavier init. NO Adam, NO BatchNorm, NO dropout, NO residual, NO attention. Trains on synthetic data; convergence test passes 11/11 but doesnt prove real-shop generalization.

YOUR ANGLE: STRICT ML CORRECTNESS

Audit ML correctness of T1-02 in mcp-server/src/engines/CrossProcessNeuralLearningEngine.ts. Find:

1. ALGORITHMIC BUGS: backprop sign errors, momentum direction, softmax numerical instability (no log-sum-exp trick?), gradient clipping absent, weight init scale wrong for tanh vs ReLU, off-by-one in batch indexing, missing epoch shuffle, mini-batch boundary handling.
2. LOSS FUNCTION: cross-entropy correctness + numerical stability. Is reported finalLoss the actual converged loss?
3. FEATURIZATION HYGIENE: numeric normalization, categorical hash collisions, NaN handling, missing-feature handling, train/test feature-space symmetry.
4. TRAIN/PREDICT SYMMETRY: forward pass identical at train and inference time?
5. CONVERGENCE GUARANTEES: is loss reduction >=30% statistically significant for a 3-class softmax (random ~ln(3)=1.099)?
6. HYPERPARAMETER SANITY: lr=0.01, momentum=0.9, batch=32, 240-sample training set.
7. REPRODUCIBILITY: RNG seeded? Shuffle uses same RNG?
8. SAVE/LOAD: saveTo + loadFrom preserve weights bit-exactly?

OUTPUT: Plain text, <=600 words, lead with most severe findings, cite line numbers, end with VERDICT line: PASS / CONCERNS / FAIL.


### Assistant | 2026-05-07T02:12:37.105Z

Review scope is T1-02 ML correctness only. I’ll read the assessment context and the neural engine source, then produce a severity-ordered findings report with exact line references.

### Assistant | 2026-05-07T02:12:37.105Z

Review scope is T1-02 ML correctness only. I’ll read the assessment context and the neural engine source, then produce a severity-ordered findings report with exact line references.

### Assistant | 2026-05-07T02:12:41.107Z

`rtk` isn’t installed in this shell, so I’m using direct file reads to continue the audit now.

### Assistant | 2026-05-07T02:12:41.107Z

`rtk` isn’t installed in this shell, so I’m using direct file reads to continue the audit now.

### Assistant | 2026-05-07T02:12:43.116Z

I’ve finished the full source read; next I’m pulling numbered lines so each finding is anchored precisely.

### Assistant | 2026-05-07T02:12:43.117Z

I’ve finished the full source read; next I’m pulling numbered lines so each finding is anchored precisely.

### Assistant | 2026-05-07T02:13:04.099Z

1) Critical algorithmic bug: hidden-layer gradient uses already-updated `W2` (in-place update order mismatch). In `stepOne`, `W2` is updated at lines 647-656, then `dHiddenPre` is computed using `this.W2` at line 664. Correct backprop requires using the pre-update `W2` from the same forward pass. This makes gradients biased and optimizer dynamics mathematically incorrect.

2) High severity feature leakage: training input includes `response_summary.success` (`f[offset++] = ...` at lines 313-315), which is downstream of the outcome you’re trying to predict. If this field exists at training time but not at real inference time, the model learns shortcut leakage and reported accuracy/loss are inflated. This is an ML correctness issue, not just data quality.

3) High severity metric correctness issue: `finalLoss` is not true post-training converged loss. It is accumulated from per-sample losses before each sample’s update (`stepOne` loss at lines 630-631, aggregated at 376-387). A true final model loss should be recomputed after the epoch with fixed weights (like `computeMeanLoss(samples)` after updates).

4) Medium algorithm concern: “mini-batch” is not true minibatch SGD. Loop uses batch boundaries (377-384) but updates per sample inside batch; no gradient accumulation/averaging. Not a bug, but hyperparameter interpretation (`batchSize`) is misleading and optimization behavior is pure online SGD with momentum.

5) Softmax/loss numerical stability: good. Softmax uses max-shift (610-620), CE clamps with epsilon (631, 689). No obvious instability bug there.

6) Featurization hygiene concerns:
- Hash bucket collisions are guaranteed to be frequent with tiny cardinalities (`material`=4, `tool_material`=3, `machine_family`=3, `operation`=3; lines 102-105, 296-310), causing aliasing and non-identifiability.
- Numeric handling maps missing/invalid/negative to 0 (277-282), conflating “missing” with real zero; no explicit missingness indicator for numeric features.
- NaN/Inf hand
... [+1295 chars truncated]

### Assistant | 2026-05-07T02:13:04.099Z

1) Critical algorithmic bug: hidden-layer gradient uses already-updated `W2` (in-place update order mismatch). In `stepOne`, `W2` is updated at lines 647-656, then `dHiddenPre` is computed using `this.W2` at line 664. Correct backprop requires using the pre-update `W2` from the same forward pass. This makes gradients biased and optimizer dynamics mathematically incorrect.

2) High severity feature leakage: training input includes `response_summary.success` (`f[offset++] = ...` at lines 313-315), which is downstream of the outcome you’re trying to predict. If this field exists at training time but not at real inference time, the model learns shortcut leakage and reported accuracy/loss are inflated. This is an ML correctness issue, not just data quality.

3) High severity metric correctness issue: `finalLoss` is not true post-training converged loss. It is accumulated from per-sample losses before each sample’s update (`stepOne` loss at lines 630-631, aggregated at 376-387). A true final model loss should be recomputed after the epoch with fixed weights (like `computeMeanLoss(samples)` after updates).

4) Medium algorithm concern: “mini-batch” is not true minibatch SGD. Loop uses batch boundaries (377-384) but updates per sample inside batch; no gradient accumulation/averaging. Not a bug, but hyperparameter interpretation (`batchSize`) is misleading and optimization behavior is pure online SGD with momentum.

5) Softmax/loss numerical stability: good. Softmax uses max-shift (610-620), CE clamps with epsilon (631, 689). No obvious instability bug there.

6) Featurization hygiene concerns:
- Hash bucket collisions are guaranteed to be frequent with tiny cardinalities (`material`=4, `tool_material`=3, `machine_family`=3, `operation`=3; lines 102-105, 296-310), causing aliasing and non-identifiability.
- Numeric handling maps missing/invalid/negative to 0 (277-282), conflating “missing” with real zero; no explicit missingness indicator for numeric features.
- NaN/Inf hand
... [+1295 chars truncated]
