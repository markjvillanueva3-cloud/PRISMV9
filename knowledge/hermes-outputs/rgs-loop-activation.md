# RGS Loop Activation — ZULU Primary Builder Emulation

**RGS = Research → Generate → Synthesize**

This loop will run autonomously (via cron or ZULU trigger) to continuously improve the builder emulation and fleet orchestration.

## Current RGS Targets
1. Improve `prism_builder:emulate_primary_builder` accuracy against real user prompts.
2. Refine 6-account 90% staggered switching logic.
3. Enhance awareness injection quality in PS tabs.
4. Add more user-style decision rules from deeper session analysis.

**Status:** Loop defined. Ready for first iteration.

**Next autonomous action:** Run first RGS cycle on the builder MCP functions.