# UNIT-0026 — Cryogenic and Hybrid Process Phenomena — GAP ANALYSIS
_Analyst: oscar (speed-feed soul) · 2026-07-02 · every claim cited file:line, read-verified_

## Existing coverage

**Cryogenic cooling + force models — THREE engines, all read end-to-end:**
- `mcp-server/src/engines/CryogenicCuttingEngine.ts:331-948` — in-process LN2/CO2/LN2+MQL physics: `cryoHeatTransfer` (Bromley film-boiling + Rohsenow nucleate-boiling correlations, :378-469), `cryoToolLife` (extended Taylor × K_cryo + Arrhenius diffusion-rate ratio + Monte-Carlo/Latin-Hypercube CI95, :489-562), `cryoForces` (Kienzle kc1.1 modified by yield-strength ratio + Hong & Ding 2001 friction-reduction, :583-665), `cryoSurfaceIntegrity` (Brinksmeier residual-stress model, white-layer risk, :684-743), `deliveryOptimization` (Weber-number jet-coherence, cost model, :762-833), `cryoMQL` (LN2+MQL synergy factor, :855-919). Literature-cited (Pusavec 2010, Jawahir 2016, Bermingham 2012, Bromley 1950, Rohsenow 1952, Kienzle 1952, Brinksmeier 1982, Hong & Ding 2001, :10-17).
- `mcp-server/src/engines/CryogenicTreatmentEngine.ts:98-265` — deep/shallow/ultra-deep cryo tool-steel treatment (retained-austenite→martensite conversion, eta-carbide precipitation, ROI/payback, :99-264). Distinct scope from CryogenicCuttingEngine per its own header comment (:1-11: pre-process cold-soak vs in-process coolant delivery).
- `mcp-server/src/engines/CryogenicMQLStrategySelectorEngine.ts:84-197` — coolant-strategy selector (dry/MQL/flood/cryo-LN2/cryo-CO2) by material/operation/MRR/sustainability, literature-cited (Sandvik §A-2, Cimcool, ASTM E2275, LBNL 2019, NFPA 484, :9-25).

**Hybrid laser-assisted machining (LAM) — TWO engines:**
- `mcp-server/src/engines/LAMThermalSofteningEngine.ts:283-848` — first-principles: Rosenthal moving-heat-source temperature field with Bessel K₀ (:328-358, self-contained rational approximation :223-240), Johnson-Cook flow-stress reduction (:360-376), tool-life extension with Monte Carlo on J-C A/B params (:508-567), laser-tool spacing optimization (:587-658), power×speed process-window grid search (:678-747), full economics/ROI (:767-841). Literature-cited (Sun/Brandt/Dargusch 2010, Lei/Shin/Incropera 2001, Anderson/Patwa/Shin 2006, Pfefferkorn 2009, Johnson-Cook 1983, :12-23).
- `mcp-server/src/engines/HybridLaserMachineEngine.ts:59-164` — simpler empirical model covering `laser_assisted_turning/milling`, `laser_clad_mill`, `additive_subtractive` process sequencing + thermal-risk classification (:60-158).

**Hybrid ultrasonic/vibration-assisted machining — TWO engines:**
- `mcp-server/src/engines/UltrasonicMachiningPhysicsEngine.ts:156-401` — Shaw MRR model (`MRR = K·f·A^1.5·d·√(σw/Hw)`, :179-246), abrasive selection (:252-319), feasibility assessment (:325-397). Literature-cited (Shaw 1956, Miller 1957, Thoe 1998, Komaraiah & Reddy 1993, :14-18).
- `mcp-server/src/engines/VibrationAssistedMachiningEngine.ts:66-204` — 5 VAM modes (ultrasonic linear/elliptical, low-freq linear/torsional, resonant 2D), critical-speed-ratio intermittent-cutting model, force reduction, tool-life multiplier, chip-breaking, actuator power (:67-200). Literature-cited (Brehl & Dow 2008, Shamoto-Moriwaki, Zheng, Weber & Endrino, :6-8).

**Magnetic-assisted machining — searched, NOT FOUND:**
- Grepped `mcp-server/src` for `magnetic.field.assist|magnetic.abrasive|MFAM|magnetic.assist.*machin` (case-insensitive) — 5 hits, all false positives on unrelated substrings (`PostPropertyTaxonomyEngine.ts`, `WEDMNeuralTrainingEngine.ts`, `InfiniteConditionCombinatorEngine.ts`, `WEDMPartFamilyMatcherEngine.ts`). Also grepped `mcp-server/data` for `magnetic abrasive|magnetic.assisted machining|MFAM` — the 2 hits (`build_taxonomy.ps1`, `prism-reference-db/cad.json`) are confirmed false positives (PowerShell var `$almFamilies`, and CAD-part catalog text with no "magnetic abrasive" phrase, read and verified). Every existing "magnetic" engine hit (`magnetic-chuck-calc`, `lathe-workholding-magnetic-chuck` per pre-grep graph) is magnetic-chuck **workholding**, not magnetic-field-assisted **material removal**. **No magnetic-field-assisted machining (MFAM) or magnetic abrasive finishing (MAF) physics engine exists anywhere in PRISM.**

**Wiring to prism_calc — PARTIAL, verified by reading calcDispatcher.ts's TWO switch blocks:**
- z.enum action lists (schema-registered, so calls pass validation) at `calcDispatcher.ts:643` (`cryo_predict/recommend/roi`), `:677` (`hybrid_laser_calc`), `:847` (`cryo_heat_transfer, cryo_tool_life, cryo_forces, cryo_surface_integrity, cryo_delivery_optimize, cryo_mql`), `:911-912` (`lam_preheat_profile, lam_force_reduction, lam_tool_life, lam_optimal_spacing, lam_process_window, lam_economics`), `:975` (`usm_mrr, usm_abrasive_select, usm_feasibility`).
- The ENGINE-CALL switch (`switch(action)` at `:1468`, runs to `default: throw new Error('Unknown calculation action')` at `:11062-11063`) has real cases for `cryo_predict/recommend/roi` (:2778-2790, CryogenicTreatmentEngine), `hybrid_laser_calc` (:4583-4594+, HybridLaserMachineEngine), `cryo_surface_integrity/cryo_delivery_optimize/cryo_mql` (:6162-6176, CryogenicCuttingEngine), `usm_mrr/usm_abrasive_select/usm_feasibility` (:7914-7926, UltrasonicMachiningPhysicsEngine), `lam_tool_life/lam_optimal_spacing/lam_economics` (:6867-6881, LAMThermalSofteningEngine).
- **A SEPARATE switch (`calcExtractKeyValues`, `:37`, a post-hoc response-formatter that runs only after `result` already exists) has formatter entries for `cryo_heat_transfer`/`cryo_tool_life`/`cryo_forces` (:372-377) and `lam_preheat_profile`/`lam_force_reduction`/`lam_process_window` (:424-428) — but I grepped the WHOLE FILE for `case "cryo_heat_transfer"` etc. and found ONLY the formatter-switch hit, no matching case in the engine-call switch.** Calling these 6 actions via `prism_calc` today hits `default: throw new Error("Unknown calculation action: ...")` (:11062-11063) — a loud failure, not a silent no-op, but a real wiring gap. The underlying engine methods (`cryoHeatTransfer`, `cryoToolLife`, `cryoForces`, `preheatProfile`, `forceReduction`, `processWindow`) are fully implemented and unit-tested directly against the engine class (see below) — they are simply never invoked from the dispatcher.
- `VibrationAssistedMachiningEngine` is wired as `vam_calculate` on a SEPARATE top-level MCP dispatcher, `prism_vibration_physics` (`vibrationPhysicsDispatcher.ts:26,51`, registered `index.ts:222`) — NOT `prism_calc`. It is reachable via MCP, but not via the literal `prism_calc` route the acceptance criterion names.
- `CryogenicMQLStrategySelectorEngine` is wired under `COOLANT_STRATEGY_ACTIONS` on `safetyDispatcher.ts:803-806`, also not `prism_calc`.

**Tests — all six candidate engines have companion coverage (read-verified file names, not content):**
- `mcp-server/src/__tests__/phase5-forge-engines.test.ts:242-336` exercises `cryoHeatTransfer`, `cryoToolLife`, `cryoForces` directly on `CryogenicCuttingEngine` (confirms the orphaned dispatcher actions ARE tested at the engine layer, just not round-tripped through the dispatcher — a dispatcher-level test gap per R15's "round-tripped THROUGH the dispatcher, not just the singleton").
- `mcp-server/src/__tests__/phase5-forge-c-engines.test.ts:118-120` exercises `LAMThermalSofteningEngine.processWindow`.
- `mcp-server/src/__tests__/non-traditional-machining.test.ts` references `UltrasonicMachiningPhysicsEngine`.
- `mcp-server/src/__tests__/batch106-engines.test.ts:6,45-74` — full `VibrationAssistedMachiningEngine` describe block (modes, workpieces, edge cases).
- `mcp-server/src/__tests__/l2-pass2-specialty-engines.test.ts` + `specialty-process-engines.test.ts` reference `HybridLaserMachineEngine`.
- `mcp-server/src/__tests__/CryogenicMQLStrategySelectorEngine.test.ts` exists.

**Existing-code caveat (flagged, not this unit's fault to fix, but relevant to physics-reviewer trust):**
- `CryogenicCuttingEngine.ts:157-184` hard-codes a local `MATERIALS` map with `kc1_1`/`mc` Kienzle values (Ti-6Al-4V, Inconel 718, AISI 4140, AISI 316L, Al 7075-T6) that duplicate — rather than import — `src/physics/constants.ts`. The engine's own comment at `:170` admits this ("kc1_1=1800, mc=0.25 per canonical ISO P in src/physics/constants.ts"), confirming the duplication is a known, uncorrected debt against the "never inline physics constants" rail. [UNVERIFIED whether the two sources have since drifted numerically — I did not diff constants.ts's current AISI 4140 P-group values against this table this session.]

**Emerging-phenomena catalog deliverable — NOT FOUND:**
- Grepped for `emerging.phenomena|EmergingPhenomena|advanced.*emerging.*process` across `mcp-server/src` — 0 hits. No consolidated catalog document/registry ties the cryogenic + hybrid engines together as a named "Domain 8: Advanced & Emerging Phenomena" surface; each engine stands alone, discoverable only via `ENGINE_DIGEST.md` grep.

## Verdict
**extend**

## Recommended action
1. **Wire the 6 orphaned `prism_calc` actions** (`cryo_heat_transfer`, `cryo_tool_life`, `cryo_forces`, `lam_preheat_profile`, `lam_force_reduction`, `lam_process_window`) — near-zero-risk, the engine methods already exist, are literature-cited, and are unit-tested at the class level; this is adding six `case` statements to the switch at `calcDispatcher.ts:1468+` that call the already-implemented `cryogenicCuttingEngine`/`lamThermalSofteningEngine` methods, mirroring the pattern at `:6162-6176`/`:6867-6881`. Add dispatcher-level round-trip tests per R15 (currently these 6 are only tested against the singleton, not through `prism_calc`).
2. **Build the ONE genuine net-new-code gap: a magnetic-field-assisted-machining / magnetic-abrasive-finishing physics engine** (e.g. `MagneticAssistedMachiningEngine`), following the `UltrasonicMachiningPhysicsEngine`/`CryogenicCuttingEngine` template — magnetic force on ferromagnetic abrasive particles, MRR model, surface-finish/force models, feasibility — literature-grounded (e.g. Shinmura et al. magnetic abrasive finishing, Singh et al. MAF surface-finish models), wired to `prism_calc` as new `mfam_*`/`maf_*` actions, with a companion test file and a physics-reviewer pass on the force/stability formula per this soul's escalation rule (`defer-chip-thinning-and-sld-to-physics-reviewer` analog — any new force model needs the same gate).
3. Optionally close the minor "emerging phenomena catalog" deliverable with a short registry/README cross-referencing the 7 existing engines (cryogenic ×3, laser-hybrid ×2, ultrasonic/VAM ×2) plus the new magnetic engine — low effort, improves discoverability, not physics-load-bearing.
4. Confirm — do NOT assume — whether JM Die has any cryogenic/laser/ultrasonic/magnetic process data; I found none in this search and the unit spec's "any available JM Die data validation" phrasing already hedges for that likely-null case. [UNVERIFIED: I did not exhaustively search the JM Die corpus this session; this is a reasonable inference from JM Die being a conventional mill/lathe/EDM job shop per `jm-die-profile.ts`, not confirmed absence.]

## ROI
**6/10** — the domain is ~85% built with genuinely rigorous, literature-cited, Monte-Carlo-uncertainty-bearing physics (this is some of the more rigorous engine work in the codebase, not a stub farm), so a from-scratch "build" verdict would be dishonest and wasteful. The remaining work is small, well-scoped, and mostly mechanical (6 dispatcher cases) plus one bounded new-physics unit (magnetic-assisted machining) that completes the acceptance criterion's explicit "laser/ultrasonic/magnetic" triad. Priority P2 / 5hr estimate in the unit spec is consistent with this being a finishing pass, not a green-field build.
