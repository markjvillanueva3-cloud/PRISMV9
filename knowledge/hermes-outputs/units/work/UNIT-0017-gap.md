# UNIT-0017 — Grinding Integration — GAP ANALYSIS
_Analyst: foxtrot (mill soul) · 2026-07-02 · every claim cited file:line, read-verified_

## Existing coverage

**Grinding force/heat models (criterion 1) — REAL physics, not stubs:**
- `mcp-server/src/engines/GrindingForceEngine.ts` (335 LOC per `mcp-server/data/GRINDING_EDM_ENGINE_AUDIT.md:21-35`, module read via dispatcher wiring below) — Malkin specific grinding energy (1989) + Jaeger moving heat source (1942) + contact-arc geometry (Marinescu 2004) + kinematic chip-thickness + G-ratio.
- Wired to **prism_calc** (`server.tool("prism_calc", ...)` confirmed at `mcp-server/src/tools/dispatchers/calcDispatcher.ts:1315-1316`): `grinding_force_calc` (:6687-6690, imports `grindingForceEngine`), `grinding_surface_finish_calc` (:6692-6695), `centerless_grinding_calc` (:6697-6700), `grinding_wheel_calc` (:5732-5735), `stochastic_grinding` (:6755-6758), `stochastic_grinding_mc` (:6787-6790), `stochastic_grinding_optimize` (:6792-6796), `grinding_burn_risk` (:8230-8233, calls `grindingSurfaceFinishEngine.assessBurnRisk`, method confirmed at `mcp-server/src/engines/GrindingSurfaceFinishEngine.ts:374`), `physics_grinding_thermal`/`physics_single_grit` (:7372-7385, maps to `grindingThermalModel`).
- Also wired to a dedicated **prism_grinding** dispatcher (`mcp-server/src/tools/dispatchers/grindingDispatcher.ts:38-40`, 10 actions: `wheel_select, dress_params, burn_threshold, surface_integrity, grinding_force, surface_finish_predict, wheel_life_predict, dressing_interval_optimize, dressing_tool_select, creep_feed_setup` — full enum at :20-24). `surface_integrity` action (:145-168) computes an `integrity_grade` (reject/caution/marginal/acceptable) from Ra, residual stress sign, burn, and micro-cracks — this is criterion "surface integrity" satisfied directly.
- **Wheel wear** (explicit sub-criterion of "force, heat, wheel wear, surface integrity"): `mcp-server/src/engines/GrindingWheelDressingOptimizationEngine.ts` implements a real G-ratio wear model — `g_ratio`/`wheel_wear_rate_mm3_per_mm3` fields (:26-27), `BASE_G_RATIO` empirical table by bond type (:74-82), `predictWheelLife()` (:170-201) computing wear volume rate and estimated life. Wired at `grindingDispatcher.ts:209-213` (`wheel_life_predict`).

**Hybrid process recommendations (criterion 2) — TWO real engines, not one, cover this:**
- `mcp-server/src/engines/ProcessRoutingEngine.ts:1-180` — routes a part's FULL build across mill/turn/drill **and** heat-treat/surface-grind/jig-grind/wire-EDM/sinker-EDM/jig-bore/assembly/inspect (`ProcessKind` union :45-58). Header (:1-35) states explicitly it composes milling decomposition with die-build stages "the milling-only ProcessPlanEngine does not cover." `hardness_hrc >= HARDENED_HRC` (50, :151) **auto-injects** heat-treat + forces post-HT grind/EDM routing (doc comment :97-98). This is the mill+grind hybrid-process-recommendation engine the unit asks for.
- `mcp-server/src/engines/GrindingReplacementEngine.ts:1-100+` — answers "can this grind op be replaced with hard turning (CBN/ceramic)?" with a `Feasibility = "replace"|"partial"|"blocked"` decision (:70-87), citing Klocke (2009)/Tönshoff (2000)/König (1996) (:24-27). Wired at `mcp-server/src/tools/dispatchers/dataDispatcher.ts:2534-2543` (`grinding_replacement_evaluate`, `grinding_replacement_stats`) on the `prism_data` tool (name confirmed `dataDispatcher.ts:268-270`). This is the turn+grind hybrid-decision engine.
- `mcp-server/src/engines/GrindingProgramAssemblerEngine.ts:1-39` — 19-operation-type G-code assembler (surface/cylindrical OD-ID/centerless/special) that turns whichever strategy is chosen into an actual program (Fanuc/Siemens/Studer/Kellenberger/Junker dialects, :17), i.e. the "hybrid strategy outputs" deliverable.

**JM Die grinding job validation (criterion 4) — REAL JM part, not a synthetic fixture:**
- `mcp-server/src/__tests__/ProcessRoutingEngine.test.ts:1-50` — fixture is explicitly the **C-033626 flattening-die-set Base component** (a real JM Die job, per file header :8 and test docstring :4-8), material AISI D2 (H-group), `hardness_hrc: 57` (:33), includes a `surface_grind` extra_step ("Surface grind 6 faces + datum A .0005", tolerance_mm 0.013, :42) alongside wire_edm/jig_bore/assembly/inspect steps. The test asserts the engine routes to the SAME JM machine + rate `ShopConfigurationEngine.selectCapableMachines` would independently pick (:4-6, 21-26) — an intent test (R9), not a shape test.
- The routing target is a real JM machine: `mcp-server/data/shop/shop-profile.json:441-447` — `"name": "Surface Grinder", "type": "Grinder", capabilities: ["surface_grinding","flat_grinding"]` inside the `jm-die` shop profile (`mcp-server/src/engines/ShopConfigurationEngine.ts:438-439` sets `id: "jm-die"`).
- `mcp-server/data/docs/grinding-test-programs.md:1-56` — reference G-code for Studer S20 OD-cylindrical-traverse and Fanuc plunge-grind-with-G31-gauging cycles (CAMX-MS22 U03, :1), used as pipeline validation fixtures.

**Additional supporting evidence (breadth, not double-counted against the 5 criteria above):**
- `mcp-server/data/GRINDING_EDM_ENGINE_AUDIT.md:1-16` lists 10 grinding engines / 9,189 LOC / 6 physics models "VERIFIED" (QA-MS10, 2026-04-13) — **NOTE, read end-to-end and CROSS-CHECKED against live code**: this audit's "Dispatcher Wiring" table (:249-258, action names `surface_grind_calc`, `centerless_setup`, `dressing_optimize`) does **NOT** match the live `grindingDispatcher.ts` ACTIONS enum (:20-24, actual names `wheel_select`/`grinding_force`/`wheel_life_predict`/etc.) — the audit doc is stale/drifted from the current dispatcher and should not be cited as current-state proof; I relied only on the live `.ts` files above, not this doc's tables.
- Companion tests exist for adjacent grinding surfaces: `mcp-server/src/__tests__/GrindingReplacementEngine.test.ts`, `MastercamGrindingBridge.test.ts`, `HyperMillGrindingBridge.test.ts`, `GrindingTribalCorpusEngine.test.ts` (confirmed present via Glob; not opened this session — **[UNVERIFIED]** contents, cited for existence only).

## Verdict
**already-covered**

## Recommended action
Do NOT build a new `GrindingIntegrationEngine` — every acceptance criterion already has a live, dispatcher-wired, read-verified implementation:
1. Force/heat/wheel-wear/surface-integrity models → `GrindingForceEngine` + `GrindingWheelDressingOptimizationEngine` + `GrindingSurfaceFinishEngine`, all wired to `prism_calc` and `prism_grinding`.
2. Hybrid process recommendations → `ProcessRoutingEngine` (mill+grind+EDM+heat-treat build routing) and `GrindingReplacementEngine` (turn-vs-grind decision), wired to `prism_data`.
3. JM Die grinding job validation → `ProcessRoutingEngine.test.ts` already validates against the real C-033626 flattening-die-set surface-grind step, routed to JM Die's actual Surface Grinder machine.
4. "Full scrutiny" is a process attestation, not a code artifact — **[UNVERIFIED]**: `CAMX-MS22.json` shows related grinding units marked `"status": "complete"` (multiple entries, e.g. line 22/33/44...) but I did not find a scrutiny-ledger entry specifically naming UNIT-0017; if the unit-plan process requires a formal 3-of-3 scrutiny sign-off tied to this exact unit ID, that administrative step (not new code) is the only open item.
If the plan insists on a named `GrindingIntegrationEngine` deliverable for discoverability, the correct move (R8/dedup) is a **thin facade/re-export module** that aggregates the existing engines' public APIs under one import path — not new physics, not new tests of substance, and only if a consumer actually needs single-import ergonomics (none identified this session).

## ROI
**1/10** — building anything net-new here duplicates ~9,000+ LOC of already-wired, already-tested, already-validated-against-a-real-JM-job grinding physics across `prism_calc`, `prism_grinding`, and `prism_data`. The only genuinely open item is a process-level scrutiny sign-off, not code.
