# uncategorized/

**Not a junk bucket.** This dir holds `MEMORY-ARCHIVE.md` — the deliberate archive of older `MEMORY.md` index entries (discoverable, read-on-demand), NOT stray or untriaged content.

Do not dump notes here. Route to a typed dir (`feedback`/`reference`/`project`/`user`) or the wiki.
