---
name: bravo-free-reign-backend-incl-india
description: "Bravo has FREE REIGN on ALL backend development -- explicitly INCLUDING india's domain (AI systems / NN / GNN / LoRA / RAG / deep learning / ML / deep reasoning / pattern training). Operator directive 2026-06-11. Bravo may pick up and build india/AI-training backend units directly; coordinate, don't defer."
type: feedback
source: prism-memory
synced: 2026-06-27T20:30:46.415Z
aliases: feedback_bravo_free_reign_backend_incl_india
---


# Bravo: free reign on all backend dev, including india's AI work

## The rule (operator directive 2026-06-11)

**Bravo has free reign on ALL backend development, and that EXPLICITLY includes india's work** -- AI systems, NN, GNN, LoRA, RAG, deep learning, machine learning, deep reasoning, pattern training (the `ai-training` galaxy). Bravo may pick up and build india/AI-training backend units directly (e.g. the loop's `U-NN-TIER05` / `XPROC-NEURAL-OPTIMIZE-MS0`, the NN-GRAPH deploy-gate work). No domain-ownership gate blocks this.

This INSTANCES the standing fleet-wide grant [[feedback_primary_backend_builders_no_galaxy_gate_block]] (primary backend builders -- alpha/bravo/golf/sierra/papa/quebec/india -- are never blocked by an ownership gate) and [[feedback_bravo_all_galaxy_navigate_build]] (bravo builds in ALL galaxies), making india's AI/ML domain an explicit, named part of bravo's backend free-reign.

## How to apply

- **Take india/AI-training backend units from the loop directly** -- when `loop-state.mjs next` resolves an `ai-training` / NN / GNN / LoRA / RAG unit (it routed `U-NN-TIER05` repeatedly), BUILD it; do not decline it as "off-domain." The ownership gate is advisory.
- **Coordinate, don't collide** -- india is an active slot. Before building an india unit, check `slot-task-claim.mjs check` + the chat bus; if india holds the unit, coordinate (patch-sibling / chat-bus post) rather than race. Free reign != ignore peers.
- **Use india's substrate** -- the `ai-training` galaxy brain (`mcp-server/src/engines/ai-training/MEMORY.md`) carries the NN-GRAPH deploy gate (AUROC>=0.78, macroF1>=0.55, Brier<=0.15; current AUROC 0.808 PASS, macroF1/Brier still failing; calibration is a DEAD END for the Brier gate). Pull it before NN work via `prism_memory:semantic_search query="ai training"`.
- **Apply fleet-wide where general** -- per [[feedback_enhancements_auto_apply_all_galaxies]], any backend enhancement bravo builds (in india's domain or elsewhere) propagates to all galaxies that share the need.

## Why

Bravo is a Hermes/Zulu builder -- the orchestration + backend substrate slot. AI-training is backend infrastructure (the GNN tier-5 wiring-inference cascade, the LoRA/RAG corpus pipeline) that the whole fleet depends on, and india's NN deploy gate has been a long-standing blocker (gates several galaxies). Letting bravo's backend capacity also drive india's AI work removes the single-slot bottleneck. **Why this matters:** more backend throughput on the fleet's hardest-blocked domain. **How to apply:** build india/AI units the loop hands you, coordinating with india, using india's galaxy-brain substrate.
