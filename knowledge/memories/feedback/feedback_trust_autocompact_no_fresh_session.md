---
name: feedback_trust_autocompact_no_fresh_session
description: NEVER tell the operator to "start a fresh session" or "wrap up for budget" on a long session. Autocompaction is solved — native autocompact at 95% + auto-written handoff + /startup resume are seamless. Trust it and KEEP DELIVERING through the 880K SOFT nudge. The model cannot invoke /compact (built-in, harness-controlled); there is nothing to "auto-invoke" — it already fires automatically.
type: feedback
source: prism-memory
synced: 2026-06-27T20:30:46.447Z
aliases: feedback_trust_autocompact_no_fresh_session
---


Operator correction (2026-06-12, slot:alpha): *"I thought we took out the fresh session stuff since we seem to have solved autocompaction."*

**Rule (FLEET-WIDE):** On a long session, do NOT suggest starting a fresh session, do NOT stop "for budget," and do NOT treat the 880K SOFT precompact nudge as a stop signal. Per R6, context growth is NOT a stop signal — only a *spiral* is. Keep delivering units. The session-continuity stack handles the reset seamlessly.

**Why:** Verified the mechanism (`precompact-auto-trigger.mjs`, 2026-06-12): SOFT 880K (handoff nudge, says "KEEP WORKING") -> HARD 940K (blocks tool calls EXCEPT the handoff-write, then arms a pending marker so work resumes) -> native autocompact at `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=95` (~950K) actually compacts -> `session-start-auto-resume` + `/startup` resume from the auto-written `HANDOFF-<slot>-<topic>.md`. The HARD-block message itself says: *"run it, then keep working (autocompact + /startup resume cleanly)."* The model was mis-reading the nudge as "wrap up."

**How to apply:**
1. At SOFT (880K): author your OWN handoff (`per-agent-handoff.mjs write --source live-chat ...`) at the next natural pause, then KEEP SHIPPING. Do not stop, do not tell the operator to start fresh.
2. The model CANNOT call `/compact` (built-in CLI; Skill tool forbids built-in commands; hooks can only block/nudge). Native autocompact is the only auto-trigger and it already fires. So "auto-invoke compaction" = trust the existing native autocompact; there is nothing to add.
3. The only tunable is WHEN native autocompact fires: `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` (95 = max working window; lower = earlier/more-proactive but more frequent handoff cycles) + `PRECOMPACT_SOFT_TOKENS` (880K) / `PRECOMPACT_HARD_TOKENS` (940K) in `H:/.claude/settings.json` env.

**LIVE VALUES (operator-chosen 2026-06-12, slot:alpha) — DO NOT silently revert** (settings drift is a known fleet hazard, [[feedback_settings_wiring_drift_2026_05_16]]): the operator asked for *"precompaction generate at 90% then compact right after."* Set in `C:/Users/wompu/.claude/settings.json` env (mirrored to H:): `PRECOMPACT_SOFT_TOKENS=860000` (86%, model writes rich handoff) · `PRECOMPACT_HARD_TOKENS=900000` (90%, handoff GUARANTEED generated by here) · `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=92` (compact fires ~920K, right after). Was 880K/940K/95. Takes effect on the NEXT session start (env is read at startup; a mid-session edit does not retune the running session). Reversible: restore 95 / remove the two PRECOMPACT_*_TOKENS lines.

Related: [[feedback_context_growth_not_a_stop_signal]] · [[reference_precompact_hook_autowrite_2026_05_15]] · [[reference_session_continuity_stack_2026_05_15]] · [[feedback_settings_wiring_drift_2026_05_16]].
