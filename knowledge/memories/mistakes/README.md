# mistakes/ — error & regression lessons (MECE)

Raw mistake/regression notes before they're distilled into a `feedback_*` memory + a wiki `lessons/` entry.

**Status (2026-06-28, sierra):** empty by current convention. PRISM routes regressions to CLAUDE.md `## Recent regressions` + `feedback_*` memories + wiki `lessons/`, and the per-chat `HANDOFF-*` captures in-session mistakes. Use this dir for a raw mistake note you have not yet distilled.
