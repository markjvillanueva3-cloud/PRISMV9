# inbox/ — capture queue (MECE)

Fleeting captures awaiting triage into a typed memory (`feedback`/`reference`/`project`/`user`) or the wiki.

**Status (2026-06-28, sierra):** NOT auto-routed. The Stop-hook memory feed (`stop-obsidian-memory-feed.mjs`) writes typed memories straight to their type dir from the C: auto-memory source — nothing currently lands here automatically. Drop a note here by hand when you want to defer categorization, then move it on triage.

Promotion path: fleeting → **inbox** → typed memory → wiki → CLAUDE.md pointer.
