---
name: cad-fusion-live_synthesis
description: "[auto-synth · verify] Compounding synthesis of the cad-fusion-live domain — recurring patterns, decisions, open threads distilled from 4 memories (LLM-generated; verify against source memories before trusting)"
metadata:
  type: patterns
  galaxy: cad-fusion-live
  synthesizedFrom: 4
  model: gpt-oss:120b
  synthesizedAt: 2026-06-26T17:58:13.007Z
  sourceHash: aa8314ee5a3c
  advisoryOnly: true
  mustHumanVerify: true
---

# cad-fusion-live — domain synthesis (compounding)

> ⚠ ADVISORY — LLM-generated (gpt-oss:120b), `mustHumanVerify`. Auto-distilled by
> `galaxy-reflection-synthesis.mjs` (B1) from 4 domain-relevant memories via the
> A6/A3 hybrid recall. Verify any rule here against its cited source memory before trusting
> it as fact (esp. safety-relevant thresholds). Regenerated on each run — edit the source
> memories, not this file. The compounding arm of the Obsidian brain.

## Recurring patterns
- **Hard‑coded toolchain baseline** – Fusion/hyperMILL + hyperCAD‑S/Mastercam are embedded directly into the UI knowledge layer for CAD‑FUSION‑LIVE [reference/reference_post_ship_cad-fusion-live-ms0-u-delta-ui-knowledge].
- **Delta‑driven asset creation** – New high‑ROI CAD assets are always generated by mining prior sessions, then wiring them into tribal wikis and rule sets before synthesis [reference/reference_delta_cad_asset_generation_2026_05_29].
- **Distill‑mode for LoRA pipelines** – A `--distill` flag is added to the domain‑corpus‑to‑LoRA conversion step, making the process repeatable across releases [reference/reference_post_ship_domain-knowledge-u-papa-lora-distill-mode].
- **Galaxy‑wide LoRA feeding** – The vault‑to‑LoRA dataset script now supports a `--source galaxy` mode, pulling 512 advisory‑tagged Alpaca pairs per galaxy to train LoRAs across all 34 galaxies [reference/reference_lora_galaxy_synthesis_feeder_2026_06_10].

## Key decisions & rules
- **Embed baseline UI knowledge**: Fusion/hyperMILL + hyperCAD‑S/Mastercam are hard‑coded as the default UI configuration for CAD‑FUSION‑LIVE [1].
- **Pre‑generation audit**: Before any delta asset generation, run a full prior‑session check and integrate findings into CLAUDE.md rules, GSD, and tribal wikis; then wire+synergize the output [2].
- **Enable distill mode**: All domain‑to‑LoRA pipelines must be invoked with `--distill` to ensure consistent corpus transformation [3].
- **Galaxy source flag**: Use `--source galaxy` in `vault-to-lora-dataset.mjs` to feed each of the 34 galaxies with advisory‑tagged Alpaca pairs, guaranteeing uniform training signals across the LoRA fleet [4].

## Open threads
- **Integration of hard‑coded UI baseline with evolving LoRAs** – How will updates from galaxy‑wide LoRA synthesis be reconciled with the static Fusion/hyperMILL configuration?
- **Conflict resolution between delta‑generated assets and existing UI knowledge** – What merging strategy should be adopted to avoid redundancy or inconsistency?
- **Tagging schema for advisory Alpaca pairs** – Need a definitive taxonomy that works across all galaxies while preserving ROI signals.
- **Scalability beyond 34 galaxies** – Assess whether the current `--source galaxy` approach can extend to additional domains without performance degradation.
