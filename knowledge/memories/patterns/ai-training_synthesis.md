---
name: ai-training_synthesis
description: "[auto-synth · verify] Compounding synthesis of the ai-training domain — recurring patterns, decisions, open threads distilled from 12 memories (LLM-generated; verify against source memories before trusting)"
metadata:
  type: patterns
  galaxy: ai-training
  synthesizedFrom: 12
  model: gpt-oss:120b
  synthesizedAt: 2026-06-11T03:22:01.302Z
  sourceHash: 43e59e5c9d29
  advisoryOnly: true
  mustHumanVerify: true
---

# ai-training — domain synthesis (compounding)

> ⚠ ADVISORY — LLM-generated (gpt-oss:120b), `mustHumanVerify`. Auto-distilled by
> `galaxy-reflection-synthesis.mjs` (B1) from 12 domain-relevant memories via the
> A6/A3 hybrid recall. Verify any rule here against its cited source memory before trusting
> it as fact (esp. safety-relevant thresholds). Regenerated on each run — edit the source
> memories, not this file. The compounding arm of the Obsidian brain.

## Recurring patterns
- **Per‑galaxy synthesis & compounding** – Each galaxy produces a `*_synthesis.md` that distills its memories into reusable patterns (B1 compounding arm) [reference_alpha_b1_galaxy_reflection_2026_05_29].  
- **Consumer arm (AMP‑CONSUME)** – After recall, slot‑specific pattern bundles are surfaced for downstream use and peer verification [reference_alpha_amp_consume_synthesis_line_2026_05_30].  
- **Cross‑galaxy LoRA training feed** – A unified dataset (`vault-to-lora-dataset.mjs`) streams 512 advisory‑tagged Alpaca pairs from all 34 galaxies into a shared LoRA model [reference_reference_lora_galaxy_synthesis_feeder_2026_06_10].  
- **Auto‑distilled post‑ship learnings** – Every major shipment (e.g., GALAXY‑ENRICH, AI‑TRAINING‑FIRST‑MS0) generates a distilled wiki entry summarising decisions, capabilities, and open items [references 4‑8].  
- **Canonical memory structure** – All `MEMORY.md` files are forced into a four‑section brain schema; reviewers flagged inflation and a failing RED test during this normalization [reference_galaxy_memory_fill_2026_06_08].

## Key decisions & rules
- **Domain‑owned self‑improving AI systems** – Every PRISM domain must clone, customize, and operate its own AI‑training pipeline from the India “ai‑training galaxy” reference implementation [feedback_domains_own_ai_training_systems].  
- **Operator directive on brain refresh** – The scheduled Brain Refresh task is currently unregistered; without registration the per‑galaxy synthesis auto‑refresh silently degrades [reference_brain_refresh_task_unregistered_2026_06_09].  
- **Memory completeness mandate** – All galaxies must host complete `MEMORY.md`, file paths, and wiki entries; gaps trigger operator remediation [reference_galaxy_memory_fill_2026_06_08].  
- **Compounding vs. Consumer separation** – The compounding arm (B1) creates pattern libraries; the consumer arm (AMP‑CONSUME) exposes them to peers for verification and downstream consumption [references 3,9].

## Open threads
- **Brain Refresh registration** – Identify the host(s) lacking the refresh task registration and automate its deployment to stop synthesis rot.  
- **Corpus inflation & RED test failure** – Resolve the 20× corpus count discrepancy and address the self‑defeating RED test uncovered during memory canonicalization.  
- **LoRA source‑galaxy mode rollout** – Validate that the `--source galaxy` flag correctly aggregates advisory tags across all 34 galaxies without data duplication.  
- **Alignment of auto‑distilled learnings** – Ensure consistency and traceability between the multiple post‑ship synthesis artifacts (e.g., GALAXY‑ENRICH, AI‑TRAINING‑FIRST‑MS0) and the central compounding repository.  
- **Consumer arm peer verification workflow** – Formalize the zebra→zulu claim process for AMP‑CONSUME patches to guarantee timely pattern adoption across slots.
