---
name: mill_synthesis
description: "[auto-synth · verify] Compounding synthesis of the mill domain — recurring patterns, decisions, open threads distilled from 6 memories (LLM-generated; verify against source memories before trusting)"
metadata:
  type: patterns
  galaxy: mill
  synthesizedFrom: 6
  model: gpt-oss:120b
  synthesizedAt: 2026-06-11T04:48:47.533Z
  sourceHash: ced86d94638b
  advisoryOnly: true
  mustHumanVerify: true
---

# mill — domain synthesis (compounding)

> ⚠ ADVISORY — LLM-generated (gpt-oss:120b), `mustHumanVerify`. Auto-distilled by
> `galaxy-reflection-synthesis.mjs` (B1) from 6 domain-relevant memories via the
> A6/A3 hybrid recall. Verify any rule here against its cited source memory before trusting
> it as fact (esp. safety-relevant thresholds). Regenerated on each run — edit the source
> memories, not this file. The compounding arm of the Obsidian brain.

## Recurring patterns
- **Auto‑distilled knowledge pipelines** – Each high‑ROI wiki tribal release (climb vs conv [3], flute‑helix tooling [4], WEDM tactics [5]) follows a repeatable “ship → auto‑distill → wiki update” loop that feeds back concrete tactics into the mill’s operational playbook.  
- **Cross‑galaxy signal injection** – The per‑galaxy LoRA synthesis brains (Obsidian) broadcast a unified training signal to all 34 galaxies, acting as a shared “knowledge feeder” for mill‑wide model updates [1].  
- **Audit flag lifecycle** – Live wiring audits generate HIGH‑ROI unwired flags and MED undocumented flags; stale/buggy flags persist until an explicit refresh step is run (e.g., `node script`) [2].  
- **Modular PDF corpus crossover** – The FOXTROT‑LIMA crossover integrates newly‑ingested PDFs into the mill’s reference set, enabling rapid iteration cycles on documentation and model grounding [6].

## Key decisions & rules
| Decision | Rule (applied across the mill) |
|----------|--------------------------------|
| **Treat HIGH‑ROI unwired flags as actionable** | Prioritize any flag flagged “Top HIGH‑ROI unwired” for immediate wiring; ignore MED undocumented flags until they are refreshed to avoid noise [2]. |
| **Standardize LoRA source mode** | When extending `vault-to-lora-dataset.mjs`, always invoke `--source galaxy` with the 512 advisory‑tagged Alpaca pairs to keep the cross‑galaxy signal consistent [1]. |
| **Canonical tooling selection** | Adopt the “flute‑helix” taxonomy from the tribal wiki as the default reference for any new tool‑selection decision; map legacy names onto this schema [4]. |
| **WEDM tactical composition** | Apply the three‑leaf pattern (wire + tension + flu) when configuring WEDM processes, as codified in the tribal release [5]. |
| **PDF corpus integration cadence** | Run the FOXTROT‑LIMA crossover after each batch of ≥100 new PDFs; verify manifest integrity before committing to the mill’s knowledge base [6]. |

## Open threads
- **Stale audit flag remediation** – The 15 MED undocumented flags remain buggy; a systematic re‑audit or automated cleanup script is needed (see live audit script reference) [2].
- **Remaining wiring coverage** – Mill galaxy is 89% wired; the path to full completion (the last 11%) lacks a concrete rollout plan.
- **LoRA signal validation** – How the injected LoRA training signal quantitatively improves downstream mill tasks has not been measured; an A/B test framework is pending.
- **Cross‑domain knowledge alignment** – Ensuring that insights from the tribal wikis (climb vs conv, flute‑helix, WEDM) stay synchronized with the PDF corpus updates remains an open coordination point.
