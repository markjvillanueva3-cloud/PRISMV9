---
name: wiring_synthesis
description: "[auto-synth · verify] Compounding synthesis of the wiring domain — recurring patterns, decisions, open threads distilled from 24 memories (LLM-generated; verify against source memories before trusting)"
metadata:
  type: patterns
  galaxy: wiring
  synthesizedFrom: 24
  model: gpt-oss:120b
  synthesizedAt: 2026-06-26T02:44:15.057Z
  sourceHash: e460ca0712fb
  advisoryOnly: true
  mustHumanVerify: true
---

# wiring — domain synthesis (compounding)

> ⚠ ADVISORY — LLM-generated (gpt-oss:120b), `mustHumanVerify`. Auto-distilled by
> `galaxy-reflection-synthesis.mjs` (B1) from 24 domain-relevant memories via the
> A6/A3 hybrid recall. Verify any rule here against its cited source memory before trusting
> it as fact (esp. safety-relevant thresholds). Regenerated on each run — edit the source
> memories, not this file. The compounding arm of the Obsidian brain.

## Recurring patterns
- **Slot‑enforced bootstrap wiring** – most shipped modules declare a `slot:romeo` (or `alpha`) and are gated by `[BOOTSTRAP-SLOT-ENFORCE]` (e.g., holder selection, wire‑fusion, MSA analysis) [1‑4],[9].
- **Holder‑centric pipelines** – the “HolderSelectionEngine” feeds real holders into downstream wires for Fusion, HyperMILL, and other CAM apps [1‑3],[4].
- **Audit & measurement integration** – audit entry consumer and MSA engine are wired as first‑class consumers of false‑positive entries and measurement data [5],[9].
- **Comprehensive semantic wiring verification** – full H‑drive to Obsidian vault mapping is validated as “VERIFIED” [8].
- **Iterative knowledge accretion** – successive DeepSource iterations (iter2 → iter9) expand the wiring galaxy’s reference pool [17],[18].

## Key decisions & rules
- **Do not wire for its own sake** – obsolete or speculative hooks must remain unwired [19].
- **Exhaustively enumerate endpoints** – every build must list *all* possible wiring consumers and viable combinations, never settle on the first obvious one [20].
- **Slot‑based ownership** – Romeo owns all `slot:romeo` holder‑related wires; Alpha owns audit‑related wires. Cross‑slot wiring is only allowed via explicit directives [1‑5],[9].
- **Tool‑holder library completeness** – Fusion holder libraries must include both names *and* collision geometry; missing CSV column was flagged as a blocker [24].
- **Baseline post‑processor hierarchy** – Hurco (mills) and Okuma LB3000/Multus B250IIW (turning) are the canonical baselines for all PRISM post‑processors [14].
- **Archive exclusion** – legacy JS archive (`PRISM_ARCHIVE_2026-02-01`) is explicitly prohibited from import or reliance [21].

## Open threads
- **Collision geometry gap in Fusion holder libs** – CSV conversion dropped `holder_segments`; need a fix to restore geometry data [24].
- **Docustrata index coverage** – 35k JMD PDFs remain un‑indexed/OCR’d, limiting searchable wiring documentation [22].
- **Synergy‑wiring completion** – only 2 of 4 hooks shipped; edge‑ordering lib and remaining per‑unit Obsidian memories still pending [15].
- **Audit false‑positive handling** – entry consumer wired but downstream impact on pruning obsolete wires is not fully documented [5].
- **Orphan hook policy enforcement** – practical guidelines needed to identify “orphan” hooks that should stay unwired per rule [19].
