---
name: dormant-data_synthesis
description: "[auto-synth · verify] Compounding synthesis of the dormant-data domain — recurring patterns, decisions, open threads distilled from 24 memories (LLM-generated; verify against source memories before trusting)"
metadata:
  type: patterns
  galaxy: dormant-data
  synthesizedFrom: 24
  model: gpt-oss:120b
  synthesizedAt: 2026-06-27T16:57:33.007Z
  sourceHash: def3247af0b5
  advisoryOnly: true
  mustHumanVerify: true
---

# dormant-data — domain synthesis (compounding)

> ⚠ ADVISORY — LLM-generated (gpt-oss:120b), `mustHumanVerify`. Auto-distilled by
> `galaxy-reflection-synthesis.mjs` (B1) from 24 domain-relevant memories via the
> A6/A3 hybrid recall. Verify any rule here against its cited source memory before trusting
> it as fact (esp. safety-relevant thresholds). Regenerated on each run — edit the source
> memories, not this file. The compounding arm of the Obsidian brain.

## Recurring patterns
- **Iterative knowledge accretion** – a steady series of “dormant‑data” iterations (1, 2, 4‑8, 10‑25) where Hermes drafts next‑layer reputable‑source research, each iteration building on the previous mastery level.  
- **Automated repository sync** – H‑drive subfolders (`archives`, `plans‑archive`, `skills‑archived`) are regenerated by `scripts/h-drive-to-vault.mjs` on demand ([reference_hdrive_h-prism-subdirs-archives], [reference_hdrive_h-prism-subdirs-plans-archive], [reference_hdrive_h-prism-subdirs-skills-archived]).  
- **Baseline definition for tooling** – post‑processor baselines (Hurco for mills, Okuma LB3000/Multus B250IIW for turning) are codified once and reused across pipelines ([reference_post_processor_fleet_baselines_2026_05_25]).  
- **Data‑gap detection & remediation** – critical mismatches such as the un‑indexed JMD PDFs in Docustrata trigger operator challenges and corrective actions ([reference_docustrata_index_misses_jmd_folders_2026_06_12]).  
- **Lock‑state recovery pattern** – a specific dead‑lock on `.brain-refresh.lock` was identified, reclaimed, and committed as a fix to keep all brain‑refresh pipelines alive ([reference_brain_lock_reclaim_2026_06_09]).

## Key decisions & rules
- **Never import outdated archives** – the `PRISM_ARCHIVE_2026-02-01` JS bundle is marked obsolete and must be excluded from any production load ([project_archive_outdated]).  
- **Refresh H‑drive vaults before any data pull** – re‑run `scripts/h-drive-to-vault.mjs` to guarantee the latest file set for each subdirectory.  
- **Maintain baseline consistency** – all new post‑processor scripts must reference the established Hurco/Okuma baselines unless a formal revision directive is issued.  
- **Index completeness rule** – any PDF collection (e.g., JMD quotes/orders) must be represented in the Docustrata `.index` and OCR‑processed before being considered searchable.  
- **Lock health monitoring** – after the lock‑reclaim fix, implement periodic checks on `.brain-refresh.lock` to preempt future dead‑locks.  

## Open threads
- **Docustrata coverage** – devise a pipeline to ingest and OCR the 35 k+ JMD PDFs and update the `.index` (see [reference_docustrata_index_misses_jmd_folders_2026_06_12]).  
- **Scaling iteration insights** – translate the accumulated expertise from iterations 21‑25 into concrete PRISM modules or APIs; current drafts stop at “design PRISM‑scale” without implementation details ([reference_dormant-data_iter25_deepsource_2026_06_14]).  
- **Refresh cadence** – decide on an automated schedule (e.g., nightly) for running `h-drive-to-vault.mjs` to keep all repo subdirs current.  
- **Future lock safeguards** – evaluate whether additional redundancy or a watchdog service is needed beyond the one‑off reclaim commit ([reference_brain_lock_reclaim_2026_06_09]).
