---
name: discovery_synthesis
description: "[auto-synth · verify] Compounding synthesis of the discovery domain — recurring patterns, decisions, open threads distilled from 12 memories (LLM-generated; verify against source memories before trusting)"
metadata:
  type: patterns
  galaxy: discovery
  synthesizedFrom: 12
  model: gpt-oss:120b
  synthesizedAt: 2026-06-11T01:47:21.961Z
  sourceHash: 8fdadc0978b2
  advisoryOnly: true
  mustHumanVerify: true
---

# discovery — domain synthesis (compounding)

> ⚠ ADVISORY — LLM-generated (gpt-oss:120b), `mustHumanVerify`. Auto-distilled by
> `galaxy-reflection-synthesis.mjs` (B1) from 12 domain-relevant memories via the
> A6/A3 hybrid recall. Verify any rule here against its cited source memory before trusting
> it as fact (esp. safety-relevant thresholds). Regenerated on each run — edit the source
> memories, not this file. The compounding arm of the Obsidian brain.

## Recurring patterns
- **Per‑galaxy compounding pipeline** – each galaxy produces a `*_synthesis.md` (the “compounding arm”) that is later consumed by the “consumer arm” (`AMP‑CONSUME`) and wired into the shared semantic recall cache.  [5], [8], [6]  
- **Cross‑galaxy wiring** – all 34 galaxy brains are linked into a single RAG/GNN layer, enabling fleet‑wide model sharing and closed‑loop audits. [11], [1]  
- **Auto‑distillation of sparse memories** – after each ship (e.g., `GALAXY‑CONTEXT‑FILL`, `U‑GE‑DEEPDOMAIN‑B2`) the system extracts 20–30 grounded domain memories and writes them to the canonical four‑section brain structure. [2], [9], [12]  
- **Continuous refresh loop** – a scheduled “Brain Refresh” task is intended to regenerate synthesis files after each new ingest, but it currently fails silently on some hosts. [10]  
- **Discovery‑phase documentation rule** – operators are instructed to log durable domain memories *during* the DISCOVER phase rather than only at close‑out. [3]

## Key decisions & rules
- **Operator directive (2026‑05‑29):** When a slot enters a DISCOVER phase, write durable domain memories immediately. [3]  
- **Compounding architecture choice:** Separate “compounding” (B1) and “consumer” (AMP‑CONSUME) arms to modularize synthesis → recall → downstream usage flow. [5], [8]  
- **Fleet integration milestone:** All 12 domain galaxies are now wired to the master brain with a closed‑loop adoption audit, establishing a shared model layer for discovery data. [11]  
- **Canonical memory format enforcement:** Every galaxy must have a four‑section `MEMORY.md`; reviewers flagged inflated corpora and a self‑defeating RED test, prompting stricter validation. [7]  

## Open threads
- **Brain Refresh registration gap:** The auto‑refresh task is unregistered on at least one host, causing synthesis files to “rot” unnoticed. [10]  
- **Incomplete galaxy memories:** Some galaxies still lack full file paths, wikis, or other assets despite the canonical template requirement. [7]  
- **Unverified deep‑domain packets:** The five research packets from `U‑GE‑DEEPDOMAIN‑B2` remain owner‑gated and unverified, limiting their utility for discovery insights. [9]  
- **RED test failure remediation:** A self‑defeating RED test was discovered during memory filling; resolution steps are pending. [7]  
- **Ongoing validation of auto‑distilled learnings:** New shipments (e.g., `DOMAIN‑GALAXY‑DOCTRINE‑MS1`) continue to produce distilled memories that need peer review before they become part of the compounding corpus. [12]
