---
name: cad_synthesis
description: "[auto-synth · verify] Compounding synthesis of the cad domain — recurring patterns, decisions, open threads distilled from 24 memories (LLM-generated; verify against source memories before trusting)"
metadata:
  type: patterns
  galaxy: cad
  synthesizedFrom: 24
  model: gpt-oss:120b
  synthesizedAt: 2026-06-11T04:46:58.417Z
  sourceHash: 9fefd18c3cf9
  advisoryOnly: true
  mustHumanVerify: true
---

# cad — domain synthesis (compounding)

> ⚠ ADVISORY — LLM-generated (gpt-oss:120b), `mustHumanVerify`. Auto-distilled by
> `galaxy-reflection-synthesis.mjs` (B1) from 24 domain-relevant memories via the
> A6/A3 hybrid recall. Verify any rule here against its cited source memory before trusting
> it as fact (esp. safety-relevant thresholds). Regenerated on each run — edit the source
> memories, not this file. The compounding arm of the Obsidian brain.

## Recurring patterns
- **Per‑slot modular buildouts** – each functional area (Xray, Echo, etc.) is encapsulated in a `PER‑SLOT‑GALAXY‑BUILDOUT` unit that ships independently and follows the same bootstrap/enforce workflow [1],[4],[5],[6],[7],[18‑24].  
- **Bootstrap‑slot‑enforce** as a shared scaffolding layer for wiring, audits, and static checks across all slots [15]–[24].  
- **Closed‑loop integration & audit** – post‑ship steps that verify data paths, coverage, and linting (e.g., `U‑ECHO‑CLOSED‑LOOP‑AUDIT`, `U‑ECHO‑COVERAGE‑AUDIT`) [19]–[21].  
- **Supervised training pipeline for CAD** – labels are generated from CAD + CNC programs; downstream components include page classification, render‑timeout tuning, and train‑set curation [4],[5],[6],[7].  
- **Deterministic resource allocation** per slot (RGS allocator) to avoid race conditions [12].  
- **Auto‑release & stop‑time advisory** after claim steps to enforce atomicity [13]–[14].  
- **Feature‑recognition restoration** via stub‑hunt modules, re‑enabling CAD feature extraction in legacy slots [8].

## Key decisions & rules
- **Slot‑specific branch naming** – developers must commit to their own NATO‑named slot branch (e.g., `slot/charlie`) rather than the shared live tree [9].  
- **Master‑Brain‑Template compliance** is required for any new CAD galaxy buildout, ensuring a uniform schema across slots [2].  
- **4‑digit‑PN blindspot fix** applied to Xray slot to capture previously missed part numbers [1].  
- **Deterministic RGS allocation** replaces ad‑hoc scheduling, guaranteeing 20/20 audit pass for claim integration [12]–[14].  
- **Static NC dialect linter + auto‑guard hook** introduced in `U‑ECHO‑NCLINT` to enforce code quality before fusion [24].  
- **Wire meta‑bus into additional domains** (India wire extended to 9 non‑hybrid slots) for cross‑slot communication [15]–[16].

## Open threads
- **Scalability of Xray render timeout** – current bump improves PDF rendering speed, but long‑term limits and impact on downstream classifiers remain untested [6].  
- **Cross‑slot feature‑recognition consistency** – stub‑hunt restoration succeeded for slot `bravo`, yet integration with other legacy slots is pending.  
- **Coverage audit expansion** – `U‑ECHO‑COVERAGE‑AUDIT` performed on initial set; full galaxy‑wide coverage metrics are still open.  
- **Training set curation pipeline** – `U‑Xray‑TRAINSET‑CURATE` established, but automation of label quality checks and incremental updates need definition [7].  
- **Meta‑bus wiring beyond India domain** – plan to extend the bus to remaining slots; design specifications and conflict resolution strategies are under discussion.
