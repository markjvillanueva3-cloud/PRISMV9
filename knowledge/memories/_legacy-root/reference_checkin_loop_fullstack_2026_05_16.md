---
name: checkin-loop-fullstack-2026-05-16
description: "Doctrine pointer naming the /checkin-<nato> /loop full-stack contract. Slot worktree + per-iter inject + ROADMAP-CONSOLIDATED pickup + error-learn + slot-routed commits, all live, all autonomous."
metadata:  
source: prism-memory
synced: 2026-05-18T01:02:09.235Z
aliases: reference_checkin_loop_fullstack_2026_05_16
---


# /checkin-<nato> /loop full-stack contract (2026-05-16, slot juliett)

User's vision: typing `/checkin-alpha /loop <task>` is the ONLY command needed
to spin up a slot-isolated, dev-tool-saturated, autonomous PRISM session.

## Status: ALREADY LIVE

Every piece is on disk and wired. This session's contribution is the
**doctrine pointer** in `CLAUDE.md` + this wiki entry naming the contract
end-to-end so chats know it works.

## What was already built (across many milestones)

- 12 `/checkin-<nato>` slot skills (alpha..lima; golf = integrator).
- SLOT-WORKTREE-MS0 — 11 `prism-slot-*` worktrees; activation peer-shipped
  2026-05-16 (`b8dfbf208 + 912f10fff`, "0 pipeline wrappers needed" per
  [[reference_slot_worktree_activation_2026_05_16]]).
- 3 default-on routing hooks: `main-tree-write-block`, `git-add-lane-guard`
  (case-insensitive after this session's fix), `worktree-commit-route`.
- Per-iter injection: `master-index-precheck-inject`, `wiki-precheck-inject`,
  `memory-relevance-inject`, `tribal-by-domain-inject`,
  `ollama-pipeline-injector` + prewarm, `comprehensive-build-enforce`.
- Subagent per-task pre-search ([[reference_subagent_per_task_presearch_2026_05_15]]).
- Error-learn loop: `error-pattern-capture`/`error-block-prewarn`/
  `error-learn-store` + Qdrant similarity.
- ROADMAP-CONSOLIDATED.json (this session) — the 5826-item master pickup set.
- /loop autonomous mode + Step 12 + 2b loop-resume
  ([[reference_checkin_autonomous_loop_2026_05_16]]).
- /compact auto-handoff + auto-resume across compact
  ([[reference_precompact_hook_autowrite_2026_05_15]],
  [[reference_session_continuity_stack_2026_05_15]]).

## What this session contributed

- `CLAUDE.md` doctrine section `## /checkin-<nato> /loop <task> — full-stack dev pipeline contract` — the single pointer that names the contract end-to-end.
- `knowledge/wiki/architecture/checkin-loop-fullstack.md` — wiki entry with the 10-step fire-order.
- This memory.

Deliberately NOT done: pick-unit modification to read ROADMAP-CONSOLIDATED
directly. The atomic-roadmap is a high-contention shared artifact; the
ROADMAP-CONSOLIDATED inventory is discoverable from the doctrine pointer +
MEMORY.md, which is enough for chats to pick from it manually or for a
future `/pick-bridge` skill that targets `bridge_units` specifically.

## Durable lesson

Many pieces were already built. The HIGHEST LEVERAGE move was naming the
contract — adding the doctrine pointer — not building more code. Before
building anything in the "/checkin-<nato> /loop" surface area, READ the
doctrine pointer in CLAUDE.md + this wiki + check what peers have already
shipped (MEMORY.md "Indexed memories" list).

Wiki: [[checkin-loop-fullstack]]. Companions:
[[roadmap-consolidation-2026-05-16]], [[misc-tasks-extraction-2026-05-16]],
[[reference_slot_worktree_activation_2026_05_16]].


## Related
[[skills/checkin-|/checkin-]] • [[skills/loop|/loop]] • [[skills/checkin-alpha|/checkin-alpha]] • [[skills/compact|/compact]] • [[skills/wiki|/wiki]] • [[skills/architecture|/architecture]] • [[skills/checkin-loop-fullstack|/checkin-loop-fullstack]] • [[skills/pick-bridge|/pick-bridge]]