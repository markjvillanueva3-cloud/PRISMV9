---
name: reference_code_engine_ppcannedcyclevalidatorengine
description: "Validate G81-G89 canned cycles"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PPCannedCycleValidatorEngine.ts
  sourceHash: a8eda2f6
  loc: 346
aliases: PPCannedCycleValidatorEngine
---

# PPCannedCycleValidatorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PPCannedCycleValidatorEngine.ts` (346 LOC)

## What it is
Validate G81-G89 canned cycles

```
PPCannedCycleValidatorEngine — Validate G81-G89 canned cycles

Canned cycles (drilling, tapping, boring) are compressed programs where
one wrong word produces a broken tap or a crashed tool. Controllers
accept them without complaint; the machine finds out later.

Cycles validated:
  G80 — cycle cancel
  G81 — simple drill (no dwell)
  G82 — drill with dwell (counterbore/spot face)
  G83 — peck drill (deep hole)
  G84 — tapping (right-hand)
  G85 — boring (rapid out)
  G86 — boring with spindle stop
  G87 — back boring
  G88 — boring with manual return
  G89 — boring with dwell

Checks:
  - missing_r_plane (error): canned cycle without R-word. R = retract
    plane; omitting it crashes through the part.
  - z_above_r_plane (error): Z >= R. Means the tool "drills" upward —
    programmer mixed up signs or incremental/absolute mode.
  - missing_peck_q (error): G83 without Q-word (peck depth
```


## Exports
`CycleSeverity` (type), `CannedCycleCode` (type), `CannedCycleIssue` (interface), `CannedCycleResult` (interface), `CannedCycleOptions` (interface), `PPCannedCycleValidatorEngine` (class), `ppCannedCycleValidatorEngine` (const)

## Key methods
`validate`, `quickCheck`, `defaultOptions`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PPCannedCycleValidatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PPCannedCycleValidatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PPCannedCycleValidatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
