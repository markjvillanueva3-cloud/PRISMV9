---
name: reference_code_engine_camfeedbackloopengine
description: "CAM-EXHAUST-MS0/U-CAM120"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMFeedbackLoopEngine.ts
  sourceHash: a9b9d14a
  loc: 770
aliases: CAMFeedbackLoopEngine
---

# CAMFeedbackLoopEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMFeedbackLoopEngine.ts` (770 LOC)

## What it is
CAM-EXHAUST-MS0/U-CAM120

```
CAMFeedbackLoopEngine — CAM-EXHAUST-MS0/U-CAM120

Continuous-learning feedback collection for CAM AGI decisions. Distinct
from existing engines:
  - CAMMLDriftMonitorEngine — watches MAE drift on a held-out test set;
    this engine watches accuracy drift on LIVE production decisions.
  - FeedbackCollectorEngine — shop-floor thumbs-up/down on whole NC
    programs; this engine captures fine-grained AI decision overrides
    (operator changed original strategy/parameter/tool to a different
    one — that pair becomes a training signal).
  - CAMConfidenceCalibrationEngine (U-CAM119) — consumes outcomes to
    calibrate confidence; this engine collects the outcomes and
    surfaces drift / patterns / training pairs.

Capabilities:
  - recordCorrection(...)        — operator overrode the AI's pick. Stored
                                   as a training pair (original, corrected,
           
```


## Exports
`CorrectionRecord` (interface), `OutcomeRecord` (interface), `DriftVerdict` (type), `AccuracyDriftReport` (interface), `CorrectionPattern` (interface), `CorrectionPatternsReport` (interface), `LoRATrainingPair` (interface), `FeedbackStats` (interface), `RecordCorrectionInput` (interface), `RecordOutcomeInput` (interface), `DriftOptions` (interface), `PatternsOptions` (interface), `LoRAExportOptions` (interface), `CAMFeedbackLoopEngine` (class), `mannKendall` (function), `ordinarySlope` (function), `camFeedbackLoopEngine` (const)

## Key methods
`setBufferCap`, `clearAll`, `recordCorrection`, `recordCorrectionFromDecision`, `recordOutcome`, `getCorrections`, `accuracyDrift`, `correctionPatterns`, `loraTrainingExport`, `feedbackStats`

## Depends on
[[reference_code_engine_camdeeplearningorchestratorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CAMFeedbackLoopEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMFeedbackLoopEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMFeedbackLoopEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
