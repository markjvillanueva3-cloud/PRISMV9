---
name: reference_code_engine_wedmautonomyengine
description: "SAE J3016-adapted autonomy level management for WEDM."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WEDMAutonomyEngine.ts
  sourceHash: 30b9038e
  loc: 327
aliases: WEDMAutonomyEngine
---

# WEDMAutonomyEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WEDMAutonomyEngine.ts` (327 LOC)

## What it is
SAE J3016-adapted autonomy level management for WEDM.

```
WEDMAutonomyEngine — SAE J3016-adapted autonomy level management for WEDM.

Phase 4 / P4-MS1 / U-P4-01 of the WEDM AGI Intelligence Roadmap.

Adapts the SAE J3016 taxonomy (originally for on-road automated driving
systems) to wire-EDM machining. The six-level ladder (L0 Manual → L5
Self-improving) defines — for each level — what the AI is allowed to do
without human confirmation and what the human must still own. The engine
is the single gatekeeper that downstream components consult before
acting; no other module should hard-code level rules.

Key properties:
  - **Level-aware permission checks** — `can(action)` returns true iff the
    current level authorises `action` (param suggest vs. auto-adjust vs.
    unattended run vs. autonomous optimisation).
  - **One-step transitions** — operators can only step up or down by ±1
    level per request (gate-review pattern). Direct jumps (e.g. L
```


## Exports
`AutonomyLevel` (type), `AUTONOMY_LEVEL_NAMES` (const), `AUTONOMY_LEVEL_HUMAN_ROLE` (const), `AutonomyCapability` (type), `CAPABILITY_MIN_LEVEL` (const), `AutonomyTransition` (interface), `AutonomySnapshot` (interface), `PromoteOptions` (interface), `DegradeOptions` (interface), `DEFAULT_STARTING_LEVEL` (const), `MAX_LEVEL` (const), `MIN_LEVEL` (const), `WEDMAutonomyEngine` (class), `wedmAutonomyEngine` (const)

## Key methods
`getLevel`, `getName`, `getHumanRole`, `can`, `snapshot`, `promote`, `demote`, `degrade`, `reset`, `load`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WEDMAutonomyEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WEDMAutonomyEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WEDMAutonomyEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
