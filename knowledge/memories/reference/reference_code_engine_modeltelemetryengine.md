---
name: reference_code_engine_modeltelemetryengine
description: "INTEL-OLLAMA-OBSIDIAN-MS0 / P23-U01"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ModelTelemetryEngine.ts
  sourceHash: b346f29a
  loc: 520
aliases: ModelTelemetryEngine
---

# ModelTelemetryEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ModelTelemetryEngine.ts` (520 LOC)

## What it is
INTEL-OLLAMA-OBSIDIAN-MS0 / P23-U01

```
ModelTelemetryEngine — INTEL-OLLAMA-OBSIDIAN-MS0 / P23-U01

Per-call telemetry logger for LLM/Ollama invocations. Captures
`{ts, model, backend, taskKind, promptTokens, completionTokens, latencyMs, outcome, errorBrief}`
per call and persists to a JSONL store on disk. Read-side surfaces
(`getRecentCalls`, `getStats`) feed the adaptive-routing tuner
(P23-U02, `scripts/adapt-router-thresholds.mjs`) that periodically
adjusts `ModelRoutingEngine` thresholds based on observed reality.

Design rules:
  1. Sync I/O on the hot path. Logging a call MUST NOT introduce
     async coupling — many callers (hooks, sync dispatchers) cannot
     await. `appendFileSync` is intentional.
  2. JSONL with one record per line, append-only. Rotation at 50 MiB
     (mirrors `HookLatencyEngine` pattern) so a runaway logger never
     eats disk.
  3. Pure read methods are mtime-agnostic — the file is small enough

```


## Exports
`ModelTelemetryOutcome` (type), `ModelTelemetryEntry` (interface), `LogCallInput` (type), `ModelStats` (interface), `ModelTelemetryStats` (interface), `ModelTelemetryEngineOptions` (interface), `UNRESOLVED_JSONL_SENTINEL` (const), `ModelTelemetryEngine` (class), `modelTelemetryEngine` (const)

## Key methods
`getStorePath`, `logCall`, `getRecentCalls`, `getStats`, `purgeOlderThan`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ModelTelemetryEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ModelTelemetryEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ModelTelemetryEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
