---
name: reference_code_script_ollamaprismbridge
description: "ollama-prism-bridge.mjs — Ollama → PRISM agentic harness (OLLAMA-EXPAND-MS0/U-OE-BRIDGE-L2)"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/ollama-prism-bridge.mjs
  sourceHash: 2cf835e6
  loc: 1808
aliases: ollama-prism-bridge.mjs
---

# ollama-prism-bridge.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/ollama-prism-bridge.mjs` (1808 LOC)

## What it is
```
ollama-prism-bridge.mjs — Ollama → PRISM agentic harness (OLLAMA-EXPAND-MS0/U-OE-BRIDGE-L2)

Layer 2 of the OLLAMA→PRISM-MCP bridge ladder (Layer 1 = ask-ollama.mjs).

Ollama is a model server, NOT an MCP client — it cannot "connect to MCP".
This script is the missing harness: it acts as the agent loop. It advertises
a curated, READ-ONLY set of PRISM knowledge tools to an Ollama model via the
/api/chat `tools` parameter, then runs the call → execute → feed-back loop
until the model produces a final answer. Ollama autonomously decides which
tool to use, like Claude Code does — but every step runs locally, so a
multi-step "where is X / how does Y work / what wires to Z" investigation
costs ~0 Claude tokens. Only the final compact answer returns.

Tool surface (read-only by construction — no tool can mutate state or write):
  viz_search    ranked keyword search of the ~27 MB system-viz grap
```


## Exports
`TOOL_NAMES` (const), `mcpAllowlist` (function), `mcpDenylist` (function), `validateMcpCall` (function), `parseArgs` (function), `pickModel` (function), `toolSpecs` (function), `buildSystemPrompt` (function), `normalizeToolCall` (function), `validateToolCall` (function), `clampToolArgs` (function), `confinePath` (function), `capToolResult` (function), `toolResultMessage` (function), `renderTranscript` (function), `bridgeSavingsFooter` (function), `renderMcpResult` (function), `listWikiLeafFiles` (function), `scoreLeafFilenames` (function), `listObsidianMemoryFiles` (function)

## Key methods
`walk`, `clearTimeout`, `embedViaOllamaAttempt`, `viz_search`, `wiki_lookup`, `read_excerpt`, `dispatcher_map`, `semantic_search`, `obsidian_lookup`, `mcp_call`, `bridgeSavingsFooter`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/ollama-prism-bridge.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find ollama-prism-bridge.mjs` or `prism_session:master_index_query`; open the source at `scripts/ollama-prism-bridge.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
