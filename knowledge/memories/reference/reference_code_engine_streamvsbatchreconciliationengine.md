---
name: reference_code_engine_streamvsbatchreconciliationengine
description: "U-LEARN-02"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/StreamVsBatchReconciliationEngine.ts
  sourceHash: 00069cac
  loc: 342
aliases: StreamVsBatchReconciliationEngine
---

# StreamVsBatchReconciliationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/StreamVsBatchReconciliationEngine.ts` (342 LOC)

## What it is
U-LEARN-02

```
StreamVsBatchReconciliationEngine — U-LEARN-02
================================================

Feature-store MLOps safety net: detects when the ONLINE path (features
computed at decision time) disagrees with the OFFLINE path (features
later reconstructed from the immutable FeatureStore) — the classic
train/serve skew that silently breaks production ML.

Every time a recommendation is served:
  1. Online feature values are passed in as `online_values` (from the
     live feature builder attached to the dispatcher action).
  2. Engine looks up the AS-OF feature row for the same entity at the
     decision timestamp via FeatureStoreEngine.getHistoricalFeatures().
  3. Per-key comparison against the feature contract's tolerance
     (rel_tolerance + abs_tolerance).
  4. Verdict: aligned | drift | missing_offline | missing_online.

Results append to state/reconciliation/<domain>/<feature_gr
```


## Exports
`ReconVerdict` (type), `KeyComparison` (interface), `ReconcileInput` (interface), `ReconcileResult` (interface), `StatsResult` (interface), `StreamVsBatchReconciliationEngine` (class), `streamVsBatchReconciliationEngine` (const)

## Key methods
`reconcile`, `query`, `stats`, `getSelfAwareness`

## Depends on
[[reference_code_engine_featurestoreengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/StreamVsBatchReconciliationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find StreamVsBatchReconciliationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/StreamVsBatchReconciliationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
