---
name: reference_code_engine_camconfidencecalibrationengine
description: "CAM-EXHAUST-MS0/U-CAM119"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMConfidenceCalibrationEngine.ts
  sourceHash: 396926dd
  loc: 650
aliases: CAMConfidenceCalibrationEngine
---

# CAMConfidenceCalibrationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMConfidenceCalibrationEngine.ts` (650 LOC)

## What it is
CAM-EXHAUST-MS0/U-CAM119

```
CAMConfidenceCalibrationEngine — CAM-EXHAUST-MS0/U-CAM119

Decision-confidence calibration & uncertainty quantification for CAM AGI
decisions. Wraps the orchestrator's raw composite confidence into a
calibrated probability that the decision will actually be correct in
production, plus an uncertainty interval. Distinct from the existing
PredictionCalibrationEngine (Bayesian Kienzle/Taylor coefficient fitting)
and CalibrationEngine (Levenberg-Marquardt physics-model fitting).

Methods (selectable per call, or auto-recommended by recommendMethod()):

  1. histogram      — equal-width bins over [0,1]; calibrated = empirical
                       accuracy in the matching bin. Robust, low-data
                       baseline. Default.
  2. isotonic       — pool-adjacent-violators (PAV) algorithm enforces a
                       monotonic raw→calibrated mapping. Non-parametric
               
```


## Exports
`CalibrationMethod` (type), `CalibrationOutcome` (interface), `ReliabilityBin` (interface), `CalibrationMetrics` (interface), `CalibrateResult` (interface), `CalibrationOptions` (interface), `CAMConfidenceCalibrationEngine` (class), `camConfidenceCalibrationEngine` (const)

## Key methods
`setOutcomeCap`, `clearOutcomes`, `getOutcomeCount`, `recordOutcome`, `calibrate`, `calibrateDecision`, `metrics`, `recommendMethod`

## Depends on
[[reference_code_engine_camdeeplearningorchestratorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CAMConfidenceCalibrationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMConfidenceCalibrationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMConfidenceCalibrationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
