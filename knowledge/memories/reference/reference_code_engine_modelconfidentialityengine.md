---
name: reference_code_engine_modelconfidentialityengine
description: "Model Confidentiality"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ModelConfidentialityEngine.ts
  sourceHash: bc6fb883
  loc: 701
aliases: ModelConfidentialityEngine
---

# ModelConfidentialityEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ModelConfidentialityEngine.ts` (701 LOC)

## What it is
Model Confidentiality

```
ModelConfidentialityEngine (U-LPR-SEC10)

Protects LoRA adapters and base weights from exfiltration and unauthorized
disclosure. Five pillars per the Phase-9 plan:

  1. Trigger-set watermark — Adi-Shamir black-box fingerprint: register a
     secret (prompt, expected_output_hash) challenge set at training time,
     verify later against a suspect model. Detects stolen weights even when
     attacker re-exports under different file hash.
     Reference: Adi et al., "Turning Your Weakness Into a Strength:
     Watermarking Deep Neural Networks by Backdooring" (USENIX Security 2018).
  2. Egress DLP — scan outbound artifacts for `.safetensors` / `.gguf` /
     `.bin` magic headers and adapter_config.json signatures. Block or
     quarantine depending on policy. Per NIST SP 800-171 §3.13.10.
  3. 4-eyes approval — dual-approver workflow for weight-export requests.
     Two distinct human id
```


## Exports
`ArtifactKind` (type), `ExportDecision` (type), `ApprovalStatus` (type), `AccessGrantStatus` (type), `WatermarkTrigger` (interface), `WatermarkVerification` (interface), `EgressScanInput` (interface), `EgressScanResult` (interface), `ExportRequest` (interface), `ApprovalRecord` (interface), `AccessGrant` (interface), `AccessAuditEntry` (interface), `ModelConfidentialityStats` (interface), `ModelConfidentialityEngine` (class), `modelConfidentialityEngine` (const)

## Key methods
`registerWatermarkTrigger`, `listWatermarkTriggers`, `verifyWatermark`, `scanEgress`, `listEgressScans`, `submitExportRequest`, `recordApproval`, `getExportRequest`, `listExportRequests`, `grantJITAccess`, `revokeJITAccess`, `checkAccess`, `listAccessGrants`, `recordAccess`, `listAuditEntries`, `verifyChain`, `getStats`, `clearAll`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ModelConfidentialityEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ModelConfidentialityEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ModelConfidentialityEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
