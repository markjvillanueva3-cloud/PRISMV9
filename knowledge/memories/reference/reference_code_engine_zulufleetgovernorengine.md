---
name: reference_code_engine_zulufleetgovernorengine
description: "Pure-core authority gate. Given a (slot, soul, task_text) tuple, returns"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ZuluFleetGovernorEngine.ts
  sourceHash: 0caed13e
  loc: 144
aliases: ZuluFleetGovernorEngine
---

# ZuluFleetGovernorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ZuluFleetGovernorEngine.ts` (144 LOC)

## What it is
Pure-core authority gate. Given a (slot, soul, task_text) tuple, returns

```
ZuluFleetGovernorEngine — HZD-02 (HZP-DASH-MS0)

Pure-core authority gate. Given a (slot, soul, task_text) tuple, returns
{ authorized, reason } based on the slot's hermes_role / domain_filter /
refuse_list. The dashboard control server consults this BEFORE any
state-changing operation (assign / veto / promote-refuse). Keeps the
authority logic deterministic, testable, and free of side effects.

Authority rules:
  1. Refuse-list hit (task_text matches any refuse rule, substring-ci) → REJECT.
  2. Domain filter set + task_text matches → ACCEPT (positive authority).
  3. Domain filter set + task_text does NOT match → REJECT (out of role).
  4. No domain_filter on the soul → ACCEPT only if soul.hermes_role is
     explicitly "fleet-orchestrator" / "generalist"; else REJECT (a slot
     with no domain filter and no orchestrator role has no authority).

R12 fail-CLOSED: a malformed regex in d
```


## Exports
`AuthorityCheckRequestSchema` (const), `AuthorityCheckRequest` (type), `AuthorityVerdict` (interface), `ORCHESTRATOR_ROLES` (const), `ZuluFleetGovernorEngine` (class)

## Key methods
`checkAuthority`, `renderVerdict`

## Depends on
[[reference_code_engine_soulfrontmatterreaderengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/ZuluFleetGovernorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ZuluFleetGovernorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ZuluFleetGovernorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
