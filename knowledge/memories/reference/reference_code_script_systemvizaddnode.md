---
name: reference_code_script_systemvizaddnode
description: "system-viz-add-node.mjs — Incremental dashed-node staging for system-viz."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/system-viz-add-node.mjs
  sourceHash: cfa0daf8
  loc: 746
aliases: system-viz-add-node.mjs
---

# system-viz-add-node.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/system-viz-add-node.mjs` (746 LOC)

## What it is
```
system-viz-add-node.mjs — Incremental dashed-node staging for system-viz.

Spec: CLEANUP-MS0 / U-CLEANUP-C3

Bridges the ~100s latency between full system-viz regenerations
(`generate-system-viz.mjs` rebuilds the whole 20K-node graph from
scratch) by appending "dashed" provisional nodes to the live graph
as soon as new engines / dispatchers / skills are created. The next
full regen reconciles them with authoritative live state.

Three-tier design:
  1. ENQUEUE (always; idempotent against the graph) — append to
     staging/add-node-queue.jsonl as one JSONL row per node.
  2. FLUSH (rate-limited, default 60s) — splice queued nodes into
     system-graph.json, atomic write, truncate queue (also atomic).
  3. GUARD (PID file) — prevent concurrent flushes from racing on
     the 41MB graph (multi-chat safe). SIGINT/SIGTERM clean it up.

Idempotency: re-running with the same --label (or --id)
```


## Exports
`FLUSH_INTERVAL_MS` (const), `MAX_BATCH` (const), `MAX_QUEUE_BYTES` (const), `VALID_LAYERS` (const), `DEFAULT_LAYER` (const), `ENGINE_SUBGROUP` (const), `DASHED_STATUS` (const), `DASHED_SIZE` (const), `DASHED_COLOR` (const), `DEFAULT_SOURCE` (const), `SCRIPT_EXTENSIONS_RE` (const), `ID_VALIDATION_RE` (const), `vizDir` (function), `stagingDir` (function), `queuePath` (function), `lastFlushPath` (function), `pidFilePath` (function), `graphPath` (function), `onCommitPidPath` (function), `slugifyLabel` (function)

## Key methods
`validateRawId`, `atomicWriteText`, `atomicWriteJson`, `emit`, `appendQueue`, `releasePidLock`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/system-viz-add-node.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find system-viz-add-node.mjs` or `prism_session:master_index_query`; open the source at `scripts/system-viz-add-node.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
