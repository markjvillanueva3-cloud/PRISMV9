---
name: reference_ollama_mode_sufficiency_2026_06_25
description: "ask-ollama now prefers a WARM qwen2.5-coder:7b for summarize/explain (measured non-inferior to 32b by the LLM-judge ladder), ~4x less VRAM, + a demand-driven cheap-tier prime. The executor wiring the architecture memory had blocked is now SHIPPED for the 2 measured modes only."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.678Z
aliases: reference_ollama_mode_sufficiency_2026_06_25
---


**OLLAMA-MAX-UTIL mode-sufficiency** (slot:alpha, 2026-06-25, commits `U-ALPHA-OLLAMA-MODE-SUFFICIENCY` + `U-ALPHA-OLLAMA-CHEAP-PRIME`). The PAYOFF that [[reference_ollama_executor_selection_architecture_2026_06_25]] had R13-blocked: it named the exact unblock condition ("difficulty-stratified quality data WITHIN each mode + an LLM-judge metric") and the judged ladder ([[reference_ollama_llm_judge_metric_2026_06_25]], commit `48f1d1266c`) met it. So the executor change is the verdict HONORED, not reversed (R7).

**Measured finding (LLM-judge, n=3 hard tier):** for `summarize` + `explain`, **qwen2.5-coder:7b is NON-INFERIOR to 32b at every difficulty** -- tie on easy/medium, tie on hard-summarize (both ~0%, 32b buys nothing), WIN on hard-explain (7b 100% vs 32b 67%). So a warm 7b = equal-or-better quality at ~5GB vs ~20GB VRAM.

**Shipped:**
- `scripts/lib/ollama-mode-sufficiency.mjs` -- `loadedPreferenceForMode(mode, base)` PREPENDS the measured cheap floor (7b) for summarize/explain ONLY; `cheapFloorForMode(mode)` single-sources the floor. `ask-ollama.mjs` non-codegen branch consumes it. PURELY ADDITIVE + strict: a COLD 7b is skipped (no cold-load); base `OFFLOAD_LOADED_PREFERENCE` UNTOUCHED (the `ask-ollama.test.mjs:181` `!includes("7b")` gate stays green); codegen branch UNTOUCHED. **LIVE-PROVEN:** 7b+32b both warm -> `summarize --json` -> `model=qwen2.5-coder:7b` (was 32b). This is the answer to "32b always wins so the lever is dead" -- a PREPEND wins even alongside a warm 32b.
- `scripts/lib/ollama-cheap-tier-prime.mjs` -- demand-driven activation: after a measured-mode offload that cold-loaded 32b, a detached/rate-limited(10min)/fail-soft `curl /api/generate` warms 7b so the NEXT one rides it (no blanket cron -> no wasted VRAM with no traffic). Kill switch `PRISM_OLLAMA_CHEAP_PRIME_DISABLE=1`. **FULL LOOP PROVEN LIVE (R15):** unloaded 7b (keep_alive:0) -> 1st `ask-ollama summarize --json` picked 32b (7b cold) + FIRED the prime -> 6s later 7b RESIDENT (32b evicted) -> 2nd `summarize --json` -> `model=qwen2.5-coder:7b`. The complete demand-driven chain (cold -> miss-and-prime -> warm -> ride) works end-to-end. **Caveat (R12, now characterized):** the warm is contention-SENSITIVE -- it TIMED OUT at 60s mid-session when a peer (india GPU training) spiked the card, then warmed in 3.7s once util dropped to 5%. So the lever fires whenever the GPU has headroom and fail-soft-degrades to the prior big-first under transient contention (stamp-on-spawn avoids piling warms onto a busy card). Not a code defect -- Ollama scheduling/contention, exactly what fail-soft is for.

**PRODUCTION-REACH VALIDATED (R15, no bypass) + a coherence win:** audited every caller of `ask-ollama summarize/explain` for an explicit `--model` that would bypass the new loaded-first downshift (explicit `--model` wins). NONE on the offload path: `ollama-task-offloader.mjs:435` emits `node scripts/ask-ollama.mjs ${mode} <file>` with NO `--model`; `trigger-command-pipeline.mjs` adds `--model` only when a step pins one. So the auto-offload path correctly uses the new downshift. COHERENCE FINDING: the manual skills `/ollama-summarize` + `/ollama-explain` (`~/.claude/commands/`) ALREADY hardcode `qwen2.5-coder:7b` for these exact modes (independent corroboration that 7b is right) -- but the AUTO-offloader fell to 32b for the IDENTICAL modes (OFFLOAD_LOADED_PREFERENCE excluded 7b). My change RESOLVES that inconsistency (R7): manual + auto now agree on 7b for summarize/explain. **Follow-up (separable, has a UX decision):** those skills call Ollama DIRECTLY via curl (hardcoded model, `head -c 8000`, fixed terse prompt) -> they bypass ask-ollama's NC-safety guard + warmth-awareness + offload telemetry. Routing them THROUGH ask-ollama would add all three but CHANGES the output format (ask-ollama has its own per-mode prompt), so it needs a UX call (preserve the terse skill format vs adopt ask-ollama's) -- not a clean drop-in, deferred.

**Scope (R12):** only summarize+explain were judged, so only they downshift. codegen/triage/viz/ask/rerank keep the big-first preference (no quality data -- extend `MODE_MIN_SUFFICIENT` only when a mode gets the same judged treatment). FLEET-WIDE by construction: `ask-ollama` is the single shared offload executor all 26 slots invoke, so every galaxy benefits from one change (R15 apply-to-all satisfied, no cloning). Tests: 11/11 sufficiency + 10/10 prime + 63/63 ask-ollama. Stale doc found: CLAUDE.md TOKEN ECONOMY says "the :7b/:14b tags were retired 2026-06-04" but `/api/tags` shows 7b/14b/1.5b ALL installed + working -- the note is wrong (surfaced, not yet corrected -- CLAUDE.md edits are golf-gated). Doc: `state/shared/ollama-generative-stratified-2026-06-25.md`. [[feedback_utilize_ollama_for_efficiency]]
