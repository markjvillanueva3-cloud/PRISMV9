---
name: reference_code_script_hermesvaultdigest
description: "hermes-vault-digest.mjs -- Hermes + Obsidian vault COMBO."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/hermes-vault-digest.mjs
  sourceHash: be35b6ff
  loc: 164
aliases: hermes-vault-digest.mjs
---

# hermes-vault-digest.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/hermes-vault-digest.mjs` (164 LOC)

## What it is
```
hermes-vault-digest.mjs -- Hermes + Obsidian vault COMBO.

Synthesizes the most-recent PRISM knowledge-vault notes (optionally filtered by --query) through
Hermes (xAI Grok, via `ask-hermes summarize -`) and writes the digest back into the vault's
`knowledge/hermes-outputs/` lane. Two utilizations rise at once:
  - Hermes: the synthesis runs through `ask-hermes`, which RECORDS the offload telemetry -> the
    MEASURED hermes utilization (reconcile-zulu-ledger gradeHermesUtilization) actually moves.
  - Obsidian vault: a fresh synthesized digest lands in the vault (searchable + auto-fed on Stop).
It spends ZERO Claude tokens (Grok does the synthesis) and needs neither the Nous desktop app nor
the :3100 stack -- only the :8645 proxy that ask-hermes already targets. Cron-wireable for a
recurring "vault brief" (the PRISM-side analog of the Nous-Hermes daily-shop-brief).

CLI: node scripts/he
```


## Exports
`slugify` (function), `filterByQuery` (function), `selectNotes` (function), `buildDigestInput` (function), `digestOutputPath` (function), `digestFrontmatter` (function), `stripFooter` (function), `main` (function)

## Key methods
`mkdirSync`, `writeFileSync`, `main`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/hermes-vault-digest.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find hermes-vault-digest.mjs` or `prism_session:master_index_query`; open the source at `scripts/hermes-vault-digest.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
