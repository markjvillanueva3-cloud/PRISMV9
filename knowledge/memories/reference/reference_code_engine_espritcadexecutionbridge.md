---
name: reference_code_engine_espritcadexecutionbridge
description: "planning↔execution bridge for ESPRIT CAM ops."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/EspritCADExecutionBridge.ts
  sourceHash: 7ba99728
  loc: 423
aliases: EspritCADExecutionBridge
---

# EspritCADExecutionBridge -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/EspritCADExecutionBridge.ts` (423 LOC)

## What it is
planning↔execution bridge for ESPRIT CAM ops.

```
EspritCADExecutionBridge — planning↔execution bridge for ESPRIT CAM ops.

Mirrors the SolidWorks / Inventor / Mastercam / HyperCAD bridge pattern but
emits an ESPRIT KBM (Knowledge Base Macro) scaffold instead of VBA / iLogic /
C# / HCS macro. ESPRIT (DP Technology / Hexagon) doesn't ship a public
MCP-style remote endpoint, so the live execution path is the operator
pasting the scaffold into ESPRIT's macro / scripting surface (KBM editor or
the newer ESPRIT TNG C# API), running it once, and verifying the result
before committing back into PRISM. Pure dry-run by design —
operator-in-the-loop is preserved.

Two pure static entry points:
  1. plan(args) — looks up the op in EspritFunctionIndexEngine
     (which auto-detects section across milling / turning / mill_turn /
     swiss), validates required + provided parameters against the catalog's
     group-nested schema, enforces type / rang
```


## Exports
`EspritPlanArgs` (interface), `EspritParamRecord` (interface), `EspritExecutionPlan` (interface), `EspritCADExecutionBridge` (class)

## Key methods
`plan`, `validateValueType`, `renderKBMScaffold`

## Depends on
[[reference_code_engine_espritfunctionindexengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/EspritCADExecutionBridge.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find EspritCADExecutionBridge` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/EspritCADExecutionBridge.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
