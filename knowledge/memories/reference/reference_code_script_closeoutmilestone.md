---
name: reference_code_script_closeoutmilestone
description: "close-out-milestone.mjs — One-command roadmap close-out (feedback_roadmap_close_out)."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/close-out-milestone.mjs
  sourceHash: ec1cfe7b
  loc: 528
aliases: close-out-milestone.mjs
---

# close-out-milestone.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/close-out-milestone.mjs` (528 LOC)

## What it is
```
close-out-milestone.mjs — One-command roadmap close-out (feedback_roadmap_close_out).

Touches the four downstream surfaces that drift when you stop at the envelope:

  1. mcp-server/data/milestones/<ID>.json   (verify, normalize updated_at)
  2. mcp-server/data/roadmap-index.json     (status, completed_units, completed_at)
  3. state/shared/MILESTONE_PROGRESS.{md,json}  (regen via build-milestone-progress.mjs)
  4. state/shared/BUILD_STATE.{md,json}     (regen via build-state-snapshot.mjs)
  + chat-bus broadcast (agent-coordination.mjs post)

Origin: OCTOPUS-NEURAL-MS0 merge landed (envelope completed) but BUILD_STATE
still flagged "claims not_started, real completed_real" because the four
downstream surfaces were never updated. This script is the missing
orchestration layer for the existing-but-unwired sub-scripts.

Modes:
  --milestone <ID>       Close out a specific milestone (most c
```


## Exports
`parseArgs` (re-export), `snapshotEnvelope` (re-export), `atomicWriteJson` (re-export), `readJson` (re-export), `renderChatBusSummary` (re-export), `COMMIT_PREFIX_RE` (re-export), `runSelfTest` (re-export)

## Key methods
`main`, `emitFinal`, `atomicWriteJson`, `check`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/close-out-milestone.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find close-out-milestone.mjs` or `prism_session:master_index_query`; open the source at `scripts/close-out-milestone.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
