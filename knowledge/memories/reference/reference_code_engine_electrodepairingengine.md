---
name: reference_code_engine_electrodepairingengine
description: "Electrode Pairing"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ElectrodePairingEngine.ts
  sourceHash: 4eb316d8
  loc: 411
aliases: ElectrodePairingEngine
---

# ElectrodePairingEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ElectrodePairingEngine.ts` (411 LOC)

## What it is
Electrode Pairing

```
ElectrodePairingEngine — ARC-MS6 / muS-C22

Sinker-EDM rougher/finisher electrode pairing: naming-convention parser
+ sizing-rule validator.

Shops fabricate electrode sets in cutting-order stages (rough → semi →
finish) and store them under shop-naming conventions where the stage is
encoded in the filename — `WAFER880_R.NC` (rough), `WAFER880_F.NC`
(finish), `ROUGH_WAFER880.NC`, `WAFER880_RUF1.NC`, etc.  This engine
groups a flat file listing into one ElectrodeSet per cavity and (when
dimensions are provided) validates that the stages are sized in cutting
order — rough ≥ semi ≥ finish in both length and width.

Inputs: a list of filenames (typically from a directory listing of the
cavity's electrode programs) and optional per-file XY dimensions.
Outputs: grouped ElectrodeSet[] with completeness flags + per-set
warnings, plus the unpaired-files list and aggregate counts.

Stage detection
```


## Exports
`StageName` (type), `StagePatterns` (interface), `PairedStage` (interface), `ElectrodeSet` (interface), `ElectrodePairingResult` (interface), `ElectrodePairingInput` (type), `ElectrodePairingEngine` (class), `electrodePairingEngine` (const)

## Key methods
`pair`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ElectrodePairingEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ElectrodePairingEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ElectrodePairingEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
