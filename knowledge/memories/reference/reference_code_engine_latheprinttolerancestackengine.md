---
name: reference_code_engine_latheprinttolerancestackengine
description: "LathePrintToleranceStackEngine — U-LTH35 (LATHE-MASTER P4)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LathePrintToleranceStackEngine.ts
  sourceHash: b276ae9f
  loc: 792
aliases: lathePrintToleranceStackEngine
---

# lathePrintToleranceStackEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LathePrintToleranceStackEngine.ts` (792 LOC)

## What it is
```
LathePrintToleranceStackEngine — U-LTH35 (LATHE-MASTER P4)

Propagates GD&T through feature→operation chain:
- Builds datum chains from GD&T relationships
- Propagates tolerances through machining sequence
- Assigns Cpk target per feature based on datum chain
- Surfaces stack-up warnings when chain exceeds tolerance budget

Uses: ToleranceAwareGenerationEngine, TurningCpkSurrogateEngine patterns

@milestone LATHE-MASTER U-LTH35
@version 1.0.0
```


## Exports
`DATUM_PRIORITY` (const), `DatumNodeSchema` (const), `DatumNode` (type), `DatumChainSchema` (const), `DatumChain` (type), `FeatureCpkTargetSchema` (const), `FeatureCpkTarget` (type), `StackAnalysisResultSchema` (const), `StackAnalysisResult` (type), `StackWarningSchema` (const), `StackWarning` (type), `ToleranceStackOutputSchema` (const), `ToleranceStackOutput` (type), `lathePrintToleranceStackEngine` (const), `LathePrintToleranceStackEngine` (re-export)

## Key methods
`propagate`, `buildDatumChains`, `assignCpkTargets`, `analyzeStacks`, `batchPropagate`, `getStackStats`, `validate`

## Depends on
[[reference_code_engine_latheturningfeaturerecognizerengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/LathePrintToleranceStackEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find lathePrintToleranceStackEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LathePrintToleranceStackEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
