---
name: reference_code_frontend_usethemetokens
description: "useThemeTokens -- per-user theme customization (FLEET-IOS-REDESIGN U3, slot:hotel)"
metadata:
  type: reference
  kind: code-frontend
  galaxy: unassigned
  sourcePath: mcp-server/web/src/hooks/useThemeTokens.ts
  sourceHash: 90e02fe5
  loc: 225
aliases: useThemeTokens
---

# useThemeTokens -- Frontend SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-04T14:51:51.225Z  |  source: `mcp-server/web/src/hooks/useThemeTokens.ts` (225 LOC)

## What it is
```
useThemeTokens -- per-user theme customization (FLEET-IOS-REDESIGN U3, slot:hotel)

Lets a user override the three theme dials that the iOS redesign exposes:
  - accent color  (--accent-rgb, a space-separated "R G B" triple)
  - density       (--density, scales control padding/height)
  - corner radius  (--radius-sm/md/lg)

CRITICAL: every override is written to document.body.style, NOT
document.documentElement.style. The iOS-mode bridge in index.css
(`body[data-theme='ios']`) re-points --accent-rgb / --radius-* ON body, which
shadows anything set on <html> (body's parent). An inline style set on body --
the SAME element that carries the selector rule -- wins by specificity (inline
style beats a selector), so the user choice always takes effect. Writing to
documentElement would be silently overridden in iOS mode. (See index.css L85:
"An inline setProperty() (useThemeTokens, U3) still win
```


## Exports
`ThemeRadius` (type), `ThemeTokens` (interface), `ACCENT_PRESETS` (const), `DEFAULT_THEME_TOKENS` (const), `applyThemeTokens` (function), `useThemeTokens` (function)

## Key methods
`useEffect`, `setTokens`, `applyThemeTokens`, `persistTokens`, `clearThemeTokens`, `clearStoredTokens`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/web/src/hooks/useThemeTokens.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Frontend

## How to query it
Recall by meaning via `/system-viz find useThemeTokens` or `prism_session:master_index_query`; open the source at `mcp-server/web/src/hooks/useThemeTokens.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
