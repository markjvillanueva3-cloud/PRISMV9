---
name: reference_zulu_obsidian_live_2026_05_30
description: ZULU-OBSIDIAN-LIVE-MS0 — gave the Hermes/Zulu orchestrator a fail-soft LIVE Obsidian-vault client (Local REST API :27123) + a hardened outward-facing Telegram mobile bridge. Built on work/zulu-obsidian-live worktree off fixed main. Also repaired a real main-HEAD regression (zebra→zulu class-rename was never committed).
type: reference
source: prism-memory
synced: 2026-06-09T14:54:11.078Z
aliases: reference_zulu_obsidian_live_2026_05_30
---


2026-05-30 (slot:bravo). Operator shared the "I Connected Hermes Agent to My Obsidian Vault" pattern (DamiDefi/X) and asked the hermes-zulu chat to apply it. Audit finding: **PRISM already implements 5 of the tweet's 6 elements** (per-slot SOUL.md = 26 slot souls; scheduled briefs = CLAUDE-BRIEF/`/wiki-morning`; multi-model routing = `aiSystemRouterEngine`; skill-file creation = `/forge-triple`; Obsidian-format vault = `obsidian-memory-sync.mjs` → `H:/prism/knowledge`). The only genuine gap = the tweet's actual innovation: a **LIVE** Obsidian instance (Local REST API `:27123`) read in-session, vs the existing one-way Stop-time file copy. Operator chose scope: **Live REST + Telegram mobile**.

**Shipped (branch `work/zulu-obsidian-live`, NOT slot/bravo — see foundation note):**
- `mcp-server/src/engines/ObsidianRestBridgeEngine.ts` — fail-soft live-vault client. `status/isLive/read/search/activeNote`; returns `{ok:false,reason}` (never throws); **no-key short-circuit opens no socket**; URL-keyed 10s health cache; "usable" = reachable AND `authenticated:true` (parses GET / body, since a bad key → 200 `authenticated:false`, not 401). **READ-ONLY v1** (no write method). Mirrors `scripts/ollama-prism-bridge.mjs` `mcpCallStreamable` idiom. 27 tests.
- Wired 3 actions into `prism_session` (`sessionDispatcher.ts`, lazy-import cases beside the Zulu ones): `obsidian_status/read/search`. No `obsidian_write`. 3 round-trip wiring tests.
- `zuluAwarenessReader.ts` — additive `liveBrainContext()` gated behind `PRISM_OBSIDIAN_LIVE=1` (default off → null, zero behavior change); `loadAwareness` byte-unchanged. 4 tests.
- `scripts/zulu-telegram-bridge.mjs` — OUTWARD-FACING mobile bridge. Long-poll getUpdates (no inbound port); **READ-ONLY**; default-DENY chat-ID allowlist (`PRISM_TELEGRAM_ALLOWED_CHAT_IDS`, empty=deny-all, unknown→silent drop + hashed-id only); fixed verb allowlist `/recall /search /status` (single-line regex, opaque query → only the live-vault search, never a path/shell/dispatcher arg); per-chat token bucket + global ceiling; 3500-char cap; `sanitizeOutput` strips env/Bearer/**Telegram-bot-token**/JWT/path/long-hex; token via `PRISM_TELEGRAM_BOT_TOKEN` only, never logged. 13 tests.
- `scripts/obsidian-live-setup-check.mjs` — green/red operator probe (never prints secrets).

**Security (per-file scrutiny caught real bugs):** the engine FAILED round-1 review on `rejectUnauthorized:false` for any https host + verbatim `PRISM_OBSIDIAN_URL` (MITM/key-exfil). Fixed: loopback-only cert relaxation + fail-CLOSED non-loopback (`urlAllowed`, opt-in `PRISM_OBSIDIAN_ALLOW_REMOTE=1`). Round-2 caught a NEW P0: `isLoopbackHost` used `startsWith("127.")` → matched the FQDN `127.0.0.1.evil.com`; fixed with a strict anchored dotted-quad regex. Telegram review caught a P1: bot-token shape survived `sanitizeOutput`; added a redaction rule. My own tests caught 2 more: newline-smuggle (`[\s\S]` matched `\n`) and a module-load-frozen URL — both fixed. Lesson: [[feedback_parallel_scrutiny_per_file]] is load-bearing; an outward-facing surface needs adversarial review, and writing real tests (R9) catches what prose review misses.

**Foundation saga (why a scope worktree, not slot/bravo):** the slot/bravo worktree was 1916 commits behind + only a partial content-sync (had main's `index.ts` but not the dispatchers it imports) → unbuildable in-place. Worse, main's **committed HEAD didn't compile**: this session's zebra→zulu migration committed the file renames + dispatcher rewire but NOT the engine class-rename — committed `sessionDispatcher` imported `Zulu*Engine` while committed engines still `export class Zebra*`. Operator chose "fix main HEAD first": committed the migration tail (3 engine class bodies + untracked `zuluAwarenessReader.ts`) to main as `71c7be4e38` `[ZULU-RENAME-MS0]/U-ZULU-TAIL-FIX`. Then built the feature in a fresh scope worktree `H:/prism-zulu-obsidian` (`work/zulu-obsidian-live`) off the fixed main — clean, complete, buildable (needed a `node_modules` junction to main's). Related: [[reference_worktree_not_main_for_big_changesets_2026_05_30]], [[reference_bravo_verify_against_main_not_worktree_2026_05_29]].

**Update 2026-05-31 — landed on main + always-on file-vault backend.** Per [[feedback_galaxy_self_commit]], bravo landed its own work directly on `cad-fusion-live-ms0`: `34e2a9cdc4` (the 10-file feature) + `1810091752` (file-vault backend). Operator asked "the plugin can't steal our data?" → scanned the bundle: **zero fetch/XHR, no telemetry/exfil host** (all external URLs are doc/charset/spec strings in bundled libs), binds **127.0.0.1 only** (we didn't set 0.0.0.0), key-gated, MIT open-source — low risk; the genuinely outward-facing surface is the Telegram bridge (hardened). I pre-staged the Obsidian side: installed Local REST API plugin 4.1.2 into `H:/prism/knowledge/.obsidian/plugins/`, enabled it, generated an API key into `data.json` + matched it in settings.json env (`PRISM_OBSIDIAN_API_KEY`) + set `PRISM_OBSIDIAN_LIVE=1`. winget unavailable → operator installs Obsidian from `https://github.com/obsidianmd/obsidian-releases/releases/download/v1.12.7/Obsidian-1.12.7.exe`. Telegram bot creation is operator-only (@BotFather). **Key pivot:** the bridge now defaults to an **always-on FILE-VAULT backend** (`rankFiles`/`searchVaultFiles` over `<vault>/memories` markdown) — no plugin, works when Obsidian is closed; live-REST is an opt-in upgrade (`PRISM_OBSIDIAN_LIVE=1`). This resolves both operator concerns (plugin-trust + only-when-open). 17 bridge tests.

**Update 2026-05-31 (b) — LAN brain web endpoint, always-on.** `scripts/zulu-brain-web.mjs` (committed main, 7 router tests): token-gated, READ-ONLY HTTP page serving the file-vault search to a phone on home WiFi (`http://192.168.50.233:8787/?tk=<persisted>`). No Telegram, no account, no plugin — and INDEPENDENT of the MCP `:3100` server (works when it's down). Wired for always-on: Windows scheduled task **"PRISM Brain Web"** (AtLogOn, restart-on-crash, hidden, survives reboot — registered + Running) + firewall inbound TCP 8787 (Private profile). Token persisted at `state/shared/.brain-web-token` (gitignored). Brain freshness needs NO new wiring — the file-vault reads `knowledge/memories`, already auto-fed every Stop by `stop-obsidian-memory-feed`. **[[reference_fleet_reaper|Fleet-reaper]] coexistence (golf):** the brain-web node process is launched by the scheduled task (parent = Task Scheduler/pwsh, NOT a claude chat), so it is NOT a chat-fleet orphan — golf's reaper must not reap "PRISM Brain Web"; flag if it ever does. Caveat: the LAN IP is DHCP — a router reassignment changes the URL host (set a DHCP reservation for a permanently-stable URL). Built by bravo per [[feedback_galaxy_self_commit]].

**Deferred (fail-loud):** (1) full-server `build:fast` / `:3100` live restart is blocked by PRE-EXISTING main build debt (≈30+ uncommitted-but-imported files: academy courses, `ideaBlockSchema`, several engines, `file:///` lib imports) — unrelated to this feature; the live end-to-end check happens when golf merges `work/zulu-obsidian-live` into main's working tree. (2) Operator one-time setup to light it up: install Obsidian + Local REST API plugin → open `H:/prism/knowledge` as a vault → `PRISM_OBSIDIAN_API_KEY` + `PRISM_OBSIDIAN_LIVE=1`; `@BotFather` → `PRISM_TELEGRAM_BOT_TOKEN` + `PRISM_TELEGRAM_ALLOWED_CHAT_IDS`; verify with `node scripts/obsidian-live-setup-check.mjs`. (3) Telegram = quebec (phone-app) territory — flagged for coordination. (4) Doc reflection: CLAUDE.md/MEMORY.md updates land via golf merge.
