---
name: reference_code_schema_blueprintextractioncontract
description: "BlueprintExtractionContract -- the VERSIONED, app-facing shape of a blueprint OCR extraction."
metadata:
  type: reference
  kind: code-schema
  galaxy: unassigned
  sourcePath: mcp-server/src/schemas/BlueprintExtractionContract.ts
  sourceHash: 572ff5d6
  loc: 401
aliases: BlueprintExtractionContract
---

# BlueprintExtractionContract -- Schema SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/schemas/BlueprintExtractionContract.ts` (401 LOC)

## What it is
```
BlueprintExtractionContract -- the VERSIONED, app-facing shape of a blueprint OCR extraction.

WHY (the app-integration keystone, blueprint-vision-app-integration-plan-2026-06-23): the
blueprint-vision backend is mature (VLM-ensemble OCR -> fused dims/gd&t/notes), but the app stops
at "file uploaded" and every consumer (the upload->extract route, the quote autopopulate, the
drawing-view panel) needs ONE stable JSON shape to bind to. This module is xray's cross-phase
deliverable: the contract every app consumer reads, VERSIONED so a producer change can never
silently break a consumer (a schemaVersion bump + migration is forced instead). The producer side
(drawing_extract route) is owned by papa/quebec per the plan; xray owns THIS contract.

NORMALIZER: `normalizeFusedToContract` maps the live `fuseEnsemble` output
(scripts/lib/vision-ensemble-fuse.mjs: {dimensions, gdt, notes, profiles, s
```


## Exports
`BLUEPRINT_EXTRACTION_CONTRACT_VERSION` (const), `OCR_PER_FIELD_CONFIRM_FLOOR` (const), `dimensionStatusEnum` (const), `contractDimensionSchema` (const), `contractCalloutSchema` (const), `titleBlockSchema` (const), `contractSummarySchema` (const), `blueprintExtractionContractSchema` (const), `BlueprintExtractionContract` (type), `ContractDimension` (type), `ContractCallout` (type), `ContractTitleBlock` (type), `normalizeFusedToContract` (function), `normalizeDrawingExtractToContract` (function), `ContractValidation` (interface), `validateBlueprintExtractionContract` (function)

## Key methods
`mapCallouts`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/schemas/BlueprintExtractionContract.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Schema

## How to query it
Recall by meaning via `/system-viz find BlueprintExtractionContract` or `prism_session:master_index_query`; open the source at `mcp-server/src/schemas/BlueprintExtractionContract.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
