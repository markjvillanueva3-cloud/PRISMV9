---
name: reference_code_engine_wedmgraphattentionengine
description: "h=4 multi-head Graph Attention layer."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WEDMGraphAttentionEngine.ts
  sourceHash: 53f27d03
  loc: 604
aliases: WEDMGraphAttentionEngine
---

# WEDMGraphAttentionEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WEDMGraphAttentionEngine.ts` (604 LOC)

## What it is
h=4 multi-head Graph Attention layer.

```
WEDMGraphAttentionEngine — h=4 multi-head Graph Attention layer.

MS-P5-GNN / U-P5-GNN-02

Implements the GAT layer from Veličković et al. (2017) "Graph Attention
Networks" over the embedding lattice produced by WEDMLatticeGraphEngine
(U-P5-GNN-01). For each node i and each head k:

    z_i^k       = W^k · h_i                           (perHeadDim)
    e_{ij}^k    = LeakyReLU(a^k · [z_i^k ‖ z_j^k])    (scalar, 0.2 slope)
    α_{ij}^k    = exp(e_{ij}^k) / Σ_{m∈N(i)} exp(e_{im}^k)
    h_i'^k      = Σ_j α_{ij}^k · z_j^k                (perHeadDim)
    h_i'        = concat_k h_i'^k                     (heads · perHeadDim = 64)

Self-loops are added implicitly (each node attends to itself via the edge
(i,i) with bias 0). Output dim equals input dim (64) so the layer is a
drop-in refinement.

Training (offline): predicts edge weight w_{ij} from sigmoid(<h_i', h_j'>).
Loss = binary cross-entrop
```


## Exports
`AttentionForwardResult` (interface), `forwardSingleHead` (function), `forwardLayer` (function), `initWeights` (function), `TrainOptions` (interface), `TrainResult` (interface), `WEDMGraphAttentionEngine` (class), `averageHeadCosine` (function), `wedmGraphAttentionEngine` (const)

## Key methods
`init`, `snapshot`, `attend`, `applyToLattice`, `train`, `updatePair`, `save`, `load`, `isStale`

## Depends on
[[reference_code_util_logger]] | [[reference_code_engine_wedmlatticegraphengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/WEDMGraphAttentionEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WEDMGraphAttentionEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WEDMGraphAttentionEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
