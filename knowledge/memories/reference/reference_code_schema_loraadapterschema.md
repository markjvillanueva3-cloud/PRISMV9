---
name: reference_code_schema_loraadapterschema
description: "LoRA Adapter & Inference-Gate Schema — U-LEARN-07"
metadata:
  type: reference
  kind: code-schema
  galaxy: unassigned
  sourcePath: mcp-server/src/schemas/loraAdapterSchema.ts
  sourceHash: d290bf75
  loc: 156
aliases: LoRAAdapterSchema
---

# LoRAAdapterSchema -- Schema SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/schemas/loraAdapterSchema.ts` (156 LOC)

## What it is
```
LoRA Adapter & Inference-Gate Schema — U-LEARN-07
==================================================

The belt that connects trained LoRAs to live inference. Utilization
Architect scrutiny (0.18 — the lowest score in any pass) said PRISM
trains LoRAs that never reach inference. This schema + the
LoRAAdapterRegistryEngine + InferenceLoRAGateEngine close that gap.

Every recommendation engine (SpeedFeed, Kienzle, PostProcessor, …) runs
its baseline through the gate; if an adapter matches the request context
the gate applies a learned residual correction and tags provenance so
operators and downstream consumers can see whether a recommendation
came from physics alone, or physics + LoRA.

@module schemas/loraAdapterSchema
@milestone PSAU P2.5-LEARN U-LEARN-07
```


## Exports
`IsoTimestamp` (const), `AdapterContextSchema` (const), `AdapterContext` (type), `AdapterStatus` (const), `AdapterStatusT` (type), `ResidualFieldSchema` (const), `ResidualField` (type), `ResidualMapSchema` (const), `ResidualMap` (type), `LoRAAdapterSchema` (const), `LoRAAdapter` (type), `RegisterAdapterInputSchema` (const), `RegisterAdapterInput` (type), `SetStatusInputSchema` (const), `SetStatusInput` (type), `ResolveAdapterInputSchema` (const), `ResolveAdapterInput` (type), `ApplyGateInputSchema` (const), `ApplyGateInput` (type), `ApplyGateResultSchema` (const)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/schemas/loraAdapterSchema.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Schema

## How to query it
Recall by meaning via `/system-viz find LoRAAdapterSchema` or `prism_session:master_index_query`; open the source at `mcp-server/src/schemas/loraAdapterSchema.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
