---
name: reference_hermes_bridge_ms0_2026_06_13
description: "Hermes install repair + PRISM->Hermes OpenAI-proxy bridge (ask-hermes.mjs). How Hermes is wired into PRISM, the proxy surface, and the Anthropic-billing caveat."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.604Z
aliases: reference_hermes_bridge_ms0_2026_06_13
---


**HERMES-BRIDGE-MS0 (2026-06-13, slot:echo).** Repaired a broken Hermes install and built+wired+live-tested the PRISM->Hermes bridge.

**The breakage ("install got messed up"):** an interrupted `hermes update` 0.15.1 -> 0.16.0. Symptoms: (1) `toolsets.py` SyntaxError crashed the backend (`Hermes backend exited (1)`) -- already resolved by the 23:55 git pull (file compiles clean now); (2) a stale `.update-incomplete` marker (hermes-agent dir) triggered a FAILING auto-recovery on EVERY `python -m hermes_cli.main` call, because the recovery's `pip install -e` cannot replace `venv/Scripts/hermes.exe` -- it is LOCKED by the running 11-slot Hermes agent fleet (`hermes.exe -p alpha/bravo/...`); (3) 6 uv orphan temp `~*rmes_agent-0.16.0.dist-info` dirs in site-packages. **Repair (non-destructive, NO fleet kill):** removed the 6 orphan temp dirs + cleared `.update-incomplete`. The install is FUNCTIONALLY 0.16.0 (editable `.pth` -> source; `hermes_cli.main` imports clean). The locked-exe shim is cosmetic (everything runs via `python -m`); finishing it needs the fleet stopped or a reboot -- NOT required for the bridge.

**Two Hermes surfaces (do not confuse):**
- **Agent** (`venv/Scripts/python.exe -m hermes_cli.main chat -q "<p>" -Q [--provider X] [-m M]`): the agentic loop (tools/skills). Pinned to `claude-opus-4-8`/anthropic by config.yaml (lines 321/391); `--provider ollama` does NOT override it. Anthropic path returns **HTTP 400 "Third-party apps now draw from your extra usage, not your plan limits. Add more at claude.ai/settings/usage"** -- this is the Anthropic third-party-app BILLING policy + the user's account credit balance, NOT a Hermes/setup defect (the 6-account OAuth pool authenticates fine). User action to fund at claude.ai.
- **Proxy** (`python -m hermes_cli.main proxy start --provider xai|nous --port 8645`): local OpenAI-compatible HTTP server forwarding to a managed-OAuth upstream. Upstreams: `xai` (Grok, READY) + `nous` (Nous Portal, not logged in). NOT anthropic. Default port **8645** (NOT 9120 -- that was a stale assumption). LIVE-verified: `/v1/chat/completions` returns real grok completions.

**The bridge -- `scripts/ask-hermes.mjs` (HERMES-BRIDGE-MS0/U-ASK-HERMES, commit on cad-fusion-live-ms0):** OpenAI `/v1` client to the proxy, mirrors `ask-ollama.mjs` conventions (modes ask/summarize/explain/triage/classify; `--model/--json/--timeout/--max-tokens/--url/--no-fallback`). Auto-resolves the model from `/v1/models`. On ANY Hermes failure it degrades to free local Ollama via `ask-ollama.mjs` (token economy; loud about why per R12). Env: `PRISM_HERMES_PROXY_URL` (default `http://127.0.0.1:8645/v1`), `PRISM_HERMES_TOKEN` (default "prism" -- proxy ignores the value, attaches the real OAuth cred), `PRISM_HERMES_MODEL`. **Wired** into `scripts/lib/task-substrate-router.mjs` (ollama-escalation + hermes direct-inference). Tests: `scripts/ask-hermes.test.mjs` 20/20 + router 9/9. LIVE E2E: bridge returned `PRISM_BRIDGE_OK` via grok.

**Now fully live in every slot (U-ASK-HERMES-SKILL + U-PROXY-ENSURE):** (1) durable proxy -- the **`PRISM Hermes Proxy`** scheduled task (`.claude/helpers/install-hermes-proxy-task.ps1`, S4U current-user, every 5min + AtStartup) runs `hermes-proxy-ensure.mjs` to keep `:8645` up; INSTALLED + State=Ready + LastResult=0. (2) first-class **`/ask-hermes`** slash command (`.claude/commands/ask-hermes.md`, force-added -- commands/* is gitignored but ~55 are tracked) -> reachable from EVERY Claude Code chat slot. LIVE E2E from the slot path returned `PRISM_SLOT_HERMES_OK` via grok. If the proxy is ever down, the bridge degrades to free ollama.

**HYBRID FIX -- agent loop now runs FREE on local Ollama (U-HYBRID-OLLAMA, 2026-06-13):** operator chose a hybrid (Claude Code CLI = brainstorm/plan/deep-reason/hard-code; Hermes strategically offloads EASIER tasks on a free local model). The agent loop was pinned to anthropic (400 on third-party billing) AND xai (bravo profile, bad key). Clean reinstall (stopped 11 slot-agents -> `pip install -e .[all]` -> 0.16.0, exe replaced) + `scripts/hermes-config-hybrid-ollama.py` (ruamel, idempotent, --dry-run): flips EVERY primary model block (model/fallback_model/auxiliary.vision) from any non-ollama provider -> local ollama across root config + all 21 `profiles/<slot>/config.yaml`. **CRITICAL GOTCHA:** Hermes `provider: ollama` defaults to Ollama CLOUD (`https://ollama.com/v1`) unless `base_url` carries `:11434` (run_agent.py/cli.py key on it) -- so the script FORCES `base_url=http://127.0.0.1:11434/v1` + dummy `api_key=ollama-local` on every ollama block (incl the aux tiers that were silently cloud-pointed). Models: `gpt-oss:120b` (reason) / `qwen2.5-coder:32b` (fallback) / `qwen2.5vl:32b` (vision), env-overridable. LIVE E2E: `hermes chat -Q -q` on active bravo profile -> `PRISM_HERMES_LOCAL_OK` via local gpt-oss:120b, exit 0, NO anthropic 400, NO xai. **Re-run after any `hermes update`** (it regenerates profiles): `python scripts/hermes-config-hybrid-ollama.py --apply`. The proxy/`/ask-hermes` path (xai grok) is independent and still works for when grok-class output is wanted.

**Fleet relaunch (operator):** the `hermes -p <slot>` persistent agents were stopped for the reinstall; they are launched by the Hermes desktop app / operator, NOT a committed PRISM script. Hermes orchestration also works on-demand via `delegation` (orchestrator_enabled, max_concurrent_children=10) + kanban (auto_decompose) -- all now on free local gpt-oss:120b. Restart the desktop app or relaunch the gateway (`hermes gateway run`) to resume persistent fleet orchestration.

**ULTRACODE UTILIZATION EVAL (2026-06-14, slot:echo, workflow wf_b36ab40c — 5 agents, high-confidence 4/4).** Operator: "did we finish bridging + is it fully active?" VERDICT: **bridging FINISHED (YES)** — proxy client + keepalive task (PRISM Hermes Proxy, LastResult=0) + /ask-hermes skill + router lane + util tracking all built; one caveat: the full Hermes *agentic chat* loop is non-functional under Anthropic third-party billing, only the PROXY completion path bridges. **Fully active = PARTIAL** — proxy ACTIVE (grok :8645) + agent loop ACTIVE free-local (root `model:` = ollama/gpt-oss:120b @ :11434, fallback qwen2.5-coder:32b, vision qwen2.5vl:32b, NO active_profile so the root block IS the loop), **but UTILIZATION ≈ 0** (byHook["ask-hermes"]={fired:1} = the util-track test call itself; nothing auto-invokes Hermes — skill is autoSuggest-tier not INVOKE_NOW, router hermes lane needs ctx scale-hints nothing auto-passes, no lifecycle hook fires it). **BRIDGED-BUT-IDLE.**

**Two units shipped from the eval (2026-06-14):** (1) **U-HERMES-UTIL-TRACK** (`b56ef64c7e`) — ask-hermes was UNMEASURED (byHook["ask-hermes"] ABSENT); added pure `tallyUsage()` (exported, +6 tests, 26/26) + fail-safe atomic `recordUsage()` at all 4 exit points; bySource splits real-hermes vs ollama-fallback. LIVE: ABSENT→{fired:1,bySource:{hermes:1}} via grok. (2) **U-HERMES-DELEGATION-PIN** (`b1ff295da7`) — `delegation.provider=auto` (empty base_url, across root+21 profiles) let orchestrated children auto-resolve to CLOUD = invisible real-money exposure; extended `hermes-config-hybrid-ollama.py` to repoint the delegation block; applied 22/22, root delegation now ollama/gpt-oss:120b/local.

**OPEN OPERATOR DECISIONS (NOT auto-built — behavioral/billing or capability-dependent):** (a) **`experimental.auto_invocation_router=true` + `claude_escalation_threshold=0.65`** (config.yaml ~587-590) — the PRIMARY loop may silently auto-escalate tasks >0.65 to PAID Claude/cloud (untracked); set `auto_invocation_router=false` or raise threshold to ~0.95 if free-local-first is the intent. (b) **5 aux sub-roles** (`web_extract`/`skills_hub`/`x_search`/`profile_describer`/`title_generation`) on `provider:auto` — do NOT blind-pin (web/x_search need cloud/web ollama lacks; R8). (c) **Activation policy** — Hermes is opt-in-only; to make it carry real work, wire a loop-state→ctx producer (task-substrate-router.mjs:15 U7) OR promote /ask-hermes to INVOKE_NOW, ELSE accept opt-in + stop calling it "active". (d) eval false-positive: `shouldFallback()` always-true is INTENTIONAL (design comment: always degrade to free ollama for usefulness) — NOT a stub; don't "fix" it.

Related: [[feedback_offload_forceable_boundaries_not_mainloop]] (offload wins at forceable boundaries), [[reference_hermes_app_launch_fix_cred_pool_2026_06_12]] (the 9-entry OAuth pool setup), [[reference_synergy_roi_verified_2026_06_14]] (the offload-surface-saturated finding from the same session).
