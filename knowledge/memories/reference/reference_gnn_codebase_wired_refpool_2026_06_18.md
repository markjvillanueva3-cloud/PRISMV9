---
name: reference_gnn_codebase_wired_refpool_2026_06_18
description: "GNN ref-pool growth lever found + built (slot:india 2026-06-18): the 2 existing feeders (outcome 139 + vault 16) are SATURATED; built a 3rd feeder wired-engines-to-refpool.mjs that mines the codebase's confirmed engine->dispatcher wirings = 3206 single-dispatcher ground-truth labels (~20x). Committed dry-run-safe (859554a148); the --apply is GATED on blast-radius verification (U-GNN-CODEBASE-WIRED-APPLY)."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.589Z
aliases: reference_gnn_codebase_wired_refpool_2026_06_18
---


**slot:india, 2026-06-18 ("do everything" turn). The model-quality lever for PSN leg #10's 27% selective coverage.**

## The chain (all empirically grounded)
1. **rsLoRA r=32 = ALREADY COMPLETE** (verified, not assumed). `state/shared/lora/adapters/fleet-rslora-r32`: adapter_model.safetensors 315MB, adapter_config r=32/alpha=32/use_rslora=true/7 target modules, fleet_training_metadata globalStep=400 (full run, max-steps=400), smoke=false, invalidSkipped=0, corpus 1372 rows (319 verified/1053 advisory/34 galaxies), base Qwen2.5-7B-Instruct, Blackwell. Finished 2026-06-15. Task was stale ("checkpoint-350=87.5%" was mid-run). NO re-launch needed (R13 no make-work).
2. **Full GNN retrain re-measured (--force) + durability VALIDATED e2e.** `nn-graph-retrain-lifecycle --force` (8GB self-reexec): trained an 8-d/768d-feature candidate on the 6000-node capped subgraph -> model-mode AUROC 0.3512 / macroF1 0.0371 / Brier 0.1507 ALL sub-gate -> action=not-promoted (R12 gate enforced live). Meanwhile stage 4b refreshed ghost embeddings + re-ran the direct-embed eval + persisted: the DEPLOYED direct-embed path HELD at AUROC 0.7891 / 27.4% coverage / deploy-ready-selective / robust -- unchanged across the forced retrain. This is the R15-step-3 VALIDATE of the whole durability arc (the trilogy + 1a/1b/4b survive a real --force cycle).
3. **The 2 existing ref-pool feeders are SATURATED.** ghost-wire-outcomes-to-refpool dry-run: 139 confirmed (all already in pool); vault: 16. Ghosts stayed 355, holdout n=84, coverage 27% across the --force re-apply -> 0 net-new. Growing those is fleet-data-gated (waits for new outcomes/vault feedback).
4. **THE LEVER (built): codebase engine->dispatcher wirings.** The 2 feeders draw from the outcome ledger + vault -- a thin slice. The live codebase has thousands of confirmed wirings (every engine a dispatcher .ts imports IS wired to it). Built `scripts/wired-engines-to-refpool.mjs` (+test 7/7) -- a faithful CLONE of ghost-wire-outcomes (shared mergeGhostsIntoGraph, heap-reexec, --revert), source = `buildEngineDispatcherMap` (existing tested scripts/lib/wired-engine-mapper.mjs). **Dry-run: 3206 single-dispatcher ground-truth labels (confidence 1.0) across 13+ dispatchers (prism_cam 857, prism_calc 538, prism_session 336, ...); 409 multi-dispatcher engines EXCLUDED as ambiguous (R12 -- HookExecutor wired to 34 dispatchers correctly dropped).** ~20x the current 155-ref pool. Namespace `ghost.codebase-wired.*` (no id-collision); eval buildHoldout (nn-graph-eval.mjs:454) selects it namespace-agnostically (refMinConf 0.8 + valid proposed_wiring) + dedups by engine label -> auto-consumed, no double-count. Committed `859554a148`, 2-arm scrutiny PASS (methodology validated: wired engines carry the same leak-stripped domain->dispatcher signal; confidence 1.0 defensible for a literal import; multi-dispatcher exclusion correct R7).

## WHY the --apply is GATED (NOT done this session) -- U-GNN-CODEBASE-WIRED-APPLY
Injecting 3206 ghost.codebase-wired.* nodes into the SHARED 542MB system-graph (25 peer chats consume it) has an UNVERIFIED blast radius: the /system-viz ghost-roosts, orphan-inventory counts, and the production ghost classifier could mistake 3206 ALREADY-WIRED engines for wiring TARGETS (they carry kind:ghost.unwired-engine like the 155 existing refs, but at 20x scale). Before --apply: (1) verify those consumer surfaces exclude proposed_by-feeder ghosts; (2) --apply (reversible); (3) build-node-embeddings --ghosts-only (covers 3206+); (4) nn-graph-eval PRISM_NNG_DIRECT_EMBED=1 -> MEASURE new selective coverage; (5) KEEP if >= 0.789/27% else --revert (never degrade the deployed state; an unrealistic inflation = a leak, be skeptical per the 2026-06-04 fake-0.98); (6) if kept+safe, wire as lifecycle stage 1c (mirror 1a/1b). Heavy + shared-graph mutation -> run SUPERVISED, not turn-end.

## This turn's commits (6, all tested + 2-arm scrutiny PASS)
NN-EVAL write-path hardening trilogy + observability: 9db8c6eace (MODE-GUARD) · e80f585eb8 (DEFERRED-FENCE) · b3aaad542f (ATOMIC-WRITE) · 324d09c661 (STATUS-DEPLOYED-METRIC). Plus the lever: 859554a148 (CODEBASE-WIRED-REFPOOL). [[reference_gnn_tier5_degenerate_2026_06_17]] holds the trilogy detail.

## ENV gotchas (this box, 2026-06-17/18)
git add is lane-guarded for slot chats; new files in the shared tree stage via `git update-index --add` (porcelain `git add` is intercepted; `git commit -- pathspec` only works for already-tracked files). The slot worktree H:/prism-slot-india on slot/india is STALE (missing refpool-merge.mjs etc) -> commit to cad-fusion-live-ms0 where the deps + work live. MCP-enforce hook eats ~1 tool call / 3min episode (re-issue). lifecycle suite needs --max-old-space-size=8192. ascii-guard blocks added em-dashes (use --).
