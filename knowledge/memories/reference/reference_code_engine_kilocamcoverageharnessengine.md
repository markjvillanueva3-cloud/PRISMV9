---
name: reference_code_engine_kilocamcoverageharnessengine
description: "KiloCamCoverageHarnessEngine — KILO-CAM-MASTERY-MS0 campaign iter 4"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/KiloCamCoverageHarnessEngine.ts
  sourceHash: af5d47c1
  loc: 262
aliases: kiloCamCoverageHarnessEngine
---

# kiloCamCoverageHarnessEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/KiloCamCoverageHarnessEngine.ts` (262 LOC)

## What it is
```
KiloCamCoverageHarnessEngine — KILO-CAM-MASTERY-MS0 campaign iter 4

Phase-5 every-toolpath coverage planner. Given a CAD-AI part with
recognized features AND a per-system toolpath catalog, returns the
"use every toolpath" plan — one operation per toolpath, with the
planner picking a target feature whose `geometry_hint` matches the
toolpath's geometry_hint (so the toolpath is at least geometrically
valid on the part).

Toolpaths with NO matching feature on the part are surfaced as
`unsatisfiable_toolpaths[]` — kilo refuse-list: never silently skip,
always tell the operator what's missing.

The planner is INTENTIONALLY breadth-first (one op per toolpath, not
the most efficient roughing chain) — Phase 5 only proves "PRISM can
use every toolpath," not "PRISM picks the best one." Phase 6 is the
optimization pass that picks the best subset.

Per kilo refuse-list:
  - no-silent-fallback-on-amb
```


## Exports
`PartFeatureSchema` (const), `PartFeature` (type), `PartGeometrySchema` (const), `PartGeometry` (type), `ToolpathRefSchema` (const), `ToolpathRef` (type), `PlanStepSchema` (const), `PlanStep` (type), `CoveragePlanSchema` (const), `CoveragePlan` (type), `PlanOpts` (interface), `kiloCamCoverageHarnessEngine` (const)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/KiloCamCoverageHarnessEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find kiloCamCoverageHarnessEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/KiloCamCoverageHarnessEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
