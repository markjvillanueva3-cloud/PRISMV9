---
name: reference_code_engine_cadscreenshotcapturer
description: "6-canonical-view PNG capturer for the ground-truth"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADScreenshotCapturer.ts
  sourceHash: a3caa9e0
  loc: 573
aliases: CADScreenshotCapturer
---

# CADScreenshotCapturer -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADScreenshotCapturer.ts` (573 LOC)

## What it is
6-canonical-view PNG capturer for the ground-truth

```
CADScreenshotCapturer — 6-canonical-view PNG capturer for the ground-truth
STEP corpus. Bypasses native CAD applications by delegating to a pythonocc
(OCCT) helper; falls back to a deterministic mock renderer for CI and for
environments without OCCT installed.

Output layout:
  {outputRoot}/{fileId}/screenshots/iso.png
  {outputRoot}/{fileId}/screenshots/front.png
  {outputRoot}/{fileId}/screenshots/top.png
  {outputRoot}/{fileId}/screenshots/right.png
  {outputRoot}/{fileId}/screenshots/section-xz.png
  {outputRoot}/{fileId}/screenshots/section-yz.png

Per-view metadata: byte size, SHA-256, render duration, view-specific
camera parameters. Aggregated into a deterministic manifest signature so
regression tests can detect drift in the capture pipeline itself.

pythonocc integration:
  The engine accepts a pluggable PythonExecutor so tests run without
  spawning a real Python process. The 
```


## Exports
`AtomicValue` (interface), `CANONICAL_VIEWS` (const), `CanonicalView` (type), `CameraParams` (interface), `CAMERA_BY_VIEW` (const), `CAPTURE_RESOLUTION` (const), `ViewResultSchema` (const), `ScreenshotResultSchema` (const), `ViewResult` (type), `ScreenshotResult` (type), `PythonExecutor` (interface), `NULL_PYTHON_EXECUTOR` (const), `manifestSignature` (function), `buildScreenshotDir` (function), `buildViewPath` (function), `CaptureOptions` (interface), `CADScreenshotCapturer` (class), `cadScreenshotCapturer` (const)

## Key methods
`probe`, `render`, `listCanonicalViews`, `getCamera`, `captureViews`, `captureView`, `validate`, `recomputeSignature`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CADScreenshotCapturer.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADScreenshotCapturer` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADScreenshotCapturer.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
