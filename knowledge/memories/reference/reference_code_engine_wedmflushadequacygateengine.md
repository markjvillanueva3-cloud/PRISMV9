---
name: reference_code_engine_wedmflushadequacygateengine
description: "Wire EDM Flushing Adequacy Gate"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WEDMFlushAdequacyGateEngine.ts
  sourceHash: ca88f835
  loc: 326
aliases: WEDMFlushAdequacyGateEngine
---

# WEDMFlushAdequacyGateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WEDMFlushAdequacyGateEngine.ts` (326 LOC)

## What it is
Wire EDM Flushing Adequacy Gate

```
WEDMFlushAdequacyGateEngine — Wire EDM Flushing Adequacy Gate

Decides whether the flushing system moves dielectric fast enough through the
kerf to evict debris before the next pulse. Thin, medium and thick workpieces
require progressively higher velocities because the flush path length grows
linearly with thickness while the kerf cross-section stays constant.

Output plugs directly into WEDMProgramSafetyGateEngine's `flushing` input and
contributes 0.15 to the composite S(x). A failing flush gate forces HARD BLOCK
of program emit unless an experienced operator explicitly overrides with a
tribal-knowledge acknowledgement — and only when velocity is at least at the
tribal_override_floor (never below).

Physics
-------

  v_f = Q / (π · g · L)                                    [m/s]

  Q  = pump volumetric flow           [mm³/s]
  g  = one-sided gap (wire offset)    [mm]
  L  = workpiece 
```


## Exports
`FlushMode` (type), `FlushAdequacyVerdict` (type), `FlushThicknessBand` (type), `FlushAdequacyInput` (interface), `FlushAdequacyResult` (interface), `WEDMFlushAdequacyGateEngine` (class), `wedmFlushAdequacyGateEngine` (const)

## Key methods
`evaluate`, `gate`, `classifyBand`, `baselineRequirement`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WEDMFlushAdequacyGateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WEDMFlushAdequacyGateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WEDMFlushAdequacyGateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
