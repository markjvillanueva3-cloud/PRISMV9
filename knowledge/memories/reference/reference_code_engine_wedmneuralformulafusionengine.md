---
name: reference_code_engine_wedmneuralformulafusionengine
description: "Fuses multiple formula-based WEDM predictions (MRR, Ra, wire-bend,"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WEDMNeuralFormulaFusionEngine.ts
  sourceHash: 3e27b3c6
  loc: 247
aliases: WEDMNeuralFormulaFusionEngine
---

# WEDMNeuralFormulaFusionEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WEDMNeuralFormulaFusionEngine.ts` (247 LOC)

## What it is
Fuses multiple formula-based WEDM predictions (MRR, Ra, wire-bend,

```
WEDMNeuralFormulaFusionEngine — MS-P0.5-COORD U-P0.5-COORD-06

Fuses multiple formula-based WEDM predictions (MRR, Ra, wire-bend,
discharge energy) into a single adaptive estimate, with weights updated
from historical residuals. The "neural" part is adaptive softmax-style
weighting over the estimator ensemble: estimators that have been
consistently accurate for a given (target, material, operation) context
get higher weight; outliers get down-weighted.

Design:
  - An estimator contributes (value, prior_weight, source).
  - Weights start at `prior_weight` and adapt as ground-truth observations
    arrive via recordObservation(actual_value).
  - Adaptation uses an exponential-weighted running mean of squared error
    per estimator; softmax on -error scales yields the next weights.
  - Fusion is a weight-weighted mean of estimator values.
  - Variance across weighted estimators feeds the 
```


## Exports
`FormulaEstimate` (interface), `FusionContext` (interface), `FusedResult` (interface), `FusionObservationResult` (interface), `FusionStats` (interface), `WEDMNeuralFormulaFusionEngine` (class), `wedmNeuralFormulaFusionEngine` (const)

## Key methods
`computeWeights`, `fuse`, `recordObservation`, `getContextStats`, `getStats`, `resetForTests`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WEDMNeuralFormulaFusionEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WEDMNeuralFormulaFusionEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WEDMNeuralFormulaFusionEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
