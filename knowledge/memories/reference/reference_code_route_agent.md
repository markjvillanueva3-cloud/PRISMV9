---
name: reference_code_route_agent
description: "PRISM Agent — REST Routes"
metadata:
  type: reference
  kind: code-route
  galaxy: unassigned
  sourcePath: mcp-server/src/routes/agent.ts
  sourceHash: 183c54d2
  loc: 212
aliases: agent.ts
---

# agent.ts -- Route SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/routes/agent.ts` (212 LOC)

## What it is
```
PRISM Agent — REST Routes
===========================

AGENT-MS5 U-AGT16 — Exposes the PRISM Agent (AGENT-MS1–MS4 engine stack)
as HTTP endpoints. Mirrors prism_agent MCP dispatcher 1:1 so callers can
pick either transport based on their environment.

Endpoints:
  POST   /api/v1/agent/chat              — synchronous agentic loop
  GET    /api/v1/agent/stream            — SSE streaming (per-phase events)
  GET    /api/v1/agent/capabilities      — action + engine introspection
  POST   /api/v1/agent/memory            — memory operations (op in body)
  POST   /api/v1/agent/self-awareness    — build self-model
  GET    /api/v1/agent/stats             — aggregate agent telemetry

All endpoints delegate to callTool("prism_agent", action, params) so the
underlying agentDispatcher is the single source of truth — no duplicated
logic between MCP and REST.

@module routes/agent
@milestone AGENT-MS5
```


## Exports
`createAgentRouter` (function)

## Key methods
`rateLimitMiddleware`, `requireFields`, `async`, `next`, `writeEvent`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/routes/agent.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Route

## How to query it
Recall by meaning via `/system-viz find agent.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/routes/agent.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
