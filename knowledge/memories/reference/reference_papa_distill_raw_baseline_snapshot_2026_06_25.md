---
name: reference_papa_distill_raw_baseline_snapshot_2026_06_25
description: "A fresh-cursor --distill run TRUNCATES the populated live LoRA dataset (domain-corpus/tribal-corpus line ~300/207) so a reaped partial silently regresses the corpus. snapshotRawBaselineBeforeTruncate() now auto-copies it to *.raw-baseline.jsonl before truncate (shrink-guarded, fail-soft) -> the truncate is LOUD + 1-cp recoverable. The baseline-preservation primitive the eventual atomic-swap reuses."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.721Z
aliases: reference_papa_distill_raw_baseline_snapshot_2026_06_25
---


**Shipped (slot:papa, 2026-06-25 autonomous overnight, commits `8d46cbd49c` U-PAPA-DISTILL-RAW-BASELINE-SNAPSHOT + `c2e81dc477` U-PAPA-DISTILL-SNAPSHOT-FAILSOFT; 3-of-3 PASS; domain-corpus 38/38 + tribal 19/19).**

**The gap.** A fresh-cursor `--distill --out` run truncates the populated live dataset (`fs.writeFileSync(outPath, "")` at `domain-corpus-to-lora-dataset.mjs:~300` and `tribal-corpus-to-lora-dataset.mjs:~207`, inside `if (cursorState.size === 0)`) so distilled rows REPLACE the raw ones. If the long Ollama run is then reaped mid-flight (it gets reaped ~5min in / ~69-of-398), the live dataset is left at a regressed, partially-distilled count -- SILENTLY feeding a smaller LoRA corpus. The documented recovery ("restore via raw `--out`") re-reads every PDF via pdftotext (slow, depends on PDFs present); and the truncate itself was silent.

**The fix.** `snapshotRawBaselineBeforeTruncate(outPath, {readImpl,writeImpl,existsImpl})` -- a pure, injected-impl helper EXPORTED from `domain-corpus-to-lora-dataset.mjs`, IMPORTED + called by the tribal sibling (R8 shared, not forked), at BOTH truncate sites just before the `writeFileSync(outPath,"")`. Before a fresh-cursor truncate it copies the populated dataset to `<out>.raw-baseline.jsonl` and prints a LOUD warn carrying the exact `cp <baseline> <out>` recover command. Guards: (1) **shrink-guard** -- never overwrite a LARGER existing baseline with a smaller current output (strict `prevRows > curRows`), so a 2nd fresh-cursor run over an already-partial dataset cannot destroy the good baseline; (2) **trim-aware row count** (a whitespace-only line is not a data row); (3) **fail-soft** -- any I/O throw (V8 utf8 512-MiB string-cap on a growing corpus, ENOSPC/read-only dir, TOCTOU unlink) is swallowed into `reason:"snapshot-error"` + warn-and-proceed, so the best-effort safety net can NEVER block the `--distill` it protects. Live temp-path proof: seeded 6 raw rows -> fresh-cursor `--distill` snapshotted all 6 + warned + truncated (the 1 processed entry was no-text -> 0 distilled = the exact regression case) -> baseline preserved 6 rows.

**Why NOT the full atomic-swap.** The truncation is fully unblocked only by the atomic-swap (distill->`.inprogress`, swap-on-complete, retire-cursor, no-defer) -- but that is the reverted-P0 surface ([[reference_papa_tribal_corpus_lora_2026_06_25]] / [[atomic-swap-over-resumable-deferred-retry-stream]]) and a DEDICATED attended unit, wrong for an unattended tick. This snapshot is its baseline-preservation PRIMITIVE/foundation (R13 logical order: build the verifiable core first). It converts the SILENT count-regression footgun into a LOUD, recoverable one -- the R12 bug-class win -- without re-entering the P0 minefield.

**Scrutiny.** 3-of-3 all PASS; arm C (analyst) raised one P2 (the snapshot's unwrapped read/write was a NEW throw surface on a path that previously only wrote two empties -- the exact V8-string-cap class that destroyed the tribal brain twice) -> remediated inline as the fail-soft commit `c2e81dc477` (operator "fix inline, don't defer" + R16). +1 R9 fail-soft test (a throwing readImpl must NOT propagate).

**Lesson.** A safety net that PRESERVES state before a destructive op must itself be fail-soft (never block the op it guards) AND shrink-guarded (a partial re-run must not clobber the good baseline) -- and "snapshot before truncate" is the cheap, certain-to-land foundation that de-risks the harder atomic-swap without shipping its P0. The 4 UNWIRED engines this tick are NOT papa-ownable: SFCInferenceGateWireEngine=oscar WIP, BlueprintOCRAdapter=xray WIP, AuthEngineV7=HTTP-layer JWT/credential engine (type-only consumed; do NOT MCP-wire), PreMOUKickoffChecklistEngine=hotel business/legal MOU-gate. -> [[reference_papa_tribal_corpus_lora_2026_06_25]] · [[atomic-swap-over-resumable-deferred-retry-stream]] · sibling [[reference_tribal_index_v8_string_cap_2026_06_08]]
