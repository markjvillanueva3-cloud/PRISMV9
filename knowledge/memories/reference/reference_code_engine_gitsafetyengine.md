---
name: reference_code_engine_gitsafetyengine
description: "U-FORE-14 (New-Coder Safety Net)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/GitSafetyEngine.ts
  sourceHash: 66927941
  loc: 179
aliases: GitSafetyEngine
---

# GitSafetyEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/GitSafetyEngine.ts` (179 LOC)

## What it is
U-FORE-14 (New-Coder Safety Net)

```
GitSafetyEngine — U-FORE-14 (New-Coder Safety Net)
===================================================

Classifies a git command before it runs. Flags destructive flags
(`--force`, `--hard`, `-D`, `clean -fd`), explains what they do in
plain language, and emits a confirmation prompt for the user.

The engine does NOT execute git. It only returns a safety verdict.
Hooks (bash-destructive-guard.mjs) are responsible for enforcement.

Inputs  → { command: string, branch?: string }
Outputs → {
  safe,
  severity: "ok"|"warn"|"block",
  reasons[],                // why we flagged it
  plainLanguageImpact,      // what will happen in one sentence
  confirmationPrompt?,      // question to ask the user
  saferAlternative?         // command that achieves the same goal with less risk
}
```


## Exports
`GitSafetyInput` (interface), `GitSafetyVerdict` (interface), `GitSafetyEngine` (class), `gitSafetyEngine` (const)

## Key methods
`classify`, `isDestructive`, `ruleIds`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/GitSafetyEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find GitSafetyEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/GitSafetyEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
