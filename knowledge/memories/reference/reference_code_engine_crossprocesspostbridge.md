---
name: reference_code_engine_crossprocesspostbridge
description: "CrossProcessPostBridge -- unified post-processor (NC emission) entry across"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CrossProcessPostBridge.ts
  sourceHash: 3b52f753
  loc: 135
aliases: CrossProcessPostBridge
---

# CrossProcessPostBridge -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CrossProcessPostBridge.ts` (135 LOC)

## What it is
CrossProcessPostBridge -- unified post-processor (NC emission) entry across

```
CrossProcessPostBridge -- unified post-processor (NC emission) entry across
mill, lathe, and wire-EDM, delegating to the canonical
MasterPostProcessorUnifiedAGIEngine.generatePost.

Sibling "post" bridge referenced by CrossProcessAIBridge (@see XPROC-POST-01)
and consumed by ProcessIntelligenceRouterEngine's pipeline `post` stage.
PURE ROUTING + DELEGATION: it emits NO G-code itself -- every byte of NC comes
from the already-built, already-tested masterPostProcessorUnifiedAGIEngine,
which is the doctrinal single entry for ALL NC emission (never ad-hoc string
concatenation -- see feedback_echo_masterpost_pipeline_route). Process
classification reuses the verified CrossProcessAIBridge.classify scorer.

One entry point:
  emit(req) -- classify + delegate to MasterPost.generatePost (or dry-run
               preview that returns only the routing decision).

Fail-loud (R12): a non-dry-run cal
```


## Exports
`CrossProcessPostRequest` (interface), `CrossProcessPostResult` (interface), `CrossProcessPostBridge` (class)

## Key methods
`emit`

## Depends on
[[reference_code_engine_crossprocessaibridge]]

## Where it lives
- **Source:** `mcp-server/src/engines/CrossProcessPostBridge.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CrossProcessPostBridge` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CrossProcessPostBridge.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
