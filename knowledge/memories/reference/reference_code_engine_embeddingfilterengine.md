---
name: reference_code_engine_embeddingfilterengine
description: "Embedding-scored companion to SituationalAwarenessFilter"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/EmbeddingFilterEngine.ts
  sourceHash: 50c1bf8d
  loc: 218
aliases: EmbeddingFilterEngine
---

# EmbeddingFilterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/EmbeddingFilterEngine.ts` (218 LOC)

## What it is
Embedding-scored companion to SituationalAwarenessFilter

```
EmbeddingFilterEngine — Embedding-scored companion to SituationalAwarenessFilter

Phase 0.13 U-SAW5 upgrade path. The baseline SituationalAwarenessFilterEngine
scores directive lines by token Jaccard. This engine does the same job but
uses cosine similarity on real sentence embeddings produced by an injected
embedder (typically LocalEmbeddingEngine or the Ollama client). Returns the
same output shape so callers can swap them at runtime based on embedder
health.

Design rules:
  - Async, because embedding is I/O-bound (even "in-process" ONNX has
    measurable per-line cost).
  - Batches every non-blank directive line + the prompt into one embed
    call when the embedder supports it, falling back to per-line embeds
    when it does not.
  - Always preserves original line order in the output (same invariant
    as the Jaccard engine).
  - Header bias still applies on top of cosine score s
```


## Exports
`EmbFilterOptions` (interface), `EmbFilterResult` (interface), `FilterEmbedder` (interface), `EmbeddingFilterEngine` (class)

## Key methods
`embed`, `filter`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/EmbeddingFilterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find EmbeddingFilterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/EmbeddingFilterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
