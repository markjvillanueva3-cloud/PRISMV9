---
name: reference_code_lib_hsmentrygeometryemit
description: "hsm-entry-geometry-emit.mjs — HSM (High Speed Machining) entry-"
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/hsm-entry-geometry-emit.mjs
  sourceHash: 0c07afc0
  loc: 250
aliases: hsm-entry-geometry-emit.mjs
---

# hsm-entry-geometry-emit.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/hsm-entry-geometry-emit.mjs` (250 LOC)

## What it is
```
hsm-entry-geometry-emit.mjs — HSM (High Speed Machining) entry-
geometry pre-emit refuse gate. Encodes the canonical operator wisdom
for how an end mill is allowed to engage material in HSM contexts:
  - Helix entry  — 1° ≤ angle ≤ 3° (tribal wisdom)
  - Ramp entry   — angle ≤ 2°
  - Plunge entry — REFUSED (HSM never plunges into solid material)
  - Lead-in arc  — radius ≥ 0.1 × tool_diameter

Why "HSM entry geometry at emit":
  Sheared end mills are the #1 cause of unscheduled-spindle-stop in
  HSM shops. Almost always the failure mode is wrong entry geometry:
  straight plunge, helix at 5° (too steep), or ramp at 4° (too steep
  for HSM cutter coatings to survive). The pitfall is silent —
  nothing in the CAM-output catches it; the operator finds out when
  the spindle alarm trips. This lib is a pre-emit hard gate that
  refuses to emit programs with bad entry geometry, emitting BLOCK

```


## Exports
`HSM_ENTRY_GEOMETRY_EMIT_SCHEMA_VERSION` (const), `DEFAULT_DECIMAL_PLACES` (const), `HELIX_MIN_ANGLE_DEG` (const), `HELIX_MAX_ANGLE_DEG` (const), `RAMP_MAX_ANGLE_DEG` (const), `ARC_LEAD_IN_MIN_RADIUS_FRACTION` (const), `SUPPORTED_ENTRY_KINDS` (const), `SUPPORTED_DIALECTS` (const), `VERDICT_PASS` (const), `VERDICT_BLOCK` (const), `formatComment` (function), `validateHelixEntry` (function), `validateRampEntry` (function), `validatePlungeEntry` (function), `validateArcLeadIn` (function), `validateEntry` (function), `validateEntrySequence` (function), `formatEntryVerdictLine` (function), `emitHSMEntryReport` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/hsm-entry-geometry-emit.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find hsm-entry-geometry-emit.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/hsm-entry-geometry-emit.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
