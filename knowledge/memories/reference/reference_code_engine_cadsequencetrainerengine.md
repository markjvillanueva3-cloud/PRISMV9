---
name: reference_code_engine_cadsequencetrainerengine
description: "CADCAM-DAGI-MS0/U-DAGI04"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADSequenceTrainerEngine.ts
  sourceHash: b2f4a74d
  loc: 415
aliases: CADSequenceTrainerEngine
---

# CADSequenceTrainerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADSequenceTrainerEngine.ts` (415 LOC)

## What it is
CADCAM-DAGI-MS0/U-DAGI04

```
CADSequenceTrainerEngine — CADCAM-DAGI-MS0/U-DAGI04

Transformer-style CAD language-model **training orchestrator**. Consumes
the token streams produced by U-DAGI01 (tokenize) + U-DAGI03 (corpus)
and drives a backend through batching, deterministic 90/10 splitting,
LoRA adapter lifecycle, convergence checks, and checkpoint ledgers.

Architecture — pure controller, injectable ModelBackend:
  The engine owns no neural tensors. A `ModelBackend` plug-in performs
  the forward/backward pass and parameter updates. Tests supply a tiny
  deterministic backend (count-table Markov model); production wires a
  real HuggingFace / llama.cpp / Ollama runtime here. This keeps the
  orchestrator unit-testable and decouples training policy from the
  underlying math library.

Key methods:
  splitCorpus(tokens, ratio, seed)   — deterministic 90/10 train/val split
  registerAdapter(config)            — dec
```


## Exports
`TokenSeq` (type), `TrainingBatch` (interface), `LoRAConfig` (interface), `TrainingConfig` (interface), `ModelBackend` (interface), `EpochMetrics` (interface), `TrainingResult` (interface), `DetokenizerFn` (type), `CADSequenceTrainerEngine` (class), `cadSequenceTrainerEngine` (const), `mulberry32` (re-export), `shuffleInPlace` (re-export)

## Key methods
`getParamCount`, `updateOnBatch`, `scoreSequence`, `predictNext`, `serializeCheckpoint`, `loadCheckpoint`, `getCapabilities`, `validate`, `registerAdapter`, `getAdapter`, `listAdapters`, `splitCorpus`, `makeBatches`, `trainEpoch`, `evaluate`, `train`

## Depends on
[[reference_code_engine_baseengine]] | [[reference_code_engine_iengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADSequenceTrainerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADSequenceTrainerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADSequenceTrainerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
