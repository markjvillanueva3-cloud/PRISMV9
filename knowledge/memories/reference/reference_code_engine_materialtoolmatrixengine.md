---
name: reference_code_engine_materialtoolmatrixengine
description: "scenario generator (U-CAMTEST08..13). The matrix itself is 9 abstract"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MaterialToolMatrixEngine.ts
  sourceHash: 9270413e
  loc: 226
aliases: MaterialToolMatrixEngine
---

# MaterialToolMatrixEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MaterialToolMatrixEngine.ts` (226 LOC)

## What it is
scenario generator (U-CAMTEST08..13). The matrix itself is 9 abstract

```
MaterialToolMatrixEngine — U-CAMTEST07
=======================================

PHASE-8: 3 × 3 (material × tool) slot matrix used by every PHASE-8
scenario generator (U-CAMTEST08..13). The matrix itself is 9 abstract
slot positions; materialization against a fixture part produces 9
concrete (material_id, tool_id) tuples via the part's
recommended_materials array and a per-category tool-class table.

Per scenario generator: 9 combos × 100 stock setups = 900 scenarios per
host. Per category, the materialization is deterministic — same part in
+ same matrix slots out = same combos out.

Tool class table (3 tool slots per part category):
  pocket_2d   — roughing_endmill, finishing_endmill, chamfer_mill
  contour_2d  — roughing_endmill, finishing_endmill, engraving_mill
  drilling    — spot_drill,       drill,             chamfer_mill
  threading   — tap_drill,        tap,               threa
```


## Exports
`MatrixSlotIndexSchema` (const), `MatrixSlotIndex` (type), `ToolClassSchema` (const), `ToolClass` (type), `MatrixSlotSchema` (const), `MatrixSlot` (type), `MaterialToolComboSchema` (const), `MaterialToolCombo` (type), `MaterialToolMatrixEngine` (class), `materialToolMatrixEngine` (const)

## Key methods
`slots`, `toolClassesFor`, `comboesForPart`, `getCombo`, `mustGetCombo`, `allCombos`, `expectedComboCount`, `auditMatrix`

## Depends on
[[reference_code_engine_fixturepartcatalogengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/MaterialToolMatrixEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MaterialToolMatrixEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MaterialToolMatrixEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
