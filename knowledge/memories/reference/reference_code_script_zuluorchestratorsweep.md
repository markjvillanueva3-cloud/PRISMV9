---
name: reference_code_script_zuluorchestratorsweep
description: "ZULU-ORCHESTRATOR-MS0 / U-ZULU02 — sweep CLI (the body)."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/zulu-orchestrator-sweep.mjs
  sourceHash: 8ddfed4e
  loc: 746
aliases: zulu-orchestrator-sweep.mjs
---

# zulu-orchestrator-sweep.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/zulu-orchestrator-sweep.mjs` (746 LOC)

## What it is
```
ZULU-ORCHESTRATOR-MS0 / U-ZULU02 — sweep CLI (the body).

One pass over every opt-in chat slot: read pressure (CHO02), make decision
(CHO01), resolve the target HWND by the stable `PRISM <slot>` window caption
(G1b / U-ZM1-05 — title-based, not PID-based), and — for executable plans —
SendKeys the slash + follow-up /checkin-<slot> backend-dev-priority directive
(U-CHO04 + U-ZULU05) into the target PowerShell window with a 5s stagger.

Knobs (cascade in this order — most specific wins):
  PRISM_ZULU_DISABLE=1       — hard off; no slots eligible.
  PRISM_ZULU_DRY_RUN=1       — gate every plan to dry-run (PS -Confirm:$false).
  PRISM_SENDKEYS_DISABLE=1    — PS script self-aborts; lib also pre-downgrades.
  PRISM_ZULU_LOG=<path>      — override log path (default state/shared/zulu-orchestrator-log.jsonl).
  PRISM_ZULU_STAGGER_MS=N    — override default 5000ms stagger (clamp ≥5000).
  PRISM_ZU
```


## Exports
`reapLoopLedger` (function), `sweepOnce` (re-export), `parseArgs` (re-export), `sendLines` (re-export), `staggerMs` (re-export)

## Key methods
`applyOptInToSlotsDoc`, `appendLog`, `formatLogEntry`, `main`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/zulu-orchestrator-sweep.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find zulu-orchestrator-sweep.mjs` or `prism_session:master_index_query`; open the source at `scripts/zulu-orchestrator-sweep.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
