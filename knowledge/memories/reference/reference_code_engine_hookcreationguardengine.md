---
name: reference_code_engine_hookcreationguardengine
description: "HOOK-SYNERGY-MS0 / U-HOOK-CREATION-GATE (H5)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HookCreationGuardEngine.ts
  sourceHash: eeaa128e
  loc: 445
aliases: HookCreationGuardEngine
---

# HookCreationGuardEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HookCreationGuardEngine.ts` (445 LOC)

## What it is
HOOK-SYNERGY-MS0 / U-HOOK-CREATION-GATE  (H5)

```
HookCreationGuardEngine — HOOK-SYNERGY-MS0 / U-HOOK-CREATION-GATE  (H5)

Hook-domain mirror of {@link DuplicationGuardEngine}. Where the generic guard
blocks on (assetType, name) collisions across all asset families, this engine
adds the two dimensions only hooks have:
  1. **event** — Claude Code lifecycle event (PreToolUse, Stop, …);
  2. **matcher** — the tool-name matcher regex (`^Bash$`, `^(Edit|Write)$`, …).

The (event, matcher) tuple is the **signature** of a hook. Two hooks with the
same signature compete on the same harness trigger, and "another `^Bash$`
PreToolUse hook?" is a real duplication-risk class the existing name-only
guards (`ai-duplication-guard`, `duplication-hard-block`) cannot see.

Inputs come from a {@link HookManifest} produced by HookManifestEngine
(P0-U01) or the equivalent {@link HookCatalogEntry[]} read from
`state/shared/HOOK_REGISTRY.json` (the .mjs-frien
```


## Exports
`HookCatalogEntry` (interface), `entriesFromManifest` (function), `entriesFromRegistry` (function), `HookCreationGuardOptions` (interface), `HookCreationGuardEngine` (class), `hookCreationGuardEngine` (const)

## Key methods
`getCapabilities`, `validate`, `loadCatalog`, `checkBeforeCreating`, `mustCheckBeforeCreating`

## Depends on
[[reference_code_engine_baseengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/HookCreationGuardEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HookCreationGuardEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HookCreationGuardEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
