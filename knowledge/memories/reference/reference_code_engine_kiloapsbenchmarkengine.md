---
name: reference_code_engine_kiloapsbenchmarkengine
description: "KiloApsBenchmarkEngine — KILO-CAM-TRAINING-MS0 campaign iter 2"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/KiloApsBenchmarkEngine.ts
  sourceHash: 5abb5dc4
  loc: 200
aliases: kiloApsBenchmarkEngine
---

# kiloApsBenchmarkEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/KiloApsBenchmarkEngine.ts` (200 LOC)

## What it is
```
KiloApsBenchmarkEngine — KILO-CAM-TRAINING-MS0 campaign iter 2

Measures Autodesk Platform Services (APS, formerly Forge) REST API
throughput against the JM-Die corpus. Used to validate that Mode 1
(APS REST) is actually faster than Mode 3 (Fusion add-in) before
committing the training pipeline to it.

Pure-function bench layer:
  - inputs:  array of file-IDs + per-call latency samples
  - outputs: throughput stats (p50/p95/p99 latency, files/sec, ETA for full corpus)

The HTTP-calling layer lives in FusionCloudConnectorEngine + AutoForgeEngine
(existing); this engine consumes their measured timings and produces the
comparison report. Pure functions are unit-testable without network calls.

Per kilo refuse-list:
  - no-silent-fallback: when latency samples are empty, return
    `{ valid: false, gap: "no samples" }` — NEVER fabricate a default
  - dropping-tolerance-stack: percentile calc
```


## Exports
`LatencySampleSchema` (const), `LatencySample` (type), `ThroughputStatsSchema` (const), `ThroughputStats` (type), `ModeComparisonSchema` (const), `ModeComparison` (type), `kiloApsBenchmarkEngine` (const)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/KiloApsBenchmarkEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find kiloApsBenchmarkEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/KiloApsBenchmarkEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
