---
name: reference_code_engine_cadcapabilitynegotiatorengine
description: "CADCapabilityNegotiatorEngine — CAD-COMPLETE-MS0 / U-CADC-AI03"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADCapabilityNegotiatorEngine.ts
  sourceHash: 2617bcf9
  loc: 500
aliases: UnsupportedCapabilityError
---

# UnsupportedCapabilityError -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADCapabilityNegotiatorEngine.ts` (500 LOC)

## What it is
```
CADCapabilityNegotiatorEngine — CAD-COMPLETE-MS0 / U-CADC-AI03

Resolves a CAD operation intent against the live CADAdapterRegistry and
decides which adapter can satisfy it. Returns a NegotiationResult that
either names the adapter to use (and what ops, if any, it cannot cover)
or throws UnsupportedCapabilityError when no policy-compatible adapter
is available.

Acceptance (envelope):
  "Intent referencing unsupported op either degrades gracefully
   (configurable) or throws UnsupportedCapabilityError with named op."

Design:
  - Pure read of CADAdapterRegistry — never mutates registry state.
  - Awaits adapter capability matrices lazily (the registry returns
    adapter singletons via dynamic import; we cache the capability
    snapshot per call so repeated lookups across a single negotiate()
    don't re-instantiate).
  - Three policies:
      "strict"   — only the preferred adapter; t
```


## Exports
`NEGOTIATION_POLICIES` (const), `NegotiationPolicy` (type), `DEFAULT_NEGOTIATION_POLICY` (const), `NegotiationIntent` (interface), `AdapterCandidate` (interface), `NegotiationResult` (interface), `UnsupportedCapabilityError` (class), `CADCapabilityNegotiatorEngine` (class), `cadCapabilityNegotiatorEngine` (const), `UnknownCADSystemError` (re-export)

## Key methods
`negotiate`, `negotiateOrThrow`, `listGaps`

## Depends on
[[reference_code_engine_cadadapterregistry]] | `ICADCodeGenerator`

## Where it lives
- **Source:** `mcp-server/src/engines/CADCapabilityNegotiatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find UnsupportedCapabilityError` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADCapabilityNegotiatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
