---
name: reference_code_script_populateqdrantmemories
description: "HMEMV09 -- seed the 17K Obsidian-vault memory embeddings into a Qdrant"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/populate-qdrant-memories.mjs
  sourceHash: 93b1540f
  loc: 121
aliases: populate-qdrant-memories.mjs
---

# populate-qdrant-memories.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/populate-qdrant-memories.mjs` (121 LOC)

## What it is
```
HMEMV09 -- seed the 17K Obsidian-vault memory embeddings into a Qdrant
collection so memory recall can use ANN instead of the per-query linear int8
scan over `state/shared/memory-embeddings-sidecar.json`. Producer increment
(R13 verifiable core); the consumer rewire of denseRankAll is a follow-up.

Clone-don't-fork of scripts/populate-qdrant.mjs: reuses its populateQdrant()
(batched upsert + curl HTTP) via the loadImpl injection. The only new part is
the loader, because the memory sidecar stores each vector as a BASE64-encoded
int8[768] string (`vec`) + an L2 `norm`, NOT a plain int8 array like the
node-embeddings jsonl. We base64-decode -> signed Int8Array -> {n,q}; the
reused buildBatch dequantizes q/127 to floats. Distance is Cosine, so the
per-vector magnitude (norm) is irrelevant and intentionally dropped.

Flags: --collection NAME (default prism_memories) --batch-size N --limit N
 
```


## Exports
`decodeInt8Vec` (function), `loadMemorySidecar` (function), `ensureCollection` (function)

## Key methods
`curlJson`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/populate-qdrant-memories.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find populate-qdrant-memories.mjs` or `prism_session:master_index_query`; open the source at `scripts/populate-qdrant-memories.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
