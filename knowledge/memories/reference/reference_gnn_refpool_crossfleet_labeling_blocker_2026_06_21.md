---
name: reference_gnn_refpool_crossfleet_labeling_blocker_2026_06_21
description: "PSN-leg-10 (GNN full-coverage) blocker characterized (slot:india 2026-06-21): the active-learning worklist's 33 ghosts need per-DOMAIN ground-truth dispatcher labels; the macro-F1-lever classes (prism_data/edm/session/cad/5axis/safety) are starved and need their DOMAIN OWNERS to label -- india-solo can only label AI-domain ghosts (prism_ai, NOT starved) so cannot move the gate alone. Actionable: cross-fleet per-slot labeling pass."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.592Z
aliases: reference_gnn_refpool_crossfleet_labeling_blocker_2026_06_21
---


**GNN deploy state (verified via `nn-graph-retrain-lifecycle.mjs --status`, 2026-06-21):** deployed direct-embed holdout AUROC **0.789** / macroF1 0.4101 / Brier 0.1887 (n=84); **selective @tau=0.7 is DEPLOY-READY** (coverage 27.4%, Brier 0.0417, macroF1 1.0, robust). Last retrain not-promoted (0.3512 < 0.78 gate -- correct, never promote sub-gate). Drift baseline 346,835 nodes / 800,041 edges / 355 ghosts. Live link-pred checkpoint AUROC 0.096 is the PRETEXT metric, NOT the deploy gate.

**THE FULL-COVERAGE BLOCKER (precise):** `state/shared/nn-graph/active-label-worklist.md` (generated by `scripts/lib/gnn-active-pool-select.mjs`) lists **33 unlabeled ghost targets**; ref pool = **178 labelled ghosts / 14 dispatcher classes**. Workflow: label each ghost's CORRECT dispatcher -> `scripts/vault-to-gnn-refpool.mjs` -> `nn-graph-retrain-lifecycle` lifts macro-F1. The **most label-starved classes (biggest macro-F1 levers)** are `prism_data`(1), `prism_edm`(2), `prism_session`(2), `prism_cad`(2), `prism_business`(4), `prism_quoting`(4), `prism_5axis`(4), `prism_safety`(5).

**WHY india-solo CANNOT move the gate (the real reason it's "multi-session"):** correct labeling needs PER-DOMAIN ground truth that the DOMAIN OWNERS hold, not india. The worklist's "predicted dispatcher" column is the GNN's low-conf WRONG guess (that's why they're high-uncertainty targets) -- e.g. `GeminiClientEngine`/`GrokClientEngine` predicted prism_5axis/prism_quoting (truly prism_ai), `TurretLayoutEngine` predicted prism_safety (truly prism_turning/whiskey), `LokiLogSinkEngine` predicted prism_turning (truly prism_dev/infra). India can fabrication-free-label only its OWN AI-domain ghosts (LLM clients -> prism_ai; TPEHyperparameterSearch/MOEAStoppingCriterion -> prism_ai), but `prism_ai` already has 20 refs (NOT starved), so labeling them yields ~0 macro-F1 lift. The STARVED high-lever classes belong to other slots: SFC engines -> oscar (prism_calc/sfc), CAD (`cadLiveDispatch`/`CadPartLibraryEngine`) -> delta (prism_cad), lathe (`TurretLayoutEngine`/`GrooveClassificationEngine`) -> whiskey (prism_turning), quoting (`SBOMReviewEngine`/`HzpDashAuditEngine`) -> charlie (prism_quoting). India must NOT guess these -- mislabeling corrupts the ref-pool (worse than abstaining); the india soul forbids fabricating labels/metrics.

**ACTIONABLE PATH (operator/fleet, not solo-india):** a CROSS-FLEET labeling pass -- each domain slot labels its own ghosts in `active-label-worklist.md` (oscar the SFC rows, delta the CAD rows, whiskey the lathe rows, charlie the quoting rows, india the AI rows), feed all via `vault-to-gnn-refpool.mjs`, then india runs `nn-graph-retrain-lifecycle --force` and reports the new macro-F1/AUROC (metrics-gated, promote IFF AUROC>=0.78/macroF1>=0.55/Brier<=0.15). Until the starved classes get domain-owner labels, the full-coverage gate stays blocked while selective-deploy @tau=0.7 remains the validated production path. Sibling: [[reference_india_ai_red_batch_2026_06_21]]; doctrine [[gnn-selective-deploy]] · [[feedback_multiseed_before_auroc_claim]].
