---
name: reference_code_lib_galaxydensererank
description: "galaxy-dense-rerank.mjs -- optional DENSE (embedding) rerank arm for the galaxy"
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/galaxy-dense-rerank.mjs
  sourceHash: 885a608f
  loc: 225
aliases: galaxy-dense-rerank.mjs
---

# galaxy-dense-rerank.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/galaxy-dense-rerank.mjs` (225 LOC)

## What it is
```
galaxy-dense-rerank.mjs -- optional DENSE (embedding) rerank arm for the galaxy
reasoning bridge, completing the sparse+dense RAG hybrid (AI-SYNERGY-AUDIT-MS0/
U-AISYN-DENSE, slot:charlie).

Two-stage hybrid: the sparse lexical retriever (galaxy-context-retrieval.mjs) selects
candidate sections; this arm embeds the top-M candidates + the query (local Ollama
nomic-embed-text, 768d), reranks by cosine, and FUSES the sparse and dense rankings via
Reciprocal Rank Fusion -- REUSING the fleet's verified scripts/lib/hybrid-retrieval.mjs
`rrfMerge` (R8/dedup: not a new fusion impl). It is strictly OPTIONAL + fail-soft: with
no embedding service it returns null and the bridge keeps the sparse result (zero
regression). Enabled via PRISM_GALAXY_RAG_DENSE=1.

cosineSim / denseRankChunks / fuseSparseDense are PURE (vectors injected) so the fusion
math is reference-value testable (R9); embedText + hyb
```


## Exports
`buildCandidatePool` (function), `chunkId` (function), `cosineSim` (function), `denseRankChunks` (function), `fuseSparseDense` (function), `embedText` (function), `hybridRetrieve` (function)

## Key methods
`clearTimeout`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/galaxy-dense-rerank.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find galaxy-dense-rerank.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/galaxy-dense-rerank.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
