---
name: reference_foxtrot_mill_wire_status_2026_06_11
description: Foxtrot mill backend is 198/204 dispatcher-wired (NOT an orphan field). 1 genuine R12-safe DATA orphan wired (hyperMILL fixture catalog). Two load-bearing lessons - dynamic-import dark-detection + shared-tree clobber.
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.580Z
aliases: reference_foxtrot_mill_wire_status_2026_06_11
---


**FOXTROT MILL BACKEND WIRE STATUS (slot:bravo cross-galaxy, 2026-06-11). Operator: "do it for foxtrot ... make sure everything backend is wired ... accelerate self-learning for milling."** Same orphan-wire axis as [[reference_sfc_orphan_wire_sweep_2026_06_11]] / [[reference_india_ai_orphan_wire_2026_06_11]].

**HONEST STATUS (R12, evidence-based): the mill backend is ALREADY 198/204 dispatcher-wired (97%) -- NOT an orphan field (mirrors echo's true-dark=0).** foxtrot did thorough wiring: `millDispatcher.ts` (`prism_mill`, 394 actions) pulls engines via **308 dynamic `await import`** lazy-loaders + 129 `getEngine()` calls, NOT static imports (0). The named foxtrot LoRA targets (`millingLoRADatasetBuilderEngine`/`millTurnLoRADatasetBuilderEngine`) are ALREADY wired (`mill_lora_build_dataset`/`millturn_lora_build_dataset`, millDispatcher:185).

**LESSON #1 -- dark-detection method for dynamic-import galaxies (load-bearing).** A `\b<singletonName>\b`-against-dispatcher grep is the WRONG dark-detector when a dispatcher wires via `(await import("../../engines/<Base>.js")).<singleton>`. My first node scan with that method false-flagged **193/204 "dark"** (it even put the provably-wired `adaptiveMillingChipLoadMonitorEngine` -- in safetyDispatcher -- in the dark list). The CORRECT detector is the **PascalCase basename as a substring** (`dispBlob.includes("<Base>")`) -- it catches the `import("...<Base>.js")` path. That gave the trustworthy 198 WIRED / 6 TRUE-DARK (sanity-checked: both proven-wired basenames present in blob). Also: `scripts/mill-wiring-audit.mjs` does NOT exist (MODULE_NOT_FOUND) -- a stale/hallucinated script ref; verify a script exists before relying on it (R8). **NOT a fleet bug (verified):** the CANONICAL detectors already handle dynamic imports correctly -- `scripts/audit-unwired-engines.mjs` (docs "literal dynamic import: await import('...EngineName.js')", basename-anchored) + `.claude/hooks/stop_on_unwired_assets.mjs:193-197` (checks `${singleton}` OR `/${moduleName}.js` OR `/${moduleName}"`). So the real takeaway is **use the canonical `audit-unwired-engines.mjs` / `stop_on_unwired_assets` detector, NOT an ad-hoc singleton-name grep** -- they were right all along; my grep was the wrong tool.

**The 6 true-dark classified (R8 -- read each engine's API):** 1 wired, 5 legit-exempt.
- **WIRED: `MonolithHyperMillFixtureDatabaseEngine`** -> `prism_mill` 8 `mill_hm_fixture_*` actions (`b4bdf8f699`, 13/13, 2-agent PASS). Pure in-memory const catalog (6 vises/7 chucks/3 clamp families) + monolith-faithful threshold auto-select (`listVises/listChucks/listClamps/getVise/getChuck/autoSelect/search/stats`). Its `search()` was already transitively reachable via `CatalogUnifiedQueryEngine`->`intelligenceDispatcher`, but the structured catalog + part-dims auto-select were exposed NOWHERE. Non-dup of the physics `fixture_*`/`workholding_*` force calculators (clamp force/deflection vs catalog DATA). Serves the directive's "work-holding/fixturing/tool-holders." `slimResponse` strips null/empty-array keys -> null-selection observable via explicit `*_found` booleans (false survives).
- **EXEMPT (5):** `HyperMillACBridgeEngine` + `HyperMillACScriptExecutor` (live hyperMILL Automation-Center process control: start/stop/execute -- side-effectful, not DATA, CAM/kilo lane); `MillingReasoningDefaultEngine` (`// WIRE-EXEMPT: internal 5-step reasoning wrapper consumed by MillingAGIMasterEngine` -- inference middleware); `CounterfactualMillEngine` (what-if inference); `HyperMillResourceIndexEngine` (already `// WIRE-EXEMPT`, no clean singleton export).

**LESSON #2 -- shared-tree clobber (load-bearing, cost me a full re-apply).** I edited `millDispatcher.ts` in the SHARED `H:/prism` tree (on `cad-fusion-live-ms0`, where foxtrot also writes) and left it UNCOMMITTED for ~15min during recon + 2 review agents. **A peer (foxtrot) clobbered my uncommitted edits** (grep -> 0 occurrences; `git status` clean = absorbed/reverted). Recovery: re-applied via an ATOMIC node `indexOf`-splice (4 edits in one write -- smallest re-clobber window) + committed IMMEDIATELY (`b4bdf8f699`). The slot worktree `H:/prism-slot-bravo` (slot/bravo) was NOT a viable fallback: **3467 commits behind** cad-fusion-live-ms0 (96 vs 394 MILL_ACTIONS; the 2026-05-26 target engine + my anchors absent). So `[BOOTSTRAP-SLOT-ENFORCE]` shared-tree is the only lane for a current-base wire -- but **commit FAST** (don't interleave long recon/review between edit and commit). This is the [[feedback_commit_to_slot_worktree]] hazard in its shared-tree form; the mitigation when the slot branch is too stale is edit->build->test->commit in one tight sequence. → [[feedback_bravo_commit_to_slot_branch]]

**Anchor-drift gotcha:** the reverted (foxtrot) file had `}\n          default:` where my original anchor had `}\n\n          default:` (blank line) -- re-apply patches must match the CURRENT file's exact whitespace, not a remembered version.

Galaxy-brain link-in (`mcp-server/src/engines/mill/MEMORY.md`) pending a fast batched commit (kept out of the clobber-prone same-session window). Cross-session: the false-WIRE-EXEMPT + dynamic-import-dark-detection patterns recur fleet-wide -- a basename-substring sweep is the right tool for any dynamic-import dispatcher galaxy.
