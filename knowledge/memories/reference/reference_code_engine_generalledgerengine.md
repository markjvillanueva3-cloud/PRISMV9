---
name: reference_code_engine_generalledgerengine
description: "GeneralLedgerEngine — Double-entry bookkeeping for the PRISM ERP layer"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/GeneralLedgerEngine.ts
  sourceHash: 5f56d6de
  loc: 889
aliases: generalLedgerEngine
---

# generalLedgerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/GeneralLedgerEngine.ts` (889 LOC)

## What it is
```
GeneralLedgerEngine — Double-entry bookkeeping for the PRISM ERP layer

Implements a real general ledger with:
  - Standard chart of accounts (manufacturing-oriented)
  - Double-entry journal entries (sum(debits) === sum(credits))
  - Structured entry recorders for common business events:
      recordInvoice, recordPayment, recordPurchase, recordPayroll,
      recordWipToCogs, createJournalEntry (generic)
  - Derived financial statements:
      getTrialBalance, getIncomeStatement, getBalanceSheet

Accounting invariants (enforced):
  - Every journal entry balances: Σdebits === Σcredits
  - Trial balance total debits === total credits (assuming balanced entries)
  - Accounting equation: Assets = Liabilities + Equity + (Revenue − Expenses)

Persistence:
  state/shared/general-ledger-state.json with schemaVersion=1.
  Atomic write via utils/atomicSessionWrite.atomicWriteJson.

References:
  
```


## Exports
`AccountType` (type), `NormalBalance` (type), `Account` (interface), `CHART_OF_ACCOUNTS` (const), `JournalLineSchema` (const), `JournalLine` (type), `CreateJournalEntryInputSchema` (const), `RecordInvoiceInputSchema` (const), `RecordPaymentInputSchema` (const), `RecordPurchaseInputSchema` (const), `RecordPayrollInputSchema` (const), `RecordWipToCogsInputSchema` (const), `JournalEntry` (interface), `LedgerState` (interface), `TrialBalanceRow` (interface), `TrialBalance` (interface), `IncomeStatement` (interface), `BalanceSheet` (interface), `generalLedgerEngine` (const), `GeneralLedgerEngine` (re-export)

## Key methods
`getChartOfAccounts`, `createJournalEntry`, `recordInvoice`, `recordPayment`, `recordPurchase`, `recordPayroll`, `recordWipToCogs`, `getTrialBalance`, `getIncomeStatement`, `marginTrends`, `cashFlowSummary`, `revenueForecast`, `getBalanceSheet`, `atomicWriteJson`, `mkdirSync`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/GeneralLedgerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find generalLedgerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/GeneralLedgerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
