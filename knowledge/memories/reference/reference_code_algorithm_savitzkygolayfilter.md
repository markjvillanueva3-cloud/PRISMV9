---
name: reference_code_algorithm_savitzkygolayfilter
description: "SavitzkyGolayFilter — polynomial-least-squares smoothing (and differentiation)"
metadata:
  type: reference
  kind: code-algorithm
  galaxy: unassigned
  sourcePath: mcp-server/src/algorithms/SavitzkyGolayFilter.ts
  sourceHash: fc6c0d63
  loc: 241
aliases: SavitzkyGolayFilter
---

# SavitzkyGolayFilter -- Algorithm SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/algorithms/SavitzkyGolayFilter.ts` (241 LOC)

## What it is
```
SavitzkyGolayFilter — polynomial-least-squares smoothing (and differentiation)
of a uniformly-sampled 1-D signal (Savitzky & Golay 1964).

Over a sliding odd window of length w, a degree-p polynomial is fit by least
squares and evaluated at each point. Unlike a moving average, this PRESERVES
peak height/width and higher moments — the standard smoother for instrument /
sensor / telemetry traces (spindle load, vibration RMS, probe data) before
feature extraction, DTW alignment, or anomaly detection downstream.

Key identity (the strongest correctness invariant): a Savitzky-Golay filter of
order p reproduces ANY polynomial of degree ≤ p EXACTLY — including at the
boundaries, because each output is an exact local polynomial fit.

Implementation: the window x-grid is fixed (0..w-1) for every position, so the
normal-equations pseudo-inverse M = (VᵀV)⁻¹Vᵀ is computed ONCE; each output is
weight
```


## Exports
`SavGolInput` (interface), `SavGolOutput` (interface), `SavitzkyGolayFilter` (const)

## Key methods
`validate`, `calculate`, `getMetadata`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/algorithms/SavitzkyGolayFilter.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Algorithm

## How to query it
Recall by meaning via `/system-viz find SavitzkyGolayFilter` or `prism_session:master_index_query`; open the source at `mcp-server/src/algorithms/SavitzkyGolayFilter.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
