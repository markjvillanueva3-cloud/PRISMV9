---
name: reference_code_script_embedwikiintotribalindex
description: "embed-wiki-into-tribal-index.mjs"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/embed-wiki-into-tribal-index.mjs
  sourceHash: c050f40e
  loc: 599
aliases: embed-wiki-into-tribal-index.mjs
---

# embed-wiki-into-tribal-index.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/embed-wiki-into-tribal-index.mjs` (599 LOC)

## What it is
```
embed-wiki-into-tribal-index.mjs

BACKEND-DEV-LOOP/U-TRIBAL-EMBED-GAP (2026-05-19, slot foxtrot).

Idempotent appender that embeds wiki markdown files into
`state/shared/tribal-embed-index.json` so they become reachable by the
automatic tribal-knowledge injection pipeline
(`tribal-by-domain-inject.mjs` → `tribal-rerank.mjs` → cosine over the
index). An entry that is NOT in this index can NEVER be surfaced by that
hook, regardless of any wiki/cron regen — the rerank's whole corpus IS
this file's `entries[]`.

## Why this exists (the gap it closes)

The BACKEND-DEV-LOOP "U-TRIBAL-BACKEND-DEV-EXHAUST" loop's documented
pattern is: write tribal wiki → embed into tribal-embed-index → commit
(index + md together). Commit `d9f1b7960f` (iter3) followed it: its 6
wikis are all IN the index. Commit `d716d20a96` ("final 3 wikis …
exhaustion 20/20") shipped only the 3 .md files and **skipped the emb
```


## Exports
`INDEX_PATH` (const), `OLLAMA_URL` (const), `MODEL` (const), `MAX_EMBED_CHARS` (const), `TEXT_MAX` (const), `DEFAULT_DOMAIN` (const), `VALID_DOMAINS` (const), `stripFrontmatter` (function), `flattenBody` (function), `isEmbeddable` (function), `makeWinPath` (function), `makeId` (function), `contentHash` (function), `buildEntry` (function), `planAppend` (function), `spliceEntries` (function), `embedText` (function)

## Key methods
`emit`, `writeCacheHit`, `writeTribalIndexGuarded`, `main`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/embed-wiki-into-tribal-index.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find embed-wiki-into-tribal-index.mjs` or `prism_session:master_index_query`; open the source at `scripts/embed-wiki-into-tribal-index.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
