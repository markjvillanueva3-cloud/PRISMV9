---
name: reference_code_engine_dualchannelcontextengine
description: "Dual Channel Context"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/DualChannelContextEngine.ts
  sourceHash: dbd06da5
  loc: 435
aliases: DualChannelContextEngine
---

# DualChannelContextEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/DualChannelContextEngine.ts` (435 LOC)

## What it is
Dual Channel Context

```
DualChannelContextEngine -- GRAPH-AS-LLM-CONTEXT-MS0 / U-GAC04 (slot:sierra)

Builds a DUAL-CHANNEL context bundle for subagent dispatch: a structured JSON
channel (an ego-graph addressed by explicit `node-id:` markers) PLUS a visual
channel (a real PNG of the same ego-graph when a system Chrome/Edge is free,
else a mermaid+markdown "visual layer" fallback). The tldraw dual-channel
insight (2025): structured-data + a spatial/visual representation beats
prose-only context for multi-agent coordination -- a subagent that can SEE the
neighbourhood AND cite node-ids coordinates far better than one handed prose.

Composes (does NOT duplicate):
  - GraphContextLensEngine (U-GAC01) -- ego-graph extraction + mermaid/markdown render.
  - scripts/render-viz-screenshot.mjs (U-GAC04) -- system-Chrome headless PNG (no npm dep).

Design note on the viz channel: PNG rendering is BEST-EFFORT and capabili
```


## Exports
`DualMode` (type), `VizChannelKind` (type), `VizRenderResult` (interface), `DualChannelRenderer` (type), `DualChannelOpts` (interface), `JsonChannel` (interface), `VizChannel` (interface), `DualChannelBundle` (interface), `DualChannelContextEngine` (class), `dualChannelContextEngine` (const)

## Key methods
`attachJsonContext`, `assertNodeId`, `attachVizScreenshot`, `buildDualChannel`

## Depends on
[[reference_code_engine_graphcontextlensengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/DualChannelContextEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find DualChannelContextEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/DualChannelContextEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
