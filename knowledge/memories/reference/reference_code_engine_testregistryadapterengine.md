---
name: reference_code_engine_testregistryadapterengine
description: "Curated bank of realistic test inputs."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/TestRegistryAdapterEngine.ts
  sourceHash: 52d392d9
  loc: 329
aliases: TestRegistryAdapterEngine
---

# TestRegistryAdapterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/TestRegistryAdapterEngine.ts` (329 LOC)

> [physics] Imports canonical physics constants (`src/physics/constants.ts`) -- safety-relevant node.

## What it is
Curated bank of realistic test inputs.

```
TestRegistryAdapterEngine — Curated bank of realistic test inputs.

Purpose (TEST-LEGIT-MS1 U-INFRA05):
  Test generators need realistic physics values (material Kc, tool geometry,
  machine envelopes) to build inputs that exercise engine code paths.
  Loading the production registries for every test is slow (MaterialRegistry
  alone is 59KB of TypeScript + filesystem I/O), so this adapter keeps a
  small bank of canonical configurations sourced directly from
  src/physics/constants.ts.

Why "curated" and not "proxy":
  Tests benefit from STABILITY — if a downstream test asserts vc=180 m/min
  for steel, we do not want a production registry update to break it.
  The adapter pins a small set of well-known configurations with named
  references (e.g. STEEL_1045, AL_6061, CARBIDE_12MM_4FL, OKUMA_LATHE).

Policy:
  Physics constants (kc1_1, mc, Taylor) come from CANONICAL_KIENZLE /
  CANONIC
```


## Exports
`TestTool` (interface), `TestMachine` (interface), `TestFormulaRef` (interface), `TestRegistryAdapterEngine` (class), `testRegistryAdapterEngine` (const)

## Key methods
`getMaterialByISO`, `getMaterial`, `listMaterials`, `getTool`, `listTools`, `getMachine`, `listMachines`, `getFormulaRef`, `listFormulas`, `spanningISOGroups`, `spanningTools`, `spanningMachines`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/TestRegistryAdapterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find TestRegistryAdapterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/TestRegistryAdapterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
