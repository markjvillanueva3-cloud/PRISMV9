---
name: reference_code_engine_trilevelkillswitchengine
description: "Tri Level Kill Switch"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/TriLevelKillSwitchEngine.ts
  sourceHash: 30ed3009
  loc: 424
aliases: TriLevelKillSwitchEngine
---

# TriLevelKillSwitchEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/TriLevelKillSwitchEngine.ts` (424 LOC)

## What it is
Tri Level Kill Switch

```
TriLevelKillSwitchEngine (U-LPR-KILL-SWITCH)

Three-layer kill-switch cascade for the lathe pilot wet-run. Each layer
is independently tripable; the strongest active layer dominates. All
trips are latency-measured against published SLAs so the MOU kill-switch
protocol is continuously auditable.

  L1 PHYSICAL E-STOP
    Operator-initiated mechanical switch on the machine. Cuts power to
    spindle + axes directly via the CNC control. Reaction time limited
    only by operator and CNC hardware; PRISM can only record the event
    (e.g. via MTConnect EMERGENCY signal) — never initiate.
    SLA: acknowledgment logged ≤ 1000 ms of physical press detection.
    Mode on trip: HARD_STOP (no program-resume permitted).

  L2 MTConnect REMOTE FEEDHOLD + E-STOP
    PRISM sends MTConnect EMERGENCY_STOP + feedhold commands. Useful
    when a collision is predicted or an alarm pattern is detected.
   
```


## Exports
`KillLevel` (type), `TripSource` (type), `KillSla` (interface), `KillEvent` (interface), `KillResetEvent` (interface), `ActiveState` (interface), `KillSwitchStats` (interface), `TriLevelKillSwitchEngine` (class), `triLevelKillSwitchEngine` (const)

## Key methods
`setSla`, `getSla`, `listSlas`, `trip`, `reset`, `getActiveState`, `dispatcherGateOpen`, `listTrips`, `listResets`, `getStats`, `complianceReport`, `clearAll`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/TriLevelKillSwitchEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find TriLevelKillSwitchEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/TriLevelKillSwitchEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
