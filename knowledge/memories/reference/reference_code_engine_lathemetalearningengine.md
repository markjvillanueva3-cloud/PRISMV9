---
name: reference_code_engine_lathemetalearningengine
description: "LATHE-META-LEARNING-MS0"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LatheMetaLearningEngine.ts
  sourceHash: dc416c1f
  loc: 2933
aliases: LatheMetaLearningEngine
---

# LatheMetaLearningEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LatheMetaLearningEngine.ts` (2933 LOC)

> [physics] Imports canonical physics constants (`src/physics/constants.ts`) -- safety-relevant node.

## What it is
LATHE-META-LEARNING-MS0

```
LatheMetaLearningEngine — LATHE-META-LEARNING-MS0
===================================================
Meta-Learning for Few-Shot Adaptation in CNC Lathe Programming

Implements complete meta-learning algorithms for rapid adaptation to new
materials, operations, machines, and shop-specific patterns. Enables learning
from just 1-10 examples by leveraging prior knowledge across tasks.

Core Meta-Learning Algorithms:
  1. MAML (Model-Agnostic Meta-Learning) — Finn et al. (2017)
     - Inner loop: task-specific gradient descent
     - Outer loop: meta-parameter optimization across tasks
     - First-order approximation (FOMAML) for efficiency

  2. Prototypical Networks — Snell et al. (2017)
     - Embedding network for operation/material representation
     - Prototype computation per class from support set
     - Distance-based classification (Euclidean, cosine, Mahalanobis)

  3. Metric Le
```


## Exports
`MetaLearningTask` (interface), `TaskType` (type), `LatheExample` (interface), `LatheOperationType` (type), `QualityClass` (type), `StrategyClass` (type), `TaskMetadata` (interface), `MAMLConfig` (interface), `MAMLModelState` (interface), `AdaptationRecord` (interface), `MAMLTrainingResult` (interface), `ConvergenceAnalysis` (interface), `PrototypicalConfig` (interface), `DistanceMetric` (type), `ClassPrototype` (interface), `PrototypicalPrediction` (interface), `NearestExample` (interface), `PrototypicalEpisode` (interface), `PrototypicalTrainingResult` (interface), `EmbeddingQuality` (interface)

## Key methods
`mamlTrain`, `adaptToMaterial`, `trainPrototypicalNetwork`, `computePrototypes`, `fewShotPredict`, `learnEmbedding`, `computeSimilarity`, `createTaskDistribution`, `adaptControllerDialect`, `transferShopPatterns`, `setMAMLConfig`, `setPrototypicalConfig`, `setMetricConfig`, `getModelStatus`, `reset`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/LatheMetaLearningEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find LatheMetaLearningEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LatheMetaLearningEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
