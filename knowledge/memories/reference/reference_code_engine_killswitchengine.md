---
name: reference_code_engine_killswitchengine
description: "Pure-core 3-level kill switch state machine. Replaces scattered per-hook"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/KillSwitchEngine.ts
  sourceHash: bca13142
  loc: 166
aliases: KillSwitchEngine
---

# KillSwitchEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/KillSwitchEngine.ts` (166 LOC)

## What it is
Pure-core 3-level kill switch state machine.  Replaces scattered per-hook

```
KillSwitchEngine — U-HAGI11 unified operator kill switch (Voxyz Layer 12)

Pure-core 3-level kill switch state machine.  Replaces scattered per-hook
disable knobs with a single primitive an operator can pull when something's
wrong.  Levels (forward-only — promotion never demotes a tier):
  - NORMAL  — system runs as usual
  - SOFT    — block new requests, drain in-flight (in-flight allowed to finish)
  - HARD    — terminate in-flight too
  - NUCLEAR — HARD + revoke all credentials (caller is responsible for cred ops;
              engine just records the level + reason + actor + timestamp)

Pure: no I/O, no fs.  Caller supplies a state-store callback for persistence.
Deterministic per (state, action) pair.  Companion to U-HAGI02 control plane.

@module engines/KillSwitchEngine
```


## Exports
`KILL_LEVELS` (const), `KillLevel` (type), `KillStateSchema` (const), `KillState` (type), `RequestDecision` (interface), `KillSwitchEngine` (class), `killSwitchEngine` (const)

## Key methods
`initial`, `promote`, `reset`, `decide`, `rank`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/KillSwitchEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find KillSwitchEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/KillSwitchEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
