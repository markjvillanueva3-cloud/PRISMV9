---
name: reference_code_engine_wikirecallcounterengine
description: "U-RECALL-COUNTER (OBSIDIAN-VIZ-MS0)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WikiRecallCounterEngine.ts
  sourceHash: 946a7053
  loc: 203
aliases: WikiRecallCounterEngine
---

# WikiRecallCounterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WikiRecallCounterEngine.ts` (203 LOC)

## What it is
U-RECALL-COUNTER (OBSIDIAN-VIZ-MS0)

```
WikiRecallCounterEngine — U-RECALL-COUNTER (OBSIDIAN-VIZ-MS0)
=============================================================

Tracks per-entry recall counts for memory + wiki vault entries. Closes
the cyrilXBT "compounding" gap: a vault that doesn't measure which
entries get re-read can't surface them. The recall counter is the
signal that drives:

  1. Node sizing in /system-viz L10 (hot entries render larger).
  2. Promotion candidates for wiki/log.md (entries with high recall
     that aren't yet wiki-canonicalized).
  3. Pruning candidates (zero-recall entries older than N days).

Storage: schemaVersion-stamped JSON sidecar at
`mcp-server/data/state/wiki-recall-counts.json`. All writes are atomic
(temp + rename) to survive concurrent multi-chat increments.

NOT placed in iooms0 worktree — that worktree is claimed by a peer
(claude-a09ce89e) per the OBSIDIAN-COMPOUND-MS0 audit. This en
```


## Exports
`WIKI_RECALL_SCHEMA_VERSION` (const), `RecallEntryKindSchema` (const), `RecallEntryKind` (type), `RecallEntrySchema` (const), `RecallEntry` (type), `WikiRecallStateSchema` (const), `WikiRecallState` (type), `WikiRecallCounterEngine` (class), `wikiRecallCounterEngine` (const)

## Key methods
`recordRecall`, `getRecallCount`, `getEntry`, `getTopRecalled`, `getAllCounts`, `getStateSnapshot`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WikiRecallCounterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WikiRecallCounterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WikiRecallCounterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
