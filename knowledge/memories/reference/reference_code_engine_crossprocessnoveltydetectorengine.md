---
name: reference_code_engine_crossprocessnoveltydetectorengine
description: "XPROC-NEURAL Tier 11 (T11-02)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CrossProcessNoveltyDetectorEngine.ts
  sourceHash: a64ea184
  loc: 161
aliases: CrossProcessNoveltyDetectorEngine
---

# CrossProcessNoveltyDetectorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CrossProcessNoveltyDetectorEngine.ts` (161 LOC)

## What it is
XPROC-NEURAL Tier 11 (T11-02)

```
CrossProcessNoveltyDetectorEngine — XPROC-NEURAL Tier 11 (T11-02)

Detects whether a candidate job lies in a region of feature space that the
outcome ledger has rarely (or never) explored. Novelty score is the
normalized k-NN distance to historical events; high scores → genuinely
new operating regime, low scores → already-seen.

When T2-01 (CrossProcessEpisodicMemoryEngine) ships on the same branch,
the detector will use episodic centroids for an O(1) lookup. Until then
it computes k-NN over the raw ledger (acceptable for ledgers ≤ 10K rows).

Score normalization: distance is divided by the median k-NN distance of
the ledger to itself, giving a unit-free score. Threshold default 1.5
means "1.5× more isolated than the median historical neighborhood".

@module CrossProcessNoveltyDetectorEngine
```


## Exports
`Candidate` (type), `HistoryRow` (type), `ScoreInput` (type), `NoveltyScore` (interface), `ScoreResult` (interface), `CrossProcessNoveltyDetectorEngine` (class), `crossProcessNoveltyDetectorEngine` (const), `crossProcessNoveltyDetector` (function)

## Key methods
`score`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CrossProcessNoveltyDetectorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CrossProcessNoveltyDetectorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CrossProcessNoveltyDetectorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
