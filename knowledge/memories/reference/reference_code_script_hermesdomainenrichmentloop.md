---
name: reference_code_script_hermesdomainenrichmentloop
description: "hermes-domain-enrichment-loop.mjs (HERMES-KNOWLEDGE / U-HERMES-ENRICHMENT-LOOP, slot:zulu)"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/hermes-domain-enrichment-loop.mjs
  sourceHash: 302d2258
  loc: 538
aliases: hermes-domain-enrichment-loop.mjs
---

# hermes-domain-enrichment-loop.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/hermes-domain-enrichment-loop.mjs` (538 LOC)

## What it is
```
hermes-domain-enrichment-loop.mjs  (HERMES-KNOWLEDGE / U-HERMES-ENRICHMENT-LOOP, slot:zulu)

Engineered per-domain parallel-Hermes knowledge-enrichment loop. Operator directive
(2026-06-29): "run loops for each domain with a parallel hermes agent until we
physically exhaust all knowledge available within our system and outside sources...
utilize engineered loops, harnesses and crons. it has to be usable when needed and
logically makes sense to pull the knowledge automatically."

WHAT IT DOES (one run = one parallel pass over all 6 domains):
  - For each of delta/echo/foxtrot/mike/whiskey/kilo, fires ONE Hermes/Grok agent
    on a ROTATING focus axis (so every pass DIVERGES), in parallel (Promise.allSettled).
  - Dedups vs the per-domain ledger (normalized-title seen-set = the "exhaustion" cursor).
  - NOVELTY-STOP: a run yielding < NOVELTY_FLOOR fresh items increments a dry streak;
    a
```


## Exports
`laneCandidates` (function), `mapPool` (function), `DOMAINS` (const), `AXES` (const), `norm` (function), `loadGrounding` (function), `loadCitedTips` (function), `composeGrounding` (function), `loadLedger` (function), `makeFailoverAsk` (function), `resolveLane` (function), `probeLane` (function), `parseItems` (function), `domainPrompt` (function), `mergeFresh` (function), `runOnce` (function), `scoreTip` (function), `emitStaging` (function), `renderDigest` (function), `emitDigest` (function)

## Key methods
`mkdirSync`, `writeFileSync`, `log`, `saveLedger`, `emitDigest`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/hermes-domain-enrichment-loop.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find hermes-domain-enrichment-loop.mjs` or `prism_session:master_index_query`; open the source at `scripts/hermes-domain-enrichment-loop.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
