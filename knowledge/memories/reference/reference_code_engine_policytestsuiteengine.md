---
name: reference_code_engine_policytestsuiteengine
description: "Pure-core curated policy-test runner. Caller supplies (a) a list of policy"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PolicyTestSuiteEngine.ts
  sourceHash: a0c4db8b
  loc: 154
aliases: PolicyTestSuiteEngine
---

# PolicyTestSuiteEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PolicyTestSuiteEngine.ts` (154 LOC)

## What it is
Pure-core curated policy-test runner.  Caller supplies (a) a list of policy

```
PolicyTestSuiteEngine — U-HAGI09 adversarial / forbidden-action verdict suite.

Pure-core curated policy-test runner.  Caller supplies (a) a list of policy
test cases — each an `{id, category, input, expectedVerdict}` tuple — and
(b) a `runner(input)` callback that returns the actual verdict.  Engine
computes pass/fail per case, rolls up by category, returns a deterministic
verdict matrix.

Curated case classes:
  - "forbidden-action"  — physics-constant tampering, safety-threshold weakening,
                           peer-claim violation, settings.json gate-softening
  - "edge-case"         — empty inputs, oversize, NaN, unicode, timezone
  - "jailbreak"         — prompt-injection patterns, refuse_list circumvention
  - "robustness"        — distribution-shift inputs, malformed Zod fixtures

Pure: no I/O, no spawn.  Caller decides what `runner` does (real dispatcher
call, mocked engine
```


## Exports
`POLICY_CATEGORIES` (const), `PolicyCategory` (type), `POLICY_VERDICTS` (const), `PolicyVerdict` (type), `PolicyCaseSchema` (const), `PolicyCase` (type), `PolicyCaseResult` (interface), `PolicySummary` (interface), `PolicyRunner` (type), `PolicyTestSuiteEngine` (class), `policyTestSuiteEngine` (const)

## Key methods
`validateCase`, `run`, `summarize`, `renderMarkdown`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PolicyTestSuiteEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PolicyTestSuiteEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PolicyTestSuiteEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
