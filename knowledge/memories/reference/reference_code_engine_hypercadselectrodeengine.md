---
name: reference_code_engine_hypercadselectrodeengine
description: "CAD-FUSION-LIVE-MS0 / U-HCS-ELECTRODE"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HyperCADSElectrodeEngine.ts
  sourceHash: 96b777d7
  loc: 316
aliases: HyperCADSElectrodeEngine
---

# HyperCADSElectrodeEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HyperCADSElectrodeEngine.ts` (316 LOC)

## What it is
CAD-FUSION-LIVE-MS0 / U-HCS-ELECTRODE

```
HyperCADSElectrodeEngine — CAD-FUSION-LIVE-MS0 / U-HCS-ELECTRODE

Typed electrode-design operations for hyperCAD-S, the OPEN MIND CAD that
ships an in-house electrode module (Fusion 360 has no equivalent — this
is the primary reason for picking hyperCAD-S as a connector target).

Operations:
  - pickHolder           — select Erowa / System-3R block holder
  - setOrbitStrategy     — set EDM orbit (Sink / Sphere / Square / etc.)
  - setDescription       — mark electrode type (Core / Cavity / Insert / ...)
  - generateElectrode    — generate electrode from cavity + burn faces
  - exportToEdm          — export STEP/DXF/native to sinker EDM
  - setupClamping        — assign clamping code + offsets
  - burnSequence         — order multi-electrode burn sequence

Catalogs (mirror `resources/OPEN MIND/hyperCAD-S/31.0/hyperCAD-S/files/electrode/`):
  - 9 electrode descriptions   (electrode_descrip
```


## Exports
`ELECTRODE_DESCRIPTIONS` (const), `ElectrodeDescription` (type), `ELECTRODE_ORBITS` (const), `ElectrodeOrbit` (type), `HOLDER_LIBRARIES` (const), `HolderLibrary` (type), `HOLDER_Z_HEIGHTS_MM` (const), `HolderZHeightMm` (type), `PickHolderSchema` (const), `PickHolderInput` (type), `SetOrbitStrategySchema` (const), `SetOrbitStrategyInput` (type), `SetDescriptionSchema` (const), `SetDescriptionInput` (type), `GenerateElectrodeSchema` (const), `GenerateElectrodeInput` (type), `ExportToEdmSchema` (const), `ExportToEdmInput` (type), `SetupClampingSchema` (const), `SetupClampingInput` (type)

## Key methods
`listElectrodeDescriptions`, `listOrbitStrategies`, `listHolderLibraries`, `listHolderZHeightsMm`, `pickHolder`, `setOrbitStrategy`, `setDescription`, `generateElectrode`, `exportToEdm`, `setupClamping`, `burnSequence`

## Depends on
[[reference_code_engine_hypercadslivebridgeengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/HyperCADSElectrodeEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HyperCADSElectrodeEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HyperCADSElectrodeEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
