---
name: reference_code_lib_camlearnedorderstore
description: "cam-learned-order-store.mjs — persist + load the LEARNED lathe op-ordering as a versioned,"
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/cam-learned-order-store.mjs
  sourceHash: f6c97016
  loc: 244
aliases: cam-learned-order-store.mjs
---

# cam-learned-order-store.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/cam-learned-order-store.mjs` (244 LOC)

## What it is
```
cam-learned-order-store.mjs — persist + load the LEARNED lathe op-ordering as a versioned,
planner-consumable artifact, closing the self-improvement loop's PERSIST/LOAD half.

The planner historically HARD-CODED LATHE_OP_ORDER, so a corpus retrain (cam-learn-order-run.mjs)
required a HUMAN to hand-transcribe the new ranking into source — the open loop. This store makes
the order map a durable DATA artifact (learned-op-order.json) the planner LOADS at runtime, with
the hard-coded map as the fail-soft fallback. Any producer (retrain, manual fix) that writes a
VALID artifact changes planner behavior with ZERO code edit — that is the closed loop.

Pure + injectable IO (readImpl) for hermetic tests. loadLearnedOrder NEVER throws — a missing or
malformed artifact degrades LOUDLY (stderr) to the caller-supplied fallback, never a silent wrong
order (R12). buildLearnedOrderArtifact fails LOUD (th
```


## Exports
`LEARNED_ORDER_SCHEMA_VERSION` (const), `LEARNED_ORDER_KIND` (const), `DEFAULT_LEARNED_ORDER_PATH` (const), `MAX_ORDER_FAMILIES` (const), `ROUGH_BEFORE_FINISH` (const), `sortOrderMap` (function), `validateOrderMap` (function), `buildLearnedOrderArtifact` (function), `writeLearnedOrderArtifact` (function), `loadLearnedOrder` (function), `deriveProvenanceFromReport` (function)

## Key methods
`writeImpl`, `mkdirSync`, `writeFileSync`, `renameSync`, `safeWarn`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/cam-learned-order-store.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find cam-learned-order-store.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/cam-learned-order-store.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
