---
name: reference_code_engine_wetrunstatemachineengine
description: "0 scrapped parts, 0 safety events. Session ready."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WetRunStateMachineEngine.ts
  sourceHash: 618e8cb9
  loc: 488
aliases: WetRunStateMachineEngine
---

# WetRunStateMachineEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WetRunStateMachineEngine.ts` (488 LOC)

## What it is
0 scrapped parts, 0 safety events. Session ready.

```
WetRunStateMachineEngine (U-LPR-WETRUN-FSM)

Explicit state machine for JM-Die pilot wet runs. Governs transitions
between WET_PASS, WET_SOFT_FAIL, WET_HARD_FAIL, and QUARANTINE states
based on scrap counts and safety events. All transitions are recorded
with timestamps and evidence for post-mortem audit.

State rules (from LATHE-PROD-READY-MS0-PLAN Phase-8 — Prod B1):

  WET_PASS          — 0 scrapped parts, 0 safety events. Session ready.
  WET_SOFT_FAIL     — ≤1 scrapped part, 0 safety events. Requires
                      root-cause analysis + patch to Phase-4 corpus +
                      re-sim + re-wet before advancing.
  WET_HARD_FAIL     — ≥2 scrapped parts OR any safety event (kill-switch
                      trip, collision, workholding breach). Forces
                      corpus-only quarantine for 5 days minimum.
  QUARANTINE        — 5-day post-mortem hold. Exit require
```


## Exports
`WetRunState` (type), `SafetyEventKind` (type), `WetRunSession` (interface), `ScrapEvent` (interface), `SafetyEvent` (interface), `StateTransition` (interface), `PostMortemRecord` (interface), `QuarantineClearance` (interface), `WetRunStateMachineEngine` (class), `wetRunStateMachineEngine` (const)

## Key methods
`startSession`, `getSession`, `listSessions`, `recordPartOutcome`, `recordSafetyEvent`, `submitPostMortem`, `clearQuarantine`, `resolveSession`, `listTransitions`, `listScrapEvents`, `listSafetyEvents`, `getPostMortem`, `listClearances`, `getStats`, `clearAll`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WetRunStateMachineEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WetRunStateMachineEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WetRunStateMachineEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
