---
name: reference_code_engine_wirebreakautorethreadengine
description: "WEDM wire-break recovery strategy"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WireBreakAutoRethreadEngine.ts
  sourceHash: 31e576d4
  loc: 224
aliases: WireBreakAutoRethreadEngine
---

# WireBreakAutoRethreadEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WireBreakAutoRethreadEngine.ts` (224 LOC)

## What it is
WEDM wire-break recovery strategy

```
WireBreakAutoRethreadEngine — WEDM wire-break recovery strategy

Closes the iter20 P1 "wire-break auto-rethread" gap. When the wire breaks mid-cut,
the machine must decide how to recover without scrapping the part:
  1. auto_rethread + restart at break_position (modern Sodick/Fanuc/Charmilles can)
  2. manual_rethread (older machines, or after multiple breaks)
  3. burnback_restart (retreat 0.5-2mm, restart fresh cut)
  4. scrap_part (irrecoverable — joint quality unacceptable for spec)

Joint-quality risk drivers (per Fanuc Robocut Wire-Break Recovery Guide §4 +
Sodick AP series troubleshooting + Charmilles RoboFil application notes):
  - Restart joint shows witness mark (~5-15 μm Ra bump vs surrounding surface)
  - Recast layer grows ~2-3× at joint (re-fused HAZ)
  - Multiple breaks in same pass → quality cumulative-degraded
  - Skim-cut breaks tolerate worse joints than roughing (less
```


## Exports
`WireMaterial` (type), `ThreadingCapability` (type), `CutPhase` (type), `FeaturePosition` (type), `WireBreakInput` (interface), `RecoveryStrategy` (type), `OperatorSkill` (type), `WireBreakResult` (interface), `WireBreakAutoRethreadEngine` (class), `wireBreakAutoRethreadEngine` (const)

## Key methods
`recover`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WireBreakAutoRethreadEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WireBreakAutoRethreadEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WireBreakAutoRethreadEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
