---
name: reference_code_engine_cadoperationdecoderengine
description: "CAD-DRAW-MAX-MS0/P1-U06"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADOperationDecoderEngine.ts
  sourceHash: 71bd8484
  loc: 264
aliases: CADOperationDecoderEngine
---

# CADOperationDecoderEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADOperationDecoderEngine.ts` (264 LOC)

## What it is
CAD-DRAW-MAX-MS0/P1-U06

```
CADOperationDecoderEngine — CAD-DRAW-MAX-MS0/P1-U06

Generative head for the CAD closed-loop NN cluster. Today the LP04
MasterBrainBackpropPropagatorEngine only *scores* op sequences via a
value head v=θ·φ — it can rank but cannot *emit* a next op. This
decoder closes that gap: given an op-stream context plus a
natural-language `intent` string, it proposes the next
{@link CADOperation} (or top-K candidates) for the AI to execute via
{@link HyperCADSLiveBridgeEngine}.

**Phase 1 — rule-based decoder.** Deterministic templates over
`CAD_OPERATION_KINDS` driven by a small priority table:
  1. **Intent override** — substrings (`fillet`, `extrude`, `revolve`,
     `hole`, `chamfer`, `shell`, `pattern`, `export`) → matching op.
  2. **Sequence templates** — empty context → sketch_create;
     after sketch → feature_extrude; after extrude → feature_fillet;
     after multiple features → export_
```


## Exports
`DecoderContext` (interface), `DecoderOptions` (interface), `ProposedOp` (interface), `DecoderStats` (interface), `CADOperationDecoderEngine` (class), `cadOperationDecoderEngine` (const)

## Key methods
`proposeNextOp`, `proposeNextOpsTopK`, `getVocabulary`, `getStats`

## Depends on
`ICADCodeGenerator`

## Where it lives
- **Source:** `mcp-server/src/engines/CADOperationDecoderEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADOperationDecoderEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADOperationDecoderEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
