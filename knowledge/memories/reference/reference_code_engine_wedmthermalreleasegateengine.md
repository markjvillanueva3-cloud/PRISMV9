---
name: reference_code_engine_wedmthermalreleasegateengine
description: "Thermal Budget & Recast Safety Gate"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WEDMThermalReleaseGateEngine.ts
  sourceHash: a7d4dbda
  loc: 410
aliases: WEDMThermalReleaseGateEngine
---

# WEDMThermalReleaseGateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WEDMThermalReleaseGateEngine.ts` (410 LOC)

## What it is
Thermal Budget & Recast Safety Gate

```
WEDMThermalReleaseGateEngine — Thermal Budget & Recast Safety Gate

Validates that thermal load during WEDM cutting stays within the dielectric
cooling capacity and predicts recast depth to ensure finish requirements.

Physics model:
  1. Thermal conductivity k(T): ASM Handbook Vol. 16 piecewise-linear interpolation
  2. Recast depth: d_recast ≈ C_recast · √(α · t_on) · η_coupling
     where α = k(T) / (ρ · cp)
  3. Thermal budget: P_mean ≤ Q_cool
     P_mean = V · I · duty
     Q_cool = ṁ · cp · ΔT_allow · coupling

The gate HARD-BLOCKS when:
  - Predicted recast exceeds finish-class threshold (fail_recast_excessive)
  - Thermal load exceeds cooling capacity (fail_heat_overload)
  - Both conditions fail (fail_both)
  - Unknown material (fail_unknown_material) — no silent fallback

Output plugs into WEDMProgramSafetyGateEngine's `thermal` input and
contributes 0.15 to the composite S(x).
```


## Exports
`FinishClass` (type), `FlushMode` (type), `ThermalVerdict` (type), `ThermalReleaseInput` (interface), `ThermalMetrics` (interface), `ThermalReleaseResult` (interface), `WEDMThermalReleaseGateEngine` (class), `wedmThermalReleaseGateEngine` (const)

## Key methods
`evaluate`, `gate`, `interpolateKT`, `resolveMaterial`, `estimateSkimsNeeded`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WEDMThermalReleaseGateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WEDMThermalReleaseGateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WEDMThermalReleaseGateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
