---
name: reference_code_engine_mastercaminhostrunnerengine
description: "Mastercam In Host Runner"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MastercamInHostRunnerEngine.ts
  sourceHash: 9dc552e8
  loc: 471
aliases: MastercamInHostRunnerEngine
---

# MastercamInHostRunnerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MastercamInHostRunnerEngine.ts` (471 LOC)

## What it is
Mastercam In Host Runner

```
MastercamInHostRunnerEngine — U-CAMTEST04
==========================================

PHASE-8: PRISM-side companion for the Mastercam X8 in-host C-Hook /
.NET-Hook test runner (resources/mastercam/PrismTestRunner.csproj +
PrismTestRunner.cs). The .NET-Hook DLL loads inside Mastercam,
opens a WebSocket to the PRISM CAMPluginCommunicationHub, and streams
scenario results back.

Architecture mirrors the prior three in-host runners (hyperMILL Python
U-CAMTEST01, Fusion 360 JS Add-In U-CAMTEST02, Inventor HSM iLogic VB
U-CAMTEST03) — same scenario contract, same 7 assertions — but pinned
to target="mastercam" with the C-Hook endpoint.

The 7-assertion contract enforced here:
  1. frame_arrival           — observed count == expected_frame_count, no gaps/dupes
  2. latency_p99             — p99 latency ≤ descriptor.latency_p99_budget_ms
  3. band_transitions        — observed transitions == des
```


## Exports
`ScenarioCategorySchema` (const), `ScenarioCategory` (type), `ScenarioDescriptorSchema` (const), `ScenarioDescriptor` (type), `PlannedFrameSchema` (const), `PlannedFrame` (type), `ScenarioPlanSchema` (const), `ScenarioPlan` (type), `ObservedFrameSchema` (const), `ObservedFrame` (type), `ScenarioResultSchema` (const), `ScenarioResult` (type), `AssertionNameSchema` (const), `AssertionName` (type), `AssertionResultSchema` (const), `AssertionResult` (type), `ScenarioSummarySchema` (const), `ScenarioSummary` (type), `RunnerStatsSchema` (const), `RunnerStats` (type)

## Key methods
`register`, `planScenario`, `frameToEnvelope`, `summarize`, `getPlan`, `getStats`, `resetSession`, `resetAll`

## Depends on
[[reference_code_engine_camplugincommunicationhubengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/MastercamInHostRunnerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MastercamInHostRunnerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MastercamInHostRunnerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
