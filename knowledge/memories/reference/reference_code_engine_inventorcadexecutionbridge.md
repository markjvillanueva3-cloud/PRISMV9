---
name: reference_code_engine_inventorcadexecutionbridge
description: "planning↔execution bridge for Inventor CAD ops."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/InventorCADExecutionBridge.ts
  sourceHash: 63701ffa
  loc: 399
aliases: InventorCADExecutionBridge
---

# InventorCADExecutionBridge -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/InventorCADExecutionBridge.ts` (399 LOC)

## What it is
planning↔execution bridge for Inventor CAD ops.

```
InventorCADExecutionBridge — planning↔execution bridge for Inventor CAD ops.

Mirrors the AutodeskFusionMCPProxyEngine.executeOperation() and
SolidWorksCADExecutionBridge.plan/render pattern but emits an iLogic VB.NET
scaffold instead of a Python script or VBA macro. Inventor's automation
surface is the .NET COM API exposed through `ThisApplication` / `ThisDoc`
inside iLogic; the catalog's `python_api` field documents the chained
receiver tree (e.g. `ComponentDefinition.Features.ExtrudeFeatures.AddByDistanceExtent`).

No live MCP-style remote endpoint exists for Inventor; the existing
InventorAutomationBridge.ts hosts a fixed verb set via COM. This engine
produces the audit-trail iLogic snippet that an operator pastes into the
iLogic Browser, runs once, and verifies before committing the result back
into PRISM. Pure dry-run by design — operator-in-the-loop preserved.

Two pure static ent
```


## Exports
`InventorPlanArgs` (interface), `InventorExecutionPlan` (interface), `InventorCADExecutionBridge` (class)

## Key methods
`plan`, `validateValueType`, `renderILogicScaffold`

## Depends on
[[reference_code_engine_inventorcadfunctionindexengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/InventorCADExecutionBridge.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find InventorCADExecutionBridge` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/InventorCADExecutionBridge.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
