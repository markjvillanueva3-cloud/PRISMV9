---
name: reference_code_engine_vendorcreditengine
description: "vendor credits (Accounts-Payable side) for the PRISM ERP"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/VendorCreditEngine.ts
  sourceHash: 52cd9f86
  loc: 389
aliases: VendorCreditEngine
---

# VendorCreditEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/VendorCreditEngine.ts` (389 LOC)

## What it is
vendor credits (Accounts-Payable side) for the PRISM ERP

```
VendorCreditEngine — vendor credits (Accounts-Payable side) for the PRISM ERP
(galaxy:business, slot:hotel).

QuickBooks-parity: the "Vendor Credit" function set (QB-PARITY Phase-3 A/P engine #1). This is the
A/P MIRROR of `CreditMemoEngine` (A/R): where a credit memo is a NEGATIVE-revenue document born from
a customer return, a vendor credit is a NEGATIVE-expense document born from a return-to-vendor, a
vendor refund, or vendor overbilling. It reduces what the SHOP owes the vendor and reverses the
inventory/expense + use-tax the original purchase landed.

Producer / PRISM synergy — this engine serves BOTH:
 - the QuickBooks A/P cycle (QUICKBOOKS-PARITY-PLAN.md): a return-to-vendor / vendor-refund /
   overbilling event creates the vendor credit (mirror of `recordPurchase` which posts the bill
   DR <expense/asset> / CR 2000 AP). The credit applies against an open vendor BILL's balance —
```


## Exports
`VendorCreditInput` (type), `VendorCreditLineResult` (interface), `VendorCreditApplication` (interface), `GLLine` (interface), `VendorCredit` (interface), `VendorCreditEngine` (class), `vendorCreditEngine` (const)

## Key methods
`create`, `applyToBill`, `remainingCredit`, `glLinesForIssue`

## Depends on
[[reference_code_engine_salesusetaxengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/VendorCreditEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find VendorCreditEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/VendorCreditEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
