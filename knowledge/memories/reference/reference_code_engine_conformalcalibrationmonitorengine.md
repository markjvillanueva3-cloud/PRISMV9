---
name: reference_code_engine_conformalcalibrationmonitorengine
description: "XPROC-NEURAL Tier 5 (T5-04b)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ConformalCalibrationMonitorEngine.ts
  sourceHash: e1b307ba
  loc: 522
aliases: ConformalCalibrationMonitorEngine
---

# ConformalCalibrationMonitorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ConformalCalibrationMonitorEngine.ts` (522 LOC)

## What it is
XPROC-NEURAL Tier 5 (T5-04b)

```
ConformalCalibrationMonitorEngine — XPROC-NEURAL Tier 5 (T5-04b)

Closes the loop on the split-conformal classifier shipped at fd519f1ab
(U-NN-CONFORMAL01). The marginal-coverage guarantee
  P(Y ∈ S(X)) ≥ 1 − α
holds only when (X, Y) is drawn exchangeably with the calibration set.
In production the underlying classifier's distribution drifts (new
materials, new machines, season-of-year operator habits) and the
exchangeability assumption fails — the empirical coverage rate drops
below 1−α and the prediction sets stop being trustworthy.

This engine is the rolling-empirical-coverage monitor that detects
exactly that. Pure aggregator over caller-supplied (predictedSet,
actualLabel) observations. Reports both the rolling coverage rate and
a drift signal that downstream consumers (operator dashboards, the
P2P pipeline's safety gate) react to.

References:
  - Vovk, Gammerman, Shafer (2005). A
```


## Exports
`MonitorConfig` (interface), `MonitorStatus` (interface), `ConfigureOk` (interface), `RecordOk` (interface), `OpErr` (interface), `ConfigureResult` (type), `RecordResult` (type), `ConformalCalibrationMonitorEngine` (class), `conformalCalibrationMonitorEngine` (const), `conformalCalibrationMonitor` (function)

## Key methods
`subscribeToOutcomes`, `unsubscribeFromOutcomes`, `isSubscribedToOutcomes`, `busStats`, `configure`, `record`, `status`, `reset`, `constants`

## Depends on
[[reference_code_engine_feedbackbusengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/ConformalCalibrationMonitorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ConformalCalibrationMonitorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ConformalCalibrationMonitorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
