---
name: reference_code_engine_costalarmengine
description: "threshold-based alarms over multi-LLM cost telemetry."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CostAlarmEngine.ts
  sourceHash: 7232a46a
  loc: 540
aliases: CostAlarmEngine
---

# CostAlarmEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CostAlarmEngine.ts` (540 LOC)

## What it is
threshold-based alarms over multi-LLM cost telemetry.

```
CostAlarmEngine — threshold-based alarms over multi-LLM cost telemetry.

Reads cost-telemetry.jsonl (per-call USD + token records), aggregates daily +
weekly totals, compares vs config thresholds, emits alarms to a JSONL log and
to AGENT_CHAT.md with cool-down suppression.

COST-CASCADE-MS0 / U-COST-ALARM.

Design: pure-core + injectable side-effect adapters (`deps`) so the test
suite never touches the real filesystem and clock-skew is provably handled
(telemetry timestamps, NOT wall-clock, drive cool-down).

R12 invariants (fail-loud + safe-default):
 1. Aggregator throw → log + return prior state, never crash the tick
 2. Config missing / malformed → safe defaults + warn (never crash)
 3. Test tentacles never count toward thresholds when ignoreTestTentacles=true
 4. Cool-down uses TELEMETRY/ALARM-LOG timestamps, not wall-clock
 5. Channel write failure → continue to next channel + reco
```


## Exports
`CostAlarmThresholds` (interface), `CostAlarmConfig` (interface), `TelemetryRecord` (interface), `AggregateWindow` (interface), `AlarmLevel` (type), `AlarmDecision` (interface), `AlarmRecord` (interface), `AlarmCheckResult` (interface), `CostAlarmDeps` (interface), `normalizeConfig` (function), `aggregateTelemetry` (function), `lookupThresholds` (function), `decideAlarms` (function), `lastFiredAt` (function), `isCoolDownActive` (function), `CostAlarmEngine` (class), `costAlarmEngine` (const), `MakeFsDepsOptions` (interface), `makeFsDeps` (function)

## Key methods
`logWarn`, `pushIfExceeded`, `check`, `rotateIfNeeded`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CostAlarmEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CostAlarmEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CostAlarmEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
