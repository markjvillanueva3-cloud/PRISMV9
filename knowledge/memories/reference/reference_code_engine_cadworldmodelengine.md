---
name: reference_code_engine_cadworldmodelengine
description: "CAD-COMPLETE-MS0 / U-AI-02"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADWorldModelEngine.ts
  sourceHash: f7b6bd95
  loc: 497
aliases: CADWorldModelEngine
---

# CADWorldModelEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADWorldModelEngine.ts` (497 LOC)

## What it is
CAD-COMPLETE-MS0 / U-AI-02

```
CADWorldModelEngine — CAD-COMPLETE-MS0 / U-AI-02
=================================================

The CAD agent's belief-state of a live CAD document. The agent cannot
see the CAD application's document tree directly between calls, so it
keeps a world model: the bodies, sketches, features, parameters,
selection and active units it *believes* the document contains. Every
operation the agent issues is also applied to this model, so the agent
can reason about what is valid next, preview a plan (U-AI-07), wrap a
sequence in a transaction (U-AI-08), and — critically — detect DRIFT
when the model and the real document disagree.

The model is a deterministic reducer: applyOp(state, op) → state.
Inconsistent operations (deleting an entity that does not exist,
creating a duplicate id) THROW rather than silently no-op — a belief
error must surface so the agent can re-sync via detectDrift().

Doc
```


## Exports
`CADUnits` (type), `CADEntityKind` (type), `CADEntity` (interface), `CADWorldState` (interface), `CADWorldOp` (interface), `CADWorldDiff` (interface), `CADWorldObservation` (interface), `CADWorldDrift` (interface), `CADWorldModelEngine` (class), `cadWorldModelEngine` (const)

## Key methods
`getOrCreate`, `applyOp`, `applyOps`, `getState`, `checkpoint`, `diffFromCheckpoint`, `diff`, `detectDrift`, `restore`, `list`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CADWorldModelEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADWorldModelEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADWorldModelEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
