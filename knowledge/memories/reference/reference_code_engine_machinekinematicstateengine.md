---
name: reference_code_engine_machinekinematicstateengine
description: "Dynamic Machine State Tracker (U-MIO39)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MachineKinematicStateEngine.ts
  sourceHash: 716eb9fe
  loc: 386
aliases: MachineKinematicStateEngine
---

# MachineKinematicStateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MachineKinematicStateEngine.ts` (386 LOC)

## What it is
Dynamic Machine State Tracker (U-MIO39)

```
MachineKinematicStateEngine — Dynamic Machine State Tracker (U-MIO39)
=====================================================================

Phase 12 Machine-state gate: a machine's real capability is DYNAMIC.
Static spec sheets tell you what the machine was *certified* to do;
this engine tracks what it can *currently* do based on:

  1. Thermal expansion state per linear axis (X/Y/Z) — ISO 230-3
     spindle-growth + body/column thermal distortion. delta_mm = L * alpha *
     (T - T_ref). alpha_steel = 1.2e-5 /K; alpha_alum = 2.3e-5 /K.
     At +5 K over a 1 m column, steel grows 60 micron — which matters for
     tolerancing.

  2. Servo following-error (lag) trend. If rolling-average lag grows
     beyond 2x the spec baseline, the servo drive is degrading
     (bearing preload loss, scale drift, belt wear). Reduce jerk limit
     to avoid further damage; flag for maintenance.

  3. Dy
```


## Exports
`MachineAxis` (type), `StructureMaterial` (type), `THERMAL_ALPHA` (const), `ControllerType` (type), `LOOKAHEAD_BLOCKS` (const), `ThermalAxisState` (interface), `ServoAxisState` (interface), `PayloadState` (interface), `LookAheadCheckInput` (interface), `MachineStateSnapshot` (interface), `AxisThermalResult` (interface), `AxisServoResult` (interface), `JerkDerate` (interface), `LookAheadResult` (interface), `DerivedState` (interface), `MachineKinematicStateEngine` (class), `machineKinematicStateEngine` (const)

## Key methods
`update`, `getLatest`, `getHistory`, `servoLagTrend`, `renderMarkdown`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MachineKinematicStateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MachineKinematicStateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MachineKinematicStateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
