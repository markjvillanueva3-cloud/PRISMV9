---
name: reference_code_engine_crossprocessprototypicalnetengine
description: "XPROC-NEURAL Tier 7 (T7-02)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CrossProcessPrototypicalNetEngine.ts
  sourceHash: f3593395
  loc: 470
aliases: CrossProcessPrototypicalNetEngine
---

# CrossProcessPrototypicalNetEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CrossProcessPrototypicalNetEngine.ts` (470 LOC)

## What it is
XPROC-NEURAL Tier 7 (T7-02)

```
CrossProcessPrototypicalNetEngine — XPROC-NEURAL Tier 7 (T7-02)

Few-shot classification and regression via class prototypes, per
Snell, Swersky, Zemel 2017, "Prototypical Networks for Few-shot
Learning," NeurIPS.

## Why prototypes for PRISM

When a new material/machine pair arrives at JM Die we have, at most,
a handful of cut samples before the AI has to recommend speed/feed.
Full re-training from scratch is impossible (too few examples). MAML
(T7-01) gives a fast-adaptation init but still requires gradient
descent. Prototypical networks need ZERO gradient steps: embed the
few examples, average them per class to form prototypes, classify
new queries by nearest prototype.

For regression (predicting Vc from material features) we extend the
idea via Nadaraya-Watson kernel regression on the same embedded
space — query value = weighted average of training values where
weights ∝ exp(-||q - 
```


## Exports
`PrototypeRef` (interface), `PrototypeOk` (interface), `ClassifyOk` (interface), `RegressOk` (interface), `OpErr` (interface), `PrototypeResult` (type), `ClassifyResult` (type), `RegressResult` (type), `CrossProcessPrototypicalNetEngine` (class), `crossProcessPrototypicalNetEngine` (const), `crossProcessPrototypicalNet` (function)

## Key methods
`computePrototype`, `classify`, `regress`, `constants`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CrossProcessPrototypicalNetEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CrossProcessPrototypicalNetEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CrossProcessPrototypicalNetEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
