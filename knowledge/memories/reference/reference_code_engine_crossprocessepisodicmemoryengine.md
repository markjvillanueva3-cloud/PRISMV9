---
name: reference_code_engine_crossprocessepisodicmemoryengine
description: "XPROC-NEURAL Tier 2 (T2-01)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CrossProcessEpisodicMemoryEngine.ts
  sourceHash: b405a0fb
  loc: 479
aliases: CrossProcessEpisodicMemoryEngine
---

# CrossProcessEpisodicMemoryEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CrossProcessEpisodicMemoryEngine.ts` (479 LOC)

## What it is
XPROC-NEURAL Tier 2 (T2-01)

```
CrossProcessEpisodicMemoryEngine — XPROC-NEURAL Tier 2 (T2-01)

Hierarchical episode store for cross-process learning. Every print-to-program
run, operator override, and safety veto is recorded as an "episode" keyed by
(process, material, feature, decision). Downstream tiers (T2-02 prioritized
replay, T3-01 online updater, T4-01 reward shaper) recall the most relevant
episodes for a new query instead of scanning the full ledger.

Why hierarchical (vs. one flat array):
  T1-02's MLP retrains on the full ledger — fine at 100 events, untenable at
  100K. Three-tier residency lets the *most useful slice* live in cache:
    • Hot   — last 100 events, full retention, O(1) append, scanned on every
              recall(). The MLP's online-update window.
    • Warm  — last 1K events, age-decayed sampling. Newer events keep
              full weight; older events are progressively down-sampled by

```


## Exports
`XProcProcess` (type), `XProcDecision` (type), `XProcOutcome` (type), `XProcEpisodeKey` (interface), `XProcEpisode` (interface), `EpisodeTier` (type), `RecallResult` (interface), `RecallResponse` (interface), `StoreSuccess` (interface), `StoreError` (interface), `StoreResult` (type), `MemoryStats` (interface), `TierCapacity` (interface), `CrossProcessEpisodicMemoryEngine` (class), `crossProcessEpisodicMemoryEngine` (const), `crossProcessEpisodicMemory` (function)

## Key methods
`store`, `recall`, `visit`, `stats`, `configure`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CrossProcessEpisodicMemoryEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CrossProcessEpisodicMemoryEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CrossProcessEpisodicMemoryEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
