---
name: reference_code_engine_lathecustomerorderlifecycleengine
description: "LatheCustomerOrderLifecycleEngine — U-LTH51 (LATHE-MASTER P5 ERP)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LatheCustomerOrderLifecycleEngine.ts
  sourceHash: 60487d43
  loc: 337
aliases: latheCustomerOrderLifecycleEngine
---

# latheCustomerOrderLifecycleEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LatheCustomerOrderLifecycleEngine.ts` (337 LOC)

## What it is
```
LatheCustomerOrderLifecycleEngine — U-LTH51 (LATHE-MASTER P5 ERP)

State machine for customer order lifecycle:

  draft → quoted → accepted → scheduled → in_production → shipped → invoiced → closed
                          ↘ rejected           ↘ on_hold

Valid transitions (enforced — invalid transitions throw):
  draft       → quoted | cancelled
  quoted      → accepted | rejected | cancelled
  accepted    → scheduled | cancelled
  scheduled   → in_production | on_hold | cancelled
  in_production → shipped | on_hold | cancelled
  on_hold     → scheduled | in_production | cancelled
  shipped     → invoiced | returned
  invoiced    → closed | disputed
  disputed    → invoiced | closed
  returned    → refunded | closed
  rejected    → closed
  cancelled   → closed
  closed      → (terminal)

Every transition writes an audit event (who, when, why, prior → next).
Persistence: state/shared/la
```


## Exports
`ORDER_STATES` (const), `OrderState` (type), `CreateOrderInputSchema` (const), `CreateOrderInput` (type), `TransitionInputSchema` (const), `TransitionInput` (type), `AuditEvent` (interface), `OrderRecord` (interface), `OrderLifecycleState` (interface), `OrderSummaryRow` (interface), `latheCustomerOrderLifecycleEngine` (const), `LatheCustomerOrderLifecycleEngine` (re-export)

## Key methods
`createOrder`, `transition`, `getOrder`, `listOrders`, `getAuditLog`, `pipelineSummary`, `asSummaryRows`, `allowedTransitions`, `atomicWriteJson`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/LatheCustomerOrderLifecycleEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find latheCustomerOrderLifecycleEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LatheCustomerOrderLifecycleEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
