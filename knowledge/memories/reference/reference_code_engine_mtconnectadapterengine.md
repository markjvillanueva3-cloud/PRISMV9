---
name: reference_code_engine_mtconnectadapterengine
description: "MTConnectAdapterEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MTConnectAdapterEngine.ts
  sourceHash: 1f1cbe14
  loc: 737
aliases: MTConnectAdapterEngine
---

# MTConnectAdapterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MTConnectAdapterEngine.ts` (737 LOC)

## What it is
MTConnectAdapterEngine

```
MTConnectAdapterEngine

HTTP/XML adapter for MTConnect-enabled CNC machines.
MTConnect is the dominant US CNC data standard — most Haas, Mazak,
Okuma, and Doosan machines ship with MTConnect agents.

Capabilities:
1. probe()     — Discover machine's MTConnect device model (axes, components, data items)
2. current()   — Get current snapshot of all data items (spindle speed, feed, position, alarms)
3. sample()    — Get historical samples within a sequence range
4. assets()    — Get cutting tool assets (tool life, usage counters)
5. monitor()   — Continuous polling with change detection and alert thresholds
6. parseAlarm()— Extract and classify MTConnect alarm conditions
7. getSpindleLoad() — Real-time spindle load % with Kienzle force comparison
8. getFeedOverride() — Feed override % and effective feed rate
9. getMachineStatus() — Aggregated machine state (running/idle/alarm/offline)

MTCo
```


## Exports
`MTConnectConfig` (interface), `MTConnectDevice` (interface), `MTConnectAxis` (interface), `MTConnectComponent` (interface), `MTConnectDataItem` (interface), `MTConnectSnapshot` (interface), `MTConnectValue` (interface), `MTConnectAlarm` (interface), `MTConnectToolAsset` (interface), `SpindleLoadResult` (interface), `MachineStatusResult` (interface), `MTConnectAdapterEngine` (class)

## Key methods
`probe`, `getAttr`, `current`, `sample`, `assets`, `monitor`, `callback`, `setTimeout`, `parseAlarms`, `getSpindleLoad`, `getFeedOverride`, `getMachineStatus`, `clearTimeout`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MTConnectAdapterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MTConnectAdapterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MTConnectAdapterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
