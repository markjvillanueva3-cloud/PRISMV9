---
name: reference_sierra_regen_fast_registration_gap_2026_05_29
description: "9 *-features.mjs generators are absent from regen-viz FAST[] (don't run on regen). merge-augmentations loads by OUTPUT-JSON name, not generator filename — so the \"missing splice block\" claim is a false signal."
type: reference
source: prism-memory
synced: 2026-06-09T14:54:10.943Z
aliases: reference_sierra_regen_fast_registration_gap_2026_05_29
---


**Verified regen-pipeline gap (2026-05-29 workflow+codex assessment, slot:sierra).** `scripts/generate-*-features.mjs` count = **49**; **9 are NOT in `regen-viz.mjs` FAST[]** → those generators never execute during a regen, so their augmentation JSON is never (re)produced: `generate-{galaxy,hermes-zulu-ops,hotel-domain,milling-tribal-tip-bridge,psn-health,quoting-pipeline,sfc-variability,svi-component,vendor-catalog}-features.mjs`.

**R8/R12 correction (the load-bearing lesson):** an assessment agent (and a naive grep) claimed "5 generators lack a merge-augmentations splice block → silent data loss." That signal is WRONG. `merge-augmentations.mjs` loads augmentations by **OUTPUT-JSON filename via `loadOptional("<name>.json")`** — NOT by generator script filename. 6 generators that ARE in FAST[] also have no filename match in merge yet work fine (cag-router, cited-tips-viz, jm-die-tribal-wiki, post-pdf-corpus, soul-health, stagnant). So a "generator filename not in merge-augmentations" check proves NOTHING about whether its data is merged. The real gap is FAST[] membership (does the generator run), then per-generator: does merge `loadOptional` its specific output name.

**Why:** verifying agent/grep claims against the ACTUAL consumer mechanism (named loadOptional vs filename match) is mandatory before acting — same class as the prior stale "open seeder bug" that was already fixed.

**How to apply (follow-up U-VIZ-FAST-REGISTER-9):** for EACH of the 9 — read generator → its output `*.json` name → confirm/add `loadOptional` in merge-augmentations → add to regen-viz FAST[] → dry-run standalone → THEN one regen materializes all + verify node-count delta. High blast radius (fleet-shared regen); peer-owned generators — coordinate, don't batch under context pressure. Spec: `state/shared/specs/SIERRA-GALAXY-COMPLETENESS-ASSESSMENT-2026-05-29.md`. See [[reference_sierra_fast_splice_dual_registration]] · [[reference_sierra_regen_pipeline_stages]].

**PROGRESS 2026-05-29 (per-generator verification, slot:sierra "build in priority order"):**
- ✅ **quoting-pipeline** — runner=self (has main), merge `loadOptional("quoting-pipeline-augmentation.json")` + splice line ~1557 pre-existed → FAST[]-add alone sufficed. Commit b6fa43575a.
- ✅ **hotel-domain** — runner=`run-hotel-domain-features.mjs` (the `generate-*` is a pure lib, NO main → FAST[] must point at the RUNNER, not the lib). Found a producer/consumer **path mismatch**: runner wrote to `staging/` but merge `loadOptional` reads VIZ_DIR **root** → roost unrendered even when run. Fixed path→root + stamped generatedAt + FAST[]-added the runner. Verified standalone (381 nodes/3 roosts). merge splice line ~1529 pre-existed. Commit 87816bf19c.
- ⏸ **7 remaining** (galaxy, hermes-zulu-ops, milling-tribal-tip-bridge, psn-health, sfc-variability, svi-component, vendor-catalog): each writes to a non-root path (staging/ or augmentations/ subdir — `loadOptional` reads VIZ_DIR root ONLY) and NONE is in merge's `loadOptional` list. Each needs (a) output→VIZ_DIR root + (b) a NEW merge `loadOptional` + splice.
- **MERGE-OOM UNBLOCKED 2026-05-29 (f87b3810ce):** the merge exit-134 was a HEAP CEILING (16GB too thin on the grown 576MB graph), NOT a reader gap. Bumped regen-viz NODE_ARGS + merge HEAP_MB_REQUIRED 16→24GB (host 136GB). **VERIFIED:** full `regen-viz.mjs` completed EXIT 0 in 6.5min, healthy 576MB graph, both already-wired roosts (ghost.business_frontend + ghost.quoting_pipeline) confirmed live. So the 7 are now splice-TESTABLE (a regen exercises them).
**RESOLVED 2026-05-30 (5/9 wired, commit 852ed7a3ac) — "build everything that will improve the system":** measured ground-truth (dry-ran all 7 from MAIN tree) → only 3 of the 7 are real wireable roosts. Wired milling-tribal-tip-bridge (12n, newNodes/newEdges, root), svi-component (15n, nodes/edges, output→root + kind-normalize splice), vendor-catalog (45n, nodes/edges, output→root + kind-normalize splice). Each: merge loadOptional + bespoke splice + FAST[]. **Verified end-to-end:** full regen EXIT 0, 573MB graph pending:0, all 3 exact ids (svi.component.wiring / ghost.vendor_catalog_audit / mill-tip-jmd-autodesk-cncbook) confirmed present in the merged graph. **DEFERRED 4 with measured reasons (wiring them would DEGRADE):** galaxy (exits 1/error), hermes-zulu-ops (emits a {panels} DASHBOARD not nodes/edges — not a roost), psn-health (>120s timeout), sfc-variability (>120s timeout + ~50K-node explosion → graph bloat). business-frontend confirmed PHANTOM (no 8th).

**LESSON (R8/R12 — workflow CWD trap):** a parallel-Explore recon workflow ran in the slot WORKTREE (H:/prism-slot-sierra) where these generators are absent/stale, and falsely reported "5 don't exist + only sfc wire-now". Trusting it would have wired the wrong set (incl. the 50K-node sfc bloat). Ground-truth MEASUREMENT from the main tree (dry-run + count) corrected it. **Workflow agents inherit a CWD that may be the worktree, not H:/prism — always re-verify file-existence/counts from /h/prism before acting on agent recon.** [[reference_sierra_system_viz_brain_assessment_2026_05_29]]

- **BUT the 7 are NOT a clean batch (per-generator quirks, discovered 2026-05-29):** key conventions DIFFER — hotel/quoting emit `{newNodes,newEdges}`; svi-component/vendor-catalog emit `{nodes,edges}` (so each splice must read the right keys). Node shapes differ too — svi-component nodes are `{id,type,label,layer,color,metadata}` with NO `ghost`/`status`/`parent`/`kind` (the roost convention other splices assume) → need shape normalization or they render unparented. Some have external data deps (svi-component fail-softs to no-output if `PSI_PATH` absent). So each of the 7 is a bespoke runner+splice+shape-normalize unit, NOT a copy-paste — match each generator's actual output keys + normalize node shape + regen-verify. Two fix-shapes proven: "FAST[]-add only" (runner+splice pre-exist, e.g. quoting) vs "runner-to-root + bespoke splice + shape-normalize" (the 7).
