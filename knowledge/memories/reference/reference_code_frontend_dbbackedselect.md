---
name: reference_code_frontend_dbbackedselect
description: "DbBackedSelect -- a reusable SFC select wired to a live reference-catalog endpoint."
metadata:
  type: reference
  kind: code-frontend
  galaxy: unassigned
  sourcePath: mcp-server/web/src/components/DbBackedSelect.tsx
  sourceHash: 1fd1cb47
  loc: 182
aliases: DbBackedSelect
---

# DbBackedSelect -- Frontend SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-04T14:51:51.225Z  |  source: `mcp-server/web/src/components/DbBackedSelect.tsx` (182 LOC)

## What it is
```
DbBackedSelect -- a reusable SFC select wired to a live reference-catalog endpoint.
=================================================================================
Loads its options from one of the 6 SFC reference-catalog endpoints (via
`SFC_CATALOG_FETCHERS` in `../api/sfcReferenceCatalogs`) and renders a native
`<select>` with honest loading / live / curated state. Replaces the hardcoded
`<select>` option lists in SpeedFeedPage / SfcCalculatorPage so every SFC input
is DB-driven.

Design rules (web/CLAUDE.md): >=44pt tap target (`h-11`, `md:h-9` desktop), no
inline hex/px (Tailwind utilities only), associated `<label>`, visible focus ring,
`aria-busy` while loading. The backend never returns a silent-empty list (it
fail-soft degrades to a curated set), so this surfaces `source` to the user
rather than hiding a degraded state.
```


## Exports
`DbBackedSelectProps` (interface), `DbBackedSelect` (function)

## Key methods
`useEffect`, `setLoading`, `setError`, `setOptions`, `setSource`, `onChange`, `onSelectOption`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/web/src/components/DbBackedSelect.tsx`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Frontend

## How to query it
Recall by meaning via `/system-viz find DbBackedSelect` or `prism_session:master_index_query`; open the source at `mcp-server/web/src/components/DbBackedSelect.tsx`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
