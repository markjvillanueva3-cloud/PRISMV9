---
name: reference_code_engine_ppmodalgroupconflictvalidatorengine
description: "Detect G/M code modal-group"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PPModalGroupConflictValidatorEngine.ts
  sourceHash: 4196a934
  loc: 361
aliases: PPModalGroupConflictValidatorEngine
---

# PPModalGroupConflictValidatorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PPModalGroupConflictValidatorEngine.ts` (361 LOC)

## What it is
Detect G/M code modal-group

```
PPModalGroupConflictValidatorEngine — Detect G/M code modal-group
conflicts within a single block.

Fanuc (and every ISO-derived control) partitions G and M codes into
*modal groups*. Only one code per group may be active. The standard
behaviour when a block contains two codes from the same group is
"last-one-wins" on some controls, "alarm" on others, and "silently
drop the earlier one" on Haas — exactly the kind of subtle dialect
divergence that makes a program pass in simulation and scrap a part
on the shop floor.

This engine does NOT track modals across blocks (see
`PPModalStateTrackerEngine` for that). It looks at *one block at a
time* and flags any pair of codes that belong to the same modal
group. A block that says `G0 G1 X10` is always ambiguous regardless
of prior state.

Modal groups enforced (Fanuc numbering):

  Group 01 — motion:            G0 G1 G2 G3 G33 G34 G73 G74 G76 G8
```


## Exports
`ModalConflictSeverity` (type), `ModalConflictKind` (type), `ModalConflictIssue` (interface), `ModalConflictResult` (interface), `ModalConflictOptions` (interface), `PPModalGroupConflictValidatorEngine` (class), `ppModalGroupConflictValidatorEngine` (const)

## Key methods
`validate`, `quickCheck`, `defaultOptions`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PPModalGroupConflictValidatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PPModalGroupConflictValidatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PPModalGroupConflictValidatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
