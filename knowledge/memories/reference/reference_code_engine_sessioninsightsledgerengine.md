---
name: reference_code_engine_sessioninsightsledgerengine
description: "Append-only reflection ledger"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SessionInsightsLedgerEngine.ts
  sourceHash: 8064d234
  loc: 142
aliases: SessionInsightsLedgerEngine
---

# SessionInsightsLedgerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SessionInsightsLedgerEngine.ts` (142 LOC)

## What it is
Append-only reflection ledger

```
SessionInsightsLedgerEngine — Append-only reflection ledger

Phase 0.13 U-SAW7 from UNIVERSAL-SKILLS-SCRIPTS-HOOKS-PLAN. Writes
schema-validated insight entries to `SESSION_INSIGHTS_LEDGER.jsonl`. Each
entry records what a session learned when a milestone completed so the
next session can reference it.

Write-contract:
  - Append-only — existing lines are never rewritten.
  - One JSON object per line (JSONL).
  - schemaVersion=1 on every entry; readers gate on it.

The engine exposes validation + serialization; the actual file I/O is
performed via the `writer` callback so callers can plug in atomic-write
utilities, in-memory sinks, or tests without patching fs.

@module engines/SessionInsightsLedgerEngine
@milestone PP-0.13-U-SAW7
```


## Exports
`InsightCategory` (type), `InsightEntry` (interface), `LedgerAppendResult` (interface), `LedgerWriter` (type), `SessionInsightsLedgerEngine` (class)

## Key methods
`validate`, `buildEntry`, `serialize`, `parse`, `append`, `record`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/SessionInsightsLedgerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SessionInsightsLedgerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SessionInsightsLedgerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
