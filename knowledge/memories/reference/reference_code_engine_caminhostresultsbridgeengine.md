---
name: reference_code_engine_caminhostresultsbridgeengine
description: "that arrive over CAMPluginCommunicationHub. Collects per-scenario"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMInHostResultsBridgeEngine.ts
  sourceHash: 78c03af4
  loc: 254
aliases: CAMInHostResultsBridgeEngine
---

# CAMInHostResultsBridgeEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMInHostResultsBridgeEngine.ts` (254 LOC)

## What it is
that arrive over CAMPluginCommunicationHub. Collects per-scenario

```
CAMInHostResultsBridgeEngine — U-CAMTEST15
===========================================

PHASE-8: Server-side aggregator for the in-host runner result envelopes
that arrive over CAMPluginCommunicationHub. Collects per-scenario
BundleResult outputs (from U-CAMTEST14) keyed by scenario_id × host,
tracks pass/fail aggregates, and persists to
`data/state/CAM_INHOST_RESULTS.json` so the U-CAMTEST16 nightly CI
orchestrator can render a dashboard from a single source of truth.

In-memory store + lazy persistence: ingestEnvelope mutates the store
synchronously; persist() flushes to disk on demand. Tests can swap the
path so production state is never touched.

@module engines/CAMInHostResultsBridgeEngine
@milestone CAM-EXHAUST-MS0 U-CAMTEST15
```


## Exports
`FixtureHostSchema` (const), `FixtureHost` (type), `FixtureCategorySchema` (const), `FixtureCategory` (type), `ResultEnvelopeSchema` (const), `ResultEnvelope` (type), `ResultsFileSchema` (const), `ResultsFile` (type), `SummarySchema` (const), `Summary` (type), `SCHEMA_VERSION` (const), `DEFAULT_RESULTS_PATH` (const), `CAMInHostResultsBridgeEngine` (class), `camInHostResultsBridgeEngine` (const)

## Key methods
`ingestEnvelope`, `ingestBundle`, `list`, `listByHost`, `listByCategory`, `listFailures`, `count`, `summarize`, `reset`, `persist`, `load`, `auditBridge`

## Depends on
[[reference_code_engine_caminhostassertionbundleengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CAMInHostResultsBridgeEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMInHostResultsBridgeEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMInHostResultsBridgeEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
