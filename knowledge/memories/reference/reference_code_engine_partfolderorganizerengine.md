---
name: reference_code_engine_partfolderorganizerengine
description: "JM Die per-customer / per-part-number folder library"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PartFolderOrganizerEngine.ts
  sourceHash: afc93e25
  loc: 840
aliases: PartFolderOrganizerEngine
---

# PartFolderOrganizerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PartFolderOrganizerEngine.ts` (840 LOC)

## What it is
JM Die per-customer / per-part-number folder library

```
PartFolderOrganizerEngine — JM Die per-customer / per-part-number folder library

Maintains the standardized intake folder for every part that enters the system:

  <libraryRoot>/<CUSTOMER>/<PART NUMBER>/
    <PART NUMBER>__<srcpdf>__p<page>.pdf   ← the print(s) + related docs (folder root)
    part.json                               ← manifest (provenance, match confidence, source paths)
    CNC PROGRAM/                             ← .min / .nc / .eia / .hnc / ...  (the NC program(s))
      <files>
    CAD-CAM/                                 ← .mcx-8 / .ipt / .iam / .step / .dwg / ...  (CAM project + CAD model)
      <files>

libraryRoot defaults to `H:/PRISM/JM DIE/_PART LIBRARY`. Layout knobs (subdir names,
sanitization rules, file-classification ext lists, customer-resolution priority) live in
`mcp-server/data/state/part-library-layout.json` so this engine and the one-time bulk
back
```


## Exports
`PartLibraryLayout` (interface), `getLayout` (function), `sanitizeSegment` (function), `partFolderPath` (function), `isPlausibleCustomer` (function), `canonicalizeCustomer` (function), `_resetCanonicalizationCache` (function), `resolveCustomer` (function), `classifyFile` (function), `PrintRef` (interface), `ProgramRef` (interface), `PartManifest` (interface), `CreatePartFolderInput` (interface), `CreatePartFolderResult` (interface), `PartFolderOrganizerEngine` (class), `partFolderOrganizerEngine` (const)

## Key methods
`execFileSync`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PartFolderOrganizerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PartFolderOrganizerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PartFolderOrganizerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
