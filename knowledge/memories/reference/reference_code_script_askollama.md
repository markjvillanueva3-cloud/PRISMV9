---
name: reference_code_script_askollama
description: "ask-ollama.mjs — local Ollama query service (OLLAMA-EXPAND-MS0/U-OE01)"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/ask-ollama.mjs
  sourceHash: 044681b8
  loc: 1325
aliases: ask-ollama.mjs
---

# ask-ollama.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/ask-ollama.mjs` (1325 LOC)

## What it is
```
ask-ollama.mjs — local Ollama query service (OLLAMA-EXPAND-MS0/U-OE01)

Routes token-heavy work to a LOCAL Ollama model so it never enters the
Claude context window. Claude invokes this via Bash; only the compact
answer returns. Heavy inputs — the ~27 MB system-viz graph, large source
files, build-error dumps — are processed here in the subprocess and
discarded.

Modes:
  viz <query>        ranked keyword search of the system-viz graph.
                     Default: returns compact hits (fast, no model).
                     --synth: also adds an Ollama-synthesized answer.
  rerank <query>     viz hits, then a VERIFIED ollama re-rank (model proposes
                     the order, code keeps only real candidate ids; fail-safe
                     to lexical). Enforces ollama for search/navigation ranking.
  summarize <file>   compact digest of a large file ("-" = read stdin)
  explain <f
```


## Exports
`MAX_FILE_BYTES` (const), `FILE_MODES` (const), `TEXT_MODES` (const), `ALL_MODES` (const), `truncate` (function), `estimateTokens` (function), `scaleTimeoutForBytes` (function), `pickModel` (function), `OFFLOAD_LOADED_PREFERENCE` (const), `CODER_LOADED_PREFERENCE` (const), `loadWarmModels` (function), `parseArgs` (function), `tokenizeQuery` (function), `scoreNode` (function), `searchGraph` (function), `renderHits` (function), `buildVizPrompt` (function), `buildFilePrompt` (function), `buildAskPrompt` (function), `buildCodegenPrompt` (function)

## Key methods
`clearTimeout`, `renderHits`, `pickLoadedChatModel`, `recordOllamaEvent`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/ask-ollama.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find ask-ollama.mjs` or `prism_session:master_index_query`; open the source at `scripts/ask-ollama.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
