---
name: reference_code_engine_hooktelemetryengine
description: "HookTelemetryEngine — Queueing Theory Metrics for Hook Health"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HookTelemetryEngine.ts
  sourceHash: 9f603438
  loc: 683
aliases: hookTelemetryEngine
---

# hookTelemetryEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HookTelemetryEngine.ts` (683 LOC)

## What it is
```
HookTelemetryEngine — Queueing Theory Metrics for Hook Health

Implements Little's Law monitoring: L = λW
- λ = arrival rate (tool calls/sec)
- μ = service rate (hook completions/sec)
- ρ = utilization = λ/μ (must be < 1 for stability)
- L_q = average queue length = ρ²/(1-ρ)
- W_q = average wait time = L_q/λ

Alerts when ρ > 0.8 (system saturating)

Theory: M/M/1 queue for single-threaded hook execution

Persistence layer (2026-05-15, pillar-telemetry-recovery U-PTR01):
  Set env `PRISM_HOOK_TELEMETRY_PATH` to an absolute JSON path to enable
  crash-survivable telemetry. The engine auto-loads on construct (sync) and
  debounce-flushes (default 5000 ms) on every recordEnd. Atomic write via
  `.tmp` + rename. Set `PRISM_HOOK_TELEMETRY_DISABLE=1` to skip even when
  the path is set. Backward compat: with neither env set, behavior is the
  pre-2026-05-15 in-memory-only engine.

@module Phase
```


## Exports
`HookInvocation` (interface), `HookStats` (interface), `SystemMetrics` (interface), `Alert` (interface), `PersistResult` (interface), `LoadResult` (interface), `hookTelemetryEngine` (const), `HookTelemetryEngine` (type)

## Key methods
`getPersistPath`, `setPersistPath`, `clearTimeout`, `markDirty`, `persist`, `loadPersisted`, `recordStart`, `recordEnd`, `getHookStats`, `getSystemMetrics`, `getAlerts`, `clearAlerts`, `getAllHookStats`, `isHealthy`, `getHealthSummary`, `reset`, `export`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/HookTelemetryEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find hookTelemetryEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HookTelemetryEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
