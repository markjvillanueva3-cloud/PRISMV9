---
name: reference_code_engine_wedmunittaggateengine
description: "Unit-Tag & 25.4× Confusion Guard"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WEDMUnitTagGateEngine.ts
  sourceHash: 35b9952c
  loc: 352
aliases: WEDMUnitTagGateEngine
---

# WEDMUnitTagGateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WEDMUnitTagGateEngine.ts` (352 LOC)

## What it is
Unit-Tag & 25.4× Confusion Guard

```
WEDMUnitTagGateEngine — Unit-Tag & 25.4× Confusion Guard

Validates that an emitted WEDM program's unit declaration is coherent across
three sources of truth:

  1. The caller-declared unit  (input.units, metric | imperial)
  2. The G-code header         (G21 metric | G20 imperial | missing)
  3. The coordinate magnitudes (numerical sanity vs declared stock size)

The 25.4× confusion class is the single most common — and most dangerous —
WEDM unit bug: a machinist programs a 2-inch feature as "2.0" but the
control is in metric, producing a 2 mm feature (or vice versa, producing
a 50.8 mm feature when 2 mm was intended). The result is machine crashes,
scrapped stock, or silent geometric errors that only surface on FAI.

This engine cross-checks all three sources and emits a single AtomicValue
verdict + a safety_gate_input shape that WEDMProgramSafetyGateEngine's
`unit_tag` slot consumes d
```


## Exports
`UnitSystem` (type), `UnitHeaderCode` (type), `UnitTagVerdict` (type), `UnitTagInput` (interface), `UnitTagComponentMetrics` (interface), `UnitTagGateResult` (interface), `WEDMUnitTagGateEngine` (class), `wedmUnitTagGateEngine` (const)

## Key methods
`evaluate`, `gate`, `detectHeader`, `detectMaxCoordinate`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WEDMUnitTagGateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WEDMUnitTagGateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WEDMUnitTagGateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
