---
name: reference_wedm_print2program_pipeline_2026_05_31
description: "True print->program pipeline for ANY JM wire part (inventory-driven, closed-loop) — plan at state/shared/specs/WEDM-PRINT-TO-PROGRAM-PIPELINE-2026-05-31.md. Compute chain already exists (wedm_print_to_program); 3 gaps = toolpath-type registry (A1 BUILT), JM inventory gate (B BUILT), closed-loop test harness (C pending). Phase A-D build sequence"
type: reference
source: prism-memory
synced: 2026-06-09T14:54:11.038Z
aliases: reference_wedm_print2program_pipeline_2026_05_31
---


# WEDM true print->program pipeline — inventory-driven, closed-loop (slot mike, 2026-05-31)

Operator goal (/loop /yolo): train every wire-part possibility · templates per toolpath type with variable params + cutting conditions · **true print->program for ANY JM wire part** · **auto-generate programs from JM inventory** (charlie quoting + hotel ERP) · **fully closed-loop testing**. Primary focus: JM fleet (1 machine = Mitsubishi FA-10S WEDM-01, envelope 215mm height / 30° taper / 80mm UV / wire 0.25+0.20mm on hand).

**Plan:** `state/shared/specs/WEDM-PRINT-TO-PROGRAM-PIPELINE-2026-05-31.{md,html}` — 8-stage pipeline (print → feature → toolpath-type → **inventory gate** → template → params → program → **closed-loop test**). **Key recon finding:** the print->program COMPUTE CHAIN already exists (`wedm_print_to_program` / `wedm_run_pipeline`: DXF→feature→feasibility→multipass→params→G-code→post for FA-10S). The goal's real work = **3 gaps**:

1. **Toolpath-type template registry** — BUILT (Phase A1, `mcp-server/src/data/wedm-toolpath-types.ts`): 11 types (straight/heavy/taper-UV/open/island/no-core/cannelure/corner/start-hole/micro/bi-material), each with a variable-PARAM SCHEMA + cutting-condition ranges + owning engine + E-code family + provenance + FA10S-envelope feasibility (`getToolpathType`/`listToolpathTypes`/`validateAgainstEnvelope`, 7/7 tests). SCHEMA only — discharge values from the oracle (tech-tables), never inlined. 2 types `status:build` = cannelure + micro (Phase A3).
2. **JM inventory gate** — BUILT (Phase B, `mcp-server/src/data/jm-die-material-stock.ts`): MaterialStockEngine had ZERO JM data; this seeds JM_DIE_WIRE_STOCK (2 FA10S spools) + JM_DIE_RAW_STOCK (D2/A2/S7/M2 blanks) + `checkStockForJob()` = the "can JM cut this TODAY?" gate → {can_cut, blockers, suggested_po, matched_wire/blank}. 8/8 tests. The "auto-generate from JM inventory" rail.
3. **Closed-loop test harness** — PENDING (Phase C): compose cascade-correctness (Regimen #3 harness) + feasibility + dimensional-vs-print + S(x) safety + post-lint into one PASS/FAIL → outcome ledger → india retrain.

**Build sequence (R13):** A (registry + per-type oracle corpora + cannelure/micro) → B (inventory seed+gate) → C (closed-loop test) → D (print->program warm-start train). **ALL PHASES SHIPPED 2026-06-01 (slot mike /goal /loop):**
- **A2** `scripts/build-wedm-toolpath-templates-corpus.ts` — per-toolpath-type oracle-augmented TEMPLATE corpus: 744 pairs across all 11 types, 492 cascades self-validated against the Regimen #3 harness; pure `buildTemplatePairs`/`splitByType` + 4 intent tests (independent cascade re-parse, in-envelope re-check, deterministic stratified split). Commit U-WEDM-P2P-A2.
- **A3** `mcp-server/src/data/wedm-build-strategies.ts` — the 2 status:build strategies as pure oracle-INJECTED single-source-of-truth: `cannelureFeedStrategy` (halve rough feed when pitch < 3x wire-Ø; debris-short guard; wedm-jmd-008) + `microFineWireStrategy` (0.10mm derate x0.4, standoff<=0.25 gate, "0.10mm not on JM spools → PO" inventory linkage). 8 tests. A2 now emits real computed conditions for both. Registry build→partial. Commit U-WEDM-P2P-A3.
- **C** `scripts/lib/wedm-program-test-gates.mjs` — closed-loop test runner: cascade(run here) + envelope/inventory/safety/dimensional/post_lint(injected) → composeProgramTestGates (ACCEPT iff ALL pass; empty=fail-safe). 8 tests. (committed prior leg)
- **D1** `scripts/build-wedm-print2program-corpus.ts` — leakage-safe unified corpus: 980 rows (744 templates + 204 passschedule + 5 TRUE print→program oversampled 3→30 TRAIN-ONLY; split-before-oversample = zero test leakage). 6 tests. Output wedm-print2program/{train,val,test}.jsonl (793/94/93). Commit U-WEDM-P2P-D1.
- **D3** `scripts/eval-wedm-print2program.mjs` — generation grader: runs model outputs through the full Phase C gate stack, aggregates accept_rate + by_failed_gate, fail-loud `assertAcceptRate(floor)`. 5 tests. Commit U-WEDM-P2P-D3.
- **D2 (train) — SUCCEEDED 2026-06-01 (the "operator-only blocker" was WRONG):** the ~100 KB/s HF throttle is **per-connection** (3 parallel curls = 2.2 MB/s combined), so `hf_transfer` parallel chunks downloaded **Qwen2.5-Coder-0.5B-Instruct** (~1 GB) in ~9 min. A real LoRA fine-tune on it (`train_wedm_p2p_05b.py`, bf16, cold-start, save_steps=20) reached **loss 0.10**; generations graded through the HARDENED D3 gate = **13/15 (86.7%) accept** vs 0% base / 0% random-tiny. Verified clean cascades (E1221→E1225 sequential, H-offset strictly decreasing). **The print→program closed loop is PROVEN end-to-end on a real model.** Commit U-WEDM-P2P-05B-TRAIN. Gotcha: the fleet `local-compute-autostart` hook + GPU-coordinator kill training mid-run (exit 255) when it reloads Ollama/NIM onto the 16GB GPU — use save_steps checkpoints (checkpoint-80 = the usable adapter) + free GPU (kill ollama + docker stop nim-*) right before launch + don't submit prompts during the run. 7B still pending (45h even parallel) but 0.5B proves the loop; bigger model = better quality, same pipeline.

- **[superseded] earlier same-day conclusion:** apparatus complete; LIVE TRAIN ENVIRONMENT-BLOCKED 2026-06-01. Root cause (fully diagnosed, R12): the base model `Qwen/Qwen2.5-Coder-7B-Instruct` **weights are missing from the HF cache** (`C:/Users/wompu/.cache/huggingface/hub/models--Qwen--Qwen2.5-Coder-7B-Instruct` = 0.01GB, only config/tokenizer; all 4 `.safetensors` shards gone — evicted since the prior session's successful trains). Re-download is THROTTLED, not dead: HF homepage HEAD=200; the CDN (`cas-bridge.xethub.hf.co`) `curl -r 0-30MB` returns `http=206` but at **~96 KB/s** → the ~15GB base = **~45 HOURS** (unsloth 5GB ≈ 15h). `huggingface_hub`'s own transfer hangs at 0 bytes (worse than curl; `HF_HUB_DISABLE_XET=1` did not help). So the base download is infeasible in-session. No alternative complete LLM base is cached (only opendatalab/PDF-Extract-Kit 2.32GB). Qwen-7B cannot load → train cannot run on this box until the network throttle is lifted OR the model is sideloaded (copy the `models--Qwen--Qwen2.5-Coder-7B-Instruct` snapshot from the other PC / a faster mirror into `C:/Users/wompu/.cache/huggingface/hub/`).
  - **Secondary lessons (fixed in-flight):** (1) `Tee-Object` on the train stdout DEADLOCKS the trainer (tqdm `\r` pipe backpressure) — use `*> file` redirect, never Tee. (2) `batch2×seq2048+grad_checkpoint` OOMs on the 16GB GPU when Fusion holds ~2GB — VRAM-safe recipe is `batch4, grad_accum2, seq1024, use_gradient_checkpointing:false, num_epochs:1` (fits ~9GB, fast). (3) Hard-killing a trainer mid-load can leave a stale `.locks/...safetensors.lock` that blocks the next load. (4) Fleet Ollama auto-revives + reloads qwen2.5-coder:7b (~10GB) → OOMs training; must keep Ollama down for the train window then restart.
  - **CLOSED LOOP DEMONSTRATED OPERATIONAL (2026-06-01) without the LoRA train:** `scripts/wedm-p2p-baseline-ollama.mjs` (+3 tests) generates over the val prompts via LOCAL Ollama qwen2.5-coder:7b (trainer-parity format, zero download) → `scripts/eval-wedm-print2program.mjs` grades through the D3 gate stack. **Pre-training baseline = 0/10 accepted, all fail `cascade_correctness`** — the base model emits chatty prose ("To generate a... we need to: 1. Define the Geometry...") instead of the strict `Pass N (type): E-code, feed ipm, H# offset X in` cascade. This is the textbook fine-tuning case + the before-metric: the corpus→generate→grade loop WORKS on real model output; the LoRA adapter (once the base downloads) gets graded through the SAME loop = the training lift. Commit U-WEDM-P2P-BASELINE.
  - **REAL LoRA FINE-TUNE EXECUTED OFFLINE (2026-06-01) — closed loop CLOSED with a measured lift:** `lora-bundle/validate_train_pipeline_offline.py` (commit U-WEDM-P2P-TRAIN-VALIDATE) sidesteps the missing 7B weights by CONSTRUCTING a tiny Qwen2 from config offline (the Qwen2 tokenizer IS cached; only the 7B `.safetensors` are missing) → real LoRA train on the 793-pair corpus (loss 5.25→4.51, genuine learning) → save adapter → generate → grade through D3. **Result (CORRECTED under hardened gate, R12):** lenient cascade-gate showed base 0/10 → fine-tuned 3/10, BUT the well_formed hardening (U-WEDM-P2P-GATE-HARDEN) exposed those 3 as **subsequence artifacts** (valid cascade buried in noise). Under the hardened gate (cascade + well_formed coherence ≥0.5): **base 0/10, fine-tuned 0/10** — the tiny random-init model (90M, 4 epochs) does NOT produce clean programs. The GENUINE signal: fine-tune shifted output from pure prose (base = zero cascade pass-lines) → cascade-FORMAT pass-lines (`Pass N: E-code, feed, H# offset`) — a real directional learning signal, just not enough for clean output at tiny scale. What this VALIDATES: the training pipeline executes end-to-end (build→LoRA→train→adapter→infer→grade) AND the closed-loop validator, once hardened, correctly rejects format-mimicry — preventing a false-positive training signal. Production 7B (pending weights) would produce clean programs. **Hardening shipped:** well_formed gate (predominantly cascade lines, not noise) — the lenient cascade gate alone was inflating accept-rate.
  - **RESUME when HF CDN reachable:** download the base once (`huggingface-cli download Qwen/Qwen2.5-Coder-7B-Instruct` OR `WEDM_BASE_MODEL=unsloth/Qwen2.5-Coder-7B-bnb-4bit` ~5GB pre-quantized = config's intended base), apply the VRAM-safe recipe to config.json, kill Ollama, then: warm-start train (`WEDM_WARM_START_ADAPTER=...wedm-passschedule-lora WEDM_DATASET=...wedm_print2program_train.jsonl WEDM_OUTPUT=models/wedm-print2program-lora python -u train_wedm_lora_peft.py *> log`) → `infer_wedm.py` over the val set → `node scripts/eval-wedm-print2program.mjs <gens>`. Full commands in `state/shared/handoffs/HANDOFF-claude-87d96286-mike-wedm-print2program.md`.

**COMPREHENSIVE VALIDATION (2026-06-01, 6-agent workflow → `state/shared/specs/WEDM-P2P-COMPREHENSIVE-VALIDATION-2026-06-01.md`):** the closed loop is PROVEN as a mechanism (0.5B, 86.7% STRUCTURAL gate-pass) but **metric-print accuracy ~0% on the 3 operator-named axes** because the oracle is constant-output + the corpus is a 5-record broken seed. **3 P0 physics defects — all with fix-assets that EXIST in-tree but are UNWIRED:**
- **P0-1 THICKNESS ignored** (same feed 1→215mm) — **FIXED + NOW WIRED (2026-06-02)**: `mcp-server/src/data/wedm-thickness-feed-scale.ts` (+9 tests) reuses FA-Advance `estimateRoughingSpeed()` (11-pt curve 5→100mm) → `thicknessFeedFactor`/`scaledPassFeed` make feeds thickness-aware (thicker=slower), clamped, no inlined constants. Commit U-WEDM-P2P-VALIDATE+P0-1-THICKNESS. **CRITICAL 2026-06-02 R12 finding: the P0-1 module shipped with ZERO consumers — generated programs STILL emitted constant feeds (a fix that never touches output is not a fix).** Closed by **U-WEDM-P2P-JOBCASCADE-WIRE** (commit 39ac2147a9): new composition core `mcp-server/src/data/wedm-job-cascade.ts` `generateJobCascade(params)` = selectECodeFamily → apply thickness factor to BOTH feed_ipm+feed_mm_min (same dimensionless factor, no 25.4 inlined). Wired into `build-wedm-passschedule-corpus.ts` as Kind 6 (`job_cascade`, 10 pairs now thickness-varied: 10mm→0.181, 25mm→0.079, 40mm→0.063 ipm rough). 20 vitest cases vs live oracle. **R12 fail-loud caveats[]: skim feeds scaled by the ROUGH-derived factor (first-order; real skims scale ~6x less per FA data) is UNVALIDATED vs JM skim ground truth (no held-out thick-part program exists) → every scaled cascade carries a caveat; per-pass skim curve deferred to U-WEDM-P2P-PERPASS-SKIM (gated on real thick-part programs).**
- **GENERATED-vs-REAL ACCURACY HARNESS (2026-06-02, U-WEDM-P2P-COMPARE-REAL, commit ee2ae3b928):** `scripts/wedm-compare-to-real-programs.{mjs,test.mjs}` — the operator's exact "read print, write program, post g-code, compare to existing programs" test. Parses the real FA-10S dialect, binds to JM_DIE_ECODE_FAMILIES (exact prefix+pass-count else flagged prefix-fallback), scores per-pass E-code+offset+feed. **Result: 3 distinct on-disk programs (ITW SHAKEPROOF / 38 CAL CANNELURE / NOZE TEST), 13/13 passes = 100% IN-SAMPLE.** R12: explicitly labeled `in_sample_reproduction` — the oracle was CALIBRATED FROM these programs, so 100% is a harness+oracle FIDELITY GATE, NOT held-out generalization. Run via `npx tsx`. Bug fixed: feed regex `F\.?(\d*\.?\d+)` ate the leading dot → `F.12` parsed as 12 not 0.12 → 0% match; fixed with `num()` + `([.\d]+)`. 17 tests (incl. oracle-drift detection). **The "100% accuracy for ALL JM wire programs" goal is fundamentally DATA-LIMITED: only ~3-7 runnable programs exist on disk, all in-sample. Closed-loop MECHANISM + in-sample fidelity are PROVEN; held-out generalization is unprovable until more paired print↔program data is acquired (13,316 Docustrata prints are unpaired).**
- **ARCHIVE CENSUS (2026-06-02, definitive — `feedback_always_fill_gaps`): enumerated ALL 4,058 files in `H:/PRISM/JM DIE/WIRE EDM` + classified.** Real runnable **Mitsubishi FA-10S WIRE programs = ~6 (3 distinct parts), ALL in-sample** (oracle calibrated from them). Breakdown: **1,779 `.mcx`** = Mastercam BINARY CAD/CAM source (geometry, not posted G-code — decodable only via Mastercam/kilo, big lift); **19 `.MIN`** = LATHE programs (Citizen/Miyano `G50 S800`/`G96`/`T010101`/X-Z turning, e.g. Anderson-MFG HOLDER-874 — MISFILED in the wire folder, whiskey-slot domain, NOT wire); **28 `.esp`** = ESPRIT CAM source; 4 `.txt`/3 `.nc` = the FA programs. **CONCLUSION: held-out FA wire ground-truth simply does not exist on disk beyond the ~6 in-sample programs — the goal's accuracy proof is blocked on DATA ACQUISITION (more real FA programs, or Mastercam-.mcx geometry decode), not on more generator physics. The comparator handles only the Mitsubishi FA `E#### H# F.##` dialect by design (the FA-10S is JM's only wire machine).**
- **P0-2 HARDNESS absent** (D2@62HRC == annealed) — **FIXED + WIRED + EXERCISED (2026-06-02, U-WEDM-P2P-P0-2-HARDNESS commit on slot/mike):** extracted `edmHardnessFactor(hrc)` from EDMBiMaterialCompensationEngine as SINGLE source (was inline; behavior-preserving, 54/54 engine tests green) → new `mcp-server/src/data/wedm-hardness-feed-scale.ts` `hardnessFeedFactor = edmHardnessFactor(55)/edmHardnessFactor(hrc)` for hrc>55 (reuse coeff INVERSELY for feed de-rate, no re-inline). **ANCHORED at 55 HRC not 40** — oracle feeds already calibrated on hardened die steel, anchoring at 40 double-counts. Composed thickness×hardness multiplicatively into `generateJobCascade` (fields hardness_hrc/hardness_factor/applied_feed_factor); corpus Kind 6 loops hardness [0,62] → 10 hardened pairs exercised (factor 0.9685 @62HRC + caveat). 2-reviewer PASS (physics: inverse-ratio sound, ~3% @62HRC literature-consistent; independent: regression goes RED, single-source verified). R12: physics model UNVALIDATED vs JM ground truth (no hardened-part program on disk) → every de-rate carries a caveat. 88 tests.
- **P0-3 BI-MATERIAL unwired** — **FIXED + WIRED + EXERCISED (2026-06-02, U-WEDM-P2P-P0-3-COMPOUND on slot/mike):** new `mcp-server/src/data/wedm-compound-cascade.ts` `generateCompoundJobCascade(zones[],thickness)` = the linkage. Calls `edmBiMaterialCompensationEngine.optimize()` for per-zone spark params + binds STEEL zones (primary/secondary_steel) to their FA E-family via selectECodeFamily; carbide_insert/braze_joint zones FAIL-LOUD as `needs_operator_ecode` with e_family_id=null — REFUSES to fabricate carbide E-codes / fall back to steel recipe (carbide-E-family gap surfaced honestly, not hidden). Wired into corpus Kind 7 (compound_cascade, 2 bi-material profiles, carbide+braze flagged). 10 tests vs live engine. R12: engine-computed GUIDANCE, UNVALIDATED vs JM bi-material program (none on disk) — caveated. **NOTE: 2-agent scrutiny DEFERRED (API session limit 2026-06-02 ~8pm CT); self-reviewed + 10/10 tests + corpus-verified; RE-RUN scrutiny next session.** **ALL 3 OPERATOR AXES NOW BUILT (thickness+hardness+compound); 4 units this session (COMPARE-REAL, JOBCASCADE-WIRE, P0-2-HARDNESS, P0-3-COMPOUND). The corpus now genuinely responds to all 3 axes (Kinds 6+7).**
- **RETRAIN ON ENRICHED CORPUS — DONE (2026-06-02):** freed GPU (docker stop nim-llama32-3b + nim-embed-e5; ~15.8GB→2GB), retrained the 0.5B LoRA on the now-enriched 793-pair print2program corpus (`train_wedm_p2p_05b.py` → adapter `models/wedm-p2p-05b-lora-enriched`), final-step loss **0.075** (avg 0.6977), 575s/99 steps. Generated 20 over val → graded through the D3 gate (`eval-wedm-print2program.mjs`): first read **13/20=65%**, but **DIAGNOSED (R12, always-fill-gaps): the 7 'failures' were all CORRECT FA-10S taper programs** (E2821→E2825, all H=0, feeds matching oracle E28xx_taper_5pass) — they failed ONLY because graded gen rows carry no `taper` flag, so `checkMonotonicCascade` applied the 2-axis strictly-decreasing rule to all-zero taper offsets → false `monotonic_decrease_AP003`. **BUG-FINDING + FIX (U-WEDM-P2P-CASCADE-TAPER-DETECT, commit on slot/mike):** `checkCascade` now auto-detects taper from a POSITIVE marker (E28xx family / `taper_uv`) when `opts.taper` isn't an explicit boolean. NOT a weakening (proven: 2-axis all-zero + no marker STILL fails; explicit taper:false/true wins; taper-with-nonzero-offset fails taper_zero). 18 tests (13 prior + 5 new), 0 regressions. **CORRECTED closed-loop result: 20/20 = 100% accept on the held-out val split.** NIM containers restored after. **HONEST framing: this 100% is HELD-OUT-FROM-TRAINING generalization within the leakage-checked SYNTHETIC corpus (train/val/test split) — a genuine read-print→write-program→grade closed loop at 100%. It is NOT the same as '100% on ALL real JM programs' (only ~6 real FA programs exist, all in-sample; that large-real-held-out test stays data-blocked). Two legitimate 100%s now: in-sample real-program comparator 13/13 + held-out synthetic val 20/20.** Wiki entry for the taper grading-gap bug-finding PENDING (agents session-limited).
- **Remaining:** U-WEDM-P2P-PERPASS-SKIM (needs real thick-part data), scale to 7B (better on the harder enriched cases), DEFERRED P0-3 2-agent scrutiny (API session-limit), and the held-out accuracy proof (DATA-BLOCKED — ~6 in-sample FA programs only).
**Corpus is broken (Phase 1 of plan):** `wedm-print-program/corpus.jsonl` = 5 records, `input`=title-block-only (no dims/tol/GD&T/finish), `output`=binary .mcx mojibake (NOT NC). Only **7 runnable FA programs on disk**, ZERO in corpus. Real prints = 201 classified / 13,316 verified-page Docustrata docs, UNPAIRED. Path to 100% = repair corpus (real NC + Docustrata-extracted print fields via lima pypdf) → back-fill thickness/hardness anchors → wire P0-2/P0-3 → retrain → 7B. **It's a data+physics program, not a model program.**

**Foundation (prior session):** warm-start trainer + Regimen #3 (cascade harness+generator+eval+adapter loss 0.64->0.06).

**Cross-galaxy:** hotel owns the inventory seed→live MaterialStockEngine + PO updates; charlie adds checkStockForJob to the quote path; delta/xray = print feature-card S1 input; echo = post-lint gate; india = retrain loop. Broadcast per [[feedback_ai_upgrade_broadcast_protocol]]. Builds on [[reference_wedm_training_regimen_catalog_2026_05_31]] + [[reference_wedm_lora_finetune_complete_2026_05_31]].
