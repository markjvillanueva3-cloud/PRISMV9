---
name: reference_code_engine_printcorpustablewriter
description: "append-only JSONL writer for the corpus-wide"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PrintCorpusTableWriter.ts
  sourceHash: ecf93045
  loc: 274
aliases: PrintCorpusTableWriter
---

# PrintCorpusTableWriter -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PrintCorpusTableWriter.ts` (274 LOC)

## What it is
append-only JSONL writer for the corpus-wide

```
PrintCorpusTableWriter — append-only JSONL writer for the corpus-wide
print-scan inventory. Wraps PrintCorpusRow with an sha256-keyed byte-offset
index for O(1) lookup, resumable checkpoint, atomic writes under O_EXCL lock,
and a per-customer sub-table mirror.

Storage layout (default DEFAULT_DIR):
  rows.jsonl            — append-only canonical record (one JSON-line per row)
  index.json            — { sha256: byteOffset } map for O(1) read(sha)
  checkpoint.json       — { shasSeen, lastScannedAt, totalRowsWritten } for
                          orchestrator resume
  by-customer/<slug>.jsonl — per-customer mirror (for 100% accuracy gate)
  .write.lock           — O_EXCL lock file (stale-locked after 30s)

Concurrency:
  - Multiple orchestrator processes may write concurrently — the lock
    serialises Index + rows.jsonl updates; rows.jsonl is opened with O_APPEND
    so the kernel guara
```


## Exports
`PrintCorpusWriteResult` (interface), `PrintCorpusCheckpoint` (interface), `PrintCorpusTableWriter` (class), `defaultPrintCorpusTableWriter` (const)

## Key methods
`write`, `read`, `has`, `allShas`, `readCheckpoint`, `totalRowCount`

## Depends on
[[reference_code_schema_printcorpusrow]]

## Where it lives
- **Source:** `mcp-server/src/engines/PrintCorpusTableWriter.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PrintCorpusTableWriter` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PrintCorpusTableWriter.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
