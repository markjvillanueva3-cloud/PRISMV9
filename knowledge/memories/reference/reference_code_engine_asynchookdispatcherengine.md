---
name: reference_code_engine_asynchookdispatcherengine
description: "HOOK-SYNERGY-MS0 / U-HOOK-ASYNC-DISPATCH (H7)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/AsyncHookDispatcherEngine.ts
  sourceHash: 534dca25
  loc: 842
aliases: AsyncHookDispatcherEngine
---

# AsyncHookDispatcherEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/AsyncHookDispatcherEngine.ts` (842 LOC)

## What it is
HOOK-SYNERGY-MS0 / U-HOOK-ASYNC-DISPATCH (H7)

```
AsyncHookDispatcherEngine — HOOK-SYNERGY-MS0 / U-HOOK-ASYNC-DISPATCH (H7)

Decouples Tier-4 (async/background) hooks from the synchronous Stop critical
path. The Stop hook in PRISM's harness blocks the session until every wired
Stop hook returns; a single slow hook (vitest gate, deep-test-sweep,
git-sync) can push wall-time past 30s. This engine offers two surfaces:

  1. `enqueue(job)` — append a job descriptor to `async-hook-queue.jsonl`
     and spawn a *detached* child process (`scripts/async-hook-runner.mjs`)
     that will pick the job up, execute the wrapped hook, and append the
     outcome to `async-hook-results.jsonl`. Returns in <2 ms; the actual
     hook can take minutes without blocking the parent.

  2. `runJob(jobId, env?, opts?)` — used *inside* the detached child. Reads
     the queue for the matching jobId, spawns the wrapped hook (synchronously
     this time — the ru
```


## Exports
`AsyncHookJob` (interface), `AsyncHookResult` (interface), `AsyncHookStats` (interface), `AsyncHookStatsSummary` (interface), `ResultsFilter` (interface), `EnqueueResult` (interface), `AsyncHookDispatcherOptions` (interface), `AsyncHookDispatcherEngine` (class), `makeJobId` (function), `hookBasename` (function), `percentile` (function), `clampWindow` (function), `clampLimit` (function), `getAsyncHookDispatcherEngine` (function), `asyncHookDispatcherEngine` (const), `ASYNC_HOOK_LIMITS` (const)

## Key methods
`enqueue`, `runJob`, `resolve`, `setTimeout`, `clearTimeout`, `getPendingJobs`, `getResults`, `getStats`, `isAvailable`, `purgeOlderThan`, `clearCache`, `setCache`, `setMtime`, `setSize`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/AsyncHookDispatcherEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find AsyncHookDispatcherEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/AsyncHookDispatcherEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
