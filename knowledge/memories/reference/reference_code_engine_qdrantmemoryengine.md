---
name: reference_code_engine_qdrantmemoryengine
description: "Phase 0.19 U-LLM4"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/QdrantMemoryEngine.ts
  sourceHash: 34a7b845
  loc: 373
aliases: QdrantMemoryEngine
---

# QdrantMemoryEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/QdrantMemoryEngine.ts` (373 LOC)

## What it is
Phase 0.19 U-LLM4

```
QdrantMemoryEngine — Phase 0.19 U-LLM4

Semantic memory layer on top of the thin `QdrantVectorStoreEngine`.
Gives higher-level engines a typed `remember / recall / forget` surface
keyed by **memory kind** (programs, outcomes, tribal tips, formulas,
playbook rules, freeform notes). Qdrant collections are created on
demand using a fixed naming convention (`prism_memory_<kind>`) so the
home + work boxes share the same layout and the H:-drive memory sync
(U-LLM8) has a deterministic target to mirror.

Embedding is pluggable. Callers pass in an `embed(text)` function when
they construct the engine — this decouples the memory layer from
whichever local model (nomic, MiniLM, Ollama's nomic-embed-text) is
in use. U-LLM9 will supply the default embedder; until then callers
must inject one.

Design rules:
  1. Never silently succeed — if the vector store isn't connected or
     embeddings fail, re
```


## Exports
`MEMORY_KINDS` (const), `MemoryKind` (type), `Embedder` (interface), `RememberInput` (interface), `RecallInput` (interface), `MemoryItem` (interface), `QdrantMemoryDeps` (interface), `MemoryResult` (type), `QdrantMemoryEngine` (class), `qdrantMemoryEngine` (const)

## Key methods
`setEmbedder`, `collectionFor`, `readCollectionFor`, `remember`, `recall`, `forgetAll`, `count`, `listKinds`

## Depends on
[[reference_code_engine_qdrantvectorstoreengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/QdrantMemoryEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find QdrantMemoryEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/QdrantMemoryEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
