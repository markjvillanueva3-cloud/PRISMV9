---
name: reference_code_engine_featureregistryengine
description: "U-LEARN-02"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/FeatureRegistryEngine.ts
  sourceHash: 27bb82b2
  loc: 328
aliases: FeatureRegistryEngine
---

# FeatureRegistryEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/FeatureRegistryEngine.ts` (328 LOC)

## What it is
U-LEARN-02

```
FeatureRegistryEngine — U-LEARN-02
===================================

Authoritative registry of feature-group contracts. Every write to
FeatureStoreEngine is validated against the contract registered here;
TrainingDatasetSnapshotEngine uses it to discover frozen versions;
StreamVsBatchReconciliationEngine uses it to know which keys to compare.

Storage: one JSON file per feature group at
  mcp-server/data/contracts/<domain>/<feature_group>.json
Atomic writes via tmp+rename so concurrent registrations don't corrupt.

Invariants:
  1. A contract's shape is immutable once `sealed: true`. Further
     register() calls with changes return `{ ok: false }` with the
     sealed-violation reason.
  2. Version numbers monotonically increase (`v1`, `v2`, `v3`...).
     register() refuses a version that's already on disk with different
     content.
  3. Duplicate feature names in `features[]` are
```


## Exports
`RegisterResult` (interface), `GetResult` (interface), `FeatureRegistryEngine` (class), `featureRegistryEngine` (const)

## Key methods
`register`, `list`, `seal`, `stats`, `getSelfAwareness`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/FeatureRegistryEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find FeatureRegistryEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/FeatureRegistryEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
