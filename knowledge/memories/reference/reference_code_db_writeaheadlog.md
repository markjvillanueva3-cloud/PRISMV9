---
name: reference_code_db_writeaheadlog
description: "WriteAheadLog — Crash-Safe Durability for PersistenceBridge (INTEG-MS2)"
metadata:
  type: reference
  kind: code-db
  galaxy: unassigned
  sourcePath: mcp-server/src/db/WriteAheadLog.ts
  sourceHash: bd4b7205
  loc: 450
aliases: writeAheadLog
---

# writeAheadLog -- DB Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/db/WriteAheadLog.ts` (450 LOC)

## What it is
```
WriteAheadLog — Crash-Safe Durability for PersistenceBridge (INTEG-MS2)
========================================================================

Implements a Write-Ahead Log (WAL) to ensure no queued writes are lost on crash.
All writes are appended to the WAL before being sent to PostgreSQL, and
checkpointed after successful persistence.

Pattern:
  1. persist() → WAL append (sync) → return sequence number
  2. flushAll() → PostgreSQL write → WAL checkpoint
  3. loadAll() → replay uncommitted WAL entries

WAL File Format (JSON Lines):
  {"seq":1,"ts":"...","op":"upsert","entity":"...","key":"...","hash":"...","data":{...}}
  {"seq":2,"ts":"...","op":"delete","entity":"...","key":"..."}
  {"seq":1,"ts":"...","op":"checkpoint"}  // marks seq 1 as committed

@version 1.0.0 — INTEG-MS2
```


## Exports
`WALOperationType` (type), `WALEntry` (interface), `WALConfig` (interface), `WALHealth` (interface), `writeAheadLog` (const), `WriteAheadLogImpl` (re-export)

## Key methods
`init`, `append`, `checkpoint`, `checkpointBatch`, `compact`, `getHealth`, `getUncommitted`, `hasUncommitted`, `sync`, `close`, `reset`, `clearInterval`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/db/WriteAheadLog.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** DB Module

## How to query it
Recall by meaning via `/system-viz find writeAheadLog` or `prism_session:master_index_query`; open the source at `mcp-server/src/db/WriteAheadLog.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
