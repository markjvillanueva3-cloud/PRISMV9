---
name: reference_code_engine_machinelorabaseengine
description: "MachineLoRABaseEngine — shared foundation for per-machine LoRA pipelines"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MachineLoRABaseEngine.ts
  sourceHash: 589abfd4
  loc: 619
aliases: BaseLoRADatasetBuilder
---

# BaseLoRADatasetBuilder -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MachineLoRABaseEngine.ts` (619 LOC)

## What it is
```
MachineLoRABaseEngine — shared foundation for per-machine LoRA pipelines
========================================================================

CAM-ML-CLOSEDLOOP-MS0 foundation. Extracted from the production Lathe
LoRA pattern (49 engines) so that the 8 machine-type pipelines
(milling, 5-axis, mill-turn, WEDM, sinker EDM, laser, waterjet,
grinding) can share machine-agnostic logic without duplicating the
~300 LOC of cadence / drift / version bookkeeping.

Provides TWO reusable classes:
  1. {@link BaseLoRADatasetBuilder} — geometry-hashed dataset assembly,
     stratified train/val/test split, Alpaca-format export.
  2. {@link BaseLoRACadence} — cadence scheduler with drift triggers,
     version management, auto-promotion, and retention pruning.

Each per-machine engine (e.g. MillingLoRACadenceEngine) is a thin
TypeScript wrapper around these classes with machine-specific defaults
an
```


## Exports
`RawJob` (interface), `LoRAExample` (interface), `DatasetSplitConfig` (interface), `DatasetBuildResult` (interface), `DEFAULT_SPLIT` (const), `geometryHash` (function), `DatasetBuilderOptions` (interface), `BaseLoRADatasetBuilder` (class), `CadenceInterval` (type), `TriggerType` (type), `RunStatus` (type), `CadenceConfig` (interface), `TrainingRun` (interface), `VersionInfo` (interface), `CadenceState` (interface), `DEFAULT_CADENCE` (const), `BaseLoRACadence` (class), `MachineLoRABaseInfo` (interface), `machineLoRABase` (const)

## Key methods
`render`, `build`, `setConfig`, `getConfig`, `getState`, `recordJobs`, `calculateNextRun`, `shouldTriggerRun`, `checkDrift`, `startRun`, `completeRun`, `failRun`, `promoteVersion`, `getInfo`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MachineLoRABaseEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find BaseLoRADatasetBuilder` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MachineLoRABaseEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
