---
name: reference_code_engine_hypercadslivebridgeengine
description: "CAD-DRAW-MAX-MS0/P0-U01"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HyperCADSLiveBridgeEngine.ts
  sourceHash: 355ff61a
  loc: 359
aliases: HyperCADSLiveBridgeEngine
---

# HyperCADSLiveBridgeEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HyperCADSLiveBridgeEngine.ts` (359 LOC)

## What it is
CAD-DRAW-MAX-MS0/P0-U01

```
HyperCADSLiveBridgeEngine — CAD-DRAW-MAX-MS0/P0-U01

Per-operation live-mutate facade over the hyperCAD-S AC Python bridge.
Mirrors `fusion360_live_*` (16 actions) so the autonomous CAD pipeline
can iteratively try → observe → adjust on a real hyperCAD-S seat the
same way it does on Fusion 360 today.

**Design.** Each live op is one CADOperation that the existing
{@link HyperCADSCodeGeneratorEngine} renders into AC Python, then
`executeScript()` ships through the AC Python bridge (or the mock
layer in CI). The bridge keeps a per-session op log so `undo()` can
roll back the last op without re-running the entire script.

**Session state.** Each `newDoc()` creates a session entry keyed by
project name. All subsequent ops append to that session's `opLog`
until a new doc is opened or `clearSession()` is called.

**Why a facade.** The codegen + executor already exist (E1160 + E1164
mock + E116
```


## Exports
`LiveBridgeContext` (interface), `LiveOpResult` (interface), `LiveSession` (interface), `SessionListing` (interface), `LiveBridgeStats` (interface), `HyperCADSLiveBridgeEngine` (class), `hyperCADSLiveBridgeEngine` (const)

## Key methods
`newDoc`, `listSessions`, `clearSession`, `createSketch`, `extrude`, `fillet`, `chamfer`, `revolve`, `hole`, `pattern`, `combine`, `shell`, `exportFile`, `getGeometry`, `undo`, `regenerate`, `executeRaw`, `getStats`

## Depends on
[[reference_code_engine_hypercadscodegeneratorengine]] | `ICADCodeGenerator`

## Where it lives
- **Source:** `mcp-server/src/engines/HyperCADSLiveBridgeEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HyperCADSLiveBridgeEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HyperCADSLiveBridgeEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
