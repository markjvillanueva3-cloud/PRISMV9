---
name: reference_xray_calibration_accumulation_and_app_plan_2026_06_23
description: xray closed-loop calibration now ACCUMULATES across runs + harvests REAL program-GT samples; + the blueprint->app integration plan. Two backend units + a plan (2026-06-23).
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.270Z
aliases: reference_xray_calibration_accumulation_and_app_plan_2026_06_23
---


**xray /checkin-xray session 2026-06-23 (slot xray, cad-fusion-live-ms0): two closed-loop calibration units + the app-integration plan.**

The closed-loop OCR calibration was permanently `reliable:false` -- `blueprint-ocr-training-loop.mjs` PHASE-1 calibrated P(correct | agreement fraction f) from ~24 SYNTHETIC prints per run then THREW the samples away, and `MIN_RELIABLE_SAMPLES` is 50. Two units fixed it:

- **U-XRAY-CALIB-ACCUMULATE** (`5ab3c49002`): new domain-neutral durable store `scripts/lib/calibration-sample-store.mjs` (pure parse/merge/serialize/stamp + fail-soft fs load/append/reset; ring-buffered cap 5000; tolerant JSONL so a torn line never crashes the loop). The runner now LOADS the store, calibrates on the UNION (persisted + fresh), APPENDS this run's fresh samples -- so the corpus crosses MIN_RELIABLE over a few nightly LAUNCHES (launch-count-driven, R12) with no new GPU/model/data. Default ON at a FIXED repo path (`state/shared/ocr-training-loop/calibration-samples.jsonl`, stable across --out-dir); `--no-calibration-store` reverts byte-identical; `--reset-calibration-store` (NOT --fresh -- the print cursor + calibration corpus are orthogonal). 17/17 tests incl. the proof that accumulation flips reliable:false(24)->true(72). GPU-free live-validated (seeded 60-sample store -> report reliable:true).

- **U-XRAY-PROGRAM-GT-CALIB** (`815567da84`): new pure `programGtAgreementSamples` (`cnc-program-gt-lib.mjs`) -- the program-GT analog of the synthetic perDimCorrectness (f = corroboration/n_models; correct = dimMatchesProgram vs the callout-class machined diameters). Wired into `validate-perfect-parts.mjs` behind opt-in `--emit-calibration` (default OFF -> byte-identical; emits source:"program-gt" into the SAME store). Piggybacks on the OCR the validation already runs -- no extra GPU, no new GT source (only gtReliable-gated scored parts, plain branch only). **DIAMETER-CLASS SCOPING** (both scrutiny arms flagged): the program GT carries only diameters, so a correctly-read LINEAR/chamfer dim would be a false correct=false -- emission adjudicates ONLY diameter-class dims (known non-diameter EXCLUDED; unknown-type kept, value-only fallback). 11/11 tests; 29/29 + 25/25 no-regression. Both per-file 2-arm scrutiny PASS.

**Harness:** `PRISM OCR Training Loop` scheduled task healthy (Ready, last run 2026-06-23 08:36 result 0, ~7142/7794 cursored ~92% drained). Did NOT kick a GPU run -- fleet contention (india/quebec/oscar/sierra/zulu loops live; vision cold-load starves under contention).

**GT-source reality (R12, per [[reference_xray_perfect_parts_gt_source_2026_06_22]]):** the closed-loop corpus is GT-limited to ~39 posted-program parts; the CAD-model GT leg stays DROPPED (only ~6 readable STEP). These units extract MORE signal (calibration samples) from the SAME trusted parts, NOT new GT sources.

**App-integration plan** delivered: `knowledge/wiki/architecture/blueprint-vision-app-integration-plan-2026-06-23.md`. Key finding from a parallel-agent consumer survey: the blueprint pipeline is wired end-to-end in the BACKEND but the app stops at "file uploaded" -- `upload.ts` sets `ready_for_ocr:true` and nothing consumes it; `BlueprintQuotePage.tsx` is a manual form next to a fully-built `blueprint_to_quote`. 5 phases: (1) upload->extract->display (the keystone, 100% backend-ready), (2) blueprint->quote autopop, (3) **auto-redaction app surface** (the explicit operator ask -- redaction exists training-side in `xray-trainset-to-lora.mjs` but NOT app-side; reuse the P1.5 region-classifier title-block segmentation to find what to mask), (4) document-reading exposure, (5) print->program. Frontend is Vite/React `mcp-server/web/src/pages/` (NOT Next.js -- quebec note was stale). Cross-galaxy: xray owns the extraction CONTRACT, quebec owns React, charlie owns quote math, kilo owns toolpath.

**U-APP-REDACT-LIB** (`47a37b714d`, the "continue" follow-on): the operator's "auto redaction" ask (Phase 3 of the plan). R8 finding -- customer anonymization ALREADY existed but was locked in the CRITICAL `BlueprintLoRABridgeEngine` (LoRA-only) + covered ~9 names while `jm-die-profile.ts` has the canonical 118-customer `JM_DIE_CUSTOMERS`. EXTRACTED build-once to shared TS `mcp-server/src/engines/blueprint-vision/blueprintRedaction.ts` (LoRA engine re-exports ANONYMIZATION_PATTERNS/applyAnonymizationPatterns, behavior byte-identical, 22/22 engine tests pass). App surface: `redactText`/`redactExtraction` (deep-copy; 31 customer-identity FIELDS masked wholesale = the safe path)/`redactionRegions` (title_block image bbox). **DISTINCTIVE TIER** (R7): default free-text scrubs distinctive customers (SEMBLEX/TOPURA, multi-word, or single-token not-common & >=4ch; CORE SFS/ITW always) but NOT common words (ACME thread/ELECTRODE/FORM/AIR/ATF/MIDWEST/short acronyms); `aggressive:true` = full 118. Audit cleartext-free by default. 25 tests; 2-arm scrutiny caught + fixed a real default-mode LEAK (P1) + cleartext audit (P2) + over-redaction (P2); re-review PASS. **[SCOPED] follow-up U-APP-REDACT-WIRE:** the redactor is the tested xray CONTRACT but NOT yet reachable -- the `blueprint_redact` dispatcher action + the quebec "export anonymized" render are the consuming unit (R12: do NOT claim the app feature ships until that lands).

NEXT (gated): per-type calibration (P2.9) unblocks once the accumulated corpus grows past MIN_RELIABLE; a GPU `validate-perfect-parts --emit-calibration` run (quiet-fleet window) seeds the real-GT share; U-APP-REDACT-WIRE (dispatcher action + quebec render) makes the redactor reachable; the Phase-1 app units land in quebec/charlie queues with xray supplying the contract. Pairs with [[blueprint-reading-improvement-backlog-2026-06-19]].
