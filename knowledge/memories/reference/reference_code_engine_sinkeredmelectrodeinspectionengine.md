---
name: reference_code_engine_sinkeredmelectrodeinspectionengine
description: "SinkerEDMElectrodeInspectionEngine — sinker-EDM electrode/cavity inspection"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SinkerEDMElectrodeInspectionEngine.ts
  sourceHash: d77f86a4
  loc: 317
aliases: sinkerEDMElectrodeInspectionEngine
---

# sinkerEDMElectrodeInspectionEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SinkerEDMElectrodeInspectionEngine.ts` (317 LOC)

## What it is
```
SinkerEDMElectrodeInspectionEngine — sinker-EDM electrode/cavity inspection
with spark-gap back-calculation.

Roadmap unit muS-D58..D59 (ARC-MS10 — "Electrode inspection protocol").

In die-sinking EDM the male electrode burns a cavity larger than itself by
one spark gap (overcut) on each side, so for any feature:

    measuredCavity = electrodeSize + 2 · sparkGap

The inspection protocol measures the finished cavity, back-calculates the
ACTUAL spark gap, compares it against the gap EXPECTED from the burn
settings (supplied from the process sheet), and flags pass / fail with a
ranked root-cause diagnosis whenever the gap is off-target. It also reports
cross-feature gap consistency — an even electrode burns an even gap.

This engine is pure geometry + inspection logic; it does not model the
discharge physics that *predict* a spark gap (that is process planning,
a separate concern — the ex
```


## Exports
`SinkerEDMElectrodeInspectionInputSchema` (const), `SinkerEDMElectrodeInspectionInput` (type), `InspectionStatus` (type), `InspectionVerdict` (type), `InspectionFinding` (interface), `FeatureInspectionResult` (interface), `SinkerEDMElectrodeInspectionResult` (interface), `sinkerEDMElectrodeInspectionEngine` (const)

## Key methods
`inspect`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/SinkerEDMElectrodeInspectionEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find sinkerEDMElectrodeInspectionEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SinkerEDMElectrodeInspectionEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
