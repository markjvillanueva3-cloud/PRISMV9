---
name: reference_code_data_sfcbatchunits
description: "SFC batch WORK-UNIT addressing layer (U-FT-03, SFC-FULLTUNE-BUILDOUT)."
metadata:
  type: reference
  kind: code-data
  galaxy: unassigned
  sourcePath: mcp-server/src/data/sfc-batch-units.ts
  sourceHash: d238ebac
  loc: 170
aliases: sfc-batch-units.ts
---

# sfc-batch-units.ts -- Data Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/data/sfc-batch-units.ts` (170 LOC)

## What it is
```
SFC batch WORK-UNIT addressing layer (U-FT-03, SFC-FULLTUNE-BUILDOUT).

The semantic partition the batch coordinator (U-FT-05) fans out to a worker
pool (U-FT-04) and the reducer (U-FT-06) folds by regime. Where the ENUMERATOR
(`sfc-combinatorial-enumerator.ts`) is an arbitrary flat-index bijection over
the 20,321,280-cell space, this module slices that space into the 1,152
REGIME-ALIGNED atoms the plan's downstream design assumes: one
(validity-cell x ISO-band) pair per unit.

WHY this falls out for free: the enumerator's mixed-radix ladder puts
VALID_CELLS (most significant) then ISO_BANDS (next) as the top two digits, so

  flatIndex = validCellIdx * (ISO * dia * flute * power * hardness * coolant)
            + isoIdx       * (dia * flute * power * hardness * coolant)
            + subIndex                              (subIndex in [0, 17640))
            = unitId * CELLS_PER_UNIT + 
```


## Exports
`ISO_COUNT` (const), `UNIT_COUNT` (const), `CELLS_PER_UNIT` (const), `BatchUnit` (interface), `unitIdFor` (function), `unitRange` (function), `unitIdOfIndex` (function), `describeUnit` (function), `enumerateUnits` (function), `cellsForUnit` (function), `representativeCell` (function)

## Key methods
`assertUnitId`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/data/sfc-batch-units.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Data Module

## How to query it
Recall by meaning via `/system-viz find sfc-batch-units.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/data/sfc-batch-units.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
