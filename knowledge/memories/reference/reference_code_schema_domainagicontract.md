---
name: reference_code_schema_domainagicontract
description: "Domain AGI Contract — INFRA-AGI-ROUTER-MS2/P0-U01"
metadata:
  type: reference
  kind: code-schema
  galaxy: unassigned
  sourcePath: mcp-server/src/schemas/domainAGIContract.ts
  sourceHash: 26d05bc3
  loc: 405
aliases: domainAGIContract.ts
---

# domainAGIContract.ts -- Schema SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/schemas/domainAGIContract.ts` (405 LOC)

## What it is
```
Domain AGI Contract — INFRA-AGI-ROUTER-MS2/P0-U01
==================================================

Unified `orchestrate(intent)` contract every domain AGI must implement.
Replaces the opaque-per-domain `Record<string, unknown>` bodies in
`CrossProcessAIBridge.AIOrchestrateRequest` with a structured intent/result
pair the router (`ProcessIntelligenceRouterEngine` — U05) dispatches
uniformly across mill/lathe/wedm without per-domain branching.

Consumers (U02-U04 will adapt to this contract):
  - MillingAGIMasterEngine.orchestrate(intent: DomainAGIIntent): DomainAGIResult
  - LatheAGIKnowledgeUnificationEngine.orchestrate(...)
  - WireEDMAGIOrchestrator.orchestrate(...)

Router (U05):
  - ProcessIntelligenceRouterEngine classifies `intent.domain` and dispatches.
  - Pipeline-level `confidence` is rolled up from per-decision confidences
    by the router (rollup algorithm — joint-probabi
```


## Exports
`DOMAIN_AGI_CONTRACT_VERSION` (const), `DomainKind` (const), `DomainKindT` (type), `MillAction` (const), `LatheAction` (const), `WedmAction` (const), `MillActionT` (type), `LatheActionT` (type), `WedmActionT` (type), `DomainAction` (const), `DomainActionT` (type), `BlueprintRefSchema` (const), `FeatureRefSchema` (const), `MachineRefSchema` (const), `ConstraintsSchema` (const), `DecisionKind` (const), `DecisionKindT` (type), `DecisionSchema` (const), `SimResultSchema` (const), `DomainAGIIntentSchema` (const)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/schemas/domainAGIContract.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Schema

## How to query it
Recall by meaning via `/system-viz find domainAGIContract.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/schemas/domainAGIContract.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
