---
name: reference_code_script_hdriveclonereconciler
description: "h-drive-clone-reconciler.mjs -- reconcile the H: drive's ~84 prism-* directories"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/h-drive-clone-reconciler.mjs
  sourceHash: 4c8cd93b
  loc: 263
aliases: h-drive-clone-reconciler.mjs
---

# h-drive-clone-reconciler.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/h-drive-clone-reconciler.mjs` (263 LOC)

## What it is
```
h-drive-clone-reconciler.mjs -- reconcile the H: drive's ~84 prism-* directories
against git's registered worktrees, so the 2nd brain knows which clones are LIVE
slot worktrees vs stale milestone worktrees vs orphan stray clones -- and which
are cleanup candidates (H-DRIVE-VAULT-SYNERGY/U-5, slot:papa).

U-1's coverage map deduped clones into a single count. This ENRICHES that: for
each prism-* dir it resolves git metadata (branch, last commit, age) and a kind:
  live-slot          -- registered worktree on a slot/<nato> branch (NEVER touch)
  registered-locked  -- a registered worktree marked locked (NEVER touch)
  registered-worktree-- a registered worktree on a work/* milestone branch
  orphan-clone       -- a prism-* dir NOT registered as a worktree (stray full clone)

Cleanup candidates = orphan-clones only (operator-review, never auto-deleted; an
orphan may hold unpushed work). Emi
```


## Exports
`parseWorktreeList` (function), `classifyClone` (function), `daysSince` (function), `buildCloneNote` (function), `buildCloneMap` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/h-drive-clone-reconciler.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find h-drive-clone-reconciler.mjs` or `prism_session:master_index_query`; open the source at `scripts/h-drive-clone-reconciler.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
