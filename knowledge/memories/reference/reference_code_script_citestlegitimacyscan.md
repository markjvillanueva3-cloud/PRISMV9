---
name: reference_code_script_citestlegitimacyscan
description: "ci-test-legitimacy-scan.mjs -- CI gate that fails the build when a CHANGED"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/ci-test-legitimacy-scan.mjs
  sourceHash: 99bb8286
  loc: 116
aliases: ci-test-legitimacy-scan.mjs
---

# ci-test-legitimacy-scan.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/ci-test-legitimacy-scan.mjs` (116 LOC)

## What it is
```
ci-test-legitimacy-scan.mjs -- CI gate that fails the build when a CHANGED
test file is a fake/placeholder/synthetic/mocked-critical-domain test.

WHY: the `test-legitimacy` PreToolUse hook only fires inside an interactive
Claude session. CI is the only gate that env flags (PRISM_ALLOW_*) cannot
bypass, so it is the durable backstop for fake tests reaching main.

SCOPE: only files CHANGED in this push/PR (vs the base ref) are gated, so the
~62 pre-existing legacy violators do not fail unrelated PRs. The full-corpus
count is printed as advisory (non-failing) so the legacy debt stays visible.

USAGE:
  node scripts/ci-test-legitimacy-scan.mjs [--base <ref>] [--all]
    --base <ref>  compare against <ref> (default: origin/main, else HEAD~1)
    --all         scan the entire corpus and FAIL on any violation (strict)

EXIT: 0 = clean (or only legacy advisory), 1 = a changed test file is fake.
```


## Exports
`scanFiles` (function)

## Key methods
`main`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/ci-test-legitimacy-scan.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find ci-test-legitimacy-scan.mjs` or `prism_session:master_index_query`; open the source at `scripts/ci-test-legitimacy-scan.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
