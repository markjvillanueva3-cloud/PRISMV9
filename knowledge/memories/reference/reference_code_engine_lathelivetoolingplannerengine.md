---
name: reference_code_engine_lathelivetoolingplannerengine
description: "feature→toolpath+G-code planner for driven tooling"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LatheLiveToolingPlannerEngine.ts
  sourceHash: 569f0067
  loc: 962
aliases: LatheLiveToolingPlannerEngine
---

# LatheLiveToolingPlannerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LatheLiveToolingPlannerEngine.ts` (962 LOC)

## What it is
feature→toolpath+G-code planner for driven tooling

```
LatheLiveToolingPlannerEngine — feature→toolpath+G-code planner for driven tooling

Re-modularized from PRISM v8.89.002 monolith
`extracted/engines/tools/PRISM_ENHANCED_LATHE_LIVE_TOOLING_ENGINE.js` (635 LOC).

Distinct from the existing live-tooling engines, which it complements rather
than duplicates:
  - LiveToolingEngine            → feasibility / power / torque (no toolpath)
  - LiveToolingSyntaxEngine      → multi-controller G-code formatting of
                                   pre-computed move arrays (no feature→path)
  - LiveToolingIntelligenceEngine→ specific-power / material intelligence
This engine owns the missing layer: given a *feature* (cross-hole, C-axis
pocket/hex/keyway, Y-axis pocket) it computes the toolpath operation list,
emits self-contained Fanuc-style G-code, and estimates cycle time.

Fail-loud (CLAUDE.md R12): the source monolith *referenced* seven feature
h
```


## Exports
`LiveToolingPlanOperation` (type), `CAxisFeatureType` (type), `YAxisFeatureType` (type), `PlannerToolSpec` (interface), `PlannerStockSpec` (interface), `PlannerMachineConfig` (interface), `CrossDrillHole` (interface), `PlannerFeature` (interface), `LiveToolingPlanInput` (interface), `PlannedMotion` (interface), `LiveToolingPlanResult` (interface), `LatheLiveToolingPlannerEngine` (class), `latheLiveToolingPlannerEngine` (const)

## Key methods
`plan`, `calculate`, `getConfidenceLevel`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/LatheLiveToolingPlannerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find LatheLiveToolingPlannerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LatheLiveToolingPlannerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
