---
name: reference_code_engine_cadregressiondashboardengine
description: "Read-only aggregation layer over the per-batch TestBatch JSON documents"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADRegressionDashboardEngine.ts
  sourceHash: 7a419727
  loc: 494
aliases: CADRegressionDashboardEngine
---

# CADRegressionDashboardEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADRegressionDashboardEngine.ts` (494 LOC)

## What it is
Read-only aggregation layer over the per-batch TestBatch JSON documents

```
CADRegressionDashboardEngine — U-CINF08 (CAD-INFRA-MS0)

Read-only aggregation layer over the per-batch TestBatch JSON documents
written by CADTestCheckpointEngine. Produces dashboard-facing snapshots
that the CINF09 web UI can render without owning any state itself.

Pure function layer — no writes. Given a batchId, this engine:
  1. Loads `{stateDir}/{batchId}.json` via injectable FS
  2. Computes top-line counters (pass/fail/error/skip/pending/running)
  3. Bins errors by ErrorType (format|parse|generation|comparison|timeout|crash)
  4. Estimates ETA from average terminal duration × remaining count
  5. Measures throughput over the last N minutes of completedAt timestamps
  6. Returns the most recent K failures sorted by completedAt desc

Duplication check: existing dashboard/monitor engines are domain-specific
(InstructorDashboardEngine, HyperMillJobMonitor, etc.). None of them read

```


## Exports
`DEFAULT_STATE_DIR` (const), `DEFAULT_THROUGHPUT_WINDOW_MIN` (const), `DEFAULT_RECENT_FAILURES` (const), `DashboardFS` (interface), `BatchLifecycleStatus` (type), `DashboardCounts` (interface), `ErrorBreakdown` (interface), `ThroughputEstimate` (interface), `RecentFailure` (interface), `DashboardSnapshot` (interface), `BatchSummary` (interface), `isTerminal` (function), `pctComplete` (function), `tallyCounts` (function), `aggregateErrors` (function), `lifecycle` (function), `throughputEstimate` (function), `recentFailures` (function), `CADRegressionDashboardEngine` (class), `cadRegressionDashboardEngine` (const)

## Key methods
`existsSync`, `readFileSync`, `readdirSync`, `getCapabilities`, `validate`, `snapshot`, `listBatches`

## Depends on
[[reference_code_engine_baseengine]] | [[reference_code_engine_iengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADRegressionDashboardEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADRegressionDashboardEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADRegressionDashboardEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
