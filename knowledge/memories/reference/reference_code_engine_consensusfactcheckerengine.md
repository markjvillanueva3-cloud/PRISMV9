---
name: reference_code_engine_consensusfactcheckerengine
description: "auto-validate consensus model answers against"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ConsensusFactCheckerEngine.ts
  sourceHash: 017c48c5
  loc: 229
aliases: ConsensusFactCheckerEngine
---

# ConsensusFactCheckerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ConsensusFactCheckerEngine.ts` (229 LOC)

## What it is
auto-validate consensus model answers against

```
ConsensusFactCheckerEngine — auto-validate consensus model answers against
the live PRISM source-of-truth artifacts.

Milestone: INTEL-OLLAMA-OBSIDIAN-MS0 / LAYER-3-FACT-CHECK.

External models (Codex, Grok, Ollama) lack PRISM context by default. Even
with PRISMContextInjectorEngine prepending CLAUDE.md + ENGINE_DIGEST, models
still hallucinate engine names ("ToolPathOptimizationEngine — wait, does
that exist?") and dispatcher actions ("call prism_calc:taylor_force"). This
engine catches those before a roadmap or refactor proposal builds on a
fictional foundation.

What it does:
  1. Tokenize each ModelResponse.answer for capitalized identifiers that
     look like engine names (`*Engine` suffix) or dispatcher actions
     (`prism_*:*` snake_case).
  2. Look each up in ENGINE_DIGEST + the live dispatcher action enum (a
     curated allowlist passed in at config time).
  3. Flag misses as
```


## Exports
`FactCheckResult` (interface), `HallucinationFlag` (interface), `VerifiedFlag` (interface), `ConsensusFactCheckerEngine` (class), `consensusFactCheckerEngine` (const)

## Key methods
`loadKnowledgeBase`, `getKnowledgeBase`, `check`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ConsensusFactCheckerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ConsensusFactCheckerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ConsensusFactCheckerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
