---
name: reference_code_script_fleetmemorymonitor
description: "fleet-memory-monitor.mjs — durable system-RAM + per-slot memory monitor."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/fleet-memory-monitor.mjs
  sourceHash: b1e4bfa0
  loc: 979
aliases: fleet-memory-monitor.mjs
---

# fleet-memory-monitor.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/fleet-memory-monitor.mjs` (979 LOC)

## What it is
```
fleet-memory-monitor.mjs — durable system-RAM + per-slot memory monitor.

Sits ALONGSIDE the fleet-reaper. The reaper reaps orphan processes of
CRASHED slots (after a 10-min confirm window) and runs at +210s phase off
the host. That covers post-crash cleanup, but leaves a real gap: when ALL
13 chats are LIVE and the box drifts toward commit-memory saturation, the
reaper has no candidates to kill, fleet-reaper-sweep returns "ok 0
candidates", and pressure climbs unnoticed until a chat dies hard.

This monitor closes that gap. Every 5 minutes (Windows Scheduled Task —
survives every chat closing) it:
  1. Samples system memory via Win32_OperatingSystem (Total+Free physical)
     and the commit budget via TotalVirtual/FreeVirtual.
  2. Enumerates running node/claude/bash/git/powershell processes with
     WorkingSetSize.
  3. Joins to chat-slots.json — each chat's harness PID anchors a proc
```


## Exports
`CHAT_ADVISORY_DEFAULT_THRESHOLD_MB` (const), `CHAT_ADVISORY_DEFAULT_COOLDOWN_SEC` (const), `LEDGER_SCHEMA_VERSION` (const), `DEFAULT_WARN_PCT` (const), `DEFAULT_CRIT_PCT` (const), `DEFAULT_ADVISORY_COOLDOWN_SEC` (const), `DEFAULT_SUSTAINED_TICKS` (const), `DEFAULT_PS_TIMEOUT_MS` (const), `samplePowerShell` (function), `readSlotsForAttribution` (function), `readPinsForAttribution` (function), `findAnchorAncestor` (function), `findPsAncestor` (function), `attributeProcesses` (function), `readChatAdvisoryLedger` (function), `appendChatAdvisorySweepRecord` (function), `evaluateChatTreeAdvisories` (function), `decideLevel` (function), `pickLargestSlot` (function), `decideAdvisory` (function)

## Key methods
`mkdirSync`, `appendFileSync`, `writeFileSync`, `renameSync`, `rotateIfLarge`, `appendTelemetry`, `writeLedger`, `appendChatBus`, `appendChatAdvisorySweepRecord`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/fleet-memory-monitor.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find fleet-memory-monitor.mjs` or `prism_session:master_index_query`; open the source at `scripts/fleet-memory-monitor.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
