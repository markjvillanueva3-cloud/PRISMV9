---
name: reference_code_engine_zulufleethealthsynthesisengine
description: "Zulu Fleet Health Synthesis"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ZuluFleetHealthSynthesisEngine.ts
  sourceHash: 796bb7a4
  loc: 353
aliases: ZuluFleetHealthSynthesisEngine
---

# ZuluFleetHealthSynthesisEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ZuluFleetHealthSynthesisEngine.ts` (353 LOC)

## What it is
Zulu Fleet Health Synthesis

```
ZuluFleetHealthSynthesisEngine -- C3 fleet-health synthesis (HZP/HZD family).

Pure-core: synthesizes three raw fleet signals -- chat-slot liveness
(heartbeat freshness), per-slot task-queue depth, and galaxy coverage --
into a single scored FleetHealthVector. This is the genuine gap the
watcher SCRIPTS (.claude/fleet-task-health.mjs, .claude/fleet-memory-monitor.mjs)
do NOT fill: those probe + alert, they do not produce a normalized,
comparable per-slot readiness score that downstream schedulers/auctioneers
can consume.

The readiness score is the load-bearing output. It is fed (as a follow-up,
NOT built here) to ZuluTaskAuctionEngine's `queue_penalty` bid factor and to
a wave scheduler so the fleet preferentially routes new work to slots that
are both ALIVE and under-loaded.

Design decisions (R8 / R7 -- read before write):
  - Liveness thresholds mirror chat-slots.mjs (STALE_TTL_MS=12
```


## Exports
`STALE_TTL_MS` (const), `CRASH_TTL_MS` (const), `W_FRESH` (const), `W_QUEUE` (const), `MAX_FLEET` (const), `SlotSignalSchema` (const), `SlotSignal` (type), `FleetHealthRequestSchema` (const), `FleetHealthRequest` (type), `SlotLiveness` (type), `PerSlotHealth` (interface), `FleetRollup` (interface), `FleetHealthVector` (interface), `SATURATION_FLOOR` (const), `ZuluFleetHealthSynthesisEngine` (class), `zuluFleetHealthSynthesisEngine` (const)

## Key methods
`synthesize`, `scoreSlot`, `classify`, `rollup`, `slotReadiness`, `auctionQueueDepths`, `renderVector`

## Depends on
[[reference_code_engine_zulutaskauctionengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/ZuluFleetHealthSynthesisEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ZuluFleetHealthSynthesisEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ZuluFleetHealthSynthesisEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
