---
name: reference_code_engine_pactcontracttestengine
description: "U-LPR-CONTRACT"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PactContractTestEngine.ts
  sourceHash: d5db632c
  loc: 595
aliases: PactContractTestEngine
---

# PactContractTestEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PactContractTestEngine.ts` (595 LOC)

## What it is
U-LPR-CONTRACT

```
PactContractTestEngine — U-LPR-CONTRACT

Consumer-driven contract testing for the LATHE-PROD-READY pipeline.

A Pact-style contract captures the exact shape + matchers that a CONSUMER
expects from a PROVIDER. Verification checks whether a real provider
output would have been accepted by the consumer at contract time. Breaking
changes (removed required fields, tightened types, widened enums consumer
hasn't seen) are flagged so the pilot can block release.

Scope is intentionally tight:
  - Pure calculation, no HTTP / no provider stubs / no broker
  - 7 matcher types: exact, type, regex, range, contains, enum, optional
  - Backward-compat diff between two contract versions
  - Deterministic output usable by audit log + four-eyes review

The consumer/provider idea maps 1:1 onto PRISM: a dispatcher action or
UI page is the consumer; an engine or orchestrator is the provider. The
contract pin
```


## Exports
`PactPrimitive` (type), `MatcherKind` (type), `ExactMatcher` (interface), `TypeMatcher` (interface), `RegexMatcher` (interface), `RangeMatcher` (interface), `ContainsMatcher` (interface), `EnumMatcher` (interface), `OptionalMatcher` (interface), `PactMatcher` (type), `MatcherMap` (type), `Interaction` (interface), `Contract` (interface), `MatchFailure` (interface), `VerificationResult` (interface), `BreakingChangeKind` (type), `BreakingChange` (interface), `CompatResult` (interface), `PactContractTestEngine` (class), `pactContractTestEngine` (const)

## Key methods
`defineContract`, `verifyInteraction`, `checkBackwardCompat`, `match`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PactContractTestEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PactContractTestEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PactContractTestEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
