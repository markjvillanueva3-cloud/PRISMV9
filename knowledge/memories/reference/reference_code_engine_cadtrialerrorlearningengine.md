---
name: reference_code_engine_cadtrialerrorlearningengine
description: "Learns from CAD generation regeneration-test failures (output of"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADTrialErrorLearningEngine.ts
  sourceHash: 90276cdc
  loc: 1212
aliases: CADTrialErrorLearningEngine
---

# CADTrialErrorLearningEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADTrialErrorLearningEngine.ts` (1212 LOC)

## What it is
Learns from CAD generation regeneration-test failures (output of

```
CADTrialErrorLearningEngine — U-CADC29

Learns from CAD generation regeneration-test failures (output of
CADRegenerationTestEngine). Extracts recurring failure patterns by
category (volume mismatch, bbox mismatch, topology, code error) and
provides risk scoring + adjustment recommendations for new generation
candidates.

Learning model:
  - Frequentist failure-rate per category (Laplace-smoothed):
      p(fail | category) = (failures + 1) / (failures + successes + 2)
  - Beta(α=1, β=1) prior, posterior Beta(1+k, 1+n-k) — see Gelman, BDA3 §2.4.
  - Recommendation strength scales with sample count via shrinkage:
      w = n / (n + κ),  κ=10 default — see Efron & Morris (1973), JASA 68.

Persistence: append-only JSONL ledger; in-memory caches rebuilt on load.
Pure-code: no external app launches, no network.
```


## Exports
`FailureCategory` (type), `RegenerationOutcome` (interface), `IngestResult` (interface), `FailurePattern` (interface), `RecommendationCandidate` (interface), `AdjustmentSuggestion` (interface), `TribalAdvice` (interface), `TribalTipProvider` (type), `CalibrationInfo` (interface), `Recommendation` (interface), `FailureStats` (interface), `LearningTrend` (interface), `RecommendationRecord` (interface), `LoopEfficacy` (interface), `resolveDefaultLedgerPath` (function), `CADTrialErrorLearningEngine` (class), `cadTrialErrorLearningEngine` (const)

## Key methods
`ingest`, `ingestBatch`, `extractPatterns`, `recommendAdjustments`, `getFailureStats`, `getLearningTrend`, `recordRecommendation`, `attributeOutcome`, `getLoopEfficacy`, `reset`, `setLedgerPath`, `loadFromDisk`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CADTrialErrorLearningEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADTrialErrorLearningEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADTrialErrorLearningEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
