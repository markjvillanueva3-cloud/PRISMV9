---
name: reference_code_script_psncoveragereport
description: "PSN Coverage Report — U-HAGI12 + U-HAGI08 live demo"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/psn-coverage-report.mjs
  sourceHash: b4f96ffd
  loc: 267
aliases: psn-coverage-report.mjs
---

# psn-coverage-report.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/psn-coverage-report.mjs` (267 LOC)

## What it is
```
PSN Coverage Report — U-HAGI12 + U-HAGI08 live demo

Scans PRISM inventory (engines/dispatchers/wiki/memory/hooks), maps
evidence onto PSN-leg × Voxyz-layer cells, runs PSNCoverageAuditEngine,
decorates the matrix with SourceChainEngine provenance, writes a
markdown + JSON dashboard.

Use: node H:/prism/scripts/psn-coverage-report.mjs
     [--out state/shared/dashboards/]
     [--covered-threshold 0.7] [--partial-threshold 0.25]

Output:
  state/shared/dashboards/PSN-COVERAGE-{YYYY-MM-DD}.{md,json}

Pure JS shim (no TS) so it can run with portable-node without build step.
The TS engines exist on disk but this script re-implements minimal audit
logic with the same algorithm so the live tool runs even before
mcp-server is rebuilt.
```


## Exports
`audit` (re-export), `digest` (re-export), `gather` (re-export), `renderMarkdown` (re-export), `PSN_LEGS` (re-export), `VOXYZ_LAYERS` (re-export)

## Key methods
`writeFileSync`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/psn-coverage-report.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find psn-coverage-report.mjs` or `prism_session:master_index_query`; open the source at `scripts/psn-coverage-report.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
