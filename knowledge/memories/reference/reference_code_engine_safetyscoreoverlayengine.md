---
name: reference_code_engine_safetyscoreoverlayengine
description: "Real-time S(x) Composite Safety Index Overlay (U-CAM95)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SafetyScoreOverlayEngine.ts
  sourceHash: 18380383
  loc: 394
aliases: SafetyScoreOverlayEngine
---

# SafetyScoreOverlayEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SafetyScoreOverlayEngine.ts` (394 LOC)

## What it is
Real-time S(x) Composite Safety Index Overlay (U-CAM95)

```
SafetyScoreOverlayEngine — Real-time S(x) Composite Safety Index Overlay (U-CAM95)
===================================================================================

PHASE-7 Intelligent Vericut: Real-time composite safety-score visualization
for the four CAM plugin adapters (hyperMILL, Fusion 360, Inventor HSM,
Mastercam X8). Consumes the `safety_score` sub-object of the PhysicsOverlay
emitted by PRISMVerificationPluginEngine.analyzePoint() — this is the
overall traffic-light output of the safety chain, with per-component
contributions from force, stability, deflection, thermal, and tool_life.

S(x) bands (hard traffic-light semantics):
  green     → S >= 0.85        — system nominal
  yellow    → 0.70 <= S < 0.85 — elevated risk, operator attention
  red       → S < 0.70         — hard-stop zone
  transition → magenta         — band flip detected this frame

Hard-stop mechanism:
  The
```


## Exports
`HexColorSchema` (const), `SafetyBandSchema` (const), `SafetyBand` (type), `SafetyScoreOverlayTargetSchema` (const), `SafetyScoreOverlayTarget` (type), `SafetyScoreOverlayFrameSchema` (const), `SafetyScoreOverlayFrame` (type), `SafetyScoreOverlayStatsSchema` (const), `SafetyScoreOverlayStats` (type), `GREEN_THRESHOLD` (const), `YELLOW_THRESHOLD` (const), `SafetyScoreOverlayEngine` (class), `safetyScoreOverlayEngine` (const)

## Key methods
`renderFrame`, `getStats`, `resetSession`, `supportedTargets`, `classify`

## Depends on
[[reference_code_engine_prismverificationpluginengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/SafetyScoreOverlayEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SafetyScoreOverlayEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SafetyScoreOverlayEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
