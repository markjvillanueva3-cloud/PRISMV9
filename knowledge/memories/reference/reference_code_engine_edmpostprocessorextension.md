---
name: reference_code_engine_edmpostprocessorextension
description: "EDMPostProcessorExtension — Post-Processor for EDM Processes"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/EDMPostProcessorExtension.ts
  sourceHash: c348da5c
  loc: 864
aliases: edmPostProcessorExtension
---

# edmPostProcessorExtension -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/EDMPostProcessorExtension.ts` (864 LOC)

## What it is
```
EDMPostProcessorExtension — Post-Processor for EDM Processes

Converts intermediate/generic EDM programs to controller-native G-code.
DISTINCT from EDMProgramAssemblerEngine (which generates complete programs
from part descriptions). This extension handles the POST-PROCESSING step:
taking existing EDM toolpath data and adapting it to specific controller
dialects with validation and optimized condition sequencing.

Wire EDM Post-Processing:
  - Condition code mapping (generic → controller-specific E/C codes)
  - Auto-threading sequence generation per controller
  - Skim pass parameter sequencing (rough → skim1 → skim2 → skim3)
  - Taper angle compensation (UV axis programming)
  - Wire offset / kerf compensation (G41/G42 or controller-specific)
  - Flushing pressure settings per controller

Sinker EDM Post-Processing:
  - Burn sequence generation (rough → semi → finish)
  - Orbiting patte
```


## Exports
`EDMPostController` (type), `EDMPostProcessType` (type), `WireCutPhase` (type), `SinkerBurnPhase` (type), `EDMOperation` (interface), `EDMPostInput` (interface), `EDMPostOutput` (interface), `edmPostProcessorExtension` (const)

## Key methods
`detectProcessType`, `parseOperations`, `postProcessWire`, `postProcessSinker`, `postProcess`, `listControllers`, `execute`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/EDMPostProcessorExtension.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find edmPostProcessorExtension` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/EDMPostProcessorExtension.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
