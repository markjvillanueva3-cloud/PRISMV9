---
name: reference_quoting_margin_floor_gate_gap_2026_06_09
description: "SHIPPED 2026-06-09 (U-QP-MARGIN-FLOOR-GATE): QuoteEstimatorEngine now gates BOTH quote paths (estimate + estimateCalibrated) — below-floor margin → WARN + pricing.below_margin_floor flag, never silent-emit. Floor = caller-supplied input.margin_floor_pct (canonical, from ShopConfigurationEngine) + DEFAULT_MARGIN_FLOOR_PCT=20 fallback. 4/4 R9 tests. Closed charlie's soul #1 refuse. Original gap: the live quoting surface had NO margin floor (only a DEAD minMargin 0.20 in the @deprecated QuotingEngine). REMAINING: dispatcher must pass margin_floor_pct from ShopConfig (callers get default today); holistic margins→ShopConfig refactor; surface below_margin_floor on frontend QuoteBuilder/QuoteAnalytics."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.141Z
aliases: reference_quoting_margin_floor_gate_gap_2026_06_09
---


**✅ SHIPPED 2026-06-09 (commit `U-QP-MARGIN-FLOOR-GATE`).** The gate is now BUILT in `QuoteEstimatorEngine` on BOTH quote-emitting paths (estimate() + estimateCalibrated): caller-overridable `input.margin_floor_pct` (canonical, from ShopConfigurationEngine) with documented `DEFAULT_MARGIN_FLOOR_PCT=20` fallback; below-floor → WARN to dfm_warnings + `pricing.below_margin_floor` flag (no silent-emit, no auto-clamp/reject). Result type gained `pricing.{below_margin_floor, margin_floor_pct}`. 5/5 R9 tests (`QuoteEstimatorEngine.marginFloor.test.ts`). The findings below are the original GAP audit (kept for provenance).

**Canonical sourcing DONE (commit `U-QP-MARGIN-FLOOR-SHOPCONFIG`):** the engine now self-sources the floor from `ShopConfigurationEngine.getActiveProfile().margin_floor_pct` (added optional field + JM Die profile=20 + validateProfile 0-100 range-check), mirroring the proven `overhead_pct` lazy-require (~line 371). Resolution: `input.margin_floor_pct` -> active shop profile -> `DEFAULT_MARGIN_FLOOR_PCT=20`. No dispatcher change needed (engine self-sources for ALL callers). KNOWN LIMIT (R12): a direct vitest spy-proof of the require() path is blocked by the CJS-require/ESM-import instance split (engine's require != test's import singleton) + profile-20==default-20; verified by pattern-equivalence to the working overhead_pct require, not an end-to-end spy.

REMAINING follow-ups: (1) holistic "all quoting margins (tier targets, volume discounts) → ShopConfigurationEngine" refactor (the floor is sourced now, but tier-margins {A30/B35/C40/new38} + volume tiers are still in-engine); (2) surface `below_margin_floor` on the frontend QuoteBuilder/QuoteAnalytics pages (the structured flag is set on the result; the UI doesn't render it yet).

---

**2026-06-09 (slot CHARLIE, synergy /goal — quoting-galaxy safety-invariant audit).**

**FINDING (verified by broad grep, NOT yet by full read — R12 caveat):** the LIVE quoting surface appears to have **no enforced margin-floor gate**. A broad grep for `marginFloor|margin_floor|MARGIN_FLOOR|minMargin|below.*floor|marginGate|margin.*reject|floor.*reject` across `src/engines/` + `businessDispatcher.ts` + `quotingDispatcher.ts` (excluding the deprecated engine + MEMORY.md docs) returned **empty**. This is charlie's soul **#1 refuse**: *"emitting-customer-quote-without-margin-floor-gate."* A quoting platform with no margin floor can emit money-losing quotes (aggressive volume + repeat discounts can drive `actualMargin` arbitrarily low with no guard).

**What I found tracing it:**
- `src/engines/QuotingEngine.ts` is **`@deprecated`** (U-CONSOL1, superseded by QuoteEstimatorEngine, "retained for backward-compat direct imports only"). Its `PRICING.minMargin = 0.20` is **DEAD** — declared but NEVER referenced in `generateQuote()` (no `actualMargin >= minMargin` check). Don't bother testing/fixing the deprecated engine — it's a dead-end (R13).
- Canonical `src/engines/QuoteEstimatorEngine.ts` (1136 lines, 2 test files: `QuoteEstimatorEngine.test.ts` + `.calibrated.test.ts`) — grep surfaced DfM warnings (tolerance/surface-finish/undercut/5-axis/tool-wear) but **no margin-floor enforcement**.

**WHY I did NOT build the gate this session (R6/R12 + soul self-consistency):** (1) YELLOW budget — a margin-floor gate in a 1136-line tested canonical engine is a careful change, not a rush. (2) The grep is not conclusive — must READ the QuoteEstimatorEngine pricing/margin path first; the gate could exist under other terminology, or margin-floor could be a deliberate human-review step / handled post-hoc by quote-vs-actual reconciliation. (3) Charlie's soul refuses BOTH `emitting-customer-quote-without-margin-floor-gate` AND `inline-shop-rate-or-margin-constants` — so the floor VALUE must come from a canonical source (shop config / pricing registry), never inlined; rushing a wrong-layer or inlined-constant gate would itself violate the soul.

**PRECISE INSERTION POINT (read this session, de-risks the build):** the gate belongs in `QuoteEstimatorEngine.estimate()` at the `return { … pricing: { unit_price, total_price, margin_pct: actualMargin, adjustments } … }` block (around lines 417-455; `actualMargin` is computed before the return — it's `pricing.margin_pct`). After volume + repeat + tier discounts stack, `actualMargin` can drop arbitrarily with no guard. **CRITICAL — there is NO canonical margin-FLOOR source today:** the engine has tier TARGET-margins (`const ... = { A: 30, B: 35, C: 40, new: 38 }` at ~line 827, percent) used as `1 - targetMargin/100`, but no FLOOR. So step 1 of the unit is *establishing* a canonical floor (e.g., `ShopConfigurationEngine` pricing block or a pricing registry — NOT inlined; soul refuse `inline-shop-rate-or-margin-constants`).

**THE UNIT (fresh-budget charlie):** (1) Add a canonical `marginFloorPct` to ShopConfigurationEngine (or pricing registry) — the deprecated dead value was 0.20/20%; confirm the right value with the operator or shop config, don't assume. (2) In `estimate()`, after `actualMargin`, compare to the canonical floor; if below, push a WARN to `dfm_warnings` (or a new `pricing.below_margin_floor: true` flag) — WARN + flag, NOT silent clamp and NOT hard reject (R12 fail-loud; a quoting tool should surface, not refuse). (3) Real R9 tests: a discount-stacked quote that drops below floor MUST flag; an above-floor quote MUST NOT; round-trip through the dispatcher. (4) Wire to every quote-emitting surface (R15). Layer decision settled: engine `estimate()` result is the right place (single chokepoint all quote actions route through), NOT a separate gate engine.

**DISTINCT from the existing charlie guards (R8-verified, not a duplicate):** [[reference_charlie_baseline_guard_2026_06_01]] + [[reference_charlie_guard_volume_synth_2026_06_01]] (`scripts/lib/quoting-baseline-guard.mjs`) guard the TRAINING/CALIBRATION DATA quality — degenerate baseline-records, machine-name-as-customer, synthetic-revenue-dominant. They do NOT touch the margin floor on an EMITTED customer quote. The margin-floor gate is a separate, output-side invariant on the quote-generation path (QuoteEstimatorEngine), not the train-cycle. The semantic recall surfaces both because they share "quoting"+"guard" — the next session must not assume the baseline guard already covers this.

Related: the charlie soul (`state/shared/slot-souls/charlie.md`), [[reference_charlie_slot_misidentified_golf_2026_06_09]], [[feedback_always_fill_gaps]] (this is owned+scoped, not log-and-dropped), [[feedback_wire_test_validate_all_galaxies]].
