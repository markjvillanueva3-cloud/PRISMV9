---
name: reference_code_engine_spindletorquegateengine
description: "SpindleTorqueGateEngine — U-LSR05 (LATHE-HARDENED-MS0)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SpindleTorqueGateEngine.ts
  sourceHash: bb87eaef
  loc: 394
aliases: SpindleTorqueBlockError
---

# SpindleTorqueBlockError -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SpindleTorqueGateEngine.ts` (394 LOC)

## What it is
```
SpindleTorqueGateEngine — U-LSR05 (LATHE-HARDENED-MS0)

Per-operation spindle torque adequacy check against the machine's torque
curve. Produces a typed gate decision that the emitter (U-LSR04 pattern)
and the formal predicate (U-LSR22) can consume as a hard-block.

── Physics ─────────────────────────────────────────────────────────────
Cutting power:              P_cut_kW = (Fc × Vc) / 60_000
  Fc in N, Vc in m/min. Derivation: P = F·v with v in m/s = Vc/60.

Required spindle torque:    T_req_Nm = 9549 × P_cut_kW / rpm
  Constant 9549 ≈ 60_000 / (2π) converts kW and rpm to Nm.

Machine torque curve (standard two-region lathe spindle):
  rpm ≤ base_rpm  →  T_avail = T_max                   (constant torque)
  rpm >  base_rpm →  T_avail = T_max × base_rpm / rpm  (constant power
                                                        above base)
  Equivalently the upper region is T_avail 
```


## Exports
`MachineSpindleSpecSchema` (const), `MachineSpindleSpec` (type), `SpindleTorqueGateInputSchema` (const), `SpindleTorqueGateInput` (type), `SpindleTorqueOpSchema` (const), `SpindleTorqueOp` (type), `SpindleTorqueGateResultSchema` (const), `SpindleTorqueGateResult` (type), `SpindleTorqueBlockError` (class), `spindleTorqueGateEngine` (const), `SpindleTorqueGateEngine` (re-export)

## Key methods
`gate`, `gateOrThrow`

## Depends on
[[reference_code_engine_latheprinttoolpathgeneratorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/SpindleTorqueGateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SpindleTorqueBlockError` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SpindleTorqueGateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
