---
name: reference_code_algorithm_knearestneighbors
description: "KNearestNeighbors — k-NN similarity search + classification + regression over"
metadata:
  type: reference
  kind: code-algorithm
  galaxy: unassigned
  sourcePath: mcp-server/src/algorithms/KNearestNeighbors.ts
  sourceHash: 79f5df84
  loc: 244
aliases: KNearestNeighbors
---

# KNearestNeighbors -- Algorithm SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/algorithms/KNearestNeighbors.ts` (244 LOC)

## What it is
```
KNearestNeighbors — k-NN similarity search + classification + regression over
a dense vector corpus. The retrieval core of RAG (embed a query, return the
k most-similar corpus vectors) and a non-parametric predictor.

  for each query q:
    score every corpus row by `metric` distance to q
    take the k nearest (smallest distance)
    task="search"   → return their indices + distances
    task="classify" → majority vote of their labels (ties → nearest wins)
    task="regress"  → mean of their (numeric) labels

Pure, deterministic, exact (full scan — no ANN index), numerically safe.
Cosine distance is 1 − cosθ (zero-norm vectors → distance 1, flagged). Pairs
naturally with the other ALGO-SYNERGY primitives: reduce dimensionality with
`ml_pca` before retrieval, or use `ml_dtw` distances for time-series k-NN.

Why PRISM needs it: RAG retrieval over india's embedded corpora, similar-job /
s
```


## Exports
`KNNMetric` (type), `KNNTask` (type), `KNNLabel` (type), `KNNInput` (interface), `KNNResult` (interface), `KNNOutput` (interface), `KNearestNeighbors` (const)

## Key methods
`validate`, `calculate`, `getMetadata`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/algorithms/KNearestNeighbors.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Algorithm

## How to query it
Recall by meaning via `/system-viz find KNearestNeighbors` or `prism_session:master_index_query`; open the source at `mcp-server/src/algorithms/KNearestNeighbors.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
