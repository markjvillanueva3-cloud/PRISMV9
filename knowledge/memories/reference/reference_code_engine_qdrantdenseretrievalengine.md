---
name: reference_code_engine_qdrantdenseretrievalengine
description: "QdrantDenseRetrievalEngine -- Thin async REST wrapper for Qdrant dense-vector search."
metadata:
  type: reference
  kind: code-engine
  galaxy: ai-training
  sourcePath: mcp-server/src/engines/ai-training/QdrantDenseRetrievalEngine.ts
  sourceHash: ad20e147
  loc: 171
aliases: QdrantDenseRetrievalEngine
---

# QdrantDenseRetrievalEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ai-training/QdrantDenseRetrievalEngine.ts` (171 LOC)

## What it is
QdrantDenseRetrievalEngine -- Thin async REST wrapper for Qdrant dense-vector search.

```
QdrantDenseRetrievalEngine -- Thin async REST wrapper for Qdrant dense-vector search.

Wraps the Qdrant REST API `/collections/{col}/points/search` endpoint.
Returns normalized hits whose shape matches the existing `defaultQdrantSearch`
output in `scripts/lib/hybrid-retrieval.mjs` so RRF fusion is drop-in.

Design choices:
  - Uses native `fetch` (Node 22+; no new dependencies).
  - `AbortController` enforces a per-call timeout (default 5000 ms).
  - Every failure path returns an empty array + logs the reason (never throws).
  - Singleton export so callers share connection config without re-passing opts.
  - No physics constants required (pure network/data engine).

Gate: this engine is intended to be invoked only when `PRISM_RAG_DENSE_QDRANT=1`
is set.  The hybrid-retrieval.mjs caller enforces the gate; this engine does not
re-check it so tests can exercise the engine directly.

@module
```


## Exports
`QdrantDenseHit` (interface), `QdrantDenseSearchOptions` (interface), `QdrantDenseSearchResult` (interface), `pickPayloadId` (function), `QdrantDenseRetrievalEngine` (class), `qdrantDenseRetrievalEngine` (const)

## Key methods
`search`, `clearTimeout`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ai-training/QdrantDenseRetrievalEngine.ts`
- **Galaxy:** **ai-training**
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find QdrantDenseRetrievalEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ai-training/QdrantDenseRetrievalEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
