---
name: reference_code_engine_stockworkholdingcatalogengine
description: "the 20 fixture parts (U-CAMTEST05) × 5 material/form templates. Each"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/StockWorkholdingCatalogEngine.ts
  sourceHash: 92a9fefa
  loc: 320
aliases: StockWorkholdingCatalogEngine
---

# StockWorkholdingCatalogEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/StockWorkholdingCatalogEngine.ts` (320 LOC)

## What it is
the 20 fixture parts (U-CAMTEST05) × 5 material/form templates. Each

```
StockWorkholdingCatalogEngine — U-CAMTEST06
============================================

PHASE-8: 100 stock + workholding setups derived as the cross-product of
the 20 fixture parts (U-CAMTEST05) × 5 material/form templates. Each
setup tells the in-host runner what raw stock to spawn and how to clamp
it — the in-host runners then synthesize the actual stock body and a
matching workholding fixture using their native CAD API.

Material/form templates (one per stock slot per part):
  slot A — 6061-T6      plate         (machinist vise, steel jaws)
  slot B — 1018-CRS     plate         (machinist vise, steel jaws)
  slot C — D2 tool-steel billet       (machinist vise, hardened jaws)
  slot D — Inconel-718  billet        (machinist vise, hardened jaws)
  slot E — UHMW         plate         (vise with soft jaws)

Stock envelope = part envelope + per-template margin (per side). The
generator r
```


## Exports
`StockMaterialSlotSchema` (const), `StockMaterialSlot` (type), `StockFormSchema` (const), `StockForm` (type), `WorkholdingKindSchema` (const), `WorkholdingKind` (type), `JawTypeSchema` (const), `JawType` (type), `WorkholdingSchema` (const), `Workholding` (type), `StockEnvelopeMmSchema` (const), `StockEnvelopeMm` (type), `StockSetupDescriptorSchema` (const), `StockSetupDescriptor` (type), `StockWorkholdingCatalogEngine` (class), `stockWorkholdingCatalogEngine` (const)

## Key methods
`list`, `listByPart`, `listByMaterial`, `listByForm`, `mustGet`, `count`, `countByMaterial`, `countByForm`, `auditCatalog`

## Depends on
[[reference_code_engine_fixturepartcatalogengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/StockWorkholdingCatalogEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find StockWorkholdingCatalogEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/StockWorkholdingCatalogEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
