---
name: reference_code_util_stockvolume
description: "stockVolume.ts -- the single, unit-agnostic stock-volume resolver"
metadata:
  type: reference
  kind: code-util
  galaxy: unassigned
  sourcePath: mcp-server/src/utils/stockVolume.ts
  sourceHash: 893748d1
  loc: 130
aliases: stockVolume.ts
---

# stockVolume.ts -- Util SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/utils/stockVolume.ts` (130 LOC)

## What it is
```
stockVolume.ts -- the single, unit-agnostic stock-volume resolver
(U-QP-STOCK-VOLUME-CORE).

Two engines independently resolved a part's STOCK volume from the same set of
sources (rectangular block bbox, cylindrical bar stock, or an explicit volume):
  - QuoteEstimatorEngine.resolveStockVolumeCm3  (returns cm^3)
  - InstantQuoteEngine.computeStockVolumeIn3     (returns in^3)
They could NOT share code by importing each other -- InstantQuoteEngine already
imports QuoteEstimatorEngine, so a back-import would create a cycle. The fix is a
LEAF utility both engines import (neither imports the other through it): compute
the volume + source tag in mm^3 here, and let each caller divide by its unit's
`mm^3 per unit` factor.

The cylinder formula (V = pi/4 * d^2 * L), the rect bbox (L * W * H), and the
finite/positive guards now live in exactly ONE place -- a future fix to the
precedence or the geo
```


## Exports
`MM3_PER_IN3` (const), `MM3_PER_CM3` (const), `StockVolumeSources` (interface), `StockVolumeSourceTag` (type), `ResolvedStockVolumeMm3` (interface), `resolveStockVolumeMm3` (function), `resolveStockVolumeIn3` (function), `resolveStockVolumeCm3` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/utils/stockVolume.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Util

## How to query it
Recall by meaning via `/system-viz find stockVolume.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/utils/stockVolume.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
