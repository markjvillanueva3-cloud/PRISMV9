---
name: reference_code_engine_cadtestcheckpointengine
description: "CAD Test Checkpoint"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADTestCheckpointEngine.ts
  sourceHash: 27356528
  loc: 304
aliases: CADTestCheckpointEngine
---

# CADTestCheckpointEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADTestCheckpointEngine.ts` (304 LOC)

## What it is
CAD Test Checkpoint

```
CADTestCheckpointEngine — U-CINF05 (CAD-INFRA-MS0)

Standalone engine owning CAD-regression checkpoint policy and persistence.
Implements the "every 100 files OR every 60s" cadence described in the
CAD-INFRA-MS0 milestone, plus load/resume-diff helpers used by dashboards,
CLI tools, and the regression orchestrator.

Scope split vs CADRegressionTestOrchestratorEngine (U-CINF04):
  - Orchestrator: owns the worker pool and task dispatch. It currently
    ships inline checkpoint logic for performance (hot path).
  - CheckpointEngine (this file): owns the POLICY — when to checkpoint,
    what directory layout to use, how to load + diff existing state.
    Consumers that aren't the orchestrator (dashboards, CLIs, resume
    tools) rely on this engine instead of duplicating policy.

API surface:
  shouldCheckpoint(state)       — pure: "is it time to persist?"
  markSaved(state)              — p
```


## Exports
`DEFAULT_CHECKPOINT_EVERY` (const), `DEFAULT_CHECKPOINT_INTERVAL_MS` (const), `DEFAULT_STATE_DIR` (const), `CheckpointCadenceState` (interface), `CheckpointCadenceOptions` (interface), `ResumeDiff` (interface), `CheckpointFS` (interface), `CADTestCheckpointEngine` (class), `cadTestCheckpointEngine` (const)

## Key methods
`existsSync`, `readFileSync`, `mkdirSync`, `getCapabilities`, `validate`, `createCadence`, `recordTransition`, `shouldCheckpoint`, `markSaved`, `save`, `load`, `defaultPath`, `resumeDiff`

## Depends on
[[reference_code_engine_baseengine]] | [[reference_code_engine_iengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADTestCheckpointEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADTestCheckpointEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADTestCheckpointEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
