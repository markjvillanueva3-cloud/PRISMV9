---
name: reference_code_schema_cammodelgatethresholds
description: "camModelGateThresholds.ts -- canonical CAM-model deploy-gate thresholds (U3)"
metadata:
  type: reference
  kind: code-schema
  galaxy: unassigned
  sourcePath: mcp-server/src/schemas/camModelGateThresholds.ts
  sourceHash: 5ca68395
  loc: 70
aliases: camModelGateThresholds.ts
---

# camModelGateThresholds.ts -- Schema SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/schemas/camModelGateThresholds.ts` (70 LOC)

## What it is
```
camModelGateThresholds.ts -- canonical CAM-model deploy-gate thresholds (U3)
===========================================================================

SINGLE SOURCE OF TRUTH (TS mirror) for the NN-GRAPH-MS0 mandatory deploy
gate, so the engine layer (`CAMModelPromotionGateEngine`) can enforce the
SAME gate the GNN retrain lifecycle enforces, WITHOUT importing a `scripts/`
module (engines must not import scripts -- layer inversion).

PROVENANCE -- these values mirror, verbatim:
  - `GATE_THRESHOLDS` in `scripts/lib/nn-graph-eval.mjs:52`
      { auroc: 0.78, macroF1: 0.55, brier: 0.15 }
  - `GNN_DEFAULTS.minConf` in `scripts/seed-ghost-gnn-classify.mjs:64`  (0.7)
  - `SELECTIVE_THRESHOLDS` in `scripts/lib/nn-graph-eval.mjs:209`

DRIFT GUARD (R12): `camModelGateThresholds.test.ts` re-reads the two `.mjs`
source files at test time and FAILS if any value here has drifted from the
canonical
```


## Exports
`CAM_GATE_THRESHOLDS` (const), `CAM_PRODUCTION_MIN_CONF` (const), `CAM_SELECTIVE_THRESHOLDS` (const), `CAM_MIN_HOLDOUT_PER_CLASS` (const), `CamGateThresholds` (type)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/schemas/camModelGateThresholds.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Schema

## How to query it
Recall by meaning via `/system-viz find camModelGateThresholds.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/schemas/camModelGateThresholds.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
