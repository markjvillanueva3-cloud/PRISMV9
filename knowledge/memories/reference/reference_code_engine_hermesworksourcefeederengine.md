---
name: reference_code_engine_hermesworksourcefeederengine
description: "HermesWorkSourceFeederEngine -- HERMES-WORK-LOOP-MS0/U3 work-source -> Subtask adapter."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HermesWorkSourceFeederEngine.ts
  sourceHash: bd06fd0b
  loc: 280
aliases: HermesWorkSourceFeederEngine
---

# HermesWorkSourceFeederEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HermesWorkSourceFeederEngine.ts` (280 LOC)

## What it is
HermesWorkSourceFeederEngine -- HERMES-WORK-LOOP-MS0/U3 work-source -> Subtask adapter.

```
HermesWorkSourceFeederEngine -- HERMES-WORK-LOOP-MS0/U3 work-source -> Subtask[] adapter.

The PARALLEL fan-out machinery already exists (HermesParallelFanoutPlannerEngine,
HermesAutonomousDriveRunnerEngine). This engine is the missing FEEDER: a thin, PURE
adapter that turns heterogeneous PRISM work-source rows (open roadmap units, research/wiki
tasks, unwired-engine wiring gaps, galaxy-synthesis gaps) into the canonical `Subtask[]`
shape those engines already consume -- AFTER deduping against the live slot-task claims so
a Hermes agent never races a work slot, and risk-classifying each unit so the downstream
executor can route it (read-only/research -> local Ollama + plan-only; code/engine -> cloud
Grok + draft-with-diff; safety-touching -> ALWAYS plan-only, high risk).

PURE (engines are calculation-only -- engines/CLAUDE.md): the caller (the U4 driver) does ALL
I/O -- reads the source
```


## Exports
`WorkSourceUnitSchema` (const), `WorkSourceUnit` (type), `FeederRequestSchema` (const), `FeederRequest` (type), `RiskClass` (type), `ClassifiedSubtask` (interface), `FeederResult` (interface), `HermesWorkSourceFeederEngine` (class), `hermesWorkSourceFeederEngine` (const)

## Key methods
`classify`, `toSubtasks`

## Depends on
[[reference_code_engine_hermesparallelfanoutplannerengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/HermesWorkSourceFeederEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HermesWorkSourceFeederEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HermesWorkSourceFeederEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
