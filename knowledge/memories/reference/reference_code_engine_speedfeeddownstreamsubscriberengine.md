---
name: reference_code_engine_speedfeeddownstreamsubscriberengine
description: "wires the 5 downstream PRISM consumers"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SpeedFeedDownstreamSubscriberEngine.ts
  sourceHash: 2981d7c2
  loc: 239
aliases: SpeedFeedDownstreamSubscriberEngine
---

# SpeedFeedDownstreamSubscriberEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SpeedFeedDownstreamSubscriberEngine.ts` (239 LOC)

## What it is
wires the 5 downstream PRISM consumers

```
SpeedFeedDownstreamSubscriberEngine — wires the 5 downstream PRISM consumers
to the SpeedFeedPropagationBridge so they auto-receive SFC updates without
having to call the orchestrator themselves.

The bridge exists (U-OSC9-03). This engine REGISTERS the subscribers so
that the auto-emit event chain actually moves data into:

  1. Post-processor pipeline cache    (drives feed/speed override blocks)
  2. Mill wizard form-defaults cache  (frontend form preload)
  3. Lathe wizard form-defaults cache (frontend form preload)
  4. Wire-EDM wizard hint cache       (pass-through advisory)
  5. Print-to-program param-pack cache (18-stage pipeline preload)

Each cache is keyed by `(machine, material, tool_diameter)` — the same key
the bridge uses. Downstream consumers query their cache by key, never call
the orchestrator directly. When a new SFC snapshot is published, all 5
caches update in-place v
```


## Exports
`SpeedFeedDownstreamSubscriberEngine` (class), `speedFeedDownstreamSubscriberEngine` (const)

## Key methods
`registerAllSubscribers`, `unregisterAllSubscribers`, `isRegistered`, `getPostProcessorPack`, `getMillWizardDefaults`, `getLatheWizardDefaults`, `getWedmWizardDefaults`, `getPrintToProgramPack`, `cacheSnapshot`, `totalVersionCount`, `clear`

## Depends on
[[reference_code_engine_speedfeedpropagationbridgeengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/SpeedFeedDownstreamSubscriberEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SpeedFeedDownstreamSubscriberEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SpeedFeedDownstreamSubscriberEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
