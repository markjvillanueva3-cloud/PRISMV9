---
name: reference_code_module_verifyblockannotations
description: "verifyBlockAnnotations — Per-block S/F gate (MS0/U-PPGM10, schema 1.1.0)."
metadata:
  type: reference
  kind: code-module
  galaxy: unassigned
  sourcePath: mcp-server/src/cps/verifyBlockAnnotations.ts
  sourceHash: 2f2b2d04
  loc: 359
aliases: verifyBlockAnnotations
---

# verifyBlockAnnotations -- Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-04T14:51:22.427Z  |  source: `mcp-server/src/cps/verifyBlockAnnotations.ts` (359 LOC)

## What it is
```
verifyBlockAnnotations — Per-block S/F gate (MS0/U-PPGM10, schema 1.1.0).

Cross-references emitted G-code blocks against the sidecar's
block_annotations[] so a post can't quietly emit a feed/speed that no
physics chain produced. Runs Node-side in the build pipeline before
publishing the .cps output (paired with NoInlinePhysicsConstantsEngine
which scans the .cps source for inlined kc1_1/Taylor C/tool-modulus
literals — different artifact, same fail-closed posture).

Contract:
  1. Parser walks each line of the emitted G-code, extracting block_id
     from Nxxx labels and the S<n>/F<n> tokens that appear on that line.
  2. Lines without an Nxxx label are skipped (anonymous blocks have no
     stable identifier the sidecar can attach annotations to).
  3. Each labelled line with at least one of S/F is verified against the
     annotation looked up by block_id. operator_override blocks byp
```


## Exports
`Tier` (type), `Verdict` (type), `MismatchReason` (type), `BlockMismatch` (interface), `VerifyResult` (interface), `VerifyOptions` (interface), `verifyBlockAnnotations` (function), `verifyOrThrow` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/cps/verifyBlockAnnotations.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Module

## How to query it
Recall by meaning via `/system-viz find verifyBlockAnnotations` or `prism_session:master_index_query`; open the source at `mcp-server/src/cps/verifyBlockAnnotations.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
