---
name: reference_code_engine_modelroutingengine
description: "Phase 0.19 U-LLM7"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ModelRoutingEngine.ts
  sourceHash: 010dba60
  loc: 864
aliases: ModelRoutingEngine
---

# ModelRoutingEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ModelRoutingEngine.ts` (864 LOC)

## What it is
Phase 0.19 U-LLM7

```
ModelRoutingEngine — Phase 0.19 U-LLM7

Decide which LLM backend/model should service a given request so simple
queries take the local Ollama path (free, on-prem, fast once warm) and
only queries that genuinely need frontier reasoning fall back to the paid
Claude/OpenAI APIs. Pure scoring function: the engine does NO network I/O
— callers feed it request features + a live backend-availability snapshot,
and it returns a routing decision with rationale. Executing the decision
is `LocalModelOrchestratorEngine`'s job (U-LLM1).

Design rules:
  1. Safety-critical manufacturing decisions (cutting force validation,
     collision safety, workholding adequacy) always route to the highest-
     capability backend. Never let a 3B local model decide whether a cut
     is safe.
  2. Embedding requests always stay local — MiniLM/nomic are purpose-built
     for this and paying frontier rates for vect
```


## Exports
`TaskKind` (type), `Backend` (type), `HardwareProfile` (type), `ModelSpec` (interface), `RoutingRequest` (interface), `RoutingContext` (interface), `RoutingDecision` (interface), `DEFAULT_MODEL_CATALOG` (const), `ModelAdaptationEntry` (interface), `ModelAdaptiveState` (type), `ModelRoutingEngine` (class), `modelRoutingEngine` (const)

## Key methods
`register`, `listModels`, `applyAdaptiveState`, `getAdaptiveState`, `getEffectiveLatency`, `route`, `getBackendStatus`, `getStats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ModelRoutingEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ModelRoutingEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ModelRoutingEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
