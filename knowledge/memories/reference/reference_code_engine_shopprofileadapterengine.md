---
name: reference_code_engine_shopprofileadapterengine
description: "per-shop learning adapter layer"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ShopProfileAdapterEngine.ts
  sourceHash: cc22bd50
  loc: 470
aliases: ShopProfileAdapterEngine
---

# ShopProfileAdapterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ShopProfileAdapterEngine.ts` (470 LOC)

## What it is
per-shop learning adapter layer

```
ShopProfileAdapterEngine — per-shop learning adapter layer

Sits ABOVE ShopConfigurationEngine (the static profile) and BELOW the
cost / quote / time engines. Learns per-shop calibration deltas from
outcome history (caller-supplied; engine is pure) and emits multipliers
that adjust baseline ShopProfile values to a specific shop's reality.

Architecture (slot:india /goal-psn-self-improving iter2):

  raw calculation (Kienzle / Taylor / quote estimator)
      │
      ▼  ShopProfileAdapterEngine.adapt(shopId, raw)
      │
      └─→ shop-calibrated result  (with per-delta confidence + provenance)

Outcome ingestion (`learnFromOutcome`) updates an in-engine delta cache
via EWMA (exponential-weighted moving average) — recent outcomes dominate,
old outcomes decay. Critical-severity outliers (S(x)<0.7) are NOT folded
into rate/time deltas (they'd poison the average); instead surfaced as
`anomali
```


## Exports
`ShopOutcomeRecord` (interface), `ShopAdapterDelta` (interface), `ShopAdapterDeltas` (interface), `AdaptedValue` (interface), `ewmaUpdate` (function), `confidenceFromEss` (function), `clampMultiplier` (function), `foldOutcomeIntoDelta` (function), `ShopProfileAdapterEngine` (class), `shopProfileAdapterEngine` (const), `projectAdaptedProfile` (function)

## Key methods
`getDeltas`, `hydrate`, `reset`, `learnFromOutcome`, `adapt`, `summarize`

## Depends on
[[reference_code_engine_shopconfigurationengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/ShopProfileAdapterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ShopProfileAdapterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ShopProfileAdapterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
