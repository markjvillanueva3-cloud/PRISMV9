---
name: reference_code_engine_cammachiningerrorpredictionengine
description: "Predictive Alerts (U-CAM102)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMMachiningErrorPredictionEngine.ts
  sourceHash: 1041d877
  loc: 474
aliases: CAMMachiningErrorPredictionEngine
---

# CAMMachiningErrorPredictionEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMMachiningErrorPredictionEngine.ts` (474 LOC)

## What it is
Predictive Alerts (U-CAM102)

```
CAMMachiningErrorPredictionEngine — Predictive Alerts (U-CAM102)
=================================================================

PHASE-7: Pre-execution scan of a CAM toolpath that flags risky segments
BEFORE the operator hits cycle start. Each segment is evaluated against
five physics-grounded predictors:

  1. CHATTER         — engagement / SLD margin
  2. DEFLECTION      — radial force vs tool stiffness (cantilever model)
  3. THERMAL         — sustained MRR with insufficient coolant capacity
  4. TOOL_OVERLOAD   — Kienzle Fc vs tool load limit
  5. SURFACE_FINISH  — Ra estimate vs target tolerance

Each predictor produces a normalized severity score (0..1) and is mapped
to one of four alert priorities:

  critical  ≥ 0.85   — STOP. Hard physical risk (tool break, crash).
  high      ≥ 0.65   — Likely failure on first run.
  medium    ≥ 0.40   — Marginal — operator review.
  low    
```


## Exports
`PredictionTargetSchema` (const), `PredictionTarget` (type), `PredictorSchema` (const), `Predictor` (type), `AlertPrioritySchema` (const), `AlertPriority` (type), `ToolpathSegmentSchema` (const), `ToolpathSegment` (type), `PredictedAlertSchema` (const), `PredictedAlert` (type), `PredictionReportSchema` (const), `PredictionReport` (type), `PredictionStatsSchema` (const), `PredictionStats` (type), `PRIORITY_CRITICAL` (const), `PRIORITY_HIGH` (const), `PRIORITY_MEDIUM` (const), `severityToPriority` (function), `predictToolOverload` (function), `predictDeflection` (function)

## Key methods
`supportedTargets`, `supportedPredictors`, `evaluateSegment`, `scanToolpath`, `encodeReport`, `getStats`, `resetSession`, `resetAll`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CAMMachiningErrorPredictionEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMMachiningErrorPredictionEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMMachiningErrorPredictionEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
