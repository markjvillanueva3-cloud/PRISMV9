---
name: reference_code_engine_hermesautomationbridge
description: "Hermes Automation Bridge"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HermesAutomationBridge.ts
  sourceHash: a3b6fc93
  loc: 603
aliases: HermesAutomationBridge
---

# HermesAutomationBridge -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HermesAutomationBridge.ts` (603 LOC)

## What it is
Hermes Automation Bridge

```
HermesAutomationBridge -- PRISM -> Hermes Agent (Nous Research) automation bridge.

Bridge A of the bidirectional Claude-Code <-> Hermes integration. Lets PRISM /
Claude Code drive the locally-installed Hermes CLI in a SANDBOXED, mock-by-default
way, mirroring the existing Fusion360AutomationBridge / cimco-sim-driver pattern.

Why a bridge: the operator runs on a Claude subscription. Hermes' desktop login
dropped OAuth, so the durable way to keep heavy agentic work on the subscription
is to drive Hermes from Claude Code rather than the reverse. This bridge is the
CC -> Hermes half (Bridge B, hermes-zulu -> CC fleet, lives in scripts/fleet).

SAFETY (mirrors the proven bridges):
  - MOCK-by-default. A LIVE run that spawns the Hermes CLI requires DUAL-KEY:
    BOTH the `noMock` flag AND env PRISM_HERMES_MOCK=0. Neither alone is enough,
    so a single switch can never accidentally spawn an
```


## Exports
`AtomicValue` (interface), `HermesRoutineSpec` (interface), `userProfileHermesCandidates` (function), `resolveHermesHome` (function), `DEFAULT_HERMES_TIMEOUT_MS` (const), `HERMES_REQUESTED_CAPS` (const), `HERMES_PLUGIN_ID` (const), `SpawnFn` (type), `HermesBridgeOptions` (interface), `HermesAutomationBridge` (class), `hermesAutomationBridge` (const)

## Key methods
`existsFn`, `status`, `probe`, `authStatus`, `walk`, `cronList`, `skillList`, `routinePlan`, `modelList`, `run`

## Depends on
[[reference_code_engine_pluginsandboxpolicyengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/HermesAutomationBridge.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HermesAutomationBridge` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HermesAutomationBridge.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
