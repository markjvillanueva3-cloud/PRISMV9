---
name: reference_code_engine_millloramasterorchestratorengine
description: "MillLoRAMasterOrchestratorEngine — Lifecycle Master (Mill parity)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MillLoRAMasterOrchestratorEngine.ts
  sourceHash: 4a9c18e4
  loc: 360
aliases: millLoRAMasterOrchestratorEngine
---

# millLoRAMasterOrchestratorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MillLoRAMasterOrchestratorEngine.ts` (360 LOC)

## What it is
```
MillLoRAMasterOrchestratorEngine — Lifecycle Master (Mill parity)
==================================================================

Master orchestrator that ties together all MillLoRA subsystems —
data → training → evaluation → first-piece-validation → deployment →
production → maintenance → decommissioning.

Mill parity for LatheLoRAMasterOrchestratorEngine (LATHE-LORA-MS0 U-LLR50)
with MILL-CANONICAL extensions:

  - Mill-canonical phase 'first_piece_validation' inserted between
    evaluation and deployment — mill jobs MUST clear an AS9102 FAI
    gate before any model is allowed to drive a production cell
  - Per-axis_mode subsystem scope — registerSubsystem accepts
    axis_mode so the orchestrator can keep 3-axis, 4-axis-index,
    and 5-axis-simul stacks in independent lifecycle phases
  - 3 mill-canonical subsystem-name conventions auto-registered
    by initializeMillStack(): 
```


## Exports
`MillSystemPhase` (type), `MillSystemHealth` (type), `MillAxisMode` (type), `MILL_CANONICAL_SUBSYSTEMS` (const), `MillCanonicalSubsystem` (type), `MillSubsystemStatus` (interface), `MillMasterState` (interface), `MillMasterConfig` (interface), `millLoRAMasterOrchestratorEngine` (const), `MillLoRAMasterOrchestratorEngine` (re-export)

## Key methods
`setConfig`, `getConfig`, `initialize`, `initializeMillStack`, `getState`, `registerSubsystem`, `updateSubsystem`, `updateMetrics`, `transition`, `beginOperation`, `endOperation`, `getEvents`, `healthCheck`, `getStats`, `getStatsByAxis`, `getSubsystemsByAxis`, `getSummary`, `shutdown`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MillLoRAMasterOrchestratorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find millLoRAMasterOrchestratorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MillLoRAMasterOrchestratorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
