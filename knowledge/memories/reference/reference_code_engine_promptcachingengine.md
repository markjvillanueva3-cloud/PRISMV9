---
name: reference_code_engine_promptcachingengine
description: "PromptCachingEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PromptCachingEngine.ts
  sourceHash: b7a4461b
  loc: 257
aliases: promptCachingEngine
---

# promptCachingEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PromptCachingEngine.ts` (257 LOC)

## What it is
```
PromptCachingEngine
====================

AGENT-MS5 U-AGT19 — Wraps system prompts and common context chunks with
Anthropic prompt caching markers (cache_control) so LLMEngine can pass
them directly to the SDK without each caller knowing the marker format.

Anthropic prompt caching spec (condensed):
  - system prompt blocks may be marked { type:"ephemeral" } to cache
  - up to 4 cache breakpoints per request
  - cache TTL = 5 minutes
  - first request after cache miss writes; subsequent reads within TTL hit
  - cached tokens are billed at 10% of input cost for reads, 125% for writes

This engine does NOT make API calls — it produces the shape of the system
array so any consumer (LLMEngine, AgentDispatcher, ExtendedThinkingBridge)
can hand it to the Anthropic SDK unchanged.

Usage:
  const { system, cache_breakpoints } = promptCachingEngine.buildCachedSystem({
    stable: [basePrompt, too
```


## Exports
`SystemBlock` (interface), `CachedSystemResult` (interface), `CacheStats` (interface), `ObservedUsage` (interface), `BuildOptions` (interface), `ANTHROPIC_MAX_CACHE_BREAKPOINTS` (const), `DEFAULT_MIN_CACHE_CHARS` (const), `promptCachingEngine` (const)

## Key methods
`buildCachedSystem`, `recordUsage`, `getStats`, `resetStats`, `wrapSystemPrompt`, `breakEvenReads`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PromptCachingEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find promptCachingEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PromptCachingEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
