---
name: reference_code_engine_corpusprovenanceledgerengine
description: "CAM-AI-TRAINING-MS0/U-CAMT-A09"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CorpusProvenanceLedgerEngine.ts
  sourceHash: e3ddeb3e
  loc: 323
aliases: CorpusProvenanceLedgerEngine
---

# CorpusProvenanceLedgerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CorpusProvenanceLedgerEngine.ts` (323 LOC)

## What it is
CAM-AI-TRAINING-MS0/U-CAMT-A09

```
CorpusProvenanceLedgerEngine — CAM-AI-TRAINING-MS0/U-CAMT-A09

Single source of truth for CAD corpus provenance. Every entry in the
unified CAM-training corpus MUST have an entry here BEFORE downstream
trainers (LoRA, GNN, RAG) are allowed to consume it. The ledger is
the gate that enforces the operator's 2026-05-25 hard constraint:

  "no synthetic parts, must be real parts taken from real reputable
   sources"

Architecture — pure-logic core, file I/O lives in the runner script:
  - register(entry)        → ProvenanceRecord (rejects synthetic, dedup by hash)
  - lookup(hash)           → ProvenanceRecord | null
  - validate(entry)        → ProvenanceCheck (rejects missing fields)
  - reconcile(catalog, ledger) → ReconciliationResult
  - aggregate(records)     → ProvenanceAggregate (by license / archive / source)
  - exportLedger()         → ProvenanceRecord[] (deterministic-order)

Why 
```


## Exports
`REPUTABLE_ARCHIVES` (const), `ReputableArchive` (type), `KNOWN_LICENSES` (const), `KnownLicense` (type), `ProvenanceRecord` (interface), `ProvenanceCheck` (interface), `ReconciliationResult` (interface), `ProvenanceAggregate` (interface), `CorpusProvenanceLedgerEngine` (class), `corpusProvenanceLedgerEngine` (const)

## Key methods
`getCapabilities`, `validate`, `checkRecord`, `register`, `lookup`, `reconcile`, `aggregate`, `exportLedger`, `reset`, `bulkLoad`

## Depends on
[[reference_code_engine_baseengine]] | [[reference_code_engine_iengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CorpusProvenanceLedgerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CorpusProvenanceLedgerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CorpusProvenanceLedgerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
