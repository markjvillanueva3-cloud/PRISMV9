---
name: reference_code_engine_zuluawarenessreader
description: "ZULU-AWARENESS-MS1 / U-AW02 — TypeScript-side awareness reader."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/lib/zuluAwarenessReader.ts
  sourceHash: 5d26a21d
  loc: 281
aliases: zuluAwarenessReader.ts
---

# zuluAwarenessReader.ts -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/lib/zuluAwarenessReader.ts` (281 LOC)

## What it is
```
ZULU-AWARENESS-MS1 / U-AW02 — TypeScript-side awareness reader.

Reads `state/shared/zulu-awareness-index.json` (built by
`scripts/zulu-awareness-run.mjs`) and exposes minimal lookups for TS engines
(AISystemRouterEngine, PRISMSelfAwarenessEngine). The full ranking pipeline
is `.mjs`-only — this reader does the read + a thin scoring projection so
consumers can answer "which slot best fits this task?" without booting
Node's ESM .mjs loader from inside the TS bundle.

Design invariants:
  1. Fail-soft — never throws; missing/corrupt file → empty envelope.
  2. Single-load memo — index is read once per process; tests call
     invalidateAwarenessCache() between runs.
  3. Same scoring shape as `scoreSlotForTask` in zulu-awareness-pipeline.mjs
     so consumers see the same evidence keys (domain:*, tribal:*, viz:*, success:*).
  4. Refuse-list HARD VETO is preserved — refused slots score -10
```


## Exports
`AwarenessFingerprint` (interface), `AwarenessIndex` (interface), `AwarenessEnvelope` (interface), `SlotRecommendation` (interface), `invalidateAwarenessCache` (function), `loadAwareness` (function), `lookupFingerprint` (function), `rankSlotsForTaskDescriptor` (function), `bestSlotForTask` (function), `LiveBrainContext` (interface), `liveBrainContext` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/lib/zuluAwarenessReader.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find zuluAwarenessReader.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/lib/zuluAwarenessReader.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
