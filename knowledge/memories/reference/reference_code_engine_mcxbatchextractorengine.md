---
name: reference_code_engine_mcxbatchextractorengine
description: "LATHE-PROD-READY-MS0/U-LPR28"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/McxBatchExtractorEngine.ts
  sourceHash: 1f222204
  loc: 556
aliases: McxBatchExtractorEngine
---

# McxBatchExtractorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/McxBatchExtractorEngine.ts` (556 LOC)

## What it is
LATHE-PROD-READY-MS0/U-LPR28

```
McxBatchExtractorEngine — LATHE-PROD-READY-MS0/U-LPR28
=======================================================

Bounded-concurrency batch extractor for the JM Die Mastercam binary
corpus — 3,713 `.mcx-8` + 1,825 `.MCX` + future `.mcam`.  Sibling
implementation to {@link ./MINBatchExtractorEngine}: same checkpoint
shape, same atomic write+rename, same skip-if-completed resume
contract.  Differences:

  • Source format: binary (delegates to {@link mcxProgramParserEngine}
    for metadata extraction).  No SDK dependency.
  • Per-file output: McxProgramMetadata (NOT MIN ParseResult).
  • Aggregations: byFormat (.mcx / .mcx-8 / .mcx-9 / .mcam),
    byMagicVerified (true/false), byCustomer (parsed from path).

Why a sibling class instead of a single shared engine?  The two
parsers have different per-file APIs (text+source_path vs
Buffer+filename), different status semantics (MIN's parse_ok car
```


## Exports
`McxBatchTaskSchema` (const), `McxBatchTask` (type), `McxBatchPerFileResultSchema` (const), `McxBatchPerFileResult` (type), `McxBatchCheckpointSchema` (const), `McxBatchCheckpoint` (type), `McxBatchResultSchema` (const), `McxBatchResult` (type), `DEFAULT_MAX_BYTES_PER_FILE` (const), `DEFAULT_CHECKPOINT_EVERY` (const), `SCHEMA_VERSION` (const), `MCX_EXTENSIONS` (const), `defaultConcurrency` (function), `discoverFilesByExt` (function), `deriveFileId` (function), `extractCustomerFromPath` (function), `buildCheckpointPath` (function), `loadCheckpoint` (function), `saveCheckpoint` (function), `concurrencyLimiter` (function)

## Key methods
`resolve`, `release`, `extractBatch`, `saveCheckpoint`, `limiter`, `recordResult`, `placeholderFor`

## Depends on
[[reference_code_engine_mcxprogramparserengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/McxBatchExtractorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find McxBatchExtractorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/McxBatchExtractorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
