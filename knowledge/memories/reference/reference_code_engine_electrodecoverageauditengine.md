---
name: reference_code_engine_electrodecoverageauditengine
description: "Electrode Coverage Audit"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ElectrodeCoverageAuditEngine.ts
  sourceHash: f58fb069
  loc: 576
aliases: ElectrodeCoverageAuditEngine
---

# ElectrodeCoverageAuditEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ElectrodeCoverageAuditEngine.ts` (576 LOC)

## What it is
Electrode Coverage Audit

```
ElectrodeCoverageAuditEngine
===========================================

Read-only inventory + cross-reference audit of the JM Die ELECTRODE / TAPTITE
corpus against the "Automated Program" tracking workbook
(`H:/PRISM/JM DIE/Automated Program_Corrected 5-25.xlsm`).

SAFETY-CRITICAL READ-ONLY (per spec TRAINING-LEARNING-MS0 / MS0-U3):
  - Engine never writes to the .xlsm. Only `fs.statSync()` + `fs.readFileSync()`
    (binary, for sha256 fingerprint) are used. NO `fs.write*`, NO `fs.unlink*`,
    NO `fs.rename*`, NO `fs.chmod*` are called on the .xlsm path or anywhere else.
  - Engine never re-walks the JM Die corpus to mutate it.
  - Test contract: `fs.statSync(.xlsm).mtimeMs` is unchanged after `engine.report()`.

Public surface (called by camDispatcher cases):
  - scanCorpus(opts?)        → CorpusScan       (live walk OR presetSnapshot)
  - xlsmFingerprint(opts?)   → XlsmFingerprint 
```


## Exports
`DEFAULT_XLSM_PATH` (const), `DEFAULT_CORPUS_ROOT` (const), `BASELINE_EXPECTED` (const), `DEFAULT_MAX_DEPTH` (const), `SAMPLE_PATHS_PER_CATEGORY` (const), `SCHEMA_VERSION` (const), `CorpusScan` (interface), `CorpusScanError` (interface), `XlsmFingerprintPresent` (interface), `XlsmFingerprintAbsent` (interface), `XlsmFingerprintError` (interface), `XlsmFingerprint` (type), `ElectrodeCoverageReport` (interface), `ElectrodeCoverageReportError` (interface), `ScanCorpusOptions` (interface), `XlsmFingerprintOptions` (interface), `ReportOptions` (interface), `ElectrodeCoverageAuditEngine` (class), `electrodeCoverageAuditEngine` (const)

## Key methods
`scanCorpus`, `xlsmFingerprint`, `report`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ElectrodeCoverageAuditEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ElectrodeCoverageAuditEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ElectrodeCoverageAuditEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
