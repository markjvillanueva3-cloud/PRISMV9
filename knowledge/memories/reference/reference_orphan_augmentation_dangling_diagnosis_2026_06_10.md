---
name: reference_orphan_augmentation_dangling_diagnosis_2026_06_10
description: "Clause-4 dormant-node hunt (sierra 2026-06-10): found 2 orphan system-viz augmentations NOT in merge-augmentations loadOptional list (jm-die-cited-tips, milling-extracted-pdf-bridge). VERIFIED both are correctly un-activatable: 100% dangling edges (242 total) vs the live-graph offset oracle, because their generators emit bridge edges to bare engine CLASS NAMES (MasterPostProcessorEngine) instead of eng.<galaxy>.<lowercase> node-ids. The documented-by endpoint-confirmation bug-class recurring. Activating = fleet-search-substrate corruption. Fix = class-name->node-id resolver in the 2 generators (NOT a merge splice)."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.688Z
aliases: reference_orphan_augmentation_dangling_diagnosis_2026_06_10
---


# Orphan augmentation dangling diagnosis -- clause-4 dormant-node hunt (2026-06-10, slot:sierra)

The /goal clause "look for inefficiencies, gaps, wirings and dormant nodes to activate" -> I hunted sierra-lane dormant nodes = **orphan augmentations** (a `*-augmentation.json` whose filename is NOT in `merge-augmentations.mjs`'s explicit `loadOptional(...)` list, so its nodes never fold into the live graph).

## Found: exactly 2 orphans (of 103 augmentations; 101 wired)
1. `jm-die-cited-tips-augmentation.json` -- 13 nodes, 11 edges. Generated by `generate-cited-tips-viz-features.mjs` (echo/post-processor; IS in regen-viz FAST[] line 107 -- the generator runs every regen, but this SECONDARY output isn't spliced into merge).
2. `milling-extracted-pdf-bridge-augmentation.json` -- 77 nodes, 231 edges. Generated by `generate-milling-extracted-pdf-bridge.mjs` (mill/foxtrot; NOT in FAST[], 2wk stale).

## VERIFIED un-activatable (R12 -- proven, not assumed)
Confirmed every edge endpoint against the node-card OFFSET ORACLE (`node-card-offsets.json`, ~336K live node-ids, no 548MB load):
- jm-die-cited-tips: **11/11 edges dangling**.
- milling-extracted-pdf-bridge: **231/231 edges dangling**.
- Total **242 dangling edges** -- activating either (adding a merge splice) would inject all 242 into the fleet search substrate = corruption (sierra's #1 refuse: silent-merge-failure / graph corruption). So they are CORRECTLY dormant; NOT a blind-splice activation candidate.

## Root cause = the documented-by endpoint-confirmation bug-class, recurring
The generators emit bridge edges to bare engine CLASS NAMES (`to: "MasterPostProcessorEngine"`, `"engine.MillMasterOrchestratorFacadeEngine"`) instead of live graph NODE-IDs. The live graph ids are `eng.<galaxy>.<lowercased-classname>` (e.g. `eng.mill.millmasterorchestratorfacadeengine`). No `eng./disp.` namespace variant of the bare names resolves -> dangle. This is the SAME class as the documented-by collapse (emit edges without confirming endpoints against the live graph) -- now found in 2 MORE generators outside my earlier 15-generator sweep scope.
- Nuance: `MasterPostProcessorEngine` surfaced only as a WIKI node (`vault.wiki.architecture.engines.cam.masterpostprocessorengine`), NOT an `eng.*` node -> some targets may be genuinely UN-GRAPHED (the engine isn't in the engine-node layer), so a pure rename-resolver won't fix all 242; those targets need the engine graphed first.

## UPDATE: cited-tips activation is PROVEN-CLEAN + ready-to-execute (verified 2026-06-10)
Re-investigated past the decline. The cited-tips generator (`generate-cited-tips-viz-features.mjs`) hardcodes `BRIDGE_ENGINES_BY_CONTROLLER` (line 31-36) emitting `to: <bareClassName>`. A resolver (oracle key ending in `.<lowercaseClassName>`, prefer `eng.*`) resolves ALL 4 distinct targets cleanly:
- MasterPostProcessorEngine -> `eng.cam.masterpostprocessorengine`
- MasterPostProcessorUnifiedAGIEngine -> `eng.cam.masterpostprocessorunifiedagiengine`
- OkumaOSPMillMasterPostEngine -> `eng.okuma.okumaospmillmasterpostengine`
- HurcoV11MillMasterPostEngine -> `eng.cam.hurcov11millmasterpostengine`
So the cited-tips augmentation (13 nodes / 11 edges) IS cleanly activatable -- 0 dangling once the generator resolves `to:` via the oracle. NOT a half-build risk (my earlier "some un-graphed" worry was wrong for cited-tips; it may still hold for some of the milling-bridge's 231 targets -- check separately).
**Ready-to-execute unit (fresh budget, ~minutes):** (1) add shared `scripts/lib/` resolver (check existing `engine-node-extractor.mjs` first -- R8 dedup) class-name->node-id via offset oracle, skip-loud if unresolvable; (2) cited-tips generator resolves each `to:` (drops unresolvable, no dangling); (3) regen the augmentation, assert 0 dangling vs oracle; (4) wire into `merge-augmentations.mjs` loadOptional + splice (ADD-only, same pattern as the 100 existing augs + my cross-substrate edges); (5) per-file 2-arm scrutiny on BOTH the generator AND the merge edit (canonical-graph writer = warrants full scrutiny); (6) folds on next regen-viz. NOT executed at this session's YELLOW-33%-ctx tail because the merge-augmentations edit (the 711MB canonical-graph writer, sierra's #1 corruption-refuse) deserves full scrutiny budget, and a half-build there could corrupt the fleet search substrate.

## Fix spec (a real multi-file build -- NOT done at session-tail, R13 no-half-build)
1. Add a class-name -> node-id resolver (reuse the eng.<galaxy>.<lowercase> scheme; confirm against the offset oracle) into BOTH `generate-cited-tips-viz-features.mjs` (echo) + `generate-milling-extracted-pdf-bridge.mjs` (foxtrot/mill).
2. For targets that DON'T resolve to any node-id (genuinely un-graphed engines), either skip-with-loud-log (no dangling edge) or ensure the engine is graphed first (owning-galaxy coordination).
3. Once edges resolve, wire the augmentation into `merge-augmentations.mjs` loadOptional + splice (the sierra-lane activation step) -- THEN the dormant nodes fold cleanly.
4. Pairs with the canonical pattern from [[reference_xsub_embeds_docby_oracle_2026_06_10]]: confirm endpoints against the merged graph / offset oracle, never emit unconfirmed.

This is the honest clause-4 outcome: the dormant nodes were FOUND + diagnosed to root cause + proven-unsafe-to-blind-activate; activation is correctly GATED on the generator endpoint-resolver fix. Backend-builder ownership gate is advisory ([[feedback_primary_backend_builders_no_galaxy_gate_block]]) so a sierra session COULD do the generator fix (it's the system-viz endpoint-confirmation pattern), but it is a fresh-budget multi-file unit, not a YELLOW-token tail half-build.

Related: [[reference_xsub_embeds_docby_oracle_2026_06_10]] (the same bug-class + canonical fix) · [[feedback_primary_backend_builders_no_galaxy_gate_block]] · [[feedback_sierra_graph_correctness_is_fleet_search]].
