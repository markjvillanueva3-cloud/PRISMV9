---
name: reference_code_script_seedghostllmclassify
description: "seed-ghost-llm-classify.mjs — SYSTEM-VIZ-FS-COVERAGE-MS2/U-LLM-CLASSIFY"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/seed-ghost-llm-classify.mjs
  sourceHash: 0a84b66e
  loc: 366
aliases: seed-ghost-llm-classify.mjs
---

# seed-ghost-llm-classify.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/seed-ghost-llm-classify.mjs` (366 LOC)

## What it is
```
seed-ghost-llm-classify.mjs — SYSTEM-VIZ-FS-COVERAGE-MS2/U-LLM-CLASSIFY

Final-tier dispatcher inference for UNKNOWN ghost.unwired-engine nodes that
survived both keyword + sibling-prefix inference. Reads each engine file's
top-N-lines header (imports + class signature + JSDoc), batches them into
Ollama qwen2.5-coder:32b prompts (10 engines per call), parses the model's
dispatcher choice + confidence, and updates the graph.

Cold-start mitigation: warms the model on first call (slow ~5s); subsequent
batches are ~2s each. 139 UNKNOWN / 10 per batch = ~14 calls → ~30s total.

Confidence: capped at 0.55 (LLM-derived, lower than keyword 0.85 / sibling 0.65)
since the LLM is using pattern-recognition not deterministic evidence.

Failure modes:
  - Ollama unreachable → skip silently, no graph change
  - Batch timeout → log + skip batch
  - Malformed JSON response → fall back to keep UNKNOWN
  
```


## Exports
`OLLAMA_URL` (const), `DEFAULT_MODEL` (const), `DEFAULT_BATCH_SIZE` (const), `DEFAULT_HEADER_LINES` (const), `LLM_CONFIDENCE` (const), `VALID_DISPATCHERS` (const), `readEngineHeader` (function), `buildBatchPrompt` (function), `callOllamaBatch` (function), `parseBatchResponse` (function), `loadUnknownGhosts` (function), `chunkBatches` (function), `classificationToGraphUpdate` (function), `main` (function)

## Key methods
`clearTimeout`, `writeGraphStreamingAtomic`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/seed-ghost-llm-classify.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find seed-ghost-llm-classify.mjs` or `prism_session:master_index_query`; open the source at `scripts/seed-ghost-llm-classify.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
