---
name: reference_code_script_postblockaudit
description: "post-block-audit.mjs -- block-by-block G-code analyzer for emitted NC (slot:echo)"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/post-block-audit.mjs
  sourceHash: c217cebb
  loc: 373
aliases: post-block-audit.mjs
---

# post-block-audit.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/post-block-audit.mjs` (373 LOC)

## What it is
```
post-block-audit.mjs -- block-by-block G-code analyzer for emitted NC (slot:echo)

Walks an emitted NC program ONE BLOCK AT A TIME and produces a per-block
annotated report: for every block it reports the line number, the parsed
address words, a motion/intent CLASS, the modal-state snapshot AFTER the
block executes, and any within-block / safety / dialect issues. It also
carries a modal-state FSM across blocks so end-of-program invariants
(units declared? feed-mode set? WCS selected? spindle/coolant left ON?
program ended?) can be checked.

This is the operator's "fully analyze every single block" surface -- the
thing you read before feeding the post's output to the CIMCO simulator,
and the precondition the CIMCO report cross-checks. PURE STATIC -- no
engine, no `dist/` build, no MCP; runs on any emitted NC in milliseconds
and is reaper-safe (short-lived node process).

DISTINCT from exi
```


## Exports
`parseBlock` (function), `classifyBlock` (function), `auditNc` (function), `crossRefGolden` (function)

## Key methods
`inv`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/post-block-audit.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find post-block-audit.mjs` or `prism_session:master_index_query`; open the source at `scripts/post-block-audit.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
