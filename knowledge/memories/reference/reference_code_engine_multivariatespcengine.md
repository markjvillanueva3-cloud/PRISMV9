---
name: reference_code_engine_multivariatespcengine
description: "Hotelling T² and MEWMA multivariate control charts"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MultivariateSPCEngine.ts
  sourceHash: 518fefef
  loc: 279
aliases: MultivariateSPCEngine
---

# MultivariateSPCEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MultivariateSPCEngine.ts` (279 LOC)

## What it is
Hotelling T² and MEWMA multivariate control charts

```
MultivariateSPCEngine — Hotelling T² and MEWMA multivariate control charts

Phase 0.22 U-SPC6. Monitors several correlated quality characteristics
jointly. Univariate charts on correlated variables miss shifts that a
joint metric detects; Hotelling's T² statistic is the multivariate
analogue of the univariate z²:

  T²_i = (x_i − μ)ᵀ Σ⁻¹ (x_i − μ)       (Hotelling 1947)

For a Phase II chart with known μ and Σ the UCL is approximated by the
chi-squared critical value χ²_{p, α}. We use the common shop-floor
threshold α = 0.0027 (≈ 3σ for p = 1 after Bonferroni-like framing).

MEWMA (Lowry et al. 1992) applies exponential smoothing to a multivariate
stream before computing the statistic:

  z_i = λ·x_i + (1 − λ)·z_{i−1},  z_0 = μ
  Σ_zᵢ = (λ/(2−λ)) · (1 − (1−λ)^{2i}) · Σ
  T²_i = (z_i − μ)ᵀ Σ_zᵢ⁻¹ (z_i − μ)

References: AIAG SPC 2nd ed. Ch. IV; NIST/SEMATECH e-Handbook §6.3.3.4.

@module e
```


## Exports
`MvSpcConfigSchema` (const), `MvSpcConfig` (type), `HotellingPoint` (interface), `MewmaConfig` (interface), `MewmaPoint` (interface), `MultivariateSPCEngine` (class), `multivariateSPCEngine` (const)

## Key methods
`getUcl`, `hotellingT2`, `hotellingStream`, `mewmaStream`, `fitFromReference`, `chiSquareCritical`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MultivariateSPCEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MultivariateSPCEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MultivariateSPCEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
