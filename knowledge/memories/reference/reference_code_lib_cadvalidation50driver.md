---
name: reference_code_lib_cadvalidation50driver
description: "cad-validation-50-driver.mjs -- U-VALIDATION-50-LIVE-RUN (slot:delta, CAD-COMPLETION T2)"
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/cad-validation-50-driver.mjs
  sourceHash: b1e6f1a5
  loc: 298
aliases: cad-validation-50-driver.mjs
---

# cad-validation-50-driver.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/cad-validation-50-driver.mjs` (298 LOC)

## What it is
```
cad-validation-50-driver.mjs -- U-VALIDATION-50-LIVE-RUN (slot:delta, CAD-COMPLETION T2)

Thin, importable driver for the CAD draw/regen validation harness
(CADDrawAnyPartValidationHarnessEngine). It loads the JM-Die validation
corpus, runs the harness against the selected orchestrator, and emits a
machine-readable JSON verdict + optional markdown.

WHY this exists (gate-integrity, R12): the T2 gate detector
(scripts/cad-completion-reconcile.mjs:136, artifactExists) flips T2
PENDING->SHIPPED on mere FILE EXISTENCE of a
state/shared/specs/cad-validation-50-*.json (content never read). So a naive
runner that writes that filename from a 12-case STUB run would FALSELY mark
T2 SHIPPED. This driver refuses to do that: it writes the gate-probed
filename ONLY when the run is genuinely gate-worthy (orchestrator==='live'
AND isFull (>= target cases) AND passedGate); otherwise it writes a
NON-probe
```


## Exports
`VALIDATION_TARGET_CASES` (const), `MILESTONE` (const), `UNIT` (const), `DEFAULT_GATE` (const), `SPECS_DIR` (const), `BASELINE_DIR` (const), `clampGate` (function), `parseArgs` (function), `decideArtifact` (function), `isGateProbedPath` (function), `inferDomain` (function), `summarizeDomains` (function), `buildResultJson` (function), `runValidation50` (function)

## Key methods
`runValidation50`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/cad-validation-50-driver.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find cad-validation-50-driver.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/cad-validation-50-driver.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
