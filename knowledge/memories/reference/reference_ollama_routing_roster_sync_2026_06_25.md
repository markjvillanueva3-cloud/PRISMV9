---
name: reference_ollama_routing_roster_sync_2026_06_25
description: "U-ALPHA-OLLAMA-ROSTER-SYNC — wedge-safe capability probe + restored dead `balanced` routing tier + the 55GB-32b VRAM-starvation root cause"
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.682Z
aliases: reference_ollama_routing_roster_sync_2026_06_25
---


**U-ALPHA-OLLAMA-ROSTER-SYNC** (slot:alpha, 2026-06-25). Operator /goal: "improve ollama offloading utilization... stress test ollama llms... then fix the graphs/protocols/pipelines/skills so we utilize local llms to max potential." Builds on yesterday's [[reference_ollama_stress_capability_2026_06_24]] (small-model frontier + concurrency knee + the wired per-request num_ctx fix).

**THREE concrete fixes shipped (evidence-backed, not assumption):**

1. **The capability-matrix generator was BLIND to 6 of 9 routing-relevant models.** `scripts/ollama-capability-probe.mjs` hardcoded `DEFAULT_MODELS = [qwen2.5-coder:1.5b, gpt-oss:20b, qwen2.5-coder:32b]` — so the nightly matrix (which `model-routing-policy.routePrompt` -> `model-tier-advisor.mjs` consumes to pick the offload model) never measured `qwen3-coder:30b` (the cost-router's PREFERRED coder, by code-comment claim only), the coder ladder (7b/14b), gpt-oss:120b, or either installed `deepseek-r1` reasoner. Expanded `DEFAULT_MODELS` to the live-installed text/code/reasoning roster (9 models, vision/embed excluded).

2. **The probe would WEDGE on that expanded roster — so I made it wedge-safe (R16, don't ship a new gap).** Added per-request `num_ctx=8192` (the proven KV-cache lever) + `unloadModel()` (keep_alive:0) called between models in `runProbe` (MODEL-OUTER: never co-reside big models). `CALL_TIMEOUT_MS` 45s->120s (cold-load tolerance). New `scripts/ollama-capability-probe.test.mjs` (6/6): unload-once-per-model-after-tasks, scoring via injected verify, best-effort-unload-non-fatal, NUM_CTX wedge-regression guard, DEFAULT_MODELS covers the decision roster.

3. **The cost-router `balanced` tier was DEAD — every balanced task escalated to gpt-oss:20b (13GB).** `.claude/hooks/lib/ollama-cost-router.mjs` `TIER_PREFERENCES.balanced` held ONLY non-installed tags (`qwen2.5:7b` / `codellama:7b` / `deepseek-coder:6.7b`) after a stale 2026-06-04 "qwen2.5-coder:7b RETIRED/deleted" note. But `qwen2.5-coder:7b` was RE-PULLED (verified live `/api/tags` 2026-06-25) and is yesterday's MEASURED mechanical sweet spot (100% classify/unit-convert/arithmetic at ~80-200 tok/s). Restored it as the FIRST balanced pick. Fixed the anti-revert test (`__tests__/ollama-cost-router.test.mjs` RETIRED list: removed 7b; kept :3b not-installed + :14b/r1:14b installed-but-not-preferred). 60/60 cost-router + 20/20 local-llm-task-router consumer tests green. **Net: the `balanced` tier was genuinely DEAD (zero installed models), so restoring coder:7b is correct regardless.** ACCURACY NUANCE (scrutiny arm B, R12): on `home_blackwell` itself the pre-existing `BLACKWELL_CEILING` still promotes `balanced -> strong -> gpt-oss:20b`, so the VRAM win lands on cheap-tier tasks + non-Blackwell hosts + the case where strong/best tiers are empty -- NOT every balanced task on this box. The dead-tier repopulation is the real, host-independent fix.

**THE REAL "MAX-POTENTIAL" BLOCKER (measured, robustly confirmed all session):** `qwen2.5-coder:32b` resides at **55GB VRAM** on the 96GB box — ~20GB weights + ~35GB KV cache at the global `OLLAMA_CONTEXT_LENGTH=131072`. The 26-slot fleet keeps it warm (re-loaded within seconds of every explicit unload), leaving only ~40GB headroom and causing chronic intermittent wedges under any added load (my own full-roster probe wedged exactly this way; `ollama-wedge-guard --recover` refused to restart a transiently-responsive daemon). **MEASURED CAVEAT (R12, corrects an earlier over-confident "num_ctx-everywhere" claim):** I tried a per-request `num_ctx=16384` to shrink the resident 32b and measured **54.2GB vs 54.7GB -- only 0.5GB / 1%**. The resident 131072 instance was REUSED (Ollama serves a smaller-num_ctx request on a larger LOADED model without reloading), so per-request num_ctx does NOT shrink an ALREADY-RESIDENT model. A clean cold-load-at-16384 test was BLOCKED by fleet contention (the fleet reloaded 32b within seconds of 3 explicit unloads -- inconclusive this session). So the lever requires BOTH (a) the model to LOAD FRESH at the small num_ctx AND (b) the prewarm + the real call to use the SAME num_ctx -- else the real call reloads at full size, defeating both. **I BUILT a `hermes-cron-prewarm` num_ctx fix (warmModel:104 sets num_predict but no num_ctx -> warms 32b at 55GB for 30m+) but did NOT ship it (reverted) -- the premise is unverified and a num_ctx mismatch with the real caller could defeat the prewarm.** Identified warm/prewarm callers missing num_ctx: `hermes-cron-prewarm.mjs:104`, `ollama-prewarm-on-pipeline.mjs`, `galaxy-reasoning-bridge.mjs` (DEFAULT_MODEL=32b). NEXT (clean-box, fleet-idle): (1) cold-load 32b at num_ctx 4096/16384/131072 and MEASURE each resident VRAM to confirm/refute the lever; (2) audit what num_ctx the real forge/rgs/galaxy-reasoning calls use; (3) if the lever holds AND real calls are small, fix the prewarm callers; else the real lever is the SERVER global `OLLAMA_CONTEXT_LENGTH` (operator tradeoff). Yesterday's c=8 wedge fix (per-request num_ctx=4096 on 7b) proves the MECHANISM reduces per-slot KV at load -- but that was CONCURRENCY (fresh slots), not a resident-model shrink, so it does not directly validate the prewarm case.

**HONEST DEFER (R12):** the rigorous `qwen3-coder:30b` vs `qwen2.5-coder:32b` codegen winner (and deepseek-r1 reasoning pick) was NOT resolved — the easy mechanical battery can't differentiate coding, and the hard `ollama-stress-expanded-run.mjs --include-codegen` run was blocked by the fleet VRAM contention above. QUEUED: run the codegen/reasoning batteries solo on a clean (fleet-idle) box; the router's "qwen3-coder:30b preferred over 32b" remains an unverified code-comment claim. Also noted-for-golf: the root CLAUDE.md TOKEN ECONOMY line ":7b/:14b tags were retired 2026-06-04" is now STALE (re-pulled) — golf-only edit.

**Lessons:** (1) a hardcoded model roster goes stale silently — the matrix generator must track installed reality; (2) READ the relevant tribal memory BEFORE launching a GPU sweep (I re-triggered yesterday's documented mixed-sweep wedge by not reading [[reference_ollama_stress_capability_2026_06_24]] first); (3) a "retired model" test fixture is DATA that rots — when a model is re-pulled, correct the fixture (R9: intent preserved, data updated), never weaken the guard.

---

**FOLLOW-UP SAME SESSION — U-ALPHA-OLLAMA-CHEAPEST-MODEL-SELECT (+ MOE-HARDEN)** (commits `c243f01414` + `2d4b7f4e72`, slot:alpha 2026-06-25, post-compact continuation). The R15 completion of the roster-sync: measuring 9 models only pays off if the routing policy then PICKS the smallest sufficient one. **The gap:** `scripts/lib/model-routing-policy.mjs::ollamaSafeClassModels` (the brain `model-tier-advisor.mjs` calls per prompt to choose WHICH local model a matrix-proven mechanical class offloads to) selected the offload model via `best = best || model` = **first-qualifying in matrix/roster iteration order** — correct ONLY by the coincidence that `DEFAULT_MODELS` lists the coder ladder small-first. A roster reorder (golf pulls/reorders) would silently route mechanical work to a needlessly-large model, wasting the exact VRAM the offload exists to save (the resident 32b's ~55GB footprint is the 96GB box's binding constraint — the "max-potential blocker" above). **Fix:** new pure exported `modelCostRank(modelTag)` parses the param-size suffix (`:1.5b`→1.5, `:32b`→32, `:120b`→120; reads the POST-colon segment so the `2.5` in `qwen2.5-coder` is never a size; `NxMb` MoE multiplier `mixtral:8x7b`→56; unparseable→Infinity→first-seen fallback = zero regression). `ollamaSafeClassModels` now picks **argmin cost among all-tasks-proven models** (proof still strictly gates membership BEFORE cost — a cheap-but-unproven model is never picked), stable first-seen tie-break. A cheaper proven model = same matrix-verified 100% quality, less VRAM, more concurrency. **Contract unchanged** (`{engine,model,tier,taskClass,reason}`); the 3 importers (`model-tier-advisor.mjs` live hook, `effort-tier-router.mjs`, `feature-routing-graph.mjs`) treat `v.model` opaquely — engine decision is invariant (can never move a task between ollama and Claude). **Live-validated NO-OP on the current 3-model matrix** (`extract→1.5b`, `format→32b` unchanged — the 3 are coincidentally cheap-first, so zero regression); value materializes when the 9-model matrix regenerates tonight + immediately for any cross-family class (e.g. `gpt-oss:20b` over `deepseek-r1:32b`) or future reorder. **36/36 policy** (big-first regression oracle + proof-gates-cost + MoE + tie + 7 `modelCostRank` unit tests — both reviewers B+C reconstructed the OLD code and confirmed the oracle FAILS on it) **+ 26/26 effort-tier consumer + live advisor smoke; 3-of-3 PASS** (all P2s closed in MOE-HARDEN: NxMb multiplier, a load-bearing slice-guard fixture `qwen2.5b-coder:32b`→32, docstring precision). **Lesson:** "pick the cheapest capable X" implemented as "pick the first in arbitrary order" is correct only by coincidence of ordering — make the cost ordering EXPLICIT, or a reorder silently regresses it (sibling of the roster-staleness lesson above). Flat-MoE caveat kept honest: `gpt-oss:20b` total≠active params, so the rank is a rough cross-arch proxy (exact within the dense coder ladder, which dominates mechanical offload).

---

**MEASURED LIVE VALIDATION (operator-authorized GPU run, 2026-06-25 ~22:30 local).** Operator chose "run the stress-test now anyway" at the fleet-coordination crossroad. TWO runs: (1) the FULL 9-model probe DIED mid-run under active-fleet contention (0-byte stdout, matrix unwritten; a peer was thrashing deepseek-r1:32b live) — **empirically reconfirms the env-block holds even WITH authorization: the active 6-peer fleet defeats clean sequential big-model probing.** (2) A wedge-safe SMALL-roster probe (`--models qwen2.5-coder:1.5b,7b,14b,gpt-oss:20b`, `--json` only — NOT `--out`, so the canonical matrix is not overwritten with a partial; foreground so reaper-safe) SUCCEEDED (exit 0, ≤14GB loads fit headroom without evicting peers). **Measured per-class proven-100% + cheapest pick:** `format` proven=[7b,14b] -> CHEAPEST **qwen2.5-coder:7b** (vs the old 3-model matrix's `format`->**32b** — an **11x VRAM cut**, 55GB->5GB, because 7b was absent from the old roster); `classify` proven=[14b] -> **qwen2.5-coder:14b** (vs old **(none proven)** — a NEW offload class unlocked); `extract` proven=[1.5b,7b,14b] -> **qwen2.5-coder:1.5b** (already optimal). **KEY FINDING: the entire mechanical-offload surface (classify/extract/format) is covered by models <=14GB; 32b/120b are NEVER the cheapest-proven pick** — empirically confirms routing mechanical work to the cheap tier (the cheapest-select fix) is correct + the big models stay reserved for codegen/reasoning. The cheapest picks are STABLE under adding the un-measured big models (nothing is smaller than 1.5b; bigger models can only prove MORE classes, never become cheaper). The win (format->7b, classify->14b) fires LIVE once the **nightly full-roster regen** (22:23 fleet-idle, wired via the coverage-guard job) refreshes the canonical matrix with the 7b/14b rows — deliberately NOT hand-merged into the canonical matrix this session (a partial overwrite would drop the big-model rows other consumers read; R8). This is the R15 VALIDATE-with-numbers completion of the cheapest-select unit.
