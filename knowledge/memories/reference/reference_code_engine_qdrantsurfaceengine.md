---
name: reference_code_engine_qdrantsurfaceengine
description: "TOOL-INVENTORY-MS0/U-TOOLINV-01"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/QdrantSurfaceEngine.ts
  sourceHash: f17e1657
  loc: 423
aliases: QdrantSurfaceEngine
---

# QdrantSurfaceEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/QdrantSurfaceEngine.ts` (423 LOC)

## What it is
TOOL-INVENTORY-MS0/U-TOOLINV-01

```
QdrantSurfaceEngine — TOOL-INVENTORY-MS0/U-TOOLINV-01
=====================================================

Adapter that exposes `QdrantMemoryEngine` via the standard `qdrant` MCP
server's tool surface (`vector_search` + `vector_upsert`). Lets external
Claude sessions or peer MCPs query our memory store without bespoke
wiring. Translates collection-string → MemoryKind enum, validates input
shape, and normalizes errors into a structured MCP error code envelope.

Why expose, not install:
  The Qdrant Labs `mcp-server-qdrant` and similar peers would duplicate
  the persistence layer our `QdrantMemoryEngine` already owns (collection
  layout, embedder, payload contract). Exposing our engine via the same
  tool schema lets us speak the standard wire format without forking the
  storage layer.

Naming + collision avoidance:
  Registered under the `prism-qdrant` MCP namespace (NOT plain `qdran
```


## Exports
`QDRANT_SURFACE_COLLECTION_PREFIX` (const), `QDRANT_SURFACE_MCP_NAME` (const), `QDRANT_SURFACE_MAX_LIMIT` (const), `QDRANT_SURFACE_DEFAULT_LIMIT` (const), `QDRANT_SURFACE_MAX_TEXT_CHARS` (const), `SurfaceErrorCode` (type), `SurfaceResult` (type), `SurfaceSearchInput` (interface), `SurfaceUpsertInput` (interface), `SurfaceHit` (interface), `SurfaceSearchValue` (interface), `SurfaceUpsertValue` (interface), `SurfaceEngineLike` (interface), `SurfaceDeps` (interface), `QdrantSurfaceEngine` (class)

## Key methods
`recall`, `remember`, `parseCollection`, `vectorSearch`, `vectorUpsert`, `listCollections`, `httpCodeFor`

## Depends on
[[reference_code_engine_qdrantmemoryengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/QdrantSurfaceEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find QdrantSurfaceEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/QdrantSurfaceEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
