---
name: reference_code_engine_reputablesourcemonitorengine
description: "AUTO-LEARNING-LOOP-MS0 / U-ALL01"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ReputableSourceMonitorEngine.ts
  sourceHash: 30968b1d
  loc: 861
aliases: ReputableSourceMonitorEngine
---

# ReputableSourceMonitorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ReputableSourceMonitorEngine.ts` (861 LOC)

## What it is
AUTO-LEARNING-LOOP-MS0 / U-ALL01

```
ReputableSourceMonitorEngine — AUTO-LEARNING-LOOP-MS0 / U-ALL01
================================================================

Multi-source external poller for reputable AI/ML/manufacturing news feeds.
Polls 10 RSS / Atom / JSON sources with conditional GET (ETag /
If-Modified-Since), exponential backoff on rate-limit (1m → 5m → 30m → 2h),
a 50 MB payload guard, redirect-cap, and fetch timeout.

Pure data engine — emits {@link PollResult} per source; persistence
(JSONL log, cron registration) lives in `scripts/source-monitor-sweep.mjs`
(U-ALL01 step-3). Dispatcher wiring is U-ALL01 step-5
(`prism_dev:source_sweep`).

// CONVENTION-EXEMPT: stateful poller. This engine carries per-source state
// (etag, lastModified, backoffUntil, consecutiveFailures) that cannot be
// expressed as static methods + AtomicValue. State is per-instance for
// hermetic test isolation; a singleton is exporte
```


## Exports
`SourceType` (type), `SourceConfig` (interface), `SourceItem` (interface), `PollStatus` (type), `SourceState` (interface), `PollResult` (interface), `PollAllResult` (interface), `DEFAULT_SOURCES` (const), `BACKOFF_SCHEDULE_MS` (const), `MAX_PAYLOAD_BYTES` (const), `FETCH_TIMEOUT_MS` (const), `ALARM_FAILURE_THRESHOLD` (const), `EngineOpts` (interface), `ReputableSourceMonitorEngine` (class), `reputableSourceMonitorEngine` (const)

## Key methods
`getSources`, `setSources`, `resetAll`, `getState`, `resetState`, `poll`, `pollAll`, `clearTimeout`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ReputableSourceMonitorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ReputableSourceMonitorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ReputableSourceMonitorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
