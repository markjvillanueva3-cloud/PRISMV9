---
name: reference_code_engine_pipelineirexecutorengine
description: "PipelineIRExecutorEngine -- PIPELINE-IR-MS0 / U-PIR03 (slot:bravo)."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PipelineIRExecutorEngine.ts
  sourceHash: e0c1cab5
  loc: 139
aliases: PipelineIRExecutorEngine
---

# PipelineIRExecutorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PipelineIRExecutorEngine.ts` (139 LOC)

## What it is
PipelineIRExecutorEngine -- PIPELINE-IR-MS0 / U-PIR03 (slot:bravo).

```
PipelineIRExecutorEngine -- PIPELINE-IR-MS0 / U-PIR03 (slot:bravo).

Executes a declarative PipelineIR: converts+validates (via PipelineIRConverterEngine),
walks stages in topological order, resolves each stage's params (literals + refs into
prior stage outputs by dotted path), evaluates an optional `condition` gate, invokes
`dispatcher:action`, and honors the `onError` policy (abort | continue | retry).

The dispatcher INVOKER is INJECTED -- `execute(ir, invoke, opts)` -- so the engine is
pure + testable without the live MCP layer. The current dispatcher wiring
(prism_orchestrate:execute_ir_pipeline) injects a DRY-RUN invoker that only RECORDS
the {dispatcher,action,params} each stage would invoke (zero actuation); live
cross-dispatcher routing is governance-gated (unsafe-fleet-control) and not yet
wired. Tests supply a deterministic mock invoker.

No physics, no direct I/O (all side ef
```


## Exports
`StageInvoker` (type), `StageOutcome` (interface), `ExecuteResult` (type), `resolvePath` (function), `PipelineIRExecutorEngine` (class), `pipelineIRExecutorEngine` (const)

## Key methods
`execute`

## Depends on
[[reference_code_engine_pipelineirconverterengine]] | [[reference_code_schema_pipelineir]]

## Where it lives
- **Source:** `mcp-server/src/engines/PipelineIRExecutorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PipelineIRExecutorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PipelineIRExecutorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
