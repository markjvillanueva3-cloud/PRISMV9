---
name: reference_code_engine_lathequalitygateengine
description: "LatheQualityGateEngine — PhD-Level Quality Validation for Lathe Programs"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LatheQualityGateEngine.ts
  sourceHash: b475c51e
  loc: 2463
aliases: latheQualityGateEngine
---

# latheQualityGateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LatheQualityGateEngine.ts` (2463 LOC)

> [physics] Imports canonical physics constants (`src/physics/constants.ts`) -- safety-relevant node.

## What it is
```
LatheQualityGateEngine — PhD-Level Quality Validation for Lathe Programs
==========================================================================

This engine provides expert-level scrutiny at every stage of the lathe
programming pipeline. It validates programs against safety, physics,
machining science, and shop-specific requirements.

Quality Gates:
  1. SAFETY GATE (Critical) — Collision-free, spindle limits, program end
  2. PARAMETER GATE — ISO material limits, surface finish achievability
  3. SEQUENCE GATE — Logical operation order, datum maintenance
  4. PHYSICS GATE — Kienzle force, Taylor life, deflection, thermal
  5. QUALITY GATE — Surface finish prediction, tolerance achievability
  6. SHOP GATE — JM Die standards, customer preferences, machine capability

Scoring System:
  - 0-100 per gate, weighted aggregate score
  - Pass (>80), Warn (60-80), Fail (<60) thresholds
  - D
```


## Exports
`QualityGateMaterial` (interface), `QualityGateCuttingParams` (interface), `QualityGateTool` (interface), `LatheOperationType` (type), `QualityGateOperation` (interface), `QualityGateMachine` (interface), `QualityGateWorkholding` (interface), `QualityGatePart` (interface), `ValidationContext` (interface), `ValidationCheck` (interface), `GateReport` (interface), `SafetyReport` (interface), `ParamReport` (interface), `SequenceReport` (interface), `PhysicsReport` (interface), `QualityReport` (interface), `ShopReport` (interface), `FullValidationReport` (interface), `Recommendation` (interface), `latheQualityGateEngine` (const)

## Key methods
`validateProgram`, `validateSafety`, `validateParameters`, `validateSequence`, `validatePhysics`, `validateQuality`, `validateShop`, `getOverallScore`, `getRecommendations`, `quickSafetyCheck`, `getSpeedRange`, `getFeedRange`, `getDocRange`, `calculateRequiredNoseRadius`, `calculateMaxFeedForRa`, `getGateWeights`, `getJMDieStandards`, `getSurfaceFinishLimits`, `getVersion`

## Depends on
[[reference_code_util_logger]] | [[reference_code_engine_omegasafetyscoreengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/LatheQualityGateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find latheQualityGateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LatheQualityGateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
