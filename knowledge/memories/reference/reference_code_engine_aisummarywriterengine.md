---
name: reference_code_engine_aisummarywriterengine
description: "AISummaryWriterEngine (G6) — natural-language summary layer."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/AISummaryWriterEngine.ts
  sourceHash: d3a071da
  loc: 451
aliases: aiSummaryWriterEngine
---

# aiSummaryWriterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/AISummaryWriterEngine.ts` (451 LOC)

## What it is
```
AISummaryWriterEngine (G6) — natural-language summary layer.

Consumes per-cadence metrics (daily/weekly/monthly) and produces a draft
narrative summary that lands in the G5 admin-approval queue. Admin reviews,
may edit, then approves — only THEN is the summary published downstream
(employee mobile portal digest / manager dashboard / executive rollup).

R8 read-before-write: deterministic per-cadence metric engines already exist
(EmployeeDailyDigestEngine, ManagerDailyDashboardEngine, ExecutiveSummaryEngine).
This engine is the LLM-narrative layer on top — it does not duplicate any
metric calculation.

Cadence (operator green-light 2026-05-26): all 3
  • daily   — per-employee end-of-shift summary
  • weekly  — per-department rollup
  • monthly — per-customer relationship summary (delivery + quality + spend)

Narrative authoring strategy (token-budget aware):
  This engine produces a DET
```


## Exports
`SummaryCadence` (type), `SummarySubjectKind` (type), `DailyEmployeeMetrics` (interface), `WeeklyDepartmentMetrics` (interface), `MonthlyCustomerMetrics` (interface), `DraftSummary` (interface), `SystemVizRoost` (interface), `aiSummaryWriterEngine` (const), `allowedCadences` (function), `allowedSubjectKinds` (function)

## Key methods
`buildDailyEmployeeSummary`, `buildWeeklyDepartmentSummary`, `buildMonthlyCustomerSummary`, `submitDraft`, `classifyCpk`, `systemVizRoost`, `reset`

## Depends on
[[reference_code_engine_aiproposalapprovalqueueengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/AISummaryWriterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find aiSummaryWriterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/AISummaryWriterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
