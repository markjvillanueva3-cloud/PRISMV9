---
name: reference_code_script_sfcvariabilitycache
description: "SFC-ACCURACY-MS1 Stage 6 — Runtime variability cache."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/sfc-variability-cache.mjs
  sourceHash: 1c6426bc
  loc: 301
aliases: SfcVariabilityCache
---

# SfcVariabilityCache -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/sfc-variability-cache.mjs` (301 LOC)

## What it is
```
SFC-ACCURACY-MS1 Stage 6 — Runtime variability cache.

Loads chunked JSONL result files from
state/shared/sfc-variability-results/<domain>/ into an in-memory
fingerprint→result map. Used by the dispatcher's sf_orchestrate path
to skip orchestrator.compute() on cache hits.

Usage (dispatcher integration):

  import { sfcVariabilityCache } from "scripts/sfc-variability-cache.mjs";
  const cached = await sfcVariabilityCache.lookup(input);
  if (cached) return cached;  // ~0ms cache hit
  const result = orchestrator.compute(input);
  await sfcVariabilityCache.upsert(input, result);  // populate for next time
  return result;

Performance:
  - Cold load: ~50ms per 5K-cell chunk (parse JSONL + index)
  - Lookup:    ~0.05ms (Map.get on fingerprint)
  - Upsert:    appended to current chunk file, in-memory map updated

Match policy:
  - Exact-fingerprint hit: return slim cached output directly
  
```


## Exports
`SfcVariabilityCache` (re-export), `getCache` (re-export), `fingerprintSlim` (re-export), `slimInput` (re-export), `slimifyOutput` (re-export)

## Key methods
`has`, `ensureLoaded`, `lookup`, `upsert`, `getStats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/sfc-variability-cache.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find SfcVariabilityCache` or `prism_session:master_index_query`; open the source at `scripts/sfc-variability-cache.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
