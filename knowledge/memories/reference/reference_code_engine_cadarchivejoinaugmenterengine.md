---
name: reference_code_engine_cadarchivejoinaugmenterengine
description: "MS-PRINT-PROGRAM-LOOP / U-PPL-D4 (Track D)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADArchiveJoinAugmenterEngine.ts
  sourceHash: affd1ca5
  loc: 624
aliases: CADArchiveJoinAugmenterEngine
---

# CADArchiveJoinAugmenterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADArchiveJoinAugmenterEngine.ts` (624 LOC)

## What it is
MS-PRINT-PROGRAM-LOOP / U-PPL-D4 (Track D)

```
CADArchiveJoinAugmenterEngine — MS-PRINT-PROGRAM-LOOP / U-PPL-D4 (Track D)

Bridges the CAD-archive master-index (`CADFileIndexerEngine` output —
`data/state/cad-file-index/master-index.json`) into the print↔program v6
join (`BlueprintProgramJoinEngine` / `ProgramPrintLinkIndexEngine.JoinIndex`)
so the join stops missing CAD-side hits (the envelope brief notes only 38
print→CAM-project hits today — that's because the v6 producer was built
from the .MIN/.mcx program corpus and CAD files were never seeded into the
augmentation pipeline).

Why this engine exists (the JM-Die tribal rule):
  For mill jobs, Mazak/Okuma lathe saves `.MIN` with a `$<INTERNAL>%`
  line-1 header, but Inventor / Fusion / SolidWorks mill saves NO G-code —
  the `.ipt` / `.iam` / `.f3d` / `.SLDPRT` IS the program (G-code goes
  USB → discard). So for matching prints to mill jobs you MUST treat those
  CAD files as pr
```


## Exports
`MILL_PROGRAM_FORMATS` (const), `CADAugmentedLink` (interface), `AugmentStats` (interface), `AugmentResult` (interface), `FilterOptions` (interface), `LoadAndAugmentOptions` (interface), `filterMillEligibleEntries` (function), `augmentJoinFromCADIndex` (function), `CADArchiveJoinAugmenterEngine` (class), `cadArchiveJoinAugmenterEngine` (const)

## Key methods
`getCapabilities`, `validate`, `loadAndAugment`

## Depends on
[[reference_code_engine_baseengine]] | [[reference_code_engine_iengine]] | [[reference_code_engine_programprintlinkindexengine]] | [[reference_code_engine_blueprintprogramjoinengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADArchiveJoinAugmenterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADArchiveJoinAugmenterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADArchiveJoinAugmenterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
