---
name: reference_code_engine_edmmultipassstrategyengine
description: "Multi-Pass (Skim Cut) Strategy Planning"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/EDMMultiPassStrategyEngine.ts
  sourceHash: 39619840
  loc: 422
aliases: EDMMultiPassStrategyEngine
---

# EDMMultiPassStrategyEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/EDMMultiPassStrategyEngine.ts` (422 LOC)

## What it is
Multi-Pass (Skim Cut) Strategy Planning

```
EDMMultiPassStrategyEngine — Multi-Pass (Skim Cut) Strategy Planning

Plans WEDM multi-pass strategies for progressive die applications:
  1. Determines optimal pass count based on finish requirements
  2. Computes wire offsets for each pass
  3. Estimates cycle time per pass
  4. Predicts final surface finish and recast depth

Physics Model — Multi-Pass Strategy
------------------------------------
WEDM achieves final dimension through successive passes:
  - Rough: Maximum MRR, largest offset, highest energy
  - Semi: Intermediate passes reducing stock and recast
  - Finish: Final passes for surface quality

Each pass removes material and recast layer:
  d_recast(n) ≈ d_recast(n-1) × (1 - η_skim)

where η_skim ≈ 0.30 (30% recast reduction per skim pass)

Wire offset per pass must maintain:
  offset(n) > offset(n+1) + min_step
  offset(final) = 0 (on-size)

MS-P3-TIER6A/U-P3-T6A-02

@see
```


## Exports
`PassDefinition` (interface), `MultiPassInput` (interface), `PassTimeEstimate` (interface), `MultiPassResult` (interface), `EDMMultiPassStrategyEngine` (class), `edmMultiPassStrategyEngine` (const)

## Key methods
`plan`, `determineFinishClass`, `determinePassCount`, `buildPassSequence`, `getPassType`, `interpolateOffset`, `estimatePassTimes`, `getPassesForClass`, `estimateCycleTime`, `calculateRecastAfterPasses`, `passesForTargetRecast`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/EDMMultiPassStrategyEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find EDMMultiPassStrategyEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/EDMMultiPassStrategyEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
