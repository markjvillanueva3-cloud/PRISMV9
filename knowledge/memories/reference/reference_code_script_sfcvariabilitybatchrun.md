---
name: reference_code_script_sfcvariabilitybatchrun
description: "SFC-ACCURACY-MS1 Stage 2 — Streaming batch worker."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/sfc-variability-batch-run.mjs
  sourceHash: 2c9303f1
  loc: 442
aliases: sfc-variability-batch-run.mjs
---

# sfc-variability-batch-run.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/sfc-variability-batch-run.mjs` (442 LOC)

## What it is
```
SFC-ACCURACY-MS1 Stage 2 — Streaming batch worker.

Reads the Stage 1 enumerator's combo stream, drives
SpeedFeedOrchestratorEngine.compute() on each cell, and persists
(input fingerprint, output, safety status) to chunked JSONL files
under state/shared/sfc-variability-results/<domain>/.

Operator directive: "generate the billions". This worker is the
streaming pump — enumerator emits ~600K/s, orchestrator processes
~5 ms/cell, so a single worker sustains ~200 cells/sec. We run multiple
workers in parallel (--workers N) and round-robin assign by combo index.

Modes:
  --domain mill|lathe   (which enumerator to use, default mill;
                         lathe enumerator built separately as Stage 1B)
  --max-minutes N       (wall-clock cap; default 60)
  --max-cells N         (output count cap; default Infinity)
  --chunk N             (rows per output chunk file; default 5000)
  --worker
```


## Exports
`runWorker` (re-export), `runOrchestrator` (re-export), `slimInput` (re-export), `slimOutput` (re-export), `loadEnumerator` (re-export), `fingerprint` (re-export)

## Key methods
`res`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/sfc-variability-batch-run.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find sfc-variability-batch-run.mjs` or `prism_session:master_index_query`; open the source at `scripts/sfc-variability-batch-run.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
