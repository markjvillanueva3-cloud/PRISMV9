---
name: reference_code_engine_autoresearchorchestratorengine
description: "AUTO-LEARNING-LOOP-MS0 / U-ALL03"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/AutoResearchOrchestratorEngine.ts
  sourceHash: 6f4074fc
  loc: 985
aliases: AutoResearchOrchestratorEngine
---

# AutoResearchOrchestratorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/AutoResearchOrchestratorEngine.ts` (985 LOC)

## What it is
AUTO-LEARNING-LOOP-MS0 / U-ALL03

```
AutoResearchOrchestratorEngine — AUTO-LEARNING-LOOP-MS0 / U-ALL03
=================================================================

Sits between U-ALL02 `NoveltyDetectionEngine` (filters known-from-novel)
and U-ALL04 `SynergyClassifierEngine` (scores research output for PRISM
fit). Its only job is to **rate-limit** the dispatch of researcher
subagents so that a 200-item RSS burst cannot turn into a token bomb
on the user's account.

Two limits, both hard:
  1. **Concurrency** — at most `maxConcurrent` (default 3) researcher
     subagents are in flight at any one wall-clock instant. The rest
     sit in the pending queue and wake up on the next `flush()` call.
  2. **Daily budget** — at most `dailyBudget` (default 12) dispatch
     *attempts* per UTC day. **Each attempt counts** — a timeout
     or crash followed by a retry consumes two of the day's slots,
     not one. This is intentio
```


## Exports
`ResearchOutcome` (type), `DispatchOutcome` (type), `SanitizedItem` (interface), `DispatchFn` (type), `QueueEntry` (interface), `QueueState` (interface), `CompletedRecord` (interface), `DeadLetterEntry` (interface), `EnqueueOutcome` (interface), `FlushOutcome` (interface), `LoadOutcome` (interface), `EngineOpts` (interface), `QUEUE_SCHEMA_VERSION` (const), `DEFAULT_MAX_CONCURRENT` (const), `DEFAULT_DAILY_BUDGET` (const), `DEFAULT_TIMEOUT_MS` (const), `DEFAULT_MAX_ATTEMPTS` (const), `DEFAULT_QUEUE_TTL_MS` (const), `DEFAULT_COMPLETED_DEDUP_MS` (const), `DEFAULT_MAX_PROMPT_CHARS` (const)

## Key methods
`setDispatch`, `isDispatchConfigured`, `getQueueState`, `loadState`, `loadJson`, `resetAll`, `getDailyUsage`, `getStats`, `enqueue`, `flush`, `resolve`

## Depends on
[[reference_code_engine_reputablesourcemonitorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/AutoResearchOrchestratorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find AutoResearchOrchestratorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/AutoResearchOrchestratorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
