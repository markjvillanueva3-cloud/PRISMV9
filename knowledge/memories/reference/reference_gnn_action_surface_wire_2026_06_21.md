---
name: reference_gnn_action_surface_wire_2026_06_21
description: "SHIPPED [AI-SYSTEMS-GNN]/U-GNN-ACTION-SURFACE-WIRE (slot:india, commit 0672129dd1, 2026-06-21): wired the action-surface dense feature (U-ENGINE-ACTION-SURFACE) into build-node-embeddings.mjs ghost source-signal, DEFAULT-OFF behind PRISM_NNG_GHOST_ACTION_SURFACE=1. New pure exported helper appendActionSurface(sig,surfText,{maxChars=1400}) appends an engine's dispatcher ACTION names (the vocab the description prose lacks) to its leak-free engineSourceSignal, in the --ghosts-only sourceSignalById builder. KEY ARCHITECTURE INSIGHT (resolved a wrong first assumption): the SAME --ghosts-only path embeds BOTH the codebase-WIRED refs (measure-codebase-wired-refpool-auroc.mjs writes them as ghost-kind nodes + spawns build-node-embeddings) AND the truly-unwired deployed-355 ghosts; actionSurfaceText returns '' for an unwired engine, so the wire auto-splits: wired refs (real .ts, real backing actions) GAIN the dispatcher-class vocab, unwired ghosts stay byte-identical (leak-free no-op, the anti fake-0.98 contract). LIVE GLUE SMOKE (2 real wired engines A2AProtocolEngine+AbrasiveJetMachiningEngine): OFF applied=0 byte-identical, ON applied=2, BOTH resume-hashes CHANGED OFF->ON (enriched text reached the vector), ollama=2 failed=0. 53/53 tests (+7 appendActionSurface: happy+3 failure+2 adversarial+cap). Per-file 2-arm + Stop 3-of-3 ALL PASS, 0 P0/P1. R12 SCOPE: does NOT retrain, does NOT flip the deployed default, does NOT mutate the 542MB graph. NEXT: the live in-deployed-format separability/AUROC re-measure (re-embed the codebase-wired refpool with the flag ON to a SEPARATE out, classSeparability vs 0.0263 baseline, WITHOUT clobbering .cwref-newemb.jsonl) -- and per the measure verdict do NOT GPU-retrain on this single modest feature alone."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.588Z
aliases: reference_gnn_action_surface_wire_2026_06_21
---


**CONTEXT:** slot:india autonomous /loop 2026-06-21, post-compact (R6 -- fresh window IS the reset I was holding for). GAP#1 GNN node-feature enrichment, the leg #10 coverage lever. R13 logical order: U-LORA-WIKI-DOMAIN (corpus) -> U-ENGINE-ACTION-SURFACE (feature core) -> U-ACTION-SURFACE-SEPARABILITY-MEASURE (go/no-go verdict) -> **THIS unit: the pipeline WIRE** (commit 0672129dd1).

**SHIPPED:** `scripts/build-node-embeddings.mjs` +56 / `.test.mjs` +50.
- New pure exported `appendActionSurface(sig, surfText, {maxChars=1400})` -- appends `"actions: <surf>"` to the engine source signal; defensive `prism_*` strip + `\s+` collapse + cap; returns `sig` UNCHANGED for empty/whitespace/non-string surf (the leak-free no-op).
- `GHOST_ACTION_SURFACE = process.env.PRISM_NNG_GHOST_ACTION_SURFACE === "1"` flag (default OFF -> deployed embeddings byte-identical), `DISPATCHERS_DIR`, `actionSurfaceApplied` stat + report line.
- Wired into the `sourceSignalById` builder: `buildActionSurfaceMap(DISPATCHERS_DIR)` built ONCE behind the flag; per node `actionSurfaceText(map, name.toLowerCase())` appended to the resolved `engineSourceSignal`.

**THE ARCHITECTURE INSIGHT (corrected a wrong first read -- R8 paid off):** I first feared wiring into `--ghosts-only` was a no-op (ghosts are unwired -> empty surface). Reading `measure-codebase-wired-refpool-auroc.mjs` resolved it: it writes the ~3206 codebase-WIRED refs as ghost-kind nodes into a temp graph and embeds them via the SAME `build-node-embeddings --ghosts-only` path (the labeled set behind the eval's 0.0263 baseline + `.cwref-newemb.jsonl`). So the one wire serves BOTH populations and the empty-for-unwired contract auto-splits them: **wired refs gain signal, unwired ghosts stay byte-identical.** The benefit is on the refs' sharper class structure, generalized to ghosts via GraphSAGE message-passing -- never the ghost's own label.

**LIVE VALIDATION (R15, real numbers):** 2-engine glue smoke (A2AProtocolEngine, AbrasiveJetMachiningEngine -- both have a .ts + an action surface): OFF `applied=0` (byte-identical), ON `applied=2`, BOTH per-row resume-hashes CHANGED OFF->ON (proves the enriched text reached the embedded vector), `ollama=2 failed=0`. build-node-embeddings OOMs at default heap when run directly -- needs `node --max-old-space-size=8192` (the measure harness always spawns it with that; not my bug -- sibling of reference_ai_systems_6unit_complete_2026_06_11).

**GATES:** 53/53 tests (+7 new). Per-file 2-arm (reviewer + code-analyzer) PASS 0 P0/P1. Stop 3-of-3 (A holistic + B test/wiring + C analyst) ALL PASS, only 2 non-blocking P2s -- (1) lookup keys off `n.label` not `engineStemFromNodeId`; harmless (a resolved .ts path already implies a real handle) but read a low `applied=N` as coverage not breakage in the re-measure; (2) co-review `engine-action-surface.mjs` before ever flipping the default ON.

**R12 SCOPE (honest):** wires the feature default-OFF + proves the glue end-to-end. Does NOT retrain, does NOT flip the deployed default, does NOT mutate the 542MB graph, does NOT clobber `.cwref-newemb.jsonl`.

**NEXT UNIT (fresh context):** the live in-deployed-format separability re-measure -- build the codebase-wired ghosts, `build-node-embeddings --ghosts-only` with `PRISM_NNG_GHOST_ACTION_SURFACE=1` to a SEPARATE `--out` (NOT the canonical `.cwref-newemb.jsonl`; india soul: never clobber the deployed/baseline store), then `analyze-ghost-embed-separability.mjs --emb <enriched>` vs the 0.0263 baseline. REPORT real numbers. Per the measure verdict ([[reference_action_surface_separability_measure_2026_06_21]]): action-surface is REAL-but-MODEST -- do NOT GPU-retrain on it ALONE; stack 1-2 more dense features (wiki-section text / AST call-graph) and re-measure (meanMargin needs ~2x to clear 0.05) BEFORE any GPU/H2GCN retrain (multi-seed gate, [[feedback_multiseed_before_auroc_claim]]).

**SIBLINGS:** [[reference_engine_action_surface_2026_06_21]] · [[reference_action_surface_separability_measure_2026_06_21]] · [[reference_lora_wiki_domain_feeder_2026_06_21]] · [[reference_gnn_embed_separability_diagnostic_2026_06_21]] · [[feedback_multiseed_before_auroc_claim]].
