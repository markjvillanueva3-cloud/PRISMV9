---
name: reference_tango_dispatcher_register_ghost_2026_06_15
description: tango registered 5 dormant dispatchers in index.ts (153 dead MCP actions) + built ghost nodes/wirings via regen-viz. slot tango 2026-06-15 (operator "wire all unwired + build ghosts").
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.219Z
aliases: reference_tango_dispatcher_register_ghost_2026_06_15
---


**TANGO WIRE-UNWIRED + GHOST-BUILD (slot tango, 2026-06-15)** under operator "complete all remaining work, wire all unwired nodes, build ghost nodes and ghost wirings. use crons, harnessed loops, hermes agents, parallel agents, ultracode, obsidian vault."

**COORDINATION FIRST (multi-chat R7/R8):** papa was ALREADY aggressively wiring the 45-engine unwired set (`[WIRE-UNWIRED-PAPA]` iter11+: MITCourse, CATIAAddin, CreoRibbon, SwissType, TurretLayout, Subprogram, SyncCode, TPE, Attractor, ERP, WetRun...). Wiring engines myself would COLLIDE (double-wire -> duplicate dispatcher actions -> fleet build break). So tango took the DISJOINT parts papa was NOT doing (announced on chat bus): (1) whole-dispatcher registration in index.ts, (2) ghost nodes/wirings. papa keeps engine->dispatcher wiring.

**DISPATCHER REGISTRATION (commit `4734d6bd85`) — 153 dead MCP actions activated.** 5 dispatchers had FILES present but ZERO index.ts registration (verified via a 5-agent [SCOPED] Workflow `wf_13210a62`, all SAFE): `mlDispatcher`(prism_ml, 100 actions), `localDispatcher`(prism_local, 20), `resourceExtractionDispatcher`(prism_resource_extraction, 14) = pure omissions; `agentDispatcher`(prism_agent, 11) + `resourceHarvestingDispatcher`(prism_resource_harvesting, 8) = STALE "NOT ON THIS BRANCH" comments (commit `0a3f083f0a2` 2026-04-26 commented them when the files were MISSING; files since restored, comment never cleared -- same pattern echo fixed for ppDispatcher in U-PP-DISPATCHER-REGISTER 2026-06-10). Each verified: export exists, schema imports resolve on disk, tool name unique (no dup), `(server)` signature matches 40+ existing calls, why-unregistered = omission/stale (NOT deliberate disable). Applied 2 uncomment-in-place + 3 new import+register pairs. **VALIDATED: `build:fast` PASS + `tsc --noEmit` 0 errors** (the pre-existing ~18-error backlog was cleared by peers since 2026-06-14). NOTE (R12): the 153 actions become LIVE on the next MCP server RESTART -- the running :3100 server uses the pre-edit build; could not live round-trip-test without restarting the fleet's shared server. **Line numbers are tree-specific** -- the verify agents ran in slot-tango (~1900 commits behind); I re-found exact anchors in cad-fusion-live-ms0 before editing (agent's L232/833 != my L145/186/237/752/791).

**GHOST NODES + WIRINGS (built via regen-viz).** `seed-ghost-from-unwired.mjs` is a 4th DIRECT writer of the 727MB system-graph, **safe ONLY under regen-viz's lock** (WIRE-NOTE) -- so I did NOT run it standalone (corruption risk on shared fleet-search infra). Ran one default `regen-viz` (485s; seed-ghost is its post-merge stage) after verifying no other writer running + flagging sierra. Result: graph rewritten + INTACT (streamed-verified: 727MB, valid JSON start `{`/end `}`, not truncated; ghost.unwired refs 1162->1172, fresh wirings from the 2026-06-15 audit). The `JSON.parse` "string longer than 0x1fffffe8" is V8's 512MB string cap NOT corruption -- must stream-verify graphs >512MB. **CAVEAT (R12):** regen-viz exited `failed=1/driftFail=true` -- the post-write `detect-system-viz-drift` gate V8-OOM'd on the 727MB graph (can't load it), so it could NOT certify integrity; I certified independently by streaming. Surfaced to sierra: the drift-gate is a meta-tool that can't handle a 727MB graph (silent integrity-cert gap; needs a streaming detector).

**COVERAGE (ALL MEANS ALL):** dispatchers 5/5 done (all that exist unregistered). Engines: 45->37 unwired and DROPPING in real-time (papa wiring + my date-stamp audit fix now tracks current reality). Ghosts: built. Report: `state/shared/specs/TANGO-DISCOVERY-SWEEP-2026-06-15.md`. Sister: [[reference_tango_discovery_sweep_2026_06_15]], [[reference_unwired_ranker_consumer_fanin_2026_06_15]]. Wiki: [[unwired-ranker-consumer-fanin]].
