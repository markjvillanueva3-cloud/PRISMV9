---
name: reference_code_engine_crossprocessrewardshaperengine
description: "XPROC-NEURAL Tier 4 (T4-01)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CrossProcessRewardShaperEngine.ts
  sourceHash: a8cdac66
  loc: 392
aliases: CrossProcessRewardShaperEngine
---

# CrossProcessRewardShaperEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CrossProcessRewardShaperEngine.ts` (392 LOC)

## What it is
XPROC-NEURAL Tier 4 (T4-01)

```
CrossProcessRewardShaperEngine — XPROC-NEURAL Tier 4 (T4-01)

Maps a CrossProcessOutcomeEvent (or any equivalent shop-floor outcome
tuple) → scalar reward used by T4-02 (PolicyGradient), T4-03 (QLearning),
and T4-04 (Bandit). The reward function is the *contract* the RL stack
optimizes against — getting the components and weights right is the
single most important RL design decision (Sutton & Barto §17.4).

Reward components (per XPROC-NEURAL-ROADMAP.md Tier 4 R1):
  1. surface_finish_error  — Ra_actual / Ra_target − 1, NEGATIVE
     (we want Ra ≤ target; positive means under-spec; clipped to ≥−2)
  2. tool_life_delta       — (life_actual − life_predicted) / life_predicted
     POSITIVE when actual outlived prediction (good); clipped to [−1, 1]
  3. cycle_time_delta      — (time_predicted − time_actual) / time_predicted
     POSITIVE when faster than predicted; clipped to [−1, 1]
  4. sa
```


## Exports
`OutcomeEvent` (interface), `RewardWeights` (interface), `ComponentBreakdown` (interface), `ShapeOk` (interface), `AuditOk` (interface), `OpErr` (interface), `ShapeResult` (type), `AuditResult` (type), `CrossProcessRewardShaperEngine` (class), `crossProcessRewardShaperEngine` (const), `crossProcessRewardShaper` (function)

## Key methods
`shape`, `audit`, `defaultWeights`, `constants`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CrossProcessRewardShaperEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CrossProcessRewardShaperEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CrossProcessRewardShaperEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
