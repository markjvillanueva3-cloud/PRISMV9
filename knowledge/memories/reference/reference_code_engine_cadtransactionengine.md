---
name: reference_code_engine_cadtransactionengine
description: "CAD-COMPLETE-MS0 / U-AI-08"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADTransactionEngine.ts
  sourceHash: b686aaff
  loc: 514
aliases: CADTransactionEngine
---

# CADTransactionEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADTransactionEngine.ts` (514 LOC)

## What it is
CAD-COMPLETE-MS0 / U-AI-08

```
CADTransactionEngine — CAD-COMPLETE-MS0 / U-AI-08
==================================================

Atomic transaction wrapper over `CADWorldModelEngine`. Composes the
world-model's `getOrCreate()` + `applyOp()` + `restore()` primitives
into ACID-style begin / apply / commit / rollback semantics so the CAD
agent can group multiple operations into a single, all-or-nothing unit
of work — and rewind the believed state if any op fails.

The world model already guarantees that a single invalid op THROWS
rather than silently corrupting state, but a single bad op mid-sequence
(e.g. a 6-op extrude+pattern sequence whose 4th op references an entity
the prior op failed to create) would otherwise leave the world model
partially advanced. The transaction engine snapshots the pre-transaction
`CADWorldState` at `begin()`, and either replays the snapshot
(`rollback()`) or finalizes the diff (`commit(
```


## Exports
`TxnState` (type), `CADTransaction` (interface), `TxnApplyResult` (interface), `TxnCommitResult` (interface), `TxnRollbackResult` (interface), `TxnStatus` (interface), `TxnApplyAllResult` (interface), `CADWorldModelLike` (interface), `CADTransactionEngineOptions` (interface), `CADTransactionEngine` (class), `cadTransactionEngine` (const)

## Key methods
`getOrCreate`, `applyOp`, `restore`, `begin`, `apply`, `commit`, `rollback`, `status`, `list`, `applyAll`, `reset`

## Depends on
[[reference_code_engine_cadworldmodelengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADTransactionEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADTransactionEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADTransactionEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
