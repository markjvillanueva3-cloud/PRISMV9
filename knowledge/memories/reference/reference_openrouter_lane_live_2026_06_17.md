---
name: reference_openrouter_lane_live_2026_06_17
description: OpenRouter cloud lane is LIVE -- key in gitignored .env (NOT in repo), nemotron-3-super-120b:free confirmed responding at $0/1M-ctx; the cloud overflow rung of the Ollama->cloud->Sonnet->Opus ladder
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.685Z
aliases: reference_openrouter_lane_live_2026_06_17
---


OpenRouter cloud lane went LIVE 2026-06-17 (slot:alpha). The operator provided `OPENROUTER_API_KEY` (a `sk-or-v1-...` 73-char key); it is stored in the **gitignored** `.env` AND `mcp-server/.env` (both confirmed `git check-ignore` ignored -- the key is NEVER committed; value lives only in those local files + the chat transcript). The MCP server reads `mcp-server/.env`; CLIs read `process.env.OPENROUTER_API_KEY` via `keyFromEnv()` in `scripts/lib/openrouter-client.mjs` (no dotenv autoload -- source `.env` or pass inline).

**LIVE PROBE (task #7 done):** `node scripts/ask-openrouter.mjs ask "..."` against `nvidia/nemotron-3-super-120b-a12b:free` returned a real completion -- 28 prompt + 32 completion tok, **$0 (free tier)**, 16.6s, "0 tokens entered the Claude context". Telemetry recorded to `mcp-server/data/state/ollama-offload-stats.json` via `recordCloudExecution` (ask-openrouter.mjs:276). Wiring suites 98/98 green (openrouter-client 26 + ask-openrouter 23 + model-routing-policy 24 + effort-tier-router 25).

**Already built (committed, prior session):** model-ladder + routing-graph integration (tasks #5 pull+probe, #6 wire, #8 U-OPENROUTER-TELEMETRY). Default model slug = `nemotron-super-free` (1M ctx), `DEFAULT_MODEL` in openrouter-client.mjs. Modes: ask / research / summarize / longread / models.

**SECURITY:** the key was pasted in plaintext in the chat -> it lives in the session transcript JSONL. If rotation is wanted, regenerate at openrouter.ai and re-run the two-line `.env` rewrite. `scrubSecret`/`scrub` in openrouter-client.mjs masks the key from all command output.

**WIRING AUDIT RESULT (no gap in this branch):** the per-PROMPT cloud routing is ALREADY fully wired + LIVE in `cad-fusion-live-ms0`: `model-routing-policy.routePrompt` (CLOUD-OVERFLOW-MS0, has CLOUD_EXPLICIT/CLOUD_LONGCTX/CLOUD_VETO + "free-local beats free-cloud" doctrine) -> consumed by `.claude/hooks/model-tier-advisor.mjs` (wired in settings.json, `v.engine==="openrouter"` block in HEAD) which injects the `ask-openrouter` directive every prompt. LIVE E2E (3 spanning classes, validated 2026-06-17): deep-research prompt -> emits "CLOUD long-context ($0 free tier)" directive ✓; mechanical prompt -> sonnet/LOW, NOT cloud (free-local-first) ✓; safety prompt -> opus, NEVER offloads ✓. The key was the ONLY missing piece -- now supplied, the directive is actionable end-to-end.

**CLOUD RUNG NOW BUILT (commit 42a9d88b9a on slot/alpha, U-SMART-EXEC-CLOUD-RUNG, 2026-06-17):** the per-STEP executor `scripts/lib/smart-executor.mjs::resolveExecutor` (which lives ONLY on slot/alpha, never merged to cad-fusion-live-ms0) now has a 5th `openrouter` lane at position 2 (after opus/safety, before ollama): fires on `CLOUD_EXPLICIT || (CLOUD_LONGCTX && !CLOUD_VETO)`. Safety/deep return at pos 1 (never offload); CLOUD_VETO keeps authoring verbs on Claude (cloud = READING only); short mechanical stays on cheaper LOCAL ollama. Slug inlined as a branch-split mirror of openrouter-client.DEFAULT_MODEL_SLUG (model-routing-policy is on the OTHER branch -- unify-on-merge flagged, R7). WIRED into the canonical /smart skill (`smart-skill-canonical.md` lane row + SMART CONFIG header + absent-script fail-loud fallback at ollama-parity), synced fleet-wide (29 targets, sync 16/16). 33/33 tests + CLI E2E; per-file 2-arm scrutiny PASS (closed P2 authoring-veto gap + 2 P1 doc-wire gaps). REMAINING follow-up: "auto-fire wiring" -- make /loop /goal auto-invoke resolveExecutor per step (bigger, touches loop machinery; the "reconcile 4 divergent /smart copies" follow-up is RESOLVED -- the sync mechanism makes all copies byte-identical from the canonical SSOT). -- is the $0 1M-ctx cloud rung actually REACHED by `resolveExecutor` (scripts/lib/... /smart executor contract) + `model-routing-policy` + `effort-tier-router` as a rung BETWEEN local-Ollama (free) and paid Sonnet, or does the fallback skip straight to Sonnet/Opus? Wiring it in is the "use the system to its fullest" follow-up. Related: [[reference_routing_graph_completeness_2026_06_17]] · [[feedback_ollama_fallback_sonnet_agents]].
