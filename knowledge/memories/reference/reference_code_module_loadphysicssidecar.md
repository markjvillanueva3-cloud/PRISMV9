---
name: reference_code_module_loadphysicssidecar
description: "loadPhysicsSidecar — CPS-side sidecar loader (MS0/U-PPGM03)."
metadata:
  type: reference
  kind: code-module
  galaxy: unassigned
  sourcePath: mcp-server/src/cps/loadPhysicsSidecar.ts
  sourceHash: d28c0ce8
  loc: 374
aliases: loadPhysicsSidecar
---

# loadPhysicsSidecar -- Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-04T14:51:22.427Z  |  source: `mcp-server/src/cps/loadPhysicsSidecar.ts` (374 LOC)

## What it is
```
loadPhysicsSidecar — CPS-side sidecar loader (MS0/U-PPGM03).

Runs INSIDE Fusion 360 / HSMWorks / Mastercam CPS post engine (Mozilla
Rhino-based). MUST NOT depend on Node modules (no fs, no crypto, no
Buffer, no require) — every primitive is pure JavaScript portable to
Rhino's ES5+ subset.

Contract (from MS0/U-PPGM03):
  1. Caller (the .cps post) calls `loadText(sidecarPath)` itself (CPS provides it)
  2. Caller invokes `loadPhysicsSidecar(jsonText)` here
  3. Returns parsed sidecar object on success
  4. THROWS on: missing meta, JSON parse failure, schema version mismatch,
     sha256 format invalid, sha256 recompute mismatch
  5. Fails closed: caller cannot emit the post if this throws

The pure-JS SHA-256 + canonicalize implementations MUST produce
byte-identical hashes to Node `crypto.createHash("sha256")` and to
`PhysicsSidecarBuilderEngine.canonicalize` — that is verified by tests
```


## Exports
`pureSha256Hex` (function), `pureCanonicalize` (function), `LoadOptions` (interface), `loadPhysicsSidecar` (function), `SidecarBlockAnnotation` (interface), `getBlockAnnotation` (function), `getBlockAnnotationsByOp` (function)

## Key methods
`String`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/cps/loadPhysicsSidecar.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Module

## How to query it
Recall by meaning via `/system-viz find loadPhysicsSidecar` or `prism_session:master_index_query`; open the source at `mcp-server/src/cps/loadPhysicsSidecar.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
