---
name: reference_code_mcp_auth
description: "PRISM MCP Server — OAuth 2.1 + PKCE Authorization"
metadata:
  type: reference
  kind: code-mcp
  galaxy: unassigned
  sourcePath: mcp-server/src/mcp/auth.ts
  sourceHash: 379d3741
  loc: 881
aliases: PrismOAuthServer
---

# PrismOAuthServer -- MCP Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/mcp/auth.ts` (881 LOC)

## What it is
```
PRISM MCP Server — OAuth 2.1 + PKCE Authorization

Implements the full OAuth 2.1 authorization code flow with PKCE
for multi-user shop deployments. Provides role-based access control
with 4 roles mapping to 67 dispatchers via DISPATCHER_ANNOTATIONS.

Roles:
  viewer     — read-only: query data, view calculations, browse catalogs
  operator   — viewer + read machine status, view alarms, see dashboards
  programmer — operator + run simulations, generate programs, modify tool libs
  admin      — full access: user management, system config, destructive ops

Token lifecycle:
  Access tokens  — 1 hour (JWT, HS256)
  Refresh tokens — 7 days (opaque, stored in-memory)
  Auth codes     — 5 minutes (one-time use)

@module mcp/auth
```


## Exports
`AuthUser` (interface), `JWTClaims` (interface), `TokenPair` (interface), `AuthorizationUrlParams` (interface), `CodeExchangeParams` (interface), `RefreshParams` (interface), `ValidateResult` (interface), `getPermissionMatrix` (function), `hasPermission` (function), `getRequiredRole` (function), `generateCodeVerifier` (function), `computeCodeChallenge` (function), `verifyCodeChallenge` (function), `PrismOAuthServer` (class), `getOAuthServer` (function), `createOAuthServer` (function), `resetOAuthServer` (function)

## Key methods
`validateClient`, `validateScopes`, `registerUser`, `getUser`, `listUsers`, `removeUser`, `generateAuthorizationUrl`, `exchangeCode`, `refreshToken`, `validateToken`, `revokeToken`, `revokeAllUserTokens`, `cleanup`, `shutdown`, `clearInterval`, `getServerMetadata`, `getPermissionSummary`, `getStats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/mcp/auth.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** MCP Module

## How to query it
Recall by meaning via `/system-viz find PrismOAuthServer` or `prism_session:master_index_query`; open the source at `mcp-server/src/mcp/auth.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
