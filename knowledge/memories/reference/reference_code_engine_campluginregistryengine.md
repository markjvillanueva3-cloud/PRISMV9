---
name: reference_code_engine_campluginregistryengine
description: "Plugin Discovery, Health, Compatibility (U-CAM98)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMPluginRegistryEngine.ts
  sourceHash: 7a621162
  loc: 533
aliases: CAMPluginRegistryEngine
---

# CAMPluginRegistryEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMPluginRegistryEngine.ts` (533 LOC)

## What it is
Plugin Discovery, Health, Compatibility (U-CAM98)

```
CAMPluginRegistryEngine — Plugin Discovery, Health, Compatibility (U-CAM98)
============================================================================

PHASE-7: Higher-level registry on top of the in-memory plugin table inside
CAMPluginCommunicationHubEngine (U-CAM96). Where the hub tracks transient
routing state, this registry persists the structured metadata that the
PRISM app, dashboards, and orchestrators need:

  - Capability advertisement (richer than the hub's simple frame-type array)
  - Health state machine: online → degraded → offline → reconnecting
  - Version compatibility vs a declared PRISM version (SemVer)
  - Auto-reconnect backoff schedule with bounded attempt count
  - Observable event log for a health dashboard (most-recent N events)

Health state machine:

    register() ──► online
         │
         │ heartbeat()
         ▼
       online ◄─────── attemptReconnect(
```


## Exports
`RegistryTransportSchema` (const), `RegistryTransport` (type), `RegistryTargetSchema` (const), `RegistryTarget` (type), `HealthStateSchema` (const), `HealthState` (type), `SemVerSchema` (const), `SemVer` (type), `CompatRangeSchema` (const), `CompatRange` (type), `PluginCapabilitySchema` (const), `PluginCapability` (type), `PluginRegistrationInputSchema` (const), `PluginRegistrationInput` (type), `RegisteredPluginSchema` (const), `RegisteredPlugin` (type), `HealthEventSchema` (const), `HealthEvent` (type), `CompatibilityResultSchema` (const), `CompatibilityResult` (type)

## Key methods
`logEvent`, `register`, `unregister`, `heartbeat`, `transition`, `reportFailure`, `attemptReconnect`, `getPlugin`, `listPlugins`, `checkCompatibility`, `dueForReconnect`, `computeHealthDashboard`, `recentEvents`, `resetRegistry`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CAMPluginRegistryEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMPluginRegistryEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMPluginRegistryEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
