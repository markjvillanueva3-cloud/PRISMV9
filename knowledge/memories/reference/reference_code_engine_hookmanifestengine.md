---
name: reference_code_engine_hookmanifestengine
description: "HOOK-MANIFEST-DAG-MS26 / P0-U01"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HookManifestEngine.ts
  sourceHash: f11e5326
  loc: 497
aliases: HookManifestEngine
---

# HookManifestEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HookManifestEngine.ts` (497 LOC)

## What it is
HOOK-MANIFEST-DAG-MS26 / P0-U01

```
HookManifestEngine — HOOK-MANIFEST-DAG-MS26 / P0-U01

Scans the Claude Code hook surface and emits a static {@link HookManifest}:
  - hook *files* on disk: `.claude/hooks/*.mjs` (harness hooks) and
    `mcp-server/src/hooks/*.ts` (MCP-server safety hooks);
  - hook *wirings* declared in the `hooks` block of the active `settings.json`
    files (user-level `<drive>/.claude/settings.json`, `~/.claude/settings.json`,
    and the project-level `.claude/settings.json` / `.claude/settings.local.json`);
  - the join of the two: which file fires on which event/matcher, with timeout
    and hard-block flags, plus orphaned files (on disk, never wired) and
    dangling refs (wired, but the file is missing).

This is the input contract for HookDAGValidatorEngine (P0-U02, cycle
detection + deterministic order) and for hook-coverage tooling.

Duplication check (manual — DuplicationGuardEngine's dynami
```


## Exports
`ManifestFS` (type), `HookManifestOptions` (interface), `HookManifestEngine` (class), `hookManifestEngine` (const)

## Key methods
`getCapabilities`, `validate`, `resolveRepoRoot`, `generate`, `defaultManifestPath`, `generateAndWrite`, `loadManifest`

## Depends on
[[reference_code_engine_baseengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/HookManifestEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HookManifestEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HookManifestEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
