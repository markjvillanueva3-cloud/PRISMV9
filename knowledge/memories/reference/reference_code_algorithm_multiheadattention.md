---
name: reference_code_algorithm_multiheadattention
description: "MultiHeadAttention — the multi-head wrapper of the Transformer attention"
metadata:
  type: reference
  kind: code-algorithm
  galaxy: unassigned
  sourcePath: mcp-server/src/algorithms/MultiHeadAttention.ts
  sourceHash: d1b543c6
  loc: 252
aliases: MultiHeadAttention
---

# MultiHeadAttention -- Algorithm SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/algorithms/MultiHeadAttention.ts` (252 LOC)

## What it is
```
MultiHeadAttention — the multi-head wrapper of the Transformer attention
mechanism (Vaswani et al. 2017). Composes the single-head
`ScaledDotProductAttention` primitive across h parallel heads:

  MHA(Q,K,V) = Concat(head_1, …, head_h) · Wᴼ
  head_g     = Attention(Q·Wq_g, K·Wk_g, V·Wv_g)

with d_model split into h heads of width d_head = d_model / h. Each head runs
the SAME numerically-stable scaled-dot-product operator already shipped
(`ml_attention`) — this module does NOT re-derive softmax/scaling; it slices
the projected Q/K/V into heads, delegates per head, concatenates, and applies
the output projection. This is the "wire all combinations" doctrine made
concrete ([[feedback_find_all_wiring_endpoints_and_combinations]]).

Learned projections (Wq, Wk, Wv, Wo, each [d_model × d_model]) are OPTIONAL —
when omitted the identity is used, giving "split-head attention" (the pure
operator)
```


## Exports
`MultiHeadAttentionInput` (interface), `MultiHeadAttentionOutput` (interface), `MultiHeadAttention` (const)

## Key methods
`validate`, `calculate`, `getMetadata`

## Depends on
[[reference_code_algorithm_scaleddotproductattention]]

## Where it lives
- **Source:** `mcp-server/src/algorithms/MultiHeadAttention.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Algorithm

## How to query it
Recall by meaning via `/system-viz find MultiHeadAttention` or `prism_session:master_index_query`; open the source at `mcp-server/src/algorithms/MultiHeadAttention.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
