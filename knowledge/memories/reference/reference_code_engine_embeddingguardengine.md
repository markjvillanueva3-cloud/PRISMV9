---
name: reference_code_engine_embeddingguardengine
description: "Tiered cosine-similarity gate with injectable embedder"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/EmbeddingGuardEngine.ts
  sourceHash: 98d6a885
  loc: 222
aliases: EmbeddingGuardEngine
---

# EmbeddingGuardEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/EmbeddingGuardEngine.ts` (222 LOC)

## What it is
Tiered cosine-similarity gate with injectable embedder

```
EmbeddingGuardEngine — Tiered cosine-similarity gate with injectable embedder

Phase 0.16 U-OP13 from UNIVERSAL-SKILLS-SCRIPTS-HOOKS-PLAN. Complements the
existing TF-IDF-based SemanticSimilarityGuardEngine by evaluating the
three-band rule (green/yellow/red) against real sentence embeddings
produced by LocalEmbeddingEngine (or any injected embedder).

  green  (< 0.70)            → pass
  yellow (0.70 ≤ c ≤ 0.85)   → warn + require justification
  red    (> 0.85)            → block

Exact-name collisions are fast-pathed to red even when the embedder is
offline. If the embedder fails mid-check we degrade to yellow so a block
isn't silently lost — the caller still must justify, and a future run
with a working embedder can tighten or loosen the decision.

@module engines/EmbeddingGuardEngine
@milestone PP-0.16-U-OP13
```


## Exports
`GuardBand` (type), `GuardCandidate` (interface), `GuardReference` (interface), `GuardConfig` (interface), `DEFAULT_EMBEDDING_GUARD_CONFIG` (const), `GuardEmbedder` (interface), `GuardMatch` (interface), `GuardDecision` (interface), `EmbeddingGuardEngine` (class)

## Key methods
`embed`, `setConfig`, `getConfig`, `addReference`, `referenceCount`, `clear`, `evaluate`, `bandFor`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/EmbeddingGuardEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find EmbeddingGuardEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/EmbeddingGuardEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
