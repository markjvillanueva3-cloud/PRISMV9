---
name: reference_code_engine_macrocandidategateengine
description: "MacroCandidateGateEngine — MACRO-PROGRAM-PIPELINE-MS0/MS0-U4 (SAFETY-CRITICAL, LOAD-BEARING)."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MacroCandidateGateEngine.ts
  sourceHash: ab90b36a
  loc: 420
aliases: macroCandidateGateEngine
---

# macroCandidateGateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MacroCandidateGateEngine.ts` (420 LOC)

## What it is
```
MacroCandidateGateEngine — MACRO-PROGRAM-PIPELINE-MS0/MS0-U4 (SAFETY-CRITICAL, LOAD-BEARING).

The safety gate around a MacroFillCandidate (produced by MS0-U2). Composes:
  • Envelope check (machine travel limits vs candidate dimensions)
  • Material sanity (positive lengths, ID < OD, drill depth/dia ratio, …)
  • S(x) safety score (≥ 0.70 HARD BLOCK — no override in this pipeline)
  • Per-machine spindle-speed check (max RPM clamp vs candidate G50)
  • Per-machine workholding sanity (counterbore vs step-OD fit)

Returns { passed, sxScore, safetyRecord, failingChecks, dossier } where:
  - passed=true ⇒ S(x) ≥ 0.70 AND all hard checks SAFE
  - passed=false ⇒ dossier === null AND NO file/path/gcode-output property
    exists on the result — emit NOTHING on FAIL is asserted at type level.

SAFETY-CRITICAL invariants (asserted by tests):
  1. HARD BLOCK at S(x) < 0.70 — no off-by-one at the 
```


## Exports
`MachineLimits` (interface), `GateSignature` (interface), `SafetyRecord` (interface), `SignoffDossier` (interface), `GateResult` (interface), `JM_DIE_MACHINE_LIMITS` (const), `macroCandidateGateEngine` (const)

## Key methods
`gateCandidate`

## Depends on
[[reference_code_engine_macrofillorchestratorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/MacroCandidateGateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find macroCandidateGateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MacroCandidateGateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
