---
name: reference_code_engine_tcodashboardengine
description: "TCODashboardEngine — Total Cost of Ownership Dashboard"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/TCODashboardEngine.ts
  sourceHash: cb6b26db
  loc: 774
aliases: tcoDashboardEngine
---

# tcoDashboardEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/TCODashboardEngine.ts` (774 LOC)

## What it is
```
TCODashboardEngine — Total Cost of Ownership Dashboard

Aggregates all cost streams from PipelineCostModelEngine and provides
actionable optimization insights across 6 dashboard views:

  1. generateDashboard(job)          → TCODashboardData
     Full cost breakdown (10 components), savings summary, top drivers

  2. compareCosts(current, optimized) → CostComparison
     Side-by-side parameter comparison with delta and ROI parts

  3. costDriverAnalysis(job)         → { drivers, pareto_data }
     Pareto chart data: drivers sorted by cost, cumulative % curve

  4. savingsOpportunities(job)       → { opportunities[] }
     Ranked action list: cycle-time, tooling, setup, coolant, shift

  5. machineUtilizationCost(machines[]) → { machine, hourly_cost, utilization_pct, effective_cost }[]
     Effective cost per hour accounting for idle time

  6. historicalTrend(part_number?, date_range?) →
```


## Exports
`TCOJobInput` (interface), `CostComponent` (interface), `TCODashboardData` (interface), `CostComparisonResult` (interface), `ParetoData` (interface), `CostDriverAnalysisResult` (interface), `SavingsOpportunity` (interface), `SavingsOpportunitiesResult` (interface), `MachineUtilizationCostResult` (interface), `HistoricalTrendResult` (interface), `MachineUtilizationInput` (interface), `CostHistoryRecord` (interface), `tcoDashboardEngine` (const), `TCODashboardEngine` (re-export)

## Key methods
`generateDashboard`, `compareCosts`, `costDriverAnalysis`, `savingsOpportunities`, `machineUtilizationCost`, `historicalTrend`, `recordCostHistory`, `injectHistory`, `clearHistory`, `getHistory`

## Depends on
[[reference_code_engine_pipelinecostmodelengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/TCODashboardEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find tcoDashboardEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/TCODashboardEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
