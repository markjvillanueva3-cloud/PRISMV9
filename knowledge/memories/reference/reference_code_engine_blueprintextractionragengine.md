---
name: reference_code_engine_blueprintextractionragengine
description: "BLUEPRINT-OCR-TRAINING-MS1/U-MS1-U7"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/BlueprintExtractionRAGEngine.ts
  sourceHash: afe081f6
  loc: 533
aliases: BlueprintExtractionRAGEngine
---

# BlueprintExtractionRAGEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/BlueprintExtractionRAGEngine.ts` (533 LOC)

## What it is
BLUEPRINT-OCR-TRAINING-MS1/U-MS1-U7

```
BlueprintExtractionRAGEngine — BLUEPRINT-OCR-TRAINING-MS1/U-MS1-U7

The composition layer (centerpiece). Wraps the base vision call
(BlueprintVisionOCREngine) with RAG augmentation. For each extraction:
  1. Pre-classify the print (blueprint-infer-class + family + customer prior)
  2. Retrieve corpus context (top-k via embed-search, filtered by domain)
  3. Retrieve tribal knowledge (shop-floor priors)
  4. Retrieve prior similar prints (dim-signature near-neighbour)
  5. Compose extraction prompt with retrieved context
  6. Run vision call (+ optional ensemble corroboration)
  7. Fuse + bound (conformal stratification)
  8. Emit BlueprintExtraction with per-region confidence + cited sources
  9. Record outcome for later operator-confirmation pairing

Pure-orchestration: every retriever + the vision call is injectable so the
engine can be unit-tested without network/disk dependencies and
```


## Exports
`EXTRACTION_DIM_TYPES` (const), `ExtractionDimType` (type), `SOURCE_KINDS` (const), `SourceKind` (type), `CONFIDENCE_FLOORS` (const), `ConfidenceFloor` (type), `RetrievedSourceSchema` (const), `RetrievedSource` (type), `ExtractionRegionSchema` (const), `ExtractionRegion` (type), `BlueprintExtractionSchema` (const), `BlueprintExtraction` (type), `ExtractRequest` (interface), `BlueprintExtractionRAGIO` (interface), `CompareToBaselineResult` (interface), `BlueprintExtractionRAGEngine` (class), `validateRequest` (function), `clamp01` (function), `detectContradictions` (function), `defaultComposePrompt` (function)

## Key methods
`extract`, `validateRequest`, `explain`, `compareToBaseline`, `clearCache`, `cacheSize`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/BlueprintExtractionRAGEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find BlueprintExtractionRAGEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/BlueprintExtractionRAGEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
