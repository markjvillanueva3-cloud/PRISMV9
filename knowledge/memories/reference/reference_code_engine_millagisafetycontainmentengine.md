---
name: reference_code_engine_millagisafetycontainmentengine
description: "MillAGISafetyContainmentEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MillAGISafetyContainmentEngine.ts
  sourceHash: 50e8edc0
  loc: 438
aliases: millAGISafetyContainmentEngine
---

# millAGISafetyContainmentEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MillAGISafetyContainmentEngine.ts` (438 LOC)

> [physics] Imports canonical physics constants (`src/physics/constants.ts`) -- safety-relevant node.

## What it is
```
MillAGISafetyContainmentEngine
================================

Bounds AGI outputs against mill physics envelopes, cost margins, shop
capacity, and mill-specific kinematic requirements. Every AGI
recommendation must pass this gate before being consumed by P1..P5.
Failures return a structured rejection with a trace of which check
failed and the bound that was violated.

Mill parity for LatheAGISafetyContainmentEngine (LATHE-MASTER U-LTH61)
with 2 MILL-CANONICAL extensions:

  - Kinematic check substitutes mill axes (4th/5th/pallet/probe) for
    lathe axes (live_tool/sub_spindle/y_axis)
  - Chatter check (MILL-CANONICAL — lathe rarely chatter-bound) —
    verifies flute_count × rpm doesn't hit a tool-tooth chatter lobe

Check categories:
  - physics     — Vc within ISO group envelope, fz_per_tooth within
                  fz_max (MILL-CANONICAL: per-tooth not per-rev)
  - economics   — q
```


## Exports
`MILL_VC_ENVELOPE` (const), `MILL_FZ_PER_TOOTH_MAX` (const), `MARGIN_MIN_HARD` (const), `MARGIN_MIN_SOFT` (const), `SPINDLE_FORCE_MARGIN_N_MILL` (const), `FLEET_CAPACITY_HOURS_DEFAULT` (const), `CHATTER_DANGER_ZONE_LOW` (const), `CHATTER_DANGER_ZONE_HIGH` (const), `MillSpeedFeedCandidateSchema` (const), `MillSpeedFeedCandidate` (type), `MillQuoteCandidateSchema` (const), `MillQuoteCandidate` (type), `MillScheduleCandidateSchema` (const), `MillScheduleCandidate` (type), `MillKinematicCandidateSchema` (const), `MillKinematicCandidate` (type), `MillChatterCandidateSchema` (const), `MillChatterCandidate` (type), `MillSafetyCheckInputSchema` (const), `MillSafetyCheckInput` (type)

## Key methods
`check`, `getStats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MillAGISafetyContainmentEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find millAGISafetyContainmentEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MillAGISafetyContainmentEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
