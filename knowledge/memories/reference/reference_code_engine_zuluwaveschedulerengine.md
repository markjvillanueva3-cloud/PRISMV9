---
name: reference_code_engine_zuluwaveschedulerengine
description: "Zulu Wave Scheduler"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ZuluWaveSchedulerEngine.ts
  sourceHash: c57cb257
  loc: 770
aliases: ZuluWaveSchedulerEngine
---

# ZuluWaveSchedulerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ZuluWaveSchedulerEngine.ts` (770 LOC)

## What it is
Zulu Wave Scheduler

```
ZuluWaveSchedulerEngine -- multi-wave DAG scheduler for Hermes fan-out plans.

The companion HermesParallelFanoutPlannerEngine (HZP01) decomposes a parent
task into N subtasks but its `plan()` only ever emits WAVE 1 -- its leaf-filter
(`subtasks.filter(s => s.depends_on.length === 0)`) returns the initial
parallelizable set and defers everything with a dependency, with no machinery
to compute the LATER waves once an earlier wave completes. That is the gap this
engine closes.

This is the pure, deterministic scheduler:
  - `computeWaveN(plan, completedIds)` -> the NEXT wave's ready subtasks: every
    subtask all of whose `depends_on` are in `completedIds`, that is not itself
    already completed. This is the incremental driver a caller invokes after each
    returned wave, feeding back the union of completed subtask_ids.
  - `allWaves(plan)` -> the full topological wave partition (wave 
```


## Exports
`WaveSchedulePlanSchema` (const), `WaveSchedulePlan` (type), `Wave` (interface), `WavePartition` (interface), `NextWave` (interface), `WaveExecution` (interface), `VetoedAssignment` (interface), `GovernedWaveExecution` (interface), `WaveDelegationGate` (interface), `ThrottledAssignment` (interface), `ThrottledWaveExecution` (interface), `ProjectedWave` (interface), `ProjectedSchedule` (interface), `ZuluWaveSchedulerEngine` (class), `zuluWaveSchedulerEngine` (const)

## Key methods
`computeWaveN`, `allWaves`, `renderPartition`, `renderNextWave`, `nextWaveAssignments`, `governedNextWave`, `projectGovernedSchedule`, `renderProjectedSchedule`, `throttleWave`, `mergeCompleted`, `loopCheckpointState`, `renderWaveExecution`

## Depends on
[[reference_code_engine_hermesparallelfanoutplannerengine]] | [[reference_code_engine_zulufleetgovernorengine]] | [[reference_code_engine_zuludelegationcontractengine]] | [[reference_code_engine_zuluadaptivebackpressureengine]] | [[reference_code_engine_soulfrontmatterreaderengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/ZuluWaveSchedulerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ZuluWaveSchedulerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ZuluWaveSchedulerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
