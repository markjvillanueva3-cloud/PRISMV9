---
name: reference_code_engine_latheactualcostreconciliationengine
description: "LatheActualCostReconciliationEngine — U-LTH49 (LATHE-MASTER P5 ERP)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LatheActualCostReconciliationEngine.ts
  sourceHash: 4798ef85
  loc: 510
aliases: latheActualCostReconciliationEngine
---

# latheActualCostReconciliationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LatheActualCostReconciliationEngine.ts` (510 LOC)

## What it is
```
LatheActualCostReconciliationEngine — U-LTH49 (LATHE-MASTER P5 ERP)

Closes the quote → job → actuals → next-quote feedback loop. Takes the
QuotePackage produced by LatheAutoQuoteFromPrintEngine (U-LTH48) and a
ShopActuals record from the completed job, and produces:

  1. Per-bucket variance  — predicted vs actual for material / tool-wear /
     cycle / setup in both absolute USD and percentage terms.
  2. Physics-root deltas  — where the variance came from in physical units:
     cycle_time_min_delta, material_mass_kg_delta, tool_changes_delta,
     setup_time_min_delta, scrap_rate_pct.
  3. Root-cause classification — one of:
     programming / tooling / setup / material / machine / operator / none.
     Heuristic driven by which delta dominates (argmax on |contribution|).
  4. Feedback packet     — per-bucket suggested multipliers in [0.8, 1.2]
     for the next quote on a similar jo
```


## Exports
`ShopActualsSchema` (const), `ShopActuals` (type), `ReconcileInputSchema` (const), `ReconcileInput` (type), `RootCause` (type), `BucketVariance` (interface), `PhysicsDeltas` (interface), `AccuracyStats` (interface), `ReconciliationReport` (interface), `ReconciliationRecord` (interface), `ReconciliationState` (interface), `latheActualCostReconciliationEngine` (const), `LatheActualCostReconciliationEngine` (re-export)

## Key methods
`reconcile`, `getAccuracyStats`, `atomicWriteJson`, `mkdirSync`

## Depends on
[[reference_code_engine_latheautoquotefromprintengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/LatheActualCostReconciliationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find latheActualCostReconciliationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LatheActualCostReconciliationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
