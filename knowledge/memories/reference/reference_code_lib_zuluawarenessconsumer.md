---
name: reference_code_lib_zuluawarenessconsumer
description: "ZULU-AWARENESS-MS1/U-AW01 — shared consumer library for the awareness pipeline."
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/zulu-awareness-consumer.mjs
  sourceHash: 832c8633
  loc: 177
aliases: zulu-awareness-consumer.mjs
---

# zulu-awareness-consumer.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/zulu-awareness-consumer.mjs` (177 LOC)

## What it is
```
ZULU-AWARENESS-MS1/U-AW01 — shared consumer library for the awareness pipeline.

Purpose: ONE place that loads `state/shared/zulu-awareness-index.json` and
the tuned weights, then exposes thin pure helpers that downstream consumers
(zulu-orchestrator-sweep.mjs, AISystemRouterEngine.ts, PRISMSelfAwarenessEngine.ts)
all use without each one re-implementing fingerprint lookup + ranking.

Design invariants:
  1. Fail-soft — every helper returns a stable empty shape when the index
     is missing/corrupt; never throws. R12: every empty result names a `reason`.
  2. Pure — readers are injected; the default `defaultReader` uses fs but tests
     can pass a synthetic reader.
  3. Single-load cache — the index is read once per process and memoized.
     Callers that need fresh data call `invalidateAwarenessCache()`.
  4. Refuse-list HARD VETO is preserved end-to-end — `rankSlots()` filters
     r
```


## Exports
`defaultReader` (function), `loadAwareness` (function), `invalidateAwarenessCache` (function), `lookupSlot` (function), `rankSlots` (function), `scoreSlot` (function), `enrichSlotPicks` (function), `recommendSlotsForDomain` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/zulu-awareness-consumer.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find zulu-awareness-consumer.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/zulu-awareness-consumer.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
