---
name: reference_code_engine_strategysequencingengine
description: "Optimizes the SEQUENCE of machining strategies for multi-operation features"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/StrategySequencingEngine.ts
  sourceHash: faca01fe
  loc: 857
aliases: StrategySequencingEngine
---

# StrategySequencingEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/StrategySequencingEngine.ts` (857 LOC)

## What it is
Optimizes the SEQUENCE of machining strategies for multi-operation features

```
StrategySequencingEngine (E1097) — CAMX-MS12 U06

Optimizes the SEQUENCE of machining strategies for multi-operation features
by tracking stock state evolution through each step.  Each operation sees
only the material that remains after all prior operations, enabling realistic
time/load estimates and safety validation.

Example deep-pocket sequence:
  1. TGAR adaptive rough  — remove ~80 % material, constant engagement
  2. Z-level semi-finish  — clean stair-steps left by roughing
  3. AMEF morphed-spiral  — semi-finish corner radii
  4. HRAF vibration-safe  — final wall quality
  5. PTDC floor finish    — deflection-compensated floor

Methods:
  sequenceStrategies()  — greedy sequencer with stock-aware rule engine
  evaluateSequence()    — score one ordered sequence (time, cost, Ra, tool changes)
  optimizeSequence()    — permutation search up to 6! = 720 combinations

All sub-engines a
```


## Exports
`IsoGroup` (type), `OperationRole` (type), `AlgoTag` (type), `StrategyCandidate` (interface), `FeatureInput` (interface), `MaterialInput` (interface), `StockDimensions` (interface), `MachineInput` (interface), `SequenceConstraints` (interface), `StockStateSnapshot` (interface), `SequenceStep` (interface), `StrategySequence` (interface), `SequenceEvaluation` (interface), `OptimizeResult` (interface), `StrategySequencingEngine` (class), `strategySequencingEngine` (const)

## Key methods
`sequenceStrategies`, `evaluateSequence`, `optimizeSequence`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/StrategySequencingEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find StrategySequencingEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/StrategySequencingEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
