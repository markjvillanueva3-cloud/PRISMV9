---
name: reference_code_engine_hermesparallelfanoutplannerengine
description: "HZP01 parallel-agent fan-out planner."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HermesParallelFanoutPlannerEngine.ts
  sourceHash: 9390f9af
  loc: 378
aliases: HermesParallelFanoutPlannerEngine
---

# HermesParallelFanoutPlannerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HermesParallelFanoutPlannerEngine.ts` (378 LOC)

## What it is
HZP01 parallel-agent fan-out planner.

```
HermesParallelFanoutPlannerEngine — HZP01 parallel-agent fan-out planner.

The existing zulu-awareness reader ranks ONE slot per task. This planner
decomposes a parent task into N independent subtasks, each routed to a
distinct slot/role, and emits a fan-out plan suitable for a single
Agent-tool batch (parallel tool calls in one message).

Pure-core: takes the parent task + a slot-recommendation list +
pre-decomposed subtasks; returns the planned fan-out (or rejects with a
reason if the work is actually sequential).

@module engines/HermesParallelFanoutPlannerEngine
```


## Exports
`SubtaskSchema` (const), `Subtask` (type), `SlotCandidateSchema` (const), `SlotCandidate` (type), `FanoutPlanRequestSchema` (const), `FanoutPlanRequest` (type), `AgentAssignment` (interface), `FanoutPlan` (interface), `DetectedDomain` (interface), `AutoFanoutAssessment` (interface), `assignSubtasksToSlots` (function), `HermesParallelFanoutPlannerEngine` (class), `hermesParallelFanoutPlannerEngine` (const)

## Key methods
`plan`, `renderPlan`, `assessAutoTrigger`, `renderAutoTrigger`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/HermesParallelFanoutPlannerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HermesParallelFanoutPlannerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HermesParallelFanoutPlannerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
