---
name: reference_code_engine_managerregistryengine
description: "ManagerRegistryEngine — central truth for employee→rank+dept+reports_to chain."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ManagerRegistryEngine.ts
  sourceHash: 33c6c1a6
  loc: 478
aliases: managerRegistryEngine
---

# managerRegistryEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ManagerRegistryEngine.ts` (478 LOC)

## What it is
```
ManagerRegistryEngine — central truth for employee→rank+dept+reports_to chain.

Every approval/handoff/promotion in the substrate currently passes
`manager_employee_id` ad-hoc. This engine is the single source of truth for:
  - employee identity → rank (apprentice..owner..admin)
  - employee → department code (links to DepartmentEngine)
  - reports_to chain (each employee names their direct manager)
  - on-rank-change auditing (who promoted who, when, why)
  - canManagerApprove(approver, subject) — used by all approval gates

Rank ladder (lowest → highest authority):
  apprentice < operator < machinist < setup < programmer < inspector <
  qa_lead < foreman < buyer < estimator < scheduler < shipping <
  receiving < accounting < sales < office_admin < lead < supervisor <
  manager < director < owner < admin

The `admin` rank is the NEW top-level rank above `owner` per operator
directive (2
```


## Exports
`Rank` (type), `RANK_ORDER` (const), `EmployeeRecord` (interface), `RankAudit` (interface), `managerRegistryEngine` (const)

## Key methods
`register`, `promote`, `demote`, `reassignDepartment`, `setReportsTo`, `deactivate`, `reactivate`, `canManagerApprove`, `reportsToChain`, `directReports`, `getEmployee`, `listEmployees`, `rankOrder`, `rankIndex`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ManagerRegistryEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find managerRegistryEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ManagerRegistryEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
