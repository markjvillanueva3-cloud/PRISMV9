---
name: reference_code_engine_cpmpertengine
description: "U-FORE-15 (Real Planning Algorithms)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CPMPERTEngine.ts
  sourceHash: 0b910680
  loc: 222
aliases: CPMPERTEngine
---

# CPMPERTEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CPMPERTEngine.ts` (222 LOC)

## What it is
U-FORE-15 (Real Planning Algorithms)

```
CPMPERTEngine — U-FORE-15 (Real Planning Algorithms)
=====================================================

Critical Path Method + Program Evaluation and Review Technique.

CPM (deterministic): computes earliest/latest start/finish and slack
for each task in an activity-on-node DAG, then identifies the critical
path (the chain of zero-slack tasks that dictates project duration).

PERT (probabilistic): uses three-point estimates (optimistic,
most-likely, pessimistic) to compute expected duration and variance
per task via te = (O + 4M + P) / 6, v = ((P - O) / 6)^2. Project
duration ~ N(sum_expected_on_critical, sum_variance_on_critical) by
the central limit theorem, so P(finish ≤ T) = Phi((T - μ) / σ).

Input:
  tasks: [{ id, duration?, optimistic?, likely?, pessimistic?, predecessors: [] }]
Output:
  { schedule: {id: {ES, EF, LS, LF, slack, critical}}, criticalPath[],
    projectDuration,
```


## Exports
`CPMTask` (interface), `TaskSchedule` (interface), `CPMResult` (interface), `CPMPERTEngine` (class), `cpmPertEngine` (const)

## Key methods
`analyze`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CPMPERTEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CPMPERTEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CPMPERTEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
