---
name: reference_code_script_buildvizadjacency
description: "build-viz-adjacency.mjs — capped node-adjacency sidecar for the 3D viewer's"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/build-viz-adjacency.mjs
  sourceHash: 5f090fbc
  loc: 85
aliases: build-viz-adjacency.mjs
---

# build-viz-adjacency.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/build-viz-adjacency.mjs` (85 LOC)

## What it is
```
build-viz-adjacency.mjs — capped node-adjacency sidecar for the 3D viewer's
blast-radius side-panel (U-VIZ-NODE-NEIGHBORS, slot:sierra 2026-06-01).

WHY: the merged system-graph.json is ~695 MB / ~302K nodes / ~1M edges. The 3D
viewer samples per-engine `eng.<domain>.<name>` nodes whose edges live ONLY in
this merged graph (the 203 MB index has no edges; the 56 MB arch-graph only has
41 eng-CLUSTER + 104 dispatcher nodes, not the per-engine nodes). To let the
side-panel show a clicked node's upstream/downstream neighbors WITHOUT the server
holding the 695 MB graph, we precompute a BOUNDED adjacency sidecar: for each node
that has edges, up to K in-neighbors + K out-neighbors as {id,type}. Capped per
direction → bounded size; the server loads this small sidecar once.

Reads via readGraphStreaming (the OOM-safe Buffer parser — never JSON.parse a
>512 MB string, per sierra soul). Single pas
```


## Exports
`buildAdjacency` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/build-viz-adjacency.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find build-viz-adjacency.mjs` or `prism_session:master_index_query`; open the source at `scripts/build-viz-adjacency.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
