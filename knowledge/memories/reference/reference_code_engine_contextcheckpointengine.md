---
name: reference_code_engine_contextcheckpointengine
description: "ContextCheckpointEngine — AI-MAX-ROADMAP U-AIMAX08"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ContextCheckpointEngine.ts
  sourceHash: 2d36621c
  loc: 425
aliases: contextCheckpointEngine
---

# contextCheckpointEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ContextCheckpointEngine.ts` (425 LOC)

## What it is
```
ContextCheckpointEngine — AI-MAX-ROADMAP U-AIMAX08
====================================================

Automatic session-state checkpointing at edit-count thresholds
with 95%+ recovery after compaction.

Problem:
  Claude sessions lose fidelity across /compact and crashes. A
  handoff file captures the next-action directive but not the
  full in-flight state (open tasks, pending files, decisions
  made this session). When compaction lands mid-work, re-
  establishing the mental model costs a full round of file reads
  and grep.

Solution:
  Write a compact JSON snapshot of session state at 15, 25, 35
  edit thresholds (and any custom set). On session-resume (or
  post-compact), load the most recent checkpoint and restore.
  Each checkpoint < 1MB by hard schema-level size cap.

Integration:
  * recordEdit() called by PostToolUse(Write|Edit) hook.
  * createCheckpoint() triggered automat
```


## Exports
`CONTEXT_CHECKPOINT_SCHEMA_VERSION` (const), `DEFAULT_EDIT_THRESHOLDS` (const), `MAX_CHECKPOINT_BYTES` (const), `MAX_CHECKPOINTS_PER_SESSION` (const), `FileInFlight` (interface), `CheckpointSnapshot` (interface), `RecoveryResult` (interface), `CheckpointConfig` (interface), `DEFAULT_CONFIG` (const), `SessionEditState` (interface), `contextCheckpointEngine` (const), `ContextCheckpointEngine` (type)

## Key methods
`setConfig`, `getConfig`, `recordEdit`, `shouldCheckpoint`, `getEditState`, `createCheckpoint`, `latestCheckpoint`, `listCheckpoints`, `getCheckpointById`, `recoverState`, `serialize`, `deserialize`, `ingestExternal`, `totalCheckpoints`, `knownSessions`, `resetEdits`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ContextCheckpointEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find contextCheckpointEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ContextCheckpointEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
