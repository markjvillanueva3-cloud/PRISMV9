---
name: reference_code_engine_cadreversetemplateengine
description: "CAD-REVERSE-ENGINEER-MS0/U1"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADReverseTemplateEngine.ts
  sourceHash: 9576621a
  loc: 302
aliases: CADReverseTemplateEngine
---

# CADReverseTemplateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADReverseTemplateEngine.ts` (302 LOC)

## What it is
CAD-REVERSE-ENGINEER-MS0/U1

```
CADReverseTemplateEngine — CAD-REVERSE-ENGINEER-MS0/U1

The **inverse** of CADDrawAnyPartOrchestratorEngine. The orchestrator
goes intent → ops → geometry; this engine goes parsed-feature-tree →
parameterized template → category + canonical name. Feed the operator's
CAD corpus through `cad_step_parse_file` + `cad_feature_tree_extract`
to obtain a `CADOperation[]`, hand that array here, and get back a
`ReverseEngineeredTemplate` that:

  1. **categorizes** the part from its op-kind histogram (turned_part,
     housing, flange, bracket, drilled_block, assembly_body,
     extruded_profile, prismatic_part) — most-specific rule wins.
  2. **names** it deterministically: `<category>_<dim>mm_<n>op_<hash4>`
     — the hash4 disambiguates same-size same-category parts so the
     whole corpus gets unique, sortable, human-readable names.
  3. **parameterizes** every finite numeric arg into a named
```


## Exports
`PartCategory` (type), `TemplateParam` (interface), `ReverseEngineeredTemplate` (interface), `ReverseTemplateStats` (interface), `CADReverseTemplateEngine` (class), `cadReverseTemplateEngine` (const)

## Key methods
`reverseEngineer`, `categorizeOnly`, `getStats`, `getCategoryVocabulary`, `isKnownOpKind`

## Depends on
`ICADCodeGenerator`

## Where it lives
- **Source:** `mcp-server/src/engines/CADReverseTemplateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADReverseTemplateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADReverseTemplateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
