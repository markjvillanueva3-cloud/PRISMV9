---
name: reference_code_engine_regretminimizationengine
description: "Multi-armed bandit with UCB and Thompson sampling"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/RegretMinimizationEngine.ts
  sourceHash: bc0ea06e
  loc: 180
aliases: RegretMinimizationEngine
---

# RegretMinimizationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/RegretMinimizationEngine.ts` (180 LOC)

## What it is
Multi-armed bandit with UCB and Thompson sampling

```
RegretMinimizationEngine — Multi-armed bandit with UCB and Thompson sampling

Phase 0.20 U-MATH16 from UNIVERSAL-SKILLS-SCRIPTS-HOOKS-PLAN. Minimises
cumulative regret when choosing among strategies with unknown reward
distributions. Implements two canonical algorithms:

  UCB1 (Auer et al., 2002): pick arm maximizing mean + sqrt(2 ln t / n_i).
  Thompson sampling (Beta-Bernoulli conjugate): sample θ_i ~ Beta(α_i, β_i).

Rewards are assumed in [0, 1]; callers scale if needed. Thompson sampling
uses a deterministic seed (provided PRNG) so tests can pin behaviour.

@module engines/RegretMinimizationEngine
@milestone PP-0.20-U-MATH16
```


## Exports
`ArmStats` (interface), `SelectionTrace` (interface), `Rng` (type), `seededRng` (function), `RegretMinimizationEngine` (class), `regretMinimizationEngine` (const)

## Key methods
`addArm`, `addArms`, `record`, `selectUcb`, `selectThompson`, `stats`, `totalPullsCount`, `clear`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/RegretMinimizationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find RegretMinimizationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/RegretMinimizationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
