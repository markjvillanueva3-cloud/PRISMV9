---
name: reference_alpha_hermes_verified_tier_2026_06_24
description: "Alpha shipped U-HERMES-VERIFIED-TIER (commit f853c08ade, 2026-06-24) -- a tiered verified-offload primitive (Hermes-strong -> Ollama -> trusted-fallback) that LIGHTS the previously-dark ask-hermes lane. Operator /goal: improve hermes/ollama/octopus/obsidian/psn/system-viz utilization. The Hermes lane was BUILT+instrumented but had ZERO recorded calls; nothing automated invoked it."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.469Z
aliases: reference_alpha_hermes_verified_tier_2026_06_24
---


# Hermes verified-offload tier -- slot:alpha 2026-06-24, commit f853c08ade

Operator `/checkin-alpha /goal [improve hermes cli/agent, obsidian, psn, system-viz, ollama offloading, octopus utilization ... engineered loops/harnesses/crons]`. Alpha-domain (token-economy + ollama/hermes offload governance).

## Verified gap (direct scout, not an audit Workflow)
- Hermes proxy :8645 is UP + authenticated (xAI Grok-4.x); `scripts/ask-hermes.mjs` exists (full ask-ollama parity) and ALREADY records `byHook["ask-hermes"]` into the canonical `mcp-server/data/state/ollama-offload-stats.json` (HERMES-UTIL-TRACK 2026-06-14). But `gradeHermesUtilization()` (`scripts/reconcile-zulu-ledger.mjs:382`) read **"no ask-hermes activity recorded"** -- the lane was genuinely DARK: nothing automated invoked it.
- Ollama lane: hooks FIRE but `executedOffloads=0, bridgeExecutions=0` -- `gradeOllamaUtilization()` already diagnoses "offload DECISIONS firing but 0 executed". The offloader hook is ADVISORY-only (no enqueue). Shared root: lanes are advisory, nothing auto-actuates.
- `scripts/lib/ollama-verified-offload.mjs` (`verifiedOffload`, alpha 2026-06-09) is Ollama-only -- NO Hermes tier.

## What shipped (Unit A)
`scripts/lib/verified-offload-tiered.mjs` (+ `.test.mjs` 19/19). COMPOSITION not a fork (R8): `verifiedOffload` already takes an injected `run` thunk, so a tiered offload = apply it across Hermes(strong) -> Ollama(free) -> trusted-fallback. The SAME pure verifier gates EVERY tier -> a hallucinated strong (Grok) answer is REJECTED exactly like a hallucinated Ollama answer, never trusted (the load-bearing safety invariant + adversarial test). Reuses ask-hermes's exported pure helpers (`buildChatBody`/`parseChatResponse`/`tallyUsage`/`estimateHermesSaved`); `recordTieredUsage` clones ask-hermes's atomic-RMW telemetry (never creates the stats file, never throws). `makeHermesRunner`/`makeOllamaRunner` build the real runners (injectable fetch/caller for hermetic R9).
- **LIVE PROOF (loss-function):** a real classify offload against live Hermes returned `source=hermes value=physics verified=true` and moved `byHook["ask-hermes"].fired 0 -> 1, bySource {hermes:1}, tokensSaved 53`. gradeHermesUtilization will now read UTILIZED.

## Scrutiny (R12 honest)
Arm A (code-analyzer) **PASS** -- deep independent pass, opened both source files, traced sentinel/descend, the verifier-every-tier invariant, predicate parity with tallyUsage, import-safety; 0 P0/P1, 3 non-blocking P2s (timeout-knob inconsistency makeOllamaRunner hardcodes 30000 vs the env knob on makeHermes; one hand-rolled no-ollama-runner record shape; tmp-orphan matching the existing fleet pattern). Arm B (reviewer) **could not run -- subagent session/quota limit (resets 12:10pm)**; the arm-B concerns (contract drift, convention, error-budget, orphan) were done inline. NOT claimed as a 2-arm pass.

## Unit B SHIPPED (commit a6a6243a2a) -- CLI wire + live-found bug fix
Wired the tiered primitive into the canonical `scripts/ollama-offload.mjs` CLI: `offloadClassifyStrong`/`offloadDigestStrong` + CLI modes `classify-strong`/`digest-strong`, reusing the BYTE-IDENTICAL classify/digest prompts+verifiers+safe-floor of the non-strong siblings (R8/R11 additive; existing paths untouched). **Live validation found a real bug (R12):** `makeHermesRunner` sent `model:undefined` when the CLI omitted `--hermes-model` -> the Hermes proxy rejected -> the strong tier SILENTLY DESCENDED and never fired (masked in Unit A because the test/proof always passed an explicit model). Fixed: `makeHermesRunner` now defaults `model` to `PRISM_HERMES_MODEL || PRISM_HERMES_FALLBACK_MODEL || "grok-4.3"` (mirrors ask-hermes.mjs:84) + a `!model` null/"" guard; +1 regression test. LIVE post-fix: `node scripts/ollama-offload.mjs classify-strong "...turning 4140 on a lathe" mill lathe wedm grinding` -> `source=hermes value=lathe verified tier=strong`. 24/24 offload + 20/20 tiered tests. **2-arm scrutiny BOTH PASS** (this time both ran; mutation-tested -- verifier-gate revert turned adversarial tests RED, model-default revert turned the regression test RED; 0 P0/P1/P2). Lesson: a default-able model id must be defaulted at the runner, not assumed from the caller -- a missing model silently degrades a "strong" lane to its fallback.

## Unit C is NOT needed -- substrate-utilization audit complete (2026-06-24, evidence-based R12)
A direct, evidence-based audit (post-/compact, same session) showed the operator's "engineered loops/harnesses/crons" for these substrates ALREADY EXIST and FIRE. Building a new Unit C cron/harness would be a dedup-law violation + the exact metric-gaming make-work flagged above. DO NOT build it. Evidence (all live this session):
- **Hermes lane LIT (Units A+B) AND a cron fleet already drives it:** 8 registered Hermes scheduled tasks (`PRISM Hermes {Vault Digest, Dream-Cycle Synth, GEPA Weekly, Proxy, Self-Reflect Weekly, Skill Loop, Prewarm}` + `Hermes-Obsidian Bridge`). `PRISM Hermes Vault Digest` (registered by `.claude/helpers/install-hermes-vault-digest-task.ps1`, runs `scripts/hermes-vault-digest.mjs`) fires every ~4h (LastTaskResult 0; outputs in `knowledge/hermes-outputs/research/vault-digest-*` at 02:37/06:37/10:37/14:37). It synthesizes vault notes through `ask-hermes` ($0 Claude) -> raises BOTH Hermes + Obsidian utilization. `ask-hermes` byHook: fired 3, 0% fail, fresh -> `gradeHermesUtilization` reads UTILIZED.
- **Ollama:** offloaded 6 / executed 2, no adoption gap -> UTILIZED.
- **Octopus:** `consensus-queue-processed.jsonl` 402 processed, `consensus-decisions.jsonl` 412 (last ~3min old), 17 queued, fresh drain -> UTILIZED (healthy trickle-drain). NOTE: octopus = bravo/zulu territory (MultiModelConsensusEngine / hermes-zulu galaxy), NOT alpha's domain_filter -- surface, don't build.
- **PSN / system-viz:** saturated injectors firing every prompt (PSN 2558 hits/~575k tok; master-index/node-card/graph). system-viz = sierra's domain.
RAW HARNESS NOTE (R7): `hermes-vault-digest.mjs` uses `ask-hermes --no-fallback` (Hermes-specific, no verifier) -- deliberately NOT routed through my verifiedTieredOffload, because tiering would credit `ollama-fallback` on a :8645-down and dilute its Hermes-raising intent. Leave it; do not "improve" it into the tiered path.

## Queued threads
- **`ollama-route-pretooluse` 231 fired / 0 offloaded / 231 kept / 0 suggested -- RESOLVED, correct-by-design (READ the hook 2026-06-24, R12).** The PreToolUse:Read hook fires on EVERY Read but only routes BULK-DATA targets (logs/jsonl/csv/big reports under state/ or data/state/ >=24KB). Source files, sub-24KB files, and the EXEMPT_BASENAMES load-bearing JSONs all return decision:"pass" -> kept. 231 kept = 231 source/small/exempt reads that MUST be kept (can't summarize a .ts you're about to edit). suggested:0 = none of the 231 were a bulk >=24KB consumable file this window. Default mode is `suggest` (nudge-only); `reroute` (actual offload) only fires in auto-mode + gist-safe (logs/dumps/archives) + ollama-up + model-in-allowlist. It is a heavily-scrutinized conservative gate (narrow gist-safe allowlist, operator-ratified 2026-06-03 per reference_blackwell_token_synergy_ms0_2026_06_03; cold-start timeout 9s->30s already fixed by zulu 2026-06-10). NOT a defect; do NOT modify. NO fix needed.
- P2 follow-ups: ~~unify makeOllamaRunner timeout knob with the Hermes one~~ SHIPPED 2026-06-24 (`U-HERMES-TIER-OLLAMA-TIMEOUT-KNOB`, commit 119a1c557d, operator "continue hardening"): exported `DEFAULT_OLLAMA_TIMEOUT_MS` (`PRISM_TIERED_OLLAMA_TIMEOUT_MS||30000`) + NaN-hardened BOTH tier constants to `Number(X)||default`; +1 R9 forwarding/default test; tiered 21/21, consumer 24/24; 2-arm scrutiny BOTH PASS. STILL QUEUED: the optional additive `"ollama"` local source in `tallyUsage` -- touches the SHARED `ask-hermes.mjs#tallyUsage` predicate (a verified local Ollama result currently tags `ollama-fallback`, mislabeling the `byHook["ask-hermes"]` source+fired for a tier:'local' call). Genuine telemetry-honesty fix but needs its own scoped unit + chat-bus claim on ask-hermes.mjs (foreign claims live).
- **SHIPPED 2026-06-24 `U-FILE-DIGEST-OFFLOAD-RECORD` (commit b46945b8c3, operator "continue hardening"):** `ollama-file-digest.mjs` (verified line-anchored digest, "the headline free-token lever") recorded NOTHING -> its $0-Claude offloads were 100% INVISIBLE to every utilization measurement + the large-read-digest advisory-decay gate (could wrongly suppress a working nudge). Added exported fail-safe `recordFileDigestOffload` bumping `byHook["ollama-file-digest"]` {fired,offloaded>0,tokensSaved} (the offload-dashboard live-byHook scan auto-recognizes an offloaded>0 bucket as a real off-Claude run); called from CLI main() ONLY on a verified ollama run (source==ollama && verified && !fellBack); R7 does NOT bump the ask-ollama-scoped `executedOffloads` (file-digest runs via callOllamaOnce, distinct lane). +4 R9 tests; 2-arm scrutiny BOTH PASS (0 findings); live-shape validated on a copy of real stats (bucket undefined -> {fired:1,offloaded:1,tokensSaved:1234}). NOTE: a `[WorkClaim]` ghost warning (`DESKTOP--93664`, a mangled/stale label) fired on the edit; the `git diff` showed ONLY my hunks + host is sole-user -> false alarm, verified by the diff, not assumed (do not revert genuine work on a ghost claim -- check the actual diff). FOLLOW-UP (harder, queued): correlate the large-read-digest advisory's take-RATE with these conversions (needs a pending-suggestion marker handshake so the decay gate sees a real take-rate, not always-0).
- **SHIPPED 2026-06-24 `U-LOCAL-OFFLOAD-VISIBLE` (commit af265b7bca):** the R15 sibling of the file-digest unit -- the LOCAL `ollama-offload.mjs` CLI modes (classify/digest/digest-files) recorded NOTHING (only the *-strong tiers logged byHook["ask-hermes"]), so a successful $0-Claude local offload was 100% invisible. Added exported `recordLocalOffload` -> byHook["ollama-offload-cli"] {offloaded>0, byMode}; wired the 3 local mode handlers in main() on a verified ollama run; R7 leaves executedOffloads untouched; classify-strong/digest-strong correctly skip it (record via recordTieredUsage, no double-count). +4 R9 tests; 28/28; 2-arm scrutiny BOTH PASS (P2-only). **DEDUP FOLLOW-UP (logged, queued):** the atomic-RMW offload-stats-bump pattern now has ~5 copies (ollama-route `updateOffloadStats`, advisory `bumpStats`, `recordTieredUsage`, `recordFileDigestOffload`, `recordLocalOffload`) -- extract a shared `scripts/lib/offload-stats-bump.mjs` helper in a separate cleanup unit (don't refactor the 5 live sites under time pressure).
- **Take-rate correlation deferred (R10 honesty):** the large-read-digest advisory take-rate fix has NO clean tight design -- the precise version needs a stateful nudge->digest marker handshake (lifecycle+races); the cheap "credit any qualifying digest" version drifts the bucket semantics + partly defeats the decay gate's suppress-the-useless purpose. Needs deliberate design, NOT a rushed pass.
Related: [[reference_alpha_autoloop_telemetry_exhaustion_2026_06_20]], [[reference_harness_loop_cron_upgrade_plan_2026_06_18]].
