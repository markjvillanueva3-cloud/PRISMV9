---
name: reference_code_engine_supplychainintegrityengine
description: "Supply Chain Integrity"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SupplyChainIntegrityEngine.ts
  sourceHash: ae9172cc
  loc: 536
aliases: SupplyChainIntegrityEngine
---

# SupplyChainIntegrityEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SupplyChainIntegrityEngine.ts` (536 LOC)

## What it is
Supply Chain Integrity

```
SupplyChainIntegrityEngine (U-LPR-SEC09)

Defends the LoRA training and serving pipeline against supply-chain
tampering. Four pillars per the Phase-9 plan:

  1. Pinned base-weight registry — register the expected SHA-256 for
     each base model (e.g. Qwen2.5-Coder-7B). Any bootstrap whose
     observed digest differs is quarantined. Mirrors the Reproducible
     Builds pinned-artifact pattern; implements NIST SP 800-161r1
     C-SCRM §3.1.3 (pedigree + provenance).

  2. Training-set manifest — SHA-256 Merkle root over the sorted
     (file_path, sha256, bytes) tuples used to train a given adapter.
     Change any byte of any training file → root changes → U-LPR27/28
     integrity check fails. Prevents silent dataset poisoning.

  3. Sigstore-style adapter signatures — each .safetensors/.gguf
     adapter is signed by a named issuer. Verification checks both
     the artifact digest A
```


## Exports
`ArtifactType` (type), `VerificationOutcome` (type), `SeverityLevel` (type), `PinnedArtifact` (interface), `TrainingManifest` (interface), `AdapterSignature` (interface), `OSVAdvisory` (interface), `VerificationResult` (interface), `SupplyChainStats` (interface), `SupplyChainIntegrityEngine` (class), `supplyChainIntegrityEngine` (const)

## Key methods
`pinArtifact`, `rotatePin`, `getPin`, `listPins`, `registerTrainingManifest`, `getManifest`, `listManifests`, `verifyManifest`, `recordAdapterSignature`, `listAdapterSignatures`, `registerAdvisory`, `clearAdvisory`, `listAdvisories`, `findBlockingAdvisory`, `verifyArtifact`, `listVerifications`, `getStats`, `clearAll`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/SupplyChainIntegrityEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SupplyChainIntegrityEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SupplyChainIntegrityEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
