---
name: reference_code_engine_subscriptionstore
description: "Persisted user -> subscription mapping. This is the missing layer that made"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SubscriptionStore.ts
  sourceHash: 7c679cd5
  loc: 166
aliases: SubscriptionStore
---

# SubscriptionStore -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SubscriptionStore.ts` (166 LOC)

## What it is
Persisted user -> subscription mapping. This is the missing layer that made

```
PRISM MCP Server -- SubscriptionStore (U-COMM-03 keystone)

Persisted user -> subscription mapping. This is the missing layer that made
entitlement enforcement dormant: tierGate.requireTier reads req.user.plan, but
nothing ever resolved a user's plan. attachUserPlan middleware reads from here.

Design:
  - In-memory Map for synchronous getPlan() (middleware hot path).
  - Lazy load from disk on first access (sync readFileSync).
  - Writes via safeWriteSync (temp + rename).
  - FAIL LOUD on an existing-but-unreadable file -- never reset-then-clobber a
    populated store (lesson: reference_tribal_index_v8_string_cap clobber class).

Source of truth for PLAN VALUES is config/pricing-registry.ts (Plan enum via
AuthEngineV7). This store only records WHICH plan each user is on.
```


## Exports
`SubscriptionStatus` (type), `SubscriptionRecord` (interface), `SubscriptionStore` (class), `subscriptionStore` (const)

## Key methods
`safeWriteSync`, `getPlan`, `getRecord`, `setPlan`, `linkCustomer`, `getUserIdByCustomer`, `getStripeCustomerId`, `listUserIds`, `invalidate`

## Depends on
[[reference_code_engine_authenginev7]]

## Where it lives
- **Source:** `mcp-server/src/engines/SubscriptionStore.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SubscriptionStore` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SubscriptionStore.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
