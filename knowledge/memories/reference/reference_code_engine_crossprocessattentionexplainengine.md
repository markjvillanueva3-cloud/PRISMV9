---
name: reference_code_engine_crossprocessattentionexplainengine
description: "interpretability + calibration +"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CrossProcessAttentionExplainEngine.ts
  sourceHash: 8cb898a1
  loc: 545
aliases: CrossProcessAttentionExplainEngine
---

# CrossProcessAttentionExplainEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CrossProcessAttentionExplainEngine.ts` (545 LOC)

## What it is
interpretability + calibration +

```
CrossProcessAttentionExplainEngine — interpretability + calibration +
anomaly detection for the T1-02 MLP classifier.

Milestone: INFRA-NEURAL-LEDGER-MS1 / U-XPROC-NEURAL-T1-04.

Why this exists
---------------
T1-02 produces a P(success/failure/operator_override) distribution but
is otherwise opaque. Three failure modes need to be addressable before
the model can drive shop-floor decisions:

  1. "Why did the model say 0.85 success?" — feature attribution so
     operators can sanity-check that the prediction is grounded in
     the right inputs (e.g. material + spindle_rpm) rather than a
     hash-collision artifact in the categorical buckets.
  2. "Should we trust 0.85 or is the model overconfident?" —
     calibration (ECE) so we know whether 0.85 means roughly 85%
     empirical success or whether the model is systematically over-
     or under-confident.
  3. "Is this run shaped li
```


## Exports
`ATTENTION_SCHEMA_VERSION` (const), `ProbClass` (type), `LimeOpts` (interface), `FeatureContribution` (interface), `LimeExplanation` (interface), `CalibrationBin` (interface), `CalibrationReport` (interface), `BaselineStats` (interface), `AnomalyScore` (interface), `CrossProcessAttentionExplainEngine` (class), `crossProcessAttentionExplainEngine` (const)

## Key methods
`explainPrediction`, `computeECE`, `registerBaseline`, `scoreAnomaly`, `getBaseline`, `resetBaseline`

## Depends on
[[reference_code_engine_crossprocessneurallearningengine]] | [[reference_code_engine_crossprocessoutcomestore]]

## Where it lives
- **Source:** `mcp-server/src/engines/CrossProcessAttentionExplainEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CrossProcessAttentionExplainEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CrossProcessAttentionExplainEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
