---
name: reference_code_engine_kaizenleansigmaengine
description: "KaizenLeanSigmaEngine — continuous-improvement substrate for every employee."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/KaizenLeanSigmaEngine.ts
  sourceHash: 92aa14a4
  loc: 712
aliases: kaizenLeanSigmaEngine
---

# kaizenLeanSigmaEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/KaizenLeanSigmaEngine.ts` (712 LOC)

## What it is
```
KaizenLeanSigmaEngine — continuous-improvement substrate for every employee.

Three subsystems in one cohesive ledger:

  1. DOWNTIME 8-waste observation log (`observeWaste`, `wasteLedger`)
     Any employee can log a waste observation (D-O-W-N-T-I-M-E):
       Defects · Overproduction · Waiting · Non-utilized talent ·
       Transportation · Inventory · Motion · Extra processing
     Each entry stamps employee_id, machine_serial (optional), gemba flag,
     severity, and optional impact_minutes for prioritization.

  2. DMAIC kaizen-event lifecycle (`openEvent`, `advanceEvent`, `closeEvent`)
     Define → Measure → Analyze → Improve → Control. Cannot skip phases
     (R12 fail-loud). Captures sponsor (manager) + champion (employee). On
     close, requires baseline + post Cp/Cpk + sustainment-plan hook.

  3. Six Sigma capability gate (`calculateCpk`, `sixSigmaGate`)
     Cp = (USL - LS
```


## Exports
`LeanWaste` (type), `DmaicPhase` (type), `EventStatus` (type), `SuggestionStatus` (type), `CapabilityVerdict` (type), `WasteObservation` (interface), `DmaicEvent` (interface), `DmaicTransition` (interface), `Suggestion` (interface), `CpkResult` (interface), `kaizenLeanSigmaEngine` (const)

## Key methods
`observeWaste`, `wasteLedger`, `wasteSummary`, `openEvent`, `advanceEvent`, `closeEvent`, `listEvents`, `calculateCpk`, `sixSigmaGate`, `submitSuggestion`, `triageSuggestion`, `listSuggestions`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/KaizenLeanSigmaEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find kaizenLeanSigmaEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/KaizenLeanSigmaEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
