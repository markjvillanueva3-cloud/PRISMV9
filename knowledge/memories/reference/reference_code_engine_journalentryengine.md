---
name: reference_code_engine_journalentryengine
description: "Memorized / Recurring / Reversing journal entries for the PRISM ERP"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/JournalEntryEngine.ts
  sourceHash: 96738d87
  loc: 418
aliases: JournalEntryEngine
---

# JournalEntryEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/JournalEntryEngine.ts` (418 LOC)

## What it is
Memorized / Recurring / Reversing journal entries for the PRISM ERP

```
JournalEntryEngine — Memorized / Recurring / Reversing journal entries for the PRISM ERP
(galaxy:business, slot:hotel). QuickBooks-parity: the "Memorized / Recurring / Reversing"
journal-entry function set (QB-PARITY Phase-4 #2).

This is the SCHEDULING / TEMPLATE layer that GeneralLedgerEngine lacks. It does NOT reimplement
double-entry, the chart of accounts, balance validation, or the financial statements — it REUSES
GeneralLedgerEngine for all of that:
  - memorize(template)            → validates the template balances NOW by mirroring GL's exact
                                    balance contract (Σdebits == Σcredits, ≥2 lines, known
                                    accounts) via GeneralLedgerEngine.CreateJournalEntryInputSchema
                                    + the GL chart. Never stores an unbalanced template.
  - materialize(template, opts)   → produces a concrete, balanc
```


## Exports
`MemorizeTemplateInput` (type), `MaterializeOptions` (type), `ScheduleRecurringOptions` (type), `ReversingOptions` (type), `CreateJournalEntryInput` (type), `MemorizedTemplate` (interface), `RecurringOccurrence` (interface), `JournalEntryEngine` (class), `journalEntryEngine` (const)

## Key methods
`memorize`, `materialize`, `scheduleRecurring`, `createReversing`

## Depends on
[[reference_code_engine_generalledgerengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/JournalEntryEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find JournalEntryEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/JournalEntryEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
