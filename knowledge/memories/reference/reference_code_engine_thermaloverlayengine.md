---
name: reference_code_engine_thermaloverlayengine
description: "Real-time Cutting Temperature Overlay (U-CAM93)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ThermalOverlayEngine.ts
  sourceHash: 172febac
  loc: 336
aliases: ThermalOverlayEngine
---

# ThermalOverlayEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ThermalOverlayEngine.ts` (336 LOC)

## What it is
Real-time Cutting Temperature Overlay (U-CAM93)

```
ThermalOverlayEngine — Real-time Cutting Temperature Overlay (U-CAM93)
========================================================================

PHASE-7 Intelligent Vericut: Real-time cutting-zone thermal visualization
for the four CAM plugin adapters (hyperMILL, Fusion 360, Inventor HSM,
Mastercam X8). Consumes the `temperature` sub-object of the PhysicsOverlay
emitted by PRISMVerificationPluginEngine.analyzePoint() and produces
render-ready frames showing current cutting temperature and thermal damage
risk.

Visual semantics:
  nominal    → green   — within material-safe operating range
  elevated   → amber   — rising thermal risk, coolant advised
  critical   → red     — thermal damage zone, tool life collapsing
  transition → magenta — status flip detected this frame

Unlike the deflection overlay (which uses nominal/warning/critical) the
temperature sub-object uses "elevated" for it
```


## Exports
`HexColorSchema` (const), `ThermalStateSchema` (const), `ThermalState` (type), `ThermalOverlayTargetSchema` (const), `ThermalOverlayTarget` (type), `ThermalOverlayFrameSchema` (const), `ThermalOverlayFrame` (type), `ThermalOverlayStatsSchema` (const), `ThermalOverlayStats` (type), `ThermalOverlayEngine` (class), `thermalOverlayEngine` (const)

## Key methods
`renderFrame`, `getStats`, `resetSession`, `supportedTargets`

## Depends on
[[reference_code_engine_prismverificationpluginengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/ThermalOverlayEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ThermalOverlayEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ThermalOverlayEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
