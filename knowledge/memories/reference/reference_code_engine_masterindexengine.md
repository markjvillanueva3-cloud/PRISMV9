---
name: reference_code_engine_masterindexengine
description: "MasterIndexEngine — Unified Master Search Across Obsidian Vault + System-Viz + Awareness"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MasterIndexEngine.ts
  sourceHash: eda6b1af
  loc: 1140
aliases: masterIndexEngine
---

# masterIndexEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MasterIndexEngine.ts` (1140 LOC)

## What it is
```
MasterIndexEngine — Unified Master Search Across Obsidian Vault + System-Viz + Awareness

Goal: ONE query call replaces N Grep/Glob/Agent calls. Future Claude/Codex
sessions hit `prism_session:master_index_query` first; only fall back to
filesystem search when this returns no high-confidence hits.

Fuses 4 pre-built indexes (no parallel infra — all sources already exist):
  1. system-graph.json (110K nodes / 114K edges, 11 layers).
     Each node already carries pre-joined `knowledge.wikiEntries[]` +
     `knowledge.memoryEntries[]` from system-viz-obsidian-bridge-v2.mjs.
  2. PRISMSelfAwarenessEngine.findCapabilities — fuzzy match across
     engines / actions / hooks / skills with confidence scoring.
  3. BUILD_STATE.json — built / unwired / pending / frontend classification
     so every hit is labelled with its lifecycle stage.
  4. Graph edge in-degree → utilization score [0,1]. Let
```


## Exports
`MasterIndexHit` (interface), `MasterIndexQueryOptions` (interface), `MasterIndexResult` (interface), `NodeStatus` (interface), `UtilizationClass` (type), `UtilizationDashboard` (interface), `NodeUtilizationRow` (interface), `masterIndexEngine` (const)

## Key methods
`clearCache`, `safeReadJson`, `query`, `getNodeStatus`, `classifyAllNodes`, `vaultStats`

## Depends on
[[reference_code_util_logger]] | [[reference_code_engine_prismselfawarenessengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/MasterIndexEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find masterIndexEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MasterIndexEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
