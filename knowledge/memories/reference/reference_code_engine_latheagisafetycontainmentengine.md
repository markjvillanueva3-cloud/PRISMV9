---
name: reference_code_engine_latheagisafetycontainmentengine
description: "LatheAGISafetyContainmentEngine — U-LTH61 (LATHE-MASTER PX-S2)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LatheAGISafetyContainmentEngine.ts
  sourceHash: 38eac698
  loc: 377
aliases: latheAGISafetyContainmentEngine
---

# latheAGISafetyContainmentEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LatheAGISafetyContainmentEngine.ts` (377 LOC)

> [physics] Imports canonical physics constants (`src/physics/constants.ts`) -- safety-relevant node.

## What it is
```
LatheAGISafetyContainmentEngine — U-LTH61 (LATHE-MASTER PX-S2)

Bounds AGI outputs against physics envelopes, cost margins, and shop
capacity. Every AGI recommendation must pass this gate before being
consumed by P1..P5. Failures return a structured rejection with a
trace of which check failed and the bound that was violated.

Check categories:
  - physics     — cutting speed within [Vc_min, Vc_max] per ISO group,
                  feed within fz_max, force within spindle torque margin
  - economics   — quote margin ≥ 0.10 (10% minimum), not 0.00 or negative
  - capacity    — schedule total hours ≤ fleet_capacity_hours
  - kinematic   — live-tool/sub-spindle required but not present

Each check has soft+hard thresholds. Soft = warning. Hard = block.

Citations:
  - Sandvik Turning Handbook (Vc envelopes)
  - CANONICAL_KIENZLE (src/physics/constants.ts)
  - JM Die shop-floor margin minimu
```


## Exports
`SpeedFeedCandidateSchema` (const), `SpeedFeedCandidate` (type), `QuoteCandidateSchema` (const), `QuoteCandidate` (type), `ScheduleCandidateSchema` (const), `ScheduleCandidate` (type), `KinematicCandidateSchema` (const), `KinematicCandidate` (type), `SafetyCheckInputSchema` (const), `SafetyCheckInput` (type), `Severity` (type), `CheckResult` (interface), `SafetyReport` (interface), `latheAGISafetyContainmentEngine` (const), `LatheAGISafetyContainmentEngine` (re-export)

## Key methods
`check`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/LatheAGISafetyContainmentEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find latheAGISafetyContainmentEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LatheAGISafetyContainmentEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
