---
name: reference_code_script_cimcobindgate
description: "CIMCO machine + controller BIND GATE — U-CIMCO-SIM-4."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/cimco-bind-gate.mjs
  sourceHash: ca91817a
  loc: 223
aliases: cimco-bind-gate.mjs
---

# cimco-bind-gate.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/cimco-bind-gate.mjs` (223 LOC)

## What it is
```
CIMCO machine + controller BIND GATE — U-CIMCO-SIM-4.

After the driver loads a machine into CIMCO Edit's Machine-Simulation setup,
this PURE gate asserts that what is ACTUALLY LOADED matches the machine the
post is FOR — before any sim run is allowed to count toward a verdict. It
closes three load-time traps, all fail-CLOSED:

  1. kinematic-mismatch — loaded sim .mcfg ≠ the resolved JM machine's .mcfg
     (proving a post against the wrong/another machine).
  2. wrong-controller-post — a Haas PRE-NGC machine (VMC-03/04) loaded with
     the NGC RPost instead of the VR/classic variant (different G/M dialect →
     a "clean" sim of the wrong post is worthless).
  3. 25.4× units — every CIMCO .mcfg is mm and JM convention is INCH; an inch
     NC simulated against a mm machine is a 25.4× scale-error. Units must be
     DECLARED + matching on both NC and .mcfg, never inferred (spec §E1).


```


## Exports
`BIND_BLOCKERS` (const), `isEdmMachine` (function), `isPreNgcHaas` (function), `classifyHaasPostGeneration` (function), `synthesizeMockReadback` (function), `assessMachineBind` (function)

## Key methods
`norm`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/cimco-bind-gate.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find cimco-bind-gate.mjs` or `prism_session:master_index_query`; open the source at `scripts/cimco-bind-gate.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
