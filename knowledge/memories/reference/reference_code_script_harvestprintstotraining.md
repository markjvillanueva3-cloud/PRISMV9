---
name: reference_code_script_harvestprintstotraining
description: "scripts/harvest-prints-to-training.mjs"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/harvest-prints-to-training.mjs
  sourceHash: e6d05f35
  loc: 258
aliases: harvest-prints-to-training.mjs
---

# harvest-prints-to-training.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/harvest-prints-to-training.mjs` (258 LOC)

## What it is
```
scripts/harvest-prints-to-training.mjs

U-TDP02 — Batch print harvester CLI shell.

Walks a directory of blueprint PDFs, runs the U-TDP01 training driver on each
NEW print (skipping already-processed ones via path-id registry), persists the
registry atomically after each pass.

USAGE:
  node scripts/harvest-prints-to-training.mjs --dir <root> [--default-part-class C] [--max N] [--stub-mode] [--force] [--json] [--dry-run]

EXAMPLE (smoke-test against JM DIE corpus, capped to 5 prints):
  node scripts/harvest-prints-to-training.mjs --dir "H:/prism/JM DIE" --max 5 --stub-mode --json

REGISTRY: state/shared/print-harvester-registry.json (override via env)
EVENTS:   state/shared/blueprint-accuracy-events.jsonl (feeds U-BPA-CONSUMER)

EXIT CODES:
  0 — all training-driver record stages succeeded (or no new files)
  2 — at least one record stage failed (silent training-signal loss prevented)
  
```


## Exports
_(no top-level exports detected)_

## Key methods
`walk`, `writeFileSync`, `renameSync`, `exit`, `atomicWriteJson`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/harvest-prints-to-training.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find harvest-prints-to-training.mjs` or `prism_session:master_index_query`; open the source at `scripts/harvest-prints-to-training.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
