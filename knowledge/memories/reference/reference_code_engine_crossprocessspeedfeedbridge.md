---
name: reference_code_engine_crossprocessspeedfeedbridge
description: "CrossProcessSpeedFeedBridge -- unified speed/feed recommendation across mill,"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CrossProcessSpeedFeedBridge.ts
  sourceHash: fd15e1fc
  loc: 136
aliases: CrossProcessSpeedFeedBridge
---

# CrossProcessSpeedFeedBridge -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CrossProcessSpeedFeedBridge.ts` (136 LOC)

## What it is
CrossProcessSpeedFeedBridge -- unified speed/feed recommendation across mill,

```
CrossProcessSpeedFeedBridge -- unified speed/feed recommendation across mill,
lathe, and wire-EDM, delegating to the canonical SpeedFeedOrchestratorEngine.

Sibling "speedfeed" bridge referenced by CrossProcessAIBridge
(@see XPROC-SFC-01) and consumed by ProcessIntelligenceRouterEngine's pipeline
`speedfeed` stage. PURE ROUTING + DELEGATION: the bridge carries NO feed/speed,
physics, or material VALUES of its own -- every numeric result comes from the
already-built, already-tested speedFeedOrchestratorEngine.compute (R8: reuse,
do not re-derive cutting physics here). Process classification reuses the
verified CrossProcessAIBridge.classify scorer.

Two entry points (mirrors CrossProcessAIBridge.orchestrate semantics):
  recommend(req) -- classify + delegate to the SF orchestrator (or dry-run
                    preview that returns only the routing decision).

Fail-loud (R12): a non-dry-r
```


## Exports
`CrossProcessSFRequest` (interface), `CrossProcessSFResult` (interface), `CrossProcessSpeedFeedBridge` (class)

## Key methods
`recommend`

## Depends on
[[reference_code_engine_crossprocessaibridge]]

## Where it lives
- **Source:** `mcp-server/src/engines/CrossProcessSpeedFeedBridge.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CrossProcessSpeedFeedBridge` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CrossProcessSpeedFeedBridge.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
