---
name: reference_c2_producer_and_lock_2026_06_18
description: "C2 (ZuluTaskContinuityEngine) fully finished this session (slot:bravo, 3 commits). It was a CONFIRMED ORPHAN -- built + dispatcher-wired (continuity_checkpoint/resume/list) but NO production code ever called checkpoint(), so the recovery oracle was never fed and resume() always returned empty. wave_loop_step (sessionDispatcher) is its FIRST producer: resume(unit_id) -> ZuluWaveSchedulerEngine.mergeCompleted -> governedNextWave -> checkpoint the advanced state back, so a multi-wave build survives /compact + a self-startup re-entry resumes it. Shipping the producer ACTIVATED a latent concurrency gap (checkpoint/clear did an unlocked RMW of the whole records map) -> added a cross-process O_EXCL lockfile, then hardened it to nonce-based content-verified ownership after scrutiny found a reap-collision TOCTOU + release-by-path hazard."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.490Z
aliases: reference_c2_producer_and_lock_2026_06_18
---


# C2 producer + lock -- the orphan-close + the latent concurrency it activated (2026-06-18, slot:bravo)

## The 3 commits (Hermes capability C2, all 3-of-3 PASS)
1. **f4c075a252 U-C2-PRODUCER-WAVE-LOOP** -- closes the orphan. `ZuluWaveSchedulerEngine.mergeCompleted(prior, newly)` (order-stable dedup, drops non-string/empty/non-array) + `loopCheckpointState(completed, execution)` (the opaque continuity `state`; `phase` flips to `wave-loop-done` on the terminal wave). New `sessionDispatcher` action **`wave_loop_step`**: `zc.resume(unit_id)` -> mergeCompleted -> `governedNextWave` (C1 governance) -> `zc.checkpoint(loopCheckpointState(...))`. 67/67 tests incl a dispatcher round-trip e2e (`sessionDispatcher.waveLoopStep.e2e.test.ts`) that PROVES the checkpoint lands by reading the temp store. A bad unit_id -> `checkpointed:false` (R12, never a silent producer miss).
2. **b7272a140a U-C2-CHECKPOINT-LOCK** -- the producer made a latent lost-update reachable: `checkpoint`/`clear` did an unlocked read-modify-write of the WHOLE records map, so two slots writing DIFFERENT unit ids could lost-update each other (atomic tmp+rename prevented torn files, NOT lost updates). The header docstring even falsely claimed it "mirrors slot-task-claim.mjs discipline" (which IS lockfile-guarded). Added a sync **O_EXCL lockfile** (`<store>.lock`) around both writers + a stale/dead-PID reaper; checkpoint timeout -> `ok:false` (has an error channel; hot loop retries), clear timeout -> THROW (boolean, fail-loud). Readers (resume/list) stay lock-free (atomic rename = always a complete store). Lock budget ctor-injectable (`__forTests` lockCfg) for fast tests.
3. **96b0e72245 U-C2-LOCK-OWNERSHIP-VERIFY** -- 3-of-3 of #2 PASSED but reviewer C flagged a P2 reap-collision TOCTOU (two reapers both `unlinkSync` -> double-holder) + a P3 release-by-path (a stalled holder's release unlinks a peer's lock). Closed by making the lock **held by CONTENT (a unique per-acquire nonce)**: verify-own-write (acquire re-reads + confirms our nonce survived), snapshot-guarded reap (only unlink if the file is STILL the exact stale bytes), release unlinks only if the nonce is still OURS (fully closes P3). Residual = an irreducible microsecond TOCTOU (no OS flock in pure sync Node) -- honestly documented, shrunk to near-zero.

## Two lessons that generalize (FLEET-WIDE)
- **"existence != working" -- a dispatcher-wired engine with NO production producer is an ORPHAN.** C2 looked done (engine + 3 dispatcher actions + tests) but `checkpoint()` was never called by real code, so the recovery oracle was never fed. When you find a "built + wired" capability, grep for a REAL producer/consumer before trusting it. Closing the orphan = wiring the first producer, proven by a round-trip that reads the durable store (not just the singleton). -> [[feedback_read_full_content_not_titles]]
- **Shipping the first producer of a dormant store ACTIVATES its latent concurrency gaps.** The unlocked RMW was harmless while nothing wrote; the producer made the multi-slot lost-update reachable. When you light up a dormant write path, scrutinize the store's concurrency (locking, atomicity, lost-update) as part of the SAME work -- the gap was always there, your producer just armed it. R16 (loop until gaps closed): scrutiny found the lock gap, then a TOCTOU in the lock, then the irreducible limit -- each iteration closed a real scrutiny-flagged issue, converging to the practical correctness limit.

## Integration seam (consumers must know)
`ok()` -> `slimResponse` (responseSlimmer.ts) DROPS EMPTY ARRAYS fleet-wide for transport efficiency. So `wave_loop_step`'s `completed_ids`/`wave_assignments` come back ABSENT (not `[]`) when empty -- a consumer (runtime loop driver) MUST default absent -> `[]` (same contract as C1's `governed_wave_execute`). The authoritative empty set lives in the UNSLIMMED durable continuity store, not the response.

## State
C2 is FINISHED (orphan closed + producer + correct lock). Next Hermes capability: C3 (Fleet Health Synthesis, `ZuluFleetHealthSynthesisEngine` exists -- partial), then C4 (Delegation Contract, `ZuluDelegationContractEngine` exists). Related: [[reference_c1_governor_gate_2026_06_17]], [[reference_self_startup_ms0_2026_06_17]] (the self-startup re-entry that resumes a wave_loop_step build across /compact).
