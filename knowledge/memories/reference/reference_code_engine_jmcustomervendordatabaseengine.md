---
name: reference_code_engine_jmcustomervendordatabaseengine
description: "query layer on top of iter56's JSONL"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/JMCustomerVendorDatabaseEngine.ts
  sourceHash: 9bfd04b2
  loc: 211
aliases: JMCustomerVendorDatabaseEngine
---

# JMCustomerVendorDatabaseEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/JMCustomerVendorDatabaseEngine.ts` (211 LOC)

## What it is
query layer on top of iter56's JSONL

```
JMCustomerVendorDatabaseEngine — query layer on top of iter56's JSONL
customer + vendor databases.

QUOTING-SYNERGY-MS0/U-QP-CUSTOMER-VENDOR-DB-QUERY (slot:charlie iter57 2026-05-27).

Reads:
  state/shared/databases/jm-customers.jsonl   (473 records, 152KB)
  state/shared/databases/jm-vendors.jsonl     (12 records, 3.4KB)

Exposes:
  - listCustomers(opts?)        → all customers, optionally filtered
  - getCustomer(key)            → single customer record by canonical key
  - searchCustomers(query)      → fuzzy name/alias match
  - topCustomersByFiles(n)      → ranked by file count
  - listVendors(opts?)          → all vendors
  - getVendor(key)              → single vendor record
  - vendorsForGrade(grade)      → vendors that supply a given material grade
  - summary()                   → counts + totals

Cache strategy: parse JSONL once, hold the Maps in memory; resetCache()
exposed f
```


## Exports
`DEFAULT_CUSTOMERS_PATH` (const), `DEFAULT_VENDORS_PATH` (const), `CustomerRecord` (interface), `VendorRecord` (interface), `DatabaseSummary` (interface), `JMCustomerVendorDatabaseEngine` (class), `jmCustomerVendorDatabaseEngine` (const)

## Key methods
`load`, `resetCache`, `listCustomers`, `getCustomer`, `searchCustomers`, `topCustomersByFiles`, `listVendors`, `getVendor`, `vendorsForGrade`, `summary`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/JMCustomerVendorDatabaseEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find JMCustomerVendorDatabaseEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/JMCustomerVendorDatabaseEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
