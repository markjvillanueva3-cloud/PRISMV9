---
name: reference_code_script_handoffprunecron
description: "handoff-prune-cron.mjs — ECHO-UNDONE H6 / U-HANDOFF-PRUNE-CRON."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/handoff-prune-cron.mjs
  sourceHash: 77b0fc2d
  loc: 246
aliases: handoff-prune-cron.mjs
---

# handoff-prune-cron.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/handoff-prune-cron.mjs` (246 LOC)

## What it is
```
handoff-prune-cron.mjs — ECHO-UNDONE H6 / U-HANDOFF-PRUNE-CRON.

Counters handoff sprawl across the 26-slot fleet. state/shared/handoffs/
accumulates one HANDOFF-*.md per chat per topic and never self-cleans — 600+
files today. This archives every handoff untouched for >30 days into
state/shared/handoffs/archive/<YYYY-MM>/ (grouped by the handoff's own
month), keeping the live directory to just the active set.

Archiving is a MOVE, never a delete — fully reversible (the file still
exists, one directory deeper). Aligns with the "never delete, only disable"
reversibility rule.

Cron cadence: the script self-throttles to once per 30 days via
state/shared/handoffs/.prune-throttle.json, so it is safe to invoke from any
frequently-running surface (a Stop hook, a watchdog) — it no-ops until a
month has elapsed. `--force` bypasses the throttle.

Safety: DRY-RUN by default. `--apply` is required 
```


## Exports
`ROOT` (const), `SCHEMA_VERSION` (const), `STALE_DAYS` (const), `THROTTLE_DAYS` (const), `HANDOFFS_DIR` (const), `ARCHIVE_DIR` (const), `archiveSubdir` (function), `planArchive` (function), `shouldRun` (function), `parseWrittenAt` (function), `main` (function)

## Key methods
`writeThrottle`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/handoff-prune-cron.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find handoff-prune-cron.mjs` or `prism_session:master_index_query`; open the source at `scripts/handoff-prune-cron.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
