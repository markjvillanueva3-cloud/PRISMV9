---
name: reference_code_engine_cascadecalibrationengine
description: "Probe-based cost-quality frontier calibration."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CascadeCalibrationEngine.ts
  sourceHash: 82c79384
  loc: 278
aliases: CascadeCalibrationEngine
---

# CascadeCalibrationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CascadeCalibrationEngine.ts` (278 LOC)

## What it is
Probe-based cost-quality frontier calibration.

```
CascadeCalibrationEngine — Probe-based cost-quality frontier calibration.

Milestone: OCTOPUS-NEURAL-MS0 / U-OCN04.

References:
  - FrugalGPT (Chen 2305.05176): cascading LLM calls from cheap → strong with
    deferral thresholds cuts inference cost ~98% on benchmarks vs always-strong.
  - GATEKEEPER (2502.19335): probe-based deferral with calibration loop closes
    the cost/quality knob without manual tuning.

Job: take a tier ladder (cheap → mid → strong, each with cost-per-probe and an
`invoke` callable) and a probe set with quality scorers, run every probe through
every tier, and emit a cost-quality frontier + threshold recommendations.

The engine does NOT write thresholds to disk. That's the CLI's job (scripts/
cascade-calibrate.mjs). The engine returns the frontier; the CLI writes
state/shared/cascade-thresholds.json after merging with any last-known-good
thresholds (preserve-on
```


## Exports
`Tier` (interface), `Probe` (interface), `CalibrationOptions` (interface), `TierFrontier` (interface), `CalibrationResult` (interface), `CascadeCalibrationEngine` (class), `cascadeCalibrationEngine` (const)

## Key methods
`calibrate`, `onNaN`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CascadeCalibrationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CascadeCalibrationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CascadeCalibrationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
