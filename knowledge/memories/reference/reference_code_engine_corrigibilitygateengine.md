---
name: reference_code_engine_corrigibilitygateengine
description: "Keep the system interruptible at all times"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CorrigibilityGateEngine.ts
  sourceHash: 7176d266
  loc: 136
aliases: CorrigibilityGateEngine
---

# CorrigibilityGateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CorrigibilityGateEngine.ts` (136 LOC)

## What it is
Keep the system interruptible at all times

```
CorrigibilityGateEngine — Keep the system interruptible at all times

Phase 0.25.1 U-SAFE3 from UNIVERSAL-SKILLS-SCRIPTS-HOOKS-PLAN. Enforces
that a human kill switch (e.g. "stop", "abort", or the NIGHT-MODE flag)
always works. Tracks the latest corrigibility state across three
dimensions:

  1. Kill-switch token — can be raised/cleared; when raised, *every*
     autonomous action is blocked until cleared by human ack.
  2. Heartbeat — periodic "human-alive" pings must arrive within a
     configurable interval; silence triggers a safety hold.
  3. Override override — no rule inside this engine can be silenced
     by another PRISM engine; the config is monotonic in a
     conservative direction (you can only *tighten* it at runtime).

@module engines/CorrigibilityGateEngine
@milestone PP-0.25.1-U-SAFE3
```


## Exports
`CorrigibilityConfig` (interface), `DEFAULT_CORRIGIBILITY_CONFIG` (const), `CorrigibilityState` (interface), `GateDecision` (interface), `CorrigibilityGateEngine` (class), `corrigibilityGateEngine` (const)

## Key methods
`tighten`, `raiseKillSwitch`, `acknowledgeKill`, `heartbeat`, `evaluate`, `snapshot`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CorrigibilityGateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CorrigibilityGateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CorrigibilityGateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
