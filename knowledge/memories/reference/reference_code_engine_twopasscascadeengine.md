---
name: reference_code_engine_twopasscascadeengine
description: "TwoPassCascadeEngine — cheap-then-strong two-pass execution with a quality gate."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/TwoPassCascadeEngine.ts
  sourceHash: 38768aeb
  loc: 490
aliases: TwoPassCascadeError
---

# TwoPassCascadeError -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/TwoPassCascadeEngine.ts` (490 LOC)

## What it is
```
TwoPassCascadeEngine — cheap-then-strong two-pass execution with a quality gate.

COST-CASCADE-MS0 / U-DISPATCHER-ACTION-TWO-PASS.

Many tasks are solved correctly by a cheap tentacle on the first pass; only
escalate to an expensive tentacle when a quality gate fails. FrugalGPT
(Chen et al., arXiv:2305.05176) reports 50-98% cost reduction at constant
quality with exactly this cascade.

This engine is the *control primitive* — pure, deterministic, and fully
injectable. The tentacle/judge functions are parameters so the cascade logic
can be tested without a model server; production callers (the
`prism_ai:two_pass` dispatcher action) wire the local Ollama client as the
default cheap/strong tentacle and a rule-based judge.

Consumed by U-CASCADE-FALLBACK-CHAIN (the multi-tier fallback chain this
unit `blocks`) — this is the single-step (cheap→strong) base case.

Safety / R12: a thrown tentac
```


## Exports
`TwoPassTentacleResult` (interface), `TentacleFn` (type), `JudgeFn` (type), `TwoPassOptions` (interface), `TwoPassResult` (interface), `TwoPassCascadeError` (class), `ruleBasedQualityScore` (function), `TwoPassCascadeEngine` (class), `twoPassCascadeEngine` (const), `makeOllamaTentacle` (function)

## Key methods
`run`, `effectiveCost`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/TwoPassCascadeEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find TwoPassCascadeError` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/TwoPassCascadeEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
