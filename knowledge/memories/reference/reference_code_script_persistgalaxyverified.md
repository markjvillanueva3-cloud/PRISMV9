---
name: reference_code_script_persistgalaxyverified
description: "persist-galaxy-verified.mjs -- deterministic JSON->markdown persister for the"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/persist-galaxy-verified.mjs
  sourceHash: de61e9c3
  loc: 159
aliases: persist-galaxy-verified.mjs
---

# persist-galaxy-verified.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/persist-galaxy-verified.mjs` (159 LOC)

## What it is
```
persist-galaxy-verified.mjs -- deterministic JSON->markdown persister for the
FLEET-KNOWLEDGE-MAX verified tier (slot:zulu, 2026-06-15).

WHY (R5): the verified-wave Workflow returns a structured array of
{galaxy, sources:[{url,title,kind,verifiedExcerpt,knowledge,fetched}], synthesis,
physicsSafe, physicsNote}. Turning that into the `<g>-foundations-verified-<date>.md`
wiki files + ledger --record entries is a PURE DETERMINISTIC TRANSFORM. Doing it by
hand (or via an LLM subagent) reads the ~30KB+ output JSON into a context window and
THRASHES it (observed 2026-06-15). A node script reads the file in-process -- zero
LLM context cost, no thrash, no fabrication (it only emits what the verified data
contains, with honest `fetched:false` markers).

Idempotent-overwrite: the output JSON is the source of truth, so each run regenerates
the canonical file (replacing any partial/earlier copy). T
```


## Exports
`parseArgs` (function), `extractGalaxies` (function), `renderMarkdown` (function), `recordUrls` (function)

## Key methods
`String`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/persist-galaxy-verified.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find persist-galaxy-verified.mjs` or `prism_session:master_index_query`; open the source at `scripts/persist-galaxy-verified.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
