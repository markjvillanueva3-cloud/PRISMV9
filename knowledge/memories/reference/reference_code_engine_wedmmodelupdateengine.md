---
name: reference_code_engine_wedmmodelupdateengine
description: "Safe model update / rollback manager."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WEDMModelUpdateEngine.ts
  sourceHash: 96419ab9
  loc: 287
aliases: WEDMModelUpdateEngine
---

# WEDMModelUpdateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WEDMModelUpdateEngine.ts` (287 LOC)

## What it is
Safe model update / rollback manager.

```
WEDMModelUpdateEngine — Safe model update / rollback manager.

Phase 3 / P3-MS1 / U-P3-03 of the WEDM AGI Intelligence Roadmap.

Orchestrates the decide → validate → commit/rollback flow for every WEDM
neural or Bayesian model update. The engine is model-agnostic: it ingests
a pair of predictor callables (old, candidate) plus a held-out ground-truth
set, and returns a Decision record that the model-update hook consumes.

Exit gate (P3-MS1): committed updates preserve ≥ 95 % of prior performance
on the held-out test set. The engine enforces this hard floor —
`minRetention` defaults to 0.95 and cannot be bypassed by the caller
without setting it explicitly.

Metrics supported:
  - mae  — Mean Absolute Error (regression)     → smaller is better
  - rmse — Root Mean Squared Error (regression) → smaller is better
  - r2   — Coefficient of determination          → larger is better
  - acc  — C
```


## Exports
`UpdateMetric` (type), `Predictor` (type), `HeldOutSample` (interface), `ModelUpdateRequest` (interface), `MetricScore` (interface), `UpdateAction` (type), `UpdateDecision` (interface), `WEDMModelUpdateEngine` (class), `wedmModelUpdateEngine` (const)

## Key methods
`evaluate`, `evaluateBatch`, `getAudit`, `resetAudit`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WEDMModelUpdateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WEDMModelUpdateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WEDMModelUpdateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
