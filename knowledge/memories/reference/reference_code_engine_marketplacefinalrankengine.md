---
name: reference_code_engine_marketplacefinalrankengine
description: "REPUTATION (SupplierReputationEngine), LOGISTICS / true landed cost"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MarketplaceFinalRankEngine.ts
  sourceHash: f828e570
  loc: 135
aliases: MarketplaceFinalRankEngine
---

# MarketplaceFinalRankEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MarketplaceFinalRankEngine.ts` (135 LOC)

## What it is
REPUTATION (SupplierReputationEngine), LOGISTICS / true landed cost

```
MarketplaceFinalRankEngine.ts — PRISM networking-marketplace capstone (galaxy:business, slot:hotel).
Fuses the Phase-1 capability MATCH (RFQMatchScoring TOPSIS closeness) with the three Phase-2
differentiators — REPUTATION (SupplierReputationEngine), LOGISTICS / true landed cost
(GeoLogisticsRoutingEngine), CAPACITY (ScheduleProjectedCapacityEngine) — into one explainable final
supplier rank for the buyer.

This is what flips a perfect-fit-but-unproven-overseas-backlogged shop below a slightly-worse-fit shop
that's trusted, local, and open — the ranking competitors (price-only) can't produce. ADDITIVE: it
composes the four already-landed signals; it does NOT modify RFQMatchScoring.

FAIL-LOUD (R12): weights must sum to 1.0; every signal must be in [0,1]; final score asserted into [0,1].
Each result carries the per-criterion weighted CONTRIBUTION (explainable — the honesty moat).
```


## Exports
`SignalEntry` (type), `RankWeights` (type), `RankedSupplier` (interface), `FinalRankResult` (interface), `MarketplaceFinalRankEngine` (class), `marketplaceFinalRankEngine` (const)

## Key methods
`rank`, `schemaVersion`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MarketplaceFinalRankEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MarketplaceFinalRankEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MarketplaceFinalRankEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
