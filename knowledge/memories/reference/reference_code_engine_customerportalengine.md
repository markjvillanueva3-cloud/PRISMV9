---
name: reference_code_engine_customerportalengine
description: "Customer Portal"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CustomerPortalEngine.ts
  sourceHash: 6d6ac749
  loc: 1193
aliases: CustomerPortalEngine
---

# CustomerPortalEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CustomerPortalEngine.ts` (1193 LOC)

## What it is
Customer Portal

```
CustomerPortalEngine -- Token-Based Customer Portal Access (durable)
====================================================================

Provides external customer access to quotes, orders, milestones, quality
documents, and messaging -- all without requiring a PRISM account.

Security model:
- Access via cryptographic tokens (base64url, 32 bytes)
- Tokens are scoped (view, respond, documents, messages)
- Time-limited (default 30 days, configurable)
- Rate-limited (10 req/min per token, configurable)
- Revocable at any time

PERSISTENCE (U-HOTEL-PORTAL-PERSISTENCE, 2026-06-09 slot:hotel)
  The four durable record types -- tokens, messages, quality documents, and
  service cases -- are persisted in a SQLite database in WAL mode, modeled on
  juliett's CoordinationStoreEngine pattern (lazy ensureOpen, prepared
  statements, synchronous=NORMAL, busy_timeout, schema_version in meta).
  Pre
```


## Exports
`PortalTokenType` (type), `PortalScope` (type), `PortalMessageSender` (type), `PortalToken` (interface), `PortalMessage` (interface), `QualityDocument` (interface), `PortalQuoteView` (interface), `PortalOrderStatus` (interface), `PortalServiceCase` (interface), `PortalTokenValidation` (interface), `CustomerPortalEngineOptions` (interface), `CustomerPortalEngine` (class), `customerPortalEngine` (const)

## Key methods
`VALUES`, `createToken`, `revokeToken`, `listTokens`, `validateToken`, `getQuoteView`, `respondToQuote`, `getOrderStatus`, `addQualityDocument`, `updateQualityDocument`, `listQualityDocuments`, `getQualityDocument`, `addMessage`, `insertAndTrim`, `listMessages`, `markMessagesRead`, `createServiceCase`, `listServiceCases`, `updateServiceCase`, `close`, `getDbPath`, `health`, `buildEntityKey`, `defaultSlaHoursForSeverity`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/CustomerPortalEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CustomerPortalEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CustomerPortalEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
