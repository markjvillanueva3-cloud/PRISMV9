---
name: reference_code_engine_fusion360strategyengine
description: "Real strategy selection for Fusion 360"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/Fusion360StrategyEngine.ts
  sourceHash: 8f3cb3eb
  loc: 342
aliases: Fusion360StrategyEngine
---

# Fusion360StrategyEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/Fusion360StrategyEngine.ts` (342 LOC)

> [physics] Imports canonical physics constants (`src/physics/constants.ts`) -- safety-relevant node.

## What it is
Real strategy selection for Fusion 360

```
Fusion360StrategyEngine — Real strategy selection for Fusion 360
=================================================================
Maps (operation type, ISO material group, feature) → Fusion 360 cycle code,
cutting parameters, and provenance. Composes Fusion360CycleCatalogEngine
for cycle data and CANONICAL_KIENZLE constants for physics-backed params.

NO-FAKE-CODE: throws when no matching cycle exists in the catalog; never
returns fabricated strategy data. RPM/feed estimates come from Sandvik
C-2920:3 ISO-group baseline tables (kc1.1 + Kienzle exponent), exactly the
same canonical reference MastercamStrategyEngine uses.

Sister engine: MastercamStrategyEngine (same shape, Mastercam catalog).

@module engines/Fusion360StrategyEngine
@milestone CAM-EXHAUST-MS0 U-CAM-FUSION-STRAT-01
```


## Exports
`ISOGroupSchema` (const), `ISOGroup` (type), `OperationTypeSchema` (const), `OperationType` (type), `FeatureKindSchema` (const), `FeatureKind` (type), `StrategyRequestSchema` (const), `StrategyRequest` (type), `ParameterEstimateSchema` (const), `ParameterEstimate` (type), `StrategyResultSchema` (const), `StrategyResult` (type), `Fusion360StrategyEngine` (class), `fusion360StrategyEngine` (const)

## Key methods
`recommend`, `pickCycle`, `baselineVc`, `baselineVcTable`, `auditEngine`

## Depends on
[[reference_code_engine_fusion360cyclecatalogengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/Fusion360StrategyEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find Fusion360StrategyEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/Fusion360StrategyEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
