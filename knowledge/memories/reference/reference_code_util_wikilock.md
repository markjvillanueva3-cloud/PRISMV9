---
name: reference_code_util_wikilock
description: "wikiLock — File-based advisory lock for `H:/prism/knowledge/wiki/` writers."
metadata:
  type: reference
  kind: code-util
  galaxy: unassigned
  sourcePath: mcp-server/src/utils/wikiLock.ts
  sourceHash: 9d3e59ae
  loc: 166
aliases: wikiLock.ts
---

# wikiLock.ts -- Util SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/utils/wikiLock.ts` (166 LOC)

## What it is
```
wikiLock — File-based advisory lock for `H:/prism/knowledge/wiki/` writers.

Implements WIKI_SCHEMA.md §6.1 (claim_file lock) + §6.6 (stale-lock reaper):

  - Lock file is `<target>.lock`, contents `{ pid, ts, agent, target }`.
  - Acquisition uses O_EXCL (open with `wx`), so a race-loser sees EEXIST
    and falls into the inspection branch.
  - A lock older than STALE_LOCK_MS whose holder PID is dead is force-reaped.
  - A lock older than STALE_LOCK_MS whose holder PID is alive is NOT reaped:
    the holder is a slow-but-live agent and silent reaping would clobber it.

Used by WikiIndexMaintainerEngine and WikiLogAppenderEngine. Mirrors the
pattern in `mcp-server/scripts/wiki-bootstrap.mjs` so a Node.js script
and an engine class can safely interleave on the same lock files.

@module utils/wikiLock
```


## Exports
`LockPayload` (interface), `LockHandle` (interface), `acquireWikiLock` (function), `withWikiLock` (function), `_internals_for_test` (const)

## Key methods
`release`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/utils/wikiLock.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Util

## How to query it
Recall by meaning via `/system-viz find wikiLock.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/utils/wikiLock.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
