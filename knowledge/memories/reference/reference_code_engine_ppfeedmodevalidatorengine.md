---
name: reference_code_engine_ppfeedmodevalidatorengine
description: "Validate feed-rate mode (G93/G94/G95)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PPFeedModeValidatorEngine.ts
  sourceHash: 8f2ccf48
  loc: 330
aliases: PPFeedModeValidatorEngine
---

# PPFeedModeValidatorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PPFeedModeValidatorEngine.ts` (330 LOC)

## What it is
Validate feed-rate mode (G93/G94/G95)

```
PPFeedModeValidatorEngine — Validate feed-rate mode (G93/G94/G95)

The feed-rate modal group decides how the F-word is interpreted. A
silent switch between modes changes cutting speed by orders of
magnitude — the classic "I changed one letter and the tool snapped"
failure is usually G94→G95 crossing or a missed M5 before G95:

  G93   — Inverse-time mode. F = 1/block_time_min. On 5-axis this
          is preferred because rotary axes have no "length".
  G94   — Feed per minute (mm/min or in/min). Default for mills.
  G95   — Feed per revolution (mm/rev or in/rev). Default for lathes
          and for rigid-tapping. Requires spindle rotating.

Failure modes this validator catches:
  - feed_mode_mixed_in_block (error): G94 and G95 both appear on the
    same block. Undefined — controllers typically take the rightmost
    silently.
  - g95_with_spindle_off (error): G95 active while M5 (or n
```


## Exports
`FMSeverity` (type), `FMIssue` (interface), `FMResult` (interface), `FMOptions` (interface), `PPFeedModeValidatorEngine` (class), `ppFeedModeValidatorEngine` (const)

## Key methods
`validate`, `parseFloat`, `quickCheck`, `defaultOptions`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PPFeedModeValidatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PPFeedModeValidatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PPFeedModeValidatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
