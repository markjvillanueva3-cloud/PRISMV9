---
name: reference_code_engine_pptoolnumberrangevalidatorengine
description: "Validate T-word numbering"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PPToolNumberRangeValidatorEngine.ts
  sourceHash: 820ba383
  loc: 338
aliases: PPToolNumberRangeValidatorEngine
---

# PPToolNumberRangeValidatorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PPToolNumberRangeValidatorEngine.ts` (338 LOC)

## What it is
Validate T-word numbering

```
PPToolNumberRangeValidatorEngine — Validate T-word numbering

The T-word selects a tool pocket (on a mill ATC) or a tool-offset
number (on a lathe). Magazine capacity varies by machine:

  - Haas Mini-Mill:        10 pockets
  - Haas VF series:        20–40 pockets
  - Mori NHX HMC:          60 pockets
  - Okuma Multus turn-mill: typically 48 lower + 16 upper

A T-word outside the installed magazine either fetches air, fetches
the wrong tool (if a previous program wrapped the index), or alarms.
`T0` is a special case — on some controls it means "no tool", on
others it's an invalid pocket and alarms.

This engine does NOT judge tool-change *sequence* (see
`PPToolChangeValidatorEngine`) or geometry — just the *number* on
the T-word.

Checks:
  - `tool_zero`             (error): T0 rejected by default — most
                                      mills treat it as an alarm.
                 
```


## Exports
`ToolNumberSeverity` (type), `ToolNumberKind` (type), `ToolNumberIssue` (interface), `ToolNumberResult` (interface), `ToolNumberOptions` (interface), `PPToolNumberRangeValidatorEngine` (class), `ppToolNumberRangeValidatorEngine` (const)

## Key methods
`validate`, `quickCheck`, `defaultOptions`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PPToolNumberRangeValidatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PPToolNumberRangeValidatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PPToolNumberRangeValidatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
