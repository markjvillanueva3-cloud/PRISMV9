---
name: reference_code_engine_quotingpublicquoteengine
description: "Quoting Public Quote"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/QuotingPublicQuoteEngine.ts
  sourceHash: c734dd09
  loc: 268
aliases: QuotingPublicQuoteEngine
---

# QuotingPublicQuoteEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/QuotingPublicQuoteEngine.ts` (268 LOC)

## What it is
Quoting Public Quote

```
QuotingPublicQuoteEngine -- the CUSTOMER-SAFE boundary over the internal FMV
estimate. This is MVP backend contract gap #1 (QUOTING-FRONTEND-MVP-PLAN
2026-06-22): the public web quote flow (upload -> instant quote) must NEVER
leak internal pricing internals -- cost breakdown, margin, raw machine rate /
material spend, the gap-vs-charged reconciliation signal, or any $/in3 basis.

The customer sees ONLY: a single quote price (which already carries the target
margin by construction in FmvResult.fmv_usd), a currency, a validity flag, and
(optionally) lead-time tiers passed through from quoting_lead_time_tiers.

Safety boundary (charlie soul: conservative; never leak internal cost basis):
 - REFUSE to emit a price when the underlying FMV is invalid (ok:false) -- a
   customer must never see a $0 / fabricated quote. Returns quotable:false +
   a SANITIZED reason (the raw FmvResult.reason can 
```


## Exports
`PublicQuantityBreak` (interface), `PublicDfmVerdict` (interface), `PublicLeadTimeTier` (interface), `PublicQuoteResult` (interface), `PublicInstantQuoteResult` (interface), `sanitizePublicReason` (function), `QuotingPublicQuoteEngine` (class), `quotingPublicQuoteEngine` (const)

## Key methods
`toPublicQuote`, `toPublicQuoteFromInstant`

## Depends on
[[reference_code_engine_fairmarketvalueengine]] | [[reference_code_engine_instantquoteengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/QuotingPublicQuoteEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find QuotingPublicQuoteEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/QuotingPublicQuoteEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
