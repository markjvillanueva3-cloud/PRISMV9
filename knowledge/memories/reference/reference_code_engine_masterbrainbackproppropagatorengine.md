---
name: reference_code_engine_masterbrainbackproppropagatorengine
description: "U-CADC-LP04 / CAD-COMPLETE-MS0"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MasterBrainBackpropPropagatorEngine.ts
  sourceHash: abd25e8a
  loc: 427
aliases: MasterBrainBackpropPropagatorEngine
---

# MasterBrainBackpropPropagatorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MasterBrainBackpropPropagatorEngine.ts` (427 LOC)

## What it is
U-CADC-LP04 / CAD-COMPLETE-MS0

```
MasterBrainBackpropPropagatorEngine — U-CADC-LP04 / CAD-COMPLETE-MS0

The back-propagation stage of the CAD closed-loop learner. Given a
prioritized replay batch (from LP03), it computes a gradient step and
applies it to BOTH:
  - the shared MASTER policy (cross-CAD-system knowledge), and
  - the per-CAD-system NN HEAD that produced the batch.
so a single CAD execution outcome updates both the shared trunk and the
adapter that owns it.

Model: each target (master + each head) is a linear value head
v = θ·φ over a fixed 4-d feature vector φ derived from a FeedbackSample
(bias, normalised timing, collision, regeneration-ok). The training
signal is the mini-batch mean weighted-least-squares loss
  L = (1/n)·Σ_i w_i·(v_i − r_i)²
where r_i is the shaped reward of sample i and w_i is its PER
importance-sampling weight from the replay batch. Per-sample gradient is
g_i = w_i·2·(v_i − r_i)·φ_i; t
```


## Exports
`MASTER_TARGET` (const), `PropagationResult` (interface), `PropagateOutcome` (interface), `TargetParams` (interface), `PerTargetCounter` (interface), `PropagatorStats` (interface), `MasterBrainBackpropPropagatorEngine` (class), `masterBrainBackpropPropagatorEngine` (const)

## Key methods
`propagate`, `consolidate`, `getParams`, `listTargets`, `getStats`, `reset`

## Depends on
[[reference_code_engine_cadheadreplaybufferengine]] | [[reference_code_engine_cadperadapterfeedbackcollectorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/MasterBrainBackpropPropagatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MasterBrainBackpropPropagatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MasterBrainBackpropPropagatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
