---
name: reference_oscar_sfc_variability_coverage_gapmap_2026_06_25
description: "VERIFIED coverage map (slot:oscar 2026-06-25) of the SFC variability sweep (sfc-variability-enumerate.mjs, ~7.8M mill combos): which of the operator's named cutting-input dimensions the sweep ACTUALLY varies, vs which the engine SUPPORTS but the sweep is BLIND to, vs which the engine does NOT model. Two-layer (enumerator-emits? engine-consumes?) + a sensitivity probe proving workpiece_height is a LIVE lever while workpiece_length/width (milling) + damping_ratio (stable regime) are INERT. Drives the 'ensure calculators correct or build more' next-build pass."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.713Z
aliases: reference_oscar_sfc_variability_coverage_gapmap_2026_06_25
---


**Context:** operator asked "what does the 7.8M combo space ACTUALLY test — does it cover machine/spindle/controller/optional-features/kinematics+envelope/build-quality/way+bed/material+hardness+machinability/stock-size+clamped/fixturing/toolpath-type/holder+connection/clamp-force+balance+rigidity+damping/tooling/entry+exit/physics+metallurgy+thermo forces/finish+rough/optimized-vs-aggressive/ROI ... we need all logical combos for every variable so calculators are correct or we build more."

**The harness:** `scripts/sfc-variability-enumerate.mjs` (mill) + `-lathe.mjs` → `sfc-variability-batch-run.mjs` (N workers drive `SpeedFeedOrchestratorEngine.compute`) → 12GB / ~7.8M mill + ~5.9M lathe results under `state/shared/sfc-variability-results/`. Durable never-stop via 6 scheduled tasks (`PRISM SFC Variability Guard`/`Batch Mill`/`Batch Lathe` 5-min relaunch loop + `Closed Loop`/`Per-Machine Sweep`/`Overnight Fresh Train` GPU pipeline). The sweep is a compatibility-filtered CARTESIAN of DISCRETE BANDS — broad breadth, but continuous params (diam/ap/ae/feed) are representative bands (engine interpolates), NOT every numeric value. Mill-only (+lathe variant); wedm separate.

**COVERED (13 axes the sweep actually varies):** machine archetype(10) · spindle taper+bearing-preload+torque-curve(via machine) · controller(10: fanuc/siemens/heidenhain/haas/okuma/mazak/mitsubishi) · build-quality rigidity(3)×guideway/way-type(3)×age(3)×preload(3) · axis accel(3)+jerk(3) · material(29, ALL 6 ISO P/M/K/N/S/H)×hardness(3) · coolant type(6)×pressure(5)×conc(3) · operation(7) · cut_type rough/semi/finish(3) · strategy(7 incl hsm) · holder type(taper-gated)×gauge(3)×TIR(3)×balance(G2.5/6.3) · tooling material(6)×coating(9)×diam(10)×flutes(6)×helix(3)×corner(5)×stickout-L/D(6)+inserts · workholding type(7)×stiffness(3)×clamp-force(4) · optimize_for(5: tool_life/productivity/surface_finish/balanced/cost). machinability + physics/metallurgy/thermo forces = COMPUTED outputs (Kienzle/Taylor/Merchant), correct to model.

**GAP-A — engine SUPPORTS it but enumerator is BLIND (the makeInput never emits it → all combos run at engine default):** verified by grep of `OrchestratorInput` (SpeedFeedOrchestratorEngine.ts:202-219): `workpiece_length_mm`/`width_mm`/`height_mm`/`diameter_mm` (:202-205), `bore_diameter_mm` (:206), `damping_ratio` (:219), `natural_frequency_hz`. The enumerator emits NONE → 7.8M combos all use default stock 100x100x50 + rigidity-derived damping.

**SENSITIVITY PROBE (`scripts/sfc-stock-damping-sensitivity-probe.mjs`, run 2026-06-25 — proves which GAP-A fields are worth wiring):**
- **`workpiece_height_mm` = LIVE** — thin-wall H=6mm moved ap 12→6mm (engine clamps ap to stock height, SpeedFeedOrchestratorEngine.ts:2853), deflection 43.3→21.7µm, power 2.51→1.26kW, force 718→359N. **HIGH-VALUE to wire** — the thin-wall ap-clamp safety behavior is currently NEVER validated (all 7.8M at default H=50). For turning the analogous live levers are `workpiece_diameter_mm`+`bore_diameter_mm` (drive Vc, :2658).
- **`workpiece_length_mm`/`workpiece_width_mm` = INERT for milling** — long-thin-bar L=400×W=20 identical to default. Engine doesn't model milling workpiece whip. Wiring them into the MILL enumerator = combo bloat, no validation value (R13 don't over-build inert axes).
- **`damping_ratio` = INERT at a stable baseline** (0.015 vs 0.10 identical, zone=stable/p_chatter=0). Conditionally live only near the chatter/stability boundary — needs a near-chatter baseline to exercise.

**GAP-B — engine does NOT model it yet (genuine "build the ENGINE more"):** machine work-envelope/travel-limits/table-size (only axis-count modeled) · entry-in/exit-out (lead-in/out, ramp/helical entry — no input field) · optional controller features (look-ahead/HSM-mode/AICC block-processing) · toolpath SOURCE (CAM/macro/conversational/hard-coded — only strategy; source arguably doesn't change cutting physics) · BIG-PLUS dual-contact flag · bed type · milling long-thin-wall deflection · ROI as an explicit objective (the `cost` optimize_for is closest). grep found NO engine input for these.

**NEXT BUILD (R13 dependency-ordered):** (1) WIRE the LIVE GAP-A levers into the enumerator — `workpiece_height_mm` (mill) + `workpiece_diameter_mm`/`bore_diameter_mm` (lathe) as new compat-filtered axes (height ≥ ap; representative bands) → validates thin-wall ap-clamp + turning-Vc across the range. CAVEAT: changing the enumerator RESETS the live sweep frontier (idx→combo + fingerprints change) → either reset the resume sidecar (fresh sweep) OR run as a PARALLEL additive validation space that doesn't disturb the running 7.8M sweep — operator-gated fork (touches a durable running system). (2) Build engine modeling for GAP-B (envelope/entry-exit/controller-features/long-thin-wall) then enumerate. Skip inert axes (mill L/W, stable-regime damping). Pairs with [[reference_oscar_sfc_jm_gauntlet_accuracy_verdict_2026_06_25]] (the JM-vs-engine accuracy proof) + [[feedback_exhaustive_variability]].

**Also found this session (separate, flagged):** the variability batch parent summary is cosmetically broken — `runOrchestrator` reads worker stdout via `.split("\n").pop()` on PRETTY (multi-line) JSON polluted by tsx/MCP boot logs → always fails to parse → reports `total_processed:0` even though workers wrote thousands of records (R12: the never-stop telemetry under-reports). Worker should emit a single-line prefixed result; parent should scan for the prefix. Low-risk R8 fix, not yet shipped.
