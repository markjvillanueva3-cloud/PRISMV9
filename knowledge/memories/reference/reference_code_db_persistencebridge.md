---
name: reference_code_db_persistencebridge
description: "PersistenceBridge — Write-Through Cache for Business Engine Data"
metadata:
  type: reference
  kind: code-db
  galaxy: unassigned
  sourcePath: mcp-server/src/db/PersistenceBridge.ts
  sourceHash: 42111b70
  loc: 440
aliases: persistenceBridge
---

# persistenceBridge -- DB Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/db/PersistenceBridge.ts` (440 LOC)

## What it is
```
PersistenceBridge — Write-Through Cache for Business Engine Data
================================================================

Bridges the synchronous in-memory Maps in business engines with the
async IBusinessStore persistence layer. Engines keep their Maps for
fast synchronous reads; the bridge ensures writes are durably persisted.

Pattern:
  - At startup: loadAll() fills engine Maps from the store
  - After mutation: persist(entity, key, value) queues write with confirmation
  - At shutdown: gracefulShutdown() drains queue with timeout

This avoids making engine APIs async (which would break all callers).

Post-review fixes: C2 (error logging + retry), H5 (flush mutex),
M5 (paginated load), M8 (reset for tests).
INFRA-1-2: Write confirmation, 3 retries with backoff, pool=50, graceful shutdown with timeout.

@version 1.2.0 — Session INFRA-1-2 (U-PER1)
```


## Exports
`POOL_SIZE` (const), `persistenceBridge` (const)

## Key methods
`registerMap`, `registerArray`, `loadAll`, `persist`, `persistAppend`, `flushAll`, `clearTimeout`, `setTimeout`, `getMode`, `isInitialized`, `getHealth`, `gracefulShutdown`, `reset`

## Depends on
[[reference_code_db_businessstore]] | [[reference_code_db_writeaheadlog]]

## Where it lives
- **Source:** `mcp-server/src/db/PersistenceBridge.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** DB Module

## How to query it
Recall by meaning via `/system-viz find persistenceBridge` or `prism_session:master_index_query`; open the source at `mcp-server/src/db/PersistenceBridge.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
