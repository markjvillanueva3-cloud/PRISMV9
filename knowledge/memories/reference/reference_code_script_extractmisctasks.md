---
name: reference_code_script_extractmisctasks
description: "extract-misc-tasks.mjs — Deterministic merge of the 10-agent misc-tasks scan."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/extract-misc-tasks.mjs
  sourceHash: ce43598f
  loc: 401
aliases: extractMiscTasks
---

# extractMiscTasks -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/extract-misc-tasks.mjs` (401 LOC)

## What it is
```
extract-misc-tasks.mjs — Deterministic merge of the 10-agent misc-tasks scan.

Spec: MISC-TASKS extraction (slot juliett, forge7, 2026-05-16).

The 10 parallel scanner agents each emit one
`state/shared/specs/misc-tasks-scan/agent-<N>.json` of candidate
"orphaned incomplete work" items. This script merges those 10 files
into a single deduplicated, roadmap-cross-referenced inventory:
`state/shared/specs/MISC-TASKS-INVENTORY.{json,md,html}`.

Pipeline (pure + deterministic — set-ops, NOT model judgement):
  1. Load + validate all 10 agent JSONs (fail loud on missing/corrupt).
  2. Dedupe: key = unit_id ? 'ID:'+normId : 'T:'+normTitle. Merge groups.
  3. Exclude `looks_completed === true` items (they shipped).
  4. Cross-reference each remaining item's milestone_or_unit_id against
     the KNOWN set (every milestone + unit id in roadmap-index.json,
     the 694 milestone envelopes, and MILE
```


## Exports
`ROOT` (const), `SCHEMA_VERSION` (const), `AGENT_COUNT` (const), `normalizeId` (function), `normalizeTitle` (function), `safeId` (function), `dedupeKey` (function), `buildKnownSet` (function), `mergeGroup` (function), `extractMiscTasks` (function), `renderMarkdown` (function), `renderHtml` (function), `loadEnvelopes` (function), `main` (function)

## Key methods
`add`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/extract-misc-tasks.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find extractMiscTasks` or `prism_session:master_index_query`; open the source at `scripts/extract-misc-tasks.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
