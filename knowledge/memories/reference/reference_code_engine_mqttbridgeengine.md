---
name: reference_code_engine_mqttbridgeengine
description: "MqttBridgeEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MqttBridgeEngine.ts
  sourceHash: 238f2ad9
  loc: 509
aliases: MqttBridgeEngine
---

# MqttBridgeEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MqttBridgeEngine.ts` (509 LOC)

## What it is
MqttBridgeEngine

```
MqttBridgeEngine

MQTT IoT bridge for shop-floor sensor integration.
Subscribes to MQTT topics for vibration, temperature, coolant flow,
and other sensor data. Aggregates and feeds into PRISM adaptive control.

Capabilities:
1. connect()        — Connect to MQTT broker
2. subscribe()      — Subscribe to sensor topics
3. getLatest()      — Get latest values for all subscribed topics
4. getHistory()     — Get buffered history for a topic
5. setAlert()       — Set threshold alerts on sensor values
6. checkAlerts()    — Check all alerts against current values
7. aggregate()      — Compute statistics on buffered data (mean, std, trend)
8. getVibration()   — Get vibration sensor data with FFT frequency analysis
9. getTemperature() — Get thermal sensor data with drift detection

MQTT Topics (configurable):
- prism/machine/{id}/vibration   — Accelerometer data (g)
- prism/machine/{id}/temperatur
```


## Exports
`MqttConfig` (interface), `SensorReading` (interface), `AlertConfig` (interface), `AlertEvent` (interface), `TopicStats` (interface), `VibrationData` (interface), `TemperatureData` (interface), `MqttBridgeEngine` (class)

## Key methods
`connect`, `subscribe`, `ingest`, `getLatest`, `getHistory`, `setAlert`, `checkAlerts`, `aggregate`, `getVibration`, `getTemperature`, `isConnected`, `getSubscriptions`, `getBufferSizes`, `disconnect`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MqttBridgeEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MqttBridgeEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MqttBridgeEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
