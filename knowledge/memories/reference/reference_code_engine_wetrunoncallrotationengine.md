---
name: reference_code_engine_wetrunoncallrotationengine
description: "WetRunOnCallRotationEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WetRunOnCallRotationEngine.ts
  sourceHash: a94afbca
  loc: 505
aliases: WetRunOnCallRotationEngine
---

# WetRunOnCallRotationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WetRunOnCallRotationEngine.ts` (505 LOC)

## What it is
WetRunOnCallRotationEngine

```
WetRunOnCallRotationEngine
------------------------------------------------------------
Contiguous on-call rotation + escalation ladder for wet-run
pilot abort response. When an abort page fires from
WetRunSessionLogEngine or BlamelessPostMortemEngine, the
current shift's primary responder is paged. If the primary
does not acknowledge within ack_window_ms, the page escalates
to the secondary. If the secondary does not acknowledge
within secondary_window_ms, the page walks the escalation
ladder in order until someone acknowledges.

Shifts are strictly contiguous: every new shift must start
exactly where the previous one ended. This prevents
accidental coverage gaps. Primary and secondary must differ,
and the escalation ladder cannot contain either of them.

Swaps are recorded with an audit trail: the swap endpoint
must be inside the shift window, must name an approver
distinct from the sw
```


## Exports
`PageStage` (type), `Shift` (interface), `ShiftInput` (interface), `SwapRecord` (interface), `Page` (interface), `EscalationNote` (interface), `Snapshot` (interface), `WetRunOnCallRotationEngine` (class), `wetRunOnCallRotationEngine` (const)

## Key methods
`configureShift`, `currentShift`, `swap`, `page`, `acknowledge`, `sweepEscalations`, `pendingEscalations`, `getPage`, `listPages`, `listShifts`, `listSwaps`, `snapshot`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WetRunOnCallRotationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WetRunOnCallRotationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WetRunOnCallRotationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
