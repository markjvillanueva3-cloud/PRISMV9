---
name: reference_code_script_buildmodularindex
description: "build-modular-index.mjs -- a MODULAR, section-loadable file index for the H:"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/build-modular-index.mjs
  sourceHash: badb7178
  loc: 387
aliases: build-modular-index.mjs
---

# build-modular-index.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/build-modular-index.mjs` (387 LOC)

## What it is
```
build-modular-index.mjs -- a MODULAR, section-loadable file index for the H:
drive codebase. Instead of one monolithic graph consumers must load whole
(the 711MB system-graph.json OOM problem), this emits:

  state/shared/modular-index/manifest.json        <- thin: one row per SECTION
  state/shared/modular-index/sections/<id>.jsonl  <- per-file rows, loaded ON DEMAND

A SECTION is (root, depth-1 subdir). To search, a consumer reads the small
manifest, picks the relevant section(s), then loads ONLY those shard(s) --
"call up a section, not the full thing." Nothing ever loads the whole index.

MEMORY MODEL (the OOM fix): shards are written via a streaming file descriptor
(fs.writeSync per file), so resident memory is O(1) per file regardless of how
many files a section holds. There is NO per-section file cap -- a section with
2M files streams 2M lines without growing the heap. This is why
```


## Exports
`buildIndex` (re-export), `indexRoot` (re-export), `discoverAllRoots` (re-export), `queryManifest` (re-export), `loadSection` (re-export), `searchSections` (re-export), `resolveSectionPath` (re-export), `sanitize` (re-export), `walkStream` (re-export), `DEFAULT_ROOTS` (re-export), `IGNORE_DIRS` (re-export), `OUT_DIR` (re-export)

## Key methods
`walkStream`, `onFile`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/build-modular-index.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find build-modular-index.mjs` or `prism_session:master_index_query`; open the source at `scripts/build-modular-index.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
