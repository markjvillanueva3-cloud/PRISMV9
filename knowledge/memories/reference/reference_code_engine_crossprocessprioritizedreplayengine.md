---
name: reference_code_engine_crossprocessprioritizedreplayengine
description: "XPROC-NEURAL Tier 2 (T2-02)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CrossProcessPrioritizedReplayEngine.ts
  sourceHash: 69d2a4da
  loc: 493
aliases: CrossProcessPrioritizedReplayEngine
---

# CrossProcessPrioritizedReplayEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CrossProcessPrioritizedReplayEngine.ts` (493 LOC)

## What it is
XPROC-NEURAL Tier 2 (T2-02)

```
CrossProcessPrioritizedReplayEngine — XPROC-NEURAL Tier 2 (T2-02)

Schaul et al. (2016) Prioritized Experience Replay over a sumtree-backed
replay buffer. Used by the (planned) T3-01 online MLP updater to draw the
*most informative* training mini-batches instead of uniform random samples.

Reference: Schaul, T., Quan, J., Antonoglou, I., & Silver, D. (2016).
           "Prioritized Experience Replay." ICLR.

Algorithm (proportional variant):
  1. Each stored experience has priority p_i = (|td_error| + ε)^α
     where α ∈ [0, 1] controls how much prioritization matters
     (α = 0 reduces to uniform sampling; α = 1 is full prioritization).
  2. Sample probability P(i) = p_i / Σ p_k.
  3. Importance-sampling weight w_i = (1 / (N · P(i)))^β, normalized by
     max(w_j) so all weights are in [0, 1]. β anneals 0.4 → 1.0 over
     training to remove bias as policy converges.

Sumtree (binary t
```


## Exports
`ReplayExperience` (interface), `ReplaySample` (interface), `SampleResponse` (interface), `AddResultOk` (interface), `AddResultErr` (interface), `AddResult` (type), `UpdateResultOk` (interface), `UpdateResultErr` (interface), `UpdateResult` (type), `ReplayStats` (interface), `CrossProcessPrioritizedReplayEngine` (class), `crossProcessPrioritizedReplayEngine` (const), `crossProcessPrioritizedReplay` (function), `DEFAULT_BETA` (re-export)

## Key methods
`at`, `sample`, `reset`, `add`, `updatePriority`, `stats`, `configure`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CrossProcessPrioritizedReplayEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CrossProcessPrioritizedReplayEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CrossProcessPrioritizedReplayEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
