---
name: reference_code_schema_ideablockschema
description: "IdeaBlock Schemas — OBSIDIAN-INTELLIGENCE-MS3 / E1"
metadata:
  type: reference
  kind: code-schema
  galaxy: unassigned
  sourcePath: mcp-server/src/schemas/ideaBlockSchema.ts
  sourceHash: ebfb2ce8
  loc: 215
aliases: IdeaBlockSchema
---

# IdeaBlockSchema -- Schema SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/schemas/ideaBlockSchema.ts` (215 LOC)

## What it is
```
IdeaBlock Schemas — OBSIDIAN-INTELLIGENCE-MS3 / E1
===================================================
Zod schemas for the IdeaBlock RAG pattern (Akshay/Blockify).

An IdeaBlock is the atomic semantic unit produced by extracting `.md`
notes into question/answer pairs anchored to source. Downstream:
 - E2 deduplicates similar blocks via cosine similarity.
 - E3 swaps the legacy paragraph-level RAG for IdeaBlock-level retrieval.
 - E4 governance gates each block.
 - D1 reads `source_path` + `extracted_at` + `model` as provenance.

**Schema evolution path** — current shape is v1. When a v2 ships, the
canonical IdeaBlockSchema becomes a `z.discriminatedUnion("schema_version",
[IdeaBlockSchemaV1, IdeaBlockSchemaV2])` and the v1 file moves to
`mcp-server/src/migrations/` as a frozen snapshot. Consumers should import
`IdeaBlockSchema` (the rolling alias), NOT `IdeaBlockSchemaV1` directly.

@mod
```


## Exports
`IdeaBlockGovernanceTagSchema` (const), `IdeaBlockGovernanceTag` (type), `IdeaBlockSchemaV1` (const), `IdeaBlockV1` (type), `IdeaBlockSchema` (const), `IdeaBlock` (type), `IdeaBlockExtractInputSchema` (const), `IdeaBlockExtractInput` (type), `IdeaBlockExtractErrorClassSchema` (const), `IdeaBlockExtractErrorClass` (type), `IdeaBlockExtractResultSchema` (const), `IdeaBlockExtractResult` (type), `__testables` (const)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/schemas/ideaBlockSchema.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Schema

## How to query it
Recall by meaning via `/system-viz find IdeaBlockSchema` or `prism_session:master_index_query`; open the source at `mcp-server/src/schemas/ideaBlockSchema.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
