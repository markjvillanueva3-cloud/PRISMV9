---
name: reference_code_engine_nxopenassemblydrawingengine
description: "U-CAD-APP-18 (PHASE-48)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/NXOpenAssemblyDrawingEngine.ts
  sourceHash: 9c511199
  loc: 1223
aliases: NXOpenAssemblyDrawingEngine
---

# NXOpenAssemblyDrawingEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/NXOpenAssemblyDrawingEngine.ts` (1223 LOC)

## What it is
U-CAD-APP-18 (PHASE-48)

```
WIRE-EXEMPT: U-CAD-APP add-in bridge -- heavy injected NX Open session deps, no singleton; awaits its NX Open add-in host (delta/CAD), not a standalone prism_* dispatcher action.
```


## Exports
`NXAssemblyDrawingClock` (interface), `NXAssemblyTransport` (interface), `NXDrawingTransport` (interface), `NXOpenAssemblyDrawingEngine` (class), `NXComponentSchema` (re-export), `NXAssemblyConstraintSchema` (re-export), `NXAssemblyResultSchema` (re-export), `NXDrawingResultSchema` (re-export), `NXDrawingSchema` (re-export), `NX_CONSTRAINT_TYPES` (re-export), `NX_VIEW_TYPES` (re-export), `NX_SECTION_TYPES` (re-export), `NX_DIMENSION_TYPES` (re-export), `NX_ANNOTATION_TYPES` (re-export)

## Key methods
`now`, `addComponent`, `removeComponent`, `replaceComponent`, `suppressComponent`, `unsuppressComponent`, `addConstraint`, `removeConstraint`, `editConstraint`, `positionComponent`, `repositionComponent`, `getComponents`, `getConstraints`, `checkInterference`, `getMassProperties`, `getBom`, `createPattern`, `mirrorAssembly`, `createArrangement`, `activateArrangement`, `explodeView`, `collapseView`, `createDrawing`, `addSheet`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/NXOpenAssemblyDrawingEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find NXOpenAssemblyDrawingEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/NXOpenAssemblyDrawingEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
