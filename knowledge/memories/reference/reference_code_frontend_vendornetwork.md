---
name: reference_code_frontend_vendornetwork
description: "vendorNetwork.ts — frontend bindings for charlie's vendor-catalog corpus + vendor lifecycle."
metadata:
  type: reference
  kind: code-frontend
  galaxy: unassigned
  sourcePath: mcp-server/web/src/api/vendorNetwork.ts
  sourceHash: c56a55b6
  loc: 113
aliases: vendorNetwork.ts
---

# vendorNetwork.ts -- Frontend SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-04T14:51:51.225Z  |  source: `mcp-server/web/src/api/vendorNetwork.ts` (113 LOC)

## What it is
```
vendorNetwork.ts — frontend bindings for charlie's vendor-catalog corpus + vendor lifecycle.

Surfaces VENDOR-NETWORK-MS0 (charlie's ingested 30+ tool-maker catalogs) and the vendor
performance / ranking actions to the ERP UI. Until now these actions were LIVE in
businessDispatcher but had NO frontend binding (charlie's corpus was invisible to the app).

Reachability is two-legged and BOTH legs are verified:
  1. the businessDispatcher CASE exists (scripts/erp-action-contract-check.mjs — proves the MCP
     action handler is live), and
  2. the HTTP route is mounted + allowlisted (src/routes/business.ts → POST /api/v1/business/dispatch,
     covered by businessDispatchRoute.test.ts — proves the browser can actually reach it).
(Leg 2 was the gap the HOTEL-NETPLAT-UI scrutiny caught: the action was live but unreachable.)
Pure binding layer — no business logic; the types mirror the engine r
```


## Exports
`ErpVendorRole` (type), `VendorTier` (type), `VendorRecord` (interface), `VendorCatalogFilter` (interface), `VendorScorecard` (interface), `RankedVendor` (interface), `vendorCatalogQuery` (const), `vendorRank` (const), `vendorComputeScorecard` (const), `vendorListAll` (const)

## Key methods
`unwrapBusiness`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/web/src/api/vendorNetwork.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Frontend

## How to query it
Recall by meaning via `/system-viz find vendorNetwork.ts` or `prism_session:master_index_query`; open the source at `mcp-server/web/src/api/vendorNetwork.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
