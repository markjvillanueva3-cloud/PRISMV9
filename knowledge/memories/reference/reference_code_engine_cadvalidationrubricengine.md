---
name: reference_code_engine_cadvalidationrubricengine
description: "CAD-DRAW-MAX-MS1/U-VALIDATION-50-SCORING"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADValidationRubricEngine.ts
  sourceHash: 4b155a74
  loc: 213
aliases: CADValidationRubricEngine
---

# CADValidationRubricEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADValidationRubricEngine.ts` (213 LOC)

## What it is
CAD-DRAW-MAX-MS1/U-VALIDATION-50-SCORING

```
CADValidationRubricEngine — CAD-DRAW-MAX-MS1/U-VALIDATION-50-SCORING

Richer scoring rubric for the {@link CADDrawAnyPartValidationHarnessEngine}.
Replaces the v1 binary pass/fail rubric (exported? → 1.0 : 0.0) with a
weighted partial-credit score that distinguishes:

  - exportedSuccessfully (0.50 weight)  — primary success signal
  - iter-efficiency      (0.20 weight)  — reward < maxOps utilization
  - op-count adequacy    (0.20 weight)  — reward opLog length within expected band
  - no-failure-halt      (0.10 weight)  — reward absence of "live-failure-halt" stop

Weights sum to exactly 1.0. The score is in [0..1]; verdict is `pass` iff
`score >= passThreshold` (default 0.70, same as the harness gate semantics).

**Pluggable into harness:** the harness's `opts.scorer` injection point
accepts a function `(case, result) → ValidationCaseVerdict`. This rubric
exports `score()` + a thin ada
```


## Exports
`WEIGHT_EXPORTED` (const), `WEIGHT_ITER_EFFICIENCY` (const), `WEIGHT_OP_COUNT` (const), `WEIGHT_NO_FAILURE_HALT` (const), `DEFAULT_RUBRIC_PASS_THRESHOLD` (const), `DEFAULT_MAX_OPS_REFERENCE` (const), `RichScoreBreakdown` (interface), `RichScoreInput` (interface), `clamp01` (function), `scoreIterEfficiency` (function), `scoreOpCount` (function), `score` (function), `scoreCaseRich` (function), `CADValidationRubricEngine` (class), `cadValidationRubricEngine` (const)

## Key methods
`score`, `scoreCase`

## Depends on
[[reference_code_engine_caddrawanypartorchestratorengine]] | [[reference_code_engine_caddrawanypartvalidationharnessengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADValidationRubricEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADValidationRubricEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADValidationRubricEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
