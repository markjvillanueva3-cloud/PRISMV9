---
name: reference_mcp_route_suppress_isverbosebash_2026_06_20
description: "Second concrete application of feedback_low_take_rate_nudges_are_net_negative: suppressed the isVerboseBash mcp-route-suggest nudge (417 fires / 0 takes / 51.4% fire-share, audit-flagged suppress-candidate) via a reversible interim static filter, telemetry preserved. Commit e0b3df1ea0 (slot:alpha)."
type: reference
slot: alpha
galaxy: token-optimization
source: prism-memory
synced: 2026-06-27T20:30:46.653Z
aliases: reference_mcp_route_suppress_isverbosebash_2026_06_20
---


**SHIPPED 2026-06-20 (slot:alpha)** -- `e0b3df1ea0` `[TOKEN-SAVINGS]/U-MCP-ROUTE-SUPPRESS-ISVERBOSEBASH`. The second concrete application of [[feedback_low_take_rate_nudges_are_net_negative]] (the first was backendAuditChain, U-MCP-ROUTE-SUPPRESS-LOW-TAKE `26ff54ae02`).

**What:** `scripts/audit-mcp-route-takerate.mjs` (re-run 2026-06-20) flagged **`isVerboseBash`** as the new dominant net-negative route-suggest nudge: **417 fires / 0 takes / 0.0% take-rate / 51.4% fire-share**, health=`genuine-low-take-rate` (credit path PROVEN LIVE: 26 evaluations, 0 landed within the take-up window). The audit labels it `suppress-candidate` -- and the route-suggest-decay actor (`scripts/lib/route-suggest-decay.mjs`, wired live 2026-06-12) deliberately will NOT auto-mute it, because it requires the exact `suppress` verdict AND takes>0 as a measurement-gap safety guard. So a 0-take suppress-candidate falls through the auto-decay.

**Fix:** new pure exported `applyInterimSuppress(messages)` + `_INTERIM_LOW_TAKE_SUPPRESS = new Set(["isVerboseBash"])` in `.claude/hooks/mcp-route-suggest.mjs` (mirrors the sibling `appendActionHints`/`_REDUNDANT_CLASSIFIERS` idiom). Wired into `main()` AFTER `_recordRouteFires` (so the fire is STILL counted -- the audit keeps measuring the would-be need) and before the decay filter. Drops the base message entirely (not just the action-hint suffix `appendActionHints` already trims). Reversible: `PRISM_MCP_ROUTE_INTERIM_SUPPRESS=0`.

**Validation:** 11/11 new tests + 29/29 redundancy regression. LIVE A/B through the real hook (`cat package.json` event, dead-Ollama-URL forces the regex path): default -> nudge suppressed; `=0` -> nudge restored. 3-of-3 PASS (arm B mutation-tested: empty-set/inverted-predicate/knob mutations all caught -> non-tautological).

**Doctrine reinforced:** the `suppress reversibly + keep telemetry counting the match` pattern is now applied twice; the static interim set is the operator-decided mute for `suppress-candidate` verdicts the decay actor's 0-take guard ignores. **Next offenders for a future audit-driven pass (NOT yet suppressed -- they are `verify-wiring`, cause unproven):** `isLargeRead` (233 fires, 28.7%) + `doctrineSurface` (95) + `backendAuditChain` (63, still firing -- the 26ff54ae02 patcher was a patch-sibling, verify whether it was ever applied live). Suppressing any of these requires first confirming the per-classifier credit path was exercised (the fleet-level `evaluations=26` does NOT prove it for an individual classifier whose own evaluations may be 0).

**CORRECTION (2026-06-20, R8 read-before-write, same session):** an EARLIER draft of this note said "the comprehensive fix (decay actor consuming `suppress-candidate` when the credit path is proven live) stays queued." **That is RULED OUT, not queued -- it is a trap.** Reading `scripts/lib/route-suggest-decay.mjs:61-77` shows `computeSuppressSet` *deliberately* requires `recommendation === "suppress"` AND `takes > 0`, with a load-bearing comment (lines 65-75) stating the `suppress-candidate` verdict (always `takes===0`) must NEVER be auto-muted because a 0-take can be a measurement artifact OR a rarely-but-genuinely-valuable nudge -- `.startsWith("suppress")` "would wrongly catch it and silently kill the operator's graph-utilization nudges." So making the decay actor consume `suppress-candidate` would CONTRADICT that safety invariant (R7). The static `_INTERIM_LOW_TAKE_SUPPRESS` set is therefore the CORRECT design (per-classifier operator deliberation = exactly the property the decay actor protects), not a stopgap awaiting a "comprehensive" replacement. A future net-negative `suppress-candidate` classifier should be added to the static set after an operator decision -- never auto-decayed. Sibling: [[reference_route_decay_splice_wired_2026_06_12]].

**SECOND TRAP AVOIDED (2026-06-20, same session, R9 read-the-test):** the queued "next unit" to relabel sub-dominant proven-credit-path 0-take classifiers (isLargeRead/doctrineSurface/backendAuditChain) from `verify-wiring` to `retune` is ALSO RULED OUT. `audit-mcp-route-takerate.mjs classify()` returns `verify-wiring` for the non-dominant (share<30%) + evaluations>0 + takes===0 case DELIBERATELY -- the comment (lines 63-71) calls it "protective, behaviour-preserving" and `audit-mcp-route-takerate.test.mjs:229-232` LOCKS it ("not dominant enough to flag as net-cost"). Relabeling would break a tested, conscious design choice (R7/R9/R11 -- the author's choice is more recent AND tested). **META-LESSON:** the route-suggest take-rate subsystem's apparent "gaps" (suppress-candidate-not-auto-decayed; non-dominant-proven-as-verify-wiring) are CONSCIOUS, TESTED, documented choices, NOT unfinished work -- stop treating them as queued comprehensive fixes; two read-before-write checks this session each caught a would-be regression of a deliberate decision. The ONLY honest residue is a minor doc-wording nit: the rendered legend (`renderMd` ~line 218) describes `verify-wiring` as "evaluations===0" while the code also emits it for non-dominant proven -- a doc inaccuracy, NOT a behavioural bug; low-priority, batch with other doc work, do NOT rewrite the tested `classify()` logic.

**SHIPPED 2026-06-20 (slot:alpha) -- `5be19a26c2` `[TOKEN-SAVINGS]/U-AUDIT-LEGEND-HONESTY`:** the legend doc-wording nit above is now FIXED. `renderMd` verify-wiring legend (line ~218) rewritten to name BOTH causes -- "either the credit path is unexercised (evaluations===0, cause unproven) OR it is exercised (evaluations>0) but the classifier is non-dominant (<30% share, so not flagged as net-cost)" -- so a `genuine-low-take-rate` dashboard (evaluations=26 PROVEN LIVE) no longer misdirects the operator to verify wiring that is actually proven for the 3 sub-dominant 0-take classifiers. `classify()`/`summarize()` UNTOUCHED (the deliberate non-dominant->verify-wiring choice preserved). +1 revert-sensitive test (26/26 pass), live render confirmed, per-file 2-arm + 2-of-2 gate PASS. **P2 residuals:** (1) docstring legend lines 14-18 (pre-suppress-candidate 4-bucket stale comment) -- **CLOSED in `0aab43dadc` `U-AUDIT-LEGEND-DOCSTRING`** (synced to the accurate 5-bucket form matching classify()+renderMd, ASCII-normalized, 2-of-2 PASS, comment-only); (2) the generated dashboard `state/shared/dashboards/mcp-route-takerate-audit.md` self-heals to the new legend on next regen (still open, low-harm, generated artifact).
