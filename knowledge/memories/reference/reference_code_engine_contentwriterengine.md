---
name: reference_code_engine_contentwriterengine
description: "that's the role of an LLM round-trip downstream. This engine is"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ContentWriterEngine.ts
  sourceHash: 505fb25c
  loc: 326
aliases: ContentWriterEngine
---

# ContentWriterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ContentWriterEngine.ts` (326 LOC)

## What it is
that's the role of an LLM round-trip downstream. This engine is

```
ContentWriterEngine
===================

OBSIDIAN-CONTENT-MS2/U-VOICE-SPEC

Voice-rule validator for content drafts. The engine itself does NOT write
prose — that's the role of an LLM round-trip downstream. This engine is
the deterministic gate the article describes:

  "Read the draft against the voice section of CLAUDE.md. Flag any
   sentence that sounds like it came from a generic AI writing tool.
   Rewrite flagged sentences before delivering."

Loads `knowledge/voice-spec.md` (or override path) — frontmatter contains
the machine-checkable rules:

  ban_words: [...]                # exact-match (case-insensitive) flags
  ban_punctuation: [";"]          # any occurrence flags
  max_sentence_length_chars: 200  # per-sentence character cap
  require_specific_number: true   # whole-draft must contain >=1 digit
  min_screenshot_line: true       # at least one line should be quotable

Out
```


## Exports
`VoiceRules` (interface), `ViolationSeverity` (type), `VoiceViolation` (interface), `ValidateOptions` (interface), `ValidateResult` (interface), `ContentWriterEngine` (class), `contentWriterEngine` (const)

## Key methods
`validate`, `loadRules`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ContentWriterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ContentWriterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ContentWriterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
