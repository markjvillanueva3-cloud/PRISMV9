---
name: reference_code_engine_utilizationcontractengine
description: "Maps every PRISM capability through its full discovery chain:"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/UtilizationContractEngine.ts
  sourceHash: d0d82243
  loc: 412
aliases: UtilizationContractEngine
---

# UtilizationContractEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/UtilizationContractEngine.ts` (412 LOC)

## What it is
Maps every PRISM capability through its full discovery chain:

```
UtilizationContractEngine — MXU-MS0A + MXU-MS0

Maps every PRISM capability through its full discovery chain:
  Engine → Dispatcher → Action → Skill → UI Surface

Identifies unreachable capabilities — engines that exist but have
no user-facing path to invoke them.

The "utilization contract" states: every engine MUST have at least
one dispatcher action that exposes it. Engines without actions are
"dark" — built but invisible to users.

Sources:
  - MXU-MS0A: Utilization Contract Hardening
  - MXU-MS0: Capability Census + Activation Matrix
  - ENGINE_DIGEST.md, DISPATCHER_DIGEST.md for static analysis
```


## Exports
`WiringLevel` (type), `EngineCapability` (interface), `DispatcherMapping` (interface), `ActivationGap` (interface), `UtilizationReport` (interface), `DomainUtilization` (interface), `CapabilityCensus` (interface), `UtilizationContractEngine` (class), `utilizationContractEngine` (const)

## Key methods
`classifyDomain`, `isInternal`, `mapCapabilities`, `findGaps`, `generateReport`, `runCensus`, `quickStats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/UtilizationContractEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find UtilizationContractEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/UtilizationContractEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
