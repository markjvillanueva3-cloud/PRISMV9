---
name: reference_code_engine_telemetryengine
description: "PRISM F3: Dispatcher Telemetry & Self-Optimization Engine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/TelemetryEngine.ts
  sourceHash: 5e4b46ca
  loc: 613
aliases: TelemetryEngine
---

# TelemetryEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/TelemetryEngine.ts` (613 LOC)

## What it is
PRISM F3: Dispatcher Telemetry & Self-Optimization Engine

```
PRISM F3: Dispatcher Telemetry & Self-Optimization Engine
===========================================================

Observes all dispatcher invocations, aggregates metrics, detects anomalies,
and (optionally) adjusts route weights for optimization.

SAFETY: Telemetry failure = zero dispatcher impact. All methods are wrapped
in try/catch to ensure observation never blocks manufacturing operations.

Architecture:
  1. RingBuffer — per-dispatcher circular buffer of TelemetryRecords
  2. Aggregator — periodic metric computation across time windows
  3. AnomalyDetector — sigma-based deviation alerts with escalation
  4. RouteOptimizer — weight adjustments (DISABLED by default)

@version 2.0.0 (rebuilt from type definitions + dispatcher interface)
@feature F3
@safety Telemetry failure = zero dispatcher impact
```


## Exports
`sha256` (function), `crc32` (const), `TelemetryEngine` (class), `telemetryEngine` (const)

## Key methods
`getInstance`, `init`, `ensureStateDir`, `record`, `pushRecord`, `recordWrapperOverhead`, `getDashboard`, `getDispatcherDetail`, `getAnomalies`, `getOptimizationLog`, `acknowledgeAnomaly`, `freezeWeights`, `unfreezeWeights`, `getDegradationLevel`, `checkSLOs`, `safeWriteSync`, `destroy`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/TelemetryEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find TelemetryEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/TelemetryEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
