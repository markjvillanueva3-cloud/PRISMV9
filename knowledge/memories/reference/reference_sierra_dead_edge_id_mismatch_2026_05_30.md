---
name: reference_sierra_dead_edge_id_mismatch_2026_05_30
description: "15,671 dead edges (1.47%) in the merged system-graph — id-scheme mismatch in a few producers (engine.<Pascal> + dispatcher.<name> vs canonical eng.<domain>/disp.*). Sweep now wired advisory into regen."
type: reference
source: prism-memory
synced: 2026-06-09T14:54:10.935Z
aliases: reference_sierra_dead_edge_id_mismatch_2026_05_30
---


**Bug-finding (slot:sierra, 2026-05-30, commit 9560b33374).** The dead-pixel sweep (`scripts/system-viz-dead-pixel-sweep.mjs`, pure core `scripts/lib/system-viz-dead-pixel-detector.mjs`, OOM-safe via `readGraphStreaming`) existed but was an **unwired manual CLI nobody ran** → the merged graph's edge-integrity regression was invisible. Wired it as an advisory (non-fatal) post-merge stage in `regen-viz.mjs` (after wiki-debt, before the drift gate) so `state/shared/system-viz-dead-pixels-<date>.{md,json}` refreshes every regen.

**VERIFIED FINDING (ran on the live 546MB graph, 301,327 nodes / 1,068,176 edges):** **15,671 dead edges (1.47%) → 50 distinct orphan targets** (edges whose target node does not exist). NOT random cruft — an **id-scheme mismatch** in a handful of producers:
- **10,291 dead edges → `engine.<PascalName>`** — e.g. `engine.AIResourceLearningEngine` (×2302) + `engine.MasterAITrainingLedgerEngine` (×2302) + `engine.SurfaceFinishPredictorEngine` (×899)…, almost all FROM `pdf-extract.*` and `college.course.*` source nodes. Canonical engine node id is `eng.<domain>.<name>` (lowercase), not `engine.<Pascal>`.
- **2,944 → `dispatcher.<name>`** — e.g. `dispatcher.prism_calc` (×984), `dispatcher.prism_ai` (×897), `dispatcher.prism_cam` (×643), FROM `ghost.unwired.*` nodes. Canonical is `disp.*` (per [[reference_sierra_dispatcher_id_ssot]]) — NOT `dispatcher.prism_X`. NOTE: seed-ghost-from-unwired.mjs uses `disp.*` correctly (verified prior session), so the `dispatcher.*` leak is a DIFFERENT producer (suspect `generate-wiring-overlay.mjs` phantom wiring edges) — confirm before fixing.
- **1,350 → `eng.<name>`** (lowercase but still missing, e.g. `eng.other.airesourcelearningengine` ×17) + 254 bare `prism_dev` + 220 `prism_data` (from `extracted.*` modules).
- By source layer: 12,806 from L9 · 786 L10 · 775 L4a · 693 L13.

**FOLLOW-UP (the producer target-id fix — NOT done, needs care):** for EACH leaking producer, either (a) remap the edge target to the real node id (`dispatcher.prism_calc`→the real `disp.*` node; `engine.<Pascal>`→`eng.<domain>.<name>`) IF the target engine exists under another id, OR (b) the target is a genuinely-absent (planned/not-in-graph) engine and the edge is correctly dangling → leave or prune. **Must verify per-target: prefix-bug vs genuinely-missing-node** before remapping (don't connect an edge to a wrong node). Highest-volume quick wins: the pdf-extract/college-course `engine.<Pascal>` resolver (~10K edges) and the `ghost.unwired → dispatcher.*` producer (~2.9K). The committed report (`state/shared/system-viz-dead-pixels-2026-05-31.md`) names every orphan target + its source examples. Reinforces [[feedback_sierra_graph_correctness_is_fleet_search]] · [[reference_sierra_dispatcher_id_ssot]].

---

**RESOLUTION (2026-05-31, slot:sierra /goal /loop — 3 commits, dry-run-corrected).** The pre-compact handoff hypothesis was WRONG on the engine class; a read-only dry-run against the LIVE 573MB graph corrected it (the "hermetic fakes don't prove production wiring" lesson, again — Half B's unit tests passed with fabricated `eng.ai.airesourcelearningengine` nodes but remapped 0 on the real graph). Three commits:
- **Half A — dispatcher PRODUCER fix** (`U-VIZ-G4-DEAD-EDGE-DISP`): extracted `mcpToolToDispNodeId` SSOT → `scripts/lib/viz-dispatcher-node-id.mjs`; routed the 3 leaking producers (`seed-ghost-llm-classify:217`, **`seed-ghost-gnn-classify:456`** — a THIRD producer the grep almost missed; builds its own edge, not via classificationToGraphUpdate, `generate-pdf-course-bridge-features` dispatcher tables) through it. Also fixed the consumer `extract-cadcam-tribal-wiki` (imports the bridge's dispatcher tables → tribal corpus now shows bare MCP names). seed-ghost-from-unwired was already correct (my 2026-05-20 G4 fix).
- **Half B — engine MERGE canon** (`U-VIZ-G4-DEAD-EDGE-ENG`): `canonicalizeGraphEdgeTargets` remaps `engine.<Pascal>`→`eng.<domain>.<name>` via graph-alias. **HONEST FINDING (R12): remaps ~0 on the current graph** — the 3,392 dead `engine.*` edges target 20 engines with NO `eng.*` node (`PdfMachiningHandbookExtractorEngine`, `PrismTrainingModuleEngine` = genuinely-nonexistent; `ToolLifeEngine`/`MaterialRegistryEngine`/`CADGeometryEngine` = exist only as variants/files, not noded). This is the pdf-course-bridge's INTENTIONAL gap-surfacing (its own docstring says so) — NOT a prefix bug. Half B is a correct, zero-risk, future-proof pass (fires when a remappable engine.* edge appears, e.g. AIResourceLearningEngine which DOES have an alias). The real engine-class fix is cross-lane (india corrects the bridge maps to real engine class names) OR a sweep-reclassification distinguishing intentional-advisory bridges from integrity defects — QUEUED, not mine this session.
- **Half C — dispatcher MERGE canon** (`U-VIZ-G4-DEAD-EDGE-DISP-MERGE`): the KEY architectural finding — **the merged graph is CUMULATIVE** (merge reads the persistent `system-graph.json` + adds, never removes stale-target edges; verified in regen-viz: merge is spawned first, no fresh-base rebuild). So Half A's producer fix alone CANNOT clear the ~2,676 accumulated `dispatcher.prism_*` edges. `canonicalizeGraphEdgeTargets` unifies engine + dispatcher remap: `dispatcher.prism_X`→`disp.<file>` via the MCP table, **node-existence-gated** (only remaps when the disp.* node exists → never mints a fresh dead target like `disp.prism_shop`), with dedup-drop onto the producer's new canonical edge. Live: ~2,643 of 2,676 dispatcher dead edges remap dead→live (the 16 table dispatchers all exist as nodes); +4 SSOT keys (prism_data/validation/machinelive/multiop) recover ~14 more. Reverts with `PRISM_VIZ_ENGINE_CANON_DISABLE=1`.

**MATERIALIZED + SWEPT (2026-05-31):** dead edges **15,671 → 6,128 → 5,342 (−10,329, 66% reduction)** — confirmed by `system-viz-dead-pixel-sweep.mjs` after each merge. (5 commits: dispatcher-producer + engine-merge-canon + dispatcher-merge-canon + classify + **bare-name canon**.) The bare-name commit (`d46594ba75`, U-VIZ-G4-DEAD-EDGE-BARE) fixed a 4th sub-class: `generate-extracted-modules-detail-features.mjs` emits BARE names (no `engine.`/`dispatcher.` prefix) on `bridge_to_existing`→matched_engine (PascalCase) + `wire_target`→recommended_dispatcher (prism_*); `canonicalizeGraphEdgeTargets` now handles these EDGE-TYPE-GATED (245+541=786 dead→live, all dry-run-verified resolvable/0 missing). Also added `classifyDeadEdgeType` to the dead-pixel detector → the residual 5,342 split **3,710 advisory** (pdf-course-bridge intentional engine bridges — cross-lane india fix) + **~1,632 defect** (the next triage: invokes 775, engine_import 198, covers 188, synergy 104, uses_type/uses_constant, imports_from, extracts). Merge `edgeCanon` log: `eng 6645 remap/3658 miss, disp 2657 remap/19 miss, 8447 dup-dropped`. NOTE the engine remap was 6,645 NOT 0 (the dry-run's 0) — a FULL merge re-adds the galaxy "planet" per-engine nodes (~5,848) which enrich the alias index, so Half B fires substantially in the real merge even though a bare dry-run on the pre-merge graph showed 0. **Materialized via `merge-augmentations.mjs` STANDALONE** (light path: ~3min, skips the 95 FAST generators) because a full `regen-viz` DIED mid-FAST at vault-graph under host memory pressure (96% commit; the SIGKILL-no-error signature). Merge-only writes a valid canon-fixed graph but skips the regen-viz post-merge stages (seed-ghost/dedup/reparent/drift-gate) — those refresh on the next FULL regen when memory headroom returns (net-positive meanwhile: fewer dead edges, graph 573→543MB). Residue 6,128 = the genuinely-missing-engine bridges (intentional advisory) + a few missing dispatchers — the cross-lane/sweep-reclassification follow-up.

**RE-DOMAIN EXTENSION (2026-05-31, commit 969edbe638) — drives to 75%:** two more dead→live canon branches in `canonicalizeGraphEdgeTargets`: (1) `eng.<wrong-domain>.<name>` MISSING → re-resolve by LAST SEGMENT to the real eng.* node (mis-domain class: `eng.other.agenticloopengine`→`eng.ai.agenticloopengine`; invokes 775 + covers 188 + engine_import 82); (2) `disp.<wrong-name>` MISSING → re-resolve via MCP table (`disp.prism_data`→`disp.datadispatcher`; 28). Ordering: `engine.` checked before `eng.` (engine.X startsWith eng. too). Materialized: dead edges **5,342 → 3,964** (−1,378). **FULL G4 ARC: 15,671 → 3,964 = −11,707 (75% reduction).** The remaining 3,964 are ~3,658 ADVISORY (bridge-to-engine/enriches-engine — pdf-course-bridge intentional gap-surfacing → cross-lane india fix, correct the bridge maps to real engine class names) + ~306 genuinely-missing (disp.unknown, prism_shop, dispatcher.prism_agent-needs-table-entry, synergy `file:` URIs = different scheme). **The sierra-fixable dead-edge pool is essentially drained at 75%.**

**REGRESSION I caused (2026-05-31, lesson):** ran `git-lock-sweeper.mjs` on a 2-min-old 5.9M `.git/index.lock` that was actually a SLOW-BUT-LIVE peer commit (slot da9aacf5, galaxy-context-card files) under host memory pressure — the sweeper judged it stale (age > 30s threshold) and removed it, likely aborting the peer's in-flight commit (recoverable — their loop retries). **Lessons:** (a) on the SHARED main tree with active peers, a slow git commit (memory-pressured) can exceed the sweeper's staleness threshold while still live — don't aggressively sweep; (b) the SAFE way to commit when peer files are staged in the shared index is a **pathspec-limited commit** `git commit -- <my-files>` which commits ONLY the named paths regardless of what else is staged (avoids the zulu-incident peer-file-sweep). This is the canonical fix for shared-index multi-writer contention short of migrating to the slot worktree.

**Standing lessons:** (1) any merged-graph edge-integrity fix must account for the CUMULATIVE merge — producer fixes don't clear accumulated edges; the merge-side canon is the only place that does (it's also the single writer, so no second-writer violation). (2) ALWAYS dry-run a graph transform against the live graph before a 6.5-min regen — hermetic tests with fabricated nodes pass while the real graph remaps 0. (3) the dead-pixel "engine.*" class was a node-COVERAGE + intentional-advisory mix, not the prefix bug the 2026-05-31 report hypothesized. 19 canon + 8 dispatcher + 36 parity tests; 4 reviewer passes (all PASS after P0 [missed consumer] + P2 [comment honesty, lazy-keep, +4 keys] fixes).
