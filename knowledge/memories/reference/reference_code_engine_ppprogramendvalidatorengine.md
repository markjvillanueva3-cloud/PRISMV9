---
name: reference_code_engine_ppprogramendvalidatorengine
description: "Validate program termination (M30/M2/M99)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PPProgramEndValidatorEngine.ts
  sourceHash: f6b01306
  loc: 415
aliases: PPProgramEndValidatorEngine
---

# PPProgramEndValidatorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PPProgramEndValidatorEngine.ts` (415 LOC)

## What it is
Validate program termination (M30/M2/M99)

```
PPProgramEndValidatorEngine — Validate program termination (M30/M2/M99)

G-code programs must terminate cleanly. Missing, misplaced, or
duplicate end codes create the classic "machine sits idle / keeps
running / runs the wrong block" post-program failure modes:

  M30  — Program end + rewind (main program). Control rewinds to the
         start and halts. Required terminator for main programs.
  M02  — Program end (no rewind). Accepted by most controls but some
         modern Fanuc/Okuma insist on M30.
  M99  — Subprogram return. Required at the end of every O-number
         subprogram body. If M99 appears at the end of the MAIN
         program on a Fanuc control, the program loops forever.
  %    — End-of-tape/file marker. Legacy but still required by some
         dripfeed/DNC setups and all Heidenhain iTNC controls.

Failure modes this validator catches:
  - missing_program_end (er
```


## Exports
`PESeverity` (type), `PEIssue` (interface), `PEResult` (interface), `PEOptions` (interface), `PPProgramEndValidatorEngine` (class), `ppProgramEndValidatorEngine` (const)

## Key methods
`validate`, `quickCheck`, `defaultOptions`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PPProgramEndValidatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PPProgramEndValidatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PPProgramEndValidatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
