---
name: reference_code_hook_renamewindowintercept
description: "rename-window-intercept.mjs — UserPromptSubmit hook."
metadata:
  type: reference
  kind: code-hook
  galaxy: unassigned
  sourcePath: .claude/hooks/rename-window-intercept.mjs
  sourceHash: 6e012dbc
  loc: 201
aliases: rename-window-intercept.mjs
---

# rename-window-intercept.mjs -- Hook SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `.claude/hooks/rename-window-intercept.mjs` (201 LOC)

## What it is
```
rename-window-intercept.mjs — UserPromptSubmit hook.

Two jobs, both with ZERO model turn:

 1. INSTANT RENAME — if the submitted prompt is `/rename <name>` (or
    `/rename-chat <name>`) for THIS chat, update the slot label in
    chat-slots.json AND flip the Windows Terminal tab title immediately,
    then BLOCK the prompt so the model never runs (no "thinking" turn).
    This is why it's a hook, not a skill: a skill always costs a turn.

 2. ALWAYS-MATCH — on every other prompt, cheaply re-assert the tab title
    to this chat's current slot topic (stamp-cached, so steady-state cost
    is one temp-file read; PowerShell only spawns when the name changed).
    Satisfies the standing rule "terminal name should match chat name".

Peer-rename (`/rename <slot> <name>`) is intentionally NOT intercepted —
it falls through to the /rename skill (rare, operator, needs the model).

Caveat (R12, 
```


## Exports
`chatIdFromSession` (function), `parseRenameArg` (function), `isPeerForm` (function), `composeSlotTitle` (function)

## Key methods
`verbose`, `main`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `.claude/hooks/rename-window-intercept.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Hook

## How to query it
Recall by meaning via `/system-viz find rename-window-intercept.mjs` or `prism_session:master_index_query`; open the source at `.claude/hooks/rename-window-intercept.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
