---
name: reference_code_lib_tokenawarenessstate
description: "TOKEN-AWARENESS-MS0 / U-TA01 — pure lib for token-awareness state machine."
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/token-awareness-state.mjs
  sourceHash: 008b709e
  loc: 266
aliases: token-awareness-state.mjs
---

# token-awareness-state.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/token-awareness-state.mjs` (266 LOC)

## What it is
```
TOKEN-AWARENESS-MS0 / U-TA01 — pure lib for token-awareness state machine.

Pure functions only. No I/O. Composes 4 data sources into one canonical
TokenAwarenessState; classifies into 4 zones; recommends actions.

Data sources (any subset may be present):
  1. statusline — { ctxTokens, ctxMaxTokens } from transcript tail-read
  2. rateLimits — { fiveHourPct, sevenDayPct, ... } from Claude Code v1.2.80+ statusLine stdin
  3. transcript — { cumulative: { input, cache_read, cache_creation, output } } dedup-by-msgId
  4. offload    — { offloaded, kept, ratio } from ollama-offload-stats.json

Zone classification — worst-of (any single signal at RED → RED):
  GREEN    worst < 60%
  YELLOW   60% <= worst < 85%
  RED      85% <= worst < 95%
  CRITICAL worst >= 95%

Honest scope (R12): an unknown signal doesn't downgrade the zone; it's
excluded from the worst-of. A stale state (>180s) is flagged
```


## Exports
`SCHEMA_VERSION` (const), `DEFAULT_THRESHOLDS` (const), `DEFAULT_STALE_TTL_MS` (const), `ZONES` (const), `sanitizePct` (function), `sanitizeTokens` (function), `computeZone` (function), `computeWorst` (function), `decideAction` (function), `isStale` (function), `mergeFromSources` (function), `applyStaleness` (function), `thresholdsFromEnv` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/token-awareness-state.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find token-awareness-state.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/token-awareness-state.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
