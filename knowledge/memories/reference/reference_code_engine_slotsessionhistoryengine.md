---
name: reference_code_engine_slotsessionhistoryengine
description: "per-slot session history sidecar."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SlotSessionHistoryEngine.ts
  sourceHash: 80e65093
  loc: 728
aliases: SlotSessionHistoryEngine
---

# SlotSessionHistoryEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SlotSessionHistoryEngine.ts` (728 LOC)

## What it is
per-slot session history sidecar.

```
SlotSessionHistoryEngine — per-slot session history sidecar.

SLOT-RECOVERY-MS0/U-SR01 (2026-05-25, slot:golf, claude-b509cb68)
Spec: state/shared/specs/SLOT-RECOVERY-MS0.md §2-3

Solves: when a chat dies, the session-id is forgotten because chat-slots.json
stores only the CURRENT chatId per slot. Last night's 20-chat crash left the
operator unable to identify which session_id belonged to which slot.

Storage:
  state/shared/slot-sessions/<nato>.jsonl — append-only, per-slot.

3 event types per spec §2:
  - session-start (SessionStart hook fires)
  - heartbeat (60s via heartbeat-keepalive chain extension)
  - session-end (Stop / PreCompact hook fires, exitState: clean/precompact/stop/crash-inferred)

Crash-inferred invariant (the key design):
  At session-start, the engine reads the previous tail of the slot's JSONL.
  If the last entry is NOT session-end (meaning the prior session never
```


## Exports
`SLOT_NAMES` (const), `NatoSlot` (type), `DEFAULT_BASE_DIR` (const), `DEFAULT_RETENTION` (const), `SCHEMA_VERSION` (const), `DEFAULT_RESUME_MAX_AGE_HOURS` (const), `STALE_THRESHOLD_MS` (const), `CRASHED_THRESHOLD_MS` (const), `EventType` (type), `ExitState` (type), `SessionStartEvent` (interface), `HeartbeatEvent` (interface), `SessionEndEvent` (interface), `SlotSessionEntry` (type), `FleetStateCard` (interface), `IsResumeEligibleResult` (interface), `SlotSessionHistoryEngine` (class), `slotSessionHistoryEngine` (function), `_resetSlotSessionHistoryEngineForTests` (function)

## Key methods
`mkdirSync`, `readAll`, `appendFileSync`, `pruneSlot`, `writeFileSync`, `renameSync`, `recordSessionStart`, `recordHeartbeat`, `recordSessionEnd`, `getLatestForSlot`, `getHistoryForSlot`, `getLatestSessionEnd`, `getAllSlotsState`, `isResumeEligible`, `detectCrashedSlot`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/SlotSessionHistoryEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SlotSessionHistoryEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SlotSessionHistoryEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
