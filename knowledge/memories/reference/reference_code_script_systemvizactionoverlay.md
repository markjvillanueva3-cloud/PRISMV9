---
name: reference_code_script_systemvizactionoverlay
description: "system-viz-action-overlay — render the append-only agent-write trace log"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/system-viz-action-overlay.mjs
  sourceHash: 5303897e
  loc: 253
aliases: system-viz-action-overlay.mjs
---

# system-viz-action-overlay.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/system-viz-action-overlay.mjs` (253 LOC)

## What it is
```
system-viz-action-overlay — render the append-only agent-write trace log
into a READ-ONLY system-viz overlay (timeline + per-agent / per-target /
per-tool rollups).

OBSIDIAN-INTELLIGENCE-MS3 / U-ACTION-TRACES (D4).

Reads `state/shared/action-traces.jsonl` (the ActionTraceEngine log;
PRISM_ACTION_TRACE_FILE override honoured for parity with the engine) and
writes ONE additive staging artifact:

  state/shared/system-viz/staging/action-trace-overlay.json

It never mutates system-graph.json or any other graph input — the overlay
is a sidecar the viz layer reads on top of the live graph. Rollback is
"delete the staging file"; the trace log is untouched.

The overlay deliberately drops `promptHash` + `sessionId` from the per-edge
`recent[]` slice — a viz timeline only needs the visible dims (ts / agent /
tool / target / action), and a JSON artifact under state/shared/ should not
carry the p
```


## Exports
_(no top-level exports detected)_

## Key methods
`atomicWriteJson`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/system-viz-action-overlay.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find system-viz-action-overlay.mjs` or `prism_session:master_index_query`; open the source at `scripts/system-viz-action-overlay.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
