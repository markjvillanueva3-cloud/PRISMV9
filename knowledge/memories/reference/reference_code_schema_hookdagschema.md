---
name: reference_code_schema_hookdagschema
description: "hookDAGSchema — HOOK-MANIFEST-DAG-MS26 / P0-U02"
metadata:
  type: reference
  kind: code-schema
  galaxy: unassigned
  sourcePath: mcp-server/src/schemas/hookDAGSchema.ts
  sourceHash: 67b6d0c3
  loc: 128
aliases: hookDAGSchema.ts
---

# hookDAGSchema.ts -- Schema SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/schemas/hookDAGSchema.ts` (128 LOC)

## What it is
```
hookDAGSchema — HOOK-MANIFEST-DAG-MS26 / P0-U02

Zod schema + types for {@link ../engines/HookDAGValidatorEngine.HookDAGValidatorEngine}'s output.

The validator consumes a {@link ../schemas/hookManifestSchema.HookManifest} (output of
{@link ../engines/HookManifestEngine.HookManifestEngine}, P0-U01) and produces, *per event*,
(a) the implicit hook execution DAG derived from settings-file ordering + cross-source
lexical order, (b) every cycle found in that DAG, and (c) a deterministic linearized
order when the per-event graph is a true DAG (no cycle).

Conflicts surfaced at the engine level (not encoded in the DAG itself but worth flagging):
 - **mixed-block-mode**: a file is wired with `continueOnError:false` in one source and
   `true`/`undefined` in another → ambiguous hard-block semantics across configs;
 - **duplicate-wiring**: a file is wired twice under the *same* (source, event, m
```


## Exports
`HOOK_DAG_SCHEMA_VERSION` (const), `HookDAGEdgeSchema` (const), `HookDAGEdge` (type), `HookDAGCycleSchema` (const), `HookDAGCycle` (type), `HookDAGConflictSchema` (const), `HookDAGConflict` (type), `HookDAGEventSchema` (const), `HookDAGEvent` (type), `HookDAGSummarySchema` (const), `HookDAGSummary` (type), `HookDAGValidationSchema` (const), `HookDAGValidation` (type)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/schemas/hookDAGSchema.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Schema

## How to query it
Recall by meaning via `/system-viz find hookDAGSchema.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/schemas/hookDAGSchema.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
