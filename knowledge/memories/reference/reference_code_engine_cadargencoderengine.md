---
name: reference_code_engine_cadargencoderengine
description: "CAD-DRAW-MAX-MS0/P1-U04"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADArgEncoderEngine.ts
  sourceHash: bf9e9b5f
  loc: 194
aliases: CADArgEncoderEngine
---

# CADArgEncoderEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADArgEncoderEngine.ts` (194 LOC)

## What it is
CAD-DRAW-MAX-MS0/P1-U04

```
CADArgEncoderEngine — CAD-DRAW-MAX-MS0/P1-U04

Encodes the **args** of a CADOperation into a fixed-dimensional vector.
NN01 (CADFoundationEncoderEngine) tokenizes only the `kind` field —
`sketch_create` vs `feature_extrude` — and is blind to `radius=5mm`,
`depth=10mm`, `draft=2deg`. For "draw any part" the AI must learn
across argument variations, not just op kinds. This adapter is the
missing arg-encoding tier between NN01's tokenizer and LP04's heads.

**Output shape.** A fixed `ARG_EMBED_DIM`-length number[] per
CADOperation. Concatenating NN01's `OP_EMBED_DIM` (8) + this
`ARG_EMBED_DIM` (8) gives a per-op feature vector with arg awareness;
pooling over a sequence then feeds LP04's value head.

**Encoding strategy.**
  - **Numeric args** (radius, depth, distance, angle, x/y/z): pulled
    into 5 canonical slots (`size`, `position`, `angle`, `count`,
    `tolerance`). Multi-field detec
```


## Exports
`ARG_EMBED_DIM` (const), `NUMERIC_SLOT_COUNT` (const), `CATEGORICAL_SLOT_COUNT` (const), `ArgEncoderStats` (interface), `CADArgEncoderEngine` (class), `cadArgEncoderEngine` (const)

## Key methods
`encodeArgs`, `encodeOpArgs`, `encodeOpArgsPooled`, `getStats`

## Depends on
`ICADCodeGenerator`

## Where it lives
- **Source:** `mcp-server/src/engines/CADArgEncoderEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADArgEncoderEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADArgEncoderEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
