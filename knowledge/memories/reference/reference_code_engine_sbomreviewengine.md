---
name: reference_code_engine_sbomreviewengine
description: "U-LPR-OPS-SBOM-REVIEW"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SBOMReviewEngine.ts
  sourceHash: 538a6c25
  loc: 504
aliases: SBOMReviewEngine
---

# SBOMReviewEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SBOMReviewEngine.ts` (504 LOC)

## What it is
U-LPR-OPS-SBOM-REVIEW

```
SBOMReviewEngine — U-LPR-OPS-SBOM-REVIEW

Quarterly SBOM review + OSV delta triage. Maintains a component catalog,
tracks vulnerability records linked to components, computes the delta
between OSV snapshots (new / unchanged / resolved), enforces quarterly
review cadence with remediation SLAs per severity, and flags
overdue-remediation conditions that block production changes.

Data model:
  - Component: package identity + version + license + supplier + last-reviewed.
  - Vulnerability: OSV/CVE id + severity + affected components + published.
  - OSVSnapshot: point-in-time vulnerability set for delta computation.
  - Remediation: per-vulnerability resolution record with due date.

Remediation SLAs (NIST SP 800-40r4, CISA BOD 22-01 alignment):
  critical → 7 days
  high     → 30 days
  medium   → 90 days
  low      → 180 days

Cadence: quarterly (90 d) full-SBOM review required; missed rev
```


## Exports
`VulnSeverity` (type), `RemediationStatus` (type), `ComponentType` (type), `Component` (interface), `Vulnerability` (interface), `OSVSnapshot` (interface), `OSVDelta` (interface), `Remediation` (interface), `SBOMReview` (interface), `SBOMPosture` (interface), `SBOMStats` (interface), `SBOMReviewEngine` (class), `sbomReviewEngine` (const)

## Key methods
`registerComponent`, `getComponent`, `listComponents`, `registerVulnerability`, `getVulnerability`, `listVulnerabilities`, `captureSnapshot`, `listSnapshots`, `computeDelta`, `listDeltas`, `openRemediation`, `updateRemediation`, `listRemediations`, `startReview`, `completeReview`, `listReviews`, `getPosture`, `getStats`, `clearAll`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/SBOMReviewEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SBOMReviewEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SBOMReviewEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
