---
name: reference_code_engine_regressionbaselineengine
description: "expected hash mismatch (pass / fail semantics changed)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/RegressionBaselineEngine.ts
  sourceHash: eeaa4e11
  loc: 334
aliases: RegressionBaselineEngine
---

# RegressionBaselineEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/RegressionBaselineEngine.ts` (334 LOC)

## What it is
expected hash mismatch (pass / fail semantics changed)

```
RegressionBaselineEngine (U-LPR-REGRESSION-BASELINE, QA B2)

Freezes the Lathe-Prod test contract at plan kickoff. Stores:
  • test_id → expected result sha256 (deterministic output hash)
  • test_id → timing p95 (Hyndman-Fan R-7 quantile of recent runs)
  • flaky-test quarantine list (explicit reason, never silent skip)

CI diff-gate: new run is compared against the frozen baseline and
failures fall into three buckets:

  RESULT_DIFF     — expected hash mismatch (pass / fail semantics changed)
  TIMING_REGRESS  — observed p95 > baseline_p95 × (1 + max_slowdown)
                    where max_slowdown defaults to 20 % per run.
  FLAKY           — test fails at rate > 1 % over 100 runs and is NOT
                    currently quarantined. Blocks until quarantine ticket
                    is filed with an explicit reason.

The engine distinguishes "hard" breaches (RESULT_DIFF, unquarantine
```


## Exports
`BreachKind` (type), `BreachSeverity` (type), `BaselineEntry` (interface), `QuarantineEntry` (interface), `TestRun` (interface), `Breach` (interface), `DiffGateReport` (interface), `RegressionBaselineEngine` (class), `regressionBaselineEngine` (const)

## Key methods
`freeze`, `getBaseline`, `listBaseline`, `quarantineTest`, `liftQuarantine`, `isQuarantined`, `listQuarantine`, `recordRun`, `evaluate`, `observedP95`, `toSnapshot`, `loadSnapshot`, `clearAll`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/RegressionBaselineEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find RegressionBaselineEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/RegressionBaselineEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
