---
name: reference_code_script_fleettaskhealthwatch
description: "fleet-task-health-watch.mjs — durable health watchdog over PRISM's"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/fleet-task-health-watch.mjs
  sourceHash: 9f599ecf
  loc: 1806
aliases: fleet-task-health-watch.mjs
---

# fleet-task-health-watch.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/fleet-task-health-watch.mjs` (1806 LOC)

## What it is
```
fleet-task-health-watch.mjs — durable health watchdog over PRISM's
Windows scheduled-task safety net.

THE GAP THIS CLOSES. PRISM's crash-prevention safety net is a set of
Windows scheduled tasks — `PRISM Fleet Reaper` (orphan-process reaper),
`PRISM Fleet Memory Monitor` (per-chat RSS attribution + /compact advisory),
`PRISM Cleanup Orchestrator`, `PRISM Node Orphan Cleaner`, `PRISM Orphan
Process Reaper (PS)`, `PRISM Zombie Reaper v2`, plus dev-cadence tasks
(`PRISM Hook Janitor`, `PRISM Synergy Regression Watch`). Every one runs
unattended on a timer. But NOTHING watches whether those tasks are THEMSELVES
healthy. Each installer verifies its own task once at registration; the
golf-slot reaper guardian re-checks ONLY `PRISM Fleet Reaper`. If any task is
disabled by a Windows update, wedges (a hung prior instance + `IgnoreNew`),
starts erroring (a nonzero `LastTaskResult` loop), or neve
```


## Exports
`LEDGER_SCHEMA_VERSION` (const), `DEFAULT_STALE_MULTIPLIER` (const), `DEFAULT_ADVISORY_COOLDOWN_SEC` (const), `DEFAULT_PS_TIMEOUT_MS` (const), `DEFAULT_HEAL_VERIFY_GRACE_MULT` (const), `DEFAULT_HEAL_VERIFY_FALLBACK_GRACE_MS` (const), `DEFAULT_HEAL_VERIFY_LOOKBACK_MS` (const), `MUST_EXIST_TASKS` (const), `CRASH_CRITICAL_TASKS` (const), `KNOWN_PRISM_TASKS` (const), `EXPECTED_UNREGISTERED_TASKS` (const), `EXPECTED_DISABLED_TASKS` (const), `readDeliberatelyDisabledTasks` (function), `MIGRATION_FREEZE_FLAG` (const), `isMigrationFreezeActive` (function), `INSTALLERS_DIR` (const), `discoverInstallerTasks` (function), `detectInstallerDrift` (function), `TASK_OWNER_DOMAIN` (const), `DEFAULT_OWNER` (const)

## Key methods
`foreach`, `writeFileSync`, `renameSync`, `rotateIfLarge`, `appendFileSync`, `mkdirSync`, `readReenableLedgerText`, `appendTelemetry`, `writeLedger`, `appendReenableLedger`, `appendChatBus`, `cmdReset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/fleet-task-health-watch.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find fleet-task-health-watch.mjs` or `prism_session:master_index_query`; open the source at `scripts/fleet-task-health-watch.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
