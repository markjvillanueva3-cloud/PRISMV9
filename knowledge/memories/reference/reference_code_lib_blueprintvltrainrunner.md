---
name: reference_code_lib_blueprintvltrainrunner
description: "scripts/lib/blueprint-vl-train-runner.mjs"
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/blueprint-vl-train-runner.mjs
  sourceHash: eb3eb022
  loc: 329
aliases: GpuNotTrainableError
---

# GpuNotTrainableError -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/blueprint-vl-train-runner.mjs` (329 LOC)

## What it is
```
scripts/lib/blueprint-vl-train-runner.mjs

T3.2 (INDIA-TAKEOVER-PLAN-blueprint-lora) — the Node→GPU runner: the missing
connective tissue between the .mjs orchestration layer and the real Qwen2.5-VL
PEFT trainer `mcp-server/scripts/blueprint_vl_train_lora.py` (T3.1, shipped
b121b19f7b). NO current engine owns this edge.

WHAT IT DOES (in dependency order, fail-loud at every gate):
  1. PRE-GATE on the STRONG GPU health check — `scripts/py/gpu_health.py` via the
     canonical py-subprocess-bridge. This is the arch_list + real-matmul probe
     (catches the silent-CPU sm_120 wheel), STRONGER than the trainer's own inline
     `preflight_gpu()` (which only does is_available()+matmul, no arch_list). We
     refuse to spawn a multi-minute training run on a GPU that would silently run on
     CPU at 1/50th speed. (This folds in T1.3's residual: the trainer's weaker inline
     preflight is no
```


## Exports
`GPU_HEALTH_SCRIPT` (const), `TRAINER_SCRIPT` (const), `DEFAULT_VL_HYPERPARAMETERS` (const), `GpuNotTrainableError` (class), `BundleUnusableError` (class), `TrainerFailedError` (class), `assertGpuTrainable` (function), `buildTrainerArgs` (function), `runBlueprintVlLoRATrain` (function), `makeBlueprintVlInnerTrain` (function)

## Key methods
`assertBundleUsable`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/blueprint-vl-train-runner.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find GpuNotTrainableError` or `prism_session:master_index_query`; open the source at `scripts/lib/blueprint-vl-train-runner.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
