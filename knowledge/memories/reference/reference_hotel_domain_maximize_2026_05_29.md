---
name: reference_hotel_domain_maximize_2026_05_29
description: Hotel business-domain maximize pass — built business-domain-atlas.mjs (deep node enum + 11-leg synergy + unwired punch-list), shared business-engine-re.mjs, /business-atlas skill, hotel-pii-redaction-guard hook; per-file scrutiny caught 7 honesty bugs (fixed)
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.609Z
aliases: reference_hotel_domain_maximize_2026_05_29
---


Hotel (galaxy:business) domain-maximize pass, session d7f7d3ce, 2026-05-29 (operator goal: "use /system-viz + master index + master graph to find ALL nodes that maximize the domain's potential; generate skills/scripts/hooks; synergize, wired, tested, validated").

**Built (committed to slot/hotel worktree):**
- `scripts/business-domain-atlas.mjs` — deep domain node atlas. Modes: default (engines + dispatcher + unwired punch-list + wiki + tribal + memories + ghost milestones), `--synergy` (11-leg PSN presence heuristic), `--unwired` (the maximize-potential punch-list), `--json`. Reads live ROOT=H:/prism, cross-tree aware for tribal + brain.
- `scripts/lib/business-engine-re.mjs` — shared business-engine prefix matcher (extracted from the duplicated regex in hotel-domain-awareness.mjs; dropped the bare `Manager` infra-collision token). Both scripts now import it (no more keep-in-sync drift).
- `.claude/commands/business-atlas.md` — `/business-atlas` skill (gitignored per PRISM convention; active in skill list).
- `.claude/hooks/hotel-pii-redaction-guard.mjs` — PreToolUse advisory enforcing soul refuse #2 (PII redaction) — the one soul-refuse with no prior hook. Fires only on high-confidence PII (formatted SSN, 16-digit card, or labeled-PII-field within 25 chars of 6+ digits). Never blocks, never echoes raw PII. **WIRING DEFERRED** (settings.json wiring pending a low-contention window; host was OOMing + a malformed settings.json edit disables fleet hooks — same honest pending status as sibling hotel-financial-invariant-guard.mjs). Inert until wired.

**Live findings (atlas --synergy = 7/11 PASS, honest after audit correction):** PASS legs 1-4,7,10,11; PARTIAL legs 5 (tribal orphan, unwired — see [[reference_hotel_psn_audit_2026_05_29]]), 6 (system-viz = wiki-derived proxy, capped PARTIAL — was briefly PASS but that double-counted LEG-3's same wiki dir; corrected post-audit), 8+9 (algorithm/formula filename-token proxies — never PASS, a filename match is not content verification). Business surface: ~258 business engines (prefix estimate), prism_business 879 actions + 574 lazy-imports + 12 sibling business dispatchers, 475 wiki business action nodes, BIZ-MS0/1/2 ghost milestones. **Unwired punch-list (maximize-potential set):** ERPImportEngine (true business), SemanticAssetIndexEngine (regex false-positive via "Asset"). Small — the business domain is already ~well-wired.

**Per-file scrutiny caught 7 real bugs I'd have shipped (2 rounds, 6 reviewer agents):** atlas — LEG-6 hardcoded `"PASS"`+literal 475 (could never GAP, R12), BIZ-MS1 omitted, LEG-8/9 filename-proxy reported as PASS (overstated), ENGINE_RE duplicated verbatim; hook — header FALSELY claimed "wired into settings.json" (same R12 false-claim class as last session's HotelERPTribal lie — caught + corrected to PENDING), "Enforces" overstated an advisory hook ("Reminds"), labeledNearDigits FP on any 4-digit constant near a PII label word (tightened to same-line 6+ digit proximity). All fixed, re-dispatched, both arms PASS. **Lesson reinforced: [[feedback_always_capture_lessons]] — the adversarial gate caught honesty drift my self-check missed, twice in two sessions on the SAME false-"wired" pattern.**

**Workflow audit (wk6nlrscl, 3 agents, 2026-05-29) ran + processed:** verdict 7/11 honestly synergized; caught the LEG-6 double-count (fixed → PARTIAL); finding "3 phantom wiki bridges" was a CROSS-TREE FALSE-POSITIVE (the agent searched main; the 3 bridges + the brain all live in the worktree, merge to main together via golf — verified by Glob, did NOT blindly delete valid citations). Domain confirmed genuinely well-covered (every PSN leg = real on-disk artifact, no stubs).

**Context-generation pass (same session, follow-on goal):** added `business/GSD.md` (domain GSD protocol + 8 invariant RULES), `[[feedback_hotel_e2e_no_paper_bridges]]` memory, `knowledge/wiki/architecture/business-erp-ap-ar-cycle.md` (procure→pay→report doctrine linking the 3 per-unit tribal leaves). Wired into galaxy MEMORY.md.

**Top CORRECTNESS backlog (audit-confirmed, do in MAIN to avoid cross-tree divergence):** create the 6 business-constants modules under `mcp-server/src/data/` (chart-of-accounts, payroll-tax-tables, pto-policies, benefits-plans, customer-terms, vendor-profile) + refactor engines that inline those to import them — the galaxy's own §2 HARD RULE / anti-pattern #1, the only confirmed dollars-to-the-cent jurisdiction-drift risk.

Deferred (honest, env/cross-tree-blocked): settings.json wiring of both hotel advisory hooks (PII + financial-invariant); the 6-constants extraction (main-tree engine refactor). Links: [[reference_hotel_psn_audit_2026_05_29]] · [[reference_hotel_business_galaxy_2026_05_28]] · [[feedback_hotel_pii_redaction_on_export]] · [[feedback_hotel_e2e_no_paper_bridges]].
