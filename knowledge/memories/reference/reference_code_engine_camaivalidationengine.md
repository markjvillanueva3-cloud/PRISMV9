---
name: reference_code_engine_camaivalidationengine
description: "CAM-EXHAUST-MS0/U-CAM127"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMAIValidationEngine.ts
  sourceHash: cb84cdfa
  loc: 848
aliases: CAMAIValidationEngine
---

# CAMAIValidationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMAIValidationEngine.ts` (848 LOC)

## What it is
CAM-EXHAUST-MS0/U-CAM127

```
CAMAIValidationEngine — CAM-EXHAUST-MS0/U-CAM127

"Production Readiness" validation harness for the CAM AGI arc
(engines U-CAM117..U-CAM124). Drives a curated suite of
behavioral scenarios through the live engine pipeline and
computes a match rate against documented contract behavior.

The "expert match" framing in the milestone spec corresponds, in
the absence of a labeled JM-Die corpus, to behavioral-contract
validation: does the running 5-engine pipeline produce the
outputs documented in docs/cam-ai/{architecture,model-serving,
lora-training,monitoring}.md and asserted in the U-CAM126
docs-validation test? Each scenario codifies one canonical
contract claim, runs it end-to-end, and reports match/no-match.

Production-readiness threshold: ≥0.95 match rate (≥14 of 15
default scenarios). Below that the engines are NOT ready for
shop-floor wiring (U-CAM128).

Engines exercised:
  - CAMRea
```


## Exports
`ScenarioVerdict` (type), `ValidationScenarioResult` (interface), `ScenarioCategory` (type), `ValidationReport` (interface), `ScenarioFn` (type), `RunValidationOptions` (interface), `CAMAIValidationEngine` (class), `camAIValidationEngine` (const)

## Key methods
`resetAllState`, `bringActiveBaseline`, `registerCanary`, `getDefaultScenarios`, `getThreshold`, `computeMatchRate`, `decideVerdict`, `runValidation`, `mkdirSync`, `writeFileSync`

## Depends on
[[reference_code_engine_camreasoningchainengine]] | [[reference_code_engine_camconfidencecalibrationengine]] | [[reference_code_engine_camfeedbackloopengine]] | [[reference_code_engine_camtransferlearningengine]] | [[reference_code_engine_cammodelservingengine]] | [[reference_code_engine_camdeeplearningorchestratorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CAMAIValidationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMAIValidationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMAIValidationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
