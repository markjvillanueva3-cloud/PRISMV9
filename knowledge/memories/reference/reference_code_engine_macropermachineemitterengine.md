---
name: reference_code_engine_macropermachineemitterengine
description: "MacroPerMachineEmitterEngine — MACRO-PROGRAM-PIPELINE-MS0/MS0-U5 (SAFETY-CRITICAL)."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MacroPerMachineEmitterEngine.ts
  sourceHash: b6120fbe
  loc: 829
aliases: macroPerMachineEmitterEngine
---

# macroPerMachineEmitterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MacroPerMachineEmitterEngine.ts` (829 LOC)

## What it is
```
MacroPerMachineEmitterEngine — MACRO-PROGRAM-PIPELINE-MS0/MS0-U5 (SAFETY-CRITICAL).

Given a U4-gated `SignoffDossier` + a part reference, this engine:
  1. Enumerates the lathe-capable JM Die fleet — IDs are imported from
     `MacroCandidateGateEngine.JM_DIE_MACHINE_LIMITS` (single source of
     truth) to prevent catalog drift between U4 (gate) and U5 (emitter).
  2. For each fitting machine: clones the candidate with that machine as the
     target, runs the U4 gate AGAINST that machine's envelope, and on PASS
     emits a labelled `<part>__<MACHINE-ID>.MIN` file with a header that
     carries the macro source, family, S(x) for THAT machine, controller,
     and the "OPERATOR REVIEW REQUIRED" prove-out flag.
  3. Updates `part.json` with a `perMachinePrograms[]` entry per emitted file.

SAFETY-CRITICAL invariants (asserted by tests):
  1. HARD RULE: no machine ever gets a file unles
```


## Exports
`ProgramForm` (type), `ControllerDialect` (type), `ControllerMemoryLimits` (interface), `FleetMachine` (interface), `PartRef` (interface), `PerMachineResult` (interface), `PerMachineEmitInput` (interface), `PerMachineEmitOutput` (interface), `macroPerMachineEmitterEngine` (const), `FLEET_TABLE_FOR_TESTS` (const), `CONTROLLER_LIMITS_FOR_TESTS` (const), `SX_THRESHOLD_FOR_TESTS` (const)

## Key methods
`emitPerMachine`, `safeWriteSync`

## Depends on
[[reference_code_engine_macrocandidategateengine]] | [[reference_code_engine_macrofillorchestratorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/MacroPerMachineEmitterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find macroPerMachineEmitterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MacroPerMachineEmitterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
