---
name: reference_code_engine_inventorhsminhostrunnerengine
description: "Inventor HSM In Host Runner"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/InventorHSMInHostRunnerEngine.ts
  sourceHash: 72b951f7
  loc: 473
aliases: InventorHSMInHostRunnerEngine
---

# InventorHSMInHostRunnerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/InventorHSMInHostRunnerEngine.ts` (473 LOC)

## What it is
Inventor HSM In Host Runner

```
InventorHSMInHostRunnerEngine — U-CAMTEST03
============================================

PHASE-8: PRISM-side companion for the Inventor HSM in-host iLogic VB
test runner (resources/inventor-hsm/PrismTestRunner.ivb). The iLogic
rule loads inside Autodesk Inventor (HSM Express / Pro), opens a
WebSocket to the PRISM CAMPluginCommunicationHub, and streams scenario
results back.

Architecture mirrors HyperMillInHostRunnerEngine (U-CAMTEST01) and
Fusion360InHostRunnerEngine (U-CAMTEST02) — same scenario contract,
same 7 assertions — but pinned to target="inventor_hsm" with the
iLogic-runner endpoint. The four engines stay structurally aligned
so the U-CAMTEST15 results bridge can iterate them uniformly.

The 7-assertion contract enforced here:
  1. frame_arrival           — observed count == expected_frame_count, no gaps/dupes
  2. latency_p99             — p99 latency ≤ descriptor.latency_p9
```


## Exports
`ScenarioCategorySchema` (const), `ScenarioCategory` (type), `ScenarioDescriptorSchema` (const), `ScenarioDescriptor` (type), `PlannedFrameSchema` (const), `PlannedFrame` (type), `ScenarioPlanSchema` (const), `ScenarioPlan` (type), `ObservedFrameSchema` (const), `ObservedFrame` (type), `ScenarioResultSchema` (const), `ScenarioResult` (type), `AssertionNameSchema` (const), `AssertionName` (type), `AssertionResultSchema` (const), `AssertionResult` (type), `ScenarioSummarySchema` (const), `ScenarioSummary` (type), `RunnerStatsSchema` (const), `RunnerStats` (type)

## Key methods
`register`, `planScenario`, `frameToEnvelope`, `summarize`, `getPlan`, `getStats`, `resetSession`, `resetAll`

## Depends on
[[reference_code_engine_camplugincommunicationhubengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/InventorHSMInHostRunnerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find InventorHSMInHostRunnerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/InventorHSMInHostRunnerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
