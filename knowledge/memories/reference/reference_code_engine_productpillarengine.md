---
name: reference_code_engine_productpillarengine
description: "Bundle engines into product pillars for user-facing capability packages:"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ProductPillarEngine.ts
  sourceHash: d5bf9d44
  loc: 541
aliases: ProductPillarEngine
---

# ProductPillarEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ProductPillarEngine.ts` (541 LOC)

## What it is
Bundle engines into product pillars for user-facing capability packages:

```
ProductPillarEngine — MXU-MS6

Bundle engines into product pillars for user-facing capability packages:
  1. Pillar definitions — Calculator, Toolpath, Quote, Quality, PostProcessor
  2. Completeness scoring — how ready is each pillar for users
  3. Entry point mapping — which action/skill starts each pillar
  4. Dependency resolution — what each pillar needs from other pillars
  5. Feature gate — enable/disable pillars per subscription tier

Sources:
  - MXU-MS6: Product-Pillar Capability Packages
```


## Exports
`PillarId` (type), `SubscriptionTier` (type), `ProductPillar` (interface), `PillarCompleteness` (interface), `PillarSummary` (interface), `DispatcherSourceReader` (type), `SkillLister` (type), `FeatureGate` (interface), `defaultDispatcherSourceReader` (const), `defaultSkillLister` (const), `WiredEngineResolution` (interface), `resolveWiredEngines` (function), `resolveLivePillarInputs` (function), `ProductPillarEngine` (class), `productPillarEngine` (const)

## Key methods
`listPillars`, `getPillar`, `scorePillar`, `getSummary`, `getSummaryLive`, `checkGate`, `getAccessiblePillars`, `getPillarCount`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ProductPillarEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ProductPillarEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ProductPillarEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
