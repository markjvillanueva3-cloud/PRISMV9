---
name: reference_code_script_probecachedaemon
description: "probe-cache-daemon — REAPER-PERMFIX-MS1 / U-C3"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/probe-cache-daemon.mjs
  sourceHash: 0012b4aa
  loc: 311
aliases: probe-cache-daemon.mjs
---

# probe-cache-daemon.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/probe-cache-daemon.mjs` (311 LOC)

## What it is
```
probe-cache-daemon — REAPER-PERMFIX-MS1 / U-C3

A single 5-second-TTL poll daemon for the three probes every fleet-reaper
sweep currently forks for itself: nvidia-smi (GPU), Ollama (/api/tags +
/api/ps), and the Docker+supporting-services health probe.

THE PROBLEM (REAPER-PERMFIX-PLAN diagnosis #2): the --monitor-loop sweep
forks ~5 subprocesses per cycle. Across 12 chats × 12 sweeps/hr that is
~720 nvidia-smi forks/hr alone — and every fork() at ≥95% commit memory
can ENOMEM-storm into "xmalloc: cannot allocate 8192 bytes". The reaper,
the very thing meant to RELIEVE fork pressure, becomes a fork-pressure
SOURCE.

THE FIX: one daemon polls all three probes every 5s and writes an atomic
JSON snapshot. All 12 chats READ that JSON (zero forks) instead of each
probing. 720 forks/hr → 12 forks/hr (just this daemon's own probes).

Readers use `readProbeCache()` (exported) which returns null 
```


## Exports
`probeGpu` (function), `probeOllamaDocker` (function), `readProbeCache` (function)

## Key methods
`mkdirSync`, `writeFileSync`, `renameSync`, `Number`, `writeCache`, `refreshLock`, `releaseLock`, `fileURLToPath`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/probe-cache-daemon.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find probe-cache-daemon.mjs` or `prism_session:master_index_query`; open the source at `scripts/probe-cache-daemon.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
