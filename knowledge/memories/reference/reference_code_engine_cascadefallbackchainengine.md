---
name: reference_code_engine_cascadefallbackchainengine
description: "multi-level fallback (cheap → mid → strong)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CascadeFallbackChainEngine.ts
  sourceHash: 7063562e
  loc: 371
aliases: CascadeFallbackChainEngine
---

# CascadeFallbackChainEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CascadeFallbackChainEngine.ts` (371 LOC)

## What it is
multi-level fallback (cheap → mid → strong)

```
CascadeFallbackChainEngine — multi-level fallback (cheap → mid → strong)
with per-tentacle circuit-breaker.

COST-CASCADE-MS0 / U-CASCADE-FALLBACK-CHAIN.

Closes the gap left by U-DISPATCHER-ACTION-TWO-PASS (which is binary: cheap +
strong only). Real cascades need 3+ tiers and an outage-aware skip so that a
dead mid-tier tentacle doesn't waste retry budget on every call.

Design: pure-core + injected-deps + an internal CircuitBreaker per tentacle.

Calibration: the cascade order is a list of tentacle ids passed in config.
U-CASCADE-CALIBRATE is externally blocked (K2-CLOUD-MS0::K2-K0); this
engine accepts a calibration-stub mode (`stubCalibration: true`) that uses
a default cascade order ["cheap", "mid", "strong"]. Real calibration will
later replace stub mode with per-task-class data-driven orderings.

R12 invariants:
  1. chain throws -> error envelope (not a real exception leak)
  2.
```


## Exports
`BreakerState` (type), `CircuitBreakerConfig` (interface), `BreakerSnapshot` (interface), `TentacleSpec` (interface), `CascadeRunInput` (interface), `CascadeRunResult` (interface), `CascadeConfig` (interface), `TentacleRunner` (type), `CascadeDeps` (interface), `STUB_CALIBRATION` (const), `evaluateBreaker` (function), `recordSuccess` (function), `recordFailure` (function), `CascadeFallbackChainEngine` (class), `cascadeFallbackChainEngine` (const)

## Key methods
`run`, `logWarn`, `recordSuccess`, `recordFailure`, `status`, `reset`, `isCascadable`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CascadeFallbackChainEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CascadeFallbackChainEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CascadeFallbackChainEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
