---
name: reference_code_data_sfccombinatorialvalidity
description: "SFC combinatorial VALIDITY matrix -- which (operation x strategy x cut_type x"
metadata:
  type: reference
  kind: code-data
  galaxy: unassigned
  sourcePath: mcp-server/src/data/sfc-combinatorial-validity.ts
  sourceHash: 3bac4b1a
  loc: 204
aliases: sfc-combinatorial-validity.ts
---

# sfc-combinatorial-validity.ts -- Data Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/data/sfc-combinatorial-validity.ts` (204 LOC)

## What it is
```
SFC combinatorial VALIDITY matrix -- which (operation x strategy x cut_type x
tool_material) cells are physically/practically real, so the sampler never
enumerates a nonsensical regime (e.g. a "trochoidal turning" cell or a
"CBN tap") silently.

OSCAR-SFC-9AXIS-MS0 / U-CSFH-02-VALIDITY-MATRIX (slot:oscar, 2026-06-10).

Layered on U-CSFH-01's axes (`./sfc-combinatorial-axes.ts`). PURE DATA +
deterministic predicates -- imports only TYPES + the axis arrays, never
`vitest` -- so the `CombinatorialCellSampler` (U-CSFH-04, a runtime engine) can
import it. (The stale plan table filed this under `__tests__/sfc/`; it lives in
`src/data/` instead -- same correction U-CSFH-01 made -- because an engine must
not depend on the test tree.)

SCOPE (per the plan): exactly the 4 axes operation x strategy x cut_type x
tool_material. Material-vs-ISO compatibility (e.g. PCD only on non-ferrous, CBN
on harde
```


## Exports
`CombinatorialCellKey` (interface), `ValidityResult` (interface), `OP_STRATEGIES` (const), `OP_CUT_TYPES` (const), `OP_TOOL_MATERIALS` (const), `validateCell` (function), `isValidCell` (function), `enumerateAllCells` (function), `enumerateValidCells` (function), `partitionCells` (function), `VALIDITY_STATS` (const)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/data/sfc-combinatorial-validity.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Data Module

## How to query it
Recall by meaning via `/system-viz find sfc-combinatorial-validity.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/data/sfc-combinatorial-validity.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
