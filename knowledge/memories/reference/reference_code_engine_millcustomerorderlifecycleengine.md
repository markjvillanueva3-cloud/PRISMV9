---
name: reference_code_engine_millcustomerorderlifecycleengine
description: "MillCustomerOrderLifecycleEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MillCustomerOrderLifecycleEngine.ts
  sourceHash: e76ea531
  loc: 397
aliases: millCustomerOrderLifecycleEngine
---

# millCustomerOrderLifecycleEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MillCustomerOrderLifecycleEngine.ts` (397 LOC)

## What it is
```
MillCustomerOrderLifecycleEngine
==================================

State machine for mill customer order lifecycle.

Mill parity for LatheCustomerOrderLifecycleEngine (LATHE-MASTER U-LTH51)
with three MILL-CANONICAL states inserted between `scheduled` and
`in_production` to model the mill-specific pre-production workflow:

  1. fixture_prep         — soft-jaw machining, dowel-locator install,
                            sub-plate setup (no lathe analog — lathe
                            chuck jaws are pre-bored elsewhere)
  2. first_piece_approval — mandatory FAI per AS9102 / iter53 FPA engine
                            before bulk production (mill GD&T verify)
  3. first_piece_rejected — FPA failed → return to fixture_prep OR
                            remain in production-tuning (feedback loop)

  draft → quoted → accepted → scheduled → fixture_prep →
          first_piece_approva
```


## Exports
`MILL_ORDER_STATES` (const), `MillOrderState` (type), `MILL_CANONICAL_STATES` (const), `CreateMillOrderInputSchema` (const), `CreateMillOrderInput` (type), `MillTransitionInputSchema` (const), `MillTransitionInput` (type), `MillAuditEvent` (interface), `MillOrderRecord` (interface), `MillOrderLifecycleState` (interface), `MillOrderSummaryRow` (interface), `millCustomerOrderLifecycleEngine` (const), `MillCustomerOrderLifecycleEngine` (re-export)

## Key methods
`createOrder`, `transition`, `getOrder`, `listOrders`, `getAuditLog`, `pipelineSummary`, `millCanonicalQueueDepth`, `asSummaryRows`, `allowedTransitions`, `getStats`, `atomicWriteJson`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MillCustomerOrderLifecycleEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find millCustomerOrderLifecycleEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MillCustomerOrderLifecycleEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
