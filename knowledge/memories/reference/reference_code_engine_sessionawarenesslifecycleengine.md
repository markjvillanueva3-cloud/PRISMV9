---
name: reference_code_engine_sessionawarenesslifecycleengine
description: "8-phase awareness loop orchestrator"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SessionAwarenessLifecycleEngine.ts
  sourceHash: 6bb76a9c
  loc: 185
aliases: SessionAwarenessLifecycleEngine
---

# SessionAwarenessLifecycleEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SessionAwarenessLifecycleEngine.ts` (185 LOC)

## What it is
8-phase awareness loop orchestrator

```
SessionAwarenessLifecycleEngine — 8-phase awareness loop orchestrator

Phase 0.13 U-SAW2 from UNIVERSAL-SKILLS-SCRIPTS-HOOKS-PLAN. A deterministic
state machine over the session awareness lifecycle:

  BOOT → VERIFY → BRIEF → EXECUTE ⇄ METACOG_CHECK → REFLECT → HANDOFF → NEXT_BOOT

This engine does not perform I/O or call other engines; it is the skeleton
that hook handlers and dispatchers use to agree on *which* phase a session
is in and *which* transitions are legal. That split keeps transition logic
unit-testable without mocking a filesystem.

Separate from SessionLifecycleEngine (metrics + checkpointing) by design:
this one tracks cognitive phase, that one tracks operational counters.

@module engines/SessionAwarenessLifecycleEngine
@milestone PP-0.13-U-SAW2
```


## Exports
`LifecyclePhase` (type), `LIFECYCLE_PHASES` (const), `PhaseEntry` (interface), `LifecycleSnapshot` (interface), `TransitionResult` (interface), `SessionAwarenessLifecycleEngine` (class), `createSessionAwarenessLifecycle` (function)

## Key methods
`legalTransitions`, `canTransition`, `getCurrent`, `getSessionId`, `getHistory`, `getExecuteToMetacogCount`, `isTerminal`, `transition`, `snapshot`, `pathTo`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/SessionAwarenessLifecycleEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SessionAwarenessLifecycleEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SessionAwarenessLifecycleEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
