---
name: reference_code_engine_lshdedupengine
description: "LSHDedupEngine — Locality-Sensitive Hashing for O(1) Semantic Dedup"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LSHDedupEngine.ts
  sourceHash: 423c017f
  loc: 331
aliases: lshDedupEngine
---

# lshDedupEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LSHDedupEngine.ts` (331 LOC)

## What it is
```
LSHDedupEngine — Locality-Sensitive Hashing for O(1) Semantic Dedup

Replaces O(n) linear scan with O(1) amortized lookup using random hyperplane LSH.
For n=2000+ engines, this reduces dedup check from ~50ms to ~0.5ms.

Theory: LSH maps similar vectors to same bucket with high probability.
Using L=20 hash tables with k=8 hash functions each:
- P(collision | sim > 0.85) ≈ 0.99
- P(collision | sim < 0.5) ≈ 0.01

Complexity: O(L·k) hash + O(|candidates|) verify ≈ O(1) amortized

@module Phase0.25 Scientific Foundations
@see UNIVERSAL-SKILLS-SCRIPTS-HOOKS-PLAN-ADDENDUM-2026-04-18.md §VIII
```


## Exports
`LSHConfig` (interface), `LSHCandidate` (interface), `LSHStats` (interface), `lshDedupEngine` (const), `LSHDedupEngine` (type)

## Key methods
`add`, `remove`, `query`, `isDuplicate`, `bulkAdd`, `getStats`, `reset`, `getEmbedding`, `has`, `getIds`, `collisionProbability`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/LSHDedupEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find lshDedupEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LSHDedupEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
