---
name: reference_code_script_extractmonolithdatabases
description: "extract-monolith-databases.mjs — deterministic extractor for EVERY data-store literal"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/extract-monolith-databases.mjs
  sourceHash: 4f4d9e88
  loc: 588
aliases: extract-monolith-databases.mjs
---

# extract-monolith-databases.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/extract-monolith-databases.mjs` (588 LOC)

## What it is
```
extract-monolith-databases.mjs — deterministic extractor for EVERY data-store literal
embedded in the non-modular PRISM monolith HTMLs + the already-split .js modules.

WHY: the operator asked to "extract all other databases other than tooling … assume I want
the whole neighborhood: inserts, fixtures, materials, machines, coolants/lubricants and all
other databases we had in the monolith" ([[feedback_think_ahead_extract_adjacent_databases]]).
Tooling already lives in jm-die-database/ — this is the SEPARATE, cross-referenced reference DB
for everything else. Nothing in src/data is overwritten; output is a new tree.

STRATEGY (comment-blind find, balanced extract, sandboxed eval):
  1. Walk the source set (2 monolith HTMLs + extracted_modules/**.js + extracted/**.js +
     data/materials_complete/**.js).
  2. Regex-find every `IDENT = {` / `IDENT = [` assignment over RAW text (comment-blin
```


## Exports
`extractLiteral` (re-export), `safeEval` (re-export), `isDataStore` (re-export), `categorize` (re-export), `looksStoreIsh` (re-export), `ASSIGN_RE` (re-export), `repairSeparators` (re-export), `findRepairEdits` (re-export)

## Key methods
`walkJs`, `atomicWriteJson`, `atomicWriteText`, `main`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/extract-monolith-databases.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find extract-monolith-databases.mjs` or `prism_session:master_index_query`; open the source at `scripts/extract-monolith-databases.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
