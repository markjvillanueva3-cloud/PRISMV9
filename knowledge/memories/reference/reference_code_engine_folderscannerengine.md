---
name: reference_code_engine_folderscannerengine
description: "FolderScannerEngine — Recursive directory scanner with change detection"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/FolderScannerEngine.ts
  sourceHash: 34362761
  loc: 441
aliases: folderScannerEngine
---

# folderScannerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/FolderScannerEngine.ts` (441 LOC)

## What it is
```
FolderScannerEngine — Recursive directory scanner with change detection

Scans JM Die source_roots paths, detects new/changed files via mtime
comparison, classifies by file type, and queues for ingestion.
Uses polling (not fs.watch — unreliable on network/mapped drives).

Stores scan state in data/state/folder-scan-state.json so subsequent
scans only process files that changed since last scan.

INGEST-MS0 / U-ING01
@module FolderScannerEngine
```


## Exports
`FileType` (type), `SeedDomainId` (type), `ScannedFile` (interface), `ScanState` (interface), `ScanResult` (interface), `ScanOptions` (interface), `folderScannerEngine` (const)

## Key methods
`classifyFile`, `inferSeedDomain`, `scan`, `walk`, `saveScanState`, `scanAllRoots`, `getState`, `getSummary`, `resetState`, `resetAllState`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/FolderScannerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find folderScannerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/FolderScannerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
