---
name: reference_code_engine_wedmbenchmarktoleranceengine
description: "WEDM-CAL-MS4 / U-CAL20"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WEDMBenchmarkToleranceEngine.ts
  sourceHash: 58b040d7
  loc: 213
aliases: WEDMBenchmarkToleranceEngine
---

# WEDMBenchmarkToleranceEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WEDMBenchmarkToleranceEngine.ts` (213 LOC)

## What it is
WEDM-CAL-MS4 / U-CAL20

```
WEDMBenchmarkToleranceEngine — WEDM-CAL-MS4 / U-CAL20

Single source of truth for WEDM benchmark tolerance bands. Loads the
tightened v2 bands from data/reference/WEDM_BENCHMARK_TOLERANCES.json
and exposes an API for:

  - material-aware band lookup (D2 uses a tighter speed band than M/K)
  - deviation classification (match / warning / major / critical)
  - bulk tolerance map for downstream engines

Why a dedicated engine:
  - Previously the bands lived inline in WEDMCalibrationReportEngine
    (±30% offset, ±20% speed) and WEDMProgramComparisonEngine (±40%
    feed, ±50% contour). Two callers, two sources, zero coordination.
  - Tightening the bands for production-gate quality required a single
    edit point. This engine is that edit point.

Algorithm:
  classify(deviation_pct, key) → 'match' | 'warning' | 'major' | 'critical'
  where the band comes from the JSON config. Warning band i
```


## Exports
`ToleranceClass` (type), `BenchmarkKey` (type), `ToleranceBand` (interface), `BenchmarkTolerancesConfig` (interface), `WEDMBenchmarkToleranceEngine` (class), `wedmBenchmarkToleranceEngine` (const)

## Key methods
`getConfig`, `getBand`, `roughSpeedBandFor`, `classify`, `classifyRoughSpeed`, `asComparisonOverride`, `reset`, `isTightened`, `overallTighteningScore`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/WEDMBenchmarkToleranceEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WEDMBenchmarkToleranceEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WEDMBenchmarkToleranceEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
