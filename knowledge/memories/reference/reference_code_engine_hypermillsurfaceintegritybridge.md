---
name: reference_code_engine_hypermillsurfaceintegritybridge
description: "Surface integrity wiring for hyperMILL finishing"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HyperMillSurfaceIntegrityBridge.ts
  sourceHash: 1da032d4
  loc: 613
aliases: HyperMillSurfaceIntegrityBridge
---

# HyperMillSurfaceIntegrityBridge -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HyperMillSurfaceIntegrityBridge.ts` (613 LOC)

> [physics] Imports canonical physics constants (`src/physics/constants.ts`) -- safety-relevant node.

## What it is
Surface integrity wiring for hyperMILL finishing

```
HyperMillSurfaceIntegrityBridge — Surface integrity wiring for hyperMILL finishing

Connects 3 PRISM surface integrity engines to hyperMILL finishing operations:
  1. SurfaceIntegrityEngine     — Ra/Rz prediction, white layer thickness,
                                  residual stress, fatigue derating
  2. SurfaceFinishPredictorEngine — Used for parameter reference; Ra/Rz from
                                    SurfaceIntegrityEngine's Brammertz model
  3. ResidualStressPredictionEngine — Tensile/compressive stress profile

White layer hard gate (U-HMR30):
  Uses a cutting temperature model based on
    θ = θ_amb + C_θ · (kc1.1 · fz^(1-mc)) ^ 0.6 · Vc ^ 0.4
  where C_θ = 0.4 (empirical coefficient from Trigger & Chao (1951) adapted by
  Boothroyd (1963) and validated in Trent & Wright (2000) "Metal Cutting" §6.4).
  If predicted temp > WHITE_LAYER_THRESHOLDS[material].threshold_C → HA
```


## Exports
`SurfaceMaterial` (type), `SurfaceProcess` (type), `SurfaceIntegrityCheckInput` (interface), `WhiteLayerGateResult` (interface), `ResidualStressEstimate` (interface), `SurfaceIntegrityCheckResult` (interface), `SurfaceIntegrityQualityReport` (interface), `HyperMillSurfaceIntegrityBridge` (class), `hyperMillSurfaceIntegrityBridge` (const)

## Key methods
`calculate`, `runWhiteLayerGate`, `generateReport`

## Depends on
[[reference_code_engine_surfaceintegrityengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/HyperMillSurfaceIntegrityBridge.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HyperMillSurfaceIntegrityBridge` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HyperMillSurfaceIntegrityBridge.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
