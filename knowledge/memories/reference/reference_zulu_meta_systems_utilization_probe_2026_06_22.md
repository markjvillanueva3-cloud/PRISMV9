---
name: reference_zulu_meta_systems_utilization_probe_2026_06_22
description: U-ZLR-META-UTIL (2026-06-22, slot:zulu) — reconcile-zulu-ledger.mjs now answers "is each meta-system USED" (ollama/hermes/octopus/obsidian) + A-16 mtime->count phantom-OPEN fix + recency gating. Zulu own-queue confirmed dry; octopus/ollama/opt-in "gaps" were stale-ledger phantoms.
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.283Z
aliases: reference_zulu_meta_systems_utilization_probe_2026_06_22
---


# ZULU meta-systems utilization probe + reorientation — 2026-06-22 (slot:zulu, session 817e5568)

Operator `/checkin-zulu /goal /loop`: read 6/09–6/21 sessions; complete remaining backend dev (priority zulu); **improve utilization** of hermes/obsidian/ollama/octopus + synergize; use ollama offload, hermes, parallel agents, loops, harnesses, crons.

## Reorientation verdict (R12): zulu's BUILDABLE backlog is dry; the named "gaps" were STALE-LEDGER PHANTOMS
Reconciled live ($0 probes), NOT from the 11-day-stale `ZULU-MASTER-CONTEXT-LEDGER-2026-06-11.md`:
- **Octopus A-04** ("wire consensus_ask to 7 dispatchers") = NOT a gap. `aiReasoningDispatcher.ts` IS the full OCTOPUS-NEURAL-MS0 surface (`consensus_ask` 4-way `MultiModelConsensusEngine.ask()` L3391-3426, + moonshot_invoke/cascade_calibrate/neural_route_decision/moa_aggregate/rankTrajectories). Octopus clients are intentionally **WIRE-EXEMPT** (centralized, tribal note `fleet-hygiene-u-wire-exempt-octopus-clients`); wiring into 7 dispatchers would be a dedup anti-pattern. Built+wired+synergized (consensus-of edge + →WeeklySynthesis). Verifying before building saved a wrong build (R8/R12).
- **Ollama offload** = healthy (last-24h **adjusted 68.1%**, the real metric; raw 22.3% is the "do NOT chase" number). The standing "17.9% < 30%" claim is stale.
- **zulu-opt-in path bug** = ALREADY FIXED 2026-06-20 (`ZULU-OPTIN-PATH-FIX`, operator-approved). The 6/19 "governance blocker" is resolved.
- **`ollama-route-pretooluse` 7736 fires / 0 offloads** = by design — a PreToolUse hook can only SUGGEST, not actuate; actuation is via ask-ollama/ask-hermes (855 offloads). Not a gap.
- **4 UNWIRED engines fleet-wide** (3613 wired-direct, 99.9%): SFCInferenceGateWireEngine (oscar's TODAY work — peer lane), AuthEngineV7 (security-sensitive), Regression/PreMOU (April, UNKNOWN dispatcher). No clean R15 wiring target.

## Shipped: U-ZLR-META-UTIL (`6f0bf387e5`, on cad-fusion-live-ms0)
Extended `scripts/reconcile-zulu-ledger.mjs` (the orchestrator truth-harness) with a **META-SYSTEMS UTILIZATION** block — answers the operator's recurring "is X USED (not just built)" for the 4 named systems in ONE $0 run, aggregating EXISTING artifacts into the orchestrator sidecar (no dup of ollama-offload-dashboard — it aggregates + names the deep-dive tool in `action`):
- `gradeOllamaUtilization(stats, nowMs)` — offloaded + executedOffloads + measuredTokensSaved + **lastUpdated recency**.
- `gradeHermesUtilization(stats)` — ask-hermes fired + bySource.fail rate (≤10%).
- `gradeOctopusUtilization({queueCount, processedCount, lastDrainAgeH})` — processed>0 + **drain recency** (newest processed ts).
- `gradeObsidianUtilization(freshness)` — synthesis COUNT (not mtime).
- helpers `readJsonSafe`, `countJsonlLines`, `newestJsonlTs`; `reconcileMetaSystems()` (injectable → fixture-testable). 27/27 tests.

**2 fixes (per-file 2-arm scrutiny PASS, 2 P2s both auto-fixed R12/R16):**
1. **A-16 phantom-OPEN**: gated on synthesis COUNT not mtime `fresh<24h`. Canonical staleness is HASH-based (`galaxy-synthesis-refresh.mjs`: stale only if memory CLUSTER changed); old mtime just means "no new memories", not stale content. Old gate gave FALSE OPEN (live: 34 present, 0 need re-synth, but only 20 mtime-fresh). Same class as the A-06 wrong-path fix. Now SHIPPED.
2. **Recency gating** (both reviewers independently flagged): `offloaded` (since 2026-04-28) and `processed.jsonl` (append-only, never rotated) are LIFETIME monotonic — count>0 read UTILIZED forever even if the lane died weeks ago. Now gated on `lastUpdated` / newest-drain-ts within **48h** (`META_RECENCY_H`).

## Live truth this run (in the sidecar `ZULU-LEDGER-RECONCILE-LATEST.json`)
- Ledger: **6 SHIPPED / 0 OPEN / 1 UNKNOWN** (A-04 peer-owned/non-file-probeable). Zulu's ledger is fully drained.
- META: **4/4 UTILIZED** — ollama (331 offloads, ~48.7k tok saved, 0.0h fresh), hermes (858 calls, 0.3% fail), octopus (130 processed, 54 queued, **drain 45.4h** — within the 48h trickle window but WATCH: near the edge), obsidian (35 syntheses).
- **Obsidian synthesis: all 34 hash-fresh, 0 need re-synthesis** (`galaxy-synthesis-refresh.mjs` run this session).

## Octopus drain — ROOT-CAUSED + FIXED (U-DRAIN-SPAWN-ENOENT, this session)
**R12 CORRECTION of my own earlier hypothesis** (I'd guessed "fleet-reaper reap / models down" — WRONG, both refuted: `H:/prism-iooms0` doesn't exist so the live drainer is selected; the reaper's confirm-after-N-ticks ~600s window far exceeds the ~90s drain; drains succeed + record when run by hand at BOTH heaps).

**VERIFIED root cause:** `stop-consensus-drain.mjs` spawned the **extensionless shim** `H:/.claude/bin/portable-node`. On Windows `cp.spawn` (no shell) cannot `CreateProcess` a no-extension file → **ENOENT**, fired **ASYNC** as an 'error' event the hook's synchronous `try/catch` CANNOT catch → the hook returned "drainer spawned" while **nothing ran**. Every recorded drain (06-17/19/21) was a **manual shell-resolved run during an investigation** — the autofire **never actually worked**. This is why the stall RECURS (~2 days) and the 06-19 "re-wire" fix didn't hold (it only verified the hook RETURNS "spawned", never that a record appeared). Proven: `cp.spawn('.../portable-node',{})`→ENOENT; `process.execPath` (real node.exe) spawns fine.

**FIX (`U-DRAIN-SPAWN-ENOENT`, committed):** spawn the real node via `resolveNodeBin()` (process.execPath / H:/Tools/nodejs/node.exe), basename-anchored guard `/(^|[\\/])node(\.exe)?$/i` (rejects `portable-node`); tee the detached child's output to an append LOG (was `stdio:"ignore"` → silent failures = why undiagnosable for days, R12); generous heap (Blackwell). **Validated end-to-end:** post-fix the hook produced a FRESH processed record (drained_at 00:26→00:37) + the log captured output. **Fleet-wide bug class** also fixed: `docker-hook-broker.mjs` FALLBACK_BIN default (shim→execPath; its test already proved the shim broken) + `stop-bg-runner.mjs` dead shim const removed. 13 tests; per-file 2-arm FAIL→fix-P1→PASS+PASS.

**LESSON:** a `cp.spawn(extensionless-shim)` on Windows ENOENTs ASYNC → uncatchable by the sync try/catch → silent dead spawn; ALWAYS spawn `process.execPath`, never `.../bin/portable-node` (works in Git Bash via shebang, NOT in CreateProcess). A "fix" that verifies a hook RETURNS "spawned" but never that the side-effect (a processed record) occurred is a FALSE validation — verify the OUTCOME, not the launch.
- **Single-voter "degradation" — RESOLVED/transient (live re-verify post-compaction, same session):** the live `consensus-queue-processed.jsonl` shows the autofire consensus running **2-voice** (`consensus_participants = [gpt-oss:20b, qwen2.5-coder:32b]`, last 25 drains 25/25 `drain_ok`, 0 errors, queue actively draining 45→42). 2-voice IS the **intended** unattended-autofire mode: the 5→7-voice cluster ([[reference_zulu_octopus_7voice_cluster_2026_06_22]]) needs cloud API keys (Grok/Gemini/DeepSeek/GLM) + gpt-oss:120b installed, all absent by design in the autofire path. `MultiModelConsensusEngine.ts` panel is **correctly install-gated** (`resolveOllamaModels()` gates by exact `installed.includes`, self-heals on retired `:3b/:7b/:14b` tags — DEFAULT_OLLAMA_MODEL gpt-oss:120b falls back to qwen2.5-coder:32b). No misconfiguration to fix (R8/R12 verify-before-build save).
- **Octopus ghost-roosts MISC-185 + MISC-253 — both STALE/resolved (verified file:line, same session):** MISC-185 ("imports nonexistent PRISMContextInjectorEngine.js") is stale — `mcp-server/src/engines/PRISMContextInjectorEngine.ts` EXISTS (168 lines, real `composeContext()`, the `.js` specifier resolves to the `.ts` source per ESM convention). MISC-253 ("WIRE-EXEMPT stubs throw fail-fast") is stale — both `PRISMContextInjectorEngine.ts` AND `ConsensusModelPerformanceEngine.ts` (228 lines, real singleton) are full implementations, no throw-stub markers; the live autofire drains being 25/25 `drain_ok` is the hot-path proof. These ghost-roosts are handoff-derived and pre-date the file restorations; the next reconcile/audit should drop them.
- Standing pattern (3rd zulu session in a row): the master ledger rots faster than curation; ALWAYS `node scripts/reconcile-zulu-ledger.mjs` for $0 truth BEFORE trusting the ROI order. Linked: [[reference_zulu_backlog_reconcile_2026_06_19]], [[reference_zulu_revival_timeout_deferred_2026_06_20]], [[reference_zulu_ledger_reconciler_2026_06_11]].

## Detection harness — meta-systems-health-inject SessionStart hook (U-META-HEALTH-INJECT, this session)
The ROOT cause that let BOTH the hermes proxy AND the octopus drain hide for >48h: the meta-systems utilization probe (`reconcileMetaSystems`) was **MANUAL-ONLY** — no cron, no hook read it. Closed it: `.claude/hooks/meta-systems-health-inject.mjs` (SessionStart, T2, wired settings.json SessionStart[0] idx 40 after substrate-health-inject, mirrored C:<->H:). Dynamic-imports the pure `reconcileMetaSystems()` (import-safe via `isMain()`; fast local fs reads, NO network) and surfaces any DOWN/UNDER-UTILIZED substrate + evidence + action + a runnable self-heal command; **SILENT when all UTILIZED** (mirrors psn-leg-state). Fires fleet-wide every SessionStart + /compact resume → next dead substrate caught in HOURS not days. Distinct from substrate-health-inject (config drift), psn-leg-state (11-leg knowledge net), nn-graph-health (GNN tier). 6/6 tests, live-validated silent-when-healthy, per-file 2-arm PASS (both P2 nits fixed inline). Knob `PRISM_META_HEALTH_INJECT_DISABLE=1`. Wiki [[hermes-proxy-silent-degradation-missing-aiohttp-2026-06-23]].

## Hermes lane — ROOT-CAUSED + FIXED (U-HERMES-PROXY-FAILLOUD, this session post-compaction)
The probe's `hermes UNDER-UTILIZED` ("no ask-hermes activity in 48h") was NOT a quiet lane — the lane was **DEAD**. Chain: probe :8645 -> ECONNREFUSED; the `PRISM Hermes Proxy` scheduled task (runs `hermes-proxy-ensure.mjs`) was failing `LastTaskResult=3` for hours; the detached `hermes proxy start` spawn used `stdio:"ignore"` so the real error was swallowed. **Foreground run revealed it: `hermes proxy requires aiohttp`** — a MISSING PYTHON DEP in the hermes venv, NOT an OAuth/credential issue. **FIX:** `pip install aiohttp` into the hermes venv (restores the hermes-agent's own `[messaging]` extra; env-level, not repo-tracked) -> proxy binds in ~1s -> **validated end-to-end:** `ask-hermes ask` returned `source:"hermes", model:"grok-4.20-0309-non-reasoning"` (real xAI Grok, not the ollama fallback). **Committed recurrence-fix:** `hermes-proxy-ensure.mjs` now tees the proxy's startup output to an append log (`PRISM_HERMES_PROXY_LOG`, default `state/shared/hermes-proxy-start.log`) so the NEXT silent failure is diagnosable in seconds (R12 fail-loud, sibling of the octopus-drain log-tee). 16/16 tests, per-file 2-arm PASS. Wiki [[hermes-proxy-silent-degradation-missing-aiohttp-2026-06-23]]. **LESSON:** "UNDER-UTILIZED" can mean DEAD not idle — the graceful ask-hermes->ollama fallback is what HID the dead proxy (nothing errored, work just silently got weaker). Capture the real stderr before assuming credentials. **Noted follow-up (NOT chased, pre-existing):** the hermes venv also reports `mcp 1.26.0 requires anyio>=4.5, which is not installed` — separate from the proxy (which works); affects a different hermes mcp surface, not the ask-hermes lane.
