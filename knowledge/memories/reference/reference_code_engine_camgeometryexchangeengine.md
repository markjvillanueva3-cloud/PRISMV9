---
name: reference_code_engine_camgeometryexchangeengine
description: "Geometry Transfer Protocol (U-CAM97)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMGeometryExchangeEngine.ts
  sourceHash: a4d6d312
  loc: 738
aliases: CAMGeometryExchangeEngine
---

# CAMGeometryExchangeEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMGeometryExchangeEngine.ts` (738 LOC)

## What it is
Geometry Transfer Protocol (U-CAM97)

```
CAMGeometryExchangeEngine — Geometry Transfer Protocol (U-CAM97)
=================================================================

PHASE-7: Standardized geometry exchange between PRISM and the four CAM
plugin adapters (hyperMILL, Fusion 360, Inventor HSM, Mastercam X8).

Supported formats:
  - STEP AP242 — ISO 10303-242, CAD-native part geometry
  - B-Rep JSON — PRISM canonical parametric boundary representation
  - STL binary — 80-byte header + triangle count + per-triangle 50 bytes
  - STL ASCII  — `solid ... endsolid` envelope
  - OBJ        — vertices + faces, Wavefront ASCII
  - PLY        — Stanford polygon format

Streaming protocol:
  Large payloads (>100 MB) are split into fixed-size chunks of
  DEFAULT_CHUNK_SIZE (1 MB) or a caller-specified size up to
  MAX_CHUNK_SIZE (16 MB). Each chunk carries a monotonic `seq`, its own
  SHA-256, and an `is_final` flag on the last chunk. R
```


## Exports
`GeometryFormatSchema` (const), `GeometryFormat` (type), `GeometryUnitsSchema` (const), `GeometryUnits` (type), `GeometryMetadataSchema` (const), `GeometryMetadata` (type), `GeometryBlobInputSchema` (const), `GeometryBlobInput` (type), `BlobRegistrationSchema` (const), `BlobRegistration` (type), `GeometryChunkSchema` (const), `GeometryChunk` (type), `ReceiveResultSchema` (const), `ReceiveResult` (type), `AssembledBlobSchema` (const), `AssembledBlob` (type), `StreamStatusSchema` (const), `StreamStatus` (type), `StreamProgressSchema` (const), `StreamProgress` (type)

## Key methods
`supportedFormats`, `computeChecksum`, `registerBlob`, `getBlob`, `getChunk`, `allChunks`, `receiveChunk`, `assemble`, `streamProgress`, `validateFormat`, `estimateChunkCount`, `isLargeModel`, `resetBlob`, `resetSession`, `resetAll`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CAMGeometryExchangeEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMGeometryExchangeEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMGeometryExchangeEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
