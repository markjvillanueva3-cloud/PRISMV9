---
name: reference_hotel_domain_status_2026_06_10
description: "HOTEL (business/ERP/HR) domain state-of-the-union — what's shipped/pending/dormant across NETPLAT + QB-PARITY + iOS + de-stub, reconciled across the slot/hotel worktree and the cad-fusion-live-ms0 MAIN tree. Read FIRST on any hotel session to skip the ~40k-token rediscovery."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.609Z
aliases: reference_hotel_domain_status_2026_06_10
---


# HOTEL domain status — reconciled 2026-06-10 (slot:hotel, session claude-d545743f)

Hotel = business/ERP/HR/accounting/marketplace galaxy (`mcp-server/src/engines/business/`). Three concurrent programs + one fleet UI campaign. **Two trees** hold hotel work — know which:
- **`slot/hotel` worktree** (`H:/prism-slot-hotel`, HEAD 2026-05-30): the ENGINE-BUILD tree. NETPLAT P0/P1 engine source built here, then golf-merged to main. Now BEHIND main on integration; its job for those engines is done.
- **MAIN = `cad-fusion-live-ms0`** (`H:/prism`): where engines get WIRED into `businessDispatcher.ts`, frontend ships, and all recent hotel work lives. Commits are `[MAIN] [BOOTSTRAP-SLOT-ENFORCE] ... (slot:hotel)`. **Recent fleet practice = work in MAIN, slot-prefix the commit** — not the legacy per-worktree lane.

## Program 1 — QB-PARITY-MS0 (ERP fiscal spine) ✅ COMPLETE
QuickBooks-parity accounting/ERP. Shipped 2026-05-29..30 (U-QBP-01..22): A/R revenue cycle, A/P + banking, books/reports, Phase-5 items, payroll filings (941/940/W2/1099). Final: `5987c29ed9 — QB-PARITY COMPLETE`. Plan: `business/QUICKBOOKS-PARITY-PLAN.md`.

## Program 2 — NETPLAT (manufacturing networking platform — "beat Axhera") 🟢 P0+P1+P2 wired
Master plan `business/PRISM-NETWORKING-PLATFORM-PLAN.md` (**STALE — dated 2026-05-29, under-reports shipped state by ~2 phases; its "VendorEngine UNWIRED / no RFQ loop" claims are obsolete**). Thesis: backend-first depth (physics-derived price, physics-true match, observed capacity, ERP loop) a vibecoded clone can't fabricate. GTM is INVERTED: supply-tool first, liquidity-gate the marketplace (≥20 daily-active shops), concierge the first ~100 RFQs.

**TRUE state (verified in MAIN `businessDispatcher.ts` 2026-06-10):** ~22 NETPLAT actions WIRED — `supplier_capability_register`, `rfq_match_score`, `quote_explain_render`, `buyer_register`, `rfq_broadcast`, `bid_rank`, `marketplace_escrow_deposit`, `supplier_onboard_apply`, `vendor_catalog_ingest`, `vendor_to_supplier_hints`, `supplier_reputation(_rank)`, `geo_route_cost/landed_cost/logistics_score`, `capacity_project/earliest_slot`, `marketplace_final_rank`, `vendor_catalog_query`, `marketplace_rank_rfq`, `marketplace_seed_from_hints`, `marketplace_lead_list`. Engines built+merged+wired: RFQBroadcast, BidCollectionRanking, BuyerAccount, MarketplaceLedger, SupplierOnboarding, SupplierCapabilityProfile, RFQMatchScoring, QuoteExplainPDF, SupplierReputation, Vendor(Catalog). Frontend bindings shipped (`U-VNET-MKT` 2026-05-31: RFQ rank + reputation + landed-cost + lead reads).
- **Action-name drift vs plan:** wired names differ from the plan's proposed names (`bid_rank` not `rfq_bid_rank`; `marketplace_escrow_deposit` not `_create`; `supplier_onboard_apply` not `supplier_onboard`). Grep the ACTUAL enum, not the plan.
- **Supplier directory: NOT a separate engine — it IS `SupplierCapabilityProfileEngine`** (verified 2026-06-11). A standalone `SupplierDirectoryEngine` would DUPLICATE the registry (DuplicationGuard would reject it). The real gap was that only `registerSupplier` was wired; the registry's read + lifecycle surface was built+tested but unwired. **✅ WIRED 2026-06-11 (`6685fcb8da`, slot/hotel, U-HOTEL-SUPPLIER-DIR-WIRE):** 6 new businessDispatcher actions — `supplier_directory_list` (listSuppliers filter), `supplier_profile_get`, `supplier_can_satisfy` (RFQ first data-check), `supplier_profile_update`, `supplier_deactivate`, `supplier_reactivate`. 8 round-trip tests (3-of-3 scrutiny PASS, 1 P1 closed). The engine's WIRE-EXEMPT deferral was retired (the reconcile made the worktree dispatcher current).
- **Buyer directory: same pattern, the BUYER side — `BuyerAccountEngine`.** ✅ WIRED 2026-06-11 (`b847c17917`, slot/hotel, U-HOTEL-BUYER-DIR-WIRE): 7 new actions — `buyer_directory_list`, `buyer_profile_get`, `buyer_profile_update`, `buyer_credit_status_set` (KYB/fraud-hold lever), `buyer_deactivate`, `buyer_reactivate`, `buyer_can_post_rfq` (RFQ gate). **PII-masked** (every action returns the masked `BuyerAccount`; `getBuyerInternal` deliberately NOT wired — hotel soul PII). 10 round-trip tests incl. PII guards + the reactivate-never-relifts-suspension fraud invariant. 2-reviewer PASS. **The marketplace's supplier + buyer admin surfaces are now BOTH wired.** PATTERN for future R15-WIRE: a registry engine with only `register*` wired but `get/list/update/deactivate/reactivate` built-but-unwired is a clean high-ROI clone-the-pattern unit (supplier then buyer both hit this).
- **Still UNBUILT gaps:** `MultiProcessRoutingValidatorEngine` (Phase-1 cross-slot kilo/mike/foxtrot/whiskey — coordinate, don't solo). GNN matcher + live MTConnect capacity = explicitly CUT from MVP, gated to Phase-3 (no trained checkpoint exists; `auroc_measured: null`).

## Program 3 — FLEET-IOS-REDESIGN (UI theme) ✅ U1–U3e shipped (MAIN)
iOS-style theme: `:root` token layer + SF font + WorkspacePrimitives + ThemeCustomizer (accent/haptics/radius) reachable at **Settings→Appearance** (Codex SettingsPage, additively extended). U3c lesson: a single dark `--accent-fg` is the only WCAG-AA-universal fg (white fails on cyan/green/orange). Commits U3b `4aed666088` … U3e `8133bbe723`. **NEXT (per handoff):** decorative-cyan polish (WorkspaceHero eyebrow + SummaryTile gradient → accent); then doctrine U4–U7.

## Campaign — SITE DE-STUB (`state/shared/SITE-DESTUB-PLAN.md`) 🟢 7/8, hotel-scope effectively done
Operator: "clean up the whole site, take out stubs, populate with JM data, start testing." 7 real de-stubs DONE (dashboard machines, AI-learning, SPC, A3, RootCause — all → real JM fleet, INCH units). ViewerPage = correctly SKIPPED (neutral 3D sample, delta/sierra's). **Two blockers remain (NOT hotel-soloable):** #1 backend rebuild+restart (golf/infra — shared `:3100` runs a stale `dist/index.js` predating `/api/v1/business/dispatch`); #2 JM engine seeding per-domain.

## OPEN THREADS (next-by-ROI, in-lane + actionable first)
0. **✅ DONE 2026-06-10 (commit `c500f1b346` on slot/hotel): CRM auto-seed.** `getEngine("customerMgmt")` factory now auto-seeds from the JM corpus on first construct when empty (shared `loadJMCustomerCorpusRecords()` helper, idempotent + fail-soft + race-free). +4 R15 round-trip tests (49/49 green), 2-agent scrutiny PASS. Closes SITE-DESTUB BLOCKER #2. ALSO this session: reconciled the slot/hotel worktree CURRENT with main (merge `70566db`, was 4114 behind + CRLF-noisy; now buildable). Remaining for full live effect: jm-customers.jsonl is a GITIGNORED generated artifact — present in main's worktree (473 rows) + auto-seed's H:/PRISM fallback finds it on this host, but a clean deploy needs it generated (juliett/data-ops). Pre-existing unrelated defect found+flagged: `JMCustomerVendorDatabaseEngine` missing in BOTH trees (romeo `U-WIRE-JMDB` committed wiring+test but never the engine file) → its suite fails to load. Romeo/golf to fix.
1. ~~CRM JM-seed never auto-invoked~~ (SUPERSEDED by #0 above). _(orig:_ **CRM JM-seed exists+wired but is NEVER auto-invoked → ERP boots EMPTY** (the TRUE BLOCKER-#2, verified 2026-06-10). DONE already: `CustomerManagementEngine.seedFromJMCorpus()` (L223, idempotent/fail-soft) + `customer_seed_jm_corpus` action (businessDispatcher L798/L3549, loads `state/shared/databases/jm-customers.jsonl` = 473 real JM customers) + test. **GAP:** the engine has NO constructor/rehydration (`customers=new Map()` L164; `createCustomer` persists write-only via `persistenceBridge.persist` L180 but nothing reads back on boot), and `customer_seed_jm_corpus` is referenced ONLY by the dispatcher+test — never auto-called. So `customer_list` returns `[]` every boot. **FIX (recommended): dispatcher `getEngine("customerMgmt")` factory auto-runs the JM seed once on first construct if the map is empty** (extract the L3549 fs-load into a shared `loadJMCustomerCorpus()` helper; idempotent so re-entrant-safe). Alt: constructor rehydrate from persistenceBridge (fixes the persist-without-read bug too, but needs the bridge read API + still needs a first seed). MAIN-tree, ~2 files (dispatcher + test), [MAIN] [BOOTSTRAP-SLOT-ENFORCE] commit. **Highest in-lane ROI — unblocks "start testing soon".**
   - **CORRECTNESS PROVEN (race-free, 2026-06-10):** `persistenceBridge.loadAll()` runs ONLY in the Postgres branch of `index.ts:498` (awaited at startup BEFORE serving); the `else` is pure in-memory (no rehydrate). So the factory `listCustomers().length===0` guard is safe both ways — Postgres: rehydrate-then-skip-if-present; in-memory (the common dev mode that CAUSES the empty-CRM): always seed. Seeded rows persist via `createCustomer`→`persistenceBridge.persist` → next Postgres boot rehydrates → idempotent. `seedFromJMCorpus` dedups by normalized name. Design: extract `loadJMCustomerCorpusRecords(path?)` helper from the L3555 `customer_seed_jm_corpus` case (DRY), call it in the `case "customerMgmt"` factory (L375) inside `if(!_customerMgmt){…try{ if(empty) seed }catch{}}`, fail-soft. Test: dispatcher round-trip `customer_list` on fresh module asserts ≥400 JM customers incl. a known name (R15 through-dispatcher) + a `loadJMCustomerCorpusRecords` unit test (473 records).
   - **⚠ BLOCKED 2026-06-10 22:17 by CONTENTION:** `businessDispatcher.ts` was under ACTIVE concurrent edit by another chat (git `M` + line numbers shifted +10 mid-session). Did NOT edit per conflict-fork rule. Build when the file is free (recheck `git status --short` clean) or coordinate via chat-bus. Live end-to-end still also needs BLOCKER-#1 (golf backend rebuild+restart).
2. iOS decorative-cyan polish + doctrine U4–U7 (MAIN, from handoff).
3. ~~`SupplierDirectoryEngine` (Phase-0 gap)~~ ✅ DONE 2026-06-11 (`6685fcb8da`) — was the unwired read/lifecycle surface of `SupplierCapabilityProfileEngine`, now wired (6 actions). / `MultiProcessRoutingValidatorEngine` (cross-slot) — still open, coordinate, don't solo.
4. Refresh the stale NETPLAT plan verification appendix (§ground-truth 2026-05-29 → 2026-06-10).
5. Galaxy `MEMORY.md` is still SCAFFOLDED (`GALAXY-BRAIN-FILL` auto-block only) — fill with live program status.

## Doctrine for hotel work (soul refuses — never violate)
Financial-invariant gate (debits=credits, trial balance) before any GL write · PII redaction on every export (last4 SSN, masked card, role-only names) · never silent-clobber a posted GL entry. `hotel-financial-invariant-guard.mjs` enforces. Default Ω≥0.95 / S(x)≥0.98 (HR/payroll Ω≥0.98).

Related: [[reference_hotel_business_galaxy_2026_05_28]] · [[reference_hotel_business_engine_buckets_2026_05_28]] · [[feedback_checkin_loop_goal_utilization_audit_2026_05_16]] · stale plan `business/PRISM-NETWORKING-PLATFORM-PLAN.md`.
