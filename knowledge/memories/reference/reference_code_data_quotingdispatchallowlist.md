---
name: reference_code_data_quotingdispatchallowlist
description: "quoting-dispatch-allowlist.ts -- the cost-basis / sensitive-pricing actions the browser must"
metadata:
  type: reference
  kind: code-data
  galaxy: unassigned
  sourcePath: mcp-server/src/data/quoting-dispatch-allowlist.ts
  sourceHash: 65ff5176
  loc: 120
aliases: quoting-dispatch-allowlist.ts
---

# quoting-dispatch-allowlist.ts -- Data Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/data/quoting-dispatch-allowlist.ts` (120 LOC)

## What it is
```
quoting-dispatch-allowlist.ts -- the cost-basis / sensitive-pricing actions the browser must
NEVER reach through the generic POST /api/v1/quoting (and /api/mcp/quoting) dispatch handler.

-- SECURITY: DENY-LIST on the generic dispatch surface (U-MKTPRICE01) --------------------------
The quoting router's generic `router.post("/")` forwards an arbitrary `{ action, params }` to
prism_quoting with only `optionalToken` (which never rejects an anonymous request). That made the
real INTERNAL cost-basis actions below reachable UNAUTHENTICATED via `{ action: "cost_index_prior" }`
-- the leak the U-MKTPRICE01 scrutiny (arm C) caught. This deny-set is the gate: the generic
handler returns 403 for any action listed here.

These actions surface the SHOP'S OWN cost basis / real sold-price distribution -- they must never
leave the operator boundary. The dedicated typed verbs that DO serve them
(POST /
```


## Exports
`QUOTING_DISPATCH_DENY_SCHEMA_VERSION` (const), `QUOTING_GENERIC_DISPATCH_DENY_SET` (const), `isQuotingGenericDispatchDenied` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/data/quoting-dispatch-allowlist.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Data Module

## How to query it
Recall by meaning via `/system-viz find quoting-dispatch-allowlist.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/data/quoting-dispatch-allowlist.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
