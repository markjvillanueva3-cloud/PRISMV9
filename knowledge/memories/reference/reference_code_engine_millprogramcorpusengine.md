---
name: reference_code_engine_millprogramcorpusengine
description: "MillProgramCorpusEngine — the REAL fuel for print-to-program replication."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MillProgramCorpusEngine.ts
  sourceHash: 51b18516
  loc: 554
aliases: millProgramCorpusEngine
---

# millProgramCorpusEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MillProgramCorpusEngine.ts` (554 LOC)

## What it is
```
MillProgramCorpusEngine — the REAL fuel for print-to-program replication.

{@link MillProgramReplicationEngine} ("generate a CNC program just by reading a
print") retrieves the most-similar existing program from a
`corpus: FeatureSequenceRecord[]` and adapts it. But nothing ever materialized
that corpus from the real shop history — so the capability was DARK for lack of
fuel: every `replicate_from_print` call required the caller to pass the records
inline, and in practice the corpus was always empty (engine returned
"empty corpus — no existing programs to replicate from").

This engine is the missing PRODUCER. It:
  1. DISCOVERS real JM mill CAM projects via juliett's `jm-die-database` files
     index (search-first — never walks the 38K-file JM DIE tree with Glob,
     which times out per the noise-path catalog).
  2. PARSES each into a {@link FeatureSequenceRecord} with the existing, p
```


## Exports
`CorpusFileEntry` (interface), `CorpusParser` (type), `BuildCorpusOptions` (interface), `CorpusStats` (interface), `BuildCorpusResult` (interface), `PersistResult` (interface), `BuildAndPersistResult` (interface), `millProgramCorpusEngine` (const)

## Key methods
`buildCorpus`, `persist`, `writeFileAtomic`, `buildAndPersist`, `load`, `getCorpus`, `computeStats`, `calculate`

## Depends on
[[reference_code_engine_hmcprojectparserengine]] | [[reference_code_engine_millprogramreplicationengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/MillProgramCorpusEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find millProgramCorpusEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MillProgramCorpusEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
