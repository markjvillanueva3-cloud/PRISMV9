---
name: reference_code_engine_coordinationstoreengine
description: "HOOK-SYNERGY-MS0 / U-HOOK-COORD-SQLITE (H8)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CoordinationStoreEngine.ts
  sourceHash: f787fda0
  loc: 565
aliases: CoordinationStoreEngine
---

# CoordinationStoreEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CoordinationStoreEngine.ts` (565 LOC)

## What it is
HOOK-SYNERGY-MS0 / U-HOOK-COORD-SQLITE (H8)

```
CoordinationStoreEngine — HOOK-SYNERGY-MS0 / U-HOOK-COORD-SQLITE (H8)

SQLite WAL-mode replacement for the single-JSON-file work-claim store at
`state/shared/WORK_CLAIMS.json`. The legacy JSON store is read+written by
the PreToolUse `work-claim.mjs` hook on every Edit/Write tool call; with
6 concurrent chats × 10 ops/min that's ~60 read-modify-writes/min on the
same file, and the lock-free fs.writeFile pattern is the dominant
contention source the H8 unit was designed to eliminate.

DESIGN
  - SQLite in `journal_mode=WAL`: concurrent readers never block writes,
    writers serialize via the WAL log, and a crashed writer auto-recovers
    on next open. Synchronous=NORMAL is the WAL-recommended setting —
    durable through OS crashes, fast on hot path.
  - busy_timeout = 5_000 ms — concurrent writers wait up to 5 s before
    returning SQLITE_BUSY. Far below the 30 s Stop budget; well abo
```


## Exports
`ClaimRow` (interface), `ClaimConflict` (interface), `ClaimSuccess` (interface), `ClaimResult` (type), `PresenceRow` (interface), `PruneResult` (interface), `MigrationResult` (interface), `CoordinationStoreOptions` (interface), `CoordinationStoreEngine` (class), `getCoordinationStoreEngine` (function), `coordinationStoreEngine` (const), `COORDINATION_STORE_LIMITS` (const)

## Key methods
`VALUES`, `claim`, `release`, `findClaim`, `liveClaims`, `allClaims`, `heartbeat`, `activeSessions`, `findPresence`, `prune`, `counts`, `migrateFromJson`, `close`, `getDbPath`, `health`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CoordinationStoreEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CoordinationStoreEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CoordinationStoreEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
