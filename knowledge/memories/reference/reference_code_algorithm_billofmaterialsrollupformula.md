---
name: reference_code_algorithm_billofmaterialsrollupformula
description: "BillOfMaterialsRollupFormula — multi-level BOM explosion + cost rollup"
metadata:
  type: reference
  kind: code-algorithm
  galaxy: unassigned
  sourcePath: mcp-server/src/algorithms/BillOfMaterialsRollupFormula.ts
  sourceHash: 49ce8834
  loc: 312
aliases: BillOfMaterialsRollupFormula.ts
---

# BillOfMaterialsRollupFormula.ts -- Algorithm SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/algorithms/BillOfMaterialsRollupFormula.ts` (312 LOC)

## What it is
```
BillOfMaterialsRollupFormula — multi-level BOM explosion + cost rollup
(hotel iter16b, 2026-05-24, U-BOM-ROLLUP).

Closes G4 from the ERP-comparison audit. A BOM is a tree where each
parent part is composed of N child parts with a quantity-per-parent. For
a top-level assembly the engine answers:

  1. explode(rootId, qty)
     Recursive multi-level explosion — for every leaf/sub-assembly,
     compute total required quantity = qty_per_parent × parent_required.
     Returns flattened "indented BOM" with running depth + accumulated
     qty (the standard MRP gross-requirements list).

  2. rollUpCost(rootId, qty)
     Recursive cost rollup — for every parent, cost = Σ (child_qty ×
     child_unit_cost). Leaves have unit_cost from the parts catalog;
     sub-assemblies compute their unit_cost bottom-up. Returns the
     assembly's total cost AND the per-level breakdown.

  3. circularRefere
```


## Exports
`BomPart` (interface), `BomComponent` (interface), `ExplodedBomRow` (interface), `BomExplosionResult` (interface), `BomCostRollupResult` (interface), `explodeBOM` (function), `rollUpBomCost` (function), `circularReferenceCheck` (function)

## Key methods
`dfs`, `getRoot`, `detectCycle`, `visit`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/algorithms/BillOfMaterialsRollupFormula.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Algorithm

## How to query it
Recall by meaning via `/system-viz find BillOfMaterialsRollupFormula.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/algorithms/BillOfMaterialsRollupFormula.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
