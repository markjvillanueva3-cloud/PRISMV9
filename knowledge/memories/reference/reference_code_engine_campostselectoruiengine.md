---
name: reference_code_engine_campostselectoruiengine
description: "Post-Processor Selector UI Feed (U-CAM100)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMPostSelectorUIEngine.ts
  sourceHash: 3c985458
  loc: 399
aliases: CAMPostSelectorUIEngine
---

# CAMPostSelectorUIEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMPostSelectorUIEngine.ts` (399 LOC)

## What it is
Post-Processor Selector UI Feed (U-CAM100)

```
CAMPostSelectorUIEngine — Post-Processor Selector UI Feed (U-CAM100)
=====================================================================

PHASE-7: Surfaces JM Die's machine/controller/post-processor matrix
into each of the four CAM plugin adapters so a user can pick the
correct post-processor from inside the CAM UI without leaving it.

Data source:   src/data/jm-die-profile.ts JM_DIE_CONTROLLER_MAP
Machine slots: 15 production machines across lathes, mills, EDMs
               (7 Okuma lathes, 5 mills, 2 sinker EDMs, 1 wire EDM)
               + 6 support machines (not in the controller map)

Per-target encoding:
  - hypermill     → XML selector list (one <machine> element per entry)
  - fusion360     → JSON dropdown items (id, label, postId, family)
  - inventor_hsm  → JSON tree grouped by controller_family
  - mastercam     → Pipe-delimited flat table
  - generic       → Plain JSON a
```


## Exports
`PostSelectorTargetSchema` (const), `PostSelectorTarget` (type), `ControllerFamilySchema` (const), `ControllerFamily` (type), `MachineCategorySchema` (const), `MachineCategory` (type), `MachineSelectorEntrySchema` (const), `MachineSelectorEntry` (type), `RecommendationStatusSchema` (const), `RecommendationStatus` (type), `PostRecommendationSchema` (const), `PostRecommendation` (type), `SelectorPayloadSchema` (const), `SelectorPayload` (type), `SelectorFilterSchema` (const), `SelectorFilter` (type), `SelectorDashboardSchema` (const), `SelectorDashboard` (type), `CAMPostSelectorUIEngine` (class), `camPostSelectorUIEngine` (const)

## Key methods
`listMachines`, `getMachine`, `recommendForMachine`, `encodeForTarget`, `controllerFamilies`, `categories`, `dashboard`, `supportedTargets`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CAMPostSelectorUIEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMPostSelectorUIEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMPostSelectorUIEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
