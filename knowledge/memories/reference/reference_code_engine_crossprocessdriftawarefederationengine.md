---
name: reference_code_engine_crossprocessdriftawarefederationengine
description: "XPROC-NEURAL Tier 6 (T6-03)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CrossProcessDriftAwareFederationEngine.ts
  sourceHash: d820138b
  loc: 433
aliases: CrossProcessDriftAwareFederationEngine
---

# CrossProcessDriftAwareFederationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CrossProcessDriftAwareFederationEngine.ts` (433 LOC)

## What it is
XPROC-NEURAL Tier 6 (T6-03)

```
CrossProcessDriftAwareFederationEngine — XPROC-NEURAL Tier 6 (T6-03)

Federated-round participation gate. Composes T3-02 drift detection
(per-client driftConfidence ∈ [0,1]) with T6-01 FedAvg / T6-02 secure
aggregation to prevent a drifting shop from poisoning the global model.

## Why a separate gate

Bonawitz secure aggregation (T6-02) gives the aggregator no view into
any individual client's gradient. That is the point — privacy. But it
is *also* a vulnerability: a malicious or simply broken client (sensor
miscalibration, post-tooling-change ledger, whatever) can submit garbage
weights and the aggregator can neither inspect them nor identify them.
The whole sum is corrupted with no breadcrumb.

Drift detection at the *client side* (T3-02) is the only signal we can
extract before secure aggregation hides everything. Each client runs
DDM/EDDM/ADWIN against its own outcome stream and rep
```


## Exports
`GateDecision` (type), `ClientDrift` (interface), `ClientGateResult` (interface), `GateOk` (interface), `DriftReportOk` (interface), `OpErr` (interface), `GateResult` (type), `DriftReportResult` (type), `CrossProcessDriftAwareFederationEngine` (class), `crossProcessDriftAwareFederationEngine` (const), `crossProcessDriftAwareFederation` (function)

## Key methods
`gate`, `driftReport`, `constants`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CrossProcessDriftAwareFederationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CrossProcessDriftAwareFederationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CrossProcessDriftAwareFederationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
