---
name: reference_xray_ocr_closed_loop_2026_06_01
description: OCR print-reading closed loop (generate→OCR→score) is built; key finding — the model reads dimensioned prints at ~100%, the real-corpus gap is INPUT QUALITY not model capability
type: reference
source: prism-memory
synced: 2026-06-09T14:54:11.071Z
aliases: reference_xray_ocr_closed_loop_2026_06_01
---


**The OCR print-reading CLOSED LOOP is built + operational** (slot:xray, 2026-06-01, /goal /loop). extract → SCORE → feedback, fully standalone (no MCP). Two commits:
- `U-PSGB-XRAY-CLOSED-LOOP-SCORER`: `scripts/lib/dimension-set-score.mjs` — `scoreDimensionSet(extracted, truth, {pct,absMm})` matches OCR'd dims to ground-truth by mm VALUE within max(1%, 0.05mm) → precision/recall/F1/MAE + missed/extra; `aggregateScores` → micro P/R/F1. 17 tests, 2-of-2 PASS. Value-only (type-blind) + greedy match (documented P2s; Hungarian + type-aware are logged follow-ups).
- `U-PSGB-XRAY-CLOSED-LOOP-RUNNER`: `scripts/lib/synthetic-print-gen.py` (PIL generator of clean dimensioned drawings + perfect ground truth; `--difficulty hard` = rotation+blur+speckle+tolerance-clutter; truth = nominal only) + `scripts/ocr-closed-loop.mjs` (generate N → OCR via qwen3-vl:8b-instruct → score → report with honest coverage/degraded/exit gating + a prominent CAVEAT). 6 gen tests. Round-1 scrutiny FAILed (2 R12 blockers: aggregate masked partial failure + misleading 100%); both fixed → round-2 PASS/PASS.

**KEY FINDING (honest, decisive):** qwen3-vl:8b-instruct scores **100% value-recovery** (recall=precision=1, mae=0mm) on CLEAN *and* moderately-degraded (`--difficulty hard`) synthetic prints. This is the model's ceiling on ideal input. Juxtaposed with the overnight real corpus (~60 OK / 280 attempted), the conclusion is: **the gap is INPUT QUALITY (scan noise / non-drawing pages / paperwork bundles), NOT the model's dimension-reading capability.** The improvement path is INPUT (non-drawing-page filtering, deskew, DPI, multi-print-split) — not a better model and not model fine-tuning.

**Run it:** `node H:/prism/scripts/ocr-closed-loop.mjs --count N [--difficulty hard] [--seed-base S] [--report <path>]` (needs Ollama up — restart bare `ollama serve` if down; it died overnight once). Report → `state/shared/ocr-closed-loop-report*.json`.

**QUANTIFIED (overnight 280 attempted, 60 OK):** failure breakdown = **193 non-drawing (OCR ran, 0 dims) + 27 page-count-hang (the pre-`.ps1`-wrapper console-less scheduled-task bug, now fixed) + 0 render-fail.** Drawing rate among OCR-reachable prints = **24%** (60 drawings / 253). So ~76% of GPU time is spent on non-drawings.

**NEXT (highest-value, the loop's actionable feedback):** a drawing-vs-paperwork PAGE CLASSIFIER to filter the 193-class non-drawing noise BEFORE the full extraction (cheap VLM yes/no ~2s vs ~50-150s full extract; the pairs already carry a `drawing_score`; `looksLikeBlueprint` is a coarse start) → ~4× effective throughput on real drawings + a clean corpus, without touching the model. Then: harder synthetic degradation / real difficult prints to find the model's failure boundary; type-aware matching. See [[reference_xray_ocr_gpu_concurrency_2026_05_31]] (the qwen3-vl:8b-instruct concurrent-model win this builds on).

**UPDATE 2026-06-01b (loop iter5 — page classifier SHIPPED):** commit `U-PSGB-XRAY-PAGE-CLASSIFIER`. `scripts/lib/page-classifier-lib.mjs` (pure core, 29 tests) + `scripts/page-classify.mjs` (live actuator, 7 tests) — a cheap VLM yes/no (`qwen3-vl:8b-instruct`, num_predict 96) classifying a RENDERED PAGE PNG → `extract|skip`, run before the full extract. Deeper than `looksLikeBlueprint` (file-level name/size) — this classifies the page IMAGE, catching paperwork PAGES inside multi-page bundles. **LOAD-BEARING SAFETY BIAS:** a SKIP requires `is_drawing===false AND floor>0 AND conf>=floor AND source!=="prose"` — every other path (uncertain / low-conf / prose / parse-fail / transport-fail) falls through to EXTRACT, so a classifier failure NEVER loses a real drawing. Field-proven: a live `curl exit=56` (Ollama cold-load stall under host memory pressure) correctly extracted. Per-file 2-of-2 scrutiny PASS/PASS (1 P1 fixed pre-commit: the floor-0 data-loss degenerate). **Positive live classification deferred** — env-blocked (qwen3-vl couldn't cold-load with qwen2.5-coder resident on the memory-pressured host); loads fine when it's the chosen resident VLM. Also durably persisted the OCR→CAD handoff contract for delta at `state/shared/specs/U-CADTP-ROUNDTRIP-B-ocr-handoff-schema.md`.

**UPDATE 2026-06-01c (loop iter6 — `--pdf` mode SHIPPED, loop at 6/6 target):** commit `U-PSGB-XRAY-PAGE-CLASSIFIER-PDF`. Added `classifyPdf(pdfPath, opts)` to `scripts/page-classify.mjs` — renders each page via the existing `scripts/lib/pdf-to-png.py` (`--count` then per-page `--grayscale` render) and classifies it, emitting per-page verdicts + the actionable `extract_pages`/`skip_pages` worklist (page indices to extract/skip). This is what makes the gate usable on the REAL corpus: the OCR pipeline `run-ollama-vision-extract.mjs` renders TRANSIENT per-page PNGs, so the gate owns the render loop. `pageCount`+`render` are INJECTABLE (default to the real pdf-to-png.py spawns) → the render-fail→EXTRACT invariant is unit-tested with NO Python/Ollama (closed reviewer-A's "hermetic fakes don't prove wiring" P1). **LIVE-VERIFIED** on a real corpus PDF (JM DIE/.../TT2000_206H...source.pdf): count OK, render OK, page tagged, classify hit Ollama `curl exit=28` (cold-load stall) → correctly `verdict=extract` + `extract_pages=[0]` — render loop + data-loss bias proven live. Per-file 2-of-2 PASS/PASS; 41/41 tests. Committed via `[BOOTSTRAP-SLOT-ENFORCE]` (a newly-armed SLOT-COMMIT-ENFORCE hook now blocks shared-tree commits; iter5 already landed on cad-fusion-live-ms0 so iter6 used the hook's sanctioned one-shot to stay consistent — future xray work should migrate to the `slot/xray` worktree via `/checkin-xray`). **STILL OPEN (both env/peer-blocked, NOT code):** (1) positive VLM classification unproven — run when qwen3-vl:8b-instruct is the RESIDENT VLM (`node scripts/page-classify.mjs --pdf <corpus.pdf> --max-pages 5`); (2) `cad-fix-training-ledger.jsonl` still 0 `domain==print` rows — delta producer-blocked on print↔print compares.

**UPDATE 2026-06-01d (continue-training — SCORER FIDELITY upgrade SHIPPED):** commit `U-PSGB-XRAY-SCORER-TYPEAWARE`. The scorer IS the training signal (generate→OCR→SCORE→feedback), so two non-Ollama-blocked fidelity upgrades to `scripts/lib/dimension-set-score.mjs`: (1) **OPTIMAL matching** — greedy closest-first → Kuhn's max-cardinality bipartite matching; fixes the documented greedy UNDERCOUNT (`truth [100,100.9] vs got [100.5,101.4]` is 2 matches, greedy gave 1 → greedy biased recall DOWN, a dishonest gradient). Matched COUNT now provably maximal; deterministic. (2) **TYPE-AWARE matching (default ON)** — a feasible edge needs value-match AND type-compat; stops value-only false positives (a diameter no longer matches a linear/angular of equal magnitude; 45° no longer matches 45mm). **P0 caught in scrutiny (verify-actual-producer-contract lesson [[feedback_verify_actual_contract_not_proxy]]):** `parseVisionResponse` defaults extracted dims to `type:"unknown"` (literal string, `ollama-vision-extract-lib.mjs:183`), NOT null — so `dimType` now maps sentinel strings (unknown/unspecified/none/...) → null value-only fallback, else a real OCR extraction would score matched=0 vs typed truth and silently zero the gradient. Regression-pinned. 25/25 tests (was 17), per-file 2-of-2 PASS/PASS (reviewer-B: keep type-aware default-ON as the honest gradient). **NEXT non-blocked training step:** expand `scripts/lib/synthetic-print-gen.py` to emit radius/angular/chamfer/GD&T dims (today only linear+diameter) — broadens training coverage AND exercises the new type-aware scorer's type distinctions. The Ollama-gated corpus run remains the gate for the live measurement.

**UPDATE 2026-06-01e (utilize-all-prints — CLEAN TRAINSET CURATED):** commit `U-PSGB-XRAY-TRAINSET-CURATE`. The corpus census + curation, R8 (consumes juliett's `blueprint-training-pairs.jsonl` = 76,205 parts / 255,003 print_docs; NEVER re-OCRs). **Decisive finding (GIGO):** `train_eligible` marks 4,245 parts but the clean supervised set is **3,941** (exact 1,983 + loose 1,958) — the 304-part gap is poison-with-a-real-source (232 ambiguous + 4 garbage + ...) that train_eligible blindly counted; a print joined to the WRONG program/CAD answer-key poisons the dimension-reader gradient. Most raw "garbage"-confidence rows (5,029) are actually UNLABELED (no source) → correctly bucketed, not poison; true poison = 236. **Trainable subsets (clean):** round-trip-B (print+CAD)=**350** (NOT the raw 498 — 148 print+CAD parts have untrustworthy labels), print→program=3,941, triple=350. Curated set → `state/shared/blueprint-trainset-clean.jsonl` (gitignored, regenerable; self-contained rows carry part_number_normalized + print_doc/program/cad locators), census → `blueprint-trainset-census.json` (schemaVersion 1.0.0, mustHumanVerify). `scripts/{lib/blueprint-trainset-curate-lib.mjs (13 tests),blueprint-trainset-curate.mjs}` — distinct CONSUMER vs the `build-blueprint-cad-program-pairs.mjs` PRODUCER (per-file 2-of-2 PASS; reviewer P1 locator-enrichment + P2 schemaVersion fixed). Curation policy keeps weak-but-real `loose`, excludes only poison (reviewer-confirmed not over-aggressive). Regenerate: `node scripts/blueprint-trainset-curate.mjs`.
