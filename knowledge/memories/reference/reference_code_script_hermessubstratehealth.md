---
name: reference_code_script_hermessubstratehealth
description: "hermes-substrate-health.mjs -- LIVE liveness harness for the Hermes substrate."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/hermes-substrate-health.mjs
  sourceHash: c28fd74f
  loc: 292
aliases: hermes-substrate-health.mjs
---

# hermes-substrate-health.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/hermes-substrate-health.mjs` (292 LOC)

## What it is
```
hermes-substrate-health.mjs -- LIVE liveness harness for the Hermes substrate.

The I/O boundary for HermesSubstrateHealthEngine (the pure grader). The operator's
"harden first, then loop" directive (2026-06-29): before building a parallel work-loop
on Hermes, PROVE the substrate works. Ten Hermes scheduled tasks are registered + Ready,
but `ask-hermes` historically fell back to Ollama 12/13 calls -- "Ready" is not proof.

This runner gathers a LIVE SubstrateProbe and grades it:
  1. Proxy /health -- reuses checkHermesProxy() from reconcile-zulu-ledger.mjs (R8 clone-don't-fork;
     the origin-strip /v1 -> root /health is already correct there, avoiding the /v1/health 404 false-DOWN).
  2. A REAL ask-hermes round-trip (`ask <q> --json`) -- the decision-time authority. We read the
     answering `source` to learn if the Hermes lane works NOW (vs the stale lifetime counter).
  3. The 10 ex
```


## Exports
`isUnderTsx` (function), `resolveTsxCli` (function), `planTsxReexec` (function), `EXPECTED_TASKS` (const), `probeRoundTrip` (function), `probeTasks` (function), `readUsage` (function), `gatherProbe` (function), `appendReceipt` (function), `main` (function)

## Key methods
`append`, `log`, `main`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/hermes-substrate-health.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find hermes-substrate-health.mjs` or `prism_session:master_index_query`; open the source at `scripts/hermes-substrate-health.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
