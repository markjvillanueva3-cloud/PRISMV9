---
name: reference_code_engine_perappincadinferenceadapter
description: "U-CAD-APP-20 (PHASE-48)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PerAppInCADInferenceAdapter.ts
  sourceHash: 90840629
  loc: 687
aliases: PerAppInCADInferenceAdapter
---

# PerAppInCADInferenceAdapter -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PerAppInCADInferenceAdapter.ts` (687 LOC)

## What it is
U-CAD-APP-20 (PHASE-48)

```
PerAppInCADInferenceAdapter — U-CAD-APP-20 (PHASE-48)

Embeds foundation model inference directly inside CAD plugin processes
for ultra-low-latency predictions (≤100ms p99 SLO).

Features:
  - In-process ONNX/TensorRT model loading per CAD application
  - Feature extraction from CAD geometry
  - Multiple inference types (tool, parameter, error, recommendation)
  - Result caching with LRU eviction
  - SLO monitoring (p50/p95/p99 latency tracking)
  - Model hot-swapping without restart
  - Quantized INT8/FP16 models for speed

Supported CAD Applications:
  - Mastercam (NET-Hook in-process)
  - Onshape (WebAssembly/SharedArrayBuffer)
  - AutoCAD (.NET add-in)
  - Siemens NX (NXOpen)
  - SolidWorks (COM add-in)
  - Fusion 360 (JavaScript sandbox)

@module engines/PerAppInCADInferenceAdapter
```


## Exports
`CADAppTypeSchema` (const), `CADAppType` (type), `InferenceTypeSchema` (const), `InferenceType` (type), `ModelFormatSchema` (const), `ModelFormat` (type), `QuantizationSchema` (const), `Quantization` (type), `ModelConfigSchema` (const), `ModelConfig` (type), `FeatureVectorSchema` (const), `FeatureVector` (type), `InferenceResultSchema` (const), `InferenceResult` (type), `LatencyStatsSchema` (const), `LatencyStats` (type), `InferenceClock` (interface), `InferenceRuntime` (interface), `FeatureExtractor` (interface), `InCADInferenceCapabilities` (interface)

## Key methods
`now`, `nowMs`, `loadModel`, `unloadModel`, `isModelLoaded`, `runInference`, `warmup`, `getMemoryUsage`, `extractFromGeometry`, `extractFromToolpath`, `extractFromOperation`, `describeCapabilities`, `hotSwapModel`, `getLoadedModels`, `getModelsForApp`, `infer`, `inferFromGeometry`, `inferFromToolpath`, `inferFromOperation`, `batchInfer`, `getLatencyStats`, `getAllLatencyStats`, `clearLatencyStats`, `getCacheStats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PerAppInCADInferenceAdapter.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PerAppInCADInferenceAdapter` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PerAppInCADInferenceAdapter.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
