---
name: reference_code_engine_employeemultijobconcurrencyengine
description: "EmployeeMultiJobConcurrencyEngine — Multi-job concurrent state + time allocation."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/EmployeeMultiJobConcurrencyEngine.ts
  sourceHash: 9e8b2cde
  loc: 519
aliases: employeeMultiJobConcurrencyEngine
---

# employeeMultiJobConcurrencyEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/EmployeeMultiJobConcurrencyEngine.ts` (519 LOC)

## What it is
```
EmployeeMultiJobConcurrencyEngine — Multi-job concurrent state + time allocation.

Closes two operator gaps:

 1. **Phone "quick menu"** — operator on a phone needs to see all the jobs
    they're checked into AT A GLANCE and pause/resume any of them in 1 tap.
    The existing portal supports ONE active task per employee with explicit
    pause/resume; this engine layers a multi-job concurrent state on top.

 2. **Concurrency-aware time allocation** — when an operator runs 3 machines
    simultaneously, wall-clock time charges fully against each task by
    default. That's unfair (one human can't truly run 3 jobs each for a full
    hour in an hour). This engine allocates wall-clock-time across the
    concurrent set via one of two strategies:
      - "equal": each active job gets wall_clock / N
      - "weighted": each active job gets wall_clock × (its_weight / sum_weights)

 Wall-clock
```


## Exports
`ConcurrentJobState` (type), `ConcurrencyStrategy` (type), `ConcurrentJob` (interface), `QuickMenuEntry` (interface), `TickAllocation` (interface), `PartCountAdjustment` (interface), `ClockedTimeAdjustment` (interface), `employeeMultiJobConcurrencyEngine` (const)

## Key methods
`checkInToJob`, `pauseJob`, `resumeJob`, `pauseAll`, `quickSwitch`, `setStrategy`, `getStrategy`, `setAttentionWeight`, `tickConcurrentTime`, `quickMenu`, `listConcurrent`, `adjustPartCount`, `adjustClockedTime`, `listPartAdjustments`, `listClockAdjustments`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/EmployeeMultiJobConcurrencyEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find employeeMultiJobConcurrencyEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/EmployeeMultiJobConcurrencyEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
