---
name: reference_code_engine_qdrantvectorstoreengine
description: "Thin client around @qdrant/js-client-rest"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/QdrantVectorStoreEngine.ts
  sourceHash: 265115ad
  loc: 283
aliases: QdrantVectorStoreEngine
---

# QdrantVectorStoreEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/QdrantVectorStoreEngine.ts` (283 LOC)

## What it is
Thin client around @qdrant/js-client-rest

```
QdrantVectorStoreEngine — Thin client around @qdrant/js-client-rest

Phase external-infra. Provides a PRISM-styled surface over the Qdrant REST
client so higher-level engines (SemanticSimilarityGuard, Tool DB vector
search, MIT course index) don't have to deal with the raw client's many
options. Methods are minimal + named after the action, not the endpoint.

Connection contract:
  - The engine is stateless until a caller invokes `connect()`. Health is
    a read-only property derived from `ping()`.
  - Every operation that touches the network returns a typed `Result` so
    the caller can reason about failure without try/catch noise.

Nothing here actually pulls in the Qdrant server — you still have to run
`docker compose up -d qdrant` (or point the engine at a remote host).

@module engines/QdrantVectorStoreEngine
@milestone PP-INFRA-QDRANT
```


## Exports
`Distance` (type), `QdrantConnectionOptions` (interface), `CollectionSpec` (interface), `UpsertPoint` (interface), `SearchOptions` (interface), `SearchHit` (interface), `Result` (type), `QdrantVectorStoreEngine` (class), `qdrantVectorStoreEngine` (const)

## Key methods
`connect`, `isConnected`, `getConnectionOptions`, `ping`, `listCollections`, `ensureCollection`, `deleteCollection`, `upsert`, `search`, `count`, `scrollAll`, `disconnect`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/QdrantVectorStoreEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find QdrantVectorStoreEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/QdrantVectorStoreEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
