---
name: reference_code_engine_extractionplanexecutor
description: "extractionPlanExecutor -- drive a blueprint/document extraction routing PLAN to actual consumer dispatch."
metadata:
  type: reference
  kind: code-engine
  galaxy: blueprint-vision
  sourcePath: mcp-server/src/engines/blueprint-vision/extractionPlanExecutor.ts
  sourceHash: fa036937
  loc: 222
aliases: extractionPlanExecutor.ts
---

# extractionPlanExecutor.ts -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/blueprint-vision/extractionPlanExecutor.ts` (222 LOC)

## What it is
```
extractionPlanExecutor -- drive a blueprint/document extraction routing PLAN to actual consumer dispatch.

WHY (cross-domain "apply extraction to ALL app features" end-to-end): `blueprintExtractionRouter`
(routeExtractionToConsumers) produces a confirm-gated fan-out PLAN -- which downstream prism feature each
extraction CAN drive -- but it is PURE: it plans, it does not call anything. Nothing turned the plan into
ACTION. This module is that missing executor: given a validated plan + a dispatcher-call fn + the
operator's confirmations, it drives the eligible, confirm-cleared consumer actions across EVERY downstream
domain (quote -> business, program -> cam, inspection/fai/cmm -> quality, feature/cad/redact -> cad, ...),
isolating per-consumer errors and aggregating a report.

SAFETY (the load-bearing rule): a COMMITMENT consumer (quote = money, print_to_program = machine motion /
scrap, i
```


## Exports
`CallToolFn` (type), `EXTRACTION_PLAN_EXECUTION_VERSION` (const), `DEFAULT_EXECUTE_KINDS` (const), `ExecutePlanOpts` (interface), `ExecStatus` (type), `ConsumerExecResult` (interface), `PlanExecutionReport` (interface), `decideRouteDisposition` (function), `executeExtractionPlan` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/blueprint-vision/extractionPlanExecutor.ts`
- **Galaxy:** **blueprint-vision**
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find extractionPlanExecutor.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/blueprint-vision/extractionPlanExecutor.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
