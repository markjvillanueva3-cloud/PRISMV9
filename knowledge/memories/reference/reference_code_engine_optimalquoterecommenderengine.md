---
name: reference_code_engine_optimalquoterecommenderengine
description: "Optimal Quote Recommender"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/OptimalQuoteRecommenderEngine.ts
  sourceHash: 5b71c7cf
  loc: 172
aliases: OptimalQuoteRecommenderEngine
---

# OptimalQuoteRecommenderEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/OptimalQuoteRecommenderEngine.ts` (172 LOC)

## What it is
Optimal Quote Recommender

```
OptimalQuoteRecommenderEngine -- QUOTING-OPTIMAL-MS0 / U7 (Layer B "predict + recommend")

The composing head of the two-layer optimal-quote algorithm. Given a Layer-A cost floor
and a customer, it returns BOTH:

  1. PREDICTED PRICE -- what the part will realistically quote/sell at, from
     RealizedPriceModelEngine (U6, fit on 1,787 real settled prices), with an honest
     log-normal CI band. ALWAYS returned (we have won-order data for this).

  2. PROFIT-OPTIMAL PRICE -- the price that maximizes margin x P(win), from
     BidWinCalibratorEngine's logistic win-probability curve. Returned ONLY when the
     win/loss training data is SUFFICIENT (both won AND lost bids, >= minBothClasses each).
     Otherwise suppressed with reason "insufficient-win-loss-data" -- a logistic curve fit
     on won-only data is meaningless (P(win) -> 1 everywhere). This is the operator's
     decision: "Bo
```


## Exports
`MIN_BOTH_CLASSES_FOR_OPTIMIZER` (const), `WinLossCounts` (interface), `ProfitOptimalResult` (interface), `MarkupOptimizer` (interface), `RecommendInput` (interface), `OptimalQuoteRecommendation` (interface), `OptimalQuoteRecommenderEngine` (class), `optimalQuoteRecommenderEngine` (const)

## Key methods
`counts`, `optimal`, `recommend`

## Depends on
[[reference_code_engine_realizedpricemodelengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/OptimalQuoteRecommenderEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find OptimalQuoteRecommenderEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/OptimalQuoteRecommenderEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
