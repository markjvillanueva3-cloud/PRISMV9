---
name: reference_synergy_roi_verified_2026_06_14
description: "Verified outcome of the 'find high-ROI synergy potential' + 'build it all' audit (slot:echo 2026-06-14): all 5 surveyed offload-actuation 'leaks' were already-fixed / deliberately-advisory / architecturally-non-actuatable / category-errors / host-latency-constrained. The offload dashboard's cumulative fire/execute ratios are a MISLEADING ROI signal. Empirical GPU gist-latency numbers included."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.214Z
aliases: reference_synergy_roi_verified_2026_06_14
---


**SYNERGY-ROI AUDIT — VERIFIED NO-SOUND-BUILD (2026-06-14, slot:echo).** Operator: "find high roi synergy potential" → "build it all" (Ultracode) → "use sound logic". A survey built from the **offload-stats dashboard** (`ollama-offload-dashboard.mjs`) flagged 5 "advisory-fires-but-never-executes" leaks. Close reading of the ACTUAL code + LIVE empirical tests **falsified all 5** as build targets. Lesson: the dashboard's raw fire/execute ratios are **cumulative and lag the fleet's real fixes** — do NOT treat them as live ROI leaks without reading the code (honesty rule: existence/count ≠ correct).

**Per-item verified ground truth:**
1. **#1a `large-read-digest-advisory.mjs`** — already wired to `advisory-decay` (muted at 0/122 conversion). A PreToolUse:Read hook can only block+substitute or allow; force-substituting a digest for a file the model deliberately opened **loses quality** (operator's own synergy def forbids "without losing quality"). The decay-mute is CORRECT. Not a sound build.
2. **#1b `ollama-route-pretooluse.mjs`** — auto-mode ALREADY enabled (`ollama-route-config.json` `mode:auto` flipped 2026-05-22 sierra + env `PRISM_OLLAMA_ROUTE_AUTO=1`); the two real bugs already fixed (deleted-model `7b`→`32b` alpha 2026-06-04; cold-start timeout 9s→30s zulu 2026-06-10). Residual 0-reroute is the **gist-safe allowlist working as designed** (`.log/.txt/.out` only; structured data correctly → suggest). The 0-offload that REMAINS is **host GPU-latency-constrained** (below). `ollama-task-offloader.mjs` = advisory-by-architecture (UserPromptSubmit can't actuate a user prompt); 6% is mostly-correct.
3. **#2 MCP bridge reaper-exemption** — ALREADY DONE: `scripts/fleet-reaper-sweep.mjs:630-665` `BRIDGE-PROTECT` (golf 2026-05-25) `_MCP_PROTECT_REGEX = mcp-http-bridge|mcp-server-supervisor|dist/index.js|...` with a CIM protected-PID set + fail-safe. The reaper does NOT kill the bridge. "MCP BRIDGE DOWN" (`mcp-connectivity-check.mjs countBridges`) just READS the reaper enum-cache as a cheap detector; bridge-down is HARNESS-side (only `/mcp` respawns it) or a stale-cache false-0.
4. **#4** — `read-auto-limit.mjs` = advisory-by-architecture (a PreToolUse hook can't rewrite Read args, only suggest). `prompt-rewriter-ollama.mjs` = a **quality-improver, NOT a token-saver** (its ledger "0 saved" is correct — category error in the survey); its real bugs (IPv6 `localhost` 2026-06-09, cold-start/loaded-only 2026-05-28) already fixed.
5. **#3 Hermes ladder rung** — ALREADY built: `scripts/lib/task-substrate-router.mjs` (tango RGS-PLANNING-LOOP-BRIDGE-MS0/U2 2026-06-11) has the full Hermes lane (`shouldUseWorkflow` gate from `hermes-workflow-planner.mjs`, hermes substrate row, `hermesGated`, concurrency cap) + this session's edit wired `ask-hermes.mjs` into both the ollama-escalation and hermes `how`-strings.

**EMPIRICAL GPU gist-latency (THIS host: RTX Pro 6000 Blackwell 96GB, gpt-oss:120b resident, under fleet load), 48KB log gist via /api/generate:**
- qwen2.5-coder:32b COLD: >31s (timed out at the hook's 30s budget) · qwen2.5-coder:1.5b COLD: 59s · gpt-oss:20b COLD: >60s (timeout) · **gpt-oss:120b WARM: 39s**.
- **EVERY path > the 30s offload budget.** Gisting 48KB on a loaded GPU is 30-60s warm-or-cold. So `ollama-route` auto-offload fail-opens (raw read) every time — **correctly** (a 30-60s stall before a Read to save ~12k tokens is a bad interactive trade). Reducing `READ_FOR_SUMMARY_CAP` (48KB) trades gist-completeness for latency (a 12KB gist of a 271KB log misses 95%) — not a clean win. The correct large-log token-saver is **read-with-offset/limit (free, instant)**, already advised by `read-auto-limit`.

**Why (the meta-finding):** PRISM's offload/actuation surface is **saturated + deliberately tuned**, not leaking. The advisory-only designs are intentional (quality-preservation, harness limits, gist-safe allowlists, host latency). Forcing "actuation" would reverse quality decisions, reintroduce fixed bugs, widen safe allowlists, or duplicate existing work — all net-negative. R12 + "use sound logic" → REPORT the truth, don't manufacture a build.

**How to apply:** (1) Don't re-run this investigation — it's verified. (2) When auditing "synergy ROI", do NOT trust cumulative offload-dashboard fire/execute ratios as live leaks; read the code + check the config + live-test before claiming a leak. (3) If hunting synergy ROI, look at a surface OTHER than offload-actuation (that one is saturated). (4) Genuine small follow-ups (LOW ROI, optional): window the offload dashboard to a recent horizon so it stops showing stale cumulative 0s; reconcile the route-model config (`qwen2.5-coder:32b` cold) with the prewarmed resident model — though neither makes 48KB gist fit the budget. Related: [[feedback_synergy_definition]], [[feedback_autonomous_loop_drift_discipline]], [[reference_hermes_bridge_ms0_2026_06_13]], [[reference_blackwell_token_synergy_ms0_2026_06_03]].
