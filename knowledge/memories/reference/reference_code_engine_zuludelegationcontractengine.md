---
name: reference_code_engine_zuludelegationcontractengine
description: "ZuluDelegationContractEngine -- C4 (ZULU fleet, HZD-NEW-03)."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ZuluDelegationContractEngine.ts
  sourceHash: bb733184
  loc: 632
aliases: zuluDelegationContractEngine
---

# zuluDelegationContractEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ZuluDelegationContractEngine.ts` (632 LOC)

## What it is
```
ZuluDelegationContractEngine -- C4 (ZULU fleet, HZD-NEW-03).

Time/token/galaxy-bounded authority DELEGATION contracts. The gap this fills:
ZuluFleetGovernorEngine (HZD-02) checks authority BINARY -- a slot's soul
(hermes_role / domain_filter / refuse_list) either clears an operation or it
doesn't, forever. It has no notion of a *bounded* delegation: "slot alpha may
perform assign/veto on mill-galaxy tasks for 30 minutes or 50K tokens,
whichever comes first." This engine adds that bound as a typed contract and a
PRE-GATE consulted before the governor's authority check.

SAFETY -- the gate is STRICTLY NARROWING (never widening):
  - composeGatedAuthority() runs the delegation check FIRST; the governor runs
    AFTER. A delegation verdict can only turn the governor's ALLOW into a DENY
    (when a matching contract is expired / revoked / over token-cap). It can
    NEVER turn a governor DEN
```


## Exports
`DelegationOperation` (type), `DelegationContractSchema` (const), `DelegationContract` (type), `GrantInputSchema` (const), `GrantInput` (type), `DelegationCheckRequest` (interface), `DelegationDecision` (type), `DelegationVerdict` (interface), `GrantResult` (interface), `RevokeResult` (interface), `ComposedAuthority` (interface), `GovernorVerdictLike` (interface), `zuluDelegationContractEngine` (const), `ZuluDelegationContractEngine` (re-export), `ORCHESTRATOR_ROLES` (re-export), `DELEGATION_OPERATIONS` (re-export), `SCHEMA_VERSION` (re-export)

## Key methods
`getInstance`, `getStorePath`, `evaluateDelegation`, `composeGatedAuthority`, `grant`, `revoke`, `recordUsage`, `check`, `allContracts`, `status`

## Depends on
[[reference_code_engine_zulufleetgovernorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/ZuluDelegationContractEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find zuluDelegationContractEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ZuluDelegationContractEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
