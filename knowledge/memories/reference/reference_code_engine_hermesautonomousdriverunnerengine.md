---
name: reference_code_engine_hermesautonomousdriverunnerengine
description: "Hermes Autonomous Drive Runner"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HermesAutonomousDriveRunnerEngine.ts
  sourceHash: dba72d8a
  loc: 211
aliases: HermesAutonomousDriveRunnerEngine
---

# HermesAutonomousDriveRunnerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HermesAutonomousDriveRunnerEngine.ts` (211 LOC)

## What it is
Hermes Autonomous Drive Runner

```
HermesAutonomousDriveRunnerEngine -- the GATED, async runner that turns the pure
HermesAutonomousDriverEngine state machine into a self-driving loop by actually
EXECUTING each ready wave via an injected executor.

HERMES-AUTONOMOUS-DRIVER consumer (2026-06-22, slot:zulu). The R13 integration layer
over the verifiable core (HermesAutonomousDriverEngine): the driver decides WHICH
subtasks are ready; this runner DECIDES HOW to run them and feeds results back.

SAFETY (R12/R6 -- this is the high-blast-radius half):
  - DEFAULT-OFF gate: drive() refuses to execute unless PRISM_HERMES_AUTONOMOUS_DRIVE=1
    (or opts.gateEnabled:true). Install never auto-fires an autonomous agent wave.
  - Bounded fan-out: at most maxParallel subtasks execute concurrently per wave.
  - Per-subtask timeout: a never-completing subtask resolves to a failure, never hangs.
  - Termination is guaranteed by the driver
```


## Exports
`SubtaskExecutor` (type), `DriveRunOptions` (interface), `DriveWaveTrace` (interface), `DriveRunResult` (interface), `HermesAutonomousDriveRunnerEngine` (class), `hermesAutonomousDriveRunnerEngine` (const)

## Key methods
`gateOn`, `drive`

## Depends on
[[reference_code_engine_hermesautonomousdriverengine]] | [[reference_code_engine_hermesparallelfanoutplannerengine]] | [[reference_code_engine_hermesgoaldecomposerengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/HermesAutonomousDriveRunnerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HermesAutonomousDriveRunnerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HermesAutonomousDriveRunnerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
