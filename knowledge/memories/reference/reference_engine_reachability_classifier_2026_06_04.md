---
name: reference_engine_reachability_classifier_2026_06_04
description: classifier-aware reachability tool — proves 49% of the ghost.unwired punch list is false-positive; 49 verified genuine orphans routed
type: reference
source: prism-memory
synced: 2026-06-09T14:54:09.103Z
aliases: reference_engine_reachability_classifier_2026_06_04
---


**[WIRING]/U-CLASSIFIER-AWARE-HUNT (slot:romeo, 2026-06-04, commit `b902ac2024`).** Built `scripts/classify-engine-reachability.mjs` (+ real-data E2E test 2/2, artifact `state/shared/engine-reachability-classification.json`) — the reachability check the system-viz `ghost.unwired` classifier LACKS. For each "unwired" candidate it does 1-level transitive classification: **DIRECT_WIRED** (dispatcher refs class/singleton), **AGGREGATOR_REACHABLE** (referenced by another engine that IS wired — WIRE-EXEMPT), **CAPABILITY_WIRED_ELSEWHERE** (de-suffixed root appears in a dispatcher → capability wired under a different name; dead-duplicate risk), **PHANTOM_NAME** (no engine file), **CONSUMED_BY_UNWIRED** (chain orphan), **GENUINE_ORPHAN** (real wire target).

**EMPIRICAL FINDING (quantifies [[reference_wire_exempt_monolith_false_ghost_2026_06_04]]):** over the full 110-engine `UNWIRED-ENGINE-AUDIT-2026-05-07`: **49 GENUINE_ORPHAN · 35 AGGREGATOR_REACHABLE · 12 DIRECT_WIRED · 7 CAPABILITY_WIRED_ELSEWHERE · 7 CONSUMED_BY_UNWIRED → 54/110 (49%) are FALSE ghost.unwired.** The awareness-snapshot "110 unwired" punch list is HALF noise. **→ sierra: port these 4 detectors into the system-viz classifier** (it currently only follows direct dispatcher edges).

**R12 catch (why the test matters):** exact-token matching alone false-flagged `ExpandingMandrelEngine` GENUINE — but the capability is wired as `turningDispatcher:lathe_workholding_expanding_mandrel` → `latheWorkholdingEngine.calculateExpandingMandrel()`. The false-GENUINE is the DANGEROUS direction (leads to duplicate wiring). Fixed with capability-root substring + snake_case + file-existence detectors; the E2E test locks the anti-false-negative guard. Ground-truth-verified 4 verdict classes.

**7 CAPABILITY_WIRED_ELSEWHERE (dead-duplicate cleanup candidates → owning slots):** ExpandingMandrelEngine, MeasureSummaryEngine, TurretLayoutEngine (whiskey/lathe); CoolantStrategyAdapter, EntryExitStrategyAdapter, IntelligentSequencingAdapter (cam/kilo); BlueprintOCRAdapter (xray). Each has a real .ts file but its capability is wired under another engine+method — verify + delete-or-WIRE-EXEMPT, do NOT wire.

**49 GENUINE_ORPHAN routed by domain (next wire targets):**
- **romeo in-lane (dev/session infra — wire next):** CircularDependencyEngine→prism_dev, SemanticAssetIndexEngine→prism_dev, PSNHealthCheckEngine→prism_session/dev, OperatorPreferencesEngine→prism_session, SlotSessionHistoryEngine→prism_session, CNCToolOffsetPersistenceEngine→prism_turning/cam.
- **NOT wire targets (test-infra, WIRE-EXEMPT):** CreoIntegrationTestSuiteEngine, PactContractTestEngine, MastercamHeadlessIntegrationTestEngine, SyncCodeVerificationEngine.
- **india (AI):** ToolDatabaseDeepLearningEngine, ToolLifeGnnEngine, TPEHyperparameterSearchEngine, MOEAStoppingCriterion, BayesianAcquisitionRefiner, DeepSeekClientEngine, GrokCLIClientEngine, CounterfactualMillEngine, AttractorDetectionEngine, EntropyTrackerEngine, MetacognitionBudgetEngine.
- **delta (CAD):** CreoAddinRibbonEngine, CATIAAddinPluginEngine, RhinoCommonBridgeEngine, OnshapeAPIBridgeEngine, OnshapeLiveCollabAdapter, NXOpenAssemblyDrawingEngine, HyperCADSElectrodeEngine, DesignToFloorPipelineEngine.
- **oscar (SFC):** SFCCompareEngine, SpeedFeedDownstreamSubscriberEngine, SpeedFeedExhaustiveCombinationEngine, SpeedFeedTriComparatorEngine.
- **whiskey (lathe):** SwissTypeDecisionEngine, BarRemnantManagementEngine, SubprogramExtractionEngine.
- **charlie/hotel:** QuotingClosedLoopRunnerEngine, JMCustomerVendorDatabaseEngine.
- Remaining infra (golf/papa): LokiLogSinkEngine, SBOMReviewEngine, TriLevelKillSwitchEngine, WetRunStateMachineEngine, SimulationStallDetectorEngine, MeasurementSystemAnalysisEngine, CohortBridgeShimEngine, HzpDashAuditEngine, PlaywrightAutomationEngine, MITCourseExpansionEngine (lima).

**FIRST WIRE FROM THE HUNT — `U-WIRE-DEPENDENCY-CYCLE-ANALYZE` (commit `6917ccaf3e`):** CircularDependencyEngine → `prism_dev:dependency_cycle_analyze` (Tarjan SCC + Kahn topo-sort over a supplied node/edge graph). 6 round-trip tests (Tarjan invariants). Correctness fix wired+tested: `addEdge` does NOT populate the nodes map but `findSCCs` iterates `nodes.keys()`, so the case `addNode`s EVERY edge endpoint — else an edge-only node is dropped and a real cycle reads acyclic.

**REFINEMENT — GENUINE_ORPHAN ≠ trivially wireable.** Genuine orphans split two ways:
- **(a) self-contained** — static methods OR a zero-arg singleton (CircularDependencyEngine). Romeo wires these directly.
- **(b) infra-dependent** — constructor-injected live services / async I/O. `SemanticAssetIndexEngine` is (b): `new SemanticAssetIndexEngine(qdrantStore, embedder, config)` needs a live Qdrant + embedder. Wiring = a cross-domain integration decision (juliett/database + india/embeddings), NOT a romeo quick-wire (refuse: cross-domain-without-justification). **→ flag SemanticAssetIndexEngine for juliett/india, do NOT force-wire.** Before wiring any GENUINE_ORPHAN, read its constructor/singleton export: no zero-arg singleton + injected deps ⇒ type (b), route to the owning domain.

**SECOND WIRE — `U-WIRE-OPERATOR-PREFS` (commit `7ec7c17249`):** OperatorPreferencesEngine → `prism_session` (3 actions: operator_prefs_set/get/apply). Type-(a) self-contained (zero-arg singleton). 8 round-trip tests (set→get singleton round-trip, defaults fallback, apply=true-with-prefs / fail-soft-without, schema-reject). Pattern note: sessionDispatcher cases `return ok({success:true,...})` directly (NOT devDispatcher's `result=...;break;` central wrapper); schemas live in `sessionActionSchemas.ts` as `.passthrough()` consts registered in `ACTION_SESSION_SCHEMAS`; camelCase params match the engine's native type (no normalizeParams alias needed).

**SHARED-TREE COMMIT CONTENTION (recurring):** on `cad-fusion-live-ms0` with ~20 live chats, `.git/index.lock` is held in short bursts by live peer `git.exe` processes (lock mtime == now, NOT a frozen orphan → do NOT remove it). A backgrounded commit that gets SIGTERM'd mid-run leaves the index dirty. ROBUST FIX: a foreground retry-loop `for i in $(seq 1 12); do [ ! -e .git/index.lock ] && git add … && git commit -F msg -- <paths> && break; sleep 3; done` — caught a window on attempt 10. Always `[BOOTSTRAP-SLOT-ENFORCE]` + path-scoped on the FIRST attempt. (The slot-worktree separate-index is the real cure; this is the shared-tree workaround.)

**THIRD WIRE — `U-WIRE-PSN-HEALTH-CHECK` (commit `b240aa83c0`):** PSNHealthCheckEngine → `prism_dev:psn_health_check`. Pure static 11-leg PSN-leg health classifier (caller assembles signal inputs; engine classifies only, no I/O; missing legs → "unknown"). 4 tests (empty→11 unknown anti-stub, green/amber/red reference-value classification, adversarial invalid-input→structured error, enum-accept). check() runs LegInputsSchema.parse internally → case try/catch converts ZodError to a structured error (not a throws-on-every-call wire). **3 genuine orphans wired this session: CircularDependency(prism_dev) + OperatorPrefs(prism_session) + PSNHealthCheck(prism_dev).**

**FOURTH WIRE — `U-WIRE-MSA-ANALYZE` (commit `6620095eaf`):** MeasurementSystemAnalysisEngine → `prism_dev:msa_analyze`. Pure static Gage R&R via crossed ANOVA (AIAG MSA 4th ed) — EV/AV/IV/PV/GRR variance components, %GRR verdict (acceptable<10/marginal/unacceptable>30), NDC. 5 tests (near-perfect gage → acceptable %GRR<10 + SS_part>>SS_equipment anti-stub reference value; 2×1×3 variability shape; insufficient-data + ragged-trials adversarial → structured errors; enum-accept). check throws on bad data → case try/catch → structured error.

**4 GENUINE ORPHANS WIRED THIS SESSION (23 round-trip tests):** CircularDependency→prism_dev:dependency_cycle_analyze · OperatorPrefs→prism_session(set/get/apply) · PSNHealthCheck→prism_dev:psn_health_check · MSA→prism_dev:msa_analyze. **Test-legitimacy gate lesson:** `expect(x).toBeTruthy()` is BLOCKED as "presence-only" — assert concrete values (`Number.isFinite(x)` + `>=0` + a domain invariant like totalVariation.stddev>0), not mere existence.

**USAGE for every future romeo pickup:** `node scripts/classify-engine-reachability.mjs <EngineName>` BEFORE wiring — if not GENUINE_ORPHAN, don't wire. Then read the engine: type (a) self-contained → wire; type (b) infra-dependent → route to owning domain. Re-run full (no args) after any audit refresh. Remaining romeo-in-lane next wires (all type-a but HEAVIER than the 4 shipped — budget for them fresh): **SBOMReviewEngine** (stateful, ~4-5 actions: registerComponent/startReview/status/osvDelta) → prism_dev · **SimulationStallDetectorEngine** (stateful singleton, ~3 actions: markProgress/scan/stats) → prism_dev · **CNCToolOffsetPersistenceEngine** → prism_turning (NEW dispatcher to learn) · **SlotSessionHistoryEngine** (file-backed, constructor baseDir). The 4 shipped were the single/few-call clean ones; these need a multi-action surface each.
