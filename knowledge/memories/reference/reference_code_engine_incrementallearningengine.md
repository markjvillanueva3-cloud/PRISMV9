---
name: reference_code_engine_incrementallearningengine
description: "Phase 0.19 U-LLM6"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/IncrementalLearningEngine.ts
  sourceHash: c5978fcf
  loc: 409
aliases: IncrementalLearningEngine
---

# IncrementalLearningEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/IncrementalLearningEngine.ts` (409 LOC)

## What it is
Phase 0.19 U-LLM6

```
IncrementalLearningEngine — Phase 0.19 U-LLM6

Nightly orchestrator that turns tracked outcomes into LoRA training
jobs. The TypeScript side:
  1. Queries `OutcomeTrackingEngine` for records in a window,
  2. Filters down to the outcome kinds the trainer actually learns
     from (default: "good" + "adjusted" — scraps are counter-examples
     handled separately in the Python pipeline),
  3. Emits a training manifest JSON to a pending directory on disk,
  4. Updates a status log as the job moves pending → running →
     succeeded / failed,
  5. Optionally shells out to a Python trainer via execFile (stub-
     friendly so tests don't need Python installed).

What this engine *does not* do: run the actual training. That is
`PRISMLoRAAdapterEngine` (U-LLM3) and the companion Python script.
This engine just herds inputs, emits a deterministic manifest, and
tracks the job lifecycle.

@module
```


## Exports
`JobStatus` (type), `TrainingManifestExample` (interface), `TrainingManifest` (interface), `JobRecord` (interface), `PrepareOptions` (interface), `PrepareReport` (interface), `RunOptions` (interface), `RunReport` (interface), `IncrementalLearningDeps` (interface), `IncrementalLearningEngine` (class), `incrementalLearningEngine` (const)

## Key methods
`prepareJob`, `runJob`, `nightly`, `listJobs`, `readJob`, `readManifest`

## Depends on
[[reference_code_engine_outcometrackingengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/IncrementalLearningEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find IncrementalLearningEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/IncrementalLearningEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
