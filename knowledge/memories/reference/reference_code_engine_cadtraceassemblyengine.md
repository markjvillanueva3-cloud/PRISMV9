---
name: reference_code_engine_cadtraceassemblyengine
description: "U-AI-10 (CAD-COMPLETE-MS0 / PHASE-47)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADTraceAssemblyEngine.ts
  sourceHash: 4781178d
  loc: 530
aliases: CADTraceAssemblyEngine
---

# CADTraceAssemblyEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADTraceAssemblyEngine.ts` (530 LOC)

## What it is
U-AI-10 (CAD-COMPLETE-MS0 / PHASE-47)

```
CADTraceAssemblyEngine — U-AI-10 (CAD-COMPLETE-MS0 / PHASE-47)

Assembles a flat OpenTelemetry span list into per-traceId end-to-end trace
views for the CAD agent's observability layer. PURE ANALYZER — it composes
the output of OpenTelemetryTracingEngine and records nothing of its own.

Given a flat span list (e.g. openTelemetryTracingEngine.getCompletedSpans()),
for each traceId it produces:
  - a span tree (forest — multi-root tolerated)
  - total wall-clock duration (max endTime - min startTime across the trace)
  - the critical path (root->leaf chain with the largest cumulative span time)
  - the slowest single span
  - an error rollup + overall status

Robustness: cyclic parent references, orphan spans (missing parent),
duplicate spanIds, in-progress spans (no endTime) and negative durations
(endTime < startTime) are all detected, repaired non-destructively, and
surfaced as warnings
```


## Exports
`TraceSpanStatus` (type), `TraceRollupStatus` (type), `TraceSpanInput` (interface), `TraceSpanNode` (interface), `CriticalPathStep` (interface), `TraceErrorSpan` (interface), `TraceView` (interface), `TraceAssemblyResult` (interface), `CADTraceAssemblyEngine` (class), `cadTraceAssemblyEngine` (const)

## Key methods
`fromOtelSpans`, `assemble`, `assembleTrace`, `dfs`

## Depends on
[[reference_code_engine_opentelemetrytracingengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADTraceAssemblyEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADTraceAssemblyEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADTraceAssemblyEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
