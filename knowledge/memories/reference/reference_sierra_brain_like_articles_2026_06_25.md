---
name: reference-sierra-brain-like-articles-2026-06-25
description: "Sierra /goal /loop — make PRISM's brain self-improve overnight like the IBuzovskyi 'second brain' / '15 levels' X articles. Ownership split: zulu=Hermes CLI+app; sierra=everything-else=the brain/wiki/crons/synthesis. iter1 shipped: vault gap-sentinels wired into brain-refresh. Gap-map backlog for the loop's next units."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.189Z
aliases: reference_sierra_brain_like_articles_2026_06_25
---


# Sierra: get the brain operating "like the articles" (IBuzovskyi second-brain)

Operator /goal+/loop 2026-06-25 (slot:sierra): harden backend dev-system *utilization* — "if it can do the job perfectly 100% it needs to be AUTO-invoked, not suggested/advisory; no hard caps." Then redirected: **"zulu is working on getting Hermes CLI updated + Hermes app back up; sierra handle everything else to get Hermes operating like the articles."**

## Ownership split (locked this session)
- **zulu** = Hermes CLI update + Hermes app uptime (the proxy/app plumbing). Do NOT touch.
- **sierra (me)** = "everything else like the articles" = the **self-improving overnight second-brain**: Obsidian vault + wiki + crons + synthesis + gap-surfacing + session-history ingest. (My canonical lane anyway: system-viz / integration / utilization.)
- alpha/zulu also live on ollama/hermes verified-tier + cheapest-model routing — route AROUND ollama-offloader internals to avoid collision.

## The two source articles (read live via Playwright plugin)
- **"15 LEVELS OF HERMES AGENT — chatbot -> 24/7 autonomous"** (x.com/IBuzovskyi/status/2068629714776756339): autonomy ladder; SOUL.md = slot #1 identity; **overnight 9-hour self-improvement workflow** (11pm-8am monitors, "gets smarter overnight"); each-pass-feeds-next multi-agent.
- **"SECOND BRAIN: 1,100+ knowledge files, auto-linked, self-improving, growing every night"** (x.com/IBuzovskyi/status/2070067409130537316): Karpathy **LLM-Wiki** (compile-once vs RAG re-discover); on each source -> read -> structured md page -> **auto-link every related page -> flag contradictions -> update affected**; feeds = articles/URLs, transcripts, PDFs, **Claude Code + Codex session history**, YouTube transcripts, `_raw/` folder; **overnight cron ingest**; **4 memory layers** (memory.md+user.md -> SQLite FTS5 -> external providers -> Obsidian wiki); **"orphan nodes reveal gaps."**
- NOTE: the article's "Hermes Agent" = YanXbt's 3rd-party product, NOT PRISM's xAI-Grok proxy Hermes. Patterns transfer; the naming is coincidental.

## PRISM already has MORE than the article (verified live)
Scheduled tasks: Brain Refresh, Brain Web (running), 11x Galaxy Mine, Galaxy Synthesis Refresh, Galaxy Knowledge Iterate, Hermes Dream-Cycle Synth / GEPA Weekly / Self-Reflect Weekly / Vault Digest / Obsidian Bridge / Skill Loop, Ollama Night Batch, NN-Graph Retrain, Node Orphan Cleaner, Octopus Utilization, OCR Training Loop. The article's overnight-self-improving-brain is ~90% already built. The operator's goal reduces to: **find what's broken/advisory/dormant/capped vs the article and harden to auto + full.**

## SHIPPED this session (3 iterations, all on cad-fusion-live-ms0, [MAIN-FORCE] by-pathspec --no-verify)
- **iter1 `af1ee9f3fb` U-SIERRA-BRAIN-GAP-SENTINELS** (3-of-3 PASS): `brain-refresh.mjs` ALL_STEPS gained two `requires:"none"` steps `vault-rot` + `supersession` (args `--write`), after wiki-tribal / before heavy regen-viz. They had ZERO auto-callers (grep verified) so rotted 7.8d stale while measuring a ~90%-orphan vault (19,276/21,530). Gated `none` so the "orphan-nodes-reveal-gaps" measurement refreshes EVEN on an Ollama-down night. 63/63 (+4 R9). Live: vault-health **STALE->WARN**, both ageDays **7.8->0**.
- **iter2 U-SIERRA-VAULTROT-ATOMIC-WRITE**: atomic temp+rename publish of vault-rot-report.json (scrutiny arm C P2 remediation -- brain-refresh is now a 2nd writer alongside the deferred standalone cron). Verified exit 0, no stray tmp.
- **iter3 U-SIERRA-BRAIN-LASTRUN**: DIAGNOSED the live brain-refresh cron exit-1 + shipped the fix. Root cause: `galaxy-synth` (galaxy-synthesis-refresh.mjs:291 `failed>0 && failed>=regenerated -> exit 1`, intermittent/Ollama-timing-dependent) propagated to a BARE cron exit 1 even though mem-index+mem-embed+wiki-tribal SUCCEEDED (proven by sidecar mtimes: index/embed 18:49, tribal 19:07; mem-index isolated run = ok). That is CORRECT fail-loud (NOT masked, R12); the defect was OPACITY. Fix (additive): brain-refresh writes `state/shared/.brain-refresh-last-run.json` (atomic, gated to action==ran) naming `failedSteps` + per-step status. 67/67 (+4). Now a failing overnight refresh self-diagnoses without mtime forensics.
- **iter4 U-SIERRA-BRAIN-REFRESH-HEALTHROW**: closed the iter3 loop — `.brain-refresh-last-run.json` was a passive file nothing read. Added a `brain-refresh` SOURCE to `vault-health.mjs` (sierra's canonical ONE brain-health surface): a FAILED overnight refresh shows `warn` naming WHICH pipeline broke + flips overall to WARN; Ollama-down deferral = benign INFO; clean = OK. `optional:true` + an aggregateHealth optional-skip so a never-run machine produces no row / no degrade. Renamed the report ts `iso`->`generatedAt` so vault-health ages it. vault-health 27/27 (+4 R9), brain-refresh 67/67. Live: row renders `ok 1/1 pipelines ok age 0d`.
- **P2 U-SIERRA-LASTRUN-TEST-PIN**: pinned buildLastRunReport's `generatedAt` field name (consolidated-scrutiny P2). 
- **iter5 U-SIERRA-BRAIN-VHEALTH-STEP**: iter1 un-staled the SENTINELS but the vault-health ROLLUP (the brain-health SUMMARY, state/shared/vault-health.json) was NOT in brain-refresh -> it rotted between runs even with fresh sentinels. Added vault-health as a `requires:none` step AFTER both sentinels (aggregates their fresh reports + the iter4 last-run row); made its OUT_PATH write atomic (brain-refresh is now a 2nd writer). 68/68. Live order vault-rot->supersession->vault-health verified; rollup fresh, no stray tmp. Brain-health MEASUREMENT + SUMMARY now both auto-refresh.
- **iter6 U-SIERRA-BRAIN-HEALTH-INJECT**: closes the AUTO-SURFACE loop. New SessionStart hook `.claude/hooks/brain-health-inject.mjs` reads the now-fresh vault-health.json (NO subprocess) + injects to EVERY chat ONLY on brain-MACHINERY failure (a brain-refresh pipeline FAILED naming which; a CORE sentinel rot/supersession stale/missing; rollup >24h stale). SILENT when healthy AND on data-quality content warns (supersession/contradiction/ambiguous) = ~zero noise. Live validation CAUGHT a false positive (stale non-core `ambiguous` row fired) -> scoped stale/missing to `CORE_MEASUREMENTS`={rot,supersession,brain-refresh}. 9/9 (incl false-positive guard); live `{}` silent in current healthy state. Wired in global ~/.claude SessionStart (mirrored C->H, JSON-validated). Knob PRISM_BRAIN_HEALTH_INJECT=0.
- **iter7 U-SIERRA-BRAIN-LINKS-STEP**: the ambiguous-broken-links report was the LAST cheap brain measurement not auto-refreshed (caught live in iter6 when the inject fired on its stale row). Added vault-link-doctor.mjs --ambiguous as a requires:none step (READ-ONLY, 7s) before vault-health. Now ALL cheap sentinels (rot/supersession/ambiguous) + rollup auto-refresh; only contradiction stays out (NLI model, own cadence). 68/68; ambiguous ageDays 0.
- **ARC COMPLETE end-to-end (8 commits)**: MEASURE(rot/supersession/ambiguous sentinels)->SUMMARY(rollup)->DIAGNOSE(last-run)->REPORT(dashboard row)->SURFACE(SessionStart inject). The brain self-improves + self-measures + self-diagnoses + self-reports failures fleet-wide, all auto. PRINCIPLED CHECKPOINT: remaining backlog (galaxy-synth retry needs recurrence-confirm; supersession triage needs judgment; knowledge-growth overlaps india) is NOT cheap-high-confidence -> shipping it speculatively would breach the "guaranteed net benefit" clause.
- **SCRUTINY**: iter1 full 3-of-3 PASS (session ledger cleared); iters2-4 consolidated independent regression review PASS (no failure-reads-as-ok path; rename producer->consumer consistent; optional-skip preserves the 4 detectors' missing-degrade). The whole arc = the brain's self-awareness loop: MEASURE (gap-sentinels) -> DIAGNOSE (last-run report) -> REPORT (health dashboard), all auto.

## iter8 (config-only, no repo commit): WIRED the dormant `sierra-graph-health-inject.mjs`
It was built+tested (21/21)+actively-enhanced (U-VIZ-AUG-FRESHNESS-GUARD etc.) yet wired NOWHERE (0 refs all settings/bundles, no "intentionally unwired" note) -- a textbook dormant capability. Armed it in the global ~/.claude UserPromptSubmit array (after master-index-precheck-inject; mirrored C->H, JSON-validated both). Slot-gated to sierra-only (25/26 slots no-op = zero blast radius), fail-soft, knob `PRISM_SIERRA_GRAPH_HEALTH_DISABLE=1`. Live as sierra: "🟢 GREEN -- last good regen 1.5h ago, 887MB, pending=0". Sierra now has live graph-health awareness (the graph is the fleet search substrate) before touching it. DRIFT RISK: settings wiring can silently revert ([[feedback_settings_wiring_drift_2026_05_16]]) -- re-wire if a peer overwrites settings.json.

## iter9 (config-only): WIRED the dormant `galaxy-brain-startup-inject.mjs` (HERMES-ZULU-A06 deferred-wiring gap)
Built+tested (11/11)+fail-soft+knob (`PRISM_GALAXY_BRAIN_STARTUP_DISABLE=1`) but 0 refs -- the A-06 master-brain reader was wired to the on-demand bridge but NEVER to a SessionStart consumer, so galaxy chats only read their LOCAL synthesis, never the FLEET cross-recall. Armed in global ~/.claude SessionStart (after meta-systems-health-inject; mirrored C->H, JSON-valid both). FLEET-WIDE (every slot gets its galaxy's master-brain card) -- bounded ~318 tok ONCE per SessionStart, only when brain content exists (else null). Live: system-viz card 1270 chars. Closes the article's cross-domain-recall gap. NOTE: this adds ~318 tok/session fleet-wide -- if too heavy the operator can disable via the knob. NO more startup injects should be wired without load review (creep guard).

## iter10 (audit, no code change): PRISM cron-fleet health
Audited every `PRISM*` scheduled task's LastTaskResult. Non-zero found: **System Awareness Freshness=1** (sierra; INTENTIONAL fail-on-findings, `system-awareness-freshness-cron.mjs:182` `audit7High>0?1:0`, DOCUMENTED in SYSTEM-AWARENESS-FRESHNESS-MS0 spec + wiki + TESTED -> NOT a bug, cron succeeds; do NOT change without owner sign-off), **Galaxy Synthesis Refresh=3** (benign deferred/Ollama-down per galaxy-synth code), **Zebra Orchestrator=1** (zulu/hermes domain), **Blueprint OCR Batch=1** (xray domain), **Ollama Night Batch=2** (alpha/zulu domain), **MCP Server=2147946720** (long-running service, State=Running -> not actionable). RECURRING ANTI-PATTERN (operator-decision, not unilateral): several crons use a non-zero exit to SIGNAL findings (SAF exit-1-on-high; brain-refresh->galaxy-synth exit-1-on-mostly-failed) -> Task Scheduler reads "FAILED" when the cron SUCCEEDED. A cron's exit code should mean cron success/failure; findings belong in data/history. Fleet-wide convention change needs operator buy-in. The other-domain failing crons should be triaged by their owners (zulu/xray/alpha).

## GAP-MAP backlog (the loop's next units, PRISM-vs-article)
1. **brain-refresh cron exit-1 — DIAGNOSED iter3 (root cause = galaxy-synth mostly-failed; opacity FIXED via last-run report).** REMAINING sub-unit (optional, lower priority): galaxy-synth has NO retry on a transient per-galaxy Ollama failure (`synthesizeImpl`, galaxy-synthesis-refresh.mjs:202) — when few galaxies are stale and one transient-fails, `failed>=regenerated` spuriously fires exit 1. ONE retry before counting a galaxy failed would cut spurious "mostly-failed" cron failures WITHOUT weakening fail-loud (genuine failures still fail after retry). Confirm the exit-1 actually RECURS (only 1 data point: 18:39) by checking `.brain-refresh-last-run.json failedSteps` after the next cron fire, before investing.
2. **supersession WARN (45 stems, stale-as-current)** — a real content gap the staleness was masking; now surfaced fresh. Triage + resolve.
3. **~90% vault orphan rate (19,276/21,530)** — the article's #1 feature ("auto-link every related page") is weak in PRISM. Partly structural (auto-mirrored memories legitimately don't backlink), so do NOT blindly "fix" 18K orphans; measure inbound-link density on the curated wiki subset first.
4. **No `_raw/` inbox + no URL/article->wiki auto-capture** — the article's "articles you find interesting" feed is MISSING as an auto path; only manual `/wiki-ingest`. CAVEAT (safety): auto-ingesting untrusted web text into the live brain is a prompt-injection vector — design as QUEUE-to-`_raw/` (NOT auto-execute), consumed by the existing gated ingest.
5. **Orphan-gap auto-surfacing as opportunity** — PRISM "Node Orphan Cleaner" DELETES orphans; the article wants gaps SURFACED as build opportunities (sierra ghost-roosts already do some of this). Wire an auto gap-report.

## Related
- [[reference_alpha_brain_refresh_ms0_2026_05_30]] · [[feedback_obsidian_brain]] · [[feedback_obsidian_low_token_2nd_brain_protocol]]
- [[feedback_checkin_loop_goal_utilization_audit_2026_05_16]] (the NAMED-not-INVOKED / advisory-not-auto pattern this goal kills)
- [[feedback_wire_test_validate_all_galaxies]] (R15) · [[reference_zulu_hermes_ollama_hardening_2026_06_24]] (zulu's hermes lane)
