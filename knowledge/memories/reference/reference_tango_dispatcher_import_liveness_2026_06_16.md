---
name: reference_tango_dispatcher_import_liveness_2026_06_16
description: tango built scripts/dispatcher-import-liveness.mjs (18/18, 2-of-2 scrutiny PASS) which found a CONFIRMED P0 -- algorithmGatewayEngine unexported -> ~40 prism_algorithm actions runtime-dead since 2026-04-23. COMMIT BLOCKED: C: temp disk FULL (ENOSPC, all Bash dead). slot tango 2026-06-16.
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.218Z
aliases: reference_tango_dispatcher_import_liveness_2026_06_16
---


**TANGO U-DISPATCHER-IMPORT-LIVENESS (slot tango, 2026-06-16)** — built a standing audit that catches a bug class no existing tool covers: a dispatcher importing a symbol its target module does NOT export.

**BUILT (2 files, 18/18 node:test, 2-of-2 per-file scrutiny PASS after FAIL->fix->re-review):**
- `scripts/dispatcher-import-liveness.mjs` (+ `.test.mjs`). Scans all 107 dispatchers; per named import (lazy `const {X}=await import` + static `import {X} from`), checks the IMPORTED (export-side) name against the target module's real exports; classifies LIVE / DEAD / INDETERMINATE. False-positive-safe: DEAD only when target readable + name not exported + no `export *` wildcard + local binding used. Tracks `{imported, local}` separately (alias-correct). CLI `--json --indeterminate --dir`.
- **COMPLEMENTS, not duplicates** `.claude/hooks/dispatcher-import-validator.mjs` (golf-owned hook) which checks only FILE existence (`fs.existsSync(EngineName.ts)`), NOT export-name liveness -- the exact gap that hid the P0. "existence != content" in code form.

**CONFIRMED P0 (true positive -> surface to owner india/algorithm-dispatcher, NOT tango/romeo lane):** `algorithmDispatcher.ts:195` lazy-imports `const { algorithmGatewayEngine } = await import("../../engines/AlgorithmGatewayEngine.js")` and calls `algorithmGatewayEngine.<41 methods>` (executeFFT/kalmanFilter/dijkstra/...). But `AlgorithmGatewayEngine.ts` exports the FUNCTION `algorithmGateway` (line 1701) + standalone fns -- NO `algorithmGatewayEngine` object (grep-empty whole-tree). The binding is `undefined` at runtime -> ~40 prism_algorithm actions throw `Cannot read properties of undefined`. PROVEN: `src/__tests__/algorithmDispatcher.test.ts` is 13/14 RED (`TypeError ... reading 'executeFFT'`), stable since the 2026-04-23 `U-EFF23` Box-restore (~2 months undetected). Fix options for owner: (a) implement+export an `algorithmGatewayEngine` object delegating to the canonical `src/algorithms/` classes, or (b) rewire the 41 actions to lazy-import the canonical classes directly (the control_fuzzy/ml_* pattern). LARGE (41 methods) -- multi-unit, owner-coordinated.

**R12 SELF-CORRECTION (scrutiny caught MY error):** first scan reported 4 DEAD; I claimed all 4 "verified-on-disk". WRONG -- 3 of them (`findDaishowaHolders`/`recommendDaishowaHolder`/`getWorkholdingSummary` in dataDispatcher.ts) were FALSE POSITIVES: the dispatcher imports them via `as` ALIAS (`findHolders as findDaishowaHolders` etc.), and the targets DO export the originals (`findHolders`/`recommendHolder`/`getCatalogSummary`). I grepped the target exports but missed the import-side alias. The tool's bug: it conflated the LOCAL alias with the EXPORTED name. BOTH per-file reviewers FAILed it (P0). Fixed: `splitImportBindings` now returns `{imported, local}`; liveness checks `imported`, usage checks `local`. Added alias->LIVE fail-on-revert oracle + real-tree negative assertion. Post-fix scan: exactly 1 DEAD (algorithmGatewayEngine), 3 false positives gone. Re-review PASS, 0 findings. **LESSON: a dead-import finding is a HYPOTHESIS until the IMPORT STATEMENT (incl. alias form) is read, not just the target's exports.** This is why the per-file scrutiny gate exists -- it caught a 1-true/3-false meta-tool before ship.

**COMMIT BLOCKED (R12 honest):** `git commit` could NOT run -- `C:\Users\wompu\AppData\Local\Temp` is FULL (ENOSPC, 0 bytes). The harness writes every Bash/PowerShell output to that volume, so ALL shell commands fail fleet-wide (chicken-and-egg: can't free disk without a shell). The 2 files are safe uncommitted in the H: working tree. RETRY when disk freed: `git add scripts/dispatcher-import-liveness.mjs scripts/dispatcher-import-liveness.test.mjs && git commit -m '[MAIN-FORCE] [DISCOVERY-EFFICIENCY]/U-DISPATCHER-IMPORT-LIVENESS ...' -- <those 2 files>` on cad-fusion-live-ms0. Whether the first attempt's `git commit` partially ran is UNKNOWN (output capture ENOSPC'd) -- verify `git log -1` after disk recovery before re-committing (avoid double commit).

**FLEET BLOCKER surfaced -> golf/operator:** C: temp disk full is acute + fleet-wide (blocks all chats' Bash). cleanup-orchestrator / fleet-reaper should sweep `C:\Users\wompu\AppData\Local\Temp\claude\**\tasks\*.output` (this long session alone accumulated hundreds). Also host RAM pressure (hooks xmalloc "cannot allocate 32 bytes"). Sister: [[reference_tango_algo_fuzzy_wire_2026_06_15]] (prior iter, the control_fuzzy wire that bypasses the broken gateway). [[reference_fleet_memory_monitor_2026_05_16]].
