---
name: reference_code_engine_cadsequencepoolengine
description: "CAD-DRAW-MAX-MS0/P1-U05"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADSequencePoolEngine.ts
  sourceHash: 9a3c6e62
  loc: 230
aliases: CADSequencePoolEngine
---

# CADSequencePoolEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADSequencePoolEngine.ts` (230 LOC)

## What it is
CAD-DRAW-MAX-MS0/P1-U05

```
CADSequencePoolEngine — CAD-DRAW-MAX-MS0/P1-U05

Position-aware pooling tier for the CAD encoder stack. NN01
(CADFoundationEncoderEngine) today mean-pools per-token embeddings,
which loses op order. For a 200-op impeller build the AI can't tell
"extrude-then-fillet" from "fillet-then-extrude" — but those produce
different parts. This engine introduces 4 position-aware pooling
strategies that callers can stack on top of NN01's token rows + the
P1-U04 arg rows.

**Strategies.** All are deterministic — no learned weights at this
layer. LP04 owns learning; the pool is a fixed transform.
  - `mean`        — baseline (matches NN01).
  - `max`         — per-dim max-pool. Preserves "strongest signal"
                    across the sequence; good for catching feature
                    presence ("did the AI ever extrude?") but blind to
                    count.
  - `last`        — return the fi
```


## Exports
`POOL_STRATEGIES` (const), `PoolStrategy` (type), `DEFAULT_EXP_DECAY_ALPHA` (const), `PoolOptions` (interface), `PoolStats` (interface), `CADSequencePoolEngine` (class), `cadSequencePoolEngine` (const)

## Key methods
`pool`, `poolAll`, `getStats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CADSequencePoolEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADSequencePoolEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADSequencePoolEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
