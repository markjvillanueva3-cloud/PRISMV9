---
name: reference_mill_course_plotting_substrate_2026_05_31
description: "Mill autonomous course-plotting substrate map + the lineage_id closed-loop join unit (foxtrot, 2026-05-31). What to REUSE before building course-plotting/closed-loop work."
type: reference
source: prism-memory
synced: 2026-06-09T14:54:09.214Z
aliases: reference_mill_course_plotting_substrate_2026_05_31
---


# Mill autonomous course-plotting substrate (foxtrot /goal /loop, 2026-05-31)

Goal: "autonomously plot courses to successful steps producing a milled part from print; then live-test the closed-loop self-learning AI." Recon (5-agent workflow) finding: **course plotting is ALREADY autonomous** — the gap was closed-loop *identity*, now shipped.

## REUSE these (do NOT rebuild — duplication guard)
- **`MillingPrintToProgramEngine.runFullPipeline(MillingInput)`** (`mcp-server/src/engines/`, ~2165 lines) — the autonomous mill course plotter: print/features → intake → classify → process-plan → tool/strategy → toolpath → G-code → safety/confidence scoring. Wired: `prism_mill mill_print_to_program` / `mill_generate_gcode` (via `.calculate("milling_print_to_program", params)`).
- **`PrintToProgramPipelineEngine`** — ultimate orchestrator (drawing intake → … → validation, setup sheet, confidence).
- Process planning: `ProcessPlanEngine` (single-setup), `OperationSequencerEngine` (topo-sort, thermal, tool-change min), `IntelligentSequencingEngine` (33-rule datum/phase/TSP/rough-before-finish), `RoutingSheetGeneratorEngine`, `AutoProgramOrchestratorEngine` (10-stage Fusion one-button).
- GOAP/search/feasibility (generic, reusable): `STRIPSPlannerEngine`, `FeasibilityOrchestratorEngine` (WorkpieceState→Accessibility→Workholding→Rigidity→SequenceFeasibility — `prism_feasibility`), `AutonomousAIOrchestrationEngine`, `AutonomousGoalSynthesisEngine`, `CAMStrategyRecommenderEngine`. Gates: `prism_atcs` (S(x)≥0.70, Ω≥0.70), `prism_validate`.
- Closed-loop ledger (REUSE): `OutcomeCaptureBusEngine` (`outcomeCaptureBusEngine.record()`/`.query({domain,kind,lineage_id,agent_id,since_iso,limit})`, atomic JSONL per `state/outcomes/{domain}.jsonl`, WIRE-EXEMPT producer surface). Schema `OutcomeEventSchema v1.1.0` (`outcomeEventSchema.ts`) ALREADY has `lineage_id`. Helper `emitP2POutcome()` (`utils/p2pOutcomeEmission.ts`) — single emission chokepoint for the 6 P2P engines; accepts `lineageId?`/`pipelineRunId?`/`jobId?`. `OutcomeRLBridge → RL kernels → PSN → GraphSAGE` consume pairs.

## SHIPPED this session
- **U-MILL-READINESS-GATE** — `result.{autonomous_execution_ready, readiness_reasons[]}` on the plotter: the safety gate the live-test harness checks before AUTO-running a course unattended. Blocks: hasCritical, safety-critical `"warn"` rules (`power_limit`/`finish_deflection`/`spindle_speed_limit`), chatter unstable/unverifiable, confidence<0.85 (NaN-guarded), needs_multi_setup, uncovered_features, empty program. **Gate-side, NOT pipeline-side** (attended workflow unaffected — program still generated). Adversarially reviewed via an ultracode 3-reviewer Workflow that caught + I fixed real **P0 power/deflection auto-execute bypass** (`"warn"` ≠ blocked by hasCritical) + **chatter `checkStability` silentCatch** (engines.md/R12 — now records `stable:false`) + uncovered-features. **GOTCHA: OutcomeCaptureBus shared-JSONL has a Windows multi-writer race** (concurrent writers EBUSY/lose events) → ledger-readback tests flake; assert the in-memory contract (lineage_id key + published) instead; flagged juliett. Reliable join proof = in-memory RL e2e.
- **U-MILL-COURSE-LINEAGE** — `MillingPrintToProgramEngine` now generates one `courseLineageId=randomUUID()` per `runFullPipeline` run, returns it on `MillingProgramResult.{lineage_id,pipeline_run_id}`, and stamps it on the existing `emitP2POutcome(...)`. Recommended course (`source:system`) + later ACTUAL outcome (reusing same `lineage_id`, `source:controller/operator`) now JOIN via `outcomeCaptureBusEngine.query({domain:"mill",lineage_id})`. Was amnesiac (throwaway UUID never returned). +9 tests (8 engine + 1 dispatcher round-trip). Branch `slot/foxtrot`.
- **U-MILL-MULTISETUP-DETECT** — `runFullPipeline` now consults `MultiSetupPlannerEngine` (via `analyzeSetups`: MillingFeature→access-vector from index_A/B, `plan()` visibility-cone partition) and surfaces `result.{needs_multi_setup,setup_count,setup_plan}` + a `warning`-severity MULTI-SETUP notice — no more silent single-setup courses for multi-setup parts. Fail-soft enrichment (only `severity:critical` blanks G-code). +5 tests, MILLING 85/85. DETECTION only — full per-setup course GEN deferred.
- **U-MILL-CLOSED-LOOP** — `MillCourseClosedLoopEngine.closeLoop({lineage_id, recommended, actual})`: reconciles recommended↔actual into per-metric deltas, records the actual to the ledger (lineage-tagged join), and **publishes the bus envelope `{record, outcomeKind}` to `feedbackBusEngine` `"outcome.completed"`** → the already-built `OutcomeRLBridgeEngine` consumes it (Q-learning/policy/bandit update). **CLOSES THE LOOP.** Maps actual→terminal kind (success/failure/operator_override) so the RL bridge PROCESSES (not skips-pending). Wired `prism_mill:mill_close_loop` (95→96). e2e test proves the loop closes (RL `total_events_seen`+`total_processed` increment). +8 tests. **CRITICAL GOTCHA: OutcomeRLBridge.handleOutcomeCompleted reads `env.record` — publish `{record, outcomeKind}`, NOT the bare record** (the bare-record bug cost one test cycle). Producer (U-MILL-COURSE-LINEAGE) + consumer now wired+tested TOGETHER → in-process closed loop is live-testable.
- **U-BRIDGE-KIENZLE-CANONICAL** + **U-MILL-SWISS-KIENZLE-GUARD** (earlier same session) — fixed fleet-wide NaN-force leak: `PipelineRegistryBridge.materialPhysicsToContext` emitted `kc1_1=undefined` for iso-default/canonical-db resolutions (MaterialEntry has no per-material kc1_1/mc — they're canonical-per-ISO). Now derives from `CANONICAL_KIENZLE`. `MillTurnSwissPipelineEngine._getKienzle` got iso-match+finite guard (shared-singleton cross-test contamination → NaN). See [[reference_mill_test_inventory_2026_05_30]].

## NEXT gaps (precise — for resuming the loop)
1. **Full multi-setup course GEN (foxtrot):** DETECTION shipped (U-MILL-MULTISETUP-DETECT — `runFullPipeline` flags `needs_multi_setup` via `analyzeSetups`). REMAINING: when multi-setup, generate a per-setup process plan + per-setup G-code + inter-setup datum-transfer/re-fixturing (today still emits ONE single-setup program even when flagged). Reuse `setup_plan.setups[]` + `IntelligentSequencingEngine` + `RoutingSheetGeneratorEngine`. Larger core-pipeline build.
2. **P0-U04 (india):** FeedbackBus → Tier-1-trainer subscriber (CrossProcessNeuralLearningEngine / BayesianCalibrationEngine / CAMLoRAAdapterTrainerEngine) — consumes the now-joinable pairs. Routed via chat-bus.
3. **Actual-side tagging (shop-floor):** MTConnect/operator outcome path must reuse `result.lineage_id` to close the loop in production.
4. **Raw-print intake (delta/xray):** `print→feature_recognize→process_plan` facade actions throw NotWiredError; `runFullPipeline` needs PRE-classified features. CADFeatureRecognitionEngine delegation for raw geometry → breadth, not autonomy blocker.

## ARCHITECTURE DETERMINATION (2026-05-31, 8-dim workflow + adversarial critique) — `state/shared/MILL-PIPELINE-ARCHITECTURE-DETERMINATION-2026-05-31.md` (commit 53b16caa38)
Whole-mill pipeline/SFC/post/CAD/CAM/AI determination. Shape is RIGHT + mostly built: ONE adaptive spine varied by substitution at 6 plug points, NOT N hardcoded pipelines. **5 pipelines:** P1 (3-axis spine `MillingPrintToProgramEngine`), **P1-5X native 5-axis IS WIRED** (`MultiAxisPrintToProgramEngine` + `FiveAxisToolpathIntegrationEngine` — CAM seat is ESCALATION not the 5-axis default; the synthesis's "must drive a seat for 5-axis" was WRONG, critic-corrected), P2 multi-setup (detect shipped, GEN = the in-domain build), P3 drive-the-seat (escalation), P4 deterministic template/macro (`GCodeTemplateEngine` 6ctrl×13op + `CNCProgramAssemblerEngine`).
**P0 BLOCKER = CAD intake:** no real raw-geom→typed `MillingFeature[]`; `CADFeatureRecognitionEngine` is a STUB, `/upload` serves hardcoded demo features (R12). Fix = extend `STEPGeometryParserEngine` + wire `prism_cad:feature_recognize_from_step` (delta+foxtrot, units-first INCH).
**Plug-and-play toolpath:** make `StrategyTaxonomyEngine` the canonical registry — today 4 competing vocabularies (`MillingStrategyLibraryEngine`/`CAMStrategyRecommenderEngine`/`ToolpathStrategyRouterEngine` 8-inline/dispatcher-stub) block free swap. 2 live defects: millDispatcher L94 points at a ToolpathStrategy STUB; Router hardcodes 8 strategies inline.
**AI (india) = EMIT-heavy/CONSUME-light:** closed-loop RL LIVE + disk-confirmed (`PRISM_XPROC_AUTOFIRE` default-on) but plotter ACTS on only 1 plan-time consult (SFC NN gate). Available-but-UNUSED: 3 LoRA legs open both ends (Milling+MillTurn+FiveAxis builders feed NO trainer — `CAMLoRAAdapterTrainerEngine` is CAM-system not mill-domain), 5-axis AI cluster dark (`FiveAxisDeepLearning/AIUltra/Decision`), `ToolLifeGnnEngine` unwired, no conformal emission (no drift-canary), no GNN/RAG at plan time (mill invisible to india classifier, AUROC 0.096 heterophily). `MillStrategyNeuralEngine` is an island parallel-loop to retire/subordinate.
**Backlog R0-R12 (dep-ordered):** R0 units-guard → R1 fix 2 strategy defects → R2 canonical taxonomy → R5 real AFR (P0) → R6 multi-setup GEN → R12 close LoRA loop + plan-time consume (india+foxtrot). Cross-slot routed via AGENT_CHAT.md.
**4-AXIS (added 2026-05-31, commit 302b44947e):** distinct pipeline **P1-4X** — 3+1 indexed + continuous wrap. Core is **BUILT-but-ORPHANED**: `FourthAxisIndexingEngine` (`mode:positional`/`wrap`, tombstone 2/4/6-face, Haas TR specs) + `FourthAxisDecisionEngine` (50+ tests in `MILL-HARD-MS2.test.ts`, ZERO dispatcher wire). `MultiAxisPrintToProgramEngine` has NO 4-axis branch (its `machine_type` enum is 5-axis/3+2 only). Sub-track: **R1b** ✅ SHIPPED 2026-05-31 (commit 02b0e4a2c3 — chose dedicated actions over machine_type delegate: `fourth_axis_index`+`fourth_axis_decide` added to `prism_multiaxis_program`, 2→4 actions; engines call STATIC `.calculate()`/`.decide()` on the class; 16/16 round-trip tests in `multiAxisProgramDispatcher-4axis.test.ts`; barrel-export reviewer-FAIL overruled — `src/engines/index.ts` has only 2 exports, no engine incl sibling MultiAxisPrintToProgramEngine is barrel-exported) → **R3b** foxtrot `fk/ik4AxisTable` + R→0 cyl-singularity guard (NOT implemented; only 5ax trunnion exists) + oscar rotary-feed SFC `F_deg=F_lin·360/(2πR)`+inverse-time (absent) → **R7b** echo posts G93/G07.1/G12.1/M10-M11 (only generic `inverse_time_feed` flag exists) → **R11b** foxtrot new `RotaryProfileEngine` (cam/lobe) + cylindrical-unwrap engine. india: add `axis_count`/`rotary_mode` to `MillingLoRADatasetBuilder` fingerprint (axis-blind today). JM driver: VMC-01 Hurco + VMC-03 Haas+TR-rotary run 4-axis; tombstone indexing high-frequency.

MCP server was DOWN this session (port 3100) — worked via direct files. Tree discipline: foxtrot edits live in `H:/prism-slot-foxtrot` (branch `slot/foxtrot`), NOT `H:/PRISM` (main, `cad-fusion-live-ms0`) — run vitest with `cd /h/prism-slot-foxtrot/mcp-server`. Edit tool may write CRLF on LF files → normalize before commit (whole-file diff = flip; `git diff --ignore-cr-at-eol` shows true change). [[feedback_always_close_out]]
