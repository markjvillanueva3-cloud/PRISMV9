---
name: reference_code_engine_reasonbeforeactionengine
description: "Reason Before Action"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ReasonBeforeActionEngine.ts
  sourceHash: 22439953
  loc: 524
aliases: ReasonBeforeActionEngine
---

# ReasonBeforeActionEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ReasonBeforeActionEngine.ts` (524 LOC)

## What it is
Reason Before Action

```
ReasonBeforeActionEngine -- the "reason before any action" control-plane core.

PRISM's AI substrate (octopus consensus, Ollama-local panel, Claude) was, until
this engine, only ever invoked CALLER-EXPLICITLY. Nothing fired a multi-model
sanity check BEFORE a consequential action (a shell command, a file overwrite,
a git push). This engine closes that gap: given a proposed action it returns a
typed verdict (PROCEED | REVISE | BLOCK) by routing to the CHEAPEST sufficient
reasoner:

  kill-switch raised -> deterministic BLOCK (the human interrupt; no model call)
  low-risk           -> deterministic PROCEED, ZERO model calls (read/search ops)
  medium-risk        -> a cheap 2-model LOCAL Ollama vote ($0, ~1-2s)
  high-risk          -> a diverse LOCAL Ollama panel vote ($0; still local-only)

This is the ENGINE only (governance-neutral -- it computes a verdict, it does not
gate anything). T
```


## Exports
`RiskTier` (type), `RBAVerdict` (type), `REASON_GATE_INFLIGHT_ENV` (const), `LOCAL_LLM_VIA_MCP_ENV` (const), `REVIEW_AGREEMENT_FLOOR` (const), `BLOCK_CONFIRM_FLOOR` (const), `BLOCK_MIN_VOICES` (const), `MEDIUM_RISK_TIMEOUT_MS` (const), `HIGH_RISK_TIMEOUT_MS` (const), `VOTE_OPTIONS` (const), `RBA_VOTE_MAX_TOKENS` (const), `RBA_PRIMARY_OLLAMA_MODEL_DEFAULT` (const), `RBA_SECONDARY_OLLAMA_MODEL_DEFAULT` (const), `RBA_OLLAMA_MODEL_ENV` (const), `RBA_OLLAMA_MODEL_2_ENV` (const), `RBAPinnedModels` (interface), `rbaPinnedModels` (function), `RBAPlanInput` (interface), `RiskAssessment` (interface), `RBAPlanResult` (interface)

## Key methods
`isInflight`, `plan`

## Depends on
[[reference_code_engine_multimodelconsensusengine]] | [[reference_code_engine_corrigibilitygateengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/ReasonBeforeActionEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ReasonBeforeActionEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ReasonBeforeActionEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
