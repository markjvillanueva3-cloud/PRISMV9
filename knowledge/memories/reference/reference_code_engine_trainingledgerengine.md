---
name: reference_code_engine_trainingledgerengine
description: "Training Ledger"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/TrainingLedgerEngine.ts
  sourceHash: d7e23a76
  loc: 390
aliases: TrainingLedgerEngine
---

# TrainingLedgerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/TrainingLedgerEngine.ts` (390 LOC)

## What it is
Training Ledger

```
TrainingLedgerEngine (U-LPR-TRAINING-LEDGER, ML R1)

Append-only ledger of every LoRA / adapter training run. Pins the
dataset-manifest hash and augmentation seed *at run start* so that
four parallel experiments cannot silently drift apart.

Drift scenarios this prevents:
  1. Experiment A starts on manifest_hash=α. Someone adds files to
     data/jm-die/train/ before experiment B starts. B's manifest hash
     is β — the two are no longer comparable, but there's no audit
     record of why.
  2. Aug seed defaults to Date.now() → irreproducible runs → ablation
     results can't be re-created.
  3. Two experiments report identical eval metrics because they
     secretly share checkpoints (the same random init survived).

The ledger stores each run's immutable fingerprint:

  RunId  = djb2(experiment_id + attempt + start_ts)
  Fingerprint = {
    base_weight_sha256,       // frozen pre-tr
```


## Exports
`RunStatus` (type), `TrainingRunStart` (interface), `TrainingRunClose` (interface), `TrainingRunRecord` (interface), `DriftReport` (interface), `TrainingLedgerEngine` (class), `trainingLedgerEngine` (const)

## Key methods
`openRun`, `closeRun`, `getRun`, `listRuns`, `driftReport`, `toSnapshot`, `loadSnapshot`, `getStats`, `clearAll`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/TrainingLedgerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find TrainingLedgerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/TrainingLedgerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
