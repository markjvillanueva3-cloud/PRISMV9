---
name: reference_code_engine_kilofusionfileparserengine
description: "KiloFusionFileParserEngine — KILO-CAM-TRAINING-MS0 campaign iter 3"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/KiloFusionFileParserEngine.ts
  sourceHash: 926af02a
  loc: 204
aliases: kiloFusionFileParserEngine
---

# kiloFusionFileParserEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/KiloFusionFileParserEngine.ts` (204 LOC)

## What it is
```
KiloFusionFileParserEngine — KILO-CAM-TRAINING-MS0 campaign iter 3

Mode-2 file-system parser for `.f3d` / `.f2d` Fusion files. These are
actually ZIP containers — the design tree, sketches, features, and
(when CAM is set up) the operations + tool list are embedded as JSON
inside the ZIP. Parsing them OFFLINE (no Fusion 360 process, no
APS network call) is the fastest possible extraction path.

This engine is the PURE-FUNCTION classifier + JSON-detail extractor.
The actual ZIP read is performed by the caller (typically a CLI script
using `node:fs` + `node:zlib` or `unzipper`); this engine consumes the
already-extracted entry list + raw JSON payloads and produces the
normalized `FusionFileSummary` shape used by downstream training.

Why pure? Keeps the engine unit-testable without filesystem fakes and
lets the same parser run in (a) the CLI bench script, (b) the MCP
action that streams a 
```


## Exports
`ZipEntrySchema` (const), `ZipEntry` (type), `FusionFileInputSchema` (const), `FusionFileInput` (type), `FusionFeatureSummarySchema` (const), `FusionFeatureSummary` (type), `FusionCamOperationSchema` (const), `FusionCamOperation` (type), `FusionFileSummarySchema` (const), `FusionFileSummary` (type), `kiloFusionFileParserEngine` (const)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/KiloFusionFileParserEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find kiloFusionFileParserEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/KiloFusionFileParserEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
