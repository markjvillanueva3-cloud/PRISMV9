---
name: reference_code_engine_rfqbroadcastengine
description: "the CORE MARKETPLACE LOOP ENTRY of the PRISM manufacturing networking"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/RFQBroadcastEngine.ts
  sourceHash: 8d5d0110
  loc: 617
aliases: RFQBroadcastEngine
---

# RFQBroadcastEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/RFQBroadcastEngine.ts` (617 LOC)

## What it is
the CORE MARKETPLACE LOOP ENTRY of the PRISM manufacturing networking

```
RFQBroadcastEngine — the CORE MARKETPLACE LOOP ENTRY of the PRISM manufacturing networking
marketplace (galaxy:business, slot:hotel). It takes a buyer's RFQ, matches a supplier shortlist
(REUSING {@link RFQMatchScoringEngine}), BROADCASTS the RFQ to the matched suppliers by opening a
timed BID WINDOW, and COLLECTS the suppliers' sealed bids into that window.

This engine OWNS the storage for three marketplace aggregates:
  - {@link MarketplaceRFQ}  — the posted RFQ (keyed by rfqId),
  - {@link BidWindow}       — the open/closed/awarded/cancelled bidding window for an RFQ (keyed by rfqId),
  - {@link SupplierBid}     — each supplier's bid into a window (keyed by bidId).
The downstream {@link BidCollectionRankingEngine} (sibling unit) READS a CLOSED window's bids and
produces an Award + a logged outcome tuple — it does NOT own the bid/window storage; this does.

PIPELINE (broadcastRFQ → su
```


## Exports
`PartEnvelopeMm` (interface), `MarketplaceRFQ` (interface), `SupplierBid` (interface), `BidWindow` (interface), `BroadcastResult` (interface), `CloseWindowResult` (interface), `RFQ_BROADCAST_SCHEMA_VERSION` (const), `BroadcastInput` (type), `SubmitBidInput` (type), `UpdateBidInput` (type), `RFQBroadcastEngine` (class), `rfqBroadcastEngine` (const)

## Key methods
`broadcastRFQ`, `submitBid`, `updateBid`, `closeWindow`, `cancelWindow`, `getRFQ`, `getWindow`, `getBids`, `listOpenWindows`

## Depends on
[[reference_code_engine_rfqmatchscoringengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/RFQBroadcastEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find RFQBroadcastEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/RFQBroadcastEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
