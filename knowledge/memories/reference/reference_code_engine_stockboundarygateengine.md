---
name: reference_code_engine_stockboundarygateengine
description: "StockBoundaryGateEngine — U-LSR06 (LATHE-HARDENED-MS0)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/StockBoundaryGateEngine.ts
  sourceHash: 0b1fe4bc
  loc: 458
aliases: StockBoundaryBlockError
---

# StockBoundaryBlockError -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/StockBoundaryGateEngine.ts` (458 LOC)

## What it is
```
StockBoundaryGateEngine — U-LSR06 (LATHE-HARDENED-MS0)

Geometric boundary check between the toolpath and the raw stock / chuck
jaws. Produces a typed gate decision the emitter (U-LSR04 pattern) and the
formal predicate (U-LSR22) can consume as a hard-block.

── Conventions ─────────────────────────────────────────────────────────
Right-handed lathe, workpiece face at Z=0, positive Z points toward the
chuck/tailstock (i.e. into the machine). Stock spans Z ∈ [0, length_mm].
Gripping region spans Z ∈ [length_mm − gripping_length_mm, length_mm];
that is the region in which the chuck jaws physically contact the bar.

X is radial (NOT diameter). The spindle centreline is X=0. For a solid
bar the tool must never cross X=0. For a tube, ID side cuts use X in
[id/2, od/2]; OD side cuts use X ≥ id/2 but will typically live above
od/2 − depth during roughing.

── Clauses ───────────────────────────
```


## Exports
`StockGeometrySchema` (const), `StockGeometry` (type), `WorkholdingSchema` (const), `Workholding` (type), `StockBoundaryGateInputSchema` (const), `StockBoundaryGateInput` (type), `BoundaryClauseSchema` (const), `BoundaryClause` (type), `BoundaryViolationSchema` (const), `BoundaryViolation` (type), `StockBoundaryOpSummarySchema` (const), `StockBoundaryOpSummary` (type), `StockBoundaryGateResultSchema` (const), `StockBoundaryGateResult` (type), `StockBoundaryBlockError` (class), `stockBoundaryGateEngine` (const), `StockBoundaryGateEngine` (re-export)

## Key methods
`gate`, `gateOrThrow`

## Depends on
[[reference_code_engine_latheprinttoolpathgeneratorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/StockBoundaryGateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find StockBoundaryBlockError` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/StockBoundaryGateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
