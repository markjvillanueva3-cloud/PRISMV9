---
name: reference_code_engine_wedmlatticegraphengine
description: "Embedding lattice over the WEDM parameter space."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WEDMLatticeGraphEngine.ts
  sourceHash: be55fe7f
  loc: 804
aliases: WEDMLatticeGraphEngine
---

# WEDMLatticeGraphEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WEDMLatticeGraphEngine.ts` (804 LOC)

## What it is
Embedding lattice over the WEDM parameter space.

```
WEDMLatticeGraphEngine — Embedding lattice over the WEDM parameter space.

MS-P5-GNN / U-P5-GNN-01

Builds a graph whose nodes represent cells of the
(material × machine × wire × thickness × Ra_target) space relevant to Wire
EDM. Each node carries a deterministic 64-dim feature embedding derived from
its attributes and physics-motivated terms (Klocke specific energy, Carslaw
thermal diffusion length, normalized pulse envelope). Edges link cells that
share attribute bits or embed near one another.

Sources:
  - 60 published pulse conditions in `data/wedm-published-conditions.ts`
    (Klocke 2013 Tables 8.1–8.4, Ho & Newman 2003, manufacturer E-pack tables)
  - Optional history ingest from WEDM_JOB_HISTORY.json (post-P4)
  - Composed spanning cells across reduced axes so the lattice reaches the
    ≥300-node exit-gate requirement on a fresh install with no job history.

Embedding layout (a
```


## Exports
`LATTICE_MATERIALS` (const), `LATTICE_CONTROLLERS` (const), `LATTICE_WIRES` (const), `MATERIAL_TO_ISO` (const), `MaterialThermalProps` (interface), `MATERIAL_THERMAL` (const), `computeLatticeEmbedding` (function), `latticeNodeId` (function), `BuildOptions` (interface), `BuildResult` (interface), `WEDMLatticeGraphEngine` (class), `cosineSim` (function), `wedmLatticeGraphEngine` (const)

## Key methods
`formatWireSlug`, `formatThicknessSlug`, `formatRaSlug`, `build`, `pushNode`, `addEdge`, `load`, `snapshot`, `getNode`, `queryByAttrs`, `stats`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/WEDMLatticeGraphEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WEDMLatticeGraphEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WEDMLatticeGraphEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
