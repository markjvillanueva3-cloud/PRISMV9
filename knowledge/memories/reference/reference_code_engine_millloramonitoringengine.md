---
name: reference_code_engine_millloramonitoringengine
description: "MillLoRAMonitoringEngine — Real-time Deployment Monitor (Mill parity)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MillLoRAMonitoringEngine.ts
  sourceHash: 5e7c1f53
  loc: 434
aliases: millLoRAMonitoringEngine
---

# millLoRAMonitoringEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MillLoRAMonitoringEngine.ts` (434 LOC)

## What it is
```
MillLoRAMonitoringEngine — Real-time Deployment Monitor (Mill parity)
======================================================================

Real-time monitoring of deployed MillLoRA models. Tracks latency,
throughput, error rates, and MILL-CANONICAL physical-process drift
signals (chatter / FPA / TCPM).

Mill parity for LatheLoRAMonitoringEngine (LATHE-LORA-MS0 U-LLR49) with
MILL-CANONICAL extensions:

  - Per-request mill-canonical signals (optional per record):
      chatter_breach    — true if the cut triggered a chatter event
      fpa_failed        — true if first-piece-approval rejected the part
      tcpm_solver_fault — true if 5-axis Heidenhain/Fanuc transform failed
  - Per-deployment health adds mill-canonical rate fields with their
    own breach thresholds (config: chatter_rate_threshold,
    fpa_failure_threshold, tcpm_fault_threshold)
  - Mill-canonical alert metrics laye
```


## Exports
`MillAlertSeverity` (type), `MillHealthState` (type), `MillAxisMode` (type), `MILL_CANONICAL_ALERT_METRICS` (const), `MillCanonicalAlertMetric` (type), `MillRequestRecord` (interface), `MillAlert` (interface), `MillDeploymentHealth` (interface), `MillMonitoringConfig` (interface), `millLoRAMonitoringEngine` (const), `MillLoRAMonitoringEngine` (re-export)

## Key methods
`setConfig`, `getConfig`, `recordRequest`, `getRecordsInWindow`, `getHealth`, `getActiveAlerts`, `getAllAlerts`, `acknowledgeAlert`, `getMonitoredDeployments`, `getHealthDistributionByAxis`, `getStats`, `getSummary`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MillLoRAMonitoringEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find millLoRAMonitoringEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MillLoRAMonitoringEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
