---
name: reference_code_script_domaincorpustoloradataset
description: "domain-corpus-to-lora-dataset.mjs (slot:papa 2026-06-24)"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/domain-corpus-to-lora-dataset.mjs
  sourceHash: aba425b1
  loc: 410
aliases: domain-corpus-to-lora-dataset.mjs
---

# domain-corpus-to-lora-dataset.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/domain-corpus-to-lora-dataset.mjs` (410 LOC)

## What it is
```
domain-corpus-to-lora-dataset.mjs  (slot:papa 2026-06-24)

THE MISSING PLUMBING: turn the rescued per-domain knowledge (the resource-pdf specs
that reclassify-domain-feeders-ollama.mjs lifted out of the keyword-unclassified
residual) into ACTUAL LoRA training data.

getDomainCorpus + the per-domain tribal corpora are a POINTER/orchestration index --
each tip says "read the source PDF". This script closes the last leg: it reads the
real PDF text (pdftotext, the proven extractor batch-pdf-extract.mjs already uses) and
emits domain-tagged Alpaca {instruction, input, output} triples onto the SAME schema
the fleet LoRA builders use (vault-to-lora-dataset.mjs), so assemble-fleet-lora-corpus.mjs
folds them into the staged training corpus once registered in the inventory.

SOURCE = the override sidecar's high-confidence rescued slugs (domains[] + conf>=0.7).
Multi-label: a spec feeding N domains
```


## Exports
`ROOT` (const), `SCHEMA_VERSION` (const), `DEFAULT_OUT` (const), `SOURCE_TAG` (const), `MIN_TEXT_CHARS` (const), `MAX_OUTPUT_CHARS` (const), `ADVISORY_WEIGHT` (const), `MAX_DISTILL_ATTEMPTS` (const), `CONTROL_RE` (const), `OLLAMA_GEN` (const), `loadRescued` (function), `parseCursorState` (function), `parseCursorDoneSet` (function), `partitionByResumeCursor` (function), `partitionForDistill` (function), `snapshotRawBaselineBeforeTruncate` (function), `isUsableText` (function), `cleanText` (function), `synthInstruction` (function), `toAlpacaPairs` (function)

## Key methods
`writeImpl`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/domain-corpus-to-lora-dataset.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find domain-corpus-to-lora-dataset.mjs` or `prism_session:master_index_query`; open the source at `scripts/domain-corpus-to-lora-dataset.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
