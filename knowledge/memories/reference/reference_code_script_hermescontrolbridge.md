---
name: reference_code_script_hermescontrolbridge
description: "hermes-control-bridge.mjs -- PRISM programmatic control of the Nous Hermes"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/hermes-control-bridge.mjs
  sourceHash: 8e4e602e
  loc: 313
aliases: hermes-control-bridge.mjs
---

# hermes-control-bridge.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/hermes-control-bridge.mjs` (313 LOC)

## What it is
```
hermes-control-bridge.mjs -- PRISM programmatic control of the Nous Hermes
desktop app's backend, WITHOUT the (broken, restart-looping) Electron renderer.
(HERMES-CONTROL-MS0/U-BRIDGE-CORE, slot:zulu 2026-06-18.)

The Hermes desktop is an Electron UI + a FastAPI Python backend
(hermes_cli/web_server.py). The renderer is stuck in a /api/ws -> bootstrap:reset
restart loop (vendored UI bug), but the BACKEND boots fine and exposes ~40 REST
routes -- "every button and function". This bridge drives that REST surface
directly: it launches a HEADLESS backend on a PINNED port (default 9119), reuses
one already up, and exposes get/set for config (real-time settings), model, env,
cron, mcp servers, sessions, status. It NEVER touches /api/ws or the
hermes:bootstrap:reset IPC (the loop triggers), so the broken GUI is irrelevant.

Spec + route map: state/shared/specs/HERMES-CONTROL-BRIDGE-SPEC-2026-06
```


## Exports
`httpCall` (function), `probe` (function), `extractInjectedDashboardToken` (function), `resolveServedToken` (function), `ensureBackend` (function), `call` (function), `getStatus` (const), `getConfig` (const), `deepMerge` (function), `setConfig` (function), `getModelOptions` (const), `setModel` (const), `listCron` (const), `listMcpServers` (const), `getEnv` (const)

## Key methods
`resolve`, `writeSidecar`, `log`, `done`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/hermes-control-bridge.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find hermes-control-bridge.mjs` or `prism_session:master_index_query`; open the source at `scripts/hermes-control-bridge.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
