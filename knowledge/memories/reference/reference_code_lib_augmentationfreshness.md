---
name: reference_code_lib_augmentationfreshness
description: "augmentation-freshness.mjs -- classify the freshness of every system-viz"
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/augmentation-freshness.mjs
  sourceHash: 9c3fbcb1
  loc: 234
aliases: augmentation-freshness.mjs
---

# augmentation-freshness.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/augmentation-freshness.mjs` (234 LOC)

## What it is
```
augmentation-freshness.mjs -- classify the freshness of every system-viz
augmentation file the MERGE actually folds into the live graph.

WHY (reference_augmentation_staleness_graph_fresh_inputs_stale_2026_06_21):
regen-viz re-merges system-graph.json and reports GREEN from whatever augmentation
files exist on disk. Two silent-staleness paths leave a GREEN graph on rotting inputs:
  (1) a FAST[] generator silently fails -> regen-viz logs `failed++` and CONTINUES
      (regen-viz.mjs:223-228, no abort) -> its augmentation freezes while the merge
      keeps folding the stale file.
  (2) a generator is RETIRED but its `loadOptional("X.json")` in merge-augmentations
      was never removed -> the orphan augmentation lingers (no producer) and its stale
      nodes/annotations keep folding into the canonical graph every regen.
Observed 2026-06-21: awareness / core-inventory / fs-inventory / f
```


## Exports
`SLOW_CADENCE` (const), `INTENTIONAL_NO_PRODUCER` (const), `FRESHNESS_DEFAULTS` (const), `freshnessThresholdsFromEnv` (function), `buildFreshnessReport` (function), `MERGE_STALE_SKIP_DEFAULT_HR` (const), `parseMergedAugmentations` (function), `classifyAugmentationFreshness` (function), `summarizeFreshness` (function), `shouldSkipStaleMerge` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/augmentation-freshness.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find augmentation-freshness.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/augmentation-freshness.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
