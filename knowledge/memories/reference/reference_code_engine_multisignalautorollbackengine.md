---
name: reference_code_engine_multisignalautorollbackengine
description: "MultiSignalAutoRollbackEngine (U-LPR-AUTOROLLBACK)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MultiSignalAutoRollbackEngine.ts
  sourceHash: e83a64eb
  loc: 491
aliases: MultiSignalAutoRollbackEngine
---

# MultiSignalAutoRollbackEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MultiSignalAutoRollbackEngine.ts` (491 LOC)

## What it is
MultiSignalAutoRollbackEngine (U-LPR-AUTOROLLBACK)

```
MultiSignalAutoRollbackEngine (U-LPR-AUTOROLLBACK)

Multi-signal auto-rollback for LoRA adapters and other hot-swappable
artifacts. Fires whenever ANY of the five trigger channels breaches,
completes the rollback in ≤ROLLBACK_SLA_MS, and records full evidence.

Trigger channels (from LATHE-PROD-READY-MS0 plan Phase-6 ML B3):

  1. EVT-POT/GPD threshold (primary — physics_validity is bimodal so
     a 2σ Gaussian is wrong). Fits a Generalised Pareto Distribution
     to the historical bad-epoch exceedances over a high threshold u,
     then triggers when the current tail probability exceeds the 99th
     percentile of that fit.
  2. Thumbs-down rate >15% over a trailing window of 20 programs.
  3. Error rate >5% over the current 5-minute window.
  4. Latency p95 >3000 ms over the current window.
  5. S(x) safety score <0.70 on any single request.

The GPD parameters (ξ shape, β scale, u t
```


## Exports
`RollbackTriggerKind` (type), `RollbackOutcome` (type), `RollbackTriggerConfig` (interface), `DEFAULT_TRIGGER_CONFIG` (const), `ProgramFeedback` (interface), `RollbackTrigger` (interface), `RollbackExecution` (interface), `GPDFit` (interface), `gpdSurvival` (function), `fitGPD` (function), `quantileR7` (function), `MultiSignalAutoRollbackEngine` (class), `multiSignalAutoRollbackEngine` (const)

## Key methods
`setConfig`, `getConfig`, `setFallback`, `getFallback`, `recordFeedback`, `evaluate`, `executeRollback`, `rearm`, `isLatched`, `listFeedback`, `listExecutions`, `getStats`, `clearAll`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MultiSignalAutoRollbackEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MultiSignalAutoRollbackEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MultiSignalAutoRollbackEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
