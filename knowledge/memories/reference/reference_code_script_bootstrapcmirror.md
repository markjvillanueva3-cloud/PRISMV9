---
name: reference_code_script_bootstrapcmirror
description: "bootstrap-c-mirror.mjs — REVERSE bootstrap of mirror-c-to-h.mjs."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/bootstrap-c-mirror.mjs
  sourceHash: a5adc8f1
  loc: 235
aliases: bootstrap-c-mirror.mjs
---

# bootstrap-c-mirror.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/bootstrap-c-mirror.mjs` (235 LOC)

## What it is
```
bootstrap-c-mirror.mjs — REVERSE bootstrap of mirror-c-to-h.mjs.

The C→H hook only captures FUTURE writes done from C:. When a fresh PC
(or a long-disconnected PC) needs to be brought into sync with what H:
already holds, this script walks H:\.claude\ and copies every file that
is MISSING or DRIFTED on C:\Users\<user>\.claude\.

Mirrors `bootstrap-h-mirror.mjs` symmetry; reuses the EXACT exclusion
list from `mirror-c-to-h.mjs` so a file the C→H hook would never push
up is also a file this script will never push down. SHA-256 byte-equal
short-circuit; symlinked C: entries (e.g. `commands -> /h/.claude/commands`)
are detected via lstat and skipped — they already point at H: directly.

Default mode is --dry-run. Pass --apply to actually copy.

Flags:
  --root <path>   Override C: profile root (default: ~/.claude)
  --apply         Actually copy (default is dry-run)
  --json          Emit J
```


## Exports
`bootstrap` (re-export), `walk` (re-export), `shouldSkipRel` (re-export)

## Key methods
`mkdirSync`, `copyFileSync`, `resolve`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/bootstrap-c-mirror.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find bootstrap-c-mirror.mjs` or `prism_session:master_index_query`; open the source at `scripts/bootstrap-c-mirror.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
