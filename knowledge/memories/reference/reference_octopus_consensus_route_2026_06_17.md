---
name: reference_octopus_consensus_route_2026_06_17
description: Octopus multi-LLM consensus wired as a first-class substrate + 4th execution dim in the feature-routing graph (2026-06-17, slot:alpha) -- improvement A of the article-driven cross-substrate pass; includes the dead-action bug the 3-of-3 caught
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.668Z
aliases: reference_octopus_consensus_route_2026_06_17
---


U-OCTOPUS-CONSENSUS-ROUTE (slot:alpha, 2026-06-17, commits `1516b6a896` build + `5714ecddc9` fix on cad-fusion-live-ms0, `[MAIN-FORCE]`). **Improvement A** of the operator's "make improvements across all substrates utilizing all the articles" pass -- the verified, article-independent piece (the article reads stayed BLOCKED on the Playwright MCP being down; ask-hermes/Grok cannot browse X live).

**The gap (verified, R8):** the prompt/task ROUTING graph (`scripts/lib/feature-routing-graph.mjs` TASK_CLASS_POLICY) modeled the octopus ONLY as a `/octopus` command on the orchestrate class -- `consensus`/`MultiModelConsensus` appeared 0× as a substrate or execution dim. Octopus is **cross-vendor** consensus (Claude+Codex+Ollama+Grok+Gemini via `MultiModelConsensusEngine.ask()` / `prism_ai:consensus_decide`), which breaks the single-model-FAMILY blind spot that the existing same-family fan-outs all share: the 3-of-3 review gate (3 Claude arms), the 5-lens brainstorm (5 Claude agents), and hermes-agents (N Claude agents). "When models agree, confidence is high; when they disagree, the disagreement IS the signal."

**What shipped:** consensus is now (1) a first-class SUBSTRATE in the generator catalog (`generate-feature-routing-graph.mjs`, 19->20); (2) a substrateLadder rung + a **4th `execution` dimension** on exactly the three high-stakes classes -- review (escalation ABOVE 3-of-3 when same-family arms could share a blind spot), plan (cross-vendor lens on a crossroad), orchestrate (the octopus fan-out primitive); (3) added to ALL THREE execution-dim filter mirrors -- `buildRoutingDigest` (lib), `renderExecutionLine` (`.claude/hooks/prompt-route-inject.mjs`), AND `renderTemplate` (`scripts/task-graph-template.mjs`). **physics (safety) carries NO consensus dim** -- Grok/Gemini are cross-vendor egress, forbidden for G-code/safety calc (no-egress invariant, test-pinned via `deepEqual` exact 3-key). Tests 43/43 lib + 33/33 hook (6 new, real R9 asserts incl the no-egress invariant). JSON regenerated (20 substrates). Live digest render confirmed consensus surfaces on review/plan/orchestrate, suppressed on physics. routing-utilization-audit 0.821, conservation OK, no regression.

**The bug the 3-of-3 caught (regression record, R12):** my first commit `1516b6a896` named **`prism_memory:consensus_recall`** in the consensus dims/substrate -- a **DEAD dispatcher action** (verified: zero `consensus_recall` case in any dispatcher). I copied it from the `/octopus` skill + `MultiModelConsensusEngine.ts:124,671` comments WITHOUT verifying it resolves (HONESTY-rule violation: used a symbol I had not read). Arm A FAILED on it; arm C flagged the same (P3) + a P2 (the 3rd renderer `task-graph-template.mjs:159` still filtered the base-3 dims, silently omitting consensus -- my comment had claimed "mirrors renderExecutionLine" assuming only 2 sites). The REAL wired recall action is **`prism_dev:consensus_cache_recall`** (`devDispatcher.ts:304` enum + `:5651` case, backed by `ConsensusRecallCacheEngine`). Fix commit `5714ecddc9`: corrected the action in ALL live strings (lib/generator/JSON/spec + the gitignored `.claude/commands/octopus.md` source) + added "consensus" to the 3rd renderer + added an **R9 negative-guard test** that FAILS if `prism_memory:consensus_recall` regresses (the substring-match positive test could not catch a dead-but-plausible action). Re-verified 3-of-3 all PASS.

**Lesson:** a routing-graph artifact injects invocation strings for the model to FIRE -- a "real-looking" action name is not enough; the named dispatcher action must be grep-verified to RESOLVE (the positive substring test is insufficient; pair it with a negative guard against known-dead names). And "mirrors X" is a claim to verify -- grep for ALL consumers of the shared shape before asserting the mirror count (there were 3 execution-dim renderers, not 2).

**OPEN follow-ups (flagged, out of this unit's scope):** (a) tracked wiki octopus.md mirrors (`knowledge/wiki/{architecture/skills/project,os/commands}/octopus.md`) + `MultiModelConsensusEngine.ts:124,671` comments still carry the dead `prism_memory:consensus_recall` label (doc/comment rot, not invocation strings); (b) the `/octopus` skill is gitignored so its local fix won't persist a fresh checkout -- the durable fix is the tracked wiki mirrors. **Article-driven pass remaining:** improvement B (PII/defensive gatekeeper on untrusted intake -- `aidefence_scan`/Jakob-Nielsen gatekeeper) + improvement C (cheap audit-digest for the review class / Review Paradox); the unread X articles need the Playwright MCP reconnected. Related: [[reference_exec_policy_routing_graph_2026_06_16]] · [[reference_feature_routing_graph_ms0_2026_06_15]] · [[reference_routing_graph_completeness_2026_06_17]] · [[reference_psn_octopus_fleet_synergy_2026_05_31]].
