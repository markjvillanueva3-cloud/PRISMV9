---
name: reference_code_engine_lathesafetypredicateengine
description: "LatheSafetyPredicateEngine — U-LSR22 (LATHE-HARDENED-MS0)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LatheSafetyPredicateEngine.ts
  sourceHash: 30e14e30
  loc: 356
aliases: SafetyPredicateViolation
---

# SafetyPredicateViolation -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LatheSafetyPredicateEngine.ts` (356 LOC)

## What it is
```
LatheSafetyPredicateEngine — U-LSR22 (LATHE-HARDENED-MS0)

Formal safety predicate over U-LSR24 signals + optional machine-envelope check.

This engine wraps the LatheSafetySignals composite gate in a deterministic,
total, NaN-guarded predicate suitable for use as a precondition in the
emitter (U-LSR04) and the proof-carrying emit record (U-LSR25).

Design axioms:
  (A1) Totality       — verify() never throws on well-typed input; shape bugs
                        in signals degrade to BLOCKED with a structured reason.
  (A2) NaN-safety     — any non-finite numeric in the signal payload causes
                        BLOCKED; the predicate refuses to trust poisoned physics.
  (A3) Monotonicity   — if `before` is SAFE and a mutation only weakens one
                        clause (worse grip, larger force, etc.), the mutated
                        result must be in {UNVERIFIED, BLOCKED} 
```


## Exports
`PredicateStatus` (type), `EnvelopeCheckSchema` (const), `EnvelopeCheck` (type), `ClauseViolationSchema` (const), `ClauseViolation` (type), `PredicateResultSchema` (const), `PredicateResult` (type), `SafetyPredicateViolation` (class), `latheSafetyPredicateEngine` (const), `LatheSafetyPredicateEngine` (re-export)

## Key methods
`verify`, `verifyOrThrow`, `isMonotonicWeakening`, `check`

## Depends on
[[reference_code_engine_lathesafetysignalengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/LatheSafetyPredicateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SafetyPredicateViolation` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LatheSafetyPredicateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
