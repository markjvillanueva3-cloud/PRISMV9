---
name: reference_code_engine_millloraexperimenttrackerengine
description: "MillLoRAExperimentTrackerEngine — Training Experiment Tracker (Mill parity)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MillLoRAExperimentTrackerEngine.ts
  sourceHash: a9427cdb
  loc: 362
aliases: millLoRAExperimentTrackerEngine
---

# millLoRAExperimentTrackerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MillLoRAExperimentTrackerEngine.ts` (362 LOC)

## What it is
```
MillLoRAExperimentTrackerEngine — Training Experiment Tracker (Mill parity)
============================================================================

Tracks MillLoRA training experiments: hyperparameters, metrics, artifacts.
Supports per-axis_mode experiment scope, mill-canonical metric vocabulary,
and best-model selection.

Mill parity for LatheLoRAExperimentTrackerEngine (LATHE-LORA-MS0 U-LLR47)
with MILL-CANONICAL extensions:

  - Per-axis_mode experiment scope — every experiment is tagged to a
    mill axis mode (3_axis / 4_axis_index / 5_axis_simul / shared) so
    best-of selection picks the right model per mill cell
  - Mill-canonical metric vocabulary (registered semantic metric kinds
    with monotonic direction so getBestMetric can correctly pick the
    winning sample without an explicit goal override):
      chatter_violation_rate  (minimize, Tlusty regenerative breach %)
```


## Exports
`MillExperimentStatus` (type), `MillAxisMode` (type), `MillMetricGoal` (type), `MILL_CANONICAL_METRICS` (const), `MillExperimentMetric` (interface), `MillArtifactType` (type), `MILL_CANONICAL_ARTIFACT_TYPES` (const), `MillExperimentArtifact` (interface), `MillExperiment` (interface), `MillTrackerConfig` (interface), `millLoRAExperimentTrackerEngine` (const), `MillLoRAExperimentTrackerEngine` (re-export)

## Key methods
`setConfig`, `getConfig`, `createExperiment`, `logMetric`, `logMetrics`, `addArtifact`, `completeExperiment`, `getMetricSeries`, `getLatestMetric`, `getBestMetric`, `getMetricGoal`, `findBestExperiment`, `findBestPerAxis`, `compareExperiments`, `getExperiment`, `getExperiments`, `archive`, `getStats`, `getSummary`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MillLoRAExperimentTrackerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find millLoRAExperimentTrackerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MillLoRAExperimentTrackerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
