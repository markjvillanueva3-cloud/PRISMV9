---
name: reference_code_schema_hookcreationguardschema
description: "hookCreationGuardSchema — HOOK-SYNERGY-MS0 / U-HOOK-CREATION-GATE (H5)"
metadata:
  type: reference
  kind: code-schema
  galaxy: unassigned
  sourcePath: mcp-server/src/schemas/hookCreationGuardSchema.ts
  sourceHash: ff71a332
  loc: 85
aliases: hookCreationGuardSchema.ts
---

# hookCreationGuardSchema.ts -- Schema SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/schemas/hookCreationGuardSchema.ts` (85 LOC)

## What it is
```
hookCreationGuardSchema — HOOK-SYNERGY-MS0 / U-HOOK-CREATION-GATE  (H5)

Zod schemas + types for {@link ../engines/HookCreationGuardEngine.HookCreationGuardEngine}.

Mirrors {@link ../engines/DuplicationGuardEngine.CheckBeforeCreatingInput}'s contract but
adds the two dimensions the generic guard can't see for hooks:
  - **event** — which Claude Code hook event the proposed hook will fire on
    (PreToolUse, PostToolUse, Stop, …);
  - **matcher** — the tool-name matcher pattern (`^Bash$`, `^(Edit|Write)$`, …).

The (event, matcher) tuple is the *signature* of a hook — two hooks with the same
signature compete on the same trigger and are a duplication-risk class the existing
name-only guards (`ai-duplication-guard`, `duplication-hard-block`) miss.

Schema version: 1.

@module schemas/hookCreationGuardSchema
```


## Exports
`HOOK_CREATION_GUARD_SCHEMA_VERSION` (const), `HookCreationMatchReasonSchema` (const), `HookCreationMatchReason` (type), `HookCreationMatchSchema` (const), `HookCreationMatch` (type), `HookCreationCheckInputSchema` (const), `HookCreationCheckInput` (type), `HookCreationRecommendationSchema` (const), `HookCreationRecommendation` (type), `HookCreationCheckResultSchema` (const), `HookCreationCheckResult` (type)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/schemas/hookCreationGuardSchema.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Schema

## How to query it
Recall by meaning via `/system-viz find hookCreationGuardSchema.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/schemas/hookCreationGuardSchema.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
