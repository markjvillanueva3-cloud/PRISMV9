---
name: reference_code_script_sfcvariabilityenumerate
description: "SFC-ACCURACY-MS1 Stage 1 — Combinatorial enumerator."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/sfc-variability-enumerate.mjs
  sourceHash: e4f18948
  loc: 577
aliases: sfc-variability-enumerate.mjs
---

# sfc-variability-enumerate.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/sfc-variability-enumerate.mjs` (577 LOC)

## What it is
```
SFC-ACCURACY-MS1 Stage 1 — Combinatorial enumerator.

Walks the full compatibility-filtered cross-product of OrchestratorInput
dimensions (machine × spindle × controller × coolant × material × tool ×
holder × operation × strategy × stickout × fixture × insert) and emits
chunked JSONL files of valid OrchestratorInput objects ready for Stage 2
(the batch runner that drives SpeedFeedOrchestratorEngine.compute on each).

The operator wants 100M+ valid combinations. Pure Cartesian is ~10^15;
compatibility filters (ISO 513 group rules, taper-holder, table-size,
coating-material, workholding-geometry) cut 99.999% of those down to a
tractable ~200M-500M envelope.

Modes:
  --estimate  (default — just count, no emission, ~10 s)
  --enumerate --out <dir> [--chunk N]   (emit chunked JSONL)
  --sample N --out <file>               (random valid sample of N for smoke testing)
  --resume               
```


## Exports
`enumerate` (re-export), `fingerprint` (re-export), `makeInput` (re-export), `modeEstimate` (re-export), `modeSample` (re-export), `modeEnumerate` (re-export), `MACHINE_ARCHETYPES` (re-export), `MATERIALS` (re-export), `OPERATIONS` (re-export), `CONTROLLERS` (re-export), `COOLANT_TYPES` (re-export), `coatingFitsIsoGroup` (re-export), `toolMaterialFitsIso` (re-export), `operationFitsToolFamily` (re-export), `strategyFitsOperation` (re-export), `stickoutFitsToolFamily` (re-export), `cutTypeFitsStrategy` (re-export), `flutesFitToolFamily` (re-export), `taperFitsToolDiameter` (re-export), `workholdingFitsIso` (re-export)

## Key methods
`writeFileSync`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/sfc-variability-enumerate.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find sfc-variability-enumerate.mjs` or `prism_session:master_index_query`; open the source at `scripts/sfc-variability-enumerate.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
