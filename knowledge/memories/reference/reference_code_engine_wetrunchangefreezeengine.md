---
name: reference_code_engine_wetrunchangefreezeengine
description: "WetRunChangeFreezeEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WetRunChangeFreezeEngine.ts
  sourceHash: bbff0460
  loc: 496
aliases: WetRunChangeFreezeEngine
---

# WetRunChangeFreezeEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WetRunChangeFreezeEngine.ts` (496 LOC)

## What it is
WetRunChangeFreezeEngine

```
WetRunChangeFreezeEngine
------------------------------------------------------------
Manages change-freeze windows that block new wet-run
authorizations, new program releases, and new deviations.

Windows are strictly non-overlapping per kind-policy:
  • quarter_end, audit, customer_visit, regulatory, holiday
    — cannot overlap any other non-emergency window.
  • emergency — can be declared even if another window is
    active (represents an unscheduled incident lockdown);
    it stacks on top until closed.

Every window requires four-eyes authorization: declared_by
and approved_by must differ. A minimum reason length of 40
characters forces a real rationale, not a rubber stamp.

Break-glass override
  Some pilot-critical changes may legitimately need to land
  inside a freeze window. An override is scoped (change_kind
  explicit list), bounded (expires_at required within the
  window
```


## Exports
`FreezeKind` (type), `ChangeKind` (type), `WindowState` (type), `FreezeWindow` (interface), `WindowInput` (interface), `OverrideGrant` (interface), `CheckResult` (interface), `Snapshot` (interface), `WetRunChangeFreezeEngine` (class), `wetRunChangeFreezeEngine` (const)

## Key methods
`declareWindow`, `closeWindow`, `cancelWindow`, `grantOverride`, `revokeOverride`, `sweepExpiredOverrides`, `checkAt`, `nowTs`, `getWindow`, `getOverride`, `listWindows`, `listOverrides`, `snapshot`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WetRunChangeFreezeEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WetRunChangeFreezeEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WetRunChangeFreezeEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
