---
name: reference_code_engine_projectautoupdaterengine
description: "Project Auto Updater"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ProjectAutoUpdaterEngine.ts
  sourceHash: 978f8905
  loc: 738
aliases: ProjectAutoUpdaterEngine
---

# ProjectAutoUpdaterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ProjectAutoUpdaterEngine.ts` (738 LOC)

## What it is
Project Auto Updater

```
ProjectAutoUpdaterEngine
========================

OBSIDIAN-INTELLIGENCE-MS3/B5/U-PROJECT-AUTO-UPDATER

Watches the project subfolders under `${vaultRoot}/project/` and keeps each
project's `overview.md` current by prepending a one-line change summary to
its `## Recent Changes` section whenever a non-overview note inside that
project folder is modified.

  * For each `${vaultRoot}/project/<name>/` subfolder, find the most-recent
    `.md` file that is NOT `overview.md`.
  * Compute a deterministic change signature `<filename>:<mtimeMs>`.
  * If that signature is already recorded in the project's `overview.md`
    (embedded as an HTML-comment marker), the project is up-to-date — skip.
  * Otherwise generate a one-line summary (Ollama qwen2.5-coder when a
    client is supplied AND the source is within tokenCapBytes; literal
    first-meaningful-line otherwise) and prepend it under
    `##
```


## Exports
`ProjectUpdateRoute` (type), `ProjectChange` (interface), `ProjectScan` (interface), `ProcessedProject` (interface), `ProjectUpdateResult` (interface), `OllamaProjectClient` (interface), `ProjectAutoUpdaterOptions` (interface), `ProjectAutoUpdaterOptionsSchema` (const), `ProjectAutoUpdaterEngine` (class), `_internals` (const), `projectAutoUpdaterEngine` (const), `runProjectAutoUpdater` (function)

## Key methods
`summarise`, `scanProjects`, `validateOptions`, `processProjects`, `mkdirSync`, `writeFileSync`, `renameSync`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ProjectAutoUpdaterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ProjectAutoUpdaterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ProjectAutoUpdaterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
