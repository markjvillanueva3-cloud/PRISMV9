---
name: reference_code_engine_ppunitsmodevalidatorengine
description: "Validate G20/G21 units mode"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PPUnitsModeValidatorEngine.ts
  sourceHash: 840430b0
  loc: 306
aliases: PPUnitsModeValidatorEngine
---

# PPUnitsModeValidatorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PPUnitsModeValidatorEngine.ts` (306 LOC)

## What it is
Validate G20/G21 units mode

```
PPUnitsModeValidatorEngine — Validate G20/G21 units mode

G20 = inch, G21 = millimeter. The units mode is persistent across
power cycles on most controls but the last state won't match what the
post expected, so programs must set their own mode at the start.

Checks:
  - missing_initial_units (error): program has motion without G20 or
    G21 set first. Controller uses prior program's mode → wrong scale.
  - mid_program_unit_switch (error): G20/G21 appears after motion
    started. Almost never valid — offsets don't re-scale, so tool
    path collapses to the wrong size.
  - implausible_inch_values (warning): G20 mode with coordinates
    that look like mm (e.g., X100., X50.) — inch parts rarely have
    single coords over 20". Often a forgotten G21.
  - implausible_mm_values (warning): G21 mode with coordinates that
    look like inches (e.g., X0.5, X2.125) — mm parts rarely have
    co
```


## Exports
`UnitsMode` (type), `UnitsSeverity` (type), `UnitsIssue` (interface), `UnitsModeResult` (interface), `UnitsModeOptions` (interface), `PPUnitsModeValidatorEngine` (class), `ppUnitsModeValidatorEngine` (const)

## Key methods
`validate`, `quickCheck`, `defaultOptions`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PPUnitsModeValidatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PPUnitsModeValidatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PPUnitsModeValidatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
