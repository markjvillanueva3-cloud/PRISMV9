---
name: reference_code_algorithm_layernormalization
description: "LayerNormalization — per-sample feature normalization (Ba, Kiros & Hinton 2016)."
metadata:
  type: reference
  kind: code-algorithm
  galaxy: unassigned
  sourcePath: mcp-server/src/algorithms/LayerNormalization.ts
  sourceHash: af5e5015
  loc: 186
aliases: LayerNormalization
---

# LayerNormalization -- Algorithm SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/algorithms/LayerNormalization.ts` (186 LOC)

## What it is
```
LayerNormalization — per-sample feature normalization (Ba, Kiros & Hinton 2016).

  For each row x (a token / sample of d features):
    μ   = mean_j x_j
    σ²  = mean_j (x_j − μ)²            (population variance, across features)
    x̂_j = (x_j − μ) / sqrt(σ² + ε)
    y_j = γ_j · x̂_j + β_j             (learned per-feature scale/shift)

Unlike batch normalization (which normalizes each feature ACROSS the batch and
is batch-statistics-dependent), LayerNorm normalizes ACROSS the features of a
SINGLE sample — so it is batch-size-independent and the canonical normalizer
inside Transformer blocks and RNNs. It directly pairs with the
`ScaledDotProductAttention` primitive shipped this milestone: attention +
layer-norm + residual are the two stateless halves of a Transformer block, so
having both as composable `prism_algorithm` actions lets the AI/NN/deep-learning
galaxies assemble a block wi
```


## Exports
`LayerNormInput` (interface), `LayerNormOutput` (interface), `LayerNormalization` (const)

## Key methods
`validate`, `calculate`, `getMetadata`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/algorithms/LayerNormalization.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Algorithm

## How to query it
Recall by meaning via `/system-viz find LayerNormalization` or `prism_session:master_index_query`; open the source at `mcp-server/src/algorithms/LayerNormalization.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
