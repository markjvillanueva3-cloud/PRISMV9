---
name: reference_code_engine_hookregistryreaderengine
description: "HOOK-SYNERGY-MS0 / U-HOOK-REGISTRY (H2)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HookRegistryReaderEngine.ts
  sourceHash: dc4f5a7b
  loc: 439
aliases: HookRegistryReaderEngine
---

# HookRegistryReaderEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HookRegistryReaderEngine.ts` (439 LOC)

## What it is
HOOK-SYNERGY-MS0 / U-HOOK-REGISTRY (H2)

```
HookRegistryReaderEngine — HOOK-SYNERGY-MS0 / U-HOOK-REGISTRY (H2)

Pure read-only consumer of `state/shared/HOOK_REGISTRY.json` (produced by
`scripts/build-hook-registry.mjs`). The registry is ~228 KB / 455 hooks, so
every method on this engine returns a *projection* — never the full blob —
and an mtime-keyed cache avoids re-parsing on every dispatcher call.

Surfaces:
  getCounts()                        — small summary (file count + byEvent/byType/byLayer)
  getCompactMap(maxPerEvent?)        — { event → top-N hook ids } (mirrors DispatcherMapEngine.getCompactMap)
  findHook(idOrSubstring)            — single hook by exact id, then case-insensitive substring
  searchHooks(query, max?)           — scored matches across id/description/event
  byEvent(event)                     — every hook registered to a specific event
  byTier(tier)                       — every hook tagged with a tie
```


## Exports
`HookEventRecord` (interface), `HookRecord` (interface), `HookRegistryShape` (interface), `HookSummary` (interface), `SearchHit` (interface), `CountsProjection` (interface), `HookRegistryReaderOptions` (interface), `HookRegistryReaderEngine` (class), `hookRegistryReaderEngine` (const)

## Key methods
`clearCache`, `logWarn`, `getMeta`, `getCounts`, `getCompactMap`, `findHook`, `searchHooks`, `byEvent`, `byTier`, `wired`, `orphaned`, `isStale`, `walkMjs`, `visit`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/HookRegistryReaderEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HookRegistryReaderEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HookRegistryReaderEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
