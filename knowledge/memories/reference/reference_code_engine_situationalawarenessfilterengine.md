---
name: reference_code_engine_situationalawarenessfilterengine
description: "Compresses a long directive to a tight slice"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SituationalAwarenessFilterEngine.ts
  sourceHash: 012353fe
  loc: 121
aliases: SituationalAwarenessFilterEngine
---

# SituationalAwarenessFilterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SituationalAwarenessFilterEngine.ts` (121 LOC)

## What it is
Compresses a long directive to a tight slice

```
SituationalAwarenessFilterEngine — Compresses a long directive to a tight slice

Phase 0.13 U-SAW5 from UNIVERSAL-SKILLS-SCRIPTS-HOOKS-PLAN. The full PRISM
self-awareness directive is 188 lines. Injecting all of it on every prompt
burns context for no benefit. This engine scores each line against the user
prompt and returns the top ≤25 most-relevant lines, preserving their
original order so that surrounding context is not destroyed.

Scoring is a keyword-overlap baseline (token Jaccard + section-header bias).
It is intentionally simple so that the engine has zero runtime dependencies
and can be swapped for MiniLM embeddings later by replacing `scoreLine`.

@module engines/SituationalAwarenessFilterEngine
@milestone PP-0.13-U-SAW5
```


## Exports
`FilterOptions` (interface), `FilterResult` (interface), `ScoredLine` (interface), `SituationalAwarenessFilterEngine` (class), `situationalAwarenessFilterEngine` (const)

## Key methods
`filter`, `scoreLine`, `similarity`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/SituationalAwarenessFilterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SituationalAwarenessFilterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SituationalAwarenessFilterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
