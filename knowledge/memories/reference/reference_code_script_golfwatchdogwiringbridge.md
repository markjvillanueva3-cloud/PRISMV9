---
name: reference_code_script_golfwatchdogwiringbridge
description: "golf-watchdog-wiring-bridge.mjs — CLEANUP-MS0 / U-CLEANUP-C5"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/golf-watchdog-wiring-bridge.mjs
  sourceHash: 73cea7d2
  loc: 419
aliases: golf-watchdog-wiring-bridge.mjs
---

# golf-watchdog-wiring-bridge.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/golf-watchdog-wiring-bridge.mjs` (419 LOC)

## What it is
```
golf-watchdog-wiring-bridge.mjs — CLEANUP-MS0 / U-CLEANUP-C5

Polling bridge that wires B1 (PeerCommitAuditorEngine) to C1
(WiringPotentialEngine.analyze), C3 (system-viz-add-node), and F8
(golf-signal-post). The B1 tick records new engine files in the
peer_audit_ticks ledger; this bridge polls those rows and, for each
newly-seen engine file, runs the wiring-potential analysis + graph
augmentation + chat-bus signal — all best-effort, all gated by a
5-minute per-file recently-analyzed cache to prevent recursion when
B1 + C5 share a cron cadence.

Design choices:
  - **Polling, not callback.** B1 was already shipped + scrutinized;
    wiring a synchronous callback would mean touching the engine.
    A polling bridge is decoupled (B1 doesn't know about C5) and
    resilient (B1 crashes don't take the bridge down).
  - **5-minute recency cache** lives in `state/shared/.c5-recent.json`.
    F
```


## Exports
`SCHEMA_VERSION` (const), `RECENT_CACHE_WINDOW_MS` (const), `DEFAULT_REPO_ROOT` (const), `DEFAULT_DB_RELATIVE` (const), `DEFAULT_CACHE_RELATIVE` (const), `POLL_LOOKBACK_MS` (const), `MAX_ENGINES_PER_TICK` (const), `ENGINE_PATH_PATTERN` (const), `parseArgs` (function), `resolvePaths` (function), `loadCache` (function), `saveCache` (function), `isRecentlySeen` (function), `pollAuditLedger` (function), `extractEngineFilesFromMeta` (function), `runWiringAnalyze` (function), `runGraphAugment` (function), `postGolfSignalDigest` (function), `runBridgeCycle` (function), `runCli` (function)

## Key methods
`writeFileSync`, `renameSync`, `visit`, `saveCache`, `runCli`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/golf-watchdog-wiring-bridge.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find golf-watchdog-wiring-bridge.mjs` or `prism_session:master_index_query`; open the source at `scripts/golf-watchdog-wiring-bridge.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
