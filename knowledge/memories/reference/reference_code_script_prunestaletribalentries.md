---
name: reference_code_script_prunestaletribalentries
description: "scripts/prune-stale-tribal-entries.mjs — U-VICTOR-C4"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/prune-stale-tribal-entries.mjs
  sourceHash: 907a5f5e
  loc: 174
aliases: prune-stale-tribal-entries.mjs
---

# prune-stale-tribal-entries.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/prune-stale-tribal-entries.mjs` (174 LOC)

## What it is
```
scripts/prune-stale-tribal-entries.mjs — U-VICTOR-C4

Reads `state/shared/.wiki-tribal-cross-ref-audit.json` (echo's audit) and
removes from `state/shared/tribal-embed-index.json` any entry whose
underlying wiki/file source has been deleted (the `staleInTribal` set).

Live audit 2026-05-22 reported 10 stale entries — they accumulate slowly
but matter: a stale embedding ships rotten retrieval. Audit calls them
out; nothing removes them until this script runs.

Emits a deletion log to `state/shared/tribal-pruned.jsonl` for audit-trail
(every prune is recoverable if a source file is restored — the original
tribal entry can be re-embedded).

Modes:
  --dry-run (default if no flag)  → reports what WOULD be pruned, no writes
  --apply                         → actually mutates the index

Exit:
  0 ok · 1 nothing-to-prune · 2 runtime error
```


## Exports
`ROOT` (const), `SCHEMA_VERSION` (const), `normalize` (function), `partitionPrune` (function), `extractWikiPath` (function), `main` (function)

## Key methods
`writeTribalIndexGuarded`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/prune-stale-tribal-entries.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find prune-stale-tribal-entries.mjs` or `prism_session:master_index_query`; open the source at `scripts/prune-stale-tribal-entries.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
