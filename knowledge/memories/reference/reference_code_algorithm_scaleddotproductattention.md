---
name: reference_code_algorithm_scaleddotproductattention
description: "ScaledDotProductAttention — the core attention primitive of the Transformer"
metadata:
  type: reference
  kind: code-algorithm
  galaxy: unassigned
  sourcePath: mcp-server/src/algorithms/ScaledDotProductAttention.ts
  sourceHash: 7c26a515
  loc: 230
aliases: ScaledDotProductAttention
---

# ScaledDotProductAttention -- Algorithm SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/algorithms/ScaledDotProductAttention.ts` (230 LOC)

## What it is
```
ScaledDotProductAttention — the core attention primitive of the Transformer
(Vaswani et al. 2017, "Attention Is All You Need").

    Attention(Q, K, V) = softmax( (Q · Kᵀ) / √d_k + mask ) · V

Pure, deterministic, numerically-stable (row-wise max-shift softmax). NO
learned weights — this is the attention *operator*; the learned Q/K/V
projections are applied upstream. A foundational deep-learning building block
that PRISM's CAM/CAD/CNC deep-learning + cross-process attention engines can
compose, and which pairs with the `ml_activation` softmax shipped alongside it.

Shapes (rows = sequence positions):
  Q : [Lq × d_k]   (queries)
  K : [Lk × d_k]   (keys)
  V : [Lk × d_v]   (values)        — K and V share the key-length Lk
  → output           [Lq × d_v]
  → attentionWeights [Lq × Lk]     (each row sums to 1)

Options:
  • scale     — override 1/√d_k (default) with a custom temperature.
 
```


## Exports
`AttentionInput` (interface), `AttentionOutput` (interface), `ScaledDotProductAttention` (const)

## Key methods
`validate`, `calculate`, `getMetadata`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/algorithms/ScaledDotProductAttention.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Algorithm

## How to query it
Recall by meaning via `/system-viz find ScaledDotProductAttention` or `prism_session:master_index_query`; open the source at `mcp-server/src/algorithms/ScaledDotProductAttention.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
