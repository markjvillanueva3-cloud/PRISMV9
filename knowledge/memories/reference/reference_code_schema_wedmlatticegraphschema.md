---
name: reference_code_schema_wedmlatticegraphschema
description: "WEDM Lattice Graph Schema — v1"
metadata:
  type: reference
  kind: code-schema
  galaxy: unassigned
  sourcePath: mcp-server/src/schemas/wedmLatticeGraphSchema.ts
  sourceHash: 0ff74a7f
  loc: 198
aliases: WEDMLatticeGraphSchema
---

# WEDMLatticeGraphSchema -- Schema SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/schemas/wedmLatticeGraphSchema.ts` (198 LOC)

## What it is
```
WEDM Lattice Graph Schema — v1

Schema for `data/state/WEDM_LATTICE_GRAPH.json`, the embedding graph built by
`WEDMLatticeGraphEngine` (MS-P5-GNN / U-P5-GNN-01).

Each node represents a (material × machine × wire × thickness × Ra_target)
cell in the WEDM parameter space; its `embedding` is a 64-dim f32 vector
computed deterministically from the cell attributes and physics-derived
features. Edges link cells that share enough attribute bits to be plausible
neighbors (same material, adjacent thickness, etc.) with a weight in [0,1].

Design constraints (from MS-P5-GNN exit gate):
  - embeddingDim = 64 exactly (literal-typed)
  - ≥ 300 nodes after build
  - adjacencySparsity = edges / nodes² < 0.05 (avoid dense false positives)

@module schemas/wedmLatticeGraphSchema
```


## Exports
`LatticeMaterialSchema` (const), `LatticeMaterial` (type), `LatticeISOGroupSchema` (const), `LatticeISOGroup` (type), `LatticeControllerSchema` (const), `LatticeController` (type), `LatticeWireSchema` (const), `LatticeWire` (type), `LatticeNodeEvidenceSchema` (const), `LatticeNodeEvidence` (type), `LatticeEdgeEvidenceSchema` (const), `LatticeEdgeEvidence` (type), `LATTICE_EMBEDDING_DIM` (const), `WEDMLatticeNodeSchema` (const), `WEDMLatticeNode` (type), `WEDMLatticeEdgeSchema` (const), `WEDMLatticeEdge` (type), `WEDMLatticeGraphSchema` (const), `WEDMLatticeGraph` (type), `EMPTY_WEDM_LATTICE_GRAPH` (const)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/schemas/wedmLatticeGraphSchema.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Schema

## How to query it
Recall by meaning via `/system-viz find WEDMLatticeGraphSchema` or `prism_session:master_index_query`; open the source at `mcp-server/src/schemas/wedmLatticeGraphSchema.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
