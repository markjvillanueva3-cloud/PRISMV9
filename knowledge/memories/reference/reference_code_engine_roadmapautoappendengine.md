---
name: reference_code_engine_roadmapautoappendengine
description: "AUTO-LEARNING-LOOP-MS0 / U-ALL06"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/RoadmapAutoAppendEngine.ts
  sourceHash: e0dcfd17
  loc: 455
aliases: RoadmapAutoAppendEngine
---

# RoadmapAutoAppendEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/RoadmapAutoAppendEngine.ts` (455 LOC)

## What it is
AUTO-LEARNING-LOOP-MS0 / U-ALL06

```
RoadmapAutoAppendEngine — AUTO-LEARNING-LOOP-MS0 / U-ALL06
===========================================================

Turns U-ALL04 `SynergyClassifierEngine` `high`-band verdicts into
proposed roadmap units (`U-AUTORES-XX` IDs). The engine does NOT
touch the roadmap file directly — file mutation belongs to the
CLI under a `prism_context:claim_file` lock (deferred to U-ALL09
cron landing). The engine emits an `AppendProposal` carrying the
YAML payload + the target milestone hint; the CLI is responsible
for the actual write.

Spec § U-ALL06 contracts:
  - **Append rate limit**: ≤ 2 new units per milestone per cron
    cycle (default), unless the candidate has ≥ 3 corroborating
    sources — in which case the rate limit is bypassed for that
    candidate. The rate-limit config persists in
    `state/shared/auto-learning/append-rate-limit.json` and is
    loaded by the CLI on startup.
  - 
```


## Exports
`AppendInput` (interface), `AppendRateLimitState` (interface), `AppendProposal` (interface), `AppendRejection` (interface), `AppendBatchResult` (interface), `EngineOpts` (interface), `RATE_LIMIT_SCHEMA_VERSION` (const), `DEFAULT_PER_MILESTONE_LIMIT` (const), `DEFAULT_CORROBORATION_BYPASS_THRESHOLD` (const), `DEFAULT_ID_PREFIX` (const), `DEFAULT_TARGET_MILESTONE` (const), `DEFAULT_TIER` (const), `MAX_TITLE_LEN` (const), `MAX_WHY_LEN` (const), `RoadmapAutoAppendEngine` (class), `roadmapAutoAppendEngine` (const)

## Key methods
`getState`, `setState`, `resetCycleCounters`, `resetAll`, `propose`, `proposeBatch`

## Depends on
[[reference_code_engine_synergyclassifierengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/RoadmapAutoAppendEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find RoadmapAutoAppendEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/RoadmapAutoAppendEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
