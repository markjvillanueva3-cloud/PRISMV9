---
name: reference_lathe_g76_dialect_fossil_2026_06_21
description: "Lathe G76 threading-dialect test FOSSIL realigned to source-of-truth (slot:india, 2026-06-21). lathe-ai-reasoning.test.ts G76 block was a U-TEST-FOSSIL absorbed orphan (2026-05-12) asserting a speculative 7-dialect design with non-canonical G-code (Haas G76-D-first, Okuma G71-as-threading, Mazak G324/G424/G420); it contradicted ThreadTurningEngine's cited-manual 5-dialect design (2026-04-27) + the 14/14-passing LatheAIReasoningEngine.test.ts companion (2026-05-16). Realigned test (9 reds -> green), no engine change. DEFERRED real gap: engine lacks native LinuxCNC/Mach3 G76 dialects -> silently emits Fanuc G76 for those controllers (latent wrong-G-code) -> whiskey/echo build + selectG76Dialect warning follow-up."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.637Z
aliases: reference_lathe_g76_dialect_fossil_2026_06_21
---


**UNIT:** `[MAIN-FORCE] [AI-LATHE-FIX]/U-LATHE-G76-FOSSIL-REALIGN (slot:india)` -- backlog #4 of [[reference_india_ai_test_reds_backlog_2026_06_21]] (`lathe-ai-reasoning.test.ts`, was 9/39 red).

**ROOT CAUSE (two contradictory test files, one engine -- the backlog-#5 pattern again):**
- `src/engines/ThreadTurningEngine.ts:278-309` `G76_DIALECTS` = **5 dialects** `fanuc_double | okuma | mitsubishi | siemens | mazak`, authored 2026-04-27 (`4ff03d7a4f`, ENGINE-WIRE-MS0/U-WIRE04) with cited controller manuals (Fanuc 0i/30i, Okuma OSP-P200/P300L, Mitsubishi M70V, Siemens 840D, Mazak Mazatrol M-Plus). `getG76Dialect` folds `haas/doosan-fanuc/hwacheon` into `fanuc_double` (Haas G76 IS Fanuc-two-line compatible). `selectG76Dialect` returns `getG76Dialect(controller) ?? "fanuc_double"` (`LatheAIReasoningEngine.ts:305`).
- The deliberate companion `src/__tests__/LatheAIReasoningEngine.test.ts` (2026-05-16, **14/14 PASSING**) behaviorally LOCKS that 5-dialect design (Fanuc->fanuc_double; Haas controller -> recommended_dialect via the fold).
- `src/__tests__/lathe-ai-reasoning.test.ts` was absorbed as an orphan in `799be785cb [CLEANUP-MS0]/U-TEST-FOSSIL: absorb 1,651 orphan...` (2026-05-12). Its G76 block asserted a **speculative 7-dialect** design (`length===7`; haas/linuxcnc/mach3/fanuc_single as first-class) with **dubious/non-canonical G-code**: Haas `G76 D`-first, Okuma `G71`+`M23/M26/M32` (G71 is a TURNING cycle on Okuma, not threading), Mazak `G324/G424/G420` (not canonical Mazak threading codes). It demanded `selectG76Dialect("Haas") -> "haas"` -- directly contradicting the companion's `-> fanuc_double`. A single engine cannot satisfy both.

**VERDICT (R7/R8/R12):** engine's cited 5-dialect design = source of truth (manufacturing-defensible + companion-locked + predates the fossil). Fossil = stale orphan with partly-WRONG G-code. Realigned the fossil's G76 Threading Dialects block + the `selectG76Dialect("Haas")` test to the engine's REAL contract -- exact reference-value asserts (real generateG76Code strings, micron conversions `depthUm=round(mm*1000)`, `f=pitch.toFixed(4)`; exact Siemens `CYCLE99(...)` positional). 9 reds -> green; `LatheAIReasoningEngine.test.ts` 14/14 unchanged. **TEST-ONLY -- zero engine/G-code behavior change** (not weakening: removed a contradiction + dubious assertions, did not gut a correct test). 2-arm per-file scrutiny PASS (both independently confirmed the contradiction + the 5 dialects' manufacturing-correctness).

**DEFERRED REAL GAP (do NOT let the realignment bury it -- R12/R13):** the fossil's intuition that **LinuxCNC + Mach3 are real distinct G76 dialects** is CORRECT, and the engine genuinely LACKS them. Worse, the engine *textually knows* them -- `getControllerRecommendations` emits LinuxCNC "R=2 constant-area regression" + "H parameter spring pass" advice (`LatheAIReasoningEngine.ts:609-625`) and tribal tips cite Mach3's 13-param G76 + LinuxCNC R/I/H (`LatheAIReasoningEngine.ts:163-168`) -- yet `getG76Dialect("PathPilot")` returns undefined -> `selectG76Dialect` falls back to `fanuc_double` -> a LinuxCNC/Mach3 program would silently receive **Fanuc two-line G76, which is NOT valid 1:1 on those controllers** (LinuxCNC uses single-block `G76 P_ Z_ I_ J_ K_ Q_ H_ E_ R_`). This is a latent wrong-G-code defect for a *recognized* controller family, not merely a missing feature.
- **OWNER:** whiskey (lathe) / echo (post-processor) -- needs authoritative `.cps`-grounded LinuxCNC + Mach3 G76 formats + threading-G-code safety review. NOT a unilateral india edit.
- **MIN follow-up (cheap, any of the 3 owners):** make `selectG76Dialect` emit a `warning` when the controller is recognized by `getControllerRecommendations` (LinuxCNC/Mach3) but unmatched by `getG76Dialect`, so the silent wrong-dialect fallback becomes loud.

**PATTERN (sibling of [[reference_india_ai_test_reds_backlog_2026_06_21]] #5 + #7):** when two test files target one engine with incompatible contracts, the deliberate convention-named companion (`EngineName.test.ts`) that PASSES is the source of truth; the absorbed `kebab-case` fossil is realigned, never the reverse. A fossil's *wrong specifics* (bad G-code) get removed; its *correct intuition* (a real missing dialect) gets surfaced as a deferred owned build, not silently dropped. Sibling: [[reference_post_ship_ai-lathe-fix-u-lathe-ai-bridge-red]].
