---
name: reference_code_engine_localembeddingengine
description: "In-process embeddings via @xenova/transformers"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LocalEmbeddingEngine.ts
  sourceHash: 0cebe6df
  loc: 171
aliases: LocalEmbeddingEngine
---

# LocalEmbeddingEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LocalEmbeddingEngine.ts` (171 LOC)

## What it is
In-process embeddings via @xenova/transformers

```
LocalEmbeddingEngine — In-process embeddings via @xenova/transformers

Phase external-infra. Runs Xenova's ONNX-backed MiniLM (or any feature-
extraction model) entirely in the Node process so PRISM's awareness and
semantic-similarity stack has a zero-service embeddings backend. The
Ollama/Qdrant path is still the production option for bigger models; this
engine is the always-available fallback that runs in the MCP server with
no external daemon.

Model files are downloaded on first use and cached in
`mcp-server/data/transformers-cache/`. You can change the model by passing
a different `model` argument to `load()`.

@module engines/LocalEmbeddingEngine
@milestone PP-INFRA-LOCAL-EMBED
```


## Exports
`LoadOptions` (interface), `EmbedResult` (interface), `DEFAULT_MODEL` (const), `DEFAULT_DIM` (const), `LocalEmbeddingEngine` (class), `localEmbeddingEngine` (const)

## Key methods
`load`, `isLoaded`, `getModel`, `embed`, `embedBatch`, `cosineSimilarity`, `unload`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/LocalEmbeddingEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find LocalEmbeddingEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LocalEmbeddingEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
