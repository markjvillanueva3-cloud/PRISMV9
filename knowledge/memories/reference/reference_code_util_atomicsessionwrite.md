---
name: reference_code_util_atomicsessionwrite
description: "atomicSessionWrite — atomic JSON state writer for per-session engines."
metadata:
  type: reference
  kind: code-util
  galaxy: unassigned
  sourcePath: mcp-server/src/utils/atomicSessionWrite.ts
  sourceHash: 9650a1c0
  loc: 164
aliases: atomicSessionWrite.ts
---

# atomicSessionWrite.ts -- Util SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/utils/atomicSessionWrite.ts` (164 LOC)

## What it is
```
atomicSessionWrite — atomic JSON state writer for per-session engines.

Problem: every per-session engine (SessionReorientation, ToolCallParallelization,
FileReadDeduplication, ConversationStaleDetector) writes to a shared JSON
file via writeFileSync. With concurrent writers (mcp-server engine + .mjs
hook + 8 sibling chats), the truncate-then-write race corrupts files.

Strategy: write to <file>.tmp + rename. On Windows NTFS, rename within
the same volume is atomic. On a corrupt-read, fall back to last-good
snapshot if available.

Also normalizes session-id resolution. Previously each engine fell back
to "default" when CLAUDE_SESSION_ID was unset, which collapsed all 8
chats onto the same file. Now we try (in order):
  1. CLAUDE_SESSION_ID
  2. TERM_SESSION_ID
  3. CLAUDE_TERMINAL_ID
  4. SSH_TTY (per-terminal stable on Linux)
  5. WT_SESSION (Windows Terminal)
  6. process.ppid (parent 
```


## Exports
`resolveSessionId` (function), `atomicWriteJson` (function), `safeReadJson` (function), `snapshotLastGood` (function), `cleanupOrphanTmps` (function)

## Key methods
`writeFileSync`, `renameSync`, `unlinkSync`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/utils/atomicSessionWrite.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Util

## How to query it
Recall by meaning via `/system-viz find atomicSessionWrite.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/utils/atomicSessionWrite.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
