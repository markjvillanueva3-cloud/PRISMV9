---
name: reference_ask_ollama_loaded_first_and_zulu_codegen_collision_2026_06_20
description: "ask-ollama loaded-first selection (prefer an already-warm chat model over a cold-load) -- foundation strict-option SHIPPED (1c6abe2878); the ask-ollama.mjs wiring is entangled with slot:zulu's LIVE uncommitted codegen-mode work in the shared tree, so I did NOT commit ask-ollama (conflict-fork rule: don't fight a peer's live-edited files)."
type: reference
slot: alpha
galaxy: token-optimization
source: prism-memory
synced: 2026-06-27T20:30:46.472Z
aliases: reference_ask_ollama_loaded_first_and_zulu_codegen_collision_2026_06_20
---


**SHIPPED 2026-06-20 (slot:alpha), commit `1c6abe2878`** -- `[TOKEN-EFFICIENCY-INJECT]/U-LOADED-CHAT-STRICT-OPTION`: added an `opts.strict` flag to `pickLoadedChatModel` (`scripts/lib/ollama-loaded-chat-model.mjs`). Default false = byte-identical for the existing prompt-rewriter consumer (9/9 regression green); strict:true drops the any-loaded-chat-model fallback so a quality-sensitive caller gets a warm model ONLY if it is in the caller preference list, else null -> caller cold-loads its best-installed pick instead of running a heavy task on an arbitrary tiny warm model. 17/17 tests (5 new strict cases). One of my own new tests caught a wrong assumption (R9): the helper test's `PREFERENCE` does NOT include `gpt-oss:120b`, so strict correctly returned null for it -- the CODE was right, I fixed the TEST to use a real preference member.

**The intended unit was U-ASK-OLLAMA-LOADED-FIRST** -- make `scripts/ask-ollama.mjs` (the canonical offload path) prefer an already-resident chat model (via `loadWarmModels()` /api/ps probe + `pickLoadedChatModel(warm, OFFLOAD_LOADED_PREFERENCE, {strict:true})`) BEFORE `resolveSynthesisModel` (best-installed, cold-load-tolerant). WHY (real token-economy win): a cold-load (~3-5s for a 32B, far longer for a ~60GB model) PLUS eviction of the warm model thrash the single shared Blackwell card for the whole fleet, and a slow offload makes the loop fall back to Claude -> token waste. **Live-validated:** /api/ps resident = `[gpt-oss:120b, qwen3-vl:8b-instruct]`; the selection correctly picked the warm `gpt-oss:120b` (zero cold-load) and excluded the warm vision model. ask-ollama tests 33/33 green.

**WHY ask-ollama.mjs was NOT committed (the important lesson):** mid-build I found **slot:zulu was LIVE-editing `scripts/ask-ollama.mjs` + `.test.mjs` in the same shared `cad-fusion-live-ms0` tree** -- building a `codegen` mode (`U-ASK-OLLAMA-CODEGEN`) directly ON TOP of my loaded-first foundation (zulu's `CODER_LOADED_PREFERENCE` comment literally says "Same strict warm-pick semantics as OFFLOAD_LOADED_PREFERENCE"; zulu REFACTORED my runRequest selection block to `mode === "codegen" ? CODER_LOADED_PREFERENCE : OFFLOAD_LOADED_PREFERENCE`, so our edits share the SAME hunks -- not cleanly separable by `git apply --cached`). Evidence zulu was live: the AGENT_CHAT claim storm on both files (03:53-04:00), and the harness "file modified by user/linter" notes appearing on ask-ollama.test.mjs mid-session (the import line gaining zulu's codegen exports). Committing `ask-ollama.mjs` by pathspec would have (a) absorbed zulu's work under MY commit (attribution loss -- the exact [[feedback_commit_to_slot_worktree]] failure) and (b) at the moment I checked, zulu's codegen had NO tests -> shipping untested peer code under my name (R12/R15 violation).

**Decision (conflict-fork rule, CLAUDE.md):** do NOT fight for a peer's live-edited files. Shipped ONLY my clean, self-contained, tested foundation (the helper). Left `ask-ollama.{mjs,test.mjs}` for zulu to commit (their codegen + my loaded-first wiring co-land; my parts are comment-attributed `slot:alpha`, so attribution-in-code survives even though zulu's commit will carry it). Logical order is correct anyway (R13): verifiable foundation first, consumer after.

**Open thread for a coordinated next step (NOT alpha-solo):** the ask-ollama loaded-first wiring is real, tested (33/33), live-validated -- it just needs to land via zulu's ask-ollama commit (with zulu's codegen tests). If zulu goes stale and the work rots uncommitted, a coordinated commit of `ask-ollama.{mjs,test.mjs}` crediting BOTH U-ASK-OLLAMA-LOADED-FIRST (alpha) + U-ASK-OLLAMA-CODEGEN (zulu) -- AFTER adding zulu's missing codegen tests -- is the cleanup. The `OFFLOAD_LOADED_PREFERENCE` deliberately excludes tiny coders (1.5b/7b) so a heavy summarize/explain never runs on a tiny warm model. Sibling: [[reference_ollama_chat_model_select_fix_2026_06_19]] (the rewriter chatmodel-select fix that birthed the helper) + [[feedback_conflict_fork_rule]].
