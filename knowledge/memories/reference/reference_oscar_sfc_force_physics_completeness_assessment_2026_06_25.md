---
name: reference_oscar_sfc_force_physics_completeness_assessment_2026_06_25
description: "First-pass DEEP ASSESSMENT (slot:oscar 2026-06-25) of whether the SFC engine system can live-calculate ALL forces simultaneously per machine (operator ask: all-forces-at-once + lathe stock stickout + tool/holder stack-up + spindle type/strength + table-vs-head kinematics + 4/5-axis + mill-turn). Evidence-grounded via targeted greps (parallel sub-agents FAILED 'prompt too long' -- session context too deep to spawn). Verdict: PARTIALLY -- the rich physics EXISTS as separate engines but the BASE orchestrator surfaces only scalar Fc; gap is largely WIRING + a few real modeling holes. Includes the ready-to-run deep-confirm spec for a FRESH-context session."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.703Z
aliases: reference_oscar_sfc_force_physics_completeness_assessment_2026_06_25
---


**Operator directive (2026-06-25):** "proper calculations of all forces working at the same time including stock stick out from lathe spindles, tool body stick out, tool holder gauge lengths and spindle strength and type. whether the table moves or the head or any combination of the 3 axis, forces in 4 and 5 axis machines and mill turns. all math and physics accounted for. do a deep assessment ... to ensure we can live calculate everything for all combinations logical per machine."

**Method note (R12):** a parallel 6-agent Workflow + 6 direct sub-agents BOTH failed with "Prompt is too long" -- this session's context (4+ compactions, 75+ tool calls) is too large to spawn subagents. Fell back to targeted greps + session-verified knowledge. This is a FIRST-PASS, evidence-grounded but NOT the exhaustive per-engine read the operator deserves -- that needs a FRESH-context session (spec below).

**VERDICT: PARTIALLY -- and the rich physics is ORPHANED from the live path.** The deep physics (3-component forces, cross-physics coupling, 5-axis, lathe thermo) EXISTS as dedicated engines, but the BASE `SpeedFeedOrchestratorEngine.compute()` surfaces only a SCALAR tangential force Fc and does NOT call them. So we canNOT today claim correct simultaneous-force live-calc across all per-machine combos -- but the gap is largely WIRING the existing physics into the live path + a few genuine modeling holes, NOT building from scratch.

**LIVE-PATH KEYSTONE FINDING (verified this session):** The CUSTOMER-facing live SFC calculator is `routes/sfc.ts /calculate -> prism_product:sfc_calculate -> ProductEngine.sfcCalculate`. ProductEngine.ts imports ONLY `physics/constants.js` + `CollisionEngine` + `AlgorithmEngine` (lines 39/80/81) -- it references NONE of SpeedFeedOrchestratorEngine / SpeedFeedNineAxisOrchestratorEngine / UltimateSpeedFeedEngine / CuttingForceEngine / any deflection engine (grep: zero matches). So the live customer calc is a SELF-CONTAINED, largely-INLINE computation (the source of the 2026-06-23 inline-MATERIAL_HARDNESS divergence + over-power-efficiency bugs), and the live SFC page's other endpoints (/deflection, /power-torque, /cycle-time) are SEPARATE scalar prism_calc calls -- NOT a coupled simultaneous-force solve. The rich force-coupled / multi-axis physics is BUILT but ORPHANED from the live path. ==> THE KEYSTONE UNIT: route the live SFC main calc through the rich orchestrator (ProductEngine delegates to SpeedFeedNineAxisOrchestratorEngine, OR /calculate calls it) so the customer gets the coupled-force result instead of the inline simplified one. This single wiring is the highest-leverage fix and reframes the roadmap below (it is WIRE, not build, for forces that already exist).

**STEP 1 RESOLVED (verified grep, same session):** the coupled simultaneous-force model ALREADY EXISTS in the UltimateSpeedFeed -> SpeedFeedNineAxisOrchestratorEngine path. NineAxis consumes `sfc.forces.radial_force_N` (SpeedFeedNineAxisOrchestratorEngine.ts:970) + `sfc.forces.resultant_force_N` (:1386, surfaced as "Cutting force resultant: N" :1400) + Altintas SLD chatter (:32) + Komanduri-Hou thermal (:395) + way-type rigidity->chatter-margin (:365). So Fc/Ff/Fp + resultant + deflection + chatter + thermal are COMPUTED + COMBINED on the Ultimate/NineAxis path -- the live ProductEngine inline calc simply does not call it. => KEYSTONE IS CONFIRMED WIRE-ONLY (route live /calculate -> NineAxis/Ultimate); the simultaneous-force CORE need NOT be built. Remaining true BUILD holes are narrower than feared: series-stiffness RCSA composition (the stack-up), kinematic-config input, multi-axis force projection. Next concrete unit for a fresh session: wire ProductEngine.sfcCalculate (or routes/sfc.ts /calculate) to delegate to the NineAxis/Ultimate path, under physics-reviewer.

**PRODUCTENGINE INTERNALS CONFIRMED (verified read ProductEngine.ts:734-759):** `sfcCalculate(params: SFCInput)` computes via an INTERNAL `calculateSpeedFeed(...)` (:755) + `SFC_GOAL_SCALERS` (cost/balanced/productivity Vc/fz tuning, :728) + inline `calculateSafetyScore` (force check :701) + `resolveMaterial`. It is a SIMPLIFIED self-contained speed/feed calc -- it does NOT call SpeedFeedNineAxisOrchestratorEngine / UltimateSpeedFeed / CuttingForceEngine, so the live customer result has NO resultant force, NO coupled chatter/thermal/deflection. The keystone wire = make sfcCalculate delegate to the NineAxis/Ultimate path (which already produces sfc.forces.resultant_force_N + chatter + thermal). PRE-WIRE BASELINE (capture before wiring, the before-oracle for regression): `sfcCalculate` is a module-level fn (export status unconfirmed) -- capture via the prism_product:sfc_calculate dispatcher round-trip OR the live :3100 /api/v1/sfc/calculate route across a spanning grid, BEFORE the wire changes published values. This is the fresh session's step-0.

**EVIDENCE (verified greps, SpeedFeedOrchestratorEngine.ts unless noted):**
- Force model: only `tangential_force_N` (Fc) computed/surfaced (:271, :3242-3310, :3750). ZERO hits for feed_force/radial_force/passive_force/resultant/force_vector. Base orchestrator does NOT import CuttingForceEngine/MillingPhysicsKernel/CrossPhysicsCoupling (only comments ref the NineAxis pattern @:48/:1162). => base SFC = single scalar force, not the 3-component (Fc,Ff,Fp) system + resultant.
- BUT the physics EXISTS elsewhere: `feed_force_N|radial_force_N|passive_force_N|thrust_force|resultant_force` matched 25+ engines incl. CuttingForceEngine, MillingPhysicsKernelEngine, AdvancedCuttingPhysicsEngine, AdvancedCuttingMathEngine, CrossPhysicsCouplingEngine, SpeedFeedNineAxisOrchestratorEngine, PartOffForceEngine, LatheThermodynamicsEngine, FiveAxisToolpathSynthesisEngine, FiveAxisDeepLearningEngine, ForceCapabilityEngine.
- Stiffness: holder `stiffness_factor` scalar multipliers (shrink 1.00/hydraulic 0.95/ER 0.80/Weldon 0.85/milling_chuck 0.90 @:734-738) + workholding `rigidity_factor` (:771-777) + a `system_stiffness_n_m?` input (:217) + single tool cantilever deflection (delta=F*L^3/3EI on `overhang_length`). This is SCALAR DERATING, NOT a physical series compliance 1/k_total = sum(1/k_i). Lathe WORKPIECE overhang (stock stickout, the part deflecting) is NOT in the chain (session probe: workpiece_length/width are INERT for milling; only workpiece_height clamps ap).
- Spindle: nat_freq from "catalog/kinematic data" (:387, :1357); torque curves exist (machine-torque-curves.ts); SPINDLE_DRIVE_EFFICIENCY over-power fix landed (2026-06-23). Reasonable, but torque-curve-caps-MRR-per-RPM in the base path UNCONFIRMED.
- Kinematics (table vs head): NO kinematicConfig/tableMoving/headMoving/movingMass input found in the base orchestrator. Machine is an archetype (rigidity/power/taper). GAP.
- The richer `SpeedFeedNineAxisOrchestratorEngine` (the one oscar has been hardening all session -- radial-engagement, runout-derate, deflection-Vc-lever, overpower-efficiency) HAS feed/radial force fields and is likely the more complete model. WHICH orchestrator the live SFC routes call (base vs 9-axis) is THE pivotal unknown.

**PER-DIMENSION (CAN / PARTIAL / GAP):**
| Dimension | State | Severity |
|---|---|---|
| Simultaneous 3-component forces (Fc/Ff/Fp + resultant) | PARTIAL -- exists in CuttingForceEngine/NineAxis, NOT in base orchestrator (scalar Fc) | HIGH |
| Series stiffness stack-up (tool+body+holder-gauge+spindle; lathe workpiece overhang) | PARTIAL -- scalar factors + single cantilever, no real series chain, workpiece not in chain | HIGH |
| Spindle strength+type (torque curve, taper, preload->stiffness/freq) | PARTIAL-GOOD -- curves+efficiency exist; per-RPM MRR cap unconfirmed | MEDIUM |
| Machine kinematics (table vs head vs combos) | GAP -- not a modeled input | MEDIUM |
| 4/5-axis + mill-turn force projection (lead/tilt -> axis forces) | EXISTS-SEPARATELY -- FiveAxis/NineAxis engines exist, base is 3-axis-orthogonal; live wiring unconfirmed | HIGH |
| Live-callable per machine (all levers accepted) | PARTIAL -- routes/OrchestratorInput accept many fields but geometry runs at defaults (proven); which orchestrator the live route hits unknown | HIGH |

**DEPENDENCY-ORDERED ROADMAP (R13):**
1. [audit, FRESH CONTEXT] CONFIRM which orchestrator the live SFC routes (`routes/sfc.ts` /calculate -> prism_product sfc_calculate -> ?) call (base `SpeedFeedOrchestratorEngine` vs `SpeedFeedNineAxisOrchestratorEngine`), and READ NineAxis + CrossPhysicsCouplingEngine + CuttingForceEngine bodies to determine if Fc/Ff/Fp are combined simultaneously. This decides wire-vs-build for everything below.
2. [wiring] Surface the 3-component force system + resultant in the live output; feed the RESULTANT (not just Fc) into deflection + workholding checks.
3. [new-engine-modeling] Real series-stiffness module: 1/k_total = sum(1/k_i) over tool-flute + tool-body + holder-gauge-length + holder-type + spindle-nose; + workpiece overhang (chuck/steady-rest/tailstock support) for turning.
4. [new-engine-modeling] kinematic-config input (table/head/gantry/combo) + dynamic-stiffness + moving-mass modifier.
5. [wiring+modeling] multi-axis force projection (lead/tilt angle -> machine-axis force components) wired into the live 5-axis/mill-turn path.
6. [validation] extend the invariant-validation pattern (scripts/sfc-stock-geometry-validate{,-lathe}.mjs shipped this session) to each.

**ALREADY CORRECT (don't rebuild):** scalar Kienzle Fc, Taylor tool life, single-cantilever deflection, holder/workholding scalar derating, spindle torque curves + drive efficiency, stability zone/p_chatter, stock-height ap-clamp (validated 192 combos/0 viol this session), turning Vc/RPM/G50-cap (validated 42 combos/0 viol). The 3-component + cross-physics + 5-axis ENGINES exist -- this is substantially a WIRING + integration problem, not a greenfield physics build.

Pairs with [[reference_oscar_sfc_variability_coverage_gapmap_2026_06_25]] + [[reference_oscar_sfc_physics_fidelity_program_2026_06_15]].
