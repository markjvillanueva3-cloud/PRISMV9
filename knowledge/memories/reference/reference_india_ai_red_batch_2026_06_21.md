---
name: reference_india_ai_red_batch_2026_06_21
description: "Newly-surfaced india AI-domain RED batch (2026-06-21) after the neural+LoRA substrate was cleared — root-caused, queued for next session. Headline = a CNCControllerDeepLearning GHOST build."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.614Z
aliases: reference_india_ai_red_batch_2026_06_21
---


**Context (slot:india, 2026-06-21).** After clearing the entire prism_ai dispatcher + LoRA/RAG/learning + CrossProcess/Neural RED surface (see [[reference_xproc_fallthrough_severed_2026_06_20]], [[reference_stale_multicomponent_test_reconcile_2026_06_20]]), a bounded sweep of the remaining AI groups (embedding/consensus/octopus/GNN/deep-learning/reasoning/intelligence) surfaced **6 RED files / 56 fails**, root-caused below. NOT started this session — the headline is a multi-method engine BUILD that cannot be completed+verified in a bounded late-session turn (R6/R12), so it is queued honestly rather than shipped partial.

**1. `CNCControllerDeepLearningEngine.learned.test.ts` — RESOLVED 2026-06-21, commit `427ec9c69a` (U-AITRAIN-POST-CNC-CONTROLLER-DL-STEP3-4). 34/34. See [[reference_cnc_controller_learned_ghost_build_2026_06_21]] (engine ingest+inference built, dispatcher `controller_ingest_learned` de-stubbed from a `deepReason` stub with a false "no file-loading API" comment, ledger schemaVersion DRAFT->1.0.0). Original GHOST-build scope below for the record:** GHOST BUILD, not a test fix. The engine HAS `recommendMacro` (`:549`) + `generateMacro` (`:559`) but LACKS `ingestLearnedPatterns` + `getLearnedPatternStats` entirely (`git log -S` finds no history → TDD-ahead, never implemented). The test specifies a full learned-pattern subsystem: `ingestLearnedPatterns(ledgerPath)` (load+replace, accurate counts, throws on empty-string path), `getLearnedPatternStats()` (`{loaded:false}` before ingest), learned-aware `recommendMacro`/`generateMacro` (seed the okuma template with learned V-variables after ingest; leave non-okuma controllers unaffected; deterministic byte-identical templates; null for a controller with no learned patterns), a real-ledger path (`data/state/learned-cnc-controller-patterns.json` — PRESENT, 60KB), a JM-Die corpus mine→ingest→recommend E2E, and dispatcher round-trip actions `prism_ai:controller_ingest_learned` + `controller_recommend_macro`. BUILD scope: implement the 2 missing methods + integrate the learned layer into recommend/generate + verify the 2 dispatcher actions are wired. Read the ledger schema + the full test (it IS the spec) first.

**2-6. Smaller cross-domain RED (1-2 fails each):**
- `LatheSpeedFeedDeepLearningAdvisorEngine.test.ts` (2) + `LatheSpeedFeedReasoningBridgeEngine.test.ts` (1): **RESOLVED 2026-06-21, commit `4c7c558ede` (U-LATHE-AI-BRIDGE-RED).** Advisor 2 = resolver incompleteness: `aluminum_6061`/`inconel_718`/`stainless_304` were not in `AISI_ALIAS` nor canonical DB keys (canon = `6061`/`Inconel 718`/`304`) -> resolveMaterial null -> success:false,confidence:0. Fix: 5 additive `AISI_ALIAS` entries -> existing canon keys (matching ss304/ss316 + al6061/al7075 sibling-pair convention); NO physics number changed. Bridge 1 = FP rounding-boundary TEST bug: `toBeCloseTo(avg, 2)` (strict <0.005) vs engine-rounded overall; `0.575*100`=57.4999... IEEE-754 -> round 0.57 -> diff exactly 0.005 fail. Fix: compare vs `round(avg*100)/100` at precision 10 (exact, not weakened). 65/65.

**7. `MaterialDatabaseEngine-U-AWR16.test.ts` + `u-arch3-material-resolution.test.ts` + `wedm-5material-validation.test.ts` (~147 fails / 8 files in the broad material sweep) -- NEW NEXT UNIT, PRE-EXISTING (empirically proven NOT caused by the U-LATHE alias add: file-scoped `git checkout HEAD -- constants.ts` A/B gave IDENTICAL `35 failed|58 passed` on the 3 reps with and without my change).** Root: a CANONICAL-KEY NAMING GAP / contested convention (R7). `CANONICAL_MATERIAL_DB` is keyed by SHORT forms (`6061`, `304`, `Inconel 718`) but `u-arch3` asserts `CANONICAL_MATERIAL_DB.aluminum_6061.kc1_1===700` / `.stainless_304.kc1_1===2100` / `.inconel_718.kc1_1===3000` (descriptive keys -> `undefined.kc1_1` throws) and `U-AWR16` asserts `AISI_CUTTING_COEFFICIENTS` coverage (>=25 grades: 4140/4340/8620, 6061-T6/7075-T6/2024-T3, 303/304/316/17-4, Ti-6Al-4V/Inconel 718/Hastelloy, plastics, etc.) -- a SEPARATE export from AISI_ALIAS. This is PHYSICS-CRITICAL + huge blast radius (147 tests, many consumers) + a genuine crossroad (short-key-canonical+descriptive-alias vs descriptive-key-canonical+short-alias). DO NOT impulse-fix at session tail: read the U-AWR16 + u-arch3 specs first (they ARE the intended contract), decide the convention (likely run `brainstorm-path-forward` or surface to operator), and treat the CANONICAL_MATERIAL_DB / AISI_CUTTING_COEFFICIENTS build (`buildMaterialPhysics`, constants.ts ~L120/L1367) as the unit -- physics-reviewer gated. My U-LATHE aliases are convention-neutral + robust to either outcome (additive descriptive->short alias paths), so they do not pre-bias this decision.
- `P5OrphanReasoningDispatchers.test.ts` (1): **RESOLVED 2026-06-21, commit `d040acddf6` (U-P5-DIAGNOSE-SOURCE-CANONICAL).** **R12 CORRECTION of the prior PRIME-SUSPECT below -- it was WRONG.** The real cause: the test passed symptoms with `source:"machine"`, which is NOT in the canonical `DiagnosticReasoningEngine.Symptom.source` enum (`alarm|operator|sensor|visual|audio`, engine type L49) that `intelligenceActionSchemas.diagnose_failure` mirrors exactly (L155). So the Zod validator REJECTED the params (`"symptoms: Invalid input"`) BEFORE the engine ran -> dispatcher returned `{success:false,...}`, top-level `diagnosis_id` undefined. The engine is permissive (`normalizeSymptom` L1305 stores `source` without enum-validating, defaults `"operator"`), which masked it -- the direct-engine sibling test passed. Fix (R9): 4x `source:"machine"`->`"sensor"` in the test (contract-valid input); schema + engine type LEFT UNCHANGED (NOT weakened). 21/21 now pass. **The discarded PRIME SUSPECT (kept as a lesson): "pressure>50 slim path drops diagnosis_id" was FALSE -- `getCurrentPressurePct()` is a backward-compat stub returning 0 always (responseSlimmer.ts:53-55), so the slim path (intelligenceDispatcher.ts:2643-2654) is DEAD CODE.** Lesson: confirm a dispatcher-drops-field hypothesis with a throwaway probe (engine-direct vs dispatcher-raw) BEFORE editing -- I'd recorded a confident wrong cause without reading `getCurrentPressurePct`'s body. SEPARATE latent observation (not a regression, a design question): the entire pressure-aware response slimming in intelligenceDispatcher has been inert since the 2026-04-25 stub.
- (2 more files in the failing set — re-run the sweep to enumerate: `npx vitest run src/__tests__/*DeepLearning*.test.ts src/__tests__/*Reasoning*.test.ts src/__tests__/*Intelligence*.test.ts`).

**How to apply (next session):** start with P5OrphanReasoningDispatchers (likely a bounded routing fix, india-domain) → LatheSpeedFeed pair (verify stale-test vs engine, defer deep lathe physics to whiskey) → then the CNCController GHOST build as a dedicated forge-team unit (engine methods + dispatcher wiring + per-file scrutiny + real reference-value tests against the 60KB canonical ledger). Sweep was bounded; full AI-substrate enumeration may surface more — re-sweep each group.
