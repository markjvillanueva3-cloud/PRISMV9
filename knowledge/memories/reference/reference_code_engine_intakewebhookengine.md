---
name: reference_code_engine_intakewebhookengine
description: "IntakeWebhookEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/IntakeWebhookEngine.ts
  sourceHash: c44d9ff7
  loc: 212
aliases: IntakeWebhookEngine
---

# IntakeWebhookEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/IntakeWebhookEngine.ts` (212 LOC)

## What it is
IntakeWebhookEngine

```
IntakeWebhookEngine
===================

OBSIDIAN-COMPOUND-MS1/S3/U-CAPTURE-WEBHOOK

Validates external webhook ingests from personal-knowledge sources
(Readwise, Telegram bot, RSS, manual paste) and lands them in
`knowledge/memories/inbox/<source>-<ts>-<sig8>.md` with frontmatter.

Pure-logic boundary: takes raw body bytes + signature + source string,
returns {ok, status, file?, error?}. Knows nothing about Express, so
tests run without spinning up a server.

Security guarantees enforced before any disk write:
  1. Payload size ≤ 256 KB                              → 413
  2. Source ∈ {readwise, telegram, rss, manual}         → 400 otherwise
  3. Signature header present                           → 401 otherwise
  4. HMAC-SHA256 over raw body, timing-safe compare     → 401 otherwise
  5. JSON parses to a non-null object with content      → 400 otherwise
  6. Optional payload.timestamp w
```


## Exports
`INTAKE_SOURCES` (const), `IntakeSource` (type), `PAYLOAD_CAP_BYTES` (const), `RATE_LIMIT_WINDOW_MS` (const), `RATE_LIMIT_MAX` (const), `TS_SKEW_MAX_MS` (const), `DEFAULT_INBOX_ROOT` (const), `IngestParams` (interface), `IngestOptions` (interface), `IngestResult` (interface), `IntakeWebhookEngine` (class), `intakeWebhookEngine` (const)

## Key methods
`ingest`, `mkdirSync`, `writeFileSync`, `resetRateLimits`, `rateLimitState`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/IntakeWebhookEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find IntakeWebhookEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/IntakeWebhookEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
