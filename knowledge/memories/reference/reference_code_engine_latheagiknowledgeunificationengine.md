---
name: reference_code_engine_latheagiknowledgeunificationengine
description: "LatheAGIKnowledgeUnificationEngine — U-LTH60 (LATHE-MASTER PX-S1)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LatheAGIKnowledgeUnificationEngine.ts
  sourceHash: 59af6dea
  loc: 1277
aliases: latheAGIKnowledgeUnificationEngine
---

# latheAGIKnowledgeUnificationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LatheAGIKnowledgeUnificationEngine.ts` (1277 LOC)

> [physics] Imports canonical physics constants (`src/physics/constants.ts`) -- safety-relevant node.

## What it is
```
LatheAGIKnowledgeUnificationEngine — U-LTH60 (LATHE-MASTER PX-S1)

Unifies three knowledge surfaces into a single query endpoint:
  1. Job knowledge graph    — (material, tool, strategy, customer, part)
  2. Physics/formula index  — Kienzle/Taylor/deflection canonical constants
  3. Tribal tips            — shop-floor empirical knowledge by domain

API:
  - upsertNode(type, id, props)
  - upsertEdge(from, to, label, props?)
  - query({ type?, id?, label_contains?, hops? }) → nodes + edges
  - traceReasoning(query) → ≥ 5-step chain of graph hops

Traversal: BFS with depth limit. Each hop emits a TraceStep so a
reasoning consumer (U-LTH58 bridge) can surface the provenance.

Persistence: state/shared/lathe-agi-knowledge-state.json v1.

@milestone LATHE-MASTER U-LTH60
```


## Exports
`NODE_TYPES` (const), `NodeType` (type), `UpsertNodeInputSchema` (const), `UpsertNodeInput` (type), `UpsertEdgeInputSchema` (const), `UpsertEdgeInput` (type), `QueryInputSchema` (const), `QueryInput` (type), `TraceInputSchema` (const), `TraceInput` (type), `Node` (interface), `Edge` (interface), `KnowledgeState` (interface), `QueryResult` (interface), `ReasoningStep` (interface), `ReasoningTrace` (interface), `TribalSeedStatus` (type), `LatheTribalSourceFn` (type), `NormalizedTribalTip` (interface), `defaultLatheTribalSource` (function)

## Key methods
`latheConsensusKitSeam`, `upsertNode`, `upsertEdge`, `query`, `traceReasoning`, `stats`, `getTribalSeedStatus`, `orchestrate`, `publishOutcome`, `atomicWriteJson`

## Depends on
[[reference_code_engine_latheagifeaturebridgeengine]] | [[reference_code_engine_latheagicontinuouslearningengine]] | [[reference_code_engine_latheagisafetycontainmentengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/LatheAGIKnowledgeUnificationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find latheAGIKnowledgeUnificationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LatheAGIKnowledgeUnificationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
