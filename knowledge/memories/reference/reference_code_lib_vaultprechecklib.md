---
name: reference_code_lib_vaultprechecklib
description: "scripts/lib/vault-precheck-lib.mjs"
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/vault-precheck-lib.mjs
  sourceHash: c5b7192d
  loc: 199
aliases: vault-precheck-lib.mjs
---

# vault-precheck-lib.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/vault-precheck-lib.mjs` (199 LOC)

## What it is
```
scripts/lib/vault-precheck-lib.mjs
SUBSTRATE-UTIL Gap 8 slice-a (2026-06-29, slot:sierra) -- shared single-source for the
obsidian-vault-precheck hook AND its body-excerpt sidecar generator.

WHY: obsidian-vault-precheck-inject.mjs scored FILENAMES only -> a prompt whose terms live in
a file BODY but not its name missed (e.g. "Okuma OSP threading" misses gsd/DEV_PROTOCOL.md).
Reading every file body per-prompt is too slow, so we mirror the proven memory-index-sidecar
pattern: a generator pre-extracts {basename, first-250-char excerpt} per file into a sidecar;
the hook scores basename (strong) + excerpt (weak, body recall) from it, and falls back to a
live basename-only walk when the sidecar is absent (fail-soft -- never worse than today).

SINGLE-SOURCE: SCAN_DIRS + tokenizer + listFiles live HERE so the hook and the generator can
never drift (the hook used to carry them inline; slice-b 
```


## Exports
`ROOT` (const), `REPO_ROOT` (const), `SIDECAR_PATH` (const), `BUILDER_SCRIPT` (const), `REGEN_STAMP_PATH` (const), `REGEN_THROTTLE_MS` (const), `SCHEMA_VERSION` (const), `MAX_FILES_PER_DIR` (const), `MAX_TOKEN_LEN` (const), `MIN_TOKEN_LEN` (const), `EXCERPT_CHARS` (const), `MIN_SCORE` (const), `TOPK` (const), `BODY_SCORE_CAP` (const), `SCAN_DIRS` (const), `SPECS_ROOT` (const), `SCAN_TARGETS` (const), `tokenize` (function), `listFiles` (function), `extractExcerpt` (function)

## Key methods
`String`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/vault-precheck-lib.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find vault-precheck-lib.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/vault-precheck-lib.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
