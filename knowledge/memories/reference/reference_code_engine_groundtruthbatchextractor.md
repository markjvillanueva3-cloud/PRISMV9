---
name: reference_code_engine_groundtruthbatchextractor
description: "orchestrates the full 4-stage ground-truth"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/GroundTruthBatchExtractor.ts
  sourceHash: c92fd7c5
  loc: 740
aliases: GroundTruthBatchExtractor
---

# GroundTruthBatchExtractor -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/GroundTruthBatchExtractor.ts` (740 LOC)

## What it is
orchestrates the full 4-stage ground-truth

```
GroundTruthBatchExtractor — orchestrates the full 4-stage ground-truth
pipeline across the 20K-file CAD corpus with bounded concurrency, atomic
checkpointing, idempotent resume, and a per-format coverage report.

Pipeline (per file):
  1. STEP export      via CADToSTEPPipelineEngine (U-CGT03)
  2. Feature tree     via GroundTruthFeatureTreeExtractor (U-CGT04)
  3. Dimensional sig  via DimensionalSignatureEngine (U-CGT05)
  4. Screenshots      via CADScreenshotCapturer (U-CGT06)

Output bundle layout (per fileId):
  {outputRoot}/{fileId}/source.step
  {outputRoot}/{fileId}/feature-tree.json
  {outputRoot}/{fileId}/dimensional-signature.json
  {outputRoot}/{fileId}/screenshots/{view}.png
  {outputRoot}/{fileId}/bundle.json   ← per-file manifest with status

Checkpoint layout:
  {outputRoot}/_checkpoints/{runId}.json

Concurrency: bounded N-worker semaphore (default 4). Per-task error is
is
```


## Exports
`BundleStatus` (type), `BatchTask` (interface), `BatchTaskSchema` (const), `BundleResult` (interface), `BundleResultSchema` (const), `CheckpointState` (interface), `CheckpointStateSchema` (const), `CoverageReport` (interface), `BatchOptions` (interface), `BatchExtractorDeps` (interface), `GroundTruthBatchExtractor` (class), `stepOutPath` (const), `groundTruthBatchExtractor` (const)

## Key methods
`extractBatch`, `generateCoverageReport`, `asTestRunner`, `validate`, `drop`

## Depends on
[[reference_code_engine_cadtosteppipelineengine]] | [[reference_code_engine_groundtruthfeaturetreeextractor]] | [[reference_code_engine_dimensionalsignatureengine]] | [[reference_code_engine_cadscreenshotcapturer]]

## Where it lives
- **Source:** `mcp-server/src/engines/GroundTruthBatchExtractor.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find GroundTruthBatchExtractor` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/GroundTruthBatchExtractor.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
