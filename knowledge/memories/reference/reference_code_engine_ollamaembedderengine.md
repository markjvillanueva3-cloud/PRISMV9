---
name: reference_code_engine_ollamaembedderengine
description: "OllamaEmbedderEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/OllamaEmbedderEngine.ts
  sourceHash: 01d0670d
  loc: 203
aliases: OllamaEmbedderEngine
---

# OllamaEmbedderEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/OllamaEmbedderEngine.ts` (203 LOC)

## What it is
OllamaEmbedderEngine

```
OllamaEmbedderEngine
====================

OBSIDIAN-AUTOMATE-MS3/U-EMBEDDING-CONNECTIONS

Lightweight wrapper around Ollama's `/api/embeddings` endpoint specialized
for the daily-brief use case: embed many short documents, then compute
pairwise cosine similarities and return them keyed by canonical pairKey
(alphabetical join of two paths).

Why this and not the existing QdrantMemoryEngine? QdrantMemoryEngine
persists embeddings into Qdrant collections — overkill for a daily brief
where the corpus is the last-7d window of vault notes (typically <100
docs). This engine embeds in-memory, computes pairs, hands the map
back, and forgets — no persistence, no vector store dependency.

Determinism + testability:
  - The HTTP fetch is injected via opts.fetchImpl (defaults to globalThis.fetch).
  - For unit tests, callers pass a deterministic mock that returns
    stable vectors per input text.
  
```


## Exports
`EmbeddingResult` (interface), `EmbeddingError` (interface), `EmbedOutcome` (type), `FetchLike` (type), `OllamaEmbedderOptions` (interface), `PairwiseInput` (interface), `PairwiseOutcome` (interface), `pairKey` (function), `cosine` (function), `OllamaEmbedderEngine` (class), `ollamaEmbedderEngine` (const)

## Key methods
`embed`, `clearTimeout`, `embedMany`, `pairwiseCosine`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/OllamaEmbedderEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find OllamaEmbedderEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/OllamaEmbedderEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
