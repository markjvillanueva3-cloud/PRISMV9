---
name: reference_code_engine_billingengine
description: "BillingEngine — SaaS billing for PRISM multi-tenant deployment"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/BillingEngine.ts
  sourceHash: c57877e5
  loc: 749
aliases: billingEngine
---

# billingEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/BillingEngine.ts` (749 LOC)

## What it is
```
BillingEngine — SaaS billing for PRISM multi-tenant deployment

Self-contained business logic for:
  - Subscription plans (free / shop / team / enterprise)
  - Usage-based tiered post pricing
  - Checkout session lifecycle (pending / completed / expired)
  - One-time post top-up purchases
  - Webhook event handling with HMAC-SHA256 signature verification
  - Stats aggregation (MRR, ARR, churn, MTD revenue)

Architecture:
  Returns Stripe-shaped objects (session_id, url, etc.) but makes zero
  network calls. A future StripeAdapterEngine can wrap this engine to
  execute real Stripe API calls against live sessions. This split keeps
  tests deterministic and preserves the business logic as the source of
  truth for pricing, entitlements, and lifecycle transitions.

Persistence:
  State lives at state/shared/billing-state.json with schemaVersion=1.
  Writes are atomic (utils/atomicSessionWri
```


## Exports
`Plan` (interface), `PLAN_CATALOG` (const), `PostPriceTier` (interface), `POST_PRICE_TIERS` (const), `PostCheckoutInputSchema` (const), `CheckoutInputSchema` (const), `PortalInputSchema` (const), `CalcPostPriceInputSchema` (const), `WebhookInputSchema` (const), `Subscription` (interface), `CheckoutSession` (interface), `WebhookEventLog` (interface), `BillingState` (interface), `BillingStats` (interface), `billingEngine` (const), `BillingEngine` (re-export)

## Key methods
`getPlans`, `getPostPrices`, `calcPostPrice`, `createCheckout`, `createPortal`, `createPostCheckout`, `handleWebhook`, `stats`, `computeSignature`, `atomicWriteJson`, `mkdirSync`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/BillingEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find billingEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/BillingEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
