---
name: reference_code_engine_gcodebidirectionaloptimizerengine
description: "closes the inverse loop. Uses"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/GCodeBidirectionalOptimizerEngine.ts
  sourceHash: 7e1926a1
  loc: 285
aliases: GCodeBidirectionalOptimizerEngine
---

# GCodeBidirectionalOptimizerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/GCodeBidirectionalOptimizerEngine.ts` (285 LOC)

## What it is
closes the inverse loop. Uses

```
GCodeBidirectionalOptimizerEngine — closes the inverse loop. Uses
the runtime predictor + reverse-CAD outputs to identify and propose
concrete G-code optimizations the operator can apply to the source
program.

The bidirectional value: now that we can predict cycle time AND
recover the part geometry, we can answer "if I changed X in the
program, the part would still be correct and cycle time would drop
by Y seconds". That's the operator-facing pitch.

Categories of optimization the loop produces:
  1. Block consolidation — adjacent G1 blocks with same modal F that
     could fit a G1 line + arc rather than many short segments
  2. Feed-rate balancing — blocks below their machine-limited feed
     that the prior block's exit velocity could have carried higher
  3. Rapid-conversion candidates — G1 blocks above stock that should
     be G0 (reverse-CAD flags them as air cuts)
  4. Tool sequ
```


## Exports
`OptimizationRecommendation` (interface), `BidirectionalOptimization` (interface), `GCodeBidirectionalOptimizerEngine` (class), `gcodeBidirectionalOptimizerEngine` (const)

## Key methods
`optimize`, `flushCluster`

## Depends on
[[reference_code_engine_gcoderuntimepredictorengine]] | [[reference_code_engine_gcodereversecadengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/GCodeBidirectionalOptimizerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find GCodeBidirectionalOptimizerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/GCodeBidirectionalOptimizerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
