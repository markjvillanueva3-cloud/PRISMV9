---
name: reference_code_engine_hypermillinhostrunnerengine
description: "Hyper Mill In Host Runner"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HyperMillInHostRunnerEngine.ts
  sourceHash: 2835eac1
  loc: 509
aliases: HyperMillInHostRunnerEngine
---

# HyperMillInHostRunnerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HyperMillInHostRunnerEngine.ts` (509 LOC)

## What it is
Hyper Mill In Host Runner

```
HyperMillInHostRunnerEngine — U-CAMTEST01
==========================================

PHASE-8: PRISM-side companion for the hyperMILL in-host Python test
runner (resources/hypermill/prism_test_runner.py). The Python runner
loads inside hyperMILL 2024 via `om.cad` / `om.cam`, sends scenario
descriptors and per-frame assertion results here through the
CAMPluginCommunicationHub. This engine:

  - Registers the in-host runner as a hub plugin at boot
  - Accepts a scenario descriptor (part + stock + material + tool)
    and expands it into a list of expected overlay frame envelopes
  - Ingests assertion results streamed back from the Python runner
    and folds them into a per-scenario ScenarioResult
  - Enforces the U-CAMTEST assertion contract:
      frame arrival monotonic, latency p99 < 100 ms, band-transition
      count matches cut geometry, hard-stop within 3 frames of a
      delibera
```


## Exports
`ScenarioCategorySchema` (const), `ScenarioCategory` (type), `ScenarioDescriptorSchema` (const), `ScenarioDescriptor` (type), `PlannedFrameSchema` (const), `PlannedFrame` (type), `ScenarioPlanSchema` (const), `ScenarioPlan` (type), `ObservedFrameSchema` (const), `ObservedFrame` (type), `ScenarioResultSchema` (const), `ScenarioResult` (type), `AssertionNameSchema` (const), `AssertionName` (type), `AssertionResultSchema` (const), `AssertionResult` (type), `ScenarioSummarySchema` (const), `ScenarioSummary` (type), `RunnerStatsSchema` (const), `RunnerStats` (type)

## Key methods
`register`, `planScenario`, `frameToEnvelope`, `summarize`, `getPlan`, `getStats`, `resetSession`, `resetAll`

## Depends on
[[reference_code_engine_camplugincommunicationhubengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/HyperMillInHostRunnerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HyperMillInHostRunnerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HyperMillInHostRunnerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
