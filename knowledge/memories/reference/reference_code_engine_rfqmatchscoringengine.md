---
name: reference_code_engine_rfqmatchscoringengine
description: "the Phase-0 PRODUCTION RFQ→supplier matcher of the PRISM manufacturing"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/RFQMatchScoringEngine.ts
  sourceHash: 9d2a5d7a
  loc: 480
aliases: RFQMatchScoringEngine
---

# RFQMatchScoringEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/RFQMatchScoringEngine.ts` (480 LOC)

## What it is
the Phase-0 PRODUCTION RFQ→supplier matcher of the PRISM manufacturing

```
RFQMatchScoringEngine — the Phase-0 PRODUCTION RFQ→supplier matcher of the PRISM manufacturing
networking marketplace (galaxy:business, slot:hotel). Joins an RFQ's requirements to the
{@link SupplierCapabilityProfileEngine} registry, HARD-FILTERS candidates by declared capability,
then RANKS the survivors by a multi-criteria TOPSIS decision into an EXPLAINED shortlist.

This is explicitly the TOPSIS matcher — NOT a GNN. It is the deterministic, fully-auditable Phase-0
production matcher the marketplace ships first; a learned GNN re-ranker is a documented future layer
(see §MAIN-WIRING). Every ranking decision here is traceable to a profile field and a cited weight.

PIPELINE (scoreShortlist):
  1. HARD FILTER — for every candidate call {@link SupplierCapabilityProfileEngine.canSatisfy}; drop
     any whose `capable===false`, but RECORD it in `excluded[]` with its `gaps` (fail-loud
     t
```


## Exports
`PartEnvelopeMm` (interface), `Rfq` (interface), `ScoreShortlistInput` (interface), `ShortlistCriteria` (interface), `ShortlistEntry` (interface), `ExcludedEntry` (interface), `ScoreShortlistResult` (interface), `ScoreShortlistArgs` (type), `RFQMatchScoringEngine` (class), `rfqMatchScoringEngine` (const), `RFQ_MATCH_SUPPORTED_PROCESSES` (const), `RFQ_MATCH_SUPPORTED_MATERIAL_GROUPS` (const)

## Key methods
`scoreShortlist`, `appendWinner`

## Depends on
[[reference_code_engine_suppliercapabilityprofileengine]] | [[reference_code_engine_topsisengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/RFQMatchScoringEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find RFQMatchScoringEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/RFQMatchScoringEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
