---
name: reference_code_engine_cammodelservingengine
description: "CAM-EXHAUST-MS0/U-CAM122"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMModelServingEngine.ts
  sourceHash: 9ec90c0d
  loc: 1191
aliases: CAMModelServingEngine
---

# CAMModelServingEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMModelServingEngine.ts` (1191 LOC)

## What it is
CAM-EXHAUST-MS0/U-CAM122

```
CAMModelServingEngine — CAM-EXHAUST-MS0/U-CAM122

Production model-serving fabric for the CAM AGI arc (U-CAM112-121).
Sits between dispatcher/orchestrator callers and the underlying model
backends (Ollama, NVIDIA Triton, vLLM, NIM) and provides:

  1. Model registry — register/version/retire models with status lifecycle
     (pending → shadow → canary → active → retired) and per-model
     rollout weights.

  2. Deterministic A/B + canary routing — every (cam_system, task) pair
     has a routing policy. Requests are routed by deterministic FNV-1a
     bucketing of `request_key` (operator-id, job-id, etc.) so the same
     logical request always lands on the same model bucket — required
     for sticky reproducibility and for shadow-vs-active comparison
     under identical inputs.

  3. SLO tracking — ring-buffered latency + error samples per model.
     Computes p50 / p95 / p99 / mean 
```


## Exports
`ModelStatus` (type), `ModelBackend` (type), `RoutingPolicyKind` (type), `ModelSpec` (interface), `Model` (interface), `RoutingPolicy` (interface), `RouteRequest` (interface), `RouteDecision` (interface), `MetricSample` (interface), `ModelHealth` (interface), `ConfirmationEnvelope` (interface), `BatchEnqueueResult` (interface), `BatchDrainResult` (interface), `RateLimitDecision` (interface), `CAMModelServingEngine` (class), `__testing` (const)

## Key methods
`ensureFinite`, `ensureNonEmpty`, `registerModel`, `ensureRange`, `ensurePositiveInt`, `deregisterModel`, `modelOrThrow`, `listModels`, `getModel`, `updateModelEndpoint`, `setRoutingPolicy`, `getRoutingPolicy`, `listRoutingPolicies`, `routeRequest`, `deployShadow`, `pushConfirmation`, `promoteToCanary`, `promoteToActive`, `demoteFromActive`, `rollbackCanary`, `retireModel`, `recordMetric`, `getModelHealth`, `listAllHealth`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CAMModelServingEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMModelServingEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMModelServingEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
