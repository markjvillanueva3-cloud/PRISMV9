---
name: reference_code_engine_crossprocesstierrouterengine
description: "XPROC-NEURAL Tier 12 (T12-01)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CrossProcessTierRouterEngine.ts
  sourceHash: 022d5d82
  loc: 339
aliases: CrossProcessTierRouterEngine
---

# CrossProcessTierRouterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CrossProcessTierRouterEngine.ts` (339 LOC)

## What it is
XPROC-NEURAL Tier 12 (T12-01)

```
CrossProcessTierRouterEngine — XPROC-NEURAL Tier 12 (T12-01)

Query classifier that routes natural-language operator questions to the
correct XPROC-NEURAL tier(s). Without this, every caller has to memorize
"is this a T9-03 question or a T11-04 question?" — the router carries
that knowledge so the orchestrator (T12-02) can fan out to the right
engines.

Design: keyword + intent matching against a fixed routing table.
NOT a learned classifier — deterministic, testable, auditable. Each
routing rule has:
  - matcher: regex over normalized query text
  - intent: enum classifying the question type
  - tiers: ordered list of tiers to invoke (primary first)
  - reason: human-readable explanation tied to roadmap

Routing decisions are returned with `availability` flags indicating
which routed tiers are actually built vs. blocked on prerequisites.
The orchestrator (T12-02) consumes this to gracef
```


## Exports
`RouteInput` (type), `Intent` (type), `TierId` (type), `RoutedTier` (interface), `RouteResult` (interface), `CrossProcessTierRouterEngine` (class), `crossProcessTierRouterEngine` (const), `crossProcessTierRouter` (function)

## Key methods
`route`, `explain`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CrossProcessTierRouterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CrossProcessTierRouterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CrossProcessTierRouterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
