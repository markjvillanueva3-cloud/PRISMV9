---
name: reference_code_engine_hermesparallelbudgetenvelopeengine
description: "HZP03 parallel fan-out budget envelope."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HermesParallelBudgetEnvelopeEngine.ts
  sourceHash: 51ae5a45
  loc: 123
aliases: HermesParallelBudgetEnvelopeEngine
---

# HermesParallelBudgetEnvelopeEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HermesParallelBudgetEnvelopeEngine.ts` (123 LOC)

## What it is
HZP03 parallel fan-out budget envelope.

```
HermesParallelBudgetEnvelopeEngine — HZP03 parallel fan-out budget envelope.

Before a caller fires N parallel agents in one tool block, this engine
answers: "will the aggregate token spend fit my remaining budget?"

Pure-core: per-size-hint cost table × agent count = envelope.  Compares
against a remaining budget and returns within / over / refused, with the
suggested max parallelism that DOES fit (so the caller can degrade
gracefully instead of failing the whole fan-out).

@module engines/HermesParallelBudgetEnvelopeEngine
```


## Exports
`SizeHintSchema` (const), `SizeHint` (type), `AgentBudgetRequestSchema` (const), `AgentBudgetRequest` (type), `CostTableSchema` (const), `CostTable` (type), `FanoutBudgetRequestSchema` (const), `FanoutBudgetRequest` (type), `SONNET_COST_BY_SIZE` (const), `BudgetVerdict` (interface), `HermesParallelBudgetEnvelopeEngine` (class), `hermesParallelBudgetEnvelopeEngine` (const)

## Key methods
`estimate`, `renderVerdict`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/HermesParallelBudgetEnvelopeEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HermesParallelBudgetEnvelopeEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HermesParallelBudgetEnvelopeEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
