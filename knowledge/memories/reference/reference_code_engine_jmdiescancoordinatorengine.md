---
name: reference_code_engine_jmdiescancoordinatorengine
description: "JM-DIE-FLEET-SCAN-MS0 / U-FS02"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/JMDieScanCoordinatorEngine.ts
  sourceHash: 95f0aed2
  loc: 249
aliases: JMDieScanCoordinatorEngine
---

# JMDieScanCoordinatorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/JMDieScanCoordinatorEngine.ts` (249 LOC)

## What it is
JM-DIE-FLEET-SCAN-MS0 / U-FS02

```
JMDieScanCoordinatorEngine — JM-DIE-FLEET-SCAN-MS0 / U-FS02

Coordinates a 100K+-file scan of H:/prism/JM DIE/ by:
  1. Reading the JMDieScanLedger to know what's already scanned.
  2. Optionally reading existing baseline JSONs to extract their abs_paths
     (for the first cold run, before the ledger is seeded).
  3. Walking the archive via JMDieFleetWideIngestEngine.
  4. Computing the to_scan delta = walked - already_scanned.
  5. Splitting the delta into batches of ~batchSize files each.
  6. Tagging each batch with a suggested pipeline engine based on the
     file mix (cnc-program → GCodeTimeEstimator,
     docustrata-doc → JMDieDocustrataIngest).

This engine is the BATCH PLANNER — it does NOT itself dispatch agents.
The caller (skill / dispatcher action) runs the parallel Agent fan-out
using the BatchPlan it returns. That keeps the engine pure (R5) +
test-only-with-fs (no live ne
```


## Exports
`BatchPlanItem` (interface), `BatchPlan` (interface), `PlanSummary` (interface), `PlanOptions` (interface), `JMDieScanCoordinatorEngine` (class), `jmDieScanCoordinatorEngine` (const)

## Key methods
`loadBaselineScanned`, `plan`, `recordBatchScanned`

## Depends on
[[reference_code_engine_jmdiefleetwideingestengine]] | [[reference_code_engine_jmdiescanledgerengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/JMDieScanCoordinatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find JMDieScanCoordinatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/JMDieScanCoordinatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
