---
name: reference_code_middleware_attachuserplan
description: "attachUserPlan -- resolves the authenticated user's subscription plan + today's"
metadata:
  type: reference
  kind: code-middleware
  galaxy: unassigned
  sourcePath: mcp-server/src/middleware/attachUserPlan.ts
  sourceHash: 7d0772fb
  loc: 72
aliases: attachUserPlan
---

# attachUserPlan -- Middleware SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/middleware/attachUserPlan.ts` (72 LOC)

## What it is
```
attachUserPlan -- resolves the authenticated user's subscription plan + today's
usage onto req.user (U-COMM-03).

THE keystone wire: tierGate.requireTier and billing /status read
`req.user.plan` / `req.user.usage`, but nothing ever populated them, so every
request resolved to the "free" fail-safe (entitlement enforcement dormant).
Mount this GLOBALLY immediately after optionalToken so /status + every gated
route see the real plan.

Pure-ish: a single sync store read + a single sync counter read. No I/O on the
request hot path beyond the in-memory maps (store is lazy-loaded once).
```


## Exports
`attachUserPlan` (function), `recordFeatureUse` (function)

## Key methods
`next`

## Depends on
[[reference_code_engine_subscriptionstore]] | [[reference_code_engine_entitlementoverridestore]] | [[reference_code_engine_licensestore]]

## Where it lives
- **Source:** `mcp-server/src/middleware/attachUserPlan.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Middleware

## How to query it
Recall by meaning via `/system-viz find attachUserPlan` or `prism_session:master_index_query`; open the source at `mcp-server/src/middleware/attachUserPlan.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
