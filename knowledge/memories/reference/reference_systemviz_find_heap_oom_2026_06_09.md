---
name: reference_systemviz_find_heap_oom_2026_06_09
description: "/system-viz find OOMs on the DEFAULT node heap (reproduced ~390MB GC-thrash) but works with --max-old-space-size=8192. system-viz-query.mjs does NOT self-bump heap; skill callers invoke it via plain node → find can OOM in production. Fix (fresh budget): self-bump re-exec. Also: obsidian-vault mirror is HEALTHY (11,886 files at H:/prism/knowledge/memories, NOT H:/knowledge/memories)."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.214Z
aliases: reference_systemviz_find_heap_oom_2026_06_09
---


**2026-06-09 (slot golf, synergy /goal — /system-viz + obsidian-vault surface verification).**

**FINDING — /system-viz `find` is heap-sensitive (PSN leg #6 degradation, actionable):** `node scripts/system-viz-query.mjs find "kienzle"` on the DEFAULT node heap **OOMs** (V8 `Scavenge ... allocation failure` at ~390MB GC-thrash, no results). Re-run with `node --max-old-space-size=8192 scripts/system-viz-query.mjs find "kienzle"` → **WORKS**, returns 30 correct matches (`core.physics`, `vault.wiki.architecture.actions.calc.kienzle-*`, etc.). The `find-cache.json` is 54MB; the OOM proves `system-viz-query.mjs` does **NOT** self-bump its heap (else it wouldn't have crashed). Callers are skills that invoke it via plain `node`: `.claude/commands/{ask-local,audit-viz-first,autopilot-full,discover-tango,forge-audit-v2}.md` (+more) — none pass `--max-old-space-size`, so a `find` query can OOM in production.

**ROOT CAUSE — FULLY RESOLVED 2026-06-09 (golf), and the self-bump fix is CONTRAINDICATED (R12 correction of the caveat below):** the "~390MB crash suspiciously low for a 2GB heap" caveat was the key clue. The heap is NOT 2GB — `node` on PATH is a bash shim (`H:/.claude/bin/node` → `portable-node`) that sets `NODE_OPTIONS=--max-old-space-size=${PRISM_HOOK_HEAP_MB:-384}` **set-if-unset** (`portable-node` line 42-46) as DELIBERATE fleet commit-budgeting (cap each of 26 chats' node procs). `heap_size_limit`=432MB. Raw `H:/Tools/nodejs/node.exe` has NO cap. find's `loadFindCache`(54MB)+`findInGraph` scoring peaks ~390MB — right at the 384 cap → OOMs ONLY under box memory pressure (ran twice this turn at 466ms, 0 OOM, when quiet; the earlier "kienzle" OOM was load-amplified). portable-node's comment: heavy scripts "opt into a larger size via [their] OWN NODE_OPTIONS (respected below)"; a capped OOM "fails LOUD... never silent" = BY DESIGN.

**DO NOT add a self-bump re-exec to `find`:** find is called **~1060×/day** (viz-first-redirect.mjs — see the FIND SHORT-CIRCUIT comment in system-viz-query.mjs). A re-exec DOUBLES the spawns on the hottest path (1060→2120/day), feeding the exact Stop/fork-storm load that causes the pressure. The self-bump is right for a RARE heavy op (the tribal inject hook does it ~1×/prompt), WRONG for a 1060×/day hot path. **The only non-harmful improvement** (if find OOMs become frequent, not rare) = make `findInGraph`/`loadFindCache` in `scripts/lib/system-viz-graph.mjs` memory-frugal (stream-parse / compact cache) so the peak stays well under 384MB — a separate careful unit on the hot path, NOT a heap bump. Until then the rare load-induced OOM is acceptable (fails loud, self-heals when load drops). **Lesson: before "fixing" a borderline-OOM with a heap bump, check (1) is the cap intentional (portable-node: YES) and (2) the call FREQUENCY — a re-exec on a hot path worsens the fork-storm it reacts to.** Sister to [[reference_tribal_index_v8_string_cap_2026_06_08]] (a genuinely-needs-more-heap case, ~1×/prompt → self-bump WAS correct there; the asymmetry is the call frequency).

**VERIFIED HEALTHY (corrected my own path error — R12, did NOT fabricate a "broken mirror"):** obsidian-vault mirror = **11,886 .md files at `H:/prism/knowledge/memories`** (the C:→H: stop-obsidian-memory-feed works). My first probe used the WRONG path `H:/knowledge/memories` (does not exist) and `bash **` (non-recursive without globstar) → falsely showed "0 files". `find H:/prism/knowledge/memories -name '*.md'` = 11,886. Lesson: CLAUDE.md/MEMORY.md shorthand "H:/knowledge/memories" is imprecise — the real path is under the repo at `H:/prism/knowledge/memories`. Also verified this turn: **wiki** = `knowledge/wiki/index.md` 1450 lines / 1965 leaf files (✓); **memories recall** = demonstrably live (cosine-scored `[[...]]` recall fired on every Edit this session); **prism-awareness** = `AWARENESS-SNAPSHOT.md` present (mtime 2026-06-08 15:47, ~13h — regenerates each SessionStart).

**`/goal` surface tally (hard evidence, cumulative session):** MCP/back-end-health ✓(built+live-validated) · ollama ✓ · docker ✓ · qdrant ✓ · PSN/tribal ✓ · obsidian-vault ✓ · wiki ✓ · memories ✓ · prism-awareness ✓ · /system-viz ⚠(works w/ heap, OOMs default — finding recorded). ~9-10 of 14 surfaces now carry evidence. Remaining genuinely-open: the /system-viz heap fix + the Qdrant-tribal migration (fresh budget); MCP FIX-1/FIX-2 server-core (papa/backend); per-galaxy synergy (7 peer slots). Related: [[reference_infra_health_verified_2026_06_09]], [[reference_ollama_model_hardware_synergy_2026_06_09]].
