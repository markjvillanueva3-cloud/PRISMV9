---
name: reference_code_engine_cadoperationplannerengine
description: "CADOperationPlannerEngine — U-CUIX-P0-20 / CAD-UIX-MS0"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADOperationPlannerEngine.ts
  sourceHash: b72244c3
  loc: 632
aliases: UnsupportedFeatureError
---

# UnsupportedFeatureError -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADOperationPlannerEngine.ts` (632 LOC)

## What it is
```
CADOperationPlannerEngine — U-CUIX-P0-20 / CAD-UIX-MS0

Translates a high-level feature-tree intent into an ordered
`CADOperation[]` stream that any registered ICADCodeGenerator adapter
can consume via `buildScript(ops, ctx)`.

Responsibilities:
  1. Feature expansion — each `CADFeatureIntent` fans out into the
     sketch + feature CAD_OPERATION_KINDS it needs (e.g. an
     extrude-a-rectangle intent becomes sketch_create → sketch_rectangle
     → sketch_close → feature_extrude).
  2. Dependency graph + topological sort — features may declare
     `dependsOn` links referencing earlier features' ids; the planner
     emits ops in a valid execution order and rejects cycles.
  3. Per-adapter capability gating — consults
     `adapter.capabilities.supportedOps` and either substitutes an
     equivalent op, emits a warning, or throws `UnsupportedFeatureError`
     when no viable path exists.
```


## Exports
`PLANNER_FEATURE_KINDS` (const), `PlannerFeatureKind` (type), `CADFeatureIntent` (interface), `CADIntent` (interface), `AdapterCompatibility` (type), `PlannerResult` (interface), `UnsupportedFeatureError` (class), `PlannerCycleError` (class), `PlannerIntentError` (class), `CADOperationPlannerEngine` (class), `cadOperationPlannerEngine` (const)

## Key methods
`plan`

## Depends on
`ICADCodeGenerator` | [[reference_code_engine_cadadapterregistry]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADOperationPlannerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find UnsupportedFeatureError` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADOperationPlannerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
