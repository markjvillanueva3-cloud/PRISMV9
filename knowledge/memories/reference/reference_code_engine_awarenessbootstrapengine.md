---
name: reference_code_engine_awarenessbootstrapengine
description: "Session awareness verification at boot"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/AwarenessBootstrapEngine.ts
  sourceHash: 928678fe
  loc: 242
aliases: AwarenessBootstrapEngine
---

# AwarenessBootstrapEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/AwarenessBootstrapEngine.ts` (242 LOC)

## What it is
Session awareness verification at boot

```
AwarenessBootstrapEngine — Session awareness verification at boot

Phase 0.13 U-SAW1 from UNIVERSAL-SKILLS-SCRIPTS-HOOKS-PLAN. Runs on SessionStart
to verify that registries, indexes, and awareness caches are fresh enough that
the session can safely act. If the computed awareness score drops below the
readiness threshold (0.80), the session is marked NOT_READY and the first
prompt must be refused until refresh succeeds.

Design rules:
  - Read-only: this engine never mutates registries; it only measures staleness.
  - Deterministic: the score is a pure function of file mtimes + existence.
  - Cheap: <50ms on a warm FS; skipped files count as missing (score penalty).
  - Extensible: `computeScore` takes a descriptor list so callers can add signals.

The 7 default signals are weighted so that missing-critical > stale-critical >
missing-optional. Tune weights in DEFAULT_SIGNALS, not in the 
```


## Exports
`READINESS_THRESHOLD` (const), `SignalSeverity` (type), `AwarenessSignal` (interface), `SignalResult` (interface), `AwarenessReport` (interface), `DEFAULT_SIGNALS` (const), `AwarenessBootstrapEngine` (class), `awarenessBootstrapEngine` (const)

## Key methods
`getThreshold`, `getSignals`, `compute`, `isReady`, `explainBlock`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/AwarenessBootstrapEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find AwarenessBootstrapEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/AwarenessBootstrapEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
