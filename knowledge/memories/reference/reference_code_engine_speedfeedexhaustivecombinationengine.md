---
name: reference_code_engine_speedfeedexhaustivecombinationengine
description: "sample-and-log the SFC parameter"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SpeedFeedExhaustiveCombinationEngine.ts
  sourceHash: b8bda769
  loc: 517
aliases: SpeedFeedExhaustiveCombinationEngine
---

# SpeedFeedExhaustiveCombinationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SpeedFeedExhaustiveCombinationEngine.ts` (517 LOC)

## What it is
sample-and-log the SFC parameter

```
SpeedFeedExhaustiveCombinationEngine — sample-and-log the SFC parameter
space across mill / lathe / wedm domains.

Operator directive (2026-05-25): "run every logical combination through the
calculator and check results one by one. test and log as nodes in the
system for calculated results of every single logical combination that
can be inputed into our prism calculator studio for the 3 primary machine
domains."

The full 9-axis × 3-domain combinatorial space is ~10^9 cells (combinatorial
explosion). This engine uses a structured sampler instead of brute-force
enumeration:

  1. FACTORIAL — enumerate the cartesian product of small-cardinality axes
     (ISO group × cut type × strategy × mode = 6 × 3 × 7 × 3 = 378 cells)
  2. PRIMARY GRID — for each factorial cell, sample (diameter × flutes ×
     coolant × holder) on a discrete grid (typically 200-2000 cells)
  3. PER-CELL RUN — invoke t
```


## Exports
`Domain` (type), `SampleMode` (type), `CombinationRunInput` (interface), `CombinationCellResult` (interface), `CombinationRunReport` (interface), `SpeedFeedExhaustiveCombinationEngine` (class), `speedFeedExhaustiveCombinationEngine` (const)

## Key methods
`run`

## Depends on
[[reference_code_engine_speedfeednineaxisorchestratorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/SpeedFeedExhaustiveCombinationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SpeedFeedExhaustiveCombinationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SpeedFeedExhaustiveCombinationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
