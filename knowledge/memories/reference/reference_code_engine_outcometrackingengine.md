---
name: reference_code_engine_outcometrackingengine
description: "Phase 0.19 U-LLM5"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/OutcomeTrackingEngine.ts
  sourceHash: 6498a355
  loc: 247
aliases: OutcomeTrackingEngine
---

# OutcomeTrackingEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/OutcomeTrackingEngine.ts` (247 LOC)

## What it is
Phase 0.19 U-LLM5

```
OutcomeTrackingEngine — Phase 0.19 U-LLM5

Log real shop outcomes for every program PRISM produces so downstream
learning loops (U-LLM6 IncrementalLearning, CausalReasoningEngine,
TransferLearningBridge) can train on truthful signal instead of
synthetic self-play.

One outcome per machine run, stored as JSON lines at
`<data>/outcomes/outcomes.jsonl`. Append-only on disk; the in-memory
index is rebuilt lazily on first read after process start.

Design rules:
  1. Never throw on missing-file — first write creates the path.
  2. Zod-validate every payload; reject the write rather than store
     garbage that would poison the learning set.
  3. Per-file write chain prevents partial lines under concurrent
     logging (common in orchestrated multi-agent runs).
  4. Queries are in-memory filters, not a database. If the file grows
     past a few hundred MB we can rotate or promote to SQLite; f
```


## Exports
`OUTCOME_KINDS` (const), `OutcomeKind` (type), `OutcomeInputSchema` (const), `OutcomeInput` (type), `OutcomeRecord` (interface), `OutcomeQuery` (interface), `OutcomeStats` (interface), `OutcomeTrackingEngine` (class), `outcomeTrackingEngine` (const)

## Key methods
`getLogPath`, `log`, `query`, `forProgram`, `stats`, `reload`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/OutcomeTrackingEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find OutcomeTrackingEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/OutcomeTrackingEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
