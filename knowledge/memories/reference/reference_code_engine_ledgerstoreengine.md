---
name: reference_code_engine_ledgerstoreengine
description: "Ledger Store"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LedgerStoreEngine.ts
  sourceHash: 5329a674
  loc: 1147
aliases: LedgerStoreEngine
---

# LedgerStoreEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LedgerStoreEngine.ts` (1147 LOC)

## What it is
Ledger Store

```
LedgerStoreEngine — CLEANUP-MS0 / U-CLEANUP-B10

Domain ledger for the golf-slot watchdog + peer chats. Wraps the SAME
SQLite WAL database as `CoordinationStoreEngine` (state/shared/coordination.db)
via an independent connection — better-sqlite3 + WAL mode supports multiple
concurrent connections to the same file, so we get atomicity without sharing
a Database handle (which would couple lifecycle in a way that flaky-out either
engine's tests).

TABLES (DDL in src/migrations/golf-ledger-v1.sql):
  bug_attribution         — per-bug audit trail (chat, commit, files, severity).
  peer_audit_ticks        — golf watchdog poll cycles (start/end/findings).
  chat_bus_signals        — structured cross-chat signals (replaces AGENT_CHAT.md chunks).
  golf_envelope_mutations — queued envelope mutations golf wants applied via R3-UU3.
  ledger_meta             — schema_version + key/value.

API (per R
```


## Exports
`getMigrationSqlPath` (function), `LEDGER_SCHEMA_VERSION` (const), `MICROS_PER_USD` (const), `microsToUsd` (function), `usdToMicros` (function), `LedgerTable` (type), `BugSeverity` (type), `BugAttributionRow` (interface), `PeerAuditTickRow` (interface), `ChatBusSignalRow` (interface), `EnvelopeMutationRow` (interface), `LedgerInsertInput` (type), `LedgerInsertResult` (type), `BugPreDispatchInput` (interface), `SlotScore24h` (interface), `MigrationResult` (interface), `LedgerStoreOptions` (interface), `LedgerStoreEngine` (class), `getLedgerStoreEngine` (function), `ledgerStoreEngine` (const)

## Key methods
`VALUES`, `AND`, `COUNT`, `SUM`, `migrate`, `insert`, `query`, `listOpenBugs`, `resolveBug`, `insertPreDispatchRow`, `getSlotScore24h`, `getSlotScoresAll24h`, `finishAuditTick`, `listRecentTicks`, `drainSignalsFor`, `markSignalConsumed`, `listPendingMutations`, `markMutationApplied`, `markMutationRejected`, `counts`, `schemaVersion`, `physicalSchemaVersion`, `close`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/LedgerStoreEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find LedgerStoreEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LedgerStoreEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
