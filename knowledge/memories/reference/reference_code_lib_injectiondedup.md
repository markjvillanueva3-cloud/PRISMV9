---
name: reference_code_lib_injectiondedup
description: "scripts/lib/injection-dedup.mjs"
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/injection-dedup.mjs
  sourceHash: 2165c75d
  loc: 132
aliases: injection-dedup.mjs
---

# injection-dedup.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/injection-dedup.mjs` (132 LOC)

## What it is
```
scripts/lib/injection-dedup.mjs
---------------------------------
TOKEN-SAVINGS-EXPAND/U-PSN-INJECTION-DEDUP-LIB (2026-05-23, slot:alpha)

Generic per-session injection deduper. UserPromptSubmit hooks fire ~14-16
context blocks per prompt; many are byte-identical to prior emissions in
the same session (wiki precheck, master-index, memory vault, slot soul).
A single goal-prereq-inject already proves the pattern works ("loop-context
dedup — this block is unchanged since an earlier prompt this session").
This lib generalizes that pattern so any hook can adopt it cheaply.

Pure-function lib + tiny in-memory cache wrapper. Sidecar I/O is owned by
the caller (so the lib stays unit-testable with no FS dependency).

Contract:
  - hashBlock(text) → short stable hex digest
  - shouldEmit(cache, hookTag, contentHash, now, ttlMs) → { emit, reason, lastSeenAt }
  - recordEmit(cache, hookTag, contentH
```


## Exports
`DEFAULT_TTL_MS` (const), `MAX_HASH_INPUT_BYTES` (const), `hashBlock` (function), `shouldEmit` (function), `recordEmit` (function), `formatDedupedMarker` (function), `pruneExpired` (function), `pruneTag` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/injection-dedup.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find injection-dedup.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/injection-dedup.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
