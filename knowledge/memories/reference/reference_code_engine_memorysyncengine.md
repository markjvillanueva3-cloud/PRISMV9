---
name: reference_code_engine_memorysyncengine
description: "Phase 0.19 U-LLM8 + CPP-MS3-U-CPP22 (CRDT merge)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MemorySyncEngine.ts
  sourceHash: 299f9d33
  loc: 513
aliases: MemorySyncEngine
---

# MemorySyncEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MemorySyncEngine.ts` (513 LOC)

## What it is
Phase 0.19 U-LLM8 + CPP-MS3-U-CPP22 (CRDT merge)

```
MemorySyncEngine — Phase 0.19 U-LLM8 + CPP-MS3-U-CPP22 (CRDT merge)

Export Qdrant-backed memory to a JSON bundle on the H: drive so the
home (RTX 4080) and work (RTX 3080) PCs share tribal tips, program
embeddings, outcome vectors, and formula memories. Pair with
`claude_sync.py`'s drive-swap workflow: before eject at site A, dump
a bundle; after `setup-new-pc.py` at site B, import it.

Bundle layout (v1):
  {
    "schemaVersion": 1,
    "createdAt": "ISO",
    "source": { "host": "...", "machine": "..." },
    "collections": [
      { "name": "prism_memory_tip", "vectorSize": 768, "distance": "Cosine",
        "points": [ { "id", "vector", "payload" }, ... ] }
    ]
  }

Design rules:
  1. Bundles are plain JSON. Forkable, diffable, friendly to git.
  2. Exports never block on a wedged daemon — if a collection fails to
     scroll we record the error per-collection and continue.
  3. I
```


## Exports
`MemoryBundlePoint` (interface), `MemoryBundleCollection` (interface), `MemoryBundle` (interface), `ExportOptions` (interface), `ExportReport` (interface), `ImportOptions` (interface), `ImportReport` (interface), `BundleSummary` (interface), `MemorySyncDeps` (interface), `MemoryCRDTEntry` (interface), `MergeReport` (interface), `MemorySyncEngine` (class), `memorySyncEngine` (const)

## Key methods
`exportBundle`, `importBundle`, `listBundles`, `bundleMetadata`, `mergeEntries`

## Depends on
[[reference_code_engine_qdrantvectorstoreengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/MemorySyncEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MemorySyncEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MemorySyncEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
