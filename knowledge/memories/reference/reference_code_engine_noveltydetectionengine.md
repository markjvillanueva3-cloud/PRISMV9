---
name: reference_code_engine_noveltydetectionengine
description: "AUTO-LEARNING-LOOP-MS0 / U-ALL02"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/NoveltyDetectionEngine.ts
  sourceHash: 3ee5b2ac
  loc: 785
aliases: NoveltyDetectionEngine
---

# NoveltyDetectionEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/NoveltyDetectionEngine.ts` (785 LOC)

## What it is
AUTO-LEARNING-LOOP-MS0 / U-ALL02

```
NoveltyDetectionEngine — AUTO-LEARNING-LOOP-MS0 / U-ALL02
=========================================================

Filters source-monitor output (U-ALL01 `ReputableSourceMonitorEngine`)
so that `AutoResearchOrchestratorEngine` (U-ALL03) only dispatches
researcher subagents on items the system genuinely hasn't seen.

Three-tier dedup pipeline:
  1. Exact-hash short-circuit — SHA-256 of normalized "title|summary".
     A bit-for-bit identical re-broadcast returns instantly; no
     embed / Jaccard work.
  2. Cosine embedding similarity — preferred path when an embedder
     is configured AND the catalog has stored embeddings. Threshold
     defaults to 0.92 per atomized spec § U-ALL02 step-1.
  3. Jaccard token overlap — fallback when embedder is unavailable
     (Ollama down, dimension mismatch, network error). Threshold
     defaults to 0.50: low enough that "Anthropic releases Claude

```


## Exports
`CatalogEntry` (interface), `CatalogState` (interface), `CATALOG_SCHEMA_VERSION` (const), `EmbedderLike` (interface), `EmbedOutcome` (type), `NoveltyMethod` (type), `NoveltyReason` (type), `NoveltyVerdict` (interface), `DetectResult` (interface), `LoadOutcome` (interface), `AddOutcome` (interface), `EngineOpts` (interface), `DEFAULT_COSINE_THRESHOLD` (const), `DEFAULT_JACCARD_THRESHOLD` (const), `NoveltyDetectionEngine` (class), `normalize` (function), `tokenize` (function), `jaccard` (function), `noveltyDetectionEngine` (const)

## Key methods
`embed`, `getCatalog`, `isCatalogLoaded`, `setCatalog`, `loadCatalog`, `resetAll`, `setEmbedder`, `detect`, `addVerifiedNovel`

## Depends on
[[reference_code_engine_reputablesourcemonitorengine]] | [[reference_code_engine_ollamaembedderengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/NoveltyDetectionEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find NoveltyDetectionEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/NoveltyDetectionEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
