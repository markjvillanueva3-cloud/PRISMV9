---
name: reference_code_engine_ppdwellvalidatorengine
description: "Validate G4 dwell commands"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PPDwellValidatorEngine.ts
  sourceHash: 55870675
  loc: 271
aliases: PPDwellValidatorEngine
---

# PPDwellValidatorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PPDwellValidatorEngine.ts` (271 LOC)

## What it is
Validate G4 dwell commands

```
PPDwellValidatorEngine — Validate G4 dwell commands

G4 forces the machine to pause for a specified time. Dwells are
commonly used:
  - after a spindle direction change or tool orientation
  - at the bottom of a drill cycle (chip breaking)
  - to allow coolant to reach the cutting zone before engagement

Dwell bugs are the quintessential "works in the sim, breaks on the
machine" class — simulators skip dwells, but real controllers honour
them to the millisecond. Two bugs dominate:
  - G4 with no time argument — some controls error, some skip, some
    hang.
  - G4 P600 (intent: 0.6 sec, actual: 600 sec = 10 min) — Fanucs
    treat P as milliseconds or seconds depending on parameter 3405
    bit 1. Post-processors often guess wrong.

Checks:
  - dwell_without_time (error): G4 on a line with no P or X word.
  - dwell_with_motion (error): G4 combined with X/Y/Z motion coords
    on the same
```


## Exports
`DwellSeverity` (type), `DwellIssue` (interface), `DwellResult` (interface), `DwellUnitMode` (type), `DwellOptions` (interface), `PPDwellValidatorEngine` (class), `ppDwellValidatorEngine` (const)

## Key methods
`validate`, `quickCheck`, `defaultOptions`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PPDwellValidatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PPDwellValidatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PPDwellValidatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
