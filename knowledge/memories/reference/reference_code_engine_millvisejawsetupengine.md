---
name: reference_code_engine_millvisejawsetupengine
description: "MillViseJawSetupEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MillViseJawSetupEngine.ts
  sourceHash: 3559cdc5
  loc: 346
aliases: millViseJawSetupEngine
---

# millViseJawSetupEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MillViseJawSetupEngine.ts` (346 LOC)

## What it is
```
MillViseJawSetupEngine
========================

Soft-jaw machining + grip-planning calculator for milling-machine vises
(Kurt 6"/8", Orange/SMW soft jaws, jaw plates with locating bushings).

Mill parity for LatheChuckJawSetupEngine (LATHE-PRO-MS11) but with
fundamentally different physics:
  - Lathe failure mode: centrifugal jaw lift-off (rotating chuck)
  - Mill failure mode:  cutting-force shear / lift against vise grip
    (stationary part, oscillating cut force)

Computes the *practical changeover math* the machinist must do when
prepping a new vise setup:

  1. Soft-jaw step bore (or jaw-pocket) depth — enough to seat the
     part without bottoming, accounting for elastic spring-back
  2. Parallel selection — required height to clear vise bed + provide
     minimum jaw-grip overlap (per ISO 16156 / SME Vise Setup Guide)
  3. Workstop / Locator standoff — distance from movable-jaw
```


## Exports
`MIN_GRIP_SAFETY` (const), `MIN_ANTILIFT_SAFETY` (const), `MIN_GRIP_OVERLAP_RATIO` (const), `MIN_GRIP_OVERLAP_ABS_MM` (const), `DEFAULT_FRICTION_COEFFICIENT_STEEL_ON_STEEL` (const), `DEFAULT_FRICTION_COEFFICIENT_AL_ON_AL` (const), `DEFAULT_JAW_MODULUS_MPA` (const), `DEFAULT_JAW_CONTACT_AREA_MM2` (const), `STANDARD_PARALLEL_HEIGHTS_MM` (const), `DEFAULT_VISE_JAW_HEIGHT_MM` (const), `ViseJawMaterial` (type), `MillViseJawInput` (interface), `MillViseJawResult` (interface), `millViseJawSetupEngine` (const)

## Key methods
`compute`, `getStats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MillViseJawSetupEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find millViseJawSetupEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MillViseJawSetupEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
