---
name: reference_code_script_buildwikirecalldigest
description: "build-wiki-recall-digest.mjs — CLEANUP-MS0 / U-CLEANUP-G5"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/build-wiki-recall-digest.mjs
  sourceHash: 076dbfc9
  loc: 655
aliases: build-wiki-recall-digest.mjs
---

# build-wiki-recall-digest.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/build-wiki-recall-digest.mjs` (655 LOC)

## What it is
```
build-wiki-recall-digest.mjs — CLEANUP-MS0 / U-CLEANUP-G5

Consumes the wiki/memory recall-counts state file
(`mcp-server/data/state/wiki-recall-counts.json`, populated by the
`recall-counter-track.mjs` PostToolUse hook) and emits two operator-facing
lists:

  1. DELETION CANDIDATES — vault entries (.md files under knowledge/wiki/
     or knowledge/memories/) that the recall counter has never seen
     (count = 0 / absent from the entries dict) AND whose file mtime is
     older than `--deletion-age-days` (default 90). These are wiki/memory
     entries that exist but no Claude session has ever read them through
     the recall path — the entries are dead weight in the vault, the
     operator can review-and-delete (or backlink them so they get found).

  2. PROMOTION CANDIDATES — entries whose recall RATE exceeds
     `--promotion-rate-per-week` (default 50). Rate is `count` divided by

```


## Exports
`parseArgs` (function), `deriveKeyFromPath` (function), `walkVaultDir` (function), `weeksElapsed` (function), `classifyDeletionCandidates` (function), `classifyPromotionCandidates` (function), `renderDigest` (function), `buildDigest` (function), `run` (function), `SCHEMA_VERSION` (re-export), `DEFAULT_DELETION_AGE_DAYS` (re-export), `DEFAULT_PROMOTION_RATE_PER_WEEK` (re-export), `DEFAULT_COUNTS_REL` (re-export), `DEFAULT_OUT_JSON_REL` (re-export), `DEFAULT_OUT_MD_REL` (re-export), `RENDER_LIMIT` (re-export), `WEEK_MS` (re-export), `DAY_MS` (re-export), `MIN_WEEKS_FOR_RATE` (re-export), `FUTURE_SKEW_MS` (re-export)

## Key methods
`recurse`, `mkdirSync`, `writeFileSync`, `writeFn`, `printHelp`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/build-wiki-recall-digest.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find build-wiki-recall-digest.mjs` or `prism_session:master_index_query`; open the source at `scripts/build-wiki-recall-digest.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
