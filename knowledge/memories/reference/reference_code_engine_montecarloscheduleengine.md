---
name: reference_code_engine_montecarloscheduleengine
description: "U-FORE-15 (Real Planning Algorithms)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MonteCarloScheduleEngine.ts
  sourceHash: 08c8ce9c
  loc: 212
aliases: MonteCarloScheduleEngine
---

# MonteCarloScheduleEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MonteCarloScheduleEngine.ts` (212 LOC)

## What it is
U-FORE-15 (Real Planning Algorithms)

```
MonteCarloScheduleEngine — U-FORE-15 (Real Planning Algorithms)
================================================================

Monte Carlo simulation over an activity-on-node DAG. Samples each
task's duration from a triangular distribution parameterised by
(optimistic, most-likely, pessimistic) estimates — the standard
PERT three-point shape — and propagates through the precedence
network to compute a distribution of project makespans.

Triangular inverse CDF:
  fm = (M - O) / (P - O)
  if u < fm: d = O + sqrt(u * (P - O) * (M - O))
  else:      d = P - sqrt((1 - u) * (P - O) * (P - M))

Why triangular and not full beta-PERT? For schedule risk at small N
(≤ 100 tasks, ≤ 10k trials) the difference is well under the noise
from imprecise O/M/P estimates, and triangular avoids importing a
gamma-function library. Standard in PRINCE2/PMI practice.

Inputs  → { tasks[], trials?, seed? }
Outp
```


## Exports
`MCScheduleTask` (interface), `MCScheduleInput` (interface), `MCScheduleResult` (interface), `MonteCarloScheduleEngine` (class), `monteCarloScheduleEngine` (const)

## Key methods
`simulate`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MonteCarloScheduleEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MonteCarloScheduleEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MonteCarloScheduleEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
