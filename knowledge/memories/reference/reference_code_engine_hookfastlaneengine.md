---
name: reference_code_engine_hookfastlaneengine
description: "HOOK-SYNERGY-MS0 / U-HOOK-FAST-LANE (H6)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HookFastLaneEngine.ts
  sourceHash: df223441
  loc: 765
aliases: HookFastLaneEngine
---

# HookFastLaneEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HookFastLaneEngine.ts` (765 LOC)

## What it is
HOOK-SYNERGY-MS0 / U-HOOK-FAST-LANE (H6)

```
HookFastLaneEngine — HOOK-SYNERGY-MS0 / U-HOOK-FAST-LANE (H6)

Splits broad PreToolUse / PostToolUse matchers in a Claude Code settings.json
into a *fast lane* for read-only tools (Read / Glob / Grep) and a *slow lane*
for write/edit/bash tools (Edit / Write / MultiEdit / NotebookEdit / Bash).

Why this matters:
  - In the current shape, `PreToolUse matcher=".*"` and `PostToolUse matcher="Bash|Read"`
    fire on every Read tool call, even though most of the hooks they contain
    only mutate state on writes or only parse bash stdout. That's ~26 hook
    fires per Read in H:/prism alone (vs ~34 per Edit) — the read path is
    paying ~76% of the write path's cost.
  - This engine consumes the H3 `// tier:` frontmatter (T0-T4) PLUS a small
    deterministic name-pattern classifier, decides per-hook whether it's
    `fast-lane-safe`, `slow-lane-only`, or `both`, then proposes a new
    `hoo
```


## Exports
`HookTier` (type), `FastLaneDecision` (type), `HookEntry` (interface), `MatcherBlock` (interface), `SettingsHooks` (interface), `SettingsShape` (interface), `HookClassification` (interface), `BlockSplit` (interface), `FastLanePlan` (interface), `ForecastReport` (interface), `HookFastLaneEngineOptions` (interface), `FAST_LANE_MATCHER` (const), `SLOW_LANE_NARROWED_MATCHER` (const), `MIN_BLOCK_SIZE_FOR_SPLIT` (const), `HookFastLaneEngine` (class), `makeStaticTierLookup` (function), `makeDiskTierLookup` (function), `getHookFastLaneEngine` (function)

## Key methods
`classifyHook`, `buildPlanForEvent`, `buildPlan`, `applyPlan`, `forecast`, `narrowMatcherForSlowLane`, `countHooksFiringFor`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/HookFastLaneEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HookFastLaneEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HookFastLaneEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
