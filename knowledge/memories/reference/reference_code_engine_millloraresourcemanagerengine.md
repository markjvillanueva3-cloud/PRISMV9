---
name: reference_code_engine_millloraresourcemanagerengine
description: "MillLoRAResourceManagerEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MillLoRAResourceManagerEngine.ts
  sourceHash: 37eba870
  loc: 301
aliases: millLoRAResourceManagerEngine
---

# millLoRAResourceManagerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MillLoRAResourceManagerEngine.ts` (301 LOC)

## What it is
```
MillLoRAResourceManagerEngine
================================

Manages compute, memory, storage, and mill-domain-specific resources
for mill LoRA pipelines. Tracks allocation, enforces quotas, supports
priority-based preemption.

Mill parity for LatheLoRAResourceManagerEngine (LATHE-LORA-MS0 U-LLR46)
with 3 MILL-CANONICAL resource types added on top of the 5 lathe-shared:

  Lathe-shared resource types (5):
    - cpu_cores
    - gpu_memory_mb
    - ram_mb
    - disk_mb
    - gpu_count

  MILL-CANONICAL resource types (3):
    - tcpm_solver_slots     — 5-axis TCPM solver concurrency licenses
    - vericut_slots          — Vericut / NCSimul / G-Wizard sim slots
    - postprocessor_slots    — Mill post-processor compile slots (Mastercam/HSMWorks/etc.)

Features:
  - GPU/CPU/memory/disk + mill-canonical-resource accounting
  - Quota enforcement (soft 80%, hard 95% default)
  - Reservation t
```


## Exports
`DEFAULT_QUOTA_SOFT_LIMIT` (const), `DEFAULT_QUOTA_HARD_LIMIT` (const), `DEFAULT_RESERVATION_TIMEOUT_MS` (const), `MillResourceType` (type), `MILL_CANONICAL_RESOURCE_TYPES` (const), `AllocationPriority` (type), `MillResourcePool` (interface), `MillResourceAllocation` (interface), `MillResourceManagerConfig` (interface), `millLoRAResourceManagerEngine` (const), `MillLoRAResourceManagerEngine` (re-export)

## Key methods
`setConfig`, `getConfig`, `setPool`, `getPool`, `getPools`, `canAllocate`, `allocate`, `release`, `reserve`, `unreserve`, `getUtilization`, `getAllocation`, `getOwnerAllocations`, `findPreemptable`, `getStats`, `getSummary`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MillLoRAResourceManagerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find millLoRAResourceManagerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MillLoRAResourceManagerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
