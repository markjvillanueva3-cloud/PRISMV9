---
name: reference_tango_algo_fuzzy_wire_2026_06_15
description: tango wired orphaned FuzzyController -> prism_algorithm:control_fuzzy (8/8 round-trip) + marked SimulatedAnnealing WIRE-EXEMPT; surfaced 3 cross-domain findings (gateway-singleton P0, FuzzyLogicEngine dedup sibling, 25-handler lossy-validation pattern). Commit BLOCKED on peer index.lock. slot tango 2026-06-15.
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.217Z
aliases: reference_tango_algo_fuzzy_wire_2026_06_15
---


**TANGO U-ALGO-FUZZY-WIRE (slot tango, 2026-06-15, push-through /loop iter 11)** — drained 2 of the 6 "orphaned" algorithms the standing coverage audit flags, with verify-on-disk overturning last session's over-conservative "route all 6 to owners".

**BUILT (3 files, tested, 2-of-2 per-file scrutiny PASS, COMMIT PENDING on lock):**
- `algorithmDispatcher.ts` — wired `control_fuzzy` action (CONTROL_ACTIONS + handler, ml_clustering template) -> canonical `FuzzyController` Algorithm<I,O> (was built-but-orphaned, no consumer import). Fully declarative input (inputs/outputs/rules/values) -> JSON-serializable. ADDITIVE: the gateway serves kalman/montecarlo/spline/localsearch but NO fuzzy action existed. Also: description hardcoded `(35 actions)` -> `${ACTIONS.length}` (self-correcting, kills the stale-count drift class).
- `SimulatedAnnealing.ts` — `// WIRE-EXEMPT` marker: `calculate()` uses a HARDCODED midpoint-seeking objective (SimulatedAnnealing.ts:93-97 cost = sum sq-dev from bounds-midpoint), NOT caller-injected -> wiring would expose a fixed-objective SA of limited utility. General SA needs objective-injection (owner refactor). Reclassifies orphaned->wire-exempt honestly.
- `algorithmDispatcher.fuzzy.synergy.test.ts` (NEW) — 8/8 vitest TRUE round-trip THROUGH the dispatcher switch+lazy-import+validate+calculate. EXACT method-independent rule-firing (mu_hot[50,100,100](90)=0.8, mu_cold[0,0,50](10)=0.8, silent rules=0 exact), defuzz-method-ROBUST output bands (hot->fan>50, cold->fan<50: holds for centroid/bisector/MoM), monotonicity (R9 intent), 3 failure modes incl. fail-loud validation-detail assertion.

**COVERAGE DELTA:** `algorithm-dispatcher-coverage.mjs`: wired 108->109/121, orphaned 6->4, wire-exempt 7->8. Remaining 4 orphaned = next-iter disposition: InterpolationEngine/KalmanFilter/MonteCarlo (gateway-OVERLAP — gateway already serves the capability; need richer-vs-same read before deciding wire-richer-variant vs mark-dup) + SafeExpressionEvaluator (keystone, likely transitively used by gateway.monteCarlo model-string compile).

**R12 FIX mid-build (per-file scrutiny reviewer-B P1):** the new handler first copied the 25-sibling `(v.errors ?? []).join` pattern -> empty "Invalid input: " because FuzzyController.validate populates `issues` not `errors`. Fixed to surface `issues` detail (fail-loud); hardened the failure-mode tests to assert the specific reason (/rule/i, /value/i) so the test catches detail-drop.

**COMMIT BLOCKED (R12 honest):** `.git/index.lock` held 3+ min by active peer (papa U-TSC-CAMPAIGN). Did NOT force-remove (fresh lock + 4 live git procs = active contention, not stale orphan; my 5-min stale threshold not met). 3 files safe uncommitted in working tree. RETRY exact command (single-quoted, explicit pathspec, on cad-fusion-live-ms0): `git add mcp-server/src/tools/dispatchers/algorithmDispatcher.ts mcp-server/src/algorithms/SimulatedAnnealing.ts mcp-server/src/tools/dispatchers/algorithmDispatcher.fuzzy.synergy.test.ts && git commit -m '[MAIN-FORCE] [DISCOVERY-EFFICIENCY]/U-ALGO-FUZZY-WIRE ...'`.

**3 CROSS-DOMAIN FINDINGS SURFACED (not tango's lane to fix):**
1. **P0-class (owner: india / algorithm-dispatcher) — `algorithmGatewayEngine` singleton appears UNEXPORTED.** `algorithmDispatcher.ts:195` lazy-imports `const { algorithmGatewayEngine } = await import("../../engines/AlgorithmGatewayEngine.js")` but the string `algorithmGatewayEngine` does NOT appear anywhere in `AlgorithmGatewayEngine.ts` (grep-empty) nor elsewhere in `engines/`. ~15 gateway-backed actions (control_kalman/control_transfer/opt_*/num_*/interp_*/graph_*) call `algorithmGatewayEngine.X`. Byte-identical at HEAD (pre-existing, NOT my regression). Tree carries 555 tsc errors (likely mid-peer-build) so this MAY be a transient refactor state — owner must verify against a CLEAN build whether those 15 actions are runtime-broken or the singleton moved. tsc-flagged at :195 + :1299(default never.split).
2. **Dedup (owner: kilo/safety + algorithm-dispatcher) — TWO Mamdani fuzzy controllers.** `FuzzyController` (algorithm, now -> prism_algorithm:control_fuzzy) and `FuzzyLogicEngine` (engine, FuzzyLogicEngine.ts:101 `evaluate()` -> prism_calc:fuzzy_evaluate + fuzzy_process_controller) are functional siblings (both triangular/trapezoidal/gaussian MF + centroid defuzz). Independent code paths; the engine predates. CONSOLIDATION CANDIDATE — not a blocker (algorithm-catalog exposure via prism_algorithm is the sanctioned pattern), but the fleet should know the two exist.
3. **Fleet fail-loud sweep (owner: any) — `(v.errors ?? [])` lossy pattern x25.** 25 ML handlers in algorithmDispatcher + spatial_ransac_fit + graph_heterophily_aggregate all use `(v.errors ?? []).join` while their Algorithm.validate populates `issues` not `errors` -> every validation failure surfaces detail-free "Invalid input: ". Fleet-wide R12 gap; fix all at once: `(v.issues ?? []).filter(i=>i.severity==="error").map(i=>i.message)`.

**LESSON:** verify-on-disk overturned a prior over-conservative disposition. Last session routed all 6 orphaned algos to owners ("wiring->romeo"); but prism_algorithm is tango's OWN algorithm-catalog surface (work order explicitly authorizes algorithm->prism_algorithm wiring; ALGO-SYNERGY 2026-05-29 precedent). Reading each algo's interface revealed 1 clean additive wire (FuzzyController), 1 honest wire-exempt (SA fixed-objective), 3 gateway-overlap, 1 keystone — a real per-orphan DECISION, not blanket routing. Sister: [[reference_tango_algorithm_coverage_diff_2026_06_15]], [[reference_tango_discovery_coverage_dashboard_2026_06_16]].
