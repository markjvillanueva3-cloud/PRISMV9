---
name: reference_code_engine_cadtosteppipelineengine
description: "end-to-end format-agnostic CAD→STEP pipeline."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADToSTEPPipelineEngine.ts
  sourceHash: 5ca73aac
  loc: 571
aliases: CADToSTEPPipelineEngine
---

# CADToSTEPPipelineEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADToSTEPPipelineEngine.ts` (571 LOC)

## What it is
end-to-end format-agnostic CAD→STEP pipeline.

```
CADToSTEPPipelineEngine — end-to-end format-agnostic CAD→STEP pipeline.

Orchestrates the full ground-truth ingestion pipeline for the 20,006-file CAD
corpus: identify format → select bridge/strategy → open → exportSTEP →
validate STEP output (AP214/AP242 schema sniff) → close → emit structured
result. Failures fall through an ordered strategy chain (native bridge →
native-parser fallback → manual-trigger) so a single missing CAD application
does not orphan a file.

Composes (does NOT duplicate):
  - CADAutomationRouter (E1307)        — primary format → bridge dispatch
  - CADAutomationMockLayer (E1306)     — fixture-backed mock support
  - FCStdNativeParserEngine (E2502)    — fallback diagnostics for .FCStd
  - F3DSQLiteParserEngine              — fallback diagnostics for .f3d

Mock mode: PRISM_CAD_MOCK=1 short-circuits bridge spawn and synthesizes a
minimal AP242-conforming STEP header
```


## Exports
`AtomicValue` (interface), `CADStrategy` (type), `StepSchemaName` (type), `StepValidationResult` (interface), `PipelineAttempt` (interface), `PipelineResult` (interface), `BatchSummary` (interface), `RunPipelineInput` (type), `BatchInput` (type), `CADToSTEPPipelineEngine` (class), `cadToSTEPPipelineEngine` (const)

## Key methods
`selectStrategy`, `listSupportedExtensions`, `runPipeline`, `validateSTEP`, `runBatch`

## Depends on
[[reference_code_util_logger]] | [[reference_code_engine_cadautomationrouter]] | [[reference_code_engine_cadautomationmocklayer]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADToSTEPPipelineEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADToSTEPPipelineEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADToSTEPPipelineEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
