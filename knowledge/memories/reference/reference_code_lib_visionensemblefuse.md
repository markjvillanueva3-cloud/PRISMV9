---
name: reference_code_lib_visionensemblefuse
description: "scripts/lib/vision-ensemble-fuse.mjs"
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/vision-ensemble-fuse.mjs
  sourceHash: 16d0c968
  loc: 580
aliases: vision-ensemble-fuse.mjs
---

# vision-ensemble-fuse.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/vision-ensemble-fuse.mjs` (580 LOC)

## What it is
```
scripts/lib/vision-ensemble-fuse.mjs

U-XRAY-VISION-ENSEMBLE — multi-VLM ensemble blueprint OCR with consensus fusion.

WHY THIS EXISTS (the Blackwell unlock):
  The single-model OCR path pins ONE vision model (qwen3-vl:8b-instruct) because the
  old RTX 4080 (16GB) could hold only one resident. The RTX PRO 6000 Blackwell (96GB,
  ~85GB idle in practice) removes that ceiling — it can hold THREE diverse VLM families
  GPU-resident at once (qwen3-vl + qwen2.5-vl + llama3.2-vision ≈ 31GB) and serve them
  CONCURRENTLY. This lib runs N independent VLMs over the SAME print in parallel and
  FUSES their extractions into a corroborated consensus dimension set.

WHY ENSEMBLE BEATS A BIGGER SINGLE MODEL (the science):
  A single generative VLM's dominant OCR failure mode is HALLUCINATION — a confidently
  reported dimension that is not on the drawing (the AI-expert pitfall "hallucination in
  gen
```


## Exports
`DEFAULT_DIM_CONF` (const), `NOISY_OR_CAP` (const), `combineConfidenceNoisyOr` (function), `normalizeModelDim` (function), `clusterAcrossModels` (function), `NON_DIM_KEY_FNS` (const), `fuseEnsemble` (function), `ocrImageWithModelAsync` (function), `runEnsembleOverImage` (function)

## Key methods
`resolve`, `done`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/vision-ensemble-fuse.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find vision-ensemble-fuse.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/vision-ensemble-fuse.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
