---
name: reference_code_algorithm_personalizedpagerank
description: "PersonalizedPageRank — seed-biased random-walk-with-restart over a graph."
metadata:
  type: reference
  kind: code-algorithm
  galaxy: unassigned
  sourcePath: mcp-server/src/algorithms/PersonalizedPageRank.ts
  sourceHash: 9babc316
  loc: 513
aliases: PersonalizedPageRank
---

# PersonalizedPageRank -- Algorithm SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/algorithms/PersonalizedPageRank.ts` (513 LOC)

## What it is
```
PersonalizedPageRank — seed-biased random-walk-with-restart over a graph.

Computes the stationary distribution of a random walker that, at each step,
either (with probability 1 − α) follows an out-edge uniformly at random or
(with probability α) teleports back to a seed distribution s. The result
`r ∈ ℝ^n` ranks every node by its "importance to the seed":

    r = α · s + (1 − α) · Pᵀ · r

where P is the row-stochastic transition matrix. Solved iteratively until
‖rₖ₊₁ − rₖ‖₁ < tol.

Properties:
  • Generalizes PageRank — when s is uniform (1/n everywhere), this is
    vanilla PageRank.
  • Concentrates importance near the seed — at small α (e.g., 0.15) most
    mass stays in the seed's neighborhood, at large α it spreads.
  • Converges in O(log(1/tol) / log(1/(1−α))) iterations — typically 30-60
    iterations for α ∈ [0.1, 0.3] and tol = 1e-6.

Why PRISM needs it (per PSN-ALGORITHM-SCO
```


## Exports
`GraphInput` (interface), `PPRInput` (interface), `PPRRankedNode` (interface), `PPROutput` (interface), `PersonalizedPageRank` (const)

## Key methods
`validate`, `calculate`, `getMetadata`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/algorithms/PersonalizedPageRank.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Algorithm

## How to query it
Recall by meaning via `/system-viz find PersonalizedPageRank` or `prism_session:master_index_query`; open the source at `mcp-server/src/algorithms/PersonalizedPageRank.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
