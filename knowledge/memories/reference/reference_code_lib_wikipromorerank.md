---
name: reference_code_lib_wikipromorerank
description: "wiki-promo-rerank.mjs — local-LLM (nomic) cosine rerank for the memory→wiki"
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/wiki-promo-rerank.mjs
  sourceHash: 985667a3
  loc: 197
aliases: wiki-promo-rerank.mjs
---

# wiki-promo-rerank.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/wiki-promo-rerank.mjs` (197 LOC)

## What it is
```
wiki-promo-rerank.mjs — local-LLM (nomic) cosine rerank for the memory→wiki
promotion advisor (`.claude/hooks/stop-memory-to-wiki-suggest.mjs`, U-HRP06).

THE GAP IT CLOSES. The advisor suggests which existing wiki entry a freshly
written memo should be promoted into / merged with. Its built-in fallback
scores the memo summary against wiki entry TITLES by token-set Jaccard — which
misses synonymy entirely: a memo summarised "out-of-memory crash bypasses the
fail-soft handler" shares ZERO title tokens with the lesson slug
`oom-bypasses-fail-soft-catch`, so the keyword path never suggests it. This
injects a nomic-embedding cosine rerank: embed the memo summary + each wiki
title (slug→words so nomic tokenises the meaning), rank by cosine.

$0 Claude tokens — local Ollama `nomic-embed-text` on the resident Blackwell.

SYNC-RERANK CONSTRAINT. The advisor calls `rerank(query, titles, topK)`
SY
```


## Exports
`TITLE_CACHE_PATH` (const), `NOMIC_MIN_SCORE` (const), `CACHE_MODEL_TAG` (const), `QUERY_TIMEOUT_MS` (const), `slugToWords` (function), `ensureTitleEmbeddings` (function), `prepareNomicRerank` (function)

## Key methods
`writeFileSync`, `renameSync`, `writeCache`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/wiki-promo-rerank.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find wiki-promo-rerank.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/wiki-promo-rerank.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
