---
name: reference_code_engine_catalogcorpusloaderengine
description: "the keystone that feeds the full vendor catalog"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CatalogCorpusLoaderEngine.ts
  sourceHash: ef19efed
  loc: 568
aliases: CatalogCorpusLoaderEngine
---

# CatalogCorpusLoaderEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CatalogCorpusLoaderEngine.ts` (568 LOC)

## What it is
the keystone that feeds the full vendor catalog

```
CatalogCorpusLoaderEngine — the keystone that feeds the full vendor catalog
corpus into the runtime tool catalog.

THE PROBLEM IT SOLVES (verified 2026-06-08, slot:romeo):
  - `mcp-server/data/CATALOG_INDEX.json` aggregates 67,178 tool entries across
    48 per-vendor files (~23 manufacturers). That index is read ONLY by
    VendorCatalogManifestEngine, which is a gap-analysis/manifest engine — it
    never produces tool records.
  - Every app exporter (Fusion / Mastercam / hyperMILL / Inventor HSM) and the
    SFC resolve tools through `toolCatalogEngine.search()`, which filters
    `ToolCatalogEngine`'s in-memory `Map`. That Map is seeded ONLY by the engine's
    ~30 hardcoded `_loadStandardTools()` getters. The ~20 `*-extracted.json`
    vendor files on disk (accupro, camfix, flash, korloy, ma-ford, rapidkut,
    yg1, …) are NEVER loaded — present but dormant.
  - `ToolCatalogEngine.a
```


## Exports
`CatalogLoadFileResult` (interface), `CatalogLoadResult` (interface), `CatalogCorpusLoaderEngine` (class), `catalogCorpusLoaderEngine` (const)

## Key methods
`resolve`, `load`, `corpusStats`, `ensureLoaded`, `resetEnsured`

## Depends on
[[reference_code_engine_toolcatalogengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CatalogCorpusLoaderEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CatalogCorpusLoaderEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CatalogCorpusLoaderEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
