---
name: reference_code_script_verifylaunchreadiness
description: "Launch-readiness verifier (LAUNCH-FE, 2026-06-23, slot:quebec)."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/verify-launch-readiness.mjs
  sourceHash: 542a2473
  loc: 309
aliases: verify-launch-readiness.mjs
---

# verify-launch-readiness.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/verify-launch-readiness.mjs` (309 LOC)

## What it is
```
Launch-readiness verifier (LAUNCH-FE, 2026-06-23, slot:quebec).

The product-launch plan docs in state/shared/specs/ have drifted from live code
TWICE (v2 said Electron/Capacitor were ZERO and the SFC honesty-fixes pending;
v3 had to correct it by reading the live tree). This harness replaces the
drift-prone manual reorientation with a deterministic, re-runnable check of the
launch INVARIANTS against the live frontend (R5: a deterministic question is
answered by code, not by re-reading docs every session).

Pure check functions (string/object in -> verdict out) are exported + unit
tested with both passing AND broken inputs (R9). The CLI runner does the IO and
emits a punch-list (markdown + --json).

  node scripts/verify-launch-readiness.mjs            # human punch-list
  node scripts/verify-launch-readiness.mjs --json     # machine report
  node scripts/verify-launch-readiness.mjs --ou
```


## Exports
`EXPECTED_GATES` (const), `KEY_FILES` (const), `SHELL_DEPS` (const), `checkPrimaryToken` (function), `checkRouteGates` (function), `checkShellDeps` (function), `checkPricing` (function), `checkKeyFiles` (function), `checkWebhookSignature` (function), `checkEntitlementEnforced` (function), `checkPostAlarmGate` (function), `checkPostExportFence` (function), `buildReport` (function), `runLaunchReadiness` (function)

## Key methods
`checkPrimaryToken`, `checkRouteGates`, `checkShellDeps`, `checkPricing`, `checkKeyFiles`, `checkWebhookSignature`, `checkEntitlementEnforced`, `checkPostAlarmGate`, `checkPostExportFence`, `groupTable`, `writeFileSync`, `main`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/verify-launch-readiness.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find verify-launch-readiness.mjs` or `prism_session:master_index_query`; open the source at `scripts/verify-launch-readiness.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
