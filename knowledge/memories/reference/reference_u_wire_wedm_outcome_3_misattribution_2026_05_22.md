---
name: reference-u-wire-wedm-outcome-3-misattribution-2026-05-22
description: U-WIRE-WEDM-OUTCOME-3 — 3 orphaned WEDM outcome engines wired into prism_edm (wedm_wire_spool_consumption, wedm_taper_error_budget, wedm_slug_tab_retention). 19/19 tests pass, tsc clean, round-1 wiring scrutiny PASS×2, test reviewers FAILed on 2 P1s both fixed with live-engine cross-check. SHIPPED but MISATTRIBUTED — absorbed into commit 50a3bd3d80 (slot:mike's U-CK09 close-out) via broad-stage peer race. The 4th absorbed-peer-commit misattribution this session-class. Charlie iter 17→18.
aliases: reference_u_wire_wedm_outcome_3_misattribution_2026_05_22
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.249Z
---


**2026-05-22 charlie /loop iter 17→18 (post-/compact fresh ctx).** Shipped U-WIRE-WEDM-OUTCOME-3 — wired 3 previously-orphaned WEDM outcome-estimator engines into the `prism_edm` MCP dispatcher.

**What it is.** Three real WEDM engines on disk had zero dispatcher references — dead capability in charlie's exact domain (wire EDM). Verified against `state/shared/UNWIRED-ENGINE-AUDIT-2026-05-07.json` + a fresh grep across ALL files under `mcp-server/src/tools/dispatchers/` (58 WEDM/WireEDM engines confirmed orphaned, audit not just stale). Picked the cleanest single-method coherent trio:
- `WEDMWireSpoolConsumptionEngine.calculate()` → action `wedm_wire_spool_consumption` — wire-spool consumption + mid-job spool-change risk (4-tier: none / single_change / multiple_changes / high_exposure).
- `WEDMTaperErrorBudgetEngine.calculate()` → action `wedm_taper_error_budget` — UV travel + RSS error budget (guide-tolerance + UV-encoder + wire-bow + calibration) + ISO 286 IT6..IT12 class prediction.
- `WEDMSlugTabRetentionEngine.calculate()` → action `wedm_slug_tab_retention` — Von Mises shear vs dynamic slug-weight demand safety factor (safe / marginal / at_risk / unsafe).

**Wiring.** Same template as the 4 ARC-MS6 units shipped this session (muS-C25/C22/C21/C23): 3 Zod schemas in `edmActionSchemas.ts` + 3 registrations in `EDM_ACTION_SCHEMAS` + 3 enum entries in `ACTIONS` + 3 case handlers + 3 `import type` lines for the typed Inputs. tsc rejected `Record<string,any> → typed Input` without an explicit cast (3 errors); fix was `params as <Input>` per case (provably safe because `validateActionParams(ALL_EDM_SCHEMAS)` runs at dispatcher line 473 before the switch and returns early on invalid params). 19-test round-trip + wiring + edge-case file `WEDMOutcomeEstimatorWiring.test.ts` (hook-scanned dir).

**Per-file scrutiny.** Round-1 wiring (schema + dispatcher pair) PASS×2 (zero P0/P1) — wiring-review-agent + reviewer both confirmed enum↔case↔schema 3-way match, schema-fidelity field-by-field, cast soundness. Round-1 test scrutiny FAILed on 3 P1s across the two reviewers: (1) `safety_factor` was `toBeGreaterThan(2)` — tautology, would pass for any positive; (2) registration test counted quoted strings ≥2 — would pass even if `case` body deleted; (3) header overclaimed "verifies BOTH halves of THE contract" while not invoking `validateActionParams`. All 3 fixed: SF now exact `toBe(59977.82)` (and the dynamic_factor case `toBe(35986.69)`) — values cross-checked by a live engine run (`npx tsx _wedm_calc_tmp.mjs` against the actual `wedmSlugTabRetentionEngine`); registration test now asserts `case "<a>:" body count == 1` AND `total quoted occurrences − cases ≥ 1`; header rewritten to state the test exercises the two composable halves WITHOUT a live MCP server and that the dispatcher forwards RAW params (not `safeParse().data`) so feeding raw input to the engine is faithful. Coverage gaps the reviewers flagged were also closed: added a `multiple_changes` spool case (exercises the engine's accumulator while-loop — previously dead under tests), a `dynamic_factor` override slug case, an `extended`-guide taper case, and a `non-positive spool_capacity_m` schema rejection.

**Round-2 test re-review rate-limited.** Both Round-2 test reviewers hit the same API rate-limit class observed at iter 13/14 (Reviewer A truncated mid-work; Reviewer B explicit `API Error: Server is temporarily limiting requests · Rate limited`). Per the iter-13/14 precedent ([[reference_us_c21_electrode_material_decision_2026_05_22]]): infra-blocked gates → never fake, document loudly, the work is mechanically verified (19/19 pass, exact literals cross-checked against live engines, all reviewer P1 findings concretely addressed).

**SHIPPED but MISATTRIBUTED — absorbed into commit `50a3bd3d80`.** When I tried to commit, the shared-tree multi-chat race did its thing: slot:mike's U-CK09 close-out commit (`[MAIN] [CLOSE-OUT]/U-CK09 (slot:mike): reconcile envelope drift — 3 commits git-proven`) ran a broad-stage that swept up my 3 worktree-modified files. `git show --stat 50a3bd3d80` confirms all 3 of my files are in it: `__tests__/WEDMOutcomeEstimatorWiring.test.ts (+313)`, `edmActionSchemas.ts (+113)`, `edmDispatcher.ts (+32)`. After absorption, `git status` for my 3 paths is empty and `git diff HEAD` for them returns nothing — the work IS shipped, the commit message just doesn't mention it. This is the 4th peer-absorption misattribution this session-class — see [[reference_u_css_chipload_complete_2026_05_20]], [[reference_iter2_html_adopt_misattribution_2026_05_18]], [[reference_u_wire_batch_processor_misattribution_2026_05_21]], [[reference_h8_misattribution_2026_05_20]]. The standing lesson: the slot-worktree migration ([[reference_slot_worktree_activation_2026_05_16]]) is the canonical fix; charlie should be on `slot/charlie` in `H:/prism-slot-charlie`, not in the shared `H:/prism` tree.

**Operator-queued next directive (charlie's next /loop phase).** Mid-iter the operator queued the next /goal block: *"compile all leftover wire edm units and tasks | utilize /system-viz to search all engines, algorithms, formulas, prism app features to finalize full build out of print to cnc programs for wire edm / wire edm wizard | use the finalized product to improve all existing wire edm programs in the JM Die system then, utilize it as training then produce brand new programs from new prints"*. Four phases: (1) compile remaining wire-EDM units; (2) /system-viz survey of EVERY existing engine/algorithm/formula/feature to compose the print-to-program wire-EDM WIZARD (integration mode, NOT new-build — the explicit instruction is "use what exists"); (3) run the wizard over JM Die's existing wire-EDM corpus (`H:/PRISM/JM DIE/WIRE EDM/...`) to improve the programs; (4) feed those improvements as training data + generate brand-new programs from new prints. Will fire on the next /loop cron (5-min cadence). Cannot start it on this iter — current iter must close out the U-WIRE-WEDM-OUTCOME-3 work cleanly first (per R10).

**Recurring lessons (session-cumulative).** (a) The multi-chat shared-tree commit race is a Karpathy R7 problem (surface conflicts, don't average them) — the right move is slot-worktree migration, not retry-fighting. (b) Reviewer logic-PASS ≠ runtime-PASS — caught real arithmetic errors in reviewer suggestions (Reviewer A's `toBe(180)` was 1000× off; the real SF is 59977.82). Always cross-check reviewer-suggested literals against a live engine run before committing them as test assertions. (c) Infra-blocked gates: ledger UNMARKED, document PENDING, retry on next fire — the pattern that worked at iter 13/14 worked again at iter 17→18.
