---
name: reference_code_script_fleetreapersweep
description: "NOTE: no shebang. This file IS run as a CLI (--once / --monitor-loop / etc.)"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/fleet-reaper-sweep.mjs
  sourceHash: 69e5db46
  loc: 3499
aliases: fleet-reaper-sweep.mjs
---

# fleet-reaper-sweep.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/fleet-reaper-sweep.mjs` (3499 LOC)

## What it is
```
NOTE: no shebang. This file IS run as a CLI (--once / --monitor-loop / etc.)
but ALL its invocations go through explicit `node X.mjs` (the scheduled task,
the Stop-hook arm, the /fleet-reaper skill, the alpha-guardian) — never via
chmod+x + `./X.mjs` direct execution. A line-1 `#!` is fine for node + esbuild
+ bash but vite's SSR transform does NOT strip it; it injects its preamble
above, stranding `#!` mid-file and breaking the whole .claude/helpers/*.test.mjs
vitest suite. Removing the vestigial shebang is the cleanest fix.
```


## Exports
`LEDGER_SCHEMA_VERSION` (const), `DEFAULT_INTERVAL_SEC` (const), `DEFAULT_AGE_FLOOR_SEC` (const), `DEFAULT_KILL_AFTER` (const), `DEFAULT_MEM_PRESSURE_PCT` (const), `DEFAULT_MEM_CRITICAL_PCT` (const), `DEFAULT_BALLAST_MB` (const), `DEFAULT_SOFT_RELIEF_AGE_SEC` (const), `DEFAULT_SOFT_RELIEF_PRESSURE_PCT` (const), `DEFAULT_SOFT_RELIEF_ALIVE_RSS_MB` (const), `DEFAULT_OLLAMA_URL` (const), `DEFAULT_OLLAMA_PREWARM_MODEL` (const), `DEFAULT_OLLAMA_KEEP_ALIVE` (const), `DEFAULT_HINT_TTL_SEC` (const), `DEFAULT_GPU_FREE_MIN_MB` (const), `DEFAULT_HINT_THRESHOLD_DELTA` (const), `HINT_SCHEMA_VERSION` (const), `readHostMemory` (function), `updateLedger` (function), `shouldReap` (function)

## Key methods
`tmpdir`, `writeFileSync`, `resolvePowershell`, `mkdirSync`, `renameSync`, `sleepSync`, `appendFileSync`, `execFileSync`, `writeLedgerAtomic`, `appendAuditLines`, `recordEvent`, `runDockerRestart`, `logSweep`, `spawnImpl`, `main`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/fleet-reaper-sweep.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find fleet-reaper-sweep.mjs` or `prism_session:master_index_query`; open the source at `scripts/fleet-reaper-sweep.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
