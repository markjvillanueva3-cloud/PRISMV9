---
name: reference_code_algorithm_dynamictimewarping
description: "DynamicTimeWarping — optimal elastic alignment of two (possibly different-length,"
metadata:
  type: reference
  kind: code-algorithm
  galaxy: unassigned
  sourcePath: mcp-server/src/algorithms/DynamicTimeWarping.ts
  sourceHash: 3be04f12
  loc: 222
aliases: DynamicTimeWarping
---

# DynamicTimeWarping -- Algorithm SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/algorithms/DynamicTimeWarping.ts` (222 LOC)

## What it is
```
DynamicTimeWarping — optimal elastic alignment of two (possibly different-length,
possibly speed-warped) time series, with the minimum-cost warping distance.

  D(i,j) = dist(aᵢ, bⱼ) + min{ D(i−1,j), D(i,j−1), D(i−1,j−1) }
  DTW    = D(T_a, T_b) ,  path = backtrace of the argmin from (T_a, T_b)

Exact dynamic program — O(T_a·T_b·d), no iteration / convergence / precision
tolerance. Unlike Euclidean distance it tolerates time shifts and local
speed differences, which is exactly what comparing two machine runs / sensor
streams / tool-wear signatures / cycle-time traces requires:
  • match a live spindle-load trace against a known-good reference run
  • cluster/retrieve similar telemetry episodes (k-NN with DTW distance)
  • align quoted vs actual cycle-time curves for reconciliation

Sequences are number[][] — each row is a timestep's feature vector (use d=1,
i.e. [[x],[y],…], for a scalar
```


## Exports
`DTWMetric` (type), `DTWInput` (interface), `DTWOutput` (interface), `DynamicTimeWarping` (const)

## Key methods
`validate`, `calculate`, `getMetadata`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/algorithms/DynamicTimeWarping.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Algorithm

## How to query it
Recall by meaning via `/system-viz find DynamicTimeWarping` or `prism_session:master_index_query`; open the source at `mcp-server/src/algorithms/DynamicTimeWarping.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
