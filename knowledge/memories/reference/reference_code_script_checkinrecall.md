---
name: reference_code_script_checkinrecall
description: "checkin-recall.mjs — local-compute recall + Ollama distill for the /checkin pipeline."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/checkin-recall.mjs
  sourceHash: 27808ff2
  loc: 327
aliases: checkin-recall.mjs
---

# checkin-recall.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/checkin-recall.mjs` (327 LOC)

## What it is
```
checkin-recall.mjs — local-compute recall + Ollama distill for the /checkin pipeline.

WHY THIS EXISTS
The /checkin pipeline's Steps 8-11 + High-ROI table NAMED many capability
surfaces but never INVOKED them ("named-not-invoked" regression class,
CLAUDE.md). The user directive (2026-05-16) is: auto-invoke every named
surface, BUT keep the token cost bounded by routing through Obsidian (local
file indexes — zero Claude tokens) and Ollama (local 7b — offloads the
summarize/distill that Claude would otherwise pay for).

This helper is the single composition point those auto-invoke steps call.
Claude only ever reads the ≤3 compact distilled lines this prints — the
search + summarize happen in local compute.

COMPOSES (does not reinvent — R8/dedup):
  - scripts/lib/master-index-search-lib.mjs  → runMasterIndexSearch / runTribalSearch
  - knowledge/wiki/index.md                  → local BM25 
```


## Exports
`roiGate` (re-export), `recallMemory` (re-export), `recallSkill` (re-export), `recallFileLines` (re-export), `ollamaDistill` (re-export), `tokenize` (re-export), `clip` (re-export)

## Key methods
`clip`, `writeFileSync`, `main`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/checkin-recall.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find checkin-recall.mjs` or `prism_session:master_index_query`; open the source at `scripts/checkin-recall.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
