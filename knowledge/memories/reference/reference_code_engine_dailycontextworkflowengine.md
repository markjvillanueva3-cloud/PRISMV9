---
name: reference_code_engine_dailycontextworkflowengine
description: "yesterday's daily context note, active project overviews, and"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/DailyContextWorkflowEngine.ts
  sourceHash: c9569021
  loc: 766
aliases: DailyContextWorkflowEngine
---

# DailyContextWorkflowEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/DailyContextWorkflowEngine.ts` (766 LOC)

## What it is
yesterday's daily context note, active project overviews, and

```
DailyContextWorkflowEngine
==========================

OBSIDIAN-INTELLIGENCE-MS3/B1/U-DAILY-CONTEXT-WORKFLOW

Builds the cyrilXBT morning brief by stitching together three vault
surfaces — yesterday's daily context note, active project overviews, and
the inbox — into one markdown summary that drops in
`knowledge/memories/generated/DAILY-CONTEXT-YYYY-MM-DD.md`.

Separation of concerns
----------------------
The engine has two phases:

  1. `collectSources()` — pure-determinist filesystem read. Given the same
     vault state + `now`, the returned `DailyContextSources` is byte-stable.
     No network, no LLM, no clock dependency outside `now`. This is the
     slice the tests pin reference values against.

  2. `synthesize()` — composes the brief. If an `ollamaClient` is supplied
     it routes each section through `qwen2.5-coder` for one-paragraph
     summarization (token-economy complia
```


## Exports
`DailyContextSource` (interface), `DailyContextSources` (interface), `DailyContextSection` (interface), `DailyContextBrief` (interface), `OllamaSummariseClient` (interface), `DailyContextOptions` (interface), `DailyContextOptionsSchema` (const), `DailyContextWorkflowEngine` (class), `_internals` (const), `dailyContextWorkflowEngine` (const), `buildAndOptionallyWriteDailyContext` (function)

## Key methods
`summarise`, `collectSources`, `validateOptions`, `synthesize`, `mkdirSync`, `writeFileSync`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/DailyContextWorkflowEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find DailyContextWorkflowEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/DailyContextWorkflowEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
