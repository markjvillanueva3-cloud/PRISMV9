---
name: reference_code_lib_bgappthrottle
description: "bg-app-throttle.mjs — pure-injected helper for FLEET-REAPER-MS3/U-FR-MS3-B."
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/bg-app-throttle.mjs
  sourceHash: ba5d9e63
  loc: 223
aliases: bg-app-throttle.mjs
---

# bg-app-throttle.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/bg-app-throttle.mjs` (223 LOC)

## What it is
```
bg-app-throttle.mjs — pure-injected helper for FLEET-REAPER-MS3/U-FR-MS3-B.

Tier-1.5 between soft-relief and serviceRestart: under memory squeeze,
drop the top-N non-Claude heavy processes (Chrome/Discord/Steam) to
BelowNormal priority. Reversible (hysteresis: drop at memPressurePct,
restore at memPressurePct - 5). The apps don't notice (idle UI threads
don't lose anything) and Claude gets the cycles.

Strictly additive; respects fleet-wide PRISM_FLEET_REAPER_DISABLE plus its
own PRISM_FR_BG_THROTTLE_DISABLE kill switch.

Anti-regression rules from spec U-FR-MS3-B:
  1. Exclusion list is exhaustive — anything not listed gets throttled
     (preferred over false-positive exclusion). Claude descendants are
     ALWAYS excluded (computed from procIndex, not hardcoded names).
  2. Hysteresis asymmetric — drop at memPressurePct, restore at -5.
  3. Stamp file capped at 1 MB; if exceeded, res
```


## Exports
`DEFAULT_TOP_N` (const), `DEFAULT_MIN_RSS_MB` (const), `MIN_TOP_N` (const), `MAX_TOP_N` (const), `MIN_MIN_RSS_MB` (const), `MAX_MIN_RSS_MB` (const), `STAMP_MAX_BYTES` (const), `ancestorChain` (function), `isClaudeRelated` (function), `shouldExclude` (function), `pickThrottleCandidates` (function), `decideAction` (function), `buildStamp` (function), `readStamp` (function), `clampTopN` (function), `clampMinRssMb` (function), `_excludedNamesForTest` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/bg-app-throttle.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find bg-app-throttle.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/bg-app-throttle.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
