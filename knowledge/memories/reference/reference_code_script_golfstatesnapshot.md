---
name: reference_code_script_golfstatesnapshot
description: "golf-state-snapshot.mjs — CLEANUP-MS0 / U-CLEANUP-G12"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/golf-state-snapshot.mjs
  sourceHash: 7ac98b0a
  loc: 372
aliases: golf-state-snapshot.mjs
---

# golf-state-snapshot.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/golf-state-snapshot.mjs` (372 LOC)

## What it is
```
golf-state-snapshot.mjs — CLEANUP-MS0 / U-CLEANUP-G12

Daily backup of the golf hygiene chat's durable state to
`H:/prism-backups/golf-state/<ISO>/`, with 30-day retention.

What it snapshots (each best-effort — a missing source is recorded in
`skipped`, never a hard failure):
  - state/shared/coordination.db        (H8 SQLite WAL — full claim/presence store)
  - state/shared/golf-owned-paths.json  (G11 — the write allowlist)
  - state/shared/golf-cron-registry.json (E2 — golf's registered crons)
  - state/shared/golf-token-budget.json (golf's daily token budget)
  - bug_attribution SQL-dump            (the bug_attribution table from
    coordination.db → bug_attribution.jsonl, one row per line. Portable +
    diffable + restorable without the full .db. Gracefully degrades: a
    missing db / missing table / unloadable sqlite module is recorded in
    `skipped`, and the raw coordination.
```


## Exports
`fsSafeIso` (function), `parseSnapshotDirName` (function), `parseArgs` (function), `dumpBugAttribution` (function), `snapshot` (function), `run` (function), `SCHEMA_VERSION` (re-export), `DEFAULT_RETAIN_DAYS` (re-export), `SOURCE_FILES` (re-export), `DUMP_TABLE` (re-export), `DUMP_BASENAME` (re-export)

## Key methods
`mkdirSync`, `copyFileSync`, `writeFileSync`, `rmSync`, `printHelp`, `run`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/golf-state-snapshot.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find golf-state-snapshot.mjs` or `prism_session:master_index_query`; open the source at `scripts/golf-state-snapshot.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
