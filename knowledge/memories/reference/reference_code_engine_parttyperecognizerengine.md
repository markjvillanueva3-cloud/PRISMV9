---
name: reference_code_engine_parttyperecognizerengine
description: "L1 of the 4-layer per-part-type pipeline stack"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PartTypeRecognizerEngine.ts
  sourceHash: f540abb6
  loc: 210
aliases: PartTypeRecognizerEngine
---

# PartTypeRecognizerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PartTypeRecognizerEngine.ts` (210 LOC)

## What it is
L1 of the 4-layer per-part-type pipeline stack

```
PartTypeRecognizerEngine — L1 of the 4-layer per-part-type pipeline stack

Maps a CAD feature signature (bounding box + feature counts + symmetry +
material + tolerance band) to a part class within a domain. Output drives
L2 adapter selection (Mill prismatic / Lathe shaft / Wire-EDM punch-die / ...).

Heuristic + deterministic — no ML. Inputs are the feature signatures already
extracted by CADFeatureRecognitionEngine; this engine is the routing layer
over those signatures. Cited rules per domain:

 - Mill: prismatic (≥3 orthogonal pocket faces + ≤2:1 aspect), mold-die
   (Z-curvature high, surface-finish-class strict), thin-wall (min-wall ≤
   1.0mm), deep-pocket (depth/diameter > 5), aerospace-pocket (Ti-Al-Inco
   + ≥3-axis Z-floor + structural-rib), micro-mill (max bbox ≤ 25mm).
 - Lathe: shaft (length/diameter > 3 + concentric features), bushing
   (length/diameter ≤ 1.5 + bore featu
```


## Exports
`Domain` (type), `PartFeatureSignature` (interface), `MillClass` (type), `LatheClass` (type), `WireEDMClass` (type), `PartClassResult` (interface), `PartTypeRecognizerEngine` (class), `partTypeRecognizerEngine` (const)

## Key methods
`recognize`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PartTypeRecognizerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PartTypeRecognizerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PartTypeRecognizerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
