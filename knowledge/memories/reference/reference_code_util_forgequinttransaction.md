---
name: reference_code_util_forgequinttransaction
description: "Forge-Quint Transaction — atomic 5-artifact asset creation"
metadata:
  type: reference
  kind: code-util
  galaxy: unassigned
  sourcePath: mcp-server/src/utils/forgeQuintTransaction.ts
  sourceHash: 20ef3e51
  loc: 435
aliases: forgeQuintTransaction.ts
---

# forgeQuintTransaction.ts -- Util SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/utils/forgeQuintTransaction.ts` (435 LOC)

## What it is
```
Forge-Quint Transaction — atomic 5-artifact asset creation

Universal Phase 0.3. Replaces the advisory "forge-triple" skill flow
(hook + action + skill — three files created one at a time, each a
potential tear point if the harness dies mid-way) with a genuine
transaction. Every new engine must ship with ALL of:

  1. engine      — the TypeScript engine file under src/engines/
  2. hook        — PreTool / PostTool enforcement hook
  3. action      — dispatcher z.enum action + switch case registration
  4. skill       — user-facing slash command under .claude/commands/
  5. registryDelta — cross-session registry entry + barrel export line

All 5 are pre-flight dedup-checked (via DuplicationGuardEngine + its
semantic-signature companion), staged to per-transaction tmp files,
then atomically renamed in a single critical section protected by
proper-lockfile. On any failure (dedup rejection, 
```


## Exports
`ForgeArtifactKind` (type), `ForgeArtifact` (interface), `ForgeQuintSpec` (interface), `ForgeQuintResult` (interface), `forgeQuint` (function), `assertQuintShape` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/utils/forgeQuintTransaction.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Util

## How to query it
Recall by meaning via `/system-viz find forgeQuintTransaction.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/utils/forgeQuintTransaction.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
