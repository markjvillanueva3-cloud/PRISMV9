---
name: reference_code_engine_actiontraceengine
description: "ActionTraceEngine.ts — append-only agent-write trace log + query API."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ActionTraceEngine.ts
  sourceHash: c06d4d54
  loc: 262
aliases: actionTraceEngine
---

# actionTraceEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ActionTraceEngine.ts` (262 LOC)

## What it is
```
ActionTraceEngine.ts — append-only agent-write trace log + query API.

OBSIDIAN-INTELLIGENCE-MS3 / U-ACTION-TRACES (D4).

Every agent write (memory mirror, wiki write, file Edit/Write) can be
recorded as a single graph edge:

  { ts, agent, sessionId, promptHash, tool, target, action }

The log is an APPEND-ONLY JSONL at `state/shared/action-traces.jsonl`.
Append-only is the load-bearing invariant: `recordTrace` only ever
`appendFileSync`s a single newline-terminated line; the file is never
rewritten, truncated, or sorted. Rollback per the unit spec is simply
"stop calling recordTrace — the existing JSONL stays as the historical
record". `queryTraces` is read-only.

Path resolution is done PER CALL (not at module load) so that the
`PRISM_ACTION_TRACE_FILE` env override is honoured by tests that set it
after import — the hermetic-temp-file pattern used across PRISM.

Wiring: queryable via
```


## Exports
`ACTION_TRACE_SCHEMA_VERSION` (const), `ACTION_TRACE_ENGINE_VERSION` (const), `ActionTraceEdgeSchema` (const), `ActionTraceEdge` (type), `ActionTraceInput` (type), `ActionTraceQuery` (interface), `ActionTraceQueryResult` (interface), `hashPrompt` (function), `recordTrace` (function), `queryTraces` (function), `actionTraceEngine` (const)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ActionTraceEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find actionTraceEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ActionTraceEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
