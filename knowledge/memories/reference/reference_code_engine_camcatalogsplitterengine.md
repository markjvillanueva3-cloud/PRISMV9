---
name: reference_code_engine_camcatalogsplitterengine
description: "Splits consolidated CAM catalog files (captured as single JSONs per CAM"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMCatalogSplitterEngine.ts
  sourceHash: bde95db9
  loc: 226
aliases: CAMCatalogSplitterEngine
---

# CAMCatalogSplitterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMCatalogSplitterEngine.ts` (226 LOC)

## What it is
Splits consolidated CAM catalog files (captured as single JSONs per CAM

```
CAMCatalogSplitterEngine — PHASE-1 catalog fan-out
=====================================================

Splits consolidated CAM catalog files (captured as single JSONs per CAM
during Phase-0 capture) into per-module deliverables expected by the
Phase-1 roadmap (U-CAM02..U-CAM12 for hyperMILL, U-CAM14..U-CAM19 for
Mastercam, etc.).

Input shape (consolidated catalog):
  { schema_version, system_id, catalog_id, catalog_name,
    modules: {
      "<module_id>": {
        module_id, module_name, description,
        total_operations, total_parameters,
        operations: [{ id, name, cfg_file, description, dialogs: [...] }]
      }
    },
    common_parameters, tilt_strategies, macro_types, tribal_knowledge
  }

Output shape (per-module deliverable) matches the consolidated module
body plus provenance breadcrumbs so downstream linkers can trace back
to the single source of truth.

Why spli
```


## Exports
`ConsolidatedCatalog` (interface), `ModuleEntry` (interface), `SplitRule` (interface), `SplitResult` (interface), `CAMCatalogSplitterEngine` (class), `camCatalogSplitterEngine` (const)

## Key methods
`split`, `splitByKeys`, `listModules`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CAMCatalogSplitterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMCatalogSplitterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMCatalogSplitterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
