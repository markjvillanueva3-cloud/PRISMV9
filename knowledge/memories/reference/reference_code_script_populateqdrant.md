---
name: reference_code_script_populateqdrant
description: "PSN-ENHANCE-MS0/U-PSN-QDRANT-POPULATE — ingest the 768d node embeddings"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/populate-qdrant.mjs
  sourceHash: 25edba28
  loc: 259
aliases: populateQdrant
---

# populateQdrant -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/populate-qdrant.mjs` (259 LOC)

## What it is
```
PSN-ENHANCE-MS0/U-PSN-QDRANT-POPULATE — ingest the 768d node embeddings
from H:/prism/state/shared/nn-graph/node-embeddings-768d.jsonl into a
named Qdrant collection. Closes iter-13 follow-up: collections exist but
are empty, blocking the hybrid (BM25 + vector + graph + episode) retrieval.

Data shape: each JSONL line is `{n: "node-id", q: int8[]}` (RaBitQ-style
int8 quantization of nomic-embed-text 768-d). Qdrant accepts plain float
vectors so we dequantize by dividing by SCALE (default 127 — calibrated
from observed range in the file). The `__meta` first-line is skipped.

Idempotent at the Qdrant API level (upsert overwrites by id).

Flags:
  --collection NAME        target collection (default: prism_engines)
  --batch-size N           upsert batch size (default: 100)
  --limit N                stop after N vectors (default: Infinity)
  --dry-run                load + dequantize, do NO
```


## Exports
`loadEmbeddings` (function), `dequantize` (function), `nodeIdToPointId` (function), `buildBatch` (function), `populateQdrant` (function), `streamPopulateQdrant` (function)

## Key methods
`flush`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/populate-qdrant.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find populateQdrant` or `prism_session:master_index_query`; open the source at `scripts/populate-qdrant.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
