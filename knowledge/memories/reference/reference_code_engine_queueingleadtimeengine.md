---
name: reference_code_engine_queueingleadtimeengine
description: "honest lead-time prediction via the Kingman (VUT) approximation."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/QueueingLeadTimeEngine.ts
  sourceHash: b9463cec
  loc: 220
aliases: QueueingLeadTimeEngine
---

# QueueingLeadTimeEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/QueueingLeadTimeEngine.ts` (220 LOC)

## What it is
honest lead-time prediction via the Kingman (VUT) approximation.

```
QueueingLeadTimeEngine — honest lead-time prediction via the Kingman (VUT) approximation.

Invention E7 (builder-ready spec:
knowledge/wiki/architecture/prism-invention-queueing-leadtime-spec.md).

The naive `prism_scheduling:lead_time_estimate` multiplies process time by a
hardcoded queue_factor (2.5). That ignores the dominant effect: queue time
EXPLODES as utilization rho -> 1. Kingman's VUT approximation:

  CT_q = ( (Ca^2 + Cs^2) / 2 ) * ( rho / (1 - rho) ) * te

  V = variability term (Ca, Cs = coeff of variation of arrivals / service)
  U = utilization term rho/(1-rho)  — 0.80 -> 4 ; 0.90 -> 9 ; 0.95 -> 19
  T = te = effective process time

Pure deterministic math — no material constants, no NN, no randomness.
Reference: Hopp & Spearman "Factory Physics"; Kingman (1961).
```


## Exports
`QueueWorkstation` (interface), `QueueLeadTimeInput` (interface), `QueuePerStation` (interface), `QueueBottleneck` (interface), `QueueLeadTimeResult` (interface), `QueueingLeadTimeEngine` (class), `queueingLeadTimeEngine` (const)

## Key methods
`predict`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/QueueingLeadTimeEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find QueueingLeadTimeEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/QueueingLeadTimeEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
