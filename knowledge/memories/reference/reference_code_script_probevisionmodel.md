---
name: reference_code_script_probevisionmodel
description: "scripts/probe-vision-model.mjs"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/probe-vision-model.mjs
  sourceHash: 65e9cd1e
  loc: 146
aliases: probe-vision-model.mjs
---

# probe-vision-model.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/probe-vision-model.mjs` (146 LOC)

## What it is
```
scripts/probe-vision-model.mjs

U-XRAY-VISION-PROBE -- empirically test ANY vision model on a REAL print, bypassing the
isThinkingTrap() pre-filter that the ensemble + bench-vision-ocr-ab use. Built because that filter
ASSUMES any non-"-instruct" qwen3-vl tag is a thinking-trap and refuses to even run it, so there was
no way to answer the zulu ladder work-order's empirical question: "is bare qwen3-vl:32b actually
JSON-safe on a blueprint, or does it leak <think>?" This probe runs the model directly + reports
(a) thinking-trap? (b) dims extracted (vs the 2-model ensemble that FAILED on this exact print).

Reuses (does NOT reimplement): buildVisionPrompt / buildOllamaRequestBody / parseVisionResponse
(ollama-vision-extract-lib) + the curl @reqfile transport (node fetch fails against Ollama, per
page-classify.mjs). Diagnostic CLI -- no dispatcher wiring (sibling of page-classify.mjs).

USAG
```


## Exports
`detectThinkingTrap` (function)

## Key methods
`mkdirSync`, `writeFileSync`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/probe-vision-model.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find probe-vision-model.mjs` or `prism_session:master_index_query`; open the source at `scripts/probe-vision-model.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
