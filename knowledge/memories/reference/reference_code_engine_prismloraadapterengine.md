---
name: reference_code_engine_prismloraadapterengine
description: "Phase 0.19 U-LLM3"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PRISMLoRAAdapterEngine.ts
  sourceHash: 77346fd6
  loc: 326
aliases: PRISMLoRAAdapterEngine
---

# PRISMLoRAAdapterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PRISMLoRAAdapterEngine.ts` (326 LOC)

## What it is
Phase 0.19 U-LLM3

```
PRISMLoRAAdapterEngine — Phase 0.19 U-LLM3

Manages the *inventory* of LoRA adapters trained by
`IncrementalLearningEngine` (U-LLM6). This is the registry side —
adapters live on disk under a root directory, each in its own folder
with a small JSON descriptor. The engine supports:

  - register()  : record a new adapter produced by a training run
  - list()      : enumerate adapters (optionally filtered by base model)
  - activate()  : mark one adapter as "active" for a base model, so
                  downstream orchestration (U-LLM4/7) knows which
                  fine-tune to apply
  - get()       : look up a single adapter by id
  - remove()    : delete an adapter (descriptor + folder) — used when
                  a run is superseded or a bad adapter must be retired

Nothing here spawns Python. Actual training is the Python trainer's
job; this engine only cares about *where* the ad
```


## Exports
`AdapterDescriptorSchema` (const), `AdapterDescriptor` (type), `RegisterInput` (interface), `ListFilter` (interface), `PRISMLoRAAdapterDeps` (interface), `PRISMLoRAAdapterEngine` (class), `prismLoRAAdapterEngine` (const)

## Key methods
`getDataDir`, `getAdapterDir`, `register`, `list`, `activate`, `activeFor`, `activeMap`, `remove`, `summary`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PRISMLoRAAdapterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PRISMLoRAAdapterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PRISMLoRAAdapterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
