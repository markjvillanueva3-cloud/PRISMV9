---
name: reference_code_engine_wetruncustomercommunicationlogengine
description: "WetRunCustomerCommunicationLogEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WetRunCustomerCommunicationLogEngine.ts
  sourceHash: 7c219d64
  loc: 483
aliases: WetRunCustomerCommunicationLogEngine
---

# WetRunCustomerCommunicationLogEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WetRunCustomerCommunicationLogEngine.ts` (483 LOC)

## What it is
WetRunCustomerCommunicationLogEngine

```
WetRunCustomerCommunicationLogEngine
------------------------------------------------------------
Append-only log of customer communications during a wet-run
pilot engagement. Every communication (outbound or inbound)
is recorded with a monotonic sequence number and timestamp,
a named author, explicit recipients, a topic class, and a
minimum-length summary.

Each topic carries a regulatory-style acknowledgment SLA. An
outbound entry with a topic that has an SLA must be
acknowledged by the customer (recorded via acknowledge())
before the SLA window expires. listBreached() surfaces the
expired unacknowledged entries so auditors can see them.

The log is append-only — entries cannot be mutated or
deleted after record(). Only acknowledgment and closure are
additive. Attempting to alter state silently is rejected.

@milestone LATHE-PROD-READY-MS0
@unit U-LPR-CUST-COMMS
```


## Exports
`Channel` (type), `Direction` (type), `TopicKind` (type), `EntryState` (type), `Acknowledgment` (interface), `CommsEntry` (interface), `RecordInput` (interface), `AckInput` (interface), `CloseInput` (interface), `BreachReport` (interface), `TopicSummary` (interface), `Snapshot` (interface), `WetRunCustomerCommunicationLogEngine` (class), `wetRunCustomerCommunicationLogEngine` (const)

## Key methods
`record`, `acknowledge`, `close`, `listBreached`, `summariseByTopic`, `getEntry`, `listEntries`, `openCount`, `breachedCount`, `snapshot`, `slaWindowMs`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WetRunCustomerCommunicationLogEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WetRunCustomerCommunicationLogEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WetRunCustomerCommunicationLogEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
