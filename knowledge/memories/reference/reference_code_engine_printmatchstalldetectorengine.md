---
name: reference_code_engine_printmatchstalldetectorengine
description: "LATHE-PROD-READY-MS0/U-LPR-STALL-PRINTS"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PrintMatchStallDetectorEngine.ts
  sourceHash: 8d4b246b
  loc: 265
aliases: PrintMatchStallDetectorEngine
---

# PrintMatchStallDetectorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PrintMatchStallDetectorEngine.ts` (265 LOC)

## What it is
LATHE-PROD-READY-MS0/U-LPR-STALL-PRINTS

```
PrintMatchStallDetectorEngine — LATHE-PROD-READY-MS0/U-LPR-STALL-PRINTS

Watchdog for the print-matching pipeline. Prints enter the match queue,
advance through stages (ingest → features → similarity → disposition),
and emit completion when a matching program is found or a no-match
disposition is reached. Anything in between that sits longer than its
budget is "stalled" — the pipeline isn't failing, it's just silent.

Silent failures in a human-in-the-loop system are worse than loud ones:
the operator starts wondering if they mistyped the print, the pipeline
never shouts for help, and hours leak. This engine converts silence into
a typed escalation payload with the context needed to rescue the job.

Semantics
---------
- startTracking(job_id, stage): job begins a stage with a timestamp
- markProgress(job_id, stage): the stage moved (reset stall clock)
- advanceStage(job_id, from_stage, t
```


## Exports
`MatchStage` (type), `StallPriority` (type), `StallEvent` (interface), `TrackerStats` (interface), `PrintMatchStallDetectorEngine` (class), `printMatchStallDetectorEngine` (const)

## Key methods
`startTracking`, `markProgress`, `advanceStage`, `completeJob`, `scan`, `scanOne`, `stats`, `statsAt`, `trackedIds`, `clear`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PrintMatchStallDetectorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PrintMatchStallDetectorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PrintMatchStallDetectorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
