---
name: reference_code_engine_cadautomationmocklayer
description: "extension-indexed fixture bank for CAD bridges."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADAutomationMockLayer.ts
  sourceHash: cf8137fb
  loc: 301
aliases: CADAutomationMockLayer
---

# CADAutomationMockLayer -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADAutomationMockLayer.ts` (301 LOC)

## What it is
extension-indexed fixture bank for CAD bridges.

```
CADAutomationMockLayer — extension-indexed fixture bank for CAD bridges.

The six CAD automation bridges (SolidWorks/Inventor/FreeCAD/Mastercam/
Fusion360/hyperMILL) each expose the same open/getGeometry/getToolpaths/
getOperationTree/exportSTEP/close surface, but in mock mode (PRISM_CAD_MOCK=1)
every bridge ships its own local fixture table. When the CADAutomationRouter
(U-CAUT10) serves arbitrary file types in CI it needs a single, canonical
source of truth it can index by extension — that's this engine.

What it provides
  - geometryFor(ext)      — canonical entity-count envelope per extension
  - operationTreeFor(ext) — canonical op count + cycle-code palette per ext
  - toolpathsFor(ext)     — flattened op list (equivalent to tree flatten)
  - fingerprint(ext)      — stable SHA-ish fingerprint for regression tests
  - listSupported()       — enumerate every extension the mock layer 
```


## Exports
`SupportedCADExt` (type), `CADMockGeometryEnvelope` (interface), `CADMockOperationEnvelope` (interface), `CADMockToolpathOp` (interface), `CADAutomationMockLayer` (class), `cadAutomationMockLayer` (const)

## Key methods
`listSupported`, `supports`, `geometryFor`, `operationTreeFor`, `toolpathsFor`, `fingerprint`, `allFingerprints`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CADAutomationMockLayer.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADAutomationMockLayer` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADAutomationMockLayer.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
