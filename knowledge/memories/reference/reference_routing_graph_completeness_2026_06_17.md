---
name: reference_routing_graph_completeness_2026_06_17
description: ROUTING-GRAPH-COMPLETENESS milestone -- advisory-feature catalog (all 832 hooks classified) + fillable task-graph template + gates-line wired into the live prompt-route auto-trigger
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.149Z
aliases: reference_routing_graph_completeness_2026_06_17
---


ROUTING-GRAPH-COMPLETENESS (slot:alpha, 2026-06-17, commits c4e8a402c8 + 777a1e8934 on cad-fusion-live-ms0). Operator /goal: "update the graph to account for every prompt... future prompts auto-trigger to look at the graph for order-of-operations... plans for all slash commands... make a template of the graph you auto-fill for the task... find all automated and advisory features and plan accordingly."

**What ALREADY existed (verified, R8 -- did NOT rebuild):** the prompt->graph->auto-trigger spine is complete: `scripts/lib/feature-routing-graph.mjs` (12 task classes x {ladder,model,commands,hooks,autoInvoke,antipattern,loopCron,execution}); `state/shared/operator-prompt-route-map.json` (4870 prompts mined from ALL 613 transcripts by `extract-operator-prompts.mjs`); `state/shared/slash-command-plans.json` (every slash command -> class); `.claude/hooks/prompt-route-inject.mjs` (the auto-trigger -- fires the route block on every UserPromptSubmit, demonstrably live).

**The two real GAPS built this milestone:**
1. `scripts/build-advisory-feature-catalog.mjs` (+test, 24 tests) -- a DETERMINISTIC routing VIEW over the canonical `HOOK_REGISTRY.json` (consumes, never re-enumerates) that enriches every hook with **behavioralKind** (block-gate=automated gate / advisory-inject / mutator / passive), **taskClass**, **disable-knob**. Emits `state/shared/advisory-feature-catalog.json`: 832 hooks, 413 wired, **125 gates / 354 advisors**, 293 knobbed. Projection = TWO buckets: `byTaskClass` (conf>0 class-specific) + `universalFeatures` (conf==0 always-on, e.g. duplication-hard-block). Conservation invariant: classSpecific(82)+universal(202)===actionableWired(284), none dropped. All 86 wired block-gates visible.
2. `scripts/task-graph-template.mjs` (+test, 11 tests) -- the keystone "fillable graph template": `fillTemplate(prompt)` composes all FOUR artifacts (graph policy + prompt-rank + slash-plans + catalog) into ONE auto-filled thorough-workflow graph; `renderTemplate()` = the repeatable numbered checklist. CLI: `node scripts/task-graph-template.mjs "<prompt>" [--json]`.
3. WIRE (R15): `prompt-route-inject.renderGatesLine` surfaces "GATES that MAY hard-stop this (if not env-muted)" on every prompt -- the catalog now reaches the live auto-trigger, not just an on-demand CLI. Knob `PRISM_ROUTE_GATES_INJECT=0`.

**Scrutiny caught (per-file 2-arm, 3 fix rounds + 3-of-3 PASS):** (a) a `classConf>0` filter silently DROPPED 205/290 actionable features incl. 61 hard gates -> fixed with the universalFeatures bucket; (b) comment-borne false-positive gates (10/134, incl *-advisory hooks) -> `stripComments`; (c) the comment-strip's first cut was a string-blind REGEX that ate `html-companion-guard`'s real `decision:"block"` (a glob literal `"patches/*.md"` mispaired `/*`...`*/`) -> rewrote `stripComments` as a string-state char-scanner.

**Doc-drift finding (R12, the catalog proved code != prose):** `CLAUDE.md` calls `comprehensive-build-enforce` a "HARD BLOCK" PreToolUse gate, but its code says `BLOCKING: never (injects additionalContext only)` -- it is advisory-inject, NOT a gate. The catalog classifies by emitted behavior (the truth), so it correctly flags it advisory. Don't trust the "HARD BLOCK" label in CLAUDE.md for this hook.

**Req #7 -- "apply the 3 x.com posts to the graph" (commit 6c04b49caa, U-XPOST-APPLY):** x.com is auth-walled (WebFetch -> HTTP 402), so principles were recovered via web search + PRISM's 25 prior vault analyses of the same genre (cyrilxbt/rody/humza/dunik/Karpathy second-brain). The prior milestone already found PRISM *exceeds* these posts. Applied to the canonical graph spec `state/shared/specs/FEATURE-ROUTING-GRAPH.md` (new section 6) by mapping each post's load-bearing principle to the CONCRETE graph element that embodies it (demonstrated via citation, not asserted): onchainmilady=agentic routing (routeTaskClass/modelTier/execution), zeuuss_01=loop engineering (loopCron/autoInvoke + the net-new **4-loop-type taxonomy** heartbeat/cron/hook/goal named per class), charliejhills=AI second-brain (operator-prompt-route-map mines all 613 transcripts = "mine your AI history"). Also fixed the spec's comprehensive-build-enforce HARD-BLOCK->advisory drift there. This is the established PRISM apply-pattern (map -> adversarial HAVE/PARTIAL/GAP, [[reference_cyrilxbt_obsidian_hermes_apply_assessment_2026_06_02]]).

**U-BUILD-COMPLETE-GATE (commits 4b895d334c + d4a0d07bbe, operator 2026-06-17 "all chats run loops until all gaps, bugs, errors and conflicts are filled and fixed before a build is considered complete"):** added `BUILD_COMPLETE_GATE` frozen constant (4 axes: gaps/bugs/errors/conflicts + loop rule) + a `doneWhen` field on the build/fix/domain classes of TASK_CLASS_POLICY (loopCron strengthened to "loop until the gate holds"). Surfaced on EVERY build-producing surface: `buildRoutingDigest` (DONE:), `prompt-route-inject` (live DONE-WHEN line every build prompt), `task-graph-template` (DONE-WHEN), the per-iteration `loop-goal-stack-advisor` (restates the gate each /loop iter -- the loop driver), the JSON catalog, + FEATURE-ROUTING-GRAPH.md section 0. Strictly additive (non-build classes byte-identical; learn/orchestrate correctly lack doneWhen -- their done-condition is corpus-drained/per-arm, not build-complete). 2-arm scrutiny PASS; loop-advisor wire closed scrutiny-B's P2. Also this session: re-affirmed [[feedback_loop_exhaustion_domain_fallback]] fleet-wide (operator: on goal-complete revert to backlog/queue/roadmaps, never idle) + routed the CLAUDE.md doctrine pointer to golf (alpha hard-blocked by claude-md-golf-only-guard).

**Iteration arc (operator "multiple rounds of loops to evaluate"):** built catalog+template+wire -> built utilization-audit (score 0.907) -> audit found locate gap -> curated-attribution fix closed it (punch list 1->0, class-coverage 1.0) -> hardened score honesty (0.821 3-leg). The audit (`scripts/routing-utilization-audit.mjs`) is the reusable "are we using it fully" answer. The catalog's `wired` flag = HOOK_REGISTRY registration, NOT live env-bypass state (e.g. stop_on_unwired_assets is wired-but-bypassed by PRISM_ALLOW_UNWIRED) -- hence "MAY hard-stop", framed honestly.

**U-CURATED-MULTIBUCKET (commit 0b6b34b023, closes the U-UTILIZATION-AUDIT deferred-P2 both scrutiny arms flagged):** a hook hand-curated into N task-classes (e.g. `scrutinize-before-stop` in build+review+session) previously landed in only the FIRST byTaskClass bucket (first-wins) -> `review` rendered 0 gates although it OWNS the scrutiny gate. Fix: `enrichHook` takes a `curatedClasses` array (primary `taskClass=cc[0]`, records the full set); `curatedClassMap` returns `{id:[classes]}` (all classes that list it); `aggregateCatalog` pushes the hook into EACH class bucket but counts the DISTINCT id ONCE via a `classSpecificIds` Set -> conservation `classSpecificCount + universalCount === actionableWired` still holds (89+195=284) while new `classPlacements` (93) >= distinct (89). Fixed the now-false "exactly one bucket" `note` (R12 doc-drift I'd have introduced). LIVE proof: `scrutinize-before-stop` now PRESENT in build+review+session; review 0->1 gate; punch-list 0; util score 0.821 stable. 113/113 across the whole milestone surface (catalog 28 + audit 13 + template 12 + loop-advisor 22 + routing-graph 38). 2-arm scrutiny PASS -- arm B traced all 3 downstream consumers (routing-utilization-audit / task-graph-template / prompt-route-inject) and confirmed each reads distinct-counts or per-class projections, never sums bucket lengths, so placements!=distinct misleads none; schema bump unwarranted (purely additive). BUILD-COMPLETE-GATE satisfied for the milestone: gaps/bugs/errors/conflicts all 0.

Related: [[reference_dev_velocity_autotrigger]] (the slash-trigger sibling); the U-SLASH-PLANS + U-PROMPT-EXTRACT + U-EXEC-POLICY units that built the spine this completes.
