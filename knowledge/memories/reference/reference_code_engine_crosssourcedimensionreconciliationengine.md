---
name: reference_code_engine_crosssourcedimensionreconciliationengine
description: "determine a part's TRUE dimensions by"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CrossSourceDimensionReconciliationEngine.ts
  sourceHash: 88f062a3
  loc: 329
aliases: CrossSourceDimensionReconciliationEngine
---

# CrossSourceDimensionReconciliationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CrossSourceDimensionReconciliationEngine.ts` (329 LOC)

## What it is
determine a part's TRUE dimensions by

```
CrossSourceDimensionReconciliationEngine — determine a part's TRUE dimensions by
reconciling candidates from the THREE independent JM data sources (xray cross-source
dimension determination, 2026-06-02):

  - print : OCR'd dimension from the engineering drawing      (what the engineer SPECIFIED) — DIMENSIONAL
  - cad   : measured from the STEP / solid-model geometry     (what was MODELED)            — DIMENSIONAL
  - cnc   : extracted from the G-code toolpath coordinates    (what actually gets CUT)      — PRESENCE-ONLY

SOURCE MODEL (grounded in corpus reality, NOT naive — mirrors the proven sibling
`scripts/lib/dimension-corroborate.mjs` (U-XCSD), which is the per-part pure-lib surface of
this same idea; THIS engine is the flat-DimCandidate[] dispatcher surface — same model, two APIs):
  • CAD geometry  → DIMENSIONAL. STEP/BRep gives exact nominal values (bbox, hole Ø, depths).
  • Prin
```


## Exports
`DimSource` (type), `DimType` (type), `DimCandidate` (interface), `DimContribution` (interface), `ReconciledDimension` (interface), `DimConflict` (interface), `ReconciliationReport` (interface), `DEFAULT_TOL_PCT` (const), `DEFAULT_TOL_ABS_MM` (const), `DEFAULT_ANGULAR_TOL_DEG` (const), `DEFAULT_SOURCE_CONFIDENCE` (const), `valuesMatch` (function), `combineConfidence` (function), `CrossSourceDimensionReconciliationEngine` (class), `crossSourceDimensionReconciliationEngine` (const)

## Key methods
`reconcile`, `flush`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CrossSourceDimensionReconciliationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CrossSourceDimensionReconciliationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CrossSourceDimensionReconciliationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
