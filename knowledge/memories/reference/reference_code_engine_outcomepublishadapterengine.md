---
name: reference_code_engine_outcomepublishadapterengine
description: "XPROC-NEURAL-CONNECT-MS0 / U-CN01"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/OutcomePublishAdapterEngine.ts
  sourceHash: cf181d3a
  loc: 465
aliases: OutcomePublishAdapterEngine
---

# OutcomePublishAdapterEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/OutcomePublishAdapterEngine.ts` (465 LOC)

## What it is
XPROC-NEURAL-CONNECT-MS0 / U-CN01

```
OutcomePublishAdapterEngine — XPROC-NEURAL-CONNECT-MS0 / U-CN01

The single canonical entry point for domain engines (mill / lathe / WEDM /
SF orchestrator / alarm decoder / probe ingest) to report a shop-floor
outcome to the cross-process learning graph. Wraps
`CrossProcessOutcomeStore.record()` (which already publishes
`outcome.recorded` to the feedback bus) plus a small set of conveniences:

  - publish()              — canonical bridge call (typed RecordEventInput)
  - publishWithActuals()   — pairs request + response + actual_metrics in one call
  - publishFailure()       — emits kind=failure with alarm/failure_mode context
  - publishOverride()      — emits kind=operator_override with reason
  - updateOutcome()        — pending → terminal transition (delegates to recordOutcome)
  - stats()                — per-adapter counters (totals + by_bridge + by_process + last_ts)
  - reset()
```


## Exports
`PublishOk` (interface), `UpdateOk` (interface), `OpErr` (interface), `PublishResult` (type), `UpdateResult` (type), `AdapterStats` (interface), `OutcomePublishAdapterEngine` (class), `outcomePublishAdapterEngine` (const), `outcomePublishAdapterDispatch` (function)

## Key methods
`publish`, `publishWithActuals`, `publishFailure`, `publishOverride`, `updateOutcome`, `stats`, `reset`

## Depends on
[[reference_code_engine_crossprocessoutcomestore]] | [[reference_code_engine_xprocoutcomeledgerdurability]]

## Where it lives
- **Source:** `mcp-server/src/engines/OutcomePublishAdapterEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find OutcomePublishAdapterEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/OutcomePublishAdapterEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
