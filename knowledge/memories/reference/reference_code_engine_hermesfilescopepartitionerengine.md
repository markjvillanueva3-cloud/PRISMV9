---
name: reference_code_engine_hermesfilescopepartitionerengine
description: "HZP02 file-scope partitioner."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HermesFileScopePartitionerEngine.ts
  sourceHash: 679f7a1d
  loc: 108
aliases: HermesFileScopePartitionerEngine
---

# HermesFileScopePartitionerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HermesFileScopePartitionerEngine.ts` (108 LOC)

## What it is
HZP02 file-scope partitioner.

```
HermesFileScopePartitionerEngine — HZP02 file-scope partitioner.

Prevents the index.lock thrash class observed 2026-05-24: when N parallel
agents try to write overlapping file sets, they serialize on the git
index.lock and the speed advantage of parallelism collapses.

Pure-core: given a set of agent specs each with a candidate file scope,
partition the files so each file is owned by exactly one agent.  Any
file claimed by 2+ agents is moved to `conflicts[]` — the caller resolves
(drop the file from one scope, or sequence those two agents).

@module engines/HermesFileScopePartitionerEngine
```


## Exports
`AgentFileScopeSchema` (const), `AgentFileScope` (type), `PartitionResult` (interface), `HermesFileScopePartitionerEngine` (class), `hermesFileScopePartitionerEngine` (const)

## Key methods
`partition`, `normalizePath`, `renderResult`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/HermesFileScopePartitionerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HermesFileScopePartitionerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HermesFileScopePartitionerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
