---
name: reference_code_engine_fusion360inhostrunnerengine
description: "Fusion360In Host Runner"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/Fusion360InHostRunnerEngine.ts
  sourceHash: ba37d51e
  loc: 639
aliases: Fusion360InHostRunnerEngine
---

# Fusion360InHostRunnerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/Fusion360InHostRunnerEngine.ts` (639 LOC)

## What it is
Fusion360In Host Runner

```
Fusion360InHostRunnerEngine — U-CAMTEST02
==========================================

PHASE-8: PRISM-side companion for the Fusion 360 JavaScript Add-In
test runner (resources/fusion360/prism-test-runner/index.js). The
Add-In loads inside Fusion 360 via the `adsk.fusion` API, registers
with the PRISM Plugin Communication Hub over WebSocket + JSON-RPC 2.0,
and streams scenario + assertion results back here.

Unlike U-CAMTEST01 (hyperMILL — XML-RPC), the Fusion 360 transport is
JSON-RPC 2.0. This engine therefore enforces JSON-RPC shape on every
outgoing envelope in addition to the 7-family assertion contract
shared across all four host runners.

Fusion-specific descriptor axes (the "exhaust input possibilities"
directive from the goal tracker maps to these):

  1. setup_type         — milling | turning | inspection
  2. wcs_mode           — model_origin | stock_top | stock_point
  3. stoc
```


## Exports
`FusionSetupTypeSchema` (const), `FusionSetupType` (type), `FusionWCSModeSchema` (const), `FusionWCSMode` (type), `FusionStockStrategySchema` (const), `FusionStockStrategy` (type), `FusionCoolantStrategySchema` (const), `FusionCoolantStrategy` (type), `FusionPostDialectSchema` (const), `FusionPostDialect` (type), `FusionToolLengthSourceSchema` (const), `FusionToolLengthSource` (type), `FusionScenarioCategorySchema` (const), `FusionScenarioCategory` (type), `FusionScenarioDescriptorSchema` (const), `FusionScenarioDescriptor` (type), `FusionPlannedFrameSchema` (const), `FusionPlannedFrame` (type), `FusionScenarioPlanSchema` (const), `FusionScenarioPlan` (type)

## Key methods
`register`, `supportedSetupTypes`, `supportedWCSModes`, `supportedStockStrategies`, `supportedCoolantStrategies`, `supportedPostDialects`, `supportedToolLengthSources`, `planScenario`, `frameToEnvelope`, `summarize`, `getPlan`, `getStats`, `resetSession`, `resetAll`

## Depends on
[[reference_code_engine_camplugincommunicationhubengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/Fusion360InHostRunnerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find Fusion360InHostRunnerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/Fusion360InHostRunnerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
