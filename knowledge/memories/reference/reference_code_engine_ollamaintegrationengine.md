---
name: reference_code_engine_ollamaintegrationengine
description: "Phase 0.19 U-LLM2"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/OllamaIntegrationEngine.ts
  sourceHash: 5c6ace35
  loc: 244
aliases: OllamaIntegrationEngine
---

# OllamaIntegrationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/OllamaIntegrationEngine.ts` (244 LOC)

## What it is
Phase 0.19 U-LLM2

```
OllamaIntegrationEngine — Phase 0.19 U-LLM2

Operational layer on top of the raw `OllamaClientEngine` RPC client.
Tracks connection health, caches the installed-model roster, stores
per-task default model selection, and offers a `warmUp()` path that
pre-loads a model into VRAM so the first shop query isn't cold.

The raw client does one thing (RPC). This engine is the "is Ollama
ready, and which model should we call?" brain that higher layers
(LocalModelOrchestrator, web status page, the `/model-status` skill)
can query without re-implementing boilerplate.

Design rules:
  1. Never throw on health/polling. A down daemon is normal operational
     state, not an exception. Every probe returns a typed record.
  2. Host candidates are tried in order. The first reachable one wins
     and the choice is remembered.
  3. Default-model mapping is in-memory for now — serializing it is
     left t
```


## Exports
`OllamaHealth` (interface), `WarmUpReport` (interface), `OllamaStatus` (interface), `OllamaIntegrationDeps` (interface), `OllamaIntegrationEngine` (class), `ollamaIntegrationEngine` (const)

## Key methods
`ensureConnected`, `ping`, `snapshotHealth`, `discoverModels`, `isModelAvailable`, `setDefaultModel`, `getDefaultModel`, `listDefaults`, `warmUp`, `status`

## Depends on
[[reference_code_engine_ollamaclientengine]] | [[reference_code_engine_modelroutingengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/OllamaIntegrationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find OllamaIntegrationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/OllamaIntegrationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
