---
name: reference_code_engine_hookengine
description: "Centralized event-driven communication system"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HookEngine.ts
  sourceHash: 6f99aa6a
  loc: 917
aliases: EventBus
---

# EventBus -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HookEngine.ts` (917 LOC)

## What it is
Centralized event-driven communication system

```
PRISM MCP Server - Hook Execution Engine
Event-driven hook system with lifecycle management

ARCHITECTURE NOTE (M-020): PRISM has a DUAL hook system by design:

1. **HookEngine** (this file) — Event-bus-based, EventEmitter pub/sub.
   Used for: cognitive patterns (Bayesian, RL, optimization), lifecycle
   events, and fine-grained event subscriptions. Phase names: event strings.

2. **HookExecutor** (HookExecutor.ts) — Phase-chain-based, synchronous enforcement.
   Used for: safety gates (pre/post-calculation, pre-output), compliance hooks,
   and dispatcher auto-fire cadences. Phase names: HookPhase enum.

Both systems coexist because they serve different execution models:
- HookEngine: async, event-driven, many-to-many subscriptions
- HookExecutor: sync-first, phase-gated, chain-ordered, blocking-capable

Future consolidation would require unifying phase naming (event strings vs
HookPha
```


## Exports
`HookPhase` (type), `HookPriority` (type), `CognitivePattern` (type), `HookDefinition` (interface), `HookHandler` (type), `HookFilter` (type), `HookContext` (interface), `HookResult` (interface), `EventDefinition` (interface), `HookChainResult` (interface), `EventBusStats` (interface), `EventBus` (class), `HookEngine` (class), `eventBus` (const), `hookEngine` (const), `registerHook` (function), `executeHooks` (function), `emitEvent` (function), `createCognitiveHook` (function)

## Key methods
`registerEvent`, `emitEvent`, `getEvents`, `getEvent`, `getHistory`, `clearHistory`, `registerHook`, `unregisterHook`, `executeHook`, `setTimeout`, `executeHookChain`, `wrapWithHooks`, `emit`, `getHook`, `listHooks`, `getHooksForEvent`, `getHooksByPattern`, `setHookEnabled`, `getStats`, `getEventBus`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/HookEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find EventBus` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HookEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
