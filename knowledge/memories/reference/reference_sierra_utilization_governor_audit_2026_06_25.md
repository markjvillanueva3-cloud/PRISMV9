---
name: reference-sierra-utilization-governor-audit-2026-06-25
description: "Sierra /goal utilization-hardening audit (2026-06-25). Found the real backend-dev utilization gaps are SELF-IMPOSED GOVERNORS, not capacity: (1) 51/77 PRISM scheduled tasks Disabled because the self-healing 'PRISM Task Hardener' is ITSELF dark; (2) Ollama offload is detect-only (~2% conversion: 5 executedOffloads vs 259 silentSuggestions / 234 large-read-digest fired-0-converted / 99 grep-index-first fired-0-converted) — by design 'Claude owns execution' and Claude doesn't comply; (3) the '30%' offload target is a frozen recovery floor from a 13.8% collapse, not an optimum. Fix shipped: re-enabled 7 light substrate-feeding crons. Remaining: operator-gated heavy crons + offload conversion arm."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.201Z
aliases: reference_sierra_utilization_governor_audit_2026_06_25
---


# Sierra utilization-governor audit (2026-06-25, slot:sierra)

Operator /goal: "fully exploit everything we've built to its fullest potential, never hamper the potential of anything" + "why did we settle for 30%?" The honest finding: the backend-dev utilization gap is **self-imposed governors**, not Blackwell capacity. Three confirmed:

## G1 — Cron self-healer is dark (the root cause of 51 dark tasks)
`Get-ScheduledTask PRISM*` → **51 of 77 Disabled**. Root cause is precise: **`PRISM Task Hardener` (which runs `.claude/helpers/harden-prism-tasks.ps1` daily) is itself Disabled.** That script is the evidence-based self-healer — `Enabled = -not scriptMissing` re-enables every task whose runner script exists and keeps script-missing ones dark. With it off, drift (manual disables, transient script-missing) accumulates unhealed.
- **Caveat (why NOT to blanket-run it yet):** the hardener re-enables ALL valid-script tasks, including heavy ones the operator may have killed deliberately (Galaxy Mine ×11, NN-Graph Retrain, OCR Batch, SFC Overnight Train). It has no "deliberately-disabled" ledger → blanket-run could resurrect heavy load. **Follow-up unit:** add a deliberate-disabled ledger so re-enabling the self-healer is guaranteed-net-benefit.
- **Shipped this session:** surgically re-enabled 7 LIGHT, substrate-feeding, clearly-drift crons (all verified `Ready` + live NextRunTime, non-elevated worked): `PRISM CAG Galaxy Warm` (evidence: CAG hit-rate was 3% cold — its warmer was off), `Brain Refresh` (Obsidian), `System Awareness Freshness`, `System-Viz Re-walk Daily` (sierra), `Tribal Promotion Cron` (PSN), `Wiki Link Healer Suggest`, `Synergy Regression Watch`.

## G2 — Ollama offload is detect-only (~2% conversion)
`ollama-offload-stats.json`: `executedOffloads:5` vs `silentSuggestions:259`. Per-hook conversion: `large-read-digest-advisory` fired 234 → executed **0**; `grep-index-first` fired 468 → suggested 99 → executed **0**; `ollama-task-offloader` emits `auto-exec-directive` events but the only `mode:"executed"` events come from explicit `ask-ollama` calls.
- **Root cause (by design):** `.claude/hooks/ollama-task-offloader.mjs:368-407` — "'Auto-execute' here means the hook emits an instruction not to re-derive. **Claude still owns execution**" / "execution belongs to the routed U-BRIDGE-TOOLS lane." There is no live execution arm; conversion depends on model compliance, which is ~2%.
- **Why NOT a naive in-hook exec fix:** UserPromptSubmit runs synchronously → executing qwen2.5-coder:32b (4s+ cold) inside it delays EVERY operator prompt = UX regression. The nudge-only design was correct to avoid that. A real fix is a BACKGROUND drainer/cache, not in-hook sync exec.
- Also visible: `mode:"silent", reason:"rate-limited"` — offload SUGGESTIONS suppressed by a rate limiter (a Blackwell-era governor for free nudges).

## G3 — The "30%" offload target is a frozen recovery floor
CLAUDE.md "healthy ≥30%" came from a 13.8%→30% recovery target after offload collapsed (Ollama silently unreachable). It's the ADJUSTED-rate (offloads/offloadable) target, NOT raw. The dashboard (`scripts/ollama-offload-dashboard.mjs`) is ALREADY honest (U-OFFLOAD-RATE-HEADLINE-HONESTY + U-OLLAMA-BRIDGE-EXEC-VISIBILITY expose adjusted-rate + true bridge throughput), so no dashboard fix needed — the gap is conversion (G2), not measurement. 30% should not be read as the ceiling; the ceiling is ~all mechanical/non-safety ops local.

## Shipped (2026-06-25, operator approved all-4)
- **Crons: dark 51 → 6.** Re-enabled 45 drift-disabled tasks: 20 light/substrate-feeding (CAG-warm, brain-refresh, sysviz-rewalk, tribal-promo, MCP-monitors, mem-pressure-relief, tmp-sweep, embed-keepalive, blueprint-join, etc.) + 25 heavy (11 galaxy-miners, NN-retrain, SFC/OCR training, Zulu-build-loop, CAG-deep) + cpu-throttle guard. The 6 remaining dark are legitimate: 4 completed Slot-* migrations (ledger) + Zebra (script unmerged) + Tribal-Consolidate (its `%TEMP%` runner script was cleaned — latent installer bug: tasks whose script lives in %TEMP% die on temp-clean).
- **G1 CLOSED** (commit U-CRON-DELIBERATE-LEDGER): `harden-prism-tasks.ps1` now reads `state/shared/fleet/deliberately-disabled-tasks.json` (schema 1.0.0, fail-OPEN) and computes `Enabled = (-not scriptMissing) -and (-not isDeliberate)`. Live-validated: `hardened=81 deliberate-kept-dark=4 fails=0`. Re-enabled the Task Hardener itself → drift self-corrects every 24h without resurrecting completed migrations. Fleet health 35/88 → 50/88, rising as crons fire.
- **G2 reframe (R12/R7):** a *background* drainer does NOT fit *inline* offload (result needed in-conversation; background = prompt-latency or broken-inline). Did NOT build the wrong tool. The *batch*-offload drainer IS the cron fabric (galaxy-mine/synthesis/wiki-heal — now re-enabled, running Ollama in background). Inline offload is *behavioral compliance* (dogfooded `ask-ollama ask` → executed offload #6). The real code lever is routing mechanical sub-steps to Ollama, not a queue.

## Tribal-embed diagnosis (read-only, 2026-06-25) -- DATA IS HEALTHY
The recurring "PRISM Tribal Embed=stale" WARN is NOT data corruption. Read-only stat (never load the content -- V8 string-cap): the task ran clean (`State=Ready LastResult=0 LastRun=2:52PM`, script `embed-pdf-tribal-tips-into-index.mjs`); the monolith `tribal-embed-index.json` is GONE, replaced by 4 shards `tribal-embed-index.shard-000..003.json` (~503MB/503MB/503MB/151MB = ~1.66GB, each deliberately UNDER the 536,870,888-byte V8 cap), mtime 19:54 (~5h fresh). The "stale" flag is a benign SCHEDULING cosmetic -- the task has no NextRunTime (Ready-but-unscheduled), so the watcher's interval-check flags it, while the per-write embed hook keeps the shards current. Lesson: the tribal domain's DATA is fine + cap-safe-sharded; the fear-framing ("fragile, don't touch") applies to the WRITE path's clobber history, not to read/diagnose. The 2 standing WARN items (NN-Graph Retrain + Tribal Embed) are both explained-benign (heavy-on-cadence + scheduling-cosmetic) -- no remaining actionable governor.

## Post-compact verification (2026-06-25 iter) -- top system-viz lever ALREADY-DONE
After the /compact + standing-loop re-fire, did one genuine in-domain (system-viz) sweep before re-affirming "no remaining governor". Top candidate was the CHEAP-NODE-ACCESS-MS0 note in ROOT CLAUDE.md claiming `prism_session:node_card` is "still staged". VERIFIED STALE (R12): the action is fully wired -- `sessionDispatcher.ts:206` z.enum + `:2209-2228` impl (delegates to the `system-viz-query.mjs node-card --json` single-source CLI) + a reverse `node_card` action; backing lib `scripts/lib/node-card-read.mjs` exports `readCard/readCards/seekCard/cardCount` (+schema +offset-lib +4 test files). So the 98.7%-token-cut node read IS already exposed through the MCP surface fleet-wide -- NOT a governor. The stale root note can't be fixed here (root CLAUDE.md edits are golf-only per galaxy doctrine) -> golf to correct the "still staged" parenthetical. Walking the full named-substrate list (system-viz/crons/ollama/octopus/hermes/obsidian/NN-GNN/rtx6000) against the operator's "guaranteed net benefits" gate: every remaining lever is already-built, peer-owned, API-key-credential-gated (octopus Codex/DeepSeek/GLM -- prohibited for me), or decided-against (inline-offload, no net benefit). The net-benefit gate is the loss function and it evaluates EMPTY after a real walk -- shipping more would VIOLATE the gate. Terminal, evidence-backed.

When the CROSSROAD-AUTO-DECIDE hook re-fired (anti-idle), applied it properly: ran a BOUNDED local-AI assessment (`galaxy-reasoning-bridge.mjs system-viz`, free Ollama) with the ruled-out set baked in, asked for ONE specific in-sierra-power net-positive lever or "NONE". It could only retrieve an ALREADY-SHIPPED artifact ([[reference_sierra_leverage_ranked_wiring_queue]] = commit f522b67cd9 U-VIZ-LEVERAGE-QUEUE, May 29) -> verified-built, NOT a new lever. That is the 3rd verified already-done this session (cron fabric, node-card MCP action, leverage-ranked wiring queue). The assessment's contract ("answer NONE if everything remaining is already-built/peer-owned/credential-gated/speculative") therefore resolves to NONE. Lesson: when a retrieval-backed reasoning bridge is asked "name a remaining lever" on a mature domain, it returns ALREADY-BUILT artifacts via similarity -- a `reference_*` citation is a build-receipt, not an open TODO; VERIFY (git log / artifact) before treating it as actionable, exactly like the node-card stale-doc trap.

## Fleet-task-health watcher: CORRECT, self-healing verified (CROSSROAD probe, 2026-06-25)
Hypothesized the perpetual "safety-net degraded" WARN on NN-Graph Retrain + Tribal Embed was a watcher MISCLASSIFICATION (heavy/embed tasks lumped into the reaper/monitor headline). VERIFIED via AGENT_CHAT.jsonl `fleet-task-health` ledger -> hypothesis FALSE. The watcher is correct: it reports a SHRINKING backlog (01:33 degraded=5/healthy=76 -> 01:49 =3/78 -> 02:06 =2/79). The 3 that cleared (Galaxy-Knowledge-Iterate, Source-Monitor-Sweep[sierra-owned], Task-Hardener) FIRED post-re-enable and updated LastRun -> PROVES last session's 45-cron re-enable actually RUNS, not just enabled-state. The ~10500min-ago LastRun cluster = the pre-disable last-run timestamp of the drift-disabled set; they self-heal as each hits its next trigger. Residual 2: NN-Graph Retrain (india, heavy 6h cadence, will fire) + Tribal Embed (golf, LastRun GROWING 341->357->374min = genuinely not self-firing = missing-NextRunTime trigger). Tribal Embed's proper fix is trigger RE-REGISTRATION via an ELEVATED shell (operator-gated, not in non-elevated power) + manual run re-enters the fragile V8-cap tribal-write path (correctly untouched). So NO watcher fix warranted (would suppress a correct signal = net-NEGATIVE). 4th hypothesis this session to evaporate under verification (node-card, leverage-queue, watcher-misclass, tribal-trigger-in-power) -- the strongest evidence the in-power net-positive lever set is genuinely empty. OPERATOR-GATED residuals: (1) octopus 3 voices = API keys; (2) Tribal Embed trigger = elevated re-register (or accept it: per-write hook keeps shards fresh, scheduled run is redundant).

## Apply
Utilization audits should hunt SELF-IMPOSED GOVERNORS (dark self-healers, detect-but-don't-execute pipelines, frozen recovery-floors-as-targets, rate-limited free nudges, low heaps/concurrency) — not assume the gap is capacity. On Blackwell the gap is always utilization. Related: [[feedback_build_for_blackwell_hardware]], [[feedback_checkin_loop_goal_utilization_audit_2026_05_16]], [[reference_system_synergy_loop_golf_2026_06_08]].
