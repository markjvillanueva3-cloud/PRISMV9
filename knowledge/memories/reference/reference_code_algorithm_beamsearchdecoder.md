---
name: reference_code_algorithm_beamsearchdecoder
description: "BeamSearchDecoder — approximate n-best sequence decoding over a log-probability"
metadata:
  type: reference
  kind: code-algorithm
  galaxy: unassigned
  sourcePath: mcp-server/src/algorithms/BeamSearchDecoder.ts
  sourceHash: 90f452c2
  loc: 206
aliases: BeamSearchDecoder
---

# BeamSearchDecoder -- Algorithm SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/algorithms/BeamSearchDecoder.ts` (206 LOC)

## What it is
```
BeamSearchDecoder — approximate n-best sequence decoding over a log-probability
trellis (the standard decoder for language models / structured-prediction).

Given per-step emission log-probs and an optional first-order transition model,
beam search keeps only the top-B partial sequences at each step:

  step 0:  score(v)        = initial[v]  + emissions[0][v]
  step t:  score(seq·v)    = score(seq)  + transition[last][v] + emissions[t][v]
           → keep the B highest-scoring partials, advance
  end:     return the top-K complete sequences by cumulative log-score

Relationship to the exact `ViterbiDecoder` shipped this milestone (same matrix
inputs, deliberately): Viterbi returns THE single MAP path in O(T·V²); beam
returns the top-K (n-best) paths in O(T·B·V). For B ≥ V beam is exact and equals
Viterbi's best as its #1; for B < V it trades a small optimality risk for
cost/scale — the 
```


## Exports
`BeamSearchInput` (interface), `BeamSearchOutput` (interface), `BeamSearchDecoder` (const)

## Key methods
`validate`, `calculate`, `getMetadata`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/algorithms/BeamSearchDecoder.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Algorithm

## How to query it
Recall by meaning via `/system-viz find BeamSearchDecoder` or `prism_session:master_index_query`; open the source at `mcp-server/src/algorithms/BeamSearchDecoder.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
