---
name: reference_code_engine_wetruncustomeracceptanceengine
description: "PRISM submitted, waiting on customer"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WetRunCustomerAcceptanceEngine.ts
  sourceHash: fceb10fa
  loc: 445
aliases: WetRunCustomerAcceptanceEngine
---

# WetRunCustomerAcceptanceEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WetRunCustomerAcceptanceEngine.ts` (445 LOC)

## What it is
PRISM submitted, waiting on customer

```
WetRunCustomerAcceptanceEngine
------------------------------------------------------------
Batch-level customer sign-off for wet-run pilot deliveries.

The wet-run pilot ends with one or more batches leaving the
floor and landing on the customer's dock. Promotion to
production cannot clear until the named customer rep has
signed off on every batch in the pilot. Four eyes applies:
the customer acceptor must NOT be the same identity as the
PRISM submitter, and a conditional accept carries a punchlist
of items that must each close by a named deadline.

States
  submitted            — PRISM submitted, waiting on customer
  accepted             — customer clean-accepted
  conditional          — customer accepted with open punchlist
  rejected             — customer rejected; promotion blocked
  withdrawn            — PRISM pulled the submission

Promotion gate
  A pilot can only promote when
```


## Exports
`SubmissionState` (type), `PunchlistItem` (interface), `AcceptanceSubmission` (interface), `SubmitInput` (interface), `DecideInput` (interface), `ClosePunchItemInput` (interface), `WithdrawInput` (interface), `PromotionGate` (interface), `Snapshot` (interface), `WetRunCustomerAcceptanceEngine` (class), `wetRunCustomerAcceptanceEngine` (const)

## Key methods
`submit`, `decide`, `closePunchlistItem`, `withdraw`, `promotionGate`, `getSubmission`, `listForPilot`, `snapshot`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WetRunCustomerAcceptanceEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WetRunCustomerAcceptanceEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WetRunCustomerAcceptanceEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
