---
name: reference_code_engine_prewetrunchaosgateengine
description: "Pre Wet Run Chaos Gate"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PreWetRunChaosGateEngine.ts
  sourceHash: a28ecb6f
  loc: 347
aliases: PreWetRunChaosGateEngine
---

# PreWetRunChaosGateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PreWetRunChaosGateEngine.ts` (347 LOC)

## What it is
Pre Wet Run Chaos Gate

```
PreWetRunChaosGateEngine (U-LPR-CHAOS-DRILL)

One-shot hard gate that must pass BEFORE the first real-world wet-run.
Injects each of N mandatory chaos scenarios into the control plane,
measures rollback latency, and requires 100% of scenarios to both
(a) trigger an auto-rollback and (b) complete rollback within the
published budget (default 60 000 ms per Phase-8 plan).

Distinct from ChaosDrillSchedulerEngine (U-LPR-OPS-CHAOS) which
schedules weekly continuous chaos post-MS0. This engine is a gate
token producer — wet-run Phase-8 is blocked until its passReport()
returns { passed: true } for the current artifact hash.

Mandatory scenarios (per plan):
  - mtconnect_stream_loss — simulate MTConnect TCP drop mid-cut
  - tenant_id_collision   — two requests with same session, different tenant
  - lora_poison           — inject physics-invalid parameter via LoRA output

Recommended additional
```


## Exports
`ChaosScenarioId` (type), `MANDATORY_SCENARIOS` (const), `DEFAULT_ROLLBACK_BUDGET_MS` (const), `ScenarioOutcome` (type), `ScenarioExecution` (interface), `GateReport` (interface), `GateTokenRecord` (interface), `PreWetRunChaosGateEngine` (class), `preWetRunChaosGateEngine` (const)

## Key methods
`setBudget`, `getBudget`, `recordExecution`, `listExecutions`, `buildReport`, `issueToken`, `listTokens`, `getStats`, `clearAll`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PreWetRunChaosGateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PreWetRunChaosGateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PreWetRunChaosGateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
