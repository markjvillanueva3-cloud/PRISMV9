---
name: reference_code_schema_pipelineir
description: "PipelineIR -- declarative, pipeline-as-data intermediate representation."
metadata:
  type: reference
  kind: code-schema
  galaxy: unassigned
  sourcePath: mcp-server/src/schemas/PipelineIR.ts
  sourceHash: 2b75b914
  loc: 150
aliases: PipelineIR
---

# PipelineIR -- Schema SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/schemas/PipelineIR.ts` (150 LOC)

## What it is
```
PipelineIR -- declarative, pipeline-as-data intermediate representation.

PIPELINE-IR-MS0 / U-PIR01 (slot:bravo). PRISM has many imperative pipelines
(print-to-program, quote-to-ship, ...) hand-wired in TS. This IR expresses a
pipeline as DATA: a DAG of stages, each invoking one `dispatcher:action`, with
params that are either literals or references to an upstream stage's output.
That lets pipelines be validated, topologically ordered, persisted, diffed, and
executed generically (see PipelineIRConverterEngine / PipelineIRExecutorEngine,
U-PIR02/U-PIR03) instead of bespoke per-pipeline glue.

This file is the FOUNDATION (the contract the converter + executor build on).
It enforces STRUCTURE only. Graph integrity (no cycles, no dangling refs, every
dependsOn resolvable) is the converter's job -- a structurally-valid IR can still
be a semantically-invalid graph, which is exactly why the con
```


## Exports
`StageErrorPolicySchema` (const), `StageErrorPolicy` (type), `StageRefSchema` (const), `StageRef` (type), `ParamSpecSchema` (const), `ParamSpec` (type), `PipelineStageSchema` (const), `PipelineStage` (type), `PipelineIRSchema` (const), `PipelineIR` (type), `PRINT_TO_PROGRAM_IR` (const)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/schemas/PipelineIR.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Schema

## How to query it
Recall by meaning via `/system-viz find PipelineIR` or `prism_session:master_index_query`; open the source at `mcp-server/src/schemas/PipelineIR.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
