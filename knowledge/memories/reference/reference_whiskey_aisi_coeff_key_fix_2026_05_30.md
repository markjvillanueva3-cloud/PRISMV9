---
name: reference_whiskey_aisi_coeff_key_fix_2026_05_30
description: "PHYS-FIX-MS0/U-AISI-COEFF-KEY — fixed dead per-material AISI kc1_1/mc override in buildMaterialPhysics (name-vs-shortcode key mismatch) + found pre-existing build:fast break (slot/whiskey 2026-05-30)"
type: reference
source: prism-memory
synced: 2026-06-09T14:54:11.040Z
aliases: reference_whiskey_aisi_coeff_key_fix_2026_05_30
---


# PHYS-FIX-MS0/U-AISI-COEFF-KEY — activate dead AISI per-material override (slot:whiskey, 2026-05-30)

Continuation of the lathe backend-dev /loop after LATHE-WIRE-MS0 ([[reference_whiskey_lathe_wire_ms0_2026_05_30]]). The deferred U-LATHE-CATALOG-ADAPTER-WIRE was **verified BLOCKED**: `CatalogConsumerAdapterEngine.ts` exists in shared `H:/prism` but NOT in slot/whiskey (the branch is **217 commits behind cad-fusion-live-ms0**), so wiring it needs a cross-tree merge, not a surgical edit. Pivoted to the highest-value in-slot lathe-physics item — the AISI-coefficient latent I'd logged last session.

**SHIPPED — `8ea404aebc` `[whiskey] [PHYS-FIX-MS0]/U-AISI-COEFF-KEY`.** `buildMaterialPhysics` (physics/constants.ts:980) resolved its per-material override via `AISI_CUTTING_COEFFICIENTS[partial.name]` — but `partial.name` is the descriptive name ("AISI 4140 Alloy Steel") while the table is keyed by SHORT code ("4140"). The lookup returned `undefined` for **every** `_RAW_MATERIAL_DB` material → the JSDoc-documented per-material precedence was DEAD CODE → every material silently used the per-ISO `CANONICAL_KIENZLE` default. This is a code-violates-its-own-contract bug (R9), confirmed live (4140 got P-default 1800, not its tuned 1950). **Fix:** added optional `aisiKey?: string` param; `CANONICAL_MATERIAL_DB` construction passes its record `key` (== the AISI short code); kept name-direct fallback. **Inlines nothing** — it makes the EXISTING canonical literature table (Machinery's Handbook 32nd; Kennametal 2023) actually take effect.

**Blast radius (bounded + enumerated):** 6 materials gain tuned kc1_1 — `1018` 1800→1700, `4140` 1800→1950, `316` 2100→2150, `7075` 700→750, `Inconel 718` 2800→**3200** (largest, +14%), `A2` 3200→3000; 3 gain tuned mc (4140 .26, Inconel .30, A2 .29). Taylor C/n UNCHANGED for DB entries (the entry re-overrides from raw at constants.ts:1059). `1045`/`304`/`6061`/`Ti-6Al-4V`/`D2`/`gray_iron` unchanged (tuned==per-ISO); `tungsten_carbide`/`C11000`/`C26000` unchanged (no AISI key). **A2/1018 LOWER kc toward calibrated values — physics-reviewer CONFIRMED correctness, NOT a safety softening** (the old per-ISO H/P bucket value was an upward-biased default; D2 vs A2 now correctly differentiated; S(x) gates untouched).

**Validation:** new oracle `MaterialAISICoefficientLookup.test.ts` 11/11 PASS (fail-on-revert, concrete `.toBe` + over-reach guards: 1045 unchanged, tungsten_carbide per-ISO, taylor stays raw 350 not AISI 320). **Stash-baseline diff** on the 5 kc-assertion candidate files = **45 failures BOTH pre- and post-fix → 0 new failures attributable** (the 45 are pre-existing repo debt: f360 schema TypeErrors, an AISI_ALIAS test, a wrong titanium-mc expectation). haas/hurco machinist-sanity + LatheMetallurgy 53/53 PASS. Per-file scrutiny: physics-review-agent + independent reviewer both **PASS, 0 P0/P1**. **tsc was OOM-killed** (rc134/SIGABRT; host commit ~96%, fleet-memory critical) — environmental, NOT a type error; types traced by both reviewers + vitest imports succeed. (Honest scope: did not run the full fleet suite; targeted before/after evidence is strong for a documented correctness fix.)

**P3 (logged, not fixed):** for loose *name-keyed* callers passing a bare code, the fix now also activates AISI taylor_C/n (not just kc1_1/mc) — intended override behavior; DB path unaffected. Follow-up: the DB deliberately keeps raw Taylor over the more-specific AISI Taylor (latent improvement debt).

**🚨 PRE-EXISTING P0 FOUND — slot/whiskey `build:fast` is RED (UNFIXED, needs a dedicated unit).** `turningDispatcher.ts:55` lazy-imports `../../engines/LatheLiveToolingPlannerEngine.js` which **does not exist** on slot/whiskey (wired-before-built; the wire was added by an old MAIN commit `1a9c3374e6`, 2026-05-18). esbuild resolves dynamic-import literals at bundle time → "Could not resolve" → the whole bundle fails. It went unnoticed because the slot's verification leaned on vitest (per-file transpile, doesn't bundle) + OOM-prone tsc. **Independent of the AISI change** (turningDispatcher untouched here). Comprehensive-route fix (R13): BUILD `LatheLiveToolingPlannerEngine` (live tooling is a legit lathe feature whiskey owns — C/Y-axis driven tooling) rather than remove the wire. Proposed next unit: **U-LATHE-LIVE-TOOLING-PLANNER-BUILD** (P0 — a red build blocks all slot toolpath/dispatcher validation). Line 506 already has a graceful method-missing fallback, so only the file's absence breaks the build.

**Process lesson:** the slot's build verification gap — relying on vitest + OOM-prone tsc — let a missing-engine bundle break go undetected. Run `npm run build:fast` (esbuild, light, won't OOM) as the cheap bundle-integrity pre-flight; it catches dynamic-import resolution failures that per-file vitest transpile misses. See [[feedback_verify_actual_contract_not_proxy]].
