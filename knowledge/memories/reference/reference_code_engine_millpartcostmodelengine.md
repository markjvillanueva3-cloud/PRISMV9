---
name: reference_code_engine_millpartcostmodelengine
description: "MillPartCostModelEngine — 8-bucket cost-per-part model for mill operations"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MillPartCostModelEngine.ts
  sourceHash: 2f986e99
  loc: 211
aliases: millPartCostModelEngine
---

# millPartCostModelEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MillPartCostModelEngine.ts` (211 LOC)

## What it is
```
MillPartCostModelEngine — 8-bucket cost-per-part model for mill operations

Mill-domain parity for LathePartCostModelEngine (LATHE-PRO-MS10). Lathe has 7 buckets;
mill adds an 8th — **fixture amortization** — because mill operations frequently use
dedicated fixtures (soft jaws, custom fixture plates, tombstones) that are amortized
over batch_size, not per-cut.

8 BUCKETS:
  1. machine    — cycle_time_hr × loaded_rate_per_hr
  2. tool       — Σ ops (cycle / tool_life) × (insert_cost + holder_amort) [mill: endmills,
                  drills, taps have different edge counts than lathe inserts]
  3. material   — (part_mass + waste_mass) × price_per_kg
                  [mill waste includes chips + cutoff stock + handling allowance]
  4. setup      — setup_time_hr × rate_per_hr / batch_size
  5. quality    — scrap_rate × (material + machine) [sunk at failure]
  6. energy     — spindle_power_k
```


## Exports
`MillOperationCost` (interface), `MillPartCostInput` (interface), `MillPartCostResult` (interface), `millPartCostModelEngine` (const)

## Key methods
`compute`, `getStats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MillPartCostModelEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find millPartCostModelEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MillPartCostModelEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
