---
name: reference_xray_training_corpus_state_2026_05_29
description: "blueprint-vision training corpus is 100% blocked on GPU-OCR — 12,321 prints need OCR, 0 text-ready"
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.278Z
aliases: reference_xray_training_corpus_state_2026_05_29
---


Verified training-readiness of the blueprint/OCR/CAD-extraction corpus (slot:xray, 2026-05-29; operator goal "train across all H: files"). Spec: `state/shared/specs/BLUEPRINT-VISION-TRAINING-READINESS-2026-05-29.md`.

**"Training" in PRISM = assemble a ground-truthed LoRA/RAG corpus; the fine-tune runs EXTERNALLY (Gemini/OpenAI/Modal) → endpoint into AISystemRouterEngine.** xray produces the corpus; india fine-tunes; juliett owns the DB. No local weight updates.

**The machinery is BUILT+WIRED but never run:** BlueprintLoRABridgeEngine (→ `mcp-server/data/training/lora/staging/`, anonymization HARD RULE, 4 confidence tiers), BlueprintExtractionRAGEngine, BlueprintCoverageAuditEngine, GroundTruth{Registry,Validation}Engine, `scripts/lora-dataset-builder.mjs`. All `blueprint_lora_*`/`blueprint_rag_*`/`blueprint_coverage_*` actions wired. BUT: accuracy/ground-truth ledgers EMPTY (0 outcomes); `lora/staging/` dir absent.

**Corpus state (juliett jm-die-database manifest):** 257,992 DocuStrata files; 111,745 indexed; 73,506 in classified-v3; 76,205 blueprint↔program joins; ~95.6 GB.

**Partition result** (`scripts/build-blueprint-training-manifest.mjs` over classified-v3, print-score-min=1 → `state/shared/blueprint-training-partition.json`):
- **blueprint_needs_ocr = 12,321** (the GPU-OCR work-list)
- **blueprint_text_ready = 0** — NO blueprint has a usable text layer (every print is a scanned drawing). The "text-layer-first no-OCR seed" idea is REFUTED — text-layer docs are 61,185 business docs (invoices/quotes/POs; the classified-v3 `extracted` object holds tax/vendor/customer/line_items, NOT dimensions). Business docs → charlie/hotel, not xray training.

**⚠ CORRECTION (operator challenged "only 4,207?", 2026-05-29) — the v6 join UNDERCOUNTED programs.** The 4,173 print↔program matches came from a v6 join built against a **lathe-only ~100-entry `program-labels.json`** (the canonical indexer `scripts/build-jm-die-program-index.mjs` docstring: "Existing program-labels.json is lathe-only (~100 entries)"). The FULL raw JM tree holds **~14,505 program files** (depth-4 bounded; 129,605 total files) / 21,679 in the indexed subset — and the PN is in the G-code HEADER (`(PROGRAM NAME - 9007405)`) + filename + rich labels (MATERIAL, tools, dims). The canonical indexer's full output `mcp-server/data/state/jm-die-full-program-index.json` had NOT been generated (only the stale lathe-only file existed). **AUTHORITATIVE corpus count** (`mcp-server/data/state/jm-die-full-program-index{,-v2}.json`, pre-existing May 9/11, NOT regenerated this session): 34,786 files = **25,949 CNC programs + 8,837 CAD** (.min 16,947 · .mcx-8 7,092 · .ipt 5,821 · .dxf 1,447 · .iam 669 · .idw 309 · .stp/.step 197 · .dwg 210 · Parasolid .x_b/.x_t 74 · .catpart 3).

**R12 SELF-CORRECTION:** I first claimed the v6 join used lathe-only labels and re-running would jump the match — WRONG. I checked the wrong dir (state/shared/ not mcp-server/data/state/) and assumed the index was missing. Mtimes disprove it: full index (May 9/11) PREDATES v6 join (May 24), and `phase16-blueprint-program-join-v6.py` reads the full index (lines 42-43) + extracts PN from G-code comments (`PROGRAM NAME -`, line 225). So **4,173 is the GENUINE print↔program overlap**, not an undercount bug. Context: of 76,205 v6 part_numbers only ~12,321 are real blueprints (rest are business docs w/ a PN); 4,173 matched programs ≈ 34% of real blueprints have a retained program — a reasonable rate. The corpus is large (25,949 programs); the print↔program INTERSECTION is the natural ~4-12K. Match could still be improved (PN normalization, customer corroboration) but it is NOT a stale-labels bug. (My own filename-only matcher gave 1,539 — WORSE than v6; the canonical full-index + content-PN is the right path. I removed a duplicate `build-jm-program-index.mjs` I'd started — `build-jm-die-program-index.mjs` is canonical; its gap is content-PN extraction from G-code headers, the real enhancement.)

**Supervised training spine BUILT** (`scripts/build-blueprint-cad-program-pairs.mjs`, 11/11 tests → `state/shared/blueprint-training-pairs.jsonl`): joins v6 print↔program + a part_number→CAD index. REAL result over 76,205 part_numbers: **4,245 train-eligible** (print + ≥1 label) — 4,173 CNC-program-labeled (the workhorse; programs encode machined dims), **498 CAD-matched, 426 full-triple**. The print INPUTS still need OCR (the gateway block); the LABELS are ready. Pair row: `{part_number, print_docs[], program_files[], cad_files[]{path,ext,customer,kind,matched_via}, has_print/program/cad, label_source, train_eligible}`.

**CAD-match lift (2026-05-29, this session):** CAD-matched PNs **272 → 498 (+83%)**, full-triple 238 → 426. Root cause was a genuine undercount bug: `candidatePNs` extracted digit-runs `\d{5,}` only, blind to ALL 4-digit PNs — but **14,394 of 76,205 v6 PNs (~19%) are 4-digit** (1005, 1588, 1800, 3016…), so CAD files genuinely named "1005 HAMMERHOB.ipt" were invisible. Lowered to `\d{4,}`, **gated by knownPNs membership** (a 4-digit run only matches an ACTUAL JM PN → false positives bounded to PN collisions e.g. a year that is also a real PN). 3-digit deliberately NOT extracted (measured +46 PNs only, but 3-digit runs are revs/dates everywhere → noise > gain). **Folder co-location was evaluated + REJECTED**: v6 program `source_path` dirs are shared/customer folders ("CNC LATHE\FORGO", "PROGRAMS MCAM X2", "CUSTOMERS\…") → many-parts-per-PN noise, would degrade label quality. Each CAD entry now carries `matched_via` (the join key) for audit. CAD-match is still lossy for description-named geometry (most .ipt/.step carry no numeric PN) — the residual gap needs content/manual mapping (juliett's lane), NOT more filename heuristics.

**OCR GATEWAY UNBLOCKED (2026-05-29, commit bb4eae6aec) — the "Ollama down" claim was WRONG.** Ollama daemon was never down (resident-model inference answered in 1s). The real blockers, all fixed + proven on a real JM electrode print (read "TAPTITE 2000 DIE" title block + dims 1.2340 dia / 0.876 / 0.3575 ±0.0002): (1) **PyMuPDF (fitz) not installed** on H:/Tools/python → PDF→PNG render exit=3 before Ollama was called → `H:/Tools/python/python.exe -m pip install pymupdf` (done). (2) **num_ctx uncapped** → qwen2.5vl:7b footprint ~23GB > 16GB GPU → CPU offload (`size_vram=0` in /api/ps) → 119s loads + multi-min inference → capped `num_ctx=8192` (lib default) → 15.3GB GPU-resident, 15s load, ~55-74s/page. THE load-bearing fix. (3) **DEFAULT_VISION_MODEL was moondream:1.8b** which PARROTS the prompt example (echoed "central_oil_hole 1.27" = the literal prompt example) → switched to **qwen2.5vl:7b** (reads real dims). Canonical OCR runner = `scripts/run-ollama-vision-extract.mjs --pdf <p> --part-class <c>` (NOT the non-existent `docustrata-pipeline.py --only gpu-ocr` — that path is wrong, delete from memory). See [[reference_xray_ocr_gateway_unblocked_2026_05_29]].

**Remaining gating work:** (1) **GPU scheduling** — the accurate VL model needs the fleet's 10GB qwen2.5-coder UNLOADED to stay GPU-resident; under fleet contention it falls to CPU. Batch over the 12,321 needs-OCR prints must run in an idle-fleet window (unload coder first). (2) external fine-tune = india. xray cannot fine-tune in a chat. See [[reference_xray_juliett_database_wiring]] · [[reference_xray_confidence_thresholds_reconciled]].
