---
name: reference_code_script_sfcvariabilitylaunch
description: "SFC-ACCURACY-MS1 — Variability batch launcher."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/sfc-variability-launch.mjs
  sourceHash: b2517b1f
  loc: 123
aliases: sfc-variability-launch.mjs
---

# sfc-variability-launch.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/sfc-variability-launch.mjs` (123 LOC)

## What it is
```
SFC-ACCURACY-MS1 — Variability batch launcher.

This is the ACTION of the per-domain on-demand scheduled task
("PRISM SFC Variability Batch Mill" / "...Lathe"). It is NOT meant to be
run by hand in normal operation — the resume-guard triggers the task via
`schtasks /run`, and Task Scheduler runs THIS launcher.

Why a launcher (not the guard spawning the batch directly): a process
spawned as a child of the guard's own scheduled-task instance lives in
that task's job object and is killed when the guard task instance exits
(5-minute cadence). By making the batch the action of its OWN task, it
runs under that task's job object — independent of the guard.

The launcher:
  1. reads the per-domain resume-state sidecar (`.resume-state.json`,
     written by the guard immediately before it triggered this task),
  2. resolves {skip, workers, chunkSize, maxMinutes} via the guard's pure
     `pickRe
```


## Exports
`parseDomain` (re-export), `readSidecarFile` (re-export)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/sfc-variability-launch.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find sfc-variability-launch.mjs` or `prism_session:master_index_query`; open the source at `scripts/sfc-variability-launch.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
