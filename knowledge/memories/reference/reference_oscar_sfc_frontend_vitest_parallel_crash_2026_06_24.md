---
name: reference_oscar_sfc_frontend_vitest_parallel_crash_2026_06_24
description: "SFC web-frontend test-integrity (slot:oscar, 2026-06-24). CORRECTS the falsified 'Three.js/WebGL-in-jsdom' diagnosis: the whole-suite vitest exit-255 is a PARALLEL-WORKER race, not WebGL/memory. Shipped MillSurfaceFinishPanel real impl (28 red->green) + vitest fileParallelism:false. Commit ea24d9cee6."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.704Z
aliases: reference_oscar_sfc_frontend_vitest_parallel_crash_2026_06_24
---


**SFC FRONTEND test-integrity (slot:oscar, 2026-06-24).** Commit `ea24d9cee6` `[SFC-FRONTEND]/U-SFC-MILL-SURFACE-FINISH`. Two fixes that make the SFC web app PROVABLE.

**1. CORRECTED DIAGNOSIS (R12 -- the prior session's was wrong).** The whole `mcp-server/web` vitest suite (100+ files) exits **255 with NO summary**. Prior handoff blamed "Three.js/WebGL-in-jsdom" and proposed mocking `three`/the viewer. **FALSIFIED by direct test:** a real `@react-three/fiber` `<Canvas>` (+ drei OrbitControls/Grid + useThree/useFrame) renders in jsdom WITHOUT crashing the worker (R3F v9 degrades gracefully; CalculatorPage subset of 2 files passes clean, 8/8). It is also NOT memory: `maxWorkers=3` + `NODE_OPTIONS=--max-old-space-size=8192` STILL exits 255 (matches the prior session's "cap=4+heap still crashed"). The REAL signature: **`--no-file-parallelism` (single worker) COMPLETES with a real summary** (exit 1 = ordinary failures, no 255); **any parallelism (even 2-3 workers) crashes 255 with no summary** = a **cross-worker race**, symptom = repeated `THREE.WARNING: Multiple instances of Three.js being imported`. Likely a shared global/resource across worker processes. FIX: `vitest.config.ts test.fileParallelism: false` -- slow (~15min full) but deterministic/PROVABLE. **Follow-up (open): surgically isolate the offending cross-worker interaction** (bisect file pairs; suspect the duplicate-three load or a shared singleton/file/port) so parallelism can be restored. This is fleet-wide web test-infra (touches all pages, quebec/frontend-app galaxy), surfaced + fixed by oscar because it blocks proving the SFC app.

**2. MillSurfaceFinishPanel real impl (28 red->green).** `mcp-server/web/src/components/calculator/MillSurfaceFinishPanel.tsx` was a `NOT_IMPLEMENTED` THROWING stub (golf `39dc290fae` GOAL-TSC-FIX iter16, "fail-loud stubs for never-extracted panels"). Its companion test `MillSurfaceFinishPanel.test.ts` (oscar MILL-STUDIO-MS0/U-MSTUD-B6) encodes the full spec; implemented to green: `raTheoreticalUm(driver,r)=driver^2/(8r) mm->um` (line-of-cusps, geometry NOT Kienzle/Taylor so no constants.ts coeff; Infinity on r<=0 = fail-loud); `isoGradeForRa` (ISO 4287 N1..N12 ladder 0.025..50um, ">N12"/"N/A"); `classifyFinish` (mirror<=0.4<fine<=1.6<general<=6.3<rough; invalid->rough); `assessMillSurfaceFinish` (ball-nose driver=stepover, face driver=fz; margin=(target-Ra)/target; recs: no-target / over-target reduce-or-increase-radius / comfortable>20% / tight-runout). Reference values verified: 0.25/3.0->2.604um/N8/general, face 0.1/0.4->3.125um, margin 0.186.

**STILL-RED (handed off, NOT oscar core domain -- CAM catalog = kilo/juliett):** `calculatorData.test.ts` (holder fallback catalog), `calculatorProgrammingCoverage.test.ts` (expanded CAM package set / thread-tap-ream-chamfer strategies / wire packages), `calculatorStrategyRegistryBridge.test.ts` (normalized strategy families) -- ~5 failures total, CAM-strategy/catalog coverage data, not surface-finish physics.

**3. SFC-LOGIC BASELINE (single-process, validated):** 24 SFC-logic test files = **229/234 passed (97.9%)**, MillSurfaceFinishPanel now GREEN. The 5 remaining failures are ALL in 3 files (calculatorProgrammingCoverage, calculatorData, calculatorStrategyRegistryBridge) and ALL one root cause: the calculator's expanded CAM **programming-environment catalog** is missing -- `PROGRAMMING_ENVIRONMENTS.length` is 66, test wants >80; missing `camworks-wire`, `Thread Milling`/`High Feed Milling` toolpaths, JM Die VDI30 holder baseline.

**ROOT CAUSE (definitive, routed to kilo):** `mcp-server/src/shared/calculatorProgrammingCatalogExtensions.ts` header says **"Stub: original file lost to exFAT corruption (2026-04-10)"** -- `SUPPLEMENTAL_PROGRAMMING_ENVIRONMENTS=[]` + `PROGRAMMING_ENVIRONMENT_TOOLPATH_SUPPLEMENTS={}` are EMPTY stubs. The expanded CAM catalog data was lost to disk corruption BEFORE git history began (verified: `camworks-wire` appears ONLY in test files across all git history; the extensions file's sole commit `9a9bedf445` created it as a stub -- "restore 65 + stub 10"). NOT recoverable from git. This is **CAM CONTENT = kilo's domain** (oscar doctrine excludes CAM strategy -> kilo); ~200 catalog entries to rebuild, spec fully pinned by `calculatorProgrammingCoverage.test.ts`. **ROUTED to kilo** via `state/shared/AGENT_CHAT.jsonl` (work-request, topic calculator-cam-catalog-rebuild-exfat-loss) -- oscar did NOT fabricate CAM vendor metadata to force-pass (R12/soul: out-of-lane content + budget). `calculatorWorkspace.ts` already calls `mergeProgrammingCatalog` so kilo only fills the two stub exports.

**HONEST SFC-PRODUCT VERDICT:** the SFC PHYSICS/calculation layer is PROVEN green (229/234; the 5 reds are CAM-catalog content, not a single speed/feed calc error). "100%" is blocked only by kilo's CAM-catalog rebuild + the holder VDI30 baseline.

**NEXT (resume):** (a) kilo rebuilds the CAM catalog (routed) -> re-run the 3 files; (b) surgically isolate the parallel-worker race to restore full-suite speed (the cross-worker race; single-process gets reaped under fleet load -- ran clean in a quieter window earlier); (c) SFC `.tsx` page-render tests (CalculatorPage.*/SpeedFeedPage/AdvancedSpeedFeedPanel) -- heavy ~28s renders, run as a small batch. Sibling: [[reference_oscar_sfc_jm_accuracy_harness_2026_06_24]] (JM-program accuracy, DONE). Frontend-build plan: [[reference_oscar_sfc_frontend_build_plan_2026_06_18]].
