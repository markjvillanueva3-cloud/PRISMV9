---
name: reference_code_engine_dreamartifactbundleengine
description: "receipt-bundle format for staged self-improvement."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/DreamArtifactBundleEngine.ts
  sourceHash: f99a84de
  loc: 569
aliases: DreamArtifactBundleEngine
---

# DreamArtifactBundleEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/DreamArtifactBundleEngine.ts` (569 LOC)

## What it is
receipt-bundle format for staged self-improvement.

```
DreamArtifactBundleEngine — receipt-bundle format for staged self-improvement.

Pure-core writer/reader for the 4-file receipt artifact that wraps a
proposed mutation set with provenance + diff metadata.  Format adopted
from Hermes Dreaming v0.1.0 (Tony Simons, https://github.com/asimons81/hermes-dreaming)
verbatim for cross-tool interop:

    <bundle-root>/<artifact-id>/
      manifest.json       — run identification, schemaVersion, parent-trace
      REPORT.md           — human-readable summary (renders in /system-viz)
      sources.jsonl       — what got scanned (handoffs, memories, wiki, transcript)
      proposals.jsonl     — proposed mutations, one per line

Pure-core only: returns serialized strings.  Filesystem writes are the
caller's responsibility (per PRISM convention: pure-core vs I/O wrapper).

Composes with:
 - DreamLoopProposalEngine (HSE06) — `fromDreamLoopBatch()` adapte
```


## Exports
`BUNDLE_SCHEMA_VERSION` (const), `MutationTypeSchema` (const), `MutationType` (type), `RiskClassSchema` (const), `RiskClass` (type), `SourceTypeSchema` (const), `SourceType` (type), `ProposalStatusSchema` (const), `ProposalStatus` (type), `ManifestSchema` (const), `Manifest` (type), `ProposalSchema` (const), `Proposal` (type), `SourceSchema` (const), `Source` (type), `BundleSchema` (const), `Bundle` (type), `SerializedBundle` (interface), `ValidationResult` (interface), `CreateBundleInputSchema` (const)

## Key methods
`createBundle`, `serializeBundle`, `parseBundle`, `validateBundle`, `appendProposal`, `appendSource`, `renderReport`, `fromDreamLoopBatch`, `fromMemoryDiff`, `getCapabilities`, `diffAgainstLive`, `planApply`, `markDiscarded`

## Depends on
[[reference_code_engine_dreamloopproposalengine]] | [[reference_code_engine_memorydiffengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/DreamArtifactBundleEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find DreamArtifactBundleEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/DreamArtifactBundleEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
