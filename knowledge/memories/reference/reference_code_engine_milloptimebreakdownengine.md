---
name: reference_code_engine_milloptimebreakdownengine
description: "MillOpTimeBreakdownEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MillOpTimeBreakdownEngine.ts
  sourceHash: e72e595e
  loc: 335
aliases: millOpTimeBreakdownEngine
---

# millOpTimeBreakdownEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MillOpTimeBreakdownEngine.ts` (335 LOC)

## What it is
```
MillOpTimeBreakdownEngine
===========================

Produces a detailed time breakdown for a single mill operation. Mill
parity for LatheOpTimeBreakdownEngine (LATHE-PRO-MS5). Lathe has 9
buckets; mill substitutes mill-specific timing categories and adds 3
MILL-CANONICAL extensions.

  Lathe-shared buckets (6):
    - cutting, air_rapid, probe, spindle_ramp, load_unload, fixed

  Mill-specific substitutions:
    - tool_change (ATC swap instead of lathe turret index)
    - thread → tap_threadmill (G84 tap or G2/G3 helical thread mill)
    - chip_conveyor_pause → chip_evac_pause (still applies; ties iter73)

  MILL-CANONICAL extensions (3 added):
    - rotary_index_sec    — 4th/5th axis A/B/C index time (ties iter74)
    - pallet_change_sec   — pallet shuttle cycle (HMC palletization)
    - tcp_transform_sec   — TCPM (G68.2) frame-transform overhead per call

The engine accepts a lean "M
```


## Exports
`DEFAULT_RAPID_FEED_MM_MIN_MILL` (const), `DEFAULT_ATC_SWAP_SEC` (const), `DEFAULT_TAP_THREADMILL_SEC` (const), `DEFAULT_PROBE_SEC_EACH` (const), `DEFAULT_CHIP_EVAC_PAUSE_DUR_SEC` (const), `DEFAULT_SPINDLE_ACCEL_RPS2` (const), `DEFAULT_LOAD_UNLOAD_SEC` (const), `DEFAULT_FIXED_OVERHEAD_SEC` (const), `DEFAULT_ROTARY_INDEX_SEC` (const), `DEFAULT_PALLET_CHANGE_SEC` (const), `DEFAULT_TCP_TRANSFORM_SEC` (const), `PRODUCTIVE_FRAC_LOW` (const), `PRODUCTIVE_FRAC_HIGH` (const), `TOOL_CHANGE_RATIO_WARN` (const), `AIR_TIME_RATIO_WARN` (const), `MillOpSummary` (interface), `MillOpTimeBreakdown` (interface), `millOpTimeBreakdownEngine` (const)

## Key methods
`compute`, `aggregate`, `getStats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MillOpTimeBreakdownEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find millOpTimeBreakdownEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MillOpTimeBreakdownEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
