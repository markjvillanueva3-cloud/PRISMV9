---
name: reference_code_engine_slotbriefengine
description: "the WRITE side of the targeted orchestrator→slot brief channel."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SlotBriefEngine.ts
  sourceHash: 77d21896
  loc: 170
aliases: SlotBriefEngine
---

# SlotBriefEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SlotBriefEngine.ts` (170 LOC)

## What it is
the WRITE side of the targeted orchestrator→slot brief channel.

```
SlotBriefEngine — the WRITE side of the targeted orchestrator→slot brief channel.

The `.claude/hooks/slot-brief-inject.mjs` hook is the READ/deliver side: it surfaces
a queued brief into a slot's next prompt and consumes it (archives to _delivered/).
THIS engine is how the Hermes app (the slot-less ZULU master, via the `prism_context`
MCP surface) and any chat ISSUE a targeted work order to ONE slot.

Symmetry: `ChatBusEngine.postMessage` is the BROADCAST writer; this is the TARGETED,
consume-once counterpart. Storage mirrors the hook exactly:
  - Queued:    state/shared/slot-briefs/<slot>.md   (one brief per slot at a time)
  - Delivered: state/shared/slot-briefs/_delivered/<slot>-<intMtimeMs>-<hash>.md
Briefs are transient runtime state (git-ignored); only the lane README is tracked.

Root resolves via PATHS.STATE_DIR (honors PRISM_ROOT env, same as the hook) with a
constructor overri
```


## Exports
`WriteBriefInput` (interface), `WriteBriefResult` (interface), `PendingBrief` (interface), `DeliveredBrief` (interface), `SlotBriefEngine` (class), `slotBriefEngine` (const)

## Key methods
`writeBrief`, `safeWriteSync`, `listPending`, `listDelivered`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/SlotBriefEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SlotBriefEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SlotBriefEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
