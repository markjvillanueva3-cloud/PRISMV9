---
name: reference_code_engine_swissparttransfersequenceengine
description: "Swiss Part Transfer Sequence"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SwissPartTransferSequenceEngine.ts
  sourceHash: 68d2537c
  loc: 344
aliases: SwissPartTransferSequenceEngine
---

# SwissPartTransferSequenceEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SwissPartTransferSequenceEngine.ts` (344 LOC)

## What it is
Swiss Part Transfer Sequence

```
SwissPartTransferSequenceEngine
================================

Generates the main → sub-spindle **part-transfer sequence** for Swiss /
mill-turn multi-channel controllers (MS6a / U-LPM03). Emits the correct
M-code ordering so that the sub-spindle has gripped the part BEFORE the
cut-off tool advances — the single most common source of catastrophic
transfer crashes.

Canonical sequence (applies to all dialects; only M-codes change):
  1.  Both channels reach safe position (sync).
  2.  Main spindle holds at cut-off RPM (CSS locked).
  3.  Sub-spindle advance to part-grip Z.
  4.  Sub-spindle collet close (M-code varies).
  5.  Sync — BOTH channels wait for grip-confirmed sensor.
  6.  (optional) phase-sync main↔sub if synchronous mode supported.
  7.  Cut-off tool advance; part-off feed.
  8.  Main spindle chuck open (or bar-pull).
  9.  Sub-spindle retract with part to Op2 position.
  
```


## Exports
`PartTransferInput` (interface), `TransferStep` (interface), `PartTransferResult` (interface), `SwissPartTransferSequenceEngine` (class), `swissPartTransferSequenceEngine` (const)

## Key methods
`generate`, `push`

## Depends on
[[reference_code_engine_swisschannelfileemitterengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/SwissPartTransferSequenceEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SwissPartTransferSequenceEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SwissPartTransferSequenceEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
