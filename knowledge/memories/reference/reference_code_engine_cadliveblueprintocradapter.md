---
name: reference_code_engine_cadliveblueprintocradapter
description: "CAD Live Blueprint Ocr Adapter"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADLiveBlueprintOcrAdapter.ts
  sourceHash: 77e59e75
  loc: 556
aliases: CADLiveBlueprintOcrAdapter
---

# CADLiveBlueprintOcrAdapter -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADLiveBlueprintOcrAdapter.ts` (556 LOC)

## What it is
CAD Live Blueprint Ocr Adapter

```
CADLiveBlueprintOcrAdapter -- CAD-DRAW-MAX-MS1/U-PRINT-OCR-LIVE
                           -- BLUEPRINT-VISION-OCR/U-PRINT-OCR-PDF (PDF + multi-page, 2026-06-19)

Live adapter wiring the existing {@link BlueprintVisionOCREngine} (free
Ollama-first llmEngine.queryVision powered) as the `ocrPrint` dependency of
the round-trip validation engine, and the engine behind the
`cad_live_blueprint_ocr` dispatcher action.

Maps `ExtractedDimension[]` (from BlueprintVisionOCREngine) ->
`PrintDimension[]` (the round-trip engine's expected shape). Preserves
dimension ids, labels, nominal values, and asymmetric tolerances.

**Hermetic-testable via injectable analyzer + rasterizer.** Default uses the
`blueprintVisionOCREngine` singleton (which calls the vision ladder live) and
the `defaultPdfRasterizer` (shells the canonical `scripts/lib/pdf-to-png.py`);
tests inject a stub analyzer + stub rasterizer (n
```


## Exports
`BlueprintAnalyzer` (interface), `PdfRasterizer` (interface), `LiveOcrAdapterOptions` (interface), `classifyPrintPath` (function), `selectPdfPageIndices` (function), `mapDimension` (function), `mapAnalysisToPrintOcr` (function), `unionDimensions` (function), `unionFeatures` (function), `sanitizeLiveOcrAdapterOptions` (function), `defaultPdfRasterizer` (const), `CADLiveBlueprintOcrAdapter` (class), `cadLiveBlueprintOcrAdapter` (const), `makeLiveOcrPrintFn` (function)

## Key methods
`analyzeBlueprint`, `pageCount`, `renderPage`, `resolvePdfToPngScript`, `String`, `ocrPrint`, `unlinkSync`

## Depends on
[[reference_code_engine_cadroundtripvalidationengine]] | [[reference_code_engine_blueprintvisionocrengine]] | [[reference_code_engine_blueprintocrengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADLiveBlueprintOcrAdapter.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADLiveBlueprintOcrAdapter` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADLiveBlueprintOcrAdapter.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
