---
name: reference_code_algorithm_lowrankapproximation
description: "LowRankApproximation — truncated SVD (rank-k) via power iteration + deflation."
metadata:
  type: reference
  kind: code-algorithm
  galaxy: unassigned
  sourcePath: mcp-server/src/algorithms/LowRankApproximation.ts
  sourceHash: ca71ab3e
  loc: 314
aliases: LowRankApproximation
---

# LowRankApproximation -- Algorithm SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/algorithms/LowRankApproximation.ts` (314 LOC)

## What it is
```
LowRankApproximation — truncated SVD (rank-k) via power iteration + deflation.

Computes the best rank-k approximation A_k ≈ U · diag(S) · Vᵀ of an m×n matrix
A by extracting the top-k singular triplets one at a time:

  for c = 1..k:
    v ← dominant eigenvector of AᵀA   (power iteration: v ← normalize(AᵀA v))
    σ ← ‖A v‖ ,  u ← A v / σ          (Rayleigh estimate — valid even pre-convergence)
    A ← A − σ u vᵀ                    (deflation removes this component)

By Eckart–Young, A_k = Σ σᵢ uᵢ vᵢᵀ is the optimal rank-k approximation in both
Frobenius and spectral norm. The reconstruction error ‖A − A_k‖_F is read
directly off the deflated residual matrix, so it is EXACT regardless of how
well each power iteration converged.

This is the math core under PRISM's ~95 LoRA engines (low-rank adaptation =
approximating a weight update ΔW by a rank-k product B·A): rank selection,
weight c
```


## Exports
`LowRankInput` (interface), `LowRankOutput` (interface), `LowRankApproximation` (const)

## Key methods
`validate`, `calculate`, `getMetadata`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/algorithms/LowRankApproximation.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Algorithm

## How to query it
Recall by meaning via `/system-viz find LowRankApproximation` or `prism_session:master_index_query`; open the source at `mcp-server/src/algorithms/LowRankApproximation.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
