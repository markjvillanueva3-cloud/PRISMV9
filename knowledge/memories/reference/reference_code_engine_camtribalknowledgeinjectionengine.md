---
name: reference_code_engine_camtribalknowledgeinjectionengine
description: "Context Tooltips for CAM UI (U-CAM101)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMTribalKnowledgeInjectionEngine.ts
  sourceHash: 01ea6481
  loc: 431
aliases: CAMTribalKnowledgeInjectionEngine
---

# CAMTribalKnowledgeInjectionEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMTribalKnowledgeInjectionEngine.ts` (431 LOC)

## What it is
Context Tooltips for CAM UI (U-CAM101)

```
CAMTribalKnowledgeInjectionEngine — Context Tooltips for CAM UI (U-CAM101)
===========================================================================

PHASE-7: Context-aware tribal-knowledge tooltip injection for the four CAM
plugin adapters. Wraps the central TribalKnowledgeEngine (3,700+ tips
across 18 CAM systems) with per-host encoders so a CAM operator sees the
most relevant operator wisdom for the current operation, material, machine,
and workholding context — without leaving the CAM UI.

Pipeline:

  ┌────────────────┐     buildContext()     ┌──────────────────────┐
  │ TooltipContext │ ─────────────────────► │ KnowledgeSearchInput │
  └────────────────┘                        └──────────┬───────────┘
                                                       │
                                           tribalKnowledgeEngine.search()
                                                  
```


## Exports
`TooltipTargetSchema` (const), `TooltipTarget` (type), `TooltipContextSchema` (const), `TooltipContext` (type), `TooltipTipSchema` (const), `TooltipTip` (type), `TooltipFrameSchema` (const), `TooltipFrame` (type), `TooltipStatsSchema` (const), `TooltipStats` (type), `DEFAULT_TIP_LIMIT` (const), `MAX_TIP_LIMIT` (const), `buildContext` (function), `relevanceScore` (function), `TipSearchFn` (type), `CAMTribalKnowledgeInjectionEngine` (class), `camTribalKnowledgeInjectionEngine` (const)

## Key methods
`supportedTargets`, `buildSearchInput`, `scoreTip`, `pickTopTips`, `renderTooltip`, `getStats`, `resetSession`, `resetAll`

## Depends on
[[reference_code_engine_tribalknowledgeengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CAMTribalKnowledgeInjectionEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMTribalKnowledgeInjectionEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMTribalKnowledgeInjectionEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
