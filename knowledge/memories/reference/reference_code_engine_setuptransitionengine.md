---
name: reference_code_engine_setuptransitionengine
description: "Three sub-engines in a single file:"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SetupTransitionEngine.ts
  sourceHash: 49f6e654
  loc: 772
aliases: SetupTransitionEngine
---

# SetupTransitionEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SetupTransitionEngine.ts` (772 LOC)

## What it is
Three sub-engines in a single file:

```
SetupTransitionEngine — MF-MS3: Setup Transition + Predictive Failure + Force Capability

Three sub-engines in a single file:

1. Setup Transition Analysis (analyzeSetupTransition):
   - Datum chain tolerance stack (RSS: σ_total = √Σtol_i²)
   - Flip/re-fixture feasibility (orientation stability, support area)
   - Pallet layout collision (envelope vs station clearance)

2. Predictive Failure Analysis (predictFailureProbability):
   - Monte Carlo N=500 trials with Box-Muller normal sampling
   - Kienzle force perturbation (Fc = kc1.1 × h^(1-mc) × ap × f)
   - Sensitivity analysis via variance decomposition
   - Per-operation failure risk map

3. Force Capability Analysis (analyzeForceCapability):
   - Power check: P = Fc × Vc / 60000 vs machine max
   - Torque check: T = Fc × D / 2000 vs torque curve
   - Thermal growth: δ_thermal = α × ΔT × L
   - Wear force increase: Fc_worn = Fc × (1 
```


## Exports
`SetupConfig` (interface), `OperationBetween` (interface), `SetupTransitionInput` (interface), `SetupTransitionResult` (interface), `PredictiveOperation` (interface), `PredictiveFailureInput` (interface), `FailureMode` (interface), `SensitivityFactor` (interface), `PredictiveFailureResult` (interface), `ForceCapabilityOperation` (interface), `MachineCapability` (interface), `AccumulatedState` (interface), `ForceCapabilityInput` (interface), `ForceCapabilityResult` (interface), `SetupTransitionEngine` (class), `setupTransitionEngine` (const)

## Key methods
`analyzeSetupTransition`, `computeSupportArea`, `predictFailureProbability`, `analyzeForceCapability`, `calculate`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/SetupTransitionEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SetupTransitionEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SetupTransitionEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
