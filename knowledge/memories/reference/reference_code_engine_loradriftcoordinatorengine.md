---
name: reference_code_engine_loradriftcoordinatorengine
description: "LoRADriftCoordinatorEngine — CAM-ML-CLOSEDLOOP-MS0 U-CMCCL10"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LoRADriftCoordinatorEngine.ts
  sourceHash: b66c7cac
  loc: 196
aliases: loRADriftCoordinatorEngine
---

# loRADriftCoordinatorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LoRADriftCoordinatorEngine.ts` (196 LOC)

## What it is
```
LoRADriftCoordinatorEngine — CAM-ML-CLOSEDLOOP-MS0 U-CMCCL10
==============================================================

Monitors drift signals from all 8 pipeline LoRA cadence engines.
Fires a MASTER retrain trigger when ≥ k pipelines drift within a
rolling time window — suggests a shared upstream cause (e.g. material
batch variation affecting milling AND turning simultaneously).

Algorithm: buffer drift observations with timestamps. On each
observation, count how many DIFFERENT pipelines have drifted within
the current window [now - windowMs, now]. If count ≥ threshold,
emit a coordinatedDrift event.

Output events:
  - pipelineDrift: single-pipeline drift (informational)
  - coordinatedDrift: ≥ threshold pipelines drifted in window (alert)
  - allClear: no active drifts in window (reset)

Alert publication: returns events to caller — the dispatcher is
responsible for writing to AG
```


## Exports
`DriftObservation` (interface), `DriftEventKind` (type), `DriftEvent` (interface), `CoordinatorConfig` (interface), `loRADriftCoordinatorEngine` (const), `LoRADriftCoordinatorEngine` (type), `createLoRADriftCoordinator` (function)

## Key methods
`setConfig`, `getConfig`, `record`, `activePipelines`, `shouldTriggerMasterRetrain`, `checkAllClear`, `bufferSize`, `reset`

## Depends on
[[reference_code_engine_masteraitrainingledgerengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/LoRADriftCoordinatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find loRADriftCoordinatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LoRADriftCoordinatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
