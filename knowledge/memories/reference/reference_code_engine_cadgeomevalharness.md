---
name: reference_code_engine_cadgeomevalharness
description: "cadGeomEvalHarness.ts -- DELTA-CAD-COMPLETION / U-DELTA-EVAL-HARNESS (geometry modality)."
metadata:
  type: reference
  kind: code-engine
  galaxy: cad
  sourcePath: mcp-server/src/engines/cad/cadGeomEvalHarness.ts
  sourceHash: 8bf78dac
  loc: 194
aliases: cadGeomEvalHarness.ts
---

# cadGeomEvalHarness.ts -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/cad/cadGeomEvalHarness.ts` (194 LOC)

## What it is
```
cadGeomEvalHarness.ts -- DELTA-CAD-COMPLETION / U-DELTA-EVAL-HARNESS (geometry modality).

The GEOMETRY half of the CAD eval-harness: score a model's predicted B-Rep geometry against the
frozen geom-50 held-out split (the B-Rep eval set), COMPOSING CADGeometryComparisonEngine.compare
(delta's existing geometry scorer -- volume / bbox / topology-Jaccard / Hausdorff). Sibling of the
dimension modality (scripts/lib/cad-eval-harness-lib.mjs, which composes dimension-set-score).

Held-out discipline (same as the dim modality): score ONLY parts in the frozen geom-50 split; a
prediction for a NON-held part is flagged as eval contamination (evaluating on a training part is the
train/test leak the leak-guard exists to prevent). The held key is the held part's TRUTH abs_path
itself (unique per the freezer's path-dedup -- no stem-collision coverage-inflation, the lesson from
the dim modality).

com
```


## Exports
`ComparisonResultLike` (interface), `GeomCompareFn` (type), `GeomPartScore` (interface), `GeomEvalAggregate` (interface), `GeomEvalResult` (interface), `normGeomKey` (function), `geomEvalHeldOut` (function), `DEFAULT_MIN_PASS_FRACTION` (const), `DEFAULT_MIN_MEAN_TOPOLOGY` (const), `DEFAULT_MIN_COVERAGE` (const), `GeomGateResult` (interface), `gateGeomEval` (function)

## Key methods
_(none extracted)_

## Depends on
[[reference_code_engine_cadgeometrycomparisonengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/cad/cadGeomEvalHarness.ts`
- **Galaxy:** **cad**
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find cadGeomEvalHarness.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/cad/cadGeomEvalHarness.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
