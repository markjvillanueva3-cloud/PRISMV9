---
name: reference_code_engine_spechtmlcompanionengine
description: "master Markdown → HTML renderer for PRISM strategic specs."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SpecHTMLCompanionEngine.ts
  sourceHash: 8958b96f
  loc: 808
aliases: SpecHTMLCompanionEngine
---

# SpecHTMLCompanionEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SpecHTMLCompanionEngine.ts` (808 LOC)

## What it is
master Markdown → HTML renderer for PRISM strategic specs.

```
SpecHTMLCompanionEngine — master Markdown → HTML renderer for PRISM strategic specs.

Owns the HTML render layer for BACKEND-DEVTOOLS-RGS6 HTML-COMPANION-MS0 (HC-0..HC-5);
HTML-PRIMARY-MS0 (U-HPS01..07) consumes / hardens it. The contract is a *pure* function
`render(md, opts) → RenderResult` — no filesystem, no network — so it is equally usable
from the `scripts/emit-spec-html.mjs` CLI, the `prism_dev:spec_html_render` dispatcher
action, and unit tests.

Self-contained: no `marked`/`markdown-it` dependency (none is installed and a strategic-spec
renderer must work offline + emit W3C-valid static HTML). The Markdown subset covered is the
one PRISM specs actually use:
  - ATX headings `#`..`######` → `<h1 id="slug">` … with collision-safe slugs (HC-3 anchors)
  - **bold** / *italic* / _italic_ / `code` / ~~strike~~  + inline `[text](url)` / `![alt](url)`
  - unordered (`-`/`*`/`+`) and or
```


## Exports
`SpecHtmlTheme` (type), `RenderOptions` (interface), `SpecHeading` (interface), `RenderResult` (interface), `escapeHtml` (function), `SpecHTMLCompanionEngine` (class), `specHtmlCompanionEngine` (const)

## Key methods
`renderInline`, `renderBlocks`, `clearMarks`, `render`, `hashSource`, `isDrifted`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/SpecHTMLCompanionEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SpecHTMLCompanionEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SpecHTMLCompanionEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
