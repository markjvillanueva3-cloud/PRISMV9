---
name: reference_gnn_tier5_degenerate_2026_06_17
description: "CORRECTED+WIN: PSN leg #10 (GNN tier-5) restored to DEPLOY-READY-SELECTIVE on 2026-06-17 -- the durable outcome-refpool growth (13->84 holdout) + a regenerated ghost-node-embeddings.jsonl cleared the selective gate (AUROC 0.789, tau=0.7: 27% coverage, macroF1 1.0, Brier 0.042, robust). The initial 'degenerate 0.5' reading was a WRONG-MODE artifact (ran the eval without PRISM_NNG_DIRECT_EMBED=1)."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.594Z
aliases: reference_gnn_tier5_degenerate_2026_06_17
---


**slot:india, 2026-06-17 (MCP-down session). Arc: an initial wrong reading -> root cause -> a real metrics-gated WIN.**

## Wrong-mode footgun -- NOW SURFACED IN --status (2026-06-17, `324d09c661`, U-GNN-STATUS-DEPLOYED-METRIC)
The lifecycle `--status` printed ONLY the trained 8-d checkpoint AUROC (~0.096) -- the link-pred PRETEXT metric -- with NO deployed-path line, so a reader (or me, at session start) misreads leg #10 as broken. FIXED: `--status` now annotates the checkpoint line "link-pred PRETEXT, NOT the deploy gate" + appends a `Deployed (direct-embed, NN-EVAL.json)` block (pure `formatDeployedStatus(evalDoc)`, reads the eval's pre-computed `selective.deployGrade`, re-derives no gate; absent/deferred/model-mode surfaced honestly). Live `--status` now shows: checkpoint 0.096 (PRETEXT) + Deployed AUROC 0.7891 selective deploy-ready @tau=0.7 27.4% robust. lifecycle 92/92, 2-arm PASS. So the wrong-mode misread can't recur from `--status`.

## What I got wrong first (R12 self-correction)
A bare `node scripts/lib/nn-graph-eval.mjs` (NO `PRISM_NNG_DIRECT_EMBED=1`) reported AUROC **0.5 degenerate** (constant-vote prism_dev) and I initially recorded leg #10 as "degenerate." That was a **wrong-mode artifact**: the default 8-d edgeless-SAGE path IS structurally degenerate (`buildGhostSubgraph` returns `edges:[]` -> MEAN-agg=0 -> constant vote -- the known 2026-06-04 defect). The DEPLOYED inference mode is **direct-embed** (raw 768-d nomic cosine k-NN), set by `PRISM_NNG_DIRECT_EMBED=1`. Lesson: ALWAYS eval tier-5 in direct-embed mode; the bare 8-d path is the un-fixed degenerate baseline. (My bare run also transiently clobbered NN-EVAL.json -> restored.)

## The real blocker (root cause)
`state/shared/nn-graph/ghost-node-embeddings.jsonl` (the direct-embed vote source) was the 2026-06-04 build: 636 `ghost.unwired-engine`, **0 `ghost.outcome-wired` / 0 `ghost.vault-wired`**. This session's durable outcome-refpool work ([[reference_outcome_refpool_durable_2026_06_17]]) added 139 outcome + 14 vault refs (confidence 0.85, ground-truth-ish confirmed wirings) that GREW the eval holdout 13->84 -- but those new high-confidence pool members had **NO embedding** -> direct-embed scored 65/84 at conf~0 -> macroF1 collapsed to 0.007. The blocker was an EMBEDDING-COVERAGE GAP, not the model.

## The fix + the WIN (verified)
Regenerated the ghost embeddings from the CURRENT graph: `node build-node-embeddings.mjs --graph state/shared/system-viz/system-graph.json --ghosts-only --out state/shared/nn-graph/ghost-node-embeddings.jsonl` (Ollama nomic-embed-text, 355 ghosts incl 139 outcome + 16 vault now covered, source-enriched, leak-stripped, ~24s). Then `PRISM_NNG_DIRECT_EMBED=1 nn-graph-eval`:
- **Selective verdict: DEPLOY-READY-SELECTIVE, robust** (every tau>=gate clears). Production gate tau=0.7: coverage 27.4% (23/84 emitted), **Brier 0.0417 (<=0.15), macro-F1 1.0 (>=0.55), accuracy 1.0**, global AUROC **0.7891 (>=0.78)**, classesEmitted 2/13 (concentrated -- honestly flagged by the eval).
- Full-holdout (84): AUROC 0.789, macroF1 0.410, Brier 0.189 -- modest (full-coverage gate still fails). The asymmetry (modest full, perfect confident-27%) is the SIGNATURE OF LEGITIMATE SELECTIVE DEPLOY, **not a leak** (a leak inflates the full set; cf the 2026-06-04 fake-0.98). Leak-strip (`ghostEmbedText` drops `proposed_wiring` + literal `prism_*`) is the same validated mechanism.
- This **RESTORES the 2026-06-06 deploy-ready-selective point** ([[reference_gnn_selective_deploy_2026_06_06]]) that the 2026-06-15 pool-collapse 62->13 ([[reference_gnn_pool_collapse_confidence_deflation_2026_06_15]] / [[reference_gnn_selective_promote_disproven_2026_06_15]]) had destroyed. The lever was exactly what the doctrine predicted: **reference-pool GROWTH** (ground-truth outcome refs) + feature coverage. NN-EVAL.json now honestly shows deploy-ready-selective; the PSN-leg-state hook (via `classifyGnn`) will report leg #10 deployable-selective, not below-gate.

## DURABILITY GAP (the next unit -- R12, the win is currently EPHEMERAL)
The two halves have UNEQUAL durability: (1) the outcome/vault REFS are durable -- re-applied pre-fingerprint by the retrain-lifecycle stages 1a/1b I shipped this session. (2) `ghost-node-embeddings.jsonl` is NOT auto-regenerated -- a regen-viz rebuild wipes it (that is exactly how it went stale: 2026-06-04 -> today) -> direct-embed loses coverage of the re-applied refs -> back to non-deployable. **To make deploy-ready-selective DURABLE, wire a ghost-embedding-refresh stage into the retrain lifecycle (mirror the 1a/1b ref-pool stages): regenerate `ghost-node-embeddings.jsonl` (Ollama, fail-soft if down, opt-out env) BEFORE the direct-embed eval.** That completes the durability arc (durable refs + durable embeddings = durable deploy-ready). Also confirm the lifecycle's eval runs in DIRECT_EMBED mode (else the lifecycle never measures the deployable path). Auto-promotion stays gated on `classifyGnn` real metrics (india refuses promotion past the gate).

**DURABILITY GAP -- CLOSED 2026-06-17 (`923f38fc86`, U-GNN-GHOST-EMBED-DURABLE):** shipped lifecycle stage 4b -- after the model eval, the retrain lifecycle now (1) refreshes `ghost-node-embeddings.jsonl` from the CURRENT graph via `build-node-embeddings --ghosts-only` (covers the durable outcome/vault refs; the embedding file is written to disk -> the deployed direct-embed path survives the regen wipe -- THE durable half), and (2) records a `directEmbedAssessment` (runAssessment directEmbed mode) on the result, measuring the DEPLOYED path (distinct from defaultEval, which grades the degenerate trained 8-d candidate). ADDITIVE (promotion still gates on model-mode); fail-soft + opt-out `PRISM_NN_RETRAIN_GHOST_EMBED_DISABLE`. 87/87 tests, 2-arm scrutiny PASS. Mirrors the 1a/1b stage pattern.

**P2 PERSIST -- DONE 2026-06-17 (`788fdebf01`, U-GNN-NNEVAL-WRITE-DURABLE):** extracted a single-sourced `writeAssessment(result,outDir)` export from the eval CLI's inline write (main() refactored, behavior-preserving); lifecycle stage 4b now PERSISTS the RAW direct-embed assessment to NN-EVAL.{md,json} via it (fail-soft, injectable). So after each retrain the PSN-leg-state hook (classifyGnn reads NN-EVAL.json) auto-reflects the deployed direct-embed state. eval 76/76 + lifecycle 89/89; 2-arm scrutiny PASS. **Durability arc COMPLETE: durable refs (1a/1b) + durable embeddings (4b refresh) + durable deployed-state report (4b persist).**

**BARE-CLI CLOBBER -- CLOSED 2026-06-17 (`9db8c6eace`, U-GNN-NNEVAL-MODE-GUARD):** a BARE `node scripts/lib/nn-graph-eval.mjs` (no flags) writes the CHECKPOINT-mode (degenerate 8-d, AUROC 0.5) assessment to the SAME NN-EVAL.json the lifecycle writes with direct-embed -> silently CLOBBERED the deployed-state file the PSN-leg hook reads (the exact footgun I hit at session start). FIX SHIPPED: `writeAssessment(result,outDir,report,{force})` now SUPPRESSES a model-mode write (`result.embeddingMode === "model"`) over an EXISTING direct-mode report (returns `{ok:true, skipped:...}`, never throws) unless `{force:true}`. Direct-mode writes are never suppressed -> the lifecycle's direct-embed persist (stage 4b, 2-arg call) is unaffected; `main()` handles the `skipped` branch (logs, exit 0). Live-validated: deployed NN-EVAL.json (embeddingMode=direct) protected from a bare model-mode write. +1 mode-matrix test (6 transitions, R9 mutation-verified), eval 77/77, 2-arm scrutiny PASS (arm-B P1 doc-drift fixed: doc states the true direct-mode-exemption mechanism, NOT a false "lifecycle persists with force" claim). **RESIDUAL GAP -- CLOSED 2026-06-17 (`e80f585eb8`, U-GNN-NNEVAL-DEFERRED-FENCE):** a bare run with NO checkpoint yields a `{deferred}` result (no `embeddingMode`), which the model-only guard let slip. CLOSED by (1) tightening the guard outer test `=== "model"` -> `!== "direct"` so ALL bare-CLI non-direct writes (model AND deferred) are fenced over a deployed direct report, and (2) making the retrain lifecycle stage 4b the AUTHORITATIVE writer via `writeEval(da, NN_DIR, undefined, {force:true})` -- so its honest direct (or deferred-direct, when ghost embeddings won't load) assessment ALWAYS lands over a prior report (R12: never keep a stale "deploy-ready" claim after the deployed path defers). This ALSO made the writeAssessment doc's "persists WITH force" claim TRUE (resolved the prior unit's arm-B doc-drift P1 at the design level, not by weakening the doc). Live-validated: a deferred bare-run is now SUPPRESSED against the real deployed NN-EVAL.json. eval 77/77 (mode-matrix now 8 transitions, R9 mutation-verified), lifecycle 89/89 (heap-bumped; asserts stage 4b passes {force:true}), 2-arm scrutiny PASS 0 P0/P1. **The full clobber-protection arc is COMPLETE: model-mode guard (U-GNN-NNEVAL-MODE-GUARD) + deferred fence + authoritative force-writer.** Deferred informational P2s (both pre-existing, in handoff): a JSON-corrupt existing NN-EVAL.json falls through to write (arguably-safe fail-open); NN-EVAL.md/json are only co-written (no atomic 2-file sync). Operationally: ALWAYS eval tier-5 with `PRISM_NNG_DIRECT_EMBED=1` (the bare path is the degenerate baseline).

**Atomic NN-EVAL write -- DONE 2026-06-17 (`b3aaad542f`, U-GNN-NNEVAL-ATOMIC-WRITE):** `writeAssessment` did 2 bare `fs.writeFileSync` -> a reader (classifyGnn) could `JSON.parse` a half-written NN-EVAL.json. Added exported `atomicWriteFileSync(target,data)` (sibling tmp `${target}.tmp-${pid}`, same-dir -> renameSync atomic/never-EXDEV; best-effort cleanup + rethrow -> writeAssessment's fail-soft {ok:false} preserved); both .md+.json route through it. Single-writer real (lifecycle holds exclusive retrain.lock; .md/.json sequential to distinct targets; cross-proc distinct pids). +1 R9 test (round-trip/no-tmp-leftover/overwrite/rethrow), eval 78/78, live NN-EVAL.json intact, 2-arm PASS. **The NN-EVAL write-path hardening TRILOGY is COMPLETE: downgrade-safe (MODE-GUARD model + DEFERRED-FENCE deferred) + torn-read-safe (ATOMIC-WRITE).**

**E2E VALIDATION (`--status`, 2026-06-17, no GPU/graph-load):** the scheduled lifecycle ran today (18:05, ok=true): drift fired (ghost pool 64.4% vs 25% band -- expected after stages 1a/1b ref-pool growth) -> retrain -> model-mode candidate AUROC 0.3512 / macroF1 0.0371 / Brier 0.1507 ALL sub-gate -> `action=not-promoted` (the promotion gate + india's "never promote past gate" refuse are LIVE-enforced). Meanwhile the deployed NN-EVAL.json stayed `embeddingMode=direct, AUROC 0.7891` -- the model-mode candidate did NOT clobber it (trilogy guard + stage-4b force-write proven on live data). The live trained checkpoint is still the 2026-05-16 degenerate 8-d (AUROC 0.096 link-pred pretext) because NO candidate ever cleared the gate -- the DEPLOYED inference is direct-embed (0.789 selective), NOT the trained 8-d. Full-coverage model lift remains ref-pool growth + sharper features (H2GCN/GPU), never calibration.

**Remaining follow-ups (heavyweight, supervised):** grow the ref pool >27% selective coverage (more outcome/vault wirings -- the model-quality lever); a full --dry-run/regen-viz retrain to re-measure (GPU/24GB, reaper-risk -> launch supervised, not turn-end); rsLoRA r=32 (GPU, checkpoint-350=87.5%, task #3 pending).
