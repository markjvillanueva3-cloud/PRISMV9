---
name: reference_code_engine_gpustackhealthengine
description: "BLACKWELL-AI-MS0 / U-PYGPU-HEALTH (TS shim)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/GpuStackHealthEngine.ts
  sourceHash: bf95e0af
  loc: 345
aliases: GpuStackHealthEngine
---

# GpuStackHealthEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/GpuStackHealthEngine.ts` (345 LOC)

## What it is
BLACKWELL-AI-MS0 / U-PYGPU-HEALTH (TS shim)

```
GpuStackHealthEngine — BLACKWELL-AI-MS0 / U-PYGPU-HEALTH (TS shim)

The MCP-surfaced face of the fail-loud GPU readiness gate. It runs
`scripts/py/gpu_health.py` (the canonical verifier) and turns its JSON
contract into a typed result that every BLACKWELL-AI TRAINING consumer gates
on before it spawns a GPU job:
  - the GNN GATv2/H2GCN retrain (MS3) checks {@link GpuStackHealthResult.ready},
  - QLoRA fine-tunes (MS4) check {@link GpuStackHealthResult.qloraReady}
    (or call {@link GpuStackHealthEngine.assertReady} with `requireBnb`),
  - the GPU batch embedder + reward model (MS2/MS6) check `ready`.

COMPANION, NOT DUPLICATE, OF OllamaCapabilityProbeEngine:
  `OllamaCapabilityProbeEngine` answers "which OLLAMA models fit free VRAM right
  now" (inference routing). THIS engine answers a different question — "is the
  PYTHON+CUDA TRAINING stack actually able to run a backward pass on thi
```


## Exports
`GPU_HEALTH_TIMEOUT_MS` (const), `HEALTH_CACHE_TTL_MS` (const), `HEALTH_NOT_READY_CACHE_TTL_MS` (const), `HealthSource` (type), `PythonSource` (type), `GpuHealthRawReport` (interface), `GpuStackHealthResult` (interface), `HealthCheckOpts` (interface), `HealthReader` (interface), `GpuStackHealthEngine` (class), `gpuStackHealthEngine` (const)

## Key methods
`runGpuHealth`, `check`, `assertReady`, `clearCache`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/GpuStackHealthEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find GpuStackHealthEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/GpuStackHealthEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
