---
name: reference_code_frontend_gatederror
description: "GatedError -- the REACTIVE companion to <FeatureGate> (LAUNCH-FE 2026-06-23)."
metadata:
  type: reference
  kind: code-frontend
  galaxy: unassigned
  sourcePath: mcp-server/web/src/components/entitlement/GatedError.tsx
  sourceHash: a0d57675
  loc: 60
aliases: GatedError
---

# GatedError -- Frontend SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-04T14:51:51.225Z  |  source: `mcp-server/web/src/components/entitlement/GatedError.tsx` (60 LOC)

## What it is
```
GatedError -- the REACTIVE companion to <FeatureGate> (LAUNCH-FE 2026-06-23).

<FeatureGate> gates a feature PROACTIVELY (client-side `can(feature)` -> no API
call for a wrong-plan user). GatedError closes the other half: when a page DID
call a gated dispatcher and the backend `requireTier` returned 403, render the
upgrade CTA instead of a raw error string. Any non-gate error (network, 500,
validation) falls through to the page's normal error UI.

It composes the canonical pieces -- `isEntitlementError` (the single definition
of a 403 tier-gate denial) + the live plan from `useEntitlement` -- so one import
gates a page's error path without each page re-plumbing the plan lookup. The page
only has to RETAIN the caught error object and pass it here.

Dormant-safe: until the backend route actually returns 403, `isEntitlementError`
is false and the fallback renders exactly as before -- so wir
```


## Exports
`GatedErrorProps` (interface), `GatedError` (function)

## Key methods
_(none extracted)_

## Depends on
[[reference_code_frontend_upgradeprompt]]

## Where it lives
- **Source:** `mcp-server/web/src/components/entitlement/GatedError.tsx`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Frontend

## How to query it
Recall by meaning via `/system-viz find GatedError` or `prism_session:master_index_query`; open the source at `mcp-server/web/src/components/entitlement/GatedError.tsx`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
