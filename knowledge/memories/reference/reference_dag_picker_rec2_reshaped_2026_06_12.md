---
name: reference_dag_picker_rec2_reshaped_2026_06_12
description: 2026-06-12 slot:alpha SCOPING of GRAPH-UTILIZATION rec #2 (U-DAG-PICKER, "fix 0/3197 depends_on loop blindness") RESHAPED it by verify-first. RoadmapDAGEngine.ts already EXISTS + is wired (devDispatcher + orchestrationDispatcher) -- do NOT rebuild. The real gap is NOT picker logic: unit dependency data (depends_on/blockedBy) DOES exist in data/milestones/*.json but is DROPPED by the consolidation step (ROADMAP-CONSOLIDATED.json has only 3), so the picker (priority-queue/slot-queue) is structurally blind to it. Building a DAG picker without propagating that data first would be inert (the local-vector-leg built-but-no-data trap again).
type: reference
galaxy: token-optimization
source: prism-memory
synced: 2026-06-27T20:30:46.535Z
aliases: reference_dag_picker_rec2_reshaped_2026_06_12
---


# GRAPH-UTILIZATION rec #2 (U-DAG-PICKER) -- reshaped by scoping (2026-06-12, slot:alpha)

**Naive rec (from GRAPH-UTILIZATION-ASSESSMENT-2026-06-12):** "fix 0/3197 depends_on loop blindness via RoadmapDAGEngine.ts." Verify-first scoping (the discipline that already refuted 2 'easy win' levers this session) shows that rec is wrong on TWO counts:

1. **RoadmapDAGEngine.ts ALREADY EXISTS and is WIRED** (`mcp-server/src/engines/RoadmapDAGEngine.ts`, U-FORE-07/PSAU-FORESIGHT; wired into `devDispatcher.ts` + `orchestrationDispatcher.ts`). Do NOT rebuild (R8/DuplicationGuard). It loads `roadmap-index.json` and exposes MILESTONE-level `dependencies`/`blocks` as a DAG for CriticalPathDetector/schedulers. It is forgiving (accepts `dependencies` or `blocks`, infers direction, filters completed).

2. **The data gap, not the picker logic, is the real blocker.** `ROADMAP-CONSOLIDATED.json` (what the consolidated views read) carries only **3** `depends_on` -- BUT the per-milestone source files `data/milestones/*.json` (AGENT-ROADMAP, AI-MAX-ROADMAP, AI-STACK-PER-DOMAIN-MS0, AI-WIRE-MS0, ARCH-MS0..4, ...) DO carry `depends_on`/`blockedBy`. So the dependency data EXISTS at the source but is **dropped by the consolidation step** (`scripts/consolidate-roadmaps.mjs`). The runtime pickers (`.claude/helpers/priority-queue.mjs`, `slot-queue.mjs`) reference NO dependency field at all -- confirmed dependency-blind.

**Therefore the genuinely high-ROI unit is NOT "build a DAG picker" -- it is two narrower, ordered pieces:**
- **U-DAG-PROPAGATE-DEPS (do FIRST, the data foundation):** make `consolidate-roadmaps.mjs` (and/or the picker's input builder) PRESERVE the per-milestone `depends_on`/`blockedBy` into the consolidated unit records + the slot-task-queue, instead of dropping them. Without this the picker has ~0 edges to reason over -- a DAG picker would be inert (the exact built-but-unwired/no-data trap from [[reference_local_vector_leg_2026_06_12]]). VALIDATE with the real count of units that GAIN a dependency edge.
- **U-DAG-PICKER (do SECOND, on the proven data foundation):** make `priority-queue.mjs` / `slot-queue.mjs` skip (or de-prioritize) a unit whose `depends_on` are not yet `complete` -- reusing `RoadmapDAGEngine`'s existing topological logic where possible rather than re-implementing. Default-on + a revert knob; VALIDATE that a known dependency-ordered pair is handed out in the right order.

**SHIPPED 2026-06-12 (slot:alpha)** — slot/alpha `d2d6d5290c` (patcher `scripts/apply-dag-picker.mjs`) + [MAIN-FORCE] `4d8f3b5b02` (live `priority-queue.mjs` + `dag-picker.test.mjs`). The picker now captures `depends_on` from the milestone envelopes it already loads (`buildEnvelopeIndex.unitDeps`) and pushes dependency-unsatisfied units BEHIND ready ones (`partitionByDeps` in `pickNextUnit`, `PRISM_PQ_DAG_DISABLE=1` revert; blocked never dropped — R12). **KEY SCRUTINY LESSON — the CANONICAL-ID GATE:** the first cut keyed deps by bare unit-id and checked against the shipped union, which a 2-reviewer adversarial pass caught as a 2-P1 FAIL — phase-letter ids (P0-U06 ×210, A2, H1) are (1) reused across milestones → last-writer-wins collision, and (2) absent from the shipped union → 61% FALSE-block. Fix: capture/check deps ONLY for canonical `U-<…>` ids (`CANON_UID=/^U-[A-Z0-9._-]+$/`, first-wins) — phase-letter units get no edge (never blocked). Result: 0 phase-letter keys, 0 conflicting canonical keys (128 reused canonical ids all dep-identical), canonical deps DO match the shipped union (U-AIMAX10 recognized shipped → U-AIMAX11 ready). 657 canonical-dep units; `U-AIW04→03B→03→02` genuine chains correctly ordered. 10/10 new + 35/35 existing tests; re-review PASS. **Doctrine: when a feature keys off unit-ids, gate to the canonical `U-` namespace — the phase-letter namespace is provably unreliable for both keying and shipped-matching (the file already documented this for `excludeIds` but the new code initially missed the lesson).**

**DEFINITIVE DATA AUDIT (2026-06-12, completed scoping) -- foundation CONFIRMED + build path SIMPLER than first reshape:**
- **118 PENDING units carry real non-empty dependency edges** (out of 2113 units across 752 milestone files; 270 units have deps, 152 already complete, **118 pending** = the picker-addressable set). Real samples: `U-AIMAX09 -> [U-AIMAX08]`, `U-AIMAX12 -> [U-AIMAX10,U-AIMAX11]`, `U-AIW02 -> [U-AIW01]`, `U-AIW03 -> [U-AIW02]`. The data is REAL (chained topological constraints), so rec #2 is NOT refuted -- it has a genuine foundation.
- **The data lives ONLY in `data/milestones/*.json`** (field `depends_on`, array of unit-ids). It is LOST before the picker: `MILESTONE_PROGRESS.json` has ZERO non-empty `dependencies` (stale, or `build-milestone-progress.mjs`'s `u.depends_on` read doesn't capture them -- verify which), and `consolidate-roadmaps.mjs::collectPendingUnits` (lines 140-147) builds records WITHOUT any deps field. So both the MP and CONSOLIDATED hops are dead ends for this data.
- **SIMPLER BUILD PATH (supersedes the earlier "propagate through consolidation" plan):** `priority-queue.mjs` ALREADY loads the envelope index from `data/milestones/*.json` (line 268, `Load + memoize the envelope index`) -- i.e. **the picker already has the raw source where `depends_on` lives.** So `U-DAG-PICKER` can read `depends_on` straight from that envelope index it already loads, check whether each candidate's deps are all shipped/complete (via MILESTONE_PROGRESS shipped-flags or the consolidated completion set), and SKIP/de-prioritize a unit whose deps are unsatisfied. **No data-propagation through consolidate-roadmaps needed** -- the original `U-DAG-PROPAGATE-DEPS` is OBVIATED; it collapses to a single picker enhancement. Default-on + revert knob; reuse `RoadmapDAGEngine` topo logic if it composes (it ingests milestone-level edges, so a unit-level check may be simpler standalone).
- **Pre-build (next pass):** read `priority-queue.mjs` pick logic + the envelope-index memo structure (line ~268-300) to see exactly where to inject the dep-satisfaction filter; confirm the completion-status source for a dep unit-id (shipped flag). VALIDATE on the 118-unit set: a known chained pair (e.g. U-AIMAX10 before U-AIMAX12) is handed out in dependency order. Build via Workflow (ultracode). This converged after a full data audit -- the build is now low-ambiguity. Sister scoping pattern: the local-vector leg (scope-pass -> build-pass).