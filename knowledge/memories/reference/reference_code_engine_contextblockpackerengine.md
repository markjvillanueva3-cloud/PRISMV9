---
name: reference_code_engine_contextblockpackerengine
description: "HMEMV07 per-block token budget packer."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ContextBlockPackerEngine.ts
  sourceHash: 2060def3
  loc: 89
aliases: ContextBlockPackerEngine
---

# ContextBlockPackerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ContextBlockPackerEngine.ts` (89 LOC)

## What it is
HMEMV07 per-block token budget packer.

```
ContextBlockPackerEngine — HMEMV07 per-block token budget packer.

Pure-core packer: caller supplies the LLM's max-context token budget plus
a list of candidate blocks (system prompt, tool defs, conversation,
recall results, scratchpad) with priorities + token estimates; engine
returns a packing plan that fits within the budget and reports what was
dropped.  Greedy by priority DESC (highest priority included first).

Complements the existing `ContextBudgetEngine` (which manages per-category
aggregate budgets across dispatchers): this engine operates at the
per-block packing layer that produces a single LLM call's context.

@module engines/ContextBlockPackerEngine
```


## Exports
`ContextBlockSchema` (const), `ContextBlock` (type), `PackingPlan` (interface), `ContextBlockPackerEngine` (class), `contextBlockPackerEngine` (const)

## Key methods
`validateBlock`, `plan`, `renderPlan`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ContextBlockPackerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ContextBlockPackerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ContextBlockPackerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
