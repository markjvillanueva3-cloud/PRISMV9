---
name: reference_code_engine_ideablockdedupengine
description: "Idea Block Dedup"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/IdeaBlockDedupEngine.ts
  sourceHash: 3fe2338a
  loc: 486
aliases: IdeaBlockDedupEngine
---

# IdeaBlockDedupEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/IdeaBlockDedupEngine.ts` (486 LOC)

## What it is
Idea Block Dedup

```
IdeaBlockDedupEngine
====================

OBSIDIAN-INTELLIGENCE-MS3/E2/U-IDEABLOCK-DEDUP

Iterative cosine-similarity deduplication of IdeaBlocks (the atomic
semantic units E1 extracts from `.md` notes). Near-duplicate blocks —
the same claim phrased slightly differently across many notes — are
collapsed to a single canonical block while their provenance
(source_path of every absorbed block) is preserved in a merge map so
D1 (provenance) and E3 (IdeaBlock-level RAG) lose nothing.

Why ITERATIVE + CONNECTED-COMPONENTS (the load-bearing design point):
  - Within a round, clustering is the connected components of the
    ≥threshold cosine graph (union-find), NOT single-linkage to a
    seed. So a transitive family A≈B, B≈C, A≉C collapses in ONE round
    (A–B and B–C edges put all three in one component) and the result
    is invariant to input order AND to which content-hash id happens
  
```


## Exports
`DedupItem` (interface), `IdeaBlockDedupResult` (interface), `IdeaBlockDedupOptions` (interface), `IdeaBlockDedupOptionsSchema` (const), `IdeaBlockDedupEngine` (class), `_internals` (const), `ideaBlockDedupEngine` (const), `runIdeaBlockDedup` (function)

## Key methods
`dedupVectors`, `validateOptions`, `dedupBlocks`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/IdeaBlockDedupEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find IdeaBlockDedupEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/IdeaBlockDedupEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
