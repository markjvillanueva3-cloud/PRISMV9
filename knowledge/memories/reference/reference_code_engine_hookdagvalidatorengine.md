---
name: reference_code_engine_hookdagvalidatorengine
description: "HOOK-MANIFEST-DAG-MS26 / P0-U02"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HookDAGValidatorEngine.ts
  sourceHash: 3d654945
  loc: 556
aliases: HookDAGValidatorEngine
---

# HookDAGValidatorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HookDAGValidatorEngine.ts` (556 LOC)

## What it is
HOOK-MANIFEST-DAG-MS26 / P0-U02

```
HookDAGValidatorEngine — HOOK-MANIFEST-DAG-MS26 / P0-U02

Consumes a {@link ../schemas/hookManifestSchema.HookManifest} (output of
{@link HookManifestEngine.HookManifestEngine}, P0-U01) and emits a
{@link ../schemas/hookDAGSchema.HookDAGValidation}: per event, the implicit hook
execution DAG, every cycle, and a deterministic linear order (when no cycle).

Why a static DAG check at all: the Claude Code harness has no documented total order
over hooks contributed by *different* settings files (user-global vs project vs
`.local`), and the per-file `order` is implicit (settings-array position). With ≥3
settings.json files contributing wirings across ≥10 events, drift creeps in fast
(e.g. a stale wiring with `continueOnError:false` left in `~/.claude/settings.json`
while the project pinned it `true`). This engine surfaces that drift before it
silently changes runtime behavior, and it gives th
```


## Exports
`DAGFS` (type), `HookDAGValidatorOptions` (interface), `HookDAGValidatorEngine` (class), `hookDAGValidatorEngine` (const)

## Key methods
`getCapabilities`, `validate`, `validateManifest`, `defaultValidationPath`, `validateAndWrite`, `loadValidation`, `addEdge`

## Depends on
[[reference_code_engine_baseengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/HookDAGValidatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HookDAGValidatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HookDAGValidatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
