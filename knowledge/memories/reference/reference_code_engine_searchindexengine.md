---
name: reference_code_engine_searchindexengine
description: "Search Index"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SearchIndexEngine.ts
  sourceHash: a5c05f32
  loc: 314
aliases: SearchIndexEngine
---

# SearchIndexEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SearchIndexEngine.ts` (314 LOC)

## What it is
Search Index

```
SearchIndexEngine -- fail-soft HTTP client for Elasticsearch.

Phase 2 of INFRA-SYNERGY-RESEARCH-2026-06-25 (slot:bravo). A durable
lexical/structured search substrate that retires the 512MB V8 string-cap
data-loss class -- the 2026-06-08 tribal-index fail-OPEN clobber
(33,639 -> 1 entries) and the unreadable 548MB system-graph.json. Talks ES
over its REST API via global `fetch` -- NO new npm dependency (mirrors the
transport in GrokClientEngine.ts).

FAIL-SOFT, FAIL-CLOSED CONTRACT (the clobber lesson, directly): a network /
ES failure NEVER throws, NEVER hangs, and NEVER claims a success it did not
VERIFY. In particular index() credits a doc only when ES returns a per-item
result without an `error`; an HTTP-200 bulk reply whose `items` count does
not match the submitted actions (none, fewer, or MORE -- truncated body,
proxy-injected reply, malformed ES response) is treated as an UNVERI
```


## Exports
`SearchBackend` (type), `IndexResult` (interface), `SearchHit` (interface), `QueryResult` (interface), `HealthResult` (interface), `QuerySpec` (interface), `IndexOptions` (interface), `SearchIndexEngine` (class), `searchIndexEngine` (const)

## Key methods
`searchBackend`, `isEsEnabled`, `health`, `index`, `query`, `clearTimeout`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/SearchIndexEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SearchIndexEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SearchIndexEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
