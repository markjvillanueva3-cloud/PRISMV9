---
name: reference_code_engine_millloradeploymentengine
description: "MillLoRADeploymentEngine — LoRA Model Deployment (Mill parity)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MillLoRADeploymentEngine.ts
  sourceHash: 013cc38d
  loc: 426
aliases: millLoRADeploymentEngine
---

# millLoRADeploymentEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MillLoRADeploymentEngine.ts` (426 LOC)

## What it is
```
MillLoRADeploymentEngine — LoRA Model Deployment (Mill parity)
================================================================

Manages MillLoRA model deployment: versioning, rollout strategies,
blue/green, canary, and rollback, with per-axis_mode active models
and mill-canonical shop-floor cell types.

Mill parity for LatheLoRADeploymentEngine (LATHE-LORA-MS0 U-LLR48)
with MILL-CANONICAL extensions:

  - Per-axis_mode active deployment — separate active models for
    3-axis, 4-axis-index, 5-axis-simul, and shared, so promoting a
    5-axis model does NOT supersede the active 3-axis model on a
    mixed-cell target
  - Mill-canonical cell types (shop-floor classification beyond
    dev/staging/prod): 3axis_cell, 4axis_cell, 5axis_cell, mill_turn_cell,
    hsm_cell (high-speed-machining), hmc_cell (horizontal MC),
    production_line, prototype_bench
  - Mill-canonical rollback triggers
```


## Exports
`MillDeploymentStrategy` (type), `MillDeploymentStatus` (type), `MillAxisMode` (type), `MillCellType` (type), `MillRollbackTrigger` (type), `MILL_CANONICAL_ROLLBACK_TRIGGERS` (const), `MillDeploymentTarget` (interface), `MillDeployment` (interface), `MillDeploymentConfig` (interface), `millLoRADeploymentEngine` (const), `MillLoRADeploymentEngine` (re-export)

## Key methods
`setConfig`, `getConfig`, `registerTarget`, `getTargets`, `getTarget`, `deploy`, `beginDeployment`, `activate`, `advanceCanary`, `updateHealth`, `updateMillSignals`, `rollback`, `markFailed`, `getActiveDeployment`, `getActiveDeploymentsByAxis`, `getDeployment`, `getDeploymentsForTarget`, `getHistory`, `getStats`, `getSummary`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MillLoRADeploymentEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find millLoRADeploymentEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MillLoRADeploymentEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
