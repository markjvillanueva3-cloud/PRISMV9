---
name: reference_code_engine_hybridagentdispatchengine
description: "HybridAgentDispatchEngine -- DOMAIN-SOUL-AGENTS / U3."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HybridAgentDispatchEngine.ts
  sourceHash: 8b6f070e
  loc: 152
aliases: HybridAgentDispatchEngine
---

# HybridAgentDispatchEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HybridAgentDispatchEngine.ts` (152 LOC)

## What it is
HybridAgentDispatchEngine -- DOMAIN-SOUL-AGENTS / U3.

```
HybridAgentDispatchEngine -- DOMAIN-SOUL-AGENTS / U3.

Pure decision core: given a domain-soul agent + a task + the live Hermes-proxy
health, decide WHICH lane carries the agent -- the Claude Agent tool (default), the
Hermes free-lane parallel fan-out (when healthy + fan-out-shaped + non-safety), or a
local-Ollama fallback. The operator's "hybrid agent": a domain-soul agent that can run
as a Claude subagent OR be dispatched through the free Hermes lane.

This does NOT replace the Agent tool. It ADVISES which lane to use. A live chat /
Workflow reads the verdict and either spawns the subagent (claude) or routes the
agent's prompt through `ask-hermes` / `HermesAutonomousDriveRunnerEngine` (hermes /
ollama). The agent persona + refuse-list are identical across lanes (same .md soul).

SAFETY (R12, fail-closed): the Hermes/Ollama lanes are free + parallel but UNTRUSTED
for safety-critical wri
```


## Exports
`Lane` (type), `TaskKind` (type), `HybridDispatchRequestSchema` (const), `HybridDispatchRequest` (type), `HybridDispatchVerdict` (interface), `MIN_FANOUT_FOR_HERMES` (const), `HybridAgentDispatchEngine` (class), `hybridAgentDispatchEngine` (const)

## Key methods
`selectLane`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/HybridAgentDispatchEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HybridAgentDispatchEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HybridAgentDispatchEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
