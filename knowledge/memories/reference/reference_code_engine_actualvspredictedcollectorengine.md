---
name: reference_code_engine_actualvspredictedcollectorengine
description: "Neural Training Feedback Collector (U-MIO31A)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ActualVsPredictedCollectorEngine.ts
  sourceHash: 72866488
  loc: 346
aliases: ActualVsPredictedCollectorEngine
---

# ActualVsPredictedCollectorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ActualVsPredictedCollectorEngine.ts` (346 LOC)

## What it is
Neural Training Feedback Collector (U-MIO31A)

```
ActualVsPredictedCollectorEngine — Neural Training Feedback Collector (U-MIO31A)
=================================================================================

Collects post-machining observations (CMM dimensions, measured cutting forces,
tool wear, surface finish, thermal measurements, chatter events) and pairs
them with the orchestrator's predictions to produce neural-training examples.

Key capabilities:
  1. In-memory example buffer with bounded capacity (ring buffer)
  2. JM-DIE-proven program detection → 2× training weight (Weibull-proven priors)
  3. Per-target residual statistics (mean absolute error, bias, variance)
  4. Readiness gate: only emit training batch when buffer has N ≥ min_batch
  5. Export to neural-model adapter format expected by MillingInferenceOrchestrator

This closes the feedback loop between quality metrology (U-MIO31) and neural
predictors. Without it, p
```


## Exports
`NeuralTarget` (type), `ObservationInput` (interface), `TrainingExample` (interface), `ResidualStats` (interface), `CollectorBatch` (interface), `CollectorConfig` (interface), `ActualVsPredictedCollectorEngine` (class), `actualVsPredictedCollectorEngine` (const)

## Key methods
`getConfig`, `record`, `recordBatch`, `getResidualStats`, `getAllResidualStats`, `emitTrainingBatch`, `clear`, `accuracyTrend`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ActualVsPredictedCollectorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ActualVsPredictedCollectorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ActualVsPredictedCollectorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
