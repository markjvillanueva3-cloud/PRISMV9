---
name: reference_code_engine_turningcpksurrogateengine
description: "TurningCpkSurrogateEngine — LATHE-PRO-MS5.4 ML surrogate."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/TurningCpkSurrogateEngine.ts
  sourceHash: eeee4d2d
  loc: 408
aliases: turningCpkSurrogateEngine
---

# turningCpkSurrogateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/TurningCpkSurrogateEngine.ts` (408 LOC)

## What it is
```
TurningCpkSurrogateEngine — LATHE-PRO-MS5.4 ML surrogate.

Learns a closed-form polynomial regression surrogate for
  Cpk ≈ f(Vc, f, ap, vb_failure, approach_deg, probe_interval)
from cascade evaluations (MS1 insert life + MS2 offset compensation).

Purpose:
  - Replace the expensive MC-in-grid inner loop of the robust optimizer
    at probe points with a O(k²) regression forward pass.
  - Provide a fast first-guess for UI "what-if" queries.
  - Serve as a sanity check: if the surrogate's R² is low, the cascade
    response surface is non-smooth / contains phase transitions, which
    is itself actionable (narrow the search, add samples).

Math:
  Feature vector φ(x) = [1, x₁, x₂, …, xₖ, x₁², x₂², …, xₖ²,
                         x₁x₂, x₁x₃, …, xₖ₋₁xₖ]  (degree ≤ 2)
  Fit β = (ΦᵀΦ + λI)⁻¹ Φᵀy    (ridge-regularised normal equations)
  Predict ŷ = φ(x_new) · β

Why degree-2 + small ridge:

```


## Exports
`SurrogateFeatureName` (type), `SurrogateSample` (interface), `FitInput` (interface), `FitResult` (interface), `PredictInput` (interface), `PredictResult` (interface), `CrossValidateInput` (interface), `CrossValidateResult` (interface), `turningCpkSurrogateEngine` (const), `TurningCpkSurrogateEngine` (re-export)

## Key methods
`fit`, `predict`, `crossValidate`, `featureOrder`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/TurningCpkSurrogateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find turningCpkSurrogateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/TurningCpkSurrogateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
