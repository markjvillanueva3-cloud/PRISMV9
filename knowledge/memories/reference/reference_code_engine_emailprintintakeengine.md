---
name: reference_code_engine_emailprintintakeengine
description: "EmailPrintIntakeEngine — weekly (Tuesday) PDF + print extraction from each"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/EmailPrintIntakeEngine.ts
  sourceHash: db4541a7
  loc: 440
aliases: InMemoryDedupLedger
---

# InMemoryDedupLedger -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/EmailPrintIntakeEngine.ts` (440 LOC)

## What it is
```
EmailPrintIntakeEngine — weekly (Tuesday) PDF + print extraction from each
JM Die team member's email, so the shop gets more accurate prints than the
mid-week ad-hoc forwards (hotel iter24, operator directive 2026-05-24).

Operator directive verbatim:
  "we need the ability for you to extract pdfs and prints from each persons
   email on tuesday so we get more accurate prints"

Architecture:
  - Pure-core engine — all I/O routed through an injected ImapAdapter so
    tests pass fake fixtures and production wires a real IMAP client.
  - Per-user InboxConfig (env-var ref for password — NEVER stored verbatim).
  - Dedup ledger by message_id (cross-run) + by SHA-256 (cross-message).
  - Tuesday-day-of-week predicate (ISO weekday=2). Cron in scheduled-task
    layer; engine exposes shouldRunOnDate() helper so the cron can verify.
  - Whitelist of print-bearing extensions: pdf, png, jpg, jpeg,
```


## Exports
`TUESDAY_ISO` (const), `PRINT_EXTENSIONS` (const), `PrintExtension` (type), `InboxConfig` (interface), `RawAttachment` (interface), `ExtractedArtifact` (interface), `BatchRunResult` (interface), `ImapAdapter` (interface), `ArtifactSink` (interface), `DedupLedger` (interface), `Clock` (interface), `systemClock` (const), `InMemoryDedupLedger` (class), `InMemoryArtifactSink` (class), `isoWeekday` (function), `shouldRunOnDate` (function), `sha256Hex` (function), `extensionOf` (function), `isPictureExtension` (function), `buildOutputPath` (function)

## Key methods
`fetchPrintAttachments`, `persist`, `hasMessage`, `hasSha`, `recordExtracted`, `now`, `size`, `registerInbox`, `listInboxes`, `getInbox`, `shouldRunNow`, `runBatch`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/EmailPrintIntakeEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find InMemoryDedupLedger` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/EmailPrintIntakeEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
