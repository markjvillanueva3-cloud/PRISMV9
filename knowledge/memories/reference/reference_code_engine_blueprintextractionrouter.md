---
name: reference_code_engine_blueprintextractionrouter
description: "blueprintExtractionRouter -- the executable 'apply this extraction to ALL prism features' backbone."
metadata:
  type: reference
  kind: code-engine
  galaxy: blueprint-vision
  sourcePath: mcp-server/src/engines/blueprint-vision/blueprintExtractionRouter.ts
  sourceHash: caa40bb5
  loc: 665
aliases: blueprintExtractionRouter.ts
---

# blueprintExtractionRouter.ts -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/blueprint-vision/blueprintExtractionRouter.ts` (665 LOC)

## What it is
```
blueprintExtractionRouter -- the executable "apply this extraction to ALL prism features" backbone.

WHY (blueprint-vision-app-integration-plan-2026-06-23): xray owns a versioned, mm-canonical
`BlueprintExtractionContract` (the stable shape app consumers bind to). The contract NORMALIZES a
single part's extraction, but nothing turned it into ACTION: "this part has dims + GD&T + a customer
title-block -> it can feed a quote, a print-to-program, an inspection plan, AND it carries PII to
redact." This module is that missing fan-out: given ONE validated contract it deterministically
decides WHICH downstream prism feature each extraction can drive, computes the per-consumer call
payload, and -- for the COMMITMENT consumers (quote = money, program = machine motion, inspection =
acceptance) -- gates them behind the operator-confirm of any below-floor field. The app's
upload->extract->route flow
```


## Exports
`BLUEPRINT_ROUTING_PLAN_VERSION` (const), `ConsumerKind` (type), `ConsumerRoute` (interface), `ExtractionRoutingPlan` (interface), `RouteExtractionOpts` (interface), `routeExtractionToConsumers` (function)

## Key methods
`nNeedsConfirm`

## Depends on
[[reference_code_schema_blueprintextractioncontract]]

## Where it lives
- **Source:** `mcp-server/src/engines/blueprint-vision/blueprintExtractionRouter.ts`
- **Galaxy:** **blueprint-vision**
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find blueprintExtractionRouter.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/blueprint-vision/blueprintExtractionRouter.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
