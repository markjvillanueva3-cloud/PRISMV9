---
name: reference_code_engine_groundtruthregistryengine
description: "indexed, queryable corpus over the"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/GroundTruthRegistryEngine.ts
  sourceHash: 141da9ad
  loc: 1192
aliases: GroundTruthRegistryEngine
---

# GroundTruthRegistryEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/GroundTruthRegistryEngine.ts` (1192 LOC)

## What it is
indexed, queryable corpus over the

```
GroundTruthRegistryEngine — indexed, queryable corpus over the
ground-truth bundles produced by GroundTruthBatchExtractor (U-CGT07).

Builds five compound indexes from a tree of bundle.json manifests:
  - byFileId          (primary key — exact match)
  - byCustomerLower   (case-insensitive customer name)
  - byFormat          (extension, e.g. ".sldprt")
  - byMachineCategory ("mill" | "lathe" | "edm" | "wedm" | "grinder" | …)
  - byComplexityTier  ("simple" | "medium" | "complex")

Customer + machine category are inferred from the JM Die path convention:
  H:\PRISM\JM DIE\CNC LATHE\ALCOA\…       → customer=ALCOA, machine=lathe
  H:\PRISM\JM DIE\CNC MILL\ITW\…          → customer=ITW,   machine=mill
  H:\PRISM\JM DIE\WIRE EDM\OPTIMAS\…      → customer=OPTIMAS, machine=wedm
Bundles outside that convention land in customer="UNKNOWN", machine="other".

Complexity tier is derived from the fea
```


## Exports
`MACHINE_CATEGORIES` (const), `MachineCategory` (type), `COMPLEXITY_TIERS` (const), `ComplexityTier` (type), `RegistryEntrySchema` (const), `RegistryEntry` (type), `RegistryStatsSchema` (const), `RegistryStats` (type), `RegistryManifestSchema` (const), `RegistryManifest` (type), `inferCustomerAndMachine` (function), `inferComplexity` (function), `CONFIDENCE_TIERS` (const), `ConfidenceTier` (type), `SOURCE_PROVENANCES` (const), `SourceProvenance` (type), `BlueprintExtractionRecordSchema` (const), `BlueprintExtractionRecord` (type), `TrainingPairSchema` (const), `TrainingPair` (type)

## Key methods
`buildIndex`, `findByFileId`, `findByCustomer`, `findByMachineCategory`, `findByComplexity`, `findByFormat`, `query`, `getStats`, `size`, `dumpManifest`, `loadManifest`, `validate`, `registerBlueprintExtraction`, `pushTo`, `joinDocustrataToPartLibrary`, `enumerateByConfidenceTier`, `flagAmbiguities`, `getTrainingPairsByCustomer`, `blueprintState`, `resetBlueprintState`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/GroundTruthRegistryEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find GroundTruthRegistryEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/GroundTruthRegistryEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
