---
name: reference_hermes_ollama_parity_2026_06_17
description: Hermes offload lane brought to FULL parity with Ollama (2026-06-17, slot:alpha) -- ask-hermes safety/robustness + viz/rerank modes + 12 cloned /hermes-* skills + offload-routing parity verified; how the two lanes now mirror, and the one intentional asymmetry
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.606Z
aliases: reference_hermes_ollama_parity_2026_06_17
---


U-HERMES-OLLAMA-PARITY (slot:alpha, 2026-06-17, operator "sync hermes to how we have ollama setup for claude code offloading so they match" -> chosen scope "Full parity (clone the skills too)"). Shipped in 4 dependency-ordered layers on `cad-fusion-live-ms0` (all `[MAIN-FORCE]` -- shared fleet offload infra; ask-hermes.mjs + ask-ollama.mjs co-live there, NOT slot/alpha):

- **L1a `328b66a52b`** -- ask-hermes safety/robustness parity. THE critical pre-fix gap: `scripts/ask-hermes.mjs` had NO NC/G-code guard, NO file cap, NO timeout scaling -- it would ship a dense G-code program UNCAPPED to the PAID Grok proxy. Fix: DRY-import ask-ollama's CANONICAL `looksLikeNcProgram`/`MAX_FILE_BYTES`/`scaleTimeoutForBytes`/`readFileCapped`/`readStdin` (no fork) + new exported pure helpers `resolveInput`(async,DI)/`shouldRefuseUnsafe`/`effectiveTimeout`. File modes refuse dense G-code unless `--allow-unsafe`; size-scaled timeout; truncation note; lenient-missing-path now emits an R12 advisory.
- **L1b `fc69b05b6d`** -- viz+rerank graph modes (the last CAPABILITY gap). DRY-import ask-ollama's `loadGraph`/`searchGraph`/`renderHits`/`buildVizPrompt` + the shared `lib/ollama-search-rerank` (buildRerankPrompt/rerankCandidates). New exported `runGraphMode` (callModel+loadGraphImpl injectable): viz=$0 LOCAL search, `--synth`/rerank touch Grok and degrade to local-hits/lexical on failure. JSON `source` reports honest backend (hermes/lexical, never the lib's "ollama"). LIVE: `viz "cutting force kienzle"` scanned 64,808 real graph nodes -> kienzle + cutting-force hits, $0.
- **L2 `9d11596a08`** -- the 12 `/hermes-*` task skills (1:1 with `/ollama-*`) via a tracked idempotent GENERATOR `scripts/generate-hermes-skills.mjs` (canonical+generate pattern, like generate-per-slot-wrappers). The skills (`.claude/commands/hermes-*.md`) are gitignored like the ollama-* family -- the generator is the durable source (survives a fresh checkout; `node scripts/generate-hermes-skills.mjs`, idempotent `--check`=0 drift). Every skill calls the CANONICAL ask-hermes.mjs (NOT the legacy raw-curl/:11434/:7b pattern), is HONEST it is PAID + points at its free `/ollama-<sib>` sibling. bridge=pointer to /hermes-workflow (no harness fork); route-check=health probe (proxy + byHook ledger). LIVE: route-check ledger shows **851 real ask-hermes answers** (lane genuinely exercised).
- **L3 `3fbb4a7d56`** -- offload-routing parity. VERIFIED already satisfied: `scripts/lib/task-substrate-router.mjs` IS the Hermes-aware router (dedicated `hermes` substrate row gated by `shouldUseWorkflow` + ask-hermes in the ollama lane). Two other routers correctly stay Hermes-FREE for principled reasons (R7): `ollama-task-offloader.mjs` Ollama-down ladder goes to CHEAP Claude (paid-Hermes-by-default would burn Grok money on every mechanical task); `local-llm-task-router.mjs` is LOCAL-ONLY by charter (invariant #3 IP-STAYS-LOCAL + [[reference_hermes_router_u1_2026_06_04]]) -- a doc note records the decision so it is not re-opened as a false gap. Reviewer enumerated all 22 *router*.mjs: no offload-home left un-wired.

**RESULT:** ask-hermes now supports ALL 6 ask-ollama modes (ask/summarize/explain/triage/viz/rerank) PLUS classify -- a SUPERSET. The directional goal (HERMES matches OLLAMA) is 100% met. **One intentional asymmetry (R12):** ask-ollama LACKS `classify` (the REVERSE direction = "sync Ollama to Hermes", not what was asked) -- deferred, noted. **Cost-design law throughout:** Hermes is a PAID 1M-ctx remote (xai Grok via the :8645 OAuth proxy), the ESCALATION tier above free local Ollama -- never a $0 cheapest-qualified default. Tests: ask-hermes 57/57 + generate-hermes-skills 14/14 + routers 20/20+9/9. Per-file 2-arm scrutiny PASS on every layer. Related: [[reference_hermes_bridge_operational_2026_06_17]] (the live bridge) · [[reference_hermes_router_u1_2026_06_04]] (the local-only router charter) · [[feedback_ollama_fallback_sonnet_agents]].
