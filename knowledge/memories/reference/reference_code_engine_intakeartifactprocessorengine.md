---
name: reference_code_engine_intakeartifactprocessorengine
description: "IntakeArtifactProcessorEngine — auto-populates tooling + inventory + parts"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/IntakeArtifactProcessorEngine.ts
  sourceHash: 25a4f0a8
  loc: 411
aliases: InMemoryToolingSink
---

# InMemoryToolingSink -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/IntakeArtifactProcessorEngine.ts` (411 LOC)

## What it is
```
IntakeArtifactProcessorEngine — auto-populates tooling + inventory + parts
from PDFs extracted by EmailPrintIntakeEngine (hotel iter25).

Operator directive (2026-05-25): "make sure pdf intake features are active
in the app for auto populating tooling and inventory and properly
synergized to the whole app and all features"

Pure-core processor — text extraction via injected PdfTextExtractor; routes
parsed entities to ToolingSink / InventorySink / PartSink with confidence
filter. Below-threshold matches → ReviewQueueSink instead of silent write.

Hotel-soul invariants:
  - Never auto-routes below confidence floor (R12 fail-LOUD)
  - Never overwrites — only proposes (sinks decide conflict policy)
  - PII redaction on every raw_text excerpt (phones, SSNs, emails)
  - Per-artifact audit history kept on engine for review
```


## Exports
`INVENTORY_CONFIDENCE_FLOOR` (const), `TOOLING_CONFIDENCE_FLOOR` (const), `PART_CONFIDENCE_FLOOR` (const), `MAX_PDF_TEXT_BYTES` (const), `ParsedTool` (interface), `ParsedInventoryItem` (interface), `ParsedPart` (interface), `ProcessedResult` (interface), `PdfTextExtractor` (interface), `ToolingSink` (interface), `InventorySink` (interface), `PartSink` (interface), `ReviewQueueSink` (interface), `InMemoryToolingSink` (class), `InMemoryInventorySink` (class), `InMemoryPartSink` (class), `InMemoryReviewQueue` (class), `redactPII` (function), `parseTools` (function), `parseInventoryItems` (function)

## Key methods
`extract`, `proposeTool`, `proposeInventoryItem`, `proposePart`, `queue`, `process`, `history_get`, `history_clear`, `history_summary`

## Depends on
[[reference_code_engine_emailprintintakeengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/IntakeArtifactProcessorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find InMemoryToolingSink` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/IntakeArtifactProcessorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
