---
name: reference_code_engine_worksurfacescaffoldengine
description: "work-surface)."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WorkSurfaceScaffoldEngine.ts
  sourceHash: 7401b01f
  loc: 192
aliases: WorkSurfaceScaffoldEngine
---

# WorkSurfaceScaffoldEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WorkSurfaceScaffoldEngine.ts` (192 LOC)

## What it is
work-surface).

```
WorkSurfaceScaffoldEngine — U-HAGI06 PrismApp web work-surface scaffold
(Voxyz L1 — work-surface).

Pure-core generator: emits a serializable manifest describing the React
work-surface for an operator role — routes, pages, panels, gate-injection
points — that a Vite+React shell consumes to render the actual UI.  No
I/O, no rendering.  The Vite+React shell lives downstream and is
intentionally out of this engine's scope (kept the engine pure-core +
fast to test).

The scaffold composes 4 other HAGI surfaces:
  - U-HAGI02 UnifiedControlPlane → per-route 4-gate check
  - U-HAGI05 BatchDeliverable     → batch-page panels
  - U-HAGI01 DurableWorkflow      → workflow-status panels
  - U-HAGI08 SourceChain          → citation footer

@module engines/WorkSurfaceScaffoldEngine
```


## Exports
`PanelKindSchema` (const), `PanelKind` (type), `RouteSchema` (const), `Route` (type), `WorkSurfaceManifestSchema` (const), `WorkSurfaceManifest` (type), `OperatorRoleSchema` (const), `OperatorRole` (type), `WorkSurfaceScaffoldEngine` (class), `workSurfaceScaffoldEngine` (const)

## Key methods
`validateManifest`, `validateRoute`, `manifestForRoles`, `routeAt`, `filterByRole`, `renderManifest`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WorkSurfaceScaffoldEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WorkSurfaceScaffoldEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WorkSurfaceScaffoldEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
