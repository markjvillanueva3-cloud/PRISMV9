---
name: reference_code_engine_autocadaddinpluginengine
description: "U-CAD-APP-12 (PHASE-48)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/AutoCADAddinPluginEngine.ts
  sourceHash: 5358b462
  loc: 456
aliases: AutoCADAddinPluginEngine
---

# AutoCADAddinPluginEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/AutoCADAddinPluginEngine.ts` (456 LOC)

## What it is
U-CAD-APP-12 (PHASE-48)

```
AutoCADAddinPluginEngine — U-CAD-APP-12 (PHASE-48)

AutoCAD add-in plugin manager for CUI ribbon integration and
AutoLISP reactors wired to PRISM's event bus.

Features:
  - Ribbon tab/panel/button registration
  - Command binding with PRISM dispatcher integration
  - AutoLISP reactor management (document, editor, database)
  - Event subscription and routing to PRISM sessions
  - Plugin state persistence
  - Command palette integration

@module engines/AutoCADAddinPluginEngine
```


## Exports
`ACAD_REACTOR_TYPES` (const), `AcadReactorType` (type), `RibbonButtonSchema` (const), `RibbonButton` (type), `RibbonPanelSchema` (const), `RibbonPanel` (type), `RibbonTabSchema` (const), `RibbonTab` (type), `CommandBindingSchema` (const), `CommandBinding` (type), `ReactorBindingSchema` (const), `ReactorBinding` (type), `ReactorEventSchema` (const), `ReactorEvent` (type), `PluginStateSchema` (const), `PluginState` (type), `AcadPluginClock` (interface), `AcadPluginTransport` (interface), `AutoCADAddinPluginEngine` (class)

## Key methods
`now`, `registerRibbonTab`, `unregisterRibbonTab`, `registerCommand`, `unregisterCommand`, `registerReactor`, `unregisterReactor`, `getPluginState`, `getRibbonTab`, `listRibbonTabs`, `updateRibbonButton`, `setRibbonTabVisibility`, `getCommand`, `listCommands`, `getReactor`, `listReactors`, `setReactorEnabled`, `updateReactorFilter`, `bindReactorToSessions`, `handleReactorEvent`, `sub`, `subscribeToEvents`, `getEventLog`, `clearEventLog`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/AutoCADAddinPluginEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find AutoCADAddinPluginEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/AutoCADAddinPluginEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
