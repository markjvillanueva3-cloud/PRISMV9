---
name: reference_code_engine_jmdiescanledgerengine
description: "JM-DIE-FLEET-SCAN-MS0 / U-FS01"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/JMDieScanLedgerEngine.ts
  sourceHash: 93289c8b
  loc: 242
aliases: JMDieScanLedgerEngine
---

# JMDieScanLedgerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/JMDieScanLedgerEngine.ts` (242 LOC)

## What it is
JM-DIE-FLEET-SCAN-MS0 / U-FS01

```
JMDieScanLedgerEngine — JM-DIE-FLEET-SCAN-MS0 / U-FS01

Append-only JSONL ledger that tracks which files in the JM Die archive have
already been scanned. Lets the coordinator skip re-work across sessions +
across parallel agent batches. Idempotent: appending the same abs_path twice
is harmless (loadScanned() de-duplicates identical strings via Set -- but NOT
across path FORMATS; fold through canonScanPath before any membership test).

Ledger row schema (JSONL):
  {
    abs_path: string,        // absolute path AS WRITTEN by the producer (format varies)
    sha_short?: string,      // first 8 hex of sha256(RAW abs_path) -- NOT a canonical key:
                             // it splits across path formats; use canonScanPath for dedup
    size_bytes: number,
    mtime_iso: string,
    scanned_at: string,      // ISO 8601
    source: "financial-baseline" | "program-analysis" | "fleet-scan-ba
```


## Exports
`LedgerSource` (type), `LedgerKind` (type), `LedgerRow` (interface), `AppendInput` (interface), `LedgerStats` (interface), `canonScanPath` (function), `JMDieScanLedgerEngine` (class), `jmDieScanLedgerEngine` (const)

## Key methods
`getPath`, `appendBatch`, `loadScanned`, `readAllRows`, `stats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/JMDieScanLedgerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find JMDieScanLedgerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/JMDieScanLedgerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
