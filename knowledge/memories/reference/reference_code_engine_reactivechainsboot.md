---
name: reference_code_engine_reactivechainsboot
description: "reactive-chains-boot.ts -- the canonical (and previously MISSING) boot site for"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/reactive-chains-boot.ts
  sourceHash: 3f8350ef
  loc: 93
aliases: reactive-chains-boot.ts
---

# reactive-chains-boot.ts -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/reactive-chains-boot.ts` (93 LOC)

## What it is
```
reactive-chains-boot.ts -- the canonical (and previously MISSING) boot site for
the EventBus reactive-chain subsystem. (BACKEND-COMPLETION / U-REACTIVE-CHAINS-BOOT)

THE GAP (verified 2026-06-18, slot:zulu). Two modules register EventBus reactive
chains + action handlers as a MODULE-LOAD SIDE EFFECT (top-level
`eventBus.registerReactiveChain(...)` / `eventBus.registerAction(...)`):
  - `reactiveChainBootstrap.ts`  -- 9 chains (job_failure_forensics, ERP job_to_invoice, ...)
  - `cycleSchedulingBridge.ts`   -- 3 chains + 4 actions (INTEG-MS3 CycleTime->Capacity->Scheduling)
But NOTHING imports either module at runtime (0 importers across the server; `index.ts`
loads engines via explicit `await import(...)` and never names these). So the chains
never register in production -- the subsystem is built + tested but DORMANT. This module
is the missing boot site that `index.ts` calls once after 
```


## Exports
`REACTIVE_CHAINS_ENV` (const), `REGISTRATION_MODULES` (const), `reactiveChainsEnabled` (function), `ReactiveChainsBootResult` (interface), `bootReactiveChains` (function)

## Key methods
_(none extracted)_

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/reactive-chains-boot.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find reactive-chains-boot.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/reactive-chains-boot.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
