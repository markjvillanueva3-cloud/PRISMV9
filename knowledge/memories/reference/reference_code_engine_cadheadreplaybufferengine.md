---
name: reference_code_engine_cadheadreplaybufferengine
description: "U-CADC-LP03 / CAD-COMPLETE-MS0"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADHeadReplayBufferEngine.ts
  sourceHash: 0887621c
  loc: 362
aliases: CADHeadReplayBufferEngine
---

# CADHeadReplayBufferEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADHeadReplayBufferEngine.ts` (362 LOC)

## What it is
U-CADC-LP03 / CAD-COMPLETE-MS0

```
CADHeadReplayBufferEngine — U-CADC-LP03 / CAD-COMPLETE-MS0

Per-NN-head Prioritized Experience Replay (PER) buffer for the CAD
closed-loop learner. Each CAD system ("NN head") gets its own bounded,
priority-weighted buffer of FeedbackSamples (produced by LP02). LP04
draws prioritized minibatches from here to back-propagate.

Reference: Schaul et al. 2015, "Prioritized Experience Replay" (ICLR 2016).
  - sampling probability  P(i) = p_i^α / Σ_k p_k^α
  - importance weight     w_i = (N · P(i))^(−β),  normalised by max_j w_j
Priorities are stored RAW (p_i = |TD-error| + ε); the α exponent is
applied at sample() time so α stays tunable without rescoring the buffer.

Duplication-guard note (checked against PrioritizedReplayBufferEngine,
U-LEARN-10): that engine implements generic PER but (a) samples with
Math.random() — it CANNOT produce the reproducible seeded replay that is
this unit's stat
```


## Exports
`ReplayEntry` (interface), `ReplayBatch` (interface), `PerHeadReplayCounter` (interface), `ReplayBufferStats` (interface), `CADHeadReplayBufferEngine` (class), `cadHeadReplayBufferEngine` (const)

## Key methods
`getSeed`, `add`, `sample`, `updatePriorities`, `getEntries`, `size`, `listHeads`, `getStats`, `reset`

## Depends on
[[reference_code_engine_cadperadapterfeedbackcollectorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADHeadReplayBufferEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADHeadReplayBufferEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADHeadReplayBufferEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
