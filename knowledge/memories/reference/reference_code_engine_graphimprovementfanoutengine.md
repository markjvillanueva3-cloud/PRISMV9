---
name: reference_code_engine_graphimprovementfanoutengine
description: "GraphImprovementFanoutEngine -- U-ALPHA-HERMES-GRAPH-IMPROVE (slot:alpha)."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/GraphImprovementFanoutEngine.ts
  sourceHash: 4cafa9fe
  loc: 279
aliases: GraphImprovementFanoutEngine
---

# GraphImprovementFanoutEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/GraphImprovementFanoutEngine.ts` (279 LOC)

## What it is
GraphImprovementFanoutEngine -- U-ALPHA-HERMES-GRAPH-IMPROVE (slot:alpha).

```
GraphImprovementFanoutEngine -- U-ALPHA-HERMES-GRAPH-IMPROVE (slot:alpha).

The PURE planning core of the parallel opus-fast-max graph-improvement loop. Given
the system-viz leverage wiring queue (each entry = a domain bucket of unwired
engines = missing engine->dispatcher edges in the graph), it:
  1. decomposes the queue into parallelizable graph-improvement subtasks
     (highest-leverage first),
  2. sizes the OPUS-FAST-MAX fan-out (OpusFastMaxAgentSpecEngine.planParallelism),
  3. assigns subtasks -> slots (HermesParallelFanoutPlannerEngine.plan), and
  4. attaches the opus-fast-max AgentSpec to each, producing an executable agent batch.

PURE (no I/O, no agent-spawning) so it is testable AND dispatcher-wireable: the
cron driver (scripts/hermes-graph-improvement-driver.mts) supplies the queue it
read from disk; the hermesDispatcher action `hermes_graph_improve_plan` supplies a
queue
```


## Exports
`WiringQueueEntrySchema` (const), `WiringQueueEntry` (type), `GraphImprovementRequestSchema` (const), `GraphImprovementRequest` (type), `GraphImprovementPlan` (interface), `DOMAIN_SLOT_MAP` (const), `FALLBACK_SLOTS` (const), `GraphImprovementFanoutEngine` (class), `graphImprovementFanoutEngine` (const)

## Key methods
`sizeHintForUnwired`, `normalizeDomain`, `String`, `queueToSubtasks`, `buildCandidates`, `plan`, `renderBatch`

## Depends on
[[reference_code_engine_opusfastmaxagentspecengine]] | [[reference_code_engine_hermesparallelfanoutplannerengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/GraphImprovementFanoutEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find GraphImprovementFanoutEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/GraphImprovementFanoutEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
