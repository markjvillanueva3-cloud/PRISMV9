---
name: reference_code_engine_latencybudgetdecompositionengine
description: "LatencyBudgetDecompositionEngine — U-LPR-PERF-SLO"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LatencyBudgetDecompositionEngine.ts
  sourceHash: 040ece06
  loc: 496
aliases: latencyBudgetDecompositionEngine
---

# latencyBudgetDecompositionEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LatencyBudgetDecompositionEngine.ts` (496 LOC)

## What it is
```
LatencyBudgetDecompositionEngine — U-LPR-PERF-SLO

End-to-end latency SLO decomposition: splits the 2.5s p95 total budget
across 7 pipeline stages, records per-stage observed latencies, computes
per-stage p50/p95/p99 via R-7 (Hyndman-Fan Type 7) quantile, and detects
per-stage and aggregate breaches. Implements fast-burn (2 %/hr) and
slow-burn (10 %/6hr) error-budget alert criteria per Google SRE workbook.

Stage budgets (p95 envelope, per roadmap Perf B1 + B2):
  OCR                   ≤300 ms
  CAD feature extract   ≤200 ms
  Tribal vector-kNN     ≤ 80 ms
  LoRA inference        ≤1500 ms (TTFT ≤400 ms if streaming)
  Physics validation    ≤150 ms
  G-code + dialect      ≤200 ms
  Sim gateway enqueue   ≤ 50 ms
  ───────────────────────────────
  Sum                   ≤2480 ms (500 ms slack vs 2.5s target)

LoRA alt-deployment profile (GPU server async queue) relaxes the LoRA
budget to p9
```


## Exports
`StageId` (type), `DeploymentProfile` (type), `BurnSeverity` (type), `StageBudget` (interface), `StageObservation` (interface), `StageStats` (interface), `EndToEndStats` (interface), `BurnRateAlert` (interface), `BudgetComplianceReport` (interface), `LatencyBudgetStats` (interface), `latencyBudgetDecompositionEngine` (const), `LatencyBudgetDecompositionEngine` (re-export)

## Key methods
`setBudget`, `getBudget`, `listBudgets`, `aggregateBudget`, `validateProfileBudget`, `recordObservation`, `listObservations`, `stageStats`, `endToEndStats`, `computeBurnRate`, `generateComplianceReport`, `getStats`, `clearAll`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/LatencyBudgetDecompositionEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find latencyBudgetDecompositionEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LatencyBudgetDecompositionEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
