---
name: reference_code_engine_safetyescalationengine
description: "Safety Escalation"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SafetyEscalationEngine.ts
  sourceHash: b9ca62bc
  loc: 500
aliases: SafetyEscalationEngine
---

# SafetyEscalationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SafetyEscalationEngine.ts` (500 LOC)

## What it is
Safety Escalation

```
SafetyEscalationEngine — CAMX-MS14/U03 (E1138)

When safety constraints are tight (veto or near-veto), automatically
escalates through progressively conservative parameter adjustments:
  1. Reduce ap by 20% (depth of cut — primary force reducer)
  2. Reduce fz by 15% (feed per tooth — secondary force reducer)
  3. Switch to lower-engagement strategy (e.g., trochoidal, adaptive)
  4. Add spring passes (light finishing pass to relieve residual stress)
  5. Suggest fixture redesign (when workholding is the bottleneck)

Iterates up to max_iterations (default 3) until all vetoes are resolved
or no further escalation is possible.

Physics basis:
  - Kienzle (1952): Fc = kc1_1 * h^(1-mc) * ap * ae
    Reducing ap → proportional force reduction
    Reducing fz → sub-linear force reduction (exponent 1-mc, mc~0.25)
  - Power: P = Fc * Vc / (60000 * eta)
  - Deflection: delta = Fc * L^3 / (3*E*I) —
```


## Exports
`VetoRuleType` (type), `EscalationVetoReport` (interface), `EscalationParams` (interface), `EscalationMachineLimits` (interface), `EscalationAction` (interface), `EscalationIteration` (interface), `EscalationResult` (interface), `SafetyEscalationEngine` (class), `safetyEscalationEngine` (const)

## Key methods
`escalate`, `preview`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/SafetyEscalationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SafetyEscalationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SafetyEscalationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
