---
name: reference_delta_closed_loop_training_live_2026_06_02
description: "PRISM's closed-loop CAD learning cycle runs end-to-end live in Fusion — build→probe→diff→propose→apply→verify→retrain, with honest R12 no-fake-gain behavior"
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.546Z
aliases: reference_delta_closed_loop_training_live_2026_06_02
---


**Closed-loop CAD training — VALIDATED LIVE in Fusion (2026-06-02, slot:delta).** The full self-improving cycle in `scripts/cad-fusion-correction-loop-live.mjs` runs end-to-end on the real `:18365` bridge: build a part proxy (revolve/extrude) → `probeFaceGeometry` → `diffXrayPrints` vs xray ground truth → `proposeFeatureCorrections` → plan (radial-hole / axial-bore) → APPLY via `/execute` participantBodies cut → re-probe → `verifyCorrectionApplied` → re-diff → append dual-training data to the compounding `state/shared/cad-fix-training-ledger.jsonl`.

**FOUR part classes now close to 100% via the closed loop (commit `8acf03b236`, re-verified honest):**
- **die**: 60% (3/5) → **100%** — `bevel_face_chamfer`✓ + `cross_drilled_relief_holes`✓, verifiedCount=2.
- **casing**: 75% (3/4) → **100%** — `bevel_face_chamfer`✓, verifiedCount=1.
- **bushing**: **100%** baseline — no gaps, honest no-op (verifiedCount=0).
- **extrude_punch**: 50% (2/4) → **100%** — `cross_drilled_relief_holes`✓ + `shoulder_fillet`✓ (fillet mechanic), verifiedCount=2.

**R12 bug found + fixed mid-build (the headline lesson):** extrude_punch first reported `shoulder_fillet✗ (apply not-ok)` YET final diff credited it at a FAKE 100%. Two root causes: (1) Fusion keeps a FAILED-to-compute chamfer/fillet feature in the timeline and STILL increments `.count` → an errored feature falsely read as present. FIX: `probeEdgeFeaturesCode` + `applyChamferCode`/`applyFilletCode` now gate on `healthState==HealthyFeatureHealthState` (count/credit ONLY healthy features). (2) the edge-feature apply ok-flag false-NEGATIVES (reading a fresh feature's healthState right after `add()` throws before recompute settles) → the orchestrator edge branch no longer early-`continue`s on `ok:false`; the RE-PROBE (healthy-count delta) is the single source of truth, the SAME health-filtered signal the final diff credits, so verify and final-credit can never disagree. After the fix, extrude_punch's fillet shows `✓ verifiedCount=2` — the 100% is real and consistent. Lesson: a "feature count" is not a "feature present" — filter by health; and an apply's ok-flag is advisory, the re-probe is truth.

**Prismatic parts (commit pending — live-verified):** **bracket 0% → 100%** — `central_oil_hole`✓ via the axial-bore mechanic on an extruded SLAB (verifiedCount=1). This proves the prismatic/slab closed-loop path end-to-end → **FIVE part classes now close to 100%** (die, casing, bushing, extrude_punch, bracket).

**KNOWN BLOCKER — plate ground truth is CORRUPT (xray's domain, do NOT silently rewrite).** `state/shared/ocr-ground-truth/cad-prototype-plate-2026-05-19.json` has `part_class:"plate"` but lists 9 features that are a TURBINE BLADE, not a flat plate: `stepped_revolved_axis` + `working_tip_taper` (rotational — impossible on a slab) and `blade_root_fillet`/`leading_edge_fillet`/`trailing_edge_fillet` (turbine-blade fillets). plate runs honest at **33.3%** — it verifies the 3 applicable corrections (central_oil_hole✓, bevel_face_chamfer✓, shoulder_fillet✓, vc=3) and is correctly blocked on the rest (add-step/taper-tip have no live apply-mechanic — and post-hoc-adding a revolve step/taper to a finished body is genuinely hard; the 3 named blade fillets have no correction rule). FIX is cross-domain: either correct the plate fixture to realistic plate features (central hole + mounting holes + edge chamfers/fillets) OR rename it to "blade" (its features ARE a blade). Surface to xray (OCR ground-truth owner); delta does not overwrite shared xray fixtures. SECONDARY in-domain gap: distinguishing fillet VARIANTS (shoulder vs blade-root vs leading/trailing-edge) needs fillet-LOCATION classification — the current probe only counts total `filletFeatures.count`, so it can credit only ONE fillet functional name; adding named-fillet rules WITHOUT per-variant crediting would re-create the verify/credit asymmetry just fixed, so that is a future unit (fillet-location classifier), not a quick rule add.

**Honesty guard (R12) confirmed working:** the loop only counts a gain when a correction is re-probe-VERIFIED; `unverifiedGain` (scoreDelta>0 with verifiedCount=0) would fall back `trustworthyScorePct` to the BEFORE score. Both live runs reported trustworthy scores.

**Leak-safety confirmed:** after both runs `app.documents.count = 1` (only `Untitled`), ZERO `PRISM-DELTA-CLIVE-*` docs leaked. The correction loop builds into its own `/new` doc and `reapCreatedDocs` closes exactly the created delta (reap `closed=1 docsBefore=3`); operator docs (DIE CASE, _okuma .f3d, UP SET) are created-delta + beforeNames protected → provably never close candidates. Unlike the `/execute` assembly build, the correction loop's first-class `/sketch`+`/revolve` verbs target the `/new` doc correctly (no doubling — die baseline reads correct 60%, not inflated).

**Fixed this session:** R12 reap-display bug (commit `37a06a2d80`) — the runner printed `remaining=undefined` because `reapCreatedDocs` returns the `reapLeakedDocuments` shape (`{closedCount, before, closed[]}`, no `remaining`) when docs were reaped; now prints `closed=N docsBefore=M`.

**UPDATE 2026-06-02b — chamfer VERIFIABLE (rung C, `0cacb79a37`) THEN wired into the closed loop → die hits 100% (`d587d68fa0`).** The "chamfer is unverifiable" gap is fully closed end-to-end. Capability (`0cacb79a37`): `scripts/lib/cad-fusion-edge-feature-probe.mjs` reads the DEFINITIVE timeline counts (`chamferFeatures.count`/`filletFeatures.count`) + geometric tallies; `applyChamferCode` applies a real chamfer feature (createInput2 + legacy fallback). Integration (`d587d68fa0`): `bevel_face_chamfer` is now buildable (`opKind:"chamfer-edge"`) in `feature-correction.mjs`; the orchestrator (`cad-fusion-correction-loop.mjs`) routes chamfer verify to the EDGE-feature probe (`planChamfer`/`verifyEdgeFeatureApplied`, `verifyKind:"edge"` — the proven face-geometry radial/axial path untouched), and `geometryToCandidatePrint` folds a confirmed chamfer into the candidate print so the final diff CREDITS it; the runner injects `probeEdgeFeatures` + `applyChamfer` deps. **LIVE :18365 die: BEFORE 60% (3/5) → APPLIED bevel_face_chamfer✓ + cross_drilled_relief_holes✓ → AFTER 100% (5/5), missing [], Δ +40%, TRUSTWORTHY 100% verifiedCount=2.** The closed loop now self-improves a part to a FULL match. R12 honesty intact: a chamfer apply reporting not-ok stays uncredited (no fake 100% — guard test). Offline 42/42 (feature-correction 18, orchestrator 15 incl. rung-C E2E die 3/5→5/5 + fake-100 guard, edge-feature 12). Back-compat: a runner that injects no `probeEdgeFeatures` dep → faces-only (unchanged).

**How to apply:** to run a closed-loop training cycle: `node scripts/cad-fusion-correction-loop-live.mjs --port 18365 --part <die|bushing|extrude_punch|casing|plate|bracket>`. Each run compounds the training corpus. Part proxies in `PROFILES`/`PRISMATIC_PROFILES`; xray ground truth in `state/shared/ocr-ground-truth/cad-prototype-<part>-*.json` (xray slot owns these). Re-check bridge `/status` first (host memory pressure makes calls slow). Related: [[reference_delta_kinematic_assembly_live_2026_06_02]] (assembly side + #34 isolation fix) · [[reference_delta_designated_port_18632]].

**UPDATE 2026-06-03 (slot:delta) — full 6-class live pass RE-VERIFIED today + a training-integrity finding.** Re-ran the whole closed loop LIVE on `:18365` (bridge `connected`, operator actively in Fusion — docs 'SAW PART'/'PPN-30098B-3P'; reap closed exactly the created delta each cycle, zero leak): **die** 60→100% (vc2: bevel_face_chamfer✓+cross_drilled_relief_holes✓) · **extrude_punch** 50→100% (vc2: cross_drilled_relief_holes✓+shoulder_fillet✓, fillet R12 path) · **casing** 75→100% (vc1: bevel_face_chamfer✓) · **bracket** 0→100% (vc1: central_oil_hole✓, prismatic slab axial-bore) · **bushing** 100% baseline (vc0, honest no-op) · **plate** honest **33.3%** (vc3) — STILL blocked on the xray-owned corrupt GT. The loop is at **honest-max coverage** for every existing part class given the 6 buildable correction mechanics (axial-bore/radial-hole/taper-tip/add-step/chamfer-edge/fillet-edge) and the current (xray-owned) ground truth; corpus compounded ~15 examples.

**NEW IN-DOMAIN FINDING — training-corpus pollution (R9/R12 garbage-in):** running `plate` appends **9 dual-training examples labeled `partClass:"plate"`** sourced from `cad-prototype-plate-2026-05-19.json`, whose features are a TURBINE BLADE (rotational `stepped_revolved_axis`+`working_tip_taper` — topologically IMPOSSIBLE on a prismatic slab — plus 3 blade fillets). The 3 verified corrections are legit, but the fixture's `part_class` label is wrong, so every example carrying it teaches a downstream blind-CAD-gen model a false "plate" archetype. **Designed fix (NEXT UNIT, 4-file vertical slice — not built this session per budget/cut-off rule):** add a deterministic quarantine guard — (1) `scripts/lib/cad-fusion-feature-correction.mjs`: `correctionsToTrainingData(corrections, {partClass, verified, prismatic=false})` + export `ROTATIONAL_ONLY_KINDS=new Set(["stepped_revolved_axis","working_tip_taper"])`; if `prismatic && any(ROTATIONAL_ONLY_KINDS.has(c.kind))` → mark ALL the fixture's datums `{quarantine:true, quarantineReason:"fixture-topology-inconsistent: rotational feature on prismatic part_class"}` (back-compat: default `prismatic=false` → unchanged). (2) `cad-fusion-correction-loop.mjs:160` thread `prismatic`. (3) runner thread `prismatic:!!PRISMATIC` + print quarantine count. (4) `cad-fix-training-ledger.mjs` `trainingDataToFixEntries` carry the flag onto the persisted entry so the trainer FILTERS (never silently drops — R12). +tests in feature-correction.test + ledger.test (prismatic-blade→all quarantined; revolve-die→none; default unchanged). **Cross-domain action:** surface plate-GT corruption to **xray** (OCR ground-truth owner) — either correct the fixture to real plate features (central hole + mounting holes + edge chamfers/fillets) or rename it `blade` (its features ARE a blade). Delta does NOT overwrite shared xray fixtures. **Frontier (multi-session builds, designed-not-built):** (B) loft/sweep/spline single-part + NURBS-surface probe (new BUILD_KIND + new probe) · (C) branching assemblies + more joint kinds · (D) targeted edge selection (not edge[0]) — risks the fillet verify/credit asymmetry, needs a fillet-LOCATION classifier first.
