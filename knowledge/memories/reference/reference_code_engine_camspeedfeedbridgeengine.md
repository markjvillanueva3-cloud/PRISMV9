---
name: reference_code_engine_camspeedfeedbridgeengine
description: "Per-CAM Speed/Feed Translation (U-CAM99)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMSpeedFeedBridgeEngine.ts
  sourceHash: 7b760c51
  loc: 402
aliases: CAMSpeedFeedBridgeEngine
---

# CAMSpeedFeedBridgeEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMSpeedFeedBridgeEngine.ts` (402 LOC)

## What it is
Per-CAM Speed/Feed Translation (U-CAM99)

```
CAMSpeedFeedBridgeEngine — Per-CAM Speed/Feed Translation (U-CAM99)
====================================================================

PHASE-7: Bridge between the four CAM plugin adapters and the central
SpeedFeedOrchestratorEngine (2,851 LOC). Each CAM host uses its own
parameter vocabulary — this engine normalizes native requests into
OrchestratorInput, runs the computation, then encodes the result back
into the host's preferred response format for dispatch over the hub.

Translation matrix (common parameters → OrchestratorInput keys):

  | CAM host        | tool_dia field     | rpm / Vc field       | fz field         |
  |-----------------|--------------------|----------------------|------------------|
  | hyperMILL       | toolDiameter       | cuttingSpeedVc       | feedPerTooth_fz  |
  | Fusion 360      | toolDiameter       | spindleSpeed         | feedPerTooth     |
  | Inventor
```


## Exports
`SFBridgeTargetSchema` (const), `SFBridgeTarget` (type), `SFNativeRequestSchema` (const), `SFNativeRequest` (type), `SFBridgeRequestSchema` (const), `SFBridgeRequest` (type), `SFBridgeResponseSchema` (const), `SFBridgeResponse` (type), `normalizeRequest` (function), `encodeResponse` (function), `SpeedFeedComputeFn` (type), `CAMSpeedFeedBridgeEngine` (class), `camSpeedFeedBridgeEngine` (const)

## Key methods
`translateRequest`, `translateResponse`, `compute`, `supportedTargets`

## Depends on
[[reference_code_engine_speedfeedorchestratorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CAMSpeedFeedBridgeEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMSpeedFeedBridgeEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMSpeedFeedBridgeEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
