---
name: reference_code_script_populate5hquota
description: "populate-5h-quota.mjs -- Alternate 5h-usage populator for token-awareness sidecars."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/populate-5h-quota.mjs
  sourceHash: c897d012
  loc: 543
aliases: populate-5h-quota.mjs
---

# populate-5h-quota.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/populate-5h-quota.mjs` (543 LOC)

## What it is
```
populate-5h-quota.mjs -- Alternate 5h-usage populator for token-awareness sidecars.

PROBLEM: Claude Code on this host does NOT emit `rate_limits.five_hour` in its
statusLine stdin JSON, so `quota.fiveHour.pct` is null in every
`state/shared/token-budget-<slot>.json` sidecar. The account-switch-restart-
coordinator reads that field and fails loud (FIVE_HOUR_SOURCE_UNAVAILABLE) when
all slots report null -- blocking account auto-switch entirely.

SOLUTION: Parse the rolling-5h token usage directly from Claude Code's own
conversation JSONL files (~/.claude/projects/**\/*.jsonl) -- the same corpus
ccusage uses. Compute the usage fraction against a documented, env-overridable
ceiling and write it into `quota.fiveHour.pct` in the sidecar, leaving all other
sidecar fields untouched (atomic merge-write).

TOKEN ACCOUNTING (R12 -- honest):
  ccusage `blocks` counts `inputTokens + outputTokens + 
```


## Exports
`DEFAULT_FIVE_HOUR_CEILING` (const), `WINDOW_MS` (const), `SOURCE_TAG` (const), `ceilingFromEnv` (function), `computeRollingSum` (function), `computePct` (function), `mergeFiveHourIntoSidecar` (function), `enumerateJsonlFiles` (function), `loadRecentRecords` (function), `readSidecar` (function), `atomicWriteSidecar` (function), `resolveProjectsDir` (function), `resolveSidecarDir` (function), `populateFiveHourQuota` (function)

## Key methods
`atomicWriteSidecar`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/populate-5h-quota.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find populate-5h-quota.mjs` or `prism_session:master_index_query`; open the source at `scripts/populate-5h-quota.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
