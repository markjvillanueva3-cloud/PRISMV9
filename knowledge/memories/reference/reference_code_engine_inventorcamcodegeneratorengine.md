---
name: reference_code_engine_inventorcamcodegeneratorengine
description: "InventorCAMCodeGeneratorEngine - Generate G-code from InventorCAM/HSMWorks (E2401)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/InventorCAMCodeGeneratorEngine.ts
  sourceHash: e29e17d9
  loc: 2685
aliases: InventorCADGeneratorAdapter
---

# InventorCADGeneratorAdapter -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/InventorCAMCodeGeneratorEngine.ts` (2685 LOC)

## What it is
```
InventorCAMCodeGeneratorEngine - Generate G-code from InventorCAM/HSMWorks (E2401)

Generates automation scripts and G-code post-processor configurations for
InventorCAM (Autodesk Inventor HSM) and HSMWorks (SolidWorks HSM). Both use
the same underlying HSM kernel from Autodesk (formerly HSMWorks ApS).

Capabilities:
  - iLogic automation scripts for Inventor
  - HSM post-processor customization (.cps files)
  - Operation creation templates
  - Tool library export/import
  - NC program generation with custom post
  - Batch processing scripts

HSM Post-Processor Architecture:
  - JavaScript-based .cps files
  - onOpen, onSection, onClose event handlers
  - Customizable G-code output formatting
  - Machine-specific kinematic support

Methods:
  generateILogicScript(ops, tools, params)    - iLogic VB.NET automation
  generatePostCustomization(machine, opts)   - Custom .cps post edits
  gene
```


## Exports
`ScriptType` (type), `OperationType` (type), `TemplateCategory` (type), `ToolSpec` (interface), `CuttingParams` (interface), `StrategyParams` (interface), `OperationSpec` (interface), `GenerateParams` (interface), `ScriptTemplate` (interface), `CodeGenerationResult` (interface), `InvLen` (type), `InvAng` (type), `InvPlane` (type), `InvBooleanOp` (type), `InvPattern` (type), `InvAssemblyMateKind` (type), `InvHoleCycle` (type), `InvFragment` (interface), `ilCreateSketch` (function), `ilSketchLine` (function)

## Key methods
`writeBlock`, `MsgBox`, `writeFrag`, `ilSketchArc`, `ilSketchPolygon`, `ilSketchEllipse`, `ilSketchSlot`, `ilExtrude`, `ilRevolve`, `ilLoft`, `ilSweep`, `ilHole`, `ilFillet`, `ilChamfer`, `ilShell`, `ilDraft`, `ilPatternRectangular`, `ilPatternCircular`, `ilMirror`, `ilMove`, `ilRotate`, `ilWorkPlane`, `ilParameter`, `generateILogicScript`

## Depends on
[[reference_code_engine_unifiedcadcodegeneratorbase]] | `ICADCodeGenerator`

## Where it lives
- **Source:** `mcp-server/src/engines/InventorCAMCodeGeneratorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find InventorCADGeneratorAdapter` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/InventorCAMCodeGeneratorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
