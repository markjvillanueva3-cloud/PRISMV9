---
name: reference_code_script_autocloseshippedenvelopes
description: "auto-close-shipped-envelopes.mjs — CLEANUP-MS0 / U-CLEANUP-F2B"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/auto-close-shipped-envelopes.mjs
  sourceHash: 7456ca2e
  loc: 598
aliases: autoCloseShippedEnvelopes
---

# autoCloseShippedEnvelopes -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/auto-close-shipped-envelopes.mjs` (598 LOC)

## What it is
```
auto-close-shipped-envelopes.mjs — CLEANUP-MS0 / U-CLEANUP-F2B

Reads state/shared/CLOSE-OUT-CANDIDATES.json (produced by
scripts/audit-close-out-candidates.mjs, U-CLEANUP-F2) and queues each
high-confidence shipped-but-pending unit as a mutation entry in
state/shared/golf-envelope-mutations.jsonl. A separate main-tree drainer
hook (out of scope for this unit) reads the queue and applies the
status flips to the milestone-envelope JSON files.

Why queue + drain rather than mutate directly: the golf hygiene chat is
write-allowlisted by `.claude/hooks/golf-slot-write-allowlist.mjs` and
CANNOT commit to `mcp-server/data/milestones/*.json` itself (would
corrupt the milestone state). The two-stage producer/consumer pattern
lets golf queue advisory mutations while keeping the destructive flip
gated through a main-tree chat that has commit authority.

Fail-loud doctrine: each candidate becomes O
```


## Exports
`parseArgs` (function), `validateMilestoneFile` (function), `validateId` (function), `capEvidence` (function), `flattenCandidates` (function), `readQueueDedup` (function), `buildMutationEntry` (function), `autoCloseShippedEnvelopes` (function), `run` (function), `SCHEMA_VERSION` (re-export), `DEFAULT_MIN_CONFIDENCE` (re-export), `DEFAULT_MAX_CANDIDATES_AGE_HRS` (re-export), `DEFAULT_CANDIDATES_REL` (re-export), `DEFAULT_QUEUE_REL` (re-export), `ACTION_FLIP_STATUS` (re-export), `PROPOSED_STATUS` (re-export), `SOURCE_TAG` (re-export), `EVIDENCE_MAX_ENTRIES` (re-export), `EVIDENCE_MAX_BYTES` (re-export), `ALLOWED_FROM_STATUSES` (re-export)

## Key methods
`cmp`, `mkdirSync`, `appendFileSync`, `appendFn`, `printHelp`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/auto-close-shipped-envelopes.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find autoCloseShippedEnvelopes` or `prism_session:master_index_query`; open the source at `scripts/auto-close-shipped-envelopes.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
