---
name: reference_code_script_digesthooklatency
description: "digest-hook-latency.mjs — HOOK-SYNERGY-MS0 / U-HOOK-ENVELOPE (H4)"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/digest-hook-latency.mjs
  sourceHash: d7cdcb23
  loc: 472
aliases: digest-hook-latency.mjs
---

# digest-hook-latency.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/digest-hook-latency.mjs` (472 LOC)

## What it is
```
digest-hook-latency.mjs — HOOK-SYNERGY-MS0 / U-HOOK-ENVELOPE (H4)
                          + CLEANUP-MS0 / U-CLEANUP-F4 extension

Reads `state/shared/hook-latency.jsonl` (produced by `_envelope.mjs`) AND
`state/shared/async-hook-results.jsonl` (produced by AsyncHookDispatcherEngine,
the H7 detached-runner for T4 hooks), computes P50/P95/P99 + fire counts per
hook over the chosen window, and writes a digest at
`state/shared/HOOK_LATENCY_DIGEST.md`. Flags regressions by diffing against
the previous digest's snapshot block.

U-CLEANUP-F4 ADDED (extends, does NOT rewrite the renderer per R1):
  1. H7 async-results merge — async-hook-results.jsonl records are normalized
     into the same {ts,hook,durationMs,exitCode} shape and merged into the
     windowed dataset, so async (T4) hooks appear in the per-hook P95 table
     AND get the same 1.5×-prior-P95 regression flagging as sync hooks.
 
```


## Exports
`parseArgs` (function), `parseWindow` (function), `loadJsonl` (function), `normalizeAsyncRecord` (function), `loadAsyncResults` (function), `loadSnapshot` (function), `percentile` (function), `statsFor` (function), `stackTimeByEventTier` (function), `computeDigest` (function), `renderMarkdown` (function), `run` (function), `REGRESSION_MULTIPLIER` (re-export), `REGRESSION_MIN_MS` (re-export), `DEFAULT_WINDOW_MS` (re-export), `DEFAULT_TOP` (re-export), `JSONL` (re-export), `ASYNC_JSONL` (re-export)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/digest-hook-latency.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find digest-hook-latency.mjs` or `prism_session:master_index_query`; open the source at `scripts/digest-hook-latency.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
