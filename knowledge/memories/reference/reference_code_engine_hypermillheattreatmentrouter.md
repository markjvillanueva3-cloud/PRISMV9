---
name: reference_code_engine_hypermillheattreatmentrouter
description: "Heat treatment routing for hyperMILL operations"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HyperMillHeatTreatmentRouter.ts
  sourceHash: 56257e76
  loc: 458
aliases: HyperMillHeatTreatmentRouter
---

# HyperMillHeatTreatmentRouter -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HyperMillHeatTreatmentRouter.ts` (458 LOC)

## What it is
Heat treatment routing for hyperMILL operations

```
HyperMillHeatTreatmentRouter — Heat treatment routing for hyperMILL operations

Examines part material and hardness requirements to determine required heat
treatment steps and insert them at the correct point in the machining sequence:

  1. Pre-machine anneal — if material arrives work-hardened or as-cast
  2. Stress relief       — after heavy roughing (prevents distortion)
  3. Through harden      — before finish grinding (achieve final hardness)
  4. Case harden         — carburize/nitride before final finish

Temperature/time/quench parameters from HeatTreatmentEngine (which pulls
from material physics DB), not hardcoded.

Material certification traceability (U-HMR37):
  Each heat treatment step produces a CertLink that chains:
    MaterialCert → HeatTreatCert → InspectionCert
  in a format compatible with MS10 quality chain and AS9102 FAI requirements.

Sources:
  - ASM Handbook Vol
```


## Exports
`HeatTreatType` (type), `HeatTreatTiming` (type), `MaterialHeatTreatProfile` (interface), `HeatTreatStep` (interface), `HeatTreatCertLink` (interface), `HeatTreatmentRouteResult` (interface), `CertificationChain` (interface), `CertDocument` (interface), `HyperMillHeatTreatmentRouter` (class), `hyperMillHeatTreatmentRouter` (const)

## Key methods
`calculate`

## Depends on
[[reference_code_engine_heattreatmentengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/HyperMillHeatTreatmentRouter.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HyperMillHeatTreatmentRouter` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HyperMillHeatTreatmentRouter.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
