---
name: reference_code_engine_lathepurchaseorderautomationengine
description: "LathePurchaseOrderAutomationEngine — U-LTH52 (LATHE-MASTER P5 ERP)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LathePurchaseOrderAutomationEngine.ts
  sourceHash: 9925f8d9
  loc: 324
aliases: lathePurchaseOrderAutomationEngine
---

# lathePurchaseOrderAutomationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LathePurchaseOrderAutomationEngine.ts` (324 LOC)

> [physics] Imports canonical physics constants (`src/physics/constants.ts`) -- safety-relevant node.

## What it is
```
LathePurchaseOrderAutomationEngine — U-LTH52 (LATHE-MASTER P5 ERP)

Generates purchase-order line items from job requirements:
  - Material stock (computed from QuotePackage.evidence: OD/length/density)
  - Tooling (inserts, drills, threading tools by ISO group)
  - Consumables (coolant concentrate, chip-pan liners, fasteners)

Behavior:
  1. Read job requirements (job_id, quote, quantity, on_hand inventory)
  2. Compute required quantities with scrap allowance
  3. Subtract on-hand inventory (if provided) — only short items emit PO lines
  4. Split by preferred vendor per category (configurable catalog override)
  5. Compute line totals (qty × unit_cost) with EOQ-aware suggest_qty
  6. Aggregate into PO drafts — one PO per vendor

Physics:
  - Required mass kg = (π/4)(OD² − ID²) · L · density_kg_m³ · (1 + scrap)
  - EOQ = √(2·D·S / H) where D=annual demand, S=order cost, H=holding cost/
```


## Exports
`VENDOR_DEFAULTS` (const), `POCategory` (type), `InventoryOnHandSchema` (const), `InventoryOnHand` (type), `BuildPOInputSchema` (const), `BuildPOInput` (type), `POLine` (interface), `PODraft` (interface), `BuildPOResult` (interface), `lathePurchaseOrderAutomationEngine` (const), `LathePurchaseOrderAutomationEngine` (re-export)

## Key methods
`build`

## Depends on
[[reference_code_engine_latheautoquotefromprintengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/LathePurchaseOrderAutomationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find lathePurchaseOrderAutomationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LathePurchaseOrderAutomationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
