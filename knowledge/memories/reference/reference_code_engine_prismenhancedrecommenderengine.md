---
name: reference_code_engine_prismenhancedrecommenderengine
description: "PrismEnhancedRecommenderEngine — pareto-optimal selection over operator's"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PrismEnhancedRecommenderEngine.ts
  sourceHash: 1e1eb660
  loc: 411
aliases: prismEnhancedRecommenderEngine
---

# prismEnhancedRecommenderEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PrismEnhancedRecommenderEngine.ts` (411 LOC)

## What it is
```
PrismEnhancedRecommenderEngine — pareto-optimal selection over operator's
available-resources subset.

Given a curated subset of available machines/tools/holders/materials plus part
requirements (stock volume, Ra spec, quantity), runs NSGA-II to find the
pareto-optimal (cheapest, fastest, balanced) combinations across 5 objectives:
  1. cost_usd            (setup + cycle×$/min + tool-amortized)
  2. cycle_time_min      (stock_volume / MRR)
  3. tool_wear_risk      (cycle_time / Taylor_life — higher = worse)
  4. surface_risk        (Brammertz Ra_um / Ra_spec_um — higher = worse)
  5. chatter_risk        (proxy from rigidity × holder TIR)

Implements SFC-ACCURACY-MS1 §8 (spec: state/shared/specs/SFC-ACCURACY-MS1-DESIGN.md).
Wires as prism_calc:prism_enhanced_recommend.

Delegates the actual NSGA-II run to the existing `multiObjectiveEngine.nsgaII`
(`mcp-server/src/engines/MultiObjectiveEn
```


## Exports
`MachineCandidateSchema` (const), `MachineCandidate` (type), `ToolCandidateSchema` (const), `ToolCandidate` (type), `HolderCandidateSchema` (const), `HolderCandidate` (type), `MaterialCandidateSchema` (const), `MaterialCandidate` (type), `PartRequirementsSchema` (const), `PartRequirements` (type), `RecommendInputSchema` (const), `RecommendInput` (type), `RecommendCombo` (interface), `RecommendResult` (interface), `prismEnhancedRecommenderEngine` (const), `PrismEnhancedRecommenderEngine` (type), `PrismEnhancedRecommenderEngineImpl` (re-export)

## Key methods
`recommend`

## Depends on
[[reference_code_engine_multiobjectiveengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/PrismEnhancedRecommenderEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find prismEnhancedRecommenderEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PrismEnhancedRecommenderEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
