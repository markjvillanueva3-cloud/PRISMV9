---
name: reference_code_engine_sessionreorientationengine
description: "SessionReorientationEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SessionReorientationEngine.ts
  sourceHash: a048ee83
  loc: 527
aliases: SessionReorientationEngine
---

# SessionReorientationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SessionReorientationEngine.ts` (527 LOC)

## What it is
SessionReorientationEngine

```
SessionReorientationEngine

For very long sessions (especially 1M-context), valuable context gets
buried under turns and turns of intermediate work. This engine generates
a "reorientation brief" — a compact in-context refresh that surfaces:

  - Current task / objective
  - Active files (most recently edited)
  - Recent decisions and their rationale
  - Open todos
  - Recent errors that were resolved (so we don't loop)
  - Files already read (so we don't re-read)

Different from compaction: nothing is dropped from history. The brief
is INJECTED via a UserPromptSubmit hook so Claude re-anchors on what's
relevant NOW, without losing any prior context.

Triggers:
  - Every N user prompts (default 15)
  - Every K tool calls (default 50)
  - On manual /reorient
  - On detected drift (topic divergence > threshold)

Anchor types tracked:
  - task_anchor:    A new task started or major pivot
  -
```


## Exports
`AnchorType` (type), `SessionAnchor` (interface), `ReorientationBrief` (interface), `SessionStats` (interface), `SessionReorientationEngine` (class), `sessionReorientationEngine` (const)

## Key methods
`snapshotLastGood`, `atomicWriteJson`, `recordAnchor`, `deactivate`, `recordPrompt`, `recordToolCall`, `generateBrief`, `shouldGenerateBrief`, `getStats`, `getAnchors`, `updateConfig`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/SessionReorientationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SessionReorientationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SessionReorientationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
