---
name: reference_code_script_hermesproxyensure
description: "hermes-proxy-ensure.mjs -- idempotent keepalive for the Hermes OpenAI proxy"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/hermes-proxy-ensure.mjs
  sourceHash: 06c739b9
  loc: 166
aliases: hermes-proxy-ensure.mjs
---

# hermes-proxy-ensure.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/hermes-proxy-ensure.mjs` (166 LOC)

## What it is
```
hermes-proxy-ensure.mjs -- idempotent keepalive for the Hermes OpenAI proxy
(HERMES-BRIDGE-MS0/U-PROXY-ENSURE).

scripts/ask-hermes.mjs talks to `hermes proxy` on :8645. That proxy is not a
service -- if it is down, the bridge silently degrades to free Ollama. This
script makes the Hermes path reliably live: it checks the proxy, and if it is
down, spawns `hermes proxy start` DETACHED so it survives this process exit.
Idempotent -- running it when the proxy is already up is a no-op. Designed to
be driven by the `PRISM Hermes Proxy` scheduled task (install-hermes-proxy-task.ps1)
on the same cadence as the other PRISM keepalives, OR run ad-hoc.

Usage:
  node scripts/hermes-proxy-ensure.mjs [--provider xai|nous] [--port 8645]
                                       [--url http://127.0.0.1:8645/v1]
                                       [--timeout 6000] [--json]

Exit codes:
  0  proxy is up 
```


## Exports
`parseEnsureArgs` (function), `resolveBaseUrl` (function), `buildProxyArgv` (function), `resolveStartLogPath` (function), `buildStartStdio` (function), `isProxyUp` (function)

## Key methods
`emit`, `mkdirSync`, `writeSync`, `main`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/hermes-proxy-ensure.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find hermes-proxy-ensure.mjs` or `prism_session:master_index_query`; open the source at `scripts/hermes-proxy-ensure.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
