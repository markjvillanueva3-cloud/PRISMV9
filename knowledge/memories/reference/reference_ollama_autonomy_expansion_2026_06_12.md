---
name: reference_ollama_autonomy_expansion_2026_06_12
description: "U-OFFLOAD-ACTION (executed-offload telemetry + live autoexec knob + adoption metric) + U-NIGHT-BATCH (off-hours Ollama lane, scheduled task 22:23 nightly) + closed-loop training routed to 9 slots. Operator's \"drastically expand Ollama autonomy / off-hours / closed-loop training\" directive (slot:zulu, 2026-06-12)."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.670Z
aliases: reference_ollama_autonomy_expansion_2026_06_12
---


# Ollama autonomy expansion (slot:zulu, 2026-06-12)

Operator: "drastically expand scripts for autonomous Ollama offload; enforce it for token savings + off-hours background tasks; closed-loop training for delta/kilo/oscar/echo/mike/foxtrot/xray/whiskey."

## U-OFFLOAD-ACTION (committed)
- **Finding:** the dashboard's `offloaded` counted directives ISSUED, never work EXECUTED — adoption was invisible; and `PRISM_OLLAMA_OFFLOAD_AUTOEXEC=1` (live env) was read by NO hook (2026-06-11 fleet audit).
- `ask-ollama.mjs` now records EXECUTED offload events (measured in/out token delta; knob `PRISM_ASK_OLLAMA_TELEMETRY=0`); routed to separate `executedOffloads`/`measuredTokensSaved` totals + dashboard `adoptionRate` sub-metric (scrutiny P1: never double-counts the headline rate).
- Offloader: SAFE_AUTOEXEC += `prism_introspect`, `search_synthesis`; the autoexec knob now bypasses the per-category rate limit for SAFE categories only (never executes in-hook — that's india's U-BRIDGE-TOOLS). 121/121 tests + live proof (tokensSaved=949 matched footer; post-fix run routed to executedOffloads).

## U-NIGHT-BATCH (committed + task registered)
- `scripts/ollama-night-batch.mjs` + `state/shared/ollama-night-batch-registry.json` + 12 tests. Window 22:00–06:00, repo-script confinement (`node -e`/absolute/traversal rejected), atomic wx lock, Ollama pre-check fail-loud, per-job fail-soft + JSONL forensics, 64MiB maxBuffer.
- **Scheduled task "PRISM Ollama Night Batch" registered** (node.exe — NOT extension-less portable-node, which has no .exe and fails Task Scheduler launch — daily 22:23, 7h limit); live-validated LastResult 0.
- Seeded: capability-probe `--out` (probe got a clobber-guard: all-zero outage matrix REFUSES to overwrite + generatedAt set) + `mine-galaxy-transcripts --next-unpopulated --next-count 2` (self-rotating).
- **Never add unattended index-mutating embed batches** (tribal clobber 8bf1873577); jobs must not spawn detached children (timeout ≠ tree-kill; reaper protection is name-pattern based).

## Closed-loop training routing (10 chat-bus work-requests)
Honest split (R12): **Ollama is inference-only — it does NOT train.** Its closed-loop role = LABELING/EXTRACT/EVAL arm emitting per-galaxy LoRA pairs (galaxy-lora-emit, 1219 rows trainingReady); **torch PEFT on Blackwell trains** (india's lifecycle; xray's Qwen2.5-VL trainer is the exemplar). Live exemplars: xray OCR loop (resumable multi-page), CAD-CLOSED-LOOP-MS0 (india), charlie quoting loop. Routed: delta/kilo/oscar/echo/mike/foxtrot/whiskey (+xray scale-up via night lane, +india training arm). Earlier same-day memos: india U-BRIDGE-TOOLS, victor U-CORPUS-SWEEPS, alpha U-LOCAL-RESEARCH.

## U-NIGHT-EXPAND (committed, second operator re-ask)
Night registry expanded 2 → 9 jobs via an 11-agent ultracode verification workflow (`wf_eaeb1510`, sonnet readers, adversarial clobber-class brief). **Seeded:** vault-to-lora feedback+galaxy (LEG-B feed, live-proven 291 pairs), distill-tribal `--no-llm --sample=500`, vault-to-gnn-refpool DRY-RUN report (`--apply` hard-banned: graph mutation + spawnSync child survives timeout kill), lint-wiki-contradictions `--limit 30`, mine-hotel `--limit 10`, mine-india `--limit 5 --no-vault` (mandatory). **Hard-rejected** (defects named in the registry note): lint-wiki-orphans, youtube-free-extract (in-process tribal ingest), galaxy-synthesis-refresh (uncoordinated sidecar rebuild), hermes-dream-cycle-synth (cascade; needs per-job env support — follow-up), batch-ollama-vision-extract (child unaudited, xray-owned). Order short→long; worst-case ~5.5h < 7h task limit; window re-check sheds the tail.

## U-YT-NIGHT-STAGE + U-YT-PROMOTE (committed `49a6d1cb80`, THIRD operator re-ask)
The rejected youtube-free-extract defect FIXED instead of re-added: `scripts/youtube-night-extract.mjs` (staging-only wrapper, FORCES `--no-ingest --no-wiki` -- invariant pinned by a source-regex test; queue `state/shared/youtube-extraction/night-queue.json` seeded with 8 closed-loop domain queries for delta/kilo/oscar/echo/mike/foxtrot/whiskey/xray; weekly OK-run rotation, failed retries nightly, 3x13min < 45min job cap) + `scripts/promote-youtube-staged.mjs` (ATTENDED promotion: dry-run default, `--apply` -> TribalKnowledgeEngine U-TK01 dedup ingest + per-video wiki via the REUSED exported ingestTips/writeWikiEntry; ledger-after-success crash-safe resume). Registry now **11 jobs** (+auto-research-weekly-digest, idempotent). **LIVE backlog discovered: 556 staged artifacts, 24 promotable (~230 tips, May-26..today) -- in-process engine ingest had been silently falling back to JSON, proving the staged+attended split.** Victor notified (chat bus) to curate queries + run promote --apply. Research answer (R12-honest): local models DO corpus research (ollama-prism-bridge L2 read-only tools + ask-ollama synthesis + auto-research digest queue); open-web research stays attended (no unattended crawler). 2-arm per-file scrutiny quota-deferred to a 16:12 one-shot cron (subagent limit reset 15:50) -- lands ~6h before the lane's first 22:23 firing.

## U-YT-INGEST-URL-FIX (committed `35d074884d`) -- 17-day silent ingest death, root-caused on first attended promotion
`ingestTips()` dynamic-imported TribalKnowledgeEngine via `import(resolve(...))` -- raw `H:\` path, Windows ESM loader rejects ("protocol 'h:'") -> EVERY in-process ingest since 2026-05-26 silently fell into the fallback-JSON path. Fixed with `pathToFileURL().href` at both import sites + source-pin test (59/59). Then promoted the stranded backlog LIVE: **24 videos / 262 tips ingested + auto-categorized, ledger complete**. LESSON (R12 class): a catch->fallback that "works" can mask a 100%-failure primary path for weeks; grep other `await import(resolve(` sites when touching dynamic-import code on Windows.

## U-DO-EVERYTHING-NOW batch (committed `36aeea3f72`, operator blanket directive)
(1) **U-LEGBC-FRESHNESS unblocked**: gate now enforces LoRA artifact mtime <=48h (`PRISM_AISYN_LORA_MAX_AGE_H`) + NN-EVAL `assessedAt` <=21d (`PRISM_AISYN_NNEVAL_MAX_AGE_D`); live gate green with per-leg evidence. (2) **LoRA clobber near-miss caught**: the 11:26 inventory partial-regen left 0 'present' lora sources; unguarded `--out` would have written 0 rows over the 1219-row LEG-B artifact. `assertNoClobber` added (>50% shrink refused); inventory builder re-run (9 sources restored); `--out` through guard = **1366 rows (+147)**; drift root-cause routed to india. (3) **Night lane 11 -> 13 jobs**: +fleet-corpus-inventory +assemble --out (dependency-ordered, guard-protected) so the LEG-B ceiling self-maintains. (4) **Sibling probe semantics**: consolidate-graph `ollamaUp()` abort = up-but-busy -> spawn (only refusal skips). (5) **YT shakedown live**: 14 mill tips staged in production, ingested=0/wikiPath=null (staging invariant held). NOT done (R12): per-job env / hermes-dream-synth (external revert stands -- needs a by-name ask); subagent scrutiny quota-blocked until 15:50 (cron chain 16:03/16:12/16:22 covers all units before the 22:23 lane run).

## U-BRAIN-NIGHT-FLOOR (committed `5485e1b1c8`, operator "accelerate 2nd brain + persistent memory fleet-wide")
ONE night-lane entry runs `brain-refresh.mjs --force --with-viz` nightly (14-job lane): memory BM25 index -> dense embeddings -> AMP2 galaxy synthesis -> wiki->tribal embed -> system-viz regen. KEY INSIGHT: `galaxy-synthesis-refresh` stays HARD-REJECTED standalone (uncoordinated rebuild) but is the FIXED form inside brain-refresh's O_EXCL lock (20m stale-reclaim, runner-kill self-healing, Ollama-health-gated, sequential execFileSync children) -- second instance of the fix-the-named-defect pattern (after U-YT-NIGHT-STAGE). The Stop hook keeps daytime light upkeep; the night entry guarantees the heavy floor. Routed: alpha (galaxy-card master-sync stamp rot detector), juliett (Qdrant memory collections + AgentDB-HNSW deep-store), sierra (regen-viz nightly-cadence heads-up). Validated: dry-run plans 5/5 willRun, live mem-index ok 1s, lane suite green.

## U-DELTA-CAD-MAX-PACKAGE (committed `734e8de528`, operator "populate delta's galaxy to maximum / Ollama CAD generation")
Zulu lane: recon + feeds + routing (delta leads the domain work). KEY RECON FINDING: the two best open-source local text->CAD paths map onto models ALREADY resident in Ollama -- Seek-CAD (training-free self-refine, DeepSeek-R1-32B, arXiv 2505.17702) and Text-to-CadQuery (170K-annotation dataset + qwen-coder fine-tune class, arXiv 2505.06507); CadQuery/build123d+LLM = parametric STEP for free; quebec's pending mcp-cadquery merge is the integration point. Package: `state/shared/specs/DELTA-CAD-GALAXY-MAX-BUILDOUT-2026-06-12.md` (dependency-ordered: SEEKCAD-LOOP -> CQ-DATASET -> MCP-CADQUERY -> UI-KNOWLEDGE -> PRINT2CAD -> content sweep). Night queue 8 -> 12 (4 CAD UI-navigation queries: fusion-ui/fusion-api/mastercam-ui/hypermill-ui). Dispatched: delta/india/quebec/xray.

## U-CAD-TEXT-BRIDGE (committed `a0bf66dcd3`, operator 2nd delta re-ask) -- LIVE Ollama text->CAD
R8 keystone: `CadQueryCodeGeneratorEngine` (562 lines, cadquery_* dispatcher actions) documented "NL -> LLM -> CadQuery" but NO code ever called an LLM. `scripts/cad-text-to-cadquery.mjs` is the missing caller: engine `getCodeGenPrompt()` (dist import via pathToFileURL) + hard-coded JM doctrine -> qwen2.5-coder:32b -> structural gates (CAD import/STEP/inch-conversion; prose never stages) -> staged `state/shared/cad-text-gen/<slug>-<ts>/`. Execution branch SELF-ACTIVATES when build123d/cadquery lands in portable Python (quebec unblock). **FIRST LIVE RUN: 1in cube + .25in hole -> 646 chars valid parametric CadQuery (IN=25.4 explicit).** Flagged to delta: model over-applied electrode SPARK_GAP to a non-electrode hole (canonical-prompt precision). 6/6 tests.

## P2s deferred (logged)
night-batch: between-jobs window re-check, lock heartbeat, impure-shell test coverage, miner torn-write on timeout-kill. offload: ask-mode savings semantics (~0 by construction), `search_synthesis` SAFE fit, sibling keep_alive env-dependent test pin. yt-lane: ledger growth unbounded (rotate at ~1MB), queue curation is manual (domain slots own wording), `auto-research-weekly-digest` input jsonl currently empty (digest is a ready surface awaiting U-ALL06 producers).
