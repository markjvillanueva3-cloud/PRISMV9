---
name: reference_code_engine_eventbusengine
description: "in-memory typed pub/sub bus with bounded ring-buffer history."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/EventBusEngine.ts
  sourceHash: 6b0b15fc
  loc: 186
aliases: EventBusEngine
---

# EventBusEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/EventBusEngine.ts` (186 LOC)

## What it is
in-memory typed pub/sub bus with bounded ring-buffer history.

```
EventBusEngine — in-memory typed pub/sub bus with bounded ring-buffer history.

STUB-RESCUE (slot:bravo 2026-05-27, U-STUB-HUNT-05). Original was tagged
as a U-EFF25 stub but had partial publish/getRecentEvents/getStats
implementations and hardcoded `subscribers: 0`. infraDispatcher routes 4
actions here (`event_bus_publish`, `event_bus_events`, `event_bus_stats`,
summary.event_bus). Real implementation adds:

  - typed subscribe(eventType, handler) → unsubscribe handle
  - subscribe("*") wildcard for global listeners
  - publish() now fans out to matching subscribers synchronously, with
    try/catch per handler so one bad subscriber can't break the bus
  - configurable ring-buffer ceiling (default 1000)
  - getStats() returns real subscriber count
  - clear() for test isolation

@version 2.0.0 — restored from stub
```


## Exports
`EventBusEvent` (interface), `EventBusRecord` (interface), `EventBusStats` (interface), `DurableEventSink` (interface), `EventBusHandler` (type), `Unsubscribe` (type), `EventBusEngine` (class), `eventBusEngine` (const)

## Key methods
`append`, `publish`, `subscribe`, `getRecentEvents`, `attachDurableSink`, `getStats`, `clear`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/EventBusEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find EventBusEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/EventBusEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
