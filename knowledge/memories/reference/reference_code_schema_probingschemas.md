---
name: reference_code_schema_probingschemas
description: "Probing Cycle Parameter Schemas — hyperMILL CAM Probing"
metadata:
  type: reference
  kind: code-schema
  galaxy: unassigned
  sourcePath: mcp-server/src/schemas/hypermill/cam/probingSchemas.ts
  sourceHash: 9637178c
  loc: 514
aliases: probingSchemas.ts
---

# probingSchemas.ts -- Schema SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/schemas/hypermill/cam/probingSchemas.ts` (514 LOC)

## What it is
```
Probing Cycle Parameter Schemas — hyperMILL CAM Probing

U-HKC26: Zod schemas for 8 probing cycle types used in hyperMILL CAM.
Covers setup probes, feature probes (bore/boss/web/surface), tool setters,
breakage detection, and in-process verification. DFM-critical parameters
are tagged in metadata for downstream validation gates.

AC Python call pattern:
  hm.probe.setup_probe(probe_type="touch", calibration_diameter=6, ...)

@domain CAM-Probing
@milestone HM-KC-MS4/U-HKC26
```


## Exports
`setupProbeSchema` (const), `SetupProbe` (type), `boreProbeSchema` (const), `BoreProbe` (type), `bossProbeSchema` (const), `BossProbe` (type), `webProbeSchema` (const), `WebProbe` (type), `surfaceProbeSchema` (const), `SurfaceProbe` (type), `toolSetterSchema` (const), `ToolSetter` (type), `toolBreakageSchema` (const), `ToolBreakage` (type), `inProcessVerifySchema` (const), `InProcessVerify` (type), `PROBING_SCHEMA_MAP` (const), `ProbingCycleType` (type), `ProbingMeta` (interface), `PROBING_METADATA` (const)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/schemas/hypermill/cam/probingSchemas.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Schema

## How to query it
Recall by meaning via `/system-viz find probingSchemas.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/schemas/hypermill/cam/probingSchemas.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
