---
name: reference_code_engine_loraadapterregistryengine
description: "U-LEARN-07"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LoRAAdapterRegistryEngine.ts
  sourceHash: 34621a81
  loc: 317
aliases: LoRAAdapterRegistryEngine
---

# LoRAAdapterRegistryEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LoRAAdapterRegistryEngine.ts` (317 LOC)

## What it is
U-LEARN-07

```
LoRAAdapterRegistryEngine — U-LEARN-07
=======================================

Cross-domain registry of every trained LoRA adapter PRISM knows about.
One active adapter per (domain, context-key); older versions stay archived
for instant rollback. Persists to state/adapters/registry.jsonl (append-only)
with atomic writes.

Lifecycle: staged → shadow → canary → active. Older active → archived on
successor promotion. rollback() flips (archived ↔ active) atomically.

Invariants:
  - One "active" adapter per (domain, context-key). Enforced on setStatus.
  - Persistence is append-only. `current_state` is a projection: replay
    all entries in order → materialize latest-wins state. Readers can
    audit every change by scanning the log.
  - Never throws — bad input returns {ok, warning}.

@module engines/LoRAAdapterRegistryEngine
@milestone PSAU P2.5-LEARN U-LEARN-07
```


## Exports
`OpResult` (interface), `LoRAAdapterRegistryEngine` (class), `loraAdapterRegistryEngine` (const)

## Key methods
`register`, `setStatus`, `rollback`, `resolve`, `list`, `stats`, `getSelfAwareness`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/LoRAAdapterRegistryEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find LoRAAdapterRegistryEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LoRAAdapterRegistryEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
