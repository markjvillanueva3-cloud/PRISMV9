---
name: reference_code_engine_inferenceloragateengine
description: "U-LEARN-07"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/InferenceLoRAGateEngine.ts
  sourceHash: db16fa5c
  loc: 161
aliases: InferenceLoRAGateEngine
---

# InferenceLoRAGateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/InferenceLoRAGateEngine.ts` (161 LOC)

## What it is
U-LEARN-07

```
InferenceLoRAGateEngine — U-LEARN-07
=====================================

Wraps every recommendation engine's output. Given (engine, domain, context,
baseline), it resolves the best-matching active/canary adapter from the
registry, applies the residual per-field to the baseline, and emits a
provenance-tagged result. Provenance tells the operator (and downstream
auditors) whether a recommendation came from physics alone or physics
plus a LoRA — and which adapter was used.

Residual kinds applied per field:
  - additive:       adapted = baseline + value
  - multiplicative: adapted = baseline * value
  - replace:        adapted = value
  - clamp:          adapted = min(max(baseline, min), max)

If no adapter matches, the gate passes the baseline through unchanged with
`adapter_used: null` — the engine still benefits from provenance + a
single audit hook without needing adapter coverage ev
```


## Exports
`InferenceLoRAGateEngine` (class), `inferenceLoRAGateEngine` (const)

## Key methods
`apply`, `getSelfAwareness`

## Depends on
[[reference_code_engine_loraadapterregistryengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/InferenceLoRAGateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find InferenceLoRAGateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/InferenceLoRAGateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
