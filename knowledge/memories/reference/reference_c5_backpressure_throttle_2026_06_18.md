---
name: reference_c5_backpressure_throttle_2026_06_18
description: "C5 (Hermes Adaptive Back-Pressure) FINISHED this session (slot:bravo, commit ccbfe4e5f4) -- and with it the buildable Hermes capability arc C1-C5 is COMPLETE. U-C5-BACKPRESSURE-LIVE-THROTTLE wired the back-pressure signal into the live governed wave: new pure ZuluWaveSchedulerEngine.throttleWave(execution, signals?, opts?) HOLDS saturated slots' assignments (re-offered next wave) -- NEVER vetoes (spec: advisory by default). 4th instance of the 'built-but-not-wired-together' seam (assessBackPressure + backpressure_* actions existed but no live consumer throttled). Dispatcher governed_wave_execute + wave_loop_step consult the durable store only on enforce/surface; default = zero-IO pass-through. 10 engine + 6 e2e tests; 3-of-3 PASS (0 blockers). C6/C7/C8 are SPEC-DEFERRED (need 20+ task-outcomes/slot, 4+ weeks of multi-wave runs)."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.492Z
aliases: reference_c5_backpressure_throttle_2026_06_18
---


# C5 back-pressure live-throttle -- FINISHED (2026-06-18, slot:bravo, commit ccbfe4e5f4)

## What shipped
`U-C5-BACKPRESSURE-LIVE-THROTTLE` (ccbfe4e5f4) closes the 4th seam of the arc -- the live governed wave now load-shapes by per-slot back-pressure:
- **`ZuluWaveSchedulerEngine.throttleWave(execution, signals?, opts?)`** -- PURE post-governor step. HOLDS (moves to `throttled`, never `vetoed`) an authorized assignment whose slot is saturated; held tasks stay incomplete -> re-offered next wave once pressure clears (throttle-and-retry, not a denial). Surfaces non-"low" signals in `back_pressure` (advisory annotation) in BOTH modes. Holds only when `enforce` + pressure >= `holdAtOrAbove` (default "blocked"). Absent/empty/non-Map signals -> byte-identical pass-through. Type-only import of `BackPressureSignal`/`PressureLevel` (acyclic -- C5 engine imports only fs/os/path; erased at runtime).
- **`ThrottledWaveExecution` extends `GovernedWaveExecution` { throttled, back_pressure }`** + **`ThrottledAssignment`** {subtask_id, slot, pressure_level, recommended_delay_ms, cause, escalate}.
- **`loopCheckpointState`** records the held `throttled` ids (defensive read -> [] for a plain GovernedWaveExecution; back-compat).
- **`sessionDispatcher.applyBackPressureThrottle()`** bridge; `governed_wave_execute` + `wave_loop_step` consult the durable store (`zb.assess` per distinct slot) ONLY when `enforce_backpressure` / `PRISM_BACKPRESSURE_ENFORCE=1` / `surface_backpressure`. DEFAULT = zero-IO pass-through (no zb import, no store read). `wave_loop_step` checkpoints the THROTTLED execution so held tasks are not recorded dispatched + re-offer next step.

## Tests (10 engine + 6 dispatcher round-trips)
advisory-surfaces-holds-nothing; enforce+blocked holds; NEVER-vetoes (seeds a pre-existing vetoed entry, asserts held id goes to throttled not vetoed); high-not-held-at-default; holdAtOrAbove=high holds high; no-signal-dispatched; undefined-signals pass-through; low-not-surfaced; escalate propagates; per-slot isolation. e2e RECORDS real-now blocked samples through `backpressure_record_sample`, asserts `backpressure_assess`=="blocked" as a precondition (fails loud on threshold drift), then proves enforce HOLDS / advisory SURFACES+dispatches / default zero-consult / unpressured-enforce holds nothing; wave_loop_step holds + checkpoints not-dispatched + re-offers. 123/123 across 6 suites incl. the C2/C3/C4 e2e regression (C2 wave loop green = default throttle path is byte-identical back-compat). Touched files tsc-clean (89 pre-existing unrelated CAD/electrode errors untouched -- the earlier "No errors found" was an rtk OOM artifact; real check needs NODE_OPTIONS=--max-old-space-size=16384).

## The arc is complete (C1-C5) + the seam pattern (4x)
This session's arc: **C1** governor gate, **C2** continuity producer+lock+ownership-verify, **C3** auction live-feed, **C4** delegation live-gate, **C5** back-pressure throttle -- ALL 3-of-3 PASS. C2 was an ORPHAN (no producer); C3/C4/C5 were all the SAME "built-but-not-wired-together" INTEGRATION SEAM -- a fully-built, fully-dispatcher-wired pure capability that the LIVE consuming loop never called. Fix shape is identical every time: find the live consumer, inject the capability (strictly-narrowing/advisory + back-compat default), round-trip e2e through the real store. -> [[reference_c4_delegation_live_gate_2026_06_18]] [[reference_c3_auction_live_feed_2026_06_18]] [[reference_c2_producer_and_lock_2026_06_18]] [[feedback_wire_test_validate_all_galaxies]]

## Next (C6-C8 are SPEC-DEFERRED -- do NOT force-build)
Per `HERMES-CAPABILITY-EXPANSION-CANDIDATES-2026-06-15.md` "Defer for now": **C6 (Live Capability Registry) / C7 (Capability Attestation) / C8 (Soul Evolution)** "require 20+ task outcomes per slot for statistical validity; wire after the fleet has been running multi-wave builds (C1) for 4+ weeks." Building them now violates R13 (proven foundation / logical order) -- the outcome-data precondition does not exist yet. NEVER-IDLE next-hunt for slot bravo descends the ladder: own-domain Hermes leftover (handoff open-threads) -> fixes (failing tests / tsc debt) -> wirings (audit-unwired-engines) -> ghost roosts -> backlog. The C5 P2 follow-ups (all safe-direction, from 3-of-3): (1) no automated escalation-on-N-consecutive-holds consumer yet -- the `escalate` flag is emitted but passively consumed; (2) `applyBackPressureThrottle` has no try/catch around `zb.assess` (relies on assess being throw-free, which holds today); (3) `PRISM_BACKPRESSURE_ENFORCE` is a fleet-wide kill-switch (over-hold direction is safe).
