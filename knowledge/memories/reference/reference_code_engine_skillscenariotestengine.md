---
name: reference_code_engine_skillscenariotestengine
description: "Skill Scenario Test"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SkillScenarioTestEngine.ts
  sourceHash: aa22c22d
  loc: 730
aliases: SkillScenarioTestEngine
---

# SkillScenarioTestEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SkillScenarioTestEngine.ts` (730 LOC)

## What it is
Skill Scenario Test

```
Skill Scenario Test Engine — U-SKU02 (SKILLS-UTILIZATION-MS0).

Implements @eng_khairallah1's Phase-3 "Three-Scenario" production-grade bar:
  - **happy**  — the normal case (~80% of invocations).
  - **edge**   — weird / incomplete / conflicting / already-done input.
  - **stress** — the biggest / messiest version of the input.
A skill is `production_grade` only when it passes all three with client-ready
output. PRISM had no skill-testing protocol; this engine + the `prism_dev:skill_test`
action + the `/skill-test` slash command install one.

──────────────────────────────────────────────────────────────────────────
SPLIT OF RESPONSIBILITY (read this before extending)
──────────────────────────────────────────────────────────────────────────
An MCP server-side engine cannot start a Claude agent loop, so it cannot
*invoke* a skill body. This engine therefore does the **deterministic** ha
```


## Exports
`SCENARIO_NAMES` (const), `ScenarioName` (type), `DEFAULT_MAX_INPUT_CHARS` (const), `MAX_COMPOSITION_DEPTH` (const), `DEFAULT_ERROR_MARKERS` (const), `ScenarioRubric` (interface), `LoadedScenario` (interface), `LoadScenariosResult` (interface), `GradeResult` (interface), `ScenarioRunResult` (interface), `SkillTestRunResult` (interface), `RecordVerdictsResult` (interface), `rubricFromFrontmatter` (function), `countHeadings` (function), `splitFixtureBody` (function), `looksLikeSkillComposition` (function), `SkillScenarioTestOptions` (interface), `SkillScenarioTestEngine` (class), `skillScenarioTestEngine` (const)

## Key methods
`loadScenarios`, `gradeOutput`, `run`, `recordVerdicts`, `withinBodyCap`

## Depends on
[[reference_code_util_logger]] | [[reference_code_registry_skillqualityregistrybuilder]]

## Where it lives
- **Source:** `mcp-server/src/engines/SkillScenarioTestEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SkillScenarioTestEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SkillScenarioTestEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
