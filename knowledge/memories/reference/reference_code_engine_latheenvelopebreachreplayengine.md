---
name: reference_code_engine_latheenvelopebreachreplayengine
description: "LatheEnvelopeBreachReplayEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LatheEnvelopeBreachReplayEngine.ts
  sourceHash: fba50a58
  loc: 178
aliases: latheEnvelopeBreachReplayEngine
---

# latheEnvelopeBreachReplayEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LatheEnvelopeBreachReplayEngine.ts` (178 LOC)

## What it is
```
LatheEnvelopeBreachReplayEngine
=================================

Block-by-block envelope-intrusion replay for turning programs.

Walks the simulated XZ toolpath against a forbidden-volume envelope
made up of:
  - chuck + jaw bounding cylinder (z ≤ chuck_face_z, r ≤ chuck_od/2)
  - tailstock body (z ≥ tailstock_face_z, r ≤ tailstock_od/2)
  - steady rest clamp ring (optional)
  - turret tool length + holder (approximated as a swept rectangle)

For each block position, tests whether the tool tip / holder
intrudes into any forbidden volume. The *first breach* block is
reported along with all subsequent hits for completeness.

Distinct from existing:
  - LatheCollisionZoneEngine    : static zone precomputation only
  - VideoReplayOrchestratorEng. : video UI replay, not geometry
  - CollisionEngine (generic)   : generic 3D mesh collision
  - This engine                 : block-granularity l
```


## Exports
`LatheBreachBlock` (interface), `LatheBreachEnvelope` (interface), `LatheEnvelopeBreachReplayInput` (interface), `LatheBreachHit` (interface), `LatheEnvelopeBreachReplayResult` (interface), `latheEnvelopeBreachReplayEngine` (const)

## Key methods
`replay`, `getStats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/LatheEnvelopeBreachReplayEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find latheEnvelopeBreachReplayEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LatheEnvelopeBreachReplayEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
