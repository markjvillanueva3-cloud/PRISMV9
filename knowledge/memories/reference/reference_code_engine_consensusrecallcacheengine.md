---
name: reference_code_engine_consensusrecallcacheengine
description: "short-circuit fan-out when an identical prompt"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ConsensusRecallCacheEngine.ts
  sourceHash: e709c7bd
  loc: 182
aliases: ConsensusRecallCacheEngine
---

# ConsensusRecallCacheEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ConsensusRecallCacheEngine.ts` (182 LOC)

## What it is
short-circuit fan-out when an identical prompt

```
ConsensusRecallCacheEngine — short-circuit fan-out when an identical prompt
already has a persisted consensus artifact in the wiki second-brain.

Milestone: INTEL-OLLAMA-OBSIDIAN-MS0 / LAYER-3-RECALL-CACHE.

Why this exists
---------------
Every consensus call costs ~$0.30 of Codex/Grok tokens + 30-60s wall time.
If a session asked the same question yesterday and persisted the answer,
re-fanning out is pure waste. This engine:

  1. Hashes the incoming prompt (same normalization as
     ConsensusObsidianPersistenceEngine — single source of truth on hash).
  2. Looks for `<wikiRoot>/consensus/<sha8>.md`.
  3. If present and not stale (default 7-day TTL), parses the frontmatter +
     reconstructs a synthetic ConsensusResult with `cached:true`.
  4. Returns null on miss — caller falls through to live fan-out.

The synthetic result preserves recommendation, agreement_score, voters,
mean_fac
```


## Exports
`RecallOptions` (interface), `CachedConsensus` (interface), `ConsensusRecallCacheEngine` (class), `consensusRecallCacheEngine` (const)

## Key methods
`recall`, `scoreCached`

## Depends on
[[reference_code_engine_consensusobsidianpersistenceengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/ConsensusRecallCacheEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ConsensusRecallCacheEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ConsensusRecallCacheEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
