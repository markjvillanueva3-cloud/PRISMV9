---
name: reference_code_engine_wiringpotentialengine
description: "Orphan-to-Dispatcher Recommendation"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WiringPotentialEngine.ts
  sourceHash: fae6163f
  loc: 598
aliases: WiringPotentialEngine
---

# WiringPotentialEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WiringPotentialEngine.ts` (598 LOC)

## What it is
Orphan-to-Dispatcher Recommendation

```
WiringPotentialEngine — Orphan-to-Dispatcher Recommendation
===========================================================

Analyzes an orphan engine (built but unwired) and ranks candidate
dispatchers it should be wired into. Combines three signals:

  1. **Semantic relevance** — regex heuristic over the engine name
     (mirrors orphan-inventory.mjs DISPATCHER_HEURISTICS so operator
     and engine agree on hints).
  2. **Capacity headroom** — reads `state/shared/DISPATCHER_CAPACITY.json`
     (CLEANUP-MS0/U-CLEANUP-F7 output). Dispatchers at ≥100% capacity
     are EXCLUDED outright; 80-99% are flagged `warn` and penalized;
     <80% are `ok` and full credit.
  3. **Documentation depth** — pre-joined wikiEntries[] + memoryEntries[]
     from the system-graph (already attached by MasterIndexEngine);
     engines with prior docs score higher because the wiring rationale
     is already par
```


## Exports
`CAPACITY_OK_MAX` (const), `CAPACITY_WARN_MAX` (const), `W_SEMANTIC` (const), `W_CAPACITY` (const), `W_DOCS_DEPTH` (const), `DEFAULT_TOP_K` (const), `MAX_TOP_K` (const), `MIN_HEURISTIC_CONFIDENCE` (const), `DOCS_DEPTH_SATURATION` (const), `DEFAULT_CAPACITY_PATH` (const), `CapacityClass` (type), `WiringCandidate` (interface), `WiringPotentialReport` (interface), `WiringPotentialOpts` (interface), `CapacityRow` (interface), `CapacityReport` (interface), `normalizeF7DispatcherName` (function), `tokenizeEngineName` (function), `WiringPotentialEngine` (class), `wiringPotentialEngine` (const)

## Key methods
`analyze`, `analyzeBatch`

## Depends on
[[reference_code_engine_masterindexengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/WiringPotentialEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WiringPotentialEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WiringPotentialEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
