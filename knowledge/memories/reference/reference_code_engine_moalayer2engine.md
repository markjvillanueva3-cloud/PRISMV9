---
name: reference_code_engine_moalayer2engine
description: "Mixture-of-Agents Layer-2 aggregator over N proposer outputs."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MoaLayer2Engine.ts
  sourceHash: 1a164e0d
  loc: 346
aliases: MoaLayer2Engine
---

# MoaLayer2Engine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MoaLayer2Engine.ts` (346 LOC)

## What it is
Mixture-of-Agents Layer-2 aggregator over N proposer outputs.

```
MoaLayer2Engine — Mixture-of-Agents Layer-2 aggregator over N proposer outputs.

Milestone: OCTOPUS-NEURAL-MS0 / U-OCN02.

Reference: Wang et al. "Mixture-of-Agents Enhances Large Language Model
Capabilities" (2406.04692). MoA-Layer-2 takes a set of "proposer" outputs
(independent answers from heterogeneous models) and distills them via a
"senior aggregator" model into a single calibrated answer that consistently
outperforms any individual proposer by 10%+ on rubric (per the paper).

PRISM's existing 3-of-3 scrutiny gate (Codex + Claude-A + Claude-B) produces
exactly the proposer set MoA wants. The gap before U-OCN02 was the *aggregation
layer*: scrutiny-3way.mjs records 3 verdicts but does not distill them into a
single calibrated decision with rationale + dissent + confidence. This engine
is that distillation layer.

Failure modes covered (per atomized spec):
  - senior aggregator down
```


## Exports
`MoaVerdict` (type), `ProposerOutput` (interface), `AggregatorCall` (type), `MoaAggregateOptions` (interface), `MoaAggregateResult` (interface), `MoaLayer2Engine` (class), `moaLayer2Engine` (const)

## Key methods
`aggregate`, `onTruncate`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MoaLayer2Engine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MoaLayer2Engine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MoaLayer2Engine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
