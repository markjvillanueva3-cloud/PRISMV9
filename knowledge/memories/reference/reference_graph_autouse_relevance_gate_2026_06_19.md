---
name: reference_graph_autouse_relevance_gate_2026_06_19
description: "GRAPH-AUTOUSE/U-RELEVANCE-GATE SHIPPED (c2a470ac11, slot:alpha, 2026-06-19): a relevance-score gate in searchGraphHits (master-index-search-lib.mjs) so all 5 BM25 graph-inject surfaces stop injecting low-confidence noise -> chats auto-use the graph on trust. Plus the 11-surface graph-auto-usage audit + 6 ranked enhancement candidates (the operator's 'make chats auto-use the graph effectively' map) -- incl the #1 lever GAP #1: production searches the 20K-node architecture-graph FALLBACK not the 110K full graph (257MB sidecar exceeds heap ceiling)."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.600Z
aliases: reference_graph_autouse_relevance_gate_2026_06_19
---


# Graph auto-use: relevance gate SHIPPED + the enhancement map (2026-06-19, slot:alpha)

Operator: "find enhancements for your graph you've been building so the chats auto use it effectively."

## SHIPPED this unit (c2a470ac11, GRAPH-AUTOUSE/U-RELEVANCE-GATE)
`applyRelevanceGate(ranked, minRatio)` (pure exported, `scripts/lib/master-index-search-lib.mjs`) wired into `searchGraphHits` after the desc sort, before the label-dedup/topK slice. Drops trailing hits below `minRatio` of THIS query's TOP score. ONE shared-lib change improves ALL 5 BM25 graph-inject surfaces at once (master-index-precheck-inject + pre-{read,grep,write,bash}-graph-inject all route through `runMasterIndexSearch` -> `searchGraphHits`; the daemon `/search` too). Default 0.15, env `PRISM_MASTER_INDEX_MIN_SCORE_RATIO` (<=0 disables = instant rollback; daemon must restart to pick up an env change -- it freezes the ratio at launch). **SAFE-BY-CONSTRUCTION:** top hit ratio = 1.0 always passes -> never drops the top, never empties a non-empty result (the `hits[0]` exact-match-collapse paths unaffected). Calibrated vs the live query-log topScore distribution (min 1.5 / p50 13.5 / max 30): drops weak-field-only noise (info-only=1.5, vault-only=1.0) while keeping label-matches (>=3.0) for typical queries (top<=20). Validated: 68/68 tests (+8 reference-value); live = dropped 0 hits on real strong queries (non-destructive). Per-file 2-arm PASS (0 P0/P1; one P2 negative-env-disable fixed inline).

## The 11 graph-auto-usage surfaces (audit map, for future iterations)
UserPromptSubmit: `master-index-precheck-inject` (top-K BM25), `node-card-prefetch-inject` (full cards for dotted node-ids in prompt -- the ONLY surface that injects card CONTENT not just names), `audit-viz-first-inject` (runs `system-viz-query find` before Grep/Glob). PreToolUse: `viz-first-redirect` (Glob/Grep subprocess), `pre-{read,grep,write,bash}-graph-inject` (in-process BM25). SessionStart/Stop/PostToolUse: `sessionstart-graph-staleness-inject`, `stop-system-viz-reminder`, `system-viz-live-bridge` (health/refresh, not retrieval).

## 6 ranked enhancement candidates (the operator's "effective auto-use" backlog)
1. **GAP-A inline card content for high-confidence hits (M) -- COMPLETE for all 4 PreToolUse BM25 hooks (slot:alpha 2026-06-19).** Shared `renderTopCardBlock(hit, seekDocs)` (canonical home `scripts/lib/graph-exact-match.mjs`, co-located with vaultPathsLine/exactMatchBanner) prepends the TOP non-exact hit's CARD (id/layer/status/label/score/info + vault doc pointers via fail-soft `vaultPathsLine`) when its BM25 score >= `PRISM_PRE_<HOOK>_INLINE_CARD_MIN_SCORE` (default 10; p25=8.5/p50=13.5). Card prepended only when combined block fits the 1500-byte cap (else names-only); fail-open; exact-match-collapse path unaffected (early return precedes all GAP-A logic; pinned by isolation tests). The #1 "zero follow-up call" win. Ships: **a15535ff04** pre-grep -> **6d7a5d9484** hoist renderTopCardBlock to the shared lib (dedup-correct foundation) -> **aee30d9363** pre-read -> **6ee830404e** pre-write -> **37601a5335** pre-bash -> **5f606e42d8** pre-grep ASCII/1500 truncation parity. Each: real tests (25/17/21/34 + 25 shared-lib) + per-file 2-arm PASS (0 findings on siblings) + LIVE inline card score 16.0. Knobs `PRISM_PRE_{GREP,READ,WRITE,BASH}_INLINE_CARD_MIN_SCORE`. **master-index-precheck-inject ASSESSED + NO-BUILD (2026-06-19, R16/R12):** it is NOT a names-only surface -- it ALREADY injects `wiki:`+`mem:` doc-pointers for every top-K hit (more complete than the PreToolUse hooks were pre-GAP-A; line 290-291). The ONLY missing card element is the node `info` description. **But `info` is BLOCKED by GAP #1:** `runMasterIndexSearch`/`searchGraphHits` returns hits WITHOUT `info` (keys: id,score,layer,label,status,noteCount,wiki,memory,explanation), and the production path uses the 64.7MB ARCHITECTURE-GRAPH fallback (the 257MB full-graph sidecar > heap ceiling) whose nodes carry PATH-LIKE info (`wiki/architecture/<name>`), NOT human descriptions -- empirically verified across L8/L10 nodes. Adding `info: node.info` to the hit shape (tried + REVERTED, byte-clean) would surface path-redundant NOISE on the production path and REGRESS the just-shipped clean GAP-A cards (the renderers already guard on `h.info` -> they'd render the path). **Real fix = GAP #1 (full system-graph load w/ real descriptions, owner sierra); once the full graph loads, add `info` to the searchGraphHits hit + the renderers light up automatically.** So the GAP-A "card content" goal is SUBSTANTIALLY MET (4 PreToolUse hooks + doc-pointers everywhere); the info element is GAP-#1-gated, not a quick win. Lesson: pre-read needed seekDocs THREADED (signature + main() resolver clone); pre-write/pre-bash already had the seekDocs resolver (sierra U-SV-NODE-VAULT-PATHS) so only the renderInject branch was added.
2. **GAP-B relevance threshold (S) -- SHIPPED THIS UNIT.**
3. **GAP-C take-rate measurement for graph injects (M):** mcp-route-takeup infra exists for MCP routes; NO equivalent for graph injects (nav-savings-ledger only credits exact-match collapses). "Can't improve what you don't measure."
4. **GAP-D semantic fallback / stopword fix (S-L):** BM25 zero-hit on paraphrases; `_node-embeddings.jsonl.partial` (399MB, incomplete) exists. Quick win: shorten the STOPWORDS list (removing "engine"/"system" helps domain queries).
5. **GAP-E dedup the Grep-time surfaces -- PREMISE PARTLY WRONG, LOW ROI (assessed 2026-06-19, slot:alpha; don't re-attempt blindly):** the "3 surfaces fire on a Grep" premise is incorrect -- `master-index-precheck-inject` is UserPromptSubmit (fires on the PROMPT, NOT on a Grep), so at most 2 PreToolUse surfaces could overlap on a Grep. The Grep-time graph hooks are NOT wired as individual entries in the canonical C: settings.json PreToolUse list (they fire via a bundle/different matcher structure -- confirming exact overlap needs deep hook-wiring archaeology). Each graph-inject hook ALREADY carries its own per-(session,content-hash) dedup (e.g. pre-read U-PRGI-DEDUP, pre-grep). Net: cross-hook Grep-time dup is unconfirmed + each hook self-dedups + the wiring is bundle-opaque -> NOT a safe quick win; needs a real wiring audit first if ever pursued. Lower priority than GAP-C/FIXES/WIRINGS.
6. **GAP-F widen node-card-prefetch trigger prefixes -- VERIFIED OUT (2026-06-19, do NOT re-attempt blindly):** the audit's claim was WRONG. Live `.offsets` tally (346,835 ids, 100 prefixes) shows `action=0` + `hook=0` (DON'T EXIST), `schema=2295` exists but is FALSE-POSITIVE-PRONE (`schema.parse`/`schema.safeParse` are ubiquitous in this Zod codebase -> useless 24MB offsets-parse each time; fails the whitelist's "distinctive, unlikely-FP" criterion), `alg=123` is safe but marginal. Net: not a worthwhile unit. Real un-whitelisted namespaces by size: vault=60218 / datacat=22178 (intentionally excluded, noisy) / reg=1001 / ppg=1311. Lesson: VERIFY a proposed namespace against the live `.offsets` (under the `.offsets` key, NOT top-level) + assess code-token false-positive risk before widening TRIGGER_PREFIXES.

## HIGH-VALUE LEVER -- GAP #1 (the real effectiveness killer, flagged for sierra/next)
Production master-index ALWAYS falls back to the **20K-node architecture-graph (65MB)**, NOT the 110K-node full `system-graph.json` (734MB > 200MB cap), because the 257MB sidecar exceeds the heap parse ceiling (~151MB on the default 384MB heap). So chats search 20K nodes, not 110K. Fixing the sidecar size (a smaller/streamable sidecar, or a warm daemon on the full graph) is the single biggest "auto-use effectively" lever -- bigger than any inject-format tweak. The daemon path (port 3101) would give full coverage if warm. Owner: sierra (system-viz).

Related: [[reference_master_index_sidecar_2026_05_19]] (prior cap raise) · [[reference_sierra_masterindex_notecount_2026_06_02]] (searchGraphHits noteCount) · the injection-dedup lib [[reference_injection_dedup_pattern_2026_05_23]].
