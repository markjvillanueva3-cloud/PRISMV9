---
name: reference_code_engine_ollamacontextfloorengine
description: "canonical PRISM brief on every local-LLM call."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/OllamaContextFloorEngine.ts
  sourceHash: 3b6a48e6
  loc: 337
aliases: OllamaContextFloorEngine
---

# OllamaContextFloorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/OllamaContextFloorEngine.ts` (337 LOC)

## What it is
canonical PRISM brief on every local-LLM call.

```
OllamaContextFloorEngine — canonical PRISM brief on every local-LLM call.

Prepends `state/shared/CLAUDE-BRIEF.md` as a system prompt to Ollama API
calls so deepseek-r1:14b and qwen2.5-coder:7b have the same baseline
awareness as Claude/Gemini/Codex without per-call repetition.

Design
------
- In-memory cache of the brief, refreshed when file mtime changes or after
  FRESH_TTL_MS (default 12h). Concurrent reads are safe.
- Skip-list of task tags that bypass injection (raw text formatting,
  embedding-only pipelines, deterministic stringification) — saves ~2K
  tokens per call when context isn't needed.
- Brief never throws — missing/empty file returns `{ briefIncluded: false }`
  with a reason string so callers can log a warning and continue.
- Oversize prompts (>1MB) throw with a descriptive error so Ollama isn't
  asked to swallow accidental dumps of source files.

Wiring
------
- Dis
```


## Exports
`BARE_OLLAMA_TAGS` (const), `BareOllamaTag` (type), `AwarenessMode` (type), `WrapInput` (type), `WrapResult` (interface), `StatusResult` (interface), `OllamaContextFloorEngine` (class), `ollamaContextFloorEngine` (const)

## Key methods
`wrap`, `status`, `refresh`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/OllamaContextFloorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find OllamaContextFloorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/OllamaContextFloorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
