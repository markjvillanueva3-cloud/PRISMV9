---
name: reference_oscar_sfc_combination_sweep_hardware_2026_06_24
description: "SFC combination/variability testing on the new 9950X3D+Blackwell hardware (slot:oscar, 2026-06-24). combination-sweep.ts WORKS (102K combos, accurate, 818x variability); fixed the parallel batch-run worker heap OOM; the 32-thread orchestrator sweep is blocked by a SECOND bug -- orchestrator import boots the MCP server. Plus a kicked-off CAM-catalog-rebuild Workflow."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.698Z
aliases: reference_oscar_sfc_combination_sweep_hardware_2026_06_24
---


**SFC COMBINATION/VARIABILITY TESTING on new hardware (slot:oscar, 2026-06-24).** Operator: "we bought this new cpu and gpu for the variable, combination tests for the sfc app. lets use them now." Host (CANONICAL-HOST-FACTS): AMD Ryzen 9 9950X3D 16c/**32-thread**, RTX PRO 6000 Blackwell **96GB VRAM**, 127GB RAM.

**1. COMBINATION TESTING PROVEN (works today):** `mcp-server/scripts/sfc-combination-sweep.ts` (UltimateSpeedFeedEngine, run `cd mcp-server && npx tsx scripts/sfc-combination-sweep.ts`) computed the full cartesian product **iso(6)xdia(3)xflutes(2)xtool_material(5)xcoolant(7)xrigidity(3)xoperation(3)xstrategy(3)xmode(3) = 102,060 combinations, 100% OK** in 6.2s (0.061 ms/cell). **800 distinct Vc**, global range **1..1040 m/min (818.9x)**. Per-axis variability: iso_group **7.93x**, tool_material **7.14x**, optimize_for 2.20x, operation 1.76x, rigidity 1.57x, coolant 1.47x, strategy 1.40x; tool_diameter + flutes **1.00x (correctly FLAT -- cutting SPEED Vc is independent of diameter; diameter only scales RPM)**. This is the "run combinations so we KNOW the calculations are accurate" deliverable, working + physically sane. UltimateSpeedFeedEngine imports CLEANLY (no server boot).

**2. SCALABLE PARALLEL HARNESS -- 1 bug fixed, 1 remaining.** `scripts/sfc-variability-batch-run.mjs` (the "generate the billions" streaming worker: `--workers N` spawns tsx child procs over the streaming generator enumerator, drives `SpeedFeedOrchestratorEngine.compute()` -- the heavier 9-axis orchestrator -- ~5ms/cell, persists JSONL, resumable via --skip). **FIXED (committed U-SFC-SWEEP-WORKER-HEAP):** workers OOM'd `ERR_WORKER_OUT_OF_MEMORY` at engine load (heavy 41K-tool catalog) -- the tsx worker_thread had default heap. Added `--max-old-space-size` (knob `PRISM_SFC_WORKER_HEAP_MB`, default 3072) as a node execArg AND NODE_OPTIONS so the tsx worker_thread inherits it. **REMAINING BLOCKER (the next unit):** with heap fixed the engine LOADS but importing `SpeedFeedOrchestratorEngine` (transitively) **BOOTS THE FULL MCP SERVER** ("Server running on stdio" + risk-tier/eventbus logs) -- pollutes worker stdout + blocks per-cell processing -> `total_processed:0`. The side effect is NOT in SpeedFeedOrchestratorEngine.ts (grep clean); it is transitive. FIX: find the server-auto-boot module in the orchestrator's import chain + guard it behind a main-module/env check (e.g. PRISM_MCP_NO_AUTOSTART), OR point the batch-run worker at a server-free compute import. Until then the 32-thread orchestrator sweep cannot run; the LIGHT-engine combination-sweep.ts is the working path (single-thread, fast). Sibling auditor: [[reference_oscar_sfc_accuracy_auditor_2026_06_23]]; parity readiness: [[reference_oscar_sfc_full_parity_readiness_2026_05_29]].

**3. CAM-CATALOG REBUILD (operator over-rode the route-to-kilo):** launched Workflow `wf_caa1288e-7a8` (opus build + sonnet adversarial verify) to reconstruct the exFAT-lost CAM programming-environment catalog (the 3 failing SFC calculator-coverage tests) -- fills `calculatorProgrammingCatalogExtensions.ts` stubs / wires into `calculatorWorkspace.ts`, spec pinned by `calculatorProgrammingCoverage.test.ts`. Running at session checkpoint -- check result next session. See [[reference_oscar_sfc_frontend_vitest_parallel_crash_2026_06_24]].

**4. 32-THREAD SWEEP NOW WORKING (committed U-SFC-PARALLEL-SWEEP).** Rather than wait on the orchestrator server-boot fix, built `mcp-server/scripts/sfc-parallel-combo-sweep.mjs` -- drives the LIGHT, server-free UltimateSpeedFeedEngine across N tsx child procs (each computes every Nth combo of the 850,500-combo product). LIVE on the 9950X3D: `--workers 24` ran the FULL 850,500 combos, **100% OK, 87.6s, Vc 1.3..1660 m/min (1277x)**. THROUGHPUT TUNING (fixed-cell): 4w=7732/s (1933/thread), 8w=9261/s, **16w=10919/s (PEAK)**, 24w=9711/s -- aggregate PEAKS at ~16 workers then DROPS (engine is memory/cache-bound: each worker loads the 41K-tool catalog). **Use `--workers 16` on this host (~10.9K cells/s; 850K in ~78s, 10M in ~15min).** This IS the operator's "use the new CPU/GPU for combination tests now" -- delivered + committed. The orchestrator (9-axis, heavier) batch-run still needs the server-boot fix for ITS richer axis set.

**NEXT:** (a) check the CAM-catalog Workflow result + commit if green; (b) scale the parallel sweep toward "billions" (expand axis grids + persist JSONL + run --workers 16 left-running) and feed the GPU/LoRA training (the Blackwell use); (c) fix the orchestrator-import server-boot side effect to unblock the richer 9-axis batch-run; (d) closed-loop compare vs HSMAdvisor/G-Wizard across the swept combos.
