---
name: reference_code_engine_masterpostfinetuningengine
description: "CAM-PARITY-AGI-MS0/U-CAMP15"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MasterPostFineTuningEngine.ts
  sourceHash: 75750a51
  loc: 1134
aliases: MasterPostFineTuningEngine
---

# MasterPostFineTuningEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MasterPostFineTuningEngine.ts` (1134 LOC)

## What it is
CAM-PARITY-AGI-MS0/U-CAMP15

```
MasterPostFineTuningEngine — CAM-PARITY-AGI-MS0/U-CAMP15
=========================================================

LoRA-style fine-tuning engine for post processors. Learns from actual vs predicted
G-code differences to continuously improve post-processor output quality.

CORE CAPABILITIES:
1. Record actual vs predicted G-code discrepancies
2. Store controller-specific parameter adjustments (LoRA "deltas")
3. Apply learned corrections to future post-processing
4. Track confidence per controller/operation combination
5. Export/import fine-tuning weights for model persistence
6. Provide feedback loop for continuous improvement

MATHEMATICAL FOUNDATIONS:
-------------------------
LoRA Adaptation Formula:
  W' = W + alpha * delta_W
  where delta_W = A * B^T (low-rank factorization)

Confidence Scoring:
  C = 1 - exp(-n/tau) * (1 + sigma/mu_err)^-1
  where n = sample count, tau = decay const
```


## Exports
`ControllerFamily` (type), `OperationType` (type), `ParameterType` (type), `DiscrepancyRecord` (interface), `LoRAWeights` (interface), `FineTunedParameters` (interface), `FineTunedGCode` (interface), `ConfidenceResult` (interface), `FineTuningExport` (interface), `FineTuningConfig` (interface), `MasterPostFineTuningEngine` (class), `masterPostFineTuningEngine` (const)

## Key methods
`recordActualVsPredicted`, `getFineTunedParameters`, `applyFineTuning`, `getConfidenceScore`, `exportFineTuningData`, `importFineTuningData`, `getStatistics`, `clear`, `getConfig`, `getHistory`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/MasterPostFineTuningEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MasterPostFineTuningEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MasterPostFineTuningEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
