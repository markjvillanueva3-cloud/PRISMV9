---
name: reference_code_engine_qdrantmemoryenginesingleton
description: "Singleton accessor + default Ollama embedder"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/QdrantMemoryEngineSingleton.ts
  sourceHash: 98978bb9
  loc: 325
aliases: QdrantMemoryEngineSingleton
---

# QdrantMemoryEngineSingleton -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/QdrantMemoryEngineSingleton.ts` (325 LOC)

## What it is
Singleton accessor + default Ollama embedder

```
QdrantMemoryEngineSingleton — Singleton accessor + default Ollama embedder

INTEL-OLLAMA-OBSIDIAN-MS0/P0-U01.

QdrantMemoryEngine is pluggable (see its `setEmbedder()` method); it does
not assume a specific embedding model. Until this unit, callers had to
inject an embedder themselves and there was no cross-call instance, so the
memory layer was orphaned (forge-audit: "5 highest-leverage reasoning
engines are orphaned").

This singleton:
  1. Holds a single QdrantMemoryEngine instance per process so dispatchers,
     hooks, and tests share the same in-memory `ensured` collection cache.
  2. Auto-injects an Ollama-backed embedder using `nomic-embed-text`
     (768-dim, matches QdrantMemoryEngine.DEFAULT_VECTOR_SIZE) on first
     access. Callers can override with `setEmbedder()` for tests.
  3. Talks to Ollama via raw `/api/embed` (the modern endpoint that returns
     `{embeddings: numbe
```


## Exports
`DEFAULT_OLLAMA_HOST` (const), `DEFAULT_EMBED_LANE_HOST` (const), `DEFAULT_EMBED_MODEL` (const), `EXPECTED_EMBED_DIM` (const), `MAX_EMBED_INPUT_CHARS` (const), `DEFAULT_EMBED_TIMEOUT_MS` (const), `DEFAULT_EMBED_LANE_PROBE_TIMEOUT_MS` (const), `DEFAULT_EMBED_KEEP_ALIVE` (const), `OllamaEmbedderOptions` (interface), `OllamaEmbedTarget` (interface), `QdrantMemoryEngineSingleton` (class)

## Key methods
`getInstance`, `setEmbedder`, `createOllamaEmbedder`, `resolveEmbedTarget`, `clearTimeout`, `reset`, `hasEmbedder`

## Depends on
[[reference_code_engine_qdrantmemoryengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/QdrantMemoryEngineSingleton.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find QdrantMemoryEngineSingleton` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/QdrantMemoryEngineSingleton.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
