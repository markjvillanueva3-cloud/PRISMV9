---
name: reference_code_lib_ensureheapfloor
description: "ensure-heap-floor.mjs — guarantee a minimum V8 old-space heap for child spawns."
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/ensure-heap-floor.mjs
  sourceHash: 3ed5342e
  loc: 39
aliases: ensureHeapFloor
---

# ensureHeapFloor -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/ensure-heap-floor.mjs` (39 LOC)

## What it is
```
ensure-heap-floor.mjs — guarantee a minimum V8 old-space heap for child spawns.

THE BUG IT FIXES (2026-06-09, confirmed via a live MCP outage). The MCP server
OOMs on boot — `FATAL ERROR: Reached heap limit Allocation failed` — because it
loads 4000+ tribal tips + registries + graph during its ~40-50s cold boot, far
exceeding the **384MB** `--max-old-space-size` cap that `portable-node` sets
(PRISM_HOOK_HEAP_MB default, fleet commit-budgeting). A child spawn INHERITS
that cap via `process.env.NODE_OPTIONS`, so any MCP spawn launched from a capped
context (e.g. `singleton-service-guard --fix` run through the node shim) crashes
the server on boot. The supervisor (mcp-server-supervisor.mjs) already floored
its spawn heap to 4096MB; the daemon helper (mcp-server-daemon.mjs) did NOT —
the two spawn paths diverged. This shared floor unifies them (06-04 plan FIX-7).

Proven: a manual boot with
```


## Exports
`ensureHeapFloor` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/ensure-heap-floor.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find ensureHeapFloor` or `prism_session:master_index_query`; open the source at `scripts/lib/ensure-heap-floor.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
