---
name: reference_code_engine_camloraadaptertrainerengine
description: "U-CAM-ML-05"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMLoRAAdapterTrainerEngine.ts
  sourceHash: 7dea9318
  loc: 686
aliases: CAMLoRAAdapterTrainerEngine
---

# CAMLoRAAdapterTrainerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMLoRAAdapterTrainerEngine.ts` (686 LOC)

## What it is
U-CAM-ML-05

```
CAMLoRAAdapterTrainerEngine — U-CAM-ML-05
===========================================

Per-CAM LoRA (Low-Rank Adaptation) adapters that correct the U-CAM-ML-04
baseline Bayesian regressor with CAM-specific deltas.

Architecture:
  y_hat = baseline_pred(x) + (B · A · x_std) · (alpha / rank)

Where:
  - baseline_pred(x): frozen Bayesian ridge prediction from U-CAM-ML-04
  - A: r × d matrix (low-rank down-projection), initialized N(0, 0.01)
  - B: 1 × r matrix (up-projection to scalar target), initialized 0
  - r = rank (default 4 per roadmap convention)
  - alpha = scaling (default 8 → alpha/rank = 2)

Training: batched gradient descent on per-CAM residuals:
  r_i = y_i - baseline_pred(x_i)
  loss = (1/N) Σ (r_i - Δ(x_i))^2

Because B starts at zero, Δ(x)=0 at the start — the adapter cannot make
predictions worse than baseline until it learns something useful.

This trains FOUR priority-4 
```


## Exports
`Priority4CAM` (type), `LoRAConfig` (interface), `DEFAULT_LORA_CONFIG` (const), `LoRAAdapter` (interface), `AdapterEvalMetrics` (interface), `TrainAllResult` (interface), `ObservedOutcome` (interface), `ObservationStatus` (interface), `DEFAULT_OBSERVATION_CAP` (const), `CAMLoRAAdapterTrainerEngine` (class), `camLoRAAdapterTrainerEngine` (const)

## Key methods
`enableOutcomeObservation`, `disableOutcomeObservation`, `isObservingOutcomes`, `getObservationBuffer`, `getObservationStatus`, `clearObservations`, `trainAll`, `trainFromFiles`, `predictWithAdapter`

## Depends on
[[reference_code_engine_camfeatureextractorengine]] | [[reference_code_engine_cammlsplitengine]] | [[reference_code_engine_cambaselineregressorengine]] | [[reference_code_engine_feedbackbusengine]] | [[reference_code_engine_crossprocessoutcomestore]]

## Where it lives
- **Source:** `mcp-server/src/engines/CAMLoRAAdapterTrainerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMLoRAAdapterTrainerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMLoRAAdapterTrainerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
