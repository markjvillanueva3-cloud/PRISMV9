---
name: reference_code_engine_bidwincalibratorengine
description: "BidWinCalibratorEngine — logistic-regression bid/win calibrator for markup optimization."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/BidWinCalibratorEngine.ts
  sourceHash: d146a3b4
  loc: 360
aliases: bidWinCalibratorEngine
---

# bidWinCalibratorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/BidWinCalibratorEngine.ts` (360 LOC)

## What it is
```
BidWinCalibratorEngine — logistic-regression bid/win calibrator for markup optimization.

Axis D of HOTEL-SHOP-OPS-GAP-MATRIX: predict P(win | markup, tier, qty) from historical
quote outcomes, then solve for the markup that maximizes expected margin contribution.

MODEL — 4-feature logistic regression:
  P(win | x) = σ(β₀ + β₁·markup_pct + β₂·tier_score + β₃·log_qty)
  where σ(z) = 1 / (1 + e^(-z))

Features:
  - markup_pct        — markup over cost as fraction (0.30 = 30% markup)
  - tier_score        — customer relationship strength (0=cold lead, 1=anchor account)
  - log_qty           — log10(quantity) to compress order-size range

Coefficient signs (typical):
  - β₀ free baseline
  - β₁ < 0 (higher markup → lower P(win))
  - β₂ > 0 (stronger relationship → higher P(win))
  - β₃ > 0 (larger orders typically more competitive bidding)

OPTIMIZATION — find markup_pct that maximizes expe
```


## Exports
`BidOutcome` (interface), `BidWinModel` (interface), `WinPrediction` (interface), `OptimalMarkup` (interface), `bidWinCalibratorEngine` (const)

## Key methods
`recordOutcome`, `calibrate`, `predictWin`, `optimalMarkup`, `getModel`, `outcomeCount`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/BidWinCalibratorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find bidWinCalibratorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/BidWinCalibratorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
