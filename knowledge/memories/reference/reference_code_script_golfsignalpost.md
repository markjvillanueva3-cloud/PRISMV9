---
name: reference_code_script_golfsignalpost
description: "golf-signal-post.mjs — CLEANUP-MS0 / U-CLEANUP-F8"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/golf-signal-post.mjs
  sourceHash: d075e0ea
  loc: 414
aliases: golf-signal-post.mjs
---

# golf-signal-post.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/golf-signal-post.mjs` (414 LOC)

## What it is
```
golf-signal-post.mjs — CLEANUP-MS0 / U-CLEANUP-F8

Unified "golf-signal" channel: a thin write surface for the 7th hygiene
chat slot to post compact digests onto the chat bus. Routes through TWO
surfaces in lockstep:

  1. `chat_bus_signals` table in state/shared/coordination.db (the B10
     LedgerStoreEngine's structured table — readable by all chats via
     `agent-coordination.mjs`).
  2. state/shared/AGENT_CHAT.jsonl (the legacy line-oriented bus that the
     `chat-bus-inject.mjs` UserPromptSubmit hook reads on every prompt).

Both writes are gated by a rate cap: NO MORE THAN MAX_POSTS_PER_WINDOW
golf-signal entries per CHAT_BUS_INJECT_WINDOW_MS. Why a cap: the inject
hook surfaces "recent chat-bus" content into every chat's context — an
unbounded golf signal stream would crowd out human-authored peer messages.

Format contract — golf-signal payloads are DIGESTS, not full findings:
```


## Exports
`SCHEMA_VERSION` (const), `SIGNAL_TYPE` (const), `MAX_POSTS_PER_WINDOW` (const), `CHAT_BUS_INJECT_WINDOW_MS` (const), `DEFAULT_REPO_ROOT` (const), `DEFAULT_DB_RELATIVE` (const), `DEFAULT_BUS_JSONL_RELATIVE` (const), `PAYLOAD_MAX_BYTES` (const), `SUMMARY_MAX_CHARS` (const), `parseArgs` (function), `resolvePaths` (function), `validateSignalInput` (function), `buildPayload` (function), `countRecentSignals` (function), `postGolfSignal` (function), `runCli` (function)

## Key methods
`jsonlAppender`, `VALUES`, `appendFileSync`, `runCli`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/golf-signal-post.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find golf-signal-post.mjs` or `prism_session:master_index_query`; open the source at `scripts/golf-signal-post.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
