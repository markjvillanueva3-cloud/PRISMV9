---
name: reference_code_engine_contextualstrategyoverrideengine
description: "Contextual Strategy Override"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ContextualStrategyOverrideEngine.ts
  sourceHash: fbf3a4a1
  loc: 676
aliases: ContextualStrategyOverrideEngine
---

# ContextualStrategyOverrideEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ContextualStrategyOverrideEngine.ts` (676 LOC)

## What it is
Contextual Strategy Override

```
ContextualStrategyOverrideEngine (E1111) — CAMX-MS12 U05

Handles edge cases where standard strategy selection must be overridden by
physical constraints.  Ten hard-override rules cover the most common failure
modes in production machining.

Rules (all mandatory unless noted):
  1. thin_wall          — wall_thickness < 2 mm → force ae < 3 %D, climb, spring passes
  2. deep_bore          — L/D > 10              → force peck / BTA, ban conventional drill
  3. fragile_material   — ISO H (>55 HRC) or ceramic/composite → low engagement, CBN/PCD
  4. prototype_batch    — qty ≤ 3               → conservative params (SF ×1.5), proven strategies
  5. hard_material      — HRC > 45              → HSM, ban conventional, min 4-flute
  6. soft_gummy         — ISO M (SS/Ti)         → chip breaker, higher feed, no rubbing
  7. micro_feature      — feature_dim < 1 mm    → micro strategies, high RPM, air 
```


## Exports
`OverrideFeature` (interface), `OverrideMaterial` (interface), `OverrideTool` (interface), `OverrideStrategy` (interface), `OverrideAdjustment` (interface), `OverrideResult` (interface), `MachiningParams` (interface), `AdjustedParams` (interface), `OverrideRuleDescription` (interface), `ContextualStrategyOverrideEngine` (class), `contextualStrategyOverrideEngine` (const)

## Key methods
`checkOverrides`, `applyOverrides`, `listRules`, `adj`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ContextualStrategyOverrideEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ContextualStrategyOverrideEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ContextualStrategyOverrideEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
