---
name: reference_code_engine_redisstreamsink
description: "RedisStreamSink -- fail-soft Redis Streams durable transport for EventBusEngine."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/RedisStreamSink.ts
  sourceHash: 1cb71f93
  loc: 309
aliases: RedisStreamSink
---

# RedisStreamSink -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/RedisStreamSink.ts` (309 LOC)

## What it is
RedisStreamSink -- fail-soft Redis Streams durable transport for EventBusEngine.

```
RedisStreamSink -- fail-soft Redis Streams durable transport for EventBusEngine.

Phase 3 of INFRA-SYNERGY-RESEARCH-2026-06-25 (slot:bravo). This is the
durable BACKEND that the in-memory EventBusEngine optionally fans out to (it
is NOT a competing event bus -- the wired EventBusEngine stays the one bus;
see INFRA-SYNERGY-PHASE3-EVENTBUS-DESIGN.md). Redis Streams consumer groups
give each consumer its OWN durable offset -> per-group lag (XPENDING) instead
of the file bus's single global "unread" (the AGENT_CHAT.jsonl 29,405 symptom),
and one published event fans out to N independent groups.

No new dependency: `ioredis` is already wired (RedisCacheProvider.ts).

FAIL-SOFT CONTRACT (mirrors RedisCacheProvider's graceful degradation): every
op returns a structured { ok:false } when Redis is unreachable and NEVER
throws on I/O. Input validation (bad ARGS) throws. The core is injectable
(att
```


## Exports
`SinkEvent` (interface), `AppendResult` (interface), `ConsumedEvent` (interface), `ConsumeResult` (interface), `AckResult` (interface), `PendingResult` (interface), `ConsumeOptions` (interface), `RedisStreamClient` (interface), `RedisStreamSink` (class), `redisStreamSink` (const)

## Key methods
`xadd`, `xgroup`, `xreadgroup`, `xack`, `xpending`, `on`, `connect`, `attachClient`, `append`, `consume`, `ack`, `pending`, `shutdown`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/RedisStreamSink.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find RedisStreamSink` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/RedisStreamSink.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
