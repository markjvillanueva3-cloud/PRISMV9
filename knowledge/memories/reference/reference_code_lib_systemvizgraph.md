---
name: reference_code_lib_systemvizgraph
description: "system-viz-graph — load-once + in-process-cache lib for the system-viz graph."
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/system-viz-graph.mjs
  sourceHash: ceb1bea8
  loc: 818
aliases: system-viz-graph.mjs
---

# system-viz-graph.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/system-viz-graph.mjs` (818 LOC)

## What it is
```
system-viz-graph — load-once + in-process-cache lib for the system-viz graph.

Extracted from scripts/system-viz-query.mjs so a single process can load
the ~24 MB graph ONCE and query it many times without re-parsing per call.

P1 / U-CACHE-LIB (SYSTEM-VIZ-UPGRADES-MS0): loadGraph() now keeps a
module-scope cache keyed on the graph file's mtime + size. A second
same-process call with an unchanged file returns the cached object
(no 24 MB re-parse). Invalidation is conservative — stat is taken
BEFORE the read so a concurrent rewrite can only ever cause a *false
miss* (an extra re-parse), never a *false hit* (serving stale bytes).

CONTRACT AMENDMENT -- loadFindCache serve-stale-then-async-heal (2026-06-09,
sierra, find-cache OOM durable fix): loadGraph()'s "never serve stale bytes"
invariant above STILL HOLDS for loadGraph itself. But loadFindCache()'s
stale/absent FALLBACK no longer falls
```


## Exports
`loadGraph` (function), `readGraphMeta` (function), `findInGraph` (function), `loadFindCache` (function), `regenFindCache` (function), `sidecarStatus` (function), `__test` (const)

## Key methods
`spawnDebouncedRegen`, `writeSidecarAtomic`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/system-viz-graph.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find system-viz-graph.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/system-viz-graph.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
