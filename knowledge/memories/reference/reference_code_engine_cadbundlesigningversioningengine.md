---
name: reference_code_engine_cadbundlesigningversioningengine
description: "U-FS-14 (PHASE-47)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADBundleSigningVersioningEngine.ts
  sourceHash: 00210ca9
  loc: 326
aliases: CADBundleSigningVersioningEngine
---

# CADBundleSigningVersioningEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADBundleSigningVersioningEngine.ts` (326 LOC)

## What it is
U-FS-14 (PHASE-47)

```
CADBundleSigningVersioningEngine — U-FS-14 (PHASE-47)

Sigstore-cosign-inspired bundle signing.

Policy:
  - Each bundle finalize call produces exactly one signature.
  - Signatures are computed over the tuple (bundleId, version, digest,
    predicate, previousDigest) canonicalised to JSON and hashed with
    SHA-256.
  - "ed25519-hmac" mode is a production-safe deterministic surrogate that
    keeps the schema honest without shipping Node crypto into the unit-test
    path: HMAC-SHA-256 with a per-key seed substitutes for an Ed25519
    signature. Real Ed25519 wiring is a drop-in replacement on the same
    interface.
  - Semver bumps (major/minor/patch) are enforced: you can't ship a patch
    that adds an operation type, you can't revert a release to an older
    version.
  - Each signature chains to the previous one by digest, giving a
    tamper-evident history.

@module engines/CAD
```


## Exports
`SigningClock` (interface), `CADBundleSigningVersioningEngine` (class), `cadBundleSigningVersioningEngine` (const)

## Key methods
`now`, `createKey`, `importKey`, `publicKey`, `sign`, `verify`, `verifyChain`, `signaturesFor`, `latest`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CADBundleSigningVersioningEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADBundleSigningVersioningEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADBundleSigningVersioningEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
