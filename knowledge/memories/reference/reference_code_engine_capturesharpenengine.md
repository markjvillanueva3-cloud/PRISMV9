---
name: reference_code_engine_capturesharpenengine
description: "sentence length within healthline range"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CaptureSharpenEngine.ts
  sourceHash: b2c9f750
  loc: 248
aliases: CaptureSharpenEngine
---

# CaptureSharpenEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CaptureSharpenEngine.ts` (248 LOC)

## What it is
sentence length within healthline range

```
CaptureSharpenEngine
====================

OBSIDIAN-CONTENT-MS2/U-CAPTURE-SHARPEN

Takes a raw inbox capture (a voice-memo transcript, a mid-day observation,
a pasted snippet) and assesses whether it meets the cyrilXBT JARVIS
sharpened-note bar:

  "If the sharpened note still needs explanation, it is not sharp enough.
   A good sharpened note reads like a headline, not a sentence from a
   paragraph."

The engine does NOT rewrite the capture (LLMs do that downstream). It
scores the capture across measurable dimensions and either accepts (the
capture stands alone) or rejects (the capture still needs explanation).
It also classifies the capture into one of the 5 cyrilXBT subfolders:

  observations / reactions / patterns / questions / numbers

and proposes 3 tags (content_pillar / note_type / topical) per the
article's process-inbox skill contract.

Score components (each in [0, 1]):

  l
```


## Exports
`CaptureType` (type), `SharpenAssessment` (interface), `SharpenOptions` (interface), `CaptureSharpenEngine` (class), `captureSharpenEngine` (const)

## Key methods
`assess`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CaptureSharpenEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CaptureSharpenEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CaptureSharpenEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
