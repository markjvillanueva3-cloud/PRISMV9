---
name: reference_code_script_fleetworkdigest
description: "fleet-work-digest.mjs -- one COMPACT cross-fleet 'what every chat built / shipped /"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/fleet-work-digest.mjs
  sourceHash: 4fc28220
  loc: 300
aliases: fleet-work-digest.mjs
---

# fleet-work-digest.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/fleet-work-digest.mjs` (300 LOC)

## What it is
```
fleet-work-digest.mjs -- one COMPACT cross-fleet "what every chat built / shipped /
is working on now" digest, so any chat slot can know the whole fleet's state from
~35 lines instead of reading 26 x ~215-line consolidated handoffs (the token blowup).

THE GAP (operator, 2026-06-15): PRISM already has fleet-status.mjs (live activity/topic
per slot), per-slot consolidated handoffs (215 lines each), and the galaxy MASTER-DIGEST
(per-DOMAIN, not per-chat-current-work). What was missing: a single token-cheap digest
combining, per slot -- WORKING ON NOW (chat-slots topic/activity) + BUILT/SHIPPED recently
(commit subjects on the slot's branch, already shaped `[SCOPE]/U-ID: title`). This script
is that aggregator. It is the "aggregate-once" half of aggregate-once -> inject-compact
(fleet-work-digest-inject.mjs) -> drill-down-on-demand (slot-query.mjs <slot>).

Token math: this digest is ~35 li
```


## Exports
`parseUnitId` (function), `trunc` (function), `isActiveSlot` (function), `buildSlotLine` (function), `composeDigest` (function), `isSafeBranch` (function), `gitSubjects` (function), `gitLastSubject` (function), `resolveBranch` (function), `buildModel` (function), `buildDigest` (function)

## Key methods
`execFileSync`, `mkdirSync`, `writeFileSync`, `renameSync`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/fleet-work-digest.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find fleet-work-digest.mjs` or `prism_session:master_index_query`; open the source at `scripts/fleet-work-digest.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
