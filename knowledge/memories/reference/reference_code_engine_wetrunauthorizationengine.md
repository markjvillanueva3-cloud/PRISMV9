---
name: reference_code_engine_wetrunauthorizationengine
description: "Wet Run Authorization"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WetRunAuthorizationEngine.ts
  sourceHash: 6edfd3f4
  loc: 320
aliases: WetRunAuthorizationEngine
---

# WetRunAuthorizationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WetRunAuthorizationEngine.ts` (320 LOC)

## What it is
Wet Run Authorization

```
WetRunAuthorizationEngine (U-LPR-WETRUN-AUTH, Delivery B3)

Top-level authorization gate for every wet-run session in the
Lathe-Prod pilot. Composes three independent verdicts into a single
go / no-go / dry-run decision with per-gate attribution, so ops can
see exactly WHICH gate blocked a run at the moment of authorization.

Upstream gates:

  G1 (KICKOFF)   — PreMOUKickoffChecklistEngine.canKickoff().
    All 10 canonical items (or their project-specific extensions)
    must be verified or have active (non-expired) waivers. Critical
    items (NDA, scope, CAD/CAM approval) never waivable.

  G2 (MOU)       — MOUStallGateEngine.computeGate().
    - mode=full         ⇒ G2 passes
    - mode=dry_run_only ⇒ G2 allows dry-runs, blocks wet-runs
    - mode=blocked      ⇒ G2 blocks everything

  G3 (REGRESSION) — RegressionBaselineEngine.evaluate() verdict.
    passed=false ⇒ G3 blocks wet-runs
```


## Exports
`RunMode` (type), `AuthVerdict` (type), `GateId` (type), `OverrideToken` (interface), `AuthRequest` (interface), `GateAttribution` (interface), `AuthDecision` (interface), `AuthLogEntry` (interface), `WetRunAuthorizationEngine` (class), `wetRunAuthorizationEngine` (const)

## Key methods
`authorize`, `listLog`, `toSnapshot`, `loadSnapshot`, `clearAll`

## Depends on
[[reference_code_engine_premoukickoffchecklistengine]] | [[reference_code_engine_moustallgateengine]] | [[reference_code_engine_regressionbaselineengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/WetRunAuthorizationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WetRunAuthorizationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WetRunAuthorizationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
