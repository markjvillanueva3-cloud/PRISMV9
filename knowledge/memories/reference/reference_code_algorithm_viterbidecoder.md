---
name: reference_code_algorithm_viterbidecoder
description: "ViterbiDecoder — exact maximum-a-posteriori (MAP) decoding of the most likely"
metadata:
  type: reference
  kind: code-algorithm
  galaxy: unassigned
  sourcePath: mcp-server/src/algorithms/ViterbiDecoder.ts
  sourceHash: 9daa9cef
  loc: 252
aliases: ViterbiDecoder
---

# ViterbiDecoder -- Algorithm SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/algorithms/ViterbiDecoder.ts` (252 LOC)

## What it is
```
ViterbiDecoder — exact maximum-a-posteriori (MAP) decoding of the most likely
hidden state sequence of a Hidden Markov Model (Viterbi 1967).

Given a sequence of observations o₁..o_T and an HMM (π start probs, A state
transitions, B state→observation emissions), returns argmax over state paths
s₁..s_T of P(s, o):

  δ₁(j)  = π_j · B_j(o₁)
  δ_t(j) = max_i [ δ_{t-1}(i) · A_{ij} ] · B_j(o_t)
  path   = backtrace of the argmax pointers from δ_T

The whole recursion runs in LOG SPACE (sums of logs, max instead of product)
so it cannot underflow on long sequences — the standard numerically-stable
formulation. log(0) = −Infinity cleanly encodes impossible transitions.

Exact dynamic program — O(T·N²), no iteration / convergence / precision
tolerance (unlike SVD or attention). It is the canonical "deep reasoning"
primitive for probabilistic sequence inference: alarm-state decoding from
sensor s
```


## Exports
`ViterbiInput` (interface), `ViterbiOutput` (interface), `ViterbiDecoder` (const)

## Key methods
`validate`, `calculate`, `getMetadata`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/algorithms/ViterbiDecoder.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Algorithm

## How to query it
Recall by meaning via `/system-viz find ViterbiDecoder` or `prism_session:master_index_query`; open the source at `mcp-server/src/algorithms/ViterbiDecoder.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
