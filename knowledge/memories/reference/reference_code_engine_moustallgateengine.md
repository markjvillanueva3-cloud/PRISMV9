---
name: reference_code_engine_moustallgateengine
description: "registered, waiting on signature before T+deadline."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MOUStallGateEngine.ts
  sourceHash: 85a65f7d
  loc: 331
aliases: MOUStallGateEngine
---

# MOUStallGateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MOUStallGateEngine.ts` (331 LOC)

## What it is
registered, waiting on signature before T+deadline.

```
MOUStallGateEngine (U-LPR-STALL-MOU, Delivery B2)

Contractual-blocker tracker for the Lathe-Prod pilot. If the MOU (or
any other dependency marked with a T+N deadline) is still unsigned at
`kickoff_ts + deadline_ms`, this engine auto-flips the affected work
into `dry_run_only` mode and records the list of units that get
deferred to the post-MS0 backlog. When the blocker signs, the engine
clears the stall and re-activates the deferred units for an explicit
"resume" decision (resume is not automatic — ops has to acknowledge).

States per blocker:

  open          — registered, waiting on signature before T+deadline.
  stalled       — T+deadline elapsed, status still unsigned. All
                  dependent units are deferred. New reads against the
                  gate report `mode=dry_run_only`.
  signed        — signed before deadline OR signed after stall (which
                  aut
```


## Exports
`BlockerStatus` (type), `GateMode` (type), `BlockerRegistration` (interface), `BlockerRecord` (interface), `GateDecision` (interface), `MOUStallGateEngine` (class), `mouStallGateEngine` (const)

## Key methods
`register`, `sign`, `waive`, `cancel`, `resumeDeferredUnits`, `sweepDeadlines`, `computeGate`, `getBlocker`, `listBlockers`, `toSnapshot`, `loadSnapshot`, `clearAll`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MOUStallGateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MOUStallGateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MOUStallGateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
