---
name: reference_code_engine_minbatchextractorengine
description: "LATHE-PROD-READY-MS0/U-LPR27"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MINBatchExtractorEngine.ts
  sourceHash: 32b0ad0d
  loc: 540
aliases: MINBatchExtractorEngine
---

# MINBatchExtractorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MINBatchExtractorEngine.ts` (540 LOC)

## What it is
LATHE-PROD-READY-MS0/U-LPR27

```
MINBatchExtractorEngine — LATHE-PROD-READY-MS0/U-LPR27
=======================================================

Bounded-concurrency batch parser for the JM Die `.MIN` corpus
(5,297 production Okuma OSP programs).  The unit spec is explicit:

  • Bounded worker pool: `min(os.availableParallelism()-1, 16)` (ceiling raised 8→16 for the 9950X3D2 16C/32T; HARDWARE-DRIVE-SYNC-AUDIT-2026-06-08 §3.3).
  • Backpressure: in-flight count never exceeds the pool size.
  • Per-file byte cap (default 32 MiB; spec allows up to 64).
  • Checkpoint every N completions (default 250).
  • Resumable: a re-run with the same `runId` skips already-completed
    file IDs without reparsing.

Design contract (every choice exists for a reason):

  1. **No p-limit dependency.**  The codebase is under heavy concurrent
     edit and adding a new dep would conflict with peer chats.  A 30-line
     handwritten async sem
```


## Exports
`MINBatchTaskSchema` (const), `MINBatchTask` (type), `MINBatchPerFileResultSchema` (const), `MINBatchPerFileResult` (type), `MINBatchCheckpointSchema` (const), `MINBatchCheckpoint` (type), `MINBatchResultSchema` (const), `MINBatchResult` (type), `DEFAULT_MAX_BYTES_PER_FILE` (const), `DEFAULT_CHECKPOINT_EVERY` (const), `SCHEMA_VERSION` (const), `defaultConcurrency` (function), `discoverFilesByExt` (function), `deriveFileId` (function), `extractCustomerFromPath` (function), `buildCheckpointPath` (function), `loadCheckpoint` (function), `saveCheckpoint` (function), `concurrencyLimiter` (function), `MINBatchOptions` (interface)

## Key methods
`resolve`, `release`, `extractBatch`, `saveCheckpoint`, `limiter`, `recordResult`

## Depends on
[[reference_code_engine_minfileparserengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/MINBatchExtractorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MINBatchExtractorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MINBatchExtractorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
