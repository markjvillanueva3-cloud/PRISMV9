---
name: reference_code_engine_millloramodelselectorengine
description: "MillLoRAModelSelectorEngine — Context-aware Model Selection (Mill parity)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MillLoRAModelSelectorEngine.ts
  sourceHash: 3ec0c911
  loc: 442
aliases: millLoRAModelSelectorEngine
---

# millLoRAModelSelectorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MillLoRAModelSelectorEngine.ts` (442 LOC)

## What it is
```
MillLoRAModelSelectorEngine — Context-aware Model Selection (Mill parity)
==========================================================================

Selects the best MillLoRA adapter(s) for a given inference context.
Uses mill-canonical specialization metadata, historical performance,
load, and mill-canonical history signals (chatter / FPA / TCPM).

Mill parity for LatheLoRAModelSelectorEngine (LATHE-LORA-MS0 U-LLR43)
with MILL-CANONICAL extensions:

  - Mill-canonical specialization tag vocabulary (replaces the lathe
    threading/turning/facing/okuma/fanuc set) covering mill ops,
    mill controllers, materials, and feature families
  - axis_mode tag on every model AND in selection context — the
    context's axis_mode is a HARD filter (5-axis context only
    selects 5_axis_simul or shared models)
  - Mill-canonical history signals influence scoring:
      chatter_history (downweight
```


## Exports
`MillSpecializationTag` (type), `MillAxisMode` (type), `MillModelDescriptor` (interface), `MillSelectionContext` (interface), `MillSelectionResult` (interface), `MillSelectorConfig` (interface), `millLoRAModelSelectorEngine` (const), `MillLoRAModelSelectorEngine` (re-export)

## Key methods
`setConfig`, `getConfig`, `registerModel`, `unregisterModel`, `recordOutcome`, `recordMillOutcome`, `scoreModel`, `select`, `release`, `getModels`, `getModel`, `findBySpecialization`, `findByAxisMode`, `getStats`, `getSummary`, `clearHistory`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MillLoRAModelSelectorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find millLoRAModelSelectorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MillLoRAModelSelectorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
