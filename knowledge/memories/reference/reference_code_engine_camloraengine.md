---
name: reference_code_engine_camloraengine
description: "Fine-tuning adapter framework for CAM knowledge."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMLoRAEngine.ts
  sourceHash: 008881a3
  loc: 616
aliases: CAMLoRAEngine
---

# CAMLoRAEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMLoRAEngine.ts` (616 LOC)

## What it is
Fine-tuning adapter framework for CAM knowledge.

```
CAMLoRAEngine — U-CAM107 (PHASE-8 / CAM-EXHAUST-MS0)
=====================================================

Fine-tuning adapter framework for CAM knowledge.

This engine is the inference-time + management layer that sits ABOVE
the existing CAMLoRAAdapterTrainerEngine (which trains adapters).

  CAMLoRAAdapterTrainerEngine  → produces  → LoRAAdapter
  CAMLoRAEngine                → consumes →  LoRAAdapter (predict / list / validate)

Why a separate engine?
  - Trainer is heavy: needs full baseline + training split + epochs.
  - Inference is lightweight: just A·B·x·(α/r) plus baseline weights·x.
  - The roadmap (U-CAM107) calls for a framework that downstream units
    U-CAM108..U-CAM111 train into, and U-CAM112 (Ollama) consumes from.

Math (LoRA forward pass, identical to trainer):
    y_hat = baseline·x_std + (B · A · x_std) · (α / r)
  where A is r×d, B is 1×r (stored as length-r vecto
```


## Exports
`AdapterPredictionInput` (interface), `AdapterPredictionResult` (interface), `AdapterValidationResult` (interface), `AdapterHealthReport` (interface), `AdapterIndexEntry` (interface), `EnsembleInput` (interface), `EnsembleResult` (interface), `STALE_AGE_DAYS` (const), `HARD_STALE_AGE_DAYS` (const), `MIN_IMPROVEMENT_PCT` (const), `getAllowedAdapterRoots` (function), `ALLOWED_ADAPTER_ROOTS` (const), `CAMLoRAEngine` (class), `camLoRAEngine` (const)

## Key methods
`loadAdapter`, `saveAdapter`, `predict`, `applyDelta`, `validateAdapterFor`, `checkAdapter`, `listAdapters`, `listAdaptersWithWarnings`, `selectAdapter`, `ensemble`, `standardize`, `defaultConfig`

## Depends on
[[reference_code_engine_camloraadaptertrainerengine]] | [[reference_code_engine_cambaselineregressorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CAMLoRAEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMLoRAEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMLoRAEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
