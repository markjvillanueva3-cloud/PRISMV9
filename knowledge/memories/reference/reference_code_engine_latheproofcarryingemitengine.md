---
name: reference_code_engine_latheproofcarryingemitengine
description: "LatheProofCarryingEmitEngine — U-LSR25 (LATHE-HARDENED-MS0)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LatheProofCarryingEmitEngine.ts
  sourceHash: 752e67d1
  loc: 452
aliases: SafetyProofViolation
---

# SafetyProofViolation -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LatheProofCarryingEmitEngine.ts` (452 LOC)

## What it is
```
LatheProofCarryingEmitEngine — U-LSR25 (LATHE-HARDENED-MS0)

Composes Phase-A safety gates into a single SafetyRecord attached to the
emitted G-code program. An independent reproducer can re-run the same
inputs and byte-compare the record to detect regressions or tampering.

── Gates composed (all optional — applied only when inputs are supplied) ─
  • U-LSR04 envelope hard-block (always — toolpath carries its own check)
  • U-LSR05 spindle torque gate (when machine spec supplied)
  • U-LSR06 stock boundary gate (when stock + workholding supplied)
  • U-LSR22 formal safety predicate (when LatheSafetySignals supplied)

── Result ──────────────────────────────────────────────────────────────
Returns { emitted, safety_record } when SAFE/WARNING. Throws
SafetyProofViolation on BLOCKED unless `allow_override=true`, in which
case the emission proceeds with an AUDIT trail on warnings and the
sa
```


## Exports
`SafetyInputsSchema` (const), `SafetyInputs` (type), `GateSignatureSchema` (const), `GateSignature` (type), `SafetyRecordSchema` (const), `SafetyRecord` (type), `ProofCarryingEmitResultSchema` (const), `ProofCarryingEmitResult` (type), `ProofEmitInputSchema` (const), `ProofEmitInput` (type), `SafetyProofViolation` (class), `latheProofCarryingEmitEngine` (const), `LatheProofCarryingEmitEngine` (re-export)

## Key methods
`emit`, `reproduce`

## Depends on
[[reference_code_engine_latheprinttoolpathgeneratorengine]] | [[reference_code_engine_latheprintprogramemitterengine]] | [[reference_code_engine_spindletorquegateengine]] | [[reference_code_engine_stockboundarygateengine]] | [[reference_code_engine_lathesafetypredicateengine]] | [[reference_code_engine_lathesafetysignalengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/LatheProofCarryingEmitEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SafetyProofViolation` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LatheProofCarryingEmitEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
