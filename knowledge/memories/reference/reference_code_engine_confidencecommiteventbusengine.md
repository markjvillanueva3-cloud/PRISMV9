---
name: reference_code_engine_confidencecommiteventbusengine
description: "ConfidenceCommitEventBusEngine — CAM-ML-CLOSEDLOOP-MS0 U-CMCCL12"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ConfidenceCommitEventBusEngine.ts
  sourceHash: cc99ca29
  loc: 212
aliases: confidenceCommitEventBusEngine
---

# confidenceCommitEventBusEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ConfidenceCommitEventBusEngine.ts` (212 LOC)

## What it is
```
ConfidenceCommitEventBusEngine — CAM-ML-CLOSEDLOOP-MS0 U-CMCCL12
=================================================================

Event bus for orchestrator confidence-commit events. CAMAGIMaster
(or any router) publishes a `confidence_commit` event after every
routing decision:

  { decisionId, reasoningMode, chosenCam, confidence, timestamp,
    outcomePlaceholder: null }

Subscribers (e.g. OutcomeAccumulatorEngine) register via `subscribe`
and receive events synchronously. When the actual outcome is later
observed, callers invoke `attachOutcome(decisionId, outcome)` and all
subscribers are re-notified with the resolved pair.

Schema is versioned. Event bus is in-memory + per-process — callers
persist externally if needed.

@module engines/ConfidenceCommitEventBusEngine
@version 1.0.0
```


## Exports
`CONFIDENCE_EVENT_SCHEMA_VERSION` (const), `ReasoningMode` (type), `CAMSystem` (type), `ConfidenceCommitEvent` (interface), `OutcomeObservation` (interface), `ResolvedCommitEvent` (interface), `CommitSubscriber` (type), `ResolvedSubscriber` (type), `confidenceCommitEventBusEngine` (const), `ConfidenceCommitEventBusEngine` (type)

## Key methods
`commit`, `attachOutcome`, `subscribe`, `pendingCount`, `resolvedCount`, `listResolved`, `reset`, `supportedCAMs`, `supportedReasoningModes`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ConfidenceCommitEventBusEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find confidenceCommitEventBusEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ConfidenceCommitEventBusEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
