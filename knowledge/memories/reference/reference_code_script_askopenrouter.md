---
name: reference_code_script_askopenrouter
description: "ask-openrouter.mjs -- OpenRouter cloud query service"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/ask-openrouter.mjs
  sourceHash: 090f3eee
  loc: 325
aliases: ask-openrouter.mjs
---

# ask-openrouter.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/ask-openrouter.mjs` (325 LOC)

## What it is
```
ask-openrouter.mjs -- OpenRouter cloud query service
(CLOUD-OVERFLOW-MS0/U-ASK-OPENROUTER, slot:alpha 2026-06-15).

Operator directive (2026-06-15): "wire cloud version, network is 1gb/sec". The
executable counterpart to ask-ollama.mjs, but for the CLOUD long-context tier:
routes deep-research / huge-document / free-overflow work to NVIDIA Nemotron-3 on
OpenRouter (1M context, $0 on the :free tier). Claude invokes this via Bash; only the
compact answer returns to the Claude context. The heavy input (a 2 MB doc, a whole
corpus) is processed in this subprocess and discarded.

WHY CLOUD AND NOT LOCAL: the assessment found the local 87 GB Nemotron quant barely
fits 96 GB VRAM with no KV headroom and is slower than gpt-oss:120b -- it cannot use
its own long-context edge locally. OpenRouter gives the full 1M context for $0.

Modes:
  ask <question>        general question, no PRISM context
  r
```


## Exports
`FILE_MODES` (const), `TEXT_MODES` (const), `META_MODES` (const), `ALL_MODES` (const), `MAX_SUMMARIZE_BYTES` (const), `MAX_LONGREAD_BYTES` (const), `MAX_INPUT_CEILING` (const), `parseArgs` (function), `systemPromptFor` (function), `capForMode` (function), `buildFallbackSignal` (function), `renderModels` (function), `readCapped` (function), `runRequest` (function), `cloudTokensSaved` (function), `recordCloudExecution` (function)

## Key methods
`recordOllamaEvent`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/ask-openrouter.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find ask-openrouter.mjs` or `prism_session:master_index_query`; open the source at `scripts/ask-openrouter.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
