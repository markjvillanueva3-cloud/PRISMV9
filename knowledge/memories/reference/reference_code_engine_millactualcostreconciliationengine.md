---
name: reference_code_engine_millactualcostreconciliationengine
description: "MillActualCostReconciliationEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MillActualCostReconciliationEngine.ts
  sourceHash: bfcaee23
  loc: 452
aliases: millActualCostReconciliationEngine
---

# millActualCostReconciliationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MillActualCostReconciliationEngine.ts` (452 LOC)

## What it is
```
MillActualCostReconciliationEngine
====================================

Closes the mill quote → job → actuals → next-quote feedback loop. Takes a
MillQuoteSnapshot (mirroring MillPartCostModelEngine iter69 buckets) and a
MillShopActuals record from the completed job, and produces:

  1. Per-bucket variance — predicted vs actual for 8 mill cost buckets:
     material / tool_wear / cycle / setup / fixture_amort / energy /
     secondary / overhead, both absolute USD and % terms.
     (Mill-canonical buckets: fixture_amort + energy, not in lathe.)

  2. Physics-root deltas — cycle_time_min_delta, material_mass_kg_delta,
     tool_changes_delta, setup_time_min_delta, scrap_rate_pct,
     fixture_amort_delta (mill-canonical), energy_kwh_delta (mill-canonical).

  3. Root-cause classification — argmax on |contribution|. Mill adds
     'fixture' to lathe's {programming, tooling, setup, materia
```


## Exports
`DEFAULT_MILL_MACHINE_RATE_USD_HR` (const), `DEFAULT_MILL_SETUP_RATE_USD_HR` (const), `MULTIPLIER_MIN` (const), `MULTIPLIER_MAX` (const), `RING_CAPACITY` (const), `ACCURACY_WINDOW_MAX` (const), `MillQuoteSnapshotSchema` (const), `MillQuoteSnapshot` (type), `MillShopActualsSchema` (const), `MillShopActuals` (type), `ReconcileInputSchema` (const), `ReconcileInput` (type), `MillRootCause` (type), `MillBucketVariance` (interface), `MillPhysicsDeltas` (interface), `MillReconciliationResult` (interface), `MillRunningAccuracy` (interface), `MillReconciliationState` (interface), `millActualCostReconciliationEngine` (const), `MillActualCostReconciliationEngine` (re-export)

## Key methods
`reconcile`, `runningAccuracy`, `listRecent`, `getStats`, `atomicWriteJson`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MillActualCostReconciliationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find millActualCostReconciliationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MillActualCostReconciliationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
