---
name: reference_code_engine_tpehyperparametersearchengine
description: "TPE Hyperparameter Search"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/TPEHyperparameterSearchEngine.ts
  sourceHash: 0f3a0b6d
  loc: 375
aliases: TPEHyperparameterSearchEngine
---

# TPEHyperparameterSearchEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/TPEHyperparameterSearchEngine.ts` (375 LOC)

## What it is
TPE Hyperparameter Search

```
TPEHyperparameterSearchEngine (U-LPR-HPSEARCH, ML B5)

Tree-structured Parzen Estimator hyperparameter search as used by
Optuna. Distinct from the existing LatheLoRAHyperparameterOptimizerEngine
which uses grid / random / GP-BO — TPE uses a non-parametric ratio
of Parzen-window density estimators, which handles the LoRA search
space (mix of categorical `target_modules` with numeric rank / lr /
dropout) without the kernel engineering that Gaussian Processes need.

Algorithm (Bergstra et al. 2011, NeurIPS):

  1. Sort historical trials by loss, ascending.
  2. Let γ ∈ (0, 1) be the split quantile (default 0.25). Take top
     ⌈γ · n⌉ as the "good" set D_l, the rest as D_g.
  3. Fit a Parzen density l(x) = (1/|D_l|) · Σ_i K(x − x_i) to the
     good set, and g(x) similarly on the bad set. Kernel K is a
     truncated Gaussian for continuous params and a Laplace-smoothed
     histogram for c
```


## Exports
`HPRank` (type), `HPTargetModules` (type), `HPSample` (interface), `TrialRecord` (interface), `SearchConfig` (interface), `DEFAULT_SEARCH_CONFIG` (const), `TPEHyperparameterSearchEngine` (class), `tpeHyperparameterSearchEngine` (const)

## Key methods
`suggest`, `tell`, `bestTrial`, `listTrials`, `getConfig`, `isExhausted`, `toSnapshot`, `loadSnapshot`, `clearAll`, `randomSample`, `perturb`, `gaussian`, `parzenLogDensity`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/TPEHyperparameterSearchEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find TPEHyperparameterSearchEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/TPEHyperparameterSearchEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
