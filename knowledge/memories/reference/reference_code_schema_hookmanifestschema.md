---
name: reference_code_schema_hookmanifestschema
description: "hookManifestSchema — HOOK-MANIFEST-DAG-MS26 / P0-U01"
metadata:
  type: reference
  kind: code-schema
  galaxy: unassigned
  sourcePath: mcp-server/src/schemas/hookManifestSchema.ts
  sourceHash: 4e99209b
  loc: 144
aliases: HookManifestSchema
---

# HookManifestSchema -- Schema SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/schemas/hookManifestSchema.ts` (144 LOC)

## What it is
```
hookManifestSchema — HOOK-MANIFEST-DAG-MS26 / P0-U01

Zod schema + types for the static hook manifest produced by
{@link ../engines/HookManifestEngine.HookManifestEngine}. The manifest is a
single JSON catalog of every hook *file* on disk (`.claude/hooks/*.mjs` +
`mcp-server/src/hooks/*.ts`) joined against every hook *wiring* declared in
the `hooks` block of the active `settings.json` files. It is the canonical
input for HookDAGValidatorEngine (P0-U02), hook-coverage tooling, and the
`prism_hook:manifest` dispatcher action.

Why a static manifest (vs. the live `hookEngine.listHooks()`): `hookEngine`
tracks the *registered* hooks of the running MCP server; this manifest tracks
the *Claude Code* hook surface (the `.mjs` files the harness fires on
SessionStart / PreToolUse / Stop / …), which `hookEngine` knows nothing
about. They are complementary, not redundant.

Schema version: 1 (no migr
```


## Exports
`HOOK_MANIFEST_SCHEMA_VERSION` (const), `KNOWN_HOOK_EVENTS` (const), `KnownHookEvent` (type), `HookKindSchema` (const), `HookKind` (type), `HookWiringSchema` (const), `HookWiring` (type), `HookEntrySchema` (const), `HookEntry` (type), `DanglingRefSchema` (const), `DanglingRef` (type), `HookManifestStatsSchema` (const), `HookManifestStats` (type), `HookManifestSchema` (const), `HookManifest` (type)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/schemas/hookManifestSchema.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Schema

## How to query it
Recall by meaning via `/system-viz find HookManifestSchema` or `prism_session:master_index_query`; open the source at `mcp-server/src/schemas/hookManifestSchema.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
