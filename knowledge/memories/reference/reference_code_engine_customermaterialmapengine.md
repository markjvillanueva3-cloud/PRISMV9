---
name: reference_code_engine_customermaterialmapengine
description: "learned customer → material distribution map"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CustomerMaterialMapEngine.ts
  sourceHash: 2c26f041
  loc: 706
aliases: CustomerMaterialMapEngine
---

# CustomerMaterialMapEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CustomerMaterialMapEngine.ts` (706 LOC)

> [physics] Imports canonical physics constants (`src/physics/constants.ts`) -- safety-relevant node.

## What it is
learned customer → material distribution map

```
CustomerMaterialMapEngine — learned customer → material distribution map

MS-PRINT-PROGRAM-LOOP / Track C / U-PPL-C2 (= RES-MS21 core).

Why this engine exists
----------------------
`MaterialResolverForProgramsEngine._resolveFromCustomer()` resolves material
from a customer folder name today, BUT its comment explicitly says (line ~293):

    "Known customer → material associations (shop tribal knowledge) —
     These would ideally come from a persistent database, but we encode
     common patterns from the Box drive folder structure"

That "persistent database" is what THIS engine provides. The current
`_resolveFromCustomer` only fires when the FOLDER NAME ITSELF contains a
material keyword (rare — most customers are names like "ALCOA", "TOPURA",
"JACOBSON" that carry no material signal). The miss rate is structural.

U-PPL-C2 produces a LEARNED map from real evidence:
  1. Filename heu
```


## Exports
`SOURCE_CONFIDENCE` (const), `ISO_GROUP_SCHEMA` (const), `ProgramSampleEntrySchema` (const), `ProgramSampleEntry` (interface), `MaterialHit` (interface), `ISOShare` (type), `MaterialCount` (interface), `CustomerMaterialDistribution` (interface), `CustomerMaterialMap` (interface), `extractMaterialFromFilename` (function), `extractMaterialFromCustomerFolder` (function), `resolveEntryMaterial` (function), `buildCustomerMaterialMap` (function), `lookupMaterialDistribution` (function), `CustomerMaterialMapEngine` (class), `customerMaterialMapEngine` (const)

## Key methods
`buildMap`, `lookup`, `extractFromFilename`, `extractFromCustomerFolder`, `resolveEntry`

## Depends on
[[reference_code_engine_materialresolverforprogramsengine]] | [[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/CustomerMaterialMapEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CustomerMaterialMapEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CustomerMaterialMapEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
