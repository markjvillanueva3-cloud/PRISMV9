---
name: reference_code_engine_wedmreasoningtraceledgerengine
description: "Append-only JSONL ledger of WEDM dispatcher reasoning traces. Each entry"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WEDMReasoningTraceLedgerEngine.ts
  sourceHash: 0de607ad
  loc: 217
aliases: WEDMReasoningTraceLedgerEngine
---

# WEDMReasoningTraceLedgerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WEDMReasoningTraceLedgerEngine.ts` (217 LOC)

## What it is
Append-only JSONL ledger of WEDM dispatcher reasoning traces. Each entry

```
WEDMReasoningTraceLedgerEngine — MS-P0.5-COORD U-P0.5-COORD-02

Append-only JSONL ledger of WEDM dispatcher reasoning traces. Each entry
records a decision point: which dispatcher, action, keywords, a short
summary of inputs/outputs, confidence, and which downstream engines were
consulted. Provides an audit trail for post-hoc analysis and drives the
reasoning-ledger heartbeat hook.

Write-contract:
  - Append-only; existing lines are never rewritten.
  - One JSON object per line (JSONL).
  - schemaVersion=1 on every entry; readers gate on it.
  - fs.appendFile writes on POSIX/Windows use O_APPEND semantics so
    concurrent appends from multiple processes do not interleave mid-line
    for writes under the OS atomic-append size.

In-memory ring buffer (TRACE_RING_SIZE=1000) serves fast query needs;
disk is the durable source of truth but not read back (forward-only).
```


## Exports
`ReasoningTraceEntry` (interface), `LedgerStats` (interface), `LedgerAppendResult` (interface), `WEDMReasoningTraceLedgerEngine` (class), `wedmReasoningTraceLedgerEngine` (const)

## Key methods
`setLedgerPath`, `setDiskWrites`, `validate`, `recordTrace`, `recordTraceSync`, `getRecent`, `queryByDispatcher`, `queryByAction`, `queryByKeyword`, `getStats`, `truncateDiskLedger`, `resetForTests`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/WEDMReasoningTraceLedgerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WEDMReasoningTraceLedgerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WEDMReasoningTraceLedgerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
