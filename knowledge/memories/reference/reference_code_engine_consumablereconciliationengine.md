---
name: reference_code_engine_consumablereconciliationengine
description: "Consumable Reconciliation"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ConsumableReconciliationEngine.ts
  sourceHash: 7f0fd737
  loc: 369
aliases: ConsumableReconciliationEngine
---

# ConsumableReconciliationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ConsumableReconciliationEngine.ts` (369 LOC)

## What it is
Consumable Reconciliation

```
ConsumableReconciliationEngine -- per-consumable-type predicted-vs-actual
reconciliation for the quoting closed loop (QUOTING-OPTIMAL/U4).

WHY (the gap this closes): perishable-tooling consumables (inserts, drills,
taps, wire, coolant, abrasives) are ~$4.9M of real JM spend
(state/shared/quoting/jm-tool-purchases.json, 7,150 line-items) yet the quote
cost stack had NO predicted-vs-actual reconciliation for them. The prediction
side exists (ToolCostAmortizationEngine / ToolCostPerPartEngine, forward
per-part cost) and aggregate actuals exist (ActualCostEngine.tooling = total
only, no per-type), but nothing reconciled per-consumable-type predicted vs
actual to feed the loop. LatheActualCostReconciliationEngine bundles ALL
tooling into ONE cycle-scaled tool_wear multiplier (no per-type breakdown);
ERPCostFeedbackEngine is an unwired ledger -- so this is NOT a duplicate.

WHAT IT DOES (hone
```


## Exports
`ConsumableCategory` (type), `ConsumableLine` (interface), `ConsumableActual` (interface), `ConsumableReconcileInput` (interface), `ConsumableVariance` (interface), `ConsumableRootCause` (interface), `ConsumableReconciliationReport` (interface), `ConsumableReconciliationEngine` (class), `consumableReconciliationEngine` (const)

## Key methods
`loadAdvisoryPrior`, `reconcile`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ConsumableReconciliationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ConsumableReconciliationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ConsumableReconciliationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
