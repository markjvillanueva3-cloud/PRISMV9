---
name: reference_code_engine_queueprocessorengine
description: "QueueProcessorEngine"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/QueueProcessorEngine.ts
  sourceHash: 08b6bd92
  loc: 699
aliases: QueueProcessorEngine
---

# QueueProcessorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/QueueProcessorEngine.ts` (699 LOC)

## What it is
QueueProcessorEngine

```
QueueProcessorEngine
====================

OBSIDIAN-INTELLIGENCE-MS3/B3/U-QUEUE-PROCESSOR

Watches `knowledge/memories/queue/` for operator-submitted request files
matching `RESEARCH-*.md`, `SYNTHESIZE-*.md`, or `DRAFT-*.md`, classifies
each by size, and routes:

  * `<= tokenCapBytes` (8 KiB default) -> Ollama qwen2.5-coder, write the
    response to `${queueRoot}/../generated/OUT-<basename>.md`, rename the
    source into `${queueRoot}/.processed/<basename>`.
  * `> tokenCapBytes` and `<= maxFileBytes` (64 KiB default) -> flag for
    Claude review: write a stub at
    `${queueRoot}/.claude-queue/<basename>.flag.json` carrying the source
    path + size + prefix, leave the source in place so a human / Claude
    session can pull it up.
  * `> maxFileBytes` -> rejected; written into the result manifest with a
    warning, source left in place.

Two-phase design (mirrors DailyContextWork
```


## Exports
`QueuePrefix` (type), `QueueRoute` (type), `QueueEntry` (interface), `QueueScan` (interface), `ProcessedEntry` (interface), `QueueProcessResult` (interface), `OllamaQueueClient` (interface), `QueueProcessorOptions` (interface), `QueueProcessorOptionsSchema` (const), `QueueProcessorEngine` (class), `_internals` (const), `queueProcessorEngine` (const), `runQueueProcessor` (function)

## Key methods
`generate`, `scanQueue`, `validateOptions`, `processQueue`, `mkdirSync`, `writeFileSync`, `renameSync`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/QueueProcessorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find QueueProcessorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/QueueProcessorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
