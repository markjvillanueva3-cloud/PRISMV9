---
name: reference_code_script_coorddbvacuum
description: "coord-db-vacuum.mjs — CLEANUP-MS0 / U-CLEANUP-G17"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/coord-db-vacuum.mjs
  sourceHash: 303a018a
  loc: 281
aliases: coord-db-vacuum.mjs
---

# coord-db-vacuum.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/coord-db-vacuum.mjs` (281 LOC)

## What it is
```
coord-db-vacuum.mjs — CLEANUP-MS0 / U-CLEANUP-G17

Weekly maintenance for state/shared/coordination.db (the SQLite WAL-mode
coordination store from HOOK-SYNERGY-MS0 / U-HOOK-COORD-SQLITE). Without
periodic compaction, the WAL grows unbounded (every claim/release/heartbeat
appends a frame) and the main DB file fragments after many overwrite cycles.

Operations (in order):
  1. record size of main DB + sidecar WAL + SHM files
  2. PRAGMA wal_checkpoint(TRUNCATE) — flush all WAL frames into the main
     DB and shrink the WAL file to zero bytes
  3. VACUUM — rewrite the main DB into a fresh fully-packed file
  4. record post-op sizes
  5. append a row to coord-db-health.json (JSONL, append-only) with
     { ts, ok, before:{db,wal,shm}, after:{db,wal,shm}, delta, durationMs,
       checkpoint:{busy,log,checkpointed}, dbPath }

Failure mode: never throws to the caller. Disk errors / SQLite er
```


## Exports
`parseArgs` (function), `safeSize` (function), `captureSizes` (function), `sizeDelta` (function), `runMaintenance` (function), `appendHealthRow` (function), `performVacuum` (function), `readRecentHealthRows` (function)

## Key methods
`safeClose`, `mkdirSync`, `appendFileSync`, `appendFn`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/coord-db-vacuum.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find coord-db-vacuum.mjs` or `prism_session:master_index_query`; open the source at `scripts/coord-db-vacuum.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
