---
name: reference_code_lib_fleetreaperstuckhunters
description: "Stuck-process hunters for fleet-reaper-sweep.mjs."
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/fleet-reaper-stuck-hunters.mjs
  sourceHash: 45e4f771
  loc: 423
aliases: fleet-reaper-stuck-hunters.mjs
---

# fleet-reaper-stuck-hunters.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/fleet-reaper-stuck-hunters.mjs` (423 LOC)

## What it is
```
Stuck-process hunters for fleet-reaper-sweep.mjs.

Catches what the regular reap loop misses by design:
  (1) bash.exe shells from hook chains that never finished — claude.exe
      parent stayed alive, so the slot-aware reaper leaves them. Claude
      hook chains finish in 1–3 s; anything older than ~5 min is wedged.
      Real-world finding (slot:golf, 2026-05-21): 3 bashes stuck for
      19 h, 3 for 1 h 40, 3 for 31 min — all from live claude.exe.
  (2) git fsmonitor--daemon orphans — intentionally detached background
      daemons whose original git process exited. The reaper's dead-parent
      check normally skips fresh detached procs (the 60 s grace window
      protects legit short-lived spawns); fsmonitor runs for hours so it
      slips past. Killing it is safe — `git status` re-spawns one on demand.
  (3) chat-slots.json entries whose stored PID is dead. The chat-slots
     
```


## Exports
`DEFAULT_STUCK_BASH_AGE_SEC` (const), `DEFAULT_FSMONITOR_AGE_SEC` (const), `DEFAULT_ORPHAN_GRACE_SEC` (const), `DEFAULT_SEARCHTOOL_AGE_SEC` (const), `findStuckBashes` (function), `findFsmonitorOrphans` (function), `findOrphanedSearchTools` (function), `findStaleSlotPidEntries` (function), `buildProtectedPidSet` (function), `runStuckHunters` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/fleet-reaper-stuck-hunters.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find fleet-reaper-stuck-hunters.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/fleet-reaper-stuck-hunters.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
