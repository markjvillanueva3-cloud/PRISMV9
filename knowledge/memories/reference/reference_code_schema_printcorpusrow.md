---
name: reference_code_schema_printcorpusrow
description: "PrintCorpusRow — one row per print in the corpus-wide scan inventory."
metadata:
  type: reference
  kind: code-schema
  galaxy: unassigned
  sourcePath: mcp-server/src/schemas/PrintCorpusRow.ts
  sourceHash: 6b21d261
  loc: 173
aliases: PrintCorpusRow
---

# PrintCorpusRow -- Schema SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/schemas/PrintCorpusRow.ts` (173 LOC)

## What it is
```
PrintCorpusRow — one row per print in the corpus-wide scan inventory.

Each row WRAPS the per-page BlueprintExtraction shape from
BlueprintExtractionRAGEngine with corpus-level metadata: source-file identity
(sha256-keyed for dedup), scan status (resumable orchestrator state),
ground-truth verification, and operator-review fields for the 100% gate.

Used by:
  - PRINT-OCR-100PCT-MS0/U2 (corpus orchestrator) — writes one row per print
  - PRINT-OCR-100PCT-MS0/U3 (accuracy proof harness) — reads + verifies each
  - PRINT-OCR-100PCT-MS0/U4 (wiki+tribal batch generator) — pattern-mines

Hard rules (Zod refines):
  R1 - sourceSha256 is 64-char hex (full SHA-256, no truncation).
  R2 - scanStatus="verified_100pct" requires operatorVerdict="approved"
       AND groundTruthAvailable=true (no silent acceptance per R12).
  R3 - worstConfidenceFloor is the WEAKEST floor across all pages.
  R4 - acc
```


## Exports
`SCAN_STATUSES` (const), `ScanStatus` (type), `PRINT_SOURCE_KINDS` (const), `PrintSourceKind` (type), `PRINT_SOURCE_FORMATS` (const), `PrintSourceFormat` (type), `GROUND_TRUTH_SOURCES` (const), `GroundTruthSource` (type), `OPERATOR_VERDICTS` (const), `OperatorVerdict` (type), `PrintCorpusRowSchema` (const), `PrintCorpusRow` (type), `CONFIDENCE_FLOOR_RANK` (const), `computeWorstFloor` (function), `aggregateConfidence` (function)

## Key methods
_(none extracted)_

## Depends on
[[reference_code_engine_blueprintextractionragengine]]

## Where it lives
- **Source:** `mcp-server/src/schemas/PrintCorpusRow.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Schema

## How to query it
Recall by meaning via `/system-viz find PrintCorpusRow` or `prism_session:master_index_query`; open the source at `mcp-server/src/schemas/PrintCorpusRow.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
