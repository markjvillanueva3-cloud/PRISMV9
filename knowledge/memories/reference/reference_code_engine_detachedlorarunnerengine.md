---
name: reference_code_engine_detachedlorarunnerengine
description: "DetachedLoRARunnerEngine (U-LPR-GPU-ASYNC)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/DetachedLoRARunnerEngine.ts
  sourceHash: 25b7b0e2
  loc: 460
aliases: QueueSaturatedError
---

# QueueSaturatedError -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/DetachedLoRARunnerEngine.ts` (460 LOC)

## What it is
```
DetachedLoRARunnerEngine (U-LPR-GPU-ASYNC)

State machine + bounded queue for long-running detached LoRA training
jobs. The actual process-spawning is caller-injected (via
`ProcessLauncher`) so this engine stays pure and testable — it owns
the lifecycle book-keeping, not the nohup invocation.

Lifecycle: queued → running → (paused ↔ running)* → {completed,
aborted, failed, killed}. Transitions auto-stamp timestamps; the
engine refuses illegal transitions (e.g. resuming a completed job)
rather than silently no-op.

Capacity protocol (plan spec):
  - Bounded FIFO queue — default 32 slots.
  - On submit when full: HTTP-429 shed semantics. Engine throws a
    QueueSaturatedError tagged with retry_after_ms (≥ p95 service
    time × remaining_queue / concurrency). Callers translate to 429.
  - Single concurrency slot per GPU (configurable).
  - Pause does NOT free the GPU slot (the job still o
```


## Exports
`JobStatus` (type), `ProcessLauncher` (interface), `JobRequest` (interface), `Job` (interface), `QueueStatus` (interface), `QueueSaturatedError` (class), `DetachedLoRARunnerEngine` (class), `InMemoryProcessLauncher` (class)

## Key methods
`spawn`, `pause`, `resume`, `kill`, `isAlive`, `configure`, `submit`, `dispatchNext`, `markComplete`, `sweepTimeouts`, `retry`, `getJob`, `listJobs`, `queueStatus`, `reconcileLiveness`, `clearAll`, `simulateExit`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/DetachedLoRARunnerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find QueueSaturatedError` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/DetachedLoRARunnerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
