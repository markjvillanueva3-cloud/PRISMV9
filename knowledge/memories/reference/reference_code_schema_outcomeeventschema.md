---
name: reference_code_schema_outcomeeventschema
description: "Outcome Event Schema — U-LEARN-01 OutcomeCaptureBus"
metadata:
  type: reference
  kind: code-schema
  galaxy: unassigned
  sourcePath: mcp-server/src/schemas/outcomeEventSchema.ts
  sourceHash: 4ca80889
  loc: 410
aliases: OutcomeEventSchema
---

# OutcomeEventSchema -- Schema SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/schemas/outcomeEventSchema.ts` (410 LOC)

## What it is
```
Outcome Event Schema — U-LEARN-01 OutcomeCaptureBus
====================================================

Universal event envelope every PRISM engine uses to emit outcomes to the
cross-domain learning spine. Per-domain JSONL shards under state/outcomes/
are append-only with atomic writes so 6 concurrent chats never corrupt the
event stream.

Producers: any engine — physics calc, CAM strategy, PP, SFC, quote,
           shop-floor scanner, operator-override capture hook, etc.
Consumers: PhysicsOutcomeCalibrator, PolicyExperienceLedger, FeatureStore,
           MLLineage, BusinessValueMetrics, TheoryOfMind, and (1.1.0)
           CrossProcessNeuralLearningEngine + downstream Tier-1 trainers.

Compatible with existing OutcomeTrackingEngine (Phase 0.19) — that engine
still owns program-outcome-specific logging; this bus covers every other
signal (override, measurement, scrap, quote-vs-actual
```


## Exports
`IsoTimestamp` (const), `OutcomeDomain` (const), `OutcomeKind` (const), `OutcomeSeverity` (const), `OutcomeSource` (const), `OutcomeContextSchema` (const), `NumericFeaturesSchema` (const), `OutcomeEventSchema` (const), `NumericFeatures` (type), `OutcomeEvent` (type), `OutcomeDomainT` (type), `OutcomeKindT` (type), `OutcomeSeverityT` (type), `OutcomeSourceT` (type), `OutcomeQuerySchema` (const), `OutcomeQuery` (type)

## Key methods
_(none extracted)_

## Depends on
[[reference_code_engine_crossprocessoutcomestore]]

## Where it lives
- **Source:** `mcp-server/src/schemas/outcomeEventSchema.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Schema

## How to query it
Recall by meaning via `/system-viz find OutcomeEventSchema` or `prism_session:master_index_query`; open the source at `mcp-server/src/schemas/outcomeEventSchema.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
