---
name: reference_quoting_vault_ai_synergy_live_2026_06_11
description: "VALIDATED-LIVE (slot:charlie 2026-06-11): the quoting galaxy's vault<->AI-reasoning synergization is LIVE + CURRENT via the build-once galaxy-reasoning-bridge (RAG/CAG over SOUL/CLAUDE/MEMORY/AWARENESS/synthesis, local Ollama $0). Quoting is 1 of only 2 FRESH galaxy synthesis brains; 19 of 34 are STALE (fleet-wide vault<->AI synergy gap)."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.143Z
aliases: reference_quoting_vault_ai_synergy_live_2026_06_11
---


**Context (slot:charlie 2026-06-11):** the operator's `/startup-charlie /goal` asked to "link your galaxy into zulu to utilize hermes capabilities and obsidian vault capabilities" + the standing fleet goal "ensure AI systems are synergized with obsidian vault... across all galaxies." Rather than build a bespoke quoting->vault link, I VALIDATED the existing build-once surface (R15 step 3: prove with live data, don't assume).

**FINDING 1 -- quoting vault<->AI-reasoning synergization is LIVE + CURRENT (proven, not asserted).** The fleet-wide `galaxy-reasoning-bridge` (`scripts/lib/galaxy-reasoning-bridge.mjs`, AI-SYNERGY-AUDIT-MS0/U-AISYN-BRIDGE) is the build-once asset that wires EVERY galaxy's own doctrine brain (SOUL + CLAUDE + MEMORY + AWARENESS + `<galaxy>_synthesis.md` Obsidian-vault brain) into local-Ollama RAG/CAG/LoRA reasoning ($0, no org rate-limit). Live smoke for quoting:
- `node scripts/lib/galaxy-reasoning-bridge.mjs quoting "<quote-vs-actual reconciliation gotchas + actuals-source engine>"`
- -> `{ ok:true, degraded:false, model:"qwen2.5-coder:32b", sources:["CLAUDE.md","retrieved:5","ai-synergy-audit","dense-degraded"] }`
- The answer was GROUNDED + accurate on quoting's real gotchas (grain-mismatch unitPrice/ext_price/predicted_fmv; placeholder/stale-data; `assessReferenceReliability`; cost-index granularity) -- all pulled from quoting's OWN vault brain, NOT generic. So "synergize souls/vault/memories/wikis with the AI stack per galaxy" is DONE + WORKING for quoting via the build-once asset; no bespoke link needed.

**FINDING 2 -- one RAG retrieval-grain miss (minor, not a link gap).** The bridge named `LatheActualCostReconciliationEngine` as the actuals source; the CURRENT correct answer is `QuotingActualOutcomeLoaderEngine` (built THIS session, commit 6b0f4d2718). Root cause is NOT staleness: `galaxy-synthesis-refresh --dry-run` classifies quoting **FRESH** (sourceHash matches its memory cluster incl the new loader). The synthesis TEXT mentions the loader as a post-ship learning, but the topK retrieval didn't surface it as "the actuals engine" for that question. Expected RAG limitation; the answer was still substantively correct. No action required beyond normal compounding.

**FINDING 3 -- FLEET-WIDE gap (the real, measured form of the original goal): 19 of 34 galaxy synthesis brains are STALE.** `galaxy-synthesis-refresh.mjs --dry-run --json` -> `{fresh:2, thin:13, stale:19}`. The 19 stale (blueprint-vision, bug-hunting, business, cad, cam, compliance-safety, database-expansion, dormant-data, hermes-zulu, knowledge-conversion, lathe, mill, post-processor, quality, speed-feed, system-viz, tribal-knowledge, wedm, wiring) have AI-reasoning bridges reasoning over OUTDATED vault context. This is exactly the standing-goal's "ensure AI systems are synergized with the obsidian vault across ALL galaxies" gap -- measured, not vibes. FIX attempt = `node scripts/galaxy-synthesis-refresh.mjs --json` (charlie ran it detached 2026-06-11).

**OUTCOME (HONEST, R12 -- partial, NOT a clean "19 refreshed"):**
- The GENERATION succeeded: all 19/19 stale synthesis brains were rewritten with fresh content + a fresh `sourceHash` stamp (e.g. `mill_synthesis.md` -> synthesizedAt 2026-06-11T04:48Z, sourceHash ced86d94638b, model gpt-oss:120b). So the bridges now read materially FRESHER vault content -- a real substantive win.
- BUT the job **exited 255** on a DOWNSTREAM step (the sidecar/cascade rebuild that runs after generation: strip-vectors -> rebuild-index -> rebuild-embeddings -> L2-cascade), so the bookkeeping pipeline did not complete cleanly.
- AND a re-run of `--dry-run` STILL classifies all 19 STALE -- the just-stored sourceHash does NOT match the classifier's recomputed `currentHash`. So the regen does NOT "stick" as fresh.

**SYSTEMIC FINDING for india/sierra (galaxy-synthesis infra, NOT charlie/quoting domain -- handed off, not fixed here):** `galaxy-synthesis-refresh`'s staleness hash appears NON-CONVERGENT for just-regenerated galaxies -- the regen-path `currentHash` and the classify-path `currentHash` (both from `gatherGalaxyMemories` embedding-recall) diverge, OR the cluster shifts between regen and classify (auto-memory Stop-feed adds files). Net effects: (1) the "N stale" metric OVER-REPORTS (a freshly-synthesized brain reads as stale), so the fleet vault<->AI freshness number is unreliable; (2) a naive cron regens all "stale" galaxies EVERY run (wasted local-GPU cycles), never converging. quoting reads FRESH only because its stored hash happened to match at dry-run time. ROOT CAUSE -- FULLY PINNED + CODE-VERIFIED 2026-06-11 (charlie; FIX is india/sierra's design call, NOT done here. R12: this CORRECTS two of my own intermediate hypotheses below):
- NOT ordering: `computeSourceHash` (galaxy-reflection-synthesis.mjs:45-49) `.sort()`s parts before sha256 -> order-safe.
- NOT a param mismatch: I hypothesized a brainBody/topK threading divergence between the stamp path (reflection) and the classify path (refresh) -- DISPROVEN by reading both. BOTH read `brainBody = <galaxy>/MEMORY.md` (refresh:247, reflection:290-293) and BOTH use `topK=24` (refresh:67, reflection:56). The gather PARAMETERS are identical.
- ACTUAL root cause: `computeSourceHash` is computed over `gatherGalaxyMemories` (:77-93), whose set is `runMemoryIndexSearch(query, topK*2)` -> first 24 RAW-namespace hits = a SEMANTIC-SEARCH result set. That set's MEMBERSHIP is unstable whenever the memory-index sidecar changes. Evidence: the live dry-run printed `[memory-index-search-lib] sidecar stale -- using anyway` x11. A synthesis run (a) writes new files + (b) triggers a sidecar rebuild (strip-vectors -> rebuild-index -> rebuild-embeddings), AND the auto-memory Stop-feed adds files every Stop -> the post-run classify recalls a DIFFERENT top-24 cluster than the stamp-time gather -> hash diverges -> perpetual stale. The exit-255 on the sidecar rebuild compounds it (sidecar left half-built).
- **FIX-SPEC (india/sierra -- a DESIGN tradeoff, do not force-patch):** the freshness hash must be computed over a DETERMINISTIC, sidecar-independent input, not a semantic-recall topK. Options: (a) hash the galaxy's namespace FILE-SET (deterministic fs enumeration of the reference/feedback/project/mistakes memories that map to the galaxy) -- but this changes WHICH memories define "changed" (file-set vs top-24-relevant); (b) snapshot the stamped cluster's {namespace/name} keys INTO the synthesis frontmatter and diff key-SETS at classify (tolerant of recall re-ranking, sensitive only to membership) -- still sidecar-dependent for membership; (c) gate classify's recall to read the SAME frozen sidecar the stamp used (version the sidecar). (a) or (b) is the real fix. Add a determinism test: two back-to-back `computeSourceHash(gather(g))` over a FIXED sidecar -> equal. R8 NOTE: the semantic-recall gather is DELIBERATE (it clusters the top-relevant memories, and the header says it uses /api/embeddings so it works even when generation is wedged) -- a naive switch to file-enum changes synthesis semantics, so this is india/sierra's call. Impact until fixed: the "N stale" metric OVER-REPORTS + a cron re-synthesizes already-fresh galaxies every run (wasted local-GPU); the CONTENT refresh still lands (the bridge reads the md directly), only the freshness BOOKKEEPING is broken -- benign but wasteful. Charlie's deep-dive stopped here (R8 foreign-domain design boundary + RED context).

**FINDING 4 -- RAG-hybrid dense arm degrades to sparse under concurrent VRAM load (for india; observed live, code is CORRECT).** The quoting bridge run reported `sources:[...,"dense-degraded"]` -> the hybrid RAG fell back to sparse-only. Root cause is NOT a code bug: `embedText` (galaxy-dense-rerank.mjs:99-118) calls `/api/embeddings` (returns `{embedding:[...]}`) and reads `j.embedding` -- endpoint+parse MATCH (verified via curl). The degradation is OPERATIONAL: `hybridRetrieve` (:~138) does `Promise.all(candidates.map(embedText))` then `if (vecs.some(v=>!v)) return null` -- ALL-OR-NOTHING, so ONE slow/timed-out embed (cold `nomic-embed-text` competing for VRAM with the resident `gpt-oss:120b` from the synthesis run) drops the WHOLE dense arm. Fleet-wide impact: under any concurrent-generation load the hybrid silently collapses to sparse for ALL galaxies (worse retrieval -> worse deep-reasoning). FIX OPTIONS: (a) env-gated GRACEFUL partial-dense -- **SHIPPED by charlie 2026-06-11, commit 86e7e6b77e** as opt-in `opts.partialDense` / `PRISM_GALAXY_RAG_PARTIAL_DENSE=1`, default-OFF so existing behavior is byte-identical (R8-safe additive, sidesteps the deliberate-bail design call). When >=2 candidate embeds survive, reranks the survivors + RRF-fuses against the FULL sparse list; <2 -> still bails to sparse. 15/15 tests (+3: graceful-fusion happy / default-bail regression-lock / <2-guard / all-succeed no-op); live bridge gate-ON -> {ok:true,degraded:false}. One build-once change covers all 34 galaxies. REMAINING for india/sierra (operational, not yet done): (b) prewarm `nomic-embed-text` keep_alive before a bridge batch + (c) bound embed concurrency / raise per-embed timeout -- these PREVENT the starvation; (a) only makes it DEGRADE GRACEFULLY. To activate (a) fleet-wide, the bridge caller must pass the flag (currently opt-in; promoting to default is india's call once (b)/(c) reduce the partial-failure rate). Non-GPU, $0. This is the THIRD verification this session that the goal's named-AI-systems levers are india/sierra-owned (GNN GPU-blocked; synthesis-staleness design-call; RAG-hybrid degradation design-call).

**NAMED-AI-SYSTEM COVERAGE MAP (charlie 2026-06-11 -- what's executable on THIS host vs GPU-blocked, re the fleet "improve all AI systems" goal):**
- **CAG (cache-augmented generation):** ✓ VALIDATED LIVE -- back-to-back identical bridge query: run1 `cached:false` (populate) -> run2 `cached:true` (hit). The galaxy-reasoning-cag answer-cache works fleet-wide (content-invalidated by doctrine fingerprint). Non-GPU.
- **RAG hybrid (sparse+dense RRF):** ✓ IMPROVED + SHIPPED (86e7e6b77e graceful partial-dense) + works live. Non-GPU.
- **Deep reasoning (galaxy-reasoning-bridge over SOUL/CLAUDE/MEMORY/AWARENESS/synthesis vault brain):** ✓ VALIDATED LIVE (grounded quoting-specific answers, $0 Ollama). Non-GPU. This IS "AI synergized with vault/souls/memories/wikis per galaxy" -- build-once, all 34 galaxies.
- **NN / GNN (GraphSAGE tier-5):** ⛔ GPU-blocked on this charlie host -- H2GCN OOMs at the 6000-node cap; the +0.138 lever DISPROVEN on 768d (multi-seed +0.003). india's GPU domain. [[reference_h2gcn_heterophily_silent_noop_2026_06_11]]
- **LoRA fine-tune:** ⛔ GPU-blocked -- torch venv absent from this host's portable python. EMIT (training-pair generation) is non-GPU + wired (`PRISM_GALAXY_BRIDGE_LORA_EMIT=1`); the FINE-TUNE is india/Blackwell.
- **Deep-learning training:** ⛔ GPU-blocked.
**ACROSS ALL 34 GALAXIES (not just quoting) -- PROVEN with numbers:** `node scripts/audit-ai-synergy.mjs --dry` -> `34 galaxies | mean 1 median 1 | strong 34 partial 0 weak 0`. Every galaxy scores the CEILING (1.0) on AI-synergy: each has CLAUDE.md + MEMORY.md + a synthesis vault brain + reasoning-bridge coverage (`servedByReasoningBridge`) + cross-substrate edges + an awareness surface, all wired together. So the vault<->AI synergization (souls/CLAUDE/MEMORY/wikis <-> CAG/RAG/deep-reasoning) the goal names is demonstrated FLEET-WIDE, 34/34 strong -- the quoting live-bridge run is one verified instance of a pattern the audit confirms across all 34.

HONEST BOTTOM LINE: every EXECUTABLE (non-GPU) named system is validated-working or improved-and-shipped this session, and the vault<->AI synergization scores 34/34 strong fleet-wide; every GPU-blocked named system (NN/GNN/LoRA-finetune/deep-learning-training) is documented with the reason + handed to india. That is the complete set of what a charlie/quoting session can do toward the fleet goal -- the GPU-bound remainder is india (Blackwell) + the synthesis-staleness bookkeeping is sierra.

**Lesson:** before building a bespoke "link galaxy X into the vault/AI stack," check the build-once `galaxy-reasoning-bridge` -- it ALREADY links all 34 galaxies; the only real work is keeping each galaxy's `<galaxy>_synthesis.md` vault brain FRESH (via `galaxy-synthesis-refresh`). Related: [[reference_ai_systems_6unit_complete_2026_06_11]], engines/CLAUDE.md S3 (PSN leg #10), [[feedback_psn_definition]].
