---
name: reference_code_engine_consensuscoordinatorengine
description: "concurrency-aware wrapper around MultiModelConsensusEngine."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ConsensusCoordinatorEngine.ts
  sourceHash: 73ae241a
  loc: 501
aliases: ConsensusCoordinatorEngine
---

# ConsensusCoordinatorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ConsensusCoordinatorEngine.ts` (501 LOC)

## What it is
concurrency-aware wrapper around MultiModelConsensusEngine.

```
ConsensusCoordinatorEngine — concurrency-aware wrapper around MultiModelConsensusEngine.

Milestone: INTEL-OLLAMA-OBSIDIAN-MS0 / AUTO-CONSENSUS.

Designed for the 6-simultaneous-Claude-terminals reality: every consensus
call hits a shared external resource (Codex API, Ollama daemon, Claude
subprocess), so naive auto-fan-out from every terminal would saturate the
fleet and exhaust the daily Codex token budget within minutes.

Coordinator responsibilities:
  1. **Cache** — prompt+taskType hash → result (1h TTL, file-backed JSONL).
     A terminal that already consensused on prompt P returns cached result
     instantly to the next terminal that asks the same.
  2. **In-flight tracking** — file-locked registry at
     data/state/consensus-inflight.json. Each entry: {terminalId, hash,
     startedAt}. Max 1 per terminal, max 3 globally.
  3. **Token budget** — daily Codex token cap (default 
```


## Exports
`CoordinatorRequest` (interface), `CoordinatorOutcome` (type), `EscalationRequest` (interface), `EscalationAttempt` (interface), `EscalationOutcome` (type), `ConsensusAskFn` (type), `ConsensusCoordinatorEngine` (class), `consensusCoordinatorEngine` (const)

## Key methods
`run`, `runWithEscalation`, `peekCache`, `getStats`, `resetForTesting`

## Depends on
[[reference_code_engine_multimodelconsensusengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/ConsensusCoordinatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ConsensusCoordinatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ConsensusCoordinatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
