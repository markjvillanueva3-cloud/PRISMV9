---
name: reference_delta_kinematic_assembly_live_2026_06_02
description: "PRISM generated a working KINEMATIC (1-DOF revolute hinge) assembly live in Fusion — 7th live proof, first kinematic assembly past rigid G7"
type: reference
source: prism-memory
synced: 2026-06-09T14:54:09.085Z
aliases: reference_delta_kinematic_assembly_live_2026_06_02
---


**Kinematic revolute assembly — LIVE-PROVEN in Fusion (2026-06-02, slot:delta).** Commit `8cb659510a` ([CAD-TRAINING-PIPELINE]/U-CADTP-REVOLUTE-ASM) is live-verified on bridge `:18365`: `node scripts/cad-fusion-assembly-poc-live.mjs --port 18365 --components 2 --joint-type revolute` →
`jointMode requested=revolute → actual=revolute (KINEMATIC hinge, 1-DOF)`, VERIFY ✓ components=4 constraints=2, `/component/list` cross-check {raw:4, occCount:4}, reap closed exactly this run's doc (`PRISM-DELTA-CLIVE-ASM-*-1`; Untitled reactivated+protected, peers refused). The committed-message "LIVE PROOF PENDING" note is **superseded by this record** — the proof passed once the host/bridge recovered.

**Why it matters:** this is the **7th live Fusion proof** and the first *kinematic* (1-DOF) assembly — the next intricacy rung past the rigid G7 rigid-joint PoC (`e95d64fc4b`), directly serving the standing goal "...adequately generate highly intricate cad files **and assemblies**". The revolute joint ACTUALLY fired (not the rigid fallback): `JointGeometry.createByPoint` on each component's origin construction point via `createForAssemblyContext`, `setAsRevoluteJointMotion(ZAxisJointDirection)`. Fallback chain (as-built-rigid → rigid-group) preserved and version-robust; default `jointType=rigid` keeps proven G7 behavior byte-identical (additive change).

**How to apply:** for any future kinematic-assembly work, the proven emitter is `assemblyPocCode({jointType:"revolute"})` in `scripts/lib/cad-fusion-assembly-builder.mjs` (10/10 offline tests). Generator-only (no bridge import) so it stays offline-testable. The revolute joint geometry pattern (construction-point + `createForAssemblyContext` + `setAsRevoluteJointMotion`) is the canonical Fusion-bridge recipe for a 1-DOF hinge under delta's `saveChanges=false` / addNewComponent (no-file_id) safety rules.

**#34 build-isolation — FIXED + LIVE-PROVEN (commit `2e9e6ddc7d`, same day).** The doubling (`components=4`=2+2, or 6/4 for a 3-comp run) came from the bridge `/new` letting the build's `app.activeProduct` resolve to a stray pre-existing Untitled, so occurrence/joint counts ACCUMULATED across runs. **Disproven first (reverted):** prepending an activate-by-name prelude before the build — `app.activeProduct` does NOT follow a mid-`/execute` `.activate()` of an existing doc (build still doubled). **The fix that worked:** `isolatedBuildWrapperCode` in `cad-fusion-doc-lifecycle.mjs` — `app.documents.add(adsk.core.DocumentTypes.FusionDesignDocumentType)` (the canonical pattern; reliably creates AND activates a fresh empty doc, unlike `.activate()`-of-existing), the inner build lands in it (0 prior occurrences → exact counts), then the SAME `/execute` activates a non-owned doc and closes OUR handle BY IDENTITY (`close(False)`, never by name) → zero leak, no reap name-collision (the earlier `documents.add` revert only broke the SEPARATE-call reap-by-name; closing in-call avoids reap entirely). Default-on; `PRISM_CAD_NO_DOC_ISOLATE=1` = legacy `/new` A/B. **Live proofs on :18365:** 3-comp → `components=3 constraints=2` (was 6/4); 5-comp revolute chain → `components=5 constraints=4` (a 4-DOF kinematic mechanism); both `docClosed=true`, `jointMode=revolute`. 32/32 doc-lifecycle tests. **SAFE:** only the `documents.add` handle is ever closed (by identity); operator docs (DIE CASE, _okuma .f3d, UP SET) are never close candidates; reap kept as belt-and-suspenders (correctly found nothing). Follow-up (non-blocking, R13): apply the same wrapper to `cad-fusion-correction-loop-live.mjs` single-part builds. Supersedes the open-issue note in [[reference_delta_fusion_doc_lifecycle_reap_gap_2026_06_02]].

Related: [[reference_delta_fusion_backend_map_2026_06_02]] (live add-in HAS /component/joint+insert+list) · [[reference_delta_designated_port_18632]] (delta drives :18365/:18632, NEVER kilo :18361).
