---
name: reference_code_engine_licensestore
description: "License Store"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LicenseStore.ts
  sourceHash: 2ab052af
  loc: 322
aliases: LicenseStore
---

# LicenseStore -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LicenseStore.ts` (322 LOC)

## What it is
License Store

```
PRISM MCP Server -- LicenseStore (U-COMM-08)

Persisted one-time (perpetual) license grants. These are NON-subscription
purchases -- the operator's launch ask: "a logical price for one time payment
for the sfc and a single post processor."

A license grants a feature FOREVER, independent of the user's subscription plan:
  - sfc_perpetual ($299)  -> blanket "speed_feed" grant (above the free 10/day cap)
  - post_perpetual ($199) -> a SINGLE controller's post output (controller-scoped)

This is the GRANT-ABOVE path, distinct from EntitlementOverrideStore (which only
DENIES below the plan ceiling). requireTier consults req.user.licenses (the
blanket grants) AFTER the admin-deny check and BEFORE the plan check, so a
perpetual buyer is never blocked by free-tier limits, but an admin can still
revoke a shop seat. Controller-scoped post grants are checked per-controller via
hasPostLicense at th
```


## Exports
`LicenseStatus` (type), `LicenseRecord` (interface), `LicenseStore` (class), `licenseStore` (const)

## Key methods
`safeWriteSync`, `generateKey`, `verifyKey`, `issue`, `activate`, `revoke`, `getUserLicenses`, `grantedFeatures`, `hasPostLicense`, `listAll`, `invalidate`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/LicenseStore.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find LicenseStore` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LicenseStore.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
