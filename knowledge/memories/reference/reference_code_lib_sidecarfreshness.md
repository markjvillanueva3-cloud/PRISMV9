---
name: reference_code_lib_sidecarfreshness
description: "sidecar-freshness.mjs -- opportunistic, no-elevation sidecar refresh at Stop."
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/sidecar-freshness.mjs
  sourceHash: 5466a6a9
  loc: 198
aliases: sidecar-freshness.mjs
---

# sidecar-freshness.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/sidecar-freshness.mjs` (198 LOC)

## What it is
```
sidecar-freshness.mjs -- opportunistic, no-elevation sidecar refresh at Stop.

The brain's recall sidecars rot between manual runs because the canonical
auto-refresh (the `PRISM Brain Refresh` scheduled task) is elevation-gated and
unregistered on this host. This lib is the no-elevation complement: the
`session-consolidate-graph.mjs` Stop hook calls `runSidecarFreshness()` to
mtime-compare two recall sidecars against their sources and, when stale,
DETACH-spawn the existing rebuild scripts. The heavy work runs after the hook
exits, so the ~5 s Stop budget is never blocked.

Two refresh targets (paths verified live 2026-06-09):
  - master-index  : system-graph.json -> system-graph-index.json  (no Ollama)
  - memory-embed  : memory-index-sidecar.json (BM25) -> memory-embeddings-sidecar.json (Ollama)

Thundering-herd safe: the 26-chat fleet all hit Stop, so a shared O_EXCL
decision lock seri
```


## Exports
`DEFAULT_COOLDOWN_MS` (const), `DEFAULT_LOCK_TTL_MS` (const), `DEFAULT_GRAPH_INDEX_MIN_FREE_MB` (const), `freeRamMB` (function), `REFRESH_TARGETS` (const), `isTargetStale` (function), `decideRefresh` (function), `acquireDecisionLock` (function), `releaseDecisionLock` (function), `runSidecarFreshness` (function), `defaultPaths` (function)

## Key methods
`releaseDecisionLock`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/sidecar-freshness.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find sidecar-freshness.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/sidecar-freshness.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
