---
name: reference_code_lib_fleetreapercrashwatch
description: "fleet-reaper-crash-watch.mjs — FLEET-REAPER-MS1 U-FR-CRASH-WATCH"
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/fleet-reaper-crash-watch.mjs
  sourceHash: 64a8d05a
  loc: 213
aliases: fleet-reaper-crash-watch.mjs
---

# fleet-reaper-crash-watch.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/fleet-reaper-crash-watch.mjs` (213 LOC)

## What it is
```
fleet-reaper-crash-watch.mjs — FLEET-REAPER-MS1 U-FR-CRASH-WATCH

The reaper kills orphan PROCESSES but is BLIND to chat CRASHES — it never
reconciles "slot X's chat just died" into an actionable signal. When 1-2
chats keep crashing the operator has no forensic trail (which slot, when,
at what heartbeat age, under what memory pressure).

This module is the missing detection layer. It is STRICTLY ADDITIVE — it
never changes a reap decision. It only:
  1. snapshots per-slot { chatId, lastHeartbeatMs } each sweep,
  2. diffs against the previous sweep's snapshot,
  3. flags slots whose heartbeat FROZE while the chatId stayed the same
     (a live chat heartbeats every prompt; a crashed one's lastHeartbeat
     stops advancing — chatId change = intentional re-claim, NOT a crash),
  4. writes a postmortem JSONL row + surfaces a count.

Pure core + injected IO — fully testable without a real f
```


## Exports
`CRASH_WATCH_SCHEMA_VERSION` (const), `DEFAULT_CRASH_STALE_MS` (const), `CRASH_WATCH_LOG_ROTATE_BYTES` (const), `snapshotSlotState` (function), `detectCrashes` (function), `formatPostmortemRow` (function), `readPrevSnapshot` (function), `writeSnapshot` (function), `appendPostmortems` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/fleet-reaper-crash-watch.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find fleet-reaper-crash-watch.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/fleet-reaper-crash-watch.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
