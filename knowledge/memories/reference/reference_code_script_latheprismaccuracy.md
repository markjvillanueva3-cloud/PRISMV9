---
name: reference_code_script_latheprismaccuracy
description: "lathe-prism-accuracy.mjs -- slot:whiskey [the real PRISM-accuracy consumer]"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/lathe-prism-accuracy.mjs
  sourceHash: 644905de
  loc: 112
aliases: lathe-prism-accuracy.mjs
---

# lathe-prism-accuracy.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lathe-prism-accuracy.mjs` (112 LOC)

## What it is
```
lathe-prism-accuracy.mjs -- slot:whiskey  [the real PRISM-accuracy consumer]
==========================================================================
The LAST piece of the exact per-pair accuracy loop: score the PRISM wizard's
per-op params (persisted on .MIN-paired Rung C-STEP steps by
U-W-RUNGC-OP-PARAMS-PERSIST) against the paired JM .MIN program, param-for-param,
via the exact scorer -- the per-part PRISM-accuracy % the band score cannot give.

VOCAB SEAM (R16, surfaced building this): the wizard emits op-types like "od_rough"
while parseMinOpParams emits "rough" (band-ish). So both sides are NORMALIZED to the
common op BAND (classifyOpBand) before matchOpPairsByType pairs them -- otherwise the
raw-type match finds no common type. (rev-over-rev worked without this because both
sides came from the same extractor; cross-source needs the normalization.)

`scorePrismVsMin` is PURE (op_
```


## Exports
`scorePrismVsMin` (function)

## Key methods
`mkdirSync`, `writeFileSync`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lathe-prism-accuracy.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find lathe-prism-accuracy.mjs` or `prism_session:master_index_query`; open the source at `scripts/lathe-prism-accuracy.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
