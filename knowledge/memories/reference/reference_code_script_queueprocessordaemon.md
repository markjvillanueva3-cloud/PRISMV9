---
name: reference_code_script_queueprocessordaemon
description: "queue-processor-daemon.mjs"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/queue-processor-daemon.mjs
  sourceHash: 3ee1578c
  loc: 407
aliases: queue-processor-daemon.mjs
---

# queue-processor-daemon.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/queue-processor-daemon.mjs` (407 LOC)

## What it is
```
queue-processor-daemon.mjs

OBSIDIAN-INTELLIGENCE-MS3/B3/U-QUEUE-PROCESSOR — daemon + cron runner.

Drives `QueueProcessorEngine` end-to-end:
  1. Loads the TS engine via `tsx/esm/api` (no build dependency).
  2. Resolves an Ollama-generate adapter if the daemon is reachable;
     otherwise the engine's claude-flag fallback owns the output.
  3. Either:
     a. `--once` — single processQueue() pass and exits (cron entry).
     b. `--watch` — fs.watch the queue dir + debounced re-process loop.
  4. Emits a one-line JSON status to stdout per pass for the cron log.

Usage:
  node scripts/queue-processor-daemon.mjs --once             # cron entry
  node scripts/queue-processor-daemon.mjs --watch            # long-running watcher
  node scripts/queue-processor-daemon.mjs --once --dry-run   # scan only, no writes
  node scripts/queue-processor-daemon.mjs --once --no-ollama # force claude-flag 
```


## Exports
_(no top-level exports detected)_

## Key methods
`clearTimeout`, `generate`, `pathToFileURL`, `emitStatus`, `appendCronLog`, `void`, `mkdirSync`, `writeFileSync`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/queue-processor-daemon.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find queue-processor-daemon.mjs` or `prism_session:master_index_query`; open the source at `scripts/queue-processor-daemon.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
