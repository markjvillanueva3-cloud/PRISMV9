---
name: reference_code_engine_authorizationengine
description: "plan-spec version)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/AuthorizationEngine.ts
  sourceHash: 9d03701a
  loc: 483
aliases: AuthorizationEngine
---

# AuthorizationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/AuthorizationEngine.ts` (483 LOC)

## What it is
plan-spec version)

```
AuthorizationEngine (U-LPR-SEC05 — plan-spec version)

Consolidated authz decision service with REST/MCP parity verification.
The earlier SEC05 commit built RateLimitingEngine; the plan spec
actually asked for a unified policy layer that ensures every protected
operation produces the same decision whether reached via REST or via
the MCP tool dispatcher.

Design:

  - Resources are identified by a canonical `(resource_type, resource_id)`
    pair (e.g. "lathe_program/P-1042"). Each resource is reachable via
    one or more bindings — a REST route + method, or an MCP tool +
    action. Bindings point to the same resource so a single policy
    covers both surfaces.

  - Policies combine RBAC (role → permissions) with scoped
    per-resource rules. Evaluation is deterministic and order-
    independent: explicit `deny` beats any `allow`, then `allow`,
    then a default `deny` fallback (den
```


## Exports
`Surface` (type), `Decision` (type), `PermissionAction` (type), `ResourceBinding` (interface), `Resource` (interface), `Role` (interface), `Permission` (interface), `Policy` (interface), `Principal` (interface), `DecisionRequest` (interface), `DecisionRecord` (interface), `ParityResult` (interface), `AuthzStats` (interface), `AuthorizationEngine` (class), `authorizationEngine` (const)

## Key methods
`registerResource`, `getResource`, `listResources`, `registerRole`, `getRole`, `listRoles`, `registerPolicy`, `removePolicy`, `listPolicies`, `evaluate`, `verifyParity`, `parityCoverageReport`, `listParityChecks`, `listDecisions`, `getStats`, `clearAll`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/AuthorizationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find AuthorizationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/AuthorizationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
