---
name: reference_code_engine_camoptimizationsuggestionengine
description: "AI Recommendations (U-CAM103)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMOptimizationSuggestionEngine.ts
  sourceHash: d4153ee4
  loc: 512
aliases: CAMOptimizationSuggestionEngine
---

# CAMOptimizationSuggestionEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMOptimizationSuggestionEngine.ts` (512 LOC)

## What it is
AI Recommendations (U-CAM103)

```
CAMOptimizationSuggestionEngine — AI Recommendations (U-CAM103)
================================================================

PHASE-7: AI-style parameter recommender that proposes one-click,
physics-justified parameter changes for the current CAM operation.

Three optimization goals are supported, each producing zero or more
suggestions ranked by predicted improvement:

  cycle_time     — push fz / ae / rpm up to extract MRR headroom
  tool_life      — pull rpm / fz back to extend Taylor life
  surface_finish — drop fz to push Ra below the target

Each suggestion is delivered as a SuggestionPatch — a sparse delta over
the current ToolpathSegment — so a CAM UI can preview the diff and
apply with a single click. Patches are bounded by absolute parameter
limits (rpm/fz/ap/ae caps) and by the U-CAM102 prediction stack:
a suggestion that would raise *any* predictor's severity past
PRIORIT
```


## Exports
`SuggestionTargetSchema` (const), `SuggestionTarget` (type), `OptimizationGoalSchema` (const), `OptimizationGoal` (type), `SuggestionLimitsSchema` (const), `SuggestionLimits` (type), `SuggestionPatchSchema` (const), `SuggestionPatch` (type), `SuggestionSchema` (const), `Suggestion` (type), `SuggestionReportSchema` (const), `SuggestionReport` (type), `SuggestionStatsSchema` (const), `SuggestionStats` (type), `DEFAULT_LIMITS` (const), `mrrOf` (function), `applyPatch` (function), `isSafe` (function), `CAMOptimizationSuggestionEngine` (class), `camOptimizationSuggestionEngine` (const)

## Key methods
`supportedTargets`, `supportedGoals`, `recommend`, `recommendAll`, `applySuggestion`, `encodeReport`, `getStats`, `resetSession`, `resetAll`

## Depends on
[[reference_code_engine_cammachiningerrorpredictionengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CAMOptimizationSuggestionEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMOptimizationSuggestionEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMOptimizationSuggestionEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
