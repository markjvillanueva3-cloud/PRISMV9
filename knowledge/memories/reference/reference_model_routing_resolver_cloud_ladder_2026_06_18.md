---
name: reference_model_routing_resolver_cloud_ladder_2026_06_18
description: "feature-routing-graph structured model-routing layer + $0 cloud fallback ladder (U-MODEL-PLAN-RESOLVER, slot:alpha 2026-06-18) -- reasoning=Opus/coding=Sonnet+coder-ensemble as machine-checkable data; folds CLOUD-OVERFLOW-MS0"
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.662Z
aliases: reference_model_routing_resolver_cloud_ladder_2026_06_18
---


**U-MODEL-PLAN-RESOLVER (slot:alpha, 2026-06-18, commit `da42da43b0` on cad-fusion-live-ms0).** Folded the fleet's live Ollama/cloud work into `scripts/lib/feature-routing-graph.mjs` (operator: "they're all working on ollama models -- update the graph relative to what they're working on" + bravo work-request 00:02Z to fold the cloud/16-model reality in). The prose `modelTier` column now has a machine-checkable structured twin:

- **`MODEL_IDS`** -- single source for concrete ids: opus=`claude-opus-4-8`, sonnet=`claude-sonnet-4-6`, haiku=`claude-haiku-4-5`, coderEnsemble=`[qwen2.5-coder:32b, qwen3-coder:30b]` (LIVE on the 16-model roster), coderEnsemblePending=`[deepseek-coder]` (NOT pulled), cloudFree=`nvidia/nemotron-3-super-120b-a12b:free` (1M ctx, $0, READ-only).
- **`resolveModelPlan(taskClass)`** -- reasoning(plan/review/physics/orchestrate/domain)=Opus ONLY; coding(build/fix)=Sonnet@max + coder ensemble, Opus escalation-only; mechanical(locate/recall/session/learn)=never-opus; mixed(quote)=Sonnet+Opus-margin. R12 honesty flag **`coding.localEnsembleWired:false`** -- the LIVE octopus picker (`MultiModelConsensusEngine.pickBestOllamaModel`, ranks by size+coder-bonus) seats `gpt-oss:120b` + ONE coder, NOT the two dedicated coders the policy declares. Wiring the two-coder combo needs a coding-task-aware picker OR a deepseek-coder pull.
- **`FALLBACK_LADDER`** -- spec twin (mirror, not reimpl -- R8) of the live `model-routing-policy.mjs` / `ollama-task-offloader.mjs` / `/smart resolveExecutor`: Ollama-free -> $0 OpenRouter Nemotron (READ-only) -> cheap-Claude -> Opus. Rules: cloud READ-only (CLOUD_VETO keeps codegen on Claude -> operator's "deepseek-coder cloud-if-free" needs a *codegen-capable* cloud rung, the current Nemotron one is reading-only), safety NEVER egresses, candidates gated by `assess-cloud-candidate.mjs`.
- **`assertModelRoleCoherence(roleMap,policy,ids)`** -- injectable, fail-loud; wired into `generate-feature-routing-graph.mjs` main(). Keeps the structured map from drifting off the prose modelTier.
- **`modelPolicyDrift(decision)`** -- operationalizes the surfaced R7 conflict (declared Sonnet coding policy vs the live router that still routes build/fix->Opus by design) into a detectable signal -- evidence, not assertion, WITHOUT touching india's router.

65/65 lib tests (+14) · prompt-route-inject 33/33 (no consumer regression) · per-file 2-arm scrutiny PASS (2 P2s fixed in-loop: weak THROW-path test made injectable+real, nested `models[]` frozen) · end 3-of-3 PASS. Emitted into `state/shared/feature-routing-graph.json` (modelIds/modelPlans/fallbackLadder). Spec §1d documents it.

**RESOLVED (operator "both" 2026-06-18) -- the "both" arc, 3 commits on cad-fusion-live-ms0:**
- **(a) `c5d2174fbf` U-LIVE-ROUTER-CODING-SONNET** -- `claude-tier-router.routeClaudeTier` coding branch opus->sonnet (OPUS_PATTERNS renamed CODING_PATTERNS; BUILD_CLASSES exported single-source); `effort-tier-router` coding-on-sonnet->HIGH effort ("Sonnet @ max"); `subagent-model-enforce` now emergently DENIES coding-on-opus (recommend sonnet); `agent-fanout-pressure-gate` P1 fix (per-file scrutiny arm B caught it) -- `classifyWorkflowMechanical` now uses a positive MECHANICAL_CLASSES allowlist (not a tier-name check) so a builder Workflow fan-out isn't false-blocked. Safety->frontier Opus + deep-think->Fable preserved; Opus is escalation-only via cost-router CLAUDE_LANE_CATEGORIES. modelPolicyDrift now reads CLEAN (regression-guard).
- **(b) `16269fd2ad` U-OCTOPUS-CODER-ENSEMBLE** -- `MultiModelConsensusEngine.coderEnsemble:true` + `CODER_ENSEMBLE_MODELS=[qwen2.5-coder:32b, qwen3-coder:30b]` seats TWO distinct coders via the diverse-panel path (reuses existing machinery, R8). deepseek-coder omitted (bare tag would never exact-match; joins when pulled).
- **(c) `aadf5a5177` U-GRAPH-RECONCILE-WIRED** -- `localEnsembleWired` false->true + `localEnsembleVia`; R7 drift marked resolved; spec §1d + JSON synced.
- Tests across arc: 204 green (claude-tier 14 + model-routing 25 + effort 26 + subagent 10 + fanout-gate 19 + consensus-resolve 45 + feature-routing-graph 65). Per-file 2-arm scrutiny on (a)+(b) + end-of-task 3-of-3 ALL PASS.
- **Deferred (non-arc, forward-merge ownership):** slot/golf carries the PRE-fix `agent-fanout-pressure-gate` -> on golf merge take the cad-fusion-live-ms0 version (chat-bus note posted). `agent-fanout-pressure-gate` GATED_TOOLS lists "Task" but settings.json wires only Agent+Workflow matchers (pre-existing golf wiring). `model-tier-advisor.mjs` has a peer's uncommitted stale `opus="building/coding"` blurb (advisory-only, functionally correct -- reads v.tier from the aligned router; owner to update).

**FOLLOW-ON (backlog WIRINGS rung, same session):**
- **`0fac5fa49c` U-OCTOPUS-DEEPSEEK-VOICE + `cc5f0d452e` FIX** -- de-orphaned `DeepSeekClientEngine` (was UNWIRED) by wiring it as a 5th octopus cross-vendor voice in `MultiModelConsensusEngine` (clones Grok/Gemini: `ModelResponse.vendor += "deepseek"`, `includeDeepSeek` KEY-GATED by DEEPSEEK_API_KEY, `callDeepSeek` fail-soft). This is the operator's "deepseek coder, cloud version" -- a codegen-capable CLOUD deepseek (V3.2 deepseek-chat / deepseek-reasoner), distinct from the read-only Nemotron rung. New `DeepSeekClientEngine.test.ts` (engine had 0 tests). **3rd-arm scrutiny caught a P1 both per-file arms missed (R12 lesson):** my "39 tests unchanged" was a CONDITIONAL keyless-host pass -- the test's `_VENDOR_KEYS` hermetic scrub didn't include DEEPSEEK_API_KEY, so a host WITH the key (the operator's target) would break voice-count tests + fire real network. Fix: add the key to the scrub + 2 ask()-level round-trip tests. Proven 41/41 keyless AND with-key. 3-of-3 PASS. **LESSON: when adding a key-gated external voice, ALSO add its key to every hermetic-isolation scrub, and prove the test green WITH the key set (not just keyless) -- a 3rd independent scrutiny arm catches conditional-pass gaps two arms share.**
- Still pending: pull a LOCAL deepseek-coder + add its exact tag to CODER_ENSEMBLE_MODELS + MODEL_IDS.coderEnsemble. Other WIRINGS: GrokCLIClientEngine (cross-vendor CLI voice), etc. (UNWIRED-ENGINE-AUDIT-2026-06-18.json, 18 total).

See [[reference_cloud_model_stack_2026_06_17]] · [[feedback_ollama_fallback_sonnet_agents]] · [[reference_octopus_consensus_route_2026_06_17]].
