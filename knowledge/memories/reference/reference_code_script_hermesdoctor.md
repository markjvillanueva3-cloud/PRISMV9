---
name: reference_code_script_hermesdoctor
description: "hermes-doctor.mjs -- idempotent pre-launch verify+repair that makes the Hermes"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/hermes-doctor.mjs
  sourceHash: 3dd24e06
  loc: 318
aliases: hermes-doctor.mjs
---

# hermes-doctor.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/hermes-doctor.mjs` (318 LOC)

## What it is
```
hermes-doctor.mjs -- idempotent pre-launch verify+repair that makes the Hermes
desktop app + CLI launch RELIABLY EVERY TIME (HERMES-LAUNCH-RELIABILITY-MS0,
slot:bravo 2026-06-30). The permanent fix for "works 1 of 10 tries."

A `PRISM Hermes Doctor` scheduled task runs it (--no-warm) every 15 min + AtStartup + AtLogOn so
the box stays launch-ready; run it MANUALLY (with warm) right before launch for the fastest first
message. (It is NOT prepended into the desktop launcher .bat.) Every check is bounded (can NEVER
hang). Idempotent: a healthy
box is a no-op. Steps:
  1. IPv4 preference -- reassert `prefixpolicy ::ffff:0:0/96 60 1` if missing, so
     the Nous boot fetch + NAS-JWT auth can't SYN_SENT-hang on this box's dead IPv6
     (the 06-26 root cause; the netsh fix is not durable, so we re-assert it).
  2. Config -- run hermes-config-apply.py --apply (pins gpt-oss:120b @>=64K local,
  
```


## Exports
`parseDoctorArgs` (function), `hasIpv4Preference` (function), `classifyProxy` (function), `versionAtLeast` (function), `buildWarmBody` (function), `resolvePython` (function), `summarize` (function), `enabledCronJobs` (function)

## Key methods
`main`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/hermes-doctor.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find hermes-doctor.mjs` or `prism_session:master_index_query`; open the source at `scripts/hermes-doctor.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
