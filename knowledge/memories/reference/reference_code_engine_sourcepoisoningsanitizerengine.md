---
name: reference_code_engine_sourcepoisoningsanitizerengine
description: "AUTO-LEARNING-LOOP-MS0 / U-ALL12"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SourcePoisoningSanitizerEngine.ts
  sourceHash: ac3e84a3
  loc: 431
aliases: SourcePoisoningSanitizerEngine
---

# SourcePoisoningSanitizerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SourcePoisoningSanitizerEngine.ts` (431 LOC)

## What it is
AUTO-LEARNING-LOOP-MS0 / U-ALL12

```
SourcePoisoningSanitizerEngine — AUTO-LEARNING-LOOP-MS0 / U-ALL12
==================================================================

First-line defence against malicious feeds. Sits between U-ALL01
`ReputableSourceMonitorEngine` (the raw poller) and U-ALL02
`NoveltyDetectionEngine` (the dedup). A poisoned feed item that
makes it past sanitization risks corrupting downstream research +
roadmap state — so this engine rejects-then-quarantines anything
that fails its trust ladder.

Defence ladder (in order — first failure short-circuits to
QUARANTINED):
  1. **Allowlist check**: the item's `source` slug must appear in
     the per-source allowlist (`state/shared/auto-learning/
     source-allowlist.json`). Unknown source → `not_allowlisted`.
  2. **Content-hash signing** (when the source advertises a
     public-key fingerprint): the engine computes SHA-256 of the
     canonical (title|guid
```


## Exports
`SourceAllowlistEntry` (interface), `SourceAllowlist` (interface), `SanitizeInput` (interface), `QuarantineReason` (type), `QuarantineRecord` (interface), `SanitizeOutcome` (interface), `SanitizerConfig` (interface), `EngineOpts` (interface), `ALLOWLIST_SCHEMA_VERSION` (const), `DEFAULT_MAX_TITLE_LEN` (const), `DEFAULT_MAX_SUMMARY_LEN` (const), `DEFAULT_GUID_LOG_TRUNC` (const), `SourcePoisoningSanitizerEngine` (class), `sourcePoisoningSanitizerEngine` (const)

## Key methods
`getConfig`, `setConfig`, `isAllowlistLoaded`, `getAllowlistSize`, `setAllowlist`, `loadAllowlistJson`, `resetAll`, `sanitize`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/SourcePoisoningSanitizerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SourcePoisoningSanitizerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SourcePoisoningSanitizerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
