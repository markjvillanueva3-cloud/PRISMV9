---
name: reference_code_engine_lathejobschedulingengine
description: "LatheJobSchedulingEngine — U-LTH50 (LATHE-MASTER P5 ERP)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LatheJobSchedulingEngine.ts
  sourceHash: 95cac07b
  loc: 537
aliases: latheJobSchedulingEngine
---

# latheJobSchedulingEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LatheJobSchedulingEngine.ts` (537 LOC)

## What it is
```
LatheJobSchedulingEngine — U-LTH50 (LATHE-MASTER P5 ERP)

Multi-machine lathe scheduling respecting:
  - 21-machine JM Die fleet (subset passable via machines param)
  - Per-job cycle time (from U-LTH48 quote.evidence.total_cycle_min) +
    setup time (setup_count × 45 min)
  - Due dates with Earliest-Due-Date (EDD) primary sort
  - Critical-Ratio (CR = slack/remaining_work) tiebreak for same due_date
  - Setup-minimizing adjacent-pair reordering inside each machine queue
    (changeover = big when material or fixture type changes, small when same)
  - Machine suitability (spindle bore, max OD, max Z, controller) — jobs
    are rejected from incompatible machines before scheduling

Algorithm:
  1. Filter feasible machines per job by envelope check
  2. Sort jobs globally by EDD → CR tiebreak
  3. For each job, assign to the earliest-available feasible machine
     using machine_available
```


## Exports
`MachineSpecSchema` (const), `MachineSpec` (type), `SchedulableJobSchema` (const), `SchedulableJob` (type), `ScheduleInputSchema` (const), `ScheduleInput` (type), `ScheduledOperation` (interface), `MachineTimeline` (interface), `UnscheduledJob` (interface), `ScheduleReport` (interface), `latheJobSchedulingEngine` (const), `LatheJobSchedulingEngine` (re-export), `SHIFT_MINUTES` (re-export)

## Key methods
`schedule`, `jobFromQuote`

## Depends on
[[reference_code_engine_latheautoquotefromprintengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/LatheJobSchedulingEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find latheJobSchedulingEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LatheJobSchedulingEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
