---
name: reference_code_engine_cadartifactstorageengine
description: "Persists per-file CAD regression-test artifacts to a canonical on-disk"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADArtifactStorageEngine.ts
  sourceHash: 68ef20d9
  loc: 364
aliases: CADArtifactStorageEngine
---

# CADArtifactStorageEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADArtifactStorageEngine.ts` (364 LOC)

## What it is
Persists per-file CAD regression-test artifacts to a canonical on-disk

```
CADArtifactStorageEngine — U-CINF07 (CAD-INFRA-MS0)

Persists per-file CAD regression-test artifacts to a canonical on-disk
layout and enforces a retention policy that keeps only the most recent N
batches. Each file in a batch has a directory containing up to four
artifact kinds:

  expected_step  — baseline ground-truth STEP
  actual_step    — STEP produced by this run
  diff_png       — pixel-diff overlay between the two
  error_log      — captured error text / stack

Layout:
  data/cad-test-artifacts/{batchId}/{fileId}/expected.step
                                             /actual.step
                                             /diff.png
                                             /error.log

Retention:
  DEFAULT_MAX_BATCHES = 5. On each pruneRetention() call, batches sorted
  by modification time are retained newest-first up to the limit; older
  batches are removed recursivel
```


## Exports
`ARTIFACT_KINDS` (const), `ArtifactKind` (type), `DEFAULT_ARTIFACT_ROOT` (const), `DEFAULT_MAX_BATCHES` (const), `ArtifactFS` (interface), `ArtifactRecord` (interface), `BatchManifest` (interface), `RetentionReport` (interface), `batchDir` (function), `fileDir` (function), `artifactPath` (function), `CADArtifactStorageEngine` (class), `cadArtifactStorageEngine` (const)

## Key methods
`existsSync`, `mkdirSync`, `readdirSync`, `statSync`, `getCapabilities`, `validate`, `write`, `listBatch`, `isDirectory`, `pruneRetention`

## Depends on
[[reference_code_engine_baseengine]] | [[reference_code_engine_iengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADArtifactStorageEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADArtifactStorageEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADArtifactStorageEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
