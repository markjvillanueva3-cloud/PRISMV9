---
name: reference_code_engine_quotingpipelinestresstestengine
description: "adaptive-variability scenario runner"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/QuotingPipelineStressTestEngine.ts
  sourceHash: 50612311
  loc: 314
aliases: QuotingPipelineStressTestEngine
---

# QuotingPipelineStressTestEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/QuotingPipelineStressTestEngine.ts` (314 LOC)

## What it is
adaptive-variability scenario runner

```
QuotingPipelineStressTestEngine — adaptive-variability scenario runner

Operator directive: "highly comprehensive, adaptive variability of scenarios
to train the pipelines and systems for quoting and how everything synergizes.
find leaks and gaps."

Composes the full quoting stack (lead-tiers / secondary-ops / tolerance /
freight / cross-part / calibration) into a single stress-test runner that:
  1. Generates N adaptive scenarios spanning material × process × tolerance
     × secondary-ops × weight × quantity × customer cohorts
  2. Runs each scenario through the full pipeline
  3. Records per-scenario {input, outputs[], engine_errors[], latency_ms}
  4. Aggregates a LEAK + GAP report — which engines fail most? which
     input ranges trigger which classes of errors? what are the
     pipeline's CV (coefficient of variation) hot spots?

Pure compositional engine — calls existing dispatc
```


## Exports
`StressScenarioMaterial` (type), `StressScenarioProcess` (type), `StressScenarioOp` (type), `StressScenario` (interface), `PipelineEngineResult` (interface), `ScenarioResult` (interface), `LeakGapReport` (interface), `StressTestInput` (interface), `StressTestResult` (interface), `QuotingPipelineStressTestEngine` (class), `quotingPipelineStressTestEngine` (const)

## Key methods
`run`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/QuotingPipelineStressTestEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find QuotingPipelineStressTestEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/QuotingPipelineStressTestEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
