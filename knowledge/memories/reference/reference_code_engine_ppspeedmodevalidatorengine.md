---
name: reference_code_engine_ppspeedmodevalidatorengine
description: "Validate spindle speed mode (G96/G97)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PPSpeedModeValidatorEngine.ts
  sourceHash: f2693faa
  loc: 333
aliases: PPSpeedModeValidatorEngine
---

# PPSpeedModeValidatorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PPSpeedModeValidatorEngine.ts` (333 LOC)

## What it is
Validate spindle speed mode (G96/G97)

```
PPSpeedModeValidatorEngine — Validate spindle speed mode (G96/G97)

The spindle-speed modal group decides whether S is RPM or surface
speed. A silent switch from one to the other multiplies or divides
spindle speed by the workpiece diameter, producing the classic lathe
failure modes:

  G96 S200   — Constant Surface Speed (CSS). S in m/min (metric) or
                sfm (imperial). RPM = (S × 1000) / (π × D_mm).
                Small diameters → huge RPM. Must pair with G50 Smax.
  G97 S2000  — Constant RPM. S in rev/min directly. Default mode
                for milling and rigid tapping.

Failure modes this validator catches:
  - css_without_s (error): G96 activated with no S-word — spindle
    runs at last-known value which is probably a G97 RPM number.
  - css_without_max_rpm (warning): G96 without a prior G50 S<max>
    limiter. Facing to zero diameter will try to spin to infinity.
```


## Exports
`SMSeverity` (type), `SMIssue` (interface), `SMResult` (interface), `SMOptions` (interface), `PPSpeedModeValidatorEngine` (class), `ppSpeedModeValidatorEngine` (const)

## Key methods
`validate`, `quickCheck`, `defaultOptions`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PPSpeedModeValidatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PPSpeedModeValidatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PPSpeedModeValidatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
