---
name: reference_india_ai_test_reds_backlog_2026_06_21
description: "India AI-domain test-reds backlog discovered + triaged 2026-06-21 (slot:india NEVER-IDLE FIXES sweep). 51 failing tests across 8 files in the CrossProcess/Neural/Consensus/Conformal/Reasoning/LoRA/Calibration/Drift families (5517 pass). All PRE-EXISTING (not caused by this session's Inventor/WEDM/material commits). Triaged by fix-class so the next loop iteration picks the next unit fast. SHIPPED this session: WEDM neural duty-cycle fixture (581269da0a)."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.615Z
aliases: reference_india_ai_test_reds_backlog_2026_06_21
---


**SWEEP METHOD:** `npx vitest run <FamilyName>` over india AI families (321 india AI test files enumerated). Two batches run: (A) `CrossProcess Neural` -> 95 files, 1 red file (FIXED: wedm-program-neural-analysis 36->38, commit `581269da0a`). (B) `Consensus Conformal Reasoning LoRA Calibration Drift` -> 238 files, 8 red files / 51 failing tests / 5517 passing. All reds are PRE-EXISTING (this session's commits touched only Inventor/WEDM/material -- verified by diff scope).

**THE 8 RED FILES (triaged by fix-class, ROI-ordered for next iterations):**

1. **`aiReasoningDispatcher.test.ts` — 1/49 — ROOT-CAUSED 2026-06-21, RE-ROUTED to FOXTROT (mill-facade design fork; NOT material, NOT india-fixable without guessing).** Test "should route P2P pipeline with aluminum parameters" (line 86) fails on line 97 `expect(data.success).toBe(true)`. **My earlier "6061-T6 material-resolution" hypothesis was WRONG (R12).** Ran both directly via `executeAIReasoningAction("ai_route_mill_pipeline", ...)` against dist/: BOTH aluminum (6061-T6/N) AND steel (4140/P) return `result.success=true, data.success=FALSE, data.request_type="print_to_program"`. Steel test PASSES only because it asserts `data.provenance` (line 112), NOT `data.success`; the aluminum test asserts `data.success===true` -> fails. Handler: `aiReasoningDispatcher.ts:1496` case `ai_route_mill_pipeline` -> `getMillFacade().orchestrate({request_type:"print_to_program",...})`; a NotWired THROW is caught and returns `{success:true, wired:false, partial}` (line 1521-1530), but the facade does NOT throw -- it RETURNS `{success:false}` (P2P sub-pipeline not fully wired), passed through as `result=response`. **THE FORK (foxtrot's mill-facade decision):** either (a) `MillMasterFacade.orchestrate` SHOULD throw NotWiredError for an unbuilt P2P (then the dispatcher wraps success:true and the test is RIGHT), or (b) returning success:false is correct and the aluminum test OVER-asserts P2P completion for a ROUTING action (dispatcher comment line ~1499/1520: "intent: verify routing, not P2P") -> the test should assert the routing contract like the steel sibling. India must NOT pick (a) vs (b) -- it is mill-facade (foxtrot) semantics. Defer to foxtrot. If foxtrot rules (b), the india-dispatcher test fix = replace line 97 `data.success===true` with a routing-contract assertion (result.success + request_type, matching steel) -- a clean india test-alignment then.

2. **`businessDispatcherLoRAGate.test.ts` — ⚠️ FABRICATED ROW (R12 corrected 2026-06-21, fresh-window re-verify).** This file DOES NOT EXIST (`npx vitest run` → "No test files found"). The real business tests are 12 `businessDispatcher.*-wire/-bridge.test.ts` files, **all 197/197 GREEN**. There is no `businessDispatch` callable to build and no `lora_gate/ml_lineage/promotion_gate/feature_store/outcome_bus/policy_ledger` action in businessDispatcher.ts (only `registerBusinessDispatcher` is exported). The "GHOST `businessDispatch is not a function`" diagnosis was hallucinated by the agent-batched sweep. **NOTHING to build here; do NOT route this to hotel.** Full correction: [[reference_fleet_test_sweep_triage_2026_06_21]]. (`lora_gate` exists as a graph node elsewhere, NOT a businessDispatcher action.)

3. **`AutoConsensusHooks.test.ts` — 21/23 — TRIAGED 2026-06-21: PEER IN-FLIGHT WIP, do NOT touch (lane discipline).** Failures (`expected undefined to be 'UserPromptSubmit'`, `expected 1 to be +0`, `expected undefined to be ''`) are the userprompt hook returning undefined fields. The test loads `.claude/hooks/auto-consensus-userprompt.mjs`, which is **uncommitted-modified (M) in the session-start git snapshot AND still M now** -- i.e. another chat is mid-edit on that hook + its `.test.mjs`. The mcp `AutoConsensusHooks.test.ts` reds are downstream of that peer WIP. file-claim-guard / never-commit-peer-claimed-files forbids india editing it. DEFER until the owning chat commits; re-check then (the reds may self-resolve on their commit).

4. **`lathe-ai-reasoning.test.ts` — 9/39 — SHIPPED 2026-06-21, `U-LATHE-G76-FOSSIL-REALIGN` (slot:india).** ROOT CAUSE = the backlog-#5 two-contradictory-test-files-one-engine pattern: this file's G76 Threading Dialects block was a `U-TEST-FOSSIL` absorbed orphan (799be785cb, 2026-05-12) asserting a SPECULATIVE 7-dialect design with non-canonical G-code (Haas `G76 D`-first, Okuma `G71`-as-threading, Mazak `G324/G424/G420`), contradicting `ThreadTurningEngine` G76_DIALECTS (cited-manual 5-dialect, 2026-04-27) AND the 14/14-passing companion `LatheAIReasoningEngine.test.ts` (2026-05-16; `selectG76Dialect("Haas")->fanuc_double`). Realigned the fossil to the engine's REAL contract (5 dialects, exact generateG76Code strings, exact CYCLE99 positional) -> 9 reds green, companion 14/14 unchanged, TEST-ONLY (zero engine change), 2-arm scrutiny PASS. **DEFERRED real gap surfaced** (whiskey/echo): engine lacks native LinuxCNC/Mach3 G76 dialects -> silently emits Fanuc G76 for a recognized LinuxCNC/Mach3 controller (latent wrong-G-code) -> needs `.cps`-grounded formats + a `selectG76Dialect` recognized-but-unmodeled warning. Full: [[reference_lathe_g76_dialect_fossil_2026_06_21]].

5. **`p1-lora-pairs.test.ts` — 4/36 (WEDM block) — ROOT-CAUSED 2026-06-21: HARD TWO-CONTRACT CONFLICT, operator/cross-domain DECISION required (NOT a unilateral india fix).** The 4 fails are all in the `WEDMLoRADatasetBuilderEngine` describe block (lines 48-86): requiredSchema lacks thickness_mm/pass_count/wire_break_count; buildDataset drops all test jobs (validate requires discharge actuals the closed-loop fixtures don't provide) -> 0 examples -> thicknessBucket/labels/instruction asserts fail. The Sinker/Laser/Waterjet/Grinding sibling blocks PASS. **The conflict (verified):** `wedmLoRADatasetBuilderEngine` is targeted by TWO test files with INCOMPATIBLE contracts: (A) `WEDMLoRADatasetBuilderWiring.test.ts` (mike's WEDM-TRAINING-WIZARD-MS0/U-WTW-AUDIT, **10/10 PASSING**) asserts `requiredSchema().features` **toEqual** exactly `[machine_class,material,op_type,wire_diameter]` + actuals `[peak_current,pulse_off,pulse_on]` (DISCHARGE contract -- the engine's current impl, U-LORA-WEDM-DATASET); (B) `p1-lora-pairs.test.ts` (U-CMCCL04 CAM-ML-CLOSEDLOOP) requires features to **contain** thickness_mm/pass_count + actuals wire_break_count (CLOSED-LOOP Ra contract, matching the 4 sibling machine types). A single requiredSchema() CANNOT satisfy `toEqual([4 discharge keys])` AND `contain(thickness_mm)` -- mutually exclusive. **R7 conflict, unresolvable without a decision:** does the WEDM LoRA dataset predict DISCHARGE params (mike's design; physically the core WEDM output; 10/10 passing + edmDispatcher `loraDataset` wired) or follow the fleet CLOSED-LOOP Ra contract (4 of 5 machine siblings)? **India must NOT pick** -- closed-loop migration breaks mike's 10/10 + the dispatcher action; discharge means the p1 WEDM block (4 tests) is testing a contract this engine deliberately doesn't implement and should be redirected/removed by the CAM-ML-CLOSEDLOOP owner. RECOMMENDATION to surface: the p1 WEDM block looks like the stale one (it assumed WEDM mirrors the other 4 machine types, but mike deliberately uses discharge ground-truth) -- fix/remove the p1 WEDM block to the discharge contract, OR operator rules WEDM must go closed-loop (then migrate engine + mike's wiring test + dispatcher together). Either path needs the CAM-ML-CLOSEDLOOP / mike owner, not a unilateral india edit.

6. **`U-WIRE-LATHE-LORA-REWARD-SHAPE.test.ts` — 3/11 — SHIPPED 2026-06-21, `U-LATHE-LORA-REWARD-CONTRACT` commit `fa08abd0d5` (slot:india).** ROOT CAUSE = schema<->engine naming drift (real production round-trip bug, NOT stale fixture): `REWARD_RESULT_SCHEMA` REQUIRED `bonuses`/`penalties` but `LatheLoRARewardShapingEngine.calculateReward` emits `bonus_reasons`/`penalty_reasons` (interface 41-42, return 222-223) -> `lathe_lora_reward_threshold`/`_summary` (input `{result:<prior calc output>}`) could NEVER validate a real calc result -> broken dispatcher round-trip in prod. Source of truth = engine + getSummary + the convention-named companion `LatheLoRARewardShapingEngine.test.ts` (23/23 passing, reads `*_reasons`); the U-WIRE schema/wiring-test/describe drifted to `bonuses`/`penalties` (the wiring-test's `r.bonuses` wasn't even type-valid against RewardResult). Aligned schema field names + calc-describe + dispatcher enum comment + the 2 wiring-test reads to `*_reasons`. Engine UNCHANGED (zero runtime change). 3 reds -> 11/11; companion 23/23 unchanged; authoritative tsc 0; 2-arm PASS (both confirmed real bug + correct source-of-truth + not weakening). Also newly-tracked the previously-UNTRACKED wiring-test file (136 lines; not peer-claimed). Same source-of-truth-wins pattern as #4/#7.

7. **`camDispatcher.lathe-lora-script-wire.test.ts` — 1/10 — SHIPPED 2026-06-21, commit `1e5c5b541b` (U-CAM-DUP-ROUTE).** The 1 fail was the anti-regression `new Set(ACTIONS).size === ACTIONS.length` (2197 vs 2198) -- a DUPLICATE `lathe_master_post_route` in the prism_cam enum (canonical group @1175 + dup group @2371) with a dead 2nd case (@20433, unreachable; detailed case @4243 wins by switch-order). Removed the dup enum entry + dead case, kept the canonical detailed handler. Zero behavior change; authoritative tsc 0 errors; 2-arm PASS. **BONUS:** the same no-dup assertion is shared by ~7 cam wire test files (bobcad/catia/dnc/fusion/hybrid/inventor-tool-export/lathe-lora) -- all were failing on this ONE dup, all now green. This was the genuinely-clean fix in the backlog (vs #1/#5/#3 = design-decisions/peer-WIP). REMAINING untriaged: #4 lathe-ai-reasoning 9/39, #6 U-WIRE-LATHE-LORA-REWARD 3/11.

**FIX-CLASS SUMMARY:** #1/#3/#4/#5/#6/#7 = stale-fixture-or-behavioral (adjudicate engine-vs-test per the U-INVENTORCAP/U-WEDM pattern: source-of-truth wins, never weaken). #2 = ghost needing a real callable-API build (hotel/india). Per one-unit-per-iteration: pick #1 next (single red, india core, root-cause narrowed). Sibling: [[reference_mastercam_cad_adapter_ghost_test_2026_06_21]] (same ghost class as #2), [[reference_material_db_descriptive_key_two_facet_2026_06_21]] (the descriptive-material-key resolution work #1 likely relates to).

## RESOLVED / CLOSED-OUT 2026-06-23 (slot:india, session b84af9c3) -- this backlog is now FULLY reconciled
Re-ran every family this session; the india AI-systems FIXES rung is CONCLUSIVELY closed (every red is green, owner-blocked-with-proof, or fabricated):
- **#3 AutoConsensusHooks** -- FIXED today, commit `b637e0be78` (the reds were stale removed-worktree paths `H:/prism-iooms0`, not a live peer edit). 3 red -> 23/23, 3-of-3 PASS.
- **#1 aiReasoningDispatcher** -- TWO reds today: (a) `should have 424 actions defined` was a NEW magic-count drift (real 426) -> FIXED `U-AIREASON-ACTION-COUNT-INVARIANT` (structural enum<->schema invariant, R9); (b) `ai_route_mill_pipeline` data.success:false RE-PROVEN owner-blocked to foxtrot (dispatcher routing correct `aiReasoningDispatcher.ts:1496-1535`; success:false originates in MillMasterOrchestratorFacadeEngine).
- **#5 p1-lora-pairs** -- re-confirmed 4/36 red, all in `WEDMLoRADatasetBuilderEngine` block; the R7 discharge-vs-closed-loop two-contract conflict stands. Owner-blocked (mike / CAM-ML-CLOSEDLOOP / operator).
- **#4/#6/#7** -- shipped 2026-06-21 (verified resolved). **#2** -- fabricated, file doesn't exist.
- Broad re-sweep this session GREEN: consensus+cross-process+continual+incremental+learning (1747/1747, 74 files); india-generic LoRA/Conformal/Drift (198/198, 7 files); aiReasoning cluster (only the 2 #1 reds above).
**Net: 0 india-solo-fixable reds remain. The 2 standing reds (#1b foxtrot, #5 mike) are PROVEN owner-blocked.** Also shipped this session: `884542bc` U-XPROC-ORCH-FANOUT-HONESTY (orchestrator R12 fan_out_mode) + wiki [[stub-fallback-must-signal-mode-not-pose-as-real]]. Drift: U-NN-TIER05 + FeedbackBus-publish verified already-shipped ([[reference_xproc_neural_milestone_drift_2026_06_23]]). True next india unit = orchestrator real-engine tier fan-out (multi-session, engine-side invoker).
