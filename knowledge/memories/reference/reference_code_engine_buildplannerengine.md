---
name: reference_code_engine_buildplannerengine
description: "U-FORE-02 (PSAU-FORESIGHT)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/BuildPlannerEngine.ts
  sourceHash: ae042493
  loc: 251
aliases: BuildPlannerEngine
---

# BuildPlannerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/BuildPlannerEngine.ts` (251 LOC)

## What it is
U-FORE-02 (PSAU-FORESIGHT)

```
BuildPlannerEngine — U-FORE-02 (PSAU-FORESIGHT)
================================================

Given a roadmap unit ID, produces an ordered DAG of atomic build steps
with prerequisites, estimated tokens, estimated duration, risk level
and a rollback stub for each step.

Pipeline:
  1. Locate the unit in milestone files (data/milestones/*.json)
  2. Hand off to AtomicStepDecomposerEngine for step extraction
  3. Apply the canonical ordering rules (schemas before engines,
     engines before dispatchers before tests before hooks before
     manifest regen before commit)
  4. Compute prerequisite edges and return a topologically sorted DAG

This is NOT an executor — it just plans. The /plan-build skill calls
it and presents the plan to the user for approval.
```


## Exports
`KIND_ORDER` (const), `StepDAGNode` (interface), `BuildPlan` (interface), `PlannerDeps` (interface), `BuildPlannerEngine` (class), `buildPlannerEngine` (const)

## Key methods
`plan`, `planFromUnit`, `validateOrdering`

## Depends on
[[reference_code_engine_atomicstepdecomposerengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/BuildPlannerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find BuildPlannerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/BuildPlannerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
