---
name: reference_code_engine_ollamacapabilityprobeengine
description: "BLACKWELL-AI-MS0 / U-CAP-PROBE (keystone)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/OllamaCapabilityProbeEngine.ts
  sourceHash: 83be4f07
  loc: 533
aliases: OllamaCapabilityProbeEngine
---

# OllamaCapabilityProbeEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/OllamaCapabilityProbeEngine.ts` (533 LOC)

## What it is
BLACKWELL-AI-MS0 / U-CAP-PROBE (keystone)

```
OllamaCapabilityProbeEngine — BLACKWELL-AI-MS0 / U-CAP-PROBE (keystone)

The single RUNTIME AUTHORITY for "what can this host actually run right now."

`ModelRoutingEngine` is a deliberately PURE scorer — its header states it does
NO network I/O and requires the caller to supply a `RoutingContext`
(`hardware` + `backendUp`). Nothing detected those from the live host, and its
catalog lists models that may be absent (e.g. `deepseek-r1:14b` is in the
catalog but not in the live `/api/tags`; the qwen3-* entries are "PULLING now"
capability declarations). A pure scorer therefore can route() to a model that
physically isn't installed.

THIS engine closes that gap. It performs the system/network I/O that
`ModelRoutingEngine` delegates, and returns a `CapabilitySnapshot` the caller
feeds straight into `ModelRoutingEngine.route()` — plus a catalog filter so
route() can only ever pick a model that
```


## Exports
`PROBE_CACHE_TTL_MS` (const), `DESKTOP_RESERVE_MIB` (const), `WDDM_FREE_IMPLAUSIBLE_RATIO` (const), `PROFILE_VRAM_THRESHOLDS_MIB` (const), `PROBE_IO_TIMEOUT_MS` (const), `GpuInfo` (interface), `LoadedModel` (interface), `ProbeSource` (type), `CapabilitySnapshot` (interface), `ProbeOpts` (interface), `ProbeReaders` (interface), `OllamaCapabilityProbeEngine` (class), `ollamaCapabilityProbeEngine` (const)

## Key methods
`clearTimeout`, `probe`, `toRoutingContext`, `routableCatalog`, `isModelPresent`, `getBestLocalModel`, `scoreOf`, `getBestReasoningModel`, `getBestChatModel`, `clearCache`

## Depends on
[[reference_code_engine_modelroutingengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/OllamaCapabilityProbeEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find OllamaCapabilityProbeEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/OllamaCapabilityProbeEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
