---
name: reference_code_script_sfcvariabilityresumeguard
description: "SFC-ACCURACY-MS1 — Variability batch resume-guard (watchdog). v4."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/sfc-variability-resume-guard.mjs
  sourceHash: 6d0996e6
  loc: 754
aliases: sfc-variability-resume-guard.mjs
---

# sfc-variability-resume-guard.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/sfc-variability-resume-guard.mjs` (754 LOC)

## What it is
```
SFC-ACCURACY-MS1 — Variability batch resume-guard (watchdog). v4.

The Stage-2 batch (`sfc-variability-batch-run.mjs`) is a multi-day workload
toward billions of compatibility-filtered combos. It has died TWICE on
machine switches (the H: drive is shared across PCs; disk state survives,
processes do not). This guard makes the batch self-resuming so it survives
machine switches, reboots, and session ends without manual relaunch.

── TWO-TASK ARCHITECTURE (why the guard does NOT spawn the batch) ───────
A process spawned as a child of a Windows Scheduled-Task process lives in
that task's job object; when the task instance ends, Task Scheduler closes
the job and KILLS the children — a detached `spawn` would NOT survive the
guard's own 5-minute task instance exiting (livelock: relaunch → exit →
batch killed → relaunch …). So the guard does NOT spawn the batch.
Instead `install-sfc-variabilit
```


## Exports
`GUARD_TASK_NAME` (re-export), `BATCH_TASK_PREFIX` (re-export), `RESUME_STATE_FILE` (re-export), `HOST_STALENESS_MIN` (re-export), `GUARD_CADENCE_MIN` (re-export), `batchTaskName` (re-export), `parseChunkName` (re-export), `groupChunksByWorker` (re-export), `lastNonEmptyLine` (re-export), `maxIdxInChunkText` (re-export), `computeResumeSkip` (re-export), `decideRelaunch` (re-export), `buildBatchArgv` (re-export), `classifyAliveFromLines` (re-export), `classifyAlive` (re-export), `requirePositiveInt` (re-export), `pickResumeParams` (re-export), `readResumeState` (re-export), `readSidecar` (re-export), `decideHostOwnership` (re-export)

## Key methods
`String`, `writeText`, `mkdirSync`, `appendFileSync`, `writeFileSync`, `relaunch`, `releaseLock`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/sfc-variability-resume-guard.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find sfc-variability-resume-guard.mjs` or `prism_session:master_index_query`; open the source at `scripts/sfc-variability-resume-guard.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
