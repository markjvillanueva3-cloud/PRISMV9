---
name: reference_code_engine_postfeatureauditengine
description: "codifies the 23-feature audit from"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PostFeatureAuditEngine.ts
  sourceHash: 56dc97fb
  loc: 237
aliases: PostFeatureAuditEngine
---

# PostFeatureAuditEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PostFeatureAuditEngine.ts` (237 LOC)

## What it is
codifies the 23-feature audit from

```
PostFeatureAuditEngine — codifies the 23-feature audit from
`state/shared/specs/HURCO-VM30i-V8.9-vs-V11-COMPARE-2026-05-25.md` as
a callable engine. Reads a .cps post-processor file from disk, counts
substring occurrences of known Hurco / PRISM-Enhanced advanced-feature
markers, and returns a structured FeaturePresenceReport with:

  - presence (count > 0)
  - bridge action recommendation (per the spec §5)
  - PRISM engine that owns the underlying calculation

This is the brand-agnostic substring scan — it works on Mastercam .pst,
hyperMILL .cnc, ESPRIT .est, etc. as well, because the FEATURE_TABLE is
keyed on substring patterns shared across the Hurco / Fanuc / Heidenhain
idiom universe. Brand-specific overlays are a follow-up.

Wired to prism_cam dispatcher as `cam_post_feature_audit` so the audit
becomes a PSN Engines-leg + system-viz node, not just a markdown spec.

Authored 2026-05-
```


## Exports
`BridgeAction` (type), `FeatureSpec` (interface), `FEATURE_TABLE` (const), `FeatureFinding` (interface), `FeaturePresenceReport` (interface), `CompareReport` (interface), `PostFeatureAuditEngine` (class), `postFeatureAuditEngine` (const)

## Key methods
`auditSource`, `auditFile`, `compareFiles`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PostFeatureAuditEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PostFeatureAuditEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PostFeatureAuditEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
