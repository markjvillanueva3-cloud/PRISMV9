---
name: reference_code_engine_ppprogrammergerengine
description: "Reassembles chunked G-code back into a single program"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PPProgramMergerEngine.ts
  sourceHash: 0a089b4c
  loc: 338
aliases: PPProgramMergerEngine
---

# PPProgramMergerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PPProgramMergerEngine.ts` (338 LOC)

## What it is
Reassembles chunked G-code back into a single program

```
PPProgramMergerEngine — Reassembles chunked G-code back into a single program

Inverse of PPProgramChunkerEngine. Takes an array of chunk texts (typically
from DNC drip-feed capture, chunked file set, or edited-in-place chunks)
and reassembles them into a single continuous G-code program.

Responsibilities:
  1. Strip duplicate leading `%` and `O####` program-number lines from
     continuation chunks (chunks 2..N).
  2. Strip chunk marker comments like `(--- CHUNK 2 — ORIGINAL LINES 51-100 ---)`.
  3. Strip modal restore blocks inserted by the chunker to avoid emitting
     redundant modal state when the merged program is executed as one unit.
  4. Preserve the trailing `%` terminator on the final chunk.
  5. Validate basic reassembly: program-number consistency, single M30/M2
     program-end, no duplicate tool-numbering anomalies.

Identification heuristics for markers and restore blo
```


## Exports
`MergerOptions` (interface), `MergeWarning` (interface), `MergeResult` (interface), `PPProgramMergerEngine` (class), `ppProgramMergerEngine` (const)

## Key methods
`merge`, `validate`, `defaultOptions`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PPProgramMergerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PPProgramMergerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PPProgramMergerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
