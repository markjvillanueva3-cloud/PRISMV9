---
name: reference_code_script_seedghostfromunwired
description: "seed-ghost-from-unwired.mjs — SYSTEM-VIZ-FS-COVERAGE-MS2/U-GHOST-UNWIRED"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/seed-ghost-from-unwired.mjs
  sourceHash: 611e6684
  loc: 363
aliases: seed-ghost-from-unwired.mjs
---

# seed-ghost-from-unwired.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/seed-ghost-from-unwired.mjs` (363 LOC)

## What it is
```
seed-ghost-from-unwired.mjs — SYSTEM-VIZ-FS-COVERAGE-MS2/U-GHOST-UNWIRED

Generates L13 ghost nodes for the BUILD_STATE-flagged unwired engines (861
engines on disk with no dispatcher reference). Each gets:
  - ghost.unwired-engine node with `proposed_wiring` + `confidence` fields
  - ghost-wire edge (relation: "proposed-wire") pointing at the inferred dispatcher

Dispatcher inference is heuristic — a name-keyword classifier with manual
confidence scoring. Confidence < 0.5 → emits the engine as ghost but with
`proposed_wiring: "UNKNOWN — review manually"` and NO edge.

Pure-export contract (for tests):
  listUnwiredEngines(enginesDir, dispatcherDir, opts) → [{ name, file, mtime, sizeKB }]
  inferDispatcher(engineName) → { dispatcher, confidence, reason }
  buildGhostFromUnwired(engine) → { node, edge | null }

Atomic write: temp + rename + Windows-safe EBUSY retry (mirrors
seed-ghost-nod
```


## Exports
`DISPATCHER_INFERENCE_RULES` (const), `MIN_CONFIDENCE` (const), `splitCamelCase` (function), `inferDispatcher` (function), `listUnwiredEngines` (function), `buildGhostFromUnwired` (function), `finalizeGraphMeta` (function), `main` (function), `MCP_TOOL_TO_DISP_NODE_ID` (re-export), `mcpToolToDispNodeId` (re-export)

## Key methods
`finalizeGraphMeta`, `writeGraphStreamingAtomic`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/seed-ghost-from-unwired.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find seed-ghost-from-unwired.mjs` or `prism_session:master_index_query`; open the source at `scripts/seed-ghost-from-unwired.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
