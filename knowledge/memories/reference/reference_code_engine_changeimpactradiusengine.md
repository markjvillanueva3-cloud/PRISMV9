---
name: reference_code_engine_changeimpactradiusengine
description: "U-FORE-01 (PSAU-FORESIGHT)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ChangeImpactRadiusEngine.ts
  sourceHash: 9cdce7de
  loc: 438
aliases: ChangeImpactRadiusEngine
---

# ChangeImpactRadiusEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ChangeImpactRadiusEngine.ts` (438 LOC)

## What it is
U-FORE-01 (PSAU-FORESIGHT)

```
ChangeImpactRadiusEngine — U-FORE-01 (PSAU-FORESIGHT)
======================================================

Universal pre-write blast-radius predictor. Given a proposed change to a
file, answers: what breaks, what runs, how long does a build take, and
how confident am I?

Returns a structured report in well under 300 ms by reusing the already
built EditImpactPredictorEngine graph and doing shallow filesystem scans
for hooks and dispatchers (both are small, bounded populations).

Design invariants:
  1. **Read-only** — never modifies the target file, graph, or FS.
  2. **Safe on missing inputs** — absent graph or unknown file yields a
     low-confidence report, not an exception.
  3. **Bounded work** — transitive depth capped (default 3) and per-level
     breadth capped so pathological graphs can't burn the time budget.

@module engines/ChangeImpactRadiusEngine
```


## Exports
`DEFAULT_TRANSITIVE_DEPTH` (const), `DEFAULT_BREADTH_PER_LEVEL` (const), `MS_PER_TS_FILE` (const), `TSC_STARTUP_MS` (const), `TIME_BUDGET_MS` (const), `ChangeKind` (type), `BlastRadiusInput` (interface), `BlastRadiusReport` (interface), `EngineDependencies` (interface), `ChangeImpactRadiusEngine` (class), `changeImpactRadiusEngine` (const)

## Key methods
`predictBlastRadius`, `predictBlastRadiusSync`

## Depends on
[[reference_code_engine_editimpactpredictorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/ChangeImpactRadiusEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ChangeImpactRadiusEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ChangeImpactRadiusEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
