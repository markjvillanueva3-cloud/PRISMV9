---
name: reference_code_engine_estimateengine
description: "customer estimates / quotes for the PRISM ERP (galaxy:business, slot:hotel)."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/EstimateEngine.ts
  sourceHash: dab0d388
  loc: 245
aliases: EstimateEngine
---

# EstimateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/EstimateEngine.ts` (245 LOC)

## What it is
customer estimates / quotes for the PRISM ERP (galaxy:business, slot:hotel).

```
EstimateEngine — customer estimates / quotes for the PRISM ERP (galaxy:business, slot:hotel).

QuickBooks-parity: the "Estimate" function set (build a non-binding price proposal, discount it,
compute tax, send → accept/reject/expire, convert an accepted estimate to a sales order).
QB-PARITY-MS0 Phase-2 engine #1 (A/R revenue cycle, the first step of the QB sales workflow:
Estimate → Sales Order → Invoice → Payment).

PRISM synergy (QUICKBOOKS-PARITY-PLAN.md thesis): an estimate is BORN from a quote —
`quote_to_ship` / the quoting galaxy produces an accepted price → formalized here as an estimate;
the tax line is computed by `SalesUseTaxEngine` (not re-derived); an accepted estimate hands off
to `SalesOrderEngine` (Phase-2 #2). An estimate is NON-BINDING and posts nothing to the GL until it
becomes an invoice — so it carries no GL effect (deliberately: §8.4 — not money movement yet).

Fin
```


## Exports
`EstimateInput` (type), `EstimateLineResult` (interface), `Estimate` (interface), `EstimateEngine` (class), `estimateEngine` (const)

## Key methods
`create`, `transition`, `isExpired`, `toSalesOrder`

## Depends on
[[reference_code_engine_salesusetaxengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/EstimateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find EstimateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/EstimateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
