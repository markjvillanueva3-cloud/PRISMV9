---
name: reference_code_engine_millloraensembleorchestratorengine
description: "MillLoRAEnsembleOrchestratorEngine — Ensemble Pipeline Orchestrator (Mill parity)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MillLoRAEnsembleOrchestratorEngine.ts
  sourceHash: f2096df9
  loc: 374
aliases: millLoRAEnsembleOrchestratorEngine
---

# millLoRAEnsembleOrchestratorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MillLoRAEnsembleOrchestratorEngine.ts` (374 LOC)

## What it is
```
MillLoRAEnsembleOrchestratorEngine — Ensemble Pipeline Orchestrator (Mill parity)
===================================================================================

Orchestrates end-to-end ensemble inference across MillLoRA adapters.
Coordinates model selection, parallel inference, voting, combination,
and MILL-CANONICAL physical-process abort signals.

Mill parity for LatheLoRAEnsembleOrchestratorEngine (LATHE-LORA-MS0
U-LLR44) with MILL-CANONICAL extensions:

  - Per-run axis_mode tag — every ensemble run is scoped to one axis
    (3_axis / 4_axis_index / 5_axis_simul / shared) so cross-axis
    model contributions don't pollute consensus
  - Per-execution mill signals — chatter_breach / fpa_pass /
    tcpm_solver_fault flags on each ModelExecution let the cascade
    mode early-stop on chatter (critical — tool damage risk)
  - 2 MILL-CANONICAL run statuses layered on lathe-shared:
 
```


## Exports
`MillEnsembleExecutionMode` (type), `MillEnsembleRunStatus` (type), `MILL_CANONICAL_RUN_STATUSES` (const), `MillAxisMode` (type), `MillModelExecution` (interface), `MillEnsembleRun` (interface), `MillEnsembleOrchConfig` (interface), `millLoRAEnsembleOrchestratorEngine` (const), `MillLoRAEnsembleOrchestratorEngine` (re-export)

## Key methods
`setConfig`, `getConfig`, `startRun`, `recordExecution`, `shouldCascadeStop`, `completeRun`, `getRun`, `getActiveRuns`, `getCompletedRuns`, `getStats`, `getStatsByAxis`, `getSummary`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MillLoRAEnsembleOrchestratorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find millLoRAEnsembleOrchestratorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MillLoRAEnsembleOrchestratorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
