---
name: reference_code_engine_cammldriftmonitorengine
description: "U-CAM-ML-07"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMMLDriftMonitorEngine.ts
  sourceHash: 59dc512b
  loc: 352
aliases: CAMMLDriftMonitorEngine
---

# CAMMLDriftMonitorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMMLDriftMonitorEngine.ts` (352 LOC)

## What it is
U-CAM-ML-07

```
CAMMLDriftMonitorEngine — U-CAM-ML-07
======================================

Continuous evaluation + drift detection for the ML training pipeline.

Re-evaluates the baseline + LoRA models against the held-out test set
(emitted by U-CAM-ML-03 customer-disjoint split) and compares MAE to the
previous run. If MAE degrades more than a configurable threshold
(default 5%) week-over-week, emits a drift_alert.

Writes append-only to `data/state/CAM_ML_DRIFT_LOG.jsonl` so a nightly
cron can run this and produce a time-series dashboard without needing
a database. Each line is one DriftReport entry.

Why append-only JSONL vs. rewriting a single file: drift is a time-series
observation — we never want to lose history. A JSONL file lets `tail -n`
give the latest state and `wc -l` gives the run count, while the full file
feeds a Grafana-style plot.

Authored 2026-04-21 — CAM-EXHAUST-MS0 PHASE-ML-TRAI
```


## Exports
`AlertLevel` (type), `ModelEvalRecord` (interface), `DriftAlert` (interface), `DriftReport` (interface), `DriftMonitorOptions` (interface), `CAMMLDriftMonitorEngine` (class), `camMLDriftMonitorEngine` (const)

## Key methods
`runOnce`, `readLog`

## Depends on
[[reference_code_engine_camfeatureextractorengine]] | [[reference_code_engine_cammlsplitengine]] | [[reference_code_engine_cambaselineregressorengine]] | [[reference_code_engine_camloraadaptertrainerengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CAMMLDriftMonitorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMMLDriftMonitorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMMLDriftMonitorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
