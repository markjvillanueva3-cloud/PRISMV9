---
name: reference_code_engine_hypermillsurfacequalitymappingengine
description: "Surface Quality + Safety Mapping Registry"
metadata:
  type: reference
  kind: code-engine
  galaxy: hypermill
  sourcePath: mcp-server/src/engines/hypermill/HyperMillSurfaceQualityMappingEngine.ts
  sourceHash: 27d38bf9
  loc: 781
aliases: HyperMillSurfaceQualityMappingEngine
---

# HyperMillSurfaceQualityMappingEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/hypermill/HyperMillSurfaceQualityMappingEngine.ts` (781 LOC)

## What it is
Surface Quality + Safety Mapping Registry

```
HyperMillSurfaceQualityMappingEngine — Surface Quality + Safety Mapping Registry

U-HKC43: Maps hyperMILL CAM parameters to downstream surface quality and safety
physics engines. Each mapping captures:
  - Which CAM parameter drives surface quality (Ra/Rz/scallop/residual stress)
  - Which safety hook enforces a hard limit (collision, breakage, over-travel, overspeed)
  - The governing physics formula (e.g. Ra = f²/(32R) for turning)

Target engines:
  - SurfaceFinishPredictorEngine  — Ra/Rz via kinematic and stochastic models
  - SurfaceIntegrityEngine        — white layer, work hardening, microstructure
  - ResidualStressPredictionEngine — residual stress from thermal + mechanical load

Coverage:
  Surface quality mappings:
    turning feed/nose_radius      → Ra prediction:   18 types × 3 = 54
    3D stepover/ball_radius       → scallop height:  12 types × 2 = 24
    5-axis scallop_hei
```


## Exports
`SurfaceQualityTargetEngine` (type), `SurfaceQualityMappingType` (type), `SurfaceQualityMapping` (interface), `SurfaceQualityMappingSummary` (interface), `SURFACE_QUALITY_MAPPINGS` (const), `SURFACE_QUALITY_MAPPING_SUMMARY` (const), `HyperMillSurfaceQualityMappingEngine` (class), `surfaceQualityMappingEngine` (const)

## Key methods
`sq`, `safety`, `getMappingsByDomain`, `getMappingsByType`, `getSafetyMappings`, `getSurfaceQualityMappings`, `getMappingsByEngine`, `getMappingsByHook`, `getMappingById`, `getMappingsWithFormula`, `getSummary`

## Depends on
[[reference_code_engine_hypermillsurfacequalitymappingengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/hypermill/HyperMillSurfaceQualityMappingEngine.ts`
- **Galaxy:** **hypermill**
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HyperMillSurfaceQualityMappingEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/hypermill/HyperMillSurfaceQualityMappingEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
