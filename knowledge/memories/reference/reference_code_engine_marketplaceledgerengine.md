---
name: reference_code_engine_marketplaceledgerengine
description: "the platform-as-intermediary FINANCIAL SPINE of the PRISM manufacturing"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MarketplaceLedgerEngine.ts
  sourceHash: a0651b13
  loc: 463
aliases: MarketplaceLedgerEngine
---

# MarketplaceLedgerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MarketplaceLedgerEngine.ts` (463 LOC)

## What it is
the platform-as-intermediary FINANCIAL SPINE of the PRISM manufacturing

```
MarketplaceLedgerEngine — the platform-as-intermediary FINANCIAL SPINE of the PRISM manufacturing
networking marketplace (galaxy:business, slot:hotel). THE money engine: it computes the
marketplace-specific accounting events — buyer escrow deposit, take-rate commission, supplier payout
net of commission, and multi-party invoice split — and RETURNS BALANCED double-entry GL lines AS
DATA. It is PURE: it does NOT post to the GeneralLedger directly (it returns lines the caller posts),
so it is deterministic + fully testable.

WHAT IT IS / WHY IT'S NET-NEW: PRISM's existing {@link GeneralLedgerEngine} models a SINGLE-PARTY shop
(JM Die invoices a customer, pays payroll, accrues job cost). A marketplace is a THREE-party flow: a
BUYER funds an order, the PLATFORM holds those funds in escrow and takes a commission, and the
SUPPLIER is paid the remainder. None of the existing recorders model "fun
```


## Exports
`MarketplaceGlLine` (interface), `EscrowDepositResult` (interface), `PayoutResult` (interface), `SplitPartyShare` (interface), `SplitInvoiceResult` (interface), `EscrowDepositInput` (type), `PayoutInput` (type), `SplitInvoiceInput` (type), `MarketplaceLedgerEngine` (class), `marketplaceLedgerEngine` (const)

## Key methods
`recordEscrowDeposit`, `recordPayout`, `recordSplitInvoice`, `escrowBalance`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/MarketplaceLedgerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MarketplaceLedgerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MarketplaceLedgerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
