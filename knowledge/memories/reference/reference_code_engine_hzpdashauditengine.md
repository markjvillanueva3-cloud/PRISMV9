---
name: reference_code_engine_hzpdashauditengine
description: "Pure-core audit-envelope builder. Every state-changing call through the"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HzpDashAuditEngine.ts
  sourceHash: b5777c65
  loc: 83
aliases: HzpDashAuditEngine
---

# HzpDashAuditEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HzpDashAuditEngine.ts` (83 LOC)

## What it is
Pure-core audit-envelope builder. Every state-changing call through the

```
HzpDashAuditEngine — HZD-03 (HZP-DASH-MS0)

Pure-core audit-envelope builder. Every state-changing call through the
dashboard control server (or the MCP wrapper) produces an audit envelope
that the server appends to `state/shared/hzp-dash-audit.jsonl`. This
engine is pure: it builds + validates envelopes but never writes to disk.

The envelope shape is intentionally narrow (12 fields max) so the JSONL
stays grep-friendly and parser-cheap. `audit_id` is a deterministic
base36-timestamp + 6-hex random — enough to disambiguate without crypto.
```


## Exports
`AuditEnvelopeRequestSchema` (const), `AuditEnvelopeRequest` (type), `AuditEnvelope` (interface), `HzpDashAuditEngine` (class)

## Key methods
`build`, `toJsonl`, `renderLine`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/HzpDashAuditEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HzpDashAuditEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HzpDashAuditEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
