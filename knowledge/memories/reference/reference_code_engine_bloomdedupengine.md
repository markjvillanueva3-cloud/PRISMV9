---
name: reference_code_engine_bloomdedupengine
description: "Fast Negative Deduplication Filter"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/BloomDedupEngine.ts
  sourceHash: 6da33623
  loc: 508
aliases: BloomDedupEngine
---

# BloomDedupEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/BloomDedupEngine.ts` (508 LOC)

## What it is
Fast Negative Deduplication Filter

```
BloomDedupEngine — Fast Negative Deduplication Filter

USSH Phase 0.25: Scientific Foundations — Complexity Theory Enhancement

Provides O(1) probabilistic duplicate detection using Bloom filters.
Before expensive embedding lookup, check Bloom filter for definite non-duplicates.

Mathematical Foundation:
  - False positive rate: (1 - e^(-kn/m))^k
  - For n=2000 items, m=1M bits, k=7 hashes: FPR ≈ 0.8%
  - Space: 128KB for 1M bits
  - Time: O(k) = O(1) per query

Usage Pattern:
  1. Check Bloom filter first (fast, O(1))
  2. If filter returns "definitely not present" → skip expensive embedding
  3. If filter returns "might be present" → run full semantic dedup

@module engines/BloomDedupEngine
@milestone USSH-P0.25/U-SCI01
```


## Exports
`BloomFilterConfig` (interface), `BloomFilterStats` (interface), `DedupCheckResult` (interface), `BulkCheckResult` (interface), `BloomDedupEngine` (class), `AssetBloomFilters` (class), `bloomDedupEngine` (const), `assetBloomFilters` (const)

## Key methods
`add`, `addBulk`, `mightContain`, `checkDedup`, `checkBulk`, `getStats`, `calculateOptimalParams`, `createOptimized`, `getFilter`, `addToFilter`, `mightContainInFilter`, `listFilters`, `getAllFilterStats`, `estimateUnionFpr`, `merge`, `clear`, `clearAll`, `getConfig`, `export`, `import`, `isDefinitelyNew`, `loadExistingAssets`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/BloomDedupEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find BloomDedupEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/BloomDedupEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
