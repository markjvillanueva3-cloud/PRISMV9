---
name: reference_code_engine_universalcadindexengine
description: "Universal CAD file indexer covering every format present across H:/prism."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/UniversalCADIndexEngine.ts
  sourceHash: d51aacaf
  loc: 287
aliases: UniversalCADIndexEngine
---

# UniversalCADIndexEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/UniversalCADIndexEngine.ts` (287 LOC)

## What it is
Universal CAD file indexer covering every format present across H:/prism.

```
UniversalCADIndexEngine — U-CADC01 (CAD-COMPLETE-MS0)

Universal CAD file indexer covering every format present across H:/prism.
Per R13-RE-06 archaeology, this engine is a thin facade over the existing
CADFileIndexerEngine — it does NOT reimplement scanning, hashing, or diffing.
Instead it:
  1. Expands the default root-path set beyond JM Die folders to cover the
     full H:/prism CAD corpus.
  2. Produces a per-format coverage report asserting every target extension
     (IPT, IAM, SLDPRT, SLDASM, STEP, STP, IGES, IGS, DWG, DXF, STL, F3D,
     3DM, X_T, SAT) is represented in the scan.
  3. Returns an AtomicValue-style result for programmatic consumption.

Design:
  - Composition, not inheritance — delegates to cadFileIndexerEngine.
  - Injectable FS passes through to the underlying indexer (test-safe).
  - Coverage report exposed as IndexCoverage structure with per-format
    counts 
```


## Exports
`TARGET_CAD_FORMATS` (const), `UNIVERSAL_ROOT_PATHS` (const), `IndexCoverage` (interface), `UniversalIndexResult` (interface), `UniversalCADIndexEngine` (class), `universalCADIndexEngine` (const)

## Key methods
`index`, `computeCoverage`, `load`, `hasUniversalCoverage`, `scan`, `search`, `stats`

## Depends on
[[reference_code_engine_cadfileindexerengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/UniversalCADIndexEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find UniversalCADIndexEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/UniversalCADIndexEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
