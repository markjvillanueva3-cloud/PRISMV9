---
name: reference_code_engine_mcmastercarrapiadapter
description: "real-time vendor pricing per McMaster B2B API"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/McMasterCarrAPIAdapter.ts
  sourceHash: a121308f
  loc: 205
aliases: McMasterCarrAPIAdapter
---

# McMasterCarrAPIAdapter -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/McMasterCarrAPIAdapter.ts` (205 LOC)

## What it is
real-time vendor pricing per McMaster B2B API

```
McMasterCarrAPIAdapter — real-time vendor pricing per McMaster B2B API

Operator directive (verbatim): "Utilize real time real world pricing of
materials, tooling costs ... https://www.mcmaster.com/help/api/".

Conforms to the FreightAdapter / VendorRealtimePricing adapter shape so
it drops into the existing engines without surgery. Auth via env var
`MCMASTER_API_KEY` (per McMaster API docs §Authentication). API base
defaults to https://api.mcmaster.com — overridable for staging/test.

Endpoints used (per McMaster B2B API docs):
  GET /v1/products/{partNumber}          → product spec + list price
  POST /v1/orders/quote                   → contract pricing + lead time
  GET /v1/products/{partNumber}/inventory → real-time stock

SAFETY:
  - Engine NEVER throws — all network errors surface as {ok:false, reason}
  - 5-minute response cache to avoid hammering the API
  - Rate-limit handling:
```


## Exports
`MMPartInfo` (interface), `MMQuoteResult` (interface), `setFetch` (function), `McMasterCarrAPIAdapter` (class), `mcMasterCarrAPIAdapter` (const)

## Key methods
`quotePart`, `clearTimeout`, `quoteBatch`, `clearCache`, `getCacheStats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/McMasterCarrAPIAdapter.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find McMasterCarrAPIAdapter` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/McMasterCarrAPIAdapter.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
