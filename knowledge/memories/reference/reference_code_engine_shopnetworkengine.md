---
name: reference_code_engine_shopnetworkengine
description: "PRISM MCP Server — ShopNetworkEngine (E1134, CAMX-MS21/U02)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ShopNetworkEngine.ts
  sourceHash: 7dd686c3
  loc: 592
aliases: shopNetworkEngine
---

# shopNetworkEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ShopNetworkEngine.ts` (592 LOC)

## What it is
```
PRISM MCP Server — ShopNetworkEngine (E1134, CAMX-MS21/U02)

Future networking concept for connecting machine shops for job sharing and
overflow routing. All data is modelled in-memory — no actual network calls
are made. The API surface is intentionally designed so that a real marketplace
back-end can be wired in later without changing the public interface.

Privacy model
─────────────
• Search returns scored matches but never exposes proprietary drawings.
• Shop identity is hidden in broadcast responses until mutual interest is
  confirmed (NDA gate). Only capability metadata and a masked shop_id are
  shared during search/broadcast phases.
• Actual job details (geometry, tolerances, drawings) are out of scope until
  an NDA has been executed — that workflow is explicitly stubbed here.

Actions (wired in camDispatcher):
  shop_network_register  — register or update a shop profile
  shop
```


## Exports
`ShopMachine` (interface), `Certification` (type), `ShiftSchedule` (interface), `ShopProfile` (interface), `ShopSearchCriteria` (interface), `ShopMatch` (interface), `JobBroadcast` (interface), `JobResponse` (interface), `shopNetworkEngine` (const)

## Key methods
`registerShop`, `searchShops`, `broadcastJob`, `respondToJob`, `getNetworkStats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ShopNetworkEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find shopNetworkEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ShopNetworkEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
