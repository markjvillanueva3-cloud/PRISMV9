---
name: reference_code_script_rendervizscreenshot
description: "render-viz-screenshot.mjs -- GRAPH-AS-LLM-CONTEXT-MS0 / U-GAC04 (slot:sierra)"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/render-viz-screenshot.mjs
  sourceHash: 9ad190d6
  loc: 294
aliases: render-viz-screenshot.mjs
---

# render-viz-screenshot.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/render-viz-screenshot.mjs` (294 LOC)

## What it is
```
render-viz-screenshot.mjs -- GRAPH-AS-LLM-CONTEXT-MS0 / U-GAC04 (slot:sierra)

Headless render of an ego-graph (GAC01 EgoGraph JSON) to a real PNG using a
SYSTEM Chrome/Edge binary's built-in `--headless --screenshot` -- NO npm
dependency (puppeteer/playwright/canvas are all absent from the tree, and
"ask before adding an unseen library" applies; the system browser is not a
package). When no Chrome/Edge is found the script exits with a structured
`{ok:false, reason:"chromium-unavailable"}` so the caller (DualChannelContextEngine)
degrades to the spec's MD-text fallback.

Spawning uses execFileSync (execFile family -- argv array, NO shell), so the
Chrome path + flags are never shell-interpolated (no command injection).

Input  : an EgoGraph JSON file (`--ego=<path>`), produced by GraphContextLensEngine.
Output : a PNG (`--out=<path>`) + a one-line JSON status on stdout:
           {ok, pa
```


## Exports
`renderEgoToPng` (function), `esc` (re-export), `buildHtml` (re-export), `findChrome` (re-export), `filterNodes` (re-export), `isEmptyForLayer` (re-export)

## Key methods
`execFileSync`, `fileUrl`, `writeFileSync`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/render-viz-screenshot.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find render-viz-screenshot.mjs` or `prism_session:master_index_query`; open the source at `scripts/render-viz-screenshot.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
