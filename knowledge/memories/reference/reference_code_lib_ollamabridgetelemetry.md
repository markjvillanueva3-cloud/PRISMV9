---
name: reference_code_lib_ollamabridgetelemetry
description: "ollama-bridge-telemetry.mjs — per-call telemetry for ollama-prism-bridge"
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/ollama-bridge-telemetry.mjs
  sourceHash: 2d9276ca
  loc: 241
aliases: ollama-bridge-telemetry.mjs
---

# ollama-bridge-telemetry.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/ollama-bridge-telemetry.mjs` (241 LOC)

## What it is
```
ollama-bridge-telemetry.mjs — per-call telemetry for ollama-prism-bridge
(OLLAMA-EXPAND-MS0/U-OE-BRIDGE-TELEMETRY).

Load-bearing prerequisite for U-OE-BRIDGE-L2B: the L2b spec calls model
tool-selection accuracy "an unproven empirical risk". We cannot scale the
tool surface without first measuring how the existing 3-tool surface
performs. This module records every bridge tool-call to an append-only
JSONL ledger and exposes a pure summarizer so L2b can gate scope on real
measurement, not optimism.

Design contract (pure-core + injected-impure-shell, matches the bridge):
  - `recordToolCallEvent(state, event)` is a pure builder — returns a frozen
    event object with normalized fields. No I/O.
  - `summarizeTelemetry(events, opts)` is pure — group + count.
  - `appendTelemetryEvent(event, opts)` is the only side-effectful entry.
    I/O deps (`appendImpl`, `now`, `ledgerPath`) are inject
```


## Exports
`SCHEMA_VERSION` (const), `DEFAULT_LEDGER_REL` (const), `KNOWN_TOOLS` (const), `MAX_EVENT_BYTES` (const), `ERROR_CLASSES` (const), `telemetryDisabled` (function), `recordToolCallEvent` (function), `eventToJsonLine` (function), `appendTelemetryEvent` (function), `pickLedgerPath` (function), `summarizeTelemetry` (function), `percentile` (function)

## Key methods
`mkdirImpl`, `appendImpl`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/ollama-bridge-telemetry.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find ollama-bridge-telemetry.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/ollama-bridge-telemetry.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
