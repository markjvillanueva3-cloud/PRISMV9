---
name: reference_code_engine_freecadautomationbridge
description: "Python-subprocess bridge for FreeCAD (.FCStd)."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/FreeCADAutomationBridge.ts
  sourceHash: e9995db8
  loc: 479
aliases: FreeCADAutomationBridge
---

# FreeCADAutomationBridge -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/FreeCADAutomationBridge.ts` (479 LOC)

## What it is
Python-subprocess bridge for FreeCAD (.FCStd).

```
FreeCADAutomationBridge — Python-subprocess bridge for FreeCAD (.FCStd).

Spawns FreeCADCmd (or python with FreeCAD on PYTHONPATH) running a small
companion script that speaks newline-delimited JSON over stdin/stdout.
Composes existing FreeCAD support engines (none wired yet — this bridge is
the first FreeCAD surface) and exposes the same lifecycle as the
SolidWorks/Inventor/Mastercam bridges: open/getGeometry/getToolpaths/
getOperationTree/exportSTEP/close.

Supported file formats: .FCStd (primary), .FCStd1 (backup/autosave).
Mock mode: set PRISM_CAD_MOCK=1 to bypass spawn/IPC and return fixtures.

@engine FreeCADAutomationBridge
@shortcode E1302
@milestone CAD-AUTOMATION-MS0/U-CAUT04
```


## Exports
`AtomicValue` (interface), `FCStdExt` (type), `FCStdGeometryEntity` (interface), `FCStdGeometry` (interface), `FCStdOperation` (interface), `FCStdToolpathGroup` (interface), `FCStdJob` (interface), `FCStdOperationTree` (interface), `FreeCADAutomationBridge` (class), `freecadAutomationBridge` (const)

## Key methods
`execFile`, `resolve`, `spawn`, `clearTimeout`, `waitReady`, `send`, `reject`, `destroy`, `open`, `getGeometry`, `getToolpaths`, `getOperationTree`, `exportSTEP`, `close`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/FreeCADAutomationBridge.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find FreeCADAutomationBridge` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/FreeCADAutomationBridge.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
