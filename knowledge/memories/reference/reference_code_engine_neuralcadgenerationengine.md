---
name: reference_code_engine_neuralcadgenerationengine
description: "CADCAM-DAGI-MS0/U-DAGI07"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/NeuralCADGenerationEngine.ts
  sourceHash: 0b8bd65d
  loc: 737
aliases: NeuralCADGenerationEngine
---

# NeuralCADGenerationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/NeuralCADGenerationEngine.ts` (737 LOC)

## What it is
CADCAM-DAGI-MS0/U-DAGI07

```
NeuralCADGenerationEngine — CADCAM-DAGI-MS0/U-DAGI07

THE CORE ENGINE. Generates valid CAD sequences from descriptions, blueprints,
or feature specifications. Outputs parametric CAD code (CadQuery preferred).

Architecture — pure controller, injectable GenerationBackend:
  The engine owns no neural weights. A `GenerationBackend` plug-in generates
  token sequences from prompts. Tests supply a deterministic mock; production
  wires Ollama/HuggingFace/OpenAI.

Pipeline:
  1. Parse input (text, blueprint, feature list)
  2. Retrieve similar parts via RAG (U-DAGI06)
  3. Generate token sequence via backend
  4. Detokenize to CadQuery code (U-DAGI01)
  5. Validate syntax (compile-check)
  6. Auto-retry on failure (up to maxRetries)
  7. Return CAD with confidence score

Key methods:
  generate(input, backend, config?)     — full generation pipeline
  parseInput(input)                     — no
```


## Exports
`TokenSeq` (type), `InputType` (type), `FeatureSpec` (interface), `BlueprintData` (interface), `GenerationInput` (interface), `ParsedInput` (interface), `GenerationConfig` (interface), `GenerationBackend` (interface), `ValidationResult` (interface), `GenerationResult` (interface), `NeuralCADGenerationEngine` (class), `neuralCADGenerationEngine` (const)

## Key methods
`generate`, `getCapabilities`, `validate`, `parseInput`, `extractFeaturesFromText`, `featuresToDescription`, `blueprintToDescription`, `blueprintToFeatures`, `buildPrompt`, `toCadQuery`, `featuresToCadQuery`, `validateSyntax`, `listPatterns`, `getPattern`

## Depends on
[[reference_code_engine_baseengine]] | [[reference_code_engine_iengine]] | [[reference_code_engine_cadtokenrepresentationengine]] | [[reference_code_engine_cadretrievalaugmentationengine]] | [[reference_code_engine_cadfeatureembeddingengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/NeuralCADGenerationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find NeuralCADGenerationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/NeuralCADGenerationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
