---
name: reference_code_engine_hypermillkienzlemappingengine
description: "Kienzle Force Mapping Registry"
metadata:
  type: reference
  kind: code-engine
  galaxy: hypermill
  sourcePath: mcp-server/src/engines/hypermill/HyperMillKienzleMappingEngine.ts
  sourceHash: 07ca9ac9
  loc: 554
aliases: HyperMillKienzleMappingEngine
---

# HyperMillKienzleMappingEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/hypermill/HyperMillKienzleMappingEngine.ts` (554 LOC)

> [physics] Imports canonical physics constants (`src/physics/constants.ts`) -- safety-relevant node.

## What it is
Kienzle Force Mapping Registry

```
HyperMillKienzleMappingEngine — Kienzle Force Mapping Registry

U-HKC40: Creates a mapping registry connecting all hyperMILL CAM cutting data
parameters to KienzleForceModelEngine computations. For each cutting data
parameter across all cycle type domains, the mapping defines which Kienzle
variable it drives, how force/power constrains its range, and which ISO group
material constants apply.

Domains covered (8 total):
  drilling   — 15 cycle types × ~5 params  = ~75 mappings
  twoD       — 9 cycle types  × ~8 params  = ~72 mappings
  threeD     — 12 cycle types × ~8 params  = ~96 mappings
  fiveAxis   — 25 cycle types × ~5 params  = ~125 mappings
  turning    — 18 cycle types × ~6 params  = ~108 mappings
  probing    — 8 cycle types  × ~2 params  = ~16 mappings
  grinding   — 5 cycle types  × ~4 params  = ~20 mappings
  cuttingData — 6 profiles    × ~8 params  = ~48 mappings

Total: >= 
```


## Exports
`KienzleInput` (type), `ConstraintType` (type), `ConfidenceLevel` (type), `PhysicsMapping` (interface), `KC1_1_SNAPSHOT` (const), `KIENZLE_MAPPINGS` (const), `KienzleMappingSummary` (interface), `KIENZLE_MAPPING_SUMMARY` (const), `HyperMillKienzleMappingEngine` (class), `kienzleMappingEngine` (const)

## Key methods
`getMappingsForDomain`, `getMappingsForCycleType`, `getMappingById`, `getMappingsForIsoGroup`, `getMappingsForKienzleInput`, `getMappingsForConstraintType`, `getSummary`, `getKc1_1Values`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/hypermill/HyperMillKienzleMappingEngine.ts`
- **Galaxy:** **hypermill**
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HyperMillKienzleMappingEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/hypermill/HyperMillKienzleMappingEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
