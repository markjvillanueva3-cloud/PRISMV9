---
name: reference_code_engine_bueonsetthresholdengine
description: "LATHE-PROD-READY-MS0/U-LPR-BUE"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/BUEOnsetThresholdEngine.ts
  sourceHash: be122cd0
  loc: 292
aliases: BUEOnsetThresholdEngine
---

# BUEOnsetThresholdEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/BUEOnsetThresholdEngine.ts` (292 LOC)

## What it is
LATHE-PROD-READY-MS0/U-LPR-BUE

```
BUEOnsetThresholdEngine — LATHE-PROD-READY-MS0/U-LPR-BUE
========================================================

Predicts built-up edge (BUE) onset risk for a turning operation given
cutting speed, workpiece material class, tool material, and rake
angle.  BUE is the welded layer of work-material that adheres to the
tool tip at low cutting speeds — it degrades surface finish, causes
dimensional drift (effective cutting-edge radius walks), and triggers
intermittent chip-load spikes that overstress the insert.

The Physics R2 spec for LATHE-PROD-READY-MS0 cites two long-standing
sources for the critical-speed regime:

  • Trent, E. M. & Wright, P. K. (2000) — "Metal Cutting" 4th ed,
    ch.4: BUE forms when v_c lies in the strain-rate window where
    the chip's deformation temperature is above the workpiece's
    bonding temperature but below the secondary-shear thermal
    softening reg
```


## Exports
`ISO_GROUP` (const), `IsoGroup` (type), `TOOL_MATERIAL` (const), `ToolMaterial` (type), `BUE_RISK_LEVEL` (const), `BUERiskLevel` (type), `BUEOnsetInputSchema` (const), `BUEOnsetInput` (type), `BUEOnsetResultSchema` (const), `BUEOnsetResult` (type), `BUEOnsetThresholdEngine` (class), `bueOnsetThresholdEngine` (const)

## Key methods
`evaluate`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/BUEOnsetThresholdEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find BUEOnsetThresholdEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/BUEOnsetThresholdEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
