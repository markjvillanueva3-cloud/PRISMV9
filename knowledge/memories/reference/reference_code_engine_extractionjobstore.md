---
name: reference_code_engine_extractionjobstore
description: "extractionJobStore.ts -- durable per-job-file store for async blueprint-OCR extraction jobs"
metadata:
  type: reference
  kind: code-engine
  galaxy: blueprint-vision
  sourcePath: mcp-server/src/engines/blueprint-vision/extractionJobStore.ts
  sourceHash: fea818e4
  loc: 164
aliases: ExtractionJobStore
---

# ExtractionJobStore -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/blueprint-vision/extractionJobStore.ts` (164 LOC)

## What it is
```
extractionJobStore.ts -- durable per-job-file store for async blueprint-OCR extraction jobs
(U-XRAY-EXTRACTION-JOB-STORE). The foundation of the async VLM-OCR path of POST /api/v1/drawing/extract:
a PDF/raster upload enqueues a job here, a background runner advances it, and the poll endpoint reads it.

Design:
 - PER-JOB FILES (`<dir>/<jobId>.json`), NOT one shared JSON blob -- so concurrent jobs on DIFFERENT
   ids never race on a single file (each job's writes are independent). NB the same-id running-claim
   (get-then-write in `transition`) assumes a SINGLE in-process dispatcher (the Express server) -- it is
   NOT a cross-process compare-and-swap; if multi-process dispatch is ever added, gate the claim with an
   O_EXCL/`wx` lock-file. The runner is the single serialization point under the current deployment.
 - FORWARD-ONLY status: queued -> running -> done|failed (terminal). `canTr
```


## Exports
`EXTRACTION_JOB_SCHEMA_VERSION` (const), `JobStatus` (type), `ExtractionJobRecord` (interface), `canTransition` (function), `ExtractionJobStore` (class)

## Key methods
`create`, `transition`, `prune`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/blueprint-vision/extractionJobStore.ts`
- **Galaxy:** **blueprint-vision**
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find ExtractionJobStore` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/blueprint-vision/extractionJobStore.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
