---
name: reference_code_engine_solidworksautomationbridge
description: "PRISM-side TypeScript host for SolidWorks COM automation."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SolidWorksAutomationBridge.ts
  sourceHash: 6a0eaf15
  loc: 433
aliases: SolidWorksAutomationBridge
---

# SolidWorksAutomationBridge -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SolidWorksAutomationBridge.ts` (433 LOC)

## What it is
PRISM-side TypeScript host for SolidWorks COM automation.

```
SolidWorksAutomationBridge — PRISM-side TypeScript host for SolidWorks COM automation.

CAD-AUTOMATION-MS0 / U-CAUT02

Architecture:
  TypeScript (this file) ──execFile──> SolidWorksBridge.exe
                         <──── newline-delimited JSON stdout ──────

The C# bridge hosts SldWorks.Application via COM interop. This engine spawns
it on first use, sends JSON commands over stdin, reads JSON responses from
stdout, and hard-kills the process on timeout or explicit close().

Mock mode: set PRISM_CAD_MOCK=1 to bypass process spawn entirely and return
deterministic fixture data. Used by tests and CI where SolidWorks is absent.

@module engines/SolidWorksAutomationBridge
```


## Exports
`SWCommand` (type), `SWRequest` (interface), `SWResponse` (interface), `FeatureNode` (interface), `BoundingBox` (interface), `OpenResult` (interface), `SolidWorksAutomationBridge` (class), `solidWorksAutomationBridge` (const)

## Key methods
`open`, `getFeatureTree`, `exportSTEP`, `exportPDF`, `getBoundingBox`, `close`, `terminate`, `reject`, `clearTimeout`, `resolve`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/SolidWorksAutomationBridge.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SolidWorksAutomationBridge` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SolidWorksAutomationBridge.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
