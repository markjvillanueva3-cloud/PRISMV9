---
name: reference_code_script_sixdomainautofirecoverage
description: "scripts/six-domain-autofire-coverage.mjs"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/six-domain-autofire-coverage.mjs
  sourceHash: ac2dadf8
  loc: 261
aliases: six-domain-autofire-coverage.mjs
---

# six-domain-autofire-coverage.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/six-domain-autofire-coverage.mjs` (261 LOC)

## What it is
```
scripts/six-domain-autofire-coverage.mjs

R15 VALIDATE leg for the SIX-DOMAIN auto-firing knowledge layer.

The goal's "we don't need the data just sitting there -- it has to be usable
when needed and logically pulled automatically" is delivered by six
`scripts/lib/<d>-approach-knowledge.mjs` libs, each consumed by a per-domain
UserPromptSubmit hook aggregated in `.claude/hooks/bundles/ups-domain-bundle.mjs`.
This validator PROVES that layer works -- it is the cross-domain VALIDATE-on-
live-data-with-numbers step that the per-domain commits never did as a unit.

For each of the 6 goal domains (cad/cam/lathe/mill/post/wedm) it reports:
  - operations taxonomy size
  - verified gate count (confidence === "verified") vs total gates
  - unverified-gap count = the specialist VERIFY BACKLOG (null = lib does not
    track gaps as a first-class export -- reported honestly, never as 0)
  - live-f
```


## Exports
`DOMAINS` (const), `BUNDLE` (const), `buildCoverage` (function), `renderReport` (function)

## Key methods
`writeFileSync`, `main`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/six-domain-autofire-coverage.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find six-domain-autofire-coverage.mjs` or `prism_session:master_index_query`; open the source at `scripts/six-domain-autofire-coverage.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
