---
name: reference_code_frontend_unwrap
description: "unwrapPrism -- normalize the two PRISM HTTP response envelopes into their payload."
metadata:
  type: reference
  kind: code-frontend
  galaxy: unassigned
  sourcePath: mcp-server/web/src/api/unwrap.ts
  sourceHash: 4f6a23fa
  loc: 26
aliases: unwrap.ts
---

# unwrap.ts -- Frontend SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-04T14:51:51.225Z  |  source: `mcp-server/web/src/api/unwrap.ts` (26 LOC)

## What it is
```
unwrapPrism -- normalize the two PRISM HTTP response envelopes into their payload.

The backend emits TWO shapes depending on the route family:
  - erp / business routes (bizRoute / bizGet in src/routes/erp.ts, and the explicit
    `res.json({ ok, data })` routes): { ok: true, data: <payload> }
  - calc / physics routes: { result, safety, meta }  (the PrismResponse type)

Every client fn in api/client.ts is typed `Promise<PrismResponse>` but returns the RAW
route JSON, so a page reading `.result` from an erp `{ok,data}` route silently gets
`undefined` -- the data never loads, and the page often renders an empty / "unavailable"
state that wrongly looks like a backend fault. This helper reads whichever field the
route actually sent.

`.data ?? .result` is safe for BOTH envelopes: an erp response has `.data` (and no
`.result`), a calc response has `.result` (and no `.data`), so the nullish 
```


## Exports
`unwrapPrism` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/web/src/api/unwrap.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Frontend

## How to query it
Recall by meaning via `/system-viz find unwrap.ts` or `prism_session:master_index_query`; open the source at `mcp-server/web/src/api/unwrap.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
