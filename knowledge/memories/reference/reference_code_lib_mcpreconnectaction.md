---
name: reference_code_lib_mcpreconnectaction
description: "scripts/lib/mcp-reconnect-action.mjs — MCP-AUTORECONNECT-MS0 / U-MCP-RECONNECT-ACTION"
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/mcp-reconnect-action.mjs
  sourceHash: f654f97a
  loc: 339
aliases: mcp-reconnect-action.mjs
---

# mcp-reconnect-action.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/mcp-reconnect-action.mjs` (339 LOC)

## What it is
```
scripts/lib/mcp-reconnect-action.mjs — MCP-AUTORECONNECT-MS0 / U-MCP-RECONNECT-ACTION
(alpha, 2026-05-31). Operator rule: "if any chat slot is disconnected they automatically
connect and check each turn to ensure you guys are always connected."

THE GAP THIS CLOSES (R8 — read before write):
  • mcp-connectivity-check.mjs  (UserPromptSubmit, every turn, throttled) — DETECTS a down
    daemon + injects the loud banner, but only ADVISES; it never reconnects.
  • mcp-daemon-autostart.mjs    (SessionStart ONLY) — health-checks + spawns the daemon, but
    does NOT run per-turn, so a daemon that dies MID-session is never auto-restarted.
  → Result: the daemon drops mid-session and every chat shows the banner, turn after turn,
    until an operator manually runs the restart. This module is the missing ACTION half:
    a fleet single-flight, throttled, detached reconnect the per-turn hook calls 
```


## Exports
`DEFAULT_DAEMON_HELPER` (const), `DEFAULT_LOCK_PATH` (const), `DEFAULT_LOCK_TTL_MS` (const), `DEFAULT_HEALTH_URL` (const), `DEFAULT_PROBE_TIMEOUT_MS` (const), `decideReconnect` (function), `BOOT_GRACE_MS` (const), `resolveBootGraceMs` (function), `bootGuardEnabled` (function), `decideRestart` (function), `acquireReconnectLock` (function), `DEFAULT_PORT_LOCK_PATH` (const), `readPortLock` (function), `writePortLock` (function), `isOwnerAlive` (function), `spawnDaemon` (function), `maybeReconnect` (function), `probeDaemon` (function), `renderReconnectLine` (function)

## Key methods
`writeExclImpl`, `unlinkImpl`, `mkdirImpl`, `writeImpl`, `finish`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/mcp-reconnect-action.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find mcp-reconnect-action.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/mcp-reconnect-action.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
