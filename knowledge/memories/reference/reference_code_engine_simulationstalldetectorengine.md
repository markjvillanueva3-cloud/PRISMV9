---
name: reference_code_engine_simulationstalldetectorengine
description: "LATHE-PROD-READY-MS0/U-LPR-STALL-SIM"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SimulationStallDetectorEngine.ts
  sourceHash: 46b57023
  loc: 236
aliases: SimulationStallDetectorEngine
---

# SimulationStallDetectorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SimulationStallDetectorEngine.ts` (236 LOC)

## What it is
LATHE-PROD-READY-MS0/U-LPR-STALL-SIM

```
SimulationStallDetectorEngine — LATHE-PROD-READY-MS0/U-LPR-STALL-SIM

Watchdog for the lathe-program simulation pipeline. Mirrors the shape
of PrintMatchStallDetectorEngine but owns the simulation-specific
stage taxonomy + budgets. A simulation that silently hangs (a stuck
BVH traversal, a collision set that explodes, a physics solve that
enters a tight oscillation) wastes shop-floor operator time — the
operator is watching a spinner that'll never turn.

This engine converts silent simulation hangs into typed stall events
with stage-specific remediation. Keeping it as its own engine (rather
than a parameterized base class shared with STALL-PRINTS) keeps the
stage enums type-safe and the remediation advice on-target.

Stages + budgets
----------------
  gcode_parse      5 s    (parse + tokenize the program)
  kinematics       10 s   (map axis moves → WCS toolpath)
  collision_check  30 s 
```


## Exports
`SimStage` (type), `StallPriority` (type), `SimStallEvent` (interface), `SimTrackerStats` (interface), `SimulationStallDetectorEngine` (class), `simulationStallDetectorEngine` (const)

## Key methods
`startTracking`, `markProgress`, `advanceStage`, `completeJob`, `scan`, `scanOne`, `stats`, `statsAt`, `trackedIds`, `clear`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/SimulationStallDetectorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SimulationStallDetectorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SimulationStallDetectorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
