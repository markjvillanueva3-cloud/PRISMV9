---
name: reference_code_engine_zuluadaptivebackpressureengine
description: "ZuluAdaptiveBackPressureEngine -- C5 (ZULU fleet, HZP-NEW-02)."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/ZuluAdaptiveBackPressureEngine.ts
  sourceHash: 5cb6630b
  loc: 437
aliases: zuluAdaptiveBackPressureEngine
---

# zuluAdaptiveBackPressureEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/ZuluAdaptiveBackPressureEngine.ts` (437 LOC)

## What it is
```
ZuluAdaptiveBackPressureEngine -- C5 (ZULU fleet, HZP-NEW-02).

Trend-aware fan-out throttle. The gap this fills: HermesParallelBudgetEnvelopeEngine
(HZP03) is a per-CALL budget check (within/over/refused) with NO model of a slot's
recent trajectory -- it cannot say "slot alpha has had queue_depth > 8 for the last 5
checks and a 40% error rate: do not fan-out more tasks to it." This engine reads a
SLIDING WINDOW of per-slot queue-depth + error-rate samples (sourced from C3
ZuluFleetHealthSynthesisEngine's FleetHealthVector over time) and emits a
BackPressureSignal so the orchestrator backs off a saturated slot before a
cascading-failure overload (saturated slot -> more failures -> more self-correction
retries -> more load).

SAFETY -- ADVISORY by default:
  - The engine NEVER vetoes. It emits a signal (pressure_level + recommended_delay_ms
    + cause); enforcement is the consumer's choi
```


## Exports
`PressureLevel` (type), `HealthSample` (interface), `BackPressureSignal` (interface), `AssessOpts` (interface), `zuluAdaptiveBackPressureEngine` (const), `ZuluAdaptiveBackPressureEngine` (re-export), `DEFAULTS` (re-export), `SCHEMA_VERSION` (re-export)

## Key methods
`getInstance`, `getStorePath`, `assessBackPressure`, `recordSample`, `recentSamples`, `assess`, `status`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/ZuluAdaptiveBackPressureEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find zuluAdaptiveBackPressureEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/ZuluAdaptiveBackPressureEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
