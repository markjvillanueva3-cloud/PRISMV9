---
name: reference_code_engine_qdrantcapacityplannerengine
description: "Pre-flight disk/RAM estimator for ingestions"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/QdrantCapacityPlannerEngine.ts
  sourceHash: 67428db7
  loc: 282
aliases: QdrantCapacityPlannerEngine
---

# QdrantCapacityPlannerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/QdrantCapacityPlannerEngine.ts` (282 LOC)

## What it is
Pre-flight disk/RAM estimator for ingestions

```
QdrantCapacityPlannerEngine — Pre-flight disk/RAM estimator for ingestions

Phase 0.25.7 U-FIX2 from UNIVERSAL-SKILLS-SCRIPTS-HOOKS-PLAN. Before the
tool catalog (95,608 points) or the JM DIE program corpus (36,929 records)
goes into Qdrant, we want a pre-flight answer: "is there enough disk?
enough RAM for HNSW?" Running out mid-ingestion corrupts state and wastes
hours. This engine is the calculator that the ingestion script calls
BEFORE it touches the vector store.

Model:
  per-vector bytes   = vectorDim × bytesPerComponent (4 float32, 1 int8)
  payload bytes      = payloadAvgBytes (caller-supplied estimate)
  overhead per point = 128 bytes (id + struct framing, Qdrant docs)
  hnsw RAM overhead  = payload + 2 × M × edgeBytes × points  (M=16 typical)

Capacity result returns a `ok | tight | insufficient` decision, the exact
numbers used, and the caller's safety headroom so an ingestio
```


## Exports
`PrecisionKind` (type), `CapacityDecision` (type), `CollectionPlanInput` (interface), `HostAvailability` (interface), `CapacityThresholds` (interface), `DEFAULT_CAPACITY_THRESHOLDS` (const), `CapacityBreakdown` (interface), `CapacityReport` (interface), `QdrantCapacityPlannerEngine` (class), `qdrantCapacityPlannerEngine` (const)

## Key methods
`setThresholds`, `getThresholds`, `plan`, `maxIngestFraction`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/QdrantCapacityPlannerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find QdrantCapacityPlannerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/QdrantCapacityPlannerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
