---
name: reference_code_algorithm_finitedifferencemethod
description: "Finite Difference Method — 1D spatial discretization + method-of-lines"
metadata:
  type: reference
  kind: code-algorithm
  galaxy: unassigned
  sourcePath: mcp-server/src/algorithms/FiniteDifferenceMethod.ts
  sourceHash: cb6ed0db
  loc: 341
aliases: FiniteDifferenceMethod
---

# FiniteDifferenceMethod -- Algorithm SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/algorithms/FiniteDifferenceMethod.ts` (341 LOC)

## What it is
```
Finite Difference Method — 1D spatial discretization + method-of-lines

Discretizes spatial derivatives of a scalar field sampled on a uniform 1D
grid, and assembles a semi-discrete method-of-lines right-hand-side that
plugs straight into [[ODEIntegrator]]. The diffusion and advection operators
it builds are exactly the kind of additively-decomposed operators that
[[OperatorSplittingMethod]] splits — so FDM is the keystone that turns the
P1/P6/P7 numerical suite into a PDE solver:

    FiniteDifferenceMethod  →  method-of-lines RHS  →  ODEIntegrator (RK4)
                            ↘  advection / diffusion operators  →  OperatorSplittingMethod

Schemes:
  - first derivative: forward / backward (O(dx)), central (O(dx²))
  - second derivative: central 3-point stencil [1,−2,1]/dx² (O(dx²)) — the
    discrete Laplacian / diffusion operator
  - boundary conditions: dirichlet (fixed end value
```


## Exports
`BoundaryCondition` (type), `FirstDerivScheme` (type), `FiniteDifferenceInput` (interface), `FiniteDifferenceOutput` (interface), `FiniteDifferenceMethod` (const), `makeMethodOfLinesRHS` (function)

## Key methods
`validate`, `calculate`, `getMetadata`

## Depends on
[[reference_code_algorithm_odeintegrator]]

## Where it lives
- **Source:** `mcp-server/src/algorithms/FiniteDifferenceMethod.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Algorithm

## How to query it
Recall by meaning via `/system-viz find FiniteDifferenceMethod` or `prism_session:master_index_query`; open the source at `mcp-server/src/algorithms/FiniteDifferenceMethod.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
