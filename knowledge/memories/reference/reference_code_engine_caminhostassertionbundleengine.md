---
name: reference_code_engine_caminhostassertionbundleengine
description: "assertion families that every in-host runner must enforce on a"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMInHostAssertionBundleEngine.ts
  sourceHash: 98759a2d
  loc: 326
aliases: CAMInHostAssertionBundleEngine
---

# CAMInHostAssertionBundleEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMInHostAssertionBundleEngine.ts` (326 LOC)

## What it is
assertion families that every in-host runner must enforce on a

```
CAMInHostAssertionBundleEngine — U-CAMTEST14
=============================================

PHASE-8: Central, host-agnostic assertion bundle covering the 7
assertion families that every in-host runner must enforce on a
scenario result. The 4 in-host runner engines (HyperMill, Fusion360,
InventorHSM, Mastercam) currently inline the same logic in their
summarize() methods — this engine is the canonical reference
implementation that future runners (and the deduplication follow-up)
call into instead of copying the math again.

The 7 assertion families:
  1. frame_arrival           observed count == expected, no gaps/dupes
  2. latency_p99             p99 latency ≤ descriptor budget
  3. band_transitions        observed transitions == descriptor expected
  4. hard_stop_trigger       deliberate ⇒ fires within budget; otherwise must not fire
  5. session_stats_reconcile delivered + queued + dro
```


## Exports
`AssertionNameSchema` (const), `AssertionName` (type), `ObservedFrameSchema` (const), `ObservedFrame` (type), `SessionStatsSchema` (const), `SessionStats` (type), `ScenarioExpectationsSchema` (const), `ScenarioExpectations` (type), `AssertionResultSchema` (const), `AssertionResult` (type), `BundleResultSchema` (const), `BundleResult` (type), `HARD_STOP_BUDGET_FRAMES_DEFAULT` (const), `p99` (function), `countBandTransitions` (function), `firstHardStopSeq` (function), `seqGaps` (function), `EvaluateInput` (interface), `CAMInHostAssertionBundleEngine` (class), `camInHostAssertionBundleEngine` (const)

## Key methods
`evaluate`, `failed`, `byName`, `auditBundle`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CAMInHostAssertionBundleEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMInHostAssertionBundleEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMInHostAssertionBundleEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
