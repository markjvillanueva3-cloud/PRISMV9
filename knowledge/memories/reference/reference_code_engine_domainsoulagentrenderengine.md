---
name: reference_code_engine_domainsoulagentrenderengine
description: "DomainSoulAgentRenderEngine -- DOMAIN-SOUL-AGENTS / U1."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/DomainSoulAgentRenderEngine.ts
  sourceHash: 4bf36007
  loc: 194
aliases: DomainSoulAgentRenderEngine
---

# DomainSoulAgentRenderEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/DomainSoulAgentRenderEngine.ts` (194 LOC)

## What it is
DomainSoulAgentRenderEngine -- DOMAIN-SOUL-AGENTS / U1.

```
DomainSoulAgentRenderEngine -- DOMAIN-SOUL-AGENTS / U1.

Pure-core: composes a slot SOUL (persona + domain_filter + refuse_list) with its
galaxy's knowledge pointers into a spawnable Claude subagent definition
(`.claude/agents/<slot>-<domain>.md`). The operator directive (2026-06-30):
"make each chat slot domain an agent soul" -- turn the 26 domain souls into
dispatchable `subagent_type`s so any (now multi-domain) chat can spawn a full
domain expert for ANY task touching that domain's data (build / review / audit).

Mirrors the proven `scripts/lib/galaxy-soul-render.mjs` render pattern, but emits
the AGENT frontmatter shape (`name`/`description`/`tools`/`maxTurns`/`model`) the
Agent tool consumes -- plus carried-through `slot`/`domain`/`domain_filter`/
`hermes_capable` keys the auto-trigger + hybrid-lane selector read.

Pure: no I/O. The generator (`scripts/generate-domain-soul-agents.mj
```


## Exports
`DEFAULT_AGENT_TOOLS` (const), `DEFAULT_AGENT_MAX_TURNS` (const), `MAX_RENDERED_BYTES` (const), `GalaxyMetaSchema` (const), `GalaxyMeta` (type), `RenderResult` (interface), `DomainSoulAgentRenderEngine` (class), `domainSoulAgentRenderEngine` (const)

## Key methods
`renderAgent`

## Depends on
[[reference_code_engine_soulfrontmatterreaderengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/DomainSoulAgentRenderEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find DomainSoulAgentRenderEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/DomainSoulAgentRenderEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
