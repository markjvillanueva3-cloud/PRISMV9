---
name: reference_code_script_validateunwiredsignal
description: "validate-unwired-signal.mjs"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/validate-unwired-signal.mjs
  sourceHash: dd41fe8a
  loc: 592
aliases: validate-unwired-signal.mjs
---

# validate-unwired-signal.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/validate-unwired-signal.mjs` (592 LOC)

## What it is
```
validate-unwired-signal.mjs
===========================
Independent verifier for the NEEDS_WIRING signal produced by
`scripts/audit-unwired-engines.mjs` + surfaced through
`state/shared/BUILD_STATE.json` + `state/shared/UNWIRED-ENGINE-AUDIT-*.json`.

Problem (CLAUDE.md regression 2026-05-14):
  The audit's NEEDS_WIRING list has a ~50% false-positive rate on sample.
  Engines flagged "unwired" are actually consumed by dispatchers/hooks/routes/
  orchestrators via patterns the audit doesn't catch (singleton wrappers with
  `instance` suffix, default imports, dynamic `import()`, type-only imports,
  action-handler maps that don't grep as `case "x":`).

  Every wiring milestone that consumes this signal mis-prioritises 50% of its
  slots on already-wired engines — wasted fleet cycles.

This script:
  1. Loads the most recent UNWIRED-ENGINE-AUDIT-*.json (or BUILD_STATE.json
     sample_engine
```


## Exports
`parseArgs` (re-export), `buildSearchPatterns` (re-export), `scanEngine` (re-export), `sampleDeterministic` (re-export), `makeRng` (re-export), `classifyConsumerFile` (re-export), `extractEngineList` (re-export), `isExempt` (re-export), `writeReport` (re-export), `loadUnwiredList` (re-export), `STRONG_CONSUMER_KINDS` (re-export), `DEFAULT_SAMPLE_SIZE` (re-export), `DEFAULT_SEED` (re-export), `DEFAULT_MAX_FP_RATE_PCT` (re-export), `MAX_CONTENT_BYTES_FOR_REGEX` (re-export)

## Key methods
`walk`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/validate-unwired-signal.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find validate-unwired-signal.mjs` or `prism_session:master_index_query`; open the source at `scripts/validate-unwired-signal.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
