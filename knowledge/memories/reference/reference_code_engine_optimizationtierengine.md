---
name: reference_code_engine_optimizationtierengine
description: "they reflect real-world constraints (fixturing, access, thermal"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/OptimizationTierEngine.ts
  sourceHash: a2e8d1bb
  loc: 2026
aliases: OptimizationTierEngine
---

# OptimizationTierEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/OptimizationTierEngine.ts` (2026 LOC)

## What it is
they reflect real-world constraints (fixturing, access, thermal

```
PRISM Manufacturing Intelligence — Optimization Tier & Consent System Engine
POST-ULT-MS6: U01–U07 Consolidated

THE critical gate for all downstream optimization. Ensures PRISM never
modifies user intent without explicit, informed consent.

Architecture:
  U01 OptimizationTierSchema      — 4-tier system with per-tier feature flags
  U02 UserIntentDetector          — Parse G-code to identify user decisions
  U03 ChangeClassifier            — Classify every modification into its tier
  U04 ChangeExplanationEngine     — Human-readable explanations with physics basis
  U05 DiffPreviewGenerator        — Visual diff summary by category
  U06 PerSuggestionApproval       — Accept/reject individual changes or categories
  U07 FusionPluginIntegration     — Plugin interface with intent metadata

Dispatcher actions:
  get_tier_config, set_tier, detect_intent, classify_changes,
  generate_diff, appl
```


## Exports
`OptimizationTier` (type), `TierConfig` (interface), `UserIntent` (interface), `ToolSelection` (interface), `StrategyType` (type), `StrategyIndicator` (interface), `RetractHeight` (interface), `LinkingMove` (interface), `ChangeCategory` (type), `ProposedChange` (interface), `ChangeExplanation` (interface), `ChangeImpact` (interface), `DiffPreview` (interface), `ApprovalDecision` (interface), `FusionPluginManifest` (interface), `FusionApiEndpoint` (interface), `PhysicsContext` (interface), `OptimizationTierEngine` (class), `optimizationTierEngine` (const)

## Key methods
`getTierConfig`, `setTier`, `getCurrentTier`, `detectUserIntent`, `classifyChange`, `classifyChanges`, `filterByTier`, `isChangeAllowed`, `generateExplanation`, `String`, `generateDiffPreview`, `getNextTierPreview`, `applyApproval`, `applyRememberedPreferences`, `getSavedPreferences`, `clearPreferences`, `getApprovedChanges`, `getApprovalSummary`, `getPluginManifest`, `exportIntentMetadata`, `validateAgainstIntent`, `handleAction`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/OptimizationTierEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find OptimizationTierEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/OptimizationTierEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
