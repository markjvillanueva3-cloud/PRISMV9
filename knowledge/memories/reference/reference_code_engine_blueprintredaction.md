---
name: reference_code_engine_blueprintredaction
description: "scripts: mcp-server/src/engines/blueprint-vision/blueprintRedaction.ts"
metadata:
  type: reference
  kind: code-engine
  galaxy: blueprint-vision
  sourcePath: mcp-server/src/engines/blueprint-vision/blueprintRedaction.ts
  sourceHash: 6e11128c
  loc: 399
aliases: blueprintRedaction.ts
---

# blueprintRedaction.ts -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/blueprint-vision/blueprintRedaction.ts` (399 LOC)

## What it is
```
scripts: mcp-server/src/engines/blueprint-vision/blueprintRedaction.ts

U-APP-REDACT-LIB -- shared blueprint customer-identity redactor.

Build-once (R15/R16): the customer-name anonymization logic was previously locked inside
BlueprintLoRABridgeEngine (LoRA-export only). This extracts the PROVEN core (the spec-mandated
deny-list + part-number patterns + the ordered scrub) into ONE shared module so BOTH the LoRA
export AND the new app-facing drawing redaction (Phase 3 of the blueprint-vision app-integration
plan) use the same redactor. The LoRA engine re-exports ANONYMIZATION_PATTERNS / applyAnonymization
Patterns from here, so its behavior is byte-identical (the CORE set is unchanged) -- only the home moved.

Two coverage levels, deliberately (R7 -- surface the conflict, do not average):
  - CORE_CUSTOMER_PATTERNS (the curated 9 spec-mandated names) -- LOW collision, SAFE for free-text

```


## Exports
`MASK` (const), `CORE_CUSTOMER_NAMES` (const), `CORE_CUSTOMER_PATTERNS` (const), `PART_NUMBER_PATTERNS` (const), `ANONYMIZATION_PATTERNS` (const), `CUSTOMER_IDENTITY_KEYS` (const), `NON_PII_VALUE_KEYS` (const), `looksLikeMaterialGrade` (function), `COMMON_WORD_CUSTOMERS` (const), `isDistinctiveCustomerName` (function), `freeTextCustomerNames` (function), `buildCustomerPatterns` (function), `getAllCustomerNames` (function), `applyAnonymizationPatterns` (function), `RedactionAudit` (interface), `RedactTextResult` (interface), `DISTINCTIVE_CUSTOMER_PATTERNS` (const), `ALL_CUSTOMER_PATTERNS` (const), `ALL_CUSTOMER_NAMES_UPPER` (const), `redactText` (function)

## Key methods
`pushAudit`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/blueprint-vision/blueprintRedaction.ts`
- **Galaxy:** **blueprint-vision**
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find blueprintRedaction.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/blueprint-vision/blueprintRedaction.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
