---
name: reference_code_data_sfccombinatorialcompare
description: "SFC combinatorial COMPARE -- diffs each driven cell's PRISM result against its"
metadata:
  type: reference
  kind: code-data
  galaxy: unassigned
  sourcePath: mcp-server/src/data/sfc-combinatorial-compare.ts
  sourceHash: 9bec173e
  loc: 258
aliases: SpeedFeedCombinatorialComparatorEngine
---

# SpeedFeedCombinatorialComparatorEngine -- Data Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/data/sfc-combinatorial-compare.ts` (258 LOC)

## What it is
```
SFC combinatorial COMPARE -- diffs each driven cell's PRISM result against its
CITED vendor row (U-CSFH-03) and classifies the per-cell agreement. Consumes the
DrivenCell[] the DRIVER (U-CSFH-06) emits; produces the per-cell verdicts +
delta distribution that BASELINE-PARAMS (U-CSFH-08) derives the per-regime moat
from. Reuse, not rewrite: the comparison tolerances + signed-percent delta come
from `SpeedFeedTriVendorBatchComparatorEngine` (whose threshold consts are
module-private there, so they are restated here with the SAME values).

OSCAR-SFC-9AXIS-MS0 / U-CSFH-07-COMPARE (slot:oscar, 2026-06-11).

The driver gives ONE cited vendor row per cell (the romeo catalog), so this is a
2-way PRISM-vs-vendor compare, NOT the full tri-vendor matrix. The honesty core:
most sampled cells carry NO resolved citation (the sampler is tool-agnostic) --
those ABSTAIN as `uncited` and NEVER count as ag
```


## Exports
`CompareVerdict` (type), `CellComparison` (interface), `DeltaStats` (interface), `CompareReport` (interface), `SpeedFeedCombinatorialComparatorEngine` (class)

## Key methods
`compare`, `compareRecords`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/data/sfc-combinatorial-compare.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Data Module

## How to query it
Recall by meaning via `/system-viz find SpeedFeedCombinatorialComparatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/data/sfc-combinatorial-compare.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
