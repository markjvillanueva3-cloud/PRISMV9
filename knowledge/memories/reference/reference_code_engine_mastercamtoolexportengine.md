---
name: reference_code_engine_mastercamtoolexportengine
description: "MastercamToolExportEngine — Export PRISM Tool Catalog to Mastercam Format (E1123)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/MastercamToolExportEngine.ts
  sourceHash: 2d24806c
  loc: 848
aliases: MastercamToolExportEngineClass
---

# MastercamToolExportEngineClass -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/MastercamToolExportEngine.ts` (848 LOC)

## What it is
```
MastercamToolExportEngine — Export PRISM Tool Catalog to Mastercam Format (E1123)

Exports PRISM's 95K+ tool catalog to Mastercam tool library format
(.mcam-tools / .mcam-operations). Supports full catalog export (partitioned
by manufacturer), job-specific export, and filtered export. Each tool
entry contains holder + cutter geometry, tool classification, material,
and per-ISO-group cutting data derived from PRISM physics engines.

Methods:
  exportLibrary(filter?, format?)          — filtered or full catalog export
  exportForJob(job_tools[])                — job-specific tool subset
  exportWithCuttingData(tools[], mats[])   — includes per-material speed/feed tables
  getFormat()                              — describe .mcam-tools format structure

Cutting data physics:
  Vc  — UltimateSpeedFeedEngine baseline per ISO group (m/min)
  fz  — Kienzle-derived optimal chipload (mm/tooth)
  
```


## Exports
`McamToolType` (type), `McamToolMaterial` (type), `McamHolderType` (type), `McamExportFormat` (type), `ISOGroup` (type), `McamExportFilter` (interface), `McamCuttingData` (interface), `McamHolder` (interface), `McamTool` (interface), `McamLibrary` (interface), `McamExportResult` (interface), `MastercamToolExportEngineClass` (class), `mastercamToolExportEngine` (const)

## Key methods
`exportLibrary`, `exportFromTools`, `exportForJob`, `exportWithCuttingData`, `getFormat`

## Depends on
[[reference_code_engine_toolcatalogengine]] | [[reference_code_engine_holderselectionengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/MastercamToolExportEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find MastercamToolExportEngineClass` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/MastercamToolExportEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
