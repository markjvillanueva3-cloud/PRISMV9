---
name: reference_code_engine_latheautoquotefromprintengine
description: "LatheAutoQuoteFromPrintEngine — U-LTH48 (LATHE-MASTER P5 ERP)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LatheAutoQuoteFromPrintEngine.ts
  sourceHash: e9d2fdae
  loc: 480
aliases: latheAutoQuoteFromPrintEngine
---

# latheAutoQuoteFromPrintEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LatheAutoQuoteFromPrintEngine.ts` (480 LOC)

> [physics] Imports canonical physics constants (`src/physics/constants.ts`) -- safety-relevant node.

## What it is
```
LatheAutoQuoteFromPrintEngine — U-LTH48 (LATHE-MASTER P5 ERP)

Bridge from P4 print-to-program pipeline output to a shop-floor-grade
instant quote. Consumes the SequencePlan + ToolpathProgram (+ optional
SignoffPackage) and produces a 6-bucket cost breakdown:

  1. Material cost   — stock volume × density × $/kg × (1 + scrap)
  2. Tool wear cost  — Σ (cycle_min_op / T_taylor) × $_per_insert
  3. Cycle cost      — (cycle_min + rapid_min) × $/machine_hr
  4. Setup cost      — setup_count × setup_min_per_setup × $/setup_hr
  5. Overhead        — direct_cost × overhead_rate
  6. Margin          — (direct + overhead) × margin_rate × rush_multiplier

Physics:
  - Stock volume:   V = π/4 · (OD² − ID²) · L  [mm³] → cm³
  - Taylor life:    T = (C / Vc)^(1/n)  [min]
  - Feed rate:      Fmm_min = fz · N  [mm/min]
  - Rapid time:     L_rapid / RAPID_FEED  [min]

Citations:
  - Machinery's Handbook 3
```


## Exports
`QuoteInputSchema` (const), `QuoteInput` (type), `ToolWearLineSchema` (const), `ToolWearLine` (type), `CostBreakdownSchema` (const), `CostBreakdown` (type), `QuoteEvidenceSchema` (const), `QuoteEvidence` (type), `VarianceBandSchema` (const), `VarianceBand` (type), `QuotePackageSchema` (const), `QuotePackage` (type), `AutoQuoteInputSchema` (const), `AutoQuoteInput` (type), `latheAutoQuoteFromPrintEngine` (const), `LatheAutoQuoteFromPrintEngine` (re-export)

## Key methods
`generateQuote`, `reconcileAgainstActual`

## Depends on
[[reference_code_engine_latheprintsequenceplannerengine]] | [[reference_code_engine_latheprinttoolpathgeneratorengine]] | [[reference_code_engine_latheprintfeaturestrategyselectorengine]] | [[reference_code_engine_latheprintprogramsignoffengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/LatheAutoQuoteFromPrintEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find latheAutoQuoteFromPrintEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LatheAutoQuoteFromPrintEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
