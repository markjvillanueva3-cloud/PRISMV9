---
name: reference_code_engine_ppredundantmodalvalidatorengine
description: "Detect redundant modal re-issuance"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/PPRedundantModalValidatorEngine.ts
  sourceHash: 46bdcea6
  loc: 363
aliases: PPRedundantModalValidatorEngine
---

# PPRedundantModalValidatorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/PPRedundantModalValidatorEngine.ts` (363 LOC)

## What it is
Detect redundant modal re-issuance

```
PPRedundantModalValidatorEngine — Detect redundant modal re-issuance

Modal commands in G-code stay active until changed. Re-issuing the same
modal value across successive blocks is harmless on Fanuc/Haas but signals
a CAM post that isn't tracking state — and on some Mitsubishi/Siemens
controllers, spurious re-issuance throws alarm PS 010 or resets modal
group tracking in confusing ways. More importantly, for operators reading
a program, redundant re-issuance obscures the actual state changes.

Common offenders:
  - `G90` ... `G90` with no intervening `G91` — distance mode stuck true
  - `G17` ... `G17` — plane select never left XY
  - `G54` ... `G54` — WCS not changed between ops
  - `F100.` ... `F100.` — same feed re-issued line after line
  - `S1000` ... `S1000` — same spindle speed re-asserted
  - `T1 ... T1` (no M6 between) — tool-preselect redundant
  - `M3` ... `M3` — spindle stat
```


## Exports
`RMSeverity` (type), `RMIssueKind` (type), `RMIssue` (interface), `RMOptions` (interface), `RMResult` (interface), `PPRedundantModalValidatorEngine` (class), `ppRedundantModalValidatorEngine` (const)

## Key methods
`validate`, `parseInt`, `quickCheck`, `defaultOptions`, `parseFloat`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/PPRedundantModalValidatorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find PPRedundantModalValidatorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/PPRedundantModalValidatorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
