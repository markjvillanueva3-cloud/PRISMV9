---
name: reference_code_engine_crossprocesspolicygradientengine
description: "XPROC-NEURAL Tier 4 (T4-02)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CrossProcessPolicyGradientEngine.ts
  sourceHash: 4d02ef03
  loc: 461
aliases: CrossProcessPolicyGradientEngine
---

# CrossProcessPolicyGradientEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CrossProcessPolicyGradientEngine.ts` (461 LOC)

## What it is
XPROC-NEURAL Tier 4 (T4-02)

```
CrossProcessPolicyGradientEngine — XPROC-NEURAL Tier 4 (T4-02)

REINFORCE with baseline (Williams 1992; Sutton & Barto 2nd ed Ch.13).
Discrete-action policy gradient over (state, action) pairs where:
  state  = encoded (process, material, feature, machine) tuple
  action = index into a discrete action space (e.g. tool/strategy choice)
  reward = scalar from T4-01 (CrossProcessRewardShaperEngine)

Algorithm:
  π(a|s; θ) = softmax(θ_s,a) over actions for the given state.
  Episode trajectory: (s_t, a_t, r_t) for t = 0..T-1.
  Return G_t = Σ_{k=t..T-1} γ^(k-t) · r_k.
  Baseline V(s) is the running mean return for state s (variance-reduction
    trick — REINFORCE-with-baseline; doesn't bias the gradient,
    dramatically reduces variance — Sutton & Barto §13.4).
  Update: θ_s,a ← θ_s,a + α · (G_t − V(s_t)) · ∇log π(a_t|s_t)
  For softmax: ∇log π(a|s) for action_a w.r.t. θ_s,j is δ(a=j) − π(j
```


## Exports
`PolicyParams` (interface), `SelectActionResult` (interface), `CommitResult` (interface), `PolicyStep` (interface), `StepOk` (interface), `CommitOk` (interface), `SelectOk` (interface), `OpErr` (interface), `StepResult` (type), `CommitResultUnion` (type), `SelectResult` (type), `CrossProcessPolicyGradientEngine` (class), `crossProcessPolicyGradientEngine` (const), `crossProcessPolicyGradient` (function)

## Key methods
`step`, `commit`, `updateBaseline`, `selectAction`, `getPolicy`, `getBaseline`, `configure`, `reset`, `constants`, `stats`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CrossProcessPolicyGradientEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CrossProcessPolicyGradientEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CrossProcessPolicyGradientEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
