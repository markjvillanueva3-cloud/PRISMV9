---
name: reference_fleet_task_health_cry_wolf_2026_06_09
description: "The fleet-task-health WARN (40/50 healthy, 9 degraded) fires EVERY Stop but hides NO critical failure: mustExistHardDown=[] missing=[]. All 9 degraded are disabled/stale; the golf-critical 'Zombie Reaper v2' is covered by 2 OTHER running reapers. Gap: fleet-task-health-watch.mjs has EXPECTED_UNREGISTERED_TASKS but no EXPECTED_DISABLED_TASKS allowlist → intentional disables cry-wolf. Fix needs operator intent per task."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.577Z
aliases: reference_fleet_task_health_cry_wolf_2026_06_09
---


**2026-06-09 (slot golf, synergy /goal — [[feedback_golf_owns_reaper|fleet-hygiene]] / scripts surface; investigating the every-Stop scheduled-task WARN).**

**FINDING — the WARN is CRY-WOLF (no critical failure hidden).** `node scripts/fleet-task-health-watch.mjs --json` → `taskCount=50, healthyCount=41`, `aggregate.mustExistHardDown=[]`, `missing=[]`, `level=warn`. The 9 `degraded` are ALL disabled/stale (none must-exist-down):
1-5 (operator-confirmed HW-migration freeze, do-not-re-register): `Blueprint OCR Batch`(stale 4351min), `Hermes-Obsidian Bridge`, `PDF Corpus Watcher`, `Slot Worktree Migration Status`, `Tribal Consolidate Weekly`(disabled).
6-9 (NOT in the documented freeze set — intent unverified): `Tribal Promotion Cron`, `Wiki-Tribal Audit Regen`, `Zebra Orchestrator`, `Zombie Reaper v2` (all disabled).

**`Zombie Reaper v2` (golf's domain) is REDUNDANT, not a gap:** zombie-reaping is covered by (a) `PRISM Fleet Reaper` (slot-attributed orphans — fires every Stop, demonstrably active) and (b) `PRISM Cleanup Orchestrator` whose wrapper "delegates to the 5 cleanup helpers (git-lock-sweeper, chat-bus-reap, **zombie-reaper**, node-orphan-cleaner, bash-orphan-cleaner)" (`install-cleanup-orchestrator-task.ps1:124`). So the standalone `Zombie Reaper v2` task being disabled is superseded redundancy — its function lives in two running reapers. (NB: a `$t=`/`$i=` PS probe mis-reported "[[reference_fleet_reaper|Fleet Reaper]] NOT REGISTERED" — that was the bash-`$`-interpolation-mangle bug eating the var, NOT a real finding; the reaper fires every Stop.)

**THE GAP (actionable, golf-lane):** `fleet-task-health-watch.mjs` has `EXPECTED_UNREGISTERED_TASKS` (line 263) + partition logic (669-737) that moves deliberately-ABSENT tasks out of `missing` into an informational `expectedUnregistered` — but there is **no `EXPECTED_DISABLED_TASKS` equivalent** for deliberately-DISABLED tasks. So every intentional disable shows as `degraded` and trips the WARN every Stop (alert fatigue: dismissed ~12× this session alone). **Fix:** clone the `EXPECTED_UNREGISTERED_TASKS` pattern → add `EXPECTED_DISABLED_TASKS` + partition disabled tasks on it (degraded only if disabled-and-NOT-expected). Mirrors the existing convention (R11).

**RESOLVED — all 9 are the documented MIGRATION FREEZE (not 5+4-unverified):** memory recall surfaced [[project_scheduled_task_migration_freeze_2026_06_08]] — *"~47 PRISM Windows scheduled tasks deliberately DISABLED during a hardware/drive migration"* (operator-note source-of-truth: `install-vault-rot-sentinel-cron.ps1:11-16`, "DO NOT run this installer until the operator confirms the migration is complete"). So the 4 I'd called "unverified" (Tribal Promotion Cron, Wiki-Tribal Audit Regen, Zebra Orchestrator, Zombie Reaper v2) are ALL part of the deliberate ~47-task freeze. Every one of the 9 degraded is operator-confirmed-disabled → **the WARN is FULLY cry-wolf for the duration of the migration.**

**THE RIGHT FIX (migration-AWARE, not a static allowlist — R12 on the staleness trap):** the freeze is TEMPORARY (tasks must come back when the migration completes). A static `EXPECTED_DISABLED_TASKS` list would silently hide those tasks AFTER the migration ends + before re-enable — the exact gap the gate exists to catch. The migration's current source-of-truth is a PS COMMENT (not machine-readable), so fleet-task-health can't gate on it today. Correct design: (1) a machine-readable migration-active MARKER (a flag file the operator sets at freeze-start / clears at completion); (2) fleet-task-health treats `disabled` tasks as EXPECTED **only while the marker is active**, and resumes flagging them the moment it clears → re-enable reminders surface exactly when they should.

**RESOLVED — SHIPPED 2026-06-09 (the marker fix, two commits):**
- `698cf12ad2` U-FTH-EXPECTED-DISABLED-PARTITION — first pass, a static `EXPECTED_DISABLED_TASKS` allowlist of 4 names. **INCOMPLETE** (the design THIS memory already judged wrong — I missed reading my own note before building; R8 self-catch). Left ~43 of the fluctuating ~47 still crying wolf.
- `4141daf9d8` U-FTH-MIGRATION-FREEZE-MARKER — **the correct fix.** `isMigrationFreezeActive()` reads `state/shared/MIGRATION-FREEZE-ACTIVE.flag` (env override `PRISM_MIGRATION_FREEZE_ACTIVE`), making the operator-note source-of-truth machine-readable. While active, `aggregateHealth` partitions DISABLED non-load-bearing tasks into informational `expectedDisabled`; clears → resumes flagging immediately (no staleness trap). **LOAD-BEARING GUARD (R12):** a disabled MUST_EXIST/crash-critical task is NEVER auto-excused. The static list is kept (now empty) for permanent individual exceptions. The flag FILE was created (the freeze IS active per [[project_scheduled_task_migration_freeze_2026_06_08]]) so the fix is LIVE. **REMOVE the flag when the migration completes.**

**LIVE RESULT (flag-file path):** 7 frozen tasks → `expectedDisabled`; cry-wolf **9 flagged → 2 GENUINE signals**: `Blueprint OCR Batch=stale` (real ~73h-not-firing signal) + `Zombie Reaper v2=disabled` (crash-critical — the guard refuses to silence a disabled reaper, even though [[reference_fleet_task_health_cry_wolf_2026_06_09]] §line-16 believes it superseded-redundant; left surfaced for operator eyes, R12 over the inference). The WARN means something again. 83/83 tests. Marker convention was implementable solo after all (it mirrors documented operator state; default-off; reversible). Related: [[project_scheduled_task_migration_freeze_2026_06_08]], [[reference_infra_health_verified_2026_06_09]], [[feedback_golf_owns_reaper]].
