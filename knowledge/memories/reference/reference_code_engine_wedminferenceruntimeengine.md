---
name: reference_code_engine_wedminferenceruntimeengine
description: "WEDMInferenceRuntimeEngine — WEDM-COMPREHENSIVE-TRAINING-PIPELINE-MS0 U-WCTP-INFERENCE"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WEDMInferenceRuntimeEngine.ts
  sourceHash: 642ed774
  loc: 260
aliases: wedmInferenceRuntimeEngine
---

# wedmInferenceRuntimeEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WEDMInferenceRuntimeEngine.ts` (260 LOC)

## What it is
```
WEDMInferenceRuntimeEngine — WEDM-COMPREHENSIVE-TRAINING-PIPELINE-MS0 U-WCTP-INFERENCE
============================================================================

Closes iter13 strategic-gap item #9 — the runtime scaffolding side. The
actual GPU-side Python inference (Unsloth / Ollama-served adapter) runs
out-of-process, but PRISM needs a fail-loud TypeScript-side runtime that:

  1. Tracks which trained adapter (if any) is registered as 'current'
     — file path on disk + version + capability metadata
  2. Gates the wizard's print-to-program path so it can ASK the runtime
     "is a trained model available?" before delegating
  3. Exposes a generate(prompt) API that EITHER returns a structured
     'not loaded' fail-loud OR delegates to the external runtime via
     a registered handler (Ollama / direct Python / future MCP bridge)

The engine itself is pure — no I/O, no fetch. Caller
```


## Exports
`WedmAdapterRegistration` (interface), `WedmAdapterMetrics` (interface), `WedmInferenceStatus` (interface), `WedmGenerateRequest` (interface), `WedmGenerateResult` (interface), `WedmGenerateHandler` (type), `wedmInferenceRuntimeEngine` (const)

## Key methods
`registerAdapter`, `unregisterAdapter`, `setGenerateHandler`, `status`, `generate`, `getAdapter`, `reset`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WEDMInferenceRuntimeEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find wedmInferenceRuntimeEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WEDMInferenceRuntimeEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
