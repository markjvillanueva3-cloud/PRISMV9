---
name: reference_code_script_validateghostwires
description: "G3 — Ghost-wire validation feedback loop."
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/validate-ghost-wires.mjs
  sourceHash: f787a6c6
  loc: 420
aliases: validate-ghost-wires.mjs
---

# validate-ghost-wires.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/validate-ghost-wires.mjs` (420 LOC)

## What it is
```
G3 — Ghost-wire validation feedback loop.

For every `ghost.unwired-engine` node in system-graph.json with a
`proposed_wiring` annotation, check whether the proposed dispatcher
file now references the engine class (lazy import / action-enum / etc).
Classify each ghost as:
  - confirmed: engine name found in dispatcher (word-boundary)
  - refuted:   not found AND daysOpen > REFUTED_AFTER_DAYS (default 30)
  - pending:   not found AND daysOpen <= REFUTED_AFTER_DAYS

Outputs:
  1. Append-only JSONL outcomes (labeled dataset for NN-GRAPH retrain):
     state/shared/ghost-wire-outcomes.jsonl
  2. Augmentation overlay (`/system-viz` G-key):
     state/shared/system-viz/staging/ghost-wire-validation-augmentation.json

Pure function `validate({graph, dispatcherReader, ageFn, now, refutedAfterDays})`
is injectable for testing. CLI wires fs + git-derived sha + real disk.

Wired into:
  - scripts/m
```


## Exports
`STREAMING_READ_THRESHOLD_BYTES` (const), `REFUTED_AFTER_DAYS` (const), `STATUS_CONFIRMED` (const), `STATUS_REFUTED` (const), `STATUS_PENDING` (const), `dispNodeIdToBasename` (function), `buildDispatcherIndex` (function), `stripComments` (function), `analyzeConfirmationContext` (function), `classifyGhostWire` (function), `validate` (function)

## Key methods
`mkdirSync`, `writeFileSync`, `runCli`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/validate-ghost-wires.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find validate-ghost-wires.mjs` or `prism_session:master_index_query`; open the source at `scripts/validate-ghost-wires.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
