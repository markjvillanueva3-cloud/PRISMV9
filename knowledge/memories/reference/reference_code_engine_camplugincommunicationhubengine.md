---
name: reference_code_engine_camplugincommunicationhubengine
description: "Bidirectional Plugin Transport Hub (U-CAM96)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CAMPluginCommunicationHubEngine.ts
  sourceHash: 90852bab
  loc: 566
aliases: CAMPluginCommunicationHubEngine
---

# CAMPluginCommunicationHubEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CAMPluginCommunicationHubEngine.ts` (566 LOC)

## What it is
Bidirectional Plugin Transport Hub (U-CAM96)

```
CAMPluginCommunicationHubEngine — Bidirectional Plugin Transport Hub (U-CAM96)
==============================================================================

PHASE-7 Intelligent Vericut: Central communication hub that routes overlay
frames from the PRISM physics engines (U-CAM90..95 + U-CAM85) to the four
CAM plugin adapters (hyperMILL, Fusion 360, Inventor HSM, Mastercam X8).

    [Force | Chatter | Deflection | Thermal | ToolLife | SafetyScore]
                              │
                              ▼   PhysicsOverlayFrame envelope
            CAMPluginCommunicationHubEngine.route()
                              │
             ┌────────────────┼──────────────────┬──────────────┐
             ▼                ▼                  ▼              ▼
       WebSocket          WebSocket             gRPC         gRPC
    (hyperMILL UI)    (Fusion 360 UI)     (Inventor HSM)  (Mastercam X8
```


## Exports
`HubTransportSchema` (const), `HubTransport` (type), `HubPluginTargetSchema` (const), `HubPluginTarget` (type), `HubFrameTypeSchema` (const), `HubFrameType` (type), `HubPluginRegistrationSchema` (const), `HubPluginRegistration` (type), `HubFrameEnvelopeSchema` (const), `HubFrameEnvelope` (type), `HubRouteStatusSchema` (const), `HubRouteStatus` (type), `HubRouteResultSchema` (const), `HubRouteResult` (type), `HubSessionStatsSchema` (const), `HubSessionStats` (type), `HEARTBEAT_STALE_MS` (const), `MAX_QUEUE_DEPTH` (const), `CAMPluginCommunicationHubEngine` (class), `camPluginCommunicationHubEngine` (const)

## Key methods
`register`, `unregister`, `heartbeat`, `registered`, `queuedDepth`, `route`, `updateLatencyStats`, `routeAll`, `batchRoute`, `drainQueue`, `getStats`, `resetSession`, `resetRegistry`, `supportedTargets`, `supportedTransports`, `supportedFrameTypes`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/CAMPluginCommunicationHubEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CAMPluginCommunicationHubEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CAMPluginCommunicationHubEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
