---
name: reference_code_module_metricscollector
description: "MetricsCollector — in-process MCP request telemetry (OBSERVABILITY-MS0, slot:bravo 2026-05-30)."
metadata:
  type: reference
  kind: code-module
  galaxy: unassigned
  sourcePath: mcp-server/src/observability/metrics-collector.ts
  sourceHash: 47b56522
  loc: 259
aliases: MetricsCollector
---

# MetricsCollector -- Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-04T14:51:22.427Z  |  source: `mcp-server/src/observability/metrics-collector.ts` (259 LOC)

## What it is
```
MetricsCollector — in-process MCP request telemetry (OBSERVABILITY-MS0, slot:bravo 2026-05-30).

WHY: chats "disconnecting" from the MCP server was invisible until processes were
hand-counted. This makes the contention class measurable — per-tool call counts,
latency percentiles (p50/p95/p99), error counts, live + peak concurrency, and a
rolling RSS ring. Surfaced via the existing GET /metrics (Prometheus) + ?format=json.

DESIGN: bounded memory (per-tool latency reservoir capped; tool cardinality capped;
RSS ring capped) and NEVER throws — instrumentation must never break the hot path.
Zero PRISM imports → unit-testable in isolation. Singleton export `metrics`.
```


## Exports
`ToolSnapshot` (interface), `MetricsSnapshot` (interface), `MetricsCollector` (class), `metricsViewHtml` (function), `metrics` (const)

## Key methods
`incInflight`, `decInflight`, `recordMethod`, `recordTool`, `sampleRss`, `snapshot`, `prometheus`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/observability/metrics-collector.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Module

## How to query it
Recall by meaning via `/system-viz find MetricsCollector` or `prism_session:master_index_query`; open the source at `mcp-server/src/observability/metrics-collector.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
