---
name: reference_code_engine_trilobeelectrodegeometryengine
description: "ELEC-PIPE-MS0"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/TrilobeElectrodeGeometryEngine.ts
  sourceHash: c30a86b1
  loc: 748
aliases: TrilobeElectrodeGeometryEngine
---

# TrilobeElectrodeGeometryEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/TrilobeElectrodeGeometryEngine.ts` (748 LOC)

> [physics] Imports canonical physics constants (`src/physics/constants.ts`) -- safety-relevant node.

## What it is
ELEC-PIPE-MS0

```
TrilobeElectrodeGeometryEngine — ELEC-PIPE-MS0

Generates parametric trilobe (taptite) electrode geometry.
REVERSE ENGINEERED from: `Automated Program_Corrected 5-25.xlsm`

The Excel macro drove a SolidWorks configurator with dimensions in cells B11-B44.
This engine replaces that dependency with native PRISM geometry generation.

Trilobe Profile Math:
---------------------
A trilobe/taptite has a 3-lobed cross-section defined by:
  - C(n): Major diameter across lobe crests [inches]
  - E(n): Minor diameter across valleys [inches]

Polar equation for 3-lobe profile:
  r(θ) = R_base + A × cos(3θ)
where:
  R_base = (C + E) / 4    — average radius
  A = (C - E) / 4         — lobe amplitude

For helical trilobes (threads), the profile rotates with lead:
  θ_offset(z) = 2π × z / lead_length

Output Formats:
---------------
- STEP (AP214): For Fusion 360, hyperMILL, Mastercam import
- DXF: 2D p
```


## Exports
`TrilobeStage` (interface), `TrilobeInput` (interface), `ProfilePoint2D` (interface), `Point3D` (interface), `TrilobeSection` (interface), `TrilobeGeometry` (interface), `CAMExport` (interface), `TrilobeOutput` (interface), `calculateTrilobeProfile` (function), `calculateLobeRotation` (function), `interpolateDiameters` (function), `calculateVolume` (function), `TrilobeElectrodeGeometryEngine` (class), `trilobeElectrodeGeometryEngine` (const)

## Key methods
`generate`, `getProfile`, `stats`

## Depends on
[[reference_code_engine_llmengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/TrilobeElectrodeGeometryEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find TrilobeElectrodeGeometryEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/TrilobeElectrodeGeometryEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
