---
name: reference_code_engine_speedfeedpropagationbridgeengine
description: "auto-propagates 9-axis SFC results downstream."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SpeedFeedPropagationBridgeEngine.ts
  sourceHash: 99cb504e
  loc: 538
aliases: SpeedFeedPropagationBridgeEngine
---

# SpeedFeedPropagationBridgeEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SpeedFeedPropagationBridgeEngine.ts` (538 LOC)

## What it is
auto-propagates 9-axis SFC results downstream.

```
SpeedFeedPropagationBridgeEngine — auto-propagates 9-axis SFC results downstream.

Problem solved: when the 9-axis orchestrator computes a new speed/feed
recommendation, every downstream consumer (post-processors, machine wizards,
print-to-program pipelines) must see the new values consistently and without
re-running the calc independently. This engine is the canonical bridge.

Architecture:
  1. Versioned snapshot store — keyed by `(machine.name, material.name,
     tooling.tool_diameter_mm)`. Bumps version on each publish.
  2. Five domain bridges that translate a NineAxisResult into the parameter
     shape each consumer expects:
      - Post-processor (feed/speed override block per cycle type)
      - Mill wizard (form defaults: Vc, fz, ap, ae, coolant, holder)
      - Lathe wizard (Vc, fn, ap, insert, chip-breaker recommendation)
      - Wire EDM wizard (parameter-pack-only — sparks
```


## Exports
`PropagationDomain` (type), `SnapshotKey` (interface), `SnapshotRecord` (interface), `PostProcessorParameterPack` (interface), `MillWizardDefaults` (interface), `LatheWizardDefaults` (interface), `WedmWizardDefaults` (interface), `PrintToProgramParameterPack` (interface), `PropagationEvent` (interface), `PropagationSubscriber` (type), `SpeedFeedPropagationBridgeEngine` (class), `speedFeedPropagationBridgeEngine` (const)

## Key methods
`publish`, `sub`, `subscribe`, `getCurrent`, `listSnapshots`, `subscriberCount`, `bridgeToPostProcessor`, `bridgeToMillWizard`, `bridgeToLatheWizard`, `bridgeToWedmWizard`, `bridgeToPrintToProgram`, `propagateAll`, `clear`

## Depends on
[[reference_code_engine_speedfeednineaxisorchestratorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/SpeedFeedPropagationBridgeEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find SpeedFeedPropagationBridgeEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SpeedFeedPropagationBridgeEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
