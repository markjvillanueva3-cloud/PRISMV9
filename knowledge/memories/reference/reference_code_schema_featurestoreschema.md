---
name: reference_code_schema_featurestoreschema
description: "Feature Store Schema — U-LEARN-02"
metadata:
  type: reference
  kind: code-schema
  galaxy: unassigned
  sourcePath: mcp-server/src/schemas/featureStoreSchema.ts
  sourceHash: 040fe236
  loc: 97
aliases: featureStoreSchema.ts
---

# featureStoreSchema.ts -- Schema SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/schemas/featureStoreSchema.ts` (97 LOC)

## What it is
```
Feature Store Schema — U-LEARN-02
==================================

Every ML model PRISM trains reads features from the FeatureStoreEngine.
The schema keeps writes append-only and point-in-time correct so that a
training job at t=T0 only sees features observable at t=T0 (no future
leakage — call-out from the Evaluation-Scientist scrutiny).

A feature row is keyed on (entity_id, event_ts) and scoped to a
(domain, feature_group, version) shard.  Consumers query by
  { entities: [...], as_of_ts, domain, feature_group }
and get AS-OF values — i.e. the most recent row per entity with
event_ts ≤ as_of_ts.

@module schemas/featureStoreSchema
@milestone PSAU P2.5-LEARN U-LEARN-02
```


## Exports
`IsoTimestamp` (const), `FeatureRowSchema` (const), `FeatureRow` (type), `FeatureQuerySchema` (const), `FeatureQuery` (type), `PutFeatureInputSchema` (const), `PutFeatureInput` (type), `FeatureQueryResultRowSchema` (const), `FeatureQueryResultRow` (type), `FeatureQueryResultSchema` (const), `FeatureQueryResult` (type)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/schemas/featureStoreSchema.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Schema

## How to query it
Recall by meaning via `/system-viz find featureStoreSchema.ts` or `prism_session:master_index_query`; open the source at `mcp-server/src/schemas/featureStoreSchema.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
