---
name: reference_code_engine_groundtruthvalidationengine
description: "corpus integrity gate for the ground-truth"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/GroundTruthValidationEngine.ts
  sourceHash: 4a80f54b
  loc: 904
aliases: GroundTruthValidationEngine
---

# GroundTruthValidationEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/GroundTruthValidationEngine.ts` (904 LOC)

## What it is
corpus integrity gate for the ground-truth

```
GroundTruthValidationEngine — corpus integrity gate for the ground-truth
bundles produced by GroundTruthBatchExtractor (U-CGT07).

Walks {outputRoot}/{fileId}/ directories, runs eight independent integrity
checks per bundle, and emits a ValidationReport whose quarantineList enumerates
every fileId that should be re-extracted or excluded from regression
comparisons.

Per-bundle checks (each maps to a stable issue code so callers can route
automated repairs without string-parsing reasons):
  bundle-json-missing         — bundle.json absent
  bundle-json-parse-error     — bundle.json present but malformed JSON
  bundle-schema-invalid       — bundle.json fails the loose schema
  bundle-status-failed        — status === "failed" (extractor gave up)
  step-missing                — source.step not present on disk
  step-not-iso-10303          — source.step lacks the ISO-10303-21 magic
  feature
```


## Exports
`ISSUE_CODES` (const), `IssueCode` (type), `CANONICAL_SCREENSHOT_VIEWS` (const), `BundleIssueSchema` (const), `BundleIssue` (type), `BundleValidationSchema` (const), `BundleValidation` (type), `ValidationReportSchema` (const), `ValidationReport` (type), `ValidateOptions` (interface), `GroundTruthValidationEngine` (class), `ExtractionTrainingPair` (interface), `ExtractionBackendOutput` (interface), `BackendValidationResult` (interface), `BackendComparisonResult` (interface), `RegressionGateResult` (interface), `pickGroundTruthScalar` (function), `extractionMatches` (function), `computeConformalCoverage` (function), `groundTruthValidationEngine` (const)

## Key methods
`validateBundle`, `hasNonFinite`, `validateCorpus`, `exportQuarantine`, `validate`, `listIssueCodes`, `validateExtractionBackend`, `compareBackends`, `snapshotBaseline`, `getBaseline`, `clearBaselines`, `regressionGate`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/GroundTruthValidationEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find GroundTruthValidationEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/GroundTruthValidationEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
