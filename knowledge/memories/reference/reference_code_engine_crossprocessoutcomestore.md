---
name: reference_code_engine_crossprocessoutcomestore
description: "event-sourced outcome ledger for the 5 XPROC"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CrossProcessOutcomeStore.ts
  sourceHash: a223dfb4
  loc: 884
aliases: CrossProcessOutcomeStore
---

# CrossProcessOutcomeStore -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CrossProcessOutcomeStore.ts` (884 LOC)

## What it is
event-sourced outcome ledger for the 5 XPROC

```
CrossProcessOutcomeStore — event-sourced outcome ledger for the 5 XPROC
bridges (XPROC-SFC, XPROC-POST, XPROC-FEAT, XPROC-AI, XPROC-ROUTER).

Every bridge invocation is logged here with full request/response context
plus eventual outcome (success / failure / operator override / pending).
Downstream Tier-1 neural engines read from this store as their training
signal source: the neural learner trains on classification accuracy, the
transfer engine looks up similar past jobs across processes, the explainer
computes feature importance from observed outcomes.

Storage:
  - In-memory ring buffer (capacity-bounded, default 10,000 events)
  - Append-only JSONL persistence at the configured store_path
  - Each event carries a schemaVersion for forward-compat migration
  - record() and recordOutcome() are O(1) append; query/retrieveSimilar
    are O(N) over the active window

Concurrency:
  - In-p
```


## Exports
`OUTCOME_BRIDGES` (const), `OutcomeBridge` (type), `OUTCOME_PROCESSES` (const), `OutcomeProcess` (type), `OUTCOME_KINDS` (const), `OutcomeKind` (type), `SCHEMA_VERSION` (const), `OutcomeNumericFeatures` (interface), `OutcomeCategoricalFeatures` (interface), `OutcomeRequestSummary` (interface), `OutcomeResponseSummary` (interface), `OutcomeOperator` (interface), `OutcomeRecord` (interface), `RecordEventInput` (interface), `OutcomeQueryFilter` (interface), `SimilarityResult` (interface), `OutcomeStats` (interface), `CrossProcessOutcomeStore` (class), `crossProcessOutcomeStore` (const), `NUMERIC_FEATURE_KEYS` (const)

## Key methods
`record`, `validateNumericFeatures`, `validateOutcomeKind`, `recordOutcome`, `query`, `retrieveSimilar`, `stats`, `replay`, `replayJob`, `replaySince`, `streamReplayFromDisk`, `setCapacity`, `clear`, `size`, `configureStorePath`, `persistEvent`

## Depends on
[[reference_code_engine_feedbackbusengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CrossProcessOutcomeStore.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CrossProcessOutcomeStore` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CrossProcessOutcomeStore.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
