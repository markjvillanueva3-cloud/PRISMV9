---
name: reference_test_rigor_floor_advisory_2026_06_24
description: "Critical-domain test-RIGOR floor (advisory) shipped 2026-06-24 slot:alpha -- the orthogonal axis to fake-test detection (catches SHALLOW real-but-thin critical tests). Why it's advisory not a hard block (measured 42.6% FP); the AI-judge next step; uncommitted follow-ups; the shared-tree lane-guard plumbing escape."
type: reference
source: prism-memory
synced: 2026-06-27T20:30:47.221Z
aliases: reference_test_rigor_floor_advisory_2026_06_24
---


# Test-RIGOR floor (advisory) -- slot:alpha 2026-06-24, commit 266812bce7

Continuation of the operator /checkin-alpha test-integrity assessment ([[reference_test_integrity_gate_audit_2026_06_23]]). Operator: "ensure tests are true/validated/exhaustive/complex... utilize ollama+octopus+hermes+one opus agent."

## Honest assessment finding
The PRISM anti-FAKE-test stack is genuinely STRONG and was just hardened (prior alpha session, was uncommitted): `test-legitimacy.mjs` (PreToolUse, BLOCKS placeholder/.skip/.only/synthetic-mass-gen/critical-domain-mocked-SUT/task-context-mismatch + presence-only DOMINANCE calibrated to ~99% FP avoided + Edit-fragment splice), `stop_on_failing_tests.mjs` (Stop, BLOCKS + new stale-GREEN closer), `ci-test-legitimacy-scan.mjs` (CI, env-flag-proof backstop), `scrutinize-before-stop` (3-of-3). `comprehensive-build-enforce` is ADVISORY-only (asks for happy+>=3 failure+>=2 adversarial, nothing verifies it).

The REAL gap: a test can pass EVERY gate while being SHALLOW -- one happy-path `it()`, zero failure-mode/adversarial assertions. That is the operator's "easy tests just to say you passed."

## What shipped (Unit 1)
`scoreTestRigor` + `detectShallowCriticalTest` in `.claude/helpers/lib/test-legitimacy-core.mjs` (shared by the PreToolUse hook AND the CI scan). Pure scorer: caseCount, strongAssertions (incl Testing-Library DOM matchers), hasFailureMode (toThrow/rejects/toBeNaN/expect(()=>)), hasAdversarialInput (NaN/Infinity/boundary/overflow/invalid/negative -- EXCLUDES null/undefined/'' as too common). Wired non-blocking into `test-legitimacy.mjs` (both entrypoints). Calibration harness: `scripts/measure-test-rigor-corpus.mjs`. Tests: `.claude/hooks/__tests__/test-rigor-floor.test.mjs` (14/14). ci.yml legitimacy gate wired.

## Why ADVISORY, not a hard block (the load-bearing decision)
Corpus calibration MEASURED a hard block on "critical-domain test with no-failure-mode AND no-adversarial" = **42.6% FP (706/1656)**. Positive reference-value tests (assert cutting-force=1234N for 6 materials) are the R9 GOLD standard, NOT shallow. Even the thin band (cases<=3 AND asserts<=6) has FPs (e.g. `sfc-nine-axis-runout-no-double-count.test.ts` is a valuable 2-case regression-lock). The regex layer CANNOT separate "thin but valuable" (regression-lock/render-smoke) from "thin and lazy" -> so it ADVISES only (thin-band fires on 1.5% = 25 files, sharp + low-noise). Hard semantic judgment is the AI judge's job.

## Validation (operator-mandated stack) -- unanimous PASS
Opus reviewer (max) + Sonnet code-analyzer (both PASS, only P2s, all in the SAFE under-advise direction; fixed the double-IO + null-input P2s) + Hermes/Grok ("advisory is correct; 42.6% FP unacceptable for a hard gate") + Ollama qwen (independent code-read matched intent). Octopus CLI returned a staging-stub (route:single-claude, dispatched:false) -- did NOT real-fan-out; the 4-model panel IS the effective consensus (reported honestly).

## Unit 2 SHIPPED (806bda494d) -- AI test-RIGOR judge (ollama/hermes)
`scripts/lib/test-rigor-judge-core.mjs` (pure + 18/18 tested: resolveSutPath incl TS NodeNext `.js`->`.ts`, buildJudgePrompt, robust parseJudgeResponse) + `scripts/test-rigor-judge.mjs` CLI. An LLM reads test+SUT, rates "would an assertion FAIL if the source regressed?". Ollama(free, /api/generate format:json) -> Hermes(:8645) fallback via fetch; NEVER fabricates (R12: both-fail -> ok:false). Batch mode pre-filtered by `detectShallowCriticalTest` (only the ~1.5pct thin critical candidates are LLM-judged -> bounded cost). LIVE-validated: rated the `sfc-nine-axis-runout` regression-lock RIGOROUS 85 (the regex advisory had flagged it "thin") and a `LatheUploadPage` render-smoke WEAK 10 -- proving the deterministic + semantic layers COMPOSE (regex pre-filters cheaply, LLM makes the call regex can't). 2-arm scrutiny PASS (code-analyzer + sonnet reviewer); fixed P1 hermesModelId timeout + P2 backslash-parity + prompt-injection fence delimiter.
Usage: `node scripts/test-rigor-judge.mjs <file.test.ts> [--hermes] [--json]` or `--batch [--limit=N]`.

## NEXT INCREMENT (Unit 3 candidate)
Wire the judge into a Stop/CI ADVISORY that auto-runs on thin critical candidates (NOT a synchronous LLM in a blocking PreToolUse hook -- latency/nondeterminism). Octopus local panel (`octopus-with-hermes-rag.mjs --local-only`) is the free multi-voice option if a single judge proves noisy.

## Uncommitted FOLLOW-UPS (flagged, left for a deliberate next cycle)
- `.claude/hooks/stop_on_failing_tests.mjs` stale-GREEN logic (good, fail-open) is UNTESTED -> refactor `newestChangedTestNewerThan` to a pure fn + add R9 test, then commit.
- `.claude/settings.json` has (a) the test-legitimacy PreToolUse WIRING (needs commit to be durable) + (b) two UNRELATED autocompact-knob deletions (`CLAUDE_AUTOCOMPACT_PCT_OVERRIDE`, `autoCompactWindow`) -- needs a deliberate keep-or-revert decision, do NOT silently bundle.

## FINDING (2026-06-24) -- stale-green check is NOT slot/session-scoped (multi-chat false-positive)
The in-flight `stop_on_failing_tests` stale-GREEN check (`newestChangedTestNewerThan`) runs `git status --porcelain` over the WHOLE shared `H:/prism` tree, so ANY peer slot's uncommitted test edit blocks EVERY slot's Stop (observed live: alpha's Stop blocked by a peer's `documentExtractionRouter.test.ts` edit alpha never touched). Safe direction (only over-blocks -> asks for a re-run, fail-open on git error), but it is cross-chat friction. The eventual R9 refactor of this fn (pure `pickStaleTestFromStatus`) should consider scoping to session-touched files -- though git status cannot attribute edits by session, so the honest options are: (a) accept the conservative cross-tree block, or (b) compare against the report's own file-set. Capture before the refactor.

## Shared-tree commit escape (operational note)
The `git-add-lane-guard` (PreToolUse) blocks `git add` in the shared tree when chat-slots[slot].branch starts with `slot/`. It reads `PRISM_GIT_ADD_LANE_DISABLE=1` from the HOOK's env (UNREACHABLE from an inline bash `VAR=1`), and editing chat-slots[slot].branch does NOT stick (a slot-bind hook re-derives `slot/<name>` each command). The working [MAIN-FORCE] escape for operator-armed shared-tree fleet-infra: stage via `git update-index --add -- <files>` (plumbing, does NOT match the `git add` regex), then `git commit -m '[MAIN-FORCE] ...'`.

## Audit COMPLETED to full 25/25 (2026-06-24, U-RIGOR-AUDIT-25)
Closed the R12 "10/25" gap to the full population (operator "exhaustive" / ALL-MEANS-ALL). Distribution: **1 rigorous / 3 shallow (would-catch but not exhaustive) / 20 weak (regression would NOT fail the test) / 1 sut-not-resolved**. The 10 overlapping files held their VERDICT BAND across two providers (pass-1 Hermes/Grok, pass-2 Ollama qwen2.5-coder:32b) though the exact integer drifted ±15 (cam-schema 35→20, CAM-engine 15→20 -- both stayed weak; runout 85 rigorous both) -- so the judge is trustworthy at the BAND level, act on the band not the score. Two HIGH-VALUE weak findings (real logic, non-load-bearing tests) are kilo's: `SelfLearningCAMEngine.test.ts` (Bayesian/Kalman/Mahalanobis ML, score 20) + `cam-dispatcher-schema-collision.test.ts` (Zod schemas never actually parsed, score 20). NEW structural finding: `web/src/__tests__/` mirrors `mcp-server/web/src/__tests__/` (CalculatorPage.*/QuoteAnalyticsPage duplicated, identical verdicts) -- stale dup test tree, flag for quebec. The 7 Lathe/ShopFloor/WireEdm render-smokes are presence-only but a known-acceptable pattern (low ROI). Commit (canonical 25/25 JSON+MD): see U-RIGOR-AUDIT-25. The system now both DETECTS (deterministic floor) and DIAGNOSES (AI judge, octopus-validated) shallow critical tests end-to-end -- the operator's "fake/easy test" gap is closed.

## U-STOPGATE-R9 (2026-06-24, commit ab2b3bc84a) -- first R9 coverage for the stop_on_failing_tests HARD-BLOCK gate
The directive's FIRST clause ("assess stop hooks like safety gates") -> the `stop_on_failing_tests` Stop gate had ZERO test coverage (Glob found none). Landed alpha's prior UNCOMMITTED stale-green freshness block (net-new vs HEAD, 97 ins) WITH: (1) pure exported `pickStaleTestFromStatus(porcelainOut, reportMs, statMtimeMs, slackMs)` extracted from `newestChangedTestNewerThan` (behavior-identical -- verified line-by-line + mutation-tested), (2) exported `normalizeReport`/`isFresh`, (3) a main-guard (`__isDirectRun` = argv[1].endsWith("/stop_on_failing_tests.mjs") OR import.meta.url===pathToFileURL(argv[1]).href) so the module is importable without running main(), (4) `.claude/hooks/__tests__/stop_on_failing_tests.test.mjs` (17/17) pinning normalizeReport shapes + isFresh + the stale-green decision in BOTH directions. VERIFIED: live subprocess (`echo '{}' | portable-node stop_on_failing_tests.mjs` -> continue:false EXIT=1, gate still fires) + 2-arm scrutiny (reviewer + code-analyzer both PASS; no under-block; fail-closed-on-crash now STRONGER since main() is wrapped in try/catch->block).
RECONCILED with echo's same-day finding [[reference_test_freshness_gate_thrash_concurrent_fleet_2026_06_24]]: the gate's whole-tree git-status scan THRASHES under concurrent fleet build (a PEER slot's untracked test blocks THIS slot's Stop -- observed live: it flagged resourceExtractionDispatcher/embed-pdf-tribal-tips peer tests). My pin is at the PURE-FN layer (a slot's OWN new test MUST block -- correct, never under-block); the thrash fix is at the CALLER layer (scope the input to the slot's own changes -- per-slot shard / session attribution), which my unit test does NOT constrain, so the pin survives that infra-lane fix. I did NOT implement the per-slot fix (infra-lane decision, competing designs; "never soften a safety gate unilaterally") nor set SKIP_FRESHNESS=1. The R9 test is the regression net that makes the eventual per-slot fix SAFE. echo also found a deeper root cause: the report is ~56-days-stale fleet-wide because the refresh cmd (`npx vitest`) exits 127 in background/cron (npx not on PATH) -- ALSO infra-lane (PATH-robust refresh via portable-node abs path). Honesty note (R12): the commit LANDS a feature (the freshness block was uncommitted at HEAD), not a no-op refactor -- framed as such.
