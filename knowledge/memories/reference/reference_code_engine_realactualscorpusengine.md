---
name: reference_code_engine_realactualscorpusengine
description: "Real Actuals Corpus"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/RealActualsCorpusEngine.ts
  sourceHash: dfbcea2f
  loc: 259
aliases: RealActualsCorpusEngine
---

# RealActualsCorpusEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/RealActualsCorpusEngine.ts` (259 LOC)

## What it is
Real Actuals Corpus

```
RealActualsCorpusEngine -- QUOTING-OPTIMAL-MS0 / U1

Builds a CLEAN, real-ground-truth training corpus from the 6,718 OCR-extracted
settled-order records in state/shared/quoting/orders-closed-actuals.jsonl
(source: docustrata-text-extracted-v3-ocr-pass). This replaces the synthetic
bootstrap baseline (actual_revenue_usd = size_bytes x $ stub) that starved the
quoting calibration loop -- every record here is a REAL invoiced price tied to a
real (customer, part_id, order_number).

WHY a filter is mandatory (measured, not assumed -- quoting-actuals-outlier-probe):
  - 217 records price < $5 (OCR decimal-point misreads / unit-vs-total confusion;
    e.g. OPTIMAS SOLUTIONS 841-SM-11 = $0.13).
  - 46 records price > $250k (concatenated totals / digit misreads; e.g.
    WRENTHAM TOOL "630-6I6" = $5,162,000 -- the "I" is a misread "1").
  - 1,523 records extraction_confidence < 0.7.
  - 1,748 us
```


## Exports
`PRICE_FLOOR_USD` (const), `PRICE_CEIL_USD` (const), `MIN_EXTRACTION_CONFIDENCE` (const), `IQR_FENCE_K` (const), `RejectReason` (type), `RawActual` (interface), `CleanActualRecord` (interface), `RejectedActual` (interface), `CorpusBuildResult` (interface), `BuildOptions` (interface), `RealActualsCorpusEngine` (class), `realActualsCorpusEngine` (const)

## Key methods
`build`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/RealActualsCorpusEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find RealActualsCorpusEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/RealActualsCorpusEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
