---
name: reference_code_engine_ollamaclientengine
description: "Thin client around the `ollama` npm package"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/OllamaClientEngine.ts
  sourceHash: d038ef21
  loc: 215
aliases: OllamaClientEngine
---

# OllamaClientEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/OllamaClientEngine.ts` (215 LOC)

## What it is
Thin client around the `ollama` npm package

```
OllamaClientEngine — Thin client around the `ollama` npm package

Phase external-infra. Wraps Ollama's local LLM runtime so PRISM callers
can request chat completions, plain-text generation, and embeddings
without importing the Ollama SDK directly. The real daemon must be
running (either via `docker compose up -d ollama` or a host install);
this engine is a stateless HTTP client.

No streaming in this first pass — everything buffers to a string. A
future `chatStream()` is trivial to add once a consumer needs it.

@module engines/OllamaClientEngine
@milestone PP-INFRA-OLLAMA
```


## Exports
`OllamaRole` (type), `OllamaMessage` (interface), `OllamaGenerateOptions` (interface), `OllamaChatOptions` (interface), `OllamaEmbedOptions` (interface), `OllamaResult` (interface), `OllamaClientEngine` (class), `ollamaClientEngine` (const)

## Key methods
`connect`, `isConnected`, `getHost`, `listModels`, `generate`, `chat`, `embed`, `disconnect`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/OllamaClientEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find OllamaClientEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/OllamaClientEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
