---
name: reference_code_engine_wedmprogramsafetygateengine
description: "Composite S(x) Safety Gate"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/WEDMProgramSafetyGateEngine.ts
  sourceHash: cb6deccc
  loc: 448
aliases: WEDMProgramSafetyGateEngine
---

# WEDMProgramSafetyGateEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/WEDMProgramSafetyGateEngine.ts` (448 LOC)

## What it is
Composite S(x) Safety Gate

```
WEDMProgramSafetyGateEngine — Composite S(x) Safety Gate

Aggregates safety scores from 7 component gates into a single weighted S(x)
score that determines whether a WEDM program can emit. This is the final
gate before G-code reaches the controller.

S(x) = Σ (weight_i × pass_i) where pass_i ∈ {0, 1}

Component weights (sum = 1.0):
  collision      0.20  — WEDMWirePathCollisionEngine
  head_clearance 0.15  — WEDMHeadClearanceEngine
  flush          0.15  — WEDMFlushAdequacyGateEngine
  thermal        0.15  — WEDMThermalReleaseGateEngine
  dialect        0.10  — WEDMControllerDialectVerifierEngine
  unit_tag       0.10  — WEDMUnitTagGateEngine
  deflection     0.15  — WEDMWireDeflectionEngine

Thresholds:
  S(x) ≥ 0.90  → PASS (green)
  0.70 ≤ S(x) < 0.90 → WARNING (yellow, operator review)
  S(x) < 0.70  → HARD BLOCK (red, cannot emit)

Mandatory components: collision and unit_tag must A
```


## Exports
`SafetyComponent` (type), `SafetyVerdict` (type), `ComponentResult` (interface), `SafetyGateInput` (interface), `ComponentScoreEntry` (interface), `SafetyGateResult` (interface), `WEDMProgramSafetyGateEngine` (class), `wedmProgramSafetyGateEngine` (const)

## Key methods
`evaluate`, `canEmit`, `getScore`, `getWeight`, `getWeights`, `getThresholds`, `gate`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/WEDMProgramSafetyGateEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find WEDMProgramSafetyGateEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/WEDMProgramSafetyGateEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
