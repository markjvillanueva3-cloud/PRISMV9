---
name: reference_code_lib_wizardbridgeabsorption
description: "wizard-bridge-absorption.mjs — concrete mill / lathe / wire-EDM wizard"
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/wizard-bridge-absorption.mjs
  sourceHash: 9fed6fdc
  loc: 134
aliases: wizard-bridge-absorption.mjs
---

# wizard-bridge-absorption.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/wizard-bridge-absorption.mjs` (134 LOC)

## What it is
```
wizard-bridge-absorption.mjs — concrete mill / lathe / wire-EDM wizard
configurations that absorb the 3 domain wizards through the iter38
wizard-node-bridge contract.

Closes U-WIZARD-ABSORB-3: proves the iter38 wizard contract isn't just
theoretical — three real wizard schemas (one per WIZARD_DOMAINS entry)
pass through createWizard() and complete end-to-end via advance(),
emit(), and summarize* helpers.

Mill wizard (12 steps): material → stock geometry → tool selection →
  strategy → DOC / WOC → coolant → safety check → emit
Lathe wizard (10 steps): material → bar stock → chuck → tool → CSS or
  G97 → feed strategy → safety → emit
Wire-EDM wizard (11 steps): material → thickness → wire spec → flush →
  passes → cut speed → corner derate → safety → emit

Each step is canonical (id + kind + prompt + required flag +
optional validator). No I/O — caller wires real UI input on top.

@miles
```


## Exports
`ABSORPTION_SCHEMA_VERSION` (const), `MIN_TOOL_DIAMETER_MM` (const), `MIN_WIRE_DIAMETER_MM` (const), `MIN_BAR_DIAMETER_MM` (const), `validateIsoGroup` (function), `validatePositiveNumber` (function), `validateMinimum` (function), `MILL_WIZARD_STEPS` (const), `LATHE_WIZARD_STEPS` (const), `WIRE_EDM_WIZARD_STEPS` (const), `ALL_WIZARD_CONFIGS` (const), `buildDomainWizard` (function), `listAbsorbedDomains` (function), `totalAbsorbedSteps` (function), `stepCountsByDomain` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/wizard-bridge-absorption.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find wizard-bridge-absorption.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/wizard-bridge-absorption.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
