---
name: reference_code_engine_quotingactivefactorloaderengine
description: "durable bridge from calibration JSON to live quote path"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/QuotingActiveFactorLoaderEngine.ts
  sourceHash: 70f21dc4
  loc: 541
aliases: QuotingActiveFactorLoaderEngine
---

# QuotingActiveFactorLoaderEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/QuotingActiveFactorLoaderEngine.ts` (541 LOC)

## What it is
durable bridge from calibration JSON to live quote path

```
QuotingActiveFactorLoaderEngine — durable bridge from calibration JSON to live quote path

Closes the runtime loop for U-QT10 calibration. The active factors live at
`state/shared/calibration/quoting-calibration-active.json` (produced by
`QuotingCalibrationEngine.derive()` + the calibration-cycle runner). This
loader is the bridge that lets EVERY downstream quote consumer (QuoteEstimator,
QuoteBuilderPage, Xometry-style flow, etc.) pull the currently-active
calibration factors with a 60-second cache + safe fallback.

WITHOUT this engine, the U-QT10 calibration cycle ships dormant — factors
exist on disk but never reach the live quote path.
WITH this engine, any predicted_usd from the quoting stack gets the
systematic over-prediction corrected automatically.

Surface contract:
  - getActiveFactors() — cached read of active JSON; metadata + ok/reason
  - applyToQuote(predicted_usd, custome
```


## Exports
`DEFAULT_ACTIVE_FACTOR_PATH` (const), `DEFAULT_TRAINING_STATUS_PATH` (const), `LiveMapeGateResult` (interface), `gateObservedMape` (function), `ActiveFactorMetadata` (interface), `ActiveFactorReadResult` (interface), `TrainingStatusReadResult` (interface), `ActiveFactorApplyResult` (interface), `QuotingActiveFactorLoaderEngine` (class), `quotingActiveFactorLoaderEngine` (const)

## Key methods
`setPath`, `getPath`, `getActiveFactors`, `applyToQuote`, `refresh`, `readLatestTrainingStatus`, `getLiveObservedMapePct`, `getMetadata`

## Depends on
[[reference_code_engine_quotingcalibrationengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/QuotingActiveFactorLoaderEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find QuotingActiveFactorLoaderEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/QuotingActiveFactorLoaderEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
