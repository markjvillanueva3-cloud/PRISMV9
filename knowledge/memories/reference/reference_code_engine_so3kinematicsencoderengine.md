---
name: reference_code_engine_so3kinematicsencoderengine
description: "SO3KinematicsEncoderEngine — SO(3) Rotation Group Kinematics Encoder"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/SO3KinematicsEncoderEngine.ts
  sourceHash: 144b046d
  loc: 1455
aliases: so3KinematicsEncoderEngine
---

# so3KinematicsEncoderEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/SO3KinematicsEncoderEngine.ts` (1455 LOC)

## What it is
```
SO3KinematicsEncoderEngine — SO(3) Rotation Group Kinematics Encoder
=====================================================================

Critical engine for 5-axis post-processing providing rigorous SO(3) rotation
group mathematics. SO(3) = Special Orthogonal Group in 3D represents all
3D rotations as 3x3 orthogonal matrices with det=1.

CAPABILITIES:
  1. Multiple rotation representations (matrix, quaternion, axis-angle, Euler)
  2. Lie algebra so(3) for neural network embedding
  3. Exponential map: so(3) -> SO(3)
  4. Logarithmic map: SO(3) -> so(3)
  5. SLERP interpolation for smooth 5-axis motion
  6. Denavit-Hartenberg kinematic chain encoding
  7. Machine topology support (trunnion, swivel head, mill-turn, 6-axis robot)
  8. Singularity detection (gimbal lock, workspace boundaries)
  9. Neural-friendly 32-dimensional embedding

MATHEMATICAL FOUNDATION:
  - Rodrigues' formula: e
```


## Exports
`Vector3` (interface), `Quaternion` (interface), `RotationMatrix` (type), `HomogeneousMatrix` (type), `AxisAngle` (interface), `EulerAngles` (interface), `EulerConvention` (type), `DHParameters` (interface), `SO3Embedding` (interface), `MachineTopology` (type), `KinematicChainConfig` (interface), `SingularityResult` (interface), `NeuralEmbedding` (interface), `so3KinematicsEncoderEngine` (const), `expMap` (re-export), `logMap` (re-export), `quatSlerp` (re-export), `quatFromRotationMatrix` (re-export), `quatToRotationMatrix` (re-export), `forwardKinematics` (re-export)

## Key methods
`fromRotationMatrix`, `fromQuaternion`, `fromAxisAngle`, `fromEulerAngles`, `fromLieAlgebra`, `identity`, `expMap`, `logMap`, `compose`, `inverse`, `interpolate`, `distance`, `rotateVector`, `toNeuralEmbedding`, `fromNeuralEmbedding`, `createKinematicChain`, `forwardKinematics`, `dhTransform`, `detectSingularity`, `isNearGimbalLock`, `degToRad`, `radToDeg`, `orthogonalize`, `getStatistics`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/SO3KinematicsEncoderEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find so3KinematicsEncoderEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/SO3KinematicsEncoderEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
