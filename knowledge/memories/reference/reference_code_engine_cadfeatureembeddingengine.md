---
name: reference_code_engine_cadfeatureembeddingengine
description: "CADCAM-DAGI-MS0/U-DAGI05"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADFeatureEmbeddingEngine.ts
  sourceHash: 0f678783
  loc: 450
aliases: CADFeatureEmbeddingEngine
---

# CADFeatureEmbeddingEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADFeatureEmbeddingEngine.ts` (450 LOC)

## What it is
CADCAM-DAGI-MS0/U-DAGI05

```
CADFeatureEmbeddingEngine — CADCAM-DAGI-MS0/U-DAGI05

Dense feature representation for CAD token sequences. Extracts embeddings
from trained CAD language models and provides FAISS-style similarity search.

Architecture — pure controller, injectable EmbeddingBackend:
  The engine owns no neural weights. An `EmbeddingBackend` plug-in extracts
  embeddings from token sequences. Tests supply a deterministic mock (hash-based);
  production wires HuggingFace sentence-transformers / Ollama / etc.

Key methods:
  embed(tokens, backend)           — extract embedding vector for a sequence
  embedBatch(corpus, backend)      — batch extraction with caching
  buildIndex(embeddings)           — construct similarity index (VP-tree or flat)
  search(query, index, k, metric)  — find k nearest neighbors
  clearCache()                     — invalidate embedding cache

Index structures:
  - FlatIndex: brute
```


## Exports
`TokenSeq` (type), `Embedding` (type), `SimilarityMetric` (type), `EmbeddingBackend` (interface), `SearchResult` (interface), `IndexedEmbedding` (interface), `SimilarityIndex` (interface), `EmbedConfig` (interface), `CADFeatureEmbeddingEngine` (class), `cadFeatureEmbeddingEngine` (const)

## Key methods
`embed`, `getCapabilities`, `validate`, `embedBatch`, `clearCache`, `cacheStats`, `buildIndex`, `search`, `searchVPTree`, `searchByTokens`

## Depends on
[[reference_code_engine_baseengine]] | [[reference_code_engine_iengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADFeatureEmbeddingEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADFeatureEmbeddingEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADFeatureEmbeddingEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
