---
name: reference_code_engine_hypermillmillturnstrategyengine
description: "Mill-Turn CSS/TNRC Strategy + C-Axis Sync"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HyperMillMillTurnStrategyEngine.ts
  sourceHash: 1032f701
  loc: 391
aliases: HyperMillMillTurnStrategyEngine
---

# HyperMillMillTurnStrategyEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HyperMillMillTurnStrategyEngine.ts` (391 LOC)

> [physics] Imports canonical physics constants (`src/physics/constants.ts`) -- safety-relevant node.

## What it is
Mill-Turn CSS/TNRC Strategy + C-Axis Sync

```
HyperMillMillTurnStrategyEngine — Mill-Turn CSS/TNRC Strategy + C-Axis Sync

Implements:
  1. CSS (Constant Surface Speed) RPM limit validation:
     RPM = (1000 × Vc) / (π × D_min)  — blocks if > machine max RPM
  2. C-axis synchronization logic for cross-hole, cross-slot, off-center features
  3. Multi-channel synchronization wired to MillTurnSwissPipelineEngine

Physics:
  RPM_required = (1000 × Vc_m_min) / (π × D_min_mm)
  If RPM_required > machine_max_rpm → switch to G97 or raise error

References:
  - Sandvik Coromant Turning Guide (2024): CSS RPM limits at small diameters
  - ISO 6983-1: G96/G97 modal group 2
  - DMG MORI NTX programming manual: C-axis synchronization codes

HM-REV-MS7 U-HMR39 + U-HMR40
```


## Exports
`CSSValidationStatus` (type), `CSSLimitCheckInput` (interface), `CSSLimitCheckResult` (interface), `CAxisSyncType` (type), `CAxisSyncInput` (interface), `CAxisSyncResult` (interface), `MultiChannelSyncInput` (interface), `MultiChannelSyncResult` (interface), `HyperMillMillTurnStrategyEngine` (class), `hyperMillMillTurnStrategyEngine` (const)

## Key methods
`checkCSSLimit`, `calculateCAxisSync`, `generateMultiChannelSync`, `calculateMillTurnStrategy`, `stats`

## Depends on
[[reference_code_util_logger]]

## Where it lives
- **Source:** `mcp-server/src/engines/HyperMillMillTurnStrategyEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HyperMillMillTurnStrategyEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HyperMillMillTurnStrategyEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
