---
name: reference_code_engine_budgetengine
description: "QuickBooks 'Budgets' + budget-vs-actual variance reporting for the PRISM ERP"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/BudgetEngine.ts
  sourceHash: 17ff60be
  loc: 405
aliases: BudgetEngine
---

# BudgetEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/BudgetEngine.ts` (405 LOC)

## What it is
QuickBooks "Budgets" + budget-vs-actual variance reporting for the PRISM ERP

```
BudgetEngine — QuickBooks "Budgets" + budget-vs-actual variance reporting for the PRISM ERP
(galaxy:business, slot:hotel). QB-PARITY-MS0 Phase-4 engine #4.

QuickBooks parity: the "Budgets" feature set — build a per-account, per-period (12-month fiscal)
budget, then run a "Budget vs. Actuals" report that shows variance per account-per-period plus
rollups (annual per account + grand total) and a favorable/unfavorable flag by account type.

This engine is a LAYER ON TOP of {@link GeneralLedgerEngine}. It does NOT reimplement the chart of
accounts, double-entry posting, balance validation, or the three statements. It REUSES:
  - {@link generalLedgerEngine.getChartOfAccounts} / the exported `CHART_OF_ACCOUNTS` — the single
    canonical 22-account chart; budget accountIds are validated against it (unknown → THROW).
  - the exported {@link AccountType} ("revenue" vs "expense" vs asset/liabili
```


## Exports
`BUDGET_PERIODS` (const), `BudgetLineSchema` (const), `BudgetLineInput` (type), `CreateBudgetInputSchema` (const), `CreateBudgetInput` (type), `ActualEntrySchema` (const), `ActualEntryInput` (type), `Budget` (interface), `VarianceCell` (interface), `AccountRollup` (interface), `BudgetVsActualReport` (interface), `BudgetEngine` (class), `budgetEngine` (const)

## Key methods
`createBudget`, `budgetVsActual`

## Depends on
[[reference_code_engine_generalledgerengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/BudgetEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find BudgetEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/BudgetEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
