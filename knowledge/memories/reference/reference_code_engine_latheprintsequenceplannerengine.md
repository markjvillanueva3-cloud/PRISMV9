---
name: reference_code_engine_latheprintsequenceplannerengine
description: "LathePrintSequencePlannerEngine — U-LTH37 (LATHE-MASTER P4)"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LathePrintSequencePlannerEngine.ts
  sourceHash: 9fc0f50d
  loc: 934
aliases: lathePrintSequencePlannerEngine
---

# lathePrintSequencePlannerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LathePrintSequencePlannerEngine.ts` (934 LOC)

## What it is
```
LathePrintSequencePlannerEngine — U-LTH37 (LATHE-MASTER P4)

From strategy plan → ordered operation sequence with:
  - Stock state tracking (diameter/length evolution)
  - Tool change minimization (group ops by tool)
  - Setup split detection (re-chuck / sub-spindle pickup)
  - Cycle-time prediction per operation
  - Precedence validation (center_drill → drill → bore, rough → finish, face → OD)

Consumes StrategyPlan from LathePrintFeatureStrategySelectorEngine (U-LTH36).

@milestone LATHE-MASTER U-LTH37
@version 1.0.0
```


## Exports
`StockStateSchema` (const), `StockState` (type), `PlannedOperationSchema` (const), `PlannedOperation` (type), `PrecedenceViolationSchema` (const), `PrecedenceViolation` (type), `SequencePlanSchema` (const), `SequencePlan` (type), `StockInputSchema` (const), `StockInput` (type), `SequenceCandidateLabelSchema` (const), `SequenceCandidateLabel` (type), `ConsensusSequencePlanSchema` (const), `ConsensusSequencePlan` (type), `SequenceConsensusFn` (type), `PlanSequenceWithConsensusOpts` (interface), `lathePrintSequencePlannerEngine` (const)

## Key methods
`planSequence`, `generateCandidatePlans`, `planSequenceWithConsensus`, `summarize`, `autoFix`

## Depends on
[[reference_code_engine_latheprintfeaturestrategyselectorengine]] | [[reference_code_engine_turningstrategycatalog]]

## Where it lives
- **Source:** `mcp-server/src/engines/LathePrintSequencePlannerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find lathePrintSequencePlannerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LathePrintSequencePlannerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
