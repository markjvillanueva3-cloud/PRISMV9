---
name: reference_code_script_buildmemoryembeddingssidecar
description: "A6 — build the memory-vault DENSE-embeddings sidecar that backs the hybrid"
metadata:
  type: reference
  kind: code-script
  galaxy: unassigned
  sourcePath: scripts/build-memory-embeddings-sidecar.mjs
  sourceHash: 40b54999
  loc: 268
aliases: build-memory-embeddings-sidecar.mjs
---

# build-memory-embeddings-sidecar.mjs -- Script SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/build-memory-embeddings-sidecar.mjs` (268 LOC)

## What it is
```
A6 — build the memory-vault DENSE-embeddings sidecar that backs the hybrid
BM25+dense+RRF retrieval added to memory-index-search-lib.mjs.

Reads:  H:/prism/state/shared/memory-index-sidecar.json  (the BM25 sidecar —
        reused as the record source so the embeddings sidecar aligns 1:1 by
        recordKey; never re-enumerate the vault here, that would risk drift).
Embeds: each record's "name. description. opening" via ollama
        nomic-embed-text /api/embeddings (768-d), quantized to int8.
Writes: H:/prism/state/shared/memory-embeddings-sidecar.json (atomic).

OFFLINE one-shot (or cron) — NOT the hot path, so async fetch + a concurrency
pool are fine here (the SEARCH path stays sync via curl, see the lib). The
~10.9k embeds take a few minutes; --resume skips already-embedded keys so a
crash or GPU stall never loses prior work.

  node scripts/build-memory-embeddings-sidecar.mjs    
```


## Exports
`parseArgs` (re-export), `runPool` (re-export), `embedOne` (re-export), `loadExistingByKey` (re-export)

## Key methods
`writeFileSync`, `writeSidecar`, `flushPartial`, `main`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/build-memory-embeddings-sidecar.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Script

## How to query it
Recall by meaning via `/system-viz find build-memory-embeddings-sidecar.mjs` or `prism_session:master_index_query`; open the source at `scripts/build-memory-embeddings-sidecar.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
