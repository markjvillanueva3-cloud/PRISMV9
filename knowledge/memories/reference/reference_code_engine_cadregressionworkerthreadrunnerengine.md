---
name: reference_code_engine_cadregressionworkerthreadrunnerengine
description: "CAD Regression Worker Thread Runner"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADRegressionWorkerThreadRunnerEngine.ts
  sourceHash: cbb4e053
  loc: 829
aliases: CADRegressionWorkerThreadRunnerEngine
---

# CADRegressionWorkerThreadRunnerEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADRegressionWorkerThreadRunnerEngine.ts` (829 LOC)

## What it is
CAD Regression Worker Thread Runner

```
CADRegressionWorkerThreadRunnerEngine — U-CINF04.x (CAD-INFRA-MS0)

Production `TestRunner` implementation for `CADRegressionTestOrchestratorEngine`.
Spawns a pool of Node `worker_threads`, dispatches FileTasks to idle workers,
collects results, and honours the orchestrator's per-file AbortSignal by
forcibly terminating workers that don't ack the abort within a short grace
window. Workers that exit unexpectedly are dropped from the pool (not reused).

Closes U-CINF04.x-WORKER-THREAD-RUNNER (envelope `deferred_units`), which was
carved out of U-CINF04 because the orchestrator's design accepts an injected
`TestRunner` — keeping orchestrator unit-testable while letting the production
worker-pool ship on its own cadence.

Design:
  - Lazy pool: workers are spawned on demand up to `poolSize` (default 8) and
    reused for subsequent tasks. Saves the 100-200 ms worker startup cost over
    20 
```


## Exports
`WorkerThreadRunnerOptions` (interface), `WorkerPoolStats` (interface), `CADRegressionWorkerThreadRunnerEngine` (class), `createCADRegressionWorkerThreadRunner` (function)

## Key methods
`resolve`, `reject`, `getCapabilities`, `validate`, `run`, `finalize`, `errorResult`, `clearTimeout`, `getPoolSize`, `getStats`, `terminate`

## Depends on
[[reference_code_engine_baseengine]] | [[reference_code_engine_iengine]] | [[reference_code_engine_cadregressiontestorchestratorengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADRegressionWorkerThreadRunnerEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADRegressionWorkerThreadRunnerEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADRegressionWorkerThreadRunnerEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
