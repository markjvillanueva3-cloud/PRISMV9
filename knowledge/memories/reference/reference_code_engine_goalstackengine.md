---
name: reference_code_engine_goalstackengine
description: "Hierarchical goal management for session awareness"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/GoalStackEngine.ts
  sourceHash: 1bbfe97b
  loc: 234
aliases: GoalStackEngine
---

# GoalStackEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/GoalStackEngine.ts` (234 LOC)

## What it is
Hierarchical goal management for session awareness

```
GoalStackEngine — Hierarchical goal management for session awareness

Phase 0.13 U-SAW3 from UNIVERSAL-SKILLS-SCRIPTS-HOOKS-PLAN. A hierarchical
goal stack where each goal may have sub-goals. Hooks call `topN()` on
UserPromptSubmit to inject the top 5 active goals into the next prompt so
the session cannot drift. Design intents:

  - Pure in-memory structure. Serialize via `toJSON()` / `fromJSON()`.
  - Deterministic IDs from a monotonically-increasing counter so tests can
    rely on the IDs produced during a sequence.
  - Goals are never mutated in place; completion / abandonment updates
    status+timestamp fields and leaves the tree structure intact so the
    insights ledger can reconstruct the timeline later.

@module engines/GoalStackEngine
@milestone PP-0.13-U-SAW3
```


## Exports
`GoalStatus` (type), `Goal` (interface), `PushOptions` (interface), `TopNEntry` (interface), `GoalStackEngine` (class), `goalStackEngine` (const)

## Key methods
`push`, `complete`, `abandon`, `completeCascade`, `current`, `topN`, `tree`, `all`, `activeCount`, `clear`, `toJSON`, `fromJSON`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/GoalStackEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find GoalStackEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/GoalStackEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
