---
name: reference_code_engine_cadexecutionoutcomebusengine
description: "U-CADC-LP01 / CAD-COMPLETE-MS0"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/CADExecutionOutcomeBusEngine.ts
  sourceHash: bc338060
  loc: 261
aliases: CADExecutionOutcomeBusEngine
---

# CADExecutionOutcomeBusEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/CADExecutionOutcomeBusEngine.ts` (261 LOC)

## What it is
U-CADC-LP01 / CAD-COMPLETE-MS0

```
CADExecutionOutcomeBusEngine — U-CADC-LP01 / CAD-COMPLETE-MS0

CAD-specific outcome bus. Dual-channel emitter:
  1. Durable channel — every published outcome is also forwarded to the
     universal OutcomeCaptureBus (cad shard, kind=cad_execution_outcome)
     so downstream offline learners (LP03/LP04 + neural-ledger pipelines)
     can re-read history.
  2. In-process channel — synchronous fan-out to registered subscribers
     (LP02 collector, LP04 propagator) so real-time learners react
     without polling the JSONL shard.

Design invariants (per duplication-guard review against existing
OutcomeCaptureBus + FeedbackBus):
  1. NEVER swallow subscriber errors silently — each handler is isolated
     with try/catch, errors recorded in stats.handlerErrors but other
     handlers still fire. R12: fail-loud via stats, not via exception.
  2. NEVER block the producer — publish() returns imm
```


## Exports
`CADExecutionOutcome` (interface), `CADOutcomeSubscriber` (type), `PerAdapterCounter` (interface), `BusStats` (interface), `PublishResult` (interface), `CADExecutionOutcomeBusEngine` (class), `cadExecutionOutcomeBusEngine` (const)

## Key methods
`publish`, `validateOutcome`, `fn`, `subscribe`, `listSubscribers`, `getStats`, `reset`

## Depends on
[[reference_code_engine_outcomecapturebusengine]]

## Where it lives
- **Source:** `mcp-server/src/engines/CADExecutionOutcomeBusEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find CADExecutionOutcomeBusEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/CADExecutionOutcomeBusEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
