---
name: reference_code_engine_neuralroutingengine
description: "Learned routing decisions over the scrutiny ledger."
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/NeuralRoutingEngine.ts
  sourceHash: 1c50264a
  loc: 384
aliases: NeuralRoutingEngine
---

# NeuralRoutingEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/NeuralRoutingEngine.ts` (384 LOC)

## What it is
Learned routing decisions over the scrutiny ledger.

```
NeuralRoutingEngine — Learned routing decisions over the scrutiny ledger.

Milestone: OCTOPUS-NEURAL-MS0 / U-OCN03.

Reference: Feng et al. "GraphRouter: A Graph-based Router for LLM Selections"
(2410.03834). Replaces hardcoded if/else routing chains ("if engine-edit then
3-of-3, else…") with a learned topology over historical scrutiny outcomes.

Design choice — k-NN over GNN: The atomized spec calls for "a lightweight
GNN." A full GNN requires tensor deps and a training pipeline. Instead we
implement k-NN on hand-engineered features (change-class, file-types,
peer-count, files-count). This is equivalent to "learned topology" in
GraphRouter's sense for the small N regime PRISM ledgers live in (< 1K
entries for the next year). When the ledger crosses ~10K entries we can swap
the engine internals for a real GNN; the public API stays stable.

Cold-start: when the ledger has < COLD_START_THR
```


## Exports
`Quorum` (type), `Tentacle` (type), `RoutingRequest` (interface), `RoutingDecision` (interface), `LedgerEntry` (interface), `NeuralRoutingConfig` (interface), `NeuralRoutingEngine` (class), `neuralRoutingEngine` (const)

## Key methods
`route`, `setLedgerForTesting`, `resetCache`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/NeuralRoutingEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find NeuralRoutingEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/NeuralRoutingEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
