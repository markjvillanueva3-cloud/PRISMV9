---
name: reference_code_lib_fleetrecurringpatterns
description: "fleet-recurring-patterns.mjs -- PURE cross-session pattern aggregator for fleet hygiene."
metadata:
  type: reference
  kind: code-lib
  galaxy: unassigned
  sourcePath: scripts/lib/fleet-recurring-patterns.mjs
  sourceHash: 16635b8d
  loc: 330
aliases: fleet-recurring-patterns.mjs
---

# fleet-recurring-patterns.mjs -- Lib Module SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `scripts/lib/fleet-recurring-patterns.mjs` (330 LOC)

## What it is
```
fleet-recurring-patterns.mjs -- PURE cross-session pattern aggregator for fleet hygiene.

Detects patterns that only emerge ACROSS many sessions/slots, which no single-session
tool surfaces: recurring regression classes (the same bug re-broken under different SHAs),
scope-focus (which [SCOPE] is consuming the most units), fleet-wide citation frequency
(which wiki/memory nodes the whole fleet leans on), and fix-then-rebreak loops.

This module is PURE: no filesystem, no process spawning, no clock. All IO -- reading
handoffs / git log / memory dir -- lives in the CLI wrapper (fleet-recurring-patterns-digest.mjs),
which feeds raw strings in and writes the rendered digest out. Keeping the analysis pure
makes it hermetically testable with real reference values (no mocks).

Karpathy discipline: CLASSIFY (text-aggregation/clustering) -> TECHNIQUE (normalize ->
token-set -> union-find containmen
```


## Exports
`DEFAULT_MIN_REGRESSION_HITS` (const), `DEFAULT_MIN_SCOPE_HITS` (const), `DEFAULT_MIN_CITATION_HITS` (const), `DEFAULT_FINGERPRINT_TOKEN_LIMIT` (const), `DEFAULT_FINGERPRINT_MIN_TOKEN_LEN` (const), `DEFAULT_TOP_LIMIT` (const), `DEFAULT_CITATION_RENDER_LIMIT` (const), `SAMPLE_CAP_PER_GROUP` (const), `SAMPLE_MAX_LEN` (const), `DEFAULT_JACCARD_THRESHOLD` (const), `fingerprintTokens` (function), `regressionFingerprint` (function), `clusterRegressions` (function), `extractScope` (function), `tallyScopes` (function), `extractWikiLinks` (function), `tallyCitations` (function), `detectFixRebreakLoops` (function), `buildDigest` (function), `renderDigest` (function)

## Key methods
_(none extracted)_

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `scripts/lib/fleet-recurring-patterns.mjs`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Lib Module

## How to query it
Recall by meaning via `/system-viz find fleet-recurring-patterns.mjs` or `prism_session:master_index_query`; open the source at `scripts/lib/fleet-recurring-patterns.mjs`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
