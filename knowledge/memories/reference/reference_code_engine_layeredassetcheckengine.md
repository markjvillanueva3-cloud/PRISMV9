---
name: reference_code_engine_layeredassetcheckengine
description: "Composite gate: fuzzy-name + semantic-embedding"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/LayeredAssetCheckEngine.ts
  sourceHash: 2015aae9
  loc: 203
aliases: LayeredAssetCheckEngine
---

# LayeredAssetCheckEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/LayeredAssetCheckEngine.ts` (203 LOC)

## What it is
Composite gate: fuzzy-name + semantic-embedding

```
LayeredAssetCheckEngine — Composite gate: fuzzy-name + semantic-embedding

Hooks that fire before asset creation (e.g. `ai-duplication-guard`) need a
single "ok / warn / block" answer. This engine orchestrates two layers
without modifying either backing engine:

  Layer 1 — fuzzy name check. Asks an injected name-check callback
            whether the candidate collides with any known asset by name
            (typically DuplicationGuardEngine). On a hard match, returns
            `block` immediately.

  Layer 2 — semantic embedding check. If the name layer is clear, asks
            an injected band-check callback (typically EmbeddingGuardEngine)
            for a green / yellow / red band on the description.

The result combines both signals into a single decision the hook can act
on, along with the full reasoning trail so the user sees *why*.

@module engines/LayeredAssetCheckEngine

```


## Exports
`LayeredDecision` (type), `NameCheckInput` (interface), `NameCheckOutcome` (interface), `BandCheckInput` (interface), `BandCheckOutcome` (interface), `LayeredCheckInput` (interface), `LayeredCheckResult` (interface), `NameChecker` (type), `BandChecker` (type), `LayeredAssetCheckOptions` (interface), `LayeredAssetCheckEngine` (class)

## Key methods
`check`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/LayeredAssetCheckEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find LayeredAssetCheckEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/LayeredAssetCheckEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
