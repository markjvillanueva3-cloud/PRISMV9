---
name: reference_code_engine_hookorchestratorengine
description: "Deterministic hook ordering per tool event"
metadata:
  type: reference
  kind: code-engine
  galaxy: unassigned
  sourcePath: mcp-server/src/engines/HookOrchestratorEngine.ts
  sourceHash: e0deb7ad
  loc: 224
aliases: HookOrchestratorEngine
---

# HookOrchestratorEngine -- Engine SUMMARY

> Auto-generated by `scripts/code-to-vault.mjs` (CODE-VAULT-BRIDGE, slot:sierra). Re-run to refresh.
> generatedAt: 2026-07-03T03:00:00Z  |  source: `mcp-server/src/engines/HookOrchestratorEngine.ts` (224 LOC)

## What it is
Deterministic hook ordering per tool event

```
HookOrchestratorEngine — Deterministic hook ordering per tool event

Phase 0.16 U-OP2 from UNIVERSAL-SKILLS-SCRIPTS-HOOKS-PLAN. PRISM's hook set
is growing fast (~86 per family). Without a formal ordering scheme,
hook execution becomes position-dependent, which breaks reproducibility.

Each hook declares:
  - phase    (PreTool / PostTool / UserPromptSubmit / SessionStart / …)
  - priority (lower runs first within a phase)
  - dependsOn (hook ids that must run before this one)
  - mutex    (hook ids that MUST NOT co-run — guard conflicting writes)

The engine:
  1. Topologically sorts hooks by dependsOn (cycle → error).
  2. Within each topo layer, stable-sorts by (priority, id).
  3. At execution, short-circuits on any hook that returns `{ block: true }`.
  4. Mutex checking catches co-scheduled hooks that cannot run together.

No I/O. The engine is a planner + runner; the hook handlers 
```


## Exports
`HookPhase` (type), `HookDefinition` (interface), `HookContext` (interface), `HookDecision` (interface), `HookHandler` (type), `PlanResult` (interface), `RunResult` (interface), `HookOrchestratorEngine` (class), `hookOrchestratorEngine` (const)

## Key methods
`register`, `unregister`, `list`, `getHooksForEvent`, `isHookEnabled`, `plan`, `run`, `pushReady`

## Depends on
_(no sibling-engine imports)_

## Where it lives
- **Source:** `mcp-server/src/engines/HookOrchestratorEngine.ts`
- **Galaxy:** _(cross-cutting / unassigned)_
- **Kind:** Engine

## How to query it
Recall by meaning via `/system-viz find HookOrchestratorEngine` or `prism_session:master_index_query`; open the source at `mcp-server/src/engines/HookOrchestratorEngine.ts`. This note IS the brain connection -- a vault memory node under `knowledge/memories/reference/`, graphed by `generate-memories-atomic` and indexed by `build-memory-index-sidecar`. Related: [[feedback_read_full_content_not_titles]].
