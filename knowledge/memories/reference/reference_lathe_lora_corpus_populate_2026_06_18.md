---
name: reference_lathe_lora_corpus_populate_2026_06_18
description: First REAL per-machine LoRA corpus population (lathe) + the quality-gate-at-corpus-layer pattern + the dedup-before-counting lesson that 3-of-3 scrutiny caught
type: reference
source: prism-memory
synced: 2026-06-27T20:30:46.637Z
aliases: reference_lathe_lora_corpus_populate_2026_06_18
---


**U-LORA-LATHE-CORPUS-POPULATE (slot:india, 2026-06-18, commits 7ebf954622 + 25d4a6641b).** The FIRST real per-machine LoRA rows to reach the GPU corpus. Lathe is the one machine whose real training data exists in-repo: `LatheLoRADatasetBuilderEngine` (whiskey's, a 716-line async archive-scanner -- NOT a RawJob wrapper, so excluded from `build-machine-lora-datasets.mjs`) parses real `.MIN` programs from `H:/PRISM/JM DIE/CNC LATHE` (16,558 .MIN found). Built `scripts/build-lathe-lora-dataset.ts` (run via `npx tsx` -- imports the engine at SOURCE because the compiled dist was stale, src last committed 2026-06-16; R12 don't run stale logic). 500 programs -> 1060 raw examples -> 735 gate-kept -> **135 UNIQUE** rows, galaxy-tagged "lathe", registered as `machine-lathe-lora` (advisory:true, w=0.5). 12/12 gate tests, 2-of-2 scrutiny PASS. **THEN grown to the FULL archive (commit 641f90a153): 16,558 programs -> 34,449 raw -> 23,366 gate-kept -> 1,657 UNIQUE rows; fleet corpus 1604 -> 3126, lathe galaxy 153 -> 1675.** Template-collapse ceiling: 23,366 gate-kept dedup to only 1,657 distinct (instruction,output) pairs (~7%) -- the unique training signal is template-bound, not program-bound, so growing the cap past the full archive yields nothing. **REBALANCE FLAG (R12) -- now RESOLVED via an opt-in cap:** lathe is ~54% of the 3126-row corpus (w=0.5 down-weights it, but a fine-tune would skew lathe-heavy) -- a transient artifact of lathe being the ONLY machine with in-repo real data. FIX shipped same session (U-LORA-CORPUS-GALAXY-CAP, commits f02a7a3582 + 0c18eafcbf): `assemble-fleet-lora-corpus.mjs --cap-per-galaxy=N` (+ `assembleCorpus(inv,{capPerGalaxy})`) caps each galaxy at N, WEIGHT-AWARE (keeps verified w=1.0 over advisory w=0.5; ties keep source order), untagged/cross-cutting uncapped, default 0=OFF (zero behavior change). Live: `--cap-per-galaxy=300` -> lathe 1675->300 (54%->17%), corpus 3126->1751, 34 galaxies preserved, verified rows preserved (325). 33/33 tests, 2-of-2 reviewers PASS. RUN THE FINE-TUNE WITH `--cap-per-galaxy=~300` until other machine-owners' real actuals catch up.

## GPU-VALIDATED end-to-end (2026-06-18, reach-destination PROVEN on Blackwell)
The corpus work's ultimate destination is a trained adapter; validated the full link with a bounded 5-step smoke fine-tune on the rebalanced corpus. Wrote `state/shared/lora/fleet-lora-combined-capped.jsonl` (1751 rows, `assemble-fleet-lora-corpus.mjs --cap-per-galaxy=300 --out <path>`), then `H:/Tools/blackwell-gpu-venv/Scripts/python.exe scripts/fleet_lora_train.py --corpus <capped> --rslora --rank 32 --alpha 32 --max-steps 5 --out <smoke>`: **CUDA OK (RTX PRO 6000 Blackwell), corpus loaded 1751 rows / 0 invalid / 34 galaxies / 12 sources / avgOutputChars 903.6, trained train_loss ~1.37, saved a valid 323MB rsLoRA r=32 adapter** (adapter_config.json + adapter_model.safetensors + tokenizer + checkpoints). Stack: torch 2.11.0+cu128, peft 0.19.1. Smoke adapter then deleted (throwaway). **FULL PRODUCTION RUN (launch-ready, de-risked):** `python scripts/fleet_lora_train.py --corpus state/shared/lora/fleet-lora-combined-capped.jsonl --rslora --rank 32 --alpha 32 --max-steps 400 --out state/shared/lora/adapters/fleet-rslora-r32-<date>` -- run FRESH (NOT --resume; the corpus changed) on the CAPPED corpus, via the reaper-immune scheduled-task pattern (`run-rslora-r32-train.ps1`; it kills in-session python -- multi-hour runs MUST be a scheduled task). NOTE the existing launcher uses the UNCAPPED default corpus + `--resume` + the prod out -> correct it (capped path, fresh, dated out) before the real run. **EXIT-255 CAVEAT (R12):** fleet_lora_train.py exits 255 AFTER a successful save (post-save bitsandbytes/CUDA Windows teardown non-zero) -- the truth signal is the `[fleet-lora] adapter + metadata saved ... (final_loss=...)` log line + the written safetensors, NOT $LASTEXITCODE; the scheduled-task wrapper's exit-code log will misread success as failure until that teardown is hardened.

**PATTERN -- quality-gate-at-the-corpus-layer (india owns corpus quality; defer engine fixes to the owner):** whiskey's `generateExamples` emits low-signal/broken pairs, so india curates at the producer (NEVER edits whiskey's engine -- defer-domain-to-domain-slot): `isQualityExample()` drops (1) the `sequence` op-order template (hallucinates ops not in the input -- a drilling->roughing request answered with "Threading after roughing"), (2) empty `code_review` analysis (header-only .MIN -> empty "...performs:" bullets), (3) HOLLOW outputs whose only content is generic boilerplate (`substantiveLines()` strips a BOILERPLATE_LINE set, requires >=1 program-specific bullet -- catches the tips=0 speed_feed shape), (4) sub-floor outputs. Source bugs (empty-analysis, sequence-hallucination, AND the `/F([\d.]+)/` feed-parser that grades a non-feed F-token "very heavy") reported to whiskey on AGENT_CHAT for the real fix.

**LESSON (3-of-3 arm-B caught my overstatement -- R12 metrics-gated):** DEDUP before reporting row counts. Templated builders collapse N programs onto a few distinct strings -- 735 gate-kept rows were only **135 unique** (`dedupRows` by (instruction,output)); claiming "735 curated" overstated the real training signal ~5.4x. AND templated-but-real-grounded data is **advisory:true (0.5 weight)**, NOT full verified weight (R7: don't blend trust levels) -- the assembler down-weights distillation-grade sources (synthesis/bridge/outcome) the same way. The first commit (7ebf954622) FAILed arm-B on both; the fix (25d4a6641b) closed all 3 P1s. Sibling: [[reference_lora_dataset_family_wire_2026_06_18]] (the 8 RawJob-wrapper builders + their corpus producer). The other 8 machines still emit 0 rows until their owning slots drop real `state/shared/training/machine-jobs/<type>.jsonl` actuals (coordinated on the bus).
