# Lost Blob — 2026-04-21

During the git-corruption repair (CADC34 / tree `18b53fe1232524bb3adafaacf7131ddfbb972005` at `mcp-server/src/hooks/`), one file became unrecoverable:

## Missing file
- **Path**: `resources/fusion360/prism-test-runner/index.js`
- **Lost blob OID**: `b55d73c37c143f9d2df48193ec2a7e3ecb3a6346`
- **Introducing commit**: `963f8fedf CAM-EXHAUST-MS0/U-CAMTEST02: Fusion 360 JS Add-In In-Host Test Runner Skeleton` (2026-04-20)
- **Recovery attempted**: `git show 963f8fedf:resources/fusion360/prism-test-runner/index.js` → `fatal: bad object` (the introducing commit's tree also references corruption; another missing tree `91c1d45f8e9f8d8eb67ad15520b50cec06b1b42d` flagged during `git log`)
- **State on disk**: file not present — only `manifest.json` (90 bytes) remains in the directory
- **State in git**: removed from index during repo-fatten; committed as deleted in `07221c99a`

## What it was
Fusion 360 JS Add-In index.js skeleton — the shop-side entry point for the in-host test runner whose PRISM-side companion is `mcp-server/src/engines/Fusion360InHostRunnerEngine.ts`. Companion engine is intact.

## Remediation
Regenerate from the engine's contract + Fusion 360 Add-In template. Implementer should:
1. Read `mcp-server/src/engines/Fusion360InHostRunnerEngine.ts` for the expected wire protocol
2. Read `resources/fusion360/prism-test-runner/manifest.json` for the Add-In declarations
3. Author a new `index.js` matching both
4. Commit under `CAM-EXHAUST-MS0/U-CAMTEST02-RESTORE: regenerate lost test-runner skeleton`

Not urgent — this file is a future-unit skeleton, not on the current critical path.

## Scope of corruption
Two confirmed bad tree OIDs in history (both unrecoverable):
- `18b53fe1232524bb3adafaacf7131ddfbb972005` — `mcp-server/src/hooks/` (disk content intact; tree re-materialized with new OID during fatten)
- `91c1d45f8e9f8d8eb67ad15520b50cec06b1b42d` — some subtree under an older commit; surfaced during `git log --all -- resources/fusion360/prism-test-runner/index.js`. Not actively blocking but means `git log --all` on some paths will emit errors.

All current refs now point to clean trees — new commits, status, diff, push should all work. Older history paths that traverse through `91c1d45f` will still error. Defer deeper cleanup until a maintenance window.
