# india session 928a8226 (2026-06-10, 71MB, spine 701KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `c593b096fb` – fixed Ollama pull script (em‑dash → hyphen), added synergy routing for gpt‑oss:120b, set `DEFAULT_NUM_PREDICT=1024` in `ask‑ollama.mjs`.  
- `13017de764` – created `fix-hookify-frontmatter.mjs`, removed dangling hook references.  
- `29f2d177f6` – generated full node‑by‑node gap‑map (`SYSTEM‑SYNERGY‑GAPMAP‑2026‑06‑08.md`).  
- `8550436c8a` – corrected CAG‑router label from phantom `qdrant://prism‑memory` to real collections.  
- `c9fe32d8b2` – fixed tribal‑embed index OOM (buffered loader, 8 GB heap flag) and added 2000‑char clamp for nomic embeddings; added empty‑text skip guard.  
- `182788232a` – cap‑safe loader for `tribal-embed-index.json`; restores PSN leg #5 after V8 string‑cap crash.  
- `5467482be4` – doc‑reflect updates (CLAUDE.md regression + wiki lesson).  
- `c994bf3aad` – memory & wiki lesson docs updated.  
- `fb314a6fd1` – Docker‑service‑health CLI; 5/5 tests, detects downed containers (qdrant, postgres, grafana, prometheus).  
- `14b8b383a2` – Stop‑hook wiring for docker‑service‑health in both settings.json files.  
- `35e01ca5f3` – singleton‑service‑guard CLI; `--fix` now reap‑all + start (MCP).  
- `d3ed737c23` – wired guard into docker‑service‑health Stop hook.  
- `e2081e0780` – U‑MCP‑FIXSTART: pure `fixPlan`, `startService`; 14/14 tests, live‑validated on real outage.  
- `d22681f5d2` – cosmetic tag fix (U‑MCP‑KEEPALIVE → U‑MCP‑FIXSTART).  
- `006c435ddb` – corrected MCP restart docs (`TRANSPORT=http node dist/index.js`).  
- `bee977a004` – added `NODE_OPTIONS=--max-old-space-size=8192` to reranker spawn; restores PSN leg #5 after index grew to 168 MB.  
- `6ca2f6afd2` – MCP boot‑heap OOM fix (`:3100` outage).  
- `ed6662f45e` – U‑MCP‑CMDMATCH‑FIX (slash‑agnostic regex, daemonCount visibility).  
- `29fb555f13` – U‑HOOK‑UNREG‑PROTOCOL‑FIX (JSON‑stdout protocol for stop‑regression bundle).  
- `U-FTH-WIKI-LESSON` – wiki entry for the cry‑wolf/fleet‑task‑health fix.  
- `tribal-by-domain-inject` – wiki promotion of tribal‑by‑domain injection.  
- `U-QP-COST-BRIDGE-WIRING-TRUTH` – cost‑bridge hooks consolidated into single router.  
- `U-QP-AWARENESS-GEN-ROUTER-AWARE` – restored and router‑aware quoting‑awareness generator.  
- `U-QP-COST-SAVINGS-TEST` – 13/13 vitest cases for CostSavingsTrackerEngine (injectable store path).  
- `OLLAMA-DISPATCHER-FIX` – updated cascade defaults to installed Blackwell models; extended source‑lock to include dispatchers.  
- `U-BW-DISPATCHER-SCAN` – repointed cascade defaults to installed Blackwell tiers, extended source‑lock guard to dispatcher tree.  
- `U-BW-DISPATCHER-SCAN-WIKI` – wiki lesson documenting blind‑spot fix.  
- `U-PSN-SNAPSHOT-REFRESH` – regenerated stale PSN snapshot (51 k nodes); confirmed 11 real P0 zero‑ref pairs, scoped cross‑substrate wiring to Sierra.  
- `U-QP-DIGITALTWIN-TEST` – added R9 reference‑value test for DigitalTwinEstimator algorithm.  
- `U-QP-MARGIN-FLOOR-GATE` (incl. SHOPCONFIG) – margin‑floor logic in QuoteEstimatorEngine, default 20 %, sourced from ShopConfig; WARN+flag on below‑floor margins.  
- `ec597dbcb3` – margin‑floor dispatcher round‑trip test (7/7).  
- `87d5c4bf9a` – margin‑floor UI banner (6/6, tsc‑clean).  
- `4b0980c56a` – AI‑substrate routing matrix coverage 4/5→5/5 + completeness invariant (27/27).  
- `62b114cb7b` – web test ResizeObserver polyfill.  
- `8258918f78` – quote‑pages test repair: fixed mock contract, added null‑guards, failure‑mode test (13/13).  
- `44fab1be37` – fleet‑synergy dispatch map (`SYNERGY‑GOAL‑STATUS‑2026‑06‑09.md`).  
- `4c12a75a8d` – U‑QP‑CLOSED‑LOOP‑PROVENANCE‑GATE (synthetic/placeholder outcomes blocked).  
- `d294957c4d` – U‑QP‑OUTBOUND‑PROMOTE‑GATE (real sold‑price alignment, fail‑closed on drift).  
- `bf10035ec0` – U‑QP‑CALIBRATION‑FRESHNESS‑PREFLIGHT (warn >24 h stale factors; hard cutoff to raw FMV).  
- `d447cee202` – U‑QP‑BLUEPRINT‑OCR‑BRIDGE‑ADAPTER (adapts OCR analysis to bridge input, fixes silent GD&T drop).

**DECISIONS**  
- Adopt systematic debugging: root‑cause investigation before patching.  
- Convert all malformed hookify files instead of altering parser; bulk transform applied to 110 files.  
- Wire engines per galaxy owner; triage 90 unwired engines (14 with suggested dispatchers, 76 unknown).  
- Keep academy tribal routing fallback (lima → “general”) intentional; no immediate fix.  
- Use local Ollama on RTX‑PRO 6000 Blackwell (96 GB VRAM) with `OLLAMA_MAX_LOADED_MODELS=3×GPU`.  
- Route gpt‑oss:120b only for reasoning tasks; use qwen2.5‑coder:1.5b for cheap tier.  
- Default `num_predict=1024` to avoid empty responses from harmony‑format models.  
- Batch workflow agents (≤3 concurrent) to stay within Anthropic rate limits.  
- Replace string‑based index loading with buffered, streaming JSON loader; add 2000‑char clamp for nomic embeddings.  
- Add NODE_OPTIONS `--max-old-space-size=8192` to reranker spawns; floor MCP spawn heap ≥4096 MB.  
- Implement singleton‑service‑guard (`--fix`) to reap wedged daemons and start clean MCP daemon; wire into Stop hook.  
- Defer Qdrant tribal migration until source corpus rebuild completes; use existing `populate‑tribal‑vault.mjs`.  
- Partition registered‑but‑disabled tasks in fleet‑task‑health to silence intentional HW‑migration freezes.  
- Consolidate 16 cost‑bridge hooks into single router to avoid double execution and false orphan reports.  
- Extend source‑lock to scan dispatcher code for retired model tags; update cascade defaults to current Blackwell roster.

**OPERATOR DIRECTIVES**  
- “synergize ollama … docker, qdrant, obsidian vault app, PSN, /system-viz, prism galaxies, prism front end and back end build, claude.md, memories, wiki, tribal knowledge, prism awareness, gsd, tdd, skills, scripts and hooks | goal clear: everything wire, tested, validated and synergized across the entire system.”  
- “continue with charlie domain work” (override fleet‑scope stop noise).  
- “utilize ollama for better task efficiency when viable” – applied to quoting galaxy audit; no high‑ROI offloads found.  
- `/startup-charlie — slot-locked /startup` (force‑take charlie slot, bind handoff).  
- `U-QP-BLUEPRINT-OCR-BRIDGE-ADAPTER loop` (build the adapter unit).  
- “utilize ollama to offload tasks we have confidence in that it 100% accomplish” – applied where appropriate.

**FINDINGS/BUGS**  
- Hookify rule files missing YAML frontmatter → 110 warnings; one file had charmap decode error.  
- Dangling module references caused 6 “Cannot find module” errors in `settings.json`.  
- Scheduled‑task WARN due to intentional hardware/drive migration freeze (4 disabled tasks).  
- 90 built engines unwired; 14 with suggested dispatchers, 76 unknown.  
- Academy tribal routing falls back to “general”; `getDomainTokens` never emits an academy token.  
- Parse error in `ollama-resilient-pull.ps1` prevented download of `gpt‑oss:120b`; pull resumed after fix and completed.  
- Synergy bug: `routeModelForTask` selects `gpt‑oss:20b` instead of `gpt‑oss:120b` for best tier.  
- V8 hard max string length (≈512 MiB) caused JSON.parse on 537 MB tribal index to throw; fixed with cap‑safe loader and buffered reader.  
- Default Node heap (~2 GB) OOMs when parsing >150 MB JSON; mitigated by `NODE_OPTIONS=--max-old-space-size=8192`.  
- Fail‑open logic in `tribal-embed-index.mjs` returned empty index on parse error, leading to a 1‑entry clobber after auto‑reconnect.  
- Duplicate/wedged MCP daemons caused port‑bind conflicts; fixed by singleton guard (`--fix`).  
- MCP restart command documented incorrectly; corrected to use `TRANSPORT=http`.  
- ResizeObserver missing in jsdom caused hidden frontend test failures; polyfill added.  
- `quoteHistory` mock violated `InstantQuoteHistory` contract → render crash; corrected.  
- Synthetic outcome promotion allowed live calibration write; blocked by provenance gate.  
- OCR‑$1 noise (~65 % of line ext_prices at $1.005) caused false drift blocks in outbound promote gate.  
- ShopDispatcher TS errors reduced from 12 to 10 after fixes.  
- System‑viz `find` OOM under heavy load; decision not to self‑bump due to fork‑storm risk.

**AI‑SYSTEM SPECIFICS**  
- **Engines / Hooks**:  
  - PlaywrightAutomationEngine, MillPrintToProgramEngine (superseded), QuotingClosedLoopEngine, SFCCompareEngine, LocalEmbeddingEngine, QdrantVectorStoreEngine, SemanticAssetIndexEngine, MITCourseIntegrationEngine, WEDMLoRADatasetBuilderEngine, JMCustomerVendorDatabaseEngine.  
  - CostSavingsTrackerEngine (`~/.prism/savings.json` persistence).  
  - QuoteEstimatorEngine (margin‑floor gate, SHOPCONFIG source).  
  - ClosedLoopVerifierEngine, ActualCostEngine, AISystemRouterEngine, Dispatcher, OCRBridgeAdapter, QuotingClosedLoopProvenanceGate, OutboundPromoteGate, CalibrationFreshnessPreflight.  
- **Models & Defaults**:  
  - `gpt‑oss:120b` (reasoning, harmony format), `qwen2.5‑coder:1.5b/32b`, `gpt‑oss:20b`, vision ensemble (`qwen3-vl`, `qwen2.5vl`, `llama3.2-vision`, `moondream`), `nomic-embed-text`.  
  - All pulled on Blackwell GPU (96 GB VRAM).  
- **Metrics**:  
  - GPU inference: gpt‑oss ≈114 tok/s; qwen2.5‑coder:1.5b ≈221 tok/s.  
  - Margin‑floor default = 20 % (configurable via ShopConfig).  
- **Datasets / Corpora**:  
  - tribal‑embed-index.json (~160 MB, 10 555 entries after cap‑safe load; target 33 639).  
  - Qdrant collections: `prism_engines` (3 866 pts), `prism_skills` (241), `prism_formulas` (32).  
  - PSN snapshot (51 k nodes, 11 real P0 zero‑ref pairs).  
- **Deploy Gates**: singleton‑service‑guard (`--fix`), stop‑regression bundle protocol, system‑viz query script, provenance gate, outbound promote gate, calibration freshness preflight.

**OPEN THREADS**  
- Re‑embed lost ~29 K tribal entries (currently 10 555 → target 33 639); consider WRITE‑side sharding or Qdrant migration.  
- Implement empty‑text skip guard in embedder; driver still running (~6,400 wiki files, ≈84 % success).  
- Confirm tribal reranker fully functional with new buffered loader; monitor coverage growth.  
- Deploy scheduled MCP keep‑alive task (singleton guard `--fix`).  
- Finalize Qdrant tribal migration strategy (deferred until source corpus rebuild).  
- Verify all galaxy‑specific bridges wired/exempted per WIRE‑EXEMPT convention.  
- Monitor system‑viz OOM under heavy load; explore alternative optimizations.  
- Complete U‑QP‑SEND‑QUOTE‑WIRE (generateQuotePdf/quote_explain_render) pending budget constraints.  
- Clean OCR noise at source before outbound promote gate can go live.  
- Finish `AccountingHardeningEngine` integration (ERP/QuickBooks feed).  
- Continue closed‑loop provenance gate, outbound promote gate, calibration freshness preflight; all tests green.
