# india session 9dc60ef7 (2026-06-19, 20.8MB, spine 120KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `dd0b90b0f0` – LLMEngine rewritten to use Ollama first (qwen2.5‑coder 32B) with Claude backup; added timeout, cache‑key hashing, system override, offline‑honesty handling. 18/18 tests green.  
- `42a6108e07` – Optional `system` override in LLMQuery; cache key now hashes full system+prompt string. 32/32 tests green.  
- `9cce7bb894` – Added `routes/knowledge.ts`, exposing 14 `/api/v1/knowledge/*` endpoints to `prism_knowledge`. 26 tests green.  
- `9faccd3cea` – ManusATCSBridge migrated to free LLMEngine; removed `hasValidApiKey`; added offline‑failure marking. 5 tests green.  
- `8738d2eed5` – manusDispatcher and ralphDispatcher migrated similarly; added throw‑on‑offline, provenance tracking. 10+ tests green.  
- `b5f0dd5006` – ralphDispatcher (throw‑on‑offline, provenance).  
- `9b371ffd1c` – atcsDispatcher (throw‑on‑offline, provenance).  
- `38fde7cc48` – Ollama‑first LLMEngine.query substrate (qwen2.5‑coder 32B).  
- `3f7df39916`, `a879e02a0e` – Vision substrate keystone (`llmEngine.queryVision`, qwen2.5vl 7B, image passthrough).  
- `0eb8353d24` – VisionActionAnalyzerEngine migrated to use `queryVision`.  

**DECISIONS**  
- Provider ladder: Ollama (free) → Claude (paid) → deterministic offline; `prefer` config (`auto`, `ollama`, `claude`).  
- Capability escalation: refusal‑leading regex + length floor for high complexity, injectable adequacy predicate.  
- Cache key: FNV‑1a hash of full system+prompt string plus complexity/min_answer_chars to avoid collisions.  
- Offline honesty: results marked `{status:"failed", error:"offline"}`; `model` field reports real provider (`offline`).  
- System override passed through LLMEngine and cached separately.  
- Vision engines: added `queryVision` path (local qwen2.5vl/qwen3‑vl) with Claude vision backup; dropped Anthropic SDK.  

**OPERATOR DIRECTIVES**  
- Continue autonomous development using harnesses, loops, crons, Ollama, Obsidian vault, Hermes, Octopus.  
- Finish all India tasks before moving to backend/frontend focus (web/phone app).  
- Use Ollama models for CAD/CAM drawing and CNC program features; free at launch.  
- Keep Claude as backup if Ollama cannot handle or task too complex.  
- Enable self‑compact automatically when needed; no manual prompts required.  

**FINDINGS/BUGS**  
- `/api/v1/knowledge` router missing → 404s for frontend dashboards (fixed).  
- Frontend `fleet/status` contract mismatched backend data; mapping corrected.  
- LLMEngine had no timeout → hanging on stalled Ollama calls (added bounded timeout with fallback to Claude).  
- `hasValidApiKey` gates blocked free path in multiple dispatchers (removed/replaced with offline‑honesty logic).  
- Cache key omitted complexity/min_answer_chars → cross‑provider collision (fixed by including them).  
- Prefix collision in system+prompt hashing resolved.  
- Vision engines lacked local vision path; identified need for `queryVision` route.  
- P2: `callVisionAPI` returned unused `tokens_used`; stale JSDoc in PartMediaToCAD; missing tokens assertion in test (fixed).  

**AI‑SYSTEM SPECIFICS**  
- **LLMEngine** – runtime provider, Ollama first (`ollama_model`, `ollama_timeout_ms`), Claude backup, offline fallback.  
- **Knowledge Router** (`routes/knowledge.ts`) – exposes 14 `/api/v1/knowledge/*` endpoints to `prism_knowledge`.  
- **Dispatchers (ManusATCSBridge, manusDispatcher, ralphDispatcher, atcsDispatcher)** – migrated to LLMEngine; gates removed; offline‑honesty logic added.  
- **Vision Engines** (`BlueprintVisionOCR`, `PartMediaToCAD`, `VisionActionAnalyzer`, `VideoLearning`) – use `queryVision`; local qwen2.5vl/qwen3‑vl models with Claude vision backup.  

**OPEN THREADS**  
- Implement and test `queryVision` route; migrate remaining vision consumers (BlueprintVisionOCR, PartMediaToCAD, VideoLearning).  
- Complete migration of all paid call sites: post‑processor generation, master post‑processor, SFC, CAD/CAM AI systems; mill, lathe, wire EDM wizard AI systems.  
- Verify self‑compact triggers in all environments; ensure no manual prompts required.  
- Final integration tests for end‑to‑end free‑AI workflow (Ollama → Claude backup) across all migrated modules.
