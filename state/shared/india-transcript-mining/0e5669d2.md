# india session 0e5669d2 (2026-06-10, 96MB, spine 827KB, 10 slice(s), model gpt-oss:20b)

**SHIPPED**  
- 168c202646 – B1 sync‑resilient: added `writeWithRetry`, guarded `ensureDir`, unit test.  
- ea21008f25 – B2 index‑meta: in‑place update of `last_verified` & “Total entries”.  
- 8c4dff660a – B3 maint‑cron installers (promote‑memory‑to‑wiki + vault‑rot‑sentinel) with `–Disabled`.  
- 984313825e – C2 link‑heal scorer: re‑calibrated tiers, reason‑based auto‑apply gate.  
- 838123429b – C3 tribal‑coverage refresh: regenerated JSON, cleared stale banner.  
- da013c4406 – audit map & node‑access mapping persisted to wiki + galaxy brain.  
- 9e4376b3b2 – U‑VAULT‑RAG‑WIRE: wired keyword recall hook; verified live fire.  
- 45225e0cc3 – persistent fix‑memory record in C: and H: mirrors.  
- d948b85a74 – hardened scorer (minLen 8, edit‑fraction guard), updated tests, regenerated candidates.  
- 43e1b8e449 – `maxConnections` →512; added TRANSIENT_PRESSURE_CODES, widened OFFLOADABLE_PATTERNS, introduced EXPECTED_UNREGISTERED_TASKS.  
- 3d796dcf5c – fleet‑task‑health‑watch.mjs: added EXPECTED_UNREGISTERED_TASKS, PRISM WSL Memory Guard.  
- 96ed5222e2 – vault-backlink-schema.mjs: reverse edge index (node → vault docs).  
- d856173b86 – staleness flag to backlinks reader.  
- 0e2724871a – regen‑wire stage in regen‑viz.mjs for auto‑refresh of vault‑backlinks.json.  
- 14aba14e3a + 4a44b5393c – doc_nodes dispatcher action, schema, tests (14/14).  
- f23c58b42d – updated vault‑access map to close .canvas gap and stale memory‑rag‑inject claim.  
- 2d49bf0d33 – canvas-read-lib.mjs + CLI `canvas` & `canvas-doc`; 15/15 tests.  
- 47fe91554f – access‑map doc update for canvas.  
- 7ec4a5ea02 – OLLAMA‑pipeline injector wired & live‑validated.  
- 65a29220e5 – prewarm hook for ollama pipeline.  
- 6fb278a2ee – Tribal‑QA LLM unblock.  
- f1b69db664 – memo‑extraction throttle fix.  
- 1ecf50b6be – user‑type mismatch fix in memo extraction.  
- b5d249f4f5, 9697a9135a, 71a818b49c – U‑WEEKLY‑SYNTH‑RESOLVER: host‑aware model selection, `num_predict=-1`, fallback to qwen2.5-coder:32b.  
- f8c183f7a5, c55e05cf03 – U‑WIKI‑NLI‑LINT: new script with 25 tests.  
- 153887a519 – U‑VIZ‑GRAPH‑ATOMIC‑WRITE: switched merge‑augmentations write to `writeGraphStreamingAtomic`.  
- 6884155fb6, 757456eaae – U‑VIZ‑MERGE‑GUARD‑OOM: off‑heap node count replaced with streaming counter.  
- ae55cea3f7 – U-VIZ-AUGMENT-MOLECULES-STREAM: `streamGraphArray()` replaces materialized graph; live‑validated under 432 MB heap.  
- 0f14952601 – #6 RESCOPE: OLLAMA‑SYNERGY premise falsified; memo caches distinct.  
- 734bcee6d4 – status‑doc reflection commit.  
- 628aaa51f5 – U-VIZ-MERGE-AUG-CAP-GUARD: loud‑degrade on >512 MiB, shared `exceedsStringParseCap`.  
- 7a1f52061b – merge validated at 660 MB (live regen).  
- 80f8059cb1 – U-VIZ-POSTMERGE-CAPSAFE: replaced JSON.stringify writes with `writeGraphStreamingAtomic` in several stages.  
- d03d8687a7 – fixed two regen‑pipeline readers to use `readGraphStreaming`.  
- 4dbd18c2e3 – U‑VIZ‑READER‑CAPSAFE‑2: 7 of 9 graph readers switched to off‑heap `readGraphStreaming`.  
- 3e4df51d04 – removed phantom FAST[] entry `generate-slot-queue-features.mjs`.  
- 17294fc77f – U‑TRIBAL‑RERANK‑STREAM: rewrote tribal‑rerank to stream entries, bounded top‑K.  
- e7704ba450 – U‑TRIBAL‑RERANK‑STREAM‑FIX: widened inject‑gate for sharded layout, fixed JSDoc drift.  
- 736c9cfd8b – U‑TRIBAL‑SIBLING‑WRITER‑SHARD‑SAFE: wired all three sibling writers to new manifest‑aware, lock‑coordinated I/O helper.  
- 8bf1873577 – HOTFIX: made `readIndex`/`writeIndex` manifest‑aware and added shard‑transition guard.  
- 6bea1b726f – RECOVERY: re‑embeddable batch restored brain to ~29 500 entries after shard‑transition crash.  
- 441a7149fc – U‑TRIBAL‑EMBED‑RESUMABLE: checkpoint every 500 embeds.  
- 46c07e9cd7 → 1322c38364 – U‑TRIBAL‑SIBLING‑WRITER‑SHARD‑SAFE (4 commits, 163 tests).  
- a6dbec1842 – U‑AUDIT‑WIRED‑VIA‑ENGINE: 23/23 tests; unwired engines metric corrected.  
- 05906647ad – U‑LARGE‑READ‑DECAY‑WIRE: 15/15 tests; live‑validated mute of 0/122 advisory.  
- f6e596b767 – Tribal monolith untrack (first live shard transition); 33 501 entries verified intact.  
- 037f61dc86 – Scrutiny P2 fix: 23/23 tests passed.  
- 8f373e9e43 – U‑GREP‑INDEX‑DECAY‑WIRE: 18/18 tests; mute of 3/283 noise.  
- 7c184bc97c – U‑ADVISORY‑DECAY‑APPLY‑ALL: 20/20 + 23/23 tests, live‑validated.  
- 88cb72b532 – U‑XSUB‑EMBEDS & U‑XSUB‑DOCBY‑ORACLE: 948 edges added; documented‑by restored to 320.  
- d0f6176db5 – U‑XSUB‑CONSENSUS‑OF: 1 edge added, staleness surfaced honestly.  
- ab9318ee47 – U‑XSUB‑BUGCLASS‑SWEEP: bug‑class confined to one generator; canonical pattern codified.  
- ee29e4ea06 – U‑XSUB‑DRIFT‑DETECT: self‑monitoring drift detector, live‑validated.  
- d132015a0e – Drift surfacing to per‑prompt inject (R15 complete).  

**DECISIONS**  
- Persist audit & mapping artifacts to wiki, galaxy brain, two C: memories, handoff; context survives beyond chat.  
- Wire keyword recall only when always‑on injector disabled; avoid double‑up patterns.  
- Replace stale index‑meta frontmatter with live date & count; preserve curated entries.  
- Tighten link‑heal scorer: demote prefix/substring below auto‑apply floor; gate on exact‑slug or ≤2‑edit Levenshtein; add reason‑based eligibility.  
- Add `coverageComplete` flag and document partial coverage in candidates file.  
- Use atomic stage+commit with CLAUDE_SESSION_ID and lane‑guard bypass for shared‑tree commits.  
- Harden scorer: minLen 8, edit‑fraction guard; cap hook heap to 384 MB via NODE_OPTIONS.  
- Increase MCP server maxConnections to 512; defer restart until operator action.  
- Add TRANSIENT_PRESSURE_CODES; modify isLaunchFailureCode to surface as “pressure”.  
- Widen OFFLOADABLE_PATTERNS (lint, classify, docstring, diff‑summary, triage, extract, rename, describe, reword, format, list).  
- Introduce EXPECTED_UNREGISTERED_TASKS for intentional unregistered vault crons.  
- Adopt off‑heap streaming (`streamGraphArray`, `readGraphStreaming`) to avoid V8 string cap and OOM in augment‑molecules, merge‑augmentations, post‑merge stages.  
- Implement cap‑safe readers/writes; loud‑degrade on oversized loads.  
- Wire all tribal index writers through guarded IO; make readers manifest‑aware; add shard‑transition guard.  
- Enable Ollama synergy: route via PRISM_OLLAMA_ROUTE_AUTO=1, use qwen2.5-coder:32b (fallback to gpt‑oss:120b on Blackwell) for host‑aware synthesis; set num_predict=-1.  
- Use atomic writes (`writeGraphStreamingAtomic`) for canonical graph outputs.  
- Add checkpointing every 500 embeds in tribal embed batch.  
- Keep dormant augmentations inactive due to dangling edges.  
- Maintain audit loop on qwen2.5-coder:32b for bug‑class checks.  

**OPERATOR DIRECTIVES**  
- “Fill all gaps” – complete B and C gap ladder from audit.  
- Ensure Obsidian vault built, operational; map paths to each vault node for token savings.  
- Use ultracode to find all obsidian related articles to regain context.  
- Wire U‑OLLAMA‑PIPELINE‑INJECTOR‑WIRE (T2) and U‑WEEKLY‑SYNTH‑RESOLVER (T3).  
- Finish all Ollama synergy tasks; utilize strongest viable models for searches, read, write, coding.  
- Accelerate self‑improving of Obsidian vault / system‑viz; wire, test, validate across all galaxies and full H‑drive.  
- After exhaustion, look for inefficiencies, gaps, wirings, dormant nodes to activate.  
- Goal: wired, tested, validated, synergized across all galaxies and full H‑drive.  

**FINDINGS/BUGS**  
- `obsidian-memory-sync.mjs`: unguarded writeFileSync caused data loss on first failure; missing export.  
- `WikiIndexMaintainerEngine.formatIndex`: stale frontmatter (last_verified 2026‑05‑08, 770 entries) not re‑emitted.  
- Scorer overconfidence: prefix/substring matches scored ≥0.85 → auto‑apply; decay path leaked >60‑char slugs above floor.  
- Stale `wiki-link-fix-candidates.json` had autoApplyCount 14100.  
- Full‑corpus suggester OOM on 15,828×51,028 candidates; partial run with --batch 100 produced incomplete file.  
- Hook overcommit: 84 processes each reserved 4 GB → ~210 GB commit; fixed by reducing reservation to 384 MB.  
- False‑positive MCP alarm due to transient resource pressure; handled via TRANSIENT_PRESSURE_CODES.  
- GPU offload coverage low (~8%); widened OFFLOADABLE_PATTERNS increased coverage.  
- Missing‑task banner noise from two vault crons added to KNOWN but not registered; addressed with EXPECTED_UNREGISTERED_TASKS.  
- Drift detector added; logs and surfaces events.  
- Silent regression: `documented‑by` collapsed from 38 → 0 due to volatile augmentation; restored to 320 edges.  
- Offload hooks status: ollama-task-offloader (62 offloads/116 fires), ollama-route-pretooluse (41 fires); ollama-auto-router dormant.  
- OOM in augment‑molecules stage (~382 MB) fixed by streaming reranker.  
- Merge‑augmentations exit 1 at ~630 MB due to string cap; resolved via cap guard and atomic writes.  
- Phantom FAST[] entry `generate-slot-queue-features.mjs` removed.  

**AI‑SYSTEM SPECIFICS**  
- Engines/actions: obsidian-memory-sync.mjs, WikiIndexMaintainerEngine.formatIndex, wiki-link-fix-suggester.mjs, cron installers (promote-memory-to-wiki.mjs, vault-rot-sentinel.mjs), fleet-task-health-watch.mjs, vault-backlink-schema.mjs, regen‑viz.mjs, doc_nodes dispatcher, canvas-read-lib.mjs, access-map updates, OLLAMA pipeline injector, prewarm hook, Tribal‑QA LLM, memo extraction, U‑WEEKLY‑SYNTH‑RESOLVER (WeeklySynthesisEngine.ts), U‑WIKI‑NLI‑LINT (lint-wiki-contradictions.mjs), system-viz graph writers/readers (merge-augmentations.mjs, augment-molecules.mjs, post‑merge stages), tribal index writers/readers, embed batch checkpointing.  
- Models: qwen2.5-coder:32b (primary for audits and synthesis), gpt‑oss:120b/20b (fallbacks), nomic-embed-text, gpt‑oss:20b for NLI, gpt‑oss:120b on Blackwell.  
- Metrics: scorer thresholds – exact 1.0, Levenshtein≤2 → 0.85–0.92 auto‑apply; prefix/substring 0.70 medium; offload ratio ~40% after widening patterns; atomic write success; OOM avoided.  
- Dataset/corpus paths: system-graph.json (~660 MB, 335k nodes), architecture-graph.json, obsidian-augmentation.json (~416 MB), vault‑backlinks.json (~19.8 MB), node_cards.jsonl (~160 MB), node-card-offsets.json (~24 MB), PRISM-System-Map.canvas (146 KB).  

**OPEN THREADS**  
- MCP daemon restart pending to apply maxConnections change.  
- Vault cron installers require operator activation; migration freeze lifted but still pending.  
- Qdrant migration pending; Docker Linux engine down – operator must start container.  
- Final coverage batch for 100% vault coverage remains to run after lock coordination verified.  
- Monitoring of shard‑transition guard in production to ensure no brain loss.  
- Clone decay wire into grep‑index‑first; increase deterministic offload coverage ≥30%.  
- Activate cited‑tips merge splice deferred due to high blast radius; pending safe edit after /compact.  
- New work order: upgrade `/smart` pipeline, attach to `/loop` + `/goal`; ensure H‑drive indexing of `H:\PRISM\resources`, `JM DIE`.  
- Pending activation of U‑MEMO‑CACHE‑CONSOLIDATE (high blast radius).  
- Continue Ollama synergy tasks via cron loop and other lanes; unbounded clauses remain handled by scheduled loops.
