# india session 06e3b710 (2026-06-24, 26.6MB, spine 207KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED (builds / commits)**  
- `5f61238333` – Added `orchestrateLive` to X‑PROC orchestrator; real Ollama‑offloaded tier fan‑out (`qwen2.5-coder:32b`).  
- `50143ece3c` – Rewired `five_axis_deep_learn` → `deepReason`; added crash‑guard schema.  
- `85f9f6fe8d` – Rewired `five_axis_decision` → `decide`; added crash‑guard schema.  
- `00f68e9997` – Rewired `fusion_5axis_strategy` → `recommendStrategy`; added crash‑guard schema.  
- `964535033a` – Rewired `cross_domain_orchestrate` → `planJob`; removed double‑wrap.  
- `da5560850f` – Rewired `foresight_orchestrate` → async `reportFor`.  
- `62661e33fb` – Rewired `wet_run_pilot_orchestrate` → positional‑arg `pilotPromotionReadiness`.  
- `82aa392d6a` – Added self‑guard to `uncertainty_pipeline_run → propagate`; all regression tests green.  
- `29af45fc12` – Rewired `smart_tool_select` → orchestrated `selectToolOrchestrated`.  
- `d1a97a3a4c` – Implemented `sampling_plan_generate` router (`standard`).  
- `9c4e94ff94` – Implemented `mit_course_knowledge_query` router (`scope` + query guard).  
- `U-DARK-FACADE-AUDIT` – Built audit harness enumerating 85 dark dispatcher actions across 10 dispatchers; snapshot committed.  
- `U-DARK-FACADE-INDIA-VERIFY` – Verified India’s 13 dark actions → 4 clean, 4 ambiguous, 4 needs‑build, 1 false‑positive; queue committed.  
- `U-ROLLBACK-PLAN-WIRE` – Rewired `rollback_plan_build` → `planRollback`; optional dry‑run via `planAndVerify`. 6/6 tests, tsc clean.  
- `U-OPDASH-ORCH-WIRE` – Added `orchestrate()` to `OperatorDashboardOrchestratorEngine`; composed status/alerts/shift with fail‑soft; extracted shared `filterAlertsView`. 8/8 new tests, tsc clean.  
- `U-MACHINE-LORA-INFO-WIRE` – Added pure `machineLoRABase.getInfo()` introspection; rewired dispatcher to return bare payload. 3/3 tests, tsc clean.  
- `U-INCAD-INFER-FAILLOUD` – Replaced crash‑facade in `PerAppInCADInferenceAdapter`; added static `describeCapabilities()`. 59/59 tests, tsc clean.  
- `U-CAD-LEARN-STATS-RATE-FIX` – Corrected `cad_learning_stats.byCategory` to credit successes on pass; aligned with `extractPatterns`. 32/32 tests, tsc clean.  
- `U-CAD-LEARN-FEATURE-SIGNAL` – Made `features` a real learning signal in `CADTrialErrorLearningEngine.recommendAdjustments`; added aggregation and persistence. 36/36 tests, tsc clean.  
- `U-CAD-LEARN-TREND` – Added read‑only `cad_learning_trend.getLearningTrend()` metric; dispatcher round‑trip test included. 41/41 tests, tsc clean.

**DECISIONS (architecture / scope + why)**  
- Adopted dark‑façade audit to surface all `"method not callable"` cases (85 actions); fixing yields a clean API surface.  
- Rewired all dark actions to real static/instance methods or added positional‑arg adapters; relied on engine self‑validation where available (`planJob`, `reportFor`).  
- Introduced Ollama offloading for X‑PROC orchestrator (`orchestrateLive`) to provide true parallel tier reasoning without local LLM overhead.  
- Enforced 16 GB heap tsc to avoid false‑clean builds; all commits now pass with this setting.  
- Added crash‑guard schemas for every newly wired action to prevent runtime dereference errors (e.g., `machine` missing, `kc11_mpa` NaN).  
- Reverted unsafe sync dispatcher (`roadmap_dag_build → load()`) and marked as *needs‑design* due to unreliable I/O.  
- Auto‑compaction enabled; will trigger at token‑RED/CRITICAL thresholds.  
- Commit companion `<Name>.test.ts` files with ≥10 cases to satisfy R15 wiring gate.

**OPERATOR DIRECTIVES (verbatim asks)**  
- “utilize sound logic to make the logical choices” – all decisions made autonomously, no operator deferment.  
- “continue” – loop should keep running until a genuine spiral or completion of all India dark‑clean actions.  
- Arm account‑switch (`node scripts/capture-claude-credentials.mjs account-N` then `node scripts/arm-account-switch.mjs --auto`).  
- Re‑run `vitest` report refresh from `mcp-server` cwd to clear stale global test gate.  
- `/loop [25m] /goal AUTONOMOUS OVERNIGHT BUILD`: improve CAD drawing/print generation, print→CAD, India AI‑systems backend, any domain; use Hermes CLI, Ollama offloading, harnesses, engineered loops, crons.  
- Auto‑compaction must stay on; run autonomously while operator sleeps.

**FINDINGS / BUGS**  
- Dark façade pattern – 85 dispatcher cases silently returned `"method not callable"`; all fixed or queued for fix.  
- Runtime crashes: `machine` missing in `deepReason`, `kc11_mpa` division by zero, double‑wrap in orchestrator leading to `data.data`.  
- tsc false‑clean – bare `npx tsc` OOMs without 16 GB heap; all builds now use the larger heap.  
- Sync dispatcher `roadmap_dag_build → load()` hangs/throws due to heavy file I/O; reverted and marked as *needs‑design*.  
- Crash‑facade in `PerAppInCADInferenceAdapter` due to missing constructor args.  
- Silent failure: `cad_learning_stats.byCategory` never credited successes → inflated failure rates.  
- Ignored feature signal in `CADTrialErrorLearningEngine.recommendAdjustments`.  
- Missing capability contract for CAD inference adapter; now exposed via `describeCapabilities()`.  
- Stale `VITEST_REPORT.json` (2026‑05‑12) – must refresh from `mcp-server` with full PATH.  
- Dispatcher double‑wrap bug (`executeAIReasoningAction`).  
- `slimResponse` pruning may drop valid falsy values.  
- CadDispatcher output `{content:[{text:JSON}]}`; tests must parse accordingly.  
- RL engine TS2554 error noted (to fix after this unit).  

**AI‑SYSTEM SPECIFICS (engines / actions / metrics)**  
| Engine | Action(s) wired | Model / Offload | Key Metrics / Notes |
|--------|-----------------|-----------------|---------------------|
| `CrossProcessHierarchicalNeuralOrchestratorEngine` | `orchestrateLive`, `orchestrateBrief` | Ollama (`qwen2.5-coder:32b`) | Parallel tier fan‑out, `fan_out_mode: live`; no local LLM cost |
| `FiveAxisDeepLearningEngine` | `deepReason`, `decide`, `recommendStrategy` | – | Crash‑guard schemas added; pure deterministic logic |
| `CrossDomainOrchestratorEngine` | `planJob` | – | Self‑validating, returns `OrchestrationPlan` |
| `ForesightOrchestratorEngine` | `reportFor` | – | Async self‑validation |
| `PilotPromotionOrchestratorEngine` | `pilotPromotionReadiness` | – | Positional‑arg adapter |
| `UncertaintyPipelineEngine` | `propagate` | – | Added self‑guard; all regression tests green |
| `SmartToolSelectorOrchestratorAdapter` | `selectToolOrchestrated` | – | Rewired from dark façade |
| `SamplingPlanEngine` | `mil1916`, `aoqlPlan` | – | Router (`standard`) with default `mil1916` |
| `MitCourseKnowledgeEngine` | `searchAlgorithms`, `searchCourses` | – | Router (`scope`) + query guard |
| `RollbackPlannerEngine` | `planRollback` | – | Real positional method, optional dry‑run via `planAndVerify` |
| `OperatorDashboardOrchestratorEngine` | `orchestrate` | – | Compose status/alerts/shift with fail‑soft; shared `filterAlertsView` |
| `machineLoRABase` | `getInfo` | – | Pure introspection of LoRA foundation; dataset: canonical defaults + 8 machine types |
| `PerAppInCADInferenceAdapter` | `describeCapabilities` | – | Fail‑loud honest capability contract (10 CAD apps × 10 inference types) |
| `cad_learning_stats` | `byCategory` | – | Credit successes on pass; aligns with `extractPatterns` |
| `CADTrialErrorLearningEngine` | `recommendAdjustments` | – | Feature signal aggregation & persistence |
| `cad_learning_trend` | `getLearningTrend` | – | Read‑only trend metric |

**OPEN THREADS**  
- India needs‑build dark actions (4): `machine_lora_base_info`, `print_corpus_orchestrate`, `operator_dashboard_orchestrate`, `rollback_plan_build`. New engine methods required; design specs queued.  
- Cross‑domain dark actions (72): distributed to owning slots via audit harness snapshot; safety‑critical physics actions remain under Oscar’s domain.  
- Resolve RL TS2554 error.  
- Refresh `VITEST_REPORT.json` correctly in next cycle (ensure PATH).  
- Verify loop closure wiring (`recordRecommendation → getLoopEfficacy → linkOutcome`) and retrain signal.  
- Text→CAD feedback loop implementation.  
- Blueprint LoRA/RAG engine internals refinement.  
- Verify/fix any genuinely dark CAD‑AI dispatcher actions by reading engines first.  
- Await operator action to arm account‑switch for overnight fleet resumption.  
- Monitor auto‑compaction status and token usage; ensure no further RED/CRITICAL spikes.
