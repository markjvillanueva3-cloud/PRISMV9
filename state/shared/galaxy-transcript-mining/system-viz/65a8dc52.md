# system-viz session 65a8dc52 (2026-06-06, 14MB, spine 82KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e140cc486f` – U‑SCP01: Source‑chain provenance on retrieval hits (`[src:<type>:<id>]`).  
- `e611001ed2` – U‑PSN‑ATTR01: PSN‑leg attribution ledger (maps each hit to one of 11 legs).  
- `f03416b662` – U‑PSN‑ATTR02: Ledger hygiene (rotation + breadcrumb, bounds live tap growth).  
- `30d7f46055` – U‑PSN‑ATTR03: Queryable coverage CLI (`scripts/psn-attribution-report.mjs`).  
- Commit of spec file `OBSIDIAN-HERMES-CONTEXT-LEARNING-ACCEL-2026-06-06.md`.

**DECISIONS**  
- Mirror TS `SourceChainEngine` in a fail‑soft `.mjs` with parity test to avoid TS import in hooks.  
- Inject provenance only on master‑index hits; memory/tribal hits remain unchanged.  
- Add ledger rotation + breadcrumb to bound ledger size and provide failure diagnostics.  
- Provide fleet‑wide PSN‑leg coverage CLI for quick insight into unused legs.  
- Cancel session‑only cron at budget ceiling; resume heavy TS dispatcher wrapper in a fresh session.

**OPERATOR DIRECTIVES**  
- `/goal`: “find ways to accelerate Obsidian/Hermes context learning.”  
- Re‑register `Hermes Dream‑Cycle Synth` and `Self‑Reflect Weekly` tasks.  
- Hand off remaining levers to India, Golf, Bravo, Alpha via handoff spec.

**FINDINGS/BUGS**  
- Offline learning loop dark: `Hermes Dream‑Cycle Synth` & `Self‑Reflect Weekly` were missing.  
- `Hermes‑Obsidian Bridge` disabled (unknown cause).  
- SourceChainEngine not propagating provenance in retrieval hits (`grep sourceChain 0`).  
- Ledger growing unbounded; rotation required.  
- Path bug: `recordMemoryHit` used `.md` extension incorrectly.

**DOMAIN SPECIFICS**  
- `SourceChainEngine` API: `decorate`, `digest`, `renderMarkdown`, `deduplicate`, `merge`.  
- Retrieval hooks: `master-index-search-lib.mjs`, `memory-index-search-lib.mjs`; inject hooks for provenance.  
- PSN legs mapping: 11 legs (wiki, memories, system_viz, etc.).  
- Ledger storage: JSON file with rotation and breadcrumb.  
- CLI script `psn-attribution-report.mjs` renders bar chart of leg coverage.

**TOOLS USED**  
- `system-viz-query`.  
- RTK commands (`rtk git`, `rtk vitest run`, `rtk tsc`, `rtk npm run build`).  
- Ollama offload (`/ollama-*` skills).  
- `prism_session:master_index_query`.  
- `prism_ai`, `prism_dev`, `prism_calc`, `prism_safety`.  
- `scrutiny-3way.mjs`.  
- `loop-state.mjs`.  
- `CronCreate` / `CronDelete`.  
- Node scripts (`checkin-recall.mjs`, `system-viz-query.mjs`).

**OPEN THREADS**  
- TS dispatcher wrapper for `psn_attribution` (heavy build, to be done in fresh session).  
- Lever #3 compaction → memo emitter.  
- Re‑enable `Hermes‑Obsidian Bridge` if required.
