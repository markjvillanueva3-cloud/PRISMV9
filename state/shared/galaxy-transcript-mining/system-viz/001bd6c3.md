# system-viz session 001bd6c3 (2026-06-09, 48.8MB, spine 451KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hermes config migrated v0→v28; primary model set to `anthropic/claude-opus-4.8`, fallback `qwen2.5-coder:32b` → later switched to `gpt‑oss:20b`.  
- Restarted Hermes after clearing `package-lock.json`; GUI launcher now boots, gateway at :9120 reachable.  
- Qdrant container (`prism-qdrant`) up on :6333 with 3 collections (Docker Desktop v29.4.3).  
- Built blind‑mapper script `node scripts/hermes‑obsidian‑app-map.mjs`.  
- Commit `28b72e4dee`: added `scripts/fill-galaxy-memory-sections.mjs` + 13‑test suite; all 34 galaxies now full `MEMORY.md`.  
- Slot/bravo commits: U5b, U5c, U6, U7, U9, U10, plus `ollama-tool-agent.mjs`, `ollama-offload-enforce.mjs`, `ollama-codegen.mjs`, `ollama-build-loop.mjs`, `ollama-capability-probe.mjs`, `ollama-coresidency.mjs`.  
- Unit U1 (offload roster & tier logic) committed `2340a2e699`.  
- Plan spec for build spine (`822c48c55a`) committed.

**DECISIONS**  
- Hermes uses Claude Code primary with local Ollama fallback; enable extra‑usage billing to avoid HTTP 400.  
- Qdrant must run via Docker on :6333; start container before MCP restarts.  
- Fleet coordination path (`slot_brief_write` → consume) remains functional independent of Qdrant.  
- Model strategy: fleet‑standard `gpt‑oss:20b` as primary for zulu, Claude fallback for extra usage.  
- Execute zulu headless via `hermes -z / hermes chat`; avoid GUI stalls.  
- Provider mapping: replace invalid `openai` with `custom` (OpenAI‑compatible) for Ollama; keep `anthropic` for Claude.  
- Use `gpt‑oss:120b` & `gpt‑oss:20b` for structured tool‑calling; use `qwen2.5-coder:32b` only for code generation.  
- Offload read/search/coding to local LLMs via new hook/agent; flag `PRISM_OLLAMA_OFFLOAD_ENFORCE` toggles enforcement vs advise.  
- Auto‑route per task handled by existing `OllamaTaskOffloaderEngine`; no new router needed.  
- Create routing config file (`config/model-routing.json`) with task→model matrix.

**OPERATOR DIRECTIVES**  
- Bring Qdrant up: `docker run -p 6333:6333 qdrant/qdrant` (or via docker‑compose).  
- Enable extra‑usage billing for Claude Code OAuth at claude.ai/settings/usage.  
- Restart :3100 MCP daemon after Qdrant online to re‑establish connection.  
- Align zulu primary model to `gpt‑oss:20b` in `config.yaml`; restart hermes headless (`hermes -z`).  
- Verify headless zulu works with `max_tokens=300`; monitor memory pressure (commit charge >95 %).  
- Commit routing config and Ollama warm‑up daemon files; schedule recurring `/loop [10m]` assessment script via `CronCreate`.  
- Re‑enable Hermes‑Obsidian Bridge scheduled task after sync.  
- Regenerate corrupted `ai-training_synthesis.md` (owner India).  
- Post‑merge tuning: adjust `NUM_PARALLEL`, `MAX_LOADED` if needed.

**FINDINGS / BUGS**  
- Qdrant not running; Docker stopped.  
- Hermes receives HTTP 400 from Anthropic; blocks request and prevents fallback.  
- Unknown provider `openai` caused agent init failures; fixed by using `custom`.  
- Stale daemon connection state: :3100 reported “qdrant not connected” due to cached failed connect at startup.  
- GUI launcher stuck due to dirty `package-lock.json`; cleared.  
- Headless zulu “no final response” with `gpt‑oss:20b` due to token budget issue; raising output tokens fixes it.  
- Corpus inflation: `node_*` graph dumps counted as memories; now excluded.  
- Test failure from injected mutation in `applyBlock`; deterministic tests rewritten.  
- `ai-training_synthesis.md` corrupted (all NUL).  
- `qwen2.5-coder:7b` absent; tag matching must strip `:latest`.  
- `git grep` requires `-E` for parentheses regexes.  
- `process.exit()` truncates stdout; probe writes report file instead.  
- Probe script had entry‑guard bug fixed.

**DOMAIN SPECIFICS**  
- Hermes desktop client uses `claude-opus-4.8` via OAuth, fallback `qwen2.5-coder:32b` → later `gpt‑oss:20b`.  
- PRISM MCP :3100 coordination hub healthy; serves dispatchers.  
- Qdrant vector store required for `semantic_search`; now online on :6333.  
- Obsidian vault REST API :27123 operational but not involved in current issue.  
- Fleet coordination slot‑brief injection works; Hermes writes briefs to slots.  
- 34 galaxies each with `MEMORY.md`, `PATHS.md`, `TOOLBELT.md`, `CLAUDE.md`; synthesis files `patterns/<galaxy>_synthesis.md`.  
- Hardware: RTX PRO 6000 Blackwell, 96 GB VRAM, 127 GB RAM.  
- Models in use: `gpt‑oss:120b` (65 GB), `gpt‑oss:20b`, `qwen2.5-coder:32b` (~37 GB), `nomic‑embed-text`.

**TOOLS USED**  
- Docker Desktop / `docker run` / `docker-compose.yml` for Qdrant, Postgres, Prometheus, Grafana.  
- Hermes CLI (`hermes config migrate`, `hermes model`, `hermes -z`).  
- Blind‑mapper script `node scripts/hermis‑obsidian‑app-map.mjs`.  
- PRISM MCP utilities (`prism_context`, `prism_session`).  
- Ultracode Workflow for planning/verification.  
- Node.js scripts: `ollama-tool-agent.mjs`, `ollama-offload-enforce.mjs`, `ollama-codegen.mjs`, `ollama-build-loop.mjs`, `ollama-capability-probe.mjs`, `ollama-coresidency.mjs`.  
- Git, Node harness, Vitest + esbuild, ascii‑guard.

**OPEN THREADS**  
1. Start Qdrant via Docker; confirm vector search available.  
2. Enable extra‑usage billing for Claude Code OAuth to eliminate 400 error.  
3. Test Hermes message after fixes; fallback only on 429.  
4. Verify fleet coordination continues once Qdrant online.  
5. Restart :3100 daemon to re‑establish Qdrant connection.  
6. Confirm headless zulu works with `gpt‑oss:20b` after token budget adjustment.  
7. Pull & test `gpt‑oss:120b`; update fleet routing accordingly.  
8. Monitor memory pressure (commit charge) and clean idle processes if needed.  
9. Regenerate `ai-training_synthesis.md` (owner India).  
10. Re‑enable Hermes‑Obsidian Bridge scheduled task.  
11. Post‑merge tuning of `NUM_PARALLEL`, `MAX_LOADED`.  
12. RTK upgrade sweep (#11) planning pending.  
13. Go‑live merge & wiring after golf’s git‑tree repair.
