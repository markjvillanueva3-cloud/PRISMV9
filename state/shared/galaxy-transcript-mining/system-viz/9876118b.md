# system-viz session 9876118b (2026-05-18, 48.5MB, spine 260KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `scripts/fleet-task-health-watch.mjs` – scheduled‑task health auditor (8 PRISM tasks).  
- `.claude/hooks/fleet-task-health-stop.mjs` – Stop hook that triggers the watchdog.  
- `.claude/hooks/critical-memory-compact-nudge.mjs` – UserPromptSubmit hook injecting `/compact` for the largest memory‑hog chat under critical pressure.  
- 32 unit tests (`fleet-task-health-watch.test.mjs`) + 19 tests (`critical-memory-compact-nudge.test.mjs`).  
- Updated `settings.json` (C: & H:) to wire hooks into Stop and UserPromptSubmit chains.  
- Documentation updates: MEMORY.md, CLAUDE.md, wiki, memory‑file index.  
- Commit `ac9cca8902`: enum‑blind `$p.Name` sanitization + 3 regression tests (fleet‑reaper‑MS0).  
- Commit `da01003b61`: FLEET‑TASK‑HEALTH‑MS0 build A/B (watchdog & nudge hook).  
- Commit `31cc2c84d3`: arm‑C P1 fixes (`C0‑strip`, `touchStamp` atomicity).  
- Commit `d3db37cc2c`: per‑PID `Stop‑Process` batch‑kill fix (fleet‑reaper‑MS1).  
- Commit `bb7d30c7cc`: slotLabel:null deep‑fix.  
- Commit `85703afab6`: U‑CHO01 decision module for clear/compact & restart logic.  
- Commit `b41b829232`: U‑FR‑SOFT‑RELIEF‑V2 – trims large alive slots under critical pressure.  
- Commit `9cfc411eb3`: U‑FR‑OPT2‑CRITICAL‑88 – lowers critical threshold to 88 % for earlier reap.  
- Commit `cc14791cd7`: U‑FR‑OLLAMA‑KEEP‑ALIVE‑1H – keeps Ollama models resident indefinitely.  
- `U‑FR‑CRASH‑WATCH` (3 files, 549 insertions) – crash‑watch integration that records post‑mortem JSONL and triggers nudge.  
- `nim-docker-launcher.mjs` + 16/16 tests – NIM container provisioner.  
- `NIM‑ACTIVATION‑MS0.md` (80 lines) – spec for migrating offload hooks to the NIM‑aware router.

**DECISIONS**  
- Deploy both watchdog (A) and critical‑pressure compact actuator (B).  
- Keep fleet‑reaper always‑on via elevated scheduled task + per‑session monitor; no duplicate monitors.  
- Replace 90 % filter with ≥95 % + actionable‑only logic in lightweight tail monitor, then lower to 88 % for earlier reap.  
- Sanitize `$p.Name` to close enum‑blind failure at peak pressure.  
- Switch batch kill to per‑PID PowerShell calls to avoid SIGKILL truncation.  
- Adopt three‑backend router (`local‑llm‑bridge.mjs`) that falls back to Ollama when NIM unavailable; defer migration of ~12 offload hooks until after reboot.  
- Keep Ollama models resident (`keep_alive=-1`).  
- Defer slotLabel null deep‑fix until memory pressure subsides (now resolved).  
- Commit new code only after a 3‑of‑3 scrutiny gate passes.

**OPERATOR DIRECTIVES**  
- Commit shipped files as `[FLEET-TASK-HEALTH-MS0]` once final scrutiny gate satisfied.  
- If memory pressure > 90 % after watchdog/actuator active, run `/compact` manually on heaviest chat.  
- Reboot machine to activate system‑managed pagefile, memory compression, and page combining (commit ceiling rises from 67 GB to ~100+ GB).  
- Start Docker Desktop; set `NGC_API_KEY` (`setx NGC_API_KEY <key>`).  
- Run `node mcp-server/scripts/nim-docker-launcher.mjs` to pull/start NIM container.  
- After reboot and NIM running, execute migration loop per hook as specified in `NIM‑ACTIVATION‑MS0.md`.  
- Monitor watchdog telemetry persistence across reboots; ensure compact nudge triggers under sustained critical pressure.  
- Track overhead of new hooks under peak chat load (currently ~5 % GPU, < 10 % CPU).

**FINDINGS/BUGS**  
- Enum‑blind regression in `process-slot-map.mjs` (`$p.Name`) fixed.  
- Batch‑kill under pressure bug in `fleet-reaper-sweep.mjs` resolved by per‑PID PowerShell calls.  
- Incorrect failure classification: non‑zero exit codes from monitor scripts treated as failures; now only true Windows launch‑failure HRESULTs flag failure.  
- SlotLabel = null deep fix pending (now resolved with commit `bb7d30c7cc`).  
- Memory‑Pressure Auto‑Relief script not registered; operator must run installer (`install-memory-pressure-task.ps1`).  
- Monitor flooding from leaky prewarm hint clause resolved by filter change.  
- Crash causes are commit‑limit failures, not physical RAM exhaustion; pagefile fixed at 4 GB with auto‑manage off.  
- `local‑llm‑bridge.mjs` and `nim-hook-bridge.mjs` orphaned; no production hooks import them.  
- Soft‑relief V1 dead because it only targeted “owned‑by‑stale” slots reclaimed before a sweep.

**DOMAIN SPECIFICS**  
- **Fleet‑Reaper**: orphan janitor, GPU coordinator, prewarm hint emitter, soft‑relief logic (V2), crash‑watch integration.  
- **Memory Monitor & Slot System**: tracks largest claude.exe trees, assigns slots via `ps-window-pin`, monitors `$p.Name` sanitization, slotLabel handling.  
- **Orchestrator‑Advisory**: injects `/clear` or `/compact` directives into chats that opt in; decision module U‑CHO01 handles clear/compact and restart logic.  
- **NIM Integration**: `nim-docker-launcher.mjs`, `local-llm-bridge.mjs`, `nim-hook-bridge.mjs`; provides OpenAI‑compatible endpoint for Claude Code with Ollama fallback.  
- **Offload Stack**: multiple hooks (`ollama-auto-router`, `ollama-unified-semantic-router`) currently call Ollama directly; migration to router pending.

**TOOLS USED**  
- PRISM scripts: `fleet-task-health-watch.mjs`, `fleet-reaper-sweep.mjs`, `nim-docker-launcher.mjs`, `local-llm-bridge.mjs`.  
- Hooks: `fleet-task-health-stop.mjs`, `critical-memory-compact-nudge.mjs`, `precompact-pending-guard.mjs`.  
- Node.js test harness (`node --test` / `node:test`).  
- Windows PowerShell cmdlets: `Get-ScheduledTask`, `Get-ScheduledTaskInfo`, `Stop‑Process`.  
- Docker Desktop, PowerShell for pagefile/memory settings.  
- Playwright/WebFetch for NVIDIA NIM docs.  
- Installers: `install-fleet-reaper-task.ps1`, `install-memory-pressure-task.ps1`.

**OPEN THREADS**  
1. **Reboot & Pagefile Fix** – must be performed to lift commit ceiling.  
2. **NIM Provisioning** – start Docker, set `NGC_API_KEY`, run launcher.  
3. **Hook Migration** – rewrite inline Ollama clients to use `local-llm-bridge.queryLocalLLM` (U‑NIM‑MIGRATE‑01…N).  
4. **Orchestrator‑Advisory ESM bug** – commit missing `renameSync` import and run tests.  
5. **Further Memory Pressure Mitigation** – evaluate additional soft‑relief thresholds or compression tuning after reboot.  
6. **Monitor Telemetry Persistence** – ensure watchdog telemetry survives reboots.  
7. **Performance Impact** – continue monitoring overhead of new hooks under peak chat load.
