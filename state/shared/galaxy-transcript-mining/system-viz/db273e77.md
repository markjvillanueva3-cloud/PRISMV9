# system-viz session db273e77 (2026-06-10, 158.4MB, spine 1339KB, 14 of 16 slice(s), HEAD+TAIL sampled, model gpt-oss:20b)

**SHIPPED**  
- `e5ad4ea802`: Vitest 4.1.5 config (poolOptions → test.*), GPU/CPU retunes, PRISM_EMBED_CONCURRENCY=16, audit doc `state/shared/DOCKER‑NIM‑AUDIT‑2026‑06‑08.md`.  
- `f9e953f0e8`: host‑portable fix to `enforce-eslint-after-edit.py`; added memory‑reference docs.  
- `786bd70566`: MEMOry.md updates (STUB→SCAFFOLDED), stale‑sync flag.  
- `873446a147`: populated CLAUDE.md §2/5/6/7, ownership note in MEMORY.md.  
- `632335cec6` (`U‑GOAL‑CLEAR‑ADVANCE`): goal‑clear → next‑unit fallback hook.  
- `c83ca9be64`: autoresume staleness window bump (4 h→12 h), boot‑path STALE hint.  
- `8cd8d615e9` (`U‑SLOT‑DOMAIN‑DEDUP`): slot‑domain injector dedup.  
- `eefef0359a` (`U‑PSN‑LEG‑DEDUP`): PSN‑leg state injector dedup.  
- `1f295f51e6`: `dedupedContext()` helper for 8 injectors.  
- `8d2b6cf8e0`: Foxtrot, Delta, Xray injector dedups (≈95 % token savings).  
- `636d36bf59`: semantic memory recall added to `memory‑relevance‑inject.mjs`.  
- `75c44d8412`: self‑refreshing cache builder for semantic recall.  
- `792beb75e8`: `resolveObsidianMemDir()` single‑source resolver.  
- `c7e346da99`: routed `cag-router.mjs` through resolver.  
- `b45f08e732`: routed four hot hooks via resolver (`precompact-memo‑emit`, `psn-leg-state-inject`, `stop-auto-capture-per-slot`, `stop-memory-size-watchdog`).  
- `8da1541c00` (`U‑OBS‑RECALL‑NODE‑EXCLUDE`): removed 9 571 node_* pointer stubs (72.3 % of corpus).  
- `d579626848` (`U‑OBS‑WIKILINK‑DANGLING‑FIX`): killed ~15 819 dangling links.  
- `01c3b15f56` (`U‑OBS‑RECALL‑COUNTER‑SERIALIZE`): added withExclusiveLock around recall counter RMW.  
- `a9052f63b0` (`U‑OBS‑BACKEND‑AUDIT‑SESSION‑GATE`): session‑gated audit messages, cut ~8 k redundant route‑suggest fires.  
- `9fcda446a1`: task‑boundary compact nudge stop hook (wired to `/checkin-alpha`).  
- `61a6288d0e`: verified search rerank unit (`ask‑ollama.mjs` rerank mode).  
- `7dec157db0`: HS‑01 root‑cause fix – explicit `--session-id`, removed unsafe PID‑pin guess.  
- `e81dec5cba`: env‑anchor `CLAUDE_CODE_SESSION_ID` for session ID propagation.  
- `92301e5574` & `8119ab7cb6`: commit‑msg drafter (verified‑offload consumer).  
- `1175a6f26b` & `8c57f02d77`: line‑anchored file digest.  
- `e928ef5010` & `71c817c2a2`: loop‑narrate hook.  
- `0acb1dcbc9` & `31fae0eaf8`: large‑read advisory hook (fires >600‑line reads).  
- `127234e940`: ollama‑nav‑rerank consumer.  
- `42384af1c6`: U‑DIGEST‑FILES verified‑offload primitive for multi‑file digest.  
- `be9182dca7` (`U‑AUTOSTART‑LOOP‑GOAL`): auto‑start sequence on startup surfaces, non‑stub handoff.  
- `c85d64e407` & `d926965854` (`U‑SUBAGENT‑GALAXY‑PACK`): spawned agents inherit parent slot’s galaxy domain pack (CLAUDE.md, MEMORY.md, PATHS/TOOLBELT).  
- `b70ec2bb3c` (`U‑GOAL‑FORCE‑100PCT`): `/goal` enforces 100 % completion with rule 5.  
- `2c006fec7c`: MEMORY_SEED reader wired to resume logic.  
- `2a5f980a5d`: ollama‑auto spec corrected.  
- `8c945662ac`: route‑suggest per‑session gate (token savings).  
- `1d85c327c6`: compact‑resume slot‑first read helper.  
- `1dd17250b3`: batch embeddings via `embedTextBatch`.  
- `826be35aa4` & `4a939fc35f`: precompact MEMORY_SEED enrichment + seed‑status patch.  
- `a6aee37203`: tmp‑orphan sweep.  
- `caabc8fea6`: Q1 tribal reference nodes promotion (176 new vault nodes).  
- `35dc2ec4c3` & `d92b9f52f4`: subagent bundle OOM fixes (bounded graph head read, tribe index cap, summarizeSystemViz bug fix).  

**DECISIONS**  
- Use `/checkin-alpha` wrapper to force‑take alpha slot; keep fleet‑reaper under golf.  
- Apply only safe retunes: `OLLAMA_MAX_LOADED`, `FLASH_ATTENTION`, `PRISM_EMBED_CONCURRENCY`.  
- Preserve Docker/NIM audit‑only until operator clears; avoid starting Docker during migration.  
- Commit via commit‑mutex helper (`U‑FGC‑1`) for pathspec commits.  
- Verify Ollama models match new CPU/GPU capabilities; align Docker/NIM to Blackwell.  
- Adopt verified‑offload pattern: model proposes → code verifies → fail‑safe fallback.  
- Do not build when commit‑pressure >90 % or YELLOW context; checkpoint handoff instead.  
- Continue standing `/goal` autonomously (`/yolo`) maximizing local LLM usage, token savings, enforce Ollama for reads/searches/navigation.  
- Use ultracode assessment to audit precompaction, compaction, handoff quality before new unit built.  
- Self‑compact chain: native auto‑compact@90 % → precompact‑handoff → session‑start‑auto‑resume; chat cannot fire `/compact`.  
- Ensure all 26 slots have the new Stop hook wired; verify settings.json entries present in both live and tracked copies.  
- Confirm MIT textbook corpus coverage (12 books) in academy/pdf‑corpus/ai‑training galaxies.

**OPERATOR DIRECTIVES**  
- “Keep pulling” models; take over for golf to pull the models.  
- Claim quality galaxy, re‑look X‑article URLs to confirm doctrine alignment.  
- Verify atomic‑level synergy across all systems (master graph, galaxy docs, souls, models).  
- Keep safe retunes only; avoid starting Docker during migration.  
- Use verified‑offload queue: ship one high‑ROI unit per iteration.  
- Do not build when budget tight or commit‑pressure >90 %; checkpoint and perform light doc/wiki/memory pass instead.  
- Monitor for any residual miskeying after self‑compaction/auto‑start fix; ensure all slots resume correctly.

**FINDINGS / BUGS**  
- PowerShell em‑dash encoding issue solved by UTF‑8 invocation.  
- Vitest 4.1.5 removed `poolOptions.threads`; regression fixed.  
- `main-tree-write-block` false positives due to slot contention.  
- Docker/NIM pull stalled at ~93 % (~6–8 MB/s).  
- `stop_on_failing_tests` hook exists but unwired; wiring would block all chats.  
- `enforce-eslint-after-edit.py` hardcoded C:/PRISM path; fixed.  
- Async `auto-lint-post-edit.mjs` is live lint path; sync hook avoided to reduce latency.  
- File:line citation doctrine absent – shipped as G2.  
- Ask‑before‑deps doctrine present but no gate hook (gap).  
- tsc‑after‑edit PostToolUse hook missing (gap).  
- MIT corpus not actually present; only conceptual references exist.  
- Regex bug in RESUME_LOOP strip caused duplicate blocks; fixed with line‑based approach.  
- Duplicate `safeSid` function removed from stop‑task‑boundary‑compact‑nudge.mjs.  
- Rate‑limited rerank arm B: API limits prevented verdict; arms A and C passed.  
- HS‑01 stable‑session‑id mismatch returned peer’s ID; fixed by explicit `--session-id` and env anchor.  
- Recurring 97 % commit‑pressure due to leaked 65 GB llama‑server orphan; reaped.  
- MCP daemon down repeatedly; recovered manually, root cause routed.  

**DOMAIN SPECIFICS**  
- **Engines/Actions:** MINBatchExtractorEngine.ts, McxBatchExtractorEngine.ts, GpuStackHealthEngine.ts, memory‑relevance‑inject.mjs, ask‑ollama.mjs (rerank), stop-goal-clear-advance.mjs, loop-state.mjs, precompact-handoff.mjs, session-start-auto-resume.mjs, stop-task-boundary-compact-nudge.mjs.  
- **Dispatchers:** chat-slots.mjs, slot-bind-enforce, stop-force-loop-continue.mjs, stop-task‑boundary‑compact‑nudge.mjs, ask‑ollama.mjs (rerank), verified-offload consumer scripts/ollama-offload.mjs.  
- **Metrics:** PRISM_EMBED_CONCURRENCY=16, OLLAMA_MAX_LOADED, FLASH_ATTENTION; token budget thresholds 88 % precompact trigger, 90 % auto‑compact threshold; loop tick interval [10 min]; handoff naming `HANDOFF-${chatId}.md`.  
- **Paths:** vitest.config.ts, MINBatchExtractorEngine.ts, McxBatchExtractorEngine.ts, gpu_health.py, GpuStackHealthEngine.ts, state/shared/DOCKER-NIM-AUDIT-2026-06-08.md, .claude/settings.json, scripts/lib/ollama‑verified‑offload.mjs, resolveObsidianMemDir() in scripts/lib/obsidian-mem-dir.mjs, stop-goal-clear-advance.mjs, loop-state.mjs, precompact-handoff.mjs, session-start-auto-resume.mjs, stop-task-boundary-compact-nudge.mjs.  

**TOOLS USED**  
- PRISM slot helpers (`chat-slots.mjs`, `slot-bind-enforce`).  
- Commit‑mutex helper (`U‑FGC‑1`).  
- audit-roadmap-drift.mjs.  
- system‑viz ping script.  
- CLAUDE.md staleness checker.  
- Soft‑config PowerShell script for Ollama env.  
- Vitest config files.  
- gpu_health.py, GpuStackHealthEngine.ts.  
- docker-compose.gpu.yml, nim-docker-launcher.mjs.  
- ultracode workflow engine.  
- hermes‑workflow orchestrator.  
- system‑viz summarizer.  
- obsidian app integration hooks.  
- PSN surfaces for memory feeds.  
- scripts/lib/ollama‑verified‑offload.mjs.  
- scripts/lib/obsidian-mem-dir.mjs.  
- exclusive-file-lock.mjs.  
- CronCreate/CronDelete.  
- 3‑of‑3 scrutiny gate (`scrutiny-3way.mjs`).  
- kill-zombie-tsservers.ps1, memory-pressure-auto-relief.ps1.

**OPEN THREADS**  
- Wire `ollama-nav-rerank` to auto‑fire after `/system-viz find`.  
- Retrofit remaining advisory hooks to verified execution where a verifier exists.  
- Final rollout of 24 callers to adopt explicit `--session-id` (env anchor covers).  
- Rerank arm B re‑run after API rate limits clear; consolidate duplicate logic between ollama-search-rerank.mjs and existing ollama-nav-rerank.mjs.  
- Full fleet application of self‑compaction/auto‑start fix; monitor for residual miskeying.  
- Finish ultracode assessment backlog: verified‑execution hook retrofit, scrutiny pre‑screen offload, other high‑ROI units.  
- Issue `/compact` to trigger clean session start via `/startup-alpha /loop [10m] /goal`.  
- Parallelize full‑fleet sweep to reduce runtime (<5 min).  
- Improve lock reclamation on killed sweeps (PID liveness check).  
- Implement per‑slot live actuation lever for Zulu orchestrator.  
- Resolve remaining OOM issues in subagent bundle; bounded graph head read, tribe index cap.  
- Update documentation and checkpoint due to budget constraints.
