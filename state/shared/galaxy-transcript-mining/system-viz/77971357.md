# system-viz session 77971357 (2026-05-17, 36.5MB, spine 184KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑FEEDBACK‑FORCING` – commit b1e599d5fc (stale‑fixture sweep, parent‑env nulling)  
- `U‑REGEN‑VIZ‑MERGE‑FAILLOUD` – commit f9dc218d78 (fail‑loud guard)  
- `U‑SOURCEHASH‑DOC‑ALIGN` – commit 0bac2d4c2f  
- `U‑COMPLEXITY‑FALLBACK` – commit 3d416cb040  
- `U‑BUG‑FINDINGS‑WIKI‑HOOK` – commit bb198d9285 (auto‑update wiki)  
- Back‑fill prior bug‑finding wiki entries – commit 1539fbc8b2  
- CLAUDE.md pointer to bug‑finding rule – commit dabb7a76c6  
- Token‑savings audit v0.1 – commit f03a11d07c (audit doc + HTML)  
- F8 cache‑reader misdiagnosis retraction – commit 17235ead06 (META probe fixed, P2 downgrade)  
- Runtime cron job `/loop 20m …` – job id 3ee16ac1 (created & deleted)  
- `build-requests-viz-sync.mjs` – 296 insertions committed  
- `high-value-additions-rank.mjs` – high‑ROI node‑wiring scan committed, logged in `USER-BUILD-REQUESTS-LOG.md`

**DECISIONS**  
- Adopt fail‑loud guard for regen‑viz merge; helper `decideMergePostState`.  
- Implement 4‑tier `resolveUnitKey` in `pick-prefresh-inject`; comprehensive tests added.  
- Create bug‑finding wiki rule (`stop-bug-finding-wiki-gate.mjs`) to auto‑update wiki on findings.  
- Install RTK 0.40.0 and wire `rtk hook claude` (PreToolUse:Bash).  
- Set `PRISM_CHAT_BUS_COMPACT=1`.  
- Slot‑worktree migration policy remains operator‑mandated; fleet‑wide enforcement pending.  
- Ollama offload scope limited to safe categories (staged, not built).  
- Drift‑checker auto‑sync log `viz status` with live system‑viz.  
- Use real `system-viz-query find` output for classifier; replace guessed regexes.  
- Log high‑ROI wiring candidates first; validate before wiring due to ~50 % false positives.

**OPERATOR DIRECTIVES**  
- Schedule recurring `/loop 20m …`; confirm cloud vs local scheduling.  
- Hook bug‑finding wiki rule and finish all current tasks within 20 min.  
- Use `/system-viz`, Prism OS/Obsidian brain, and memories for efficient surface awareness.  
- Build wiring candidates with high ROI in system‑viz; validate before action.

**FINDINGS/BUGS**  
- Silent merge failure in regen‑viz due to Node heap limit (≈97 % commit pressure); fixed by guard.  
- Legacy nested sidecar plan shape caused test failures; resolved via hook & fixtures update.  
- Control‑byte separator issue in spec fixed with node script.  
- Token‑savings probe misread `.hits` field → false “dead” flag.  
- Memory‑relevance inject hook lacks telemetry; hit‑rate unmeasurable.  
- Slot‑worktree migration not enforced; shared‑tree commits cause stale attribution & re‑staging churn.  
- `classifyHits` regexes incorrectly matched `L6/engine`; correct format is `L<layer>/<subgroup> <node-id> <label>`; ghost nodes prefixed with `ghost.`.  
- Raw `NEEDS_WIRING` signal has ~50 % false positives.

**DOMAIN SPECIFICS**  
- PRISM system‑graph regeneration (`regen-viz.mjs`, augmentation generators, merge step).  
- Hook system: `pick-prefresh-inject.mjs` with `resolveUnitKey`.  
- Slot-task claims & chat slots for slot locking.  
- Per-file scrutiny gate (2 reviewers per file).  
- Bug‑finding wiki rule integration (`stop-bug-finding-wiki-gate.mjs`).  
- Engines built: 2 421; wired: 836; new: 56 (e.g., `NN‑STACK‑INTEG‑MS0`).  
- Metrics: system‑viz graph size – 99 k nodes, 557 k edges; envelope drift reduced to 11; commit‑pressure 97 %.  
- Paths: `state/shared/system-viz/EXECUTIVE-BRIEFING.md`, `state/shared/handoffs/HANDOFF-claude-lima-recover…md`.  
- High‑ROI wiring cluster: Lathe domain – 82 unwired engines.  
- Other stats: 729 engines flagged `NEEDS_WIRING` (~77.7 % wire rate); F3 orphan‑engines P0 lev 72.9; F1 orphan‑hooks P0, 369 orphans / 78.5 %, lev 61.9.

**TOOLS USED**  
- Scripts: `regen-viz.mjs`, `scripts/lib/regen-viz-merge-guard.mjs`.  
- Hooks: `pick-prefresh-inject.mjs`, `error-pattern-promote.mjs`, `file-read-cache.mjs`, RTK setup script, `stop-bug-finding-wiki-gate.mjs`.  
- PRISM CLI skills: `/checkin-lima`, `/fleet-reaper`, `/loop`, `/schedule`, `/system-viz`.  
- Scheduler: CronCreate (Windows task scheduler).  
- Git utilities (`git rev‑parse`, `git status`, `git add -u`).  
- System‑viz query tools: `system-viz-query find`.  
- High‑ROI ranking & validation: `high-value-additions-rank.mjs`, `validate-unwired-signal.mjs`.  
- Test harnesses (arm A/B gates, node scripts).

**OPEN THREADS**  
- Backlog units: `U-RIE-ADAPTER`, `U-CALIBRATION`, `U-TRANSFER` (MS1 P1).  
- Refine bug‑finding wiki rule for edge cases; monitor merge failures under high commit pressure.  
- Enforce slot‑worktree migration fleet‑wide.  
- Narrow & implement Ollama offload scope.  
- Add telemetry to memory‑relevance inject hook (hit counter).  
- Resolve merge‑augmentations heap issue (increase Node heap or split job).  
- Run `validate-unwired-signal.mjs` to filter real orphans before wiring.  
- Wire 82 Lathe engines after validation.  
- Monitor drift‑checker for future log entries.
