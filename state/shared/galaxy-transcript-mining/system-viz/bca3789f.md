# system-viz session bca3789f (2026-05-19, 30.5MB, spine 132KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**

- **charlie**
  - `baef3c361d`: Dashboard offload‑rate fixed (10.9 % → 42.1 % real last‑24h).  
  - `94d4d0feac`: Wiki recall expanded to 22,734 leaf `.md` files via `wiki_lookup`.  
  - `doc-reflection`: Memory file + wiki append for two iterations.  
- **delta**
  - `ecbadc2862`: Added `obsidian_lookup` tool to Ollama bridge (U‑OBSIDIAN‑LOOKUP).  
  - `b756569c04`: Added `dispatcher_map` read‑only tool (U‑DISPATCHER‑MAP).  
  - `c55b68b8ea`: Fixed Docker compose production target, port filter, launcher guard (U‑INFRA‑DOCKER‑FIX).  
  - `b041693181`: Separated infra‑mutations from real Ollama routing; filtered fleet reaper noise (U‑REAPER‑COORD‑NOISE).  
- **lima**
  - `60dbc72116`: Adopted kilo’s stranded RTK rate‑limiter cluster (`rtk-prefix-reminder`).  
  - Iter 2/3/4 HTML+RTK upgrades completed: root‑doc guard, Windows‑correctness, hook coverage.  
  - (uncommitted) In‑progress HTML companion guard patch & tests.

**DECISIONS**

- Pivot to backend dev‑tool focus; expand Ollama bridge from 3 → 7 read‑only tools.  
- Keep L2b live MCP dispatcher blocked; shift to in‑process bridge enhancements.  
- Adopt stranded kilo cluster for RTK and HTML upgrades with honest co‑authorship.  
- Defer root‑doc guard patch until CLAUDE.md clean (avoid stale twin landmine).  
- Use `[MAIN]` commit prefix in shared tree; rely on worktree‑route hook.  
- Add `obsidian_lookup` & `dispatcher_map`; no MCP transport needed.  
- Fix Docker compose: add production stage, port‑conflict pre‑filter, launcher guard.  
- Remove noisy fleet‑reaper suggestions by filtering infra mutations.  
- Adopt R12 fail‑loud detection in `build-wiki-leaf-index`.  
- Enforce per‑file 2‑reviewer scrutiny and commit hygiene; use Co‑Authored‑By for cross‑slot work.

**OPERATOR DIRECTIVES**

- `/loop HTML+RTK system upgrades` – adopt stranded kilo cluster, finish RTK half then HTML half.  
- `/goal expand obsidian wiki + tribal knowledge injection + prism os + obsidian brain + ollama + system‑viz functionality. focus on back end development tools and improvements`.  
- `continue` (autonomous loop).  
- “Continue html and rtk upgrades then continue with obsidian wiki and context managements enhancements utilizing obsidian, ollama and system‑viz”.

**FINDINGS/BUGS**

- L2b transport blocked (`POST /mcp`) – prevented live MCP dispatcher usage; skipped.  
- Offload‑rate misleading (10.9 %) – corrected dashboard math, added keep‑breakdown.  
- Docker compose broken (missing production stage) – fixed with target, port filter, launcher guard.  
- Root‑doc twin landmine in HTML guard – deferred patch until CLAUDE.md clean.  
- R9 test gap for md‑to‑html hash – added 19/19 tests + CLI E2E.  
- Postgres port 5432 conflict → prism‑postgres won’t bind.  
- Silent no‑op exit‑0 regression in `build-wiki-leaf-index` under memory pressure (no write, no output).  
- Parser bug with legacy `triggers:` frontmatter – fixed on main; slot‑lima stale copy caused false positives.  
- Cross‑chat commit misattribution due to shared index.  
- Disk full ENOSPC during Bash/PowerShell tool outputs in worktree.

**DOMAIN SPECIFICS**

- Bridge tools: `viz_search`, `wiki_lookup`, `read_excerpt`, `obsidian_lookup`, `dispatcher_map`, `semantic_search`, `tribal_lookup`.  
- Dispatcher schemas: `electrode_ai_reason_full`, `electrode_advanced_analysis`, `electrode_deep_learning_analyze`, `electrode_ultimate_analyze`.  
- Engines/actions: `build-wiki-leaf-index`, `semantic_lookup`, `tribal_lookup`, `obsidian_lookup`, `dispatcher_map`, `reaper_coord_noise`.  
- Metrics: offload‑rate, keep‑breakdown, real routing vs infra mutations, Prometheus telemetry, Postgres master‑index health.  
- Paths: `knowledge/wiki/architecture/*` (leaf files), `mcp-server/data/docs/DISPATCHER_DIGEST.md`, `state/shared/{specs,research}/**.{md,html}`, `state/shared/dashboards/patches/*.{md,html}`, `_leaf-index.jsonl`.

**TOOLS USED**

- PRISM utilities: `chat-slots.mjs`, `/checkin` pipeline, `/startup` pipeline, `rtk-prefix-reminder.mjs`, `build-wiki-leaf-index`, `semantic_lookup`, `tribal_lookup`.  
- Hooks/skills: `slot-bind-enforce`, `handoff helper`, `duplicationGuardEngine.mustCheckBeforeCreating()`, per‑file scrutiny arms A/B, R12 fail‑loud detection.  
- Scripts: `stable-session-id.mjs`, `per-agent-handoff.mjs`, `precompact-pending-guard.mjs`, `milestone-tracker.mjs`.  
- Testing: `node:test` with hermetic mocks, real‑data E2E.

**OPEN THREADS**

1. Iter 2 HTML adopt – finish root‑doc guard patch (defer until CLAUDE.md clean).  
2. HTML‑PRIMARY follow‑ups – Windows‑correctness, lock‑guard, dispatcher mirror, double‑escape.  
3. RTK coverage gaps – ensure all hook paths exercised.  
4. Obsidian wiki enhancements – final integration of `obsidian_lookup` and `dispatcher_map` into user prompts.  
5. L3 agent loop (full plan→act→verify) – deferred to next session due to time constraints.  
6. AGENT_CHAT visibility on non‑zero exit (unit pending).  
7. Stage‑order assertion unit pending.  
8. Exit‑code constants export unit pending.  
9. `_stats.md` write protection unit pending.  
10. Streaming JSONL write unit pending.
