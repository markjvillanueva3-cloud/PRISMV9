# system-viz session 58bd7f4e (2026-05-17, 6MB, spine 7KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- VIZ‑COVERAGE‑MS0/U‑VIZ‑COVERAGE‑FIX (commit `d9f9cf5670`): replaced hardcoded engine counts with BUILD_STATE‑derived L5 nodes; added `meta.coverage`; removed legacy `eng.other`. Verified coverage invariants.  
- Path‑collision discovery (commit `f5bc835747`): identified that `generate-system-viz.mjs` and `regen-viz.mjs` both write `system-graph.json`, causing silent overwrite; documented regression.

**DECISIONS**  
- Build Engine Domains layer from `BUILD_STATE.COVERAGE_BY_DOMAIN.rows` to keep viz headline in sync.  
- Remove hardcoded domain array; use dynamic derivation for L5 nodes.  
- Add `meta.coverage` field (`{total,wired,unwired}`) to store coverage metrics.  
- Accept shared output path between `generate-system-viz.mjs` and `regen-viz`; plan separate `OUT_FILE` to avoid collision.

**OPERATOR DIRECTIVES**  
- `/loop [20m] continue work on system‑viz upgrades /goal`.  
- Force‑take echo slot with `--force true --confirmRecent true`.  
- Reclaim stale slots before claiming new one.

**FINDINGS/BUGS**  
- Path‑collision: both viz generators write `system-graph.json`; last writer wins, corrupting graph.  
- Host memory pressure during `regen-viz --full` causes OOM/SIGKILL.  
- Removed hardcoded 28%/898 drift from roadmap.

**DOMAIN SPECIFICS**  
- L5 Engine Domains: 41 nodes derived from `BUILD_STATE.COVERAGE_BY_DOMAIN.rows`; top‑40 domains + aggregated miscdomains.  
- `meta.coverage` field in viz data.  
- `system-graph.json` used by both a 20K‑node graph (`generate-system-viz.mjs`) and a 372K‑node fsCoverage graph (`regen-viz.mjs`).  
- System‑viz‑first audit doctrine, Obsidian‑PRISM‑OS routing.

**TOOLS USED**  
- `chat-slots.mjs` (reclaim/claim) for slot binding.  
- `audit-roadmap-drift.mjs`.  
- `generate-system-viz.mjs`, `regen-viz.mjs`.  
- Node scripts for pipeline delegation (`/checkin`).  
- `/loop`, `/goal` commands.

**OPEN THREADS**  
- Path‑collision fix: give `generate-system-viz.mjs` its own `OUT_FILE`; run `regen-viz --full` after.  
- SYSTEM‑VIZ‑FS‑COVERAGE‑MS1 Phase 1/2 close‑out debt (deferred files verified).  
- Monitor and mitigate memory pressure during full regen‑viz runs.
