# system-viz session 19dff632 (2026-06-09, 11MB, spine 93KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e44a3a1592` – wired 5 orphan `PayrollLiabilityFilingEngine` methods into `prism_business`.  
- `e649790e76` – added missing `payroll_remit_liability` case, fixed test harness.  
- `099e6b92bd` – committed HOTEL‑FORGE‑ROADMAP‑2026‑06‑09.md (dependency‑ordered units).  
- `9dfd621910` – released reusable Ollama transcript miner (`scripts/mine-hotel-transcripts.mjs`).  
- `f069052772` – verified‑only unit U‑HOTEL‑P0‑VERIFY‑SHIPPED.  
- Guard + detector files (203 insertions) – guard now passes all 20/20 tests.

**DECISIONS**  
- Use **Ollama** (`gpt-oss:20b`, `qwen2.5-coder:32b`) for bulk summarization and code grunt work.  
- Deploy **ultracode workflow** (8‑agent fan‑out, resumable via `resumeFromRunId`).  
- Commit to hotel slot with `[BOOTSTRAP‑SLOT‑ENFORCE]` prefix; enforce slot‑commit hook.  
- Apply R15 wiring‑close‑out: expose all orphaned engine methods before new build.  
- Prioritize cheap verify units (constants audit, mobile‑wire‑exempt) over heavy persistence work.  
- Seed a lead via `seedFromHints` so guard detects real wiring failures instead of empty‑store nulls.  
- Run 3‑of‑3 scrutiny gate against `--target HEAD` after committing new files to avoid ENOBUFS.  
- Broaden `PLACEHOLDER_RE` to match `"Unknown business action: X"`.  
- Add historical `{note:"method not callable"}` idiom for legacy error handling.  
- Update meta‑tests to assert real dispatcher output, not fabricated strings; include `afterAll` cleanup.

**OPERATOR DIRECTIVES**  
- `/compact` → start fresh context for next build unit.  
- Execute U‑HOTEL‑PORTAL‑PERSISTENCE after verification.  
- File remaining bug: Q2S DFM contract issue to charlie.  
- Resolve regression guard findings (`marketplace_lead_get` ambiguity, regex bug).  
- Filter RGS queue for hotel slot to eliminate off‑domain drift.

**FINDINGS/BUGS**  
- Stale audit: “missing tax engine” already built; 5 orphan payroll methods wired now.  
- Mobile engines: 6 previously thought orphaned are actually wired; only `MobileVoice` remains exempt‑tagged correctly.  
- Q2S DFM bug: non‑iterable `ctx.features` caused runtime error – filed to charlie.  
- Allowlist regression guard detected two false‑wire candidates (`marketplace_lead_get` null return, regex mis‑match).  
- Off‑domain drift: ~11/19 hotel sessions performed non‑ERP work due to unfiltered RGS queue.  
- Regex bug: `PLACEHOLDER_RE` (`unknown\s+(?:action|tool|command)`) does not match `"Unknown business action: X"`.  
- Probe param mismatch caused `getLead(undefined)` → null; fixed by using `{supplierId}`.  
- Meta‑test fed fabricated string, giving false R9 proof; must test against real output.

**DOMAIN SPECIFICS**  
- Engines: `PayrollLiabilityFilingEngine`, `CustomerPortalEngine`, `MobileVoice`.  
- Dispatcher: `businessDispatcher.ts` (`prism_business`), lazy `getEngine()` pattern, Zod validation per‑engine.  
- Routes: `/api/v1/business`, `/hotel-portal`, `/realtime`.  
- Data paths: `src/data/payroll-tax-tables.js`, `src/data/business-dispatch-allowlist.ts`.  
- Metrics (IRS reference): FUTA $72, W‑2 box4 $3,100, SS cap $10,918.20, Medicare addl $4,075.  
- Action: `marketplace_lead_get` → `MarketplaceSeedingEngine.getLead(supplierId)`.  
- Leads stored in `supplierId → lead` Map (lines 174/217).  
- Default dispatcher string: `"Unknown business action: X"` (businessDispatcher.ts:7641).

**TOOLS USED**  
- Ollama local LLM (`qwen2.5-coder:32b`, `gpt-oss:20b`).  
- Ultracode workflow (8‑agent fan‑out, resumable via `resumeFromRunId`).  
- Scripts: `scripts/mine-hotel-transcripts.mjs`, `ask-ollama.mjs`.  
- Slot helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
- Test harnesses: `businessDispatcher.payroll-filing-wire.test.ts`, `businessDispatchRoute.test.ts`.  
- PRISM guard & detector, seed API (`seedFromHints`), exec‑log tail, roadmap generator, reviewer prompt generator, Git commit workflow, 3‑of‑3 scrutiny gate.

**OPEN THREADS**  
- **U‑HOTEL‑PORTAL‑PERSISTENCE** – build persistence layer for 4 in‑memory maps.  
- **ALLOWLIST‑WRITE‑REVIEW / REALTIME‑VERIFY** – post‑persistence verification units.  
- **FALSE‑WIRE‑REGRESSION‑GUARD** – fix `marketplace_lead_get` ambiguity and regex bug, then re‑run guard.  
- Resolve off‑domain drift by filtering RGS queue for hotel slot.  
- Finalize minor tag corrections (e.g., mobile‑wire‑exempt).  
- Fix `PLACEHOLDER_RE`, update meta‑tests, add historical idiom `{note:"method not callable"}`.  
- Implement `afterAll` cleanup; verify reset API and confirm real default‑case string in businessDispatcher.
