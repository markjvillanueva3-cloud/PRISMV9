# system-viz session c478f3f9 (2026-05-25, 36.1MB, spine 125KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED (commits & tests)**  
- `U-BRIDGE-SFC-ESPRIT` 42a21c464d – 13✓  
- `U-PSN-UNLINKED-MENTIONS` 092ed84bfc† – 21✓  
- `U-PSN-ALIASES-FRONTMATTER` f6b5f0dce8 – data‑only, no tests  
- `U-PSN-CONNECTION-FINDER` 2245de0258 – 16✓  
- `U-PSN-GAP-FINDER` 6da49ecc6e – 12✓  
- `U-PSN-BLOCK-HEADING-LINKS` 7b45e1dac2 – 17✓  
- `U-PSN-MOC-LAYER` a5cc012fa1 – 9✓  
- `U-PSN-DEEP‑RESEARCH‑SYNTESIS‑2026‑05‑23` ff644c1e9d – 21✓ (synthesis index)  
- `U-PSN-MASTER‑INDEX‑ALIASES‑2026‑05‑23` ff644c1e9d – alias parser wired, 46 lib + 23 sidecar tests  
- `U-PSN-GRAPHITI‑LITE` – state/shared/episodes.jsonl created; lib & seeder tested (16✓)  
- `U-PSN-GRAPHITI‑DISPATCHER` – iter 12 wire, `/graphiti` skill live  
- `U-PSN-QDRANT‑REVIVE` – Qdrant online, 3 collections present (no vectors yet)  
- `U-PSN-QDRANT‑POPULATE‑2026‑05‑25` – scripts/populate-qdrant.mjs; 1 669 vectors → `prism_engines`: 3 866 points (green)  
- `U-PSN-OBSERVABILITY‑LEG` – `/observability` skill, 17/17 tests, hallucination & RAGAS metrics added  
- `U-PSN-MULTI-PROVIDER‑ROUTER` – `/route` skill, 29/29 tests, routes Claude/Gemini/GPT‑4.1/DeepSeek/Ollama  
- `U-PSN-MCP‑MANIFEST` – README + manifest for 103 dispatchers (24/24 tests)  
- `U-PSN-GRAPHITI‑SEED‑ENTITY‑FIX` – fixed `--name-only`; entities populated in 3 commit episodes  
- `U‑PSN‑HYBRID‑RETRIEVAL‑WIRE` – hybridSearch() lib, CLI scripts/prism-hybrid.mjs, `/hybrid` skill; 44/44 tests  
- `U‑PSN‑QDRANT‑PAYLOAD‑DEBUG` – canonical `engine:Foo` IDs in hits; 50/50 tests  
- `U‑PSN‑HYBRID‑VIZ‑ROOST` – ghost node generator for `/system-viz`  
- `U‑PSN‑HYBRID‑VIZ‑ROOST‑WIRE` – spliced into regen‑viz.mjs & merge‑augmentations.mjs  
- `U‑PSN‑GRAPHITI‑SEED‑EXPANDED` – episode store 7 → 2 004 (286×)  
- `U‑PSN‑GRAPHITI‑SEED‑TESTS` – updated test helper, 17/17 PASS  
- `U‑PSN‑HYBRID‑RETRIEVAL‑WIKI` – wiki architecture page  
- `U‑PSN‑HYBRID‑MCP‑WIRE` – added `prism_session:hybrid_search` MCP action  

**DECISIONS**  
- Use pure‑Node JSONL episode store (Pattern B) to avoid external DBs.  
- Adopt `W_ALIAS=3.0` scoring in memory-index-search-lib; synthetic pointer exclusion (`_synthetic/`).  
- Enable Qdrant dense vector layer; populate via CLI, use FNV‑1a hash for deterministic point IDs, 768‑dim nomic‑embed-text vectors.  
- Wire MCP dispatcher for Graphiti after core lib stable (iter 12).  
- Stop obsidian memory feed with stamp file `obsidian-memory-feed-last.json`.  
- Four‑substrate hybrid retrieval (memory, master, episode, vector) with RRF fusion k=60; expose via CLI, skill `/hybrid`, `/system-viz`, MCP.  
- Consolidate documentation into single wiki page per doc‑reflection rule.

**OPERATOR DIRECTIVES**  
- Stop hook active: `[ build anything and everything, do deep research … | goal clear: build, wired and synergized to PSN and /system-viz ]`.  
- Bypass feeder only when authorized; use stamp‑file forward‑dating.  
- Execute parsed prompt immediately after scheduling; never ask for clarification.  
- Ensure all layers are shipped and synergized to the PSN system (repeat until complete).  
- Build prism MCP tools and prism_safe tools for high ROI.

**FINDINGS/BUGS**  
- `system-graph.json` drift: 0 fsCoverage namespaces, orphan `.tmp.system-graph.json`.  
- Misattribution bug (`index.lock` race) caused commits to land in wrong chat.  
- Feeder race (H writes overwritten by C sync); resolved via stamp‑file pause.  
- Qdrant health script reads `vectorCount` vs `points_count`.  
- Graphiti episode store had empty `entities[]`; fixed with `--name-only` parse bug.  
- Corrupt Git tree (`e36809bbd2`) blocked `--name-only`; resolved with `--no-files`, left empty `entities[]`.  
- OOM in regen‑viz V8 prevented final hybrid roost render.  
- Silent test regressions from RECSEP change; fixed in Iter 24.  
- Pre‑existing test failures due to wrong tie‑breaker assertion; corrected.

**DOMAIN SPECIFICS**  
- PSN legs: 13 (Obsidian, PRISM OS, Wiki, Memories, Tribal, System‑Viz, Engines, Algorithms, Formulas, NN/GNN, AI‑AI, Observability, Multi‑Provider Router).  
- Memory index weights: `W_NAME`, `W_DESC`, `W_BODY`, `W_ALIAS=3.0`.  
- Episode store schema: `{id, source, source_id, body, valid_from, valid_until?, entities[], relationships[], superseded_reason?}`.  
- System‑Viz roosts: ghost‑episode nodes added iter 12; hybrid retrieval layer (BM25 + vector + graph + episode) live after Qdrant populate.  
- Engines: `EmbeddingGuardEngine`, `LocalEmbeddingEngine`.  
- Actions/dispatchers: `prism_session:hybrid_search` (MCP), `/hybrid` skill, `scripts/prism-hybrid.mjs`.  
- Metrics: RRF rank scores; per‑source provenance surfaces `{memory:1, master:1, vector:1}`.  
- Key paths: `scripts/lib/hybrid-retrieval.mjs`, `scripts/generate-hybrid-retrieval-features.mjs`, `state/shared/system-viz/hybrid-retrieval-augmentation.json`.

**TOOLS USED**  
- `/checkin-sierra` wrapper, `chat-slots.mjs`, `CronCreate`, Skill tool, `mcp-server`, graphiti-lite lib, `scripts/populate-qdrant.mjs`, `memory-index-search-lib`, `master-index-precheck-inject.mjs`, `wiki-link-backfill`, `unlinked-mentions-scan`.  
- PRISM CLI: `scripts/prism-hybrid.mjs`.  
- Hybrid retrieval library: `scripts/lib/hybrid-retrieval.mjs`.  
- MCP server dispatcher: `mcp-server/src/tools/dispatchers/sessionDispatcher.ts`.  
- Regeneration utilities: `regen-viz.mjs`, `merge-augmentations.mjs`.  
- Episode seeder: `scripts/seed-episodes-from-git.mjs`.  
- Test helper: `buildGitOutput` in seed‑tests.

**OPEN THREADS**  
1. Ingest formulas & skills into Qdrant (`prism_formulas`, `prism_skills`).  
2. Repair corrupt Git tree (fsck, `--no-files`) to enable full file‑entity ingestion.  
3. Build Tier‑2 MCP tools: `episode_query`, `rrf_merge`, `embed`, `rag_construct`, `psn_health`, `engine_recommend`, `slot_query`.  
4. Mirror `prism_safe:hybrid_search` for tenant isolation.  
5. NN/GNN AUROC retraining for leg 10.  
6. Resolve V8 OOM in regen‑viz to render final hybrid roost visualization.  
7. Compose BM25, vector, graph, episode into single `/search` API (hybrid retrieval wire).  
8. Build and register `prism_graphiti:*` actions in Graphiti MCP dispatcher.  
9. Implement reasoning‑trace leg: trace‑back through episodes.  
10. Ensure ghost‑episode nodes render correctly with new PSN legs; address V8 OOM in regen‑viz.
