# system-viz session e6145e8b (2026-05-22, 23.6MB, spine 132KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED (builds/commits)**
- `U‑SLOT‑TASK‑CLAIM‑DRIFT` – commit dfd672046a: 42/42 unit +5/5 e2e PASS, 3‑of‑3 scrutiny.
- G1 type‑backfill (`system-viz-high-roi-ms0`) – shipped & verified.
- G2 query‑telemetry – 25/25 tests, 195 live log records.
- G4 dead‑pixel‑detector – shipped; duplicate `dead-pixel-sweep.mjs` removed.
- `U‑VIZ‑MS‑ENVELOPE‑NODES` – commit 673132a79c: 26/26 test PASS, 3‑of‑3 scrutiny.
- `U‑VIZ‑SLOT‑TOUCH‑HEATMAP` – commit 19912d204c: 32/32 tests, 3‑of‑3 scrutiny.
- G3 Ghost‑Wire Validation Feedback Loop – 11/11 tests, wired into `merge‑augmentations.mjs` & `regen‑viz.mjs`; 2‑of‑2 scrutiny PASS.
- G5 Tribal‑Density Heatmap Roost – core generator, 10/10 tests, atomic commit; wired into augmentation pipeline.

**DECISIONS (architecture/scope + why)**
- Use `chat-slots.mjs`’s `SLOT_NAMES` as authoritative source; drop hard‑coded VALID_SLOTS.
- Adopt `generator → merge‑augmentations → regen‑viz FAST[]` pipeline for all viz‑augmentation units (G3–G8).
- Envelope generator collision handling: throw → deterministic numeric suffix; update tests accordingly.
- Slot‑touch augmentation uses `ghost.slot_activity.<name>` roost to avoid ID collisions with `ghost.slot_synergy`.
- All new units ship with ≥3 failure modes + ≥2 adversarial tests, no `toBeDefined()` stubs; wired into pipeline before commit.
- Use slot‑binding wrapper (`slot-bind-enforce.mjs`) for deterministic claim of the *sierra* slot pre‑`/startup` or `/loop`.
- Commit prefix `[MAIN]` for all shared‑tree commits; chain `git add && git commit` atomically to avoid peer absorption window.
- Adopt overlay vs newNodes merge conventions per augmentation site (annotations/edges vs newNodes/newEdges).
- OOM mitigation: run `regen‑viz.mjs --max-old-space-size=16384`.

**OPERATOR DIRECTIVES (verbatim asks)**
- `/goal [ complete all remaining tasks and units for sierra task queue … ]`
- `/loop [5m] /goal` – autonomous loop to finish G3–G8 units.
- Resume directive: finish `validate‑ghost‑wires.mjs`, wire into all three sites of `merge‑augmentations.mjs`, add to `regen‑viz FAST[]`, run tests, scrutiny, commit `[MAIN] [SYSTEM-VIZ-HIGH-ROI-MS0]/U-VIZ-GHOST-WIRE-VALIDATE`.
- Preserve `--source live-chat` flag on all per‑agent handoff writes.

**FINDINGS/BUGS**
- VALID_SLOTS drift fixed; test rewritten to use `SLOT_NAMES`.
- Duplicate dead‑pixel detector removed.
- Envelope generator collision throw replaced with numeric suffix; stale collision‑throws test updated.
- Slot‑touch `resolveFsNodeId` climb bug fixed (stop at one level under prism).
- Peer commit `d86d5925a8` changed scrutiny policy to 2-of-2; CLAUDE.md still shows 3-of-3 – needs edit.
- Test file for envelope generator missing after context cap – must be recreated.
- Slot‑task‑claim drift: hard‑coded 12 slots vs 26 resolved via `SLOT_NAMES`.
- Stale slug‑collision test in milestone‑envelope replaced with numeric suffix disambiguation.
- Peer absorption window caused untracked files to merge into other commits; fixed by atomic add/commit.
- OOM on regen‑viz due to 425 MB graph – addressed with larger heap flag.
- Misattribution of wiring delta in G3 commit documented in memory file.

**DOMAIN SPECIFICS**
- Ghost‑wire schema: `ghost.unwired-engine` nodes (`proposed_at`, `proposed_wiring`, `confidence`) + edges `relation:"proposed-wire"`.
- MCP tool → disp node id map (16 entries, e.g., `prism_calc→disp.calcdispatcher`).
- Dispatcher files live in `mcp-server/src/tools/dispatchers/<camelCaseName>.ts`.
- System‑viz graph size ~405 MB; memory‑pressure block at G9/G10.
- Augmentation pipeline: generator → optional load → version stamp → `mergeIndexedAugmentation` (3 sites) → `regen-viz FAST[]`.
- Milestone‑envelope generator: numeric suffix disambiguation, emits `ms-envelope.<slug>` nodes and `contains` edges to `core.milestones`.
- Tribal‑density generator: scans tip frontmatter, buckets into hot/warm/cold per domain; outputs `ghost.tribal_density` roost.
- Metrics: ghost scan counts 636, confirmed/pending/unresolvable stats, AUROC retrain feedback.

**TOOLS USED**
- PRISM helpers: `chat-slots.mjs`, `slot-task-claim.mjs`, `merge‑augmentations.mjs`, `regen‑viz.mjs`, `per-agent-handoff.mjs`, `precompact-pending-guard.mjs`, `scrutiny-3way.mjs`.
- Scripts created/modified: `generate-milestone-envelope-atomic.mjs`, `generate-slot-touch-augmentation.mjs`, `validate-ghost-wires.mjs` (and its test).
- Generators: `scripts/generate-wiki-tribal-features.mjs`.
- Testing framework: Node test runner (`node --test`).
- Git workflow: atomic `git add && git commit`.

**OPEN THREADS**
- G7 Master‑Index Sidecar Incremental – design & implementation pending; risk of fleet‑wide post‑commit hook breakage.
- G8 Post‑Commit Hook – high blast radius across all 26 chats; pending implementation.
- Milestone‑Envelope Generator – replace stale slug‑collision test, run tests, commit `[MAIN]`.
- G5 Tribal‑Density Heatmap – confirm data source (tribal‑embed-index.json vs mixed wiki+tribal).
- G9/G10 memory‑pressure blocked (405 MB graph); require out‑of‑session retrain or larger heap strategy.
- Peer commit changed scrutiny policy to 2-of-2; CLAUDE.md update required.
