# system-viz session c7361c9f (2026-06-10, 72.9MB, spine 495KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `ascii‑guard.mjs` enforcement (commit 86b4bf86) – smart‑punctuation drift prevention.  
- MCP heap floor 24 GB & watchdog RSS threshold 18 GB for Blackwell (commit ed94bc47).  
- Concurrency‑aware semaphore + inflight metrics on `/mcp`; watchdog defers restarts during bursts (commit ed94bc47).  
- `U-MCP-CONCURRENCY-HARDEN` – 3‑commit MCP hardening: `/health` concurrency metrics, `RequestSemaphore` (64 concurrent builds + 512 queue, HTTP 503 backpressure), watchdog RSS preempt defer.  
- G7 audit fixed crash‑critical “Zombie Reaper v2” disabled.  
- G10 auto‑reenable guard (`selectReenableTargets`, `reenableTasks`) for scheduled tasks.  
- Golf galaxy completion plan v1→v2 committed (state/shared/golf-galaxy-completion-plan-2026-06-09.md).  
- `U-GOLF-TASK-OWNER-MAP` (commit 5dc70e36bd) – forward & reverse owner‑map guard.  
- Reverse‑completeness guard for crash‑critical tasks (commit 13596741a4).  
- Production heap env‑tunable script (`U-PROD-HEAP-ENV-TUNABLE`, commit 52fc2bd0fe).  
- GPU‑VRAM admission guard lib + hook (commit f3eb0c1c15) – footprint `params * 650 + 2048` MiB, 90 % pressure floor.  
- vLLM migration plan only (commit c2b86de650).  
- MCP server hardening: 5 validated fixes (`index.ts`, `bridge.ts`, `responseSlimmer.ts`, `eventBus.ts`, `transportError`) – all tests pass.  
- MCP‑server depth‑guard refinement (commits 63d08b5eed, 5181350fd7).  
- Bravo lone‑surrogate 400 error fixed: surrogate‑safe truncate lib, slot‑soul‑inject hardened, fallback regex corrected (commits c1a50b7c99, 3495a1dc6a).  
- BaseRegistry/Logger restored with lightweight Logger class; atomic persist & single‑flight init fixes (commit 62fe49af70).  
- Express.json body limit raised to 50 MB, env‑overridable (`PRISM_MCP_BODY_LIMIT`) (commit 361725ffc2).  
- UnhandledRejection opt‑in flag `PRISM_MCP_FATAL_REJECTIONS` added, default off (commit 73d6fa49ea).  
- GPU‑VRAM guard already wired for block mode; operator can enable via `PRISM_VRAM_GUARD_MODE=block`.  

**DECISIONS**  
- Fleet‑reaper ownership permanently moved to slot **golf**; always run on `/checkin-golf`.  
- Disable legacy allowlist hook for golf slot writes.  
- Claim golf slot without `--golf`; treat as normal work slot.  
- Stop NIM containers; keep Ollama primary offload.  
- Enforce ASCII guard on code files to prevent smart‑punctuation drift.  
- Tune MCP heap floor & watchdog RSS threshold to match 136 GB RAM, eliminating frequent disconnects.  
- Add inflight metrics to `/health`; make watchdog concurrency‑aware; cap concurrent `buildRequestServer()` calls.  
- Adopt bounded concurrency (≤3–4 agents) workflow to avoid 9‑agent rate‑limit failure.  
- Use `RequestSemaphore` with explicit “abandoned” handling for client disconnects while queued.  
- Replace stale G5 premise with existing TTL‑based orphan stamp sweeper (`active-chat-priority-decay.mjs`).  
- Treat G1 premise as false; commit only safe, local `.git/info/exclude` patterns (no broad gitignore).  
- Implement auto‑reenable guard in `fleet-task-health-watch.mjs`.  
- Remove blanket `migrationFreezeActive` gate from `selectReenableTargets`; crash‑critical tasks must re‑enable.  
- Keep freeze‑excuse only for non‑load‑bearing tasks; crash‑critical remain degraded.  
- Add native Ollama probe before Docker in Stop hook to avoid suppression by Docker‑down state.  
- Build GPU‑VRAM guard first; enforce VRAM safety before vLLM deployment.  
- Generate comprehensive vLLM migration plan via ultracode/Ollama; defer actual build until operator approval.  
- Warn entire fleet about upcoming vLLM POC and GPU contention.  
- Keep fleet reapers/monitors active and run autonomous bounded reap‑checks.  
- Use explicit pathspec commits to avoid shared-index race and guard‑unstaged files.  
- Adopt surrogate‑safe truncation across all naive `.slice` sites (`String.prototype.toWellFormed`).  
- Increase express.json limit to 50 MB, expose via `PRISM_MCP_BODY_LIMIT`.  
- Introduce opt‑in exit on unhandledRejection (`PRISM_MCP_FATAL_REJECTIONS`).  
- Keep GPU‑VRAM guard in warn mode by default; operator can switch to block.  

**OPERATOR DIRECTIVES**  
- Start Qdrant, Docker, WSL, Ollama; ensure all active.  
- Verify fleet reapers are running post‑upgrade.  
- Return to git tree work; activate chat‑fleet commit slots across whole fleet; resolve conflicts.  
- Enforce ASCII guard and text rules; focus on em dashes, smart punctuation.  
- Utilize Ollama for task efficiency whenever viable.  
- Improve MCP server performance to eliminate disconnects; leverage new PC hardware.  
- Account for fleet spawning parallel agents.  
- Keep system in ultracode mode; launch workflow agents with unlimited capacity.  
- Use `/startup-golf` only when auto‑pin missed; force‑take golf slot, reclaim stale slots, claim with `--force true --confirmRecent true`, then run `/fleet-reaper`.  
- After `/startup-golf`, run `/fleet-reaper` before any startup pipeline.  
- AUTO‑FIX INLINE – fix bugs, stale tests, wiring on the fly.  
- BUILD FOR BLACKWELL – target RTX 6000/9950X3D/136 GB RAM.  
- Comprehensive‑build discipline: R12 fail‑loud, R13 comprehensive route, R15 WIRE→TEST→VALIDATE→ALL‑GALAXIES.  
- Read three articles via ultracode; assess/plan/implement golf units.  
- Warn the whole fleet about vLLM POC and GPU contention.  
- Confirm durable Fleet Reaper remains enabled.  

**FINDINGS/BUGS**  
- Legacy allowlist hook blocks non‑hygiene writes.  
- NIM containers saturate GPU, downgrade router; removed.  
- ASCII guard not enforced → smart punctuation drift in code files.  
- MCP watchdog restarts at 3 GB RSS on new hardware → frequent disconnects.  
- Concurrency gap: each `/mcp` request spawns a fresh `McpServer`; watchdog is concurrency‑blind, causing mid‑burst restarts and slot‑leak when client disconnects while queued.  
- Slot routing fully active; backlog of ~16 k untracked wiki/memories entries (knowledge base).  
- CRLF→LF line‑ending churn inflates uncommitted file count (~6 k modified).  
- RequestSemaphore leak when client disconnects while queued – fixed.  
- G5 premise “active‑chat‑boost” stale; orphan stamp cleanup exists.  
- G1 premise “22k untracked = noise” false – genuine commit backlog; only safe local exclusions applied.  
- Drift test #69 failed due to over‑reach adding synthesis‑cron names to `KNOWN_PRISM_TASKS`; reverted.  
- Index.lock contention during G10 revert commit – resolved by retrying after peer git ops finished.  
- MCP daemon pileup (duplicate non‑serving daemons) – fixed via singleton-service-guard --fix.  
- GPU‑VRAM guard missing – identified as high‑ROI feature for Blackwell box.  
- vLLM migration plan pending; requires infra change to serve Gemma 4 with fp8/prefix‑caching on RTX 6000.  
- `docker pull vllm/vllm-openai:latest` exits 0 but image never commits due to manifest corruption – needs Docker Desktop restart.  
- Logger stub in `utils/Logger.ts` causes BaseRegistry constructor failure; flagged as breaking change.  
- Lone‑surrogate 400 errors caused by mid‑emoji `.slice(0,N)` truncation; fixed globally.  
- Express.json default limit (100 KB) silently returned 413 for larger payloads; raised to 50 MB.  
- UnhandledRejection logs silent; added opt‑in exit flag.  

**DOMAIN SPECIFICS**  
- Engines/dispatchers: fleet‑reaper, fleet-memory-monitor, MCP server (`buildRequestServer`), local‑llm‑bridge (NIM first cascade), ascii‑guard hook.  
- Metrics: `slots["owned-by-crashed"]`, GPU stats (`gpu.freeMb`, `gpu.utilizationPct`), Ollama cost router categories (`cheap`, `synthesis`, `code/reasoning`).  
- Paths: `/checkin-golf`, `/mcp`, `/health`, `fleet-task-health-stop.mjs`.  
- MCP Server files: `index.ts`, `request-semaphore.ts`, watchdog preempt decisions scripts.  
- Fleet‑Reaper skill, host presets (`PRISM_FLEET_REAPER_*`).  
- Active‑Chat Priority: `active-chat-priority-boost.mjs`, `active-chat-priority-decay.mjs`.  
- Fleet‑Task‑Health: `fleet-task-health-watch.mjs`, `fleet-task-health-stop.mjs`; crash‑critical vs expected‑disabled partitioning.  
- Ultracode workflow engine (`wsvw240ov`, etc).  
- GPU VRAM guard: PreToolUse hook (`gpu-vram-admission-guard.mjs`), footprint estimate, fail‑open mode.  
- vLLM spec: Qwen/Qwen3‑Next‑80B‑A3B‑Instruct; fp8 quantization ~43 GB reservation, port 8020, OpenAI API compatible.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `fleet-reaper-sweep.mjs`.  
- Hooks/dispatchers: `ascii-guard.mjs`, `encoding-guard.mjs`, `fleet-task-health-watch.mjs`, `mcp-server-supervisor.mjs`, `mcp-server-watchdog.mjs`.  
- Settings: `C:/Users/wompu/.claude/settings.json` (c‑to‑h mirror).  
- Scripts: `git`, `node`, `npm run build`, `tsc`, `vitest`, `node:test`.  
- Docker CLI & Compose.  
- Ultracode workflow harness.  
- Cron jobs (`e12cf005`, `c95fc33c`).  

**OPEN THREADS**  
- Finalize concurrency‑aware watchdog & semaphore fix; monitor long‑term MCP heap tuning under sustained bursts.  
- Commit or ignore backlog of ~16 k wiki/memories untracked files; resolve CRLF→LF churn.  
- Resolve Docker Desktop restart to complete vLLM image pull.  
- Finalize MCP server fixes after Logger stub issue; confirm all 5 hardening patches pass.  
- Build GPU‑VRAM guard first; enable block mode if operator approves.  
- Generate and review comprehensive vLLM migration plan via ultracode/Ollama; defer build until approval.  
- Wire & test unwired engines / wiring bridges once fresh context available.  
- Evaluate removal of `PRISM_ALLOW_UNWIRED=1`.  
- Schedule next fleet‑hygiene reap‑check (autonomous, bounded).  
- Await operator decision on vLLM go/no‑go; monitor GPU contention.
