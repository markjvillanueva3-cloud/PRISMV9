# system-viz session 2afa1e56 (2026-05-25, 39.8MB, spine 96KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- AISystemRouterEngine extended with `blueprint_extraction` & `corpus_harvest` tasks (commit fb15ea5bad).  
- Wiki pages for BlueprintExtractionRAG, BlueprintCorpusHarvest, BlueprintLoRABridge, JMDieArchiveBackAnnotation written (commit 5525c14ab7).  
- knowledgeDispatcher wired with 6 corpus‑harvest actions; aiReasoningDispatcher wired with 4 blueprint‑lora actions (post‑bbdeeb5c45).  
- Memory file for PSN synergy memo auto‑fed to Obsidian.  
- System‑viz regeneration upgraded: streaming JSON I/O (`scripts/lib/graph-io.mjs`), merge‑augmentations refactored, generate-engine-wiki migrated; 4 missing engine pages regenerated. Passes with zero failures.  
- NN tier‑5 AUROC raised from 0.3833 to 0.5 after `loadEmbeddingFeatures` change; gating fixed across 6 graph‑load sites.  
- Bridge Path‑2 resolver added (`candidateBasenames`, `buildEngineWikiBasenameIndex`, `nodeToEmbeddingRow`) – full test coverage 60/60.  
- Engine wiki embedder script created; first 200 embeddings committed; detached process running for remaining 3,338 engines (stalled at 400/3338).  
- College‑course nodes integrated: 1,483 college‑related nodes cross‑walk to PSN legs documented (`knowledge/wiki/architecture/college-courses-psn-incorporation.md`).  
- Deep‑learning / deep‑reasoning training substrate built: `psn-deep-learning-reasoning-training-substrate.md`; `scripts/build-psn-training-corpus.mjs` orchestrator (9‑leg); test PASS 10/10; live build → `state/shared/training/*.jsonl` – 369,734 rows in 22.7 s.  
- 46 V8 graph‑read crash fixes applied + idempotent migration tool.

**DECISIONS**  
- Adopt streaming JSON I/O to eliminate V8 max‑string‑length crashes in system‑viz regen and all graph loads.  
- Accept asymptotic dimensional accuracy; focus on logged coverage, validator‑trace, self‑consistency proofs.  
- Use CLAUDE.md §GOAL‑COMPLETE GATE escape hatch for data‑side gaps (missing engine wiki embeddings).  
- Prioritize filling tribal‑embed index with engine wiki embeddings to lift NN tier‑5 AUROC above 0.78.  
- Adopt R3 top‑10 picks + deep‑reasoning doctrine to define data‑source × PSN‑leg mapping; built orchestrator script for one‑line extraction.  
- Eliminate legacy `GRAPH` callsite crash pattern via systematic V8 class removal.  
- Use ghost‑roost generators & node‑card cheap reads for efficient graph regeneration.

**OPERATOR DIRECTIVES**  
- Monitor detached processes: `phase18-v6`, `embed-engines-detached.log`; restart engine‑wiki embedder as needed.  
- After all embeddings finish, run `nn-graph-retrain-lifecycle.mjs --force` to obtain final AUROC.  
- Finalize college‑course PSN cross‑walk; implement `U-COURSE-FORGE-P1-DISPATCHER` and MCP wiring.  
- Add deep learning & deep reasoning training for entire `/system-viz`, PSN and all domains.  
- Conduct further research on deep learning, deep reasoning, AI systems, neural nets, GNNs, Obsidian, `/system‑viz` to identify additional synergies.

**FINDINGS/BUGS**  
- V8 max‑string‑length crash on JSON.stringify >512 MB graphs fixed via streaming I/O; system‑viz regen now passes.  
- Ollama `/api/chat` dead/cold‑load timeout due to VRAM contention with NIM endpoints (not critical).  
- `validate-ghost-wires.mjs:264` regex missed variable name – pending fix.  
- Engine‑wiki embedder stalled at 400/3338 (resource contention) – needs restart/resource tuning.  
- Commit of 46 V8 fixes had staging issues; selective staging now resolved.  
- `U-COURSE-FORGE-P1-DISPATCHER` operator‑gated MCP wiring incomplete.

**DOMAIN SPECIFICS**  
- Engines: AISystemRouterEngine, BlueprintExtractionRAGEngine, BlueprintCorpusHarvestEngine, BlueprintLoRABridgeEngine, JMDieArchiveBackAnnotationEngine.  
- Dispatchers: knowledgeDispatcher (corpus harvest), aiReasoningDispatcher (blueprint_lora), system‑viz dispatcher.  
- Metrics: NN tier‑5 AUROC (0.5 → target > 0.78), Brier, macroF1; validator‑trace tiers; logged coverage percentages.  
- Key paths: `H:/prism/state/shared/system-viz/system-graph.json`; `scripts/lib/graph-io.mjs`; embed‑engines script path.  
- PSN Incorporation Research R3 – 11 legs, cross‑domain mapping; deep‑reasoning doctrine (4‑tier model ladder); CAD & CAM Deep AI modules.  
- Core subsystems: ghost‑wires, engine‑wiki embedder, fleet search substrate.  
- GNN focus: HGT replacing GraphSAGE (unbuilt but high leverage).

**TOOLS USED**  
- PRISM core tools: AISystemRouterEngine, knowledgeDispatcher, aiReasoningDispatcher, generate-engine-wiki.mjs, merge-augmentations.mjs, system‑viz regen scripts.  
- Graph I/O library (`scripts/lib/graph-io.mjs`); node testing framework (`node --test`).  
- Detached Windows processes via `Start-Process -WindowStyle Hidden`.  
- Ollama embeddings API (`/api/embeddings`); Playwright for tweet fetch.  
- Ghost‑roost generators, node-card cheap reads, cross-substrate edges.  
- `scripts/build-psn-training-corpus.mjs` & test.

**OPEN THREADS**  
- Complete engine wiki embedding to fill tribal‑embed index → raise NN tier‑5 AUROC above 0.78; restart/tune embedder.  
- Final NN tier‑5 promotion after data fill.  
- Resolve `validate-ghost-wires` regex bug.  
- Resolve Ollama cold-load timeout (VRAM allocation).  
- Finalize MCP wiring for `U-COURSE-FORGE-P1-DISPATCHER`.  
- Draft R5 spec: multimodal LLM adapters, sparse autoencoder interpretability, HELM-style evaluation, diffusion-language modeling.  
- Build & integrate HGT GNN module into PSN.
