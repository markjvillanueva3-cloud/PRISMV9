# system-viz session 5c91e392 (2026-06-09, 16.2MB, spine 150KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Resumable corpus loop (`ocr-training-loop-lib.mjs`) with cursor file `processed-cursor.jsonl`; reaper‑survivability.  
- Manifest generator for 7 794 drawing‑role PDFs; PDF rasterizer (`pdf-to-png.py`) now processes all pages of multi‑page PDFs (96 % contamination).  
- Run‑as‑main guard added to CLI scripts (`blueprint-ocr-training-loop.mjs`, `xray-trainset-to-lora.mjs`).  
- Downstream dedup in `xray-trainset-to-lora.mjs` (key+image last‑wins); string‑score coercion for `print_score`.  
- Worklist comment line skip logic; reaper‑immune scheduled task launcher (PowerShell wrapper + installer) for unattended closed‑loop training.  
- GPU environment unblocked: torch 2.11+cu128, qlora, bitsandbytes 4‑bit; 32‑core CPU rasterization.  
- Ensemble validated with two reliable VLMs (`qwen3-vl:8b-instruct`, `qwen2.5vl:7b`); LoRA trainer self‑test passed.  
- Commit 250ffb8a6e: `find-perfect-parts.mjs`; 10/10 mutation tests; markdown & JSON report of 91 perfect parts (print + CAD + CNC). Final gate closed.

**DECISIONS**  
- Use resumable loop with cursor to avoid infinite GPU burn under reaper pressure.  
- Rasterize every page of multi‑page PDFs; keep OCR for text‑layer docs out of VLM ensemble, use cheap read instead.  
- Run closed‑loop training as unattended scheduled task to evade foreground reaper kills.  
- Limit ensemble to two reliable VLMs; other models timeout or return empty responses.  
- Use 32‑core CPU for rasterization/parsing, GPU for vision inference.  
- Adopt CNC program as ground truth; add binary guard (`isParsableNcText`) and feed‑move aware GT extraction to avoid inflated toolpath counts.  
- Sanitize filenames before `basename` to resolve scanned PDFs correctly.  
- Correct axis classifier regex to distinguish mill vs lathe programs.  
- Cluster diameters to reduce roughing‑ramp inflation in callout‑class denominator.  
- Run “true‑test” on 91 perfect parts first; defer full corpus until OCR recall improves.

**OPERATOR DIRECTIVES**  
- Upgrade OCR/blueprint reading with new GPU and CPU; wire, test, then begin closed‑loop training on all prints (jm folder/docustra).  
- Execute true‑test harness on the 91 parts; compare OCR dimensions to CNC GT.  
- Verify findings, commit results, report OCR recall/precision before scaling.

**FINDINGS / BUGS**  
- Full‑page rasterization fixed ~76 % missing dimension pages from page‑0 only OCR.  
- Cursor survived reaper kills; re‑OCR count = 0.  
- GPU contention caused VLM timeout during calibration; schedule quieter window.  
- Worklist comment lines miscounted as missing prints; now skipped.  
- String‑score values coerced to numbers.  
- Server‑side rate limiting blocked reviewer gate; resolved by retrying.  
- Binary `.mcx‑8` files produced garbage coordinates → fixed with `isParsableNcText`.  
- Feed‑move aware extraction reduced GT from 224 toolpath points to ~28 callout dims, eliminating inflated recall denominator.  
- Resolver bug: sanitized filenames before `basename`; now resolves scanned PDFs correctly.  
- Axis classifier regex corrected; mill programs no longer misclassified as lathe.  
- OCR recall on real scanned PDFs is 0 (1–3 dims read vs 7–14 GT); system not ready for full‑corpus run.

**DOMAIN SPECIFICS**  
- Engines / Actions: `ocr-training-loop-lib.mjs`, `pdf-to-png.py`, vision ensemble runner, LoRA trainer (`blueprint_vl_train_lora.py`), `xray-trainset-to-lora.mjs`, `find-perfect-parts.mjs`.  
- Dispatchers / Hooks: `chat-slots.mjs`, `slot-bind-enforce.mjs`, `/checkin` pipeline.  
- Metrics / Paths: cursor file `processed-cursor.jsonl`; trainset rows (`key`, `image`, `dims`); metrics `trainable_labels`, `tier_totals`, `active_learning_queue`. 7 794 PDFs processed.  
- Unique paths: `H:/prism/.claude/...`, `mcp-server/scripts/blueprint_vl_train_lora.py`.  
- Blueprint OCR pipeline uses Qwen2.5‑VL LoRA trainer chain on RTX PRO 6000 GPU; closed‑loop training loop generates trainset rows from prints, CAD, CNC programs.  
- PRISM system‑viz components: dispatcher actions (`cad_step_parse_file`, `findOne`), find‑perfect‑parts script.

**TOOLS USED**  
- PRISM tools: `chat-slots.mjs`, `slot-bind-enforce.mjs`, `/checkin` pipeline, vision-model-select.  
- Scripts / Hooks: `ocr-training-loop-lib.mjs`, `xray-trainset-to-lora.mjs`, `pdf-to-png.py`, PowerShell wrappers (`run-ocr-batch-overnight.ps1`, installer), `find-perfect-parts.mjs`.  
- Dependencies: torch 2.11+cu128, qlora, bitsandbytes 4‑bit, transformers, peft, trl, datasets.  
- GPU: RTX PRO 6000 Blackwell.

**OPEN THREADS**  
- Continue full corpus training via scheduled task; monitor GPU contention.  
- LoRA fine‑tune with real trainset and HF base model pull (quiet GPU window).  
- Generate operator gold labels for domain‑shift gate; need real‑scan GT.  
- Optimize ensemble concurrency; add more reliable VLMs if available.  
- Final deployment of trained model after validation.  
- Improve OCR recall on real scanned prints before full‑corpus run.  
- Validate and extend true‑test harness to additional parts.  
- Finalize closed‑loop training pipeline with verified GT.  
- Monitor system‑viz metrics for future scaling.
