# agent-orchestration session 5c91e392 (2026-06-09, 16.2MB, spine 150KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `b76b4d55ab`: resumable corpus loop (stream‑append + resume cursor).  
- Commit `265e8a6e41`: multi‑page PDF rasterization, manifest for ~7,794 prints; page‑0 OCR bug fixed.  
- Commit `5a8dbfefdd`: doc‑reflection updates (CLAUDE.md regression entry, wiki lesson).  
- Commit `cdc9ec44b3`: reaper‑immune scheduled task launcher for closed‑loop training.  
- Commit `250ffb8a6e`: reproducible script `scripts/find-perfect-parts.mjs`, 8 mutation tests, JSON/Markdown report for 91 “perfect” parts.  
- Earlier commits: hardening rasterize loop (try/finally), 3‑of‑3 Stop‑gate ledger marks, resolver bug fix.

**DECISIONS**  
- Adopt resumable corpus loop; stream‑append per print with cursor.  
- Process all pages of multi‑page PDFs (96 % contamination).  
- Ensemble VLMs: `qwen3-vl-instruct`, `qwen2.5vl`; drop non‑responsive models.  
- Deploy closed‑loop training as reaper‑immune overnight task.  
- Keep GPU+CPU env: torch 2.11+cu128, 32 CPU cores, pip deps.  
- PRISM agent orchestration: ARM A/B/C reviewers + 3‑of‑3 consensus gate before commits.  
- OCR GT from CNC feed‑move geometry; add binary guard, feed‑move extraction, callout‑class GT.  
- Fix resolver: sanitize `/` & `:` in display names, glob with sanitized disk name.  
- Correct axis classifier (mill vs lathe); add roughing‑ramp clustering to reduce fake diameters.  
- OCR recall < 1 % → stop full corpus until OCR improves.

**OPERATOR DIRECTIVES**  
- `/goal [ read previous sessions for xray | upgrade ocr & blueprint reading with new GPU/CPU, validate, then start closed‑loop training on jm/docustra prints ]`.  
- Run true‑test harness on 91 parts; compare OCR dimensions against CNC GT; decide on full corpus.

**FINDINGS/BUGS**  
- Page‑0 OCR bug: 96 % PDFs dropped >75 % of dimensions.  
- Reaper kill: job exit 255, cursor survived → re‑OCR = 0 proven.  
- Missing `run-as-main` guard in manifest builder & training runner; fixed.  
- Comment line (`#`) counted as missing print; now skipped.  
- GPU contention caused one VLM timeout under fleet load; corroboration reduced.  
- OCR reads 1–3 dims per PDF; recall = 0 vs ~7–14 callouts → severe weakness.  
- 3 prints unresolved due to filename sanitization bug (`/` & `:`).  
- Binary `.mcx‑8` misparsed as text; fixed by binary guard.  
- Mill program `9102741.hnc` misclassified as lathe; axis classifier fixed.  
- Roughing‑ramp steps inflated GT counts; clustering cuts callouts ~50 %.

**DOMAIN SPECIFICS**  
- OCR training loop: `blueprint‑ocr‑training‑loop.mjs`; PDF rasterizer `pdf-to-png.py`.  
- Ensemble models: `qwen3-vl-instruct`, `qwen2.5vl`, `moondream`, `llama3.2‑vision`.  
- Trainset generation & active‑learning queue: `xray‑trainset‑to‑lora.mjs`.  
- Metrics: trainable labels per print, corroboration fraction, GPU utilization.  
- Blueprint–program join (`blueprint-program‑join‑full‑v6.jsonl`): part_number, print filenames, CAD (.step/.ipt), CNC (.MIN/.nc/.mcx).  
- OCR pipeline outputs `fused.dimensions`.  
- GT extraction: CNC feed‑move (G1/G2/G3) → distinct diameters + envelope.  
- Resolver maps display names to sanitized disk paths on H: (`H:/PRISM/Docustrata`, `H:/PRISM/JM DIE`).

**TOOLS USED**  
- PRISM slot helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
- `/checkin-xray` wrapper.  
- OCR extractor: `batch‑ollama‑vision‑extract.mjs`.  
- Training runner: `blueprint‑ocr‑training‑loop.mjs`.  
- Trainer CLI: `xray‑trainset‑to‑lora.mjs`.  
- PDF rasterizer: `pdf-to-png.py`.  
- Vision model selector: `vision-model-select`.  
- PowerShell scripts: `run-ocr-batch-overnight.ps1`, `install-blueprint-ocr-batch-task.ps1`.  
- PRISM agents: ARM A (holistic), ARM B (test integrity), ARM C (regression/I/O).  
- Scripts: `scripts/find-perfect-parts.mjs`, test harness, `cad-analyze-step.mjs`.  
- Node.js + Jest for tests; Git.  
- PRISM dispatcher: `prism_cad:cad_step_parse_file`.

**OPEN THREADS**  
- Complete LoRA fine‑tune with real trainset & operator‑verified gold labels (domain‑shift gate).  
- Process active‑learning queue for operator verification.  
- Optimize GPU concurrency / ensemble size under fleet load.  
- Deploy trained model to production; monitor metrics.  
- Finalize documentation & knowledge base updates.  
- True‑test verdict: OCR recall < 1 % → enhance OCR or pre‑filter scans before full corpus.
