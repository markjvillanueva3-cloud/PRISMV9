# agent-orchestration session 001bd6c3 (2026-06-09, 52.8MB, spine 479KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `c988a21ec4`: Hermes boot‑loop fix (stale config).  
- Commit `56aa423427`: Hermes → Claude OAuth + local Ollama fallback.  
- Builds `ab2ccf42a4`, `e6713584e2`: system‑viz assets deployed.  
- Docker Desktop → Qdrant container `prism-qdrant` v1.17.0, 3 collections (`prism_engines`, `prism_formulas`, `prism_skills`).  
- Hermes config migrated to primary `gpt‑oss:20b` (local), fallback `anthropic/claude-opus-4.8`. GUI now reachable on :9120.  
- Commit `28b72e4dee`: added `fill-galaxy-memory-sections.mjs`, tests; updated 34 engine‑MEMORY.md to full 4/4 canonical sections.  
- Slot‑bravo units:  
  - `822c48c55` – verified‑corrected plan (U-OAB‑PLAN).  
  - `3499f5f20f` – U5b (`ollama-tool-agent.mjs`).  
  - `d807d46f44` – U5c (`ollama-offload-enforce.mjs`).  
  - `a59f35221e` – U6 (`ollama-codegen.mjs`).  
  - `cfda0e391a` – U7 (`ollama-build-loop.mjs`).  
  - `e17be56e82` – U10 (`ollama-capability-probe.mjs`).  
  - `7771ca7f86` – U9 (`ollama-coresidency.mjs`).  
- Offload router roster refreshed (U1, commit `2340a2e699`).  
- Route‑hook default set to resident `gpt‑oss:20b`; shipped `ollama-route-config.json` (`a2756779c2`).  
- Take‑rate banner corrected; offload rate 0.9 % (`a60d7ba0bf`).  
- LLM compressor for RTK residue, safety denylist fixed (`612418fde7`).

**DECISIONS**  
- Fleet coordination via single :3100 MCP only; drop second filesystem‑MCP to avoid collisions.  
- Hermes primary: `gpt‑oss:20b`; fallback: Claude Opus 4.8 (OAuth). Local Ollama `qwen2.5-coder:32b` for code generation only.  
- Revert supervisor cwd patch; update CapabilityIndexEngine to resolve from `PRISM_ROOT`.  
- Keep auto‑route on; use existing `OllamaTaskOffloaderEngine.ts`; no new fork.  
- Commit slot‑bravo units only to `slot/bravo` worktree until golf’s git‑tree repair completes.  
- Set GPT‑OSS‑120B as default fleet‑wide coding model when offloading coding tasks (operator approval pending).  
- Defer Kimi 2.6 cloud‑only voice integration; evaluate env‑gated octopus voice.  
- RTK config tightening (`config.toml`) remains a live local change; confirm before touching `%APPDATA%\rtk\`.  
- Go‑live merge of `slot/bravo` into main deferred until above decisions resolved.

**OPERATOR DIRECTIVES**  
- “Get Qdrant up and running.”  
- Verify Hermes reachable on :9120; send test prompt (`hermes -z "test"`) → response from `gpt‑oss:20b`.  
- Confirm MCP server at :3100 healthy (HTTP 405) and Hermes can connect.  
- Ensure Qdrant container remains up; if stale connection, restart :3100 MCP service.  
- Commit only to slot/bravo worktree.  
- Finish remaining Ollama‑autorun units and upgrade RTK/existing systems for new hardware.  
- Proceed with U4 sync (ask‑ollama.mjs integrated).  
- Evaluate cloud Kimi 2.6 voice; confirm or deny RTK config tightening.  

**FINDINGS/BUGS**  
- Qdrant vector store down → `semantic_search` empty, 400 errors for Claude calls.  
- Hermes boot logs show 429 rate‑limit from Anthropic; later 400 error due to extra‑usage billing not enabled.  
- Slot‑brief_write / slot_brief_inject path proven live and consume‑once.  
- `self_awareness_search` returns 5 hits; `master_index_query` returns 20 hits.  
- Unknown provider 'openai' fixed by using custom provider ID for Ollama.  
- Docker Desktop engine pipe missing initially → Qdrant down; resolved by starting Docker Desktop.  
- Hermes startup stalled due to dirty `package-lock.json`; cleared and relaunched.  
- Qdrant daemon cached “not connected” state; requires MCP restart to reconnect.  
- 400 error on Claude primary due to exhausted extra‑usage balance; fallback not triggered (non‑retryable).  
- 12 missing galaxy directories; audit revealed 29 weak galaxies (only Master‑brain link present).  
- Hermes custom provider mis‑parses gpt‑oss reasoning channel → “no final response”.  
- `node_*` auto‑generated files inflated memory counts by ~20×.  
- `ai-training_synthesis.md` corrupt (all‑NUL).  
- CRLF→LF line‑ending flip on U1 commit; corrected via amend.  
- Denylist regex bug in U4 caused P0 safety breach for lowercase/no‑space G‑code; fixed with case‑insensitive, boundary‑aware patterns.  
- Offload router listed absent/retired models; now points to resident `gpt‑oss:20b` / `qwen2.5-coder:32b`.  

**DOMAIN SPECIFICS**  
- Hermes app: local Ollama (`qwen2.5-coder:32b`, `qwen3-vl`) + Claude Opus 4.8 via OAuth; fallback to `gpt‑oss:20b` for tool‑calling.  
- Obsidian vault REST API on :27123 (PRISM↔Obsidian bridge).  
- PRISM MCP at :3100 with dispatchers (`prism_memory`, `prism_session`, `prism_knowledge`).  
- Qdrant vector store (Docker `prism-qdrant`) port 6333, collections: `prism_engines`, `prism_formulas`, `prism_skills`.  
- Slot‑briefs system: `slot_brief_write`, `slot_brief_inject` hook, `chat_post`.  
- Galaxy layout: `mcp-server/src/engines/<galaxy>/MEMORY.md`, `PATHS.md`, `TOOLBELT.md`; vault mirror at `memories/galaxies/<galaxy>/MEMORY.md`.  
- Engine actions: `OllamaTaskOffloaderEngine`, `ask-ollama.mjs`, route hooks (`ollama-route-pretooluse.mjs`), `loadRouteConfig`.  

**TOOLS USED**  
- PRISM commands: `/checkin-bravo`, `chat-slots.mjs`, `audit-roadmap-drift.mjs`, `wire-hermes-local-backend.mjs`, `mcp-server-supervisor.mjs`.  
- Node scripts: `fill-galaxy-memory-sections.mjs`, `syncGalaxyMemories`, `galaxy-synthesis-refresh.mjs`, `ollama-tool-agent.mjs`, `ollama-offload-enforce.mjs`, `ollama-codegen.mjs`, `ollama-build-loop.mjs`, `ollama-capability-probe.mjs`, `ollama-coresidency.mjs`.  
- Docker Compose (`docker‑compose.yml`), Docker Desktop (Qdrant).  
- Ollama runtime (`gpt‑oss:120b`, `gpt‑oss:20b`, `qwen2.5-coder:32b`).  
- Ultracode workflow for planning/brainstorming.  
- Vitest, Node’s built‑in `node:test` for unit tests.  
- ESLint, TypeScript (`@types/node`) for linting.  

**OPEN THREADS**  
1. Bring Qdrant up and restore semantic_search; monitor 400 errors on Claude calls.  
2. Enable extra‑usage billing on Claude to eliminate 400 errors for Hermes primary calls.  
3. Finalize supervisor cwd fix by updating CapabilityIndexEngine to use `PRISM_ROOT`.  
4. Verify Hermes fallback to local Ollama when Claude is rate‑limited or returns 429.  
5. Confirm Hermes restart fully loads new config and clears stale Qdrant connection.  
6. Verify that `gpt‑oss:20b` produces final responses in one‑shot mode (adjust max_tokens if needed).  
7. Decide on billing vs. local fallback for Claude primary; monitor extra‑usage balance.  
8. Ensure Obsidian REST API remains reachable and synced with PRISM.  
9. Regenerate `ai-training_synthesis.md` (India’s responsibility).  
10. Re‑enable disabled Hermes‑Obsidian Bridge task.  
11. Verify post‑fill that all 34 galaxies remain at 4/4 after future synthesis refreshes.  
12. Go‑live merge of `slot/bravo` into main (#14) pending golf’s git‑tree repair and RTK config tightening confirmation.  
13. Evaluate cloud Kimi 2.6 voice integration (env‑gated octopus).  
14. Monitor GPU utilisation; target 30–40 % with pre‑loaded models (`gpt‑oss:120b`, `qwen2.5-coder:32b`, `gpt‑oss:20b`).  
15. Add alert if `gpt‑oss:120b` falls below 80 % utilisation or latency > 2× baseline.
