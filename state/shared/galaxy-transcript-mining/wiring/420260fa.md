# wiring session 420260fa (2026-05-17, 26.8MB, spine 119KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `54f704fb1` – NN‑STACK‑INTEG‑MS0 File 1 (MultiModelConsensusEngine)  
- `685e48cfa` – NN‑STACK‑INTEG‑MS0 File 2 (ConsensusNeuralFeedbackEngine)  
- `444dab3cf` – NN‑STACK‑INTEG‑MS0 File 3 (CrossProcessConformalClassificationEngine + ConformalCalibrationMonitorEngine)  
- `d80030059` – XProcNeuralAutoFireEngine wiring for NN stack  
- `3686d3f36` – Fleet‑Reaper Docker probe restart gating (MS1.1)  
- `6d01c5f40` – Fleet‑Reaper in‑session Monitor → JSONL tail (MS1.2)  
- `5e8f0556e` – absorbed into fleet‑reaper updates  
- `b8cfb27a42` – U‑B1 tiered kill‑after: 80 %→1, 95 %→0  
- `080d64b754` – U‑B3 memory ballast (256 MB, released on critical)  
- `06308f8d60` – U‑D4 Ollama inject threshold 0.9→0.5  
- `8af0bb8ef5` – U‑F2 installer hardening (S4U+AtStartup+Restart3)  
- `3609fc1f32` – U‑G124 host UAC: pagefile 96 GB, MemCompr, Defender  
- `ef2a70392e` – U‑E1E2 auto‑restart stale services (default OFF)  
- `6233822bc1` – WIRE‑UNWIRED‑MS0 compact‑planner (4 actions)  
- `9e755becac` – parallel‑planner (3 actions)  
- `351ccc6808` – ctx‑pressure (4 actions)  
- `86af981425` – prompt‑compress test file  
- `5ef9475683` – prompt‑compress fix‑up (dispatcher+schemas)  
- `e84f5f8430` – U‑CLEANUP‑A6 bootstrap‑golf.mjs  
- `27cb36522c` – WIRE‑CADBRIDGE (4 files, 378 LOC, 11 tests)

**DECISIONS**  
- Adopt Hermes Agent architecture: SOUL.md identity, MEMORY.md persistent memory, skill_manage for self‑evolving skills.  
- Fleet‑Reaper always‑on: S4U scheduled task (5 min, restart on failure) + per‑session persistent Monitor; kill switch `PRISM_FLEET_REAPER_DISABLE=1`.  
- Permanent reaper fix: memory ballast 256 MB, Docker probe restart gating, GPU offload threshold 0.5, pagefile 96 GB, enable memory compression, WSL2 isolation, Windows Service supervisor.  
- GPU for Ollama (qwen2.5‑coder 32b‑q4) + CPU orchestration; per‑slot Job Object capped at 50 processes.  
- Hermes proposals queued: A – skill author loop, B – USER.md + per‑slot SOUL.md, C – Telegram ChatOps.  
- Fleet‑reaper ownership moved to golf (2026‑05‑16).  
- Queue order: reaper upgrades → GNN deploy gate → NN‑STACK‑INTEG ff‑merge → tests‑through‑dispatcher.

**OPERATOR DIRECTIVES**  
- Verify session is alpha chat; force‑take alpha slot and run `/checkin-alpha` with permanent reaper fix for 12 simultaneous chats.  
- Ensure Monitor survives `/compact`; keep alive across compaction.  
- Add Hermes proposals to task queue without overwriting existing tasks.  
- Run elevated installer:  
  ```
  powershell -NoProfile -ExecutionPolicy Bypass `
    -File H:/prism/.claude/helpers/install-fleet-reaper-task.ps1 -RunNow
  ```  
- Apply U‑G124 host UAC commands (pagefile 96 GB, MemCompr, Defender) and reboot.  
- Use `/handoff` + `/compact` before further unit commits to mitigate fork‑storm risk.

**FINDINGS/BUGS**  
- Monitor killed by `/compact`; reaper coverage lost for minutes.  
- Fork failures >95 % memory; no ballast (4 GB pagefile).  
- Scheduled task Interactive‑only → fails after logout/reboot.  
- Docker stack stopped; Postgres/Prometheus probes down.  
- Memory compression disabled → high RAM usage.  
- `stable-session-id.mjs` returns cached ID → handoff mis‑targeting.  
- Drift audit: 30 milestones vs BUILD_STATE 11.  
- Host memory ~99 % → critical fork‑storm (xmalloc errors).  
- GNN deploy gate blocked by system‑viz classifier degeneracy.  
- Tests‑through‑dispatcher (#18) blocked by host fork‑storm.

**DOMAIN SPECIFICS**  
- Engines: fleet‑reaper, fleet‑memory‑monitor, NN‑STACK‑INTEG (MultiModelConsensusEngine, ConsensusNeuralFeedbackEngine, CrossProcessConformalClassificationEngine, ConformalCalibrationMonitorEngine), XProcNeuralAutoFireEngine, WIRE‑CADBRIDGE, WIRE‑COMPACT‑PLANNER, WIRE‑PARALLEL‑PLANNER, WIRE‑CTX‑PRESSURE, WIRE‑PROMPT‑COMPRESS.  
- Dispatchers: alpha‑work, golf; prism_context enum expanded 74→87.  
- Metrics: tiered kill‑after thresholds (80 %→1, 95 %→0), 256 MB ballast, GPU/Ollama threshold 0.5, reaper sweep counts.  
- Paths: `H:/prism-slot-alpha/scripts/fleet-reaper-sweep.mjs`, `H:/prism/.claude/hooks/ollama-task-offloader.mjs`.  
- Hermes files under `~/.hermes/`: config.yaml, SOUL.md, MEMORY.md, skills/, state.db (SQLite + FTS5).  
- Slot worktrees: `H:/prism-slot-<name>` with per‑slot branches and handoff topics.  
- Metrics via nomic‑embed‑text vectors.

**TOOLS USED**  
- Scripts/hooks: `per-agent-handoff.mjs`, `chat-slots.mjs`, `stable-session-id.mjs`, `fleet-reaper-sweep.mjs`, `install-fleet-reaper-task.ps1`, `ollama-task-offloader.mjs`.  
- PRISM tools: `/fleet-reaper`, `/checkin-alpha`, `/startup`, `/loop`, `/compact`, `/handoff`.  
- Skills: skill_manage, monitor, cron, memory monitor, reaper sweep, dispatcher round‑trip tests.  
- Monitor tool, audit-roadmap-drift.mjs, build‑state snapshot scripts.  
- Playwright for article extraction.

**OPEN THREADS**  
- Finalize permanent reaper fix (MS2): Windows Service supervisor, WSL2 isolation.  
- Complete Docker restart gating for Postgres/Prometheus/Qdrant; enable auto‑restart on probe failure.  
- Enable memory compression and increase pagefile to 96–128 GB.  
- Tune GPU offload threshold in Ollama coordinator; prewarm strategy.  
- Merge NN‑STACK‑INTEG‑MS0 into `cad-fusion-live-ms0`, run tests, deploy.  
- Finish Hermes proposals A/B/C (skill author loop, USER.md + SOUL.md per slot, Telegram ChatOps).  
- Resolve drift audit discrepancy and stable-session-id fallback bug.  
- Pending MS1 units: U‑A5 pure‑Node, U‑C3 probe‑cache, U‑C1 Job Object, U‑D1+D2 Docker Ollama.  
- GNN deploy gate blocked upstream; classifier fix required.  
- Tests‑through‑dispatcher (#18) blocked by host fork‑storm; needs memory cleanup.  
- Remaining queue: COMMAND-KERNEL-MS0, WIRE‑UNWIRED (other sample engines), B9 model‑drift eval suite.  
- Apply G124 commands + reboot before resuming loop.
