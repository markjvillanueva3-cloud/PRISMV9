# wiring session 001bd6c3 (2026-06-09, 49.6MB, spine 462KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hermes config migrated v0→v28 (`hermes config migrate`).  
- Qdrant Docker container started on :6333; 3 collections healthy (`prism_engines`, `prism_formulas`, `prism_skills`).  
- Hermes CLI now uses:  
  - `model.default = gpt‑oss:20b` (local primary)  
  - `fallback_model.provider = anthropic`, `fallback_model.model = claude‑opus‑4.8`.  
- Commit **28b72e4dee** – 34 engine `MEMORY.md` files updated to full 4‑section structure; added `fill-galaxy-memory-sections.mjs` + test suite; vault mirror synced.  
- Commit **822c48c55** – verified‑corrected plan spec (`OLLAMA-AUTORUN-BUILD-PLAN`).  
- Commits:  
  - **3499f5f20f** U5b: local gpt‑oss tool‑calling read/search agent.  
  - **d807d46f44** U5c: PreToolUse hook enforcing read/search offload.  
  - **a59f35221e** U6: qwen2.5‑coder coding‑offload script.  
  - **cfda0e391a** U7: Opus tail‑review gate for build quality.  
  - **e17be56e82** U10: capability probe validating all local LLMs run and auto‑use.  
  - **7771ca7f86** U9: core‑residency mutex/keep‑alive logic.  
  - **2340a2e699** U1: Offload‑router roster refresh (tier‑then‑latency).  
  - **a2756779c2** U2: route‑hook default change `qwen2.5-coder:7b → gpt‑oss:20b` + shipped `ollama-route-config.json`.  

**DECISIONS**  
- Hermes primary on Claude Code; fallback to local Ollama if 429 or extra‑usage error.  
- Adopt fleet‑standard local model (`gpt‑oss:20b`) as primary; keep Claude‑Code fallback for extra‑usage scenarios.  
- Run Hermes headless via PowerShell CLI (`hermes -z …`) instead of Electron GUI to avoid bootstrap stalls.  
- Offload read/search/coding to local LLMs: `gpt‑oss:120b` (tool‑calling) and `qwen2.5-coder:32b` (code generation).  
- Keep alive/NUM_PARALLEL set to 2; un‑strand `gpt‑oss:120b` from stale FLOOR tiers; keep MAX_LOADED ≤ 4 due to VRAM limits.  
- Auto‑route lever already enabled (`ollama-route-config.json` mode=auto); routing table deduped against existing `OllamaTaskOffloaderEngine.ts`.  

**OPERATOR DIRECTIVES**  
1. Start Qdrant Docker container on :6333.  
2. Enable “extra usage” billing in Claude account (`claude.ai/settings/usage`).  
3. Restart Hermes after config changes; verify MCP link at `http://127.0.0.1:3100/mcp/health`.  
4. Test zulu headless prompt:  
   ```powershell
   & "C:\Users\...\hermes.exe" -z "Check in as Zulu fleet orchestrator"
   ```  
5. Audit each galaxy sequentially; use Obsidian vault and Ollama/Docker stack to fill gaps.  
6. Apply A1–A5: set `OLLAMA_KEEP_ALIVE=1`, `MAX_LOADED_MODELS=5`; edit router for priority rule (`gpt‑oss:120b` when GPU free ≥ 20%); create fallback chain (120b → 32b → 20b → Claude).  
7. Run quick coder task to verify `gpt‑oss:120b` usage; check GPU utilisation (<10%) after pre‑load script runs.  
8. Commit changes to slot/bravo worktree and push.  

**FINDINGS / BUGS**  
- Hermes received HTTP 400 from Anthropic (missing extra‑usage billing); local fallback never triggered.  
- Qdrant not listening; stale “not connected” state cached by Hermes daemon.  
- Hermes memory bridge dormant – no memories yet.  
- Unknown provider `openai` error in Hermes 0.16.0; must use `custom` for Ollama.  
- GUI launcher stalls on dirty `package-lock.json`; fixed by discarding file.  
- 20× corpus inflation due to `node_*` graph dumps counted as memories; exclusion logic added.  
- Corrupt `ai-training_synthesis.md` flagged for owner India (no regeneration).  
- Many galaxies had only Master‑brain link present; missing other canonical sections.  
- Offload router had absent/retired models → ~0% offload; fixed in U1.  
- CRLF line ending flip on U1 commit; corrected via amend.  
- Stale async tests for `decideRoute` and `runRoute`; updated pure‑fn tests, flagged async test rot.  
- RTK cannot be LLM‑upgraded (external Rust binary).  
- Take‑rate calculation conflated advisory nudges with offload actions; requires correction.  

**DOMAIN SPECIFICS**  
- **Hermes app** – desktop client using Claude Code OAuth and Ollama fallback.  
- **Obsidian vault** – REST API on :27123 fully functional.  
- **PRISM MCP** – :3100 healthy; coordination surface operational.  
- **Qdrant vector store** – required for `semantic_search`; currently down (now up).  
- **Hermes memory bridge** – empty (`hermes/memories/`).  
- **Engines** – local Ollama LLMs: `gpt‑oss:120b` (20 GB), `gpt‑oss:20b`, `qwen2.5-coder:32b` (20 GB).  
- **Actions** – read/search offload via tool‑calling; code generation; Opus tail review gate.  
- **Dispatchers / hooks** – `ollama-prism-bridge.mjs`, `ask-ollama.mjs`.  
- **Metrics** – 0 Claude tokens for read/offload, VRAM usage per model (RTX PRO 6000 96 GB), latency ~30 s for `gpt‑oss:120b`.  
- **Paths** – slot/bravo worktree (`H:/prism-slot-bravo`), branch `slot/bravo`, vault mirror `memories/galaxies/<g>/MEMORY.md`.  

**TOOLS USED**  
- Hermes CLI & GUI (`hermes-setup.exe`, `hermes config migrate`, `hermes -z`).  
- Docker Desktop + `docker-compose.yml` (Qdrant).  
- PRISM MCP client/server (`:3100`).  
- Obsidian local‑REST API plugin.  
- Local Ollama stack (v1.x) + Docker.  
- Scripts: `fill-galaxy-memory-sections.mjs`, `galaxy-synthesis-refresh.mjs`, `syncGalaxyMemories()`, `ollama-prism-bridge.mjs`, `ask-ollama.mjs`, `scripts/ollama-tool-agent.mjs`, `scripts/ollama-offload-enforce.mjs`, `scripts/ollama-codegen.mjs`, `scripts/ollama-build-loop.mjs`, `scripts/ollama-capability-probe.mjs`, `scripts/ollama-coresidency.mjs`.  
- PRISM ultracode Workflow (`wbmnnd902`, `w2hu6dowo`).  
- `/loop` cron job (`f3a1c2b4d7`).  
- Node 22 harnesses for test validation.  

**OPEN THREADS**  
- Bring Qdrant up and verify semantic_search works.  
- Enable extra‑usage billing or reconfigure Hermes to fallback on 400 errors.  
- Re-test Hermes message after fixes; monitor failures.  
- Lazy reconnect policy for Qdrant (restart or periodic probe).  
- Corrupt `ai-training_synthesis.md` flagged to India pending regeneration.  
- Re‑enable Hermes‑Obsidian Bridge scheduled task.  
- Potential future regeneration of syntheses for other galaxies.  
- Go‑live merge slot/bravo → main and wiring `settings.json`.  
- Apply U1 NUM_PARALLEL 4→2 config tweak live.  
- Un‑strand `gpt‑oss:120b` from stale tiers (U3).  
- RTK upgrade sweep (#11) – plan & implement.  
- Kimi K2.6 integration decision; potential env‑gated octopus voice.  
- Cleanup stale async tests in `runRoute`.
