# wiring session 4c896ca9 (2026-06-22, 225.6MB, spine 342KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `1166e477db` – De‑inline HeatTreatmentAwareSpeedFeedEngine; fixed dangling `REGIME_MODIFIERS`, added NaN guard.  
- `626481e848` – Wired `heat_treat_regime` into Vc path (gap #2).  
- `c127137384` – Added fail‑loud fuzzy‑material resolution (gap #3).  
- `ed91a74f2c` – Honesty warning for hardened‑steel force under‑prediction.  
- `556d2b65d3` – Replaced Euler–Bernoulli with Timoshenko deflection (gap #5b).  
- `e6a23caf4a` – Added additive `surface_integrity` output (gap #6).  
- `U‑SFC‑LIFE‑UNCERTAINTY‑FOSM` – Canonical Taylor‑sensitivity life‑uncertainty.  
- `U‑SFC‑BALL‑END‑DEFF` – Reusable effective‑diameter formula wired additively (gap #8).  
- Backend SFC units: `U‑SFC‑COOLANT‑THERMAL`, `U‑SFC‑KC‑EFFECTIVE‑ISO‑FORCE`, `U‑SFC‑KIENZLE‑FOSSIL‑TEST‑FIX`, `U‑SFC‑THERMAL‑KC‑HARDENED`.  
- Gap #10 outcome‑capture sink verified (R8).  
- P0 regression baseline for `SpeedFeedOrchestratorEngine.compute()` (numeric‑regression snapshot, 4 deterministic core tests).  
- P1 adapter: `OrchestratorInput → UltimateSpeedFeedInput` with 10 unit tests.  
- Proven‑pipeline lathe harness (`U‑SFC‑PROVEN‑PIPELINE‑ACTIVATE`) – 13/13 tests, 3‑of‑3 scrutiny PASS.  
- Mill require‑fix (CommonJS→ESM) – build‑clean, 12 live chip‑load samples, analyst PASS.  
- Convergence‑diff harness built and verified (analyst PASS).  
- Turning bug fix: Vc corrected to 54.2 m/min; requires bridge restart.  
- `c5fac24e43` – UI uncertainty banner + helper `deriveAdvisory`.  
- `26d5adbb36` – P2‑level safety‑physics & test hardening for uncertainty banner.  
- `09d605bac1` – `mineJMDiePrograms` skips/records Mastercam `.mcx‑8` binaries (R12).  
- `3c26c7ae04` – Boring rpm/Vc uses optional `bore_diameter_mm`.  
- `80aeec91d1` – CV% formatter & renderer for Uncertainty tab.  
- `fd8df11f81` – Cam‑strategy fidelity: preserve operator label, add PRISM normalizer “prism”.  
- `b359d166a5` – No‑op `clearCache()` on stateless orchestrator engine.  
- `efb0c97358` – Material canonical fix: `"steel"` alias 1018→1045 (kc 1700→1800), removed unsafe fallback, switched to `default_fallback`. Safe direction.  
- `396ae501b7` – Inconel test fix: kc 2800→3200 for grade‑specific assertion. Safe direction.  
- `efb570b720` – Machine constraints: optional `base_rpm?`, `max_cutting_feed_mmmin?`; engine updated, fixtures corrected. Safe direction.

**DECISIONS**  
- Keep SFC core immutable; add physics via additive report‑only fields.  
- Resolve only gaps that can be proven safe with a single change; defer high‑risk force‑path changes.  
- Adopt two‑engine architecture: production uses `SpeedFeedOrchestratorEngine` (milling‑centric); convergence targets use `UltimateSpeedFeedEngine`.  
- Convergence re‑baseline (#15) must be signed off before deployment; turning bug fix is autonomous but needs bridge restart.  
- UI must surface all backend uncertainty signals (`condition_warning`, CV% metrics, recommendations).  
- Added `deriveAdvisory` helper to encode safety intent and confidence bands (R9).  
- Made orchestrator stateless; added explicit `clearCache()` for API consistency.  
- Prioritize backend fixes over frontend UI; once backend 100 % green proceed with electron/iOS/Android build.  
- Adopt safe‑direction policy for material‑resolution changes: increase kc where under‑predicted, lower confidence when fallback used.  
- Fix machine constraints in place rather than refactor entire schema; added optional fields so existing code remains unaffected.  
- Defer deep physics redesigns (advanced‑AI self‑consistency, chatter/stability) to a fresh iteration.

**OPERATOR DIRECTIVES**  
- “continue” – resume autonomous loop after checkpoint.  
- Original goal: complete remaining backend tasks, prioritize OSCAR/SFC, finish frontend UI, prove 100 % before building electron/iOS/Android.  
- “Start frontend phase‑1”.  
- “Converge: orchestrator delegates to UltimateSpeedFeedEngine”.  
- “Reconcile against published refs first”.  
- Await sign‑off on convergence direction and P2 implementation.  
- Optionally run full lathe mine or extend diff harness.

**FINDINGS / BUGS**  
- Dangling `REGIME_MODIFIERS` & `REGIME_EXPECTED_HARDNESS` in HeatTreatmentAwareSpeedFeedEngine.  
- Missing NaN guard → silent NaN speed/feed outputs.  
- Silent fuzzy‑material match could default to steel.  
- Hardened‑steel force under‑prediction (base kc used after H‑switch).  
- Cryo thermal regression: cryogenic coolant produced higher temp than flood.  
- Stale guardian tests for Kienzle `mc` values; mismatched canonical constants.  
- Turning rpm/Vc computed from tool diameter (~1.8 m/min) instead of workpiece diameter (~185 m/min); fixed to 54.2 m/min after bridge restart.  
- Cam‑strategy fidelity: strategy names collapsed to “conventional”; fixed by preserving operator label and adding PRISM normalizer entry.  
- Mastercam `.mcx‑8` binaries silently counted as 0; now skipped with accounting (`skipped_programs`).  
- Missing `clearCache()` caused aspirational test failure; added no‑op implementation.  
- UI previously dropped backend uncertainty signals; fixed by extending result type and rendering banner.  
- Alias mismatch: `MATERIAL_ALIASES["steel"]="1018"` vs canonical `"steel":"1045"`.  
- Unsafe fallback `||"steel"` caused unknown materials to resolve confidently as steel.  
- Machine spec mismatch: engine read `spindle.power` while fixtures used `power_kw`; missing `base_rpm` and `max_feed_mmmin` fields in type definitions.  
- Advanced‑AI confidence: binning bug (10‑unit vs intended 10 % ranges), double penalty on self‑consistency, non‑deterministic noise (`Math.random()`).

**DOMAIN SPECIFICS**  
Engines: `HeatTreatmentAwareSpeedFeedEngine`, `UltimateSpeedFeedEngine`, `SurfaceIntegrityEngine`, `StochasticToolWearEngine`, `BallEndMillEngine`, `SpeedFeedOrchestratorEngine`, `NineAxisOrchestratorEngine`, `ProvenSpeedFeedAggregatorEngine`, `SpeedFeedAdvancedAIEngine`, `MachineAwareSpeedFeedEngine`.  
Dispatchers/paths: SFC calc pipeline (`prism_calc`), dispatcher round‑trip tests, `/api/v1/speed-feed/orchestrate → sf_orchestrate`, `/api/v1/sfc/calculate`, `/sfc/calculate`, `mineJMDiePrograms`, UI components (`SpeedFeedPage.tsx`, `UncertaintyAdvisoryBanner`, `SfcCalculatorPage`).  
Metrics: Vc, Vf, force, torque, power, life uncertainty, surface integrity, kc (cutting coefficient), confidence scores, S(x) safety metric, CV% metrics, Ra, uncertainty ci95.

**TOOLS USED**  
PRISM tooling: `/checkin-oscar`, slot‑claim helper, commit‑lane guard, fanout‑gate, `calcDispatcher`, outcome‑capture bus engine, `UltimateSpeedFeedEngine`, `SpeedFeedOrchestratorEngine`, `NineAxisOrchestratorEngine`, `ProvenSpeedFeedAggregatorEngine`.  
Scripts: `sfc-full-sweep-compare.mjs`, `extract-jm-proven-speedfeed.mjs`, `mineJMDiePrograms` script, `atomicWriteJson`, `safeWriteSync`.  
Hooks/utilities: `slot-bind-enforce.mjs`, `chat-slots.mjs`, `toBeDefined()` completeness gate, commit hooks (lint, test, safety).  
Build/test: `tsc --noEmit`, Vitest (jsdom + RTL), `tsx` for runtime validation.  
Agents: physics‑reviewer, safety‑physics, fact‑checker, explore agent Sonnet, 3‑of‑3 scrutiny gate.

**OPEN THREADS**  
- Frontend phase‑1: deprecate orphan `SpeedFeedPage`, surface surface‑uncertainty signal, port‑3100 E2E with Quebec.  
- Electron/iOS/Android builds after backend/frontend 100 % proof.  
- P2 convergence implementation (delegate compute to engine).  
- Load‑at‑init for proven store.  
- Full lathe mine scheduled task.  
- Mill extraction of `.mcx` files (not yet done).  
- Thermal‑k derivation physics cycle (#11).  
- Proven full‑mine cron over 171 K program corpus (#17).  
- #21 pt2 – render `*_cv_pct` on `SfcCalculatorPage`.  
- #25 Advanced‑AI self‑consistency redesign.  
- #29 Chatter/Stability physics – missing `calculateProcessDamping`.  
- #28 Cross‑galaxy machine data wiring for real production packages.  
- #27 Consistency guard after #28.  
- Turning fix deployment – restart bridge and rebuild Vite app.  
- Sign‑off on convergence re‑baseline (#15) before production cutover.
