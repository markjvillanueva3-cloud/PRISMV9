# wiring session 5c91e392 (2026-06-09, 16.2MB, spine 150KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `b76b4d55ab`: Resumable OCR loop (`blueprint-ocr-training-loop.mjs`) with cursor logic and multi‑page PDF→PNG rasterization.  
- `265e8a6e41`: Manifest builder (`build-print-corpus-manifest.mjs`) → 7 794‑print worklist, bucketed by text‑layer / image‑only / excluded.  
- `5a8dbfefdd`: Doc‑reflection updates – CLAUDE.md regression entry, wiki lesson, MEMORY‑RECENT pointer.  
- `cdc9ec44b3`: PowerShell launcher (`run-ocr-training-overnight.ps1`) + Task Scheduler installer (`install-blueprint-ocr-batch-task.ps1`).  
- Updated `xray-trainset-to-lora.mjs` with idempotent dedup logic; added run‑as‑main guards to all CLI scripts.  
- `250ffb8a6e`: Added `scripts/find-perfect-parts.mjs`, tests, markdown/JSON report for 91 perfect triples (print + CAD + CNC).  
- Hardened OCR loop: per‑print try/finally cleanup, rate‑limit stall handling, P2 hardening logs.

**DECISIONS**  
- Adopt `/checkin-xray` slot‑binding wrapper to bind chat to `xray` slot before any `/checkin`.  
- Replace single‑page OCR with multi‑page rasterization (96 % PDFs) and per‑page ensemble inference.  
- Use two‑model VLM ensemble: `qwen3-vl:8b-instruct`, `qwen2.5vl:7b`; discard timeouts/empty responses.  
- Make training loop resumable via `processed-cursor.jsonl`; re‑OCR = 0 after kills.  
- Wire unattended training through Task Scheduler (reaper‑immune).  
- Keep domain‑shift gate & operator gold labeling as future work; current run produces silver labels + active‑learning queue.  
- Use CNC program as ground truth with feed‑move‑aware extraction and binary guard.  
- Resolve filename sanitizer bug (`/` & `:` → `_`) for scanned PDFs.  
- Correct mill‑program classification (axis detection) and cluster roughing‑ramp diameters to avoid inflated GT.

**OPERATOR DIRECTIVES**  
- Wire, test, validate OCR pipeline; then start closed‑loop training on all prints in JM folder/docustra.  
- Use 91‑part set as true test before full corpus run; dispatch reviewers ARM A/B/C for scrutiny.

**FINDINGS/BUGS**  
- Page‑0‑only OCR bug fixed by rasterizing every page (96 % PDFs multi‑page).  
- Resume cursor works: re‑OCR = 0 after two reaper kills.  
- Comment lines (`#`) in worklist skipped; previously treated as prints.  
- GPU contention caused one VLM timeout per print, reducing corroboration from 2→1.  
- PowerShell codepage bug (em‑dash) replaced with ASCII `-`.  
- Missing commit `01ce06d589` restored.  
- Reviewer rate‑limit stalls resolved by retry after cooldown.  
- GT denominator too coarse (121 toolpath points); fixed via feed‑move filter.  
- Binary `.mcx‑8` files caused false matches; added `isParsableNcText()` guard.  
- Print resolver failed on filenames with `/` & `:`; sanitized before basename.  
- Mill program misclassified as lathe; corrected axis classifier and added mill regression test.  
- Roughing‑ramp inflation reduced via diameter clustering.

**DOMAIN SPECIFICS**  
Engines: `qwen3-vl`, `qwen2.5vl`, `moondream`, `llama3.2-vision`.  
Actions/dispatchers: `/checkin-xray`, `blueprint-ocr-training-loop.mjs`, `batch-ollama-vision-extract.mjs`, `find-perfect-parts.mjs`, `cad_step_parse_file` action, CNC program parser.  
Metrics: `trainable_labels`, `tier_totals`, `active_learning_queue`, `ensemble_failed`, `missing`, `recall`, `precision`, `calloutGT`, `toolpath_points`.  
Paths:  
- `H:/prism/.claude/helpers/chat-slots.mjs`  
- `scripts/pdf-to-png.py`  
- `vision-model-select.js`  
- `xray-trainset-to-lora.mjs`  
- `build-print-corpus-manifest.mjs`  
- `blueprint-ocr-training-loop.mjs`  
- `run-ocr-batch-overnight.ps1`  
- `install-blueprint-ocr-batch-task.ps1`  
- `H:/prism/.../Docustrata`  
- `H:/prism/JM DIE`  
- `state/shared/ocr-training-loop/`.

**TOOLS USED**  
PRISM commands: `/checkin-xray`, `/loop`, `/goal`.  
Helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
Scripts: `build-print-corpus-manifest.mjs`, `blueprint-ocr-training-loop.mjs`, `xray-trainset-to-lora.mjs`, `run-ocr-batch-overnight.ps1`, `install-blueprint-ocr-batch-task.ps1`, `find-perfect-parts.mjs`.  
External utilities: `pdf-to-png.py`, Ollama, PyTorch 2.11+cu128, bitsandbytes, trl, peft, transformers.

**OPEN THREADS**  
- Full corpus training run pending; reaper‑immune scheduled task running → silver trainset + active‑learning queue on completion.  
- LoRA fine‑tune end‑to‑end: requires HF base model pull, quiet GPU window, operator‑verified gold labels (domain‑shift gate).  
- Operator gold labeling needed to close domain‑shift gate; pending.  
- Ensemble concurrency: add third reliable VLM once GPU contention mitigated.  
- Deployment plan after fine‑tune for model serving & downstream integration.  
- Improve real‑scan OCR recall via model fine‑tuning, data augmentation.  
- Integrate true‑test harness into `/checkin` pipeline as gate before full corpus run.  
- Verify additional gold samples and operator‑verified dimensions for training set expansion.
