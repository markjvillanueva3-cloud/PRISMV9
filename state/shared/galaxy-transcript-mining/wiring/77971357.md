# wiring session 77971357 (2026-05-17, 36.5MB, spine 184KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U‑FEEDBACK‑FORCING (commit b1e599d5fc) – stale‑fixture sweep, explicit parent‑env nulling in `runHook`, regex‑suffix invariant test, dev‑tool‑leverage.md staged via worktree‑route hook.  
- U‑REGEN‑VIZ‑MERGE‑FAILLOUD (commit f9dc218d78) – fail‑loud merge guard + 4‑tier resolver for regen‑viz.  
- U‑SOURCEHASH‑DOC‑ALIGN (commit 0bac2d4c2f) – fixed legacy nested plan shape regression in pick‑prefresh‑inject tests.  
- U‑COMPLEXITY‑FALLBACK (commit 3d416cb040) – new heuristic for `complexityFor` in rgs‑tool‑planner.  
- U‑BUG‑FINDINGS‑WIKI‑HOOK (commit bb198d9285) – automated wiki updates on bug findings; hook now operational.  
- Backfill wiki entries for prior session bugs (commit 1539fbc8b2).  
- Doc‑reflect commits updating CLAUDE.md and memory files (commits 9c5377a2a7, dabb7a76c6).  
- `build-requests-viz-sync.mjs` (296 LOC) – drift‑checker for USER‑BUILD‑REQUESTS‑LOG.md.  
- `high-value-additions-rank.mjs` – high‑ROI wiring scan logged.

**DECISIONS**  
- Adopt hard‑gate verification feedback loop per Boris doctrine.  
- Implement fail‑loud merge guard for regen‑viz to avoid silent regressions.  
- Ship bug‑finding wiki hook rule and backfill entries before proceeding.  
- Postpone U‑RIE‑ADAPTER, U‑CALIBRATION, U‑TRANSFER until next session.  
- Defer CLAUDE.md MS1 doc‑reflection to separate close‑out commit; no history rewrite on dev‑tool‑leverage.md.  
- Treat injection as pre‑searched result; consume to avoid re‑grep (core doctrine).  
- Add consumption rules for each injector (`master-index-precheck`, `wiki-precheck`, etc.).  
- Rely on graph regeneration for discoverability; do not expand system further.  
- Justify `build-requests-viz-sync` only if log > 20–30 rows; otherwise manual column acceptable.  
- Use validator + system‑viz coverage instead of raw `NEEDS_WIRING` signal (~50% false positives).

**OPERATOR DIRECTIVES**  
- “continue all current tasks in this chat /loop [20m] /goal finish all tasks” – completed.  
- “old lima chat crashed please regain context and continue where it left off” – handled.  
- “make rule to always update wiki for bug findings … that should be hooked” – implemented.  
- “Build it and look for node wirings with high ROI in system‑viz.”

**FINDINGS/BUGS**  
- Silent merge‑fail in regen‑viz due to default Node heap limit; fixed with guard & fail‑loud exit.  
- Legacy nested plan shape regression in pick‑prefresh‑inject tests corrected.  
- Bug‑finding wiki hook rule now operational, preventing regressions.  
- `classifyHits` regex mis‑parsed system‑viz find output; fixed by using real format (`L<layer>/<subgroup> <node-id> <label>`).  
- Ghost nodes identified by prefix `ghost.` (not layer token).  
- Drift‑checker flagged ambiguous status for “High‑ROI node wirings” row – genuine ambiguity.  
- Raw `NEEDS_WIRING` signal unreliable; ~50 % false positives per CLAUDE.md regression.  
- Per-file scrutiny: two parallel reviewers – Arm A passed (no P0/P1), Arm B failed with 2 P0s + 1 P1; all issues resolved in same commit.

**DOMAIN SPECIFICS**  
- Lima slot lifecycle (chat‑slots, slot‑task‑claims, handoff).  
- System‑viz graph updates and drift gate checks.  
- RGS‑TOOL‑AUTOINVOKE‑MS1 unit flow: feedback forcing → RIE adapter → calibration → transfer.  
- Bug‑finding wiki integration with CLAUDE.md regression tracking.  
- Key invariants: regex‑suffix invariant, priority‑chain ordering, three `CURRENT_POSITION` parse shapes.  
- Worktree‑route hook used for collateral staging of `dev-tool-leverage.md`.  
- Chat‑bus notified peer `claude-a61bbf34`.  
- Injection hooks list: `master-index-precheck`, `wiki-precheck`, `audit-viz-first`, `chat-bus`, `awareness-snapshot`, `build-state`, `claude-brief`, Ollama routers, `memory-relevance`, `subagent-start-context`.  
- System‑viz find output format: `L<layer>/<subgroup> <node-id> <label>`; ghost nodes start with `ghost.`.  
- High‑ROI cluster: Lathe domain – 82 unwired engines (best single milestone).  
- Other clusters: F3‑orphan‑engines (P0, lev 72.9), F1‑orphan‑hooks (P0, 369 orphans, 78.5 %, lev 61.9).

**TOOLS USED**  
- `/checkin-lima` skill; scripts/regen‑viz.mjs & merge‑augmentations.mjs; hooks/pick‑prefresh‑inject.mjs.  
- Tests in `scripts/__tests__`.  
- Git for commits, resets, handoff management.  
- c-to-h-mirror hook for settings replication.  
- Per-file scrutiny (parallel reviewers).  
- worktree-route hook; chat-bus.  
- `build-requests-viz-sync.mjs` drift‑checker; `high-value-additions-rank.mjs`; `validate-unwired-signal.mjs`.  
- `system-viz-query find`.  
- 14 node:test cases for sync script.  
- Injection‑system awareness docs (`feedback_tribal_obsidian_viz_utilization_protocol.md`).

**OPEN THREADS**  
- Pending units: U‑RIE‑ADAPTER, U‑CALIBRATION, U‑TRANSFER (MS1 P1 backlog).  
- Potential `/forge-audit-v2` audit scope pending.  
- Optimize regen‑viz merge guard under high memory pressure.  
- Review edge cases for bug‑finding wiki hook rule.  
- Validate raw `NEEDS_WIRING` signal before wiring 82 Lathe engines; execute wiring pass once validator confirms true orphans.  
- Monitor log growth; decide if sync script remains necessary beyond ~30 entries.  
- Doc‑reflection surfaces remain peer‑edited; reference memo and handoff awaiting finalization.
