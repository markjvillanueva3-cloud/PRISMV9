# wiring session 0e5669d2 (2026-06-09, 38.2MB, spine 315KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- **B1 sync‑resilient (168c202646)** – added `writeWithRetry`, guarded `ensureDir`, exported helper.  
- **B2 index‑meta (ea21008f25)** – public `refresh()` for `WikiIndexMaintainerEngine.formatIndex`; new in‑place meta‑stamp script; updated `wiki/index.md`.  
- **B3 maint‑cron installers (8c4dff660a)** – two disabled cron jobs: *promote-memory-to-wiki.mjs* & *vault-rot-sentinel.mjs*.  
- **C2 link‑heal scorer (984313825e)** – recalibrated tiers, reason‑based auto‑apply gating; auto‑apply count now 2.  
- **C3 tribal‑coverage refresh (838123429b)** – audit re‑run; coverage corrected to **83.7 %**; JSON reports updated.  
- **Fix-memory (45225e0cc3)** – durable build record in C: & H: mirrors.  
- **d948b85a74** – hardened scorer: `MIN_AUTOAPPLY_LEN=8`, `MAX_AUTOAPPLY_EDIT_FRACTION` guard, regenerated candidate file.  
- **3d796dcf5c** – fleet‑task‑health‑watch added `EXPECTED_UNREGISTERED_TASKS`; banner noise refined; WSL drift‑sync enabled.  
- **96ed5222e2** – vault‑backlink‑schema: reverse edge index (`doc_nodes`).  
- **d856173b86** – staleness flag for backlink reader; CLI & tests updated.  
- **0e2724871a** – `regen-viz.mjs` auto‑refresh of `vault-backlinks.json`.  
- **14aba14e3a + 4a44b5393c** – `doc_nodes` dispatcher action (enum, case, wrapper, schema).  
- **f23c58b42d** – vault‑access map updated to close `.canvas` gap; fixed stale *memory‑rag‑inject* claim.  
- **2d49bf0d33 + 47fe91554f** – `canvas-read-lib.mjs`, CLI subcommand, tests; access map for canvas reader updated.  
- **T2 – OLLAMA‑PIPELINE‑INJECTOR wired** – orphan injector activated with kill switch & keyword gating.  
- **T1 – U‑VIZ‑WIKI‑NARRATIVE built** – flag‑gated post‑pass generator using `generateBlurb()`, default **qwen2.5‑coder:32b**.  
- **#8 – OLLAMA‑PREWARM‑WIRE activated** – orphan prewarm hook wired, safe detached `curl -m30`; 10 min cooldown.  
- **#2 – U‑TRIBAL‑QA‑LLM‑UNBLOCK shipped** – ollama Q&A script (`callOllama` + `resolveSynthesisModel`); full test & live validation.

---

**DECISIONS**  
- Wire *memory-rag-inject* into global settings; avoid double‑blocking with *memory-index-precheck-inject*.  
- Add public `refresh()` to `WikiIndexMaintainerEngine`; skip re‑implementing index generation.  
- Install cron jobs in disabled state; document migration‑safe header.  
- Recalibrate link‑heal scorer: drop structural prefix/substring auto‑apply, keep exact slug + Levenshtein ≤ 2 as high confidence; add reason‑based gating to prevent decay‑leak.  
- Accept partial candidates file (processed = 100) after OOM on full run; mark coverage via `totalBroken` vs `processed`.  
- Introduce scorer floor: `MIN_AUTOAPPLY_LEN=8`, ratio guard `MAX_AUTOAPPLY_EDIT_FRACTION`; constants added.  
- Replace over‑confident auto‑apply path in link‑heal; verify only two legitimate auto‑applies remain.  
- Keep stray peer archive file harmless additive artifact.  
- Adopt flag‑gated post‑pass generator pattern for narrative (`generateBlurb()`).  
- Default to **qwen2.5‑coder:32b** for text generation; reject gpt‑oss models due to empty `.response`.  
- Wire orphan hooks with kill switches, keyword gating, `continue:true` fail‑open semantics.  
- Enforce duplicate‑check logic (`/dedup`) before any asset creation.

---

**OPERATOR DIRECTIVES**  
- “Fill all gaps.”  
- Ensure obsidian vault built & operational; map paths for new H drive nodes; use ultracode to locate all obsidian articles; provide tool calls for cheap token reads.  
- Build & upgrade MCP server: fully utilize CPU, RAM, GPU, NVMe; restart MCP daemon with `maxConnections=512`; register vault crons (*Vault Memory Promotion*, *Vault Rot Sentinel*).  
- Arm deferred automation post‑migration: restart MCP daemon, register vault crons.  
- Validate commit‑cap fix (~70 %); tidy “MISSING-task” banner noise.  
- Continue system viz & obsidian app synergy; keep node mapping for quick searches/tool usage savings.  
- Utilize ollama for task efficiency when viable; fully synergize ollama LLM within prism OS (system‑viz, obsidian vault, memory, wiki, tribal knowledge creation/injection); identify additional use cases.

---

**FINDINGS/BUGS**  
- Unprotected `writeFileSync` in *obsidian-memory-sync.mjs*; missing export of `syncMemories`.  
- *memory-rag-inject* unwired; always‑on & keyword recall surfaces disabled.  
- Stale frontmatter (`last_verified`, “Total entries”) in `index.md`; no re‑emit path.  
- Link‑heal scorer over‑confidence: structural prefix/substring auto‑apply, decay‑leak for long slugs.  
- Stale candidates file with `autoApplyCount = 14100`; full‑corpus suggester OOM (~800M scoreCandidate calls).  
- WSL balloon (95 GB) ignored `.wslconfig` until shutdown; resolved by `wsl --shutdown`.  
- Commit‑pressure: vmmemWSL & llama‑server dominate commit; node fleet fine.  
- Staleness flag correctly detects drift in reverse edge and canvas reader.  
- Lgit layer regex missed alphabetic suffix; fixed to include `Lgit`.  
- Duplicate daemon instances (3 MCP daemons) causing port contention – singleton guard required.  
- gpt‑oss:20b/120b return empty `.response` due to num_predict starvation; resolved by setting 1024 and switching to qwen2.5‑coder.  
- Prewarm hook safe but verified detached `curl -m30`; no fleet‑reaper orphan.

---

**DOMAIN SPECIFICS**  
- **Engines/Hooks**: *obsidian-memory-sync.mjs*, `WikiIndexMaintainerEngine`, link‑heal suggester, `fleet-task-health-watch.mjs`, `vault-backlink-schema.mjs`, `canvas-read-lib.mjs`.  
- **Dispatchers/skills**: *memory-rag-inject*, *node-card-prefetch-inject*, *build-wiki-leaf-index.mjs*, `doc_nodes` dispatcher action (enum, case, wrapper, schema), planned `canvas-doc`; `slot-bind-enforce.mjs`, `chat-slots.mjs`, settings.json wiring hooks (*master-index-precheck-inject*).  
- **Metrics/paths**: `processed` vs `totalBroken`, `autoApplyCount`, `coverageComplete` flag, node‑card query paths, commit‑charge, banner partitioning (`EXPECTED_UNREGISTERED_TASKS`), staleness timestamps, WSL memory cap, MCP daemon health.  
- **Key paths**: `H:/prism/scripts/lib/canvas-read-lib.mjs`; `system-viz-query.mjs`; `regen-wiki-from-viz.mjs`; `knowledge/wiki/architecture/obsidian-vault-node-access-map.md`; `regen‑viz.mjs`; `vault-backlinks.json`; `doc_nodes` schema files.

---

**TOOLS USED**  
- PRISM tooling: `/checkin-sierra`, chat‑slot helpers, settings.json editor, git commit with lane guard, scrutiny gate, ultracode workflow, Jina fetch, node:test/vitest, bash slot‑binding scripts.  
- Scorer module & candidate generator; Jest/Node test framework; Git utilities; `fleet-task-health-watch.mjs`; hook scripts (`write-block`, `slot-routing`).  
- Ollama runtime: **qwen2.5‑coder:32b** (default), gpt‑oss 120B/20B, vision, embeddings.  
- Scripts: `canvas-read-lib.mjs`, `system-viz-query.mjs`, `ask-ollama.mjs` (`callOllama`), `contextual-blurb.mjs` (`generateBlurb()`), `regen-wiki-from-viz.mjs`; settings.json; chat‑slot helpers.

---

**OPEN THREADS**  
- Finalize short‑token Levenshtein auto‑apply gating (min‑length floor).  
- Rewrite full‑corpus suggester to avoid OOM and produce complete candidates file.  
- Optional `coverageComplete` marker for clarity in output.  
- Wire `U-OLLAMA-PIPELINE-INJECTOR-WIRE` – pending final integration tests.  
- Implement `U-VIZ-WIKI-NARRATIVE` – GPT‑OSS paragraph generation for viz entries (already built, awaiting deployment).  
- Finalize `U-WEEKLY-SYNTH-RESOLVER` import change.  
- Monitor WSL memory after shutdown to ensure cap enforcement persists; schedule `/compact`.  
- Verify migration freeze remains ON until operator lifts it; defer MCP restart and vault cron registration.  
- WeeklySynthesisEngine resolver (remaining synthesis script).  
- `wiki‑NLI‑lint` – linting for wiki NLI content.  
- `U-MEMO‑EXTRACT‑THROTTLE` per‑session throttle fix.  
- `memo‑synth‑schedule` – freeze‑gated memo schedule task.
