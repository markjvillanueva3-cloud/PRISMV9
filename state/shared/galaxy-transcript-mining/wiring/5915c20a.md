# wiring session 5915c20a (2026-06-19, 33.3MB, spine 261KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5752cc01af` – U‑TAKEUP‑EVAL‑DENOMINATOR: added `evaluations` denominator & honest `genuine‑low‑take‑rate` signal.  
- `39b83fd04c` – U‑ROUTE‑SAVINGS‑HEADLINE‑HONEST: propagated honest signal to SessionStart banner (R15).  
- `1e9c4a1ee2` – U‑CLASSIFY‑SUPPRESS‑CANDIDATE: added advisory `suppress‑candidate` branch for proven‑low‑take classifiers.  
- `8faf5eea5d` – hardening comment on suppress‑candidate guard.  
- `cb690f9bda` – U‑WT‑TAB‑SINGLE‑LETTER: single‑letter tab resolver tier (`a…z`).  
- `939f98a2f2` – U‑SELF‑RESOLVE‑ENV‑FALLBACK: fallback to `CLAUDE_CODE_SESSION_ID`.  
- `c2a470ac11` – U‑RELEVANCE‑GATE: 15 % relative relevance gate in `searchGraphHits`.  
- `a15535ff04` – GAP‑A: inline node‑card content extraction (`renderTopCardBlock`).  
- `6d7a5d9484` – extracted `renderTopCardBlock` to shared lib.  
- `aee30d9363` – wired into pre‑read.  
- `6ee830404e` – wired into pre‑write.  
- `37601a5335` – wired into pre‑bash.  
- `5f606e42d8` – ASCII‑only truncation marker across all hooks.  
- `a7c9011bec` – Token‑economy computeROI wiring.  
- `4e7e77d5a4` – Token‑ledger most‑expensive wiring.  
- `c11a0f8393` – DiffTokenEstimator maxBuffer/fallback fix (64 MB + `--numstat`).  
- `e7da2020f1` – ContextPreloaderEngine & DiffToken security injection fixes.

**DECISIONS**  
- Adopt denominator‑based take‑rate measurement to separate genuine low‑take from wiring gaps.  
- Use advisory `suppress‑candidate` suppression; avoid automatic decay to preserve graph integrity.  
- Implement YELLOW‑zone checkpoint: halt new units, trigger auto‑compact, await `/compact`.  
- Add single‑letter tab matching as safety tier for WT tabs (`a…z`).  
- Provide session‑id fallback helper for bare/cron invocations.  
- Prune low‑score BM25 hits with 15 % relevance gate; keep top hit.  
- Refactor node‑card injection into shared lib (`renderTopCardBlock`) and propagate to all PreToolUse hooks.  
- Enforce ASCII ellipsis (`...`) for truncation parity.  
- Reject CAD/CAM bridge wiring in alpha slot due to lack of testability/risk.  
- Fix DiffTokenEstimator ENOBUFS bug by raising buffer to 64 MB + `--numstat`.  
- Remove command‑injection via `execFileSync(GIT_BIN, …)` and reject leading‑dash refs.

**OPERATOR DIRECTIVES**  
- Continue autonomous build loops/crons using Ollama+Obsidian+Hermes+Octopus.  
- One unit per iteration: real reference‑value tests + 2‑arm scrutiny + end‑of‑task 3‑of‑3; commit `[MAIN-FORCE]` on `cad‑fusion‑live‑ms0`.  
- Never idle while token budget green; checkpoint at YELLOW and let auto‑compact reset.  
- Dedup before create, units first; reasoning=Opus / coding=Sonnet+coder‑ensemble.  
- Fix self‑compaction/auto‑startup so fleet can perform `/compact` and `/startup‑<slot>` after compaction.

**FINDINGS/BUGS**  
- `focusWtTabBySlot` fails to locate WT tabs: launcher names `<slot> | <tag>`, but resolver matches only `<slot>` or “PRISM <slot>”; live tabs use single letters (`a–z`).  
- `self‑startup` main() reads slot only from `--session-id`; missing env fallback causes bare invocations to fail.  
- DiffTokenEstimator silent ENOBUFS bug caused “0 files” lie on >1 MB diffs (~108 MB); fixed with 64 MB buffer + `--numstat`.  
- Security injection via `getDeltaBoot(since_commit)` and `estimateBetween` eliminated by shell‑free `execFileSync` and rejecting leading‑dash refs.  
- GAP‑A (master-index-precheck) not needed; architecture‑graph nodes provide path‑like info only.  
- GAP‑E premise wrong; hooks self‑dedup.

**DOMAIN SPECIFICS**  
- PRISM slot architecture: each chat runs in its own Windows Terminal tab; self‑compaction/auto‑startup rely on UIA tab focus (`wt-tab-focus.mjs`).  
- Take‑rate audit subsystem (`audit-mcp-route-takerate.mjs`, `mcp-route-takeup.mjs`) now exposes `evaluations` metric and `suppress‑candidate` verdict.  
- Engines/actions: `self‑compact.mjs`, `self‑startup.mjs`, `wt‑tab‑focus.mjs`, `session‑id‑resolve.js`.  
- Metrics/paths: WT tab names (`a…z`), session IDs (full UUID vs canonical `claude‑<8hex>`).  
- Graph auto‑use: BM25 surfaces (`master-index-precheck-inject`, `pre-{read,grep,write,bash}-graph-inject`, `node-card-prefetch-inject`).  
- Token‑economy diagnostics: `TokenEconomyEngine.computeROI`, `SessionTokenLedgerEngine.mostExpensive`.  
- DiffTokenEstimator for estimating uncommitted changes; ContextPreloaderEngine for context delta boot.  
- Prism dispatcher pattern, Zod schemas, responseSlimmer (Infinity → null).  
- CAD/CAM bridge wiring: `CreoToolkitBridge`, `CATIACAAV5Bridge`, etc.

**TOOLS USED**  
- PRISM scripts: `self‑compact.mjs`, `self‑startup.mjs`, `wt‑tab‑focus.mjs`, `session‑id‑resolve.js`, `regenerate-launch-fleet.mjs`.  
- Coder ensembles: Ollama qwen3‑coder 30b + qwen2.5‑coder 32b; Sonnet agents.  
- Consensus & safety: Octopus (`prism_ai:consensus_decide`), Opus architecture/safety layer.  
- Node scripts: `audit-unwired-engines.mjs`, `system-viz-query.mjs`, `fleet-survival-status.mjs`.  
- Git by‑pathspec commits (`[MAIN‑FORCE]`).  
- Deduplication guard `/dedup`; Obsidian vault recall via `/wiki-query`.  
- PRISM dispatchers: `prism_context`, `prism_dev`.

**OPEN THREADS**  
- Resolve WT tab naming mismatch so `focusWtTabBySlot` locates fleet’s own tab (adjust resolver or enforce single‑letter titles).  
- Validate updated resolver against live UIA output; re‑run 2‑arm + 3‑of‑3 scrutiny before commit.  
- After fix, trigger `/compact` to reset context and resume build loop.  
- GAP‑#1: full graph load (110 k nodes) exceeds heap; need streamable sidecar or warm daemon.  
- Remaining backlog units in Sierra’s vault‑ops, system‑viz, other domains.  
- Future refinement of node‑card injection (inline content for high‑confidence hits).  
- GAP‑C take‑rate measurement scope pending.  
- CAD/CAM bridge wiring in other slots.  
- Further security hardening for engines using `execSync` patterns.  
- PC‑health monitor loop idle until next reset.
