# wiring session 416be9ac (2026-05-17, 22.1MB, spine 93KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `scripts/node-staleness-rank.mjs` – staleness‑per‑truth‑source tool (delegates to MasterIndexEngine).  
- `scripts/scan-memory-obsolete-refs.mjs` – META scanner for stale memory refs.  
- `.claude/helpers/meta-task-suppressor.mjs` – meta‑vs‑domain gate (≥2 token hits, PRISM_META_SUPPRESS_DISABLE knob).  
- `.claude/hooks/stop‑memory‑size‑watchdog.mjs` – warns when `MEMORY.md` ≥90 % of 24 576‑byte ceiling.  
- `knowledge/tribal/auto‑ingested‑quarantine/*` – 326 auto‑ingested tip files moved to quarantine with allowlist entry.  
- `CLAUDE.md` – cross‑ref comments added, duplicate‑candidate note inserted; PRISM_SLOT_TASK_CLAIM_DISABLE knob removed.  
- `MEMORY.md` – compressed from 24 688 B → 23 184 B (94.3 % of ceiling).  
- `file‑relocation‑allowlist.json` – auto‑ingested pattern entry added.  
- `checkin-mike` front‑matter updated: R3 trigger count ≥3, R7 example output added.  
- Plan artifacts: `state/shared/specs/OBSOLESCENCE-CLEANUP-MS0-{PLAN, FOLLOWUPS, STALE-NODES-AUDIT, INJECTOR-UTILIZATION, CLAUDE-MD-DUPLICATION-CANDIDATES, ENVELOPE‑DRIFT-PATCHES}.md` and corresponding HTML.

---

**DECISIONS**  
- Replace custom `audit-auto-injectors.mjs` with existing `prism_hook:hook_efficiency_roi`, `coverage`, etc. to avoid duplication.  
- Use MasterIndexEngine for orphan/ghost classification; drop redundant wikiCoverage logic from node‑staleness‑rank.  
- Adopt Boris‑discipline forge audit pipeline (verification gate, peer review, HTML companion, auto‑loop).  
- Keep 13‑slot fleet (alpha…mike) and enforce slot‑aware reaper (`PRISM_FLEET_REAPER_DISABLE=1` only switch).  
- Gate noise hooks (`discipline-expert-inject`, `comprehensive-build-enforce`) via meta‑task suppressor; keep tip‑auto filtering in tribal‑by‑domain inject.  
- Persist memory watchdog and scan artifacts as META tools for weekly/daily loops.  
- Reaper always‑on by default: global scheduled task (`PRISM Fleet Reaper`) + per‑session Monitor (armed via `/checkin-alpha` or bare `/fleet-reaper`).  
- Only alpha chat may run `/fleet-reaper`; non‑alpha chats are silent no‑ops (enforced by `alpha-slot-reaper-guardian.mjs`).  
- Scheduled task uses S4U principal, restarts on failure ×3, AtStartup trigger; one elevated install is “set‑and‑forget”.  
- Monitor idempotent: existence checked with `schtasks /Query` and TaskList; steps skipped if already present.

---

**OPERATOR DIRECTIVES**  
- “continue”  
- “do a full /forge7 brainstorming session and planning to plan and account for everything. utilize /system-viz to get a full picture.”  
- “add 13th chat slot mike, update everything that needs to update to intake a 13th chat.”  
- “don't pivot every time I say something new, always work in sequential order and note new tasks until current task is done.”  
- Set the env var that makes the sweep refuse to kill/nudge/prewarm/write hint fleet‑wide before investigating a mis‑kill.  
- If another chat previously armed a Monitor, run `--uninstall` there first, then re‑arm in alpha.  
- Re‑arm after `/compact`, chat crash/force‑close, or when verifying always‑on status; use bare `/fleet-reaper`.  
- If `schtasks /Query /TN "PRISM Fleet Reaper"` shows absent, run the elevated installer (`install-fleet-reaper-task.ps1`).  
- To arm Monitor from a non‑owning slot (e.g., mike), explicitly request it or use `/checkin-golf /fleet-reaper`.

---

**FINDINGS/BUGS**  
- `MEMORY.md` truncation (100.5 % of ceiling).  
- Stale digests: ENGINE_DIGEST 72.5 h, DIRECTORY_DIGEST 103.1 h, DISPATCHER_DIGEST 1.1 h.  
- Envelope drift: 11 milestones flagged; header overcount on placeholders in several units.  
- Duplicate CLAUDE.md sections (project ↔ global).  
- Injection noise: 3/5 viz/obsidian injectors fire but skip telemetry; master‑index and memory‑relevance hooks missing writes.  
- Skill‑lint R3/R7 failures for `checkin-mike`.  
- 326 auto‑ingested tip files present in `knowledge/tribal` (noise).  
- Scheduled task logon mode is Interactive only; not hardened to S4U/AtStartup.  
- No real orphans (2 bash candidates under 45 s floor).  
- GPU commit at 86.4 % (below 90 % threshold); no coordinator action needed yet.  
- Docker health probe unavailable (host‑installed daemon – acceptable but noted).

---

**DOMAIN SPECIFICS**  
- **Engines:** 3 257 total, 2 421 wired (74 %), 836 unwired.  
- **Dispatchers:** 97 (manufacturing 29 / system 23 / ai_intel 22 / business 13 / knowledge 7).  
- **Actions:** 7 715 across dispatchers; `/fleet-reaper`, `/checkin-alpha`, `/compact`, `--uninstall`.  
- **Graph:** 72 451 nodes, 224 613 edges, 11 layers.  
- **Hooks:** discipline‑expert‑inject, comprehensive‑build‑enforce, tribal‑by‑domain‑inject, meta‑task‑suppressor, stop‑memory‑size‑watchdog, alpha-slot-reaper-guardian.mjs.  
- **Metrics:** memory usage (`MEMORY.md`), Ollama offload ratio (~20 %), wiki gap (1 348 wired engines missing entries), envelope drift count (11), sweep counts (procs, protected, owned), GPU stats, docker probe status, coordinator threshold.  

---

**TOOLS USED**  
- PRISM tools: `/system-viz`, `/checkin`, `/loop`, `/forge7`, `/forge-audit‑v2`.  
- Hooks: discipline‑expert‑inject, comprehensive‑build‑enforce, tribal‑by‑domain‑inject, meta‑task‑suppressor, stop‑memory‑size‑watchdog, alpha-slot-reaper-guardian.mjs.  
- Scripts: `node-staleness-rank.mjs`, `scan-memory-obsolete-refs.mjs`, `skill-lint.mjs`.  
- Dispatcher actions: `prism_hook:hook_efficiency_roi`, `coverage`, `performance`, etc.; `/fleet-reaper`, `/checkin-alpha`, `/compact`, `--uninstall`.  
- PowerShell script: `H:/prism/.claude/helpers/install-fleet-reaper-task.ps1`.  
- Windows task scheduler (`schtasks /Query`).  

---

**OPEN THREADS**  
1. **CLAUDE.md duplication collapse** – scheduled for 2026‑05‑24 (7‑day grace).  
2. **Injector telemetry replacement / viz/obsidian utilization** – follow‑up loop `AUTO-INVOCATION-MS1`.  
3. **Memory namespace audit** – weekly `/loop --interval 7d scan-memory-obsolete-refs.mjs`.  
4. **Daily CLAUDE.md drift check** – `/loop --interval 1d claude-md-drift.mjs`.  
5. **Digest regeneration daily** – `/loop --interval 1d regen-digests.mjs`.  
6. **Fleet reaper baseline** – `PRISM_FLEET_REAPER_DISABLE=1` is the only switch; skill remains active on all slots.  
7. **Harden scheduled task to S4U/AtStartup** (currently Interactive only).  
8. **Ensure docker health probe becomes available or document fallback.**  
9. **Implement mechanism to set protective env var automatically when a mis‑kill is detected.**  
10. **Validate that non‑alpha chats remain silent and do not spawn redundant Monitors under all conditions.**
