# wiring session 83734e27 (2026-05-19, 5.2MB, spine 50KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-WD-ASCII` – ASCII‑only fix for `ensure-all-watchdogs.ps1`.  
- `U-FR-MONITOR-SELFREAP` – added `fleet-reaper-sweep` to `PROTECTED_PATTERNS`, stops monitor from being reaped.  
- `U-FR-TRIGGER-STALL-DETECT` – stalled‑trigger detection across sweep, task‑health, and golf‑guardian; 3 files, reviewed by two reviewers.  

**DECISIONS**  
- Treat the **golf slot as a normal work slot** (no legacy hygiene flag).  
- Make `/checkin-golf` non‑skippable: always run fleet‑reaper sweep + monitor.  
- Bypass or disable legacy `golf-slot-write-allowlist.mjs` via env var or editing the PreToolUse hook.  
- Fix `ensure-all-watchdogs.ps1` encoding to ASCII only for PowerShell 5.1 compatibility.  
- Commit stalled‑trigger detection and monitor self‑reap fixes; use scheduled task as durable net, in‑session Monitor for live feed.  
- Use `/fleet-reaper-work` on smaller GPU hosts to write the work preset automatically.

**OPERATOR DIRECTIVES (verbatim)**  
- “get fleet reaper running first so I can unpause all the chats”  
- “run the fleet reaper monitor, thats the issue”  
- “complete all tasks in high roi order” (repeated)  
- “make any high roi synergy build for the neural network system and ai systems being auto used”  
- “fix the blox of whatever high roi feature is almost complete”

**FINDINGS / BUGS**  
- `ensure-all-watchdogs.ps1` had non‑ASCII characters; caused parse errors in PowerShell 5.1 – fixed to ASCII only.  
- Fleet‑reaper monitor was dying every ~5–10 min because the sweep could kill it; resolved by adding to `PROTECTED_PATTERNS`.  
- Stalled‑trigger detection added; tests passing but commit blocked by shared index contention.  
- Host saturated with 13 concurrent chats → memory ceiling reached, node spawns timing out; need to shed load or run from a separate worktree.  
- Legacy allowlist hook blocks writes unless bypassed via `PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1` or disabling the hook.

**DOMAIN SPECIFICS**  
- **Engines / actions / dispatchers**: fleet‑reaper, golf slot, chat‑slots helper scripts (`claim`, `reclaim`), `slot-bind-enforce.mjs`, `golf-slot-reaper-guardian.mjs`.  
- **Metrics / paths**: `slots["owned-by-crashed"]`, `leftover-bash-task`, `unowned`; `softRelief.{priorityDemoted,workingSetTrimmed,rssReclaimedBytes}`; GPU (`freeMb`, `utilizationPct`); Ollama (`reachable`, `loaded[]`); coordinator (`shouldPrewarm`, `prewarmFired`, `hintWritten`, `thresholdDelta`, `hintMode`).  
- **Watchdog stack**: fleet‑reaper, memory monitor, cleanup orchestrator, zombie reaper, etc.  
- **Preset env knobs**: `PRISM_FLEET_REAPER_OLLAMA_PREWARM_MODEL`, `_KEEP_ALIVE`, `_GPU_FREE_MIN_MB`, `_MEM_PRESSURE_PCT`, `_MEM_CRITICAL_PCT`, `_SOFT_RELIEF_PRESSURE_PCT`, `_HINT_THRESHOLD_DELTA`, `_BALLAST_MB`.  

**TOOLS USED**  
- PRISM skills: `/checkin-golf`, `/fleet-reaper`, `/startup-golf`, `/loop`, `/schedule`.  
- Scripts / helpers: `fleet-reaper-sweep.mjs`, `install-fleet-reaper-task.ps1`, `process-slot-map.mjs`, `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
- Hooks: SessionStart + UserPromptSubmit (`golf-slot-reaper-guardian.mjs`, `slot-bind-enforce.mjs`), PreToolUse (`golf-slot-write-allowlist.mjs`).  
- Cron utilities: `CronCreate`, `CronDelete`.  
- Monitor command via `Monitor({command:"node ... --monitor-loop", interval:300})`.

**OPEN THREADS**  
- Commit `U-FR-STALL-TESTS` (28/28 passing) still pending due to shared‑index contention; will land when index is free or from a separate worktree.  
- Need to shed load / reduce chat count or run `/startup-golf` from `H:\prism-slot-golf` to avoid shared‑index bottleneck.  
- Verify that the reaper monitor remains alive after memory pressure subsides.  
- Optional: migrate PowerShell 5.1 hooks (`ensure-all-watchdogs.ps1`, install scripts) to PowerShell 7 for future robustness.  
- Continue high‑ROI queue: `prism_safe` “some chats won’t connect”, ban‑character guard hook, powershell→pwsh migration, FLEET‑RESILIENCE Units 2–5.
