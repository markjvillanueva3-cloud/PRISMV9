# wiring session 3a620309 (2026-06-21, 4.8MB, spine 40KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `web/src/api/envelopeGuard.ts` – new shared envelope‑error guard, applied to the live `sfcApi`. Commit `82cabc91e4`. 15/15 vitest tests passed; tsc clean.  
- `state/shared/specs/PRODUCT-LAUNCH-COMPLETION-PLAN-2026-06-20.md` – full launch‑completion plan, pricing tiers, critical path, unit dependencies. Commit `b22f00ba0d`.  

**DECISIONS**  
- Prioritize SFC frontend: start with U‑SFC‑L1 page wiring (standalone calculator page).  
- Coordinate with oscar for missing `/sfc/tool-life/curve` endpoint needed by U‑SFC‑L3.  
- Route commercial layer (entitlement enforcement, Stripe billing) to papa/hotel per plan; all other product units (post‑processor, quoting, ERP, Electron/iOS/Android) will proceed in parallel once SFC is launch‑ready.  
- Adopt subscription model with one‑time purchase options as defined in the plan: SFC $299, single post‑processor $249/controller, tiered shop plans.  

**OPERATOR DIRECTIVES**  
- “Pickup Quebec task.”  
- “Analyze and assess ALL chats/sessions, road maps, and plans; then brainstorm & plan completion of SFC calculator pages, post‑processor generator, quoting feature, ERP features; launch product soon; set pricing tiers; use ultracode, ollama offloading, obsidian vault, hermes agents, harnesses, loop engineering, crons.”  

**FINDINGS/BUGS**  
- Silent‑zero regression in `sfcApi` fixed by new guard.  
- Dead `calcApi` duplicated live client – removed; all production calls now use `sfcApi`.  
- `/material/search` route missing explicit `.load()` but internal call handles it; health registry 0 is a false alarm.  
- Backend bug: `src/routes/sfc.ts` swallows `{error}` into `{}` (P2).  
- Entitlement enforcement absent; billing engines exist but have 5 P0 bugs.  
- Product readiness gaps: Post‑Processor, Quoting, ERP, Electron/iOS/Android still incomplete per plan.  

**DOMAIN SPECIFICS**  
- Quebec slot = Frontend web + phone app (Vite+React SPA).  
- Uses `resilientFetch` via `web/src/api/*`; interacts with dispatcher endpoints (`sfcApi`, `postProcessorApi`, etc.).  
- SFC calculator page consumes `/sfc/*` routes; needs 9‑axis, stochastic, tri‑compare, and tool‑life curve data.  
- Post‑processor generator, quoting feature, ERP features are backend‑heavy with thin frontends.  
- Electron/iOS/Android plans rely on Capacitor wrappers (no code yet).  

**TOOLS USED**  
- PRISM tooling: `chat-slots.mjs`, `slot-bind-enforce.mjs`, harnesses, loop engineering, crons.  
- Development stack: ultracode, ollama offloading, obsidian vault, hermes agents, vitest, tsc, Stripe billing engine, AuthEngineV7, TierLimits.  

**OPEN THREADS**  
- U‑SFC‑L1: wire standalone calculator page to enriched `sfcApi` endpoints (9‑axis, stochastic, tri‑compare).  
- U‑SFC‑L3: obtain `/sfc/tool-life/curve` endpoint from oscar’s physics lane.  
- Post‑processor generator, quoting feature, ERP units – schedule parallel work per plan.  
- Electron/iOS/Android implementation (Capacitor wrappers) – start after web launch.  
- Entitlement enforcement implementation (papa/hotel).  
- Finalize pricing tiers and one‑time payment logic (review spec §5).
