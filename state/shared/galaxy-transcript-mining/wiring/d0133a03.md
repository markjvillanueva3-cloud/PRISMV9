# wiring session d0133a03 (2026-06-10, 54.6MB, spine 498KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit 23ad2cdf3e – HONESTY RULES block (global CLAUDE.md), tournament‑rank.mjs, intake-quarantine-guard.mjs, save-workflow.md, ULTRACODE‑SYNERGY spec.  
- Commits 037e3ac930 & 3fa529432f – GroupRelativeRewardNormalizerEngine + ledger field; dispatcher `group_normalize_reward` (schema: optional `group_advantage`).  
- Commit 46553bb74a – MultiModelConsensusEngine `rankTrajectories` method, dispatcher `prism_ai:rank_trajectories`.  
- Fact‑checker agent file added to `.claude/agents/fact-checker.md` (git‑ignored).  
- Commit 1deb6ff521 – Docustrata ignore fix.  
- Commit 50f598afcf – Lock‑orphan‑leak cleanup.  
- Commit accf6f247f – Spec doc‑reflection update.  
- Commit cebde4fd94 – Ollama keep‑alive 30m, max loaded models capped at 4.  
- GWizardToolCribExportEngine – new export engine, 13/13 round‑trip tests.  
- Commit df6bf7a4d1 – CSV escaping hardening (newline handling).  
- WSL Memory Guard registered; MCP server restarted.  
- Commits 44c41ee643 & a6d80537cf – FusionMachineLibraryExportEngine, 1082 machines, round‑trip vs Autodesk goldens.  
- Commit 4ab181a78d – HolderSelectionEngine core (real holder selection).  
- Commit 82ca289ef4 – Fusion holder wiring into FusionToolExportEngine.  
- Commit 7783cbef3b – Mastercam holder wiring into MastercamToolExportEngine.  
- Commit 4a2f84eacc – HyperMill holder wire, real holders in `.hmt`, round‑trip via `prism_cam`.  
- Commit 96b7849416 – Material→type→brand tooling DB, 643 real holders to disk (`by-type-brand/<TYPE>/<BRAND>.csv`).  
- Commit 592133f902 – HyperMill per‑tool SFC cutting data (NCTools.max_spindle_speed/max_feedrate), coatingMult bug fixed.  
- Commit ed312a4d91 – Mastercam `exportFromTools` entrypoint mirroring HyperMill subset export.  
- Commit 361e4710e1 – JMCustomerVendorDatabaseEngine → prism_business, 19/19 round‑trip tests, 13/13 engine regression; live data: 473 customers, 12 vendors.  
- Commit 818870ea59 – DocuStrataMaterialPriorEngine → prism_data, 13/13 round‑trip tests, 23/23 engine regression; live data: 164 line items, $155 314.14 spend, 9 grades.

**DECISIONS**  
- Slot golf is normal work slot (no legacy hygiene flag).  
- Fleet‑reaper ownership moved from alpha to golf; durable task re‑enabled, 9/9 hygiene cluster restored.  
- Use ultracode dynamic workflow for synergy analysis (fan‑out, adversarial verify, synthesize).  
- Build GRPO engine as core RL component; add optional `group_advantage` ledger field.  
- GRPO uses z‑score normalisation with bounded rank fallback; no NaN/Infinity throws.  
- `rankTrajectories` added as separate method (mode “compare”) to avoid mutating `ask()`.  
- Ollama keep‑alive changed from –1 to 30 min; max loaded models capped at 4 for Blackwell box.  
- MCP Priority Guardian registered to demote Ollama runners, raise MCP listener priority; replaces broken CPU‑throttle task.  
- Feature crons (wiki/tribal, vault) left disabled by design; not golf’s domain.  
- Use high‑end local LLMs (qwen2.5‑coder 32B, gpt‑oss 120B) for bug discovery and scrutiny when Claude reviewers are rate‑limited.  
- Prioritize Fusion first, then Mastercam, hyperMILL next; rely on existing golden samples, no new formats.  
- Use PRISM catalog (`toolCatalogEngine`) as single source of truth for all tooling data; no CSV adapters.  
- Organize holder DB by **type → brand**; tooling DB by **material category → tool type → brand** with pre‑optimized SFM per material.  
- HyperMill `buildNCTool` pulls real holders via `holderSelectionEngine.select()`; defaults to synthetic gauge only when no match.  
- Mastercam exporter exposes subset entrypoint (`exportFromTools`) so both CAM apps emit per‑leaf libraries from same catalog slice.

**OPERATOR DIRECTIVES**  
- “Make sure fleet reaper is running and monitor it often.”  
- “Finish all remaining tasks.” Bypass legacy allowlist (`PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1`).  
- Final directive: `build` (trigger highest‑leverage remaining unit).  
- “Continue from where you left off.” `/checkin-romeo` with loop [5m] goal … resume database integration work.  
- Operator decision on re‑enabling the remaining 35 operator‑scope tasks (batch/orchestrator/feature crons).  
- “build” → proceed with next ULTRACODE order (Order 5); “continue” → resolve critical memory pressure and re‑enable reaper.  
- `/loop [10m] /goal [ utilize strongest ollama llms for searches, reads and grunt work coding to save on tokens. | goal clear: complete all remaining romeo tasks and units in current queue and overall work queue. max out database potential benefits that can affect anything throughout the app ] /yolo-mode`.  
- “Populate tool holder and tooling databases” (type→brand for holders, material→type→brand for tools).  
- “All tooling databases added to Fusion, HyperMill, Mastercam, HSMAdvisor, G‑Wizard, PRISM SFC, mill/lathe wizards, CAD/CAM galaxies.”

**FINDINGS / BUGS**  
- Legacy allowlist hook blocked writes to golf slot; bypass required.  
- HONESTY RULES block missing in global CLAUDE.md until added.  
- Tournament‑rank logic had seed‑bias bug; fixed to use bracket champion.  
- `makeInput` helper dropped `group_advantage`; corrected to forward field.  
- Ledger query test mis‑destructured result; updated to `{ tuples }`.  
- Ollama models pinned forever (`OLLAMA_KEEP_ALIVE=-1`) – resolved with TTL change & daemon restart.  
- WSL commit bloat (vmmemWSL) freed ~94 GB after shutdown.  
- 11 stale‑heartbeat “crashes” were idle/mid‑compact peers – reaper logged postmortems, no kills.  
- Zombie Reaper v2 disabled; re‑enabled and verified running.  
- MCP server transiently down; MCP Priority Guardian mitigates priority inversion but does not restart the server.  
- WSL guard installer refused due to missing token; fixed by adding literal `wsl-memory-guard`.  
- MCP server crash‑loop (4th occurrence); restart restores but root cause pending.  
- Memory pressure high earlier, resolved after daemon restart and keep‑alive fix.  
- Deepseek‑r1:70b pull stalled at manifest fetch; later resolved after daemon restart; download throttled (~1 MB/s).  
- Gpt‑oss offload returned reasoning only in `thinking`; set `num_predict≥3000`.  
- Minor ESLint/ASCII guard warnings on generator comments – resolved.  
- CSV escaping hardening fixed newline handling.  
- HyperMill tool export silently capped at 5 000 tools; raised max to 100 k.  
- Mastercam tool export used synthetic holders; replaced with real catalog via `HolderSelectionEngine`.  
- Fusion machine library exporter missing; now shipped.  
- CRLF→LF normalization occurred in some commits but had no functional impact.  
- Tool_length floor‑clamp bug in HyperMill export (real holder gauge caused negative lengths); fixed by separating gauge from stickout.  
- coatingMult substring shadow (`"altin".includes("tin")`) incorrectly applied TiN values; fixed longest‑key‑first logic.

**DOMAIN SPECIFICS**  
Engines: GroupRelativeRewardNormalizerEngine, MultiModelConsensusEngine (rankTrajectories), ConsensusFactCheckerEngine, GWizardToolCribExportEngine, FusionMachineLibraryExportEngine, HolderSelectionEngine, FusionToolExportEngine, MastercamToolExportEngine, HyperMillToolExportEngine, UltimateSpeedFeedEngine, JMCustomerVendorDatabaseEngine, DocuStrataMaterialPriorEngine.  
Dispatchers: `prism_ai:group_normalize_reward`, `prism_ai:rank_trajectories`, `prism_cam` (round‑trip test harness), `businessDispatcher` (8 actions), `dataDispatcher` (5 actions).  
Metrics: fleet‑reaper JSON (`slots["owned-by-crashed"]`, `softRelief.{…}`, `gpu.{freeMb,…}`); reaper sweep logs; commit charge; memory pressure tier; MCP listener priority; 1082 machines exported; ~74 k tools cataloged; round‑trip tests passed; 19/19 round‑trip tests for customer/vendor; 13/13 engine regression; 164 line items spend $155 314.14; 473 customers, 12 vendors.  
Paths: `/checkin-golf` pipeline, `chat-slots.mjs`, `golf-slot-reaper-guardian.mjs`, `H:/Tools/ollama/models`, `GWizard.<hash>/toolcrib.csv`, `dist/data/`, `H:/PRISM/...`.

**TOOLS USED**  
PRISM scripts: `/checkin-golf`, `/forge7`, `fleet-reaper-sweep.mjs`, `chat-slots.mjs`; Playwright for ultracode sources; Node scripts (`node H:/prism/.claude/helpers/...`); TSC, Jest (28+), Vitest, TS‑X, tsc; Linting/formatting ESLint, Prettier; Git hooks (`[MAIN] [BOOTSTRAP-SLOT-ENFORCE]`); PRISM startup pipeline (`startup.md`); `slot-bind-enforce.mjs`; `ask-ollama`; scrutiny framework (3‑of‑3 PASS); WSL Memory Guard; MCP server; Ollama Serve; `git-sync-stop` hook.

**OPEN THREADS**  
- Cross‑slot units (india: GRPO normalizer consumer; sierra: fact‑checker agent) remain unbuilt—specs delivered for peer slots.  
- Operator decision on re‑enabling remaining 35 operator‑scope tasks (batch/orchestrator/feature crons).  
- Fact‑checker agent file remains git‑ignored; not a committed deliverable yet.  
- GPT‑OSS 120b model pull still in progress (resumed detached); finalisation pending.  
- Vault crons and other feature crons remain disabled by design – operator decision required if re‑enabled.  
- 2,733 unpushed commits handled automatically by `git-sync-stop` hook; no manual action needed.  
- HSMAdvisor tool‑library export format unknown; need reader to enable round‑trip test.  
- Verify Fusion/hyperMILL/Mastercam exporters cover full 73 827‑tool catalog.  
- Audit SFC/Mill/Lathe wizard wiring for tool‑library load/query actions.  
- P2s: dispatcher‑level round‑trip test for G‑Wizard exporter, merge_existing lossy column fix.  
- Mastercam/HyperMill tree replication step 2 – build per‑leaf libraries from catalog slice.  
- Expand `HolderSelectionEngine` to include REGOFIX/TUNGALOY catalogs (future task).  
- Final integration tests for cross‑app consistency (Fusion ↔ HyperMill ↔ Mastercam).  
- Next unwired engines: ERPImportEngine, SemanticAssetIndexEngine, LocalEmbeddingEngine.
