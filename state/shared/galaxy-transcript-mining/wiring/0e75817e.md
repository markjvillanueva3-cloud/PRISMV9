# wiring session 0e75817e (2026-05-26, 13.6MB, spine 58KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Admin launcher updated to launch elevated PowerShell 7 (`Claude Code (Admin).lnk`).  
- `regenerate-launch-fleet.mjs` now unions `chat‑slots.json` with `H:/FLEET-STATUS.md`; bat regenerated (`LAUNCH-PRISM-FLEET.bat`) – commit d81e53af63.  
- Zebra 3‑step pipeline upgrade: `/precompact‑<slot>` → `/compact` → `/startup‑<slot>` (slot/golf commit 94450f405c).  
- Fleet‑reaper upgraded to detect npx‑wrapper zombies (`01220f8a5f`).  
- Git lock routing re‑enabled by removing DISABLE knobs; per‑slot worktrees now active.  
- FLEET‑STATUS.md updated to include charlie & zebra; bat regenerated (commit 675c25cb56).  

**DECISIONS**  
- Slot “golf” is a normal work slot, not legacy hygiene; fleet‑reaper ownership stays with golf.  
- Use per‑slot worktrees for all git commits to eliminate cross‑chat lock contention.  
- Duplicate scheduled tasks are an operator issue (requires admin PowerShell); cannot auto‑fix.  
- GPU contention between Ollama and NIM is doctrine‑driven; no code fix, only operator action.  

**OPERATOR DIRECTIVES**  
- Clean duplicate PRISM scheduled tasks (`schtasks /Delete …`) via elevated PowerShell.  
- Free GPU memory or stop `nim‑embed-e5` if you want Ollama LLM back.  
- Run the updated admin shortcut to ensure all monitors stay alive.  
- If launching fleet while chats are open, expect resume conflicts; consider closing current fleet first for a clean start.  

**FINDINGS/BUGS**  
- 5.5 MB stale `H:/PRISM/.git/index.lock` caused git hangs – cleared.  
- Duplicate scheduled tasks (Fleet Reaper, MCP Server, etc.) may race on locks.  
- System‑viz regeneration segfaults at “generate base graph” step (large JSON).  
- Ollama `/api/chat` hangs due to GPU contention with NIM embed.  
- Fleet‑reaper previously missed npx‑wrapper zombies; new hunter `findStaleOrphanedNodes` added.  

**DOMAIN SPECIFICS**  
- 26 NATO chat slots; slot/golf is integrator, others bound per FLEET‑STATUS.md.  
- Engines/dispatchers: fleet‑reaper, git‑lock‑sweeper, findStaleOrphanedNodes, zebra orchestrator, session‑start hooks (precompact, startup).  
- Metrics tracked: GPU freeMb/utilizationPct, ollama tags/generate endpoints, system‑graph size.  

**TOOLS USED**  
- PRISM scripts: `fleet-reaper-sweep.mjs`, `git-lock-sweeper.mjs`, `findStaleOrphanedNodes` lib, `regenerate-launch-fleet.mjs`, `zebra-orchestrator-lib.mjs`, `scripts/regen-viz.mjs`.  
- Dispatchers/hooks: chat‑slots claim, precompact‑handoff, session‑start‑auto‑resume.  

**OPEN THREADS**  
- Resolve system‑viz segfault (large graph handling).  
- Address Ollama `/api/chat` GPU contention (operator‑level stop/restart of NIM or switch to CPU).  
- Operator cleanup of duplicate scheduled tasks.  
- Consider adding a `--dry-run` / `--fresh` mode to the fleet launcher for safer launches.
