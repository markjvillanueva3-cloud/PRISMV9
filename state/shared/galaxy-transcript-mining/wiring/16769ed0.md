# wiring session 16769ed0 (2026-06-21, 28.4MB, spine 282KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**

- `U‑REWRITER‑SKIP‑LOOP‑DIRECTIVES` – 6a7b572eae  
- `U‑REWRITER‑CHATMODEL‑SELECT` – 778be5414f  
- `U‑REWRITER‑FALSE‑WARM‑COMMENT‑FIX` – fee67d7760  
- `U‑LOADED‑CHAT‑STRICT‑OPTION` – 1c6abe2878  
- `U‑OLLAMA‑PS‑PROBE‑DEDUP` – 70b94eb1c9  
- `U‑MCP‑ROUTE‑SUPPRESS‑ISVERBOSEBASH` – e0b3df1ea0  
- `audit‑viz‑first legend correction (renderMd)` – 5be19a26c2  
- `docstring legend update for audit‑viz‑first` – 0aab43dadc  
- `injection‑dedup guard + pruneTag migration` – 8d344941fe  
- `injection‑dedup lib + pruneTag implementation` – d3e0b7ebaf  
- `U‑PSN‑CHECKLIST‑TEST‑ENABLED` – 4307ece067  
- Feature‑routing “both” arc – c5d2174fbf, 16269fd2ad, aadf5a5177  

**DECISIONS**

- Feature‑routing “both” arc shipped; no further work.  
- Rewriter revival closed (low ROI).  
- Prioritized high‑ROI token‑efficiency units over rewriter revival.  
- Adopted strict `opts.strict` gate on chat model selection.  
- Consolidated duplicate `/api/ps` probes into single lib (`scripts/lib/ollama-ps-probe.mjs`).  
- Suppressed `isVerboseBash` nudge; telemetry preserved.  
- Parked ask‑ollama loaded‑first wiring pending Zulu commit & attribution rule.  
- Replaced generic `pruneExpired` with per‑tag `pruneTag`; migrated 12 evictors + wrappers.  
- Added injection‑dedup guard to audit‑viz‑first‑inject; migrated 10 hooks to use `pruneTag`.  
- Re‑lighted frozen read‑auto-limit ledger via subprocess hook.  
- Self‑compact cannot auto‑reset in fast loop at YELLOW; requires manual `/compact` or slot re‑claim.

**OPERATOR DIRECTIVES**

- Finish any in‑flight unit, descend NEVER‑IDLE ladder (own‑domain → FIXES → WIRINGS → GHOSTS).  
- Use by‑pathspec commits only; no backticks in git‑m bodies; enforce R12 fail‑loud, R13/R15/R16 comprehensive.  
- Prioritize high‑ROI token‑efficiency units; idle if dry and healthy.  
- Monitor PC health; perform orphan cleanup & self‑compact when thresholds exceeded.  
- Use Ollama coder ensemble (`qwen2.5-coder:32b`, `qwen3-coder:30b`), Sonnet fallback, Hermes sonnet agents, octopus consensus for decisions.  
- Commit with `[MAIN‑FORCE]`; update handoff after each unit; record lessons.

**FINDINGS/BUGS**

- Prompt‑rewriter mis‑handled AUTONOMOUS BUILD directives → fixed by LOOP_DIRECTIVE_RE.  
- ask‑ollama pickModel incorrectly selected vision models; corrected via new helper (`ollama-loaded-chat-model.mjs`).  
- Rewriter header claimed nonexistent 24h keep_alive pin → removed.  
- Self‑compact race at YELLOW; requires manual `/compact` or slot re‑claim.  
- Ask‑ollama wiring conflicts with Zulu codegen edits; pending attribution resolution.  
- `isVerboseBash` nudge fires 417×, 0 takes → suppressed; telemetry preserved.  
- Decay actor ignores suppress-candidate verdicts (takes = 0).  
- Mislabeling of `verify-wiring` classifiers in audit; should be `retune`.  
- Route‑suggest take‑rate audit mapping intentional; no change needed.  
- Large‑read‑digest advisory muted; 3314 fires are probe counters.  
- `audit‑viz‑first‑inject` lacked dedup guard → added.  
- `pruneExpired` evicted higher‑TTL entries → replaced with per‑tag `pruneTag`.  
- 4 psn‑prompt‑checklist tests failed due to env flag; fixed.  
- Frozen read‑auto-limit ledger re‑lighted via subprocess hook.  
- Aggregator correctly maps nudge‑emitted hits; no savings inflation.

**DOMAIN SPECIFICS**

- PRISM 26‑slot autonomous fleet; alpha focuses on token‑efficiency & Obsidian memory governance.  
- Key engines/actions: `prompt-rewriter-ollama.mjs` (UserPromptSubmit), `ollama-loaded-chat-model.mjs`, `ask-ollama.mjs`.  
- Metrics/paths: `/api/ps` warm‑probe, `/api/tags` up‑probe; GPU shared RTX 6000 96 GB.  
- Feature‑routing “both” arc deployed; octopus consensus & Hermes‑forge agents active.  
- Route‑suggest hook chain: telemetry (`_recordRouteFires`) → hint trim (`appendActionHints`) → decay filter → take‑rate footer.  
- Token‑efficiency doctrine: suppress low‑take high‑fire nudges, preserve telemetry.  
- Shared injection‑dedup cache (`injection-dedup-cache.json`): 12 evictors + wrappers; per‑tag TTL 5 min–24 h; uses `pruneTag`.  
- Ledger for read‑auto-limit: `{ts, kind:"hit"|"nudge-emitted", file, file_kb, est_tokens}`.  
- Injection‑dedup lib (`scripts/lib/injection-dedup.mjs`): hashBlock, shouldEmit, recordEmit, formatDedupedMarker, pruneExpired → replaced by `pruneTag`.  
- Audit modules: `audit-mcp-route-takerate.mjs`, `route-suggest-decay.js`; hooks include `audit-viz-first-inject`, `galaxy-claudemd-inject`, etc.

**TOOLS USED**

- PRISM commands: `/checkin-alpha`, `/compact`, `node scripts/audit-unwired-engines.mjs`, `node scripts/fleet-survival-status.mjs`, `node scripts/self-compact.mjs`.  
- Node scripts: `scripts/lib/ollama-ps-probe.mjs`, `scripts/ask-ollama.mjs`, `audit-mcp-route-takerate.mjs`, `system-viz-query.mjs`, `vault-health.mjs`, `audit-unwired-engines.mjs`.  
- Scrutiny framework: `node .claude/scripts/scrutiny-3way.mjs` (two‑arm reviewer + code‑analyzer).  
- Test runner: direct `node <file>.test.mjs`.  
- Self‑compact helper: `scripts/self-compact.mjs`.  
- Ollama coder ensemble (`qwen2.5-coder:32b`, `qwen3-coder:30b`), Sonnet fallback, Hermes sonnet agents.  
- Octopus consensus, Opus.

**OPEN THREADS**

- Build fleet offload‑warmth unit: keep a small coder warm in `/api/ps`.  
- Continue NEVER‑IDLE ladder with next high‑ROI token‑efficiency unit; monitor self‑compact race at YELLOW.  
- Ask‑ollama loaded‑first wiring pending Zulu commit & attribution rule resolution.  
- Correct audit `verify-wiring` classifier to `retune`; verify credit‑path exercised logic.  
- Identify and suppress additional low‑take high‑fire nudges (`isLargeRead`, `doctrineSurface`, `backendAuditChain`).  
- Ensure self‑compact reliability: monitor slot claim status; re‑claim alpha when needed.  
- Complete WIRINGS audit‑unwired‑engines, GHOST builds, miscellaneous backlog tasks.  
- Verify feature‑routing arc regressions (none expected); confirm all ledgers live and test coverage complete.  
- Update handoff file with latest unit status and lessons.
