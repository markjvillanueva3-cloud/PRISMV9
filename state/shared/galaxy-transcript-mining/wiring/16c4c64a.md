# wiring session 16c4c64a (2026-06-04, 22.6MB, spine 128KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `c0ce903f35` – U‑CGP‑CONCURRENCY: async worker pool for vision OCR on RTX 6000.  
- `8fd4193762` – U‑CGP‑MEASURE: real‑data E2E throughput (0.189 pages/min/worker, 2× effective).  
- `ca4395947b` – U‑CGP‑NUMPARALLEL‑RECO: advisory to set `OLLAMA_NUM_PARALLEL=4` on Blackwell.  
- `9b5aa4c2b6` – U‑WIRE‑SHOP‑OUTCOME‑INGEST: wired ShopOutcomeIngestProcessorEngine → `prism_dev:shop_outcome_ingest`.  
- `267718a1ee` – U‑WIRE‑GCODE‑MATERIAL‑PARSE: wired GCodeMaterialParserEngine → `prism_dev:gcode_material_parse`.  
- `b902ac2024` – classifier‑aware hunt tool (`classify-engine-reachability.mjs`, test, artifact).  

**DECISIONS**  
- Drive extraction concurrency from GPU profile (`detectGpuTier().concurrency`).  
- Measure real throughput from checkpoint instead of estimates.  
- Fix `OLLAMA_NUM_PARALLEL` mismatch by recommending host‑aware config (4 for Blackwell).  
- Adopt classifier‑aware reachability to filter ghost.unwired labels before wiring.  
- Commit changes atomically with path‑scoped commit to avoid absorption.  
- Route **SBOMReviewEngine** → `sierra` (system‑viz, infra‑dependent type b).  
- Route **CNCToolOffsetPersistence** → `delta/oscar/whiskey` for future wiring; no code change now.  

**OPERATOR DIRECTIVES**  
- `/loop [5m] → /goal [ complete classifier aware hunt ]`.  
- Wire the 6 genuine orphans identified by the classifier:  
  - CircularDependencyEngine → `prism_dev:dependency_cycle_analyze`  
  - SemanticAssetIndexEngine → `prism_dev:semantic_asset_index`  
  - PSNHealthCheckEngine → `prism_session:psn_health_check`  
  - OperatorPreferencesEngine → `prism_session:operator_preferences`  
  - SlotSessionHistoryEngine → `prism_session:slot_history`  
  - CNCToolOffsetPersistenceEngine → `prism_turning:cnc_tool_offset` (pending routing to delta/oscar/whiskey).  

**FINDINGS/BUGS**  
- R12 bug: `ExpandingMandrelEngine` misclassified as genuine; capability wired elsewhere.  
- `OLLAMA_NUM_PARALLEL=2` throttles Blackwell to 2×; config script sets 4 but not applied.  
- Ghost.unwired false positives (~49 % of 110 audit list).  
- Commit absorption into xray’s commit due to slot‑enforce hook race; resolved with path‑scoped commit.  

**DOMAIN SPECIFICS**  
- Engines: CircularDependencyEngine, SemanticAssetIndexEngine, PSNHealthCheckEngine, OperatorPreferencesEngine, SlotSessionHistoryEngine, CNCToolOffsetPersistenceEngine, ShopOutcomeIngestProcessorEngine, GCodeMaterialParserEngine, SBOMReviewEngine.  
- Dispatchers: `prism_dev` (devDispatcher.ts), `prism_session`, `prism_turning`, `businessDispatcher`.  
- Metrics: pages/min/worker for OCR extraction; concurrency speedup; `OLLAMA_NUM_PARALLEL` slots.  
- Paths: `scripts/batch-ollama-vision-extract.mjs`, `lib/catalog-gpu-profile.mjs`, `measure-catalog-extraction-rate.mjs`, `classify-engine-reachability.mjs`.  

**TOOLS USED**  
- PRISM skills: `/checkin`, `/startup`, `/loop`, `CronCreate`; skill hooks (`slot-bind-enforce.mjs`, `chat-slots.mjs`).  
- Scripts: batch‑ollama‑vision‑extract, catalog‑gpu‑profile, measure‑catalog‑extraction‑rate, classify‑engine‑reachability.  
- Dispatchers: devDispatcher.ts, businessDispatcher.ts, etc.  
- Testing tools: Jest / test scripts, round‑trip tests.  

**OPEN THREADS**  
- Wire the remaining 6 genuine orphans with full scrutiny and tests.  
- Integrate classifier into system‑viz ghost.unwired detector to eliminate false positives.  
- Apply `OLLAMA_NUM_PARALLEL` config change on Blackwell hosts (restart Ollama).  
- Monitor GPU utilization and verify 3× throughput once slots increased.  
- Final wiring of **SBOMReviewEngine** in `sierra`; **CNCToolOffsetPersistence** to `delta/oscar/whiskey`.
