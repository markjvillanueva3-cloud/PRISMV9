# wiring session 32a39c0c (2026-05-16, 34.7MB, spine 179KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U‑PPL‑A1 (TurningMinFingerprintEngine) + 69 tests shipped; peer commit `4050f3b35`.  
- U‑PPL‑B1 (ProgramReoptimizationOrchestratorEngine) + 48 tests shipped; peer commit `4050f3b35`.  
- Phase21 PDF splitter (`phase21-split-containers.py`) built, smoke‑tested on 5 docs; running as background job on all 8 396 multi‑print containers.  
- `synergy-regression-watch.mjs` daily watcher verified; logs to JSONL & log file.  
- `install-synergy-watch-task.ps1` Windows scheduled‑task installer confirmed.  
- Audit artifacts: `FORGE-AUDIT-v2.md`, `AUDIT-DEV-TOOLS-PIPES-2026-05-16.html`.  
- Docustrata pipeline upgrades:  
  - `phase16-blueprint-program-join-v6.py` (VLM‑aware join).  
  - `docustrata-pipeline.py` extended to 9 stages (`join`, `customer-rollup`).  
  - `phase15-deep-rescan-parallel-memsafe.py` wired for OOM‑safe OCR.  
- `DocustrataCustomerIndexEngine.ts` + dispatcher case + 61‑test suite (all PASS).  
- Built `helper-orphan-rank.mjs`; 8/8 tests passed; baseline: 187 helpers, 76 orphan (40.6 %).

**DECISIONS**  
- Docustrata milestones MS‑DOCU‑FINISH/INGEST complete → `/goal` met.  
- Proceed with U‑PPL‑B1 after confirming landing; B2 remains research handoff.  
- Build Phase21 splitter first; then schedule full re‑index of 104 K new PDFs (phase6→7→15) and orchestrate unified OCR pipeline.  
- Use Docker + GPU for stages 3–5 once daemon restored; otherwise CPU fallback.  
- Treat Docustrata pipeline as declarative orchestrator: add join & customer rollup so one command yields final index.  
- Keep VLM OCR optional; use only when present to avoid wasted GPU time.  
- Switch deep‑rescan to memsafe variant to prevent OOM on huge PDFs.  
- Make main() case‑insensitive on win32; reaped orphan processes before retrying writes; replaced NUL bytes with spaces; added anchor, non‑degeneracy floor, bracketing negative control to tests; reclassified helpers: 91 wired‑strong + 20 cross‑helper.

**OPERATOR DIRECTIVES**  
- Execute 104 K PDF re‑index (phase6→7→15) and build full OCR/print‑reading orchestrator pipeline.  
- Restart Docker Desktop / WSL to enable GPU containers; launch PaddleOCR, eDOCr2, VLM stages.  
- Monitor background Phase21 job (`b7dvt7ale`) for completion counts; re‑run idempotently if fails.  
- `/loop continue finding development tool and pipeline upgrades` – loop target 6 (F3/F6 META artifacts).  
- No new commits requested; changes remain uncommitted until explicit commit.

**FINDINGS/BUGS**  
- 5 of 7 hand‑built `.MIN` templates in `Resources/MACRO PROGRAMS/` corrupted (null bytes, git‑blob header, JSON state); no repo history.  
- Lane‑guard mis‑resolved foxtrot slot; resolved by unstaging peer‑claimed dispatcher files and letting B1 land.  
- Docker daemon down (API 500) blocks GPU stages; must restart.  
- Phase21 splitter running; no errors reported.  
- Synergy‑watch: `cmd /c` quote‑strip trap caused false exit code 1; fixed with double‑outer quotes.  
- Docustrata delta: earlier “104K” phantom resolved to 7,235 PDFs; pipeline now bridges gap automatically.  
- Hook count budget: current set 605 growing ~205/day; no hard budget yet.  
- `/tmp` path resolution fails on Windows → main() not firing.  
- Transient `xmalloc` error during hook edit – not a code bug.  
- Literal NUL bytes at offsets 7685/7695 corrupt script files.  
- PostToolUse hook crashes repeatedly (fork‑storm); PreToolUse:Write hook crashed initially.

**DOMAIN SPECIFICS**  
- Docustrata root `H:/prism/Docustrata`: ~215 k PDFs, 8 396 containers ≥2 prints each.  
- U‑PPL‑A1 parses Okuma G‑code; fingerprints programs into LathePartFamily using 16‑dim vectors & cosine distance.  
- U‑PPL‑B1 orchestrates program re‑optimization with safety checks and diff output.  
- B2 wiring unit connects MillProgramOptimizerEngine to `prism_cam:program_optimize` & `prism_mill:mill_program_optimize`.  
- Unified OCR pipeline stages: CPU classifier → Phase21 splitter → GPU PaddleOCR → eDOCr2 → optional VLM → PRISM RAG engines.  
- Engines: DocustrataCustomerIndexEngine (index lookup), BlueprintProgramJoinEngine.  
- Dispatchers: cadDispatcher case added for `docustrata_customer_index`; git post‑commit, PreToolUse & PostToolUse hooks.  
- Metrics/paths: `phase20-verified-prints-by-doc.jsonl` → `phase23-customer-folder-index.json`.  
- Synergy ratio tracked by `system-synergy-map.mjs`.  
- Core scripts: `helper-orphan-rank.mjs` (F6), `cold-script-rank.mjs` (F5).  
- Metrics: 187 helpers, 76 orphan; cross‑helper 20; wired‑strong 91.

**TOOLS USED**  
- PRISM helpers: `/checkin`, `/loop`, chat‑slots, milestone‑tracker.  
- System utilities: `git`, Docker (WSL), PyMuPDF, PaddleOCR, eDOCr2, VLM container, rtk compression wrapper, system‑viz queries, obsidian indexing.  
- Hook pipelines: audit‑roadmap‑drift.mjs, precompact‑pending‑guard.mjs, lane‑guard, write‑allowlist for golf slot.  
- PRISM: `system-viz`, `forge-audit-v2`, `docustrata-pipeline.py`, `phase16-blueprint-program-join-v6.py`, `phase15-deep-rescan-parallel-memsafe.py`.  
- External scripts: synergy-regression-watch.mjs, PowerShell installer.  
- Testing: Vitest, TypeScript compiler, per‑file scrutiny gate.

**OPEN THREADS**  
- B2 not yet built; next foxtrot session must execute wiring unit in one pass.  
- Full re-index of 104 k new PDFs pending (phase6→7→15).  
- Docker/GPU stages blocked until daemon restart; resolve .MIN corruption via backup or manual restoration.  
- Verify completion of Phase21 job and integrate output into Docustrata pipeline.  
- Remaining META artifacts (F3/F6): hook-count-budget.mjs, stale-milestone-rank.mjs, script-count.mjs, helper-orphan-rank.mjs.  
- Scheduler persistence: verify PowerShell installer works on all target Windows versions (admin elevation).  
- Documentation: update CLAUDE.md § Recent regressions with synergy‑watch alert text once committed.  
- File 2 (`cold-script-rank.mjs`) pending write & full test run after retrying hook crashes.  
- Further validation of cross‑helper classification and orphan rate correction.  
- Resolve fork‑storm issues in PreToolUse/PostToolUse hooks.  
- Final baseline verification for F6 metrics.
