# wiring session 859c0089 (2026-06-02, 11.6MB, spine 55KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED (builds/commits)**  
- 7 commits on `cad-fusion-live-ms0` (T1, T3/T4, G1, G2, G2‑fix, G3).  
- All commits are tsc‑clean, 2‑reviewer per‑file scrutiny passed.  
- Generated artifacts:  
  - Fusion `.tools` JSON libraries (218 tools × 7 presets each).  
  - CSV tool libraries with real `holder_segments`.  
  - 6 Fusion machine XML definitions.  
  - Compatibility gate (`compatibleIsoGroups`) applied to all preset rows (1526 → 1151 rows).  

**DECISIONS (architecture/scope + why)**  
- **Use `lookupCuttingData()`** for optimal SFM/feed instead of the heavy `UltimateSpeedFeedEngine.calculate()`.  *Why:* avoids OOM in vitest, provides deterministic per‑ISO presets.  
- **Implement a material‑compatibility gate** (`CoatingSelectionAdapter.compatibleIsoGroups`) to filter preset rows by coating/substrate chemistry.  *Why:* satisfies new goal constraint and fixes safety P0 bug (unknown coatings).  
- **Lean TSX generators** for G3, G4, G5 that import only catalog‑free engines (`UltimateSpeedFeedEngine`, `CoatingSelectionAdapter`, `JmDieMachineConfigEngine`).  *Why:* prevents the ESM `__dirname` crash and keeps memory usage low.  
- **Revert unverified foundational change** to `catalogLoader.ts`.  *Why:* build/test could not be verified under current fleet load; safety discipline forbids shipping an unverified file.  

**OPERATOR DIRECTIVES (verbatim asks)**  
- “continue building the tool, tool holder and machine database for fusion that can then be converted to hypermill and mastercam.”  
- “goal clear: all data fields accurately filled out in fusion, hypermill and mastercam for all tooling including dimensions, collision avoidance data, sfm data, parameters etc...categorize by material type and only populate each material type domain compatible with that material type, make sure cutting data, parameters and sfm data are accurate for each tool relative to the material domain.”  
- “The Stop hook is right — the goal needs all three CAM formats and G4/G5 remain. I'm at ~72% (auto‑compact at 80%), so I have room for one more unit.”  

**FINDINGS/BUGS**  
- **ESM `__dirname` crash** in `catalogLoader.ts`; fixed by reverting change until verified.  
- **Heavy memory OOM** during vitest when importing full `ToolCatalogEngine`.  *Mitigation:* use lean generators that avoid the catalog import.  
- **Performance regression** from calling `UltimateSpeedFeedEngine.calculate()` per tool; replaced with lightweight table lookup (`lookupCuttingData`).  
- **Safety P0 bug:** unknown coatings defaulted to `[P,M,K,S,H]`; fixed to conservative `[P,M,K]`.  
- **HSS compatibility bug:** HSS drills incorrectly excluded cast‑iron (K); corrected to include K.  
- **TiCN regex ordering safe**; no change needed.  
- **Test bugs in G4 generator** (multi‑line INSERT parsing, off‑by‑one capture group).  *Fixes applied.*  

**DOMAIN SPECIFICS (engines/actions/dispatchers/metrics/paths unique to this galaxy)**  
- Fusion `.tools` JSON format and `CSV_TOOLS_VERSION_1` CSV schema.  
- ISO material groups: P, M, K, N, S, H.  
- Compatibility matrix from `CoatingSelectionAdapter`.  
- Cutting physics via `UltimateSpeedFeedEngine.lookupCuttingData()`.  
- Fusion machine XML (`hsmworks`) definitions.  
- HyperMILL SQLite schema (Tools, GeometryClasses, CuttingMaterials, etc.).  
- Mastercam `.tooldb` SQLite format; existing engine emits JSON `.mcam-tools`.  

**TOOLS USED**  
- PRISM CLI: `/checkin`, `chat-slots.mjs` helpers, `slot-bind-enforce.mjs`.  
- TypeScript compiler (`tsc`), Vitest test runner.  
- Build scripts: `build:fast`.  
- Engines imported: `UltimateSpeedFeedEngine`, `CoatingSelectionAdapter`, `JmDieMachineConfigEngine`.  
- Exporters referenced (but not used due to ESM issue): `HyperMillToolExportEngine`, `MastercamToolExportEngine`.  

**OPEN THREADS**  
1. **G4 HyperMILL generator** – build lean TSX version that emits `.sql` DDL+INSERTs using the shared model and compatibility gate; verify against real JM crib.  
2. **G5 Mastercam exporter** – produce JSON `.mcam-tools` (or SQLite `.tooldb`) from the same shared model; ensure schema matches Mastercam 2025+.  
3. **Re‑apply & verify `catalogLoader.ts` ESM fix** once fleet load subsides, to enable future use of full export engines if desired.  
4. **Final commit of G4/G5** – after successful tests and no OOM, push to repo; ensure Stop hook condition satisfied (all three formats populated with accurate data).
