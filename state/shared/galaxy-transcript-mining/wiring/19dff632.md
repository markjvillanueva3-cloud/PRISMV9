# wiring session 19dff632 (2026-06-09, 11.7MB, spine 99KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e44a3a1592` – wired orphaned PayrollLiabilityFilingEngine methods (`compute940`, `generateW2`, `reconcileW2sTo941`, `contractor1099Totals`) into `businessDispatcher`.  
- `e649790e76` – added `remitLiability` wiring + 14 round‑trip tests.  
- `f069052772` – verified 6 “false gaps” (business route, hotel portal route, realtime route, payroll‑filing wiring, DocuStrata population, JM data fill).  
- `9dfd621910` – added `scripts/mine-hotel-transcripts.mjs`.  
- `61518eb988` – fixed miner bugs; regenerated digests.  
- `099e6b92bd` – committed roadmap spec (`state/shared/specs/HOTEL-FORGE-ROADMAP-2026-06-09.md`).  
- `6b9ed8520d` – FALSE‑WIRE‑REGRESSION‑GUARD unit built, 20/20 passing, 3‑of‑3 scrutiny cleared.  

**DECISIONS**  
- Use Ollama (`qwen2.5-coder:32b`) for routine summarization; reserve `gpt-oss:120b` for heavy synthesis.  
- Commit prefixes: `[BOOTSTRAP-SLOT-ENFORCE]` (slot commits) and `[MAIN]` (shared‑tree).  
- Promote `qwen2.5-coder:32b` into cost‑router “strong” path; keep `gpt-oss:20b` for ultracode workflow.  
- Apply R15 wiring close‑out: expose orphaned engine methods via dispatcher + tests.  
- Verify‑first approach for shipped units (constants audit, mobile wire‑exempt).  
- 3‑of‑3 scrutiny gate with independent reviewers.  
- Regex guard broadened to match `Unknown business action: ${action}` and `{note:"method not callable"}` patterns.  

**OPERATOR DIRECTIVES**  
- Resume workflow after rate limits; use Ollama for token efficiency, ultracode if needed.  
- Execute `U-HOTEL-PORTAL-PERSISTENCE` next (after verify units).  
- Decide persistence strategy: `/compact` then start or push‑through immediately.  
- Follow up on `ALLOWLIST-WRITE-REVIEW` and `REALTIME-VERIFY`.  

**FINDINGS/BUGS**  
- Stale audit claimed missing tax engine; all tax engines built & wired.  
- PayrollLiabilityFilingEngine had 5 orphaned methods – wiring added.  
- Off‑domain drift: ~11 of 19 hotel sessions performed non‑ERP work due to unfiltered loop queue.  
- Shared‑tree commit contention (`index.lock`) recurring issue.  
- Mobile engines: only `MobileVoice` unwired; others wired/tagged.  
- Allowlist regression guard uncovered false wires for `marketplace_lead_get`; regex fixed.  
- Q2S DFM contract bug confirmed (non‑iterable `ctx.features`).  

**DOMAIN SPECIFICS**  
- Slot: **hotel** (ERP/HR/business specialist).  
- Engines: `PayrollLiabilityFilingEngine`, `CustomerPortalEngine`, `MarketplaceSeedingEngine`, `MobileVoice`.  
- Dispatchers: `businessDispatcher.ts` with lazy `getEngine`; actions:  
  - `payroll_compute_940`, `payroll_generate_w2`, `payroll_reconcile_w2_941`, `payroll_contractor_1099_totals`, `payroll_remit_liability`.  
  - `marketplace_lead_get` (uses `supplierId`).  
- Metrics: IRS values – FUTA $72, W‑2 box4 $3,100, SS cap $10,918.20, Medicare $4,075, 1099 $1,500.  
- Paths: `H:/prism/.claude/helpers/chat-slots.mjs`, `scripts/mine-hotel-transcripts.mjs`, `businessDispatcher` tests.  

**TOOLS USED**  
- `ask-ollama.mjs` (summarization).  
- `mine-hotel-transcripts.mjs` (transcript miner).  
- `chat-slots.mjs` (slot claim/reclaim).  
- Ultracode workflow (`resumeFromRunId`).  
- 3‑of‑3 scrutiny gate (Codex + Claude).  
- BusinessDispatcher test harness.  
- Git hooks: slot‑commit‑enforce, bootstrap prefix.  
- PRISM guard & detector regex; `__resetForTests()` helper.  

**OPEN THREADS**  
- Build `U-HOTEL-PORTAL-PERSISTENCE` (persist CustomerPortalEngine maps via SQLite‑WAL).  
- Allowlist‑write/realtime verification pending persistence.  
- Resolve off‑domain drift by filtering loop queue.  
- Mitigate shared‑tree commit contention (`index.lock`).
