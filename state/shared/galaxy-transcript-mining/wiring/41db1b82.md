# wiring session 41db1b82 (2026-05-17, 97.3MB, spine 667KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `scripts/lib/tribal-graph-clusters.mjs` (15c161f63) – 692 LOC, Jaccard backbone L0‑L8, 62 tests.  
- `scripts/lib/tribal-graph-embedding.mjs` (862137931) – 736 LOC, Ollama nomic‑embed‑text 768d, cosine ≥ 0.75, 109 tests.  
- `course-mapper-lib.mjs` (ac8d56da2) – 135 course nodes, 54 tests.  
- `syllabus-extract.mjs` (7c87d216) – extracts syllabus level from 3 courses.  
- `unzip-extract.mjs` (fe1a32efc) – processes 227 zips → 209 course‑tribal nodes, 109 tests.  
- `course-embed.mjs` (fc608e6a0) – embeds 192 courses, creates 618 semantic wires; idempotent checkpointed.  
- `U-KC-B1` (aa0335a8d) – MIT‑OCW → tribal tips: 126 tips, 44 tests.  
- `U-KC-B2` (3d9324f2a) – Monolith → tribal tips: 133 tips, 52 tests.  
- `U-KC-B3` (44980b391) – Round‑trip wiring test: 8 vitest cases, 7141 doc‑learned tips loaded.  
- Ledger artifacts: `U-KC-C1` (e4a48ebf3), `U-KC-C2` (05152dff62), `U-KC-D1` (`cd00120dcd`, `66aa07afa4`), `U-KC-E1` (d99df13f5).  
- Wiki/os namespace schema `U‑CK04` (4da65465c7) – 11 entities, 6 kinds.  
- Command‑frontmatter JSON schema `U‑CK06` (790ec17f25).  
- `/pick-task` audit report `U‑CK10` (0ca1d0feb0).  
- KnowledgeInjectionPipelineEngine + CLI runner – 28 hermetic + real‑data E2E tests; 126 routed, 110 eligible.  
- Numerical primitives: OperatorSplittingMethod, ODEIntegrator, LinearStateSpaceModel, FiniteDifferenceMethod, GradientDescent, FiniteElementMethod1D, LagrangianMechanics – 236 tests pass.  
- SafeExpressionEvaluator – 60 tests.

**DECISIONS**  
- Adopt Algorithm C: Jaccard for aggregation backbone (L0‑L3), Ollama nomic‑embed‑text cosine for lateral wires.  
- Two‑arm per‑file scrutiny gate; stable‑session‑id helper for handoff persistence.  
- Switch to `node:test` from Vitest due to incompatibility.  
- No auto‑generated engines; candidate formulas queued for human review.  
- Use PRISM dispatchers (`tribal_search`, `tribal_capture`, etc.) – no new stubs.  
- Keep provenance tags exempt from `MAX_TAG_LEN`; never silently drop them.  
- Force‑take India slot via `/checkin-india`; bind handoff to `india-work`.  
- Adopt 20‑iteration /loop window; continue until operator stops with `CronDelete`.  
- Enforce precompact‑pending‑guard before `/compact`.

**OPERATOR DIRECTIVES**  
- `/loop extract college coursework until all units are complete = /goal`.  
- `/checkin-india` claim India slot, run ingestion pipeline (U‑KC‑B1/B2), convert to PRISM modules, enqueue into `/forge` queue.  
- `/yolo-mode` – autonomous, no questions.  
- `/pick-task` audit & deprecation report; `CronDelete 32fcf842` to stop loop.  
- Run `/forge-triple` on selected nodes (operator‑gated).  
- Execute `--apply` on KnowledgeInjectionPipelineEngine to commit real bindings.

**FINDINGS/BUGS**  
- Fixed silent success on malformed embedding input; added dimensionality guard in `mergeIntoCheckpoint`.  
- Corrected stats aggregation: separate malformed vs fetch failures.  
- Removed U+001F contamination from embedding text; deterministic sorting.  
- Resolved P0/P1 bugs in converter (confidence floor, domain key separator, atomic write).  
- PDFs in MIT courses are scanned images → use `data.json` inside zips.  
- 17 catalog‑only courses lack zip – remain metadata‑only.  
- `classifySchool` returned array → fixed to object `{code,score,matched}`.  
- Control‑char stripping bug fixed via `String.fromCharCode`.  
- Regex boundary issue corrected; limit truncation risk resolved by setting `TRUNCATION_PROOF_LIMIT=10000`.  
- Negative zero coefficient bug in LinearStateSpaceModel Faddeev–LeVerrier normalized to +0.  
- Fork‑storm host‑level issue resolved by janitor cleanup.

**DOMAIN SPECIFICS**  
- Graph-of-Graphs: 9‑level hierarchy (atom → universe), aggregation DAG, lateral wire types (`relates-to`, `prerequisite-for`).  
- Taxonomies: `SCHOOL_TAXONOMY` (33 codes), `DOMAIN_TAXONOMY` (13 domains).  
- Variability Layer: `required_inputs`, `optional_inputs`, `output_variables`, `confidence_fn`.  
- Embedding: Ollama nomic‑embed‑text 768d, cosine similarity, U+001F separator, djb2 hash.  
- State Management: checkpoint.jsonl, atomic save/merge, vector shape validation.  
- KnowledgeTip schema: id,title,body,category,tags,confidence,source,created_at; optional domain.  
- Engine overrides: `id=TK‑DL‑<docId>-NNN`, `source=document:<docId>`, tags append `doc:<docId>`.  
- Dispatcher return shape: `{content:[{type:"text",text:JSON.stringify(slimResponse(result))}]}`.  
- Node types: knowledge, algorithm, formula, engine, skill, pipeline.  
- Metrics: 209 course‑tribal nodes, 192 syllabus level, 618 lateral wires; 126/133 tips from B1/B2; 7141 doc‑learned tips loaded.

**TOOLS USED**  
- PRISM scripts: tribal-graph-clusters.mjs, tribal-graph-embedding.mjs, course-mapper-lib.mjs, syllabus-extract.mjs, unzip-extract.mjs, course-embed.mjs, U-KC-B1/B2/B3, KnowledgeInjectionPipelineEngine.  
- Dispatchers/skills: `prism_knowledge:tribal_search`, `tribal_capture`, `tribal_suggest`, `tribal_stats`, `tribal_export_*`.  
- Hooks: `/checkin`, `precompact-pending-guard.mjs`, `golf-slot-write-allowlist.mjs`, `c-to-h-mirror.mjs`, `stable-session-id` helper.  
- External: Ollama nomic‑embed‑text API, Docker for isolated runs, Obsidian integration via markdown links.  
- CI: `node:test`, Vitest (U-KC-B3), tsc, Jest, 3‑of‑3 scrutiny gate agents.

**OPEN THREADS**  
- Re‑run `node --test scripts/course-to-tribal-tips.test.mjs` after compact to confirm all tests green.  
- Commit U‑KC‑B1/B2/B3 and wiring verification test (U-KC-B3 already passes).  
- Verify ledger artifacts (`U-KC-C1/C2`) – ensure no duplicate candidate queue.  
- Dispatcher wiring for closure‑input nodes pending evaluator integration.  
- Full project build (`npm run build`) and peer‑wide test run.  
- Execute `--apply` on KnowledgeInjectionPipelineEngine to commit bindings (operator‑gated).  
- Review/physics‑gate remaining P1–P10 candidates; run `/forge-triple` on selected nodes.  
- U‑CK05 mirror‑gen pending operator review; corpus migration units U‑CK07/08/09 not addressed yet.  
- Drift‑detector repair and close‑out triage completed.
