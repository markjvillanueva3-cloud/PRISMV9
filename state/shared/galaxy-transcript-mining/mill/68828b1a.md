# mill session 68828b1a (2026-06-04, 26.7MB, spine 164KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit 06ac0f7ab8 – memory + rule that Bravo may launch Hermes & Obsidian; soul updated to `hermes‑zulu-builder`.  
- Commit df0d7cdeed – slot‑brief/hand‑off commit.  
- Commit ed5f6d3cde – `FLEET-DOMAIN-GOALS-2026-06-03.md` (17‑galaxy goals, synergy matrix).  
- Commit 6ead9a74e9 – 30 new RGS pipeline rules + 6 agent rules; `rgs-pipeline-rules.mjs`.  
- Commit a679e455c1 – U2 (`switch-claude-account.mjs`) and lib changes for credential swap.  
- Commit 856c417d2b – Octopus upgraded to gpt‑oss:120b primary + qwen2.5‑coder:32b secondary; vision exclusion added.  
- Commit 1bb66a1822 – Hermes workflow planner & harness emitter (6 patterns, 37/37 tests).  
- Commit 443c84d08d – Reactive 429‑fallback executor for rate‑limit handling.  
- Commit 23de0e7881 – Dream‑cycle OOM fix (inverted‑index blocking; <4 s on 11k memos).  
- Commit f2564b81a0 – H: drive atlas mapping all 145 top‑level dirs into vault (`knowledge/h-drive-atlas/INDEX.md`).  
- Commit c988a21ec4 – Safe Hermes config patcher with auto‑rollback (ready after Qwen3‑Coder download).  

**DECISIONS**  
- Hermes runs on Claude Opus 4.8 via Anthropic provider; MCP at :3100 wired.  
- Hermes memories bridged to Obsidian vault (`knowledge/hermes‑brain/`) every 15 min.  
- Octopus uses gpt‑oss:120b + qwen2.5‑coder:32b (plan to switch to Qwen3‑Coder 30B once downloaded).  
- Account‑switch system: U1 capture → U2 swap (`switch-claude-account.mjs`) → U4 populator (missing data source) → U6 coordinator (`account-switch-restart-coordinator.mjs`).  
- RGS pipeline roadmaps generated for all primary domains; rules updated to include new tools.  
- Hermes workflow planner implements 6 patterns from the X article, with failure‑mode → pattern mapping.  
- Reactive fallback replaces telemetry for rate‑limit detection (primary → fallback chain).  
- Dream‑cycle uses inverted‑index blocking + dfCap to avoid O(N²) similarity blowup.  

**OPERATOR DIRECTIVES**  
- “lets keep building until we have full autonomous hermes + obsidian vault.”  
- “Synergize hermes to obsidian and mcp, verify hermes works as intended and tie up loose ends on account switching at 90% 5 hour limits with staggered chat restarts.”  
- “update octopus to use more powerful LLMs.”  
- “hold on the pull until i get home… pivot to something else but dont forget about the pull for ollama.”  
- “look into the full slash command list, assess pipelines, address underutilization; route high‑ROI commands cheaply via Ollama.”  

**FINDINGS / BUGS**  
- Hermes boot‑loop caused by credit‑exhausted OpenRouter; fixed by switching provider.  
- `cron_mode: deny` blocks scheduled autonomy.  
- Obsidian local REST API plugin installed but not MCP; need `mcp‑obsidian` bridge.  
- 5 h quota saturated; U4 populator cannot run (no `rate_limits.five_hour`).  
- Shared‑tree git index lock contention caused commit absorption.  
- Hermes kanban board empty, no goal seeded.  
- Octopus defaults still reference retired Ollama models (`deepseek-r1:14b`, `qwen2.5-coder:14b`).  
- Pre‑existing env‑isolation bug leaked `GEMINI_API_KEY` → test failures.  
- PRISM context injector had stale assertions (old banner format).  

**DOMAIN SPECIFICS**  
- **Hermes agent** – Electron desktop app + Python backend; config.yaml, MCP integration, kanban dispatcher, curator, memory.  
- **Zulu orchestrator** – runtime liveness task for Hermes.  
- **RGS pipeline roadmaps** – workflow that reads forge‑audit, generates domain roadmaps, updates `rgs-pipeline-rules.mjs`.  
- **Account‑switch system** – U1 capture (`capture-claude-credentials`), U2 swap (`switch-claude-account.mjs`), U4 populator (missing), U6 coordinator (`account-switch-restart-coordinator.mjs`) + `fleet-wake-sequencer.mjs`.  
- **Slot‑briefs** – `state/shared/slot-briefs/<slot>.md`; injected via `slotBriefEngine.writeBrief`.  
- **CronCreate** – used for recurring tasks (e.g., Hermes‑Obsidian bridge).  
- Engines: `MultiModelConsensusEngine`, `PRISMContextInjectorEngine`, `DreamCycleSynth`.  
- Dispatchers/routers: `multi-provider-router.mjs`, `ollama-pipeline-injector.mjs`, `ask-ollama.mjs`.  

**TOOLS USED**  
- `/startup-bravo`, `chat-slots.mjs`, `CronCreate`, `slot-query.mjs`, `fleet-wake-sequencer.mjs`, `token-awareness-sidecar.mjs`, `account-switch-restart-coordinator.mjs`, `hermes-obsidian-memory-bridge.mjs`, `rgs-pipeline-rules.mjs`, `scripts/lib/claude-account-lib.mjs`.  
- PRISM tooling: Mill Wizard, HSM/trochoidal/adaptive, ap/ae engagement, face‑mill, pocketing.  
- Testing: Vitest, Node `test`.  
- Type checking: `tsc`.  
- Git hooks: `git-lock-sweep`.  
- Playwright for X article extraction.  
- Ollama CLI & HTTP API (`http://127.0.0.1:11434/v1`).  

**OPEN THREADS**  
- Enable Hermes `cron_mode` to allow scheduled jobs.  
- Seed Hermes kanban with an autonomous goal (once quota clears).  
- Wire `mcp‑obsidian` stdio bridge into Hermes.  
- Build U4 5h‑populator once `rate_limits.five_hour` data source is available.  
- Finalize octopus model defaults to use powerful LLMs (`gpt‑oss:120b`, `qwen2.5-coder:32b`).  
- Ensure account‑switch auto‑swap path fully integrated and tested; trigger on 90 % of 5‑hour quota.  
- Qwen3‑Coder 30B model still downloading (held until home internet).  
- Wire Hermes config to local backend after download (`wire-hermes-local-backend.mjs`).  
- Rotate exposed `GEMINI_API_KEY`.  
- Register nightly dream‑cycle task (currently not scheduled).  
- Verify Obsidian vault bridge fully feeds new memories into vault.
