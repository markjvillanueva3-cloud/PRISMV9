# mill session 671682f9 (2026-05-13, 31.5MB, spine 139KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- **H2 U‑HOOK‑REGISTRY** – `76eef5f95` + close‑state `e6c9f30a2`: HookRegistryReaderEngine.ts, tests, actions `prism_dev:hook_registry`, `hook_map_compact`.  
- **H4 U‑HOOK‑ENVELOPE** – `d2c9aa95a` + `da4bd9a23`: `_envelope.mjs`, HookLatencyEngine.ts, action `prism_dev:hook_latency`; nightly digest script.  
- **H9 U‑HOOK‑COMPRESS** – `71f71cb07` + `a629212a4`: refactored hooks via `duplication-guard.mjs`; tests; envelope updates.  
- **H3 U‑HOOK‑TIERS** – `0889abfad` + `334a1e886`: classify-hook-tiers.mjs, tier frontmatter.  
- **AsyncHookDispatcherEngine (H7)** – commit `3c62bd7ae`: fire‑and‑forget Tier‑4 hooks with detached child.  
- **CoordinationStoreEngine (H8)** – commit `590f96d4c`: SQLite WAL coordination store replacing JSON claims.

**DECISIONS**  
- Adopt engine‑shim pattern: hook files delegate to engines; domain logic isolated.  
- 5‑tier classification (`T0–T4`) via `classify-hook-tiers.mjs` to gate priority and reduce critical‑path load.  
- AsyncHookDispatcherEngine (H7) decouples Tier‑4 hooks from Stop path.  
- Replace JSON claim store with SQLite WAL CoordinationStoreEngine (H8).  
- Fast‑lane matcher split in `settings.json` cuts Read fires 26→6, ~77 % latency reduction.

**OPERATOR DIRECTIVES**  
- “continue where we left off with hook work. run /loops until this /goal is complete”  
- “do one then run precompact”  
- “pick‑unit + /loop + /goal = complete”

**FINDINGS/BUGS**  
- `inventory-check-guard` no‑op due to phantom `readStdinSafe`; fixed.  
- H6 fast‑lane missing ACTION enum entry; added to `ACTIONS`.  
- Precompact hook auto‑writer banned; must use live chat (`precompact-pending-guard.mjs`).  
- H9 compression net –67 LOC; smoke tests pass.  
- H3 classifier bypassed edit‑tool chain; 507 file changes did not trigger regen hooks.

**DOMAIN SPECIFICS**  
- **Engines**: HookRegistryReaderEngine, HookLatencyEngine, HookFastLaneEngine, HookTierValidator, AsyncHookDispatcherEngine, CoordinationStoreEngine, dedup-auto-invoke, ai-feature-recommend, inventory-check-guard.  
- **Actions**: T0 hard exit/block, T1 advisory, T2 injector, T3 observer, T4 async; `prism_dev:hook_registry`, `hook_latency`, `hook_fast_lane`, `hook_map_compact`.  
- **Dispatchers**: devDispatcher (`prism_dev`), sessionDispatcher (`hook_map_compact`), async_dispatch.  
- **Metrics**: hook latency P50/P95/P99 per hook; read‑path reduction 76.9 %; inventory-check-guard emitted 2977 engines / 95 dispatchers / 5928 actions first run.  
- **Key paths**: `mcp-server/src/engines/*`, `.claude/hooks/*`, `scripts/digest-hook-latency.mjs`, `state/shared/.hook-latency-digest-snapshot.json`.

**TOOLS USED**  
- PRISM helpers: per-agent-handoff.mjs, stable-session-id.mjs, chat-slots.mjs, milestone-tracker.mjs, precompact-pending-guard.mjs.  
- Scripts: classify-hook-tiers.mjs, migrate-claims-to-sqlite.mjs, apply-hook-fast-lane.mjs, digest-hook-latency.mjs, h6-survey-matchers.mjs.  
- Hook helpers: duplication-guard.mjs, dedup-auto-invoke.mjs, ai-feature-recommend.mjs, inventory-check-guard.mjs.  
- Build tools: esbuild (engine bundles), vitest (tests).

**OPEN THREADS**  
1. Apply fast‑lane matcher changes to live `settings.json` (`node scripts/apply-hook-fast-lane.mjs`).  
2. Migrate claims to SQLite (`node scripts/migrate-claims-to-sqlite.mjs`); update config to `coord_sqlite`; replace `work-claim.mjs` hook with `prism_context:coord_sqlite`.  
3. Wrap Tier‑4 git-sync-stop hook via `async-hook-enqueue.mjs --hook … --tier T4 --event Stop`; smoke‑test wrapper first.
