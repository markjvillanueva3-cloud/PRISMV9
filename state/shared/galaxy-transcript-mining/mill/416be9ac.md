# mill session 416be9ac (2026-05-17, 22.1MB, spine 93KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `scripts/node-staleness-rank.mjs` – staleness‑per‑truth‑source tool (delegates wikiCoverage & utilizationClassificationHealth to MasterIndexEngine).  
- `scripts/scan-memory-obsolete-refs.mjs` – META scanner for stale memory references.  
- `scripts/stop-memory-size-watchdog.mjs` – Stop hook throttles when MEMORY.md ≥ 90 % of 24 576‑byte ceiling; wired into global & project settings.  
- `H:/prism/.claude/helpers/meta-task-suppressor.mjs` – pure function classifying prompts as meta vs domain (threshold ≥ 2 token hits, Karpathy R5 latency guard).  
- 326 `knowledge/tribal/auto‑ingested‑tips-*` files quarantined to `knowledge/tribal/auto‑ingested‑quarantine/`.  
- `CLAUDE.md` – added cross‑ref comment for duplicated candidate section; removed dead knob `PRISM_SLOT_TASK_CLAIM_DISABLE`; fixed R3 & R7 in `/checkin-mike`.  
- `MEMORY.md` compressed from 24 688 B to 23 184 B (94.3 % of ceiling).  
- `file-relocation‑allowlist.json` – allowlist entry for auto‑ingested tip moves.  
- Plan & follow‑up artefacts:  
  - `state/shared/specs/OBSOLESCENCE-CLEANUP-MS0-PLAN.{md,html}`  
  - `state/shared/specs/OBSOLESCENCE-CLEANUP-MS0-FOLLOWUPS.md`  
  - `state/shared/specs/STALE-NODES-AUDIT-2026‑05‑16.{md,html}`  
  - `state/shared/specs/INJECTOR-UTILIZATION-2026‑05‑17.md`  
  - `state/shared/specs/CLAUDE-MD-DUPLICATION-CANDIDATES-2026‑05‑17.md`  
  - `state/shared/specs/ENVELOPE-DRIFT-PATCHES-2026‑05‑17.md`

**DECISIONS**  
- Delete `scripts/audit-auto-injectors.mjs`; replace with `prism_hook` dispatcher actions (coverage, gaps, performance, hook_efficiency_roi).  
- Refactor `node-staleness-rank.mjs` to delegate wikiCoverage & utilizationClassificationHealth to MasterIndexEngine.  
- Keep `meta-task-suppressor.mjs` pure; avoid calling `contextPriorityEngine.classifyTask()` in hot‑path hooks.  
- Wire `stop-memory-size-watchdog.mjs` into global and project settings for 12 h throttle.  
- Quarantine auto‑ingested tip files; add allowlist entry.  
- Compress `MEMORY.md` to stay below truncation threshold; schedule weekly memory‑audit loop.  
- Add cross‑ref comment in `CLAUDE.md`, remove dead knob, fix R3/R7 lint rules.  
- Adopt `prism_hook` dispatcher actions for hook telemetry and performance monitoring (no new meta tools).  
- Adopt two‑layer reaper architecture: durable scheduled task **PRISM Fleet Reaper** runs every 5 min with S4U principal, elevated installer, restarts on failure ×3, AtStartup trigger; per‑session Monitor armed by `/checkin-alpha` or bare `/fleet-reaper`; dies when chat closes and auto‑rearms in next alpha session.  
- Doctrine: run `/fleet-reaper` only in designated **alpha** slot; non‑alpha chats are silent no‑ops via `alpha-slot-reaper-guardian.mjs`.  
- Idempotence guarantees: re‑running skill safe because Monitor gated by `TaskList` and scheduled task by `schtasks /Query`.

**OPERATOR DIRECTIVES**  
- Continue `/loop` autonomous mode on 13th slot “mike”.  
- Do not pivot; work sequentially through queued tasks.  
- Keep `/fleet-reaper` always‑on; disable only via `PRISM_FLEET_REAPER_DISABLE=1`.  
- Run `/fleet-reaper` to confirm baseline hygiene after each session.  
- Run `/fleet-reaper` only in one chat (alpha slot).  
- If another chat previously armed a Monitor, run `--uninstall` there first, then re‑arm in alpha.  
- Use bare `/fleet-reaper` to re‑arm after `/compact`, a chat crash, or to verify always‑on status.  
- To upgrade logon mode from interactive‑only to S4U+AtStartup, execute:  
  ```
  powershell -NoProfile -ExecutionPolicy Bypass -File H:/prism/.claude/helpers/install-fleet-reaper-task.ps1 -RunNow
  ```  
- If you want the Monitor armed from this chat anyway, say so (proper path is `/checkin-golf /fleet-reaper`).

**FINDINGS/BUGS**  
- `MEMORY.md` truncation at 100.5 % (critical).  
- `ENGINE_DIGEST` and `DIRECTORY_DIGEST` stale > 72 h, 103 h respectively.  
- Envelope drift: 11 milestones claim_completed_but_units_pending.  
- Orphan count 12 129; ghost count 823 after F4 fix.  
- 326 auto‑ingested tip files causing noise in tribal‑by‑domain injectors.  
- Skill‑lint R4 “placeholder” false positive; R3/R7 missing in `/checkin-mike`.  
- D1: 2/5 viz/obsidian injectors fire but skip telemetry writes.  
- Current logon mode interactive‑only; needs upgrade to S4U+AtStartup.  
- Docker‑health probe unavailable on host‑installed daemon.  
- Two `bash.exe` candidates too young (< 45 s) pending re‑evaluation.  
- Memory commit 86.4% (below 90 % coordinator threshold); no action required yet.

**DOMAIN SPECIFICS**  
- Engines: MasterIndexEngine, contextPriorityEngine, awareness‑snapshot engine, memory‑size‑watchdog engine.  
- Dispatchers: `prism_hook` (coverage, gaps, performance, hook_efficiency_roi), `mcp_server` for system telemetry, `skill-lint` dispatcher for static analysis.  
- Actions/Tools: `node-staleness-rank.mjs`, `scan-memory-obsolete-refs.mjs`, `stop-memory-size-watchdog.mjs`, `/checkin-mike`, `/loop`, `/fleet-reaper`, `alpha-slot-reaper-guardian.mjs`.  
- Hooks: `meta-task-suppressor.mjs` (UserPromptSubmit), `discipline-expert-inject.mjs`, `comprehensive-build-enforce.mjs`, `tribal-by-domain-inject.mjs`, `stop-memory-size-watchdog.mjs`.  
- Reaper architecture: durable scheduled task **PRISM Fleet Reaper** (S4U principal, AtStartup trigger); per‑session Monitor via `/checkin-alpha` or bare `/fleet-reaper`; doctrine enforced by `alpha-slot-reaper-guardian.mjs`.  
- Metrics paths: memory phys 65.1%, commit 86.4%; GPU free 15165 MB; ollama reachable; docker health probe status.

**TOOLS USED**  
- Node scripts (`node <script>.mjs`) for audits, scans, telemetry.  
- `/checkin-mike` slot‑binding wrapper (calls full `/checkin`).  
- `/loop` autonomous loop engine.  
- `/fleet-reaper` fleet hygiene monitor.  
- `system-viz-query.mjs` for graph metrics.  
- `skill-lint` static linter.  
- PowerShell installer (`install-fleet-reaper-task.ps1`) and `schtasks /Query`.  
- `alpha-slot-reaper-guardian.mjs`.

**OPEN THREADS**  
1. AUTO‑INVOCATION-MS1 – schedule injector telemetry + viz‑backed replacements.  
2. MEMORY‑AUDIT‑WEEKLY – `/loop --interval 7d scan-memory-obsolete-refs.mjs`.  
3. CLAUDE‑MD‑DRIFT‑DAILY – `/loop --interval 1d claude-md-drift.mjs`.  
4. DIGEST‑REGEN‑DAILY – `/loop --interval 1d regen-digests.mjs`.  
5. CLAUDE.md collapse – after 7‑day grace window (2026‑05‑24+).  
6. Paragraph‑level CLAUDE.md fingerprint audit – pending review.  
7. `/fleet-reaper` – confirm always‑on status, schedule periodic health checks.  
8. Upgrade interactive‑only logon mode to S4U+AtStartup.  
9. Implement docker‑health probe for host‑installed daemon.  
10. Ensure non‑alpha chats do not spawn redundant Monitor processes.  
11. Verify idempotence of re‑arming after `/compact` and chat crashes.
