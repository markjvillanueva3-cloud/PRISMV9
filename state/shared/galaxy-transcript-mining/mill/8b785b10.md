# mill session 8b785b10 (2026-06-04, 29.9MB, spine 186KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- e2cdbe2e86 – Cost‑router now reaches 32 b “best” tier on Blackwell (prev cap 14 b).  
- 606424dc12 – U‑FGC‑4/5: orphan `.git/index.lock` self‑heal + JIT PreToolUse hook; cleared 217 s freeze.  
- ae2fbfdff8 – Host‑aware synthesis‑model resolver defaults to installed 32 b model.  
- 9bd4b22abd – Repo‑wide `.gitattributes` (eol=lf) committed, fixes CRLF/LF flip bug.  
- 24478d31aa – Removed untracked 675 MB `system‑graph.json` from git history.  
- 049e981158 – Cost‑router “best” tier now prefers 70 B‑class MoE tags over old 32 b floor.  
- c4eb71913d – Ask‑ollama wired to host‑aware resolver; all five synthesis scripts use 32 b model.  
- 74077e38cb – Retired qwen2.5‑coder 3/7/14b & deepseek‑r1:14b; routing re‑pointed to qwen2.5‑coder:32b floor; installed‑gated gpt‑oss:120b/20b.  
- 318d0c062b – Hermes 7‑unit build plan + dead‑offloader bug fix in `ollama-route-config.json`.  
- f0e72dd6e0 – LocalLLMTaskRouterEngine (U1) composer, 18 tests, 2‑reviewer hardened.  
- 28c56cd437 – Wired gemma4:31b into cost‑router best tier; confirmed hybrid/octopus support.  
- 0615b476d5 – Fixed stale tags in `OllamaHookBridgeEngine` (defaultModel & overrides).  
- fc9038ca2a – Removed deleted model entries from four .ts registries (`ModelRoutingEngine`, `OLLAMATaskOffloaderEngine`, `ConsensusAIBridgeEngine`, `LocalLearningEngine`).  
- 8e2b2500c6 – Widened anti‑revert guard regex, fixed default in `OLLAMAContextFloorEngine`, added `isViolation()` test; all 3‑of‑3 scrutiny PASS.  
- `multi-provider-router.mjs` + standalone Hermes local backend + dynamic‑workflow emitter (Bravo).

**DECISIONS**  
- Fleet‑reaper ownership moved from alpha → golf for unified fleet hygiene.  
- Adopt RTX PRO 6000 Blackwell: pull large MoE models locally, keep data on‑prem; keep vision VLM ensemble on Blackwell.  
- Remove all small 3/7/14 b LLMs only after pulling new 70–120 B model and re‑pointing fallbacks; enforce anti‑revert guard across src/engines & data/state.  
- Adopt local Ollama + gpt‑oss:120b as best tier; gemma4:31b for reasoning.  
- Compose Hermes router via existing `routeModelForTask`, `resolveSynthesisModel`, and `multi-provider-router`; no new policy layer.  
- Use two‑reviewer scrutiny gate for every commit; extend guard to cover parentheses & array literals.  
- Schedule recurring `/loop` with 5 min cadence for ongoing monitoring.  
- Unified Hermes auto‑invoke router lives in Bravo; Alpha lane handles Ollama execution; hold alpha’s ollama‑autoexec build until Bravo boundary confirmed.

**OPERATOR DIRECTIVES**  
- Run precompact, plan pulling powerful LLM models, wire them into system, remove lower LLM models to prevent accidental revert.  
- Cloud Kimi2.6 rejected – not free & would expose IP; no cloud version of kimi2.6 or larger models unless free and data secure.  
- Generate skills, scripts, hooks slash‑command pipelines keyed for Hermes, centered on Ollama/local LLMs, with reviewer agent for enhancement/gap‑fill.  
- Ensure Hermes knows tool, skill, memory, tribal knowledge, model, and other Prism features per task.  
- Route tool calls through Ollama where possible to save tokens without quality loss; focus on subtasks (summarize, explain) rather than deterministic commands.  
- Execute P0: enable auto‑invoke for Ollama, prove offloader works.

**FINDINGS/BUGS**  
- Orphan `.git/index.lock` caused fleet commit stalls; solved by U‑FGC‑5 sweep.  
- Write tools emit CRLF on LF repo → resolved with `.gitattributes`.  
- TSC “unused import” diagnostics in .mjs files are false positives (checkJs).  
- Cloud Kimi2.6 is paid and would leak data – disallowed.  
- Stale references to deleted models in multiple .ts engines & config files (`ollama-route-config.json`, `multi-provider-router`, `AISystemRouterEngine`).  
- Dead‑offloader bug: `ollama-route-pretooluse` returned *suggest* instead of *reroute*, causing 0 offloads.  
- Anti‑revert guard missed parentheses/array literal patterns; fixed by regex expansion.  
- `OLLAMAContextFloorEngine` default pointed to deleted qwen2.5-coder:7b; corrected.  
- Auto‑invoke uneven: skills strong, scripts weak (~0.8% take‑rate), pipelines medium (suggest only).  
- Layer‑2 hooks can nudge but cannot execute skills/scripts directly.  
- Two retirement findings: `ollama-deepseek` deleted model, bare deepseek-r1 may breach no‑cloud policy.  
- Offloader bug: cold start `/api/tags` timeout causes `cascade_unreachable → pass`; warm run reroutes correctly.  
- Stale telemetry (5,800× fired, 0 offloaded) misrepresents current state.

**DOMAIN SPECIFICS**  
- Engines: `aiSystemRouterEngine`, `cost-router`, `multi-provider-router`, `OllamaHookBridgeEngine`, `AISystemRouterEngine`, `MultiModelConsensusEngine` (octopus), `OLLAMAContextFloorEngine`.  
- Models: qwen2.5‑coder:32b floor, gpt‑oss:120b/20b, gemma4:31b, vision VLMs (`llama3.2‑vision`, `qwen2.5vl`).  
- Features: anti‑revert guard test suite, 3‑of‑3 scrutiny gate, slot‑souls, skill-auto-trigger, master-index.  
- Helpers: `chat-slots.mjs`, `git-commit-mutex.mjs`, `ollama-cost-router.mjs`, `host-aware-synthesis-model.mjs`.  
- Workflow orchestration: background agents, self‑committing via mutex, precompact handoff (`per-agent-handoff.mjs` + `precompact-pending-guard.mjs`).  
- Assets: ~440 skills (`Skill` tool), node scripts (`node scripts/<X>.mjs`), pipelines (`/forge`, `/quote-to-ship`, `/rgs`).  
- Auto‑trigger mechanisms: `skill-auto-trigger.mjs` with `_skill-triggers.jsonl`; route‑nudges for scripts; pipeline auto‑suggest via `PostToolUse`.  
- Offloader logic in `ollama-route-pretooluse.mjs`: guard `isGistSafe`, mode `"auto"`/`"suggest"`, reroute on gist‑safe logs.  
- Hermes router brain (Bravo) + local‑LLM-task‑router.mjs classification lib.

**TOOLS USED**  
- `/checkin-alpha`, `/startup-alpha`.  
- chat-slots.mjs, git-commit-mutex.mjs, ollama-cost-router.mjs, host-aware-synthesis-model.mjs.  
- Workflow background agents (re‑pointing, pull, delete).  
- Precompact hook and per‑agent handoff.  
- Workflow background multi‑agent orchestrator.  
- CronCreate for recurring `/loop`.  
- Ollama API (curl + local server).  
- TSC & esbuild for TypeScript builds.  
- Node test framework for guard tests.  
- Git commit mutex with lock handling.  
- Skill‑auto-trigger, master-index, slot-souls.  
- PRISM: Skill tool, scripts, pipelines, Hermes auto‑invoke router.  
- Bravo dispatchers & hooks (`multi-provider-router.mjs`).  
- Alpha execution hands (Ollama slice).  
- local-llm-task-router.mjs, ollama-route-pretooluse.mjs.

**OPEN THREADS**  
- Final LLM upgrade – Pull gpt‑oss:120b (65 GB), re‑point all small‑model defaults to 32 b, add anti‑revert guard.  
- Obsidian‑memory‑sync – Rank 1 content‑skip implementation.  
- System‑viz‑bridge – Await‑no‑effect bug fix (line 301).  
- Unused‑import cleanup – remove spurious imports in synthesis scripts/tests.  
- Cost‑router test rewrite – ensure new tier definitions.  
- U1b: `.ts` dispatcher wrapper for LocalLLMTaskRouterEngine (heavy TSC build pending).  
- AISystemRouterEngine enum refactor to remove legacy deepseek/r2 references.  
- Extend anti‑revert guard to scan `data/state` & `src/engines`.  
- Finalize hybrid/octopus secondary voice selection logic.  
- Monitor 5‑min cron loop for regressions.  
- Build alpha’s ollama‑autoexec after Bravo boundary confirmation.  
- Verify offloader on cold start (timeout adjustment or warm cache).  
- Resolve retirement findings for deepseek models.  
- Ensure no duplication between Bravo’s unified router and Alpha’s execution logic.
