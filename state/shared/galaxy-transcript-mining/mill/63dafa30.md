# mill session 63dafa30 (2026-06-25, 6.7MB, spine 61KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `f5a64533de`: system‑viz‑query OOM on `--help` fixed; added 16 GB heap self‑respawn.  
- Commit `c93d0179c2`: node‑dispatch empty‑output thrash fixed via CLI‑only respawn.  
- Commit `baaf3c7859`: introduced `respawnWithHeap` dedup and NODE_OPTIONS‑aware planner in `scripts/lib/viz-query-heap-reexec.mjs`.  
- Commit `67baa0e72b`: right‑sized heap guard from 16384 → 8192 MB, validated on heavy graph commands.  
- Commit (script‑aware portable‑node split): added per‑script 2048 MB heap while keeping hook swarm at 384 MB; live‑validated.

**DECISIONS**  
- Adopted heap‑respawn pattern to avoid OOMs in graph‑loading CLIs, ensuring deterministic auto‑invoke where safe.  
- Consolidated respawn logic into shared module (`scripts/lib/viz-query-heap-reexec.mjs`) for reuse and testability.  
- Made respawn logic NODE_OPTIONS‑aware but only when heap ≥ desired default, to avoid overriding intentional 384 MB hook cap.  
- Reduced reserved heap from 16 GB to 8 GB based on measured floor of 4 GB; improves Windows commit reservation without risking OOM.  
- Declined global `NODE_OPTIONS` raise due to Windows commit‑reservation risk (MCP‑FLEET‑CAPACITY‑MS0).  
- Declined Ollama auto‑invoke change because advisory ops are lossy and already muted by advisory‑decay.

**OPERATOR DIRECTIVES**  
- “do it all” – explicit authorization to execute all queued threads, including global `NODE_OPTIONS` heap raise.  
- Goal: fully tested, stress‑tested, validated no hard caps on utilization; auto‑invoke deterministic ops only.

**FINDINGS/BUGS**  
- system‑viz‑query `--help` and typo OOM due to eager `loadGraph()` without guard.  
- Missing heap guard in several graph‑loading CLIs (system‑viz‑query, node‑dispatch).  
- Global 384 MB `NODE_OPTIONS` cap set by portable‑node wrapper; Windows commit reservation causes fleet‑wide limit.  
- Ollama offload suggestions had 0% conversion; deterministic bridge already 100% auto‑invoke.  
- Regression: backward `NODE_OPTIONS` check in `planHeapRespawn` suppressed respawn on 384 MB cap.

**DOMAIN SPECIFICS**  
- Engines/actions: system‑viz‑query, node‑dispatch, hub‑blast‑radius‑rank, build‑system‑viz‑livediff.  
- Dispatchers/skills: `/checkin-sierra` wrapper, chat‑slots helper, `planHeapRespawn` module.  
- Metrics: `silentSuggestions`, `executedOffloads`, `tokensSaved`, GC thrash logs.  
- Paths: `scripts/lib/viz-query-heap-reexec.mjs`, portable‑node wrapper (`C:/Users/.../.claude/bin/portable-node`).

**TOOLS USED**  
- PRISM tools: `/checkin-sierra`, `/checkin` pipeline, chat‑slots helper, `planHeapRespawn` module, `respawnWithHeap`.  
- Scripts/hooks: `scripts/lib/viz-query-heap-reexec.mjs`, portable‑node wrapper, c-to-h-mirror hook.

**OPEN THREADS**  
- None remaining; all queued threads either shipped or declined with evidence.
