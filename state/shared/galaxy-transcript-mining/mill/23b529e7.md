# mill session 23b529e7 (2026-06-26, 5.6MB, spine 70KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hermes update finished (git tree current, venv reinstall succeeded).  
- Desktop app now loads successfully.  
- CLI works after IPv4‑prefer policy applied.

**DECISIONS**  
- Follow Iron Law: root‑cause first before any patch.  
- Relaunch desktop to skip failed auto‑update venv recreation.  
- Apply `netsh interface ipv6 set prefixpolicy ::ffff:0:0/96 60 1` to force IPv4 and unblock auth init.  
- Copy Nous/Grok credentials from bravo profile to default (zulu) without sharing tokens.  
- Keep fleet on Ollama; defer grok rollout until operator approves.

**OPERATOR DIRECTIVES**  
- Fix Hermes app install & CLI launch in PowerShell.  
- Connect Hermes CLI to paid Nous account and working Grok key.

**FINDINGS/BUGS**  
- Desktop failed because auto‑update stuck recreating venv while proxy holds `.pyd` locks.  
- CLI hung on auth provider init due to IPv6 handshake to Cloudflare endpoint never answering; IPv4 works fine.  
- Nous portal not logged in (credentials only exist in bravo profile); xAI OAuth token revoked across all profiles.

**DOMAIN SPECIFICS**  
- Hermes app: Electron desktop, `desktop.log`, `bootstrap-installer.log`.  
- Hermes CLI: `hermes.exe` in `hermes-agent\venv`, per‑profile `auth.json`.  
- Auto‑update process: `git pull`, venv recreation, log entries.  
- Network: IPv6 prefixpolicy, Cloudflare endpoints (`2606:4700::…`).  
- Profiles: 21 fleet profiles, each with separate auth store.

**TOOLS USED**  
- Systematic Debugging skill.  
- Hermes CLI commands: `update`, `status`, `portal`, `auth add xai-oauth`.  
- `netsh interface ipv6 set prefixpolicy ::ffff:0:0/96 60 1`.  
- Built script `propagate-hermes-fleet-auth.mjs` for credential propagation.

**OPEN THREADS**  
- Decision on fleet‑wide propagation of Nous/Grok credentials and model config.  
- Verify Nous billing/credits after subscription purchase.  
- Monitor concurrent usage limits if grok is enabled for all agents.
