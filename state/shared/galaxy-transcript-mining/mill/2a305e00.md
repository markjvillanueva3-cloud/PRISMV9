# mill session 2a305e00 (2026-06-25, 7.4MB, spine 48KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- **U‑CAD‑LEARN‑CALIBRATE** – commit `1a910d6015`: added self‑correcting calibration (raw‑anchored logit‑shift) to the CAD trial‑error learning loop; wired to all live consumers. 3‑of‑3 scrutiny passed.  
- **U‑CAD‑TEXT‑LEARN‑PROMPT** – commit `50bd919799`: closed the reverse arrow by feeding learned failure modes back into the text→CAD generation prompt. 2‑arm scrutiny passed.  

**DECISIONS**  
- *Scope*: focus on in‑lane units that add new knowledge (no duplication).  
- *Architecture*: extend existing learning loop with calibration, add reverse‑arrow injection, and create a resumable transcript drain to ingest video captions.  
- *Why*: these changes directly improve the AI’s ability to generate accurate CAD drawings and reduce failure rates while satisfying the operator’s mandate for tribal knowledge injection.

**OPERATOR DIRECTIVES** (verbatim)  
```
/goal improve the learning and ai systems for cad drawing, print generation, print to cad file in fusion, hypercad and mastercam. utilize hermes cli capabilities, hermes agents, ollama offloading, octopus, harnesses, engineered loops, crons, jm files, obsidian vault and full system capabilities. zulu is adding more tribal knowledge so ensure your adding tribal knowledge injections. run the hermes /learn pipeline on all cad and engineering related sources in H:\PRISM\resources and all other sources we have in the h drive from MIT and other college courses. include videos and other reputable sources from online, dont duplicat knowledge though. only add new knowledge
```

**FINDINGS/BUGS**  
- PDF‑only drain omitted lecture‑video transcripts (`.vtt`/`.srt`).  
- No existing SRT parser; VTT parsing already available via `youtube-free-extract.mjs`.  
- Manual batch launch was reaped by the fleet‑reaper, leaving a stale lock – cleared manually (R14).  
- RL‑CAM feedback engine had an arity mismatch; routed to owner instead of auto‑fix.  

**DOMAIN SPECIFICS**  
- `CADTrialErrorLearningEngine` – Laplace‑smoothed failure rates, shrinkage, closed‑loop attribution (`recordRecommendation → attributeOutcome → linkOutcome → getLoopEfficacy`).  
- Calibration logic – raw risk score, logit‑shift with shrinkage, `calibrationShift`, `calibrationApplied`.  
- Text→CAD generation loop (`cad-text-to-cadquery.mjs`) and reverse arrow injection.  
- Transcript ingestion pipeline: `parseVtt()`, new `parseSrt()`, chunking → Ollama tip generation → tribal embedding.  

**TOOLS USED**  
- PRISM slot‑binding helpers (`chat-slots.mjs`).  
- `/checkin-india` wrapper for full check‑in pipeline.  
- Drain scripts: `scripts/drain-resources-tribal.mjs`, `youtube-free-extract.mjs`.  
- Node test harnesses (`node:test`, `vitest`) and TypeScript compiler (`tsc --noEmit`).  

**OPEN THREADS**  
1. **Print→CAD generation tribal feed** – cross‑lane change to Xray/Delta backend; requires handoff coordination.  
2. **Expand /learn pipeline to other online sources (videos, reputable sites)** – new batch ingestion beyond PDFs/transcripts.  
3. **RL‑CAM regression** – already routed to owner; no in‑lane action pending.  
4. **Ensure no duplication** – ongoing dedup checks for future ingest units.
