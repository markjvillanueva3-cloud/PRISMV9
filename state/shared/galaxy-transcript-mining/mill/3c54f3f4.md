# mill session 3c54f3f4 (2026-06-22, 23.1MB, spine 145KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `d197fa6cd5` – Mill‑program GT extraction added; closed‑loop recall now includes mill parts (29/29 tests, live OCR pass).  
- `7e8cb4ef57` – Doctrine sync: corrected stale “format:\"json\" NOT YET BUILT” note.  
- `4c0828c118` – Thread‑callout normalizer (`normalizeThreadCallout`) added to OCR extract lib; P1/P2 bugs fixed, 88/88 tests pass.  
- `3a2316206c` – Added `--axis` flag to validation runner for targeted mill recall measurement (GPU run).  
- `20661dda93` – TS clone of thread normalizer wired into production `BlueprintVisionOCREngine`.  
- `0440ed4f04` – Chamfer normalizer added; ASCII‑guarded, 92/92 tests pass.  
- `f0a08b7c02` – Pure core for region tiling (grid + merge) implemented and unit‑tested.  
- `d012c5e0a5` – Integration of tiling into ensemble OCR pipeline; live E2E validated, shape‑mismatch bug fixed.  
- `761c045224` – Fixed baseline OCR accounting bug (`models_ok=0`) and default model choice; 11/11 tests pass.  
- `d94fc110ae` – Wired tiling path into closed‑loop validator; head‑to‑head recall comparison now possible.  
- `865c312428` – GD&T symbol normalizer added to OCR extract lib; datum‑deficiency flag works for non‑canonical symbols.  
- `377e99e57e` – TS clone of GD&T normalizer wired into production engine (`convertGDT`).  
- `c1a0498791` – Added FCF validation call in second production OCR engine’s `extractGDT`.  
- `463b1d8fa1` – U‑XRAY‑PERPRINT‑UNITS: per‑print unit propagation with corroboration gate, 121/121 tests, live‑validated on a 3‑page lathe scan (recall 0.43).  
- `c6b6e7b14a` – BACKLOG‑TRUTH update: P1.6 done, P2.9 dormant; GPU usable, drawing worklist mostly single‑page.  
- `5d67236d37` – P1.5 REGION‑ROUTING design grounded on page‑classifier foundation (pure lib + glue).  
- `68150b27a0` – Symbol normalizers loop unit shipped.  
- `141ce06eb8` – Multi‑page training‑data unit fix shipped.  
- `661f3db76d` – Cron update with `--force-units in` shipped.

**DECISIONS**  
- Shift focus to high‑ROI mill‑GT and normalizer build‑it‑everywhere pattern; ship each new normalizer as script (.mjs) + TS clone.  
- Prioritize GPU‑bound validation only after core logic fully unit‑tested & live‑validated on real data.  
- Use `forceUnits` channel for per‑print unit propagation; keep `extractDimension` precedence unchanged.  
- Add corroboration gate requiring title‑block identity field to anchor units, preventing hallucinated bare‑unit overrides.  
- Skip P1.6 (recall‑first fusion already implemented); defer P2.9 calibration until sample volume sufficient.  
- Proceed with P1.5 layout‑aware region routing after confirming 0.43 recall baseline; GPU contention resolved by waiting for free slot.

**OPERATOR DIRECTIVES**  
- `/loop [10m] complete all remaining back end development tasks, priority on xray tasks, continue improving ocr and blue print reading capabilities. continue with closed loop training | goal clear: utilize ollama offloading, hermes agents, parallel agents, engineered loops, harnesses and crons`.

**FINDINGS/BUGS**  
- Thread regex collision with mixed‑fraction drills (`1‑15/32 DRILL`); fixed stricter regex.  
- Unbounded bore extraction (`I999999`) poisoned recall denominator; corrected.  
- `M2 STEEL` tool grades incorrectly fabricated into metric threads (critical for JM Die).  
- Spot‑drill misinterpreted as hole feature; resolved.  
- Baseline OCR path failed when `models_ok=0`; accounting bug fixed.  
- Ensemble output shape mismatch (`value_mm` vs `nominal_mm`) caused over‑merge; resolved with shape‑tolerant accessors.  
- CAD‑model GT and program‑selection levers refuted before coding (low payoff).  
- GPU contention caused vision models to load empty during earlier runs; now resolved.  
- OCR Training Loop falsely reported missing – actually Ready.  
- Multi‑page PDF claim (96 %) contradicted by worklist analysis: most drawings single‑page.  
- Tiling recall lift part‑dependent; improves dense mill prints but not sparse lathe parts.  
- Per‑print unit propagation bug fixed; band‑aid `--force-units in` replaced with principled logic.

**DOMAIN SPECIFICS**  
- Blueprint‑Vision galaxy (`xray` slot): OCR extraction, CNC‑program GT triangulation, mill‑GT, thread/chamfer/GD&T normalizers.  
- Engines: `BlueprintVisionOCREngine`, `BlueprintOCREngine.ts`, `vision‑tiling‑extract`, `runEnsembleOverImage`, `validate‑perfect‑parts.mjs`.  
- Actions/dispatchers: page‑classify, region‑classify, training‑loop (`blueprint‑ocr‑training‑loop.mjs`), cron job `run‑ocr‑training‑loop‑overnight.ps1`.  
- Metrics: OCR recall vs CNC GT, FCF validity/issue counts, unit consistency checks.  
- Paths: `/checkin-xray`, slot binding via `slot-bind-enforce.mjs`, handoff to `xray-work`.

**TOOLS USED**  
- PRISM CLI: `/loop`, `/checkin`, `/goal`, `/pick-unit`, `/checkin-xray`.  
- Scripts/dispatchers: `scripts/capture‑claude‑credentials.mjs`, `scripts/arm-account-switch.mjs`, `scripts/blueprint‑ocr‑training‑loop.mjs`, `scripts/vision‑tiling‑extract.mjs`, `scripts/lib/vision‑tiling‑lib.mjs`.  
- Hooks: `slot-bind-enforce.mjs`, `chat-slots.mjs`.  
- Libraries: `runEnsembleOverImage`, `vision-ensemble-fuse.mjs`.  
- Engines: `BlueprintVisionOCREngine.ts`, `BlueprintOCREngine.ts`.  
- Build tools: TSC (large heap), Vitest for unit tests.

**OPEN THREADS**  
- Full‑corpus GPU recall lift measurement (mill + tiling) – pending reaper‑resumable run.  
- P1.5 LAYOUT‑AWARE REGION ROUTING – to be built (region‑classifier lib + glue).  
- TILE‑VS‑BASELINE CORPUS MEAN RECALL comparison – GPU‑heavy, deferred until free slot.  
- PER‑FEATURE‑TYPE CONFIDENCE CALIBRATION (P2.9) – dormant due to sample volume.  
- CONSUMER‑WIRING for GD&T enrichment – currently dormant; may be revisited after region routing.  
- Weld‑symbol normalizer (P2.8) – still to implement and ship.  
- Final wiki lessons entry (compounding doctrine) – drafted, awaiting commit.  
- Optional refinement of region‑tiling heuristics for dense pages.
