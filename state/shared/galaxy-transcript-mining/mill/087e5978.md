# mill session 087e5978 (2026-06-14, 26.4MB, spine 189KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Fixed `nn-eval-refresh.mjs`; writes `latest‑candidate.json`, resolves stale PSN banner.  
- Codemod `scripts/fix-windows-esm-entry-guards.mjs` applied to 18 dead CLI entry points; parse‑validated and live.  
- Published baseline spec `FLEET-TOKEN-SAVINGS-BASELINE-2026-06-13.md`.  
- Added memory artifacts: `reference_hermes_app_launch_fix_2026_06_12.md`, `reference_ai_synergy_crosssubstrate_verified_real_2026_06_12.md`.  
- Implemented self‑compact actuator (`scripts/self-compact.mjs`) with tests; commits 087e73d166, fede01d2b2, f97c2b299f on `cad-fusion-live-ms0`.  
- Updated docs correcting Zulu hwnd resolution claim.

**DECISIONS**  
- Use existing hook system and guard pattern.  
- Pivot to token‑optimization ROI; defer guard fixes until after reviewing blocking hooks.  
- WT tab focus via `focusWtTabBySlot` for reliable window targeting; keep `PRISM_SELF_COMPACT_DISABLE` knob.  
- Resolve stale `slot.pid` only when stable terminal ID present.  
- Reuse routing utilities (`routeTask`, `classifyTaskClass`, `resolveExecutor`) for goal/loop logic.

**OPERATOR DIRECTIVES**  
- Session‑scoped Stop hook: activate all 6 Hermes accounts and switchable in Claude Code CLI.  
- YOLO mode: proceed with highest‑priority inefficiencies without asking.  
- Finalize self‑compact implementation, correct docs.  
- Develop `/goal` and `/loop` slash commands using composed routing logic.

**FINDINGS/BUGS**  
- Windows ESM entry‑guard bug caused 23 dead CLIs; 3 dead hooks.  
- `nn-eval-refresh.mjs` never executed due to guard; now fixed.  
- Hermes launch crash: syntax error in `toolsets.py`; resolved.  
- Ref‑pool empty (0 entries) – blocker for full‑coverage NN/GNN gate.  
- CLAUDE.md size reduced from 103 K to ~98 K chars (~7,200 tokens/turn).  
- `chat-slots.json` stores transient dead PIDs → unreliable hwnd resolution.  
- All fleet slots are Windows Terminal tabs; send‑keys path falls back correctly.  
- Earlier docs incorrectly claimed Zulu used `slot.pid`; actual uses UIA tab focus and title matching.  
- Test 17 had a P1 due to unsafe fall‑through; fixed.

**DOMAIN SPECIFICS**  
- Hermes credential pool: 6 OAuth accounts, `active_provider=anthropic`.  
- Claude Code account switching via `scripts/switch-claude-account.mjs` (captures `.credentials.json`, `.claude.json`).  
- NN/GNN tier‑5 metrics: AUROC 0.4286, macro‑F1 0.1053, Brier 0.2555 (latest retrain).  
- PSN banner reads `latest-candidate.json`; updated by fixed script.  
- WT tab must be named “PRISM <slot>” for self‑compact to actuate.  
- Zulu resolver: `focusWtTabBySlot` → `matchWindowsByTitle` → owning‑window pid.

**TOOLS USED**  
- CI: `rtk git`, `rtk tsc`, `rtk npm run build`.  
- Codemod: `scripts/fix-windows-esm-entry-guards.mjs`.  
- Evaluation: `nn-eval-refresh.mjs`, `measure-fleet-token-savings.mjs`.  
- Graph queries: `system-viz-query.mjs`.  
- LLM offloading: `ollama-file-digest.mjs`, `scripts/ollama-offload.mjs`.  
- Self‑compact: `scripts/self-compact.mjs/.test.mjs`; helpers `send-keys.mjs`, `resolve-hwnd.mjs`, `wt-tab-focus.mjs`.  
- Zulu orchestrator: `zulu-orchestrator-sweep.mjs`.

**OPEN THREADS**  
- Capture remaining 5 Claude accounts via `/login` in Claude Code; run `capture account‑N <label>`.  
- Review 3 dead hooks (`cag-soul-cache-block`, `meta-learning-inject`, `stop-playbook-corpus-drift-advisory`) before enabling.  
- Commit guard‑fix batch (branch per slot discipline).  
- Final trim of CLAUDE.md to remove redundant sections.  
- Grow ref‑pool via active‑label worklist, retrain NN/GNN, re‑evaluate gate.  
- Build advisor composing routing logic into `/goal` and `/loop`.  
- Finalize integration of ~40‑system stack per iteration.
