# mill session ef40a9d1 (2026-05-27, 38.5MB, spine 138KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**

- `60579c5e7c` – PB-MS0/P3-U01 anti‑pattern severity elevation (21/21 tests).  
- `<corpus>` – MILL-PDF-CORPUS-MS0/U-FOXTROT-LIMA-CROSSOVER (24 PDFs, 15 cited tips, bridge into wizard).  
- `<vendor-online>` – MILL-PDF-CORPUS-MS0/U-VENDOR-ONLINE-DEEP-RESEARCH (9 vendors, 52 URLs, 11 cited tips).  
- `<jm-die>` – JM‑DIE‑TRIBAL‑WIKI‑CORPUS (68 PDFs extracted, 62 tribal tips).  
- `<training-index>` – milling-training-index.ts (AI‑reachable index, BM25 search, 34 tests).  
- `<system-viz>` – system‑viz augmentation JSONs (12/78 bridged tips, 77/115 extracted nodes).  
- `<video-extraction>` – U‑VIDEO‑EXTRACTION‑START & U‑VIDEO‑EXTRACT‑DAPRA‑HAAS‑SANDVIK‑PTSECRETS (225 cited milling tips).  
- `067a2b24ff` – U‑DAPRA‑HEM‑VIDEO‑CORPUS (9 Dapra tips).  
- `85fbe34fb8` – U‑HAAS‑SANDVIK‑VIDEO‑CORPUS (12 Haas/Sandvik tips).  
- `9446ea8f8c` – U‑DRILLING‑INDEXABLE‑ROLLIN (4 PTS drilling + Sandvik roll‑in tips).  
- `32ffe7bbe7` – U‑TTOB01 (TribalTipOutcomeBridgeEngine + 9/9 tests).  
- `af13bbd78a` – U‑TTOB02 (dispatcher actions for record/effectiveness).  
- `92a3c13dca` – U‑TTOB03 (retrieval ranking by effectiveness).  
- `9076f604a2` – U‑TTOB04 (auto‑instrumentation wrapper).  
- `13d4cb063c` – U‑TTOB05 (wiki doctrine entry).  
- `827dc78459` – embed‑cited‑tips‑into‑tribal‑index.mjs (indexes 309 tips).  
- `0e1391396f` – MillingPrintToProgramEngine instrumentation (auto‑record tip usage).  
- `d6750d71b8` – IntelligentSequencingEngine wired as S3.7 stage in OOP pipeline.  
- `6410a4e8e1` – MILLING‑OOP‑PIPELINE‑AND‑CLOSED‑LOOP‑RUNBOOK‑2026‑05‑27.md (pipeline spec & runbook).

**DECISIONS**

- Bridge `KnowledgeCurriculumBridgeEngine.lessonsForOperation()` into `MillMasterOrchestratorFacadeEngine`.  
- Use `scripts/pdf-parse-extract.mjs` for page‑by‑page extraction; store JSONL ledger and wiki markdowns.  
- Implement AI‑reachable training index (`MillingTrainingNode`) with BM25‑lite, expose via MCP actions.  
- Generate system‑viz augmentation linking L10 PDF nodes to L11 extracted nodes and tribal tips.  
- Adopt foxtrot‑soul attribution: every tip must have `sourceId`, `vendor`, `citation`, `evidence_level`.  
- Fix silent guard bug in `generate-milling-tribal-tip-bridge-features.mjs` (Windows import.meta.url issue).  
- Schedule recurring `/loop` via `CronCreate` with 5‑minute cadence.  
- Build closed‑loop self‑training infra for milling tribal tips; bridge tip applications with outcome logs.  
- Wire dispatcher actions (`prism_mill:mill_tribal_tip_record_application`, `…effectiveness`) for write/read paths.  
- Instrument MillPrintToProgramEngine so every generated program auto‑records used tips.  
- Create embed script to make new tips searchable in tribal‑search.  
- Add IntelligentSequencingEngine as OOP sequencing stage; plan later wiring of SmartToolSelector & MillingStrategyLibrary.

**OPERATOR DIRECTIVES**

- `/checkin-foxtrot /goal [reorientate and regain context ...] /loop [5m] /goal`.  
- “extract wiki + tribal knowledge … cover top 25 brands of tooling vendors…”.  
- “extract from videos … generate wiki and tribal knowledge nodes for every single tool path type”.  
- “try other harvey url so we can move on”.  
- “continue with deep research for tribal and wiki extraction and injections for milling wizard.”  
- “build the closed‑loop self‑training milestone”.  
- “continue then build what we need and wire into mill wizard. deep dive… assess what else we need to build to generate a program from print. develop an order of operation pipeline … begin closed loop training”.  
- `/yolo-mode` (stop‑hook condition).

**FINDINGS/BUGS**

- Silent guard bug in `generate-milling-tribal-tip-bridge-features.mjs` caused main() to skip on Windows.  
- Multiple vendor URLs returned 404; resolved by discovering new URL patterns.  
- Git lock contention due to peer commit; handled via shared‑tree absorption.  
- PDF extraction incomplete for some large manuals (e.g., hyperMILL_2D_3D.pdf) – deferred until `/pdf-learn` pipeline runs.  
- Missing PDF‑learn execution due to lack of `pdftoppm`; infrastructure ready but not yet run.  
- Session limit resets & rate limiting caused intermittent failures.  
- Peer‑absorption hazard: some commits (e.g., iter25) merged into a peer commit; foxtrot attribution lost.  
- Ollama offline prevented embed script from running until recovery.  
- No consumer of tribal tips existed in MillWizard before instrumentation.  
- Hook noise (CAG‑router, discipline‑expert, etc.) required disabling via env vars.

**DOMAIN SPECIFICS**

- Engines: `KnowledgeCurriculumBridgeEngine`, `MillMasterOrchestratorFacadeEngine`, `FeatureInteractionEngine` (anti‑pattern logic), `TribalTipOutcomeBridgeEngine`, `MillingPrintToProgramEngine`, `IntelligentSequencingEngine`, `SmartToolSelectorEngine` (unused), `MillingStrategyLibraryEngine`, `BlueprintOCREngine` (planned).  
- Scripts/hooks: `scripts/pdf-parse-extract.mjs`, `generate-milling-tribal-tip-bridge-features.mjs`, `scripts/youtube-free-extract.mjs`, `embed‑cited‑tips‑into‑tribal‑index.mjs`.  
- MCP actions: `mill_training_for_operation`, `mill_training_search`, `mill_training_summary`, `prism_mill:mill_tribal_tip_record_application`, `prism_mill:mill_tribal_tip_effectiveness`.  
- System‑viz augmentation generator.  
- Paths: `H:/prism-slot-whiskey/state/shared/extracted-pdfs/whiskey-milling-oop-2026-05-26.jsonl`; wiki markdowns under `knowledge/wiki/lessons/`; ingestion cache files `mcp-server/data/ingestion_cache/milling-pdf-corpus.json`, `milling-vendor-online-resources.json`, `jm-die-tribal-wiki-corpus.json`.  
- Metrics: tip effectiveness weighted scores + Laplace smoothing; data/tribal‑outcomes/tip-program-applications.jsonl; tribal‑embed-index.json.

**TOOLS USED**

- PRISM tools: Mill Wizard, MillingWizardEngine, KnowledgeCurriculumBridgeEngine, MillingPrintToProgramEngine.  
- Scripts/hooks: `scripts/pdf-parse-extract.mjs`, `generate-milling-tribal-tip-bridge-features.mjs`, `scripts/youtube-free-extract.mjs`, `embed‑cited‑tips‑into‑tribal‑index.mjs`.  
- Scheduler: `CronCreate`, `CronDelete`; cron loop `*/5 * * * *`.  
- Dispatcher actions for training index and system‑viz augmentation.  
- Ollama (offline during some runs).

**OPEN THREADS**

1. Run full `/pdf-learn` pipeline on remaining PDFs (e.g., hyperMILL_2D_3D.pdf).  
2. Extract JM Die machine manuals, alarm books, parts books, kinematics data into nodes.  
3. Complete video extraction with Ollama when available; deeper transcript‑level extraction.  
4. Bridge vendor‑online tips into system‑viz augmentation (currently unmatched due to https URLs).  
5. Finalize thread milling variants, 5‑axis swarf/flow toolpaths, and 4th‑axis indexing patterns for wizard coverage.  
6. Wire SmartToolSelectorEngine and MillingStrategyLibraryEngine into the OOP pipeline (replace inline heuristics).  
7. Wire BlueprintOCREngine as pre‑S1 intake bridge to close print→features gap.  
8. Replicate closed‑loop bridge pattern for lathe, WEDM, and CAM domains.  
9. Continue video extraction for remaining channels (Iscar, Kennametal, etc.).  
10. Monitor closed‑loop training progress and adjust weighting/embedding as new data arrives.
