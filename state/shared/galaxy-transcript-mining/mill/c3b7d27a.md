# mill session c3b7d27a (2026-06-24, 12.2MB, spine 96KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `synergy-ask.mjs` (graph + vault → Ollama) – 15/15 tests, live‑validated.  
- `/synergy-ask` skill trigger & TOOLBELT entry (`ca7af888b5`).  
- Zero‑grounding guard added to `synergy-ask` (`8f358a2e19`) – skips Ollama when no hits.  
- Wiki pattern entry for combiner (`d92e709553`).  
- Graph‑combine + node‑card enrichment & graph‑slot reservation (`7fd4d41a4c`).  
- Additional commits: `715755e2ed`, `befb50c7c7`, `3dea00d0e7`.  
- Tests: `audit‑viz‑first` 35/35, `viz‑first‑redirect` 30/30.

**DECISIONS**  
- Build single synergy tool using system‑viz query + vault lookup + Ollama.  
- Wire `audit‑viz‑first` reflex into combiner; defer new hook.  
- Add zero‑grounding guard to prevent LLM calls on empty grounding.  
- Reserve up to ⅓ of hit cap for structural graph hits; enrich with `seekCard`.  
- Create wiki pattern entry; no extra hooks or heavy builds.  
- Defer regen‑post‑merge stamp, sfc‑fold stamp (needs quiesced fleet), MCP dispatcher wrap (:3100).

**OPERATOR DIRECTIVES**  
- “Improve utilization of system‑viz, Obsidian vault and Ollama offloading.” – completed.  
- “continue”  
- “point me at a specific next target”

**FINDINGS/BUGS**  
- Memory‑heavy Levenshtein pre‑cap caused health incident; fixed with streaming & cap‑bound logic.  
- `executedOffloads` was zero due to no Ollama execution; now accurate.  
- Zero‑grounding case previously invoked Ollama → hallucination risk; short‑circuited.  
- Graph‑combine vault‑only grounding due to hit‑cap ordering; fixed by slot reservation & card enrichment.  
- Regen‑viz lock contention caused orphaned processes; resolved with stale‑lock kill/retry.  
- False missing‑file alarm from wrong `augmentations/` prefix path; corrected.

**DOMAIN SPECIFICS**  
- **system‑viz**: `regen-viz.mjs`, FAST[] generators, merge‑augmentations, `foldRoostAug`.  
- **Obsidian vault**: wiki/memories, `seekCard` offset index, `vault.mem.*` / `vault.wiki.*`.  
- **Ollama offloading**: `ask-ollama.mjs`, `recordExecution()`, resident models `gpt‑oss`, `qwen3‑coder`.  
- **Skill infra**: `skill-auto-trigger`, `_skill-triggers.jsonl`, TOOLBELT discovery.  
- **Engines/actions**: synergy‑ask utilization arc, graph‑combine reservation, node‑card enrichment.  
- **Dispatchers/hooks**: `audit‑viz‑first`, `viz‑first‑redirect`, `system-viz-query`.  
- **Paths**: generator writes to `state/shared/system-viz/sfc-variability-summary-augmentation.json`; merge reads from VIZ_DIR.

**TOOLS USED**  
- `synergy-ask.mjs` (combiner).  
- `system-viz-query.mjs`.  
- `ask-ollama.mjs`.  
- `seekCard` helper.  
- `foldRoostAug` for merge‑augmentations.  
- `viz-first-redirect.mjs`, `audit-viz-first-inject.mjs`.  
- `skill-auto-trigger` injector, TOOLBELT markdown.  
- PRISM tools: viz‑first‑redirect, audit‑viz‑first, synergy‑ask.  
- Scripts: `generate-sfc-variability-features.mjs`, `merge-augmentations.mjs`.  
- Hooks: `loadOptional`, `foldRoostAug`.

**OPEN THREADS**  
1. Regen post‑merge stamp – resolved; monitor for future failures.  
2. sfc‑fold stamp – requires quiesced fleet; schedule dedicated session.  
3. MCP dispatcher wrap for `/synergy-ask` – needs :3100 & tsc build; defer until idle.  
4. Optimize graph‑combine hit reservation – evaluate impact on diverse query types.
