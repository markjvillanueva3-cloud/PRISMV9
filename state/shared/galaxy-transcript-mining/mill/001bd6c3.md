# mill session 001bd6c3 (2026-06-09, 45.8MB, spine 423KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `822c48c55` – verified‑corrected plan (U-OAB‑PLAN)  
- `3499f5f20f` – U5b: local gpt‑oss tool‑calling read/search agent (`ollama-tool-agent.mjs`)  
- `d807d46f44` – U5c: search‑offload enforcement hook (`ollama-offload-enforce.mjs`)  
- `a59f35221e` – U6: coding‑task offload (`ollama-codegen.mjs`)  
- `cfda0e391a` – U7: Opus tail‑review gate (`ollama-build-loop.mjs`)  
- `e17be56e82` – U10: capability probe proving each local LLM runs and auto‑runs per task  
- Commit `28b72e4dee`: 34 engine `MEMORY.md` files updated to full 4‑section structure; added `fill-galaxy-memory-sections.mjs` & test suite.  
- Docker Desktop installed; Qdrant container (`prism-qdrant`) started automatically on engine restart (v1.17.0, healthy).  
- Hermes GUI backend up at :9120 after cleaning dirty `package-lock.json`.  
- Hermes config migrated to v28: `model.default = anthropic/claude‑opus‑4‑8`, `fallback_model.provider = custom` (`qwen2.5‑coder:32b @ 127.0.0.1:11434/v1`).  

**DECISIONS**  
- Hermes ↔ Claude Code linkage via OAuth (`anthropic`) with local Ollama fallback; keep `claude-opus-4-8` primary, `qwen2.5-coder:32b` as custom fallback.  
- Use **gpt‑oss:120b** for native tool‑calling (structured `tool_calls`); keep gpt‑oss:20b & qwen2.5-coder:32b resident; un‑strand 120b from stale FLOOR tiers.  
- Adopt **gpt‑oss:20b** as fleet general‑work model with Claude fallback; upgrade to 120b when available.  
- Run Hermes headless via PowerShell CLI (`hermes -z`); drop Electron GUI reliance.  
- Use local Ollama/Docker stack for audit, gap‑filling, and read/search offload; limit `MAX_LOADED ≤ 4`, `NUM_PARALLEL = 2`.  
- Auto‑route tasks via `ollama-route-config.json` (`mode:auto`) using `OllamaTaskOffloaderEngine`; keep routing logic deduped.  
- Keep Zulu orchestrator dormant until slots opt‑in; do not patch Hermes custom provider.  

**OPERATOR DIRECTIVES**  
- Bring Qdrant online (ensure Docker container healthy).  
- Verify Hermes reachable at `http://127.0.0.1:9120`; restart daemon if stale Qdrant connection.  
- Send test prompt (“Hello”) in Hermes; confirm no 400 errors and provider is gpt‑oss:20b.  
- Adjust `max_tokens` for gpt‑oss:20b to avoid “no final response” token starvation.  
- Monitor memory pressure; consider `/compact` on idle chats if commit charge high.  
- Audit each galaxy sequentially; sync Obsidian vault via `obsidian-memory-sync.mjs`.  
- Use ultracode Workflow for brainstorming, planning, and verification phases; commit only to slot/bravo worktree.  

**FINDINGS/BUGS**  
- Stale MCP daemon on :3100 with stale `N:\` root – restarted cleanly.  
- Qdrant down: vector store not listening on :6333 (semantic_search affected).  
- Hermes 400 error from Anthropic (`Third‑party apps now draw from your extra usage`) – OAuth token authenticates but billing policy blocks third‑party traffic; fallback not triggered.  
- 429 rate limit observed when testing Claude endpoint (pool exhaustion).  
- Supervisor cwd patch regression risk – reverted, PRISM_ROOT env documented instead.  
- Unknown provider `openai` caused agent init failures; fixed to `custom`.  
- GUI stuck on git‑pull abort due to dirty `package-lock.json`; resolved by discarding file and restarting launcher.  
- One‑shot CLI returned “no final response” for gpt‑oss:20b – token budget too low.  
- 20× memory‑count inflation from auto‑generated `node_*` graph files; excluded them in script.  
- Corrupted `ai-training_synthesis.md` (all‑NUL) flagged, not patched automatically.  
- Hermes custom provider mis‑handles gpt‑oss reasoning channel → “no final response” bug.  
- Zulu orchestrator scheduled task present but no slots opted in; remains dormant.  
- Relative‑path guard in U10 probe caused silent no‑op – fixed to use absolute path.  
- Nomic‑embed tag mismatch (`nomic-embed-text:latest`) prevented presence detection; corrected.  
- False‑green issue in probe’s `assertCodeOutput` regex tightened.  
- VRAM load race in probe’s `loads` check changed to deterministic 200‑status verification.  

**DOMAIN SPECIFICS**  
- PRISM MCP server at :3100 (dispatcher hub for Hermes, Obsidian, system‑viz).  
- Qdrant vector store at :6333 with collections: `prism_engines`, `prism_formulas`, `prism_skills`.  
- Hermes provider registry: `anthropic` (Claude Code), `custom` (OpenAI‑compatible Ollama endpoint).  
- Obsidian REST API exposed on :27123; used for PRISM↔Obsidian bridge.  
- 34‑galaxy architecture: each galaxy has `MEMORY.md`, `PATHS.md`, `TOOLBELT.md`.  
- MASTER‑BRAIN‑TEMPLATE requires four sections: Master‑brain link, High‑ROI memories, Indexed memories, Cross‑galaxy bridges + Known failure modes.  
- Hermes custom provider uses Ollama/Local; gpt‑oss returns separate `reasoning` field.  
- Zulu orchestrator (`zulu-orchestrator-sweep.mjs`) sends SendKeys to worker windows; `zuluOptIn` currently empty.  
- Engines/dispatchers: `OllamaTaskOffloaderEngine.ts`, `ollama-prism-bridge.mjs`, `ask-ollama.mjs`; PreToolUse hook, gpt‑oss tool‑calling agent loop, search‑enforcement hook.  
- Metrics: VRAM per model (qwen2.5-coder ≈ 37 GB at 16K), KV cache overhead (~17 GB).  
- Paths/Configs: `ollama-route-config.json` (`mode:auto`, `NUM_PARALLEL=2`, `MAX_LOADED=4`); `settings.json` wiring; slot‑bravo worktree isolation.  

**TOOLS USED**  
- PRISM ultracode Workflow (e.g., `wbmnnd902`) for brainstorming/plan/synthesis.  
- `/loop` cron (`819251b0`) for periodic builds.  
- Node scripts: `ollama-tool-agent.mjs`, `ollama-offload-enforce.mjs`, `ollama-codegen.mjs`, `ollama-build-loop.mjs`, `session-start-ollama-chat-probe.js`; `fill-galaxy-memory-sections.mjs`, `galaxy-synthesis-refresh.mjs`, `obsidian-memory-sync.mjs`.  
- Docker Desktop & docker‑compose (`docker-compose.yml`) for Qdrant and local LLM stack.  
- Hermes CLI commands: `hermes config migrate`, `hermes doctor`, `hermes model`, `hermes -z`.  
- Node script `node scripts/hermes-obsidian-app-map.mjs` for blind mapping of app state.  
- Test harnesses (`node:test`), R9/10 test suites, live probe execution.  

**OPEN THREADS**  
- Bring Qdrant online (start Docker container, expose :6333).  
- Resolve Hermes 400 error: enable “extra usage” billing on Claude.ai or switch provider; update fallback logic if needed.  
- Confirm local Ollama fallback works under load (rate‑limit handling).  
- Document PRISM_ROOT env fix for supervisor restarts.  
- Confirm Hermes fully operational after restart; tune `max_tokens` for gpt‑oss:20b.  
- Monitor memory pressure; consider periodic `/compact`.  
- Upgrade to gpt‑oss:120b when available; plan upgrade path.  
- Re‑enable Hermes‑Obsidian Bridge scheduled task (currently disabled).  
- Regenerate corrupted `ai-training_synthesis.md`.  
- Schedule periodic `syncGalaxyMemories` to prevent drift.  
- Prompt Zulu orchestrator slots to opt‑in for active fleet coordination.  
- U9 co‑residency module (mutex + env config) – write, test, commit.  
- Go‑live merge of slot/bravo → main and wiring `settings.json` for enforcement hook; gated on golf’s git‑tree repair.  
- RTK & existing‑systems upgrade sweep (#11) pending after go‑live.  
- U1/U3 VRAM routing config edits (NUM_PARALLEL, MAX_LOADED, un‑strand 120b) – to be applied post‑merge.
