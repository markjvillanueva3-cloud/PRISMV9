# mill session e54d46d0 (2026-06-26, 13.3MB, spine 102KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Octopus runs multiple xAI Grok models via Hermes proxy – commit 2b990d785d.  
- Stress‑test harness for Ollama LLMs (false‑0 guard & LLM‑judge) – commits 5e8638f141, cc24367e41, 014cfefb46.  
- Tri‑state judge & no‑signal handling added – commit 056d0710bc.  
- Hard‑tier battery expanded to n=3 – commit 52cd201270.  
- Cheap‑model routing for summarize/explain wired into ask‑ollama – commits 179865164e, 619e7af8d1.  
- Demand‑driven cheap‑tier prime loop (cold → 32b pick → prime → warm 7b) proven live – commit a4fc24ea2e.  
- Stratified docs & wiki lessons shipped – commit a5ff03c32b.

**DECISIONS**  
- Adopt additive per‑mode preference: prepend qwen2.5-coder:7b for summarize/explain only; no forced cold‑load (strict additive).  
- Run judged sweeps per model (1.5 b, 7 b, 14 b, 32 b) <10 min to avoid fleet‑reaper >10 min kills.  
- No deferred reversible/internal fork; all resolvable work executed immediately.  
- Executor wiring (cheap‑model preference + demand‑driven warm‑up) complete and live‑validated.  
- Remove runtime difficulty classifier; measured non‑inferiority across all difficulty levels.  
- Fail‑soft, rate‑limited warm of 7b to avoid VRAM waste; fallback on timeout.

**OPERATOR DIRECTIVES**  
- `/goal …` – enhance octopus with Hermes multi‑model, improve CLI/agent/vault, maximize Ollama offloading, stress‑test LLMs, fix graphs/protocols.  
- “you’re the only one operating” – GPU idle, dist rebuild safe.  
- “continue” – keep working through remaining tasks.

**FINDINGS / BUGS**  
- False‑0 bug: timeouts/no‑output treated as failures; fixed by adding noSignal state at subject and judge layers.  
- Ledger mapper collapsed multiple xAI voices to bare "xai"; generalized to tag vendor:model when >1 voice.  
- Fleet‑reaper kills node processes running >10 min; resolved by per-model sweeps <10 min.  
- ask‑ollama default preference prevented cheap‑model selection; fixed with per‑mode override.  
- Warm‑up of 7b timed out under GPU contention (60 s); now warms in ~3.7 s when idle – fail‑soft design.  
- Manual `/ollama-*` skills hardcoded 7b vs auto‑offloader using 32b; inconsistency resolved.  
- CLAUDE.md stale note (“:7b/:14b retired”) conflicts with `/api/tags`; flagged for correction.

**DOMAIN SPECIFICS**  
- Engines: MultiModelConsensusEngine.ts, ask‑ollama.mjs.  
- Dispatchers/paths: octopus-dispatch.mjs → ledger mapping; octopus-utilization-driver.mjs; install-octopus-utilization-task.ps1.  
- Metrics: stress battery (BATTERY), passRate, noSignal, LLM‑judge semantic score, VRAM usage (~5 GB vs ~20 GB), warm latency, model resident status.  
- Paths unique to this galaxy: /checkin-alpha slot binding; chat-slots.mjs reclaim/claim; audit-roadmap-drift.mjs.  
- Prime activation loop: cold → 32b pick → prime → load 7b → evict 32b.  
- Validation path: R15‑VALIDATE audit confirms no `--model` bypass; production reach verified.

**TOOLS USED**  
- PRISM tools: /checkin-alpha, /checkin, chat-slots.mjs, audit-roadmap-drift.mjs.  
- Scripts: runTierSweep.mjs, stress-battery-generative.mjs, stress-j... (judge), octopus-dispatch.mjs, install-octopus-utilization-task.ps1, trigger-command-pipeline.mjs.  
- Build tools: npm run build:incremental, esbuild, tsc.  
- Testing: node:test + vitest for engine tests; scrutiny script, mutation tests, fleet git‑lock sweep, R15 validation harness.

**OPEN THREADS**  
- Run remaining per‑model judged sweeps (7 b & 32 b) in reaper‑safe windows to complete hard‑tier data.  
- Final executor‑routing payoff: confirm cheap‑model preference wired into ask‑ollama’s loaded‑first logic and live warm‑up.  
- Update documentation/wikis with new architecture memory resolution, lessons learned, and correct stale CLAUDE.md note (“7b/14b retired”).  
- Extend measured battery to triage/codegen modes (GPU idle now; queued).  
- Route `/ollama-*` skills through ask‑ollama for NC‑safety guard and telemetry.  
- Advisory‑layer hints (model‑tier advisor / cost router) – low priority.
