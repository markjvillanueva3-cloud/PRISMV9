# mill session 8dd04bd9 (2026-06-24, 24.7MB, spine 173KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-XRAY-CAD-GT-TRIANGULATE` – `cad-dimension-gt-lib.mjs`: CAD‑model dimensional GT layer + CrossSourceDimensionReconciliationEngine “(b) cad” adapter, GPU‑free `--cad-triangulate`.  
- `U-XRAY-EXTRACTION-CONTRACT` – `BlueprintExtractionContract.ts`: versioned app‑facing extraction contract.  
- `U-XRAY-RECONCILE-CANDIDATES` – `reconcile-candidate-adapters.mjs`: cnc, print & cad source adapters; closed the `cadGtToCandidates` orphan.  
- `U-XRAY-RECONCILE-E2E` – integration test proving adapter→engine consensus end‑to‑end.  
- `U-XRAY-DRAWING-EXTRACT-CONTRACT-NORMALIZER` – `normalizeDrawingExtractToContract.js`: normalizer for Drawing2DExtractionEngine output (unit conversion, type mapping).  
- `U-XRAY-EXTRACT-CONTRACT-WIRE` – `blueprint_extract_contract` dispatcher action + `/api/v1/cad/blueprint-extract-contract` route; de‑orphaned contract via API.  
- `9e755f940b` – PowerMill orchestrator: replaced non‑existent `selectStrategy()` with real `recommend()` API, added mapping to expected shape; 12 reference‑value tests (`95ff48e50a`) now green.  
- `723cc5fbd2` – Fixed stale backlog line “GD&T operator‑confirm surface” (doc‑accuracy).  
- `U-XRAY-CORPUS-TOLERANCE-SHAPE-FIX` – fixed flagged‑but‑unowned `ToleranceCallout.kind` regression (tsc 19→3, corpus test 22/22).

**DECISIONS**  
- Ship CAD GT triangulation lib to provide missing cad adapter for CrossSourceDimensionReconciliationEngine (GPU‑free).  
- Ship extraction contract and normalizers before wiring them into a dispatcher action to avoid orphaning.  
- De‑orphan contract via `blueprint_extract_contract` dispatcher, mirroring the blueprint_redact pattern.  
- Skip building document reading contract now due to cross‑domain ambiguity and lack of consumer.  
- Do not run GPU mill‑recall validation immediately; defer until a quiet fleet window.  
- Do not re‑enable OCR training crons automatically; require operator confirmation (elevated shell needed).  
- Accept CAD‑GT as low‑payoff recall lever; focus on dimension determination.  
- Convert all tolerance callouts to `{tolerance_mm}` (inch→mm), fold surface/material annotations into `intent/description`.  
- Add regression lock on the `tolerance_mm` shape in tests; strengthen assertion from “≥8 of 12” to exact shape.  
- Treat RL/CAM tsc errors (`selectStrategy`, `step()` outcome mismatch) as design decisions for Kilo/India; do not fix here.  
- Keep GPU‑heavy tasks (OCR recall, calibration harness) background; schedule via cron only after confirming loop state.

**OPERATOR DIRECTIVES**  
- “continue closed‑loop training”  
- “plan how to utilize blueprint reading, OCR scanning, document reading functions and features into the prism app features”  
- “use prints + CAD + programs to determine dimensions”  
- “Continue the overnight autonomous build (slot xray, ANY‑DOMAIN never‑idle ladder: FIXES → WIRINGS → ghost‑builds → backlog).”

**FINDINGS/BUGS**  
- CAD‑GT for OCR recall deprioritized; only 5 of 11 neutral‑STEP parts have posted program → low payoff.  
- OCR/training crons all Disabled/MISSING (Blueprint OCR Batch failed, OCR Training Loop missing).  
- `cadGtToCandidates` emitted type “length” instead of canonical “linear”, causing consensus mismatch.  
- `validate-perfect-parts` harness only uses program GT; CAD GT not used for recall.  
- CrossSourceDimensionReconciliationEngine lacked the “(b) cad” adapter – added in lib.  
- GPU mill‑recall validation would evict shared coder model if run now.  
- `ToleranceCallout.kind` regression: callouts empty strings; fixed by type conversion and folding.  
- 3 tsc errors remain: RL‑CAM `step()` expects outcome object (design decision for Kilo); `closeFeedbackLoop` passes wrong arg (`v.done`) (design decision).  
- Data‑loss bug: 3 surface annotations dropped during conversion; fixed by folding into intent/description.

**DOMAIN SPECIFICS**  
- Engines: CrossSourceDimensionReconciliationEngine, fuseEnsemble, Drawing2DExtractionEngine (`extractDrawing`), cnc-program-gt-lib, step-dimension-extract, OCR training loop harness.  
- Actions/dispatchers: cadDispatcher.blueprintRedact, blueprint_extract_contract action, `/api/v1/cad/blueprint‑extract‑contract` route.  
- Metrics: ReconciliationReport (dimensions, conflicts, coverage); OCR recall metrics (`RECALL_PASS=0.5`).  
- Paths: `H:/prism/.claude/helpers/chat-slots.mjs`, `.claude/helpers/slot-bind-enforce.mjs`, `commands/checkin.md`.  
- BlueprintExtractionContract (Zod schema, versioned contract).  
- ToleranceCallout encoder (`{tolerance_mm?, gdt_symbol?, feature?}`).  
- slimResponse utility for serializing dispatcher results.  
- Dispatcher pattern: createXRouter(callTool) → lazy‑import engines, return `{success,data}` serialized by slimResponse.  
- CAD dispatcher registration via registerCadDispatcher(stubServer).  
- PowerMill orchestrator (recommend API).  
- Tolerance shape regression lock.

**TOOLS USED**  
- PRISM tools: `/checkin-xray` wrapper, chat-slots helper scripts, slot-bind-enforce hook.  
- Dispatchers: cadDispatcher, blueprint_extract_contract.  
- Scripts/hooks: `node:test`, `vitest`, `tsc`, `node:assert/strict`; `.claude/helpers/install-<task>-task.ps1` (cron re‑registration).  
- Source files: `reconcile-candidate-adapters.mjs`, `cad-dimension-gt-lib.mjs`, `BlueprintExtractionContract.ts`, `normalizeDrawingExtractToContract.js`.  
- Prism dispatcher & CAD dispatcher (`prism_cad` tool).  
- slimResponse serialization.  
- `ask-ollama.mjs`, `ask-hermes.mjs` for mechanical work (mining, summarizing, graph search).  
- CronCreate for scheduling recurring prompts.

**OPEN THREADS**  
- GPU mill‑recall validation (needs quiet fleet window; would evict shared coder model).  
- OCR/training crons re‑enable (requires elevated shell; currently disabled).  
- Phase‑1 upload→extract route to be built by quebec/papa (not yet implemented).  
- Document reading contract remains unbuilt due cross‑domain.  
- Remaining tsc errors: RL‑CAM design decisions for Kilo/India.  
- OCR recall GT ceiling; need more data to lift threshold.  
- Per‑type calibration volume (P2.9) still below 50 samples per type.  
- Unwired engines: 4 remaining, all gated or out‑of‑domain (AuthEngineV7, SFCInferenceGateWireEngine, PreMOUKickoffChecklistEngine, BlueprintOCRAdapter).  
- No remaining blueprint‑vision orphan engines after audit.
