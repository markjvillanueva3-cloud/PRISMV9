# mill session 95e7030e (2026-05-25, 58.8MB, spine 232KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `20+ alpha‑slot units` to `H:/prism-slot-alpha`.  
- Artifacts:  
  - `mcp-route-suggest.mjs` (`_PREFERRED_ACTION_FOR_CLASSIFIER`, defer‑queue write).  
  - `ollama-pipeline-injector.mjs` (verb‑trigger routes → `prism_dev:ollama_hook_query`).  
  - `audit-nudge-mcp-actions.mjs`.  
  - Stop hooks: `stop-defer-queue-drain.mjs`, `stop-session-spend-summary.mjs`.  
  - Libraries: `defer-queue.mjs`, `injection-dedup.mjs` (tests).  
  - `/r12‑audit` skill & docs.  
  - Wiki entries for action‑hint, banner, discovery, injection‑dedup pattern.  
  - Memory pointers (`reference_psn_*`) auto‑mirrored to `H:/knowledge/memories/`.  
- Build commits:  
  - `4acc0dd927`: `prompt-rules-dedup`, `stop-hook-timeout-budget`.  
  - `10aa20de27`: Wiki & memory updates.  
  - `bc22625104`: U‑PTSM02/03 detectors + tests; mirrored to slot/alpha (`fdeca4c5a7`).  
  - `646790f668`: Prompt‑rewriter overhaul (fail‑loud telemetry, model cache, prompt‑hash dedup, PSN feed).  
  - `510011c39a`: MCP‑disconnect detector + route‑suggest patch.  
  - `5cf9a1ab97`: Ledger prune lib (`jsonl-tail-prune.mjs`) + stop hook.  
  - `8da8622ee0`: PSN aggregator (`stop‑psn‑savings‑aggregate.mjs`).  
  - `e0c7d0428f`: Session‑start headline consumer (`session‑start‑savings‑headline.mjs`).  
  - `61744422ee`: RTK daily rollup stop hook.  
  - `5fb53ef…`: U‑PTSM04/05 detectors (Read, BashNode, WebSearch).  
  - `5c16cbfcbe`: Calibration & confidence boosts (5‑boost scheme).  
  - `e7f3b2b79f`: RTK boost/positive calibration.  
  - `scripts/lib/token-savings-router-table.mjs` (44+ frozen‑Map entries, 8 files, 1308 insertions).  
  - `pre-tool-router-table-advise.mjs`.  
  - `posttool-rtk-adoption-measure`, `posttool-ollama-rewriter-corpus`.  
  - `detector-bandit-tune.mjs`.  
  - `rtk-fraction-tune.mjs`.  
  - TTL bumps: slot‑soul, discipline‑expert, comprehensive‑build (5 min → 24 h).  
  - `psn-prompt-checklist-inject.mjs`.  
  - Per‑file dedup for 6 hooks.  
  - `unified-pre-search.mjs` (BM25 merger, 14 tests).  
  - `apply-token-savings-wires.mjs`.  
  - Cron jobs: `/goal` every 10 m (`94d46b4a`) and every 5 m (`2f96dc3b`).  

**DECISIONS**  
- Fleet‑reaper ownership moved from *alpha* to *golf*.  
- Adopt defer‑queue for mid‑task advisories; queued via Stop hook.  
- R12 audit derives `KNOWN_REAL` actions from dispatcher source, eliminating drift.  
- Canonical source unification of take‑up credit set (`TAKEUP_CREDITED_ACTIONS`).  
- Stop‑hook timeout budget guard (`stop-hook-timeout-budget`) to prevent runaway execution.  
- Injection‑dedup library for deduplicating repeated UserPromptSubmit injections.  
- Calibrate rewrite confidence: penalty/boost scheme, floor 1.00 for perfect score.  
- Detect MCP‑disconnect; suppress route‑suggest when offline (~140 tokens saved).  
- Prune telemetry ledgers to 1 MB tail (`jsonl-tail-prune.mjs`).  
- Aggregate PSN telemetry into `psn-savings-aggregate.jsonl`.  
- Use single intent‑router instead of ~2 K per‑node wrappers; top‑100 first strategy for long‑tail nodes.  

**OPERATOR DIRECTIVES**  
- `/checkin-alpha reorientate with where we left off with alpha work`.  
- `/loop [5m] /goal`: “apply all 5 high‑ROI gap fills”, “scope more high‑ROI functionality and token saving measures especially with tool calls + hooks + stop hooks”.  
- “commit to alpha work tree, completed and wired to all viable nodes and synergized to PSN”.  
- “build the 2k with parallel agents, highest ROI first”.  
- “complete remaining waves for token savings”, “fill all remaining high‑ROI token saving optimizations”.  

**FINDINGS/BUGS**  
- R12 fake actions in `ollama-pipeline-injector` (iter 4) fixed by mapping to real `prism_dev:ollama_hook_query`.  
- Drift of hard‑coded take‑up credit set resolved via canonical source export.  
- Git index lock contention mitigated by committing directly in alpha worktree (`git -C H:/prism-slot-alpha`).  
- Stop hooks hung due to unbounded context size; fixed with `capAdditionalContext` helper.  
- Ollama `/api/chat` dead → prompt‑rewriter skipped calls (timeout >60 s).  
- RTK adoption ~3 % hits; low token savings – addressed by calibration boosts and enforcement flag.  
- Many nodes initially unaccounted; audit closed gaps after adding detectors for grep, read, bash/git, write, search.  
- Stop‑hook timeout budget under‑used until new hooks wired.  
- MCP connectivity probe only checks TCP port → false positives; now includes JSON‑RPC handshake.  
- Slot‑soul, discipline‑expert, comprehensive‑build TTLs 5 min caused >5 min prompts to re‑emit full block (38 K tokens).  

**DOMAIN SPECIFICS**  
- Engines/dispatchers: `mcp-route-suggest`, `mcp-route-takeup`, `ollama-pipeline-injector`, `prism_dev:ollama_hook_query`.  
- Hooks: `pre-tool-router-table-advise.mjs`, `posttool-rtk-adoption-measure.mjs`, `stop-token-savings-summary.mjs`, `detector-bandit-tune.mjs`.  
- Libraries: `injection-dedup.mjs` (hashBlock, shouldEmit), `defer-queue.mjs`, `jsonl-tail-prune.mjs`, `rtk-fraction-tune.mjs`.  
- Metrics: take‑rate sidecar (`state/shared/mcp-route-suggest-stats.json`), banner stats, token‑savings telemetry dashboards.  
- Paths: `H:/prism-slot-alpha/`, `H:/knowledge/memories/`, `state/shared/dashboards/injection-dedup-cache.json`.  

**TOOLS USED**  
- PRISM tools/skills/scripts/hooks: `/checkin-alpha`, `/loop`, `/goal`, `.claude/hooks/*`.  
- Scripts: `apply-token-savings-wires.mjs`, `detector-bandit-tune.mjs`, `unified-pre-search.mjs`.  
- Cron utilities: `CronCreate`, `CronDelete`.  

**OPEN THREADS**  
- Activate RTK enforce flag (`PRISM_RTK_ENFORCE_ENABLE=1`) and wire `stop-token-savings-summary.mjs` into settings.json.  
- Schedule weekly cron for `rtk-fraction-tune` recalibration.  
- Restart Ollama daemon to restore prompt‑rewriter functionality; re‑run after recovery.  
- Validate PSN aggregator output in nightly master‑index regen; ensure session‑start headline reflects aggregated savings.  
- Monitor remaining 37 K candidate nodes for future detector needs.  
- Confirm all detectors wired to every viable node; run final audit post‑commit.  
- Finalize doctrine‑surface TTL fix (staged on disk, pending peer‑lock clearance).  
- Commit MRI (memory‑relevance‑inject) hook once lock clears.  
- Verify unified pre‑search injector active and redundant sources silenced via envVars (`PRISM_*_INJECT=0`).  
- Ensure 5 min cron hygiene tasks apply all pending commits automatically.
