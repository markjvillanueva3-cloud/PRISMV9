# mill session 58cb6b0a (2026-06-23, 17.6MB, spine 120KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5ab3c49002 – U‑XRAY‑CALIB‑ACCUMULATE`: durable calibration‑sample store (ring buffer), flips `reliable:true`.  
- `815567da84 – U‑XRAY‑PROGRAM‑GT‑CALIB`: emits program‑GT `{f,correct}` samples into same store.  
- `cd108380ac – U‑XRAY‑APP‑INTEGRATION‑PLAN`: 5‑phase blueprint‑reading integration plan.  
- `198d811ef4 – U‑XRAY‑CALIB‑STATS`: CLI `summarizeCalibrationStore` for calibration corpus observability.  
- `47a37b714d – U‑APP‑REDACT‑LIB`: shared TS redaction module; fixes customer‑name‑in‑key leak & over‑redaction.  
- `62c20067d1 – U‑APP‑REDACT‑WIRE`: wires redactor into `prism_cad:blueprint_redact`.  
- `b8ef51c9fe – U‑XRAY‑READING‑KNOWLEDGE`: curated GD&T/ASME Y14.5 knowledge bundle injected opt‑in into VLM prompt.  
- `fa6a037974 – U‑XRAY‑TRUNCATION‑KEYCUT`: tier‑2 JSON salvage recovers ~30 dims on dense prints after mid‑key truncation.  
- `U-XRAY-NUM-PREDICT-TUNABLE`: env‑raisable `PRISM_OCR_NUM_PREDICT`; default 4096, tested green; no recall lift at 8192.  
- `U-XRAY-READING-GUIDANCE-VALIDATE-WIRE`: `--reading-guidance` threads into `validate-perfect-parts` on region‑route & plain paths; byte‑identical when off.  
- `e1cfab0e20 – Added --enhance flag to rasterize path (pdf-to-png.py)`, wired into harness; behavior‑changing but no recall lift.

**DECISIONS**  
- Persist calibration samples in ring buffer (`calibration-sample-store.mjs`) to avoid GPU runs, achieve `reliable:true`.  
- Emit program‑GT only when recall scorer trusted (`gtReliable`).  
- Use opt‑in byte‑identical knowledge channel (`blueprint-reading-knowledge.mjs`) for VLM prompt injection.  
- Replace hard‑coded redaction logic with shared TS module; wire via dispatcher to expose app surface.  
- Add tier‑2 JSON salvage (`salvageTruncatedJson`) to recover partial outputs when truncation cuts mid‑key.  
- Keep production default `num_predict=4096`; 8192 offers no recall benefit and risks hallucination.  
- Default `--reading-guidance` remains off; no precision/recall improvement observed.  
- Do not flip defaults until a GT‑bounded part shows lift; focus on fixture quality instead.  
- Wire preprocessing (`--enhance`) for future experiments; current test shows no recall gain.

**OPERATOR DIRECTIVES**  
- “continue” (loop 10 min) – complete remaining backend tasks, priority on xray tasks.  
- “improve blue print reading capabilities by tying in all gd&t, engineering courses, resources, wikis and tribal knowledge.”  
- “keep making improvements.”  
- “plan how we can utilize blueprint reading/OCR scanning/document reading functions into Prism app features (Ollama offloading, Hermes agents, parallel loops, engineered loops, harnesses, crons).”  
- “push forward.”

**FINDINGS/BUGS**  
- Customer‑name‑in‑key leak fixed with full‑registry key masking; over‑redaction of short acronyms mitigated by tier logic.  
- Mid‑key truncation bug: VLM output cut at `num_predict=4096` caused dense print loss; resolved with tier‑2 JSON salvage, restoring ~30 dims per print (live 0 → 28 dims).  
- Dense prints contain 56–86 dims at `num_predict=8192`; 28 dims recovered by truncation salvage.  
- On part 05850 recall pinned at 3/7 regardless of `num_predict`, `--reading-guidance`, or `--enhance`.  
- No hallucination inflation observed when increasing `num_predict` on this part; precision stable.  
- Enhancement flag changes dims (39→37) but does not recover missing GT dims—those are absent from the scan, not OCR‑missed.

**DOMAIN SPECIFICS**  
- Engines: `blueprint-ocr-training-loop.mjs`, `validate-perfect-parts.mjs`, `vision-ensemble-fuse.mjs`; `blueprint-vision` (OCR extraction), `region-classify`.  
- Actions/dispatchers: `cadDispatcher` (blueprint actions), `prism_cad:blueprint_redact`, `--reading-guidance` threads into `validate-perfect-parts`.  
- Metrics/paths: calibration‑sample store (`state/shared/ocr-training-loop/calibration-samples.jsonl`), knowledge bundle (`scripts/lib/blueprint-reading-knowledge.mjs`), JSON parse success (`parse_ok`).  
- Unique to galaxy: closed‑loop OCR training harness, program‑GT emission logic, GD&T recall scorer integration.

**TOOLS USED**  
- `/checkin-xray` wrapper (slot binding + handoff); `chat-slots.mjs` helpers.  
- PRISM scripts: `calibration-sample-store.mjs`, `cnc-program-gt-lib.mjs`, `blueprint-reading-knowledge.mjs`, `salvageTruncatedJson`.  
- OCR harnesses (`node:test`, `jest`), 2‑arm scrutiny, TSC type checks.  
- PDF rasterization script `pdf-to-png.py`; buildVisionPrompt, runEnsembleOverImage, fuseEnsemble, extractWithRegionRouting, validate-perfect-parts.  
- Node tooling: `node --check`, `parseArgs`.  
- Git hooks: `[MAIN-FORCE]` lane guard, commit hygiene.

**OPEN THREADS**  
- Validate recall on a fixture where the 4 missing GT dims are legibly present; then test OCR levers.  
- After confirmation, consider raising production default `num_predict` to 8192 and enabling `--reading-guidance`.  
- Continue closed‑loop training of VLM prompts with updated knowledge channel.  
- Explore integration of blueprint reading/OCR into Prism app features (Ollama offloading, Hermes agents, parallel loops).  
- Wire knowledge channel into region‑route crop path for default‑on usage.  
- Re‑run closed‑loop training cron now that dense prints yield dims again.  
- Finalize operator‑gated Y14.5 corpus drop and RAG engine activation for live extraction.
