# mill session c4e1875f (2026-06-26, 4.1MB, spine 31KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `3e67bee1dd` – “HERMES‑GROK‑CAP / U‑GROK‑HIGHEST‑CAPABILITY”  
  * Added pure ranker `scripts/lib/grok-capability-rank.mjs` (19 tests).  
  * Wired into: `ask-hermes.mjs`, `hermes-mcp-server.mjs`, `verified-offload-tiered.mjs`, and octopus `hermesAgentLenses`.  
  * All 189/189 tests green; both scrutiny arms passed.

**DECISIONS**  
- Use a single shared ranker to determine the highest‑capability Grok model instead of hard‑coding the first entry.  
- Delegate `pickModel` in all consumers to this ranker and expose an optional env pin (`PRISM_HERMES_PREFERRED_MODEL`).  
- Keep design import blocked until the `claude_design` MCP is authenticated; do not attempt to fetch design data without credentials.  
- Register `claude_design` in `.mcp.json` (project‑scoped) so future sessions can prompt for OAuth.

**OPERATOR DIRECTIVES**  
- “can you connect the claude design mcp?” – user explicitly asked to register the MCP; action taken: added entry to `.mcp.json`.  
- Await user to restart session and authenticate (`/design-login`) before proceeding with design import.  
- Optionally, if a Chrome instance with Claude extension is connected, user may issue “go” for browser‑pull of `Kienzle Tool Crib.dc.html`.

**FINDINGS / BUGS**  
- `PRISM Hermes Proxy` task failed (`port 8645 refused`, `authenticated:false`). All Hermes features inactive until proxy restarted and OAuth added.  
- XAI mis‑ranking bug: `grok-4-fast-non-reasoning` incorrectly received reasoning bonus; fixed in ranker.  
- No Chrome with Claude extension connected (browser pull path unavailable).  
- MCP config missing for `claude_design`; now registered but requires user authentication and session restart.

**DOMAIN SPECIFICS**  
- Slot‑binding wrappers: `/checkin-alpha`, `/checkin-quebec` use `chat-slots.mjs` (`claim`, `reclaim`).  
- Canonical `/checkin` pipeline stages (drift check, audit-roadmap-drift, BUILD_STATE, system-viz ping, etc.).  
- Hermes components: `ask-hermes.mjs`, `hermes-mcp-server.mjs`, `verified-offload-tiered.mjs`, octopus `hermesAgentLenses`.  
- Design import via `claude_design` MCP (requires OAuth).  

**TOOLS USED**  
- Node scripts: `chat-slots.mjs`, `checkin.md`.  
- CLI commands: `claude mcp add`, `claude mcp list`, `claude mcp remove`.  
- Test frameworks: Node test runner, Vitest.  
- Git utilities for branch/commit checks (`git rev-parse`).  

**OPEN THREADS**  
1. **MCP Authentication & Session Restart** – once user authenticates the `claude_design` MCP and restarts, design import will trigger.  
2. **Proxy Fix** – restart Hermes proxy and add OAuth to enable all Hermes features.  
3. **Backend‑to‑Frontend Wiring** – pending real design data from Kienzle Tool Crib; once available, plan and implement full wiring.
