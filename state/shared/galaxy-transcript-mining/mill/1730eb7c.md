# mill session 1730eb7c (2026-05-27, 127MB, spine 288KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**

- `84db861343` – Academy → Lathe‑Wizard bridge (6 priors, 15/15 tests)  
- `38bcc524f5` – 4 local PDFs + 6 vendor research files  
- `5121df34cd` – 14‑vendor master index + AI‑queryable helper (`scripts/query-lathe-tribal.mjs`)  
- `88c1b98e48` – 8 vendor PDFs + curriculum manifest  
- `6fda303ee1` – per‑page PDF extractor (186 pages, 7/7 tests)  
- *unknown* – quality pipeline & PSN training loop specs; ALCOA baseline (57/100); physics deviation detection (41/41); vendor breadth (25 tooling, 25 machine, 12 holders, 13 vises, 19 media); video tribal extractor + 54 wiki stubs; channel‑targeted harvest → 200 videos (6/6 tests); body‑level topic classifier (34 buckets, 12/12 tests); low‑coverage fill rounds 1–2; insert‑geometry classifier expansion (2→8 codes, 12/12); corpus milestone – 300 videos / 79 lathe‑relevant / 34 buckets  
- `feedback_yolo_mode_nonterminal_goal_pattern.md` – doctrine memory on non‑terminal `/yolo-mode` loops  
- Iter49–115 – JM‑fleet controller coverage (Okuma, Mazak, Haas, Fanuc, Doosan) and advanced topics (turret ops, live‑tool, threading, G7x cycles, polygon turning, Swiss headstock, thermal compensation, MQL, MTConnect, first‑part inspection)  
- Iter116–120 – Mastercam/Esprit/Fusion 360/hyperMILL/Inventor HSM bridge coverage; operator‑named brand content (Performancetoolingsecrets, Titans of CNC, Iscar, Widia/Kennametal, Ingersoll Cutting Tools, DMG MORI/Mori Seiki)  
- Iter121–125 – Tribal‑query dispatcher design, training‑loop stages 4–5, G76 validator test scaffold, AB‑locator regression tests  
- Iter126–128 – Shop‑tool library bridge implementation, tribal‑query engine, wizard vendor lookup  
- Iter129–131 – Training‑loop stage 4 REASON & Stage 5 GENERATE implementations; E2E smoke test  
- Iter132–135 – Real‑data validation (JM‑Die A/B programs); fixes for CRLF, operation inference, T‑format, byte‑exact passthrough, comment extraction, controller‑aware G70  
- Iter136–140 – AB‑locator A‑marker stripping; Stage 4/5 regression tests  
- Iter141–145 – Operator‑facing templates (tool‑list markdown, vendor PDF ingestion, MCP dispatcher action, CLI scan‑runner, TypeScript wiring)  
- Iter199–207 – Meta‑README indexing operator templates; AB‑locator CLI scanner & regression test; full archive scan → 14 475 A/B pairs; customer‑rootfile mis‑classification fix; pair‑type classification (`prism_upgraded`, `human_revision`, `empty_source`)  
- Iter216, 218, 227–228 – `--score` flag for Δ‑data emission; Stage‑4 detector `detectMissingSafetyStateFlags`; regression test  
- Iter250–260 – Durable cron 8505e156 replacing dead session‑only cron; AGRATI & CAMCAR customer scans; metric corrections after parseBlocks fix; retraction of v2.0.0 upgrade claim  
- Iter271, 273–285 – Documentation of unsatisfiable `/goal /yolo-mode` loop; cross‑customer matrix extended to 20 customers (HOLO‑KROME, OPTIMAS, BIRMINGHAM FASTENER, etc.)  
- Iter302 – Training‑signal roadmap memo (4‑path priority list)  
- Iter303 – Closed‑loop infrastructure `PPGSFCClosedLoopOrchestratorEngine` (U‑PPG‑SFC‑14) with 6‑phase SFC, LoRA adapters, EWC++ forgetting protection, Brier shadow‑mode gate  

**DECISIONS**

- Adopt non‑terminal `/yolo-mode` doctrine: cron auto‑loops every 5 min until condition met; no manual pause.  
- Slot `whiskey` owns LATHE domain; all work scoped to that slot.  
- Use `slot-bind-enforce.mjs` for deterministic slot claim; `CronCreate`/`CronDelete` for goal enforcement.  
- JM‑fleet coverage via design memo + shipping iteration per controller.  
- Test‑first, contract‑driven TDD for P0 engines (G76 validator, shop‑tool bridge, tribal query, wizard vendor lookup, AB locator, training loop).  
- Real‑data validation mandatory: synthetic fixture bugs surface on real JM‑Die programs before completion.  
- Closed‑loop training uses operator overrides as primary signal; 6‑phase orchestration (SFC → override capture → 30‑experience threshold → LoRA per customer+material+machine → Brier evaluation → adapter promotion).  
- EWC++ forgetting‑rate tracking protects against catastrophic forgetting.  

**OPERATOR DIRECTIVES**

- “Extract wiki and tribal knowledge from as many videos … generate nodes for every toolpath type, tooling usage, order of operation, tips and tricks.”  
- “Cover top 25 tooling brands, top 25 machine vendors, top 10+ tool holders, top 10+ vises, manufacturing magazines.”  
- “Run loops in `/yolo-mode` with `/loop [5m]`; the stop‑hook will block until the condition holds.”  
- “Wire `prism_lathe:query_vendor_tribal` MCP dispatcher (≈50 lines).”  
- “Persist adapter registry to disk (`mcp-server/data/state/lora-adapter-registry.json`).”  
- “Implement real LoRA training backend (Flow Nexus/Ollama) and auto‑trigger on 30‑experience threshold.”  
- “Build Brier oracle with real shop‑floor outcome feed.”  

**FINDINGS / BUGS**

- R12 fail‑loud: `String.match()` missing `/g` → fixed via `matchAll`.  
- Regex patterns missed G73, G75, speed‑feed; expanded.  
- Video transcripts router‑focused; added disambiguation logic in topic classifier.  
- CRLF preservation bug (Stage 5) – fixed iter148.  
- Missing drilling cycle operation inference – fixed iter153.  
- T‑format lookup failure (Mazak ↔ Fanuc) – fixed iter151.  
- Byte‑exact passthrough regression when no levers applied – fixed iter154.  
- Operator comment extraction for T‑blocks – fixed iter156.  
- Okuma G70 finish‑pass logic corrected – fixed iter159.  
- AB‑locator mis‑pairing with `‑B` suffix variants – fixed.  
- parseBlocks matched G‑codes inside parenthesized comments – fixed.  
- Unsatisfiable loop between `/goal /yolo-mode` hook and doctrine documented (iter271).  
- Missing operator‑override capture path; override history empty.  
- Adapter persistence lost on restart.  
- Real LoRA training backend absent; orchestrator only stubs training phase.  
- Brier oracle stubbed – needs real shop‑floor outcome feed.  

**DOMAIN SPECIFICS**

| Component | Role |
|-----------|------|
| **Engines** | `LatheAITrainingEngine`, `LatheCSSOptimizerEngine`, `LatheChipMechanicsEngine`, `LathePrintSequencePlannerEngine`, `LathePostProcessor`, `BoringBarDeflectionEngine`, `lathe-ab-version-locator.mjs`, `scan-jm-die-ab-pairs.mjs`, `lathe-training-loop-stage-4-reason.mjs`, `PPGSFCClosedLoopOrchestratorEngine` |
| **Actions / Dispatchers** | `/checkin-whiskey`, `slot-bind-enforce.mjs`, `CronCreate`, `CronDelete`, `goal-complete-gate.mjs`, `/system-viz`, `prism_lathe:query_vendor_tribal`, `prism_ai:submit_sfm_override` |
| **Metrics** | 34 topic buckets coverage; ~200 videos processed (6/6 tests); lathe‑relevant rate ≈ 26 % → ~80 %; test pass rates 47/47; avg_delta lines per pair; G40/G80 parity; archive coverage per customer scan |
| **Unique Paths** | `H:/prism-slot-whiskey`, `mcp-server/data/ingestion_cache/lathe-tribal-master-index-2026-05-26.json`, `knowledge/wiki/lessons/video-extract-*.md`, `PRISM_UPGRADED` folder patterns, `B_FOLDER_PATTERNS`, customer directories in JM‑Die archive |

**TOOLS USED**

- PRISM tools: `/checkin-whiskey`, slot‑claim helpers (`chat-slots.mjs`), cron utilities (`CronCreate`, `CronDelete`), stop‑hook (`goal-complete-gate.mjs`), `/system-viz`.  
- Scripts/skills: `scripts/lib/lathe-academy-priors.mjs`, `scripts/extract-lathe-pdfs-per-page.mjs`, `scripts/query-lathe-tribal.mjs`, `youtube-free-extract.mjs`, `scripts/patch-lathe-pdf-extracts.mjs`.  
- Hooks: `slot-bind-enforce.mjs`, `stop-hook` (goal‑complete‑gate), feedback memory feed (`stop-obsidian-memory-feed.mjs`).  
- AB‑locator scanner (`scan-jm-die-ab-pairs.mjs`); parseBlocks comment‑strip; `--score` flag for Δ‑data.  
- Detector: `detectMissingSafetyStateFlags`.  
- MCP dispatcher framework (Zod schemas, action enums).  
- Flow Nexus / Ollama for LoRA training backend (to be wired).  
- aiSystemRouterEngine for routing AI calls.  

**OPEN THREADS**

1. Wire `prism_lathe:query_vendor_tribal` MCP dispatcher (~50 lines).  
2. Persist adapter registry to disk (`mcp-server/data/state/lora-adapter-registry.json`).  
3. Implement real LoRA training backend (Flow Nexus/Ollama) and auto‑trigger on 30‑experience threshold.  
4. Build Brier oracle with real shop‑floor outcome feed.  
5. End‑to‑end orchestrator chaining all lathe engines (12 stages).  
6. Real shop tool‑list ingestion (iter194 template) and PDF ingestion for ground truth (iter195).  
7. Finalize dashboard regeneration and wiki entries for new engine modules.  
8. Integrate vendor graph node visualization (`lathe_vendor_graph_node_design`).
