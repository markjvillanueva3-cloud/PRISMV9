# mill session 501bd704 (2026-06-04, 22.3MB, spine 93KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `scripts/py/gpu_health.py` – GPU readiness probe (torch guard, sm tag check).  
- `scripts/lib/py-subprocess-bridge.mjs` + tests – Node→Python NDJSON bridge with tree‑kill on Windows and timeout warning.  
- `GpuStackHealthEngine.ts` + tests – PRISM engine executing `gpu_health.py`, exposes `prism_dev:gpu_stack_health`.  
- Wiring into `devDispatcher` (action `gpu_stack_health`).  
- GNN F0 fix: source‑enrichment of ghost embeddings (`engineSourceSignal`), direct‑embed inference path (`PRISM_NNG_DIRECT_EMBED`), base‑rate normalization in `voteDispatcher`, calibration logic added but disabled.  
- Resume‑hash bug fix – restored missing `\x1F` delimiter, added equivalence test (`da6aedfc07`).  
- Commit `9bd4b22abd` (U‑FGC‑2b) added `.gitattributes eol=lf`.  
- Created memory `feedback_recurring_fleet_write_mistakes`, wiki page `recurring-fleet-write-mistakes`.  
- Default evaluation split switched to stratified; CLI flag `--flat-holdout` added.  
- All 62 eval tests pass: AUROC 0.808, macroF1 0.439, Brier 0.179.

**DECISIONS**  
- Split GPU readiness into probe → bridge → engine → dispatcher for clarity.  
- Use direct‑embed inference (8‑d edgeless model unusable).  
- Keep base‑rate normalization mandatory; calibration optional and documented as negative result.  
- Adopt stratified holdout as default to avoid zero‑sample classes; keep flat split via `--flat-holdout`.  
- Record split type in NN‑EVAL.json and runAssessment output.

**OPERATOR DIRECTIVES**  
- Continue `/loop [5m] /goal` monitoring GNN metrics.  
- Do not disable or bypass any review/3‑of‑3 scrutiny gates; maintain full cross‑galaxy authority.  
- Keep GPU training golf‑gated until torch cu129/sm_120 installed.  
- “Say the word” to continue loop with next units (calibration‑done‑right, class‑balancing).

**FINDINGS/BUGS**  
- Feature starvation + edgeless inference caused constant‑vote degeneracy (AUROC 0.5).  
- Leak in ghost embed text inflated AUROC; removed.  
- Calibration regression: LOO calibration over‑confident on holdout → worsened AUROC & Brier.  
- Resume‑hash bug: missing `\x1F` delimiter caused silent re‑embedding of ~372k nodes.  
- CRLF/LF flip issue resolved; memory still warns about missing `.gitattributes`.  
- Flat split caused zero holdout for `prism_calc`, dragging macroF1 down; stratified split corrects this.

**DOMAIN SPECIFICS**  
- Engines/dispatchers: `U-CAP-PROBE`, `U-PY-BRIDGE-LIB`, `GpuStackHealthEngine.ts`, `devDispatcher` action `gpu_stack_health`.  
- Metrics: AUROC, macro‑F1, Brier, accuracy, `isDegenerate`.  
- Paths: `system-graph.json`, `node-embeddings.jsonl`, `ghost-node-embeddings.jsonl`, `nn-graph-eval` inputs, `.gitattributes`.  
- Evaluation flow: `runAssessment` → `assessHoldout` → NN‑EVAL.json.

**TOOLS USED**  
- PRISM CLI (`/checkin-india`, `/startup-india`, `/loop`).  
- Chat‑slot helpers (`chat-slots.mjs`).  
- Node/TS tooling: `devDispatcher.ts`, `GpuStackHealthEngine.ts`, `scripts/lib/py-subprocess-bridge.mjs`.  
- Python scripts: `gpu_health.py`, `build-node-embeddings.mjs`.  
- Test harness: Vitest (per‑file scrutiny, 3‑of‑3 gates).  
- Git (`git log`, `.gitattributes`).

**OPEN THREADS**  
- Calibration remains unimplemented; need robust holdout‑aware method.  
- Macro‑F1 still below deploy gate (~0.55); requires balanced reference pool or richer features.  
- GPU training still golf‑gated – torch cu129/sm_120 must be installed before engines run.  
- Next milestone: `GpuTrainingSchedulerEngine` to allocate VRAM slots for all galaxies.  
- Proceed with calibration‑done‑right (held‑out split / temperature scaling preserving AUROC).  
- Class‑balance reference pool (currently 45 % `prism_turning`) to push macroF1 toward 0.55.  
- Finalise memory updates for the other four recurring mistakes.  
- Verify all gate thresholds with new stratified default before next release.
