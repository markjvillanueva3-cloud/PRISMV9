# mill session ad9c3041 (2026-06-16, 30.9MB, spine 153KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑SELFCOMPACT-CONFIRM` – self‑compaction end‑to‑end proof; P0/P2 fixes (commits 55ec1198c4, c04746880f, ac966750b0). Path: `scripts/lib/self-compact-confirm-lib.mjs`.  
- `FEATURE‑ROUTING‑GRAPH` – unified 12‑class task→route graph + catalog (commit 281fdecd93). Path: `model-routing-policy.mjs`.  
- `FORCE‑USE‑MAP` – dormant‑feature scrutiny; revived `getGraphNodeHits`, added force‑deny for grep, fixed policy P1s (commits 3eb9344b76, db02ed6b11). Path: `scripts/lib/force-use-map.mjs`.  
- `PROMPT‑ROUTE‑MAP` – mined 613 sessions; frequency‑weighted route map; auto‑inject hook wired (commits 23e4499e9d, 56a19e3da8). Path: `scripts/lib/prompt-route-map.mjs`.  
- `U‑OPENROUTER‑WIRE` – OpenRouter client/CLI, cloud Nemotron‑3‑super tier added to routing policy and advisor (commit 91c…).  
- `U‑OPENROUTER‑TELEMETRY` – OpenRouter client + ask‑openrouter CLI with full telemetry (`executedOffloads`, `measuredTokensSaved`). Path: `scripts/lib/openrouter-client.mjs`.  
- `U‑GREP‑FORCE‑ACTIVATE` – grep‑index‑first force‑deny active via cap‑safe name→path resolver (`CODE_SYSTEM_INDEX.json`). Path: `grep-index-first.mjs`.  
- `U‑CLASSIFY‑RESEARCH` – added high‑precision phrase signals to `classifyRoutingClass`; research/investigate prompts route to learn/fix. Path: `model-routing-policy.mjs`.  
- `U‑DOCKER‑SCOUT‑WIRE` – read‑only Docker Scout wrapper, org‑gated, fail‑loud, ready‑on‑enroll. Path: `docker-scout.mjs`.  
- `U‑DOCKER‑ASSESS‑REFRESH` – updated 4‑week old Docker Business assessment. Path: `docker-mcp-catalog.mjs`.  
- CAG hit‑rate “honesty” unit – added miss‑reason buckets, warm‑hit‑rate calculation; dashboard & tests updated.  
- Route‑savings banner fix – measurement‑gap branch for fires≥50 & 0 takes.

**DECISIONS**  
- Adopt NVIDIA Nemotron‑3‑super free tier via OpenRouter (no local pull; 87 GB VRAM limit).  
- Record every executed OpenRouter offload on canonical stats surface (`executedOffloads`, `measuredTokensSaved`).  
- Wire cap‑safe name→path resolver to activate dormant grep force‑deny.  
- Expand classifier signals to avoid misclassifying research prompts while preserving build semantics.  
- Wire Docker Scout as read‑only, org‑gated; defer Hub registry & MCP catalog until credentials available.  
- Treat CAG hit‑rate 10 % as cold‑start dominated; expose warm‑hit‑rate instead.  
- Label route‑savings 0/372 as measurement gap when MCP bridge offline.

**OPERATOR DIRECTIVES**  
- Provide `OPENROUTER_API_KEY` to run live probe (`ask-openrouter.mjs ask "reply PONG"`).  
- Enroll Docker Scout: `docker login`, `docker scout config organization <org>`.  
- Configure Hub private registry & MCP catalog once credentials available.  
- Build next self‑drive unit; resume loop.  
- Wire cloud Nemotron‑3‑super tier (network 1 Gb/s).

**FINDINGS/BUGS**  
- Self‑compact actuation dormant; resolved via UIA tab naming.  
- Silent‑dead `getGraphNodeHits` due to V8 string cap (727 MB graph); fixed with find‑cache fallback.  
- OpenRouter client `redactKey` case‑sensitive bug; fixed.  
- Policy P1s over‑broad cloud trigger & missing build veto tightened regexes.  
- Advisor hook exit‑code mis‑reporting under redirection (shell artifact).  
- Grep index dead because find‑cache nodes lacked paths; resolved via cap‑safe resolver.  
- Classifier mis‑routes research/investigate to build; fixed with phrase signals.  
- CAG hit‑rate 10 % dominated by cold starts; added warm‑hit‑rate metric.  
- Route‑savings banner incorrectly reported “below target” when MCP bridge offline; corrected.

**DOMAIN SPECIFICS**  
- Engines/dispatchers: `chat-slots`, `checkin pipeline`, `audit-roadmap-drift.mjs`, `self-compact confirm lib`, `feature-routing graph components` (`task-substrate-router`, `model-routing-policy`, `loop-goal-stack-advisor`, `skill-auto-trigger`), OpenRouter client/CLI.  
- Metrics: compact_boundary transcript markers, ledger of send/fallback/confirm, prompt‑route frequency map, offload stats (`executedOffloads`, `measuredTokensSaved`), CAG cache stats (`hits`, `misses`, `warmHitRate`, `missReasons`), route‑savings banner (`fires`, `takes`).  
- Paths: `H:/prism/.claude/helpers/chat-slots.mjs`, `H:/prism/.claude/commands/checkin.md`, `scripts/lib/self-compact-confirm-lib.mjs`, `scripts/lib/openrouter-client.mjs`, `model-routing-policy.mjs`.

**TOOLS USED**  
- PRISM tooling: chat‑slots, checkin pipeline, audit‑roadmap‑drift, Obsidian integration.  
- Workflow/Agent orchestration (fan‑out, Sonnet miners).  
- Scrutiny suite (opus reviewer, analyst, code‑analyzer).  
- OpenRouter client library, ask‑openrouter CLI.  
- Docker CLI (Scout, MCP catalog).  
- Node.js test harnesses (vitest, node tests).

**OPEN THREADS**  
1. `OPENROUTER_API_KEY` – required to run live probe and close task #7.  
2. Docker Scout enrollment & Hub registry config – enable CVE scanning and private image pulls.  
3. MCP catalog consumption wiring – integrate pre‑built MCP servers into PRISM workflow.  
4. Further efficiency units (route‑suggest take‑rate, CAG cache invalidation monitoring) pending after above actions.
