# mill session 14093afb (2026-06-26, 17.2MB, spine 146KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `lathe-closed-loop-full.mjs` – unified driver for Rung A (34,993 .MIN), B round‑trip, C STEP leg (`full_geometry_loop_closed=true`).  
- `U-W3` – live‑tooling archetypes for Rung B.  
- `turningDispatcher.ts` – wired 4 Okuma engines (step parse, macro convert, manual tips, transcript mine).  
- `LatheLoRASafetyEvaluatorEngine` – safety evaluator; fleet‑floor envelope 3800 rpm/11 kW.  
- `U-W6` – tribal knowledge max‑out (101→694 tips, dry on all local PDFs; target 500 exceeded).  
- `U-W8` – Kienzle in‑page rename (`brand='Kienzle'`).  
- `lathe-tip-classify.mjs` + runner – classifier hardened to reject FF2 hallucination.  
- `U-W9` – closed‑loop Rung‑B grid regrounded in JM’s real tool‑steel stock (annealed‑P turning state); driver heap hardened to 8 GB.  
- `U-W10` – fleet‑wide EMFILE concurrency fix; 3,989 materials load successfully under harness.  
- `U-W11` – in‑flight guard added to `MaterialRegistry`; harness runs clean at default 4 GB heap.  
- Kienzle build – FE↔BE wiring verified live end‑to‑end; G4 handoff written for Quebec.

**DECISIONS**  
- Adopt unified driver (`lathe-closed-loop-full.mjs`) to avoid duplicated pipelines and guarantee consistent dashboard output.  
- Introduce fleet‑floor safety envelope (3800 rpm/11 kW) as weakest JM lathe; guarantees never‑false‑UNSAFE while keeping cost/efficiency checks.  
- Add pure‑JS STEP geometry leg (`lathe-rungc-step-loop.mjs`) to close Rung C without GPU dependency, enabling full closed‑loop test.  
- Deploy deduplication guard via `duplicationGuardEngine` before any new asset; require reference‑value tests (≥3 happy, ≥2 adversarial).  
- Fix path‑casing double‑count and `sfm_max` drift bugs; clamp to [20, 3000] ft/min.  
- Route mechanical text to Ollama; reserve Claude for physics/safety/synthesis.  
- Stop only on a genuine spiral or when all units are complete.

**OPERATOR DIRECTIVES**  
- Build sequence: U‑W2 → U‑W3 → U‑W4 → U‑W5 → U‑W6 → U‑W7 → U‑W8.  
- Use dedup‑guard, reference‑value tests, per‑file 2‑arm scrutiny.  
- Commit each unit with `[MAIN-FORCE] [KIENZLE-LATHE-WIZARD]/U-ID` tags and tick loop state (`loop-state.mjs`).  
- Do not re‑run `/checkin`.  
- Report honestly shipped vs pending (R12).

**FINDINGS/BUGS**  
- Path‑casing double‑count on case‑insensitive Windows paths caused duplicate scoring.  
- `sfm_max` drifted from m/min to ft/min; fixed conversion and clamped to [20, 3000] ft/min.  
- FF2 control‑code hallucination produced false `feed_factor:1.2`; added quantitative guard.  
- Advisory vs parametric yield: ~5% of tips contain actionable speed/feed signals; rest advisory.  
- Stale corpus‑coverage WARN fixed by using `--all-roots` to scan all 34,993 .MIN files.  
- JM `.MIN` files lack material callouts; grid premise “free‑machining” incorrect.  
- EMFILE concurrency bug in `MaterialRegistry.load()` caused OOM under harness load; race condition led to repeated loads and silent emptiness.  
- Loader read error mislabelled as “parse”; real cause was EMFILE (“too many open files”).  
- Grid must include tool‑steel grades (H13, M2, D2, S7) and annealed‑P turning state.

**DOMAIN SPECIFICS**  
- Engines/Dispatchers: `lathe-closed-loop-full.mjs`, `BlueprintVisionOCREngine → TurningPrintIntakeEngine → runPipeline` for PDF prints; `turningDispatcher.ts`; `LatheLoRASafetyEvaluatorEngine`.  
- Metrics: `full_geometry_loop_closed`, `full_geometry_loop_closed_step/ocr`, `steps_scored`, `paired`, `safety_basis`; SFM envelope accuracy, feed envelope, mean confidence.  
- Endpoints/Paths: `/api/v1/lathe/*` (wizard‑submit, upload, result).  
- Triggers: `duplicationGuardEngine`, `audit-close-out-candidates.mjs`.

**TOOLS USED**  
- Node scripts: `loop-state.mjs`, `audit-close-out-candidates.mjs`.  
- Deduplication guard engine.  
- `/checkin-whiskey` wrapper (slot‑binding).  
- `chat-slots.mjs`, `lathe-tip-classify.mjs`, `lathe-tribal-classify.mjs`.  
- Ollama for mechanical‑text routing.  
- Git commit tags `[MAIN-FORCE] [KIENZLE-LATHE-WIZARD]/U-ID`.

**OPEN THREADS**  
- U‑W2 – unified full‑corpus driver (Rung A over all 34,993 .MIN).  
- U‑W3 – Rung B live‑tooling archetypes.  
- U‑W4 – wire 4 Okuma engines to `turningDispatcher`.  
- U‑W5 – wire `LatheLoRASafetyEvaluatorEngine`.  
- U‑W6 – max lathe tribal (PDF‑learn, video‑learn, lathe‑learn).  
- U‑W7 – 3 FE/BE lathe API gaps from Quebec‑FE‑BE‑Wiring‑Map; cross‑lane.  
- Video/MIT sources for `/video-learn` – operator‑supplied URLs/files required.  
- G4 frontend design concept – queued to Quebec lane.  
- Cross‑lane – Quebec’s 10 UI‑gap Kienzle front‑end surfaces pending implementation.
