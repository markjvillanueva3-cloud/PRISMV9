# mill session 885d585d (2026-06-24, 29.8MB, spine 360KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `266812bce7` – deterministic test‑rigor floor (`detectShallowCriticalTest`) + CI legitimacy gate.  
- `806bda494d` – AI semantic judge (`test-rigor-judge.mjs`).  
- `3575eeb71e` – audit 10/25 thin critical tests; octopus 2‑voice consensus.  
- `02641a95ca` – Hermes fallback 400 bug fix + two‑voice octopus confirmation.  
- `e845db2c89` – full 25/25 test‑rigor audit (audit MD & JSON).  
- `ab2b3bc84a` – first R9 coverage for `stop_on_failing_tests`: main guard + pure `pickStaleTestFromStatus`.  
- `1a0177736a` – `callJudge` CLI test suite (8 hermetic tests).  
- `f853c08ade` – verified‑offload tiered primitive (`verified-offload-tiered.mjs`).  
- `a6a6243a2a` – `ollama-offload.mjs` CLI wiring + fixed `makeHermesRunner` missing‑model bug.  
- `119a1c557d` – `HERMES-TIER-OLLAMA-TIMEOUT-KNOB`: env knob & NaN hardening for Ollama tier timeout.  
- `b46945b8c3` – telemetry bucket for `ollama-file-digest.mjs` offloads (`byHook["ollama-file-digest"]`).  
- `aadf5a5177` – FEATURE‑ROUTING “both” arc verified (localEnsembleWired = true).  
- `7d6f314990` – shared atomic‑RMW offload‑stats envelope + 4 execution writers (`recordTieredUsage`, `recordFileDigestOffload`, `recordLocalOffload`, `recordUsage`).  
- `1f927b3c1b` – 4 advisory hooks migrated onto the envelope (`large‑read‑digest`, `nav‑rerank`, `wiki‑read‑offload`, `ollama‑nav‑enforce`).  
- `6c2b3c8476` – wiki entry enriched to expose offload‑stats envelope as discoverable asset.  
- `152586c025` – adversarial tests for corrupt byHook recovery (`ensureOffloadBucket`).  
- `b5fa10a632` – cross‑bucket take‑rate measurement primitive (adds `decayReport.xtake`, CLI column).  
- `3c3d7bf2e7` – decision‑half spec added to `/wiki-query`.

**DECISIONS**  
- Cancelled all durable `/checkin-india` crons; alpha cron remains.  
- Re‑bound handoff to **alpha** (`alpha-work`).  
- Added main guard to `stop_on_failing_tests`; R9 test now passes.  
- Feature‑routing “both” arc confirmed shipped – no further work needed.  
- Adopted env‑knob for Ollama tier timeout; telemetry bucket for file‑digest offloads created.  
- Decision‑half built as fail‑safe un‑mute override (31/31 tests green).  
- Operator‑only fork resolved: capture credentials & arm account switch before 5 h limit (~90%) expires.

**OPERATOR DIRECTIVES**  
- “Please stop whatever is causing you to switch to india.”  
- “Use sound logic to make the most optimal decisions to continue forward.”  
- `/checkin-alpha` goal: improve Hermes CLI, Hermes agent, Obsidian vault, PSN, system‑viz, Ollama offloading, octopus utilization.  
- “Continue hardening and improving utilization.”  
- Verify WT tab naming (“PRISM alpha”) for `/compact` SendKeys.

**FINDINGS / BUGS**  
- Stale‑green gate (`stop_on_failing_tests`) remains untested; main guard added.  
- Hermes fallback 400 bug fixed in `02641a95ca`.  
- `makeHermesRunner` omitted default model → empty fallback; fixed by using `DEFAULT_HERMES_MODEL`.  
- `ollama-file-digest.mjs` never recorded offloads → 0‑Claude conversions invisible; telemetry bucket added.  
- Ollama tier timeout hardcoded to 30 s; env knob now available (`HERMES-TIER-OLLAMA-TIMEOUT-KNOB`).  
- `ask-hermes.mjs` default model constant not exported, causing mis‑configuration.  
- `/compact` SendKeys dormant due to missing “PRISM alpha” tab name.  
- `CONVERSION_BUCKET_MAP` key mismatch (`"ollama-nav-enforce-inject"` vs `"ollama-nav-enforce"`).

**DOMAIN SPECIFICS**  
- Engines: Hermes CLI/agent (`ask-hermes.mjs`), Ollama offloading (`ask-ollama.mjs`, `ollama-offload.mjs`).  
- Offload‑stats envelope (`recordTieredUsage`, `recordFileDigestOffload`, `recordLocalOffload`, `recordUsage`) exposed via wiki.  
- Octopus consensus dispatcher (`prism_ai:consensus_decide`).  
- Feature‑routing graph reconciled (`localEnsembleWired` flag).  
- Adversarial tests for corrupt byHook recovery (`ensureOffloadBucket`).  
- Decision‑half spec live on `/wiki-query`.  
- Context zone YELLOW: token zone worstPct 0.60, ~600K tokens.

**TOOLS USED**  
- PRISM scripts: `scripts/lib/verified-offload-tiered.mjs`, `node scripts/ask-hermes.mjs`, `node scripts/ollama-offload.mjs`, `node scripts/audit-unwired-engines.mjs`.  
- CI/validation: `audit-jm-cam-libraries.mjs`, `fleet-reaper-sweep.mjs`, `system-viz-query.mjs`, `vault-health.mjs`.  
- LLM orchestration: Ollama (`qwen3‑coder:30b`, `qwen2.5‑coder`), Hermes (Grok), Octopus (gpt‑oss + qwen2.5‑coder).  
- Telemetry buckets: `byHook["ask-hermes"]`, `byHook["ollama-fallback"]`, `byHook["ollama-file-digest"]`.  
- Testing harnesses: Vitest, Node test runner.

**OPEN THREADS**  
- Take‑rate decision half (design‑heavy; requires full re‑validation of mute logic on 4 live hooks).  
- Resolve `/compact` SendKeys dormancy – ensure WT tab named “PRISM alpha”.  
- Fix `CONVERSION_BUCKET_MAP` key mismatch to activate override.  
- Build decision‑half as fail‑safe un‑mute override (already green, pending final merge).  
- Remaining ghost builds and misc tasks in backlog after runway cleared.
