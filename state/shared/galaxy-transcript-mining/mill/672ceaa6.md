# mill session 672ceaa6 (2026-06-26, 9.8MB, spine 82KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `b8184fb49a` – infra‑synergy research + Phase 1 Nginx overlay & certs, Phase 2 ES overlay, fail‑soft `SearchIndexEngine`.  
- `67b8358b0f` – design spec for adding a Redis‑Streams backend to the existing `EventBusEngine`.  
- `04101a7911` – implementation of `RedisStreamSink` + additive `EventBusEngine` integration (12/12 live tests).  
- `622c817802` – self‑signed TLS certs generated and gitignored for Nginx.  
- `34c059bc36` – fixes to `SearchIndexEngine`: IPv6 fetch bug, flaky sleep replaced with bounded poll.  
- `6112afa40a` – added a first‑class `redis` service to the compose stack; live validation passes.  

**DECISIONS**  
- **Cutover strategy:** perform a single‑consumer test behind the flag (`PRISM_SEARCH_BACKEND=es`) before fleet‑wide flip.  
- **Hermes update completion:** must close all Hermes processes (agent fleet + desktop) and run `hermes-setup.exe` to rebuild venv, npm install, electron build; then restart fleet.  

**OPERATOR DIRECTIVES**  
1. Stop every running Hermes process (`hermes.exe`, `python.exe` from the venv).  
2. Run `C:\Users\<user>\AppData\Local\hermes\hermes-setup.exe` (or the downloaded 6/17 installer).  
3. Verify bootstrap logs show “Repository ready” → “venv ready” → “desktop build complete”.  
4. Restart the Hermes fleet (agent backend + desktop app) once bootstrap finishes.  

**FINDINGS / BUGS**  
- *Git pull blocker* – dirty `package-lock.json` & bundled `main.cjs` caused `git pull failed`. Fixed by stashing and fast‑forwarding to `origin/main`.  
- *Bootstrap venv lock* – running Hermes fleet prevented removal of `.pyd` files; resolved by stopping all processes before bootstrap.  

**DOMAIN SPECIFICS**  
- **Hermes Agent:** Python virtualenv, Electron desktop front‑end, Git‑based update mechanism (`hermes_bootstrap_lib`).  
- **Update flow:** `repository` (git pull), `venv` (recreate venv), `npm install`, electron build, stamp refresh.  
- **Infrastructure stack:** Docker compose with Redis, Elasticsearch, Nginx; all services default to file‑based backends until flags enabled.  

**TOOLS USED**  
- Git (`stash`, `pull`, `checkout`).  
- Node.js `fetch` (for ES client).  
- Electron build tooling (`electron-builder`).  
- Docker Compose overlays (`docker-compose.nginx.yml`, `docker-compose.search.yml`, `docker-compose.redis.yml`).  

**OPEN THREADS**  
- **Hermes bootstrap completion:** must be finished before fleet can resume.  
- **Flag‑based cutover test:** once Hermes is stable, enable `PRISM_SEARCH_BACKEND=es` for a single consumer and run parity gate.  
- **Full fleet cutover:** after successful flag test, flip both `PRISM_SEARCH_BACKEND` and `PRISM_EVENT_BUS`.
