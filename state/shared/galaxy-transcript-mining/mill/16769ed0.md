# mill session 16769ed0 (2026-06-21, 28.4MB, spine 282KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑REWRITER‑SKIP‑LOOP‑DIRECTIVES` (6a7b572eae): loop‑directive skip before Ollama round‑trip; tightened regex, added tests.  
- `U‑REWRITER‑CHATMODEL‑SELECT` (631e273cd2): replaced inline family regex with reusable `ollama-loaded-chat-model.mjs`; fixed gpt‑oss/deepseek “no‑model” and vision‑model mis‑selection.  
- `U‑REWRITER‑FALSE‑WARM‑COMMENT‑FIX` (fee67d7760): removed false 24 h keep‑alive claim; documented real max 30 m prewarm.  
- `U‑LOADED‑CHAT‑STRICT‑OPTION` (1c6abe2878): added `opts.strict` to `pickLoadedChatModel`; all helper & rewriter tests pass.  
- `U‑OLLAMA‑PS‑PROBE‑DEDUP` (70b94eb1c9): consolidated `/api/ps` and `/api/tags` probes into one lib; 15/15 tests green, live‑validated.  
- `U‑MCP‑ROUTE‑SUPPRESS‑ISVERBOSEBASH` (e0b3df1ea0): static suppress filter for `isVerboseBash`; 11/11 new + 29/29 regression tests pass.  
- `audit‑viz legend R12 honesty fix` (5be19a26c2): added missing “evaluations>0” cause.  
- `audit‑viz docstring legend update` (0aab43dadc): ASCII normalised, updated legend.  
- `audit‑viz‑first‑inject uses shared injection‑dedup lib` (8d344941fe): added `decideAuditVizEmit`, dedup guard.  
- `U‑PRUNE‑TAG‑SHARED‑CACHE` (d3e0b7ebaf): per‑tag cache pruning replaces generic `pruneExpired`; 12 evictors migrated, 35/35 tests green.  
- `U‑PSN‑CHECKLIST‑TEST‑ENABLED` (4307ece067): fixed env‑dependent test hermeticity for `shouldInject`; 16/16 tests green.

**DECISIONS**  
- Feature‑routing “both” arc shipped; no further work.  
- Rewriter revival low ROI → closed after root‑cause doc.  
- Next focus: high‑ROI Ollama offload optimisation (warm‑coder resident).  
- Self‑compact at YELLOW unreliable in fast loops → manual `/compact` required.  
- Commit only self‑contained units; skip ask‑ollama loaded‑first wiring due to Zulu conflict.  
- Introduce `strict` option for warm chat model selection.  
- Deduplicate Ollama probes into shared lib, add tests.  
- Suppress `isVerboseBash` nudge per low‑take doctrine, keep telemetry (`_recordRouteFires`).  
- Migrate all writers of injection‑dedup cache (including wrapper layers) to eliminate orphan deps.  
- Replace generic `pruneExpired` with per‑tag `pruneTag`; foreign tags preserved.  
- Pin `opts.enabled:true` in tests for hooks that capture module‑level `ENABLED`.  
- Re‑light dark ledger for `read-auto-limit.mjs`: add import guard, ensure writes only on real runs; refactor hook for testability.

**OPERATOR DIRECTIVES**  
- Maintain continuous “never idle” loop with per‑unit 2‑arm scrutiny, 3‑of‑3 gate.  
- Use `[MAIN‑FORCE]` commits, `--no‑verify`, pathspecs; update handoff; monitor PC health before new build.  
- Rails: R12 fail‑loud; R13/R15/R16 comprehensive; dedup before create; units first.  
- Reasoning=Opus, coding=Sonnet+coder‑ensemble (qwen3‑coder 30b + qwen2.5‑coder).  
- Harness Ollama, Obsidian, Hermes agents, Octopus consensus for autonomously built loops/crons.  
- Finish any in‑flight unit → descend NEVER‑IDLE ladder; idle if dry on safe work & health OK.  
- Snapshot PC health (Golf variant); read vault‑ops backlog, run `node scripts/vault-health.mjs`, prioritize vault‑ops hardening then system‑viz queries (Sierra variant).

**FINDINGS/BUGS**  
- Rewriter dead: `pickModel` never used loaded chat model → 8 s Ollama round‑trip per directive; no warm coder kept alive.  
- Mis‑selection of gpt‑oss/deepseek as “no‑model”; vision models returned for `/api/chat`.  
- False 24 h keep‑alive claim; actual max 30 m, pipeline‑triggered only.  
- Self‑compact race: queued `/compact` did not fire before next prompt; context remained YELLOW.  
- Ask‑ollama loaded‑first wiring conflicts with Zulu’s live edits → cannot commit.  
- Self‑compact actuation fails when slot is null or SendKeys layer missing; native auto‑compact required.  
- Decay actor ignores `suppress‑candidate` verdicts (takes = 0); cannot auto‑consume them.  
- `isVerboseBash` nudge fires often but <5% take → net‑negative; suppression needed.  
- Shared injection‑dedup cache bug: short‑TTL evictors pruned long‑TTL siblings → token leak.  
- Env‑dependent tests in `psn-prompt-checklist` caused false failures due to module‑level `ENABLED` captured at import.  
- Dark ledger for `read-auto-limit` frozen since 2026‑05‑26; aggregator maps `nudge-emitted` to hit with zero `savedTokens`.

**DOMAIN SPECIFICS**  
- PRISM 26‑slot autonomous fleet (alpha token‑efficiency domain).  
- Prompt‑rewriter hook (`prompt-rewriter-ollama.mjs`) – UserPromptSubmit, fleet‑wide.  
- Ollama offload economy: `/api/ps` warm‑probe, `/api/tags` up‑probe, `keep_alive`, `LOADED_MODEL_ONLY`; shared RTX‑PRO 6000 GPU.  
- Per‑agent handoffs via `.claude/helpers/per-agent-handoff.mjs`.  
- Auto‑memory writes to `C:/Users/.../.claude/projects/H--prism/memory/`; Stop hook mirrors to `H:/knowledge/memories/`.  
- Scrutiny gates: per‑file 2‑arm, end‑of‑task 3‑of‑3 ledger.  
- Token‑economy (alpha): injection‑dedup lib (`hashBlock`, `shouldEmit`, `recordEmit`, `pruneTag`), audit‑mcp‑route‑takerate, route‑suggest‑decay.  
- Vault‑ops hardening: `node scripts/vault-health.mjs`, system‑viz queries (`node scripts/system-viz-query.mjs`).  
- Injection‑dedup library now used by `audit‑viz‑first‑inject`.  
- Engines/Actions: `injection-dedup.mjs`, `pruneTag`, `psn-prompt-checklist` hooks, `read-bundle.mjs`, `read-auto-limit.mjs`, `psn-savings-aggregate.mjs`.  
- Dispatchers: `node scripts/audit-unwired-engines.mjs`, `fleet-survival-status.mjs`, `self-compact.mjs`.  
- Metrics: token usage, `savedTokens`, `est_tokens` hit counts.  
- Paths: shared injection‑dedup cache JSON sidecar, `read-auto-limit-ledger.jsonl`, `psn-prompt-checklist` test files.

**TOOLS USED**  
- Hook scripts: `prompt-rewriter-ollama.mjs`, `ollama-loaded-chat-model.mjs`, `ask-ollama.mjs`, `mcp-route-suggest`, injection‑dedup lib, `psn-prompt-checklist`.  
- Build helpers: `node scripts/audit-unwired-engines.mjs`, `node scripts/fleet-survival-status.mjs`, `node scripts/self-compact.mjs`, `node scripts/vault-health.mjs`, `node scripts/system-viz-query.mjs`.  
- Offload orchestration: ask‑ollama, Hermes/Forge‑Team agents, Octopus consensus.  
- Testing harness: `node <file>.test.mjs`.  
- Scrutiny: `node .claude/scripts/scrutiny‑3way.mjs`.  
- Mill Wizard (HSM/trochoidal/adaptive), ap/ae engagement, face‑mill, pocketing, mill speed/feed.

**OPEN THREADS**  
- Fleet offload warm‑coder resident unit (keep a small coder continuously warm).  
- Further token‑efficiency units in alpha domain (e.g., optimizing other Ollama hooks).  
- Manual `/compact` to reset context before next loop tick.  
- Verify per‑classifier credit path for `isLargeRead`, `doctrineSurface`, `backendAuditChain`.  
- Potential suppression of other low‑take nudges (`isLargeRead`, etc.).  
- Resolve self‑compact actuation issue if needed.  
- Re‑light dark ledger for `read-auto-limit` and refactor hook for testability – pending commit.  
- Audit unwired engines and ghost builds backlog.  
- Vault‑ops hardening tasks queued.
