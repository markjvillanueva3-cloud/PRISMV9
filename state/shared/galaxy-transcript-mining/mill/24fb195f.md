# mill session 24fb195f (2026-06-23, 2.5MB, spine 31KB, 1 slice(s), model gpt-oss:20b)

**DECISIONS**  
- Pin the model to `opus[1m]` in the global `~/.claude/settings.json` so 1 M context is always requested regardless of alias resolution.  
- Remove the undocumented `autoCompactWindow: 800000` and leftover `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` from the project‑level settings (`H:/prism/.claude/settings.json`) to eliminate the 800 K cap that was limiting context after compaction.  

**OPERATOR DIRECTIVES**  
- Restart the Claude Code Desktop app so the updated settings take effect.  
- Verify that the context bar shows `/1M` and remains at 1 M even after a compaction cycle.  
- If the context still drops to 200 K, run `/model opus[1m]` in the Desktop session as a temporary workaround.

**FINDINGS/BUGS**  
- No desktop‑only configuration directory exists; the Desktop “Code” tab shares `~/.claude/settings.json` and project settings with the CLI.  
- The 800 K cap was caused by an undocumented `autoCompactWindow: 800000` entry that persisted in the project file.  
- The 1 M context reverted to 200 K after compaction because the model alias `"opus"` resolved via `ANTHROPIC_DEFAULT_OPUS_MODEL`, which lost the `[1m]` suffix across a compaction boundary.  
- The behavior of context reverting post‑compaction is undocumented; pinning the model with `[1m]` is the most reliable fix.

**DOMAIN SPECIFICS**  
- PRISM’s `precompact-auto-trigger.mjs` hook blocks tool calls at ~900 K tokens but cannot trigger `/compact` on Desktop.  
- Native Claude Code auto‑compaction is controlled by `autoCompactEnabled` and `DISABLE_AUTO_COMPACT`; the removed knobs were PRISM‑only.  
- The `[1m]` suffix in the model ID (`claude-opus-4-8[1m]`) is the official way to request 1 M context.

**TOOLS USED**  
- Local configuration inspection (searching `~/.claude/settings.json`, project settings, and potential desktop config paths).  
- Documentation lookup agent for model‑config reference.  
- JSON validation of edited files.  

**OPEN THREADS**  
- Await user confirmation after restarting the Desktop app that 1 M context persists post‑compaction.  
- If the issue recurs, file a bug with Anthropic regarding context loss after compaction.  
- Decide whether to commit the project‑level settings change so fleet automation does not revert `autoCompactWindow` removal.
