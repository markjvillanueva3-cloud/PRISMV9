# mill session 30a6a98b (2026-05-23, 57.2MB, spine 159KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U‑RAG‑1 audit blind‑spot fix – `scripts/wiki-tribal-cross-ref-audit.mjs` (commit e07edcbf76); coverage 97.2 % ≥ 95 %.  
- U‑RAG‑2 two‑stage lexical reranker wired to all inject hooks (`tribal-by-domain`, `master-index`, `memory-relevance`, `wiki-precheck`) – commits 6df057e098, c8acc5accd, 52b1fe91e8, bb1ff5f589.  
- U‑RAG‑3 contextual retrieval lib (`scripts/lib/contextual-blurb.mjs`) + embed flag `--with-context` (commit 92aa9279d6).  
- U‑RAG‑4 synergy wiring: system‑viz roost, wiki architecture entry, 5 per‑unit obsidian memories, GNN bridge docs – commits 8554ca7c4d, 12182c62dd, 8105fbf76d.  
- U‑RAG‑5 RetrievalEvalEngine harness & dispatcher wiring (`devDispatcher.ts` actions `rag_eval_score`, `rag_eval_run`) (commit 619e22f9cc).  
- U‑RAG‑PSN‑AI‑WIRE – added `RAG_CROSSWIRE_ACTIONS` and case `"rag_rerank"` to `aiReasoningDispatcher.ts`.  
- PSN‑leg‑11 wired; 10/11 legs now expose RAG surface.  
- GNN retrain pipeline triggered (legacy 8‑d fallback due to missing node‑id ↔ wiki‑path mapping).

**DECISIONS**  
- Adopt audit‑only coverage metric; skip re‑embed under `wiki:` scheme.  
- Keep mixed ID index; unification optional.  
- Use two‑stage reranker for all inject hooks (lift precision@k by 15–30 %).  
- Defer GNN feature layer mapping (node‑id ↔ wiki‑path) outside current scope.  
- Default `/loop` to auto‑accept edits in automode; bypass permissions only when physically away.  
- Run fleet‑reaper on 128 GB for orphan cleanup, slot hygiene, GPU coordination.  
- PSN integration via cross‑dispatcher `*_CROSSWIRE_ACTIONS` pattern (no delegation).

**OPERATOR DIRECTIVES**  
- `/checkin-bravo continue u-rag` – resume RAG‑UPGRADE‑MS0 work.  
- `continue current tasks /yolo-mode` – maximum velocity, bypass permissions.  
- `finish whats left` – list remaining operator‑only items.  
- `do everything you suggested` – start PSN‑leg‑2 wiring & mapping layer (not yet shipped).  
- User query: “should we be in automode instead of bypass permissions mode” → answer: use automode.

**FINDINGS/BUGS**  
- Audit blind spot: `tribalWikiPath()` counted only `wiki:` IDs; real embedder uses `external:` → 0.8 % coverage loss.  
- GNN retrain fell back to legacy 8‑d features (`embeddingHitCount:0`); AUROC 0.297 vs baseline 0.096, below promotion gate.  
- Node‑embed job died at 223 K/377 K; no resume logic → restart required.  
- MCP zombie pile (~106 processes) caused high memory usage; permanent hunter added to reaper.  
- Fleet‑reaper preset persisted but initial run missed stale MCP processes.

**DOMAIN SPECIFICS**  
- **Engines / Actions**: `tribal-by-domain-inject`, `master-index-precheck-inject`, `memory-relevance-inject`, `wiki-precheck-inject`; `ReRankerEngine.rerank`; `scripts/lib/contextual-blurb.mjs`; `embed-wiki-into-tribal-index.mjs`.  
- **Dispatchers**: `devDispatcher.ts` (`rag_eval_score`, `rag_eval_run`); `aiReasoningDispatcher.ts` (cross‑wire RAG).  
- **Metrics**: retrieval precision@k, recall@k, MRR, mAP via RetrievalEvalEngine; GNN AUROC.  
- **Key Paths**: `scripts/wiki-tribal-cross-ref-audit.mjs`, `scripts/embed-all-wiki.mjs`, `scripts/lib/lexical-rerank.mjs`, `scripts/lib/contextual-blurb.mjs`; `state/shared/system-viz/_node-embeddings.jsonl`; `state/shared/nn-graph/graphsage-checkpoint-768d-node-embedded.json`.

**TOOLS USED**  
- PRISM chat‑slots helper (`chat-slots.mjs`), slot‑bind‑enforce, `/fleet-reaper` suite (orphan cleaner, GPU coordinator).  
- `/startup-golf` wrapper; `/handoff` skill for session continuity.  
- RetrievalEvalEngine, lexical‑reranker, system‑viz generator scripts (`regen-viz.mjs`, `merge-augmentations.mjs`).  
- GNN retrain pipeline (`graphsage-train-pipeline.mjs`).  

**OPEN THREADS**  
1. Node‑embed restart – resume from 223 K or fresh run (~8 h).  
2. Full context‑aware embed pass (`--with-context`) – Ollama‑driven, ~hours runtime.  
3. Graph‑node‑id ↔ wiki‑path mapping layer (U‑RAG‑6) – required for GNN retrain.  
4. PSN‑leg‑2 wiring (prism_operating_system).  
5. Finalize GNN retrain after mapping layer – ensure node IDs match embeddings; target AUROC ≥ 0.78.  
6. Monitor MCP zombie hunter integration for regressions.
