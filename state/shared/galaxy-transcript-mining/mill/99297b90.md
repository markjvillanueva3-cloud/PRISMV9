# mill session 99297b90 (2026-06-11, 31.2MB, spine 283KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `0a966b5696` – Compaction‑boundary & drift guard fix; updated `transcript-token-counter.mjs`, `precompact-auto-trigger.mjs`.  
- `3f0a9aef11` – Fleet‑wide dense RAG backfill (34 galaxies, 64/64 tests).  
- `d12424096c` – Alpha brain refresh: token‑opt memory + ROI queue.  
- `0dda52f7de` – Drift guard for compaction loop.  
- CLAUDE.md slim (U‑CBF01/02): 167 KB → 92 KB, ~21K tokens saved fleet‑wide.  
- `injection-dedup-fs.mjs` + sidecar cache: dedup heavy hooks, saving ~2.3 KB/turn (~665 tokens).  
- GNN heterophily predictor fix: persisted config, AUROC 0.5, 369/369 tests.  
- Self‑reexec heap guard for graph loading scripts; OOMs eliminated (542 MB graph).  
- Golf reaper re‑enabled to clear orphaned looping chats (API rate limits).  
- Compaction fix (U‑CBF01/02): constant‑compact loop resolved, 49/49 tests.  
- Read‑everything rule fleet‑wide; HONESTY doctrine enforced.  
- `verify-galaxy-ai-synergy.mjs`: 34/34 synergy confirmed, 0 gaps.  
- `676dd275b5` – GNN heterophily checkpoint provenance (369/369).  
- `83e5aa61d4` – Fleet lone‑surrogate API‑400 chokepoint (28 hooks + 4 injectors, all tests passed).  
- `791f2073ac` – Prompt‑context‑inject throttle: 204 B “daemon down” notice suppressed to 0 B on repeat.  
- `001a3e627c` – AI‑systems dispatch plan mapping owners for standing goal.  
- `7b8dbde2dd` – Compaction fix: byte‑estimate fallback flagged suspect, downgraded to warn; no false triggers.  
- `87f90f8bd0` – Doc‑reflection entry for compaction regression.  
- `088e74fb92` – GNN selective‑promote branch (`PRISM_NN_SELECTIVE_PROMOTE`).  
- `cebb5639b5` – Zulu fleet self‑compaction enabled (24 slots, 24 h dry‑run).  
- Build state: all `.mjs` clean (`node --check`), tests green (chat-token-watch 42/42, graphsage 369/369).

**DECISIONS**  
- Replace legacy `"isCompactSummary":true` with `compact_boundary`; guard `[940K,1.1M]`.  
- De‑duplicate PostToolUse wiring; keep sidecar refresh hook only.  
- Use reaper‑proof scheduled task (`install-galaxy-mine-task.ps1`) over chat‑spawned mining.  
- Pivot to Sonnet agents for synthesis; fallback to Opus main chat due to prompt limits.  
- Add fleet rule: “Existence ≠ complete – read full content, not just titles.”  
- Persist heterophily parameters in GNN checkpoints (avoid runtime dim mismatch).  
- Adopt single FS wrapper injection dedup (`injection-dedup-fs.mjs`); sidecar cache TTL 5 min.  
- Apply self‑reexec heap guard for large graph loads to prevent OOMs.  
- Re‑enable golf reaper & monitors; avoid manual chat intervention.  
- Keep selective‑deploy promotion path separate from production checkpoint swap (India GPU lane pending).  
- Strip lone UTF‑16 surrogates (`stripLoneSurrogates`) across 28 hooks + 4 injectors.  
- Throttle prompt‑context‑inject “daemon down” notice to 30 min, suppress repeats.  
- Flag context‑bundle daemon as down; postpone restart until Phase C1 migration.  
- Dispatch standing goal into per‑slot owners; no cross‑lane edits.  
- Enable suspect guard on `chat-token-watch` byte‑estimate to prevent phantom compaction triggers.  
- Enable hermes/zulu fleet self‑compaction with 24 h dry‑run grace.  
- Deploy GNN selective promotion path after operator approval (`PRISM_NN_SELECTIVE_PROMOTE=1`).  
- Schedule recurring maintenance loop (Cron `4e514422`) for token‑savings checks every 10 min.  
- Use per‑agent handoff with stable session ID to preserve state across `/compact`.

**OPERATOR DIRECTIVES**  
- Fix constant‑compact loop; take control of alpha slot and avoid current issue.  
- Utilize ultracode + ollama llms + octopus to read previous alpha conversations, generate categorized data for tasks left/completed/dormant, update galaxy docs (claude.md, memories, wikis, souls.md, prism awareness), wire to obsidian vault, hermes/zulu; optimize context retention and task queue by ROI.  
- Optimize token‑eating injections fleet‑wide: audit, dedup, throttle.  
- Enforce rule to read everything, not just titles or existence checks.  
- Reduce 200 k token injection size; make it more efficient.  
- Run GNN loop for India.  
- Identify cause of API errors (only active chat).  
- Activate golf reaper and monitors back on.  
- Other chats waiting on completion of GNN build.  
- Flip `PRISM_NN_SELECTIVE_PROMOTE=1` to deploy the GNN, or specify bounded piece.  
- Execute AI‑SYSTEMS‑GOAL‑DISPATCH plan by owner.  
- Run `/compact` after handoff written.

**FINDINGS/BUGS**  
- Compaction loop caused by transcript‑marker format change; byte estimator counted entire file → unguarded hard block in `[940K,1.1M]`.  
- `precompact-auto-trigger.mjs` had two broken paths: legacy marker only and assistant‑source path misread cache.  
- Sidecar healthy because reads usage blocks, not bytes.  
- Sonnet agents failed due to >200 k token injection from CLAUDE.md + engine digest + wiki index; prompt too long before any read.  
- CLAUDE.md injection bloated >200 k tokens (308 lines stale commit log).  
- Three heavy UserPromptSubmit hooks re‑emit unchanged content each turn → ~2.3 KB waste per chat.  
- GNN heterophily predictor dim mismatch: trainer expands 768→3072 but predictor uses raw 768; eval defers forever.  
- Default heap OOMs in graph loading scripts (542 MB graph).  
- API errors due to orphaned looping chats (~19) stuck in `/goal` loops, hammering Anthropic quota.  
- Lone UTF‑16 surrogate in injected JSON caused 400 errors; fixed by `stripLoneSurrogates`.  
- Legacy injectors still wired while context‑bundle daemon is down → restarting would add tokens.  
- Byte‑estimate fallback in `chat-token-watch` over‑counted transcript (~1.86 M “tokens”) → false critical pressure and constant `/compact`; fixed with suspect guard.  
- Duplicate wirings in `settings.json` were false positives; verified before removal.  
- Some injectors lacked dedup or surrogate safety; wrapped accordingly.

**DOMAIN SPECIFICS**  
Engines/Actions:  
- `scripts/lib/transcript-token-counter.mjs`, `precompact-auto-trigger.mjs`, `token-awareness-sidecar.mjs`, `galaxy-context-retrieval.mjs`, `galaxy-dense-rerank.mjs`, `galaxy-cag-cache.mjs`, `galaxy-lora-emit.mjs`, `mine-galaxy-transcripts.mjs`, `scripts/lib/injection-dedup.mjs`, `injection-dedup-fs.mjs`, `graphsage-predictor.mjs`, `heterophily-features.mjs`, `safe-truncate.mjs`, `dedupeOrMarker`, `chat-token-watch`, `zulu-orchestrator-sweep`, `precompact-pending-guard.mjs`, `per-agent-handoff.mjs`, `stable-session-id.mjs`, `measure-injection-budget.mjs`.  

Metrics: token counts, compaction thresholds (SOFT 880 K / HARD 940 K / CAP 1 M), sidecar usage %, AUROC/macroF1/Brier for GNN, OOM heap size, injection budget savings (~21 K tokens fleet‑wide, ~2.3 KB/turn), context retention per slot.  

Paths:  
- `H:/prism/.claude/helpers/milestone-tracker.mjs`  
- `state/shared/specs/AI-SYNERGY-AUDIT.md`, `state/CURRENT_POSITION.md`  
- `state/shared/dashboards/injection-dedup-cache.json`  
- GNN checkpoint config (`model.config.heterophily`)  
- `state/shared/zulu-orchestrator-log.jsonl`  
- Slot injectors: `slot-context-bundle-inject`, `prompt-context-inject`, `slot-brief-inject`, `chat-bus-inject`, `awareness-snapshot-inject`, `master-index-precheck-inject`.

**TOOLS USED**  
systematic-debugging skill, precompact-pending-guard.mjs, per-agent-handoff.mjs + stable-session-id.mjs, mine-galaxy-transcripts.mjs, install-galaxy-mine-task.ps1, token counter lib (`scripts/lib/transcript-token-counter.mjs`), precompact trigger hook, sidecar token-awareness-sidecar.mjs, injection-dedup-fs.mjs, slim-claude-md-injection.mjs, regression-auto-write.mjs, graphsage-predictor.mjs, heterophily-features.mjs, self‑reexec heap guard pattern (`shouldReexecForHeap`, `nodeArgsWithHeap`), golf reaper dispatcher, CronCreate (job 4e514422), skills: `/loop`, `/goal`, `/precompact`, `/compact`; helpers/skills: milestone-tracker.mjs, precompact-pending-guard.mjs.

**OPEN THREADS**  
- Unblock subagents: reduce prompt size by removing CLAUDE.md log injection; enable Sonnet workflow.  
- Finalize reaper‑proof mining for all galaxies (ensure scheduled task runs/resumes).  
- Implement fleet rule “read full content, not just titles” in all relevant docs/flows.  
- Verify new compaction guard fully prevents future loops after any marker format change.  
- Complete remaining ROI tasks from synthesized transcript list (subagent unblocking, Hermes/Zulu integration).  
- Selective‑deploy promotion path for GNN pending operator approval (`PRISM_NN_SELECTIVE_PROMOTE=1`).  
- Full‑coverage GNN retrain with ref‑pool growth (India’s GPU lane).  
- Adopt injection dedup wrapper in remaining hooks (8 copy‑paste adopters).  
- Per‑galaxy deep AI improvements (owner slots).  
- Continuous monitoring of API usage to prevent future rate limits.  
- Activate Zulu orchestrator sweep opt‑in for fleet compaction; monitor after 24 h dry‑run.  
- Monitor zulu sweep graduation after 24 h; ensure live compaction starts.  
- Continue recurring loop to catch new token/efficiency regressions.  
- Other lanes: LoRA fine‑tune, 33 domain slots sync, Hermes orchestration pending.
