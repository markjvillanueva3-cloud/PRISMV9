# mill session 2110a43c (2026-06-26, 1MB, spine 6KB, 1 slice(s), model gpt-oss:20b)

**DECISIONS**  
- Removed the uncommitted `claude_design` block from `H:/prism/.mcp.json`.  
  *Why*: The URL (`https://api.anthropic.com/v1/design/mcp`) is not a real endpoint (404), so reconnection was impossible; deletion resolves the error.

**FINDINGS/BUGS**  
- `claude_design` entry pointed to a non‑existent Anthropic MCP server, causing `/mcp` reconnect failures.  
- The block was uncommitted and thus safe to delete without affecting version history.

**DOMAIN SPECIFICS**  
- Configuration file: `.mcp.json`.  
- Active servers after edit: `prism` (port 3100) and `hermes` (port 8645).  
- MCP concept used for design tool integration; Figma MCP (`mcp.figma.com/mcp`) is the available alternative.

**TOOLS USED**  
- Git (`git diff`, `git restore`) to inspect and revert local changes.  
- JSON validation to ensure file integrity after edit.  

**OPEN THREADS**  
- Potential Figma OAuth flow if the user decides to replace the dead entry with a real design MCP; currently pending user confirmation.
