# mill session c7361c9f (2026-06-10, 72.9MB, spine 495KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- ascii‑guard.mjs (commit 86b4bf86) – diff‑aware PreToolUse BLOCK for code files.  
- MCP heap floor 4 GB→24 GB & watchdog RSS threshold 3 GB→18 GB (U‑MCP‑BLACKWELL‑HEAP).  
- PRISM Zombie Reaper v2 re‑enabled; migration‑freeze cleared.  
- NIM vision‑11b container removed; GPU routing restored to Ollama best‑tier models.  
- Fleet‑reaper sweep completed, stale slots reclaimed.  
- Slot‑commit routing fully active; large wiki/memories backlog deferred.  
- U‑MCP-CONCURRENCY‑HARDEN commits (/health, request‑semaphore, watchdog defer logic) – all passes.  
- G10 auto‑re‑enable guard (watcher, stop hook, helper, test suite) live‑validated PowerShell enable.  
- G5 CLOSED‑REDUNDANT (TTL sweep of orphan boost‑stamps).  
- G1 CLOSED‑PREMISE‑CORRECTED – 22 K untracked backlog cleaned.  
- G6 SHIPPED: native Ollama probe added before Docker probe, advisory only.  
- U‑GOLF‑CRASH‑POSTMORTEM‑DIGEST – aggregates crash data, writes re‑enable ledger.  
- U‑GOLF‑TASK‑OWNER‑MAP (commit 5dc70e36bd) – deterministic WARN routing to slot owners.  
- Node heap cap for regen raised from 8 GB → 24 GB (commit 5dc70e36bd).  
- Reverse‑completeness guard on owner map (commit 13596741a4).  
- Production heap env‑tunable scripts (`deploy/start.sh`, `mcp-server/scripts/start-production.*`) – commit 52fc2bd0fe.  
- GPU‑VRAM admission guard PreToolUse Bash hook + shared lib – commit f3eb0c1c15 (33/33 tests).  
- vLLM Phase‑0 POC kit (compose file, benchmark harness, runbook) – commit 4a5ba7f59d.  
- MCP‑server hardening: 5 additive fixes + depth‑guard refinement (18 vitest cases).  
- Bravo lone‑surrogate 400 error fixed – safe‑truncate lib, slot‑soul‑inject hardened, fallback regex corrected (`c1a50b7c99`, `3495a1dc6a`).  
- BaseRegistry/Logger lightweight class added; atomic‑persist & single‑flight init (commit 62fe49af70).  
- Express.json body limit raised to 50 MB env‑overridable – silent 413 bug removed (`361725ffc2`).  
- UnhandledRejection → exit opt‑in added (`73d6fa49ea`, default off).  
- vLLM admission guard config complete; `PRISM_VRAM_GUARD_MODE=block` knob wired.  
- Fleet reapers running, zombie cleanup performed (~10 orphaned node.exe + 1 git).

---

**DECISIONS**  
- Diff‑aware ascii guard to block new smart punctuation while preserving existing lines.  
- Raise MCP heap floor 4 GB→24 GB & watchdog RSS threshold 3 GB→18 GB to leverage 136 GB RAM and reduce disconnects.  
- Lift migration freeze after PC upgrade; re‑enable Zombie Reaper v2, allow all monitors.  
- Remove redundant NIM containers; restore GPU routing to Ollama best‑tier models.  
- Defer large knowledge‑base commits to avoid race with active peers.  
- Adopt 3‑of‑3 scrutiny gate for safety and rate‑limit avoidance.  
- Bounded concurrency ≤3 agents for ultracode workflows (prevent double failures).  
- Force‑take golf slot on startup, run `/fleet-reaper` before standard pipeline; golf slot owns reaper.  
- Treat G5 redundant; revert synthesis‑cron names in `KNOWN_PRISM_TASKS` to avoid false WARN.  
- Remove `isMigrationFreezeActive()` gate in G10; align re‑enable logic with `aggregateHealth` crash‑critical semantics.  
- Probe native Ollama (`:11434`) before Docker probe to avoid suppression when Docker down.  
- Keep fleet hook heap caps unchanged; raise regen chain cap only per commit‑pressure policy.  
- Route WARN advisories via static `TASK_OWNER_DOMAIN` map (U‑GOLF‑TASK‑OWNER‑MAP).  
- Build GPU‑VRAM admission guard first, then generate vLLM migration plan via ultracode/Ollama.  
- Keep reapers/monitors active; bounded autonomous reap‑check each cycle.  
- Warn fleet of GPU contention risk before proceeding with vLLM work.  
- Do not restart Docker Desktop automatically; operator handles image‑manifest corruption.  
- Prioritize Bravo P0 → MCP hardening → BaseRegistry/Logger → vLLM guard.  
- Adopt pathspec commits to avoid shared‑index race.  
- Make body limit and unhandledRejection opt‑in env‑overridable defaults.

---

**OPERATOR DIRECTIVES**  
- Start Docker, Qdrant, WSL, Ollama, NVIDIA NIM; ensure fleet‑reaper & monitors active.  
- Verify Ollama model roster vs hardware; assess NIM necessity.  
- Add age stamp to stop‑hook advisories.  
- Restore fleet reapers after PC upgrade; run `/startup-golf` (force‑take golf slot → `/fleet-reaper` → standard startup).  
- Return to git‑tree work: confirm slot‑branch routing, fix conflicts/backlog; enforce advisory rules (em‑dashes, ASCII issues).  
- Utilize Ollama for task efficiency and grunt work in ultracode loops.  
- Build for Blackwell hardware (RTX 6000 96 GB / 9950X3D / 136 GB RAM); use ultracode/Ollama for vLLM plan generation.  
- Run precompact, then build GPU‑VRAM admission guard; re‑register stale Blueprint OCR Batch task.  
- Keep fleet reapers running; perform bounded autonomous reap‑checks and report deltas (killed counts, freed MB, commit % change).  
- Warn fleet of GPU contention risk before proceeding with vLLM work.  
- Do not restart Docker Desktop automatically; operator handles image‑manifest corruption.

---

**FINDINGS/BUGS**  
- NIM vision‑11b crashed due to GPU memory → container removed.  
- Offload routing prioritized NIM, downgrading Ollama best‑tier models.  
- ascii‑guard advisory orphaned; no enforcement of smart punctuation.  
- MCP watchdog RSS threshold 3 GB too low → frequent disconnects; concurrency gap: fresh McpServer per request spikes memory; watchdog not concurrency‑aware.  
- Migration freeze prevented reapers/monitors from starting after PC upgrade.  
- Git tree had ~14k uncommitted files (13,995) and 22K untracked backlog (G1).  
- Semaphore leak bug: client disconnect while queued caused abandoned slot; fixed in `acquireRequestSlot`.  
- G10 freeze‑gate suppressed crash‑critical task re‑enable; bug fixed.  
- `merge‑augmentations.mjs` fails at 630 MB even with 24 GB heap → separate merge bug flagged to Sierra.  
- Recurring stuck Git (~49 every ~20 min) due to shared‑tree `index.lock`.  
- MCP daemon pileup (4 duplicates) resolved via `singleton-service-guard --fix`; root cause supervisor spawn under load.  
- False‑positive in `stop_on_unwired_assets` due to global flag `PRISM_ALLOW_UNWIRED=1`; must be removed for enforcement.  
- 4096 MB heap caps hard‑coded; now env‑overridable (deploy/start.sh, mcp-server/scripts/start-production.*).  
- GPU monitoring shows no current admission guard; GPU‑VRAM guard pending.  
- Docker image pull for `vllm/vllm-openai:latest` exits 0 but never commits image → requires Docker Desktop restart.  
- BaseRegistry missing Logger class; new Logger throws “Logger is not a constructor”.  
- Express.json default 100 KB limit silently rejected large dispatcher payloads.  
- UnhandledRejection default log‑only; made opt‑in exit.  
- vLLM 80B‑fp8 needs ~83 GB VRAM → cannot coexist without guard block mode.

---

**DOMAIN SPECIFICS**  
- **Fleet‑reaper sweep** (`fleet-reaper-sweep.mjs`) – orphan process monitor, GPU stats, Ollama routing hints.  
- **Aggressive‑killer** (`06-aggressive-killer.mjs`) – clears zombie node.exe/git/bash orphans.  
- **Task health watch** (`fleet-task-health-watch.mjs` / `fleet-task-health-stop.mjs`) – reports reaper & monitor status, degraded tasks.  
- **Slot task claim list** (`slot-task-claim.mjs`) – tracks cross‑chat unit claims.  
- **MCP server** – `request-semaphore.ts`, watchdog defer logic, depth guard (slimResponse), bridgeError, EventBus.handlerTimes trimming; per-request McpServer instances.  
- **Active-chat-priority-decay/boost** modules – orphan stamp cleanup and TTL sweep.  
- **Local‑llm‑bridge.mjs** – NIM first cascade routing; `nim-docker-launcher.mjs` launches NVIDIA NIM containers.  
- **Ollama-cost-router.mjs** – routes best‑tier models.  
- **ascii-guard.mjs** – PreToolUse BLOCK enforcement on code files.  
- **gpu-vram-admission-guard.mjs** (PreToolUse Bash hook) + shared lib (`scripts/lib/gpu-vram-guard.mjs`).  
- **BaseRegistry/Logger** class added; atomic‑persist & single‑flight init.  
- **Express.json body limit** raised to 50 MB env‑overridable; unhandledRejection opt‑in.  
- **Cron e12cf005** – 30‑min hygiene loop (zombie sweep + reaper check).  
- **vLLM admission guard** config with `PRISM_VRAM_GUARD_MODE` knob.

---

**TOOLS USED**  
- PRISM skills: `/startup-golf`, `/fleet-reaper`, `CronCreate`, `CronDelete`.  
- `/checkin-golf` script (slot claim/reclaim).  
- `chat-slots.mjs` helpers (reclaim/claim).  
- `fleet-reaper-sweep.mjs`, `06-aggressive-killer.mjs`, `fleet-task-health-watch.mjs`.  
- `nim-docker-launcher.mjs`, `local-llm-bridge.mjs`, `ollama-cost-router.mjs`.  
- `ascii-guard.mjs`, `gpu-vram-admission-guard.mjs` (PreToolUse Bash hook), `scripts/lib/gpu-vram-guard.mjs`.  
- MCP server scripts: supervisor, watchdog, singleton-service-guard, request‑semaphore.ts.  
- Settings.json hooks (PreToolUse).  
- Git tooling for routing and conflict resolution.  
- Ultracode workflow orchestration.  
- Ollama LLM (`gpt-oss:120b`, `qwen2.5-coder:32b`) for deep reads & summarization.  
- Node testing frameworks: vitest, node:test, tsc --noEmit.  
- Cron scheduler e12cf005.  
- Docker Compose config/pull; Docker Desktop restart handling.  
- Agent coordination broadcast (`agent-coordination.mjs post`).  
- fxtwitter API for X posts.

---

**OPEN THREADS**  
- Inflight‑aware watchdog to defer restarts during bursts.  
- Concurrency cap/queue on `buildRequestServer()` to bound memory spikes.  
- Full audit of advisory‑to‑enforce rules (ascii‑guard, stop‑hook advisories).  
- Finalize MCP gap fills; validate and deploy updated MCP heap/watchdog config.  
- Remaining golf units: G9‑rest, G6 (docker probe), G2 (gpt‑oss smoke test), G8 (eval verify), G4 (70‑worktree consolidation).  
- Root cause of recurring disabling of crash‑critical tasks.  
- `merge‑augmentations` bug in regen chain.  
- GPU‑VRAM admission guard pending after `/compact`; vLLM plan generation.  
- Wiring 89 unwired engines / 26 bridges.  
- Re‑register Blueprint OCR Batch task.  
- Remove `PRISM_ALLOW_UNWIRED=1` flag.  
- Operator must restart Docker Desktop for image‑manifest corruption.  
- Resolve BaseRegistry Logger stub breakage.  
- Final decision on vLLM go/no‑go after measuring fp8 footprint (~83 GB VRAM).  
- Continue fleet‑hygiene monitoring (zombie sweep, reaper status).
