# mill session db273e77 (2026-06-09, 93.8MB, spine 779KB, 9 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e5ad4ea802` – HW‑retune: CPU/GPU config, Vitest 4 upgrade; 9 tracked files (vitest config, batch‑extractor engines & tests, GPU health script).  
- `873446a147` – Alpha quality galaxy claim; CLAUDE.md §2/5/6/7 populated, MEMORY.md ownership note.  
- `1443283f8b` – 12 MIT‑AI textbooks registered in `pdf-sources/registry.json`; added `ai_textbook` category.  
- `3935f8d4ad` – Added `topics/source/note` to `PDFSourceMetadata`, updated tests.  
- `632335cec6` – Goal‑clear → next‑unit fallback hook (`U-GOAL-CLEAR-ADVANCE`).  
- `c83ca9be64` – F5 autoresume staleness fix: `DEFAULT_MAX_AGE_MIN 240→720`; boot‑stale hint.  
- `8cd8d615e9` – `slot-domain-awareness-inject.mjs` dedup (token savings).  
- `eefef0359a` – `psn-leg-state-inject.mjs` dedup.  
- `1f295f51e6`, `8d2b6cf8e0`, `87e96fa485`, `afe169ab0e` – shared `dedupedContext()` helper + 8 per‑prompt injectors (≈5–12 K tokens/slot/session).  
- `636d36bf59`, `75c44d8412` – F3 semantic memory recall with self‑refresh cache; obsidian fully wired, vault value.  
- `HIGHVALUE-DISCOVERY-2026-06-08.md` – ultracode discovery deliverable (5 agents, ~1.04 M tokens).  
- `792beb75e8` – `resolveObsidianMemDir()` single‑source resolver; removed 15,819 dangling vault links from `extractWikilinks`.  
- `e1b95b05a8` – wired canonical C:→H: feed through resolver.  
- `d579626848` – removed 15,819 dangling vault links (duplicate of above).  
- `a9052f63b0` – backendAudit route‑suggest session‑gate; serialized WikiRecallCounter RMW with exclusive lock; excluded node_* pointer stubs; routed hot hooks through resolver.  
- `01c3b15f56` – serialized WikiRecallCounter RMW (duplicate).  
- `8da1541c00` – excluded 9,571 node_* pointer stubs from memory recall.  
- `b45f08e732` – routed four hot hooks via resolver.  
- `Q11` – revived & semantically‑ranked memory→wiki promotion advisor; per‑memo dedup.  
- `Q9` – dream‑cycle LLM rationale pass applied to live vault; promoted to nightly cron (`install-hermes-dream-cycle-task.ps1`).  
- `Q10` – weekly‑synthesis optional local‑LLM theme pass (default OFF).  
- `Q14` – backfilled alias‑preserving `[[wikilinks]]` into 236 canonical memos (409 links added).  
- `e80e6e3a41` – Vision-model single‑source (`VISION_FAMILY_LEADERS`); removed orphaned import; 175 vision tests pass.  
- `89146678bf` – Embed‑progress honesty fix; marker reports “stale”.  
- `415941b1f0` – Rtk‑guard false‑positive removal (`isAlreadyRtk`).  
- `2999dac687` – Final synthesis report (`state/shared/specs/GOAL-DISCOVERY-SYNTHESIS-2026_06_09.md`).  
- `58688be398` – System‑viz OOM findings (both `find` and `node-card` paths).  
- `U-OBS-GALAXY-BRAIN-RECALL + P2` – 34 galaxy‑brain domains wired into semantic recall.  
- `R1-C1` – disabled `pre-tool-router-table-advise.mjs` (82 K token savings).  
- `R1-C2` – session‑once gate for `node-no-rtk-wrap` nudge (~159 K tokens saved).  
- `R4-C1` – phantom‑link filter in `knowledge-link-audit` (15.5 % false positives removed, 7,886 broken links).  
- `R3-C1` – vault‑memo semantic recall (304 H‑only memos embedded).  
- `U-OBS-BRAIN-LOCK-RECLAIM` – fixed corrupt `.brain-refresh.lock`.  
- `R5-C1` – slot-context-bundle dedup (~1,056 tokens per repeat).

**DECISIONS**  
- Keep 47 migration‑tasks disabled (operator hold).  
- Do not start Docker/NIM; audit only until operator clears.  
- Apply safe retunes: `OLLAMA_MAX_LOADED=6`, `FLASH_ATTENTION=1`, `PRISM_EMBED_CONCURRENCY=16`.  
- Resolve Vitest 4 regression by moving `poolOptions.threads` to top‑level `test.*`.  
- Leave `stop_on_failing_tests.mjs` unwired (fleet‑wide fail‑closed hook would block all chats).  
- Use single uninterrupted background pull for `gpt‑oss:120b / gemma4:31b`; do not kill/restart.  
- Commit via commit‑mutex (`U-FGC-1`) to respect lane discipline.  
- Alpha now owns the quality cross‑galaxy gate; keep model pull in golf’s durable lane (`ollama-resilient-pull.ps1`).  
- Skip MIT‑textbook corpus ingestion (lima/academy responsibility).  
- Adopt fleet‑fallback in `loop-state.mjs`: own‑domain units first, then fallback to fleet.  
- Implement `stop-goal-clear-advance` hook to auto‑continue after goal clear; use `process.execPath`.  
- Use env override `PRISM_GOAL_CLEAR_ADVANCE_SLOTS_JSON` for hermetic testing.  
- Adopt single‑source homedir resolver to eliminate split‑brain C:↔H: feeds.  
- Exclude auto‑generated node_* stubs from recall; serialize RMW on `wiki-recall-counts.json`.  
- Gate duplicate backend audit messages per session (~8,400 tokens saved).  
- Use ultracode discovery workflow; accept transient API rate limits and re‑run when quota recovers.  
- Adopt local‑LLM (Blackwell qwen2.5‑coder) for vault‑value enhancements ($0 Claude cost).  
- Wire resolver into every consumer deriving `PRISM_OBSIDIAN_MEM_DIR`.

**OPERATOR DIRECTIVES**  
- Perform model pull (`gpt‑oss:120b`, `gemma4:31b`); re‑run if stalled.  
- Read X posts via Playwright/API; verify system setup against prescriptions.  
- Take over golf’s model pull infra; claim quality galaxy.  
- Verify MIT textbook corpus coverage per article 2.  
- Complete ultracode workflow and confirm system matches all three X posts.  
- Monitor background pull (`bittzospv`) for completion; resume when finished.  
- Use ultracode, hermes‑workflow, system‑viz, obsidian app & PSN to find high‑value token savings, context retention/expansion; fully wire obsidian to H drive; enhance vault usage/value.  
- Ensure all galaxies and chat slots fall back to remaining tasks/units when goal clear.  
- Use Ollama to improve task efficiency; switch coder LLM if possible; set highest‑optimized defaults per task; coordinate with bravo.  
- Re‑register scheduled tasks (dream cycle, zombie‑reaper, blueprint‑OCR) via elevated PowerShell.

**FINDINGS / BUGS**  
- Vitest 4 removed `poolOptions.threads`; regression fixed by moving to top‑level config.  
- `soft-config` had mojibake em‑dash → PowerShell parse error; resolved with UTF‑8 flag.  
- GPU health instructions referenced cu129; corrected to cu128, added reconciliation note.  
- Pull stalled at ~93 % due to network throttling; partials preserved but not resuming (restart re‑downloads layer).  
- `stop_on_failing_tests.mjs` exists on disk but unwired in settings.json / bundles.  
- Docker/NIM config Blackwell‑aligned; no P0.  
- Doc‑drift cu129 vs cu128 resolved with banner note.  
- `enforce-eslint-after-edit.py` had hard‑coded path & wrong config filename; fixed but remains unwired.  
- Missing file:line citation doctrine (no hook).  
- Ask-before-add-deps exists only as a doctrine line in CLAUDE.md; no gate.  
- MIT textbook corpus not present in PRISM’s content layer.  
- Corrupted `gemma4` partial from earlier pull attempt cleared for fresh resume.  
- Memory pressure 96–98 % commit charge (dominated by vmmemWSL); mitigated by killing zombie TS servers.  
- MCP server down due to memory pressure; fixed by restarting supervisor/server.  
- ENOENT bug in hooks using `"node"` → resolved with `process.execPath`.  
- Settings.json drift: tracked file behind runtime; corrected by committing only hook entry.  
- Dangling‑link generator emitted ~15,819 broken links (67 % of all).  
- 9,571 node_* pointer stubs polluted recall corpus, inflating BM25 scores and causing 9,571 needless stat+read calls.  
- Lost‑update race on `wiki-recall-counts.json` under the 26-chat fleet.  
- Backend audit route‑suggest fired byte‑identically on every backend edit (~8,400 tokens/session).  
- Galaxy brains excluded from semantic recall; never surfaced in natural‑language queries.  
- System‑viz node-card OOM on current PC (both paths).  
- Duplicate MCP daemon detected; potential RAM waste.  
- Scheduled-task health degraded to 44/50; requires operator action.

**DOMAIN SPECIFICS**  
Engines / Actions:  
- `MINBatchExtractorEngine.ts`, `McxBatchExtractorEngine.ts`, `GpuStackHealthEngine.ts`  
- `CpkPredictionGateEngine`, `SPCProcessCapabilityEngine`  
- PDF engines: `PDFSourceRegistryEngine`, `PDFFormulaExtractionEngine`, `PDFHandbookBatchProcessorEngine`, `PDFMaterialPropertyExtractionEngine`, `PDFTableExtractionEngine`  
- `memory-index-search-lib.mjs`, `resolveObsidianMemDir()`, `withExclusiveLock()`  
- Hooks/Dispatchers: `stop-goal-clear-advance.mjs`, `loop-state.mjs`, `loop-iteration-inject.mjs`, `stop-force-loop-continue.mjs`, `slot-domain-awareness-inject.mjs`, `psn-leg-state-inject.mjs`, `backendAudit route‑suggest` (session‑gate), `node-no-rtk-wrap`, `knowledge-link-audit`, `brain-refresh.mjs`, `slot-context-bundle-inject`, `precompact-memo-emit.mjs`, `stop-auto-capture-per-slot.mjs`, `stop-memory-size-watchdog.mjs`, `memory-rag-inject.mjs`, `wiki-promo-rerank.mjs`  
Metrics / Paths:  
- Token savings: 82–95 % via dedup; ~4–6 K tokens/slot/session; 159 K tokens saved by node-no-rtk-wrap gate; 1,056 tokens per repeat for slot-context-bundle dedup; 8,400 tokens per session for backend audit route‑suggest; 15.5 % false positives removed (7,886 broken links); 15,819 dangling vault links removed.  
- Memory pressure: 96–98 % commit charge; MCP server memory issue.  
- Vision tests: 175 pass.  
- Pull progress ~93 %.  
- OOM in system‑viz node-card path.  
Paths: `H:/prism/.claude`, `H:/prism/knowledge/memories`, `C:/Users/wompu/.claude/projects/H--prism/memory`, `H:/prism/.claude/settings.json`, `state/shared/*.md`, `mcp-server/src/__tests__/…`, `scripts/lib/injection-dedup.mjs`, `scripts/lib/memo-embed-lib.mjs`, `scripts/lib/obsidian-mem-dir.mjs`, `scripts/lib/exclusive-file-lock.mjs`, `obsidian-memory-sync.mjs`.

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `audit-roadmap-drift.mjs`, `stop-goal-clear-advance.mjs`.  
- Commit‑mutex (`U-FGC-1`).  
- Ollama CLI (via correct binary path).  
- fxtwitter API for X article extraction.  
- Playwright.  
- Ultracode workflow engine (background subagents).  
- Hermes workflow scripts (`hermes-dream-cycle-task.ps1`, `weekly-memory-synthesis.mjs`).  
- System‑viz hooks.  
- Obsidian app integration.  
- PSN.  
- nomic‑embed‑text.  
- Git with scoped pathspecs, 3‑of‑3 scrutiny gate.  
- Node portable binary (`process.execPath`).  
- Scripts: `scripts/lib/injection-dedup.mjs`, `scripts/lib/memo-embed-lib.mjs`, `scripts/lib/obsidian-mem-dir.mjs`, `scripts/lib/exclusive-file-lock.mjs`.

**OPEN THREADS**  
- Operator‑gated re‑registration of scheduled tasks (dream cycle, zombie‑reaper, blueprint‑OCR).  
- Hermes‑Obsidian bridge repoint pending operator confirmation.  
- Weekly‑synthesis cron opt‑in not yet enabled.  
- Remaining ultracode discovery queue items: embed‑stub filter, isLargeRead gate, etc., queued for next cron.  
- Coordination with bravo on Ollama model routing (U1–U7 plan) to finalize per‑task defaults; verify new autoexec flag once bravo ships U5.  
- Wire semantic recall into `memory-rag-inject.mjs` to enable galaxy brain usage in natural‑language queries.  
- Fine‑tune galaxy-tier floor; confirm precision/recall trade‑off across all consumers.  
- Validate that resolver path is exercised by all remaining legacy hooks not yet routed.  
- Address system‑viz OOM fix and duplicate MCP daemon cleanup (future improvements).  
- Tier‑4 items: node_* pointer stub recall exclusion, precompact seed‑refresh queued for next session.
