# mill session 2993382b (2026-06-25, 14.4MB, spine 150KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑OSC‑SFC‑PRODUCT‑BRIDGE`: added machine‑bridge helper, fixed product dispatcher P0 bug.  
- `U‑OSC‑SFC‑GAUNTLET‑DURABLE‑TASK`: Windows‑task installer for SFC accuracy‑refresh cron.  
- `U‑OSC‑SFC‑DEFLECTION‑WIRE`: route bridge `/deflection` (stickout → overhang_length).  
- `U‑OSC‑SFC‑CYCLE‑TIME‑WIRE`: mapped cycle‑time fields to backend schema.  
- Commit c941a18243: mill stock‑geometry validation (`sfc-stock-damping-sensitivity-probe.mjs`) – 192 combos, 140 thin‑wall clamps, 0 invariant violations.  
- Commit ba2f6d603b: lathe stock‑geometry validation – 42 combos, 11 G50‑capped cases, 0 violations.  
- GPU closed‑loop training job (`gpt‑oss:20b` on RTX Blackwell, 12.9 GB VRAM); CPU sweep ~19 node procs.

**DECISIONS**  
- Drop frontend/mobile builds; focus backend accuracy after Sandvik rename.  
- Centralize machine‑field bridging via `applySfcMachineBridge`; use route‑scoped bridges for deflection & cycle‑time.  
- Wire live SFC calculator (`ProductEngine.sfcCalculate`) to rich `SpeedFeedNineAxisOrchestratorEngine` (full coupled force model).  
- Use single general engine with machine‑specific profiles; no per‑machine custom engines.  
- Compose physics pipeline: forces → deflection/RCSA → chatter → spindle limits → thermal.  
- Capture pre‑wire baseline of `ProductEngine` outputs in fresh `/checkin-oscar`.  
- Scale variability sweep to 32 workers; fix telemetry bug (`total_processed:0`).

**OPERATOR DIRECTIVES**  
- Run closed‑loop training and max‑combination tests on RTX Blackwell GPU + CPU to full capacity.  
- Ensure accurate data for all machines, spindles, controllers, build quality, materials, stock dimensions, fixturing, workholding, tooling, tool holder, tool path, desired output, optimization settings.  
- Verify 7.8 M combo sweep covers all logical combinations; include stock geometry/damping variations (currently default 100×100×50).  
- Perform deep assessment of force‑physics engines to decide on custom per‑machine engines.

**FINDINGS/BUGS**  
- SFC web calculator blocked by `pre-machine-completeness-gate`; frontend sends flat machine fields, product dispatcher never bridged.  
- `/deflection` endpoint mismatch: stickout vs. overhang_length; `/cycle-time` field mismatch (cut_length×num_passes → cutting_distance).  
- `power-torque` & `tool-life` endpoints blocked by machine‑completeness gate despite being machine‑independent.  
- Default‑H material assumption causes aggressive flags; material‑resolution join infeasible due to key mismatches.  
- Closed‑loop variability sweep batch dead: only 4 workers, ~1 k combos/5 min, GPU idle; scheduled batch exits immediately (parent not waiting for worker exit codes).  
- Workers killed by Task Scheduler job object teardown; direct run survives.  
- Cosmetic telemetry bug: `total_processed` reported as 0 due to JSON parsing error (`split("\n").pop()`).  
- Live SFC calculator uses inline `calculateSpeedFeed`; rich orchestrator orphaned from live path.  
- 7.8 M sweep does not vary stock geometry/damping; all combos on default 100×100×50.

**DOMAIN SPECIFICS**  
- Engines: SFC engine, SpeedFeedOrchestratorEngine, SpeedFeedNineAxisOrchestratorEngine, UltimateSpeedFeedEngine, ProductEngine.  
- Actions/dispatchers: `productDispatcher`, `calcDispatcher`, `preMachineCompletenessGate` (MachineValidationHooks.ts), routes/sfc.ts → prism_product:sfc_calculate → ProductEngine.sfcCalculate; batch‑run orchestrated by `sfc-variability-batch-run.mjs`; resume guard (`sfc-variability-resume-guard.mjs`); launcher (`sfc-variability-launch.mjs`).  
- Metrics: cycle time, power torque, tool life, deflection, surface finish; combos processed (192/42/7.8M), worker count (default 4 → target 32), chunk size 50 default, max minutes 600 default.  
- Paths: `/api/v1/sfc/*`; `sfc‑variability‑batch-run.mjs`, `install-sfc-gauntlet-task.ps1`; galaxy paths `H:\prism-slot-oscar\sfc-variability-*`.

**TOOLS USED**  
- PRISM utilities: `sfcMachineBridge.ts`, `applySfcMachineBridge`, `productDispatcher.ts`, `calcDispatcher.ts`.  
- Hooks: `pre-machine-completeness-gate` (MachineValidationHooks.ts), hookExecutor.  
- Test harnesses: `sfcMachineBridge.test.ts`, etc.  
- Scripts: `install-sfc-gauntlet-task.ps1`, `sfc-variability-batch-run.mjs`, `sfc-variability-enumerate.mjs`, `sfc-variability-resume-guard.mjs`, `sfc-variability-launch.mjs`.  
- Orchestrator engines.  
- Closed‑loop training via Ollama (`gpt‑oss:20b`).  
- Hermes agents, Ultracode workflow, Sonnet.

**OPEN THREADS**  
- Finalize gate scoping for `power-torque`/`tool-life` (fleet safety review).  
- Wire backend to new Claude‑desktop‑app frontend once design location provided.  
- Resolve dead variability sweep batch; run full closed‑loop training and sweep at full RTX Blackwell + CPU capacity.  
- Material‑resolution join feasibility remains uncertain.  
- Wire live SFC calculator to rich orchestrator (physics‑reviewer required).  
- Capture pre‑wire baseline of `ProductEngine` outputs before wiring change.  
- Add RCSA series‑stiffness chain & lathe workpiece overhang into force model.  
- Implement kinematic‑config input for table/head/combination motion; add multi‑axis force projection.  
- Scale variability sweep to 32 workers and fix telemetry bug (`total_processed`).  
- Validate remaining physics engines (chatter, thermal, wear) correctly composed in new pipeline.
