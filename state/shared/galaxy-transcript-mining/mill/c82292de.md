# mill session c82292de (2026-06-25, 36.7MB, spine 345KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `55cf3dd18d` – U-BPA-LORA-PAIRS-WIRE (de‑orphaned, wired to both dispatchers, added empty:true flag, clone‑don’t‑fork logic, tests)  
- `4fa054801a` – Wiki entry on LoRA pairs wire unit  
- `10735ad466` – U-QP-SIMILAR-JOB-RETRIEVE (thin KNN wrapper, dispatcher case, schema entry, round‑trip tests; closes India backlog #5)  
- `d5ff9fdf90` – GNN active‑worklist labeling unit U-GNN-VAULT-WIRE-LABELS  
- `5114e25fc0` – handoff commit for India loop  
- `d90e92c530` – diagnostic commit on stale NN‑Graph Retrain  
- `80b36e5358` – U-BPA-CONSUMER-STATE-ISOLATE (isolated consumer state file, idempotent blueprint‑accuracy consumer)  
- `2f163d92df` – U-BPA-DOCFRESH (updated consumer & hook docs)  
- `da9f7cc3cd` – U-BPA-LOOP-DRAIN-CORE (injectable drain core with skip logic; P1 fix for outcome_record)  
- `179ca3d723` – U-BPA-DRAIN-NOOP-COUNT (corrected counter to distinguish real vs noop dispatches)  
- `1f7d03f33d` – U-BPA-LOOP-DRAIN-DISPATCH (`prism_ai:blueprint_loop_drain`, routes learning primitives, skips outcome_record)  
- `4b0aa55769` – U-GNN-FEEDER-SPECULATIVE-WARN (fail‑loud on SPECULATIVE_RE drop; 31/31 tests)  
- `b025c6ee11` – U-GNN-RECOVERED-LABELS (recovered two ground‑truth labels; feeder count 25→27)  
- `3b8ff98b2c` – U-GNN-FEEDER-WARN-SIGNAL-CLEAN (removed self‑flag from SBOM example; 3 remaining WARNs legitimate)

**DECISIONS**  
- De‑orphaned `buildLoRAPairsFromLedger`, wired to both dispatchers, added empty:true flag.  
- Implemented clone‑don’t‑fork logic for `prism_ai` dispatcher.  
- Built thin wrapper U-QP-SIMILAR-JOB-RETRIEVE over KNN with dispatcher & schema wiring.  
- Adopt stacked build pipeline: mechanical code → Ollama coders (qwen3‑coder:30b, qwen2.5‑coder) → Hermes/Forge agents → Octopus consensus → Opus safety design.  
- Use YELLOW checkpoint + auto‑compact; never bypass RED account‑switch gate.  
- When 5 h quota critical or context YELLOW, checkpoint and stop rather than start new unit.  
- Ground‑truth GNN active‑worklist ghosts (11 labels) via `vault-to-gnn-refpool.mjs` to move ref‑pool toward macro‑F1 gate 0.55.  
- Confirmed blueprint LoRA/RAG prediction→outcome→retrain loop closed; no further work needed on that surface.  
- Split consumer & hook state into separate files to avoid non‑idempotent reprocessing.  
- Skip `outcome_record` in dispatcher; keep ledger‑only.  
- Defer large dispatcher edits until risk analysis and build‑gate (`npm run build:fast`).  
- At YELLOW context, perform checkpoint via auto‑compact per directive.

**OPERATOR DIRECTIVES**  
- `/checkin-india /loop /goal` – force‑take India slot, run full check‑in pipeline.  
- Trigger GNN GPU retrain or clear/compact to reset budget after YELLOW checkpoint.  
- `node scripts/capture-claude-credentials.mjs account-N` (capture current login).  
- `node scripts/fleet-survival-status.mjs` – verify GREEN status.  
- `node scripts/arm-account-switch.mjs --auto` – arm RED‑gated switch.  
- `/clear` or `/compact` (manual if auto fails; native auto‑compact at ~95%).  
- `nn-graph-retrain-lifecycle.mjs --force` – apply all 27 vault labels + feeder, re‑grade macro‑F1.  
- `node scripts/fleet-reaper-sweep.mjs`, `audit-unwired-engines.mjs`, `self-compact.mjs`.

**FINDINGS/BUGS**  
- TS2554 error in `ReinforcementLearningCAMFeedbackEngine.step()` (missing 5th outcome).  
- Clone‑don’t‑fork violation on `prism_ai` dispatcher.  
- Silent empty‑set footgun when ledger returns zero pairs.  
- Missing wiring for `buildLoRAPairsFromLedger`.  
- Self‑compact YELLOW path never actuates; native auto‑compact at ~95% will reset.  
- Manual `/compact` blocked by crossroad hook; native auto‑compact will fire.  
- SPECULATIVE_RE filter in `vault-to-gnn-refpool.mjs` silently dropped two wired lines; fixed by scrubbing words and exposing `speculativeSkipped`.  
- 06‑18 mass‑dump rejection of 3206 code‑base labels proved only sparse, balanced feeders (≤ +3/class) safe; current +11 feeder compliant.  
- Hook & consumer wrote same `blueprint-accuracy-state.json`, causing duplicate processing; split state files.  
- Dispatcher miscounted noop dispatches as real; added `dispatchedNoop` counter.  
- Body‑carrying case inserted into fragile bare‑fall‑through block in `aiReasoningDispatcher`; risk of silent loss ~115 actions.  
- 5 h account‑quota limit will block fleet; requires operator action to arm switch.  
- Auto‑compact SendKeys harness cannot trigger `/compact` from this slot (known limitation).  
- 9 canonical machining‑tactics wiki files deleted during shared‑tree contention; restored from HEAD.

**DOMAIN SPECIFICS**  
- Engines/dispatchers: `blueprint_lora_prepare_set`, `cadDispatcher(recordOutcome)`, `prism_ai`, `quoting`, `aiReasoningDispatcher(routeXprocAction)`, `vault-to-gnn-refpool.mjs`, `blueprint-lora-pair-builder.mjs`.  
- Metrics: token budget YELLOW ≈0.66; auto‑compact at ≈0.95; macro‑F1 gate 0.55; AUROC 0.846; GNN ref‑pool growth 179→190 after labeling; feeder count 25→27; CPU%, RAM usage, node/claude process counts.  
- Paths: `src/data/controller-dialects/<vendor>.ts`, `src/physics/constants.ts`, `state/shared/blueprint-accuracy-state.json` (hook), `state/shared/blueprint-accuracy-consumer-state.json` (consumer), `state/shared/nn-graph/active-label-worklist.{json,md}`, `state/shared/blueprint-accuracy-events.jsonl`, `state/shared/gnn-refpool/...`.  
- Special hooks: `prism_ai:consensus_decide`, `node scripts/fleet-survival-status.mjs`, `node scripts/self-compact.mjs`.  
- GNN retrain status: AUROC 0.846, macro‑F1 < gate; ref‑pool pending.

**TOOLS USED**  
- PRISM helpers: `ask-ollama.mjs` (qwen3‑coder:30b, qwen2.5‑coder), `ask-hermes.mjs`, `audit-unwired-engines.mjs`, `fleet-survival-status.mjs`, `self-compact.mjs`, `fleet-reaper-sweep.mjs`, `vault-to-gnn-refpool.mjs`, `blueprint-lora-pair-builder.mjs`, `nn-graph-retrain-lifecycle.mjs`.  
- Skills/agents: Sonnet, qwen3‑coder:30b, qwen2.5‑coder, hermes/forge-team Agents, Octopus consensus (`prism_ai:consensus_decide`), Opus.  
- Node scripts: `capture-claude-credentials.mjs`, `arm-account-switch.mjs`, `audit-unwired-engines.mjs`, `self-compact.mjs`, `fleet-survival-status.mjs`, `fleet-reaper-sweep.mjs`, `nn-graph-retrain-lifecycle.mjs`.  
- Build tooling: `npm run build:fast`, TypeScript compiler, Vitest.

**OPEN THREADS**  
- Operator must greenlight stale NN‑Graph Retrain GPU task (apply +11 labels, re‑grade macro‑F1).  
- After retraining, schedule `blueprint_loop_drain` dispatcher action for autonomous cron (operator/golf‑gated).  
- Complete GNN ref‑pool GPU retrain once sufficient labels collected.  
- Resolve 5 h account‑quota block by arming account switch (operator action).  
- Monitor and maintain NN‑Graph/Tribal‑Embed stale warnings; ensure background tasks healthy.  
- Coordinate with Bravo on `U-NN-TIER05 / XPROC-NEURAL-OPTIMIZE-MS0` milestone; no unilateral build allowed.  
- After auto‑compact reset, resume India loop: next high‑ROI unit (align blueprint‑accuracy‑guard or wire `blueprint_rag_extract recordOutcome IO`).  
- Monitor for new silent‑drop bugs in other feeders; maintain R12 fail‑loud policy.
