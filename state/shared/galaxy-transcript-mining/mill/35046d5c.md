# mill session 35046d5c (2026-06-24, 10.2MB, spine 99KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `5e4c9158ac`: GIGO‑safe CAD/CAM tribal feeder + sidecar overrides.  
- Commit `a95356c003`: `build-domain-knowledge-feeders.mjs` – generates per‑domain feeders for all manufacturing domains.  
- Commit `5d865b0301`: rewrote feeder to canonical path, added ownership guard, excluded cad/cam, 11/11 tests passed.  
- Removed stale cron `d946b614` from scheduled tasks.

**DECISIONS**  
- Commit new feeder code via `[MAIN‑FORCE]`; lane guard allows infra commits.  
- Route CAD/CAM knowledge to xray/kilo/delta/india; other domain feeders to their slots.  
- Keep Hermes utilization as real work (`ask-hermes`).  
- Do not run full Vitest suite on live tree; run on clean CI instead.  
- Conform feeder output to `state/shared/<domain>-tribal-corpus.jsonl`.  
- Add `weOwnCorpus()` guard to avoid clobbering dedicated corpora.  
- Exclude cad/cam from generic feeder; retain existing generators.  
- Rely on scheduled cadence for fragile tribal embedder.  
- Persist India’s `getDomainCorpus` via durable chat‑bus work‑request.

**OPERATOR DIRECTIVES**  
- Apply CAD/CAM feeder logic to all manufacturing domains.  
- Finalize and commit verified code (“continue” / “do it”).

**FINDINGS/BUGS**  
- Offload hooks fired 123×, executed 0× (expected advisory layer).  
- Stale cron `d946b614` removed; no tribal drain impact.  
- Restored machine‑handbook files; re‑ran tests after fixing `LathePostProcessorEngine.test.ts`.  
- CAD starvation: path classifier yields 0 CAD; OCR recovers some CAM but not CAD (CAD docs in JM drawings).  
- R8/R11 defect: feeder wrote orphan `domain-knowledge/...` path.  
- Embedder ingests only `.md`; jsonl consumers are cad/cam‑specific.  
- Guard and domain checks mitigate clobber risk; unreachable‑code warning is false positive.

**DOMAIN SPECIFICS**  
- Hermes agent (`ask-hermes`), Ollama offloading hooks, Octopus drain, Obsidian vault integration.  
- Zulu orchestrator sweep, scheduled‑task health checks, slot‑binding enforcement.  
- Domain knowledge feeders (CAD/CAM + others) – tribal index, GIGO‑safe pointer validation, sidecar overrides.  
- OCR content classification pipeline for CAD docs (xray/kilo/india).  
- Canonical corpus path: `state/shared/<domain>-tribal-corpus.jsonl`.  
- `AIResourceLearningEngine.getCadCamCorpus()` consumes cad/cam corpora.  
- `tribal-by-domain-inject` uses `SLOT_TRIBAL_DOMAIN` to embed into `tribal-embed-index.json`.  
- Active domains: tooling, mill, lathe, post‑proc, speed‑feed; empty domains omitted.

**TOOLS USED**  
- PRISM helpers: `slot-bind-enforce.mjs`, `chat-slots.mjs`.  
- `/checkin` pipeline (`H:/prism/.claude/commands/checkin.md`).  
- Hermes CLI (`ask-hermes.mjs`).  
- Ollama offloading hooks, Windows Task Scheduler for cron.  
- Vitest test runner (node‑test).  
- Node scripts: `extract-cadcam-tribal-wiki.mjs`, `build-domain-knowledge-feeders.mjs`.  
- Git workflow (`git add`, `[MAIN-FORCE]` commit).  
- `AIResourceLearningEngine.ts` (`getCadCamCorpus`).  
- `tribal-by-domain-inject` hook.  
- `fs.appendFileSync` for durable chat‑bus work‑request.

**OPEN THREADS**  
- Wire per‑domain feeders into tribal index so specialist slots consume them.  
- Run OCR classification on JM drawings to recover CAD docs (delta/xray/india).  
- Execute full Vitest suite on clean CI build for authoritative metrics.  
- Implement India’s `getDomainCorpus()` in mcp‑server and integrate with AIResourceLearningEngine.  
- Ensure generic jsonl consumer or embedder can ingest `<domain>-tribal-corpus.jsonl`.  
- Monitor hand‑curated corpora to avoid clobbering.
