# mill session 928a8226 (2026-06-09, 55.7MB, spine 568KB, 7 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hookify rule files (.local.md) → YAML frontmatter; removed 6 dangling hook refs (110 malformed files fixed).  
- Ollama resilient‑pull script (`ollama-resilient-pull.ps1`) fixed; gpt‑oss:120b download completed (~65 GB); ask‑ollama `num_predict` set to 1024.  
- Node synergy gap‑map generated (14 surfaces, 8 ranked gaps).  
- CAG‑router “qdrant://prism‑memory” label corrected; tribal‑embed index OOM resolved via cap‑safe loader and nomic context clamp ≤2000 chars.  
- Local‑GPU embedder guard & driver launch verified.  
- Documentation updates: CLAUDE.md regression, wiki lesson, gap‑map revision.  
- Docker‑service‑health CLI guard added; detects orphaned containers and auto‑restarts.  
- Singleton‑service‑guard CLI with `--fix` to reap wedged daemons & respawn MCP daemon.  
- Stop‑hook wiring for docker and singleton guards added to settings.json.  
- MCP boot‑heap floor set to 4096 MB; reap‑then‑start logic implemented; corrected restart command in banner.  
- Fleet‑task‑health migration‑freeze marker file created.  
- Cost‑bridge router consolidated and wired; awareness generator restored.  
- Cost savings tracker tests (13 vitest cases) passed.  
- PSN snapshot refreshed; 11 under‑wired pairs identified.  
- DigitalTwinEstimator reference‑value test added.  
- Margin‑floor gate added to QuoteEstimatorEngine (default 20 % from ShopConfig) and UI banner for below‑margin floor.  
- Dispatcher round‑trip test confirming margin‑floor enforcement.  
- AI‑substrate routing matrix coverage extended to 5 routes; completeness invariant added.  
- ResizeObserver polyfill in web tests exposed hidden failures.  
- Dispatch map for fleet synergy goal created.

**DECISIONS**  
- Bulk transform hookify rules; back up originals before rewrite.  
- Remove dead wiring from settings.json; add to INTENTIONALLY_DISABLED list.  
- Wire unwired engines per‑owner, avoid mass re‑enable.  
- Use local Ollama stack on RTX PRO 6000 (3 GPU models); set `OLLAMA_MAX_LOADED_MODELS=3×GPU`.  
- Batch workflow agents ≤3 concurrent to respect Anthropic limits.  
- Set `DEFAULT_NUM_PREDICT=1024` for ask‑ollama; clamp nomic embed input to ≤2000 chars.  
- Use buffer‑based incremental JSON parser for tribal index; fail loudly on truncation.  
- Treat MCP as reactive‑only; implement single‑command `--fix` that cleans duplicate daemons and starts fresh one.  
- Wire docker & singleton guards into Stop hook chain.  
- Correct MCP restart command to use `TRANSPORT=http`.  
- Replace static frozen‑task list with migration‑freeze marker file (`state/shared/MIGRATION-FREEZE-ACTIVE.flag`).  
- Consolidate cost‑bridge hooks into single router; adjust awareness checks accordingly.  
- Tag `WEDMQuoteBridgeEngine` as WIRE‑EXEMPT (invoked via routes/adapter).  
- Update aiReasoningDispatcher cascade defaults to installed Blackwell tiers; extend source‑lock guard to dispatcher tree.

**OPERATOR DIRECTIVES**  
- “please fix whatever is causing all the hook errors”  
- “continue from where you left off.”  
- “look for bugs, wirings, gaps and fixes that are holding the potential of our system back”  
- `/checkin-golf /goal [ finish pulling the ollama llm models … ]`  
- “make sure fleet reaper and monitors are running please”  
- “Synergize ollama … docker … qdrant … obsidian vault app … PSN … /system‑viz … prism galaxies … front end and back end build … claude.md … memories … wiki … tribal knowledge … prism awareness … gsd … tdd … skills … scripts and hooks | goal clear: everything wired, tested, validated and synergized across the entire system, node by node, galaxy by galaxy.”  
- “utilize ollama for better task efficiency when viable”  
- `/checkin-charlie`

**FINDINGS/BUGS**  
- Hookify rule files lacked YAML frontmatter; non‑ASCII emoji caused charm‑parse errors.  
- 6 hook references in settings.json pointed to missing .mjs → “Cannot find module”.  
- `stop_on_hook_unregistration` gate blocked until dead refs removed and INTENTIONALLY_DISABLED updated.  
- Scheduled‑task WARNs are intentional migration freezes; no action needed.  
- `getDomainTokens(lima)` fallback to “general” is by design.  
- 90 built engines unwired (38 core, 23 bridge, 14 ops); some orphaned stubs (`MillPrintToProgramEngine`).  
- Ollama resilient‑pull script had PowerShell parse error; fixed; download now completes (~65 GB).  
- Synergy routing bug: `search_synthesis` on Blackwell still selects gpt‑oss:20b instead of 120b.  
- V8 string cap (~512 MiB) caused tribal index reads to fail silently; default Node heap (~2 GB) OOMed when parsing 168 MB index.  
- Duplicate MCP daemon pileup caused port‑bind conflicts and silent outages.  
- Wrong restart command in MCP banner led to manual restarts failing.  
- Qdrant label “prism_memory” nonexistent; cosmetic only.  
- Tribal‑embed index 533 MB → Node string limit (512 MiB) → OOM for consumers reading it.  
- Embedder full‑body input > nomic context window → 500 errors; fixed by 2000‑char clamp.  
- Empty‑text wiki files caused chunk failures in embed driver.  
- Hidden frontend test failures revealed by missing ResizeObserver polyfill (null‑safety bug in QuoteAnalyticsPage).  
- Server‑side rate‑limit blocked 3‑of‑3 scrutiny agents for margin‑floor commits.

**DOMAIN SPECIFICS**  
- Hookify plugin (`config_loader.py`), Node scripts `fix-hookify-frontmatter.mjs`, `remove-dangling-hook-refs.mjs`, `audit-unwired-engines.mjs`, `fleet-reaper-sweep.mjs`.  
- Ollama model routing via `ollama-cost-router.mjs`; resilient pull script `ollama-resilient-pull.ps1`.  
- Domain token mapping (`getDomainTokens`) and tribal routing (`tribal-by-domain-inject.mjs`).  
- Unwired engines audit from `scripts/audit-unwired-engines.mjs`.  
- Docker‑service‑health guard `docker-service-health-check.mjs`; singleton‑service‑guard `singleton-service-guard.mjs`.  
- MCP server (`mcp-server`, `mcp-daemon-helper`, `mcp-daemon-autostart`); MCP boot‑heap floor helper.  
- Stop‑hook wiring scripts added to settings.json.  
- Qdrant collections: `prism_skills`, `prism_engines`, `prism_formulas`; tribal index at `state/shared/tribal-embed-index.json`.  
- AI routing engines: `ModelRoutingEngine.ts`, `aiReasoningDispatcher.ts`; QuoteEstimatorEngine, DigitalTwinEstimator, CostSavingsTrackerEngine.  
- PSN snapshot refresh script; cost‑bridge router `cost‑bridge-dispatch.mjs`.

**TOOLS USED**  
- PRISM scripts: `scripts/fix-hookify-frontmatter.mjs`, `remove-dangling-hook-refs.mjs`, `audit-unwired-engines.mjs`, `fleet-reaper-sweep.mjs`, `docker-service-health-check.mjs`, `singleton-service-guard.mjs`, `mcp-connectivity-check.mjs`.  
- PowerShell: `ollama-resilient-pull.ps1`.  
- Node (v18+), Jest/Vitest test suites, Docker CLI (`docker start`, `docker ps`).  
- Git for backup/restore of `.local.md` files.  
- Qdrant REST API; Ollama CLI; Docker Compose; CronCreate; ask‑ollama CLI.

**OPEN THREADS**  
- Wire unwired engines per‑owner (core, bridge, ops) – 90 engines pending.  
- Resolve synergy routing bug to select gpt‑oss:120b for `search_synthesis` on Blackwell GPUs.  
- Confirm scheduled‑task migration freeze lifted before re‑enabling missing tasks.  
- Validate academy tribal routing if new “academy” domain introduced.  
- Ensure fleet‑reaper runs correctly after each `/checkin-golf`.  
- Qdrant tribal collection migration (deferred until corpus stable).  
- MCP keep‑alive scheduled task (`singleton-service-guard --fix`) to prevent reactive outages.  
- Re‑embed remaining ~29 K delta entries once sharding is in place.  
- Per‑galaxy synergy work (foxtrot/whiskey/mike/kilo/charlie/delta/lima) remains outside this slot’s scope.  
- Cross‑substrate wiring (Sierra lane) and Ollama catalog realign (Alpha lane).  
- Finalization of quoting galaxy Ollama offload audit pending rate‑limit clearance.  
- Runtime debug of `quote-pages` “Generate Pricing Packet” flow; fix null‑safety bug in `QuoteAnalyticsPage`.  
- Complete `U-QP-ACCOUNTING-WIRE` unit and its 3‑of‑3 scrutiny.
