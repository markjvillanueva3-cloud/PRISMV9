# mill session 06e3b710 (2026-06-24, 26.6MB, spine 207KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5f61238333` – added async `orchestrateLive` (Ollama‑offloaded tier fan‑out) to XPROC orchestrator engine.  
- `50143ece3c` – rewired `five_axis_deep_learn` → real static `deepReason`.  
- `85f9f6fe8d` – wired `five_axis_decision` → real static `decide`.  
- `00f68e9997` – connected `fusion_5axis_strategy` → real `recommendStrategy`.  
- `964535033a` – fixed `cross_domain_orchestrate → planJob`.  
- `da5560850f` – wired `foresight_orchestrate → reportFor`.  
- `62661e33fb` – rewired `wet_run_pilot_orchestrate → pilotPromotionReadiness`.  
- `82aa392d6a` – added self‑guard to `uncertainty_pipeline_run → propagate`.  
- `29af45fc12` – corrected `smart_tool_select` → `selectToolOrchestrated`.  
- `d1a97a3a4c` – implemented standard router for `sampling_plan_generate` (mil1916/aoql).  
- `9c4e94ff94` – added scope router with query guard to `mit_course_knowledge_query`.  
- `U-DARK-FACADE-AUDIT` – script `audit-dark-facade-actions.mjs`; enumerated 85 truly dark dispatcher actions.  
- `U-DARK-FACADE-INDIA-VERIFY` – verification run; classified India’s 13 dark actions into 4 clean, 4 ambiguous, 4 needs‑build, 1 false‑positive.  
- `U-ROLLBACK-PLAN-WIRE` – wired `rollback_plan_build` → real positional `planRollback(unitId, steps)` with dry‑run verification.  
- `U-OPDASH-ORCH-WIRE` – added `orchestrate()` to `OperatorDashboardOrchestratorEngine`; composed status/alerts/shift with fail‑soft per section and shared `filterAlertsView`.  
- `U-MACHINE-LORA-INFO-WIRE` – exposed pure `machineLoRABase.getInfo()` returning canonical LoRA defaults; rewired dispatcher to bare‑payload envelope.  
- `U-INCAD-INFER-FAILLOUD` – replaced crash‑facade in `PerAppInCADInferenceAdapter` with static `describeCapabilities()` and fail‑loud honest error when runtime/feature extractor missing.  
- `U-CAD-LEARN-STATS-RATE-FIX` – corrected `cad_learning_stats` to credit successes on passes; aligned per‑category rates with `cad_learning_patterns`.  
- `U-CAD-LEARN-FEATURE-SIGNAL` – made `features` field a real learning signal in `CADTrialErrorLearningEngine`; added aggregation and scoring.  
- `U-CAD-LEARN-TREND` – added read‑only `getLearningTrend()` to expose CAD failure‑rate trend metric.  
- Companion tests: `MachineLoRABaseEngine.test.ts`, `OperatorDashboardOrchestratorEngine.test.ts`, `UncertaintyPropagationPipelineEngine.test.ts`.  

**DECISIONS**  
- Dark‑facade actions must be replaced with real engine methods; silent failures are unacceptable.  
- Adopted async, parallel fan‑out (`orchestrateLive`) for tier reasoning offloading to Ollama; dispatcher remains pure I/O.  
- Fail‑loud honest reporting preferred over silent crashes in dispatcher adapters.  
- Added static capability descriptors (e.g., `describeCapabilities()`) to avoid constructor side‑effects and enable introspection.  
- For learning engines, ensure success crediting in statistics and expose trend metrics (`getLearningTrend`).  
- Every modified engine now has ≥10‑case companion test (R15 wiring gate).  
- Declared loop termination when India’s clean dark queue is exhausted; remaining items are *needs‑build* or cross‑domain.  

**OPERATOR DIRECTIVES**  
- `/loop [25m] /goal AUTONOMOUS OVERNIGHT BUILD …` – run crons, harnesses, engineered loops autonomously while operator sleeps.  
- Offload mechanical work to Ollama (`node scripts/ask‑ollama.mjs`) and Hermes (`node scripts/ask‑hermes.mjs`).  
- Preserve auto‑compaction on; do not start heavy builds in RED context (token zone 0.96).  
- Never touch Oscar safety‑critical physics or commit peer‑claimed files.  

**FINDINGS / BUGS**  
- Dark façade pattern identified: 85 truly silent failures, 324 false positives.  
- `deepReason` crashed on empty `part_features[0]`.  
- `kc11_mpa` division by zero produced NaN cutting parameters.  
- Double‑wrap of results caused `data.data` nesting in `cross_domain_orchestrate`.  
- `smart_tool_select` miswired to non‑existent method; corrected.  
- `PerAppInCADInferenceAdapter` constructor threw on missing runtime/feature extractor (crash‑facade).  
- `cad_learning_stats.byCategory` never credited successes → inflated failure rates.  
- Several dark‑facade dispatcher probes (`rollback_plan_build`, `operator_dashboard_orchestrate`) had no real method.  
- In `CADTrialErrorLearningEngine`, the `features` field was ignored; no aggregation or scoring.  
- MODULE_NOT_FOUND due to wrong cwd; resolved by `cd mcp-server`.  
- Stale VITEST_REPORT.json (56 days old) caused gate failure; npx PATH missing in detached shell → exit 127.  
- Token zone RED (0.96); auto‑compaction pending.  
- TS2554 error in `ReinforcementLearningCAMFeedbackEngine.ts` noted for next ladder fix.  

**DOMAIN SPECIFICS**  
- **Engines**: `CrossProcessHierarchicalNeuralOrchestratorEngine`, `fiveAxisDeepLearningEngine`, `SmartToolSelectorOrchestratorAdapter`, `SamplingPlanEngine`, `MitCourseKnowledgeEngine`, `RollbackPlannerEngine`, `OperatorDashboardOrchestratorEngine`, `MachineLoRABase`, `PerAppInCADInferenceAdapter`, `CADTrialErrorLearningEngine`.  
- **Dispatchers**: `aiReasoningDispatcher`, `orchestrationDispatcher`, `camDispatcher`, `calcDispatcher`, `rollback_plan_build`, `operator_dashboard_orchestrate`, `machine_lora_info`, `per_app_incad_infer`, `cad_learning_stats`, `cad_learning_trend`.  
- **Metrics/paths**: CAD failure‑rate trend, per‑category success/failure rates; XPROC orchestrator actions (`xproc_*`); AI reasoning actions; domain pipelines (lathe, mill, wire wizard).  
- **Paths**: `/checkin-india` slot binding; `/loop [10m] …`; `/checkin.md` pipeline; `H:/prism/.claude/commands/checkin.md`.  

**TOOLS USED**  
- PRISM tooling: `chat-slots.mjs`, `/checkin.md`, `audit-roadmap-drift.mjs`, `audit-dark-facade-actions.mjs`.  
- TypeScript compiler (`tsc --max-old-space-size=16384` or `--noEmit`).  
- Vitest for unit tests; mock‑server harnesses for dispatcher path testing.  
- Node scripts: `ask-ollama.mjs`, `ask-hermes.mjs`.  
- Auto‑compaction, cron job `f09e8cee` for overnight loop.  
- Offload engines: Ollama (Sonnet→Opus ladder), Hermes (xAI Grok OAuth).  
- Git commit workflow with lock‑retry under heavy slot contention; R15 wiring gate.  

**OPEN THREADS**  
- Cad‑learning loop‑closure: predict → outcome attribution → retrain linkage.  
- Text→CAD feedback loop.  
- Blueprint LoRA/RAG engine internals (training, evaluation).  
- Pending R15 companion tests for any remaining rewires.  
- Awaiting operator to arm account‑switch so the overnight cron can resume after the 5 h wall.  
- Correctly refresh `VITEST_REPORT.json` in foreground (ensure PATH, run full suite).  
- Trigger manual `/compact` if auto‑compaction fails; monitor token budget.  
- Fix TS2554 error in RL engine after current unit completes.  
- Verify autonomous crons/harnesses are running per operator’s overnight directive.
