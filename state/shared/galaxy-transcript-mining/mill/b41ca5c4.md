# mill session b41ca5c4 (2026-06-23, 3.4MB, spine 26KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-WEDGE-GUARD-AUTOWIRE`: Fixed recover() to enable a disabled task before starting, extracted pure `buildRecoveryScript()`, wired golf‑level cron (`PRISM Ollama Wedge Guard`) that runs every 10 min.  
- `U-ZLR-GEN-PROBE-FAST`: Replaced the 32 b/20 s truth‑harness probe with a fast non‑thinking 1.5 b model and 30 s timeout; eliminated false‑OPEN flag.  
- Hermes lane re‑exercised: routed a real summarization through `ask-hermes`, flipped Hermes to UTILIZED.  
- `U-WIKI-OLLAMA-WEDGE-LESSON`: Added wiki entry documenting the wedge‑guard bug and fast‑probe fix.

**DECISIONS**  
- Adopted double‑gated, permanent self‑heal cron for Ollama (10 min cadence).  
- Switched truth‑harness canary to a fast model to avoid cold‑load false negatives.  
- Enabled `PRISM Ollama Serve` task after confirming it was disabled; restored service health.  
- Chose not to auto‑restart shared Ollama without operator confirmation, following the 2026‑06‑14 precedent.

**OPERATOR DIRECTIVES**  
- “Recover + auto‑wire cron” – authorized immediate recovery of the Ollama wedge and wiring of the golf cron.  
- Confirm before restarting the shared Ollama service (standing precedent).  

**FINDINGS/BUGS**  
- Ollama `/api/generate` hung (“wedged”) due to a disabled scheduled task; recover() failed because it didn’t enable the task first.  
- Truth‑harness `OLLAMA-GEN` probe falsely flagged wedged status when cold‑loading a 32 b model.  
- Hermes proxy was under‑utilized (no traffic after recent fix).  
- `PRISM Ollama Serve` scheduled task was disabled; `PRISM Ollama Embed Keepalive` also disabled.

**DOMAIN SPECIFICS**  
- Zulu slot binding (`zulu-work`) is self‑exempt in orchestrator sweep.  
- Scheduled tasks: `PRISM Ollama Serve`, `PRISM Ollama Wedge Guard`, `PRISM Hermes Proxy`.  
- Meta‑system UTILIZED status tracked via zulu ledger (6 SHIPPED / 0 OPEN).  
- Truth‑harness probes use models and timeouts; canary logic in `scripts/ollama-wedge-guard.mjs`.

**TOOLS USED**  
- Node scripts: `chat-slots.mjs`, `ollama-wedge-guard.mjs`.  
- PowerShell scheduled‑task installers (`install-ollama-wedge-guard-task.ps1`, `install-ollama-embed-keepalive-task.ps1`).  
- Test harnesses (R9 regression, 13/13 pass).  
- `/checkin` pipeline and slot‑binding helpers.  

**OPEN THREADS**  
- Enable `PRISM Ollama Embed Keepalive` if Obsidian recall performance degrades.  
- Continue the autonomous loop (`/loop`) for fleet‑wide NEVER‑IDLE tasks (e.g., monitoring, further self‑heal wiring).
