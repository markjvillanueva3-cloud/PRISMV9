# mill session be279b4f (2026-06-11, 54.3MB, spine 376KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `LAUNCH‑PRISM‑FLEET.bat` – regenerates from `chat-slots.json`, refreshes compaction‑recovery files, prints launch summary, auto‑closes stale slots.  
- `ollama-docker-launcher.mjs` – added `--ensure-native-ollama`; starts Ollama detached before fleet tabs.  
- Docker data moved from C: to H: (301 GB freed) via `robocopy /J` + junction.  
- Ollama models `qwen3‑coder:30b`, `deepseek‑r1:32b` pulled, registered; routing updates U1, U5, U7 committed.  
- Hybrid RAG enabled fleet‑wide (`U-FLOR-MODEL-EXPAND`, `U-FLOR-HYBRID-DEFAULT`); CAG cache & LoRA emit active.  
- `qwen3‑coder:30b` set as default code model (`U-FLOR-CODER-DEFAULT`).  
- MCP heap divergence fixed; `PRISM_MCP_HEAP_FLOOR_MB` aligned (daemon 4 GB → supervisor 24 GB).  
- Deep‑reasoning opt‑in added to galaxy bridge (`b6bc5de8cd`); fast coder remains default.  
- LoRA assembler updated to accept prompt/completion schema; `trainingReady` true (856→1138 rows).  
- Real QLoRA trainer (`fleet_lora_train.py`) – commit 378e702505 – per‑sample weighted loss, checkpointing, CUDA pre‑flight.  
- Goal‑loss‑function detector (`U-GOAL-LOSS-FN-DETECT`, 0e9b6ef88a) committed; lazy‑import guard added.

**DECISIONS**  
- Use self‑regenerating `.bat` to avoid stale launch decisions and ensure fresh context.  
- Warm Docker Desktop + native Ollama before fleet spawn to eliminate cold starts.  
- Keep vLLM disabled by default (`PRISM_VLLM_ENABLE=0`).  
- Manual junction move of Docker data over WSL2 compact (zeroed‑block limitation).  
- Apply Ollama cap `MAX_LOADED_MODELS=2`, `KEEP_ALIVE=10m`; pagefile raise pending reboot.  
- Adopt hybrid RAG fleet‑wide; enable CAG cache and LoRA emit.  
- Use 4‑bit QLoRA on RTX PRO 6000 (cu128) for efficient GPU usage.  
- Keep fast coder default; add opt‑in deep‑reasoning via cost‑router.  
- Implement code‑based classifier for `/goal` loss‑function enforcement.

**OPERATOR DIRECTIVES**  
- Free C: space: `docker system prune -a -f`, clear Docker Desktop image, empty recycle bin.  
- Commit untracked `smart.md` changes (git‑ignored).  
- Verify `.dockerignore` excludes large node_modules/data folders.  
- Decide on model placement (Ollama vs Docker Model Runner); update `ollama-cost-router.mjs`.  
- Continue autonomously; apply pagefile raise and Ollama cap after admin reboot if desired.  
- Apply both levers (pagefile + Ollama cap).  
- Check current Ollama setup before pulling models; pull if missing.  
- Move Docker to H: completed.  
- Execute user’s “goal clear” rule: scan H‑drive, wire all files into Obsidian vault, train Hermes/Obsidian on JM docs.

**FINDINGS / BUGS**  
- C: drive 100 % full → prevents commits and command output; `smart.md` edits uncommitted due to git‑ignore.  
- Docker compose build fails (~2.9 GB image context) – missing `.dockerignore`.  
- vLLM parked, not running.  
- Docker Model Runner stores models on C:, contributing to disk fill.  
- Docker vhdx on C: 301 GB; prune freed space but file didn’t shrink (WSL2 ext4 zero‑block issue); compact failed.  
- `.claude.json` corrupted by disk‑full write; restored from backup.  
- MCP server heap floor diverged (daemon 4 GB vs supervisor 24 GB); fixed.  
- Pagefile anomaly: registry shows only C: pagefile, live system has 5 pagefiles; reboot could drop others.  
- GNN reference pool AUROC 0.808 selective‑deploy; full coverage requires GPU retrain.  
- `/compact` blocked by Anthropic server rate‑limit (“Server is temporarily limiting requests”).  
- Trainer collate KeyError due to HF Trainer `remove_unused_columns=True`; fixed.  
- Orphaned 3B training run hung after step 100; resumed from checkpoint‑100.

**DOMAIN SPECIFICS**  
- Key files: `LAUNCH‑PRISM‑FLEET.bat`, `regenerate-launch-fleet.mjs`, `slot-tab-boot.ps1`, `ollama-docker-launcher.mjs`, `smart.md`, `ollama-cost-router.mjs`.  
- Docker data path: `C:\Users\wompu\AppData\Local\Docker\wsl\disk\docker_data.vhdx`; moved to H: via junction.  
- Ollama models stored on H: (`OLLAMA_MODELS` env).  
- Hybrid RAG uses nomic‑embed‑text, dense rerank, CAG cache, LoRA, per‑galaxy reasoning.  
- 34 galaxies each contain `SOUL.md`, `CLAUDE.md`, `MEMORY.md`, `AWARENESS.md`.  
- Engines: Hermes loop, CAG cache, hybrid RAG, LoRA emit, GNN retrain (`nn‑graph-retrain-lifecycle.mjs`).  
- Dispatchers: `qwen3‑coder`, `deepseek‑r1`, `gpt‑oss:20b` for reasoning sweeps.  
- Metrics: AUROC, macroF1, Brier (GNN); `hybrid_fired` counts; LoRA corpus rows (856→1138).  
- Paths: Obsidian vault, Hermes, PSN, Prism awareness, `claude.md`, `souls.md`, `memories`, wikis across all galaxies.

**TOOLS USED**  
- Node scripts: `regenerate-launch-fleet.mjs`, `ollama-docker-launcher.mjs`, `goal-prereq-inject.mjs`, `chat-slots.mjs`.  
- PowerShell: `slot-tab-boot.ps1`, junction creation, `.claude.json` restore.  
- Docker CLI: `docker system prune`, `docker compose build`, `docker model pull`.  
- Git for commits and tracking.  
- `robocopy /J` for vhdx copy; `diskpart compact vdisk` (failed).  
- Python venv (`uv`), torch 2.11 cu128, bitsandbytes, trl, peft, datasets, accelerate.  
- Playwright for article preview fetching.  
- PRISM command pipeline: `/checkin`, `/loop`, `/goal`, `/smart`, `/startup-nato`.

**OPEN THREADS**  
- Resume U2–U10 build units; apply pagefile raise after admin reboot if desired.  
- Final confirmation of AI‑synergy integration across all galaxies (bridge now active).  
- GNN full‑coverage GPU retrain pending reference‑pool growth.  
- 7B adapter run pending download and training.  
- Implement second half of goal enhancement: capture `--check` / `--metric` in goal‑keeper.  
- Reaper mis‑reaping session‑owned background children audit.  
- Run Playwright reads of Twitter articles to extract actionable insights.
