# mill session 6437979f (2026-05-19, 3MB, spine 32KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `state/shared/specs/SKILL-AUTOINVOKE-COVERAGE-AUDIT-2026-05-19.{md,html}` – peer‑reviewed audit of skill auto‑invoke coverage (121/622 skills).  
- `state/shared/specs/AUTOINVOKE-HITRATE-MS0-PLAN-2026-05-19.{md,html}` – unified fix plan for skill & memory auto‑invoke.  
- `scripts/lib/semantic-retrieve.mjs` – wrapper around existing `prism_memory:semantic_search`.  

**DECISIONS**  
- Auto‑invoke must use semantic retrieval; keyword‑only hooks are insufficient (skill coverage 23.5 %, memory precision low).  
- Build loop paused until: (1) peer review of plan passes, (2) live memory chat confirms no conflict on `memory-rag-inject` wiring.  
- Skill side can proceed independently once peer review is green; memory side waits for coordination reply.

**OPERATOR DIRECTIVES**  
- User: “start building /loop [5m] complete both phases.”  
- Assistant: “Proceed with skill‑side units now (U‑D3 → U‑D1 → U‑D2) if you accept possible rework; otherwise wait for peer review and memory chat reply.”

**FINDINGS/BUGS**  
- Skill auto‑invoke covers only 23.5 % of skills; missing triggers for ~40–60 high‑ROI dark skills.  
- `memory-rag-inject.mjs` is orphaned (not in `settings.json`) and logs `scanned=0`.  
- Semantic engines (`prism_memory:semantic_search`, `qdrant_vector_search`) exist but are unused by any hook.  
- `skill-auto-trigger.mjs` uses BM25 keyword scoring; no semantic fallback.

**DOMAIN SPECIFICS**  
- **Skills** – 622 total, 481 trigger ledger entries, 121 reachable via auto‑invoke.  
- **Memories** – Obsidian vault, `memory-relevance-inject.mjs`, `memory-rag-inject.mjs`.  
- **Hooks/Dispatchers** – `skill-auto-trigger.mjs`, `memory-relevance-inject.mjs`, `post-memory-context-eval`.  
- **Engines** – `prism_memory` (semantic search, Qdrant).  
- **Metrics** – trigger coverage %, memory precision@3, recall, MEMORY.md size (<22 KB).  

**TOOLS USED**  
- `/forge-audit-v2`, `/forge7`, `skill-auto-trigger.mjs`, `memory-relevance-inject.mjs`, `memory-rag-inject.mjs`, `prism_memory` APIs, `semantic-retrieve.mjs`, `settings.json`, `AGENT_CHAT.md`.  

**OPEN THREADS**  
- Await peer‑review verdict on plan (Phase 4.5).  
- Await memory chat coordination reply (U‑A1) to confirm wiring of `memory-rag-inject`.  
- If user opts, build skill‑side units immediately; otherwise resume loop after both approvals.
