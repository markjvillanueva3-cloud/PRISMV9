# cad session df944902 (2026-05-19, 52.8MB, spine 122KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**

- U‑CK16 – skill‑auto‑trigger pipeline‑aware (commit 1763248116) – 35/35 tests, PASS/SHIP  
- U‑CK27 – telemetry→adaptive‑thresholds + `--signals` dormant report (commit 44a95cc1c0) – 12/12 tests, 6/6 DORMANT smoke‑test  
- MTC07 – operation‑ordering canonical (commit c2551512e5)  
- MTC08 – workholding‑practices canonical (commit 4bcbeb4669)  
- MTC09 – part‑setup/probing canonical (commit c986865c56)  
- MTC10 – coolant/chip‑evacuation canonical (commit ea0f4546cc)  
- U‑FGE03 – persist learned‑blended prevalences + auto‑apply in `templateFor()` (commit e7d583b344) – 68/68 tests, no tsc errors  
- U‑ROADMAP‑INDEX‑WRITER‑CONSOLIDATE – atomic‑json helper (commit d877d1c970)

**DECISIONS**

- Prioritize today’s work from golf’s `FLEET-PENDING-EXTRACT-2026-05-18`; discard stale auto‑write templates  
- Use `slot-bind-enforce.mjs` to claim bravo slot; run `/checkin-bravo` wrapper for full pipeline  
- Consolidate five inline atomic JSON writers into single helper (`atomic-json.mjs`) to avoid concurrent‑writer collisions  
- Adopt dev‑tools/backend‑dev priority per `feedback_prioritize_devtools_backend`  
- Expand skill‑auto‑trigger to listen on Stop, PostToolUse, UserPromptSubmit; widen auto‑derive to all `kind:pipeline` entries  
- Use call‑time env‑var getters (`triggersPath()`, `pipelinesDir()`…) for hermetic testing  
- Persist learned‑blended prevalences via overlay file and wire into default `templateFor()` path  
- Add `templateForStatic()` to preserve static‑vs‑corpus drift semantics for evidence actions  
- Adopt per‑file scrutiny gate (2 parallel reviewers) with FAIL→fix→re‑dispatch protocol  

**OPERATOR DIRECTIVES**

- `/goal compile all bravo tasks from previous sessions and add to task queue, place ahead of rgs tasks. complete units.`  
- `/loop [5m] /goal`  
- `/goal focus development tools, obsidian, system‑viz tasks`  
- Continue loop; compile remaining work from today’s chat bus (`FLEET-PENDING-EXTRACT-2026‑05‑18`)  
- Complete any pending units; resume autonomous `/loop`

**FINDINGS/BUGS**

- Stale auto‑write template picks removed  
- Path constants in `skill-auto-trigger` switched to call‑time getters (hermetic testing)  
- `replace_all` corrupted env‑var names – restored  
- False‑green subprocess oracles replaced with explicit `assert.ok`  
- TriggerCommand auto‑derive widened from `trigger:command` to all `kind:pipeline`; gated by `trigger_command:false` opt‑out  
- Stale env var in `runHook` pinned to LIVE registry paths; cleared knobs  
- Atomic JSON writers now use per‑PID temp suffix + cleanup (no fixed `.tmp`)  
- Telemetry event mismatch fixed; added `--signals` mode and dormant‑signal report  
- U‑FGE03: added `persistLearned()`, overlay loader, `overlayStatus()`, `clearOverlayCache()`; wired into `templateFor()`  
- Dispatcher for `cad_corpus_apply_learned` now includes `persist` flag  
- Updated dispatcher round‑trip to parse MCP envelope; strengthened learner tests with real serialization assertions  

**DOMAIN SPECIFICS**

- Engines: `CADClassFeatureLibraryEngine`, `CADCorpusFeaturePrevalenceLearnerEngine`  
- Actions: `cad_corpus_apply_learned` (persist), `cad_class_build_sequence_evidence`, `buildSequenceForEvidence`  
- Dispatchers: `skill-auto-trigger.mjs`, `atomic-json.mjs`, `register-*` scripts  
- Metrics: telemetry event counts, adaptive‑thresholds, R12 dormant‑signals report  
- Paths: `H:/prism/mcp-server/src/engines/*`, `H:/prism/scripts/lib/*`, `H:/prism/state/shared/dashboards/*`  
- PRISM 13-chat fleet slot system (alpha…golf)  
- COMMAND‑KERNEL‑MS0 milestone (29 units, psk syscall layer, pipeline registry)  
- MACHINING‑TRIBAL‑COVERAGE‑MS0 – tribal knowledge canonicals (`knowledge/wiki/code‑tribal/canonical/`)  
- Slot‑bind‑enforce hook for deterministic bravo slot claim  
- Chat bus (`agent-coordination.mjs`, `state/shared/AGENT_CHAT.jsonl`) used to redistribute work (Golf’s FLEET‑PENDING‑EXTRACT)  
- Settings.json wiring: Stop[0].hooks, PostToolUse[0].hooks updated  

**TOOLS USED**

- PRISM command wrappers (`/checkin-bravo`, `/startup-bravo`)  
- `chat-slots.mjs` helper for slot claim/reclaim  
- `skill-auto-trigger.mjs` hook and test harness (`node:test`)  
- `atomic-json.mjs` helper  
- Vitest for unit tests; node subprocess oracles for hooks  
- Settings.json auto‑mirror (`c-to-h-mirror`)  
- Per‑file scrutiny gate (2 reviewers)  
- 3‑of‑3 Stop gate  

**OPEN THREADS**

- Pending CK units: U‑CK11, U‑CK14, U‑CK15, U‑CK28, U‑CK29 (COMMAND‑KERNEL‑MS0)  
- Environmental block: `C:\Users\wompu\AppData\Local\Temp\claude\` out of space → all Bash/Powershell harnesses fail (`ENOSPC`). Must free ≥10 MB before resuming `/checkin-bravo /loop`.  
- Loop state at iteration 11 pending; after freeing temp space resume `/checkin-bravo /loop`.  
- Minor P3 items: Bash substring `includes` risk, telemetry append rotation cap, trigger_command opt‑out ambiguity – noted but non‑load‑bearing.  
- Further refinement of system‑viz and obsidian tasks remains.
