# cad session f81732d5 (2026-05-25, 39.7MB, spine 142KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED (builds/commits)**  
- `a75d27afd8` – AI‑wire learning engines wired into `aiReasoningDispatcher`.  
- `d915fa3be8` – Learning routes and docs for BP‑MS0.  
- `fc4cf18ace` – RL CAM feedback engine (`ReinforcementLearningCAMFeedbackEngine`).  
- `173c562e04` – MCP connectivity fixes (ESM import bugs).  
- `1dda943c11` – ASCII‑fold MCP task install scripts.  
- `302533f792` – PWA SW test commit.  
- `60009927bc` – 9 PWA files landed in iter 1 of academy mobile.  
- `reference_lima_5_22_to_5_23_2026.md` – Lima ledger.  
- `reference_lima_psn_synergy_2026_05_23.md` – PSN 11‑leg mapping for lima work.  
- `wiki/architecture/prism-academy-mobile-ms0.md` – Academy architecture surface.  
- `course-0a-shop-math.ts`, `course-0b-hand-tools.ts`, `course-0c-blueprint-reading.ts`, …, `course-17-tooling-codes.ts` – 18 new courses (0a‑34) added this session.  
- `TrainingSchedulerEngine.ts` + 18‑case test suite.  
- `knowledgeDispatcher` wired with 7 new `academy_*` actions.  
- `b16465341` – System‑viz fast regen task completed successfully.  
- Iterations: `070a9b31fb` → `857bcb2aed`.  
- 35 Academy courses live; 17 added this session (18–34).  
- 40/46 Vitest passes, zero TypeScript errors after recent fixes.  

**DECISIONS (architecture/scope + why)**  
- Slot‑binding wrapper (`/checkin-lima`) isolates slot ownership and enables autonomous `/loop` execution.  
- `knowledgeDispatcher` as single surface for all academy actions; decouples domain logic from UI, supports PSN integration.  
- Split responsibilities: `CurriculumEngine` (immutable data) + `AssessmentEngine` (pluggable logic).  
- Added interactive primitives (calculator, diagram, sandbox, video, 3D viewer, animation) to lesson schema for visual learning.  
- Introduced `TrainingSchedulerEngine` for per‑employee enrollment, status tracking, remediation recommendation, adaptive scheduling.  
- Course-level modularity: each domain in separate file (`course‑13`, `14`, `15`, `16`, `17`) for independent evolution.  
- Dual‑level pedagogy template (layman + advanced math/science) for all new courses.  
- Centralized function‑index dispatchers (`<cam>_function_index_*`) expose every CAM function/input.  
- Unified `ModuleQuiz` union and widened lesson content types to resolve web/backend shape mismatches.  
- Optional chaining on `module.lessons` guards against Lima‑style module crashes.  

**OPERATOR DIRECTIVES**  
- Treat `/goal` directives verbatim; no user prompts.  
- Each new goal adds a course or feature (e.g., tooling‑codes, CAD/CAM entry training).  
- Loop advanced automatically after each commit chain to satisfy 5 min fire cadence.  
- Expand all machining domains, machine types, CAD/CAM toolpaths & functions with examples; include tooling utilization, optimal parameters, material‑specific cutting guidance, operation order rationale.  
- Ensure UI/UX is phone‑friendly, interactive, visually rich for learners who struggle with text.  

**FINDINGS / BUGS**  
- State‑file conflicts (`BUILD_STATE.json`, `MILESTONE_PROGRESS.json`) during cherry‑pick; resolved with `-Xtheirs`.  
- Memory size ceiling (22 KB) prevented adding a large memory file; deferred to Obsidian feed hook.  
- System‑viz regen initially exited 255; fixed by ensuring all graph nodes were present before regen.  
- Test failures in `TrainingSchedulerEngine` due to missing enrollment status logic; added comprehensive test suite and corrected implementation.  
- Inline backtick errors in course files (e.g., `course‑18`).  
- Quiz shape mismatch causing web build failures; resolved via type widening.  
- Runtime crashes on `module.lessons` access in Lima modules; fixed with optional chaining.  
- 50+ pre‑existing TypeScript errors across web and backend cleared after recent patches.  

**DOMAIN SPECIFICS (engines/actions/dispatchers/metrics/paths)**  
- Engines: `SpeedFeedOrchestratorEngine`, `ReinforcementLearningCAMFeedbackEngine`, `TrainingSchedulerEngine`, `CurriculumEngine`.  
- Dispatchers: `knowledgeDispatcher` (`academy_enroll`, `academy_get_enrollments`, …).  
- Metrics: quiz scores, enrollment completion rates, remediation success rate (tracked by scheduler).  
- Paths: `/checkin-lima` → slot‑claim → `/loop` → `/goal`; all work committed to `slot/lima`.  

**TOOLS USED**  
- PRISM CLI scripts (`chat-slots.mjs`, `audit-roadmap-drift.mjs`).  
- Git for version control and cherry‑pick.  
- TypeScript compiler (`tsc`) for type safety.  
- Jest test runner (40 unit tests) + Vitest (40/46 passes).  
- Obsidian feed hook for memory discovery.  
- System‑viz engine for graph regeneration.  
- React, Playwright (planned visual‑verify), CI/CD pipeline with automated linting and type checks.  

**OPEN THREADS**  
1. CAD/CAM entry‑level training – create courses covering major CAD/ CAM software, toolpath generation, basic simulation.  
2. Alarm database integration – map alarm codes to troubleshooting procedures and embed in curriculum.  
3. Business management & shop‑floor leadership modules – content on efficiency metrics, cost analysis, lean practices.  
4. Full 3D viewer primitive – implement `3d_viewer` lesson type for complex geometry walkthroughs.  
5. Animation primitives – add animated step‑by‑step guides for advanced machining operations.  
6. System‑viz finalization – ensure all new nodes (courses, quizzes, scheduler actions) are fully represented and searchable via PSN.  
7. Mobile safe‑area wrapping at layout level; populate video/3D viewer/sandbox URLs in course content; capture Playwright screenshots; finalize mobile‑first Academy page routing and navigation.
