# cad session 7361b856 (2026-05-13, 15.4MB, spine 108KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- HS‑14: memory‑pressure‑auto‑relief script fixed (exit 0, BOM restored).  
- HS‑15: tool‑watchdog now records `durationMs` and emits “SLOW” nudges.  
- HS‑06 Phase 1: archived 85 bucket‑E skills to `commands‑archive/`.  
- HS‑06 Phase 2: added `archived‑skill‑suggest.mjs` hook (wired PreToolUse/PostToolUse) and archived 29 bucket‑C/D skills.  
- DEV‑VELOCITY‑AUTOTRIGGER‑MS0: shipped 13 slash‑command skills, orchestrator hook, trigger extractor, CLAUDE.md splicer, wiring updates; 14 commits on `cad-fusion-live-ms0` (branch ~180 ahead).  
- P0‑U05 in ACP‑MS0: 1 commit, milestone now 5/5.  
- Follow‑up tasks: soak baseline, big‑blob hunt, envelope drift fix, wire‑unwired Lathe proposal – 4 commits.  
- Regex‑bug fix: surfaced 1 033 hidden units, updated 166 drift cases.  
- Total session: 23–24 commits; all four 4‑surface surfaces aligned.

**DECISIONS**  
- Staged HS‑06 delivery: archive bucket‑E first, then recall hook + bucket‑C/D.  
- Adopted LFS migration (`git lfs migrate import …`) for U‑GC‑02 (~3 GiB win, no history rewrite).  
- Selected Path B for U‑GC‑15 post‑LFS migration.  
- Install memory‑pressure scheduled task on each machine; pending `DESKTOP‑N7MI1VB`.  
- `/checkin` protocol sufficient for slot coordination (no manual handoff beyond initial claim).  
- Adopted `skill-auto-trigger.mjs` to surface top‑K suggestions per prompt.  
- Switched `mcp-route-suggest.mjs` classifier to suppress false positives on `git/.claude/state‑shared/scripts`.  
- Added `git-lock-sweeper.mjs` for NTFS race handling (30 s lock threshold).  
- Ship wire‑unwired batch after regenerating stale wiring index; 73 Lathe engines remain unwired.  
- Fixed regex in gap map (`BROKEN_CHAINS P1`) blocking `/pick-unit`.

**OPERATOR DIRECTIVES**  
- “continue”  
- “run /loops until this /goal is complete”  
- `"/loop until this /goal is complete"` – run tasks sequentially  
- `"/pick‑unit"` (optional `--slot`, `--priority`, `--tier`, `--limit`, `--json`)  
- Companion commands: `/checkin`, `/master-index <q>`, `/awareness-snapshot`, `/orphan-inventory`

**FINDINGS/BUGS**  
- Memory‑pressure script returned non‑zero exit; fixed with explicit `exit 0`.  
- Tool‑watchdog produced `durationMs:null`; added PreToolUse stash.  
- BOM removal caused PowerShell parse errors; restored UTF‑8 BOM.  
- Namespace mismatch in recall hook; now generates `<parent>-<basename>` keys.  
- Git‑ignore prevented new skill files; used `git add -f` and documented pattern.  
- P0 failures in skill specs (wrong commit scope, missing trigger tables); resolved pre‑commit.  
- Regex bug in gap map → hidden shipped units & 166 drift cases.  
- Wiring index stale (~24 h) → under‑count of wired engines; fixed.  
- Classifier false positives on `git/.claude/state‑shared/scripts` suppressed.  
- NTFS race lock issue resolved with `git-lock-sweeper`.

**DOMAIN SPECIFICS**  
- PRISM harness hooks: PreToolUse, PostToolUse, UserPromptSubmit.  
- Skill archive (`commands‑archive/`) + recall hook (`archived‑skill‑suggest.mjs`).  
- Memory‑pressure auto‑relief task `03-memory-pressure-auto-relief.ps1`.  
- Tool‑watchdog hook `tool-watchdog.mjs`.  
- Forge‑7 Boris‑loop pipeline for skill deployment.  
- System‑viz queries to identify unwired engines & missing skills.  
- Lathe engines: 73 unwired after regen; dispatcher candidates `turningDispatcher`, `aiReasoningDispatcher`.  
- Skill‑auto‑trigger orchestrator reads `_skill-triggers.jsonl`, surfaces top‑K suggestions.  
- `mcp-route-suggest` classifier for route suggestion.  
- `git-lock-sweeper` PreToolUse arm + retry‑with‑backoff.  
- Scripts: `extract-skill-triggers.mjs`, `regen-claude-md-sections.mjs`.

**TOOLS USED**  
- `/checkin` fleet‑coordination protocol (slot claim, drift report).  
- PowerShell `03-memory-pressure-auto-relief.ps1`.  
- Node hooks: `tool-watchdog.mjs`, `archived-skill-suggest.mjs`, `skill-auto-trigger.mjs`, `mcp-route-suggest.mjs`, `git-lock-sweeper.mjs`, `extract-skill-triggers.mjs`, `regen-claude-md-sections.mjs`.  
- Git commands: `git add -f`, `git commit` (no stash).  
- Forge‑7 pipeline, system‑viz adapters.  
- Obsidian/wiki frontmatter for skill metadata & trigger extraction.  
- Follow‑up tools: `big-blob-hunt`, `envelope-drift-fix`, `wire-unwired`.  
- Memory entry `reference_dev_velocity_autotrigger.md` in MEMORY.md.

**OPEN THREADS**  
- Install memory‑pressure scheduled task on `DESKTOP‑N7MI1VB`.  
- Finalize U‑GC‑01/26 forge‑orphan cleanup (no action yet).  
- Confirm U‑GC‑15 Path B selection in production.  
- Schedule P3 quiesce window for next release cycle.  
- Next deterministic pick: AI-MAX-MS0/U-AIMAX11 (AI Reasoning Skill Commands).  
- Drift triage queue of 166 cases pending.  
- Remaining NEEDS_WIRING engines (~879 after regen).  
- Units in ACP‑MS1/P0-U01 and subsequent milestones still to ship.
