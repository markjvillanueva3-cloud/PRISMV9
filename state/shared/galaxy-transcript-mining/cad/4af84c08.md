# cad session 4af84c08 (2026-05-20, 2.2MB, spine 50KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Synced 3 plan files from H: to C:.  
- Applied `apply-host-fleet-tuning.ps1` (ports, Defender exclusions, Ultimate Performance, TdrDelay = 60 s).  
- Applied `apply-pagefile-config.ps1` (Option D: G/J/L 32 GB each + C 4 GB).  
- Downloaded MSI BIOS 7E12v1KB to H:\BIOS and prepared USB stick for Flashback/M‑Flash.  

**DECISIONS**  
- Use Windows Terminal as default terminal → scrollable streaming.  
- Enable EXPO in BIOS (DDR5‑6400 if stable, else DDR5‑6000 CL30).  
- Set Ultimate Performance power plan; TdrDelay = 60 s for GPU workloads.  
- Adopt 3‑way NVMe stripe pagefile (Option D) to avoid USB/slow‑SATA swap and provide ~96 GB swap ceiling.  
- Keep current state (no revert).  

**OPERATOR DIRECTIVES**  
- “keep current” after patching pagefile.  
- Build an app that launches 20 chats, assigns slots via click, double‑click opens chat, windows blink on turn, orchestrator monitors and compacts.  
- Cycle all 6 accounts at 95 % session/weekly limit; orchestrator must log in, wait 2–3 min between starts to avoid API errors.  

**FINDINGS / BUGS**  
- PS 7 CIM provider fails for non‑C pagefile entries → bypassed with direct registry writes (needs script patch).  
- Snapshot path resolution bug (`..` relative) – cosmetic but fixed.  
- ConHost terminal caused scrollback lock; switched default to Windows Terminal.  
- Pagefile on USB drive (H:) was 192 GB/96 GB, removed after reboot.  

**DOMAIN SPECIFICS**  
- H: portable drive holds shared `.claude` config, handoffs, transcripts.  
- C: local machine‑specific content.  
- Chat slots (`alpha..mike`) tracked via `chat-slots.json`, `slot-task-claims`, `loop-state.mjs`.  
- Orchestrator logic uses `AGENT_CHAT.jsonl`, `chat-bus-inject.mjs`, and auto‑compact hooks.  

**TOOLS USED**  
- `/sync-drives` skill (node script).  
- PowerShell scripts: `apply-host-fleet-tuning.ps1`, `apply-pagefile-config.ps1`.  
- WMI/CIM & registry edits for pagefile, TdrDelay, power plan.  
- BIOS Flashback/M‑Flash via USB stick.  
- Windows Terminal + PowerShell 7 as runtime environment.  

**OPEN THREADS**  
- Implement multi‑chat launcher UI (20 windows, slot assignment, blink on turn).  
- Turn‑state detector (`slot-turn-<slot>.json`).  
- Drift detector & orchestrator brain to enforce progress and auto‑compact.  
- Account rotation logic: login, wait 2–3 min between starts, handle API limits.
