# cad session c7361c9f (2026-06-10, 27.1MB, spine 211KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Started Docker, WSL, Ollama (10 models), Qdrant, NIM containers; removed redundant NIMs that saturated GPU.  
- Restored fleet‑reapers & monitors; lifted migration‑freeze marker after PC upgrade.  
- Confirmed slot‑worktree routing active across all 26 slots; deferred wiki/memories backlog to quiet‑window commit.  
- Added diff‑aware `ascii‑guard.mjs` as enforced PreToolUse block (blocks em‑dash/curly‑quote in code).  
- Repaired MCP daemon; raised heap floor 4 GB→24 GB, watchdog RSS threshold 3 GB→18 GB for Blackwell hardware.  
- Hardened MCP concurrency (`U-MCP-CONCURRENCY-HARDEN`): RequestSemaphore, watchdog defer, RSS preempt logic (64 concurrent builds, 512 queue).  
- Closed G5 (boost‑stamp janitor), G1 (backlog cleanup), G10 (auto‑enable guard, freeze‑gate removed, live‑validation).  
- Added Ollama :11434 health arm to `docker-service-health-stop.mjs` (`U-GOLF-G6-OLLAMA-HEALTH-ARM`).  
- Cron‑lock sweep & orphan lock reap (`galaxy-buildout-bravo.lock`, G9).

**DECISIONS**  
- Keep fleet reapers running; remove migration freeze after PC upgrade.  
- Commit wiki/memories backlog only when fleet idle to avoid contention.  
- Enforce ascii guard on all code files; allow markdown.  
- Raise MCP heap/watcher limits (24 GB/18 GB) for 136 GB RAM system.  
- Adopt bounded ≤3‑agent ultracode workflow; use gpt‑oss:120b for grunt reads, qwen2.5‑coder:32b for drafts, 1.5b for cheap classification.  
- Replace boost‑stamp janitor with TTL‑sweep in `active-chat-priority-decay.mjs`.  
- Store noise exclusions only in `.git/info/exclude`; keep source trees intact.  
- Keep fleet‑reaper as sole crash‑critical reaper; auto‑enable guard runs on every Stop hook, never disables tasks.  
- Remove blanket `isMigrationFreezeActive()` gate from `selectReenableTargets`; keep crash‑critical tasks always degraded in aggregateHealth.  
- Treat native Ollama as separate Stop advisory, not covered by Docker probes.

**OPERATOR DIRECTIVES**  
- Start qdrant, docker, wsl, ollama, nvidia nim; ensure fleet reapers & monitors running.  
- Verify ollama models vs GPU/CPU/NVMe/128 GB RAM; target offload take‑rate 30 %.  
- Use `/startup-golf` wrapper: force‑take golf slot (evict prior owner), bind handoff to `golf-work`, then run `/fleet-reaper`.  
- After startup, execute G5 → G1 → G10 sequence without user pause; auto‑clear goals via Stop hook.  
- Run `/compact`, then build‑iter‑A: G5 → G1 → G10; bypass legacy golf‑slot write allowlist (`PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1`).  
- Always run fleet‑reaper on `/checkin-golf`.  
- Commit to own chat slot branches; resolve git tree conflicts/issues.  
- Enforce advisory hooks (em‑dash/ascii) as mandatory.

**FINDINGS/BUGS**  
- Redundant NIM containers removed; GPU idle 5 % (offload 5–9 % vs target 30 %).  
- Docker daemon initially down, then started successfully.  
- WSL running; Ollama up with 10 models.  
- Qdrant healthy; postgres/prometheus/grafana up.  
- NIM vision model crash‑loop removed due to VRAM constraints.  
- ASCII guard previously advisory and orphaned; now enforced diff‑aware block.  
- MCP daemon repaired; heap floor/watcher thresholds too low for new hardware, increased.  
- Concurrency gap: per‑request fresh `McpServer` causes memory spikes; fixed by inflight‑aware watchdog + concurrency cap/queue.  
- RequestSemaphore leaked slot on client disconnect (fixed by `acquireRequestSlot`).  
- `.active-chat-boost` premise stale – existing decay logic cleans orphan stamps.  
- 22 k untracked files are genuine commit backlog, not noise.  
- Drift test #69 failed due to missing synthesis‑cron installers; regression reverted.  
- Crash‑critical reaper disabled caused WARN; auto‑enable guard now restores it.  
- G10 initially no‑op because `isMigrationFreezeActive()` true; guard returned [] and never healed disabled Zombie Reaper v2.  
- Root cause of recurring Zombie Reaper v2 disable (Windows auto‑disable or external policy) remains open.

**DOMAIN SPECIFICS**  
- PRISM 26‑chat fleet; slot golf owns fleet‑reaper.  
- Hardware: NVIDIA RTX PRO 6000 Blackwell (96 GB VRAM), AMD Ryzen 9 9950X3D, 136 GB RAM.  
- Ollama stack: gpt‑oss:120b, gpt‑oss:20b, qwen2.5‑coder:1.5b/32b, vision ensemble; `routeModelForTask` logic.  
- NIM containers via `nim-docker-launcher.mjs`; `local‑llm‑bridge` cascades NIM first.  
- MCP server builds fresh `McpServer` per `/mcp` request; supervisor + watchdog; singleton-service-guard.  
- Migration‑freeze marker: `state/shared/MIGRATION-FREEZE-ACTIVE.flag`.  
- Slot‑worktree commit routing (26 slot worktrees).  
- `ascii-guard.mjs` diff‑aware PreToolUse block.  
- Ultracode workflow tool for bounded ≤3 agents.  
- MCP server concurrency: RequestSemaphore, watchdog defer, RSS preempt restart; 64 concurrent builds, 512 queue.  
- Fleet‑reaper tasks: Node orphan cleaner, Zombie Reaper v2, PS orphan reaper, memory pressure relief, hook janitor, cleanup orchestrator.  
- Active‑chat priority: `active-chat-priority-boost.mjs` writes finite `expiresAt`; `active-chat-priority-decay.mjs` TTL‑sweeps every Stop.  
- Fleet-task-health watch: crash‑critical list, EXPECTED_DISABLED_TASKS, aggregateHealth logic, runOnce, sampleScheduledTasks.  
- Docker-service-health-stop merges container health + new Ollama native probe.  
- Migration freeze semantics: only non‑load-bearing tasks are freeze‑excused.  
- Cron‑lock files in `.cron-locks/`; ancestry confirmation before reaping.

**TOOLS USED**  
- Docker, WSL, Ollama CLI (gpt‑oss:120b, qwen2.5‑coder:32b, 1.5b), Qdrant, NIM containers via docker.  
- PRISM scripts: `fleet-reaper-sweep.mjs`, `fleet-task-health-watch.mjs`, `docker-service-health-stop.mjs`.  
- PreToolUse hooks: `encoding-guard.mjs`, `ascii-guard.mjs`.  
- Git tooling: branch, worktrees, status, log, reflog.  
- Node scripts for slot claiming (`chat-slots.mjs`), ultracode workflow tool (bounded ≤3 agents).  
- CronCreate / CronDelete for recurring `/goal` loop.  
- Node test framework `node:test`.  
- Git hooks: `slot‑commit-enforce`, `git-add-lane-guard`.  
- Ollama LLM for brainstorming and plan generation.

**OPEN THREADS**  
- Implement inflight‑aware watchdog + concurrency cap/queue on `buildRequestServer()`.  
- Finalize MCP concurrency hardening blueprint (once workflow completes).  
- Commit wiki/memories backlog when fleet idle.  
- Ensure ascii guard fully integrated & tested across all code files.  
- Verify Ollama offload take‑rate improvement to target 30 %.  
- Monitor GPU utilization after changes.  
- Remaining golf units: G9‑rest (docker probe), G6 (gpt‑oss smoke test), G2 (gpt‑oss durability), G8 (eval verify), G4 (70‑worktree consolidation).  
- Auto‑sync `KNOWN_PRISM_TASKS` to include new synthesis cron installers.  
- Final handoff for next `/loop` iteration (build‑iter B+C) pending execution.  
- Root cause of recurring Zombie Reaper v2 disable (Windows auto‑disable or external policy) remains to be traced.
