# cad session 928a8226 (2026-06-10, 67.5MB, spine 673KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `c593b096fb`: Resilient Ollama pull fixed (em‑dash → hyphen), added DEFAULT_NUM_PREDICT=1024 to ask‑ollama, removed dangling hook references.  
- `13017de764`: Hookify frontmatter conversion script & dangling‑hook cleanup.  
- `29f2d177f6`: Generated SYSTEM‑SYNERGY‑GAPMAP‑2026_06_08.md (node‑by‑node synergy gaps).  
- `8550436c8a`: Corrected cag‑router phantom Qdrant collection label to real collections.  
- `c9fe32d8b2`: Fixed tribal embedder OOM & context‑window issues; added empty‑text guard.  
- `182788232a`: Implemented buffered JSON loader for tribal‑rerank (cap‑safe), restored PSN leg #5.  
- `5467482be4`: Updated CLAUDE.md regression log & wiki lesson for tribal index bug.  
- `a3e6d3ca97`: Fail‑open guard + closing‑`]` assert; prevented silent truncation.  
- `fb314a6fd1`: Docker‑service‑health CLI (5/5 tests).  
- `14b8b383a2`: Singleton‑service‑guard Stop hook wired to Docker health chain; caught live MCP outage.  
- `e2081e0780` / `d22681f5d2`: Guard upgraded to reap‑all+start for MCP (14/14 tests).  
- `bee977a004`: NODE_OPTIONS=--max-old-space-size=8192 added to reranker spawn; fixed default‑heap OOM on >150 MB index.  
- `ed6662f45e`: Slash‑agnostic regex for mcp‑daemon cmdline (U‑MCP‑CMDMATCH‑FIX).  
- `29fb555f13`: stop_on_hook_unregistration now emits JSON, removes false “NOT evaluated” warning (U‑HOOK‑UNREG‑PROTOCOL‑FIX).  
- `6ca2f6afd2`: Ensures ≥4096 MB heap on all MCP spawn paths; resolves recurring boot‑OOM (U‑MCP‑BOOT‑HEAP‑FLOOR).  
- `698cf12ad2`: Static list of 4 “freeze” tasks.  
- `4141daf9d8`: Fleet‑task‑health migration‑freeze marker replaces static list.  
- `U-QP-COST-BRIDGE-WIRING-TRUTH`: Router‑aware quoting awareness file for 16 cost‑bridge hooks.  
- `U-QP-AWARENESS-GEN-ROUTER-AWARE`: Quoting awareness generator router‑aware.  
- `source-lock-dispatcher-extension`: Guard extended to dispatcher tree (no retired LLM defaults).  
- `U-BW-DISPATCHER‑SCAN`: Repointed cascade defaults to live Blackwell models; closed source‑lock blind spot.  
- `U-BW-DISPATCHER‑SCAN‑WIKI`: Wiki lesson on guard‑blind‑spot root cause.  
- `U-PSN-SNAPSHOT‑REFRESH`: Regenerated stale PSN snapshot; verified 11 real P0 underwired pairs.  
- `U-QP-DIGITALTWIN‑TEST`: Added R9 reference‑value test for last untested quoting algorithm.  
- `U-QP-MARGIN‑FLOOR‑GATE`: Margin‑floor guard in QuoteEstimatorEngine.  
- `U-QP-NRE-AMORTIZE‑TEST‑FIX`: Corrected stale NRE amortization test.  
- `U-QP-MARGIN‑FLOOR‑SHOPCONFIG`: Added margin_floor_pct to ShopConfigurationEngine.  
- `ec597dbcb3`: Margin‑floor dispatcher round‑trip (7/7 tests).  
- `87d5c4bf9a`: Margin‑floor UI banner (6/6, tsc‑clean).  
- `4b0980c56a`: AI‑substrate routing matrix coverage 4/5→5/5 + completeness invariant (27/27).  
- `62b114cb7b` / `8258918f78`: ResizeObserver polyfill; unmasked hidden frontend failures.  
- `44fab1be37`: Fleet‑synergy dispatch map rows 8–15 assigned to owning slots.  
- `U-QP-CLOSED‑LOOP‑PROVENANCE‑GATE` (commit 4c12a75a8d): Blocks synthetic/placeholder outcomes from calibration promotion.  
- `U-QP-OUTBOUND-PROMOTE-GATE` (commit d294957c4d): Real outbound sold‑price alignment, fail‑closed.  
- `U-QP-CALIBRATION-FRESHNESS‑PREFLIGHT` (commit bf10035ec0): Warn on >24 h stale factors; hard cutoff → raw FMV.

**DECISIONS**  
- Fleet‑reaper ownership moved to golf/charlie per slot‑aware strategy; legacy allowlist hook disabled via env var or settings edit.  
- Unwired engines triaged per owner (wire, archive, exempt) instead of bulk wiring.  
- Adopted resilient PowerShell pull with UTF‑8 fix; set ask‑ollama num_predict=1024 to avoid empty responses.  
- Created gap‑map workflow for node‑by‑node synergy gaps; kept session‑scoped Stop hook active until all gaps assigned.  
- Cap‑safe loader chosen over rewrite due to V8 string cap (512 MiB); fail‑open bug removed.  
- Docker health guard duplicated, singleton guard extended to start missing MCP daemon; Qdrant migration postponed.  
- Focused golf‑lane effort on infra/system‑health surfaces only; unified heap sizing via `ensureHeapFloor`.  
- Replaced backslash‑only cmdMatch with slash‑agnostic regex; added port‑owner union logic.  
- Switched stop_on_hook_unregistration protocol to JSON‑stdout; implemented `EXPECTED_DISABLED_TASKS` partition in fleet‑task‑health.  
- Migration‑freeze marker file (`state/shared/MIGRATION-FREEZE-ACTIVE.flag`) replaces static list (~47 tasks).  
- Consolidated 16 cost‑bridge hooks into single router (`cost-bridge-dispatch.mjs`).  
- Quoting awareness generator made router‑aware; injected `storePath` for isolated CostSavingsTrackerEngine tests.  
- Extended source‑lock test to dispatcher tree; confirmed no retired LLM tags remain.  
- Margin‑floor guard caller‑overridable, sourced from canonical shop config; UI banner added.  
- Closed‑loop learning fail‑closed: provenance gate blocks synthetic outcomes; outbound alignment gate compares per‑part‑job FMV distribution; calibration freshness preflight prevents stale factors.  
- No high‑ROI Ollama offload in quoting galaxy; ultracode workflow for orchestration, local Ollama for grunt tasks.

**OPERATOR DIRECTIVES**  
- `export PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1` (session‑scoped).  
- Edit `C:/Users/wompu/.claude/settings.json`: remove entry for `golf-slot-write-allowlist.mjs`.  
- Claim golf slot:  
  ```
  node H:/prism/.claude/helpers/chat-slots.mjs claim \
    --chatId "$STABLE" --branch "$BRANCH" --topic "golf-work" \
    --activity "checkin" --preferSlot golf --force true --confirmRecent true
  ```  
- Run fleet‑reaper sweep: `node H:/prism/scripts/fleet-reaper-sweep.mjs --once --json`.  
- Ensure fleet reaper, monitors, Docker, Qdrant, obsidian vault app, PSN, /system‑viz, prism galaxies, front‑end/back‑end build, claude.md, memories, wiki, tribal knowledge, prism awareness, gsd, tdd, skills, scripts and hooks are running.  
- Use Ollama locally for reads/searches/coding; ultracode workflow for planning/deep reasoning.  
- `/startup-charlie /loop [10m] /goal …` – focus on closed‑loop + production readiness.  
- Continue with charlie domain work (override fleet‑scope stop).  
- Build `U-QP-BLUEPRINT-OCR-BRIDGE-ADAPTER` loop for OCR → bridge mismatch.

**FINDINGS/BUGS**  
- Hookify rule files missing YAML frontmatter → 110 warnings; non‑ASCII bytes caused “charmap” errors.  
- Four .mjs hooks referenced in `settings.json` but absent on disk → “Cannot find module”.  
- 90 engines built, 14 with suggested dispatchers; some superseded orphans (`MillPrintToProgramEngine`).  
- Academy tribal routing falls back to “general” by design (not a bug).  
- Ollama pull stalled at 12 % due to em‑dash parse error; fixed → full download of `gpt‑oss:120b`.  
- `routeModelForTask` still selects `gpt‑oss:20b` for best‑tier `search_synthesis` on Blackwell GPU.  
- V8 hard string cap (512 MiB) caused tribal index parse failure; buffered loader solves it.  
- Default Node heap (~2 GB) OOMs when parsing >150 MB JSON brain; increased to 8192 MB for reranker, 4096 MB for MCP.  
- Duplicate‑daemon pileup on MCP :3100 due to orphaned processes; no persistent supervisor at time of outage.  
- Qdrant collection mismatch in cag‑router label corrected.  
- Tribal embed index exceeded V8 string limit (~512 MiB); fixed with buffered loader & streaming JSON.  
- Embedding driver OOM (533 MB index) resolved by heap increase and empty‑text guard.  
- MCP server boot OOM due to 384 MB heap cap from `/h/.claude/bin/node` shim; now ≥4096 MB.  
- cmdMatch regex backslash‑only caused daemonCount=0; replaced with slash‑agnostic pattern.  
- stop_on_hook_unregistration gate used exit‑code protocol → false “NOT evaluated” warning; switched to JSON‑stdout.  
- /system‑viz `find` OOM at ~390 MB due to 384 MB cap; self‑bump decision pending.  
- Scheduled‑task WARN flagged disabled HW‑migration freezes as degraded; added `EXPECTED_DISABLED_TASKS`.  
- Static list of 4 freeze tasks incomplete; actual freeze spans ~47 fluctuating tasks.  
- Cost‑bridge hooks reported “0 wired” because router‑wired, not per‑file.  
- Awareness generator stale snapshot claimed “16 unwired”; now router‑aware.  
- Accounting engine lacked “next unit” pointer (ERP/QuickBooks feed missing).  
- WEDMQuoteBridgeEngine tagged `WIRE‑EXEMPT` (intentional route‑adapter).  
- CostSavingsTrackerEngine persisted to real user dir; made testable via injectable path.  
- aiReasoningDispatcher had hard‑coded defaults for retired 3b/7b/14b models → silent failures.  
- Cascade defaults pointed to retired, un‑pulled models (Blackwell) – fixed by dispatcher scan.  
- Stale PSN snapshot misidentified 11 P0 zero‑ref pairs; now corrected.  
- Margin‑floor logic missing in quoting engine – added guard.  
- NRE amortization test stale; code correct.  
- Server rate‑limit blocked multi‑agent scrutiny; deferred review.  
- ResizeObserver missing in jsdom caused hidden test failures; polyfill added.  
- Flat array quoteHistory mock violated InstantQuoteHistory contract → render crash; corrected.  
- Synthetic outcome promotion allowed live calibration write – provenance gate now blocks it.  
- OCR `$1.00` noise dominates 65 % of `jm-sold-orders.json`; engine surfaces “degenerate‑reference”.  
- Stale calibration factors >24 h silently applied – fixed by freshness preflight.  
- Blueprint→quote intake path (`shopDispatcher.ts`) feeds OCR output into bridge expecting different `BlueprintAnalysis` shape → silent GD&T/dimension loss.

**DOMAIN SPECIFICS**  
- Hookify rule files: `.claude/hookify.*.local.md` with YAML frontmatter schema; config in `settings.json`.  
- Unwired engines audit via `node scripts/audit-unwired-engines.mjs`.  
- Ollama local LLM stack on RTX PRO 6000 Blackwell (96 GB VRAM, 128 GB RAM).  
- Cost‑router tiers: cheap → balanced → strong → best.  
- gpt‑oss harmony format emits `thinking` channel before `response`.  
- nomic‑embed‑text vectors: 768‑dim (~2048‑token context window).  
- Qdrant collections: `prism_engines`, `prism_skills`, `prism_formulas`; tribal index stored in `tribal‑embed‑index.json` (10 555 entries).  
- MCP :3100 HTTP server (`TRANSPORT=http node dist/index.js`).  
- Engines/dispatchers: MCP supervisor, daemon, autostart; Ollama models (`gpt‑oss:120b`, `qwen2.5-coder:32b`); vision ensemble (`qwen3‑vl`, `llama3.2‑vision`).  
- Quoting galaxy: engines in `mcp-server/src/engines/` (Cost*, Quote*, Quoting*), cost‑bridge-dispatch.mjs, ModelRoutingEngine.ts; dispatcher tree `/mcp-server/src/tools/dispatchers/*`.  
- Awareness generator: `scripts/generate-quoting-awareness.mjs`; test harnesses Vitest (`src/__tests__/*.test.ts`) and Node.test scripts.  
- Shop configuration engine: `ShopConfigurationEngine.js` (margin_floor_pct).  
- Closed‑loop components: QuoteOutcomeFeedEngine, ActualCostEngine, QuoteAutopilotEngine; calibration file `quoting-calibration-active.json`.  
- Metrics: MCP health via :3100, node heap size, Qdrant collection sizes, MAPE, medianRatio, referenceReliable flag.  
- Paths: `jm-sold-orders.json` (reference), `quoting‑calibration‑active.json`.

**TOOLS USED**  
- Hookify parser (`config_loader.py`).  
- Node scripts: `fix-hookify-frontmatter.mjs`, `audit-unwired-engines.mjs`, `fleet-reaper-sweep.mjs`, `remove-dangling-hook-refs.mjs`.  
- PowerShell script `ollama-resilient-pull.ps1`; `ask‑ollama.mjs`.  
- Gap‑map generation (`SYSTEM-SYNERGY-GAPMAP-2026_06_08.md`).  
- Embed wiki into tribal index (`embed-wiki-into-tribal-index.mjs`), tribal‑rerank (`tribal-rerank.mjs`).  
- Buffered JSON loader in `lib/`.  
- Docker Compose, Qdrant CLI; Node spawn with `NODE_OPTIONS`.  
- PRISM hooks: `singleton-service-guard.mjs`, `stop_on_hook_unregistration.mjs`, `fleet-task-health-watch.mjs`, `populate-tribal-vault.mjs`.  
- Dispatcher scripts: `mcp-server-supervisor.mjs`, `mcp-server-daemon.mjs`, `mcp-daemon-autostart.mjs`.  
- Source‑lock test (`no-retired-llm-refs.test.mjs`).  
- Vitest, Node.test, TSC, esbuild.  
- Ultracode workflow/multi‑agent orchestration; local Ollama CLI.  
- Jest with hermetic fixtures; TypeScript compiler (tsc 16 GB heap).  
- Git hooks, CI pipeline, handoff writer.

**OPEN THREADS**  
- Resolve `routeModelForTask` bug → select `gpt‑oss:120b` for best‑tier tasks on Blackwell GPU.  
- Per‑owner triage of ~90 unwired engines (wire, archive, exempt).  
- Scheduled‑task migration freeze – operator action to re‑enable missing tasks after migration completes.  
- Re‑embed remaining ~29 k tribal entries; implement write‑side sharding once index >33 905 entries.  
- Qdrant tribal‑collection migration to replace JSON brain scaling (operator confirmation needed).  
- Persist MCP supervisor (current watchdogs reactive only).  
- Verify Ollama model list fully matches pulled hardware models; resolve stale defaults.  
- Data‑integration for `U-QP-ACCOUNTING-WIRE` (ERP/QuickBooks feed).  
- Run remaining 11 galaxies’ slots to satisfy “galaxy‑by‑galaxy” goal.  
- Confirm no dispatchers reference retired LLM tags after source‑lock extension.  
- Implement `U-QP-BLUEPRINT-OCR-BRIDGE-ADAPTER` (adapter + tests).  
- Clean up OCR `$1` noise or provide live E2/QuickBooks actuals for outbound gate.  
- Further use‑layer assessment (intake → quote → PDF bridge).  
- Address hotel analytics page failures and remaining 9 frontend test failures (hotel team action).  
- Dispatch fleet‑synergy goal rows 8–15 to owning slots (zebra/golf orchestration).
