# cad session 9a962981 (2026-06-04, 20.5MB, spine 93KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-NODECARD-OFFSET-INDEX` – 301 185 cards, 24 MB offsets table (commit ce8d69bb3f).  
- `U-NODECARD-PREFETCH-HOOK` – UserPromptSubmit hook injects card via offset index (commit 158d364493).  
- `U-NODECARD-DISPATCHER` – MCP dispatcher action `sessionNodeCardAction` shells out to `system‑viz-query.mjs node-card --json` (commit a1dfb9791f); doc‑reflect update in slot/sierra (9d7abd3f29).  
- `U-XSUB-DOCUMENTED-BY` – 38 typed edges linking system‑viz nodes to Obsidian/wiki docs.

**DECISIONS**  
- Single‑source reader (`node-card-read.mjs`) exposed via dispatcher action.  
- Lightweight offset index rebuilt on each `regen‑viz`; backfilled from fresh 193 MB sidecar.  
- Prefetch hook uses seek‑only logic (<3 s); no full graph fallback.  
- Git‑ignore artifacts; commit only source files.  
- Slot binding wrapper `/checkin-sierra` guarantees handoff, drift check, build state injection before pipeline runs.  
- Switch from `cad-fusion-live-ms0` to slot worktree: back up 10 orphan `[PSN‑SYNERGIZE]` commits (`sierra-orphan-psn-synergize`, cec11257aa) then reset forward to live tip.  
- Commit future work to `slot/sierra` in `H:/prism-slot-sierra`; merge back into shared tree later.  
- Investigate why slot‑routing hooks did not auto‑engage fleet‑wide.

**OPERATOR DIRECTIVES**  
- `/checkin-sierra`: force‑take sierra slot and run `/checkin`.  
- `/loop [5m] /goal`: continue loop, building remaining units within 5 min timeout.  
- Cut over to `slot/sierra` worktree (`H:/prism-slot-sierra`) for all future commits; stop committing to shared tree.  
- Apply doc‑reflect updates for `U-NODECARD-DISPATCHER` in slot branch.  
- Investigate root cause of slot‑routing hooks failing to enforce separation from shared tree.

**FINDINGS/BUGS**  
- Peer commit absorption broke HEAD (dispatcher import missing); resolved by committing `sessionNodeCardAction.ts` to shared tree then moving into slot worktree.  
- Stale `index.lock` from interrupted reset prevented checkout of slot worktree; cleared manually.  
- Dead data in `node-capability-index.json` (0/302 k matches) removed.  
- `find-cache.json` stale; now use fresh `system‑graph‑index.json`.  
- Developers committing to shared tree caused index.lock contention and peer‑absorption.  
- Slot‑routing enforcement hooks (`main-tree-write-block`) did not auto‑engage despite `chat-slots.json[sierra].branch` set to `slot/sierra`.  
- Windows H: drive checkout of 8,771 files exceeded background‑task window; reset truncated leaving 587 partial files + orphan lock.

**DOMAIN SPECIFICS**  
- Engines/Dispatchers: MCP dispatcher (`sessionDispatcher.ts`) with `sessionNodeCardAction`.  
- Actions/Hooks: UserPromptSubmit hook `node-card-prefetch-inject.mjs`; reader lib `node-card-read.mjs`; offset index builder `build-card-offset-index.mjs`.  
- Metrics: 301 185 cards, 24 MB offsets; token cost ~200 tokens/card vs ~186 K for full graph load.  
- Unique to this galaxy: Blackwell GPU usage planned but not wired; focus on CPU‑friendly primitives.  
- Workflow: per‑slot branches (`slot/*`) versus shared live tree `cad-fusion-live-ms0`.  
- Slot routing enforced via `chat-slots.json`, `main-tree-write-block` hooks, slot‑worktree model.  
- Environment: Windows H: drive, background-task window limitations.

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
- Pipelines: `/checkin`, `/startup`.  
- Workflow tool: `ultracode` for parallel agent mapping.  
- Node modules: `system-viz-query.mjs`, `node-card-read.mjs`, `build-card-offset-index.mjs`.  
- Build & test: TypeScript compiler, Vitest, `tsc`.  
- Git operations: `reset --hard`, `commit`, `sync`, `cutover`; shared‑tree and slot worktrees.

**OPEN THREADS**  
- Final integration of dispatcher action into `slot/sierra` worktree.  
- Commit doc‑reflect updates for `U-NODECARD-DISPATCHER` in slot branch.  
- Future GPU‑accelerated “near” mode for node‑card queries (planned).  
- Monitoring and cleanup of stale index locks on slot worktree to prevent aborts.  
- Decision on handling 10 orphan `[PSN‑SYNERGIZE]` commits (integrate or discard).  
- Investigate why slot‑routing hooks did not auto‑engage fleet‑wide; implement fleet‑wide enforcement.  
- Resolve Windows checkout truncation issue to prevent future partial resets.
