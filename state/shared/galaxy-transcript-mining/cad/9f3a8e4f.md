# cad session 9f3a8e4f (2026-05-25, 69.1MB, spine 239KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `42b44bd00a` – HPM half‑wire fix (dispatch case + slimmer rewrite) – slot: india.  
- `4c3c46f70a` – HPM test suite, 15/15 PASS – slot: india.  
- `21a01b4e11` – wiki lesson on HPM bug class – slot: india.  
- `219eb69789` – EWMA triplet for MIT 2.830 – slot: india.  
- `4622619d48` – batch extractor & stub index (94 courses) – slot: india.  
- `fa0c809c1c` – 96 baseline spec files + master index – slot: india.  
- `865fa9fccc` – widened discovery script, master index, augmentation generator – slot: india.  
- `a5d7c15a8f` – wiring gate for 5 PART‑TYPE‑STACK actions in calcDispatcher – slot: india.  
- `6f289da344` – envelope drift fix (misattributed to lima) – slot: india.  
- `6721d8cfdd` – PostProcessorUnificationEngine wire (4 actions + tests) – misattributed to lima.  
- `1bdcbff625` – Consolidator + JSON/MD index for CAD/CAM corpus (delta & kilo).  
- `2256216327` – Tribal‑JSONL + Wiki indexes per book (manual page‑cited tips from 4 books).  
- `13362c6e7f` / `54bd1e47b7` – System‑Viz roost (`ghost.cadcam_training_corpus.*`) – 210 leaf nodes, 2 pivots.  
- `972d84093e` – Batch extractor stubs (24 heuristic tips flagged needs_curation:true).  
- `c2bacb4498` – Coverage flagging: all consolidated PDFs now have extracted/pending status.  
- `312c830c0f` – In‑process engine consumer (`AIResourceLearningEngine.getTribalGuidanceForEngine()`).  
- `e96fae3caa` – PRISM AI action `ai_cadcam_corpus_pointers`.  
- `4b477365cd` – Added 8 InventorCAM SWARF tips (swf24‑001…008); books 116, tips 485, nodes 596; PDF coverage 210/210.  

**DECISIONS**  
- India slot remains post‑processor specialist; MIT extraction delegated to same slot.  
- Adopt derived‑artifact model: spec files, augmentation JSONs, node memories gitignored; only emitter scripts and master index committed.  
- `/system-viz` is the single source of truth for PSN visibility; nodes surface automatically on regen.  
- Implement 5‑lane discovery (catalog, recursive MIT walk, course dirs, PDF inventory, JM Die) to capture >1000 courses.  
- Commit wiring gate in calcDispatcher to satisfy `stop_on_unwired_assets`.  
- Two‑tier extraction: manual page‑cited tips + batch pdftotext stubs for remaining PDFs.  
- Introduced four edge types (`bridge-to-engine`, `enriches-engine`, `feeds-dispatcher`, `feeds-training`).  
- Normalized PDF paths to forward slashes; cron loop `/loop [5m] /goal` for continuous coverage discovery.  
- Dual‑directory scanning with canonical path precedence; dedupe by `tip.id`; ID collision detection and renaming.  
- Enforced git commit prefix `[BOOTSTRAP-SLOT-ENFORCE]` and lock‑aware retry loop (10 iter).  
- Prioritize books/pdfs first per Stop hook; cron re‑arms every 5 min until condition cleared.

**OPERATOR DIRECTIVES**  
- “A session‑scoped Stop hook is now active with condition: …” (repeated 10×).  
- `/checkin-india reorientate with all india work from 5/22/2026‑5/23/2026`.  
- `/goal [ extract data from all college courses to be utilized in PSN and Prism App | goal clear: all courses accounted for ]`.  
- `/loop [5m] /goal` – repeatedly trigger extraction goal every 5 min.  
- `/goal "[ extract data from sources … ]"` – user‑defined extraction target.  
- `CronDelete e12e2726` (cancelled) – stop recurring loop once coverage complete.

**FINDINGS/BUGS**  
- HPM half‑wire: dispatch case missing, slimmer expected wrong shape → fixed.  
- MitOcwResourceResolverEngine: joint‑course cross‑listing bug; added `crossListing`.  
- Misattribution hazard: peer git‑add races caused commits to land under lima; mitigated with atomic add/commit.  
- `stop_on_unwired_assets`: 5 PART‑TYPE‑STACK actions missing in calcDispatcher – resolved.  
- Shared‑tree lock contention on `H:/PRISM/.git/index.lock` prevented bulk spec commit; current state relies on derived artifacts.  
- ID collision (swg‑101…107) → renamed to swg‑901…908.  
- Git lock contention in iter 55; resolved with 10‑iter retry loop.  
- Ingestion‑cache root guard hook triggered mid‑session; fixed by moving canonical path and updating generator.  
- Unicode apostrophe “Mill Operator’s Manual” handled correctly.  
- Ollama hook false positives (“missing closing brace”, “pdf_path contains backslashes”) – ignored as JSONL per‑line valid.  
- 30 s `pdftotext` timeout workaround: use `--pages 1` for large catalogs (Sandvik, AccuPro, Iscar).  
- Path mismatch backslashes → forward slashes fixed in iter 43.  
- `parseArgs` short‑circuit bug (`0 || 100`) fixed in iter 31.  
- Large catalog PDFs (>30 s parse timeout): 6 PDFs timed out; pending retry or alternative parser.  
- Pending PDF backlog: 178 PDFs still marked pending; batch extractor produced 24 stubs.

**DOMAIN SPECIFICS**  
- Engines: `PostProcessorUnificationEngine`, `HybridPostMergeEngine`, `MitOcwResourceResolverEngine`, `LatheCorePhysicsEngine`, InventorCAM modules (SWARF, 3D HSM/HSR), Mastercam Dynamic Milling, hyperMILL, Autodesk Fundamentals, CNCCookbook.  
- Skills/commands: `/college-extract <slug>`, `/checkin-india`, `/goal`, `/loop [5m] /goal`.  
- Dispatchers: `devDispatcher` (z.enum), `calcDispatcher`.  
- Scripts: `auto-college-course-spec-emit.mjs`, `generate-college-course-features.mjs`, `mit-course-triplet-extractor.mjs`, `consolidate-cadcam-corpus.mjs`, `generate-pdf-course-bridge-features.mjs`, `extract-batch.js`, `generate-extracted-pdf-tips-features.mjs`, `merge-augmentations.mjs`, `regen-viz.mjs`.  
- PSN legs: Wiki, Memories, Tribal, System‑Viz, Engines, Algorithms, Formulas, NN/GNN, PRISM AI.  
- Nodes: `node_course_*`, `node_formula_mit_*`, `ghost.college_courses`, `ghost.cadcam_training_corpus.*`, `ghost.extracted_pdf_tips`.

**TOOLS USED**  
- PRISM CLI wrappers (`/checkin-india`).  
- Git tooling (atomic add/commit, lock‑aware retry loop, `[BOOTSTRAP-SLOT-ENFORCE]` prefix).  
- `/system-viz` regen scripts.  
- Playwright + WebFetch for live extraction.  
- Jest/Vitest for unit tests.  
- CronCreate/CronDelete for loops.  
- `pdftotext -layout` (Poppler) with `--pages 1`, `-f N -l M`.  
- Vitest, `regen-viz.mjs`, `merge-augmentations.mjs`.  
- AIResourceLearningEngine API.

**OPEN THREADS**  
- Persist 1008 spec files as individual git entries once shared‑tree lock clears.  
- Run `/college-extract` on each of the 1008 courses to wire engines, algorithms, formulas.  
- Final PSN integration: ensure all new nodes surface in `ghost.college_courses` and are consumable by Prism App UI.  
- Complete remaining book chapters: Autodesk (chapters 4‑6, 8‑9), Planchard (chapters 3‑11), InventorCAM 3D HSR, Geodesic Machining, Multiaxis Roughing Pt 1/2, CS50 Notes, Basic_3D_Machining, Autodesk_CNCBOOK.  
- Validate and commit newly extracted SWARF tips into wiki (`inventorcam-swarf.md`).  
- Integrate remaining 5‑axis InventorCAM modules (HSR, Geodesic, Multiaxis Roughing).  
- Finalize G/M code reference integration for all post‑processors.  
- Ensure cross‑engine bridge mappings complete for newly added tips.  
- Scale batch extractor to clear 178‑PDF backlog; refine confidence scoring; handle large catalog PDFs.
