# cad session 32c4ef87 (2026-06-11, 35.8MB, spine 256KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- AI‑synergy audit instrument – `97dbe1a46b` (24 tests + generator).  
- Fleet awareness hook – `cc5ec2ca05`.  
- 34 SOUL.md files from real data – `a222ab5b7a`.  
- Dispatcher‑wiring measurement – `e7adf32cba`.  
- Generic reasoning bridge (live Ollama, 3 sources + grounded answer) – `19fafee8b1`.  
- RAG retrieval hybrid upgrade – `aa45a70d9a`.  
- CAG cache with content‑fingerprint invalidation – `d65aa580c0`.  
- Dense hybrid retrieval (sparse → nomic‑embed‑text 768d → cosine → RRF fusion) – `caa0c29cb8`, `c773d1af96`.  
- LoRA self‑improvement emitter – `e165c015a7`.  
- NN/GNN node‑features for all 34 galaxies – `c9ea46b9f1`.  
- QuotingActualOutcomeLoaderEngine + prism_quoting:closed_loop_provenance_check – `6b0f4d2718`.  
- Vault↔AI synergy validated live; 19 stale synthesis brains regenerated – `67e2717b57`.  
- RAG‑hybrid partial‑dense improvement – `86e7e6b77e`.  
- Silent‑failure guard in GNN retrain fixed (all 61/61 tests green).  

**DECISIONS**  
- Measure first, then build real capability; avoid stubs.  
- Reframe `ownsOrWiresAi` → `max(owns,wires)`; cross‑substrate weights: owned/documented 0.5 each, consensus/embeds 0.1.  
- Use existing `lexical-rerank.mjs`; no BM25 duplication.  
- One generic bridge & RAG per galaxy – no 34 clones.  
- Commit with bootstrap token; enforce slot‑commit rules.  
- Build‑once, ship‑once strategy for all AI components.  
- Prioritize code‑completable fixes over GPU training; GPU jobs stay background.  
- Local Ollama fan‑out for bulk mechanical work to avoid Anthropic limits.  
- Persist enriched soul features in GNN embedding source for every retrain.  
- Charlie’s domain is QUOTING; NN/GNN/LoRA belong to India (GPU torch stack missing).  

**OPERATOR DIRECTIVES**  
- Use Hermes agentic coding / Workflow fan‑out for design/verification.  
- Ensure synergy across Obsidian vault, Hermes, PSN, prism awareness, CLAUDE.md, SOUL.md, memories, wikis.  
- Target AI‑synergy score 1.0.  
- Lift cross‑domain restriction: build in all galaxies.  
- Improve AI systems with full context of each galaxy’s CLAUDE.md, memories, wikis, souls, prism awareness; wire to Obsidian vault and Hermes/Zulu.  
- Re‑rank task queue by ROI.  
- Utilize Ollama for searches/reads/data gathering; offload easier tasks to Sonnet or Haiku.  

**FINDINGS / BUGS**  
- Engines live FLAT in `mcp-server/src/engines/*.ts`; galaxy dirs doctrine only – fixed via name‑attribution.  
- Edge node‑id forms `eng.<g>` & `ghost.galaxy.<g>` caused cross‑substrate under‑count – corrected.  
- AwarenessSurface 1/34 → resolved with per‑galaxy `AWARENESS.md`.  
- Discoverability shortfall in 9 galaxies fixed by injecting AI‑capabilities into `MEMORY.md`.  
- Duplicate BM25 replaced by verified `lexical-rerank.mjs`.  
- Disk‑full (`ENOSPC`) blocked dense arm; freed C: temp space.  
- GNN retrain lifecycle OOM (~381 MB); fixed with `--max-old-space-size=8192` and heap bump for child spawn.  
- H2GCN heterophily hops = 3 silently no‑op at 6000‑node cap – trainer bug, pending fix.  
- Silent‑failure bug in GNN retrain (R12) fixed; no silent exits now.  
- H2GCN AUROC lift disproven on 768d substrate (+0.003 vs claimed +0.138).  
- LoRA fine‑tune requires torch venv – GPU infra missing on this host.  
- RAG‑hybrid dense arm degraded when embed fails; partial‑dense fix added.  
- CAG cache validated (cached:true after first run).  
- Vault↔AI synergy audit: all 34 galaxies “strong” (no weak/partial).  
- Synthesis‑staleness hash bug identified – belongs to India/Sierra infra, not charlie.  

**DOMAIN SPECIFICS**  
- PRISM architecture: 34 galaxies (22 slot‑mapped, 12 slotless).  
- AI‑synergy dimensions: discoverability, ownsOrWiresAi, vaultSynergy, crossSubstrate, awarenessSurface.  
- Reasoning bridge uses Ollama (`qwen2.5-coder:32b`) with 3 sources + grounded answer.  
- CAG cache keyed by galaxy+model+question; invalidated on doctrine edit (content fingerprint).  
- Engines/Actions: deep‑reasoning bridge (`galaxy-context-retrieval.mjs`), RAG sparse (`lexical-rerank.mjs`), dense hybrid (`galaxy-dense-rerank.mjs`), CAG cache, LoRA emitter, GraphSAGE GNN node‑feature builder.  
- Metrics: AUROC, macro‑F1, Brier for GNN; soulGrade/claudeGrade from audit.  
- Paths: `gatherGalaxyDocs` includes `SOUL.md`; embedding source `node-embeddings-768d.jsonl` updated with galaxy rows; retrain lifecycle `nn-graph-retrain-lifecycle.mjs`.  

**TOOLS USED**  
- PRISM scripts: `ai-synergy-audit-lib.mjs`, `generate-galaxy-awareness.mjs`, `inject-galaxy-ai-capabilities.mjs`, `reasonForGalaxy.mjs`, `build-galaxy-ai-bridge-registry.mjs`, `galaxy-reasoning-bridge.mjs`, `hybridRetrieve.js`, `generate-quoting-awareness.mjs`, `audit-ai-synergy.mjs`, `galaxy-synthesis-refresh.mjs`, `galaxy-dense-rerank.mjs`.  
- `node:test` for unit tests.  
- Hermes Workflow (6 agents) for design/verification & dedup detection.  
- `lexical-rerank.mjs` and `hybrid-retrieval.mjs` from PRISM core.  
- Ollama fan‑out (`scripts/lib/ollama-fanout.mjs`) for local GPU inference.  
- Sonnet subagents for builds, tests, scrutiny.  
- Git with bootstrap token; slot‑enforced commits.  

**OPEN THREADS**  
- Free disk space on C: (temp dir, large git‑ignored caches) to enable further tool execution.  
- Build dense embedding rerank arm (`PRISM_GALAXY_RAG_DENSE`) – code‑completable but pending due to disk.  
- Schedule NN/GNN AUROC lift training runs (multi‑hour GPU jobs).  
- Schedule LoRA retrain runs (multi‑hour GPU jobs).  
- ROI #2: `generate-quoting-awareness.mjs` – bug fixed, pending final commit.  
- ROI #3: wire `prism_quoting:training_status`.  
- ROI #4: feed `jm-vendor-cost-index.json` into training loop.  
- ROI #5: add `prism_quoting:cost_savings_*` action (0 consumers yet).  
- Synthesis‑staleness hash bug fix spec for India/Sierra infra.  
- GPU‑based AI systems (NN/GNN/LoRA) – handoff to India’s Blackwell host.  

All code‑completable AI‑system components are shipped, scrutiny‑passed, and integrated; remaining work is GPU training and the H2GCN silent‑no‑op bug fix.
