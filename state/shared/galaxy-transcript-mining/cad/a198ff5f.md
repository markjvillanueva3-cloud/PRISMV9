# cad session a198ff5f (2026-05-29, 53.3MB, spine 280KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**

- `d6e5e4109f`: added 4 new galaxies (romeo/uniform/victor/xray); galaxy dirs now 24/26 mapped.  
- `34af429f9e`: self‑build kit, 24 per‑slot dispatch briefs, 48 skill wrappers (`galaxy-buildout-<slot>`, `smart-<slot>`).  
- `96a5496b37`: HTML twin for build kit; master protocol `PER-SLOT-GALAXY-BUILD-KIT.md`.  
- `18ca66fb61`: India meta‑bus spec, stop‑auto‑capture hook, skill‑recommendations JSON, wiring in `.claude/settings.json`.  
- `92c55ee62f`: 11 galaxy CLAUDE.md files appended with closed‑loop section; helper script for idempotent append.  
- `ccd1d9f82b`: HP bar accuracy fix and system‑viz regen commit.  
- `aeaeff6ca4`: deep assessment of cache‑miss mitigation #1 (3 noisy hooks disabled).  
- U‑MCP‑SHARDING‑MS0 envelope scaffold – unit decomposition for per-domain shard milestone.  
- U‑PLB01: `/ready` endpoint, `waitForReady()` bridge logic, MP‑5h quota bar, and `outcome-bus-auto-tap.mjs`.  
- U‑PLR01: canonical source refresh; HP bar uses `ctxMaxEffective`; MP‑bar shows 5 h Anthropic quota.  
- U‑PSCL02: India meta‑bus doctrine wired into 11 galaxy CLAUDE.md files (mill, lathe, wedm, blueprint‑vision, quoting, business, speed‑feed, post‑processor, cad, cam, india).  
- U‑PSCL01: `state/shared/specs/PER‑SLOT‑CLOSED‑LOOP‑INTEGRATION‑2026‑05‑28.md`, `outcome-bus-auto-tap.mjs`, `stop-auto-capture-per-slot.mjs`.  
- U‑PSCL04: extended India wire to 9 additional slots (20/26 total).  
- Opus 4.8 fleet‑wide launch flag (`--model claude-opus-4-8[1m] --fallback-model claude-opus-4-7[1m]`) added to `slot-tab-boot.ps1`.  
- Galaxy‑first‑on‑resume override – forces `ForceCheckin` if galaxy incomplete.  
- Codex CLI registered (`claude mcp add codex …`) and Octopus probe cache refreshed (Claude, Ollama, Gemini live).  
- MASTER-BRAIN-TEMPLATE.md + generator edits (FAIL 12/13 gates) – alpha owns canonical Obsidian brain template.  
- Drop of `prism_safe` per‑chat monolith; fleet now runs a single shared HTTP backend (`:3100`).  
- MCP Tier‑1 domain filter committed (env‑based `MCP_TOOL_DOMAINS`, fail‑open).  
- CWD self‑resolution activation added to bridge (`PRISM_BOOT_SLOT` precedence tier).  
- U‑MCP‑ROLLOUT patch staged (launcher env emission) – pending golf’s launcher commit.  
- Per‑galaxy memory routing built: write‑time slot→galaxy mapping, `obsidian-memory-sync.mjs` updated, `reconcileGalaxies()` added; galaxy dirs created and gitignored.  
- df8688d1a2 U‑MCP‑ACTIVATE – PRISM_BOOT_SLOT tier filter now fires fleet‑wide on next launch.  
- 63bb5048fe U‑GALAXY‑MEMORY – write‑time routing by slot, reconcileGalaxies quarantine; 797 synced / 93 routed / 0 errors.

**DECISIONS**

- Adopt per‑domain shard architecture (Option B): single master MCP; shards own heavy engines, master owns shared state.  
- Consolidate MCP servers to 4 domain slices via filter on shared backend (`:3100`); no new processes.  
- Use native `claude.exe`; rewrite LAUNCH-PRISM-FLEET.bat to call `slot-tab-boot.ps1 -File`.  
- Reconfigure fleet layout to 2×2 quadrant grid (24 chats, 6 per window) with alphabetical groups: NW‑alpha, NE‑foxtrot, SW‑kilo, SE‑papa.  
- Add four new domain slots (romeo, uniform, victor, xray); update `CHAT-SLOT-DOMAINS.md`.  
- Implement master index / graph / `/system-viz` usage policy; enforce via memory “always use master index/graph when searching codebase.”  
- Deploy per‑slot self‑build instructions so each chat builds its galaxy on launch (galaxy‑first‑on‑resume).  
- Use fleet‑reaper as sole watchdog; eliminate 20 per‑chat supervisors.  
- Ship `/ready` endpoint and `waitForReady()` bridge logic to avoid cold‑start disconnects.  
- Deploy Opus 4.8 fleet‑wide with 4.7 fallback via launcher flag.  
- Wire India meta‑bus closed‑loop (outcome bus, NN‑GRAPH retrain, tribal capture, calibration monitor) to all slots.  
- Alpha becomes sole owner of master Obsidian brain; others clone/fine‑tune.  
- Write‑time routing of memories by slot; keep `PRISM_BOOT_SLOT` env for per‑slot tool filtering with fail‑open fallback.  
- Blocking 3‑of‑3 gate remains Claude‑only; octopus/codex advisory only.  
- For heavy workloads, audits, deep dives run synchronous octopus consensus (`prism_ai consensus_decide`) in addition to Claude blocking layer; never let octopus/codex block a Stop.  
- Default WT profile PS5.1 → consider switch to PS7 to avoid fallback/python errors.  
- Fixed `/checkin‑<slot>` bare slash command by wrapping in natural‑language prompt.

**OPERATOR DIRECTIVES**

- Fix LAUNCH-PRISM-FLEET.bat; ensure PowerShell 7 launch, alphabetical order, resume chats from 5/27/2026.  
- Create desktop icon pointing to updated launcher.  
- Generate per‑slot self‑build instructions (galaxy‑first‑on‑resume).  
- Review work per chat slot with parallel agents (4 + me).  
- Update master index / graph / system‑viz usage policy; enforce memory “always use master index/graph when searching codebase.”  
- Ensure Ollama, Docker, NVIDIA NIM running; fix terminal UI HP context and MP bar.  
- Retire 32 GB work PC; use 128 GB RAM.  
- Fill all gaps before fleet launch; activate Opus 4.8 fleet‑wide.  
- Wire India doctrine to all non‑hygiene slots; use forge slash commands automatically.  
- Keep only Claude flow tools mapping to PRISM primitives.  
- Commit FLEET‑EXPANSION‑PLAN; test dynamic workflow and Obsidian brain integration.  
- Add kernel units to alpha (PSK); reduce MCP servers to 4.  
- Execute U‑MCP‑ROLLOUT; confirm CWD activation in production.  
- Run octopus cross‑vendor pass on two commits; codify rule into CLAUDE.md + memory.

**FINDINGS/BUGS**

- LAUNCH-PRISM-FLEET.bat `;` collision → error 2147942402; patched to use `-File`.  
- `.cmd` wrapper caused claude.cmd errors; switched to native `claude.exe`.  
- Pre‑compact auto‑trigger fired at 880K/940K regardless of override; added PRECOMPACT_SOFT_TOKENS/HARD_TOKENS.  
- Duplicate resume race on live alpha due to stale PID; added JSONL mtime check.  
- Tier 1 missed reaped‑claim chats; introduced Tier 1.5 scan.  
- Regenerator still emitted `;` collision pattern; patched to use `-File`.  
- OneDrive desktop redirect caused wrong .bat path; fixed by creating new icon on local Desktop.  
- 8 broken Stop hooks (Class A/B) resolved by removing stale wiring and correcting schema.  
- Ollama offline during sessions; restarted successfully.  
- MP bar displayed fleet‑wide quota only; redesign options proposed.  
- `outcome-bus-auto-tap.mjs` missing – gap identified.  
- OOM leak (error_ledger_recall_similar) mitigated by 4 GB heap bump; root cause remains.  
- MCP disconnects on cold start fixed via `/ready` endpoint and `waitForReady()`.  
- Outcome‑bus dead‑letter: no consumer reads `state/shared/outcome-bus.jsonl`; bridge pending.  
- 33 dormant T0 hooks identified (unused).  
- Codex CLI registered but not authenticated; login pending.  
- Octopus voices missing due to auth; Grok not reachable.  
- Docker vs native Ollama conflict; GPU contention, rewriter offload 50/50 skip rate.  
- `migrate-memories-to-galaxies.mjs` missing; classifier misroutes ~79% of memories to “business”.  
- `prism_safe` redundant per‑chat monolith; dropped in favor of shared HTTP backend.  
- CWD activation inert in production (launcher always starts at H:/prism).  
- Master-index back‑pointer and recall verification absent from Obsidian brain; audit flagged as declared‑not‑working on 2/4 axes.  
- AgentType “Explore” incompatible with schema → zero‑token fast‑fail in dynamic workflows.  
- `/checkin‑<slot>` unknown skill args error resolved by natural‑language wrapper.  
- Tab titles overwritten by claude.exe; fixed via WT `suppressApplicationTitle: true`.  
- Launcher uses PS7 but default profile is PS5.1 → fallback causes python errors.

**DOMAIN SPECIFICS**

- Slots: alpha, bravo, charlie, delta, echo, foxtrot, golf, hotel, india, juliett, kilo, lima, mike, november, oscar, papa, quebec, romeo, sierra, tango, uniform, victor, whiskey, xray, zulu.  
- Dispatchers/Boot Scripts: `slot-tab-boot.ps1`, updated `Launch-PRISM-FLEET.bat`, `scripts/regenerate-launch-fleet.mjs`.  
- Metrics: HP bar (quota burn), MP bar (5 h Anthropic quota / cache hit rate / SVI Ψ), system‑viz graph, master index.  
- Paths:  
  - `C:\Users\wompu\Desktop\LAUNCH-PRISM-FLEET.bat` (updated).  
  - `H:\Tools\prism-fleet\Launch-PRISM-FLEET.ps1`.  
  - `H:\Tools\nodejs\node_modules\@anthropic-ai\claude-code\bin\claude.exe`.  
  - Slot worktrees: `H:\prism-slot-<slot>`.  
  - Shared state: `state/shared/handoffs`, `CHAT-SLOT-DOMAINS.md`, `state/shared/outcome-bus.jsonl`.  
  - MCP bridge: `mcp-http-bridge.mjs` → `http://127.0.0.1:3100/mcp`; per‑chat stdio servers (`prism_safe`, `claude-flow`, `shadcn`).  
- Slot-to-galaxy mapping via `SLOT_GALAXY_MAP` in `slot-context-bundle-inject.mjs`.  
- Galaxy buildout brief Markdown at `state/shared/per-slot-galaxy-buildout/<slot>.md`.  
- India meta‑bus surfaces: outcome-feedback bus, NN‑GRAPH retrain, tribal capture, calibration monitor.  
- HP bar uses `ctxMaxEffective` from sidecar; MP bar shows 5 h Anthropic quota.  
- PostToolUse hook auto‑publishes to `state/shared/outcome-bus.jsonl`.  
- Tool domains: compute, cognitive, devops, business, frontend; always‑included dispatchers (session, context, etc.).  
- Memory routing: flat-type directories in `knowledge/memories/`; per-galaxy dirs under `memories/<galaxy>/` created by write‑time slot mapping.  
- Obsidian brain axes: PULL, PUSH, MASTER‑INDEX back‑pointer, RECALL; enforced via generator and brief templates.  
- Kernel units: PSK (COMMAND-KERNEL-MS0) tied to brain/OS substrate; 28 of 29 units complete.

**TOOLS USED**

- PRISM scripts: `Launch-PRISM-FLEET.ps1`, `slot-tab-boot.ps1`, `scripts/regenerate-launch-fleet.mjs`.  
- Hooks/Skills: `stop-auto-capture-per-slot.mjs`, `outcome-bus-auto-tap.mjs`, `PER‑SLOT‑CLOSED‑LOOP‑INTEGRATION‑2026‑05‑28.md`, `.claude/settings.json` wiring.  
- Skill wrappers: `.claude/commands/galaxy-buildout-<slot>.md`, `.claude/commands/smart-<slot>.md`.  
- Generation scripts: `generate-per-slot-skill-wrappers.mjs`, `generate-per-slot-galaxy-buildout-files.mjs`, `scripts/generate-per-slot-mcp-config.mjs` (planned).  
- UI tweaks: `statusline.mjs` (HP bar), system‑viz regen script.  
- MCP server & bridge: `mcp-server`, `mcp-http-bridge.mjs`.  
- Codex CLI (`claude mcp add codex`) and Octopus consensus engine (`consensus‑queue.jsonl`, `prism_ai consensus_decide`).  
- Forge commands / slash commands (auto‑invoke).  
- Dynamic workflow harness: `workflow` keyword, `skill-auto-trigger.mjs`, `extract-skill-triggers.mjs`.  
- Memory sync script: `obsidian-memory-sync.mjs`.  
- Generator templates: `state/shared/specs/FLEET-EXPANSION-PLAN.md`, `MASTER-BRAIN-TEMPLATE.md`.  
- Windows Terminal setting tweak: `suppressApplicationTitle`.

**OPEN THREADS**

- Ship `outcome-bus-auto-tap.mjs` and implement outcome‑bus consumer that reads `state/shared/outcome-bus.jsonl`, triggers tribal capture, NN‑GRAPH retrain, calibration monitor.  
- Finalize MP bar redesign; decide on A/B/C options.  
- Ensure Ollama, Docker, NVIDIA NIM running/stable; unify GPU usage (container vs native).  
- Validate all 8 broken Stop hooks remain resolved after future regenerator runs.  
- Confirm master index / system‑viz policy enforced in every slot’s CLAUDE.md.  
- Monitor per‑chat MCP architecture; keep single‑master until shard/per‑chat solution tested.  
- Run `codex login` and complete Octopus probe (add Grok, XAI_API_KEY) to bring all five voices live.  
- Decide on Docker vs native Ollama; unify GPU utilization.  
- Evaluate replacing HTTP bridge with pure stdio child per slot for MCP.  
- Audit 33 dormant T0 hooks; remove or activate as needed.  
- Optional: add trigger‑suggest hook for “workflow” prompts in dynamic workflows harness.  
- Plan Qwen2.5‑coder NIM deployment to replace Ollama offload.  
- U‑MCP‑ACTIVATE pending golf’s launcher commit to emit `$env:PRISM_SLOT_GALAXY`.  
- ReconcileGalaxies cleanup review passed; ensure stale galaxy removal.  
- Launch readiness: confirm all slots build galaxies with new brain template and tool filter; no critical blockers.  
- Complete octopus cross‑vendor pass on two commits after rate‑limit resolves.  
- Codify rule into CLAUDE.md + memory; persist in wiki/knowledge base.  
- Verify/optionally switch WT default profile to PS7 if desired.  
- Capture boot script changes (not repo‑tracked) in a wiki entry or patch.
