# cad session 0170cb0a (2026-05-20, 0.8MB, spine 7KB, 1 slice(s), model gpt-oss:20b)

**DECISIONS**  
- Fleet‑reaper ownership moved from *alpha* → *golf* (2026‑05‑16) to centralize fleet hygiene.  
- `/checkin-golf` now **always** runs the fleet‑reaper; this step is non‑skippable.  
- Golf slot is treated as a normal work slot—no `--golf` flag in `chat-slots.claim`.  
- Legacy write‑allowlist hook can be bypassed per session (`PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1`) or disabled globally by removing its entry from `settings.json`.

**OPERATOR DIRECTIVES**  
- Claim golf slot with:  
  ```bash
  node H:/prism/.claude/helpers/chat-slots.mjs claim \
    --chatId "$STABLE" --branch "$BRANCH" --topic "golf-work" \
    --activity "checkin" --preferSlot golf --force true --confirmRecent true
  ```
- Before writing in the golf slot, either set `PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1` or edit `settings.json` to delete the `golf-slot-write-allowlist.mjs` PreToolUse entry.  
- If the hook remains enabled, any Write/Edit/MultiEdit outside the legacy hygiene allowlist will error.

**FINDINGS/BUGS**  
- Scheduled task “PRISM Fleet Reaper” was disabled (4 missed runs); auto‑enabled by `golf-slot-reaper-guardian.mjs`.  
- Docker daemon down; Ollama reachable but coordinator below pressure floor.  
- Fleet‑reaper sweep found 1 unowned orphan candidate, which the Monitor reaped.  
- Branch `cad-fusion-live-ms0` diverged 381 ahead / 1 behind origin; 16 411 git changes uncommitted.

**DOMAIN SPECIFICS**  
- Fleet‑reaper JSON metrics:  
  - `slots["owned-by-crashed"]`, `leftover-bash-task`, `unowned` (reap candidates).  
  - `softRelief.{priorityDemoted,workingSetTrimmed,rssReclaimedBytes}`.  
  - `gpu.{freeMb,utilizationPct}`; `ollama.{reachable,loaded[]}`.  
  - `coordinator.{shouldPrewarm,prewarmFired,hintWritten,thresholdDelta,hintMode}`.  
- Slot binding script uses `chat-slots.mjs` reclaim/claim with STABLE, BRANCH, TOPIC “golf-work”.  
- Scheduled task details: TaskName “PRISM Fleet Reaper”, Principal=SYSTEM, RunLevel=Highest, LastResult=0.  
- Branch divergence and git uncommitted changes tracked for context.

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs` (reclaim/claim), `fleet-reaper-sweep.mjs`, `golf-slot-reaper-guardian.mjs` hook, `golf-slot-write-allowlist.mjs` PreToolUse.  
- Node scripts executed via `node H:/prism/.claude/helpers/...` and `H:/prism/scripts/fleet-reaper-sweep.mjs`.  
- PowerShell command to enable scheduled task (`Enable-ScheduledTask`).  
- Environment variable `PRISM_GOLF_WRITE_ALLOWLIST_BYPASS`.

**OPEN THREADS**  
- Restart Docker daemon if it remains down.  
- Verify that the “PRISM Fleet Reaper” scheduled task stays enabled after guardian hook; consider permanent enablement.  
- Decide whether this PC is a *home* or *work* tier and run `/fleet-reaper-home` or `/fleet-reaper-work` once to lock the preset.  
- Confirm no write errors in golf slot after bypass/disable of allowlist.  
- Continue monitoring fleet‑reaper health: orphan counts, RAM relief metrics, GPU utilization, Ollama routing hints.
