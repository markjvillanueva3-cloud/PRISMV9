# cad session 30dbe35a (2026-05-20, 33.1MB, spine 105KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `35c65c4a3f`: 7‑file close‑out for U‑VIZ‑F11‑CROSS‑LOCK, U‑CLEAR‑AUTO‑RESUME, U‑ACTIVATE‑BEFORE‑BUILD‑PRECHECK.  
- Commit `87c464b214`: envelope reconciliation for FLEET‑REAPER‑MS3, SYSTEM‑AWARENESS‑FRESHNESS‑MS0, SFC‑ACCURACY‑MS1.  
- Commit `51bbe5c79d` (`[MAIN]`): Wave 3 – shipped U‑PTR01 (afe5da94ee), U‑PTR04 (06c251286a); pending U‑PTR02 with RCA.

**DECISIONS**  
- High‑ROI backend dev first rule for slot‑task queue.  
- AI training prioritized before revenue units.  
- Enforce R12: fix code or test; never ship unverified code; no stub engines; no inline physics constants (import from `src/physics/constants.ts`).  
- Delete‑only disable prohibited (`feedback_never_delete_only_disable`).  
- No public H drive usage, no git stash in shared tree, no schedule wakeup inside `/loop`.  
- CLAUDE.md hard‑blocked to golf slot.  
- MEMORY.md ≤200 lines / 24 KB ceiling.  
- Keep alpha slot bound for all work; use `/checkin-alpha` to force claim.  
- Fleet‑reaper ownership moved from alpha → golf (doctrine shift).  
- Commit prefix `[MAIN]` on shared `cad-fusion-live-ms0`.  
- Source of truth files after `/compact`: `CLAUDE-BRIEF.md`, `PRISM‑BUILD‑CONTEXT.md`, `PRISM‑BUILD‑VISION.md`.

**OPERATOR DIRECTIVES**  
- Session‑scoped Stop hook active: `[ complete all tasks in high ROI order | complete and wired if needed ] /loop [5m] /goal`.  
- Compile leftover tasks 5/18/2026–5/19/2026.  
- Pivot to AI‑TRAINING-FIRST-MS0.  
- Ensure check‑in into alpha (`/checkin-alpha`).  
- Verify U‑PTR02 RCA commit present.  
- Re‑engage `/loop` after slot verification.

**FINDINGS/BUGS**  
- Peer‑absorption bug: staged files swept into peer commit `e6cbcc3d48`.  
- Schema‑loader gotcha: requires `units.id` and top‑level `units{}` map; envelopes lacking these fields invisible.  
- Stale close‑out debt: 585 hidden‑shipped units across 75 milestones + silent debt from 2026‑05‑12 history‑strip envelopes.  
- Missing `id` field in milestone envelopes (e.g., SYSTEM‑AWARENESS‑FRESHNESS‑MS0).  
- `esbuild` banner redeclaration error (`const __filename` vs auto `var __filename`).  
- `MaterialResolverForProgramsEngine.ts`: `MATERIAL_KEYWORDS` defined but not exported.  
- Pathspec‑only pre‑commit guard misreports own‑chatId claims as peer‑held.  
- Shared‑tree git‑add window misattribution due to mid‑stage staging.  
- Index.lock contention from peers; requires manual `rm -f` and retry.

**DOMAIN SPECIFICS**  
- Engines/units: U‑BRIDGE‑WIRE‑MILLING, U‑CTE02/03/04, FLEET‑REAPER‑MS3, SYSTEM‑AWARENESS‑FRESHNESS‑MS0, SFC‑ACCURACY‑MS1.  
- Dispatchers: `mcp-server/src/tools/dispatchers/millDispatcher.ts`, `ActionSequenceExtractorEngine`.  
- Platform: PRISM manufacturing‑intelligence (CADQuery/Fusion360/SolidWorks → CNC G‑code).  
- Feature recognition, DfM, GD&T, blueprint‑to‑model conversion.  
- Milestone progress loader requires `ms.id` and `units[].id`; atomic write pattern `tmp + rename` with PID suffix.  
- Metrics: MILESTONE_PROGRESS, roadmap‑index, slot‑task‑queues.json, close‑out deferred ledger.

**TOOLS USED**  
- Slot helpers: `chat-slots.mjs` (claim/reclaim).  
- Precompact handoff: `precompact-handoff.mjs`.  
- Milestone progress loader: `build-milestone-progress.mjs`.  
- Close‑out reconciler: `close-out-milestone.mjs`.  
- CAD training emitter: `emit-cad-training-extractions.mjs`.  
- Banner handling: `esbuild`.  
- Material resolver engines: `MaterialResolverForProgramsEngine.ts`, `CustomerMaterialMapEngine.ts`.  
- Roadmap drift audit: `audit-roadmap-drift.mjs`.  
- Checkin/startup pipelines: `/checkin-alpha`, `/startup-alpha`.  
- Duplication guard: `duplicationGuardEngine.mustCheckBeforeCreating()`.

**OPEN THREADS**  
- Remaining alpha queue: U‑BRIDGE‑WIRE‑MULTI (10), U‑BRIDGE‑WIRE‑FIVE (9), other bridge units (~300 engines).  
- AI‑TRAINING-FIRST-MS0 enroller scaffold for 25 units.  
- NN‑STACK‑INTEG‑MS0 pending DRY fixes and remaining files.  
- Further close‑out of stale envelopes (SFC‑ACCURACY‑MS1, SYSTEM‑AWARENESS‑FRESHNESS‑MS0).  
- Export & wire `MATERIAL_KEYWORDS` correctly; validate esbuild banner change (`var` instead of `const`).  
- Fix pathspec‑only guard bug to avoid false peer‑claim reports.  
- Complete U‑PTR02 wiring and ship all pending units.  
- Resolve silent close‑out debt for 2026‑05‑12 history‑strip envelopes.  
- Confirm fleet‑reaper ownership shift fully applied in golf slot.  
- Re‑run `/loop` to process remaining high‑ROI tasks.
