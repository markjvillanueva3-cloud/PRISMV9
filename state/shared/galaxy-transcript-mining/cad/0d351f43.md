# cad session 0d351f43 (2026-06-09, 0.8MB, spine 15KB, 1 slice(s), model gpt-oss:20b)

**DECISIONS**  
- Keep core PRISM repo (≈1038 GB) + `prism-backups` (≈42 GB).  
- Drop SteamLibrary, Tools runtime folder, Claude profile copies, and all git‑worktree archives (~2.6 M files).  
- Exclude regenerable caches (`.venv*`, `.hf-cache`, `.uv-cache`) and credential dirs (`.claude-*`).  
- Use **rclone** (direct Box API) instead of Box Drive mount because C: cache is 12 GB free, Box Drive pins data and never evicts it.  
- Require a Box plan with ≥1 TB free space and per‑file limits >150 GB; confirm via web UI.

**OPERATOR DIRECTIVES**  
- `copy P:\ to Box folder "Prism H Drive"` (additive, no deletes).  
- Verify Box quota & file‑size limits in the Box web console.  
- Run `rclone authorize box` and paste token.  
- Execute rclone backup with persisted exclude list (`H:\.tmp\rc-backup-excludes.json`).  
- Monitor C: free space during copy; pause if <1 GB remains.

**FINDINGS/BUGS**  
- P: is a byte‑for‑byte mirror of H:.  
- Box Drive mount at `C:\Users\wompu\Box` has 12.3 GB free; robocopy writes faster than upload, causing cache overflow.  
- Enumerating destination fails (`ERROR 31`) due to huge online‑only file count.  
- Box Drive does not back‑pressure or evict cached files; `attrib +U` ineffective.  
- Worktree detection via `.git` presence accurately separates reconstructable copies from real content.

**DOMAIN SPECIFICS**  
- Drives: P: (mirror), H: (live PRISM), C: (system, limited cache).  
- Key folders: `PRISM`, `Tools`, `SteamLibrary`, `prism-backups`, `.claude\worktrees`, `blobs`.  
- Exclusion logic based on git worktree status and known junk patterns.  
- Box Drive uses Windows Cloud Filter API; rclone streams directly to Box API.

**TOOLS USED**  
- Bash (`/usr/bin/bash`) for initial drive checks.  
- PowerShell tool for environment discovery.  
- `robocopy` (dry‑run, full copy).  
- `attrib +U` for attempted dehydrate.  
- `rclone` (planned direct upload).  
- Box Drive virtual filesystem mount.

**OPEN THREADS**  
- Finalize and test rclone command with exclude list; ensure resumability.  
- Confirm Box plan meets size & per‑file requirements.  
- Remove 5.5 GB probe copy from C: to free space.  
- Validate post‑copy integrity (hashes, Git history).  
- Automate monitoring of C: usage during backup and implement pause/resume logic if needed.
