# cad session 86373eb3 (2026-06-10, 48.8MB, spine 403KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `Launch‑PRISM‑Fleet‑3win.ps1` → 3‑window fleet launcher; updated to 4‑window layout (NW: alpha/bravo/golf/india/papa/sierra, NE: charlie/hotel/oscar/echo/xray, SW: delta/kilo/foxtrot/mike/whiskey, SE: romeo/juliett/lima).  
- `capture‑accounts‑runbook.md` + `node scripts/capture‑claude‑credentials.mjs …` for accounts 2–6.  
- Updated `settings.json`: `LOCAL_LLM_BACKEND=ollama`, `NIM_FALLBACK_TO_OLLAMA=0`.  
- Guard added to `nim-autostart.mjs`; removed `ollama`/`ollama‑nomic‑preload` from `COMPOSE_SERVICES` in `docker-intel-autostart.mjs`.  
- Edited `ollama-docker-launcher.mjs`: dropped `ollama` from `DEFAULT_SERVICES`, fail‑fast guard for `--services=mcp`.  
- Updated `docker-compose.yml`: removed obsolete `version:` key, set `KEEP_ALIVE=30m`, `MAX_LOADED_MODELS=4`.  
- Infra hardening: NIM containers disabled (`restart=no`), native Ollama auto‑starts via scheduled task `PRISM Ollama Serve`.  
- Recall knobs (`MASTER_INDEX`, `MEMORY_INDEX`, `WIKI_PRECHECK_INJECT`) flipped to “1”; vault now injects memories/wikis.  
- Master‑index OOM bug fixed with heap guard; regression tests added.  
- Reverse Obsidian → C: mirror wired (PostToolUse hook).  
- Temp‑reaped cron runners patched to write durable `.claude/cron-runners/`.  
- GNN feeder (`vault-to-gnn-refpool.mjs`) built, 9/9 tests passed; applied to live 642 MB `system‑graph.json` adding 4 high‑confidence reference ghosts.  
- LoRA feeder (`vault-to-lora-dataset.mjs`) built, 15/15 tests passed; produced 245 valid Alpaca triples.  
- `scripts/lib/graph‑io.mjs` extended with `readGraphStreaming`, `writeGraphStreamingAtomic`; feeders updated to use streaming I/O.  
- Fixed `seed‑ghost-from-unwired.mjs --apply` (ERR_STRING_TOO_LONG) and corresponding revert path.  
- Regex fix for `frontmatterField` (`\s → [ \t]`) and CRLF stripping in LoRA output.  
- Commits: `cf74c95edb` – content‑based discovery, per‑transcript slice cap (`MAX_SLICES_PER_TX=14`), synthesis timeout override (`SYNTH_TIMEOUT_MS`).  
- Commit `5203edf318` – `--next-unpopulated --backend-only` mode for ROI‑ordered backend mining; updated galaxy‑mining registry and helpers.  
- 35 galaxy synthesis memos populated; compounding pipeline refreshed all 34 brains.  
- Hermes self‑learning loop fully operational on local models (inbox sweep, morning brief); Obsidian REST bridge authenticated (`:27123`), gateway running.

**DECISIONS**  
- Switch local LLM backend to Ollama only; permanently disable NIM containers.  
- Keep native Ollama as sole GPU service; remove compose‑ollama from autostart; ensure `PRISM Ollama Serve` scheduled task runs.  
- Guard launcher against mis‑typed `--services=mcp`.  
- Recall knobs set to “1” → vault injects memories/wikis.  
- Keep CAG router gating (avoid token‑cost regressions).  
- Leave brain‑refresh as‑is (no `--with-viz`).  
- Do not register 4 dated‑synthesis crons; only 3 cron‑ready runners active.  
- Wire only reverse mirror hook; other RAG hooks redundant.  
- Use proven `seed‑ghost‑from‑unwired` pipeline; add vault‑source mode for high‑confidence engine→dispatcher pairs.  
- Build separate LoRA feeder for feedback memories; keep distinct from DB‑driven dataset builders.  
- Do not auto‑force Ollama routing for all sessions; keep advisory hooks.  
- Generalize India transcript miner to all 34 galaxies (deferred until parameterized implementation).  
- Avoid clobbering active `mine-india-transcripts.mjs` session.  
- Discovery: switch from handoff‑filename gating to content classification (slot hint + prose keyword matching).  
- ROI ordering: mine backend galaxies first before primary domains.  
- Per‑galaxy foreground runs; protect against fleet‑reaper kills, respect 1100 s wall.  
- Cache per‑transcript classification results on first run (~50 ms).  
- Chunk cap `MAX_SLICES_PER_TX=14` to bound long transcripts.  
- Synthesis timeout (`GEN_TIMEOUT_MS`) for 120b increased to 900 s.  
- Remove `grep | timeout` wrapper; use vault memo presence as success signal.  
- Set `OLLAMA_CONTEXT_LENGTH=65536` (user‑scope) overriding default 16384; update Hermes `model.context_length`, `max_tokens`.  
- Removed `prism-vault-fs` MCP from Hermes config.

**OPERATOR DIRECTIVES**  
- Run capture script for accounts 2–6 (`node scripts/capture-claude-credentials.mjs …`).  
- Verify scheduled task `PRISM Ollama Serve` enabled/running; reboot to confirm no port‑11434 collision, Docker shows only required services (qdrant, postgres, prometheus); NIM containers stopped.  
- Run elevated PowerShell block to register 6 scheduled tasks (tribal, synthesis, PDF watcher if desired). Ping after running to verify `LastResult 0x0`.  
- Decide commit vs leave live edits for the 9 changes (7 H: files + 2 C: settings).  
- Build LoRA feeder coordinated with India session; populate every galaxy/domain with transcript‑derived context using Ollama + ultracode; ensure each has current `MEMORY.md`, `CLAUDE.md`, synthesis file.  
- Do not overwrite files owned by India session or other active workers.  
- Run `hermes gateway install` (elevated) to keep the gateway alive post‑logoff.  
- Validate weekly self‑review job on gpt‑oss:120b.  
- Mine GEPA round 2 success/failure traces and review candidate skill updates.  
- Re‑register missing scheduled tasks (OCR training loop, slot worktree migration status, system awareness freshness) via elevated PowerShell.

**FINDINGS/BUGS**  
- NIM containers OOM‑crashed (exit 137) due to 16 GB WSL cap vs >20 GB model size; Docker memory cap 15.6 GB caused repeated crashes.  
- Compose autostart listed `ollama` → port collision with native Ollama at boot.  
- Launcher default services included `ollama`; fragile filter removed.  
- Mis‑typed `--services=mcp` warnings; guard added.  
- Master‑index OOM bug: loading 201 MB sidecar into 384 MB heap; fixed with size guard.  
- Recall knobs off → vault did not inject memories/wikis.  
- Temp‑reaped cron runners caused broken tasks (`0x41303`).  
- Brain‑refresh heavy flag would cause regression (30‑min regen‑viz every 45 min).  
- `find-connections` per‑target tool, not batch cron runner.  
- False‑positive hook warning about large commented block.  
- Zero‑coverage rollup bug (`P1‑B`).  
- `preSince` split logic mis‑handled missing file case.  
- Regex for prose keyword matching failed; only matched slugs.  
- `NUM_CTX` accidentally removed during slice‑cap edit; restored.  
- Reaper killed background `--all` runs before completion.  
- GPU contention prevented `nomic-embed-text` sidecar rebuild; embeddings step flaky.  
- Misleading exit 255 from `grep | timeout`; vault memo ground truth.  
- Synthesis timed out on dense token‑optimization digests until timeout increased.  
- Ollama `/v1` endpoint silently ignores `options.num_ctx`; truncated responses and Hermes failures.  
- `node-process-janitor.mjs` killed orphan MCP processes; fixed by protecting pipeline in reaper regex.  
- SYSTEM-task for MCP server failed to load `.env`; resolved via engine fallback to plugin config.  
- Missing scheduled tasks (OCR training loop, slot worktree migration status, system awareness freshness) require elevated PowerShell registration.

**DOMAIN SPECIFICS**  
- PRISM fleet launcher: orchestrates Windows Terminal windows & slot tabs via PowerShell.  
- Local‑LLM bridge routes requests based on `LOCAL_LLM_BACKEND`.  
- Account switching handled by `claude-account-lib.mjs` + `switch-claude-account.mjs`; credentials in `~/.claude/.credentials.json`.  
- Docker compose services: `prism-server`, `qdrant`, `ollama`, `postgres`, `prometheus`.  
- NVIDIA GPU (RTX PRO 6000 Blackwell, 96 GB VRAM) used exclusively by Ollama after cleanup.  
- PRISM platform components: slot‑tab‑boot, memory‑index, CAG router, Qdrant, Docker compose, Ollama, NIM, Windows Task Scheduler.  
- Obsidian vault integration: recall injection hooks, reverse mirror, memory-index precheck, wiki‑precheck.  
- AI system usage: RAG corpus & cache (vault), LoRA training data source, NN/GNN label/feature source; binary weights remain on disk.  
- GNN feeder uses `seed‑ghost-from-unwired.mjs`, `buildHoldout`, `isValidDispatcher`, `DISPATCHER_RE=/^prism_[a-z0-9_]+$/`.  
- LoRA feeder consumes feedback memories, outputs Alpaca triples per `LatheLoRADatasetBuilderEngine` schema.  
- Transcript miner (India) reads session `.jsonl`, routes via Ollama to vault; scoped to India’s AI‑systems galaxy.  
- Discovery engine `discoverByContent` content classifier; dispatchers: `mineGalaxyTranscripts`, `buildMemoryIndexSidecar`, `buildMemoryEmbeddingsSidecar`, `galaxySynthesisRefresh`.  
- Metrics/paths: per‑transcript digest JSONL (`digest.jsonl`), vault memo `reference_<galaxy>_transcript_synthesis.md`, cache file `content_classification_cache.json`.  
- Hermes agent orchestration: master brain, teacher, orchestrator, learner roles; cron jobs for inbox sweep, morning brief, weekly review.  
- Obsidian REST bridge on port 27123; authentication via plugin `data.json` key.  
- MCP server hosts Prism HTTP endpoints; protected by fleet‑reaper sweep.

**TOOLS USED**  
- PowerShell: `Launch‑PRISM‑Fleet.ps1`, `slot-tab-boot.ps1`, `install‑tribal‑cron.ps1`.  
- Docker Desktop (WSL2) with `docker-compose.yml`.  
- Node.js scripts: `switch-claude-account.mjs`, `capture-claude-credentials.mjs`, `seed‑ghost-from-unwired.mjs`, `vault-to-gnn-refpool.mjs`, `vault-to-lora-dataset.mjs`, `mine-galaxy-transcripts.mjs`.  
- Docker compose services: `prism-server`, `qdrant`, `ollama`; NIM containers (disabled).  
- OLLAMA serve task; Hermes cron (`hermes cron create/run/tick`).  
- Node.js utilities (`fs`, `v8`, `process`).  
- Ultracode workflow for transcript summarization.  

**OPEN THREADS**  
- Commit vs leave live edits for the 9 changes (7 H: files + 2 C: settings).  
- Re‑register missing scheduled tasks (OCR training loop, slot worktree migration status, system awareness freshness) via elevated PowerShell.  
- Generalize India transcript miner to all 34 galaxies (parameterized registry & router).  
- Final integration of LoRA feeder into existing training pipeline; ensure no duplicate data.  
- Resolve remaining silent coverage holes in handoff classification (long‑UUID, legacy forms).  
- Verify weekly self‑review job on gpt‑oss:120b is live‑validated.  
- Mine GEPA round 2 success/failure traces and review candidate skill updates.
