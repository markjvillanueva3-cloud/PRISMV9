# cad session 9e91d800 (2026-05-26, 19.9MB, spine 86KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED (builds/commits)**  
- `d02f713f0a` – SlotSessionHistoryEngine + 57 tests, sidecar helpers & hooks, spec `SLOT‑RECOVERY-MS0.md`.  
- `b79651ea41` – MCP root‑cause spec, revert of buggy factory, queued P0/MCP‑FACTORY‑REFACTOR.  
- `58545f51e5` – `U‑MILESTONE‑REGISTER` (3 fleet milestones).  
- `a9a12477a8` – `U‑MCP‑CAPACITY‑PLAN‑PUBLISH`.  
- `02ed3166e6` – `U‑FH03` MCP scheduled‑task triage.  
- `11303f4d17` – `U‑OBSOLETE‑REAPER‑TASKS` cleanup.  
- `76bf6f8709` – Docker daemon recovery check.  
- `f0c7bba10e` – `/checkin §Resume` block added to skill.  
- `e3b579f6e1` – E2E integration test for slot‑recovery stack.  
- `8d563aab3e` – Ghost slot‑queue generator (`U‑FD06`) committed in the golf worktree.  
- `U‑QUEUE‑ROLLIN` – 13 new tasks appended to `state/shared/slot-task-queues.json`.  

**DECISIONS (architecture/scope + why)**  
- Adopt a *sidecar* JSONL per‑slot (`state/shared/slot-sessions/<nato>.jsonl`) for session history; keeps crash‑inferred invariants and enables `/checkin` resume.  
- Shift fleet‑reaper ownership from **alpha → golf** to centralize hygiene under one slot (Doctrine 2026‑05‑16).  
- Bypass legacy `golf-slot-write-allowlist.mjs` via `PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1` for recovery work; global disable only if operator explicitly chooses.  
- Use `chat-slots.claim --force true --confirmRecent true` to force‑take golf, ensuring a clean slot state before `/checkin`.  
- Implement `SlotSessionHistoryEngine` with three event types (start, heartbeat, end) and an eligibility matrix for resume logic.  
- Queue tasks in `slot-task-queues.json` v1.1.0; each entry carries `dedicated`, `added_by`, etc., to support per‑slot vs general pool.  

**OPERATOR DIRECTIVES (verbatim asks)**  
- `/checkin-golf — slot-locked /checkin + fleet-reaper owner`.  
- “Force-take the golf slot … always run the fleet‑reaper.”  
- “Disable or bypass the legacy allowlist hook” – choose Option A (`PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1`) for this session.  

**FINDINGS/BUGS**  
- `settings.json` hooks arrays were missing `hooks:[]` wrapper → `/doctor` errors; fixed by wrapping with matcher and adding `type:"command"`.  
- Line 233 in settings had malformed path `"H:.claude\binportable-node"` – prevented `substrate-health-inject.mjs`.  
- Heartbeat‑keepalive timeout set to 8 ms (typo) → chat‑slot TTL expired prematurely; fixed to 8000 ms.  
- MCP server disconnects after a few minutes: each `/mcp` request created a new `StreamableHTTPServerTransport` via `server.connect()` without session reuse, violating the single‑transport invariant. Reverted factory and queued dedicated‑chat refactor.  
- Duplicate fleet‑reaper scheduled tasks (MS1–3) running concurrently; disabled obsolete ones.  

**DOMAIN SPECIFICS**  
- **Engines/Actions**: `SlotSessionHistoryEngine.ts`, `slot-session-sidecar.mjs` helpers, `fleet-reaper-sweep.mjs`.  
- **Dispatchers**: `chat-slots.mjs` (claim/reclaim), `slot-task-queues.json` v1.1.0.  
- **Metrics/Paths**: `state/shared/handoffs/HANDOFF‑golf-slot-recovery-ms0.md`, `state/shared/slot-sessions/golf.jsonl`.  
- **Unique to this galaxy**: PRISM_GOLF_WRITE_ALLOWLIST_BYPASS env var, c-to-h-mirror hook, `PRISM_SLOT_SESSION_BASE_DIR` override.  

**TOOLS USED**  
- `.claude/helpers/slot-session-sidecar.mjs` (write/read sidecar).  
- Hooks: `slot-session-sidecar-{sessionstart,heartbeat,stop}.mjs`.  
- Spec files in `state/shared/specs/`.  
- Fleet‑reaper scripts (`fleet-reaper-sweep.mjs`, guardian hooks).  
- Git workflow with `git commit -- <pathspec>` to avoid peer‑absorption.  

**OPEN THREADS (what still to build)**  
1. **U‑SR05** – Launcher rewrite: 26‑slot, 4‑window, `--resume` eligibility matrix.  
2. **U‑SR06** – `/slot-resume` skill for explicit resume command.  
3. **U‑SR07 / U‑SR08** – already shipped; integration tests passing.  
4. **FLEET‑DASHBOARD‑MS0** – `U‑FD02`, `U‑FD03`, `U‑FD04` (auto‑task‑detect hook, queue‑add skill, viz generators).  
5. **MCP FACTORY REFACTOR** – dedicated chat MCP server with session pooling; pending design and implementation.  
6. **Re‑enable/optimize fleet‑reaper** – ensure no duplicate tasks, confirm GPU/Ollama hints.  

These items remain to be completed before the next `/checkin-golf` can fully realize the planned recovery and dashboard integration.
