# cad session 96317abd (2026-05-25, 107.1MB, spine 263KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**

* `4690e17f3b` – FLEET‑REAPER‑MS3 close‑out (`U‑FR‑MS3‑A`) envelope flip, milestone complete.  
* `0950c701d3` – CAD‑DRAW‑MAX‑MS0 envelope creation (10 silent‑close units).  
* `510440ac24` – `U‑VALIDATION‑50` harness (36 tests).  
* `e63c683f94` – `U‑VALIDATION‑50‑SCORING` rubric (37 tests).  
* `91a25d68fc` – `U‑VALIDATION‑50‑CORPUS` starter (12 cases, 21 tests).  
* `1c231d6f36` – Baseline report (75 % accuracy ≥70 % gate).  
* `5b4f541121` – Bulk close‑out tool (`scripts/close-out-cad-silent-debt.mjs`) flipped 38 units.  
* `ed3aac690f` – `U‑CAD22` Regeneration Test Harness (28 tests).  
* `e66d99f2d0` – `U‑CAD40` hyperMILL SDK reference (3 files).  
* `54d69bfe70` – `U‑CAD32` CADPartArchetypeRegistryEngine (8 archetypes, 25 tests, 4 actions).  
* `2b200c4f56` – `U‑CAD33` CADJMDieArchetypeFrequencyEngine (16 tests, 4 actions).  
* `3dd4cc17aa` – PSN‑CAD‑PRODUCER unified facade (21 tests, 4 dispatcher actions).  
* `995e343c98` – `U‑KEC‑CAD‑PARAM‑EMITTER` (59 tests, 2 dispatcher actions).  
* `b2d1f2bd6c` – 3 extraction scripts + 17 sample wiki/tribal nodes.  
* `3488a74f89` – HyperCadS pull, 982 NN‑graph seeds.  
* `c78ab7b0ca` – Unified 5‑CAD pull, 2 632 seeds.  
* `6c8d13fcb6` – Full 5‑CAD extraction, 6 999 seeds.  
* `5ade492b43` – CAD FILES regen test set index + smoke runner.  
* `27fb50624b` – Fixed missing exports in GraphSAGE trainer; NN retrain now runs.  
* `2aa917e301` – Wiki tribal entry, `/cad‑regen` skill, T2 hook.  
* `6bc89da5ef` – First NN retrain run (AUROC 0.39).  
* `1b335c019c` – Corpus re‑extract v2, topology emit v2, sweep v2; 1 122 files.  
* `15350407bf` – Real‑placement topology emitter for impeller, validator, desktop delivery skill.  
* `a37b9919d7` – Corpus‑wide multi‑cylinder emit (avg 12.14 faces).  
* `e6cbad7e3a` – Schema v2 placement extraction and real‑placed emit (39 faces for impeller).  
* 47 commits on `slot/delta` (H:/prism-slot-delta) – full CAD topology pipeline, 98 test cases green.  

**DECISIONS**

* Prioritized hyperCAD → Fusion → Mastercam → SolidWorks → Inventor → Esprit; other CAD systems put on indefinite hold.  
* Adopted silent‑close detection modes A–E to drain backlog; remaining 181 `CAD-COMPLETE-MS0` units are genuine engineering work.  
* PSN‑CAD‑PRODUCER now produces parts via unified facade; production requires corpus scaling & training loop.  
* Archived out‑of‑priority units (155) to reduce clutter.  
* Built parameter emitter (`U-KEC-CAD-PARAM-EMITTER`) for wiki/tribal node conversion.  
* Dedicated slot‑worktree `H:/prism-slot-delta` to avoid Git lock contention; chunked write with `setImmediate`, skip‑existing logic, resumable bulk‑write hook bypass.  
* Force‑add `.jsonl` seeds (`-f`) despite gitignore for canonical corpus data.  
* Implemented workerPool concurrency (8) for large STEP walks; fixed index‑as‑timeout bug.  
* Docker‑compose profiles for discover, batch, archetype, validate, fidelity stages → parallel, reproducible pipeline.  
* Ollama `qwen2.5-coder:7b` for archetype labeling; rule‑based fallback when offline.  
* Octopus orchestrator for multi‑agent coordination of iterative training loops.  
* Upgraded geom.json schema v1→v2 (placement) → v3 (plane slabs).  

**OPERATOR DIRECTIVES**

```
/checkin-delta /loop [5m] /goal   # priority hyperCAD > Fusion > Mastercam > SolidWorks > Inventor > Esprit
/checkin-delta — slot-locked /checkin
complete next 4 units
bash scripts/cad.sh status
bash scripts/cad.sh dry-run --slug <slug>
bash scripts/cad.sh emit <slug> [--json] [--to-desktop]
bash scripts/cad.sh test
node scripts/cad-pipeline-status.mjs
```

**FINDINGS/BUGS**

* Hitchhike absorption of commits into peers (alpha, bravo, whiskey); logged in `CLOSE-OUT-DEFERRED.md`.  
* Mode E path‑prefix bug fixed; no more false positives.  
* Bulk‑write hook truncated after ~13 files – solved with chunked write.  
* Background harness timed out on large walks → moved to foreground, added timeout handling.  
* Missing exports in `graphsage-trainer.mjs` caused SyntaxError – restored `positiveTypeMarginal`, `sampleStratifiedNegativeEdges`.  
* Initial pseudo‑regen produced empty CLOSED_SHELL; later emitters added real topology (3→9→39 faces).  
* Schema v1 lacked placement data; upgraded to v2/v3 for full geometry fidelity.  
* Baseline accuracy 75 % on 12‑case corpus; need >1000 cases for production.  
* MIT course content extraction incomplete – only pointer stubs present.  

**DOMAIN SPECIFICS**

* Engines: `CADDrawAnyPartValidationHarnessEngine`, `CADValidationRubricEngine`, `CADRoundTripValidationEngine`, `CADLiveBlueprintOcrAdapter`, `CADModelDimensionExtractorEngine`, `CADPrintRegeneratorEngine`, `CADArchetypeRegistryEngine`, `CADJMDieArchetypeFrequencyEngine`, `CADArchetypeRankerEngine`, `PSN-CAD-PRODUCER`, `U-KEC-CAD-PARAM-EMITTER`.  
* Dispatchers: `prism_cad` actions (`cad_draw_any_part_validate`, `validate_render`, etc.), `cad_multi_system_produce_part/assembly`.  
* Metrics: `MILESTONE_PROGRESS`, `BUILD_STATE`; baseline accuracy 75 % (17/17 tests pass).  
* Paths: `H:/prism/mcp-server/src/engines/`, `scripts/close-out-cad-silent-debt.mjs`, `state/shared/`, `H:/prism-slot-delta`.  

**TOOLS USED**

* PRISM CLI (`/checkin-delta`, `/loop`, `/goal`).  
* Chat‑slot helpers (`chat-slots.mjs`).  
* Audit scripts (`audit-roadmap-drift.mjs`, system‑viz ping).  
* Bulk close‑out tool (`scripts/close-out-cad-silent-debt.mjs`).  
* Baseline runner (`run-hypercad-validation.mjs`).  
* Node scripts: `cad-emit-impeller-fusion-step.mjs`, `cad-dry-run-corpus.mjs`, `cad-pipeline-status.mjs`, `cad-to-desktop.mjs`, `cad-fidelity-html-report.mjs`.  
* Docker compose with profiles for pipeline stages.  
* Ollama API (`qwen2.5-coder:7b`) for archetype labeling.  
* Octopus CLI for orchestrating agents.  
* Git slot manager helpers, CronCreate for recurring `/loop` tasks.  

**OPEN THREADS**

1. **Remaining 181 CAD‑COMPLETE-MS0 units** – schedule ~10 per session or bulk tool across all milestones.  
2. **Corpus scaling** – `U-PSN-CAD-CORPUS-SCALE` to >1000 cases for production accuracy; retrain GraphSAGE until AUROC ≥ 0.78.  
3. **Reverse‑engineering turbine parts** – new units: `U-PSN-CAD-ROUNDTRIP-LIVE`, `U-PSN-CAD-REWARD-WIRE`, `U-PSN-CAD-NIGHTLY-SELFPLAY`.  
4. **MIT course content extraction** – `U-KEC-MIT-CONTENT-PULL` & `U-KEC-CAD-PARAM-ENUMERATE`.  
5. **Blade surface topology** – close remaining ~446‑face gap for impeller (plane slabs, B‑spline surfaces).  
6. **MCP server reconnect** – enable live Fusion/HyperCAD script execution for true regeneration.  
7. **Full training loop** – run NN retrain with expanded seed pool until AUROC ≥ 0.78 gate is achieved.  
8. **Corpus‑wide re‑extract v3** – include plane slabs and full placement data for all 560+ parts.  
9. **Validate closed‑loop fidelity** across entire corpus (ISO 2768‑mK tolerance bands).
