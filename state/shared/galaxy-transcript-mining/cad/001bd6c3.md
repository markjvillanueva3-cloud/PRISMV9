# cad session 001bd6c3 (2026-06-09, 56.9MB, spine 514KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commits: ab2ccf42a4, e6713584e2, c988a21ec4, 56aa423427, 28b72e4dee (36 files), 822c48c55a (U‑OAB‑PLAN), 7771ca7f86 (U9), 2340a2e699 (U1), a2756779c2 (U2), a60d7ba0bf (U3), 612418fde7 (U4), 3904b5e7b0 (U‑OAB‑120B), 5a91ba1862 (cad‑fusion‑live‑ms0).  
- Artifacts: system‑viz roost, Hermes config v28, Docker Qdrant container (`prism-qdrant:v1.17.0`), headless zulu agent, `scripts/fill-galaxy-memory-sections.mjs`, test suite (13 passing tests), `ollama-tool-agent.mjs`, `ollama-offload-enforce.mjs`, `ollama-codegen.mjs`, `ollama-build-loop.mjs`, `ollama-capability-probe.mjs`, `ollama-coresidency.mjs`.  

**DECISIONS**  
- Use Path D: read vault knowledge via :3100 MCP; no filesystem server.  
- Revert supervisor cwd‑fix; rely on daemon restart for stale‑N bug.  
- Hermes primary model: local Ollama (`gpt‑oss:20b` or `qwen2.5-coder:32b`); fallback to Anthropic Claude Code (Opus 4.8) only if billing enabled.  
- Adopt `gpt‑oss:120b` fleet‑wide coding default; upgrade from 20b when available.  
- Keep `ai-training_synthesis.md` untouched; flag for owner.  
- Use deterministic file‑based audit (`scripts/fill-galaxy-memory-sections.mjs`) instead of headless Hermes calls to avoid API throttling.  
- VRAM budget: NP = 2, MAX_LOADED = 3, NUM_PARALLEL = 2; 120b + 32b cannot co‑reside.  
- Offload strategy: read/search/coding via local LLMs; Opus tail review for QA.  

**OPERATOR DIRECTIVES**  
- Finish Hermes–Obsidian vault synergy and link Hermes app to Claude Code.  
- Verify Hermes running with `model.default = gpt‑oss:20b` (or Anthropic if billing enabled).  
- Confirm MCP at `http://127.0.0.1:3100/mcp` reachable; Hermes can POST to it.  
- Monitor memory pressure; watch commit‑charge spikes.  
- Verify all 34 galaxies have 4 canonical brain sections in `MEMORY.md`.  
- Re‑enable disabled PRISM Hermes‑Obsidian Bridge scheduled task (`install‑<task>-task.ps1`).  
- Regenerate `ai-training_synthesis.md` via `galaxy-synthesis-refresh.mjs --galaxy ai-training`.  
- Validate blind‑mapping script (`node scripts/hermes‑obsidian‑app‑map.mjs`) continues to surface config_version and gateway state.  

**FINDINGS/BUGS**  
- Qdrant :6333 down → Docker Desktop restart needed; semantic_search fails.  
- Hermes provider `openai` invalid → 400 error; resolved by switching to local provider or enabling billing.  
- Config version mismatch (`config_version:0`) caused stale connection; fixed via migration.  
- GUI launcher stuck on dirty `package-lock.json`; cleared.  
- Self‑awareness search returned stale N:\PRISM path; daemon restart resolved.  
- Hermes config lacked vault‑fs channel; bridge dormant (memories empty).  
- HTTP 400 from Anthropic due to extra‑usage billing; fallback not triggered.  
- Memory count inflation: auto‑generated `node_*` dumps excluded (`cam: 66 curated + 1296 node_* excluded`).  
- Test regression fixed with deterministic mutation testing.  
- Corrupt `ai-training_synthesis.md`: all NUL bytes, flagged for owner.  
- Edge extraction bug in `extractExistingEdges`; now strips managed block first.  
- `qwen2.5-coder:7b` absent from Ollama roster; `gpt‑oss:120b` tool‑calling works, qwen2.5‑coder does not emit structured calls.  
- Git grep requires `-E` for BRE patterns with parentheses.  
- Probe entry guard failed on relative paths; fixed to use absolute URLs.  
- Tag‑match bug: `nomic‑embed‑text` appears as `nomic-embed-text:latest`; stripped `:latest`.  
- Process.exit truncates stdout; probe now writes report file.  

**DOMAIN SPECIFICS**  
- Dispatchers: prism_memory, prism_session, prism_knowledge, slot_brief_write, chat_post, slot_brief_list.  
- Engines: CapabilityIndexEngine, OutcomeCaptureBusEngine, DocuStrataMaterialPriorEngine, JMCustomerVendorDatabaseEngine, OllamaTaskOffloaderEngine, AISystemRouterEngine, ModelRoutingEngine, gpt‑oss:120b tool‑calling engine, qwen2.5‑coder:32b coding engine.  
- Paths: :3100 MCP (PRISM), :9120 Hermes UI, :27123 Obsidian REST API, :11434 Ollama, :6333 Qdrant, :11434 local Ollama server.  
- Hardware: RTX PRO 6000 Blackwell 96 GB VRAM, 127 GB RAM, Ryzen 9 9950X3D 16‑core.  
- Models & sizes: gpt‑oss:120b (~65 GB), gpt‑oss:20b (~14 GB), qwen2.5‑coder:32b (~37 GB at 16K), qwen2.5‑coder:1.5b, nomic‑embed‑text, qwen3‑vl:8b, qwen2.5‑vl:7b, llama3.2‑vision:11b, moondream.  
- VRAM constraints: 120b + 32b cannot co‑reside; NP = 2, MAX_LOADED = 3, keep_alive 30 min, NUM_PARALLEL = 2.  

**TOOLS USED**  
- Node.js (v20+), NPM scripts (`hermes config migrate`, `hermes doctor`, `hermes -z`, `hermes chat`, `hermes send`), Docker Desktop & `docker-compose.yml`.  
- Qdrant CLI, Obsidian REST API plugin (`:27123`).  
- PRISM MCP client (`mcp-server-supervisor.mjs`).  
- Custom scripts: `scripts/fill-galaxy-memory-sections.mjs`, `ollama-tool-agent.mjs`, `ollama-offload-enforce.mjs`, `ollama-codegen.mjs`, `ollama-build-loop.mjs`, `ollama-capability-probe.mjs`, `ollama-coresidency.mjs`.  
- Ultracode workflows (`wbmnnd902`, `w2hu6dowo`, etc.).  
- Git hooks: `slot-bind-enforce.mjs`, `chat-slots.mjs`.  

**OPEN THREADS**  
- Bring Qdrant up (restart or install).  
- Enable extra‑usage billing on Claude to allow Hermes primary; otherwise fallback only.  
- Verify Hermes memory bridge writes memories into vault.  
- Confirm PRISM MCP remains healthy after daemon restart.  
- Finalize supervisor cwd‑fix if needed.  
- Finalize Hermes primary to `gpt‑oss:20b` and confirm headless zulu produces clean final answer (adjust max‑tokens).  
- Await completion of `gpt‑oss:120b` pull; plan upgrade path.  
- Validate blind‑mapping script (`node scripts/hermes‑obsidian‑app‑map.mjs`) continues to surface config_version and gateway state accurately.  
- Monitor memory‑pressure metrics to avoid future commit‑charge spikes.  
- Re‑enable disabled PRISM Hermes‑Obsidian Bridge scheduled task.  
- Regenerate `ai-training_synthesis.md` via `galaxy-synthesis-refresh.mjs`.  
- RTK upgrade sweep (#11) pending; plan integration of external Rust binary.  
- Go‑live merge of `slot/bravo` into main and wiring of `settings.json`.  
- System upgrades for new hardware (U1/U3 VRAM routing config).  
- No further router unit needed after dedup; focus on remaining build units.
