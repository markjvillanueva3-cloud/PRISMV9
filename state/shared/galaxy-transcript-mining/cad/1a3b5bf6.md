# cad session 1a3b5bf6 (2026-05-22, 12.7MB, spine 47KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U‑AIW05 – commit cb6b9fc945: wired 3 neural engines (`neural_determinism_test`, `neural_weight_persist`, `deep_logic_trace`).  
- U‑AIW09 – commit a75d27afd8: wired 3 learning engines (`ai_transfer_learn`, `ai_continual_learn`, `ai_few_shot_learn`).  
- d915fa3be8 – shipped CC‑EXT‑MS0/P0‑U07 and BP‑MS0/U‑LEARN1; added 5 document routes in `learning.ts`.  
- fc4cf18ace – shipped CADCAM‑DAGI‑MS4/U‑CAMAGI13 (ReinforcementLearningCAMFeedbackEngine) with 3 sub‑engines, 16 tests passed.  
- 173c562e04 – fixed missing module imports and JSON import attribute for MCP server stability.  
- 1dda943c11 – ASCII‑folded PowerShell installers to enable supervisor tasks.  
- 50a3bd3d80 (peer‑absorbed) – rewritten `mcp-http-bridge` with retry, self‑heal, 60 s init budget.

**DECISIONS**  
- Resolve MCP server crashes by fixing extensionless imports and JSON import attribute; ensure all dispatchers compile.  
- Install supervisor and watchdog scheduled tasks for automatic restart on reboot/logon.  
- Rewrite `mcp-http-bridge` to include backoff retry for `initialize` and self‑heal when :3100 is down.  
- Keep `node-orphan-cleaner` protection of port 3100 to prevent reaper kills.  
- Use O_EXCL lock in bridge spawn to avoid race conditions among multiple chats.

**OPERATOR DIRECTIVES**  
- “do anything and everything possible to make sure th mcp server is always connected”  
- “get everything up and running”

**FINDINGS/BUGS**  
- 4 extensionless relative imports caused `ERR_MODULE_NOT_FOUND`; fixed.  
- 1 bare JSON import caused `ERR_IMPORT_ATTRIBUTE_MISSING`; fixed with `with { type: "json" }`.  
- PowerShell installer scripts had UTF‑8 em‑dash corruption, preventing task registration; ASCII‑folded.  
- Supervisor scheduled tasks were never installed; now registered and idempotently start the server.  
- Bridge originally lacked retry/backoff; rewritten to handle cold starts.

**DOMAIN SPECIFICS**  
- U‑AIW05: `neural_determinism_test`, `neural_weight_persist`, `deep_logic_trace` engines in `prism_ai`.  
- U‑AIW09: `ai_transfer_learn` (TransferLearningEngine), `ai_continual_learn` (ContinualLoRAEngine), `ai_few_shot_learn` (ProtoMAMLPredictEngine).  
- P0-U07: 5 document routes added to `learning.ts` for `prism_doc_learn`.  
- CADCAM‑DAGI‑MS4/U‑CAMAGI13: orchestrator combining MillingRL, ContinualLoRA, CAMFeedbackLoop; exposes actions `cam_rl_feedback_close_loop`, `_compute_reward`, `_preserve_skill`, `_stats`.  
- MCP server operates via stdio→HTTP bridge to 127.0.0.1:3100; dispatcher uses lazy import pattern.

**TOOLS USED**  
- `/checkin-lima`, `/startup-lima`  
- `chat-slots.mjs` (claim, reclaim)  
- `build-doctor` subagent for TypeScript error fixing  
- `mcp-http-bridge.mjs` (rewritten with retry/self‑heal)  
- Supervisor scripts: `mcp-server-watchdog.mjs`, `mcp-server-supervisor.mjs`  
- PowerShell installers: `install-mcp-server-task.ps1`, `install-mcp-server-watchdog-task.ps1`  
- `node-orphan-cleaner.mjs` for port protection  
- `tsc`, `vitest` for compile and test verification

**OPEN THREADS**  
- Build remaining lima units: AI‑TRAINING-FIRST-MS0/U‑AITRAIN-* (4 heavy corpus training units) – deferred.  
- ARC-MS11/muS-D73-75/D79-82 phantom envelope – no file; deferred.  
- CAMX-V17-P11/U-CAMX13 already deferred by foxtrot.  
- 3‑of‑3 scrutiny on the three MCP commits (173c562e04, 1dda943c11, 50a3bd3d80) is owed.  
- Verify bridge retry logic under real chat conditions; currently tested with harness but pending full production validation.
