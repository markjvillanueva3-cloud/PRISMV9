# cad session 9a25c01e (2026-05-17, 2.2MB, spine 7KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Merge commit `730f141f5` pushed to `origin/cad-fusion-live-ms0`; includes new test file, no conflicts.  
- Portable PATH script `H:\Tools\setup-portable-path.ps1` installed and idempotently added required entries (`nodejs`, `python`, `python\Scripts`, `Tesseract-OCR`, `ollama`).  

**DECISIONS**  
- Use sibling worktree (`H:/prism-sync`) for merges to preserve peer’s staged `MillingAGIMasterEngine.*`.  
- Treat `H:\Tools\python` as an NTFS junction so it travels with the H: drive.  
- Write PATH entries directly to registry (HKCU\Environment) after dry‑run to avoid accidental wipe.  
- Adopt cross‑PC seam audit: verify `settings.json` SHA equality, use runtime `os.homedir()` in init scripts, and flag hardcoded `C:\Users\wompu\Box\…` paths for dynamic resolution.  

**OPERATOR DIRECTIVES**  
- Ensure seamless operation across PCs; maintain peer staged work intact during merges.  
- Wire PATH portably on every Windows user per PC.  
- Convert hardcoded Box paths to use `process.env.PRISM_BOX_DIR || homedir+'/Box'`.  

**FINDINGS/BUGS**  
- Pre‑commit hook `pre-commit-cross-session-guard` falsely flagged plumbing merge; required manual restoration of peer staging.  
- User PATH registry value temporarily cleared during dry‑run; resolved by explicit write.  
- Hardcoded paths for user `wompu` will silently skip on other PCs.  

**DOMAIN SPECIFICS**  
- Engines: `MillingAGIMasterEngine`, `BoxProgramCensusEngine`, `EDMPostProcessGCodeEngine`.  
- Test files: `cwedm-real-shop-programs.test.ts`, `lathe-real-program-validation.test.ts`.  
- Dispatchers/hooks: `pre-commit-cross-session-guard`, `c-to-h-mirror`, `memory-system-init.mjs`, `plugin-path-fixer.mjs`.  
- Metrics: `settings.json` SHA `295E9E3E…` equal on C:↔H:.  

**TOOLS USED**  
- Python LSP (`python-lsp-server 1.14.0`) with plugins (jedi, rope, pyflakes, etc.).  
- Git (sibling worktree, merge-tree plumbing).  
- PowerShell script `setup-portable-path.ps1`.  
- Registry editor for PATH.  

**OPEN THREADS**  
- Update hardcoded Box paths to dynamic env‑based resolution.  
- Verify that all tools resolve correctly after PATH changes on every PC.  
- Ensure future merges preserve peer staged work without manual intervention.
