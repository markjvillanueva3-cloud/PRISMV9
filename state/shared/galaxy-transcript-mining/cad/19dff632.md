# cad session 19dff632 (2026-06-09, 16.1MB, spine 126KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e44a3a1592` – wired orphaned `PayrollLiabilityFilingEngine` methods (`compute940`, `generateW2`, `reconcileW2sTo941`, `contractor1099Totals`) into `prism_business`.  
- `e649790e76` – added fifth orphan `remitLiability` (positional‑arg) and updated tests.  
- `9dfd621910` – committed `scripts/mine-hotel-transcripts.mjs` (Ollama‑based transcript miner).  
- `61518eb988` – fixed four audit bugs in the miner (`isNoise`, empty‑response, combined‑file glob).  
- `099e6b92bd` – added roadmap spec `state/shared/specs/HOTEL-FORGE-ROADMAP-2026_06_09.md`.  
- `f069052772` – verified all 6 “false gaps” from audit are shipped.  
- `dd57b82b52` – U‑HOTEL‑PORTAL‑PERSISTENCE: folded SQLite WAL store into CustomerPortalEngine; 92/92 tests green.  
- `17f3e0ffec` – scrutiny‑fix for persistence build.  
- `c9b5156d0b` – U‑HOTEL‑ALLOWLIST‑WRITE‑REVIEW + REALTIME‑VERIFY: security review of write‑allowlist; SSE transport verified end‑to‑end.  
- `6b9ed8520d` – FALSE‑WIRE‑REGRESSION‑GUARD: 20/20 guard, 3‑of‑3 PASS.

**DECISIONS**  
- Use **Ollama** for token‑heavy mechanical work (transcript mining, summarization).  
- Use **ultracode Workflow** for high‑level reasoning and roadmap synthesis.  
- Adopt a **verify‑first** strategy: confirm audit claims before building.  
- Commit roadmap spec to `state/shared/specs/…` so any slot can execute it.  
- Keep payroll wiring commit under `[BOOTSTRAP-SLOT-ENFORCE]` prefix (shared‑tree).  
- Fold SQLite WAL backing into CustomerPortalEngine instead of separate store to avoid name collision.  
- Add `.db`, `.db-wal`, `.db-shm` ignore rules for `customer-portal.db`.  
- Broaden placeholder regex to match `"Unknown business action: X"` and `{note:"method not callable"}` idiom.  
- Use `qwen2.5-coder:32b` as default grunt‑work coder; reserve higher tier for heavy synthesis only.  
- Commit guard changes immediately after each fix, then run 3‑of‑3 scrutiny before advancing.

**OPERATOR DIRECTIVES**  
- “read previous few sessions of hotel to regain full context”  
- “continue” (multiple) – proceed with hotel work after context recovery.  
- “read transcripts from previous hotel sessions… can we use ollama for better efficiency?”  
- “if we can utilize ollama to generate a brainstormed planned out forge roadmap … if we can't use ollama for high end quality work, utilize ultracode instead”  
- “try again, we hit session limits, had to switch accounts” – resume workflow.  
- “start” – execute the roadmap from the first pending unit.  
- “push through”  
- “can we upgrade the coder to a higher tier model?”  
- “Continue from where you left off.”

**FINDINGS/BUGS**  
- Payroll engine had 5 orphaned methods; all wired in this session.  
- Audit’s “missing tax pillar” was stale—tax engines already exist.  
- Off‑domain drift: ~11 of 19 hotel sessions performed non‑ERP work due to unfiltered loop queue.  
- Shared‑tree commit contention (`index.lock`) observed during builds.  
- `marketplace_lead_get` returned `null`; detector cannot distinguish legit “not found” from false wire—needs chained probe.  
- Regex in placeholder detector broke on tokens ending with `:`; fixed.  
- Placeholder regex missed real dispatcher string → false‑green guard; fixed.  
- Meta‑test used fabricated string, causing R9 failure; corrected to assert actual output.  
- `params.supplierId` mismatch in marketplace lead test; resolved by seeding a lead.  
- `.db` files not gitignored → P1 leak risk; added ignore rules.  
- No remaining false‑wire or param mismatches after fixes.

**DOMAIN SPECIFICS**  
- **Engines/Actions**: `PayrollLiabilityFilingEngine`, `businessDispatcher`, mobile engines (`MobileVoice`, etc.), allowlist actions, DFM bug at `ctx.features`.  
- **Dispatchers**: `prism_business` (lazy `getEngine`), `devDispatcher.ts`.  
- **Metrics/Paths**: payroll tax tables (`payroll-tax-tables.js`), business‑dispatch allowlist (`src/data/business-dispatch-allowlist.ts`).  
- **Unique to Hotel**: ERP‑HR domain, financial‑invariant gate, PII redaction, HR/payroll wiring.  
- PRISM 26‑slot NATO fleet, hotel slot (ERP/HR/business).  
- CustomerPortalEngine persistence via SQLite WAL; rateBuckets remain transient.  
- Realtime SSE transport (`routes/realtime.ts`, `RealtimeEventBridge`) with event types `quote:update`, `job:progress`.  
- Role‑gate design for write‑allowlist (e.g., `handoff_counterparty_respond`).  

**TOOLS USED**  
- `scripts/mine-hotel-transcripts.mjs` (Ollama miner).  
- `ask-ollama.mjs`, `ollama‑cost‑router.mjs`.  
- Ultracode Workflow (`wf_2bfa0b6b-9b0`).  
- Slot‑binding helpers: `slot-bind-enforce.mjs`, `chat-slots.mjs`.  
- Checkin pipeline (`H:/prism/.claude/commands/checkin.md`).  
- Business dispatcher test harnesses (`businessDispatcher.payroll-filing-wire.test.ts`, `businessDispatcher.false-wire-regression-guard.test.ts`).  
- vitest, tsc, git, better-sqlite3, CoordinationStoreEngine pattern.  

**OPEN THREADS**  
1. **U‑HOTEL‑PORTAL‑PERSISTENCE** – persist 4 `CustomerPortalEngine` in‑memory maps to SQLite‑WAL store; kill‑restart readback E2E test (completed).  
2. **ALLOWLIST‑WRITE‑REVIEW / P3‑REALTIME‑VERIFY** – post‑portal persistence, verify allowlist wiring and realtime SSE endpoint (completed).  
3. Resolve `marketplace_lead_get` null ambiguity (chain probe).  
4. Finalize placeholder detector regex bug (already fixed).  
5. Design per‑action role gate to enable manager writes (`handoff_counterparty_respond` and others).  
6. Document/trace realtime producer depth (which business events publish forwarded bus patterns).
