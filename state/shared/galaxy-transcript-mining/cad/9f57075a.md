# cad session 9f57075a (2026-05-17, 20.5MB, spine 56KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `ce2c284651` – deterministic RGS per‑slot allocator (`allocate-rgs-per-slot.mjs`) and 78‑unit spec.  
- `a308f353d8` – top‑up of starved slot queues; updated `state/shared/slot-task-queues.json`.  
- `b66dde0a68` – ghost nodes/wires for feature gaps (`ghost.feature_gap_audit`) and domain pipelines (`ghost.domain_pipelines`); generator scripts registered in `regen-viz.mjs` & `merge‑augmentations.mjs`.  
- `4ad4bb334a` – 18‑stage print‑to‑part pipeline spec (`DOMAIN‑PIPELINE-MS0`) and AI‑training‑first directive queued.  
- `b72faff96d` – `/forge-audit-v2` domain allocation, 3235 units distributed across slots; audit‑gap units added to roadmap.

**DECISIONS**  
- Adopt a deterministic RGS allocator delegating to the existing priority‑queue for reproducibility and drift avoidance.  
- Merge audit‑gap units into the main roadmap and expose them via ghost roosts in system‑viz.  
- Map 15 functional domains onto 12 slots by merging related areas (erp+hr, post+speedfeed).  
- Design a universal 18‑stage print‑to‑part pipeline; each domain receives the subset that applies to it.  
- Prioritize AI training before revenue to maximize feature value and reduce post‑release drips.  
- Implement an end‑to‑end auto‑checkout chain: claim‑on‑pick, heartbeat, release on commit, stop‑advisory.

**OPERATOR DIRECTIVES**  
- Ensure every chat automatically picks up its next unit via `/checkin` + `slot-task-claim`.  
- After shipping a unit, run the queued post‑build utility scan (`U-GAP-POST-BUILD-UTILITY-SCAN`) on lima.  
- Verify system‑viz updates after each commit; monitor ghost roosts for new nodes.

**FINDINGS/BUGS**  
- Initial allocator had empty‑pool exit‑code collision & silent field‑name coupling – fixed.  
- Spec overclaims claim‑filtering – corrected.  
- Minor doc pointer rot: CLAUDE.md references non‑existent `slot-queue.mjs`.  
- Hermes auto‑skill generation gap remains unimplemented.  
- Post‑build utility scan hook not yet built due to token limit.

**DOMAIN SPECIFICS**  
| Slot | Domain(s) | Key Features |
|------|-----------|--------------|
| alpha | mill | machining, tooling, fixture design |
| bravo | lathe | 3‑axis turning, toolpath generation |
| charlie | wire | wire EDM, precision cutting |
| delta | cad | geometry parsing, OCR, feature recognition |
| echo | cam | toolpath generation, simulation |
| foxtrot | tribal | knowledge injection, support |
| hotel | erp+hr | business & employee management |
| india | post | post‑processor generator, master post |
| juliett | speedfeed | feed rate calculation |
| kilo | print2prog | print‑to‑program orchestration |
| lima | academy | learning pipelines, AI training |
| mike | misc | miscellaneous support tasks |

System‑viz ghost roosts: `priority_queue`, `misc_tasks`, `bridge_synergy`, `feature_gap_audit`, `domain_pipelines`.  
Pipeline stages (18 total): PRINT_INTAKE → GEOMETRY_PARSE → PRINT_OCR → FEATURE_RECOG → MATERIAL_SELECT → MACHINE_SELECT → TOOLING_SELECT → FIXTURE_DESIGN → OPERATION_SEQUENCE → SPEED_FEED → TOOLPATH_GEN → SAFETY_VALIDATE → POST_PROCESS → SIMULATE → OPERATOR_GATE → MACHINE_RUN → QUALITY_VERIFY → LEARNING_LOOP.

**TOOLS USED**  
- `/checkin-juliett`, `/forge-audit-v2`  
- `scripts/allocate-rgs-per-slot.mjs`, `topup-slot-queues.mjs`  
- `generate-feature-gap-features.mjs`, `merge‑augmentations.mjs`, `regen‑viz.mjs`  
- `slot-task-claim.mjs`, post‑commit hook (`slot-task-claim-release-on-commit.mjs`)  
- `stop-slot-task-claims-advisory.mjs`, `priority-queue.mjs`  
- `system-viz-query.mjs`, `generate-domain-pipeline-features.mjs`

**OPEN THREADS**  
- Build and deploy the post‑build utility scan advisory hook (`U-GAP-POST-BUILD-UTILITY-SCAN`).  
- Implement Hermes auto‑skill generation unit and enqueue it.  
- Monitor and mitigate memory pressure during large writes (xmalloc OOMs).  
- Confirm regen‑viz cron picks up new ghost roosts immediately after commits.  
- Audit orphan nodes in system‑viz for any remaining gaps.
