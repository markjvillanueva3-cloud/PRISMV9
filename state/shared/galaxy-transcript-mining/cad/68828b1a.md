# cad session 68828b1a (2026-06-04, 26.7MB, spine 164KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hermes config updated to `claude-opus-4-8` (commit 6ead9a74e9).  
- U2 credential‑swap script (`switch-claude-account.mjs`) added & tested (commit a679e455c1, 12 tests).  
- U6 auto‑swap wiring merged into echo’s commit 5cf1a88ed7.  
- RGS pipeline roadmaps written (`MASTER-RGS-ROADMAPS-2026-06-03.md`, 30 rules) (commit 6ead9a74e9).  
- Fleet goal doc committed (`FLEAD-DOMAIN-GOALS-2026-06-03.md`) (commit ed5f6d3cde).  
- Hermes‑Obsidian memory bridge script shipped, scheduled every 15 min.  
- Account‑switch coordinator & wake‑sequencer built & tested (87/87 tests).  
- Octopus consensus upgrade to powerful LLMs – commit `856c417d2b` (56/56 green).  
- Hermes “coder brain” planner + harness emitter – commit `1bb66a1822` (43/43 green).  
- Dream‑cycle OOM fix (inverted‑index blocking) – commit `23de0e7881`.  
- H: drive atlas generator – commit `f2564b81a0`.  
- Reactive 429‑fallback router – commit `443c84d08d` (52/52 green).  
- Hermes wiring script (auto‑rollback, model guard) – commit `c988a21ec4`.

**DECISIONS**  
- Run Hermes on Claude Opus 4.8 via shared subscription to avoid OpenRouter credit exhaustion.  
- Use account‑switch cycle (capture → swap → staggered restart) for 5‑hour quota limits; coordinator auto‑fire when quota data available.  
- Keep `cron_mode: deny` until MCP and kanban wired; enable overnight autonomy thereafter.  
- Wire Obsidian as real MCP via `mcp‑obsidian`; current REST API insufficient.  
- RGS pipeline roadmaps become single source of truth for domain plans (high‑ROI ordering).  
- Use local LLMs (Qwen3‑Coder 30B or Gemma 4 32B) for heavy work, Opus for review; require ≥64 K context.  
- Replace proactive 5h telemetry with reactive 429‑fallback routing.  
- Implement planner mapping failure modes to six 0xCodez workflow patterns before execution.  
- Fix dream‑cycle OOM via inverted‑index blocking + top‑K cap.  
- Build H: drive atlas so Obsidian vault maps all 145 top‑level H: areas.

**OPERATOR DIRECTIVES**  
- “Let’s keep building until we have full autonomous Hermes + Obsidian vault.”  
- “Hold pull until home… pivot to something else but don’t forget about the pull for Ollama.”  
- “Look into full slash command list, assess pipelines, address underutilization.”

**FINDINGS/BUGS**  
- Shared‑tree `git index.lock` contention resolved with pathspec commits.  
- U4 5‑hour populator missing data source (`rate_limits.five_hour` not emitted).  
- Hermes boot‑looped due to OpenRouter credit exhaustion; fixed by switching to Anthropic provider.  
- `cron_mode: deny` blocks scheduled autonomy; must be changed after MCP/kanban ready.  
- Obsidian local REST API is not a true MCP; requires `mcp‑obsidian` bridge.  
- Account‑switch coordinator cannot auto‑fire because quota percentage data is null.  
- Pre‑existing test failures from env isolation bug (`GEMINI_API_KEY` leak).  
- 11 stale consensus test failures fixed by updating defaults & assertions.  
- Dream‑cycle OOM at ~11 k memos; resolved with inverted-index blocking + top‑K cap.  
- Reactive fallback router lacked execution logic; added `routeWithFallback()` and tests.  
- Underutilized slash commands: only advisory nudge present; built executable command→Ollama route registry & runner.

**DOMAIN SPECIFICS**  
- Hermes/Zulu: Electron desktop app, Python backend (`%LOCALAPPDATA%\hermes\`), MCP on :3100, kanban dispatcher, curator, memory store.  
- Account‑switch cycle (U1–U6): capture credentials, swap active account, 5‑hour populator, coordinator, wake‑sequencer.  
- RGS pipeline: `rgs-pipeline-rules.mjs`, unit‑text → skill+reviewer mapping, high‑ROI ordering.  
- Slot‑briefs: `state/shared/slot-briefs/<slot>.md` via `slotBriefEngine.writeBrief`.  
- Cron tasks: Hermes memory bridge (15 min), future cron_mode enablement.  
- Hermes agent architecture: three‑layer memory, ≥64 K context requirement.  
- Octopus consensus engine: multi‑LLM resolution.  
- Workflow planner per 0xCodez patterns.  
- Dream‑cycle synthesis algorithm: Jaccard similarity, inverted index.  
- H: drive atlas generation for Obsidian vault mapping (145 top‑level areas).  
- Reactive 429‑fallback router for rate‑limit handling.

**TOOLS USED**  
- `/startup-bravo`, `chat-slots.mjs`, `CronCreate`, `CronDelete`.  
- Hermes config (`config.yaml`), MCP server, kanban API.  
- Scripts: `capture-claude-credentials.mjs`, `switch-claude-account.mjs`, `account-switch-restart-coordinator.mjs`, `fleet-wake-sequencer.mjs`, `rgs-pipeline-rules.mjs`, `slotBriefEngine.writeBrief`.  
- PRISM tools: octopus engine, Hermes agent, dream‑cycle, atlas generator, reactive fallback router, wiring script.  
- Scripts: `scripts/wire-hermes-local-backend.mjs`, `scripts/trigger-command-pipeline.mjs`.  
- Testing frameworks: node:test, vitest, tsc.  
- Git hooks: lock‑sweep, peer‑commit absorption.

**OPEN THREADS**  
- Enable Hermes `cron_mode` and seed kanban board.  
- Wire Obsidian as real MCP via `mcp‑obsidian` bridge.  
- Implement U4 5‑hour populator once `rate_limits.five_hour` emitted.  
- Fully test auto‑swap and staggered restart with live quota reset.  
- Update octopus model defaults to use powerful LLMs (`gpt‑oss:120b`, `qwen2.5-coder:32b`).  
- Resolve shared‑tree lock contention for future commits.  
- Wire Hermes config to local backend once Qwen3‑Coder 30B downloaded (auto‑rollback guard).  
- Rotate exposed `GEMINI_API_KEY`.  
- Finalize slash‑command routing for high‑ROI commands and skill/container improvements.  
- Enable bidirectional Obsidian vault sync (HMEMV04‑06).
