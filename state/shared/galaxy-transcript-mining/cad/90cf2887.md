# cad session 90cf2887 (2026-06-09, 0.8MB, spine 4KB, 1 slice(s), model gpt-oss:20b)

**DECISIONS**  
- Convert every interactive PRISM scheduled task to **S4U (session 0)** so it never shows a console window.  
- Wrap node/powershell actions in the existing `run‑hidden.vbs` launcher to guarantee hidden execution.  
- Upgrade the daily **PRISM Task Hardener** (`harden-prism-tasks.ps1`) to run with **Highest privilege**; this allows its `Set‑ScheduledTask` calls to succeed instead of silently failing under “Access is denied.”  
- Persist these changes in all installers so re‑registration does not revert tasks back to Interactive.

**OPERATOR DIRECTIVES**  
- *User request:* “Make all local tasks that chat fleet spawns run hidden, no pop‑ups during gaming sessions.”

**FINDINGS/BUGS**  
- Only **2 of 47 PRISM tasks flash windows**:  
  - `PRISM Hermes‑Obsidian Bridge` – Interactive principal, launches `node.exe` directly.  
  - `PRISM Zombie Reaper v2` – Interactive principal, spawns a child `node.exe` after a hidden PowerShell wrapper.  
- The **Task Hardener** runs as S4U with `RunLevel=Limited`; elevation is required to modify root‑folder tasks but the script silently fails (`Access is denied`) and exits successfully, leaving the flashing tasks untouched.  
- `Hermes‑Obsidian Bridge` has no installer; it remains registered as Interactive.  
- `Zombie Reaper v2` installer registers the task without a principal, causing it to default to Interactive.

**DOMAIN SPECIFICS**  
- **PRISM Task Hardener** (`harden-prism-tasks.ps1`) – daily self‑healing sweep that converts Interactive → S4U.  
- **run‑hidden.vbs** – wrapper used by other PRISM reapers to hide console windows for node/powershell actions.  
- Windows **Task Scheduler API** (`Set-ScheduledTask`), S4U principals, session 0 execution context.  
- Node.js and PowerShell invoked via scheduled task actions.

**TOOLS USED**  
- PowerShell scripts: `harden-prism-tasks.ps1`, installer `.ps1` files.  
- `run‑hidden.vbs` launcher.  
- Windows Task Scheduler API (`Set-ScheduledTask`).  
- Elevated privilege checks (to ensure Highest run level).

**OPEN THREADS**  
- Add S4U principal to the Hermes‑Obsidian Bridge registration (create or update its installer).  
- Ensure all PRISM installers use `run‑hidden.vbs` for node/powershell actions.  
- Verify that the Task Hardener runs with Highest privilege on every machine and actually modifies tasks.  
- Test end‑to‑end: no scheduled task should flash a window during gaming sessions.  
- Resolve API rate‑limit issues encountered while applying automated updates.
