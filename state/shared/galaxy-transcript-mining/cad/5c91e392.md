# cad session 5c91e392 (2026-06-09, 16.2MB, spine 150KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit cdc9ec44b3: reaper‑immune scheduled task for unattended closed‑loop training; GPU+CPU ready, resumable loop with cursor.  
- Commit b76b4d55ab: resumable corpus loop (stream‑append + resume cursor).  
- Commit 01ce06d589: manifest builder (`build-print-corpus-manifest.mjs`) and multi‑page OCR fix.  
- Commit 5a8dbfefdd: doc‑reflection updates (CLAUDE.md, wiki, MEMORY.md).  
- Commit 250ffb8a6e: `scripts/find-perfect-parts.mjs`, 10‑test suite, markdown/JSON report of 91 perfect print+CAD+CNC triples; true‑test harness fixed.

**DECISIONS**  
- Use `xray` slot‑binding wrapper to guarantee handoff before `/checkin`.  
- Build resumable OCR training loop with stream‑append + resume cursor; reaper‑immune scheduled task for unattended run.  
- Manifest counts only drawing‑type prints (7 794) as denominator, not 257 K.  
- Rasterize all PDF pages (fix page‑0‑only bug).  
- Select reliable VLMs (`qwen3-vl:8b-instruct`, `qwen2.5vl:7b`); ignore failing models.  
- Use CNC program feed‑move geometry (G1/G2/G3) as ground truth; cluster diameters to collapse roughing ramps.  
- Add binary‑file guard (`isParsableNcText`) and feed‑move aware extraction.  
- Resolve print‑filename sanitization bug; reclassify program axis correctly (mill vs lathe).  
- Conclude OCR recall < 5 % on real scanned PDFs; postpone full‑corpus scaling until OCR improves.

**OPERATOR DIRECTIVES**  
- Monitor reaper‑immune scheduled task to completion and final trainset output.  
- Run the 91‑part true‑test as gate before scaling corpus.  
- Commit verdict report (markdown + JSON) and finish `/checkin-xray` pipeline (slot claim, handoff, build, test).  
- Integrate true‑test harness into regular `/checkin` so it runs automatically on future commits.

**FINDINGS/BUGS**  
- OCR reads only 1–3 dimensions per scanned print; recall = 0 vs CNC GT.  
- Initial GT metric inflated by all toolpath points; fixed to callout‑class diameters.  
- Binary `.mcx‑8` files produced garbage coordinates; now rejected.  
- Mill programs misclassified as lathe caused spurious GT; corrected.  
- Print resolver failed on filenames with `/` and `:`; sanitized before basename.  
- GPU contention caused one VLM to fail under heavy fleet load; mitigated by scheduling at quiet window.  
- Reaper kills occur, but resume cursor guarantees no re‑OCR of completed prints.

**DOMAIN SPECIFICS**  
- PRISM architecture: `chat-slots.mjs`, `slot-bind-enforce.mjs`, `/checkin` pipeline.  
- Runner script `blueprint-ocr-training-loop.mjs`; LoRA staging wrapper `xray-trainset-to-lora.mjs`.  
- Manifest builder `build-print-corpus-manifest.mjs`.  
- CNC program parser (`extractProgramGT`) – feed‑move extraction, axis classification, diameter clustering.  
- Step geometry surface via `prism_cad:cad_step_parse_file` (optional STEP GT).  
- Metrics: recall, precision, calloutGT count, toolpath_points.  
- GPU environment: Blackwell RTX PRO 6000, torch 2.11+cu128, bitsandbytes, qlora, triton.

**TOOLS USED**  
- Node.js (`*.mjs` scripts), Python (`pdf‑to‑png.py`, `gpu_health.py`).  
- Ollama for VLM inference.  
- Git for slot claiming and commit hygiene.  
- PowerShell (`run‑ocr‑batch‑overnight.ps1`, `install‑blueprint‑ocr‑batch‑task.ps1`) for reaper‑immune task.  
- `scripts/find-perfect-parts.mjs` + test harness (`test/*.mjs`).  
- PRISM dispatcher actions (`prism_cad:cad_step_parse_file`, `findOne` resolver).  
- Slot claim helper (`chat-slots.mjs`) and `/checkin-xray` wrapper.

**OPEN THREADS**  
- Full corpus training run still in progress; monitor completion.  
- LoRA fine‑tune pending on real trainset (operator gold labels).  
- Verify GPU contention resolution or adjust model selection/scheduling if needed.  
- Confirm reaper‑immune task finishes entire corpus and produces final training data.  
- Publish verdict report and integrate true‑test harness into `/checkin`.  
- After OCR improvements, re‑run full‑corpus test to confirm recall > 90 %.
