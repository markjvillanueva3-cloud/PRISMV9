# cad session 4e607fe7 (2026-06-06, 16.9MB, spine 55KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-GNN-SELECTIVE-DEPLOY` (commit b0b5b08716): proved calibration dead‑end; wired selective‑prediction eval; GNN tier‑5 deploy‑ready‑selective @ τ=0.7.  
- `U-LEG-STATE-SELECTIVE-WIRE` (15eceefa24): surfaced SELECTIVE‑DEPLOY in PSN banner and health digest.  
- `U-SELECTIVE-CLASS-HONESTY` (0977fea472): surfaced class concentration (“spans 2/6 classes”) to correct macro‑F1 claim.  
- `U-CALIB-GATE-SOURCE` (cd8fcd6159): single‑sourced calibration gate constants from `GATE_THRESHOLDS`.  
- `U-GPU-STACK-PROVISION` (fb3062febb): provisioned Python 3.13 GPU stack, torch 2.11+cu128 sm_120, bitsandbytes NF4; wired `PRISM_PYTHON_GPU_PATH`.

**DECISIONS**  
- Calibration is a dead end → focus on selective‑deploy evaluation.  
- Deploy gate thresholds sourced from `GATE_THRESHOLDS` to avoid drift.  
- Class concentration must be surfaced to prevent overclaiming macro‑F1.  
- Provision GPU stack locally (bypass golf) to unblock MS2/MS3/MS4.  
- Use risk‑coverage curve at production τ = 0.7 as primary deploy metric.

**OPERATOR DIRECTIVES**  
- “bypass golf and do it yourself”

**FINDINGS/BUGS**  
- Calibration miscalibration only 0.0197 of Brier; best honest calibrator 0.178 > 0.15 → calibration gate closed.  
- Selective‑deploy evaluation: GNN tier‑5 deploy‑ready‑selective @ τ=0.7, Brier 0.101, macro‑F1 0.587.  
- Class concentration: emitted set spans only 2 of 6 dispatcher classes; macro‑F1 1.0 over concentrated subset.  
- Sentence‑transformers segfaults on import (pyarrow native module conflict, Windows/py3.13).  
- GPU stack provision succeeded with torch 2.11+cu128 sm_120 and bitsandbytes NF4.

**DOMAIN SPECIFICS**  
- PSN leg #10 (NN/GNN) deploy gate, selective‑deploy evaluation.  
- Risk‑coverage metrics: coverage, Brier, macro‑F1, AUROC.  
- Deployment thresholds: `GATE_THRESHOLDS` constants for Brier and macro‑F1.  
- GPU stack path `PRISM_PYTHON_GPU_PATH` used by Node bridge and scheduled tasks.  
- ModelRoutingEngine integration with OllamaCapabilityProbeEngine.

**TOOLS USED**  
- `/checkin-india` wrapper (chat‑slots.mjs, audit-roadmap-drift.mjs).  
- `CronCreate`/`CronDelete` for recurring loop.  
- `nn-graph-eval.mjs` harness for evaluation metrics.  
- `gpu_health.py` gate for GPU verification.  
- `uv` for Python 3.13 installation.  
- `pip install` for torch, bitsandbytes, sentence‑transformers, peft, accelerate, datasets.  
- Node bridge settings.json env block.

**OPEN THREADS**  
- GNN GPU retrain (MS3) – requires PyG install and training script; not yet built.  
- LoRA trainer (MS4) – pending after GPU stack provision; training pipeline not started.  
- RAG GPU batch embed (MS2) – sentence‑transformers bug unresolved, fallback to Ollama nominal.  
- `qwen2.5-coder:32b` pull for model routing – pending.
