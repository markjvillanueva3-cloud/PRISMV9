# cad session 32a39c0c (2026-05-16, 34.7MB, spine 179KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U‑PPL‑A1 – TurningMinFingerprintEngine (573 L, 69 tests) → dispatcher actions `turning_min_fingerprint`, `turning_min_classify`; committed a76ea58c5.  
- U‑PPL‑B1 – ProgramReoptimizationOrchestratorEngine (474 L, 48 tests) → wiring `prism_cam:program_optimize`, `prism_mill:mill_program_optimize`; committed 4050f3b35.  
- Phase21 PDF splitter (`phase21-split-containers.py`) – split 8 396 multi‑print containers into `Docustrata/_organized/PRINTS/` (job `b7dvt7ale`).  
- phase22-delta-detector.py, phase6b-delta-page-count.py – delta detector & ledger bridge for 7 235 PDFs.  
- Updated `docustrata-pipeline.py` – 9‑stage orchestrator with VLM‑aware join, customer‑folder rollup, memsafe deep‑rescan.  
- DocustrataCustomerIndexEngine.ts – dispatcher case, Zod schema, 61 Vitest tests (PASS).  
- synergy-regression-watch.mjs + `install-synergy-watch-task.ps1` – daily Windows scheduled task active.  
- helper-orphan-rank.mjs – pure core injection + real‑data E2E; 8/8 tests PASS; baseline: 187 helpers, 76 orphan (40.6 %).  

**DECISIONS**  
- Ship A1 & B1 immediately; no further work in this session.  
- Skip U‑PPL‑B2 build; handoff research for next session.  
- After split job completes, run full re‑index of 104 K PDFs and launch OCR pipeline (CPU classifier → GPU PaddleOCR/eDOCr2/VLM fallback).  
- Use Docker + NVIDIA GPU for stages 3–5; restart daemon before use.  
- Unified orchestrator: GPU OCR output optional in join; deep‑rescan memsafe variant.  
- Add customer‑folder index engine; schedule durable installer instead of cron.  
- Pure core injection + real‑data E2E for validation; enforce Windows path case‑insensitivity, mandatory anchor/non‑degeneracy floor/negative control bracketing; replace NUL bytes with spaces.  

**OPERATOR DIRECTIVES**  
- Verify `b7dvt7ale` completes before proceeding.  
- Run `/loop continue finding development tool and pipeline upgrades`.  
- Build DocustrataCustomerIndexEngine test file (`/checkin-foxtrot`).  
- Execute `/forge-audit-v2 /system-viz`.  
- Schedule synergy‑regression‑watch.mjs via durable installer.  
- Restart Docker Desktop / WSL to enable GPU containers.  

**FINDINGS/BUGS**  
- .MIN seed corruption: 5 of 7 templates corrupted (null‑bytes, JSON state); restore from external backup.  
- Git DAG reflog corruption: `git log --all` fails; HEAD and objects intact.  
- Lane‑guard mis‑resolution: foxtrot slot evicted to “kilo”; resolved by unstaging files before commit.  
- Docker daemon down – blocks GPU stages.  
- Phantom 104 K PDFs actually 7 235 delta files.  
- GPU OCR not used in join; now integrated with optional VLM fields.  
- Deep‑rescan OOM on large PDFs fixed via memsafe variant.  
- `cmd /c` quote‑strip bug prevented scheduled task; fixed with double outer quotes.  
- PS 5.1 codepage misparsed non‑ASCII em‑dashes/smart quotes – cleaned to ASCII.  
- Stale system‑viz graph (>6 h) accepted for audit.  
- Hook count growth, orphan helper ratio (159/187), synergy regression (-1.09 pp).  
- Transient `xmalloc` fork‑storm in PreToolUse/PostToolUse hooks.  
- `main()` not firing on Windows due to drive‑letter/slash case mismatch.  
- Literal NUL bytes at offsets 7685/7695 corrupting JS files; removed via latin1 round‑trip.  
- PostToolUse hook crashes; PreToolUse:Write hook fails to apply write.  
- Orphan rate overstated (85 % vs actual 40.6 %).  

**DOMAIN SPECIFICS**  
- PRISM architecture: multi‑chat fleet, 10 slots (alpha–foxtrot work, golf hygiene).  
- Docustrata ingestion: 215 k PDFs in `H:/prism/Docustrata`; phase15 deep‑scan, phase20 verified prints; phase21 splitter active.  
- Engine stack: OkumaOSPParserEngine → TurningMinFingerprintEngine (seeded by 2 clean .MIN templates) → dispatcher actions; ProgramReoptimizationOrchestratorEngine uses LatheProgramOptimizerEngine & MillProgramOptimizerEngine.  
- GPU pipeline: PaddleOCR, eDOCr2, VLM (PaddleOCR‑VL‑0.9B or dots.ocr‑3B), containerized via Docker.  
- Engines: DocustrataCustomerIndexEngine, BlueprintProgramJoinEngine; dispatcher `cadDispatcher` with `docustrata_customer_index`.  
- Metrics: synergy ratio, delta PDF count, OCR page‑count, GPU usage, helpers vs wired‑strong/cross‑helper counts, orphan rate.  
- Paths: `H:/prism/Docustrata/.index/*`, `scripts/docustrata/*.py`, `src/mcp-server/...`, `helper-orphan-rank.mjs`, `cold-script-rank.mjs`.  

**TOOLS USED**  
- chat-slots.mjs (claim/reclaim), per-agent-handoff.mjs, `/checkin.md` pipeline, audit-roadmap-drift.mjs, milestone-tracker.mjs.  
- phase21-split-containers.py (PyMuPDF, CPU parallel).  
- Docker + NVIDIA GPU containers for OCR stages.  
- Python (phase scripts, orchestrator), Node.js (`system-viz-query.mjs`, `system-synergy-map.mjs`), PowerShell (`install‑synergy-watch-task.ps1`).  
- Vitest + tsc for engine tests; esbuild for bundling.  
- GPU OCR: Qwen2.5‑VL, PaddleOCR, eDOCr2, Tesseract.  
- PRISM pure core injection, real‑data E2E harness, Git post‑commit hook, settings.json, PreToolUse/PostToolUse hooks, byte‑level NUL removal via latin1 round‑trip, Windows path case‑insensitive gate.  

**OPEN THREADS**  
- U‑PPL‑B2 build: wiring into 18 k‑line dispatcher; requires new session with full research handoff.  
- Re‑index 104 K PDFs (phase6→7→15) to update Docustrata scan.  
- GPU stages: restart Docker daemon, then launch orchestrator stages 3–5.  
- Restore corrupted .MIN templates from backup or manual reconstruction.  
- Remaining audit meta scripts (F3/F6) under construction; verify synergy watch logs and regression alerts.  
- Resolve fork‑storm crashes in PreToolUse/PostToolUse hooks; re‑run cold-script-rank.mjs after write fix.  
- Verify `main()` fires correctly on Windows; finalize orphan rate metrics/baseline stats.  
- Ensure all NUL bytes removed across repository.
