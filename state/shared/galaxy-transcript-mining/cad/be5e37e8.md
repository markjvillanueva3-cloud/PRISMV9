# cad session be5e37e8 (2026-05-19, 28MB, spine 135KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U‑FGE01 – `CADClassFeatureLibraryEngine.buildSequenceForEvidence` + dispatcher `cad_class_build_sequence_evidence`. 35 tests, 4 P0/P1 fixes.  
- U‑BPA‑CONSUMER – offline JSONL consumer (`scripts/lib/blueprint-accuracy-consumer-lib.mjs`). 35 tests.  
- U‑FGE02 – wired evidence ranking into live `cad_class_drive_build` (optional flag). 1 commit, 0 new TS errors.  
- U‑TDP01 – training driver (`training-driver-print-to-cam.mjs`) with stub adapters; full 4‑stage pipeline verified on synthetic data.  
- U‑TDP02 – batch harvester (`harvest-pdf-text-batch.{py,mjs}`) idempotent registry, 25 tests.  
- U‑TDP03 – extraction aggregator (`aggregate-extractions-to-template.mjs`) using Welford stats; 35 tests.  
- U‑TDP04 – OCR benchmark runner (`benchmark-vs-cad-gt.mjs`); 34 tests, full P/R/F1 metrics vs CAD GT.  
- U‑TDP05 – CAD‑derived ground truth extractor (`cad-gt-extractor.mjs`). 39 tests, 24 regression fixes.  
- U‑TDP07 – deterministic embedded‑text PDF extractor (`pdf-text-extract-lib.mjs`) with inline‑± pattern & OCR‑fragment guard; 57/57 tests, A/B proof (4→6 PDFs, 30 dims). Committed to `slot/mike`.  
- U‑TDP08 – new regexes for paired‑tolerance bands & Rc hardness ranges. 77/77 tests pass.  
- Picker‑fix commit `1576134f55` on main (`cad-fusion-live-ms0`) removes HOOK‑SYNERGY‑MS0 units from priority queue.

**DECISIONS**  
- Split architecture: deterministic (U‑TDP07) for ~13 % text‑rich PDFs, vision (pending U‑TDP06) for ~87 % image‑only.  
- Vision OCR via Ollama: prefer `qwen2.5vl:7b` (~5 GB) on 10 GB RTX 3080; fallback to `llama3.2‑vision:11b` on 16 GB RTX 4080.  
- Adopt R12 fail‑loud, per‑file scrutiny gate; all P0/P1 defects fixed before commit.  
- Store extraction state in `blueprint-accuracy-events.jsonl → state.json`; aggregate templates into `shared/learned-templates`.  
- OCR training proceeds with deterministic path (U‑TDP07/08) until a vision model fits 10 GB RTX 3080; qwen2.5vl:7b chosen fallback for remaining PDFs.  
- Vision OCR (`llama3.2‑vision:11b`) blocked on current GPU; no new vision work until suitable model pulled.  
- Wire deterministic extractor into `U‑TDP04` benchmark grader to generate real training signals.

**OPERATOR DIRECTIVES**  
- Commit U‑TDP07 to `slot/mike`, then run `/compact`.  
- Pull `qwen2.5vl:7b` via Ollama; verify OCR accuracy on a sample JM Die PDF.  
- Integrate vision adapter into U‑TDP01 driver (`--use-vision`).  
- Run full corpus harvest (76 166 PDFs) and collect metrics with U‑TDP04 vs CAD GT.  
- Do not step on Mike’s toes – OCR work owned by BLUEPRINT‑OCR‑TRAINING‑MS1/2; handoff OCR entirely to that milestone.  
- Maintain `slot/mike` worktree discipline: all commits for this session are on `slot/mike`.  
- R12 fail‑loud, no stubs or weakened assertions, per-file scrutiny with two reviewers and a 3‑of‑3 stop gate.

**FINDINGS/BUGS**  
- `moondream:1.8b` hallucinated dimensions; discarded for accuracy.  
- `llama3.2‑vision:11b` OOM on 10 GB GPU; fits only on 16 GB RTX 4080.  
- Parser had OCR fragment bug (fabricated “476”/“3345”); fixed with boundary guard.  
- Split‑token dropped‑dot tolerance (“- 10”) fabricated; now refuses, never fabricates.  
- Inline‑± pattern added; increases coverage from 4→6 PDFs, 30 dims on rich‑text subset.  
- Only ~3.5 % of JM Die PDFs contain embedded text; rest image‑only.  
- Moondream hallucinations produce unusable JSON.  
- Existing extractor missed paired‑tolerance bands & Rc hardness ranges; new patterns added in U‑TDP08.  
- Priority‑queue bug: namespace mismatch caused complete HOOK‑SYNERGY units to leak into picks.

**DOMAIN SPECIFICS**  
- Engines: `CADClassFeatureLibraryEngine`, `BlueprintExtractionRAGEngine`, `GroundTruthRegistryEngine`.  
- Dispatchers: `cadDispatcher`, `blueprint-accuracy-consumer-lib.mjs`.  
- Metrics: precision/recall/F1, dimension distribution (mean ± stddev), tolerance coverage.  
- Paths: `slot/mike` worktree (`slot/mike` branch), U‑TDP units, harvest script in `scripts/harvest-pdf-text-batch.{py,mjs}`.

**TOOLS USED**  
- PRISM CLI (`/checkin-mike`, `/loop`).  
- Node.js scripts (`chat-slots.mjs`, `milestone-tracker.mjs`).  
- Python `fitz` (PyMuPDF) for PDF → PNG rendering.  
- Ollama HTTP API; models: `moondream:1.8b`, `llama3.2‑vision:11b`, pending `qwen2.5vl:7b`.  
- Node test framework (`node:test`).  
- Git worktree (`slot/mike`), commit hooks, per-file scrutiny agents.

**OPEN THREADS**  
- Vision OCR path implementation (U‑TDP06).  
- Pull and validate `qwen2.5vl:7b`; integrate into driver.  
- Scale extraction to full 76 166 PDF corpus; compute accuracy metrics.  
- Train LoRA/fine‑tune model on extracted data; evaluate CAD/CAM generation quality.  
- Add additional text‑extraction patterns (e.g., legacy AutoCAD/Inventor formats).  
- Verify GPU passthrough and memory usage for vision models on 16 GB RTX 4080.  
- Wire U‑TDP07/08 into `U‑TDP04` grader (`makeLiveExtractor` → real extraction).  
- Run full 600‑PDF corpus delta measurement (current subdir rate 21 % vs baseline 13.5 %).  
- Add comma‑decimal normalization, MAX/MIN qualifiers, slash‑paired tolerance (candidate U‑TDP09).  
- Finalize benchmark harness and publish training data for downstream CAD/CAM phases.
