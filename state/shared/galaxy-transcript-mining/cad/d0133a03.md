# cad session d0133a03 (2026-06-10, 54.6MB, spine 498KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `23ad2cdf3e`: added HONESTY RULES block, updated `tournament-rank.mjs`, `intake-quarantine-guard.mjs`, `save-workflow.md`; built & unit‑tested `GroupRelativeRewardNormalizerEngine` (GRPO) and wired into `aiReasoningDispatcher`.  
- `037e3ac930`, `3fa529432f`: ULTRACODE Order 3 – GRPO implementation.  
- `46553bb74a`: added `rankTrajectories` to `MultiModelConsensusEngine`.  
- `accf6f247f`: fixed spec drift; added fact‑checker pointer in CLAUDE.md HONESTY block.  
- `cebde4fd94`: Ollama keep‑alive fix (`keepAlive=30m`, `maxLoaded=4`).  
- `20181a4c78`: G‑Wizard tool‑crib exporter (`GWizardToolCribExportEngine`).  
- `df6bf7a4d1`: newline‑in‑cell bug hardening.  
- `44c41ee643`, `a6d80537cf`: Fusion machine‑library export (1082 machines, round‑trip verified).  
- `4ab181a78d`: `HolderSelectionEngine` core (loads 643 real holders).  
- `82ca289ef4`: Fusion holder wiring via `holderSelectionEngine.select()`.  
- `7783cbef3b`: Mastercam holder wiring.  
- `6064ace7a4`: lifted 5 k‑tool cap in hyperMILL/Mastercam exporters (now ~74 k).  
- `4a2f84eacc`: HyperMill holder wire (real holders into all CAM exporters).  
- `96b7849416`: material→type→brand tooling DB (218 JM tools → 41 leaf CSVs, pre‑optimized SFM per material).  
- `592133f902`: HyperMill per‑tool SFC cutting‑data ceiling (`max_spindle_speed`, `max_feedrate`).  
- `6e0c8cba8c`: holder type→brand DB written to disk (9 leaf CSVs).  
- `ed312a4d91`: Mastercam `exportFromTools(prismTools[])` entrypoint.  
- `361e4710e1`: U‑WIRE‑JMDB – `JMCustomerVendorDatabaseEngine → prism_business` (8 actions).  
- `818870ea59`: U‑WIRE‑DOCUSTRATA – `DocuStrataMaterialPriorEngine → prism_data` (5 actions).

**DECISIONS**  
- Fleet‑reaper ownership shifted alpha→golf; disabled legacy allowlist hook (`PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1`).  
- Restored full fleet‑hygiene cluster (12 tasks) after bulk‑disable of 46/47 PRISM tasks.  
- Chose GRPO as highest‑leverage unit; added optional `group_advantage` ledger field for backward compatibility.  
- Added `rankTrajectories` method to avoid mutating `ask()` in `MultiModelConsensusEngine`.  
- Set Ollama keepAlive=30 min, maxLoaded=4; restarted daemon post‑config change.  
- Left feature crons (knowledge/wiki/tribal, Vault) disabled pending operator action; cross‑PC gitignore of auto‑generated wiki pages decided as policy.  
- HSMAdvisor adapter limited to single `<Tool>` block; awaiting new library format/writer or updated adapter.  
- Prioritized Fusion machine export first; used proven CSV tool‑library with holder columns; implemented pure `HolderSelectionEngine` core before wiring.  
- Wired real holders into Fusion → Mastercam → HyperMill (next).  
- Adopted high‑end local LLMs (qwen2.5‑coder 32B, gpt‑oss 120B) for scrutiny when Claude reviewers rate‑limited.  
- Organized tooling DB by material→type→brand with pre‑optimized SFM; added `exportFromTools` to Mastercam exporter.  
- Resolved slug collisions via injective slugs; guarded ISO segment collision with fail‑loud invariant.  
- Wired all database engines to dispatchers; replaced `__dirname` in `catalogLoader.ts` with `import.meta.url`; adopted explicit `found/count` contract; stopped new multi‑file units when budget >65 % (YELLOW).

**OPERATOR DIRECTIVES**  
- `/checkin-golf`: force golf slot, bind handoff to `golf-work`, always run fleet‑reaper.  
- Bypass legacy allowlist hook globally or session‑scoped.  
- Build ultracode synergy via `/forge7` and PSN; finish remaining tasks.  
- “go back git tree work”; “lets build”, “apply all fixes and look for more gap fills”.  
- `/loop [10m] /goal [utilize strongest ollama llms … | goal clear: complete all remaining romeo tasks and units in current queue and overall work queue] /yolo-mode`.  
- `/go` – start Mastercam holder wiring now; continue CAD/CAM database work with Fusion prioritized.  
- “All tool holder, tooling, inserts and machine databases are added to fusion, hypermill, mastercam, hsmadvisor, gwizard, prism sfc, mill and lathe wizards apps and galaxies.”  
- “Now that you finished machines for fusion, populate the tool holder and tooling database…”.  

**FINDINGS/BUGS**  
- Legacy allowlist hook blocked non‑hygiene writes → bypassed.  
- Fleet‑reaper task disabled; 46/47 PRISM tasks bulk‑disabled → restored hygiene cluster.  
- Initial monitor loop died on transient failure → replaced with resilient loop.  
- `tournament-rank.mjs` dead code & bracket‑logic bug fixed.  
- `intake-quarantine-guard.mjs` lacked tests for exfil‑bypass patterns → added comprehensive suite.  
- `group_advantage` field not persisted through append → corrected wiring.  
- Critical memory pressure from Ollama `keepAlive=-1` (~70 GB) resolved by setting keepAlive=30m, maxLoaded=4.  
- Reaper flagged stale MCP server; guardian mitigates priority inversion.  
- G‑Wizard export newline bug fixed.  
- HSMAdvisor adapter limitation (single tool state only).  
- MCP server crash‑loop observed 5×; restart restores service but OOM/heap issue unaddressed.  
- HyperMill holder wire had `tool_length` floor clamp bug → fixed by using real holder gauge for spindle‑taper only.  
- CoatingMult substring shadow bug (`"altin".includes("tin")`) resolved with longest‑key‑first logic.  
- Slug collision in partitioning (type/brand keys) resolved via injective slugs; ISO segment collision guarded.  
- Missing `max_spindle_speed`, `max_feedrate` columns added to HyperMill schema.

**DOMAIN SPECIFICS**  
- Slot management: `chat-slots` helper (`reclaim`, `claim`), `slot-bind-enforce.mjs`.  
- Fleet‑reaper: orphan‑process janitor, GPU coordinator, Ollama routing‑hint emitter.  
- Ultracode dynamic workflows: 6 patterns (classify‑and‑act, fan‑out‑synthesize, adversarial verification, generate‑and‑filter, tournament, loop‑until‑done).  
- GRPO engine: group‑relative reward normalization across trajectories; z‑score with rank fallback.  
- `MultiModelConsensusEngine` now includes `rankTrajectories`.  
- PRISM engines: `FusionMachineLibraryExportEngine`, `HolderSelectionEngine`, `MastercamToolExportEngine`, `HyperMillToolExportEngine`, `GWizardToolCribExportEngine`.  
- Dispatchers: `prism_cam` (tool/machine exports), `prism_calc` (HSMAdvisor/G‑Wizard).  
- Data sources: `ToolCatalogEngine`, `ToolHolderDatabaseEngine`, `MachineProfilesCatalog`, `JMCustomerVendorDatabaseEngine`, `DocuStrataMaterialPriorEngine`.  
- Formats: Autodesk XML `.machine`, JM CSV tool library with holder columns, CSV_TOOLS_VERSION_1 (173 cols).  
- SFC cutting‑data lookup via `UltimateSpeedFeedEngine.lookupCuttingData()`.

**TOOLS USED**  
- Node.js (`.mjs` scripts, TypeScript compiler, Jest/TS‑Test, Vitest).  
- Playwright for ultracode source reading.  
- `/forge7`, Workflow tool for dynamic orchestration.  
- PowerShell & Git for task orchestration and commits.  
- Local LLMs: qwen2.5‑coder 32B, gpt‑oss 120B (Ollama offload).  
- esbuild, tsx for ESM handling; `import.meta.url` replacement in `catalogLoader.ts`.  
- AI tooling: Ollama large models for searches, coding, and scrutiny.

**OPEN THREADS**  
1. **HSMAdvisor export** – determine existence of bulk library format or create new writer; design round‑trip exporter once spec confirmed.  
2. **MCP server stability** – monitor crash‑loop pattern; document root cause (heap/oom) for owner action.  
3. **HyperMill holder wiring** – complete same pattern as Mastercam; finalize integration tests and documentation.  
4. **Replicate tooling DB to Mastercam/HyperMill** – decide on source fidelity (CSV adapter vs catalog objects); generate per‑leaf libraries, validate against `prism_cam`.  
5. **Expand REGOFIX/TUNGALOY catalogs** – slated for blueprint‑vision/catalog‑enricher task.  
6. **Pending database engine wiring** – next candidates: `ERPImportEngine`, `SemanticAssetIndexEngine`, `LocalEmbeddingEngine`.  
7. **Gitignore of auto‑generated wiki pages** – decision pending; 9 242 tracked files.  
8. **2 733 unpushed commits** – coordinated push required.  
9. **`gpt‑oss:120b` pull completion** – monitor manifest finish, model load status.  
10. **Feature crons (knowledge/wiki/tribal, Vault)** – remain disabled; operator action pending.
