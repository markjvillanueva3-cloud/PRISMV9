# cad session be279b4f (2026-06-11, 42.3MB, spine 286KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- LAUNCH‑PRISM‑FLEET.bat updated (256 MB size cap, self‑regen wrapper).  
- Added recover-today-context.mjs & session-start-auto-resume.mjs; 11 slot recovery files.  
- Implemented launcher improvements: self‑regen, recovery refresh, launch summary/log, smarter liveness‑skip.  
- Extended ollama-docker-launcher.mjs with `--ensure-native-ollama` (commit 1645c20d83); pre‑warms Docker Desktop + compose stack and native Ollama before fleet spawn.  
- U1 resolveExecutor lane router – commit 05cd41c100, 52/52 tests.  
- U7 wire executor contract into /checkin.md, /startup.md, /goal – commit 4c0b87c315.  
- Docker data moved from C: to H: via junction; containers/volumes intact.  
- U‑FLOR-MODEL‑EXPAND (qwen3-coder 30B & deepseek‑r1 32B) – commit a06de59033.  
- U‑FLOR-HYBRID‑DEFAULT (hybrid RAG fleet‑wide) – commit 52b83b819f.  
- U‑FLOR-CODER‑DEFAULT (qwen3-coder 30B active) – commit 904c32c193.  
- U‑FLOR-MCP‑HEAP (daemon/supervisor heap floor unified) – commit 806423f1e5.  
- U‑FLOR-SYNERGY-DOCREFLECT cascade‑propagated to all 34 galaxies.  
- U‑FLOR-BRIDGE-LORA-WIRE (4aedb8ab94) – bridge‑reasoning LoRA source wired, tests pass.  
- souls.md stamped for every galaxy (34/34) – commit 5772941d2b.  
- b6bc5de8cd – opt‑in deep‑reasoning mode (`--deep`) added to galaxy bridge; default fast coder remains. 25/25 tests.  
- 5ffc77fb35 – LoRA assembler updated for `{prompt,completion}` pairs; registered `wiki-canonical-pairs.jsonl`; trainingReady gate true (1138 rows).  

**DECISIONS**  
- Native Ollama :11434 primary LLM runtime; vLLM gated off (`PRISM_VLLM_ENABLE=0`).  
- Docker compose stack separate; prism‑server image build failure noted but not required.  
- Auto‑offload hooks advisory until lane‑aware router & slash‑command integration complete.  
- Move Docker data from C: to H: via junction; compact vhdx rejected due WSL2 ext4 non‑zeroing freed blocks.  
- Use Ollama pulls instead of Docker Model Runner for new models.  
- Activate hybrid RAG by default fleet‑wide; qwen3-coder 30B set as active code model.  
- Align daemon & supervisor heap floors to `PRISM_MCP_HEAP_FLOOR_MB` to fix MCP OOM.  
- Keep fast coder model as bridge default; expose deep‑reasoning via flag/env var.  
- Flip LoRA trainingReady by integrating real wiki data (schema mismatch blocker).  
- Accept GNN full retrain GPU‑bound and out of scope for this session.  

**OPERATOR DIRECTIVES**  
- Free C: space: `docker system prune -a -f`, cancel hanging pulls.  
- Verify existing Ollama models; pull missing ones before proceeding.  
- Commit smart.md changes (currently git‑ignored) once disk space sufficient.  
- Increase pagefile from 64 GB to 256 GB; raise Ollama resident‑model cap.  
- Compact storage and continue autonomously after goal completion.  

**FINDINGS/BUGS**  
- C: drive 0.08 GB free → Git ops fail; Docker pulls aborted ENOSPC.  
- Docker compose prism‑server image fails to build (~2.9 GB context).  
- smart.md edits untracked; changes live on disk but not versioned.  
- Docker vhdx (~302 GB) remained on C: despite prune; compact via diskpart failed due ext4 blocks not zeroed/TRIMmed.  
- `.claude.json` corrupted (0 bytes) from disk‑full write; restored from backup.  
- MCP server OOM caused by mismatched heap floor: daemon 4 GB, supervisor 24 GB.  
- Pagefile config anomaly: registry lists only C:, live system has five pagefiles (C/G/J/L/H).  
- Host commit pressure (~65% RAM) causes API errors; NIM container removed, leaving ~100 GB headroom.  
- GNN retrain AUROC 0.40 capped; OOM at >6000 nodes – GPU‑bound.  
- LoRA corpus below trainingReady gate (823→1000 rows); low distinct‑pair yield from reasoning sweep.  
- Bridge‑reasoning output orphaned; fixed by wiring source and canonical path.  
- Vault‑to‑GNN feeder used stale path – corrected to canonical inventory entry.  
- Anthropic server‑side rate‑limit blocks `/compact`; context critical (~70% full).  
- LoRA trainingReady gate false due missing cam-master-training-set; dormant `wiki-canonical-pairs.jsonl` with schema mismatch.  

**DOMAIN SPECIFICS**  
- Fleet launcher thin wrapper → `.bat`, `slot-tab-boot.ps1`, session‑start auto‑resume logic.  
- Offload routing via `ollama-cost-router.mjs`; `routeModelForTask` handles Claude tiers only.  
- Two runtimes on same GPU: native Ollama & Docker Model Runner; need single primary to avoid VRAM contention.  
- PRISM architecture: cost‑router (`routeModelForTask`), resolveExecutor, lane routing (prism_calc/claude/vllm/ollama), slash‑command wrappers (/checkin‑*, /startup‑*, /goal, /loop, /smart, /forge).  
- Galaxy/soul/wikis/memories/settings propagation across 34 galaxies.  
- AI synergy stack: hybrid RAG, CAG, LoRA, GNN, PSN, vault integration.  
- PRISM galaxy‑reasoning‑bridge (PSN leg #10): hybrid RAG + CAG cache + LoRA emit, Hermes agentic loop.  
- Obsidian vault integration: CLAUDE.md cascade, souls.md synergy, memories & wikis per galaxy.  
- GNN reference pool growth (+8 high‑confidence labels) and LoRA corpus assembly (746→823 rows → 1138 after wiki data).  
- Vault‑to‑LoRA dataset pipeline (`vault-to-lora-dataset.mjs`, `build-fleet-training-corpus-inventory.mjs`, `assemble-fleet-lora-corpus.mjs`).  
- GNN reference pool via `nn-graph-eval.buildHoldout()`; high‑confidence ghost nodes.  
- MCP heap floor management (`ensure-heap-floor.mjs`, `PRISM_MCP_HEAP_FLOOR_MB`).  
- 3‑of‑3 scrutiny gate (`.claude/scripts/scrutiny-3way.mjs`).  
- Awareness backbone files: `CLAUDE-BRIEF.md`, `PRISM-BUILD-CONTEXT.md`, `PRISM-BUILD-VISION.md`.  

**TOOLS USED**  
- Node.js scripts (`recover-today-context.mjs`, `ollama-docker-launcher.mjs`, `session-start-auto-resume.mjs`, `regenerate-launch-fleet.mjs`, `ask-ollama.mjs`, `routeModelForTask`).  
- PowerShell (.bat wrapper, `slot-tab-boot.ps1`, Optimize‑VHD, diskpart).  
- Docker Desktop CLI (pulls, system prune, compose stack).  
- Git for commits and scrutiny.  
- robocopy for large file copy.  
- vitest for unit tests.  
- PRISM dispatchers: U‑FLOR-* units, Hermes local loop (`reasonForGalaxy`).  
- Ollama routing engine with tier preferences.  
- Scripts: `vault-to-lora-dataset.mjs`, `assemble-fleet-lora-corpus.mjs`, bridge‑reasoning sweep, GNN reference pool feeder.  
- Test harnesses for souls.md stamping and bridge source wiring.  
- 3‑of‑3 reviewers for gate validation.  

**OPEN THREADS**  
- Auto-model routing in slash commands pending disk-space resolution.  
- vLLM bring‑up decision remains parked.  
- Docker compose build failure for prism‑server image needs cleanup or `.dockerignore` adjustment.  
- Commit smart.md changes to versioned repository.  
- Remaining units U2 (fail‑loud offload hooks), U4 (dead route‑pretooluse allowlist), U6 (broken model‑router path), U8 (forge7/forge‑audit‑v2 offload), U9–U10 propagation to all galaxies/souls/wikis/settings.  
- Integration of X templates (periodic summarization, Fable‑5 security audit).  
- Decision on pagefile raise (H: 64 GB → 256 GB) and Ollama loaded‑model cap (max 2 models, shorter keep‑alive).  
- Final verification that hybrid RAG is active fleet‑wide and qwen3-coder 30B default coder.  
- GPU training of GNN/LoRA on India slot to reach trainingReady ≥1000 rows.  
- GNN full retrain (GPU lane) to achieve AUROC ≥ 0.55 / Brier ≤ 0.15.
