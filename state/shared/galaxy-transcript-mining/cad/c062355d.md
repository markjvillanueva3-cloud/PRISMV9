# cad session c062355d (2026-05-25, 1.5MB, spine 1KB, 1 slice(s), model gpt-oss:20b)

**DECISIONS**  
- Project‑level `H:/prism/.claude/settings.json` is the active source of truth for hook configuration.  

**OPERATOR DIRECTIVES**  
- “read H:\LAST.MD scope and investigate any issues involving ALL hooks.”  
- “use /system-viz as a master index to scope.”  

**FINDINGS/BUGS**  
- `PreToolUse:Glob` hooks fail with error `/h/prism/.claude/bin/portable-node: No such file or directory`.  
- No hook source files contain the bad path; likely a relative resolution against `H:/prism`.  
- API rate‑limit error encountered during investigation.  

**DOMAIN SPECIFICS**  
- Hooks: `PreToolUse:Glob` (triggered before tool execution).  
- Environment: Git Bash path resolution, missing `portable-node` binary in `/h/prism/.claude/bin/`.  
- Master index: `/system-viz` used to map hook scopes.  

**TOOLS USED**  
- PRISM configuration files (`settings.json`).  
- Hook dispatcher for `PreToolUse:Glob`.  
- System visualization tool `/system-viz`.  
- Git Bash for path resolution.  

**OPEN THREADS**  
- Resolve missing `portable-node` binary or adjust hook paths.  
- Verify that all hooks reference correct, existing binaries.  
- Handle API rate‑limit to complete full scope audit of `H:\LAST.MD`.
