# cad session 0fb9f93e (2026-05-27, 39.2MB, spine 110KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `regenerate-launch-fleet.mjs` regenerated `LAUNCH‑PRISM‑FLEET.bat`: alphabetical tab ordering, size‑guarded resume logic (40 MB guard), absolute paths to `wt.exe`, `pwsh.exe`, `claude.cmd`.  
- Added preflight MCP daemon check (`http://127.0.0.1:3100/health`) before any chat launch.  
- Updated fleet‑reaper script: immediate sweep, kill orphan `node.exe`/`bash.exe` (~140 nodes, 500 bash), report service health; runs as durable scheduled task + in‑session monitor (golf slot owner).  
- Commit **61074fa740** (iter 28) closed all TS errors in `mcp-server/web` → 0 TS errors.  
- Commit **420052f83d** added staleness guard to launch‑fleet script (`MCP_RESUME_MAX_AGE_MIN = 90 min`).  
- 10+ TS‑config fixes dropped > 4000 TypeScript errors across workspace.

**DECISIONS**  
- Slot binding enforced via `slot-bind-enforce.mjs`; zebra/golf exempt from fleet sweeps.  
- Fleet‑reaper ownership moved to golf slot (2026‑05‑16 doctrine); always on unless `PRISM_FLEET_REAPER_DISABLE=1`.  
- Launcher uses absolute paths to avoid PATH inheritance issues in elevated sessions.  
- 40 MB JSONL size guard: large transcripts fall back to `/checkin‑<slot>` instead of `--resume <uuid>`.  
- Staggered quadrant launches (15 s apart) mitigate Anthropic API rate limits.  
- Parallel size‑ and age‑guards (`MCP_RESUME_MAX_AGE_MIN = 90 min`) for resume logic in `regenerate-launch-fleet.mjs`.  
- Relaxed `noImplicitAny` in `H:/prism/src/tsconfig.json` for legacy code.

**OPERATOR DIRECTIVES**  
- Ensure all chats connect to MCP server immediately on launch.  
- Verify fleet reaper running; kill orphan/broken `node.exe`/`bash.exe` processes.  
- Keep launcher tabs named with slot + primary domain, alphabetically NW→NE→SW→SE.  
- Update prism fleet.bat to include most up‑to‑date chats per slot.  
- Continue from where left off.

**FINDINGS/BUGS**  
- `wt.exe` missing from PATH in elevated sessions; fixed by hard‑coding location.  
- `claude.cmd` shim caused “not found” when launcher used `where claude.exe`; resolved.  
- JSONL transcripts > 40 MB crashed `claude --resume`; bypassed with `/checkin‑<slot>`.  
- Race condition on MCP supervisor lock (20 simultaneous chats) solved via preflight daemon check.  
- Anthropic API rate‑limit errors mitigated by launch staggering.  
- Orphan `node.exe`/`bash.exe` processes (~140 nodes, 500 bash) killed during manual sweep.  
- Stale JSONL sessions: echo 129 min, foxtrot 123 min, victor 92 min caused outdated resumes; addressed with staleness guard.  
- Missing optional props in `SummaryTile`, `StatusPill`, `ActionButton` resolved.  
- Interface mismatch `ToolCatalogItem`/`ToolInventoryItem` (price/condition) fixed.  
- Excess literal properties in `StepToolpath` / `StepWcs` removed.

**DOMAIN SPECIFICS**  
- PRISM slot system alpha–zulu; golf owns fleet reaper.  
- MCP server HTTP daemon on :3100 bridged by `mcp-http-bridge.mjs`.  
- Chat‑slot state files: `chat-slots.json`, `state/shared/*.json`.  
- Fleet status file: `H:/FLEET-STATUS.md`.  
- Scheduler tasks: PRISM Fleet Reaper, PRISM MCP Server, PRISM MCP Connectivity Monitor.  
- `mcp-server/web`: React components strict TS mode custom UI primitives.  
- Slot orchestration via `regenerate-launch-fleet.mjs` and `LAUNCH‑PRISM‑FLEET.bat`; mapping uses `chat-slots.json` + JSONL transcripts.  
- Staleness guard inspects `ageMin` from `findSessionUuidForChatId`.

**TOOLS USED**  
- Node scripts: `regenerate-launch-fleet.mjs`, `chat-slots.mjs`, `fleet-reaper-sweep.mjs`.  
- PowerShell helper: `install‑fleet‑reaper‑task.ps1`.  
- Windows batch launchers: `LAUNCH‑PRISM‑FLEET.bat`, `PRISM‑Zebra‑Chat.bat`.  
- TypeScript compiler (`tsc`), npm, git, bash, grep.  
- Docker Desktop, Ollama, NVIDIA NIM containers; scheduled tasks via `schtasks`.  
- npm packages: `@types/node`, `zod`, `express`, `vitest`, etc.  
- React 18/19 useRef migration tooling, Recharts type inference fix.

**OPEN THREADS**  
- Verify MCP daemon remains healthy after future fleet launches.  
- Fine‑tune rate‑limit handling if Anthropic limits change.  
- Confirm fleet reaper cleans orphan processes without false positives.
