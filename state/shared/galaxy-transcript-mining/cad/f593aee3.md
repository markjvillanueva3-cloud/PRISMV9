# cad session f593aee3 (2026-06-10, 37.5MB, spine 351KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `c6748e2a11` – fixed dead‑tag in `cad-ollama-archetype-label.mjs`, added dynamic model picker.  
- `2efed07642` – removed ReferenceError, added `--limit` flag & tunable timeout.  
- `2df2a61ed3` – repaired malformed `CAD-DRAW-MAX-MS1.json`.  
- `8c542f3c92` – flipped 6 “not_started” U‑AI engines to “shipped”.  
- `b6e1bbd3c3` – queue‑sharpen commit (pathspec‑only).  
- `c98911a48f` – session artifacts: `transcript-digest.mjs`, briefing, roadmap, task queue.  
- `21ba9aeb17` – functional dedup of 4 U‑AI engines (`U‑AI‑08,13,06,11`).  
- `7974ffd6ec` – full dedup of 9 U‑AI engines (7 satisfied, 2 new).  
- `db1a8f5a6e` – proven trilobe STEP generation & validation.  
- `a697629fbf / c91fde85d1` – turbine blisk generation + defect discovery; fixed validate(), added parser error; 109/109 tests green.  
- `47277de794` – closed‑loop training convergence on turbine blisk (volume).  
- `bcb6c2f336` – closed‑loop convergence on turbine blisk (volume, 1 secant iteration).  
- `48204b3bd9` – real‑reference characterization & loop tick commit.  
- `407eb7587c` – deep search enforcement & reference characterization.  
- `4a166e0dde` – `U‑CAD‑COMPARE‑UNIT‑NORMALIZE` + closed‑loop replication doctrine.  
- `U-CAD-REGEN-CORRECT` – Stage‑6 CORRECT→CONVERGE engine, 27 tests.  
- `U-CAD-COMPARE-UNIT-NORMALIZE` – unit‑blind comparator fixed (live‑validated).  
- `U-CAD-REGEN-LIVE-PROOF` – live closed‑loop convergence on `blisk.stp`; 0.000 % bbox delta in 3 iterations.  
- `U-CAD-TOPOLOGY-QUANTIFY` – Jaccard ceiling 95 % CARTESIAN_POINT entities.  
- `U-CAD-FIDELITY-E2E-VALIDATE` – Hausdorff: 0.000 % dims, 1.551 % mean surface, 5.087 % worst.  
- `U-CAD-SECOND-REFERENCE-PART` – regression guard for Impeller turbine.stp.  
- `U-CAD-CORPUS-CLASS-COVERAGE` – extractor proven on analytic/mixed/free‑form; inch→mm normalization locked.

**DECISIONS**  
- Use Ultracode orchestrator + Ollama local LLM for grunt tasks; commit artifacts & handoff, avoid auto‑compact.  
- Defer P1 merge until fresh context (19 conflicting files).  
- Replace hard‑coded dead tag with runtime model picker; add `--limit`/timeout flags.  
- Stream transcript extraction (`transcript-digest.mjs`) and mine galaxy transcripts with Ollama.  
- Stop heavy whole‑engine builds in delta slot; dedup U‑AI engines by name → reduce to ~2 new units.  
- Schedule recurring `/loop` every 10 min via `CronCreate`; maintain autonomous progress within memory & throttle limits.  
- Prioritize headless token‑economy work (Ollama offload) over Claude fan‑out when throttled.  
- Use live Fusion bridge `localhost:18361` for NURBS proof; create fresh `/new` doc per build to avoid stale timeline.  
- Adopt strict closed‑loop replication methodology (INGEST→PARAMETERIZE→GENERATE→COMPARE→CORRECT→CONVERGE).  
- Reject blind generation of parts; always ingest & parameterize reference first.  
- Commit unit‑normalization fix as separate high‑priority unit (`U‑CAD‑COMPARE‑UNIT‑NORMALIZE`).  
- Add Stage‑6 controller to close closed‑loop gap; move counter update after `correct()`.  
- Introduce 4 Zod schemas for dispatcher actions (`cad_regen_correct`, `_apply_template`, `_params_from_template`, `_stats`).  
- Implement unit normalization in `CADGeometryComparisonEngine`; replace volume proxy with real bbox volume.  
- Adopt lock‑free git commit pattern to avoid peer‑lock stalls.

**OPERATOR DIRECTIVES**  
- Schedule recurring prompt via `CronCreate`; parse interval and schedule immediately.  
- Activate session‑scoped Stop hook: “complete all delta & CAD tasks…prove 100 % accurate complex part”.  
- Enforce deep search (Glob/master‑graph/obsidian/Ollama) before any absence claim; make fleet‑wide memory.  
- Use Ollama only for qualified offloading tasks (code summarization, content search).  
- Complete closed‑loop training and all roadmap units.  
- Prove generation of 100 % accurate turbine/blisk/engine in resources folder.  
- Run in `/yolo-mode`; block stopping until four conditions hold.

**FINDINGS / BUGS**  
- Dead Ollama tag caused silent no‑op in `cad-ollama-archetype-label.mjs`.  
- ReferenceError at line 138 fixed by using returned model value.  
- Ollama generation down under GPU contention → synthesis‑refresh deferred for 9 galaxies.  
- Merge conflict risk: 19 overlapping files (`settings.json`, `CLAUDE.md`, `cadDispatcher.ts`).  
- U‑AI envelope drift: 6 engines marked “not_started” but actually shipped.  
- Ollama daemon down (:11434 unreachable) → silent fallback to Claude; search stack broken.  
- Anthropic org‑throttle active – Claude fan‑out blocked engine builds.  
- Memory pressure 98 % (219.8/227.1 GB); risk of crash cascade; no zombies to kill.  
- Stale PRISM Blueprint OCR Batch task flagged; needs elevated shell re‑registration.  
- Duplicate U‑AI engines found: CADTransactionEngine, DfMRulesEngine, CADOperationPlannerEngine, CADConsensusEngine.  
- Stale Fusion timeline missing hub; fixed by fresh `/new` doc per build.  
- `CADGeometryComparisonEngine` unit‑blind inch→mm conversion bug → 96 % bbox delta; fixed with conversion‑unit check.  
- Volume metric used bounding-box proxy; reported volume > real cylinder – separate bug (`U‑CAD‑VOLUME‑METRIC`).  
- Initial blind generation produced 30 blades vs 48 in `blisk.stp`; reverse‑engineered file has 48 blades.  
- Headless faceted emitter cannot produce NURBS surfaces; live Fusion kernel can, proving capability blocker broken.  
- Plateau off‑by‑one caused premature max‑delta trigger.  
- Missing Zod schemas for dispatcher actions.  
- Stale per‑file `dist/engines/*.js` caused unit mismatch; resolved by incremental tsc rebuild.  
- Probe errors reading wrong field names masqueraded as engine defects.

**DOMAIN SPECIFICS**  
- Engines: CADFeatureRecognitionEngine, CADAtomicOpsEngine, cadDispatcher, DFMPhysicsGateEngine, CADOpTransactionEngine, HierarchicalTaskPlannerEngine, BliskCADEngine, CADGeometryComparisonEngine, CADReverseTemplateEngine, CADRegenCorrectionEngine.  
- Actions/dispatchers: `cad_regen_correct`, `_apply_template`, `_params_from_template`, `_stats`; feature recognition, chamfer/fillet detection, assembly PoC, live Fusion API server (`CAD‑FEATURE‑RECOGNITION‑MS0`).  
- Metrics: 100 % match closed‑loop print→CAD tests; 75 % gate; axial‑bore accuracy 0–11.1 %; 43 115 entities trilobe STEP; 400 973.6 mm³ turbine blisk volume; 7.40 % volume Δ; bbox delta 0.000 %; Hausdorff dims 0.000 %, mean surface 1.551 %, worst 5.087 %; Jaccard ceiling 95 %.  
- Paths: slot/delta branch, shared tree `cad-fusion-live-ms0`; `H:/prism-slot-delta/`, `H:/prism/`; PRISM repo layout (`mcp-server/`, `scripts/`, `H:/PRISM/resources/CAD FILES/`); live Fusion bridge at `127.0.0.1:18361`; ports 18360 API, 18361 CAM.

**TOOLS USED**  
- Ultracode workflow orchestrator (`wf_*`).  
- Scripts: `transcript-digest.mjs`, `mine-galaxy-transcripts.mjs`, `ask‑ollama.mjs`, `cad-corpus-catalog-build.mjs`, `cad-regen-test.mjs`.  
- Chat‑slot helpers (`chat-slots.mjs`); `/checkin.md` pipeline; `audit-roadmap-drift.mjs`; system‑viz ping.  
- Node/TypeScript: tsc, esbuild, vitest/jest, tsx.  
- Git lock‑free commit pattern.  
- Ollama (qwen2.5‑coder) for summarization/offloading; Claude fallback.  
- Fusion API bridge (`PRISMBridge.py`); MCP server (`mcp-server/dist/`).  
- Zod schemas for dispatcher actions.

**OPEN THREADS**  
- P1 merge of slot/delta into trunk (needs fresh context).  
- P6 CAD‑FEATURE‑RECOGNITION‑MS0 build & closed‑loop training.  
- Build & test `U‑BLISK‑6SERIES‑PARSE` per‑file scrutiny gate; currently throttled.  
- NURBS emitter for blisk (P7) or live Fusion bridge to convert feature ops → STEP for 100 % accurate comparison.  
- Provide real turbine/blisk reference STEP in resources folder and use it.  
- Restart Ollama daemon, make silent fallback loud.  
- Resolve memory pressure: run `/compact`, close idle Claude chats.  
- Implement `CADRegenCorrectionEngine` (CORRECT→CONVERGE stage).  
- Add blade‑root fillets to replica to push Jaccard > 0.80.  
- Replace bounding‑box volume with true solid volume metric (`U‑CAD‑VOLUME‑METRIC`).  
- Full‑blade live run for turbine class; finalize 48‑blade blisk generation.  
- Validate unit normalization across all STEP files.
