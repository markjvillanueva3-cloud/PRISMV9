# cad session 5852a0b9 (2026-05-20, 43MB, spine 187KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- TOKEN‑AWARENESS‑MS0: 12 units (sidecar writer `token-awareness-sidecar.mjs`, statusline update, precompact‑auto‑trigger, dispatcher actions, docs).  
- ZEBRA‑MS0: U‑ZEBRA01 HWND resolver; U‑ZEBRA02 main loop; U‑ZEBRA03 scheduled‑task installer; U‑ZEBRA04 drift detection; U‑ZEBRA05 backend‑dev priority payload; U‑ZEBRA06 advisory hook; U‑ZEBRA07 doc reflection.  
- HERMES‑MS0: U‑HERMES02 slot souls + inject hook; U‑HERMES03 skill‑candidate detection lib & hook.  
- ZEBRA‑AWARENESS‑MS0 pipeline, CLI, tests, wiki entry, memory file committed.  
- 17 new files (HERMES‑MS0/1/EXT, ZEBRA‑AWARENESS‑MS0) – 102 tests passing.  
- Branch `cad-fusion-live-ms0` +459 commits ahead, –1 behind origin.

**DECISIONS**  
- Adopt sidecar‑first token read; drop precompact suppression logic.  
- Set `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=80%`; auto‑compact fires at ~800k tokens before hard block (~1M).  
- Use PowerShell SendKeys (`send‑keys-to-window.ps1` + `send‑keys.mjs`) for `/compact` and `/checkin`; hooks cannot invoke slash commands.  
- Account cycling via external login script `PRISM_ZEBRA_LOGIN_SCRIPT`, no repo secrets.  
- 60 s TTL sidecar token budget (`state/shared/token-budget‑<slot>.json`).  
- Zone thresholds: GREEN <60%, YELLOW 60–85%, RED 85–95%, CRITICAL ≥95%.  
- Zebra orchestrator monitors sidecar, invokes `/compact` via SendKeys, then `/checkin‑<slot>`, prioritizes backend‑dev tasks.  
- Closed learning loop integrated into zebra.  
- Slot claim: `bravo` taken by `claude-72b4eba8`; prior owner evicted.  
- Memory usage RSS 2.86 GB > 2.00 GB → recommend `/compact` before next work order.  
- Orphan Process Reaper task disabled; re‑register via PowerShell.

**OPERATOR DIRECTIVES**  
- Run `/precompact` then `/compact` to clear hard block and allow ZEBRA units commit.  
- After compaction, `git add .claude/hooks/*` & `git commit -o <files>`.  
- Verify statusline shows zone badge (line 1) and alert text (line 2).  
- `/checkin-bravo` – slot‑locked /checkin.  
- Use Playwright to assess token‑usage awareness for compaction strategy.  
- Build comprehensive version covering all gaps, plan with Codex 5.5 if possible.  
- Synergize zebra with prism‑awareness, obsidian brain, wiki, tribal knowledge, neural network, skills, scripts, hooks, memories, system‑viz.  
- `/goal [complete remaining tasks and next batch | completed and wired]`.  
- `/loop [5m] /goal` – autonomous loop for ZEBRA‑AWARENESS‑MS1 + HERMES‑MS2 wiring.  
- Build shared consumer lib first, then wire each consumer.  
- Ensure terminal UI MP bar shows live task‑queue countdown.

**FINDINGS/BUGS**  
- Precompact auto‑trigger suppressed by over‑counting bytes; fixed via sidecar-first read order.  
- Statusline TDZ bug referencing constants before declaration; corrected.  
- Zone state machine now applies 60 s TTL and never downgrades on stale data.  
- Auto‑compact originally fired at ~950k tokens; now at ~800k after override.  
- Precompact hook timed out under load; resolved by prioritizing sidecar source.  
- Lane‑guard active blocking U‑ZEBRA03/04/07 commits; requires `PRISM_GIT_ADD_LANE_DISABLE=1` or slot‑worktree session.  
- Auto‑compact not firing due to suspect token count; hard block at 1 M tokens.  
- Chat‑bus warning: Orphan Process Reaper disabled; must re‑enable.  
- Fleet-memory-monitor warning: RSS 2.86 GB > threshold.

**DOMAIN SPECIFICS**  
- Token budgeting sidecar JSON (`state/shared/token-budget‑<slot>.json`) with `ctx.tokens`, `pct`, `zone`, `capturedAt`.  
- Context window pressure engine, compact boundaries via `"isCompactSummary":true`.  
- Slot management: `chat-slots.json` holds terminalWindowId (PID & HWND), opt‑in flags.  
- Orchestrator stack: CHO01 decision module, CHO02 token watch, CHO04 SendKeys actuator; U‑ZEBRA04 drift detection; U‑ZEBRA05 backend‑dev priority payload.  
- Skill-loop pipeline (`skill-loop-pipeline.mjs`) for closed learning loop.  
- Zebra-awareness pipeline (`zebra-awareness-run.mjs`) indexing 11 surfaces, training ranker.  
- Statusline rendering (`statusline.mjs`) with zone tags, quota tags, HP bar.  
- Precompact-auto-trigger logic (`precompact-auto-trigger.mjs`) reads sidecar to decide `/compact`.  
- Token‑awareness state machine (`token-awareness-sidecar.mjs`).  
- Skill‑candidate detection lib (`skill-candidate-observe.mjs`).  
- Resolve-hwnd (`resolve-hwnd.mjs`) for HWND resolver.  
- Drift detection lib (`scripts/lib/zebra-drift-detect.{mjs,test.mjs}`).  
- Backend‑dev priority builder (`scripts/lib/zebra-bd-priority.{mjs,test.mjs}`).  
- Doc reflection wiki entry, CLAUDE.md patch-sibling.  
- Memory usage thresholds: RSS 2.86 GB > 2.00 GB.

**TOOLS USED**  
- Playwright (Reddit research).  
- Claude Code statusLine stdin JSON.  
- `precompact-auto-trigger.mjs`, `token-awareness-sidecar.mjs`.  
- `statusline.mjs`.  
- SendKeys PowerShell script (`send‑keys-to-window.ps1` + `send‑keys.mjs`).  
- Scheduled-task installer (`install-zebra-orchestrator-task.ps1`).  
- Drift detection lib (`scripts/lib/zebra-drift-detect.{mjs,test.mjs}`).  
- Backend-dev priority builder (`scripts/lib/zebra-bd-priority.{mjs,test.mjs}`).  
- Advisory hook (`.claude/hooks/zebra-advisory-inject.mjs`).  
- Doc reflection wiki entry, CLAUDE.md patch-sibling.  
- Hooks: `token-awareness-inject.mjs`, `statusline.mjs`, `precompact-auto-trigger.mjs`, `zebra-advisory-inject.mjs`, `skill-candidate-observe.mjs`.  
- Scripts lib: `resolve-hwnd.mjs`, `skill-loop-pipeline.mjs`, `zebra-awareness-run.mjs`.  
- MCP dispatcher (`registerContextDispatcher` in `prism_context`).  
- Settings.json auto‑mirror C→H for hook wiring.  
- Git commit workflow with lane‑guard; requires `PRISM_GIT_ADD_LANE_DISABLE=1`.  
- Node test framework for shared consumer lib tests.

**OPEN THREADS**  
- Final commit of U‑ZEBRA03/04/07 after clearing `.git/index.lock`.  
- Implement U‑ZEBRA08 account‑cycling logic (credential handling, external login script).  
- Integrate Hermes plan into zebra orchestrator.  
- Validate auto compaction and precompact flows across all slots; ensure no false positives.  
- Confirm statusline zone badge displays correctly for every slot state.  
- Test drift detection accuracy and orchestrator scheduling.  
- Commit remaining U‑ZEBRA03/04/07 files (resolve lane‑guard).  
- Ship U‑ZEBRA02 main loop (wire decision, sensor, actuator).  
- Install zebra orchestrator scheduled task (U‑ZEBRA03).  
- Finish drift detection (U‑ZEBRA04) and backend‑dev priority payload (U‑ZEBRA05).  
- Finalize U‑HERMES01 adoption matrix decisions.  
- Design & implement U‑ZEBRA08 account‑cycle credential mechanism.  
- Configure U‑HERMES08 multi‑surface messaging config.  
- Run `/compact` to clear hard block and allow session to resume with full budget.  
- Push 459 commits to origin (operator decision).  
- Re-enable orphan process reaper task via PowerShell.  
- Execute `/compact` before next session due to memory usage.  
- Finish remaining ZEBRA‑AWARENESS‑MS1 and HERMES‑MS2 wiring tasks.
