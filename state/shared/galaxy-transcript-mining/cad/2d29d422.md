# cad session 2d29d422 (2026-05-26, 55.1MB, spine 147KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5b370300f0` – iter9: clean customer extraction, extended NON_CUSTOMER_SUBDIRS regex, 14/14 tests.  
- `acee69cad3` – iter10: JSONL drift‑ledger builder (`train-cycle-history.jsonl`), import‑safe guard, 13/13 tests (hotel‑slot absorption logged).  
- `bd3ad1ffc7` – iter11: rolling‑window summarizer with percentile fix, 21/21 tests.  
- `b1c6a096ff` – iter12: 3‑tier alert classifier (`detectDriftAlert`), 21/21 tests.  
- `71e08eae58` – iter13: per‑record signal variance injection via `deriveRecordDefaults`.  
- `88f6f975ae` – iter14: wiki entry for pipeline discoverability.  
- `4f00ed1473` – iter15: latest‑drift‑alert.json state file.  
- `15b09088ad` – iter16: bootstrap distribution probe (`--summary`).  
- `3de92ef087` – iter17: round‑trip E2E composition test; fixed `cov_gate_fail_rate`.  
- `3820f1ed4f` – iter18: Docustrata bridge shim (`mergeDocustrataRevenue`) with `revenue_source` flag.  
- `2d4e2cfa3e` – iter19: Docustrata payload validator (`validateDocustrataPayload`).  
- `d9f727aa06` – iter20: synthetic revenue generator (`generateSyntheticRevenueRecords`).  
- `cb52c38aee` – iter21: pipeline orchestrator CLI (`runDocustrataPipeline`).  
- `84b5ed57a9` – iter29: extractor spec.  
- `7bc1c940e3` – iter26: Windows Scheduled Task installer (`install‑quoting‑pipeline‑cron.ps1`).  
- `0158f14138` – iter27: sample fixture & contract tests.  
- `d74521aa4c` – iter28: SessionStart alert banner formatter; fixed Math.round boundary bug.  
- `211ab8e1f3` – iter32: verify‑script bug fix (cron‑install test discovery).  
- `67e1b53aa4` – iter33: live baseline staleness finding; regenerated baseline (`baseline-records.json`).  
- `8235c3c725` – iter34: baseline regeneration & new findings.  
- `848e0107ab` – iter35: regex extension for F1 noise patterns.  
- `eafec0ccb9` – iter36–39: JM Die layout audit and filter fixes (`{MACHINE}/{CUSTOMER}` inversion).  
- `491ed8602c` – iter37: machine‑customer filter fixes.  
- `a38d790324` – iter38: real‑customer chain evidence.  
- `4f6a1c92fc` – iter39: balanced sampling, 3‑way variance.  
- `ae75d99e9b` – iter40: numbered‑prefix filter fix.

**DECISIONS**  
- Adopt slot‑binding wrapper for `/checkin-charlie`; enforce exclusive ownership.  
- Use Chain‑of‑Verification as pure substrate; all domain engines wrap it.  
- Persist calibration drift via append‑only JSONL ledger → audit trail.  
- Separate summarizer and alert detector to expose single actionable flag.  
- Guard scripts with `if (require.main===module)`; enforce empty‑index pre‑commit check.  
- Closed‑loop pipeline: bootstrap → train cycle → ledger → summarizer → alert → state file.  
- Variance injection via `deriveRecordDefaults` to avoid degenerate training data.  
- Docustrata revenue overlay (`mergeDocustrataRevenue`) with mode detection flag.  
- Formal contract lock on payload shape (`validateDocustrataPayload`).  
- Synthetic generator (`generateSyntheticRevenueRecords`) for bootstrap before real extractor.  
- Single‑command orchestrator (`runDocustrataPipeline`) for nightly runs.  
- Cron‑install via PowerShell script with explicit command discovery.  
- Session memory auto‑feed into Obsidian for cross‑session recall.  
- Health check (`quoting-pipeline-verify.mjs`) aggregates all unit tests and discovers hidden failures.  
- Full smoke test (`full-chain smoke`) to catch integration regressions.

**OPERATOR DIRECTIVES**  
- “Continue all remaining quoting units and keep training the system. Operator is asleep — yolo mode, no questions, build the next unit, commit, tick, continue.” (repeated each cron fire).

**FINDINGS/BUGS**  
1. Shared‑tree lock absorption of 4 hotel files (iter10).  
2. NIST percentile bug fixed by test (iter11).  
3. `cov_gate_fail_rate` assumption wrong; corrected in iter17.  
4. Math.round‑before‑compare boundary bug in alert banner (iter28).  
5. Cron‑install tests silently excluded from verify; regex fixed (iter32).  
6. Baseline-records.json pre‑iter13 stale; regenerated baseline needed (iter33,34).  
7. Regex anchored too strictly caused noise patterns to leak (F1) – fixed in iter35.  
8. JM Die archive layout inverted (`{MACHINE}/{CUSTOMER}`); audit and filter fixes (iter36–39).  
9. Numbered‑prefix filter closed remaining R12 finding (iter40).

**DOMAIN SPECIFICS**  
- **Engines/Actions**: `QuotingTrainingOrchestratorEngine`, `TrainCycleLedgerBuilder`, `SummarizeLedger`, `DetectDriftAlert`, `bootstrap`, `trainCycle`, `buildLedgerRow`, `mergeDocustrataRevenue`, `validateDocustrataPayload`, `generateSyntheticRevenueRecords`, `runDocustrataPipeline`.  
- **Dispatchers/Hooks**: PowerShell cron installer, session‑memory stop hook, wiki auto‑feed.  
- **Metrics/Paths**: MAPE, CoV gate fail rate, safe_to_activate, psi‑fed total; files: `state/shared/quoting/active-calibration.json`, `train-cycle-history.jsonl`, `latest-drift-alert.json`, `baseline-records.json`.  
- **Unique Galaxy Paths**: `state/shared/quoting/*`, `scripts/quoting-*.*mjs`, `install‑quoting-pipeline-cron.ps1`.

**TOOLS USED**  
- Node.js scripts (`*.mjs`) for bootstrap, ledger, summarizer, alert.  
- Vitest/Jest test harnesses (`*.test.mjs`).  
- PowerShell installer script.  
- Obsidian memory hook (`feedback_auto_memory_feeds_obsidian_stophook`).  
- Git hooks, tsc, CronCreate.

**OPEN THREADS**  
- Docustrata extractor implementation (iter42): adapter to read curated JSON sidecar and feed into orchestrator; pending full integration with real extraction engine.  
- Dispatcher wire: peer‑contended file area coordination before final wiring.  
- Final validation against live JM Die archive once extractor wired; run end‑to‑end on production data to confirm variance and pricing spread targets.
