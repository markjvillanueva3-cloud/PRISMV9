# cad session 14c8b81e (2026-06-09, 5.4MB, spine 74KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `316872f7`: added `scripts/call‑engine.mjs` (server‑free engine invoker) with Windows/Bash wrappers and an 8‑case node:test regression suite (`call-engine.test.mjs`).  
- Commit `38af3407`: added WSL memory guard (`27-wsl-memory-guard.mjs`) with 15‑case unit tests, a PowerShell installer (`install-wsl-memory-guard-task.ps1`), and updated documentation.  

**DECISIONS**  
- Build a thin CLI that imports `src/engines/*.ts` directly (no MCP server needed) to replicate dispatcher behavior; use tsx for ESM import.  
- Use one‑shot `[BOOTSTRAP-SLOT-ENFORCE]` prefix for cross‑cutting tooling commits on the shared tree.  
- Omit a MEMORY.md index pointer because the 22 kB ceiling would be exceeded; rely on per‑memory file auto‑feed to Obsidian.  
- Guard WSL memory cap by reading `.wslconfig` and `vmmemWSL`; advise mode only, with optional `--enforce` that runs `wsl --shutdown`.  

**OPERATOR DIRECTIVES**  
- “commit” – performed for both toolsets.  
- “fix whatever is constantly causing the api limit error” – identified as WSL memory over‑commit; removed zombie tsservers and unloaded idle Ollama models, reducing pressure from 98.4 % to 68.5 %.  

**FINDINGS/BUGS**  
- MCP server not required for code navigation or engine invocation; all dispatchers are thin Zod validators that call plain TS singletons.  
- WSL memory cap (`memory=16GB` in `.wslconfig`) was never enforced (vmmemWSL committed ~96 GB).  
- Three zombie tsservers leaked ~3 GB; killing them reduced pressure to 79.1 %.  
- Three concurrent Ollama models (~65 GB) were idle and could be unloaded, dropping pressure to 68.5 %.  
- Stop‑hook “MCP Server=failing” warning was a false positive (task scheduler status, not live endpoint).  

**DOMAIN SPECIFICS**  
- Dispatchers: ~107 thin routers (`prism_calc`, `prism_cam`, `prism_safety`, `prism_session`, `prism_memory`, etc.).  
- Engines: plain TS singletons in `src/engines/*.ts` (e.g., `prismSelfAwarenessEngine`).  
- Actions tested: `validateKienzle`, `findCapabilities`, `--list`, `--methods`, malformed JSON handling, semantic memory search.  
- Metrics tracked: commit pressure, vmmemWSL usage, llama‑server resident memory, tsserver orphan count.  

**TOOLS USED**  
- `tsx` for direct TS import of engines.  
- Node.js (`node:test`) for regression suite.  
- PowerShell scripts: `02-kill-zombie-tsservers.ps1`, `install-wsl-memory-guard-task.ps1`.  
- System‑health utilities: `03-commit-pressure-check.mjs`.  
- Bash wrappers (`call-engine.sh`), Windows CMD wrapper (`call-engine.cmd`).  

**OPEN THREADS**  
- Run `wsl --shutdown` to reclaim ~80 GB immediately (requires user terminal).  
- Install the WSL guard scheduled task via `install-wsl-memory-guard-task.ps1 -RunNow`.  
- Optionally wire the guard into the fleet‑task‑health list for Stop‑hook monitoring.  
- Integrate `call-engine.mjs` as a `/call-engine` skill or Prism session action for discoverability.  
- Execute `/compact` to finalize the session and apply the handoff.
