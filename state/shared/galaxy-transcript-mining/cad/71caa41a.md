# cad session 71caa41a (2026-05-23, 58.2MB, spine 178KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-ZM1‑01–05` – full MS0/MS1 build: slot‑binding, rename‑window intercept, sweep logic, PRISM <slot> title convention, single‑HWND enumeration, tabbed‑fleet diagnostic (commits e78444ba53, 18fa048414, b2d80e3921, ea8ad48afe, 1480ae6b95).  
- `U-ZM2‑01` – UIA tab‑focus actuator: multi‑WT support, pane‑safety checks, env var `PRISM_SENDKEYS_CONFIRM` override (commit 108e27b32d).  
- `U-ZM1‑05.1` – scope refinement after UIA probe (commit 9df86169d0).  
- `U-ZM2‑01.close‑out` – empirical proof of end‑to‑end actuation for 6 live slots in dry‑run mode (commit b9e65d78d2).  
- `U-ZPSN02` – slot‑souls for all 27 slots; PSN‑aware `[psn:…]` tags emitted in every SendKeys payload.  
- Spec `ZEBRA‑ACCOUNT‑CYCLE‑MS0` (commit 83876baae4) pending implementation.  
- Three new geometry files written during U‑ZPSN02 close‑out, pending test/wire/commit.

**DECISIONS**  
- Adopt UIA root walker over Get‑Process for HWND resolution; resolve all tabs via root element.  
- Enforce stable window titles `PRISM <slot>` to avoid title‑missing bugs.  
- Use env var `PRISM_SENDKEYS_CONFIRM=1` to bypass PowerShell `$false` coercion bug.  
- Ship PSN tags only in SendKeys payload; target‑side parsing deferred to U‑ZPSN03.  
- Keep account‑rotation logic separate until concrete implementation agreed (spec only).  
- Introduce `PRISM_BOOT_SLOT` env var for session‑start resume, eliminating silent failure point.  
- Replace hard‑coded `SLOT_NAMES` with import from `chat-slots.mjs`.  
- Adopt stable caption convention `"PRISM <slot>"` in fleet launcher; UIA can resolve tabs across multiple WT windows.  
- Switch to single enumeration of WT windows per sweep (instead of 13 PowerShell spawns).  
- Implement UIA tab‑focus actuator with pane‑count safety checks; env var override `PRISM_SENDKEYS_CONFIRM`.  
- Zebra scheduler switched to non‑elevated installer path that registers S4U task.

**OPERATOR DIRECTIVES**  
- `/goal [ complete all zebra units, fully autonomous building operation, and assess Hermes agent capability ] /loop [5m]`

**FINDINGS/BUGS**  
- Restart‑resume gap fixed by `PRISM_BOOT_SLOT`.  
- Manual Zebra scheduler registration resolved with non‑elevated installer path.  
- HWND resolution failure due to WT tabs fixed by multi‑window enumeration and stable caption convention.  
- Dead chat slots cleaned by reaper and opt‑in logic; stale entries removed from `chat-slots.json`.  
- PowerShell argument coercion bug (`-Confirm:$false`) fixed via env‑var override.  
- UIA tab‑select failures resolved by enumerating all WT windows and verifying single pane per tab.  
- `Get‑Process WindowsTerminal.MainWindowHandle` returned only one WT window; solved by UIA root walker.  
- Missing parser for `[psn:…]` tags; U‑ZPSN03 pending.  
- Exit 255 on E2E run due to stdin CRLF handling in PowerShell; resolved by file‑redirect canonical stdin path.  
- API rate limiting error (“Server is temporarily limiting requests”).  
- Size guard tripped; required env‑knob workaround.

**DOMAIN SPECIFICS**  
- Engines/Actions: `per-agent-handoff.mjs`, `psk.mjs` composite, `checkin.md` report, `slot-tab-boot.ps1`, `zebra-orchestrator-sweep.mjs`, `send-keys-to-window.ps1`, `rename-window-intercept.mjs`, `wt-tab-focus.mjs`.  
- Dispatchers: `chat-slots.mjs` (claim/slot‑state), `session-start-auto-resume.mjs` (SessionStart hook), Zebra scheduler task.  
- Metrics/Paths: decision logs (`zebra-orchestrator-log.jsonl`), dry‑run grace period 24 h per slot, `PRISM_HANDOFFS_DIR`, `state/shared/zebra-opt-in.json`.  
- Hermes agent orchestrator drives chat‑fleet via SendKeys.  
- PSN self‑awareness engine supplies domain/role/queue/tribal fingerprints.  
- Slot‑souls encode personality and domain for each NATO slot.  
- UIA tab resolution required due to single WT window with multiple tabs/panes.  
- 5‑hour session limits on Claude Max plans drive account rotation logic.

**TOOLS USED**  
- PRISM scripts: `zebra-orchestrator-sweep.mjs`, `rename-window-intercept.mjs`, `wt-tab-focus.mjs`, `scripts/lib/zebra-bd-priority.mjs`.  
- Git pathspec commits (`git commit -m "msg" -- <files>`).  
- Node test harness (`node --check`, `node scripts/...`).  
- UI Automation (UIA) via PowerShell and Win32 APIs.  
- `/startup` pipeline, `/startup-bravo` for args forwarding.  
- Node script for env‑knob escape hatch.  
- `c-to-h-mirror` for live settings.json wiring.  
- `MEMORY.md` pointer update (21573 B).  
- Git cherry‑pick in progress; `checkin-bravo`.

**OPEN THREADS**  
- Full actuation for all 25 slots – proven for 6 live slots; remaining need UIA tab resolution after cleaning dead processes and ensuring each WT window contains correct tabs.  
- Operator‑side topology reconciliation – confirm launcher consistently creates third WT windows or adjust tab‑focus logic accordingly.  
- Post‑dry‑run execution path – verify that after the 24 h grace period, Zebra sends `/precompact`, `/compact`, and `/checkin-<slot>` into each chat without side effects.  
- Documentation & memory updates – finalize wiki sections for `zebra-orchestrator.md` (MS2 scope) and update `MEMORY.md`.  
- U‑ZPSN03 – UserPromptSubmit hook to parse `[psn:…]` tags into structured context.  
- U‑ZM2‑02 – Pane‑focus logic for multi‑pane tabs (currently safe‑refuse).  
- U‑ZM2‑03 – Execute‑mode smoke after 24 h grace expires (~2026‑05‑23 20:06).  
- U‑ZM2‑04 – PID‑liveness gate in `pickActionableSlots`.  
- Account‑rotation implementation – build scripts, credential capture, swap logic.  
- Hermes capability assessment – final validation that parser hook yields closed‑loop agent behavior.  
- Test, wire, and commit the three geometry files from U‑ZPSN02 close‑out.  
- Resolve API rate limiting before next E2E run.  
- Finalize cherry‑pick integration before adding to main branch.  
- Confirm E2E smoke passes with corrected stdin handling.
