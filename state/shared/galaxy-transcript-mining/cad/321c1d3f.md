# cad session 321c1d3f (2026-06-03, 37.4MB, spine 386KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑PT‑KNOWLEDGE‑ENRICH` – added `--from‑knowledge`; emits tribal/playbook/Okuma tips, SEQ‑001 & SEQ‑003; 68/68 tests green.  
- `U‑PT‑HAAS‑SPEC` – ground‑truth spec for full Haas master‑post engine (mirrors Hurco template).  
- `U‑PT‑HAAS‑ENGINE` – `HaasNGCMillMasterPostEngine`; fixed 25.4× inch scaling, feed guard, arc safety; 27/27 tests green.  
- `U‑WINMAX‑LIVE‑PROBE‑FINDING` – live WinMax Mill sim probe captured button set and verified softkey‑tiebreak logic.  
- `U‑WINMAX‑BUTTON‑SIGNATURE` (commit a1c1efa31d) – added distinctive‑button extraction to `signatureOf`; recorded `DRAW_VERIFY` screen with real 26‑button set; 31/38 tests green.  
- `U‑WINMAX‑NAV‑PATH‑CONTRACT` (commit dd1fcdb48c) – unified CLI contract (`pathResult()` envelope), single‑line JSON for `whereami`/`navigate`; 68/68 + 30/30 tests green.  
- `U‑PT‑HAAS‑CANNED‑CYCLES` (commit a1acfda90b) – canned‑cycle emission (G81/G83); 45/45 tests + 3/3 corpus.  
- `U‑PT‑HAAS‑CYCLE‑BYTE‑MATCH` (commit 8601451b) – bare cycle line, G99 default, byte‑match guard; 47/47 tests + 3/3 corpus.  
- `U‑CIMCO‑WIKI‑TRIBAL` – CIMCO verification/simulation wiki + 12 tribal tips.  
- `U‑PATCHES‑ALLOWLIST` – fleet‑wide leave‑a‑copy fix for patch‑consumption workflow.  
- `U‑CIMCO‑CONTROL‑MAP` (`cimco-control-map.mjs`) – command catalog, `.mcfg` reader, Simulation‑Report parser; 19/19 tests.  
- `U‑CIMCO‑MACHINE‑INDEX` – indexed 86 machine defs → `state/shared/cimco/machine-index.json`; 44 vendor defs flagged **units‑UNRESOLVED**.  
- `U‑CIMCO‑POST‑INDEX` – indexed 25 `.js` + 44 `.eRPost` posts → `post-index.json`; 5/5 tests.  
- `SPINE‑1 bridge engine` (`CimcoVerificationBridgeEngine.ts`) – dispatcher, 22/22 tests.  
- `NC normalizer core` (`scripts/lib/nc-normalize.mjs`) – full test suite; 18/18 pass.  
- `JM↔CIMCO machine map generator` (`scripts/cimco-jm-machine-map.mjs → jm‑fleet‑sim‑map.json`).  
- `U‑CIMCO‑TOOL‑INDEX` – 366 cutters, 14 libs.  
- `U‑CIMCO‑PER‑GALAXY‑PLOTS`.  
- `297a3c9194` – Post‑proof readiness ledger (7/7 tests); 15 machines, 9 143 golden programs, 246 content‑drift groups.  
- `430f735fff` – Workflow P0 fix + Hurco `.hnc` coverage + fleet roadmap report.  
- `d0e5df9e16` (`scripts/lib/nc-dialect-masks.mjs`) – `roundTrip` classifier; 10/10 tests.  
- `38da8cb412` – Drift audit refactor to use dialect‑aware `roundTrip`; reclassifies 247 semantic‑drift, 8 byte‑identical, 2 volatile‑header‑only pairs.  
- `7a14bf0def` – Fixed nc‑dialect mask fail‑open bug (greedy patterns, unknown dialect handling).  

**DECISIONS**  
- Use **echo slot** for post‑processor work; force‑take to avoid conflicts.  
- Build order: 1) wiki+tribal knowledge, 2) Haas full post, 3) live navigation.  
- For safety‑critical engines, enumerate fully before any write; never half‑build a 2000‑line post.  
- Additive button tiebreak (`signatureOf.buttons`) keeps softkey and UIA dimensions orthogonal.  
- CLI outputs must be single‑line `{ok,…}` envelopes to satisfy consumers.  
- Use MCP dispatchers (`prism_calc`, `prism_safety`, etc.) with `duplicationGuardEngine.mustCheckBeforeCreating()` to avoid duplicate assets.  
- Enforce 2‑parallel reviewer per file + 3‑of‑3 gate (`scrutiny-3way.mjs`).  
- Reject speculative dialect for G86/G89/G76; only implement features grounded in the golden archive.  
- Remove `M29` before `G84`; it is Fanuc‑only and would hang Haas control.  
- Adopt bare cycle line (no X/Y) with default `G99` per JM golden archive; drop `G98`.  
- Add guards for first‑hole coupling and NaN in approach block.  
- YOLO mode active: no questions, auto‑select highest priority, immediate execution, write directly, no implicit unit caps.  

**OPERATOR DIRECTIVES**  
- `/checkin‑echo /goal …` – start goal work, force‑take echo slot.  
- `/loop [5m] continue…` – schedule recurring YOLO loop every 10 min (default).  
- `/yolo-mode` – activate autonomous build mode; no questions, auto‑select highest priority task.  
- “im launching mill hurco for you” – operator brought WinMax sim live to unblock condition 1.  
- Provide live‑app capture session for UIA driver (SPINE‑2).  
- Confirm headless automation + 2026 REST API subscription tier.  
- Supply MariaDB credentials for juliett/hotel if required.  

**FINDINGS/BUGS**  
- Softkey‑tiebreak needed: numeric softkeys polluted signature; fixed by excluding pure‑numeric IDs.  
- `DRAW_VERIFY` screen missing → added to map with real button set.  
- Double‑broken CLI contract (`path` emits array, consumer expects `{ok,…}`); fixed by `pathResult()` and single‑line JSON.  
- Haas engine had 25.4× inch scaling bug, non‑finite feed, arc safety; all corrected.  
- No canned‑cycle emission in Haas engine; design pending (now implemented).  
- P0: VMC‑03 mis‑mapped to 5‑axis trunnion (`Haas VF‑2TR`). Fixed by broadening `axisHints` regex and adding 3↔5‑axis regression lock test.  
- nc‑dialect mask fail‑open: greedy `[^)]*` patterns swallowed G‑code; unknown dialect masked everything – fixed.  
- Provenance bug in PRISM_UPGRADED header (machineModel vs source path).  
- 44 of 86 machine defs lack `Header.Unit`/`Orientation`; flagged for safety.  
- Server rate limiting blocks parallel agent dispatch (scrutiny, per‑galaxy workflow).  

**DOMAIN SPECIFICS**  
- **Engines/Dispatchers**: `HurcoV11MillMasterPostEngine`, `OkumaMillMasterPostEngine`, `HaasNGCMillMasterPostEngine`; router `master_post_by_machine`.  
- **Actions**: `generateProgram`, `emitToolpath`, `emitCannedCycle` (planned), `matchScreen`, `signatureOf`.  
- **Metrics/validation**: dialect‑lint errors, structural conformance, playbook sequencing rules (SEQ‑001, SEQ‑003).  
- **Paths**: `winmax-ui-map.json`, `winmax-course-run.mjs`, CLI verbs (`path`, `whereami`, `navigate`).  
- **CIMCO file formats**: `.mcfg` (JSON), `.js` (posts), `.eRPost` (binary controller defs), `.tmlib` (XML tool libs).  
- **Simulation report parsing**: pass/fail gate logic, UIA fallback mandatory.  
- **Machine‑model mapping criteria**: orientation, axis count, native vs generic templates.  
- **Post‑processor verification**: NC normalizer + simulation comparison.  

**TOOLS USED**  
- RTK (`rtk git`, `rtk vitest run`, `rtk tsc`, `rtk npm run build`).  
- Ollama offload (`/ollama-*`, qwen2.5-coder:7b).  
- System‑viz queries (`system-viz-query.mjs`).  
- Obsidian 2nd‑brain queries (`wiki-query`, `prism_memory:semantic_search`, `prism_knowledge:tribal_search`).  
- Master‑index awareness (`prism_session:master_index_query`).  
- Duplication guard (`duplicationGuardEngine.mustCheckBeforeCreating()`).  
- Scrutiny gate (`scrutiny-3way.mjs`).  
- Loop state (`loop-state.mjs`) and CronCreate for recurring `/loop`.  

**OPEN THREADS**  
1. **U‑PT‑HAAS‑CANNED‑CYCLES** – final integration of canned‑cycle emission (G81/G83) in Haas engine; pending enumeration and full end‑to‑end test.  
2. Bring WinMax sim back online to finish remaining condition 1 items (softkey‑tiebreak auto‑resolve, lathe field‑id probe).  
3. Verify full end‑to‑end navigation with updated `DRAW_VERIFY` screen and button tiebreak in live environment.  
4. SPINE‑2 UIA driver + sim cycle‑time + DNC/MDC integration (requires live app).  
5. Full CIMCO file‑compare byte‑equivalence post‑emit gate (next unit to build).  
6. Authoring `.mcfg` for Okuma/Hurco/Roku machines (generic templates currently used).  
7. Validate that all 246 drift groups are correctly classified post‑masking.  
8. Per‑galaxy plot workflow resume when server rate limits lift.  
9. Confirm MariaDB credentials and license tier for juliett/hotel integration.
