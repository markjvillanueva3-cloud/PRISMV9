# cad session 5815c28b (2026-05-26, 23.8MB, spine 60KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `slot/delta` branch now contains **≈105 k real CAD files** (FreeCAD, KiCad, NopSCADlib, ABC shards).  
- 5 AI systems trained on full corpus: k‑NN classifier, PageRank‑GNN, TF‑IDF RAG, LoRA adapter, Deep‑Reasoning ensemble.  
- **115 wiki entries + 115 tribal tips** covering 11 archetypes and 104 atomic ops (38 base + 66 extended).  
- **418 PSN‑routable CAD‑action nodes** (38 ops × 11 platforms).  
- **12‑stage perpetual loop driver** (`cad-perpetual-loop-driver.mjs`) retrains all models each cycle; 33/50 cycles executed.  
- Round‑trip print→CAD→print pipeline validated on **21 k unique files** at 100 % accuracy; remaining shards queued for full coverage.  
- Templates for every function in HyperCAD‑S, HyperMILL, Mastercam, ESPRIT (49, 40, 43, 38 functions respectively).  

**DECISIONS**  
- Adopt bulk‑ZIP/7z downloads from public repos and ABC dataset to reach 100k+ files without credentials.  
- Reject synthetic data; focus on real STEP/IGES files only.  
- Map every CAD software button → PSN node, then build a “CAD‑atomic‑ops engine” that routes calls via the PSN graph.  
- Use a declarative config runner (`cad-train-pipeline-runner.mjs`) for all training pipelines; keep logic in JSON configs.  
- Persist large corpora and ledgers outside Git (gitignore).  

**OPERATOR DIRECTIVES**  
- Train system to **100 % accuracy** across all CAD files & prints.  
- For parts with only a print, generate CAD → regenerate print → compare & retrain.  
- Develop templates for every single function in HyperCAD, HyperMILL, Mastercam, ESPRIT.  
- No synthetics; batch download real CAD files from vendors (GrabCad, Misumi, etc.) once credentials are supplied.  

**FINDINGS/BUGS**  
- Vendor APIs require authenticated sessions or B2B certificates; GrabCad and Misumi cannot be bulk‑downloaded without operator‑supplied tokens.  
- GitHub trees API rate‑limits unauthenticated calls (56 per hour); a `GITHUB_TOKEN` is needed for large repo scans.  
- ABC shards are `.7z`; 7‑Zip required for extraction on Windows.  
- Some legacy scripts still reference old `cad-corpus-print-all.mjs`; updated to new `cad-corpus-roundtrip-full.mjs`.  

**DOMAIN SPECIFICS**  
- **PRISM CAD pipeline** (`slot/delta`, `CAD-PIPELINE-WIRE-MS0`).  
- **Atomic‑ops ontology**: 38 base ops × 11 platforms = 418 PSN nodes.  
- **Archetype classifier** (11 archetypes + 66 extended).  
- **AI systems**: k‑NN, PageRank‑GNN, TF‑IDF RAG, LoRA adapter, Deep‑Reasoning ensemble.  
- **Perpetual loop** stages: data extraction → feature emit → model training → evaluation → ledger update.  

**TOOLS USED**  
- Node.js scripts (`cad-*.mjs`, `cad-perpetual-loop-driver.mjs`, `cad-train-pipeline-runner.mjs`).  
- Git for slot claiming and commit hygiene.  
- 7‑Zip for ABC shard extraction; PowerShell Expand‑Archive for bulk ZIPs.  
- GitHub API (trees, archives) for repo enumeration.  
- PRISM helper modules (`chat-slots.mjs`, `audit-roadmap-drift.mjs`).  

**OPEN THREADS**  
- Complete full round‑trip validation on the remaining ~84 k ABC files (currently 21 k processed).  
- Integrate vendor credential handling (GrabCad, Misumi) once tokens are supplied.  
- Finalize PSN‑based CAD‑atomic‑ops engine integration with all 418 nodes for runtime execution.
