# cad session b1a71f80 (2026-06-09, 0.7MB, spine 8KB, 1 slice(s), model gpt-oss:20b)

**DECISIONS**
- Default model set to **fable** (`"model":"fable"`).
- Enable **ultracode** and **workflows** (`"ultracode":true`, `"enableWorkflows":true`).
- Keep existing `effortLevel:"xhigh"` (required for ultracode).
- Note that fable may not support xhigh/ultracode; if it fails, fallback to **opus** for ultracode runs.

**OPERATOR DIRECTIVES**
- “can you make the default model fable ultracode?”

**FINDINGS/BUGS**
- Fable’s compatibility with `xhigh`/ultracode is unverified; the flag may silently no‑op.
- Current session remains on Opus 4.8 until `/model` is run; changes apply only to new sessions.

**DOMAIN SPECIFICS**
- Ultracode requires workflows enabled **and** an xhigh‑capable model.
- Settings hierarchy: global (`~/.claude/settings.json`) → project (`.claude/settings.json`) → local (`.claude/settings.local.json`).
- `c-to-h-mirror` hook automatically replicates changes from C drive to H drive.

**TOOLS USED**
- **Configuration skill** (edit settings.json)
- **Read tool** (pre‑merge read of existing file)
- **Edit tool** (merge new values)
- `/config` slash command (suggested for simple settings)
- `/model` command (change model in session)
- Hooks: `PreToolUse`, `PostToolUse`, `PreCompact`, `PostCompact`, `Stop`, `Notification`, `SessionStart`
- `c-to-h-mirror` hook

**OPEN THREADS**
- Verify fable supports ultracode by starting a fresh session and checking for the “ultracode is on” reminder.
- Decide whether to keep ultracode enabled with fable or pin opus for guaranteed ultracode support.
