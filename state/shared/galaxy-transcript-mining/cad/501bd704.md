# cad session 501bd704 (2026-06-04, 22.3MB, spine 93KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- GPU‑stack health foundation (MS0): `scripts/py/gpu_health.py`, `scripts/lib/py-subprocess-bridge.mjs` (23 tests), `GpuStackHealthEngine.ts` (17 tests) wired to `devDispatcher`. Commits: `f11efe4047`, `9e5dcca6ad`.  
- NN/GNN degeneracy fix (F0): base‑rate normalization, direct‑embed inference, leak‑free source enrichment; calibration disabled by default. Commits: `a6bd95c163`, `8abba4f27c`, `ae60024500`, `2e9e94808d`.  
- GNN feature‑enrichment: engine‑source signal extraction, hash‑delimiter fix (`da6aedfc07`). Commit: `dcb2c86bb8`.  
- Calibration negative result: LOO calibration regressed AUROC/Brier; shipped optional. Commit: `44f4a90ccb`.  
- Fleet learning artifacts & CRLF handling: wiki lesson on CRLF/lf, memory consolidation, stale‑memory fixes. Commits: `6f2bad4792`, `9bd4b22abd` (adds `.gitattributes eol=lf`).  

**DECISIONS**  
- Separate GPU health gate from training pipeline; use Node→Python NDJSON bridge to avoid non‑zero exit swallowing.  
- Base‑rate normalization in `voteDispatcher` prevents majority‑class collapse.  
- Direct‑embed inference bypasses broken 8‑d edgeless model; relies on full‑coverage nomic embeddings.  
- Source enrichment (docblock + public API) replaces name‑only embeddings; leak‑free.  
- Calibration optional after empirical regression; future holdout‑aware calibration design.  
- Resume‑hash must include hidden `\x1F` delimiter for non‑ghost nodes.  
- Default per‑class stratified holdout split; zero‑holdout classes disallowed, ensuring fair macro‑F1/AUROC.  
- CLI flag `--flat-holdout` preserves legacy flat splits.  
- Current‑state banners in memories prevent CRLF re‑introduction.  
- Target macro‑F1 ≥ 0.55 via class‑balancing reference pool (currently 45 % `prism_turning`).  

**OPERATOR DIRECTIVES**  
- Keep review gates active; cross‑galaxy work allowed.  
- Continue loop with calibration‑done‑right (temperature scaling preserving AUROC) and class‑balancing to push macroF1 → 0.55, or redirect.  

**FINDINGS/BUGS**  
- Feature starvation + edgeless inference caused constant‑vote collapse.  
- Leak: ghost embeddings contained truth label (`proposed_wiring`).  
- Calibration overconfidence due to LOO pool density; AUROC/Brier regressed.  
- Resume‑hash omitted `\x1F` delimiter → silent re‑embed of ~372k nodes.  
- Stale memory artifacts about `.gitattributes` and CRLF handling.  
- Flat seededShuffle split gave `prism_calc` zero holdout samples, inflating macro‑F1 & under‑stating AUROC.  
- Stratified split yields AUROC 0.808 (passes gate), macro‑F1 0.439, Brier 0.179; accuracy drops but reflects true performance.  
- Recurring mistakes: commit‑message quoting + `--pathspec‑last`, shared‑tree absorption, `index.lock` polling, invisible‑byte hash invariants.  

**DOMAIN SPECIFICS**  
- Engines/Actions: `GpuStackHealthEngine.ts`, `py-subprocess-bridge.mjs`, `gpu_health.py`.  
- Dispatchers: `devDispatcher` wiring for `prism_dev:gpu_stack_health`.  
- Metrics: AUROC, macro‑F1, Brier, accuracy, `isDegenerate`.  
- Paths: `scripts/py/gpu_health.py`, `scripts/lib/py-subprocess-bridge.mjs`, `src/GpuStackHealthEngine.ts`, `src/__tests__/GpuStackHealthEngine.test.ts`, `seed-ghost-gnn-classify.mjs`, `build-node-embeddings.mjs`, `runAssessment`, `makeGraph`, `NN‑EVAL.json`.  
- Gate requirement: per‑file review with two reviewers and R12 “is this gaming the metric?” check.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `/checkin` pipeline, `/startup` pipeline.  
- Node/TS tooling: `vitest`, TypeScript compiler, esbuild bundler.  
- Python: torch (not installed), nomic embeddings API.  
- Git hooks: slot‑enforce, commit‑enforce, index.lock handling.  
- Git (`.gitattributes`).  
- Memory system & wiki for recurring mistakes.  
- CLI flags (`--flat-holdout`).  
- Evaluation scripts/tests (58 baseline + 4 stratification‑specific).  
- `runAssessment` pipeline and `makeGraph` generator.  

**OPEN THREADS**  
1. Design holdout‑aware calibration that improves AUROC/Brier without overconfidence.  
2. Balance/enlarge reference pool to lift macroF1 toward ≥ 0.55 gate; class‑balancing target.  
3. Deploy GPU training once torch is installed (golf‑side).  
4. Finalize PSN leg gating ensuring all metrics meet thresholds.  
5. Integrate source enrichment into production pipeline; monitor for future leaks.  
6. Finalize shipping of stratified default split; ensure all tests pass under new config.  
7. Verify per‑file gate compliance with two reviewers and R12 check.
