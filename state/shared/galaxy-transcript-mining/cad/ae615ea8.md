# cad session ae615ea8 (2026-06-10, 40.6MB, spine 268KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- SIM‑4 (machine‑bind gate) – 54 tests, 3/3 scrutiny passed.  
- SIM‑5 (run‑complete gate) – 63 tests, 3/3 scrutiny passed.  
- SIM‑6 (assessLiveRunClearance 5‑gate combiner) – 53 vitest, 3/3 scrutiny passed.  
- SIM‑7 (fleet rollup `cimco‑sim‑fleet.mjs`) – 10 tests, 3/3 scrutiny passed.  
- U‑CIMCO‑SIM‑1A (`--op read‑report` driver + normalizer) – 67/67 green, 3/3 scrutiny passed.  
- Echo Forge Roadmap v2 (state/shared/specs/ECHO‑FORGE‑ROADMAP‑2026‑06‑09.md).  
- `U-CIMCO-FSM-LIVE-DRIVE` commit dba50eb3da – orchestrator chaining navigate→run→read‑report, fail‑closed logic.  
- `CLEARANCE-COMPOSE` commit e482189325 – maps drive result to dispatcher clearance gate; adds non‑clearing report-header-only.  
- `SIM-AUTONOMOUS-PROOF` commit 40cf2e0d3b – single‑process C# op invoke-read launching CIMCO, running Simulate, reading Report grid.  
- All‑15 fleet sweep commit a28927fc0b – PowerShell per‑machine driver (`--from-envelopes`), reaper-safe; outputs `fleet-drive-results.json`.  
- `e276f13216`: added `--pre <cfg>` capability to pre‑configure CIMCO simulation.  
- `af005aa2d7`: spec mapping of all CIMCO settings surface.  
- `c05b697d77`: renamed `preCtl` to avoid HashSet collision.  
- `1672656ada`: recon commit confirming Configure Machine Type dialog opens a Win32 property sheet.  
- `9a3d782ae5`: reverted crash‑prone MSAA read‑window; restored safe list-windows.  
- `39508c2774`: implemented crash-safe `list-windows` op (Win32 EnumWindows).  
- `96bf5a573e`: P1 fix to skip MSAA ribbon walk in `list-windows`.  
- `f426d4a18a`: verified CIMCO Edit 26.01.10 licensed via About dialog.  
- `2322f566b3`: mapped all 23 CIMCO Setup pages with `setup-pages` op.  
- `78d3580c59`: updated docs with machine‑config model correction (Backplot Setup combo).  
- `1090ae5055`: added cross‑process combo‑read capability for Control Type and Machine setup combos.  
- `795df9573e`: implemented safe‑by‑default `set-setting` write op (toggle → verify → discard on Cancel).  
- PrismCimcoUI.exe built via `build.ps1` using .NET Framework 4.0 csc.exe.

**DECISIONS**  
- Use Ollama qwen2.5-coder:32b for bulk deep‑dive; keep 3-of-3 Claude scrutiny.  
- Adopt “Ollama pre‑flight + keep 3‑of‑3” token strategy; limit workflow to 4 agents (3 lenses + 1 synth).  
- Operator‑gated tasks remain (SIM‑1 live report-grid de‑risk, C# read-report build); U-CIMCO-FSM-LIVE-DRIVE is next critical step.  
- Switch fleet sweep to PowerShell (`cimco-fleet-sweep.ps1`) with `--from-envelopes` finalize mode; avoid node reaper kills.  
- Declared header‑only reports as non‑clearing to preserve ambiguity between clean runs and unexecuted collisions.  
- Use Win32 API (EnumWindows, SendMessage) for UI automation; drop MSAA except for FireControl helper.  
- Pre‑configure simulation via FireControl before opening CIMCO; replace crash-prone read-window with safe `list-windows`.  
- Tailor CIMCO by mapping all Setup pages and identifying machine-config combos; defer write ops until fresh budget due to safety surface.

**OPERATOR DIRECTIVES**  
- Start U-CIMCO-FSM-LIVE-DRIVE (final live‑drive FSM unit).  
- Open CIMCO Edit on VMC‑01 to trigger first live E2E simulation once FSM built.  
- Enable `/yolo-mode`: activate simulation add‑on settings (collision checking, stock, machine config) for meaningful simulations.  
- Complete closed-loop testing of post processors on 15 JM machines (12 simulatable); then build posts for highest-selling machines if finished before midnight.  
- Use Ollama/Hermes for searches, reading, writing, coding; adjust every CIMCO setting.

**FINDINGS/BUGS**  
- Torn‑commit race lost Program.cs in read-report; fixed with `git commit --only`.  
- Dispatcher `prism_pp` commented out – entire post surface dark.  
- Rate-limit failure resolved by limiting workflow to 4 agents.  
- Unit test failures in read-report normalizer fixed: discriminated frame vs report grid, rejected empty arrays, corrected fixture paths.  
- Stale assumption that CIMCO required operator‑opened session; MSAA driver proved cold launch works.  
- Reaper killed node drivers after second CIMCO launch → solved by per-machine PowerShell sweep.  
- Orphan PrismCimcoUI.exe left behind when using `--keep`; fixed in `killCimco()` helper.  
- Header-only reports mislabelled as opaque; corrected to distinct non-clearing label.  
- Old MSAA read-window crashed (exit 255); replaced by Win32 messages.  
- UI driver uses only safe cross‑process messages; no AccessibleObjectFromWindow or AccessibleChildren.  
- Safe-by-default writes may discard changes if not verified; dialog always closed via WM_CLOSE unless OK pressed.

**DOMAIN SPECIFICS**  
- SIM‑4: bind gate (machine/read-back, VR-not-NGC, 25.4× units).  
- SIM‑5: run‑complete gate (terminal‑complete, quiescence, coverage, modal watcher).  
- SIM‑6: assessLiveRunClearance (5‑gate combiner: bind, units, kinematics, sim, run‑complete).  
- SIM‑7: fleet rollup `cimco-sim-fleet.mjs` over 15 JM machines.  
- U-CIMCO-SIM-1A: MSAA reader `--op read-report` (value-aware docking pane walk, fail‑closed ladder).  
- PrismCimcoUI.exe: C# UI driver (IAccessible, map/find/invoke); path `mcp-server/data/posts/prism-base/cimco-bridge/ui-driver/Program.cs`.  
- CIMCO simulation workflow: Machine Simulation tab → Simulate button → Report grid.  
- Fleet-drive script `cimco-fleet-sweep.ps1` iterates over JM fleet; writes JSONL envelopes (`fleet-drive-results.json`).  
- Clearance gate `cimco_live_run_clearance` consumes envelope data to decide cleared / clearedForLiveRun.  
- CIMCO Edit simulation via PrismCimcoUI.exe driver (compiled with csc).  
- FireControl helper resolves controls, applies MotionDeny checks, fires actions.  
- .mcfg files in `resources/cimco-2026/CIMCOEdit/MachineCfg`; indexed by `machine-index.json`.  
- `jm-fleet-sim-map.json` maps JM machines to corresponding `.mcfg`.  
- Setup pages accessed through SysTreeView32 Tree1; combos for Control Type, Machine setup.  
- Win32 dialog navigation: property sheet #32770 with 23 pages; use TVM_GETNEXTITEM (0x110A) / TVM_SELECTITEM (0x110B).  
- Checkbox style via GetWindowLongPtr(GWL_STYLE) & 0x0F.  
- Combo box text retrieval via SendMessageW + StringBuilder.

**TOOLS USED**  
- Ollama qwen2.5-coder:32b for deep-dive extraction.  
- Ultracode workflow (4 agents) for high-level synthesis.  
- Git + `git commit --only` for atomic commits on shared tree.  
- CronCreate / CronDelete for recurring `/loop`.  
- PRISM checkin pipeline, skill tool, `/startup`, `/checkin-echo`.  
- PRISM helpers: chat-slots.mjs, ask-ollama.mjs.  
- C# driver executable PrismCimcoUI.exe.  
- Node scripts for orchestration and testing.  
- PowerShell sweep `cimco-fleet-sweep.ps1`.  
- Win32 API wrappers (EnumWindows, SendMessage, GetDlgCtrlID).  
- .NET Framework 4.0 csc.exe.  
- Hermes agentic coding capabilities.  
- `.claude/scripts/scrutiny-3way.mjs` for code review.

**OPEN THREADS**  
- Load machine kinematics via .mcfg per machine (default CIMCO machine used currently).  
- Create known‑bad NC to exercise collision-row extraction once machine config is in place.  
- Produce production NCs for each JM machine; validate inch vs mm consistency.  
- Adjust simulation add‑on settings (collision checking, stock) per `/yolo-mode`.  
- Build combo‑write capability to select machine‑config combos and trigger `load-machine`.  
- Validate over‑travel NC after loading machine.  
- Finish closed-loop testing on all JM machines; activate/configure CIMCO simulation add‑on.  
- Integrate new loop knowledge & Hermes agentic coding into workflow.  
- Build/test U-CIMCO-FSM-LIVE-DRIVE (next code step).  
- Operator-gated SIM‑1 live report-grid de-risk.  
- Live E2E simulation once FSM built.
