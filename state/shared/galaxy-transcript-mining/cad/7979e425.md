# cad session 7979e425 (2026-05-26, 17.2MB, spine 51KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e47be02504` – Hurco parser extension (20/20 tests, new ops).  
- `d7f88bb618` – Laser‑tribal corpus build.  
- `e81f0fc590` – Token‑savings coverage (Cat‑B dedup).  
- `e9bf140cbc` – U‑ZO‑MS0‑02/03/04 readers (3 read‑side surfaces).  
- `755ef9f182` – Doc reflection for MS0.  
- `6a3a5e99c4` – U‑ZO‑MS0‑05/06 + CLI wrapper (MS0 complete).  
- `f13cc886fe` – Final doc reflection after MS0 closure.  
- `1805325b14` – Per‑slot PSN aggregator hook + fleet dashboard.  
- `8e6e23bcf0` – MS1 Batch B1 enrichSlot foundation (5 slots enriched, 99 enrichment edges).  

**DECISIONS**  
- Hermes/Zebra scope: complete all MS0 surfaces first; then ship MS1 decider before any UIA work.  
- Keep Zebra orchestrator singular; only generalize the PSN aggregator to every slot via a lightweight hook.  
- Use `/goal` + `/loop [5m]` to auto‑drive completion of remaining units in current envelope.  
- Adopt per‑slot `loadSlotContext` as the single source of truth for self‑awareness.  
- Opted to ship MS1 Batch B1 (enrichment) before full MS1 decider; this keeps context budget within limits.  

**OPERATOR DIRECTIVES**  
- Run G10 dry‑run and real registration (`install-zebra-orchestrator-task.ps1`).  
- Edit `chat-slots.json` to set `zebraOptIn=true` for selected slots (conservative: bravo + golf; moderate: add mike).  
- After G10/G12, monitor Zebra sweep output in `/startup‑zebra`.  

**FINDINGS/BUGS**  
- Windows ESM import required `pathToFileURL`; otherwise `ERR_UNSUPPORTED_ESM_URL_SCHEME`.  
- OneDrive redirects Desktop to `C:/Users/<user>/OneDrive/Desktop/`; earlier `.bat` placed files in legacy path.  
- CLAUDE CLI not found when invoked from PowerShell; resolved by using explicit full path or adding to PATH.  
- PSN aggregator hook initially emitted no block due to silent try/catch; fixed by exposing actual error and ensuring output.  

**DOMAIN SPECIFICS**  
- Hermes orchestrator: single‑source decision engine, operator‑gated suggestions (G4 doctrine).  
- Zebra‑Omniscient MS0: 6 surfaces – brief, vision, bridge_units, soul, loop, tokenZone.  
- PSN: 11‑leg taxonomy; `loadSlotContext` composes 5 legs into a single bundle.  
- Slot souls & refuse_list enforce hard constraints on suggestions.  
- Loop state and token zone provide dynamic awareness for each chat.  
- CAD integration via `zebra-context-bundle.mjs`; supports feature extraction, resource linking, and tribal knowledge.  

**TOOLS USED**  
- PRISM helpers: `.claude/helpers/chat-slots.mjs`, `.claude/hooks/slot-context-bundle-inject.mjs`.  
- Scripts: `scripts/zebra-context-bundle.mjs`, `scripts/zebra-context-fleet-dashboard.mjs`, `scripts/zebra-orchestrator-sweep.mjs`.  
- Node utilities: `regen-viz.mjs`, `merge-augmentations.mjs`, `node .claude/helpers/priority‑queue.mjs`.  
- Git (CLI, lockfile handling).  
- Windows Terminal / PowerShell for operator actions.  

**OPEN THREADS**  
- **MS1 Decider** – full `decideSlotAction` ADT and sweep wiring still pending.  
- **UIA Pane Focus** (`U‑ZM2‑02/03/04`) – requires native binding research; high risk, separate session.  
- **MS2 Goal‑Aware Planner** – depends on MS1 completion.  
- **MS4 Closed‑Learning Harness** – out of current scope; to be revisited after MS1 data available.  
- **P1 Follow‑ups** from scrutiny (surfacesOkCount, schemaVersion enforcement, loop cache invalidation).  
- **Full Fleet Node Population** – generator created roost + 26 nodes, but enrichment for remaining 21 slots still pending beyond the initial 5.  
- **Operator‑action G10/G12** – not yet executed; must be run by operator to activate Zebra sweep.
