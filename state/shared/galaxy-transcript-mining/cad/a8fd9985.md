# cad session a8fd9985 (2026-05-22, 28MB, spine 256KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `/fleet-reaper-home` run → host preset `home`; 6 tasks fired, sweep clean, JSONL‑tail monitor armed.  
- Golf slot converted from hygiene‑only to full work slot; soul file updated (`role: work`, `refuse_list` removed); committed on `slot/golf`.  
- `claude-md-golf-only-guard.mjs` identity bug fixed (payload session_id match via `matchesGolfId()` helper); commit 4d4a4900e7.  
- CADDispatcher wired with `CADToSTEPPipelineEngine`: added enum entries and case handlers; mock‑mode test written but not yet committed.  
- Commit 4d4a4900e7 updated `CLAUDE.md` to declare Golf as full‑work slot (removed hygiene flags).  
- Retraction commit removed false “blisk proof” claim, added `feedback_api_success_is_not_proof.md`.  
- Asset‑copy commit manually copied `cad-token-vocabulary.json` from `src/data/` to `dist/data/`; build script notes updated.  
- U‑FR‑STUCK‑HUNT commits adding fleet‑reaper hunters for stuck bash, fsmonitor orphans, stale slot PIDs (33/33 tests).  
- Docker & Qdrant restored; MCP connectivity re‑established.  
- Memory pressure reduced from ~97 % to ~70 %.

**DECISIONS**  
- Doctrine shift: fleet‑reaper ownership moved alpha → golf (2026‑05‑16); Golf slot reclassified as full work slot, hygiene removed (2026‑05‑20).  
- Home‑tier preset selected for PC (RTX 4080+, 64 GB RAM).  
- Adopted proof standard: “print→CAD→new‑print comparison table” per `feedback_api_success_is_not_proof.md`.  
- `/goal` remains open across `/compact`; requires CAD→drawing emission action to close.  
- Build script must copy all data assets into `dist/` (e.g., `cad-token-vocabulary.json`).  
- Prioritize infrastructure cleanup: Docker/Qdrant restored, memory pressure reduced 97 % → 70%; upgrade RAM or close ~6 chat windows (~30 GB) before running blisk proof.

**OPERATOR DIRECTIVES**  
- `/checkin-golf /fleet-reaper-home` – claim golf slot, run fleet‑reaper.  
- `/goal [complete all remaining tasks and units left for golf | completed and wired] /loop [5m]`.  
- Change golf settings to allow full work slot operation; commit to own work tree.  
- Ensure all fleet‑hygiene helpers (Node orphan cleaner, zombie reaper v2, PS orphan reaper, memory pressure auto‑relief, hook janitor, cleanup orchestrator) active.  
- Verify flet monitor and memory pressure helpers running.  
- New goal: train CAD capabilities; prove blisk drawing in hypercad.

**FINDINGS/BUGS**  
- `claude-md-golf-only-guard` ID check mismatch (claude-a8fd9985 vs claude-8852f3dc); fixed via payload session_id match.  
- `golf-slot-write-allowlist.mjs` unwired → no write restriction; now wired.  
- `cad-token-vocabulary.json` missing from `dist/data/`; caused `cad_text_parse` crash – manual copy performed, build‑script gap noted.  
- `CADToSTEPPipelineEngine` existed but not wired in `cadDispatcher.ts`; added enum + case handlers (uncommitted earlier).  
- No CAD→drawing emission action present; required for roundtrip proof.  
- Build script fails to copy data assets on incremental builds (`npm run build:*`); updated to always copy.  
- PDF reader `pdftoppm` unavailable, blocking blueprint text extraction; restored.  
- `cad_pdf_blueprint_extract` under‑fitted; fixed with proper notation.  
- `part_template_cylinder` produced degenerate output (radius: null).  
- Docker failed due to memory starvation (WSL2 SIGBUS); Qdrant unhealthy → MCP disconnects.  
- Stuck bash processes (>5 min), fsmonitor orphans, stale slot PIDs; hunters added.  
- Fleet‑reaper self‑protection bug fixed with `buildProtectedPidSet`.

**DOMAIN SPECIFICS**  
- 26‑slot NATO chat fleet; slot worktree layout `H:/prism-slot-golf` (branch `slot/golf`).  
- Golf slot now full work slot + reaper owner.  
- CAD engines: `CADToSTEPPipelineEngine (E2503)`, `GroundTruthFeatureTreeExtractor (E2504)`, `DimensionalSignatureEngine (E2505)`.  
- PDF/blueprint extraction (`cad_pdf_blueprint_extract`), feature‑tree validation, dimensional signature.  
- Fleet‑reaper tasks: Node orphan cleaner, zombie reaper v2, PS orphan reaper, memory pressure auto‑relief, hook janitor, cleanup orchestrator.  
- Docker + Qdrant + NIM stack for semantic search backend.

**TOOLS USED**  
- Node scripts: `chat-slots.mjs`, `fleet-reaper-home`, `precompact-pending-guard.mjs`, `per-agent-handoff.mjs`.  
- PRISM helpers: `stable-session-id.mjs`, `claude-md-golf-only-guard.mjs` (hook), `__tests__/claude-md-golf-only-guard.test.mjs`.  
- Git for commits/branch.  
- Vitest (`PRISM_CAD_MOCK=1`) for CAD dispatcher tests.  
- Per‑file scrutiny gate & per‑agent handoff agents.  
- Docker CLI, `docker-compose`; Qdrant REST API; NIM container pull.  
- Node test harness: `node:test`, `node:assert/strict`.

**OPEN THREADS**  
- Commit & test wiring of `CADToSTEPPipelineEngine` in `cadDispatcher.ts`.  
- Wire dispatcher action `cad_drawing_emit` to walk CAD graph and emit dimensional array.  
- Fix build script to copy all data assets (`cad-token-vocabulary.json`, etc.) on every build; restart MCP after copy.  
- Run small JM‑Die STEP file through `cad_dimensional_signature` for first real dimensional signature.  
- Execute print→CAD→new‑print roundtrip (JM‑Die) and generate comparison table → blisk proof.  
- Finalize handoff file (`HANDOFF-claude-9d009d1c-cad-fusion-live-ms0.md`) for fresh session resumption.  
- Ensure Docker/NIM stack stable after memory upgrade or chat‑window reduction; keep fleet‑hygiene tasks active post `/compact`.
