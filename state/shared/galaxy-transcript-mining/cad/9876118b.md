# cad session 9876118b (2026-05-18, 48.5MB, spine 260KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `da01003b61` – FLEET‑TASK‑HEALTH‑MS0 (watchdog + nudge hooks, 6 files)  
  - Watchdog (`fleet-task-health-watch.mjs`) with 32 tests; telemetry writer `state/shared/fleet-reaper-candidates.jsonl`.  
  - Nudge hook (`critical-memory-compact-nudge.mjs`) with 19 tests.  
- `ac9cca8902` – enum‑blind `$p.Name` C0‑strip fix + regression tests.  
- `d3db37cc2c` – fleet‑reaper‑sweep per‑PID Stop‑Process patch (`windowsKill` batch‑timeout bug).  
- `bb7d30c7cc` – slotLabel:null deep‑fix (unblocks Build B).  
- `9cfc411eb3` – fleet‑reaper critical threshold lowered 95→88%.  
- `b41b829232` – soft‑relief V2 (trims alive large processes under critical pressure).  
- `cc14791cd7` – Ollama keep_alive set to “‑1” (pins models in VRAM).  
- `U‑CRIT‑NUDGE‑PATHFIX` – fixes nudge hook path bug.  
- `nim-docker-launcher.mjs` – NIM server provisioner, 16/16 tests.  
- `[NIM‑ACTIVATION‑MS0]/SPEC` – migration spec for offload hooks to use local‑llm‑bridge.  
- `U‑FR‑CRASH‑WATCH`, `U‑CHO01–04` (orchestrator decision, token watch, crash‑watch, advisory injection) – all passing tests.

**DECISIONS**  
- Fleet‑reaper ownership moved to the golf slot; runs on every `/checkin-golf`.  
- Reaper always on; alpha owns global scheduled task and per‑session monitor.  
- Critical pressure threshold set to 88 % (down from 95 %).  
- Soft‑relief V2 enabled to trim large processes under critical pressure.  
- Slot ownership enforced by `golf-slot-reaper-guardian.mjs`; non‑alpha chats silently no‑op.  
- Batch‑kill per‑PID patch applied; batch‑timeout bug fixed.  
- Auto‑cycle 5 Claude accounts as usage limits approach – design pending.

**OPERATOR DIRECTIVES**  
- Treat golf slot as normal work slot (no `--golf` flag).  
- Use `/fleet-reaper` only in the golf slot per doctrine.  
- Run `/compact` on the largest chat after watchdog/actuator activation (~99 % memory).  
- Force‑take golf slot on `/checkin-golf`, bind handoff to `golf-work`.  
- Disable legacy allowlist hook (`export PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1`).  
- Reboot after pagefile fix (raises commit ceiling).  
- Start Docker Desktop, set `NGC_API_KEY`, then run `node mcp-server/scripts/nim-docker-launcher.mjs`.

**FINDINGS/BUGS**  
- Watchdog mis‑classified exit 1/2/3 as failures; now flags only HRESULT ≥0x80000000.  
- Path‑order bug in compact nudge: used `.claude/state/chat-slots.json` before `state/shared`; corrected.  
- Import side‑effect leak caused test failures; added guard to hooks.  
- Enum‑blind regression in `process-slot-map.mjs` (`$p.Name`) fixed.  
- Batch‑kill timeout under high pressure caused silent “no result” mislabeling – per‑PID patch applied.  
- SlotLabel:null deep‑fix pending (affects Build B nudge).  
- Monitor flooding due to aggressive‑offload hint clause resolved by filter change.  
- Memory‑pressure auto‑relief task missing; operator must install `install-memory-pressure-task.ps1`.  
- Crashes (`xmalloc` failures) due to commit‑limit (pagefile 4 GB); fixed but requires reboot.

**DOMAIN SPECIFICS**  
- Fleet‑reaper: `fleet-reaper-sweep.mjs`, scheduled task “PRISM Fleet Reaper”, per‑session monitor (`bbyba1uq0`).  
- Memory monitor: `fleet-memory-monitor.mjs`, `process-slot-map.mjs`, `chat-slots.mjs`.  
- Hooks: `golf-slot-write-allowlist.mjs`, `precompact-pending-guard.mjs`, `per-agent-handoff.mjs`, `critical-memory-compact-nudge.mjs`, `fleet-task-health-watch.mjs`.  
- State paths: `state/shared/fleet-reaper-candidates.jsonl`, `state/CURRENT_POSITION.md`, `state/shared/claude-usage-telemetry.jsonl`.  
- NIM integration: `local-llm-bridge` (NIM→vLLM→Ollama), `nim-hook-bridge` client, launcher.  
- Orchestrator modules: `chat-orchestrator-decisions.mjs`, `chat-token-watch.mjs`, crash‑watch, advisory injection.

**TOOLS USED**  
- Node.js scripts (`mjs`), PowerShell installers (`install-fleet-reaper-task.ps1`, `install-memory-pressure-task.ps1`).  
- Windows Task Scheduler (`schtasks /Query /TN "PRISM Fleet Reaper"`).  
- Docker Desktop, NGC API key.  
- Git for commits and lane‑tracking.  
- Node test runner (`node:test`).

**OPEN THREADS**  
- SlotLabel:null deep‑fix (requires peer claim on `fleet-memory-monitor.mjs` & `process-slot-map.mjs`).  
- Offload‑hook migration – rewrite ~12 hooks to use `local-llm-bridge.queryLocalLLM`.  
- Verify that per‑PID kill logic fully eliminates batch‑timeout failures under sustained >90 % pressure.  
- Ensure memory‑pressure auto‑relief task is installed and active on all hosts.  
- Auto‑cycle 5 Claude accounts as usage limits approach – design proposal pending.
