# cad session 0769892b (2026-05-19, 0.9MB, spine 2KB, 1 slice(s), model gpt-oss:20b)

**OPERATOR DIRECTIVES**  
- “Make Codex 5.5 review every line of code so there are no syntax errors.”  
- “Check `/system‑viz` to generate the node for system viz for the new file it just reviewed.”  
- “Hunt down nodes to ghost wire using logic and knowledge of the Prism app purpose and backend development system.”

**DECISIONS**  
- Use two parallel reviewers per file (per‑file scrutiny gate) to cover all lines.  
- Redirect authored content (plans, scripts) via the H: drive enforcement hook to `H:\.claude\`.  
- Leverage existing modules: `parseVerdictLine` (`.claude/helpers/scrutiny-ledger.mjs`) and `runCodexReview` (exported from `scrutiny‑3way`).  

**FINDINGS/BUGS**  
- Bug in module function: `this?.ranAt` is invalid; should reference `result.ranAt`.  
- Hardening of top‑level `main()` invocation required.

**DOMAIN SPECIFICS**  
- Engines/actions: Codex 5.5 reviewer, system‑viz node generator, ghost‑wire hunter.  
- Dispatchers/metrics: per‑file scrutiny gate dispatching two parallel reviewers; tracking syntax error counts.  
- Paths unique to this galaxy: `H:\.claude\` for plan storage; `.claude/helpers/scrutiny-ledger.mjs`; `/system-viz`.

**TOOLS USED**  
- Codex 5.5 (reviewer).  
- Scrutiny‑3way (`runCodexReview`).  
- Helper module `.claude/helpers/scrutiny-ledger.mjs` (`parseVerdictLine`).  
- System‑viz module for node generation.  
- Ghost‑wire logic component.  
- H: drive enforcement hook.

**OPEN THREADS**  
- Implement per‑file scrutiny gate dispatching two parallel reviewers for File 1.  
- Integrate system‑viz node creation after each review.  
- Develop ghost‑wire hunting routine that uses Prism app purpose and backend dev knowledge.  
- Finalize bug fix (`result.ranAt`) and harden `main()` invocation.
