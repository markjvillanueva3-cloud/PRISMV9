# cad session 859c0089 (2026-06-02, 11.6MB, spine 55KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- **T1** – Added `lookupCuttingData()` to `UltimateSpeedFeedEngine` and rewired `_generatePresets` to use it (optimal per‑ISO presets). 13 tests + 2‑reviewer pass.  
- **G1** – Implemented `CoatingSelectionAdapter.compatibleIsoGroups()`; added safety P0 fix (`[P,M,K]` for unknown coatings). 10 tests + physics review PASS.  
- **G2** – Applied compatibility gate to the Fusion CSV generator (7 presets/tool → 1234 rows). 12 tests + 2‑reviewer pass.  
- **G3** – Built six Fusion `.machine` XML definitions (4 VMCs, multus mill‑turn, Roku‑Roku 5‑axis). 6 unit tests PASS.  
- **T3/T4** – Generated the full Fusion tool/holder libraries from JM’s real CSVs (218 tools → 1526 rows), added per‑material‑group presets and collision data. 2‑reviewer pass, all tests green.  

**DECISIONS**  
- Use lean TSX generators that import only catalog‑free engines (`UltimateSpeedFeedEngine`, `CoatingSelectionAdapter`, `JmDieMachineConfigEngine`) to avoid the `__dirname` crash in `catalogLoader.ts`.  
- Source cutting data exclusively from `lookupCuttingData()` for all formats (Fusion, hyperMILL, Mastercam) to guarantee consistency.  
- Apply a material‑compatibility gate (`compatibleIsoGroups()`) before emitting any preset; this satisfies the new goal constraint and fixes a safety P0 bug.  
- Commit each unit separately with per‑file scrutiny (2 reviewers/file); halt further work when context > 65 % (RED) to avoid half‑built units.  
- Use the comprehensive‑build cut‑off rule: once all enumerated units are committed, stop adding new ones until a fresh context window is available.

**OPERATOR DIRECTIVES**  
- `/goal [ /loop [5m] ... ]` – build tool/holder/machine DB for Fusion, then convert to hyperMILL and Mastercam; all data fields must be accurate, categorized by material type, and only populated for compatible domains.  
- Session‑scoped Stop hook: the goal remains active until the condition is met (i.e., G4 and G5 are completed).  

**FINDINGS/BUGS**  
- `catalogLoader.ts` uses `__dirname`, causing ESM crashes in TSX; prevented reuse of existing export engines.  
- Heavy memory usage during `vitest` caused OOMs; mitigated by running tests with a single fork and larger heap.  
- Safety P0 bug: unknown coatings were defaulting to `[P,M,K,S,H]`; fixed to conservative `[P,M,K]`.  
- Test bugs in G4 hyperMILL generator (multi‑line INSERT regex, off‑by‑one group index). Fixed without changing emission logic.  
- Turning context misroute: lathe tools incorrectly mapped to milling classes; resolved by reordering mapping logic.

**DOMAIN SPECIFICS**  
- **Fusion**: `.tools` JSON + `CSV_TOOLS_VERSION_1` CSV (172 data cols, 173 header tokens).  
- **ISO material groups**: P, M, K, N, S, H.  
- **Coating compatibility matrix** (ASM Vol.16 / Sandvik / Kennametal).  
- **Cutting physics**: `UltimateSpeedFeedEngine.lookupCuttingData()` (CUTTING_PARAMS table + diameter scaling + HSS derate).  
- **Machine DB**: hsmworks XML (`<machine>` namespace).  
- **hyperMILL**: SQLite `.hmt` schema (GeometryClasses, CuttingMaterials, Tools, etc.).  
- **Mastercam**: SQLite `.tooldb`; engine emits PRISM‑JSON `.mcam-tools`.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `checkin.md` pipeline.  
- Build/test tools: `tsc`, `vitest`, `build:fast`, Node ESM, Git.  
- Engines/scripts: `FusionToolExportEngine.ts`, `UltimateSpeedFeedEngine.ts`, `CoatingSelectionAdapter.ts`, `JmDieMachineConfigEngine.ts`, `HyperMillToolExportEngine.ts`, `MastercamToolExportEngine.ts`.  

**OPEN THREADS**  
1. **G4 hyperMILL generator** – finalize after turning‑context bug fix; ensure SQL output matches the engine’s contract and passes all unit tests.  
2. **G5 Mastercam export** – build a lean generator that emits PRISM‑JSON `.mcam-tools` (or SQLite `.tooldb`) using the shared `jm-tool-model.ts`.  
3. Re‑verify the ESM‑safe `catalogLoader.ts` fix once fleet load subsides; if successful, reuse existing export engines for G4/G5.  
4. Confirm that all generated Fusion libraries and machine DBs satisfy downstream conversion tooling (hyperMILL/Mastercam) without data loss.  

These items summarize what remains to be done to complete the galaxy’s work.
