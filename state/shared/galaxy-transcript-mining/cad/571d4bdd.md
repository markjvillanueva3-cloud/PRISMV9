# cad session 571d4bdd (2026-05-19, 16.8MB, spine 56KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-TDP06` – CNC‑derived ground‑truth library (`scripts/lib/cnc-ground-truth-lib.mjs`, tests, CLI build). Commit `01e59ad58a`.  
- `U-TDP07` – OCR cascade (PyMuPDF vector extraction + Qwen2.5‑VL raster OCR) wired into benchmark. Commit `5270c65aea`.  
- Mike+Golf queue consolidation: mike queue expanded to 69 units, golf trimmed to 5 hygiene‑only units.  

**DECISIONS**  
- CNC GT must be *presence‑only*; nominal extraction impossible from raw G‑code → removed `nominal_mm` field and aligned with CAD GT contract.  
- OCR pipeline staged: (1) vector PDF text via PyMuPDF (exact, no OCR), (2) raster PDFs rendered to PNG + Qwen2.5‑VL inference, (3) optional eDOCr2‑style segmentation for future GD&T extraction.  
- Use local Ollama on RTX 4080; prefer `qwen2.5vl:7b` (≈15 GB VRAM) over smaller VLMs to maximize accuracy while staying within GPU limits.  
- Prewarm VLM once per benchmark run to avoid cold‑load delays (~110 s).  

**OPERATOR DIRECTIVES**  
- “Continue improving our OCR and print reading capabilities.”  
- “Do deep research and find other reputable tools from good sources to plug into our OCR pipeline to improve accuracy.”  
- Resume blueprint‑reading training at U‑TDP07 (cascade) after completing 3‑of‑3 scrutiny for U‑TDP06.  

**FINDINGS/BUGS**  
- `nominal_mm` vs benchmark’s `.nominal` caused P0 in first review → switched to presence‑only.  
- Regex bug (`\bL\.E\.`) broke token extraction; fixed.  
- VLM cold‑load timeout (≈110 s) handled by prewarm and extended `VLM_TIMEOUT_MS`.  
- CLI reviewer‑B blocked by Ollama usage limit; pending re‑run after quota reset.  

**DOMAIN SPECIFICS**  
- **OCR Pipeline**:  
  - *Stage 1*: PyMuPDF (`get_text("words")`, `render_page`) → word tokens + bounding boxes, PNG raster for pages lacking text streams.  
  - *Stage 2*: Qwen2.5‑VL via Ollama → image→text+semantic tags; output parsed into `{kind, presence_only:true}` records.  
  - *Stage 3 (future)*: eDOCr2 segmentation for GD&T symbols.  
- **CNC GT Lib** (`scripts/lib/cnc-ground-truth-lib.mjs`): tokenizes G‑code, identifies drill/turning cycles, classifies into `central_oil_hole`, `cross_drilled_relief_holes`, `stepped_revolved_axis`; outputs presence‑only records.  
- **Benchmark Integration**: `run-ocr-benchmark.mjs` now calls `makeLiveExtractor()` returning the cascade; grades against U‑TDP05 (CAD) and U‑TDP06 (CNC).  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `per-agent-handoff.mjs`, `stable-session-id.mjs`.  
- Scripts: `hook-archive-crossref.mjs`, `cnc-ground-truth-lib.mjs`, `blueprint-extract-sidecar.py`, `blueprint-extract-io.mjs`.  
- Libraries: PyMuPDF, Ollama (qwen2.5vl:7b), Node.js test harness (`node:test`).  

**OPEN THREADS**  
1. Run the 3‑of‑3 scrutiny gate for U‑TDP06 commit `01e59ad58a` (pending after context cap).  
2. Re‑run CLI reviewer‑B once Ollama usage limit resets.  
3. Create a real PDF ground‑truth corpus to fully evaluate the OCR cascade against U‑TDP04 benchmark.  
4. Investigate eDOCr2 segmentation integration for GD&T extraction (next unit candidate).
