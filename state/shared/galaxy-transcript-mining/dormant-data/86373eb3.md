# dormant-data session 86373eb3 (2026-06-09, 30.1MB, spine 355KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**

- Added `Launch‑PRISM‑Fleet‑3win.ps1` – 3‑window, per‑account launcher.  
- Updated `settings.json`: `LOCAL_LLM_BACKEND=ollama`, `PRISM_RESUME_MAX_MB=200`.  
- Patched `nim‑autostart.mjs` guard to skip NIM when backend is ollama.  
- Cleaned `docker-compose.yml`: removed obsolete `version:`, set `KEEP_ALIVE=30m`, reduced `MAX_LOADED_MODELS` to 4, dropped compose `ollama` service from autostart list.  
- Repointed desktop shortcuts: `PRISM Fleet.lnk` → original 4‑window launcher; `PRISM Fleet (24‑tab).lnk` → new 3‑window launcher.  
- Verified native Ollama auto‑start via scheduled task “PRISM Ollama Serve”.  
- Built GNN feeder (`vault-to-gnn-refpool.mjs`) – 9/9 tests passed, applied to `system‑graph.json`.  
- Built LoRA feeder (`vault-to-lora-dataset.mjs`) – 15/15 tests passed, live validated with 245 Alpaca triples.  
- Fixed `seed-ghost-from-unwired --apply/revert` streaming I/O; added `writeGraphStreamingAtomic`.  
- Updated `graph‑io.mjs` to expose `readGraphStreaming`, `writeGraphStreaming`, `writeGraphStreamingAtomic`.  
- Added `mine-galaxy-transcripts.mjs` (content‑based discovery, per‑galaxy mining) – 22/22 passes.  
- Added `discoverByContent` registry entry covering all 34 galaxies (2,097 handoffs, 33/34 mineable).  
- Implemented slice cap (`MAX_SLICES_PER_TX=14`) and per‑call timeout override for 120b synthesis (900 s).

**DECISIONS**

- Adopt three windows with one Max account per window to avoid API “thundering herd”.  
- Raise resume size guard to 200 MB.  
- Disable NIM containers; route all local‑LLM traffic to Ollama only.  
- Remove compose `ollama` from autostart to prevent port‑11434 collision with native Ollama.  
- Keep `LOCAL_LLM_BACKEND=ollama` for all slots; disable nim‑autostart.  
- Use advisory routing (node_card, summarization) instead of force‑routing Ollama fleet‑wide.  
- Replace naive JSON.parse on 642 MB graph with streaming I/O to avoid `ERR_STRING_TOO_LONG`.  
- Keep LoRA dataset extraction from vault feedback memories as distinct training signal; do not duplicate DB‑driven builders.  
- Use atomic streaming writes for safety.  
- Transcript‑content discovery over handoff filenames; mine per‑galaxy foreground tasks in ROI order (backend first).  
- One‑galaxy‑per‑task strategy to avoid timeouts on large galaxies like dormant‑data (~288 MB).

**OPERATOR DIRECTIVES**

- Run `capture‑accounts‑runbook.md` to snapshot accounts 2–6 into `H:/.claude-accounts/`.  
- After capturing, double‑click **PRISM Fleet (3‑window)**; verify all 19 slots launch and resume correctly.  
- Reboot PC and confirm no NIM containers restart; Ollama starts via scheduled task; Docker compose does not start a second ollama service.  
- Register missing synthesis crons (`weekly‑memory‑synthesis`, `run‑daily‑context`, `run‑knowledge‑distillation`) via elevated PowerShell.  
- Run elevated PowerShell block to register missing scheduled tasks (synthesis crons, tribal tasks).  
- Re‑run dormant‑data mining task to resume and complete synthesis; schedule remaining light galaxies one per task.

**FINDINGS/BUGS**

- NIM crash loop: exit 137 (OOM) due to 16‑GB WSL cap; fixed by disabling autostart.  
- Dead‑endpoint probe: `LOCAL_LLM_BACKEND=auto` caused latency penalties; resolved by forcing ollama.  
- Compose service name mismatch (`mcp` vs `prism-server`) – added fail‑fast guard.  
- Port collision: native Ollama (11434) + compose ollama – removed compose entry.  
- Duplicate MCP daemons cleaned up; now single healthy daemon.  
- Qdrant embed dimension mismatch risk mitigated: all collections 768‑dim, matching Ollama nomic‑embed.  
- `seed-ghost-from-unwired --apply` crashed due to V8 string cap; fixed with streaming I/O.  
- GNN reference pool starved at 0; now +4 high‑confidence refs added.  
- LoRA extraction leaked CRLF into output (2,260 occurrences); regex fix applied.  
- Missing scheduled‑task registrations blocked by elevation; three tasks missing health check.  
- `classifyHandoff` regex too strict: missed `.archive` suffixes, long‑UUID/Agent@HOST forms.  
- GPU contention: `nomic-embed-text` stalls when 64 GB gpt‑oss:120b resident → embeddings sidecar rebuild fails; need to load before synthesis.

**DOMAIN SPECIFICS**

- Engines / Actions:  
  - `Launch‑PRISM‑Fleet‑3win.ps1`, `slot-tab-boot.ps1`, `nim-autostart.mjs`, `local-llm-bridge.mjs`.  
  - Docker services: `prism-server`, `qdrant`, `ollama`.  
  - GNN feeder (`vault-to-gnn-refpool.mjs`), LoRA feeder (`vault-to-lora-dataset.mjs`).  
  - `seed-ghost-from-unwired.mjs`, `graph‑io.mjs`.  
  - Transcript mining: `mine-galaxy-transcripts.mjs`, `discoverByContent`, `galaxy-synthesis-refresh.mjs`.  
  - Dispatchers: memory‑index‑precheck‑inject, master‑index‑precheck‑inject, wiki‑precheck‑inject, rag‑inject, CAG‑router, h‑to‑c‑obsidian‑mirror.  
- Metrics:  
  - `PRISM_RESUME_MAX_MB=200`.  
  - `KEEP_ALIVE=30m`, `MAX_LOADED_MODELS=4`.  
  - 2,097 handoffs processed; 33/34 galaxies mineable; 22/22 mining passes; 9/9 GNN tests; 15/15 LoRA tests.  
  - Slice cap: `MAX_SLICES_PER_TX=14`; synthesis timeout: 900 s for 120b model.  
- Paths:  
  - `system‑graph.json` (642 MB).  
  - `reference-pool-seed-2026-05-23.json`.  
  - Vault memories at `knowledge/memories/*`.  
  - Wiki at `knowledge/wiki/*.md`.  
  - Memos stored as `reference_<galaxy>_transcript_synthesis.md`.

**TOOLS USED**

- PRISM scripts: `Launch‑PRISM‑Fleet‑3win.ps1`, `slot-tab-boot.ps1`, `nim-autostart.mjs`, `local-llm-bridge.mjs`, `vault-to-gnn-refpool.mjs`, `vault-to-lora-dataset.mjs`, `seed-ghost-from-unwired.mjs`, `graph‑io.mjs`, `mine-galaxy-transcripts.mjs`, `discoverByContent`, `galaxy-synthesis-refresh.mjs`.  
- Docker/compose: `docker-compose.yml`, `docker-intel-autostart.mjs`.  
- Settings & hooks: `settings.json`, scheduled task “PRISM Ollama Serve”.  
- Capture runbook: `capture‑accounts‑runbook.md`.  
- Scheduled task installers: `install-tribal-consolidate-cron.ps1`, `install-wiki-tribal-audit-task.ps1`, `install-brain-refresh-task.ps1`, `install-synthesis-crons.ps1`.  
- Node.js tooling (`fs`, `child_process`).

**OPEN THREADS**

- Register missing synthesis crons via elevated PowerShell.  
- Confirm operator’s decision to commit changes or keep live; verify recall knobs functioning and telemetry collecting.  
- Mine remaining backend galaxies (agent‑orchestration, discovery, dormant‑data, knowledge‑conversion, corpus‑aggregation, database‑expansion) one by one with `--next-unpopulated --backend-only`.  
- Run `galaxy-synthesis-refresh.mjs` on each newly populated galaxy to compound into `<galaxy>/MEMORY.md`.  
- Schedule protected foreground task (or cron) to run `--next-unpopulated --backend-only` until all 34 galaxies memo‑filled.  
- Resolve GPU contention for embeddings sidecar rebuild; ensure `nomic-embed-text` loads before synthesis.  
- Harden reaper settings or use detached session with watchdog to avoid accidental kills.  
- Generalize transcript miner to all 34 galaxies via registry and parameterized miner.  
- Harden `classifyHandoff` regex to capture `.archive`, long‑UUID/Agent@HOST forms, slot‑keyed forms.  
- Finalize LoRA feeder integration with galaxy‑synthesis‑refresh; ensure every galaxy has mineable transcripts.  
- Verify all galaxies covered (33/34; dormant-data correctly empty).  
- Ensure atomic streaming writes for all graph modifications.
