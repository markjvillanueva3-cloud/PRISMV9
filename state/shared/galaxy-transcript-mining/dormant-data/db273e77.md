# dormant-data session db273e77 (2026-06-09, 104.1MB, spine 872KB, 10 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit e5ad4ea802 – U‑ALPHA‑HWSYNC‑RETUNES: 9 source files, audit `state/shared/DOCKER-NIM-AUDIT-2026‑06‑08.md`; added `PRISM_EMBED_CONCURRENCY=16`.  
- Concurrency bump in Vitest (`maxThreads` 8→16) and batch extractors (`os.availableParallelism()`).  
- Added `stop-goal-clear-advance.mjs` hook; updated `loop-state.mjs` with fleet‑fallback, `PRISM_LOOP_MAX_ROLLS`.  
- Dedup helper `dedupedContext()` adopted in 8 injectors (commits 1f295f51e6, eefef0359a, 8d2b6cf8e0, 87e96fa485, afe169ab0e).  
- MEMORY_SEED reader (`2c006fec7c`) now consumes orphaned producer; 51/51 tests.  
- Batch embeddings `embedTextBatch()` (1dd17250b3): 80 memos → 78 vectors in 0.576 s (~3× faster).  
- Ollama‑auto config correction (`2a5f980a5d`).  
- `resolveObsidianMemDir()` resolver added (792beb75e8, e1b95b05a8); removed 9 571 node_* stubs from recall.  
- Backend audit route‑suggest session gate (`a9052f63a0`) saves ~4 k tokens per fire.  
- Galaxy‑brain recall wiring `U-OBS-GALAXY-BRAIN-RECALL + -P2` (34 domains).  
- Vision‑model single‑source refactor (`e80e6e3a41`); added `VISION_FAMILY_LEADERS`.  
- Embed‑progress honesty fix (`89146678bf`) with PID stamp.  
- Dark‑wiki recall ranking: 32 630 files, 71 high‑value selected (commit unnamed).  
- System‑viz streaming degree pass (`graph-stream-degree.mjs`) replaces full graph load to avoid 643 MB OOM.  
- `ollama-nav-enforce-inject.mjs` hook injects ollama bridge for nav intents; pinned bridge model to `gpt‑oss:20b`.  

**DECISIONS**  
- Keep alpha slot bound to `alpha-work`; fleet‑reaper guardian left unwired (ownership moved to golf).  
- Use commit‑mutex (`U-FGC-1`) for pathspec commits.  
- Wire stop‑goal-clear hook across all slots; keep UserPromptSubmit untouched.  
- Adopt single‑source resolver for Obsidian paths, remove split‑brain feeds.  
- Disable redundant rtk.git route (`pre-tool-router-table-advise.mjs`); session‑once gate for `node-no-rtk-wrap`.  
- Prioritize high‑value token savings over marginal work; defer new ultracode discoveries until fresh budget.  
- Split Ollama default: alpha handles config/cleanup, bravo handles engine routing and model matrix.  

**OPERATOR DIRECTIVES**  
- Use ultracode, /hermes-workflow, /system-viz, obsidian app, PSN to find high‑value improvements with new PC specs, local LLMs, current Prism config.  
- Find token savings, context retention/expansion; ensure obsidian fully wired to H drive and enhance vault usage/value.  
- Goal clear: exhaustively maximize local LLM utilization for searches/navigating codebase, no quality loss.  
- Check chatbus; coordinate with bravo on Ollama defaults.  

**FINDINGS/BUGS**  
- Vitest 4 regression (`poolOptions.threads.*` ignored); migrated to top‑level `test.*`.  
- `stop_on_failing_tests.mjs` unwired in settings.json – potential fleet impact if wired.  
- Encoding bug: em‑dash mis‑parsed by PowerShell 5.1; fixed via UTF‑8 invocation.  
- GpuStackHealthEngine operator instructions wrong (cu129 vs cu128).  
- Partial Ollama pulls stalled (~60 GB gpt‑oss, ~18 GB gemma4); each restart re‑downloads layer → must run uninterrupted.  
- `execFileSync("node", …)` ENOENT in hooks; replaced with `process.execPath`.  
- Regex for RESUME_LOOP had `$`+`m`; removed `m`, anchored to EOL.  
- 9 571 node_* stubs inflated recall noise (72 %); removal restores real content.  
- Recall‑counter lost‑update race fixed via `withExclusiveLock`.  
- Backend audit route‑suggest fired identical text on every edit – token waste; session gate added.  
- `extractWikilinks` produced ~15 819 dangling links; removed.  
- System‑viz node‑card path OOM (~458 MB heap); replaced with streaming degree pass.  

**DOMAIN SPECIFICS**  
- Engines/Actions: `loop-state.mjs`, `MINBatchExtractorEngine.ts`, `McxBatchExtractorEngine.ts`, `GpuStackHealthEngine.ts`, `dedupedContext()`, `MEMORY_SEED reader`, `embedTextBatch()`, `resolveObsidianMemDir()`, `ollama-nav-enforce-inject.mjs`, `vision-model-select.mjs`.  
- Dispatchers/Hooks: stop‑goal-clear hook, backend audit route‑suggest, memory‑index‑search‑lib, pre-tool-router-table-advise, knowledge-link-audit, brain-refresh, slot-context-bundle-inject.  
- Paths: `state/shared/DOCKER-NIM-AUDIT-2026‑06‑08.md`, `H:/prism/.claude/settings.json`, `C:/settings.json`, `mcp-server/src/engines/*`, `scripts/ollama-resilient-pull.ps1`, `scripts/handoff-memory-seed.mjs`.  
- Metrics: PRISM_EMBED_CONCURRENCY 16, Vitest maxThreads 16, token savings ~4–12 K per prompt (dedup), context retention window 720 min, 82–95 % token savings via dedup, 176 tribal nodes promoted to vault, 0.576 s for batch embeddings, 1 942 memo backlog recovered, 15.5 % false‑positive link reduction.

**TOOLS USED**  
- PRISM: ultracode workflows (`wgypolzah`, `w3qho9bc3`), hermes‑workflow, system‑viz, obsidian app, PSN, slot-bind-enforce.mjs, chat-slots.mjs.  
- Scripts/Helpers: `resolveObsidianMemDir()`, `withExclusiveLock`, `memory-relevance-inject`, `regen-find-cache.mjs`, `graph-stream-degree.mjs`.  
- External: Ollama CLI (`ollama pull`), PowerShell scripts, Node.js `process.execPath`.

**OPEN THREADS**  
- Owner‑gated tasks: Sierra – integrate `graph-stream-degree` into system‑viz queries; India/Sierra – resolve V8‑cap sharding blocker for dark‑wiki priority embedding.  
- Bravo – implement advisory‑decay gate to suppress low‑value offload nudges.  
- Remaining ultracode discoveries #2–#6 (dedup for other injectors, embed‑stub filter, large‑read gate).  
- Operator‑gated re‑registration of Zombie Reaper v2 and Blueprint OCR Batch.  
- No additional alpha‑lane work pending; all high‑value levers routed to respective owners.
