# dormant-data session 928a8226 (2026-06-09, 63.2MB, spine 634KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Updated `ollama-resilient-pull.ps1` (ASCII hyphens, removed parse error); gpt‑oss:120b fully installed (65 GB).  
- Commit c593b096fb: added DEFAULT_NUM_PREDICT=1024, updated callOllama() with num_predict & truncation diagnostics; unit tests.  
- Commit 13017de764: fixed hook‑ify front‑matter parsing errors, removed dangling hook references.  
- 182788232a: cap‑safe loader for tribal index (buffered read + incremental brace walker).  
- 5467482be4 / c994bf3aad: doc‑reflection of fix across CLAUDE.md, wiki lesson, memory.  
- a3e6d3ca97: fail‑open clobber guard – readIndex throws on truncated; writeIndex refuses >50 % shrink.  
- fb314a6fd1: docker‑service‑health CLI guard (detects down/created/exited containers, auto‑fix via `docker start <name>`).  
- 35e01ca5f3 + d3ed737c23: singleton‑service‑guard (reap‑all + start) wired into Stop hook.  
- 006c435ddb: corrected MCP restart docs (`TRANSPORT=http node dist/index.js`).  
- e2081e0780 / d22681f5d2: upgraded singleton‑guard to reap‑then‑start, updated banner & memory.  
- U‑MCP‑FIXSTART – reap‑then‑start recovery for MCP.  
- U‑MCP‑CMDMATCH‑FIX – slash‑agnostic regex + daemon‑count fix.  
- U‑HOOK‑UNREG‑PROTOCOL‑FIX – JSON‑stdout protocol for `stop_on_hook_unregistration`.  
- U‑MCP‑BOOT‑HEAP‑FLOOR – heap floor ≥4096 MB for all MCP spawn paths.  
- U‑QP‑COST‑SAVINGS‑TEST – 13/13 vitest for CostSavingsTrackerEngine.  
- U‑BW‑DISPATCHER‑SCAN – repointed cascade defaults to installed Blackwell models.  
- U‑BW‑DISPATCHER‑SCAN‑WIKI – wiki lesson on guard blind spots.  
- U‑PSN‑SNAPSHOT‑REFRESH – refreshed stale PSN snapshot; 11 real under‑wired pairs.  
- U‑QP‑DIGITALTWIN‑TEST – added R9 reference‑value tests for DigitalTwinEstimator.  
- U‑QP‑MARGIN‑FLOOR‑GATE – margin‑floor guard (WARN+flag) to QuoteEstimatorEngine.  
- U‑QP‑MARGIN‑FLOOR‑SHOPCONFIG – self‑source margin floor from ShopConfigurationEngine.  
- U‑QP‑NRE‑AMORTIZE‑TEST‑FIX – corrected stale NRE amortization test.  
- ec597dbcb3 – margin‑floor dispatcher round‑trip (7/7).  
- 87d5c4bf9a – margin‑floor frontend banner (6/6, tsc‑clean).  
- 4b0980c56a – AI‑substrate routing‑matrix coverage 4/5→5/5 (27/27).  
- 62b114cb7b – web‑test ResizeObserver polyfill; hidden Recharts failures now green.  
- 8258918f78 – U‑QP‑FRONTEND‑TEST‑REPAIR: fixed quote‑history mock, added null guards, updated assertions (13/13).  
- Commit 4c12a75a8d – U‑QP‑CLOSED‑LOOP‑PROVENANCE‑GATE: synthetic outcomes blocked from writing live calibration factors; 40/40 tests.

**DECISIONS**  
- Encoding fix first to allow resilient pull; scope limited to pull script.  
- Keep existing cost‑router wiring; add DEFAULT_NUM_PREDICT=1024, truncation diagnostics; validate routing after changes.  
- Tribal index loader must use buffered incremental parsing; re‑embed ~29 K entries without sharding; MCP supervisor unchanged; singleton guard reap‑then‑start.  
- Remain in golf lane: defer Qdrant tribal migration; add universal heap floor for MCP spawns; replace hook exit‑code protocol with JSON output.  
- Source‑lock guard must scan dispatcher tree; margin‑floor gate requires caller override, default from ShopConfig, WARN+flag; dispatcher round‑trip test added; frontend expose `pricing.below_margin_floor`.  
- No high‑ROI quoting‑specific Ollama offload identified; retain Claude for reasoning per directive.  
- Continue with charlie domain work; prioritize building QuotingActualOutcomeLoaderEngine after live credentials.

**OPERATOR DIRECTIVES**  
- Verify MCP restart command uses `TRANSPORT=http node dist/index.js`; ensure Qdrant migration runs post re‑embedding; monitor singleton guard output during idle periods.  
- Synergize ollama (ensure correct models pulled for GPU/CPU/NVMe SSD/128 GB RAM), docker, qdrant, obsidian vault app, PSN, /system‑viz, prism galaxies, front‑end & back‑end build, claude.md, memories, wiki, tribal knowledge, prism awareness, gsd, tdd, skills, scripts and hooks – everything wired, tested, validated node‑by‑node, galaxy‑by‑galaxy.  
- Utilize ollama for better task efficiency when viable; continue with charlie domain work.

**FINDINGS / BUGS**  
- Parse error in `ollama-resilient-pull.ps1` caused 12 % stalled download of gpt‑oss:120b (65 GB).  
- `routeModelForTask({category:"search_synthesis",hardware:"blackwell"})` returned gpt‑oss:20b instead of installed gpt‑oss:120b.  
- Empty response bug: reasoning model emits chain‑of‑thought in `thinking`; consumers reading only `json.response` get empty string when token budget exhausted.  
- Missing DEFAULT_NUM_PREDICT caused 128‑token default to be consumed entirely by reasoning model before any response produced.  
- Fallback argument missing in `resolveSynthesisModel()` production calls.  
- Fail‑open clobber guard triggered 1‑entry brain; fixed by guard and restored baseline of 4 162 entries.  
- MCP down due to duplicate wedged daemons; resolved by reaping all and starting one clean daemon.  
- Docker health guard now correctly detects and fixes orphaned containers.  
- cmdMatch regex used backslashes only → missed forward‑slash daemon (`mcp-server/dist/index`).  
- `stop_on_hook_unregistration` emitted no JSON → always “NOT evaluated” in stop‑regression bundle.  
- MCP server boot OOM due to inherited 384 MB heap cap; fixed by enforcing ≥4096 MB on all spawn paths.  
- System‑viz `find` OOM under fleet’s 384 MB shim; self‑bump avoided to prevent double hot‑path spawns.  
- PSN snapshot stale; 11 real under‑wired pairs confirmed.  
- Margin‑floor guard missing in QuoteEstimatorEngine; added.  
- NRE amortization test stale; corrected.  
- QuoteHistory mock returned flat array → runtime crash (`undefined.length`); fixed by aligning to `InstantQuoteHistory`.  
- Missing ResizeObserver polyfill caused hidden Recharts failures; polyfill added.  
- Synthetic data could have promoted calibration factors – blocked by provenance gate (U‑QP‑CLOSED‑LOOP‑PROVENANCE‑GATE).  
- OCR‑$1 noise in `jm‑sold‑orders.json` causes medianRatio drift flagging; documented.  
- Lock contention on `.git/index.lock`; bounded retry with pathspec scoping implemented.

**DOMAIN SPECIFICS**  
- Models: gpt‑oss:120b, gpt‑oss:20b, qwen2.5-coder:32b, vision ensemble (qwen3-vl, qwen2.5vl, llama3.2-vision, moondream).  
- Scripts/hooks: `ollama-resilient-pull.ps1`, `octopus-setup.mjs`, `acquire-models` skill, `ollama-cost-router.mjs` (`routeModelForTask`), `callOllama()`, `ask‑ollama.mjs`, `resolveSynthesisModel()`.  
- Tribal index loader: `scripts/lib/load-tribal-index.mjs`, `scripts/embed-wiki-into-tribal-index.mjs`; guard scripts: `tribal-embed-index.mjs`, `docker-service-health-check.mjs`, `singleton-service-guard.mjs`.  
- MCP restart command: `TRANSPORT=http node dist/index.js`; singleton‑service‑guard, mcp-server-supervisor, mcp-server-daemon.  
- Qdrant collections: `prism_skills`, `prism_engines`, `prism_formulas`; tribal tips injected via `populate-tribal-vault.mjs`.  
- System‑viz: `findInGraph` script; V8 string cap 512 MB.  
- PSN snapshot refresh path: `/psn/snapshot/refresh`.  
- Engines/actions: QuoteEstimatorEngine, CostSavingsTrackerEngine, DigitalTwinEstimator, QuotingClosedLoopEngine, OutboundPriceIndexEngine, ActualCostEngine, ClosedLoopRunnerEngine, QuoteAutopilotEngine; dispatcher `businessDispatcher`; actions `quote_estimate`, `quote_generate`.  
- Metrics: token generation rate, response length, below_margin_floor flag, margin_floor_pct, confidence_score, MAPE, drift tolerance.  
- API paths: `/api/v1/quote/estimate`, `/api/tags`, `/psn/snapshot`.

**TOOLS USED**  
- PowerShell (`ollama-resilient-pull.ps1`).  
- Node.js scripts (`node H:/prism/scripts/fleet-reaper-sweep.mjs`, `node H:/prism/.claude/helpers/chat-slots.mjs`).  
- Ollama API (`ollama list`, `ollama serve`, `/api/tags`).  
- PRISM tooling: source‑lock guard, catalog install‑gate, dispatcher scan, wiki lesson generator, PSN snapshot refresh.  
- Test framework: vitest (engine & frontend), node:test.  
- Docker CLI (`docker start`, `docker ps`, `docker inspect qdrant`).  
- Custom scripts: `scripts/lib/load-tribal-index.mjs`, `scripts/embed-wiki-into-tribal-index.mjs`, `scripts/docker-service-health-check.mjs`, `scripts/singleton-service-guard.mjs`.  
- Frontend build tools: npm run build:frontend, build:backend.  
- Workflow orchestrator: ultracode (multi‑agent orchestration), local Ollama for grunt tasks.

**OPEN THREADS**  
- Verify routing logic after DEFAULT_NUM_PREDICT and cost‑router changes; validate GPU smoke test for gpt‑oss:120b.  
- Persist pull resilience: add pre‑commit hook to flag non‑ASCII in `.ps1`.  
- Document synergy change (memory entry reference_gptoss_synergy_fix_2026_06_09.md).  
- Verify all consumers handle harmony format (`thinking` vs `response`); ensure new defaults covered; monitor downstream impact.  
- Re‑embed remaining ~29 K tribal entries; implement write‑side sharding when index >512 MiB.  
- Qdrant tribal migration (deferred pending operator confirmation).  
- Final validation of MCP persistent supervisor (watch singleton guard output during idle periods).  
- System‑viz find‑heap self‑bump avoided; monitor for OOMs under fleet shim.  
- U‑QP‑ACCOUNTING‑WIRE closed‑loop accounting wire pending; rate‑limit and budget constraints prevent immediate start.  
- Remaining quoting frontend failures in hotel domain (analytics/scan) not addressed.  
- Fleet‑scope synergy goal still open; dispatch to other slots via created dispatch map.  
- Resume closed‑loop learning unit when full budget available.  
- Implement QuotingActualOutcomeLoaderEngine once live ERP credentials are provisioned.  
- Wire outbound alignment gate into runCycle’s promote logic (post‑provenance gate).  
- Clean OCR‑$1 noise in reference data or adjust drift thresholds for reliable gating.  
- Coordinate fleet‑wide synergy dispatch via zebra/golf to satisfy 20+ slot goal.
