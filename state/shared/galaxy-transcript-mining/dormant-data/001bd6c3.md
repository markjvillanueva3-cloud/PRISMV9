# dormant-data session 001bd6c3 (2026-06-09, 53.5MB, spine 485KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hermes‑Obsidian integration & `/system-viz` roost (ab2ccf42a4/e6713584e2).  
- PRISM MCP restart logic (c988a21ec4).  
- Hermes configured to Claude Code OAuth (`claude-opus-4-8`) with local Qwen fallback (56aa423427).  
- Docker Desktop started; Qdrant container `prism-qdrant` up on :6333.  
- Hermes config migrated v0→v28; provider strings corrected: `model.default → custom/qwen2.5-coder:32b@11434/v1`, later switched to `gpt‑oss:20b`; `fallback_model → anthropic/claude-opus-4‑8`.  
- Hermes headless CLI (`hermes -z`) operational.  
- Commit 28b72e4dee: MEMORY.md updated to 4 sections; added `scripts/fill-galaxy-memory-sections.mjs` & test suite; vault mirror via `syncGalaxyMemories()`.  
- U5b, U5c, U6, U7, U9, U10 commits (ollama-tool-agent, offload-enforce, codegen helper, build-loop gate, coresidency mutex, capability probe).  
- U9 – VRAM co‑residency & mutex commit 7771ca7f86.  
- U1 – offload router roster, tier-aware selection commit 2340a2e699.  
- U2 – route-hook default + config commit a2756779c2.  
- U3 – honest take-rate banner commit a60d7ba0bf.  
- U4 – LLM compressor for RTK residue commit 612418fde7.  
- U‑OAB‑120B – fleet-wide default coding model = gpt‑oss:120b commit 3904b5e7b0.

**DECISIONS**  
- Hermes primary → Claude Code OAuth; fallback → local Ollama `qwen2.5-coder:32b` (429 handling).  
- Revert supervisor cwd pin; use launch env PRISM_ROOT.  
- Qdrant optional; enable only when Docker available and billing configured for Claude Code.  
- Use `gpt‑oss:20b` as local primary model; keep Claude fallback.  
- Disable GUI launcher; run Hermes headless.  
- Routing engine: existing `OllamaTaskOffloaderEngine.ts`; no new router.  
- Model selection: `gpt‑oss:120b` tool‑calling, `qwen2.5-coder:32b` codegen, `gpt‑oss:20b` fallback, `nomic‑embed‑text` embeddings.  
- KV-cache: CTX=16384, MAX_LOADED=3; pin 120b+8b+nomic; keep 32b on‑demand.  
- Auto-offload enforcement PRISM_OLLAMA_OFFLOAD_ENFORCE=1.  
- Go-live merge: cherry-pick six commits onto `cad-fusion-live-ms0`; not main.

**OPERATOR DIRECTIVES**  
- Verify Docker Desktop running, Qdrant reachable (`curl http://localhost:6333`).  
- Restart Hermes after config changes (`hermes.exe --restart` or `hermes -z "Check in as zulu"`).  
- Monitor memory pressure; avoid heavy subprocesses until MCP healthy.  
- Commit only to slot/bravo worktree; no main changes until go-live.  
- Use ultracode workflows for planning/verification; do not poll loop manually.  
- After checkpoint, proceed with RTK upgrade sweep (#11) and plan go‑live merge.

**FINDINGS / BUGS**  
- Qdrant offline (Docker missing).  
- Hermes messages fail HTTP 400 from Anthropic → requires extra‑usage billing at claude.ai/settings/usage.  
- Unknown provider `openai` fixed by using custom.  
- GUI launcher stalled on dirty package-lock.json; cleared.  
- gpt‑oss 20b mis‑parses reasoning field → “no final response”.  
- Vault mirror missing 12 of 34 galaxy dirs; only 22 present, 11 empty.  
- Memory count inflated (auto-generated node_* files counted as real).  
- Corrupt `ai-training_synthesis.md` (all‑NUL) flagged.  
- qwen2.5-coder:32b lacks structured tool_calls; only gpt‑oss supports native tool‑calling.  
- VRAM/KV-cache overestimation corrected to 37 GB resident for 32b at CTX=16K.  
- `git grep` needed -E for regex with parentheses; otherwise agent mis‑parsed patterns.  
- Probe script entry guard failed on relative paths; fixed by absolute path or robust check.  
- Nomic‑embed‑text tag matched :latest; corrected to strip suffix.

**DOMAIN SPECIFICS**  
- Hermes: desktop app, OAuth to Claude Code, local Ollama fallback; endpoints `/api/status`, `/api/chat`; local Ollama `http://127.0.0.1:11434/v1`.  
- Obsidian REST API on :27123; sync lock `knowledge/memories/**`.  
- PRISM MCP JSON‑RPC dispatchers at :3100 (`prism_memory`, `prism_session`, `prism_knowledge`).  
- Qdrant collections `prism_engines`, `prism_formulas`, `prism_skills`; healthy on :6333.  
- Slot coordination: `slot_brief_write → state/shared/slot-briefs/<slot>.md`; slot‑brief‑inject hook.  
- Galaxy structure: `engines/<galaxy>/MEMORY.md`, `PATHS.md`, `TOOLBELT.md`, `CLAUDE.md`.  
- Models: gpt‑oss 20b/120b, qwen2.5-coder 32b, qwen3-vl 8b-instruct, nomic‑embed‑text; Ollama local provider.  
- Zulu orchestrator (`zulu-orchestrator-sweep.mjs`) sends `/checkin-<slot>` via SendKeys; uses `chat-slots.json`.  
- Metrics: token usage per model, MCP :3100 health, Obsidian REST status, VRAM usage per model, latency (~30 s tool‑calling loop).

**TOOLS USED**  
- PRISM helpers: `/checkin-bravo`, `chat-slots.mjs`, `audit-roadmap-drift.mjs`.  
- Hermes config editor (`config.yaml`).  
- Docker Desktop / docker-compose; Qdrant image `qdrant/qdrant:v1.17.0`.  
- Claude Code OAuth credentials `~/.claude/.credentials.json`.  
- Node scripts: `scripts/hermes-obsidian-app-map.mjs`, `scripts/fill-galaxy-memory-sections.mjs`, `galaxy-synthesis-refresh.mjs`, `obsidian-memory-sync.mjs`/`syncGalaxyMemories()`, `zulu-orchestrator-sweep.mjs`, `ollama-prism-bridge.mjs`.  
- Ultracode workflows (`wbmnnd902`, `w2hu6dowo`).  
- Git hooks: slot‑commit‑enforce, chat‑slots reclaim/claim.  
- Node test harness (`node --test`) and Vitest.

**OPEN THREADS**  
- Spin up Docker, run Qdrant, configure vector store.  
- Enable extra‑usage billing on Claude Code or switch Hermes to local-only mode.  
- Verify Hermes messaging after billing fix & Qdrant availability.  
- Re‑validate slot coordination end‑to‑end once Hermes operational.  
- Restart :3100 MCP daemon after Qdrant start; confirm Hermes picks up config changes (`/api/status`).  
- Decide on extra‑usage billing vs local fallback.  
- Finalize PowerShell zulu script & scheduled task for fleet orchestration.  
- Re‑enable PRISM Hermes‑Obsidian Bridge scheduled task (elevated shell).  
- Regenerate corrupt `ai-training_synthesis.md` via local Ollama.  
- Schedule regular `syncGalaxyMemories()` to prevent drift.  
- Patch gpt‑oss reasoning channel handling in Hermes custom provider.  
- Verify all 34 galaxies at 4/4 after updates.  
- RTK upgrade sweep (#11) – LLM‑based summarization extension.  
- Go-live merge (#14) – slot/bravo → main, wire `ollama-offload-enforce` into `settings.json`.  
- U1 / U3 config edits pending post go-live to avoid stale-tree issues.
