# dormant-data session b5de5424 (2026-06-09, 21.1MB, spine 141KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `0b7fea59` – SYNERGY matrix spec (`GALAXY‑SYNERGY‑MATRIX‑2026‑06‑09.md`) + HTML twin.  
- `6d3222f2`, `030c70c9` – first synergy docs (Romeo↔GNN, India+Xray).  
- `d475e1a3` – initial octopus rows for wedm & speed‑feed (bloat later fixed).  
- `6e1fd0ab46` – final octopus rows for cam, cad, post‑proc.  
- `c422543813` – 8 galaxy synthesis refreshes: quoting, shop‑floor, speed‑feed, system‑viz, token‑optimization, tribal‑knowledge, wedm, wiring.  
- `77e1546048`, `5265e09ae0`, `c5c4a66a9d`, `5c64915525` – algorithm‑primitive wiring into 13 galaxies.  
- `8a52eeb0f5` – soul realignments for golf, romeo, oscar, papa.  
- **F1** – Tribal‑shard‑aware writer (`writeTribalIndex`, `loadShardedIndex`) – 24 tests, 3/3 scrutiny PASS, validated against 159.9 MB index.  
- **I2** – Opportunistic sidecar‑freshness at Stop – 15 tests, 2‑reviewer PASS, P2 hardening (corrupt lock reclaim).  
- **RAM‑gate** – Master‑index rebuild protection – 17 tests, 3/3 scrutiny PASS; prevents >96 % commit pressure heap spawns.  
- `U-CANONICAL-HOST-FACTS.md` – single source of truth for PC specs and Ollama roster.

**DECISIONS**  
- Use inline processing (no agent fan‑out) to avoid Anthropic rate limits.  
- Commit only targeted files with reset‑first discipline; avoid pathspec commits that absorb peer churn.  
- Identify & retract false‑positive batches (B3 & B4).  
- Diagnose brain‑refresh task unregistration as root cause of stale syntheses; hand off elevation requirement to golf.  
- Prioritise R0: register `PRISM Brain Refresh` (elevated shell) – highest ROI.  
- Next, I2 opportunistic sidecar freshness and RAM‑gate hardening for master‑index rebuild.  
- Capture all new facts in canonical doc then propagate per‑galaxy via chat bus to avoid drift.

**OPERATOR DIRECTIVES**  
- Coordinate with Bravo – completed.  
- Continue deep synergizing galaxies; fill exhaustively with domain data – ongoing.  
- Use ultracode, system‑viz, obsidian, docker, ollama (`qwen2.5-coder`, `gpt‑oss:120b`) for grunt work.  
- Push through compaction survival via precompact handoff & auto‑resume.  
- “Take ram gate; when finished begin updating all documents to be more recent, get rid of all drift.”  
- Update everything on new PC specs and Ollama LLMs; track system‑viz upgrades and propagate to memories, wikis, settings, CLI configs, claude.md across all galaxies.  
- Update skills, scripts, hooks container skills, and ALL slash commands/pipelines relative to updated features and PC specs.  
- Make chat slot responsible for all fleet‑wide, galaxy‑wide updates; utilize ollama when viable.

**FINDINGS/BUGS**  
- Anthropic server rate limiting killed workflow fan‑outs → switched to inline.  
- Shared‑tree commit bloat caused by pathspec‑less `git commit` with stale index; resolved with reset‑first + plain commit.  
- Peer “all” synthesis run left 27 orphan pattern files; isolated own 8 via reset‑first.  
- Brain‑refresh task not registered on host → stale syntheses; requires elevated PowerShell (`install-brain-refresh-task.ps1`).  
- PC spec drift claim false: physical RAM is 127 GB (commit limit 227 GB).  
- Ollama default `qwen2.5-coder:7b` not installed – actual defaults are `32b` and `1.5b`.  
- Corrupt lock wedge in master‑index rebuild lock caused permanent stalls; fixed with mtime fallback.  
- Orphan git lock detected during I2 commit; cleared safely.  
- RAM‑gate bug: missing free‑RAM probe caused unnecessary spawns under high pressure; added `freeRamProbe` and tests.

**DOMAIN SPECIFICS**  
- **Galaxies (named slots)**: Alpha, Bravo, …, Zulu – each owns `CLAUDE.md`, `MEMORY.md`, `TOOLBELT.md`.  
- **Engines/Actions**:  
  - `galaxy-synthesis-refresh.mjs` (content‑stale detection & Ollama distillation)  
  - `brain-refresh.mjs` (orchestrator, stage 6 = galaxy‑synth)  
  - `TribalShardWriter`, `SidecarFreshnessHook`, `BrainRefreshTask`  
- **Dispatchers**:  
  - Chat‑slot claim (`slot-bind-enforce.mjs`, `chat-slots.mjs`)  
  - `/startup` pipeline, `/compact` bus, `slot-commit-enforce` hook.  
- **Metrics/Paths**:  
  - `sourceHash` for synthesis freshness; PSN leg #8 for algorithm‑primitive awareness.  
  - `GALAXY-COMPLETENESS-AUDIT.json`, `state/shared/specs/BRAIN-ACCELERATION-OPPORTUNITIES.md`.  

**TOOLS USED**  
- ultracode workflows, system‑viz, obsidian, docker, ollama (`qwen2.5-coder:32b`, `gpt‑oss:120b`).  
- Scripts: `chat-slots.mjs`, `slot-bind-enforce.mjs`, `galaxy-synthesis-refresh.mjs`, `brain-refresh.mjs`, `install-brain-refresh-task.ps1`, `writeTribalIndex.mjs`, `loadShardedIndex.mjs`.  
- PRISM CLI (`/startup-papa`, `/compact`), chat‑bus (`AGENT_CHAT.jsonl`).  
- Test harnesses: unit tests (12/12, 15/15, 24/24), integration smoke.  
- Workflow engine IDs: `wf_07062fe6-3e9` (5‑agent audit), `wf_108e7c01-3e9` (RAM‑gate build).

**OPEN THREADS**  
1. Register `PRISM Brain Refresh` task on host (requires elevated PowerShell).  
2. Fix Ollama default drift (`qwen2.5-coder:7b → 32b/1.5b`) across all engines/docs.  
3. Propagate system‑viz upgrades to memories, wikis, settings, CLI configs, claude.md in all galaxies.  
4. Full fleet‑wide update campaign (memories, wikis, GSD, skills, scripts, hooks, slash commands, pipelines) orchestrated via canonical host‑facts doc and routed per galaxy.  
5. Monitor future memory additions; trigger synthesis refresh automatically via existing `brain-refresh` stage.  
6. Verify remaining non‑named galaxies (`dormant-data`, `shop-floor`, etc.) if scope expands.
