# bug-hunting session 14c8b81e (2026-06-09, 5.4MB, spine 74KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit 316872f7: `scripts/call‑engine.mjs`, `.cmd/.sh` wrappers, and `call‑engine.test.mjs`. 8‑case node:test suite passes (8/8).  
- Commit 38af3407: `27-wsl-memory-guard.mjs`, its test (`27-wsl-memory-guard.test.mjs`) and PowerShell installer (`install-wsl-memory-guard-task.ps1`). 15/15 pure‑logic tests pass; CLI reports OVERRUN correctly.

**DECISIONS**  
- *Server‑free engine invoker*: dispatcher routers are thin Zod→engine.method calls; we can import the plain `src/engines/*.ts` singleton directly via `tsx`.  
- *WSL memory guard*: read `.wslconfig` for real cap, query `vmmemWSL` via PowerShell, report overrun, advise or enforce with `wsl --shutdown`.  Guard runs only in CLI mode; pure helpers exported for unit tests.  
- *Test strategy*: black‑box spawn of the harness (no shell:true on Windows) to avoid JSON mangling; pure logic tests cover config parsing and threshold classification.  
- *Deployment*: scheduled task installed by PowerShell script, 15‑min interval, advise mode only; `--enforce` flag gated for manual use.

**OPERATOR DIRECTIVES**  
- Run `wsl --shutdown` now to reclaim ~80 GB (removes the current overrun).  
- Install watchdog: `powershell -NoProfile -ExecutionPolicy Bypass -File H:/prism/.claude/helpers/install-wsl-memory-guard-task.ps1 -RunNow`.  
- After those, run `/compact` to finish the session.  

**FINDINGS/BUGS**  
- API‑limit errors were caused by host memory pressure: `vmmemWSL` committed ~96 GB against a 16 GB cap (cap not applied).  
- Ollama models (~65 GB resident) also contributed; unloading idle models reduced commit from 98.4 % to 68.5 %.  
- All 107 dispatchers are thin wrappers; only ~5 clusters truly need the MCP server (semantic memory, live machine, WebSocket push).  
- `call‑engine.mjs` works without the MCP server; dispatcher functionality is fully replicated.

**DOMAIN SPECIFICS**  
- Engines: `src/engines/*.ts` export camelCase singletons or named functions.  
- Dispatchers: `mcp-server/src/tools/dispatchers/*` validate with Zod then call `engine.method`.  
- Call‑engine harness uses `tsx` to import engine modules directly; supports `--list`, `--methods`, positional JSON args, fail‑loud exit codes 2–5.  
- WSL guard reads `.wslconfig` (memory=XXGB), queries `vmmemWSL` via PowerShell (`Get-WmiObject -Class Win32_Process | Where-Object {$_.Name -eq 'vmmemWSL'}`), compares commit to cap, outputs JSON or quiet text.  
- Scheduled task installer uses standard Windows Task Scheduler API via PowerShell; triggers every 15 min.

**TOOLS USED**  
- `tsx` (Node.js TypeScript runner) for harness and guard.  
- `node:test` for unit tests.  
- PowerShell scripts (`install-wsl-memory-guard-task.ps1`, `02-kill-zombie-tsservers.ps1`).  
- `wsl --shutdown`.  
- `ollama stop <model>` (via API or CLI).  
- Git hooks: `per-agent-handoff.mjs`, `precompact-pending-guard.mjs`, `milestone-tracker.mjs`.  

**OPEN THREADS**  
- Execute the two elevated‑shell commands (`wsl --shutdown` and watchdog install).  
- Verify scheduled task registers correctly; optionally add guard to fleet‑task‑health list.  
- Integrate `call-engine.mjs` into a `/call-engine` skill or Prism session action for discoverability.  
- After those, run `/compact` to finalize the session.
