# bug-hunting session 19dff632 (2026-06-09, 12.6MB, spine 104KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e44a3a1592` – wired 4 orphaned PayrollLiabilityFilingEngine methods (`compute940`, `generateW2`, `reconcileW2_941`, `contractor1099_totals`) into `businessDispatcher`.  
- `e649790e76` – added fifth orphan `remit_liability`; updated tests.  
- `f069052772` – committed “verify‑shipped” unit (`U-HOTEL-P0-VERIFY-SHIPPED`).  
- `9dfd621910` – added reusable Ollama transcript miner script (`scripts/mine-hotel-transcripts.mjs`).  
- `61518eb988` – fixed/re‑verified miner noise‑filter bugs.  
- `099e6b92bd` – finalized hotel forge roadmap spec (`state/shared/specs/HOTEL-FORGE-ROADMAP-2026-06-09.md`).  
- `6b9ed8520d` – built and committed FALSE‑WIRE‑REGRESSION‑GUARD; all 17 allowlisted actions now pass (20/20).  

**DECISIONS**  
- Use Ollama for bulk summarization/token‑economy tasks; ultracode workflow for high‑level reasoning.  
- Default coder model: `qwen2.5-coder:32b` (code‑tuned, 20 GB).  
- Do not rebuild shipped code; focus on missing wiring and pending units.  
- Next build target: `U-HOTEL-PORTAL-PERSISTENCE`.  
- Persist `CustomerPortalEngine` maps next; decide between `/compact` first or immediate build.

**OPERATOR DIRECTIVES**  
- `/checkin-hotel` → recover hotel context.  
- `"continue"` / `"start"` → execute roadmap, beginning with verify unit.  
- Use Ollama for mechanical work; ultracode for brainstorming/synthesis.  
- Resume rate‑limited workflow after account switch.  
- Promote `qwen2.5-coder:32b` into cost‑router’s code‑reasoning path.  
- Choose persistence strategy: run `/compact` then start `U-HOTEL-PORTAL-PERSISTENCE`, or push through immediately.

**FINDINGS/BUGS**  
- PayrollLiabilityFilingEngine wired all 5 orphaned methods; MobileVoice remains orphaned, tagged `WIRE‑EXEMPT`.  
- Stale audit claim “missing tax engine” false.  
- Q2S DFM bug (`ctx.features` non‑iterable) filed to Charlie.  
- Allowlist regression guard flagged two potential false wires: `marketplace_lead_get` null & placeholder regex mismatch; both fixed (regex broadened, `{note:"method not callable"}` added).  
- Marketplace lead probe used wrong param name (`{id}` → `supplierId`); signature clarified and lead seeded to trigger guard.

**DOMAIN SPECIFICS**  
- Slot: **hotel** – ERP/HR domain.  
- Engines/actions:  
  - `PayrollLiabilityFilingEngine`: `compute940`, `generateW2`, `reconcileW2_941`, `contractor1099_totals`, `remit_liability`.  
  - `CustomerPortalEngine`.  
  - `MarketplaceSeedingEngine`.  
  - Mobile engines (`MobileVoice`).  
- Dispatcher: `mcp-server/src/tools/dispatchers/businessDispatcher.ts` (879 actions, 17 read‑only allowlisted via `BUSINESS_DISPATCH_ALLOWLIST`).  
- Seed API: `seedFromHints` creates leads keyed by `supplierId`.  
- Default case string: `result = { error: \`Unknown business action: ${action}\` }` (line 7640‑1).  
- Paths: `scripts/mine-hotel-transcripts.mjs`, `slot-bind-enforce.mjs`, `chat-slots.mjs`, `businessDispatcher.test.ts`.

**TOOLS USED**  
- Ollama (`ask-ollama.mjs`) for transcript mining, summarization, cost‑routing.  
- Ultracode workflow (8‑agent fan‑out) for roadmap synthesis.  
- PRISM guard test (`businessDispatcher.false-wire-regression-guard.test.ts`).  
- Vitest (`isolate:true`).  
- 3‑of‑3 scrutiny gate (Codex + Claude reviewers; reviewers A, B, C).  
- Git hooks: `slot-commit-enforce`, `bootstrap-slot-enforce`.  
- Node scripts: `chat-slots.mjs`, `reclaim`, `claim`.

**OPEN THREADS**  
1. Persist `CustomerPortalEngine` in‑memory maps to SQLite‑WAL (`U-HOTEL-PORTAL-PERSISTENCE`); decide on `/compact` first or immediate build.  
2. Finalize allowlist regression guard (resolve detected false wires).  
3. Verify remaining orphaned mobile engines beyond `MobileVoice`.  
4. Confirm operator approval for `qwen2.5-coder:32b` coder tier.  
5. Continue with pending units per roadmap after persistence build.
