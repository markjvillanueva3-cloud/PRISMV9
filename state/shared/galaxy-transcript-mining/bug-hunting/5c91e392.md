# bug-hunting session 5c91e392 (2026-06-09, 16.2MB, spine 150KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `b76b4d55ab` – resumable corpus loop with `processed-cursor.jsonl`.  
- `01ce06d589` – multi‑page OCR support, manifest builder, trainset dedup.  
- `5a8dbfefdd` – CLAUDE.md regression entry, wiki lesson, MEMORY.md pointer.  
- `cdc9ec44b3` – PowerShell launcher + Task Scheduler installer for unattended closed‑loop training.  
- `250ffb8a6e` – true‑test harness, GT extractor fixes, resolver bug patch, 91‑part report (`find-perfect-parts.mjs`).  

**DECISIONS**  
- Use resumable corpus loop keyed by `processed-cursor.jsonl`; guarantees no re‑OCR after a kill (R12).  
- Rasterize all PDF pages (`pdf-to-png.py --count`) – fixes 96 % multi‑page “page‑0‑only” bug.  
- Ensemble limited to two reliable VLMs: `qwen3-vl:8b-instruct`, `qwen2.5vl:7b`.  
- Deploy closed‑loop training runner as reaper‑immune scheduled task (Task Scheduler → SYSTEM).  
- GPU+CPU environment fully unblocked (`torch 2.11+cu128`, qlora ready, 32‑core CPU rasterizer).  
- Adopt a *3‑of‑3 reviewer* stop gate for critical changes; fallback to per‑file 2‑reviewer gate for routine edits.  
- Use CNC program text (feed‑move only) as ground truth; add binary guard and feed‑move aware extraction to eliminate false GT points.  
- Replace `calloutDimsIn` denominator with a *callout‑class* metric (distinct diameters + envelope).  

**OPERATOR DIRECTIVES**  
- Run the true‑test on the 91 “perfect” parts before scaling to the full corpus.  
- Produce a verdict report summarizing OCR recall, precision, and identified bugs.  
- Commit all changes (`250ffb8a6e`) and run per‑file scrutiny on new files.  

**FINDINGS / BUGS**  
- **Page‑0‑only OCR bug** – 96 % of prints were multi‑page; only the first page processed.  
- **Reaper kill scenario** – foreground corpus run exited with `255`; cursor survived, proving resumability.  
- **GPU contention** – under heavy fleet load one VLM timed out per print, reducing corroboration to 1/2.  
- **Worklist comment line bug** – lines starting with `#` were treated as missing prints; now skipped.  
- **Duplicate trainset rows on replay** – fixed by keying rows (`print#page`) and last‑wins dedup in `xray-trainset-to-lora.mjs`.  
- **Real‑scan OCR reads only 1–3 dimensions per print; recall = 0 vs CNC GT.**  
- **Binary `.mcx‑8` programs misparsed, inflating GT counts – fixed with `isParsableNcText`.**  
- **Feed‑move aware extraction required to avoid rapid/rapids and roughing ramp inflation.**  
- **Print resolver bug** – filenames containing `/` or `:` truncated; resolved by sanitizing before lookup.  
- **Axis classifier misidentified `.hnc` mill programs as lathe – corrected.**  

**DOMAIN SPECIFICS**  
- `/checkin-xray` wrapper → slot‑binding, drift check, BUILD_STATE inject.  
- `chat-slots.mjs`, `slot-bind-enforce.mjs` helpers for reclaim/claim.  
- `batch-ollama-vision-extract.mjs` – raw OCR extractor (overnight task).  
- `blueprint-ocr-training-loop.mjs` – resumable closed‑loop training runner.  
- `pdf-to-png.py` – PDF rasterizer (`--count`, `--page`).  
- `vision-model-select.js` – selects reliable VLMs; runs `runEnsembleOverImage`.  
- PowerShell scripts: `run-ocr-batch-overnight.ps1`, `install-blueprint-ocr-batch-task.ps1`.  
- Blueprint‑Program Join v6 (`blueprint-program-join-full-v6.jsonl`) – 76 205 part numbers.  
- Prints in `H:/PRISM/Docustrata/_organized/SCANS`; CAD `.ipt`, `.stp` in `H:/PRISM/JM DIE/...`.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
- Checkin pipeline (`checkin.md`).  
- GPU health gate (`gpu_health.py`).  
- OCR training loop (`blueprint-ocr-training-loop.mjs`).  
- PDF rasterizer (`pdf-to-png.py`).  
- Vision ensemble runner (`vision-model-select.js`, `runEnsembleOverImage`).  
- PowerShell Task Scheduler scripts.  
- PRISM `scrutiny` script (3‑of‑3 reviewers).  
- Per‑file 2‑reviewer gate (`ARM A/B/C`).  
- `find-perfect-parts.mjs` query harness.  
- Test suite (`jest`) for GT extractor and runner.  
- Resolver utilities (`findOne`, filename sanitization).  
- CNC program parser (`extractProgramGT`, `classifyProgramAxis`).  

**OPEN THREADS**  
1. **Domain‑shift calibration gate (STEP 4)** – build comparison harness; operator must supply real‑scan gold to validate synthetic trust function.  
2. **LoRA fine‑tune (STEP 6)** – run full GPU fine‑tune on overnight corpus trainset, verify checkpoint creation.  
3. **Operator verification of active‑learning queue** – review silver labels from first prints before scaling.  
4. **GPU contention mitigation** – schedule training for quieter windows or throttle VLM concurrency to avoid timeouts.  
5. **Ensemble expansion** – monitor availability of additional VLMs (e.g., larger Qwen‑VL, InternVL) and integrate when ready.  
6. **Finalize honest verdict report** – include OCR recall, precision, bug list; commit all changes to branch.  
7. **Verify full corpus OCR launch only after real‑scan recall improves**.
