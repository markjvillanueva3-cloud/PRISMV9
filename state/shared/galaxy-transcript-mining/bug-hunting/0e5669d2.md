# bug-hunting session 0e5669d2 (2026-06-09, 38.4MB, spine 318KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `/checkin-sierra` wrapper executed → slot `sierra` bound to chat‑id `claude-0e5669d2`.  
- Ultraflow audit completed; cheap‑token read path ~136 tokens (vs 186k).  
- Node‑access map persisted in wiki, system‑viz/MEMORY.md, two C: memories, handoff file.  
- P0 bug **U‑VAULT‑RAG‑WIRE** wired keyword recall (`memory-rag-inject`).  
- Commits landed:  
  - `168c202646` (sync‑resilient)  
  - `ea21008f25` (index‑meta)  
  - `8c4dff660a` (maint‑cron installers)  
  - `984313825e` (link‑heal scorer – decay leak fixed)  
  - `838123429b` (tribal‑coverage refresh)  
  - `45225e0cc3` (fix‑memory artifact)  
- Hardened scorer, updated tests, regenerated candidates (`d948b85a74`).  
- Hook heap cap set to 384 MB (`portable-node` & `.cmd`).  
- MCP maxConnections raised to 512 (`mcp-server/src/index.ts`).  
- Added `TRANSIENT_PRESSURE_CODES`, `isTransientPressureCode` (`fleet-task-health-watch.mjs`).  
- GPU offload patterns widened in `OllamaTaskOffloaderEngine`.  
- `EXPECTED_UNREGISTERED_TASKS` added, missing‑task logic partitioned.  
- U‑VIZ‑WIKI‑NARRATIVE (`cd54edb940`) flag‑gated Ollama blurbs.  
- U‑OLLAMA‑PIPELINE‑INJECTOR‑WIRE (`7ec4a5ea02`).  
- U‑OLLAMA‑PREWARM‑WIRE (`65a29220e5`).  
- U‑TRIBAL‑QA‑LLM‑UNBLOCK (`6fb278a2ee`).

**DECISIONS**  
- Use `/checkin-sierra` wrapper for handoff & drift checks before canonical `/checkin`.  
- Adopt ultraflow audit workflow (6 agents) for verified context recovery.  
- Persist all audit artifacts in sierra’s domain surfaces; code changes only to shared repo.  
- Resolve peer‑claim & lane‑guard conflicts via kill switches (`PRISM_GIT_ADD_LANE_DISABLE`, `PRISM_MAINTREE_WRITE_BLOCK_DISABLE`).  
- Hook heap cap 384 MB → allows 133 node procs, reduces commit charge from ~92 % to ~52 % after `wsl --shutdown`.  
- MCP maxConnections 512 for 26 slots + spawned agents.  
- Treat transient pressure codes as non‑failing to stop false alarms.  
- GPU offload coverage target ~30 %; broaden `OFFLOADABLE_PATTERNS` (lint, classify, diff‑summary, triage, extract, rename).  
- Suppress missing‑task banner noise via `EXPECTED_UNREGISTERED_TASKS`; intentionally unregistered vault crons become informational.  
- Keep migration freeze ON; defer MCP restart & vault cron registration until lift.  
- Reject widening router premise; focus on existing offload wins.

**OPERATOR DIRECTIVES**  
- “Fill all gaps” – complete remaining B/P1–C/P3 ladder.  
- Persist recovered context for sierra’s domain (already done).  
- Validate commit‑cap fix, tidy missing‑task banner noise, write handoff, recommend `/compact`.  
- Arm deferred items: restart MCP daemon for maxConnections 512, register vault crons if migration freeze lifted.  
- Continue with system viz & obsidian app synergy; keep mapping nodes for quick searchs/tool usage savings.  
- Utilize Ollama for better task efficiency when viable; fully synergize within Prism OS (system‑viz, obsidian vault, memory, wiki, tribal knowledge).  
- `/startup-sierra` – force‑take sierra slot, bind handoff to `sierra-work`, run `/startup`.  
- `/loop [10m] /goal [ finish all ollama synergy tasks… ]` – autonomous loop pursuing Ollama synergy.  
- Cross‑chat permission granted – can edit alpha/bravo lane files.

**FINDINGS/BUGS**  
| Bug | Location | Fix |
|-----|----------|-----|
| Keyword recall disabled (`PRISM_MEMORY_INDEX_INJECT=0`) | `memory-rag-inject.mjs` | Wired hook, verified live fire. |
| Sync resilience – unguarded `writeFileSync` at line 342 | `obsidian-memory-sync.mjs` | Added `writeWithRetry`, exported helper, wrapped write. |
| Index‑meta stale frontmatter (`last_verified`, “Total entries”) | `WikiIndexMaintainerEngine.formatIndex` / `index.md` | In‑place metadata stamp script; no re‑emit of catalog. |
| Link‑heal scorer overconfidence (prefix/substring auto‑apply) | `wiki-link-fix-suggester.mjs` | Lowered score thresholds, added reason gating. |
| Decay leak – long slugs with 3 edits > floor | Same file | Reason‑based gating (`autoApplyEligible` only on exact/Levenshtein≤2). |
| Stale candidates file (autoApplyCount 14100) | `state/shared/.wiki-link-fix-candidates.json` | Regenerated with new scorer; partial coverage 100 links, disarmed. |
| Over‑commit from hook processes (~210 GB commit charge) | N/A | Reduced to ~52 % after wsl shutdown & heap cap. |
| False‑positive MCP failing alarm (`0x800710E0`) | `fleet-task-health-watch.mjs` | Added TRANSIENT_PRESSURE_CODES handling. |
| Offload coverage low (~8 %) | `OllamaTaskOffloaderEngine` | Broadened OFFLOADABLE_PATTERNS; target ~30 %. |
| Missing‑task banner noise from intentionally unregistered vault crons | `fleet-task-health-watch.mjs` | Suppressed via EXPECTED_UNREGISTERED_TASKS. |
| gpt‑oss:20b/120b returned empty `.response` | `generateBlurb()` | Switched to qwen2.5‑coder:32b, adjusted timeout. |
| Duplicate MCP daemons detected (3) | N/A | Flagged for cleanup.

**DOMAIN SPECIFICS**  
- **Engines/Actions:** WikiIndexMaintainerEngine, obsidian-memory-sync.mjs, memory-rag-inject, node-card-prefetch-inject, portable-node wrapper, mcp-server/src/index.ts, fleet-task-health-watch.mjs, OllamaTaskOffloaderEngine, regen-viz.mjs, canvas-read-lib.mjs, doc_nodes action, U‑VIZ‑WIKI‑NARRATIVE (regen-wiki-from-viz.mjs), U‑OLLAMA‑PIPELINE‑INJECTOR‑WIRE, U‑OLLAMA‑PREWARM‑WIRE, U‑TRIBAL‑QA‑LLM‑UNBLOCK.  
- **Metrics:** cheap‑token read path ~136 tokens; tribal coverage 83.7 %; hook heap cap 384 MB; MCP maxConnections 512; offload ratio target ~30 %; commit charge reduced from 92 % to ~52 %; GPU VRAM usage; missing‑task counts.  
- **Paths:** `H:/prism/.claude/helpers/...`, `C:/Users/.../.claude/settings.json`, `H:/prism/.claude/settings.json`, `system-viz/MEMORY.md`, `wiki/obsidian-vault-node-access-map.md`, `state/shared/.wiki-link-fix-candidates.json`, `regen-wiki-from-viz.mjs`, `canvas-read-lib.mjs`.

**TOOLS USED**  
- PRISM tools: `/checkin-sierra` wrapper, `slot-bind-enforce.mjs`, ultraflow workflow (`ultracode`), `portable-node`, `mcp-server`, `fleet-task-health-watch`, Ollama infra (`callOllama.mjs`), `generateBlurb()`, `scrutiny-3way.mjs`.  
- Dispatchers/skills: git commit/add, vitest, jest, tsc type‑check, system-viz-query.mjs, regen-wiki-from-viz.mjs.  
- Scripts/hooks: chat-slots.json, `PRISM_GIT_ADD_LANE_DISABLE`, `PRISM_MAINTREE_WRITE_BLOCK_DISABLE`, settings.json UserPromptSubmit.

**OPEN THREADS**  
- Build U‑WIZ‑NARRATIVE (`regen-wiki-from-viz.mjs`) – add narrative paragraphs via gpt‑oss:20b.  
- Wire U‑OLLAMA‑PIPELINE‑INJECTOR‑WIRE – expose existing Ollama call infra as hook/action for tool calls across system‑viz, vault, memory, wiki, tribal knowledge.  
- Implement U‑WEEKLY‑SYNTH‑RESOLVER – resolve synthesis model selection for weekly scripts.  
- Further integrate Ollama into system synergy: tool calls, `/system‑viz` queries, obsidian vault access, memory distillation, wiki creation/injection, tribal knowledge generation.  
- Memo‑extract throttle (per‑session vs fleet‑global).  
- Wiki‑NLI lint.  
- CLAUDE.md 7b→32b doc fix.  
- Memo‑synth schedule (freeze‑gated).  
- Clean up duplicate MCP daemons; arm deferred items after migration freeze lift.
