# bug-hunting session 001bd6c3 (2026-06-09, 50.3MB, spine 466KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hermes config migrated to v28; provider strings set to `anthropic/claude‑opus‑4.8` primary, `custom/qwen2.5-coder:32b` fallback – later overridden by decision to use `gpt‑oss:20b` primary with Claude fallback.  
- Qdrant container `prism-qdrant` healthy on `:6333`.  
- Hermes backend 4 processes reachable at `:9120` (`config_version 28`).  
- Headless zulu via PowerShell CLI (`hermes -z`) functional.  
- Commit **28b72e4dee**: MEMORY.md filled for all 34 galaxies; deterministic test suite (13 tests) added.  
- Commits **7d79f345c2**, **1ab785c21d**, **3ea4f40192**, **2579da89a4**, **e6eba32eec** applied (CLAUDE.md domains, PSN edge, soul realignments, thin filler removal).  
- Cron job `180007b9` retired after completion.  
- Units **U5b**, **U5c**, **U6**, **U7**, **U9**, **U10** committed to slot/bravo; **U1–U3**, **U2**, **U9** also committed.

**DECISIONS**  
- Adopt Path D: use existing PRISM MCP `:3100` for Hermes→Obsidian read; no second filesystem server.  
- Provider strategy: set Hermes primary to `gpt‑oss:20b` (general work) with Claude fallback; upgrade to `gpt‑oss:120b` when available. Billing only if higher‑tier or external API needed.  
- Fallback: Hermes falls back to local Ollama on 429/connection errors; 400 errors from Claude handled by enabling extra‑usage billing or switching to local model.  
- Token budget for `gpt‑oss:20b` increased (output limit raised).  
- Auto‑route lever enabled (`ollama-route-config.json` mode = auto); task→model router in `OllamaTaskOffloaderEngine.ts`.  
- Co‑residency mutex for `gpt‑oss:120b` + `qwen2.5-coder:32b`: `NUM_PARALLEL = 2`, `MAX_LOADED = 3` to keep VRAM <95 GB.  
- Extend existing `ask‑ollama/OllamaTaskOffloaderEngine` instead of forking; add optional tier field; drop retired models from offload router.  
- Replace default model in `ollama-route-pretooluse.mjs` with resident `gpt‑oss:20b`; ship accompanying `ollama-route-config.json`.  
- Correct take‑rate calculation to exclude advisory classifiers (honest 0.9 % rate).

**OPERATOR DIRECTIVES**  
- Restart Hermes daemon after config changes.  
- Verify `:9120/api/status` shows `config_version:28`, `model.provider=anthropic` (or updated provider).  
- Run test prompt via CLI: `hermes -z "Hello"`.  
- If using headless zulu, create PowerShell wrapper (`zulu-cli.ps1`) and schedule it.  
- Monitor memory pressure; clear idle chat logs if commit‑charge >95 % for >30 s.  
- Audit each galaxy sequentially to ensure full context (MEMORY.md, PATHS.md, TOOLBELT.md, wikis, tribal knowledge).  
- Re‑enable Hermes‑Obsidian Bridge scheduled task; confirm healthy.  
- Coordinate with Papa slot for soul realignment; avoid editing slot‑owned stubs unless instructed.  
- Use ultracode workflows for brainstorming, planning, verification.

**FINDINGS/BUGS**  
- Qdrant down initially; now up on `:6333` (vector store available).  
- Hermes messages previously failed HTTP 400 from Anthropic due to exhausted extra‑usage balance; fallback not triggered on 400. Enable extra‑usage billing or switch to local model.  
- Unknown provider `'openai'` unsupported; must use custom or anthropic.  
- Stale daemon connection state at `:3100` when Qdrant down; restart required.  
- GUI stuck on git pull due to dirty `package-lock.json`; fixed by stashing/removing file and restarting launcher.  
- `gpt‑oss:20b` one‑shot CLI returned “no final response” – token budget insufficient; increased output limit.  
- Memory inflation bug: node_* graph dump files counted as memories (e.g., cam reported 1362 vs. 66 real).  
- Corrupt ai‑training synthesis file (all‑NUL); flagged for owner regeneration.  
- Session‑consolidate‑graph timeout (8 s) and wiki↔tribal coverage gap (17.1%) pending remediation.  
- Offload router had absent/retired models → ~0 % offloading; fixed by roster refresh.  
- Stale tests in route‑hook (`decideRoute`, `runRoute`) updated or deferred.

**DOMAIN SPECIFICS**  
- Hermes: desktop app config at `C:/Users/.../hermes/config.yaml`; backend on `:9120`; provider registry includes anthropic, custom, gpt‑oss.  
- Obsidian vault REST API plugin on `:27123` (plugin installed, key present).  
- PRISM MCP at `http://127.0.0.1:3100/mcp` healthy; dispatchers `prism_memory`, `prism_session`, `prism_knowledge`.  
- Qdrant container `prism-qdrant` REST API on `:6333`; collections `prism_engines`, `prism_formulas`, `prism_skills`.  
- Slot coordination: `slot_brief_write → slot‑briefs/bravo.md`.  
- 34 galaxies each with MEMORY.md, PATHS.md, TOOLBELT.md; engines under `mcp-server/src/engines/*.ts`; MASTER‑BRAIN‑TEMPLATE sections required.  
- Models: `gpt‑oss:120b` (65 GB), `gpt‑oss:20b`, `qwen2.5-coder:32b` (~37 GB), `qwen3-vl:8b`, `llama3.2-vision:11b`, `nomic-embed-text`.  
- Hardware: RTX PRO 6000 Blackwell 96 GB VRAM, 127 GB RAM; local LLMs loaded via Ollama Docker stack.

**TOOLS USED**  
- PRISM CLI (`/checkin-bravo`, `chat-slots.mjs`, `audit-roadmap-drift.mjs`).  
- Hermes CLI commands: `hermes config migrate`, `hermes doctor`, `hermes model`, `hermes -z`.  
- Node scripts: `node scripts/hermes‑obsidian‑app‑map.mjs`, `syncGalaxyMemories.mjs`, `fill-galaxy-memory-sections.mjs`, `galaxy-synthesis-refresh.mjs`.  
- Docker Desktop (`docker compose up`, `docker run` for Qdrant, Ollama).  
- Ultracode workflows (e.g., `wbmnnd902`, `w2hu6dowo`) for brainstorming and verification.  
- Test harnesses: Node test (`node --test`), vitest in CI.  
- Git worktree utilities; commit‑enforce hook.

**OPEN THREADS**  
- Enable extra‑usage billing in Claude account or switch to local model to eliminate 400 errors.  
- Verify Hermes fallback to Ollama after Qdrant and billing enabled.  
- Re‑validate slot coordination after Qdrant restart.  
- Pull `gpt‑oss:120b` when available; upgrade primary provider.  
- Finalize auxiliary model routing (`ask‑ollama/OllamaTaskOffloaderEngine`).  
- Sync `ask‑ollama.mjs` and `host‑aware‑synthesis-model.mjs` into bravo for U4.  
- Greenlight live RTK `config.toml` edit at `%APPDATA%\rtk\`.  
- Approve or reject Kimi K2.6‑free as env‑gated octopus consensus voice.  
- Clean up stale async `runRoute` tests; separate hygiene unit.  
- Resolve session‑consolidate‑graph timeout and wiki↔tribal coverage gap.
