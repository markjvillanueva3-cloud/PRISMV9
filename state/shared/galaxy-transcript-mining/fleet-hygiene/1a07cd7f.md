# fleet-hygiene session 1a07cd7f (2026-05-20, 23.9MB, spine 149KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `/fleet-reaper-home` preset applied to DESKTOP‑N7MI1VB: 8 `PRISM_FLEET_REAPER_*` keys, prewarm `qwen2.5-coder:14b` (keep_alive).  
- Fleet‑Reaper skill upgraded to MS3 (Tier‑1.5 bg‑throttle + Tier‑3 SYSTEM principal default + host‑preset awareness).  
- Docker stack up (`postgres`, `qdrant`, `prometheus`; Grafana moved 3001→3601 → 3601:3000).  
- Precompact handoff written with explicit RESUME directive.  
- Hook `wiki-read-offload-advisory.mjs` created + tests added.  
- Commit `b3c8c8c42c`: `[MAIN] [DOC-HYGIENE]/U‑CLAUDE‑MD‑COMPRESS` – CLAUDE.md shrunk 58 % (162→67 KB), regression archive created.  
- Created `route-to-obsidian.md` skill (gitignored).  
- Built `Launch‑PRISM‑Fleet.ps1` + desktop shortcut; three spaced windows, 14 tabs (zebra last).  
- Installed Python 3.14.5 on H: and C:, swapped junction to `H:\Tools\python`, set `PYTHON_GIL=0`.

**DECISIONS**  
- Fleet‑Reaper ownership moved from alpha → golf; doctrine now non‑skippable on `/checkin-golf`.  
- Adopted MS2 enumeration cache + cross‑PC host filter for slot‑aware reaping.  
- Tier‑3 SYSTEM principal default for durable scheduled task (`PRISM Fleet Reaper`).  
- Host‑preset system: `applyHostPresetForCurrent()` keyed by `os.hostname()`, “custom” label allowed; preset set to *home* tier (`PRISM_FLEET_REAPER_*` env vars).  
- Cap TypeScript Language Server memory globally (`typescript.tsserver.maxTsServerMemory=1024`) to reduce ~16 GB RAM leak.  
- Docker Compose used over Kubernetes for single‑host deployment; port collision handled via Hyper‑V reserved range.  
- Adopted Option A: `/checkin-golf` & `/startup-golf` delegate a single line to `/fleet-reaper`.  
- Added Step 0 in `/fleet-reaper`: fire all six supporting reapers, sweep, arm monitor.  
- Fixed `hooks.Stop[3].matcher` from `null` → `""`; mirrored to H:.

**OPERATOR DIRECTIVES**  
- Run `/startup-golf` to claim golf slot and start fleet‑reaper + startup pipeline.  
- Verify Docker stack: ollama, grafana (3601), postgres (healthy), prometheus, qdrant; prism‑server context bug noted.  
- Monitor re‑arm via `/fleet-reaper`.  
- Install NIM GPU passthrough (`pwsh H:/Tools/nim/start.ps1`).  
- Commit Grafana port change (`git add docker-compose.yml && git commit -m "[GOLF] grafana port 3001→3601"`).  
- Wire hook in `C:/Users/wompu/.claude/settings.json` under PreToolUse:Read.  
- Run tests (`node --test .claude/hooks/wiki-read-offload-advisory.test.mjs`).  
- Commit hook, test, skill, and compose file (`git commit -m "[MAIN] [HIGH‑ROI‑TOKEN‑SAVINGS]/U‑WIKI‑OFFLOAD‑ADVISORY: …"`).  
- Run scrutiny gate (`node .claude/scripts/scrutiny-3way.mjs --session-id <sid> --target HEAD`).  
- End loop (`node H:/prism/.claude/helpers/loop-state.mjs end --session <sid> --reason done`).

**FINDINGS / BUGS**  
- TSServer memory leak: 16 instances ≈ 16.5 GB RAM; global cap applied.  
- Docker port collision: Hyper‑V reserved 2972–3600; Grafana moved to 3601.  
- NIM keepalive flapping: `start.ps1` restarts every 5 min, process dies before next sweep – operator restart required.  
- Docker stack missing containers (`prism-server`, `ollama`, `grafana`) – now Grafana is up.  
- Fleet‑Reaper task status unknown; verified via PowerShell that task exists and ready.  
- `settings.json` error caused config ignore → statusline HP/MP bars missing (fixed matcher).  
- Dockerfile for `prism-server` uses wrong context (`.` → `./mcp-server`).  
- Postgres initially down, recovered after health‑check.  
- Reaper monitor armed; all six reapers fired (Node orphan, Zombie v2, PS orphan, Memory pressure, Hook janitor, Cleanup orchestrator).

**DOMAIN SPECIFICS**  
- Fleet‑Reaper architecture: MS0 → MS1 → MS2 (enumeration cache + host filter) → Tier‑1/2 → MS3 (Tier‑3 SYSTEM principal, NIM keepalive).  
- Slot binding: `slot-bind-enforce.mjs` deterministically claims `golf`; `/checkin-golf` always runs reaper.  
- Host‑preset system: `setPresetForHost({host,label,presetBody})`, `applyHostPresetForCurrent()`.  
- Precompact handoff: auto‑synthesizes RESUME directive; must be written before `/compact`.  
- Loop state: `.claude/helpers/loop-state.mjs` manages 5 min cadence for goal completion.  
- Engines/dispatchers: fleet‑reaper, node orphan cleaner, zombie v2, PS orphan reaper, memory‑pressure auto‑relief, hook janitor, cleanup orchestrator.  
- Key paths: `H:/prism/.claude/helpers/fleet-reaper-host-presets.mjs`, `scripts/fleet-reaper-sweep.mjs`, `state/shared/dashboards/fleet-reaper-host-presets.json`.

**TOOLS USED**  
- PRISM tools: `/fleet-reaper`, `/startup-golf`, `/checkin-golf`, `route-to-obsidian.md`, `statusline.mjs`.  
- Scripts: `Launch‑PRISM‑Fleet.ps1`, Python install scripts, node commands for preset/apply.  
- Hooks: `compact-interval-warning.mjs`, `precompact-pending-guard.mjs`, `wiki-read-offload-advisory.mjs`.  
- Docker Compose (`docker compose up -d`).  
- PowerShell scripts for NIM and task registration.  
- Node test runner (`node --test`).  
- `chat-slots.mjs`, `fleet-reaper-sweep.mjs`, `install-fleet-reaper-task.ps1`, `install-mcp-server-task.ps1`, `install-mcp-server-watchdog-task.ps1`.

**OPEN THREADS**  
- NIM restart – operator‑only; verify GPU passthrough after `start.ps1`.  
- Dockerfile context bug: fix `prism-server` to use `./mcp-server`.  
- Resolve “bit tasks” issue (specific process class not yet identified).  
- Ensure statusline renders correctly in all terminal windows after settings fix.  
- High‑ROI skill verification – run `/route-to-obsidian` in production to confirm 30 % offload target.  
- Memory monitoring enhancements – integrate tsserver exclusion into `tsconfig.json` and monitor via fleet‑reaper metrics.  
- PRISM MCP Server & Watchdog install – elevated PowerShell scripts pending.  
- Docker‑MCP‑WIRE‑MS0 units – 7 operator‑gated activations (git‑connect, time, fetch, markdown, arxiv, playwright, semgrep).
