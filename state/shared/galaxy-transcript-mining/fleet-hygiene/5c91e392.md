# fleet-hygiene session 5c91e392 (2026-06-09, 16.2MB, spine 150KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `b76b4d55ab` – resumable corpus loop (cursor file + slot‑binding wrapper).  
- `01ce06d589` – multi‑page PDF rasterizer & manifest builder; added dedup key to trainset rows.  
- `5a8dbfefdd` – doc‑reflection updates (`CLAUDE.md`, wiki lesson, MEMORY.md pointer).  
- `cdc9ec44b3` – reaper‑immune PowerShell launcher for overnight closed‑loop training.  
- `250ffb8a6e` – `scripts/find-perfect-parts.mjs`; 10/10 mutation tests; markdown+JSON report of 91 perfect parts.

**DECISIONS**  
- Adopt resumable corpus loop with cursor file → no re‑OCR after reaper kills.  
- Rasterize all pages of multi‑page PDFs (96 % were page‑0 only).  
- Ensemble two VLMs (`qwen3-vl:8b-instruct`, `qwen2.5vl:7b`); third omitted due to timeout.  
- Schedule closed‑loop training nightly, reaper‑immune via Task Scheduler parent.  
- Delay LoRA fine‑tune until operator gold labels available; run self‑test first.  
- Run true‑test on 91 perfect parts before full‑corpus training; halt if OCR recall remains <1%.  
- Add third reliable VLM once available.  
- Resolve GPU contention by throttling model count or scheduling during quieter windows.

**OPERATOR DIRECTIVES**  
- `/loop [5m] /goal [read previous sessions for xray to reorientate | goal clear: upgrade our OCR and blueprint reading capabilities utilizing new GPU/CPU; wire, test, validate then begin closed‑loop training on all prints in jm folder/docustra]`.  
- Ensure GPU+CPU environment functional (torch 2.11+cu128, qlora ready).  
- Validate OCR pipeline end‑to‑end before launching full corpus run.  
- Run true‑test on 91 parts; report OCR recall before proceeding.  
- Verify real‑scan OCR reads dimensions; if recall <1%, pause full‑corpus run.

**FINDINGS/BUGS**  
- 96 % of drawing PDFs are multi‑page → lost ~76 % dimensions originally.  
- Reaper kills (exit 255) under fleet load; cursor survives, re‑OCR = 0 proven.  
- Worklist reader mis‑treated comment lines as prints – fixed (`#` skip).  
- GPU contention caused one VLM to time out → only one model survived per print.  
- `slot-bind-enforce` hook already claimed slot; manual bash unnecessary.  
- OCR on real scans yields 1–3 dimensions per print, matching **0** machined GT → recall gap.  
- Binary `.mcx‑8` files parsed as text inflated GT counts – fixed.  
- Mill programs (`*.hnc`) misclassified as lathe → fake callout diameters; now flagged non‑lathe and skipped.  
- Roughing‑ramp inflation in GT extraction halved via clustering.

**DOMAIN SPECIFICS**  
- **Engines/Actions**: `/checkin-xray` wrapper, `chat-slots.mjs`, `blueprint-ocr-training-loop.mjs`, `batch-ollama-vision-extract.mjs`.  
- **Dispatchers/Tools**: `pdf-to-png.py`, `xray-trainset-to-lora.mjs`, PowerShell launchers (`run‑ocr‑batch‑overnight.ps1`, `install‑blueprint‑ocr‑batch‑task.ps1`).  
- **Metrics/Paths**: cursor file (`processed-cursor.jsonl`), trainset (`trainset.jsonl`), active‑learning queue, GPU utilization (91 GB VRAM under fleet load).  
- **Unique Paths**: `H:/prism/.claude/helpers/chat-slots.mjs`, `scripts/run-ocr-batch-overnight.ps1`.

**TOOLS USED**  
- PRISM utilities: `/checkin`, slot‑binding helpers, `slot-bind-enforce`.  
- OCR/vision stack: Ollama (`qwen3-vl`, `qwen2.5vl`), Torch 2.11+cu128, bitsandbytes, peft, transformers, datasets, pymupdf.  
- PDF rasterizer: `pdf-to-png.py`.  
- Scheduler: Windows Task Scheduler via PowerShell scripts.  
- Fleet hygiene: reaper, orphan/zombie sweep, tmp‑orphan janitor, fleet‑task‑health, git‑contention, memory monitor.

**OPEN THREADS**  
- Run full overnight corpus training (7794 drawing prints) and monitor progress.  
- Acquire operator‑verified gold labels for domain‑shift gate; then run LoRA fine‑tune on real trainset.  
- Add third reliable VLM to ensemble once available.  
- Resolve GPU contention by scheduling during quieter windows or throttling model count.  
- Verify active‑learning queue contents and downstream training pipeline.  
- Improve real‑scan OCR recall (pre‑processing or model fine‑tuning).  
- Expand true‑test to larger sample after resolver/GT extraction stability.  
- Integrate validated OCR outputs into closed‑loop training once recall acceptable.
