# fleet-hygiene session 001bd6c3 (2026-06-09, 49.3MB, spine 458KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit ab2ccf42a4 / e6713584e2 – Hermes‑app integration with PRISM MCP & `/system-viz`.  
- Commit c988a21ec4 – Fix boot‑loop after blind edit of Hermes config.  
- Commit 56aa423427 – Hermes runs on Claude Opus 4.8 via OAuth; local `qwen2.5-coder:32b` fallback.  
- Docker Desktop → `prism-qdrant` container UP (`:6333`, healthy, 3 collections).  
- Hermes launcher finished booting; backend ready (no agent‑log errors).  
- Obsidian REST API at `:27123` reachable; vault config verified.  
- Commit 28b72e4dee – Adds `scripts/fill-galaxy-memory-sections.mjs`; updates all 34 galaxy `MEMORY.md` to full 4‑section structure; vault mirror updated.  
- U5b – Ollama read‑search agent (`ollama-tool-agent.mjs`) – live‑validated, 12 R9 tests.  
- U5c – Search‑offload enforcement hook (`ollama-offload-enforce.mjs`) – live‑validated, 8 tests.  
- U6 – Coding offload generator (`ollama-codegen.mjs`) – qwen2.5‑coder:32b tool‑calling, 9 R9 tests.  
- U7 – Opus tail‑review gate (`ollama-build-loop.mjs`) – default‑closed gate, 10 tests.  
- U10 – Capability probe (`ollama-capability-probe.mjs`) – verifies all 4 models run and auto‑used, 8 R9 tests.  
- U9 – Core‑residency mutex (`ollama-coresidency.mjs`) – 11 safety tests; committed to slot/bravo.  
- U1 – Ollama offload router roster refresh (commit 2340a2e699); tier field added; EOL normalized.  
- U2 – `ollama-route-pretooluse.mjs` default model changed to resident `gpt‑oss:20b`; new `ollama‑route‑config.json`; stale tests fixed (commit a2756779c2).

**DECISIONS**  
- Use existing PRISM MCP dispatchers (`prism_memory`, `prism_session`, `prism_knowledge`); drop separate filesystem‑MCP server.  
- Primary LLM: Claude Opus 4.8 via OAuth; fallback to local `qwen2.5-coder:32b` on 429/ rate limit.  
- Supervisor cwd fix reverted; CapabilityIndexEngine now uses PRISM_ROOT.  
- Qdrant must be started via Docker (now UP).  
- Provider mapping updated: Hermes `model.default = anthropic/claude‑opus‑4.8`, `fallback_model = custom/qwen2.5-coder:32b@11434/v1`; later switched to fleet‑aligned primary `gpt‑oss:20b`.  
- Restart :3100 MCP daemon after Qdrant up; clears stale connection state.  
- Headless zulu: use `hermes -z` CLI instead of GUI for fleet orchestration.  
- Billing vs local: keep Claude fallback; use local gpt‑oss:20b primary → no extra‑usage cost unless billing enabled.  
- Routing engine already exists (`OllamaTaskOffloaderEngine.ts`); U8 rejected as duplicate.  
- Tool‑calling only supported by gpt‑oss family; qwen coder emits text, so U5b uses gpt‑oss:120b/20b.  
- Offload search requires `git grep -E`; fixed in U5c.  
- Capability probe entry guard bug and tag‑match bug fixed before commit.  
- VRAM math corrected: 120b + 32b cannot co‑reside; set `MAX_LOADED=3`, `NUM_PARALLEL=2`.  
- Do not fork U1; extend existing engine to keep history clean.  
- Add optional `tier` field and tier‑then‑latency sort to prefer strong resident models.  
- Keep `KEEP_ON_CLAUDE_PATTERNS` unchanged for safety fence.  
- Normalize file EOLs to LF after commit to avoid noise commits.

**OPERATOR DIRECTIVES**  
- “get qdrant up and running.” (completed).  
- “hermes is no longer setup. make sure to link hermes app to claude code.” (completed).  
- Restart the :3100 MCP daemon (`hermes restart` or `systemctl restart hermes-mcp`).  
- Verify Qdrant connectivity from the MCP (`curl http://127.0.0.1:6333/health`).  
- Run a test zulu check‑in via CLI: `hermes -z "Check in as ZULU fleet orchestrator"` and confirm response.  
- If using Claude Code primary, enable extra‑usage billing at claude.ai/settings/usage.  
- Commit exclusively to slot/bravo worktree (`H:/prism-slot-bravo`).  
- Use ultracode workflows for planning/verification.  
- Finish remaining phases, upgrade existing systems for new hardware, evaluate RTK upgrade via LLM/hardware leap.

**FINDINGS / BUGS**  
- Stale MCP daemon: `N:\PRISM\src\tools\dispatchers` reference; resolved after restart.  
- Qdrant down → vector store not listening → empty semantic_search.  
- Anthropic 400 error: third‑party OAuth traffic billed to “extra usage”; Hermes message fails with 400 (no fallback).  
- 429 rate limit: shared 5 h Claude Max pool exhausted.  
- Hermes memory bridge dormant: no memories in `hermes/memories/`.  
- Hermes not reading Obsidian vault resolved by using MCP read path; no filesystem server needed.  
- Unknown provider ‘openai’ error – Hermes 0.16.0 does not support that string; must use custom or anthropic.  
- Stale connection state in :3100 daemon after Qdrant down.  
- Git pull aborted due to dirty `package-lock.json`; fixed by stashing/removing file and restarting launcher.  
- Hermes GUI stuck on “Finalizing desktop startup” until user clicks through setup screens; headless CLI bypasses this.  
- Dedup gate caught duplicate router (U8).  
- VRAM KV‑cache overestimation corrected.  
- Offload substrate stale‑on‑arrival: only resident `qwen2.5-coder:32b` available, others absent → 0 % offload.  
- RTK cannot be upgraded; external Rust binary remains heuristic‑only.

**DOMAIN SPECIFICS**  
- Engines: Hermes (app), PRISM MCP, Qdrant vector store, Octopus‑RAG, Dream‑Cycle, OllamaTaskOffloaderEngine, `ollama-route-pretooluse.mjs`, `ask-ollama.mjs`.  
- Dispatchers: `prism_memory`, `prism_session`, `prism_knowledge`, `prism_context` (`slot_brief_write`).  
- Metrics: `self_awareness_search`, `master_index_query`; resident model roster; tier latency ordering.  
- Paths: `knowledge/`, `hermes/memories/`, `state/shared/slot‑briefs/`, `scripts/fill-galaxy-memory-sections.mjs`, `galaxy-synthesis-refresh.mjs`, `syncGalaxyMemories()`.  
- Hardware: RTX PRO 6000 Blackwell 96 GB VRAM, 127 GB RAM; models: gpt‑oss:120b (65 GB), gpt‑oss:20b (10 GB), qwen2.5‑coder:32b (~37 GB), qwen2.5‑coder:1.5b, qwen3‑vl:8b, nomic‑embed-text.  
- Offload hooks: PreToolUse (search), PostToolUse (read).  
- Opus tail‑review gate (`gateIncorporation`).  
- Capability probe (`probeModel`).  
- Core‑residency mutex with hard‑reason lock.

**TOOLS USED**  
- PRISM helpers: `/checkin-bravo`, chat‑slots helper, `audit-roadmap-drift.mjs`, `wire-hermes-local-backend.mjs`.  
- Hermes config (`config.yaml`), OAuth credentials (`~/.claude/.credentials.json`).  
- Docker (for Qdrant); docker‑compose.  
- Node scripts: `mcp-server-supervisor.mjs`, `ollama-tool-agent.mjs`, `ollama-offload-enforce.mjs`, `ollama-codegen.mjs`, `ollama-build-loop.mjs`, `ollama-capability-probe.mjs`, `ollama-coresidency.mjs`.  
- Ultracode workflow engine (`wbmnnd902`, `w2hu6dowo`).  
- PRISM `/loop` scheduler, `/startup-bravo`.  
- Ollama CLI via local Docker.  
- Git hooks for slot‑enforcement.  
- Vitest harness; Node.js.

**OPEN THREADS**  
- Resolve Anthropic 400 error by enabling “extra usage” billing or switching to a different LLM provider.  
- Finalize CapabilityIndexEngine path‑fix (use PRISM_ROOT).  
- Confirm Hermes fallback works when Claude pool exhausted.  
- Provider config finalization – decide whether to keep Claude fallback or switch entirely to local gpt‑oss:20b.  
- Memory pressure monitoring – set up periodic check on commit charge; consider compacting idle chats if spikes occur.  
- Headless zulu deployment – create PowerShell wrapper (`zulu-cli.ps1`) and scheduled task for autonomous fleet runs (planned).  
- Ensure automatic cron for vault sync so galaxy mirrors stay up‑to‑date (missing).  
- Await clearance of Claude API rate limits before attempting any external reviewer passes again.  
- Go‑live merge – slot/bravo → main and wiring of `settings.json` (gated by golf’s git‑tree repair).  
- RTK upgrade plan – ultracode workflow output pending; will produce dependency‑ordered, slot‑safe plan to add an LLM compressor for RTK.  
- U11 cron installer – wrapper script for scheduled probe gate (not yet built).
