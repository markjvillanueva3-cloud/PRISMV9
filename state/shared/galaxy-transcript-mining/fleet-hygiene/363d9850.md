# fleet-hygiene session 363d9850 (2026-05-19, 1.9MB, spine 12KB, 1 slice(s), model gpt-oss:20b)

**DECISIONS**  
- Doctrine shift 2026‑05‑16: fleet‑reaper ownership moved from *alpha* → *golf*, unifying all fleet‑hygiene under one slot.  
- `/checkin-golf` now always runs the fleet‑reaper; the skill’s `golf-slot-reaper-guardian.mjs` hook makes this step non‑skippable.  
- Operator directive: golf is claimed as a normal work slot (no legacy `--golf` flag).  
- Pre‑flight bypass for legacy allowlist (`golf-slot-write-allowlist.mjs`) – either set `PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1` or edit settings.json to remove the hook.  

**OPERATOR DIRECTIVES**  
- If you want to trigger the dev pipeline, run `/checkin-golf /loop <task>` or supply a unit ID after the check‑in.  
- Otherwise leave golf running; it will continue as the fleet‑hygiene watch.  
- Consider disabling the legacy allowlist hook if you need unrestricted writes in this slot.

**FINDINGS/BUGS**  
- 11 of 13 slots crashed (alpha, bravo, …, mike); postmortems written to `chat-crash-postmortems.jsonl`.  
- Branch `cad‑fusion‑live‑ms0` is 375 commits ahead and 1 behind origin; push will fail until merged/rebased.  
- 16,326 uncommitted files present (mostly memory files, hooks, docs).  
- Memory spiked to ~90 % warn tier during a burst; fleet‑reaper escalated to aggressive tier, reaped orphans, and prewarmed Ollama `qwen2.5-coder:3b`. No action required.  
- Docker probe unavailable (host daemon only); Ollama reachable directly.

**DOMAIN SPECIFICS**  
- Engines/dispatchers: fleet‑reaper, fleet‑memory‑monitor, chat‑slots.claim, in‑session Monitor.  
- Hooks/scripts: `golf-slot-reaper-guardian.mjs`, `golf-slot-write-allowlist.mjs` (PreToolUse), `fleet-reaper-sweep.mjs`.  
- Metrics/paths: GPU utilization/freeMb, Ollama reachable/models, coordinator hint status (`auto`).  
- Hand‑off file: `HANDOFF-claude-363d9850-golf-work.md`.  
- Crash log file: `chat-crash-postmortems.jsonl`.

**TOOLS USED**  
- PRISM skill `/checkin-golf` (force‑take slot, run fleet‑reaper).  
- Node helpers: `chat-slots.mjs reclaim/claim`, `fleet-reaper-sweep.mjs`.  
- Scheduled task: “PRISM Fleet Reaper” (5‑min cadence, SYSTEM principal).  
- In‑session Monitor script (user‑level reaping).  
- Environment variable `PRISM_GOLF_WRITE_ALLOWLIST_BYPASS` for bypassing legacy allowlist.

**OPEN THREADS**  
- Dev pipeline steps 8–14 remain untriggered; need to supply a task or unit ID after `/checkin-golf`.  
- Resolve branch divergence and commit the 16k uncommitted files before next push.  
- Monitor continues running; no immediate action required unless new failures appear.
