# fleet-hygiene session e20e2b52 (2026-05-20, 26MB, spine 86KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-MEMORY-COMPRESS-V2` code + tests – commit 3798922e49 (27 compressor tests, 47 gate tests).  
- PreToolUse gate for `MEMORY.md` – added mtime‑check and hard‑block logic.  
- Fleet‑Reaper “home” preset written & verified (`qwen2.5-coder:7b`, 256 MB ballast).  
- Doctrine update from 13 → 26 slots – bulk patch via `scripts/fleet-doctrine-sweep.mjs` (commit 57f28a1ad6).  
- All 13‑slot references in `CLAUDE.md` and user‑global copy rewritten; inbox‑pointer added.  

**DECISIONS**  
- Use Windows Terminal with admin shortcut for chat fleet; VS Code only for code editing.  
- Keep golf slot as reaper owner (no switch to alpha).  
- Apply home preset on this machine; no need for `/fleet-reaper-work`.  
- Update all doctrine docs to 26 slots; maintain backward‑compatibility via patch‑siblings.  

**OPERATOR DIRECTIVES**  
1. **Iter 8 of SAF loop** – write golf‑slot patch‑sibling at `state/shared/dashboards/patches/CLAUDE-MD-PATCH-line333-counts.md` replacing the literal “722‑entry catalog (575 engines + 90 dispatchers + 57 memories)” with a pointer to `PRISM-INVENTORY-LATEST.md`.  
2. **Next unit** – `U‑SAF‑D2`: reconcile `MEMORY.md` ↔ auto‑memory reference (`C:/Users/wompu/.claude/projects/H--PRISM/memory/`).  
3. Address remaining pending items:  
   - `U‑SAF‑C1` wiki cross‑ref gaps,  
   - `U‑SAF‑D1` superseded docs,  
   - `U‑SAF‑F3` CLAUDE.md inbox auto‑pop.  

**FINDINGS/BUGS**  
- `slot-bind-enforce.mjs` & `process-slot-map.mjs` had hardcoded 13 names → fixed to 26.  
- PreToolUse gate missed mtime check → added.  
- CountClaims detector false positives from markdown bold, date adjacency, capacity assertions → corrected.  
- Broken wikilinks due to dash/underscore mismatch → resolved with `linkExists`.  
- Stale‑family detection now uses inbox haystack for accurate counts.  

**DOMAIN SPECIFICS**  
| Component | Role |
|-----------|------|
| `fleet-reaper-sweep.mjs` | Orchestrates orphan cleanup, GPU coordination, Ollama hint emission. |
| `slot-bind-enforce.mjs`, `process-slot-map.mjs` | Slot‑claim logic & mapping; now 26 slots. |
| `chat-slots.mjs` | Slot state persistence and reclamation. |
| `memory-compress-v2.mjs` | Compresses `MEMORY.md`; gate enforces size ceiling. |
| Metrics output (JSON) | GPU free MB, memory‑pressure %, soft relief, Ollama keep‑alive, reaper status. |
| Paths | `H:/PRISM/.claude/helpers/`, `scripts/`, `state/shared/` for config & audit data. |

**TOOLS USED**  
- Windows Terminal (`wt.exe`) with admin shortcut.  
- PRISM session startup via `.claude/commands/startup.md`.  
- `fleet-reaper-sweep.mjs`, `install-fleet-reaper-task.ps1`.  
- `per-agent-handoff.mjs` + `stable-session-id.mjs` for handoff writing.  
- `milestone-tracker.mjs` to mark completions.  

**OPEN THREADS**  
- `U‑SAF‑D2` memory recon pending (auto‑memory reference).  
- `U‑SAF‑C1` wiki cross‑ref gap detection.  
- `U‑SAF‑D1` superseded docs cleanup.  
- `U‑SAF‑F3` CLAUDE.md inbox auto‑population.  
- Per‑agent handoff write failed earlier; ensure next session writes correctly (use `--source live-chat`).  

**NOTE** – The loop is capped at 20 iterations; we are at **iter 7/20**. Run `/compact` to clear the Stop hook and allow the next session to resume with the above RESUME directive.
