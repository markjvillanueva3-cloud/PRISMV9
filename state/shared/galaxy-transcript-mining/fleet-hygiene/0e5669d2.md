# fleet-hygiene session 0e5669d2 (2026-06-09, 38.1MB, spine 314KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- B1 sync‑resilient – `168c202646` (writeWithRetry fixes data‑loss)  
- B2 index‑meta – `ea21008f25` (in‑place stamp of `wiki/index.md`)  
- B3 maint‑cron installers – `8c4dff660a` (migration‑safe, -Disabled flag added)  
- C2 link‑heal scorer – `984313825e`, `d948b85a74` (hardened, auto‑apply dropped 33→2)  
- C3 tribal‑coverage refresh – `838123429b` (83.7 % coverage)  
- memo / persistent context memories – `45225e0cc3`  
- audit & RAG‑wire artifacts – `da013c4406`, `9e4376b3b2`  
- fleet‑task‑health watch – `3d796dcf5c` (EXPECTED_UNREGISTERED_TASKS added)  
- vault‑backlink schema & builder – `96ed5222e2`  
- backlink reader staleness flag – `d856173b86`  
- regen‑wire stage in `regen-viz.mjs` – `0e2724871a`  
- `prism_session:doc_nodes` dispatcher – `14aba14e3a`, `4a44b5393c`  
- vault‑access map update – `f23c58b42d`  
- canvas‑reader lib + CLI – `2d49bf0d33`  
- access‑map update closing `.canvas` gap – `47fe91554f`  
- T1 U‑VIZ‑WIKI‑NARRATIVE – `cd54edb940` (qwen2.5‑coder:32b)  
- T2 OLLAMA‑PIPELINE‑INJECTOR – `7ec4a5ea02`  
- #8 U‑OLLAMA‑PREWARM‑WIRE – `65a29220e5`  
- #2 U‑TRIBAL‑QA‑LLM‑UNBLOCK – `6fb278a2ee`

**DECISIONS**  
- Use `/checkin-sierra` wrapper to claim sierra slot before canonical `/checkin`.  
- Persist audit findings, node‑access map, memory‑recall hook on four durable surfaces (galaxy MEMORY.md, wiki entry, two C: memories, H: mirror).  
- Wire `memory-rag-inject.mjs` after disabling always‑on injector.  
- Replace unguarded `writeFileSync` in `obsidian-memory-sync.mjs` with retryable helper (`writeWithRetry`).  
- Add public `refresh()` to `WikiIndexMaintainerEngine`; script updates stale frontmatter of `index.md`.  
- Install two Windows cron jobs (promotion & rot‑sentinel) disabled, ready for activation.  
- Recalibrate link‑heal scorer: drop auto‑apply on bare prefix/substring matches; keep exact‑slug and ≤2‑edit Levenshtein as high confidence.  
- Cap hook heap to 384 MB (`PRISM_HOOK_HEAP_MB`).  
- Raise MCP server `maxConnections` to 512 in `mcp-server/src/index.ts`.  
- Add pressure status to fleet‑task‑health; treat HRESULTs 0x800710E0, 0x8007000E, 0x800705AA, 0x8007012B as transient.  
- Widen GPU offload patterns in `OllamaTaskOffloaderEngine`.  
- Introduce `EXPECTED_UNREGISTERED_TASKS` to silence vault‑cron “missing” noise.  
- Defer MCP restart until commit‑cap verified; pagefile relocation unnecessary (100 GB already).  
- Adopt qwen2.5‑coder:32b as default text generator; switch gpt‑oss to 1024 predict or 120b when needed.  
- Reuse `generateBlurb` for viz‑wiki narrative; flag‑gated post‑pass generator keeps hot‑path scripts untouched.  
- Keep existing narrow routing logic (router‑widening falsified).  
- Use prewarm hook (`ollama-prewarm-on-pipeline.mjs`) over manual warm‑up.

**OPERATOR DIRECTIVES**  
- “Fill all gaps.”  
- “Reorient from recent sierra sessions, verify vault built & operational, map paths to each Obsidian vault node for cheaper token usage / free token usage.”  
- Persist recovered context in sierra’s domain (galaxy MEMORY.md, wiki entry, C: memories, H: mirror).  
- “Continue with system‑viz and Obsidian app synergizing; keep mapping nodes for quick searches and tool usage savings.”  
- “Utilize Ollama for better task efficiency when viable. Keep improving system synergy for higher token savings throughout the system. Make sure Ollama LLM is fully synergized within the complete PRISM OS system so we can utilize it for tool calls, /system‑viz, Obsidian vault, memory, wiki and tribal knowledge creation and injection.”  
- “See where else we can utilize Ollama LLM for better system efficiency.”

**FINDINGS/BUGS**  
- `obsidian-memory-sync.mjs` had unguarded writes causing batch aborts.  
- `index.md` frontmatter stale; file is 1,128‑entry catalog, not full 39K leaf index.  
- Maint‑cron installers missing `-Disabled` flag and do‑not‑run‑during‑migration header.  
- Link‑heal overconfident: prefix/substring matches auto‑applied (0.88–0.95); short Levenshtein ≤2 edits flagged high confidence.  
- Stale `wiki-link-fix-candidates.json` had `high:14100` disarmed to `high:33`; only first 100 links due to batch limits → partial coverage artifact.  
- Full‑corpus scan (15.8K × 51K) OOM’d; cannot produce complete candidates file in one run.  
- Over‑commit from 4 GB hook heap caused false MCP‑failure alarm; fixed by cap.  
- GPU offload coverage low (8 %); widened patterns improved to ~30 %.  
- Vault crons not registered → flagged as missing; now marked `EXPECTED_UNREGISTERED`.  
- WSL Memory Guard installer present but task unregistered → real missing.  
- Commit‑charge 90.8 % dominated by vmmemWSL (95 GB) and llama-server (45 GB); node fleet fine at 13 GB.  
- `.wslconfig memory=16GB` cap not enforced until `wsl --shutdown`; operator authorized shutdown reclaiming ~79 GB.  
- Stale `vault-backlinks.json` after graph regen; staleness flag now fires.  
- Canvas reader mis‑counted `Lgit` layer due to regex; fixed.  
- Peer‑contended git index: `sessionDispatcher.ts` & `sessionActionSchemas.ts` had interleaved changes; surgical staging used.

**DOMAIN SPECIFICS**  
- Engines/dispatchers/actions: `WikiIndexMaintainerEngine`, `build-wiki-leaf-index.mjs`, `wiki-link-fix-suggester.mjs`, `prism_session:doc_nodes`, `prism_session:node_card`, `prism_session:canvas_doc`, `OllamaTaskOffloaderEngine`, `SemanticSimilarityGuardEngine.cosineCheck()`, `PRISMSelfAwarenessEngine.searchAIFeatures()`.  
- Actions: node‑card queries (`node-card <id>`), memory recall hooks (`memory-rag-inject`), cron job installers, sync‑memories batch.  
- Metrics/paths: `last_verified` date, `Total entries` in `index.md`; cheap‑token read via node‑card; auto‑apply eligibility thresholds; coverageComplete flag; hook heap cap 384 MB; MCP maxConnections 512; pressure status; GPU offload patterns; staleness timestamps; commit-charge; WSL memory reservation.  
- Unique paths: `H:/prism/.claude/settings.json`, `C:/Users/.../.claude/settings.json`, `H:/prism-slot-sierra`, `slot/sierra`, `H:/prism/scripts/lib/canvas-read-lib.mjs`, `H:/prism/knowledge/wiki/architecture/obsidian-vault-node-access-map.md`.

**TOOLS USED**  
- PRISM fleet hygiene: `fleet-reaper`, `golf reaper`, `orphan/zombie sweep`, `tmp-orphan janitor`, `fleet-task-health-watch.mjs`, `git-contention monitor (git-lock-sweep)`, `memory monitor`.  
- Dispatchers/hooks/scripts: `/checkin-sierra` wrapper, `update-config` skill, git commit lane guard, scrutiny gate, ultracode workflow, `c-to-h mirror` hook, `memory-rag-inject.mjs`, `node-card-prefetch-inject.mjs`, `search-thoroughness-inject.mjs`, `portable-node` wrapper, `mcp-server/index.ts`, `ask-ollama.mjs`, `resolveSynthesisModel`, `canvas-read-lib.mjs`, `system-viz-query.mjs`, `regen-wiki-from-viz.mjs`, `slot-bind-enforce.mjs`, `ollama-prewarm-on-pipeline.mjs`.  
- PowerShell installers: `install-tribal-promotion-cron.ps1`, `install-vault-rot-sentinel.ps1`.

**OPEN THREADS**  
- Streaming/chunked write for `wiki-link-fix-suggester` to avoid OOM.  
- Add min token‑length floor & tie‑veto to link‑heal scorer.  
- Introduce `coverageComplete` boolean or document partial coverage in candidates JSON.  
- Activate maint‑cron installers (promotion & rot‑sentinel) after migration freeze lifted.  
- Restart MCP after applying `maxConnections:512`.  
- Register vault crons post‑migration; fix WSL Memory Guard missing registration.  
- Implement top Sierra tasks: U‑VIZ‑WIKI‑NARRATIVE, U‑OLLAMA‑PIPELINE‑INJECTOR‑WIRE, U‑WEEKLY‑SYNTH‑RESOLVER.  
- Evaluate additional surfaces for Ollama offload beyond shipped synthesis scripts.  
- Continue monitoring commit‑charge & WSL balloon; ensure `wsl --shutdown` remains applied after Docker restarts.  
- Rewrite per‑session throttle for `U‑MEMO‑EXTRACT‑THROTTLE`.  
- Resolve remaining unit of WeeklySynthesisEngine resolver.  
- Add wiki‑NLI linting for wiki NLI generation.  
- Upgrade CLAUDE.md model from 7b→32b.  
- Freeze‑gated scheduling task for memo‑synth.
