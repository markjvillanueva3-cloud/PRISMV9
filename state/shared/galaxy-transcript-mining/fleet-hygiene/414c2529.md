# fleet-hygiene session 414c2529 (2026-06-04, 24.2MB, spine 182KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `bd15f6512e`: Blackwell GPU synergy – removed 4080 refs, set Ollama GPU‑offload, verified 96 GB VRAM usage.  
- `4529d13a25` (`U-BOOTGRACE-PRODUCER-WIRE`): wired `bootStartedAt` into `mcp-server-supervisor.mjs`, `mcp-server-daemon.mjs`.  
- `b3026dfb51`: resilient Ollama pull infra (`ollama-resilient-pull.ps1`) with self‑resuming loop, orphan curl cleanup.  
- `50f598afcf`: U‑FGC‑2 lock‑orphan fix – rename→unlink + `.gitignore` guard, regression tests.  
- `c7d44e0dd1`: cry‑wolf advisory fix (`previewReclaimable`, reaper wiring, 5 new tests).  
- `dc4c7a1d5b`: boot‑guard default‑on (`bootGuardEnabled`) enabling boot‑grace producer.  
- `3e39feeaaa`: scrutiny‑fix – MaxTries wall‑clock deadline, anchored NAME match.

**DECISIONS**  
- Golf slot becomes canonical owner of fleet‑reaper & memory monitor (Doctrine 2026‑05‑16).  
- Boot‑grace flap‑prevention dormant until BOOTGUARD flag enabled; wiring complete.  
- Replace lock rename with `unlinkSync` in `chat-slots.mjs`; add `.gitignore` guard.  
- Adopt detached, self‑resuming Ollama pull loop for gpt‑oss 120b.  
- Wire `previewReclaimable` into reaper advisory to correct stale‑slot over‑reporting.  
- Flip watchdog boot‑guard to default‑on; activate boot‑grace producer.  
- Prioritize gaps by ROI; defer operator‑gated or high‑risk changes until approval.  
- Add transient‑tool hunter (grep/find/head) to canonical fleet‑reaper.

**OPERATOR DIRECTIVES**  
- `/checkin-golf`: force‑take golf slot, run `fleet-reaper-sweep.mjs`, bind to `golf-work`.  
- Keep reapers/monitors operational; check every 10 min.  
- Discovery loop: identify highest‑ROI in‑lane gap, verify, build/fix with tests+scrutiny or enumerate & check‑in if context insufficient.  
- Resolve MCP flapping by wiring boot‑grace producer and enabling BOOTGUARD.  
- Clean up ~28 k orphan lock files (`chat-slots.lock.released-*`).  
- Perform gpt‑oss completion check (list, smoke test, synergy verification, post‑completion cleanup).  
- Harden MCP server to handle fleet/agent load; prevent crashes.  
- Activate session‑scoped Stop hook: “look for inefficiencies, high ROI gap fills, dormant features…”.  
- Immediately start building missing transient‑tool hunter.

**FINDINGS / BUGS**  
- `mcp-server-supervisor.mjs` & `mcp-server-daemon.mjs` never wrote `bootStartedAt`; MCP flaps.  
- `chat-slots.mjs` lock rename left ~28 761 orphan files; fixed with unlink.  
- Repo contains 56 571 untracked transient artifacts (locks, temps).  
- gpt‑oss pull stalled (~27 GB at 212 KB/s) due to network degradation; resilient loop will retry.  
- Reaper advisory over‑reported stale slots (cry‑wolf); corrected.  
- Shared‑index foreign absorption hazard resolved by staging and peer commit reset.  
- U‑FGC‑3 slot‑worktree sprawl (~84 worktrees vs 26 slots) caused git fsmonitor daemons & orphaned processes.  
- build-node-embeddings spawned multiple orphan copies; orphan cleaner not firing on cadence.  
- grep/find/head orphan processes regenerate ~6× per 30 min; reaper blind spot.  
- Echo slot crash at 44 m tracked; window‑alive, not reclaimable.  
- 4 dead‑parent orphans reaped each tick.

**DOMAIN SPECIFICS**  
- **Fleet Reaper** (`fleet-reaper-sweep.mjs`): orphan janitor, GPU coordinator, Ollama routing hint emitter.  
- **Memory Monitor**: tracks RAM usage, triggers soft relief.  
- **MCP Server Watchdog / Connectivity Monitor**: monitors `:3100`, auto‑reconnect via `mcp-reconnect-action.mjs`.  
- **Ollama Serve / CPU Throttle**: GPU‑offloaded model serving, throttling logic.  
- **Slot Binding** (`chat-slots.mjs`): claim/reclaim logic, lock handling.  
- **Engines/Actions**: `chat-slots.mjs`, `fleet-reaper-sweep.mjs`, `ollama-resilient-pull.ps1`, `mcp-http-bridge.mjs`, `mcp-server-supervisor.mjs` + watchdog.  
- **Paths**: `H:/prism/scripts/`, `H:/prism/.claude/helpers/`, `H:/prism/state/shared/`.  
- **Galaxy‑only**: gpt‑oss 120b MoE integration, Ollama routing hint emitter, 26‑chat fleet orchestration.  
- **Transient‑tool hunter** for grep/find/head.

**TOOLS USED**  
- PRISM tools: `CronCreate`, `loop-state.mjs`, `fleet-reaper-sweep.mjs`.  
- Windows utilities: PowerShell `Get-ScheduledTask`, `Invoke-WebRequest`.  
- Skill hooks: `golf-slot-write-allowlist.mjs`, `golf-slot-reaper-guardian.mjs`.  
- Git Bash, PowerShell (`Get-ScheduledTask`, `Invoke-WebRequest`), `ollama.exe`, ultracode workflow engine, scrutiny‑3way, git index lock sweep hook.  
- `scripts/lib/fleet-reaper-stuck-hunters.mjs` (stuck‑bash + fsmonitor hunters).  
- `/dedup` command & duplication guard engines.  
- PRISM session hooks (`prism_session:action_search`, `prism_dev:copilot_check_duplication`).

**OPEN THREADS**  
- Enable BOOTGUARD flag (`PRISM_MCP_WATCHDOG_BOOTGUARD=1`) to activate boot‑grace.  
- Complete U‑FGC‑2 churn quarantine (remove 56 571 untracked files).  
- Finalize gpt‑oss synergy integration; ensure auto‑promotion on pull.  
- Persist lock‑orphan cleanup across future slot releases.  
- U‑FGC‑2 >90%: decide track‑vs‑ignore for ~16 k wiki pages.  
- MCP BOOTGUARD already enabled; next step concurrency/backpressure mitigation (plan banked).  
- U‑FGC‑3 slot‑worktree adoption: prune abandoned worktrees, eliminate fsmonitor daemons.  
- Investigate orphan persistence >10 min dead‑parent processes beyond current reaper scope.  
- Monitor resilient gpt‑oss pull loop; consider bandwidth throttling or alternate mirror if stalls persist.  
- Implement & wire transient‑tool hunter into canonical reaper; add tests.  
- Clear session hook once condition satisfied.  
- Ship `reference_fleet_monitor_consolidation_plan_2026_06_04.md` to close blind spot.  
- Monitor orphan regeneration post‑fix.
