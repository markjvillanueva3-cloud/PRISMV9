# fleet-hygiene session 14c8b81e (2026-06-09, 5.4MB, spine 74KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit 316872f7 – `scripts/call‑engine.mjs`, `.cmd`, `.sh` (server‑free engine invoker) + `call-engine.test.mjs` (8‑case node:test suite).  
- Commit 38af3407 – `27-wsl-memory-guard.mjs` (WSL 16 GB cap monitor), its tests, and `install-wsl-memory-guard-task.ps1` (15‑min scheduled watchdog).

**DECISIONS**  
- MCP server is optional for code‑base navigation; built a thin dispatcher‑replacement that imports engines directly via tsx.  
- WSL memory overrun was root cause of “API limit” errors; implemented a guard that reads `.wslconfig`, reports overrun, and can enforce `wsl --shutdown`.  
- Use PowerShell for zombie‑tsserver reaper (dry‑run + live kill) to reclaim ~3 GB.  
- Adopt node:test for regression testing of pure logic in the guard; keep CLI guarded by `import.meta.url` check.

**OPERATOR DIRECTIVES**  
- “commit” – executed, staged and committed both toolsets.  
- “continue from where you left off” – responded with status updates on pressure relief and remaining actions.  
- “fix whatever is constantly causing the api limit error” – diagnosed memory‑pressure issue; applied fixes.

**FINDINGS/BUGS**  
- API errors were due to host memory pressure: WSL committed ~96 GB against a 16 GB cap (cap not enforced) and three Ollama models (~65 GB).  
- tsserver leaks (~3 GB) identified and killed.  
- `.wslconfig` present but never applied; `wsl --shutdown` needed to enforce cap.  
- Dispatcher logic is thin Zod‑router → engine.method; all pure compute dispatchers can be invoked directly.

**DOMAIN SPECIFICS**  
- Engines: `src/engines/*.ts` (e.g., `prismSelfAwarenessEngine`, `CuttingForceEngine`).  
- Dispatchers: 107 thin routers (`prism_*`) that validate with Zod then call engine methods.  
- Actions: semantic search (`prism_memory:semantic_search`), machine live (`prism_machine_live:*`), realtime WebSocket push, local Ollama offload (`prism_local:*`).  
- Metrics/paths: `vmmemWSL` commit, `llama-server` resident memory, `tsserver` orphan count.  

**TOOLS USED**  
- PRISM harness tools: `Read`, `Write`, `Glob`, `Grep`, `Bash`, `Node`.  
- Dispatchers: all thin Zod routers in `src/engines`.  
- Scripts/hooks: `scripts/call-engine.*`, `27-wsl-memory-guard.mjs`, `install-wsl-memory-guard-task.ps1`, `02-kill-zombie-tsservers.ps1`.  
- Testing: `node:test` framework.  

**OPEN THREADS**  
- Run `wsl --shutdown` (requires user terminal) to reclaim ~80 GB and enforce the 16 GB cap.  
- Install the WSL guard scheduled task (`install-wsl-memory-guard-task.ps1 -RunNow`).  
- Wire the guard into `fleet-task-health` monitored‑task list for Stop‑hook safety net.  
- Optionally expose `call-engine.mjs` as a `/call-engine` skill or Prism action for discoverability.
