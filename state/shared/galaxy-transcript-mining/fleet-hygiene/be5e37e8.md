# fleet-hygiene session be5e37e8 (2026-05-19, 28MB, spine 135KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑FGE01` – 62b5794101: STEP geometry corpus → evidence‑based build sequence (`cad_class_build_sequence_evidence`).  
- `U‑FGE02` – c60f6c9396: wired evidence ranking into Fusion360 orchestrator (`cad_class_drive_build`).  
- `U‑BPA‑CONSUMER` – 6cbe5b1561: offline events consumer for blueprint‑accuracy loop.  
- `U‑TDP01` – 5e3048ad71: print‑to‑CAM training driver (extract → CAD → CAM → record).  
- `U‑TDP02` – 8d533c1e10: batch harvester with idempotent registry.  
- `U‑TDP03` – 99288aec44: extraction aggregator → per‑class templates (Welford stats).  
- `U‑TDP04` – 5709f19d7b: OCR extraction benchmark runner vs CAD GT (stubbed, returns null).  
- `U‑TDP05` – 1bc36d949d: CAD‑derived ground truth extractor.  
- `U‑TDP07` – d85573b7d4: deterministic embedded‑text PDF dimension extractor (inline‑± parser, OCR guard; SolidWorks split‑token parser).  
- `U‑TDP08` – new regexes for paired‑tolerance bands (`1.002/1.000`) and Rc hardness ranges (`Rc 58‑60`, `55‑57 Rc`).  
- Picker‑fix commit `1576134f55`: milestone‑name keyed exclusion in `priority‑queue.mjs`.

**DECISIONS**  
- Use `/checkin‑mike` slot‑claim wrapper; ship only U‑TDP07/08 to this slot.  
- Split extraction: deterministic embedded‑text (~3.5 % of JM Die PDFs) + vision OCR (~96.5 %) using `qwen2.5vl:7b`.  
- Adopt `qwen2.5vl:7b` (fits 10 GB RTX3080); discard `llama3.2‑vision:11b` (OOM on 10 GB, fits only 16 GB RTX4080).  
- Add inline‑± regex parser and OCR fragment guard to `pdf-text-extract-lib`.  
- Activate offline consumer (`blueprint‑accuracy‑consumer`) for drift loop; use evidence ranking in `CADClassFeatureLibraryEngine.buildSequenceForEvidence`.

**OPERATOR DIRECTIVES**  
- “continue” training of OCR print reading.  
- Confirm system setup ready for training; utilize GPU/docker on 4080 if needed.  
- Slot worktree discipline: commit only to own slot (`slot/mike`).  
- Never inline Kienzle/Taylor/material physics constants; import from `src/physics/constants.ts`.  
- Never create stub engines or weaken assertions (`.skip` tests); R12 fail‑loud for missed dimensions.

**FINDINGS / BUGS**  
- moondream hallucinated dimensions, truncated JSON; missing dot in tolerance regex → fabricated values.  
- Parser regex bugs: missing dot, unicode minus, chained ±; low extraction rate (~13 %).  
- Vision model load issues due to GPU passthrough and memory limits; `llama3.2‑vision:11b` OOM on 10 GB RTX3080.  
- OCR fragment guard required to reject garbled text fragments.  
- Event consumer state.json empty before adding offline consumer; now populated.  
- Priority‑queue namespace mismatch leaked HOOK‑SYNERGY units; fixed by milestone‑name filter.

**DOMAIN SPECIFICS**  
- Engines/Adapters: `CADClassFeatureLibraryEngine`, `BlueprintExtractionRAGEngine`, `GroundTruthRegistryEngine`; U‑TDP01–06 (training driver, harvester, aggregator, OCR benchmark grader stubbed, ground‑truth GT, vision adapter pending); U‑TDP07 deterministic extractor; U‑TDP08 regex patterns.  
- Metrics: precision/recall/F1; dimension mean/stddev and tolerance distribution; extraction confidence; dimension count per PDF; error rates vs GT; drift loop `state.json`.  
- Paths: `H:/prism/.claude/helpers/chat-slots.mjs`, `audit-roadmap-drift.mjs`, `scripts/lib/pdf-text-extract-lib.mjs`, `scripts/harvest-pdf-text-batch.py/mjs`, `scripts/blueprint-accuracy-consumer.mjs`; JM Die corpus (`JM DIE/_PART LIBRARY/...`); consumer logs (`blueprint‑accuracy-events.jsonl`).

**TOOLS USED**  
- PRISM slot‑claim system (`/checkin-mike` wrapper), full `/checkin` pipeline, `audit-roadmap-drift.mjs`, `build-state`, `system-viz ping`.  
- Ollama API for vision models; PyMuPDF (`fitz`) PDF→PNG rendering.  
- Node.js test harness (`node:test`), TypeScript compiler (`tsc`).  
- Git hooks: `chat-slots.mjs reclaim/claim`, `milestone-tracker.mjs`; `priority‑queue.mjs`, `shipped-units-source-of-truth.mjs`.  
- External models: `llama3.2‑vision:11b`, `qwen2.5vl:7b`.

**OPEN THREADS**  
- Pull and integrate `qwen2.5vl:7b` vision OCR as U‑TDP06 adapter; validate accuracy on 87 % image‑only PDFs.  
- Wire `pdf-text-extract-lib` into U‑TDP04 benchmark grader for live training signals.  
- Complete full CAD→CAM generation loop (CAM program generation pending).  
- Scale harvesting of entire JM Die corpus (~600 PDFs) and automate end‑to‑end pipeline; measure delta vs current sub‑dir results.  
- Add additional regex patterns: comma‑decimal normalization, MAX/MIN qualifiers, slash‑paired tolerance handling (`U‑TDP09`).  
- Train LoRA/RAG models on extracted data once vision path functional; implement ground truth extraction from CNC programs.
