# fleet-hygiene session 19dff632 (2026-06-09, 11.5MB, spine 95KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- e44a3a1592: wired 4 orphan `PayrollLiabilityFilingEngine` methods into `businessDispatcher`.  
- e649790e76: added fifth orphan `remitLiability`; updated dispatcher test suite (14 round‑trip cases).  
- f069052772: verified all 6 stale audit items shipped; no rebuild needed.  
- 9dfd621910: committed reusable Ollama transcript miner (`scripts/mine-hotel-transcripts.mjs`).  
- 61518eb988: fixed four miner bugs (empty‑response, noise filtering, combined glob).  
- 099e6b92bd: committed final hotel forge roadmap spec.  
- Guard test now passes 20/20 (17 parametric, 1 coverage, 2 meta-tests).  
- Committed two scoped files (+203 insertions) and a single‑file fix (~23+4 changes).

**DECISIONS**  
- Use Ollama for transcript summarization; ultracode workflow for roadmap synthesis.  
- Default grunt‑work coder `qwen2.5-coder:32b`; reserve `gpt-oss:120b` for heavy multi‑file work.  
- Do not rebuild audit items already shipped; focus on wiring orphaned methods and verifying dispatchers.  
- Persist miner script/digests as reusable tooling; keep roadmap in shared spec.  
- Broaden guard regex to match dispatcher default “Unknown business action: ${action}”.  
- Add `{note:"method not callable"}` handling (dispatcher lines 5813‑5825).  
- Update meta‑test to assert actual dispatcher output.  
- Seed one lead via `seedFromHints` for guard detection.

**OPERATOR DIRECTIVES**  
- `/checkin-hotel`: recover hotel context.  
- `continue` / `start`: proceed with remaining hotel units.  
- Use Ollama for efficiency – confirmed.  
- Generate brainstormed forge roadmap – committed to spec.  
- Resume rate‑limited workflow after account switch.

**FINDINGS/BUGS**  
- 5 orphan `PayrollLiabilityFilingEngine` methods missing from `businessDispatcher` (now wired).  
- Mobile engines all wired except `MobileVoice` (tagged).  
- Audit counts stale; many claimed “orphan” engines already wired.  
- Guard regex previously missed dispatcher default case → false‑wire for `marketplace_lead_get` returned null.  
- Probe used wrong param `{id}` instead of `{supplierId}` → `getLead(undefined)` returned null.  
- Meta‑test falsely proved R9 with fabricated lowercase string; fixed by regex update.

**DOMAIN SPECIFICS**  
- `PayrollLiabilityFilingEngine`: 5 public methods + positional `remitLiability`.  
- `businessDispatcher.ts`: added 5 enum entries & switch cases; lazy `getEngine`.  
- Mobile engines list (`EmployeeShopFloorMobile`, `MobileLookup`, …).  
- Allowlist `src/data/business-dispatch-allowlist.ts` (17 read‑only actions).  
- `MarketplaceSeedingEngine` line 173 with `seedFromHints`.  
- Action `marketplace_lead_get`: engine call `getLead(params.supplierId)`; leads keyed by `supplierId`.  
- Dispatcher default at lines 7640‑7641: `result = { error: \`Unknown business action: ${action}\` }`.

**TOOLS USED**  
- Ollama (`qwen2.5-coder:32b`, `gpt-oss:20b`).  
- ultracode workflow (8 agents, resumable).  
- `scripts/mine-hotel-transcripts.mjs`; `ask-ollama.mjs` helper.  
- `slot-bind-enforce.mjs`, `chat-slots.mjs`.  
- PRISM guard test harness, detector regex, meta-tests.  
- Git contention handling (`index.lock` clearance), `__resetForTests()` API.  
- 3‑of‑3 scrutiny gate (per-arm weighting).

**OPEN THREADS**  
1. **U-HOTEL-PORTAL-PERSISTENCE** – persist 4 in‑memory `CustomerPortalEngine` maps to SQLite‑WAL; needs fresh budget & E2E test.  
2. **ALLOWLIST-WRITE-REVIEW / REALTIME-VERIFY** – build after persistence.  
3. Resolve `marketplace_lead_get` null-return ambiguity (detector flagged potential false-wire).  
4. Two remaining heavy build units still budget‑blocked; awaiting completion before next loop.
