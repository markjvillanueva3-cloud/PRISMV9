# fleet-hygiene session 4ded360f (2026-05-14, 0.3MB, spine 4KB, 1 slice(s), model qwen2.5-coder:32b)

- **DECISIONS**
  - `/checkin` command used for fleet check-in, drift/conflict/commit guard.
  - `--golf` option restricts chat to hygiene slot with specific path allowlist.
  - `--roadmap devtools` and `--roadmap revenue` options specify roadmap focus.

- **OPERATOR DIRECTIVES**
  - Run `/checkin` after every `/compact`.
  - Use `--topic <slug>` to explicitly bind the topic.
  - Use `--force --confirmRecent --preferSlot <name>` to force-take a recently claimed slot.
  - Use `--golf` for hygiene tasks.
  - Use `--roadmap devtools` or `--roadmap revenue` to specify roadmap focus.

- **DOMAIN SPECIFICS**
  - Hygiene slot (`--golf`) has specific path allowlist and is restricted to certain files and directories.
  - Roadmaps: `BACKEND-DEVTOOLS-RGS6-MEGA-ROADMAP` (devtools) and `REVENUE-ROADMAP-v7.6` (revenue).

- **TOOLS USED**
  - `/checkin` command with various flags for slot management and roadmap focus.

- **OPEN THREADS**
  - Documentation of the full list of allowed paths in the hygiene slot.
  - Completion of the revenue roadmap section cut off in the transcript.
