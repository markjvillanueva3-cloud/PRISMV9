# mit-curriculum session 0fb9f93e (2026-05-27, 39.2MB, spine 110KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Regenerated `LAUNCH-PRISM-FLEET.bat` – absolute paths (`%WT%`, `%PWSH%`, `%CLAUDE%`) + 40 MB JSONL size guard, fallback to `/checkin‑<slot>`.  
- Updated `scripts/regenerate-launch-fleet.mjs`: added size & age guards (default 90 min), absolute path logic, 15 s inter‑quadrant stagger.  
- Added MCP daemon preflight (`http://127.0.0.1:3100/health`) before any chat launch.  
- Implemented fleet‑reaper always‑on via scheduled task `PRISM Fleet Reaper`; in‑session monitor; immediate sweep killed 143 orphan `node.exe` & 493 orphan `bash.exe`.  
- Final commit `61074fa740` (iter 28) resolved all TS errors in `mcp-server/web`.

**DECISIONS**  
- Use absolute paths for external tools to avoid PATH inheritance issues across UAC elevation.  
- JSONL size threshold: default 40 MB; fallback to fresh `/checkin‑<slot>`.  
- Preflight MCP daemon before launching any chat to eliminate thundering herd race.  
- Stagger quadrant launches by 15 s to spread Anthropic API calls and avoid rate limits.  
- Keep fleet reaper always‑on: register global scheduled task, run in‑session monitor only for golf, perform manual sweeps on demand.  
- Add size & age guards to `regenerate-launch-fleet.mjs`; default age threshold 90 min.  
- Relax `noImplicitAny` in workspace tsconfig for legacy code; keep strictness elsewhere.  
- Cascade‑first strategy: bulk fixes on shared type errors before per‑file edits.

**OPERATOR DIRECTIVES**  
- Ensure fleet reaper is running; kill orphan/zombie nodes, git, bash, grep tasks still open.  
- Fix prism fleet launcher – alpha didn’t launch with the rest of the fleet.  
- Update `prism fleet.bat` to use most up‑to‑date chats per slot; coordinate with alpha, run reaper/monitors, clear cache, kill orphan tasks.

**FINDINGS / BUGS**  
- Alpha tab crashed: JSONL (~53 MB) exceeded resume size limit.  
- MCP daemon race condition spawned 20 supervisors simultaneously – resolved by preflight health check.  
- Anthropic API rate‑limit banners when launching all 20 chats at once; mitigated by staggering quadrants.  
- Fleet reaper scheduled task missing initially; now registered & verified.  
- Orphan `node.exe`/`bash.exe` consumed ~61 % RAM – sweep removed them.  
- Stale chat slots: echo (131 min), foxtrot (123 min), victor (92 min) – now fall back to `/checkin‑<slot>`.  
- TS error clusters 2322, 2345, 2339, 2352/2554 resolved.

**DOMAIN SPECIFICS**  
- Chat slots & JSONL: `H:/prism/state/shared/chat-slots.jsonl`; per‑slot handoffs (`zebra-work`, `golf-work`).  
- Launcher scripts: `LAUNCH-PRISM-FLEET.bat`, `scripts/regenerate-launch-fleet.mjs`.  
- MCP server: HTTP daemon on port 3100, bridge script `mcp-http-bridge.mjs`.  
- Fleet reaper: skill `fleet-reaper`, scheduled task `PRISM Fleet Reaper`, monitor logic in `/checkin-golf`.

**TOOLS USED**  
- Node.js (`.mjs` helpers), PowerShell, cmd.exe, Windows Terminal (`wt.exe`).  
- Claude CLI (`claude.cmd`/`claude.exe`).  
- Git, Bash, npm, pip, Docker Desktop, NVIDIA NIM, Ollama.  
- Scheduled tasks via `schtasks`.  
- TypeScript compiler (tsc) with custom tsconfig overrides; @types packages.

**OPEN THREADS**  
- Verify all chats connect to the MCP server immediately on launch (post‑preflight).  
- Ensure fleet reaper continues to run and clean orphans after future chat crashes.  
- Next focus: integration testing & deployment readiness.
