# mit-curriculum session 92ef25c0 (2026-05-27, 25.7MB, spine 88KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**

- 60 curriculum modules (courses 44‑58, 52‑55, 56‑58, 59, 60) – all wired into `CurriculumEngine.ts` and exposed via web blueprints (`web/src/data/academy.ts`).  
- 20 wiki entries (15 machine runbooks + 3 methodology + 2 deep‑research).  
- 8 946 tribal feed entries: 154 machine + 43 data + 8 752 page‑level from JM‑Die corpus.  
- 47 graph‑node bridges (`post‑processor`, `speed‑feed`, `tooling`, `workholding` engines).  
- Free local video extraction stack (Whisper, Ollama vision/text models) – commit `f15b58ba38`.  
- Page‑by‑page PDF extractor (pypdf) – full corpus drain (`3eaef94ca7`).  
- AcademyHub drill‑down UI (`/learning/academy`) with domain/sub‑category selection and “Generate Optimized Coursework” button – commit `a7a4e1b4ef`.  
- PWA infrastructure: `manifest.webmanifest`, `sw.js`, dev‑seed page, cloudflared tunnel script – commit `4ec78cc987`.  
- Video embed support (YouTube iframe) and curated picks (`youtube-picks.ts`) – commit `56930728f5`.

**DECISIONS**

- Slot‑locked `/checkin‑lima` wrapper to enforce exclusive work on the “lima” slot; uses chat‑slots helper for claim/reclaim.  
- Adopt free local AI stack (Whisper, Ollama) to replace paid APIs for video learning.  
- Use pypdf + regex scoring for PDF extraction; avoid external OCR/translation services.  
- PWA‑first approach: manifest & service worker mandatory for mobile install; cloudflared tunnel used for quick HTTPS access during dev.  
- Per‑employee dev‑seed page to bootstrap role‑based clearance without ERP integration.  
- AcademyHub drill‑down UI chosen over flat catalog to reduce cognitive load and enable auto‑prerequisite ordering.  
- R12 fail‑loud policy: defer physics derivations, blisk gap, hard‑machining content until source PDFs are available.

**OPERATOR DIRECTIVES**

- “Continue from where you left off.”  
- Earlier directives (implicit in transcript): expand courses to all domains, generate optimized coursework button, auto‑select tracks, persist picks, add video sections to courses, deploy permanent URL, integrate real ERP records, enable offline lesson caching, push notifications.

**FINDINGS/BUGS**

- Encrypted PDFs (3 of 8 easiest) – need source or unlock.  
- Missing `manifest.webmanifest` & incorrect dev‑seed redirect (`/academy` → `/learning/academy`).  
- Cloudflared tunnel pointed to rogue Express on `127.0.0.1:3100`; fixed by killing process and pointing to Vite’s 3100.  
- R12 fail‑loud: no corpus pages for blisk; no hard‑machining PDF coverage.  
- Dev‑seed page now correctly redirects after route fix.

**DOMAIN SPECIFICS**

- `VideoLearningEngine` (FFmpeg + Whisper + Vision) → `/video-learn`.  
- `CurriculumEngine.ts` – imports, RICH_MODULES, courseDefinitions.  
- `pdf-learn` pipeline (pypdf extraction, regex scoring).  
- PWA assets (`manifest.webmanifest`, `sw.js`) and cloudflared tunnel script.  
- AcademyHub component (`/learning/academy`) with domain/sub‑category chips and topo‑sorted course list.  
- Per‑employee track JSON (`EMPLOYEE_TRACKS`).  
- Graph nodes: `post‑processor`, `speed‑feed`, `tooling`, `workholding` engines.

**TOOLS USED**

- PRISM helpers: `chat-slots.mjs`, `/checkin.md`.  
- Video extraction: `video_learn_local.py`, `whisper_local.py`, `vision_analyze_ollama.py`, `knowledge_extract_ollama.py`.  
- PDF extraction: `pypdf` (Python).  
- PWA tooling: Vite dev server, cloudflared, `vercel.json`, `netlify.toml`.  
- Testing: Playwright smoke spec (`apprentice-smoke.spec.ts`).  
- CI artifacts: commit hashes, test suites.

**OPEN THREADS**

1. **AcademyHub auto‑select & persist picks** – implement `U-ACADEMY-TRACK-AUTOSELECT` and `U-ACADEMY-PICKS-PERSIST`.  
2. **Add video sections to all 60 courses** – batch insert YouTube URLs using `youtube-picks.ts`.  
3. **Permanent deployment URL** – finalize Vercel/Netlify production build.  
4. **Real ERP employee records** – replace dev‑seed with `/api/v1/auth/login` integration.  
5. **Offline lesson caching** – enable SW to cache lesson assets for offline use.  
6. **Push notifications** – add FCM/APNs support for course reminders.
