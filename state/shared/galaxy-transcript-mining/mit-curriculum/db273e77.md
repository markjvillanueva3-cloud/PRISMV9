# mit-curriculum session db273e77 (2026-06-10, 158.4MB, spine 1339KB, 14 of 16 slice(s), HEAD+TAIL sampled, model gpt-oss:20b)

**SHIPPED**  
- `e5ad4ea802` – U‑ALPHA-HWSYNC-RETUNES (9 files + vitest.config.ts update)  
- `f9e953f0e8` – MEMORY.md business & AI‑training updates  
- `786bd70566` – CLAUDE.md §5/6/7 populated; MEMORY.md ownership note  
- `873446a147` – quality galaxy claimed by alpha; CLAUDE.md & MEMORY.md finalized  
- `792beb75e8` – obsidian‑mem‑dir resolver, node_* stubs removed, split‑brain fix (C:→H: feed)  
- `36105372ec` – U‑OLLAMA‑NAV‑ENFORCE + model pin (`gpt‑oss:20b`)  
- `be52720b32` – U‑DARK‑WIKI‑RANK (rank 32 630 dark wiki files → 71 high‑value embeds)  
- `U‑LINT‑ORPHAN‑OOM` – vault‑orphan detector OOM fix  
- `U‑GRAPH‑STREAM‑DEGREE` – streaming pass over 643 MB system‑graph to avoid heap OOM  
- `127234e940` – ollama‑nav‑rerank CLI (verified navigation re‑rank)  
- `0c641ef45a`, `4b299e313b` – auto‑fire advisory hook for nav rerank  
- `530afadcfa` – system‑health scripts (`kill-zombie-tsservers.ps1`, `memory-pressure-auto-relief.ps1`)  
- `0e12da9955` – `.gitignore + file‑relocation‑allowlist.json`  
- `92301e5574`, `8119ab7cb6` – commit‑msg drafter (`subjectShape`)  
- `1175a6f26b`, `8c57f02d77` – line‑anchored file digest lever  
- `e928ef5010`, `71c817c2a2` – loop‑narrate consumer (exit‑code verdict)  
- `0acb1dcbc9`, `31fae0eaf8` – large‑read advisory hook (auto‑fires on >600‑line reads)  
- `8c73bd5aaf`, `b10e6829cd`, `6980943ba8` – doc‑reflection updates  
- `be9182dca7` – U‑AUTOSTART‑LOOP‑GOAL (auto‑start `/startup‑<slot> /loop [10m] /goal`)  
- `c85d64e407`, `d926965854` – U‑SUBAGENT‑GALAXY‑PACK (inherit parent slot’s galaxy domain pack)  
- `b70ec2bb3c` – U‑GOAL‑FORCE‑100PCT (inject rule 5: 100 % completion across galaxies)  
- `9fcda446a1` – stop-task-boundary‑compact-nudge.mjs  
- `61a6288d0e` – ollama-search-rerank.mjs + ask-ollama.mjs  
- `7dec157db0` – HS‑01 root fix (explicit `--session-id`, peer‑leak removal)  
- `e81dec5cba` – env‑anchor fix (`CLAUDE_CODE_SESSION_ID`)  

**DECISIONS**  
- Shift slot ownership from alpha to golf for fleet hygiene.  
- Keep 47 migration‑related PRISM tasks disabled until operator clears them.  
- Use `commit -- <paths>` (U‑FGC‑1) to avoid peer churn in shared index.  
- Adopt async lint (`auto-lint-post-edit.mjs`); discard sync eslint hook.  
- Ship G2 file:line citation doctrine; no further hooks needed.  
- Claim quality galaxy, populate CLAUDE.md with verified engine constants; do not fabricate cross‑galaxy gates.  
- Stop session‑bound model pull; hand over to golf’s durable `ollama-resilient-pull.ps1`.  
- Use PowerShell `Start‑Process` detached for pulls so they survive session end.  
- Adopt native auto‑compact@90 % for fleet compaction; Zulu SendKeys unusable due to WT‑tab HWND wall.  
- Replace 24 caller edits with single per‑process env var anchor (`CLAUDE_CODE_SESSION_ID`).  
- Use verified‑offload keystone: model proposes → code verifier → fail‑safe fallback.  
- Keep goal‑force rule across all galaxies; ensure consistency of galaxy pack inheritance.  
- Implement per‑slot live actuation lever overriding global dry‑run.

**OPERATOR DIRECTIVES**  
- Finish HW‑sync tasks, verify Ollama models match new CPU/GPU stack.  
- Audit Docker/NVIDIA NIM config for Blackwell compatibility (report‑only).  
- Verify atomic‑level synergy across all systems (master graph, galaxy CLAUDE.md/MEMORY.md, souls, model routing).  
- Keep 47 scheduled tasks disabled until migration complete.  
- Take over for golf to pull models; keep pulling then claim quality galaxy.  
- Relook into earlier X articles for alpha comparison.  
- Ensure Ollama models match new CPU/GPU, Docker/NVIDIA‑NIM synergy, all systems atomically synchronized.  
- Force‑take alpha slot, bind handoff to `alpha-work`, run `/startup` pipeline.  
- Run autonomous builds every 10 min (`/loop [10m]`) and execute immediately.  
- Maximize local LLM utilization; enforce Ollama for reads/searches/navigation; fully wire Obsidian/PSN/system‑viz; apply learned patterns.  
- Decide on force‑tracking `yolo-mode.md` in git.

**FINDINGS / BUGS**  
- PowerShell UTF‑8 parsing bug in soft‑config script (`—` mis‑decoded); fixed with explicit UTF‑8 flag.  
- Vitest 4 deprecation: `poolOptions.threads.*` ignored; migrated to `test.maxThreads`.  
- Main‑tree-write-block slot mismatch (H:/prism resolved to papa instead of alpha) – shared‑tree contention bug fixed.  
- Stalled Ollama pulls: 93 % freeze due to connection limits; each restart re‑downloads layer from start.  
- `stop_on_failing_tests.mjs` present but unwired in settings.json.  
- PowerShell UTF‑8 parsing bug in soft‑config script (duplicate mention).  

**DOMAIN SPECIFICS**  
Engines/Actions:  
- U‑FGC‑1 commit‑mutex, chat-slots.mjs slot claiming, checkin.md pipeline, audit-roadmap-drift.mjs, main-tree-write-block, vitest.config.ts, MINBatchExtractorEngine.ts, McxBatchExtractorEngine.ts, gpu_health.py, GpuStackHealthEngine.ts, memory-index-search-lib.mjs, precompact-memo-emit.mjs, psn-leg-state-inject.mjs, stop-auto-capture-per-slot.mjs, stop-memory-size-watchdog.mjs, ollama‑verified‑offload.mjs, ollama-nav-rerank.mjs, ollama-search-rerank.mjs, ask-ollama.mjs, precompact-handoff.mjs, session-start-auto-resume.mjs, stop-task-boundary-compact-nudge.mjs, stable-session-id.mjs, obsidian-mem-dir resolver (`scripts/lib/obsidian-mem-dir.mjs`), nav-rerank-advisory hook, large-read advisory hook, loop-state.mjs, loop-iteration-inject.mjs, goal-prereq-inject.mjs, subagent-start-context.mjs, zulu-orchestrator-sweep.mjs.  

Paths & Metrics:  
- `state/shared/slot-souls/<slot>.md`, `C:/Tools/python` fix, `PRISM_OBSIDIAN_MEM_DIR`, `PRISM_MEMORY_DIR`, `H:/prism/knowledge/memories`, `C:/Users/wompu/.claude/projects/H--prism/memory`.  
- `PRISM_EMBED_CONCURRENCY=16`.  
- Token cuts 82–95 % via dedup.  
- doctrineSurface 25→1.  
- 176 tribal reference nodes.  
- System‑viz engine count 3697 wired / 89 unwired.  
- Auto‑compact trigger at 90 %.  
- Context usage 55–85 % for task‑boundary nudge.

**TOOLS USED**  
PRISM CLI helpers (`chat-slots.mjs`, `stable-session-id.mjs`, `per-agent-handoff.mjs`), Ultracode workflow, Hermes‑workflow, System‑viz, Obsidian sync scripts, Scrutiny 3‑way (`scrutiny-3way.mjs`), PowerShell `Start‑Process`, `ollama-resilient-pull.ps1`, Git add/commit pattern, CronCreate/CronDelete, `callOllamaOnce`, `ollama-fanout.mjs`, `lineAnchoredVerifier`, `isCleanExitZero`.

**OPEN THREADS**  
- Re‑run rerank arm B (rate‑limited) and consolidate search/nav‑rerank dedup.  
- Fully fix HS‑01 stable‑session‑id bug; update findHandoff anchoring.  
- Implement remaining verified‑offload queue items: advisory‑hook retrofit, scrutiny pre‑screen offload.  
- Explore full‑fleet self‑compaction strategy beyond WT‑tab limitation (per‑window slots).  
- Full rollout of `--session-id` anchor to all callers.  
- Monitor MCP‑down handling and token‑budget discipline for future builds.  
- Decide on force‑tracking `yolo-mode.md` in git.  
- Continue ultracode assessment for further precompaction/compaction optimizations.  
- Implement per‑slot live actuation lever overriding global dry‑run.  
- Parallelize full‑fleet sweep to reduce > 2 min enumeration.  
- Improve lock reclaim on killed sweeps (PID‑liveness check).  
- Verify goal‑force rule across all 34 galaxies; ensure consistency of galaxy pack inheritance.
