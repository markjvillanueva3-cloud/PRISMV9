# mit-curriculum session 451f7328 (2026-05-25, 64.7MB, spine 149KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**

- Envelopes & close‑outs: `bd6931867b` (WEDM‑NEXT‑MS0 U‑WN06+U‑WN08 envelope flip), `27832ae6f9` (MS‑P1.5‑ONESHOT/U‑P1.5‑OS‑01 close‑out).  
- PSN automation: `4606d6066a` (PSN‑INCORPORATION‑MS0 envelope, 105 units); `9d2c0f952f` (`scripts/psn-incorp-automate.mjs` + stop hook); `0d8473a75b` (`/psn‑automate` skill, 58 plans); `58a480d778` (`PSNIncorporationOrchestratorEngine.ts`, 13/13 tests); `84c5010fd1` (dispatcher actions `prism_dev:psn_incorp_*`).  
- SVI enhancement & moat math: `dd38559c21` (`SVIEnhancedCalculatorEngine.ts` + `SVIMoatFormulaEngine.ts`); `9491badb25` (`U‑SVI‑E02` reward extension); `cf6730381f` (`U‑SVI‑E04/E05/E06` live refresh, path‑to‑perfect, first real Ψ measurement); `ad5f4dcc08` (`U‑SVI‑E03/E08/E09` system‑viz roost, Kolmogorov wrapper, MI learner); `ff8318b3d9` (`U‑SVI‑E10` Monte Carlo competitor sim + `U‑PP01` JMDieMachineEnvelopeCatalogEngine); `5344ec2c69` (`U‑PP02/U‑PP03` interval arithmetic predicate, cert orchestrator).  
- Program‑proof engine skeleton: `8088f29ab6` (PROGRAM‑PROOF‑MS0 U‑PP07‑10 topology layer), `6340aa19f6` (SVI‑ENHANCE‑MS0 E07 moat formula integrated).  
- Misc utilities: `bee9828667` (`scripts/psn-autonomy-data-ingest.mjs`, 194 events captured); `069d9ab492` (`PSNAutonomyLoopEngine.scoreEvent({type:"psi_delta"})` + trainer‑manifest wiring); `d8fcf6ef82` (automation layer for P2–P5 primitives).  
- QUOTING‑PIPELINE‑MS0: commits `7eb093a0f6` + `d399233c84` (full end‑to‑end quoting stack, 8 actions, Express bridge, React page, PWA SW).  
- JM‑DIE‑FINANCIAL‑BASELINE‑MS0: commits `13362c6e7f`, `U-JM04` bundle (301 218 files ingest, baseline JSON, PSN psi_delta feed).  
- JM‑DIE‑PROGRAM‑ANALYSIS‑MS0: commit `7eb093a0f6` (program parsing, time‑in‑cut, FMV calculation, 15‑case E2E test).  
- Goal‑16 (Ledger): commit `13362c6e7f` (100 % coverage ledger `jm-die-scan-ledger.jsonl`, coordinator, UI panel).  
- Role‑aware document usage: commit `0f4702ba53` (`JMDieDocumentQueryEngine`, dispatcher actions, UI panel).  
- Training loop & Xometry‑style inputs: commits `cabbc07257`, `95f865e0bc`, `2975d98fc8` (QuotingTrainingLoopEngine, XometryStyleQuoteInputsEngine, OutsourceRecommenderEngine, ScenarioGeneratorEngine, OutsideKnowledgeSourceCatalogEngine, QuotingDeepReasoningBridgeEngine).  
- Calibration engine: commit `060e0189a1` (`QuotingCalibrationEngine`, derive/apply/measure).

**DECISIONS**

- Use forced slot claim `/checkin-charlie`; all commits prefixed `[MAIN] [BOOTSTRAP‑SLOT‑ENFORCE]`.  
- Delegate full `/checkin` pipeline after claim; split into slot‑claim and dev phases.  
- Sidecar integration of `audit-close-out-candidates.mjs` (P2) to capture silent drift automatically.  
- Build PSN automation kernel: script + skill + stop hook + orchestrator engine, wired via MCP dispatcher (`prism_dev`).  
- Adopt 5‑axis moat formula for SVI (coverage, depth, cross‑coupling, quality, compounding rate).  
- Use `execFileSync` only; block `execSync`; external commands satisfy security hooks.  
- Defer wiring of large dispatchers when collision risk > YELLOW; document deferral per `feedback_conflict_fork_rule`.  
- Adopt PSN Synergy Network (11 legs) as core integration layer.  
- Modular “engine + dispatcher” pattern; each feature in its own TS module, exposed via `prism_quoting` actions.  
- Append‑only JSONL ledger (`jm-die-scan-ledger.jsonl`) with SHA‑short dedup and R12 error counting.  
- Parallel extraction agents: default 3+ workers per batch (feedback_parallel_agents_default_for_extractions).  
- Safety tier Ω≥0.95, S(x)≥0.98; never inline hard constants – import from `src/physics/constants.ts`.  
- No public H:/prism data publication.

**OPERATOR DIRECTIVES**

- `/checkin-charlie /loop [5m] /goal` – refresh close‑out audit, complete goal.  
- “do deep research on what else we can incorporate into PSN with high ROI value.”  
- “do deep research on learning and reasoning to add to your list.”  
- “build everything we need … develop the web app page and phone app for live testing …” (final goal).  
- “scan every single file … keep track of what’s already been scanned.”  
- “add usages for all documents throughout the different employee roles and app features.”  
- “develop training loop to improve accuracy… add Xometry‑style inputs, suggest outsourcing, test millions of scenarios, use outside resources, improve NN/GNN/deep learning.”  
- “exhaust all scenario tests | complete and log test results to train the system … synergize with PSN and JM die shop data.”

**FINDINGS / BUGS**

- Rate‑limit errors on external API calls; mitigated by retry logic.  
- Regex bug in `psn-autonomy-data-ingest.mjs` – fixed to capture `[MAIN] [BOOTSTRAP-SLOT-ENFORCE] [SCOPE]/U-ID`.  
- NaN bug in audit‑drift detection (broken array misuse) – corrected.  
- Concurrent‑staging collisions: 4–5 peer‑absorbed commits; resolved by deferral and documentation.  
- Missing units in PROGRAM‑PROOF‑MS0 (PP04‑PP10, PP06 pilot).  
- Git lock contention during large commits; resolved via slot‑worktree isolation (`feedback_commit_to_slot_worktree`).  
- Missing constants in G‑code engine; fixed by importing from `src/physics/constants.ts`.  
- Initial MAPE 171.9 % bias (+146 %) on JM Die baseline; addressed with `QuotingCalibrationEngine` reducing bias <0.01 % and MAPE to 93.6 %.  
- Dedup logic initially counted empty per‑customer bias; corrected helper.

**DOMAIN SPECIFICS**

- **Engines / Units** – PROGRAM‑PROOF-MS0 (U‑PP01‑PP10), SVI‑ENHANCE-MS0 (E01‑E10), PSNIncorporationOrchestratorEngine, PSNAutonomyLoopEngine, JMDieScanLedgerEngine, JMDieFleetScanStatusPanel, QuotingTrainingLoopEngine, XometryStyleQuoteInputsEngine, OutsourceRecommenderEngine, ScenarioGeneratorEngine, OutsideKnowledgeSourceCatalogEngine, QuotingDeepReasoningBridgeEngine, QuotingCalibrationEngine.  
- **Dispatchers / Actions** – `prism_dev:psn_incorp_*`, `svi_enhanced_compute/score_event/...`, `jm_die_envelope_catalog*` family, `prism_quoting` actions (8 quoting actions).  
- **Metrics** – Ψ (coverage), SVI_MOAT, ΔΨ, Monte Carlo competitor probability, Kolmogorov bound; FMV = (time·rate + material·markup) × (1+overhead) × (1+margin); MAPE/MAE/RMSE; signed bias.  
- **Paths / Interfaces** – `/psn‑automate`, `/psn-autonomy-cycle`, `/system-viz`, `/forge-audit-v2`; `/checkin-charlie`.  
- **PSN Legs** – Obsidian, PRISM OS, Wiki, Memories, Tribal Knowledge, System Viz, Engines, Algorithms, Formulas, NN/GNN, AI systems.  
- **Deep‑reasoning routing matrix** – 5 question classes → 5 AI substrates (PRISMCreativeReasoning, Claude, Ollama, PSN NN‑GNN, Tribal RAG).

**TOOLS USED**

- PRISM helpers: `chat-slots.mjs`, `audit-roadmap-drift.mjs`, `close-out-audit-suggest.mjs`.  
- CronCreate / CronDelete for recurring goals.  
- Skill tool (auto‑discover, `/psn-automate`).  
- Dispatcher framework (`devActionSchemas.ts`, `devDispatcher.ts`, `prism_quoting` actions).  
- Scripts: `run-calibration-cycle.mjs`, `execute-fleet-scan-massive.mjs`.  
- Test harness: Vitest (120/120 PASS), tsx for runtime.  
- Git hooks: commit prefixes, slot‑claim scripts (`feedback_commit_to_slot_worktree`).

**OPEN THREADS**

- Finish PROGRAM‑PROOF‑MS0 units: U‑PP04 auto‑fix engine, U‑PP05 pre‑emit gate, U‑PP06 WEDM pilot, U‑PP07‑10 topology layer.  
- Build quoting engine web/phone app (JM Docustrata integration, CAD files, program data; live OCR, part lookup, pricing, AI chat).  
- Validate and deploy full PSN automation loop in production; monitor ΔΨ rewards and auto‑tune.  
- Per‑customer / per‑document calibration (U‑QT11).  
- NN/GNN AUROC grading & integration (`U-NN-PREDICTOR-EMBED-WIRE`).  
- Scenario runner for 1 M synthetic scenarios (U‑QT07).  
- External knowledge ingestion pipeline (`OutsideKnowledgeSourceCatalogEngine`) pending full data load.  
- Production‑grade auto‑promotion cron and real‑customer outcome feedback hook.
