# mit-curriculum session b7530614 (2026-05-19, 21.9MB, spine 85KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-VIZ-F11-CROSS-LOCK` – commit 4022e99606 (shared PID‑lock for system‑graph.json writers).  
- `U-CLEAR-AUTO-RESUME` – commits 5b16e56c68, c004ad1cb8 (SessionStart auto‑resume hook).  
- `U-MASTER-INDEX-SIDECAR` – commit 291646f427 (5‑file inverted‑index sidecar).  
- `U-ACTIVATE-BEFORE-BUILD-PRECHECK` – shipped after sidecar, satisfies S5 F1 dependency.

**DECISIONS**  
- Fleet‑reaper ownership moved from **alpha** to **golf** (unified hygiene slot).  
- Chose inverted‑index sidecar over raising `PRISM_GRAPH_MAX_BYTES` to 512 MB due to OOM measurement.  
- Implemented shared `.system-graph-write.pid` lock for all system‑graph.json writers.  
- Adopted per‑file scrutiny + 3/3 gate, loop‑state with high‑ROI priority queue.

**OPERATOR DIRECTIVES**  
- Continue `/loop` on alpha slot, prioritizing highest ROI units first.  
- Resume next unit after `U-ACTIVATE-BEFORE-BUILD-PRECHECK` (next in priority‑queue).  
- Ensure loop tick and task tracker updated; handoff for next iteration ready.

**FINDINGS/BUGS**  
- `__isMain` guard bug in golf‑only‑guard prevented main() from running – fixed by case‑insensitive URL comparison.  
- Raising cap to 512 MB caused 138 s / 1.6 GB RSS OOM; sidecar chosen instead.  
- Stale `index.lock` due to peer git process – cleared with sweeper.  
- Test failures from invalid engine names fixed by correcting fixtures.

**DOMAIN SPECIFICS**  
- Files: `system-graph.json`, `architecture-graph.json`.  
- Modules: `master-index-search-lib.mjs`, `build-graph-index.mjs`.  
- Lock lib: `scripts/lib/system-graph-write-lock.mjs`.  
- Writers: `regen-viz.mjs`, `system-viz-on-commit.mjs`, `system-viz-add-node.mjs`, `seed-ghost-from-unwired.mjs`.  
- Slot tooling: `slot-task-claim.mjs`, `per-agent-handoff.mjs`, `precompact-pending-guard.mjs`, `stable-session-id.mjs`, `audit-roadmap-drift.mjs`.

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `slot-task-claim.mjs`, `per-agent-handoff.mjs`, `precompact-pending-guard.mjs`, `stable-session-id.mjs`.  
- Node scripts (`execFileSync`, `node:test`).  
- Git commands for commit, lock sweeping.  
- PRISM command pipeline (`/checkin`, `/loop`).

**OPEN THREADS**  
- Next highest‑ROI alpha unit pending in priority queue (not yet shipped).  
- Verify loop continues correctly after compact with handoff.  
- Ensure no remaining git lock contention or stale locks.
