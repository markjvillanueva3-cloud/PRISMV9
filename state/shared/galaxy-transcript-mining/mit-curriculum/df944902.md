# mit-curriculum session df944902 (2026-05-19, 52.8MB, spine 122KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**

- U‑CK16 – `skill-auto-trigger.mjs` – 1763248116 – 35/35 tests PASS  
- U‑CK27 – adaptive‑thresholds telemetry → close‑out wiring – 44a95cc1c0 – 12/12 tests, live 6/6 dormant signals reported  
- U‑MTC07 – operation‑ordering canonical – c2551512e5 – all audit categories now canonical  
- U‑MTC08 – workholding canonical – 4bcbeb4669 – canonical  
- U‑MTC09 – part‑setup/probing canonical – c986865c56 – canonical  
- U‑MTC10 – coolant/chip‑evacuation canonical – ea0f4546cc – canonical  
- U‑FGE03 – persist learned‑blended prevalences & auto‑apply in `templateFor()` – e7d583b344 – 68/68 tests, no tsc errors  
- U‑ROADMAP‑INDEX‑WRITER‑CONSOLIDATE – atomic JSON helper for 5 writers – d877d1c970  

**DECISIONS**

- Prioritize today’s real work over stale auto‑write templates.  
- Hook env constants moved to call‑time getters (`triggersPath()`, `pipelinesDir()`).  
- Auto‑derive widened: all `kind:pipeline` entries trigger command unless `trigger_command:false`.  
- Run `/startup-bravo` for full startup audit before dev pipeline.  
- Use `slot-bind-enforce.mjs` for deterministic bravo slot claim; manual bash only if hook fails.  
- Queue high‑ROI devtools/backend units first (`feedback_prioritize_devtools_backend`).  
- Loop‑state ticked to iter 8 (running) ready for next `/checkin-bravo /loop`.  

**OPERATOR DIRECTIVES**

- Compile remaining work from today’s chats (Golf’s `FLEET-PENDING-EXTRACT-2026-05-18`) and add to task queue ahead of rgs tasks.  
- Verify/ship pending units in COMMAND‑KERNEL-MS0: U‑CK11, 14, 15, 28, 29.  
- Resolve environmental block: free ≥10 MB on `C:\Users\wompu\AppData\Local\Temp\claude\`.  

**FINDINGS/BUGS**

- Hook import path constants captured at module load → swapped to call‑time getters.  
- `replace_all` corrupted env‑var names – restored 3 lines + docstring.  
- False‑green subprocess oracles (`out.hookSpecificOutput`) → unconditional `assert.ok`.  
- TriggerCommand auto‑derive too narrow – widened to all pipelines with opt‑out flag.  
- Stale env var in `runHook` – pinned LIVE registry paths, cleared knobs.  
- CAD‑Fusion: `applyLearned()` returned blended templates only in‑memory; added `persistLearned()`, overlay loader, auto‑apply in `templateFor()`.  
- Frontmatter populator bugs (digit regex, CRLF/BOM parsing, symmetric `validateAdditions`, YAML emitter safety, empty‑FM handling, error counters, `--field` allowlist).  
- Atomic JSON helper (`atomic-json.mjs`) added to eliminate concurrent‑writer collision.  

**DOMAIN SPECIFICS**

- Engines: `CADCorpusFeaturePrevalenceLearnerEngine`, `CADClassFeatureLibraryEngine`.  
- Dispatchers/Actions: `skill-auto-trigger.mjs`, `cad_corpus_apply_learned.ts`, `adaptive-thresholds.mjs`, `close-out-milestone`, `register-*` scripts.  
- Telemetry metrics: dormant‑by‑data signals, build‑sequence inference, adaptive thresholds; future wiring of `event:"violation"` / `event:"outcome"`.  
- Tribal coverage canonicalization paths: `knowledge/wiki/code-tribal/canonical/`.  
- Slot-task-claim workflow: `slot-bind-enforce.mjs`, `chat-slots.mjs`.  

**TOOLS USED**

- PRISM slot helpers (`chat-slots.mjs`), `slot-bind-enforce.mjs`.  
- Pipelines: `/checkin`, `/startup`.  
- Loop state manager (`loop-state.mjs`).  
- Node.js test harnesses (`node:test`, `vitest`).  
- Git, shell scripts (bash, PowerShell) for slot claiming and loop tick.  
- PRISM dispatcher (`agent-coordination.mjs`, `state/shared/AGENT_CHAT.jsonl`).  
- Settings mirroring (`c-to-h-mirror` hook).  

**OPEN THREADS**

- Pending CK units: U‑CK11, 14, 15 (pending), U‑CK28, 29.  
- Uncategorised 63.8 % of MTC workload still unaddressed.  
- `hook-fire-counts.jsonl` lacks rotation cap.  
- Boolean‑vs‑string ambiguity in `trigger_command` opt‑out gate.  
- Environmental block: free temp space on C: drive to allow Bash/PowerShell harnesses.  
- Future work: wire `event:"violation"` / `event:"outcome"` into adaptive‑thresholds (U‑CK28/U‑CK29).  
- Loop-state iter 8 (`running`) – ready for next `/checkin-bravo /loop`.
