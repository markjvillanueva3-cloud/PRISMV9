# mit-curriculum session 9f3a8e4f (2026-05-25, 69.1MB, spine 239KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**

- `fa0c809c1c` – baseline 96 MIT‑OCW spec files (`state/shared/college-course-specs/*`)  
- `865fa9fccc` – widened emitter script + master index (`COLLEGE-COURSE-AUTOGEN-INDEX-2026-05-24.md`) and 5‑lane discovery logic  
- `ec8c38aa9c` – wiki entry & PSN map (`knowledge/wiki/architecture/college-course-autogen-specs.md`) + augmentation generator (`scripts/generate-college-course-features.mjs`)  
- `a5d7c15a8f` – wiring gate for 5 PART‑TYPE‑STACK actions in `calcDispatcher`  
- `/college-extract <slug>` skill (on‑disk, `.claude/commands/*`, git‑ignored)  
- `6422115748` – bulk commit of 1401 college specs (incl. 1008 derived spec files)  
- `4d0158c78d` – 893 PDF AUTOGEN‑EXTRACT‑SPECs + generator & tests  
- `b382b4328c` – 2541 bridge‑to‑engine edges (iter20)  
- `a34daf16fd` – PRISM AI action `ai_cadcam_corpus_pointers`  
- `4b477365cd` (iter 77) – 8 InventorCAM SWARF tips added → books 115→116, tips 477→485, nodes 596; cumulative page‑cited tips now **257**  
- Iter43 – graph coverage node set for all 210 consolidated PDFs  

**DECISIONS**

- Adopt AUTOGEN‑SPEC blueprint pattern → deterministic spec generation & idempotent commits.  
- Use `ghost.college_courses` and `ghost.resource_pdfs` roosts in system‑viz for discoverability.  
- Bridge edges: `bridge-to-engine`, `enriches-engine`, `feeds-dispatcher`, `feeds-training`.  
- PRISM AI action `ai_cadcam_corpus_pointers` exposes corpus paths for AI training pipelines.  
- Cron‑based `/loop [5m]` (job `d90e7eac`) for idempotent goal re‑evaluation; auto‑expire after 7 days.  
- Path normalization bug (`parseInt("0") || 100`) fixed; now returns 1 for zero.  
- Dual‑dir generator: `state/shared/extracted-pdfs/` + `mcp-server/data/ingestion_cache/extracted-pdfs/`; canonical path precedence to avoid stale writes.  
- Resolve ID collisions by renaming peer‑shipped IDs (e.g., `swg‑101…108 → swg‑901…908`).  
- 10‑iteration retry loop for Git lock contention; never kill peer processes.

**OPERATOR DIRECTIVES**

- “Prioritize books & PDFs first” – triggered extraction of core books, batch stub generation for remaining PDFs.  
- “Extract data from all sources to wiki/memories/tribal nodes” – fulfilled via iterative commits and system‑viz roost updates.  
- User requests: acknowledge goal immediately, treat condition as directive, do not pause or ask user; stop hook blocks until condition met, auto‑clears on success.

**FINDINGS / BUGS**

- Shared‑tree lock contention prevented bulk commit of 1008 spec files; resolved with retry loop and `git -c core.fsmonitor=false add`.  
- ParseArgs bug (`parseInt("0") || 100`) fixed; now returns 1 for zero.  
- Path mismatch between PDF leaves and spec paths normalized in iter43.  
- ID collision detected (iter 58) – resolved by renaming IDs.  
- Git lock contention (iter 55) – handled with retry loop.  
- Ingestion‑cache root guard (iter 53) – migrated to canonical path.  
- Unicode apostrophe filename (“Mill Operator’s Manual”) handled correctly.  
- Ollama false positives on JSONL validation ignored; all lines valid.  

**DOMAIN SPECIFICS**

- Course data sources: MIT OCW catalog, nested MIT course directories, PDF inventory, JM Die subdirs, basic training days, prism‑training/prism‑personal/knowledge‑pack.  
- PSN integration via `ghost.college_courses` roost; system‑viz augmentation; wiki mapping; Obsidian memory auto‑feed.  
- Engines: `UltimateSpeedFeedEngine`, `SpeedFeedOrchestratorEngine`, `FormulaExtractorEngine`, `GDTValidationEngine`, `ToleranceStackEngine`, `CADGeometryEngine`, `LathePostProcessorEngine`, `MachineControllerEngine`, `InjectionMoldQuoteEngine`, `FatigueLifeEngine`, `ClosedLoopFeedbackEngine`, `AdaptiveFeedrateEngine`.  
- Dispatchers: `calcDispatcher PART-TYPE-STACK`, `/college-extract <slug>`, `/pdf-learn <pdf>`.  
- Metrics: bridge‑edge counts 2541 → 12648; PDF extraction coverage 210 PDFs extracted (all covered); pending‑PDF flagging cleared.  
- Paths:  
  - `state/shared/college-course-specs/*`  
  - `COLLEGE-COURSE-AUTOGEN-INDEX-2026-05-24.md`  
  - `state/shared/system-viz/college-course-augmentation.json` (git‑ignored)  
  - `ghost.college_courses`, `ghost.resource_pdfs`, `ghost.extracted_pdf_tips` roosts  
  - `H:/PRISM/resources/RESOURCE PDFS/`  
  - `state/shared/extracted-pdfs/`  
  - `mcp-server/data/ingestion_cache/extracted-pdfs/`

**TOOLS USED**

- Node scripts: `auto-college-course-spec-emit.mjs`, `generate-college-course-features.mjs`, `generate-extracted-pdf-tips-features.mjs`, `extract-cadcam-tribal-wiki.mjs`, `query-extracted-tips.mjs`, `regen-viz.mjs`, `merge-augmentations.mjs`, `generate-pdf-course-bridge-features.mjs`, `consolidate-cadcam-corpus.mjs`.  
- Cron jobs: `d90e7eac` (5 min loop), `9977a63e`, `e12e2726`.  
- Git utilities: `git -c core.fsmonitor=false add`, lock‑handling scripts.  
- External utilities: `pdftotext -layout`.

**OPEN THREADS**

- Remaining book/chapter extraction queue:  
  * InventorCAM 3D HSR (sibling to 3D HSM)  
  * InventorCAM Geodesic Machining  
  * InventorCAM Multiaxis Roughing Pt1/Pt2  
  * CS50 Notes (CS curriculum side)  
  * Basic_3D_Machining  
  * Autodesk_CNCBOOK  
- Migration of ingestion cache to `mcp-server/data/ingestion_cache/` pending.  
- Next iteration will target above candidates per cron schedule; no pending PDF coverage.
