# mit-curriculum session 410fbc86 (2026-05-17, 1.6MB, spine 8KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Unit 1: Fixed `parseTriggers` YAML reader → 36→399 ledger entries (coverage 5.9%→64.9%) – commit +382 insertions.  
- High‑ROI skill audit script (`high-roi-skill-rank.mjs`) and history log (`state/shared/high-roi-skill-history.jsonl`).  
- Slot‑claim wrapper for `/checkin-lima` (force‑take Lima slot, bind to `lima-work`, run full `/checkin` pipeline).  

**DECISIONS**  
- Adopt a slot‑binding wrapper for `/checkin-lima` to enforce exclusive access and delegate the canonical `/checkin` pipeline.  
- Prioritize fixing the YAML trigger parser (`parseTriggers`) over re‑reading directories; this addresses the real extraction gap (F3).  
- Build user‑prompt‑submit hooks (`/ollama-route-check`, `/route-to-obsidian`) to surface under‑tuned Ollama routes and route wiki queries.  
- Use PreToolUse Read hook for `/wiki-summarize-via-ollama` on large markdown files to avoid token costs.  

**OPERATOR DIRECTIVES**  
- “use /system‑viz to find high roi skills that can be auto triggered with hooks and routed in a way that its cheap or free token usage.”  

**FINDINGS/BUGS**  
- Audit misattributed extraction gap to `extract-skill-triggers.mjs` reading only one dir; actually caused by naive YAML parser dropping entries lacking `matcher.value`.  
- Metrics snapshot (2026‑05‑17):  
  - Skills total 614, triggers 126 (20%), ledger 36 (5.9% → target ≥30%) – **critical**.  
  - Extraction rate 29% → target ≥90% – **critical**.  
  - Model frontmatter 27 (4.4%) → target ≥50% – warn.  
  - Ollama offload ratio 7.8% → target ≥30% – warn.  
- `INJECT_THRESHOLD` already shipped at 0.80; no further action needed.  

**DOMAIN SPECIFICS**  
- Engines/dispatchers: `chat-slots.mjs`, `audit-roadmap-drift.mjs`, `system-viz-query.mjs`.  
- Actions: slot‑claim, drift check, commit hygiene, roadmap slice, Obsidian recent sync, system‑viz ping, CLAUDE.md staleness, local‑compute health, fleet activity pickup.  
- Metrics/paths: ledger entries (`_skill-triggers.jsonl`), Ollama offload stats (`ollama-offload-stats.json`), hook map (`byHook`).  
- Unique paths: `H:/prism/.claude/helpers/chat-slots.mjs`, `state/shared/specs/HIGH-ROI-SKILL-ROUTING-AUDIT-2026-05-17.md`.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs` (claim/reclaim), `audit-roadmap-drift.mjs`, `high-roi-skill-rank.mjs`, `extract-skill-triggers.mjs`, `parseTriggers` YAML reader.  
- Skills/hooks: `/ollama-route-check`, `/wiki-summarize-via-ollama`, `/route-to-obsidian`.  
- Scripts: `system-viz-query.mjs`, `checkin.md` pipeline steps (3‑14).  

**OPEN THREADS**  
- Fully implement and test `/ollama-route-check` + UserPromptSubmit hook.  
- Build `/wiki-summarize-via-ollama` PreToolUse Read hook for large wiki files.  
- Implement `/route-to-obsidian` UserPromptSubmit hook to route “what does the wiki say / find in vault” queries.  
- Verify post‑fix extraction rate reaches ≥90% and ledger coverage ≥30%.  
- Explore extending `system-viz-query.mjs` with `--kind=skill/hook` filters for future audits.
