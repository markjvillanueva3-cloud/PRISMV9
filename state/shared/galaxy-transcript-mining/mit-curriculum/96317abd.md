# mit-curriculum session 96317abd (2026-05-25, 107.1MB, spine 263KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**

- `995e343c98` – U‑KEC‑CAD‑PARAM‑EMITTER (59 tests, 2 dispatcher actions)  
- `3dd4cc17aa` – PSN‑CAD‑PRODUCER (21 tests, 4 dispatcher actions)  
- `379960610f` – U‑ADC‑NN04/05/06 adapters (24 tests, 4 dispatcher actions)  
- `dfae92f44c` – batch silent‑close‑out of 15 priority units (PHASE 15/20/25/26)  
- `54d69bfe70`, `2b200c4f56` – clean ships of U‑ADC32 & U‑ADC33 (41 tests)  
- `ee72fa2a5c` – archive of 155 non‑priority units + assessment docs  
- `4690e17f3b`, `0950c701d3`, `1cdb3283a8`, `5b4f541121` – bulk‑drain deployments (38 units flipped)  
- `b2d1f2bd6c` – 3 extraction scripts + 17 wiki/tribal nodes + handoff (slot‑delta)  
- `3488a74f89` – HyperCADS pull, 982 NN‑graph seeds  
- `c78ab7b0ca` – unified 5‑CAD pull script, 2 632 seeds  
- `6c8d13fcb6` – full 5‑CAD extraction, 6 999 cad_parameter seed records (force‑added)  
- `5ade492b43` – CAD FILES regeneration manifest + smoke‑runner for 30 test files  
- `27fb50624b` – fixed missing exports in `graphsage-trainer.mjs`; unblocked NN retrain  
- `6bc89da5ef` – first NN retrain run, AUROC 0.39 (pre‑promotion)  
- `2aa917e301` – Wiki tribal entry, `/cad‑regen` skill, T2 hook, 4‑unit batch for turbine regen  
- `15350407bf` – real topology‑rich STEP emitter for impeller, validator, desktop delivery skill  
- `a37b9919d7` – corpus‑wide topology sweep (6 789 faces), multi‑cylinder emitter  
- `e6cbad7e3a` – placement extraction, real‑placed emitter (39 faces for impeller)  
- `1b335c019c` – full corpus re‑extract to schema v2, topology emit, 23 790 total faces  
- **Delta worktree** – 47 commits on `slot/delta` (iter +1 → iter +47)  
- **Schema & helpers** – 5 geom.json schemas (v1–v5), 8 emitter helpers, 2 detector helpers, `PIPELINE_VERSION = "iter+38"`, 5 env knobs, 4 test files, 2 wrappers (`cad.sh`, `cad.ps1`), 98 unit tests

---

**DECISIONS**

- Adopt silent‑close‑out detection stack (Modes A–E) and batch flips per priority phase.  
- Prioritize CAD systems: HyperCAD → Fusion → Mastercam → SolidWorks → Inventor → Esprit; archive others.  
- Build `U‑KEC‑CAD‑PARAM‑EMITTER` to auto‑generate wiki/tribal nodes & NN seeds from `FunctionIndexEngine`.  
- Deploy unified PSN facade (`PSN‑CAD‑PRODUCER`) for any priority CAD system.  
- Archive non‑priority units; keep 157 units indefinitely.  
- Use batch close‑out tool with `--all` and dispatcher‑grep mode.  
- Commit to slot‑delta worktree (`H:/prism-slot-delta`) to avoid `git/index.lock`.  
- Chunked‑write pattern with `setImmediate`; skip‑existing logic to bypass bulk‑write hook truncation.  
- Persist NN‑graph seeds as `.jsonl` (force‑added) to survive `.gitignore`.  
- Run Ollama qwen2.5‑coder:7b for archetype labeling; fallback rule‑based when offline.  
- Deploy 5‑service Docker compose stack (`cad-discover`, `batch`, `archetype`, `validate`, `fidelity`) for corpus‑scale parallelism.  
- Treat stop hook as unconditional directive: “extract all CAD data, generate skills/hooks, reverse engineer turbine”.

---

**OPERATOR DIRECTIVES**

- `/checkin-delta` with force‑take of delta slot and handoff to `delta-work`.  
- `/loop [5m] complete next 4 units` – repeat every 5 min.  
- “continue, can you give me a generate cad file of the turbine on my desktop so I can open in fusion to see if you generated a true turbine”.  
- Repeatedly issue “complete next 4 units” for iterative development.

---

**FINDINGS/BUGS**

- Silent‑close‑out detection floor after Mode E; 181 genuinely unbuilt units remain.  
- Hitchhike absorption logs: U‑ADC40 absorbed into whiskey, U‑ADC33 reverse‑absorbed 5 peer files (`CLOSE‑OUT‑DEFERRED.md`).  
- Windows path separator bug fixed (`joinPosix` used instead of `node:path.join`).  
- No R12 fail‑loud violations; all new commits passed 3‑of‑3 scrutiny gate.  
- Bulk‑write hook truncated scripts after ~13 files – resolved with chunked writes and `PRISM_HOOK_PROFILE=minimal`.  
- Background harness timed out on 24K‑file walk; switched to foreground execution.  
- Missing exports in `graphsage-trainer.mjs` (restored `positiveTypeMarginal`, `sampleStratifiedNegativeEdges`).  
- WorkerPool bug: passed index as timeout, killed child – fixed by passing only file path.  
- Initial topology emitter produced empty CLOSED_SHELL; upgraded to multi‑cylinder and real‑placed emitters (39 faces).  
- Corpus re‑extract required schema v2 for placement data; completed in 107 s.  
- OOM on full corpus re‑emit – increased Node heap.  
- Missing machine catalog files in shared tree – restored from peer slot.  
- Test failures due to banner version change (`[cad-status]` → `[cad-status:iter+38]`).  
- `spawnJson()` field mismatch (`status` vs `exit`) fixed.  
- Print‑compare ledger schema missing; added tests and wrapper subcommand.

---

**DOMAIN SPECIFICS**

- **Engines/Actions** – `CADDrawAnyPartValidationHarnessEngine`, `CADValidationRubricEngine`, `CADRoundTripValidationEngine`, `CADLiveBlueprintOcrAdapter`, `CADModelDimensionExtractorEngine`, `CADPrintRegeneratorEngine`, `PSN‑CAD‑PRODUCER`, `U‑KEC‑CAD‑PARAM‑EMITTER`.  
- **Dispatchers** – `prism_cad.ts` (16+ actions), `workerPool` (bounded concurrency).  
- **Metrics** – baseline accuracy 75 % on 12‑case corpus; target >95 % on ≥1,000‑case corpus. AUROC 0.39 after first retrain; 23 790 total faces in corpus; 6 999 cad_parameter seeds; 95 % mean match score on turbine subset.  
- **Paths** – `mcp-server/src/engines/...`, `scripts/run-hypercad-validation.mjs`, `state/shared/cad-param-graph-seed.jsonl`, `knowledge/wiki/architecture/cad-pipeline-closed-loop.md`, `cad-to-desktop.mjs`.  

---

**TOOLS USED**

- PRISM CLI helpers (`chat-slots.mjs`, `/checkin-delta`, `feedback_commit_to_slot_worktree`).  
- Node.js scripting (`*.mjs`), workerPool, setImmediate.  
- Docker compose (`cad-corpus.yml`) and 5‑service stack (`cad-discover`, `batch`, `archetype`, `validate`, `fidelity`).  
- Ollama API (`qwen2.5-coder:7b`).  
- Git hooks (bulk‑write, commit‑hygiene).  
- Scripts: `scripts/close-out-cad-silent-debt.mjs`, `run-hypercad-validation.mjs`, `CADRoundTripValidationEngine.ts`.  

---

**OPEN THREADS**

1. Reverse‑engineer turbine parts in HyperCad, Fusion, Mastercam – full PSN integration and validated part generation pipeline.  
2. Full training loop to raise NN AUROC from 0.096 → ≥0.78 (multi‑iteration on 6 999‑seed pool).  
3. Complete knowledge extraction from MIT OCW & PRISM Academy courses (metadata only now).  
4. Populate every CAD function input as wiki/tribal nodes – implement remaining parameter emitters for Fusion, HyperCad, etc.  
5. Validate turbine regen by generating a full part from scratch and comparing against ground truth (>1 000‑case test suite).  
6. MCP/Fusion 360 connectivity – live bridge to execute emitted `impeller-turbine.fusion360.py`.  
7. Full turbine regen with blade surfaces (currently only cylinders; need B‑spline blade faces for 485‑face match).  
8. PSN integration – expose generated artifacts (geom.json, scripts, STL) to downstream RAG/AI workflows.  
9. Finalize corpus‑wide print‑compare aggregation (currently per‑slug).  
10. Extend self‑check to include print‑compare pass rate (v2 in progress).  
11. Refine machine‑catalog pattern list or automate via count detector.  
12. Future audit loop integration (`/forge-audit-v2`) pending next session.
