# mit-curriculum session 17b9f42e (2026-06-15, 118.6MB, spine 705KB, 9 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `LAUNCH-HERMES-FLEET.bat` – WT 21 tabs, each `hermes -p <slot>`.  
- `hermes-slot-tab-boot.ps1` – per‑tab bootstrap to slot worktree.  
- 21 SOUL files: `H:\Tools\prism-fleet\hermes-souls\<slot>.md`.  
- 21 Hermes profiles: `%LOCALAPPDATA%\hermes\profiles\<slot>`.  
- Cross‑substrate edges (56 566 total) staged for regen‑viz.  
- Commit `5010a01a82`: removed duplicate `prism_ai`, added dedup guard at `proxiedTool`, renamed `claudeAccountDispatcher` → `prism_clade_account`; rebuilt MCP server, `/health` healthy (`status:"healthy", version:"2.10.0"`).  
- Commit `4bbb8b97`: U‑BRIDGE‑KEEPALIVE – added `keep_alive`, timeout bump to galaxy‑reasoning‑bridge (3/3 scrutiny PASS).  
- Commit `U-BRIDGE-WIKI`: wiki inclusion opt‑in, dense‑rerank fix, deterministic test.  
- Commit `U-BRIDGE-FALLBACK`: model‑fallback ladder 120B→32B→20B→1.5B.  
- Commit `U-OLLAMA-WEDGE-GUARD`: auto‑recover `/api/generate` wedge.  
- Commit `CAG-usedModel fix`: now records actual producing model in galaxy‑reasoning‑bridge cache.  
- Close‑out candidates refreshed (2.1 h stale cleared).  
- Commit `30b7765743`: CAG‑usedModel transparency fix.  
- Unit `1438960f58`: fixed dead `stop_on_stale_handoff`, added 10 R9 revert‑proof tests, live‑validated on 1078 handoffs.  
- Unit `da66478fbc`: consolidated handoff data for missing slots via canonical generator.  
- Unit `cd2ad2979b`: corrected fabricated claim – “ollama‑verified‑offload” has 6 callers.  
- Commit `5d08e32cc1`: CAG hit‑rate telemetry for all 34 galaxies.  
- Commit `a979a2be8c`: shipped‑ledger update.  
- Commit `4c0410301b`: exported `readFleetLoops()` API; refactored `cmdList`.  
- Commit `79f452a2bf`: added `prism_session:loop_state_query` dispatcher action.  
- Commit `30474ebae1`: milestone wiki synthesis (synergy across all galaxies).  
- Commit `f9f5770cd2`: Stop‑hook wrapper for `cross-pc-handoff-verify.mjs`; OOM guard added.  
- Commit `d24f48cd16`: SessionStart headline hook exposing CAG hit‑rate.  
- Commit `1f4dbf635a`: installed Tailscale 1.98.4; deployed read‑only `tailnet-probe.mjs`.  
- Commit `51b0330b35`: orphan memory‑provider framework registry, CLI, `/memory-providers` skill.  
- Commit `bd3dc4eb65`: script to append ≥3 AI terms in each galaxy’s CLAUDE.md (discoverability gap fix).  
- Commit `07c6847ca2`: updated owner/consumer aware documentation; gate now green.  
- Commit `32718b045a` – U‑LORA‑GALAXY‑AISYN: extracts 23 AI‑Synergy pairs from CLAUDE.md, 34/34 coverage.  
- Commit `dd3ef00c1f` – U‑LORA‑OWNER‑COVERAGE: owner‑only LoRA source, removes 18 consumer boilerplate (12 high‑signal), 34/34 coverage.  
- Commit `c1b4e0a00d` – U‑SOUL‑AISTACK‑DURABLE: embeds AI‑Stack block in soul renderer; idempotent stamper.  
- 15 units committed to `cad-fusion-live-ms0`: Loop 8/8 telemetry, orphan MemoryProvider framework, Tailscale install.  
- Restarted PRISM Ollama service after operator approval.

**DECISIONS**  
- Use Hermes profile system (`-p <name>`, `HERMES_HOME`) for isolated instances; 21 NATO slots with domain‑seeded SOULs and dedicated worktrees.  
- Launcher built as static `.bat` mirroring Claude fleet layout (WT tabs, quadrant snap).  
- Perform GNN ref‑pool growth (+8 wirings) before launch; generate cross‑substrate edges (embeds + documented‑by) to connect all 34 galaxies.  
- Remove duplicate `prism_ai` registration; add last‑wins dedup guard at SDK’s `proxiedTool`; rename `claudeAccountDispatcher` → `prism_clade_account`.  
- Re‑scope unbounded prose goal to bounded subgoal: read all bravo sessions via Ollama, max out Oscar knowledge.  
- Use deterministic loss function `ai-systems-synergy-goal-gate.mjs` (4/4 PASS) as stop criterion; no new keeper fabrication.  
- After fresh‑mined brains, propagate into LoRA training substrate (`vault-to-lora-dataset.mjs`).  
- Ship only in‑lane AI‑substrate improvements: `keep_alive`, wiki opt‑in, fallback ladder, wedge guard, CAG fix—no speculative GNN retraining.  
- Accept gate; authorize candidate‑gated retrain (operator‑approved); ground‑truth grow + single‑seed retrain failed (AUROC 0.1396 < 0.78).  
- Build bridge graph for Hermes/Obsidian/PSN synergy; plan stored in `state/shared/specs/AGENTIC-SUBSTRATE-BRIDGE-PLAN-2026-06-14.md`.  
- Fix stale‑handoff scan bug (root‑scan dead → `state/shared/handoffs/`).  
- Pause heavy edits until clean context (`/compact`).  
- Mesh‑control dispatcher for Tailscale requires logged‑in tailnet & second host.  

**OPERATOR DIRECTIVES**  
- Duplicate launcher on desktop; one launch hermes versions of chat fleet.  
- Read all bravo sessions today/yesterday, max out Oscar knowledge /yolo‑mode.  
- Briefly acknowledge goal, then immediately start working toward it.  
- `/clear` to terminate current loop and reset context.  
- `/compact` after self‑compact fallback.  
- Install missing programs (Tailscale).  
- Build Tailscale fleet‑mesh.  

**FINDINGS/BUGS**  
- R14 runaway `bash.exe` spike resolved; no spawns remain.  
- GNN ref‑pool growth merged live (+8 wirings).  
- Cross‑substrate synergy edges added 55 218 new edges (56 566 total).  
- Owned‑by‑slot gap: 5 correct skips, 4 need synthetic domain node (`sierra`).  
- Deterministic AI‑systems synergy gate passes 4/4.  
- Duplicate tool registration caused hard crash after SDK drift to 1.29.0; stale `git.index.lock` cleared.  
- Memory pressure spikes (~97 % commit) due to Ollama model loads; auto‑relief observed, no zombies.  
- Stale CLOSE‑OUT‑CANDIDATES artifact cleared via `/close-out-audit`.  
- Stale “stub souls” and LoRA dataset identified & refreshed.  
- 159 orphan `bash.exe` processes detected (no live claude parent); flagged for fleet reaper cleanup.  
- Gate timeout/crash (“8 gates NOT evaluated”) symptomatic of unbounded prose loop.  
- GNN full‑coverage residual AUROC 0.4286 on holdout nodes; 123 ground‑truth labels available but single‑seed retrain failed.  
- CAG hit/miss telemetry added to bridge; AUROC, macro‑F1, Brier for GNN retrain.  
- Stale handoff scan bug fixed (scan `state/shared/handoffs/`).  
- Duplicate API names in bridge plan corrected.  
- False zero‑caller claim for “ollama‑verified‑offload” library removed.

**DOMAIN SPECIFICS**  
- Hermes profiles: `%LOCALAPPDATA%\hermes\profiles\<slot>`; HERMES_HOME set per slot.  
- Slot worktrees & SOUL.md: `H:\Tools\prism-fleet\hermes-souls\<slot>.md`.  
- Cross‑substrate edges: galaxy ↔ Obsidian memory/wiki, RAG embeddings, etc.  
- GNN reference pool fed by `vault-to-gnn-refpool.mjs` into system graph.  
- Dispatchers: `registerAIDispatcher`, `registerAIReasoningDispatcher`, `registerAuthDispatcher`, `claudeAccountDispatcher`.  
- Tool names: `prism_ai`, `prism_auth`, `prism_clade_account`.  
- MCP health: `/health` → `{status:"healthy", version:"2.10.0"}`; duplicate‑tool warnings `[MCP-DEDUP]`.  
- Paths: `src/index.ts` (boot), `proxiedTool` line 551, `bindDispatchers` 680–704.  
- Engines/dispatchers: `ai-systems-synergy-goal-gate.mjs`, `vault-to-gnn-refpool.mjs`, `vault-to-lora-dataset.mjs`, `galaxy-synthesis-refresh.mjs`, `galaxy-reflection-synthesis.mjs`, `mine-galaxy-transcripts.mjs`.  
- Metrics: AUROC 0.8084 (selective GNN), LoRA rows 1366, CAG coverage 100 %, synergy‑structure gaps = 0/34.  
- Paths: `engines/<galaxy>/CLAUDE.md`, `SOUL.md`, `MEMORY.md`.  
- CAG cache: `scripts/lib/galaxy-cag-cache.mjs`.  
- Ollama service restarts handled by `ollama-wedge-guard.mjs`.

**TOOLS USED**  
- Hermes CLI (`hermes_cli.main`).  
- `vault-to-gnn-refpool.mjs`, `generate-cross-substrate-edges.mjs`, `ai-systems-synergy-goal-gate.mjs`.  
- PRISM tooling: `galaxy-reasoning-bridge.mjs`, `galaxy-cag-cache.mjs`, `ollama-wedge-guard.mjs`, `close-out-candidates-refresh.mjs`.  
- Node test harness (`node:test`).  
- Tailscale client (1.98.4).  
- Ollama local LLM server.  
- Git, per‑file scrutiny reviewers, 3‑of‑3 Stop gate.

**OPEN THREADS**  
- GNN full‑coverage retrain in India slot pending operator decision.  
- Implementation of synthetic domain node unit for 4 missing galaxies (`sierra`).  
- Scheduled mining/synthesis tasks (04:00 mine, weekly).  
- Potential refinement of Hermes launcher layout (WT quadrant snap).  
- Tailscale mesh dispatcher once second host joins.  
- Further AI‑systems gap hunt post‑discoverability.
