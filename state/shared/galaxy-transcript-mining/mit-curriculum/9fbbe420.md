# mit-curriculum session 9fbbe420 (2026-05-25, 83.9MB, spine 274KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED (builds/commits)**  
- `e853edcf93` – GNN node‑id ↔ wiki‑path bridge + lifecycle wiring  
- `6b2d1437f4` – Bridge visualizer & roost generator (`U-GNN-NODE-EMBED-BRIDGE-VIZ`)  
- `29529f05b2` – NN‑GRAPH trainer export/restore (missing functions)  
- `b10218652f` – Home‑preset upgrade: `qwen2.5-coder:14b` prewarm for golf slot  
- `9738616649` – High‑ROI AI‑PSN scope spec (`U-SCOPE-RAG-NN-GNN-ML-DL`, 48 units)  
- `77b7057aa2` – HyDE (A8) unit shipped  
- `3cca69b796` – LLM reranker (A7) unit shipped  
- `ec8c38aa9c` – PSN prompt‑checklist inject hook (`U-PSN-PROMPT-CHECKLIST-INJECT`) + test  
- `U-NN-PREDICTOR-EMBED-WIRE` – 45+103 tests, B8 GNN retrain unblocked  
- `U-SYSTEM-VIZ-PSN-AWARE` – PSN render‑substrate doc, 21/21 tests  
- `fleet-reaper home suite` – 7 tasks fired, 1.7 GB freed (precompact auto‑trigger)  
- `feedback_commit_to_slot_worktree.md` – slot‑worktree commit discipline applied  
- `feedback_psn_definition.md` – canonical 11‑leg taxonomy fixed pointer  
- `CLAUDE.md §CANONICAL SOURCES` rows added (`4e786eddd8`)  
- `U-PSN-LEG-STATE-INJECT` series – 3 → 6 → 10 → 11 legs, 54/54 tests, live hook; `/psn-state` skill registered  
- `generate-extracted-modules-features.mjs`, `regen-viz.mjs`, `merge-augmentations.mjs` – 52‑node augmentation JSON written & merged (102 graph refs)  
- Extraction units shipped:  
  - `U-EXTRACT-JC-CONSTANTS`, `U-EXTRACT-ACTIVATIONS`, `U-EXTRACT-DBSCAN`, `U-EXTRACT-KMEDOIDS`, `U-EXTRACT-TSNE`, `U-EXTRACT-PARTICLE-FILTER`  
  - `U-EXTRACT-LR-SCHEDULER` (9 schedulers)  
  - `U-EXTRACT-PRIORITIZED-REPLAY`, `U-EXTRACT-NSTEP-RETURNS`, `U-EXTRACT-DUELING-Q-DECOMPOSITION`, `U-EXTRACT-DOUBLE-DQN-TD`  
  - `U-EXTRACT-POLICY-GAE`  
  - `U-EXTRACT-WELFORD-MEAN-VAR`, `U-EXTRACT-SCALING-DOT-PRODUCT-ATTENTION`, `U-EXTRACT-LAYER-NORMALIZATION`, `U-EXTRACT-SINUSOIDAL-POSE`, `U-EXTRACT-MULTI‑HEAD-ATTENTION` (all tests passed)  
- `4adb916929` – MCP broadcast trio (`mcp-broadcast-reconnect.mjs`, inject hook, skill) + synthesis wiki  
- `6270570625` – Stop‑hook for MCP server heal (`stop-mcp-server-heal.mjs`)  
- `c20f47ed0f` – Added `BRIDGE_PROTECT_REGEX` to `fleet-reaper-sweep.mjs` (exempt MCP bridges)  
- `iter47` – Durable 5‑min monitor task (`scripts/monitor-mcp-and-reaper.mjs`) + PRISM MCP Connectivity Monitor  

**DECISIONS (architecture/scope + why)**  
- Fleet‑reaper ownership moved to golf slot → unified hygiene & monitoring.  
- Home preset: `qwen2.5-coder:14b` prewarm, 10 min keep‑alive, 2 GB GPU floor, 90 % mem floor (RTX 4080 SUPER).  
- Precompact auto‑trigger + session‑resume hooks → continuous loop; target 999 for “indefinite” runs.  
- PSN prompt‑checklist hook forces use of all 11 legs before answering.  
- Scope spec: 48 high‑ROI units (A1, B8, A7, E1, D1, C1, B9, C8, A14, A8).  
- Adopted slot‑worktree commit discipline (`feedback_commit_to_slot_worktree.md`).  
- PSN synergy: canonical taxonomy, checklist hook, leg‑state hook, `/psn-state` skill, system‑viz augmentation.  
- YOLO mode enabled via `PRISM_GOAL_GATE_AUDIT_BYPASS=1`, `PRISM_ALLOW_UNWIRED=1`.  
- 5‑layer MCP redundancy: bridge retry/self‑heal → supervisor task → watchdog task → fleet‑wide stop hook → broadcast signal.  
- Exempt MCP bridge processes from reaper kills (solves “owned‑by‑crashed” disconnects).  
- Replace ad‑hoc monitor with scheduled 5‑min cron for durability.  

**OPERATOR DIRECTIVES (verbatim asks)**  
- `/goal [ fully complete 1 and 2 | completed and wired to all logical nodes ] /loop [5m] /goal`  
- `continue previous task`  
- `apply all fixes, scope any potential upgrades or issues then continue current batch of work`  
- `build everything we need so we get the best possible out put every turn`  
- `reorientate with all remaining golf work from 5/22/2026‑5/23/2026`  
- `/goal loop: “improve PSN system, extract all high‑ROI features and finish extracting modules.”`  
- `# /checkin-golf` – force‑take golf slot, run fleet‑reaper v2 home sweep, arm lightweight monitor.  
- `# /fleet-reaper-home` – write home preset for this host.  
- `# /mcp-broadcast` (or run `node scripts/mcp-broadcast-reconnect.mjs --reason "…") to push reconnect signal.  
- “fix the mcp server and synergize the full build.”  
- “monitor mcp server connectivity and monitor the reaper fleet.”  
- “the ui disappeared on the terminal so now I cant tell which chat slot is which since their names are gone from the ui”.  

**FINDINGS / BUGS**  
- Predictor embedding mismatch (`inputDim 768 ≠ projector 8`) – fixed via `embeddingSource` metadata.  
- Bash subsystem congestion: ~18 stale slots, >30 orphan greps; 7 candidate fixes (route‑migration, scheduled‑task batch, per‑chat bash budget).  
- PSN keyword auto‑invoke missing; only tag‑syntax routing exists.  
- Fleet‑reaper sweep freed ~1.7 GB (grep orphans + MCP zombie + stale slots).  
- JSONL tail monitor exited 255 – replaced by durable scheduled task (`iter47`).  
- ESM `require("node:fs")` bug → fixed with top‑level import.  
- Merge‑augmentations OOM resolved by 16 GB heap flag; still pending full regen fix.  
- NN/GNN leg flagged UNGRADED (B8).  
- Missing 727 of 876 priority modules in `mcp-server/src/`.  
- MCP disconnections stem from bridge child processes cycling on `/compact`; not server crashes.  
- 21 active chats intermittently disconnect – likely due to rapid bridge restarts under load.  
- Stale slot heartbeats (30‑180 m) caused reaper to kill bridges → chats disconnect.  
- MultiObjectiveParetoEngine.ts missing after peer checkout; restored from `slot/tango`.  
- Monitor task initially failed due to quoting in Task Scheduler; fixed with wrapper `.bat`.  

**DOMAIN SPECIFICS (unique to this galaxy)**  
- Engines / Actions: fleet‑reaper, graph‑node‑embedding‑bridge, GraphSAGE retrain pipeline, predictor, LLM reranker, HyDE, MCP broadcast trio, stop‑hook, monitor task.  
- Dispatchers: `prism_*` actions for PSN legs; `chat-slots.mjs`; `fleet-reaper-sweep.mjs`.  
- Metrics / Paths: PRISM_FLEET_REAPER_* env knobs; `state/shared/fleet-reaper.log`; `state/shared/loop-state/*.json`; `state/shared/embed-all-wiki-progress.json`; `state/shared/dashboards/mcp-reaper-monitor.jsonl`; `state/shared/system-viz/extracted-modules-augmentation.json`.  
- Unique paths: `/startup-golf`, `/checkin-golf`, `/fleet-reaper-home`, `/system-viz.md` (PSN‑aware), `/psn-prompt-checklist-inject.mjs`.  
- PSN legs: Obsidian brain, PRISM OS, Wiki, Memories, Tribal, System Viz, Engines, Algorithms, Formulas, NN/GNN, PRISM AI.  
- Extraction inventory audit: `EXTRACTION-STUB-CLASSIFIER.json` (73 files → 66 substantial).  

**TOOLS USED**  
- Scripts: `generate-extracted-modules-features.mjs`, `regen-viz.mjs`, `merge-augmentations.mjs`, `monitor-mcp-and-reaper.mjs`, `mcp-broadcast-reconnect.mjs`, `scripts/mcp-server-supervisor.mjs`, `scripts/mcp-health-watchdog.mjs`, `scripts/mcp-server-watchdog.mjs`.  
- Skills: `/psn-state`, `/fleet-reaper-home`, `/mcp-broadcast`, `/rename-tab-self` (planned), `golf-slot-write-allowlist` (bypassed).  
- Git hooks: slot‑worktree commit, shared‑tree absorption.  
- Testing: Vitest (per‑module tests).  
- PRISM helpers: `chat-slots.mjs`, `fleet-reaper-sweep.mjs`, `applyHostPresetForCurrent()`.  
- Monitoring: JSONL tail monitor, `fleet-reaper.log`, `mcp-reaper-monitor.jsonl`.  

**OPEN THREADS (still to build)**  
1. PSN keyword auto‑invoke hook (`U-PSN-KEYWORD-AUTO-INVOKE`).  
2. Bash congestion fixes – implement `U-MCP-ROUTE-MIGRATION`, `U-LONG-BATCH-AS-SCHEDULED-TASK`, `U-BASH-PER-CHAT-BUDGET`.  
3. A1 Ollama batch completion – finish full wiki corpus re‑embed (~30 k files).  
4. A7 reranker integration – wire `bge-reranker-v2-m3` into Stage‑3 LLM rerank.  
5. B9 NN tier‑4 fallback – expose `deepseek-r1:14b` on‑demand reasoning engine.  
6. C1 CAM classifier, D1 vision OCR, C8 cycle‑time regressor – build & test.  
7. PSN consumption patterns – add hooks to surface leg usage metrics for monitoring.  
8. `U-SYSTEM-VIZ-REGEN-FIX` (merge‑augmentations exit 1).  
9. `U-PSN-LEG-STATE-WIRE-COMPLETE / EXTEND-11`.  
10. Remaining 63 substantial extraction candidates from stub classifier.  
11. `U-MCP-ROUTE-MIGRATION`, `U-PSN-INVENTORY-SIDECAR`.  
12. Chat‑level stability: monitor bridge restarts under 21 chats; throttle `/compact`.  
13. Fleet‑Reaper coverage: verify all slots owned by reaper and clean orphan bash without killing active bridges.  
14. MCP server resilience: ensure supervisor & watchdog registered after reboot.  
15. Implement ANSI‑title‑set helper (`/rename-tab-self`) to restore tab titles; resolve cross‑tab actuation limitations.
