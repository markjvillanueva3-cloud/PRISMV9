# mit-curriculum session 625e0262 (2026-05-27, 67.2MB, spine 193KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U‑MEMORY‑COMPACT‑NULL‑HOLDER‑FIX – memory‑compact 99 %→49 % token reduction.  
- U‑MCP‑ROUTE‑TAKEUP‑WINDOW‑EXTEND – take‑up window 60 s→600 s.  
- U‑DORMANT‑FEATURES‑ENUM – Phase‑1 spec (22 items).  
- U‑LOOP‑STATE‑CLI‑FLAG‑ALIAS – `/loop` alias support.  
- U‑A11‑A13‑PROMPT‑NOISE‑CLEANUP – hook‑registry & watchdog noise trim (~4 K tokens).  
- U‑A12‑RECALL‑COUNTER‑NOISE‑SUPPRESS – recall‑counter telemetry toggle.  
- U‑MCP‑ROUTE‑AUDIT‑SCRIPT‑RESTORE (B5) – audit script re‑added, 14/14 tests.  
- U‑A7‑SEPARATOR‑TYPOGRAPHY‑TRIM – separator block removal (~2.7 K tokens).  
- U‑C4‑GIT‑LOCK‑SWEEPER‑NOISE‑SUPPRESS – git‑lock telemetry suppression.  
- U‑C1‑BACKEND‑AUDIT‑CHAIN‑RETUNE – backend audit chain narrowed, 50–60 % fewer fires.  
- Domain‑galaxy doctrine spec (`c30db8e851`).  
- Noise‑paths catalog (`6db4ae601a`).  
- Galaxy CLAUDE.md sentinels: mill, lathe, wedm, quoting, business (5 units).  
- Phase‑A rollup spec (`e1bfd137a6`).  
- Golf handoff request (`18084311dd`).  
- Reference memory + `MEMORY‑RECENT` pointer.  
- Scope‑expansion 7‑directive spec (`2dfddc5fb1`).  
- MS1 envelope JSON – lists all remaining units.  
- U‑GALAXY‑MS1‑E2 – feature‑counter lib extended with optional `domain`.  
- F3 slot‑context galaxy line – zebra‑tagged helper.  
- F2 cross‑galaxy warn hook – new telemetry hook.  
- Three additional galaxy stub pairs (cad, cam, shop‑floor) via template.  
- 403aa127a4 – B2 memory‑namespace‑router wire‑in (26/26 tests).  
- 73ceb31ff4 – B3 hermes‑self‑reflect populater (24/24 tests).  
- 0df9eac44c – B1 hermes dream‑cycle synthesis (29/29 tests).  
- b0161ab37e – D1 quoting CLAUDE.md header refresh & archive corrupted `.ts‑1`.  
- 92d121992b – D2 business CLAUDE.md header refresh + BusinessSync anomaly resolved.  
- 3b53f835bb – B2 universal‑reachability bonus follow‑up (28/28 tests).  
- Pre‑shipped B5 canvas cluster (`822d26a4c76e`).  
- 618184b818 – B3‑HMEMV06‑DISPATCHER‑WIRE sidecar.  
- 6f9a21c99a – B1‑HMEMV04‑CRON‑REGISTRATION installer.

**DECISIONS**  
- Fleet‑reaper ownership moved from alpha to golf for hygiene.  
- Disabled slot‑bridge enforcement hooks (`*_DISABLE=1`) to avoid worktree conflicts.  
- Adopted mini‑galaxy: each domain gets its own CLAUDE.md + MEMORY.md, isolated evolution.  
- Introduced feature‑counter lib with optional `domain`; extended U‑GALAXY‑MS1‑E2.  
- Created MS1 envelope JSON for single‑loop yolo‑mode execution.  
- Used CronCreate to schedule recurring `/loop` tasks (e.g., 10 m → `*/10 * * * *`).  
- Completed all alpha‑owned MS1 units; precompact to stay within token budget.  
- Added B2 universal‑reachability follow‑up before compact.  
- Deterministic slot binding via `slot-bind-enforce.mjs`; skip manual bash if hook succeeds.  
- Enforced per‑file scrutiny gate: 2 parallel reviewers, 3‑of‑3 at Stop.  
- Shared‑tree absorption pattern (`slot-worktree`) to merge alpha commits into Sierra branch.  
- Fixed Windows `.cmd` shim by using `process.execPath`.  
- Kept `/loop [5m] /goal /yolo-mode` active until `/compact`; auto‑clear after condition met.  
- Prioritized shipping P1 follow‑ups (B3 dispatcher‑wire, B1 cron‑task) before final compact.

**OPERATOR DIRECTIVES**  
- Ship next pending units; complete all wired, synergized to PSN and fully tested.  
- Run remaining MS1 units in yolo‑mode single loop until `/compact`.  
- Disable slot‑bridge hooks; perform forge audit; adopt Bibryam patterns; plan gap‑fills; treat each domain as its own galaxy.  
- Execute `/precompact-alpha` (slot‑locked) to finalize handoff and exit cleanly.  
- Schedule recurring `/loop [5m] /goal /yolo-mode`; auto‑clear after `/compact`.  
- After 6‑unit DOMAIN‑GALAXY‑DOCTRINE‑MS1 chain, run `/compact`.  
- Ship remaining P1 item `U-NN-TRAINER-EXPORT-RESTORE` before next cycle.

**FINDINGS/BUGS**  
- memory‑compact bug fixed; MEMORY.md size 24 KB→12 KB.  
- mcp-route-takeup window corrected to 600 s.  
- audit script `audit-mcp-route-takerate.mjs` restored (14/14 tests).  
- cache‑breakpoint sweep pending (Phase‑3).  
- Ollama daemon dead → zero token savings; revive needed.  
- system‑viz regen exit 134 due to merge‑augmentations crash; investigate.  
- Hook untracked in git (`slot-commit-worktree-enforce.mjs`) added.  
- Lock contention on `git` resolved via retry logic.  
- dispatcher `agent_memory_remember` missing key param fixed.  
- universal doctrine classification reachable via key override.  
- D1 corrupt `.ts‑1` archived per policy; D2 BusinessSync anomaly resolved.  
- P1 findings (routingMeta lock‑step, doc claim over‑claim, CLI guard over‑match) fixed.  
- Node `.cmd` shim path errors fixed with `process.execPath`.  
- API rate‑limit error during handoff write retried after back‑off.  
- Missing exports in `graphsage-trainer.mjs`: `positiveTypeMarginal`, `sampleStratifiedNegativeEdges`.  
- Cross‑galaxy edits auto‑cascaded and guarded; R12 fail‑loud honored.

**DOMAIN SPECIFICS**  
- Engines/dispatchers: `memoryDispatcher.ts` (wire‑in, universal reachability, hermes_reflection sidecar), Hermes self‑reflection dispatcher.  
- Actions: `memory-namespace-router`, `weekly_synthesis_get`, `hermes-self-reflect-populater.mjs`, `hermes-dream-cycle-synth.mjs`.  
- Feature‑counter lib (`feature-counter.mjs`) – per‑feature telemetry, optional domain field.  
- Slot‑bridge enforcement hooks: `main-tree-write-block`, `worktree-commit-route`, `git-add-lane-guard`, `slot-commit-worktree-enforce`.  
- Memory‑compact script (`memory-compact.mjs`).  
- MCP route take‑up script (`mcp-route-takeup.mjs`).  
- Audit scripts: `audit-mcp-route-takerate.mjs`, `backend-audit-chain-retune.mjs`.  
- System‑viz pipeline: `regen-viz.mjs` → `merge-augmentations.mjs`.  
- CronCreate for recurring `/loop` tasks.  
- Galaxy CLAUDE.md / MEMORY.md per domain under `mcp-server/src/engines/<galaxy>/`.  
- Metrics: 26/26, 24/24, 29/29, 28/28 test passes; 109 new tests PASS overall.

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`, `precompact-pending-guard.mjs`.  
- PRISM utilities: `/loop`, `/goal`, `/yolo-mode`, `/precompact-alpha`, `/precompact`, `/handoff`, `/startup`.  
- CronCreate (Skill tool) for recurring tasks.  
- Scripts: `memory-compact.mjs`, `mcp-route-takeup.mjs`, `audit-mcp-route-takerate.mjs`, `backend-audit-chain-retune.mjs`, `regen-viz.mjs`, `merge-augmentations.mjs`, `feature-counter.mjs`.  
- Dispatchers/skills: `memoryDispatcher.ts`, Hermes self‑reflection dispatcher.  
- Git hooks & commit‑enforcement scripts (`feedback_commit_to_slot_worktree`).  
- Node child_process with `process.execPath` for Windows compatibility.

**OPEN THREADS**  
- Remaining MS1 units: 25 pending (A1‑A3, B1‑B5, C1‑C2, D1‑D3, E1‑E3, F1‑F2, G1‑G4).  
- Phase‑3 cache‑breakpoint sweep (`U-CACHE-BREAKPOINT-SWEEPER`) pending.  
- Ollama daemon revive needed (token savings zero).  
- system‑viz regen exit 134 unresolved.  
- NN/GNN evaluation pending.  
- Memory‑archive query surface pending.  
- Operator‑touch items: B1, B3, B4, B7; A1, H1, H2, C2 Lima Ahmad curriculum.  
- Remaining P1 follow‑ups: B3 dispatcher‑wire, B1 reverse‑mirror cron registration, universal opt‑in for other dispatchers.  
- Ship remaining P1 item `U-NN-TRAINER-EXPORT-RESTORE` (add missing exports).  
- Execute `/compact` to finalize session.
