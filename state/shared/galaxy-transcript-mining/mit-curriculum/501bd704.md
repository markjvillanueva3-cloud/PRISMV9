# mit-curriculum session 501bd704 (2026-06-04, 22.3MB, spine 93KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- GPU‑stack health foundation (gpu_health.py, py‑subprocess‑bridge.mjs, GpuStackHealthEngine.ts, devDispatcher wiring) – commits f11efe4047 / 9e5dcca6ad.  
- F0 degeneracy fix: base‑rate normalization + direct‑embed inference + source‑enriched ghost embeddings; calibration negative test suite – commits a6bd95c163 (spec), 8abba4f27c (core), ae60024500 (eval), 2e9e94808d (P1 hardening).  
- Source‑enrichment & calibration negative commits dcb2c86bb8 / 44f4a90ccb.  
- Hash‑delimiter bug fix da6aedfc07; added \x1F delimiter to node hash.  
- GNN improvement: source enrichment commit dcb2c86bb8, typefix 5e7bbbac9d, wiki lesson 6f2bad4792.  
- `.gitattributes eol=lf` committed in `9bd4b22abd` (U‑FGC‑2b) – fixes CRLF/LF flip.  
- Wiki page `recurring-fleet-write-mistakes`; memory `feedback_recurring_fleet_write_mistakes`.  

**DECISIONS**  
- Adopt MS0 GPU‑stack health; Node→Python NDJSON bridge with fail‑loud gate.  
- Implement base‑rate normalization in voteDispatcher to prevent majority‑class collapse.  
- Replace GraphSAGE (edgeless inference) with direct‑embed k‑NN over 768‑d embeddings.  
- Enrich ghost embeddings leak‑free engine source signal; keep calibration off after negative results.  
- Add \x1F delimiter in node hash for resume cache stability.  
- Adopt per‑class stratified holdout for evaluation to avoid zero‑sample classes.  
- Make stratified split default; expose flat split via CLI flag `--flat-holdout`.  
- Record chosen split type in runAssessment result and propagate through assessHoldout.  

**OPERATOR DIRECTIVES**  
- `/checkin-india` slot binding → india‑work handoff.  
- `/loop [5m] /goal` targeting GNN improvement; `/startup-india` for loop/goal.  
- Keep all review/scrutiny gates active; bypass only gates that block work in other galaxies.  
- Operator decides next focus: continue loop with calibration‑done‑right and class‑balancing or redirect.  

**FINDINGS/BUGS**  
- Feature starvation (64 ghosts had embeddings) fixed by full‑coverage embedder.  
- Edgeless inference made GraphSAGE a no‑op; replaced with direct‑embed.  
- Base‑rate normalization required to avoid majority‑class collapse.  
- Leak in ghost embeddings (truth label in info field) stripped.  
- LOO calibration worsened AUROC/Brier; default off.  
- Missing \x1F delimiter caused resume cache invalidation – fixed.  
- CRLF/LF flip bug resolved by `.gitattributes`.  
- Flat seededShuffle split caused zero holdout samples for `prism_calc`, inflating macroF1 and under‑stating AUROC.  
- Stratified split corrects metrics: AUROC 0.808, macroF1 0.439, Brier lower, accuracy lower.  

**DOMAIN SPECIFICS**  
- Engines: `gpu_health.py` (GPU readiness probe), `py‑subprocess‑bridge.mjs`, `GpuStackHealthEngine.ts`.  
- Dispatchers: `devDispatcher` wiring for `prism_dev:gpu_stack_health`; `voteDispatcher.js`; `ghostEmbedText.js`.  
- Metrics & gates: AUROC ≥0.78 (now 0.808), macroF1 target 0.55, Brier ≤0.15, accuracy thresholds.  
- Paths: `scripts/py/gpu_health.py`, `scripts/lib/py-subprocess-bridge.mjs`, `src/GpuStackHealthEngine.ts`, `src/devDispatcher.ts`, `src/voteDispatcher.js`, `src/ghostEmbedText.js`.  
- Evaluation logic: `computeMacroF1` averages per‑class F1 over union(predicted, truth); split handling via `assessHoldout`, `runAssessment`; CLI flag `--flat-holdout`.  

**TOOLS USED**  
- PRISM tooling: `chat-slots.mjs`, `/checkin.md`, `/startup.md`, devDispatcher, OCLU.md, GNN model files (`graphsage-model.mjs`), `build-node-embeddings.mjs`.  
- Testing: vitest; test harnesses `makeGraph`, eval tests.  
- Node scripts & git hooks for slot enforcement and commit hygiene; Git operations with index.lock handling.  
- PRISM memory & wiki system (`feedback_recurring_fleet_write_mistakes`, `recurring-fleet-write-mistakes`).  
- Loop command `/loop`; CLI flag handling; NN‑EVAL.json writer and assessment dispatcher.  

**OPEN THREADS**  
- MacroF1 still below deploy gate; target 0.55 via class‑balancing reference pool.  
- Calibration strategy unresolved; LOO calibration failed, alternative methods pending (temperature scaling preserving AUROC).  
- Monitor AUROC after source enrichment (currently 0.808); ensure no regressions.  
- Verify resume cache continues to work with new hash delimiter across all nodes.  
- Finalize and merge stratified split as default; confirm `--flat-holdout` flag behaves correctly in downstream pipelines.
