# mit-curriculum session 41db1b82 (2026-05-17, 97.3MB, spine 667KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `tribal‑graph‑clusters.mjs` + tests – commit 15c161f63 (62 tests).  
- `tribal‑graph‑embedding.mjs` + tests – commit 862137931 (109 tests).  
- `course-mapper-lib` – 135 nodes, 54 tests – commit ac8d56da2.  
- `syllabus‑extract.mjs` – 3 courses – commit 7c87d216.  
- `unzip‑extract.mjs` – 227 zips → 209 nodes – commit fe1a32efc.  
- `course‑embed.mjs` – 192 courses, 618 semantic wires – commit fc608e6a0.  
- Phase 0 audit – commit 4086c8009.  
- U‑KC‑B1 (MIT‑OCW tips) – 126 tips, 44 tests – commit aa0335a8d.  
- U‑KC‑B2 (monolith tips) – 133 tips, 52 tests – commit 3d9324f2a.  
- U‑KC‑B3 wiring test – 7141 doc‑learned tips loaded – commit 44980b391.  
- U‑KC‑C1 formula ledger – 12 formulas mapped – commit e4a48ebf3.  
- U‑KC‑C2 algorithm ledger – 52 algorithms (18 ported, 19 ambiguous, 15 unported) – commit 05152dff62.  
- U‑KC‑D1 routing ledger – 65 routed, 69 FORGE‑QUEUE, 10 DUPLICATE, 16 DISCARD – commits cd00120dcd / 66aa07afa4.  
- U‑KC‑E1 doc‑reflection artifacts – commit d99df13f5d.  
- F1 B9 G9 triage closure – commit 7c9d6c1476.  
- Wiki/os namespace build (U‑CK04) – 6 syscall docs – commit 0ec9663dff.  
- Canonical command schema (U‑CK06) – commit 790ec17f25.  
- Caller audit & deprecation report (U‑CK10) – commit 0ca1d0feb0.  
- U‑KC‑E1‑tail pointer – commit dc45efac0b.  
- Wiki/os entities (6 syscalls, 1 process, 1 command, 1 pipeline, 1 runqueue, 1 session) – commits 0ec9663dff / 4da65465c7 / 9d44c4ac12 / a46d6a98b3 / 64ccbed855 / f5cb159398.  
- Session‑summary spec (iter 27) – commit eb7853a6a8.  
- Hand‑off artifact for loop session – commit 93648157b1.  
- Algorithm nodes: OperatorSplittingMethod 1323fa4ee7, ODEIntegrator b38a9f2285, LinearStateSpaceModel a547223bbf, FiniteDifferenceMethod 7cbbe511d7, GradientDescent 271351e7ec, FiniteElementMethod1D 937bc66e76, LagrangianMechanics 56243befc9, SafeExpressionEvaluator 47e93d03fa.  
- Proposals & tooling – commit dea7274d23.  
- Bulk emitter – commit 5d5c363f0e.  
- CLI test suite – commit 6ae5399608.  
- Doc‑reflection commits: 592cc28260, e0fbe51aed, 0b237f435b.

**DECISIONS**  
- Adopt graph‑of‑graphs taxonomy (L0 atom → L8 universe); DAG aggregation via Jaccard; lateral wires via Ollama nomic‑embed‑text cosine.  
- Two‑arm per-file scrutiny gate + 3‑way stop gate for task completion.  
- No auto‑generated stub engines; reuse existing PRISM engines (`prism_calc`, `prism_adaptive_control`, `prism_intelligence`).  
- Slot claim: active work slot **india**; all edits staged via `git update-index` to bypass lint‑staged stash/restore.  
- Safety constraints: no inline constants, destructive ops require explicit authorization.  
- Precompact handoff pattern: stable session ID + per‑agent handoff file (`HANDOFF-<STABLE>.json`).  
- `/checkin-india` forces take of india slot and binds handoff to `india-work`.  
- Autonomous loop via `/loop`; cron trigger (*/10 min) with optional `/yolo-mode` for auto‑fix up to 3 attempts.  
- `/compact` after precompact threshold; handoff resumes next step automatically.  
- Enforce `TRUNCATION_PROOF_LIMIT = 10000` in all exact‑count tests.  
- Knowledge Injection Pipeline Engine (`KnowledgeInjectionPipelineEngine`) CLI runner for injecting extracted knowledge into PRISM OS, Obsidian, AI registry.

**OPERATOR DIRECTIVES**  
- `/loop extract college coursework until all units complete = /goal`.  
- `/checkin-india …` – force‑take india slot, run full checkin pipeline.  
- `/yolo-mode` – autonomous execution, auto‑fix up to 3 attempts, no questions unless blocking ambiguity.  
- `/compact` – required after precompact threshold; handoff will resume next step automatically.  
- `/rgs register milestone KNOWLEDGE-CONVERSION-MS0`.  
- `/goal finish all units (KNOWLEDGE‑CONVERSION‑MS0)`.  
- `/loop all work until finished`.  
- `CronDelete 32fcf842` – stop recurring loop when desired.  
- `yolo no-cap` – continue looping without capacity limits until explicit deletion.

**FINDINGS/BUGS**  
- Slot‑claim bug: “kilo” → bravo; resolved by claiming india.  
- Cluster & embedding lib test failures fixed: malformed input, endpoint drift, hostile input, vector‑dim mismatch, U+001F contamination, retry jitter, orphan `.tmp`.  
- Git commit issue due to lint‑staged stash/restore – workaround `git commit -- <paths>`.  
- Set.has override exploit & endpoint contract drift (P0/P1).  
- `classifySchool` → undefined → fallback `Z_uncategorized`; `classifyDomain` → array → `"X_unknown"`.  
- `tribal_export_bulk` inverted prismMapping fixed by reversing lookup.  
- 17 courses remain metadata‑only; scanned PDFs contain only cover boilerplate, real text in per‑page `data.json`.  
- Embedding idempotency bug: checkpointed vectors prevented duplicate wires on resume – fixed.  
- Course-mapper issues: 0‑confidence promotion, domain key normalization, unstable IDs, missing provenance override – corrected.  
- Precompact threshold hit after 967 k tokens; U‑KC‑B1 tests pending re‑run post‑compact.  
- Read pipeline strips C0 control bytes → replaced with `String.fromCharCode` source‑escape helper.  
- Regex boundary issue `\bPOST\b` fixed to underscore‑aware boundaries.  
- Engine `search()` truncation risk (`limit < 10000`) resolved by raising limit and documenting change.  
- Guard auto‑unstage race mitigated via pathspec‑only unstage logic.  
- Provenance tag MAX_TAG_LEN >80 chars preserved.

**DOMAIN SPECIFICS**  
- Engines/Actions: `tribal-graph-clusters`, `tribal-graph-embedding`, `buildLateralWires`, `mergeIntoCheckpoint`, `loadCheckpoint`, `tribal_search`, `tribal_capture`, `tribal_suggest`, `tribal_stats`, `tribal_export_bulk`, `prism_calc`, `prism_adaptive_control`, `prism_intelligence`, `TribalKnowledgeEngine.loadDocumentLearnedTips()`, `knowledgeDispatcher.ts` (`prism_knowledge:tribal_search`), `course-to-tribal-tips.mjs`, `monolith-to-tribal-tips.mjs`, `KnowledgeInjectionPipelineEngine`, `SafeExpressionEvaluator`, `OperatorSplittingMethod`, `ODEIntegrator`, `LinearStateSpaceModel`, `FiniteDifferenceMethod`, `GradientDescent`, `FiniteElementMethod1D`, `LagrangianMechanics`.  
- Dispatchers: `course-data-router-lib`, `chat-slots.mjs`, `audit-roadmap-drift.mjs`, `checkin.md` pipeline, `knowledge-injection-pipeline.ts`.  
- Metrics/Paths: `TRIBAL_TIP_INDEX.json` (11 586 tips), `mcp-server/src/data/*‑tips.ts`, `state/shared/AGENT_CHAT.jsonl`; `/system-viz` graph nodes L4a course‑tribal; lateral wires `relates-to`; checkpointed embeddings; provenance fields; doc:`<docId>` tags; confidence * log2(usage_count+2); `TRUNCATION_PROOF_LIMIT 10000`; U+001F separator for deterministic sorting.  
- Artifacts: MIT‑OCW tips JSON, monolith tips JSON, routing ledger JSON/MD; wiki/os namespace schema (U‑CK04), command frontmatter schema (U‑CK06), caller audit report (U‑CK10); session summary spec; handoff artifact.

**TOOLS USED**  
- PRISM CLI: `/checkin`, `/compact`, `/loop`, `/yolo-mode`, `/rgs`, `/goal`, `/pick-unit`, `/task`, `/build`, `/forge7`, `/forge-audit-v2`, `/fleet-reaper`.  
- Git hooks: `lane‑guard`, `comprehensive-build-enforce`, `pretooluse-unified`, `lint-staged`, `git-add-lane-guard`.  
- Node.js test harness: `node:test` (vitest), `node-process-janitor`.  
- Docker: Ollama nomic‑embed‑text 768d container; isolated runs.  
- Obsidian integration: wiki notes, memory index, CLAUDE.md pointer.  
- System viz: `/system-viz-brain`, `/system-viz-first`, system‑viz ping.  
- Scripts: `sync-h-c-drives.mjs`, `bootstrap-h-mirror.mjs`, `knowledge-injection-pipeline.ts`, `mcp-server/scripts/knowledge-injection-pipeline.ts`, `per-agent-handoff.mjs`, `stable-session-id.mjs`, `audit-roadmap-drift.mjs`, `chat-slots.mjs`, `precompact-pending-guard.mjs`.  
- External: pdftotext & pymupdf (OCR disabled).

**OPEN THREADS**  
- Re‑run U‑KC‑B1 tests after `/compact`; dispatch reviewers again; commit converter + test + generated JSON.  
- Ensure precompact guard cleared post‑`/compact`; session can terminate automatically.  
- Dispatcher wiring for closure‑input nodes pending operator decision.  
- Full `npm run build` of mcp‑server not executed; confirm compile.  
- Real Knowledge Injection Pipeline execution (`--apply`) not yet run – knowledge not written into repo.  
- U‑CK05 mirror‑gen pending operator review.  
- U‑CK07/08/09 scope confirmation pending.  
- `/pick-task` alias phase B+ not yet backgrounded.  
- Drift‑detector false positives repaired (2‑line fix).  
- Loop remains in “abandoned” state; heartbeats continue until `CronDelete 32fcf842`.
