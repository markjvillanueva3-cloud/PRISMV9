# mit-curriculum session 77971357 (2026-05-17, 36.5MB, spine 184KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-REGEN-VIZ-MERGE-FAILLOUD` – helper, tests, merge‑guard (`scripts/regen-viz-merge-guard.mjs`).  
- `U-SOURCEHASH-DOC-ALIGN` – spec & test‑docstring updated.  
- `U-COMPLEXITY-FALLBACK` – lib for `complexityFor`, planner update, 41/41 tests.  
- `U-BUG-FINDINGS-WIKI-HOOK` – stop‑hook auto‑writes wiki entries; 20/20 tests.  
- Backfill of three prior‑session bug‑finding wiki entries (dog‑food).  
- Two doc‑reflect commits (`CLAUDE.md` regression, self‑awareness pointer).  
- `b1e599d5fc` – `U-FEEDBACK-FORCING` shipped; composite key issue resolved.  
- `17235ead06` – F8 “cache reader dead” misdiagnosis retracted; token‑savings probe fixed.  
- `5146a943df` – memo‑guard for `error-pattern-promote` (`shouldSkipMemo`) merged with tests.  
- RTK 0.40.0 binary installed to `H:/prism/.claude/bin/rtk.exe`; hook wired in `settings.json`.  
- `build-requests-viz-sync.mjs` – 296 LOC, commits to repo.  
- `high-value-additions-rank.mjs` logged in `USER-BUILD-REQUESTS-LOG.md`.

**DECISIONS**  
- Adopt 4‑tier `resolveUnitKey` resolver (composite → bare ID → slot → current position).  
- Use “fail‑loud” (`/karpathy`) for merge‑augmentations; stop silent rollback to stale graph.  
- Keep peer‑claimed files uncommitted; use `git reset`.  
- Ship bug‑finding wiki rule immediately after hook implementation; auto‑backfill prior entries.  
- Ship `U-FEEDBACK-FORCING` now; defer remaining MS1 P1 backlog (`U-RIE-ADAPTER`, `U-CALIBRATION`, `U-TRANSFER`).  
- Do not build new cache‑reader hook – existing `file-read-cache.mjs` suffices.  
- Accept RTK missing on host; install manually and wire hook.  
- Scope narrowing for F3 Ollama offload before building hook.  
- Persist awareness improvements via protocol docs, not session state.

**OPERATOR DIRECTIVES**  
- `/checkin-lima`, `/loop`, `/goal`, `/forge-audit-v2` – run pipelines.  
- `node scripts/validate-unwired-signal.mjs` to separate real orphans from noise.  
- Wire 82 unwired engines in Lathe domain after validation.  
- Schedule recurring/self‑paced prompts via `/loop`.  
- Improve awareness of `/system-viz`, Obsidian brain, prism‑awareness, memories, tribal knowledge injection system.

**FINDINGS/BUGS**  
- Silent failure of `merge-augmentations.mjs` under 4 GB heap; resolved by `--max-old-space-size=8192`.  
- Regex boundary bug in `complexityFor`: removed trailing `\b`.  
- Stale test fixture `SAMPLE_SIDECAR_ENTRY` updated to flat plan format.  
- Peer‑claimed file (`dev-tool-leverage.md`) accidentally staged; corrected via safe reset.  
- `classifyHits` regex mis‑parsed system‑viz output; fixed with real `find` format.  
- Ghost nodes identified by prefix `ghost.`.  
- F8 cache reader misdiagnosis due to probe looking for non‑existent `.hits`.  
- Memory‑relevance‑inject hook emits no telemetry; hit‑rate unmeasurable.  
- Merge step in `scripts/regen-viz.mjs` fails silently under 4 GB heap (duplicate of above).  

**DOMAIN SPECIFICS**  
- **Hooks/Dispatchers**: `pick-prefresh-inject.mjs`, `stop-bug-finding-wiki-gate.mjs`, `error-pattern-promote`.  
- **Slot & Claim Stores**: `state/shared/chat-slots.json`, `state/shared/slot-task-claims.json`.  
- **Graph Engine**: `scripts/regen-viz.mjs` orchestrator, merge‑guard helper.  
- **Metrics**: system‑viz graph size (nodes 145k, edges 700k), envelope drift reduced 175→11, commit memory pressure 97 % used.  
- **Paths**: `/checkin-lima`, `/loop`, `/goal`, `/forge-audit-v2`, `H:/prism/.claude/hooks/pick-prefresh-inject.mjs`, `scripts/regen-viz.mjs`, `merge-augmentations.mjs`, `state/shared/system-viz/...`, `H:/prism/.claude/bin/rtk.exe`.  

**TOOLS USED**  
- PRISM CLI skills: `/checkin-lima`, `/loop`, `/goal`, `/forge-audit-v2`.  
- Scripts: `scripts/regen-viz.mjs`, `merge-augmentations.mjs`, `build-requests-viz-sync.mjs`, `high-value-additions-rank.mjs`, `validate-unwired-signal.mjs`.  
- Hooks: `pick-prefresh-inject.mjs`, `stop-bug-finding-wiki-gate.mjs`, `error-pattern-promote`.  
- Test harness: Jest (`.test.mjs`).  
- RTK install script (`rtk-ai/rtk` release fetch & extraction).  

**OPEN THREADS**  
1. F3 Ollama offload – scope narrowing, then build hook.  
2. Slot‑worktree migration – decide fleet‑wide default.  
3. Memory‑relevance‑inject telemetry – add counter/cap for hit‑rate measurement.  
4. Further system‑viz + Obsidian integration – formalize injection awareness protocol.  
5. Execute wiring pass for Lathe domain after validation.  
6. Monitor log drift continuously; auto‑resync if entries >30.  
7. Verify system‑viz graph regeneration indexes new awareness docs and build logs.
