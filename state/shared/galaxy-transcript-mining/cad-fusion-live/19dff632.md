# cad-fusion-live session 19dff632 (2026-06-10, 19.2MB, spine 139KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `PayrollLiabilityFilingEngine` wiring – commits e44a3a1592 & e649790e76 added 5 dispatcher actions (`payroll_compute_940`, `payroll_generate_w2`, `payroll_reconcile_w2_941`, `payroll_contractor_1099_totals`, `payroll_remit_liability`).  
- Transcript‑mining tool – commit 9dfd621910 (scripts/mine‑hotel-transcripts.mjs) + regenerated digests 61518eb988.  
- Hotel forge roadmap spec – commit 099e6b92bd (`state/shared/specs/HOTEL-FORGE-ROADMAP-2026-06-09.md`).  
- Verify‑first unit `U-HOTEL-P0-CONSTANTS-AUDIT` – commit f069052772.  
- `U-HOTEL-PORTAL-PERSISTENCE` – CustomerPortalEngine’s 4 durable Maps replaced with SQLite‑WAL; rateBuckets left in memory (tests 92/92).  
- `U-HOTEL-ALLOWLIST-WRITE-REVIEW + REALTIME-VERIFY` – realtime transport verified, write‑allowlist remains 403 for all financial/PII actions.  
- `U-HOTEL-ALLOWLIST-WRITE-ENABLE` – opened manager‑tier workflow writes (`handoff_*`) behind per‑action role gate; tests 23/23.

**DECISIONS**  
- Adopt “verify‑first” discipline; skip rebuilding already shipped units.  
- Default grunt coder: qwen2.5‑coder:32b; heavy synthesis uses gpt‑oss:120b only when needed.  
- Mechanical summarization via Ollama (qwen2.5‑coder:32b for code, gpt‑oss:20b for bulk).  
- High‑level reasoning & roadmap via ultracode workflow (8 agents, resumable).  
- Fold persistence into existing CustomerPortalEngine using CoordinationStore pattern; no separate store engine to avoid duplicate‑name conflict.  
- Keep `rateBuckets` transient – reset on restart.  
- Use existing `verifyToken`, `requireRole`, and global `auditLog` middleware for auth/audit; no new RBAC subsystem.  
- Open only workflow‑state writes (`handoff_*`) to browser dispatch; keep all financial/PII writes 403 by omission from write map.  
- Guard regex broadened to match real “Unknown business action: X” string; added `{note:"method not callable"}` idiom.

**OPERATOR DIRECTIVES**  
- `/checkin-hotel` (slot bind, run full checkin).  
- `/startup-hotel` (force slot claim, run startup pipeline).  
- `/loop [10m] /goal …` (run loop with goal to finish remaining hotel units).  
- `/compact` before starting `U-HOTEL-PORTAL-PERSISTENCE`.  
- “start” → execute roadmap from first pending unit.  
- Approve role model → implemented `U-HOTEL-ALLOWLIST-WRITE-ENABLE`.  

**FINDINGS/BUGS**  
- Stale audit claim: “missing tax engine” is false; all tax engines already built/wired.  
- Mobile engines: 6 wired, only `MobileVoice` orphan (already tagged).  
- Q2S DFM bug confirmed at line 1471/1512; filed to charlie.  
- Allowlist regression guard detected two issues:  
  - `marketplace_lead_get` returns null → ambiguous; needs real‑id probe.  
  - Regex in placeholder detector fails on “TODO: wire engine”.  
- P0: guard regex missed real default string – fixed.  
- P1: new `.db` files not git‑ignored – added to `.gitignore`.  
- Stale header doc drift in write‑review spec – corrected.  
- Pre‑existing bugs surfaced during grounding:  
  - `hr_pto_approve` double‑apply (no status guard).  
  - Empty `approved_by` accepted.  
  - Two divergent PTO approval engines.

**DOMAIN SPECIFICS**  
- Engines: `PayrollLiabilityFilingEngine`, `CustomerPortalEngine`, `MarketplaceSeedingEngine`, mobile engines (`MobileVoice`, etc.).  
- Actions wired via `businessDispatcher.ts`: payroll actions, allowlist actions (17).  
- Metrics: IRS values used in tests – FUTA $72, W‑2 box4 $3,100, SS cap $10,918.20, Medicare $4,075, 1099 $1,500.  
- Paths: `mcp-server/src/tools/dispatchers/businessDispatcher.ts`, `src/data/payroll-tax-tables.ts`, `state/shared/specs/HOTEL-FORGE-ROADMAP-2026-06-09.md`.  
- Persistence Pattern: SQLite‑WAL via better-sqlite3, `CoordinationStoreEngine` pattern.  
- Dispatchers: `businessDispatcher.ts` (real dispatcher), `realtimeDispatcher.ts`.  
- API endpoints: `/api/v1/business/dispatch`, `/api/v1/realtime/*`; SSE event types (`quote:update`, `job:progress`).  

**TOOLS USED**  
- Slot‑binding helpers (`slot-bind-enforce.mjs`, `chat-slots.mjs`).  
- Transcript miner (`scripts/mine-hotel-transcripts.mjs` via `ask-ollama.mjs`).  
- Ultracode workflow (`wf_2bfa0b6b-9b0`).  
- PRISM guard detector & 3‑of‑3 scrutiny gate (A/B/C reviewers).  
- Vitest test harness (`false-wire-regression-guard`, portal E2E).  
- Node scripts: `chat-slots.mjs`, `ask-ollama.mjs` (cost router).  
- Git for commits, ledger JSON (`SCRUTINY_LEDGER.json`).  

**OPEN THREADS**  
- Build `U-HOTEL-PORTAL-PERSISTENCE` (persist 4 CustomerPortalEngine maps to SQLite‑WAL).  
- Fix allowlist regression guard: resolve null case for `marketplace_lead_get`, correct regex in placeholder detector.  
- Verify completion of remaining roadmap units after persistence build.  
- Follow‑up on identity mapping for workflow writes (A‑P2).  
- Fix `hr_pto_approve` double‑apply and related PTO bugs.  
- No pending background tasks; slot ready for next unit.
