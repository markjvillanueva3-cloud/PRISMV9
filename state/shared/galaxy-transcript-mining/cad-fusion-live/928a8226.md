# cad-fusion-live session 928a8226 (2026-06-10, 68.1MB, spine 675KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `fix-hookify-frontmatter.mjs` – 110 `.local.md` files fixed; 28 non‑ASCII files sanitized.  
- `remove-dangling-hook-refs.mjs` – removed six dead module refs from `settings.json`.  
- `audit-unwired-engines.mjs` – inventory of 90 unwired engines (53 core, 23 external bridges, 14 ops/test scaffolds).  
- Commit c593b096fb – Ollama pull & synergy fix (`gpt‑oss:120b` routed correctly; GPU smoke test passes).  
- Commit 13017de764 – Hookify frontmatter & dangling hook‑refs fixes.  
- Commit 29f2d177f6 – System‑wide gap‑map artifact.  
- Commit 8550436c8a – CAG‑router phantom Qdrant collection label fixed.  
- Commit c9fe32d8b2 – Wiki↔tribal embedder OOM & context‑window bug fixed; driver launched.  
- `182788232a` – cap‑safe tribal index loader (8/8 tests).  
- `a3e6d3ca97` – fail‑open guard for over‑cap parse, shrink‑guard on write, closing‑] assertion (7/7 tests).  
- `5467482be4`, `c994bf3aad` – doc‑reflection of CLAUDE.md regression & wiki lesson.  
- `fb314a6fd1` – Docker‑service‑health CLI (`docker-service-health-check.mjs`; 5/5 tests).  
- `d3ed737c23` – Stop‑hook wiring of Docker guard into fleet Stop chain.  
- `e2081e0780`, `d22681f5d2` – U‑MCP‑FIXSTART: reap‑then‑start recovery for MCP outages (14/14 tests).  
- `006c435ddb` – corrected MCP restart documentation.  
- `bee977a004` – added `NODE_OPTIONS=--max-old-space-size=8192` to reranker spawn env.  
- `ed6662f45e` – U‑MCP‑CMDMATCH‑FIX: slash‑agnostic regex & daemon‑count logic.  
- `29fb555f13` – U‑HOOK‑UNREG‑PROTOCOL‑FIX: JSON‑stdout protocol for `stop_on_hook_unregistration`.  
- `6ca2f6afd2` – U‑MCP‑BOOT‑HEAP‑FLOOR: 4096 MB heap floor on all MCP spawn paths.  
- `698cf12ad2` – static list of 4 “freeze‑tasks”.  
- `4141daf9d8` – migration‑freeze marker (`state/shared/MIGRATION-FREEZE-ACTIVE.flag`).  
- `U-FTH-WIKI-LESSON` – wiki lesson compiling fleet‑task‑health cry‑wolf fix.  
- `tribal-by-domain-inject` – wiki promotion for PSN/tribal knowledge.  
- `U-QP-COST-BRIDGE-WIRING-TRUTH` – confirmed cost‑bridge router wiring (16 hooks via one dispatcher).  
- `U-QP-AWARENESS-GEN-ROUTER-AWARE` – restored missing awareness generator, made it router‑aware.  
- `U-QP-COST-SAVINGS-TEST` – 13/13 vitest cases for CostSavingsTrackerEngine.  
- `U-BW-DISPATCHER-SCAN` – repointed cascade defaults to live Blackwell models; extended source‑lock guard to dispatcher tree.  
- `U-BW-DISPATCHER-SCAN-WIKI` – wiki lesson documenting blind‑spot fix.  
- `U-PSN-SNAPSHOT-REFRESH` – regenerated 6‑day stale PSN snapshot; verified real under‑wired P0 pairs.  
- `U-QP-DIGITALTWIN-TEST` – added reference‑value test for DigitalTwinEstimator (5th algorithm).  
- `U-QP-MARGIN-FLOOR-GATE` – margin‑floor guard on QuoteEstimatorEngine; WARN+flag on below‑floor margins.  
- `U-QP-MARGIN-FLOOR-SHOPCONFIG` – self‑source floor from ShopConfigurationEngine (default 20 %).  
- `U-QP-NRE-AMORTIZE-TEST-FIX` – corrected stale NRE amortization test.  
- `ec597dbcb3` – margin‑floor dispatcher round‑trip (7/7).  
- `87d5c4bf9a` – margin‑floor banner on QuoteBuilderPage (6/6, tsc‑clean).  
- `4b0980c56a` – AI‑substrate routing matrix coverage 5/5 (27/27).  
- `62b114cb7b` – ResizeObserver polyfill for quote‑pages test (13/13 green).  
- `8258918f78` – U‑QP‑FRONTEND‑TEST‑REPAIR: fixed flat array mock, added null‑guards and failure‑mode test.  
- `44fab1be37` – dispatch map for fleet synergy goal (rows 8–15 assigned to owning slots).  
- Provenance gate commit (`4c12a75a8d`) – blocks synthetic/placeholder calibration factors from promoting live quote‑time file.  
- `U-QP-OUTBOUND-PROMOTE-GATE` (`d294957c4d`) – real outbound sold‑price distribution alignment gate (fail‑closed).  
- `U-QP-CALIBRATION-FRESHNESS-PREFLIGHT` (`bf10035ec0`) – warns on >24 h stale factors; optional hard cutoff to raw FMV.

**DECISIONS**  
- Unwired engines: triage per galaxy owner, not wire blindly.  
- Golf slot: keep legacy `golf-slot-write-allowlist.mjs` disabled globally (edit `settings.json`).  
- Ollama pull: resume stalled `gpt‑oss:120b` after fixing PowerShell parse error; use `"home_blackwell"` hardware gate.  
- DEFAULT_NUM_PREDICT=1024 in ask‑ollama; batch workflow agents to 3 sequential runs; add tests for new behavior and truncation diagnostics.  
- Node heap: `--max-old-space-size=8192`; clamp embed input to ~2000 chars.  
- Re‑embedding: index <512 MiB, <150 MiB heap; sharding only after ~33 K entries.  
- MCP keep‑alive: upgraded guard solves redundancy; floor heap on all spawn paths (4096 MB).  
- Migration‑freeze: replace static 4‑task list with marker for ~47 tasks; partition disabled migration‑freeze tasks in fleet-task-health-watch.  
- Cost‑bridge: router consolidation to avoid double execution and false cry‑wolf reports; tag `WEDMQuoteBridgeEngine` as WIRE‑EXEMPT.  
- Source‑lock: extend to dispatcher tree; repoint cascade defaults to live Blackwell tiers.  
- PSN snapshot: refresh stale data; route fix to Sierra’s cross‑substrate lane.  
- DigitalTwin: add reference‑value test (R9).  
- Margin‑floor gate: implemented with caller‑override and canonical ShopConfig source.  
- Provenance & outbound gates: adopt fail‑closed design; keep loop closed by wiring `QuoteEstimatorEngine.applyToQuote()`.  
- Focus on charlie domain per operator directive; do not fabricate peer‑galaxy work.

**OPERATOR DIRECTIVES** (verbatim)  
- `/startup-charlie /loop [10m] /goal …` – resume charlie slot, closed‑loop + quoting production readiness.  
- “synergize ollama … all surfaces … goal clear: everything wire, tested, validated and synergized across the entire system.”  
- “Utilize ultracode for planning, deep reasoning and orchestrating + ollama local llm for grunt work (searching, reads, coding).”  
- “continue with charlie domain work” (twice).  
- `/compact` to continue from handoff or focus on open threads.

**FINDINGS/BUGS**  
- Hookify: 110 files missing YAML front‑matter; one file had UTF‑8 emoji causing `charmap` decode error.  
- Dangling refs: six dead `.mjs` hooks removed from `settings.json`.  
- Scheduled‑task warning: three “MISSING” tasks part of migration freeze (no action until migration completes).  
- Unwired engines: 90 built engines lack dispatcher/route/hook reference; one orphan (`MillPrintToProgramEngine`).  
- Domain routing: `getDomainTokens(lima)` cannot emit “academy”; tribal-by-domain-inject maps lima→general.  
- Ollama pull parse error: em‑dashes in `ollama-resilient-pull.ps1` halted download at ~12 %; replaced non‑ASCII, resumed (~2.7 GB remaining).  
- Synergy bug: after installing 120b, `routeModelForTask({category:"search_synthesis", hardware:"blackwell"})` still selects 20b.  
- BLACKWELL_CEILING path failed due to wrong hardware string; ask‑ollama default num_predict=128 caused empty response for 120b.  
- Workflow tool exceeded API rate limits with 11 concurrent agents.  
- CAG‑router hotSources label referenced non‑existent `prism-memory` collection.  
- Wiki↔tribal embedder crashed on index >512 MiB and exceeded nomic’s context window.  
- V8 string‑length cap caused silent PSN leg #5 failure on 537 MB index; fail‑open bug in tribal-embed-index.mjs clobbered brain to 1-entry stub.  
- Duplicate MCP daemons (two orphan processes) caused :3100 down; auto‑reconnect used correct command but did not clean old daemons.  
- Default Node heap OOM on >150 MB index prevented live reranker.  
- MCP server OOM on boot due to inherited 384 MB NODE_OPTIONS; fixed by heap floor.  
- singleton-service-guard cmdMatch regex matched only backslashes, missing real daemon.  
- stop_on_hook_unregistration emitted no JSON → always “NOT evaluated”.  
- fleet-task-health-watch warned on intentionally‑disabled migration freezes; added EXPECTED_DISABLED_TASKS partition.  
- System‑viz find OOMs under 384 MB cap.  
- Cry‑wolf bug: static list of 4 tasks does not cover ~47 disabled tasks.  
- Cry‑wolf bug in quoting cost‑bridge: per-file wiring check reports “0 wired” while router wires all 16 hooks.  
- Stale awareness generator (QUOTING-AWARENESS.md) missing generator and false “unwired”.  
- WEDMQuoteBridgeEngine incorrectly flagged as orphan; it is route‑only adapter (WIRE‑EXEMPT).  
- Retired model defaults (:3b, :7b, :14b) still hard‑coded in aiReasoningDispatcher.ts.  
- Source‑lock test omitted dispatcher tree.  
- Silent cascade failures due to retired model defaults and dispatcher blind spot.  
- PSN snapshot stale but real under‑wired P0 pairs.  
- Frontend did not surface `below_margin_floor`; banner added after dispatcher round‑trip test.  
- NRE amortization logic correct; test was wrong.  
- Server rate‑limit blocked multi‑agent scrutiny agents (deferred).  
- Flat array mock in quoteHistory caused render crash at QuoteBuilderPage.tsx:2115.  
- Missing ResizeObserver in jsdom caused test failures; polyfill fixed.  
- Synthetic promotion of calibration factors allowed; provenance gate added.  
- OCR `$1` noise dominates 65 % of `jm-sold-orders.json` ext_price; outbound gate stays unverified.  
- Structural mismatch: OCR `BlueprintAnalysis` vs bridge expected shape → silent GD&T loss, dimensions mis‑read.  
- TSC OOM abort (exit 134) gives false “clean” result; full‑heap run shows baseline errors unrelated to new changes.  
- Stale calibration factor (>24 h) previously applied silently; fixed by freshness preflight.

**DOMAIN SPECIFICS**  
- **Hooks & Settings**: `settings.json` (SessionStart, UserPromptSubmit, PreToolUse arrays), `golf-slot-write-allowlist.mjs`, `golf-slot-reaper-guardian.mjs`.  
- **Engine Wiring**: `millDispatcher.ts`, `automationDispatcher.ts`, `SFCCompareEngine`, `LocalEmbeddingEngine`, `QuoteEstimatorEngine`, `DigitalTwinEstimator`, `CostSavingsTrackerEngine`, `QuotingClosedLoopEngine`, `OutboundPriceIndexEngine`, `BlueprintOCREngine`, `AccountingHardeningEngine`.  
- **Ollama**: models – `gpt‑oss:120b` (65 GB), `qwen2.5-coder:32b`; vision ensemble (`qwen3-vl`, `qwen2.5vl`, `llama3.2‑vision`, `moondream`).  
- **Docker**: 4/4 prism containers up; health guard via `docker-service-health-check.mjs`.  
- **Qdrant**: collections – `prism_skills`, `prism_engines`, `prism_formulas`; singleton container `prism-qdrant`.  
- **PSN / tribal injection**: leg #5 returns ranked hits from hyperMILL docs; `tribal-by-domain-inject` maps domains.  
- **Obsidian vault**: 11,886 `.md` files in `H:/prism/knowledge/memories`.  
- **System‑viz**: `findInGraph` queries; heap cap 384 MB via `/h/.claude/bin/node`.  
- **Prism galaxies / front/back end build**: per‑galaxy sync, frontend merges (quebec).  
- **CLAUDE.md**: stale `qwen2.5-coder:7b` in user’s global copy; project file cleaned.  

**TOOLS USED**  
- Node scripts: `fix-hookify-frontmatter.mjs`, `remove-dangling-hook-refs.mjs`, `audit-unwired-engines.mjs`, `fleet-reaper-sweep.mjs`, `singleton-service-guard.mjs`, `stop_on_hook_unregistration.mjs`.  
- PowerShell: `ollama-resilient-pull.ps1` (ASCII‑sanitized).  
- Docker CLI & health check (`docker-service-health-check.mjs`).  
- Qdrant client.  
- Ultracode workflow orchestrator.  
- TypeScript compiler (`tsc`, 16 GB heap for full checks).  
- Jest / Vitest test frameworks.  
- Git (commits, pushes).  
- Ask‑ollama CLI (ask‑ollama.mjs).  
- `load-tribal-index.mjs`, `embed-wiki-into-tribal-index.mjs`.  

**OPEN THREADS**  
- **Qdrant tribal migration** – finalize moving 33 K index into Qdrant; ensure stable corpus.  
- **fleet‑task‑health** – complete EXPECTED_DISABLED_TASKS partition; monitor migration freeze completion.  
- **MCP resilience** – verify U‑MCP‑FIXSTART and heap floor across all spawn paths; confirm no OOMs.  
- **Unwired engine triage** – owners decide wire/exempt/archive for 90 engines (core, bridges, scaffolds).  
- **Charlie domain work** – complete closed‑loop provenance & outbound gates; build OCR bridge adapter (`U-QP-BLUEPRINT-OCR-BRIDGE-ADAPTER`).  
- **ERP/QuickBooks integration** – data‑blocked for `AccountingHardeningEngine`.  
- **Wiki↔tribal re‑embed coverage** – finish driver, reduce 84 % chunk failures; ensure no OOMs.  
- **Frontend UI/UX** – refine `below_margin_floor` banner and other UI elements; address remaining hotel domain failures (`QuoteAnalyticsPage`, `JMDieFleetScan`).  
- **Rate‑limit handling** – implement back‑off for multi‑agent scrutiny agents.  
- **OCR noise cleanup** – remove `$1` noise from `jm-sold-orders.json`; enable outbound gate live activation.  
- **Source‑lock guard** – extend to dispatcher tree; ensure no hard‑coded retired models remain.  
- **Testing coverage** – add missing tests for new guards, provenance, and outbound logic.
