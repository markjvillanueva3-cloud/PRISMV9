# cad-fusion-live session 0e5669d2 (2026-06-10, 44.9MB, spine 372KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- B1 sync‑resilient (`168c202646`): `writeWithRetry`, guard `ensureDir/writeFileSync`.  
- B2 index‑meta (`ea21008f25`): stamp `last_verified`, add `regen-wiki-index-meta.mjs`.  
- B3 maint‑cron installers (`8c4dff660a`): disabled Windows tasks for promote‑memory‑to‑wiki & vault‑rot‑sentinel.  
- C2 link‑heal scorer (`984313825e + d948b85a74`): recalibrate tiers, harden `autoApplyEligible`, decay leak fixed; autoApplyCount 33k→2.  
- C3 tribal‑coverage refresh (`838123429b`): coverage 83.7 %; stale 31.5 % banner cleared.  
- RAG‑wire fix (`9e4376b3b2`): wire `memory-rag-inject`.  
- MCP‑FLEET‑CAPACITY‑MS0 (`43e1b8e449`): hook heap capped to 384 MB, MCP maxConnections ↑512, pressure status added, GPU offload widened.  
- fleet-task-health-watch.mjs (`3d796dcf5c`): add `EXPECTED_UNREGISTERED_TASKS`, `aggregateHealth`, WSL Memory Guard; 62/62 tests.  
- vault-backlink-schema.mjs (`96ed5222e2`): key normalization, node‑id sorting, backlink record creation; 17 tests.  
- regen-viz stage (`0e2724871a`): auto‑refresh `vault-backlinks.json` on graph regeneration.  
- doc_nodes dispatcher (`14aba14e3a + 4a44b5393c`): enum, case, schema, wrapper; 14 tests.  
- vault‑access map update (`f23c58b42d`): close `.canvas` gap, fix stale `memory-rag-inject`.  
- canvas-read-lib.mjs & CLI subcommands (`2d49bf0d33`): add `canvas`, `canvas-doc`; 15 tests, staleness flag, Lgit regex fix.  
- access-map doc (.canvas closure) (`47fe91554f`).  
- U‑VIZ‑WIKI‑NARRATIVE (`cd54edb940`): Ollama “what/why” narrative for viz→wiki entries.  
- U‑OLLAMA‑PIPELINE‑INJECTOR‑WIRE (`7ec4a5ea02`): expose local‑model routes `/rgs`, `/scrutinize`.  
- #8 U‑OLLAMA‑PREWARM‑WIRE (`65a29220e5`).  
- U‑TRIBAL‑QA‑LLM‑UNBLOCK (`6fb278a2ee`).  
- Memo‑Throttle & User‑Type Fixes (`f1b69db664`, `1ecf50b6be`): per‑session throttle, correct transcript path, capture user turns, replace execSync curl with `callOllama`.  
- U‑NAV‑ACCEL‑GAP‑A‑FALSIFIED (`2c766b9308`): Gap A already node‑covered; skip generator.  
- U‑WEEKLY‑SYNTH‑RESOLVER (`b5d249f4f5`): host‑aware model selection, env‑pin override, timeout bump, TS error suppression.

**DECISIONS**  
- Verify gaps via recon workflow before building (R8).  
- Commit order: B1→B2→B3→C2→C3.  
- Use lane‑guard bypass (`PRISM_GIT_ADD_LANE_DISABLE=1`) and write‑block kill switch for shared tree edits.  
- Omit `coverageComplete`; document OOM limitation instead.  
- Fix scorer overconfidence: require exact match or Levenshtein ≤ 2; demote prefix/substring to medium.  
- Cap hook heap to 384 MB (from 4 GB) to cut commit reservation.  
- Raise MCP maxConnections 200→512 for future load.  
- Add `TRANSIENT_PRESSURE_CODES` and `isTransientPressureCode`.  
- Widen `OFFLOADABLE_PATTERNS` to include lint, classify, docstring, diff‑summary, triage, etc.  
- Introduce `EXPECTED_UNREGISTERED_TASKS` set.  
- Defer MCP daemon restart until migration freeze lifted; keep current daemon running.  
- Adopt atomic stage+commit with `CLAUDE_SESSION_ID`.  
- Reclaim WSL memory via `wsl --shutdown` and `.wslconfig memory=16GB`.  
- Use Ollama for task efficiency when viable; fully synergize within Prism OS system.  
- Skip rebuilding Gap A after confirming node coverage.

**OPERATOR DIRECTIVES**  
- “fill all gaps” (B1–C3).  
- Ensure vault built and operational; map cheap‑token paths; persist context for sierra domain.  
- Validate 384 MB hook cap is holding (commit charge ~70%).  
- Tidy missing‑task banner noise by classifying vault crons as expected‑unregistered.  
- Write handoff documentation, recommend `/compact`.  
- Arm deferred items: restart MCP daemon after migration freeze; register vault crons after freeze.  
- `/startup-sierra` – force‑take sierra slot and run standard startup pipeline.  
- `/loop [10m] /goal …`: finish all Ollama synergy tasks, use strongest viable models for grunt work (search, read, write, coding), wired/validated across galaxies.  
- Operator ask: utilize Ollama to code direct paths to each file/node; generate skill‑script hook combos for tool calls.

**FINDINGS/BUGS**  
- Unprotected `ensureDir/writeFileSync` caused data loss in `obsidian-memory-sync.mjs`.  
- Stale `index.md` frontmatter (`last_verified: 2026‑05‑08`, 770 entries).  
- Scorer autoApplyEligible overconfident on substring/prefix; long‑slug decay leak.  
- Stale `wiki-link-fix-candidates.json` high:14100; required regeneration.  
- Full‑corpus scan OOM; partial batch (100) disarmed but incomplete.  
- Over‑commit from 4 GB hook heap caused ~210 GB commit reservation; false‑positive MCP alarm HRESULT 0x800710E0.  
- GPU offload coverage only 8%; most tasks on Claude.  
- Missing‑task banner noise from two vault crons (not registered under migration freeze).  
- Drift test failure for PRISM WSL Memory Guard installer not registered.  
- `vmmemWSL` over‑committed 95 GB despite `.wslconfig` cap; autoMemoryReclaim=gradual reclaimed it.  
- Lgit layer regex missed alphabetic suffix, miscounted as “other”.  
- Peer contamination on dispatcher edits; resolved via surgical staging and HEAD reinsert.  
- Amend‑on‑shared‑tree hazard: single doc line folded into peer’s commit (attribution issue).  
- Falsified premise: router widening to hit 30 % would offload judgment work, not beneficial.  
- Three fleet‑wide defects in memo extractor: throttle, transcript path, user‑turn capture; command‑injection via execSync curl fixed.  
- Stale test asserting 7b model updated to 32b.  
- TS7016 errors on `.mjs` imports suppressed with `@ts-expect-error`.

**DOMAIN SPECIFICS**  
Engines/Actions:  
- WikiIndexMaintainerEngine, memory‑rag‑inject hook, node-card-prefetch-inject, search-thoroughness-inject, fleet-task-health-watch.mjs (health classifier), OllamaTaskOffloaderEngine, SemanticSimilarityGuardEngine.cosineCheck(), PRISMSelfAwarenessEngine.searchAIFeatures().  
Actions: obsidian-memory-sync.mjs, promote-memory-to-wiki.mjs, vault-rot-sentinel.mjs, build-wiki-leaf-index.mjs, link-fix-suggester.mjs, regen-viz.mjs (regen‑wire), doc_nodes dispatcher, canvas-read-lib.mjs, memo‑extractor hardening scripts, U‑VIZ‑WIKI‑NARRATIVE via generateBlurb(), U‑OLLAMA‑PIPELINE‑INJECTOR‑WIRE, U‑TRIBAL‑QA‑LLM‑UNBLOCK, U‑WEEKLY‑SYNTH‑RESOLVER.  
Paths: H://.claude/bin/portable-node; mcp-server/src/index.ts; scripts/fleet-task-health-watch.mjs; .claude/hooks/ollama-task-offloader.mjs; regen-viz.mjs; system-viz-query.mjs; sessionDispatcher.ts / sessionActionSchemas.ts; host-aware-synthesis-model.mjs; multi-provider-router.mjs; ask-ollama.mjs; resolveSynthesisModel(); generateBlurb(); slot-bind-enforce.mjs; chat-slots.mjs; settings.json wiring; stop‑obsidian-memory-extract.mjs.

**TOOLS USED**  
PRISM commands: `/checkin-sierra`, slot‑bind‑enforce, chat-slots.mjs, update-config.  
Testing: node:test (assert/strict), vitest (scorer tests), esbuild, tsc --noEmit, npm run build.  
Git utilities: git add/reset/commit, lane‑guard env vars (`PRISM_GIT_ADD_LANE_DISABLE`).  
Scripting: bash for slot claim, node scripts for resilience test and scorer tests.  
Portable-node wrapper (bash & .cmd) sets `NODE_OPTIONS`; MCP server dispatcher; fleet-task-health watcher; Ollama offload engine.  
ask-ollama.mjs / callOllama(); scrutiny‑3way.mjs; scrutiny-before-stop.mjs.

**OPEN THREADS**  
- Full‑corpus wiki‑link‑fix‑suggester regeneration (15.8 K links) – OOM; need streaming or chunked approach.  
- Refine scorer short‑token Levenshtein autoApply gating.  
- Restart MCP daemon after migration freeze to apply `maxConnections=512`.  
- Register vault crons after migration freeze.  
- Resolve drift test for PRISM WSL Memory Guard (register task or confirm intentional defer).  
- Monitor commit charge; verify GPU offload coverage reaches ~30 %.  
- Further optimization of Ollama usage across system (tool calls, memory distillation, tribal knowledge injection).  
- Refine staleness detection for canvas reader.  
- Backlog: #4 wiki‑NLI‑lint, #6 memo‑cache‑consolidate.  
- Follow‑on unit for handling unset `num_predict` on 120b model.  
- U‑OLLAMA‑FORGE‑ASSIST (Gap C) pending.  
- Gap B semantic reranker build pending.
