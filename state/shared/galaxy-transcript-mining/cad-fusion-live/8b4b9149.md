# cad-fusion-live session 8b4b9149 (2026-06-10, 47.1MB, spine 511KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5ae481f748` – EPERM rename bug fix (atomic‑append + retry, orphan `.tmp` cleanup)  
- `86f0e3fe0c` – Dispatcher wiring for tri‑vendor comparator & 16 round‑trip tests (`speed_feed_tri_compare`, `speed_feed_exhaustive_combination`, `speed_feed_downstream_packs`)  
- `16d6eecef4` – SpeedFeedCalibrationPersistEngine + unit/dispatcher tests  
- `890b2c39e7` – VC‑assessment spec addendum (baseline comparison)  
- `3438987f0e` – Hardening: clamped flag fix, persist tmp cleanup, `ledger_rows_skipped` field  
- `f31398a1a5` – GPU‑judge engine + test + dispatcher wiring  
- `f5d14ddb29` – GPU‑judge hardening (probeGpuResidency bug, persist‑skip on limit, silent‑exclusion note, fetch timeout)  
- `951f5ac335` – Polishing: `isProbe` constant & suppression‑on‑probe test  
- `3a1c20fca2` – Full sweep mode + `runStreaming` generator + driver update (≈69 k cells)  
- `af0ac16c5a` – Scrutiny fixes (`cells.length` ReferenceError, ISO‑group collapse lock)  
- `b847c0f1ff` – Scoping spec for axis‑awareness enhancement  
- `28eca0ed84` – Doc reflection of GPU‑judge findings  
- `54c0ae06c8` – Spec addendum memory commit  
- `658c8280fe` – Canonical tool‑material speed‑factor table added to `constants.ts`; safety guard; tests  
- `585584e3ae` – Wired existing `CoolantVcModifier` into engine; removed duplicate factor table  
- `7d0affcae6` – De‑inlined rigidity constants (0.7/1.0/1.1) to `constants.ts`; updated engine/tests  
- `f998f8af71` – Hoisted tool‑material, coolant, rigidity multipliers into `alts.balanced.vc` path  
- `da41a58fd1` – Full 102 k‑combination sweep (Vc, RPM, feed, MRR)  
- `c48829e331` – Axis‑liveness probe (`sfc-orchestrator-axis-liveness.ts`)  
- `d03458fff1` – Extended probe to include tool life  
- `2070c472a4` – Workholding.type → live cutting‑force cap  
- `7dc7798033` – Spindle.hp → power clamp  
- `8b191d5427` – Tool_holder.type runout → life reduction  
- `a257f872bb` – Updated probe: added missing axes, fixed phantom field bug  
- `81e37ece16` – Numeric finish‑Ra axis (`target_ra_um`) added; physics‑reviewed  
- `6a3ad56545` – Compare tool_material variability (tri‑vendor sweep now varies HSS/ceramic/cbn)  
- `c78faa5a73` – HSS baseline entries added (+107.8 % delta surfaced)  
- `835df42c74` – Ceramic & CBN baseline entries added (12 points)  
- `4c544db4ae` – Baseline_detail per‑vendor surfaced; explicit G‑Wizard/CNCCookbook and HSMAdvisor deltas  
- `08d7fc6d37` – All‑axis sweep: OAT + factorial sweep over every named axis (16 live / 9 honest triaged, two‑regime to expose conditional caps)  
- `907e74acab` – Fixed tool_material speed‑factor table & widened clamp (HSS cast‑iron +108 %, CBN +49 %)  

**DECISIONS**  
- Adopt *backend‑first* workflow: fix data spine → archive stray files → wire engines before closed‑loop comparison.  
- Use bootstrap slot‑enforce pattern (`[BOOTSTRAP‑SLOT‑ENFORCE]`) for shared‑tree commits.  
- Run closed‑loop test only after EPERM bug fixed and tri‑vendor comparator wired.  
- CPU for calibration derive; GPU‑resident LLM judge added to close loop.  
- Full sweep mode (≈69 k cells) + `runStreaming` generator to remove 50‑cell cap.  
- Axis‑awareness build order: tool material → coolant → rigidity; each change additive, passes 2‑reviewer gate.  
- De‑inline physics constants into `constants.ts`; apply multipliers via `alts.balanced.vc`.  
- Safety guard for inferred tool material (apply only when explicitly set).  
- Controller default mode left inert per product philosophy; decide later whether to add realizability factor.  
- Live G‑Wizard/HSMAdvisor numbers operator‑gated; require export from closed apps.  
- Richer carbide baseline and exotic combos pending OCR catalog pass (multi‑hour GPU job).  

**OPERATOR DIRECTIVES**  
- Run calculations for every possible combination of machines, spindles, controller, materials, workholding/fixture, tool holder connection type & holding mechanism (balance, max speeds, rigidity, dampening capability, accuracy), tooling + insert if viable, coolant type, tool path type, cutting parameters, desired finish quality (roughing, semifinish, finish). Compare ALL possible calculations and parameters with maximum variability to G‑Wizard and HSMAdvisor. No pauses for user input; continue until condition met.

**FINDINGS/BUGS**  
- EPERM rename caused orphan `.tmp` files – fixed.  
- G‑Wizard crib: 41 210 tools, 0 non‑NaN sfm → comparator abstains from all cells.  
- HSMAdvisor constant 634.6 m/min for every material; flagged `aligned:false`.  
- Consensus computation incorrectly includes not‑aligned externals – defect.  
- Clamped flag false‑positive (P2).  
- Persist tmp not cleaned on rename failure (P2).  
- Skipped‑lines observability missing (P2).  
- `probeGpuResidency` prefix‑match bug; `cells.length` ReferenceError after runStreaming refactor.  
- Tool material inertness: engine ignores `tool_material`; coolant type inertness: flood/mist/dry produce identical Vc.  
- Many axes not modeled (machine, spindle, controller, workholding, insert).  
- ISO‑group collapse: 15 names → 6 physics profiles.  
- PRISM under‑speeds published references by ~25 % (worst –53 % on aluminum).  
- Orchestrator default mode ignored tool material, coolant, rigidity multipliers.  
- G‑Wizard `toolcrib.csv` geometry‑only speeds 0 → zero matches.  
- Kennametal Master catalog PDFs require vision‑OCR; text extraction fails.  
- PRISM over‑speeds HSS cast iron +108 % and CBN hardened steel +49 %.  
- fz uniformly +67–91 % higher than published sources (not a defect, surfaced).  
- Controller_features wiring gap identified (applies only in aggressive_rush mode).  

**DOMAIN SPECIFICS**  
- **Engines / Actions**: `OutcomeCaptureBusEngine`, `SpeedFeedTriComparatorEngine`, `SpeedFeedExhaustiveCombinationEngine`, `SpeedFeedDownstreamSubscriberEngine`, `SpeedFeedCalibrationPersistEngine`, `SpeedFeedGpuJudgeEngine`, `UltimateSpeedFeedEngine`, `ClampingForceEngine`, `SpeedFeedNineAxisOrchestratorEngine`, `SpeedFeedBaselineComparatorEngine`.  
- **Dispatcher**: `calcDispatcher.ts` – actions added (`speed_feed_calibration_persist`, `speed_feed_gpu_judge`, etc.).  
- **Metrics / Fields**: Vc, RPM, feed_rate_mmmin, MRR, tool_life_min, cost_per_part_usd, cycle_time_min, `ledger_rows_skipped`, `matched_model`, `gpu_resident`, `vram_used_mib`, `target_ra_um`.  
- **Paths / Artifacts**:  
  - Assessment report: `H:/prism/state/shared/specs/SFC-VC-ASSESSMENT-2026-06-08.md` (commit 86f0e3fe0c).  
  - Full sweep ledger: `sfc-full-sweep-ledger.jsonl`.  
  - Axis‑liveness probe script: `sfc-orchestrator-axis-liveness.ts`.  
  - All‑axis sweep driver: `sfc-all-axis-sweep.mjs`.  
- **Vendor Data**: `src/data/<vendor>-speed-feed-data.ts` (G‑Wizard, HSMAdvisor, Sandvik, Kennametal, CNCCookbook, Titans_of_CNC).  

**TOOLS USED**  
- PRISM helpers (`chat-slots.mjs`, `slot-bind-enforce.mjs`).  
- Hermes‑workflow planner, Ultracode workflow engine.  
- `/forge-audit-v2`.  
- Node.js scripts (`spawnSync`, `npx tsx`), Vitest test framework.  
- Ollama inference server on RTX PRO 6000 Blackwell (GPU‑judge).  
- CronCreate for recurring `/goal`.  
- Scrutiny script, Git hooks (`git commit --only`).  
- TSC compile, esbuild.  

**OPEN THREADS**  
1. Closed‑loop test after EPERM bug fix & engines wired.  
2. Auth provider selection (Clerk / Supabase / Auth0).  
3. Webhook‑signature fix deployment (provider‑independent).  
4. Consensus computation bug fix – exclude not‑aligned externals.  
5. Implement machine rigidity, spindle, controller physics.  
6. Add coolant effect to Vc calculation.  
7. Model workholding/fixture influence on speed/feed.  
8. Insert geometry impact on feed.  
9. Finalize tool‑material speed‑factor table & tests.  
10. Validate full sweep against G‑Wizard/HSMAdvisor with updated axes.  
11. U‑OSC‑MRR‑RECONCILE – reconcile new factor propagation with MRR multiplier in orchestrator.  
12. U‑OSC‑RIGIDITY‑DOC – integrate rigidity into DOC cap analysis.  
13. Remaining inert axes: holder/spindle/controller/workholding/insert (wire to orchestrator).  
14. Full sweep vs G‑Wizard/HSMAdvisor with vendor calibration (vision‑OCR extraction of catalog PDFs, especially Kennametal Master).  
15. Vision‑OCR batch on Blackwell GPU for all high‑priority vendor catalogs (~80 catalogs, multi‑hour job).  
16. Live G‑Wizard/HSMAdvisor numbers remain operator‑gated; need export from closed apps.  
17. Richer carbide baseline / exotic combos pending OCR catalog pass (multi‑hour GPU task).  
18. `U‑OSC‑FZ‑FORCE‑VALIDATE` – physics‑reviewer audit of +67–91 % fz finding.  
19. `U‑OSC‑DEAD‑AXIS‑TRIAGE` – fix controller_features wiring gap, add tool_life tracking for holder_runout/tool_holder_type.  
20. Further calibration of ceramic/CBN speeds with new vendor sources (Tungaloy, Iscar).
