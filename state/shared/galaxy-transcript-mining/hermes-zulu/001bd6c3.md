# hermes-zulu session 001bd6c3 (2026-06-10, 59.2MB, spine 531KB, 7 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commits: `ab2ccf42a4`, `e6713584e2`, `c988a21ec4`, `56aa423427`; `822c48c55`, `3499f5f20f`, `d807d46f44`, `a59f35221e`, `cfda0e391a`, `e17be56e82`, `7771ca7f86`; `2340a2e699`, `a2756779c2`, `a60d7ba0bf`, `612418fde7`, `3904b5e7b0`, `5a91ba1862`; `28b72e4dee`.  
- Hermes config migrated to schema v28; provider strings corrected (Claude Code primary, Ollama local).  
- Qdrant container (`prism-qdrant` v1.17.0) up with 3 collections; semantic_search now functional.  
- Hermes headless CLI (`hermes -z`) operational; GUI launcher removed.  
- `scripts/fill-galaxy-memory-sections.mjs` + 13‑test suite injected MASTER‑BRAIN‑TEMPLATE sections into all 34 galaxies.  
- `config/ollama-routing.json`, `config/ollama-config.yaml` created; routing engine (`OllamaTaskOffloaderEngine`) refreshed with tiered selection.  
- VRAM co‑residency controller (`ollama-coresidency.mjs`) deployed for RTX PRO 6000 (96 GB).  
- RTK compressor (`ollama-codegen.mjs`, `ollama-capability-probe.mjs`) built; RTK config (`config.toml`) tightened `[limits]`.  

**DECISIONS**  
- Adopt Path D: route vault reads through :3100 PRISM MCP, drop second filesystem server.  
- Hermes → Claude Code primary; local Ollama (`gpt‑oss:20b`, `qwen2.5-coder:32b`) fallback; avoid Qdrant for primary path.  
- Revert supervisor cwd‑fix; document true root‑fix in memory.  
- 6‑agent deep verification workflow for critical paths.  
- gpt‑oss 20b as local Zulu primary (64 K context); keep Claude Code fallback until billing enabled.  
- Tiered Ollama model selection: prefer `gpt‑oss:120b` fleet‑wide coding default, fall back to `gpt‑oss:20b`, then `qwen2.5-coder`.  
- Route‑hook default changed from retired `qwen2.5-coder:7b` to resident `gpt‑oss:20b`; keep route‑hook resident to avoid 0 % offload.  
- Use ultracode workflows for brainstorming, plan synthesis, and verification.  
- Change `OllamaClientEngine` host from hardcoded `localhost` to `127.0.0.1`, overridable via `OLLAMA_HOST`.  
- Build LLM compressor as sole RTK “upgrade”; RTK binary cannot be upgraded internally.  

**OPERATOR DIRECTIVES**  
- Kill & relaunch Hermes daemon; verify :3100 MCP reachable post‑restart.  
- Update `config.yaml` with provider strings (custom gpt‑oss:20b, fallback anthropic/claude-opus-4.8).  
- Bring Qdrant up and connect `semantic_search`.  
- Audit each galaxy sequentially; use Obsidian + Ollama to fill gaps, no external API calls.  
- Commit exclusively to slot/bravo worktree (`H:/prism-slot-bravo`).  
- Green‑light U5 RTK config and #14 go‑live merge; set `PRISM_CODEGEN_MODEL=gpt‑oss:120b`.  
- Re‑enable Hermes‑Obsidian Bridge scheduled task (currently disabled).  
- Regenerate `ai-training_synthesis.md` via India’s workflow.  

**FINDINGS/BUGS**  
- Qdrant not connected → `semantic_search` empty; stale connection state due to early start.  
- `self_awareness_search` ENOENT on stale drive letter (`N:\PRISM\src\tools\dispatchers`).  
- Hermes memory bridge dormant: no memories in `hermes/memories/`.  
- Dream‑cycle writes to `knowledge/memories/dreams/` inside sync lock zone.  
- 400 error from Anthropic (extra‑usage balance exhausted); resolved by switching to local model.  
- 429 rate limit indicates shared 5 h Claude Max pool exhausted.  
- Provider string `openai` invalid in Hermes 0.16.0 → agent init failures.  
- Stale roster & absent models caused ~0 % Ollama offload; fixed by U1.  
- Route‑hook default pointed to retired `qwen2.5-coder:7b`; fixed by U2.  
- CRLF line‑ending flip introduced in U1; corrected via amend.  
- Safety denylist in U4 allowed lowercase/no‑space G‑code to reach LLM; fixed with case‑insensitive regexes.  
- Tag‑match bug caused `nomic‑embed‑text` reported absent.  
- Stale re‑claim test tautological; fixed with fresh‑lock discriminator.  
- Kimi 2.6 free tier violates data‑sovereignty; not wired.  
- Windows resolves `localhost` to `::1`; Ollama binds only to `127.0.0.1`, causing connection refusals.  

**DOMAIN SPECIFICS**  
- Engines/Dispatchers: `CapabilityIndexEngine`, `OutcomeCaptureBusEngine`, `DocuStrataMaterialPriorEngine`, `JMCustomerVendorDatabaseEngine`, `PrismMemory`, `PrismSession`, `PrismKnowledge`, `PrismGsd`, `PrismContext`, `OllamaTaskOffloaderEngine`, `OllamaClientEngine`, `MultiModelConsensusEngine`, `OllamaCapabilityProbeEngine`.  
- Actions: `slot_brief_write`, `slot_brief_inject`, `chat_post`, `slot_brief_list`, `ask-ollama.mjs`, `octopus-with-hermes-rag.mjs`, `octopus-dispatch.mjs`.  
- Paths: `/checkin-bravo` wrapper, `hermes‑obsidian‑memory‑bridge.mjs`, `generate-hermes-features.mjs`, `octopus-rag`, `dream-cycle`, `scripts/fill-galaxy-memory-sections.mjs`, `galaxy-synthesis-refresh.mjs`, `config/ollama-routing.json`, `config/ollama-config.yaml`, `build.sh`, `scripts/check_ollama_health.mjs`, `scripts/claude_opus_review.mjs`.  
- Metrics: `master_index_query` hits, `semantic_search` hits, offload‑rate banner, take‑rate calculation, `OLLAMA_MODELS` roster, `PRISM_CODEGEN_MODEL` env.  

**TOOLS USED**  
- Docker Desktop (Qdrant container).  
- Hermes CLI (`hermes`, `hermes config migrate`, `hermes -z`).  
- Node.js scripts (`node scripts/hermes-obsidian-app-map.mjs`, `node scripts/ollama-tool-agent.mjs`, etc.).  
- Git for commits, cherry‑pick, merge.  
- PRISM MCP server at :3100.  
- Ultracode workflow engine (brainstorming, plan synthesis).  
- Vitest (`node --test`) for unit tests.  
- esbuild (`build:fast`), TypeScript compiler (`tsc --emit`).  

**OPEN THREADS**  
- Restart Hermes daemon after config changes; confirm :3100 MCP connectivity.  
- Bring Qdrant up, connect `semantic_search`.  
- Resolve 400/429 errors from Anthropic/Claude (billing or local fallback).  
- Finalize Hermes coordination loop; ensure dispatch/brief consumption reliable.  
- Safeguard dream‑cycle output location (avoid sync lock zone).  
- Reconcile CapabilityIndexEngine vs repo‑root cwd conflict.  
- Ensure Hermes memory bridge produces memories for Obsidian vault.  
- Monitor memory pressure; eliminate zombie processes.  
- Decide on extra‑usage billing vs local‑only operation.  
- Upgrade to `gpt‑oss:120b` once model download completes.  
- Re‑enable Hermes‑Obsidian Bridge scheduled task.  
- Regenerate `ai-training_synthesis.md`.  
- Schedule periodic `syncGalaxyMemories()`.  
- RTK upgrade sweep (#11): integrate LLM compressor with external RTK binary (await ultracode output).  
- Go‑live merge & wiring: finish golf’s git‑tree repair, merge slot/bravo into main, update `settings.json`.  
- Routing tier adjustments: un‑strand `gpt‑oss:120b`, adjust `keep_alive`/`NUM_PARALLEL` for new hardware.  
- U11 cron installer for probe gating (post‑U10).  
- Execute octopus live producer (`octopus-with-hermes-rag.mjs` with `LIVE_DISPATCH=1`).  
- Coordinate Ollama‑server restart (#1) to apply `RECOMMENDED_ENV` for 17 peers.  
- Finalize synergy plan across slots (keystone #3 pending cross‑lane coordination).  
- Run `tsc --emit` to regenerate `dist/engines/*.js`; add regression test for `OllamaClientEngine` host default (`127.0.0.1`).
