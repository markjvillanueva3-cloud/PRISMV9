# hermes-zulu session 99297b90 (2026-06-11, 8.8MB, spine 79KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `0a966b5696` – Compaction‑boundary fix (5 byte‑estimators + drift guard).  
- `3f0a9aef11` – Fleet‑wide dense‑RAG improvement (lexical‑miss chunk rescue).  
- `d12424096c` – Alpha galaxy‑brain refresh (MEMORY, CLAUDE.md, task queue).  
- `0dda52f7de` – Drift guard test for future compact‑marker changes.  

**DECISIONS**  
- Centralize compact‑marker detection in `transcript-token-counter.mjs`; add legacy and current markers.  
- Fix `precompact-auto-trigger.mjs` to use the new marker, remove double‑wired PostToolUse path.  
- De‑duplicate redundant `--post` hook wiring; keep only sidecar refresh.  
- Add a drift guard test so future transcript‑format changes cannot silently re‑introduce loops.  
- Use shared lib for all byte‑estimator logic to avoid scattered inline copies.  

**OPERATOR DIRECTIVES**  
- Resolve alpha’s constant compaction and take control of the alpha slot.  
- Read & categorize all prior alpha conversations (tasks left, started‑but‑unfinished, dormant, articles fed).  
- Update alpha galaxy CLAUDE.md, memories, wikis, souls, awareness; wire to Obsidian vault and Hermes/Zulu.  
- Optimize context retention, persistent memory, and ROI‑ordered task queue.  
- Leverage India & Zulu domains for AI‑system improvements (NN/GNN/LoRA/CAG/RAG hybrids).  

**FINDINGS / BUGS**  
- Transcript format change removed legacy `"isCompactSummary":true`; new marker is `compact_boundary`.  
- Byte‑based estimator counted entire transcript because no boundary was found → over‑estimation in 940 K–1.1 M band, triggering hard block each tool call.  
- Sidecar reads authoritative usage blocks; unaffected by byte loop.  
- `precompact-auto-trigger` double‑wired (PreToolUse + PostToolUse) caused redundant checks.  
- No suppression for the unguarded band → infinite compaction loop.  

**DOMAIN SPECIFICS**  
- PRISM Hermes/Zulu orchestration: slot‑alpha, master orchestrator, fleet coordination, consensus routing.  
- Token‑budget sidecar (`token-budget-alpha.json`) and `statusline.mjs`, `chat-token-watch.mjs`.  
- Transcript‑token‑counter lib (shared substrate).  
- Precompact‑auto‑trigger hook (tier‑T0, hard/soft thresholds).  
- Dense‑RAG arm (`galaxy-dense-rerank.mjs`), CAG cache, LoRA emit.  
- Compact‑boundary marker format: `{"type":"system","subtype":"compact_boundary", ...}`.  

**TOOLS USED**  
- Systematic Debugging skill (root‑cause investigation).  
- `transcript-token-counter.mjs`, `precompact-auto-trigger.mjs`.  
- Sidecar, statusline, chat‑token‑watch modules.  
- `mine-galaxy-transcripts.mjs` (Ollama miner) and Sonnet‑agent workflow for transcript synthesis.  
- Reaper‑protected installer script (`install-india-mine-task.ps1` clone).  

**OPEN THREADS**  
- Run a reaper‑proof, scheduled mining job to process all remaining alpha transcripts.  
- Finalize the Sonnet‑agent workflow (reduce prompt size, ensure subagents stay under token limits).  
- Commit and run the drift guard test (`0dda52f7de`) in CI; resolve pending 3‑of‑3 scrutiny due to rate limiting.  
- Update souls.md with new task queue and ROI ordering.  
- Verify that future compact‑marker changes are caught by the drift guard before deployment.
