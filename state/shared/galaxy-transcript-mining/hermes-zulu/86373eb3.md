# hermes-zulu session 86373eb3 (2026-06-10, 48.8MB, spine 403KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Updated Launch‑PRISM‑Fleet.ps1 (4‑window layout) and created Launch‑PRISM‑Fleet‑3win.ps1 (19 slots).  
- Set PRISM_RESUME_MAX_MB=200; guard in nim-autostart.mjs to skip when LOCAL_LLM_BACKEND=ollama.  
- docker-compose.yml: removed ollama service, fixed KEEP_ALIVE=-1→30m, MAX_LOADED_MODELS=6→4.  
- NIM disabled, Ollama routing enabled; native Ollama starts at logon via scheduled task “PRISM Ollama Serve”.  
- Master‑index hook OOM fixed by heap guard + sidecar fallback; fire counter added for memory‑index.  
- Tribal cron installers rewritten durable runner scripts; install‑synthesis‑crons.ps1 created for 3 synthesis runners.  
- Wired h-to-c-obsidian-mirror PostToolUse hook.  
- Commits: 679565fcb5 [BOOTSTRAP‑SLOT‑ENFORCE] (cache & slice cap logic), cf74c95edb (restore NUM_CTX, null‑dereference guard), 5203edf318 (--next-unpopulated --backend-only mode). All 22/22 unit tests pass.  
- vault-to-gnn-refpool.mjs built; 9/9 tests passed; added 4 high‑confidence reference ghosts.  
- seed‑ghost-from-unwired.mjs fixed streaming I/O; atomic writes; resolved V8 string‑cap crash on 642 MB graph.  
- vault-to-lora-dataset.mjs built; 15/15 tests passed; live‑validated 245 Alpaca triples; CRLF, .md extension, frontmatter regex bugs fixed.  
- 34 galaxy memo vaults populated (knowledge/memories/reference_*.md).  
- Hermes/Zulu master orchestrator wired; gateway daemon PID 57264 running.  
- Obsidian REST bridge authenticated and live on :27123; self‑learning loop operational.  
- Overnight vault pipeline at 29/34 memos, compounding queued automatically.  
- Cron jobs scheduled: inbox sweep, morning brief, weekly review.  
- GEPA‑lite skill candidate staged for review.  
- MCP server single daemon (:3100) healthy; Obsidian API bound.

**DECISIONS**  
- Keep safe 4-window fallback launcher; deploy separate 3-window per-account launcher.  
- Raise resume size cap to 200 MB so heavy slots resume instead of fresh checkin.  
- Switch LOCAL_LLM_BACKEND from auto to ollama, dropping NIM containers and autostart hook.  
- Remove compose‑ollama service to avoid port collision with native Ollama; rely on scheduled task “PRISM Ollama Serve”.  
- Add fail-fast guard for misuse of --services=mcp in launcher.  
- Recall hooks remain disabled by default; enable via settings to avoid token-cost regressions.  
- Do not add --with-viz to brain‑refresh; leave redundant RAG hooks dead (obsidian-precheck-inject, ollama-obsidian-rag).  
- Use CAG router for cache‑augmented context; vault serves as corpus and cache.  
- Build GNN feeder only for non-overlapping gap to avoid clobbering India’s in-flight transcript miner.  
- Extend LoRA feeder to extract feedback_* memories into Alpaca instruction‑tuning format; separate from DB-driven LoRA builders.  
- Replace JSON.parse/stringify on 642 MB graph with streaming I/O to avoid V8 string-cap crashes.  
- Keep isValidDispatcher shape‑only validation; adjust tests accordingly.  
- Switch discovery from handoff‑based to transcript‑content‑based for full coverage.  
- Persist per‑transcript classification in cache file to avoid repeated scans.  
- Add MAX_SLICES_PER_TX (default 14) to limit MAP calls on large transcripts.  
- Raise synthesis timeout to 900 s for gpt‑oss:120b.  
- Use vault memo presence as sole success signal; remove grep wrapper that mangled exit codes.  
- Run mining one galaxy at a time in foreground with --limit 6, reaper‑protected.  
- Set OLLAMA_CONTEXT_LENGTH=65536 and Hermes model.context_length: 65536 to avoid prompt truncation.  
- Use only local Ollama models; no external Anthropic usage due to billing constraints.  
- Do not re-add prism-vault-fs MCP to Hermes config to keep MCP layer stable.  
- Patch mcp-server-supervisor.mjs to run with correct cwd so .env loads.  
- Extend fleet‑reaper protect regex (PRISM_REAPER_PROTECT_EXTRA) to shield long‑running pipelines.

**OPERATOR DIRECTIVES**  
- Capture accounts 2–6 via claude login + node scripts/capture-claude-credentials.mjs account‑N …; create ROTATION_ORDER.json.  
- Verify scheduled task “PRISM Ollama Serve” is enabled and runs at logon.  
- After capturing, run Launch‑PRISM‑Fleet‑3win.ps1 to start the 3-window fleet.  
- Monitor API‑limit errors; adjust Docker memory cap if needed.  
- Decide: commit the 9 edits (7 H: files + 2 C: settings) or keep them live.  
- Run elevated PowerShell block to register missing scheduled tasks (3 synthesis, 3 tribal).  
- Verify take‑rate telemetry after enabling recall hooks.  
- Build only the NON‑overlapping gap (GNN feeder); build LoRA feeder too, coordinated.  
- Run hermes gateway install (elevated) to persist gateway after logoff.  
- Re-register missing scheduled tasks: OCR Training Loop, Slot Worktree Migration Status, System Awareness Freshness (requires elevated PowerShell).  
- Add extra usage credits on claude.ai if opus runs are needed.

**FINDINGS/BUGS**  
- NIM containers OOM due to WSL2 memory cap (~16 GB vs 20–40 GB); Docker Desktop reports only 15.6 GB RAM.  
- Backend auto‑probing dead NIM caused latency and 429 errors; resolved by switching to Ollama.  
- Misnamed service mcp in autostart launcher produced “no such service: mcp” warnings.  
- Compose ollama conflicted on port 11434 with native Ollama.  
- Duplicate MCP daemons reported; cleaned up.  
- Qdrant collections use 768‑dim vectors, matching Ollama’s nomic‑embed; no re‑index needed.  
- Master-index hook OOM on sidecar load; resolved with heap guard.  
- Temporary cron runners broke tribal tasks; fixed by durable path.  
- 4 dated‑synthesis crons missing; installer created for 3 runnable ones.  
- Brain‑refresh task correctly light (no --with-viz); no change needed.  
- find-connections script is per-target only, not schedulable.  
- Scheduled-task registration requires elevated shell.  
- seed‑ghost-from-unwired.mjs crashed on 642 MB graph due to V8 string cap; fixed via streaming I/O.  
- isValidDispatcher only checks regex shape, not existence; test updated.  
- CRLF leaked into 53/245 output fields; normalized.  
- .md extension could leak into instruction field; stripped.  
- frontmatterField regex matched newlines in comments; changed to non‑newline class.  
- Fleet‑reaper kills long detached background jobs; mining must be foreground or reaper‑protected.  
- Large live‑session transcripts (~48 MB) cause >2 min spine extraction; slice cap mitigates.  
- Missing NUM_CTX caused runtime error after cache edit.  
- Grep wrapper produced misleading exit 255; vault memo is ground truth.  
- Synthesis timed out on dense token‑optimization digests until timeout increased.  
- Ollama ignored options.num_ctx causing prompt truncation; fixed via env override.  
- MCP server failed to load .env due to wrong cwd; resolved by supervisor patch.  
- Fleet‑reaper killed overnight vault pipeline twice; regex protection added.  
- Obsidian REST bridge initially bound only after vault opened; solved with URI launch.  
- Hermes local models had tool execution issues tied to context truncation.

**DOMAIN SPECIFICS**  
- Engines: Ollama gpt‑oss:20b (MAP), gpt‑oss:120b (SYNTHESIS), nomic‑embed‑text, ObsidianRestBridgeEngine, MCPServerEngine.  
- Actions: discoverByContent, mineGalaxyTranscripts, build-memory-index-sidecar.mjs, build-memory-embeddings-sidecar.mjs --resume, galaxy-synthesis-refresh.mjs, vault-to-gnn-refpool.mjs, seed‑ghost-from-unwired.mjs, vault-to-lora-dataset.mjs.  
- Metrics: 2,097 handoffs; 33/34 galaxies mineable; 474 transcripts genuinely gone; 34 galaxy memo vaults populated; 29/34 memos processed overnight; 22/22 unit tests pass; 9/9 gnn refpool tests passed; 15/15 lora dataset tests passed.  
- Paths: H:/prism/knowledge (vault), scripts/mcp-server-supervisor.mjs, scripts/fleet-reaper-sweep.mjs, scripts/overnight-vault-compound.mjs, system‑graph.json, reference_<galaxy>_transcript_synthesis.md in each galaxy dir, vault memos stored as reference_<galaxy>_transcript_synthesis.md.  
- Slot lookup: slot-tab-boot.ps1 tiered tiers 1–3; PRISM_RESUME_MAX_MB guard; CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=99; launcher uses Resolve‑SlotCwd, Build‑WtArgString, SetWindowPos.  
- Account switching via claude-account-lib.mjs activateAccount and fleet-wake-sequencer.mjs.  
- nim-autostart triggers H:/Tools/nim/start.ps1 on SessionStart.  
- local-llm-bridge routes to Ollama or NIM based on LOCAL_LLM_BACKEND.  
- QdrantMemoryEngine.ts enforces 768‑dim vectors; cache & slice cap logic added.

**TOOLS USED**  
- Scripts: Launch‑PRISM‑Fleet.ps1, Launch‑PRISM‑Fleet‑3win.ps1, slot-tab-boot.ps1, switch-claude-account.mjs, account-switch-restart-coordinator.mjs, nim-autostart.mjs, local-llm-bridge.mjs, vault-to-gnn-refpool.mjs, seed‑ghost-from-unwired.mjs, vault-to-lora-dataset.mjs, mine-galaxy-transcripts.mjs, build-memory-index-sidecar.mjs, build-memory-embeddings-sidecar.mjs, galaxy-synthesis-refresh.mjs, overnight-vault-compound.mjs, fleet-reaper-sweep.mjs, mcp-server-supervisor.mjs, install‑tribal-consolidate-cron.ps1, install‑tribal-promotion-cron.ps1, install-wiki-tribal-audit-task.ps1, install‑synthesis-crons.ps1.  
- Configs: docker-compose.yml, .wslconfig, scheduled task “PRISM Ollama Serve”, OLLAMA_CONTEXT_LENGTH env, Hermes config.  
- Libraries: QdrantMemoryEngine.ts, graph‑io.mjs (readGraphStreaming, writeGraphStreamingAtomic), dispatcher utilities (isValidDispatcher).  
- External: Ollama serve task, Obsidian local REST API plugin, Playwright via fxtwitter.

**OPEN THREADS**  
- Implement automatic 90% rate‑limit rotation (missing rate_limits.five_hour).  
- Verify native Ollama auto‑start persists across reboots; test scheduled task.  
- Optimize GPU usage: tune OLLAMA_GPU_OVERHEAD, MAX_LOADED_MODELS for Blackwell.  
- Consider raising Docker memory cap if fleet needs exceed 16 GB.  
- Operator must run elevated PowerShell block to register missing scheduled tasks (3 synthesis, 3 tribal).  
- Confirm take‑rate telemetry after enabling recall hooks.  
- Decide on committing edits to slot worktree.  
- Potential scope creep: scheduling find-connections.  
- Remaining backend galaxies mining: agent-orchestration, discovery, dormant-data, knowledge-conversion, corpus-aggregation, database-expansion.  
- Rebuild sidecar and embeddings after each mining run before synthesis‑refresh.  
- Ensure reaper‑protected foreground runs for each galaxy; schedule operator‑run of full population.  
- Verify vault memos are correctly picked up by synthesis‑refresh.  
- Validate weekly self-review job on gpt‑oss:120b.  
- Mine GEPA round 2 success/failure traces.  
- Ensure gateway persists after logoff (hermes gateway install).  
- Re-register missing scheduled tasks (elevated).  
- Monitor Obsidian bridge stability across reboots.
