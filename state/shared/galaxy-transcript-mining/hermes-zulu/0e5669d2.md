# hermes-zulu session 0e5669d2 (2026-06-10, 96MB, spine 827KB, 10 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `168c202646` – B1 sync‑resilient (`obsidian-memory-sync.mjs`)  
- `ea21008f25` – B2 index‑meta (frontmatter stamp)  
- `8c4dff660a` – B3 maint‑cron installers (`install-promote.ps1`, `install-rot.ps1`)  
- `984313825e` – C2 link‑heal scorer (recalibrated, decay‑leak fix)  
- `838123429b` – C3 tribal‑coverage refresh (audit JSONs regenerated)  
- `45225e0cc3` – Fix‑memory record (persistent domain artifact)  
- `da013c4406` – Audit map & node‑access wiki entry (pre‑commit)  
- `9e4376b3b2` – U‑VAULT‑RAG‑WIRE (keyword recall wiring)  
- `cd54edb940` – T1 U‑VIZ‑WIKI‑NARRATIVE (`generateBlurb`, `qwen2.5-coder:32b`)  
- `7ec4a5ea02` – T2 U‑OLLAMA‑PIPELINE‑INJECTOR‑WIRE (settings.json hook)  
- `65a29220e5` – #8 U‑OLLAMA‑PREWARM‑WIRE (`qwen2.5-coder:32b`)  
- `6fb278a2ee` – #2 U‑TRIBAL‑QA‑LLM‑UNBLOCK (gpt‑oss:120b)  
- `f1b69db664`, `1ecf50b6be` – U‑MEMO‑EXTRACT‑THROTTLE & user‑type fix (per‑session throttle, transcript path)  
- `b5d249f4f5`, `9697a9135a`, `71a818b49c` – #3 U‑WEEKLY‑SYNTH‑RESOLVER (host‑aware model selection, `num_predict=-1`)  
- `f8c183f7a5`, `c55e05cf03` – #4 U‑WIKI‑NLI‑LINT (contradiction linter)  
- `153887a519` – U‑VIZ‑GRAPH‑ATOMIC‑WRITE (`writeGraphStreamingAtomic`)  
- `6884155fb6`, `757456eaae` – U‑VIZ‑MERGE‑GUARD‑OOM (off‑heap node count guard)  
- `ae55cea3f7` – U‑VIZ‑AUGMENT‑MOLECULES‑STREAM (`streamGraphArray`)  
- `0f14952601` – #6 RESCOPE (OLLA SYNERGY premise falsified)  
- `734bcee6d4` – status doc reflection  
- `628aaa51f5` – U‑VIZ‑MERGE‑AUG‑CAP‑GUARD (`exceedsStringParseCap`)  
- `7a1f52061b`, `80f8059cb1` – post‑merge capsafe (atomic streaming writes)  
- `3e4df51d04` – removed phantom FAST[] entry (`generate-slot‑queue‑features.mjs`)  
- `d03d8687a7` – regen‑pipeline readers now use `readGraphStreaming`  
- `4dbd18c2e3`, `17294fc77f`, `e7704ba450`, `441a7149fc`, `736c9cfd8b`, `8bf1873577`, `6bea1b726f` – U‑VIZ‑READER‑CAPSAFE‑2, tribal rerank stream/fix, embed resumable, sibling writer shard‑safe, manifest‑aware read/write, CLAUDE.md regression entry  
- `46c07e9cd7 → b637bfb0c4 → 9fd0c8c7d1 → 1322c38364` – U‑TRIBAL‑SIBLING‑WRITER‑SHARD‑SAFE (shared guarded IO, lock)  
- `7166f51e41` – wiki entry on shard transition  
- `f6e596b767`, `a6dbec1842`, `037f61dc86`, `05906647ad` – tribal monolith untrack, audit wired via engine, Scrutiny P2 fix, large‑read decay wire  
- `8f373e9e43`, `7c184bc97c`, `88ec35bf6d`, `ab9318ee47`, `d0f6176db5`, `ee29e4ea06`, `d132015a0e`, `88cb72b532`, `3a038b96f6` – grep‑index decay wire, advisory apply all, offload‑rate honesty fix, bug‑class sweep, xsub consensus of, drift detect, per‑prompt inject surface, embeds edge + documented‑by fix, doc‑reflect  
- PRISM_OLLAMA_ROUTE_AUTO=1 enabled; auto‑offload hooks (`ollama-task-offloader`, `ollama-route-pretooluse`) live and fail‑open verified  

**DECISIONS**  
- Use `/checkin‑sierra` wrapper for deterministic slot binding before canonical `/checkin`.  
- Verify audit findings via 6‑agent ultracode workflow; no blind code changes.  
- Prioritize B1–B3 (critical ops) then C2–C4 (maintenance & data integrity).  
- Close C1 (sidecar re‑embed) – self‑healed.  
- Leave partial 100‑link candidates file; document OOM on full scan.  
- Add `coverageComplete` flag only if streaming writes are implemented.  
- Hook‑heap cap set to 384 MB (down from 4 GB) to avoid over‑commit and MCP spawn failures.  
- MCP server maxConnections raised to 512 for future 26‑slot load; no immediate benefit but headroom.  
- Offload pattern list widened in `OllamaTaskOffloaderEngine.ts`; more tasks routed to GPU (Blackwell).  
- Treat `ERROR_NO_SYSTEM_RESOURCES` as benign “pressure” via `isTransientPressureCode()`.  
- Adopt streaming I/O for all graph reads/writes (`readGraphStreaming`, `writeGraphStreamingAtomic`) to avoid V8 512 MiB string cap.  
- Replace materialized `augment-molecules.mjs` with per‑node streaming projection; remove OOM.  
- Loud‑degrade guard on oversized optional loads (exceedsStringParseCap).  
- Atomic streaming writes for all post‑merge stages (`writeGraphStreamingAtomic`).  
- Shared guarded IO pair (`tribal-index-guarded-io.mjs`) replaces monolith‑only writers; lock protection via `withTribalIndexLock`.  
- Close recurring tribal brain clobber vector (4× incidents) by wiring embed‑wiki, embed‑all‑wiki, prune‑stale, retag through guarded IO.  
- Elevate Sierra to full authority; create memory/rule in MEMORY.md bypassing lane restrictions for backend dev.  
- Enable `PRISM_OLLAMA_ROUTE_AUTO=1` auto‑offload; keep manual fallback to Claude.  

**OPERATOR DIRECTIVES**  
- “Finish all ollama synergy tasks – use strongest viable ollama models for searches, read, write, coding.”  
- “Accelerate self‑improving of obsidian vault and `/system-viz`.”  
- “When all work is exhausted, look for inefficiencies, gaps, wirings, dormant nodes to activate.”  
- “Goal: wired, tested, validated, synergized across all galaxies and full H drive.”  
- “Stop hook condition: finish all ollama synergy tasks… accelerate self‑improving of our obsidian vault, /system‑viz. When all work is exhausted, look for inefficiencies, gaps, wirings and dormant nodes to activate | goal clear: wired, tested, validated and synergized across all galaxies and full h drive systems.”  
- “/yolo-mode in proper loops” (for cron/peer loops).  

**FINDINGS / BUGS**  
- `obsidian-memory-sync.mjs`: unguarded `writeFileSync`, missing export → data‑loss on first error.  
- `memory-rag-inject.mjs`: keyword recall surface unwired; both injectors disabled.  
- `WikiIndexMaintainerEngine.formatIndex`: stale frontmatter (`last_verified`, `Total entries`) never refreshed.  
- `wiki-link-fix-suggester.mjs`: over‑confidence (≥0.85 auto‑apply); decay path leaks >60‑char slugs above floor.  
- Stale candidates JSON (`autoApplyCount:14100` → 33) covers only 100 links; full scan OOM (~800 M calls).  
- `merge-augmentations.mjs`: exit‑1 at ~630 MB due to V8 string cap on optional load (`obsidian`).  
- Phantom FAST[] entry caused MODULE_NOT_FOUND.  
- Large‑read decay wire noise (0/122); offload‑rate misreporting 10.7% → corrected to 40.6%.  
- `documented-by` collapsed from 38→0 due to volatile source; restored via oracle confirmation.  
- Drift detector needed for silent collapse detection; implemented and live‑validated.  
- Orphan augmentations (jm-die-cited-tips, milling-extracted-pdf-bridge) with dangling edges → cannot activate without canonical graph edit.  
- Engine class names mismatch (`MasterPostProcessorEngine` vs `eng.*`) – documented‑by endpoint bug.  
- Auto‑route hook dormant; auto‑offload hooks active but require env flag for full automation.  

**DOMAIN SPECIFICS**  
- Engines/Actions: `obsidian-memory-sync.mjs`, `memory-rag-inject.mjs`, `WikiIndexMaintainerEngine.formatIndex`, `wiki-link-fix-suggester.mjs`, `cron installers` (`install-promote.ps1`, `install-rot.ps1`), `system-viz query CLI` (`node-card`, `doc-nodes`, `canvas`, `canvas-doc`), `regen‑viz.mjs`, `augment-molecules.mjs`, `merge-augmentations.mjs`, post‑merge scripts, tribal index writers (`embed-engines`, `embed-knowledge-store`, `embed-cited-tips`, `embed-wiki`, `embed-all-wiki`, `prune-stale`, `retag-backend-dev`).  
- Guarded IO: `readTribalIndexGuarded`, `writeTribalIndexGuarded`; lock helper `withTribalIndexLock`.  
- Shard threshold: 480 MiB → manifest + shard files; monolith removed on exceed.  
- Metrics: node/edge counts, graph size ~236 MB → 333 k nodes; OOM at 800 M scoreCandidate calls; hook‑heap cap 384 MB; MCP maxConnections 512.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
- Canonical `/checkin` pipeline (`H:/prism/.claude/commands/checkin.md`).  
- Ultracode workflow (6 agents, 90 tool uses).  
- Node test (`node:test`) and Vitest.  
- Git hooks: lane guard, write‑block guard, commit‑ledger.  
- c-to-h mirror for global→H sync.  
- Scrutiny‑3way.mjs (3‑of‑3 reviews).  
- CronCreate/CronDelete for recurring `/loop`.  
- Ollama models (`qwen2.5-coder:32b`, `gpt‑oss:120b`) via `scripts/ask-ollama.mjs` (`callOllama`).  
- Graph I/O: `graph-io.mjs` (`readGraphStreaming`, `writeGraphStreamingAtomic`, `streamGraphArray`).  

**OPEN THREADS**  
- Remaining seven cap‑unsafe graph readers (core search‑lib, other utilities) – to be fixed in next context tick.  
- Full audit of all graph I/O for future regressions.  
- Activation of cited‑tips merge splice pending canonical graph edit (two‑arm scrutiny).  
- Dormant engine activation remains in November’s `U‑DEA` lane (not this session).  
- Full H‑drive synergy across all galaxies is unbounded; will continue via cron loop `296523b3`.  
- Expand deterministic offload coverage to meet 30 % target.  
- Monitor shard transition guard and monolith removal during live merges.
