# hermes-zulu session 19dff632 (2026-06-10, 19.7MB, spine 146KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e44a3a1592`: wired 5 orphaned `PayrollLiabilityFilingEngine` methods into `businessDispatcher`.  
- `e649790e76`: added missing `payroll_remit_liability` case, fixed positional‑arg handling; all 14 round‑trip tests pass.  
- `9dfd621910`: committed reusable Ollama transcript miner (`scripts/mine-hotel-transcripts.mjs`).  
- `099e6b92bd`: committed final hotel forge roadmap spec (`state/shared/specs/HOTEL-FORGE-ROADMAP-2026_06_09.md`).  
- `f069052772`: verified all 6 “stale” gaps shipped; no build needed.  
- **U‑HOTEL‑PORTAL‑PERSISTENCE**: replaced 4 in‑memory maps with SQLite WAL store; full E2E persistence tests green.  
- **U‑HOTEL‑ALLOWLIST‑WRITE‑ENABLE**: manager‑tier workflow writes gated by per‑action role; financial/PII writes remain 403.  
- **FALSE‑WIRE‑REGRESSION‑GUARD**: regex fixed, guard passes all 17 read actions and catches re‑stub cases.

---

**DECISIONS**  
- Use Ollama (`qwen2.5-coder:32b`) for bulk summarization; ultracode workflow for high‑level roadmap synthesis.  
- Default grunt‑work coder: `qwen2.5-coder:32b`; heavy multi‑file synthesis: `gpt-oss:120b`.  
- Commit miner script & digests as reusable tool; keep roadmap spec in repo.  
- Roadmap order: verify shipped gaps → constants audit → mobile wire exempt (satisfied) → Q2S DFM bug → false‑wire guard → portal persistence build.  
- Role model approved – keep `qwen2.5-coder:32b` as grunt‑work tier; no shift to GPT‑OSS.  
- UI overhaul will build on Quebec’s existing `WorkspacePrimitives`; no new design system fork.  
- Haptics are phone‑app only → flag for future native shell work, not part of current scope.  
- Must run `/compact` (fresh budget) before any multi‑file UI changes.

---

**OPERATOR DIRECTIVES**  
- “read previous few sessions of hotel to regain full context”  
- “continue” / “start” – execute roadmap units.  
- “read transcripts … use workflow if needed, can we use ollama for better efficiency?”  
- “if we can utilize ollama to generate a brainstormed planned out forge road map … please do so; if not, use ultracode.”  
- “try again, we hit session limits, had to switch accounts”  
- Approve role model (already done).  
- Decide whether to proceed with iOS‑style UI redesign under current constraints; if yes, trigger `/compact`.

---

**FINDINGS/BUGS**  
- Payroll engine orphaned methods wired; 5 previously unexposed methods fixed.  
- Audit stale: missing tax engine built; dedup found near‑duplicate `FormW2Engine`.  
- Mobile engines mostly wired; only `MobileVoice` orphan but exempt.  
- Q2S DFM bug confirmed (`non‑iterable ctx.features`).  
- False‑wire guard regex missed “Unknown business action: X”; fixed.  
- `customer-portal.db` and WAL files not gitignored → added `.gitignore`.  
- Design doctrine conflict (R7): Quebec’s industrial dark HUD forbids iOS‑style look; must reconcile or defer.  
- Haptics unsupported in current web stack; requires native Capacitor layer.

---

**DOMAIN SPECIFICS**  
- **Engines**: `PayrollLiabilityFilingEngine`, `CustomerPortalEngine`, `MobileVoice`, etc.  
- **Actions (businessDispatcher)**: `payroll_compute_940`, `payroll_generate_w2`, `payroll_reconcile_w2_941`, `payroll_contractor_1099_totals`, `payroll_remit_liability`.  
- **Dispatchers**: `businessDispatcher.ts` (lazy `getEngine`).  
- **Metrics/Values**: IRS values in tests – FUTA $72, W‑2 box4 $3,100, SS cap $10,918.20, Medicare addl $4,075, 1099 $1,500, half‑even rounding.  
- **Paths**: `routes/business.ts`, `hotel-portal.ts`, `realtime.ts`, `src/data/payroll-tax-tables.ts`.  
- `CustomerPortalEngine` uses SQLite WAL; `rateBuckets` remains transient.  
- Auth via `req.userRoles`; auditLog middleware records user ID and roles on all writes.  
- Role gate implemented in‑handler for `/api/v1/business/dispatch`.

---

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`, `businessDispatcher`, false‑wire guard test, auditLog middleware, CoordinationStoreEngine pattern.  
- Ollama helper: `ask-ollama.mjs` (`qwen2.5-coder:32b`).  
- Miner script: `scripts/mine-hotel-transcripts.mjs`.  
- Ultracode workflow for roadmap synthesis.  
- 3‑of‑3 scrutiny gate (reviewers A, B, C).  
- Slot‑commit‑enforce hook (`[BOOTSTRAP-SLOT-ENFORCE]`).  
- ASCII‑guard hook.  
- SQLite (`better-sqlite3`) with WAL.  
- Vitest for unit tests; Git for commits and scrutiny ledger.

---

**OPEN THREADS**  
- UI redesign scope pending decision on design doctrine conflict & haptics feasibility.  
- `/compact` must be run before any UI changes to secure fresh budget.  
- Follow‑up on identity mapping (A‑P2) remains future task if manager‑role gate expanded.
