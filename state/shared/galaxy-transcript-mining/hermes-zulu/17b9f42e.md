# hermes-zulu session 17b9f42e (2026-06-13, 41.2MB, spine 240KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Desktop launcher `LAUNCH-HERMES-FLEET.bat` built; 21 Hermes profiles created & seeded with SOULs.  
- Cross‑substrate synergy edges: +55 218 new edges across all 34 galaxies.  
- GNN reference pool expanded by +8 confirmed wirings merged into system graph.  
- Commit `5010a01a82`: MCP boot‑crash fix, tool dedup guard, rename `claudeAccountDispatcher` → `prism_claude_account`, updated error handling.  
- Deterministic gate `ai-systems-synergy-goal-gate.mjs` PASS on all 4 legs (synergy, LoRA, GNN selective AUROC 0.8084, CAG coverage).  
- Miner script `mine-galaxy-transcripts.mjs` invoked for Bravo sessions since 2026‑06‑12.

**DECISIONS**  
- Use Hermes profile system (`hermes -p <slot>`) per NATO slot; seed each with SOUL & worktree cwd.  
- Generate cross‑substrate edges via `generate-cross-substrate-edges.mjs` after ref‑pool growth.  
- Adopt deterministic loss function gate as authoritative completion signal; operator must `/clear` or re‑scope to stop.  
- Remove legacy `aiDispatcher.ts`; keep canonical 12‑action dispatcher.  
- Add last‑wins dedup guard at `proxiedTool()`; pin SDK to fixed minor version, reconcile TS API drift.  
- Use atomic write (`writeGraphStreamingAtomic`) for graph mutations.

**OPERATOR DIRECTIVES**  
- Duplicate existing Claude fleet launcher to launch Hermes instances per slot.  
- Read all Bravo sessions from 06‑12/13 and today/yesterday via Ollama `qwen2.5vl:32b`; resume where left off; max out Oscar knowledge.  
- Improve AI systems across all galaxies with full synergy (deterministic gate).  
- Implement synthetic domain node unit to resolve 4‑galaxy owned‑by‑slot gap.  
- Hook auto‑clears when deterministic gate PASS; no manual `/goal clear`.

**FINDINGS/BUGS**  
- R14 bash.exe runaway due to simultaneous heavy background tasks; resolved by stopping workflow & miner nodes.  
- LoRA dataset bound at 1366 rows; regeneration would regress below floor, left unchanged.  
- 9‑galaxy owned‑by‑slot gap: 5 correct skips, 4 require synthetic domain node unit.  
- MCP server crash caused by duplicate `prism_ai` registration; error swallowed as `{}`; source identified & plan to fix.  
- Duplicate `prism_auth` registration (`claudeAccountDispatcher` mis‑named).  
- SDK caret drift caused hard throw on duplicate tool names.  
- Stale `git.exe` lock (5 h) cleared safely.  
- Memory pressure spikes (~97 % commit) due to transient Ollama model loads; self‑recovered after reaper sweep.

**DOMAIN SPECIFICS**  
- Hermes profiles stored under `%LOCALAPPDATA%\hermes\profiles\<slot>`.  
- Slot boot script `hermes-slot-tab-boot.ps1` runs `hermes -p <slot>` in slot worktree.  
- Cross‑substrate edges embed galaxy ↔ NN/GNN/RAG 768d nodes & documented‑by (galaxy ↔ memory/wiki synthesis).  
- GNN reference pool feeder reads vault, writes CONFIRMED labels at ≥0.8 confidence.  
- Dispatchers: `registerMemoryDispatcher`, `registerAuthDispatcher`, `registerAIDispatcher`, `registerAIReasoningDispatcher`.  
- Tools: `prism_ai`, `prism_auth`, `prism_claude_account`.  
- Galaxy assets: `souls.md`, `claude.md`, `memories.md`, `wikis.md`.

**TOOLS USED**  
- Hermes CLI (`hermes.exe`, `hermes profile create`).  
- PowerShell scripts (`LAUNCH-HERMES-FLEET.bat`, `hermes-slot-tab-boot.ps1`).  
- Node scripts: `generate-cross-substrate-edges.mjs`, `vault-to-gnn-refpool.mjs`, `vault-to-lora-dataset.mjs`.  
- Deterministic gate script `ai-systems-synergy-goal-gate.mjs`.  
- Miner script `mine-galaxy-transcripts.mjs`; Ollama (`qwen2.5vl:32b`).  
- PRISM SDK (`McpServer.tool()`, `proxiedTool()`), atomic write helper.  
- Build tools: esbuild, TypeScript compiler.  
- Git hooks: `git-lock-sweep`.

**OPEN THREADS**  
- GNN full‑coverage AUROC training (≥ 0.78) on India GPU – pending GPU/India bound.  
- Implementation of synthetic domain node unit to resolve owned‑by‑slot gap.  
- Continuous monitoring & tuning of memory pressure; potential automated reaper.  
- Final pinning of SDK minor version and full API drift resolution.  
- Max out Oscar (Speed‑Feed‑Calculator) knowledge – miner run pending.  
- Optional refinement of `claudeAccountDispatcher` logic for new Claude‑account features.
