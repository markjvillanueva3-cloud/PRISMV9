# hermes-zulu session 928a8226 (2026-06-10, 68.1MB, spine 676KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `scripts/fix-hookify-frontmatter.mjs` – rewrote 110 `.local.md` files to YAML front‑matter; transliterated non‑ASCII bytes.  
- `scripts/remove-dangling-hook-refs.mjs` – removed 6 dead module refs from `settings.json`.  
- `ollama-resilient-pull.ps1` (c593b096fb) – fixed em‑dash parse error, added `DEFAULT_NUM_PREDICT=1024`, completed download of `gpt‑oss:120b`.  
- `13017de764` – hookify frontmatter conversion & dangling hook cleanup.  
- `29f2d177f6` – node‑by‑node synergy gap‑map artifact (27/27 tests).  
- `8550436c8a` – corrected phantom Qdrant label in CAG‑router.  
- `c9fe32d8b2` – embedder OOM & context‑window fix (heap flag + 2000‑char clamp).  
- `5467482be4`, `c994bf3aad` – bug‑finding log, wiki lesson, updated gap‑map.  
- `182788232a` – cap‑safe loader for tribal index (`tribal-embed-index.json`).  
- `a3e6d3ca97` – fail‑loud read guard + clobber‑preventer; restored 4 162‑entry baseline.  
- `fb314a6fd1` – Docker‑service‑health‑check CLI (5/5 tests).  
- `35e01ca5f3` – singleton‑service‑guard CLI for MCP (`--fix`).  
- `e2081e0780`, `ed6662f45e`, `29fb555f13`, `6ca2f6afd2` – MCP daemon detection regex, JSON‑stdout protocol, heap floor helper (≥4096 MB).  
- `4141daf9d8` – fleet‑task‑health migration‑freeze marker.  
- `U-QP-COST-BRIDGE-WIRING-TRUTH`, `U-QP-AWARENESS-GEN-ROUTER-AWARE`, `U-QP-COST-SAVINGS-TEST` – router consolidation, awareness generator, testable CostSavingsTrackerEngine (13 vitest cases).  
- `U-BW-DISPATCHER-SCAN`, `U-BW-DISPATCHER-SCAN-WIKI` – cascade defaults repointed to installed Blackwell models; source‑lock guard.  
- `U-PSN-SNAPSHOT-REFRESH` – regenerated 6‑day stale PSN snapshot (11 P0 under‑wired pairs).  
- `U-QP-DIGITALTWIN-TEST`, `U-QP-MARGIN-FLOOR-GATE`, `U-QP-MARGIN-FLOOR-SHOPCONFIG`, `U-QP-NRE-AMORTIZE-TEST-FIX` – DigitalTwinEstimator tests, margin‑floor gate & shop config, NRE amortization test fix.  
- `ec597dbcb3`, `87d5c4bf9a` – margin‑floor dispatcher round‑trip & alert banner (7/7 & 6/6 tests).  
- `4b0980c56a` – AI‑substrate routing matrix coverage 27/27.  
- `62b114cb7b` – ResizeObserver polyfill + null‑guards; fixed hidden frontend test failures.  
- `8258918f78` – flat‑array mock fix in `quoteHistory`; added failure‑mode guard.  
- `44fab1be37` – dispatch map for fleet‑scope synergy goal (rows 8–15).  
- `U-QP-CLOSED-LOOP-PROVENANCE-GATE`, `U‑QP‑OUTBOUND‑PROMOTE‑GATE`, `U‑QP‑CALIBRATION‑FRESHNESS‑PRELIGHT` – closed‑loop learning gates, outbound price alignment gate, 24 h stale calibration warning.  

**DECISIONS**  
- Convert all hookify rule files to canonical YAML front‑matter; strip non‑ASCII chars.  
- Remove dangling hook references from `settings.json`; treat unwired engines as owner‑triage items.  
- Use resilient PowerShell pull script (no em‑dash); set `DEFAULT_NUM_PREDICT=1024` in ask‑ollama to avoid empty responses.  
- Batch subagent orchestration in groups of 3 to stay within Anthropic rate limits.  
- Replace string read of 533 MB tribal‑embed‑index with buffered loader; add fail‑loud guard and clobber‑preventer.  
- Add `ensureHeapFloor` helper (≥4096 MB) to all MCP spawn paths; fix daemon detection regex.  
- Consolidate cost‑bridge router, awareness generator, and CostSavingsTrackerEngine into a single wiring truth.  
- Wire cascade defaults to installed Blackwell models; close source‑lock blind spot.  
- Implement margin‑floor gate with caller override, default, and shop‑config sourcing.  
- Tag intentional adapters (e.g., WEDMQuoteBridge) as `WIRE‑EXEMPT`.  

**OPERATOR DIRECTIVES**  
- Finish pulling Ollama LLM models (`gpt‑oss:120b`, `qwen2.5-coder:1.5b`); integrate into system tasks.  
- Validate that `gpt‑oss:120b` is selected for best‑tier categories (e.g., `search_synthesis`).  
- Monitor fleet‑reaper output after each `/checkin-golf`.  
- Synergize Ollama, Docker, Qdrant, Obsidian vault app, PSN, system‑viz, Prism galaxies, front‑end/back‑end build, Claude.md, memories, wiki, tribal knowledge, Prism awareness, GSD, TDD, skills, scripts and hooks; goal: everything wired, tested, validated, synergized node by node, galaxy by galaxy.  
- Utilize ultracode for planning, deep reasoning, orchestrating + Ollama local LLM for grunt work.  

**FINDINGS/BUGS**  
- Hookify rule files missing YAML front‑matter → “missing front‑matter” warnings; one file had UTF‑8 emoji causing charmap decode error.  
- 6 dangling `.mjs` references in `settings.json` caused “Cannot find module” errors.  
- Scheduled‑task WARNs due to intentional hardware/drive migration freeze (false positive).  
- 90 engines built but unwired (53 core, 23 bridge, 14 ops); some deliberate orphans (`MillPrintToProgramEngine`).  
- Ollama pull stalled at 12 % from em‑dash parse error; after fix, `gpt‑oss:120b` downloaded but router still selects `gpt‑oss:20b` for best‑tier tasks.  
- Embedder OOM from 533 MB tribal‑embed‑index (exceeded Node heap & V8 string limit); full‑body input exceeded Nomic context window.  
- Rate limiting hit when 11 subagents ran concurrently against Anthropic API.  
- Qdrant container downtime caused by orphaned “Created” state; fixed with correct start command.  
- MCP server boot‑OOM due to 384 MB heap cap; resolved by enforcing ≥4096 MB.  
- `stop_on_hook_unregistration` gate was a no‑op (protocol mismatch); fixed to emit JSON.  

**DOMAIN SPECIFICS**  
- **Engines/Models:** `gpt‑oss:120b`, `qwen2.5-coder:1.5b`, Blackwell models, `CostSavingsTrackerEngine`, `WEDMQuoteBridgeEngine`, `QuoteEstimatorEngine`, `DigitalTwinEstimator`, `OutboundPriceIndexEngine`.  
- **Actions / Dispatchers:** `ask‑ollama.mjs`, `host-class.mjs`, `resolveSynthesisModel`, `cag-router.mjs`, `tribal-rerank.mjs`, `embed-wiki-into-tribal-index.mjs`, `aiReasoningDispatcher.ts`, `businessDispatcher`.  
- **Metrics:** GPU utilization, Ollama load times (~39 s for 65 GB), token generation rates (~114 tok/s), response emptiness rate, rate‑limit status, margin‑floor pass (7/7), routing matrix coverage (27/27).  
- **Paths / Files:** `scripts/fix-hookify-frontmatter.mjs`, `scripts/remove-dangling-hook-refs.mjs`, `ollama-resilient-pull.ps1`, `load-tribal-index.mjs`, `embed-wiki-into-tribal-index.mjs`, `docker-service-health-check.mjs`, `singleton-service-guard.mjs`, `mcp-server-daemon.mjs`.  

**TOOLS USED**  
- PRISM orchestration: `/loop` (CronCreate), fleet‑reaper, memory‑monitor, orphan‑process reaper.  
- Hookify plugin (`config_loader.py`).  
- Node scripts: `fix-hookify-frontmatter.mjs`, `remove-dangling-hook-refs.mjs`, `audit-unwired-engines.mjs`.  
- PowerShell script: `ollama-resilient-pull.ps1`.  
- Docker‑service‑health‑check CLI, singleton‑service‑guard CLI.  
- Vitest (13 vitest cases for CostSavingsTrackerEngine, DigitalTwinEstimator).  
- Ultracode workflow engine (`wf_8da40cea-18a`).  

**OPEN THREADS**  
- Wire remaining unwired core engines (owner triage).  
- Resolve router selection bug so `gpt‑oss:120b` is used for best‑tier tasks.  
- Finalize integration of all installed Ollama models into task dispatchers.  
- Re‑embed lost ~29 K tribal entries; implement write‑side sharding to avoid future string/heap caps.  
- Migrate tribal data from JSON into Qdrant collections (deferred).  
- Evaluate need for persistent MCP supervisor (current watchdogs sufficient).  
- Complete per‑galaxy synergy audit across all peer slots (foxtrot, whiskey, mike, kilo, charlie, hotel, oscar, juliett, quebec, sierra, papa).  
- Integrate accounting with ERP/QuickBooks (data‑blocked ERP/QuickBooks integration).  
- Finish closed‑loop learning critical path (`U-QP-ACCOUNTING-WIRE`).  
- Address remaining quoting frontend failures in hotel domain.
