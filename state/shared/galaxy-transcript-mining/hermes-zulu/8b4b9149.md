# hermes-zulu session 8b4b9149 (2026-06-10, 47.8MB, spine 516KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5ae481f748` – EPERM leak fix in OutcomeCaptureBusEngine (atomic append, bounded retry).  
- `86f0e3fe0c` – wired tri‑vendor comparator, exhaustive combination, downstream subscriber engines + 16 round‑trip tests.  
- `a2dbfa76e1` – closed‑loop driver; rescued 7 untracked SFC engines; fixed live consensus bug (exclude not‑aligned externals).  
- `43e1b8e449` – added G‑Wizard alignment symmetry, regression test for misaligned advisory.  
- `891c66e728` – full‑sweep comparison driver + corrected ISO‑material map (added N & H coverage, fixed aluminum misclassification).  
- `16d6eecef4` – SpeedFeedCalibrationPersistEngine; unit tests, dispatcher wiring, round‑trip test.  
- `890b2c39e7` – VC assessment spec addendum (live results).  
- `3438987f0e` – hardening of calibration engine: clamped flag fix, persist‑tmp cleanup, skipped‑lines observability.  
- `f31398a1a5` – SpeedFeedGpuJudgeEngine; wired to prism_calc.  
- `f5d14ddb29` – GPU judge hardening: probe‑gpu‑residency prefix‑match fix, timeout, matched‑model surfacing.  
- `951f5ac335` – GPU judge polish: isProbe constant, loud warning note, 0‑judgeable guard.  
- `3a1c20fca2` – full‑input sweep unit; added full mode, runStreaming, driver changes; 69 120‑cell sweep.  
- `af0ac16c5a` – scrutiny fixes: cells.length ReferenceError, ISO‑group collapse fix (tool‑material inertness).  
- `b847c0f1ff` – scoping spec for axis‑awareness enhancement (tool material → coolant → holder/machine/spindle …).  
- `28eca0ed84` – doc reflection of GPU judge findings.  
- `54c0ae06c8` – spec addendum for GPU judge and full sweep comparison.  
- `658c8280fe` – canonical Tool‑Material axis table + engine wiring.  
- `e9b68da865` – (same as above, de‑duplicated).  
- `585584e3ae` – Coolant axis wired into UltimateSpeedFeedEngine via CoolantVcModifier.  
- `7d0affcae6` – Rigidity‑to‑Vc factor de‑inlined to constants.  
- `f998f8af71` – applied tool‑material, coolant and rigidity factors to engine alternatives.  
- `da41a58fd1` – full 102 060‑combination sweep ledger (`sfc-full-sweep-ledger.jsonl`).  
- `c48829e331` – axis‑liveness probe (Vc/RPM/feed/MRR).  
- `d03458fff1` – extended probe to tool_life; closed runout‑impact loophole.  
- `2070c472a4` – workholding.type → live (ClampingForceEngine).  
- `7dc7798033` – spindle.hp → live.  
- `8b191d5427` – tool_holder.type runout→life → live.  
- `a257f872bb` – axis‑liveness complete; added missing axes (`toolpath.strategy`, `cut_type`, `mode`).  
- `81e37ece16` – U‑OSC‑FINISH‑RA‑CAP (numeric Ra target, 12 tests).  
- `6a3ad56545` – COMPARE‑TOOLMATERIAL‑VARIABILITY (tri‑vendor sweep varies tool_material; 576 comparisons).  
- `c78faa5a73` – COMPARE‑HSS‑BASELINE (web‑verified HSS baseline, +107.8 % delta).  
- `835df42c74` – COMPARE‑CERAMIC‑CBN‑BASELINE (ceramic & CBN entries added, 12 points).  
- `4c544db4ae` – COMPARE‑PER‑VENDOR (PRISM vs G‑Wizard/HSMAdvisor deltas; 10 tests).  
- `08d7fc6d37` – ALL‑AXIS‑SWEEP (OAT × 2 regimes + factorial sweep of all 25 named axes; 16 live, 9 inert‑at‑baseline).  
- `93de130f59` – ALL‑AXIS‑TOOLLIFE (tool‑life added to liveness check → 18/25 live).  
- `907e74acab` – HSS SPEED MODEL FIX (over‑speed bug eliminated; +108 % → −23 %, 20 tests).

**DECISIONS**  
- Replace costly read‑rewrite‑rename append with `fs.appendFileSync` + bounded retry to eliminate EPERM leaks.  
- Wire tri‑vendor comparator, exhaustive combination and downstream subscriber engines directly into dispatcher; expose DownstreamSubscriber via registration status & cache snapshot introspection.  
- Consensus logic: exclude not‑aligned externals when a better aligned external exists; include misaligned only if sole signal.  
- Add G‑Wizard alignment check to mirror HSMAdvisor’s flagging; enforce symmetric safety filtering.  
- Commit orphan engines and add missing ISO groups (N, H) to exhaustive sweep for full input coverage.  
- Create SpeedFeedCalibrationPersistEngine that derives per‑ISO‑regime calibration factors; no auto‑apply due to safety concerns.  
- Add GPU‑based machinist‑judge engine; expand exhaustive sweep from 86 demo cells to ≈69 k combos.  
- Fix all P2 findings: clamped flag, persist tmp cleanup, skipped_lines observability, cells.length error, ISO‑group collapse (tool‑material inertness).  
- Prioritize building tool‑material speed‑factor table; integrate into physics engine.  
- Adopt dedup‑first strategy: reuse existing CoolantVcModifier instead of duplicating a table.  
- Canonicalize physics constants in `constants.ts`; enforce `CONFIRM_CRITICAL=true`.  
- De‑inline hardcoded rigidity factor to satisfy soul rule and enable future stability‑lobe work.  
- Hoist axis factors into single source so both primary Vc and alternatives receive them, fixing inert‑axis problem.  
- All inert axes must be physics‑reviewed before shipping; no ad‑hoc wiring.  
- Controller remains inert on default (product‑philosophy decision per physics‑reviewer).  
- Comparison sweep must span all vendor‑modeled materials; identified missing `tool_material` axis.  
- Use existing engine logic (`runoutImpact`, `ClampingForceEngine`) rather than new models to avoid double‑counting.  
- Extend BaselineSource enum for ceramic/CBN sources (Tungaloy, Iscar, NTK).  
- Create sibling module for material‑specific tool‑material speed factor override; widen clamp range 0.10–8.00.  
- Add `baseline_detail` field to TriCompareResult so per‑vendor deltas are surfaced without re‑deriving input.  
- Implement two‑regime sweep (rigid‑roughing + high‑RPM/finishing) to capture conditional caps.  
- Include tool‑life spread in liveness classification; adjust regime logic to be effect‑aware.

**OPERATOR DIRECTIVES**  
- “Run calculations for every possible combination of machines, spindles, controller, materials, work holding/fixture, tool holder connection type and holding mechanism (balance, max speeds, rigidity, dampening capability, accuracy), tooling + insert if viable, coolant type, tool path type, cutting parameters, desired finish quality, roughing; semifinish; or finish quality.”  
- “Compare ALL possible calculations and parameters with max variability to G‑Wizard and HSMAdvisor.”  
- “Continue where we left off / reorientate with previous real sessions of Oscar and continue where we left off.”  
- “Utilize the new CPU, 128 GB RAM, RTX 6000 Blackwell, NVMe SSD … run full SFC closed‑loop training and comparison of all potential inputs … compare output to G‑Wizard and HSMAdvisor as baseline comparison.”

**FINDINGS/BUGS**  
- EPERM leak in OutcomeCaptureBusEngine due to concurrent rename failures.  
- G‑Wizard toolcrib contains 0 tools with proven surface speed (sfm) → cannot contribute to comparison.  
- HSMAdvisor returns constant 634.6 m/min for all materials; engine flags not aligned, but consensus calculation mistakenly includes it.  
- ConsensusOf(externals) bug: unaligned systems included in median.  
- P2 bugs: clamped flag false‑positive; persist tmp not cleaned on rename failure; missing skipped_lines observability; cells.length ReferenceError after refactoring to streaming driver; ISO‑group collapse (tool material within group produces identical Vc).  
- GPU judge probe‑gpu‑residency prefix‑match bug.  
- PRISM under‑speeds published references by ~25 % (worst –53 % on 6061 Al).  
- Orchestrator default mode reads `sfc.alternatives.balanced.vc`, which omits tool‑material, coolant and rigidity factors → axes appear inert.  
- G‑Wizard crib rows all have sfm=ipt=0; geometry‑only.  
- HSMAdvisor data files contain only geometry/prefs; no stored cutting data.  
- Engine’s `runoutImpact.life_reduction_pct` never applied to recommendation (inert).  
- Controller on default is intentionally inert; not a gap.  
- Comparison sweep hard‑coded `tool_material="carbide"`; other materials omitted.

**DOMAIN SPECIFICS**  
*Engines* – OutcomeCaptureBusEngine, SpeedFeedTriComparatorEngine, SpeedFeedExhaustiveCombinationEngine, SpeedFeedDownstreamSubscriberEngine, SpeedFeedBaselineComparatorEngine, SpeedFeedPropagationBridgeEngine, SpeedFeedOutcomeFeedbackBridgeEngine, SpeedFeedSelfTuningEngine, SpeedFeedCalibrationPersistEngine, SpeedFeedGpuJudgeEngine, UltimateSpeedFeedEngine, SpeedFeedNineAxisOrchestratorEngine, ClampingForceEngine.  
*Actions/dispatchers* – `speed_feed_tri_compare`, `speed_feed_exhaustive_combination` / sweep, `speed_feed_downstream_packs`, `speed_feed_calibration_persist`, `speed_feed_gpu_judge`, `prism_calc:speed_feed_calibration_persist`, `calculate()`, `translateToUltimate()`, `buildModeRecommendation()`, `compare()`, `findBaseline()`.  
*Metrics* – Vc (m/min), feed rate mm/min, rpm, fz mm, MRR cm³/min, tool_life min, stability margins/DOC, consensus median, baseline Vc, HSMAdvisor alignment flag, G‑Wizard geometry‑only status, per‑vendor deltas (`vc_variance_pct`, `fz_variance_pct`).  
*Paths* – `%APPDATA%\G-Wizard\toolcrib.csv`; `settings_v2.xml`; `state/outcomes/sfc-full-sweep-ledger.jsonl`; `state/outcomes/sfc-calibration-model.json`; `sfc-full-sweep-compare.mjs`; `sfc-all-axis-sweep.mjs`; `sfc-all-axis-toollife.mjs`; `src/physics/constants.ts`; `algorithms/CoolantVcModifier.js`; `calcDispatcher.ts:9681`; baseline DB (`BASELINE_DB`); per_source array; enum `BaselineSource`.

**TOOLS USED**  
PRISM helpers (`chat-slots.mjs`, `slot-bind-enforce.mjs`), workflow orchestration (`hermes-workflow-planner.mjs`, ultracode engine), audit tooling (`forge-audit-v2`), test harness (Vitest, tsx driver script, TypeScript compiler, esbuild, tsc incremental), GPU inference (Ollama CUDA‑backed LLMs, `nvidia-smi`, qwen2.5‑VL vision‑OCR on Blackwell GPU for vendor catalog extraction), Node hooks (`CronCreate`, `node -e` scripts), PRISM sweep tooling (`sfc-full-sweep-ledger.jsonl`, etc.), physics‑reviewer and independent reviewer (per‑file scrutiny).

**OPEN THREADS**  
1. Wire `PRISM_SFC_CALIB_APPLY` keystone (operator‑gated apply of calibration factors).  
2. Run GPU‑based closed‑loop training on 86‑row ledger; schedule.  
3. Validate persisted calibration model applied correctly across ISO regimes.  
4. Extend G‑Wizard alignment checks to additional scenarios; ensure geometry‑only status handled consistently.  
5. Final QA of full pipeline (data ingestion → sweep → comparison → calibration persist → apply) before production launch.  
6. Implement tool‑material speed‑factor table and integrate into physics engine.  
7. Add coolant, holder, machine/spindle/controller, workholding, insert axes to SFC model.  
8. Refine safety gating for aggressive modes (aggressive_rush).  
9. Consider further scaling of sweep if additional input dimensions added.  
10. Fix `controller_smoothing_factor` usage in `prism_optimized`; physics‑reviewer required.  
11. Validate +67 %–+91 % fz force envelope; physics‑reviewer audit.  
12. Run live G‑Wizard/HSMAdvisor calculators and export speeds for full comparison (operator action).  
13. Finalize triage plan for residual inert axes (`controller_features`, `radial_pct`, `axial_depth`) with effect‑aware regime logic.  
14. Expand baseline to additional non‑carbide materials once catalog‑OCR passes.
