# database-expansion session d0133a03 (2026-06-09, 37.1MB, spine 364KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit 23ad2cdf3e: HONESTY RULES block in CLAUDE.md, tournament‑rank.mjs, intake‑quarantine‑guard.mjs, save‑workflow.md; ULTRACODE‑SYNERGY‑MS0 spec with India/Sierra cross‑slot.  
- GRPO (GroupRelativeRewardNormalizerEngine) built & wired to AI dispatcher; optional `group_advantage` added to policy ledger schemas; tests 20/20 engine +5 dispatcher E2E.  
- Fleet‑Reaper/hygiene cluster restored: re‑enabled 12 tasks, durable task Ready; health watch CRITICAL→WARN (34/47).  
- GRPO Order 3 commits `037e3ac930`, `3fa529432f`; RULER Order 4 (`rankTrajectories`) commit `46553bb74a`.  
- Fact‑checker agent file created in `.claude/agents/fact-checker.md` (git‑ignored).  
- Spec drift fix commit `accf6f247f` marks Orders 3–5 SHIPPED.  
- Doc‑ignore for Docustrata corpus commit `1deb6ff521`.  
- Lock‑orphan‑leak cleanup commit `50f598afcf`; config & reaper fixes (keep_alive, max_loaded, reaper default) commit `cebde4fd94`.  
- GWizardToolCribExportEngine: 2 commits, 13/13 round‑trip tests, scrutiny 2/2 PASS; wired to `prism_calc:gwizard_export_toolcrib`.  
- WSL Memory Guard registered (installer token fixed).  
- MCP server restarted; crash‑loop documented and restored.  
- FusionMachineLibraryExportEngine commits `44c41ee643`, `a6d80537cf`: 1082 machines, XML round‑trip verified vs Autodesk goldens.  
- Tool‑catalog cap lift for HyperMill & Mastercam exporters commit `6064ace7a4`; removed silent 5 000‑tool limit → ~74 K catalog with `max_tools`.  
- HolderSelectionEngine commit `4ab181a78d`: loads 643 branded holders (HAIMER 489, BIG Daishowa 131, GUHRING 23).  
- Fusion tool exporter wired to real holders commit `82ca289ef4`; Mastercam tool exporter wired to real holders commit `7783cbef3b`.  

**DECISIONS**  
- Fleet‑Reaper ownership moved from alpha → golf; doctrine non‑skippable via `golf-slot-reaper-guardian.mjs`.  
- Disable legacy `golf-slot-write-allowlist` hook (`PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1`).  
- Adopt ultracode workflow: fan‑out‑and‑synthesize + adversarial‑verify + build‑plan synthesis.  
- Do not build cross‑slot India/Sierra engines directly; deliver ready specs instead.  
- GRPO engine chosen as highest‑leverage unit; added `group_advantage` field to ledger schema.  
- Set Ollama keep_alive to 30 m, max_loaded to 4 for 96 GB Blackwell box.  
- Shut down idle Docker/WSL (`wsl --shutdown`) to reduce commit bloat.  
- Register MCP Priority Guardian to prevent priority inversion; auto‑restart only on manual trigger.  
- Separate pure core `HolderSelectionEngine` from exporter wiring; synthetic fallback only when no catalog match.  
- CRLF→LF normalization on large files for repo consistency (non‑semantic).  
- Use local LLMs (`qwen2.5-coder:32b`, `gpt-oss:120b`) sequentially for scrutiny to avoid GPU contention.  
- Do not fabricate a holder‑only format; rely on proven tool‑library CSV with holder columns.  
- Implement hyperMILL holder wiring by injecting shank/taper into `buildNCTool` and calling `holderSelectionEngine.select()`.  

**OPERATOR DIRECTIVES**  
- “Make sure fleet reaper is running and monitor it often.”  
- “Finish all remaining tasks.”  
- `/checkin-golf – slot‑locked /checkin + fleet‑reaper owner`.  
- Bare “build” command.  
- `/checkin‑romeo` with `/loop [5m] /goal …` – resume tool/database integration loop.  
- `build`, `continue`, `apply all fixes and look for more gap fills`, `go back git tree work`.  
- “go” – proceed to Mastercam holder wiring after Fusion priority.  
- “All tool holder, tooling, inserts and machine databases are added to fusion, hypermill, mastercam, hsmadvisor, gwizard, prism sfc, mill and lathe wizards apps and galaxies.”  
- “Utilize highest‑end local LLMs (gpt‑oss:120b, qwen2.5‑coder:32b) for efficiency.”  
- “Populate fields for models in fusion, hypermill and mastercam for all tool holders and tooling.”  

**FINDINGS/BUGS**  
- Bulk‑disable of 46/47 PRISM scheduled tasks; restored hygiene cluster.  
- Tournament rank logic bug (seed bias) fixed by correct champion recording.  
- Test harness `node -e` quoting issue resolved.  
- `makeInput` helper dropped `group_advantage`; now forwards it.  
- Health‑watch CRITICAL→WARN after re‑enable.  
- Ollama keep_alive –1 triggered memory‑pressure gate; changed to 30 m, max_loaded 4.  
- WSL commit bloat (24 GB vmmemWSL) resolved with shutdown.  
- Index lock dropped 6 files on first GRPO commit; re‑staged in second commit.  
- Orphan `node.exe` process killed.  
- MCP server occasionally ECONNREFUSED; Guardian adjusts priorities but does not restart automatically.  
- Reaper cluster missing tasks (MCP Priority Guardian) registered.  
- gpt‑oss:120b pull stalled at 99 % manifest stuck; relaunched detached to finish.  
- HSMAdvisor adapter reads only single `<Tool>` block → cannot round‑trip‑verify bulk export.  
- MCP server crash‑loop documented and restored.  
- WSL Memory Guard installer failed due to missing token; fixed.  
- Deepseek pull stalled at manifest fetch; resolved by daemon restart (~1 MB/s).  
- HyperMill & Mastercam exporters capped at 5 000 tools; limit raised to 100 000 with `max_tools`.  
- CRLF→LF normalization caused large diff artifacts; addressed by ignoring whitespace in scrutiny.  

**DOMAIN SPECIFICS**  
- **Engines/Actions**: `GroupRelativeRewardNormalizerEngine`, `MultiModelConsensusEngine` (`rankTrajectories`), `GWizardToolCribExportEngine`, `FusionMachineLibraryExportEngine`, `HSMAdvisorSettingsExportEngine`, `HolderSelectionEngine`, `FusionToolExportEngine`, `MastercamToolExportEngine`, `HyperMillToolExportEngine`, `UltimateSpeedFeedEngine`.  
- **Dispatchers**: `prism_ai` (`group_normalize_reward`, `rank_trajectories`), `prism_calc` (`gwizard_export_toolcrib`), `prism_cam` (`fusion_export_machine_library`, `mastercam_tool_export`).  
- **Metrics/Paths**: reaper sweep logs, memory‑pressure counters, MCP priority adjustments, health‑watch (34/47 WARN), round‑trip verification logs for Fusion machine library and tool exporters.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `fleet-reaper-sweep.mjs`, `slot-bind-enforce.mjs`.  
- PreToolUse hooks (`golf-slot-write-allowlist.mjs`, `intake-quarantine-guard.mjs`).  
- Settings.json editing via node splice.  
- Playwright for ultracode source reading.  
- `/forge7` skill for synergy analysis and build‑plan generation.  
- `/save-workflow.md` skill to bridge workflows → skills.  
- Node scripts (`scripts/fleet-reaper-sweep.mjs`, `helpers/chat-slots.mjs`).  
- PRISM CLI commands (`/checkin-golf`, `/build`, `/checkin‑romeo`).  
- TypeScript compiler (`tsc`) and Jest test runner.  
- WSL CLI (`wsl --shutdown`).  
- esbuild, vitest, tsx for build utilities.  

**OPEN THREADS**  
- Build cross‑slot India/Sierra units (GRPO consumer, fact‑checker agent).  
- Enable operator‑scope tasks: Blueprint OCR Batch, Brain Refresh, NN‑Graph Retrain, etc. (currently disabled).  
- Verify final health‑watch metrics after re‑enable; ensure no lingering stale tasks.  
- 2,733 unpushed commits pending coordinated push.  
- MCP server restart requirement if it ever goes down; Guardian only adjusts priorities.  
- gpt‑oss:120b pull remains in detached “finish manifest” state; will complete on next run.  
- HSMAdvisor library export – confirm existence of a reader for `settings_v2.xml` or design one before building exporter.  
- MCP crash‑loop root cause (OOM tuning) – owner investigation needed.  
- Verify remaining gaps in SFC, Mill Wizard, Lathe Wizard tool catalog wiring completeness.  
- Network throughput for large model pulls (Deepseek, llama70b) – consider bandwidth/proxy adjustments.  
- HyperMill holder wiring implementation (inject shank/taper, call `holderSelectionEngine.select()`).  
- Populate Unit 3 organized tooling libraries (`material → type → brand`) with pre‑optimized SFM presets via `UltimateSpeedFeedEngine`.  
- Await mapping workflow completion before committing remaining changes.
