# quality session 928a8226 (2026-06-10, 68.1MB, spine 676KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- c593b096fb – resilient Ollama pull script fixed em‑dash parse bug; pulled gpt‑oss 120b wired to cost‑router.  
- 13017de764 – hookify frontmatter fixer + dangling hook reference remover (110 malformed hookify.*.local.md).  
- 29f2d177f6 – node‑by‑node synergy gap‑map for all 14 surfaces, ranked gaps and owners.  
- 8550436c8a – CAG‑router label corrected; removed phantom qdrant://prism‑memory reference; now points to prism_engines, prism_skills, prism_formulas.  
- c9fe32d8b2 – embedder OOM & context‑window bug fixed (heap bump + 2000‑char clamp); wiki→tribal embedding driver launched.  
- 182788232a – cap‑safe loader for tribal index; restored PSN leg #5.  
- a3e6d3ca97 – added fail‑loud parse guard, shrink‑guard; brain restored to ~4 k entries (10/10 tests).  
- 5467482be4 + c994bf3aad – documented data‑loss incident in CLAUDE.md, wiki lesson, memory.  
- fb314a6fd1 – Docker‑service‑health‑check CLI; restored Qdrant & Docker health.  
- 14b8b383a2 – wired docker‑service‑health‑stop hook into Stop chain.  
- 35e01ca5f3 – singleton‑service‑guard CLI (reap‑only logic).  
- e2081e0780 + d22681f5d2 – upgraded guard to reap + start, added startService; tests pass; MCP recovery “--fix” reap‑then‑start.  
- ed6662f45e – slash‑agnostic regex for mcp‑daemon cmdline (U‑MCP‑CMDMATCH‑FIX).  
- 29fb555f13 – stop_on_hook_unregistration now emits JSON, stops false “NOT evaluated” warnings (U‑HOOK‑UNREG‑PROTOCOL‑FIX).  
- 6ca2f6afd2 – ensures all MCP spawn paths use ≥4096 MB heap; fixes recurring boot‑OOM (U‑MCP‑BOOT‑HEAP‑FLOOR).  
- 4141daf9d8 – migration‑freeze marker partitions ~47 tasks, restores cry‑wolf warning to 2 genuine signals.  
- U-QP-COST-BRIDGE-TRUTH – cost‑bridge hooks routed via cost‑bridge-dispatch.mjs.  
- U-QP-AWARENESS-GEN-ROUTER‑AWARE – restored quoting awareness generator with router‑aware wiring check.  
- U-QP-COST-SAVINGS-TEST – injected storePath, added 13 Vitest cases covering all formulas and persistence.  
- U-QP-WIRE‑EXEMPT‑WEDM – added WIRE‑EXEMPT tag to WEDMQuoteBridgeEngine.  
- U-BW-DISPATCHER-SCAN – repointed cascade defaults to live Blackwell models; closed source‑lock blind spot; validated against /api/tags.  
- U-BW-DISPATCHER-SCAN-WIKI – wiki lesson documenting guard‑blind‑spot root cause.  
- U-PSN-SNAPSHOT-REFRESH – regenerated stale PSN snapshot (51 k nodes); confirmed 11 real P0 underwired pairs; scoped fix to Sierra’s cross‑substrate lane.  
- U-QP-DIGITALTWIN-TEST – added R9 reference‑value test for DigitalTwinEstimator; all 5 quoting algorithms now covered.  
- U-QP-MARGIN-FLOOR-GATE – margin‑floor guard in QuoteEstimatorEngine, sourced from ShopConfigurationEngine (default 20 %).  
- U-QP-MARGIN-FLOOR-SHOPCONFIG – extended ShopProfile with optional margin_floor_pct defaulted to 20 %.  
- U-QP-NRE-AMORTIZE-TEST-FIX – corrected stale NRE amortization test.  
- ec597dbcb3 – margin‑floor dispatcher round‑trip (7/7 tests).  
- 87d5c4bf9a – margin‑floor UI banner on QuoteBuilderPage (6/6, tsc‑clean).  
- 4b0980c56a – AI‑substrate routing matrix coverage 4/5→5/5 + completeness invariant (27/27 tests).  
- 62b114cb7b – web‑test ResizeObserver polyfill; unmasked hidden frontend failures (13/13 quote‑pages green, tsc clean).  
- 44fab1be37 – fleet synergy‑goal dispatch map (state/shared/SYNERGY‑GOAL‑STATUS‑2026‑06‑09.md).  
- 4c12a75a8d – U-QP-CLOSED‑LOOP‑PROVENANCE‑GATE – blocks synthetic/placeholder outcomes from promoting calibration factors.  
- d294957c4d – U-QP-OUTBOUND-PROMOTE-GATE – real outbound sold‑price distribution alignment gate (against:"line").  
- bf10035ec0 – U-QP-CALIBRATION-FRESHNESS-PREFLIGHT – warns on >24 h stale factor, optional hard cutoff.  

**DECISIONS**  
- Adopt systematic debugging: root‑cause first, then pattern analysis before patching.  
- Convert all malformed hookify files; ensure ASCII safety for 28 non‑ASCII files.  
- Do not wire unwired engines en masse; each belongs to a galaxy owner and may be intentionally orphaned or superseded.  
- Academy tribal routing fallback (`lima → general`) is intentional design, no fix required.  
- Use local GPU (RTX PRO 6000) with 3 resident Ollama models; keep OLLAMA_MAX_LOADED_MODELS at default 3×GPU to avoid eviction penalties.  
- Batch workflow agents in groups of ≤3 to stay within Anthropic rate limits; add fleet‑wide doctrine for future concurrency control.  
- Replace string‑based tribal index loader with buffered, streaming JSON reader to bypass V8’s 512 MiB string cap.  
- Run detached driver for wiki→tribal embedding to avoid blocking chat and allow continuous coverage growth.  
- Focus session on golf‑lane infra/system‑health surfaces only; defer per‑galaxy or peer‑owned work.  
- Adopt slash‑agnostic regex for singleton‑service‑guard cmdMatch to match forward‑slash daemon command lines.  
- Implement universal heap floor helper (ensureHeapFloor) in all MCP spawn paths (supervisor, daemon‑helper, autostart).  
- Add EXPECTED_DISABLED_TASKS partition in fleet‑task‑health to silence intentional migration freezes; leave genuine degraded tasks visible.  
- Closed‑loop learning now gated at both promote and emission steps (provenance + outbound gates).  
- Build U-QP-BLUEPRINT-OCR-BRIDGE-ADAPTER to fix blueprint→quote intake mismatch.  

**OPERATOR DIRECTIVES**  
- “Please fix whatever is causing all the hook errors.” – slice1.  
- “Continue from where you left off.” – slice1.  
- Confirm fleet reaper & monitors are running (verified). – slice2.  
- Keep session‑scoped Stop hook active until full node‑by‑node synergy condition holds. – slice2.  
- Use ultracode for planning, deep reasoning, orchestrating; Ollama for grunt work (searches, reads, coding). – slice7.  
- `/startup-charlie /loop [10m] /goal …` – focus on charlie domain, closed‑loop + production readiness. – slice8.  
- “continue with charlie domain work” (twice) – override fleet goal stop. – slice8.  

**FINDINGS/BUGS**  
- Hookify rule files: 110 missing YAML front‑matter → warnings; 1 file caused chardet codec error due to UTF‑8 emoji.  
- Dangling .mjs hook references (4) in settings.json produced Cannot find module errors.  
- Scheduled‑task warning: 3 tasks marked MISSING during hardware/drive migration freeze; no action until migration completes.  
- Unwired engines audit: 90 built engines with no dispatcher/route/hook consumer; 53 core, 23 bridge, 14 ops/test scaffolds; some intentionally orphaned (e.g., MillPrintToProgramEngine).  
- Ollama pull script parse error prevented completion of gpt‑oss:120b; ASCII fix resumes download.  
- Synergy bug: routeModelForTask still selects gpt‑oss:20b for “best” tier even though gpt‑oss:120b is installed.  
- V8 string cap overflow broke tribal index reads; fixed with buffer loader.  
- Data‑loss incident: fail‑open clobber of the index due to empty‑text guard; restored from backup.  
- Default heap OOM on large JSON parse; resolved by NODE_OPTIONS flag.  
- MCP duplicate daemon leak caused port conflicts; reaped and restarted clean daemon.  
- Qdrant container left in “Created” state; restarted correctly.  
- PSN snapshot stale (6 days) misidentified 11 P0 pairs; verified real under‑wired links.  
- ResizeObserver crash in QuoteBuilderPage caused hidden test failures; polyfill added.  
- Synthetic outcome promotion bug: synthetic outcomes promoted to live quote‑time factors; added provenance classifier to block this.  
- Server‑side rate limit blocked multi‑agent scrutiny on margin‑floor commits; deferred to next non‑rate‑limited run.  

**DOMAIN SPECIFICS**  
- Hookify rule files (`hookify.*.local.md`) use YAML front‑matter; parser in `config_loader.py`.  
- Settings.json hook arrays (SessionStart, UserPromptSubmit, PreToolUse) control fleet‑wide hooks.  
- Engine wiring: dispatcher mapping (`automationDispatcher.ts`, etc.), engine registry, unwired‑engine audit script.  
- Slot claim logic for golf slot; reaper ownership moved from alpha to golf; `golf-slot-reaper-guardian.mjs` enforces reaper run on `/checkin-golf`.  
- Ollama model routing (`ollama-cost-router.mjs`) selects models per tier and hardware; `routeModelForTask` central.  
- Tribal index loader (`load‑tribal-index.mjs`) with brace‑aware walker.  
- PSN leg #5: tribal injection via `tribal-rerank.mjs`.  
- MCP server: `TRANSPORT=http node dist/index.js`; reactive reconnect only.  
- Docker‑service‑health‑check and docker‑service‑health‑stop hooks for container health.  
- Singleton‑service‑guard CLI for orphan daemon cleanup.  
- Qdrant collections: `prism_engines`, `prism_skills`, `prism_formulas`.  
- QuoteEstimatorEngine, DigitalTwinEstimator, CostSavingsTrackerEngine; ShopConfigurationEngine (margin_floor_pct).  
- Business dispatcher (`prism_business`); source‑lock guard.  
- Front‑end QuoteBuilderPage.tsx; API types (QuoteEstimate); alert banner for margin floor.  

**TOOLS USED**  
- PRISM tooling: `CronCreate`, `ask-ollama`, workflow orchestration, test harness (`mjs` tests), `host-class`, `resolveSynthesisModel`, `cag-router`, `tribal-rerank`, embedder scripts.  
- External: Ollama CLI, Nomic‑embed-text API, Qdrant server, Docker health probes, jsdom polyfill for ResizeObserver.  
- Node modules: `octopus-setup.mjs`, `routeModelForTask` in `ollama-cost-router.mjs`.  
- PRISM scrutiny (3‑of‑3 ledger), ultracode workflow/multi‑agent, Vitest, Jest, tsc with 16 GB heap.  

**OPEN THREADS**  
- Wire unwired engines on a per‑owner basis; prioritize core engines, confirm intentional orphan status.  
- Resolve academy tribal routing if desired (add “academy” domain and corpus).  
- Finalize `gpt‑oss:120b` pull and ensure synergy selection in all tiers.  
- Monitor reaper health post‑migration; verify GPU/ollama coordination.  
- Address any remaining scheduled‑task freeze after migration completes.  
- Write‑side sharding for tribal index once it exceeds 512 MiB.  
- Re‑embed lost ~29 K entries from backup sources.  
- MCP keep‑alive scheduled task (reap + start) – already implemented; monitor stability.  
- Qdrant tribal collection migration to move brain out of JSON file – deferred until corpus rebuild.  
- Per‑galaxy synergy tasks in other slots (foxtrot/whiskey/mike/kilo/charlie/delta/lima) – pending owner action.  
- Build U-QP-BLUEPRINT-OCR-BRIDGE-ADAPTER: adapter mapping OCR → bridge, round‑trip tests, integration.  
- Operator/ERP cleanup of `jm‑sold‑orders` OCR noise to enable outbound gate live.  
- Verify ShopConfig floor propagation to all downstream consumers (frontend, API, analytics).  
- Complete Ollama routing invariant tests to ensure no regression silently sends offloadable work to Claude.  
- Final UI integration of margin‑floor banner into all quote views (pending review).  
- Continue fleet‑wide ollama‑offload audit for other galaxies once budget allows.
