# quality session c7361c9f (2026-06-10, 29.7MB, spine 227KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `ascii‑guard.mjs` – diff‑aware PreToolUse block blocks non‑ASCII smart punctuation (enforced, 3/3 reviewed).  
- `/checkin-golf` stop hook (`buildAdvisory`) stamps advisories with audit age.  
- MCP server heap floor raised to **24 GB**; watchdog RSS threshold set to **18 GB** on a 136 GB RAM machine.  
- Migration‑freeze marker removed; all 8 fleet‑reaper/monitor tasks re‑enabled.  
- Docker, WSL, Ollama, Qdrant started; NIM containers stopped (router downgrade avoided).  
- Slot‑worktree commit routing (`slot/<name>` branches) active across fleet; backlog of wiki/memories deferred to quiet‑window commit.  
- `U-MCP-CONCURRENCY-HARDEN` – MCP server hardened for unlimited parallel agents (3/3 gate cleared).  
- G5 closed redundant TTL sweep for orphan boost‑stamps (`U-GOLF-G5-CLOSE`).  
- G1 closed premise‑corrected backlog; safe slice committed, updated `.git/info/exclude`.  
- G10 crash‑critical auto‑re‑enable guard shipped (4 files, 14 tests, live‑validated).  
- G6 native Ollama stop‑advisory arm added to `docker-service-health-stop.mjs` (11/11 tests).  
- Crash‑postmortem digest script built and tested; integrated with `watch.mjs`.  
- `state/shared/golf-galaxy-completion-plan-2026-06-09.md` v2 committed.

**DECISIONS**  
- Golf slot owns fleet‑reaper, hygiene duties (doctrine moved from alpha → golf).  
- Adopt bounded 3/3 scrutiny gate to avoid fan‑out rate limits.  
- Use Ollama (`gpt‑oss:120b`) for grunt reading of golf sessions; ultracode workflows limited to ≤3 agents.  
- Slot binding enforced via `slot-bind-enforce.mjs`; `/startup-golf` forces golf slot and runs startup audit.  
- Auto‑re‑enable guard wired into `fleet-task-health-watch.mjs`’s `runOnce`, guarded by `EXPECTED_DISABLED_TASKS`.  
- RequestSemaphore caps concurrent builds at 64, bounded queue 512; watchdog defers RSS preempt during burst.  
- Native Ollama probe runs before Docker guard to avoid suppression when Docker down.  
- Freeze‑gate logic: `selectReenableTargets` only short‑circuits on `migrationFreezeActive`; only `EXPECTED_DISABLED_TASKS` controls re‑enable.

**OPERATOR DIRECTIVES**  
- `/startup-golf`: force‑take golf slot, bind handoff to `golf-work`, run startup pipeline.  
- `/compact`: reset context before executing G5→G1→G10 sequence.  
- Read three articles via ultracode + Ollama; capture insights.  
- Bypass legacy `golf-slot-write-allowlist.mjs` (Option A) to allow writes in this session.  
- Force‑take golf slot (`--force true --confirmRecent true`) and bind handoff to `golf-work`.  
- Run fleet‑reaper sweep (`fleet-reaper-sweep.mjs`) each `/checkin-golf`.

**FINDINGS/BUGS**  
- NIM containers caused router downgrade; removed.  
- Offload take‑rate 5–9 % vs target 30 %; GPU idle 5 %.  
- Docker daemon initially down – fixed.  
- Stop hook WARN stale due to migration freeze – cleared by removing marker.  
- MCP watchdog preempt restart at 3 GB RSS caused frequent disconnects – tuned to 18 GB.  
- Fresh `McpServer` per `/mcp` request creates memory spike; concurrency gap identified (needs inflight‑aware watchdog & semaphore cap).  
- RequestSemaphore leak when client disconnects while queued – fixed in `acquireRequestSlot`.  
- Active‑chat‑boost premise stale; decay already TTL‑sweeps orphan stamps.  
- G1 noise premise incorrect – 22 k untracked files genuine commit backlog, not noise.  
- Drift test #69 failed due to missing synthesis‑cron names; fixed by reverting change.  
- Index.lock contention during peer git operation resolved by waiting for lock release before committing revert.

**DOMAIN SPECIFICS**  
- Fleet: 26 chat slots (`alpha…zulu`); Golf slot owns fleet‑reaper, hygiene.  
- Hardware: NVIDIA RTX PRO 6000 Blackwell (96 GB VRAM), AMD Ryzen 9 9950X3D, 136 GB RAM.  
- Ollama local LLM stack: `gpt‑oss:120b`, `gpt‑oss:20b`, `qwen2.5-coder:1.5b/32b`.  
- NIM via `nim-docker-launcher.mjs`; router cascades to NIM first (now disabled).  
- MCP server architecture: fresh `McpServer` per request, watchdog + supervisor; semaphore cap needed.  
- Slot‑worktree commit routing (`slot/<name>` branches) active across fleet.  
- Fleet‑reaper tasks: Node Orphan Cleaner, Zombie Reaper v2, PS Orphan Reaper, Memory Pressure Auto‑Relief, Hook Janitor, Cleanup Orchestrator; slot‑aware sweep and JSONL‑tail monitor.  
- Ultracode workflow dispatcher (`wf_*`); background notification on completion.

**TOOLS USED**  
- `/checkin-golf`, `fleet-reaper-sweep.mjs`, `local-llm-bridge.mjs`, `nim-docker-launcher.mjs`, `ascii-guard.mjs`, `slot-bind-enforce.mjs`, `fleet-task-health-watch.mjs`, `docker-service-health-stop.mjs`, `watch.mjs`.  
- Ollama (`gpt‑oss:120b`, `qwen2.5-coder:32b`, `1.5b`).  
- Ultracode Workflow (multi‑lens assessment).  
- Node test framework (`node:test`).  
- Git tooling (`git log`, `git reflog`, `git status`).

**OPEN THREADS**  
- Implement inflight‑aware watchdog and semaphore cap for MCP concurrency.  
- Final integration of migration freeze removal across all fleet slots.  
- Commit backlog of wiki/memories after quiet‑window.  
- Optimize Ollama offload take‑rate toward 30 %.  
- Root cause of recurring disable of crash‑critical tasks (likely Windows auto‑disable or external script).  
- Pending G2 gpt‑oss model‑pull durability smoke‑test.  
- Remaining build‑iter‑C units: G8 eval verify, G4 worktree consolidation, G9 cron‑lock sweep.  
- Further refinement of freeze‑gate logic for other non‑load‑bearing crash‑critical scenarios.
