# quality session 8b4b9149 (2026-06-10, 48.2MB, spine 520KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5ae481f748` – EPERM leak fix in `OutcomeCaptureBusEngine`.  
- `86f0e3fe0c` – wired `TriComparator`, `ExhaustiveCombination`; added 16/16 round‑trip tests.  
- `a2dbfa76e1` – closed‑loop driver, rescued orphan SF engines, fixed consensus pollution bug.  
- `43e1b8e449` – G‑Wizard alignment symmetry P3 fix & regression test.  
- `891c66e728` – added `SpeedFeedCalibrationPersistEngine`, tests, dispatcher wiring.  
- `16d6eecef4` – spec addendum with closed‑loop training results.  
- `890b2c39e7` – memory snapshot and VC assessment update.  
- `3438987f0e` – hardening of calibration engine (clamped flag fix, ledger_rows_skipped).  
- `f31398a1a5` – `SpeedFeedGpuJudgeEngine`, driver, GPU‑based judge tests.  
- `f5d14ddb29` – GPU judge hardening (probe prefix‑match, cleanup on rename failure).  
- `951f5ac335` – GPU judge polish (single `isProbe`, zero‑judge suppression).  
- `3a1c20fca2` – full‑sweep mode (`69 120` cells), streaming API.  
- `af0ac16c5a` – removed `cells.length` ReferenceError, documented ISO‑group collapse.  
- `b847c0f1ff` – scoping spec for axis‑awareness (tool material, coolant, etc.).  
- `28eca0ed84` – doc‑reflection of GPU judge findings & safety flag.  
- `54c0ae06c8` – GPU judge layer spec addendum.  
- `658c8280fe` – canonical tool‑material speed‑factor table (safety guard).  
- `585584e3ae` – wired coolant axis to existing `CoolantVcModifier`.  
- `7d0affcae6` – inlined rigidity factor removed; moved to `CANONICAL_MACHINE_RIGIDITY_VC_FACTOR`.  
- `f998f8af71` – alts‑factor commit: tool‑material, coolant & rigidity multipliers applied.  
- `da41a58fd1` – 102 060‑combination sweep (`sfc-combination-sweep.ts`).  
- `c48829e331` – axis‑liveness probe (Vc/RPM/feed/MRR).  
- `d03458fff1` – extended probe to include `tool_life`.  
- `2070c472a4` – added `ClampingForceEngine` (workholding clamp‑cap).  
- `7dc7798033` – wired spindle‑power clamp (`hp → power`).  
- `8b191d5427` – runout→tool_life link applied.  
- `a257f872bb` – complete axis‑liveness map (adds toolpath.strategy, cut_type, mode).  
- `81e37ece16` – numeric finish‑Ra cap (`target_ra_um`).  
- `6a3ad56545` – 576 tool‑material sweep (`U‑OSC‑COMPARE‑TOOLMATERIAL‑VARIABILITY`).  
- `c78faa5a73` – HSS baseline (54 points).  
- `835df42c74` – ceramic/CBN baseline.  
- `4c544db4ae` – explicit G‑Wizard/HSMAdvisor deltas (`U‑OSC‑COMPARE‑PER‑VENDOR`).  
- `08d7fc6d37` – all‑axis sweep (OAT×2 regimes, 16/25 axes live).  
- `93de130f59` – all‑axis sweep with tool‑life added (18/25 axes live).  
- `907e74acab` – fixed HSS cast‑iron & CBN over‑speed (+108% → –23%, +49%).

---

**DECISIONS**  
- Backend‑first: fix data‑spine leak, wire orphan engines, then run closed‑loop comparison (PRISM SFC vs G‑Wizard/HSMAdvisor).  
- Consensus logic: exclude not‑aligned externals unless better aligned data exists; keep misaligned advisory visible but excluded otherwise.  
- Add alignment check to G‑Wizard mirroring HSMAdvisor.  
- Persist calibration model from sweep ledger (advisory‑only, safety‑gated).  
- Defer GPU training until memory pressure cleared; use Blackwell RTX 6000 for future apply layer.  
- Incremental axis‑awareness: tool material → coolant → rigidity‑Vc → alts‑factor, each verified by gauntlet/variability tests.  
- Safety gate: all Vc‑changing edits must pass physics‑reviewer and S(x)≥0.70 before commit.  
- Default controller influence remains product decision; no code change yet.  
- Use existing `CoolantVcModifier` instead of duplicating a table; de‑inline hardcoded rigidity factor.  
- Add `baseline_detail` field to tri‑comparator for per‑source deltas.  
- Extend `BaselineSource` enum to include Tungaloy/Iscar/NTK for ceramic/CBN.

---

**OPERATOR DIRECTIVES**  
- Deliver VC‑grade assessment report (`SFC‑VC‑ASSESSMENT‑2026‑06‑08.md`).  
- Resume closed‑loop testing with live vendor data (G‑Wizard crib, HSMAdvisor baseline).  
- Decide on auth provider (Clerk / Supabase / Auth0) and ship webhook signature fix if chosen.  
- Populate G‑Wizard crib with computed S/F per tool via closed Adobe‑AIR app.  
- Run Blackwell `qwen2.5‑vl` OCR batch over vendor catalog PDFs (Kennametal, etc.).  
- Run all‑axis sweep (machine/spindle/controller/… axes live) and compare results with maximum variability against G‑Wizard/HSMAdvisor.  
- Use strongest Ollama models for grunt work; auto‑fix any bug found during a task immediately.

---

**FINDINGS/BUGS**  
- EPERM leak caused ledger writes to fail; resolved by atomic append + retry.  
- Unwired engines (`TriComparator`, `ExhaustiveCombination`, `DownstreamSubscriber`) now wired and tested.  
- G‑Wizard crib contains 41 k tools but zero proven sfm values → comparator abstains.  
- HSMAdvisor returns constant 634.6 m/min for all materials; engine flags as unaligned advisory but still includes it in consensus median (bug).  
- Consensus computation incorrectly incorporates not‑aligned systems; needs correction.  
- Missing N/H coverage in exhaustive sweep; only P/M/K/S present.  
- Aluminum 6061 misclassified under ISO group M (stainless); misclassification bug.  
- G‑Wizard alignment check used wrong field, flagged all tools as unaligned.  
- Seven SF engines orphaned in git.  
- Consensus folded in unaligned HSMAdvisor advisory causing impossible titanium consensus.  
- Tool‑holder type runout inert on tool_life; must be wired.  
- Spindle.hp, workholding.type, controller inert by design or physics review.  
- Controller smoothing factor only applies in aggressive_rush mode; default remains controller‑agnostic.  
- PRISM fz uniformly +67–91 % higher than all published sources (Sandvik/Kennametal/CNCCookbook/HSMAdvisor).  
- HSS cast‑iron over‑speed (+108%) and CBN over‑speed (+49%).  
- Dead axes in sweep: machine_accuracy, controller_brand, controller_features, spindle_thru, tool_holder_type, target_ra, radial_pct.  
- Wiring gaps: `controller_smoothing_factor` applied only in aggressive_rush; missing in prism_optimized.  
- Optimizer internalized radial_pct/axial_depth (affecting MRR but not headline metrics).  

---

**DOMAIN SPECIFICS**  
- Engines: `SpeedFeedCalibrationPersistEngine`, `SpeedFeedGpuJudgeEngine`, `UltimateSpeedFeedEngine`, `SpeedFeedNineAxisOrchestratorEngine`, `ClampingForceEngine`.  
- Actions/dispatchers: `speed_feed_calibration_persist`, `speed_feed_gpu_judge`, `sfc-full-sweep-compare.mjs`, `sfc-all-axis-sweep.mjs`.  
- Metrics: Vc (mm/min), feed_rate, MRR, tool_life_min, per‑source deltas (`vc_variance_pct`, `fz_variance_pct`), `ledger_rows_skipped`, `gpu_resident`, `vram_used_mib`.  
- Paths: `state/outcomes/sfc-full-sweep-ledger.jsonl`, `state/outcomes/sfc-calibration-model.json`, `resources/MANUFACTURER_CATALOGS/`, `catalog‑sfc‑extraction‑manifest.json`.  

---

**TOOLS USED**  
- PRISM internal tools: `/hermes-workflow`, `/forge-audit-v2`, `ultracode`, `checkin-oscar`.  
- Node 22 + tsx, Vitest test suites.  
- Ollama (`qwen2.5-coder:32b`, `qwen2.5‑vl`).  
- NVIDIA tools: `nvidia-smi` for VRAM usage.  
- Git hooks (scrutiny script, stop hook).  
- CronCreate/CronDelete for recurring goals.  
- VSCode/Obsidian memory sync.  

---

**OPEN THREADS**  
- Finalize closed‑loop comparison with live vendor data and produce validated result.  
- Fix consensus computation to exclude not‑aligned systems.  
- Decide on auth provider (Clerk / Supabase / Auth0) and ship webhook signature fix if chosen.  
- Complete VC report presentation for investor outreach.  
- Implement `PRISM_SFC_CALIB_APPLY` keystone to apply persisted calibration factors with safety gating.  
- Run GPU‑based training on Blackwell to refine model from sweep ledger.  
- Validate that N/H baseline data is now included and correct.  
- Document G‑Wizard geometry‑only limitation; no speed/feed baseline available.  
- Add tool‑material speed factor to the SFC physics (root‑cause fix).  
- Incorporate coolant type effect on Vc/feed.  
- Model machine/spindle/controller/workholding rigidity/damping effects.  
- Include insert geometry impact on cutting parameters.  
- Validate new axes against vendor baselines and extend GPU judge for larger sweeps.  
- Fix wiring gaps: `controller_features`, `spindle_thru`, partial fix for `tool_holder_type`.  
- Sweep‑widen target_ra and other cap‑binding axes.  
- Resolve optimizer internalization of radial_pct/axial_depth (add tool_life effect).  
- Physics‑reviewer validation of PRISM fz‑force envelope (+67–91 % over‑feed).  
- Live G‑Wizard/HSMAdvisor number export (operator‑gated).  
- Vision‑OCR batch to extract speed/feed tables from high‑priority vendor catalogs.  
- MRR reconcile (`U-OSC-MRR-RECONCILE`).  
- Rigidity‑DOC integration (`U-OSC-RIGIDITY-DOC`).  
- Remaining inert axes: holder/spindle connection, controller, workholding, insert.  
- Full combinatorial sweep against live G‑Wizard and HSMAdvisor data (once vendor catalogs are calibrated).
