# token-optimization session 0e5669d2 (2026-06-09, 38.7MB, spine 321KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `obsidian-memory-sync.mjs` rewritten: `writeWithRetry`, `ensureDir`; commit 168c202646.  
- `WikiIndexMaintainerEngine.formatIndex.refresh()` added; commit ea21008f25.  
- PowerShell cron installers (`promote‑memory‑to‑wiki.ps1`, `vault‑rot‑sentinel.ps1`); commit 8c4dff660a.  
- Link‑heal scorer hardened & auto‑apply gating; commit 984313825e.  
- Tribal coverage audit fixed (31.5 % → 83.7 %); commit 838123429b.  
- Persisted full build mirror (C: & H:); commit 45225e0cc3.  
- Audit map & node‑access wiki written; commits da013c4406, 9e4376b3b2.  
- `fleet-task-health-watch.mjs`: added `EXPECTED_UNREGISTERED_TASKS`, WSL memory guard; 62/62 tests (commit 3d796dcf5c).  
- `vault-backlink-schema.mjs` & staleness flag (`backlinksFor`); 17/14 tests (commits 96ed5222e2, d856173b86).  
- `regen-viz.mjs`: auto‑refresh `vault-backlinks.json`; commit 0e2724871a.  
- `doc_nodes` dispatcher/action/schema; 15/15 tests (commit 14aba14e3a + 4a44b5393c).  
- `canvas-read-lib.mjs` CLI subcommands; 15/15 tests (commit 2d49bf0d33).  
- Updated access‑map for `.canvas`, stale memory‑rag‑inject claim (commit 47fe91554f).  
- `U‑VIZ‑WIKI‑NARRATIVE`: flag‑gated Ollama “what/why” generator using `generateBlurb()`; commit cd54edb940.  
- `U‑OLLAMA‑PIPELINE‑INJECTOR‑WIRE` wired into `settings.json`; commit 7ec4a5ea02.  
- `U‑OLLAMA‑PREWARM‑WIRE`: prewarm qwen2.5‑coder:32b; commit 65a29220e5.  
- `U‑TRIBAL‑QA‑LLM‑UNBLOCK` added gated LLM Q‑A path to `distill‑tribal.mjs`; commit 6fb278a2ee.

**DECISIONS**  
- Use `/checkin-sierra` wrapper → slot binding before canonical `/checkin`.  
- Adopt “verify‑before‑build” workflow: agent reads live code, writes change only after verification (R8).  
- Commit only unit‑affected files; bypass lane guard (`PRISM_GIT_ADD_LANE_DISABLE=1`) and use `CLAUDE_SESSION_ID` to avoid cross‑chat index contention.  
- Persist all artifacts in Sierra domain: `system-viz/MEMORY.md`, wiki entry, C: memories auto‑feed H: vault, handoff file.  
- Full‑corpus candidate regeneration OOM → ship disarmed 100‑link file with `processed/totalBroken`; flag coverage limit.  
- Set `PRISM_HOOK_HEAP_MB=384` via portable‑node wrappers (10.7× commit reduction).  
- Raise MCP server `maxConnections` to 512 (`mcp-server/src/index.ts`).  
- Add `pressure` status in fleet‑task‑health to avoid false “MCP failing” alarms.  
- Widen `Ollama OFFLOADABLE_PATTERNS` (lint, classify, docstring, diff summary, triage) → GPU offload from ~8 %→30 %.  
- Introduce `EXPECTED_UNREGISTERED_TASKS` for vault crons; treat as informational.  
- Keep migration freeze ON; defer MCP restart & vault cron registration until lift.  
- Wire Ollama injector first, then prewarm; default to qwen2.5‑coder:32b (reject gpt‑oss models).  
- Flag‑gate post‑pass wiki narrative generator; exclude `layer-stack-overview.md`.  

**OPERATOR DIRECTIVES**  
- “Fill all gaps” – complete every listed B/P1/P2 gap.  
- Build and upgrade MCP server to use new CPU, RAM, GPU, NVMe SSD; run full fleet without failure.  
- Continue work; autonomous loop `/loop [10m] /goal [finish all ollama synergy tasks … ]`.  
- Ensure Obsidian vault built & operational (all X articles).  
- Persist all context for Sierra domain; do not fabricate content.  
- Use ultracode to fetch new X/Twitter article verbatim.  
- `/startup-sierra` – force‑take slot, bind handoff to `sierra-work`, run standard `/startup`.  

**FINDINGS/BUGS**  
- `obsidian-memory-sync.mjs`: unguarded `writeFileSync`; data loss on first throw.  
- `index.md` frontmatter stale (`last_verified: 2026‑05‑08`, 770 entries).  
- Scorer over‑confidence: prefix/substring ≥0.85 auto‑apply; decay leak >0.85 for long slugs.  
- Stale candidates JSON (`autoApplyCount: 14100`) after partial regeneration.  
- Full‑corpus candidate run OOM (~800M `scoreCandidate` calls).  
- Over‑commit: 84 hook processes ×4 GB → ~210 GB commit charge.  
- False positive MCP failure alarm (HRESULT 0x800710E0, transient spawn refusal).  
- Low GPU offload ratio due to narrow OFFLOADABLE_PATTERNS.  
- Vault cron “missing” banner noise; resolved with `EXPECTED_UNREGISTERED_TASKS`.  
- Shared‑index race: changes committed under peer commit (`43e1b8e449`).  
- Commit‑pressure: vmmemWSL balloon (95 GB) not respecting `.wslconfig`; auto‑reclaim fixes.  
- Staleness flag needed for reverse edge & canvas reader to detect drift.  
- Lgit layer regex missed alphabetic suffix; fixed.  
- Peer contamination risk with git amend on shared tree; resolved by preserving history.  
- Duplicate guard (`ng()`) throws on duplicate; `SemanticSimilarityGuardEngine.cosineCheck()` works.  
- gpt‑oss:20b/120b return empty `.response`; cold‑load >30 s → rejected for prose generation.  
- Prewarm hook safe (advisory, never blocks).  
- Injector wiring safe (fail‑open, keyword‑gated).  
- Layer‑stack‑overview collision detected; excluded from narrative injection.  
- Dry‑run now skips Ollama calls and returns counts only.

**DOMAIN SPECIFICS**  
- **Engines/Actions**: `WikiIndexMaintainerEngine`, `obsidian-memory-sync.mjs`, `node-card-prefetch-inject.mjs`, `memory-rag-inject.mjs`, `fleet-task-health-watch.mjs`, `regen-viz.mjs`, `doc_nodes` dispatcher, `canvas-read-lib.mjs`, `SemanticSimilarityGuardEngine.cosineCheck()`, `PRISMSelfAwarenessEngine.searchAIFeatures`.  
- **Dispatchers**: PowerShell cron installers (`promote‑memory‑to‑wiki.ps1`, `vault‑rot‑sentinel.ps1`), `node_card`, `U‑OLLAMA‑PIPELINE‑INJECTOR‑WIRE`, `U‑TRIBAL‑QA‑LLM‑UNBLOCK`.  
- **Metrics/Paths**: node‑access map (`obsidian-vault-node-access-map.md`), memory recall hooks, `system-viz/MEMORY.md`, commit charge %, pagefile distribution (C/G/J/L drives), GPU VRAM (~96 GB idle), offload ratio 8 %→30 %.  
- **Unique Paths**: `/checkin-sierra`, `/startup-sierra`, `C:/Users/wompu/.claude/settings.json` (injector & prewarm), `H:/prism/.claude/settings.json` mirror, `mcp-server/src/index.ts`, `portable-node` wrapper (`H:/claude/bin/portable-node`).  

**TOOLS USED**  
- PRISM CLI helpers (`chat-slots.mjs`, `slot‑claim`, `reclaim`), lane guard & pre‑commit hook.  
- Ultracode workflow (6 agents, 90 tool uses).  
- Node.js test harness (`node:test`) and Vitest for scorer tests.  
- Portable‑node wrappers, MCP server, fleet-task-health, Ollama offload engine.  
- `ai‑duplication‑guard.mjs`, `ollama-prewarm-on-pipeline.mjs`, `ollama-pipeline-injector.mjs`.  
- LLM call helper (`ask-ollama.mjs` + `resolveSynthesisModel`).  
- Narrative generator (`contextual-blurb.mjs`).  
- Wiki regeneration pipeline (`regen-wiki-from-viz.mjs`).  
- Canvas reader (`canvas-read-lib.mjs`).  
- Scrutiny 3‑of‑3 (`scrutiny-3way.mjs`).  

**OPEN THREADS**  
- Short‑token Levenshtein auto‑apply eligibility (min‑length floor or tie‑veto).  
- Full‑corpus candidate regeneration / coverageComplete marker (streaming write or larger heap).  
- Restart MCP daemon to activate `maxConnections=512`.  
- Register vault crons if migration freeze lifted; monitor GPU offload ratio.  
- Implement `prism_session:canvas_nodes` dispatcher mirror.  
- Finalize U‑OLLAMA‑PIPELINE‑INJECTOR‑WIRE wiring & settings entry.  
- Weekly synth resolver patch (best model import).  
- Gap A: emit per‑file node cards (~3,300 missing files).  
- Gap B: add lazy semantic reranker after BM25.  
- Memo‑extract throttle fix (per‑session rate limiting).  
- Complete Wiki‑NLI lint & CLAUDE.md doc‑fix.  
- Potential OLLAMA‑FORGE‑ASSIST unit (scaffold generation of skill/script/hook triples).
