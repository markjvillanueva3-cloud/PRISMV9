# token-optimization session a198ff5f (2026-05-29, 53.3MB, spine 280KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `LAUNCH‑PRISM‑FLEET.bat` rewritten – uses `pwsh.exe`, no `;` collision, calls `slot-tab-boot.ps1`.  
- `Launch-PRISM-Fleet.ps1`: 4 windows × 6 tabs (24 slots) in 2×2 quadrant layout.  
- `slot‑tab‑boot.ps1`: now spawns `claude.exe`, tier‑resolved resume logic, anti‑compact env vars (`CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=99`).  
- Regenerator (`scripts/regenerate-launch-fleet.mjs`) emits per‑tab `-File slot‑tab-boot.ps1 -Slot X`.  
- OneDrive Desktop shortcut fixed – local `.lnk` points to updated bat.  
- 4 new galaxy dirs (romeo, uniform, victor, xray); all 26 slots now have full galaxy (`CLAUDE.md`, `MEMORY.md`, `PATHS.md`, `TOOLBELT.md`, skills/scripts/hooks).  
- Auto‑capture Stop hook wired before `stop-obsidian-memory-feed.mjs`.  
- India meta‑bus integration: 10 consumer galaxy `CLAUDE.md` files appended with closed‑loop section.  
- `outcome-bus-auto-tap.mjs` (India’s first ship) committed (`18ca66fb61`).  
- System‑viz graph regenerated; master index updated (`state/shared/master-index.json`).  
- HP status line fixed to use sidecar token counts.  
- Commits: `1dbda26868` (/ready, waitForReady, MP‑5h quota), `ccd1d9f82b` (HP bar context‑aware), `92c55ee62f` (11‑galaxy India wiring), `288de9a63b` (India doctrine to 20 slots).  
- Rebuilt MCP server (`4bf2df6a1d`); Opus 4.8 launcher wired; Ollama offload fixed.  
- Built MCP server (`npm run build`) and restarted supervisor.  
- `MASTER-BRAIN-TEMPLATE.md` + generator edits committed (FAIL 12/13 gates) – alpha owns canonical Obsidian brain.  
- Dropped per‑chat `prism_safe`; fleet runs single shared `:3100` MCP backend.  
- Tier‑1 domain filter (`mcp-tool-domains.mjs`) with fail‑open fallback and CWD‑self‑resolution added to bridge.  
- Write‑time routing by slot for per‑galaxy memory via `obsidian-memory-sync.mjs`; created `knowledge/memories/<galaxy>/` namespace; `reconcileGalaxies()` cleanup logic.  
- U‑MCP‑ROLLOUT activation code committed (`df8688d1a2`).  
- MCP activation PASS (24 tests, fail‑open).  
- Per‑galaxy memory sync PASS (797 synced / 93 routed / 0 errors).  
- 3‑of‑3 gate: Arm A/B/C PASS; marks recorded under `claude‑71645850`.

**DECISIONS**  
- Use `claude.exe` directly to avoid `0x80070002`.  
- Create local Desktop shortcut for OneDrive redirect.  
- Regenerator must emit per‑tab `-File`; ensures Windows Terminal parsing.  
- Galaxy buildout self‑bootstraps: each slot checks for its galaxy on first launch and runs 11‑step buildout if missing.  
- Parallel agents review work; findings merged into single commit per slot.  
- Master index / system‑viz are canonical code‑base search sources; all slots reference them.  
- Keep single‑master MCP server; add `/ready` + `waitForReady()` to eliminate cold‑start race.  
- Fleet‑reaper sole watchdog for all PIDs (no per‑chat supervisors).  
- Activate Opus 4.8 on every slot launch (fallback 4.7).  
- Wire India meta‑bus doctrine to all non‑hygiene, non‑unallocated slots (20 total).  
- Adopt dynamic‑workflow “workflow” keyword for transient fan‑out tasks; keep PRISM slots for sustained domain ownership.  
- Alpha sole owner of master brain; drop identical `prism_safe`.  
- Use Tier‑1 filter (env‑driven) instead of 4 separate MCP servers; keeps context window small while preserving fail‑open safety.  
- Production activation via `$PRISM_BOOT_SLOT` set by `slot-tab-boot.ps1`; zero SPOF change.  
- Per‑galaxy memory routing at write time using slot’s `slot:` frontmatter; classifier abandoned.  
- Codify heavy‑workload rule: synchronous octopus consensus (`prism_ai consensus_decide`) as advisory only; never block Stop.  
- Apply `suppressApplicationTitle:true` in WT profiles to preserve slot titles.  
- Change `/checkin-$Slot` from bare slash command to natural‑language prompt (“Run /checkin‑$Slot …”) so Claude’s skill tool handles args.  
- Consider switching WT defaultProfile from PowerShell 5.1 to PS7 for fallback safety.

**OPERATOR DIRECTIVES**  
- “Fix launch‑prism‑fleet.bat on my desktop.”  
- “Use parallel agents to review your work.”  
- “Make each chat slot auto‑build its galaxy on fleet launch.”  
- “Ensure every slot has memory, wiki, tribal knowledge, custom CLAUDE.md, obsidian brain feed, master graph/index, skills/scripts/hooks, efficient tool calls.”  
- “Add MP bar redesign (choose metric) and HP context fix.”  
- “Do one final assessment on the per‑chat galaxy pipeline.”  
- “Fill all gaps and fixes before launching fleet.”  
- “Use forge commands / slash commands, automate when needed.”  
- “Upgrade forge‑audit to v3.”  
- “Run octopus cross‑vendor pass on the two commits (rate‑limit noted).”  
- “Codify rule into CLAUDE.md + memory.”  
- “Fix UI to show chat names by enabling `suppressApplicationTitle`.”  
- “Relaunch fleet after title fix; optionally flip WT defaultProfile to PS7.”

**FINDINGS/BUGS**  
- `0x80070002` from `.cmd` wrapper – solved by calling `claude.exe`.  
- OneDrive Desktop redirect caused missing shortcut – fixed.  
- Regenerator had `;` collision – patched.  
- Duplicate resume race on live alpha – liveness guard now checks JSONL mtime in addition to PID.  
- Tier‑1.5 scan for reaped claims added.  
- Cache‑miss mitigation #1 applied (3 noisy hooks disabled).  
- MP bar redesign pending; HP status line fixed.  
- `/ready` route missing → rebuild required.  
- Dead‑letter loop not wired: outcome‑bus publisher has no consumer.  
- Codex MCP plugin not connected (`codex login`).  
- Octopus voices: Codex & Grok missing; 3/5 live.  
- OOM leak mitigated by heap bump but still present.  
- 20 watchdogs unnecessary – fleet‑reaper covers all PIDs.  
- Dynamic‑workflow integration incomplete.  
- Ledger marks initially mis‑nested under `.entries`; corrected.  
- `/checkin-$Slot` caused “unknown skill args” → natural‑language wrapper added.  
- Tab titles overwritten by claude.exe; resolved with `suppressApplicationTitle`.  
- WT defaultProfile PS5.1 → fallback to non‑PS7 on parse failure; potential PS7 switch.  
- `terminal-title-update.mjs` hook orphaned (never wired).  
- 646 hooks on disk, 216 wired, 430 not wired but many intentional.

**DOMAIN SPECIFICS**  
- **Engines**: `forge-audit-v3`, MCP servers (`prism`, `claude-flow`, `shadcn`), per‑slot dispatchers grouped into domains (compute, cognitive, devops, business, frontend).  
- **Dispatchers**: 90 total; filter applied at bridge `tools/list`.  
- **Hooks/skills**: `outcome-bus-auto-tap.mjs`, `stop-auto-capture-per-slot.mjs`, `stop-obsidian-memory-feed.mjs`, `post-tool-use` hooks, `slot-context-bundle-inject.mjs`, `skill-auto-trigger.mjs`, `extract-skill-triggers.mjs`.  
- **Metrics**: HP bar (context‑token aware), MP bar (5 h quota burn), 17 M tokens in dynamic workflow run, 134 tool‑uses.  
- **Paths**: `state/shared/master-index.json`, `scripts/generate-per-slot-mcp-config.mjs`, `regenerate-launch-fleet.mjs`, `obsidian-memory-sync.mjs`, `mcp-tool-domains.mjs`, `knowledge/memories/<galaxy>/`.

**TOOLS USED**  
- PRISM launcher scripts (`LAUNCH‑PRISM‑FLEET.bat`, `Launch-PRISM-Fleet.ps1`, `slot-tab-boot.ps1`).  
- Claude binary (`claude.exe`).  
- Regenerator (`scripts/regenerate-launch-fleet.mjs`).  
- Auto‑capture hook, outcome‑bus auto‑tap, system‑viz regen script.  
- Parallel agent framework (`/smart-*` skill wrappers).  
- MCP server (`mcp-server`, `octopus`, `codex`, `forge`, dynamic‑workflow).  
- Docker/NVIDIA NIM containers for Ollama & embeddings.  
- Skill auto‑invoke (`skill-auto-trigger.mjs`, `extract-skill-triggers.mjs`).  
- Obsidian memory sync (`obsidian-memory-sync.mjs`).  
- MCP bridge (`mcp-http-bridge.mjs`) with `mcp-tool-domains.mjs`.  
- Terminal title update hook (`terminal-title-update.mjs`).

**OPEN THREADS**  
- Execute octopus cross‑vendor pass on commits (resolve rate limit).  
- Commit rule codification to CLAUDE.md + memory.  
- Optional WT defaultProfile → PS7 change.  
- Persist boot‑script `/checkin` fix in version control or capture as wiki entry.  
- Fleet relaunch required after title and checkin fixes.  
- MCP‑SHARDING‑MS0 milestone: per‑domain shard architecture & unit decomposition.  
- Dead‑letter bridge: connect outcome‑bus publisher to consumer (tribal capture, NN‑GRAPH retrain).  
- Codex login / Octopus voice completion (Grok, Codex).  
- NIM chat model deployment (`qwen2.5‑coder‑32b‑instruct‑nim`).  
- Ollama consolidation (kill native or container).  
- Dynamic‑workflow integration: upgrade forge‑audit to v3, add workflow keyword suggestion hook.  
- Verify per‑galaxy memory sync produces no stale copies after `reconcileGalaxies()`.  
- Confirm `$PRISM_BOOT_SLOT` is set in production and bridge logs correct `toolDomains`.
