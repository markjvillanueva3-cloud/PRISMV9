# token-optimization session 8b785b10 (2026-06-04, 29.9MB, spine 186KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e2cdbe2e86` – cost‑router now reaches 32 b “best” tier on Blackwell (previously capped at 14 b).  
- `606424dc12` – U‑FGC‑4/5: orphaned `.git/index.lock` self‑heal + JIT PreToolUse hook; live verified.  
- `ae2fbfdff8` – host‑aware synthesis‑model resolver returns installed 32 b model on Blackwell.  
- `9bd4b22abd` – repo‑wide `.gitattributes` (eol=lf) committed, normalizing CRLF/LF.  
- `24478d31aa` – untracked the 675 MB `system‑graph.json`.  
- `c4eb71913d` – `ask‑ollama.mjs` wired to host‑aware resolver; passes all tests (5/5).  
- `049e981158` – updated cost‑router tiers: added `gpt‑oss:120b` as best tier (65 GB, 134 tok/s), promoted `qwen2.5 72b` to strong, removed 3 b/7 b/14 b coders; re‑pointed routing to qwen2.5‑coder 32b floor.  
- `74077e38cb` – retired weak LLMs (qwen2.5‑coder 3b/7b/14b, deepseek‑r1 14b); installed gpt‑oss 120b/20b as install‑gated best tier; added anti‑revert guard.  
- `318d0c062b` – Hermes 7‑unit build plan written; dead‑offloader config bug fixed (`ollama-route-config.json`).  
- `f0e72dd6e0` – U1 router keystone (`local‑llm‑task‑router.mjs`) composed, two‑reviewer scrutiny passed.  
- `28c56cd437` – Gemma 4 31B wired into cost‑router best tier (install‑gated), ready for pull.  
- `0615b476d5` – OllamaHookBridgeEngine defaults updated to remove deleted tags; guard now covers this file.  
- `fc9038ca2a` – retired deleted tags from four .ts registries (`ModelRoutingEngine`, `OllamaTaskOffloaderEngine`, etc.) and extended anti‑revert guard to scan `src/engines`.  
- `8e2b2500c6` – Guard widened (handles `.default()` and array literals), fixed live default in `OllamaContextFloorEngine`; added `isViolation` test; all scrutiny checks green.  
- 5 commits today: `multi-provider-router.mjs`, standalone Hermes local backend, dynamic‑workflow emitter, boundary/dedup logic.

**DECISIONS**  
- Move heavy Ollama workloads to RTX‑PRO 6000 Blackwell; eliminate cloud reliance for core synthesis.  
- Lift golf gate (worktree restriction) so alpha can commit directly via mutex, enabling full punch‑list execution.  
- Pull `gpt‑oss:120b` first (65 GB, 134 tok/s), re‑point all small‑model defaults to `qwen2.5‑coder:32b`, then delete 3 b/7 b/14 b coders; preserves fail‑soft fallbacks.  
- Retire all models below qwen2.5‑coder 32b; promote gpt‑oss 120b as best tier, Gemma 4 31B as install‑gated secondary tier.  
- Build U1 router keystone first; defer heavy .ts wrapper (U1b) until budget allows.  
- Extend anti‑revert guard to scan all engine sources and `data/state` configs; widen regex for `.default()`/array literals, add `isViolation`.  
- Use Ollama‑route‑pretooluse executor (`mode:"auto"`) for tool‑call routing once offloader bug fixed.  
- Split responsibilities: Bravo (Hermes lane) unified router brain; Alpha (efficiency lane) execution hands for Ollama slice, local‑llm‑task‑router.mjs, offload telemetry.  
- Hold Alpha’s ollama‑autoexec build until Bravo confirms boundary to avoid duplication.

**OPERATOR DIRECTIVES**  
- Pull and wire powerful LLM models into the system; remove lower LLMs so they cannot be reverted.  
- Avoid cloud Kimi2.6 or larger models unless free and data‑secure.  
- Generate skills, scripts, hooks slash‑command pipelines keyed for Hermes; center on Ollama/local LLMs with reviewer agent.  
- Research Gemma 4 31B utilization and hybrid/combo LLMs via Octopus.  
- Schedule recurring loop `/loop 5m /goal …` to monitor progress.  
- Ensure all tool calls route through Ollama without quality loss.  
- Build a faithful, runnable demonstration of the five selection functions.  
- Flip `ollama-route-pretooluse` from `mode:"suggest"` to `mode:"auto"` for safety.

**FINDINGS/BUGS**  
- Orphaned `.git/index.lock` caused fleet‑wide commit stalls; solved by U‑FGC‑4/5.  
- TSC “unused import” diagnostics on .mjs files are false positives (checkJs).  
- `ask‑ollama` previously hardcoded to a 3 b model; now wired to host‑aware resolver.  
- Best tier in cost router incorrectly limited to 32 b; updated to include gpt‑oss:120b.  
- Cloud Kimi2.6 is paid and hosted outside China → rejected.  
- Stale deleted‑model tags present in multiple .ts engines; fixed by string swaps/registry edits.  
- Anti‑revert guard missed `.default()` and array literal patterns; regex widened, added `isViolation`.  
- Offloader bug: `decideRoute` returned “suggest” not “reroute”, causing dead offload.  
- Guard did not scan `data/state` configs; now included.  
- Auto‑invoke uneven: skills auto‑trigger (~17 mandatory), scripts only suggest (~0.8 % take‑rate), pipelines only suggest.  
- Model ignores advisory text; layer‑2 hooks can nudge but not execute.  
- Retirement findings: `ollama-deepseek` deleted model, bare `deepseek-r1` potential cloud breach.  
- Stale stats misread offloader behavior; cold start skips offload (timeout 1500 ms).

**DOMAIN SPECIFICS**  
- Engines & helpers: `chat-slots.mjs`, `git-commit-mutex.mjs`, `ollama-cost-router.mjs`, `host-aware-synthesis-model.mjs`, `ask‑ollama.mjs`, `precompact-pending-guard.mjs`, `per-agent-handoff.mjs`, `local‑llm‑task‑router.mjs`, `OllamaHookBridgeEngine`, `AISystemRouterEngine`, `MultiProviderRouter`, `multi-provider-router.mjs`.  
- Paths: `mcp-server/src/engines/*`, `data/state/*.json`, `ollama-route-config.json`.  
- Assets: ~440 skills/slash‑commands (`Skill` tool), node scripts `<X>.mjs`.  
- Dispatchers: Hermes auto‑invoke router, Bravo boundary/dedup logic.  
- Metrics: route‑nudge take‑rate (~0.8 %), skill auto‑trigger coverage (~490 triggers).

**TOOLS USED**  
- PRISM Workflow (background multi‑agent orchestrator).  
- CronCreate for recurring `/goal` loop.  
- Skill‑auto-trigger, master-index, slot‑souls.  
- tsc build & esbuild parse for .ts compilation checks.  
- curl + Ollama API for model pulls/deletes.  
- Bash scripts, `Skill` tool, `chat-slots.mjs`, `git-commit-mutex.mjs`, `ollama-cost-router.mjs`, `host-aware-synthesis-model.mjs`, `ask‑ollama.mjs`, `precompact-pending-guard.mjs`, `per-agent-handoff.mjs`.

**OPEN THREADS**  
1. Pull & activate `gpt‑oss:120b`; re‑point all small‑model defaults to `qwen2.5‑coder:32b`.  
2. Delete obsolete coders (3 b/7 b/14 b).  
3. Finalize cost‑router tests against new tier definitions (39/39 pass).  
4. Complete high‑ROI tasks: Obsidian‑memory‑sync content‑skip, system‑viz‑bridge await‑bug.  
5. Validate routing of all 5 synthesis scripts to `gpt‑oss:120b`.  
6. Build U1b `.ts` dispatcher wrapper (budget pending).  
7. Final guard check for comma‑position bypass in state configs.  
8. Coordinate with Bravo’s Hermes memory vault integration.  
9. Pull Gemma 4 31B and gpt‑oss models; enable hybrid/octopus demo.  
10. Resolve retirement findings (`ollama-deepseek`, `deepseek-r1`).  
11. Finalize Alpha’s ollama‑autoexec build once Bravo confirms boundary.  
12. Ensure unified router integration (Bravo + Alpha) without duplication.
