# Session evidence pack: be5e37e8
- date: 2026-05-19  topic: alpha-work  size: 28MB  turns: 2342
- raw transcript: C:\Users\wompu\.claude\projects\H--prism\be5e37e8-9aa8-4749-b02f-a97fde0830dd.jsonl

> Sonnet: categorize this session from the evidence below. Sections are noise-filtered extracts
> (tool-call churn stripped). If a section is thin you MAY grep the raw transcript above for more.

## USER PROMPTS (operator intent + asks) (24)
- # /checkin-mike — slot-locked /checkin

Force-takes the **mike** slot (evicting any prior owner with `--force true --confirmRecent true`), binds the handoff to `mike-work`, then runs the standard `/checkin` pipeline. Slot added 2026-05-16 as the 13th fleet slot per the operator directive "add a 13th chat slot, update everything that needs to update to intake a 13th chat".

## Slot binding (replaces /checkin Step 2)

```bash
STABLE="claude-<8hex-from-Chat-Isolation-line>"
BRANCH=$(git -C H:/prism rev-parse --abbrev-ref HEAD 2>/dev/null)
SLOT="mike"
TOPIC="mike-work"

# Reap stale slots first, then force-take mike from whoever holds it.
node H:/prism/.claude/helpers/chat-slots.mjs reclaim
node
- Continue from where you left off.
- chat crashed continue where we left off
- chat crashed, were you working on ocr, cad and cam training or was that another chat?
- continue
- continue so we cant start training the system to read prints accurately, generate cad files from print then generate cam program
- start training print reading first, we need to start generating templates for dimensions and features extracted plus tolerancing. once we ran every single print in the entire system, move to training cad drawing
- are you working on training the ocrs and print readers first to develop templates for common parts in our system and then generate it for more complex parts. we first need to prove we can extract the correct data from prints before anything else
- did you train the ai with print reading accurracy?
- use ollama if its accurate enough, if we can't get it to train properly with that, we'll look at other options. can we utilize my gpu and docker?
- This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Summary:
1. Primary Request and Intent:
   - Initial: `/checkin-mike /goal train cad and cam ai systems so they can accurately read prints, generate cad files relative to prints then generate cnc programs with cam. start with fusion360. /loop [10m] complete goal`
   - Mid-loop expansion: "it might be a good idea to also train the ocr readers and print interpretation capabilites, if we need to build an ai system for that, do it"
   - Phase priority clarification: "start training print reading first, we need to start generating templates for d
- had to restart continue
- check gpu pipeline first
- do we have a proper system setup now to start training our print reading and data extraction capabilities?
- try it, will my 4080 on my home pc help and if we only ran 1 chat?
- This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Summary:
## AWARENESS BACKBONE (preserve — read these files directly if context is missing)
Three auto-injected source-of-truth files exist on disk; trust them over compacted memory:
- `H:/prism/state/shared/CLAUDE-BRIEF.md` — what PRISM has
- `H:/prism/state/shared/PRISM-BUILD-CONTEXT.md` — what's being built (commits/claims/handoffs)
- `H:/prism/state/shared/PRISM-BUILD-VISION.md` — per-component max-value vision + gaps
Build Doctrine: before any new engine/script/hook, check CLAUDE-BRIEF, check claims in PRISM-BUILD-CONTEXT, check compone
- <local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>
- what chat slot was this primarily for?
- continue, i dont think there are any open chat slots left so try to stay organzied and survive compactions
- # Pre-Compact — Session Continuation Handoff

You are about to prepare for `/compact`. This is the LAST thing you do before the user runs the real `/compact` command. Your job is to capture everything needed so the next session can seamlessly continue.

**CRITICAL**: The RESUME directive you write here is what enables auto-continue after compaction. If you write a vague RESUME, the next session will be lost. Be SPECIFIC.

**WHY THIS IS CRITICAL (2026-05-06):** The PreCompact hook auto-writer is BANNED. It used to fall back to a generic "Pre-compact snapshot (RESUME generated)" stub, which clobbered real RESUMEs and left next sessions blind. Now ONLY the live chat (this conversation, via this
- [Request interrupted by user]
- This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Summary:
## AWARENESS BACKBONE (preserve — read these files directly if context is missing)
Three auto-injected source-of-truth files exist on disk; trust them over compacted memory:
- `H:/prism/state/shared/CLAUDE-BRIEF.md` — what PRISM has (process priority mill > lathe > WEDM; CAM tier-1 priority Fusion > hyperMILL > Mastercam > Esprit; JM fleet; AI hierarchy; gaps)
- `H:/prism/state/shared/PRISM-BUILD-CONTEXT.md` — what's being built (commits/claims/handoffs)
- `H:/prism/state/shared/PRISM-BUILD-VISION.md` — per-component max-value visio
- continue training the ocr print reading capabilities of the prism system
- This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Summary:
1. Primary Request and Intent:
   - **Original session goal:** `/checkin-mike /goal train cad and cam ai systems so they can accurately read prints, generate cad files relative to prints then generate cnc programs with cam. start with fusion360. /loop [10m] complete goal`
   - **Phase priority (explicit):** print-reading accuracy MUST be proven before CAD/CAM phases ("we first need to prove we can extract the correct data from prints before anything else").
   - **Post-compact directive (1):** "continue training the ocr print readin


## COMMIT SUBJECTS (what shipped) (60)
- Recovering — verifying U-FGE01 commit landed, then resuming iter 2 (OCR/print-interpretation training, per the mid-loop user expansion).
- ✅ U-FGE01 landed at `62b5794101`. Closing iter 1, opening iter 2 — OCR/print-interpretation training inventory + build.
- iter 2 shipped ✅. Per CLAUDE.md doctrine, iter 3 = doc-reflection (CLAUDE.md regression entry + Obsidian memory) for both U-FGE01 and U-BPA-CONSUMER before next build.
- Same chat (slot mike, `claude-be5e37e8`). Resume hook confirms it: "BLUEPRINT-OCR-TRAINING-MS2 — last: U-BPA-CONSUMER". Status:
- 1. **U-FGE01** @ `62b5794101` — wired the trained STEP geometry corpus (662/665 files, evidence counts per part_class) into `CADClassFeatureLibraryEngine.buildSequenceForEvidence` + new dispatcher action `cad_class_build_sequence_evidence`. Closes the "geometry not auto-wired into build-sequence inference" gap from yesterday. 35/35 tests PASS. 4 P0/P1 reviewer findings fixed pre-commit (16MB cap, CWD-independent path, R12 success-on-fail, NaN-poison guard).
- 2. **U-BPA-CONSUMER** @ `6cbe5b1561` — built the offline events consumer (`scripts/blueprint-accuracy-consumer.mjs` + pure lib + 35 tests) that finally activates the dead `blueprint-accuracy-events.jsonl → state.json → xproc_*` loop. The MS1 hook has been writing events into the void since 2026-05-16; this drains them. 869 LOC, idempotent, atomic state writes, smoke-tested live (4 synthetic events → 4 xproc actions).
- **Key R8 dedup finding:** All 8 BLUEPRINT-OCR-TRAINING-MS1 units (RAG engine, LoRA bridge, GroundTruthRegistry extension, accuracy-guard hook) shipped 2026-05-12..16. The training **infrastructure** was complete; the **activation glue** (consumer) was missing. That's what U-BPA-CONSUMER fixes.
- ✅ U-FGE02 landed @ `c60f6c9396`. Loop at iter 5/60.
- | 1 | U-FGE01 | 62b5794101 | Engine method + new dispatcher action (35 tests) |
- | 2 | U-BPA-CONSUMER | 6cbe5b1561 | Offline events consumer activating the dead loop (35 tests) |
- | 5 | U-FGE02 | c60f6c9396 | Wired evidence-ranking into live `cad_class_drive_build` Fusion360 path |
- Writing tests next (matches U-BPA-CONSUMER pattern: `node:test` with full coverage floor).
- ✅ U-TDP01 landed `5e3048ad71`. Pipeline live. Continuing iter 7: U-TDP02 = batch harvester that scans a print directory + runs the training driver across it, maintaining a processed-prints registry for idempotent re-runs.
- ✅ U-TDP02 landed `8d533c1e10`. Iter 8/60.
- | 1 | `62b5794101` | U-FGE01 | Engine method + new dispatcher action — STEP corpus → build sequence (35 tests) |
- | 2 | `6cbe5b1561` | U-BPA-CONSUMER | Offline events-JSONL → state.json consumer (35 tests) |
- | 5 | `c60f6c9396` | U-FGE02 | Wired evidence ranking into live `cad_class_drive_build` Fusion360 orchestrator |
- | 6 | `5e3048ad71` | U-TDP01 | 4-stage print-to-CAM training driver (32 tests, full chain live) |
- | 7-8 | `8d533c1e10` | U-TDP02 | Batch harvester with idempotent registry (25 tests, smoke-tested live) |
- That chains: scan → infer → drive (extract→CAD→CAM) → emit JSONL events → registry persisted. Re-runs are idempotent. The U-BPA-CONSUMER on a separate cron drains the JSONL into `blueprint-accuracy-state.json` for the LoRA bridge / xproc dispatches to consume.
- Building iter 9: **U-TDP03 — Extraction Aggregator** (lib + tests + CLI).
- ✅ U-TDP03 landed `99288aec44`. Iter 10/60.
- **Stub-mode produces meaningful baselines** (sanity-check ranges per part_class) so operators can compare real extractions against expected distributions when the vision-LLM credentials get wired (U-TDP04+).
- - 5 commits (U-FGE01, U-BPA-CONSUMER, U-FGE02, U-TDP01, U-TDP02, U-TDP03)
- Want me to keep going (next candidate: U-TDP04 = wire real vision-LLM extract adapter, OR template-divergence reporter against the hand-tuned baseline), or pause for review?
- Building iter 10: **U-TDP04 — OCR Extraction Benchmark Runner**.
- Acknowledged the user's insight: CAD files + CNC programs are also ground truth. I'll ship U-TDP04 (benchmark framework) first, then immediately U-TDP05 (CAD/CNC ground-truth extractors that feed U-TDP04).
- Cleaning + committing U-TDP04.
- ✅ U-TDP04 landed `5709f19d7b`. The 5-file count picked up 2 prior unstaged files (training-driver + harvester stub-alignment from earlier in the chain — those were related to U-TDP03 but got grouped here; net-positive, none lost).
- Iter 12: U-TDP05 — CAD-derived ground truth extractor (uses the already-shipped STEPGeometryParserEngine to auto-derive GT from JM Die's CAD files, feeding directly into U-TDP04's benchmark).
- The engine has `evidenceForFeatureKinds(geometry)` — already maps STEP geometry to specific feature kinds. For U-TDP05 I'll extend the benchmark to support `presence_only: true` GT entries (match by kind alone, since STEP geometry gives presence but not nominals) + ship the CAD-GT extractor.
- Full chain live: CAD-derived GT (11 classes from real JM Die STEP corpus) → benchmark → all classes PASS with stub extractor. Exit 0. Committing U-TDP05.
- ✅ U-TDP05 landed `1bc36d949d`. Iter 13/60.
- | U-FGE01 | `62b5794101` | STEP geometry corpus → build-sequence evidence ranking |
- | U-FGE02 | `c60f6c9396` | Wire evidence ranking into Fusion360 cad_class_drive_build |
- | U-BPA-CONSUMER | `6cbe5b1561` | Offline events-JSONL drainer (activates the MS1 hook's writes) |
- | U-TDP01 | `5e3048ad71` | 4-stage print-to-CAM training driver |
- | U-TDP02 | `8d533c1e10` | Batch print harvester with idempotent registry |
- | U-TDP03 | `99288aec44` | Extraction aggregator → per-class learned templates (Welford) |
- | U-TDP04 | `5709f19d7b` | OCR extraction benchmark (precision/recall/F1 + dim percentiles) |
- | U-TDP05 | `1bc36d949d` | CAD-derived GT from existing STEP corpus (11 classes, no labeling) |
- **Next candidate**: U-TDP06 = CNC-program-derived GT (G-code → cutting-depth + tool-diameter ground truth via G81/G83 cycles + tool calls). Continue, or pause for review?
- While the 11B model loads (will take ~3 min cold), drafting **U-TDP07** — the embedded-text extractor. Even though it only covers 3.5% of the corpus, that's still **~2,700 PDFs** with deterministic high-precision extraction (no LLM hallucinations) — worth shipping alongside the vision path.
- No existing PDF text extractor lib — safe to build U-TDP07 (R8 dedup-preflight clean). Building it now while the vision model loads:
- 1. **Embedded-text extractor (U-TDP07 — just shipped, working at 0.95 confidence on real JM Die PDFs).** Handles the 3.5% of corpus with embedded text = **~2,700 PDFs deterministically with zero hallucination**.
- Committing U-TDP07 first — proven working, load-bearing — then pulling the right vision model in the background while we keep moving:
- Lock released. Committing U-TDP07:
- While the lock clears, wiring the embedded-text extractor as the first adapter in the U-TDP01 training driver. Reading the existing driver to find the integration point:
- All tests green. Committing U-TDP07 (lib) + the driver wiring together — same logical unit, one commit:
- Worktree `H:/prism-slot-mike` on branch `slot/mike` created. Copying U-TDP07 files into it and committing there:
- - commit: `d85573b7d4 [APS-FUSION-CLOUD-MS0] [TRAIN-OCR]/U-TDP07: deterministic embedded-text PDF dimension extractor + training-driver adapter`
- - ✅ **U-TDP07 (embedded-text path)** — handles ~3.5% of JM Die corpus (~2,700 PDFs) deterministically, zero LLM, zero hallucinations
- - ⏸ **U-TDP06 (vision path)** — gated by VRAM. moondream:1.8b too inaccurate, llama3.2-vision:11b OOMs on 10GB. Queued: pull `qwen2.5vl:7b` (~5GB) as the right-sized fallback for the 96.5% image-only PDFs
- [U-TDP02] harvester → registry of (path, inferred part_class)            ✅ live
- [U-TDP01] driver  ─┬→ [U-TDP07] embedded-text  → BlueprintExtraction     ✅ live (3.5% coverage, 0.95 conf)
- └→ [U-TDP06] vision adapter → BlueprintExtraction     ❌ unfit (moondream dumb, 11B OOMs)
- [U-BPA-CONSUMER] → state.json daily ledger                               ✅ live
- [U-TDP03] aggregator → per-class template (Welford stats)                ✅ live
- [U-TDP04] benchmark vs [U-TDP05] CAD-derived GT → P/R/F1, dim-error      ✅ live
- | Run harvester across 76K PDFs and route by inferred part_class | ✅ now | U-TDP02 idempotent + part-class heuristic shipped |


## DEFERRED / UNFINISHED / UNWIRED markers (39)
- - next: act on work order — train CAD/CAM AI: print → CAD → CAM, start Fusion360
- Found the gap signal: `STEPGeometryParserEngine` is `unknown` (likely unwired) and `cad_class_build_sequence`/`strategy_sequence_build` are STUB L8 actions even though they have wiki entries. The geometry corpus has rich feature evidence (central_oil_hole×71, bevel_chamfer×38, etc.) but build-sequence inference isn't reading it. Let me trace the precise wiring gap.
- Both reviewers caught the same 4 real issues. Applying P0/P1 fixes:
- Spec exists. Checking which MS1 units shipped vs pending to identify the precise consumer gap.
- 1. **U-FGE01** @ `62b5794101` — wired the trained STEP geometry corpus (662/665 files, evidence counts per part_class) into `CADClassFeatureLibraryEngine.buildSequenceForEvidence` + new dispatcher action `cad_class_build_sequence_evidence`. Closes the "geometry not auto-wired into build-sequence inference" gap from yesterday. 35/35 tests PASS. 4 P0/P1 reviewer findings fixed pre-commit (16MB cap, CWD-independent path, R12 success-on-fail, NaN-poison guard).
- Improving repair to handle arrays with incomplete first child (track open-bracket stack), and adding a test for the moondream-chaos response:
- **What's shipped vs deferred:**
- Both reviewers returned **FAIL** with overlapping, legitimate P0 data-poisoning defects. Per the per-file gate I must fix every P0+P1 before committing — this file feeds AI training data, so correctness is non-negotiable. Consolidated fix list:
- - **P0-1** (both): `±25` and `±.25` both → `0.25` — the regex's `\.?` eats the dot, silently rewriting integer tolerances 100×. Fix: capture the dot, make `tolValue` dot-significant.
- - **P0-2**: Unicode minus (U+2212) not in prefix → handle it.
- - **P0-3**: `15.00 ±.25 ±.30` greedily takes first pair — refuse chained-± (R12).
- - **P1-1** (both): kind-blind dedup drops legit distinct same-nominal feature (R5 vs linear 5) — make dedup kind-aware.
- - **P1-2**: geometric-only GD&T sheets confidence-deflated to 0.50 — count circularity/TIR.
- - **P1-3**: `±0` false-toleranced — treat as untoleranced.
- - **P1-4**: confidence ladder magic numbers violate file's own named-constant convention.
- - Pre-existing split-token OCR gap (reviewer B P0-1): out of U-TDP07 scope (U-TDP01–06 code) — log honestly as follow-up, don't expand scope.
- Now rewrite the inline-pm scan with all P0/P1 fixes (kind-aware dedup, dot-significant tolValue, chained-± rejection, label capture, ±0 untoleranced):
- Now rewrite the confidence block with named constants + the geometric-only tier (P1-2, P1-4):
- All constants consumed. Now extend the test suite with the reviewer-mandated regression tests for the previously-unprotected invariants (P0-1 integer-tol, P0-2 unicode-minus, P0-3 chained-±, P1-1 kind-aware dedup, P1-2 geometric confidence, P1-3 ±0, P2 label, Infinity guard):
- Real bug caught by the test (correct R12 behavior): `TO WITHIN .002 T.I.R.` → got `2` not `0.002`. Root cause is the **same dot-eating defect as P0-1** but in `TIR_RE`/`OUT_OF_ROUND_RE` — their `\.?` sits *outside* the capture group, so `tolValue` can't see the dot. The test is correct; fix the regexes to capture the dot:
- All 7 P0/P1 defects fixed + P2 labels now captured (`label=C/E/E.POINT` — materially better training attribution), TIR bug fixed, zero fabrication. Per-file gate requires re-dispatching both reviewers after a FAIL→fix. Re-verifying:
- Reviewer A: **PASS** (all closed, 1 documented P2). Reviewer B: **FAIL** on **B2-P0-1** — and B is right: I wrongly assumed the split-token path was pre-existing U-TDP01–06 code. Git proves the whole file ships in one commit (d85573b7d4 = U-TDP07), so `parseSignedTolLine("- 10")` → fabricated ±10mm (dropped-dot OCR) is **in-scope U-TDP07** and undisclosed while a lesser concern is loudly deferred — a real R12 violation. The honest fix is to actually close it with the same doctrine the inline pat
- The canonical split-token tolerance grammar is **always** a leading-dot fraction (`.10`, `- .02`, `.00`); a pure-integer token (`- 10`) is the dropped-dot OCR class. Fix `parseSignedTolLine` to refuse it (same R12 doctrine as the inline path), closing B2-P0-1 with an actual fix:
- Add the B2-P0-1 regression test (pins the dropped-dot split-token refusal — must fail if `parseSignedTolLine` reverts to fabricating):
- 57/57 pass (+3 for B2-P0-1). The fix is strictly more conservative (refuses, never fabricates). Per the gate doctrine (FAIL→fix→re-dispatch both), final focused re-verification of the delta:
- - `"- 10"`/`"10"`/`"+ 5"` → tok has no `.` → `return null` ✓ **REFUSED** (B2-P0-1 closed)
- At the context hard threshold (998K/1M) — must precompact now. State is safe: parser fully hardened (57/57 tests, all P0/P1 + B2-P0-1 closed, real PDF verified clean), handoff already written. Invoking precompact:
- • Per-file scrutiny found 8 P0/P1 data-poisoning defects (incl. a silent 100× tolerance
- scripts/lib/pdf-text-extract-lib.test.mjs  — 33→57 tests (+24: inline-± + P0/P1 regressions)
- DEFERRED / HONEST CAVEATS:
- exhausted (resets 11:20pm Chicago). Round-2: reviewer A PASS, reviewer B FAIL on B2-P0-1
- Every concrete priority-queue pick I verified (H1, H1.0, C1, C2) is **already shipped on disk** — the queue is feeding done work as "pending." That's the root cause of the duplication risk you flagged. Rather than pick a unit I can't trust, let me check whether the close-out audit data can fix the picker itself.
- HOOK-SYNERGY-MS0 envelope status is **`complete`** — every unit done — yet the priority queue surfaces H1-H7 as pending. The queue is structurally unreliable for picking, and that's the exact mechanism behind the duplication risk you flagged.
- Both reviewers: **PASS**, 0 P0. Reviewer B raised 2 P1s — I'll fix both: **P1-A** (uncached double-scan — add an mtime cache mirroring the file's existing pattern) and **P1-B** (docstring caveat for the id-collision tail risk + disclose the behavior change in the commit). Applying P1-A:
- A peer chat (PID 40404, `[WIRE-UNWIRED]/U-ECHO-AUDIT-ACTIONMAP`) holds `.git/index.lock` mid-commit — shared main-tree index contention, not file conflict. It's alive, not crashed (lock is ~2min old, likely a slow pre-commit hook). I'll wait for it to clear rather than break a live peer's commit. Meanwhile I'll write the OCR handoff (no git needed):
- Both reviewers PASS. Arm A's lone P1 (paired-band ordering vs bare-number triple) is acknowledged as a narrow false-positive surface, tolerable per R12 — I'll note it in the commit body. Cleared to write tests.
- Apply Reviewer A's 2 legit findings (REJECT tests need stricter `length === 0` assertion + line 695 needs exact 0.85). Skip A's P1 on line 731 (misread — `"60-62"` is the correct raw in-loop capture, no `" HRC"` suffix, test already passes).
- 3-of-3 ledger marked all PASS. Now doc-reflection: write the memory reference + MEMORY.md index entry. (Skip CLAUDE.md — U-TDP08 is a forward feature, not a regression. Wiki entry deferred to a follow-up since this is a small lib extension.)
- ### Deferred follow-ups (named in handoff)


## ARTICLES / REFS FED (1)
- - Default OLLAMA_URL=http://127.0.0.1:11434


## ASSISTANT TAIL (end-of-session status)
All three arms PASS. Mark the ledger.

---

3-of-3 ledger marked all PASS. Now doc-reflection: write the memory reference + MEMORY.md index entry. (Skip CLAUDE.md — U-TDP08 is a forward feature, not a regression. Wiki entry deferred to a follow-up since this is a small lib extension.)

---

Memory file + MEMORY.md index updated — Obsidian feed will replicate on next Stop hook. Now write the slot-mike handoff for next session.

---

## §Report — slot mike (claude-be5e37e8) — OCR training continuation

**Work order:** *"continue training the ocr print reading capabilities of the prism system"*

### What shipped this session

| Item | Detail |
|------|--------|
| Picker-fix | `1576134f55` on main (`cad-fusion-live-ms0`) — `U-PQ-MS-COMPLETE-FILTER` — pickers now exclude units belonging to envelope-complete milestones (HOOK-SYNERGY-MS0 leakage class). 66/66 tests, 2-reviewer PASS. |
| **U-TDP08** | `cafd0871c1` on `slot/mike` — paired-tolerance-band (`1.002`/`1.000` form) + Rc hardness range (`Rc 58-60`, `55-57 Rc`, `58 TO 60 Rc`). 77/77 tests (57 prior + 20 new). |
| Per-file scrutiny (lib) | Arm A physics-review + Arm B reviewer → both PASS |
| Per-file scrutiny (tests) | Arm A test-review FAIL → 2 fixes applied (`dimensions.length===0` lock; exact `===0.85` ladder) → re-PASS. Arm B PASS. |
| 3-of-3 Stop gate | Arms opus + claude + analyst → all PASS, `blockCount: 0`, ledger marked for `claude-396bc735` |
| Memory | `reference_u_tdp08_paired_band_rc_2026_05_19.md` + MEMORY.md index entry (Obsidian auto-feed on next Stop) |
| Handoff | `HANDOFF-claude-be5e37e8-mike-cad-fusion-live.md` written with concrete RESUME for next session |

### Training-signal evidence (real corpus)

Re-ran extractor on `JM DIE/_PART LIBRARY/ACCURATE THREADED FASTENERS` (19 emitted PDFs):
- **7 new paired-band dims** across 3 PDFs (this pattern was 0 before U-TDP08)
- **4 PDFs got Rc hardness filled** (was 0)
- All new dims correctly classified `stepped_revolved_axis` (no kind drift)
- Subdir rate: 4/19 = 21.1% vs U-TDP07 baseline 81/600 = 13.5%

### Discipline observed

- R12 fail-loud: every REJECT test locks `dimensions.length === 0` (not just `paired.length === 0`) — reviewer A caught the silent-fallthrough class
- No inline physics constants (clamps are extraction heuristics, not Kienzle/Taylor)
- No stubs / no softened gates — Reviewer A's misread of line 731 (`"60-62"` vs `"60-62 HRC"`) was independently verified against the in-loop GRADE_RE capture
- Slot-worktree discipline maintained: U-TDP08 on `slot/mike`, picker-fix on main (fleet tooling)

### Deferred follow-ups (named in handoff)

- Full 600-PDF corpus delta measurement (harvester stalled in background, ran on 19-PDF subdir)
- Comma-decimal normalization, MAX/MIN qualifiers, slash-paired tolerance (U-TDP09 candidates)
- Wiring `pdf-text-extract-lib` into U-TDP04 benchmark grader (currently `makeLiveExtractor returns null`)
