# token-optimization session 928a8226 (2026-06-09, 61.5MB, spine 623KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hookify frontmatter conversion (`scripts/fix-hookify-frontmatter.mjs`) fixed 110 malformed `.local.md` files; removed 4 dead `settings.json` hook refs.  
- Resilient Ollama pull script (`ollama-resilient-pull.ps1`) repaired em‑dash parse error → `gpt‑oss:120b` installed, default `num_predict=1024`.  
- Commit c593b096fb – fixed pull script & added default predict; commit 13017de764 – cleaned hook frontmatter.  
- Commit 29f2d177f6 – node‑by‑node gap‑map artifact; 8550436c8a – corrected CAG router Qdrant label.  
- Commit c9fe32d8b2 – embedder OOM & nomic context‑limit fixes, empty‑text skip guard added.  
- Driver (PID 54544) started embedding wiki files on idle GPU.  
- Commit 182788232a – cap‑safe loader for `tribal-embed-index.json`.  
- Commit 5467482be4 – doc‑reflect updates; a3e6d3ca97 – clobber guard & shrink guard; c994bf3aad – memory docs of clobber incident.  
- Commit fb314a6fd1 – `docker-service-health-check.mjs` CLI, 5/5 tests.  
- Commit 14b8b383a2 – singleton‑service‑guard Stop hook wired into Docker health stop chain.  
- Commit 006c435ddb – corrected MCP restart command (`TRANSPORT=http node dist/index.js`).  
- Commit e2081e0780 (U‑MCP‑FIXSTART) – guard upgraded to reap‑all + start daemon; 14/14 tests, 3‑of‑3 scrutiny PASS.  
- Commit d22681f5d2 – cosmetic tag fix; bee977a004 – added `NODE_OPTIONS=--max-old-space-size=8192` to reranker spawn env.  
- Commit 6ca2f6afd2 – MCP boot‑heap OOM fixed (floor 4096 MB).  
- Commit 4141daf9d8 – fleet‑task‑health migration‑freeze marker; U‑FTH‑WIKI‑LESSON – wiki lesson for freeze marker.  
- Commit U‑QP‑COST‑BRIDGE‑WIREING‑TRUTH – restored cost‑bridge router logic, removed stale “0 wired” warning.  
- Commit U‑QP‑AWARNESS‑GEN‑ROUTER‑AWARE – recovered quoting awareness generator.  
- Commit U‑QP‑COST‑SAVINGS‑TEST – 13 vitest cases covering savings formulas.  
- Commit U‑QP‑WEDM‑EXEMPT‑TAG – added `// WIRE‑EXEMPT:` tag to `WEDMQuoteBridgeEngine`.  
- Commit U‑BW‑OLLAMA‑MODEL‑FIX – cascade defaults now use only pulled Blackwell models; U‑OLLAMA‑SOURCE‑LOCK‑EXTEND – guard extended to dispatcher tree.  
- Commit U‑QP‑DIGITALTWIN‑TEST – 5 R9 reference tests for `DigitalTwinEstimator`.  
- Commit U‑QP‑MARGIN‑FLOOR‑GATE – margin‑floor guard in `QuoteEstimatorEngine`; U‑QP‑MARGIN‑FLOOR‑SHOPCONFIG – source floor from `ShopConfigurationEngine`.  
- Commit U‑QP‑NRE‑AMORTIZE‑TEST‑FIX – corrected stale NRE amortization test.  
- Commit ec597dbcb3 – margin‑floor dispatcher round‑trip (7/7 tests).  
- Commit 87d5c4bf9a – margin‑floor alert banner on `QuoteBuilderPage`.  
- Commit 4b0980c56a – AI‑substrate routing matrix coverage 27/27 tests.  
- Commit 62b114cb7b – ResizeObserver polyfill; hidden frontend failures fixed (13/13 quote‑pages tests).  
- Commit 8258918f78 – `U‑QP‑FRONTEND‑TEST‑REPAIR`: corrected mock, added null‑guards, failure‑mode test.  
- Commit 44fab1be37 – dispatch map for fleet‑synergy goal (`state/shared/SYNERGY‑GOAL‑STATUS‑2026‑06‑09.md`).  
- Commit 4c12a75a8d – closed‑loop provenance gate (`classifyOutcomeProvenance`), 40/40 tests, 3‑of‑3 scrutiny PASS.

**DECISIONS**  
- Fix hook errors first; avoid reimplementing logic.  
- Leave unwired engines untouched until owner triage.  
- Resolve Ollama pull parse error instead of rewriting workflow.  
- Use local GPU for all Ollama inference to bypass Anthropic limits.  
- Batch subagent workflows in groups of 3 to stay within server throttle.  
- Replace string read of tribal index with buffered loader (V8 cap‑safe).  
- Implement empty‑text skip guard in embedder.  
- Wire singleton‑service‑guard into Stop hook; auto‑recover MCP outages.  
- Fix documentation of MCP restart command.  
- Add heap flag to reranker (`--max-old-space-size=8192`).  
- Use slash‑agnostic regex for daemon detection; add `--fix` entry point to singleton services.  
- Replace exit‑code protocol with JSON‑stdout in `stop_on_hook_unregistration`.  
- Floor MCP server heap on all spawn paths (≥4096 MB).  
- Defer Qdrant tribal migration; avoid duplicate path.  
- Partition disabled tasks in fleet‑task‑health (`EXPECTED_DISABLED_TASKS`).  
- Adopt router‑consolidation design for cost‑bridge hooks.  
- Use `WIRE‑EXEMPT` convention for routed adapters.  
- Migration‑freeze marker machine‑readable (`state/shared/MIGRATION-FREEZE-ACTIVE.flag`).  
- Utilize ultracode for planning, deep reasoning & orchestrating; Ollama local LLM for grunt work.

**OPERATOR DIRECTIVES**  
- `export PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1` to bypass golf write allowlist.  
- Edit `C:/Users/wompu/.claude/settings.json` → remove PreToolUse entry for golf‑slot‑write‑allowlist hook.  
- Stop hook condition: “synergize ollama … docker, qdrant, obsidian vault app, PSN, /system‑viz, prism galaxies, front‑end & back‑end build, claude.md, memories, wiki, tribal knowledge, prism awareness, gsd, tdd, skills, scripts and hooks | goal clear.”  
- Operator may `/compact` for fresh Qdrant tribal migration budget or redirect to next unit.  
- Use ultracode for planning, deep reasoning & orchestrating; Ollama local LLM for grunt work (explicit).  
- Utilize Ollama for better task efficiency when viable.

**FINDINGS/BUGS**  
- Hookify: 110 files missing YAML frontmatter; 1 file crashed with UnicodeDecodeError.  
- Scheduled‑task WARN: all failures intentional migration freeze (42 tasks disabled); no action needed.  
- Unwired engines audit: 90 built engines lacking dispatcher/hook reference (53 core, 23 external‑tool bridges, 14 ops/test scaffolds). Some deliberate orphans.  
- Ollama synergy bug: `gpt‑oss:120b` returned empty when `num_predict` too low; fixed with default 1024.  
- Rate limiting from 11 concurrent subagents mitigated by batching.  
- Embedder OOM on tribal index due to Node heap limit; solved with `--max-old-space-size=8192`.  
- Nomic embed context window exceeded for large files; added 2000‑char clamp.  
- Empty‑text files caused chunk aborts; skip guard added.  
- Synergy bug: `routeModelForTask` still selects `gpt‑oss:20b` instead of `120b`.  
- V8 hard string cap (512 MiB) caused JSON.parse to throw; fixed by buffer loader.  
- Default Node heap (~2 GB) OOM on >150 MB tribal index; mitigated with NODE_OPTIONS.  
- Fail‑open clobber in `tribal-embed-index.mjs` overwritten brain; guard added.  
- Duplicate wedged MCP daemons port‑bind conflict fixed by singleton guard + correct restart command.  
- Silent cascade failures due to retired un‑pulled models; source‑lock blind spot on dispatchers.  
- PSN snapshot stale (6 days) revealed real under‑wired substrate pairs.  
- NRE amortization test stale; code correct.  
- System‑viz `find` OOM resolved by acknowledging fleet‑budget cap.  
- Frontend type mismatch: `QuoteBuilderPage` lacked nested pricing block; fixed via dispatcher round‑trip test and UI banner.

**DOMAIN SPECIFICS**  
- Hookify rule files (`.claude/hookify.*.local.md`) require YAML frontmatter, pattern extraction, action mapping.  
- Settings.json hook wiring for SessionStart, UserPromptSubmit, PreToolUse entries (golf slot & reaper guardian).  
- Unwired‑engine audit script `scripts/audit-unwired-engines.mjs`.  
- Ollama integration: `ollama-resilient-pull.ps1`, `ollama-cost-router.mjs`; GPU metrics via `/api/tags`.  
- Tribal index loader `scripts/lib/load-tribal-index.mjs` (cap‑safe, brace‑aware).  
- PSN snapshot `psn-synergy-snapshot.json`.  
- Qdrant collections: `prism_engines`, `prism_skills`, `prism_formulas`.  
- MCP server (`node dist/index.js` on :3100); health checked by connectivity banner & singleton guard.  
- Docker services (qdrant, postgres, grafana, prometheus) health‑checked by `docker-service-health-check.mjs`.  
- AI‑substrate routing matrix (`getRoutingMap`).  
- Engines: `QuoteEstimatorEngine`, `DigitalTwinEstimator`, `CostSavingsTrackerEngine`, `ShopConfigurationEngine`.  
- ClosedLoopEngine runCycle with `classifyOutcomeProvenance`.  
- Margin‑floor guard path (engine→dispatcher→UI).  

**TOOLS USED**  
- Python `config_loader.py`; Node scripts: `fix-hookify-frontmatter.mjs`, `audit-unwired-engines.mjs`, `fleet-reaper-sweep.mjs`, `ollama-resilient-pull.ps1`, `embed-wiki-into-tribal-index.mjs`, `tribal-rerank.mjs`.  
- PowerShell for script edit/execution.  
- PRISM tools: `singleton-service-guard.mjs`, `stop_on_hook_unregistration.mjs`.  
- Node runtime with `--max-old-space-size=8192`.  
- Vitest (`src/__tests__/*.test.ts`) & node:test (`scripts/*.test.mjs`).  
- Docker CLI, Qdrant REST API.  
- Ultracode workflow/multi‑agent orchestration.  
- TypeScript compiler (`tsc --noEmit`).

**OPEN THREADS**  
- Resolve routing‑model bug: update `routeModelForTask` or model‑tier config so `gpt‑oss:120b` is selected for best‑tier categories.  
- Verify unwired engines triaged; wire high‑value ones (e.g., `PlaywrightAutomationEngine → automationDispatcher`).  
- Confirm reaper GPU/OLLAMA metrics stable after pull.  
- Finish empty‑text guard in embedder; driver still fails ~84 % chunks.  
- Complete remaining wiki→tribal embedding run to 100 % coverage.  
- Resolve MCP keep‑alive scheduled task (periodic `singleton-service-guard --fix`).  
- Qdrant tribal migration: reembed lost entries (~29K) into Qdrant (next high‑value unit).  
- Write‑side sharding for tribal index >512 MiB.  
- Cross‑substrate PSN snapshot wiring for Sierra’s lane; other lanes pending.  
- Implement `QuotingActualOutcomeLoaderEngine` to load real ERP data.  
- Add `prism_quoting:closed_loop_provenance_check` observability action.  
- Obtain live E2 credentials / invoice ingestion pipeline.  
- Complete fleet‑scope synergy across 20+ peer slots via orchestrator dispatch.  
- Address cross‑slot residual retired‑tag references (handled by india/alpha slots).
