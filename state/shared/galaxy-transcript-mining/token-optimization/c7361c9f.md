# token-optimization session c7361c9f (2026-06-10, 72.9MB, spine 495KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- ascii‑guard.mjs diff‑aware PreToolUse blocks smart‑punctuation in code files only.  
- MCP server heap floor 4 GB→24 GB; watchdog RSS threshold 3 GB→18 GB (watchdog < heap).  
- Migration‑freeze cleared; all 8 reapers/monitors enabled and running.  
- NIM containers stopped; Ollama stack validated, GPU utilization ~5–9 % offload take‑rate <30 %.  
- Fleet‑reaper sweep executed; guardian hook active.  
- Git‑tree routing active; backlog deferred to quiet‑window commit.  
- Stop‑hook WARN now includes audit age stamp (“audit X m ago”).  
- `U-MCP-CONCURRENCY-HARDEN` (RequestSemaphore, watchdog defer) – 3 commits, 3‑of‑3 gate cleared.  
- Plan v2 (`state/shared/golf-galaxy-completion-plan-2026-06-09.md`) – 9 golf units + G10 guard + handoffs committed.  
- `G10` crash‑critical auto‑enable guard (fleet‑task‑health watch, PowerShell enable) – 292 lines, 14 tests, live R15 validation, 3‑of‑3 scrutiny passed.  
- `G5` closed redundant boost‑stamp janitor; verified by existing decay logic.  
- `G1` closed noise‑filter premise; safe `.git/info/exclude` patch applied.  
- G6 native‑Ollama `:11434` Stop‑advisory arm added to `docker-service-health-stop.mjs`; 11/11 tests, live‑validated.  
- U‑GOLF‑CRASH‑POSTMORTEM‑DIGEST – read‑only digest of `chat-crash-postmortems.jsonl`, ledger for re‑enable events; 8/8 digest tests, 4 producer tests added.  
- U‑GOLF‑TASK‑OWNER‑MAP – deterministic task→owner routing helper; pure export, 6/6 tests (`5dc70e36bd`).  
- `13596741a4` – reverse‑completeness guard for owner map; 7‑test suite, 3/3 PASS.  
- `52fc2bd0fe` – production‑heap env‑tunable scripts (`deploy/start.sh`, `start-production.*`) added to VCS.  
- GPU‑VRAM admission guard (PreToolUse Bash hook + shared lib) – commit `f3eb0c1c15`; 33/33 tests, live‑validated.  
- vLLM incorporation plan – commit `c2b86de650` (plan‑only).  
- Phase‑0 POC kit for vLLM: docker‑compose file, benchmark harness, runbook – commit `4a5ba7f59d`.  
- MCP‑server hardening: 5 additive fixes + P2‑1 depth‑guard refinement; 18 Vitest cases; 3‑of‑3 scrutiny PASS (`63d08b5eed`, `5181350fd7`).  
- Bravo lone‑surrogate 400 blocker: safe‑truncate lib, slot‑soul‑inject hardened, fallback regex fixed (`c1a50b7c99`, `3495a1dc6a`).  
- BaseRegistry/Logger: added lightweight Logger class; restored 15 registry classes; atomic persist + single‑flight init fixes (`62fe49af70`).  
- Express.json body limit raised from 100 KB to 50 MB, env‑overridable (`361725ffc2`).  
- UnhandledRejection → exit opt‑in added (`73d6fa49ea`).  
- vLLM benchmark harness fixed; A/B pipeline proven end‑to‑end.

**DECISIONS**  
- Remove redundant NVIDIA NIM containers; adopt Ollama offload.  
- Enforce ascii guard diff‑aware for code files; keep encoding guard advisory only.  
- Tune MCP heap/watchdog to 24 GB floor, 18 GB RSS threshold; watchdog < heap on 136 GB RAM.  
- Keep migration freeze until concurrency fix implemented; later remove.  
- Prioritize bounded ultracode workflow (≤3 agents) to avoid rate‑limit failures.  
- Use Ollama `gpt-oss:120b` for grunt reading, `qwen2.5-coder:32b` for code generation, `1.5b` for classification.  
- Implement 3‑of‑3 scrutiny gate for all substantive commits.  
- Design RequestSemaphore with leak‑prevention (`acquireRequestSlot`).  
- Force‑take golf slot via hook claim; skip manual bash if successful.  
- Deploy fleet‑reaper host presets per tier (Blackwell, Home, Work); run sweep on startup.  
- Remove blanket `isMigrationFreezeActive()` gate from `selectReenableTargets`; align with `aggregateHealth`.  
- Probe native Ollama before Docker in Stop hook.  
- Raise system‑viz regen heap cap to 24 576 MB; keep fleet‑proc caps at 384 MB.  
- Add pure `buildReenableLedgerRows`/`appendReenableLedger` helpers.  
- Implement task owner map for WARN advisories.  
- Golf slot owns fleet reaper; hygiene ticks via cron `e12cf005`; budget guard (R6) prevents new golf unit during tick.  
- Backend backlog (89 unwired engines, 26 wiring bridges) staged after `/compact`.  
- Pre‑compact before large build; wire GPU‑VRAM admission guard first, then vLLM migration plan via ultracode/Ollama.  
- Build #2 (GPU safety) first, then generate comprehensive plan (#1) via ultracode + Ollama.  
- Keep all fleet reapers/monitors active; bounded autonomous reap‑checks.  
- Strategic build order: Bravo P0 → MCP hardening → BaseRegistry/Logger → vLLM admission guard.  
- Operator may enable `PRISM_VRAM_GUARD_MODE=block` for vLLM guard.

**OPERATOR DIRECTIVES**  
- Start Qdrant, Docker, WSL, Ollama; ensure fleet reapers running.  
- Verify 3 NIM containers needed? Confirm Ollama fully optimized.  
- Add audit age to Stop‑hook WARN.  
- Get fleet reapers back up after PC upgrade.  
- Return to git tree work: slot‑branch commit routing active, fix conflicts/backlog.  
- Enforce advisory hooks (ascii guard, encoding guard).  
- Utilize Ollama for task efficiency when viable.  
- Find MCP server gap fills; improve performance to prevent disconnects.  
- Account for fleet spawning parallel agents.  
- `/startup-golf`: force‑take golf slot, bind handoff to `golf-work`, then run standard `/startup`.  
- `/compact` after startup: resume G5→G1→G10 sequence.  
- No manual re‑enable of Zombie Reaper v2; auto‑guard handles it.  
- AUTO‑FIX INLINE: fix bugs, stale tests, wiring immediately.  
- BUILD FOR BLACKWELL (RTX 6000 96 GB / 9950X3D / 136 GB RAM).  
- Use Ollama for grunt work; employ ultracode workflows for planning.  
- Comprehensive‑build discipline (R12 fail‑loud, R13 route, R15 wire→test→validate).  
- Read three articles via fxtwitter; use ultracode to assess & plan golf tasks.  
- Schedule recurring 30 min check (CronCreate `7,37 * * * *`) to kill zombie nodes/git/bashs and read cross‑chat open tasks.  
- Run pre‑compact (`/compact`).  
- Build GPU‑VRAM admission guard (task #2).  
- Use ultracode + Ollama to produce comprehensive vLLM integration plan (task #1).  
- Restart Docker Desktop to resolve image‑manifest corruption for `docker pull vllm/vllm-openai:latest`.  
- Review 8 go/no‑go questions in §9 of the vLLM plan; decide Phase‑0 measurement or defer.  
- “Go with your defaults” for remaining #1 and #3 items.  
- Ensure fleet reapers running and zombies cleared.  
- Continue building three pending #1 changes (pagination clamp, revocation TTL, health probes).

**FINDINGS/BUGS**  
- NIM containers caused downgrade & GPU saturation; removed.  
- GPU utilization ~5–9 %; offload take‑rate <30 %.  
- ascii guard missing; encoding guard advisory only.  
- MCP watchdog preempt restart too aggressive (3 GB RSS) → disconnects on new hardware.  
- Concurrency spike from parallel agents not bounded; watchdog unaware of inflight count → mid‑burst restarts.  
- Slot routing active; git tree backlog due to line‑ending churn.  
- Stop‑hook WARN stale due to migration freeze.  
- RequestSemaphore leak when client disconnects while queued (fixed by `acquireRequestSlot`).  
- G7 revealed Zombie Reaper v2 crash‑critical disabled; auto‑reenable guard fixed.  
- G1 premise of 22K untracked files noise false – genuine commit backlog.  
- Drift regression from adding S3 cron names; reverted.  
- G10 freeze‑gate bug → guard no‑op on real disabled task.  
- `detectInstallerDrift` #69 test failure (not regression).  
- Merge‑augmentations step in regen‑viz fails at 630 MB; flagged to Sierra.  
- Stuck git processes (~49 per ~20 min) due to index.lock; cleared by aggressive killer.  
- Node heap cap conflict: raising fleet caps worsens commit pressure.  
- PRISM_ALLOW_UNWIRED=1 flag bypasses `stop_on_unwired_assets`; removal needed.  
- MCP daemon pileup (4 daemons, 3 non‑serving); fixed via `singleton-service-guard --fix`.  
- False‑positive in `stop_on_unwired_assets` regex resolved.  
- Docker image manifest corruption: `docker pull` exits 0 but `docker image inspect` fails; requires restart.  
- Logger stub in `utils/Logger.ts`: only `log`; BaseRegistry construction failures.  
- Concurrent commit race on shared tree; resolved via pathspec commits.  
- Lone surrogate 400 error in Bravo fixed with surrogate‑safe truncate lib.  
- Express.json default 100 KB limit silently returned 413; increased to 50 MB.  
- UnhandledRejection not exit on fatal errors.

**DOMAIN SPECIFICS**  
- Engines/dispatchers: fleet‑reaper (orphan janitor, GPU/Ollama metrics), local‑llm‑bridge (NIM‑first cascade), nim‑docker‑launcher, mcp-server (`src/index.ts`), fleet‑task‑health watch (`fleet-task-health-watch.mjs`), docker-service-health-stop (`docker-service-health-stop.mjs`), active-chat-priority-boost/decay, slot‑soul‑inject, bravo context injection (`slot-soul-inject.mjs`, `galaxy-context-card.mjs`).  
- Metrics: GPU free memory, Ollama offload usage, inflight requests, RSS threshold, crash‑critical task state, WARN advisories, cron‑lock status, zombie node counts.  
- Paths: `H:/prism-slot-<name>`, `state/shared/MIGRATION-FREEZE-ACTIVE.flag`, `.claude/hooks/fleet-task-health-stop.mjs`, `scripts/lib/gpu-vram-guard.mjs`, `.claude/hooks/gpu-vram-admission-guard.mjs`, `chat-crash-postmortems.jsonl`, `fleet-reaper-sweep.mjs`, `06-aggressive-killer.mjs`.

**TOOLS USED**  
- PRISM helpers: `ascii-guard.mjs`, `encoding-guard.mjs`, `fleet-reaper-sweep.mjs`, `nim-docker-launcher.mjs`, `chat-slots.mjs`.  
- Docker Desktop, WSL2, Ollama CLI (`gpt-oss:120b`, `qwen2.5-coder:32b`, `1.5b`).  
- Node scripts for slot claiming, reaper sweep, watchdog logic; Express.js server.  
- Ultracode workflow + Ollama for planning and POC generation.  
- CronCreate (`e12cf005`, `7,37 * * * *`) for hygiene loops.  
- Docker Compose (`docker-compose.local-llm.yml`), benchmark harness (`scripts/vllm-poc/benchmark.mjs`).  
- Node 20+, Git, TypeScript compiler (tsc), Vitest, esbuild.

**OPEN THREADS**  
- Implement inflight‑aware watchdog; add bounded concurrency cap/queue on `buildRequestServer()` for MCP.  
- Add bounded ultracode workflow (≤3 agents) to avoid rate‑limit failures.  
- Remaining golf units: G9 rest, G6, G2, G8, G4 (next loop iterations).  
- Auto‑sync of `KNOWN_PRISM_TASKS` to capture new cron installers.  
- Pending #1 changes: pagination clamp, token‑revocation TTL, response truncation + deep health probes – edit, test, pass 3‑of‑3 scrutiny.  
- Merge‑augmentations bug in regen‑viz pending fix by Sierra.  
- Monitor for node heap cap adjustments under fleet commit pressure constraints.  
- Docker Desktop restart to complete vLLM image pull; operator decision on vLLM go/no‑go (Q3 and plan questions).  
- Fix Logger stub / BaseRegistry construction issue before further registry changes.
