# cam session 86373eb3 (2026-06-09, 30.9MB, spine 357KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Added `Launch‑PRISM‑Fleet‑3win.ps1` (3‑window launcher) and updated desktop shortcuts (`PRISM Fleet → 4‑window fallback`, `PRISM Fleet (3‑window)`).
- Created `capture‑accounts‑runbook.md`; added account capture CLI `capture‑claude‑credentials.mjs`.
- Set `LOCAL_LLM_BACKEND=ollama`; stopped all NIM containers (`restart=no`) and guarded `nim-autostart.mjs` to skip when backend is ollama.
- Removed compose‑ollama from `docker‑intel‑autostart.mjs`; updated `docker-compose.yml`: `KEEP_ALIVE=30m`, `MAX_LOADED_MODELS=4`.
- Infra hardening: NIM disabled, Ollama only; recall hooks enabled (`memory-index-precheck`, `wiki-precheck`), master‑index OOM guard added.
- Wired reverse Obsidian mirror as PostToolUse (`h-to-c-obsidian-mirror.mjs`).
- Patched tribal installers to write cron runners to `.claude/cron-runners/`; created new synthesis‑cron installer (weekly‑memory‑synthesis, run‑daily‑context, run‑knowledge‑distillation).
- Added `vault-to-gnn-refpool.mjs` → 4 GNN ghosts in `system‑graph.json`.
- Fixed `seed-ghost-from-unwired.mjs` streaming I/O; now succeeds on 642 MB graph.
- Created `vault-to-lora-dataset.mjs` → 245 Alpaca pairs, CRLF removed, `.md` stripped.
- Generalized transcript miner (`mine-galaxy-transcripts.mjs`) with registry for all 34 galaxies, slice cap (MAX_SLICES_PER_TX=14), 900 s synthesis timeout; added `--next-unpopulated --backend-only`.
- Updated `ollamaCall()` per‑call timeouts (`SYNTH_TIMEOUT_MS=900000`); all unit tests pass.

**DECISIONS**  
- Drop NIM stack; run Ollama only (avoids OOM under 16 GB WSL cap).  
- Keep native Ollama auto‑start via scheduled task `PRISM Ollama Serve`.  
- Remove compose‑ollama from autostart scripts.  
- Disable duplicate RAG hooks; wire reverse mirror to close vault gap.  
- Enable recall hard‑off by default, toggle via CAG router.  
- Do not re‑register brain‑refresh with `--with-viz` (30 min regen conflict).  
- Adopt one‑galaxy‑per‑task strategy (`--next-unpopulated --backend-only`) to avoid wall timeouts.

**OPERATOR DIRECTIVES**  
- Log into each of the 6 Claude Max accounts once; run:  
  `node capture‑claude‑credentials.mjs <account-N> <label> <pos>`  
- Verify scheduled task `PRISM Ollama Serve` is enabled and runs at logon.  
- After account capture, double‑click **PRISM Fleet (3‑window)** to launch; run `Launch‑PRISM‑Fleet‑3win.ps1 -DryRun` first.  
- Register all pending scheduled tasks with elevated PowerShell:  
  ```powershell
  powershell -NoProfile -ExecutionPolicy Bypass -File H:/prism/.claude/helpers/install-synthesis-crons.ps1 -RunNow
  powershell -NoProfile -ExecutionPolicy Bypass -File H:/prism/.claude/helpers/install-tribal-consolidate-cron.ps1 -RunNow
  powershell -NoProfile -ExecutionPolicy Bypass -File H:/prism/.claude/helpers/install-tribal-promotion-cron.ps1 -RunNow
  powershell -NoProfile -ExecutionPolicy Bypass -File H:/prism/.claude/helpers/install-wiki-tribal-audit-task.ps1 -RunNow
  ```  
  Verify all six tasks show `LastResult 0x0`.  
- Mine remaining backend galaxies sequentially:  
  `node mine-galaxy-transcripts.mjs --next-unpopulated --backend-only`  
  After each run, execute:  
  `build-memory-index-sidecar.mjs` → `build-memory-embeddings-sidecar.mjs --resume` → `galaxy-synthesis-refresh.mjs`.  
- Repeat the same pattern for primary app domains (speed‑feed, post‑processor, cam, mill, lathe, wedm, cad).  
- Monitor GPU usage; pause mining if embeddings sidecar stalls.

**FINDINGS/BUGS**  
- NIM containers OOM (`exit 137`) due to 16 GB WSL cap; PRISM probed dead NIM on every request → latency.  
- Qdrant collections are 768‑dim, matching Ollama’s `nomic‑embed`; no re‑index needed.  
- Duplicate MCP daemons cleared; “3 daemons, reap 2” warning transient.  
- Compose `mcp` service name mismatch caused “no such service: mcp” errors.  
- Port‑11434 collision from native Ollama vs compose ollama removed.  
- Master‑index hook OOM fixed with heap‑guard (201 MB sidecar).  
- Recall hard‑off enabled; vault now responds.  
- Tribal cron runners moved to durable `.claude/cron-runners/`.  
- Seed‑ghost crash (`ERR_STRING_TOO_LONG`) resolved via streaming I/O.  
- LoRA dataset CRLF and `.md` leaks fixed; final JSONL has 0 escaped CRs.  
- `classifyHandoff` regex hardened; recovered ~250 handoffs.  
- 33/34 galaxies covered; missing galaxy `dormant-data` legitimately empty.  
- Reaper killed background `--all` runs; must run foreground or schedule protected tasks.  
- Embeddings sidecar rebuild required after mining to expose new memos.  
- Initial slice cap and NUM_CTX restore fixed crash on large transcripts.  
- Synthesis timeout raised from 240 s to 900 s resolves token‑optimization aborts.  
- Grep/timeout wrapper removed (misleading exit codes).  
- Dormant‑data stopped mid‑synthesis after 5 of 6 transcripts (~1100 s timeout).  
- Batch of 5 light galaxies failed; single‑galaxy tasks reliable.  
- Knowledge‑conversion startup returned exit 255 with no output.  
- Backend phase closed unexpectedly after cad domain start.

**DOMAIN SPECIFICS**  
- Slots/windows layout: 3 windows, 19 slots (4‑window fallback).  
- Account switching via `claude-account-lib.mjs` & `fleet-wake-sequencer.mjs`.  
- Local LLM routing in `local‑llm‑bridge.mjs`.  
- Docker compose services: `prism-server`, `qdrant`, `ollama`.  
- WSL memory cap set in `.wslconfig`.  
- Engines/dispatchers: `memory-index-precheck`, `wiki-precheck`, `master-index-search-lib`, `CAG router`, `recall-inject` counters, planned take‑rate telemetry.  
- Paths: vault at `H:/prism/.claude/knowledge/memories/**`; cron runners in `.claude/cron-runners/`.  
- Engines/actions: `system-viz` (graph read/write), LoRA/NN/GNN, Obsidian sync, RAG corpus, QDRANT, ultracode, Ollama local LLM.  
- Dispatchers: `mcpToolToDispNodeId`, `isValidDispatcher`, `galaxy-synthesis-refresh`.  
- Metrics: 4 GNN ghosts, 245 LoRA pairs, 2 096 topic handoffs, per‑galaxy transcript count/digest size/synthesis duration, content discovery cache hit ratio.  
- Mining engine: Ollama MAP (`gpt-oss:20b`) → REDUCE → session digest → cross‑session SYNTHESIS (`gpt-oss:120b`).  
- Discovery: `discoverByContent` reads transcript spine, extracts slot hint & topic keywords; caches assignments.  
- Indexing: `build-memory-index-sidecar.mjs` (BM25), `build-memory-embeddings-sidecar.mjs` (`nomic‑embed-text`).  
- Synthesis refresh: `galaxy-synthesis-refresh.mjs` clusters memories via BM25+dense recall, recomputes cluster hash, writes `<galaxy>/MEMORY.md`.

**TOOLS USED**  
- PowerShell: `Launch‑PRISM‑Fleet‑3win.ps1`, `slot-tab-boot.ps1`, `install-synthesis-crons.ps1`, `install-tribal-consolidate-cron.ps1`, `install-tribal-promotion-cron.ps1`, `install-wiki-tribal-audit-task.ps1`.  
- Node CLI: `capture‑claude‑credentials.mjs`, `nim-autostart.mjs`, `local-llm-bridge.mjs`, `master-index-precheck-inject.mjs`, `memory-index-precheck-inject.mjs`, `wiki-precheck-inject.mjs`, `h-to-c-obsidian-mirror.mjs`.  
- Docker compose: `docker-compose.yml`.  
- Scripts: `vault-to-gnn-refpool.mjs`, `seed-ghost-from-unwired.mjs`, `graph-io.mjs`, `vault-to-lora-dataset.mjs`, `mine-india-transcripts.mjs`, `galaxy-synthesis-refresh.mjs`, `ultracode`, `ollama-prism-bridge.mjs`.  
- Mining: `mine-galaxy-transcripts.mjs`, `galaxy-mining-registry.mjs`, `build-memory-index-sidecar.mjs`, `build-memory-embeddings-sidecar.mjs`.  
- LLMs: `gpt-oss:20b`, `gpt-oss:120b`, `nomic‑embed-text`.

**OPEN THREADS**  
- Verify native Ollama auto‑start remains enabled after future updates.  
- Monitor for accidental NIM container reintroduction; confirm no port conflicts with compose services.  
- Keep account capture runbook updated if new accounts or slots added.  
- Elevated PowerShell registration of six scheduled tasks (operator required).  
- Confirm `find-connections.mjs` is not a cron‑ready runner; consider wrapper.  
- Evaluate Obsidian integration as training data for LoRA and label/feature source for GNN/NN, ensuring binary artifacts stay on disk.  
- Run generalized transcript miner across all galaxies (goal).  
- Ensure no clobber with India session’s in‑flight work.  
- Resolve scheduled‑task elevation issue.  
- Commit new files after final scrutiny gate passes.  
- Mine remaining backend galaxies: `agent-orchestration`, `discovery`, `dormant-data`, `knowledge-conversion`, `corpus-aggregation`, `database-expansion`.  
- Run primary app domain mining after backends done.  
- Rebuild embeddings sidecar after each mining run.  
- Schedule protected foreground tasks to avoid reaper kills.  
- Verify final brain compounding (`galaxy-synthesis-refresh`) for all galaxies.  
- Document operator instructions for `--next-unpopulated --backend-only` workflow.  
- Finish synthesis for dormant‑data (resume after 5–6 transcripts).  
- Resolve cad first domain timeout; investigate tool timeout cause.
