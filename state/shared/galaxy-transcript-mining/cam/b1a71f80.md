# cam session b1a71f80 (2026-06-09, 0.7MB, spine 8KB, 1 slice(s), model gpt-oss:20b)

**DECISIONS**  
- Set default model to **fable** (`"model": "fable"`).  
- Enable **ultracode** and **workflows** (`"ultracode": true`, `"enableWorkflows": true`).  
- Keep existing `effortLevel: xhigh` (already set).  
- Note: changes apply to *new* sessions; current session remains Opus 4.8 unless `/model fable` is run.

**OPERATOR DIRECTIVES**  
- “can you make the default model fable ultracode?”  
- “Want me to set up the reliable combination instead — keep `model` on Fable for speed but leave ultracode off until we confirm Fable supports it, or pin Opus specifically so ultracode is guaranteed to engage?”

**FINDINGS/BUGS**  
- Uncertain whether **fable** supports `xhigh` effort / ultracode; flag may silently no‑op.  
- If “ultracode is on” reminder does not appear in a fresh session, ultracode likely requires Opus.

**DOMAIN SPECIFICS**  
- Settings hierarchy: user → project → local (`~/.claude/settings.json`, `.claude/settings.json`, `.claude/settings.local.json`).  
- Hook events: `PreToolUse`, `PostToolUse`, `PreCompact`, `PostCompact`, `Stop`, `Notification`, `SessionStart`.  
- Merge strategy: read existing file, preserve arrays, append new entries.  
- Permission syntax: exact match, prefix wildcard, tool‑only.  
- Environment variables and model/agent config sections.  
- `c-to-h-mirror` hook auto‑mirrors changes to `H:` drive.

**TOOLS USED**  
- **Configuration skill** (managing settings).  
- **Read tool** (to fetch current JSON before editing).  
- **Edit tool** (to modify JSON).  
- **AskUserQuestion** for ambiguity resolution.  
- `/config` slash command suggestion for simple keys.  
- Hook system (`PreToolUse`, `PostToolUse`, etc.) for automated actions.

**OPEN THREADS**  
- Verify that **fable** supports ultracode with `xhigh` effort (check “ultracode is on” reminder).  
- Decide whether to pin Opus for guaranteed ultracode support or keep Fable and disable ultracode until confirmation.  
- Ensure new sessions load the updated model; current session remains unchanged unless `/model fable` is invoked.
