# cam session 04256fb3 (2026-06-17, 48.6MB, spine 205KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `[GOLF-FLEET-HYGIENE]/U-*` pending on slot/golf.  
- Commit `4c7fba6287`: suppressed “MCP BRIDGE DOWN” banner, probe timeout 1 s→3 s, knob `PRISM_MCP_FLEET0_BANNER`.  
- Commit `9da42f74c6`: gated broadcast‑reconnect in `mcp-bridge-enforce.mjs`; deterministic tests added.  
- Commit `89cd1b5da5`: extracted capacity guard into `resolveMcpCapacity`, added test `mcp-capacity-contract.test.ts`.  
- Handoff file: `HANDOFF-claude-04256fb3-mcp-reliability.md`.

**DECISIONS**  
- Fleet‑reaper ownership moved from alpha to golf (2026‑05‑16); golf is canonical owner of all fleet‑hygiene tasks; reaper sweep runs on every `/checkin-golf`.  
- Do not kill windows with stale heartbeats; surface them instead.  
- `ask` mode in `node scripts/ask-ollama.mjs` accepts inline text only; `triage`, `summarize`, `explain` require file paths.  
- Hermes proxy (`:8645`) used only when a stronger managed model is needed.  
- Commit one hygiene unit per tick if clear; otherwise report clean and persist `reference_*.md` to Obsidian.  
- Treat 0 transient bridges as “idle”; keep per‑chat sentinel for real bridge death.  
- Keep `/health` probe (3 s timeout) as sole source of truth for server health.  
- Preserve concurrency limits (64 concurrent requests / 512 queue); expose via `resolveMcpCapacity`.  
- Add end‑to‑end regression guard verifying a 16‑chat burst can be handled without shedding beyond configured limit.  
- Continue using fleet‑hygiene cron (~20 min) and perf‑watch cron (~13 min).

**OPERATOR DIRECTIVES**  
- “reorientate to most recent sessions and continue in engineered loops and harnesses and crons utilizing hermes agents and obsidian vault and ollama offloading optimally.”  
- “im heading out for a bit, make sure fleet reapers and monitors stay active! monitor pc performance and task manager periodically in loops.”  
- Register the `PRISM Fleet Task Health` scheduled task (requires elevated PowerShell/UAC).  
- Inspect frozen romeo (`claude-a8796b17`) window: alive but idle ~49 min.  
- Update ask‑ollama documentation: `triage`/`summarize` are FILE‑only modes; `ask` is inline.

**FINDINGS/BUGS**  
- Legacy `golf-slot-write-allowlist.mjs` blocks writes outside legacy hygiene allowlist; must bypass or disable.  
- Misrouted india `/goal` cron orphaned in golf terminal; removed.  
- Frozen chat romeo (claude-a8796b17) alive but heartbeat‑dead; surfaced, not killed.  
- `ask-ollama` bug: inline text treated as file path for triage/summarize; fixed by using ask mode only.  
- GPU usage spiked to 81 GB (gpt‑oss:120b) legitimate; prewarm floor prevented unnecessary kills.  
- “MCP BRIDGE DOWN” banner fired on idle servers; suppressed by gating on health probe and bumping probe interval to 3 s.  
- Real disconnects ≈4 over fleet history; bridge spawn count ~1,486 across sessions; 0‑bridge state expected during idle.  
- Server handles ≥32 concurrent `/initialize` requests in <1 s; concurrency limits not saturated.  
- Idle‑fleet residual: `/mcp reconnect` broadcast fires every ~15 min when health cache stale (UserPromptSubmit throttle).  
- CPU transient 86%; GPU temp 79 °C; no orphan node/python processes.

**DOMAIN SPECIFICS**  
- Engines/Actions: `fleet-reaper-sweep.mjs`, `chat-slots.mjs`, slot‑binding helper, reaper guardian hook `golf-slot-reaper-guardian.mjs`, `mcp-bridge-enforce.mjs`, `mcp-connectivity-check.mjs`, `resolveMcpCapacity`, `readCachedServerUp`.  
- Dispatchers: AGENT_CHAT bus for status posts; `mcp-broadcast-reconnect-inject.mjs` for reconnect signals.  
- Metrics/Paths: PowerShell CPU/RAM snapshots, `nvidia-smi` GPU stats, scheduled task states (`PRISM Fleet Reaper`, `PRISM Fleet Memory Monitor`), `reference_*.md` persistence to Obsidian.

**TOOLS USED**  
- PRISM scripts: `fleet-reaper-sweep.mjs`, `chat-slots.mjs`.  
- Node helpers: `stable-session-id.mjs`, `per-agent-handoff.mjs`, `ask-ollama.mjs`, `ask-hermes.mjs`.  
- PowerShell & `nvidia‑smi` for performance snapshots; `Get-ScheduledTask` queries.  
- Windows Scheduled Tasks (`PRISM Fleet Reaper`, `PRISM Fleet Memory Monitor`).  
- Git workflow with lane guard; commits via pathspec to bypass shared‑tree restrictions.  
- Build/test tools: `tsc`, `vitest`, `esbuild`.

**OPEN THREADS**  
- Commit hygiene unit `[GOLF-FLEET-HYGIENE]/U-*`.  
- Register missing durable task `PRISM Fleet Task Health` scheduled task.  
- Resolve legacy allowlist hook blocking writes.  
- Ensure reaper sweep logic handles orphan/mcp‑zombie detection correctly.  
- Persist `reference_*.md` only on real anomalies (currently none detected).  
- Finalize & commit end‑to‑end load‑test regression guard; verify all tests pass before merging.  
- Monitor GPU temperature trend over time to stay below 83 °C threshold.  
- Resolve idle‑fleet broadcast issue via health cache refresh or hot‑path enforce‑gate change (requires 3‑of‑3 review).  
- Update ask‑ollama mode documentation.  
- Monitor frozen romeo window for potential cleanup.
