# cam session 6a8a0fc5 (2026-06-23, 50.3MB, spine 400KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑QP‑TSX‑REEXEC‑E2E‑ENV‑ROBUST` – 5fc84e6fbf (20/20 E2E tests)  
- Xray 1: `region‑classifier‑lib.mjs` – 0a41c90a4c  
- Xray 2: `region‑glue‑lib.mjs`  
- Xray 3: `region‑classify.mjs` (DI orchestrator + live curl→Ollama)  
- Xray 5: wired `--region-route` into `validate‑perfect‑parts.mjs` – e9e338adfd  
- Xray 8: added `buildRegionRoutedFused`, exposed `fused` in `extractWithRegionRouting` (111 insertions)  
- Xray 9: added summary‑recompute to `buildRegionRoutedFused`  
- d13211934f – dense‑page rescue: `buildRegionRoutedFused` synthesizes `summary.n_models`  
- f93c14d6b1 – wired `--region-route` into live training cron (P1.5 consumer)  
- 7c8ca636ba – doc‑reflect of P1.5 arc in durable backlog/design doc  
- ca91dcb5d5 – region routing underperformed single run (0 recall, 1/3 pages)  
- 08f77dee4e – 1‑seed live VALIDATE instrumentation flagged for revision  
- 7a58fb6d0a – 3‑seed close: 9/9 page‑occurrences OK, recall 0.4286 stable  
- 6a9f5253f6 – U‑XRAY‑GDT‑CORPUS‑SCAN‑ROUTE (PDF‑parse guard, OCR routing)  
- be6099d9c4 – GD&T corpus ingestion arc documentation update  
- 2d77536fe8 – U‑XRAY‑GDT‑WATCHER‑LANE‑CAPTURE (stdout parsing & logging)  
- a783df2419 – `fuseEnsemble` returns full non‑dim fields (gdt, notes, profiles, surface_finishes)  
- abc63f4874 – added `gdt_labels` tiering to OCR training loop; runner writes trainable labels  
- e7fd24791b – `mergeRegionFused` restores GD&T/notes/profiles on dense‑page rescue (recall‑first union)  
- 3a330195d6 – `buildGdtRecords` + `VERIFY-gdt.csv` for operator gold‑verify gate  
- `U-XRAY-OCR-ADAPTER-WIRE-EXEMPT` applied then reverted; inline docstring notes UNWIRED state  

**DECISIONS**  
- Adopt R13 cores‑first strategy: pure lib → glue → live orchestrator.  
- Use DI‑testable orchestrator pattern for GPU‑free unit tests.  
- Region routing is rescue for dense pages; no `num_ctx` tweak needed.  
- Hybrid fused function + summary recompute unblocks training cron; keep `--region-route` opt‑in default‑off.  
- Complete live router coding (Sonnet), octopus picker, graph reconcile → descend NEVER‑IDLE ladder.  
- Defer multi‑part `--region-route` runs until VRAM headroom & survivable runway; current host near 93 GB limit.  
- Add per‑page drop attribution and cross‑seed consumer to instrumentation; confirm recall 0.4286 stable across seeds.  
- Keep GD&T hardening complete, FIXES green; WIRINGS deferred/peer‑domain; GHOST builds blocked by tool‑budget ceiling.  
- Reject `// WIRE‑EXEMPT` tag; maintain UNWIRED until actual wiring.  

**OPERATOR DIRECTIVES**  
- `/checkin-xray … /loop [10m]`: finish remaining back‑end tasks, prioritize xray, improve OCR/blueprint reading, continue closed‑loop training (Ollama offloading, Hermes agents, parallel agents).  
- Alpha/Zulu/Golf/Sierra loops: finish in‑flight unit → FEATURE‑ROUTING arc → descend NEVER‑IDLE ladder; commit `[MAIN‑FORCE]`; never idle while token budget green.  
- Checkpoint at YELLOW → auto‑compact reset; re‑read handoff, roadmap, Obsidian brain each iteration.  
- Maintain units‑first, R12 fail‑loud, R13/R15/R16 comprehensive scrutiny.  

**FINDINGS/BUGS**  
- E2E test failure due to Node 24 assumption fixed by asserting honest JSON verdict.  
- Full‑page OCR returned 0 dims on dense 3400×2200 page; region routing recovers 28 dims.  
- Signature mismatch in `buildRegionRoutedFused` resolved.  
- Cron wiring dropped non‑dimension labels; hybrid fused preserves full schema and recomputes `n_hallucination_candidates`.  
- Mid‑iteration git‑sync clobber fixed by committing each unit before long dispatch.  
- GD&T/notes merge moot for cron (`buildTrainsetRow` reads only `dimensions` + `summary.n_models`).  
- Region‑route instrumentation: per‑page drop attribution added; 3‑seed run shows recall 0.4286, more dims than full‑page.  
- BlueprintOCRAdapter flagged as unwired; marked wire‑exempt and audit now correct.  

**DOMAIN SPECIFICS**  
- Engines/dispatchers: Ollama (`qwen3-vl`, `qwen2.5-coder`), Sonnet agent, Hermes agents, Octopus consensus, Opus fallback.  
- Actions: `audit-unwired-engines.mjs`, `extractWithRegionRouting`, `buildRegionRoutedFused`, `mergeTiledDimensions`, `region-classifier-lib.mjs`, `region-glue-lib.mjs`, `validate-perfect-parts.mjs`, `buildTrainsetRow`, `classifyActiveLearning`, `vision-ensemble-fuse.mjs`, `ocr-training-loop-lib.mjs`, `buildGdtRecords`.  
- Metrics: recall‑first union, data‑loss‑safe contract (`full_page` fallback), `n_hallucination_candidates` recompute, per‑page drop stats.  
- Paths: `H:/prism/.claude/helpers/chat-slots.mjs`, `system-viz/master-index`, `node scripts/fleet-survival-status.mjs`, `state/shared/specs/BRAVO-HERMES-ZULU-OPEN-TASKS-LEDGER.md`.  

**TOOLS USED**  
- PRISM helpers: `/dedup`, `/wiki-query`, Obsidian vault, `slot-bind-enforce.mjs`, `chat-slots.mjs`, canonical `/checkin` pipeline, `loop-state.mjs`, `audit-unwired-engines.mjs`, `self-compact.mjs`, `fleet-reaper`.  
- Node utilities: `node scripts/fleet-survival-status.mjs`, `node scripts/self-compact.mjs`, `node scripts/audit-unwired-engines.mjs`, `node scripts/system-viz-query.mjs`.  
- AI stack: Ollama coder‑ensemble (`qwen3‑coder 30b`, `qwen2.5‑coder 32b`), Sonnet agents, Hermes orchestrator, Octopus consensus.  

**OPEN THREADS**  
1. Finish FEATURE‑ROUTING model‑routing “both” arc (live router → Sonnet, octopus picker, graph reconcile).  
2. Run `audit-unwired-engines.mjs` and GHOST builds for own‑domain backlog; 4 peer‑domain unwired engines pending wiring.  
3. Descend NEVER‑IDLE ladder into remaining backlog: FIXES → WIRINGS → GHOST → MISC.  
4. Queue multi‑part `--region-route` run once VRAM headroom & survivable runway available.  
5. Operator‑stage GD&T source PDFs for P2.10 ingestion; callout‑GT dataset needed to broaden region‑route default‑on.  
6. Future GPU‑heavy tasks: triangulation (P2.7), GD&T refinement (P2.10).
