# cam session f593aee3 (2026-06-09, 7.9MB, spine 49KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `c6748e2a11` – Fixed dead‑model tag in `cad-ollama-archetype-label.mjs`; dynamic model selection, removed hard‑coded `qwen2.5-coder:7b`.  
- `2efed07642` – Clean commit of the corrected script (pathspec‑only).  

**DECISIONS**  
- Use **ultracode** for high‑level planning/roadmap; use **Ollama local LLMs** for grunt tasks (search, read, coding).  
- Replace hard‑coded model tag with runtime discovery of the strongest installed Ollama model (`gpt-oss:120b` → fallback to `qwen2.5-coder:32b`).  
- Keep all geometry/units/GD&T logic on Claude; only descriptive/advisory data may be sent to Ollama (red‑lines).  
- Add a `--limit N` flag for bounded runs and expose `TIMEOUT_MS` env var so inference can succeed under GPU contention.  

**OPERATOR DIRECTIVES**  
- Continue the loop (`/loop /goal …`).  
- Execute the roadmap workflow (`wf_7909e40d-9ee`) to order remaining units, hardware upgrades, and closed‑loop learning tasks.  
- Apply the A1 fix (already committed) and monitor for runtime errors.  

**FINDINGS / BUGS**  
- Dead model tag caused silent fallback to rule‑based classifier.  
- ReferenceError at line 138 (`OLLAMA_MODEL` constant missing). Fixed by removing the constant and using `process.env.OLLAMA_MODEL`.  
- Runtime timeout (30 s) aborted inference on `gpt-oss:120b`; GPU contention is the root cause, not model choice.  
- Need to adjust `TIMEOUT_MS` or queue inference to avoid fallback.  

**DOMAIN SPECIFICS**  
- **Slot/delta** branch – 408 commits ahead of shared `cad-fusion-live-ms0`.  
- **CAD‑TRAINING‑PIPELINE**: closed‑loop print→CAD→compare→correct; self‑improving for die/bushing/extrude_punch/casing.  
- **CAD‑FUSION‑LIVE‑MS0**: live Fusion API server (ports :18365/:18630/:18632/:18638).  
- Models: `gpt-oss:120b` (top), `qwen2.5-coder:32b`, `gpt-oss:20b`.  
- Tools/scripts: `cad-ollama-archetype-label.mjs`, `host-aware-synthesis-model.mjs`, `resolveSynthesisModel()`, `callLocalModel()`.  

**TOOLS USED**  
- Ultracode workflow `wf_7909e40d-9ee` (roadmap planning).  
- Local Ollama API (`/api/tags`).  
- Git for slot binding and commit.  
- Node scripts: `cad-ollama-archetype-label.mjs`, `host-aware-synthesis-model.mjs`.  

**OPEN THREADS**  
1. Merge the 408‑commit `slot/delta` arc into `cad-fusion-live-ms0`.  
2. Run the parked revolute‑assembly live proof (`node scripts/cad-fusion-assembly-poc-live.mjs --port :18365`).  
3. Resolve GPU contention for Ollama inference (increase timeout or queue).  
4. Finalize closed‑loop learning pipeline and ensure all units complete under new hardware.  
5. Verify that no further reference errors exist in the updated script.
