# cam session f6c0a693 (2026-06-26, 3MB, spine 33KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hermes desktop app restored, updated to hermes‑agent v0.17.0 (HEAD `7cd5eaa64`).  
- GUI rebuilt (`npm run pack`); new renderer bundle `index-BP2lCrYS.js`.  
- Rollback backup `win-unpacked.working-bak` retained.

**DECISIONS**  
- Adopted *full update + rebuild the GUI* path for Hermes CLI.  
- Keep rollback net until operator visually confirms GUI; deletion deferred.  
- Persist knowledge in wiki entry `hermes-desktop-app-dead-venv-deps-and-full-rebuild-2026_06_25.md`.  
- No auto‑exec of ollama offload; awaiting operator confirmation.

**OPERATOR DIRECTIVES**  
- “please update hermes cli and get the hermes app working again. its been down for over a week.”  
- Operator chose *Full update + rebuild the GUI*.

**FINDINGS/BUGS**  
- Desktop backend crashed after 06‑17 auto‑update: missing `annotated_doc`, `fastapi`, `uvicorn`.  
- Venv dependency install never finished; proxy `:8645` remained healthy.  
- Offload suggestion→execution gap: 654+61 suggestions, only ~1 direct `ask‑ollama`; off‑Claude work via Hermes is active.

**DOMAIN SPECIFICS**  
- Two Hermes systems: `:8645` proxy (ask‑hermes) and Nous desktop app (Electron + Python).  
- PRISM slot system: zulu orchestrator, self‑exempt slots `["zulu","golf"]`.  
- Live reconciler `$0`: 6 shipped builds, 0 open, 1 unknown.  
- Offload decision rate ≈43% (61/141 suggestions executed via Hermes).  
- Scheduled tasks: PRISM Hermes Proxy + 8 cron jobs.

**TOOLS USED**  
- PRISM slot helpers (`slot-bind-enforce.mjs`, `chat-slots.mjs`), `/checkin.md` pipeline.  
- Python `pip install -e .` for venv deps.  
- npm workspaces monorepo; `npm run pack` (electron‑builder).  
- PowerShell cmdlets (`Get-CimInstance`, `Get-ScheduledTask`).  
- Wiki entry creation via memory reference file.

**OPEN THREADS**  
- Operator must visually confirm Hermes GUI (especially Settings page).  
- Decision to delete rollback backup pending confirmation.  
- Potential further hardening of ollama offloading if requested.
