# cam session 0170cb0a (2026-05-20, 0.8MB, spine 7KB, 1 slice(s), model gpt-oss:20b)

**DECISIONS**  
- Golf slot is now a normal work slot (no `--golf` flag); legacy hygiene mode removed from `/checkin-golf`.  
- Fleet‑reaper ownership transferred from alpha to golf on 2026‑05‑16; doctrine enforced via `golf-slot-reaper-guardian.mjs`.  
- `/checkin-golf` always runs fleet‑reaper sweep and ensures the durable 5‑min scheduled task.  
- Allowlist hook (`golf-slot-write-allowlist.mjs`) remains active unless explicitly bypassed or disabled.

**OPERATOR DIRECTIVES**  
- Choose one allowlist bypass:  
  - `export PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1` (session scoped) **or**  
  - Edit `C:/Users/wompu/.claude/settings.json` to remove the PreToolUse entry for `golf-slot-write-allowlist.mjs`.  
- Run `/fleet-reaper-home` or `/fleet-reaper-work` once per machine to lock host presets; thereafter use `/fleet-reaper`.

**FINDINGS/BUGS**  
- Fleet‑reaper sweep: ok=true, 32 targets, 1 unowned candidate.  
- Monitor armed (`bcs25oher`) and reaped orphan PID 22940.  
- Scheduled task `PRISM Fleet Reaper` disabled (4 missed runs); needs enabling via PowerShell.  
- Docker daemon down; Ollama reachable, coordinator below pressure floor—advisory only.  
- Branch `cad-fusion-live-ms0` diverged 381 ahead / 1 behind origin; 16 411 uncommitted changes.  
- Bash mangled `/TN` flag during scheduled‑task check (MSYS path translation issue).

**DOMAIN SPECIFICS**  
- Slot binding via `chat-slots.mjs`: reclaim, claim with `--preferSlot golf`, no `--golf`.  
- Fleet‑reaper JSON metrics: `slots["owned-by-crashed"]`, `softRelief.{priorityDemoted,workingSetTrimmed,rssReclaimedBytes}`, `gpu.{freeMb,utilizationPct}`, `ollama.{reachable,loaded[]}`, `coordinator.{shouldPrewarm,prewarmFired,hintWritten,thresholdDelta,hintMode}`.  
- Paths: `H:/prism/.claude/helpers/chat-slots.mjs`, `H:/prism/scripts/fleet-reaper-sweep.mjs`, `H:/PRISM/.claude/commands/`.  
- Metrics logged to `state/shared/golf-bypass.jsonl` when bypass env var set.

**TOOLS USED**  
- PRISM skills: `/checkin-golf`, `/fleet-reaper`, `/fleet-reaper-home`, `/fleet-reaper-work`.  
- Hooks/scripts: `golf-slot-reaper-guardian.mjs`, `golf-slot-write-allowlist.mjs`, `chat-slots.mjs`, `fleet-reaper-sweep.mjs`.  
- External tools: PowerShell (`Enable-ScheduledTask`), Docker (named‑pipe status).  

**OPEN THREADS**  
- Decide and apply allowlist bypass method.  
- Enable the disabled scheduled task (`Enable-ScheduledTask -TaskName 'PRISM Fleet Reaper'`).  
- Confirm host preset selection (home vs work) by running `/fleet-reaper-home` or `/fleet-reaper-work`.  
- Resolve Docker daemon downtime if persistent; otherwise monitor for future re‑starts.
