# cam session 9dc60ef7 (2026-06-19, 20.8MB, spine 120KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `LLMEngine.query` rewritten (38fde7cc48): Ollama‑first → Claude backup → deterministic offline.  
- Capability‑escalation logic tuned (dd0b90b0f0).  
- System‑prompt override added to `LLMQuery.system` (42a6108e07).  
- `ManusATCSBridge.callClaude` migrated to use `llmEngine`; removed `hasValidApiKey`, added R12‑honesty (offline → FAILED) (9faccd3cea).  
- `manusDispatcher` relaxed gates, R12 offline → FAILED (8738d2eed5).  
- `ralphDispatcher` throw‑on‑offline, honest provenance (b5f0dd5006).  
- `atcsDispatcher` throw‑on‑offline, real‑provider record (9b371ffd1c).  
- `AgentExecutor` dropped Anthropic SDK; no‑simulation R12 throw (c4d1f8f24b).  
- Vision substrate: `LLMEngine.queryVision` with Ollama‑vision‑first → Claude vision backup → offline; image normalization & `OllamaClientEngine` passthrough (3f7df39916 + a879e02a0e).  
- `VisionActionAnalyzerEngine` migrated onto `queryVision`, removed Anthropic SDK, R12 throw‑on‑offline (0eb8353d24).

**DECISIONS**  
- Provider ladder: Ollama‑first free → Claude backup on capability/availability → deterministic offline; $0 launch cost.  
- Single runtime chokepoint (`LLMEngine`) for all text & vision calls to simplify routing, testing, metrics.  
- R12 honesty pattern: offline results marked FAILED or throw, never masquerading as real work.  
- Gate relaxation: remove `hasValidApiKey`/`getApiKey`; free path reachable without Claude key.  
- Two‑arm scrutiny gate (reviewer + code‑analyzer) per file before commit.

**OPERATOR DIRECTIVES**  
- Continue autonomous development via harnesses/loops/crons; finish India tasks, then focus on AI system improvements (post‑processor generation, master post, SFC, CAD/CAM, mill/lathe/wedm wizard AI).  
- Use Ollama models for CAD/CAM drawing & print‑to‑CNC to keep product free at launch.  
- Ensure backend remains robust, streamlined, reasoned, intelligent, adaptive.  
- Auto self‑compact: trigger compaction automatically when needed; no manual ask.

**FINDINGS / BUGS**  
- Removed `hasValidApiKey` gate that blocked free path.  
- Added test guard to prevent network calls in test env (LLMEngine returned offline).  
- Tuned capability escalation regex/length thresholds.  
- Switched cache key from prefix slicing to full string hash to avoid collisions.  
- Addressed P2 cosmetic issues (test count, duration_ms).  
- Deferred P2/P3 findings: `min_answer_chars` floor edge; `media_type` optionality in vision calls safe.  
- Cleaned orphaned JSDoc & eslint comments.  
- All unit tests (41/41) and type checks passed.

**DOMAIN SPECIFICS**  
- **Engines**: `LLMEngine`, `OllamaClientEngine`, `VisionActionAnalyzerEngine`, `LatheAIUltraEngine`, `ManusATCSBridge`.  
- **Dispatchers**: `manusDispatcher`, `ralphDispatcher`, `atcsDispatcher`, `knowledgeDispatcher`, `llmEngine` dispatcher via `callTool`.  
- **Metrics/paths**: `tokens_used.input/output`, `duration_ms`; endpoints `/api/v1/knowledge`, `/api/v1/ai/reasoning` (unmounted); functions `callClaudeForUnit`, `executeWithClaudeAPI`, `queryVision` with image normalization.  

**TOOLS USED**  
- PRISM runtime: `chat-slots.mjs`, checkin pipeline, lane‑guard, self‑compact script.  
- LLM engines: `LLMEngine`, `OllamaClientEngine`, `ClaudeCallEngine`.  
- Testing & linting: Vitest, TypeScript (`tsc`), per‑file scrutiny gate, code‑analyzer.  
- External libs: removed `@anthropic-ai/sdk`; used `ollama` client.

**OPEN THREADS**  
1. Migrate `ralphDispatcher.callClaudeApi`: remove gate, throw on offline.  
2. Migrate remaining dispatchers (`atcsDispatcher`, others) using same recipe.  
3. Migrate `AgentExecutor` with tool handling.  
4. Implement new `queryVision` path for vision engines; route to local Ollama vision models or Claude vision backup.  
5. Ensure self‑compact rule triggers automatically when needed.  
6. PartMediaToCAD – multi‑image vision consumer migration onto `queryVision`.  
7. BlueprintVisionOCR – xray‑domain OCR engine migration.  
8. VideoLearning – batched‑frame vision consumer migration.
