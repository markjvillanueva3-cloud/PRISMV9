# cam session 49ed5a8b (2026-06-24, 31.4MB, spine 326KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑PP‑KIENZLE‑EMIT‑REGRESSION` – 8 reference‑value tests lock Stage 1.1 (commit 7cf0427bfb)  
- `GCodeSnippetEngine` companion test – 13 deterministic tests (195785a944)  
- `GCodeOptimizationEngine` companion test – 16 reference‑value tests (426ace969f)  
- `U‑PP‑GCODE‑OPT‑CLASSIFIER‑TIGHTEN` – arc‑classifier regex fix, 24‑test suite (39e8324c38)  
- `U‑PP‑GCODE‑SNIPPET‑FILL‑INJECTION‑SAFE` – split/join refactor, 2 proof tests (86a321a3c4)  
- `PostProcessorTelemetryEngine` companion test – 14 reference‑value tests (b9d9e31d50)  
- `U-CIMCO-EXE-PATH-RESOLVE` – CIMCO launch path fixed to H: install, live loop verified  
- `U-PP-BIDIR-OPT-TRAILING-FLUSH` – block consolidation bug fixed, 24‑test suite green  
- `U-PP-POSTVERSIONING-TEST` – 15‑test companion (two defects logged)  
- `U‑PP‑GCODEVERIFY‑TEST` – 19 reference‑value tests for GCodeVerificationEngine  
- `U‑PP‑GCODEVERIFY‑CONTINUITY‑WIKI` – documentation of motion‑continuity defect & G90/G91 parser issue  
- `U‑PP‑LAUNCH‑GOAL‑BOUNDED` – launch‑goal spec, G1–G7 gate defined  
- `U‑PP‑GCODEVALIDATION‑TEST` – 18 reference‑value tests for GCodeValidationEngine  
- `U‑PP‑GCODETEMPLATE‑TEST` – 18 reference‑value tests for GCodeTemplateEngine  
- `U‑PP‑GCODERUNTIME‑TEST` – 16 reference‑value tests for GCodeRuntimePredictorEngine  
- `306aa50786 – U‑PP‑GCODEENERGY‑TEST` – 24 reference‑value tests (GCodeEnergyOptimizerEngine)  
- `48340a3109 – U‑PP‑POSTANALYZER‑TEST` – 14 reference‑value tests (PostProcessorAnalyzerEngine)

**DECISIONS**  
- Mechanical code → Ollama coder ensemble (qwen3‑coder 30B, qwen2.5‑coder) → Hermes/Forge agents → Octopus consensus → Opus safety design.  
- Deduplication before creation (`/dedup` guard); units‑first build order; never idle while token budget green.  
- Atomic `[MAIN‑FORCE]` commits (add + commit in one command, wait for `index.lock`).  
- Commit by‑pathspec only; avoid `git add -A`.  
- Self‑compact at YELLOW context; allow native auto‑compact reset once threshold reached (~0.95).  
- Use `node scripts/fleet-survival-status.mjs` to guard against 5 h session limit; arm account switch when GREEN (`node scripts/arm-account-switch.mjs --auto`).  
- Scope shared‑tree stop gates per‑slot and resolve test‑freshness gate thrash (infra tasks).  
- Complete all post‑processor engines for every machine/controller/spindle combo; enable CIMCO live simulation with per‑machine `.mcfg`; expose web/electron/mobile bridge.  
- Two‑arm testing: static byte‑proof + live sim via `PrismCimcoUI.exe` (`--launch`, `--op load-machine`).  

**OPERATOR DIRECTIVES** (verbatim or distilled)  
- “Continue building… finish any in‑flight unit; complete the FEATURE‑ROUTING model‑routing ‘both’ arc … descend the NEVER‑IDLE hunt ladder.”  
- “One unit per iteration: real reference‑value tests + per‑file 2‑arm scrutiny + end‑of‑task 3‑of‑3 + commit `[MAIN‑FORCE]` on cad‑fusion‑live‑ms0 + update handoff.”  
- “NEVER idle while token budget is green; checkpoint at YELLOW and let auto‑compact reset.”  
- “Honor all rails (R12 fail‑loud, R13/R15/R16 comprehensive, dedup before create, units‑first, reasoning=Opus / coding=Sonnet+coder‑ensemble).”  
- “Never `git add -A`; no backticks in double‑quoted `git -m` bodies.”  
- “Never arm the frozen maint crons (`MIGRATION‑FREEZE‑ACTIVE.flag`).”  
- “Fully reorientate and read ALL chats, sessions, plans and roadmaps for all echo and post processor content.”  
- “Utilize workflow, hermes agents / hermes cli, obsidian vault, wikis, tribal knowledge, cimco, jm die files, octopus, ollama offloading, engineered loops, harnesses and crons to plan and execute all remaining tasks we need to finish to launch the product on the prism web app, electron app and ios/android app.”  
- “We need to finalize every post processor for all machine, controller, spindle and other optional or variable feature offered from different brands and all their combinations.”  
- Capture credentials: `node scripts/capture-claude-credentials.mjs account-N`.  
- Arm account switch once GREEN: `node scripts/arm-account-switch.mjs --auto`.  
- Scope shared‑tree stop gates per‑slot (infra task).  
- Resolve test‑freshness gate thrash (infra task).  

**FINDINGS / BUGS**  
- Arc‑classifier regex `/G0?[23]/` miscounted G20/G21, G28–G30 → over‑estimated cycle time; fixed with negative lookahead.  
- `GCodeSnippetEngine.fill` used `new RegExp(key)` + `String.replace`, causing `$&` injection and metachar key throws; replaced with literal split/join.  
- `PostProcessorTelemetryEngine` guard `dlTime > viewTime` mis‑commented; clarified in tests.  
- `PostVersioningEngine.computeHash` uses `Date.now()` (non‑deterministic); `getHistory.total` returns sliced length instead of true count – pending fix.  
- CIMCO launch path hardcoded to `C:\Program Files\CIMCO 2026`; fixed to use H: install.  
- Motion‑continuity logic measured feed length instead of toolpath jump; queued for fix (`U‑PP‑GCODEVERIFY‑CONTINUITY‑FIX`).  
- G90/G91 modal state ignored in coordinate parser; will be corrected with continuity fix.  
- 5 h session limit exceeded (>120%); fleet blocked until operator arms account switch.  
- Shared‑tree stop gates (`stop_on_failing_tests`, `leave-a-copy-behind`) scan entire working tree, causing cross‑slot thrashing.  
- Test‑freshness gate thrash: foreign test edits re‑stale global report; single slot cannot clear during active fleet work.  
- Leave‑a‑copy guard flagged deleted machine‑handbook JSONs (git‑recoverable).  
- Auto‑compact not actuating on this terminal; native auto‑compact (~95%) will reset context.  
- GCodeEnergyOptimizerEngine lacks config‑validation guard → NaN propagates.  
- Coolant phase_breakdown row reports kWh>0 but time_s=0 (internal inconsistency).  
- `VITEST_REPORT.json` 56‑day stale because `npx` not on PATH; refresh tooling broken.  
- Self‑compact (`node scripts/self‑compact.mjs`) fails on this terminal (UIA tab resolver returns no‑tab).

**DOMAIN SPECIFICS**  
- **Engines**: PostProcessorPipelineEngine, GCodeSnippetEngine, GCodeOptimizationEngine, GCodeBidirectionalOptimizerEngine, PostProcessorTelemetryEngine, PostVersioningEngine, GCodeVerificationEngine, GCodeValidationEngine, GCodeTemplateEngine, GCodeRuntimePredictorEngine, GCodeEnergyOptimizerEngine, PostProcessorAnalyzerEngine, GCodeSafetyAnalyzer, VirtualMachiningDeepLearningEngine.  
- **Actions/Dispatchers**: model‑routing (live router → Sonnet), octopus picker, graph reconcile (`localEnsembleWired→true`), hermes/forge agents, octopus consensus, Opus safety design, PrismCimcoUI.exe launcher, `cimco-fleet-drive.mjs`, `fleet-reaper-sweep.mjs`.  
- **Metrics**: cycle‑time estimate, feed distance, telemetry state transitions, launch‑goal gate (G1–G7), 5 h rolling window (~0.65–0.73 YELLOW threshold), auto‑compact trigger ~0.95, VITEST_REPORT.json freshness, motion‑continuity compliance.  
- **Paths**: `cad-fusion-live-ms0`, `state/shared/handoffs/HANDOFF-claude-dc3f020e-vault-ops.md`, `jm-fleet-sim-map.json`, `.mcfg` machine‑load files, `src/__tests__/…`, `mcp-server/src/engines/...`, `state/shared/cimco/jm-post-proof.json`.

**TOOLS USED**  
- PRISM tooling: Sonnet, Ollama coder ensemble (qwen3‑coder 30B, qwen2.5‑coder), Hermes/Forge agents, Octopus consensus (`prism_ai:consensus_decide`), Opus architecture.  
- CAM outputs: Mastercam, Fusion, HyperMILL, Inventor HSM, NX, Esprit, SolidCAM, PowerMill.  
- Test harness: Vitest via `rtk npx`, node scripts (`audit-unwired-engines.mjs`, `fleet-survival-status.mjs`, `self-compact.mjs`, `ask-hermes.mjs`, `ask-ollama.mjs`, `dedup`, `vault-health.mjs`, `system-viz-query.mjs`).  
- Git workflow: atomic `[MAIN‑FORCE]` commits, by‑pathspec, shared‑index lock handling.  

**OPEN THREADS**  
1. Resolve VITEST_REPORT.json staleness (make PATH robust or scope per‑slot).  
2. Fix self‑compact on this terminal; ensure native auto‑compact triggers at YELLOW.  
3. Build and ship GCodeSafetyAnalyzer (2066 L) with full scrutiny window.  
4. Cover remaining untested *Post* engines (e.g., PostProcessorVerificationEngine, others – 38 pending).  
5. Implement continuity fix (`U‑PP‑GCODEVERIFY‑CONTINUITY‑FIX`) – correct discontinuity semantics and G90/G91 parser.  
6. Build `.mcfg` load‑machine wire (`--op load-machine` combo write); unit‑test collision detection with real machine envelope.  
7. Arm account switch to lift 5 h session limit; resolve shared‑tree stop gates per‑slot and test‑freshness gate thrash.  
8. Finalize launch goal execution (G1–G7 gate) for global web/electron/mobile release.
