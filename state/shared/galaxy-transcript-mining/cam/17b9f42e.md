# cam session 17b9f42e (2026-06-14, 91.8MB, spine 523KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `LAUNCH-HERMES-FLEET.bat` – opens 21 Hermes tabs (`hermes -p <slot>`) with domain‑seeded SOUL, creates profiles in `%LOCALAPPDATA%\hermes\profiles\<slot>` (23 KB `.env`).  
- Commit **5010a01a82**: MCP‑server `index.ts` dedup guard added to `proxiedTool`; duplicate dispatcher removed.  
- Commit **30b7765743**: CAG‑usedModel transparency fix (`scripts/lib/galaxy-cag-cache.mjs`).  
- Commit **1438960f58**: Fixed dead `stop_on_stale_handoff` hook; added 10 R9 revert‑proof tests.  
- Commit **da66478fbc**: Consolidated handoff data for three missing slots (26/26 coverage).  
- Commit **cd2ad2979b**: Corrected fabricated premise in bridge plan (`ollama-verified-offload.mjs`).  
- `U-BRIDGE-KEEPALIVE` – added keep‑alive & 120 s timeout to galaxy‑reasoning bridge (PSN leg #10).  
- `U-BRIDGE-WIKI` – wiki link resolver, dense‑rerank fix, test pin.  
- `U-BRIDGE-FALLBACK` – model fallback ladder (gpt‑oss 120b → qwen2.5‑coder 32b …).  
- `U-OLLAMA-WEDGE-GUARD` – self‑healing guard for recurring Ollama `/api/generate` wedge.  
- `U-BRIDGE-CAG-USEDMODEL` – CAG cache now records actual model (`usedModel`).  
- Cross‑substrate edge generator produced **55 218** new edges (34 galaxies).  
- Vault‑to‑GNN ref‑pool added 8 high‑confidence labels; graph size ~542 MB.  
- Vault‑to‑LoRA dataset rebuilt – **474** Alpaca pairs across 34 galaxies (`vault-galaxy-synthesis-dataset.jsonl`).  
- LoRA rows: **1366** (CAG coverage 100%).  
- GNN selective AUROC **0.8084**; full‑coverage AUROC **0.4286** (India GPU).  

**DECISIONS**  
- Use Hermes profile system (`HERMES_HOME`) for per‑slot isolation; seed with SOUL and cwd.  
- Treat unbounded prose goal as deterministic gate (`ai-systems-synergy-goal-gate.mjs` 4/4 PASS); stop chasing `/goal`.  
- Avoid heavy background jobs in chat turns; rely on scheduled 04:00 mining/synthesis tasks.  
- De‑duplicate MCP tool registration (last‑wins guard).  
- Prioritize bridge keep‑alive, wiki inclusion, and Ollama wedge recovery to unblock AI synergy stack.  
- Authorize India‑side GNN full‑coverage retrain only after operator sanction; otherwise accept 4/4 gate as goal met.  

**OPERATOR DIRECTIVES**  
- Duplicate launcher on Desktop for Hermes fleet.  
- Read all Bravo sessions from today/yesterday via Ollama, pick up where left off.  
- Max out Oscar knowledge (Phase 1 mine + Phase 2 constant validation).  
- “CRITICAL: Respond with TEXT ONLY. Do NOT call any tools.”  
- “Briefly acknowledge the goal, then immediately start (or continue) working toward it — treat the condition itself as your directive and do not pause to ask the user what to do.”  
- When a `/compact` is warranted, run `scripts/self-compact.mjs`.  

**FINDINGS/BUGS**  
- 425 `bash.exe` spikes from simultaneous mine+workflow launch; resolved by killing miner nodes.  
- Edge generator skipped **9 galaxies** due to missing `eng.<galaxy>` nodes (5 correct skips, 4 need synthetic node).  
- GNN ref‑pool feeder added 8 labels (0 conflicts).  
- Stale close‑out candidates cleared via `/close-out-audit`.  
- Ollama generate wedge recurred; guard now auto‑recovers.  
- CAG cache bug: `usedModel` missing → fixed.  
- Duplicate API names and false “0‑caller” claim for `ollama-verified-offload.mjs` discovered.  

**DOMAIN SPECIFICS**  
- Hermes profiles: `%LOCALAPPDATA%\hermes\profiles\<slot>`.  
- Slot worktree model: each slot’s SOUL file (`<slot>.md`) and cwd bound at launch.  
- Cross‑substrate synergy edges via `generate-cross-substrate-edges.mjs` (links galaxies to Obsidian, memory/wiki, PSN, RAG/embedding).  
- GNN ref‑pool feeder: `vault-to-gnn-refpool.mjs`.  
- LoRA dataset generator: `vault-to-lora-dataset.mjs`.  
- Galaxy reasoning bridge (`scripts/lib/galaxy-reasoning-bridge.mjs`): PSN leg #10, hybridRetrieve, LoRA pairs, CAG cache, wiki resolution, model fallback ladder.  
- CAG cache (`scripts/lib/galaxy-cag-cache.mjs`): key/fingerprint storage, freshness check, `usedModel`.  
- GNN tier‑5: state files in `state/shared/nn-graph/*`; latest candidate AUROC 0.4286.  

**TOOLS USED**  
- Hermes CLI (`hermes_cli.main:main`).  
- Batch launcher (`LAUNCH-HERMES-FLEET.bat`).  
- PowerShell boot script (`hermes-slot-tab-boot.ps1`).  
- Edge generator (`generate-cross-substrate-edges.mjs`).  
- Ref‑pool feeder (`vault-to-gnn-refpool.mjs`).  
- LoRA dataset builder (`vault-to-lora-dataset.mjs`).  
- AI‑synergy gate (`ai-systems-synergy-goal-gate.mjs`).  
- Galaxy reasoning bridge (`galaxy-reasoning-bridge.mjs`).  
- CAG cache (`galaxy-cag-cache.mjs`).  
- Self‑compact script (`scripts/self-compact.mjs`).  
- MCP server (tool registration, `proxiedTool`).  

**OPEN THREADS**  
- GNN full‑coverage retrain on India GPU (AUROC ≥ 0.78) – requires operator sanction.  
- Remaining bridge plan units: loop‑state‑query dispatcher, cross‑pc‑verify wire, cron‑registry autoreconcile, Tailscale mesh dispatcher; re‑verification of premises needed before building.  
- Monitor memory pressure if new processes are spawned in future iterations.  
- Fleet‑wide `keep_alive` extension for other Ollama callers pending cross‑slot coordination.
