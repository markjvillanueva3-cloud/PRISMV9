# cam session 5c91e392 (2026-06-09, 16.2MB, spine 150KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED (builds/commits)**
- `b76b4d55ab` – Resumable OCR training loop with cursor logic.
- `cdc9ec44b3` – Multi‑page PDF rasterizer + manifest builder; run‑as‑main guards, dedup logic; PowerShell launcher & Task Scheduler installer for unattended closed‑loop training.
- `5a8dbfefdd` – CLAUDE.md regression entry, wiki lesson, MEMORY.md pointer.
- `250ffb8a6e` – `scripts/find-perfect-parts.mjs`, tests, 91‑part report, true‑test harness scoring OCR vs CNC GT.

**DECISIONS (architecture/scope + why)**
- Adopt `/checkin-xray` slot‑binding wrapper to guarantee handoff before `/checkin`.
- Implement resumable corpus loop: stream‑append trainset rows, cursor file per print; protects against reaper kills.
- Fix PDF rasterizer to process all pages – 96 % of prints were multi‑page; eliminates silent data loss.
- Select two reliable VLMs (`qwen3-vl:8b-instruct`, `qwen2.5vl:7b`) for ensemble; discard non‑responsive models.
- Create reaper‑immune scheduled task to run closed‑loop training overnight, ensuring GPU survival under heavy fleet load.
- Add run‑as‑main guards on all CLI scripts to avoid accidental execution during tests.
- Introduce `trainsetRowDedupKey` and downstream dedup in `xray-trainset-to-lora.mjs` for idempotent replay.
- Use CNC program (feed‑move only) as ground truth; drop binary `.mcx` files and non‑lathe programs from scoring.
- Replace inflated toolpath‑point denominator with callout‑class GT (max diameter + length).
- Sanitize `/` & `:` in print filenames before globbing.
- Skip full corpus run until real‑scan OCR recall > threshold; current recall ≈ 0 on 91 parts.

**OPERATOR DIRECTIVES (verbatim asks)**
- Upgrade OCR and blueprint reading with new GPU/CPU resources.
- Wire, test, validate, then begin closed‑loop training on all prints in JM folder/docustra.
- Read previous xray sessions to reorientate.
- Run true‑test on 91 perfect parts; compare OCR output to CNC GT; report verdict.
- Use `/checkin-xray` wrapper to claim `xray` slot before committing.

**FINDINGS/BUGS**
- Page‑0‑only OCR bug: 96 % of PDFs multi‑page; only first page processed → silent data loss.
- Reaper kills caused infinite GPU burn; cursor logic proved re‑OCR = 0 after each kill.
- Worklist reader mis‑treated comment lines (`# …`) as prints; fixed to skip comments.
- Under heavy fleet load, one VLM timed out per print → 1‑model survival and loss of corroboration.
- GPU contention degraded calibration samples during overnight run.
- OCR reads only 1–3 dimensions per scanned PDF; recall = 0 against 7–14 callout dimensions → system not ready for full corpus.
- Binary `.mcx‑8` files produced garbage GT; fixed by `isParsableNcText`.
- Non‑lathe programs (e.g., mill `.hnc`) inflated GT; fixed axis classifier.
- Resolver bug: `basename()` mangled filenames containing `/`; fixed by sanitizing before basename.
- GT metric previously used all toolpath points; corrected to feed‑move only and clustered diameters.

**DOMAIN SPECIFICS (engines/actions/dispatchers/metrics/paths)**
- Slot engine: `chat-slots.mjs` (deterministic claim), `slot-bind-enforce.mjs` hook.
- Checkin pipeline: canonical `/checkin.md`; handoff binding, drift check, `BUILD_STATE` injection.
- OCR training loop lib: `ocr-training-loop-lib.mjs` – resume‑cursor functions, trainset row formatting.
- PDF rasterizer: `pdf-to-png.py` – page extraction, DPI control.
- Vision model selector: `vision-model-select.js` – chooses diverse VLMs; metrics: `trainable_labels`, `tier_totals`, `ensemble_failed`.
- Trainset dedup: `xray-trainset-to-lora.mjs` – keying by `print#page` for idempotent replay.
- Metrics: calibration trust floor, gold/silver label counts, active‑learning queue size.
- OCR of scanned drawings; CAD extraction (STEP/Inventor); CNC program parsing for ground truth.

**TOOLS USED**
- `.claude/helpers/chat-slots.mjs`, `.claude/helpers/slot-bind-enforce.mjs`
- `.claude/commands/checkin.md`
- `blueprint-ocr-training-loop.mjs`, `ocr-training-loop-lib.mjs`
- `batch-ollama-vision-extract.mjs`, `vision-model-select.js`
- `xray-trainset-to-lora.mjs`
- `scripts/find-perfect-parts.mjs`; custom CNC GT extractor (`extractProgramGT`, `scorePartAgainstProgram`)
- Node.js test harness (Mocha/Chai)
- PRISM slot‑claim helper `/checkin-xray`
- Reviewers ARM A/B/C for 3‑of‑3 scrutiny
- External: Ollama, PyTorch 2.11+cu128, Bitsandbytes, Trl, PEFT, Transformers, datasets, pymupdf, `pdf-to-png.py`

**OPEN THREADS**
1. Full corpus training run – monitor overnight reaper‑immune task; collect trainset and active‑learning queue.
2. LoRA fine‑tune on Blackwell once real trainset available (HF base model pull & quiet GPU window).
3. Domain‑shift gate – operator‑verified gold labels for real scans; build comparison harness, shadow deployment until gold arrives.
4. Ensemble concurrency – evaluate adding third VLM when suitable model becomes available; adjust `vision-model-select`.
5. GPU contention mitigation – schedule training during low‑fleet load windows or throttle per‑print batch size to avoid 1‑model failures.
6. Documentation & wiki updates – finalize memory entry, update CLAUDE.md regression list, publish wiki lesson on multi‑page OCR bug.
7. Integrate true‑test harness into regular pipeline once OCR recall improves.
8. Refine GT extraction (handle probing, multi‑op programs).
9. Expand resolver to cover all Docustrata folder layouts and edge‑case filenames.
10. Investigate OCR model upgrades or pre‑processing to raise real‑scan recall before full corpus run.
