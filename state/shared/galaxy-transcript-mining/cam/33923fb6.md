# cam session 33923fb6 (2026-06-22, 6.6MB, spine 53KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `74304444bb` – Fixed Vite dev‑proxy bugs: wrong target (`:3000` → live `:3100`), port collision with the shared backend, IPv6 localhost mismatch.  
- `9963087faa` – Added a dependency‑free static server (`scripts/serve-web-static.mjs`) that serves `dist/web`, streams `/api` and `/ws` to the live backend on `:3100`, includes 4 unit tests.  

**DECISIONS**  
- Pivot to a customer‑facing frontend; build order: **web → electron (React reuse) → CAD‑plugin**; defer native iOS/Android.  
- Use a standalone static server + reverse proxy instead of modifying the shared backend, keeping fleet traffic untouched.  
- Keep dev‑proxy pointing at the live `:3100` backend and force IPv4 (`127.0.0.1`) to avoid 405 “GET only” errors.

**OPERATOR DIRECTIVES**  
- “lets start getting front end build usable.”  

**FINDINGS/BUGS**  
- Vite build passes clean (21 s).  
- Dev server port `3100` collides with the live backend; proxy defaulted to dead `:3000`.  
- Proxy used `localhost` → IPv6 (`::1`) while backend listens on IPv4, causing 405 errors.  
- Backend already has code to serve `dist/web` at `:3100`, but it was not mounted because the server started before the build existed.  
- POST body forwarding failed in the original Vite proxy; resolved by the new standalone server.

**DOMAIN SPECIFICS**  
- Quoting API routes: `/api/mcp/quoting` (mobile), `/api/v1/quote/*` (web).  
- Backend (`mcp-server`) runs on `:3100`.  
- Frontend is a Vite + React SPA built to `dist/web`.  
- Static server uses Node’s `http`, `fs`, streams request bodies with `req.pipe`, provides SPA fallback and path‑traversal guard.

**TOOLS USED**  
- PRISM slot‑claim helpers (`chat-slots.mjs`).  
- `/checkin` pipeline.  
- Vitest, tsc for test & type checks.  
- Custom script `scripts/serve-web-static.mjs`.  
- Node’s built‑in modules (`http`, `fs`).  

**OPEN THREADS**  
- Visual browser render‑verify of the SPA (ensure no client‑side JS errors).  
- Full customer‑facing MVP screens build (public upload → instant quote → configurator → quote packet).  
- Remaining backend blockers: xray‑OCR data scale, ERP credentials – to be addressed in future loops.
