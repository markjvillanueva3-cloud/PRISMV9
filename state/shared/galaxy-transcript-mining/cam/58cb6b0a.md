# cam session 58cb6b0a (2026-06-23, 17.6MB, spine 120KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5ab3c49002` – U‑XRAY‑CALIB‑ACCUMULATE: persistent calibration‑sample-store.mjs, cross MIN_RELIABLE=50.  
- `815567da84` – U‑XRAY‑PROGRAM‑GT‑CALIB: real program‑GT emission into same store (byte‑identical opt‑in).  
- `cd108380ac` – U‑XRAY‑APP‑INTEGRATION‑PLAN: 5‑phase PRISM app integration plan.  
- `198d811ef4` – U‑XRAY‑CALIB‑STATS: CLI for calibration-store observability.  
- `47a37b714d` – U‑APP‑REDACT‑LIB: shared TS redaction module (118 customers).  
- `62c20067d1` – U‑APP‑REDACT‑WIRE: wired blueprintRedaction.ts into prism_cad dispatcher; P1 leak closed.  
- `b8ef51c9fe` – U‑XRAY‑READING‑KNOWLEDGE: curated GD&T, tribal tips, ASME Y14.5 bundle injected opt‑in into buildVisionPrompt.  
- `fa6a037974` – U‑XRAY‑TRUNCATION‑KEYCUT: tier‑2 JSON salvage for mid‑key truncation; restores ~30 dims on dense prints (0 → 28).  
- `U‑XRAY‑NUM-PREDICT‑TUNABLE` – env‑tunable PRISM_OCR_NUM_PREDICT with auto‑coupled num_ctx; default 4096.  
- `U‑XRAY‑READING‑GUIDANCE‑VALIDATE‑WIRE` – wired --reading-guidance into both region‑route and plain paths of validate-perfect-parts.  
- Commit `e1cfab0e20` – added --enhance flag to rasterize (pdf-to-png.py) for preprocessing/deskew; default off, byte‑identical.

**DECISIONS**  
- Closed‑loop calibration must accumulate across runs → persistent store (`calibration-sample-store.mjs`).  
- Real program‑GT samples essential → opt‑in emission into same store.  
- Auto‑redaction covers all 118 customers; use distinctive tier + full‑registry key scrub (ALL_CUSTOMER_NAMES_UPPER).  
- Knowledge injection bounded (max items/char) to prevent truncation; opt‑in default off, byte‑identical when off.  
- Truncation salvage only for mid‑key cuts; fail‑loud retained when no complete data remains.  
- Keep `num_predict=4096` as production default; 8192 shows no recall lift and risk of hallucination.  
- Default `--reading-guidance=false`; no precision benefit observed.  
- Enable truncation salvage non‑default to avoid over‑exposure.  
- Wire knowledge channel only into consumer pipelines, not global default.  
- Add `--enhance` flag for preprocessing/deskew; use when fixture quality is bottleneck.

**OPERATOR DIRECTIVES**  
- “keep making improvements” – continuous enhancement of xray capabilities.  
- “Push forward” – identify a GT part with legible missing callouts and run OCR knob experiments.  
- “Continue all remaining back‑end development tasks, priority on xray.” – focus on closing gaps in blueprint‑vision pipeline.

**FINDINGS/BUGS**  
- Calibration never reached `reliable:true` due to fixed sample count → solved by accumulation store.  
- Program‑GT samples missing → added emission.  
- Auto‑redaction leaked customer names in object keys → key‑scrub with full registry.  
- Knowledge bundle bloat risk → bounded selector, opt‑in flag.  
- Dense prints truncated mid‑key → tier‑2 JSON salvage restores dims; fail‑loud for empty payloads.  
- `num_predict=8192` does not increase recall on part 05850; dims actually decrease slightly.  
- `--reading-guidance` yields no recall improvement and a small precision drop.  
- `--enhance` changes extracted dim count but does not recover missing GT callouts; 05850 is GT‑ceiling bound.  
- Truncation salvage reliably recovers dense prints (28 dims) without regression.  
- No hallucination observed when raising `num_predict`; empirical evidence supports safe env tunability.

**DOMAIN SPECIFICS**  
- Engines: `validate-perfect-parts.mjs`, `blueprint-vision`, `ocrImageWithModelAsync`, `runEnsembleOverImage`, `fuseEnsemble`, `extractWithRegionRouting`, `blueprint-ocr-training-loop.mjs`, `vision-ensemble-fuse.mjs`.  
- Dispatchers: `cadDispatcher.ts` (prism_cad), `buildVisionPrompt` pipeline.  
- Metrics: calibration reliability (`reliable:true/false`), dim extraction counts (~30 dims restored), parse_ok flag, recall, precision, dims count, relErr.  
- Paths: `/checkin-xray` wrapper → slot‑binding → canonical `/checkin`; `buildVisionPrompt → ensemble → probe → validate-perfect-parts`; rasterize chokepoint at `pdf-to-png.py`.

**TOOLS USED**  
- PRISM CLI hooks: `slot-bind-enforce.mjs`, `chat-slots.mjs`.  
- Scripts: `calibration-sample-store.mjs`, `cnc-program-gt-lib.mjs`, `blueprint-reading-knowledge.mjs`, `pdf-to-png.py`.  
- Test harnesses: node test, 2‑arm scrutiny; harness: `PRISM_GIT_ADD_LANE_DISABLE`, `--source live-chat` handoff.  
- Git hooks: `[MAIN-FORCE] lane guard`, `node --check`.

**OPEN THREADS**  
1. Increase `num_predict` (currently 4096) to reduce truncation frequency; evaluate impact on dense prints.  
2. Wire knowledge bundle into region‑route crop path for default‑on usage.  
3. Re‑run closed‑loop training cron now that dense prints yield dims again.  
4. Explore further GD&T corpus drop‑zone integration (operator‑gated).  
5. Select a GT part with legible missing callouts to test OCR knobs (model choice, prompt tuning).  
6. Finalize default changes after validation on that part.  
7. Integrate knowledge channel into production pipeline (enable by default once validated).  
8. Monitor for hallucinations when `num_predict` is raised in production.  
9. Explore additional preprocessing levers (deskew, contrast) beyond `--enhance`.
