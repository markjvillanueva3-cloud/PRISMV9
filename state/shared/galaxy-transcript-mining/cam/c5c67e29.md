# cam session c5c67e29 (2026-06-22, 18MB, spine 100KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- a6977bc839 – QX1 entitlement keystone (`useEntitlement`, `FeatureGate`, `UpgradePrompt`, 403 helpers).  
- 18d1e2d15f – QX2 SFC standalone page with full 9‑axis orchestrator & UQ.  
- 4d7441540e – QX4 post‑payment‑404 fix (Stripe success/cancel routes added).  
- a48c29bb53 – Coolant alias mapping (`mql`, `air_blast`).  
- 07c1b5fcc5 – QX4b owned‑controllers display on Post‑Processor Store page.  
- dfd3332831 – QX‑403 SFC tier‑gate 403 handling (code‑based disambiguation).  
- 0119ef872d – QX7 PPG generator pages gated behind `post.generate`.  
- 158a1ef156 – QX5 Electron desktop shell scaffold (`main.cjs`, `preload.js`).  
- a895131184 – QX6 Capacitor iOS/Android shell scaffold (Capacitor config).  
- 7147897322 – `resolveCheckout` helper.  
- 19f251fa98 – `bundleSalesMailto`: bundle buttons now route to sales mailto; fixed mischarge ($199 → $799/$2499).  
- 561a1bd480 – `resolvePostPurchase` extracted & tested.  
- QX3 de‑inline Taylor physics removed; canonical curve via `sfcApi.toolLife` (loading/error/empty states).  
- Billing surface built: `resolveCheckout`, `resolvePostPurchase`, `bundleSalesMailto`, `resolveBillingPortal`. All tests passed (18/18).

**DECISIONS**  
- Adopt web‑launch‑first; defer Electron/Capacitor shells until later.  
- Gate paid features via `FeatureGate` + entitlement matrix; deny‑by‑default on 403s.  
- Subscription model: one‑time SFC $299, single post $199.  
- Extract checkout & purchase logic into pure helpers (`resolveCheckout`, `resolvePostPurchase`, `bundleSalesMailto`, `resolveBillingPortal`).  
- Fix bundle mischarge by routing to sales mailto.  
- Move `resolveBillingPortal` into `lib/checkout.ts`; keep billing actions behind tested helpers.  
- Use existing `/sfc/tool-life` endpoint and `sfcApi.toolLife` client for QX3; no new backend needed.  
- Stop further extraction beyond current scope (diminishing returns).  
- Defer backend or shell activation work to other teams.

**OPERATOR DIRECTIVES**  
- Complete all pending Quebec frontend work: shell scaffolds, gating, revenue bugs.  
- Proceed after QX3 de‑inline; no new backend for QX3 required.  
- Await go‑ahead for CalculatorPage tab‑split refactor (visual verification environment).

**FINDINGS/BUGS**  
- Bundle mischarge bug fixed: single‑post price used for bundles corrected to $799/$2499.  
- 403 handling now propagates `ApiError.code` (`TIER_LIMIT`, `ENTITLEMENT_REVOKED`).  
- Coolant aliases added (`mql`, `air_blast`).  
- Post‑payment success/cancel routes added; resolved 404 for paid users.  
- Inline Taylor physics removed; canonical engine output used via `sfcApi.toolLife`.  
- Added AbortSignal handling to `sfcApi.toolLife`.

**DOMAIN SPECIFICS**  
- Pricing matrix: `data/pricing.ts` (`ENTITLEMENT_MATRIX`, `FeatureKey`, `planIncludes()`).  
- Entitlement engine: `requireTier()` middleware returns 403 with code.  
- SFC endpoints: `/sfc/calculator`, `/speedfeed`, `/sfc/tool-life`; charts use `sfcApi.toolLife` for canonical curves.  
- Billing API: `createCheckout()`, `purchasePost()`, `getLicenses()`.  
- FeatureGate component uses `feature` prop, checks `isPaid()`; `UpgradePrompt`, `useEntitlement` (deny‑by‑default).  
- Billing actions: `resolveCheckout`, `resolvePostPurchase`, `bundleSalesMailto`, `resolveBillingPortal`.  
- Electron + Capacitor shells; file:// router handling.

**TOOLS USED**  
- PRISM helpers (`.claude/helpers/chat-slots.mjs`, `.claude/commands/checkin.md`), code‑generation hooks, dispatchers, scripts.  
- Ultracode, ollama offloading, Obsidian vault, Hermes agents, harnesses, loop engineering, crons.  
- Vite + React stack; Tailwind, TanStack Query, Zustand, Recharts.  
- Electron scaffold (`main.cjs`, `preload.js`); Capacitor config.  
- TypeScript (`tsc`).  
- Jest/TS‑Jest tests (`checkout.test`, helper tests, SFC area tests).  
- Git commits lock‑aware.

**OPEN THREADS**  
- QX3: backend blocked `/sfc/tool-life‑curve` endpoint needed for Taylor physics de‑inline.  
- Shell activation tests: packaging/launch test pending.  
- CalculatorPage tab‑split refactor pending visual verification approval.  
- Backend bundle self‑serve checkout & `post_processor_purchased` webhook.  
- Post AlarmDB handling.  
- Quoting accuracy logic.  
- ERP integration.
