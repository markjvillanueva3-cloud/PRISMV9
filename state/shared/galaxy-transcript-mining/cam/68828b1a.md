# cam session 68828b1a (2026-06-04, 26.7MB, spine 164KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hermes desktop app configured for Claude Opus 4.8 (Anthropic) – `config.yaml` updated (`model.default: claude-opus-4‑8`, `provider: anthropic`).  
- Zulu Orchestrator registered; burn‑in sweep succeeded – runtime liveness restored.  
- Memory rule added: bravo may launch Hermes & Obsidian apps without per‑time confirmation.  
- Hermes‑Obsidian memory bridge (`hermes-obsidian-memory-bridge.mjs`) committed; 10/10 tests passed.  
- Account‑switch autonomy loop (U2 + U6) – commit a679e455c1, 12+51 tests green.  
- Octopus consensus upgraded to powerful LLMs – commit 856c417d2b, 56/56 tests green.  
- Hermes “coder brain” planner (`hermes-workflow-planner.mjs`) + harness emitter – commits 0f178c6370 & 1bb66a1822, 43/43 tests green.  
- Dream‑cycle OOM fix via inverted‑index blocking – commit 23de0e7881; overnight synthesis <4 s on 11k+ memos.  
- H drive atlas generator for Obsidian vault – commit f2564b81a0, 9/9 tests green.  
- Reactive 429‑fallback router (rate‑limit detection & switch) – commit 443c84d08d, 52/52 tests green.  
- Re‑enabled the Obsidian‑vault bridge (previously disabled).  
- Wire‑Hermes‑local‑backend script (`wire-hermes-local-backend.mjs`) – commit c988a21ec4; auto‑rollback & model guard.  
- RGS pipeline roadmaps generated: `MASTER-RGS-ROADMAPS-2026-06-03.md`; `rgs-pipeline-rules.mjs` updated with 30 new PIPELINE_RULES + 6 AGENT_RULES, all tests green.  
- Master synthesis docs (per‑galaxy master plan & priority‑build plan) written and committed.  
- Hermes routing to domain chats via slot‑briefs – 15 briefs created in `state/shared/slot-briefs/`.  
- U2 swap script (`switch-claude-account.mjs`) built, committed; auto‑swap wiring added to coordinator with `--auto-swap` flag.

**DECISIONS**  
- Hermes will use Claude Opus 4.8 (Anthropic) and OAuth credentials from `~/.claude/.credentials.json`; native 64K‑context local LLM Qwen3‑Coder 30B as workhorse.  
- Account switching uses U2 swap script; auto‑swap opt‑in via CLI flag.  
- RGS defaults: primary `gpt‑oss:120b`, secondary `qwen2.5-coder:32b`; vision models excluded from chat consensus.  
- Reviewer options: `gpt‑oss 120b` (local) or Claude Opus 4.8, with 429‑fallback to reviewer.  
- Planner implements 6 dynamic‑workflow patterns and emits a runnable harness.  
- Dream‑cycle OOM fixed by inverted‑index blocking + top‑K buffer; now <4 s on 11k+ memos.  
- H drive atlas maps all 145 top‑level directories into Obsidian.  
- Reactive fallback router added to switch automatically when a 429 is returned.

**OPERATOR DIRECTIVES**  
- Sign into first account and reset 5‑hour window; test account‑switch system.  
- Build U2 swap script and wire coordinator for auto‑swap (`--auto-swap`).  
- Rotate the exposed `GEMINI_API_KEY`.  
- Capture account‑2 credentials: `claude login`, then `node scripts/capture-claude-credentials.mjs`.  
- After Qwen3‑Coder 30B finishes downloading, run:
  ```
  node scripts/wire-hermes-local-backend.mjs --model qwen3-coder:30b --apply
  ```  
- Run the dream‑cycle scheduler once (`install-hermes-dream-cycle-task.ps1 -RunNow`).  
- Use `/compact` to reset context before next build phase.  
- Verify Obsidian vault bridge is active and memory sync works.

**FINDINGS / BUGS**  
- Shared Claude Max 5‑hour pool saturated → 429 errors for Hermes turns & build agents; `rate_limits.five_hour` not emitted by current Claude Code.  
- MCP :3100 occasionally down during loops.  
- Git index lock contention from peer commits; commit absorption issues observed.  
- `cron_mode` enum value uncertain; editing could boot‑loop Hermes.  
- Obsidian local REST API plugin installed but not an MCP server; need `mcp-obsidian` stdio bridge.  
- Pre‑existing consensus tests failed due to env isolation and hardcoded default model names; fixed by clearing `GEMINI_API_KEY` and updating defaults.  
- Three stale PRISM context marker assertions updated after injector changed format.  
- Dream‑cycle OOM caused by N² similarity; resolved with inverted‑index blocking & DF cap.  
- H drive atlas mapping initially missed some directories; corrected.

**DOMAIN SPECIFICS**  
- **Hermes**: Electron + Python backend, `config.yaml`, `cron_mode`, kanban board, curator, memory DB, gateway at :3100, web UI :9120.  
- **Zulu Orchestrator**: runtime liveness task, burn‑in sweep, slot opt‑in logic.  
- **RGS pipeline rules**: unit‑text → skill+reviewer mapping; GENERIC_FALLBACK gap closed; 30 new PIPELINE_RULES + 6 AGENT_RULES.  
- **Slot‑brief engine** writes briefs to `state/shared/slot-briefs/<slot>.md` for domain chats.  
- **Account‑switch system**: U1 capture, U2 swap, U4 5h‑populator (pending), U6 coordinator + fleet‑wake‑sequencer.  
- **Octopus model roster**: defaults now `gpt‑oss:120b` (primary) & `qwen2.5-coder:32b` (secondary); vision models excluded.  
- **Obsidian vault bridge**: local REST API plugin; pending `mcp-obsidian` stdio bridge to become MCP server.

**TOOLS USED**  
- Hermes desktop app, Obsidian vault, PRISM MCP server, Zulu Orchestrator, RGS pipeline system, `cronCreate`, Git with pathspec commits.  
- Scripts/skills: `capture-claude-credentials.mjs`, `switch-claude-account.mjs`, `account-switch-restart-coordinator.mjs`, `fleet-wake-sequencer.mjs`, `hermes-obsidian-memory-bridge.mjs`, `slotBriefEngine.writeBrief`, `rgs-pipeline-rules.mjs`.  
- External engines: Ollama (qwen3‑coder 30B, qwen2.5‑coder 32B, gpt‑oss 120b), Claude Opus 4.8.  
- PRISM tools/dispatchers: octopus engine, Hermes planner (`hermes-workflow-planner.mjs`), dream‑cycle script, atlas generator, reactive router, wire‑Hermes‑local‑backend.

**OPEN THREADS**  
- Enable Hermes `cron_mode` and seed kanban once quota is available.  
- Wire `mcp-obsidian` stdio bridge so Obsidian becomes a real MCP server.  
- Implement U4 5h‑populator when Claude Code emits `rate_limits.five_hour`.  
- Resolve shared‑tree Git lock contention for future commits.  
- Automate account switching without manual login (requires email MCP or OAuth flow).  
- Finalize octopus defaults to use powerful LLMs and exclude vision models.  
- Finish pulling Qwen3‑Coder 30B (~18 GB, ~5 h download) and execute wiring script.  
- Enable and schedule the dream‑cycle task (`install-hermes-dream-cycle-task.ps1`).  
- Verify Obsidian vault bridge active; memory sync works.  
- Confirm Hermes config schema includes `fallback_providers` and correct model URLs.
