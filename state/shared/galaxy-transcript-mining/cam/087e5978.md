# cam session 087e5978 (2026-06-14, 26.1MB, spine 187KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hermes backend crash fixed (`toolsets.py` max_concurrent_children removal; `prompt_builder.py` return indentation).  
- Revived 18 dormant CLI scripts (Windows‑ESM guard fix, safe fallback).  
- Patched `nn-eval-refresh.mjs` to write `latest-candidate.json`.  
- Built reusable codemod `scripts/fix-windows-esm-entry-guards.mjs`.  
- Published cross‑galaxy token‑savings baseline spec (`state/shared/specs/FLEET-TOKEN-SAVINGS-BASELINE-2026-06-13.md`).  
- Ultracode assessment of 34 galaxies (`e403233551`), slot‑galaxy‑map bug fixes.  
- `galaxy-claudemd-inject.mjs` loader injection, `claude-md-golf-only-guard` activation (`94ae4ded51`).  
- Guarded apply tool `gateDraft` with §0 safety gate (`54764ade24`).  
- Wave 1 & 2 galaxy template compliance commits (`a40105a746`, `28c6b04bb6`).  
- Added own‑window PID resolution + fallback (`087e73d166`).  
- Wired zulu’s tiered resolver (`fede01d2b2`), strengthened multi‑pane safety test (`f97c2b299f`).  

**DECISIONS**  
- Use existing `switch-claude-account.mjs`/`capture-claude-credentials.mjs` for account switching.  
- Adopt Windows‑ESM guard pattern (`|| process.argv[1].endsWith(...)`) across all scripts.  
- Keep Hermes’ `active_provider` set to `anthropic`; drop failing `xai-oauth`.  
- Treat dead hooks as separate review tasks.  
- Use ultracode + Ollama sonnet agents for CLAUDE.md assessment; lock 14‑section template (~80–160 lines).  
- Loader injection (`galaxy-claudemd-inject`) replaces monolith; deduped per‑30 min load.  
- Guard allows regression‑append edits but blocks all other edits to main CLAUDE.md (pure col‑0 boundaries).  
- Self‑compact uses UIA tab‑focus resolver (`focusWtTabBySlot`); fallback writes handoff and prompts `/compact`.  

**OPERATOR DIRECTIVES**  
- “get all 6 accounts active on hermes and switchable in claude code cli”  
- “can you fix the hermes app, it wont launch anymore”  
- “what is our current default model setting?”  
- “upgrade our compactions that if you tell me we should compact and start a new session, you kick it off yourself if possible.”  
- “continue with fixes” (self‑compact)  
- “make it so goal + loop slash commands develop utilizing all this optimally, variably, efficiently and effectively.”  

**FINDINGS/BUGS**  
- Hermes backend crashed on import (`toolsets.py` syntax error).  
- `prompt_builder.py` had indented return outside function.  
- Custom UI TS files contain 18+ compile errors (TabBar.tsx, providers-settings.tsx, use-prompt-actions.ts).  
- Windows ESM guard bug caused silent CLI death in 23 scripts (incl. 3 hooks).  
- Hermes credential pool: 5 accounts, only 4 healthy; account‑3 expired, account‑4 tokenless.  
- Ref‑pool for NN/GNN empty; stale eval metrics from Jun‑6 still used by PSN banner.  
- Stale auto‑manager comment for CLAUDE.md; duplicate `slim-claude-md-regressions.mjs`.  
- Dormant hooks: `claude-md-golf-only-guard`, `galaxy-completeness-advisory`.  
- Slot‑galaxy‑map bugs (`bravo/zebra/zulu → "hermes-zebra"`, `papa → "frontend-app"`).  
- Guard had P1 span‑escape and fence‑aware bypasses; fixed with end‑inclusive span check.  
- Self‑compact initially used dead `slot.pid`; resolved via WT‑tab focus (`focusWtTabBySlot`).  

**DOMAIN SPECIFICS**  
- **Hermes**: credential_pool.anthropic JSON, config.yaml `active_provider`, runs as Python CLI (`hermes_cli`).  
- **Claude Code CLI**: OAuth token in `~/.claude/.credentials.json`; account metadata in `~/.claude.json`.  
- **PRISM**: 34 galaxy directories each with CLAUDE.md; slot‑galaxy mapping via `scripts/lib/slot-galaxy-map.mjs`.  
- **Loader hook** (`galaxy-claudemd-inject`) runs on `UserPromptSubmit`; deduped by `injection-dedup.mjs`.  
- **Guard hook**: PreToolUse listener for Edit|Write|MultiEdit.  
- **Self‑compact**: UIA tab‑focus resolver (`scripts/lib/wt-tab-focus.mjs`), fallback to handoff `/compact`.  

**TOOLS USED**  
- `scripts/fix-windows-esm-entry-guards.mjs`, `nn-eval-refresh.mjs`, `switch-claude-account.mjs`, `capture-claude-credentials.mjs`.  
- Ultracode Workflow + Ollama sonnet agents, `galaxy-verify.mjs`, `gateDraft` (`scripts/self-apply.mjs`).  
- `slot-soul-inject.mjs`, `injection-dedup.mjs`, `safe-truncate.mjs`, `claude-md-golf-only-guard.mjs`.  
- `resolve-hwnd.mjs`, `send-keys.mjs`; replaced by `focusWtTabBySlot` (`wt-tab-focus.mjs`).  
- Git commits: `087e73d166`, `fede01d2b2`, `f97c2b299f`, `a40105a746`, `28c6b04bb6`.  

**OPEN THREADS**  
1. Capture remaining 5 Claude accounts via `/login → capture account‑N <label>`; sync to Hermes pool, set rotation order.  
2. Review & revive dead hooks (`cag-soul-cache-block`, `meta-learning-inject`, `stop-playbook-corpus-drift-advisory`).  
3. Commit guard fixes (branch‑per‑slot discipline).  
4. Trim `H:/prism/CLAUDE.md` to remove redundant sections for token‑saving ROI.  
5. Finalize and publish cross‑galaxy token‑savings baseline; integrate into PRISM awareness pipeline.  
6. Optional leanness trim of 34 CLAUDE.md files (target 80–160 lines).  
7. Register `scripts/self-compact.mjs` as a skill; ensure auto‑invocation on `/compact`.  
8. Verify self‑compact works on all fleet slots (WT tabs); log fallback cases.  
9. Rename WT tabs to `PRISM <slot>` convention for automatic zulu & self‑compact activation.  
10. Resume per‑slot CLAUDE loop (`/loop`) or other goal/loop enhancements.
