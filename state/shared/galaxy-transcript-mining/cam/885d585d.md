# cam session 885d585d (2026-06-24, 29.8MB, spine 360KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `266812bce7`: deterministic test‑rigor floor (`detectShallowCriticalTest`) + CI legitimacy gate.  
- `806bda494d`: AI semantic judge (Ollama→Hermes).  
- `3575eeb71e`, `02641a95ca`: audit 10/25, Octopus‑2‑voice validation, Hermes‑model fix.  
- `e845db2c89`: full 25/25 test‑integrity audit (1 rigorous, 3 shallow, 20 weak).  
- `ab2b3bc84a`: hardened `stop_on_failing_tests.mjs` (main‑guard, 17 R9 tests).  
- `1a0177736a`: added R15 TEST leg for AI judge fallback ladder.  
- `f853c08ade`: tiered verified‑offload primitive (Hermes/Ollama).  
- `a6a6243a2a`: CLI wire + `makeHermesRunner` fix.  
- `119a1c557d`: Ollama‑tier timeout env‑knob (`PRISM_TIERED_OLLAMA_TIMEOUT_MS`) & NaN hardening.  
- `b46945b8c3`: file‑digest offload telemetry bucket (`byHook["ollama-file-digest"]`).  
- `af265b7bca`: local CLI offloads made visible.  
- `7d6f314990`: extracted shared `offload‑stats` envelope + 4 writers.  
- `1f927b3c1b`: migrated 4 advisory hooks onto the envelope.  
- `6c2b3c8476`: enriched wiki entry for reusable envelope.  
- `152586c025`: added adversarial tests for corrupt `byHook`.  
- `b5fa10a632`: cross‑bucket take‑rate measurement primitive (observable).  
- `aadf5a5177`: FEATURE‑ROUTING “both” arc verified shipped.  
- `3c3d7bf2e7`: decision‑half spec committed to wiki (`ollama-offload-u-advisory-decay-xbucket.md`).  

**DECISIONS**  
- Calibrated regex floor from 42.6 % FP → 1.5 % advisory threshold; added deterministic layer.  
- Adopt Octopus consensus (Hermes + Ollama) for validator validation.  
- Cancel recurring `/checkin-india` crons; bind handoff to `alpha`.  
- Auto‑wire AI judge as default OFF Stop advisory pending operator confirmation.  
- Implement tiered verified‑offload: Hermes‑strong → Ollama‑free → trusted fallback.  
- Introduce env knob `PRISM_TIERED_OLLAMA_TIMEOUT_MS` for Ollama tier timeout.  
- Record offloads from file‑digest CLI in dedicated telemetry bucket (`byHook["ollama-file-digest"]`).  
- Dedup‑first policy: skip new harness if equivalent exists.  
- Self‑compact triggered at YELLOW; `/compact` SendKeys failed due to WT tab naming—context remains YELLOW.  
- 5 h token limit (~90%) binding constraint; operator must arm account‑switch (`capture‑claude‑credentials`).  

**OPERATOR DIRECTIVES**  
- Assess stop hooks, stub prevention, real build/code generation validation.  
- Ensure tests are true/exhaustive/complex; use Ollama, Octopus, Hermes, Opus at max settings.  
- Stop India switching crons; keep alpha loop armed.  
- Build autonomously harness loops/crons with ollama+obsidian+hermes+octopus.  
- Capture Claude credentials and arm account‑switch to avoid 5 h block.  
- Verify WT tab named “PRISM alpha” so `/compact` can execute on next cycle.  

**FINDINGS/BUGS**  
- Stale‑green logic blocks all slots when peer edits test file; needs main‑guard.  
- Leave‑a‑copy guard triggered on deleted machine handbooks; restored via `git show`.  
- Octopus fallback bug: Hermes forwarded wrong model ID → HTTP 400, fixed by routing to Hermes default.  
- 42.6 % FP rate in regex floor calibrated to 1.5 %.  
- No test for stale‑green logic (R9 gap).  
- Peer‑file thrash in `stop_on_failing_tests` blocks all slots ~2 h; per‑slot scoping not applied yet.  
- Hermes lane built but not invoked; telemetry shows 0.  
- Ollama offload decisions fire, no executions recorded – executor hook missing/not scheduled.  
- Duplicate test tree detected (`web/` mirrors `mcp-server/web`).  
- Two weak CAM tests flagged for Kilo’s domain.  
- `makeHermesRunner` omitted model field → proxy rejected request → silent fallback.  
- `ollama-file-digest.mjs` never recorded verified offload; conversions invisible to dashboards.  
- Telemetry mislabel: `byHook["ask-hermes"]` bucket counted Ollama‑fallback results as Hermes.  
- Hardcoded Ollama timeout 30 s lacked env knob.  
- P1 bug: `CONVERSION_BUCKET_MAP` key mismatch (`"ollama-nav-enforce-inject"` vs hook key `"ollama-nav-enforce"`).  

**DOMAIN SPECIFICS**  
- Slots: alpha, india, zulu, sierra, golf, echo.  
- Engines/dispatchers: `ask-ollama.mjs`, `ask-hermes.mjs`, `prism_ai:consensus_decide`, `audit-roadmap-drift.mjs`, `test-legitimacy.mjs`, `stop_on_failing_tests.mjs`.  
- Scripts/engines: `audit-unwired-engines.mjs`, `self-compact.mjs`, `fleet-reaper-sweep.mjs`, `vault-health.mjs`, `system-viz-query.mjs`, `verified-offload-tiered.mjs`, `ollama-offload.mjs`, `makeHermesRunner`, `feature-routing-graph`.  
- Metrics/gates: token zone YELLOW/RED, auto‑compact trigger, `stop_on_failing_tests` hard block, audit pass/fail counts, offload‑stats byHook buckets, take‑rate, tokens saved, fleet‑survival status.  
- Paths: `cad-fusion-live-ms0`, `/state/shared/specs/BRAVO-HERMES-ZULU-OPEN-TASKS-LEDGER.md`, `/ledger state/shared/specs/ECHO-OPEN-TASKS-LEDGER.md`, `/state/ollama-offload-stats.json`.  

**TOOLS USED**  
- PRISM scripts: `audit-unwired-engines.mjs`, `self-compact.mjs`, `fleet-reaper-sweep.mjs`, `vault-health.mjs`, `system-viz-query.mjs`, `verified-offload-tiered.mjs`, `ollama-offload.mjs`.  
- LLM interfaces: Ollama CLI (`qwen3-coder`, `qwen2.5-coder`), Hermes CLI (`ask-hermes.mjs`), Octopus consensus (`prism_ai:consensus_decide`).  
- CI hooks: `ci-test-legitimacy-scan.mjs`, `test-legitimacy.mjs`.  
- Git utilities: `git update-index --add`, `git commit --no-verify -m`.  
- Node test runner: Vitest (`npx vitest run <file>`).  

**OPEN THREADS**  
1. Commit `settings.json` wiring for `test‑legitimacy PreToolUse`; restore autocompact knobs.  
2. Auto‑wire AI judge as default OFF Stop advisory (operator confirmation pending).  
3. Add main‑guard to `stop_on_failing_tests.mjs`; expose pure API; finalize R9 suite.  
4. Implement per‑slot stale‑green scoping fix for golf/sierra/zulu (`newestChangedTestNewerThan` scoped to current slot).  
5. Build thin wrapper around Hermes tier offload, record telemetry, schedule cron harness.  
6. Resolve peer‑file thrash or implement scoping before re‑enabling gate.  
7. Correlate `large-read-digest` advisory take‑rate with file‑digest offloads (pending analysis).  
8. Fix `tallyUsage` local `"ollama"` source to correct mislabeling of `byHook["ask-hermes"]`.  
9. Complete design‑heavy take‑rate decision half; revalidate mute logic on 4 live hooks.  
10. Queue low‑value `tallyUsage` unit (not started).  
11. Defer Zulu backlog ghost builds, wiring audits until 5 h limit cleared.  
12. Pending Echo lane GCode post‑engine tests continuity fix.  
13. Resolve operator‑only 5 h token limit via account‑switch.  
14. Fix `CONVERSION_BUCKET_MAP` key mismatch bug.  

---
