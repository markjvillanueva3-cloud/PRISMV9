# cam session 9f57075a (2026-05-17, 20.5MB, spine 56KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `ce2c284651` – per‑slot RGS allocation (`scripts/allocate-rgs-per-slot.mjs`, `state/shared/specs/JULIETT-PER-SLOT-RGS-ALLOCATION-2026-05-17.{json,md}`) and top‑up of starved slots in `slot-task-queues.json`.  
- `a308f353d8` – `scripts/topup-slot-queues.mjs` that injects missing units into the live queue (top‑up only).  
- `b72faff96d` – domain partitioning and feature‑gap discovery (`allocate-domains-to-slots.mjs`, `FEATURE-GAP-AUDIT-2026-05-17.{md,html}`, `FEATURE-GAP-UNITS-2026-05-17.json`) plus re‑keyed 3235 units into the 13 slot queues.  
- `b66dde0a68` – system‑viz ghost roost for feature gaps (`scripts/generate-feature-gap-features.mjs`, regen‑viz registration, merge‑augmentations splice) and canonicalization of gap units into roadmap (`FEATURE-GAP-AUDIT-MS0.json`).  
- `4ad4bb334a` – 18‑stage machining domain pipelines (`generate-domain-pipeline-features.mjs`, config/spec/wiki/CLAUDE.md), AI‑training‑first doctrine captured as `feedback_ai_training_first_before_revenue.md`, and regen‑viz registration.  

**DECISIONS**  
- Use a deterministic RGS allocator that delegates picking to the existing priority‑queue; allocate 6 units per slot, top‑up only for starved slots (no overwrite of peer work).  
- Collapse 15 conceptual domains into 12 chat slots by merging “ERP/HR” → `hotel`, “post‑processor + master” → `india`, and “database” → `golf`.  
- Treat feature gaps as first‑class roadmap units; inject them into the queue and render them in system‑viz via a dedicated ghost roost.  
- Design a canonical 18‑stage print‑to‑part pipeline, with domain‑specific subsets (mill/lathe/wire = full, cad/cam = partial).  
- Prioritize AI training before revenue; capture this as a standing doctrine (`feedback_ai_training_first_before_revenue.md`).  
- Verify the auto‑checkout chain end‑to‑end and queue a post‑build utility‑scan hook for future implementation.  

**OPERATOR DIRECTIVES (verbatim)**  
- “make sure we're setup so that chats automatically check out their tasks and units as they complete them.”  

**FINDINGS/BUGS**  
- Empty‑pool exit‑code collision (P0) during allocator run.  
- Silent field‑name coupling (P1) in slot‑queue handling.  
- Spec overclaims claim‑filtering (P1) – resolved by tightening exclude logic.  
- Minor doc pointer rot: `scripts/slot‑queue.mjs` referenced but actual helper is `priority‑queue.mjs`.  

**DOMAIN SPECIFICS**  
- **RGS Refresh** (`ROADMAP-CONSOLIDATED`, `MILESTONE_PROGRESS`).  
- **Priority‑Queue** (`.claude/helpers/priority‑queue.mjs`) – core picker for all queues.  
- **Slot‑Task‑Claim** (`slot-task-claim.mjs`) – claim, heartbeat, release on commit.  
- **System‑Viz Ghost Nodes** – `ghost.feature_gap_audit`, `ghost.domain_pipelines`.  
- **Domain Pipelines** – 18 stages, with domain‑specific subsets and safety invariants (Ω≥0.95, S(x)≥0.98).  
- **AI Training First Doctrine** – pre‑revenue training units (`*MetaLearning`, `*DeepLearning`).  
- **Post‑Build Utility Scan Hook** – queued as roadmap unit for future implementation.  

**TOOLS USED**  
- PRISM scripts: `allocate-rgs-per-slot.mjs`, `topup-slot-queues.mjs`, `allocate-domains-to-slots.mjs`, `generate-feature-gap-features.mjs`, `generate-domain-pipeline-features.mjs`.  
- System‑viz generators: `regen-viz.mjs`, `merge-augmentations.mjs`.  
- Claim & queue helpers: `.claude/helpers/priority-queue.mjs`, `slot-task-claim.mjs`, `scripts/slot-task-claim-release-on-commit.mjs`.  
- Hooks: post‑commit, stop‑slot‑task‑claims‑advisory.  

**OPEN THREADS**  
- Implement the queued **post‑build utility‑scan hook** (`stop-utility-scan-after-build.mjs`).  
- Commit the Hermes‑related research units (currently uncommitted).  
- Ensure system‑viz ghost nodes are regenerated after each commit (cron job pending).  
- Finalize any remaining doc pointer rot (e.g., `scripts/slot‑queue.mjs` reference).
