# cam session 571d4bdd (2026-05-19, 16.8MB, spine 56KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U‑TDP06 CNC‑derived ground truth (commit `01e59ad58a`) – pure‑core library, tests, CLI build script.  
- U‑TDP07 OCR cascade (commit `5270c65aea`) – PyMuPDF sidecar, Qwen2.5‑VL 7B inference via Ollama, core extractor lib, benchmark wiring.  
- U‑OBF‑F4‑ARCHIVE‑CROSSREF – cross‑reference scanner for unwired hooks.  
- Mike+Golf queue consolidation (mike 6→69 units; golf 68→5 hygiene).  
- MCP scaling validation – confirmed stateless transport handles ≥15 chats × 5–10 agents.

**DECISIONS**  
- Adopt a two‑stage OCR pipeline: vector text extraction with PyMuPDF, raster OCR with Qwen2.5‑VL 7B (fits RTX 4080).  
- Use presence‑only ground truth for CNC to match benchmark grading path.  
- Replace moondream with Qwen2.5‑VL due to higher accuracy and GPU fit.  
- Add a prewarm helper and raise VLM timeout to cover cold‑load latency (~110 s).  
- Lower default DPI from 120 to 72 for performance on the 4080.

**OPERATOR DIRECTIVES**  
1. Run 3‑of‑3 scrutiny on commit `01e59ad58a` (`node .claude/scripts/scrutiny-3way.mjs --target HEAD`).  
2. Run 3‑of‑3 scrutiny on commit `5270c65aea`.  
3. Create a PDF ground‑truth corpus aligned with U‑TDP04 benchmark (real PDFs, not synthetic URIs).  
4. Evaluate the OCR cascade against that corpus and record accuracy metrics.  
5. Plan next unit (U‑TDP08): either integrate eDOCr2 segmentation or fine‑tune a small head on the JM Die corpus.

**FINDINGS/BUGS**  
- `nominal_mm` vs `.nominal` mismatch caused a P0; fixed by aligning keys.  
- Qwen2.5‑VL cold load ~110 s and eviction due to 16 GB VRAM; mitigated with prewarm and extended timeout.  
- DPI default too high (120); lowered to 72 for speed.  
- Ollama returned bare array responses; parser updated accordingly.  
- PDF survey initially misclassified raster PDFs; corrected to ~96 % vector after proper extraction.

**DOMAIN SPECIFICS**  
- Blueprint OCR training: U‑OBF‑F4 crossref → U‑TDP04 benchmark → U‑TDP05 CAD GT → U‑TDP06 CNC GT → U‑TDP07 OCR cascade.  
- CNC ground truth library (`cnc-ground-truth-lib.mjs`) emits presence‑only records.  
- PyMuPDF sidecar renders PDFs to PNG for VLM input; handles both vector and raster PDFs.  
- Qwen2.5‑VL 7B via Ollama on RTX 4080 drives OCR inference.  
- Benchmark (`run-ocr-benchmark.mjs`) grades against GT using the same schema.

**TOOLS USED**  
- `scripts/hook-archive-crossref.mjs` & test suite.  
- `cnc-ground-truth-lib.mjs`, `.test.mjs`.  
- `blueprint-extract-sidecar.py`.  
- `blueprint-extractor-lib.mjs`, `.test.mjs`.  
- `blueprint-extract-io.mjs`.  
- `run-ocr-benchmark.mjs`.  
- `scrutiny-3way.mjs`.  
- External: PyMuPDF, Ollama (Qwen2.5‑VL 7B), moondream (initial plan).  
- Node.js test framework.

**OPEN THREADS**  
- 3‑of‑3 scrutiny pending for U‑TDP06 and U‑TDP07.  
- Creation of a real PDF ground‑truth corpus aligned with the benchmark.  
- Full evaluation of the OCR cascade on that corpus; capture metrics.  
- Decision on next unit (U‑TDP08): eDOCr2 segmentation or fine‑tune model on JM Die data.
