# cam session 321c1d3f (2026-06-03, 37.4MB, spine 386KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑PT‑KNOWLEDGE‑ENRICH` – added `--from‑knowledge`; 68/68 tests; live on okuma‑mill, okuma‑lathe, hurco‑standalone.  
- `U‑PT‑HAAS‑SPEC` – first Haas post engine spec (Hurco template).  
- `U‑PT‑HAAS‑ENGINE` – `HaasNGCMillMasterPostEngine`; 27/27 tests; 3/3 PERFECT on reference jobs; fixed inch‑scale P0‑1 bug, added feed guard.  
- `U‑WINMAX‑LIVE‑PROBE‑FINDING` – live‑sim WinMax mill UIA; verified softkey‑tiebreak logic.  
- `U‑WINMAX‑BUTTON‑SIGNATURE` – extracted 26‑button set; 38/38 tests pass; recorded DRAW_VERIFY screen.  
- `U‑WINMAX‑NAV‑PATH‑CONTRACT` – unified path CLI contract; 68/68 + 30/30 tests pass.  
- `U‑PT‑HAAS‑CANNED‑CYCLES` (a1acfda90b) – G81/G82/G83/G84/G85 with G98/G99 defaults.  
- `U‑PT‑HAAS‑CYCLE‑BYTE‑MATCH` (8601451b27) – byte‑exact G83: bare line, G99 default, no XY on cycle.  
- `U‑CIMCO‑WIKI‑TRIBAL` – CIMCO verification/sim wiki + 12 tribal tips.  
- `U‑PATCHES‑ALLOWLIST` – fleet‑wide leave‑a‑copy patch‑consumption fix.  
- `U‑CIMCO‑CONTROL‑MAP` – `cimco-control-map.mjs`; Simulation Report parser; 19/19 tests.  
- `U‑CIMCO‑MACHINE‑INDEX` – 86 machine defs, 44 flagged missing Unit.  
- `U‑CIMCO‑POST‑INDEX` – 25 .js + 44 .eRPost vendor defs parsed; 5/5 tests.  
- `U‑CIMCO‑WIKI‑REFLECT2` – updated wiki Shipped list with new tool-index commit.  
- `CimcoVerificationBridgeEngine.ts` (1031ecea70) – bridge engine, dispatcher prism_cimco; 22/22 tests; 3‑of‑3 scrutiny PASS.  
- `scripts/lib/nc-normalize.mjs` (4d17ba2aea) – NC normalizer + compareNC; 18/18 test suite.  
- `scripts/cimco-jm-machine-map.mjs → jm-fleet-sim-map.json` (0a1d8fc168) – kinematic‑fit scorer mapping JM fleet to CIMCO sim machines; 9/9 tests.  
- `Post‑proof readiness ledger` (297a3c9194) – 15 machines, 9 143 golden programs, 246 content‑drift groups identified.  
- `Workflow P0 fix + Hurco .hnc coverage + fleet readiness report` (430f735fff).  
- `scripts/lib/nc-dialect-masks.mjs` (d0e5df9e16) – safe round‑trip classifier; volatile‑header detection from JM golden headers.  
- `Refactored drift audit to use roundTrip classifier` (38da8cb412).  
- `Fixed dialect‑mask fail‑open bug` (7a14bf0def).

**DECISIONS**  
- Use `/checkin‑echo` wrapper to force echo slot for post‑processor work; avoid slot conflicts.  
- Adopt YOLO mode: auto‑select highest priority unit, no questions, commit atomically (`git add … && git commit -F`). Schedule recurring `/loop /yolo-mode` every 10 min via CronCreate; auto‑expire after 7 days.  
- Commit strategy: atomic stage/commit to prevent absorption by peer commits; use `--force` only when reclaiming slots.  
- Enumerate next unit (`U‑PT‑HAAS‑CANNED‑CYCLES`) before writing to keep context light (R6/R10).  
- Adopt MCP dispatchers (`prism_calc`, `prism_safety`, `prism_cam`, etc.) and enforce `duplicationGuardEngine.mustCheckBeforeCreating()` to avoid asset duplication.  
- Enforce 3‑of‑3 scrutiny gate (`scrutiny-3way.mjs`) even in YOLO mode; stop only after all reviewers pass.  
- Use system‑viz‑query first for existence checks, fallback to Grep if confidence < 0.5.  
- Reject speculative dialects (G86/G89/G76) unless proven byte‑equivalent against golden archive.  
- Adopt G99 as default retract code and remove XY from cycle lines per JM‑Haas golden data.  
- Use offline proof foundation: golden‑integrity ledger + dialect masks; no live CIMCO sim or Mastercam source yet.  
- Do not apply Roku‑Roku HC horizontal flip (risk of wrong kinematics).  
- Derive volatile header families from shop data only; vendor manuals excluded.  
- Employ adversarial reviewer workflow as safety gate.  
- Bridge‑engine architecture (`prism_cimco`) keeps code size small and allows independent scrutiny.  
- Two‑stage NC normalizer: strip line numbers then optionally renumber; preserves tape markers/comments for strict byte‑equivalence checks.  
- Generic kinematic‑fit scorer ranks CIMCO templates by orientation + axis count.

**OPERATOR DIRECTIVES**  
- `/checkin‑echo …` – start goal loop.  
- “im launching mill hurco for you right now” – bring WinMax sim live to unblock condition 1.  
- `# /loop — schedule a recurring prompt` – set up 10 min cron for YOLO mode.  
- “build whatever we need, wire an test it then proceed with further post processor generation training and generation.”  
- “utilize workflow to ensure we have everything to build properly and effectively.”  
- “I might have a better work around, i paid for a subscription of cimco edit 2026 + machine simulation. download what you can for cimco edit 2026 + their simulation software to assess how we can utilize not just for post processor but for the whole system.”  
- `/goal [ /loop [10m] continue plotting the entire cimco app for full blind navigation so we can utilize it to test all prism generated post processors. if possible. utilize the machine models we have in the system to utilize for simulation within cimco. if there are native simulation machines in cimco, lets add them.`  
- `utilize workflow and parallel agents` – activate YOLO mode, no questions.

**FINDINGS/BUGS**  
- Softkey‑tiebreak auto‑resolve needed; field‑less screens caused ambiguity.  
- Missing DRAW_VERIFY screen & button list in WinMax map.  
- Doubly broken CLI↔consumer contract for `path`, `whereami`, `navigate`.  
- Commit absorption hazard: peer `git add -A` can swallow staged files; mitigated by atomic commit.  
- CRLF line‑ending not a regression (camDispatcher historically uses CRLF).  
- Haas engine had inch‑scale P0‑1 bug and missing feed guard.  
- M29 before G84 is Fanuc‑only; removed to avoid Haas hang.  
- Byte‑drift: original engine emitted `G98` & XY on cycle lines; fixed to `G99`, bare line, no XY.  
- NaN guard added to approach block to prevent silent wrong‑XY first‑hole drilling.  
- Approach/cycle coupling issue resolved with explicit divergence check.  
- Golden archive confirmed JM‑Haas uses G83 without X/Y and always G99.  
- CIMCO Simulation GUI‑only; no headless CLI → UIA read of Simulation Report required.  
- Vendor `.mcfg` defs lack `Orientation` & `Unit`; 44/86 flagged.  
- Server rate‑limiting blocks agent‑based scrutiny; self‑review used instead.  
- Test assertion error: real mill `.mcfg` uses `Header.Orientation === "Horizontal"` not `"Mill"`. Fixed.  
- Fail‑open bug in `parseSimulationReport`: replaced `??` with `||`.  
- NC normalizer test incorrectly expected tokenization of “Haas VF‑2”; corrected.  
- Generic floor kinematic mismatch: initial scorer collapsed all lathes/mills into single default template; resolved by weighting orientation & axis count.  
- P0: VMC‑03 mis‑mapped to 5‑axis trunnion; fixed via regex & test.  
- P0: nc‑dialect masks greedy patterns caused fail‑open; fixed with paren anchors & fail‑closed logic.  
- 246 same‑name golden groups show genuine semantic drift (not header churn).  
- Hurco coverage expanded to 25 goldens from 1.  
- VMC‑02 Okuma‑5ax has 0 goldens; VMC‑01 Hurco has 1.  
- Provenance mismatch in PRISM_UPGRADED lathe output (`machineModel` vs `source:` path).

**DOMAIN SPECIFICS**  
- Engines: `camDispatcher.ts`, `master_post_by_machine`, `HaasNGCMillMasterPostEngine`, `prism_cimco`, `CimcoVerificationBridgeEngine.ts`, `scripts/lib/nc-normalize.mjs`, `scripts/cimco-jm-machine-map.mjs`.  
- Dispatchers/skills: `prism_calc`, `prism_safety`, `prism_cam`, `prism_cad`, `prism_turning`, `prism_ai`, `prism_dev`, `prism_shop_practice:tribal_enrich`, `winmax-ui-map.mjs`, `winmax-course-run.mjs`.  
- Metrics: playbook sequencing (SEQ‑001, SEQ‑003), G‑code dialect conformance, content‑drift group count, volatile‑header detection.  
- Paths: WinMax UIA tree, `signatureOf` extraction, `matchScreen` logic, `state/shared/cimco/machine-index.json`, `state/shared/cimco/post-index.json`, `H:/prism/resources/cimco-2026/`.  
- CIMCO API surface: DNC‑Max REST, MariaDB SQL for DB galaxies; UIA fallback for simulation.  
- Post‑processor validation: byte‑equivalence gate (`compareNC`) against golden Haas `.NC` corpus; integration with SPINE‑1 dispatcher actions.

**TOOLS USED**  
- RTK CLI wrappers (`rtk git`, `rtk vitest run`, `rtk tsc`, `rtk npm run build`).  
- Ollama offload (`/ollama-*`, `qwen2.5-coder:7b`).  
- System‑viz query (`node H:/prism/scripts/system-viz-query.mjs`).  
- Obsidian queries (`/wiki-query`, `prism_memory:semantic_search`, `prism_knowledge:tribal_search`).  
- Master-index awareness (`prism_session:master_index_query`).  
- Duplication guard (`duplicationGuardEngine.mustCheckBeforeCreating()`).  
- Scrutiny gate (`scrutiny-3way.mjs`); 2 parallel reviewers per file, 3‑of‑3 gate.  
- Loop state manager (`loop-state.mjs`).  
- CronCreate for recurring `/loop`.  
- Git hooks & lock handling (`index.lock` recovery).  
- Node scripts for copying, indexing, CI tests.

**OPEN THREADS**  
1. Build `U‑PT‑HAAS‑CANNED‑CYCLES` (add G81/G83 cycle emission to Haas engine).  
2. Restart WinMax sim to complete remaining condition 1 items (softkey‑tiebreak, DRAW_VERIFY screen).  
3. Finalize pending post‑commit hygiene for enumerated units.  
4. Integrate additional cycle codes (G86/G89/G76) once golden‑archive proof available; validate byte‑equivalence on other machine families beyond Haas.  
5. CIMCO↔PRISM bridge engine wiring pending heavy tsc build.  
6. Live UIA driver for `CIMCOSimulation.exe` (operator‑gated).  
7. Per‑galaxy plot workflow re‑run when server rate‑limit lifts.  
8. Further post‑processor generation training/generation tasks.  
9. SPINE‑2 UIA driver & live‑app capture session – required for blind navigation of full CIMCO app.  
10. License‑tier confirmation – headless automation + 2026 REST API availability.  
11. MariaDB credentials – needed for juliett/hotel data integration.  
12. Author native `.mcfg` files for Okuma, Hurco, Roku‑Roku, Mitsubishi EDM to replace generic templates once bridge engine validated.  
13. Post‑processor harness build – run all JM‑fleet posts through CIMCO simulation; verify 100 % pass before live testing.  
14. Live CIMCO simulation app required to validate sim‑machine mapping.  
15. Mastercam CAM source needed for re‑emission parity.  
16. Deferred parallel extraction & scrutiny of dialect masks pending agent reset.  
17. Comprehensive dialect‑extract workflow still pending after agents become available.
