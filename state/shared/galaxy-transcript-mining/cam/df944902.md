# cam session df944902 (2026-05-19, 52.8MB, spine 122KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- **U‑CK16** – skill‑auto‑trigger.mjs (pipeline‑aware) – commit 1763248116 – 35/35 tests passed (2‑reviewer round‑1 FAIL → PASS).  
- **U‑CK27** – telemetry → adaptive‑thresholds wiring, `--signals` report – commit 44a95cc1c0 – 12/12 tests; live smoke shows 6/6 dormant signals.  
- **MTC07** – operation‑ordering canonical – commit c2551512e5.  
- **MTC08** – workholding canonical – commit 4bcbeb4669.  
- **MTC09** – part‑setup/probing canonical – commit c986865c56.  
- **MTC10** – coolant/chip‑evacuation canonical – commit ea0f4546cc.  
- **U‑FGE03** – persist‑learned overlay + auto‑apply in `templateFor()` – commit e7d583b344 – 68/68 tests, no type errors.  
- **U‑ROADMAP‑INDEX‑WRITER‑CONSOLIDATE** – atomic JSON writer consolidation – commit d877d1c970.

---

**DECISIONS**  
- Prioritize real units over stale auto‑write templates; focus high‑ROI dev‑tools/obsidian/system‑viz queue.  
- Adopt `/loop [5m] /goal` for priority‑ordered task scheduling.  
- Ship units that unblock downstream pipelines (U‑CK27, MTC07–10).  
- Consolidate atomic JSON writes into single helper; per‑PID temp suffix + orphan‑temp cleanup removes concurrent‑writer race.  
- Move hook path resolvers to call‑time getters (`triggersPath()`, `pipelinesDir()`); hermetic testing enabled.  
- Widen auto‑derive: all `kind:pipeline` entries trigger `triggerCommand` unless `fm.trigger_command===false`.  
- Auto‑consume persisted overlay in `buildSequenceFor()`; added `templateForStatic()` to preserve FGE01 drift semantics.  
- Add `persistLearned()`, `overlayStatus()`, `clearOverlayCache()` APIs; overlay path governed by `PRISM_CAD_PREVALENCE_OVERLAY_PATH`.  

---

**OPERATOR DIRECTIVES**  
- Check earlier chats, compile remaining work from today’s chat bus (`FLEET-PENDING-EXTRACT-2026‑05‑18`).  
- Session‑scoped Stop hook: complete all high‑ROI tasks first → `/loop [5m] /goal`.  
- Continue autonomous `/loop` after slot‑bind.  
- Resolve environmental block: free ≥10 MB on `C:\Users\wompu\AppData\Local\Temp\claude\`.  

---

**FINDINGS/BUGS**  
- Stale auto‑write template picks (INFRA‑CONSENSUS‑WIRE‑MS0) not real units.  
- U‑CK27 had dormant telemetry signals; fixed by adding `--signals` report and ensuring tuner receives correct events.  
- Atomic JSON writes collided due to fixed `.tmp`; resolved with per‑PID suffix & orphan‑temp cleanup.  
- Persisted learned‑blended prevalences were not written to disk; added `persistLearned()` & updated library loader.  
- False‑green subprocess oracles replaced with explicit assertions (`assert.ok`).  
- Hook self‑import path constants captured at load → fixed to call‑time getters.  
- `replace_all` corrupted env‑var names → restored 3 lines + docstring.  
- Narrow auto‑derive missed `/loop` (trigger:cron) & `/goal-complete` (trigger:hook); widened to all pipeline kinds.  
- Stale env var in `runHook` → pinned LIVE registry paths, cleared knobs.  
- Telemetry append had no rotation cap; noted for future.  
- Bash substring `includes` risk in `matchPipelinesForTool`; flagged low‑impact.  
- Boolean‑false vs string “false” ambiguity in `trigger_command` opt‑out; both forms used, no impact yet.

---

**DOMAIN SPECIFICS**  
- Engines: `CADCorpusFeaturePrevalenceLearnerEngine`, `CADClassFeatureLibraryEngine`.  
- Actions/dispatchers: `cad_corpus_apply_learned` (with persist flag), `close-out-milestone`, dispatcher round‑trip for skill‑auto‑trigger.  
- Metrics/paths: telemetry path `PRISM_SKILL_AUTO_TRIGGER_TELEMETRY_PATH`; overlay file `mcp-server/data/state/cad-learned-prevalence-overlay.json`.  
- Loop‑state ledger (`loop-state.mjs`) tracks iterations & task queue.  
- Chat‑slot system (`chat-slots.mjs`), slot‑bind‑enforce hook, audit‑roadmap‑drift.  
- PRISM 13‑chat fleet slot system (alpha…mike + golf).  
- Slot‑task‑claim.mjs with `--ack-stale`.  
- `/startup-bravo` wrapper claims bravo slot via `slot-bind-enforce`.  
- Pipeline registry: `knowledge/wiki/os/pipelines/*.md` frontmatter (`slug`, `kind`, `trigger`, `composed_of`).  
- Hooks: UserPromptSubmit, PostToolUse, Stop; dispatch via stdin `hook_event_name`; suggest‑only contract.  
- Per‑file scrutiny gate (CLAUDE.md doctrine): 2 parallel reviewers per file; FAIL → fix → re‑dispatch both.  
- MACHINING‑TRIBAL‑COVERAGE-MS0 canonical tribal knowledge at `knowledge/wiki/code-tribal/canonical/`.  
- Golf hygiene slot publishes `FLEET-PENDING-EXTRACT` via chat bus (`agent-coordination.mjs`).  
- C→H mirror hook replicates `settings.json`.  
- Node:test subprocess oracles use env‑knob path getters for hermetic testing.

---

**TOOLS USED**  
- PRISM commands: `/checkin`, `/startup`, `/loop`, `/goal`.  
- Hooks/skills: `slot-bind-enforce.mjs`, `skill-auto-trigger.mjs`, `atomic-json.mjs`, dispatcher scripts (`cad_corpus_apply_learned`, `close-out-milestone.mjs`).  
- Scripts: `audit-roadmap-drift.mjs`, `chat-slots` helper, U‑CK16 source, U‑CK27 source, MTC07–10 sources, U‑FGE03 source, `U-ROADMAP-INDEX-WRITER-CONSOLIDATE`.  
- Test harnesses: `node:test`, `vitest` (for U‑FGE03).  
- Git helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
- Script utilities: `populate-command-frontmatter.mjs`, `agent-coordination.mjs`.

---

**OPEN THREADS**  
- Pending units: **U‑CK11**, **U‑CK14**, **U‑CK15** (queued), **U‑CK28**, **U‑CK29**.  
- Remaining dev‑tools/obsidian/system‑viz tasks after high‑ROI items.  
- Future work: atomic‑json concurrency testing, telemetry event mapping, telemetry append rotation cap.  
- Bash `includes` risk in pipeline matching; trigger_command opt‑out ambiguity (boolean vs string).  
- Free C:\ temp space → resume loop to address U‑CK15 & pending CK units.
