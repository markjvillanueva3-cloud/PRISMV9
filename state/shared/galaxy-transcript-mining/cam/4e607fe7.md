# cam session 4e607fe7 (2026-06-06, 16.9MB, spine 55KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `b0b5b08716` – U‑GNN‑SELECTIVE‑DEPLOY (proved calibration dead‑end, wired selective eval)  
- `15eceefa24` – U‑LEG‑STATE‑SELECTIVE‑WIRE (surfaced SELECTIVE‑DEPLOY in PSN banner & health digest)  
- `0977fea472` – U‑SELECTIVE‑CLASS‑HONESTY (added class‑concentration flag to reports)  
- `cd8fcd6159` – U‑CALIB‑GATE‑SOURCE (single‑sourced 0.15/0.55 gate constants from GATE_THRESHOLDS)  
- `fb3062febb` – U‑GPU‑STACK‑PROVISION (Python 3.13 venv, torch 2.11+cu128 sm_120, bitsandbytes NF4, PRISM_PYTHON_GPU_PATH wired)

**DECISIONS**  
- Treat calibration as a dead end; shift focus to selective‑prediction evaluation and deployment criteria.  
- Surface deploy state via `classifyGnn`, `formatDigest`, `legStateNnGraph` so fleet readers show SELECTIVE‑DEPLOY, not “tier‑5 dormant”.  
- Add class‑concentration metric to avoid over‑reading macro‑F1 = 1.0 on a 2/6‑class subset.  
- Centralize gate thresholds in GATE_THRESHOLDS to eliminate drift risk.  
- Provision GPU stack locally (Python 3.13, torch cu128 sm_120, bitsandbytes) instead of waiting for golf; wire into PRISM via settings.json.

**OPERATOR DIRECTIVES** *(verbatim)*  
- “complete next unit” (multiple times)  
- “bypass golf and do it yourself”

**FINDINGS/BUGS**  
- Calibration mis‑calibration ≈ 0.0197 of 0.179 Brier; residual is refinement loss → calibration ineffective.  
- Class‑concentration: τ=0.7 emits only 2/6 dispatcher classes, macro‑F1 = 1.0 misleading.  
- Inlined gate constants (0.15/0.55) caused drift risk; now sourced from GATE_THRESHOLDS.  
- torch install failed due to cross‑filesystem hardlink copy; resolved with `--link-mode=copy`.  
- sentence‑transformers segfaults on import via datasets→pyarrow native module on Windows py3.13 (documented follow‑up).  

**DOMAIN SPECIFICS**  
- Engines: GNN tier‑5, ModelRoutingEngine, OllamaCapabilityProbeEngine.  
- Actions/dispatchers: `classifyGnn`, `formatDigest`, `legStateNnGraph`, `aiReasoningDispatcher`.  
- Metrics/paths: NN‑EVAL.json (gradeMetrics, computeBrier, computeMacroF1), selective‑prediction curve (`riskCoverageCurve`), deployGrade.  
- Paths: `/checkin-india`, `/loop`, cron job `dbda3e6e` (now cancelled).  

**TOOLS USED**  
- PRISM scripts: `nn-graph-eval.mjs`, `scripts/lib/nn-graph-eval.mjs`, `gpu_health.py`.  
- Infra tools: `uv`, Python 3.13 venv, torch 2.11+cu128, bitsandbytes 0.49.2, sentence‑transformers, peft, accelerate, datasets, pyarrow.  
- Scheduler: CronCreate/CronDelete (job id dbda3e6e).  

**OPEN THREADS**  
- GNN GPU retrain (MS3) – requires PyG install and training code.  
- LoRA trainer (MS4) – needs PEFT/Unsloth, bitsandbytes already green.  
- RAG re‑embed (MS2) – sentence‑transformers issue; fallback to Ollama nomic‑embed.  
- Follow‑up: fix pyarrow segfault on Windows py3.13, install PyG, pull qwen2.5‑coder:32b for llama.cpp.  
- Next unit after provisioning: GNN GPU retrain toward full‑coverage deploy.
