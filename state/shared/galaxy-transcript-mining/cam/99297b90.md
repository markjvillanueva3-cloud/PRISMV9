# cam session 99297b90 (2026-06-11, 31.2MB, spine 283KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `0a966b5696`: Compaction‑boundary fix – 5 byte‑estimator hooks + drift guard.  
- `3f0a9aef11`: Fleet‑wide dense‑RAG backfill (lexical‑miss rescue) for all 34 galaxies.  
- `d12424096c`: Galaxy‑brain refresh – alpha MEMORY, CLAUDE.md, souls.md updates.  
- `0dda52f7de`: Drift guard for precompact auto‑trigger.  
- CLAUDE.md slim: 167 KB → 91 KB (~21K tokens/turn) with self‑trim cap; stale commit log archived.  
- `injection-dedup-fs.mjs` adopted by 3 heavy hooks (25/25 tests pass).  
- `676dd275b5`: GNN heterophily checkpoint provenance (`config.heterophily` persisted & read back).  
- `83e5aa61d4`: Fleet lone‑surrogate API‑400 guard – `dedupeOrMarker` strips surrogates for 28 hooks + 4 injectors.  
- `791f2073ac`: Prompt‑context‑inject throttle – daemon‑down notice once/30 min, otherwise suppressed (0 B).  
- `7b8dbde2dd`: Compaction phantom byte‑estimate guard in `chat-token-watch.mjs`; suspect flag downgrades “critical” → “warn”.  
- `088e74fb92`: GNN tier‑5 selective‑promote path enabled (`PRISM_NN_SELECTIVE_PROMOTE`).  
- `001a3e627c`: AI‑SYSTEMS‑GOAL‑DISPATCH plan written to `state/shared/`.  
- Golf reaper activated: cleans ~19 orphaned `claude.exe` loops causing API rate‑limit errors.  
- Doc‑reflection of recent regressions (`87f90f8bd0`).  
- Enabled zulu fleet self‑compaction (`cebb5639b5`): 24 slots, 24 h dry‑run grace.

**DECISIONS**  
- Centralized compact‑marker detection in `transcript-token-counter.mjs`; updated `precompact-auto-trigger.mjs` to recognize `compact_boundary` and add byte‑estimator suppression.  
- Propagated changes to inline consumers: `token-awareness-sidecar`, `statusline`, `chat-token-watch`.  
- De‑duplicated redundant PostToolUse hook; kept only sidecar refresh.  
- Moved stale commit log out of CLAUDE.md; added self‑trim cap to `regression-auto-write.mjs`.  
- Adopted shared dedup lib via FS wrapper (`injection-dedup-fs.mjs`) for consistency.  
- Persisted heterophily config in checkpoints and mirrored it in `loadCheckpoint`; used heap guard pattern (`shouldReexecForHeap`) for GNN retrain to avoid OOM.  
- Centralized lone‑surrogate sanitization at `dedupeOrMarker` chokepoint; added suspect guard to byte‑estimator so only genuine RAM pressure triggers compaction.  
- Opt‑in selective GNN promotion via `PRISM_NN_SELECTIVE_PROMOTE`; fleet self‑compactor enabled with 24 h dry‑run, auto‑graduate after.  
- Scheduled recurring maintenance loop (`4e514422`) every 10 min for token‑savings checks; let operator execute AI‑SYSTEMS‑GOAL‑DISPATCH plan.

**OPERATOR DIRECTIVES**  
- Flip `PRISM_NN_SELECTIVE_PROMOTE=1` to deploy GNN tier‑5 model (or specify bounded piece).  
- Execute `state/shared/specs/AI‑SYSTEMS‑GOAL‑DISPATCH‑2026‑06‑11.md` (owner responsibility).  
- Run `/compact` after handoff to clear pre‑compact guard; run now to finalize.  
- Optimize token‑eating injections fleet‑wide (~21 K tokens saved per turn); review injection budget and deduplication.  
- Run GNN loop for India; monitor zulu sweep graduation after 24 h.  
- Investigate API errors (orphaned claude.exe loops) – golf reaper already activated.

**FINDINGS/BUGS**  
- Constant compaction loop caused by transcript‑marker change (`isCompactSummary` → `compact_boundary`) and byte‑estimator overcount (~1.86 M tokens), triggering hard block [940k, 1.1 M].  
- Precompact-auto-trigger thresholds lacked suppression for new marker; `findLastCompactOffset` never saw boundary.  
- Subagent workflows failed due to >200 k token context injection (CLAUDE.md + engine digest + wiki index) → prompts too long.  
- Stale commit log block (273 lines) in CLAUDE.md removed; regression-auto-write lacked cap → infinite bloat.  
- GNN predictor dim mismatch: trainer expands 768→3072 but checkpoint didn’t record hops → eval deferred; fixed by persisting heterophily config.  
- OOM during GNN retrain on default heap; resolved with self‑reexec heap guard (`shouldReexecForHeap`).  
- ~19 orphaned `claude.exe` processes looping, causing API rate‑limit errors – golf reaper cleans them.  
- LoadCheckpoint dropped `config.heterophily`; fixed.  
- Lone surrogate in request bodies caused API 400; resolved with surrogate guard.  
- Audit workflow reported duplicate wirings (false positives); corrected before editing.  
- Prompt‑context‑inject spam of daemon‑down notice throttled to once/30 min.  
- Unsatisfiable‑loop anti‑pattern caused fleet rate‑limit storms – fixed.  
- Constant‑compaction phantom bug resolved across all 6 surfaces.  
- CAG answer‑cache functional (cached:true on repeat query).

**DOMAIN SPECIFICS**  
- Engines/Actions: `precompact-auto-trigger.mjs`, `transcript-token-counter.mjs`, `token-awareness-sidecar.mjs`, `statusline.mjs`, `chat-token-watch.mjs`, `graphsage-predictor.mjs`, `injection-dedup-fs.mjs`, `regression-auto-write.mjs`, `nn-graph-retrain-lifecycle.mjs`, `dedupeOrMarker`, slot injectors (`slot-context-bundle-inject`, `prompt-context-inject`, `slot-brief-inject`).  
- Dispatchers/Scripts: `mine-galaxy-transcripts.mjs`, `install-galaxy-mine-task.ps1`, `build-session-evidence-packs.mjs`, `consolidate-evidence-digest.mjs`, `state/shared/zulu-orchestrator-log.jsonl`, AI‑SYSTEMS‑GOAL‑DISPATCH spec.  
- Metrics/Paths: token counts, compaction thresholds (SOFT 880k, HARD 940k), transcript byte size (~1.86 M tokens), injection budget per turn (~21 K tokens saved, 167 KB → 91 KB CLAUDE.md), fleet injection budget (60 hooks, ~4.6 KB slot‑context-bundle), suspect guard in `chat-token-watch.mjs`, zulu self‑compactor dry‑run (24 h).

**TOOLS USED**  
- PRISM hooks: systematic‑debugging skill, precompact-auto-trigger, per-agent handoff helper, stable-session-id helper.  
- Scripts/skills: `transcript-token-counter.mjs`, `token-awareness-sidecar.mjs`, `statusline.mjs`, `chat-token-watch.mjs`, `mine-galaxy-transcripts.mjs`, `install-galaxy-mine-task.ps1`, `build-session-evidence-packs.mjs`, `consolidate-evidence-digest.mjs`.  
- PRISM scripts: `slim-claude-md-injection.mjs`, `measure-injection-budget.mjs`, `injection-dedup.mjs`, `injection-dedup-fs.mjs`, `graphsage-predictor.mjs`, `nn-graph-retrain-lifecycle.mjs`.  
- Golf reaper script (fleet‑reaper), Sonnet agents for summarization.  
- Scripts/lib: `safe-truncate.mjs` (surrogate sanitization), `chat-token-watch.mjs` (byte-estimator, suspect guard).  
- PRISM tooling: CronCreate, per-agent-handoff.mjs, stable-session-id.mjs, precompact-pending-guard.mjs, milestone-tracker.mjs.  
- Skills/commands: `/loop`, `/goal`, `/precompact`, `/compact`.

**OPEN THREADS**  
- Subagent unblocking: remove CLAUDE.md bloat from subagent prompts; slim cold‑cache injection.  
- Re‑run durable mine: schedule reaper‑immune galaxy‑wide transcript miner after current session completes.  
- Finalize /goal loop: ensure AI systems (NN, GNN, LoRA, CAG/RAG hybrids) fully synergized across galaxies.  
- Selective GNN promotion path: deploy via `PRISM_NN_SELECTIVE_PROMOTE`; monitor deployment.  
- Full‑coverage GNN retrain with ref‑pool growth (India’s GPU lane).  
- Adoption of injection‑dedup wrapper in remaining UserPromptSubmit hooks; verify deduplication beyond 4 standalone injectors.  
- Enable fleet self‑compactor (Zulu orchestrator) opt‑in flag; monitor after 24 h dry‑run.  
- Monitor zulu sweep graduation; adjust if needed.  
- Continue LoRA/RAG fine‑tuning for other galaxies (dispatched in AI‑SYSTEMS‑GOAL‑DISPATCH).  
- Run `/compact` now to finalize handoff and clear guard.
