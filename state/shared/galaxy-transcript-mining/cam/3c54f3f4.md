# cam session 3c54f3f4 (2026-06-22, 23.1MB, spine 145KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**

- `d197fa6cd5` – Mill‑program ground‑truth extraction; closed‑loop recall now covers mill parts (R15 validated).  
- `7e8cb4ef57` – Doctrine sync: updated stale `format:"json"` status.  
- `4c0828c118` – Thread‑callout normalizer (`.mjs`) with tests and live validation.  
- `3a2316206c` – Added `--axis` filter to `validate‑perfect‑parts` for targeted recall measurement.  
- `20661dda93` – Thread normalizer TS clone (build‑it‑everywhere).  
- `0440ed4f04` – Chamfer normalizer (`.mjs`) with tests and live validation.  
- `f0a08b7c02` – Pure‑core tiling logic: clique‑partition merge, no‑transitive over‑merge.  
- `d012c5e0a5` – Integrated tiling into validator pipeline; E2E test.  
- `761c045224` – Fixed baseline OCR accounting bug and updated E2E report JSON.  
- `d94fc110ae` – Wired `--tile` option into closed‑loop validator; head‑to‑head recall comparison.  
- `865c312428` – GD&T symbol normalizer (`.mjs`) with tests and live validation.  
- `377e99e57e` – GD&T normalizer TS clone for production OCR engine.  
- `c1a0498791` – Added FCF validation to second production OCR engine’s GD&T extraction.  
- `463b1d8fa1` – *U‑XRAY‑PERPRINT‑UNITS*: per‑print unit propagation with corroboration gate, live‑validated on a 3‑page print.  
- `c6b6e7b14a` – *BACKLOG‑TRUTH*: P1.6 implemented; P2.9 dormant (sample size); GPU usable for vision; current OCR worklist largely single‑page.  
- `5d67236d37` – *P1.5 REGION‑ROUTING DESIGN GROUNDING*: design ready for next build.

**DECISIONS**

- Adopt per‑file scrutiny + live‑validation (R15) before commit; prevent dead‑end levers via premise‑validation.  
- Extend closed‑loop recall from lathe to mill parts with ground‑truth extraction.  
- Add `--axis` filter to isolate mill axis for GPU validation and reduce contention.  
- Dual‑home normalizer pattern (.mjs + .ts) with identical tests; clone into production OCR engine (build‑it‑everywhere).  
- Greedy clique‑partition chosen over union‑find for tile overlap merging to avoid non‑transitive over‑merge.  
- Wire `--tile` flag into validator pipeline for recall lift measurement at corpus scale.  
- Resolve GPU contention: vision models run concurrently after code model releases VRAM.  
- Implement per‑print unit propagation via `resolvePageTitleBlockUnit`, `pageForceUnit`; gate by title‑block identity.  
- Switch default OCR model to stable `qwen3-vl:8b-instruct`.

**OPERATOR DIRECTIVES**

- `/checkin-xray`: bind slot `xray`, read sessions 1/01/2026–6/19/2026 for xray chat slot.  
- Complete remaining backend tasks; priority on xray.  
- Continue improving OCR and blueprint reading capabilities.  
- Keep training and closed‑loop improvement.  
- Utilize Ollama offloading, Hermes agents, parallel agents, engineered loops, harnesses, crons.

**FINDINGS/BUGS**

- Thread regex collision with mixed‑fraction drills; fixed via negative lookahead.  
- Unbounded bore extraction (`I999999`) poisoned recall denominator; added `BORE_MAX_IN` bound.  
- Tool‑steel grade (M2 STEEL) incorrectly fabricated into metric thread; added material guard.  
- Spot‑drill misinterpreted as hole feature; corrected regex to exclude SPOT.  
- Baseline OCR path had `models_ok=0` bug causing false positive recall lift; fixed accounting logic.  
- Fused ensemble dims shape mismatch caused over‑merge; added shape‑tolerant accessors.  
- Unit conversion issue with Ø.94 misinterpreted as mm; added `force-units` flag and threaded through pipeline.  
- GD&T symbol normalization bug (TP, FLAT) prevented datum‑deficiency detection; implemented `normalizeGdtSymbol`.  
- Tiling lift part‑dependent: improves dense mill prints but not sparse lathe parts.  
- Greedy clique‑partition fixed over‑merge bug in tiling.  
- `forceUnits` band‑aid replaced by per‑print propagation, preventing wrong‑unit labels on multi‑page prints.

**DOMAIN SPECIFICS**

- Engines/dispatchers: `validate-perfect-parts.mjs`, `cnc-program-gt-lib.mjs`, `ollama-vision-extract-lib.mjs`, `BlueprintVisionOCREngine.ts`, `BlueprintOCREngine.test.ts`, `vision-ensemble-fuse.mjs`, `runEnsembleOverImage`.  
- Actions: chat‑slot claim, PreToolUse hook, atomic git commit pattern, tsc with heap bump, vitest, node test framework.  
- Metrics: recall, precision, new‑dim count, dimension extraction accuracy, GPU usage, baseline vs tiling comparison.  
- Additional engines: `BlueprintVisionOCREngine`, `VisionTilingExtract`, `validate-perfect-parts`, `blueprint-ocr-training-loop`, `region-classifier-lib`, `page-classify`.

**TOOLS USED**

- PRISM tools/dispatchers/skills/scripts/hooks: `/checkin-xray` wrapper, `chat-slots.mjs`, PreToolUse hook, atomic git commit (`git add && git commit -F -`).  
- Build/test tooling: tsc (heap bump), vitest, node test framework.  
- Vision: Ollama models (`qwen2.5vl`, `qwen3-vl-instruct`), Python PIL for image cropping.  
- PRISM scripts: `lib/vision-tiling-lib.mjs`, `vision-tiling-extract.mjs`, `blueprint-ocr-training-loop.mjs`, `capture-claude-credentials.mjs`, `arm-account-switch.mjs`.  
- Hooks: `slot-bind-enforce.mjs`, `chat-slots.mjs`, stop hooks for graph consolidation.  
- Skills: Hermes agents, parallel agents, engineered loops, harnesses, crons.

**OPEN THREADS**

- Finalize chamfer normalizer TS clone for production OCR engine.  
- Run full corpus‑scale GPU validation of tiling recall lift (currently single‑part test).  
- Implement weld‑symbol normalizer (pending).  
- Verify all new normalizers wired into main OCR extraction flow; no metric regression.  
- Build P1.5 layout‑aware region routing (`region-classifier-lib` + glue).  
- Conduct recall measurement baseline on multi‑page prints to quantify tiling vs region routing lift.  
- Validate per‑type calibration once sample size sufficient.  
- Continue closed‑loop training with updated `--force-units in` flag in the cron.  
- Monitor GPU availability for future builds.
