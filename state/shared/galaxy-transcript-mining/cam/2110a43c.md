# cam session 2110a43c (2026-06-26, 1MB, spine 6KB, 1 slice(s), model gpt-oss:20b)

**DECISIONS**  
- Removed the dead `claude_design` block from `H:/prism/.mcp.json`.  
- Kept only the two real servers (`prism`: 3100, `hermes`: 8645).  
- No commits were made; change is uncommitted and fully reversible.

**FINDINGS/BUGS**  
- The `claude_design` entry pointed to a non‑existent endpoint (`https://api.anthropic.com/v1/design/mcp`).  
- Resulted in 404 `/mcp` reconnect failures.  
- Removal resolves the issue; banner disappears on next reload.

**DOMAIN SPECIFICS**  
- Configuration file: `.mcp.json`.  
- MCP servers: `prism`, `hermes`.  
- No design‑MCP endpoint is available for Anthropic; Figma MCP (`mcp.figma.com/mcp`) is the only installed design tool.

**TOOLS USED**  
- Local config editing (assistant auto‑decide protocol).  
- JSON validation to ensure file integrity after edit.

**OPEN THREADS**  
- None. The optional Figma OAuth flow remains available if the operator explicitly requests it.
