# cam session a264d369 (2026-05-22, 32.8MB, spine 127KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-HMT-HYPERCAD-REEXTRACT` – re‑extracted 309 tips; committed `[MAIN] [HM-TRAINING-WIRING-PLAN]/U-HMT-HYPERCAD-REEXTRACT`.  
- `U-HMT-CONSUMER-MEASURE` – added `knowledgeStats()` to three engines; present in HEAD.  
- `U-HMT-EXTRACTOR-FAILLOUD` – silent extraction error guard; 11/11 tests pass.  
- `U-NN-TRAINER-EXPORT-FIX` – stratified sampler, updated trainer & pipeline; 57/57 trainer + 103/103 pipeline tests pass.  
- `COMMIT‑COORD-MS0` – committed three untracked files via slot‑worktree; 19/19 unit tests passed, hooks installed in `settings.json`.  
- `U-HMT-FUSION-CAD-FIX` – earlier commit (2129f8a226) remains in history.  
- `U-HMT-GRAPHSAGE-SEED-HM` – seed code shipped; blocker resolved.

**DECISIONS**  
- Adopt deterministic rock‑paper‑scissors (`rps-core.mjs`) for commit arbitration.  
- Use atomic‑RMW JSON store (`commit‑coordinator.mjs`) as lane mutex, fail‑open design.  
- PreToolUse acquire hook serializes `git commit`; PostToolUse releases “lane open” via chat bus.  
- Slot worktree commits when shared index contention prevents normal `git commit`.  
- Employ `stable-session-id.mjs` for reliable handoff IDs; avoid `$PPID` rotation.  
- Defer COMMIT‑COORD-MS0 until quieter fleet window due to persistent `index.lock` churn.

**OPERATOR DIRECTIVES**  
- “make it so chats automatically coordinate with git commits and file lockouts… winner commits first then tells other chats its open when they’re done.”  
- `/goal [ complete all current tasks and units in your queue | completed and wired ] /loop [5m] /goal`.  
- “continue” after interruption.  
- Implicit stop‑hook: *complete all current tasks and units in queue* before session ends.

**FINDINGS/BUGS**  
- `rtk`’s git add wrapper corrupts staging → use raw `git` (`rtk proxy git`).  
- Shared-index contention misattributed staged files; commit‑ownership guard added.  
- Regex in commit hook needed one‑line edit for anchored `git commit`.  
- P0/P1: timeout budget overrun, crashed‑holder stale window 120 s→45 s, `saveStore` flag misuse, `loadStore` parse‑retry.  
- Persistent `index.lock` contention on `H:/prism`; background jobs killed by fleet reaper.  
- `rtk proxy git` exits 255 for paths containing “commit”; use `command git`.  
- Untracked file ownership mis‑attributed to peer `claude-eca6e8bb`; corrected in `session-file-ownership.json`.  
- Two separate Ollama model stores caused extraction failures (CLI pulled 7b, server had only 3b).  
- Background extraction jobs (>10 min) reaped by fleet; need longer confirm window or job isolation.

**DOMAIN SPECIFICS**  
- PRISM slot‑task‑claim & chat‑bus (`AGENT_CHAT.jsonl`) for inter‑chat coordination.  
- Atomic‑RMW JSON store pattern (openSync(wx), tmp+rename).  
- PreToolUse/PostToolUse hook contract: `{decision:"approve"}` / `{continue:true}`.  
- `settings.json` mirror from C:→H:.  
- Slot worktree vs shared tree git index handling.  
- Engines: `HyperMillDeepLearningEngine`, `HyperMillStrategyKnowledgeEngine`, `MillingAIUnificationEngine`.  
- Actions/dispatchers: `knowledgeStats()`, `rps-core.mjs`, commit‑coordinator, slot‑task‑claim, chat‑slots, per-agent handoff.  
- Metrics: knowledge stats counts (tips, best_practices, common_mistakes), lock contention timers, background job durations.  
- Paths: `H:/prism/cad-engine/knowledge_store/doc-cad-manual-en-us.json`, `H:/prism/mcp-server/src/...`, `H:/prism/.claude/helpers/...`.

**TOOLS USED**  
- `.claude/helpers/chat-slots.mjs`, `.claude/helpers/rps-core.mjs`, `.claude/helpers/commit-coordinator.mjs`.  
- Hooks: `commit‑coordination-acquire.mjs` (PreToolUse), `commit‑coordination-release.mjs` (PostToolUse).  
- Raw Git via `rtk proxy git`.  
- Node scripts: `milestone-tracker.mjs`, `per-agent-handoff.mjs`, `precompact-pending-guard.mjs`, `stable-session-id.mjs`.  
- Vitest test framework (`commit-coordinator.test.mjs`).

**OPEN THREADS**  
1. Finalize COMMIT‑COORD-MS0 commit (5 files + message) once quiet index window or successful slot‑worktree commit.  
2. Finish remaining foxtrot units: `U-HMT-GRAPHSAGE-SEED-HM`, `U-HMT-HMACOLOR-EXTRACT`, `U-HMT-V31-EXTRACT`.  
3. Run per‑file scrutiny and 3‑of‑3 gate for these commits.  
4. Finalize documentation (wiki/CLAUDE.md) for commit‑coord system.  
5. Resolve `U‑HMT‑V31‑EXTRACT` blockage: unify Ollama model store, extend job isolation to >10 min extraction jobs.  
6. Potential future work: refine RPS arbitration thresholds, scale to more concurrent chats.
