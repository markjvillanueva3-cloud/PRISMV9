# cam session 95e7030e (2026-05-25, 58.8MB, spine 232KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**

- 14 PSN‑synergy units (`U‑PSN‑ACTION‑HINT` … `U‑PSN‑R12‑AUDIT‑POST‑CAMEL`).  
- 5 gap‑fill units: defer‑queue, post‑tool Ollama nudge, bridge‑prefer ranker, wiki‑link sweep proposer, NN/GNN reference‑pool seed.  
- 14 high‑ROI hooks/stop‑hooks: injection‑dedup lib, stop‑skip‑when‑clean adopter, cap‑additional‑context helper, should‑fire‑noop helper, hook‑profile‑reminder, stop‑hook‑timeout budget, stop‑session‑spend‑summary.  
- Alpha‑tree commits `8b3f86f55c`, `c46a7d5537` (injection‑dedup lib & adopters).  
- Audit snapshots: `r12-audit-baseline-2026-05-23.json`, `r12-audit-post-camel-2026-05-23.json`.  
- Dedup & Hook Wiring: `injection-dedup.mjs` + adopters at lines 135‑160 and `stop‑session‑spend‑summary.mjs`.  
- Stop‑Hook Timeout: `stop-hook-timeout-budget.mjs` wired into stop-session-spend-summary.  
- Prompt‑Rewriter: `prompt-rewriter-ollama.mjs` (15 s timeout, smallest‑first cascade, prompt‑hash dedup).  
- PSN Aggregator & Headline: `stop-psn-savings-aggregate.mjs`, `session-start-savings-headline.mjs`.  
- RTK Enhancements: `rtk-prefix-reminder.mjs`, `posttool-rtk-adoption-measure.mjs`, `rtk-fraction-tune.mjs`; PreToolUse hook `U‑RTK‑ENFORCE`.  
- Stop‑Token‑Savings Summary: `stop-token-savings-summary.mjs`.  
- Ledger Prune: `jsonl-tail-prune.mjs`, `stop-ledger-prune.mjs` (cap JSONL to 5 MB, keep 1 MB tail).  
- Audit Script: `audit-token-savings-coverage.mjs` (full 5‑universe sweep).  
- Calibration Boosts: five positive boosts for perfect score.  
- Training Loop: `stop-rtk-fraction-recalibrate` hook.  
- Detectors U‑PTSM02–06 (git misattribution, grep, read, websearch, bash node) – 25 kB, 62/62 tests PASS.  
- Token‑savings router table (`scripts/lib/token-savings-router-table.mjs`) – 44 frozen‑Map entries for RTK, MCP, Ollama, slash commands; 25 tests.  
- PreToolUse router‑advise hook (`.claude/hooks/pre-tool-router-table-advise.mjs`) – lazy‑import, fail‑soft fallback; 37 tests.  
- PostToolUse telemetry hooks: `posttool-rtk-adoption-measure`, `posttool-ollama-rewriter-corpus` – record delta & LoRA corpus; 2 tests each.  
- U‑AUDIT‑V4 system‑graph coverage audit – 12 prefix rules, 28/28 tests.  
- U‑LS‑CAT‑ENFORCE hard‑block with CLAUDE.md tool‑selection – 29/29 tests.  
- U‑MCP‑ROUTE‑AUDIT take‑rate audit – 24/24 tests; dashboard `mcp-route-takerate-audit.{json,md}`.  
- Category A TTL upgrades: slot‑soul, discipline‑expert, comp‑build‑enforce, PSN‑checklist – ~73 k tokens saved/chat.  
- Category B per‑file dedup (CCG, TCE, PWGI, PRGI, PBGI, PGGI) – 6/7 applied; ~30 k tokens saved/chat.  
- Category C MVP `scripts/lib/unified-pre-search.mjs` – unified BM25 injector; ~35–45 k tokens when wired.  
- Category D operator wires (`scripts/apply-token-savings-wires.mjs`) – settings.json entries for PRISM_LS_CAT_ENFORCE_ENABLE, PRISM_RTK_ENFORCE_ENABLE, router‑advise, stop‑summary.  
- MRI commit `f73769ccfc` memory‑relevance injector finalized.  
- MRS‑DOCTRINE TTL fix staged on disk (next cron auto‑commit).

---

**DECISIONS**

- Reassign fleet‑reaper ownership from alpha to golf; keep hook on disk but unwired.  
- Adopt R12 fail‑loud doctrine: never claim functionality without grep‑verified MCP action.  
- Canonical source for TAKEUP_CREDITED_ACTIONS: `mcp-route-takeup._ACTION_TO_CLASSIFIERS`.  
- Auto‑derive dispatcher actions from `mcp-server/src/tools/dispatchers/*Dispatcher.ts` to cut audit noise.  
- Use defer‑queue pattern to batch mid‑task advisories; drain at session end.  
- Introduce injection‑dedup library (TTL 60 s, 16‑char SHA‑256 hashBlock) for deduplicating repeated UserPromptSubmit injections.  
- Enforce stop‑hook timeout budget: default 5 s + 800 ms to avoid hanging hooks.  
- Adopt smallest‑first model cascade in Ollama; fail‑loud telemetry for silent skips.  
- Bidirectional calibration (penalties + boosts) to reach perfect score; raise floor to 1.00.  
- PSN aggregator unifies all telemetry sidecars into single JSONL consumer (`dashboards/psn-savings-aggregate.jsonl`).  
- Stop‑hook timeout budget enforces per‑session limits.  
- Ledger prune caps JSONL growth at 5 MB, retains 1 MB tail.  
- RTK enforcement guarantees adoption; optional env flag `PRISM_RTK_ENFORCE_ENABLE=1`.  
- Parallel agent deployment for gap‑fill and audit to avoid lock contention.

---

**OPERATOR DIRECTIVES**

- Commit all changes to alpha worktree (`slot/alpha`); no `/goal clear` needed after success.  
- Wire new hooks in `settings.json`: `stop-token-savings-summary`, `stop-ledger-prune`, `stop-psn-savings-aggregate`.  
- Run `/compact` to finalize session; ensure lock contention cleared.  
- Set `PRISM_RTK_ENFORCE_ENABLE=1` (or use env flag) if 100 % RTK adoption desired.  
- Wire all stop hooks in `settings.json` and verify order.  
- Resolve any remaining lock contention via `/compact`.  
- Enable RTK enforcement (`PRISM_RTK_ENFORCE_ENABLE=1`) and monitor adoption; adjust thresholds if needed.  
- Validate PSN aggregator output via master‑index regen.  
- Ensure nightly `stop-rtk-fraction-recalibrate` runs and updates fractions table.

---

**FINDINGS/BUGS**

- R12 violations from fake `prism_intelligence:ollama_*`; fixed by mapping to real `prism_dev:ollama_hook_query`.  
- Git lock contention on shared index during concurrent peer commits; resolved by committing directly to alpha worktree.  
- Deferred‑queue `main()` invoked on import caused TAP stream pollution; guarded with `process.argv` check.  
- Stop‑hook timeout exceeded for slow Bash commands; added budget guard and skip‑when‑clean logic.  
- Ollama `/api/chat` dead → prompt‑rewriter silent skip; fixed with early exit & state check.  
- MCP server disconnected → route‑suggest suppressed via `mcp-state-check`.  
- Low RTK usage (~8 %) addressed by `U‑RTK‑ENFORCE`.  
- Token‑savings coverage gaps identified; audit shows 0 candidate nodes after sweep.  
- **1176 fires / 0 takes** in MCP route audit – measurement loop broken (take‑up hook not writing to sidecar).  
- MCP probe only checks TCP port, not JSON‑RPC handshake → false “alive” status.  
- Slot‑soul dedup TTL = 5 min; session‑wide content should live 24 h → ~38 k tokens saved/chat.  
- Many injection markers duplicate work (~48 % of token budget).  
- `comprehensive-build-enforce` and other static blocks use short TTLs; similar savings.

---

**DOMAIN SPECIFICS**

- **Engines/dispatchers:** `prism_dev:ollama_hook_query`, `prism_intelligence:*`, `mcp-route-suggest`, `mcp-route-takeup`.  
- **Hooks categories:** PreToolUse (grep, glob, write, bashgit, bashnode, read, websearch), PostToolUse (`posttool‑rtk‑adoption‑measure`), Stop (`stop-session-spend-summary`, `stop-ledger-prune`, `stop-token-savings-summary`, `stop-psn-savings-aggregate`, `stop-rtk-fraction-recalibrate`).  
- **Metrics/paths:** `state/shared/mcp-route-suggest-stats.json`, `state/shared/defer-queue.json`, `state/shared/injection-dedup-cache.json`, `dashboards/psn-savings-aggregate.jsonl`; ledgers: `rtk-savings-ledger.jsonl`, `prompt-rewrites.jsonl`, `pre-tool-savings-multi.jsonl`.  
- **PSN architecture:** 11‑leg (Obsidian brain, Wiki, Memories, Tribal, System‑viz, Engines, Algorithms, Formulas, NN/GNN, PRISM‑AI).  
- **RTK bases:** 16 tools (git, gh, npm, npx, yarn, pnpm, vitest, tsc, tsx, node, docker, docker-compose, grep, rg, find, cat, ls).  
- **MCP routes:** bounded output for token‑savings substrate; take‑rate telemetry.  
- **Telemetry ledgers:** JSONL in `state/shared/dashboards/`; tail prune to limit growth.  
- **Bandit lib** (`detector-bandit-tune`) for adaptive suppression/boost of detectors.  
- **LoRA fine‑tuning corpus** (`posttool-ollama-rewriter-corpus`).  
- **Injection‑dedup cache:** `injection-dedup-cache.json`.

---

**TOOLS USED**

Scripts/hooks:  
`injection-dedup.mjs`, `stop-hook-timeout-budget.mjs`, `prompt-rewriter-ollama.mjs`, `rtk-prefix-reminder.mjs`, `pre-tool-savings-multi.mjs`, `posttool-rtk-adoption-measure.mjs`, `rtk-fraction-tune.mjs`, `stop-token-savings-summary.mjs`, `stop-ledger-prune.mjs`, `audit-token-savings-coverage.mjs`, `stop-psn-savings-aggregate.mjs`, `session-start-savings-headline.mjs`, `pre-tool-router-table-advise.mjs`, `posttool-ollama-rewriter-corpus.mjs`, `token-savings-router-table.mjs`, `unified-pre-search.mjs`, `apply-token-savings-wires.mjs`.  
Libraries: `scripts/lib/injection-dedup.mjs`, `scripts/lib/stop-skip-when-clean.mjs`, `scripts/lib/mcp-state-check.mjs`, `scripts/lib/ollama-state-check.mjs`, `scripts/lib/jsonl-tail-prune.mjs`, `scripts/lib/rtk-fraction-tune.mjs`.

---

**OPEN THREADS**

1. Wire all stop hooks in `settings.json` and verify order.  
2. Run `/compact`; resolve any remaining lock contention.  
3. Enable RTK enforcement (`PRISM_RTK_ENFORCE_ENABLE=1`) and monitor adoption; adjust thresholds if needed.  
4. Validate PSN aggregator output via master‑index regen.  
5. Ensure nightly `stop-rtk-fraction-recalibrate` runs and updates fractions table.  
6. MRS‑DOCTRINE TTL fix staged on disk (auto‑commit next cron).  
7. Replace TCP‑only MCP probe with JSON‑RPC handshake to stop false “alive” status.  
8. Verify take‑up wiring: ensure `mcp-route-takeup.mjs` writes into sidecar.  
9. Regenerate system‑viz after dedup caches written; confirm single node representation.  
10. Monitor for residual injection markers not yet deduped (e.g., doctrine‑surface, pre‑write‑graph).  
11. Finalize operator wire settings.json entries and backup.
