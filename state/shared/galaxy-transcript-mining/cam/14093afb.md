# cam session 14093afb (2026-06-26, 17.2MB, spine 146KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `lathe-closed-loop-full.mjs` – unified driver for Rung A (34,993 .MIN), B round‑trip, C STEP; emits `full_geometry_loop_closed=true`.  
- `turningDispatcher.ts` – wired 4 Okuma engines: `okuma_step_parse`, `okuma_macro_convert`, `okuma_manual_tips_extract`, `okuma_transcript_mine`.  
- `LatheLoRASafetyEvaluatorEngine` – safety evaluator integrated into dispatcher (fleet‑floor envelope 3800 rpm / 11 kW).  
- `U-W3` – live‑tooling archetype runner for Rung B.  
- `U-W6` – tribal max‑out pipeline drained to dry (101 → 694 tips).  
- `U-W8` – in‑page Kienzle rename (`brand='Kienzle'`).  
- `lathe-tip-classify.mjs` – classifier hardened against FF2 hallucination; 20/20 tests.  
- Wiki lesson entry `d0f42b358d` documenting bug‑finding compliance.  
- Kienzle build located (`H:/KIENZLE APP BUILD.zip`); FE↔BE wiring verified end‑to‑end.  
- `U-W9` – closed‑loop Rung‑B grid regrounded; 60 programs, 0 errors, 100 % SFM envelope, 96.3 % feed; driver heap hardened to 8 GB.  
- `U-W10` – EMFILE concurrency bug fixed: bounded inner `Promise.all` + sequential outer load; `MaterialRegistry` loads 3989 materials, no EMFILE/OOM.  
- `U-W11` – added in‑flight guard to `MaterialRegistry.load()`; validated clean exit at default heap.

**DECISIONS**  
- Adopt unified full‑corpus driver for all 34,993 .MIN files to avoid duplicated pipelines and ensure consistent metrics.  
- Implement fleet‑floor safety envelope (3800 rpm / 11 kW) in `LatheLoRASafetyEvaluatorEngine`; conservative to avoid false SAFE marks.  
- Do not wire parametric tribal signals into generation after <1 % yield & hallucinations observed.  
- Use pure‑JS occt path for STEP geometry leg to eliminate GPU stalls.  
- Apply bounded concurrency + in‑flight guard to `MaterialRegistry.load()` to resolve EMFILE/OOM and redundant loads.  
- Route remaining cross‑lane work: 10 UI gaps to Quebec (FE) and shared‑core loader bug to Oscar/Papa; no further whiskey‑lane units pending.

**OPERATOR DIRECTIVES**  
- Provide video/MIT source URLs or files if extending tribal pipeline beyond current dry corpus.  
- Supply frontend design concept (or hand off to Quebec) for Kienzle FE build.  
- Build next dependency‑ordered unit if any remain (`U-W2–U-W8` considered complete).  
- Provide missing G4 design concept if required for cross‑lane items.

**FINDINGS/BUGS**  
- Path‑casing double‑count on STEP roots fixed by normalizing case.  
- `sfm_max` units drifted between m/min and ft/min; corrected & bounded to [20, 3000] ft/min.  
- FF2 control‑code mention caused hallucinated `feed_factor:1.2`; guard added to reject non‑numeric directives.  
- Low parametric tribal yield (~0 %) confirmed; no benefit in wiring into generation.  
- EMFILE concurrency bug in `MaterialRegistry.load()` fixed with bounded inner concurrency + sequential outer loop.  
- Missing in‑flight guard resolved redundant loads and race conditions.

**DOMAIN SPECIFICS**  
- **Engines:** TurningPrintIntakeEngine, LatheTribalIntegrationEngine, LatheLoRASafetyEvaluatorEngine, BlueprintVisionOCREngine.  
- **Dispatchers:** `turningDispatcher.ts` (Okuma engines), unified driver dispatcher (`lathe-closed-loop-full.mjs`).  
- **Metrics:** `full_geometry_loop_closed`, `safety_basis`, `tribal_advisory_count`, `collision_avoidance_score`, SFM envelope, feed %.  
- **Paths:** `scripts/lathe-closed-loop-full.mjs`, `lathe-tip-classify.mjs`, `turningDispatcher.ts`, `src/MaterialRegistry.ts`, `utils/files.ts` (readJsonFile), `mcp-server/web/design-imports/kienzle-app-build/`.

**TOOLS USED**  
- PRISM scripts: `lathe-closed-loop-full.mjs`, `lathe-tip-classify.mjs`.  
- Node scripts: `audit-close-out-candidates.mjs`, `loop-state.mjs`.  
- Engines/modules: BlueprintVisionOCREngine, TurningPrintIntakeEngine, LatheTribalIntegrationEngine, runPipeline.  
- PRISM core modules: `MaterialRegistry.ts`, `utils/files.ts`.  
- Git commit workflow: `[MAIN-FORCE] [KIENZLE-LATHE-WIZARD]/U-ID`.  
- Ollama for mechanical‑text routing.

**OPEN THREADS**  
- Video/MIT sources missing; operator input required to proceed with tribal pipeline extension.  
- G4 frontend build (Quebec) needs design concept from operator.  
- 10 UI gaps in Quebec’s FE (Shop‑Floor Live → Scheduling → Inventory → Blueprint/OCR/CAD → CAM/Lean → Accounting/GL/Sales).  
- Ensure fixed `MaterialRegistry` changes propagate to all physics consumers; monitor for residual load inefficiencies.
