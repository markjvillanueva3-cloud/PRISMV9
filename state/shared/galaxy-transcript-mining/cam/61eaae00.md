# cam session 61eaae00 (2026-06-23, 11.3MB, spine 96KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-ZLR-OLLAMA-ADOPTION-GAP` (commit 0a6ec90ca2): added adoption‑gap flag, evidence string, action hint to ollama grader.  
- `U-ZLR-HERMES-RECENCY-GATE` (commit 256275995b): 48 h recency gate in hermes grader; added `lastUsed`, `adoptionGap`; updated tests.  
- `U-HERMES-VAULT-DIGEST`: script synthesizes recent vault notes via Hermes, writes to `/knowledge/hermes‑outputs/`.  
- `U-HVD-NO-FALLBACK`: patched `ask-hermes` to honor `--no-fallback`, preventing silent Ollama fallback.  
- Commit b3356e88cb: wired RegressionBaselineEngine to `prism_dev:regression_baseline`; all 14 round‑trip tests passed, no new TS errors.

**DECISIONS**  
- Adopt mandatory‑directive pattern (≥ 0.85 ROI) for high‑ROI suggestions; scope limited to ollama offloading first with flag `PRISM_ENFORCE_OLLAMA_OFFLOAD`.  
- Wire Hermes into Claude Code by adding `vault-fs` MCP server to Nous Hermes’ `config.yaml`; requires operator restart.  
- Cron‑wire Hermes vault digest as non‑elevated Windows task every 4 h for sustained $0‑Claude utilization.  
- Add single `regression_baseline` action with modes: freeze, evaluate, quarantine, lift, list, observed_p95, snapshot; persist snapshots to `STATE_DIR`, env‑overridable via `PRISM_REGRESSION_BASELINE_PATH`; use cold‑load‑once guard for `recentRuns`.

**OPERATOR DIRECTIVES**  
- “Can we apply enforcement for high ROI suggestions?”  
- “Wire Hermes into the Claude Code desktop app.”  
- “Optimize utilization of Hermes agent, Hermes and Obsidian vault combos.”  
- Decide on gated levers: enforcement design (ollama‑offload flag) and fleet protection (credential capture).  
- Choose whether to descend to next hunt rung or address remaining unwired engines.

**FINDINGS/BUGS**  
- Ollama offloader adoption gap: 44 decisions, ~2 executions; fixed by `adoptionGap` flag.  
- Hermes grader lacked recency gate; added `lastUsed`, `META_RECENCY_H=48h`.  
- `ask-hermes` silently fell back to Ollama when `--no-fallback` omitted; patched to fail loudly.  
- Hermes `config.yaml` missed `vault‑fs` MCP server; added with backup.  
- P2: `ACTION_DEV_SCHEMAS` lacks Zod coverage; `evaluate()` should validate before recording.  
- `recentRuns` buffer not persisted by design; partial‑batch append on mid‑batch throw is self‑healing.  
- No new TypeScript errors introduced; existing 19 unrelated tsc errors remain.

**DOMAIN SPECIFICS**  
- Grader functions: `gradeOllamaUtilization`, `gradeHermesUtilization`.  
- Metrics: `estimatedTokensSaved`, `measuredTokensSaved`, `adoptionGap`, `lastUsed`.  
- Dispatchers/Engines: `prism_hermes` dispatcher, `hermes‑automation‑bridge`; MCP servers (`:3100/mcp`, `vault-fs`).  
- Paths: `/knowledge/hermes‑outputs/`, `/scripts/hermes-vault-digest.mjs`, `/install-hermes-vault-digest-task.ps1`.  
- RegressionBaselineEngine (April orphan) modes: freeze, evaluate, quarantine, lift, list, observed_p95, snapshot.  
- Related engines: `BlueprintOCRAdapter`, `SFCInferenceGateWireEngine`, `AuthEngineV7`, `PreMOUKickoffChecklistEngine`.

**TOOLS USED**  
- Slot binding helpers: `slot-bind-enforce.mjs`, `chat-slots.mjs`.  
- Reconciliation pipeline: `reconcile-zulu-ledger.mjs`.  
- Hermes tooling: `ask-hermes.mjs`, `hermes‑vault‑digest.mjs`.  
- MCP SDK 1.29.0 (`McpServer`, `StdioServerTransport`).  
- Windows task scheduler script: `install-hermes-vault-digest-task.ps1`.  
- PRISM dispatcher (`registerDevDispatcher`), `safeWriteSync`, `STATE_DIR`.  
- Test harness: `devDispatcher.modelTelemetry.test.ts`; fake‑server for round‑trip tests.  
- Vitest framework; `tsc --noEmit` type checking; NodeNext import resolution (.js).

**OPEN THREADS**  
- Implement enforcement unit for high‑ROI suggestions (ollama‑offload flag).  
- Monitor fleet health post‑cron‑wire; verify sustained Hermes utilization over time.  
- Validate added `vault-fs` MCP server works after Hermes app restart.  
- Explore remaining backend-dev units if resources allow.  
- Add Zod coverage to `ACTION_DEV_SCHEMAS`; validate before recording in `evaluate()`.  
- Address remaining unwired engines: `BlueprintOCRAdapter`, `SFCInferenceGateWireEngine`, `AuthEngineV7`, `PreMOUKickoffChecklistEngine`.
