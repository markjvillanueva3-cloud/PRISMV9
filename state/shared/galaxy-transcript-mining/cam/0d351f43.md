# cam session 0d351f43 (2026-06-09, 0.8MB, spine 15KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- None yet; the copy operation has not begun.

**DECISIONS**  
- Keep core PRISM repo (≈1 038 GB) + `prism-backups` (≈42 GB).  
- Drop all git worktrees, SteamLibrary, Tools runtime folder, and Claude profile copies.  
- Final payload ≈1.2 TB.  
- Use **rclone** for direct‑to‑Box streaming; robocopy/Box Drive mount is infeasible due to C: cache wall.

**OPERATOR DIRECTIVES**  
- Verify the Box account has ≥1 TB free and supports files >150 GB (Business/Enterprise plan).  
- Run `rclone authorize box` once, approve in browser, paste token.  
- Remove the 5.5 GB probe copy from `Box\Prism H Drive\blobs` after authorization.  
- Execute rclone with the persisted exclude list (`H:\.tmp\rc-backup-excludes.json`).  

**FINDINGS/BUGS**  
- Box Drive mounts pin all written files to C:, never evicting them; `attrib +U` fails.  
- C: has only ~12 GB free; a 1.2 TB copy would overflow immediately.  
- Robocopy cannot enumerate the existing backup due to large online‑only file count (`ERROR 31`).  

**DOMAIN SPECIFICS**  
- PRISM H drive contains multiple `prism-*` directories; worktrees identified by presence of `.git`.  
- Exclude set stored as JSON at `H:\.tmp\rc-backup-excludes.json`.  
- Target folder on Box: `Box\Prism H Drive`.

**TOOLS USED**  
- PowerShell (drive enumeration, file checks).  
- robocopy (dry‑run sizing).  
- rclone (direct Box API streaming, resumable uploads).  

**OPEN THREADS**  
- Confirm Box plan capacity and per‑file limits.  
- Decide whether to proceed with rclone or explore alternative cloud APIs.  
- Monitor C: usage during rclone; ensure no local cache buildup.  
- Validate that the exclude list fully removes all non‑PRISM content before full copy.
