# cam session 35046d5c (2026-06-24, 10.2MB, spine 99KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `a95356c003`: added `scripts/build-domain-knowledge-feeders.mjs` + test – generalizes GIGO‑safe feeder to all domains; outputs to `state/shared/domain-knowledge/`.  
- Commit `5e4c9158ac`: updated `extract-cadcam-tribal-wiki.mjs` with GIGO‑safe filter (`fs.existsSync`) and reclassifier wiring.  
- Commit `5d865b0301`: wired feeder to canonical path, added ownership guard (`weOwnCorpus()`); 11/11 tests passed; live data validated (tooling 312, mill 39, lathe 12, post‑proc 6, speed‑feed 4).  

**DECISIONS**  
- Generalize domain knowledge feeder for every domain; keep feeders in a git‑ignored, regenerable location.  
- Adopt GIGO‑safe filtering: emit only entries whose source file exists (`fs.existsSync`).  
- Conform feeder output to canonical `state/shared/<domain>-tribal-corpus.jsonl`; exclude cad/cam domains from generic feeder.  
- Add ownership guard so the feeder never overwrites cad/cam or hand‑curated corpora.  
- Confirm embedder (`walkMd`) processes only `.md`; JSONL corpora require a dedicated consumer.  
- Route CAD lever to xray/kilo/india; keep CAD/CAM work separate from other domains.  
- Persist durable dispatch record on chat bus to trigger India’s `getDomainCorpus` work.  

**OPERATOR DIRECTIVES**  
- “do the work for the other domains” (explicit request).  
- Implicit continuation commands (“continue”, “do it”).  

**FINDINGS / BUGS**  
- Path‑based classification recovers 0 CAD, 7 CAM; OCR content classification needed for CAD.  
- Vitest config gates reporters/outputFile on `CI=true`; full suite reports not captured without CI flag.  
- 219 failing test files due to uncommitted peer changes; shared tree unreliable.  
- R8/R11 defect: feeder wrote orphan path; no consumer reads it.  
- Embedder only ingests `.md`; JSONL corpora are not auto‑embedded.  
- False positive unreachable‑code warning in feeder’s try/catch block.  
- 0‑byte files for empty domains were materialized then removed.  

**DOMAIN SPECIFICS**  
- Slot “zulu” orchestrator: `chat-slots.mjs`, enforce hook `slot-bind-enforce.mjs`.  
- Checkin pipeline (`commands/checkin.md`) invoked after binding.  
- Leave‑a‑copy‑behind guard protects safety files.  
- Stop_on_failing_tests gate checks test freshness; crossroad auto‑decide for lane commits.  
- Canonical corpus path: `state/shared/<domain>-tribal-corpus.jsonl`.  
- `AIResourceLearningEngine.getCadCamCorpus()` consumes cad/cam corpora.  
- `tribal-by-domain-inject` uses slot→domain to embed corpora into `tribal-embed-index.json`.  
- India’s future `getDomainCorpus` method will provide generic domain corpus access.  

**TOOLS USED**  
- PRISM slot helpers (`chat-slots.mjs`, zulu‑orchestrator lib).  
- Git hooks: `slot-commit-enforce`, `leave-a-copy-behind guard`.  
- Node scripts: `build-domain-knowledge-feeders.mjs`, `extract-cadcam-tribal-wiki.mjs`.  
- Vitest + `node --test` runner.  
- `AIResourceLearningEngine.ts` (getCadCamCorpus).  
- `tribal-by-domain-inject` hook.  

**OPEN THREADS**  
- Wire per‑domain feeders into tribal index / master‑index so specialist slots consume them.  
- Implement OCR content classification pass to recover CAD knowledge (integration with xray/kilo/india pending).  
- Finalize hermes utilization routing (already routed to bravo, may need tuning).  
- India’s `getDomainCorpus` engine method – consume generic domain corpora.  
- Integrate a generic JSONL consumer into `AIResourceLearningEngine` or the embedder.  
- Verify ownership guard protects hand‑curated corpora.  
- Ensure chat bus dispatch remains durable and triggers India’s processing.
