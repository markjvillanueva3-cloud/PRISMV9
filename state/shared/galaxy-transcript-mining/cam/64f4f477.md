# cam session 64f4f477 (2026-06-04, 16.1MB, spine 95KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit a4648b64ba: A1 – 3 JM financial stores added to `DB_MANIFEST.json`, wired into `quoting/` & `business/PATHS.md`; registered as `[database]` surface in `PRISM_SHARED_INDEX_SURFACES.md`. Tests 16/16 passed.  
- Commit 8300622f39: B1 – `documents.jsonl` wired with `jm_die_doc_lookup` engine; schema added to dispatcher; tests 14/14 passed; date parsing fixed; comment restored.  
- Commit 0674947b2c: LF‑normalization of `dataDispatcher.ts` & `dataActionSchemas.ts` after CRLF flip.

**DECISIONS**  
- GPU concurrency for embeddings: batch `/api/embed` with `PRISM_EMBED_CONCURRENCY=8`.  
- `tribal-index-lock.mjs`: `staleMs=600 s`; guards multi‑writer access to `state/shared/tribal-embed-index.json` (507 MB).  
- Single‑source DB registry: `DB_MANIFEST.json` now lists all 33 stores; added as `[database]` surface for fleet discovery.  
- Replace object‑key writes with array pushes in `embed-cited-tips.mjs`; ensures compatibility with `tribal-rerank`.  
- Fix path rendering bug in `wire-db-stores-to-consumers.mjs` (fallback to `source_file`).  
- Use per‑file scrutiny gate instead of full diff to avoid git timeout on 28K uncommitted files.  
- Revert streaming load claim: use `readFileSync` for document index; performance 742 ms vs >30 s.  
- Parse `created_at` dates with `parseDocDate` (US+ISO → epoch) instead of lexicographic compare.  
- Restore LF line endings after CRLF flip that caused 3,378 deletions.

**OPERATOR DIRECTIVES**  
- `/loop [5m] /goal [ path the 3 jm financial stores ]` – recurring loop, verify‑first.  
- `/loop [5m] /goal [ tackle b1 ]` – recurring loop, verify‑first.  
- Use RTX PRO 6000 Blackwell GPU for embedding pipelines.  
- Employ workflow tools: `/system-viz`, `psn`, `obsidian app`, `hermes`, `ollama`.  
- Run verify‑first audit on remaining gap‑list items; report upon completion.

**FINDINGS/BUGS**  
- Cited‑tips bug: object-key writes caused loss of tips (0 → 10 after fix).  
- Lock staleMs bug: default 30 s too short for ~250 s write; increased to 600 s.  
- Undefined path bug in `wire-db-stores-to-consumers.mjs`; fixed fallback to `source_file`.  
- DB_MANIFEST summary drift by_type/by_status off by 3; reconciled.  
- Streaming claim vs actual: engine claimed streaming but used `readFileSync`; corrected comment, kept fast sync load.  
- Date parsing bug on `MM/DD/YYYY HH:MM:SS`; replaced with proper parser via `parseDocDate`.  
- CRLF line‑ending flip in `dataDispatcher.ts` & `dataActionSchemas.ts`; restored LF convention.

**DOMAIN SPECIFICS**  
- Engines/Actions: `embed-wiki-into-tribal-index.mjs`, `embed-engines-into-tribal-index.mjs`, `embed-cited-tips.mjs`, `jm_die_doc_lookup` engine, `ProgramPrintLinkIndexEngine`.  
- Dispatchers: `dataDispatcher.ts` (runtime queries for docs & program‑link lookups).  
- `JMDieDocIndexEngine.ts`: `loadDocIndex` (mtime‑cached), `queryDocs` filters by role/tier/notebook/text/date/print_score.  
- Action `prism_data:jm_die_doc_lookup` with schema, `z.enum`, dispatcher case.  
- Metrics: 111 k docs loaded in 692 ms; live query returns correct docs.  
- Paths: `state/shared/tribal-embed-index.json` (507 MB), `DB_MANIFEST.json`, `quoting/PATHS.md`, `business/PATHS.md`, `PRISM_SHARED_INDEX_SURFACES.md`.

**TOOLS USED**  
- PRISM helpers: `/checkin-juliett`, `/startup-juliett`, `chat-slots.mjs`, `CronCreate`/`CronDelete`.  
- Embedding pipeline: `embed-pool.mjs`, `tribal-index-lock.mjs`, Ollama (`nomic-embed-text`).  
- Testing: Vitest, tsc.  
- Git utilities: `git add`, `git commit <path>`, lock‑sweep hooks, `spawnSync git ETIMEDOUT`.  
- PRISM gap‑list, close‑out pipeline, scrutiny script (opus, claude, analyst), loop tick handoff, goal gate refresh, `/goal` gate.  
- Dispatchers & skills scripts for `prism_data` actions; TypeScript compiler; live data validation tests.

**OPEN THREADS**  
- **C4:** Consolidate 6 embedding sidecars after Sierra’s final node‑embedding write completes (~1.2 GB sprawl pending).  
- Gap‑list ~16 open items: B2, C1–C9, D2–D7.  
- Domain tasks: D3 revenue OCR → xray; D4 CAM RAG → kilo; D5 PDF index → oscar/lima; C8 rollback debris → golf.  
- Audit needed to confirm which `DB_MANIFEST` stores are truly unwired; current gap‑list is hypothesis.
