# cam session db273e77 (2026-06-09, 108.4MB, spine 904KB, 11 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e5ad4ea802` – U‑ALPHA‑HWSYNC‑RETUNES (9 files, audit doc).  
- `f9e953f0e8` – ESLint‑hook path bug fix; G2 file:line citation added to CLAUDE.md.  
- `786bd70566` – MEMORY.md updated (STUB→SCAFFOLDED, stale‑sync flag).  
- `873446a147` – Quality galaxy claimed; CLAUDE.md §5/6/7 populated.  
- `1443283f8b` – 12 AI textbooks registered (`pdf-sources/registry.json`).  
- `3935f8d4ad` – Added `topics`, `source`, `note`; regex fix, env override `PRISM_GOAL_CLEAR_ADVANCE_SLOTS_JSON`.  
- `632335cec6` – `stop-goal-clear-advance.mjs` hook.  
- `1f295f51e6` – `dedupedContext()` helper (used by 8 injectors).  
- `eefef0359a`, `8d2b6cf8e0`, `87e96fa485`, `afe169ab0e` – 4 semantic‑memory injectors.  
- `636d36bf59` – `memory-relevance-inject.mjs`.  
- `75c44d8412` – self‑refreshing cache for semantic recall.  
- `826be35aa4`, `4a939fc35f` – precompact MEMORY_SEED enrichment & status.  
- `8c945662ac` – route‑suggest per‑session gate (25→1 fires/session).  
- `a6aee37203` – consolidated tmp‑orphan sweep (`sweepStaleTmpOrphans`).  
- `c28c8875ca`, `98312e8a08` – concurrency race fix in doctrine rate limiter.  
- `35dc2ec4c3` – subagent bundle OOM mitigation (bounded head‑read).  
- `d92b9f52f4` – system‑viz summary bug fix.  
- `caabc8fea6` – tribal reference node promotion to vault (`Q1`).  
- `d07b148690` – 5‑clause report.  
- `edea8cb893` – Q3 memo‑recall (master‑index gate removed).  
- `60805c36c6`, `b9b223d5e7`, `b7c763bbcd`, `f6f1d425a8`, `1edf02aa26` – Q11 memory→wiki promotion.  
- `d0566c6116` – Q9 dream‑cycle enrichment (nightly cron).  
- `792beb75e8`, `e1b95b05a8` – dead foreign‑machine path resolver (`resolveObsidianMemDir()`).  
- `8da1541c00` – U‑OBS‑RECALL‑NODE‑EXCLUDE (removed 9 571 node_* stubs).  
- `c7e346da99` – U‑OBS‑MEMDIR‑PORTABILITY.  
- `d579626848` – U‑OBS‑WIKILINK‑DANGLING‑FIX (15 819 dangling links removed).  
- `b45f08e732` – U‑OBS‑MEMPATH‑PORTABILITY (4 hot hooks routed).  
- `01c3b15f56` – U‑OBS‑RECALL‑COUNTER‑SERIALIZE.  
- `a9052f63b0` – backendAudit route‑suggest session gate (~8 k token savings).  
- `e80e6e3a41` – Vision‑model single‑source refactor (`VISION_FAMILY_LEADERS`).  
- `89146678bf` – Embed‑progress honesty fix (PID stamp, CLI flag).  
- `415941b1f0` – RTK‑guard false‑positive fix.  
- `2999dac687` – synthesis report capturing ultracode discovery.  
- `58688be398` – system‑viz node‑card OOM fix.  
- `36105372ec` – `ollama-nav-enforce` hook + test.  
- `be52720b32` – pin bridge default model to `gpt‑oss:20b`.  
- `767e8fea4c` – wiki lesson on streaming‑graph pattern (`doc-reflect`).  
- `graph-stream-degree.mjs` – streaming degree helper (eliminates 643 MB graph OOM).  
- `dark-wiki-rank` script – prioritizes 71 dark files for sharding.  
- `U‑ADVISORY‑DECAY` (3 commits) – injection‑based metric, self‑gating (`fleet-reaper-coordinator`).  

**DECISIONS**  
- Adopt `/checkin-alpha` slot‑binding wrapper; fleet‑reaper ownership → **golf**.  
- Disable main‑tree write block for this session (`PRISM_MAINTREE_WRITE_BLOCK_DISABLE=1`).  
- Use async `auto-lint-post-edit.mjs`; skip sync lint hook to avoid latency.  
- Ship quality galaxy, populate CLAUDE.md with verified engine data; leave CHAT‑SLOT‑DOMAINS update to operator.  
- Keep all fixes additive; never merge drift sync or secrets into a goal‑clear commit.  
- Replace fragile regex block‑strip with line‑based dedup for injectors.  
- Fail‑open semantics for all hooks (runtime failures do not block session).  
- Gate backendAudit route‑suggest per‑edit to eliminate ~8 k redundant tokens/session.  
- Serialize recall counter writes (`withExclusiveLock()`) to avoid lost‑update race.  
- Adopt single‑source resolver `resolveObsidianMemDir()` for C:/ vs H:/ homedir split.  
- Use Ollama for navigation enforcement; default model pinned to `gpt‑oss:20b`.  
- Stream graph degree via `graph-stream-degree.mjs` to avoid 643 MB OOM.  
- Prioritize dark‑wiki sharding list (71 files).  
- Implement advisory‑decay gate (injection‑based metric, self‑gating).  

**OPERATOR DIRECTIVES**  
- “finish all remaining tasks”.  
- Ensure ollama models match new CPU/GPU; docker & nvidia nim synergized.  
- Try again (re‑pull request).  
- Read X article via Playwright and verify system setup.  
- Take over golf’s durable lane for model pulls (`ollama-resilient-pull.ps1`).  
- Register Zombie Reaper v2 and OCR Batch tasks via elevated PowerShell.  
- Wire semantic recall into `memory-rag-inject.mjs`.  
- Pull and execute Sierra’s durable find‑cache task.  
- Re‑register scheduled tasks for dream/zombie‑reaper/blueprint‑OCR, weekly‑synthesis cron opt‑in.  

**FINDINGS / BUGS**  
- PowerShell UTF‑8 em‑dash parsing error fixed (`-Encoding utf8`).  
- Vitest 4.1.5 deprecates `poolOptions.threads`; migrated to top‑level `test.*` (16‑thread tuning active).  
- Docker/NIM alignment fine; gemma4 tag real but registry endpoint 404 on tags list.  
- Model pull stalled due to network throughput; each restart re‑downloads layer – cannot resume automatically.  
- `stop_on_failing_tests` hook present but unwired; left untouched.  
- PowerShell script `ollama-resilient-pull.ps1` now detached, survives session end.  
- Memory‑pressure spike (WSL balloon + Ollama) resolved by cache drop.  
- MCP server down → restarted successfully (`HTTP 200`).  
- Dead foreign‑machine path recovered 1 602 stranded “just‑shipped” memos.  
- OOM in subagent bundle mitigated; bounded head‑reads now safe.  
- System‑viz node‑card OOM (~458 MB heap) fixed by `58688be398`.  
- RTK‑guard false positive resolved (`isAlreadyRtk`).  

**DOMAIN SPECIFICS**  
- Engines: `PDFSourceRegistryEngine`, `PDFFormulaExtractionEngine`, `PDFHandbookBatchProcessorEngine`, `PDFMaterialPropertyExtractionEngine`, `PDFTableExtractionEngine`; `MINBatchExtractorEngine.ts`, `McxBatchExtractorEngine.ts`.  
- Semantic‑memory injectors (`memory-relevance-inject.mjs`, `slot-domain-awareness-inject.mjs`, etc.).  
- Route‑suggest gate, subagent bundle OOM mitigation, system‑viz summary.  
- Memory‑index search lib, memo‑embed lib, obsidian‑mem‑dir resolver, dream‑cycle script, weekly synthesis.  
- Metrics: 82–95% token savings (dedup), 17.1 % coverage after audit, 16‑thread Vitest tuning, 8 k token savings per session via backendAudit gate, 15.5 % false positives removed by phantom‑link filter.  

**TOOLS USED**  
- PRISM helpers: `/checkin-alpha`, `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
- Ultracode discovery workflows (`wi5silr6x`, `w9brtuij1`, `wgypolzah`, etc.).  
- Hermes‑workflow orchestrator.  
- System‑viz query scripts, node‑card path.  
- Obsidian app hooks, PSN integration.  
- Local LLMs: `qwen2.5-coder:32b`, `gpt‑oss:120b/20b`; Ollama API (`/api/embeddings`).  
- Scripts/hooks: `memory-relevance-inject.mjs`, `wiki-promo-rerank`, `memory-rag-inject.mjs`, `backendAudit route-suggest`, `tribal-inject-on-edit.mjs`, `tribal-by-domain-inject.mjs`.  
- Utilities: `resolveObsidianMemDir()`, `isNodePointerStub()`, `withExclusiveLock()`.  

**OPEN THREADS**  
- Wire semantic recall into `memory-rag-inject.mjs` (not yet implemented).  
- Implement `ollama-nav-enforce-inject` hook for navigation intents.  
- Resolve V8‑cap sharding for dark wiki embeds.  
- Pull and execute Sierra’s durable find‑cache task; monitor OOM avoidance.  
- Finalize advisory‑decay gate implementation.  
- Expand vault value: orphan‑lint reporting, backlink index, additional dark‑wiki prioritization.  
- Integrate PSN with local LLMs for search/reads.  
- Monitor lock contention under high‑concurrency edits (exclusive lock).  
- Complete remaining ultracode discovery items (#3–#6) in next cron.
