# cam session 928a8226 (2026-06-09, 65.1MB, spine 654KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `scripts/fix-hookify-frontmatter.mjs` – converted 110 malformed `.local.md` hook files, sanitized 28 non‑ASCII.  
- `scripts/remove-dangling-hook-refs.mjs` – removed 6 dangling `*.mjs` references (`linear-roadmap-sync`, etc.).  
- `ollama-resilient-pull.ps1` – replaced em‑dash with hyphen; restored `gpt‑oss:120b` download.  
- `ask‑ollama.mjs` – added `DEFAULT_NUM_PREDICT=1024`.  
- CAG‑router label correction – replaced phantom `qdrant://prism-memory` with real collections.  
- `SYSTEM‑SYNERGY‑GAPMAP‑2026‑06‑08.md` – system‑wide synergy gap map artifact.  
- Embedder overhaul – 8 GB heap flag, 2000‑char clamp for nomic, empty‑text skip guard (`tribal‑embed‑index.json`).  
- Documentation updates (`5467482be4`, `c994bf3aad`) – CLAUDE.md regression log, wiki lesson.  
- `loadTribalIndex` (cap‑safe loader) – 537 MB index restored in 3.4 s.  
- `singleton-service-guard.mjs` – added `--fix` to reap-all + start; idempotent.  
- `docker-service-health-check.mjs` CLI – detects container states, emits `docker start <realName>`.  
- MCP boot‑heap fix (`6ca2f6afd2`) – ensured ≥4096 MB heap for all spawn paths.  
- `stop_on_hook_unregistration.mjs` – JSON‑stdout protocol fix.  
- `U-QP-COST-SAVINGS-TEST` – 13 Vitest cases for CostSavingsTrackerEngine.  
- `U-BW-DISPATCHER-SCAN` – repointed cascade defaults to installed Blackwell tiers; source‑lock extended.  
- `U-BW-DISPATCHER-SCAN-WIKI` – wiki lesson on guard blind‑spot fix.  
- `U-PSN-SNAPSHOT-REFRESH` – regenerated stale PSN snapshot; verified 11 P0 under‑wired pairs real.  
- `U-QP-DIGITALTWIN-TEST` – reference‑value test for DigitalTwinEstimator.  
- `U-QP-MARGIN-FLOOR-GATE` – margin‑floor guard in QuoteEstimatorEngine.  
- `U-QP-MARGIN-FLOOR-SHOPCONFIG` – self‑source floor from ShopConfigurationEngine.  
- `U-QP-NRE-AMORTIZE-TEST-FIX` – corrected stale NRE amortization test.  
- Frontend surfacing commit `87d5c4bf9a` – added pricing.below_margin_floor banner, updated types, 2 R9 tests.  
- `U-QP-FRONTEND-TEST-REPAIR` (commit 8258918f78) – fixed flat‑array quoteHistory mock, ResizeObserver polyfill, null‑guards; 13/13 tests green.  
- `U-QP-OUTBOUND-PROMOTE-GATE` (d294957c4d) – outbound‑alignment promote gate wired; 62 tests green.  
- `U-QP-CALIBRATION-FRESHNESS-PREFLIGHT` (bf10035ec0) – freshness preflight for calibration factors added; 9/9 tests pass.  
- `698cf12ad2` – static list of 4 frozen tasks (superseded by marker).  
- `4141daf9d8` – migration‑freeze marker (`state/shared/MIGRATION-FREEZE-ACTIVE.flag`).  
- `U-FTH-WIKI-LESSON` – wiki entry documenting cry‑wolf fix.  
- `tribal-by-domain-inject` wiki promotion (PSN/tribal sync).  
- `U-QP-COST-BRIDGE-WIRING-TRUTH` – restored router‑aware cost‑bridge awareness generator.  
- `ec597dbcb3` – dispatcher round‑trip test for quote_estimate.  
- `4b0980c56a` – routing‑matrix coverage commit.

**DECISIONS**  
- Root‑cause analysis before patching; avoid random fixes.  
- Convert hook files instead of rewriting parser logic.  
- Flag unwired engines for owner triage, not mass wiring.  
- Fix Ollama pull script encoding issue; keep resilient‑pull workflow.  
- Use local Ollama on RTX PRO 6000 Blackwell; set `OLLAMA_MAX_LOADED_MODELS=3×GPU`.  
- Adopt harmony‑format reasoning models: consume `.thinking` before `.response`.  
- Route synthesis via `resolveSynthesisModel`; default to gpt‑oss:120b for home_blackwell.  
- Batch workflow agents ≤ 3 concurrent to avoid Anthropic rate limits.  
- Persist fleet hygiene with 5‑min scheduled task (`PRISM Fleet Reaper`) and in‑session monitor; kill orphaned processes only for dead slots.  
- Replace fragile `fs.readFileSync(...,"utf8")` with buffer‑based incremental parser to avoid V8 string cap overflow.  
- Guard against silent clobber: readIndex fails loudly, writeIndex refuses >50 % shrink.  
- Treat MCP as reactive‑only service; add one‑command `--fix` that reaps + respawns daemon.  
- Keep Qdrant in single container; ensure proper start command (`TRANSPORT=http`).  
- Document correct restart procedure to prevent operator confusion.  
- Wire cascade defaults to installed Blackwell tiers and close source‑lock blind spot.  
- Add margin‑floor guard in QuoteEstimatorEngine with caller override, sourced from ShopConfigurationEngine.  
- Keep `.ts‑1` artifact untouched; harmless.  
- Use ultracode workflow for planning/coordination; use ollama for grunt tasks per directive.  
- Fail‑closed gating: synthetic or placeholder outcomes withheld from live writes; real data promoted only on reliable reference.  
- Calibration factors must be freshness‑checked before application; stale (>24 h) factors warned/rejected.  
- OCR‑$1 noise filter required in OutboundPriceIndexEngine before enabling outbound gate.

**OPERATOR DIRECTIVES**  
- `export PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1` (session‑scoped bypass of legacy allowlist hook).  
- Edit `C:/Users/wompu/.claude/settings.json`: remove PreToolUse entry for `golf-slot-write-allowlist.mjs`.  
- Continue working toward session‑scoped Stop hook: “synergize ollama, docker, qdrant, obsidian vault app, PSN, /system‑viz, prism galaxies, frontend+backend build, claude.md, memories, wiki, tribal knowledge, prism awareness, gsd, tdd, skills, scripts and hooks | goal clear…”.  
- “Synergize ollama (make sure we pulled correct models relative to gpu, cpu, nve ssd and 128 gb of ram), docker, qdrant, obsidian vault app, PSN, /system‑viz, prism galaxies, prism front end and back end build, claude.md, memories, wiki, tribal knowledge, prism awareness, gsd, tdd, skills, scripts and hooks | goal clear: everything wire, tested, validated and synergized across the entire system, node by node, galaxy by galaxy.”  
- “Utilize ultracode for planning, deep reasoning and orchestrating + ollama local llm for grunt work like searching, reads and coding and building tasks that it's qualified for with our strongest models.”  
- `/startup-charlie /loop [10m] /goal …` – auto‑commit each unit, finish remaining phases, upgrade for new hardware, achieve fully closed‑loop quoting ready for JM Die.

**FINDINGS/BUGS**  
- Hookify rule files: 110 missing YAML frontmatter warnings; one file caused charmap decode error.  
- Dangling module references: 6 Cannot find module errors due to non‑existent `.mjs` hooks.  
- Unwired engines audit: 90 built engines with no dispatcher/route/hook consumer (53 core, 23 bridge, 14 ops).  
- Academy tribal routing fallback to “general” domain; requires corpus work if desired.  
- Ollama resilient‑pull script parse error (em‑dash) stalled gpt‑oss:120b download at 12 %.  
- Synergy bug: gpt‑oss:120b returned empty `.response` due to `num_predict` default consumed by reasoning model’s `.thinking`; fixed with `DEFAULT_NUM_PREDICT=1024`.  
- Rate limiting: 11 concurrent subagents triggered Anthropic throttle; resolved by batching to 3 agents.  
- Phantom Qdrant label: CAG‑router advertised nonexistent `qdrant://prism-memory`; corrected to real collections.  
- Embedding OOM: `tribal‑embed‑index.json` (~533 MB) exceeded V8 string‑length limit; replaced with buffered loader and streaming JSON support.  
- Empty‑text guard: many wiki files produced empty embeddings; added `isEmbeddable()` filter.  
- MCP server OOM on boot due to 384 MB heap cap; fixed by ensuring ≥4096 MB heap for all spawn paths.  
- cmdMatch regex backslash‑only bug caused guard to miss daemon; corrected.  
- stop_on_hook_unregistration protocol mismatch (exit code vs JSON stdout); fixed.  
- /system‑viz find OOM due to 384 MB cap; noted but not fixed.  
- Fleet‑task‑health WARN was cry‑wolf alert from disabled tasks; partitioned out of degraded set.  
- V8 hard string cap (512 MiB) caused tribal index parse failures.  
- Default Node heap (~2 GB) OOMs when parsing >150 MB JSON indices.  
- Read-index catch silently returned empty index → auto‑reconnect spawned daemon that never served :3100, leading to duplicate daemons.  
- MCP restart docs contained wrong commands (`--port 3100`, compose service).  
- Duplicate orphan daemons caused port‑bind conflicts and intermittent MCP outages.  
- OCR noise causes reference median ≈$1.005; IQR collapse marks reference as reliable incorrectly, leading to false predicted‑high drift flags.  
- Stale calibration factors applied silently at quote time (ignored `isStale` flag).

**DOMAIN SPECIFICS**  
Engines/Actions:  
- host‑aware‑synthesis‑model, resolveSynthesisModel, ask‑ollama.mjs (`callOllama`), CAG‑router hotSources, tribal‑embed‑index loader, tribal‑rerank.mjs, CostBridge, AccountingHardeningEngine, AwarenessGenerator, WEDMQuoteBridgeEngine, CostSavingsTrackerEngine, QuoteEstimatorEngine (margin‑floor gate), DigitalTwinEstimator, OutboundPriceIndexEngine, QuotingClosedLoopEngine, QuotingActiveFactorLoaderEngine, QuotingActualOutcomeLoaderEngine, QuoteOutcomeFeedEngine, ActualCostEngine, ShopConfigurationEngine.  

Dispatchers/Guard:  
- fleet reaper (scheduled task + in‑session monitor), MCP server watchdog / supervisor / daemon helper, singleton-service-guard, stop_on_hook_unregistration, fleet-task-health-watch, populate-tribal-vault, aiReasoningDispatcher.ts, ModelRoutingEngine.ts, PostToolUse group, business dispatcher for `quote_estimate`.  

Metrics/Flags:  
- GPU utilization (RTX PRO 6000), Ollama model load counts, Qdrant collection sizes (`prism_engines`, `prism_skills`, `prism_formulas`), embedder index size (~533 MB), rate‑limit counters, MCP port health :3100, daemonCount, task status counts, margin_floor_pct, below_margin_floor, dfm_warnings, MAPE, medianRatio, referenceReliable flag.  

Paths:  
- H:/prism/...; node shim `/h/.claude/bin/node`; portable-node; `state/shared/MIGRATION-FREEZE-ACTIVE.flag`; `install-vault-rot-sentinel-cron.ps1`; `jm-sold-orders.json` (real outbound data); `tribal‑embed‑index.json`.

**TOOLS USED**  
PRISM tooling: Ollama CLI/API, Qdrant, Obsidian vault app, PSN, `/system‑viz` scripts, Prism galaxies, frontend/backend build scripts, CLAUDE.md, memories, wiki, tribal knowledge, prism awareness, GSD, TDD, skills.  
Ultracode workflow engine for orchestration; Node.js (scripts, tests); PowerShell (`ollama-resilient-pull.ps1`, `install-vault-rot-sentinel-cron.ps1`); Docker CLI for container health checks; Git hooks and shared‑tree commit prefixes; Vitest, Jest, TypeScript compiler (`tsc`); CI tooling: 3‑of‑3 scrutiny reviewers, ledger.

**OPEN THREADS**  
- Wire unwired engines per domain owner; archive/delete as needed.  
- Finalize Ollama model roster sync across fleet; monitor GPU/Ollama routing hints.  
- Qdrant tribal migration to avoid V8 string/heap caps; move growing JSON index into Qdrant.  
- Full re‑embed of lost ~29 K entries; write‑side sharding once >512 MiB.  
- Persistent MCP supervisor evaluation beyond reactive `--fix`.  
- Implement OCR‑$1 noise filter in OutboundPriceIndexEngine before enabling outbound gate.  
- Real data ingestion pipeline for actual quote outcomes (ERP credentials pending).  
- Full production readiness: integrate with JM Die’s ERP/QuickBooks connector, validate live quote generation path.  
- Per‑galaxy synergy across foxtrot, whiskey, mike, kilo, delta, charlie’s peers; frontend merges (quebec).  
- Implement cost‑bridge router consolidation fix; update awareness generator snapshot.  
- Add margin‑floor guard to QuoteEstimatorEngine; source from ShopConfigurationEngine.  
- Resolve stale calibration factors and enforce freshness preflight.  
- Ensure routing matrix coverage for all substrates (`4b0980c56a`).  
- Finalize closed‑loop learning pipeline: provenance gate, synthetic outcome promotion blocking.  
- Confirm that the fleet‑task‑health disabled‑task marker implementation is complete.
