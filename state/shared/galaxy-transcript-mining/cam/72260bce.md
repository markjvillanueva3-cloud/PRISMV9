# cam session 72260bce (2026-06-19, 13.3MB, spine 104KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED (builds/commits)**  
- `8cfd4da130`: OCR grinder switched to continuous “do‑it‑all‑until‑complete” (unlimited ExecutionTimeLimit, 30 min backstop).  
- `9c7943f019`: Blueprint/GD&T tribal‑knowledge injection plan and drop‑zone.  
- `27659a5c8f`: Fast‑exit R16 gap‑closure (`--retry-failed` guard now excludes `--real-png/--real-dir`).  
- `73582a78c0`: Added `--retry-failed` recovery mode + corrected recall diagnosis (transient grind failures).  
- `ed8dcf451b`: Closed zulu VLM‑ladder work‑order; built reusable probe tool (`probe-vision-model.mjs`) and documented bare `qwen3-vl:32b` unusability.  
- `bfcd8256fe`: Fixed probe’s dim‑key bug (parsed.dimensions vs parsed.extraction.dimensions).  
- `1f16ca589c`: Updated backlog – P0.3 scan‑preprocessing refuted; added evidence that page‑0‑only OCR is root cause of recall = 0.  
- `d820c15936`: Implemented multi‑page OCR fix (rasterize all pages, union dims); validated end‑to‑end recall 0 → 0.21 on a test part.  
- `8199b56166`: Added research backlog for blueprint‑reading improvements.  
- `scripts/lib/ocr-training-loop-lib.mjs`: added retry‑status handling and corpus‑drain logic (`isCorpusDrained()`).  
- `extractProgramGT`: added `programGtReliability`, `contourFraction`, `gtReliable`, `gtClass`.  
- Tests for GT reliability classifier (18 tests pass after ASCII‑guard fixes).  
- Wired reliability flags into `validate-perfect-parts.mjs`; skip contour parts before OCR, update report buckets.

**DECISIONS (architecture/scope + why)**  
- Adopted slot‑binding wrapper `/checkin-xray` → `chat-slots.mjs` → standard `/checkin`.  
- OCR grinder continuous; added `--until-complete` flag and `isCorpusDrained()` fast‑exit.  
- Added `--retry-failed` mode to re‑run transient failures (≈15 % recoverable).  
- Multi‑page OCR fix: rasterize all pages, union dims → recall raised from 0 to ~0.32 (test part 0.21).  
- Keep calibrated high‑confidence singletons; do not relax corroboration gate immediately.  
- Program‑derived GT only for stepped parts; exclude contour parts via `gtReliable` flag and `contourFraction`.  
- Confirm live OCR path (`run‑ollama‑vision‑extract.mjs`) is multi‑page; no page‑0‑only leak.

**OPERATOR DIRECTIVES (verbatim asks)**  
- “change from nightly to do it all until complete.”  
- “build it all” (run the existing TRUE‑test harness).  
- “do everything that will improve blueprint reading.”  
- “keep pushing” (accept risk of a safety‑relevant change).

**FINDINGS/BUGS**  
- Page‑0‑only OCR on multi‑page PDFs caused recall = 0; fixed by rasterizing all pages.  
- Ensemble corroboration filter not cause; `fused.dimensions` already includes singletons.  
- Bare `qwen3-vl:32b` produces empty output and is ≈6× slower than ensemble.  
- Scan quality not primary issue; clean drawings read 24 dims, scanned docs that are non‑drawings return nothing.  
- Transient grind failures (~15 %) recoverable via `--retry-failed`.  
- Trainset plateau due to calibration gating on 1‑of‑2 singletons.  
- Program‑derived GT over‑counts callouts on contoured parts; introduced `gtReliable` flag and `contourFraction`.  
- Real OCR weakness still present but masked by unreliable GT for contour geometries.

**DOMAIN SPECIFICS (engines/actions/dispatchers/metrics/paths)**  
- **Engines / Dispatchers**: `/checkin-xray`, `chat-slots.mjs`, `vision-model-select.mjs`, `blueprint‑ocr‑training‑loop`, `vision‑ensemble‑fuse`, `run‑ollama‑vision‑extract`, `pdf‑to‑png.py`, `page‑classify.mjs`.  
- **Scripts / Hooks**: `probe-vision-model.mjs`, `ocr-al-queue-surface.mjs`, `validate-perfect-parts.mjs`, `blueprint‑ocr‑training-loop.mjs`, `ocr-training-loop-lib.mjs`, `extractProgramGT`.  
- **Paths**: `H:/prism/.claude/...`, `resources/blueprint-gdt-corpus/`; PDF → PNG preprocess; OCR → dimension list; CAD STEP → GT extraction; CNC program → GT extraction.  
- **Metrics**: recall, precision, F1, dims_extracted, gt_matched, contourFraction, gtReliable flag.

**TOOLS USED**  
- PRISM scripts: `/checkin-xray`, `/checkin`, `chat-slots.mjs`, `vision-model-select.mjs`, `probe-vision-model.mjs`, `ocr-al-queue-surface.mjs`, `validate-perfect-parts.mjs`, `blueprint‑ocr‑training-loop.mjs`, `ocr-training-loop-lib.mjs`.  
- Python utilities: `pdf-to-png.py`, `page-classify.mjs`.  
- VLMs via Ollama: `qwen3-vl:8b-instruct`, `qwen2.5vl:7b` ensemble, bare `qwen3-vl:32b`.  
- Windows Scheduled Task “PRISM OCR Training Loop”.

**OPEN THREADS (what is still to build)**  
1. Trace live print‑reading path (`cad_pdf_blueprint_extract`, `blueprint_to_quote`) for page‑0‑only bug and apply multi‑page fix.  
2. Run full `--retry-failed` pass when GPU headroom allows to recover transient failures and grow trainset.  
3. Evaluate adding calibrated high‑confidence singleton path (future task #9).  
4. Consider GT normalization or 3rd VLM if future recall gains are needed.  
5. Finalize metric aggregation to include only stepped parts; verify across larger part set.  
6. Handle known‑limit cases (G70/G71 invisible contours, NaN‑poison latent/unreachable, first‑move plunge classified as contour).  
7. Integrate GT reliability classification into future training loops and live dispatchers.  
8. Validate that new skip logic does not drop valid OCR results in edge cases.
