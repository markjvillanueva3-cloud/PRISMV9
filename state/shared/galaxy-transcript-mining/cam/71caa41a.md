# cam session 71caa41a (2026-05-23, 58.2MB, spine 178KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `72130062c3` – slot‑keyed handoff read tier + psk composite integration.  
- `6150dd6eb2` – U‑ZM1‑01/02/03 (precompact injection, opt‑in store, integration fixes).  
- `a1575d05ed` – PRISM_BOOT_SLOT env‑var for session‑start auto‑resume.  
- `1480ae6b95` – stable “PRISM <slot>” caption convention; single WT window enumeration.  
- `108e27b32d` – UIA tab‑focus actuator (multi‑window enum, pane safety).  
- `b9e65d78d2` – empirical close‑out proof for 6 slots actuated; updated docs/memory entries.  
- `U‑ZM1‑04` – core MS0/MS1 zebra orchestrator logic & autonomous task registration.  
- `U‑ZM1‑05` – rename‑window‑intercept, single‑sweep window enumeration, honest tabbed‑fleet diagnostic.  
- `U‑ZM2‑01` – UIA tab‑focus actuator (`wt-tab-focus.mjs`) with pane‑count guard; env‑var override for `$false`.  
- `U‑ZPSN02` (commit 4a6ef551da) – full slot‑soul population, awareness index rebuilt to 27 slots, `[psn:…]` tags fleet‑wide.  
- `U‑ZEBRA08` spec (commit 83876baae4) – design doc for 6‑account rotation at 95 % session limit.  
- Commit of `settings.json` (C→H mirror).  
- Commit after doc reflection.  
- Commit of `RECENT-SHIPMENTS` append.

**DECISIONS**  
- Adopt slot‑keyed handoff read to enable resume post‑restart.  
- Introduce PRISM_BOOT_SLOT env‑var for auto‑resume on startup.  
- Build fleet launcher: 3 WT windows × 5 PowerShell tabs = 15 slots, each `/checkin‑<slot>`.  
- Use single enumeration of WT windows; stable “PRISM <slot>” titles to avoid title‑mismatch bugs.  
- Drop per‑tab title resolution; use UIA tab selection + pane focus as sole actuation path.  
- Enforce `PRISM_SENDKEYS_CONFIRM` env var to bypass PowerShell `$false` argv bug.  
- Ship PSN‑aware `[psn:…]` tags fleet‑wide (U‑ZPSN02); defer target‑side parsing until U‑ZPSN03.  
- Persist slot titles as “PRISM <slot>” for deterministic HWND resolution.  
- Hook placed immediately after `slot-soul-inject` (line 1129) for correct in‑spirit behavior.  
- Use canonical env‑knob escape hatch via Bash + Node script due to Edit tool env limitation.  
- Mirror C: → H: using `c-to-h-mirror` for settings wiring.

**OPERATOR DIRECTIVES**  
- `/goal [complete all tasks to make zebra fully operational and fully autonomous building operational | 100% proven full automation orchestrated by zebra] /loop [5m] /goal`.  
- Earlier: `/checkin-bravo`, `/startup-bravo`, `/loop …`.  
- “please make sure hermes zebra agent is fully built.”  
- “assess zebra's synchronization with claude code cli + PSN.”  
- “complete all zebra units | assess zebra's hermes agent capability.”  
- “fix everything” (triggered full rebuild).  
- User command: “continue”.  
- Arguments: `/goal [ complete all zebra units | assess zebra's hermes agent capability ] /loop [1m] /goal`.

**FINDINGS/BUGS**  
- Instance‑keyed handoffs caused resume failure → fixed with slot‑keyed read tier.  
- Session‑start auto‑resume only triggered on compact/clear → added PRISM_BOOT_SLOT env‑var.  
- Precompact injection missing → added to `composeSendKeysText`.  
- Opt‑in store required for persistence across restarts → implemented `zebra-opt-in.mjs`.  
- UIA tab resolution issues: multiple WT windows, title mismatch (“PRISM <slot>” vs bare slot) → solved by stable caption convention and multi‑window enum.  
- `send-keys-to-window.ps1` – `-Confirm:$false` bug due to argv parsing → added env‑var override `PRISM_SENDKEYS_CONFIRM`.  
- Stale slots in `chat-slots.json` caused missing chats → cleaned via reaper; PID‑liveness gate pending (U‑ZM2‑04).  
- Title mismatch caused “no‑match”/“title‑missing” diagnostics.  
- PSN tags never consumed on target side; U‑ZPSN03 pending.  
- `Get‑Process WindowsTerminal.MainWindowHandle` returned only one WT window → UIA RootElement walker now enumerates all top‑level windows.  
- Exit 255 from stdin CRLF on Windows echo; resolved by file‑redirect.  
- API rate limiting error encountered.  
- Size guard tripped during `MEMORY.md` pointer handling (21573B).  

**DOMAIN SPECIFICS**  
- Engines/Actions: per-agent-handoff.mjs, psk.mjs, checkin.md pipeline, slot‑binding wrapper (`/checkin-bravo`), stop-tab-blink.mjs hook, Launch-PRISM-Fleet.ps1 launcher, slot-tab-boot.ps1 tab init, rename-window-intercept.mjs caption prefixing, zebra-orchestrator-sweep.mjs decision loop, zebra-opt-in.mjs opt‑in store, UIA tab focus script (wt-tab-focus.mjs), send-keys-to-window.ps1.  
- Dispatchers: chat-slots.mjs for slot state, per-agent-handoff helper, psk kernel, zebra orchestrator sweep dispatcher, UIA dispatcher.  
- Metrics/Paths: handoff staleness, drift, pressure level, token estimate, decision gate (dry‑run vs live), SendKeys char count; paths include `H:/prism/.claude/helpers/per-agent-handoff.mjs`, `.claude/kernel/psk.mjs`, `.claude/commands/checkin.md`, `.claude/hooks/stop-tab-blink.mjs`, `Tools/prism-fleet/Launch-PRISM-Fleet.ps1`, `slot-tab-boot.ps1`, `rename-window-intercept.mjs`, `zebra-orchestrator-sweep.mjs`, `zebra-opt-in.mjs`, `wt-tab-focus.mjs`.  
- PRISM chat‑slot fleet: 26 NATO slots (alpha–zulu) + “zebra”; slot‑souls stored as YAML+Markdown in `state/shared/slot-souls`.  
- Hermes agent framework drives zebra orchestrator; PSN engine supplies capability fingerprints.  
- UIA tabbed‑fleet topology: two WT windows, each with multiple split panes (ConPTYs).  
- SendKeys actuation pipeline: `/precompact`, `/compact|/clear`, `/checkin‑<slot> … [psn:…]`.  

**TOOLS USED**  
- PRISM helpers: per-agent-handoff.mjs, chat-slots.mjs.  
- Kernel: psk.mjs.  
- Commands: checkin.md.  
- Hooks: stop-tab-blink.mjs.  
- Launcher scripts: Launch‑PRISM‑Fleet.ps1, slot‑tab‑boot.ps1.  
- Orchestrator libs: zebra-orchestrator-sweep.mjs, zebra-opt-in.mjs, rename-window-intercept.mjs.  
- UIA script: wt-tab-focus.mjs.  
- PowerShell send‑keys-to-window.ps1.  
- `scripts/lib/wt-tab-focus.mjs` (UIA tab focus).  
- `zebra-orchestrator-sweep.mjs`.  
- `rename-window-intercept.mjs`.  
- Hooks: `slot-bind-enforce.mjs`, `session-limit-detector.mjs`.  
- Skills: `switch-claude-account.ps1`, `switch-claude-profile.ps1`.  
- Bash + Node script for env‑knob escape hatch.  
- Edit tool (env limited).  
- File‑redirect for stdin handling.

**OPEN THREADS**  
- Actuate all 15 slots (currently 6 proven); handle missing tabs after cleaning stale slots.  
- Validate SendKeys execution in live environment post‑dry‑run; confirm no side effects during the 24 h grace period (~2026‑05‑23 20:06).  
- Finalize documentation and memory entries for full automation status.  
- Integrate U‑ZM2‑02 (UIA pane focus), U‑ZM2‑03 (execute‑mode smoke after grace expiry), U‑ZM2‑04 (PID‑liveness gate in `pickActionableSlots`).  
- Implement account rotation: build `switch‑claude‑account.ps1`, sidecar population, watchdog hook for 95 % session limit.  
- Resolve cherry‑pick integration before adding to tree; confirm E2E smoke test passes after hook placement fix.  
- Handle API rate limiting retries.  
- Finalize pipeline wiring and documentation reflection.
