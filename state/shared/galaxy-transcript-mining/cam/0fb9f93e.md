# cam session 0fb9f93e (2026-05-27, 39.2MB, spine 110KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Regenerated `LAUNCH‑PRISM‑FLEET.bat` (alphabetical slot order, size guard >40 MB, absolute paths for `wt.exe`, `pwsh.exe`, `claude.cmd`).  
- Updated `regenerate-launch-fleet.mjs`: JSONL size guard + staleness guard (>90 min), new tab‑title `<slot> | <domain>`.  
- Added MCP daemon preflight check `http://127.0.0.1:3100/health` before chat launch.  
- Fixed fleet reaper scheduled task; sweep killed 143 orphan `node.exe`, 493 `bash.exe`; left 21 live Claude sessions.  
- Commit **61074fa740**: `mcp‑server/web` compiles with 0 TS errors (cascade fixes).  
- Commit **420052f83d**: added staleness guard to `regenerate-launch-fleet.mjs`, updated bat.

**DECISIONS**  
- Use absolute paths for terminal/PowerShell/Claude executables.  
- JSONL size threshold default 40 MB; fallback to `/checkin‑<slot>` when resume fails or slot stale (>90 min).  
- Preflight MCP daemon before chat launch to avoid port 3100 race.  
- Fleet reaper always‑on via durable scheduled task; disable only with `PRISM_FLEET_REAPER_DISABLE=1`.  
- `findSessionUuidForChatId()` returns `{uuid, sizeMb, ageMin}` for staleness logic.  
- All TS errors resolved via cascade fixes; no `any` sprinkling.

**OPERATOR DIRECTIVES**  
- Ensure chats connect to MCP server immediately and maintain connection.  
- Launch each slot with most recent chat or fresh handoff (`/checkin‑<slot>`).  
- Verify staleness guard active when running `LAUNCH‑PRISM‑FLEET.bat`.  
- Confirm fleet reaper / monitor tasks (Docker, Ollama, NVIDIA NIM) are running and orphaned processes cleaned.

**FINDINGS/BUGS**  
- Missing `wt.exe` in PATH caused launcher failure; fixed with absolute path.  
- Resume crashes for slots with JSONL >40 MB (alpha, delta, echo, whiskey); resolved by size guard.  
- MCP daemon race caused connection failures; solved by preflight check.  
- Orphaned `node.exe`/`bash.exe` processes (~143 nodes, 493 bash) killed during fleet‑reaper sweep.  
- Stale chat bindings in `chat-slots.json` (echo, foxtrot, victor) fixed with staleness guard.

**DOMAIN SPECIFICS**  
- PRISM multi‑domain architecture: milling, lathe, wire, databases, AI/ML, post‑processors, etc.  
- NATO slot system (alpha…whiskey) drives fleet launcher and chat routing.  
- MCP server at 127.0.0.1:3100; fleet reaper monitors `node.exe` (Claude hooks), `bash.exe`, `git.exe`.  
- Windows Terminal (`wt.exe`) for tabbed UI; PowerShell scripts; Node.js for Claude CLI & MCP bridge.

**TOOLS USED**  
- PRISM scripts: `regenerate-launch-fleet.mjs`, `chat-slots.mjs`, `fleet-reaper-sweep.mjs`.  
- Windows utilities: `wt.exe`, `pwsh.exe`, `claude.cmd` (npm shim).  
- Node.js, npm packages (`@anthropic-ai/claude-code`, `@types/node`, `zod`, `express`, etc.).  
- Git, Bash, Grep processes spawned by chats.  
- TypeScript compiler `tsc` with updated `tsconfig.json` (noImplicitAny relaxed, noUnusedLocals/Parameters disabled).

**OPEN THREADS**  
- Verify MCP preflight stability under load; monitor rate‑limit spikes.  
- Ensure fleet reaper scheduled task remains registered and functional after system changes.  
- Monitor orphan process cleanup frequency to prevent memory pressure.  
- Run `LAUNCH‑PRISM‑FLEET.bat` in production to confirm staleness guard behavior.
