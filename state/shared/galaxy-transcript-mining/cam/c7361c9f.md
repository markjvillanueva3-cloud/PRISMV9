# cam session c7361c9f (2026-06-09, 25.8MB, spine 197KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Fleet‑reapers & monitors restored (Zombie Reaper v2 enabled; migration‑freeze marker cleared).  
- ascii‑guard diff‑aware PreToolUse block added – 3/3 scrutiny passed.  
- MCP daemon repaired: heap floor ↑ 4 GB→24 GB, watchdog RSS threshold ↑ 3 GB→18 GB.  
- Docker, WSL, Ollama, Qdrant running; NIM containers stopped & removed from routing.  
- Slot‑worktree commit routing fully active fleet‑wide; backlog deferred to quiet‑window commit.  
- `U-MCP-CONCURRENCY-HARDEN` – MCP server hardened for unlimited parallel agents (3 commits).  
- `G10` – Crash‑critical auto‑re‑enable guard live‑validated (4 files, 14 tests).  
- `plan v2` – Golf galaxy completion plan committed (`state/shared/golf-galaxy-completion-plan-2026-06-09.md`).  
- `G5` – Closed‑redundant boost‑stamp janitor.  
- `G1` – Noise premise corrected; 22K untracked files shipped, `.git/info/exclude` patched.

**DECISIONS**  
- Lift migration freeze after PC upgrade; keep all reapers running.  
- Enforce ascii‑guard diff‑aware block; audit other advisory hooks.  
- Use Ollama for offload tasks; NIM containers redundant.  
- Adopt bounded ≤3‑agent ultracode workflow to avoid 9‑agent rate limits.  
- Apply 3/3 scrutiny gate to all substantive changes.  
- Leverage `gpt-oss:120b` (grunt reads), `qwen2.5-coder:32b` (code gen), `1.5b` (classification).  
- Harden MCP against unlimited parallel agents via inflight‑aware watchdog + bounded semaphore.  

**OPERATOR DIRECTIVES**  
- Start Qdrant & Docker; ensure WSL and Ollama active – **done**.  
- Verify fleet reapers/monitors running – **done**.  
- Confirm NIM containers not needed – **no**.  
- Improve context (age‑stamp Stop hook) – implemented.  
- Commit to own slot branches across fleet; fix git tree issues – verified, backlog deferred.  
- Enforce advisory hooks (em dashes, ascii) – ascii‑guard added.  
- Utilize Ollama for task efficiency – adopted.  
- Tune MCP heap/watchdog; address concurrency gap – identified, pending fix.  
- Support unlimited workflow agents (ultracode mode) – MCP hardening planned.  
- Use `/startup-golf` wrapper to force‑take golf slot and audit.  
- After `/compact`, resume G5→G1→G10 sequence – no further prompts.

**FINDINGS/BUGS**  
- NIM containers saturated GPU & downgraded router – stopped.  
- ascii‑guard advisory only → now enforced diff‑aware block.  
- Stop hook warning stale age → timestamp fixed.  
- Migration freeze marker cleared.  
- MCP watchdog restarted at 3 GB RSS → thresholds raised.  
- Concurrency gap: each `/mcp` request spawns fresh `McpServer`; memory spikes; watchdog blind to inflight – need inflight‑aware watchdog + bounded semaphore.  
- RequestSemaphore leak on client disconnect → fixed with `acquireRequestSlot`.  
- Active‑chat‑boost premise stale → TTL sweeps orphans.  
- G1 noise premise false: 22K untracked files genuine backlog; junk removed.  
- Drift test #69 failed (missing synthesis‑cron names) – cataloged, later reverted.  
- Index.lock contention during revert – resolved by waiting for live git process.  
- Freeze‑gate bug (`if (cfg.migrationFreezeActive) return []`) suppressed re‑enables – removed.  
- Drift cry‑wolf overreach reverted (`U-GOLF-G10-DRIFT-REVERT`).  
- Orphan cron lock cleared.

**DOMAIN SPECIFICS**  
- **Fleet‑reaper**: orphan‑process janitor, GPU coordinator, Ollama routing hint emitter.  
- **ascii‑guard**: PreToolUse block blocking smart punctuation (diff‑aware).  
- **MCP server**: `buildRequestServer`, supervisor, watchdog, singleton-service-guard; metrics (`inflight`, `peakInflight`).  
- **Slot‑worktree routing**: via `chat-slots.mjs` & `settings.json` hooks.  
- **Encoding‑guard.mjs**: BOM preservation.  
- **nim‑docker‑launcher.mjs**: NIM container orchestration.  
- PRISM task categories: `CRASH_CRITICAL_TASKS`, `EXPECTED_DISABLED_TASKS`, `KNOWN_PRISM_TASKS`, `MUST_EXIST_TASKS`.  
- Health assessment: `aggregateHealth`; re‑enable logic: `selectReenableTargets(classified, cfg)`.  
- Watchdog cycle: `runOnce → sampleScheduledTasks → classifyTask → aggregateHealth → selectReenableTargets → reenableTasks`.

**TOOLS USED**  
- PRISM scripts: `/checkin-golf`, `fleet-reaper-sweep.mjs`, `chat-slots.mjs`.  
- Hooks: `ascii-guard.mjs` (PreToolUse), `fleet-task-health-stop.mjs`.  
- MCP server files: `index.ts`, `mcp-server-supervisor.mjs`, watchdog scripts.  
- Docker, WSL, Ollama CLI, Qdrant service.  
- Git tooling for slot‑branch routing & conflict resolution.  
- Node.js (`fleet-task-health-watch.mjs`), `node:test`.  
- Ultraforce/Ultracode for grunt work generation (Ollama).  
- CronCreate for recurring `/goal` loop.

**OPEN THREADS**  
- Implement inflight‑aware watchdog + bounded semaphore to harden MCP.  
- Audit and enforce other advisory hooks (e.g., encoding‑guard).  
- Monitor GPU utilization post‑changes; verify Ollama offload take‑rate improves.  
- Re‑introduce NIM containers if specific workloads require them.  
- Execute remaining golf units: G9 rest, G6, G2, G8, G4.  
- Auto‑sync `KNOWN_PRISM_TASKS` to include new synthesis cron tasks.  
- Final cleanup of stale `close-out-candidates`.  
- Windows‑side disable trace for recurring `PRISM Zombie Reaper v2` disable events.
