# cam session 24fb195f (2026-06-23, 2.5MB, spine 31KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Updated global `~/.claude/settings.json` (mirrored to `H:/prism`) – set `"model": "opus[1m]"`.  
- Removed `autoCompactWindow: 800000` and `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE: 95` from project `H:/prism/.claude/settings.json`.  
- Verified JSON validity in all layers; changes now live in the shared configuration.

**DECISIONS**  
- Chose a *shared* config change because no Desktop‑only settings directory exists.  
- Pinning the `[1m]` suffix on the model (`opus[1m]`) is the only documented knob that guarantees 1 M context across compaction boundaries.  
- Removing the undocumented `autoCompactWindow` cap eliminates the 800 K limit that was overriding the desired 1 M window.

**OPERATOR DIRECTIVES**  
- “After compaction it switched back to 200k context instead of 1m. Please fix the settings just for the Claude Code desktop app not the CLI so it hits the full 1m context, not the 800k cap.”  
- Earlier directive: “please fix whatever is causing my claude code desktop app to not compact properly.”

**FINDINGS/BUGS**  
- PRISM’s `precompact-auto-trigger.mjs` blocks tool calls at ~900 K tokens; on Desktop it cannot trigger `/compact`, so native autocompact is preempted.  
- The 800 K cap originates from an undocumented `autoCompactWindow: 800000` entry in the project settings, not a global knob.  
- No Desktop‑only config directory exists (`AppData/Roaming/.claude` does not exist); Desktop shares the same global and project settings as CLI.  
- The 200 K context after compaction is an undocumented behavior; pinning `opus[1m]` mitigates it but may still revert if a Claude Code bug persists.

**DOMAIN SPECIFICS**  
- Slot‑binding wrapper `/checkin-alpha` → `/checkin` pipeline (steps 3–14).  
- Chat‑slot helpers: `chat-slots.mjs`.  
- Precompact hooks: `precompact-auto-trigger.mjs`, `precompact-trigger.jsonl`.  
- Settings layers: global `~/.claude/settings.json`, project `H:/prism/.claude/settings.json`.  
- Environment variables: `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE`, `PRECOMPACT_SOFT_TOKENS`, `PRECOMPACT_HARD_TOKENS`, `autoCompactWindow`, `DISABLE_AUTO_COMPACT`.

**TOOLS USED**  
- PRISM CLI commands: `/checkin-alpha`, `/checkin`.  
- Node scripts: `chat-slots.mjs`, `precompact-auto-trigger.mjs`.  
- Git for branch detection.  
- Documentation lookup agent (for model‑suffix rules).  

**OPEN THREADS**  
- Commit the project‑settings change (`autoCompactWindow` removal) so fleet automation does not revert it.  
- Verify that after a Desktop restart, context stays at 1 M even after compaction; if it still drops to 200 K, file a bug with Anthropic.
