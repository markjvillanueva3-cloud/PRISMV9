# cam session 63dafa30 (2026-06-25, 6.7MB, spine 61KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `f5a64533de` – system‑viz‑query: added usage guard, 16 GB self‑respawn for graph commands (fixed OOM on `--help`, `coverage-by-domain`).  
- `c93d0179c2` – system‑viz‑node‑dispatch: CLI‑only heap respawn to avoid empty output/GC thrash.  
- `baaf3c7859` – extracted shared heap‑respawn logic into `scripts/lib/viz-query-heap-reexec.mjs`; added NODE_OPTIONS‑aware planner and `respawnWithHeap`.  
- `67baa0e72b` – measured right‑size of respawn from 16 GB → 8 GB (halves Windows commit reservation, still covers all graph commands).  
- `c3d1f5a4e7` – implemented script‑aware `portable-node` split: `/scripts/` CLIs get 2 GB heap, hooks stay at 384 MB; live‑validated with unit tests and smoke.  

**DECISIONS**  
- Adopt a build‑once, self‑respawn pattern for all graph‑loading CLIs to avoid OOMs while preserving deterministic behavior.  
- Keep the deliberate 384 MB hook heap cap (Windows commit reservation) intact; do not raise it globally because it would re‑introduce MCP spawn failures.  
- Do **not** force advisory Ollama offloads to auto‑invoke; the system already auto‑invokes deterministic ops and mutes lossy suggestions, satisfying the “100 % guaranteed net benefit” criterion.  

**OPERATOR DIRECTIVES** (verbatim)  
- “do it all” – execute all queued threads, including global NODE_OPTIONS raise.  
- “no hard caps on utilization” – lift verified‑safe Blackwell utilization caps.  
- “if it can do the job perfectly 100 % it needs to be auto invoked not suggested or advisory.”  

**FINDINGS/BUGS**  
- `--help` and typos in system‑viz‑query caused full graph load → OOM (defect A).  
- Main graph path lacked heap guard, causing OOM on heavy commands (defect B).  
- Windows `--max-old-space-size` is a commit reservation; the fleet’s 384 MB cap protects against commit‑storm.  
- Global NODE_OPTIONS raise would break hook swarm (MCP‑FLEET‑CAPACITY‑MS0).  
- Ollama advisory suggestions are lossy and have 0 % conversion; auto‑invoking them would harm model content quality.  

**DOMAIN SPECIFICS**  
- Engines: `system-viz-query`, `system-viz-node-dispatch`, `hub-blast-radius-rank`, `build-system-viz-livediff`.  
- Actions/dispatchers: `planHeapRespawn`, `respawnWithHeap`, `isKnownGraphCmd`, `maxOldSpaceMb`.  
- Metrics: OOM counts, GC thrash logs, token savings (282 k tokens).  
- Paths unique to this galaxy: `H:/prism/.claude/bin/portable-node`, `scripts/lib/viz-query-heap-reexec.mjs`, chat‑slot helpers (`chat-slots.mjs`).  

**TOOLS USED**  
- PRISM CLI: `/checkin-sierra`, `/loop`.  
- Helpers: `chat-slots.mjs`, `planHeapRespawn` module, `respawnWithHeap`.  
- Hooks: `slot-bind-enforce.mjs`, `stop-force-loop-continue`, `crossroad-auto-decide`.  
- Testing: Node test runner (`node --test`), unit tests for heap logic.  

**OPEN THREADS**  
- Ollama “auto‑invoke not advisory” thread queued (deterministic bridge already auto‑invokes; lossy suggestions remain advisory).  
- No outstanding commits or regressions; loop is at a natural boundary ready to resume with next iteration.
