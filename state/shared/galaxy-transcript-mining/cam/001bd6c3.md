# cam session 001bd6c3 (2026-06-09, 45.9MB, spine 430KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Patched `hermes/config.yaml` to use Claude Opus 4.8 primary, Ollama (`qwen2.5‑coder:32b`) fallback; updated memory files for fleet coordination proof.  
- Launched Hermes desktop app; `slot_brief_write` test succeeded immediately.  
- Qdrant container `prism-qdrant` (v1.17.0) up with 3 live collections (`:6333`).  
- Migrated Hermes config v0→v28, corrected provider strings to `anthropic/claude‑opus‑4‑8` primary and `custom/qwen2.5-coder:32b` fallback; GUI launcher cleared dirty `package-lock.json`.  
- Commit `28b72e4dee`: added `scripts/fill-galaxy-memory-sections.mjs`, 13 tests, updated 34 `engines/<gal>/MEMORY.md` to full 4‑section structure; updated `syncGalaxyMemories()` call.  
- Commits: `822c48c55` (VRAM math & routing tiers), `3499f5f20f` U5b tool‑calling agent, `d807d46f44` U5c PreToolUse hook, `a59f35221e` U6 coding‑offload script, `cfda0e391a` U7 Opus tail‑review gate, `e17be56e82` U10 capability probe.

**DECISIONS**  
- Adopt Hermes primary on Claude Code, fallback to Ollama; use MCP daemon `:3100` for fleet coordination.  
- Revert supervisor cwd patch; fix via PRISM_ROOT‑relative paths.  
- Keep Qdrant down until operator enables it; resolve 400 error by enabling “extra usage” billing or reconfiguring app.  
- Adopt `gpt‑oss:20b` (128 K context) as fleet primary local model, keep Claude fallback for extra‑usage billing.  
- Run zulu headless via PowerShell (`hermes -z …`); GUI no longer required.  
- Use Claude Code CLI + local Ollama stack for zulu orchestration; Hermes app unnecessary.  
- Adopt idempotent additive `fill-galaxy-memory-sections.mjs`; exclude auto‑generated `node_*` files from memory totals.  
- Treat gpt‑oss reasoning channel bug as Hermes‑adapter limitation; switch to clean‑content models (`qwen3‑vl`) for headless runs.  
- Flag corrupt `ai-training_synthesis.md` for owner review, not silent patching.  
- Use `gpt‑oss:120b` & `20b` for native tool‑calling; `qwen2.5‑coder` only for code generation.  
- Offload Read/Read‑Slice, Search, Coding to Ollama (highest token ROI).  
- Keep auto‑routing via existing `OllamaTaskOffloaderEngine.ts`; remove duplicate router (U8).  
- Commit exclusively to slot/bravo; merge after golf’s git‑tree repair.  
- Set `NUM_PARALLEL=2`, `MAX_LOADED_MODELS=3`, `OLLAMA_KEEP_ALIVE=30m` for VRAM safety.  
- Enforce auto‑route flag (`ollama-route-config.json` mode:auto).

**OPERATOR DIRECTIVES**  
1. Restart Hermes after any config change; verify `gpt‑oss:20b` primary, fallback to Claude if billing disabled.  
2. Restart MCP daemon `:3100` (or reboot host) to re‑establish Qdrant connection.  
3. Bring Qdrant up (`docker compose up -d prism-qdrant`).  
4. Set env vars: `OLLAMA_KEEP_ALIVE=60s`, `NUM_PARALLEL=4`, `MAX_LOADED_MODELS=3`.  
5. Deploy Docker overrides exposing GPU; run `build.sh` with `llm-run --task pre-build-checks --config llm-routing.yaml`.  
6. Run ultracode workflow (`wbmnnd902`) to generate “LLM‑Enabled PRISM Build Blueprint” and commit to `llm-integration-prism` branch.  
7. Verify Hermes messages processed; resolve HTTP 400 by enabling “extra usage” billing or adjusting app.  
8. Monitor GPU utilisation via `nvidia-smi`; target 30–40 % during peak builds.  
9. Clean zombie `tsserver` processes if memory pressure rises.  
10. Audit each galaxy sequentially, ensuring MEMORY.md, PATHS.md, TOOLBELT.md fully populated; use Obsidian REST (`:27123`) and Ollama/Docker stack for fills.

**FINDINGS/BUGS**  
- N‑drive bug resolved after MCP restart; stale daemon removed.  
- `semantic_search` fails due to Qdrant down; `self_awareness_search` & `master_index_query` work.  
- Hermes init HTTP 400 from Claude without extra‑usage credit; 429 indicates shared pool rate limiting.  
- CapabilityIndexEngine vs cwd conflict; supervisor patch would regress other engines.  
- Unknown provider `'openai'` fixed by using `custom` for Ollama.  
- gpt‑oss returns separate `reasoning` field → Hermes custom provider mis‑parses, causing “no final response”.  
- Auto‑generated `node_*` graph dumps inflated memory counts; excluded from totals.  
- Corrupt `ai-training_synthesis.md` flagged (all‑NUL).  
- VRAM math corrected: qwen32b ≈37 GB resident; 120B+32B cannot co‑reside.  
- gpt‑oss:120b stranded by stale router tiers; unstranding required (U3).  
- Tool‑calling works only on gpt‑oss family; qwen2.5‑coder emits text calls, unusable for tool loops.  
- Entry guard bug in U10 probe relative path no‑op; nomic tag‑match bug (`:latest`).  
- Duplicate router found (U8) – removed.

**DOMAIN SPECIFICS**  
- MCP dispatchers: `prism_memory`, `prism_session`, `prism_knowledge`, `slot_brief_write`, `chat_post`, `slot_brief_list`.  
- Hermes config paths: `~/.claude/.credentials.json`, `C:/Users/.../hermes/config.yaml`.  
- Qdrant vector store (`:6333`) for semantic search.  
- Obsidian REST API (`:27123`); vault sync lock at `knowledge/memories/**`.  
- Engine clusters: `CapabilityIndexEngine` (src/tools/dispatchers), `OutcomeCaptureBusEngine`, `DocuStrataMaterialPriorEngine`, `JMCustomerVendorDatabaseEngine`.  
- PRISM stack: MCP server (`:3100`), Qdrant (`:6333`), Hermes agent (local Ollama via custom provider, Claude fallback).  
- 34 galaxies: each has `MEMORY.md`, `PATHS.md`, `TOOLBELT.md` under `engines/<gal>/`.  
- MEMORY.md must contain four canonical sections plus connection axes (`UP‑pull`, `DOWN‑push`, `MASTER‑INDEX`).  
- Zulu orchestrator sends `/checkin‑<slot>` directives via SendKeys; slots powered by Claude Code CLI.  
- Local stack: Ollama (qwen2.5-coder, qwen3-vl), MCP :3100, Qdrant :6333, Obsidian REST :27123.

**TOOLS USED**  
- PRISM MCP server (Node.js).  
- Hermes desktop app & launcher (`hermes‑setup.exe`).  
- Qdrant Docker container.  
- Obsidian REST API plugin.  
- Node scripts: `chat-slots.mjs`, `wire-hermes-local-backend.mjs`, `mcp-server-supervisor.mjs`.  
- Docker Desktop + docker-compose for Qdrant.  
- Hermes CLI (`hermes config migrate`, `hermes model`, `hermes doctor`).  
- Custom blind‑mapper script: `node scripts/hermes-obsidian-app-map.mjs`.  
- Local Ollama models (gpt‑oss, qwen).  
- PowerShell for headless zulu execution.  
- Scripts: `scripts/fill-galaxy-memory-sections.mjs`, `galaxy-synthesis-refresh.mjs`, `syncGalaxyMemories()`, `zulu-cli.ps1`.  
- Docker/Ollama CLI, Git, Node.js (`execFileSync`, `git grep`).  
- Ultracode workflows (`wbv7itkph`, `wbmnnd902`).

**OPEN THREADS**  
- Start Qdrant Docker container and configure vector store.  
- Resolve Hermes 400 error by enabling “extra usage” billing or adjusting app.  
- Implement PRISM_ROOT-relative fix for CapabilityIndexEngine vs cwd conflict.  
- Verify Hermes fallback path works under rate limiting/billing errors.  
- Restart `:3100` daemon after Qdrant starts to clear stale connection state.  
- Confirm Hermes uses `gpt‑oss:20b` primary; adjust `max_tokens` if needed.  
- Verify fallback provider works when extra‑usage billing disabled.  
- Automate headless zulu checks via scheduled PowerShell script.  
- Re‑enable disabled Hermes‑Obsidian Bridge scheduled task (requires elevated shell).  
- Regenerate corrupt `ai-training_synthesis.md` once owner ready.  
- Periodic re‑run of `syncGalaxyMemories()` to keep vault mirror fresh.  
- Monitor Hermes custom provider for future patch that handles gpt‑oss reasoning channel correctly.  
- U9 co‑residency module (env mutex, keep_alive, hard‑reason lock).  
- Go‑live merge slot/bravo→main + wiring `settings.json` for auto‑route enforcement.  
- RTK & existing systems upgrade sweep (#11) pending post‑merge.  
- Further optimization of routing tiers (U3/U4) once main tree synced.  
- `/compact` recommended before next unit to clear context.
