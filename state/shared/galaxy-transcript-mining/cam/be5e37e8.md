# cam session be5e37e8 (2026-05-19, 28MB, spine 135KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑FGE01` – `62b5794101`: STEP corpus → evidence‑based build sequence (`cad_class_build_sequence_evidence`).  
- `U‑BPA‑CONSUMER` – `6cbe5b1561`: offline consumer for `blueprint‑accuracy-events.jsonl`.  
- `U‑FGE02` – `c60f6c9396`: wired evidence ranking into Fusion360 orchestrator (`cad_class_drive_build`).  
- `U‑TDP01` – `5e3048ad71`: 4‑stage print‑to‑CAM training driver (extract → CAD → CAM → record).  
- `U‑TDP02` – `8d533c1e10`: batch harvester with idempotent registry.  
- `U‑TDP03` – `99288aec44`: extraction aggregator (Welford stats, per‑class templates).  
- `U‑TDP04` – `5709f19d7b`: OCR extraction benchmark runner vs CAD GT.  
- `U‑TDP05` – `1bc36d949d`: CAD‑derived ground truth extractor from STEP corpus.  
- `U‑TDP07` – `d85573b7d4`: deterministic embedded‑text PDF extractor (SolidWorks split‑token + inline‑±, OCR‑fragment guard).  
- `U‑TDP08` – `?`: extended regexes for paired‑tolerance bands (`1.002/1.000`) and Rc hardness ranges (`Rc 58‑60`, `55‑57 Rc`).  
- `1576134f55` (picker‑fix): milestone‑name filter to stop re‑picking units from completed envelopes (HOOK‑SYNERGY‑MS0).  

**DECISIONS**  
- Adopt two‑tier extraction: deterministic embedded‑text (~3–13 % of corpus) + vision OCR for remaining 87 %.  
- Use `qwen2.5vl:7b` (≈5 GB) on RTX 3080; defer `llama3.2‑vision:11b` due to OOM.  
- Wire evidence ranking into Fusion360 path (`cad_class_drive_build`) to close “geometry not auto‑wired” gap.  
- Implement offline consumer for `blueprint‑accuracy-events.jsonl`.  
- Harden PDF parser with inline‑± regex, OCR‑fragment guard, split‑token drop‑dot refusal; fix all P0/P1 defects.  

**OPERATOR DIRECTIVES**  
- `/checkin-mike /goal train cad and cam ai systems so they can accurately read prints, generate cad files relative to prints then generate cnc programs with cam. start with fusion360. /loop [10m] complete goal`  
- `try it, will my 4080 on my home pc help and if we only ran 1 chat?`  
- `continue training the ocr print reading capabilities of the prism system`  
- `are you working on training the ocrs and print readers first to develop templates for common parts in our system and then generate it for more complex parts. we first need to prove we can extract the correct data from prints before anything else`  

**FINDINGS/BUGS**  
- `moondream:1.8b` hallucinated dimensions; `llama3.2‑vision:11b` OOM on 10 GB VRAM.  
- Parser previously fabricated split‑token tolerances (`- 10`) and OCR fragments; fixed to refuse.  
- Only ~3–13 % of JM Die PDFs contain embedded text; deterministic path yields 13.5 % extraction rate on a 600‑PDF sample.  
- Vision model `qwen2.5vl:7b` fits GPU, queued for pull.  
- New regexes raise extraction to ~21 % on a 19‑PDF subdir (paired bands + Rc hardness).  

**DOMAIN SPECIFICS**  
- Engines/dispatchers: `CADClassFeatureLibraryEngine`, `BlueprintExtractionRAGEngine`, `GroundTruthRegistryEngine`.  
- Actions: `cad_class_build_sequence_evidence`, `cad_class_drive_build`, `blueprint‑accuracy-consumer-lib.mjs` event consumer.  
- Metrics: precision, recall, F1, percentile R‑7; rolling window stats via Welford; per‑PDF extraction confidence; dimensions.count.  
- Paths: `H:/prism-slot-mike/scripts/lib/pdf-text-extract-lib.mjs`, `scripts/harvest-pdf-text-batch.py/.mjs`.  

**TOOLS USED**  
- PRISM tooling: `chat-slots.mjs`, `checkin.md` pipeline, per‑file scrutiny agents.  
- Scripts: `pdf-text-extract-lib.mjs` (deterministic extractor), `training-driver-print-to-cam.mjs`, `blueprint-accuracy-consumer-lib.mjs`.  
- Vision inference: Ollama (`qwen2.5vl:7b`, `moondream:1.8b`).  
- PDF rendering: PyMuPDF (`fitz`).  

**OPEN THREADS**  
1. Wire deterministic extractor into U‑TDP04 grader (`makeLiveExtractor` → `pdf-text-extract-lib`).  
2. Pull, test, and integrate `qwen2.5vl:7b` as vision OCR adapter (U‑TDP06).  
3. Measure full 600‑PDF corpus delta after both paths integrated; target ≥70 % extraction coverage before LoRA/RAG training.  
4. Expand regex set to handle comma‑decimal, MAX/MIN qualifiers, slash‑paired tolerances (future U‑TDP09).  
5. Finalize CAM program generation in Fusion360 workflow and integrate into U‑TDP01 pipeline.
