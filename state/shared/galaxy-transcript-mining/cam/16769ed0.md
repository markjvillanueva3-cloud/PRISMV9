# cam session 16769ed0 (2026-06-21, 28.4MB, spine 282KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U‑REWRITER‑SKIP‑LOOP‑DIRECTIVES – loop‑directive skip before Ollama round‑trip; 9/9 tests.  
- U‑REWRITER‑CHATMODEL‑SELECT – helper `ollama-loaded-chat-model.mjs`; 12 ref‑value tests.  
- U‑REWRITER‑FALSE‑WARM‑COMMENT‑FIX – corrected 24 h keep_alive comment.  
- 1c6abe2878 – U‑LOADED‑CHAT‑STRICT‑OPTION; added `opts.strict` to `pickLoadedChatModel`.  
- 70b94eb1c9 – U‑OLLAMA‑PS‑PROBE‑DEDUP; consolidated `/api/ps` / `/api/tags` probes.  
- e0b3df1ea0 – U‑MCP‑ROUTE‑SUPPRESS‑ISVERBOSEBASH; reversible suppression filter.  
- 5be19a26c2 – audit‑mcp‑route‑takerate legend correction (R12).  
- 0aab43dadc – audit‑viz‑legend docstring update (P2 closure).  
- 8d344941fe – audit‑viz‑first injection dedup added.  
- d3e0b7ebaf – U‑PRUNE‑TAG‑SHARED‑CACHE; per‑tag prune of shared injection‑dedup cache.  
- 4307ece067 – PSN CHECKLIST TEST ENABLED; pinned `opts.enabled` for hermetic tests.

**DECISIONS**  
- Rewriter arc closed; focus on token‑efficiency units.  
- Adopt R12 fail‑loud + R13/R15/R16 scrutiny; all units must pass 3‑of‑3 ledger gate.  
- Ship only safe code changes; defer policy‑level VRAM warm‑policy to separate sign‑off.  
- Ask‑ollama wiring left uncommitted pending zulu edits; loaded‑first logic remains comment‑at‑slot:alpha.  
- Dedup `/api/ps` probes → shared lib for maintainability.  
- Suppress “isVerboseBash” nudge (reversible); meets doctrine `feedback_low_take_rate_nudges_are_net_negative`.  
- Route‑suggest subsystem fully coherent; no further changes needed.  
- Feature‑routing “both” arc already shipped.  
- Do not start prune‑TTL fix at YELLOW token zone; await fresh budget.  
- Replace generic `pruneExpired` with per‑tag `pruneTag`; keep backward compatibility.

**OPERATOR DIRECTIVES**  
- Continue autonomous builds; finish any in‑flight unit before idling.  
- Never idle while token budget green; checkpoint at YELLOW, let auto‑compact reset.  
- Prioritize own‑domain leftovers → FIXES → WIRINGS → GHOST builds → backlog.  
- Use Sonnet agents for parallel scrutiny; commit with `[MAIN-FORCE]` on `cad-fusion-live-ms0`.  
- Alpha: finish feature‑routing arc (done).  
- Golf/Sierra/Zulu: snapshot PC health, idle only when healthy and no safe high‑ROI work remains.

**FINDINGS/BUGS**  
- `pickModel` never found loaded chat model → “no-model” returned.  
- Vision models (`qwen2.5vl`, `llama3.2‑vision`) mis‑selected for `/api/chat`.  
- False 24 h keep_alive comment; actual longest is 30 m pipeline‑triggered.  
- Self‑compact race: queued `/compact` did not fire before next prompt at YELLOW (~0.66), risking RED gate hit.  
- Ask‑ollama loaded‑first wiring conflict with zulu’s live edits (conflict‑fork).  
- Duplicate `/api/ps` / `/api/tags` probes resolved by shared lib.  
- Net‑negative “isVerboseBash” nudge suppressed; first concrete application of doctrine.  
- Shared injection‑dedup cache bug: `pruneExpired` dropped tags older than caller TTL → token leaks; fixed by `pruneTag`.  
- Env‑dependent failures in `psn-prompt-checklist`; resolved by pinning `opts.enabled`.  
- Dark ledger for `read-auto-limit` frozen since 2026‑05‑26; re‑lit via subprocess hook.  
- Large‑read‑digest advisory muted correctly; probe counter 3314 fires, no tokens saved.

**DOMAIN SPECIFICS**  
- Engines/dispatchers:  
  - `prompt-rewriter-ollama.mjs` (UserPromptSubmit), `ask-ollama.mjs`, `ollama-loaded-chat-model.mjs`.  
  - `pickLoadedChatModel`, `mcp-route-suggest` chain (`_recordRouteFires`, `appendActionHints`, `route‑suggest‑decay`).  
  - `injection-dedup.mjs` with `hashBlock`, `shouldEmit`, `recordEmit`, `pruneTag`.  
  - `audit-mcp-route-takerate.mjs`, `psn-prompt-checklist`, `read-bundle.mjs`.  
- Paths:  
  - `H:/prism/.claude/hooks/prompt-rewriter-ollama.mjs`  
  - `H:/prism/.claude/scripts/lib/ollama-loaded-chat-model.mjs`  
  - `node scripts/fleet-survival-status.mjs`, `self-compact.mjs`, `audit-unwired-engines.mjs`, `vault-health.mjs`.  
  - `scripts/lib/ollama-ps-probe.mjs`, `injection-dedup.mjs`.  
- Metrics: token budget zones GREEN/YELLOW/RED; PC‑health thresholds (RAM > 88 %, GPU > 93 GB, node > 250, bash > 120); token savings ledger entries; hit/nudge counts.

**TOOLS USED**  
- PRISM dispatchers/scripts: `ask-ollama`, `prompt-rewriter-ollama.mjs`, `ollama-loaded-chat-model.mjs`, `scripts/lib/ollama-ps-probe.mjs`.  
- AI agents: Claude, Sonnet, Octopus, Hermes; Ollama coder‑ensemble (qwen3‑coder 30 b, qwen2.5‑coder).  
- Test harnesses: `node <file>.test.mjs`, 3‑of‑3 scrutiny agents (reviewer + code‑analyzer).  
- Build tooling: `cad-fusion-live-ms0` repo, `[MAIN-FORCE]` commits.  
- Node scripts: `fleet-survival-status.mjs`, `self-compact.mjs`, `audit-unwired-engines.mjs`, `vault-health.mjs`, `system-viz-query.mjs`.

**OPEN THREADS**  
- Ask‑ollama loaded‑first wiring – pending zulu’s commit; must avoid attribution loss.  
- Credit‑path verification for remaining low‑take nudges (`isLargeRead`, `doctrineSurface`, `backendAuditChain`).  
- Self‑compact reliability – monitor future gaps, ensure alpha slot reclamation.  
- Prune‑TTL fix – migrate `pruneTag` to all 10 evictors; queued for next fresh‑budget build.  
- Remaining backlog: vault‑ops hardening, system‑viz graph/ghost‑roost/master‑index/node‑cards audit.  
- Any pending FIXES for failing tests or TypeScript errors not yet addressed.
