# quoting session 72260bce (2026-06-19, 13.3MB, spine 104KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `8cfd4da130`: OCR grinder switched to continuous (`--until-complete`, unlimited ExecutionTimeLimit, 30‑min backstop).  
- `73582a78c0` / `scripts/blueprint-ocr-training-loop.mjs`: added `--retry-failed`; fast‑exit for drained corpus; multi‑page rasterization support.  
- `d820c15936`: Multi‑page OCR fix – rasterize all pages, union dims; recall ↑ from 0 to ~0.32 on scored parts.  
- `scripts/lib/ocr-training-loop-lib.mjs`: new `RETRYABLE_FAILURE_STATUSES`, `isCorpusDrained()`, refactored `parseCursorDoneSet()` (last‑status‑wins).  
- Tests: 26 unit tests for training loop lib + 18 GT classifier tests; all pass.  
- New GT reliability library (`programGtReliability`) with `contourFraction`, `gtReliable`, `gtClass`; integrated into `extractProgramGT`.  
- Updated `validate-perfect-parts.mjs`: consumes GT reliability, skips contour‑unreliable parts, records transparency fields.  
- `9c7943f019`: Blueprint + GD&T tribal‑knowledge injection plan & drop‑zone.  
- `27659a5c8f`: R16 gap‑closure – fast‑exit guard excludes `--real-png/--real-dir`.  
- `bfcd8256fe`: Fixed probe dim‑key bug; added tests.

**DECISIONS**  
- Switch OCR grinder from nightly to continuous until corpus drained.  
- Adopt `--until-complete` flag and enable `--retry-failed` in training loop.  
- Use full‑page extraction (not page‑0 only); confirm live path already multi‑page.  
- Classify CNC programs as stepped (GT‑reliable) or contour (GT‑unreliable); aggregate recall over stepped parts only.  
- Skip contour‑unreliable parts in validation to avoid metric artifacts.  
- Plan tribal knowledge injection for blueprint reading + GD&T.

**OPERATOR DIRECTIVES**  
- “change from nightly to do it all until it's complete.”  
- “plan tribal‑knowledge injection for blueprint reading + GD&T.”  
- “build it all.” (autonomous shipping of safe improvements)  
- “Complete all xray tasks, then improve blueprint-reading & OCR capabilities.”

**FINDINGS/BUGS**  
- Page‑0‑only OCR caused recall = 0; fixed by rasterizing all pages.  
- Multi‑page fix raised recall from ~0 to 0.32 on scored parts (previously 0.21).  
- qwen3‑vl:32b produces empty output, 5–8× slower – not usable.  
- Scan preprocessing ineffective on clean drawings; not root cause.  
- Ensemble corroboration filter does not collapse dims; fusion returns singletons but validate uses full set.  
- Transient grind failures (183 prints) due to contention; recoverable via `--retry-failed`.  
- GT over‑count for contoured/tapered parts caused metric artifact; recall inflated by contour diameters.  
- Test failure fixed by deterministic test on LATHE_FEED.

**DOMAIN SPECIFICS**  
- Engines/actions: `blueprint-ocr-training-loop`, `run-ollama-vision-extract.mjs`, `cad_pdf_blueprint_extract`, `blueprint_to_quote`, `print_to_program_full`.  
- Dispatchers: OCR closed‑loop training dispatcher, vision‑ensemble‑fuse, PDF rasterizer.  
- Metrics: `dimension-set-score.mjs` (recall/precision), GT reliability (`contourFraction`, `gtReliable`).  
- Paths: PDF → PNG via `pdf-to-png.py`; page classification via `page-classify.mjs`; OCR extraction via Ollama VLMs.

**TOOLS USED**  
- PRISM `/checkin-xray` wrapper, `chat-slots.mjs`, node helpers for slot claim.  
- Vision‑model‑select.mjs, probe‑vision‑model.mjs, pdf‑corpus‑watcher‑sweep.mjs, pdf‑to‑png.py (`--preprocess/--deskew`).  
- ocr‑al‑queue‑surface.mjs, scoreDimensionSet.mjs, CrossSourceDimensionReconciliationEngine.ts, validate‑perfect‑parts.mjs, ocr‑closed‑loop.mjs.  
- Git commit hooks `[MAIN-FORCE]`, handoff scripts.  
- PRISM xray / blueprint‑vision/OCR slot.  
- OCR training loop (`blueprint-ocr-training-loop.mjs`).  
- Vision ensemble (`vision-ensemble-fuse.mjs`).  
- Ollama (qwen3-vl:8b-instruct, qwen2.5vl:7b).  
- PDF utilities (`pdf-to-png.py`, `page-classify.mjs`).

**OPEN THREADS**  
- Trace live print‑reading path for page‑0 bug; confirm same issue in production consumers.  
- Evaluate calibration tuning to allow singletons after `--retry-failed`.  
- Final integration of GT reliability into overall recall/precision reporting.  
- Expand contour‑unreliable detection to other part types (non‑lathe, non‑NC).  
- Verify live delta consumer paths continue to use multi‑page extraction post‑deployment.  
- Gather additional GD&T training data via pdf‑learn/video‑learn for future OCR improvements.
