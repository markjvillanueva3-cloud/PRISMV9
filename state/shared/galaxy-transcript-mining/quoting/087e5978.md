# quoting session 087e5978 (2026-06-14, 26.4MB, spine 189KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Fixed Windows‑ESM entry‑guard in `nn-eval-refresh.mjs` → writes `latest-candidate.json`.  
- Created codemod `scripts/fix-windows-esm-entry-guards.mjs`; applied to 18 dead CLI scripts, adding fallback `|| process.argv[1].endsWith(...)`.  
- Identified 23 files lacking safe ESM guard; 3 `.claude/hooks/` still dead.  
- Trimmed duplicated RTK reference block in `H:/prism/CLAUDE.md` (≈103 k → ≈98 k chars).  
- Drafted regression‑section slimmer script for “Recent regressions” collapse.  

- Commit `e403233551`: ultracode+ollama assessment of 34 galaxies; fixed slot‑galaxy‑map bugs (`Hermes slots → hermes‑zulu`, `papa → backend‑helper`).  
- Commit `da3ead84e0`: `galaxy-claudemd-inject.mjs` loader injects each slot’s CLAUDE.md.  
- Commit `94ae4ded51`: `claude-md-golf-only-guard.mjs` blocks non‑golf edits, allows inbox‑append, adds redirect message.  
- Commit `54764ade24`: guarded apply tool for fine‑tuning drafts (safety gate §0).  
- Commits `a40105a746` & `28c6b04bb6`: Wave 1/2 – all 34 CLAUDE.md files rewritten to locked 14‑section template (~80–160 lines).  

- Cad‑fusion‑live‑ms0 commits:  
  - `087e73d166`: owning‑window‑pid resolution + fallback.  
  - `fede01d2b2`: wired Zulu’s UIA tab‑focus resolver (`focusWtTabBySlot`).  
  - `f97c2b299f`: fixed multi‑pane safety test (P1).  
- Peer commit preserved: `16050d67c6`.

**DECISIONS**  
- Prioritize dormant CLIs; defer hook safety review until batch validation.  
- Stage edits for later branch/commit; avoid immediate commit of codemod & RTK trim.  
- Trim only RTK block now; full “Recent regressions” slimming after backup.  
- Treat 3 `.claude/hooks/` files as potential blockers → per‑hook safety check required.  
- Lock 14‑section template (~80–160 lines) for all CLAUDE.md; use `scripts/lib/slot-galaxy-map.mjs` as source of truth.  
- Activate dormant guard with inbox‑allowance & redirect logic; enforce span‑inclusive checks.  
- Replace dead pid‑based SendKeys path in self‑compact with UIA tab focus (`focusWtTabBySlot`); fallback to operator instruction if no WT tab.  
- Correct false claim: Zulu auto‑compacts via UIA tab‑focus, not dead `entry.pid`.  
- Self‑compact cannot act on current session (WT tab named “PRISM alpha”); will fall back and handoff.  
- Rename WT tabs to `PRISM <slot>` for real compaction.  
- Plan integration of `/goal`/`/loop` slash commands with full stack router.

**OPERATOR DIRECTIVES**  
- Upgrade compactions: if user requests compact & new session, initiate automatically when possible.  
- Develop goal + loop slash commands to utilize all optimally: integrate AI systems (prism learning, reasoning, PSN), system‑viz, Obsidian vault, Hermes, Claude CLI, skills, scripts, hooks, CLAUDE.md per galaxy, souls.md, memories, wikis, tribal knowledge injections, container skills, mcp-server tools, token‑savings measures, precompaction/compaction, session handoff, auto‑startup, check‑in system, hook invocation mechanisms, git tree organization, LoRA, NN/GNN/CAG/RAG, engines, pipelines, algorithms, databases, resource folder, JM die shop fleet, documents, prism app features, galaxy/chat slots/domains, prism awareness/injection, context injection during 1 min sessions, auto‑compaction triggers, token‑offloading strategies, automatic model switching. All must work together in unison.

**FINDINGS / BUGS**  
- Windows ESM guard fails on Windows; 23 files lack fallback (18 simple scripts).  
- 3 `.claude/hooks/` dead due to same guard.  
- `H:/prism/CLAUDE.md` bloated (~103 k chars) from duplicated RTK tables & 30 k‑char regressions section.  
- `slot-galaxy-map.mjs`: Hermes slots → hermes‑zulu; papa → backend‑helper bugs fixed.  
- Auto‑manager for CLAUDE.md missing; duplicate tool created but unnecessary.  
- Enforcement guard initially allowed span‑escape edits; fixed with end‑inclusive check.  
- Self‑compact originally used stale `slot.pid`; WT tabs cannot be resolved via pid → silent no‑op.  
- Zulu’s compaction uses UIA tab focus (`focusWtTabBySlot`), not pid; documentation updated.  
- Test 17 multi‑pane safety test had P1 bug, fixed in `f97c2b299f`.  
- Self‑compact cannot act on current session due to WT tab naming.

**DOMAIN SPECIFICS**  
- **Hermes**: credential_pool.anthropic holds 5 OAuth accounts; active_provider switched from failing Grok key to anthropic.  
- **Claude Code CLI**: single‑account auth in `~/.claude/.credentials.json`; account‑switching workflow planned.  
- **NN/GNN tier‑5**: retrain metrics – AUROC 0.4286, macro‑F1 0.1053, Brier 0.2555 (below promotion gate); ref‑pool empty.  
- **Token optimization**: CLAUDE.md load cost ≈7 k tokens/turn due to duplication; expected 30 % savings after slimming.  
- **Engines/Tools**: ultracode + ollama sonnet agents (assessment), Zulu orchestrator (pressure‑triggered compaction), `sendKeysToWindow`, `focusWtTabBySlot` (UIA tab focus).  
- **Dispatchers/Hooks**: `slot-soul-inject.mjs`, `galaxy-claudemd-inject.mjs`, `precompact-auto-trigger.mjs`, `self-compact.mjs`.  
- **Metrics/Paths**: per‑slot dedup loader, `scripts/lib/slot-galaxy-map.mjs` (keystone mapping).  
- **Self‑compaction**: `/compact` skill → `self-compact.mjs`; uses Zulu’s UIA tab focus (`focusWtTabBySlot` → `matchWindowsByTitle`).  
- **WT‑tab naming**: convention `PRISM <slot>`.  
- **Goal/loop slash commands**: injection hooks – `loop-iteration-inject.mjs`, `goal-prereq-inject.mjs`; routing via `routeTask`, intent classification via `classifyTaskClass`, model selection via `resolveExecutor`.

**TOOLS USED**  
- Repository ops: `rtk git`, `rtk tsc`, `rtk npm run build`.  
- Codemod & eval: `scripts/fix-windows-esm-entry-guards.mjs`, `nn-eval-refresh.mjs`.  
- File discovery: `system-viz-query.mjs find "<name>"`.  
- Offloading: `ollama-file-digest.mjs`, `ollama-offload.mjs classify`.  
- Token audit: `scripts/measure-fleet-token-savings.mjs`.  
- PRISM workflows: assessment, draft+verify, apply tool (`54764ade24`).  
- Scripts/hooks: `self-compact.mjs`, `slot-galaxy-map.mjs`, `claude-md-golf-only-guard.mjs`, `galaxy-claudemd-inject.mjs`.  
- PRISM tools: `/self‑compact` skill, `focusWtTabBySlot`, `matchWindowsByTitle`.  
- Testing utilities: 3‑of‑3 arms, ledger.  
- Git commit workflow.  
- Routing scripts: `scripts/lib/task-substrate-router.mjs`, `scripts/lib/local-llm-task-router.mjs`, `.claude/hooks/lib/ollama-cost-router.mjs`.

**OPEN THREADS**  
1. Hook safety review – decide reactivation or redesign of 3 dead hooks.  
2. Full CLAUDE.md slimming – run regression‑section slimmer after backup; quantify token savings.  
3. Commit staged changes – branch, test, commit codemod, RTK trim, `nn-eval-refresh` fix.  
4. NN/GNN retraining – label data when ref‑pool grows to meet promotion gate.  
5. Finalize and commit `self-compact.mjs` with UIA tab focus; ensure fallback logic for non‑WT tabs.  
6. Update docs/memory on Zulu’s compaction mechanism (UIA tab focus).  
7. Optional leanness trim of fine‑tuned CLAUDE.md files (target 80–160 lines).  
8. Monitor self‑compaction triggers across sessions.  
9. Implement goal/loop slash command integration with stack router.  
10. Rename WT tabs to `PRISM <slot>` for real auto‑compaction.  
11. Manual `/compact` required for current session; verify synergy injection into goal/loop.
