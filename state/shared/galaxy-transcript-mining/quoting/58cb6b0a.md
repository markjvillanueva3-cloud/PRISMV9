# quoting session 58cb6b0a (2026-06-23, 17.6MB, spine 120KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5ab3c49002` – U‑XRAY‑CALIB‑ACCUMULATE (ring‑buffer calibration store; reliable:true at 50 samples)  
- `815567da84` – U‑XRAY‑PROGRAM‑GT‑CALIB (real GT program emission)  
- `cd108380ac` – U‑XRAY‑APP‑INTEGRATION‑PLAN (blueprint OCR/reading plan)  
- `198d811ef4` – U‑XRAY‑CALIB‑STATS CLI  
- `47a37b714d` – U‑APP‑REDACT‑LIB (TS redaction module)  
- `62c20067d1` – U‑APP‑REDACT‑WIRE (wired to prism_cad:blueprint_redact)  
- `b8ef51c9fe` – U‑XRAY‑READING‑KNOWLEDGE (tribal + Y14.5 knowledge channel, ≤8 items/1100 chars)  
- `fa6a037974` – U‑XRAY‑TRUNCATION‑KEYCUT (tier‑2 JSON salvage; recovers 0→28 dims)  
- `U-XRAY-NUM-PREDICT-TUNABLE` (env PRISM_OCR_NUM_PREDICT, probe flag --num-predict)  
- `U-XRAY-READING-GUIDANCE-VALIDATE-WIRE` (wired reading‑guidance into validate‑perfect‑parts)

**DECISIONS**  
- Persist calibration samples in ring‑buffer; reach reliable:true at 50 samples.  
- Emit program‑GT only on real‑machine GT; synthetic GT for training.  
- Wire redaction as opt‑in TS module; default off.  
- Build bounded knowledge channel (≤8 items/1100 chars) for GD&T/tribal guidance; keep byte‑identical when off.  
- Tier‑2 JSON salvage for mid‑key truncation; tier‑1 value‑salvage fallback, fail loudly if none survive.  
- Keep reading‑guidance default off; enable only on region‑route crop path after safe‑crop validation.  
- Production num_predict = 4096; expose tunable knob (PRISM_OCR_NUM_PREDICT) and probe flag --num-predict for experimentation.  
- Wire validate‑perfect‑parts to receive knowledge‑channel guidance for all consumers.

**OPERATOR DIRECTIVES**  
- Continue closed‑loop training; improve OCR/blueprint reading, integrate into PRISM app features.  
- Execute highest‑value GT validation (e.g., part 05850) and next steps.  
- Leverage Ollama offloading, Hermes agents, engineered loops, harnesses, crons.  
- Apply further improvements; auto‑redaction library deployment.

**FINDINGS/BUGS**  
- Dense JM prints lose all dims due to mid‑key truncation; tier‑2 salvage recovers 0→28 dims (fa6a037974).  
- num_predict = 8192 yields 56–86 dims vs 28 at 4096 on part 05850, no hallucination inflation.  
- Recall on part 05850 remains 3/7 regardless of num_predict, reading‑guidance, or --enhance; missing dims due to GT ceiling (not OCR).  
- Redaction lib leak fixed: exact‑key masking + free‑text scrub now prevent customer name exposure.  
- RAG engine dormant in live extraction path; GD&T corpus empty (operator‑gated).

**DOMAIN SPECIFICS**  
- Slot `xray` handles OCR training loop, region routing, GD&T recall scoring.  
- Engines: validate‑perfect‑parts.mjs, cnc-program-gt-lib.mjs, buildVisionPrompt.js, runEnsembleOverImage.js, probe‑vision‑model.js, ocrImageWithModelAsync, extractWithRegionRouting.  
- Dispatchers: cadDispatcher.ts (blueprint actions), prism_cad:blueprint_redact, validate‑perfect‑parts harness.  
- Metrics: calibration reliability (reliable:true), dim extraction count, parse_ok flag, recall, precision, latency.  
- Paths: `/checkin-xray` → slot claim → `/checkin`; store at `state/shared/ocr-training-loop/calibration-samples.jsonl`; env knob PRISM_OCR_NUM_PREDICT; flags --num-predict, --reading-guidance, --enhance.

**TOOLS USED**  
- PRISM CLI: `/checkin`, `/checkin-xray`.  
- Slot helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
- Scripts: `calibration-sample-store.mjs`, `blueprint-ocr-training-loop.mjs`, `cnc-program-gt-lib.mjs`, `buildVisionPrompt.js`, `runEnsembleOverImage.js`, `probe-vision-model.js`, `pdf-to-png.py` (preprocess/deskew).  
- Redaction module: `src/engines/blueprint-vision/blueprintRedaction.ts`.  
- Dispatcher wiring: `cadDispatcher.ts`.  
- Environment variable PRISM_OCR_NUM_PREDICT; CLI flags --num-predict, --reading-guidance, --enhance.  
- Validation tooling: `node --check`, unit tests (45/45), GT harness JSON reports.  
- Ultracode workflow orchestration.

**OPEN THREADS**  
- Per‑type calibration (>50 samples per type) for reliable:true.  
- Multi‑part region comparison; need corpus with >1 scoreable callout‑GT part.  
- GPU calibration seeding run after dense‑print fix (quiet fleet window).  
- Y14.5 corpus drop pending operator release.  
- RAG engine activation in live extraction path.  
- Academy course integration into knowledge channel.  
- num_predict bump to 8192 for truncation reduction; risk assessment needed.  
- Enable reading‑guidance on region‑route crop path after safe‑crop validation.  
- Closed‑loop training cron re‑run post dense‑print recovery.  
- Run GT validation on additional parts beyond 05850.  
- Decide on raising production default for num_predict after broader evidence.  
- Evaluate fixture quality improvements (P0.3) to lift recall where OCR not bottleneck.  
- Explore further knowledge‑channel enhancements (additional tribal data).
