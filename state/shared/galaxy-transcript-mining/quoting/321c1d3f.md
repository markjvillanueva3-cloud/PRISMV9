# quoting session 321c1d3f (2026-06-03, 37.4MB, spine 386KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑PT‑KNOWLEDGE‑ENRICH` – commit ef49daac34: --from‑knowledge leg, 68/68 tests, 2‑of‑2 scrutiny PASS.  
- `U‑PT‑HAAS‑SPEC & ENGINE` – commit 3dbc22e2f0: ground‑truth spec + HaasNGCMillMasterPostEngine, 27/27 tests, 2‑of‑2 scrutiny PASS; fixed inch‑scale P0 bug.  
- `U‑WINMAX‑LIVE‑PROBE‑FINDING` – commit 7ae1230a27: live WinMax Mill probe, verified whereami.  
- `U‑WINMAX‑BUTTON‑SIGNATURE` – commit a1c1efa31d: added distinctive‑button tiebreak to signatureOf; recorded DRAW_VERIFY screen with real 26‑button set.  
- `U‑WINMAX‑NAV‑PATH‑CONTRACT` – commit dd1fcdb48c: fixed double‑broken CLI contract, hardened mapCli.  
- `U‑PT‑HAAS‑CANNED‑CYCLES` – commit a1acfda90b: added G81/G83 canned cycles; removed M29 before G84; tap‑feed contract (feed = pitch × rpm).  
- **CIMCO units**  
  - `U‑CIMCO‑WIKI‑TRIBAL & REFLECT2` – updated wiki + tool index.  
  - `U‑CIMCO‑CONTROL‑MAP` – .mcfg reader, sim report parser (19/19 tests).  
  - `U‑CIMCO‑MACHINE‑INDEX` – 86 defs, 44 unresolved.  
  - `U‑CIMCO‑POST‑INDEX` – 25 .js posts + 44 .eRPost parsed; 5/5 tests.  
  - `U‑CIMCO‑TOOL‑INDEX` – 14 libs, 366 cutters.  
  - `U‑CIMCO‑PER‑GALAXY‑PLOTS` – dependency‑ordered build plan.  
  - `CimcoVerificationBridgeEngine.ts + dispatcher prism_cimco` – 22/22 tests, 3‑of‑3 scrutiny PASS.  
  - `scripts/lib/nc-normalize.mjs` – 18/18 tests.  
  - `scripts/cimco-jm-machine-map.mjs → jm-fleet-sim-map.json` – 9/9 tests.  
- `297a3c9194` – Post‑proof readiness ledger: per‑machine golden corpus + sim machine + proof method + content‑drift audit (246 drift groups).  
- `430f735fff` – Workflow P0 fix, Hurco .hnc coverage, fleet readiness report.  
- `d0e5df9e16` – `scripts/lib/nc-dialect-masks.mjs`: dialect masks from JM headers; 10/10 safety‑invariant tests.  
- `38da8cb412` – Drift audit refactor using roundTrip: reclassifies 247 semantic‑drift, 8 byte‑identical, 2 volatile‑header‑only.  
- `7a14bf0def` – Fixed dialect‑mask fail‑open (greedy patterns); masks now fail‑closed on unknown/malformed headers.

**DECISIONS**  
- Use echo slot for post‑processor work; force‑take to avoid conflicts.  
- Build knowledge‑enrich leg before other units (R13 order).  
- Create Haas spec first, then engine to satisfy safety‑critical build rules.  
- Add button‑signature tiebreak as additive change; keep existing signature shape intact.  
- Resolve nav path contract by standardizing CLI envelopes and hardening parser.  
- Enumerate U‑PT‑HAAS‑CANNED‑CYCLES before writing to avoid context drift (R6/R10).  
- Remove M29 command before G84 for Haas; add tap‑feed contract (feed = pitch × rpm) with JSDoc.  
- Implement inverted‑geometry guard (R12) and NaN guard in approach block.  
- Change cycle line to bare X/Y format with G99 default, matching JM golden NC format.  
- Reject speculative cycles U‑PT‑HAAS‑G86‑G89‑G76; no use per archive.  
- Adopt offline‑first CIMCO integration: use installed binaries & data; UIA fallback for Simulation Report.  
- Skip headless CLI for CIMCOSimulation.exe; use UIA read + JSON parsing.  
- Defer heavy CIMCO↔PRISM bridge engine to next tick; queued after current loop.  
- Accept rate‑limit on parallel agent dispatch; shift to main‑loop self‑review for critical paths.  
- Use lightweight dispatcher prism_cimco instead of camDispatcher.ts.  
- Port parseSimulationReport to TS, keep byte‑faithful logic (|| not ??).  
- Normalizer pure, no heavy deps.  
- Machine mapping uses orientation + axis‑count scoring; reject flat generic floor.  
- Adopt workflow + parallel agents for high‑leverage offline unit; defer extraction when agent session limit reached.  
- Do not apply P1 Roku‑Roku HC flip; keep as spec‑verification due to safety risk.  
- Use roundTrip classifier as single source of truth for volatile masking.

**OPERATOR DIRECTIVES**  
- `/checkin‑echo /goal …` – start goal loop.  
- `/loop /yolo-mode` – schedule recurring YOLO mode every 10 min (slice1) and 5 min (slice3/4); use CronCreate `*/5 * * * *` job ID `306122b6`.  
- `im launching mill hurco for you right now` – bring WinMax sim live to unblock condition 1.  
- Provide live‑app capture session for UIA driver (SPINE‑2) and CIMCO Simulation Report extraction.  
- Confirm headless automation + 2026 REST API tier available; supply MariaDB credentials for juliett/hotel integration.

**FINDINGS/BUGS**  
- Softkey‑tiebreak auto‑resolve needed; numeric softkeys polluting signature.  
- Inch‑mode 25.4× scale bug in Haas engine (P0).  
- Missing DRAW_VERIFY screen and button set caused ambiguous whereami.  
- Double‑broken CLI contract: path emitted bare array, consumer expected `{ok,…}`; whereami/navigate produced multi‑line JSON.  
- M29 bug causes silent tap hang on Haas; approach/cycle first‑hole coupling issue leading to incorrect XY drilling.  
- Missing NaN guard in approach block.  
- Byte‑drift from synthetic Hurco sample; corrected by referencing JM golden archive.  
- Fail‑open bug in parseSimulationReport (`?? → ||`) fixed; regression test added.  
- Mapping scorer initially collapsed all lathes/mills to generic template; corrected via orientation & axis count weighting.  
- One test failure due to incorrect assertion on tokenization – resolved.  
- P0: VMC‑03 misclassified as 5‑axis trunnion (tr\d regex).  
- P0 fail‑open in dialect masks – greedy `[^)]*` swallowed semantic G‑code.  
- Provenance bug: PRISM_UPGRADED header lists GENOS L200E-M but source path says Multus_B250II.  
- 246 content‑drift groups across fleet; many golden files differ beyond header churn.

**DOMAIN SPECIFICS**  
- WinMax automation stack: winmax-ui-map.mjs, winmax-course-run.mjs, UIA driver PrismWinMaxUI.exe.  
- PRISM post‑training harness with `--from‑knowledge`; contract prism_shop_practice:tribal_enrich.  
- Master‑post router camDispatcher.ts dispatches per‑controller engines (Hurco, Okuma‑mill/lathe, Mitsubishi).  
- Physics constants CANONICAL_KIENZLE, CANONICAL_TAYLOR.  
- Haas engine uses G81/G83 canned cycles; Hurco reference used for format.  
- CIMCO file formats: `.mcfg` JSON machine defs, `.js` posts, `.eRPost` binary controllers, `.tmlib` XML tool libs.  
- Simulation report parsing via parseSimulationReport; categories, pass/fail gate.  
- Machine mapping logic: orientation (Horizontal/Vertical), axis count, kinematic templates.  
- Post‑processor verification harness pending (next unit).  
- Post‑proof readiness ledger path `state/shared/cimco/post-proof-readiness.md`.  
- scripts/lib/nc-dialect-masks.mjs; scripts/lib/nc-normalize.mjs; scripts/cimco-jm-machine-map.mjs → jm-fleet-sim-map.json.

**TOOLS USED**  
- PRISM CLI: `/checkin‑echo`, `/loop`, `/yolo-mode`, `/ollama-*` skills.  
- RTK wrapper (`rtk git`, `rtk vitest run`, `rtk tsc`, `rtk npm run build`).  
- CronCreate/cronDelete for recurring loops.  
- System‑viz query: `node H:/prism/scripts/system-viz-query.mjs find "<name>"`.  
- Wiki & memory search: `/wiki-query`, `prism_memory:semantic_search`, `prism_knowledge:tribal_search`.  
- Master-index awareness: `prism_session:master_index_query`.  
- Scrutiny agents: `scrutiny‑3way.mjs` (3‑of‑3 gate), `duplicationGuardEngine.mustCheckBeforeCreating()`.  
- Dispatcher modules: `prism_calc`, `prism_safety`, `prism_cam`, `prism_cad`, `prism_turning`, `prism_ai`, `prism_dev`, `prism_cimco`.  
- Vitest test runner, esbuild, tsc, npm scripts.

**OPEN THREADS**  
1. CIMCO↔PRISM bridge engine (SPINE‑2 UIA driver & sim‑cycle‑time) – requires live CIMCO app capture session.  
2. Post‑proof harness – build and test full post‑processor verification pipeline to 100 % coverage.  
3. License‑tier confirmation – headless automation + REST API availability; MariaDB credentials for juliett/hotel integration.  
4. Unit resolution for vendor machine defs (44 unresolved) – need external source or manual mapping before sim use.  
5. Per‑galaxy plot workflow – leaner agent set; to run when rate‑limit clears.  
6. Live CIMCO app for simulation runs (GUI‑only).  
7. Mastercam CAM source re‑emit golden parity.  
8. Parallel extraction of dialect masks & scrutiny pending agent reset.
