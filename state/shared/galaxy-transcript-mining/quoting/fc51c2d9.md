# quoting session fc51c2d9 (2026-06-03, 12.8MB, spine 93KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit 4047a82236: GPU‑swap config → “blackwell” (prewarm qwen2.5-coder:32b, GPU_FREE_MIN_MB=24576, KEEP_ALIVE=60m).  
- Commit f737e23661: added 5 qwen3 catalog entries as floor‑tier declarations.  
- Clean reinstall of Ollama v0.30.3; removed old portable engine (H:/Tools/ollama/ollama.exe); set `OLLAMA_MODELS=H:/Tools/ollama/models` and GPU‑offload env vars.  
- Docker Desktop restored v29.4.3; NIM container nim‑llama32‑3b running on :8000.  
- Obsidian API key wired (`PRISM_OBSIDIAN_API_KEY`, `PRISM_OBSIDIAN_URL`).  
- Fleet‑reaper & memory monitors verified; orphaned WSL VM reclaimed → commit pressure 52 %.  
- Fresh dist built at 15:35 via esbuild (catalog + fleet commits); `build:fast` completed; full `npm run build` blocked by 13 tsc errors in `shopDispatcher.ts`/`sessionDispatcher.ts`.

**DECISIONS**  
- Keep GPU slot “golf” as normal work slot.  
- Use H:/Tools/ollama/models exclusively; delete redundant portable install.  
- Commit catalog floor tiers first; defer hook‑bridge default swap until qwen3 models resident to avoid R13 routing errors.  
- Rebuild dist only after `build:fast` succeeds (esbuild-only) to avoid stale bundle.  
- Hold MCP restart until authorized; now authorized to restart :3100.  
- Use targeted grep, register missing PRISM Zulu Orchestrator via PowerShell script.  
- Set Ollama env for Blackwell tuning (`MAX_LOADED_MODELS=1`, `NUM_PARALLEL=1`, `NUM_THREAD=1`, `KEEP_ALIVE=10m`).

**OPERATOR DIRECTIVES**  
- Removed duplicate Ollama instances; confirmed single server instance.  
- Synced Ollama setup before next task.  
- Executed pull‑independent builds for catalog floor tiers.  
- Workflow `wf_ba53bcc8-f49` running; will output brief when finished.  
- Posted notifications via chat‑bus after each milestone (GPU swap, ollama fix, catalog commit).  
- User authorized MCP restart; no collision with India’s consensus‑engine edits.

**FINDINGS/BUGS**  
- Duplicate Ollama installs caused CPU‑heavy inference; old v0.24.0 lacked sm_120 support → all inference ran on CPU.  
- Registry pull speed for qwen3 models ~100 KB/s; full 106 GB stack >1 h at current rate.  
- Orphaned `vmmemWSL` (28 GB) caused commit pressure 96 % → Docker daemon down, NIM fallback triggered; reclaimed VM reduced pressure to 52 %.  
- Recursive grep over 4165 chunk files stopped.  
- `npm run build` blocked by 13 tsc errors in `shopDispatcher.ts`/`sessionDispatcher.ts`.  
- PRISM Zulu Orchestrator missing/crash‑critical; PRISM Ollama Serve failing 0xFFFFFFFF.  
- 16 other tasks unregistered (Cost Alarm, Tribal crons, etc.).  
- MCP pid 63644 hung; :3100 not responding.

**DOMAIN SPECIFICS**  
- Engines/Actions: Ollama inference server (GPU offload), NVIDIA NIM container, Prism MCP server, Hermes agent, Octopus voice‑routing, GNN feature extractor, RAG/CAG pipelines, System‑Viz dashboards.  
- Dispatchers/Tools: `chat-slots.mjs`, `fleet-reaper-sweep.mjs`, `nim-hook-bridge.mjs`, `apply-host-ftuning.ps1`, `settings.json` env vars, Docker Compose (`docker-compose.gpu.yml`).  
- Metrics/Paths: `/api/tags`, `/api/ps` (size_vram), `fleet-reaper-host-presets.json`, `state/shared/golf-bypass.jsonl`.  
- Paths: dist built at `H:/prism/.claude`; PowerShell script at `H:/prism/.claude/helpers/install-zulu-orchestrator-task.ps1`.

**TOOLS USED**  
- PRISM tooling (chat‑slot helpers, fleet‑reaper sweep, nim hook bridge, host‑tuning script).  
- Docker Desktop & Compose.  
- Ollama CLI (`ollama pull`, `/api/version`).  
- Workflow engine ultracode.  
- Git for commits.  
- esbuild, npm (`run build`, `build:fast`), PowerShell install‑zulu‑orchestrator-task.ps1.

**OPEN THREADS**  
- Complete qwen3 stack download (embedding, coder); 24 % embedding layer downloaded.  
- After models land, swap `DEFAULT_MODEL` in `ollama-hook-bridge.mjs` to `qwen3-coder:30b-a3b`.  
- Deploy reranker helper and Octopus dual‑voice once models resident.  
- Re‑index embeddings after qwen3 embedding available (India’s GNN fix).  
- Final dist rebuild (`build:fast`) and MCP restart when all code changes verified.  
- Monitor memory pressure post‑WSL cleanup; watch for spikes.  
- Restart MCP :3100 and verify shared dist live.  
- Register missing tasks via PowerShell; monitor crash‑critical status.  
- Resolve tsc errors in `shopDispatcher.ts`/`sessionDispatcher.ts` to enable full build.  
- Tune Ollama concurrency for Blackwell embeddings.
