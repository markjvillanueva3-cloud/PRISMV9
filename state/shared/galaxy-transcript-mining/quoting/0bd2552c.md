# quoting session 0bd2552c (2026-05-18, 9.4MB, spine 32KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `scripts/ask-ollama.mjs` – local Ollama query service (viz search, summarize, explain, triage, ask).  
- `scripts/__tests__/ask-ollama.test.mjs` – 55‑case test suite with 2 real‑data E2E.  
- `.claude/commands/ask-local.md` – `/ask-local` skill (gitignored).  
- `knowledge/wiki/architecture/ollama-expand-ms0.md` – wiki entry.  
- `state/shared/specs/OLLAMA-PRISM-MCP-BRIDGE-DESIGN.md` – bridge spec.  

**DECISIONS**  
- Use local Ollama (`qwen2.5-coder:3b`) for all token‑saving modes; keep‑alive, 180 s timeout.  
- Replace single‑token `system-viz-query.mjs find` with multi‑keyword ranked search in a subprocess (no model).  
- Do not copy entire system‑viz graph to Ollama – too large for context window.  
- Implement callable offloader (`ask-ollama`) to force token offload; hooks only suggest.  
- Bridge Ollama to PRISM MCP via a read‑only harness (L2); full MCP bridge (L2b) remains queued.

**OPERATOR DIRECTIVES**  
- Generate high‑ROI skills for back‑end dev efficiency and token saving.  
- Drastically expand local Ollama usage; offload 10.2 % → target 30 %.  
- Enable system‑viz graph querying via local Ollama (with optional synthesis).  
- Hook Ollama to PRISM MCP server (Docker compose spec pending).  

**FINDINGS/BUGS**  
- Multi‑word viz search returned no hits; fixed with ranked scorer.  
- Ollama cold load 113 s on memory‑pressured host; switched to single 3 B model, keep‑alive.  
- OOM when loading fallback `system-graph.json` (370 MB); added size cap in `loadGraph`.  
- Offloader hooks suggested but never forced; implemented callable `ask-ollama`.  
- Missing wiki path (`knowledge/wiki/architecture/index.md`) – corrected to `knowledge/wiki/index.md`.  
- Unused imports and magic numbers flagged; cleaned.  

**DOMAIN SPECIFICS**  
- Engines/actions: `system-viz`, `wiki_lookup`, `read_excerpt`.  
- Dispatchers: PRISM MCP (port 3100), local Ollama API (`/api/generate`, `/api/chat`).  
- Metrics: offload rate 10.2 % (target 30 %), graph sizes 26.8 MB / 370 MB, token estimates via `estimateTokens`.  
- Paths: `H:/prism/scripts/ask-ollama.mjs`, `system-graph.json`, `architecture-graph.json`, `_embeddings.jsonl`.  

**TOOLS USED**  
- PRISM helpers: `/checkin-charlie`, `chat-slots.mjs`, `per-agent-handoff.mjs`, `stable-session-id.mjs`, `milestone-tracker.mjs`, `precompact-pending-guard.mjs`.  
- Node test framework (`node:test`).  
- Local Ollama API via `fetch`.  
- Git hooks for commit prefix `[MAIN]`.  

**OPEN THREADS**  
- Build L2b harness to connect Ollama to full MCP server (U‑OE‑BRIDGE‑L2B).  
- Docker compose spec for Ollama + MCP + Qdrant/Postgres.  
- Further token‑saving hook wiring (ensure all 536 suggested offloads are forced).
