# quoting session c1682147 (2026-05-18, 7.4MB, spine 37KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-NIM-ENV`: Engine now reads canonical `NIM_URL` and fixes `/v1/v1` URL doubling.  
- `U-NIM-DEPLOY`: Sets default model to `llama-3.2-3b`, raises timeout to 30 s, restores port mapping.  
- `U-NIM-DOCFIX`: Removes stale JSDoc on `timeoutMs`.  

**DECISIONS**  
- Golf slot is sole owner of fleet‑reaper; `/checkin-golf` auto‑invokes it.  
- Fleet‑reaper always‑on: 5‑min scheduled task + per‑session monitor, idempotent re‑arm via bare `/fleet-reaper`.  
- NIM KV‑cache limit set to `NIM_MAX_MODEL_LEN=16384` to fit 16 GB GPU.  
- Default model for PRISM engine changed from 8B to 3B to match deployed NIM.  

**OPERATOR DIRECTIVES**  
- Run `/fleet-reaper` only in golf chat; do not pass `--golf` flag.  
- Use `PRISM_FLEET_REAPER_DISABLE=1` to disable re‑aper globally (kill switch).  
- Disable legacy allowlist hook (`export PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1`) before writing in golf.  
- Close genuinely dead heartbeat‑frozen chats (e.g., mike, india) to free ~10–15 GB RAM.  

**FINDINGS/BUGS**  
- NIM image never pulled; Docker compose background job was reaped by fleet‑reaper.  
- Restart loop caused by `ValueError: max seq len 131072 > KV cache 66832`. Fixed with `NIM_MAX_MODEL_LEN=16384`.  
- Port mapping dropped during edit; container healthy inside but unreachable from host. Restored `127.0.0.1:8000`.  
- Engine bug: `/v1/v1` URL doubling and wrong default model (`llama-3.1-8b`). Fixed in commits.  
- Memory pressure peaked at 97 % due to heartbeat‑frozen chats; relieved to ~84 %.  

**DOMAIN SPECIFICS**  
- Engines/dispatchers: `NVIDILLMCAMEngine`, `prism_cam:nvidia_cam_*`.  
- Actions: `strategyRecommend`, `parameterExtract`, `operationClassify`.  
- Metrics: fleet‑reaper JSON (`slots["owned-by-crashed"]`, `softRelief.{priorityDemoted,...}`, `gpu.{freeMb,utilizationPct}`, `ollama.{reachable,loaded[]}`, `coordinator.{shouldPrewarm,...}`).  
- Paths: `/v1/health/ready`, `/v1/models`, `/v1` endpoint for inference.  

**TOOLS USED**  
- PRISM CLI: `/checkin-golf`, `/fleet-reaper`, `chat-slots.mjs`, `fleet-reaper-sweep.mjs`.  
- Windows tools: `install-fleet-reaper-task.ps1`, `schtasks /Query`.  
- Docker: `docker pull`, `docker compose up`, `docker port`, healthcheck.  
- Scripts: `normalizeNimBase()`, environment vars (`NIM_URL`, `NIM_MAX_MODEL_LEN`).  

**OPEN THREADS**  
- Deploy updated engine (MCP server restart) to pick up new code.  
- Decide on `.wslconfig` change from `autoMemoryReclaim=gradual` to `dropcache`.  
- Monitor and close any remaining heartbeat‑frozen chats for sustained memory stability.
