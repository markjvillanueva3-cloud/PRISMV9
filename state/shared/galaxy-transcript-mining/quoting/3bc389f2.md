# quoting session 3bc389f2 (2026-06-04, 0.9MB, spine 11KB, 1 slice(s), model gpt-oss:20b)

**DECISIONS**  
- Doctrine shift (2026‑05‑16): fleet‑reaper ownership moved from **alpha → golf**, unifying all fleet hygiene under one slot. Golf already hosts fleet‑memory‑monitor.  
- `golf-slot-reaper-guardian.mjs` SessionStart + UserPromptSubmit hook now makes the doctrine non‑skippable on every explicit `/checkin-golf`.  
- Prior `/checkin-alpha` fleet‑reaper section removed (SUPERSEDED).  
- Slot binding: reclaim stale slots then force‑take `golf` from any current owner; no `--golf` flag is passed to `chat-slots.claim`.  

**OPERATOR DIRECTIVES**  
- Skill claims golf as a **normal work slot**, NOT the legacy hygiene chat.  
- Do NOT pass the historic `--golf` flag to `chat-slots.claim`.  
- To bypass the legacy allowlist hook:  
  - *Option A* (session‑scoped): set `export PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1` (writes logged to `state/shared/golf-bypass.jsonl`).  
  - *Option B* (global disable): edit `C:/Users/wompu/.claude/settings.json`, remove the PreToolUse entry referencing `golf-slot-write-allowlist.mjs`.  

**FINDINGS/BUGS**  
- The legacy allowlist hook (`golf-slot-write-allowlist.mjs`) blocks any Write/Edit/MultiEdit outside the legacy hygiene allowlist unless bypassed.  
- Removing the `/checkin-alpha` fleet‑reaper section prevents double execution of the reaper on `/checkin-golf`.  

**DOMAIN SPECIFICS**  
- **Engines/Actions**: `chat-slots.mjs` reclaim & claim; `fleet-reaper-sweep.mjs`; hooks in `golf-slot-write-allowlist.mjs`, `golf-slot-reaper-guardian.mjs`.  
- **Metrics/Paths**: JSON output from fleet‑reaper sweep includes `slots["owned-by-crashed"]`, `leftover-bash-task`, `unowned`; `softRelief.{priorityDemoted,workingSetTrimmed,rssReclaimedBytes}`; `gpu.{freeMb,utilizationPct}`; `ollama.{reachable,loaded[]}`; `coordinator.{shouldPrewarm,prewarmFired,hintWritten,thresholdDelta,hintMode}`.  
- **Report**: surface these metrics as the `fleet-reaper:` line in the check‑in report.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs` (reclaim/claim), `golf-slot-write-allowlist.mjs` (PreToolUse), `golf-slot-reaper-guardian.mjs` (SessionStart + UserPromptSubmit).  
- Scripts: `fleet-reaper-sweep.mjs`.  
- Git: `git rev-parse --abbrev-ref HEAD`.  
- Environment variable: `PRISM_GOLF_WRITE_ALLOWLIST_BYPASS`.  

**OPEN THREADS**  
- Ensure writes are permitted after bypassing the legacy allowlist hook.  
- Capture and report reaped candidates, RAM relief, GPU status, Ollama reachability, and coordinator hints from the fleet‑reaper sweep.  
- Verify that stale slots are correctly reclaimed before each `/checkin-golf`.
