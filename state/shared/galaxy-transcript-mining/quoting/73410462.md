# quoting session 73410462 (2026-06-19, 6.2MB, spine 37KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `93da648386`: CAD‑drawing pipeline spec (`state/shared/specs/CAD-DRAWING-PIPELINE-COMPREHENSIVE-2026-06-19.md`)  
- `37e5d383f0`: U1 – `CADFeatureCompletenessLedgerEngine` (feature ledger, reconcile, advance)  
- `aa11b794db`: U2 – `CADSketchDimensionGateEngine` (sketch‑first gate)  
- `781a7131ba`, `b3ef9f69dc`: U3 – `CADTribalDrawInjectionEngine` + catalog fix (`src/data/cad-draw-tribal-tips.jsonl`)  
- `11aa5eea9b`: U4 – `CADStockAllowanceEngine` (secondary‑op stock, EDM spark‑gap)  
- `cfbce95394`: U5 – added `cad_drawing` task class to `AISystemRouterEngine` (Ollama first / Claude failsafe)

**DECISIONS**  
- Root cause of missing features: OCR/VLM prompt omission → enumerate all print features and reconcile downstream artifacts.  
- Build 7‑unit pipeline: ledger, sketch gate, tribal injection, stock allowance, route class, print‑regen validate (U6), stepped‑bore feature recognition (U7).  
- Use pure engines, wired through `prism_cad` dispatcher, tsc‑clean, round‑trip tested.

**OPERATOR DIRECTIVES**  
- “Brainstorm a comprehensive pipeline.”  
- “Use sketches as the first line of defense for checking your work to the print.”  
- “Use tribal knowledge, wikis and memories for drawing features.”  
- “Final validation: generate a new print of the model you drew and compare it dimension‑by‑dimension with the exact same layout as the original print.”  
- “Local llms draw via PRISM AI system (cheap/free); Claude is the failsafe.”

**FINDINGS/BUGS**  
- OCR/VLM prompt omission caused missed stepped‑bore far‑side diameter & lead‑in chamfer.  
- Ollama→CAD lane exists but STEP output blocked (no `cadquery` in portable Python).  
- Fusion bridge provides geometry only, no 2D constraints; print‑regen validation is entirely new.  
- Seeded tribal corpus was gitignored → moved to `src/data` catalog.

**DOMAIN SPECIFICS**  
- Engines: `CADFeatureCompletenessLedgerEngine`, `CADSketchDimensionGateEngine`, `CADTribalDrawInjectionEngine`, `CADStockAllowanceEngine`.  
- Actions: `cad_feature_ledger_*`, `cad_sketch_dim_gate`, `cad_tribal_draw_query`, `cad_apply_stock_allowance`.  
- Dispatchers: `prism_cad` dispatcher, `ai_route_task` via `intelligenceDispatcher`.  
- Paths: spec at `state/shared/specs/CAD-DRAWING-PIPELINE-COMPREHENSIVE-2026-06-19.md`; catalog at `src/data/cad-draw-tribal-tips.jsonl`.

**TOOLS USED**  
- PRISM tools: `/checkin-delta`, `/checkin` pipeline, chat‑slot helpers.  
- Dispatchers & hooks: lazy `getEngine` registry, `validateActionParams`, `normalizeParams`.  
- Testing: Vitest, Zod schemas.

**OPEN THREADS**  
- U6 PRINT‑REGEN‑VALIDATE (view projection → dim extraction → pairing).  
- U7 STEPPED‑BORE‑FEATURE recognition extension.  
- Awaiting independent scrutiny review once backend stability returns.
