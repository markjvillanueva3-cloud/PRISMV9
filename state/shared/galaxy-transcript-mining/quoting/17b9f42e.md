# quoting session 17b9f42e (2026-06-13, 8.8MB, spine 67KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `LAUNCH-HERMES-FLEET.bat` – desktop launcher that opens the Hermes fleet in WT.  
- `hermes-slot-tab-boot.ps1` – per‑tab boot script that runs `hermes -p <slot>`.  
- 21 SOUL files (`H:\Tools\prism-fleet\hermes-souls\<slot>.md`).  
- 21 Hermes profiles created under `%LOCALAPPDATA%\hermes\profiles\<slot>` (fully isolated, cloned config).  
- Cross‑substrate synergy edges file (56 566 typed edges) staged for the next `regen-viz`.

**DECISIONS**  
- Use absolute path to the Hermes executable (`C:\Users\wompu\AppData\Local\hermes\hermes-agent\venv\Scripts\hermes.exe`) because spawned WT tabs do not inherit PATH.  
- Pre‑create profiles with `hermes profile create <name> --clone` before launch to guarantee auth, config, and memory isolation.  
- Mirror the Claude fleet’s 4‑quadrant WT layout but replace `claude.exe` with Hermes; keep slot count at 21 (existing roster).  
- Avoid fan‑out background jobs in a single session to prevent R14 bash.exe runaway; run heavy tasks only via scheduled cron or manually when fleet is idle.  
- Use `hermes -p <slot>` for isolated instance per slot.

**OPERATOR DIRECTIVES**  
- *“how do i launch hermes in cli”* → answer: open any terminal and type `hermes`; subcommands listed (`model`, `tools`, `gateway`, etc.).  
- *“can you duplicate the launcher on my desktop but make one launch hermes versions of the chat fleet”* → built `LAUNCH-HERMES-FLEET.bat` that launches Hermes per slot.

**FINDINGS/BUGS**  
- Hermes CLI resides at `C:\Users\wompu\AppData\Local\hermes`; PATH not set in current shell.  
- 425 runaway `bash.exe` processes were spawned by the workflow + miner; resolved by stopping the workflow and killing miner nodes.  
- Regenerating the LoRA dataset would regress below the floor (only 764 new rows vs 1366 existing); left unchanged.  
- Cross‑substrate edge generator skipped 9 galaxies: 5 correctly omitted (no domain node), 4 fixable but need synthetic domain nodes (`sierra` unit).  
- GNN ref‑pool feeder added 8 confirmed entries, no conflicts.

**DOMAIN SPECIFICS**  
- Hermes profiles system: `HERMES_HOME` + `<slot>` under `%LOCALAPPDATA%\hermes\profiles\<name>`.  
- Hermes CLI entry point: `hermes_cli.main:main`; command syntax `hermes -p <profile>`.  
- PRISM fleet launcher uses Windows Terminal tabs; each tab runs `slot-tab-boot.ps1` → `hermes -p <slot>`.  
- Galaxy synthesis refresh tool (`galaxy-synthesis-refresh.mjs`) for incremental updates of galaxy brains.

**TOOLS USED**  
- Hermes CLI (`hermes_cli.main:main`, profile commands).  
- PRISM tools/dispatchers:  
  - `ai-systems-synergy-goal-gate.mjs` (deterministic gate).  
  - `vault-to-gnn-refpool` feeder.  
  - `generate-cross-substrate-edges.mjs`.  
  - `galaxy-synthesis-refresh.mjs`.  
  - `merge-augmentations.mjs`.  
- Windows Terminal scripting (`.bat`, `.ps1`).  
- Hermes workflow skill `/hermes-workflow`.

**OPEN THREADS**  
- **GNN full coverage** – requires India team’s GPU re‑evaluation of the expanded ref‑pool.  
- **Scheduled mining & synthesis refresh** (04:00 daily, weekly cron) to grow vault data for LoRA and GNN.  
- **Fix missing domain nodes** for 4 galaxies in cross‑substrate edges via a synthetic domain node generator (`sierra` unit).  
- Optional: run the `sierra` unit or hand over GPU re‑eval once the ref‑pool is grown.
