# quoting session 95e7030e (2026-05-25, 58.8MB, spine 232KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Alpha‑slot commits (20+): `U‑PSN‑ACTION‑HINT`, `U‑PSN‑BANNER‑FAIL‑LOUD`, `U‑PSN‑DOC‑REFLECT`, `U‑PSN‑OLLAMA‑VERB‑TRIGGER` (+R12‑FIX), `U‑PSN‑TAKEUP‑MAP‑EXPORT`, `U‑PSN‑NUDGE‑R12‑AUDIT*`, `U‑PSN‑DEFER‑QUEUE`, `U‑PSN‑GAPS‑2345`, `U‑PSN‑A5‑B5`, `U‑PSN‑E3‑DEFER‑TELEM`, `U‑PSN‑DISCOVERIES‑WIKI`, `U‑PSN‑INJECTION‑DEDUP‑LIB`, `U‑PSN‑DEDUP‑WIRE‑N6`.  
- Commit IDs: `4690e17f3b`, `2548d0cc74`, `c46a7d5537`, `646790f668`, `f73769ccfc`.  
- New modules: `prompt-rewriter-ollama.mjs` (timeout 15 s, smallest‑first cascade, prompt‑hash dedup), `stop-hook-timeout-budget.mjs`, `stop-session-spend-summary.mjs`, `injection-dedup.mjs`, `pre-tool-savings-multi.mjs`, `read-auto-limit.mjs`, `rtk-prefix-reminder.mjs`, `stop-psn-savings-aggregate.mjs`, `session-start-savings-headline.mjs`, `jsonl-tail-prune.mjs`, `stop-ledger-prune.mjs`, `mcp-state-check.mjs`, `posttool-rtk-adoption-measure.mjs`, `rtk-fraction-tune.mjs`, `token-savings-router-table.mjs`, `pre-tool-router-table-advise.mjs`, `stop-token-savings-summary.mjs`, `posttool-ollama-rewriter-corpus.mjs`, `detector-bandit-tune.mjs`, `audit-mcp-route-takerate.mjs`, `injection-dedup-dashboard.mjs`.  
- Final MRI commit: `f73769ccfc`; slot‑soul, discipline‑expert, comp‑build‑enforce, PSN‑checklist (4 units).  

**DECISIONS**  
- Move fleet‑reaper ownership from **alpha** to **golf**; `/checkin‑alpha` now forces slot claim.  
- Adopt bidirectional calibration for rewriter confidence (penalties + boosts, 1.00 floor).  
- Switch to smallest‑first Ollama model cascade; raise timeout to 15 s.  
- Enable per‑session model cache & prompt‑hash dedup.  
- Add global MCP‑disconnect suppressor (`mcp-state-check.mjs`).  
- Implement RTK enforcement hook (env flag `PRISM_RTK_ENFORCE_ENABLE=1`).  
- Create PSN aggregation pipeline: telemetry sidecars → session‑start headline, stop‑psn‑savings‑aggregate.  
- Unified router table for 44+ RTK/MCP routes; use bandit tuning (`detector-bandit-tune.mjs`).  
- Prioritize top‑ROI wave (top‑100 nodes >80 % savings); audit remaining waves after wrap.  

**OPERATOR DIRECTIVES**  
- Set `PRISM_RTK_ENFORCE_ENABLE=1` in `settings.json`.  
- Restart Ollama daemon (`ollama serve`).  
- Verify MCP connectivity; suppress nudge spam until reconnect.  
- Run nightly RTK fraction recalibration (`stop‑rtk‑fraction‑recalibrate`).  
- Commit all high‑ROI token‑saving optimizations to alpha work tree (`H:/prism-slot-alpha`).  
- Apply operator‑side wires via `apply-token-savings-wires.mjs` (envVars + hook entries).  

**FINDINGS/BUGS**  
- R12 banner hard‑coded 30 % doctrine; fake action refs removed.  
- TAKEUP_CREDITED_ACTIONS drift due to hard‑coded set.  
- Import‑guard caused `exit‑255`; fixed by guard on `main()`.  
- Git lock contention resolved with direct alpha commits (`git -C H:/prism-slot-alpha`).  
- 16 Tier B leaks eliminated via camelCase dispatcher mapping.  
- MCP route take‑rate: 1176 fires / 0 takes (no JSON‑RPC handshake).  
- Slot‑soul, discipline‑expert, comp‑build enforce TTL set to 5 min; should be session‑lifetime (24 h).  
- PSN‑checklist lacked dedup logic; now uses `injection-dedup.mjs`.  
- Per‑file dedup missing for test‑coverage, code‑completeness, pre‑write‑graph, doctrine‑surface, memory‑relevance.  

**DOMAIN SPECIFICS**  
- **Hooks/dispatchers**: PreToolUse, PostToolUse, UserPromptSubmit, Stop (budget, ledger prune, token‑savings summary).  
- **Detectors**: grep/multiline, glob/broad, write/size, bash/git commit/push, read/large, websearch keyword.  
- **Metrics**: per‑session token‑savings dashboard, route take‑rate stats (`mcp-route-suggest-stats.json`), injection cost audit (243 K tokens / 1,613 injections).  
- **Paths**: `scripts/lib/token-savings-router-table.mjs`, `.claude/hooks/pre-tool-router-table-advise.mjs`, `posttool-ollama-rewriter-corpus.mjs`.  

**TOOLS USED**  
- PRISM hooks framework (pre/post/stop), `/checkin-alpha`, `CronCreate` (`94d46b4a`, `2f96dc3b`).  
- Scripts: `scripts/lib/*` utilities (`token-savings-router-table`, `detector-bandit-tune`, `jsonl-tail-prune`, `mcp-state-check`, `apply-token-savings-wires`).  
- State dashboards (`state/shared/dashboards/*.jsonl`).  
- Settings.json for hook ordering & knobs.  

**OPEN THREADS**  
- Commit staged doctrine‑surface TTL fix (`U-MRS-DOCTRINE-TTL-FIX`) after peer lock clears.  
- Verify MCP route take‑rate wiring (probe/handshake).  
- Trigger system‑viz regeneration to confirm unified pre‑search injector node.  
- Monitor token‑savings dashboard for 5 M tokens/day fleet‑wide target.  
- Ensure operator‑side wires are applied and backed up (`settings.json` backups).
