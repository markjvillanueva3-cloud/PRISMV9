# quoting session 928a8226 (2026-06-10, 71MB, spine 701KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `scripts/fix-hookify-frontmatter.mjs`: 110 malformed `.local.md` hooks → YAML front‑matter; ASCII‑sanitized 28 non‑ASCII (130 rewrites).  
- `C:/Users/wompu/.claude/settings.json`: removed 6 dead `.mjs` refs; mirrored to H:.  
- `ollama-resilient-pull.ps1`: fixed em‑dash parse error; re‑launched pull for `gpt‑oss:120b`, now ~94 MB/s (~96 % complete).  
- Commit `c593b096fb`: added DEFAULT_NUM_PREDICT to ask‑ollama.mjs, updated callOllama.  
- Commit `13017de764`: `remove-dangling-hook-refs.mjs` removed invalid hook refs.  
- Commit `29f2d177f6`: SYSTEM‑SYNERGY‑GAPMAP‑2026_06_08.md – node‑by‑node state table & ranked gaps.  
- Commit `8550436c8a`: updated cag-router.mjs to reference existing Qdrant collections.  
- Commit `c9fe32d8b2`: added 8 GB heap flag & 2000‑char clamp to embed‑wiki‑into‑tribal-index.mjs; fixed OOM/context bugs.  
- Commit `182788232a`: cap‑safe tribal‑index loader, fail‑open guard, restored PSN leg #5 and live reranker.  
- Commit `a3e6d3ca97`: defensive fail‑open logic, brain restored to 4 162 entries; documented in CLAUDE.md & wiki.  
- Commit `fb314a6fd1`: Docker‑service‑health CLI + 5/5 tests; detects down/wedged daemons and reports fix command.  
- Commit `14b8b383a2`: wired singleton‑service‑guard into Stop hook chain; live MCP recovery via reaping & respawn.  
- Commit `006c435ddb`: corrected MCP restart docs (must use `TRANSPORT=http`).  
- Commits `e2081e0780`, `d22681f5d2`: upgraded singleton‑service‑guard to reap + start using mcp‑server‑daemon helper; 3‑of‑3 scrutiny PASS.  
- Commit `bee977a004`: added NODE_OPTIONS=--max-old-space-size=8192 for reranker, preventing OOM on large index.  
- Commit `698cf12ad2`: static list of 4 “freeze‑eligible” tasks (superseded).  
- Commit `4141daf9d8`: migration‑freeze marker (`state/shared/MIGRATION-FREEZE-ACTIVE.flag`) replacing hard‑coded list; partitions ~47 disabled PRISM tasks.  
- Commit `U-FTH-WIKI-LESSON`: wiki entry documenting cry‑wolf fix (marker vs static list).  
- Commit `U-QP-COST-SAVINGS-TEST`: test suite for CostSavingsTrackerEngine after making its store path injectable.  
- Restored and router‑aware `scripts/generate-quoting-awareness.mjs`.  
- Added `WIRE‑EXEMPT` tag to `WEDMQuoteBridgeEngine.ts`.  
- Updated `aiReasoningDispatcher.ts` cascade defaults to installed Blackwell models (`qwen2.5-coder:1.5b`, `gpt‑oss:20b`, `qwen2.5-coder:32b`).  
- Commit `U-BW-DISPATCHER-SCAN`: cascade defaults repointed to installed Blackwell tiers; source‑lock extended.  
- Commit `U-BW-DISPATCHER-SCAN-WIKI`: wiki lesson documenting guard blind spot and fix.  
- Commit `U-PSN-SNAPSHOT-REFRESH`: 6‑day stale PSN snapshot regenerated; real under‑wired pairs confirmed.  
- Commit `U-QP-DIGITALTWIN-TEST`: added reference‑value test for last untested quoting algorithm.  
- Commit `U-QP-MARGIN-FLOOR-GATE`: margin‑floor guard inserted into QuoteEstimatorEngine; caller override, default 20 % and WARN flag.  
- Commit `U-QP-MARGIN-FLOOR-SHOPCONFIG`: floor now self‑sourced from ShopConfigurationEngine (mirrors overhead_pct).  
- Commit `U-QP-NRE-AMORTIZE-TEST-FIX`: stale NRE amortization test corrected; code unchanged.  
- Commit `ec597dbcb3`: margin‑floor dispatcher round‑trip (7/7).  
- Commit `87d5c4bf9a`: margin‑floor alert banner on QuoteBuilderPage (6/6, tsc‑clean).  
- Commit `4b0980c56a`: AI‑substrate routing‑matrix coverage 4/5→5/5 (27/27).  
- Commit `62b114cb7b`: ResizeObserver polyfill + test fix for quote‑pages (13/13).  
- Commit `8258918f78`: U‑QP‑FRONTEND‑TEST‑REPAIR: mock contract correction, null‑guards, failure‑mode test.  
- Commit `4c12a75a8d`: synthetic/placeholder outcomes cannot promote calibration factors to live quote‑time file (closed‑loop provenance gate).  
- Commit `d294957c4d`: real outbound sold‑price distribution alignment fail‑closed promote gate.  
- Commit `bf10035ec0`: quote‑time gate warns on >24 h stale factors, can hard‑cut to raw FMV (calibration freshness preflight).  
- Commit `d447cee202`: typed adapter converting BlueprintOCREngine output into shape expected by bridge(); rewires shopDispatcher and businessDispatcher.

**DECISIONS**  
- Root‑cause debugging first; bulk transform hook files instead of ad‑hoc patches.  
- Remove dead wiring from settings.json rather than disabling hooks.  
- Leave unwired engines for owner triage to avoid resurrecting retired stubs.  
- Fix pull script parse error, preserve resilient logic.  
- Run local Ollama on RTX PRO 6000 Blackwell; set `OLLAMA_MAX_LOADED_MODELS` default 3×GPU.  
- Treat `gpt‑oss:120b` as reasoning model → larger `num_predict` (default 1024).  
- Batch subagent workflow to 3 concurrent agents to avoid Anthropic rate limits.  
- Buffered loader for tribal index; add 8 GB heap flag; add empty‑text skip guard to embedder.  
- Singleton‑service‑guard `--fix`: reap/start recovery; regex slash‑agnostic.  
- Stop‑regression bundle emits JSON on both allow/block paths.  
- MCP server OOM fixed by raising node heap floor ≥4096 MB across spawn paths.  
- Qdrant tribal migration deferred due to mid‑rebuild source index.  
- Fleet‑task‑health watch: add `EXPECTED_DISABLED_TASKS` partition for intentional disabled tasks.  
- Closed‑loop learning gates fail‑closed; synthetic data cannot promote, stale factors withheld.  
- Margin‑floor enforcement as gate with caller override and canonical source.  
- Ollama used for heavy summarisation when Claude rate‑limited.

**OPERATOR DIRECTIVES**  
- Outside golf allowlist → set `PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1` or edit settings.json to remove `golf-slot-write-allowlist.mjs`.  
- After migration freeze ends, re‑enable the 47 disabled scheduled tasks via installers.  
- Pull ollama models matching GPU/CPU/NVMe SSD and 128 GB RAM (verified).  
- Verify docker, qdrant, obsidian vault app, PSN, system‑viz, prism galaxies, front‑end & back‑end build, claude.md, memories, wiki, tribal knowledge, prism awareness, gsd, tdd, skills, scripts and hooks are all wired, tested, validated.  
- Clear goal when all surfaces satisfy above condition.  
- Use ultracode for planning/automation; use ollama for grunt tasks.  
- Continue charlie domain work; start charlie slot, run full startup audit, aim for closed‑loop learning ready for JM Die beta next week.

**FINDINGS/BUGS**  
- Hookify: 110 front‑matter warnings + 1 charmap crash → bulk rewrite & ASCII sanitization.  
- Dangling module refs: 6 `Cannot find module` errors removed from settings.json.  
- Scheduled‑task WARN: 3 tasks marked MISSING due to migration freeze (intentional).  
- Unwired engines: 90 built engines with no dispatcher; 14 suggested dispatchers but many deliberate orphans.  
- Ollama pull parse error prevented `gpt‑oss:120b` download; fixed, now ~94 MB/s.  
- Synergy bug: router still selects `gpt‑oss:20b` for best‑tier tasks after 120b installation (needs review).  
- V8 string cap caused tribal‑index parse failure → buffer loader.  
- Default heap OOM on large index mitigated by raising node heap to 8192 MB.  
- MCP server OOM on boot due to inherited 384 MB heap cap → fixed via heap floor helper.  
- System‑viz `find` OOM caused by default node heap; no re‑exec guard added intentionally.  
- PSN snapshot stale → regenerated, under‑wired pairs confirmed.  
- Synthetic data promotion bug fixed by `classifyOutcomeProvenance()`.  
- Stale calibration factor issue resolved by freshness preflight gate.  
- OCR `$1` noise in `jm-sold-orders.json` source‑side issue; code will not drop rows.

**DOMAIN SPECIFICS**  
- Hook files: `.claude/hookify.*.local.md`, `config_loader.py`.  
- Settings: `C:/Users/wompu/.claude/settings.json`.  
- Engines: Ollama (`gpt‑oss 120b`, `qwen2.5‑coder 1.5b`), Nomic‑embed‑text, Qdrant collections (`prism_engines`, `prism_skills`, `prism_formulas`).  
- Actions/dispatchers: ask‑ollama.mjs, embed‑wiki‑into‑tribal-index.mjs, tribal‑rerank.mjs, cag-router.mjs, scripts/fix-hookify-frontmatter.mjs, scripts/remove-dangling-hook-refs.mjs, aiReasoningDispatcher.ts, cost‑bridge‑dispatch.mjs, shopDispatcher, businessDispatcher.  
- Metrics: GPU utilization (0 % → 54 GB free after fix), Ollama inference speed (~114 tok/s for 120b), embedder throughput, `margin_floor_pct`, `below_margin_floor` flag, NRE amortization values, PSN synergy scores.  
- Paths: H:/prism/scripts/ollama-resilient-pull.ps1, H:/prism/.claude/hooks/stop_on_hook_unregistration.mjs, H:/prism/.claude/scripts/embed-wiki-into-tribal-index.mjs, `mcp-server-supervisor.mjs`, `singleton-service-guard.mjs`, `system-viz-query.mjs`, `fleet-task-health-watch.mjs`, `populate‑tribal‑vault.mjs`.

**TOOLS USED**  
- PRISM skills: `/loop`, `/goal`, `/checkin-golf`, `/effort`.  
- Node scripts: fix-hookify-frontmatter.mjs, remove-dangling-hook-refs.mjs, ollama-resilient-pull.ps1, ask‑ollama.mjs, embed‑wiki‑into‑tribal-index.mjs, tribal-rerank.mjs, cag-router.mjs, singleton-service-guard.mjs, mcp-server-daemon.mjs, system-viz-query.mjs.  
- Docker compose (Qdrant, Postgres, Grafana, Prometheus).  
- Node.js with V8 heap flag adjustments; portable-node shim.  
- Vitest test harness, TypeScript compiler (`tsc --noEmit`), Docker-service-health-check CLI.

**OPEN THREADS**  
- Wire the 14 unwired engines with suggested dispatchers (owner triage needed).  
- Resolve router logic so `gpt‑oss:120b` selected for best‑tier categories.  
- Re‑enable missing scheduled tasks after migration freeze ends.  
- Verify all remaining hook errors resolved in subsequent sessions.  
- Re‑embed remaining ~29 K tribal entries; implement WRITE‑side sharding; migrate tribal collection to Qdrant.  
- Review/possibly consolidate MCP supervisor stack; consider keep‑alive task if needed.  
- Per‑galaxy synergy (foxtrot/whiskey/mike/kilo/charlie/delta/lima) pending.  
- Build `U-QP-SEND-QUOTE-WIRE` (send button + PDF render); requires `/compact`.  
- Activate real ERP actuals via QuotingActualOutcomeLoaderEngine once credentials available.  
- Corpus activation (`U-QP-CORPUS-ACTIVATE`) to refresh calibration data.  
- Front‑end UI design for final app build; resolve remaining quoting frontend failures in hotel domain.  
- Source‑side OCR cleanup of `$1` noise remains operator responsibility.
