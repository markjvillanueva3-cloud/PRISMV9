# quoting session e54d46d0 (2026-06-26, 13.3MB, spine 102KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `2b990d785d`: Octopus now supports multiple Hermes‑Grok voices via `hermesGrokModels` input and ledger tagging.  
- `5e8638f141`: Full generative stress harness built covering summarize/explain at easy/medium/hard with n=3 hard tier.  
- `cc24367e41`: False‑0 guard added to `runTaskOnModel`, distinguishing timeouts/no‑output from real failures.  
- `014cfefb46`: LLM‑judge module (32 b) introduced, replacing keyword overlap for hard‑tier evaluation.  
- `056d0710bc`: Tri‑state judge handling (PASS/FAIL/ABSTAIN) and no‑signal mapping added; low‑n flag rendered in matrix output.  
- `52cd201270`: Hard‑tier n=3 expansion of stress battery plus wiki lesson documenting false‑0 principle.  
- `87c428c0bd`: Root‑cause analysis of fleet‑reaper killing long runs (>10 min); per‑model invocation recipe documented.  
- `619e7af8d1` / `a4fc24ea2e`: Cheap‑model routing – `ask‑ollama` now prefers warm **qwen2.5‑coder:7b** (~5 GB VRAM) over cold 32 B (~20 GB), validated by stress harness; fail‑soft priming on first use.  
- `a5ff03c32b`: Stratified documentation, wiki lesson, and memory entries documenting change and validation.

**DECISIONS**  
- Adopt multi‑voice Hermes strategy; keep existing architecture intact.  
- Build deterministic stress harness with LLM‑judge; use tri‑state grading to separate timeouts from failures.  
- Use per‑model invocation (<10 min) to avoid fleet‑reaper kills; cheap‑model routing into `ask‑ollama` after validating 7 B selection satisfies memory unblock condition.  
- Adopt prepend‑based warm‑first strategy for summarize/explain, keeping 32 B as fallback; no runtime difficulty classifier needed.  
- Fail‑soft design: if warm load times out under contention, fall back to prior behavior.

**OPERATOR DIRECTIVES**  
- “you’re the only one operating” – GPU idle; dist rebuilds and deferred GPU work now safe.  
- “continue” – proceed with remaining build tasks without deferring reversible/internal forks.

**FINDINGS/BUGS**  
- U1 route hook healthy (no code change needed).  
- U4 advisory paths (`large‑read‑digest`, `grep-index-first`) correct‑by‑design; no action required.  
- False‑0 bug class identified: timeouts/no‑output must be third state at both subject and judge layers.  
- Fleet‑reaper kills any node process running >10 min, causing judged runs to exit 255; resolved by per‑model invocation strategy.  
- 32 b judged run crashed due to reaper; 7 b/14 b runs succeeded.  
- Stale doc: `CLAUDE.md` incorrectly states 7 B/14 B tags retired; `/api/tags` shows all three installed.  
- Warm‑7 B load timed out under high GPU contention (60 s); resolved when GPU idle (3.7 s).  
- Manual `/ollama-*` skills hardcoded 7 B, while auto‑offloader used 32 B; change aligns both paths.

**DOMAIN SPECIFICS**  
- Engines: `MultiModelConsensusEngine.ts`, `ask‑ollama.mjs`, `ask‑hermes.mjs`.  
- Dispatchers: `octopus-dispatch.mjs`, `octopus-utilization-driver.mjs`.  
- Paths: `ask‑ollama`, `/api/tags`, `trigger-command-pipeline.mjs`, `/ollama-summarize`, `/ollama-explain`.  
- Metrics/paths: stress‑battery‑generative, runTierSweep, ledger tagging (`vendor:model`), false‑0 guard, LLM‑judge verdicts.  
- VRAM usage: ~5 GB for warm 7 B vs ~20 GB for cold 32 B; test counts (84 tests, 11/10/63 per reviewer).

**TOOLS USED**  
- PRISM slot helpers: `chat-slots.mjs`; audit: `audit-roadmap-drift.mjs`.  
- Build: `npm run build:incremental`, `npm run build`.  
- Stress harness scripts: `scripts/lib/stress-battery-generative.mjs`, `scripts/ollama-stress-test.mjs`.  
- Cron helper: `.claude/helpers/install-octopus-utilization-task.ps1`.  
- PRISM scrutiny script (3‑of‑3 reviewers, mutation testing).  
- Fleet lock sweep, live‑executor change.  
- R15 validate harness, production‑reach audit.  
- Test harness (84 tests), memory & wiki generation scripts.

**OPEN THREADS**  
- Extend judged battery to triage/codegen modes once GPU idle (queued).  
- Route `/ollama-*` skills through `ask‑ollama` for NC‑safety guard and telemetry (UX decision pending).  
- Correct stale note in `CLAUDE.md` (“7 B/14 B retired”).  
- Implement advisory‑layer hints (model‑tier advisor / cost router) – low value, non‑executing.
