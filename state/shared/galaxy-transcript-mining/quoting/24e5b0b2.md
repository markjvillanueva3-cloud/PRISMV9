# quoting session 24e5b0b2 (2026-05-19, 13.5MB, spine 50KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `70938167bf` (slot/india): 6 core files – mill & lathe enumerators, batch worker, system‑viz emitter, runtime cache, resume guard + launcher scripts, spec (`SFC-ACCURACY-MS1-DESIGN.md`).  
- Commit `3d4d1b9f98` (slot/india): 5 new files – sidecar‑aware guard logic, test harnesses, installer script for scheduled tasks.  
- System‑viz augmentation file `state/shared/system-viz/augmentations/sfc-variability.json` now contains ~12 k nodes / 24 k edges from the mill batch; lathe augmentation pending.

**DECISIONS**  
- Adopt a *two‑task* architecture: guard + detached scheduled‑task batch to survive machine switches.  
- Implement a per‑domain **resume‑state sidecar** (`.resume-state.json`) that records host, pid, launchSkip, worker frontier; used by guard to compute exact resume point.  
- Use round‑robin chunking `(idx‑1) % N === w` and store chunks as `chunk-w<worker>-<12‑digit-start>.jsonl`.  
- Persist results in **system‑viz** nodes per machine type, attach DB edges for tooling/machine/material; feed into GNN training.  
- Wire a runtime lookup engine (`sfc_variability_lookup`) that mmaps chunk files and returns nearest‑canonical envelope at calc time.  
- Scale via Docker + `--workers N` (default 4) with child processes reusing warm orchestrator instances.  
- Use Obsidian wiki for documentation; PRISM dispatcher hooks for batch launch/monitoring.

**OPERATOR DIRECTIVES**  
- Resume after computer switch: ensure both mill and lathe batches are alive, sidecars present.  
- Start the scaled batch (4 workers) immediately; monitor chunk growth (~200 cells/min).  
- Commit all new guard / launcher files to the `slot/india` worktree (`H:/prism-slot-india`).  
- Register scheduled tasks via elevated PowerShell script `install-sfc-variability-task.ps1`.  
- Proceed with wiring: implement `SfcVariabilityCorpusEngine`, expose `prism_calc:sfc_variability_lookup`.  

**FINDINGS/BUGS**  
- Initial tsx path mis‑resolution caused worker crashes; fixed to use `node_modules/.bin/tsx/dist/cli.mjs`.  
- Resume logic bug: skip derived from chunk start index caused gaps; corrected to min frontier over workers.  
- Cross‑PC lock stale issue (10 min window) prevented guard from relaunching after host change; added host‑keyed lock and staleness check.  
- Empty enumeration case incorrectly triggered relaunch; now guarded against by detecting zero chunks before launch.  
- Performance: 4 cells/s per worker with full orchestrator bootstrap (~30 s); 607 k combos/s enumerator, 710 k combos/s lathe enumerator.

**DOMAIN SPECIFICS**  
- **SFC Calculator** (`web/src/pages/SfcCalculatorPage.tsx`) – new UI.  
- **SpeedFeedOrchestratorEngine** (3 575 LOC) – core physics engine; dispatcher entry `prism_calc:sf_orchestrate`.  
- **Enumerators** – mill (`sfc-variability-enumerate.mjs`), lathe (`sfc-variability-enumerate-lathe.mjs`).  
- **Batch worker** (`sfc-variability-batch-run.mjs`) – drives orchestrator over combos.  
- **System‑viz augmentation** – emits machine‑type roost + cell leaves, DB attachment edges.  
- **Runtime cache** (`sfc-variability-cache.mjs`) – 0 ms hit on cached cells.  
- **AutoAdjustCascadeEngine** & **PrismEnhancedRecommenderEngine** – future cost‑aware recommendation hooks.

**TOOLS USED**  
- PRISM CLI: `/checkin-india`, dispatcher, `prism_calc` commands.  
- Scripts: enumerators, batch runner, emitter, cache, resume guard (`sfc-variability-resume-guard.mjs`).  
- Docker + `docker-compose.yml` (planned).  
- Obsidian wiki integration via `AGENT_CHAT.md`.  
- System‑viz augmentation pipeline (`regen-viz FAST[]`, `merge-augmentations`).  

**OPEN THREADS**  
1. **Scheduled task registration** – requires elevated PowerShell; must be run to enable auto‑resume across reboots.  
2. **Lathe batch wiring** – ensure sidecar and guard are active, monitor chunk growth.  
3. **Runtime lookup engine** – implement `SfcVariabilityCorpusEngine`, expose via dispatcher.  
4. **GNN training feed** – confirm augmentation file is picked up by `regen-viz` cron; validate edge counts.  
5. **Performance tuning** – benchmark orchestrator throughput, consider worker‑level caching of material/tool data.  
6. **Documentation & Obsidian wiki** – finalize wiki pages for future chat handoffs.
