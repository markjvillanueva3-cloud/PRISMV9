# quoting session 6a8a0fc5 (2026-06-23, 50.3MB, spine 400KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `region-classifier-lib.mjs` (commit 0a41c90a4c) – GPU‑free page→region segmentation, data‑loss‑safe routing.  
- `region-glue-lib.mjs` – fraction→pixel scaling, recall‑first union (`mergeRegionResults`).  
- `region-classify.mjs` – live glue with DI‑testable orchestrator, Ollama qwen3‑vl segmenter (8 DI tests).  
- `buildRegionRoutedFused.mjs` – hybrid fused output preserving full‑page floor and non‑dim fields.  
- Summary recompute for active‑learning to recognize region‑only hallucination candidates.  
- 6a9f5253f6 U-XRAY-GDT-CORPUS-SCAN-ROUTE – blocks empty‑note pollution in pdf‑parse‑extract.  
- be6099d9c4 GD&T corpus ingestion doc update.  
- 2d77536fe8 U-XRAY-GDT-WATCHER-LANE-CAPTURE – logs OCR vs text lanes.  
- a783df2419 ENSEMBLE-NONDIM-UNION (`fuseEnsemble`) returns GD&T, notes, profiles, surface finishes with recall‑first union.  
- abc63f4874 GDT-LABEL-TIER – adds `gdt_labels` to OCR training loop; persisted in JSONL and exposed via LoRA pair builder.  
- e7fd24791b REGION-NONDIM-RESCUE (`mergeRegionFused`) unions per‑region fused non‑dim data with full‑page floor.  
- 3a330195d6 GDT-GOLD-VERIFY – `buildGdtRecords` emits `VERIFY-gdt.csv`.  
- U-XRAY-OCR-ADAPTER-WIRE-EXEMPT-REVERT (doc‑only) – reverted WIRE‑EXEMPT tag, added inline docstring.

**DECISIONS**  
- Region routing rescues dense pages (3400×2200 → 28 dims vs 0 dims); recall ≈ 0.43 on part 05850; full‑page floor must be preserved.  
- Cron wiring blocked until hybrid fused summary recompute; once available, wire `--region-route` opt‑in/off.  
- No cheap num_ctx tweak fixes dense failures – region routing is correct solution.  
- BlueprintOCRAdapter is pure‑type, no runtime consumers → marked WIRE‑EXEMPT but later decision to keep UNWIRED visible and document reason.  
- Build loop order: finish any in‑flight Zulu unit → complete FEATURE‑ROUTING “both” arc (live router coding→Sonnet, octopus picker, graph reconcile) → descend NEVER‑IDLE ladder into backlog.  
- GD&T corpus ingestion hardening completed; remaining work data‑gated region‑route default‑on or operator‑gated P2.10 ingest.  
- No GD&T calibration now due to volume constraints; next unit will be generic FIXES/WIRINGS task.

**OPERATOR DIRECTIVES**  
- Continue autonomous `/loop` on xray slot (one unit/iteration, real reference‑value tests, 2‑arm scrutiny).  
- Prioritize back‑end dev for xray OCR/blueprint reading.  
- Use Ollama offloading, Hermes agents, parallel agents, engineered loops, harnesses and crons.  
- After region routing arc, wire `--region-route` into training cron; validate multi‑part comparison against baseline 0.43.  
- Finish any in‑flight Zulu unit; complete FEATURE‑ROUTING “both” arc; then descend NEVER‑IDLE ladder.  
- Read handoff docs (`state/shared/handoffs/HANDOFF‑claude‑dc3f020e‑vault‑ops.md`); build next highest ROI or improve existing units.  
- Prefer Sierra’s domains (vault‑ops hardening, system‑viz).  
- Never idle while token budget green; checkpoint at YELLOW and let auto‑compact reset.

**FINDINGS / BUGS**  
- Full‑page VLM fails on dense pages (0 dims); region routing adds recall without dropping floor.  
- Cron wiring previously dropped GD&T/notes due to fused output lacking fields.  
- Summary recompute needed for active‑learning to recognize hallucination candidates.  
- `fuseEnsemble` silently dropped GD&T/notes/profiles/surface finishes; `buildRegionRoutedFused` lost GD&T on dense pages.  
- Scan‑route unit prevents empty‑note pollution in pdf‑parse‑extract.  
- Watcher lane capture logs OCR vs text lanes.  
- BlueprintOCRAdapter misidentified as wireable; later fixed with WIRE‑EXEMPT marker and inline docstring.  
- WIRE‑EXEMPT tag caused re‑chase loop; memory entry `reference_xray_blueprint_ocr_adapter_deferred_2026_06_23` records decision.

**DOMAIN SPECIFICS**  
- **Engines/Modules:** region-classifier-lib, region-glue-lib, region-classify (Ollama qwen3‑vl), vision‑ensemble‑fuse, ocr‑training‑loop‑lib, region‑glue‑lib, region‑classify.  
- **Dispatchers:** Hermes/forge‑team agents (Sonnet), Octopus consensus (`prism_ai:consensus_decide`), Opus fallback.  
- **Pipeline:** runEnsembleOverImage → fuseEnsemble → buildTrainsetRow → classifyActiveLearning; pdf‑parse‑extract, pdf‑watcher‑sweep, validate-perfect-parts, vision‑tiling‑lib.  
- **Metrics/Outputs:** recall per part, union_scoreable, floor_ok, region_ok, merge_stats; `n_gdt`, `n_notes`, `n_profiles`, `n_surface_finishes`; `VERIFY-gdt.csv`.  
- **Key Paths:** `src/physics/constants.ts`, `scripts/region-classify.mjs`, `pdf-parse-extract.mjs`.

**TOOLS USED**  
- PRISM tooling: `chat-slots.mjs`, `/checkin` pipeline, `loop-state.mjs`, auto‑compact, dedup guard.  
- Agents: Hermes (Sonnet), Octopus consensus, Sonnet coder ensemble, Ollama qwen3‑vl / qwen3‑coder / qwen2.5‑coder.  
- Scripts: `audit-unwired-engines.mjs`, `mergeTiledDimensions.mjs`, `buildRegionRoutedFused.mjs`, `extractWithRegionRouting.mjs`, `vault-health.mjs`, `system-viz-query.mjs`, `self-compact.mjs`, `fleet-survival-status.mjs`.  
- Memory recall hook for decision persistence; tool‑batch budget tracker.

**OPEN THREADS**  
- Wire `--region-route` into training cron (unblocked with hybrid fused + summary recompute).  
- Heavy multi‑part comparison of region‑route vs tile vs full‑page recall on dense subset.  
- New backlog items: P2.7 GT triangulation, P2.10 GD&T tribal.  
- Validate that cron wiring preserves all non‑dimension training labels and active‑learning routing behaves correctly.  
- Finish any in‑flight Zulu unit; complete FEATURE‑ROUTING “both” arc; descend NEVER‑IDLE ladder into backlog.  
- Operator‑staged GD&T source PDFs for P2.10 ingest; data‑gated region‑route default‑on broadening dataset; potential domain‑tag fix if operator triggers it.  
- Remaining system‑viz ghost builds pending.  
- GD&T per‑type calibration (volume‑blocked).  
- Ongoing audit cleanup (false positives in unwired list).  
- Tool‑batch budget exceeded 191/183 – fresh session needed to resume GHOST builds.
