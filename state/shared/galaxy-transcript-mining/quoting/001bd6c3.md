# quoting session 001bd6c3 (2026-06-10, 106.3MB, spine 954KB, 11 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hermes integration commits ab2ccf42a4, e6713584e2, c988a21ec4, 56aa423427 – PRISM MCP 3100, config.yaml updated.  
- Hermes config migrated v0→v28 (config_version 28).  
- Qdrant container `prism‑qdrant` healthy on :6333; Obsidian REST API reachable (:27123).  
- Commit 28b72e4dee – 34 MEMORY.md files updated, added `fill-galaxy-memory-sections.mjs` & tests.  
- U‑OAB‑PLAN commits: U9 co‑residency (7771ca7f86), offload router roster refresh (2340a2e699), route hook default change (a2756779c2), take‑rate banner fix (a60d7ba0bf), LLM compressor on RTK residue (612418fde7).  
- Go‑live merge onto `cad-fusion-live-ms0` (5a91ba1862); fleet‑wide default coding model GPT‑OSS‑120B via settings.json (3904b5e7b0).  
- U‑OCTOPUS‑LIVE‑PRODUCER, U‑OLLAMA‑FANOUT, U‑CONSENSUS‑DRAIN‑LOCAL, U‑REWRITER‑LOCALHOST‑FIX, U‑OPTCTX‑LOCALHOST‑FIX, U‑LOCALHOST‑HARDCODE‑GUARD, U‑RAG‑DENSE‑EMBED‑127.  
- Commits for pressure handling: 9a598c52c7 (field mismatch), d257350cf3 (CHO02 accuracy), f8b7fa6d44 (sidecar first readChatPressure), 098a9f7651 (TTL comment).  
- IncludeCodex flag commit 1b7bce6a91; panel switch 4fdf30e8f5; ledger tagging 1b7bce6a91; runLive prewarm guard bb3503a5b8; wiki entry b9ab0dc185.  
- Dream‑queue producer wired (69f82bb12c); bravo all‑galaxy gate lift (8eada5f6ea); soul evolution de‑dup (a18dbc012e).  
- Suppressed CAG route commit 76ef09d348; same‑prompt throttle commits 7bcdd4b49b, 87e5057dd1; dedup wiki tribal coverage commit 392195175b.

**DECISIONS**  
- Use :3100 MCP for Hermes coordination; no separate filesystem server.  
- Hermes primary model: gpt‑oss:20b (fleet‑standard) fallback Claude Code → later upgrade to GPT‑OSS‑120B for read/search, Qwen2.5‑Coder 32b for coding; local Ollama stack for audit/fill.  
- Run Zulu headless (`hermes -z`); use `hermes config migrate`.  
- Revert supervisor cwd patch; adjust CapabilityIndexEngine to PRISM_ROOT.  
- Do not wire Kimi 2.6 cloud (data sovereignty); make GPT‑OSS‑120B fleet‑wide default coding model.  
- Route fan‑out via local Ollama, host 127.0.0.1; write‑time guard for hardcodes; activate consensus drain in local‑only mode.  
- Suppress low‑confidence CAG route; add same‑prompt throttle to injectors; drop redundant sample list from wiki tribal coverage.  
- Treat Codex as normal voice with `includeCodex`; harden octopus consensus path; per‑model ledger IDs.  
- Adopt import‑safe guard (`isDirect`) for all hooks.  
- Use two‑tier escalation in readChatPressure; sidecar‑first logic.

**OPERATOR DIRECTIVES**  
- Bring Qdrant up, confirm Obsidian REST API running.  
- Verify Hermes messages after billing fix; link Hermes app to Claude Code.  
- Monitor Ollama health loop (10 min); prove most powerful local LLMs auto‑run per task.  
- Verify headless Zulu with gpt‑oss:20b end‑to‑end; pull/test gpt‑oss:120b; reconfigure auxiliary models to local Ollama; monitor memory pressure.  
- Audit each galaxy sequentially, fill missing memory sections; regenerate `ai-training_synthesis.md`.  
- Improve token efficiency across galaxies/domains; offload into Ollama where possible; suggest alternatives.  
- Apply dreams for all galaxies; lift gates for bravo; push through next task.

**FINDINGS/BUGS**  
- Hermes memory bridge dormant; Qdrant down → semantic_search empty; self_awareness_search stale path; Anthropic API 400 error (extra‑usage billing); 429 rate limit; Obsidian REST API not running; Docker missing or Qdrant container not started.  
- Unknown provider openai error fixed by custom provider for Ollama; Hermes daemon cached “not connected” after Qdrant down; Vision auxiliary models still point to Claude.  
- 29/34 galaxies had only 1/4 canonical sections; auto‑generated `node_*` inflated memory count; `ai-training_synthesis.md` corrupted with NUL bytes; test failures fixed.  
- GPU usage ~6 % on RTX PRO 6000 Blackwell (96 GB); GPT‑OSS‑120B installed but idle; routing infrastructure references 120B model but not used; small models used for routine tasks.  
- Offload router and route hook referenced retired models → 0 % offloading; compressor had safety breach; Kimi 2.6 free tier violates data sovereignty.  
- Windows localhost bug (localhost→::1) causing ~6–7 % offload rate; Claude workflow bursts >3 agents trigger Anthropic limit; consensus drain under‑drains queue; RAG dense arm bug with localhost.  
- Field mismatch caused `zulu-advisory-inject` to ignore pressure events; hook executed main on import blocking tests; byte‑estimate over‑reported critical pressure (~1.7 M tokens); compact boundary detector missed markers in long transcripts; sidecar TTL comment misdoc (60 s→180 s); dream cycle producer starved.  
- Codex voice always enabled → phantom `failed:spawn‑enoent` entries; VRAM co‑resident check dropped 120B when paired with 32B; drain script lacked `isDirect` guard; runLive prewarm guard untestable; RTK output missing stdin support and safety guard.

**DOMAIN SPECIFICS**  
- Hermes app, PRISM MCP (HTTP 3100), Obsidian vault (`knowledge/`), slot‑briefs directory.  
- Dispatcher actions: `prism_memory`, `prism_session`, `prism_knowledge`, `slot_brief_write`, `slot_brief_inject`.  
- Qdrant vector store on :6333; collections `prism_engines`, `prism_formulas`, `prism_skills`.  
- Hermes config file `config.yaml` (model, fallback_model, mcp_servers.prism).  
- Engines: `OllamaClientEngine`, `MultiModelConsensusEngine`, `LatheLoRAOllamaDeployerEngine`, `ConsensusQueueDrain`, `OctopusLiveProducer`, `OllamaFanout`, `RAGDenseEmbed`, `DreamLoopProposalEngine`, `DreamConsolidationEngine`, `DreamArtifactBundleEngine`.  
- Actions/dispatchers: `ask-ollama`, `dispatchOctopus`, `rankTrajectories`, `memory-index-precheck-inject`, `master-index-precheck-inject`, `cag-router`, `tribal-by-domain-inject`, `wiki-tribal-coverage-inject`, `wiki-precheck-inject`, `zulu-advisory-inject`, `readChatPressure` (`scripts/lib/chat-token-watch.mjs`), `findLastCompactOffsetInBuffer`.  
- Metrics: GPU utilization 6 %; VRAM usage 65 GB (120b) + 37 GB (32b); offload rate ~6–7 % before fix; token savings ≈50 tokens/prompt (CAG), 290 tokens/loop repeat (master), 180 B/45 tokens/session start dedup.  
- Paths: `state/shared/token-budget-<slot>.json`, `state/shared/dream‑queue/`, `state/shared/orchestrator-directives.json`, `state/shared/ERROR_LEDGER.jsonl`, `mcp-server/data/state/ERROR_LEARN_LEDGER.jsonl`, `scripts/lib/inject-throttle.mjs`, `scripts/lib/chat-token-watch.mjs`, `scripts/lib/ollama-fanout.mjs`, `octopus-first-live-record.mjs`, `zulu-advisory-inject.mjs`.

**TOOLS USED**  
- PRISM tools: Hermes CLI (`config migrate`, `doctor`, `model`, `-z`), Docker Desktop + docker‑compose, PRISM MCP client (`prism_context`, `prism_session`), Obsidian REST API (`/active/`, `/vault/`).  
- Node.js scripts: `syncGalaxyMemories`, `galaxy-synthesis-refresh.mjs`, `fill-galaxy-memory-sections.mjs`, `ask-ollama`, `ollama-prism-bridge.mjs`, `OllamaTaskOffloaderEngine.ts`.  
- Build tools: tsc, esbuild, ts-node, vitest, node:test.  
- Ultracode workflows (`wbmnnd902`, `w2hu6dowo`).  
- Git operations: commit, cherry‑pick, merge, worktree isolation.

**OPEN THREADS**  
1. Bring Qdrant up (start container or install) and confirm Obsidian REST API running.  
2. Resolve Anthropic 400 error via extra‑usage billing or rely on local fallback; verify Hermes messages after fix.  
3. Verify headless Zulu with gpt‑oss:20b end‑to‑end; pull/test gpt‑oss:120b; reconfigure auxiliary models to local Ollama; monitor memory pressure.  
4. Audit and regenerate `ai-training_synthesis.md`; enable Hermes‑Obsidian Bridge if needed.  
5. Implement dream queue producer & surface hook; refine dream‑queue output cap/rule ranking.  
6. Resolve consensus drain scaling issue (heap vs timeout); improve queue size handling.  
7. Finalize memory bridge to write memories into vault if required.  
8. Complete offload fan‑out via Ollama, fix localhost hardcodes, enforce `OLLAMA_URL` env var.  
9. Activate dormant hooks: `wiki-precheck-inject` with same‑prompt throttle; `zulu-advisory` consumer; orchestrator‑advisory producer (governance gating).  
10. Validate cron job model accuracy for 100 % accuracy requirement.  
11. Monitor GPU utilization & offload rate after applying NUM_PARALLEL=2, MAX_LOADED=3, VRAM budget 16384 CTX; ensure 120b + 32b not co‑resident.  
12. Add take‑rate instrumentation of route‑savings banner (~770 B/session).  
13. Implement master‑index relevance floor via U‑RAG‑5 eval harness.  
14. Ensure import‑safe guard pattern for all hooks; test with `node --test`.
