# quoting session bca3789f (2026-05-19, 30.5MB, spine 132KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `baef3c361d` – Dashboard offload‑rate correction (10.9 % → 42.1 %) with drift‑guard tests.  
- `94d4d0feac` – Wiki recall expanded 33×; `wiki_lookup` surfaces 22,734 MD files (`knowledge/wiki/architecture/*`).  
- `doc-reflection` – Docs for Iter 2 & 3 of OLLAMA‑EXPAND.  
- `ecbadc2862` – Added `obsidian_lookup` (reads 644 memory files).  
- `b756569c04` – Implemented `dispatcher_map`.  
- `c55b68b8ea` – Docker compose fixed: production target, port‑conflict guard, launcher entrypoint.  
- `b041693181` – Repaired reaper‑coord noise; only real routing counted.  
- `60dbc72116` – Adopted kilo’s stranded RTK rate‑limiter cluster (`rtk-prefix-reminder`).  
- `53957302d4 [LIMA]` – R12 fail‑loud guard for silent exit‑0 in `build-wiki-leaf-index.mjs`; closes [[wiki-recall-index-stale-2026-05-18]].

**DECISIONS**  
- Expand Ollama bridge to 7 read‑only tools: viz_search, wiki_lookup, read_excerpt, obsidian_lookup, dispatcher_map, semantic_search, tribal_lookup.  
- Adopt stranded html+rtk cluster from slot kilo; defer root‑doc guard until twin conflict resolved.  
- Consolidate work in dedicated slot (delta/lima) to reduce chat fragmentation and avoid cross‑chat misattribution.  
- Add R12 fail‑loud guard: post‑walk empty check, size assert, signal handlers, top‑level try/catch, env‑knob validation.  
- Defer L3 agent loop until MCP transport (POST /mcp) and Docker are stable.

**OPERATOR DIRECTIVES**  
- “Continue html and rtk upgrades; then continue with obsidian wiki and context management enhancements using obsidian, ollama, and system‑viz.”

**FINDINGS/BUGS**  
- Dashboard offload‑rate miscalculated due to side‑channel events; fixed via drift‑guard.  
- L2b MCP transport blocked at HTTP layer (POST /mcp); pivoted to bridge tools.  
- Root‑doc guard conflict: stale CLAUDE.md/MEMORY.md twins caused false‑drift; deferred.  
- False positives in test harnesses (Arm A P0/P1) resolved with entrypoint guard and isolation.  
- Silent exit‑0 regression in `build-wiki-leaf-index.mjs` under memory pressure; no output/write.  
- Parser bug for triggers frontmatter fixed on main; slot‑lima copy stale.  
- Disk space exhaustion (`C:\Users\wompu\AppData\Local\Temp`) caused tool failures; resolved after cleanup.  
- Cross‑chat misattribution: peer commit swept files; handled by recording override.

**DOMAIN SPECIFICS**  
- Engines/Actions: `dispatcher_map`, `semantic_search`, `tribal_lookup`, `obsidian_lookup`.  
- Dispatchers: MCP dispatcher (`edmDispatcher.ts`) with lazy‑loaded AI engines.  
- Metrics: offload rate, token savings, drift‑guard counts.  
- Paths: `knowledge/wiki/architecture/*`, `state/shared/unit-knowledge-packs/*`, `scripts/lib/master-index-search-lib.mjs`.  
- Wiki leaf index (`_leaf-index.jsonl`) as recall sidecar for Obsidian memory files.  
- System‑viz graph used by regen orchestrator to read wiki data.  
- Infra: Docker compose port conflict guard, launcher entrypoint, reaper‑coord-noise dashboard separation.

**TOOLS USED**  
- PRISM pipelines: `/checkin`, `/startup`, `/loop`, `/goal`.  
- Slot helpers: `chat-slots.mjs`, `stable-session-id.mjs`, `per-agent-handoff.mjs`.  
- Scripts: `build-wiki-leaf-index.mjs`, `rtk-prefix-reminder.mjs`, `dispatcher_map.js`.  
- Testing: `node:test` with hermetic mocks, real‑data E2E; `build-wiki-leaf-index-failloud.test.mjs`.  
- Docker compose for infra services.  
- Git hooks, `core.fsmonitor=false` override.

**OPEN THREADS**  
- Final commit of Iter 2 HTML adopt (root‑doc guard deferral, integration test).  
- Complete RTK coverage gaps and system‑viz enhancements.  
- Deploy L3 agent loop once MCP transport is fixed.  
- Consolidate remaining delta‑slot work into shared tree with `[MAIN]` prefix.  
- AGENT_CHAT visibility on non‑zero exit (unit pending).  
- Stage-order assertion unit; exit-code constants export.  
- `_stats.md` write protection; streaming JSONL write to avoid V8 string cap.
