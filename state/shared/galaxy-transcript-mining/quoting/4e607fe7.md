# quoting session 4e607fe7 (2026-06-06, 16.9MB, spine 55KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-GNN-SELECTIVE-DEPLOY` (b0b5b08716): proved calibration dead‑end, wired selective‑prediction eval → deploy‑ready‑selective @ τ=0.7.  
- `U-LEG-STATE-SELECTIVE-WIRE` (15eceefa24): surfaced SELECTIVE‑DEPLOY in fleet PSN banner & health digest.  
- `U-SELECTIVE-CLASS-HONESTY` (0977fea472): surfaced class concentration (spans 2/6 classes).  
- `U-CALIB-GATE-SOURCE` (cd8fcd6159): single‑sourced calibration gate constants from `GATE_THRESHOLDS`.  
- `U-GPU-STACK-PROVISION` (fb3062febb): provisioned Python 3.13 venv, torch 2.11+cu128 sm_120, bitsandbytes NF4, wired `PRISM_PYTHON_GPU_PATH`.

**DECISIONS**  
- Provision GPU stack locally (uv + CPython 3.13) to unblock MS2/MS3/MS4 instead of waiting for golf.  
- Verify torch via `gpu_health.py` gate; wire venv into fleet settings (`PRISM_PYTHON_GPU_PATH`).  
- Keep RAG fallback to Ollama nomic‑embed due to sentence‑transformers segfault.

**OPERATOR DIRECTIVES**  
- “bypass golf and do it yourself”

**FINDINGS/BUGS**  
- Calibration gate is a dead end; Murphy reliability 0.0197 of Brier.  
- Selective deploy clears both Brier and macro‑F1 at τ=0.7, but class concentration only 2/6 classes.  
- Inlined gate constants (0.15/0.55) replaced by `GATE_THRESHOLDS`.  
- Sentence‑transformers import segfaults via pyarrow native module on Windows/py3.13.

**DOMAIN SPECIFICS**  
- Engines: GNN tier‑5, LoRA trainer, RAG re‑embed, GPU retrain (GATv2/H2GCN).  
- Actions: risk‑coverage evaluation, `selectiveDeployPoint`, `gradeSelectiveDeploy`, `classifyGnn`, `formatDigest`, `legStateNnGraph`.  
- Dispatchers: `aiReasoningDispatcher`, `ModelRoutingEngine`, `OllamaCapabilityProbeEngine`.  
- Metrics: Brier, macro‑F1, AUROC, class concentration, coverage, reliability (Murphy).  
- Paths: `nn-graph-eval.mjs` harness, GPU stack at `H:/Tools/python-gpu`, `PRISM_PYTHON_GPU_PATH`.

**TOOLS USED**  
- PRISM commands: `/checkin-india`, `CronCreate`, `CronDelete`.  
- Node helpers: `chat-slots.mjs`, `audit-roadmap-drift.mjs`, `nn-graph-eval.mjs`.  
- Python tools: `uv`, `pip install`, `gpu_health.py`, `torch`, `bitsandbytes`, `sentence-transformers`, `peft`, `accelerate`, `datasets`.  
- Git utilities for commit/lock handling.

**OPEN THREADS**  
- GNN GPU retrain (MS3) toward full‑coverage deploy (requires PyG, GATv2/H2GCN).  
- LoRA trainer (MS4) using bitsandbytes NF4 on GPU stack.  
- RAG re‑embed (MS2) fallback to Ollama; fix sentence‑transformers segfault for future use.  
- Potentially pull `qwen2.5-coder:32b` into Ollama if needed.
