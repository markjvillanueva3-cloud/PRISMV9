# ai-training session 1a3b5bf6 (2026-05-22, 12.7MB, spine 47KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- **U‑AIW05** – `cb6b9fc945` – wired 3 neural engines (`NeuralDeterminismTestingEngine`, `NeuralWeightPersistenceEngine`, `DeepLogicTraceEngine`) into `prism_ai`.  
- **U‑AIW09** – `a75d27afd8` – wired 3 learning engines (`ai_transfer_learn`, `ai_continual_learn`, `ai_few_shot_learn`).  
- **P0‑U07 / U‑LEARN1** – `d915fa3be8` – added 5 document routes to `learning.ts`; all tests pass.  
- **U‑CAMAGI13 (CADCAM‑DAGI‑MS4)** – `fc4cf18ace` – built RL feedback orchestrator; 16/16 tests pass, dispatcher actions wired.

**DECISIONS**  
- Use a slot‑binding wrapper (`/checkin-lima`, `/startup-lima`) to avoid full startup audit when already in a session.  
- Prioritize high‑ROI units: close out verified units first, then wire missing engines, then build new ones.  
- Commit immediately after each unit to prevent peer‑absorption of work.  
- Fix server crashes by adding `.js` extensions to lazy imports and `with { type:"json" }` for bare JSON imports.  
- Install a supervisor (`mcp-server-supervisor.mjs`) and watchdog task; have the bridge auto‑spawn the supervisor if :3100 is down.  
- Rewrite `mcp-http-bridge.mjs` with 60 s init budget, retry logic, and self‑heal on port loss.

**OPERATOR DIRECTIVES** (verbatim)  
- “fix whatever issue is causing chat slots to exit out of the prism mcp server. we need to always be connected to it”  
- “do anything and everything possible to make sure th mcp server is always connected”

**FINDINGS/BUGS**  
- `ERR_MODULE_NOT_FOUND` – 4 lazy dispatchers imported relative modules without `.js`.  
- `ERR_IMPORT_ATTRIBUTE_MISSING` – `calculatorProgrammingCatalog.ts` imported JSON without `with { type:"json" }`.  
- Installer scripts (`install‑mcp-server-task.ps1`, `install-mcp-server-watchdog-task.ps1`) contained UTF‑8 em‑dashes that broke PowerShell 5.1 parsing, so scheduled tasks were never registered.  
- Bridge had no retry or self‑heal; any cold start >30 s caused chat disconnect.

**DOMAIN SPECIFICS**  
- Milestone envelope: `AI-WIRE-MS0.json` (12 units).  
- Dispatchers: `prism_ai`, `prism_doc_learn`, `prism_cam`.  
- Engines/actions added: `NeuralDeterminismTestingEngine`, `NeuralWeightPersistenceEngine`, `DeepLogicTraceEngine`, `ai_transfer_learn`, `ai_continual_learn`, `ai_few_shot_learn`, `ReinforcementLearningCAMFeedbackEngine`.  
- Metrics/paths: `/health` endpoint, `tsc --noEmit` compile, vitest test suites for each unit.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs` (claim/reclaim), `build-doctor`, `mcp-http-bridge.mjs`.  
- Supervisor/watchdog scripts (`mcp-server-supervisor.mjs`, `mcp-server-watchdog.mjs`).  
- PowerShell installers (`install-mcp-server-task.ps1`, `install-mcp-server-watchdog-task.ps1`).  
- Build tools: TypeScript compiler, vitest, git.  

**OPEN THREADS**  
- 3‑of‑3 scrutiny on the three MCP commits (173c562e04, 1dda943c11, 50a3bd3d80) still owed.  
- Reboot‑durability: ensure scheduled tasks are registered (already done; verify after next reboot).  
- Bridge retry logic is verified but may need additional backoff tuning for very high concurrency.  
- Continue loop to pick remaining units: defer heavy training units (`U-AITRAIN-*`), ARC-MS11 phantom envelope, and any new units added to the priority queue.
