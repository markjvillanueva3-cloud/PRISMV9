# ai-training session 7361b856 (2026-05-13, 15.4MB, spine 108KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**

- `65bdddcd2`: HS‑14 memory‑pressure auto‑relief (exit 0, BOM restored) & HS‑15 tool‑watchdog duration derivation fix.  
- `d81fc8009`: HS‑06 Phase 1: archived 85 bucket‑E “claude‑flow” skills (`commands/ → commands‑archive/`).  
- `e27f4e212`: HS‑06 Phase 2: recall hook wired to PreToolUse/PostToolUse, archived 29 bucket‑C/D skills.  
- `09a8c2bed`: DEV‑VELOCITY‑AUTOTRIGGER‑MS0 plan docs (P0–P12).  
- `2c12c0498`: `/scrutiny‑batch.md` skill file force‑added to repo.  
- `46c8805c3`: Phase A.2/3: added `/quick‑archive.md`, `/encoding‑guard.mjs` (JSONL append‑only).  
- `a6e9f043c`: `/dispatcher‑coverage.md`.  
- `1d1c70202`: `/peer‑file‑isolation.md`.  
- `04d41edf7`: `/staged‑sanity.md`.  
- `7cdf1b5eb`: `/scrutiny‑replay.md`.  
- `58aa85d75`: modified `mcp‑route-suggest.mjs` (smart classifier).  
- `475592b5a`: extended `git‑lock‑sweeper.mjs` with backoff.  
- `833007f23`: D‑series final commit: `/wire‑unwired.md`, skill‑auto‑trigger hook, stage‑22 extractor, CLAUDE.md splicer, wiring into bundles and settings.json.  
- 24 commits on `cad-fusion-live-ms0` (regex‑fix, unit builds, scripts).  
- Milestones: DEV‑VELOCITY‑AUTOTRIGGER‑MS0 (16/16), ACP‑MS0 (5/5).  
- Units shipped: 1099 fleet‑wide after regex fix; 1033 previously hidden.

**DECISIONS**

- Adopt staged delivery (Option F) for HS‑06 archive + recall.  
- Use Obsidian wiki entries to generate `_skill‑triggers.jsonl` via `stage‑22`.  
- Switch encoding‑guard to JSONL append‑only to avoid RMW race.  
- Wire git‑lock‑sweeper into bash bundle; add exponential backoff.  
- Enforce per‑file scrutiny: 2 reviewers before next file.  
- Two‑tier architecture for skill auto‑trigger: `skill-auto-trigger.mjs` + `_skill-triggers.jsonl`.  
- Classifier (`mcp-route-suggest.mjs`) suppresses false positives on git/.claude/state‑shared/scripts.  
- Prioritize devtools roadmap over revenue per user rule; `/pick-unit` deterministic.

**OPERATOR DIRECTIVES**

- “continue” after each step.  
- “run /loops until this /goal is complete.”  
- “plan for adding them to existing development slash command and pipelines so they're auto used when needed.”  
- “can we utilize obsidian and claude.md for this?”  
- “do we need to archive them? I thought we were using obsidian in some way to inject skills…”.  
- “/pick‑unit – always pick from the right roadmap, in the right order.”  
- “Close out session and close out unit tasks completed so road map stays updated.”

**FINDINGS/BUGS**

- Memory‑pressure script returned exit 1 on healthy path → fixed with explicit `exit 0`.  
- Tool‑watchdog durationMs null due to missing PreToolUse wiring → fixed.  
- Encoding‑guard had RMW race → switched to JSONL append‑only.  
- git‑lock‑sweeper dead code wired into bundles and added backoff.  
- Regex bug (`BROKEN_CHAINS`) caused 1033 shipped units to be invisible; fixed in commit `a70f1c18`.  
- Wiring index stale (24 h) – regenerated, added lazy‑import logic.  
- Drift cases: MF‑MS1 & MF‑MS2 pending manual triage; 166 new drift cases surfaced post‑fix.  
- False‑negative class for LatheAuxAxisTimingEngine resolved.

**DOMAIN SPECIFICS**

- Engines/dispatchers/hooks: PreToolUse, PostToolUse, UserPromptSubmit, `mcp-route-suggest.mjs`, `skill-auto-trigger.mjs`, `git-lock-sweeper.mjs`.  
- Actions/skills: memory‑pressure auto‑relief, tool‑watchdog, encoding‑guard, quick‑archive, big‑blob‑hunt, skill‑recall‑tune, dispatcher‑coverage, peer‑file‑isolation, staged‑sanity, scrutiny‑replay, envelope‑drift‑fix, wire‑unwired.  
- Engines: `Lathe`, `AI Reasoning Dispatcher`, `turningDispatcher`.  
- Metrics tracked: tool durationMs (HS‑15), AUROC, Brier, F1 – updated after each commit cycle.

**TOOLS USED**

- PRISM harness: `/checkin`, `/awareness-check`, `/system-viz`, `/forge`, `/rgs`, `/roadmap`, `/forge-audit`, `/milestone`, `/handoff`.  
- Scripts/hooks: `03-memory-pressure-auto-relief.ps1`, `tool-watchdog.mjs`, `encoding-guard.mjs`, `quick-archive.md`, `big-blob-hunt.md`, `skill-recall-tune.md`, `dispatcher-coverage.md`, `peer-file-isolation.md`, `staged-sanity.md`, `scrutiny-replay.md`, `envelope-drift-fix.md`, `wire-unwired.md`, `skill-auto-trigger.mjs`, `stage‑22 trigger extractor`, `CLAUDE.md splicer`.  
- Build utilities: `build-engine-index.mjs`.  
- CLI utilities: `/pick-unit`, `/checkin`, `/master-index`, `/awareness-snapshot`, `/orphan-inventory`.

**OPEN THREADS**

- Remaining 9 DEV‑VELOCITY units (quick‑archive, encoding‑guard done; big‑blob‑hunt & skill‑recall‑tune pending).  
- Install memory‑pressure scheduled task on DESKTOP‑N7MI1VB.  
- Resolve git‑tree gates: U‑GC‑01/26 forge‑orphans, U‑GC‑02 cleanup level, U‑GC‑15 Path B/C, P3 quiesce window.  
- Verify skill‑auto‑trigger integration into `/forge` pipelines and ensure trigger extraction works for all skills.  
- Next deterministic devtools pick: **AI‑MAX‑MS0/U‑AIMAX11** (AI Reasoning Skill Commands).  
- Resolve 166 drift cases surfaced by regex fix.  
- Complete remaining big‑blob cleanup candidates from `BIG-BLOB-CANDIDATES.json`.  
- Finalize wiring for any engines still flagged as unwired after index regeneration.
