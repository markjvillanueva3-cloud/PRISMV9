# ai-training session 72a2ebd6 (2026-06-03, 4.4MB, spine 26KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `[MAIN] [BOOTSTRAP-SLOT-ENFORCE] [WEDM-P2P-ACCURACY]`: 7 files, +898/-9 on `cad-fusion-live-ms0`.  
- Slot *mike* bound to `claude‑72a2ebd6`; evicted crashed `claude‑87d96286`.

**DECISIONS**  
- Use slot‑binding wrapper for `/checkin-mike` to enforce claim, pipeline, and loop state.  
- Build closed‑loop print→wire accuracy harness; focus on regression lock over the 3 raw‑G‑code programs.  
- Fix silent regression in `tech-tables.ts` (add ACU families from extracted FA‑S data).  
- Make exotic‑material fallback in `patterns.ts` fail loud to stop training poisoning.  
- Adopt adversarial workflow verification; resolve P0/P1 off‑by‑one and matcher false‑positives.

**OPERATOR DIRECTIVES**  
- `/goal [ /loop [5m] Utilize workflow to do highly comprehensive closed loop training of print to wire program utilizing all jm data we have available and our systems. check calculations and parameters relative to part thickness hardnessess and compound material cutting. /yolo-mode | goal clear: prove 100% accurracy of print to cnc programs for all wire programs in the jm system. test by reading print, writing program and posting the g code to compare to existing programs. ensure our data is optimized /yolo-mode ]`  
- `add a 13th chat slot, update everything that needs to update to intake a 13th chat`

**FINDINGS/BUGS**  
- Only **3 raw‑G‑code wire programs** in WIRE EDM; rest are binary Mastercam projects or `.MIN` lathe files.  
- Parser bug: feed regex omitted leading decimal dot → fixed.  
- Model gap: `E952_acu_7pass_thin` & `E56xx_acu_7pass_thick` never emitted (silent fallback).  
- `patterns.ts` silent fallback for exotic materials (carbide, Inconel, Ti) poisons neural training.  
- `tech-tables.ts` lost 2 ACU families; test RED → fixed by adding data from extracted FA‑S tech.  
- Divergence between `patterns.ts` and `tech-tables.ts`: different thresholds, material lists, silent vs fail‑loud.  
- Adversarial workflow identified P0 (thin‑family mapping off‑by‑one) & P1 (material matcher false positives); resolved via raw `.tech` XML.

**DOMAIN SPECIFICS**  
- Engines: `WEDMPrintToProgramEngine`, `AutoPrintToProgramBridgeEngine`, `getJMDiePatternForMaterial`, `selectECodeFamily`, `WireEDMDeepAIHardeningEngine._selectECodeFamily`.  
- Actions/dispatchers: `/checkin-mike`, `/loop`, `/goal`, `/yolo-mode`, `/system-viz-first` audit doctrine, Obsidian‑PRISM‑OS routing, CLAUDE.md+GSD+skills+hooks+RTK token savings.  
- Metrics: accuracy harness (100% regression lock over 3 programs), test coverage (168/168 green), loop state target N iter K.

**TOOLS USED**  
- PRISM tools: `chat-slots.mjs` reclaim/claim, `audit-roadmap-drift.mjs`, `checkin.md` pipeline, node scripts, `tsx/dist` engines, Vitest, 8‑agent workflow, memory protocol, wiki entry, CLAUDE.md regression entry.  
- Dispatchers/scripts/hooks: `/checkin-mike` wrapper, `/loop`, `/goal`, `/yolo-mode`, `/system-viz-first` audit doctrine, Obsidian‑PRISM‑OS routing.

**OPEN THREADS**  
- Add held‑out corpus from JM programs outside the 3 raw‑G‑code files to expose real gaps.  
- Update `patterns.ts` to emit ACU families (currently still missing).  
- Further accuracy testing with held‑out data; verify training labels for exotic materials.  
- Continue loop for next task #3: full `WEDMPrintToProgramEngine → G‑code` emission and validation.
