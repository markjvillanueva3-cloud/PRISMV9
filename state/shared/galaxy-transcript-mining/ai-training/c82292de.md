# ai-training session c82292de (2026-06-25, 39.2MB, spine 370KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `55cf3dd18d` – *U‑BPA‑LORA‑PAIRS‑WIRE* (LoRA pair builder wired into dispatchers, empty‑signal guard).  
- `4fa054801a` – Wiki entry for LoRA unit.  
- `10735ad466` – *U‑QP‑SIMILAR‑JOB‑RETRIEVE* (kNN quoting retriever, dispatcher wiring, 19/19 tests).  
- `d5ff9fdf90` – *U‑GNN‑VAULT‑WIRE‑LABELS* (11 new GNN ref‑pool labels).  
- `5114e25fc0` – *U‑GNN‑VAULT‑LABELS‑HANDOFF*.  
- `d90e92c530` – *U‑NNGRAPH‑WARN‑ROOTCAUSE* (fleet WARN root‑cause fix).  
- `4b0aa55769` – *U‑GNN‑FEEDER‑SPECULATIVE‑WARN* (fail‑loud on SPECULATIVE_RE drop).  
- `b025c6ee11` – *U‑GNN‑RECOVERED‑LABELS* (2 recovered ground‑truth labels).  
- `3b8ff98b2c` – *U‑GNN‑FEEDER‑WARN‑SIGNAL‑CLEAN*.  
- `80b36e5358` – *U‑BPA‑CONSUMER‑STATE‑ISOLATE* (idempotent consumer state).  
- `2f163d92df` – *U‑BPA‑CONSUMER‑DOCFRESH*.  
- `da9f7cc3cd` – *U‑BPA‑LOOP‑DRAIN‑CORE* (injectable drain core, P1 fix).  
- `179ca3d723` – *U‑BPA‑DRAIN‑NOOP‑COUNT* (corrected noop dispatch count).  
- `1f7d03f33d` – *U‑BPA‑LOOP‑DRAIN‑DISPATCH*.  
- `5bbaac1503` – *U‑CONSENSUS‑PERF‑PERSIST* (saveState & recordOutcomeAndPersist).  
- `1d8afe22b7` – *P2‑FIX* (temp‑file leak cleanup).  

**DECISIONS**  
- De‑orphan LoRA pair builder; make it optional ledger source.  
- Add `empty:true` flag for zero‑pair cases in both dispatchers (`prism_cad`, `prism_ai`).  
- Wire pure kNN wrapper into `prism_quoting`.  
- Checkpoint at YELLOW, rely on auto‑compact (manual `/compact` broken).  
- Stop building at YELLOW; wait for auto‑compact or operator reset.  
- After reset, resume GNN ref‑pool labeling to reach macro‑F1 ≥ 0.55 gate.  
- Operator must greenlight `nn‑graph‑retrain‑lifecycle.mjs --force` (GPU retrain).  
- Defer dispatcher case for drain core until clean YELLOW checkpoint to avoid 5 h quota.  
- Persist consensus performance via in‑process API; no dispatcher action.  
- Commit units atomically with by‑pathspec staging.

**OPERATOR DIRECTIVES**  
- `/checkin-india /loop /goal` – start India loop.  
- Greenlight GNN full‑coverage retrain (GPU stratified).  
- Coordinate quoting corpus loader with Charlie/Jüliett.  
- `/clear` or `/compact` – reset context before next unit.  
- After reset, handoff resumes India’s GNN labeling work automatically.  
- `nn‑graph‑retrain‑lifecycle.mjs --force` – apply all 27 vault labels and re‑grade macro‑F1.  
- `node scripts/capture-claude-credentials.mjs account-N`.  
- `node scripts/arm-account-switch.mjs --auto`.  
- `node scripts/fleet-survival-status.mjs` (check 5 h zone).  
- Execute Echo overnight build.

**FINDINGS / BUGS**  
- Self‑compact YELLOW branch never actuates (`self-compact-fix-u-yellow-branch`).  
- `ReinforcementLearningCAMFeedbackEngine.ts` TS2554 signature mismatch.  
- Feeder `vault-to-gnn-refpool.mjs` dry‑run increased labels 14→25 (+11).  
- SPECULATIVE_RE filter dropped two true wired lines; fixed by scrubbing words.  
- Cross‑feeder conflict resolved – new labels disjoint from outcome feeder (141 entries).  
- Silent drop of SPECULATIVE_RE lines in GNN feeder; added fail‑loud warning.  
- Recovered two ground‑truth labels (`prism_calc`, `prism_session`).  
- Self‑compact deadlock observed; awaiting operator intervention.

**DOMAIN SPECIFICS**  
- **Blueprint LoRA**: `blueprint_lora_prepare_set`, `buildLoRAPairsFromLedger`.  
- **Blueprint RAG**: `cadDispatcher.ts` recordOutcome wiring, `prism_quoting` dispatcher.  
- **Cad‑learning**: `cad_learning_*` actions wired and loop closed.  
- **GNN ref‑pool labeling**: current F1 = 0.439; target macro‑F1 ≥ 0.55 gate.  
- **Engines/Dispatchers**: `vault-to-gnn-refpool.mjs`, `blueprint_rag_extract` (recordOutcome), `buildLoRAPairsFromLedger`, `aiReasoningDispatcher`, `prism_ai`, `cadDispatcher`.  
- **Paths**: `state/shared/nn-graph/active-label-worklist.{json,md}`, `state/shared/blueprint-accuracy-events.jsonl`, `/checkin-india` pipeline, handoff topic `india-work`, dispatcher case `blueprint_loop_drain`.  
- **Metrics**: AUROC 0.846; macro‑F1 gate 0.55 pending ref‑pool growth; context YELLOW > 0.71; auto‑compact ~ 0.95.

**TOOLS USED**  
- Sonnet, Ollama coder ensemble (`ask-ollama.mjs` – qwen3‑coder, qwen2.5‑coder), Hermes (`ask-hermes.mjs`).  
- Octopus consensus (`prism_ai:consensus_decide`).  
- Opus for safety design.  
- Node scripts: `audit-unwired-engines.mjs`, `self-compact.mjs`, `fleet-survival-status.mjs`, `capture-claude-credentials.mjs`, `arm-account-switch.mjs`, `vault-health.mjs`, `system-viz-query.mjs`, `nn‑graph‑retrain‑lifecycle.mjs`.  
- PRISM components: LoRA, RAG, PSN, deep‑learning/reasoning stack.  
- Testing/Build: `vitest`, `tsc`.

**OPEN THREADS**  
- Operator‑gated GNN GPU retrain (next unit).  
- Cross‑lane corpus loader / featurizer for quoting retriever.  
- Resolve self‑compact YELLOW bug to enable automatic checkpointing.  
- Design decision on RL `step()` signature and outcome mapping.  
- Reset context (`/clear` or `/compact`) before next build cycle.  
- Further labeling after retrain – target remaining false‑negatives in starved classes.  
- Coordination with Bravo for U‑NN‑TIER05 / XPROC‑NEURAL‑OPTIMIZE‑MS0.  
- Echo overnight build pending execution.  
- Sierra backlog: vault‑ops hardening, system‑viz graph/ghost‑roost/master‑index/node‑cards.  
- Resolve self‑compact deadlock (operator action).  
- Continue India loop after context reset.  
- Deeper scan for engines lacking dispatcher or in‑process caller.  
- Align `blueprint_accuracy_guard.mjs` hook event shape to consumer‑lib contract.  
- Wire `cadDispatcher` recordOutcome IO for `blueprint_rag_extract` to shared `blueprint-accuracy-events.jsonl`.  
- Coordinate with CAM/kilo domain for `MachineLearningStrategyRankerEngine`.
