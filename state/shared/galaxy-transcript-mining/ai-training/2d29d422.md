# ai-training session 2d29d422 (2026-05-26, 55.1MB, spine 147KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-COV-01 ChainOfVerificationEngine` – substrate primitive (commit 834145ad9a)  
- `U-COV-QUOTING + ActiveFactorLoaderEngine + Health UI` – calibration runtime, dispatcher actions, UI page (commit afe76af0a2)  
- 13‑axis audit spec & `estimateCalibrated` wire (commit 78e8e27a7c)  
- Lead‑time, secondary‑ops, tolerance‑pricing, cross‑part synergy engines + dispatcher actions (commit 4122176561)  
- Tesseract OCR bridge & freight cost engine (commit f407c6d527)  
- `QuoteOutcomePSIDeltaBridge` – NN/GNN retraining trigger (commit 7030bfae9a)  
- `QuotingWorkbenchPage.tsx` – mobile‑first end‑to‑end quoting UI (commit d050b3ecab)  
- `McMasterCarrAPIAdapter`, stress‑test, and `DocustrataHistoricalPricingTrainerEngine` stub (commit 3b82ce312c)  
- `U-QP-TRAINING-ORCHESTRATOR` engine (`runOnce()`) – keeps training loop alive (commit 3d7535feed)  
- Scripts: `quoting-train-cycle.mjs`, `quoting-baseline-bootstrap.mjs`  
- Iter‑9: clean customer extraction, regex guard (commit 5b370300f0)  
- Iter‑10: JSONL drift ledger (`buildLedgerRow`) (commit acee69cad3)  
- Iter‑11: rolling‑window summarizer (`summarizeLedger`, NIST percentile fix) (commit bd3ad1ffc7)  
- Iter‑12: 4‑tier alert classifier (`detectDriftAlert`) (commit b1c6a096ff)  
- Iter‑13: per‑record signal variance (`deriveRecordDefaults`) (commit 71e08eae58)  
- Iter‑14: wiki entry for quoting training pipeline (commit 88f6f975ae)  
- Iter‑15: drift‑alert state file (`latest-drift-alert.json`) (commit 4f00ed1473)  
- Iter‑16: bootstrap distribution probe (`--summary` flag) (commit 15b09088ad)  
- Iter‑17: round‑trip E2E composition test (commit 3de92ef087)  
- Iter‑18: Docustrata bridge shim (`mergeDocustrataRevenue`) (commit 3820f1ed4f)  
- Iter‑19: Docustrata payload validator (`validateDocustrataPayload`) (commit 2d4e2cfa3e)  
- Iter‑20: synthetic revenue generator (`generateSyntheticRevenueRecords`) (commit d9f727aa06)  
- Iter‑21: pipeline orchestrator (`runDocustrataPipeline`) (commit cb52c38aee)  
- Iter‑23: pipeline‑verify health check script (commit f464588376)  
- Iter‑24: wiki addendum (iter15‑23 sections) (commit 78a1f41f57)  
- Iter‑25: operator runbook (`PIPELINE-RUNBOOK.md`) (commit f7829ece9f)  
- Iter‑26: Windows Scheduled Task installer (`install-quoting-pipeline-cron.ps1`) and tests (commit 7bc1c940e3)  
- Iter‑27: sample fixture (`docustrata-revenues.sample.json`) + contract tests (commit 0158f14138)  
- Iter‑28: SessionStart alert banner formatter (`formatAlertBanner`) (commit d74521aa4c)  
- Iter‑29: Docustrata extractor spec blueprint (commit 84b5ed57a9)  
- Iter‑31: full‑chain smoke test (every pure export exercised) (commit 3d3ca77553)  
- Iter‑32: verify script catches silent exclusions (cron‑install tests) (commit 211ab8e1f3)  
- Iter‑33: live‑run evidence + staleness finding (baseline pre‑iter13) (commit 67e1b53aa4)  
- Iter‑34: regenerated baseline + new findings (regex anchoring, layout mismatch) (commit 8235c3c725)  
- Iter‑35: regex extension for noise patterns (`PRISM MODIFIED POST PROCESSORS`, etc.) (commit 848e0107ab)  
- Iter‑36: JM Die layout audit (structural finding `{MACHINE}/{CUSTOMER}`) (commit eafec0ccb9)  
- Iter‑37: machine‑customer filter fixes (real customers surface) (commit 491ed8602c)  
- Iter‑38: real‑customer chain evidence (override spread 2.67×) (commit a38d790324)  
- Iter‑39: balanced sampling → 3‑way machine variance, 5.83× pricing spread (commit 4f6a1c92fc)  
- Iter‑40: numbered‑prefix filter (closes iter39 R12 finding) (commit ae75d99e9b)  
- Iter‑42: extractor adapter implementation (reads curated JSON, maps to validator shape; CLI `--source extractor`) (commit ?).

**DECISIONS**  
- Adopt substrate‑first ChainOfVerificationEngine for all domain engines.  
- Yolo mode overnight training via separate orchestrator engine + cron script.  
- Ledger‑based drift tracking (JSONL) with 4‑tier alerting exposed to PSN.  
- Import‑guard in new modules; pre‑commit empty‑index check.  
- Keep `DocustrataHistoricalPricingTrainerEngine` stubbed until real invoice data wired.  
- Closed‑loop pipeline: bootstrap → train cycle → ledger → summarizer → alert → state file.  
- Per‑record variance injection (`deriveRecordDefaults`) to avoid degenerate training data.  
- Synthetic revenue generator for pre‑production testing; real‑customer bridge via Docustrata shim.  
- Single‑command orchestrator (`runDocustrataPipeline`).  
- Cron installer + health check script guarantee unattended nightly runs.

**OPERATOR DIRECTIVES**  
- “Continue all remaining quoting units and keep training the system.”  
- “Operator is asleep — yolo mode, no questions, build the next unit, commit, tick, continue.” (repeated at each cron fire).

**FINDINGS/BUGS**  
- Percentile calculation for p95 fixed to NIST nearest‑rank formula.  
- Customer‑extraction regex extended to capture `POST PROCESSORS`, `_PART LIBRARY`.  
- Peer‑slot absorption mitigated with pre‑commit empty‑index check and chat‑bus notice.  
- Baseline staleness: `baseline-records.json` pre‑iter13 flat defaults; regenerated after iter13 variance.  
- Regex anchoring missed `PRISM MODIFIED POST PROCESSORS`, `HURCO CNC PROGRAMS`; fixed in iter35.  
- JM Die layout mismatch `{CUSTOMER}/{MACHINE}` inverted; audit (iter36) and filter update (iter37).  
- Math.round‑before‑compare boundary bug in alert staleness detection fixed in iter28.  
- Cron‑install tests silently excluded due to verify script regex bug; fixed in iter32.  
- Synthetic‑to‑real variance gap: real baseline spread 2.67× vs 1.32×; addressed by balanced sampling (iter39).  
- Live run on pre‑iter13 data revealed missing `machine_class` field and stale customer list (iter33).

**DOMAIN SPECIFICS**  
- **Engines/Actions**: QuotingCalibrationEngine, ActiveFactorLoaderEngine, LeadTimePricingTier, SecondaryOpsQuotePricingEngine, TolerancePricingImpactEngine, CrossPartToolingSynergyEngine, MachineInvestmentROIEngine, DynamicShopRateEngine, McMasterCarrAPIAdapter, DocustrataHistoricalPricingTrainerEngine, U-QP-TRAINING-ORCHESTRATOR, bootstrap script (`quoting-baseline-bootstrap.mjs`), ledger writer, summarizer, alert classifier, state emitter, banner formatter.  
- **Dispatchers**: 27 `prism_quoting` actions (e.g., `quoting_calibration_derive_with_cov`, `quoting_active_factor_apply`, `wizard_to_quote`, `print_to_program_to_quote`, `speed_feed_to_cycle`, `dynamic_shop_rate`).  
- **Metrics/Paths**: Calibration factor surface (`state/shared/calibration/quoting-calibration-active.json`), drift ledger (`train-cycle-history.jsonl`), alert JSON (`drift-alerts.json`), latest‑drift‑alert (`latest-drift-alert.json`), distribution histograms via `--summary`.  
- **PSN Legs**: Obsidian (memory, wiki), PRISM OS (dispatcher), Tribal (tips), System Viz (auto‑pickup), NN/GNN (psi_delta feed).  

**TOOLS USED**  
- PRISM CLI helpers (`chat-slots.mjs`, `checkin.md` pipeline).  
- Node.js ESM modules, Vitest for unit tests.  
- CronCreate/cron expressions for recurring training loop; PowerShell scheduled task installer (`install-quoting-pipeline-cron.ps1`).  
- Git hooks: `feedback_commit_to_slot_worktree`, `feedback_commit_prefix_main_on_shared_tree`.  
- Session memory + Obsidian vault integration.  

**OPEN THREADS**  
1. Final Docustrata extractor implementation (iter42) and wiring to pipeline; bridge real invoice extraction.  
2. Dispatcher wire – peer‑contended file area still pending code.  
3. Full production baseline regeneration after Docustrata extractor lands, using iter13 variance.  
4. PSN integration of drift alerts – expose `detectDriftAlert` output to System Viz / PRISM OS dashboards.  
5. Optional: Machine investment ROI & dynamic shop rate UI surface exposure in main workflow.  
6. Documentation & wiki updates for all new engines/actions (pending after absorption incidents).
