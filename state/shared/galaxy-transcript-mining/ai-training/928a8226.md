# ai-training session 928a8226 (2026-06-10, 68.1MB, spine 675KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hookify rule‑file errors fixed (110 malformed `.local.md` → YAML frontmatter; 28 non‑ASCII sanitized; backup in `.claude/.hookify-backup-*`).  
- Dangling hook references removed from `settings.json`; mirrored to `H:/prism/.claude/settings.json`.  
- Unwired engines audit: 90 built but not wired; audit data stored in `reference_unwired_engine_gap_audit_2026_06_08.md`.  
- `c593b096fb` – Ollama pull + synergy fix (120 B model wired, GPU residency verified).  
- `13017de764` – Hookify front‑matter & dangling hook‑ref cleanup.  
- `29f2d177f6` – System‑wide gap‑map artifact `SYSTEM-SYNERGY-GAPMAP-2026-06-08.md`.  
- `8550436c8a` – Fixed phantom Qdrant collection reference in CAG‑router.  
- `c9fe32d8b2` – Embedded‑index OOM & nomic context window bug fix (heap bump + 2000‑char clamp).  
- `182788232a` – Added empty‑text skip guard to embedder driver; fail‑loud guard to loadIndex, shrink‑guard on writeIndex.  
- `a3e6d3ca97` – Restored brain to ~10 k entries (fail‑loud guard, shrink‑guard).  
- `5467482be4`, `c994bf3aad` – Documentation updates: CLAUDE.md regression log, wiki lesson, gap‑map revision.  
- `fb314a6fd1` – Docker‑service‑health CLI + 5/5 tests; guard detects down/created/exited services.  
- `14b8b383a2` – Wired singleton‑service‑guard into Stop hook chain (docker-service-health-stop).  
- `006c435ddb` – Corrected connectivity hook restart command (`TRANSPORT=http`).  
- `e2081e0780` – Extended singleton‑service‑guard to reap + start MCP daemon.  
- `d22681f5d2` – Cosmetic tag fix; U‑MCP‑FIXSTART (reap‑then‑start recovery for MCP).  
- `bee977a004` – Added `NODE_OPTIONS=--max-old-space-size=8192` to avoid OOM on large index.  
- `U-MCP-CMDMATCH-FIX` (`ed6662f45e`) – Slash‑agnostic regex for singleton‑service‑guard daemon detection.  
- `U-HOOK-UNREG-PROTOCOL-FIX` (`29fb555f13`) – JSON‑stdout protocol for `stop_on_hook_unregistration`.  
- `U-MCP-BOOT-HEAP-FLOOR` (`6ca2f6afd2`) – Heap‑floor helper applied to all MCP spawn paths.  
- `4141daf9d8` – Fleet‑task‑health migration‑freeze marker (cry‑wolf fix).  
- `698cf12ad2` – Static list of 4 frozen tasks (superseded by marker).  
- `U-FTH-WIKI-LESSON` – Wiki lesson for fleet‑task‑health cry‑wolf fix.  
- `QUOTING-SYNERGY-MS0/U-QP-COST-BRIDGE-WIRING-TRUTH` – Router‑based cost‑bridge wiring verified (no double‑execution).  
- `U-QP-COST-SAVINGS-TEST` – CostSavingsTrackerEngine testable via injectable store path; 13 vitest cases.  
- `OLLAMA-MODEL-DEFAULTS-CORRECT` – Fixed dispatcher defaults to installed Blackwell models.  
- `U-BW-DISPATCHER-SCAN` – Cascade defaults repointed to installed Blackwell tiers; source‑lock extended.  
- `U-BW-DISPATCHER-SCAN-WIKI` – Wiki lesson documenting guard blind spot.  
- `U-PSN-SNAPSHOT-REFRESH` – 6‑day stale PSN snapshot regenerated.  
- `U-QP-DIGITALTWIN-TEST` – Reference‑value test for DigitalTwinEstimator.  
- `U-QP-MARGIN-FLOOR-GATE` – Margin‑floor guard added to QuoteEstimatorEngine (caller‑override + default).  
- `U-QP-MARGIN-FLOOR-SHOPCONFIG` – Floor sourced from ShopConfigurationEngine.  
- `U-QP-NRE-AMORTIZE-TEST-FIX` – Stale NRE amortization test corrected.  
- Dispatcher round‑trip test for margin‑floor flag (`ec597dbcb3`, 7/7 tests).  
- Frontend surfacing: added `pricing.below_margin_floor` alert banner; extended QuoteEstimate type; two R9 tests.  
- `4b0980c56a` – AI‑substrate routing matrix coverage 5/5 (27/27 tests).  
- `62b114cb7b` – Web‑test ResizeObserver polyfill; fixed quote‑pages test.  
- `44fab1be37` – Fleet‑synergy dispatch map rows 8–15 assigned to owning slots.  
- `U-QP-CLOSED-LOOP-PROVENANCE-GATE` (`4c12a75a8d`) – Synthetic/placeholder outcomes blocked from promoting calibration factors.  
- `U-QP-OUTBOUND-PROMOTE-GATE` (`d294957c4d`) – Real outbound sold‑price distribution alignment, fail‑closed.  
- `U-QP-CALIBRATION-FRESHNESS-PREFLIGHT` (`bf10035ec0`) – Warn on >24h stale factor; hard cutoff → raw FMV.  
- Built type‑adapter between OCR output (`BlueprintAnalysis`) and bridge engine’s expected shape; implemented `fromOCRAnalysis()` & `bridgeFromOCR()`.  
- Wired adapters into businessDispatcher (line 3072) and shopDispatcher (lines 1479‑1481).  
- Added hermetic round‑trip tests with real OCR fixtures.

**DECISIONS**  
- Do not wire all unwired engines blindly; triage per galaxy owner.  
- Academy tribal routing fallback to `general` is intentional.  
- For golf slot check‑in, use session‑scoped bypass (`PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1`) unless global hook disable required.  
- Fix encoding corruption in `ollama-resilient-pull.ps1`; resume download of `gpt‑oss:120b`.  
- Use local GPU (RTX 6000 Blackwell) for Ollama; set `OLLAMA_MAX_LOADED_MODELS=3×GPU`.  
- Batch workflow orchestration to 3 concurrent subagents to avoid Anthropic rate limits.  
- Replace string‑based index load with buffered, streaming JSON reader for tribal‑rerank and embedder.  
- Commit a system‑wide gap‑map as shared work‑order; let each slot own remaining gaps.  
- Keep fleet reaper & monitors running; rely on scheduled task + in‑session monitor.  
- Focus on golf‑lane infra/health surfaces; avoid fresh‑budget architectural builds.  
- Make `cmdMatch` regex slash‑agnostic to match real daemon command lines.  
- Add shared `ensureHeapFloor` helper and wire into all MCP spawn paths.  
- Replace stop‑hook protocol with JSON‑stdout so bundle can evaluate allow/block.  
- Defer Qdrant tribal migration because existing path already exists; risk of duplicate store or unstable data.  
- Partition deliberately‑disabled tasks in fleet-task-health-watch to silence only intentional freezes, preserving real degraded signals.  
- Cascade routing must use installed Blackwell models; guard blind spot fixed by extending source‑lock to dispatcher tree.  
- Margin‑floor gate must be caller‑overridable and sourced from canonical ShopConfig; no inline constants.  
- Dispatcher round‑trip test required for R15 compliance; added.  
- Frontend must expose `pricing.below_margin_floor` via alert banner; implemented.  
- Build type‑adapter between OCR output and bridge engine shape.  
- Implement `fromOCRAnalysis()` & `bridgeFromOCR()`.  
- Wire adapters into businessDispatcher (line 3072) and shopDispatcher (lines 1479‑1481).  
- Add hermetic round‑trip tests with real OCR fixtures.

**OPERATOR DIRECTIVES**  
- Export `PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1` for current session.  
- Edit `C:/Users/wompu/.claude/settings.json` to remove PreToolUse entry referencing `golf-slot-write-allowlist.mjs`.  
- Run `ollama-resilient-pull.ps1` in background: `powershell -ExecutionPolicy Bypass -File ollama-resilient-pull.ps1`.  
- Claim golf slot for check‑in:  
  ```bash
  node H:/prism/.claude/helpers/chat-slots.mjs reclaim
  node H:/prism/.claude/helpers/chat-slots.mjs claim \
    --chatId "$STABLE" --branch "$BRANCH" --topic "golf-work" \
    --activity "checkin" --preferSlot golf --force true --confirmRecent true
  ```  
- Fix all hook errors (front‑matter, dangling refs).  
- Identify and close bugs/gaps that limit system potential.  
- Ensure fleet reaper and monitors are active.  
- Eliminate rate‑limiting; improve efficiency without sacrificing quality.  
- Use ultracode for planning, deep reasoning, orchestrating; use Ollama local LLM for grunt work (searches, reads, coding).  
- Goal: finish closed‑loop learning and make quoting system production‑ready for JM Die.

**FINDINGS/BUGS**  
- Hookify rule-file errors: missing YAML frontmatter & charmap decode error → fixed.  
- Dangling hook references caused “Cannot find module” → removed.  
- 90 engines unwired; some intentional orphans (e.g., `MillPrintToProgramEngine`).  
- Academy tribal routing fallback design; not a bug.  
- `ollama-resilient-pull.ps1` had UTF‑8 em‑dash corruption preventing download of `gpt‑oss:120b`; fixed.  
- Gpt‑oss 120 B produced empty `.response` due to harmony format & small `num_predict`; fixed by setting default 1024 tokens.  
- Subagent workflow triggered Anthropic rate limit; resolved by batching to 3 agents.  
- Tribal‑embed index (~533 MB) exceeded V8 string length → OOM; replaced with buffered loader.  
- Nomic embedder exceeded context window on large files; added 2000‑char clamp.  
- Empty‑text wiki pages caused batch failures; added skip guard.  
- PowerShell 5.1 parse error due to em‑dashes in `ollama-resilient-pull.ps1`.  
- MCP server boot OOM due to inherited 384 MB heap cap; required ≥4096 MB for cold start.  
- `/system‑viz find` OOM at ~390 MB because of same 384 MB cap; self‑bump would double hot‑path spawns (contradicted).  
- fleet-task-health-watch warning was cry‑wolf: all degraded tasks were intentional migration freezes, not real failures.  
- Qdrant tribal migration duplicated existing path and risked building on unstable corpus.  
- `cmdMatch` regex used backslashes only → missed real daemon; fixed.  
- `stop_on_hook_unregistration` emitted no JSON → bundle marked NOT evaluated; silent no‑op.  
- Pre-existing shopDispatcher TS errors (10 remain, 2 removed).  
- OCR `$1` noise issue remains operator‑side; not addressed here.

**DOMAIN SPECIFICS**  
- Hookify rule files: `.claude/hookify.*.local.md`.  
- Settings file: `C:/Users/wompu/.claude/settings.json`; mirrored to `H:/prism/.claude/settings.json`.  
- Unwired engines audit script: `node scripts/audit-unwired-engines.mjs`; audit data in `reference_unwired_engine_gap_audit_2026_06_08.md`.  
- Golf slot check‑in uses `chat-slots.mjs` with branch, topic “golf-work”, activity “checkin”.  
- Ollama models stored in `H:/Tools/ollama/models`; resilient pull script at `ollama-resilient-pull.ps1`.  
- Qdrant collections: `prism_engines`, `prism_skills`, `prism_formulas`; PSN leg #5 uses tribal injection.  
- Tribal knowledge index `tribal-embed-index.json` (768‑dim embeddings, 160 MB JSON); V8 string cap 512 MiB.  
- MCP daemon HTTP server on port 3100; started with `TRANSPORT=http node dist/index.js`; helper `mcp-server-daemon.mjs`.  
- Docker services: `prism-qdrant`, other Prism containers.  
- Ollama GPU models: gpt‑oss:120b (65 GB), qwen2.5-coder:32b, vision ensemble (`qwen3-vl`, `qwen2.5vl`, `llama3.2-vision`, `moondream`).  
- Cost‑bridge router `cost-bridge-dispatch.mjs`; quoting engines `QuoteEstimatorEngine`, `CostSavingsTrackerEngine`.  
- ShopConfigurationEngine provides margin_floor_pct.  
- Frontend alert banner `pricing.below_margin_floor`.  
- OCR bridge adapter: `BlueprintOCREngine` → `QuotingClosedLoopRunnerEngine`.

**TOOLS USED**  
- Hookify plugin parser (`config_loader.py`).  
- Node scripts: `audit-unwired-engines.mjs`, `fleet-reaper-sweep.mjs`, `singleton-service-guard.mjs`, `mcp-server-daemon.mjs`, `docker-service-health-check.mjs`.  
- PowerShell script: `ollama-resilient-pull.ps1`.  
- Chat‑slot helper: `chat-slots.mjs`.  
- PRISM scripts/skills/hooks: `scripts/lib/load-tribal-index.mjs`, `embed-wiki-into-tribal-index.mjs`, `docker-service-health-check.mjs`, `singleton-service-guard.mjs`, `mcp-connectivity-check.mjs`.  
- Testing frameworks: Jest/Vitest, Node.js test scripts.  
- Ultracode workflow (`wf_8da40cea‑18a`).  
- TypeScript compiler (full‑heap run).  
- Git for commits.

**OPEN THREADS**  
- Triage unwired engines per galaxy owner; decide which to wire, exempt, or archive.  
- Validate that after download completes, routing logic selects `gpt‑oss:120b` for best‑tier categories (`search_synthesis`, etc.).  
- Ensure golf write‑allowlist bypass applied and fleet‑reaper runs during `/checkin-golf`.  
- Confirm PSN snapshot stale issue resolved; verify real under‑wiring.  
- Verify margin‑floor gate works end‑to‑end (engine → dispatcher → UI).  
- Final implementation of `U-QP-BLUEPRINT-OCR-BRIDGE-ADAPTER` (adapter code, dispatcher rewiring, tests); commit & 3‑of‑3 review.  
- Post‑commit handoff for next charlie unit (operator‑side data cleanup or further USE‑layer work).  
- Verify all other dispatchers free of retired‑tag defaults (source‑lock now covers them).  
- Run full quoting test suite after adding `CostSavingsTrackerEngine` tests to confirm 100 % coverage.  
- Monitor future migration freezes for new operator‑created markers; ensure flag removal logic is robust.  
- Broader guard to catch default pointing at never‑pulled models (un‑retired).  
- Offload re‑measure after cascade fix to confirm <30 % take‑rate target.  
- Monitor routing invariant for Ollama vs Claude in quoting reasoning bridges if new classify tasks appear.  
- Remaining 9 quoting‑frontend failures in hotel domain not addressed; schedule fixes.  
- Fleet‑scope synergy goal rows 8–15 need dispatch to owning slots.  
- Data integration for accounting (ERP/QuickBooks) remains blocked; no code unit to ship.
