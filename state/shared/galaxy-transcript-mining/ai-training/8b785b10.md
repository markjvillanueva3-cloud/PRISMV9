# ai-training session 8b785b10 (2026-06-04, 29.9MB, spine 186KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `9bd4b22abd`: `.gitattributes` eol=lf (CRLF/LF normalisation)  
- `24478d31aa`: removed 675 MB `state/shared/system‑viz/system-graph.json` (largest conflict surface)  
- `606424dc12`: orphan‐`.git/index.lock` self‑heal + PreToolUse Bash hook clears young orphans before any git command  
- `e2cdbe2e86`: `ollama-cost-router.mjs` extended to 32 b “best” tier on Blackwell  
- `ae2fbfdff8`: host‑aware synthesis model resolver (`resolveSynthesisModel`) picks highest installed tier per category  
- `c4eb71913d`: wired `ask-ollama.mjs` to use the resolver, defaults to `qwen2.5-coder:32b` (removed hardcoded 3 b/7 b)  
- `049e981158`: added 70 B “best‑tier” ceiling (`gpt-oss:120b`, `qwen2.5:72b`, etc.) and updated tier prefs  
- `74077e38cb`: model‑upgrade retired 4 small LLMs; set `qwen2.5-coder 32b` floor; gated install of `gpt‑oss 120b`  
- `318d0c062b`: Hermes 7‑unit build plan + dead‑offloader config fix (`ollama-route-config.json`)  
- `f0e72dd6e0`: U1 keystone: `local‑llm‑task‑router.mjs`, two‑reviewer hardened, real‑composition tests  
- `28c56cd437`: wired Gemma 4 31b into cost‑router best tier; octopus auto‑promotes as secondary voice  
- `0615b476d5`: retired deleted tags in `OllamaHookBridgeEngine.ts`  
- `fc9038ca2a`: removed 11 stale model entries from `.ts` registries (`ModelRoutingEngine`, `OllamaTaskOffloaderEngine`, etc.)  
- `8e2b2500c6`: guard widened to catch `(`/`[` patterns; fixed live default in `OllamaContextFloorEngine.ts`; added self‑proving test  
- Added `multi-provider-router.mjs`, standalone Hermes local backend, dynamic‑workflow emitter, `ollama-route-pretooluse.mjs` (mode:"auto" but defaults to suggest), and `local-llm-task-router.mjs`

**DECISIONS**  
- Pull `gpt‑oss:120b` via Ollama; re‑point small‑model defaults to `qwen2.5-coder:32b`; delete 3 b/7 b/14 b models  
- Keep xray multi‑VLM ensemble (llama3.2‑vision, qwen3‑vl) for OCR  
- Decline Cloud Kimi (paid API, data leakage risk)  
- Continue using alpha slot (token‑optimization); main‑tree write blocked unless `[MAIN]` prefix  
- All routing on local Ollama; delete weak LLMs  
- `qwen2.5-coder 32b` as installed floor; `gpt‑oss 120b/20b` gated for best/strong tiers  
- Gemma 4 31b becomes best tier once pulled → hybrid octopus (text+vision)  
- Build Hermes efficiency router as single keystone engine  
- Split architecture: Bravo = unified Hermes router; Alpha = execution layer with `local‑llm-task-router.mjs`  
- Flip `ollama-route-pretooluse` mode from `"suggest"` to `"auto"`  
- Octopus combo: `qwen2.5-coder 32b + gemma4 31b`; vision models excluded by `isVisionOllamaModel`

**OPERATOR DIRECTIVES**  
- Run `/compact` precompact handoff; resume with new LLM plan  
- Ensure background workflow for bulk re‑point edits completes; if stalled, restart streamed pull (≤30 min)  
- Generate Hermes skills/scripts/hooks slash‑command pipelines; centralise on Ollama/local LLMs  
- Include reviewer agent for gap‑fill output  
- Set recurring `/loop 5m /goal …` and session‑scoped Stop hook until goal holds  
- Confirm boundary, dedup check, retirement findings posted to bravo  
- Enable auto‑mode for `ollama-route-pretooluse`  
- Run octopus combo demo (`qwen2.5-coder + gemma4`)  
- Verify offloader works after warming Ollama; resolve cold‑start cascade_unreachable

**FINDINGS/BUGS**  
- orphan‐`.git/index.lock` self‑heal works; PreToolUse hook clears young orphans  
- `ask-ollama` now uses resolver, defaults to `qwen2.5-coder:32b`  
- TSC “unused import” warnings on `.mjs` files are false positives (checkJs)  
- Cloud Kimi not free → cannot use  
- Stale deleted‑model tags in `.ts` engines fixed (11 files)  
- Guard now scans `data/state` and `mcp-server/src/engines`  
- `ollama-route-config.json` corrected to reference active tag; live default in `OllamaContextFloorEngine.ts` fixed  
- Tool calls cannot be routed through Ollama without quality loss; token savings from offloading subtasks (~12–14% → target 30+%)  
- Auto‑invoke only suggests; take‑rate ~0.8%; 17 skills have mandatory “INVOKE_NOW” directive  
- Layer‑2 hooks nudge but not execute; model ignores advisory text  
- Offloader bug: cold start causes `cascade_unreachable` → pass due to timeout; warm run reroutes correctly  
- Retirement findings: `ollama-deepseek` deleted; `deepseek-r1` may breach no‑cloud policy  
- Octopus combo logic incorrectly claimed vision usage; corrected to exclude VLMs

**DOMAIN SPECIFICS**  
- Slot‑worktree model: alpha bound to `slot/alpha`; main-tree write blocked unless `[MAIN]` prefix  
- `chat-slots.mjs` for claiming/reclaiming slots  
- `git-commit-mutex.mjs` provides advisory lock, ref‑race retry, pathspec commits  
- Ollama cost router & resolver: `ollama-cost-router.mjs`, `host-aware-synthesis-model.mjs`  
- PreCompact handoff writes single‑shot resume directive  
- Engines: `local‑llm‑task‑router.mjs`, `OllamaHookBridgeEngine.ts`, `AISystemRouterEngine`, `MultiProviderRouter.mjs`, `MultiModelConsensusEngine.ts`, `OllamaContextFloorEngine.ts`  
- Dispatchers: cost‑router tiers (cheap/balanced/strong/best) and multi‑provider fallback  
- Metrics: token‑savings from offload rate (~12–14% → target 30+%), offloader suggestion vs execution gap  
- Octopus combo selection functions: `pickBestOllamaModel`, `ollamaModelSize`, `isVisionOllamaModel`

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `git-commit-mutex.mjs`, `stable-session-id.mjs`, `per-agent-handoff.mjs`, `precompact-pending-guard.mjs`  
- Ollama API (HTTP) for pulling/removing models  
- Workflow background agents for bulk re‑point edits, test rewrites, mapping/routing audit  
- Ultracode session posture  
- Hermes‑efficiency‑router plan/spec (`HERMES‑EFFICIENCY‑ROUTER‑PLAN‑2026‑06‑04.md`)  
- CronCreate for recurring `/loop`  
- Skill tool, Bash scripts, pipeline skill framework  
- Dynamic‑workflow emitter

**OPEN THREADS**  
1. Pull `gpt‑oss:120b` (streamed if needed)  
2. Complete background workflow that re‑points small‑model defaults to `qwen2.5-coder:32b`  
3. After pull, run anti‑revert guard and remove 3 b/7 b/14 b models via `ollama rm`  
4. Verify all synthesis scripts use resolver and produce tiered model  
5. Run full test suite (cost‑router, ask‑ollama, etc.)  
6. Finalise documentation updates for new LLM strategy  
7. Build U1b: `.ts` dispatcher wrapper + `AISystemRouterEngine` enum refactor  
8. Extend guard to scan all `data/state` configs fully  
9. Verify/adjust `pickBestOllamaModel` ranking (prefer text over vision)  
10. Complete golf’s pull of Gemma 4 31b (and gpt‑oss 120b/20b) to activate hybrid octopus  
11. Integrate tool‑call pre‑digesting into Ollama routing  
12. Resolve cold‑start offloader issue (ensure consistent reroute)  
13. Publish octopus combo demo results
