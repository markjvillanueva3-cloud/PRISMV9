# ai-training session 06e3b710 (2026-06-24, 26.6MB, spine 207KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5f61238333`: added `orchestrateLive` (Ollama‑offloaded tier fan‑out) in `CrossProcessHierarchicalNeuralOrchestratorEngine`.  
- `50143ece3c`: wired `five_axis_deep_learn` to real `deepReason`; closed learning loop (`recordOutcome`, `getLearningStats`).  
- `85f9f6fe8d`: rewired `five_axis_decision` to static `decide`; added crash‑guard tests.  
- `00f68e9997`: wired `fusion_5axis_strategy` to `recommendStrategy`; introduced `standard` router (default `mil1916`).  
- `U-DARK-FACADE-AUDIT`: harness identified 85 dark dispatcher actions across 10 dispatchers.  
- `U-DARK-FACADE-INDIA-VERIFY`: verified India’s 13 dark actions; fixed 4 clean ones:  
  * `cross_domain_orchestrate → planJob` (self‑validating)  
  * `foresight_orchestrate → reportFor` (async self‑validating)  
  * `wet_run_pilot_orchestrate → pilotPromotionReadiness` (positional‑arg adapter)  
  * `uncertainty_pipeline_run → propagate` (engine‑level guard).  
- `964535033a`, `da5560850f`, `62661e33fb`, `82aa392d6a`: committed the above four fixes.  
- `29af45fc12`: wired `smart_tool_select` to `selectToolOrchestrated`.  
- `d1a97a3a4c`: introduced `standard` router for `sampling_plan_generate`.  
- `9c4e94ff94`: added `scope` router and query guard for `mit_course_knowledge_query`.  
- `U-INCAD-INFER-FAILLOUD`, `U-CAD-LEARN-STATS-RATE-FIX`, `U-CAD-LEARN-FEATURE-SIGNAL`, `U-CAD-LEARN-TREND`: CAD‑learning fixes (feature signal, trend metric, failure‑rate consistency).  
- `U-ROLLBACK-PLAN-WIRE`, `U-OPDASH-ORCH-WIRE`, `U-MACHINE-LORA-INFO-WIRE`: India dark‑facade rewires.  
- 10 commits banked this session (4 CAD‑learning‑AI units + 3 R15 companion tests).  

**DECISIONS**  
- Adopt dark‑façade detection pattern; rewire dispatcher cases to real engine methods with crash guards or self‑validation.  
- Use Ollama offloading for orchestrator fan‑out (`orchestrateLive`).  
- Apply sound logic autonomously—no operator deferral.  
- Halt loop at iteration 14/20 due to token zone (~96 %) and completion of all clean fixes; resume only after auto‑compaction or manual `/compact`.  
- Restore stranded machine‑handbook files (`haas-vf-2`, `hurco-vm30i`, `okuma-m460v-5ax`) to satisfy leave‑a‑copy gate.  
- Do not start new builds until operator arms account‑switch and Vitest report is refreshed.  

**OPERATOR DIRECTIVES** (verbatim)  
- `/checkin-india` – force‑take India slot, bind handoff to `india-work`, run full `/checkin` pipeline.  
- Goal: “complete all remaining back‑end dev tasks, priority on India; improve ALL AI systems for all domains with priority on SFC, post processor, print reading, CAD drawing, CAM programming, quoting, business ERP, lathe, mill and wire wizard.”  
- Use memories, wikis, tribal knowledge to improve AI systems per domain.  
- “Utilize Ollama offloading, Hermes agents/CLI if possible, parallel agents, engineered loops, harnesses and crons.”  
- “Utilize sound logic to make the logical choices.”  
- “Continue.”  

**FINDINGS / BUGS**  
- Dark‑façade dispatcher pattern identified; 85 confirmed dark actions across dispatchers.  
- `deepReason` crash on empty `part_features`.  
- `kc11_mpa` NaN in `getMaterialScale`.  
- Double‑wrap envelope issue (`data.data`) resolved; now returns `{success,data}`.  
- Missing schema for `smart_tool_select`; guard added.  
- Query guard missing in `mit_course_knowledge_query`; added.  
- `roadmap_dag_build → load()` hang; reverted and reclassified as needs‑design.  
- tsc “false‑clean” OOM when using bare `npx tsc` without 16 GB heap; use `node --max-old-space-size=16384`.  
- RL error in `ReinforcementLearningCAMFeedbackEngine.ts` (TS2554) remains pending.  
- Stale‑test gate cleared after Vitest refresh from `mcp-server` cwd; detached shell PATH issue resolved.  

**DOMAIN SPECIFICS**  
- India slot AI systems: GNN, LoRA, RAG, AGI, deep learning, reasoning, CAD learning, print generation, print→CAD loop.  
- Engines modified/used: `CrossProcessHierarchicalNeuralOrchestratorEngine`, `FiveAxisDeepLearningEngine`, `VirtualMachiningDeepLearningEngine`, `SmartToolSelectorOrchestratorAdapter`, `SamplingPlanEngine`, `MitCourseKnowledgeEngine`, `CADTrialErrorLearningEngine`, `RollbackPlannerEngine`, `OperatorDashboardOrchestratorEngine`, `ReinforcementLearningCAMFeedbackEngine`.  
- Dispatchers involved: `aiReasoningDispatcher`, `orchestrationDispatcher`, `camDispatcher`, `calcDispatcher`, `qualityDispatcher`, etc.  
- Metrics: AUROC 0.808 for GNN selective‑deploy; crash‑guard coverage metrics (high); token zone ~96 % critical; iteration 14/20 halted.  

**TOOLS USED**  
- PRISM helpers (`claim`, `reclaim`, `/checkin`, `/slot‑claim`, `/loop`).  
- Audit harnesses (`audit-roadmap-drift.mjs`, `audit-dark-facade-actions.mjs`).  
- Dispatcher architecture with Zod schema validation.  
- Ollama offloading client (`ollamaClientEngine.generate()`).  
- Hermes agents/CLI integration scripts.  
- LoRA training & RAG pipelines; PSN, deep‑learning/reasoning modules, octopus consensus.  
- AUROC/Brier/F1 deploy gates.  
- Vitest test harnesses; `npx vitest run`.  
- TypeScript compiler (`tsc --noEmit` with 16 GB heap).  
- Cron job `f09e8cee` (every 30 min) for overnight builds.  
- Auto‑compaction permanently enabled for autonomous loop runs.  

**OPEN THREADS**  
1. Refresh `VITEST_REPORT.json` correctly from `mcp-server` cwd; resolve detached shell PATH issue.  
2. Await native auto‑compaction to reset token budget before resuming loops; trigger manual `/compact` if needed.  
3. Complete CAD learning loop‑closure unit tests and wiring (link recommendation → outcome).  
4. Fix RL error in `ReinforcementLearningCAMFeedbackEngine.ts`.  
5. Schedule crons/harnesses for overnight autonomous operation.  
6. Monitor token zones; trigger manual `/compact` if auto‑compaction fails.  
7. India needs‑build dark actions: `machine_lora_base_info`, `print_corpus_orchestrate`, `operator_dashboard_orchestrate`, `rollback_plan_build`.  
8. Cross‑domain dark action backlog of 72 items pending implementation in calc/physics, quality, cam, turning, CAD, auth.  
9. Optimize orchestrator fan‑out and Hermes integration for parallel loops.  
10. Await operator to arm account‑switch (`node scripts/capture-claude-credentials.mjs`, `node scripts/arm-account-switch.mjs --auto`) before new heavy builds.
