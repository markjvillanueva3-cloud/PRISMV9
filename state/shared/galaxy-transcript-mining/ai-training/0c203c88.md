# ai-training session 0c203c88 (2026-05-23, 45.5MB, spine 117KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**

- Updated CLIs: Claude Code 2.1.148, Codex 0.133.0.  
- Octopus integration wired; credentials (`XAI_API_KEY`, `grok login`, `codex login`) now active – 3/5 voices live, drain verified.  
- Master‑index autoupdate fixed – OOM heap bump to 8 GB, TTL stale‑lock backstop, sidecar rebuild in post‑commit chain, fail‑loud marker, stop‑hook staleness backstop, session‑start staleness inject.  
- Graph hooks: `pre-read`, `pre-grep`, `pre-write`, `pre-bash` implemented and wired via `settings.json`.  
- Track A (4 units), B (5 units), C (6 units) complete – drain verified; sync trigger + verdict + stakes gating combined into one hook.  
- Slot‑worktree enforcement: `chat-slots.json` branch field set to `slot/<nato>` for all 17 slots; routing hooks (`main-tree-write-block`, `worktree-commit-route`, `git-add-lane-guard`, `slot-worktree-cwd-advisory`) wired in `settings.json`; env knobs enabled.  
- MEMORY.md index + 8 orphan‑promotion memories committed to `slot/echo`.  
- First unit of RESOURCE‑CODE‑DSL (`scripts/build-resource-codes.mjs`) drafted and committed to `slot/echo`.  
- PSK syscall extension `psk.resolve` stubbed, tests passing.  
- Milestone **GRAPH‑OCTOPUS‑AUTOWIRE‑MS0** 17/17; D3 doc reflection committed.

---

**DECISIONS**

- TTL‑based stale‑lock reclaim (`system-graph-write-lock.mjs`) to avoid Windows PID‑reuse phantom.  
- Raise Node heap for regen chain (`NODE_OPTIONS=--max-old-space-size=8192`).  
- Wire all graph hooks into `settings.json`; no hardcoded constants.  
- Adopt fail‑open hook discipline: emit `{continue:true}` on any error path.  
- Create high‑ROI doctrine memories (`feedback_r5_thru_r12_doctrine.md`, `feedback_karpathy_discipline.md`) to surface core rules automatically.  
- Keep PSN definition in memory; plan dedicated file for PSK, Obsidian brain, PRISM OS, SVI/Psi, ATCS entries.  
- Adopt M#### / W##### code scheme for memories/wikis (~70 % size reduction); keep Markdown index; reject HTML verbosity.  
- Enforce slot‑worktree commits via hooks; commit prefix `[MAIN]` on shared tree, `[slot:<name>]` on slot trees.  
- Use atomic write pattern (tmp → parse → fsync → rename) for all config changes.

---

**OPERATOR DIRECTIVES**

- No secrets may be committed; use `PRISM_MEMORY_APPEND_OK=1` if size > 22 kB.  
- Maintain slot binding via `slot-bind-enforce.mjs`; avoid manual claim unless hook emits `⚠️ no harness session_id`.  
- Do not force‑push to main/master; all commits must go through PR workflow.  
- Verify that `settings.json` changes are mirrored to `H:/.claude/settings.json`.  
- `/startup-echo — slot-locked /startup` to force‑take echo slot and run full startup pipeline.  
- Run `/loop [5m] /goal` after compact; audit orphan doctrine, promote memories, then proceed with RESOURCE‑CODE‑DSL unit 1.  
- Verify `slot-slots.json` branch field starts with `slot/`.  
- Ensure all four hooks are referenced in `settings.json`; if missing, add them.

---

**FINDINGS/BUGS**

| Issue | Fix | Status |
|-------|-----|--------|
| V8 heap OOM during merge‑augmentations | 8 GB heap bump | Resolved – graph now regenerates (452 MB) |
| Stale PID locks (`.system-graph-write.pid`, `.system-viz-on-commit.pid`) | TTL‑based reclaim + mtime check | Fixed; no stale lock remains |
| Silent post‑commit hook failures | Fail‑loud marker, completion sentinel | Implemented; failures logged to `.last-regen-failure.json` |
| Sidecar stale (2 days) | Rebuild sidecar in on‑commit chain | Done – sidecar 258k nodes rebuilt |
| Octopus underutilized | Credentials added, stubs replaced with real engines | Now 3/5 voices live; drain verified |
| PSN concept not surfaced automatically | No dedicated memory file | **Pending** – create `feedback_psn.md` (or similar) |
| Git lock contention (78 s) | Wait and retry logic | Resolved |
| Hook misrouting (`worktree-commit-route`) | Override with `[MAIN]` when committing from shared tree | Fixed |
| M‑namespace collisions | Prefix slug with namespace | Solved |
| Wiki‑codes JSON size 4.2 MB | Plan to compress via path derivation | Planned |
| `psk.resolve` test failures (syntax error) | Restore original tests, add defensive parsing | Resolved |

---

**DOMAIN SPECIFICS**

- **Engines / Actions**: `MultiModelConsensusEngine`, `GrokCLIClientEngine`, `CodexLoginEngine`, `OllamaEngine`, `psk.resolve`.  
- **Dispatchers**: `/octopus`, `/startup`, `/handoff`, `/pre-read‑graph-inject`, `/pre-grep‑graph‑inject`, `/pre-write‑graph‑inject`, `/pre-bash‑graph‑inject`.  
- **Metrics / Paths**: `system-graph.json` (master index), `system-graph-index.json` (sidecar), `architecture-graph.json` (fallback).  
- **Special Hooks**: `stop-graph-staleness-backstop.mjs`, `session-start-staleness-inject.mjs`.  
- **PSN**: PRISM Synergy Network – 11‑leg concept (Obsidian brain, Prism OS, Wiki, Memories, Tribal Knowledge, System Viz, Engines, Algorithms, Formulas, Neural Net/GNN, Prism AI systems).  
- **Slot worktrees**: `H:/prism-slot-<nato>` on branch `slot/<nato>`.  
- **Routing hooks**: `main-tree-write-block`, `worktree-commit-route`, `git-add-lane-guard`, `slot-worktree-cwd-advisory`.  
- **Environment knobs**: `PRISM_WORKTREE_ROUTE_ENABLE`, `PRISM_GIT_ADD_LANE_ENABLE`, `PRISM_MAINTREE_WRITE_BLOCK_ENABLE`.  
- **Memory index**: `MEMORY.md` hard cap 24576 B; target 22000 B.  
- **Resource‑code DSL**: M#### for memories, W##### for wiki entries; lookup tables in `state/shared/{memory,wiki}-codes.json`.

---

**TOOLS USED**

- Graph hooks templates: `pre-read-graph-inject.mjs`, `pre-grep-graph-inject.mjs`, `pre-write-graph‑inject.mjs`, `pre-bash-graph‑inject.mjs`.  
- Octopus components: `MultiModelConsensusEngine.ts`, `GrokCLIClientEngine.ts`, `CodexLoginEngine.ts`.  
- Slot worktree helpers: `chat-slots.mjs`, `worktree-commit-route.mjs`, `main-tree-write-block.mjs`, `slot-worktree-cwd-advisory.mjs`.  
- Scripts: `audit-orphan-doctrine.mjs`, `scripts/build-resource-codes.mjs`.  
- Kernel syscall extension: `.claude/kernel/psk.mjs`.  
- Testing framework: Vitest (`psk.test.ts`).  
- CI tooling: per-file 2‑of‑2 scrutiny gate, stop‑hook ledger.  
- Atomic write utilities.

---

**OPEN THREADS**

1. Finalize RESOURCE‑CODE‑DSL implementation (units 2–7).  
2. Verify memory index compression to < 22000 B after code scheme applied.  
3. Run full suite of tests for `psk.resolve` and resource-code DSL across all slots.  
4. Ensure no residual git lock contention; monitor peer commit patterns.  
5. Validate that all chats now commit only to their slot worktrees without manual overrides.  
6. Create dedicated PSN memory files (`feedback_psn.md`, PSK kernel, Obsidian brain, PRISM OS, SVI/Psi, ATCS).  
7. Integrate PSN context injection into consensus engine; add `--use-psn` flag to `/octopus`.  
8. Plan unit to prune/compact 452 MB master graph while preserving search quality.  
9. After PSN entries, scan for other orphan concepts (PRISM OS kernel, SVI/Psi, ATCS) and add them.  
10. Resume `/loop [5m] /goal` to iterate over remaining tasks automatically once above are committed.
