# ai-training session c785ffe4 (2026-05-13, 11.2MB, spine 77KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- A1 – chat‑slot foundation (`chat-slots.mjs`, `chat-slots.json`) – commit `8cd6ab1a5`  
- A6 – bootstrap‑golf helper (`bootstrap-golf.mjs`) – commit `7301708ef` (script) + `846fc6bd3` (tests)  
- A5 – golf‑slot write‑allowlist hook & wiring – commit `d034758d5` (hook, settings, tests)  
- B3 – git‑log‑tail helper – commit `dd20a1da6`  
- B10 – LedgerStoreEngine + migration SQL – commit `d9f2a29bc`  
- B1 – PeerCommitAuditorEngine – commit `5f11d0eef`  
- B11 – LedgerProjectorEngine – commit `4bc764e59`  
- B2 – dispatcher wiring & schemas – commits `722bb7dd9`, `b60dd777b`, `4d7c964c5`

**DECISIONS**  
- Adopt a 7‑slot chat fleet (alpha–foxtrot + golf) to isolate cleanup work.  
- Use cross‑worktree firewall (`hook-cross-worktree-block.mjs`) and per‑file scrutiny gate (2 reviewers) for all edits.  
- Employ hybrid‑race commit pattern: fork into a dedicated worktree, commit, then reverse‑merge‑then‑ff‑only back to main.  
- Build the 67‑unit CLEANUP‑MS0 envelope via `/rgs6 generate`, prioritizing memory stability, orphan cleanup, bug‑watching, and CLAUDE.md token savings.  
- Schedule a recurring 7‑min orphan monitor (`04-prism-mcp-orphan-monitor.ps1`) with auto‑escalation to `node-orphan-cleaner.mjs` if pressure ≥ 90 %.  

**OPERATOR DIRECTIVES** (verbatim user asks)  
- “lets build /loop until this /goal is complete”  
- “continue where we left off. run /loops until this /goal is complete”  
- “Memory monitor tick — run the PRISM orphan reaper and report.” (repeated many times)

**FINDINGS/BUGS**  
- 4 orphan MCP processes + 1 stale `.git/index.lock` in initial pass; subsequent ticks show no orphans.  
- Memory usage fluctuates 59–68 % but never > 90 %.  
- P0/P1 findings in B3, B10, B1, B11, B2 were resolved in‑commit per `feedback_always_close_out`.  
- Test failures due to `spawnSync git ENOENT` and NUL handling; fixed by explicit git path and `%x1F` separator.  
- Git lock staleness threshold (60 s) caused a 215‑s stale lock that blocked commits; manual sweep cleared it.

**DOMAIN SPECIFICS**  
- **Engines/Actions**: `bootstrap-golf.mjs`, `git-log-tail.mjs`, `LedgerStoreEngine`, `PeerCommitAuditorEngine`, `LedgerProjectorEngine`.  
- **Dispatchers**: async dispatcher (`async_dispatcher.mjs`), commit‑reviewer dispatch hook.  
- **Metrics**: memory usage, orphan count, git lock sweeps, unit test pass/fail counts.  
- **Paths**: `H:/prism/scripts/system-health/04-prism-mcp-orphan-monitor.ps1`, `H:/prism/.claude/helpers/node-orphan-cleaner.mjs`, `H:/prism/state/shared/specs/GOLF-WATCHDOG-MS0-2026-05-13.md`.

**TOOLS USED**  
- Adaptive thresholds (`adaptive-thresholds.mjs`)  
- Pipeline telemetry (`pipeline-telemetry.mjs`)  
- Node orphan cleaner (`node-orphan-cleaner.mjs`)  
- CronCreate / schedule skill for recurring monitor  
- Per‑agent handoff (`per-agent-handoff.mjs`), stable session ID helper, milestone tracker.  
- Vitest + TypeScript for unit tests.

**OPEN THREADS**  
Remaining units to ship (critical path): B4, B5, G3, C5, F8, G11, E2.  
No outstanding bugs; all P0/P1 findings resolved.  The next loop will continue with B4 once the handoff is written and `/compact` has been executed.
