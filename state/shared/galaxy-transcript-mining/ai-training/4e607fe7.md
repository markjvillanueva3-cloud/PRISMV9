# ai-training session 4e607fe7 (2026-06-06, 16.9MB, spine 55KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-GNN-SELECTIVE-DEPLOY` – commit b0b5b08716 (selective‑deploy eval, τ=0.7, 32% coverage)  
- `U-LEG-STATE-SELECTIVE-WIRE` – commit 15eceefa24 (fleet banner now shows SELECTIVE‑DEPLOY)  
- `U-SELECTIVE-CLASS-HONESTY` – commit 0977fea472 (class‑concentration flag, 2/6 classes)  
- `U-CALIB-GATE-SOURCE` – commit cd8fcd6159 (single‑source gate constants from GATE_THRESHOLDS)  
- `U-GPU-STACK-PROVISION` – commit fb3062febb (Python 3.13 venv, torch 2.11+cu128 sm_120, bitsandbytes NF4, PRISM_PYTHON_GPU_PATH set)

**DECISIONS**  
- Calibration is a dead end: Murphy decomposition shows only 0.0197 of the 0.179 Brier is miscalibration; post‑hoc calibration cannot clear the gate.  
- Deployability must be measured as selective prediction (risk‑coverage) at the production confidence threshold (τ = 0.7).  
- Class concentration must be surfaced to avoid over‑reading macro‑F1 = 1.0.  
- Gate constants should come from a single source (`GATE_THRESHOLDS`) to prevent drift.  
- GPU stack provisioning is required for all remaining high‑value India units (RAG, LoRA, GNN retrain).

**OPERATOR DIRECTIVES**  
- “bypass golf and do it yourself” – provision the GPU training stack locally.

**FINDINGS/BUGS**  
- Calibration gate constants were inlined; now sourced.  
- Sentence‑transformers import segfaults via `datasets` → `pyarrow` on Windows/py3.13 (needs separate fix).  
- Torch install failed due to cross‑filesystem hardlink; resolved with `--link-mode=copy`.  
- GPU health gate confirms real sm_120 matmul; QLoRA gate green.

**DOMAIN SPECIFICS**  
- PSN leg‑state banner, `classifyGnn`, `formatDigest` – now report SELECTIVE‑DEPLOY.  
- `nn-graph-eval.mjs` – extended with risk‑coverage metrics and selective verdict.  
- `ModelRoutingEngine.route()` wired to `OllamaCapabilityProbeEngine`.  
- `PRISM_PYTHON_GPU_PATH` env var used by Node bridge for GPU tasks.  
- GATE_THRESHOLDS constants (Brier 0.15, macro‑F1 0.55) now central.

**TOOLS USED**  
- CronCreate / CronDelete (loop scheduling).  
- gpu_health.py (GPU verification gate).  
- nn-graph-eval.mjs & scripts/lib/nn-graph-eval.mjs (metrics harness).  
- uv, pip (Python 3.13 venv).  
- torch 2.11+cu128, bitsandbytes 0.49.2, sentence‑transformers, PyG.  
- PRISM dispatchers: aiReasoningDispatcher, ModelRoutingEngine, OllamaCapabilityProbeEngine.

**OPEN THREADS**  
- GNN GPU retrain (MS3) – requires PyG install and full training pipeline.  
- LoRA trainer (MS4) – pending QLoRA integration on the new stack.  
- RAG re‑embed (MS2) – sentence‑transformers segfault fix; fallback to Ollama embed.  
- PyG installation for GATv2/H2GCN retrain.  
- Potential future work: full‑coverage deploy, model routing updates, additional infra provisioning.
