# ai-training session 32a39c0c (2026-05-16, 34.7MB, spine 179KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U‑PPL‑A1 (TurningMinFingerprintEngine) – 9/23 units, all tests passed, landed `a76ea58c5`.  
- U‑PPL‑B1 (ProgramReoptimizationOrchestratorEngine) – 48 tests passed, peer commit `4050f3b35`.  
- Phase21 PDF splitter (`phase21-split-containers.py`) built; idempotent, runs on 8 396 containers → `_organized/PRINTS/`, `_split-manifest.jsonl`.  
- `helper-orphan-rank.mjs` – 8/8 tests passed.  
- `synergy-regression-watch.mjs`; `install-synergy-watch-task.ps1` (Windows scheduled task).  
- `DocustrataCustomerIndexEngine.ts` + dispatcher case (`docustrata_customer_index`).  
- `phase16-blueprint-program-join-v6.py` – now consumes VLM output.  
- `docustrata-pipeline.py` – extended to 9 stages, OOM‑safe deep‑rescan.  
- `cold-script-rank.mjs` (F5) ready for suite run.

**DECISIONS**  
- Ship A1 & B1 immediately; handoff B2 as wiring unit (not built yet).  
- Build Phase21 splitter first → re‑index 104 K PDFs later.  
- Wire Qwen2.5‑VL into join; add `join` & `customer‑rollup` stages for single‑command pipeline.  
- Replace deep‑rescan with mem‑safe variant to avoid OOM on large PDFs.  
- Build dedicated customer‑folder index engine (DocustrataCustomerIndexEngine) exposed via CAD dispatcher.  
- Persist synergy regression monitoring with durable Windows scheduled task; fix cmd / c quote‑strip.

**OPERATOR DIRECTIVES**  
- “Do it all” – build CPU re‑index path and GPU orchestrator pipeline in next session.  
- Monitor split job completion (`b7dvt7ale.output`); rerun `phase21-split-containers.py --min-prints 2` if needed.

**FINDINGS/BUGS**  
- 5 of 7 `.MIN` templates corrupted (null‑bytes, JSON state).  
- Lane‑guard mis‑resolves foxtrot → kilo; absorption happened for A1 & B1.  
- Docker daemon down – GPU OCR stages blocked (`wsl --shutdown` + Docker Desktop).  
- Quote‑strip bug in `cmd /c` fixed (double‑outer quotes).  
- PowerShell codepage mis‑parsed non‑ASCII em‑dashes; rewritten ASCII only.  
- Deep‑rescan OOM resolved with mem‑safe script.  
- Phantom “104K unscanned” count artifact → true delta 7,235 PDFs.  
- Phase16 originally ignored VLM pages; now optional and guarded.  
- `xmalloc` errors transient fork‑storm symptoms.  
- `/tmp` path resolution fails on Windows; script exits gracefully in pretty mode.  
- `main()` not firing due to NTFS case‑insensitivity – fixed by gate change.  
- PreToolUse/PostToolUse hooks crash resolved via reaping & retry logic.  
- Literal NUL bytes at offsets 7685/7695 removed.

**DOMAIN SPECIFICS**  
- **Engines:** TurningMinFingerprintEngine, ProgramReoptimizationOrchestratorEngine, DocustrataCustomerIndexEngine.  
- **Actions:** `turning_min_fingerprint`, `turning_min_classify`, `prism_cam:program_optimize`, `prism_mill:mill_program_optimize`, `docustrata_customer_index`.  
- **Metrics:** cosine similarity, fingerprint distance; synergy ratio 21.1 % baseline; orphan rate corrected to 40.6 %; delta count 7,235 PDFs.  
- **Paths:**  
  - `H:/prism/Docustrata/.index/phase21-split-containers.py`  
  - `H:/prism/mcp-server/data/milestones/MS‑PRINT‑PROGRAM‑LOOP.json`  
  - `src/dispatchers` (dispatcher files)  
  - `state/shared/specs/BORIS-LOOP-AGENT-DOCTRINE.md`  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `per-agent-handoff.mjs`, `milestone-tracker.mjs`, `audit-roadmap-drift.mjs`.  
- Docker, WSL, GPU containers (PaddleOCR, eDOCr2, VLM).  
- Python (`PyMuPDF`), Node scripts (`update-prism-inventory.mjs`, `build-state-snapshot.mjs`).  
- System‑Viz (`system-viz-query.mjs`), Forge‑Audit v2 (Boris discipline).  
- PowerShell (`install-synergy-watch-task.ps1`).  
- Vitest + tsc for unit tests; esbuild for dispatcher bundling.  

**OPEN THREADS**  
- Finish re‑index of 215 K PDFs (phase6→7→15).  
- Restore Docker daemon & run GPU OCR stages (3–5).  
- Build B2 wiring unit next session (dispatcher edits, schemas, tests).  
- Resolve lane‑guard slot mis‑resolution for future commits.  
- Verify split job completion and aggregate counts (`b7dvt7ale.output`).  
- Run full cold‑script‑rank suite on all .mjs/.js/.py scripts; confirm no NUL bytes or hook crashes.  
- Finalize orphan‑rate metrics & update documentation (40.6 %).  
- Ensure reaping logic covers all fork‑storm scenarios before next commit.  

Artifacts remain uncommitted per policy and are ready to commit when explicitly requested.
