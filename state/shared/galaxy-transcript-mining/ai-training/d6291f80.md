# ai-training session d6291f80 (2026-06-03, 2.7MB, spine 17KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- None of the builds/commits from this slice have been marked as shipped.  

**DECISIONS**  
- Use `/checkin-hotel` wrapper to force‑claim the `hotel` slot and bind it to topic `hotel-work`.  
- Run the canonical `/checkin` pipeline (slot‑claim + dev phases) after binding.  
- Source Quote‑to‑Ship orchestration from `mcp-server/src/engines/QuoteToShipOrchestratorEngine.ts`; invoke directly via `tsx` to avoid stale dist.  
- Build a JM order/job catalog from the authoritative `Docustrata` manifest (`manifest.json`) rather than the 73 k classified subset.  
- Require both `drawing_pdf` and `drawing_text` for INTAKE; supply STEP files or explicit `feature_candidates` for FEATURE_RECOGNITION.  
- Resolve DFM_CHECK failure by ensuring `ctx.features` is an array (bridge missing features from blueprint analysis).  

**OPERATOR DIRECTIVES**  
- `/checkin-hotel /loop system-viz-brain until /goal` – start dev loop under hotel slot.  
- `/quote-to-ship run <material> <quantity>` – execute full 21‑stage pipeline on real JM data.  
- `/quote-to-ship validate <material>` – pre‑run validation of material spec.  

**FINDINGS/BUGS**  
- Docustrata classified index omitted real `JMD Quotes`, `Sales Orders`, etc.; must use manifest.  
- INTAKE fails if only `drawing_text` is supplied; needs both PDF and text.  
- FEATURE_RECOGNITION fails because `_extractFeatureCandidates` receives no `feature_candidates`; blueprint analysis features are not bridged into the pipeline.  
- DFM_CHECK crashes: `"features is not iterable"` – `ctx.features` is non‑array due to previous stage output.  

**DOMAIN SPECIFICS**  
- Engines: `QuoteToShipOrchestratorEngine`, `BlueprintOCREngine`, `StepImportEngine`.  
- Dispatchers/paths: `businessDispatcher.ts:4025`, `mcp-server/src/...`.  
- Data roots: `JM DIE/`, `JM DIE/Prism JM Die/`, `Docustrata/` (manifest + `.index/`).  
- Metrics: 21‑stage pipeline, stage descriptors, runtime ~1.9 s for smoke test.  

**TOOLS USED**  
- PRISM slot helpers (`chat-slots.mjs`, `slot-bind-enforce.mjs`).  
- `/checkin` canonical pipeline scripts.  
- Node/TSX harness for engine invocation.  
- Scripts under `scripts/jm-sim/` to build order catalog from Docustrata manifest.  

**OPEN THREADS**  
1. Implement bridge that extracts feature candidates from blueprint OCR notes and injects them into FEATURE_RECOGNITION.  
2. Ensure DFM_CHECK receives an array of features (fix inter‑stage contract).  
3. Validate full pipeline on the entire 12‑year JM job set; capture per‑stage results.  
4. Integrate generated order/job catalog into orchestrator input schema (`material_spec`, `quantity`, `customer_id`, etc.).  
5. Confirm that `/quote-to-ship run` completes without halting at APPROVAL_GATE or other gates.
