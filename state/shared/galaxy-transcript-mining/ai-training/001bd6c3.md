# ai-training session 001bd6c3 (2026-06-10, 58.3MB, spine 523KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Patched `config.yaml` to use Claude‑Code (`claude-opus-4‑8`) with local Ollama fallback (`qwen2.5-coder:32b`).  
- Reverted supervisor cwd patch; no repo changes committed.  
- Hermes app launched (Web UI :9120).  
- Docker Desktop started → `prism-qdrant` container UP on :6333, 3 healthy collections.  
- Hermes config migrated v0→v28 (`hermes config migrate`).  
- Commit 28b72e4dee: added `scripts/fill-galaxy-memory-sections.mjs`, updated all 34 galaxy `MEMORY.md`.  
- U9 commit 7771ca7f86: VRAM co‑residency env + hard‑reason mutex.  
- U1 commit 2340a2e699: offload router roster refreshed, tiered selection added.  
- U2 commit a2756779c2: route hook default → resident `gpt‑oss:20b`; shipped `ollama-route-config.json`.  
- U3 commit a60d7ba0bf: take‑rate banner corrected.  
- U4 commit 612418fde7: RTK residue compressor, safety denylist fixed.  
- Go‑live #14 merge commit 5a91ba1862 onto `cad-fusion-live-ms0`.  
- Settings update `PRISM_CODEGEN_MODEL=gpt‑oss:120b` (commit 3904b5e7b0).  
- Scripts shipped: `ollama-tool-agent.mjs`, `ollama-offload-enforce.mjs`, `ollama-codegen.mjs`, `ollama-build-loop.mjs`, `ollama-coresidency.mjs`, `ollama-capability-probe.mjs`.  

**DECISIONS**  
- Use PRISM MCP :3100 for vault read; drop second filesystem‑MCP.  
- Link Hermes to Claude Code via OAuth, fallback to local Ollama when rate‑limited or billing‑blocked.  
- Revert supervisor cwd patch to preserve `cwd=repo‑root`.  
- Keep Qdrant disabled until Docker available; semantic_search remains unavailable.  
- Replace invalid provider `openai` with custom (`base_url: http://127.0.0.1:11434/v1`).  
- Primary model: `gpt‑oss:20b`; fallback Claude‑Opus‑4.8.  
- Vision: switch from Claude to local Ollama `qwen3-vl:8b-instruct`.  
- Headless zulu (`hermes -z`) recommended over desktop GUI.  
- Use local Ollama/Docker stack for audit/fill; exclude auto‑generated `node_*` files from memory counts.  
- Replace failing live test with deterministic synthetic tests + mutation probe.  
- Flag corrupt `ai-training_synthesis.md`.  
- Offload read/search/coding to local LLMs (gpt‑oss family); qwen2.5‑coder:32b only for code generation.  
- Auto‑route per task via existing engine; keep `PRISM_OLLAMA_ROUTE_AUTO` enabled.  
- Go‑live merge & settings.json wiring gated until golf’s git‑tree repair completes.  
- Adopt tiered selection in offload router; resident models: `gpt‑oss:20b`, `qwen2.5-coder:32b`.  
- Set GPT‑OSS‑120B fleet‑wide default for code generation; keep 20B for general tasks.  
- RTK upgrade cannot LLM‑upgrade binary; use U4 compressor instead.  
- Apply U9 RECOMMENDED_ENV to Ollama server launch (restart‑gated).  

**OPERATOR DIRECTIVES**  
- Bring Qdrant up (Docker must be installed & container started).  
- Restart Hermes daemon after config migration; send test message (`hermes -z "Hello"`).  
- Update `auxiliary.vision` to local Ollama model once pulled.  
- Monitor memory‑pressure; compact idle chats or throttle concurrent agents if spikes occur.  
- Verify Qdrant connectivity → `prism_memory:semantic_search` returns results.  
- Confirm supervisor cwd fix does not regress any engine’s path expectations.  
- Use ultracode workflow for planning/verification (already running).  
- Commit only to slot/bravo worktree (`H:/prism-slot-bravo`).  
- Complete U5 RTK config and Go‑live #14.  
- Investigate cloud version of Kimi 2.6; note data‑sovereignty conflict.  

**FINDINGS/BUGS**  
- Qdrant :6333 not listening → `semantic_search` empty; only lexical `master_index_query` works.  
- Hermes messages fail with HTTP 400 from Anthropic (billing restriction); 429 seen when pool exhausted.  
- Unknown provider `openai` caused agent init failures; fixed by using custom.  
- Hermes daemon cached “not connected” state for Qdrant → requires restart to reconnect.  
- Docker Desktop had dirty `package-lock.json`; cleared and relaunched.  
- Auxiliary vision still on Claude → would trigger 400s if images processed.  
- 20× corpus inflation: node_* files mistakenly counted as memories; excluded now.  
- Self‑defeating test never passed; rewritten deterministically.  
- `ai-training_synthesis.md` corrupt (all NUL); flagged for owner.  
- Offload router & route hook referenced absent models → 0 % offloading.  
- CRLF→LF flip on U1 edit fixed via amend.  
- Kimi free tier violates PRISM data‑sovereignty; not wired.  

**DOMAIN SPECIFICS**  
- Engines/dispatchers: `prism_context:slot_brief_write`, `prism_memory:semantic_search`, `prism_session:master_index_query`, `prism_knowledge:search`.  
- Hermes ↔ Obsidian vault sync via Local REST API :27123; Hermes ↔ PRISM MCP :3100 channel.  
- Qdrant vector store for semantic search (currently down).  
- Ollama local LLMs: `gpt‑oss:120b`, `gpt‑oss:20b`, `qwen2.5-coder:32b`; RTK binary; Claude Opus.  
- Actions: read/search/coding offload, auto‑routing, co‑residency, capability probe, Opus tail review gate.  
- Metrics: token savings (Read/IsLargeRead), VRAM usage, ~30 s latency for tool loop, confidence gate.  
- Paths: slot/bravo worktree `H:/prism-slot-bravo`; isolated commits; settings.json wiring.  

**TOOLS USED**  
- PRISM command `/checkin‑bravo`.  
- Hermes config editor (`config.yaml`).  
- Supervisor script `mcp-server-supervisor.mjs`.  
- Docker / docker-compose.yml (Qdrant).  
- HTTP client for Anthropic OAuth token tests.  
- Node.js scripts: `syncGalaxyMemories`, `fill-galaxy-memory-sections.mjs`, `galaxy-synthesis-refresh.mjs`.  
- Ollama CLI + local models.  
- Ultracode workflows (`wbmnnd902`, `wq09sxi8s`).  
- Git hooks (`slot-bind-enforce.mjs`, `chat-slots.mjs`).  
- Node test harness (`node --test`, `vitest`).  

**OPEN THREADS**  
- Bring Qdrant up (Docker install & container start).  
- Resolve Hermes message failure; enable “extra usage” billing or adjust fallback logic.  
- Verify `prism_memory:semantic_search` returns results once Qdrant running.  
- Confirm supervisor cwd fix does not regress any engine’s path expectations.  
- RTK upgrade sweep (#11) pending.  
- Go‑live merge to main + wiring of settings.json (gated).  
- System upgrades for new hardware (RTK, existing systems).  
- Post‑go‑live routing tier adjustments (U1/U3).  
- Octopus consensus producer enable (`LIVE_DISPATCH=1`).  
- Cross‑substrate synergy plan execution.  
- Kimi integration review (data sovereignty).
