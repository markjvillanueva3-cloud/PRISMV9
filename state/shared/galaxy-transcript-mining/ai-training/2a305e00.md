# ai-training session 2a305e00 (2026-06-25, 7.4MB, spine 48KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑CAD‑LEARN‑CALIBRATE` (commit 1a910d6015) – self‑correcting calibration loop for CAD trial‑error learning.  
- `U‑CAD‑TEXT‑LEARN‑PROMPT` (commit 50bd919799) – reverse arrow that feeds learned failure modes back into the text→CAD generation prompt.

**DECISIONS**  
- Only add new knowledge; avoid duplicating existing PDF or transcript ingestion.  
- Ship calibration self‑correction and reverse arrow first, then build a separate unit for video‑transcript ingestion.  
- Do not auto‑fix the RL‑CAM step arity mismatch (would inject wrong rewards); route it to its owner.  
- Hand off cross‑lane print→CAD tribal injection to the xray/delta team; do not modify their GenerationBackend.  
- Use existing `parseVtt` and build a lightweight SRT parser for the new transcript drain.

**OPERATOR DIRECTIVES (verbatim)**  
> “improve the learning and ai systems for cad drawing, print generation, print to cad file in fusion, hypercad and mastercam… ensure your adding tribal knowledge injections. run the hermes /learn pipeline on all cad and engineering related sources in H:\PRISM\resources and all other sources we have in the h drive from MIT and other college courses. include videos and other reputable sources from online, dont duplicate knowledge though. only add new knowledge”

**FINDINGS/BUGS**  
- `getLoopEfficacy` measured calibration error but never acted on it → fixed by adding raw‑anchored logit‑shift in `recommendAdjustments`.  
- Reverse arrow missing in text→CAD bridge → added `loadLearnedRisk()` to inject learned failure modes.  
- RL‑CAM `step()` signature changed; callers lack required fields → cannot auto‑fix; routed to owner.  
- Manual `/learn` batch was reaped by fleet‑reaper, leaving a stale lock → cleared per R14.  
- Transcript ingestion gap: MIT/college `.vtt/.srt` files were not in the PDF drain; built `U‑CAD‑LEARN‑VIDEO‑TRANSCRIPT‑DRAIN`.  
- No existing SRT parser; created lightweight implementation.

**DOMAIN SPECIFICS**  
- Engines: `CADTrialErrorLearningEngine`, `cad_text_to_cadquery.mjs`, `neuralCADGenerationEngine`.  
- Dispatchers: `cadDispatcher.ts` (recommend, record, trial), `cad_learning_*` actions.  
- Metrics: AUROC, Brier score, calibration shift, logit‑shift learning.  
- Paths: `/checkin-india`, `/goal`, `scripts/drain-resources-tribal.mjs`, `build-cad-cam-resources-pdf-index.mjs`.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `/checkin` pipeline, `/loop`.  
- Node scripts: `youtube-free-extract.mjs`, `parseVtt()`, `parseSrt()`.  
- Test harnesses: `node:test`, `vitest`, `tsc --noEmit`.  
- Scheduler: scheduled task “PRISM Resources Tribal Drain”.  

**OPEN THREADS**  
1. **Print→CAD tribal injection** – requires cross‑lane coordination with xray/delta’s GenerationBackend.  
2. **Video & online reputable source expansion** – beyond local MIT transcripts, need to ingest YouTube/online captions and other corpora.  
3. **GNN AUROC threshold** – current 0.759 < 0.78; may defer until higher‑tier cascade is ready.  
4. **Full `/learn` pipeline coverage** – ensure all online reputable sources are queued (currently only PDFs & local transcripts).  
5. **Video transcript drain unit** – still under development; pending commit once tests and scrutiny pass.
