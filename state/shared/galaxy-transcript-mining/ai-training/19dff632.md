# ai-training session 19dff632 (2026-06-10, 19.2MB, spine 139KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e44a3a1592` – wired orphaned PayrollLiabilityFilingEngine methods into `businessDispatcher`.  
- `e649790e76` – added `remitLiability` method (positional args) and updated tests.  
- `f069052772` – committed verify‑first unit **U‑HOTEL‑P0‑VERIFY‑SHIPPED** (6 stale gaps confirmed shipped).  
- `099e6b92bd` – finalized **HOTEL‑FORGE‑ROADMAP‑2026‑06‑09.md**.  
- `9dfd621910` – added reusable Ollama transcript miner (`scripts/mine-hotel-transcripts.mjs`).  
- `61518eb988` – fixed four miner bugs (empty‑response, noise filtering, combined‑file glob, regex) and re‑verified tests.  
- `dd57b82b52` – **U‑HOTEL‑PORTAL‑PERSISTENCE**: SQLite WAL folded into `CustomerPortalEngine`.  
- `17f3e0ffec` – scrutiny‑fix for persistence.  
- `c9b5156d0b` – **U‑HOTEL‑ALLOWLIST‑WRITE‑REVIEW + REALTIME‑VERIFY**.  
- **U‑HOTEL‑FALSE‑WIRE‑REGRESSION‑GUARD** – built, 20/20 tests, 3‑of‑3 PASS.  
- `18f37c812e` – core of **U‑HOTEL‑ALLOWLIST‑WRITE‑ENABLE**.  
- `d8d2824cf2` – polish; 23/23 route tests, 3‑of‑3 PASS.

**DECISIONS**  
- Use Ollama for mechanical summarization; ultracode Workflow for high‑level reasoning/roadmap synthesis.  
- Adopt `qwen2.5-coder:32b` as default coder; reserve `gpt-oss:120b` for heavy multi‑file synthesis.  
- Proceed with **U‑HOTEL‑PORTAL‑PERSISTENCE** after all verify units complete.  
- Treat earlier audit gaps as stale; roadmap now 3 real builds + 1 verify + 2 file/tag actions.  
- Fold durable Maps (tokens, messages, qualityDocs, serviceCases) into single SQLite WAL store; keep rateBuckets in memory.  
- Reuse existing auth middleware (`verifyToken`, `requireRole`) and global auditLog; no new RBAC subsystem.  
- Implement per‑action manager‑tier role gate inside business/dispatch handler; exclude financial/PII write actions from write map (403).  
- Use existing RealtimeEventBridge for SSE; verified end‑to‑end transport.

**OPERATOR DIRECTIVES**  
- “read previous few sessions of hotel to regain full context”.  
- “continue” / “start” – execute roadmap, route grunt work to Ollama.  
- If Ollama can generate brainstormed roadmap, use it; otherwise use ultracode.  
- “approve role model” – operator sign‑off on **U‑HOTEL‑ALLOWLIST‑WRITE‑ENABLE**.  
- “push through” – start **U‑HOTEL‑PORTAL‑PERSISTENCE** immediately after slot claim.  
- Slot‑binding via `/startup-hotel` (force‑take hotel, then run standard `/startup`).

**FINDINGS/BUGS**  
- Audit’s “missing tax engine” claim stale; all payroll‑tax engines wired.  
- Only `MobileVoice` remains orphaned mobile engine; other six wired.  
- Q2S DFM bug: `ctx.features = resolved?.features ?? resolved ?? []` assigns non‑array → “features is not iterable”.  
- False‑wire regression guard caught `marketplace_lead_get` returning null (needs real‑id probe) and regex bug in placeholder detector (`\b...\b` fails on “TODO:” suffix).  
- Guard regex missed “Unknown business action: X” string – fixed.  
- `customer-portal.db`, `-wal`, `-shm` not gitignored – added to `.gitignore`.  
- HTTP gate authorizes by role only; engine methods still check caller‑asserted IDs, not bound to `req.userId`.  
- Stale header documentation drift; added identity note comment.  
- Missing manager‑role test coverage – full suite added.  
- Stale scheduled tasks: Blueprint OCR Batch and Zombie Reaper v2.

**DOMAIN SPECIFICS**  
- Hotel slot: ERP/HR/business specialist, branch `cad-fusion-live-ms0`.  
- Slot binding via `slot-bind-enforce.mjs`; handoff topic `hotel-work`.  
- Dispatcher pattern: lazy `getEngine("payrollLiabilityFiling")`, enum + switch case wiring.  
- 3‑of‑3 scrutiny gate (holistic, test/wiring, analyst).  
- Local Ollama models (`qwen2.5-coder:32b`, `gpt-oss:20b`).  
- Engines: `CustomerPortalEngine` (SQLite WAL), `MarketplaceSeedingEngine`.  
- Actions: `marketplace_lead_get`, `marketplace_lead_list`, `handoff_counterparty_respond`, `handoff_manager_approve`, `handoff_mark_executed`, `handoff_cancel`, `po_approve`, `hr_pto_approve`, `pto_approve_request`.  
- Dispatchers: `businessDispatchRoute` POST `/api/v1/business/dispatch`; `realtimeDispatcher` `/api/v1/realtime/*`.  
- Metrics/SSE event types via RealtimeEventBridge (`quote:update`, `job:progress`, `job:complete`, `notification`).

**TOOLS USED**  
- `scripts/mine-hotel-transcripts.mjs` (Ollama summarizer).  
- ultracode Workflow `wf_2bfa0b6b‑9b0`.  
- `businessDispatcher.ts`, `PayrollLiabilityFilingEngine.ts`.  
- Tests: `businessDispatcher.payroll-filing-wire.test.ts`, `businessDispatchRoute.test.ts`.  
- Slot‑commit‑enforce hook `[BOOTSTRAP-SLOT-ENFORCE]`.  
- PRISM core: `businessDispatcher`, `business.ts allowlist`, `RealtimeEventBridge`, `auditLog` middleware, `requireRole/verifyToken`.  
- Scripts/hooks: `chat-slots.mjs`, `slot-bind-enforce.mjs`, `/startup` pipeline, `ask-ollama.mjs`.  
- Testing: vitest (isolate:true), tsc incremental build.

**OPEN THREADS**  
- Implement identity‑to‑user mapping for handoff actions (A‑P2 follow‑up).  
- Fix payroll/PO bugs surfaced during grounding (double‑apply, empty `approved_by`, divergent PTO paths).  
- Resolve stale scheduled tasks: Blueprint OCR Batch, Zombie Reaper v2.  
- Verify that all remaining roadmap units are still valid after recent changes (e.g., allowlist size, dispatcher references).  
- No further hotel units pending; all roadmap items shipped or awaiting operator‑approved extensions.
