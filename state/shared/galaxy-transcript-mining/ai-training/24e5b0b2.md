# ai-training session 24e5b0b2 (2026-05-19, 13.5MB, spine 50KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `sfc-variability-enumerate.mjs` (mill & lathe) – 576 LOC / 470 LOC respectively.  
- `sfc-variability-batch-run.mjs` – 424 LOC, 4‑worker launcher with resume‑state sidecar.  
- `generate-sfc-variability-features.mjs` – 330 LOC, emits `state/shared/system-viz/augmentations/sfc-variability.json`.  
- `sfc-variability-cache.mjs` – 280 LOC, in‑memory lookup of chunk fingerprints.  
- `sfc-variability-resume-guard.mjs` (v4) – 753 LOC, host‑keyed lock + sidecar writer.  
- `install-sfc-variability-task.ps1` – PowerShell installer for Guard, Mill & Lathe scheduled tasks.  
- `state/shared/specs/SFC-ACCURACY-MS1-DESIGN.md`.  
- Unit tests (62) all green; integration test suite passes.  
- Commit to `slot/india` (`70938167bf` → `3d4d1b9f98`) contains 5 new files and the guard/launcher code.  

**DECISIONS**  
- Two‑task architecture: Guard runs as a separate scheduled task; batch runs in its own task to avoid child termination on job close.  
- Resume‑state sidecar (`.resume-state.json`) records `{host,pid,ts,launchSkip,workers}` for deterministic skip calculation.  
- Host‑keyed lock (`.sfc-variability-guard-<host>.lock`) prevents cross‑PC stale replays.  
- Chunk naming: `chunk-w<workerIdx>-<12‑digit startIdx>.jsonl`.  
- Default runtime: 4 workers, chunk size = 50, max minutes = 600 (mill) / 240 (lathe).  
- System‑viz augmentation format: one machine‑type roost + per‑cell leaf nodes; edges for “contains/produces”.  

**OPERATOR DIRECTIVES**  
- Register the three scheduled tasks on both PCs via `install-sfc-variability-task.ps1` (elevated PowerShell).  
- Continue running the scaled batch; monitor chunk growth (`state/shared/sfc-variability-results/mill/`).  
- Wire a lookup engine (`sfc_variability_lookup`) that mmaps the JSONL chunks, builds an index by machine_type/controller/material/op/chip‑clear/etc., and exposes it via `prism_calc:sfc_variability_lookup`.  
- Commit any new wiring code to the `slot/india` worktree.  

**FINDINGS / BUGS**  
- No active runtime bugs; all tests green.  
- Guard currently inert until scheduled tasks are registered (needs elevated PS).  
- If a batch dies before guard runs, manual resume via sidecar is required.  

**DOMAIN SPECIFICS**  
- **Engine**: `SpeedFeedOrchestratorEngine` (`mcp-server/src/engines/SpeedFeedOrchestratorEngine.ts`) – 3,575 LOC, 70+ input fields, returns `AtomicValue<OrchestratorResult>`.  
- **Enumeration**: Compatibility‑filtered Cartesian product → billions of combos; current state ~2M combos (mill) + 180K (lathe).  
- **System‑viz**: Augmentation file `sfc-variability.json` contains machine‑type roost, per‑cell nodes, and DB‑attachment edges.  
- **Cache**: In‑memory fingerprint lookup; hit rate 100 % for existing cells.  

**TOOLS USED**  
- PRISM core: GNN (GraphSAGE), LoRA, RAG, PSN, AUROC/Brier/F1 gates.  
- Scripts: `sfc-variability-enumerate.mjs`, `sfc-variability-batch-run.mjs`, `generate-sfc-variability-features.mjs`, `sfc-variability-cache.mjs`.  
- PowerShell installer (`install-sfc-variability-task.ps1`).  
- Node/TSX runtime for workers.  

**OPEN THREADS**  
1. Register scheduled tasks on both PCs (needs admin).  
2. Implement and test the lookup engine (`sfc_variability_lookup`) that consumes the chunk corpus at runtime.  
3. Scale enumeration to billions: adjust worker count / chunk size or prune dimensions if needed.  
4. Verify system‑viz integration with GNN training pipeline (GraphSAGE poolSize ≥ 2).  
5. Ensure cache persistence across reboots; consider DB fallback for very large corpora.  
6. Monitor long‑term durability: guard should auto‑resume after any machine switch or reboot without manual intervention.
