# ai-training session 578fef86 (2026-05-23, 8.2MB, spine 40KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `60009927bc`: PWA shell – `manifest.webmanifest`, icons (`icon-192.svg`, `icon-512.svg`, `icon-maskable-512.svg`, `apple-touch-icon.svg`), `sw.js`, `registerServiceWorker.ts`, updated `index.html`, and `main.tsx`.  
- Auth namespacing commit: added `useStudentId()` hook, exported helper from `AuthContext.tsx`, updated `useCourses.ts` to namespace localStorage by `employee.id`; all tests pass.  
- `src/__tests__/registerServiceWorker.test.ts` created and committed (verified via Git status).

**DECISIONS**  
- Adopt **PWA‑first** delivery vector; Capacitor wrap deferred to iter 5+.  
- Service‑worker caching: shell/assets → cache‑first, `/api/+/mcp/` → network‑first, all other routes → network‑first with offline fallback.  
- Use `AuthContext.employee.id` for localStorage key namespacing; skip new login screen (existing UI suffices).  
- Keep recurring cron job `4a64743e` (`*/5 * * * *`) to invoke `/goal` every 5 min; re‑arm after `/compact`.  

**OPERATOR DIRECTIVES**  
- Continue all remaining tasks for lima, completed and wired.  
- Session‑scoped Stop hook condition: “make expansive upgrades to the prism app training academy. | get it setup for phone (ios and android) so workers can start utilizing it soon”.  
- `/loop [1m] /goal` after `checkin-lima`.  

**FINDINGS/BUGS**  
- `pick-unit.mjs SLOT_TO_CHAT` only maps alpha…golf; lima → NaN bug – workaround via `priority‑queue.mjs --slot lima`.  
- Git lock cleanup required due to rate limiting and stale peer locks.  
- Precompact guard armed; must run `/compact` to preserve handoff.  
- Context near 880 K soft threshold; hard cap approaching.

**DOMAIN SPECIFICS**  
- Engines: `CurriculumEngine`, `AssessmentEngine`, `LessonRendererEngine` (knowledgeDispatcher.ts lines 1735‑1815).  
- Actions/dispatchers: `useCourses`, `AuthContext`, `useStudentId`, `registerServiceWorker`.  
- Metrics: CronCreate expressions, `/goal` gate, loop‑state tracking.  
- Key paths: `H:/prism/.claude/helpers/*`, `H:/prism/mcp-server/web/*`, `state/shared/handoffs/`.

**TOOLS USED**  
- `slot-bind-enforce.mjs`, `chat-slots.mjs` (claim/reclaim), `croncreate`, `per-agent-handoff.mjs`, `stable-session-id.mjs`, `milestone-tracker.mjs`, `precompact-pending-guard.mjs`.  
- Vitest for unit tests; Git commands via node scripts.

**OPEN THREADS**  
- Iter 3 content expansion of `academy.ts` (≈78.9 KB) pending.  
- Iter 4 documentation surface pending.  
- Confirm commit status of `registerServiceWorker.test.ts`.  
- Implement stale‑map fix for `pick-unit.mjs`.  
- Re‑arm `/goal` cron and handoff after `/compact`.  
- Ensure resume directive points to next iter steps (auth, content, docs).
