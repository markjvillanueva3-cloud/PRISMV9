# ai-training session 30a6a98b (2026-05-23, 57.2MB, spine 159KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- **U‑RAG‑1** – audit blind‑spot fix (`tribalWikiPath()` now counts `external:`); coverage 97.2 % (commit `e07edcbf76`).  
- **U‑RAG‑2** – two‑stage lexical reranker wired into all four inject hooks (`tribal-by-domain`, `master-index`, `memory-relevance`, `wiki-precheck`); commits `6df057e098`, `c8acc5accd`, `52b1fe91e8`, `bb1ff5f589`.  
- **U‑RAG‑3** – contextual retrieval lib (`scripts/lib/contextual-blurb.mjs`) + embed‑with‑context flag; commit `92aa9279d6`.  
- **U‑RAG‑4** – synergy wiring: system‑viz roost generator, wiki architecture entry, five per‑unit obsidian memories, GNN bridge docs; commits `8554ca7c4d`, `12182c62dd`, `8105fbf76d`.  
- **U‑RAG‑5** – retrieval evaluation harness (`RetrievalEvalEngine.ts`); commit `619e22f9cc`.  
- **PSN leg #11 wiring** – `aiReasoningDispatcher.ts → rag_rerank`; cross‑wire constant + case handler, 13 unit tests, spec row, memory entry.  
- **Fleet‑reaper sweep & preset persisted**; MCP zombie hunter added to reaper script (46 zombies reclaimed, ~39 GB freed).  

**DECISIONS**  
- Keep U‑RAG‑1 as audit fix only – avoid duplicate `wiki:` re‑embedding.  
- Wire U‑RAG‑2 across all hooks for 15–30 % retrieval lift.  
- Trigger GNN retrain (768‑d feature layer); accept node‑id → wiki‑path mapping as separate follow‑up.  
- Defer U‑RAG‑6 until remaining units complete and `/goal` gate satisfied.  
- Keep fleet‑reaper active on 128 GB host; orphan/slot cleanup, GPU coordination, stuck‑bash hunting remain essential.  
- Default to auto‑accept edits (`Shift+Tab`) for autonomous `/loop`; bypass permissions only when physically away and commit is known‑good.

**OPERATOR DIRECTIVES**  
- Use automode for autonomous `/loop` work; bypass permissions only when explicitly required.  
- Approve node‑id → wiki‑path mapping layer to enable full GNN feature usage.  
- Clear `/goal` Stop hook once all U‑RAG‑1–5, synergy wiring, and GNN retrain verified.  
- Complete remaining RAG‑UPGRADE‑MS0 units:  
  1. Run full `--with-context` wiki corpus pass (`embed-all-wiki.mjs`).  
  2. Restart node‑embedding job from scratch (current run died at ~223 K/377 K).  
  3. Build graph‑node‑id ↔ wiki‑path mapping layer (new milestone, required for future GNN retrain).  
- Verify GNN retrain uses correct node‑ID keys; re‑trigger after mapping ready.

**FINDINGS/BUGS**  
- `tribalWikiPath()` now counts `external:` ids; previous blind‑spot fixed.  
- Re‑embedding rewrite would double index – avoided.  
- GNN retrain fell back to legacy 8‑d features due to missing node‑ID → wiki‑path mapping.  
- No active concurrency issue after audit fix; `tribal-embed-index.json` writes idempotent.  
- Node‑embed job terminated during `/compact`; no resume logic – must start fresh.  
- Wiki corpus pass missing `--with-context` flag in batch driver (fixed).  
- Fleet‑reaper MCP zombie hunter reclaimed 46 zombies, freeing ~39 GB.

**DOMAIN SPECIFICS**  
- PRISM manufacturing‑intelligence platform, 26‑slot NATO chat fleet.  
- Slots: `golf` (fleet‑hygiene / reaper owner), `bravo` (current work slot).  
- Fleet‑reaper: orphan/zombie process janitor + GPU coordinator; remains active.  
- RAG upgrades: hybrid search, two‑stage rerank, contextual retrieval, edge‑ordering (“lost in the middle”).  
- Embedding stack: `nomic-embed-text` (768 d) for wiki corpus; `nv-embedqa-e5-v5` unused due to VRAM limits.  
- GNN retrain: GraphSAGE on 768‑d embeddings, AUROC gate 0.78 required.

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `/checkin` pipeline, `embed-all-wiki.mjs`.  
- Vector DB: Qdrant v1.17.0.  
- LLM inference: Ollama (5 models), NIM trimmed to fit 16 GB GPU.  
- Git for commits and state tracking.  
- Node.js test harness (`node:test`, `vitest`).  
- System‑viz generator, GNN retrain script (`graphsage-train-pipeline.mjs`), audit scripts.  
- PRISM hooks: `master-index-precheck-inject.mjs`, `memory-relevance-inject.mjs`, `wiki-precheck-inject.mjs`.  
- Fleet‑reaper helpers: `fleet-reaper-sweep.mjs`.  
- Embedder scripts: `build-node-embeddings.mjs`.  
- Dispatcher wiring: `aiReasoningDispatcher.ts`.

**OPEN THREADS**  
1. Run full wiki corpus pass with `--with-context` (`embed-all-wiki.mjs`).  
2. Restart node‑embedding job from 0 (377 K records).  
3. Implement graph‑node‑id ↔ wiki‑path mapping layer.  
4. Re‑trigger GNN retrain once mapping in place; verify AUROC ≥ 0.78 gate.  
5. Final verification of `/goal` completion and clearing Stop hook.  
6. Optional: PSN leg #2 wiring (`prism_operating_system`).  
7. Potential re‑embedding with context flag once GPU resources allow (Ollama offload).
