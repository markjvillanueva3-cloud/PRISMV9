# ai-training session a8fd9985 (2026-05-22, 28MB, spine 256KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit 9057df86f6 / 4d4a4900e7 – golf slot soul rewritten to full‑work slot (role changed, refuse_list removed).  
- Commit 81ed0d959c – merged `cad-fusion-live-ms0` into `slot/golf`.  
- Guard fix commit – corrected `claude-md-golf-only-guard` identity logic; updated `golf-slot-reaper-guardian.mjs` (429 insertions).  
- Build‑script patch – copied `cad-token-vocabulary.json` from src→dist, unblocking `cad_blueprint_generate`.  
- Retraction commit – withdrew false “blisk proof” claim; added `feedback_api_success_is_not_proof.md`.  
- U‑FR‑STUCK‑HUNT – added fleet‑reaper hunters for stuck bash, fsmonitor orphans, stale slot PIDs (3 commits, 33/33 tests).  
- Docker & Qdrant recovered; MCP connectivity restored; memory dropped from ~97 % to ~70 %.

**DECISIONS**  
- Shift golf slot doctrine to full work slot while retaining fleet‑reaper ownership.  
- Apply home‑tier preset: RTX 4080 + 64 GB.  
- Wire `CADToSTEPPipelineEngine` into `cadDispatcher.ts` (enum entries + case handlers).  
- Prioritize CAD training; prove blisk drawing via print→CAD→print comparison table.  
- Highest ROI: fix build asset, add CAD‑to‑drawing emitter, restart MCP, run full roundtrip.  
- Prioritize infrastructure cleanup over CAD proof due to machine saturation; handoff remaining CAD/blisk goal after stabilization.

**OPERATOR DIRECTIVES**  
- Change golf settings to allow operation like any other chat slot.  
- Ensure fleet‑hygiene monitors and memory‑pressure helpers active (slot‑bind‑enforce, fleet‑reaper).  
- Run `/compact` first; clear orphan nodes, bash, git processes in Task Manager.  
- Upgrade fleet reaper to hunt stuck shells, fsmonitor orphans, stale slot PIDs.  
- Verify Docker, NIM, Qdrant running post‑compact.

**FINDINGS/BUGS**  
- `golf-slot-write-allowlist.mjs` unwired; only soul restriction existed.  
- `claude-md-golf-only-guard` identity bug – strict ID check never matched (`claude-a8fd9985` vs `claude-8852f3dc`).  
- `cad-token-vocabulary.json` missing from dist; build script fails to copy assets.  
- `CADToSTEPPipelineEngine` exists but not wired in `cadDispatcher.ts`; no CAD→drawing emission action present.  
- `part_template_cylinder` produced degenerate output (`radius: null`).  
- NACA 6‑series parser accepts only 4‑digit codes; “65‑010” unsupported.  
- `cad_pdf_blueprint_extract` returned no dimensions until blueprint‑style text used.  
- Memory pressure ~95–97 % caused Docker daemon failures and MCP timeouts; resolved after freeing RAM and restarting.  
- Qdrant unhealthy → MCP disconnects; fixed once Docker healthy.  
- NIM pull failed from corrupt cache layer; cleared cache, still needs headroom.

**DOMAIN SPECIFICS**  
- Engines: `cadDispatcher`, `CADToSTEPPipelineEngine`, `DimensionalSignatureEngine`, `PDFBlueprintDimensionExtractorEngine`, `BlueprintExtractionRAGEngine`, `LathePrintIngestPipelineEngine`, `GroundTruthFeatureTreeExtractor`.  
- Actions (`cadDispatcher.ts`): `cad_dimensional_signature`, `cad_to_step_run`, `cad_to_step_batch`, `cad_to_step_extensions`; pending CAD→drawing emit action.  
- Metrics: fleet‑reaper task health, memory pressure thresholds (90 % floor), GPU floor (2 GB), dimensional comparison table for print→CAD→new‑print proof.  
- Paths: `H:/prism-slot-golf`, `H:/prism/mcp-server/src/tools/dispatchers/cadDispatcher.ts`; unique path `U-CAD-DRAWING-EMIT-FROM-CAD-GRAPH`.

**TOOLS USED**  
- PRISM helpers: `slot-bind-enforce.mjs`, `chat-slots.mjs`, `per-agent-handoff.mjs`, `precompact-pending-guard.mjs`, `stable-session-id.mjs`.  
- Hooks/skills: `claude-md-golf-only-guard.mjs`, `golf-slot-reaper-guardian.mjs`, `/fleet-reaper-home`, `/fleet-reaper`.  
- Scripts: `node H:/prism/.claude/helpers/milestone-tracker.mjs`; `npm run build:*`; manual copy of data assets.  
- Testing: `node:test` with `node:assert/strict`.

**OPEN THREADS**  
- Wire `CADToSTEPPipelineEngine` into `cadDispatcher.ts` (enum + case handlers).  
- Implement dispatcher action that walks CAD graph and emits drawing/blueprint (`CAD→drawing emitter`).  
- Fix build script to permanently copy all required data assets (`cad-token-vocabulary.json`, etc.).  
- Restart MCP server after asset copy so new modules load correctly.  
- Run full roundtrip: `cad_pdf_blueprint_extract` → `cad_blueprint_generate` → CAD→drawing emit → comparison table vs original print; validate with small JM Die part then blisk.  
- Commit wiring and test changes to slot/golf (`cad_to_step_run` tests using `PRISM_CAD_MOCK=1`).  
- Update CLAUDE.md and wiki once proof closed.  
- Finalize NIM pull after ensuring sufficient memory.  
- Ensure fleet‑reaper fully hunts stuck shells, cleans stale slots; resolve remaining Docker/Qdrant/MCP issues.
