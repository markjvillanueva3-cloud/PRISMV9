# ai-training session cd8e1622 (2026-06-03, 12.9MB, spine 98KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Extended `scripts/lib/code-path-resolver.mjs`: O(1) node↔path, added `type`, `byCode`, optional line, repo‑root-relative `repoPath`. Commits: 33753f4c67, ffcfdb2b5d, 754626f63f; bulk changes absorbed into peer commit 155902c36e.  
- Added `scripts/lib/nav-savings-ledger.mjs` (`recordNavHit`, telemetry via SOURCES.nav).  
- Created `scripts/lib/graph-exact-match.mjs` (exactMatchHit, navPathLine, exactMatchBanner).  
- Updated four PreToolUse hooks: master-index-precheck-inject, pre-bash‑graph‑inject, pre-grep‑graph‑inject, pre-write‑graph‑inject.  
- Implemented `/nav` skill using resolver & helper.  
- Tests: 11 resolver, 6 ledger, 20 hook, 12 helper – all green (~95 unit tests total). All 3‑of‑3 scrutiny gates cleared.

**DECISIONS**  
- Pivoted to extend existing resolver after /dedup flagged near‑duplicate; ship full nav‑inject feature instead of new cross‑lane units.  
- Adopt O(1) resolver using CODE_SYSTEM_INDEX.json + mtime cache; add repoPath sentinel.  
- Introduce nav‑savings ledger with credit‑on‑emit gating.  
- Use exactMatchHit to collapse duplicate paths in graph injections.  
- Wire hooks into PreToolUse/Stop for automatic `Read <repoPath>` injection.  
- Employ YOLO mode loop (5 min) with RTK prefix, Ollama offload (qwen2.5‑coder:7b), /system‑viz query first.  
- Enforce duplicationGuardEngine.mustCheckBeforeCreating(); 2‑agent scrutiny per file + 3-of‑3 gate at task end.  
- Route MS0 gaps U-P3-FORGE-OLLAMA-CODEGEN & U-P1-QDRANT-EPIRIC-RECALL to alpha and juliett/india slots.  
- Plan Sierra dedicated slot worktree after next session.

**OPERATOR DIRECTIVES**  
- `priority‑queue.mjs --pick --slot sierra` – pick next Sierra‑lane unit post‑handoff.  
- Stop cron `def83581` if 5 min /yolo loop no longer desired or context limit reached.  
- Run `/goal /loop [5m] /yolo-mode` to continue autonomous work; re‑arm YOLO loop as needed.

**FINDINGS/BUGS**  
- Shared‑tree absorption race: peer commit 155902c36e absorbed bulk changes, attribution mixed.  
- `.git/index.lock` contention (~100 s) indicates hung git process.  
- Repo‑root-relative path bug fixed (`repoPath` now points to `mcp-server/...`).  
- Credit‑on‑emit bug in pre‑bash resolved; credit only when banner actually emits.  
- Envelope drift audit: SYSTEM‑VIZ‑BRAIN‑MS0 claimed completed but real state 24/26 (2 done‑untracked, 2 genuine gaps).  
- E2E exact‑match “Read” line test blocked by environment (master‑index returns no single‑token hits).

**DOMAIN SPECIFICS**  
- Engines/actions: `code-path-resolver`, `nav-savings-ledger`.  
- Hooks: master-index-precheck-inject, pre-bash-graph-inject, pre-grep-graph-inject, pre-write-graph-inject, stop-psn-savings-aggregate.  
- Metrics: PSN savings token counts, hit counts (`byLedger.nav`).  
- Paths: `H:/prism/.claude/...`, `scripts/lib/`, `knowledge/wiki/architecture/node-path-template.md`.  
- System‑viz (sierra) domain: 3D map, canonical roadmap, master-index awareness.  
- Nav-savings ledger stored in `state/shared/dashboards/nav-savings-ledger.jsonl` with size cap.

**TOOLS USED**  
- PRISM core: `/dedup`, `/nav`, `graph-exact-match.mjs`, `nav-savings-ledger.mjs`.  
- RTK prefix (`rtk git`, `rtk vitest run`).  
- Ollama offload via `/ollama-*` skills (qwen2.5‑coder:7b).  
- System‑viz query (`node H:/prism/scripts/system-viz-query.mjs find`).  
- Wiki & memory queries (`/wiki-query`, `prism_memory:semantic_search`, `prism_knowledge:tribal_search`).  
- Master‑index awareness (`prism_session:master_index_query`).  
- Duplication guard engine (`duplicationGuardEngine.mustCheckBeforeCreating()`).  
- Scrutiny gates (`node H:/prism/.claude/scripts/scrutiny-3way.mjs`, `loop-state.mjs` for YOLO loop).

**OPEN THREADS**  
- Cross‑lane MS0 gaps routed to alpha and juliett/india; no pending work for Sierra.  
- Envelope-status correction (mark two done‑untracked units as in‑progress).  
- Migration of Sierra to dedicated slot worktree planned after next session.  
- Execute `/compact` to clear context before next session.  
- Re‑arm YOLO loop with `/loop 5m /yolo-mode` if further autonomous work desired.  
- Verify nav skill integration in production environment (optional).
