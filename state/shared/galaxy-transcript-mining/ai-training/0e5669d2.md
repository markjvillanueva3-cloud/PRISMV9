# ai-training session 0e5669d2 (2026-06-10, 44.9MB, spine 372KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `da013c4406` – Obsidian‑vault audit + cheap‑token node‑access map persisted (wiki, galaxy brain, two memories, handoff).  
- `9e4376b3b2` – U‑VAULT‑RAG‑WIRE keyword recall hook wired & live‑fire verified.  
- `168c202646` – B1 sync‑resilient: added `writeWithRetry()`, guarded `ensureDir()`, exported helper.  
- `ea21008f25` – B2 index‑meta stamp update (770→1128) + safe `refresh()` in `WikiIndexMaintainerEngine`.  
- `8c4dff660a` – B3 maint‑cron installers (promote & rot‑sentinel) built, tested, disabled for migration safety.  
- `984313825e + d948b85a74` – link‑heal scorer recalibration: minLen=8, ratio guard, short‑token demotion; auto‑apply reduced from 14 100 → 2 links.  
- `838123429b` – C3 tribal‑coverage audit correction (31.5 % → 83.7 %).  
- `45225e0cc3` – Persisted fix‑memory to C:/ & H:/ mirrors; durable build record.  
- **MCP‑FLEET‑CAPACITY-MS0** – portable-node heap cap 384 MB, MCP `maxConnections=512`, GPU offload patterns widened (≈30 % target).  
- `3d796dcf5c` – fleet‑task‑health watch: banner noise removed, drift‑sync added, WSL guard recorded.  
- `96ed5222e2` – vault‑backlink schema & builder (29 k keys → 1.52 M edges).  
- `0e2724871a` – regen‑viz wire: auto‑refresh of `vault‑backlinks.json` on graph regen.  
- `14aba14e3a + 4a44b5393c` – `doc_nodes` dispatcher, schema, tests (15/15 pass).  
- `f23c58b42d` – vault‑access map updated to close reverse‑edge gap.  
- `2d49bf0d33` – canvas‑read lib & CLI with staleness flag; 15/15 tests.  
- `47fe91554f` – access‑map doc: `.canvas` row closed, stale “memory‑rag‑inject” claim fixed.  
- `7ec4a5ea02` – U‑OLLAMA‑PIPELINE‑INJECTOR wired to `settings.json`.  
- `65a29220e5` – U‑OLLAMA‑PREWARM‑WIRE activated for pipeline models.  
- `6fb278a2ee` – U‑TRIBAL‑QA‑LLM‑UNBLOCK added (Ollama Q‑A via `callOllama`, `resolveSynthesisModel`).  
- `f1b69db664` – memo‑extract throttle fix: per‑session rate file replaces fleet‑global throttle; transcript size proxy bug removed.  
- `1ecf50b6be` – user‑type mismatch fix in memo hook (`user` vs `human`).  
- `2c766b9308` – U‑NAV‑ACCEL‑GAP‑A‑FALSIFIED spec hardening.  
- `9ce1ea6633` – SHIPPED flag added.  
- `b5d249f4f5` – U‑WEEKLY‑SYNTH‑RESOLVER committed.

**DECISIONS**  
- Use `/checkin‑sierra` wrapper to enforce slot binding before standard `/checkin`.  
- Persist recovered Obsidian context on four durable surfaces (wiki, galaxy brain, two memories, handoff).  
- Wire `memory-rag-inject` globally; verify live fire before commit.  
- Resolve P0 RAG‑wire by editing global `settings.json` and mirroring to H:.  
- Adopt guard‑first build strategy for sync‑resilient & index‑meta.  
- Accept partial candidates file (`processed:100`) as honest; add `coverageComplete:true` if time permits.  
- Leave maint‑cron installers disabled until migration completes.  
- Harden scorer with minLen=8, ratio guard to avoid overconfidence on short edits.  
- Cap hook node heap from 4 GB → 384 MB via `portable-node` wrapper (`PRISM_HOOK_HEAP_MB`).  
- Raise MCP server `maxConnections` to 512 for 26 slots + agents.  
- Add pressure status to fleet‑task‑health; treat transient codes as non‑failing.  
- Widen GPU offload patterns (≈30 % target).  
- Do not raise hook heap beyond 384 MB; keep existing offload for synthesis scripts (32 B model).  
- Keep Ollama integration without widening router to 30 % (premise falsified).  
- Prioritize T1 U‑VIZ‑WIKI‑NARRATIVE → T2 U‑OLLAMA‑PIPELINE‑INJECTOR‑WIRE → T3 U‑WEEKLY‑SYNTH‑RESOLVER.  
- Deploy next unit: Gap B semantic reranker after memo‑extract & user‑type patches.

**OPERATOR DIRECTIVES**  
- “Fill all gaps” (B1–B3, C2–C3).  
- Persist recovered context for Sierra’s domain.  
- Map cheap‑token paths to every Obsidian vault node.  
- Fetch and summarize new X article (`https://x.com/humzaakhalid/status/2062821175546515504`).  
- “Utilize Ollama for better task efficiency when viable.”  
- Keep improving system synergy for higher token savings throughout the system.  
- Deploy Gap B – semantic reranker integration after memo‑extract & user‑type patches.  
- Verify `callOllama` with `num_predict≥1024`, timeout ≤180 s; maintain kill‑switch `PRISM_OLLAMA_PIPELINE_INJECT=0`.

**FINDINGS/BUGS**  
- Memory recall wiring: injector disabled + keyword fallback unwired → no recall.  
- `obsidian-memory-sync.mjs` unguarded `ensureDir()` & `writeFileSync`; missing exported helper.  
- `WikiIndexMaintainerEngine.formatIndex` stale frontmatter (`last_verified`, “Total entries”) never updated.  
- Link‑heal scorer over‑confidence on structural matches + short Levenshtein ≤2 edits → auto‑apply eligible; decay‑leak for >60‑char slugs allowed scores ≥0.85.  
- Stale candidates JSON (autoApplyCount:14100) still armed; full‑corpus run OOM’d, leaving only 100 links processed.  
- Overcommit from 4 GB hook heap caused false MCP‑failing alert.  
- Short‑token edit overconfidence bug fixed by minLen & ratio guard.  
- False‑positive pressure code classification (0x800710E0) mislabelled as failing; corrected.  
- Offload coverage too low (~8 %); widened patterns increased GPU routing.  
- Commit charge now 70 %; banner noise removed; drift‑sync added.  
- WSL balloon reclaimed automatically via `autoMemoryReclaim`; manual shutdown confirmed.  
- Lgit layer regex bug fixed (Lgit miscounted as “other”).  
- No data loss from `--amend` on shared tree; attribution issue noted but left unchanged.  
- Three fleet‑wide defects fixed in memo‑extractor: transcript size proxy, per‑session throttle, user turn drop.  
- ExecSync curl injection vulnerability removed; switched to `callOllama`.  
- User turns dropped (`type:"human"` only) – corrected.  
- TS7016 errors on `.mjs` imports suppressed with `@ts-expect-error`.

**DOMAIN SPECIFICS**  
- Engines/dispatchers: `WikiIndexMaintainerEngine`, `obsidian-memory-sync.mjs`, `memory-rag-inject` hook, `node-card-prefetch-inject`, `system-viz-query.mjs`, `build-wiki-leaf-index.mjs`, `promote-memory-to-wiki.mjs`, `vault-rot-sentinel.mjs`, `regen-viz.mjs`, `doc_nodes` dispatcher, `canvas_doc`, `node_card`, `U‑OLLAMA‑PIPELINE‑INJECTOR`, `U‑TRIBAL‑QA‑LLM‑UNBLOCK`, `WeeklySynthesisEngine`.  
- Hooks: `fleet-task-health-watch.mjs`, `regen-viz.mjs`, `callOllama`, `resolveSynthesisModel`, `ask-ollama.mjs`, `host-aware-synthesis-model.mjs`, `canvas-read-lib.mjs`, `master-index-search-lib.mjs`, `tribal-rerank.mjs`, `node-card-prefetch-inject.mjs`.  
- Scripts: `build-vault-backlink-index.mjs`, `generate-vault-graph.mjs`, `host-aware-synthesis-model.mjs`, `ask-ollama.mjs`, `stop-obsidian-memory-extract.mjs`, `resolveSynthesisModel`, `canvas-read-lib.mjs`, `master-index-search-lib.mjs`, `tribal-rerank.mjs`.  
- Metrics/paths: cheap‑token read path (~136 tokens vs 186K), frontmatter timestamps, `last_verified` dates, node‑card offset index (`seekCard(id)` ~200 tokens), commit charge 70 %, vmmemWSL reservation 95 GB reclaimed, MCP `maxConnections=512`, portable-node heap cap 384 MB, GPU offload ratio target ≈30 %.  
- Unique paths: `H:/prism/.claude/settings.json`, `C:/Users/.../.claude/settings.json`, `mcp-server/src/engines/system-viz/MEMORY.md`, `node-card-offsets.json`.

**TOOLS USED**  
- PRISM helpers: `/checkin‑sierra`, `slot-bind-enforce.mjs`, `chat-slots.mjs`, `PRISM_GIT_ADD_LANE_DISABLE`, update-config skill.  
- Git lane guard, main-tree-write-block.  
- Node testing tools: `vitest`, `jest`, `node:test`.  
- Ultracode workflow orchestration.  
- Scrutiny gate (2‑arm reviewers), scrutiny scripts.  
- Portable-node wrapper (`portable-node.sh`/`.cmd`) with `PRISM_HOOK_HEAP_MB`.  
- MCP server (`mcp-server/src/index.ts`).  
- Fleet-task-health watch script.  
- Ollama offloader engine & hooks, dispatcher action `node_card`.  
- `callOllama`, `ask-ollama.mjs`, `resolveSynthesisModel`, `host-aware-synthesis-model.mjs`.  
- esbuild, tsc strict, vitest.

**OPEN THREADS**  
1. Finalize scorer min‑length floor for short Levenshtein over‑confidence (monitor).  
2. Implement streaming/chunked suggester to avoid full‑corpus OOM and produce `coverageComplete:true` file.  
3. Enable maint‑cron installers post‑migration (currently disabled).  
4. Verify partial candidates file (`processed:100`) acceptable or replace with larger batch before deployment.  
5. Wire U‑OLLAMA‑PIPELINE‑INJECTOR hook (already wired; ensure integration).  
6. Implement Gap B semantic reranker after BM25; test and monitor.  
7. Register vault crons (Memory Promotion, Rot Sentinel) when ready.  
8. Validate live after MCP restart; target GPU offload ratio ≈30 %.  
9. Monitor staleness flags on regenerated indices (canvas, reverse edge).  
10. Verify token‑savings gains from new cheap node‑access paths in production runs.  
11. Follow‑on unit for reviewer P2 issue: `num_predict` unset → empty `.response`.  
12. Address migration‑freeze gated item #7 (no action yet).  
13. Finalize expected‑unregistered task handling to eliminate banner noise.
