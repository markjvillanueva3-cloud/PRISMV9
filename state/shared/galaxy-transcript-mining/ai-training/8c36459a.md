# ai-training session 8c36459a (2026-06-03, 5.3MB, spine 36KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `f406d71c08` – U‑CAM‑RETRAIN‑LIFECYCLE (WRITE side) closed the self‑improving loop.  
- `5050abf060` – WIP U‑CAM‑SELFLEARN‑PERSIST cleaned and committed.

**DECISIONS**  
- Keep operator‑pin logic in `fusion-kilo-claim.json`; never override with auto‑detect.  
- Resolve Fusion instance claim to :18361 (old add‑in) after confirming :18362 is delta’s CAD.  
- Use offline static probe (`LiveProbe`) + `compile(recipe, decisionRules, probe, toolLib)` to generate the 5‑axis program without Fusion.  
- Treat UPSET recipe as the hard target; build missing rungs bottom‑up in its order rather than starting a new part.  
- Adopt a two‑phase self‑improving loop: LOAD side (U‑CAM‑SELFLEARN‑PERSIST) + WRITE side (U‑CAM‑RETRAIN‑LIFECYCLE).  

**OPERATOR DIRECTIVES**  
- `/goal utilize workflow, parallel agents, ollama, obsidian app, psn, /system-viz + hermes agent to continue comprehensive closed loop learning and blind navigation of fusion cam | goal clear: generate accurate 5 axis program on the UPSET file we've been using`.  
- `delta claimed one of the two instances of fusion open so claim the other one /yolo-mode`.  
- `/goal utilize workflow to assess what else we need`.

**FINDINGS/BUGS**  
- Resolver auto‑detect incorrectly picked :18362; fixed by restoring operator pin.  
- `scoreGeneratedVsCorpus` expected op objects, not strings – caused test failures; corrected.  
- Honesty reporting bug: `netSatisfied` mis‑calculated; fixed with Condorcet‑cycle test.  
- Duplicate skip reason (`invariant‑violation:invariant‑violation`) – cosmetic fix.  
- 3 Fusion bridge ports live; only :18361 safe for CAM authoring.  
- Offline loop fidelity 0.9376 (80/200 inversions).  
- Self‑improving loop works on real data at relaxed thresholds, but production threshold (0.75) yields no-op.

**DOMAIN SPECIFICS**  
- Engines: `CAMDriveRecipeEngine.ts`, `cam-drive/` folder, `U-CAM-RETRAIN-LIFECYCLE`, `U-CAM-SELFLEARN-PERSIST`.  
- Actions/dispatchers: `fusion-claim-instance.mjs`, `fusion-kilo-claim.json`, `audit-roadmap-drift.mjs`, workflow `wf_a8c6ac3a-a46`.  
- Metrics: mean sequence fidelity, inversions, 101/101 planner count, 15‑family matrix.  
- Paths: `H:/prism-slot-kilo` (slot/kilo worktree), `H:/prism/.claude/helpers/chat-slots.mjs`, `state/shared/fusion-instance-claims.json`.

**TOOLS USED**  
- PRISM helpers: chat‑slots, audit‑roadmap‑drift, system‑viz ping, local‑compute health, fleet activity.  
- Workflow engine (`wf_a8c6ac3a-a46`).  
- Node.js scripts (`fusion-claim-instance.mjs`, `fusion-kilo-claim.json`).  
- Grep (`rg`), Git, TSX runner for offline compile.  

**OPEN THREADS**  
1. **Phase 0 – Offline compile of UPSET recipe**: build static `LiveProbe` from recorded geometry and run `compile(recipe, decisionRules, probe, toolLib)` to produce a fully‑resolved 17‑step program.  
2. **Phase 1 – Fill milling gaps** identified by the compiler (tool‑axis resolution, WCS setup, parting strategy).  
3. **Phase 2 – Live drive**: restart Fusion on kilo’s :18361 with new `PRISM_Fusion_Drive` add‑in, post Okuma NC once program is validated offline.  

These are the next actionable items to finish the 5‑axis goal.
