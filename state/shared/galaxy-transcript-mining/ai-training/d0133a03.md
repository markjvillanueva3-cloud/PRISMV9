# ai-training session d0133a03 (2026-06-10, 54.6MB, spine 498KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit 23ad2cdf3e – 5 golf‑owned units shipped (CLAUDE.md, tournament-rank.mjs, intake-quarantine-guard.mjs, save-workflow.md, ULTRACODE‑SYNERGY). Tests: 28 unit + 4 live integration; 20/20 dispatcher E2E, 40/40 full suite.  
- Commits 037e3ac930 & 3fa529432f – GroupRelativeRewardNormalizerEngine wired to `prism_ai:group_normalize_reward`.  
- Commit 46553bb74a – added `rankTrajectories()` to MultiModelConsensusEngine, dispatcher `prism_ai:rank_trajectories`.  
- Fact‑checker agent written (.claude/agents/fact-checker.md).  
- Commit cebde4fd94 – OLLAMA_KEEP_ALIVE set 30m, MAX_LOADED_MODELS capped.  
- Spec drift corrected (accf6f247f); orders 3–5 marked SHIPPED; MCP Priority Guardian registered (49 tasks).  
- Commit 20181a4c78 – GWizardToolCribExportEngine: 11/11 round‑trip tests, wired to `prism_calc:gwizard_export_toolcrib`.  
- Commit df6bf7a4d1 – CSV escaping hardening; newline split bug fixed.  
- Commit 27-wsl-memory-guard.mjs – WSL Memory Guard now runs every 15 min + AtStartup.  
- Commits 44c41ee643 & a6d80537cf – FusionMachineLibraryExportEngine: 1082 `.machine` files round‑trip verified against Autodesk XML goldens.  
- Commit 4ab181a78d – HolderSelectionEngine loads 643 branded holders, exposes `select()`.  
- Commits 82ca289ef4 & 7783cbef3b – Fusion & Mastercam tool exporters wired to real holders; dispatcher round‑trip passes (7/7, 8/8).  
- Commit 6064ace7a4 – Tool export cap lifted for hyperMILL & Mastercam (5 000 → 100 k tools).  
- Commits 592133f902, 4a2f84eacc, 6e0c8cba8c, ed312a4d91 – HyperMill per‑tool SFC data wired; holder integration; 643‑holder DB materialized; Mastercam `exportFromTools` added.  
- Commits 361c64da28 & 818870ea59 – JMCustomerVendorDatabaseEngine → prism_business (8 actions); DocuStrataMaterialPriorEngine → prism_data (5 actions).

**DECISIONS**  
- Fleet‑reaper ownership moved to golf; `/checkin-golf` always runs reaper.  
- Slot binding: reclaim stale slots, force‑take golf, `preferSlot golf`; disable legacy allowlist via PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1 or removal.  
- Use `fleet-reaper-sweep.mjs --once --json` for metrics (`slots["owned‑by‑crashed"]`, `gpu.freeMb`, `ollama.reachable`, `coordinator.hintWritten`).  
- Wire GroupRelativeRewardNormalizerEngine into aiReasoningDispatcher; add action enum `group_normalize_reward`.  
- Add dedicated `rankTrajectories()` method to MultiModelConsensusEngine.  
- Resolve memory pressure by bounding Ollama keep‑alive to 30 min, not WSL shutdown.  
- Register MCP Priority Guardian to prevent priority inversion.  
- Centralize holder lookup with single HolderSelectionEngine; wire into Fusion, Mastercam, HyperMill exporters.  
- Organize tooling DB as material→tool type→brand; expose `select()` for real holders.  
- Extend HyperMillToolExportEngine.buildNCTool to use holderSelectionEngine.select(); add Mastercam.exportFromTools API.

**OPERATOR DIRECTIVES**  
- Claim golf slot normally (no `--golf` flag); ensure fleet‑reaper running and monitor.  
- Bypass legacy allowlist hook before writes in golf.  
- Build next ULTRACODE order; continue after critical‑pressure handling.  
- Apply all fixes, look for gap fills, review previous sessions for context.  
- `/loop [5m] /goal …` with romeo slot bound; confirm MCP up, read HSMAdvisor `settings_v2.xml`.  
- `/loop [10m] /goal`: use strongest Ollama LLMs (gpt‑oss:120b, qwen2.5‑coder:32b) for searches/reads/grunt work; complete all remaining romeo tasks and maximize DB benefits across app.  
- Persist directive: add all tool holder, tooling, inserts, machine databases to Fusion, HyperMill, Mastercam, HSMAdvisor, GWizard, PRISM SFC, mill/lathe wizards, CAD/CAM galaxies (Fusion‑prioritized).

**FINDINGS / BUGS**  
- Durable Fleet Reaper task disabled; re‑enabled hygiene cluster (9/9 ready).  
- `tournament-rank.mjs` seed‑bias bug fixed.  
- BashDanger bypass regex updated to block `node -e`, `cp` of credential dotfiles, `$*_KEY`.  
- `group_advantage` field persisted after makeInput fix; simplified ok flag logic in normalizer base().  
- Index‑lock caused single-file commit; resolved by re‑staging all files.  
- Critical gate triggered by Ollama keep‑alive -1; fixed with 30 min TTL.  
- WSL commit reservation not root cause; hog was 4 large Ollama models (~70 GB).  
- MCP server flapped; Priority Guardian handles priority but does not restart server (watchdog remains).  
- Orphan node process removed.  
- G‑Wizard export comment‑close bug (`*/` in glob path) fixed.  
- WSL guard installer missing token fixed by adding header.  
- MCP server crash‑loop root cause unknown, owner flagged.  
- HSMAdvisor adapter reads only single-tool state; no bulk library reader.  
- HyperMill `tool_length` floor clamp bug resolved; `coatingMult` substring shadow bug fixed with longest-key-first logic.  
- Slug collision in type/brand partitioning addressed via injective slugs.  
- ISO segment theoretical collision mitigated.

**DOMAIN SPECIFICS**  
- Engines: GroupRelativeRewardNormalizerEngine, MultiModelConsensusEngine, GWizardToolCribExportEngine, WSLMemoryGuardEngine, FusionMachineLibraryExportEngine, HolderSelectionEngine, FusionToolExportEngine, MastercamToolExportEngine, HyperMillToolExportEngine, UltimateSpeedFeedEngine, JMCustomerVendorDatabaseEngine, DocuStrataMaterialPriorEngine.  
- Dispatchers: aiReasoningDispatcher (`group_normalize_reward`), prism_ai (`rank_trajectories`), calcDispatcher (`gwizard_export_toolcrib`), camDispatcher (`prism_cam`), businessDispatcher (8 actions), dataDispatcher (5 actions).  
- Metrics: fleet‑reaper sweep JSON (`slots["owned‑by‑crashed"]`, `gpu.freeMb`, `ollama.reachable`, `coordinator.hintWritten`); 28 unit + 4 live integration tests; 20/20 dispatcher E2E, 40/40 full suite; 19/19 round‑trip Fusion machine library; 11/11 HolderSelectionEngine; 14/14 engine tests + 7/7 Fusion tool export; 8/8 Mastercam.  
- Paths: `H:/prism/.claude/helpers/chat-slots.mjs`, `scripts/fleet-reaper-sweep.mjs`, `.claude/settings.json`, global `CLAUDE.md`, `resources/FUSION360/hsm-posts/res/Machines/`, `C:/Users/wompu/.ollama/models`.

**TOOLS USED**  
- PRISM helpers: `/checkin-golf`, chat‑slot helpers (`reclaim`, `claim`), slot-bind-enforce.mjs, preToolUse hook `golf-slot-write-allowlist.mjs`.  
- Scripts/hooks: `fleet-reaper-sweep.mjs`, WSLMemoryGuardEngine, GWizardToolCribExportEngine, calcDispatcher, camDispatcher.  
- Build/test tooling: TypeScript compiler (`tsc`), Jest, vitest, esbuild, tsx, Git (commit staging/locking).  
- LLMs: gpt‑oss:120b, qwen2.5‑coder:32b; Playwright for ultracode extraction; forge7 skill; dynamic workflow tool.  
- Monitoring: Fleet Reaper, Memory Pressure Auto‑Relief, MCP Server Watchdog, MCP Priority Guardian.

**OPEN THREADS**  
- Cross‑slot units (India GRPO normalizer, Sierra fact‑checker) ready specs but not built; operator decision pending.  
- Operator‑scope tasks disabled – may need re‑enable per policy.  
- gpt‑oss:120b pull stalled at 99 % – relaunched (PID 536).  
- 2,733 unpushed commits remain; SessionStart `git-sync-stop` will push automatically.  
- U‑FGC‑2 git churn >90 % target unmet flagged for operator policy.  
- MCP Priority Guardian registered; no further action unless MCP restarts required.  
- HSMAdvisor tool‑export write‑back pending bulk library format or new reader.  
- MCP server crash‑loop root cause investigation ongoing.  
- Verify SFC/Mill/Lathe wizard wiring for tool libraries (pending final checks).  
- HyperMILL holder wiring pending missing shank/taper data.  
- Organize tooling libraries material→tool‑type→brand across full catalog; replicate to Mastercam/hyperMILL exporters.  
- Monitor remaining tsc errors in legacy modules.
