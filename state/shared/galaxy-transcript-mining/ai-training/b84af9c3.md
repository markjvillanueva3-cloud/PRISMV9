# ai-training session b84af9c3 (2026-06-23, 31.2MB, spine 295KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `a2a3b793ff` – U‑XPROC‑ATTN‑DIM‑STALE‑FIX (INPUT_DIM → exported constant 144)  
- `22d4536e91` – U‑DEEPAI‑SUGGEST‑TIMING‑FIX (Date.now→performance.now, fallback added)  
- `efc891c3af` – U‑DEEPAI‑SUGGEST‑PIN (new non‑zero suggestion pin test)  
- `94ae9f7fa` – U‑HOOKEXEC‑API (getHook/getAllHooks/… + execution count API)  
- `8af8b856b2` – U‑HOOKEXEC‑STATS‑ORACLE (strengthened stats test)  
- `c6c3d77bf9` – U‑SELFAWARE‑DRIVE‑AWARENESS (`getFullDriveAwareness` + oracle)  
- `836c9bd937` – U‑VID‑02 extractPlaybookRules (real cue‑phrase extractor, oracle test)  
- `ad65e6c5f7` – U‑SELFAWARE‑EXPORT‑CLASS (PRISMSelfAwarenessEngine class export)  
- `2864dddba6` – retired 114‑failing fossil PRISMSelfAwarenessEngine tests; ported 19 real‑value tests (23→57)  
- `c692ac0292` – wiki lesson on fossil‑reconciliation playbook  
- `46c6ffa7a5` – added 7 coverage‑fill tests for searchJMDieCustomer, getJMDieProgramPaths, getFullDriveAwareness  
- `a3e0117b28` – fixed basename assertion in PRISMSelfAwarenessEngine test (P1)  
- `86df6d9fae` – corrected LearningLoopEngine.clearAll() to set `initialized=true`  
- `4ff03e9f7b` – added floor (`>=3`) to containsSimilar match‑ratio  
- `e5808b26fb` – fully wired CROSS-DOMAIN-RAG-FEDERATION-MS0 engine, dispatcher case, 28 real‑value tests  
- `b716e0414e` – loraComposition flake fixed (awaited handler promise; removed 50 ms timer race)  
- `0ca453bddf` – GNN embedder stack: `scripts/embed-nodes-gpu.py`, `build-node-embeddings.mjs`, `quantize-vecs-to-ghost-embeddings.mjs`; tests & docs  
- `b637e0be78` – U‑AUTOCONSENSUS-TEST-WORKTREE-FIX (dead worktree hooks repointed, 23/23 tests green)  
- `884542bc` – U‑XPROC-ORCH-FANOUT-HONESTY (fan_out_mode flag added to orchestrator; 29/29 tests green)

**DECISIONS**  
- Alpha’s FEATURE‑ROUTING arc already shipped → no action.  
- India loop: descend NEVER‑IDLE ladder only after all rungs dry; loop now exhausted.  
- Auto‑compact reset at YELLOW token budget; pause further builds until context cleared.  
- Fossil realignment deferred to dedicated session; current export‑class fix retained.  
- LoraComposition flake and OllamaObsidianRag wiring remain deferred (cross‑domain/settings sensitivity).  
- Retire stale fossil tests exercising dead API paths; port live coverage instead.  
- Wire dispatcher cases only after `validateActionParams` confirms no orphan/duplicate actions.  
- Apply floor (`>=3`) to containsSimilar match‑ratio to avoid false positives.  
- Delegate heavy, context‑intensive work (loraComposition flake, GNN retrain) to fresh‑context agents per operator directive.  
- Checkpoint after exhausting all bounded rungs; defer open‑ended or high‑budget items.  
- GNN full‑coverage requires 1024d migration + GraphSAGE checkpoint → deferred to next window.  
- Adopt mxbai ghosts only if selective gate clears (currently does not).  
- Orchestrator’s defaultInvoker stub hazard mitigated with `fan_out_mode` flag; magic count 424 replaced by invariant (actual 426).  
- Wiring the 10‑tier engines requires heterogeneous adapters → multi‑session research unit, deferred.

**OPERATOR DIRECTIVES**  
- Continue building; finish any in‑flight units.  
- Complete FEATURE‑ROUTING model‑routing “both” arc: live router coding→Sonnet, octopus picker, graph reconcile (localEnsembleWired=true, drift=clean).  
- After that, descend NEVER‑IDLE hunt ladder into backlog.  
- One unit per iteration: real reference‑value tests + per‑file 2‑arm scrutiny + end‑of‑task 3‑of‑3 + commit `[MAIN-FORCE]` on cad-fusion-live-ms0 + update handoff.  
- Never idle while token budget green; checkpoint at YELLOW, then auto‑compact reset.  
- Utilize parallel agents; fix issues as they arise—don’t defer.  
- Improve ALL AI systems for all domains (SFC, post‑processor, print reading, CAD drawing, CAM programming, quoting, ERP, lathe, mill, wire wizard).  
- Employ Ollama offloading, Hermes agents, parallel agents, engineered loops, harnesses, and crons.  
- Execute option A2: re‑embed GNN corpus with stronger GPU model (mxbai‑embed‑large‑v1, 1024d→768d).  
- Prioritize India tasks; use memories, wikis, tribal knowledge to improve all AI systems.

**FINDINGS/BUGS**  
- loraComposition flake fixed – awaited handler promise; removed race with 50 ms timer (fire‑and‑forget + setTimeout anti‑pattern resolved).  
- LearningLoopEngine.clearAll() reset `initialized=true` to avoid test pollution.  
- containsSimilar now has floor (`>=3`) to prevent false positives.  
- PRISMSelfAwarenessEngine.test: 134 tests failed due to missing class export; 20 revived, 114 remain for full realignment.  
- AWR16 material kc1.1 crossroad: per‑grade cutting‑force coefficients conflict with canonical values (safety‑negative).  
- BusinessDispatch LoRAGate ghost: 7‑subsystem callable not implemented (hotel dispatcher architecture decision).  
- OllamaObsidianRag settings wiring: unregistered hook; requires R8 understanding before edit.  
- Fossil test file in engines directory exercised dead sync/string API → removed.  
- Hook path drift and stale cap constant in consensus hooks realigned to repo‑root paths; pinned `PRISM_CONSENSUS_QUEUE_MAX`.  
- Line‑ending normalization noise (CRLF→LF) in TS files resolved via `.gitattributes`.  
- Stub fan‑out produced fake “Primary answer” headline → fixed with `fan_out_mode` signal.  
- Magic count 424 in dispatcher test replaced by invariant (actual 426).  
- Owner‑blocked reds confirmed: mill‑pipeline (foxtrot) and WEDM LoRA dataset builder (mike).

**DOMAIN SPECIFICS**  
- Engines: DeepAIIntelligenceEngine, HookExecutor, PRISMSelfAwarenessEngine (`getFullDriveAwareness`), VideoLearningEngine.extractPlaybookRules, LearningLoopEngine, CROSS‑DOMAIN‑RAG‑FEDERATION‑MS0, GNN retrain (ref‑pool growth + GPU retrain), embedder scripts (`build-node-embeddings.mjs`, `quantize-vecs-to-ghost-embeddings.mjs`), loraComposition, Blackwell stale‑tag engines.  
- Metrics & gates: AUROC ≥ 0.78, macroF1 ≥ 0.55, Brier ≤ 0.15; selective deploy τ=0.7; full‑coverage (macroF1≥0.55 & Brier≤0.15).  
- Paths: `src/engines/`, `mcp-server/src/__tests__/`, `scripts/audit-unwired-engines.mjs`, `scripts/self-compact.mjs`, `scripts/lib/nn-graph-eval.mjs`, `scripts/lib/nn-graph-holdout-variance.mjs`, `system-viz-query.mjs`.  
- Dispatchers: Hermes agents, Octopus consensus (`prism_ai:consensus_decide`), Opus safety design, CrossProcessTierRouterEngine wired into 2 dispatchers; orchestrator routes through `CrossProcessHierarchicalNeuralOrchestratorEngine`.

**TOOLS USED**  
- PRISM tooling: LoRA, RAG, PSN, deep‑learning/reasoning stack (QdrantMemoryEngineSingleton, embedder factory), octopus consensus.  
- Coding & orchestration: Sonnet (live router coding), qwen3-coder 30b + qwen2.5-coder via Ollama, Hermes/Forge‑Team agents, Octopus consensus middleware, AutoConsensusHooks.  
- Scripts/agents: `build-node-embeddings.mjs`, `quantize-vecs-to-ghost-embeddings.mjs`, `nn-graph-eval.mjs`, `nn-graph-holdout-variance.mjs`, `audit-unwired-engines.mjs`, `system-viz-query.mjs`, `self‑compact.mjs`, `scripts/embed-nodes-gpu.py`.  
- Testing: Vitest, TypeScript compiler (`tsc --noEmit`), code‑analyzer hooks, scrutiny‑3way.mjs.  
- GPU environment: `H:/Tools/python-gpu/Scripts/python.exe` with sentence-transformers 5.5.1, transformers 5.10.2, torch 2.11.0+cu128.

**OPEN THREADS**  
- PRISMSelfAwarenessEngine.test realignment – full 114‑test fix (rename/async getManifest, de‑hardcoded counts).  
- GNN full‑coverage retrain – 542 MB graph load + GPU training; 1024d migration & GraphSAGE checkpoint pending next window.  
- Adoption of mxbai ghosts contingent on gate clearance (currently not cleared).  
- 10‑tier engine adapter research (heterogeneous contracts) → next session.  
- Re‑embed GNN corpus with stronger GPU model (option A2, 768d).  
- OllamaObsidianRag settings wiring – register hook, align with `settings.json`.  
- BusinessDispatch LoRAGate ghost (hotel dispatcher architecture decision).  
- AWR16 kc1.1 crossroad review (oscar/physics).  
- AuthEngineV7 wiring investigation (security‑adjacent).  
- Remaining GHOST rung survey (dry; no bounded units left).  
- Continue improving AI systems for other domains: SFC, post‑processor, print reading, CAD drawing, CAM programming, quoting, ERP, lathe, mill, wire wizard.
