# ai-training session 95e7030e (2026-05-25, 58.8MB, spine 232KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- 20+ alpha‑slot units committed (iter 1–14 + 5 gap‑fills + 9‑rule discovery memory + 14 high‑ROI hooks/stop‑hooks).  
- Core artifacts: `mcp-route-suggest.mjs`, `defer‑queue.mjs`, audit script (`audit-nudge-mcp-actions.mjs`), `/r12‑audit` skill, token‑savings telemetry, and 4 new stop‑hook adapters.  
- N1 adopter: `prompt-rules-inject.mjs:135‑160`.  
- N2 adopter: `stop-session-spend-summary.mjs`.  
- Health‑warn hook: `prompt-rewriter-health-warn.mjs`.  
- Calibration code U‑PRF03/04/05/06 (boost + floor).  
- Ledger prune lib & hook: `stop-ledger-prune.mjs`.  
- PSN aggregator: `stop-psn-savings-aggregate.mjs`.  
- Session‑start headline consumer: `session-start-savings-headline.mjs`.  
- MCP disconnect detector & suppressor patch to `mcp-route-suggest.mjs` + `scripts/lib/mcp-state-check.mjs`.  
- RTK enforce hook: `rtk-enforce-pretool.mjs`.  
- Post‑tool RTK adoption counter: `posttool-rtk-adoption-measure.mjs`.  
- RTK fraction tuner: `scripts/lib/rtk-fraction-tune.mjs`.  
- Parallel audit sweep of `audit-token-savings-coverage.mjs` (≈331 K nodes).  
- New detectors in `pre-tool-savings-multi.mjs` (grep, read, bashgit, write, websearch, bashnode).  
- Top‑ROI wave agents: `token-savings-top-roi-candidates.json`, `scripts/lib/token-savings-router-table.mjs`, `.claude/hooks/pre-tool-router-table-advise.mjs`.  
- Detectors & training units U‑PTSM02..06, U‑RTK‑ENFORCE, U‑STSS, U‑RFT, U‑SHEXT.  
- Audit units: U‑AUDIT‑V4 (system‑graph coverage), U‑LS‑CAT‑ENFORCE (ls/cat hard‑block).  
- MCP route audit: U‑MCP‑ROUTE‑AUDIT (1176 fires/0 takes).  
- Hook‑inject ROI audit: U‑HOOK‑INJECT‑ROI.  
- Category A TTL fixes – slot‑soul, discipline‑expert, comp‑build‑enforce, PSN‑checklist dedup.  
- Category B per‑file dedups – 6/7 hooks (CCG, TCE, PWGI, PRGI, PBGI, PGGI).  
- Category C MVP – `scripts/lib/unified-pre-search.mjs` + injector hook.  
- Category D operator wires – `scripts/apply-token-savings-wires.mjs`; settings.json updated.  
- Canonical cache: `state/shared/dashboards/injection-dedup-cache.json`.  
- All commits now in `cad‑fusion‑live‑ms0` branch (after resolving index.lock contention).  

**DECISIONS**  
- Fleet‑reaper ownership moved from alpha to golf slot; hook `alpha-slot-reaper-guardian.mjs` kept but unwired.  
- Adopt defer queue pattern for mid‑task advisories (`backendAuditChain`, `doctrineSurface`) to raise take‑rate.  
- Canonical source unification: derive `TAKEUP_CREDITED_ACTIONS` from `mcp-route-takeup._ACTION_TO_CLASSIFIERS`.  
- Introduced camelCase dispatcher mapping (e.g., `aiReasoningDispatcher.ts → prism_ai`).  
- R12 audit script with tier‑A/B classification and `/r12‑audit` skill.  
- Added stop‑hook timeout budget (`stop-hook-timeout-budget.mjs`) and session‑spend summary hook.  
- Adopt dedup on slash path; smallest‑first model preference, 15 s timeout for Ollama calls.  
- Calibrate confidence with boosts + floor → perfect score (later raise floor to 0.90 then 1.00).  
- Prune telemetry ledgers to ≤5 MB (retain 1 MB tail).  
- PSN aggregator + session‑start headline provide single savings summary.  
- Enforce RTK usage; allow opt‑out via env flag (`PRISM_RTK_ENFORCE_ENABLE=1`).  
- Deploy parallel agents for audit and training to hit all nodes (~331 K).

**OPERATOR DIRECTIVES**  
- `/checkin-alpha reorientate with where we left off with alpha work`.  
- Repeat `/loop [5m] /goal` commands: apply high‑roi gap fills, scope more token‑saving measures, complete new units for alpha slot.  
- Restart Ollama daemon (`ollama serve`) and verify `/api/chat` <15 s.  
- Set `PRISM_RTK_ENFORCE_ENABLE=1` in settings.json (or env).  
- Wire new hooks into Stop chain: add `stop-ledger-prune.mjs`, `stop-psn-savings-aggregate.mjs`, `session-start-savings-headline.mjs`, `rtk-enforce-pretool.mjs`.  
- Run `scripts/apply-token-savings-wires.mjs --apply` to update settings.json and back‑up.  
- Trigger `/system-viz regen` after changes; verify MCP connectivity probe (replace TCP-only with JSON‑RPC handshake).  
- Monitor MCP health; run `node scripts/lib/mcp-state-check.mjs` nightly or via cron.  
- Run `node scripts/lib/rtk-fraction-tune.mjs` nightly to keep RTK fraction table up‑to‑date.

**FINDINGS / BUGS**  
- R12 violations: fake `prism_intelligence:ollama_*`; fixed mapping to `prism_dev:ollama_hook_query`.  
- Import‑guard bug in hooks (`main()` executed on import) caused exit 255; guarded with `process.argv[1].endsWith()`.  
- Lock contention on `.git/index.lock` during concurrent peer commits; resolved by committing directly to slot tree.  
- Drift: sidecar telemetry mis‑reported take‑rate; corrected banner logic.  
- Prompt‑rewriter silently skipping due to Ollama `/api/chat` timeouts; fixed with health‑warn and timeout bump.  
- High gap in RTK adoption (~3 % hits); addressed by enforcement hook and adoption counter.  
- MCP disconnect caused noisy route‑suggest nudge; now suppressed when MCP is down.  
- Token‑savings coverage gaps largely resolved; audit shows 0 candidates after full sweep.  
- Compounding token savings plateau after ~200 nodes; 2 K real tool surfaces yield >80 % of gains.  
- MCP route audit shows 1176 fires / 0 takes due to broken take‑up wiring (measurement artifact).  
- Slot‑soul, discipline‑expert, comp‑build‑enforce TTLs too short → repeated injections (~38 k tokens/session).  
- PSN‑checklist lacked dedup; added static cache entry.  
- Injection‑dedup lib needed to write to canonical cache for `/system-viz` synergy.

**DOMAIN SPECIFICS**  
- Slot system: 26 slots, alpha = mill specialist.  
- PSN legs: 11‑leg architecture (brain, OS, wiki, memories, tribal, viz, engines, algorithms, NN/GNN, AI/Ollama).  
- Engines/Actions: slot‑soul, master‑index, memory‑vault, wiki‑precheck, obsidian‑vault, BM25 pre‑search, RTK passthroughs (git, grep, ls), MCP routing.  
- Dispatchers: RTK, MCP; hooks: PreToolUse advisory, PostToolUse measurement, Stop token‑savings‑summary.  
- Metrics: token savings per surface, take‑up rates, injection counts, audit dashboards (`state/shared/dashboards/*.json`).  
- Key Paths: `scripts/lib/token-savings-router-table.mjs`, `.claude/hooks/pre-tool-router-table-advise.mjs`, `state/shared/dashboards/injection-dedup-cache.json`.

**TOOLS USED**  
- PRISM CLI (`/checkin`, `/loop`, `/goal`).  
- Hook files in `.claude/hooks/` (e.g., `mcp-route-suggest.mjs`, `pre-tool-router-table-advise.mjs`).  
- Scripts in `scripts/` (`defer‑queue.mjs`, `audit-nudge-mcp-actions.mjs`, `stop-ledger-prune.mjs`, `rtk-enforce-pretool.mjs`, etc.).  
- Test harness: Node `--test`.  
- Git ops via `node .claude/helpers/chat-slots.mjs`.  
- Calibration functions (`calibrateConfidence`).  
- Ledger prune library (`pruneExpired`).  
- PSN aggregator & headline consumer scripts.  
- MCP state checker (`isMcpDown`).  
- RTK enforcement pre‑tool hook.  
- Parallel agent framework for audit and training.

**OPEN THREADS**  
- Ollama recovery: GPU saturation or daemon crash must be fixed on operator side.  
- RTK enforcement activation: ensure all slots honor env flag; monitor adoption banner.  
- Settings.json wiring: final commit of new hooks into Stop chain and session‑start consumer.  
- MCP health monitoring: schedule periodic checks to keep suppressor accurate.  
- Calibration refinement: fine‑tune boost thresholds based on observed adoption rates over time.  
- Doctrine‑surface TTL fix pending peer lock clearance (next cron auto‑commit).  
- Cat‑C cutover envVars (`PRISM_MASTER_INDEX_INJECT=0` etc.) to silence redundant sources.  
- MRI commit staged; will commit when lock clears.  
- Residual hygiene via crons: `94d46b4a` (10 m `/goal`) and `2f96dc3b` (5 m “complete all remaining”).  
- Monitor MCP connectivity probe; replace TCP‑only check with JSON‑RPC handshake validation.
