# ai-training session 10f4a625 (2026-06-03, 7.9MB, spine 92KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- None

---

**DECISIONS**  
- Fleet‑reaper ownership moved from alpha → golf (2026‑05‑16).  
- `/checkin‑golf` always runs fleet‑reaper; guardian hook enforces non‑skippable.  
- Golf treated as normal work slot (no legacy `--golf`).  
- Bypass legacy allowlist via session env var or global PreToolUse removal.  
- Adopt R10 drift discipline: limit anomaly investigations to ≤ 1 step, resume loop immediately.  
- Treat golf passes strictly as hygiene checks—no feature work.

---

**OPERATOR DIRECTIVES**  
- Skill claims golf as normal slot; do NOT pass `--golf` flag to `chat-slots.claim`.  
- Disable or bypass `golf-slot-write-allowlist.mjs` before non‑hygiene writes.  
- Run single fleet‑hygiene pass (session 10f4a625‑b…):  
  1. `rtk node H:/prism/scripts/fleet-reaper-sweep.mjs --once --json | tail -80`  
  2. Verify PRISM Fleet Reaper task via PowerShell `schtasks /Query …`  
  3. `rtk node H:/prism/.claude/helpers/chat-slots.mjs reclaim`  
  4. `rtk node H:/prism/.claude/helpers/slot-task-claim.mjs list`  
  5. `rtk git status | head -5` (note divergence only if worsening)  
  6. Report delta or “nominal”  
  7. Tick loop state: `rtk node H:/prism/.claude/helpers/loop-state.mjs tick --session … --status ok`

---

**FINDINGS/BUGS**  
- Guardian probe race: `golf-slot-reaper-guardian.mjs:182` reports “NOT REGISTERED” when task running; correlates with 4‑s timeout or non‑zero exit from `schtasks /Query`.  
- MCP daemon flapping: :3100 disconnects every ~10–15 min, self‑heal via watchdog; backend investigation needed.  
- Reaper scheduled run anomalies: intermittent `LastResult` 0x1 & 0x41306, trailing MCP flaps by one cycle; auto‑cleared next run.  
- Crash‑frozen count spiked to 17 (from ~2–4) due to heartbeat lag from batched prompt burst.  
- Reaper reaped 0 orphans; all frozen slots are live‑window frozen chats.  
- Stale slots: 16, claims: 0; backbone healthy (`Ready/0x0`).  
- No true orphan processes detected; node‑process spikes transient instrumentation noise.

---

**DOMAIN SPECIFICS**  
- Scripts/dispatchers: `fleet-reaper-sweep.mjs`, `chat-slots.mjs reclaim`, `slot-task-claim.mjs list`, `loop-state.mjs tick`.  
- Hooks/guards: `golf-slot-write-allowlist.mjs`, `golf-slot-reaper-guardian.mjs`.  
- Metrics: `slots["owned‑by‑crashed"]`, `staleSlots`, `crashWatch`, `softRelief.*`, `gpu.*`, `ollama.*`, `coordinator.*`; `git status` divergence; `crash‑frozen count`; stale slots; claim count.  
- Paths: `H:/prism/scripts/`, `H:/prism/.claude/helpers/`.

---

**TOOLS USED**  
- Node.js scripts (`node H:/prism/...`)  
- PowerShell `schtasks /Query`  
- Git (`git status`)  
- `rtk node` wrapper  
- Unix `tail -80`  
- Cron job `dbe4e26e` (5‑min cadence)

---

**OPEN THREADS**  
1. Fix guardian probe race → unit `[FLEET‑REAPER]/U-GUARDIAN-PROBE-RUNSTATE-RACE`.  
2. Investigate MCP daemon restart loop (RSS pressure vs crash).  
3. Monitor node‑process trend for potential leak; keep watch.  
4. Confirm heartbeat catch‑up on next pass; monitor crash‑frozen spike resolution.  
5. Maintain Cron job `dbe4e26e`.

No new feature work required; loop continues per operator mandate.
