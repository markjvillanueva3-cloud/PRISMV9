# ai-training session 7e610092 (2026-05-20, 11.2MB, spine 50KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `scripts/slot‑query.mjs` – unified slot‑keyed lookup across 5 data surfaces (chat‑slots, claims, queues, handoffs, git log).  
- `scripts/slot‑query.test.mjs` – 24 / 24 unit tests, all passing.  
- `.claude/commands/slot-query.md` – skill wrapper for `/slot‑query`.  
- `knowledge/memories/feedback/feedback_slot_query_by_name_and_recency.md` – doctrine memory pinning the tool under “Standing”.  

**DECISIONS**  
- Use `git log --grep=slot:<name>` with a 30 s timeout; bug fixed by bumping `GIT_LOG_TIMEOUT_MS`.  
- Persist terminalWindowId via a sidecar (`window-slot-bindings.mjs`) to avoid drift when slots are reclaimed.  
- Replace recursive `findPsAncestorPid` walk with a snapshot‑then‑walk strategy and expose a `write-explicit` CLI for manual pinning on hosts where auto‑walk fails.  
- Commit only the slot‑query files (`git commit -- <files>`) to avoid misattribution when peers hold an active lock.  

**OPERATOR DIRECTIVES**  
- Finish hygiene close‑out of P0‑U03 and U04 (already done).  
- After the `.git/index.lock` is cleared, commit the slot‑query files with a banner noting the previous misattribution.  
- Coordinate with bravo on compaction work; ensure no overlap with token‑awareness/precompact hooks.  

**FINDINGS/BUGS**  
- `execFileSync('git', …)` returned 0 commits until timeout was increased to 30 s.  
- Misattribution class: peer commits captured our banner; resolved by pathspec commit.  
- Stale `.git/index.lock` (≈5 h old) prevented slot‑query commit; cleared via `git-lock-sweeper`.  
- TerminalWindowId mapping lost when a slot is reclaimed, causing silent drift.  
- `ps-window-pin.mjs` never wrote to `state/shared/ps-window-pins.json`; `findPsAncestorPid` failed on this host due to orphaned bash PIDs.  

**DOMAIN SPECIFICS**  
- Slot‑task‑claims & queues stored in `state/shared/*.json`.  
- Handoffs named `HANDOFF-<chatId>-<slot>-<topic>.md`.  
- Git log grep uses `--grep=slot:<name>`.  
- `MILESTONE_PROGRESS.json` is an array; queries must use `.find()` not key lookup.  
- Build scripts: `build-milestone-progress.mjs`, `build-state-snapshot.mjs`.  
- Conflict‑fork doctrine: avoid retrying on active `.git/index.lock`; use worktree or pathspec commit.  

**TOOLS USED**  
- Node.js (`execFileSync`, `node:test`).  
- PRISM helpers: `chat-slots.mjs`, `slot-query.mjs`, `close-out-milestone.mjs`.  
- Git hooks: `git-lock-sweeper.mjs`.  
- PS‑pin utilities: `ps-window-pin.mjs`, `window-slot-bindings.mjs`.  

**OPEN THREADS**  
1. Commit the slot‑query files once the lock is cleared (pathspec commit).  
2. Verify sidecar persistence of terminalWindowId works across reclaims.  
3. Confirm that the memory index now points to the new slot‑query doctrine.  
4. Coordinate with bravo on compaction; ensure no overlapping precompact hooks.
