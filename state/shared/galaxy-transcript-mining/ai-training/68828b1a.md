# ai-training session 68828b1a (2026-06-04, 26.7MB, spine 164KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hermes desktop app running on Opus 4.8 (Claude Max provider) – config updated.  
- `hermes-obsidian-memory-bridge.mjs` deployed; 15‑min sync task to `knowledge/hermes-brain/`.  
- Account‑switch autonomy loop (U2,U6 wiring) commit **a679e455c1** (12 + 51 tests).  
- RGS pipeline roadmaps generated: `MASTER-RGS-ROADMAPS-2026-06-03.md` commit **6ead9a74e9**.  
- Master plan docs committed: `MASTER-FLEET-PLAN`, `PRIORITY-BUILD-PLAN`.  
- Slot briefs for 15 domains live in `state/shared/slot‑briefs/`.  
- Loop job scheduled via CronCreate ID 45fefb00 (10 min cadence).  
- Octopus consensus upgraded to **gpt‑oss:120b** + **qwen2.5-coder:32b**, vision exclusion – commit **856c417d2b**.  
- Hermes “coder brain” planner & harness emitter commits **0f178c6370** & **1bb66a1822**.  
- Dream‑cycle OOM fix via inverted‑index blocking – commit **23de0e7881**.  
- H: drive atlas generator mapping all 145 top‑level H: dirs – commit **f2564b81a0**.  
- Reactive 429‑fallback executor for rate‑limit handling – commit **443c84d08d**.  
- Re‑enabled Obsidian‑vault bridge (bridge now live).  
- Safe Hermes local‑backend wiring script with auto‑rollback – commit **c988a21ec4**.

**DECISIONS**  
- Use Hermes desktop app as autonomous orchestrator; run on Claude Max Opus 4.8 to avoid credit exhaustion.  
- Obsidian vault is memory sink; bridge syncs Hermes memories to `knowledge/hermes‑brain/`.  
- Account‑switch coordinator uses token‑counter gating (90 % of 5‑hour window) and staggered chat restarts.  
- RGS pipeline roadmaps drive domain‑specific plans with high‑ROI ordering.  
- Master plan + priority build plan orchestrate work across galaxies via Hermes slot briefs.  
- Octopus consensus engine updated to use powerful LLMs; retired Ollama models removed; vision exclusion keeps Ollama lightweight.  
- Hermes planner encodes 0xCodez dynamic‑workflow patterns to decide workflow shape before execution.  
- Dream‑cycle OOM resolved by blocking on keyword posting lists and capped top‑K buffer.  
- Reactive fallback router replaces missing executor; switches on real 429 errors, no telemetry needed.  
- Slash‑command routing will be made executable via an offload registry to Ollama.

**OPERATOR DIRECTIVES**  
- “lets keep building until we have full autonomous hermes + obsidian vault.”  
- “hold on the pull until i get home … pivot to something else but dont forget about the pull for ollama.”  
- “look into the full slash command list available, assess their pipelines and address underutilization.”

**FINDINGS/BUGS**  
- Shared‑tree git index lock contention resolved with pathspec commits.  
- U4 5‑hour populator missing data source (`rate_limits.five_hour` not emitted).  
- Hermes boot‑loop due to credit‑exhausted OpenRouter fixed by switching to Claude Max.  
- `cron_mode` enum not verified – risk of misconfig if left as `deny`.  
- Octopus defaults still reference retired Ollama models; need update.  
- Account‑switch auto‑swap path wired but requires manual trigger until U4 data available.  
- Pre‑existing env‑isolation bug leaked GEMINI_API_KEY fixed.  
- 11 consensus test failures due to unstubbed third voice resolved by env isolation + default‑model updates.  
- Dream‑cycle OOM at ~11k memos fixed with inverted‑index blocking and top‑K cap.  
- Reactive fallback router missing executor added `routeWithFallback()`.  
- Slash‑command routing underutilized (0.6 % take‑rate); built executable registry + runner.

**DOMAIN SPECIFICS**  
- Hermes: Electron desktop, Python backend, `config.yaml`, MCP :3100, kanban, gateway, memory.  
- Obsidian vault: `knowledge/` folder in vault, REST API plugin for MCP bridge.  
- Slot briefs: `state/shared/slot‑briefs/<slot>.md` used by `slotBriefEngine.writeBrief`.  
- RGS pipeline roadmaps: 16 domains, high‑ROI order, atomic node/wiring/bridge units.  
- Master plan docs: `FLEET-DOMAIN-GOALS-2026-06-03.md`, `MASTER-FLEET-PLAN`, `PRIORITY-BUILD-PLAN`.  
- Account‑switch system components: `token-awareness-sidecar`, `fleet-wake-sequencer`, `account-switch-restart-coordinator`, `switch-claude-account.mjs`.  
- Octopus consensus engine: `MultiModelConsensusEngine.ts`.  
- Hermes “coder brain” planner & harness emitter scripts.  
- Dream‑cycle synthesizer, H: drive atlas generator, reactive fallback router, slash‑command pipeline injector, ask‑ollama executor.  
- Metrics: 56/56 consensus tests green; 28/28 slash‑router tests green; 3 OOM failures resolved; 145 H: dirs mapped.

**TOOLS USED**  
- CronCreate for `/loop` scheduling (ID 45fefb00).  
- Hermes agent scripts (`hermes-agent`, `wire-hermes-local-backend.mjs`, `trigger-command-pipeline.mjs`).  
- RGS tools (`rgs-pipeline-rules.mjs`, roadmap generation workflow).  
- Slot‑slot helpers (`claim`, `reclaim`).  
- Credential helpers: `capture-claude-credentials`, `switch-claude-account`, `account-switch-restart-coordinator`, `fleet-wake-sequencer`.  
- Library: `lib/claude-account-lib.mjs`.  
- Bridge script: `hermes-obsidian-memory-bridge.mjs`.  
- Octopus engine: `MultiModelConsensusEngine.ts`.  
- PRISM tools: octopus, multi‑model resolver, context injector, dream‑cycle, atlas generator, fallback router, slash‑command injector.  
- Libraries: vitest, node:test, tsc, Ollama HTTP API.

**OPEN THREADS**  
- Enable Hermes `cron_mode` to allow scheduled autonomy (needs config edit).  
- Install and wire `mcp-obsidian` stdio bridge into Hermes.  
- Finalize autonomous kanban seed and goal assignment for Hermes.  
- Resolve U4 5‑hour populator data source once Claude Code emits `rate_limits.five_hour`.  
- Update octopus defaults to use powerful LLMs (`gpt‑oss:120b` primary, `qwen2.5-coder:32b` secondary).  
- Handle shared‑tree git lock contention in future commits.  
- Pull a 64K-native model (e.g., Qwen3‑Coder 30B) and configure Hermes `config.yaml` with fallback providers.  
- Wire Opus or gpt‑oss:120b as reviewer once local workhorse ready.  
- Run final wiring script (`node scripts/wire-hermes-local-backend.mjs --model qwen3-coder:30b --apply`) after pull completes.  
- Rotate exposed `GEMINI_API_KEY`.  
- Register nightly dream‑cycle task (`install-hermes-dream-cycle-task.ps1`).  
- Expand slash‑command offload registry to cover remaining high‑ROI commands.
