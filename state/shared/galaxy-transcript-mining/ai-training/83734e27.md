# ai-training session 83734e27 (2026-05-19, 5.2MB, spine 50KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-WD-ASCII` – ASCII‑cleaned `ensure-all-watchdogs.ps1`, committed.  
- `U-FR-MONITOR-SELFREAP` – added reaper processes to `PROTECTED_PATTERNS`; Monitor no longer self‑reaped, commit landed.  
- `U-FR-TRIGGER-STALL-DETECT` – stalled‑trigger detection logic in `fleet-reaper-sweep.mjs`, `fleet-task-health-watch.mjs`, and `golf-slot-reaper-guardian.mjs`.  

**DECISIONS**  
- Doctrine shift: fleet‑reaper ownership moved from **alpha → golf** (2026‑05‑16).  
- Golf slot treated as normal work slot; legacy hygiene flag removed.  
- Session‑scoped bypass (`PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1`) chosen for write allowlist.  
- Reaper monitor protected via `PROTECTED_PATTERNS` to avoid self‑kill.  
- Stalled‑trigger detection added with cooldown marker file; prevents endless re‑runs.  
- PowerShell 5.1 encoding bug fixed by ASCII‑cleaning `ensure-all-watchdogs.ps1`; future migration to `pwsh` is optional.  
- High‑ROI queue order: watchdog ASCII → monitor self‑reap → stalled‑trigger detection → tests → prism_safe → ban‑char guard → pwsh migration.

**OPERATOR DIRECTIVES** (verbatim)  
- “run the fleet reaper monitor, thats the issue”  
- “complete all tasks in high roi order”  
- “make any high roi synergy build for the neural network system and ai systems being auto used”  
- “fix the blox of whatever high roi feature is almost complete”  
- “any other high roi features/nodes just a wiring away from being useful? use system‑viz if it helps with searching”

**FINDINGS/BUGS**  
- `ensure-all-watchdogs.ps1` had non‑ASCII characters → parse errors in PowerShell 5.1; fixed.  
- Reaper monitor was killed by its own sweep (missing PROTECTED_PATTERNS); resolved.  
- Stalled trigger detection needed a cooldown marker file to avoid infinite re‑runs; added.  
- Host memory saturated with 13 chats → `node.exe` fork failures; need to shed load before heavy pipelines.  
- Git index contention prevented committing `U-FR-STALL-TESTS`; pending until shared index is free.

**DOMAIN SPECIFICS** (unique to this galaxy)  
- Scripts/skills: `fleet-reaper-sweep.mjs`, `fleet-task-health-watch.mjs`, `golf-slot-reaper-guardian.mjs`, `process-slot-map.mjs`, `ensure-all-watchdogs.ps1`, `slot-bind-enforce.mjs`.  
- Commands: `node H:/prism/scripts/fleet-reaper-sweep.mjs --monitor-loop --interval 300`; scheduled task `PRISM Fleet Reaper`.  
- Slot claim helpers: `chat-slots.mjs` (reclaim/claim).  
- Metrics exposed by reaper sweep: `slots["owned‑by‑crashed"]`, `softRelief.{priorityDemoted,…}`, `gpu.{freeMb,utilizationPct}`, `ollama.{reachable,loaded[]}`, `coordinator.{shouldPrewarm,…}`.  
- Paths: `state/shared/golf-bypass.jsonl`, `H:/prism/state/shared/handoffs/HANDOFF-golf‑*.md`.  
- Preset file for work‑tier host: `H:/prism/.claude/helpers/fleet-reaper-host-presets.mjs`.

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `fleet-reaper-sweep.mjs`, `fleet-task-health-watch.mjs`, `golf-slot-reaper-guardian.mjs`, `process-slot-map.mjs`.  
- Hooks: `slot-bind-enforce.mjs`, `ensure-all-watchdogs.ps1`.  
- Scheduler: `CronCreate`/`CronDelete`; Monitor via `Monitor()` helper.  
- Skill invocation: `/fleet-reaper`, `/startup-golf`, `/schedule`.  
- Git for commits; backgrounded `git log/status` to avoid contention.

**OPEN THREADS**  
- Commit `U-FR-STALL-TESTS` (28/28 passing) – pending after shared‑index lock clears.  
- Resolve “some chats won’t connect” (`prism_safe`) issue.  
- Implement ban‑character guard hook.  
- Optional migration from PowerShell 5.1 to `pwsh`.  
- Wire unwired engines (e.g., Lathe cluster) for next high‑ROI push.
