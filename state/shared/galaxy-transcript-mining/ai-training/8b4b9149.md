# ai-training session 8b4b9149 (2026-06-10, 47.1MB, spine 511KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- 5ae481f748 – EPERM leak fixed in `OutcomeCaptureBusEngine.ts` (atomic append + retry).  
- 86f0e3fe0c – wired `SpeedFeedTriComparatorEngine`, `SpeedFeedExhaustiveCombinationEngine`, `SpeedFeedDownstreamSubscriberEngine` into `calcDispatcher`; added 16 round‑trip tests.  
- 16d6eecef4 – added `SpeedFeedCalibrationPersistEngine` + 14 unit tests, dispatcher wiring, spec addendum (`src/engines/...`).  
- 890b2c39e7 – live‑results addendum to VC assessment spec (removes dangling blocker).  
- 3438987f0e – hardening calibration engine: clamped flag fix, persist tmp cleanup, skipped_rows observability.  
- f31398a1a5 – added `SpeedFeedGpuJudgeEngine` + tests + dispatcher wiring.  
- f5d14ddb29 – GPU judge hardening: probe prefix‑match, silent‑exclusion note, fetch timeout, matched_model field.  
- 951f5ac335 – polished GPU judge (cleaned temp files, added `isProbe`, suppression test).  
- 3a1c20fca2 – full‑mode exhaustive sweep engine + `runStreaming` generator + driver (`--mode full`) + 11 tests.  
- af0ac16c5a – fixed `cells.length` ReferenceError, ISO‑group collapse lock test, doc comment update.  
- 28eca0ed84 – spec reflection of GPU‑judge findings (aggressive_rush safety flag).  
- 54c0ae06c8 – spec addendum for full sweep comparison to G‑Wizard/HSMAdvisor.  
- b847c0f1ff – scoping spec for axis‑awareness enhancement.  
- e9b68da865 – canonical `CANONICAL_TOOL_MATERIAL_SPEED_FACTOR` table (carbide 1.0 / HSS 0.35 / ceramic‑CBN‑PCD 2.5).  
- 658c8280fe – wired tool‑material factor into `UltimateSpeedFeedEngine.calculate()`; +10 unit tests.  
- 585584e3ae – de‑duplicated coolant table via existing `CoolantVcModifier`.  
- 7d0affcae6 – moved hard‑coded rigidity factor to `constants.ts`.  
- f998f8af71 – hoisted tool‑material / coolant / rigidity multipliers to single source; applied to engine alternatives.  
- da41a58fd1 – full 102 k combination sweep (calc half).  
- c48829e331 – axis‑liveness probe (`sfc-orchestrator-axis-liveness.ts`).  
- d03458fff1 – extended probe to include `tool_life → runout`.  
- 2070c472a4 – workholding → force‑cap (physics‑reviewed).  
- 7dc7798033 – spindle.hp → power clamp (physics‑reviewed).  
- 8b191d5427 – tool_holder.type runout → life.  
- 81e37ece16 – numeric target‑Ra finish axis added.  
- a257f872bb – U‑OSC‑AXIS‑LIVENESS‑COMPLETE (all named axes live).  
- 6a3ad56545 – U‑OSC‑COMPARE‑TOOLMATERIAL‑VARIABILITY (tri‑vendor sweep over tool_material).  
- c78faa5a73 – U‑OSC‑HSS‑BASELINE (web‑verified HSS baseline added).  
- 835df42c74 – U‑OSC‑COMPARE‑CERAMIC‑CBN‑BASELINE.  
- 4c544db4ae – U‑OSC‑COMPARE‑PER‑VENDOR (explicit per‑vendor deltas for G‑Wizard/CNCCookbook & HSMAdvisor).  
- 08d7fc6d37 – U‑OSC‑ALL‑AXIS‑SWEEP (max‑variability sweep over every named axis).  
- 907e74acab – U‑OSC‑HSS‑SPEED‑MODEL‑REVIEW (fixed PRISM over‑speed bug for HSS cast iron & CBN hardened).

**DECISIONS**  
- Replace read‑rewrite‑rename append with atomic file append + retry to eliminate EPERM/orphan files.  
- Wire all dispatcher‑unreachable engines; use lifecycle actions for `SpeedFeedDownstreamSubscriberEngine`.  
- Run closed‑loop test against live G‑Wizard & HSMAdvisor data → need honest output handling.  
- Build GPU‑resident “closed‑loop” judge (`SpeedFeedGpuJudgeEngine`) to satisfy Stop hook RTX 6000 requirement.  
- Expand exhaustive sweep to full mode (~69k cells) and expose `runStreaming` API for driver.  
- Harden calibration engine (clamped flag, persist tmp cleanup, skipped_rows).  
- Fix GPU‑judge probe logic; add matched_model evidence, silent‑exclusion warning, fetch timeout, single‑source `isProbe`.  
- Document many axes inert; plan axis‑awareness starting with tool material.  
- Commit under safety gate S(x)≥0.98; keep operator‑gated apply layer for speed adjustments.  
- Add canonical physics tables (tool‑material, coolant, rigidity); de‑inline constants.  
- Wire existing coolant model; dedup before adding new logic.  
- Apply axis multipliers to both primary Vc and alternative mode so orchestrator surface is accurate.  
- Use vendor catalog OCR for calibration via Blackwell GPU extraction pipeline.  
- Run full sweep & comparison now that axes are live; defer remaining safety‑critical physics changes until fresh context.

**OPERATOR DIRECTIVES** (verbatim)  
- “Pick auth provider (Clerk / Supabase / Auth0) to unblock revenue path.”  
- “Ship webhook signature fix immediately if required (provider‑independent).”  
- “Update closed‑loop driver to expose alignment flags and exclude unaligned externals from consensus median.”  
- “Run calculations for every possible combination of machines, spindles, controller, materials, work holding/fixture, tool holder connection type and holding mechanism … compare ALL possible calculations and parameters with max variability to gwizard and hsmadvisor.”  
- “Can we utilize a better model now that we have better equipment?”  
- “Ensure the galaxy is mapped to the resources folder file path (H:\PRISM\resources).”  
- “Look into what Charlie was pulling for vendors.”  
- “Briefly acknowledge the goal, then immediately start (or continue) working toward it — treat the condition itself as your directive and do not pause to ask the user what to do.”

**FINDINGS/BUGS**  
- EPERM rename failure in `OutcomeCaptureBusEngine` caused ledger write loss.  
- G‑Wizard crib has 0 tools with proven surface speed → cannot contribute to comparison.  
- HSMAdvisor returns constant 634.6 m/min for all materials; engine flags unaligned but still included in consensus median (bug).  
- Consensus logic incorrectly includes not‑aligned external systems.  
- Inert axes: coolant type, holder type, machine/spindle/controller/workholding/inserts, tool material (within ISO group) do not affect Vc – missing `toolMat` term.  
- ISO‑group collapse: all materials within an ISO group produce identical Vc.  
- ReferenceError: `cells.length` used after switching to streaming API.  
- GPU judge bugs fixed: probe prefix match, silent‑exclusion observability, fetch timeout, matched_model field.  
- Calibration engine bugs fixed (P2): clamped flag false‑positive, persist tmp cleanup, skipped_rows observability.  
- Mean |Δvc| ≈ 25 % under‑speeding vs published baselines; worst –53 % on 6061 Al.  
- Orchestrator dropped tool‑material, coolant, rigidity factors; fixed by applying them to alternatives.  
- Coolant model wired now; rigidity factor de‑inlined to constants.  
- MRR coupling issue: new factors affect MRR via `machine_rigidity_factor × coolant_effectiveness`; flagged for reconciliation.  
- G‑Wizard `toolcrib.csv` geometry‑only (all speed columns zero); live comparison yields 0 matched cells – data limitation, not a bug.  
- Vendor catalog PDFs font‑encoded; require vision‑OCR on Blackwell GPU.  
- PRISM over‑speeds HSS cast iron by +108 % and CBN hardened steel by +49 %.  
- PRISM fz uniformly +67–91 % higher than all published sources; considered aggressive roughing strategy, not defect.  
- Several axes dead at baseline (controller_brand, machine_accuracy, etc.) are either by‑design or optimizer‑internalized; identified `controller_features` wiring gap and missing tool_life tracking for holder_runout/tool_holder_type.

**DOMAIN SPECIFICS**  
Engines: `OutcomeCaptureBusEngine`, `SpeedFeedTriComparatorEngine`, `SpeedFeedExhaustiveCombinationEngine`, `SpeedFeedDownstreamSubscriberEngine`, `SpeedFeedCalibrationPersistEngine`, `SpeedFeedGpuJudgeEngine`, `UltimateSpeedFeedEngine`, `SpeedFeedNineAxisOrchestratorEngine`, `ClampingForceEngine`, `SpindlePowerClampEngine`, `ToolHolderRunoutEngine`, `FinishRaEngine`, `SpeedFeedBaselineComparatorEngine`.  
Dispatchers/Actions: `calcDispatcher` (actions: `speed_feed_calibration_persist`, `speed_feed_gpu_judge`), `SpeedFeedBaselineComparatorEngine.compare()`, U‑OSC axis‑liveness probe (`sfc-orchestrator-axis-liveness.ts`), `sfc-full-sweep-compare.mjs`, `sfc-all-axis-sweep.mjs`.  
Metrics: Vc (m/min), rpm/feed, stability margin, DOC, fz (feed/tooth), MRR, tool_life_min, numeric finish‑Ra.  
Key paths: `src/engines/...`, `src/physics/constants.ts`, `scripts/sfc-full-sweep-compare.mjs`, `sfc-all-axis-sweep.mjs`, `SFC-VC-ASSESSMENT-2026-06-08.md`.

**TOOLS USED**  
Node.js + tsx scripts, Vitest harness, ultracode workflow orchestration, `/hermes-workflow` planner, `/forge-audit-v2`, Git tooling (slot‑bind‑enforce, chat‑slots), CronCreate/CronDelete, Skill slash commands (`/goal`, `/loop`), Ollama inference server (GPU‑resident LLM), `nvidia-smi`, PRISM core engines & orchestrator scripts, physics-reviewer, two‑agent per‑file scrutiny, `/startup-oscar`.

**OPEN THREADS**  
- Finalize closed‑loop driver with honest output; exclude unaligned externals from consensus.  
- Resolve HSMAdvisor constant bug or adjust consensus logic to ignore unaligned data.  
- Decide on auth provider and deploy webhook signature fix.  
- Add G‑Wizard tool‑selection logic if operator supplies proven speeds.  
- U‑OSC‑DEAD‑AXIS‑TRIAGE: wire `controller_features`, add tool_life tracking for holder_runout/tool_holder_type.  
- U‑OSC‑FZ‑FORCE‑VALIDATE: physics‑reviewer audit of PRISM fz +67–91 % divergence.  
- Vendor calibration: run vision‑OCR batch on Kennametal & other catalogs; generate per‑vendor `speed-feed-data.ts`.  
- MRR reconciliation: adjust multiplier to account for new axis factors.  
- Rigidity DOC integration: flow machine/holder stiffness into stability‑lobe analysis.  
- Remaining inert axes (holder/spindle/controller/workholding/insert, tool path type, finish quality) – define physics models.  
- Full sweep vs live G‑Wizard/HSMAdvisor numbers – obtain populated `toolcrib.csv` & HSMAdvisor XML; re‑run comparison after calibration.  
- Controller default‑mode feed decision pending operator approval.
