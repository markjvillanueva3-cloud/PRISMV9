# ai-training session 1c2ad8c5 (2026-06-04, 13.3MB, spine 111KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED (builds/commits)**  
- `U‑OSC9‑GPU‑TAG‑CACHE` (`7fb4fabb54`) – frozen GPU‑tag cache; 12 base + 6 turning/carbide cells.  
- `U‑OSC9‑ALL‑LOGICAL‑INPUTS` (`546f00cb36`) – sweep: milling + drilling + turning × 6 ISO × 5 diameters (180 cells).  
- `U‑CSFH‑01` (`df68a51086`) – extracted SFC variability axes & physics‑law oracles.  
- `U‑OSC9‑DRILL‑CHIPGEOM` (`81a3eb72c8`) – fixed drilling/tapping regime; safety‑physics gate S(x)=0.92.  
- `U‑GWIZARD‑COMPARE` (`2d0a2d54ea`) – 3‑way comparison infra (PRISM vs G‑Wizard).  
- `U‑CHATTER‑TEST‑TRACK` (`d1a57b9fac`) – chatter‑stability adapter test suite.  
- `b69f872681` – `U‑OSC9‑OPEN‑CARTESIAN‑COMPARE` runner (`openCartesianCompareSweep.ts`); 19/19 vitest green, 3‑of‑3 Stop PASS.  
- `7431657f68` – safety‑critical physics patch: hardened‑steel carbide Vc over‑speed fixed (107 m/min → 38.5 m/min), 13/13 dedicated tests.

**DECISIONS (architecture/scope + why)**  
- Freeze GPU tags on RTX6000 Blackwell + Ollama qwen2.5-coder for deterministic baseline.  
- Expand logical sweep to include turning & drilling; add min‑n guard for degenerate baselines (turning/N).  
- Add keystones `U‑OSC9‑CALIB‑PERSIST` & `U‑OSC9‑CALIB‑APPLY‑WIRE`; gate with safety‑physics oracle, 3‑of‑3 Stop mandatory.  
- Defer romeo/juliett DB‑accessor unit until catalogs ready.  
- Build full closed‑loop calibration: CALIB‑PERSIST → CALIB‑APPLY‑WIRE → hardened‑physics root fix.  
- Use `VendorDeltaCalibrationBridge` to ingest `open_cartesian_compare.jsonl`; prioritize physics bug fix first.

**OPERATOR DIRECTIVES (verbatim asks)**  
- Verify foxtrot status; get 48 h scope update.  
- Run precompact to plan closed‑loop testing if ready.  
- Await romeo/juliett data when catalogs available.  
- Start closed‑loop tests & comparisons against the other two calculators.  
- Run on live data, produce divergence + specialized‑calc candidates.  
- Proceed with full closed‑loop build; verify PRISM aggressive speeds vs handbook conservative values.

**FINDINGS/BUGS**  
- Broken drilling regime (ap=0 → Kienzle rejects).  
- Fabricated HSS cells in older GPU classification (carbide turning mis‑classified as HSS).  
- EPERM lock on outcome bus when writing to live calibration ring; solved by fresh sweep cwd.  
- Silent drop of results beyond comparator cap (P0).  
- Weak rule coverage: regimes where both vendors disagree but only one clears threshold silently dropped (P1).  
- Min‑n guard required for degenerate baselines.  
- Infinity bug resolved; vitest pool workaround confirmed.  
- G‑Wizard abstains on generic material×diameter combos (zero `gwizard_computed`).  
- 5 ranked specialized‑calc candidates: hardened steel (>45 HRC) + turning + small diameter; all PRISM‑hotter.  
- Safety‑critical physics bug: PRISM overestimates Vc for carbide on >45 HRC hardened steel (107 m/min vs handbook 38.9).  
- Flaky gauntlet test timeout increased from ~75 s to 120 s.

**DOMAIN SPECIFICS (engines/actions/dispatchers/metrics/paths)**  
Engines/Actions:  
- `SpeedFeedTriVendorBatchComparatorEngine`  
- `HSMAdvisorComparatorBridgeEngine`  
- `SpeedFeedDeepLearningEngine`  
- `OutcomeFeedbackBridgeEngine`  
- `SpeedFeedExhaustiveCombinationEngine`  
- `jmFirstSweep`  
- `openCartesianCompareSweep`  
- `UltimateSpeedFeedEngine.calculate`

Dispatchers:  
- `VendorDeltaCalibrationBridge` (reads `open_cartesian_compare.jsonl`)  
- `sfc_hsmadvisor_calibrate`

Metrics/Paths:  
- Divergence, posture, PRISM vs handbook speeds; output file `open_cartesian_compare.jsonl`.  
- `sfc-full-logical-sweep.mts`, `sfc-gpu-classify-deferred-series.mts`.  
- Worktree: `H:/prism-slot-oscar` (branch `slot/oscar`).

**TOOLS USED (PRISM tools/dispatchers/skills/scripts/hooks)**  
- Slot‑binding helpers (`chat-slots.mjs`, `slot-bind-enforce.mjs`)  
- Stable session ID helper (`stable-session-id.mjs`)  
- Per‑agent handoff writer (`per-agent-handoff.mjs`)  
- Precompact guard (`precompact-pending-guard.mjs`)  
- Workflow orchestration (ultracode)  
- Safety‑physics oracle  
- Reviewer agents for per‑file scrutiny  
- Vitest (`vitest run --pool=forks`) integration tests  
- 3‑of‑3 Stop gate  
- Ultracode, code‑explorer plugin  
- PRISM AI stack: NN/GNN/GraphSAGE, LoRA, RAG, PSN, deep‑learning/reasoning, octopus consensus, AUROC/Brier/F1 deploy gates

**OPEN THREADS (what is still to build)**  
- Implement `U-CSFH-03-DB-ACCESSORS` (romeo/juliett catalog accessors) when catalogs ready.  
- Finalize `CALIB-PERSIST` to write factor state to disk; gate `PRISM_SFC_CALIB_APPLY` around apply‑wire logic.  
- Validate closed‑loop: run dense sweep after calibration persistence, confirm divergent regimes move toward vendor envelope.  
- Full closed‑loop test harness (feed deltas back to calibration engine).  
- Resolve any remaining EPERM/outcome‑bus issues in new branches.  
- Finalize flaky gauntlet timeout fix; re‑run full gauntlet for regression certification.  
- Monitor for additional physics bugs in other material–tool combinations.
