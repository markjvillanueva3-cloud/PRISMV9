# ai-training session 23c10eea (2026-05-17, 27.9MB, spine 129KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- f4ab9e01d9 – U‑FR‑TIER1‑AGGRESSIVE‑THRESHOLDS (graduated memory‑pressure gate)  
- bf679c899b – U‑FR‑MEM‑BALLAST (256 MB cushion)  
- a89feb6ca5 – U‑FR‑SERVICE‑RESTART (advisory‑by‑default Docker/Postgres/Prometheus restart, PID lock, fail‑soft shell)  
- 87936f6242 – U‑1‑REFERENCE‑POOL‑SEED‑STAGE (`seed‑ghost‑from‑unwired.mjs` wired into regen‑viz)  
- 27bab0a052 / a6abf27043 / 27fc8c71ce – DOC‑REFLECT (CLAUDE.md, wiki, Obsidian memory, MEMORY.md) for units & NN‑GRAPH‑MS1  
- 97c9286311 – U‑NNG‑PIPELINE‑STRATIFIED‑WIRE (stratified negative sampling in GraphSAGE pipeline)  
- 2d94304535 – U‑NN‑GRAPHR‑RETRAIN‑LIFECYCLE (autonomous retrain orchestrator, installer, tests)  
- 817a2fd71b – DOC‑REFLECT for NN‑GRAPH‑MS2  
- U-NNG-768D-FEATURES – GraphSAGE 8‑d → 768‑d feature swap  
- U-AI-OPP-MAP – AI/NN wiring opportunity assessment  
- U-GAP-MILL-FFT-CHATTER – added `predictWithTrend()` to ChatterPredictionEngine (new test, wiki, Obsidian docs)

**DECISIONS**  
- Shift fleet‑reaper ownership from alpha to golf; keep alpha‑slot‑reaper‑guardian.mjs unwired.  
- Wire stratified negative sampling into GNN pipeline (`nodeTypeField`, `negPHard`) → AUROC 0.096 fixed.  
- Replace binary memory‑pressure gate with 3‑band graduated gate (`tierFromPressure`).  
- Make service restart advisory‑by‑default; add PID lock and fail‑soft shell.  
- Wire `seed‑ghost‑from‑unwired` as post‑merge regen‑viz stage to eliminate poolSize 0.  
- Implement autonomous retrain lifecycle with candidate promotion only on gate pass (AUROC≥0.78, macroF1≥0.55, Brier≤0.15).  
- Use 2‑reviewer scrutiny + 3‑of‑3 end‑of‑task gate for all commits.  
- No new AI/NN units in this loop; remaining queued next session.

**OPERATOR DIRECTIVES**  
- `/loop [10m] finish all alpha units /goal`  
- `/startup-alpha` (force‑take alpha slot, run standard startup audit)  
- `powershell -File H:/prism/.claude/helpers/install-nn-graph-retrain-task.ps1 -RunNow` (trigger NN‑1 retrain lifecycle)  
- Use `/system-viz` to locate AI/NN opportunities.

**FINDINGS/BUGS**  
- `readDockerHealth` shape error fixed by folding real producer.  
- P0 in service‑restart unit removed after adding real‑producer E2E test.  
- NN‑GRAPH still has model‑side dormancy gate (AUROC < 0.78) unresolved; needs out‑of‑session stratified retrain on full graph.  
- Commit memory critical 97.4%; heavy builds deferred until after `/compact`.  
- `xmalloc` fork‑storm resolved with node‑process‑janitor.  
- Test failures: `loadCheckpoint` signature mismatch, `findStablePockets` bug in main branch.  
- Peer file claim conflict on CLAUDE.md deferred via chat‑bus.

**DOMAIN SPECIFICS**  
- Engines: GraphSAGE GNN, fleet‑reaper, nn‑graph‑retrain lifecycle.  
- Dispatchers: memory‑pressure dispatcher, service‑restart dispatcher, retrain scheduler.  
- Metrics: AUROC (0.096 → target ≥0.78), macroF1 (≥0.55), Brier (≤0.15), pressurePct, killAfter.  
- Paths: `scripts/lib/graphsage-train-pipeline.mjs`, `scripts/nn-graph-retrain-lifecycle.mjs`, `scripts/fleet-reaper-sweep.mjs`, `scripts/seed-ghost-from-unwired.mjs`, `H:/prism/scripts/lib/graphsage-train-pipeline.mjs`, `PRISM_FFT_PREDICTIVE_CHATTER.js`.  
- Hooks: chat‑slots, inventory‑refresh, build-state‑snapshot, milestone‑progress, audit‑unwired‑engines.

**TOOLS USED**  
- PRISM skills: `/checkin-alpha`, `/loop`, `/compact`, `/forge-audit`, `/close-out-audit`, `/system-viz`  
- Helpers: `chat-slots.mjs`, `stable-session-id.mjs`, `per-agent-handoff.mjs`, `inventory-refresh.mjs`, `build-state-snapshot.mjs`, `milestone-progress.mjs`, `audit-unwired-engines.mjs`, `node-process-janitor`  
- Scripts/skills: `graphsage-train-pipeline.mjs`, `nn-graph-retrain-lifecycle.mjs`, `seed-ghost-from-unwired.mjs`, `fleet-reaper-sweep.mjs`, `install-nn-graph-retrain-task.ps1`, `PRISM_FFT_PREDICTIVE_CHATTER.js`  
- Node tooling: `node:test`, `tsc`

**OPEN THREADS**  
- Execute operator‑out‑of‑session stratified retrain on full 372 k graph → candidate checkpoint for AUROC≥0.78.  
- Verify model‑side gate after retraining; if still below threshold, build additional feature/ensemble unit (e.g., 768‑D features).  
- Close‑out audit for CAM‑lane candidates pending but not alpha‑lane.  
- No further alpha‑lane units remain; loop ended and handoff written.  
- `U05 ToolAxisOptimizationEngine` & `U-MF01 AccessibilityAnalysisEngine` buildable, no peer collision.  
- Operator actions NN‑2/NN‑3 pending after NN‑1 retrain completes.
