# ai-training session 41db1b82 (2026-05-17, 97.3MB, spine 667KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `15c161f63` – `scripts/lib/tribal‑graph‑clusters.mjs` (692 LOC) + tests (579 LOC, **62/62 PASS**)  
- `862137931` – `scripts/lib/tribal‑graph‑embedding.mjs` (736 LOC) + tests (1 168 LOC, **109/109 PASS**)  
- `ac8d56da2` – `course-mapper-lib.js` (135 course‑tribal nodes)  
- `7c87d216` – `syllabus-extract.mjs` (unzip 4 courses, confidence 0.75)  
- `fe1a32efc` – `course-unzip-extract.mjs` (227 zips → 209 nodes)  
- `fc608e6a0` – `course-embed.mjs` (192 courses → 618 semantic wires, idempotent checkpoint)  
- `aa0335a8d` – U‑KC‑B1 `scripts/course-to-tribal-tips.mjs` (126 MIT‑OCW tips, **44 tests**)  
- `3d9324f2a` – U‑KC‑B2 `monolith-to-tribal-tips.mjs` (133 monolith tips, **52 tests**)  
- `44980b391` – U‑KC‑B3 round‑trip wiring test (`prism_knowledge:tribal_search`)  
- `e4a48ebf3` – U‑KC‑C1 formula verification ledger  
- `05152dff62` – U‑KC‑C2 algorithm verification ledger  
- `cd00120dcd`, `66aa07afa4` – U‑KC‑D1 course‑data routing pipeline (lib, CLI, tests)  
- `d99df13f5d` – U‑KC‑E1 doc‑reflection commit  
- `4086c8009` – Phase 0 of KNOWLEDGE‑CONVERSION‑MS0 (U‑KC‑A1 + U‑KC‑A2)  
- `0ec9663dff`, `4da65465c7` – U‑CK04 wiki/os namespace schema & 10 syscall entities  
- `790ec17f25` – U‑CK06 canonical command frontmatter schema docs (`_command-schema.md`)  
- `0ca1d0feb0` – U‑CK10 `/pick-task` caller audit & deprecation report  
- `a46d6a98b3` – wiki/os/pipelines/loop.md (autonomous‑iteration doctrine)  
- `93648157b1` – wiki/os/pipelines/goal-complete.md (stop‑hook gate for `/goal`)  
- `9bf6627388`, `eb7853a6a8` – session‑summary spec & latest update  
- `dea7274d23` – COURSE‑FORGE‑PROPOSALS.md (P1–P10)  
- `5d5c363f0e` – bulk emitter + `COURSE‑FORGE‑STUBS.md` (62 stubs)  
- `6ae5399608` – CLI test suite for emitter (**13/13 vitest PASS**)  
- `592cc28260`, `0b237f435b` – doc‑reflection updates (CLAUDE.md, wiki, Obsidian)  
- `1323fa4ee7` – P1 OperatorSplittingMethod node (**28 vitest PASS**)  
- `b38a9f2285` – P7 ODEIntegrator node (**56 vitest PASS**)  
- `a547223bbf` – P6 LinearStateSpaceModel node (**78 vitest PASS**)  
- 8 algorithm nodes (P1, P7, P6, FDM, GD, FEM, LAG, SafeExpressionEvaluator) – **208 tests**  
- KnowledgeInjectionPipelineEngine (`U‑KIP01`, `U‑KIP02`, `U‑KIP03`) – 28 tests + real‑data E2E; CLI dry‑run: 126 routed /110 eligible.  
- Total test pass: **236/236** across all shipped suites.

---

**DECISIONS**  
- Adopt a 9‑layer graph‑of‑graphs taxonomy (atom → universe).  
- Use Jaccard similarity for L0–L3 aggregation; Ollama `nomic‑embed‑text` cosine for lateral wires (≥ 0.75).  
- Enforce safety tier “shop_floor” (Ω≥0.95, S(x)≥0.98) unless overridden.  
- Disallow stub engines and inline constants; all physics constants imported from `mcp-server/src/physics/constants.ts`.  
- Leverage existing PRISM stochastic engines (`prism_calc`, `prism_adaptive_control`, `prism_intelligence`).  
- Confidence floor set to 1 for knowledge tips; domain keys normalised (collapse spaces/hyphens/underscores).  
- Atomic JSON write via `.tmp` + rename; output sorted deterministically (`courseId`, then `kind\x1Fname`).  
- VALID_KNOWLEDGE_CATEGORIES frozen list of 32 members; mapping table verified.  
- Three‑lane conversion model: direct‑wire (A), port‑verify (B), forge queue (C).  
- Keep algorithm nodes WIRE‑EXEMPT until dispatcher wiring; closure‑input requires SafeExpressionEvaluator.

---

**OPERATOR DIRECTIVES**  
- `/loop extract college coursework until all units complete = /goal`.  
- Utilize `system-viz + obsidian` for index, searching and visualising the prism brain.  
- Download textbooks from reputable sources with virus/malware scan; never inline constants or create stub engines.  
- Destructive ops (`rm -rf`, `git reset --hard`, force‑push) only with explicit user authorization.  
- `/checkin-india` slot‑claim & handoff; `/yolo-mode` autonomous, auto‑fix up to 3 attempts.  
- After `/compact`, run `node --test scripts/course-to-tribal-tips.test.mjs`.  
- Register milestone via `/rgs`; dispatch reviewer arms on test file again if PASS.  
- Cron `32fcf842`: 10‑minute autonomous loop; delete with `CronDelete 32fcf842` when finished.

---

**FINDINGS / BUGS**  
- `tribal‑graph‑clusters`: P0/P1 failures (empty ID collision, shallow freeze, nondeterministic Math.random, bagFn errors, separator injection) – fixed.  
- `tribal‑graph‑embedding`: silent success on malformed input, wrong‑dim checkpoint merge, dual‑endpoint drift (`/api/embed` vs legacy), hostile `skipIds`, U+001F contamination, sort instability, missing retry jitter – all resolved.  
- Slot claim bug: “kilo” fell back to “bravo”; now India slot correctly claimed.  
- Lint‑staged stash/restore cycle caused empty commits; workaround: `git commit -- <paths>`.  
- Set.has override exploit & endpoint contract drift fixed in iter 1.  
- `classifyDomain` array issue → all nodes had `domain:"X_unknown"` – fixed in iter 2.  
- `nodesFromIndex` inverted mapping → 0 edges; corrected in iter 3.  
- 17 catalog‑only courses remain metadata‑only (no zip/data.json).  
- Drift‑detector false positives for MF‑MS1, MF‑MS2, ACP‑MS0 – audit note added.  
- CK05 mirror‑gen flagged fleet‑impact risk; deferred.  
- Negative‑zero emission bug in SafeExpressionEvaluator fixed.  
- Test design errors (assertion thresholds, safety weighting) corrected.

---

**DOMAIN SPECIFICS**  
- **Engines / Actions**: `tribal_search`, `tribal_capture`, `buildLateralWires`, `embedWithRetry`, `mergeIntoCheckpoint`, `knowledgeDispatcher.ts` (`prism_knowledge:inject`), `TribalKnowledgeEngine.loadDocumentLearnedTips()`.  
- **Scripts & Paths**:  
  - `scripts/lib/tribal‑graph‑clusters.mjs`, `scripts/lib/tribal‑graph‑embedding.mjs`, `scripts/tribal‑graph‑build.mjs`.  
  - `course-mapper-lib.js`, `syllabus-extract.mjs`, `course-unzip-extract.mjs`, `course-embed.mjs`.  
  - `scripts/course-to-tribal-tips.mjs`, `monolith-to-tribal-tips.mjs`, `course-data-router-lib.mjs`, `course-data-router.mjs`.  
  - KnowledgeInjectionPipelineEngine scripts (`U‑KIP01/02/03`).  
- **Data Sources**: `mcp-server/src/data/*-tips.ts`, `mcp-server/data/state/TRIBAL_TIP_INDEX.json`, `PRISM_COURSE_CATALOG.json`, `MIT_COURSE_INDEX.json`, per‑course `data.json`.  
- **Metrics**: Monte Carlo variability (`prism_calc:monte_carlo_*`), 618 semantic wires, 209 course‑tribal nodes, 126 MIT‑OCW tips, 133 monolith tips.  
- **Schemas**: KnowledgeTip (id, title, body, category, tags, confidence, source, created_at); KnowledgeCategory union; `prism_knowledge:tribal_search` contract.  

---

**TOOLS USED**  
- PRISM tooling: `/checkin`, `/compact`, `/loop`, `/goal`, `/pick-unit`, `/build`, `/handoff`, `precompact-pending-guard.mjs`.  
- Node.js (`node:test`, vitest), Git, Ollama (`nomic‑embed‑text 768d`), Docker (containerised tooling), Obsidian, system‑viz.  
- PRISM helpers: `chat-slots.mjs`, `audit-roadmap-drift.mjs`, `stable-session-id.mjs`.  
- Skills/hooks: GSD+skills+hooks+RTK token savings, cron (`32fcf842`), schedule skill.

---

**OPEN THREADS**  
1. Run `scripts/tribal‑graph‑build.mjs` to cluster + embed full 11 586 real tribal tips.  
2. Ingest remaining college coursework units (OpenStax, MIT OCW, Open Textbook Library, gov PDFs, Internet Archive) with malware scan.  
3. Wire wiki injection for new content into PRISM knowledge graph; finalize safety‑tier enforcement and audit destructive‑operation hooks.  
4. Verify orchestrator DAG produces correct aggregation & lateral wires before merging to production.  
5. Re‑run all tests after `/compact`; dispatch reviewers on test files; commit U‑KC‑B1 once PASS.  
6. Implement U‑KC‑B2 (monolith‑to‑tribal‑tips) and U‑KC‑B3 (wiring verification).  
7. Register milestone via `/rgs`.  
8. Monitor ID stability strategy for future re‑runs.  
9. CK05 mirror‑generation review; corpus migration units CK07/CK08/CK09 pending scope confirmation.  
10. Wire closure‑input nodes to MCP surface (`prism_knowledge:inject`).  
11. Execute KIP `--apply` to inject real knowledge into repo; run full npm build and peer test suite for regression safety.  
12. Final doc‑reflection for any new nodes once FiniteDifferenceMethod built.  
13. Delete autonomous loop cron `32fcf842` when finished.
