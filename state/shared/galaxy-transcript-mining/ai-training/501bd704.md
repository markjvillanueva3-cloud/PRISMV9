# ai-training session 501bd704 (2026-06-04, 22.3MB, spine 93KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- GPU‑stack health foundation (`gpu_health.py`, `py‑subprocess‑bridge.mjs`, `GpuStackHealthEngine.ts`, devDispatcher wiring) – commits f11efe4047 / 9e5dcca6ad.  
- F0 degeneracy fix: base‑rate normalization + direct‑embed inference + leak‑free source enrichment – commits a6bd95c163 (spec), 8abba4f27c (core), ae60024500 (eval), 2e9e94808d (P1/P2 hardening).  
- Source‑enrichment commit dcb2c86bb8.  
- Calibration negative result (LOO isotonic) – commit 44f4a90ccb.  
- Resume‑hash bug fix (missing `\x1F` delimiter) – commit da6aedfc07.  
- Wiki lesson & memory updates – commit 6f2bad4792.  
- `.gitattributes eol=lf` fix – commit 9bd4b22abd.  
- Stratified holdout split set as default; added CLI flag `--flat-holdout`.  
- Updated `NN‑EVAL.json` to use stratified split; all 62 eval tests pass.

**DECISIONS**  
- Keep per‑file and 3‑of‑3 review gates; no bypass for cross‑galaxy work.  
- Use direct‑embed inference + base‑rate normalization to break GNN degeneracy; source enrichment with leak‑free engine signal is primary improvement.  
- Calibration off by default (LOO isotonic regressed AUROC/Brier); explore Platt/temperature scaling.  
- GPU training foundation pending: build `GpuTrainingSchedulerEngine` once torch on golf.  
- Replace flat split with per‑class stratified holdout; AUROC rises to 0.808 (> 0.78).  
- Keep `.gitattributes` fix permanent; add current‑state banners.  
- Require two parallel reviewers with R12 “metric integrity” check.

**OPERATOR DIRECTIVES**  
- Continue `/loop /goal` to close Brier gate and push macroF1 ≥ 0.55.  
- Build `GpuTrainingSchedulerEngine` now (after torch on golf).  
- Verify GPU training remains gated until torch is available.  
- Implement `calibration-done-right` (temperature scaling) and class‑balancing reference pool to target macroF1 → 0.55.

**FINDINGS/BUGS**  
- Feature starvation + edgeless inference caused constant‑vote degeneracy; fixed by direct‑embed.  
- Base‑rate normalization alone insufficient.  
- Source enrichment improved macroF1 but worsened Brier/AUROC (underconfidence, cap‑0.8 vote share).  
- LOO isotonic calibration overfit → regressed metrics; keep off.  
- Resume‑hash bug fixed.  
- CRLF/LF flip bug fixed; stale `.gitattributes` memories removed.  
- Flat split caused zero holdout for `prism_calc`, inflating macroF1 and under‑estimating AUROC.  
- Stratified split yields AUROC 0.808, macroF1 0.439, Brier 0.179.

**DOMAIN SPECIFICS**  
- Engines: GPU stack health probe, py‑subprocess bridge, `GpuStackHealthEngine`, devDispatcher (`prism_dev:gpu_stack_health`).  
- GNN: GraphSAGE model, voteDispatcher, ghost‑node embeddings, direct‑embed inference, source enrichment.  
- Metrics: AUROC, macroF1, Brier, accuracy.  
- Paths: `scripts/py/gpu_health.py`, `scripts/lib/py-subprocess-bridge.mjs`, `src/GpuStackHealthEngine.ts`, devDispatcher, `build-node-embeddings.mjs`, `ghost‑node‑embeddings.jsonl`.  
- Split strategies: flat vs stratified holdout.  
- Key paths: `runAssessment`, `NN‑EVAL.json`.

**TOOLS USED**  
- PRISM helpers: chat‑slots, startup/checkin pipelines, devDispatcher.  
- Review gates: 2‑reviewer per‑file scrutiny, 3‑of‑3 gate.  
- Libraries: Node.js/TypeScript, Vitest, Python (torch), Ollama/Nomic embeddings, GPU drivers.  
- `.gitattributes` for line ending enforcement.  
- `runAssessment` dispatcher and CLI flag parser (`--flat-holdout`).  
- Test harness (`makeGraph`, eval tests).  
- Git commit `9bd4b22abd`.

**OPEN THREADS**  
- Find Brier calibration that improves Brier without hurting AUROC/macroF1.  
- MacroF1 < 0.55: consider larger balanced holdout or additional feature engineering.  
- Implement `GpuTrainingSchedulerEngine` (requires golf’s torch).  
- Confirm GPU training remains gated until torch is installed.  
- Implement `calibration-done-right`: held‑out split / temperature scaling preserving AUROC.  
- Build class‑balancing reference pool to push macroF1 → 0.55.  
- Verify all `.gitattributes` memories cleared; no residual references remain.  
- Final gate review with two parallel reviewers and R12 metric integrity check.
