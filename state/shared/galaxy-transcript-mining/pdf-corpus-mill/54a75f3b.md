# pdf-corpus-mill session 54a75f3b (2026-06-04, 14.4MB, spine 77KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `vision-ensemble-fuse.mjs` + CLI (`vision-ensemble-extract.mjs`) – multi‑VLM ensemble OCR with concurrent execution on RTX 6000 Blackwell; includes string‑aware leading‑dot decimal repair.  
- `ocr-training-loop-lib.mjs` (commit c8bb8a3a25) – closed‑loop training engine: agreement‑fraction calibration, isotonic regression, tiered labeling, emits `trainset.jsonl`.  
- `xray→india LoRA staging seam` (`4fec77e8c1`) – uses existing `BlueprintLoRABridgeEngine` to convert `trainset.jsonl` into an anonymized `local‑lora` bundle for India.  
- `blueprint_vl_train_lora.py` + tests (commit b121b19f7b) – real Qwen2.5‑VL PEFT trainer, replaces the simulation stub.

**DECISIONS**  
- Unlock Blackwell GPU by running ≥3 diverse VLMs concurrently; ensemble consensus OCR replaces single‑model baseline.  
- Closed‑loop training independent of delta: use ensemble distillation + agreement‑fraction calibration to generate pseudo‑labels and a reliable trainset.  
- Link xray to India’s LoRA stack via the proven `BlueprintLoRABridgeEngine`; no new engines required.  
- Take over India’s blocked fine‑tune by implementing real VL trainer, GPU runner, preflight checks, and expanding the trainset.

**OPERATOR DIRECTIVES**  
1. Execute T3.2: wire `py-subprocess-bridge` → `DetachedLoRARunnerEngine` + monitor.  
2. Add GpuStackHealth preflight for sm_120 Blackwell (handle false‑positive `torch.cuda.is_available()`).  
3. Grow trainset to ≥50 reliable samples before training.  
4. Operator‑gated steps: install `torch≥2.7/cu128`, `peft/datasets/trl`; create an `operator_verified` eval split with Brier ≤ 0.15.

**FINDINGS / BUGS**  
- Leading‑dot decimal parsing bug in vision extraction JSON caused silent data loss; fixed with a string‑aware scanner.  
- Ensemble OCR previously used only one 8B VLM; Blackwell can run ≥3 models concurrently.  
- Calibration originally used raw `k`; replaced by fraction `f = k / n_models` and gated on `n_models ≥ 2`.  
- Torch 2.6/cu124 lacks kernels for sm_120; requires torch ≥ 2.7/cu128.  
- `wedm_train_lora.py` was a simulation stub; replaced with real trainer.

**DOMAIN SPECIFICS**  
- Engines: `vision-ensemble-fuse`, `ocr-training-loop-lib`, `BlueprintLoRABridgeEngine`, `DetachedLoRARunnerEngine`.  
- Actions/dispatchers: `blueprint-ocr-training-loop`, `cadDispatcher:cad_pdf_blueprint_extract_ensemble`, `blueprint_lora_prepare_set/export`.  
- Metrics: agreement‑fraction calibration, isotonic regression accuracy, Brier score for operator‑verified gate.  
- Key paths: `H:/prism/.claude/...`, `scripts/lib/…`, `scripts/…`.

**TOOLS USED**  
- PRISM helpers: `/checkin-xray`, `/startup-xray`, per‑agent handoff, precompact guard, milestone‑tracker, stable‑session‑id, chat‑slots.  
- Dispatchers: `blueprint-ocr-training-loop`, `cadDispatcher`, `BlueprintLoRABridgeEngine`.  
- Scripts: `vision-ensemble-fuse.mjs`, `ocr-training-loop-lib.mjs`, `trainset-to-lora-pairs.mjs`, `xray-trainset-to-lora.mjs`, `blueprint_vl_train_lora.py`, `py-subprocess-bridge`.

**OPEN THREADS**  
- Implement T3.2 (GPU runner + monitor).  
- Add GpuStackHealth preflight for sm_120 Blackwell.  
- Expand trainset to ≥50 reliable samples (more synthetic prints or VLM pre‑warm).  
- Install required torch/peft/datasets/trl in India environment.  
- Create operator_verified eval split with Brier ≤ 0.15 before training.
