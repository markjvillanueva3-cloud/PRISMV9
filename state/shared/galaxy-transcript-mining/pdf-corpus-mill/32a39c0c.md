# pdf-corpus-mill session 32a39c0c (2026-05-16, 34.7MB, spine 179KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**

- U‑PPL‑A1 TurningMinFingerprintEngine – 69 tests passed, committed to HEAD (`a76ea58c5`).  
- U‑PPL‑B1 ProgramReoptimizationOrchestratorEngine – all unit tests passed, landed in HEAD (`4050f3b35`).  
- Phase21 PDF splitter `H:/prism/Docustrata/.index/phase21-split-containers.py` – idempotent, job `b7dvt7ale` splits 8 396 containers into PDFs.  
- `synergy-regression-watch.mjs` (F1) – daily JSON alert logger, history JSONL writer.  
- `install-synergy-watch-task.ps1` – Windows Scheduled Task installer, end‑to‑end verified.  
- `helper-orphan-rank.mjs` (F6) – ranks orphan helpers, outputs JSONL counts/top offenders.

---

**DECISIONS**

- Ship A1 & B1 immediately after confirming HEAD inclusion; no further work on them.  
- Skip B2 build now; handoff prepared for next session to avoid partial dispatcher edits.  
- Accept 5/7 `.MIN` templates corrupted (no Git history); rely on two clean anchors + JM Die corpus.  
- Use Docker+GPU pipeline only after Docker daemon restored.  
- Adopt “meta‑artifact” pattern: each audit script has `--pretty`, `--json`, safe‑read helpers, ROOT resolve, concise README.  
- Enforce per‑file scrutiny gate (2 reviewers + Codex) on every new file; no commits until explicit user request.  
- Use PowerShell installer template from fleet‑reaper for durability.  
- Implement case‑insensitive path comparison on Win32 to allow `main()` to fire.  
- Reap orphan processes per 2026‑05‑14 regression before retrying writes.  
- Reclassify helpers as wired‑strong/cross‑helper; correct orphan rate from claimed 85 % to 40.6 %.  
- Add mandatory anchor, non‑degeneracy floor, and bracketing negative control to tests.

---

**OPERATOR DIRECTIVES**

- Verify split job output (`b7dvt7ale.output`); re‑run `phase21-split-containers.py --min-prints 2` if incomplete.  
- Build remaining “do it all” tasks:  
  - Re‑index 104 K PDFs – delta detector + phases 6→7→15.  
  - Orchestrate full Docustrata pipeline stages 1–5 GPU containers (PaddleOCR, eDOCr2, VLM).  
- Restore Docker: restart WSL & Docker Desktop; resume GPU stages once running.  
- `/loop` continue development of audit meta scripts: `stale-milestone-rank.mjs`, `hook-overhead-profiler.mjs`, `script-cold-ratio.mjs`.  
- After each file, run scrutiny gate; fix all P0/P1 before proceeding.  
- Finish running `cold-script-rank.mjs` test suite; ensure hooks write correctly after reaping.  
- Process remaining files beyond file 2 under per‑file scrutiny gate.  
- Validate orphan rate stays at 40.6 % across full corpus.  
- Resolve lingering fork‑storm crashes in PreToolUse/PreToolUse:Write hooks.

---

**FINDINGS/BUGS**

- 5 of 7 `.MIN` seed templates corrupted; no Git history → cannot recover via Git.  
- Docker daemon down (API 500); all GPU stages blocked until restored.  
- Phase21 splitter job running but not finished – monitor for errors.  
- Stale milestones need ranking script (`stale-milestone-rank.mjs`).  
- Hook overhead missing profiler (`hook-overhead-profiler.mjs`).  
- Script cold‑ratio metric missing (`script-cold-ratio.mjs`).  
- Quote‑strip bug in PowerShell installer fixed (double outer quotes).  
- `xmalloc` errors during hook edits are transient fork‑storm symptoms.  
- `/tmp` does not resolve on Windows; baseline capture affected.  
- `main()` fails due to NTFS case‑insensitivity mismatch in is‑main check.  
- NUL bytes at offsets 7685/7695 corrupt JS source – replaced with spaces via byte‑level rewrite.  
- PostToolUse and PreToolUse:Write hooks crash during fork‑storm; writes fail until reaping.

---

**DOMAIN SPECIFICS**

- PRISM roadmap MS‑PRINT‑PROGRAM‑LOOP: 23 units, tracks A–D.  
- Docustrata pipeline phases: 6 (page count), 7 (text density), 15 (deep scan), 20 (verified prints), **21** (container splitter).  
- Engines: TurningMinFingerprintEngine uses OkumaOSPParserEngine; ProgramReoptimizationOrchestratorEngine uses LatheProgramOptimizerEngine & MillProgramOptimizerEngine.  
- Dispatchers: camDispatcher.ts (~18 K lines), millDispatcher.ts (~946 lines).  
- PRISM MCP server engines & dispatchers live under `scripts/`.  
- Hook system `.claude/hooks/*.mjs`; script registry `find .claude/scripts -name "*.mjs"`.  
- System‑viz metrics: synergy ratio, stale milestone counts, hook budget.  
- helper-orphan-rank.mjs baseline 187 helpers, 76 orphan (40.6 %).  
- cold-script-rank.mjs: 358 `.mjs`, 77 `.js`, 203 `.py`; zero refs in hooks/skills/settings/package.json/ps1‑installers/git‑hooks/cross‑script.  
- Path issue: `/tmp` problematic on Windows.

---

**TOOLS USED**

- PRISM core: `/checkin`, `/loop`, `/goal`, `/compact`, per-agent handoff, milestone-tracker, system-viz, obsidian integration.  
- External tooling: PyMuPDF (PDF parsing), Docker containers for PaddleOCR, eDOCr2, VLM (`PaddleOCR‑VL‑0.9B`).  
- Hardware: RTX 4080 SUPER 16 GB CUDA; CPU parallelism via 16 cores.  
- Node.js v20+, TypeScript compiler `tsc`, Vitest unit tests, node scripts CLI utilities, PowerShell 5.1 installer, Git per-file scrutiny gate.  
- PRISM git post‑commit hook, helper-orphan-rank.mjs, cold-script-rank.mjs, PreToolUse:Write, PostToolUse hooks, fork‑storm reaping mechanism, test harness (8/8 pass), Latin1 round‑trip byte rewrite for NUL removal.

---

**OPEN THREADS**

- B2 build: wire engines into camDispatcher & millDispatcher, add schemas/actions, round‑trip tests.  
- Re‑index remaining 104 K PDFs – delta detector + phases 6→7→15 to bring Docustrata pipeline up‑to‑date.  
- GPU stages 3–5 resume after Docker restored; monitor resource limits and VLM fallback logic.  
- Track C (lathe‑knowledge‑direct) pending units; schedule once pipeline ready.  
- Monitor split job output – ensure all 8 396 containers fully burst into `Docustrata/_organized/PRINTS/`.  
- Build & audit remaining meta scripts: `stale-milestone-rank.mjs`, `hook-overhead-profiler.mjs`, `script-cold-ratio.mjs`; verify against per‑file scrutiny gate; no commits until user explicitly asks.  
- Finish running `cold-script-rank.mjs` test suite; ensure hooks write correctly after reaping.  
- Process remaining files beyond file 2 under per‑file scrutiny gate.  
- Validate orphan rate stays at corrected 40.6 % across full corpus.  
- Resolve any lingering fork‑storm crashes in hooks.
