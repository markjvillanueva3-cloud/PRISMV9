# pdf-corpus-mill session 17b9f42e (2026-06-15, 118.6MB, spine 705KB, 9 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hermes fleet launcher (`LAUNCH-HERMES-FLEET.bat`) – 21 slots, isolated profiles in `%LOCALAPPDATA%\hermes\profiles\<slot>`.  
- GNN reference pool +8 nodes via `vault-to-gnn-refpool.mjs`; cross‑substrate edges +55 218 typed links to RAG/embedding & Obsidian memory/wiki.  
- U‑BRIDGE‑KEEPALIVE, U‑BRIDGE‑WIKI, U‑BRIDGE‑FALLBACK, U‑OLLAMA‑WEDGE‑GUARD, U‑BRIDGE‑CAG‑USEDMODEL – 5 commits (≈55 ins).  
- CAG hit‑rate telemetry for all 34 galaxies (`reasonForGalaxy`, `cagStatsFile`).  
- Loop‑state API (`readFleetLoops`) and dispatcher action `prism_session:cag_stats`.  
- Cross‑pc‑verify Stop hook with OOM guard; Tailnet‑probe read‑only hook.  
- Owner‑only LoRA extraction – 1312 rows, 34/34 galaxies.  
- AI‑Stack block idempotently rendered in souls (`renderGalaxySoul`).  
- GNN selective‑deploy AUROC 0.8084 (India GPU); full coverage pending.

**DECISIONS**  
- Adopt per‑slot Hermes profiles; use absolute path to executable to avoid PATH lookup failures.  
- Duplicate Claude fleet launcher for Hermes, seed each slot with its own SOUL file and work‑tree cwd.  
- Use deterministic 4‑leg gate (`ai-systems-synergy-goal-gate.mjs`) as sole stop criterion; re‑scope unbounded prose goal to bounded subgoal: GNN full‑coverage AUROC ≥ 0.78 (India GPU).  
- Skip heavy processes under memory pressure; use lightweight dry‑runs and incremental refreshes.  
- Accept gate, let operator decide `/clear` or authorize candidate‑gated retrain.  
- Self‑compact when context depth reaches clean boundary after shipping units (`/compact`).  
- Adopt owner‑only LoRA extraction to improve signal quality; restart shared Ollama service post‑approval.  
- Convert unbounded `/goal` to deterministic loss‑function gate on AI‑synergy; bounded goal: eliminate discoverability gaps.  
- Skip GNN full‑coverage retrain now; out of scope for this slot.  
- Do not build harness‑only tools until Tailscale present; defer mesh dispatcher pending user login & second host.

**OPERATOR DIRECTIVES**  
- “Briefly acknowledge the goal, then immediately start working toward it – treat condition itself as directive.”  
- Use forge and rgs, plan mode, Hermes crons/loops to build, wire, test, validate, synergize.  
- `/clear` to end goal; re‑scope to India GNN full‑coverage AUROC ≥ 0.78; authorize candidate‑gated retrain if desired.  
- If Tailscale mesh activation required, run `tailscale up` on second host.

**FINDINGS/BUGS**  
- R14 runaway `bash.exe` processes resolved by stopping workflow and killing spawned nodes.  
- Duplicate `prism_ai` tool registration fixed with last‑wins guard in MCP server bootstrap (`5010a01a82`).  
- 9 galaxies lacked `owned-by-slot` edges; 4 need synthetic Sierra domain node.  
- GNN full‑coverage AUROC 0.4286 (India GPU); selective deploy AUROC 0.8084 passes ≥ 0.78.  
- Memory pressure spike caused transient bash shells; resolved by cleaning orphan subtree.  
- Stale LoRA dataset updated to 474 pairs after fresh synthesis brains.  
- CAG cache hit path reported requested model instead of producer; fixed with `usedModel`.  
- Ollama wedge guard hardened against stale orphan detection; 404 vs hang discrimination added.  
- OOM in `cross-pc-handoff-verify.mjs` fixed by size cap.  
- Backtick substitution bug in commit messages escaped; superstring main‑guard issue fixed by requiring path separator.  
- Tailnet probe blocked by security hook; resolved after installing Tailscale MSI.  
- Missing `settings.json` entry for new Stop hook added and mirrored to H: config.

**DOMAIN SPECIFICS**  
- Hermes profiles: `%LOCALAPPDATA%\hermes\profiles\<slot>`.  
- Slot‑tab‑boot.ps1 changes cwd to slot worktree, runs `hermes -p <slot>`.  
- SOUL files: `H:\Tools\prism-fleet\hermes-souls\<slot>.md`.  
- Cross‑substrate edges via `generate-cross-substrate-edges.mjs` linking galaxies to RAG/embedding & Obsidian memory/wiki.  
- GNN reference pool via `vault-to-gnn-refpool.mjs`; LoRA pairs via `vault-to-lora-dataset.mjs` (1312 rows).  
- CAG hit‑rate telemetry (`reasonForGalaxy`, `cagStatsFile`).  
- Loop‑state API (`readFleetLoops`) reads loop state JSON.  
- Dispatcher action `prism_session:cag_stats`.  
- Cross‑pc‑verify Stop hook scans handoff markdowns; OOM guard added.  
- Tailnet‑probe.mjs reports Tailscale status, login & peers.  
- Memory‑provider-status.mjs CLI consumer; `/memory‑providers` skill.

**TOOLS USED**  
- Hermes_cli.main, generate-cross-substrate-edges.mjs, vault-to-gnn-refpool.mjs, ai-systems-synergy-goal-gate.mjs, slot-query.  
- Windows Terminal, PowerShell scripts (`LAUNCH-HERMES-FLEET.bat`, `slot-tab-boot.ps1`).  
- vault-to-lora-dataset.mjs, galaxy-reasoning-bridge.mjs, gatherGalaxyDocs, assembleGalaxyContext, ask-ollama.mjs.  
- cross-pc-handoff-verify.mjs, tailnet-probe.mjs, memory-provider-status.mjs.  
- node:test harness; 3‑of‑3 scrutiny gate.  
- Ollama API (Qwen2.5VL 32B, 1.5B, gpt‑oss 120B).

**OPEN THREADS**  
- India GPU GNN full‑coverage training to reach AUROC ≥ 0.78 (operator decision).  
- Tailscale mesh dispatcher pending user login & second host.  
- Deep AI‑synergy gap hunt for undiscovered gaps beyond discoverability.  
- Optional candidate‑gated retrain experiment if authorized.  
- Verify plan corrections and re‑run unit builds after `/compact`.  
- Final validation of CAG hit‑rate telemetry integration across all 34 galaxies.  
- Tmp‑orphan cleanup to address C‑drive space spike (non‑blocking).
