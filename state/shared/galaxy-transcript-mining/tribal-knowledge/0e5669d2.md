# tribal-knowledge session 0e5669d2 (2026-06-09, 39.3MB, spine 333KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `/checkin-sierra` wrapper executed; slot `sierra` bound to `claude‑0e5669d2`.  
- Ultraflow audit: Humza Khalid article summary captured; Obsidian vault verified (39k wiki files, 11k H‑memories).  
- Artifacts persisted:  
  - `obsidian-vault-node-access-map.md` (wiki entry)  
  - `system-viz/MEMORY.md` (galaxy brain)  
  - Memories `reference_obsidian_vault_audit_*`, `reference_humza_khalid_*`.  
  - `HANDOFF-claude‑0e5669d2-sierra-work.md`.  
- Fixed U‑VAULT‑RAG‑WIRE (`memory-rag-inject` wired).  
- Commit units: B1 (`168c202646`) sync‑resilient, B2 (`ea21008f25`) index‑meta, B3 (`8c4dff660a`) maint‑cron installers, C3 (`838123429b`) tribal‑coverage refresh, C2 (`984313825e`) link‑heal scorer fix.  
- Session memory persisted (`45225e0cc3`).  
- Commits: `d948b85a74` (scorer hardening, MIN_AUTOAPPLY_LEN, MAX_AUTOAPPLY_EDIT_FRACTION), `43e1b8e449` (MCP heap cap 384 MB, maxConnections 512, transient pressure codes, GPU offload widening, EXPECTED_UNREGISTERED_TASKS).  
- Additional commits: `3d796dcf5c` (fleet‑task‑health‑watch updates), `96ed5222e2` (vault‑backlinks.json builder), `0e2724871a` (regen‑viz auto‑refresh wire), `14aba14e3a + 4a44b5393c` (doc_nodes dispatcher), `f23c58b42d` (vault-access-map update, .canvas gap fix), `2d49bf0d33` (canvas-read lib & CLI), `47fe91554f` (access‑map doc).  
- Ollama wiring: `7ec4a5ea02` (OLLAMA‑PIPELINE‑INJECTOR), `cd54edb940` (U‑VIZ‑WIKI‑NARRATIVE generator), `65a29220e5` (OLLAMA‑PREWARM‑WIRE), `6fb278a2ee` (TRIBAL‑QA‑LLM‑UNBLOCK).

**DECISIONS**  
- Use slot‑binding wrapper for `/checkin-sierra`; commit to shared tree with `[MAIN]` prefix; bypass lane guard via `PRISM_GIT_ADD_LANE_DISABLE=1`.  
- Resolve peer‑claim guard by atomic stage+commit (`CLAUDE_SESSION_ID`).  
- Disarm stale candidates file: partial batch `--batch 100` (autoApplyCount 14100 → 33).  
- Keep output honest: rely on `processed/totalBroken`; no extra `coverageComplete`.  
- Harden scorer: add min‑length floor and Levenshtein edit‑fraction guard.  
- Cap hook‑heap to **384 MB**; raise MCP server `maxConnections` to **512**.  
- Add transient pressure code set (`0x800710E0`, …) to silence false “MCP Server failing” alerts.  
- Widen GPU offload patterns in `OllamaTaskOffloaderEngine`; target ~30 % offload.  
- Introduce `EXPECTED_UNREGISTERED_TASKS` to separate intentional unregistered vault crons.  
- Reject router‑widening premise for Ollama offload; focus on proven levers (synthesis scripts, canvas & doc_nodes dispatcher).  
- Keep hook‑heap cap at 384 MB; avoid raising to prevent spawn‑refusal.

**OPERATOR DIRECTIVES**  
- “Fill all gaps.”  
- Ensure Obsidian vault built and operational; map paths for cheap token usage.  
- Verify hook‑heap cap holds; clean banner noise, register vault crons after migration freeze.  
- Write handoff and recommend `/compact`.  
- Arm deferred items: restart MCP daemon (maxConnections 512), register vault crons (`PRISM Vault Memory Promotion Cron`, `PRISM Vault Rot Sentinel Cron`).  
- Execute `/startup-sierra` + `/loop [10m] /goal [...]`; resume sierra slot, pursue Ollama synergy.  
- Cross‑chat permission granted; unblock alpha/bravo lanes.

**FINDINGS/BUGS**  
- `memory-rag-inject` unwired → keyword recall dead.  
- Stale candidates file (autoApplyCount = 14100) disarmed; only 100 links processed due to OOM.  
- Scorer overconfidence: short‑token Levenshtein ≤2 edits on <5‑char tokens auto‑applied; flagged by reviewers.  
- Full‑corpus candidate generation OOMed; current approach limited to 100 links.  
- Peer‑claim guard caused commit stalls; resolved with atomic stage+commit.  
- vmmemWSL balloon (~95 GB) ignored `.wslconfig`; `wsl --shutdown` drops to <5 GB.  
- Lgit layer regex missed “Lgit”; miscounted as “other”.  
- Duplicate check (`/dedup`) flagged no existing assets for new Ollama‑pipeline injector wire.

**DOMAIN SPECIFICS**  
- Obsidian vault: 39k wiki files, 11k H‑memories; node-access mapping (~136 tokens per read).  
- Memory recall injection engines: `memory-rag-inject`, `memory-index-precheck-inject`.  
- Index‑meta stamping via `WikiIndexMaintainerEngine.formatIndex`.  
- Cron installers: promotion-memory-to-wiki, vault-rot-sentinel.  
- Link‑heal scorer logic (`scoreCandidate`, `autoApplyEligible`).  
- Candidate generation for broken links: `wiki-link-fix-suggester.mjs`.  
- Fleet‑task‑health‑watch: banner noise cleanup, EXPECTED_UNREGISTERED_TASKS, WSL guard.  
- Vault‑backlinks.json builder from `node-cards.jsonl`; staleness flag added; regen‑viz auto‑refresh wire.  
- Canvas reader lib (`canvas-read-lib.mjs`) with staleness guard; CLI `canvas`, `canvas-doc`.  
- Doc_nodes dispatcher (`prism_session:doc_nodes`), wrapper, tests, schema.  
- Ollama wiring: `master-index-precheck-inject`, OLLAMA‑PREWARM‑WIRE, TRIBAL‑QA‑LLM‑UNBLOCK.  
- GPU offload patterns widened in `OllamaTaskOffloaderEngine.ts`.  
- MCP server config (`mcp-server/src/index.ts`): `httpServer.maxConnections = 512`.  
- Hook heap cap via `portable-node`: `NODE_OPTIONS="--max-old-space-size=384"`.

**TOOLS USED**  
- `/checkin-sierra` wrapper script.  
- Ultraflow workflow (6 agents + synthesis).  
- Git hooks: `chat-slots.mjs`, `PRISM_GIT_ADD_LANE_DISABLE`.  
- Node.js tools: `node:test`, `vitest`, custom scripts for memory persistence, wiki entry creation.  
- Scrutiny gate reviewers (2‑arm per‑file and session).  
- `portable-node` wrapper (bash & cmd) for heap cap.  
- MCP server source (`src/index.ts`).  
- Health watch script (`fleet-task-health-watch.mjs`).  
- Offload engine (`OllamaTaskOffloaderEngine.ts`).  
- Jest/Node test suite for scorer, pressure code, offload patterns.  
- PRISM tooling: `askOllama.mjs`, `resolveSynthesisModel`, `generateBlurb`.  
- Workflow/ultracode agents for parallel scrutiny and 3‑of‑3 review.

**OPEN THREADS**  
1. Build Gap A (file‑level node generator): include FAST[] + merge‑splice; dedup against existing generators.  
2. Implement Gap B semantic reranker after BM25.  
3. Develop Gap C Ollama forge assist scaffolds; full duplication & scrutiny pipeline.  
4. Resolve memo‑extract throttle bug (#5) and per‑session keying.  
5. Verify no overlap with alpha’s `ollama-nav-enforce`.  
6. Restart MCP daemon to activate `maxConnections` 512 (not yet done).  
7. Register vault crons after migration freeze lifted.  
8. Decide arm status of `PRISM WSL Memory Guard`.  
9. Monitor GPU offload ratio; target ~30 %.  
10. Implement `prism_session:canvas_nodes` dispatcher.  
11. Finalize weekly synth resolver.  
12. Explore further Ollama synergies in system‑viz description/rerank, vault/memory distillation, wiki creation ops, tribal rerank/distill.  
13. Monitor post‑WSL shutdown commit‑charge stability.
