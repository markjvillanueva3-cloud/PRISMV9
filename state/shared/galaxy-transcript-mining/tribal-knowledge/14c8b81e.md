# tribal-knowledge session 14c8b81e (2026-06-09, 5.4MB, spine 74KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit 316872f7: `scripts/call-engine.mjs` + Windows/Bash wrappers, plus `call-engine.test.mjs` (8‑case node:test suite).  
- Commit 38af3407: `27-wsl-memory-guard.mjs`, its 15‑case test, and `install-wsl-memory-guard-task.ps1`.  

**DECISIONS**  
- MCP server is *not* required for code‑base navigation or H: drive tooling; all dispatcher functionality can be invoked directly via the new harness.  
- WSL memory cap (16 GB) was not being enforced – a `wsl --shutdown` is needed to apply it.  
- A scheduled watchdog (`install-wsl-memory-guard-task.ps1`) will flag future over‑runs but must be installed manually with admin rights.  
- Zombie tsservers and idle Ollama models were killed to relieve immediate pressure; this does not solve the root WSL issue.  

**FINDINGS/BUGS**  
- “API limit error” was caused by host memory pressure (vmmemWSL ~96 GB committed vs 16 GB cap) plus three resident Ollama servers (~65 GB).  
- Unloading idle Ollama models dropped commit from 98.4 % to 68.5 %.  
- The Stop‑hook “MCP Server=failing” warning was a false positive (task scheduler status, not actual server health).  

**DOMAIN SPECIFICS**  
- PRISM engines live in `src/engines/*.ts`; dispatchers are thin Zod routers that call `engine.method()`.  
- `call-engine.mjs` uses tsx to import these engines directly.  
- WSL memory guard reads `.wslconfig`, queries `vmmemWSL` via PowerShell, and reports over‑run status.  

**TOOLS USED**  
- Node.js (tsx, node:test), PowerShell scripts (`install-wsl-memory-guard-task.ps1`, `02-kill-zombie-tsservers.ps1`), Git, rtk, stable-session-id.mjs, per-agent-handoff.mjs, precompact-pending-guard.mjs.  

**OPEN THREADS**  
- Execute `wsl --shutdown` (requires user terminal).  
- Run `powershell -NoProfile -ExecutionPolicy Bypass -File H:/prism/.claude/helpers/install-wsl-memory-guard-task.ps1 -RunNow`.  
- Optionally wire the new guard into the fleet‑task‑health list.  
- Finalize session with `/compact` (handoff already written).
