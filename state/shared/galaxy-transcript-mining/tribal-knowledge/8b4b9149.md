# tribal-knowledge session 8b4b9149 (2026-06-09, 41.6MB, spine 459KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5ae481f748` – EPERM leak fix in OutcomeCaptureBusEngine (atomic append + retry).  
- `86f0e3fe0c` – wired SpeedFeedTriComparator, ExhaustiveCombination, DownstreamSubscriber to calcDispatcher.  
- `a2dbfa76e1` – closed‑loop driver; rescued 7 SF engines; fixed consensus bug.  
- `43e1b8e449` – G‑Wizard alignment symmetry check added.  
- `891c66e728` – full‑sweep driver + material‑map fix (N/H coverage, aluminum misclass).  
- `16d6eecef4` – SpeedFeedCalibrationPersistEngine (CPU calibration) with unit & dispatcher tests.  
- `890b2c39e7` – VC assessment spec addendum; memory snapshot.  
- `3438987f0e` – Calibration engine hardening: clamped‑flag, ledger_rows_skipped, loud‑when‑all‑dropped note.  
- `f31398a1a5` – SpeedFeedGpuJudgeEngine (GPU LLM judge) with dispatcher wiring.  
- `f5d14ddb29` – GPU judge hardening: probeGpuResidency exact‑match, silent‑exclusion note, fetch timeout, tmp cleanup.  
- `951f5ac335` – GPU judge polish (isProbe constant, suppression test).  
- `3a1c20fca2` – Full sweep engine full mode; runStreaming generator; 69 120‑cell tests.  
- `af0ac16c5a` – Driver ReferenceError fix (`totalCells`).  
- `b847c0f1ff` – Scoping spec for axis awareness (tool material, coolant, holder/machine/spindle, insert, controller/workholding).  
- `28eca0ed84` – Spec doc‑reflection of GPU judge findings.  
- `54c0ae06c8` – Memory file recording full sweep & GPU judge evidence.  
- `658c8280fe` – Tool‑material axis constants table; UltimateSpeedFeedEngine wired (10 tests).  
- `585584e3ae` – CoolantVcModifier algorithm wired into engine.  
- `7d0affcae6` – Rigidity factor de‑inlined to constants (`CANONICAL_MACHINE_RIGIDITY_VC_FACTOR`).  
- `f998f8af71` – Alts‑factor fix: tool‑material, coolant, rigidity multipliers in alternatives calculation.  
- `da41a58fd1` – Full 102 k‑combination sweep (calc half).  
- `c48829e331` – Axis‑liveness probe (all goal axes live).  
- `d03458fff1` – Probe extended to tool_life; closed runout loophole.  
- `2070c472a4` – Wired workholding.type → live (chip‑force clamp).  
- `7dc7798033` – Wired spindle.hp → live (power clamp).  
- `8b191d5427` – Wired holder‑runout → life (life‑reduction).  
- `81e37ece16` – Added numeric finish axis (`target_ra_um`).  
- `a257f872bb` – Updated probe to include toolpath.strategy, cut_type, mode; fixed phantom‑field bug.  
- `6a3ad56545` – Compare‑toolmaterial‑variability (576 comparisons).  
- `c78faa5a73` – Compare‑HSS baseline (54 points, 6 tests).  
- *(new)* – Compare‑CERAMIC‑CBN baseline (12/12 tests).

**DECISIONS**  
- Adopt 41 physics engines + 9‑axis shop‑context model; ISO coverage full.  
- Closed‑loop testing against 5‑vendor DB, G‑Wizard, HSMAdvisor; expose alignment flags; exclude non‑aligned externals unless sole source.  
- EPERM leak blocks closed‑loop → fixed before wiring SFC engines.  
- Consensus filter: exclude not‑aligned externals only when better data exists; fallback to include if sole external (R7).  
- Calibration persist engine advisory‑only; auto‑apply disabled, INCVC regimes flagged for safety.  
- Two‑layer closed‑loop: CPU calibration + GPU LLM judge.  
- Full sweep engine full mode covering all ISO groups, diameters, flutes, cut types, strategies, modes, coolant, holder, machine‑independent axes.  
- RunStreaming generator to avoid 50‑cell cap; 69 120‑cell tests.  
- Canonical additive constants table for axis multipliers (tool material, coolant, rigidity).  
- Apply axis factors only when explicitly specified; default inferred → 1.0.  
- De‑inline hardcoded rigidity factor to constants (`CANONICAL_MACHINE_RIGIDITY_VC_FACTOR`).  
- Extend alternatives calculation with tool material, coolant, rigidity multipliers.  
- Gating logic: reference‑data coverage drives baseline DB; asymmetrical gating (vendor side abstains only).  
- Baseline DB keyed by `(iso, tool_material, diameter_bucket, operation, cut_type)`; non‑carbide baselines added via web‑verified sources.  
- Defer non‑carbide richer baseline catalog OCR until operator input.  
- Keep controller inert on default Pareto‑knee (product philosophy).  

**OPERATOR DIRECTIVES**  
- Select auth provider (Clerk / Supabase / Auth0) to unblock revenue; deploy webhook signature fix (~½ day).  
- Confirm consensus logic excludes non‑aligned external systems.  
- Provide proven sfm values in G‑Wizard crib or adjust comparison logic accordingly.  
- Run full SFC closed‑loop training + compare all inputs with G‑Wizard & HSMAdvisor on new CPU (128 GB RAM), RTX 6000 Blackwell, NVMe SSD.  
- Continue from where left off; run calculations for every possible combination of machines, spindles, controller, materials, workholding/fixture, tool holder connection type, coolant, tool path type, cutting parameters, desired finish quality, roughing/semi‑finish/finish.  
- Run full vendor‑catalog OCR batch (Kennametal, etc.) on Blackwell GPU; generate `<vendor>-speed-feed-data.ts`.  
- Calibrate `CUTTING_PARAMS` from extracted tables; apply to engine constants.  
- Reconcile MRR factor with coolant/rigidity multipliers (`U-OSC-MRR-RECONCILE`).  
- Execute full sweep‑compare against live G‑Wizard and HSMAdvisor data after calibration.  

**FINDINGS/BUGS**  
- EPERM leak in OutcomeCaptureBusEngine caused data spine corruption; fixed by atomic append rewrite with retry & orphan cleanup.  
- Unwired SFC engines prevented full functionality; wired them.  
- G‑Wizard toolcrib has 0 tools with proven sfm → no speed/feed data; baseline comparison limited.  
- HSMAdvisor returns constant 634.6 m/min for all materials; flagged as unaligned and mistakenly included in consensus median, producing impossible numbers.  
- Consensus computation bug: includes not‑aligned externals; needs exclusion.  
- Missing N (aluminum) & H (hardened) coverage in exhaustive sweep; added to material map.  
- Aluminum incorrectly classified under M (stainless); corrected ISO group mapping.  
- Calibration factors derived from delta would increase Vc for 8/12 regimes → unsafe if auto‑applied; flagged INCVC.  
- GPU judge bugs: probeGpuResidency prefix‑match could hit wrong model; fixed to exact match and surface `matched_model`.  
- No fetch timeout in GPU judge; added 60 s AbortSignal.timeout.  
- Driver ReferenceError (`cells.length`) in non‑JSON path; replaced with `totalCells`.  
- ISO‑group collapse: tool material within a group produces identical Vc; physics uses ISO groups, not individual alloys.  
- Tool material, coolant, holder, machine, spindle, controller, workholding, insert are inert in current engine; plan to add physics for these axes.  
- PRISM under‑speeds published references by ~25 % on average; worst case –53 % (6061 Al).  
- HSS cast‑iron over‑speed by +108 % (123 SFM vs ~59 SFM literature); HSS steel mildly aggressive (+31%), aluminum matches baseline (-5%).  
- Ceramic under‑exploits hot capability (-49 % relative to baseline); CBN slightly aggressive (+48.6%).  

**DOMAIN SPECIFICS**  
- **Engines/dispatchers**: `SpeedFeedTriComparatorEngine`, `SpeedFeedExhaustiveCombinationEngine`, `SpeedFeedDownstreamSubscriberEngine`, `SpeedFeedBaselineComparatorEngine`, `SpeedFeedOutcomeFeedbackBridgeEngine`, `SpeedFeedSelfTuningEngine`, `SpeedFeedCalibrationPersistEngine` (CPU), `SpeedFeedGpuJudgeEngine` (GPU LLM), `UltimateSpeedFeedEngine`, `SpeedFeedNineAxisOrchestratorEngine`, `ClampingForceEngine`.  
- **Algorithms**: `CoolantVcModifier` (algo 8.5), `HPCVcBoostCalculator` (algo 8.7).  
- **Metrics/fields**: Vc (m/min), feed, rpm, mrr_cm3min, tool_life_min, cost_per_part_usd, cycle_time_min, safety gate `S(x)≥0.98`, `ledger_rows_skipped`, `gpu_resident`, `vram_used_mib`, `matched_model`, `target_ra_um`.  
- **Paths**: `src/physics/constants.ts`, `src/engines/speed-feed/UltimateSpeedFeedEngine.ts`, `src/algorithms/CoolantVcModifier.js`, `state/outcomes/sfc-full-sweep-ledger.jsonl`, `state/outcomes/sfc-calibration-model.json`, `buildModeRecommendation`, `translateToUltimate`.  

**TOOLS USED**  
- PRISM core tools: speed‑feed engine, orchestrator, comparator engines, test harnesses (`vitest`).  
- Dispatchers: `calcDispatcher.ts` (actions: `speed_feed_tri_compare`, `speed_feed_exhaustive_combination`, `speed_feed_downstream_packs`, `speed_feed_calibration_persist`, `speed_feed_gpu_judge`).  
- Build/test tooling: esbuild, tsc, vitest, node scripts (`update-prism-inventory.mjs`, `sfc-full-sweep-compare.mjs`).  
- GPU judge: Ollama LLM on RTX 6000 Blackwell (`qwen2.5-vl`), vision‑OCR for vendor catalogs (pypdf).  
- CI hooks: per-file scrutiny, physics reviewer arm, `hermes-workflow`, `forge-audit-v2`, `ultracode`.  

**OPEN THREADS**  
1. Auth provider selection & webhook signature deployment.  
2. Consensus logic exclusion of non‑aligned externals; adjust comparison logic for G‑Wizard proven sfm.  
3. Validate HSMAdvisor integration (ensure alignment flag usage).  
4. Closed‑loop testing with updated driver and honest reporting of alignment flags.  
5. GPU training layer (`PRISM_SFC_CALIB_APPLY`) to apply persisted calibration factors with safety gating; requires memory clearance & further testing.  
6. Add physics for remaining inert axes: tool material, coolant, holder/machine/spindle rigidity, insert geometry, controller/workholding.  
7. Optimize GPU judge throughput (batching, caching vendor files).  
8. Extend full sweep to include additional ISO groups or custom tooling if needed.  
9. Finalize safety gate logic for aggressive modes on hardened materials.  
10. Non‑carbide richer baseline catalog OCR (multi‑hour GPU job).  
11. Live G‑Wizard/HSMAdvisor numbers export & integration.  
12. Controller‑on‑default decision (product philosophy).  
13. HSS cast‑iron over‑speed fix (`U‑OSC‑HSS‑SPEED‑MODEL‑REVIEW`).
