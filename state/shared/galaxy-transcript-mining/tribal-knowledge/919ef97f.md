# tribal-knowledge session 919ef97f (2026-06-09, 17.8MB, spine 222KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `CatalogCorpusLoaderEngine.ts` (U1 keystone) – loads full 62 727‑tool corpus into `ToolCatalogEngine`. *Commit* aca389cc97.  
- U1 scrutiny‑fix – removes enum orphans & fixes `corpusStats()` bug. *Commit* a44345e90f.  
- `camDispatcher.ts` – added `ensureLoaded()` guard and uncapped `search()` in Fusion, Mastercam, hyperMILL, Inventor adapters (U3–U6). *Commit* 7c182b38b2.  
- `SpeedFeedOrchestratorEngine.ts` – resolves tool geometry via `tool_catalog_id`. *Commit* 3131f8ccae.  
- `millDispatcher.ts` / `turningDispatcher.ts` – expose `mill_tool_catalog_query` & `turning_tool_catalog_query`. *Commit* d839da375b.  
- Hidden Fusion branches fixed; added `ensureLoaded()` (U3‑secondary P2). *Commit* 0fe3b9de3a.  
- `regenerate-catalog-index.mjs` – regenerates `CATALOG_INDEX.json` to 62 727 entries. *Commit* 05fdfb0474.  
- Wiring of `ExpandingMandrelEngine.analyze` → new action `lathe_expanding_mandrel_analyze` in `prism_turning`. *Commit* f5d14ddb; attribution marker eeabb9d68e added to `AGENT_CHAT.jsonl`.

**DECISIONS**  
- Use top‑level `import` for `CatalogCorpusLoaderEngine` / `ToolCatalogEngine` (no circular dependency).  
- Add idempotent `ensureLoaded()` guard in all catalog consumers.  
- Expose full corpus via wizard dispatchers (`mill_tool_catalog_query`, `turning_tool_catalog_query`) so wizards can query 62 k tools directly.  
- Regenerate `CATALOG_INDEX.json` whenever vendor files change; keep reusable script for drift.  
- Close hidden Fusion search cap bug by uncapping all paths.  
- Treat `ExpandingMandrelEngine` as distinct action (R7); add engine schema to `surface_finish_compare`.  
- Use commit‑coordinator lane to avoid shared‑tree absorption during edits.

**OPERATOR DIRECTIVES**  
- None explicitly requested.

**FINDINGS/BUGS**  
- Search default `max_results=20` silently truncated exports – fixed.  
- `CATALOG_INDEX.json` undercounts tools (51 336 vs 62 727) – regenerated.  
- `require()` pattern caused test failures; replaced with top‑level imports.  
- Lost‑update race on `SCRUTINY_LEDGER.json` under concurrent writes.  
- Slot‑contention routing hooks misfire; resolved via kill switches / re‑binding slot branch.  
- Ghost actions in enums (e.g., `lathe_workholding_expanding_mandrel`) not wired – added enum entry.  
- Duplicate detection: existing `lathe_workholding_expanding_mandrel` caused R7 conflict – resolved with attribution marker.  
- CRLF→LF normalization produced large diff; fixed by normalizing on disk before staging.

**DOMAIN SPECIFICS**  
- **Engines:** `CatalogCorpusLoaderEngine`, `ToolCatalogEngine`, `SpeedFeedOrchestratorEngine`, `ExpandingMandrelEngine`, `SFCCompareEngine`.  
- **Dispatchers:** `camDispatcher` (Fusion, Mastercam, hyperMILL, Inventor), `calcDispatcher`, `millDispatcher`, `turningDispatcher`, `prism_turning`, `prism_calc`.  
- **Actions:**  
  * `tool_catalog_load_corpus`  
  * `tool_catalog_corpus_stats`  
  * `fusion_export_tool_library / mastercam_tool_export / hypermill_tool_export / inventor_tool_export`  
  * `mill_tool_catalog_query`  
  * `turning_tool_catalog_query`  
  * `lathe_expanding_mandrel_analyze`  
  * `surface_finish_compare`  
- **Metrics:** corpus size = 62 727 tools; search result counts; mandrel grip pressure, contact area, torque, radial deformation; SFC compare avgRa, stdDev, cpk, assessment.  
- **Data paths:** `data/CATALOG_INDEX.json`, `src/data/*.json` (vendor extracts).

**TOOLS USED**  
- PRISM slot helpers (`slot-bind-enforce.mjs`, `chat-slots.mjs`).  
- Dispatcher scripts (`calcDispatcher.ts`, `camDispatcher.ts`).  
- Engine source (`CatalogCorpusLoaderEngine.ts`, `ToolCatalogEngine.ts`, `SpeedFeedOrchestratorEngine.ts`, `ExpandingMandrelEngine.ts`).  
- Build/test tools: TypeScript compiler `tsc`, Vitest, Jest‑style round‑trip tests, tsx smoke tests.  
- Git hooks & routing (`main-tree-write-block`, `git-add-lane-guard`).  
- Script utilities (`regenerate-catalog-index.mjs`, `build-catalog-json.mjs`, stash/commit helpers).  
- PRISM code_search, commit_coordinator.mjs, workflow engine, Zod schemas, `AGENT_CHAT.jsonl` bus.

**OPEN THREADS**  
- **U7:** SFC catalog lookup integration (resolve tool by `tool_catalog_id`) – import strategy fix pending.  
- **U8:** Mill/lathe wizard bridge actions – implemented; further refinement may be needed.  
- **U9:** HSMAdvisor/G‑Wizard cribs – handed off to oscar.  
- **U10:** Machine‑DB export – handed off to juliett/oscar.  
- **U2:** Holder/insert split refinement – pending.  
- Wire `surface_finish_compare` using commit‑lane protocol; add enum, case, schema, round‑trip test.  
- Ensure future commits use lane; verify no CRLF normalization diffs.
