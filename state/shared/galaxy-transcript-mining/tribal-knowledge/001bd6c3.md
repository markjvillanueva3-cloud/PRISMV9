# tribal-knowledge session 001bd6c3 (2026-06-09, 51.8MB, spine 473KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hermes config patched: primary gpt‑oss:20b (fleet) + Claude Code fallback; earlier had claude‑opus‑4‑8 + qwen2.5‑coder.  
- Qdrant container started via Docker Desktop, running at :6333 with collections present.  
- Hermes app running (4 processes); `/api/status` reports `gateway_running=true`, `config_version=28`.  
- Config migration v0→v28 completed: `model.default=anthropic/claude‑opus‑4‑8`, `fallback_model=custom/qwen2.5-coder:32b@11434/v1`; later switched to gpt‑oss:20b primary.  
- Commit 28b72e4dee added `scripts/fill-galaxy-memory-sections.mjs`; updated 34 galaxy `MEMORY.md` to full 4 canonical sections; vault mirror matches engine tree.  
- Added U5b, U5c, U6, U7, U10, U9 commits for ollama‑tool‑agent, offload‑enforce, codegen, build‑loop, capability‑probe, coresidency.  
- Added U1, U2, U3, U4 commits: offload‑router roster refresh, route‑hook default change + `ollama-route-config.json`, take‑rate/savings honesty fix, compressor safety‑gating unit tests.

**DECISIONS**  
- Use MCP Path D for Obsidian vault read; keep Qdrant optional until service up.  
- Hermes primary gpt‑oss:20b (tool‑capable), Claude Code fallback for extra usage billing; run headless via CLI.  
- Vision models: switch to local Ollama (`qwen3-vl`) if image processing needed.  
- Adopt deterministic script for galaxy memory sections; exclude `node_*` graph dumps from counts.  
- Use gpt‑oss:120b for native tool‑calling, qwen2.5-coder:32b exclusively for code generation.  
- VRAM budget: NP 4→2, MAX_LOADED ≤ 4, un‑strand 120b from floor tiers; enforce keep_alive/mutex via U9.  
- Auto‑route per task using existing `OllamaTaskOffloaderEngine`; no new router needed.  
- Merge slot/bravo→main and wire `ollama-offload-enforce.mjs` into `settings.json`.

**OPERATOR DIRECTIVES**  
- Bring Qdrant up (Docker container) and verify collections exist.  
- Enable extra‑usage billing on Claude Code; confirm `/api/status` shows `config_version=28`, correct `model.provider`.  
- Restart Hermes after any config change; verify provider string in memory.  
- Ensure MCP server :3100 healthy; restart if stale “qdrant not connected”.  
- If Hermes reports HTTP 400 on Claude, enable billing or switch to local model.  
- Re‑enable PRISM Hermes‑Obsidian Bridge scheduled task.  
- Run `galaxy-synthesis-refresh.mjs` after owner fixes `ai-training_synthesis.md`.  
- Decide auxiliary vision model provider (Claude vs local).  
- Monitor memory pressure/commit usage; cleanup if high commit persists.  
- Confirm sync of `ask‑ollama.mjs` into bravo for U4.  
- Approve or defer live RTK `config.toml` edit (U5).  
- Decide on env‑gated Kimi free voice wiring.

**FINDINGS/BUGS**  
- Qdrant down → semantic_search no results; now running at :6333.  
- Hermes HTTP 400 “extra usage” error due to missing billing; resolved by enabling billing or local fallback.  
- Unknown provider `openai` fixed by using custom/Ollama or anthropic strings.  
- One‑shot CLI gpt‑oss:20b returned “no final response”; raise max_tokens/output limit.  
- GUI launcher stalled on dirty `package-lock.json`; fixed by stashing file and restarting.  
- 29/34 galaxies missing full memory sections; memory count inflated by `node_*` dumps; corrected via script.  
- Corrupt `ai-training_synthesis.md` flagged for owner.  
- VRAM math mis‑calculated: 120b + 32b cannot co‑reside on 96 GB GPU; addressed in U9.  
- Offload router default pointed to retired model → 0% offloading; fixed by U2.  
- Advisory classifiers inflated take‑rate/savings calculations; corrected in U3.  
- Safety denylist missed lowercase/no‑space G‑code patterns; fixed in U4.

**DOMAIN SPECIFICS**  
- Hermes: `prism_context:slot_brief_write`, `prism_memory`, `prism_session`; gateway :9120, MCP :3100.  
- Obsidian vault REST API :27123, memory bridge `hermes/memories`.  
- Qdrant vector store for semantic_search at :6333.  
- Engines/Dispatchers: `QdrantVectorStoreEngine`, `QdrantMemoryEngine`, Hermes MCP server, Hermes gateway.  
- Actions/Skills: `/qdrant-revive`, `prism_context:slot_brief_list`, `prism_session:master_index_query`.  
- Metrics/state: `config_version`, `gateway_running`, `model.provider`, Qdrant collection count.  
- Paths: `docker-compose.yml` (missing qdrant), `hermes_cli.main dashboard`, `node scripts/hermes‑obsidian‑app‑map.mjs`.  
- 34‑galaxy architecture: `engines/<g>/MEMORY.md`, `PATHS.md`, `TOOLBELT.md`; vault mirror at `memories/galaxies/<g>`.  
- Zulu orchestrator: `zulu-orchestrator-sweep.mjs` sends SendKeys to slot windows; no slots opted in (`zuluOptIn` empty).  
- Ollama local LLMs: `gpt‑oss:120b`, `gpt‑oss:20b`, `qwen2.5-coder:32b`, `qwen3-vl`.  
- Actions/Dispatchers for ollama: read/search offload, codegen, Opus tail review, capability probe, coresidency mutex; via `ask‑ollama.mjs`, `ollama-prism-bridge.mjs`, PreToolUse hook (`offload-enforce`).  
- Metrics: VRAM usage ≤95.6 GB, token cost `Read/IsLargeRead`, latency ~30 s per tool-loop.  
- Paths: slot/bravo worktree `H:/prism-slot-bravo`; main branch `cad-fusion-live-ms0`.

**TOOLS USED**  
- PRISM helpers: `/checkin-bravo`, `chat-slots.mjs`, `audit-roadmap-drift.mjs`, `wire-hermes-local-backend.mjs`, `mcp-server-supervisor.mjs`.  
- External: Docker Desktop (Qdrant), Ollama local server, npx, Claude Code OAuth (`~/.claude/.credentials.json`).  
- Hermes CLI: `hermes config migrate`, `hermes doctor`, `hermes -z`, `hermes.exe`.  
- Node scripts: `syncGalaxyMemories`, `fill-galaxy-memory-sections.mjs`, `galaxy-synthesis-refresh.mjs`, `hermes‑obsidian‑app‑map.mjs`.  
- Obsidian REST API `/27123`.  
- Ultracode workflows, `/loop` cron, `/startup-bravo`.  
- Scripts/hooks: `ask‑ollama.mjs`, `ollama-prism-bridge.mjs`, `offload-enforce (U5c)`, `tool-agent (U5b)`, `codegen (U6)`, `build-loop (U7)`, `capability-probe (U10)`, `coresidency (U9)`.  
- Vitest and `node:test` for unit tests.

**OPEN THREADS**  
- Verify Qdrant container healthy; collections exist.  
- Enable extra‑usage billing on Claude Code to remove 400 error.  
- Re‑test Hermes message flow after fixes.  
- Monitor fleet coordination once Qdrant back up.  
- Confirm Hermes runtime picks up new config after restart.  
- If :3100 daemon stale Qdrant connection, perform clean restart.  
- Decide auxiliary vision model provider (Claude vs local).  
- Monitor memory pressure/commit usage; cleanup if high commit persists.  
- Re‑enable PRISM Hermes‑Obsidian Bridge scheduled task.  
- Run `galaxy-synthesis-refresh.mjs` after owner fixes corruption.  
- Monitor Claude API rate limits; consider moving orchestration to local stack.  
- Ensure slots opt‑in for Zulu orchestrator once ready.  
- Decide on env‑gated Kimi free voice wiring.  
- Confirm sync of `ask‑ollama.mjs` into bravo for U4.  
- Approve or defer live RTK `config.toml` edit (U5).  
- Next build cycle resume at U4 if not redirected.  
- Start recurring `/goal /loop 10m assess ollama setup`; auto‑route tasks, verify local LLMs active, Claude Opus tail review.
