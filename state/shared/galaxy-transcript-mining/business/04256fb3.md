# business session 04256fb3 (2026-06-17, 61.6MB, spine 269KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `4c7fba6287`: suppress “MCP BRIDGE DOWN” banner on healthy server; bump probe timeout 1 s→3 s.  
- Commit `9da42f74c6`: add `serverUp` guard to `mcp‑bridge‑enforce.mjs`; prevent broadcast when bridges zero but server up.  
- Commit `89cd1b5da5`: extract capacity guard; added `mcp‑capacity‑contract.test.ts`.  
- Commit `[GOLF‑FLEET‑HYGIENE]/U-*`: golf hygiene unit on slot/golf.  
- Added E2E concurrency regression test: MCP capacity 64 concurrent / 512 queue, verifies a 16‑chat burst never queues.

**DECISIONS**  
- Golf slot now owns fleet‑reaper; reorient to recent sessions; run continuous loops via Hermes agents, Obsidian vault, Ollama offload.  
- Never burn Opus on mechanical text; use `ask` mode for inline input, file‑path modes routed through Ollama/Hermes proxy if needed.  
- Keep fleet reapers/monitors active unattended; monitor PC performance & task manager in loops.  
- Treat “0 transient bridges” as idle; gate all fleet‑wide banners on `/health` status.  
- Introduce `PRISM_MCP_FLEET0_BANNER` knob for legacy behavior.  
- Extract capacity guard into pure function, test with `mcp-capacity-contract.test.ts`; enforce 16‑chat floor via RequestSemaphore (64 concurrent / 512 queue).  
- Disable bridge‑down banner when bridge count zero; add `serverUp` flag to broadcast logic.  
- Reaper sweep + reclaim housekeeping; persist `reference_*.md` only on real anomalies.  
- Fleet health monitoring runs unattended with lean check‑in; alert thresholds: CPU > 90 %, RAM > 85 %, GPU temp > 83 °C.

**OPERATOR DIRECTIVES**  
- Keep fleet healthy: monitor CPU, RAM, GPU, task counts; never kill alive windows.  
- Snapshot PC performance (CPU %, RAM used %) and GPU metrics via PowerShell & `nvidia-smi`; flag if thresholds exceeded.  
- Census processes: node, claude, python, ollama, git, docker; detect orphans/spikes.  
- Run reaper sweep (`node scripts/fleet-reaper-sweep.mjs --once --json`) and chat‑slot reclaim (`node .claude/helpers/chat-slots.mjs reclaim`).  
- Verify durable tasks `PRISM Fleet Reaper`, `PRISM Fleet Memory Monitor` are Ready; start any disabled scheduled tasks.  
- Register missing `PRISM Fleet Task Health` scheduled task (PowerShell/UAC).  
- Post one‑line status to AGENT_CHAT bus; persist reference_*.md only on real anomalies.  
- Route triage/summarize to Ollama ask mode (`node scripts/ask-ollama.mjs ask "..."`); use Opus for judgment.  
- Verify frozen “romeo” window remains alive (~49 min idle).  
- Update ask‑ollama mode documentation (file vs inline).  
- Await operator approval before merging MCP detector changes.

**FINDINGS/BUGS**  
- Misrouted india `/goal` cron removed.  
- `romeo` chat frozen ~49 min ago but recovered; crashWatch flags idle chats benign.  
- GPU load high (81 GB) due to gpt‑oss:120b model, temp 36 °C, util 0%; last spike 84 °C transient inference burst.  
- No orphan node/python processes detected; reaper cleans only genuine orphans (reaped=2).  
- False‑positive “MCP BRIDGE DOWN” banner fired thousands of times during idle periods; real disconnects only 4. Bridge spawn count ~1,486 cycles in a 16‑chat session; zero live bridges normal idle.  
- Probe timeout increased from 1 s to 3 s eliminates spurious banners.  
- GPU temp trend peaked at 70 °C under heavy inference (still below 83 °C flag).  
- PRISM Fleet Task Health scheduled task missing; needs registration.  
- Stale slots reduced 7→3, crashes 6→2 after housekeeping.  
- Durable tasks `PRISM Fleet Reaper`, `PRISM Fleet Memory Monitor` remain Ready.

**DOMAIN SPECIFICS**  
- `fleet-reaper-sweep.mjs`: ancestry‑confirmed orphans, mcpZombieHunt, stuckHunt, crashWatch, GPU coordinator (24 GB free floor).  
- `.claude/helpers/chat-slots.mjs` reclaim: keeps window‑alive chats; reaps only dead PID/crashed.  
- Ollama ask mode inline text; other modes via file path; Hermes proxy at :8645 for managed models.  
- MCP server stateless per-request (`McpServer` per POST); gated by RequestSemaphore (64 concurrent / 512 queue).  
- Per‑chat bridges transient `mcp-http-bridge.mjs`; zero live bridges = normal idle.  
- Banner logic in `mcp-connectivity-check.mjs` and `mcp-bridge-enforce.mjs` gated by server health; `serverUp` flag added.  
- Scheduled tasks: PRISM Fleet Reaper, PRISM Fleet Memory Monitor (5 min cadence); PRISM Fleet Task Health missing.  
- AGENT_CHAT bus for status dissemination; OLLAMA ask script (`node scripts/ask-ollama.mjs`).  
- PowerShell perf watch + `nvidia-smi` used for monitoring.

**TOOLS USED**  
- Node scripts: `fleet-reaper-sweep.mjs`, `.claude/helpers/chat-slots.mjs`, `ask-ollama.mjs`, `ask-hermes.mjs` (proxy :8645).  
- PowerShell: Get-CimInstance, Get-Process, Get-ScheduledTask, Start‑ScheduledTask; CPU/RAM queries.  
- NVIDIA CLI: `nvidia-smi --query-gpu=memory.used,memory.total,utilization.gpu,temperature.gpu --format=csv,noheader`.  
- AGENT_CHAT bus for status dissemination.  
- Obsidian vault (`reference_*.md`).

**OPEN THREADS**  
- Register missing `PRISM Fleet Task Health` scheduled task (PowerShell/UAC).  
- Confirm GPU prewarm threshold logic; monitor future spikes.  
- Advance one clear golf hygiene unit when ready (commit `[GOLF‑FLEET‑HYGIENE]/U-*`).  
- Continue monitoring crashWatch idle chats; ensure no genuine hangs.  
- Final operator approval for MCP detector changes before production merge.  
- Verify capacity guard stability under real 16‑chat bursts.  
- Monitor GPU temperature trend over extended periods.  
- Documentation fix for ask‑ollama mode (file vs inline).
