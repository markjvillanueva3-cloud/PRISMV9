# business session 8b4b9149 (2026-06-10, 45.7MB, spine 500KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- 5ae481f748 – EPERM‑leak fix in `OutcomeCaptureBusEngine` (atomic append + retry).  
- 86f0e3fe0c – wired tri‑vendor comparators (`SpeedFeedTriComparatorEngine`, `SpeedFeedExhaustiveCombinationEngine`, `SpeedFeedDownstreamSubscriberEngine`) & 16 dispatcher tests.  
- 16d6eecef4 – `CalibrationPersistEngine` + tests.  
- 3438987f0e – hardened `CalibrationPersistEngine`.  
- f31398a1a5 – added `SpeedFeedGpuJudgeEngine` (driver).  
- f5d14ddb29 – GPU‑judge hardening & bug fixes.  
- 951f5ac335 – polished GPU‑judge test coverage.  
- 3a1c20fca2 – full 69 k‑cell sweep + `runStreaming`.  
- af0ac16c5a – ReferenceError & ISO‑group collapse fixes.  
- b847c0f1ff – axis‑awareness scoping spec.  
- 54c0ae06c8 – spec addendum.  
- e9b68da865, 658c8280fe – canonical `CANONICAL_TOOL_MATERIAL_SPEED_FACTOR` table (carbide 1.0 / HSS 0.35 / ceramic‑CBN 2.5).  
- 585584e3ae – wired `CoolantVcModifier` into `UltimateSpeedFeedEngine`; removed duplicate table.  
- 7d0affcae6 – de‑inlined hardcoded rigidity factor to constants.  
- f998f8af71 – hoisted tool-material, coolant, rigidity multipliers into alternatives path.  
- da41a58fd1 – full 102 k‑combination sweep (VC, RPM, feed, MRR, life).  
- c48829e331 – axis‑liveness probe; identified 8 live / 4 inert axes.  
- d03458fff1 – extended liveness to `tool_life`; confirmed runout→life inertness.  
- 2070c472a4 – wired `workholding.type` → live (chip‑load clamp).  
- 7dc7798033 – wired `spindle.hp` → power clamp.  
- 8b191d5427 – wired `tool_holder.type` runout→life; added runout impact to life.  
- 0a146e22c6 – removed inlined controller look‑ahead constant.  
- 81e37ece16 – added numeric finish‑Ra axis (`target_ra_um`) with physics‑reviewer cap.  
- a257f872bb – updated liveness probe (toolpath.strategy, cut_type, mode).  
- 6a3ad56545 – `U‑OSC‑COMPARE‑TOOLMATERIAL‑VARIABILITY` (576 tool_material sweeps).  
- c78faa5a73 – `U‑OSC‑COMPARE‑HSS‑BASELINE` (web‑verified HSS baseline, 54 pts, 6 tests).  
- 835df42c74 – `U‑OSC‑COMPARE‑CERAMIC‑CBN‑BASELINE` (ceramic & CBN entries added, 12 tests).  
- 907e74acab – `U‑OSC‑HSS‑SPEED‑MODEL‑REVIEW` (fixed HSS cast‑iron +108% over‑speed; CBN +49%).  
- 4c544db4ae – `U‑OSC‑COMPARE‑PER‑VENDOR` (per‑source deltas).  

**DECISIONS**  
- Adopt bootstrap slot‑enforce commit pattern for shared‑tree changes.  
- Replace tmp‑rename append with atomic file append + bounded retry; remove orphan temp logic.  
- Wire engines via dispatcher enum/case pattern (lazy import, try/catch, `{success:true,result}`).  
- Present closed‑loop results honestly: show alignment flags & abstention reasons.  
- Add GPU‑based “machinist‑judge” layer (`SpeedFeedGpuJudgeEngine`).  
- Full‑mode sweep in `SpeedFeedExhaustiveCombinationEngine` (~69 k cells); replace 86‑cell demo.  
- Implement `runStreaming` generator to stream all cells (no 50‑cap).  
- Fix clamped flag false‑positive, tmp‑cleanup on persist rename failure, skipped‑rows observability.  
- Add exact‑model match logic for GPU probe (`matched_model`) & fetch timeout.  
- Determine tool material, coolant, holder physics, machine/spindle/controller/workholding/insert are inert; root cause in Vc formula.  
- Dedup‑first: use existing `CoolantVcModifier`/`HPCVcBoostCalculator`.  
- Move all physics constants to `physics/constants.ts`; enforce `CONFIRM_CRITICAL=true`.  
- Apply axis multipliers only when explicitly specified (defaults 1.0).  
- Resolve orchestrator default mode by including multipliers in alternatives so PRISM‑optimized recommendation reflects tool-material, coolant, rigidity.  
- Ship workholding, spindle power, runout-life, finish‑Ra as safety‑positive clamps; controller inert on default per physics‑reviewer verdict B.  
- Add `baseline_detail.per_source` to `TriCompareResult`.  
- Widen clamp band for tool‑material speed factor (0.10–8.0).  
- Extend `BASELINE_DB` with web‑verified HSS, ceramic, CBN entries; add `BaselineSource` enum extensions (Tungaloy/Iscar/NTK).  
- Surface PRISM vs G‑Wizard/HSMAdvisor published references rather than live closed‑app data (operator‑gated).  
- Treat PRISM fz over‑feed (+67–91 %) as deliberate low‑speed/high‑feed roughing strategy.  

**OPERATOR DIRECTIVES**  
- Choose an auth provider (Clerk / Supabase / Auth0) to unblock revenue path.  
- Ship webhook signature fix (provider‑independent, ~½ day).  
- If operator supplies proven G‑Wizard `sfm` data, add real tool selector for meaningful comparison.  
- Session‑scoped Stop hook active: run calculations for every possible combination of machines, spindles, controller, materials, workholding/fixture, tool holder connection type & holding mechanism (balance, max speeds, rigidity, dampening capability, accuracy), tooling + insert if viable, coolant type, tool path type, cutting parameters, desired finish quality, roughing.  
- Schedule recurring prompt `/loop` every 10 min: `/goal [ Run calculations … ]`.  
- Run calculations for all machining axes with max variability and compare against G‑Wizard/HSMAdvisor.  
- Ensure galaxy mapped to `H:\PRISM\resources`.  
- Investigate Charlie’s vendor catalog pulls (241 PDFs, 80 high‑priority vendors).  
- Evaluate newer models on Blackwell GPU (`gpt‑oss 120b`, `qwen2.5‑coder 32b`).  

**FINDINGS/BUGS**  
- EPERM leak resolved; orphan tmp files cleaned.  
- G‑Wizard crib contains 0 tools with proven `sfm`; cannot contribute to comparison unless operator enters data.  
- HSMAdvisor returns constant 634.6 m/min for all materials, flagged unaligned; engine marks it but consensus median includes it.  
- Consensus computation bug: not‑aligned externals averaged into median.  
- Clamped flag false‑positive corrected.  
- Persist tmp not cleaned on rename failure fixed with try/finally.  
- Skipped_rows observability added (`ledger_rows_skipped`).  
- ProbeGpuResidency prefix‑match bug replaced with exact match (`matched_model`).  
- ReferenceError: `cells.length` resolved by using `totalCells`.  
- ISO‑group collapse – all materials within an ISO group produce identical Vc; documented and locked.  
- Inert axes (coolant, holder type, machine/spindle/controller/workholding/insert) identified; root cause in Vc formula.  
- PRISM under‑speeds published references by ~25 % mean, worst –53 % on 6061 Al.  
- Orchestrator’s default mode omitted tool-material/coolant/ridigity factors → axes appear inert at surface.  
- G‑Wizard `toolcrib.csv` geometry‑only; no live speed data for comparison.  
- Vendor catalog PDFs font‑encoded; require vision‑OCR on Blackwell GPU.  
- HSS cast‑iron over‑speed +108% fixed.  
- CBN hardened over‑speed +49% fixed.  
- PRISM fz consistently 67–91 % higher than all published vendor tables; deemed valid strategy.  

**DOMAIN SPECIFICS**  
- **Engines/Dispatchers:** `OutcomeCaptureBusEngine`, `SpeedFeedTriComparatorEngine`, `SpeedFeedExhaustiveCombinationEngine`, `SpeedFeedGpuJudgeEngine`, `CalibrationPersistEngine`, `UltimateSpeedFeedEngine`, `SpeedFeedNineAxisOrchestratorEngine`, `SpeedFeedBaselineComparatorEngine`.  
- **Metrics/Paths:** Vc, RPM, feed_rate_mmmin, MRR, tool_life_min, cost_per_part_usd, cycle_time_min; ledger_rows_skipped; gpu_resident (`gpu_resident:true`), matched_model.  
- **Sweep paths:** full‑mode sweep (`full`), streaming ledger output (`runStreaming`), `sfc-full-sweep-compare.mjs`, `sfc-all-axis-sweep.mjs`.  

**TOOLS USED**  
- PRISM core tools (dispatcher, engine wiring, spec memory hooks).  
- External: Ollama (`gpt‑oss 120b`, `qwen2.5‑coder 32b`), nvidia‑smi for GPU metrics, Node.js `AbortSignal.timeout`, `fs/promises`.  
- Testing: Vitest (dispatcher tests), Jest (invariant tests).  
- Scripts: `tsx`, `sfc-full-sweep-compare.mjs`, `SpeedFeedGpuJudgeEngine.ts`, `runStreaming` generator.  
- Git hooks (`slot-bind-enforce`).  
- Ultracode orchestration, forge‑audit‑v2 audit skill, hermes‑workflow planner.  

**OPEN THREADS**  
- Implement real G‑Wizard tool selector when operator supplies proven sfm data.  
- Fix consensus logic to exclude not‑aligned externals.  
- Deploy webhook signature fix and finalize auth provider integration.  
- Re‑run closed‑loop test after above fixes.  
- Vendor calibration OCR batch on Blackwell GPU (Kennametal first).  
- MRR reconciliation (`U‑OSC‑MRR‑RECONCILE`).  
- Rigidity‑DOC integration (`U‑OSC‑RIGIDITY‑DOC`).  
- Remaining inert axes (holder/spindle/controller/workholding/insert) to be fully swept.  
- Full sweep vs live G‑Wizard toolcrib and HSMAdvisor once calibration applied.  
- Live extraction of G‑Wizard/HSMAdvisor closed‑app data (operator‑gated).  
- Catalog‑OCR pass for richer carbide baseline.  
- Physics‑reviewer unit to validate PRISM fz‑force envelope.  
- Final confirmation that all machine/spindle/controller/workholding/holder/coolant axes are fully swept at max variability.
