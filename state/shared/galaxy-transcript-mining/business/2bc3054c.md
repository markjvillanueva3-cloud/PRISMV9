# business session 2bc3054c (2026-05-25, 46.1MB, spine 144KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**

- `b7690253ac` – EmployeeShopFloorMobileEngine + 59 unit‑tests (QR scan, task state machine, messaging, hot‑job audit).  
- `a0fca79858` – 17 base `emp_*` actions wired into `prism_shop`.  
- `03b3781dad` – 6 phone‑calculator actions (`emp_calc_speed_feed`, `emp_calc_kienzle`, etc.).  
- `40a6e8aa80` – 10 doc‑intake / program‑gen / DNC/CIMCO actions.  
- `71fe33d699` – Role‑based ACL (R1) with 9 tests.  
- `142c04aaf7` – 11 round‑trip dispatcher integration tests.  
- `98c9293fe7` – ShopFloorLayoutEngine + 29 tests, 10 `emp_layout_*` actions (video walkthrough → 3D layout).  
- `e23d046509` – ACL auto‑attach to EmployeeEngine + 5 office‑personnel aggregation actions (`emp_office_*`).  
- `90991099ca` – Frontend: `EmployeePhonePortalPage.tsx`, API client `employeePortal.ts`.  
- `iter10` – DistributorSearchEngine (MSC, Grainger, McMaster…) + 25 tests, 5 dispatcher actions.  
- `iter11` – HaversineDistanceAlgorithm + VendorRegionEngine + 44 tests, 4 dispatcher actions.  
- `e5d4c2e176` – AmortizationSchedule + RecurringExpense (G8).  
- `a3da9d6c37` – InventoryReorderPoint, ARAgingEngine, PriceBreakOptimization, ABCClassification (G11‑G13).  
- `f6a98430f8` – ToolLifeEconomicReplacement (G10).  
- `044eaba95d` – CriticalPathScheduling (G5).  
- `e1414eac60` – BillOfMaterialsRollup (G4).  
- `f28feb7e95` – JobRoutingTemplateEngine (G7).  
- `6569dea057` – VendorQuoteToPurchaseOrderEngine (G3).  
- `f76700a282` – InvoiceTextParserFormula & X12EdiSegmentParserFormula (G2/G6).  
- `01ab2d277c` – ProspectiveCustomerEngine, FirstContactEmailTemplateFormula, jm‑die‑prospects‑seed.  
- `466b943e2e` – National prospect expansion (20 prospects, $23.4 M pipeline).  
- `c3f9a856f7` – JM Die user profiles & RBAC (8 team members, 35 tests).  
- `26b1c803dd` – Tuesday Email/Print Intake Engine (PDF/CAD extraction, dedup, PII gate).  
- `3a21b4f7e2` – PDF Auto‑Populate Processor.  
- `8b86437610` – MachiningVisionDiagnosticEngine (photo‑based chip/part/tool analysis & G‑code adjustment).

**DECISIONS**

- Use slot‑bind‑enforce hook for deterministic hotel‑slot acquisition; skip manual bash when hook succeeds.  
- Adopt pathspec commit form (`git commit -m … -- <file>`) to avoid peer‑absorption races.  
- Wire all new engines through `prism_shop` or `prism_business` dispatchers, then add PSN synergy via wiki & MEMORY.md entries.  
- Keep hotel‑soul invariants: R12 fail‑loud, defensive copies, reason ≥ 3 chars for privileged actions, S(x)≥0.990 gate on DNC export.  
- Build mobile‑first features (scan, messaging, hot‑job priority) before desktop portals; UI lives in `EmployeePhonePortalPage`.  
- Use Vitter Algorithm‑R reservoir sampling for telemetry latency percentiles; expose via `AutomationChainTelemetryEngine`.  
- Adopt PSN 11‑leg integration for every new asset to guarantee cross‑domain synergy.  
- Enforce R12 fail‑loud: no silent failures, explicit error codes (`*_NOT_WIRED`).  
- Use slot‑worktree migration (`H:/prism-slot-hotel`) to eliminate peer‑absorption; all future commits target `slot/hotel`.  
- Keep all PII redacted on export; use `verify` prefixes for seed data.  
- Separate pure algorithm modules from stateful engines; keep adapters “not wired” until operator configures them at boot.

**OPERATOR DIRECTIVES**

- “Fill all gaps. Generate new algorithms and formulas to better results, calculations and efficiency | build, wire and synergize to PSN and prism app/phone app.”  
- Earlier directives: close out hotel units, build phone portal for shop‑floor & office personnel, add video walkthrough → 3D layout, build backend for all portals (business, quoting, accounting, sales, JM Die), compare with competitors, assess document‑reading capabilities.  
- “continue jm die user profiles. make sure pdf intake features are active in the app for auto populating tooling and inventory and properly synergized to the whole app and all features.”  
- “build a feature that will allow the operator to take a picture of chips, part and tooling to determine issues to make automatic parameter adjustments to the program and speed and feed alterations to ensure proper chip thickness and formation and heat dissipation for roughing operations.”

**FINDINGS/BUGS**

- Shared‑tree `git‑add‑A` race caused peer‑absorption; mitigated by pathspec commits and early slot‑worktree fork rule.  
- MaterialRegistry repoint stale: only 1 ISO group present in `materials_v9_complete`; full conversion needed.  
- `CatalogRegistryBridgeEngine.enrichAll()` never called, leaving vendor catalogs unused until DistributorSearchEngine added.  
- VendorRegionEngine missing distance sorter; Haversine algorithm implemented (iter 11).  
- RecurringExpenseEngine and AmortizationScheduleFormula still under construction (iter 12).  
- ABC classifier mis‑bucketed single‑item case → switched to before‑cum rule.  
- PriceBreak test over‑specified candidate count → relaxed to feasibility assertion.  
- Vision diagnostic engine had missing confidence floor; added 0.85 threshold and anti‑runaway delta cap (30 %).  
- Multiple peer‑absorption incidents: `a3da9d6c37`, `e1414eac60`, `f28feb7e95`, `26b1c803dd`, `3a21b4f7e2`. All code present; attribution fixed via slot‑worktree migration.  
- NotWired adapters (`NotWiredImapAdapter`, `NotWiredPdfTextExtractor`) now throw explicit errors until wired.

**DOMAIN SPECIFICS**

- **Engines:** EmployeeShopFloorMobileEngine, ShopFloorLayoutEngine, DistributorSearchEngine, VendorRegionEngine, RecurringExpenseEngine, AutomationChainTelemetryEngine, AmortizationSchedule, InventoryReorderPoint, ARAgingEngine, PriceBreakOptimization, ABCClassification, ToolLifeEconomicReplacement, CriticalPathScheduling, BillOfMaterialsRollup, JobRoutingTemplateEngine, VendorQuoteToPurchaseOrderEngine, InvoiceTextParserFormula, X12EdiSegmentParserFormula, ProspectiveCustomerEngine, EmailPrintIntakeEngine, IntakeArtifactProcessor, MachiningVisionDiagnosticEngine.  
- **Actions/Dispatcher:** `emp_*` (scan, task state, messaging, hot‑job audit, office aggregation), `distributor_*`, `vendor_region_*`, `prospect_*`, `email_intake_*`, `intake_*`, `vision_*`. 29 actions total.  
- **Metrics/guards:** Telemetry percentiles via Vitter Reservoir; job priority audit logs; live job tracking timestamps; confidence floor ≥ 0.85; dedup by SHA‑256 + message_id; anti‑runaway delta cap 30 %; PII redaction; R12 fail‑loud.  
- **API paths:** `/api/v1/portal/*`, `/api/v1/business/*`; `/prism_business/prospect_first_contact_email`; `/email_intake/run_batch`; `/intake/process_artifact`; `/vision/analyze_photo`.

**TOOLS USED**

- PRISM slot‑binding hook (`slot-bind-enforce.mjs`).  
- Chat‑slots helper scripts (`chat-slots.mjs`).  
- Checkin pipeline (`checkin.md`).  
- System‑viz ping, audit‑roadmap‑drift.  
- R12 fail‑loud pattern, Vitter Algorithm‑R reservoir sampling.  
- Pathspec commit form for atomic file commits.  
- PRISM core: `businessDispatcher.ts`, `prismBusiness.ts`.  
- PSN hooks, `/checkin-hotel §2c` migration scripts.  
- Skill scripts: Karpathy pre‑coding, PII redaction utilities.  
- Testing harness: Jest/TS test suites (358+ tests).

**OPEN THREADS**

1. Express dispatch routes for `/api/v1/portal/*` and `/api/v1/business/*`.  
2. Mobile AR walkthrough client (iOS ARKit / Android ARCore).  
3. Persistence backend (SQLite/qdrant write‑through).  
4. MaterialRegistry repoint – convert JS batches to JSON per material.  
5. Full double‑entry GL/AP/AR ledger implementation.  
6. Live pricing pipeline (provider adapters, 24 h cache).  
7. Parts ordering for fleet machines (per‑machine BOM bridge).  
8. Recurring expense tracking (subscription flagging).  
9. Region‑aware vendor selection – wire distance sorter into distributor search logic.  
10. G3–G6 gaps: live pricing, parts ordering, recurring expenses, GL/AP/AR ledger.  
11. Wire production adapters: IMAP (`imapflow`), PDF extractor (`pdf-parse`), vision model (ONNX/TF.js chip classifier).  
12. Integrate UPS/FedEx and Excel/Docustrata ingestion for Vicky’s shipping portal.  
13. Build UI components: Sales tab in EmployeePhonePortalPage, interactive tutorials for owner, PRISM feature showcase reports.  
14. Implement G2 (AP‑invoice OCR) and G6 (Customer EDI X12) with external runtimes; schedule dedicated cycles.  
15. Finalize slot‑worktree migration (`/checkin-hotel §2c`) to prevent future absorption.
