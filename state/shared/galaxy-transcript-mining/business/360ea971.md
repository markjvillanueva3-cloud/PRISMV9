# business session 360ea971 (2026-06-25, 5.8MB, spine 109KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `6a361cfb01` – Compliance‑cluster routing (OSHA incident/create, OSHA 300 log, audit schedule/finding/CAPA, management‑review package).  
- `796be2c6f1` – Vendor Scorecard adapter: engine `listScorecards()`, dispatcher `vendor_list_scorecards`, routes `/vendor-list`, `/vendor-scorecard/:id`; 7 unit tests.  
- `49bc4ed13a` – Pipeline Forecast & Stages adapters: engines `pipelineForecast()`, `pipelineStages()`, routes `/pipeline-forecast`, `/pipeline-stages`; 2 unit tests.

**DECISIONS**  
- Three‑tier wiring model: pure‑wire, adapter‑required, engine gap.  
- Adapter‑first for clusters where FE expects richer shapes (vendor scorecards, pipeline/vendor/root‑cause).  
- Keep `avg_lead_days` signed as schedule variance (negative = early); document probabilities: cold→researched→first_contact→engaged→quoted→won.  
- Remove dead `-Infinity` branch in scorecard logic; use defensive throw.  
- Route‑only adapter for `/root-cause-incidents`: unwrap `{count, root_causes}` → array of incidents (`ncr_id→id`, `recorded_at→date`). Defer orphan `/root-cause-analysis`.  

**OPERATOR DIRECTIVES** – none beyond initial assessment.

**FINDINGS/BUGS**  
- 53 dead FE calls → 48 after vendor & pipeline commits.  
- Shape mismatch in pipeline/vendor/root‑cause caused revert of route edit.  
- Stale FE tree `H:/prism/web`; canonical build is `mcp-server/web`.  
- QuickBooks parity backend (22 engines, dispatcher actions) lacks FE routes/UX spec.  
- Root‑cause page expected array; engine returned `{count, root_causes}` → adapter added.  
- Orphan `/root-cause-analysis` remains unimplemented.

**DOMAIN SPECIFICS**  
- Engines: `ShippingReceivingLogEngine`, `QualityManagementEngine` (`gage_rr_msa_calculate`), `NotificationEngine` (`ne_send`, `ne_mark_read`, `ne_set_preferences`), `QuickBooksParityEngine`, `ProspectiveCustomerEngine`.  
- Routes: `/osha-incident-create`, `/osha-log-300`, `/audit-schedule`, `/audit-finding-create`, `/audit-capa-create`, `/management-review-package`, `/vendor-list`, `/vendor-scorecard/:id`, `/pipeline-forecast`, `/pipeline-stages`, `/root-cause-incidents`.  
- Metrics: vendor scorecards (quality, delivery, price, composite), audit schedules, OSHA 300 logs, signed `avg_lead_days` schedule variance, stage probabilities, backlog value, forecast windows.

**TOOLS USED**  
- PRISM helpers (`chat-slots.mjs`, `audit-roadmap-drift.mjs`).  
- Routing helpers (`rfqRoute`, `bizGet`, `callTool`).  
- Dispatcher (`registerBusinessDispatcher`).  
- Jest (`tsx` runner) for unit tests.  
- Build/type‑check (`tsc`, `build`).  
- Auditor for dead client call tracking.

**OPEN THREADS**  
- Wire remaining pure‑wire clusters: downtime‑pareto, active‑machine‑jobs, employee‑ytd.  
- Adapter for pipeline/vendor/root‑cause cluster (full NC flow).  
- Implement engine gaps: shipping list/BOL/tracking, calibration lockout/GR&R, kanban board, QBO per‑type sync.  
- Add QuickBooks‑style UX routes; integrate `qb-parity-erp-ux-design-spec.md`.  
- Resolve stale FE tree (`H:/prism/web`).  
- Implement orphan `/root-cause-analysis` POST (contained‑NC status mapping).  
- Address E2/QBO integrations, notification writes, audit‑list/8D actions.
