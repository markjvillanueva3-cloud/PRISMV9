# business session 0e5669d2 (2026-06-09, 43.1MB, spine 360KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- B1 sync‑resilient (`168c202646`) – `writeWithRetry` wired to `obsidian-memory-sync.mjs`.  
- B2 index‑meta (`ea21008f25`) – safe in‑place meta‑stamp for `index.md`, added to regen chain.  
- B3 maint‑cron installers (`8c4dff660a`) – Windows cron scripts (promote & rot‑sentinel) installed, disabled by default.  
- C2 link‑heal scorer (`984313825e`) – tightened scoring, auto‑apply gate, decay‑leak fix.  
- C3 tribal‑coverage refresh (`838123429b`) – audit JSONs refreshed; coverage 83.7 %.  
- Fix‑memory artifact (`45225e0cc3`) – persisted build state to `C:/` & `H:` mirrors.  
- Hook‑heap cap – `portable-node` wrappers reserve **384 MB** per hook (was 4 GB).  
- MCP server maxConnections raised to **512** (`mcp-server/src/index.ts`).  
- Fleet‑task‑health watch (`3d796dcf5c`) – added `EXPECTED_UNREGISTERED_TASKS`, cleaned banner, drift‑sync for WSL guard.  
- Vault‑backlink schema & builder (`96ed5222e2`), staleness flag added (`d856173b86`).  
- Regen‑viz hook‑wire (`0e2724871a`) – auto‑refresh `vault-backlinks.json`.  
- Dispatcher actions (`14aba14e3a`, `4a44b5393c`) – `prism_session:doc_nodes` wrapper, tests, schema.  
- Vault‑access map updated (`f23c58b42d`) – closed `.canvas` gap, fixed stale “memory‑rag‑inject” claim.  
- Canvas‑read lib & CLI (`2d49bf0d33`, `47fe91554f`).  
- Ollama synthesis offload active via `host-aware-synthesis-model.mjs` (qwen2.5‑coder:32b).  
- T1 – U‑VIZ‑WIKI‑NARRATIVE (`cd54edb940`) – GPT‑OSS paragraph generator, 15 tests.  
- T2 – OLLAMA‑PIPELINE‑INJECTOR wired (`7ec4a5ea02`).  
- #8 – OLLAMA‑PREWARM‑WIRE (`65a29220e5`).  
- #2 – U‑TRIBAL‑QA‑LLM‑UNBLOCK (`6fb278a2ee`) – 19/19 tests.  
- #33 – MEMO‑EXTRACT‑THROTTLE & USER‑TYPE FIX (`f1b69db664`, `1ecf50b6be`).  
- U‑NAV‑ACCEL‑GAP‑A‑FALSIFIED spec corrected (`2c766b9308`).  
- #5 SHIPPED (`9ce1ea6633`).  

**DECISIONS**  
- Use `/checkin-sierra` wrapper to guarantee slot binding before canonical `/checkin`.  
- Persist audit/artifact surfaces (wiki, galaxy brain, two memories, handoff).  
- Wire `memory‑rag‑inject` via global settings; close P0 “RAG‑wire” first.  
- Bounded‑batch (`--batch 100`) link‑fix regeneration to avoid OOM.  
- Cap hook heaps at 384 MB (avoid over‑commit); defer MCP restart until operator approval.  
- Introduce `EXPECTED_UNREGISTERED_TASKS` to filter intentional deferrals.  
- Adopt qwen2.5‑coder:32b as default Ollama model; use `callOllama + resolveSynthesisModel`.  
- Prioritize Gap A first but reject regeneration (spec false).  
- Keep injector & prewarm hooks fail‑open, keyword‑gated.  

**OPERATOR DIRECTIVES**  
- “Fill all gaps” – complete remaining B/P1/P2/P3 units.  
- Ensure obsidian vault built and operational; map paths for token savings.  
- Validate commit‑cap fix: monitor commit charge & RAM after full fleet cycle.  
- Tidy missing‑task banner noise (already done).  
- Write handoff, recommend `/compact`.  
- Arm deferred items: restart MCP daemon post‑migration lift; register vault crons (`Vault Memory Promotion Cron`, `Vault Rot Sentinel Cron`).  
- Run `/startup-sierra` → force claim sierra slot, bind handoff to `sierra-work`, then full `/startup`.  
- Autonomous loop `/loop [10m] /goal "finish all ollama synergy tasks…"`.  
- Use strongest viable Ollama models for grunt work (searches, reads, coding).  

**FINDINGS/BUGS**  
- `obsidian-memory-sync.mjs`: unguarded `ensureDir`/`writeFileSync`; missing export of `syncMemories`.  
- `memory‑rag‑inject` unwired → no keyword recall.  
- Stale frontmatter in `index.md`; `WikiIndexMaintainerEngine.formatIndex` never invoked.  
- Scorer overconfidence (score ≥ 0.85) on noisy prefix/substring matches; decay‑leak for long slugs (>60 chars).  
- Corrupted `wiki-link-fix-candidates.json` (`autoApplyCount: 14100`).  
- Full‑corpus regeneration OOM; only 100‑link batch succeeded.  
- WSL balloon (95 GB) not respecting `.wslconfig`; resolved via `wsl --shutdown`.  
- Lgit layer miscounted regex `L[0-9]+[a-z]?`.  
- Peer contamination risk mitigated with surgical staging/atomic checkout‑reinsert.  
- Duplicate hook wiring; gpt‑oss empty‑response due to `num_predict` starvation (fixed by raising to 1024).  
- Memo‑extract throttle: fleet‑wide rate file blocked chats; replaced with per‑session throttle.  
- Transcript path bug – now uses `transcript_path`.  
- User turns dropped – capture both `type:"user"` and `type:"human"`.  
- Command injection in `execSync(curl …localhost)` → replaced with `callOllama`.  

**DOMAIN SPECIFICS**  
- Engines/dispatchers: `WikiIndexMaintainerEngine.formatIndex`, `node-card-prefetch-inject`, `memory-rag-inject.mjs`, `sessionDispatcher.ts`, `sessionActionSchemas.ts`.  
- Actions: `obsidian-memory-sync.mjs` (sync), `promote-memory-to-wiki.mjs`, `vault-rot-sentinel.mjs`, `regen‑wiki-from-viz.mjs`, `generateBlurb`.  
- Metrics/paths: node‑access map (`obsidian-vault-node-access-map.md`), system‑viz/MEMORY.md, settings.json at `C:/Users/.../.claude/settings.json` & `H:/prism/.claude/settings.json`; vault‑backlinks JSON; `node-card-offsets.json`.  
- Unique paths: `H:/last.md`, `H:/prism-slot-sierra`, `H:/prism/slot/sierra-work`.  

**TOOLS USED**  
- PRISM helpers: `/checkin-sierra`, `chat-slots.mjs`, `settings-wiring`.  
- Ultracode workflow (6‑agent orchestration).  
- Node tooling: `node:test`, `vitest`, `git commit` with lane/slot guards.  
- Scripting: Bash, PowerShell installers, JSON manipulation scripts.  
- Scrutiny gate (2‑arm reviewers), ledger.  
- Portable-node wrappers (`bash & cmd`).  
- MCP server (`mcp-server/src/index.ts`).  
- Fleet‑task‑health watch (`fleet-task-health-watch.mjs`).  
- Ollama helpers: `ask-ollama.mjs`, `resolveSynthesisModel`, `callOllama`.  

**OPEN THREADS**  
- Full‑corpus link‑fix candidate regeneration (OOM → streaming/chunked).  
- Performance tuning of suggester for 15 k×51 k comparisons.  
- Enable disabled cron jobs (`promote`, `rot‑sentinel`) after migration freeze lift.  
- Add `coverageComplete` flag to candidates output.  
- MCP restart to apply `maxConnections=512`.  
- Register vault crons post‑lift.  
- Monitor GPU offload coverage after pattern expansion.  
- Confirm WSL Memory Guard registration; keep flagged until active.  
- Wire `U-OLLAMA-PIPELINE-INJECTOR-WIRE` into injector hook, verify deep‑search routing.  
- Implement `U-WEEKLY-SYNTH-RESOLVER` import change for model selection.  
- Add GPT‑OSS paragraph to viz→wiki entries (T1).  
- Explore further Ollama use in tool calls, memory distillation, tribal knowledge injection.  
- Monitor WSL balloon after `wsl --shutdown`.  
- Gap B semantic search reranker (BM25+embedding).  
- U‑OLLAMA‑FORGE‑ASSIST scaffold forge for skill/script/hook combos.  
- Remaining backlog: U‑NAV‑ACCEL‑SPEC queued, other alpha/bravo lane items pending.
