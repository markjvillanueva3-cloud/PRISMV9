# business session 001bd6c3 (2026-06-09, 57.6MB, spine 516KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit ab2ccf42a4 / e6713584e2: P4 system‑viz roost live.  
- Commit c988a21ec4: Hermes config reset; linked to Claude via OAuth (56aa423427).  
- Commit 28b72e4dee: added `scripts/fill-galaxy-memory-sections.mjs`; updated 34 galaxy `MEMORY.md` to canonical 4/4.  
- Docker Desktop + docker‑compose.yml: Qdrant container `qdrant/qdrant:v1.17.0` healthy on :6333.  
- Hermes config migrated v28; provider strings set to `custom` (Ollama qwen2.5-coder:32b) and later gpt‑oss:20b.  
- Hermes backend started, gateway ready at :9120 after GUI launch resolved git‑pull abort.  
- U9 (`7771ca7f86`): VRAM co‑residency env & hard‑reason mutex.  
- U1 (`2340a2e699`): Offload router roster refreshed tier‑aware selection.  
- U2 (`a2756779c2`): Route hook default to resident gpt‑oss:20b; added `ollama-route-config.json`.  
- U3 (`a60d7ba0bf`): Take‑rate banner corrected (14/1570 = 0.9%).  
- U4 (`612418fde7`): LLM compressor for RTK residue safety denylist fixed.  
- U‑OAB‑120B (`3904b5e7b0`): Fleet‑wide default coding model set to gpt‑oss:120b via `PRISM_CODEGEN_MODEL`.  
- Go‑live merge commit onto `cad-fusion-live-ms0` (`5a91ba1862`).  
- RTK config.toml created, limits tightened and validated.  
- U5b–U10 scripts (ollama‑tool‑agent.mjs, ollama‑offload‑enforce.mjs, ollama‑codegen.mjs, ollama‑build‑loop.mjs, ollama‑capability‑probe.mjs, ollama‑coresidency.mjs) live‑validated.  
- Commit 822c48c55a: corrected VRAM math & routing.

**DECISIONS**  
- Use PRISM MCP `:3100` for all vault read paths; drop filesystem‑MCP.  
- Hermes primary model → Claude Opus 4.8 via OAuth, fallback to local Ollama qwen2.5-coder:32b (later gpt‑oss:20b).  
- Keep slot‑brief write/consume coordination via MCP; no new dispatcher.  
- Revert supervisor cwd patch; use `PRISM_ROOT`.  
- Adopt headless zulu execution (`hermes -z` PowerShell CLI); eliminate GUI dependency.  
- Set GPT‑OSS‑120B as fleet‑wide coding offload default (`PRISM_CODEGEN_MODEL`).  
- Auto‑route lever already on (`ollama-route-config.json mode:auto`).  
- Design task‑type → LLM mapping logic; tune keep‑alive, parallelism.  
- Merge only net‑new commits onto live branch; supersede U1/U2/U3.

**OPERATOR DIRECTIVES**  
- Verify Hermes boot & Claude link; check `/api/status` for updated provider.  
- Monitor MCP `:3100` health; ensure Hermes connects.  
- Update gpt‑oss token budget in Hermes config to avoid “no final response”.  
- Schedule headless zulu runs via PowerShell script.  
- Verify Qdrant online on :6333.  
- Confirm auxiliary models rerouted to local Ollama (avoid 400s).  
- Validate headless zulu integration with MCP in production schedule.  
- Verify all installed Ollama models loaded; tune GPU/CPU usage (`OLLAMA_KEEP_ALIVE`, `NUM_PARALLEL`, `MAX_LOADED_MODELS`).  
- Map each task type to specific LLM (e.g., code review → gpt‑oss:120b, summarization → qwen2.5-coder:32b).  
- Integrate routing layer into build pipeline; run Claude Opus QA after each step.

**FINDINGS/BUGS**  
- Stale `N:\` drive reference in self‑awareness search; daemon restarted with correct `H:` root.  
- Qdrant service initially down; now healthy on :6333.  
- Hermes OAuth returns HTTP 400 “extra usage”; 429 rate‑limit observed; no fallback triggered.  
- Supervisor cwd patch would regress repo‑root engines – reverted.  
- Hermes init failed due to unknown provider `openai`; fixed by valid IDs.  
- GUI launcher stuck on git‑pull abort (dirty `package-lock.json`); resolved.  
- Auxiliary models still routed to Claude → risk of 400s on image OCR.  
- Memory‑inflation bug: `node_*` files counted as real memories (1362→66 curated).  
- Test failure from injected mutation in `applyBlock`; fixed.  
- Corrupted `ai-training_synthesis.md` all‑NUL; flagged for owner India.  
- gpt‑oss reasoning channel issue resolved by clean‑content models; custom provider mis‑parsed.  
- qwen2.5-coder:32b ≈37 GB resident at 16K; cannot co‑reside with gpt‑oss:120b (~65 GB).  
- gpt‑oss:20b loops on broad searches, falls back to Claude; targeted lookups converge in ~5 iterations.  
- Tool‑calling only supported by gpt‑oss family; qwen2.5‑coder emits text, not structured calls.  
- Offloader “suggest‑only”; 5% offload metric stale history.  
- VRAM math miscalculated (under‑counted KV); corrected to CTX=16384, MAX_LOADED=3.  
- Duplicate router engine detected and removed.  
- Probe entry guard bug fixed; nomic tag mismatch resolved.  
- Stale‑reclaim test tautology fixed with fresh‑lock discriminator.

**DOMAIN SPECIFICS**  
- Hermes ↔ Obsidian vault via `knowledge/` read surface (`prism_memory`, `prism_session`).  
- Slot‑brief hooks: `slot_brief_write`, `slot-brief-inject`.  
- PRISM MCP dispatchers: `prism_context`, `prism_knowledge`, `prism_memory`.  
- Qdrant collections: `prism_engines`, `prism_formulas`, `prism_skills`; offline initially.  
- Dream‑cycle, octopus‑RAG, memory bridge, system‑viz integration.  
- Galaxy artifacts: `MEMORY.md`, `PATHS.md`, `TOOLBELT.md`, `CLAUDE.md`.  
- Zulu orchestrator (`zulu-orchestrator-sweep.mjs`) sends SendKeys; uses `chat-slots.mjs`.  
- Engines: Ollama local LLMs (gpt‑oss:120b, qwen2.5-coder:32b), tool‑calling loop, search offload, coding offload, Opus tail review gate, capability probe, coresidency mutex.  
- Dispatchers/bridges: `ask-ollama.mjs`, `ollama-prism-bridge.mjs`, `ollama-task-offloader`.  
- Metrics: VRAM per model, token cost (Read / isLargeRead), latency (~30 s tool‑call loop).  
- Paths: slot/bravo worktree `H:/prism-slot-bravo`; live branch `cad-fusion-live-ms0`; env var `PRISM_CODEGEN_MODEL`.

**TOOLS USED**  
- Docker Desktop + docker-compose.yml.  
- Hermes CLI (`hermes config migrate`, `hermes -z`).  
- Qdrant image `qdrant/qdrant:v1.17.0`.  
- Prism MCP server, Hermes agent, Obsidian local‑rest‑api plugin.  
- Node.js scripts: `fill-galaxy-memory-sections.mjs`, `galaxy-synthesis-refresh.mjs`, `obsidian-memory-sync.mjs`, `chat-slots.mjs`, `zulu-orchestrator-sweep.mjs`, `ollama-tool-agent.mjs`, `ollama-offload-enforce.mjs`, `ollama-codegen.mjs`, `ollama-build-loop.mjs`, `ollama-capability-probe.mjs`, `ollama-coresidency.mjs`.  
- Ultracode workflows (planning/brainstorm/verify).  
- Git (branching, commits, slot/bravo worktree).  
- Test harnesses: `node:test`, `vitest`; Node 22.  
- RTK binary (`rtk config`).  
- Kimi API via OpenRouter / Cloudflare Workers AI.

**OPEN THREADS**  
- Resolve Hermes 400 error: enable “extra usage” billing at claude.ai/settings/usage or switch provider – pending.  
- Confirm fallback path works under current rate‑limit conditions – pending.  
- Restart Hermes to load migrated config and new provider strings – pending.  
- Adjust Hermes token budget for gpt‑oss to ensure final response – pending.  
- Confirm auxiliary models rerouted to local Ollama to avoid 400s – pending.  
- Complete pulling of gpt‑oss:120b on Blackwell hardware – pending.  
- Validate headless zulu integration with MCP in production schedule – pending.  
- Design and implement task‑type → LLM mapping logic – pending.  
- Tune Ollama keep‑alive & parallelism for sustained GPU usage – pending.  
- Integrate routing layer into existing build workflow – pending.  
- Verify auto‑route lever remains functional on new hardware – pending.  
- Ollama‑server‑env restart (#1) to apply `RECOMMENDED_ENV` – pending.  
- Octopus 3‑voice integration (Claude + Ollama + Gemini; optional Kimi).  
- Model‑ref doc cleanup for stale deepseek comments.  
- Await synergy workflow results.
