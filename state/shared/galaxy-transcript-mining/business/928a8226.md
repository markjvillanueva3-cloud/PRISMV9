# business session 928a8226 (2026-06-10, 67.1MB, spine 670KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `scripts/fix-hookify-frontmatter.mjs` – bulk YAML front‑matter conversion & ASCII transliteration for 110 `.local.md`.  
- `H:/prism/.claude/helpers/ollama-resilient-pull.ps1` – em‑dash → hyphen patch, added `DEFAULT_NUM_PREDICT=1024`, empty‑response guard.  
- Updated `C:/Users/wompu/.claude/settings.json` (and H: mirror) – removed 6 dangling hook refs (`linear-roadmap-sync.mjs`, `stop-mcp-server-heal.mjs`, etc.).  
- `ask-ollama.mjs` – set `num_predict=1024`; batch workflow agents 3× to respect Anthropic limits.  
- `SYSTEM-SYNERGY-GAPMAP-2026-06-08.md` – node‑by‑node inventory of all 14 surfaces, ranked gaps/owners.  
- `cag-router.mjs` – fixed Qdrant label to real collections (`prism_engines`, `prism_skills`, `prism_formulas`).  
- `embed-wiki-into-tribal-index.mjs` – added empty‑text skip guard; updated tests.  
- Cap‑safe tribal index loader (`182788232a`) – incremental brace‑aware buffer read, 10/10 tests.  
- Clobber‑incident fix (`a3e6d3ca97`) – fail‑open → fail‑loud `readIndex`, shrink guard; restored baseline 4 k entries.  
- Docker‑service‑health‑check CLI (`fb314a6fd1`); stop hook (`d3ed737c23`).  
- Singleton‑service‑guard CLI (`35e01ca5f3`) – reap‑all duplicates, never kill serving PID; report‑only unknowns.  
- MCP recovery via `TRANSPORT=http node dist/index.js`; doc bug fix (`006c435ddb`).  
- Qdrant container started; collections intact.  
- PSN leg #5 restored with `NODE_OPTIONS=8192`.  
- Singleton guard added `--fix` & slash‑agnostic `cmdMatch` (`e2081e0780`, `ed6662f45e`).  
- `stop_on_hook_unregistration.mjs` – JSON‑stdout protocol, `buildVerdict`, `isMcpDaemonCmdline`.  
- MCP boot‑heap floor (`6ca2f6afd2`) – unified spawn paths to ≥4096 MB heap.  
- Fleet‑task‑health migration‑freeze marker (`4141daf9d8`); wiki lesson (`U-FTH-WIKI-LESSON`).  
- Cost‑bridge router consolidation (`U-QP-COST-BRIDGE-WIRING-TRUTH`).  
- Router‑aware quoting‑awareness generator (`U-QP-AWARENESS-GEN-ROUTER-AWARE`).  
- CostSavingsTrackerEngine test suite (13 vitest cases) (`U-QP-COST-SAVINGS-TEST`).  
- WEDMQuoteBridgeEngine exempt tag (`U-QP-WEDM-EXEMPT-TAG`).  
- Ollama dispatcher defaults updated to 1.5b/20b/32b tags (`U-OLLAMA-MODEL-FIX`).  
- Dispatcher scan for Blackwell tiers (`U-BW-DISPATCHER-SCAN`, `U-BW-DISPATCHER-SCAN-WIKI`).  
- PSN snapshot refresh (6‑day stale, 11 P0 zero‑ref pairs routed to Sierra) (`U-PSN-SNAPSHOT-REFRESH`).  
- DigitalTwinEstimator unit test (8/8 R9) (`U-QP-DIGITALTWIN-TEST`).  
- Margin‑floor guard added to QuoteEstimatorEngine (`U-QP-MARGIN-FLOOR-GATE`, `U-QP-MARGIN-FLOOR-SHOPCONFIG`); dispatcher round‑trip test (R15).  
- Frontend QuoteBuilderPage warning banner for `below_margin_floor`; type added, 2 R9 tests.  
- Margin‑floor dispatcher round‑trip (`ec597dbcb3`), frontend banner (`87d5c4bf9a`).  
- AI‑substrate routing matrix coverage (27/27) (`4b0980c56a`).  
- ResizeObserver polyfill + quote‑specialization test fix (`62b114cb7b`, `8258918f78`).  
- Closed‑loop provenance gate (`U-QP-CLOSED-LOOP-PROVENANCE-GATE` – 4c12a75a8d).  
- Outbound promotion gate (`U-QP-OUTBOUND-PROMOTE-GATE` – d294957c4d).  
- Calibration freshness preflight warning (>24 h stale) (`U-QP-CALIBRATION-FRESHNESS-PREFLIGHT` – bf10035ec0).

**DECISIONS**  
- Bulk transform hookify files; backup originals.  
- Delete dangling hook refs instead of disabling to avoid runtime errors.  
- Flag 90 unwired engines for owner review; do not auto‑wire all.  
- Resume stalled `gpt‑oss:120b` pull by fixing script parse error.  
- Adjust routing logic (`routeModelForTask`) or install‑gate to prioritize `gpt‑oss:120b`.  
- Adopt cap‑safe loader over string parse for tribal index; plan Qdrant migration.  
- Convert fail‑open bug to fail‑loud with shrink guard; prevent empty‑index clobber.  
- Consolidate cost‑bridge hooks into single router; tag exempt engines (`WIRE‑EXEMPT`).  
- Extend “no‑retired‑LLM‑refs” source‑lock to dispatcher tree.  
- Use migration‑freeze marker file for PRISM tasks instead of static list.  
- Adopt fail‑closed gating for provenance and outbound alignment.  
- Reject cost‑grain feeding into price slot; keep price‑grain loop.  
- Defer Qdrant tribal migration until corpus stabilizes.

**OPERATOR DIRECTIVES**  
- Fix all hook errors, wirings, gaps, and system backlogs.  
- Continue from unresolved issues (routing bug, fleet‑reaper monitoring).  
- `/loop [10m] /goal …` – focus on closed‑loop learning & production readiness.  
- Run peer slots for remaining galaxies (foxtrot, whiskey, mike, kilo, charlie, delta, hotel, oscar, juliett, quebec, sierra, papa).  
- Remove `state/shared/MIGRATION‑FREEZE‑ACTIVE.flag` once migration completes.  
- “Utilize Ollama for better task efficiency when viable.”  
- Final goal: fully closed‑loop quoting system ready for JM Die.

**FINDINGS/BUGS**  
- Hookify frontmatter missing YAML → parser warnings; one file had UTF‑8 emoji causing `charmap` decode error.  
- Six dangling hook refs caused “Cannot find module” errors.  
- 90 built engines unwired (53 core, 23 bridges, 14 ops); some superseded (`MillPrintToProgramEngine`).  
- Academy tribal routing falls back to `general`; requires owner‑specific build if desired.  
- `gpt‑oss:120b` pull stalled at 12 % due to em‑dash parse error.  
- Routing logic selects `gpt‑oss:20b` over installed `120b`.  
- V8 string length cap caused silent failure of tribal index >512 MiB; default Node heap OOM (~150 MB).  
- Fail‑open bug in `tribal-embed-index.mjs` clobbered empty index on parse error.  
- MCP auto‑reconnect used wrong command (`stdio`) → no :3100 service; fixed to `TRANSPORT=http`.  
- Docker‑service‑health‑check lacked start command; added.  
- Qdrant container left “Created” due to rename conflict; started correctly.  
- Singleton guard regex matched only backslashes → daemon invisible.  
- stop_on_hook_unregistration used exit‑code protocol but bundle expected JSON → always “NOT evaluated”.  
- System‑viz `find` OOM at ~390 MB; self‑bump avoided to prevent spawn storm.  
- Fleet‑task‑health watch warned on intentionally disabled HW‑migration tasks (cry‑wolf).  
- 11 P0 zero‑ref pairs in stale PSN snapshot; real under‑wiring.  
- OCR‑$1 spike: 65 % of line ext_prices ≈ $1.00 flagged `degenerate-reference`.  
- Stale calibration factor (>24 h) silently applied to live quotes – fixed by freshness preflight.

**DOMAIN SPECIFICS**  
- **Engines/Models**: Ollama (`gpt‑oss:120b`, `qwen2.5‑coder:1.5b`), Nomic embed text, Qdrant collections (`prism_engines`, `prism_skills`, `prism_formulas`).  
- **Dispatchers/Consumers**: `ask-ollama.mjs`, `resolveSynthesisModel`, `cag-router.mjs`, `embed-wiki-into-tribal-index.mjs`, `tribal-rerank.mjs`, `businessDispatcher` (`quote_estimate`), `runCycle`.  
- **Metrics**: GPU load times (39 s for 65 GB), token rates (~114 tok/s), margin_floor_pct, below_margin_floor, confidence_score, MAPE, medianRatio.  
- **Paths/Files**: `scripts/fix-hookify-frontmatter.mjs`, `scripts/remove-dangling-hook-refs.mjs`, `scripts/ollama-resilient-pull.ps1`, `scripts/ask-ollama.mjs`, `embed-wiki-into-tribal-index.mjs`, `tribal-rerank.mjs`, `system-viz-query.mjs`, `singleton-service-guard.mjs`, `mcp-server-supervisor.mjs`.  
- **Collections**: Qdrant (`prism_engines`, `prism_skills`, `prism_formulas`).  
- **Frontends**: QuoteBuilderPage warning banner, QuoteAnalyticsPage, JMDieFleetScanStatusPanel.

**TOOLS USED**  
- PRISM scripts/hooks: `fleet-reaper-sweep.mjs`, `fleet-task-health-watch.mjs`, `audit-unwired-engines.mjs`, `singleton-service-guard.mjs`, `mcp-server-daemon.mjs`.  
- Node.js, PowerShell, Python (`config_loader.py`), Git diff/grep.  
- Test frameworks: vitest (engines), node:test, R9/R15 unit tests.  
- PRISM workflow engine (`ultracode`), 3‑of‑3 scrutiny reviewers.  
- Docker CLI (`docker ps`, health checks).  
- TSC with 16 GB heap for full type checking.

**OPEN THREADS**  
- Owner‑specific triage of the 90 unwired engines (wire vs. archive).  
- Resolve routing bug: adjust `routeModelForTask` or install‑gate to prioritize `gpt‑oss:120b`.  
- Monitor scheduled‑task WARNs; confirm migration freeze resolved before re‑enabling golf‑write‑allowlist.  
- Verify fleet‑reaper continues to reap orphan processes and GPU resources after changes.  
- Qdrant tribal migration (deferred) – new collection or reuse once corpus stabilizes.  
- Full re‑embedding of lost ~29 K entries after write‑side sharding implementation.  
- Review remaining raw‑string readers for fail‑open patterns.  
- Evaluate persistent MCP supervisor if current watchdog proves insufficient.  
- Remaining 11 galaxies’ peer slots still need to run (synergize goal).  
- Data‑blocked ERP/QuickBooks integration for AccountingHardeningEngine (`U-QP-ACCOUNTING-WIRE`).  
- PSN cross‑substrate wiring remains in Sierra’s lane.  
- Blueprint→quote intake path: implement `U-QP-BLUEPRINT-OCR-BRIDGE-ADAPTER` and round‑trip tests.  
- Frontend failures in hotel domain (`QuoteAnalyticsPage`, `JMDieFleetScanStatusPanel`).  
- Fleet‑scope synergy goal still open; requires operator dispatch of remaining slots.
