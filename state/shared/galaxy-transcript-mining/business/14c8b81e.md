# business session 14c8b81e (2026-06-09, 5.4MB, spine 74KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit 316872f7 – `scripts/call-engine.mjs` + `.cmd/.sh` wrappers + 8‑case `call-engine.test.mjs`.  
- Commit 38af3407 – `27-wsl-memory-guard.mjs`, its test suite, and the PowerShell installer for a recurring watchdog.

**DECISIONS**  
- Map all 107 dispatchers to plain `src/engines/*.ts` singletons; MCP server is only needed for three narrow capabilities.  
- Build a server‑free engine invoker (`call-engine.mjs`) that replicates dispatcher behaviour via direct `tsx` imports.  
- Use a bootstrap‑slot‑enforce prefix when committing cross‑cutting tooling from the shared tree.  
- Kill zombie TS servers to reclaim ~3 GB of memory.  
- Unload idle Ollama models (~65 GB) to drop commit pressure below the Stop‑hook threshold.  
- Add a WSL memory guard (`27-wsl-memory-guard.mjs`) that reads `.wslconfig`, flags overruns, and advises `wsl --shutdown`.  
- Install a 15‑min scheduled task (via PowerShell) to run the guard in advise mode; optional `--enforce` flag for manual shutdown.

**OPERATOR DIRECTIVES** *(verbatim user requests)*  
- “since we do everything locally… need the MCP server?”  
- “do it” (after asking to map dispatchers).  
- “continue from where you left off.” (repeated many times).  
- “fix whatever is constantly causing the api limit error”.  
- “try again”.  
- “commit”.  

**FINDINGS/BUGS**  
- MCP server not required for code navigation or H: drive tooling; all dispatchers are thin routers.  
- Only ~5 capability clusters truly need the server (semantic memory, live machine connectivity, WebSocket push).  
- `call-engine.mjs` works without the server; test harness initially failed 3/8 cases due to Windows shell JSON mangling (`shell:true`). Fixed by invoking `node tsx/dist/cli.mjs` directly.  
- WSL2 was ignoring its `.wslconfig` memory cap (16 GB) and had ~96 GB committed, causing host‑level OOM that surfaced as “API limit error”.  
- Three zombie TS servers were leaking ~3 GB; killed them safely.  
- Three Ollama model servers (~65 GB resident) were idle; unloading them dropped commit from 98.4 % to 68.5 %.  

**DOMAIN SPECIFICS**  
- Engines: `src/engines/*.ts` singletons (e.g., `prismSelfAwarenessEngine`).  
- Dispatchers: thin Zod‑validated routers (`engine.method(params)`).  
- Call‑engine harness: uses `tsx` to import engines, supports `--list`, `--methods`, JSON params, fail‑loud exit codes.  
- WSL guard: parses `.wslconfig`, reads `vmmemWSL` via PowerShell, classifies overrun, outputs JSON/quiet modes.  
- System‑health series (`03-commit-pressure-check.mjs`) and scheduled‑task installer pattern used for the new watchdog.

**TOOLS USED**  
- Node.js (v22), tsx, node:test, npx, PowerShell scripts, `wsl`, `ollama` CLI, Git, rtk, PRISM harness tools (`Read`, `Write`, `Glob`, `Grep`, `Bash`, `LSP`, `Agent`, `Workflow`).  

**OPEN THREADS**  
- Run `wsl --shutdown` to reclaim the ~80 GB WSL overrun (requires user’s terminal).  
- Install the recurring watchdog (`install-wsl-memory-guard-task.ps1 -RunNow`) via elevated PowerShell.  
- Wire the guard into the fleet‑task‑health monitored‑tasks list (optional).  
- Execute `/compact` to finalize the session and load the handoff.
