# business session 0d351f43 (2026-06-09, 0.8MB, spine 15KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- None yet – the copy operation has not begun.

**DECISIONS**  
- Keep core PRISM repo (~1 TB) and `prism-backups` (42 GB).  
- Drop all git‑worktree archives, SteamLibrary, Tools runtime folder, and Claude profile copies.  
- Use rclone to stream directly to Box; robocopy via Box Drive mount is infeasible due to C: cache wall.  
- Persist exclude list at `H:\.tmp\rc-backup-excludes.json`.  

**OPERATOR DIRECTIVES**  
- Run `rclone authorize box` and complete browser approval.  
- Verify the Box account has ≥ 1 TB free space and supports large‑file uploads (≥ 150 GB).  
- Remove the 5.5 GB probe copy from `C:\Users\wompu\Box\Prism H Drive\blobs`.  
- Execute rclone backup with the exclude file, monitoring progress; resume if interrupted.

**FINDINGS/BUGS**  
- Box Drive pins all written files on C: and never evicts them (`attrib +U` ineffective).  
- Robocopy fails to enumerate destination due to `ERROR 31`; cannot use for additive copy.  
- File count after excluding worktrees drops from 4.49 M to ~1.89 M, but byte size only shrinks from 1.70 TB to 1.54 TB.  
- SteamLibrary (141 GB) and Tools runtime (175 GB) are not PRISM content; should be excluded.

**DOMAIN SPECIFICS**  
- PRISM H‑drive contains a main repo with `.git` history, nested worktrees (`.claude\worktrees`), and large data corpora.  
- Exclusion logic relies on detecting `.git` directories to identify reconstructable worktrees.  
- Target Box folder: `C:\Users\wompu\Box\Prism H Drive`.  

**TOOLS USED**  
- PowerShell, Bash (for drive enumeration).  
- robocopy (dry‑run only; not used for actual copy).  
- rclone (direct API streaming to Box).  
- Box Drive virtual filesystem (mounted at `C:\Users\wompu\Box`).  

**OPEN THREADS**  
- Confirm Box plan capacity and per‑file limits.  
- Determine if any remaining large files exceed Box’s per‑file cap; may need chunking or splitting.  
- Monitor rclone progress and handle resumptions.  
- Validate that excluded directories (SteamLibrary, Tools, Claude profiles) are indeed non‑essential before final backup.
