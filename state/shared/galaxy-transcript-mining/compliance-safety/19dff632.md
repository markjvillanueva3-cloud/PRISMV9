# compliance-safety session 19dff632 (2026-06-10, 19.2MB, spine 139KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e44a3a1592`: wired PayrollLiabilityFilingEngine (`compute940`, `generateW2`, `reconcileW2sTo941`, `contractor1099Totals`) into businessDispatcher.  
- `e649790e76`: added `remitLiability` wiring; 14 round‑trip dispatcher tests pass.  
- `9dfd621910`: committed `scripts/mine-hotel-transcripts.mjs`.  
- `61518eb988`: fixed Ollama noise filtering, empty‑response bug, combined‑glob issue.  
- `099e6b92bd`: added `state/shared/specs/HOTEL-FORGE-ROADMAP-2026-06-09.md`.  
- `f069052772`: verified P0‑VERIFY‑SHIPPED unit – all six “false gaps” live.  
- `U-HOTEL-PORTAL-PERSISTENCE`: migrated 4 durable Maps to SQLite WAL; 92/92 tests green, 3‑of‑3 scrutiny PASS.  
- `U-HOTEL-ALLOWLIST-WRITE-REVIEW + REALTIME-VERIFY`: realtime end‑to‑end verified; write actions remain 403 by default.  
- `U-HOTEL-ALLOWLIST-WRITE-ENABLE`: opened manager/HR workflow writes (`handoff_*`); 23/23 route tests, 3‑of‑3 scrutiny PASS.  
- `FALSE-WIRE-REGRESSION-GUARD`: fixed regex placeholder bug; added audit‑trail note; 20/20 passing.

**DECISIONS**  
- Use Ollama (`qwen2.5-coder:32b`) for grunt summarization / bulk transcript mining; ultracode workflow for high‑end synthesis.  
- Default `gpt‑oss:20b` for summaries, reserve `gpt‑oss:120b` for heavy synthesis.  
- Skip non‑hotel pending tuples; focus on roadmap from U‑HOTEL‑PORTAL‑PERSISTENCE onward.  
- Implement per‑action manager gate (role model) instead of new RBAC; keep financial/PII writes permanently 403.  
- Persist portal state via SQLite WAL (`state/shared/customer-portal.db`) and fold into existing engine.

**OPERATOR DIRECTIVES**  
- “continue” hotel ERP work; “start” roadmap from U‑HOTEL‑PORTAL‑PERSISTENCE.  
- If Ollama can generate a brainstormed road map, use it; otherwise use ultracode.  
- Read previous hotel sessions for full context (already done).  
- `/startup-hotel … /goal …` executed automatically; slot claimed and startup pipeline ran.  
- “push through” – trigger persistence build immediately.  
- Approve role model – operator sign‑off for write‑enable unit.

**FINDINGS/BUGS**  
- 6 pending JM‑DOC tuples cross‑domain; hotel portion complete at 67%.  
- PayrollLiabilityFilingEngine had 5 orphan methods wired.  
- Mobile engines: only `MobileVoice` unwired; others satisfied P4 unit.  
- Q2S DFM bug: non‑iterable `features`; filed to charlie.  
- False‑wire regression guard: `marketplace_lead_get` null handling and regex placeholder bug (fixed).  
- Off‑domain drift: ~11 of 19 hotel sessions performed non‑ERP work due to unfiltered RGS queue.  
- Shared‑tree commit contention (`index.lock`) resolved by cleanup; rate limiting on ultracode workflow resumed.  
- P0 placeholder regex missed “Unknown business action: X”; broadened match, added audit note.  
- P1 new `.db`, `-wal`, `-shm` files not gitignored – added ignore block for `customer‑portal.db`.  
- P2 HTTP gate authorizes by role only; engine methods still rely on caller‑asserted IDs (flag for auth‑user→employee mapping).  
- P3 header doc drift and missing identity note in tests fixed.  
- Pre‑existing bugs: PTO double‑apply, empty `approved_by`, divergent PTO approval paths.

**DOMAIN SPECIFICS**  
- Hotel slot: ERP/HR/business – payroll filing, portal persistence, dispatcher wiring, audit of constants, mobile engine integration.  
- Key engines: `PayrollLiabilityFilingEngine` (`compute941`, `compute940`, `generateW2`, `reconcileW2sTo941`, `contractor1099Totals`, `remitLiability`), `CustomerPortalEngine`, `MobileVoice`, `Q2S DFM engine`.  
- Dispatch allowlist: `src/data/business-dispatch-allowlist.ts`; lazy `getEngine` in `businessDispatcher.ts`.  
- MarketplaceSeedingEngine seeds leads keyed by `supplierId`.  
- CustomerPortalEngine uses SQLite WAL (`state/shared/customer-portal.db`) for tokens, messages, qualityDocs, serviceCases; `rateBuckets` transient.  
- Realtime SSE/WS bridge (`RealtimeEventBridge.ts`) emits `quote:update`, `job:progress`; consumer in `web/src/hooks/useSSE`.  
- Write actions: `handoff_counterparty_respond`, `handoff_manager_approve`, `handoff_mark_executed`, `handoff_cancel` (workflow‑only); financial/PII writes stay 403.

**TOOLS USED**  
- Ollama (`qwen2.5-coder:32b`) for grunt work; `gpt‑oss:20b` summarization, `gpt‑oss:120b` heavy synthesis.  
- ultracode workflow (8 agents, resumable via `resumeFromRunId`).  
- `scripts/mine-hotel-transcripts.mjs`, `ask-ollama.mjs`.  
- PRISM guard (`ascii-guard`), 3‑of‑3 scrutiny gate, audit trail middleware.  
- Vitest + Jest; SQLite via `better-sqlite3` with WAL.  
- Git hooks for slot binding and commit enforcement (`[MAIN] [BOOTSTRAP-SLOT-ENFORCE]`).  
- Node/npm/tsc build pipeline.

**OPEN THREADS**  
1. Build U‑HOTEL‑PORTAL‑PERSISTENCE (pending).  
2. Resolve false‑wire regression guard issues (`marketplace_lead_get` null handling, regex bug).  
3. Finalize ALLOWLIST‑WRITE-REVIEW and REALTIME‑VERIFY units.  
4. Filter RGS queue for hotel slot to eliminate off‑domain drift.  
5. Ensure shared‑tree commit contention fully mitigated (worktree migration if needed).  
6. Implement auth‑user → employee‑id mapping or confirm role gate sufficiency.  
7. Fix PTO double‑apply, empty `approved_by`, divergent PTO approval paths.  
8. Monitor stale Blueprint OCR Batch task (outside hotel scope).
