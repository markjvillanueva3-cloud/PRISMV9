# compliance-safety session 8b4b9149 (2026-06-10, 47.1MB, spine 511KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5ae481f748`: EPERM leak fix in `OutcomeCaptureBusEngine` (atomic‑append rewrite).  
- `86f0e3fe0c`: Wiring of `TriComparator`, `ExhaustiveCombination`, `DownstreamSubscriber` into `calcDispatcher`; 16 round‑trip dispatcher tests.  
- `890b2c39e7`: VC assessment spec addendum + closed‑loop training results.  
- `16d6eecef4`: `SpeedFeedCalibrationPersistEngine` + tests, dispatcher wiring.  
- `3438987f0e`: Calibration engine hardening (clamped flag, ledger_rows_skipped, persist tmp‑cleanup).  
- `f31398a1a5`: `SpeedFeedGpuJudgeEngine` + dispatcher case.  
- `f5d14ddb29`: GPU judge hardening (probeGpuResidency fix, silent‑exclusion note, fetch timeout).  
- `951f5ac335`: GPU judge polish (isProbe constant, suppression test).  
- `3a1c20fca2`: Full 69 120‑cell sweep engine (`full` mode + runStreaming) and driver.  
- `af0ac16c5a`: Scrutiny fixes: ReferenceError in driver, ISO‑group material collapse lock.  
- `b847c0f1ff`: Scoping spec for axis‑awareness enhancement.  
- `28eca0ed84`: Doc reflection of GPU‑judge findings.  
- `54c0ae06c8`: VC assessment spec addendum (GPU‑judge evidence).  
- `e9b68da865`, `658c8280fe`: Tool‑material axis canonical table + engine wiring.  
- `585584e3ae`: Coolant axis wired via existing `CoolantVcModifier`.  
- `7d0affcae6`: Rigidity factor de‑inline to constants.  
- `f998f8af71`: Alts‑factor linchpin: apply tool/coolant/rigidity factors to alternatives.  
- `da41a58fd1`: Full 102 060‑combination sweep (calc half).  
- `c48829e331`: Axis‑liveness probe (measured all axes, identified inert ones).  
- `d03458fff1`: Extended probe to include `tool_life`; confirmed runout inert.  
- `a257f872bb`: Consolidated liveness results; numeric finish‑Ra target added.  
- `2070c472a4`: Workholding clamp‑cap (force‑based derate).  
- `7dc7798033`: Spindle‑hp power clamp.  
- `8b191d5427`: Runout→tool_life link.  
- `81e37ece16`: Numeric target‑RA finish axis (physics‑reviewed, 12 tests).  
- `0a146e22c6`: Hygiene fix for controller look‑ahead constant.  
- `6a3ad56545`: Tool‑material variability sweep (576 tool‑material combos).  
- `c78faa5a73`: Compare HSS baseline (web‑verified, 54 pts, 6 tests).  
- `835df42c74`: Compare ceramic/CBN baseline (12 tests).  
- `4c544db4ae`: Per‑vendor baseline detail exposed; explicit G‑Wizard/CNCCookbook and HSMAdvisor deltas.  
- `08d7fc6d37`: All‑axis sweep (OAT × 2 regimes, factorial 25 axes; 16 live, 9 inert).  
- `907e74acab`: Material‑specific tool‑material speed factor override (fixed over‑speed bug for HSS cast iron & CBN).

**DECISIONS**  
- Adopt slot‑binding wrapper for `/checkin-oscar` to guarantee handoff before canonical `/checkin`.  
- Use ultracode multi‑agent orchestration for back‑end repair, engine wiring, closed‑loop testing.  
- Replace atomic append with `fs.appendFileSync` + bounded retry; remove orphan temp files.  
- Expose dispatcher actions: `speed_feed_tri_compare`, `speed_feed_exhaustive_combination`, `speed_feed_register_all_subscribers`.  
- Add tool‑material speed factor multiplier to physics engine (root cause at line 2080).  
- Build tool‑material axis via canonical constants; keep vendor catalog extraction separate.  
- De‑inline rigidity factor to constants for safety compliance.  
- Apply factors only when explicitly specified; preserve default behavior.  
- Surface per‑vendor deltas by adding `baseline_detail` to TriCompareResult; retain existing median logic.  
- Build exhaustive all‑axis sweep (OAT × 2 regimes) to satisfy Stop hook clause 1.  
- Fix over‑speed bug via material‑specific speed factor override (no core physics change).  

**OPERATOR DIRECTIVES**  
- Run calculations for every possible combination of machines, spindles, controller, materials, workholding/fixture, tool holder connection type, rigidity, dampening, accuracy, tooling+insert, coolant, tool path, cutting parameters, finish quality; compare ALL with G‑Wizard and HSMAdvisor.  
- Populate computed S/F per tool in G‑Wizard UI (closed Adobe‑AIR app).  
- Export live G‑Wizard/HSMAdvisor data via operator‑initiated closed‑app export.  
- Run Blackwell `qwen2.5-vl` OCR batch over vendor catalog PDFs (multi‑hour GPU job).  
- Do not pause; treat the condition itself as directive; block stopping until met.

**FINDINGS/BUGS**  
- `OutcomeCaptureBusEngine` dropped ledger writes due to EPERM rename failure – fixed.  
- Three engines (`TriComparator`, `ExhaustiveCombination`, `DownstreamSubscriber`) were unreachable from dispatcher – wired now.  
- G‑Wizard crib defaults to first tool; crib has 0 proven sfm → cannot contribute.  
- HSMAdvisor returns constant 634.6 m/min for all materials; engine flags unaligned but consensus median incorrectly includes it.  
- Consensus computation bug: not‑aligned systems still averaged into external consensus – pending fix.  
- Tool material inert (no multiplier in Vc formula); coolant, holder, machine/spindle/controller/workholding/insert axes produce identical outputs – root cause: `toolMat` read but never applied.  
- Over‑speed bug: PRISM over‑speeds HSS cast iron by +108 % and CBN hardened by +49 %; fixed via material‑specific override.  
- Fz uniformly +67–91 % higher than published sources – coherent high‑feed strategy, not a defect.  
- Several axes dead at baseline (controller_brand, machine_accuracy) – verified as honest inert or conditional caps.  
- Vendor catalog PDFs require vision‑OCR; text extraction fails due to font encoding.

**DOMAIN SPECIFICS**  
- **Engines**: `OutcomeCaptureBusEngine`, `SpeedFeedCalibrationPersistEngine`, `SpeedFeedGpuJudgeEngine`, `UltimateSpeedFeedEngine`, `SpeedFeedNineAxisOrchestratorEngine`, `SpeedFeedTriComparatorEngine`, `SpeedFeedBaselineComparatorEngine`.  
- **Dispatcher actions**: `speed_feed_tri_compare`, `speed_feed_exhaustive_combination`, `speed_feed_register_all_subscribers`, `speed_feed_calibration_persist`, `speed_feed_gpu_judge`.  
- **Metrics/fields**: Vc, RPM, feed_rate_mmmin, fz, mrr_cm3min, tool_life_min, Ra, ledger_rows_skipped, gpu_resident, matched_model, vram_used_mib.  
- **Paths**: `src/engines/speed-feed/*`, `src/data/<vendor>-speed-feed-data.ts`, `catalog-sfc-extraction-manifest.json`, `state/shared/specs/SFC-VC-ASSESSMENT-2026-06-08.md`.  

**TOOLS USED**  
- `/hermes-workflow` planner, `/forge-audit-v2` Boris‑discipline audit.  
- Ultracode multi‑agent orchestration (parallel agents, adversarial verification).  
- `chat-slots.mjs` helpers (`claim`, `reclaim`).  
- Node scripts: `update-prism-inventory.mjs`, `build-state-snapshot.mjs`, `system-synergy-map.mjs`, `system-viz-query.mjs`.  
- Esbuild for dispatcher compilation; Vitest for round‑trip tests.  
- PRISM scrutiny gate script (`node -e`), CronCreate, `/loop`, `/goal`.  
- Ollama inference (GPU) – qwen2.5‑coder:32b, qwen2.5‑VL.  
- nvidia‑smi, tsc, vitest.  
- Git hooks for slot binding, commit tagging.  

**OPEN THREADS**  
1. Resolve consensus bug: exclude unaligned external systems from median calculation.  
2. Live G‑Wizard/HSMAdvisor numbers – operator must run closed apps and export data.  
3. Richer carbide baseline / OCR catalog pass pending (multi‑hour GPU task).  
4. Fz‑force validation unit (`U‑OSC‑FZ‑FORCE‑VALIDATE`) queued.  
5. Dead‑axis triage unit (`U‑OSC‑DEAD‑AXIS‑TRIAGE`) to fix controller_features wiring gap and add tool_life tracking.  
6. Controller default feed decision – product‑philosophy choice (leave inert or add MRR cap).  
7. Comparison sweep tool_material variability – implement `U-OSC-COMPARE-TOOLMATERIAL-VARIABILITY`.
