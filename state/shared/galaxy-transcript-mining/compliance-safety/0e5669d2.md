# compliance-safety session 0e5669d2 (2026-06-10, 44.9MB, spine 372KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `168c202646`: B1 sync‑resilient – added `writeWithRetry`, guarded writes, test suite.  
- `ea21008f25`: B2 index‑meta – in‑place metadata stamp on `index.md` (last_verified & Total entries).  
- `8c4dff660a`: B3 maint‑cron installers – PowerShell scripts for promote‑memory‑to‑wiki and vault‑rot‑sentinel, all installed with `-Disabled`.  
- `984313825e`: C2 link‑heal scorer – recalibrated scoring (prefix/substring demoted, reason‑based autoApplyEligible, decay‑leak fixed).  
- `838123429b`: C3 tribal coverage refresh – re‑ran audit, cleared stale 31.5 % banner, updated JSON.  
- `da013c4406`: Audit map persisted to galaxy MEMORY.md and wiki entry.  
- `9e4376b3b2`: U‑VAULT‑RAG‑WIRE wired & verified (keyword recall restored).  
- `45225e0cc3`: fix‑memory persisted (C: & H: mirrors).  
- `d948b85a74`: Hook‑heap hardening – portable‑node wrappers default to `--max-old-space-size=384`, updated tests, scorer regeneration.  
- `43e1b8e449`: Peer commit containing above changes in HEAD after shared‑index race.  
- `3d796dcf5c`: fleet‑task‑health‑watch hook (EXPECTED_UNREGISTERED_TASKS, aggregateHealth partitioning, WSL guard added).  
- `96ed5222e2`: vault‑backlink schema & builder (`normalizeVaultKey`, `sortNodeIds`, `makeBacklinkRecord`, `assertBacklink`; `build-vault-backlink-index.js`).  
- `d856173b86`: staleness flag in reader (backlinksFor returns stale boolean).  
- `0e2724871a`: regen‑wire stage added to `regen‑viz.mjs` after `build‑graph‑index`.  
- `14aba14e3a`: `prism_session:doc_nodes` dispatcher wrapper + tests (`runDocNodesAction.ts`, vitest suite).  
- `4a44b5393c`: schema entry for `doc_nodes` in ACTION_SESSION_SCHEMAS.  
- `f23c58b42d`: vault‑access map update (reverse edge closed, stale memory‑rag‑inject claim fixed).  
- `2d49bf0d33`: canvas‑read lib & CLI subcommands (`canvas`, `canvas-doc`) with staleness flag and tests.  
- `47fe91554f`: access‑map doc updated (.canvas row closed, memory‑rag‑inject claim corrected).  
- `7ec4a5ea02`: T2 – Ollama pipeline injector wired to `settings.json`.  
- `cd54edb940`: T1 – U‑VIZ‑WIKI‑NARRATIVE flag‑gated post‑pass generator (`generateBlurb`, qwen2.5‑coder:32b).  
- `65a29220e5`: #8 – U‑OLLAMA‑PREWARM‑WIRE wired after injector.  
- `6fb278a2ee`: #2 – U‑TRIBAL‑QA‑LLM‑UNBLOCK shipped with gpt‑oss:120b support.  
- `f1b69db664` / `1ecf50b6be`: memo‑extractor hardening (per‑session throttle, correct transcript parsing, user/human turn capture, command‑injection fix).  
- `2c766b9308`: U‑NAV‑ACCEL‑GAP‑A‑FALSIFIED – avoided duplicate 3,300‑node generator; spec corrected.  
- `9ce1ea6633`: marked #5 SHIPPED in audit backlog.  
- `b5d249f4f5`: U‑WEEKLY‑SYNTH‑RESOLVER – host‑aware model selection, env‑pin override, timeout 180 s.

**DECISIONS**  
- Use `/checkin-sierra` wrapper to deterministically bind the sierra slot before canonical check‑in.  
- Adopt ultracode for comprehensive vault audit and context recovery; persist recovered context on wiki entry, galaxy MEMORY.md, C: & H: mirrors.  
- Wire `memory-rag-inject` after disabling always‑on injector; close P0 RAG‑wire to restore keyword recall.  
- Implement sync‑resilient with retry helper (`writeWithRetry`) and guard against unprotected writes.  
- Add public `refresh()` for index‑meta that stamps only metadata, not entry bodies.  
- Deploy maint‑cron installers with `-Disabled` flag for migration safety.  
- Recalibrate link‑heal scorer: demote prefix/substring auto‑apply, enforce reason‑based `autoApplyEligible`, add min‑length floor on Levenshtein.  
- Refresh tribal coverage audit to correct stale banner and expose disabled cron.  
- Cap hook heap at 384 MB (`PRISM_HOOK_HEAP_MB`) to reduce commit reservation from ~4 GB → 0.38 GB per process (~10×).  
- Raise MCP server `maxConnections` to 512 for full 26‑slot fleet + spawned agents (next daemon restart).  
- Add `TRANSIENT_PRESSURE_CODES` in `fleet-task-health-watch.mjs` (e.g., HRESULT 0x800710E0) as transient system‑resource pressure.  
- Widen GPU offload pattern list (`OFFLOADABLE_PATTERNS`) from ~8 % to ~30 %.  
- Introduce `EXPECTED_UNREGISTERED_TASKS` for vault crons intentionally unregistered during migration freeze; surface as informational.  
- Use atomic stage+commit with `CLAUDE_SESSION_ID` to avoid shared‑index race.  
- Keep hook heap cap at 384 MB (breaks fleet scaling if raised).  
- Reclaim WSL balloon via `wsl --shutdown` after operator approval; migration freeze remains ON, no deferred items armed.  
- Build reverse edge (vault doc → graph node) by inverting `node‑cards.jsonl`; add staleness flag to reader.  
- Add regen‑wire stage to auto‑refresh `vault-backlinks.json` after graph regen.  
- Implement dispatcher action `prism_session:doc_nodes` for cheap reverse lookup; wire into `system-viz-query.mjs` before `loadGraph()`.  
- Add canvas reader (`canvas`, `canvas-doc`) as third cheap join; fix Lgit regex bug and staleness flag.  
- Use Ollama offload only where evidence shows benefit; reject widening router to 30 % due to falsified premise.  
- Use `generateBlurb` for narrative generation (fail‑soft, cacheable, idempotent).  
- Default LLM calls to qwen2.5‑coder:32b; use gpt‑oss:120b only when `num_predict≥1024` and timeout ≥ 180 s.  
- Wire `ollama-prewarm-on-pipeline.mjs` immediately after injector to hide cold‑load latency.  
- Replace execSync curl injection in stop hooks with `callOllama`.  
- Enforce `/dedup` before forging assets; adopt kill‑switches and keyword gating.  
- Switch memo extraction throttle from fleet‑global to per‑session rate files (`sessionRateFile(session_id)`, 24 h prune).  
- Shift memo‑extractor throttle to per‑session; Gap A false premise avoided.  
- Weekly Synth Resolver default model host‑best (gpt‑oss:120b on Blackwell), env‑pin wins, fail‑soft fallback to 32b, timeout 180 s.  
- Suppress TS7016 errors with `@ts-expect-error` where pattern already present in production imports.

**OPERATOR DIRECTIVES**  
- Validate hook‑heap cap (confirm commit charge dropped from ~92 % → ~70 %).  
- Tidy banner noise – add vault cron names to `EXPECTED_UNREGISTERED_TASKS`.  
- Arm deferred items: after migration freeze lifts, restart MCP daemon (512 maxConnections) and register vault crons (`PRISM Vault Memory Promotion Cron`, `PRISM Vault Rot Sentinel Cron`).  
- Consider moving pagefile to H: for faster I/O; optional.  
- “Make sure Obsidian vault is built and operational like all the X articles fed.”  
- “If you lost context, use ultracode to find all Obsidian‑related articles to regain context.”  
- “Map paths to each Obsidian vault node for cheaper token usage / free token usage.”  
- “Continue with system viz and obsidian app synergizing… keep mapping nodes for quick searches/tool usage savings.”  
- “Can we utilize ollama to code direct paths to every file/node for faster, more efficient searches/reads of the codebase?”  
- “Generate skill‑script hook combos via Ollama (U‑OLLAMA‑FORGE‑ASSIST) pending.”  
- `/loop [10m] /goal …`: finish all ollama synergy tasks; use strongest viable models for grunt work, wired/validated across galaxies.  

**FINDINGS/BUGS**  
- Unprotected `writeFileSync` in sync‑resilient caused data loss.  
- `index.md` frontmatter stale (`last_verified 2026‑05‑08`, `Total entries 770`).  
- Maint‑cron installers missing `-Disabled`; risk of running during migration.  
- Link‑heal scorer overconfidence: prefix/substring auto‑apply, decay leak for slugs > 60 chars.  
- Stale candidates file with `autoApplyCount 14100`; full‑corpus regen OOM’d.  
- Short‑token Levenshtein (≤2) false positives flagged by reviewers.  
- Over‑commit of 4 GB hook heap reservation triggered false “MCP failing” alerts (HRESULT 0x800710E0).  
- Offload coverage too low due to narrow regex list; target ~30 %.  
- Shared‑index race caused commit to be merged into peer’s commit; attribution lost.  
- WSL balloon not applied (95 GB commit) until `wsl --shutdown`.  
- Hook heap cap 384 MB correct lever; raising would cause spawn‑refusal.  
- Staleness flag needed for vault‑backlink reader and canvas reader; added.  
- Lgit regex bug miscounted layer; fixed.  
- stale memory‑rag‑inject claim in access map; corrected.  
- Memo‑extractor defects: fleet‑global throttle, wrong transcript selection, dropped user turns.  
- Command‑injection vulnerability (`execSync(curl …)`) → replaced by `callOllama`.  
- Gap A false premise avoided duplicate 3,300‑node generator.  
- gpt‑oss empty‑response due to `num_predict` starvation; fixed by raising to 1024.  

**DOMAIN SPECIFICS**  
- `WikiIndexMaintainerEngine.formatIndex` handles metadata stamping.  
- `obsidian-memory-sync.mjs` orchestrates memory sync, uses `ensureDir`, `writeFileSync`.  
- `memory-rag-inject` hook provides keyword recall fallback.  
- `node-card-prefetch-inject` & `search-thoroughness-inject` give cheap‑token access paths.  
- `build-wiki-leaf-index.mjs` builds leaf catalog for Obsidian vault.  
- Link‑heal scorer logic (`scoreCandidate`, `rankCandidates`) in `scripts/link-heal-scoring.js`.  
- PowerShell cron installers: `promote-memory-to-wiki.ps1`, `vault-rot-sentinel.ps1`.  
- `fleet-task-health-watch.mjs` – new `TRANSIENT_PRESSURE_CODES`, `EXPECTED_UNREGISTERED_TASKS`.  
- `mcp-server/src/index.ts` – `httpServer.maxConnections = 512`.  
- `OllamaTaskOffloaderEngine.ts` – widened `OFFLOADABLE_PATTERNS`; GPU config (Blackwell tier, KEEP_ALIVE=-1).  
- `build-vault-backlink-index.js`, `canvas-read-lib.mjs`, `host-aware-synthesis-model.mjs`.  
- Paths: `scripts/lib/vault-backlink-schema.mjs`, `system-viz-query.mjs`, `regen-viz.mjs`, `canvas-read-lib.mjs`.  

**TOOLS USED**  
- `/checkin-sierra` wrapper, `chat-slots` helper, `slot-bind-enforce.mjs`.  
- Ultracode workflow orchestrator.  
- Hooks: `memory-rag-inject`, `node-card-prefetch-inject`, `search-thoroughness-inject`.  
- Modules: `WikiIndexMaintainerEngine`, `obsidian-memory-sync.mjs`.  
- PowerShell cron installer scripts (`promote-memory-to-wiki.ps1`, `vault-rot-sentinel.ps1`).  
- Node test suite (`node:test`) for scorer and cron tests.  
- Git commit workflow with lane‑guard bypass, shared‑index guard.  
- Jest/Node test harnesses (scorer, hook, offloader).  
- Supervisor/launcher for MCP daemon restarts.  
- `ask‑ollama.mjs` (`callOllama`), `resolveSynthesisModel`.  
- `scrutiny-3way.mjs`, `scrutinize-before-stop.mjs`.  
- Build tools: esbuild, vitest, tsc (strict), npm run build.  

**OPEN THREADS**  
- Full‑corpus regeneration of wiki‑link‑fix‑suggester candidates file (OOM issue; need streaming or chunked approach).  
- Add `coverageComplete` flag to candidates output for clarity.  
- Implement semantic reranker after BM25 using node embeddings (Gap B).  
- Build U‑VIZ‑WIKI‑NARRATIVE (`regen-wiki-from-viz.mjs`) – add narrative paragraphs via Ollama.  
- Build U‑OLLAMA‑PIPELINE‑INJECTOR‑WIRE – wire injector hook for deep‑search/RGS/pdf‑learn routes.  
- Implement weekly synth resolver (U‑WEEKLY‑SYNTH‑RESOLVER) – host‑aware model selection, timeout 180 s.  
- Monitor offload coverage in production; target ~30 %.  
- Restart MCP daemon to apply `maxConnections=512`.  
- Register vault crons after migration freeze via `Enable-ScheduledTask` or `install‑*.ps1 –RunNow`.  
- Resolve any remaining shared‑tree amend conflicts if new commits arise.  
- Monitor WSL balloon and Docker state; ensure `wsl --shutdown` applied when needed.  
- Address P2 `num_predict` unset issue (empty `.response`).  
- Consolidate memo cache, finalize #6 wiki‑NLI‑lint implementation.
