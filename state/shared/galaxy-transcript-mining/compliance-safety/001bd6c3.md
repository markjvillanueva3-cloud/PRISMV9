# compliance-safety session 001bd6c3 (2026-06-10, 58.3MB, spine 523KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `ab2ccf42a4`/`e6713584e2`: Hermes‑app integration with PRISM MCP & `/system‑viz`.  
- Commit `c988a21ec4`: Hermes crash fix on config edit.  
- Commit `56aa423427`: Hermes configured to run Claude Opus 4.8 via OAuth, fallback Ollama `qwen2.5-coder:32b`.  
- Commit `28b72e4dee`: added `scripts/fill-galaxy-memory-sections.mjs` + 13‑test suite; updated all 34 galaxy `MEMORY.md`.  
- Commit `822c48c55`: VRAM‑routing plan spec (`OLLAMA-AUTORUN-BUILDLOOP-PLAN-2026-06-09.md`).  
- Commit `3499f5f20f`: `ollama-tool-agent.mjs`.  
- Commit `d807d46f44`: `ollama-offload-enforce.mjs`.  
- Commit `a59f35221e`: `ollama-codegen.mjs`.  
- Commit `cfda0e391a`: `ollama-build-loop.mjs` (Opus tail‑review gate).  
- Commit `e17be56e82`: `ollama-capability-probe.mjs`.  
- Commit `7771ca7f86`: `ollama-coresidency.mjs` (mutex & keep‑alive for co‑resident models).  
- Commit `2340a2e699`: Ollama router roster refresh, tier‑aware selection.  
- Commit `a2756779c2`: route‑hook default to resident model + shipped config.  
- Commit `a60d7ba0bf`: take‑rate banner honesty fix.  
- Commit `612418fde7`: LLM compressor for RTK residue, safety‑denylist fixed.  
- Commit `3904b5e7b0`: fleet‑wide default coding model set to gpt‑oss:120b (`PRISM_CODEGEN_MODEL`).  
- Commit `5a91ba1862`: Go‑live merge of net‑new subset into `cad‑fusion‑live‑ms0`.  
- Docker Desktop launched; Qdrant container healthy on `:6333`.  
- Hermes config migrated v0→v28; provider strings corrected (`anthropic` → `custom`).  
- Headless zulu test via `hermes -z "<prompt>"` succeeded.  
- Alpha pull of `gpt‑oss:120b` confirmed; fleet routing now uses `gpt‑oss:20b`.

**DECISIONS**  
- Primary LLM = Claude Opus 4.8 (Anthropic OAuth); fallback local `qwen2.5-coder:32b`.  
- Hermes dispatches via PRISM MCP at `:3100`; no separate filesystem server.  
- Vault read path uses existing MCP dispatchers (`prism_memory`, `prism_session`, `prism_knowledge`).  
- Qdrant optional but should run for full feature set; semantic search via `master_index_query`.  
- Model strategy: fleet standard gpt‑oss:20b primary with Claude fallback; upgrade to gpt‑oss:120b once pulled.  
- Run zulu headless (`hermes -z`) instead of Electron GUI.  
- Use local Ollama stack for synthesis/fill work due to Claude API rate limits.  
- Enable `OLLAMA_KEEP_ALIVE=1`; set `NUM_PARALLEL=4`, `MAX_LOADED_MODELS=6`.  
- Routing matrix: code synthesis → gpt‑oss:120b; medium review → qwen2.5-coder:32b; small tasks → qwen2.5-coder:1.5b or gpt‑oss:20b; vision/OCR → VLMs.  
- Wrap routing logic in `ollama-task-router.mjs`; integrate into PRISM build pipeline before each step, Claude‑Opus after all steps.  
- Health checks via `ollama-health-check.mjs` every 10 min.  
- Commit changes with message: “feat(llm): routing matrix, keep‑alive, parallelism, build integration & health checks”.  
- Keep local models only; do not wire Kimi K2.6 cloud (data sovereignty).

**OPERATOR DIRECTIVES**  
- Get Qdrant up and running.  
- Verify Hermes‑to‑Claude flow after Qdrant is running.  
- Build blind‑mapping observability surface (`node scripts/hermes‑obsidian‑app‑map.mjs`).  
- Verify `hermes -z` produces output with gpt‑oss:20b primary; adjust max‑tokens if “no final response”.  
- Monitor memory pressure (commit charge 99 %); compact idle chats or reduce active threads.  
- Audit each galaxy sequentially, fill gaps using Obsidian and Ollama/docker setup.  
- Use ultracode for planning, orchestrating, building.  
- Commit only to slot/bravo worktree; go‑live merge gated until golf’s git‑tree repair completes.  
- Finish remaining phases of units/tasks; upgrade RTK with LLM compressor layer.

**FINDINGS/BUGS**  
- Hermes message failure: HTTP 400 from Anthropic (“extra usage” billing); fallback not triggered.  
- Qdrant down: Docker container missing/not running → semantic search unavailable.  
- N:\ drive bug resolved after MCP daemon restart; `self_awareness_search` returns hits.  
- Semantic_search fails due to Qdrant offline; `master_index_query` works fine.  
- Docker Desktop installed but not running → Qdrant down until engine started.  
- `docker-compose.yml` lacked qdrant service; launcher expected it, causing startup failure.  
- Hermes reported “Unknown provider ‘openai’”; correct local‑Ollama provider is `custom`.  
- Hermes daemon cached stale connection state (`isConnected()` flag) when Qdrant was down; no lazy reconnect.  
- GUI launcher stuck on git‑pull abort due to dirty package-lock.json; fixed by discarding changes and restarting.  
- Headless zulu works; gpt‑oss:20b is a thinking model, so Hermes’ one‑shot harness needs enough output tokens to surface final answer.  
- gpt‑oss reasoning channel mis‑handled by Hermes custom provider → “no final response” in headless mode.  
- Claude API rate limiting blocked multi‑agent audit workflows twice.  
- Memory count inflation: auto‑generated `node_*` files counted; corrected 20× bug.  
- Corrupted ai‑training synthesis (`ai-training_synthesis.md` all NUL bytes); flagged to owner (India).  
- Probe entry guard failed under relative paths; fixed to use absolute path or proper comparison.  
- Tag‑match bug: `nomic‑embed‑text:latest` reported absent; fixed by stripping `:latest`.  
- Stale‑reclaim test tautological; added fresh‑lock discriminator and bounded reclaim recursion.

**DOMAIN SPECIFICS**  
- Engines/dispatchers/actions: CapabilityIndexEngine, OutcomeCaptureBusEngine, DocuStrataMaterialPriorEngine, `prism_context:slot_brief_write`, `prism_memory:semantic_search`, `prism_session:master_index_query`, `prism_knowledge:search`.  
- Paths unique to galaxy: `/checkin-bravo`, `/slot‑briefs/*`, `state/shared/slot‑briefs/*`, `H:/prism/mcp-server/dist/index.js`, `scripts/fill-galaxy-memory-sections.mjs`, `syncGalaxyMemories()`, `galaxy-synthesis-refresh.mjs`, `zulu-orchestrator-sweep.mjs`.  
- Metrics: per‑galaxy memory count, wiki count, tribal count; GPU utilization 0–30 %, VRAM total 96 GB, free ~57 GB.  
- Paths: `scripts/ollama-tool-agent.mjs`, `scripts/ollama-offload-enforce.mjs`, `scripts/ollama-codegen.mjs`, `scripts/ollama-build-loop.mjs`, `scripts/ollama-capability-probe.mjs`, `scripts/ollama-coresidency.mjs`, `node scripts/hermes‑obsidian‑app‑map.mjs`.

**TOOLS USED**  
- PRISM commands: `/checkin`, `/checkin-bravo`, chat‑slots.mjs, audit-roadmap-drift.mjs.  
- Scripts/hooks: `wire-hermes-local-backend.mjs`, `mcp-server-supervisor.mjs`.  
- External services: Docker (Qdrant), Ollama (`:11434`), Anthropic API (`api.anthropic.com`).  
- Docker Desktop + docker-compose.yml.  
- Hermes CLI commands (`hermes config migrate`, `hermes model`, `hermes doctor`, `hermes -z`).  
- PRISM MCP server (`:3100/mcp`).  
- Qdrant container image `qdrant/qdrant:v1.17.0`.  
- Obsidian REST API plugin (local‑rest‑api).  
- Blind‑mapper script (`node scripts/hermes‑obsidian‑app‑map.mjs`).  
- Ultracode workflows.  
- Node test harnesses (`vitest`, `node --test`).

**OPEN THREADS**  
- Start Qdrant container and ensure `:6333` listening; semantic search works.  
- Resolve Hermes 400 error – enable “extra usage” billing on Claude Code or switch provider.  
- Verify Hermes‑to‑Claude flow after Qdrant running.  
- Re‑enable Hermes‑Obsidian Bridge scheduled task (PRISM Hermes‑Obsidian Bridge=disabled).  
- Regenerate ai‑training synthesis via `galaxy-synthesis-refresh.mjs` (owner: India).  
- Monitor vault mirror drift; schedule periodic `syncGalaxyMemories()` runs.  
- Resolve remaining Claude API rate limits for future multi‑agent audits.  
- Investigate gpt‑oss reasoning channel fix in Hermes custom provider.  
- Apply U9’s RECOMMENDED_ENV to Ollama server restart (#1).  
- Octopus 3‑voice build – larger effort, not loop‑tick safe.  
- Model‑ref doc cleanup (stale deepseek comments, qwen2.5‑coder:7b drift).  
- Full synergy plan execution pending completion of `wzows014c` output.  
- Go‑live merge & settings.json wiring – gated until golf’s git‑tree repair completes.  
- U1/U3 routing config edits – adjust NUM_PARALLEL, MAX_LOADED and un‑strand 120b tiers after go‑live.
