# compliance-safety session 928a8226 (2026-06-10, 68.1MB, spine 676KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `fix-hookify-frontmatter.mjs` – fixed 110 `.local.md` front‑matter & transliterated 28 non‑ASCII files.  
- `remove-dangling-hooks.json` – removed four missing `.mjs` refs from `settings.json`; JSONs now byte‑identical.  
- `intentionally-disabled-list.patch` – added removed hooks to `INTENTIONALLY_DISABLED`.  
- `ollama-resilient-pull-fix.ps1` – replaced em‑dashes; `gpt‑oss:120b` download 96 % complete.  
- `c593b096fb` – Ollama 120 B pull, synergy routing fix, set `num_predict=1024`.  
- `13017de764` – hookify front‑matter conversion + dangling hook removal.  
- `29f2d177f6` – node‑by‑node gap‑map (14 surfaces).  
- `8550436c8a` – replaced phantom “qdrant://prism‑memory” label with real collections.  
- `c9fe32d8b2` – tribal embed index OOM fix (`--max-old-space-size=8192`) + 2000‑char clamp for nomic context.  
- `182788232a` – cap‑safe loader for `tribal-embed-index.json` (3.4 s load).  
- `a3e6d3ca97` – fail‑open guard & shrink‑guard; restored brain to 4,162 entries.  
- `fb314a6fd1` – Docker‑service‑health CLI (`docker-service-health-check.mjs`).  
- `14b8b383a2` – wired Stop‑hook for Docker health check.  
- `006c435ddb` – fixed wrong restart command in `mcp-connectivity-check.mjs`.  
- `e2081e0780 + d22681f5d2` – singleton‑service‑guard update; MCP “--fix” reap+start recovery.  
- `bee977a004` – added `NODE_OPTIONS=--max-old-space-size=8192` to reranker spawn env.  
- `ed6662f45e` – slash‑agnostic regex for mcp‑daemon detection (`U-MCP-CMDMATCH-FIX`).  
- `29fb555f13` – JSON‑stdout protocol for `stop_on_hook_unregistration` (`U-HOOK-UNREG-PROTOCOL-FIX`).  
- `6ca2f6afd2` – ensured MCP spawn paths use ≥4096 MB heap (`U-MCP-BOOT-HEAP-FLOOR`).  
- `4141daf9d8` – fleet‑task‑health migration‑freeze marker; partitions ~47 frozen tasks.  
- `U-QP-COST-SAVINGS-TEST` – 13/13 vitest coverage of CostSavingsTrackerEngine.  
- `U-BW-DISPATCHER-SCAN` – repointed cascade defaults to live Blackwell models; closed source‑lock blind spot.  
- `U-PSN-SNAPSHOT-REFRESH` – refreshed stale PSN snapshot; 11 real P0 under‑wired pairs identified.  
- `U-QP-DIGITALTWIN-TEST` – reference‑value tests for all 5 quoting algorithms.  
- `U-QP-MARGIN-FLOOR-GATE` – added margin‑floor guard to QuoteEstimatorEngine.  
- `U-QP-MARGIN-FLOOR-SHOPCONFIG` – canonical `margin_floor_pct` in ShopConfigurationEngine.  
- `U-QP-NRE-AMORTIZE-TEST-FIX` – updated stale NRE amortization test.  
- `ec597dbcb3` – margin‑floor dispatcher round‑trip (7/7).  
- `87d5c4bf9a` – margin‑floor UI banner; tsc‑clean.  
- `4b0980c56a` – AI‑substrate routing matrix coverage 4/5→5/5 (27/27).  
- `62b114cb7b` – ResizeObserver polyfill for web tests; uncovered hidden failures.  
- `8258918f78` – U‑QP‑FRONTEND‑TEST‑REPAIR commit.  
- `44fab1be37` – dispatch map for fleet‑synergy goal.  
- `4c12a75a8d` – closed‑loop provenance gate (`U-QP-CLOSED-LOOP-PROVENANCE-GATE`).  
- `d294957c4d` – outbound promotion gate (`U-QP-OUTBOUND-PROMOTE-GATE`).  
- `bf10035ec0` – quote‑time freshness preflight (`U-QP-CALIBRATION-FRESHNESS-PREFLIGHT`).  

**DECISIONS**  
- Debug hook errors by fixing root cause; removed dangling hooks instead of disabling globally.  
- Added removed hooks to `INTENTIONALLY_DISABLED`; gate passes next run.  
- Adopted slash‑agnostic regex for MCP daemon detection; ensured ≥4096 MB heap floor on all spawn paths.  
- Switched stop‑hook protocol from exit‑code to JSON‑stdout with `continue:true/false`.  
- Deferred Qdrant tribal migration until corpus stability confirmed.  
- Consolidated cost‑bridge hooks into single spawn; avoided double execution.  
- Made awareness generator router‑aware; updated dispatcher cascade defaults to live Blackwell models.  
- Implemented margin‑floor gate and freshness preflight for quoting closed‑loop learning.  

**OPERATOR DIRECTIVES**  
- “continue” after prior fixes (slice 1).  
- “make sure fleet reaper and monitors are running please” (slice 2).  
- “synergize ollama … docker, qdrant, obsidian vault app, PSN, system‑viz, prism galaxies, front‑end & back‑end build, claude.md, memories, wiki, tribal knowledge, prism awareness, gsd, tdd, skills, scripts and hooks” (slice 4).  
- “goal clear: everything wire, tested, validated and synergized across the entire system, node by node, galaxy by galaxy.” (slice 5).  
- “Utilize ultracode for planning, deep reasoning and orchestrating + ollama local llm for grunt work…” (slice 7).  
- `/startup-charlie /loop /goal [closed‑loop + quoting production‑ready]` – focus on charlie domain (slice 8).  

**FINDINGS/BUGS**  
- Hookify rule‑file errors: 110 missing YAML frontmatter, 1 charmap crash, 28 non‑ASCII bytes.  
- Dangling hook refs: four `.mjs` files referenced in `settings.json`.  
- Stop‑hook unregistration gate blocked; resolved by adding to `INTENTIONALLY_DISABLED`.  
- Unwired engines audit: 90 built engines with no dispatcher/route/hook consumer (53 core, 23 bridges, 14 ops).  
- Ollama resilient pull script had PowerShell parse error due to em‑dashes.  
- Synergy bug: `routeModelForTask` selected `gpt‑oss:20b` instead of newly installed `gpt‑oss:120b`.  
- V8 string cap (512 MiB) caused PSN leg #5 OOM; fixed with buffer loader.  
- Default‑heap OOM (~150 MB) after index grew to 168 MB; resolved by raising heap size for reranker.  
- MCP restart command wrong (`--port` → stdio); corrected to `TRANSPORT=http`.  
- Fleet‑task‑health static list of 4 tasks was wrong; actual freeze set ~47 tasks (cry‑wolf).  
- Cost‑bridge “0 wired” alarm false positive due to router consolidation.  
- Awareness generator stale snapshot misreported unwired tasks.  
- Dispatcher cascade defaults referenced retired models, causing silent failures.  
- PSN snapshot stale by 6 days; refreshed and identified under‑wired pairs.  

**DOMAIN SPECIFICS**  
- Hookify `.local.md` rule files (YAML frontmatter).  
- `settings.json` hook arrays & `INTENTIONALLY_DISABLED`.  
- Golf slot claim logic (`/checkin-golf`).  
- Ollama model routing config (`ollama-cost-router.mjs`) & resilient pull script.  
- Tribal index loader (`tribal-embed-index.json`, cap‑safe buffer).  
- PSN leg #5 tribal injection via `tribal-rerank.mjs`.  
- Qdrant collections: `prism_engines`, `prism_skills`, `prism_formulas`.  
- MCP server (daemon, supervisor, helper, autostart); port 3100 health guard.  
- Docker health check (`docker-service-health-check.mjs`).  
- Fleet‑task‑health watch (`fleet-task-health-watch.mjs`).  
- CostSavingsTrackerEngine, WEDMQuoteBridgeEngine, QuoteEstimatorEngine, DigitalTwinEstimator.  
- ShopConfigurationEngine (canonical `margin_floor_pct`).  

**TOOLS USED**  
- Hookify plugin (`config_loader.py`).  
- Node scripts: `fleet-reaper-sweep.mjs`, `audit-unwired-engines.mjs`.  
- PowerShell script: `ollama-resilient-pull.ps1`.  
- Docker CLI (`docker ps`, `docker start`).  
- Node.js scripts: `load-tribal-index.mjs`, `singleton-service-guard.mjs`.  
- Ultracode workflow engine.  
- Vitest, Jest test harnesses.  
- PRISM tools: `mcp-server-daemon.mjs`, `mcp-connectivity-check.mjs`.  
- Stop‑hook framework (`stop_on_hook_unregistration.mjs`).  

**OPEN THREADS**  
- Wire the 90 unwired engines (owner‑triage).  
- Resolve synergy bug so `gpt‑oss:120b` is selected for best‑tier tasks.  
- Finalize remaining ~2.7 GB of `gpt‑oss:120b` download & verify integrity.  
- Confirm fleet‑reaper ownership and reaping logic post‑migration freeze.  
- Monitor scheduled‑task health after migration; ensure no unintended task loss.  
- Update documentation for intentional academy routing fallback.  
- MCP keep‑alive scheduled task (`singleton-service-guard --fix`) periodic run.  
- Qdrant tribal migration deferred until corpus stability confirmed.  
- Write‑side sharding for tribal index once >512 MiB.  
- Re‑embed lost ~29 K delta from on‑disk wiki/memory sources.  
- Full Ollama offload audit pending rate‑limit resolution.  
- Build & test `U-QP-BLUEPRINT-OCR-BRIDGE-ADAPTER`; integrate into `shopDispatcher` & `businessDispatcher`.  
- Verify all quoting‑frontend tests pass hermetically; run full‑heap tsc to confirm no regressions.  
- Confirm OCR noise floor issue remains source‑side; document for operator/ERP cleanup.  
- Fleet‑wide goal remains unsatisfied (outside charlie scope).
