# academy session 51013954 (2026-05-18, 4MB, spine 20KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-GAP-POST-GCODE-BACKPLOT` – commit c6deb1d17f, 21/21 tests, envelope flipped (12/64).  
- `U-GAP-POST-RL-POSTPROCESSOR` – committed, 18/18 tests, envelope flipped.  
- `CK-MS12/U02 ProgramCompareEngine` – committed, 18/18 tests, envelope flipped.  

**DECISIONS**  
- Slot‑binding wrapper for `/checkin-india`: force‑claim slot “india”, bind to topic `india-work`, then run full `/checkin`.  
- Pipeline split: (3‑7) slot‑claim & drift checks; (8‑14) dev pipeline only when task args present.  
- Dedup‑preflight first: verify engine already on disk and wired before adding tests.  
- Test‑coverage strategy: add real‑value behavioral suites for each ported engine, then per‑file scrutiny.  
- Heavy‑build triage: defer `JMDIE-LEARNING` & `WIRE-BACKLOG-POST`; capture triage in handoff.  
- Loop directive: “complete all remaining tasks” is a multi‑session goal; use loop‑state + handoff to span `/compact`.  
- Memory‑pressure handling: stop heavy builds when host hits 94–98 % commit pressure; commit durable artifacts first.

**OPERATOR DIRECTIVES**  
- “A session‑scoped Stop hook is now active with condition: *complete all remaining tasks in your task queue. /loop [10m] complete /goal*. Briefly acknowledge the goal, then immediately start (or continue) working toward it — treat the condition itself as your directive and do not pause to ask the user what to do.”

**FINDINGS/BUGS**  
- Crash postmortems: `foxtrot` & `mike` died under 98 % memory pressure; host now fails `xmalloc: cannot allocate 8192 bytes`.  
- Test bug in arc fixture (`G3 … I‑10 J0`) – wrong center; fixed by prepending positioning move.  
- Fast‑close vein exhausted: of next 60 units, only two have same‑named untested engines and both are heavy builds.  
- Host memory failure blocks further scrutiny/compilation; must pause until allocator recovers.

**DOMAIN SPECIFICS**  
- Engines: `BackplotEngine`, `RLPostProcessorEngine`, `ProgramCompareEngine`.  
- Dispatchers: `prism_calc` (backplot_parse/backplot_statistics), `camDispatcher` (multi‑engine wiring).  
- Metrics/Checks: envelope flips, commit hygiene, drift audit (`audit-roadmap-drift.mjs`), system‑viz ping, CLAUDE.md staleness, local‑compute health, fleet activity & pickup candidates.  
- Paths: `/checkin-india` → `chat-slots.mjs reclaim/claim`, full `/checkin` pipeline (steps 3‑7 & 8‑14).

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `audit-roadmap-drift.mjs`, system‑viz ping, CLAUDE.md staleness script.  
- Test harness: Jest per‑file suites, two‑reviewer scrutiny agents.  
- Loop & handoff: loop‑state persistence, handoff file (`HANDOFF-claude-51013954-india-work.md`).  
- Git utilities for branch/commit checks.

**OPEN THREADS**  
- Remaining 377 units in the “india” queue (incl. `JMDIE-LEARNING`, `WIRE-BACKLOG-POST`).  
- Need to resume loop after memory recovery or a fresh `/compact` context.  
- Capture triage of deferred heavy builds (`WIRE-BACKLOG-POST`) in handoff for next session.  
- Ensure loop‑state and handoff survive across sessions; continue with minimal‑footprint units once allocator stabilizes.
