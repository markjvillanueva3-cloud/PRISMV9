# academy session 9876118b (2026-05-18, 48.5MB, spine 260KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `scripts/fleet-task-health-watch.mjs` – watchdog, 32 tests passed.  
- `.claude/hooks/fleet-task‑health-stop.mjs` – stop hook wired to both settings.json copies.  
- `.claude/hooks/critical-memory-compact-nudge.mjs` – UserPromptSubmit hook injecting `/compact`; 19 tests passed.  
- Docs: `MEMORY.md`, `CLAUDE.md`, wiki entry; memory‑file index < 24576 bytes.  
- Re‑armed fleet‑reaper Monitor (`b71gnezkl`) – global scheduled task “Ready”.  
- Commit **FLEET-TASK-HEALTH-MS0** (da01003b61) – watchdog + nudge hook, 6 files.  
- Commit `31cc2c84d3` – Arm‑C P1 blocker fixes; 55/55 tests green.  
- Commit `ac9cca8902` – Enum‑blind regression fix (`process-slot-map.mjs $p.Name C0‑strip`).  
- Commit `d3db37cc2c` – WindowsKill per‑PID kill rewrite in `fleet-reaper-sweep.mjs`; 55/55 tests green.  
- Commit `bb7d30c7cc` – slotLabel:null deep‑fix (unblocks Build B).  
- Commit `85703afab6` – U‑CHO01 chat‑orchestrator decision module.  
- Commit `U‑CHO02` – context‑fill monitor (`chat-token-watch.mjs`).  
- Commit `b41b829232` – U‑CRIT‑NUDGE‑PATHFIX (ps-window-pin path fix) + SOFT‑RELIEF‑V2.  
- Commit `9cfc411eb3` – U‑FR‑OPT2‑CRITICAL‑88 (lower critical threshold).  
- Commit `cc14791cd7` – U‑FR‑OLLAMA‑KEEP‑ALIVE‑1H (permanent keep‑alive).  
- Commit `U‑FR‑CRASH‑WATCH` – crash‑watch integration.  
- Commit `405ac15be7` – `nim-docker-launcher.mjs` (NIM server provisioner).  
- Commit `NIM‑ACTIVATION‑MS0.md` – spec for migrating inline Ollama hooks to NIM router.

**DECISIONS**  
- Build both watchdog and nudge hook as requested.  
- Move chat from golf to mike slot; later re‑own fleet‑reaper under golf slot (alpha-slot-reaper-guardian.mjs).  
- Classify task failing only when launch‑failure HRESULT high bit set (`>=0x80000000`); small exit codes are findings.  
- Add entry‑point guard in hooks to avoid side‑effects during tests.  
- Resolve path order for chat‑slot lookup in nudge hook to match monitor’s source file.  
- Disable legacy `golf-slot-write-allowlist.mjs`; use session‑scoped bypass.  
- Use lightweight JSONL‑tail monitor (`bbyba1uq0`) with ≥95 % filter to avoid flooding.  
- Replace batch PowerShell kill loop with per‑PID invocation (fleet-reaper-sweep).  
- Keep enum‑blind fix active; schedule slotLabel:null deep‑fix next.  
- Use chat-slots + ps-window-pin mapping instead of editing peer‑dirty `chat-slots.mjs`.  
- Adopt pagefile auto‑managed + memory compression & page combining as primary memory‑pressure fix.  
- NIM integration: build 3-backend router (`local-llm-bridge.mjs`) that falls back to Ollama; provision via Docker/NGC key; migrate offload hooks one at a time.

**OPERATOR DIRECTIVES**  
- Use `/system-viz` to identify existing plans or components for fleet‑reaper.  
- Continue improving fleet‑reaper efficiency; address remaining crashed chats.  
- Research Windows and NVIDIA features to mitigate constant memory pressure.  
- Apply all identified fixes.  
- Get NVIDIA NIM working and integrated with Prism OS, Obsidian Brain, system‑viz, AI systems, Prism awareness, and the full Prism stack.

**FINDINGS/BUGS**  
- Watchdog mis‑flagged tasks exiting with code 1/2/3 as failures – fixed by `isLaunchFailureCode`.  
- Hooks ran `main()` on import; fixed with entry‑point guard.  
- Path mismatch between monitor and nudge hook caused potential `/compact` mis‑routing – corrected to use `.claude/state/chat-slots.json` first.  
- Enum‑blind regression in process enumeration caused reaper blindness at >90 % commit pressure.  
- Batch‑kill timeout bug truncated trailing PIDs; replaced by per‑PID kill.  
- `slotLabel:null` prevented Build B nudge hook from firing; advisories named “tree‑<PID>”.  
- Memory‑pressure auto‑relief task missing; operator must run installer manually.  
- Soft‑relief V2 now handles large alive slots under critical pressure.  
- Pagefile mis‑configured (fixed size, auto‑manage off); commit limit 67 GB; requires reboot.  
- NIM server never provisioned; `nim-docker-launcher.mjs` now builds it.  
- `local-llm-bridge` orphaned – no consumer hooks use it yet.  
- All inline Ollama hooks need rewrite to route through router (`local‑llm‑bridge.queryLocalLLM`).  
- Memory pressure spikes (~99 % commit) trigger fork failures (`xmalloc`, `cudaMallocHost`).

**DOMAIN SPECIFICS**  
- **Fleet‑Reaper**: orphan‑process janitor, GPU coordinator, Ollama hint emitter; always‑on via global scheduled task + per-session monitor (owned by golf slot).  
- **Scheduled‑task health watchdog** audits all PRISM * tasks (Launch, LastRunTime, LastTaskResult).  
- **Memory‑monitor telemetry** (`state/shared/fleet-memory-history.jsonl`) drives compact nudge logic.  
- **Chat‑slot claim system** (`chat-slots.claim`, `chat-slots.mjs`) ties processes to slots; `slotLabel` null was root cause of orphan mis‑attribution.  
- **Process‑Slot‑Map.mjs** maps PIDs to chat slots; sanitizes `$p.Name`.  
- **WindowsKill per‑PID kill rewrite** in `fleet-reaper-sweep.mjs`.  
- **Hooks**: stop hook, UserPromptSubmit hook, `golf-slot-write-allowlist.mjs`, `precompact-pending-guard.mjs`, `local-llm-bridge.mjs`.  
- **Orchestrator design**: crash detection → terminal respawn; context‑budget monitoring → `/clear` or `/compact`; advisory injection via UserPromptSubmit.  
- **ps-window-pin mapping** for chat slots.  
- **NIM router** (`local-llm-bridge.mjs`) with fallback to Ollama.

**TOOLS USED**  
- Node scripts: `fleet-task-health-watch.mjs`, `fleet-task-health-stop.mjs`, `critical-memory-compact-nudge.mjs`, `fleet-reaper-sweep.mjs`, `chat-slots.mjs`, `nim-docker-launcher.mjs`, `local-llm-bridge.mjs`.  
- `node:test` test runner (no vitest).  
- Windows PowerShell: `Get-ScheduledTask`, `Get-ScheduledTaskInfo`; per‑PID kill loop replaced by direct invocation.  
- PRISM hooks: Stop hook, UserPromptSubmit hook; c-to-h mirror for settings sync.  
- Chat‑slot claim helpers and state files (`.claude/state/chat-slots.json`, `state/shared/fleet-memory-history.jsonl`).  
- Docker Desktop + NGC key provisioning for NIM.  
- `system-viz` generator.

**OPEN THREADS**  
1. slotLabel:null deep‑fix in `fleet-memory-monitor` + `process-slot-map.mjs` (peer‑claimed by claude-339c8ff7).  
2. Register memory‑pressure auto‑relief task (`install-memory-pressure-task.ps1`).  
3. Implement account‑cycler to rotate 5 Claude accounts when usage limits near.  
4. Stress‑test per‑PID kill under sustained >95 % commit pressure.  
5. NIM migration – rewrite each inline Ollama hook to use `local-llm-bridge.queryLocalLLM` (U-NIM-MIGRATE‑01…N).  
6. Reboot to activate system‑managed pagefile and memory compression/page combining; required before expanding commit ceiling.  
7. Docker + NGC key provisioning – operator must start Docker Desktop, set `NGC_API_KEY`, run `nim-docker-launcher.mjs`.  
8. Orchestrator final units – U-CHO03 (advisory injection), U-CHO05 (UI Automation SendKeys) still pending.  
9. Fleet‑reaper tuning – confirm soft-relief V2 triggers under real critical pressure; monitor for residual crash patterns.
