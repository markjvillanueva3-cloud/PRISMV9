# academy session bca3789f (2026-05-19, 30.5MB, spine 132KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `baef3c361d` – Dashboard offload‑rate recalculation (10.9 % → 42.1 % last‑24h).  
- `94d4d0feac` – Wiki recall expanded from 722 index entries to 22,734 leaf files via `wiki_lookup`.  
- `ecbadc2862` – `U‑OBSIDIAN‑LOOKUP`: memory lookup tool added to Ollama bridge.  
- `b756569c04` – `U‑DISPATCHER‑MAP`: dispatcher‑digest scan tool.  
- `c55b68b8ea` – `U‑INFRA‑DOCKER‑FIX`: compose target, port filter, entrypoint guard.  
- `b041693181` – `U‑REAPER‑COORD‑NOISE`: dashboard infra‑mutation filtering (265 infra vs 125 real).  
- `6d0139c0b6` – `U‑SEMANTIC‑LOOKUP`: BM25+int8 vector search tool (14,738‑vector index).  
- `aa58c8f3eb` – `U‑OBSIDIAN‑PRECHECK`: hook to inject memory corpus before wiki lookup.  
- `60dbc72116` – RTK half of HTML/RTK upgrade committed to main tree.  
- `b2236a1b6c`, `22ce231882`, `5709f19d7b`, `f344e371ab` – Charlie’s unit‑knowledge‑pack & injection hooks (U‑UKP01 / U‑UKP02).  
- `53957302d4` – `LIMA [WIKI‑LEAFIDX]/U‑WIKI‑LEAFIDX‑FAILLOUD`: R12 fail‑loud detection for silent no‑op exit‑0 in `build-wiki-leaf-index.mjs`.

**DECISIONS**  
- Pivot from L2b MCP dispatcher to high‑ROI wiki leaf scan after offload‑rate misreading.  
- Expand Ollama bridge to 7 read‑only tools; keep all as “read” to preserve token budget.  
- Adopt stranded HTML/RTK cluster with honest co‑attribution (avoids cross‑chat mis‑commit).  
- Defer root‑doc guard patch due to stale twin issue; ship only patch‑sibling target.  
- Use `[MAIN]` commit prefix for worktree commits; commit only tracked paths (`git commit -- <pathspec>`).  
- Commit only deliverables in `slot-lima` worktree; avoid touching peer‑staged files.  
- Adopt R12 fail‑loud doctrine for silent failures (e.g., `build-wiki-leaf-index.mjs`).  
- Enforce per‑file 2‑reviewer scrutiny; any P0/P1 must be fixed before commit.  
- Never inline physics constants; import from `mcp-server/src/physics/constants.ts`.  
- Do not create stub engines; use `duplicationGuardEngine.mustCheckBeforeCreating()`.  
- Disable destructive git actions unless explicitly requested.

**OPERATOR DIRECTIVES**  
- `/goal expand obsidian wiki + tribal knowledge injection + prism os + obsidian brain + ollama + system‑viz functionality. focus on back end development tools and improvements`.  
- `/loop HTML+RTK system upgrades`: adopt stranded cluster, finish HTML adoption, address RTK coverage gaps.  
- Continue html/rtk upgrades then continue with obsidian wiki & context management enhancements using obsidian, ollama, system‑viz.  
- Resume loop after compaction; pick a bounded enhancement.

**FINDINGS/BUGS**  
- Offload rate 10.9 % inflated by side‑channel `fleet‑reaper‑coordinator`; true offload ≈ 9.3 %.  
- L2b MCP transport blocked at port 3100 (initialize + tools/call timeout).  
- Docker compose broken: missing production stage, host‑bound PostgreSQL conflict (5432 already bound).  
- Root‑doc twin stale caused false drift detection in `html-companion-guard`.  
- R9 test gap for HTML source‑hash; added CLI E2E to verify hash consistency.  
- Silent no‑op exit‑0 regression in `build-wiki-leaf-index.mjs` under memory pressure.  
- Parser bug for triggers frontmatter (flat‑string vs structured) – 103 emitted out of 399 entries (fixed on main).  
- Fleet reaper hung `git commit` holding `.git/index.lock`; killed to unblock.  
- Bash/PowerShell ENOSPC due to full temp disk during harness output.

**DOMAIN SPECIFICS**  
- **Engines / Actions / Dispatchers**: `edmDispatcher`, `dispatcher_map`, `wiki_lookup`, `semantic_search`, `tribal_lookup`, `obsidian_lookup`, `rtk-prefix-reminder`, `html-companion-guard`, `build-wiki-leaf-index.mjs`, `extract-skill-triggers.mjs`.  
- **Metrics**: offload rate, token savings, drift‑detection counts, test coverage, `_leaf-index.jsonl` size.  
- **Unique Paths**: `H:/prism-slot-delta`, `H:/prism-slot-lima`, `state/shared/unit-knowledge-packs/`, `knowledge/wiki/architecture/`, `PRISM_WIKI_LEAFINDEX_MIN_ENTRIES` env knob, `PRISM_WIKI_LEAFIDX_FAILLOUD` tests.

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `per-agent-handoff.mjs`, `stable-session-id.mjs`, `milestone-tracker.mjs`.  
- Hooks: `unit-knowledge-pack-inject.mjs`, `rtk-prefix-reminder.mjs`, `html-companion-guard.mjs`.  
- Scripts: `unit-knowledge-pack.mjs`, `semantic_search.js`, `tribal_lookup.js`, `build-wiki-leaf-index.mjs`, `build-wiki-leaf-index-failloud.test.mjs`, `dispatcher_map`.  
- Docker compose, Qdrant, Prism‑Server.  
- Git hooks: `worktree-commit-route`, `core.fsmonitor` disable.  
- Pipeline: `/checkin.md` full dev pipeline.

**OPEN THREADS**  
- Commit of `U-TRIBAL-LOOKUP` pending final scrutiny (currently in delta tree).  
- HTML adopt Iter 2 not yet committed; root‑doc guard deferred.  
- RTK coverage gaps still to be addressed.  
- L3 full agent loop blueprint remains deferred.  
- Integration test for patch‑sibling target in `html-companion-guard` pending.  
- AGENT_CHAT visibility on non-zero exit (unit 1).  
- Stage‑order assertion (unit 2).  
- Exit‑code constants export (unit 3).  
- `_stats.md` write protection (unit 4).  
- Streaming JSONL write to mitigate V8 string cap (unit 5).  
- Pre-existing `U-HTML-GUARD-ROOTDOC` (unit 6).  
- Potential further improvements to obsidian wiki recall index after the fix.  
- Additional R12 fail‑loud tests for other silent failures.
