# academy session 77971357 (2026-05-17, 36.5MB, spine 184KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑FEEDBACK‑FORCING` (RGS‑TOOL‑AUTOINVOKE‑MS1) – commit b1e599d5fc  
- `U‑REGEN‑VIZ‑MERGE‑FAILLOUD` – commit f9dc218d78  
- `Doc‑reflect for regression entry` – commit 9c5377a2a7  
- `U‑SOURCEHASH‑DOC‑ALIGN` – commit 0bac2d4c2f  
- `U‑COMPLEXITY‑FALLBACK` – commit 3d416cb040  
- `U‑BUG‑FINDINGS‑WIKI‑HOOK` – commit bb198d9285  
- `U‑BUG‑FINDINGS‑WIKI‑BACKFILL` – commit 1539fbc8b2  
- `Doc‑reflect pointer for fleet discoverability` – commit dabb7a76c6  
- RTK 0.40.0 installed, hook wired (`rtk hook claude`)  
- `PRISM_CHAT_BUS_COMPACT=1` set in C: & H: settings.json  
- File‑read‑cache.mjs confirmed working; F8 audit retracted  
- Persistent build‑request log created at `state/shared/USER-BUILD-REQUESTS-LOG.md`  
- Tribal/Obsidian/viz utilization protocol extended (`feedback_tribal_obsidian_viz_utilization_protocol.md`)  
- `build-requests-viz-sync.mjs`: 296 insertions, drift‑checker for build‑log  
- High‑ROI node‑wiring scan logged (Lathe domain: 82 unwired engines; 729 engines flagged NEEDS_WIRING)

**DECISIONS**  
- Resume MS1 P1 backlog → `U‑RIE‑ADAPTER`, `U‑CALIBRATION`, `U‑TRANSFER`  
- Fix regen‑viz merge‑step silent failure first (precompact & compact to run before implementation)  
- Adopt bug‑finding→wiki rule (hook already shipped)  
- Run Boris audit v2 after above fixes  
- RTK install authorized; binary upgraded to 0.40.0  
- Chat‑bus compact default set to reduce per‑prompt overhead  
- Slot‑worktree migration remains operator‑decided, not auto‑applied  
- F8 audit finding retracted (no new hook built)

**OPERATOR DIRECTIVES**  
- `/checkin-lima /loop all lima work` – start autonomous loop on queued tasks  
- `continue` – resume after loop completion  
- Rule: always update wiki for bug findings hooked (implemented via hook)  
- `/checkin-lima continue all current tasks in this chat /loop [20m] /goal finish all tasks` – 20‑min budget to ship remaining units  
- Set `PRISM_CHAT_BUS_COMPACT=1` in both settings.json copies  
- Upgrade RTK binary from 0.37.1 → 0.40.0 and wire hook

**FINDINGS/BUGS**  
- `regen-viz.mjs` merge step fails silently when `merge‑augmentations.mjs` > 4 GB heap; add `--max-old-space-size=8192`  
- Legacy nested `{plan: …}` in pick‑prefresh fixtures → test failures; fixed to flat plan  
- Hook OOMs (`xmalloc: cannot allocate 8192 bytes`) under high commit pressure – pending root‑cause analysis  
- `MEMORY.md` exceeds truncation ceiling by 27 bytes (24 576 B)  
- `CLAUDE.md` size far above doctrine limit  
- File‑read‑cache is a functioning PreToolUse:Read deny‑dedup hook; earlier “0 hits” misdiagnosis  
- Memory‑relevance injection hook fires on every edit/write but emits no telemetry → hit‑rate unmeasurable  
- Slot‑worktree migration not enforced; shared‑tree commit collisions observed (peer‑commit 5146a943df absorbed F5 changes)  
- `classifyHits` regex mis‑parsed real system‑viz query output; fixed to format: `L<layer>/<subgroup> <node-id> <label>` with ghost nodes prefixed `ghost.`

**DOMAIN SPECIFICS**  
- PRISM slot‑task‑claims & chat‑slots for handoff management  
- Regen‑viz orchestrator: 48 augmentation generators → merge step → post‑merge (graph repair, drift gate)  
- `U‑FEEDBACK‑FORCING` resolves composite key (`resolveUnitKey`)  
- Bug‑finding wiki hook auto‑appends regressions to `CLAUDE.md`, creates wiki entries (Boris pattern)  
- Boris audit v2 adds HTML output, self‑scheduling `/loop`, anti‑drift checkpoints  
- Engines: RTK, file‑read‑cache, chat‑bus, system‑viz graph  
- Actions/dispatchers: PreToolUse hooks, CronCreate, schedule skill, handoff workflow  
- Metrics: token‑savings ledger, cache entry freshness, memory‑relevance hit counts (missing)  
- Unique paths: `U‑FEEDBACK‑FORCING`, MS1 P1 backlog units (`U‑RIE‑ADAPTER`, `U‑CALIBRATION`, `U‑TRANSFER`), `U‑CLEANUP‑B7/B9`  
- Injection system surfaces: tribal/memories/system‑viz; injectors – master-index-precheck, wiki-precheck, audit-viz-first, chat-bus, awareness-snapshot, build-state, claude-brief, Ollama routers, memory-relevance, subagent-start-context  
- Log file: `state/shared/USER-BUILD-REQUESTS-LOG.md`  
- System‑viz query output format & ghost node detection as above  
- High‑ROI wiring scan: 729 engines flagged NEEDS_WIRING (~77.7 % wire rate), raw signal ~50 % false positives; top cluster Lathe domain with 82 unwired engines

**TOOLS USED**  
- `/checkin-lima` skill (slot binding + pipeline delegation)  
- `scripts/regen-viz.mjs`, `merge‑augmentations.mjs`  
- `pick-prefresh-inject.mjs` hook  
- Test harness (`scripts/__tests__/`)  
- Git helpers: `chat-slots.mjs`, `slot-task-claims.mjs`, `git reset/commit`  
- `c-to-h-mirror` for settings.json edits  
- CronCreate, schedule skill, PreToolUse hooks (`rtk hook claude`, `file-read-cache.mjs`)  
- GitHub API for RTK release download → `H:/.claude/bin/`  
- Settings.json manipulation (C↔H mirroring)  
- Memory docs & handoff files  
- `build-requests-viz-sync.mjs` drift checker  
- `system-viz-query find`  
- `high-value-additions-rank.mjs`  
- `node scripts/validate-unwired-signal.mjs`  
- PRISM injection‑awareness docs (`feedback_tribal_obsidian_viz_utilization_protocol.md`)

**OPEN THREADS**  
- Remaining MS1 P1 units: `U‑RIE‑ADAPTER`, `U‑CALIBRATION`, `U‑TRANSFER`  
- Regen‑viz merge‑step silent failure fix pending  
- Hook OOMs under high commit pressure – root‑cause analysis needed  
- Boris audit v2 not yet executed; schedule after current fixes  
- Fleet‑wide slot‑worktree migration to eliminate shared‑tree commit collisions  
- Scope‑narrowed build of Ollama offload (F3) for token savings  
- Add telemetry/threshold tuning to memory‑relevance injection hook  
- Continue token‑savings audit: remaining P1/P2 items, especially F5‑style findings  
- Run validator to separate real NEEDS_WIRING orphans from false positives before wiring  
- Await go‑ahead to wire 82 Lathe engines (or other high‑ROI clusters)  
- Monitor log drift beyond ~20 entries; consider automated re‑check if threshold exceeded
