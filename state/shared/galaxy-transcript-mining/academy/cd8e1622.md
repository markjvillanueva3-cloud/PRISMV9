# academy session cd8e1622 (2026-06-03, 12.9MB, spine 98KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `scripts/lib/code-path-resolver.mjs` – O(1) node↔path resolver (`byCode`, `type`, optional `line`, `repoPath`).  
- `scripts/lib/nav-savings-ledger.mjs` – ledger for navigation hits, credit logic, capped 5 MB JSONL.  
- `scripts/lib/graph-exact-match.mjs` – shared `exactMatchHit`, `navPathLine`; banner “→ Read <repoPath>”.  
- Hook rewrites:  
  - `.claude/hooks/master-index-precheck-inject.mjs` (injects banner & credits nav hit).  
  - `.claude/hooks/pre-bash-graph-inject.mjs`, `pre-grep-graph-inject.mjs`, `pre-write-graph-inject.mjs` – now use shared helper.  
- `/nav` skill wired to resolver & ledger.  
- Tests: 95+ passing (resolver, ledger, hooks, integration).  
- Docs: `knowledge/wiki/architecture/node-path-template.md`; repo‑root feedback; MS0 drift audit.  
- Commits: `33753f4c67`, `ffcfdb2b5d`, `754626f63f` (bulk absorbed in peer commit `155902c36e`).  

**DECISIONS**  
- Extend existing resolver instead of building new graph parser to avoid duplication.  
- Add repo‑root‑relative path to eliminate stale `src/` reads.  
- Implement credit‑on‑emit gate to prevent over‑crediting on deduped banners.  
- Use master‑index precheck inject to replace fs‑search in tool calls for deterministic resolution.  
- Cap nav‑savings ledger at 5 MB JSONL; aggregate and credit navigation efficiency (`PRISM_NAV_EST_TOKENS`).  
- Enforce 2‑agent scrutiny per file + 3‑of‑3 gate before task completion (YOLO mode).  
- Stop autonomous loop when context >64% to prevent overflow.  

**OPERATOR DIRECTIVES**  
- “Sierra in‑lane work COMPLETE. Fresh session: pick a new sierra‑lane unit via `priority‑queue.mjs --pick --slot sierra`. The 2 MS0 gaps are CROSS‑LANE (`U-P3-FORGE-OLLAMA-CODEGEN→alpha`, `U-P1-QDRANT-EPDRIC-RECALL→juliett/india`).”  
- `/loop [5m] /yolo-mode` started; cron `def83581`.  
- User instructed to continue from last state, no questions allowed.  
- Auto‑fix protocol: 3 attempts per error → diagnose → fix → retry.  

**FINDINGS/BUGS**  
- Shared‑tree absorption race: bulk code landed in peer commit `155902c36e`; attribution mixed.  
- `.git/index.lock` held >100 s → stalls all commits; needs investigation.  
- Repo‑root‑relative path bug fixed (previously emitted bare `src/...`).  
- Environment‑blocked e2e exact‑match test: master‑index returns no single‑token hits in this env.  
- Dedup guard correctly blocked duplicate engine creation (`MillProgramOptimizerEngine`).  
- Context reached 64% during YOLO loop, triggering safe stop and cron deletion.  

**DOMAIN SPECIFICS**  
- Engines/skills: `code-path-resolver`, `/nav`.  
- Hooks: `master-index-precheck-inject.graph‑inject.mjs`, `pre-bash-graph-inject.mjs`, `pre-grep-graph-inject.mjs`, `pre-write-graph-inject.mjs`, `stop‑psn-savings‑aggregate.mjs`.  
- Metrics: nav‑savings ledger (`SOURCES.nav`), token‑saving telemetry (`PRISM_NAV_EST_TOKENS`).  
- Paths: repo‑root‑relative paths via resolver; exact‑match banner format “→ Read <repoPath>”.  

**TOOLS USED**  
- PRISM core: `/checkin`, `/dedup`, `milestone-tracker.mjs`, `per-agent-handoff.mjs`, `stable-session-id.mjs`.  
- Scripts: `graph-exact-match.mjs`, `code-path-resolver.mjs`, `nav-savings-ledger.mjs`.  
- Testing: `vitest` (`rtk vitest run`).  
- System‑viz query, wiki‑query, memory search.  
- RTK (git, vitest, tsc, npm).  
- Ollama offload (`/ollama-*`, qwen2.5-coder:7b).  
- `/system-viz-query.mjs` for live 3D map queries.  
- Obsidian queries (`/wiki-query`, `prism_memory:semantic_search`).  
- Master‑index query (`prism_session:master_index_query`).  
- MCP dispatchers (`prism_calc`, `prism_safety`, `prism_cam`, `prism_cad`, `prism_turning`, `prism_ai`, `prism_dev`).  
- Scrutiny gates (`scrutinize-before-stop`, `scrutiny-3way.mjs`).  
- Loop state management (`loop-state.mjs`).  

**OPEN THREADS**  
1. Build cross‑lane units (`U-P3-FORGE-OLLAMA-CODEGEN`, `U-P1-QDRANT-EPDRIC-RECALL`) in their respective slots.  
2. Correct SYSTEM‑VIZ‑BRAIN‑MS0 envelope status for the two done‑untracked units.  
3. Resolve shared‑tree absorption race and `.git/index.lock` stall; consider moving sierra to a dedicated slot worktree.  
4. Maintain 5 min `/yolo` cron (`def83581`) until next session or manual cancellation.  
5. Run `/compact` to clear context (currently at 64%).
