# academy session b8c9990e (2026-06-03, 13.6MB, spine 174KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑CIMCO‑NAV‑MAP` – commit 0b9b4308fc, 9 files (+9513 insertions).  
- Cron job 84be705d: every 5 min `/yolo-mode`.  
- `U‑CIMCO‑DRIFT‑GROUPING‑FIX` – split name‑collision from true drift; added Jaccard test (0.4); 240 true drifts + 7 collisions reported.  
- `U‑CIMCO‑LAUNCH‑PROBE` – verified 4‑exe inventory, launch pattern `CIMCOEdit.exe <file>`, blind‑safe External‑Command hook `$FILEPATH → $OUTFILE`; added `engine.launchSurface()` and dispatcher `cimco_launch_surface`.  
- `U‑CIMCO‑DIALECT‑ALLOWLISTS` – mined 706 JM goldens; per‑dialect allowlists (Okuma‑OSP 224, Prism 388, Hurco 35, Mastercam 6, Mitsubishi‑EDM 2); added `engine.dialectAllowlist()`, `dialectLint()` and dispatchers `cimco_dialect_allowlist`, `cimco_dialect_lint`.

**DECISIONS**  
- Adopt blind‑navigation workflow (18 agents) parsing 154 CHM pages → **511 surfaces**; ship loader/query API into `CimcoVerificationBridgeEngine`.  
- Keep autonomous loop via `/loop` + cron; no manual checkpoints.  
- Resolve Okuma drift bug: fix grouping logic, add okuma‑osp mask.  
- Use `nav-map.json` as sole UI driver; all CIMCO interactions via external command hook, drop UIA.  
- Split name‑collision from drift for byte‑equivalence audit.  
- Choose MSAA over UIA for Simulation Report reading (UIA shows 0 actionable elements).  
- Machine Simulation is licensed add‑on: activate license & select model; no new installer.  
- Path‑scoped `git commit -- <paths>` to avoid shared-index race.

**OPERATOR DIRECTIVES**  
- `/checkin‑echo` – force‑take echo slot, run full `/checkin`.  
- `/goal /loop [10m]` – start autonomous loop for blind navigation & post‑processor testing.  
- `/loop 5m /babysit-prs` (example) – recurring interval command.  
- `cimco app up` – signal live CIMCO editor running.  
- Activate YOLO mode (`/yolo-mode`).  
- Tick `loop-state.mjs` each iteration; slot‑binding wrapper `/checkin‑echo /loop system-viz-brain until /goal`.

**FINDINGS/BUGS**  
- Okuma lathes flagged “semantic‑drift”: only 40 of 298 groups classified due to grouping bug & unreachable okuma‑osp mask.  
- Supported-controls cluster failed (StructuredOutput‑schema incompatibility).  
- Git lock contention during commit; resolved by retry loop and path‑scoped commits.  
- UIA tree of CIMCOEdit shows 0 actionable elements; Codejock Xtreme Toolkit renders custom GDI controls without UIA provider.  
- MSAA probe initially failed signed‑int bug; after fix, **11 accessibility nodes** found (3 `XTPReport` grids) but empty until Machine Simulation runs.  
- Duplicate asset creation throws from `duplicationGuardEngine.mustCheckBeforeCreating()`.  
- Scrutiny gate now satisfies 3/3 PASS.

**DOMAIN SPECIFICS**  
- Engines/actions: `cimco-control-map.mjs`, `cimco-post-proof.mjs`, `nc-dialect-masks.mjs`, `CimcoVerificationBridgeEngine.ts`, `cimcoDispatcher.ts`.  
- Dispatchers added: `navQuery`, `navReadiness` (bridge actions), `cimco_launch_surface`, `cimco_dialect_allowlist`, `cimco_dialect_lint`.  
- Metrics: **511 navigable surfaces**, 370 proof‑relevant; all 5 critical post‑proving paths verified “navigable=true”.  
- Unique paths: `state/shared/cimco/nav-map.json`, `scripts/cimco-nav-map.mjs`, `scripts/cimco-nav-map-ingest.mjs`.  
- Helpers: `walkNC`, `detectDialect`, `extractCodes`; Machine‑model files (`*.mcfg`) bundled in CIMCOEdit.

**TOOLS USED**  
- PRISM tooling: RTK (git, vitest, tsc), CronCreate, system-viz-query, wiki-query, checkin-recall, scrutiny-3way.  
- Skills/scripts/hooks: `/checkin‑echo` wrapper, `loop-state.mjs`, ingest script, nav-map loader.  
- Bash prefix `rtk` for all commands; `/ollama-*` skills for summarization/linting.  
- Node scripts: `H:/prism/scripts/system-viz-query.mjs`, `H:/prism/.claude/scripts/scrutiny-3way.mjs`.  
- Semantic search: `prism_memory:semantic_search`; tribal search: `prism_knowledge:tribal_search`.  
- Session queries: `prism_session:master_index_query`; state: `BUILD_STATE`, `MILESTONE_PROGRESS`.  
- Duplication guard: `duplicationGuardEngine.mustCheckBeforeCreating()`.  
- MSAA COM interop (`AccessibleObjectFromWindow`).

**OPEN THREADS**  
1. Finalize Machine Simulation Report reader – run simulation on real file, build MSAA or screenshot reader to feed `evaluateSimulationReport()`.  
2. Verify “Machine Simulation” license button enabled; activate license if needed.  
3. Generate per‑post static readout: ranked list of byte‑equivalent & dialect‑clean posts for live testing.  
4. Reassess UIA viability if future UI updates expose controls; currently MSAA is only viable channel.  
5. Ensure `/loop` continues with next unit (`U-CIMCO-UIA-REPORT-READER` or fallback) after simulation report becomes available.  
6. Implement `U‑CIMCO‑SETUP‑SIDECAR‑AUTHOR`, `U‑CIMCO‑FTP‑SHIP‑VERIFY`.  
7. Resolve duplicate asset creation guard enforcement before new file writes.
