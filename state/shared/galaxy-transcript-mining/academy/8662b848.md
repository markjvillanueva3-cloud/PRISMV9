# academy session 8662b848 (2026-06-04, 3.2MB, spine 19KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit Jun 3 13:47 – `PipelineHarnessAdaptersEngine.ts` updated with `makeLatheAdapter()` wiring the live `turningPrintToProgramEngine`.  
- Added vitest spec `lathe-roundtrip-accuracy-harness.spec.ts`; all 6 assertions green.  
- Script `scripts/lathe-jmdie-param-accuracy-harness.mjs` remains in repo; no changes.

**DECISIONS**  
- Adopted a two‑rung harness architecture: Rung A (ground‑truth cloud extraction) + Rung B (live roundtrip diff).  
- Decided to use CSS‑only SFM band for reference, discarding G97‑implied π·D·RPM/12 values as non‑physical.  
- Chose to enforce G50 cap and M30 in all generated programs; safety checks now mandatory before download.

**OPERATOR DIRECTIVES**  
- “Utilize workflow + parallel agents to do highly comprehensive closed‑loop training of print → lathe programs.”  
- “Prove 100 % accuracy of print→CNC programs for all lathe programs in the JM system.”  
- “Ensure our data is optimized.”

**FINDINGS/BUGS**  
- SFM agreement low (67 %) due to artifact‑contaminated reference band; corrected to ~82–88 % after CSS‑only adjustment.  
- Harness bug: `drill sfm_n=19` band mis‑classified, causing false positives.  
- Missing `thread` key in harness → thread ops fell back to G96 literal.  
- Vitest assertion failure on rung string mismatch (expected “B – …” vs actual).  
- G50 cap and M30 now 100 % compliant; canned cycles improved from legacy 32 % to 100 %.

**DOMAIN SPECIFICS**  
- **Engines/Dispatchers:** `turningPrintToProgramEngine`, `prism_turning_program:*` actions, `PipelineHarnessAdaptersEngine`.  
- **Metrics:** Feed (IPR) agreement, Safety correctness, SFM agreement.  
- **Paths:** Rung A harness (`audit-jm-die-lathe-corpus.mjs`, `lathe-jmdie-param-accuracy-harness.mjs`), Rung B harness (`lathe-roundtrip-accuracy-harness.ts`).  
- **Unique to galaxy:** JM Die corpus (118 customers, 16 558 programs), Okuma OSP fleet integration.

**TOOLS USED**  
- PRISM CLI helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
- Node scripts: `lathe-roundtrip-accuracy-harness.ts`, `audit-jm-die-lathe-corpus.mjs`.  
- Testing framework: Vitest (spec for Rung B harness).  
- Git tooling for slot claim and branch detection.

**OPEN THREADS**  
- Fix harness bugs (`drill sfm_n=19`, missing `thread` key) before re‑running Rung B.  
- Finalize CSS‑only SFM reference band and confirm empirical composition.  
- Apply workflow synthesis verdict: decide on PRISM tuning vs reference correction; implement chosen changes.  
- Re‑validate closed‑loop accuracy after fixes; target ≥90 % feed & SFM agreement, 100 % safety.
