# academy session 8b4b9149 (2026-06-10, 55.6MB, spine 589KB, 7 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5ae481f748`: EPERM data‑spine bug fixed – replaced atomicAppend with `fs.appendFileSync`, added retry & orphan cleanup (performance ↑2700×).  
- `86f0e3fe0c`: Wired 3 previously unwired engines (`SpeedFeedTriComparatorEngine`, `SpeedFeedExhaustiveCombinationEngine`, `SpeedFeedDownstreamSubscriberEngine`) into `calcDispatcher`; added 16 round‑trip dispatcher tests.  
- `a2dbfa76e1`: Closed‑loop driver rescued 7 untracked SFC engines; fixed consensus bug (exclude `aligned:false` externals).  
- `43e1b8e449`: G‑Wizard alignment symmetry added to tri‑comparator + regression test for misaligned advisory exclusion.  
- `891c66e728`: Full‑sweep comparison driver – 96 cells → 74 usable; updated material ISO mapping (Al→N, stainless→M).  
- `16d6eecef4`: SpeedFeedCalibrationPersistEngine foundation + unit tests; dispatcher wiring & round‑trip test.  
- `890b2c39e7`: VC‑assessment spec live‑results addendum.  
- `3438987f0e`: Engine hardening – clamped flag fix, `ledger_rows_skipped`, loud note.  
- `f31398a1a5`: SpeedFeedGpuJudgeEngine + driver + 11 tests (qwen2.5‑coder:32b on RTX 6000).  
- `f5d14ddb29`: GPU judge hardening – peer‑file absorption fix, matched_model surfacing, timeout handling, loud notes.  
- `951f5ac335`: Polish GPU judge – reference‑model surfacing, fetch timeout, silent‑exclusion note.  
- `3a1c20fca2`: Full sweep mode added to exhaustive engine; `runStreaming` generator; driver updated for `--mode full`; 5 new regression tests.  
- `af0ac16c5a`: Scrutiny fixes – `cells.length` ReferenceError, ISO‑group collapse test, doc comment cleanup.  
- `28eca0ed84`: Spec addendum documenting GPU judge findings.  
- `54c0ae06c8`: Spec doc‑reflection for full sweep & GPU judge.  
- `b847c0f1ff`: Scoping spec for axis‑awareness enhancement (tool material, coolant, holder, machine, spindle, controller, workholding, insert).  
- `658c8280fe`: Tool‑material axis – canonical speed‑factor table, explicit‑only safety guard; 10 tests.  
- `585584e3ae`: Coolant axis wired to existing `CoolantVcModifier`.  
- `7d0affcae6`: Rigidity‑to‑Vc factor moved to constants (`CANONICAL_MACHINE_RIGIDITY_VC_FACTOR`).  
- `f998f8af71`: Alts‑factor fix – hoisted tool‑material, coolant, rigidity multipliers into alternatives.  
- `da41a58fd1`: Full combinatorial sweep – 102 060 combos; metrics captured (Vc, rpm, MRR).  
- `U-OSC-VENDOR-COMPARE`: First real PRISM vs published baseline harness (Sandvik/Kennametal/HSMAdvisor).  
- `c48829e331`: Axis‑liveness probe – identified 8 live axes & 4 inert ones.  
- `d03458fff1`: Probe extended to include `tool_life`; runout has no effect on recommendation.  
- `2070c472a4`: Wired workholding clamp‑cap (`ClampingForceEngine`) – moves feed/MRR, safe‑direction only.  
- `7dc7798033`: Implemented spindle‑hp power clamp – reduces chip load beyond power envelope.  
- `8b191d5427`: Applied runout→life derate; `tool_life` now varies with holder type.  
- `a257f872bb`: Probe updated to cover `toolpath.strategy`, `cut_type`, `mode`; fixed phantom field bug.  
- `81e37ece16`: Added numeric target‑Ra finish axis – caps `fz` so predicted Ra meets user value.  
- `6a3ad56545`: U‑OSC‑COMPARE‑TOOLMATERIAL‑VARIABILITY – 576 tool‑material sweeps.  
- `c78faa5a73`: COMPARE‑HSS‑BASELINE – 54 HSS baseline points, 6 tests.  
- `835df42c74`: COMPARE‑CERAMIC‑CBN‑BASELINE – baseline entries for ceramic & CBN.  
- `4c544db4ae`: COMPARE‑PER‑VENDOR – explicit PRISM vs G‑Wizard/HSMAdvisor deltas (`baseline_detail`).  
- `08d7fc6d37`: ALL‑AXIS‑SWEEP – OAT × 2 regimes + factorial, 25 axes (20/25 live).  
- `93de130f59`: ALL‑AXIS‑TOOLLIFE – tool‑life liveness added → 18/25 live.  
- `a2ec922ca2`: CONTROLLER‑FEATURES FIX – controller smoothing now applied in prism_optimized.  
- `dde2b56ac7`: target_ra finish‑Ra bind.  
- `a8f72823cb`: U‑OSC‑RUNOUT‑LIFE DERATE (single‑source runout TIR derate).  
- `73b97ef25f`: U‑OSC‑HOLDER‑RUNOUT‑DEDUP – double‑count regression fixed.  
- `e4d6c00fa8`: doc‑reflect – regression entry + lesson memory.

**DECISIONS**  
- Adopt *back‑end first*: fix data‑spine, wire engines, then run closed‑loop vendor comparison.  
- Replace read‑rewrite‑rename append with atomic file append + bounded retry (performance ↑2700×).  
- Expose dispatcher actions for tri‑comparator & exhaustive combination; use lazy imports and `zod` schemas.  
- Use ultracode workflow orchestration for multi‑agent tasks, ensuring token budgets & anti‑pattern compliance.  
- Exclude `aligned:false` externals from consensus unless they are sole external (R7 middle path).  
- Add G‑Wizard tool‑diameter alignment check mirroring HSMAdvisor logic.  
- Correct ISO mapping: aluminum → N, stainless → M; populate N and H groups.  
- Persist calibration factors as advisory‑only model (no auto‑apply).  
- Use RTX 6000 Blackwell + 128 GB RAM + NVMe for GPU‑resident LLM judge (`qwen2.5‑coder:32b`).  
- Expand exhaustive sweep to full SFC app‑page input space (~69 k combos) via `full` mode & streaming API (`runStreaming`).  
- Replace 50‑cell cap with generator to avoid OOM/reaper limits.  
- Add GPU judge engine (`SpeedFeedGpuJudgeEngine`) that probes residency, surfaces `matched_model`, enforces fetch timeout, emits loud notes for silent exclusions.  
- Document inert axes (coolant, holder, machine/spindle/controller/workholding/insert) and ISO‑group material collapse; prioritize tool‑material physics next.  
- Vendor calibration OCR batch: Blackwell vision‑OCR over 287 catalog PDFs (`qwen2.5‑VL`).  
- Resolve G‑Wizard crib geometry‑only issue – adjust comparator logic accordingly.

**OPERATOR DIRECTIVES**  
- “Continue from where you left off.”  
- “Reorientate with previous real sessions of oscar and continue where we left off.”  
- “Give full path for assessment” (already answered).  
- Run calculations for every possible combination of machines, spindles, controller, materials, work‑holding/fixture, tool holder connection type & holding mechanism, coolant type, toolpath strategy, cut type, finish Ra, roughing/semifinish.  
- Populate computed S/F per tool in G‑Wizard UI before comparison can complete.  
- Run Blackwell `qwen2.5‑vl` vision‑OCR batch over 287 catalog PDFs for richer baseline.  
- Auto‑fix any bug discovered during a task immediately.  
- Coordinate UI design with Quebec (Apple‑iOS feel, haptics).  

**FINDINGS/BUGS**  
- EPERM leak in `OutcomeCaptureBusEngine` caused orphan temp files; fixed.  
- Three engines were dispatcher‑unreachable; wired now.  
- G‑Wizard tool selection defaults to first crib tool when no selector provided; all 41,210 tools have NaN surface speed → vendor cannot contribute without operator input.  
- HSMAdvisor returns constant 634.6 m/min for every material and flags alignment as false; engine marks it unaligned but still includes in consensus median (bug).  
- `SystemOpinion.vc_mpm` nested under `axes`; test assertions incorrectly referenced top‑level field.  
- Consensus folded in explicitly unaligned HSMAdvisor cut (634 m/min for Ti), producing impossible consensus values.  
- Misalignment bug: HSMAdvisor cut flagged `aligned:false` but still used as data point.  
- G‑Wizard crib geometry‑only → no speed/feed baseline available; comparison yields 0 matched cells.  
- Under‑speeding vs published baselines (~25 % mean, −53 % worst).  
- Vendor catalog PDFs require vision‑OCR due to font encoding; plain text extraction fails.  
- PRISM over‑speeds HSS cast iron by +108 %, CBN hardened steel by +49 %; under‑speeds ceramic by –49 %.  
- Several axes (machine_accuracy, controller_brand, target_ra, radial_pct) inert or mis‑classified; clarified by‑design vs wiring gaps.  
- Radial_pct duty‑cycle tool‑life correction deferred (safety‑critical physics decision pending).  

**DOMAIN SPECIFICS**  
- **Engines / Actions**: `OutcomeCaptureBusEngine`, `SpeedFeedTriComparatorEngine`, `SpeedFeedExhaustiveCombinationEngine`, `SpeedFeedDownstreamSubscriberEngine`, `GWizardAdapterEngine`, `HSMAdvisorAdapterEngine`, `SpeedFeedCalibrationPersistEngine`, `SpeedFeedGpuJudgeEngine`, `UltimateSpeedFeedEngine`, `NineAxisOrchestratorEngine`, `ClampingForceEngine`.  
- **Dispatcher**: `calcDispatcher` (z.enum + case handlers for SFC actions).  
- **Metrics / Fields**: Vc, rpm, spindle_rpm.unit, stability_lobe_analysis outputs, `ledger_rows_skipped`, `matched_model`, `gpu_resident`, `vram_used_mib`, `tool_life_min`, `feed_rate_mmmin`, `ap`, `AE_pct`, `MRR_cm3min`, `cost_per_part_usd`, `Ra`.  
- **Paths**: `/checkin-oscar` wrapper, `slot-bind-enforce.mjs`, `hermes-workflow-planner.mjs`, `forge-audit-v2`, `sfc-full-sweep-compare.mjs`, `runStreaming`, `sfc-orchestrator-axis-liveness.ts`.  
- **Axis‑Awareness**: tool_material, coolant, rigidity, machine.max_rpm, tool_holder.balance_class, spindle.hp (after clamp), workholding.type (after clamp), tool_holder.type→life, toolpath.strategy, cut_type, mode, target_ra_um.  

**TOOLS USED**  
- PRISM core tools (`/hermes-workflow`, `/forge-audit-v2`, `/checkin`).  
- Ultracode workflow orchestration (parallel agents, token budgeting).  
- Node.js + tsx for TypeScript execution; `vitest` & `jest` test harnesses.  
- Ollama (`qwen2.5‑coder:32b`, `qwen2.5‑vl`) for GPU LLM inference and vision‑OCR.  
- nvidia‑smi for GPU monitoring.  
- CronCreate `/loop` for recurring prompts.  
- Git hooks: `slot-commit-enforce`, `slot-bind-enforce`.  
- Scrutiny gate (`scrutiny-3way.mjs`) with physics reviewer & independent reviewer.  

**OPEN THREADS**  
1. **Controller‑on‑default** – decide whether to expose controller capability in default recommendation.  
2. **Live G‑Wizard / HSMAdvisor numbers** – operator must run closed calculators and export computed speeds or populate G‑Wizard crib.  
3. **Richer baseline OCR batch** – Blackwell vision‑OCR over 287 catalog PDFs; requires multi‑hour GPU job.  
4. **Comparison sweep tool_material variability** – extend tri‑vendor sweep to iterate over HSS, ceramic, CBN in addition to carbide.  
5. **Fix coolant effectiveness ceiling** (≥1.40) and resolve TSC double‑count for through_tool coolant type.  
6. **Radial_pct duty‑cycle tool‑life correction** – deferred; safety‑critical physics decision pending fresh context.  
7. **Document inert axes** (`machine_accuracy`, `controller_brand`) and clarify by‑design vs wiring gaps.  
8. **UI design** – Quebec‑led; no further action in this slot.
