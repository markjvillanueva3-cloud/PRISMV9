# academy session 0d351f43 (2026-06-09, 0.8MB, spine 15KB, 1 slice(s), model gpt-oss:20b)

**DECISIONS**  
- Source = P: (mirror of H:) → copy additive (/E) with excludes.  
- Excludes: caches, Docker/WSL, `.claude` worktrees, SteamLibrary, Tools, Claude profile copies; keep PRISM core + prism‑backups.  
- Final payload ≈ 1.2 TB (≈ 1038 GB PRISM + 42 GB backups).  
- Switch from robocopy via Box Drive to rclone direct‑to‑Box streaming because C: cache wall prevents bulk copy.

**OPERATOR DIRECTIVES**  
- Confirm the Box account has ≥ 1 TB free and supports files > 150 GB.  
- Run `rclone authorize box` (follow browser prompt, paste token).

**FINDINGS/BUGS**  
- C: drive only 12.3 GB free; Box Drive pins uploads, never evicts → robocopy would crash.  
- robocopy fails scanning destination (`ERROR 31`) due to > 4 M online‑only files.  
- Excluding worktrees cuts file count by ~2.6 M but bytes drop only 155 GB; bulk is PRISM core.  
- SteamLibrary (~141 GB) and Tools (~175 GB) are non‑PRISM content, should be excluded.

**DOMAIN SPECIFICS**  
- PRISM H Drive layout: `PRISM` (core repo + .git), `prism-backups`, `.claude` worktrees, credential dirs.  
- Worktree detection via presence of `.git`; nested worktrees dropped.  
- Box Drive mount at `C:\Users\wompu\Box`; uses Windows Cloud Filter API; no automatic de‑hydration.

**TOOLS USED**  
- PowerShell & Bash for drive discovery.  
- robocopy (dry run `/L`) for sizing and exclude testing.  
- `attrib +U` attempted eviction.  
- rclone (planned) for direct Box streaming.

**OPEN THREADS**  
- Execute rclone copy with the curated exclude set.  
- Monitor C: free space during transfer; ensure no overflow.  
- Verify Box quota & per‑file limits before starting.  
- Validate backup integrity after completion.  
- Consider chunked copying + pause/resume if needed.
