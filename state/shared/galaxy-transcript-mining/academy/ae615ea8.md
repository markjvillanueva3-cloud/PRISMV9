# academy session ae615ea8 (2026-06-10, 29.7MB, spine 191KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- SIM‑4 bind gate module & tests (`7fc028fc1e`) – machine + controller identity, VR‑not‑NGC assertion, 25.4× unit refusal.  
- SIM‑5 run‑complete detection module & tests (`51699e483e`).  
- SIM‑6 assessLiveRunClearance extended with bind/runComplete gates; dispatcher updates and schema registration (`f3ca55e928`, `b5886a26e2`).  
- SIM‑7 fleet rollup module & tests (`350f62bc0d`, `f380fd7155`) – `planFleet()` over 15 JM machines.  
- U‑CIMCO‑SIM‑1A read‑report driver (parts 1–2: `01c53f6872`, `679565fcb5`).  
- Echo Forge Roadmap v2 spec (`1a09e0b961`).  
- U‑CIMCO‑FSM‑LIVE‑DRIVE orchestrator (`dba50eb3da`) – `navigateLive → run Machine Sim → poll read‑report → assessReadReport`.  
- CLEARANCE‑COMPOSE (`composeClearanceInput`) wiring to 5‑gate dispatcher.  
- CLOSED‑LOOP‑STATUS doc (HTML + JSON witness).  
- Single‑process `invoke‑read` op with `--then "Simulate"` (`40cf2e0d3b`).  
- Fleet‑drive script (`cimco-fleet-drive.mjs`) & all‑15 sweep results (`fleet-drive-results.json`, `a28927fc0b`).  
- Witness JSON for invoke‑read envelope (`43e5309554`).  
- Supporting commits: `e482189325` (resumable driver + header‑only fix), `59fa8456f5` (scrutiny fixes).  
- Pre‑configure sim commit (`e276f13216`); root‑cause & tailoring spec (`af005aa2d7`); doc clarity fix (`c05b697d77`).  
- Recon confirming load‑machine build needs two caps (`1672656ada`).  
- Read‑window crash finding (`9a3d782ae5`).

**DECISIONS**  
- Adopt *Ollama pre‑flight* + 3‑of‑3 Claude scrutiny; switch to `qwen2.5-coder:32b`.  
- Critical path: **U‑CIMCO‑FSM‑LIVE‑DRIVE** – operator must open CIMCO Edit on VMC‑01.  
- Use shared‑tree guard (`git commit --only`) to avoid torn commits.  
- MSAA over UIA for headless enumeration (>2000 controls, cold launch).  
- Build single‑process `invoke‑read` to eliminate two‑process attach gaps.  
- Reaper‑safe PowerShell sweep (`cimco-fleet-sweep.ps1`).  
- Incremental flush + resume cursor in fleet driver.  
- Treat header‑only report grids as non‑clearing verdict (`report-header-only`).  
- Header‑only reads caused by missing machine envelope & stock geometry → load `.mcfg` and add stock.  
- Adopt `--pre <cfg‑ctl>` pre‑configure sim; cross‑slot collaboration via “Configure Machine Type” ribbon.  
- Next build: `--load-machine <mcfg>` requiring MSAA set‑edit-field-value + read‑arbitrary-window.  
- Harden read‑window against unmanaged crash.

**OPERATOR DIRECTIVES**  
- Start **U‑CIMCO‑FSM‑LIVE‑DRIVE** (open CIMCO Edit on VMC‑01).  
- Run byte‑golden CI or other phases only if time permits; priority is live loop.  
- Re‑register PRISM Blueprint OCR Batch & PRISM Zombie Reaper v2 if desired.  
- “Complete all cimco tasks, utilize ollama for searches, read/write/coding.”  
- “Begin closed‑loop testing of post processors with CIMCO as editor.”  
- “Test on all JM fleet machines; /yolo‑mode: build posts for highest selling machines.”  
- Ensure simulation add‑on activated and CIMCO settings tailored.

**FINDINGS/BUGS**  
- Torn commit fixed with `git commit --only`.  
- Missing `read-report` op added to C# driver.  
- Unit tests resolved report‑grid‑not‑found & empty array bugs.  
- `gpt‑oss:120b` returned empty; switched to `qwen2.5-coder:32b`.  
- MSAA driver can launch cold autonomously (no operator needed).  
- Reaper kills node after second launch → orphan `PrismCimcoUI.exe`; fixed by reaper‑safe sweep.  
- Header‑only report grids mislabeled; corrected to `report-header-only`.  
- Two‑process invoke‑read unreliable; solved with single‑process op.  
- Simulation add‑on settings not configured (collision checking, stock/workpiece/fixture, machine type).  
- Header‑only reads due to missing envelope & stock geometry.  
- Path‑quoting bug in `build.ps1` `/out:` argument.  
- Pre param collision → renamed to `preCtl`.  
- Read‑window crash from unmanaged MSAA provider fault on arbitrary window walk.  
- Units mismatch: `.mcfg` mm vs JM inch (25.4×).

**DOMAIN SPECIFICS**  
- SIM‑4 bind gate: machine+controller identity, VR‑not‑NGC assertion, 25.4× unit refusal, EDM route.  
- SIM‑5 run‑complete: terminal completion, report quiescence, line/block coverage, modal watcher.  
- SIM‑6 assessLiveRunClearance: 5‑gate combiner (machineBound, machineUnitsKnown, kinematicsVerified, simClearedForLiveRun, runComplete).  
- SIM‑7 fleet rollup: `planFleet()` dispatch over 15 JM machines → drive‑ready status.  
- Post‑processor engines: MasterPostProcessorAIEngine, roadmap to activate `prism_pp` dispatcher (8 stub + ~14 AGI‑tier).  
- PrismCimcoUI.exe driver ops: `invoke-read`, `FireControl`, `navigateLive`.  
- driveLiveFsm chain: `navigateLive → run Machine Sim → poll read-report → assessReadReport`.  
- Fleet‑drive script (`cimco-fleet-drive.mjs`) with incremental flush & resume cursor.  
- Single‑process `invoke‑read` uses `--name "Machine Simulation" --then "Simulate"`.  
- Report grid: table `r24`, columns “Start Time (Line)”, “Type”, “Message”, “Action”; header row `r25`; data rows `r28`.  
- `composeClearanceInput` wiring to 5‑gate dispatcher.  
- `cimco-fleet-sweep.ps1` reaper‑safe sweep.

**TOOLS USED**  
- `/checkin-echo`, `/startup-echo`, `/loop`, CronCreate.  
- Ultracode workflow orchestration.  
- Ollama (`qwen2.5-coder:32b`).  
- Git, Node test framework, PrismCimcoUI.exe driver, `csc.exe` compiler.  
- PRISM chat‑slots helper (`chat-slots.mjs`).  
- `/startup` pipeline.  
- `ask-ollama.mjs`.  
- Node scripts: `cimco-fleet-drive.mjs`, `cimco-sweep.ps1`.  
- Git commit hooks `[BOOTSTRAP-SLOT-ENFORCE]`.  
- Unit tests (69 driver + 18 normalizer).  
- Status doc generation.

**OPEN THREADS**  
- Operator‑gated live E2E on VMC‑01 (open CIMCO Edit).  
- Full implementation of **U‑CIMCO‑FSM‑LIVE‑DRIVE**.  
- Activation of dark `prism_pp` dispatcher & related post‑processor engines.  
- Load `.mcfg` machine configurations per JM machine.  
- Create known‑bad NC for collision‑row extraction.  
- Per‑machine production NCs and inch‑vs‑mm validation.  
- Tailor simulation add‑on settings: enable collision checking, stock/workpiece/fixture, configure machine type, set stop conditions.  
- Build single‑process `invoke‑read` with pre‑step for enabling settings.  
- Integrate `/yolo-mode` tasks (build posts for highest selling machines).  
- Hardened read‑window targeting specific dialog hwnd with defensive COM wrapping.  
- Implement set‑value capability to type `.mcfg` path into file dialog.  
- Build `--load-machine <mcfg>` combining above capabilities; wire via `jm-fleet-sim-map.json`.
