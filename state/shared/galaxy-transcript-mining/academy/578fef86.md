# academy session 578fef86 (2026-05-23, 8.2MB, spine 40KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `60009927bc` – PWA shell for the Academy:  
  - `mcp-server/web/public/manifest.webmanifest` (4 shortcuts, theme & background colors)  
  - Icons: `icon-192.svg`, `icon-512.svg`, `icon-maskable-512.svg`, `apple-touch-icon.svg`  
  - Service‑worker `sw.js` (cache‑first for shell/assets, network‑first for `/api/+/mcp/`, fallback to `index.html`)  
  - `src/lib/registerServiceWorker.ts` (production guard, update event dispatch)  
  - Updated `index.html` and `src/main.tsx` to register the SW.  
- Commit of `src/__tests__/registerServiceWorker.test.ts` – three Vitest cases validating SW registration logic.

**DECISIONS**  
- Adopt a **PWA‑first** delivery vector (browser installable, no Capacitor wrapper).  
- Service‑worker caching strategy:  
  - Shell/assets → cache‑first.  
  - `/api/+/mcp/` → network‑first.  
  - All other requests → network‑first with `index.html` offline fallback.  
- Namespaced localStorage key for academy progress (`useStudentId()` from `AuthContext`) to avoid cross‑worker overwrite on shared tablets.  
- Keep the existing Academy page untouched; only add PWA layer.  

**OPERATOR DIRECTIVES**  
- Session‑scoped Stop hook: **“make expansive upgrades to the prism app training academy. | get it setup for phone (ios and android) so workers can start utilizing it soon.”**  
- Recurring `/goal` cron every 5 min (`*/5 * * * *`) to drive multi‑session progress.  
- After `/checkin-lima`, continue iter 2 (auth namespacing) and defer iter 3 (content expansion) & iter 4 (docs surface).  

**FINDINGS/BUGS**  
- `pick-unit.mjs`’s `SLOT_TO_CHAT` map only covers alpha–golf → NaN for lima. Workaround: use `priority‑queue.mjs --slot lima`.  
- `registerServiceWorker.ts` was truncated; rewritten fully.  
- Test file initially placed in `src/lib/`; moved to `src/__tests__/` per Vitest config.  
- Git lock from a crashed peer process; removed after >60 s and committed the test file.  

**DOMAIN SPECIFICS**  
- Core engines: `CurriculumEngine`, `AssessmentEngine`, `LessonRendererEngine` (knowledgeDispatcher.ts lines 1735‑1815).  
- Slot system: 26 NATO slots, with **lima** dedicated to the Academy domain.  
- CronCreate used for recurring slash commands (`/goal`).  
- Loop state tracked via `.claude/helpers/loop-state.mjs`.  
- PWA assets live under `mcp-server/web/public`; SW code in `src/lib/registerServiceWorker.ts`.  

**TOOLS USED**  
- `/startup-lima`, `/checkin-lima` (slot‑binding wrappers).  
- `/loop`, `/goal`, `/compact` pipelines.  
- Per‑agent handoff (`per-agent-handoff.mjs`) with `stable-session-id.mjs`.  
- Git helpers: `chat-slots.mjs`, `milestone-tracker.mjs`.  
- CronCreate for scheduling.  
- Vitest for unit tests.  

**OPEN THREADS**  
1. **Iter 3 – Academy content expansion (78.9 KB)** – deferred to next `/loop` tick; requires its own session window due to size and potential peer‑collision.  
2. **Iter 4 – Documentation surface** – pending after iter 3 completion.  
3. **Retroactive 3‑of‑3 scrutiny on commit `60009927bc`** – still awaiting final gate approval.  
4. **Fix stale `SLOT_TO_CHAT` map in `pick-unit.mjs`** – to be committed once the patch is ready.  
5. Re‑arm `/goal` cron and `/compact` guard after next compaction (not yet executed).
