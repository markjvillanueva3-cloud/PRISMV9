# academy session 10f4a625 (2026-06-03, 7.9MB, spine 92KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- None reported in the transcript.  

**DECISIONS**  
- Golf slot now owns fleet‑reaper (moved from alpha on 2026‑05‑16).  
- `/checkin‑golf` always runs fleet‑reaper; no `--golf` flag passed to `chat-slots.claim`.  
- Legacy hygiene allowlist hook (`golf-slot-write-allowlist.mjs`) must be bypassed or disabled for golf work.  

**OPERATOR DIRECTIVES**  
- Force‑takes the **golf** slot (evicting any prior owner with `--force true --confirmRecent true`), binds handoff to `golf-work`, always runs fleet‑reaper.  
- Do NOT pass the `--golf` flag to `chat-slots.claim`; golf is a normal work slot.  
- Execute a single fleet‑hygiene monitoring pass every 5 min (slot “golf”).  
- Limit any anomaly investigation to ≤1 extra step per R10 drift discipline.  

**FINDINGS/BUGS**  
- Crash‑frozen count spiked to **17** (from ~2–4) due to heartbeat lag from a batched prompt burst.  
- Reaper reaped **0** orphans; backbone healthy (`Ready/0x0`).  
- Stale slots: **16**, claims: **0** – no action required beyond nominal reporting.  
- Guardian probe race: `golf-slot-reaper-guardian.mjs` reports “NOT REGISTERED” when fleet‑reaper is in `State:Running`; root cause timeout or non‑zero exit on `schtasks /Query`.  
- MCP daemon flapping (~10–15 min cadence): port 3100 disconnects (timeout, ECONNREFUSED) auto‑restart via watchdog; no fleet impact but warrants backend investigation.  
- Reaper scheduled run errors: intermittent `LastResult=0x1` or `0x41306` observed when MCP restarts; manual sweeps (`fleet-reaper-sweep.mjs --once`) always clean.  
- Node‑process noise: transient spikes in total node count (up to ~199) are short‑lived and not orphaned; no leak detected.  

**DOMAIN SPECIFICS**  
- **Engines/Actions:** `fleet-reaper` (orphan‑process janitor, GPU coordinator, Ollama hint emitter), `chat-slots`, `slot-task-claim`.  
- **Dispatchers/Metrics:** `staleSlots`, `crashWatch`, `reclaimedBytes`, `gpu.freeMb/utilizationPct`, `ollama.reachable/loaded[]`, `coordinator.hintMode`.  
- **Unique Paths:** `/checkin‑golf` pipeline, `golf-slot-reaper-guardian.mjs`, `golf-slot-write-allowlist.mjs`.  

**TOOLS USED**  
- PRISM helpers:  
  - `rtk node H:/prism/.claude/helpers/chat-slots.mjs reclaim`  
  - `rtk node H:/prism/.claude/helpers/slot-task-claim.mjs list`  
  - `rtk node H:/prism/.claude/helpers/loop-state.mjs tick`  
  - `rtk node H:/prism/scripts/fleet-reaper-sweep.mjs --once --json`  
- PowerShell: `schtasks /Query /TN "PRISM Fleet Reaper" /FO LIST | Select‑String ...`  
- Git status checks (`git status | head -5`).  
- Unix utilities (`tail`, `head`).  

**OPEN THREADS**  
- Implement a unit to fix the guardian probe race (`U-GUARDIAN-PROBE-RUNSTATE-RACE`).  
- Investigate MCP daemon restart loop (RSS pressure vs crash).  
- Consider improving allowlist hook bypass logic for golf.  
- Optional: refine reaper scheduled‑run error handling to avoid false `LastResult` flags.  
- Monitor heartbeat lag resolution; expect crash‑frozen count to normalize.  
- Continue 5‑min cadence monitoring; no new anomalies reported.
