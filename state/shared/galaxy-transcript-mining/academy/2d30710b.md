# academy session 2d30710b (2026-05-18, 23.8MB, spine 69KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑CK05` – mirror‑gen engine (`.claude/kernel/mirror-gen.mjs`) + 29 tests (commit 904de5196b).  
- `U‑CK08` – command‑migrate‑runner (`.claude/scripts/command-migrate-runner.mjs`) + 24 tests (commit 649dfc4f78).  
- `U‑CK26` – full producer build spec for psk record (`U‑CK26‑PRODUCER‑BUILD‑SPEC.md`, commit 3‑file change).  
- `U‑SLOT‑BIND‑ENFORCE` – deterministic slot‑bind hook (commit 679feae088).  

**DECISIONS**  
- Adopt a UserPromptSubmit hook (`slot-bind-enforce.mjs`) that reads the harness `session_id` from stdin to force‑claim slots, eliminating model‑copying bugs.  
- Flip envelope status of `COMMAND-KERNEL-MS0` from `not_started` → `in_progress`; mark completed units (`U‑CK05`, `U‑CK08`, etc.) as `complete`.  
- Use a 10 min cron job (`87b293af`) for autonomous `/loop` execution; schedule only when interval < 60 m and no daily phrasing.  
- Enumerate CK26 fully (producer, telemetry) instead of partial build to avoid re‑derivation in subsequent iterations.  
- Skip rebuilding existing telemetry data (`psk record`, `pipeline‑telemetry.jsonl`) – rely on R8 “check before building”.  

**OPERATOR DIRECTIVES**  
- `/loop — schedule a recurring or self-paced prompt` → parsed as `[10m] build everything in logical order /goal`.  
- `/startup-hotel help charlie with os kernel. /loop [10m] build all os kernal units /goal`.  
- `build everything in logical order /goal` (repeated).  
- `pull all tasks that never got completed from all previous sessions attached to your slot`.  

**FINDINGS/BUGS**  
- RTK token‑killer silently dropped staged files for U‑CK05; bypassed with raw git.  
- Peer absorption of pipeline.md + envelope flips (commit e484539c0f).  
- Slot drift: chat ID `93351de7` used instead of live `claude-2d30710b`, causing hotel to be unbound and CK09 claimed by peer.  
- `/checkin‑hotel` wrapper relied on model copying a placeholder ID; after /compact it copied stale summary ID → slot never forced.  
- `stable-session-id.mjs` fallback returned peer’s cached session (`9876118b`) when invoked via Bash post‑/compact.  
- Cron job is session‑only; will die if chat exits, requiring re‑issuance after /compact.  

**DOMAIN SPECIFICS**  
- Milestone: `COMMAND-KERNEL-MS0` (29 units across phases P0–P4).  
- Engines/actions: `mirror-gen` (`SchemaCoverageAuditEngine`, `command-migrate-runner`).  
- Dispatchers: slot‑task‑claim, loop‑state, per‑agent handoff.  
- Metrics: psk record, pipeline‑telemetry.jsonl, adaptive‑thresholds.mjs.  
- Paths: `knowledge/wiki/os/…`, `state/shared/os-mirrors/`, `mcp-server/data/milestones/COMMAND-KERNEL-MS0.json`.  

**TOOLS USED**  
- PRISM helpers: `/checkin-hotel`, `/startup-hotel`, `/loop`, `CronCreate`, `slot-task-claim.mjs`, `stable-session-id.mjs`, `per-agent-handoff.mjs`.  
- Scripts: `audit-docker-files.mjs`, `audit-memory-dbs.mjs`, `md-to-html.mjs`.  
- Engines: `.claude/kernel/mirror-gen.mjs`, `mcp-server/src/engines/SchemaCoverageAuditEngine.ts`, `command-migrate-runner.mjs`.  
- Hook: `slot-bind-enforce.mjs` (UserPromptSubmit).  

**OPEN THREADS**  
- Remaining COMMAND‑KERNEL-MS0 units: CK09 (peer claimed, pending), CK11 (hotel claimed but depends on CK09), CK14–16, CK26–29.  
- Need to finish CK27–29 and then CK09 after peer release.  
- Ensure cron job persists across `/compact` or re‑issue loop after compaction.  
- Verify that the new slot‑bind hook correctly handles future `/checkin‑<slot>` invocations under all circumstances.
