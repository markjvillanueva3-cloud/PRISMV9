# academy session 396bc735 (2026-05-19, 24.3MB, spine 489KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `817c95ba72` – wired `backend‑dev` as 6th DOMAIN_MAP entry; added R12 `--domain` validator; created `retag‑tribal-backend-dev.mjs` (34 retags); tests updated (40/40 inject + 24/24 retag).  
- `c346fe51b2` – iter 1: 6 high‑ROI wikis committed.  
- `a8f3475028` – iter 2: +23 retags, +2 wikis.  
- `d9f1b7960f` – iter 3: +5 retags, +9 wikis.  
- `d716d20a96` – iter 6: recovered 3 cancelled wikis (LoRA, RL, MCP‑tool).  
- `e190747c7d` – lima worktree commits for 3 operator‑pain‑point wikis.  
- `dc3f2328b7` – merged `slot/lima → cad‑fusion‑live‑ms0`; unblocked 22 wikis + watchdog.  
- `f2e4e98b` – cron job (every 5 min) to run `/loop [5m] continue high value wiki and obsidian usage`.  
- Wiki docs added:  
  - `parallel-tool-call-discipline.md` (105 L)  
  - `fleet-coordination-discipline.md` (149 L)  
  - `cron-and-scheduled-task-discipline.md` (164 L)  
  - `commit-message-conventions.md` (187 L)  
  - `prism-self-update-loop.md` (163 L)  
  - `recall-injection-flow.md` (178 L)  
  - `bash-vs-powershell-on-windows.md` (219 L)  
- 17 new wikis in `software‑engineering/` (schema‑read, git‑shared‑index, hook‑authoring, subagent‑orchestration, parallel‑tool‑call, fleet‑coordination, cron‑and‑scheduled‑task, commit‑message, prism‑self‑update, recall‑injection, bash‑vs‑powershell, obsidian‑vault, dispatcher‑action, engine‑creation, html‑companion, jsonl‑ledger, loop‑state‑tracking).  
- 1 wiki improvement: `wiki‑automation‑discipline.md`.  
- 3 memory entries: feedback_autonomous_loop_drift_discipline, reference_wiki_recall_index_stale_2026‑05‑18, feedback_scan_lived_signals_for_wiki_gaps.  
- Cron job `ae7b42d1` cancelled; ~30 duplicate cron triggers removed.

**DECISIONS**  
- Place `backend-dev` last in DOMAIN_MAP to keep manufacturing precedence.  
- Add R12 domain validator to `tribal-rerank.mjs`; emit JSON error on unknown domain.  
- Create idempotent retag script (`retag‑tribal-backend-dev.mjs`) with atomic write/try/catch.  
- Commit new wikis via lima worktree; merge manually after user approval (option A).  
- Wire watchdog into Stop chain (`settings.json:527`, 3000 ms) to auto‑regen stale indices.  
- Keep `/loop` interval at **5 min** for high‑value wiki production; no cloud schedule unless ≥60 min or daily phrasing.  
- For Cloud schedule: invoke `schedule` skill directly, not CronCreate.  
- Use `git add -- <file>` pathspec clamp to avoid peer sweep during bulk commits.  
- Defer commit of `fleet-coordination-discipline.md` until lock contention clears; apply patient‑retry drift discipline.  
- Adopt loop‑state tracking (`loop-state.mjs start/tick/end`).  
- Cancel duplicate cron triggers to eliminate drift.

**OPERATOR DIRECTIVES**  
- `/loop [5m] continue high value wiki and obsidian usage` – schedule recurring loop; no further prompts.  
- Verify all 61 `backend-dev` wikis in `_leaf-index.jsonl`; run recall test for auto‑injection.  
- “Make sure everything you’re building is automated or it will sit stagnant.” (PRISM automation).  
- Prompt operator: *Cloud schedule* vs *Session only*; explain persistence implications.

**FINDINGS/BUGS**  
- `karpathy-12-rule-discipline.md` missing from leaf index → recall test empty.  
- Recall test for entire wiki set fails (empty result).  
- Embeddings stale (~89 h) due to Ollama bootstrap delay; watchdog will trigger next Stop cycle.  
- `index.lock` contention: size 0 + mtime > 60 s; resolved via patient retry.  
- Pathspec clamp mis‑interpretation during bulk commit; verified with `git show --stat`.  
- Drift discipline capped to 1 extra tick after >1 iteration swallow.  
- ~30 duplicate cron triggers fired → redundant loops.  
- Master‑index stuck on same 5 results → stale recall index.  
- Cron fired before session closed due to misconfiguration.

**DOMAIN SPECIFICS**  
- `tribal-by-domain-inject.mjs` – token→domain mapping, last‑match precedence.  
- `tribunal-rerank.mjs` – cosine top‑K with 2× in‑domain boost; `--domain` validator.  
- Retag script – regex `BD_KEYWORD_RE`, scoring, classification, plan/apply logic.  
- `wiki-precheck-inject.mjs` – BM25 + semantic recall over `_leaf-index.jsonl` & `_embeddings.jsonl`.  
- System‑viz post‑commit hook – triggers index regeneration and Obsidian feed.  
- Slot‑worktree model (`H:/prism-slot-lima`) – isolated commits, `git -C <path>` workaround.  
- Watchdog – Stop chain hook that checks for stale indices and forces regen.  
- PRISM pipeline stages: WRITE → SYSTEM‑VIZ → WIKI INDEXES → OBSIDIAN VAULT.  
- Engines/dispatchers: CronCreate, Windows Scheduled Task, ScheduleWakeup, Git hooks (`post‑commit`, `worktree‑commit‑route`).  
- Metrics: lock contention duration, commit size, wiki line counts, recall surface freshness.  
- Paths: `knowledge/wiki/software-engineering/*`; `H:/prism/.git/index`; `C:/Users/.../memory/*.md`.  
- Scheduling primitives: cron, scheduled tasks, loop-state tracking.  
- Recall injection flow & user prompt submit chains.  
- Dispatcher action design (`ACTION_MAP`).  
- Engine creation discipline pre‑checks.  
- Wiki automation discipline (freshness thresholds).  
- JSONL ledger conventions for persistence.

**TOOLS USED**  
- Node scripts: `chat-slots.mjs`, `tribal-rerank.mjs`, `retag‑tribal-backend-dev.mjs`, `loop-state.mjs`.  
- Git commands: `git -C`, commit, merge, add -- <file>.  
- CronCreate (session‑only, 5 min).  
- `schedule` skill for cloud scheduling.  
- Precompact, `/compact` for handoff.  
- AskUserQuestion interface.  
- Skill({skill:"schedule", …}) tool.

**OPEN THREADS**  
1. Run full recall test after watchdog triggers to confirm all 61 wikis surface.  
2. Monitor Ollama bootstrap; ensure `_embeddings.jsonl` refreshed within next Stop cycle.  
3. R8 dedup‑preflight: verify no new wiki duplicates existing entries.  
4. Confirm cron job persists after session restarts (cloud schedule if long‑term).  
5. Integrate recall‑injection flow into main PRISM awareness loop.  
6. Verify all new wikis indexed and discoverable in OBSIDIAN.  
7. Clean up stale `index.lock` entries post‑commit.  
8. Refresh stale recall index / master‑index.  
9. Expand coverage beyond current domains.  
10. Implement remaining wiki improvements (e.g., host‑memory‑no‑op).  
11. Investigate root cause of duplicate cron triggers.
