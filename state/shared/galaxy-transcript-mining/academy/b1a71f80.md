# academy session b1a71f80 (2026-06-09, 0.7MB, spine 8KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Updated global `~/.claude/settings.json` (mirrored to `H:`) with:  
  - `"model": "fable"` (was `"opus"`)  
  - `"ultracode": true`  
  - `"enableWorkflows": true`  
  - `effortLevel` remains `"xhigh"`

**DECISIONS**  
- Default model switched to **Fable** per user request.  
- Enabled **Ultracode** and workflow orchestration, assuming Fable supports `xhigh`.  
- If Fable lacks `xhigh` capability, fallback to **Opus** for ultracode runs.

**OPERATOR DIRECTIVES**  
- User asked: “make the default model fable ultracode.”  
- Assistant applied changes; noted that current session remains Opus until `/model` is invoked.

**FINDINGS/BUGS**  
- Unverified whether Fable supports `xhigh` effort / Ultracode.  
  - If unsupported, Ultracode will silently no‑op (no “ultracode is on” reminder).  
- Current session still uses Opus; changes take effect only on new sessions.

**DOMAIN SPECIFICS**  
- **Settings files & scope:** `~/.claude/settings.json` (global), `.claude/settings.json` (project), `.claude/settings.local.json` (local).  
- **Hook events:** PreToolUse, PostToolUse, PreCompact, PostCompact, Stop, Notification, SessionStart.  
- **Permission syntax:** exact match, prefix wildcard, tool‑only; arrays must be merged, not replaced.  
- **Model & agent config:** `"model"`, `"agent"`, `"alwaysThinkingEnabled"`.  
- **Environment variables** via `env` object.  
- **Attribution** fields for commits/PRs.

**TOOLS USED**  
- `Read` tool (to fetch existing settings).  
- `Edit` tool (to merge new keys).  
- `AskUserQuestion` (for ambiguous edits, not used here).  
- `/config` slash command (recommended for simple settings).  
- `/model` command (to switch model in current session).  
- `c-to-h-mirror` hook (auto‑mirrors changes to `H:`).

**OPEN THREADS**  
- Verify Fable’s compatibility with `xhigh` effort / Ultracode.  
- If incompatible, pin Opus for ultracode runs while keeping Fable for other sessions.  
- Confirm “ultracode is on” reminder appears in a fresh session after changes.  
- Handle current session still using Opus until `/model fable` is run.
