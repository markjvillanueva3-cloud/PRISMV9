# academy session 001bd6c3 (2026-06-10, 85.1MB, spine 782KB, 9 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Docker Desktop + Qdrant (`prism‑qdrant`, v1.17.0) on `:6333`.  
- Hermes backend & CLI headless (`hermes_cli.main dashboard`, config_version 28).  
- Commit `28b72e4dee`: 34 MEMORY.md files canonicalized; added `fill-galaxy-memory-sections.mjs` + tests; synced via `syncGalaxyMemories`.  
- Commits `7d79f345c2`, `1ab785c21d`, `3ea4f40192`, `e6eba32eec`, `2579da89a4`: CLAUDE.md domain cores, PSN edge, padding memory cleanup, soul realignments, galaxy scripts & tests.  
- Commits `822c48c55`–`7771ca7f86`: build‑plan, U5b (`ollama-tool-agent.mjs`), U5c (`ollama-offload-enforce.mjs`), U6 (`ollama-codegen.mjs`), U7 (`ollama-build-loop.mjs`), U10 (`ollama-capability-probe.mjs`), U9 (`ollama-coresidency.mjs`).  
- Commits `2340a2e699`, `a2756779c2`, `a60d7ba0bf`, `612418fde7`, `3904b5e7b0`, `5a91ba1862`: offload router refresh, route‑hook default change, banner honesty fix, LLM compressor patch, GPT‑OSS‑120B fleet coding model, go‑live cherry‑pick.  
- Commits `U-OCTOPUS-LIVE-PRODUCER`, `U-OLLAMA-FANOUT`, `U-CONSENSUS-DRAIN-LOCAL`, `U-REWRITER-LOCALHOST-FIX`, `U-OPTCTX-LOCALHOST-FIX`, `U-LOCALHOST-HARDCODE-GUARD`, `U-GUARD-CURL-PRECISION`, `U-RAG-DENSE-127`.  
- Commits `9a598c52c7`, `d257350cf3`, `f8b7fa6d44`, `098a9f7651`: pressure normalization, two-tier escalation in `readChatPressure`, sidecar‑first read, TTL fix.  
- Commits `d1fafa2e1f`, `4fdf30e8f5`, `801237de5c`, `1b7bce6a91`, `ea45b16481`, `bb3503a5b8`: includeCodex flag, co‑resident Ollama pair panel, wiki header fix, ledger tagging, vendor normalization, runLive prewarm guard.

**DECISIONS**  
- Primary model: Anthropic Claude Code (`claude-opus‑4‑8`); fallback: custom/Ollama (`http://127.0.0.1:11434/v1`).  
- Fleet local standard: `gpt‑oss:20b` (128 K context, tool‑capable).  
- Headless PowerShell CLI for Zulu orchestration; increase gpt‑oss output token budget to avoid “no final response”.  
- Adopt deterministic script (`fill-galaxy-memory-sections.mjs`) to fill MEMORY.md sections; flag corrupted `ai-training_synthesis.md`.  
- Sequential 10 min loop (ultracode) to assess each galaxy until canonical 11‑artifact rubric met.  
- Keep all grunt work on local Ollama models; GPT‑OSS‑120B set as fleet coding default (`PRISM_CODEGEN_MODEL`).  
- Use existing `OllamaTaskOffloaderEngine.ts`; no new router needed.  
- Default consensus drain to local Ollama; optional Claude via env flag.  
- Disable Kimi 2.6 free tier (data sovereignty).  
- Normalize vendor keys in HOC04 for diagnostics without altering weight logic.

**OPERATOR DIRECTIVES**  
- Bring Qdrant up (`docker run`, expose `:6333`).  
- Resolve Hermes 400 error (enable “extra usage” billing or trigger local fallback).  
- Monitor Alpha’s model pulls (`gpt‑oss:120b`) and MCP server health (`:3100`).  
- Verify Obsidian REST API (`:27123`) reachable; vault graph intact.  
- Restart Hermes after config changes; tune gpt‑oss output token budget.  
- Await completion of Alpha’s `gpt‑oss:120b` pull; then merge slot/bravo to main.  
- Continue ultracode workflows for planning, verification, execution; commit only to `slot/bravo`.  
- Re‑enable PRISM Hermes‑Obsidian Bridge scheduled task.  
- Regenerate corrupted `ai-training_synthesis.md` via `galaxy-synthesis-refresh.mjs`.  
- Scale consensus drain for larger batch sizes; monitor queue size and timeouts.

**FINDINGS/BUGS**  
- Hermes 400 error: missing “extra usage” billing; fallback not retried.  
- Qdrant service down (no listening port).  
- Hermes agent init failed `Unknown provider 'openai'`; fixed to `custom`.  
- Git pull aborted due dirty `package-lock.json`; resolved by stashing/committing.  
- gpt‑oss reasoning channel mis‑parsed; “no final response”.  
- Claude API rate limiting prevented multi-agent workflows; local Ollama stack used instead.  
- Galaxy mirror drift: 22/34 directories missing → re‑run `syncGalaxyMemories`.  
- Memory count inflation corrected (66 real + excluded 1296).  
- Corrupted `ai-training_synthesis.md` flagged for owner review.  
- Offload router pointed at absent models; route hook default used retired `qwen2.5‑coder:7b`.  
- Take‑rate banner conflated advisory nudges with real offloads.  
- LLM compressor had safety‑denylist regex breach.  
- Kimi 2.6 free tier violates data sovereignty.  
- RTK config limits tightened; validated against live RTK.  
- Windows `localhost` resolves to IPv6 → node-fetch failures (~64 ms).  
- Claude fan‑outs >3–4 agents caused rate‑limit errors (“Server is temporarily limiting requests”).  
- Consensus drain queue grows faster than drains; large batch (`--max=20`) fails due to cumulative memory/timeouts.  

**DOMAIN SPECIFICS**  
- Hermes ↔ Claude Code integration (OAuth token in `~/.claude/.credentials.json`).  
- PRISM MCP at `:3100` (`slot_brief_write`, `chat_post`).  
- Obsidian REST API on `:27123`.  
- Qdrant vector store on `:6333`; collections: `prism_engines`, `prism_formulas`, `prism_skills`.  
- Slot‑brief dispatch & slot‑brief‑inject hook.  
- Engines/Actions: `OllamaTaskOffloaderEngine.ts`, `MultiModelConsensusEngine`, `ask-ollama`, `ollama-prism-bridge`, `ollama-tool-agent`, `ollama-offload-enforce`, `ollama-codegen`, `ollama-build-loop`, `ollama-capability-probe`, `ollama-coresidency`.  
- Paths: `state/shared/token-budget-{slot}.json`, `octopus-outcomes/hermes-zulu.jsonl`, `syncGalaxyMemories` output, `readChatPressure` sidecar TTL 180 s.  

**TOOLS USED**  
- Hermes CLI & desktop (`hermes_cli.main dashboard`, `hermes -z`).  
- PRISM MCP commands (`prism_context`, `slot_brief_write`).  
- Docker Desktop + `docker-compose.yml`; Qdrant container health checks.  
- Obsidian REST API plugin.  
- Node.js scripts: `syncGalaxyMemories`, `fill-galaxy-memory-sections.mjs`, `galaxy-synthesis-refresh.mjs`.  
- Ultracode workflows (`wbmnnd902`, `w2hu6dowo`); CronCreate/Delete for recurring `/loop`.  
- Git (commit, cherry‑pick, stash).  
- Docker/OLLAMA runtime on RTX PRO 6000 Blackwell 96 GB GPU.  
- Node fetch / curl for local Ollama calls; guard scripts (`U-LOCALHOST-HARDCODE-GUARD`).  

**OPEN THREADS**  
1. Restart Qdrant, expose port `6333`, load initial index.  
2. Enable “extra usage” billing on Claude Code or trigger local fallback on HTTP 400.  
3. Confirm Hermes semantic search once Qdrant operational; verify MCP health after restarts.  
4. Re‑enable PRISM Hermes‑Obsidian Bridge scheduled task.  
5. Regenerate corrupted `ai-training_synthesis.md` via `galaxy-synthesis-refresh.mjs`.  
6. Final review of deterministic tests (mutation‑testing pass).  
7. Monitor Claude API rate‑limit spikes; consider local fallback scripts for new multi‑agent workflows.  
8. Build & run Octopus live producer (`U-OCTOPUS-LIVE-PRODUCER`) with GPT‑OSS‑120B & Qwen 32b; record outcomes in `octopus-outcomes/`.  
9. Restart Ollama server with `RECOMMENDED_ENV` from U9; enable resident model limits.  
10. Integrate Octopus consensus drain outputs into downstream consumers (WeeklySynthesis, viz roost).  
11. Clean up stale deepseek references per fleet campaign.  
12. Scale consensus drain for larger batch sizes (heap vs timeout measurement pending).  
13. Wire remaining dormant hooks in bravo’s lane (e.g., zulu‑advisory‑inject).  
14. Deploy guarded localhost hardcode prevention across all lanes (Papa, India RAG scripts).  
15. Dream‑cycle upstream feed: emit `kind:"correction"` events to AGENT_CHAT.jsonl; capture slot attribution.  
16. Implement orchestrator‑advisory producer (`orchestrator-directives.json`).  
17. Resolve octopus recurring trigger child_process constraint & overlap with existing drain.  
18. Verify CHO02 cache correctness (shared‑lib change under review).  

All remaining dormant features in bravo’s lane are either wired or pending the above threads.
