# academy session 0e5669d2 (2026-06-10, 71.7MB, spine 600KB, 7 slice(s), model gpt-oss:20b)

**SHIPPED**  
- B1 sync‑resilient – `168c202646` (writeWithRetry, guarded sync)  
- B2 index‑meta – `ea21008f25` (frontmatter refresh of index.md)  
- B3 maint‑cron installers – `8c4dff660a` (promote & rot‑sentinel crons)  
- C2 link‑heal scorer – `984313825e` (prefix/substring demotion, reason‑based auto‑apply gate)  
- C3 tribal‑coverage refresh – `838123429b` (re‑run audit, clear stale banner)  
- Audit map & RAG‑wire commits – `da013c4406`, `9e4376b3b2`  
- Fix‑memory persistence – `45225e0cc3`  
- Hook‑heap cap change – portable-node wrapper default `--max-old-space-size=384 MB` (no commit id)  
- MCP server maxConnections bumped to 512 (restart pending)  
- Transient‑pressure-code set added; false “MCP Server failing” alert fixed  
- GPU offload patterns widened – offload ratio ~30%  
- `EXPECTED_UNREGISTERED_TASKS` introduced for vault crons (`fleet-task-health-watch.mjs`, commit `3d796dcf5c`)  
- PRISM WSL Memory Guard added to `KNOWN_PRISM_TASKS` & `CRASH_CRITICAL_TASKS`  
- Phase‑0 relief: killed 22 orphan processes; commit charge ↓ ~210 GB → ~160 GB (~79%)  
- Tests updated – pressure, offloader, drift now green  
- Vault‑backlink schema & builder – `96ed5222e2` (29 479 keys → 1 520 813 edges, 19.8 MB artifact)  
- Regen‑viz stage auto‑refresh of vault-backlinks.json – `0e2724871a`  
- Doc_nodes dispatcher action + wrapper + schema – `14aba14e3a` / `4a44b5393c` (15/15 pass)  
- Vault-access map update – `f23c58b42d` (closed reverse‑edge gap, fixed stale “memory‑rag‑inject” claim)  
- Canvas reader lib & CLI – `2d49bf0d33` (15/15 tests, staleness flag, Lgit regex fix)  
- Access-map doc updated – `47fe91554f` (.canvas row added, stale claim removed)  
- OLLAMA‑PIPELINE‑INJECTOR wired – `7ec4a5ea02`  
- U‑VIZ‑WIKI‑NARRATIVE built – `cd54edb940` (qwen2.5‑coder:32b narrative generation)  
- OLLAMA‑PREWARM‑WIRE wired – `65a29220e5`  
- U‑TRIBAL‑QA‑LLM‑UNBLOCK shipped – `6fb278a2ee` (gpt‑oss:120b distill‑tribal)  
- Memo‑throttle fix – `f1b69db664` (per‑session rate file)  
- User‑type bug in memo extraction fixed – `1ecf50b6be`  
- HTML twin update – `2c766b9308`  
- U‑WEEKLY‑SYNTH‑RESOLVER + U‑WEEKLY‑SYNTH‑NUMPREDICT commits `b5d249f4f5`, `9697a9135a`, `71a818b49c` (host‑aware model resolution)  
- U‑WIKI‑NLI‑LINT commits `f8c183f7a5`, `c55e05cf03` (contradiction detection via gpt‑oss:20b)  
- U‑VIZ‑GRAPH‑ATOMIC‑WRITE – `153887a519`  
- U‑VIZ‑MERGE‑GUARD‑OOM commits `6884155fb6`, `757456eaae` (off‑heap node counter)  
- U‑VIZ‑AUGMENT‑MOLECULES‑STREAM – `ae55cea3f7` (streamGraphArray off‑heap)  
- OLLAMA‑SYNERGY #6 rescope – `0f14952601`  
- Status doc reflection – `734bcee6d4`  
- U‑VIZ‑MERGE‑AUG‑CAP‑GUARD – `628aaa51f5`  
- Merge validated at 660 MB post‑cap guard – `7a1f52061b`  
- U‑VIZ‑POSTMERGE‑CAPSAFE – `80f8059cb1` (writeGraphStreamingAtomic)  
- Removed phantom FAST[] entry – `3e4df51d04`  
- Fixed regen‑pipeline readers to readGraphStreaming – `d03d8687a7`  
- Fixed cap‑unsafe readers (master-index-search-lib, namespace-churn-ranker, etc.) – `4dbd18c2e3`

**DECISIONS**  
- Bind *sierra* slot deterministically via `/checkin-sierra`; persist audit results to wiki node‑access map, MEMORY.md, two C: memories, Handoff file.  
- Wire `memory-rag-inject` only after disabling precheck injector; avoid double‑up patterns (R7).  
- Close P0 RAG‑wire bug by wiring keyword recall surface.  
- Recalibrate link‑heal scorer: demote prefix/substring below auto‑apply floor, add reason‑based gating, clamp decay for long slugs.  
- Do not raise hook‑heap cap beyond 384 MB; keep WSL balloon at 16 GB via `wsl --shutdown`.  
- Keep migration freeze ON; defer MCP daemon restart and vault cron registration until lift.  
- Adopt off‑heap streaming (`readGraphStreaming` / `writeGraphStreamingAtomic`) for large arrays to avoid OOM/string‑cap.  
- Use atomic graph writes in merge‑augmentations.mjs to prevent truncation of system‑graph.json.  
- Keep sidecar fast‑path for master‑index search; defer core migration until fresh context.  
- Prioritize memo‑throttle and user‑type fixes before semantic reranker.

**OPERATOR DIRECTIVES**  
- Validate commit‑cap fix (monitor commit charge).  
- Tidy missing‑task banner by adding vault crons to `EXPECTED_UNREGISTERED_TASKS`.  
- Write handoff, recommend `/compact`; arm deferred items: restart MCP daemon for maxConnections=512, register vault crons when migration freeze lifted.  
- Finish all Ollama synergy tasks; use strongest viable Ollama models (qwen2.5‑coder:32b, gpt‑oss:120b) for searches, read/write, coding.  
- Accelerate self‑improving of Obsidian vault; wire `/system‑viz`, memory, wiki, tribal knowledge across all galaxies and full H drive systems.  
- Look for system inefficiencies, hook inefficiencies, auto‑enforcement suggestions after nightly completion.

**FINDINGS/BUGS**  
- `memory-rag-inject` unwired → keyword recall broken.  
- Sync bug: unguarded `writeFileSync` & `ensureDir` cause data loss on first failure.  
- Index‑meta stale frontmatter (last_verified 2026‑05‑08 / 770 entries); frozen index.md needs refresh.  
- Scorer overconfidence: prefix/substring auto‑apply eligible; decay leak for slugs >60 chars.  
- Stale candidates file with `autoApplyCount 14100` still armed.  
- Full‑corpus scan (15.8K links × 51K candidates) OOMs when batch=0.  
- Over‑commit caused false “MCP Server failing” alert; hook heap reservation inflated commit charge to ~210 GB → ~160 GB after cap.  
- Offload coverage only ~8% before widening; now ~30%.  
- Pagefile on slower drives not critical post‑cap.  
- Shared‑index race caused commit attribution loss but content committed.  
- WSL balloon 95 GB not respecting `.wslconfig memory=16GB` until `wsl --shutdown`.  
- Lgit regex missed alphabetic suffix → fixed.  
- Peer contamination in shared‑tree; resolved via surgical staging (`git apply --cached`).  
- ExecSync injection vulnerability in old `queryOllama`; replaced with safe `callOllama`.  
- gpt‑oss empty response due num_predict starvation; set `num_predict ≥ 1024` or `-1`.

**DOMAIN SPECIFICS**  
- Engines/Actions/Dispatchers: WikiIndexMaintainerEngine, obsidian-memory-sync.mjs, wiki-link-fix-suggester.mjs, fleet-task-health-watch.mjs, OllamaTaskOffloaderEngine, doc_nodes dispatcher, node_card dispatcher, regen-viz.mjs stage, system‑viz-query.mjs CLI, canvas-read-lib.mjs, ask-ollama.mjs, resolveSynthesisModel.mjs, graph‑io.mjs utilities (`readGraphStreaming`, `writeGraphStreamingAtomic`, `streamGraphArray`), augment-molecules.mjs.  
- Paths: wiki node-access map `obsidian-vault-node-access-map.md`; MEMORY.md `H:/prism/mcp-server/src/engines/system-viz/MEMORY.md`; C: memories `reference_obsidian_vault_audit_*`, `reference_humza_khalid_*`; Handoff `HANDOFF-claude-0e5669d2-sierra-work.md`; node-card-prefetch-inject, pre-bash-graph-inject scripts; system‑graph.json (merged), architecture‑graph.json (fallback); master-index sidecar.  
- Metrics: commit charge ↓ ~210 GB → ~160 GB; offload ratio ~30%; graph size 660 MB; heap usage 432 MB; 333 k nodes, 675 k edges.

**TOOLS USED**  
- `/checkin-sierra` wrapper, chat-slots helper, Settings.json wiring via `update-config`, Ultracode workflow (6 agents), `node:test` & `vitest`, Git tooling (`git add`, `commit`, `reset`), Scrutiny gate, Handoff write tool, C: auto‑memory protocol, portable-node wrapper scripts, MCP server source (`src/index.ts`), Fleet-task-health-watch.mjs, OllamaTaskOffloaderEngine.ts, Node.js test framework, shared-index guard, PRISM CLI skills (`/loop`, `/goal`, `/startup-sierra`), CronCreate, graph-io.mjs utilities, esbuild, tsc --noEmit.

**OPEN THREADS**  
- Full‑corpus candidates regeneration OOM → need streaming or chunked approach.  
- Restart MCP daemon to apply `maxConnections=512` (operator decision).  
- Register vault crons after migration freeze lifted.  
- Monitor commit charge stability; add missing tests for `EXPECTED_UNREGISTERED` partition edge cases.  
- Implement U‑OLLAMA‑PIPELINE‑INJECTOR‑WIRE dispatcher for tool calls (pending).  
- Finalize weekly synth resolver and NLI lint integration; ensure host‑aware model selection.  
- Integrate tribal-rerank as lazy post‑BM25 reranker (Gap B).  
- Streaming-object loader to preserve Obsidian linkage beyond 512 MiB.  
- 44‑file projection sweep from `readGraphStreaming` → `streamGraphArray` for heap/commit‑pressure optimization.  
- Core search lib migration deferred until fresh context; keep sidecar fast‑path.  
- Auto‑enforcement suggestions hunt after nightly completion.
