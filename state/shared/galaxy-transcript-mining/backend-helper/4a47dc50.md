# backend-helper session 4a47dc50 (2026-06-25, 84.9MB, spine 347KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `AIResourceLearningEngine.getDomainCorpus()` + dispatcher action `ai_domain_corpus_pointers` (ZULU → INDIA pipelines).  
- `reclassify-domain-feeders‑ollama.mjs`: rescued 65 of 769 keyword‑unclassified specs; full reclassification verified round‑trip.  
- `domain-corpus-to-lora-dataset.mjs` upgraded to `qwen2.5-coder:14b` distillation – 20/20 tests, produces Alpaca Q&A pairs.  
- `tribal-corpus-to-lora-dataset.mjs` (commit 5d9dbbc91c): GIGO‑guarded converter with cursor support; 398 raw rows, 0 distilled yet.  
- Wiki lessons: `atomic-swap-over-resumable-deferred-retry-stream.md`.  
- Self‑healing distill resume (`U-PAPA-DISTILL-RESUME-SELFHEAL`): 3 files, 29/29 green; `keep_alive` + retry fixes VRAM eviction wedge.  
- `ollama-fanout.mjs`: bounded concurrency wrapper to avoid burst hangs (~100 calls).  
- Snapshot helper `snapshotRawBaselineBeforeTruncate()` (commits 8d46cbd49c & c2e81dc477): loud WARN on truncate, atomic‑swap baseline preservation; all tests green.  

**DECISIONS**  
- Adopted `qwen2.5-coder:14b` after 7b under‑classifies and 32b too heavy; added fanout pacing.  
- Deferred new builds when session limit imminent; performed watchdog hygiene, triggered overdue tasks.  
- Left hung Tribal Embed running to avoid index corruption; operator supervision required.  
- Reverted atomic‑distill redesign (P0 data loss); will tackle in fresh session with constraints captured in wiki.  
- Cad/cam LoRA parity, scaling extraction, and never‑idle ladder items deferred until operator or loop can handle them.  

**OPERATOR DIRECTIVES**  
- Trigger overdue `PRISM Tribal Embed` task (now Running).  
- Commit any remaining uncommitted changes (none pending).  
- Verify no orphan background tasks remain.  
- Run `node scripts/capture-claude-credentials.mjs account-N`; once GREEN, execute `node scripts/arm-account-switch.mjs --auto`.  

**FINDINGS / BUGS**  
- Zulu’s domain‑corpus consumer phantom → missing `getDomainCorpus()`.  
- 769 specs keyword‑unclassified; resolved via Ollama reclassifier.  
- Ollama burst (~100 calls) caused rapid failures → fallback to raw text; solved with fanout pacing.  
- Stale “Tribal Embed” warning was misdiagnosis (task succeeded but remained Running).  
- VRAM eviction wedge fixed by `keep_alive` + retry.  
- Distill resume logic now self‑healing; no regressions.  
- Cad/cam tips are pointers → must be distilled, not raw dumps.  
- Atomic distill‑to‑temp caused silent data loss (P0); design flaw identified and reverted.  
- Session limit approaching 5 h – long runs risk being killed.  
- Audit‑unwired engines: 4 flagged; 2 peers, 2 require judgment.  
- Tribal‑Embed transient failure due to host load (MCP spawn‑refused).  
- Silent truncate regression bug fixed – loud WARN + recoverable baseline snapshot.  
- Fail‑soft I/O wrapper prevents V8 string‑cap crashes.  

**DOMAIN SPECIFICS**  
- `AIResourceLearningEngine.getDomainCorpus()` returns live counts from per‑domain tribal corpora.  
- Dispatcher action `ai_domain_corpus_pointers` exposes domain pointers to DL/NN/GNN pipelines.  
- `reclassify-domain-feeders‑ollama.mjs`: keyword‑based reclassification with Ollama, override application.  
- `domain-corpus-to-lora-dataset.mjs`: converts rescued PDFs into Alpaca Q&A pairs (distill mode).  
- `tribal-corpus-to-lora-dataset.mjs`: handles tribal tips → Alpaca pairs; uses `pdfToText`, `buildPairsForEntry`.  
- Atomic‑write state machine for resumable streams.  
- Wiring audit engine list: AuthEngineV7, PreMOUKickoffChecklistEngine, etc.  
- Domain knowledge: 10 non‑cadcam + cad/cam tribal corpora (1,310 entries).  
- LoRA pipeline schema `{instruction,input,output}`; assembler merges `domain-knowledge-lora` and `tribal-knowledge-lora`.  
- Ollama local LLM `qwen2.5-coder`: `keep_alive`, fanout, bounded concurrency.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`, `.claude/helpers/per-agent-handoff.mjs`, `scripts/build-milestone-progress.mjs`, `build-state-snapshot.mjs`, `close-out-milestone.mjs`, `hook-health-check.mjs`.  
- Checkin pipeline `/checkin.md`; learning router `/learn` and specialist skills (`/pdf-learn`).  
- Dispatcher engine `aiReasoningDispatcher.ts`.  
- Test harnesses: Vitest, TypeScript compiler, node:test.  
- Ollama local inference `qwen2.5-coder`.  
- Claude for judgment.  
- Scripts: `domain-corpus-to-lora-dataset.mjs`, `tribal-corpus-to-lora-dataset.mjs`, `assemble-fleet-lora-corpus.mjs`, `audit-unwired-engines.mjs`, `per-agent-handoff.mjs`.  

**OPEN THREADS**  
- Cad/cam LoRA parity (clone `domain-corpus-to-lora-dataset.mjs` for cad/cam tribal corpora).  
- Scaling extraction beyond 65 rescued PDFs.  
- Never‑idle ladder items (fixes → wirings → ghost builds).  
- NN‑Graph Retrain trigger and account‑switch gating.  
- Resolve hung Tribal Embed task (operator‑supervised stop/restart).  
- Full distill of cad/cam tribal corpus to Q&A pairs – handled by autonomous loop’s resumable cursor.  
- Distill quality upgrade for domain knowledge already in progress.  
- Atomic distill‑to‑temp design needs separate unit.  
- Monitor session limit; avoid long runs that could be killed before completion.
