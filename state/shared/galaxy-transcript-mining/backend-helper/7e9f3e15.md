# backend-helper session 7e9f3e15 (2026-06-24, 15.5MB, spine 132KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Phase‑B extractor `scripts/extract-catalog-cutting-params.mjs` – parses SFM/feed tables from 287 vendor PDFs → `ManufacturerSpeedFeed` JSON + tribal tips.  
- 4× Ollama concurrency wrapper (Blackwell GPU, `qwen2.5-coder:32b`).  
- Yield‑priority ordering drains cutting‑tool catalogs first; catalog tips auto‑wired into tribal‑embed index (RAG/GNN/LoRA).  
- Stop‑gate “vitest‑scope” (`U‑STOPGATE‑VITEST‑SCOPE`) removes node:test false positives from `stop_on_failing_tests`.  
- Crossroad auto‑decide module/hook (`U‑CROSSROAD‑AUTO‑DECIDE`) uses deep‑reasoning engines (Creative, MultiPath, Decision) to auto‑advance reversible forks.  
- Session‑attribution filter added to `stop_on_failing_tests` (commit 4e684d9d2a); scopes stale‑test checks to current slot edits via `transcript_path`.  
- Wiki entry e04a764932 documents bug‑finding, fix rationale, test coverage.  
- All committed to `slot/papa` (worktree `H:/prism-slot-papa`, branch `slot/papa`).  

**DECISIONS**  
- Durable Windows scheduled tasks (`PRISM Catalog Cutting Params`, `PRISM Tribal Resources Drain`, `PRISM Tribal Embed`) run long‑running extraction; reaper cannot kill work.  
- Offload all heavy LLM calls to Ollama (`qwen2.5-coder:32b`); $0 Claude tokens, GPU‑accelerated.  
- Phase‑B extractor reads actual SFM/feed tables instead of ISO aggregates; bounded concurrency (3×) + page‑windowed resume keeps extraction within 30‑min task window.  
- Prioritize cutting‑tool catalogs first to maximize valuable records per run.  
- Wire catalog tips into tribal index automatically; no manual ingestion needed.  
- Scope vitest tests to `mcp-server/` only for stop‑gate false positives.  
- Implement crossroad auto‑decide so chats that would normally pause proceed automatically when fork reversible.  
- Add caller‑layer session‑attribution filter to `stop_on_failing_tests`; keep pure logic unchanged, preserve under‑block protection (fallback block on uncertainty).  
- Audit of 14 stop hooks: only `stop_on_failing_tests` required clean session‑attribution fix; others unchanged.  

**OPERATOR DIRECTIVES**  
- Send message to Oscar via chat bus (`state/shared/chat-bus/messages/`) – auto‑reads enabled.  
- Continue running Hermes `/learn` feature on durable tasks; no in‑session work needed.  
- Crossroad auto‑decide now handles user‑input forks automatically (deep reasoning).  
- Account switch confirmed; proceed with normal operation.  

**FINDINGS/BUGS**  
- Stale `resources-drain.lock` from dead PID stalled overnight drain; cleared and re‑armed task.  
- Drain task expired at 12:44 PM; re‑armed for 20‑min intervals.  
- Ollama down during some runs – restarted, now healthy.  
- Vendor mislabel bug: GC catalog mapped to Sandvik → corrected mapping to Tungaloy.  
- Feed‑type confusion (fz vs fn) fixed via logic/validation update.  
- ISO normalization bug (`ISO M` → null) resolved with robust token extraction.  
- Stop‑gate false positives on node:test files scoped to `mcp-server/`.  
- Leave‑a‑copy guard false positive restored recoverable peer files from HEAD.  
- Peer‑edit false positive in `stop_on_failing_tests` blocked innocent slots (e.g., `RFQToOrderOrchestratorEngine.test.ts`).  
- Adversarial review uncovered P1 under‑block: rename status (`R old → new`) not decomposed; fixed by using destination path.  
- Session‑edited‑files lib built & unit‑tested (19 lib + 34 hook tests).  

**DOMAIN SPECIFICS**  
- Slot: `papa`; worktree `H:/prism-slot-papa`, branch `slot/papa`.  
- Pipeline: PDF → tribal tips → tribal‑embed index (PSN L1 vector, 768‑d, 4 shards).  
- Catalog schema (`ManufacturerSpeedFeed`): `{series, isoGroup, vc_min/max, fz_min/max, dc_min/max?}`.  
- Ollama API endpoint `http://127.0.0.1:11434/api/chat`.  
- Durable tasks: `PRISM Catalog Cutting Params`, `PRISM Tribal Resources Drain`, `PRISM Tribal Embed`.  
- Crossroad auto‑decide uses deep‑reasoning engines (Creative, MultiPath, Decision).  
- Hooks/dispatchers: `stop_on_failing_tests`, `leave-a-copy-behind-guard`, `scrutinize-before-stop`.  
- Slot binding wrapper `/checkin-papa` forces claim via `slot-bind-enforce.mjs` & `chat-slots.mjs`.  
- Metrics/paths: catalog cutting params at `mcp-server/data/catalog-cutting-params/<slug>.json`; catalog processing window (`PRISM_CATALOG_PAGE_WINDOW=30`, `MAX_PAGES=400`).  

**TOOLS USED**  
- Extractor script `scripts/extract-catalog-cutting-params.mjs`.  
- Unit tests: `node:test` & `vitest`.  
- Ollama (`qwen2.5-coder:32b`, `gpt-oss`).  
- Hermes CLI, ultracode, Blackwell GPU.  
- Windows Task Scheduler (durable tasks).  
- Chat bus (`state/shared/chat-bus/messages/`) & agent coordination.  
- System‑viz, Obsidian vault, PSN, tribal wiki/memories.  
- PRISM helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
- Hook runner `runHook` (stdin piped).  
- Transcript reader `session-edited-files.mjs`.  
- Test harness & adversarial review via Sonnet (two parallel reviewers).  

**OPEN THREADS**  
- Implement per‑slot freshness scoping for `stop_on_failing_tests` to avoid stale‑report thrash.  
- Continuous monitoring of Ollama health; auto‑restart under GPU load.  
- Optimize extraction: smarter page‑window sizing, adaptive concurrency.  
- Automate integration of catalog‑cutting JSON store into Oscar’s `ToolCatalogEngine`.  
- Validate crossroad auto‑decide classifier against all operator‑only categories to eliminate false positives.  
- Apply session‑attribution pattern to other stop‑scanning hooks (e.g., `scrutinize-before-stop`) if needed.  
- Verify long‑term reliability of **session-file-ownership.json** for gate ownership tracking.
