# backend-helper session c478f3f9 (2026-05-25, 36.1MB, spine 125KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**

- U‑BRIDGE‑SFC‑ESPRIT – commit `42a21c464d` – 13 tests  
- U‑PSN‑UNLINKED‑MENTIONS – commit `092ed84bfc` – 21 tests  
- U‑PSN‑ALIASES‑FRONTMATTER – commit `f6b5f0dce8` – data‑only, no tests  
- U‑PSN‑CONNECTION‑FINDER – commit `2245de0258` – 16 tests  
- U‑PSN‑GAP‑FINDER – commit `6da49ecc6e` – 12 tests  
- U‑PSN‑BLOCK‑HEADING‑LINKS – commit `7b45e1dac2` – 17 tests  
- U‑PSN‑MOC‑LAYER – commit `a5cc012fa1` – 9 tests  
- U‑PSN‑DEEP‑RESEARCH‑SYNTHESIS‑2026‑05‑23 – commit `ff644c1e9d` – synthesis index over 7 prior PSN specs  
- U‑PSN‑MASTER‑INDEX‑ALIASES‑2026‑05‑23 – commit `ff644c1e9d` – added alias parsing to memory‑index‑search‑lib  
- Iter 17 – U‑PSN‑QDRANT‑POPULATE – 3,866 vectors in `prism_engines` (1669 vectors, 3 866 points)  
- Iter 18 – U‑PSN‑HYBRID‑RETRIEVAL‑WIRE – `hybridSearch()` lib + CLI (`scripts/prism-hybrid.mjs`) + `/hybrid` skill; 44/44 tests  
- Iter 19 – U‑PSN‑QDRANT‑PAYLOAD‑DEBUG – vector hits expose canonical `engine:Foo` IDs; 50/50 tests  
- Iter 21 – U‑PSN‑HYBRID‑VIZ‑ROOST – viz augmentation generator (4/4 GREEN substrates)  
- Iter 22 – U‑PSN‑HYBRID‑VIZ‑ROOST‑WIRE – regen‑viz + merge‑augmentations splices  
- Iter 23 – U‑PSN‑GRAPHITI‑SEED‑EXPANDED – episode store 7 → 2,004 (286×)  
- Iter 24 – U‑PSN‑GRAPHITI‑SEED‑TESTS – 17/17 tests; fixed two silent regressions  
- Iter 25 – U‑PSN‑HYBRID‑RETRIEVAL‑WIKI – wiki architecture consolidation  
- Iter 26 – U‑PSN‑HYBRID‑MCP‑WIRE – `prism_session:hybrid_search` MCP action (dispatcher enum, schema, handler)

**DECISIONS**

- Adopt pure‑Node JSONL episode store mirroring getzep/graphiti; avoid external graph DBs.  
- Implement alias scoring (`W_ALIAS=3.0`) in memory-index-search-lib; backfill deterministically via `scripts/backfill-memory-aliases.mjs`.  
- Enable Qdrant vector DB; populate `prism_engines` with 1669 vectors (3 866 points).  
- Add `/observability` skill for hallucination detection & RAGAS‑style metrics.  
- Create multi‑provider router (`/route`) for Claude, Gemini, GPT‑4.1, DeepSeek, Ollama.  
- Build MCP manifest for external agent consumption; expose hybrid retriever via lib, CLI, MCP action.  
- Use FNV‑1a 32‑bit hash for deterministic Qdrant point IDs; payload debug surfaces human‑readable engine IDs.  
- Fuse four substrates (BM25, vector, graph, episode) with RRF (k=60) in `hybridSearch()`.

**OPERATOR DIRECTIVES**

- Max out all memory aliases; bypass any stop hooks that impede PSN integration.  
- Loop next batch of work every 10 min; ensure everything wired and synergized to PSN system.  
- Build deep research on merging ideas, hybrids for better quality & efficiency; keep aligned with `/system-viz`.  

**FINDINGS/BUGS**

- Drift in `system-graph.json`: 0 fsCoverage namespaces after FS‑COVERAGE-MS0/MS1; orphan `.tmp.system-graph.json` artifacts.  
- Feeder stop‑hook race caused vault coverage drop; fixed via stamp‑file pause.  
- Qdrant health probe misread `points_count`; corrected to use correct metric.  
- `node_*` synthetic pointers dilute alias density; excluded from BM25.  
- OOM during system‑viz regeneration due to large JSON string length (flagged for future fix).  
- Corrupt tree object (`e36809bbd2`) blocked `--name-only`; introduced `--no-files` fallback & RECSEP parser.  
- Two silent test regressions fixed in Iter 24.  

**DOMAIN SPECIFICS**

| Category | Details |
|----------|---------|
| **Engines** | CAMSpeedFeedBridgeEngine, EspritCAMBridgeEngine, U‑BRIDGE‑SFC‑ESPRIT, EmbeddingGuardEngine, LocalEmbeddingEngine |
| **Dispatchers / Skills** | `prism_graphiti:*` (add_episode, search, traceback), `/graphiti`, `/observability`, `/route`, `/qdrant-revive`, `/populate-qdrant`, `/hybrid`, `scripts/prism-hybrid.mjs`, `prism_session:hybrid_search` MCP |
| **Metrics** | `W_ALIAS=3.0`, hallucination score, RAGAS precision@k/recall@k/MRR, Qdrant `points_count`, system‑viz node count, hybrid RRF k=60, provenance surface counts (memory:1, master:1, vector:1) |
| **Paths / Artifacts** | `H:/prism/.claude/helpers/chat-slots.mjs`, `scripts/backfill-memory-aliases.mjs`, `scripts/populate-qdrant.mjs`, `state/shared/episodes.jsonl`, `state/shared/UNLINKED-MENTIONS.{json,md}`, `state/shared/system-viz/hybrid-retrieval-augmentation.json` |

**TOOLS USED**

- PRISM utilities: `chat-slots.mjs`, checkin pipeline, CronCreate, Skill tool invocation.  
- Scripts: `backfill-memory-aliases.mjs`, `populate-qdrant.mjs`, `prism-hybrid.mjs`, `seed-episodes-from-git.mjs`.  
- Libraries: `scripts/lib/hybrid-retrieval.mjs`, `hybrid-retrieval.test.mjs`.  
- Viz pipeline: `generate-hybrid-retrieval-features.mjs`, `regen-viz.mjs`, `merge-augmentations.mjs`.  
- MCP server: `sessionDispatcher.ts`, `sessionActionSchemas.ts`.  
- External services: Qdrant (vector DB), Ollama, Playwright via chrome‑devtools MCP.

**OPEN THREADS**

1. U‑PSN‑QDRANT‑INGEST‑OTHER‑COLLECTIONS – embed & populate `prism_formulas`, `prism_skills`.  
2. U‑PSN‑GIT‑TREE‑REPAIR – fsck and repair corrupt tree for full file‑entity ingestion.  
3. Tier‑2 MCP tools: `episode_query`, `rrf_merge`, `embed`, `rag_construct`, `psn_health`, `engine_recommend`, `slot_query`.  
4. prism_safe:hybrid_search – tenant‑isolated, audit‑log enabled mirror of the MCP action.  
5. NN/GNN AUROC retraining (PSN leg #10).  
6. Resolve regen‑viz V8 OOM to render hybrid roost in `/system-viz`.
