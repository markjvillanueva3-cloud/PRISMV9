# shop-floor session 928a8226 (2026-06-10, 68.1MB, spine 675KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `scripts/fix-hookify-frontmatter.mjs`: converted 110 malformed `.local.md` files to valid YAML frontmatter.  
- `scripts/remove-dangling-hook-refs.mjs`: removed 6 dead hook references from C: and H: copies.  
- `scripts/audit-unwired-engines.mjs`: audited unwired engines; results in `audit/unwired_engines_2026_06_08.json` (90 built, 14 with dispatcher, 76 UNKNOWN).  
- `c593b096fb`: fixed Ollama resilient pull (em‑dash → hyphen), pulled gpt‑oss:120b (~96 % progress, final 2.7 GB @94 MB/s).  
- `13017de764`: hookify frontmatter conversion + dangling hook ref cleanup; all hooks load without errors.  
- `29f2d177f6`: gap-map artifact covering node‑by‑node synergy status for 14 surfaces (8 ranked gaps/owners).  
- `8550436c8a`: corrected cag‑router phantom Qdrant collection label to real collections; tests pass.  
- `c9fe32d8b2`: fixed embedder OOM (heap flag) and added empty-text skip guard; driver now adds embeddings for good files.  
- `182788232a`: updated tribal-rerank & embed scripts with buffered index loader; cap‑safe loader for tribal index.  
- `5467482be4`, `c994bf3aad`: documentation & wiki updates (regression, gap-map).  
- `fb314a6fd1`: Docker-service-health CLI detecting container states and suggesting `docker start <name>`.  
- `14b8b383a2`: singleton‑service‑guard Stop hook wired into docker health chain; auto‑reaps orphan MCP daemons.  
- `006c435ddb`: corrected MCP restart command in docs (`TRANSPORT=http node dist/index.js`).  
- `e2081e0780`, `d22681f5d2`: upgraded singleton-service-guard to reap-all+start (3-of-3 scrutiny PASS).  
- `bee977a004`: set NODE_OPTIONS=--max-old-space-size=8192 for reranker to avoid heap OOM on large JSON indices.  
- `U-MCP-FIXSTART`, `U-MCP-CMDMATCH-FIX`, `U-HOOK-UNREG-PROTOCOL-FIX`, `U-MCP-BOOT-HEAP-FLOOR`: MCP recovery, daemon detection regex fix, stop_on_hook_unregistration protocol, enforce ≥4096 MB heap on MCP spawn.  
- `4141daf9d8`, `698cf12ad2`, `U-FTH-WIKI-LESSON`: fleet‑task‑health migration‑freeze marker and wiki lesson; static-list commit superseded.  
- `tribal-by-domain-inject`: wiki promotion of PSN/tribal‑knowledge hook.  
- `6ca2f6afd2`: MCP boot‑heap OOM resilience unit.  
- `U-QP-COST-SAVINGS-TEST`: added 13/13 vitest suite for CostSavingsTrackerEngine.  
- `U-BW-DISPATCHER-SCAN`, `U-BW-DISPATCHER-SCAN-WIKI`: rewired cascade defaults to installed Blackwell tiers; source‑lock guard extended to dispatcher tree; wiki lesson.  
- `U-PSN-SNAPSHOT-REFRESH`: regenerated stale PSN snapshot; identified 11 real under‑wired pairs (Sierra lane).  
- `U-QP-DIGITALTWIN-TEST`: added reference‑value test for DigitalTwinEstimator (5/5 algorithms).  
- `U-QP-MARGIN-FLOOR-GATE`, `U-QP-MARGIN-FLOOR-SHOPCONFIG`: margin‑floor check in QuoteEstimatorEngine; self‑source from ShopConfigurationEngine.  
- `ec597dbcb3`: dispatcher round‑trip test for margin‑floor gate (7/7).  
- `87d5c4bf9a`: frontend alert banner for below_margin_floor; updated QuoteEstimate type; 2 R9 tests.  
- `U-QP-NRE-AMORTIZE-TEST-FIX`: corrected stale NRE amortize test.  
- `4b0980c56a`: AI‑substrate routing matrix coverage 5/5 (27/27).  
- `62b114cb7b`: web test ResizeObserver polyfill; quote-specialization pages green.  
- `8258918f78`: U-QP-FRONTEND-TEST-REPAIR (13/13 quote-pages tests, tsc clean).  
- `U-QP-BLUEPRINT-OCR‑BRIDGE‑ADAPTER` commit `e3f9c2b1a0`: adapter between OCR output and bridge; instance method; full test suite.  
- `4c12a75a8d`: provenance gate blocks synthetic/placeholder promotion.  
- `d294957c4d`: outbound promote gate aligns real sold‑price distribution (fail‑closed).  
- `bf10035ec0`: calibration freshness preflight warns on >24 h stale factors.

**DECISIONS**  
- Root‑cause first debugging workflow for hook errors; treat unwired engines as triage list, never delete.  
- Keep golf slot claim logic unchanged; added bypass flag `PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1`.  
- Use Ollama local LLM stack on RTX PRO 6000 Blackwell; set `OLLAMA_MAX_LOADED_MODELS` to 3×GPU (~114 GB VRAM).  
- Set `DEFAULT_NUM_PREDICT=1024` for ask‑ollama to avoid empty responses.  
- Replace string read of tribal‑embed-index with buffered loader (V8 512 MiB cap); use fail‑loud guard, shrink‑guard, closing‑`]` assertion.  
- Batch workflow agents in groups of 3 to stay within Anthropic rate limits; keep Fleet Reaper and monitors always‑on via durable scheduled task.  
- Defer Qdrant tribal migration until corpus stable; use bounded read‑only diagnostics before committing changes.  
- Consolidate 16 cost‑bridge hooks into single dispatcher (`cost-bridge-dispatch.mjs`); add WIRE‑EXEMPT tags for intentionally invoked engines.  
- Add provenance gate in QuotingClosedLoopEngine to block synthetic outcome promotion.  
- Create dispatch map (`44fab1be37`) to delegate remaining fleet‑scope clauses.  
- Use ultracode workflow for planning/reasoning; Ollama for grunt reads/coding to conserve budget.  
- Commit only verified units; defer multi‑agent scrutiny when rate‑limited.

**OPERATOR DIRECTIVES**  
- `export PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1` or edit settings.json to remove golf-slot-write-allowlist.mjs entry.  
- Run `node H:/prism/scripts/fleet-reaper-sweep.mjs --once --json` after each `/checkin-golf`; surface JSON in report.  
- Verify Ollama models pulled match GPU/CPU/NVMe SSD and 128 GB RAM; ensure docker, qdrant, obsidian vault app, PSN, system‑viz, prism galaxies, front‑end & back‑end build, claude.md, memories, wiki, tribal knowledge, prism awareness, gsd, tdd, skills, scripts and hooks are wired, tested, validated, synergized node‑by‑node, galaxy‑by‑galaxy.  
- Maintain Stop hook condition: “synergize ollama, docker, qdrant, obsidian vault app, PSN, /system‑viz, prism galaxies, front‑end/back‑end build, claude.md, memories, wiki, tribal knowledge, prism awareness, gsd, tdd, skills, scripts and hooks” until all are wire‑tested.  
- Utilize Ollama for better task efficiency when viable (token‑heavy work).  
- Finish all remaining phases of units and tasks; upgrade existing systems relative to new PC hardware and system utilization.  
- Final goal: fully finished closed‑loop learning; quoting system ready for use within JM Die.

**FINDINGS/BUGS**  
- Hookify: 110 frontmatter warnings + 1 charmap crash resolved; no more hook errors.  
- Unwired engines: 90 built, 14 with dispatcher, 76 UNKNOWN (likely superseded/orphan).  
- Golf slot claim succeeded; fleet‑reaper now owned by golf per doctrine shift.  
- Ollama pull stalled due to PowerShell parse error; fixed em‑dash → hyphen; completed ~96 % progress, final 2.7 GB @94 MB/s.  
- Synergy bug: `routeModelForTask` still selects gpt‑oss:20b instead of gpt‑oss:120b for search_synthesis on Blackwell; pending priority/tier mapping fix.  
- V8 string‑cap caused silent failure of JSON.parse on 537 MB tribal index → PSN leg #5 dead; fixed with cap‑safe loader and fail‑loud guard.  
- Fail‑open in `tribal-embed-index.mjs` allowed empty base, leading to one‑entry clobber when auto‑embed hook ran.  
- MCP multi‑instance leak: orphan daemons left port 3100 bound → recurring outages; fixed with singleton‑service‑guard and boot‑heap floor ≥4096 MB.  
- Qdrant collection `prism‑memory` absent; cag‑router label misleading but not harmful.  
- Tribal‑embed-index grew to 537 MB, exceeding V8 string limit → JSON.parse crash; fixed with buffered loader.  
- Embedder crashed on large index due to string length cap; fixed with buffered loader.  
- Empty‑text files caused batch aborts; added skip guard.  
- System‑viz `find` OOM on default node heap – decided against patching (intentional fleet budgeting).  
- No margin‑floor enforcement in QuoteEstimatorEngine – added gate.  
- Stale NRE amortize test caused false RED; corrected.  
- Cry‑wolf from static disabled‑task list (4 names vs ~47 real tasks); fixed with migration‑freeze marker.  
- False “0 wired” alarm for 16 cost‑bridge hooks; router consolidation hidden wiring.  
- AccountingHardeningEngine fully wired but data‑blocked on live ERP feed.  
- WEDMQuoteBridgeEngine incorrectly flagged as orphan (actually a route adapter).  
- AI dispatcher defaulted to retired qwen2.5-coder models after Blackwell migration; extended source‑lock guard.

**DOMAIN SPECIFICS**  
- Engines: Hookify, PlaywrightAutomationEngine, LocalEmbeddingEngine, SemanticAssetIndexEngine, MillPrintToProgramEngine, Ollama (gpt‑oss:120b, qwen2.5‑coder), Nomic‑embed-text, CostSavingsTrackerEngine, AccountingHardeningEngine, WEDMQuoteBridgeEngine, QuoteEstimatorEngine, DigitalTwinEstimator, BlueprintOCREngine, BridgeEngine.  
- Dispatchers/Routes: automationDispatcher.ts, millDispatcher.ts, ollama-cost-router.mjs, resolveSynthesisModel, detectHostClass, host-aware-synthesis-model, ask‑ollama.callOllama, cag‑router.hotSources, tribal-rerank.loadTribalIndex, embed-wiki-into-tribal-index, businessDispatcher, shopDispatcher, cost-bridge-dispatch.mjs, aiReasoningDispatcher.ts.  
- Metrics/Paths: `/api/tags` for installed Ollama models; `routeModelForTask`; GPU utilization 0–57 GB free; Ollama model load times; token generation rates; driver chunk success rate; Qdrant collections (`prism_engines`, `prism_skills`, `prism_formulas`); PSN snapshot; tribal‑embed-index.json (~160 MB); MCP port :3100 health; task health levels.  
- Paths: `H:/prism/ollama-resilient-pull.ps1`; `scripts/fix-hookify-frontmatter.mjs`; `scripts/remove-dangling-hook-refs.mjs`; `scripts/audit-unwired-engines.mjs`; `docker-service-health-check.mjs`; `singleton-service-guard.mjs`; `mcp-server-supervisor.mjs`; `stop_on_hook_unregistration.mjs`; `fleet-task-health-watch.mjs`; `scripts/lib/load-tribal-index.mjs`.

**TOOLS USED**  
Systematic Debugging skill, Hookify parser (`config_loader.py`), Unwired‑engine audit script, Fleet‑reaper sweep, Ollama resilient pull script, ollama-cost-router, Docker-service-health CLI, singleton-service-guard, MCP server supervisor/daemon, stop_on_hook_unregistration hook, fleet-task-health watch, Qdrant client, tribal index loader (`scripts/lib/load-tribal-index.mjs`), Vitest test harness (unit, dispatcher round‑trip, frontend), ultracode workflow, TypeScript compiler (`tsc --noEmit`), Jest + hermetic fixtures, Node shim `/h/.claude/bin/node`, portable-node, CronCreate for `/loop`, OLLAMA local inference.

**OPEN THREADS**  
- Resolve `routeModelForTask` logic to prioritize gpt‑oss:120b for search_synthesis on Blackwell.  
- Final verification that all 90 unwired engines are correctly triaged (wire, exempt, delete) by slot owners.  
- Confirm golf‑slot write‑allowlist bypass functioning in single‑chat sessions.  
- Re‑embed lost ~29 k tribal entries (currently at 4 k baseline); implement write‑side sharding of tribal index; migrate tribal collection from JSON to Qdrant when corpus stable.  
- Audit remaining raw‑string readers for fail‑open clobber patterns (none after audit).  
- Implement fleet‑task‑health disabled‑task suppression marker (`MIGRATION-FREEZE-ACTIVE.flag`).  
- Complete integration of U-QP-ACCOUNTING-WIRE: live ERP/QuickBooks feed.  
- Finish remaining fleet‑scope synergy clauses (20+ peer slots) via dispatch map completion.  
- Monitor potential synthetic promotion edge cases; further UI polish of margin‑floor warning banner if desired.
