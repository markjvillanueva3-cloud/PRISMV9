# shop-floor session 8b4b9149 (2026-06-10, 47.1MB, spine 511KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- 5ae481f748 – EPERM leak fix in `OutcomeCaptureBusEngine` (atomic append + retry).  
- 86f0e3fe0c – wired `SpeedFeedTriComparatorEngine`, `SpeedFeedExhaustiveCombinationEngine`, `SpeedFeedDownstreamSubscriberEngine` into `calcDispatcher`.  
- 16d6eecef4 – added `speed_feed_calibration_persist` engine, wiring & 18 round‑trip tests.  
- 890b2c39e7 – VC assessment spec addendum (`SFC-VC-ASSESSMENT-2026-06-08.md`).  
- 3438987f0e – hardened P2 findings + 5 regression‑lock tests.  
- f31398a1a5 – `SpeedFeedGpuJudgeEngine` engine, test & dispatcher wiring.  
- f5d14ddb29 – GPU judge hardening (peer files absorbed).  
- 951f5ac335 – polish GPU judge commit (timeout, matched_model note).  
- 3a1c20fca2 – full‑input sweep unit: `full` mode + `runStreaming`; 5 new regression locks.  
- af0ac16c5a – ReferenceError fix, ISO‑group claim lock, doc update.  
- b847c0f1ff – axis‑awareness enhancement spec.  
- 54c0ae06c8 – memory file spec addendum.  
- 28eca0ed84 – final GPU judge evidence write.  
- e9b68da865 – added `CANONICAL_TOOL_MATERIAL_SPEED_FACTOR` (carbide 1.0, HSS 0.35, ceramic/cbn/pcd/diamond 2.5).  
- 585584e3ae – wired `CoolantVcModifier` into `UltimateSpeedFeedEngine`.  
- 7d0affcae6 – de‑inlined rigidity→Vc multiplier to `constants.ts`.  
- f998f8af71 – fixed `U‑OSC‑ALTS‑FACTOR` propagation.  
- U‑OSC‑VENDOR‑COMPARE – comparison harness PRISM vs Sandvik/Kennametal/HSMAdvisor/G‑Wizard.  
- U‑OSC‑AXIS‑SPEC‑UPDATE – updated axis spec docs.  
- a257f872bb – AXIS‑LIVENESS‑COMPLETE (all 9 axes live).  
- 81e37ece16 – FINISH‑RA‑CAP (numeric `target_ra_um`).  
- 2070c472a4 – workholding.type → force‑cap.  
- 7dc7798033 – spindle.hp → power clamp.  
- 8b191d5427 – tool_holder.type runout → tool_life.  
- 6a3ad56545 – COMPARE‑TOOLMATERIAL‑VARIABILITY (576 comparisons).  
- c78faa5a73 – COMPARE‑HSS‑BASELINE (54 baseline points, +107.8 % delta).  
- 835df42c74 – COMPARE‑CERAMIC‑CBN‑BASELINE (12 new entries).  
- 4c544db4ae – COMPARE‑PER‑VENDOR (per‑source deltas).  
- 08d7fc6d37 – ALL‑AXIS‑SWEEP (2‑regime OAT, 16/25 axes live).  
- 907e74acab – FIXED‑HSS‑OVER‑SPEED (removed +108 % over‑speed).

**DECISIONS**  
- Adopt ultracode multi‑agent orchestration; use `/hermes-workflow` planner.  
- Run `/forge-audit‑v2` Boris discipline audit.  
- Replace `atomicAppend` with `fs.appendFileSync` + bounded retry to eliminate EPERM leaks.  
- Wire unwired engines via dispatcher enum/case pattern.  
- Exclude G‑Wizard from comparison; flag HSMAdvisor advisory only.  
- Add full sample mode to exhaustive combination engine; expose all selectable inputs.  
- Implement `runStreaming` generator beyond 50‑cap.  
- Create GPU judge (`SpeedFeedGpuJudgeEngine`) on RTX 6000 Blackwell; add safety gating S(x)≥0.98 and exact‑model matching.  
- Build axis‑awareness factor‑by‑factor: tool‑material → coolant → rigidity.  
- De‑inlined hardcoded constants for safety compliance; explicit‑only guard for inferred tool material.  
- Wire factors into alternatives (`U‑OSC‑ALTS‑FACTOR`).  
- Postpone MRR reconciliation and full rigidity‑DOC physics integration until reviewer‑gated unit.  
- Treat controller‑on‑default as product‑philosophy decision (keep inert).  
- Extend comparison sweep to vary `tool_material` across all vendor‑modeled materials; add per‑material summary.  
- Add numeric finish‑quality axis (`target_ra_um`).  
- Use existing runoutImpact.life_reduction_pct for runout→life.  
- Replace uniform `CANONICAL_TOOL_MATERIAL_SPEED_FACTOR` with sparse override table and widened clamp (0.10–8.0).  
- Implement two‑regime OAT sweep (rigid‑roughing + high‑RPM/finishing) to expose conditionally‑live axes.  
- Add `tool_life_min` to liveness checks.

**OPERATOR DIRECTIVES**  
- “Use ultracode and `/hermes-workflow`; give full breakdown of SFC calculator tech.”  
- “Give full path for assessment; reorientate with previous real sessions of Oscar and continue where left off.”  
- `/goal [Run calculations for every possible combination … compare ALL to G‑Wizard/HSMAdvisor]`.  
- Continue from where you left off.  
- `/loop — schedule recurring prompt interval 10 min.`  
- “Compare ALL possible calculations with max variability to G‑Wizard and HSMAdvisor.”  
- “Use best models now that we have Blackwell GPU.”  
- Map galaxy to `H:\PRISM\resources`.  
- Look into what Charlie was pulling for vendors.  
- AUTO‑FIX‑INLINE: any bug discovered must be fixed immediately, not deferred.

**FINDINGS/BUGS**  
- EPERM leak in `OutcomeCaptureBusEngine` → 11 888 orphan `.tmp` files (fixed).  
- Three engines dispatcher‑unreachable (`SpeedFeedTriComparatorEngine`, `SpeedFeedExhaustiveCombinationEngine`, `SpeedFeedDownstreamSubscriberEngine`).  
- G‑Wizard tool selector defaults to first tool; crib has 41 k geometry‑only rows, zero speed/feed → no contribution.  
- HSMAdvisor returns constant 634.6 m/min for all materials; only one open cut used and flagged unaligned.  
- Consensus computation incorrectly includes not‑aligned systems in median.  
- Tool material input inert; Vc always carbide‑anchored (missing multiplier).  
- Coolant type & holder type had no effect on output.  
- ISO‑group collapse: multiple materials within same ISO group produce identical Vc.  
- ReferenceError after switching to streaming (`cells.length`).  
- GPU judge false‑positive model matching fixed; exact‑model check added.  
- Full sweep revealed material‑dependent conservatism: conservative on non‑ferrous & steel, slightly aggressive on hardened.  
- PRISM under‑speeds published baselines by ~25 % average; worst case –53 % on 6061 aluminum.  
- Orchestrator dropped tool‑material, coolant, rigidity factors; default mode omitted these.  
- Inferred tool material caused aggressive over‑speeding; fixed with explicit‑only guard.  
- Coolant axis inert due to missing wiring; resolved by reusing `CoolantVcModifier`.  
- Rigidity factor inlined; de‑inlined for safety compliance.  
- PRISM over‑speeds HSS cast‑iron by +108 % (fixed).  
- PRISM fz uniformly +67–91 % higher than published sources (surfaced, not fixed).  
- 9 axes dead at baseline; after two‑regime sweep 16/25 live, remaining dead honest by design or optimizer internalized.  
- `controller_features` wiring gap identified (applies only in aggressive_rush mode).

**DOMAIN SPECIFICS**  
Engines:  
- `OutcomeCaptureBusEngine`, `SpeedFeedTriComparatorEngine`, `SpeedFeedExhaustiveCombinationEngine`, `SpeedFeedDownstreamSubscriberEngine`, `calcDispatcher`, `SpeedFeedCalibrationPersistEngine`, `SpeedFeedGpuJudgeEngine`, `UltimateSpeedFeedEngine`, `SpeedFeedNineAxisOrchestratorEngine`, `SpeedFeedBaselineComparatorEngine`.  

Algorithms:  
- `CoolantVcModifier` (coolant × ISO factor table), `HPCVcBoostCalculator`, `getToolMaterialSpeedFactor`, `getMultipliers`.  

Metrics:  
- Vc (m/min surface speed), S(x) gating, fz feed per tooth, MRR, tool_life_min, radial_depth_mm/pct, axial_depth_mm/pct, confidence intervals, ledger_rows_skipped, GPU residency, matched_model, verdict categories.  

Key paths:  
- `H:/prism/.claude/helpers/chat-slots.mjs`, `.claude/commands/checkin.md`, `calcDispatcher.ts`, `OutcomeCaptureBusEngine.ts`, `constants.ts`, `UltimateSpeedFeedEngine.calculate()`, `SpeedFeedNineAxisOrchestratorEngine.translateToUltimate()`, `BASELINE_DB` (keyed by `(iso, tool_material, diameter_bucket, operation, cut_type)`), per‑source breakdown.

**TOOLS USED**  
- `/hermes-workflow` dynamic planner, `/forge-audit‑v2` Boris discipline audit.  
- ultracode workflow engine, Node scripts (`chat-slots.mjs`, `tsx`).  
- `fs.appendFileSync` + retry logic.  
- PRISM dispatcher pattern (`calcDispatcher.ts`).  
- Vitest/Jest test harnesses, tsc compiler.  
- npx for script execution.  
- Ollama server (qwen2.5‑coder/vision) on RTX 6000 Blackwell.  
- CronCreate/CronDelete for recurring prompts.  
- Skill tool for slash‑command invocation.  
- Git tooling: commit, status, stash; hooks (`slot-bind-enforce`, `stop-hook`).  

**OPEN THREADS**  
- Finalize closed‑loop comparison driver script (G‑Wizard abstains, HSMAdvisor advisory).  
- Resolve or exclude HSMAdvisor alignment from consensus calculation.  
- Decide on auth provider (Clerk/Supabase/Auth0) to unblock revenue path.  
- Implement webhook signature fix for Stripe billing.  
- Add G‑Wizard tool selection logic in driver if crib gains sfm data.  
- Finalize physics for tool material speed factor (exact multiplier values).  
- Add coolant physics to affect Vc and feed.  
- Incorporate holder/machine/spindle/controller/workholding/insert physics.  
- Expand GPU judge to handle larger datasets efficiently.  
- Finalize safety gating thresholds and documentation.  
- Resolve peer‑file absorption issue in commit workflow.  
- U‑OSC‑RIGIDITY‑DOC: integrate rigidity into stability‑lobe analysis and DOC cap.  
- U‑OSC‑MRR‑RECONCILE: reconcile MRR factor interaction with new axis factors.  
- Wire remaining inert axes (holder, spindle, controller, workholding, insert) in orchestrator.  
- Vendor calibration for under‑speeding – extract speed/feed tables from vendor catalogs via vision OCR batch.  
- Full sweep vs G‑Wizard/HSMAdvisor once vendor calibration completes.  
- Controller default‑mode feed decision (product‑philosophy).  
- Live G‑Wizard/HSMAdvisor numbers – operator to run closed apps and export speeds.  
- Richer baseline OCR batch – Blackwell qwen2.5‑vl vision‑OCR over 287 catalog PDFs.  
- Comparison‑tool_material variability unit – pending implementation.  
- U‑OSC-DEAD-AXIS-TRIAGE: fix `controller_features` wiring gap, add `tool_life_min` to liveness.  
- U‑OSC-FZ-FORCE-VALIDATE: physics‑reviewer audit PRISM fz ≥ +67 % vs published sources.  
- Live G‑Wizard/HSMAdvisor calculator comparison remains operator‑gated.  
- OCR‑batch task stale warning (requires elevated shell).
