# shop-floor session 0e5669d2 (2026-06-10, 44.9MB, spine 372KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED (builds/commits)**  
- `168c202646` – sync‑resilient wrapper for sierra slot (`/checkin-sierra`).  
- `ea21008f25` – index‑meta refresh.  
- `8c4dff660a` – maint‑cron installers (promote & rot sentinel, disabled).  
- `984313825e` – link‑heal scorer fix + reason‑based gating; autoApplyCount dropped 14 100 → 2.  
- `838123429b` – tribal‑coverage refresh.  
- `45225e0cc3` – persistent context memory written to C: & H: mirrors, handoff file for sierra domain.  
- `d948b85a74` – hardened scorer (8‑char minLen floor, ratio guard).  
- `43e1b8e449` – portable‑node wrappers, MCP maxConnections 512, pressure status handling, GPU offload patterns, EXPECTED_UNREGISTERED_TASKS.  
- `3d796dcf5c` – fleet-task-health-watch.mjs: added EXPECTED_UNREGISTERED_TASKS, refined aggregateHealth, PRISM WSL Memory Guard.  
- `96ed5222e2` – reverse‑edge build (doc-nodes CLI + builder); 29 479 vault keys → 1 520 813 edges, 19.8 MB artifact.  
- `d856173b86` – staleness flag to doc‑nodes reader.  
- `0e2724871a` – regen-wire stage in regen-viz.mjs: auto-refresh of vault-backlinks.json on graph rebuild.  
- `14aba14e3a` / `4a44b5393c` – doc_nodes dispatcher action (wrapper, tests, enum case, schema).  
- `f23c58b42d` – access-map update: closed reverse-edge gap, fixed stale memory‑rag‑inject claim.  
- `2d49bf0d33` – canvas-read-lib.mjs + CLI subcommands (`canvas`, `canvas-doc`).  
- `47fe91554f` – access-map update: closed .canvas gap, fixed stale memory‑rag‑inject claim.  
- `7ec4a5ea02` – OLLAMA‑PIPELINE‑INJECTOR (wire orphan injector to expose local‑model routes).  
- `6fb278a2ee` – U‑TRIBAL‑QA‑LLM‑UNBLOCK (Ollama-powered Q&A path in `distill‑tribal.mjs`).  
- `65a29220e5` – U‑OLLAMA‑PREWARM‑WIRE (warm qwen2.5‑coder:32b during prompt→first‑tool window).  
- `f1b69db664` / `1ecf50b6be` – Memo‑Throttle & User‑Type fixes (per‑session rate file, transcript path, user/type capture, execSync injection fix).  
- `2c766b9308` – U‑NAV‑ACCEL‑GAP‑A‑FALSIFIED audit (avoid duplicate 3 300‑node generator).  
- `9ce1ea6633` – marked #5 SHIPPED in OLLAMA‑SYNERGY audit backlog.  
- `b5d249f4f5` – U‑WEEKLY‑SYNTH‑RESOLVER (host‑aware model selection, env‑pin override, timeout 180 s for gpt‑oss:120b).

---

**DECISIONS (architecture/scope + why)**  
- Force‑claim sierra slot via `/checkin-sierra` before any `/checkin`.  
- Persist audit context in sierra domain (`system-viz/MEMORY.md`, wiki, C:/H mirrors).  
- Wire `memory-rag-inject`; pre‑check injector disabled fleet‑wide.  
- Adopt “coverageComplete” flag only if needed; keep honest output with `processed/totalBroken`.  
- Accept partial candidates file (100 links) as honest; document OOM on full regen.  
- Cap hook heap to 384 MB (`NODE_OPTIONS="--max-old-space-size=384"`).  
- Raise MCP server `maxConnections` to 512 for future load.  
- Expand OFFLOADABLE_PATTERNS to include lint, classify, etc., boosting GPU offload coverage.  
- Add EXPECTED_UNREGISTERED_TASKS to suppress banner noise from intentionally unregistered vault crons.  
- Wire OLLAMA pipeline injector and prewarm hook immediately; use qwen2.5‑coder:32b as default grunt engine, gpt‑oss:120b for deep reasoning.  
- Declared Gap A duplicate (atomic generators already emit per‑file nodes); postpone its build.  
- Implement staleness flag in doc-nodes reader to detect drift.  
- Auto‑refresh vault-backlinks.json on graph rebuild (`regen-wire` stage).  
- Use per‑session rate limiting (`sessionRateFile(session_id)`) to avoid cross-chat throttling.

---

**OPERATOR DIRECTIVES (verbatim asks)**  
- “make sure obsidian vault is built and operational like all the x articles ive fed you.”  
- “if you lost context on it, use ultracode to find all obsidian related articles to regain context.”  
- “map paths to each obsidian vault node for cheaper token usage/ free token usage.”  
- “fill all gaps.”  
- “Validate that the commit‑cap fix holds and tidy missing‑task banner noise (completed).”  
- “Write handoff and recommend `/compact`.”  
- “Arm deferred items: restart MCP daemon for `maxConnections` 512, register vault crons if migration freeze lifted.”  
- “can we utilize ollama to code direct paths to each and every single file/nodes and wire them directly to relevant nodes for faster, more efficient searches and reads of the codebase when we utilize local llms and for claude to navigate the code base quicker.”  
- “what about using ollama for generating skill script hook combos for tool calls for it to use while navigating the code base, context invocation, knowledge system, so it builds to the best of its ability.”  
- “finish all ollama synergy tasks. utilize strongest possible viable ollama models for grunt work like searches, read, write and coding tasks that it's qualified for. | goal clear: wired, tested, validated and synergized across all galaxies and full h drive systems.”

---

**FINDINGS/BUGS**  
- `obsidian-memory-sync.mjs` had unguarded `writeFileSync` (line 342); missing export.  
- `WikiIndexMaintainerEngine.formatIndex` never invoked → stale frontmatter (`last_verified`, entry count).  
- Scorer over‑confidence: prefix/substring matches auto‑apply eligible; fixed by lowering threshold, adding reason‑based gating, 8‑char minLen floor, ratio guard.  
- Decay leak for slugs > 60 chars (score ≥ 0.85) – gated on reason.  
- Stale candidates file (`high:14100`) disarmed but only processed 100 links; full‑corpus regen OOM’d.  
- Overconfidence bug in scorer fixed by minLen floor & ratio guard.  
- False‑positive “MCP server failing” alert caused by commit‑charge overcommit; resolved by hook heap cap.  
- GPU offload coverage low (~8 %); widened patterns improved it.  
- Missing vault crons caused banner noise; suppressed via EXPECTED_UNREGISTERED_TASKS.  
- WSL memory guard task remained a real missing (not deferred).  
- vmmemWSL balloon (~95 GB) not respecting `.wslconfig` cap; resolved by `wsl --shutdown`.  
- Lgit layer regex missed alphabetic suffix, mis‑counted as “other”; fixed.  
- Stale memory‑rag‑inject claim in access map; corrected.  
- Peer contamination on shared tree during amend: attribution error but no data loss.  
- Duplicate injector – orphan hook existed but never wired; now activated.  
- gpt‑oss empty‑response due to `num_predict` starvation; resolved by raising ≥1024.  
- Fleet‑global 5‑min throttle bug; replaced with per‑session rate file.  
- Transcript size proxy bug fixed (read transcript path from stdin).  
- ExecSync injection vulnerability replaced by safe `callOllama`.  
- User/type mismatch in memo extraction corrected.

---

**DOMAIN SPECIFICS**  
- **Engines/Actions**: `WikiIndexMaintainerEngine`, `obsidian-memory-sync.mjs`, `wiki-link-fix-suggester.mjs`, `fleet-task-health-watch.mjs`, `doc_nodes` dispatcher, `canvas-read-lib.mjs`, `regen-viz.mjs`, `ollama-pipeline-injector.mjs`, `ollama-prewarm-on-pipeline.mjs`.  
- **Metrics**: `autoApplyCount` (2), `processed/totalBroken`, `coverageComplete`, commit charge 92.5 % → 70.5 %, free commit ~17 GB → 67 GB, reverse‑edge edges 1 520 813, canvas JSON files ~150 KB, node-card-offsets.json (~200 tokens).  
- **Paths**: `H:/prism/.claude/settings.json`, `C:/Users/.../.claude/settings.json`, `H:/prism-slot-sierra`, `node‑cards.jsonl` (160 MB), `vault-backlinks.json` (~20 MB).  
- **Unique to this galaxy**: reverse edge, canvas reader, staleness flag, auto-refresh of vault‑backlinks on graph rebuild, OLLAMA pipeline injector, prewarm hook, memo throttle/user-type fixes.

---

**TOOLS USED**  
- PRISM tools: `/checkin`, `/checkin-sierra`, `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
- Dispatchers/skills/hooks: PowerShell scheduled‑task installers, `memory-rag-inject`, pre‑check injector hook, c-to-h mirror hook, `fleet-task-health-watch.mjs`, `OllamaTaskOffloaderEngine.ts`.  
- Scripts/tools: `build-wiki-leaf-index.mjs`, `regen-wiki-from-viz.mjs`, `wiki-link-fix-suggester.mjs`, `ask-ollama.mjs`, `resolveSynthesisModel`, `system-viz-query.mjs`, `master-index-search-lib.mjs`.  
- Testing: Vitest, Jest/node test framework, tsc type‑check, scrutiny‑3way.mjs.  
- Build: esbuild, npm scripts (`npm run build`).  
- Slot management: `slot-bind-enforce.mjs`, `chat-slots.mjs`.  
- Review dispatch: automated 3‑of‑3 gate scripts.

---

**OPEN THREADS**  
1. Activate B3 maint‑cron installers (currently disabled).  
2. Address reviewer P1 on C2: short‑token Levenshtein over‑confidence (add min‑length floor & tie‑veto).  
3. Full‑corpus candidates regen still OOM; need streaming or larger heap approach.  
4. Optional: add explicit `coverageComplete` flag to candidates output for clarity.  
5. Restart MCP daemon to apply `maxConnections` 512 (deferred).  
6. Register vault crons if migration freeze lifted.  
7. Monitor WSL Memory Guard task registration; consolidate‑graph counter pending.  
8. Implement Gap B – Semantic Reranker after BM25.  
9. Scaffold generation of skill/script/hook triples via Ollama (`U‑OLLAMA‑FORGE‑ASSIST`).  
10. Remaining OLLAMA‑synergy backlog items (memo‑cache‑consolidate, wiki‑NLI‑lint).  
11. Address reviewer P2: guard against empty `.response` when `num_predict` unset for gpt‑oss:120b.  
12. Verify timeout adjustment fully covers 120 B model latency in production cron.
