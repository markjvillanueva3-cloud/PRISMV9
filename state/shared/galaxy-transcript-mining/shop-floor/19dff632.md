# shop-floor session 19dff632 (2026-06-10, 19.2MB, spine 139KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- PayrollLiabilityFilingEngine wiring: commits e44a3a1592 & e649790e76 added 5 dispatcher actions (`payroll_compute_940`, `payroll_generate_w2`, `payroll_reconcile_w2_941`, `payroll_contractor_1099_totals`, `payroll_remit_liability`); all 14 round‑trip tests pass.  
- Transcript miner script `scripts/mine-hotel-transcripts.mjs` (commit 9dfd621910) regenerated digests (`61518eb988`).  
- Hotel forge roadmap spec written to `state/shared/specs/HOTEL-FORGE-ROADMAP-2026-06-09.md` (099e6b92bd).  
- Verify‑first unit `U-HOTEL-P0-VERIFY-SHIPPED` committed f069052772 – all 6 “false gaps” live.  
- CustomerPortalEngine persistence: SQLite WAL, 4 durable maps → DB; rateBuckets stay in‑memory; 92 tests green (commits dd57b82b52 & 17f3e0ffec).  
- Allowlist write enable (`U-HOTEL-ALLOWLIST-WRITE-ENABLE`): manager/HR `handoff_*` actions dispatchable via per‑action role gate; financial/PII writes remain 403 (commits 18f37c812e & d8d2824cf2).

**DECISIONS**  
- Use Ollama (`qwen2.5-coder:32b`) for token‑efficient mechanical work; ultracode workflow for high‑level reasoning.  
- Adopt dependency‑ordered forge roadmap (verify → constants audit → mobile wire exemption → Q2S bug filing → false‑wire regression guard → portal persistence → allowlist/write review & realtime verify).  
- Commit convention: `[BOOTSTRAP-SLOT-ENFORCE]` for slot worktree, `[MAIN]` for shared tree.  
- Fold SQLite WAL into CustomerPortalEngine; keep rateBuckets in‑memory.  
- Reuse existing auth middleware (`requireRole`, `req.userRoles`) and global auditLog; per‑action role gate inside business/dispatch route, fail‑closed.  
- Exclude financial/PII write actions from allowlist.

**OPERATOR DIRECTIVES**  
- Completed “Read previous hotel sessions” via transcript miner.  
- Continue roadmap: U-HOTEL-P0-VERIFY-SHIPPED done → next build U-HOTEL-PORTAL-PERSISTENCE.  
- Use Ollama for high‑end work if possible; otherwise ultracode.  
- Operator approved role model → enabled `U-HOTEL-ALLOWLIST-WRITE-ENABLE`.  
- `/startup-hotel` invoked to claim slot and run startup pipeline (already completed).

**FINDINGS/BUGS**  
- Payroll wiring orphaned methods fixed; no duplicate engines.  
- Stale audit claim (“build a tax engine”) false – all tax engines wired.  
- Mobile engines: 6 wired, only `MobileVoice` exempt.  
- Q2S DFM bug confirmed and filed to charlie.  
- Guard regex missed “Unknown business action: X” string; fixed.  
- Marketplace lead get param mismatch corrected.  
- P0 guard bug detected by arm B resolved.  
- PTO double‑apply, empty `approved_by`, divergent PTO paths flagged for future fix.  
- Database files (`customer-portal.db` + WAL/SHM) added to `.gitignore`.

**DOMAIN SPECIFICS**  
- Slot: `hotel` – ERP/HR/business specialist, galaxy `business`.  
- Engines: `PayrollLiabilityFilingEngine`, `CustomerPortalEngine` (SQLite‑backed), `MarketplaceSeedingEngine`, `MobileVoice`.  
- Dispatchers: `prism_business` / `businessDispatcher`, `RealtimeEventBridge`.  
- Actions wired: payroll_compute_940, payroll_generate_w2, payroll_reconcile_w2_941, payroll_contractor_1099_totals, payroll_remit_liability; 17 read‑only allowlisted actions; write gated (`handoff_*`).  
- Paths: `/api/v1/business/dispatch`, `/api/v1/realtime/*`; audit paths `jm-population-status.md`, `jm-die-database/manifest.json`.

**TOOLS USED**  
- Ollama local LLM (`qwen2.5-coder:32b` for code, `gpt-oss:20b` summarization).  
- `scripts/mine-hotel-transcripts.mjs` extract‑summarize pipeline.  
- Ultracode workflow engine (`wf_2bfa0b6b-9b0`).  
- 3‑of‑3 scrutiny gate (reviewers A, B, C).  
- Git hooks: `slot-commit-enforce`, `bootstrap-slot-enforce`.  
- PRISM core: business dispatcher, realtime event bridge, auditLog middleware.  
- Testing: vitest isolate, jest‑style assertions, guard tests.  
- Build & CI: tsc, git hooks, cost‑router (`ask-ollama.mjs` with `qwen2.5-coder:32b`).  
- Database: better-sqlite3 in WAL mode.

**OPEN THREADS**  
- `U-HOTEL-FALSE-WIRE-REGRESSION-GUARD`: guard implementation and final validation pending.
