# shop-floor session 001bd6c3 (2026-06-10, 58.3MB, spine 523KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit 56aa423427 – Hermes set to Claude‑Code Opus 4.8 primary, local qwen2.5‑coder:32b fallback.  
- Commit c988a21ec4 – reverted boot‑loop config; migrated provider to custom (Ollama) + anthropic fallback.  
- Commits ab2ccf42a4/e6713584e2 – PRISM `/system‑viz` assets live (`generate-hermes-features.mjs`).  
- Docker Desktop engine started → Qdrant container `prism-qdrant` auto‑started (v29.4.3); healthy collections: prism_engines, prism_formulas, prism_skills.  
- Hermes gateway :9120; GUI launcher hermes‑setup.exe now boots.  
- Commit 28b72e4dee – MEMORY.md fill for all 34 galaxies; added `fill-galaxy-memory-sections.mjs`, test suite; vault mirror sync via `syncGalaxyMemories()`.  
- Commits 7d79f345c2, 1ab785c21d, 3ea4f40192, 2579da89a4, e6eba32eec – CLAUDE.md domain core, PSN edit, thin memory deletions, soul realignment, doc fixes.  
- Commit 822c48c55 – OLLAMA‑AUTORUN‑BUILD plan spec (`state/shared/specs/OLLAMA-AUTORUN-BUILDLOOP-PLAN-2026-06-09.md`).  
- Commits 3499f5f20f (U5b), d807d46f44 (U5c), a59f35221e (U6), cfda0e391a (U7), e17be56e82 (U10), 7771ca7f86 (U9) – Ollama tooling & hooks.  
- Commits 2340a2e699 (U1), a2756779c2 (U2), a60d7ba0bf (U3), 612418fde7 (U4), 3904b5e7b0 (U‑OAB‑120B) – offload router, route hook, banner fix, RTK compressor, fleet‑wide coding model default.  
- Merge commit 5a91ba1862 onto `cad-fusion-live-ms0` (net‑new subset only).

**DECISIONS**  
- Use existing :3100 MCP dispatchers for Hermes‑Vault read; avoid extra filesystem-MCP server.  
- Primary LLM: Claude‑Code Opus 4.8 with local qwen2.5‑coder:32b fallback to keep 5 h Max pool and billing safety.  
- Provider hierarchy: custom (Ollama) primary, anthropic fallback → later adopt gpt‑oss:20b as Hermes primary, upgrade to 120b when available.  
- Headless Zulu orchestration via `hermes -z "<prompt>"`; GUI optional.  
- Increase token budget for thinking models (gpt‑oss) to avoid “no final response”.  
- Qdrant required for semantic_search; must be Docker‑started and healthy.  
- Supervisor cwd fix reverted; root‑fix documented (`reference_mcp_cwd_convention_conflict_2026_06_08`).  
- OllamaTaskOffloaderEngine already exists; no new router needed. Auto‑route enabled by default; only offload hooks activated.  
- Un‑strand gpt‑oss:120b from FLOOR tiers, keep NUM_PARALLEL=2, MAX_LOADED=3.  
- Use gpt‑oss family for tool‑calling; qwen2.5‑coder reserved for coding offload.  
- Core residency mutex (U9) to serialize 120 B and 32 B model access.  
- Adopt GPT‑OSS‑120B as fleet‑wide coding-model default (`PRISM_CODEGEN_MODEL`).  
- Skip Kimi 2.6 cloud wiring due to data sovereignty.  
- Merge only net‑new commits; defer Ollama‑server‑env restart (#1).

**OPERATOR DIRECTIVES**  
- Start Qdrant via Docker (ensure :6333 up); verify semantic_search returns hits.  
- Restart Hermes: `taskkill /IM hermes-setup.exe /F && hermes -z "Check in as Zulu"`.  
- Verify MCP status: `curl http://127.0.0.1:3100/mcp/status`.  
- Monitor memory pressure; if >95 % commit, clear idle chat logs.  
- Audit each galaxy sequentially; use Obsidian + Ollama/Docker to fill MEMORY.md gaps.  
- Ensure Hermes settings and fleet orchestration fully functional; Zulu runs headless via PowerShell (`zulu-cli.ps1`).  
- Commit only to slot/bravo worktree; resolve golf git‑tree contention.  
- Run `/goal [ /loop [10m] assess ollama setup…`; aim to prove local LLMs auto‑run per task and inject into build pipeline with Claude review.  
- Complete RTK config (U5) and perform go‑live merge onto main.  
- Decide on Ollama‑server‑env restart (#1); evaluate Octopus live dispatcher activation.

**FINDINGS/BUGS**  
- Qdrant :6333 down → semantic_search empty; start Docker container.  
- Hermes HTTP 400: “Third‑party apps now draw from your extra usage” – enable Claude‑Code billing or fallback to local.  
- N:\ drive reference error fixed after MCP daemon restart (now uses H:).  
- master_index_query returns 20 hits; prism_knowledge:search empty but non‑critical.  
- Slot brief write/read/consume functional.  
- Docker Desktop engine missing caused Qdrant downtime until started.  
- Hermes init error “Unknown provider 'openai'” fixed by custom provider.  
- Qdrant container up but :3100 daemon cached “not connected”; restart needed.  
- 400 error from Claude due to exhausted extra‑usage balance (billing not enabled).  
- Headless harness returned “no final response” for gpt‑oss:20b; token budget insufficient.  
- Memory count inflation by node_* auto‑generated files; fixed and disclosed.  
- Corrupted ai-training_synthesis.md flagged for owner.  
- Rate limiting on Claude agents prevented multi‑agent workflow; switched to deterministic local stack.  
- Ollama GPU underutilized (~6%); gpt‑oss:120b not auto‑used; offload rate ~5%.  
- 34/34 gate non‑discriminating fixed with frontmatter galaxy tag, node_* filter.  
- Router and route hook referenced retired models → 0 % offload; CRLF flip corrected.  
- RTK config schema verified; live write works.

**DOMAIN SPECIFICS**  
- Dispatchers/actions: prism_memory, prism_session, prism_knowledge, prism_context; slot_brief_write, chat_post, slot_brief_list.  
- Paths: /checkin-bravo → /checkin; chat‑slots helper (H:/prism/.claude/helpers/chat-slots.mjs).  
- Metrics: OEE, travelers, dispatch queue, labor/clock tracking, work-orders, operator gates, setup sheets.  
- Hermes gateway :9120; config blocks model, fallback_model, mcp_servers.prism.  
- MCP :3100 (HTTP 405 alive); Qdrant :6333 collections prism_engines, prism_formulas, prism_skills.  
- Obsidian REST API :27123.  
- Galaxy layout: mcp-server/src/engines/<galaxy>/MEMORY.md, PATHS.md, TOOLBELT.md, CLAUDE.md; vault mirror at knowledge/memories/galaxies/<galaxy>/MEMORY.md.  
- Zulu orchestrator (zulu-orchestrator-sweep.mjs) sends SendKeys to PRISM windows; zuluOptIn empty.  
- Engines: ModelRoutingEngine, AISystemRouterEngine, OllamaTaskOffloaderEngine, OllamaTaskOffloaderEngine.ts.  
- Actions/dispatchers: local‑llm-task-router.mjs, ollama-cost-router.mjs, ask-ollama.mjs, PreToolUse hook (ollama-route-pretooluse.mjs).  
- Metrics: GPU utilization (~6% baseline), offload rate (~5%), VRAM usage per model, take‑rate banner.

**TOOLS USED**  
- PRISM CLI (/checkin, /checkin-bravo); chat-slots.mjs; audit-roadmap-drift.mjs.  
- Docker Desktop + docker-compose (docker-compose.yml).  
- Hermes CLI (`hermes config migrate`, `hermes -z`).  
- Qdrant vector store.  
- Prism MCP server.  
- Obsidian local REST API plugin (`obsidian-local-rest-api`).  
- Claude Code CLI; Ollama local stack via Docker.  
- Node scripts: syncGalaxyMemories(), galaxy-synthesis-refresh.mjs, fill-galaxy-memory-sections.mjs, ollama-tool-agent.mjs, ollama-offload-enforce.mjs, ollama-codegen.mjs, ollama-build-loop.mjs, ollama-capability-probe.mjs, ollama-coresidency.mjs, local-llm-task-router.mjs, ollama-cost-router.mjs.  
- Ultracode workflow engine (assessment, planning).  
- Git (branch, cherry‑pick, merge); Git worktree isolation hooks.

**OPEN THREADS**  
- Start Qdrant Docker container; verify semantic_search hits; restart :3100 MCP after start.  
- Confirm Hermes fully operational: send test prompt; ensure fallback to local if Claude billing blocked.  
- Monitor memory usage; clear idle chat logs if >95 % commit.  
- Re‑enable disabled Hermes‑Obsidian Bridge scheduled task; regenerate corrupted ai-training_synthesis.md via owner workflow.  
- Schedule periodic syncGalaxyMemories() to prevent drift.  
- Build system to auto‑inject LLMs into build pipeline with Claude review gate (U7).  
- Optimize Ollama PC settings/hardware usage; increase GPU utilization, NUM_PARALLEL=2, MAX_LOADED=3.  
- Integrate VLM/OCR ensemble, embeddings, RTK summarization, Octopus multi‑model consensus.  
- Complete RTK config (U5) and perform go‑live merge onto main; wire settings.json for auto‑route hooks.  
- Decide on Ollama‑server‑env restart (#1); evaluate Octopus live dispatcher activation (`PRISM_OCTOPUS_LIVE_DISPATCH=1`).  
- Upgrade RTK via LLM/hardware leap (RTK upgrade sweep #11).  
- Refine routing config: adjust NUM_PARALLEL, MAX_LOADED, keep‑alive settings.  
- Optimize offload heuristics (dynamic threshold, regex handling).  
- Clean up stale model references; remove retired models from router and route hook.
