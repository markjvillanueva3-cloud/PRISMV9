# wedm session 8b4b9149 (2026-06-09, 38.2MB, spine 432KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5ae481f748` – EPERM rename bug in `OutcomeCaptureBusEngine`; atomic `fs.appendFileSync`, retry, orphan‑tmp cleanup.  
- `86f0e3fe0c` – wired tri‑vendor comparator (`speed_feed_tri_compare`) & exhaustive combiner into dispatcher; 16 round‑trip tests.  
- `a2dbfa76e1` – closed‑loop driver (rescued 7 SF engines, fixed live‑caught consensus bug).  
- `43e1b8e449` – G‑Wizard alignment symmetry (R7 resolution).  
- `891c66e728` – full‑sweep comparison driver; corrected material map (added N/H groups, fixed misclassification).  
- `16d6eecef4` – `SpeedFeedCalibrationPersistEngine`; dispatcher wiring & unit tests.  
- `890b2c39e7` – VC assessment spec addendum (closed‑loop status).  
- `3438987f0e` – calibration engine hardening: clamped flag fix, `ledger_rows_skipped`, tmp cleanup on rename failure.  
- `f31398a1a5` – `SpeedFeedGpuJudgeEngine`; GPU‑resident LLM judging support.  
- `f5d14ddb29` – GPU judge hardening: probe‑GPU residency prefix bug, `matched_model`, silent‑exclusion warning, fetch timeout.  
- `951f5ac335` – polish commit (refactor, test lock‑ins).  
- `3a1c20fca2` – full‑input sweep (`full` mode, `runStreaming`, 69 120 cells).  
- `af0ac16c5a` – scrutiny fixes: removed `cells.length` ReferenceError, ISO‑group collapse docs.  
- `b847c0f1ff` – axis‑awareness scoping spec.  
- `54c0ae06c8` – VC assessment spec addendum (GPU‑judge findings).  
- `28eca0ed84` – memory file recording GPU‑judge evidence.  
- `658c8280fe` – canonical tool‑material speed‑factor table (`CANONICAL_TOOL_MATERIAL_SPEED_FACTOR`).  
- `585584e3ae` – wired existing `CoolantVcModifier`.  
- `7d0affcae6` – de‑inlined rigidity factor to `constants.ts`.  
- `f998f8af71` – hoisted multipliers into primary Vc path & alternatives.  
- `da41a58fd1` – full 102 060‑combination sweep (calc half).  
- `c48829e331` – axis‑liveness probe (`Vc/RPM/feed/MRR`).  
- `d03458fff1` – extended probe to include tool_life.  
- `2070c472a4` – workholding.type → force cap (`ClampingForceEngine`).  
- `7dc7798033` – spindle.hp → power clamp (physics‑reviewed).  
- `8b191d5427` – tool_holder.type runout → life (`runoutImpact`).  
- `0a146e22c6` – controller hygiene fix (removed inlined constant).

**DECISIONS**  
- Adopt ultracode multi‑agent workflow (`/hermes-workflow`) for assessment; fan‑out + adversarial verification.  
- Replace costly read‑rewrite with atomic append, bounded retry; remove orphan tmp files.  
- Expose dispatcher actions: `speed_feed_tri_compare`, `speed_feed_exhaustive_combine`, `speed_feed_downstream_packs`.  
- Closed‑loop test must use live G‑Wizard crib & HSMAdvisor; present honest alignment flags, abstention reasons.  
- Consensus filter: exclude not‑aligned externals only when better data exists (R7).  
- Add G‑Wizard alignment check mirroring HSMAdvisor logic.  
- Correct material‑ISO mapping: aluminum → N, stainless → M; add missing N/H groups.  
- Calibration persist engine design: per‑ISO/mode factors, flag `INCVC`, advisory‑only apply (operator gated).  
- Two‑layer closed‑loop: CPU calibration (`SpeedFeedCalibrationPersistEngine`) + GPU judge (`SpeedFeedGpuJudgeEngine`).  
- Expand sweep to full SFC app‑page input space; replace capped slice with streaming generator.  
- ISO‑group collapse logic: tool material inert, documented and locked.  
- Enforce strict GPU residency checks (`matched_model`, `gpu_resident`), timeout handling.  
- Add canonical `CANONICAL_TOOL_MATERIAL_SPEED_FACTOR`; use existing `CoolantVcModifier`; de‑inline rigidity factor; propagate multipliers to alternatives.  
- Treat inert axes as separate physics units; ship each with reviewer, test, live validation.  
- Keep controller axis inert by default; apply safe MRR adjustment only if explicitly enabled.

**OPERATOR DIRECTIVES**  
- “Reorientate with previous real sessions of oscar and continue where we left off.”  
- “Give full path for assessment.” (answered).  
- “Run calculations for every possible combination … compare ALL to G‑Wizard & HSMAdvisor.”  
- Populate computed S/F per tool in G‑Wizard UI before next session.  
- Run Blackwell `qwen2.5-vl` OCR batch over vendor catalog PDFs.  
- Verify galaxy mapping to `H:\PRISM\resources`.  
- Investigate “Charlie” vendor catalogs.

**FINDINGS/BUGS**  
- EPERM rename failure caused orphan `.tmp`; fixed.  
- Dispatcher wiring for `SpeedFeedDownstreamSubscriberEngine` incorrect – zero‑arg getters replaced with keyed cache API.  
- G‑Wizard tool selector defaulted to first crib tool; crib contains no speed data → comparator always returns same tool.  
- HSMAdvisor publishes single open cut (634.6 m/min) for all materials; engine flags unaligned but still includes in consensus median.  
- Consensus calculation incorrectly incorporates not‑aligned external data, producing impossible medians.  
- G‑Wizard `toolcrib.csv` geometry‑only → 0 matched cells.  
- Vendor catalogs are font‑encoded PDFs; plain text extraction fails.  
- Calibration factors would increase Vc for 8/12 regimes → unsafe if auto‑applied.  
- P2 clamped flag false‑positive; skipped_lines observability missing.  
- ReferenceError: `cells.length` after switching to streaming.  
- ISO‑group collapse: tool material variations produce identical Vc (tool_material inert).  
- GPU judge probe‑GPU residency prefix bug, silent‑exclusion warning absent, no fetch timeout.  
- Axis‑liveness probe revealed 8 live axes; 4 inert: spindle.hp, workholding.type, tool_holder.type, controller.  
- RunoutImpact existed but never applied to life; fixed by applying reduction factor.

**DOMAIN SPECIFICS**  
- Engines: `SpeedFeedTriComparatorEngine`, `SpeedFeedExhaustiveCombinationEngine`, `SpeedFeedDownstreamSubscriberEngine`, `SpeedFeedBaselineComparatorEngine`, `SpeedFeedPropagationBridgeEngine`, `SpeedFeedOutcomeFeedbackBridgeEngine`, `SpeedFeedDeepLearningEngine`, `SpeedFeedCalibrationPersistEngine`, `SpeedFeedGpuJudgeEngine`, `UltimateSpeedFeedEngine`, `SpeedFeedNineAxisOrchestratorEngine`, `ClampingForceEngine`.  
- Actions/dispatchers: `speed_feed_calibration_persist`, `speed_feed_gpu_judge`, `speed_feed_tri_compare`, `speed_feed_exhaustive_combine`, `speed_feed_downstream_packs`.  
- Metrics: `Vc_mpm`, consensus median, delta percentages, `INCVC` flag, `gpu_resident`, `vram_used_mib`, `matched_model`, `ledger_rows_skipped`, `skipped_lines`.  
- Paths: `/mcp-server/src/engines/speed-feed/`, `/src/tools/dispatchers/calcDispatcher.ts`; ledger at `state/outcomes/sfc-full-sweep-ledger.jsonl`; model output at `state/outcomes/sfc-calibration-model.json`.  
- Physical constants: `CANONICAL_TOOL_MATERIAL_SPEED_FACTOR` (carbide = 1.0, HSS ≈ 0.35, ceramic/CBN ≈ 2.5), coolant modifiers, rigidity factor in `constants.ts`.

**TOOLS USED**  
- PRISM wire‑EDM tools: Wire Wizard, multi‑pass skim, wire‑break prediction, flush, recast/HAZ, tech‑tables, E‑code families.  
- Internal tooling: dispatcher (`calcDispatcher.ts`), engines above, CronCreate for recurring goals.  
- Scripts/hooks: `update-prism-inventory.mjs`, `build-state-snapshot.mjs`, `system-viz-query.mjs`, node‑splice with `CONFIRM_CRITICAL=true`.  
- Test harnesses: Vitest, `calcDispatcher.uwire-sfc-batch1.test.ts`, `sfc-full-sweep-compare.mjs`.  
- External: Ollama vision‑OCR (`qwen2.5-vl`), Blackwell RTX 6000 GPU.

**OPEN THREADS**  
- Finalize and run closed‑loop driver against live G‑Wizard crib & HSMAdvisor; present honest alignment flags, abstention reasons.  
- Apply calibration to live PRISM SFC (`PRISM_SFC_CALIB_APPLY`) – operator gating & integration pending.  
- Verify G‑Wizard baseline extraction (currently geometry‑only); document limitation or add UI for speed entry.  
- Expand vendor baseline DB to include N/H groups; run vision‑OCR batch on catalog PDFs.  
- Scale GPU judge to handle full 69 k sweep without OOM (caching, batching).  
- Validate safety gating (`increases_vc` flag) for newly added axes before operator approval.  
- Reconcile MRR behavior after applying factors to alternatives (`U-OSC-MRR-RECONCILE`).  
- Implement DOC calculation using rigidity and holder/spindle stiffness (`U-OSC-RIGIDITY-DOC`).  
- Finish remaining inert axes (holder type, spindle controller, workholding, insert).  
- Decision on controller philosophy (default‑mode feed adjustment).  
- Execute multi‑hour GPU OCR batch for richer baseline.
