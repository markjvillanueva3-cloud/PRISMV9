# speed-feed session 16769ed0 (2026-06-21, 28.4MB, spine 282KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U‑REWRITER‑SKIP‑LOOP‑DIRECTIVES – commit 6a7b572eae (skip operator directives before Ollama round‑trip)  
- U‑REWRITER‑CHATMODEL‑SELECT – commit 778be5414f (`ollama-loaded-chat-model.mjs` helper; fixes gpt‑oss/deepseek mis‑selection, vision‑model leakage)  
- U‑REWRITER‑FALSE‑WARM‑COMMENT‑FIX – commit fee67d7760 (removes false “24h keep_alive” claim)  
- Feature‑routing “both” arc shipped (live router → Sonnet, octopus picker, graph reconcile)  
- 1c6abe2878 – U‑LOADED‑CHAT‑STRICT‑OPTION: `opts.strict` added to `pickLoadedChatModel`; 17/17 helper tests + 9/9 rewriter regression PASS  
- 70b94eb1c9 – U‑OLLAMA‑PS‑PROBE‑DEDUP: consolidated `/api/ps` probes into `scripts/lib/ollama‑ps‑probe.mjs`; 15/15 tests PASS  
- e0b3df1ea0 – U‑MCP‑ROUTE‑SUPPRESS‑ISVERBOSEBASH: reversible static suppress for `isVerboseBash` nudge (417 fires / 0 takes); 11/11 new tests + 29/29 regression PASS  
- d3e0b7ebaf – U‑PRUNE‑TAG‑SHARED‑CACHE: replaced generic `pruneExpired` with per‑tag `pruneTag`; migrated all 12 evictors; fixed token leak; 35/35 tests PASS  
- 5be19a26c2 – audit‑mcp‑route‑takerate legend update (R12)  
- 0aab43dadc – audit‑viz‑legend docstring sync (R16)  
- 8d344941fe – audit‑viz‑first injection dedup added, `pruneTag` introduced  
- 4307ece067 – U‑PSN‑CHECKLIST‑TEST‑ENABLED: pin `{enabled:true}` in `shouldInject`; resolved env‑dependent failures; 16/16 tests PASS  
- **Parked**: `scripts/ask‑ollama.mjs` (loaded‑first wiring) remains uncommitted

---

**DECISIONS**  
- Close rewriter revival loop; focus on token‑efficiency units.  
- Ship only self‑contained helpers; leave ask‑ollama wiring to zulu.  
- Deduplicate `/api/ps` probes into shared lib.  
- Suppress `isVerboseBash` nudge per low‑take‑rate doctrine (417 fires / 0 takes).  
- Do not auto‑compact during this session; re‑claim alpha slot to restore normal compaction flow.  
- Keep route‑suggest take‑rate subsystem unchanged; do not relabel `suppress‑candidate`.  
- Add `pruneTag` to `injection-dedup.mjs`, replace all 10 evictor calls, keep legacy `pruneExpired` with warning.  
- Do not start prune‑TTL fix at YELLOW token zone (risk of RED).  
- Use per‑tag pruning to avoid cross‑TTL evictions; test env‑captured flags explicitly.

---

**OPERATOR DIRECTIVES**  
- Finish any in‑flight unit, then descend NEVER‑IDLE ladder: own‑domain leftovers → FIXES → WIRINGS → GHOST builds → backlog.  
- Never idle while token budget green; checkpoint at YELLOW and let auto‑compact reset.  
- Use PC‑health monitor (CPU / RAM / GPU / node / bash / claude counts); throttle if RAM>88 %, node>250, bash>120, GPU>93 GB, CPU>90 %. Idle when dry on safe work and health OK.  
- Commit with `[MAIN‑FORCE]` on cad‑fusion‑live‑ms0; by‑pathspec only (no `git add -A`).  
- Update handoff after each unit; record a memory.  
- Reorientate reading sessions from 6/09–6/19; build backlog in order: own‑domain leftovers → slot‑task → FIXES → WIRINGS → GHOST → MISC.  
- Re‑claim alpha slot to restore self‑compact flow.

---

**FINDINGS / BUGS**  
- Rewriter dead due to `pickModel` selecting vision models or rejecting gpt‑oss/deepseek; added skip for loop directives and bound regex slice.  
- False warm‑comment in rewriter hook corrected.  
- Self‑compact fails at YELLOW (race with fast loop); slot released (`"alpha": null`) – re‑claiming restores behavior.  
- ask‑ollama wiring conflicts with zulu’s live edits → cannot commit.  
- Linter false positives: unreachable-after-return, finally after return, commented‑out code block warnings.  
- `isVerboseBash` nudge net‑negative (417 fires / 0 takes); suppression needed.  
- Audit logic mislabels `verify‑wiring` classifiers; should be `retune`.  
- R8/R9 traps: suppress‑candidate relabel would break tests; non‑dominant → verify‑wiring intentional.  
- Large‑read‑digest advisory muted (`decayDecision` fire:false, muted:true).  
- audit‑viz‑first injection lacked dedup guard – caused repeated 8 s subprocesses.  
- `pruneExpired` evicted higher‑TTL entries; replaced by `pruneTag` preserving per‑tag TTLs.  
- Token leak from `pruneExpired`; fixed with `pruneTag`.  
- Env‑dependent test failures in psn‑prompt‑checklist resolved by pinning `{enabled:true}`.  
- Untracked core lib `injection-dedup.mjs` caused latent dependency issues.  
- Dark ledger for `read-auto-limit` frozen since 2026‑05‑26; re‑lighting required real hook run to append entries.  
- Aggregator correctly maps `nudge-emitted` → hit with zero `savedTokens`.

---

**DOMAIN SPECIFICS**  
- **Engines/dispatchers:** Sonnet, Qwen3‑coder, Qwen2.5‑coder, Ollama offload (`ask‑ollama.mjs`), prompt‑rewriter hook, hermes agents, octopus consensus, Opus reasoning.  
- **Hooks/scripts:** `prompt-rewriter-ollama.mjs`, `ask-ollama.mjs`, `ollama-prewarm-on-pipeline.mjs`, `ollama-pipeline-injector.mjs`, `mcp-route-suggest` (`route‑suggest-decay.mjs`), `injection-dedup.mjs`.  
- **Metrics/paths:** Token zones (GREEN/YELLOW/RED), health monitor (`node scripts/fleet-survival-status.mjs`), audit‑unwired‑engines, system‑viz query, vault‑health, handoff files (`HANDOFF‑claude‑…`).  
- **Injection‑dedup cache:** shared JSON, 12 evictors, per‑tag TTLs (5 min–24 h).  
- **Ledger:** `read-auto-limit-ledger.jsonl`; writes only on real runs.  

---

**TOOLS USED**  
- PRISM SFC 9‑axis physics constants, chip‑load calculations.  
- Scripts: `node scripts/fleet-survival-status.mjs`, `self-compact.mjs`, `pruneTag` lib, `ollama‑ps‑probe.mjs`.  
- Git workflow: `[MAIN‑FORCE]` on cad‑fusion‑live‑ms0, by‑pathspec commits.  
- Reviewers & code‑analyzer (2‑arm scrutiny).  
- Octopus consensus, Hermes sonnet agents for parallel evaluation.  

---

**OPEN THREADS**  
1. **Re‑light dark ledger unit:** refactor `read-auto-limit.mjs` hook, add tests, commit.  
2. **Ask‑ollama loaded‑first wiring:** await zulu’s merged commit (wiring + codegen mode).  
3. **Self‑compact reliability:** confirm SendKeys actuation works after slot re‑claim; monitor for future failures.  
4. **Low‑take‑rate offenders:** target next highest fire counts (`isLargeRead`, `doctrineSurface`, `backendAuditChain`) with credit‑path verification before suppression.  
5. **Fleet offload‑warmth unit:** build small coder resident, obtain policy sign‑off.  
6. **Next high‑ROI token‑efficiency unit** after rewriter arc closure.  
7. **Resolve self‑compact race condition** or enforce manual `/compact` at YELLOW.
