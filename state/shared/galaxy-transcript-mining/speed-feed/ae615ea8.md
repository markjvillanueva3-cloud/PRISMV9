# speed-feed session ae615ea8 (2026-06-10, 40.6MB, spine 268KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑CIMCO‑SIM‑4` – `cimco-bind-gate.mjs` (machine, VR‑not‑NGC, 25.4× units, EDM route)  
- `U‑CIMCO‑SIM‑5` – `assessRunCompleteness` (terminal‑complete, quiescence, coverage, modal watcher)  
- `U‑CIMCO‑SIM‑6` – `assessLiveRunClearance` 5‑gate combiner (`bindVerdict`, `runComplete`)  
- `U‑CIMCO‑SIM‑7` – `cimco-sim-fleet.mjs` (planFleet() from `cimco-nav-planner.mjs`)  
- `U‑CIMCO‑SIM‑1A` – read‑report driver + Node normalizer (`cimco-report-normalize.mjs`)  
- Echo Forge Roadmap v2 (`state/shared/specs/ECHO‑FORGE‑ROADMAP‑2026‑06‑09.md`)  
- PrismCimcoUI.exe built via `build.ps1` (.NET Framework csc.exe, Accessibility.dll)  
- `U‑CIMCO‑FSM‑LIVE‑DRIVE` (commit dba50eb3da)  
- CLEARANCE‑COMPOSE (`composeClearanceInput`, commit e482189325)  
- SIM‑AUTONOMOUS‑PROOF – single‑process `invoke-read` (commit 40cf2e0d3b + witness 43e5309554)  
- All‑15 JM fleet closed‑loop sweep – PowerShell `cimco-fleet-sweep.ps1`, results `fleet-drive-results.json` (commit a28927fc0b)  
- Pre‑config capability (`--pre <cfg>`) commit e276f13216  
- Full CIMCO tailoring spec commit af005aa2d7  
- R14 spec fix commit c05b697d77  
- Crash‑safe Win32 enum for read-window commit 1672656ada  
- Safe‑by‑default set-setting write op commit 795df9573e  
- Combo-read support CB_GETLBTEXT commit 1090ae5055  
- List-windows shipped commit 39508c2774  
- P1 skip MSAA ribbon walk commit 96bf5a573e  
- About‑verify CIMCO Edit 26.01.10 active web license commit f426d4a18a  
- Setup-pages mapping all 23 CIMCO pages commit 2322f566b3  
- Machine-config model correction doc commit 78d3580c59  

**DECISIONS**  
- Adopt Ollama pre‑flight (qwen2.5-coder) + 3-of-3 Claude scrutiny via `.claude/scripts/scrutiny-3way.mjs`  
- Use ultracode workflow engine (4 agents) for synthesis  
- Prioritize `U‑CIMCO‑SIM‑1A` before live simulation  
- Compile PrismCimcoUI with csc.exe, no SDK; use Win32-only message automation to avoid MSAA crashes  
- Commit atomically (`git commit --only <path>`) to guard against torn commits  
- Single-process C# op `invoke-read` replaces two‑process attach for reliability  
- Drive each JM machine via PowerShell per-machine (`cimco-fleet-sweep.ps1`)  
- Incremental flush + resume cursor in `cimco-fleet-drive.mjs`  
- Keep 5‑gate clearance dispatcher separate; compose input via `composeClearanceInput`  
- Closed-loop testing scope: 15 JM machines (12 simulatable lathe/mill, 3 EDM)  
- Scrutiny gate: 2‑arm code analyzer + reviewer per file, final 3-of-3  
- Loop discipline: closed-loop eval-gate each iteration; budget stop condition  

**OPERATOR DIRECTIVES**  
- Use Ollama to generate brainstormed forge roadmap (Echo Forge Roadmap v2)  
- Begin closed‑loop testing of post processors using CIMCO as editor and simulator  
- Activate simulation add‑on, tailor all CIMCO settings (collision check, stock, machine config)  
- Proceed with `/yolo-mode` for rapid changes to enable add‑on  
- Complete all CIMCO tasks; use Ollama for searches, read/write/coding  
- If finished during night, build posts for highest-selling machines globally  

**FINDINGS/BUGS**  
- Torn commit removed `read-report`; fixed with `git commit --only`  
- Normalizer bug: wrong grid column mapping & empty‑array handling corrected  
- Driver bug: missing value extraction for grid cells; fail‑closed ladder added  
- Dispatcher registration: `prism_pp` dispatcher commented → post surface dark, now enabled  
- Empty report parsing accepted empty array – fixed to reject  
- Stale UIA assumption operator-opened CIMCO required – superseded by MSAA driver that can launch cold  
- Orphan-kill bug: PrismCimcoUI.exe left resident; killed in `killCimco()`  
- Two‑process attach issue solved with single-process chain  
- Header-only report mislabeling fixed to distinct `report-header-only`  
- Reaper killed long node driver (~125 s) – resolved by PowerShell sweep (node never orphaned)  
- Old MSAA read-window crashed; replaced by crash-safe Win32 enum  
- Set-setting initially reported orphan dialog & intent vs actual state – fixed via WM_CLOSE fallback  

**DOMAIN SPECIFICS**  
- SIM‑4 bind gate: `cimco-bind-gate.mjs` verifies machine, VR‑not‑NGC, 25.4× units, EDM route  
- SIM‑5 run‑complete gate: `assessRunCompleteness` (terminal‑complete, quiescence, coverage, modal watcher)  
- SIM‑6 assessLiveRunClearance: 5‑gate combiner (`bindVerdict`, `runComplete`)  
- SIM‑7 fleet rollup: `cimco-sim-fleet.mjs` using `planFleet()` from `cimco-nav-planner.mjs`  
- PrismCimcoUI.exe MSAA/IAccessible driver: map/find/invoke/window-info, get_accValue  
- Node normalizer: `cimco-report-normalize.mjs` → `{line,type,description,action}`  
- CronCreate schedules recurring `/loop` jobs  
- Engines/Actions: `invoke-read`, `navigateLive`, `probeLive`, `assessReadReport`, `FireControl`, `list-windows`, `setup-pages`, `read-setting`, `set-setting`, `combo-read`  
- Dispatchers/Metrics: `cimco_live_run_clearance` 5‑gate, `composeClearanceInput`, `driveLiveFsm`  
- Paths/Files: `state/shared/cimco/live-report-structure.json`, `fleet-drive-results.json`, `cimco-fleet-sweep.ps1`, `cimco-fleet-drive.mjs`  

**TOOLS USED**  
- CronCreate  
- Ollama local models (qwen2.5-coder:32b, gpt-oss:120b)  
- Ultracode workflow engine  
- Git (`git add`, `commit --only`, `show --stat`)  
- Node test harness (`node:test`, `probeEnv(parseArgs(...))`)  
- PrismCimcoUI build via `csc.exe` (.NET Framework 4.0.30319) + Accessibility.dll  
- PRISM helpers: `/startup` pipeline, `chat-slots.mjs`, `ask-ollama.mjs`  
- Node scripts: `cimco-fleet-drive.mjs`, `driveLiveFsm.js`  
- PowerShell sweep (`cimco-fleet-sweep.ps1`)  
- Win32 APIs: EnumWindows, SendMessage, GetClassName, GetWindowText, GetDlgCtrlID, TVM_GETNEXTITEM, TVM_SELECTITEM, BM_GETCHECK, BM_CLICK, CB_GETCURSEL, CB_GETLBTEXT, CB_GETLBTEXTLEN, GetWindowLongPtr  
- `.claude/scripts/scrutiny-3way.mjs`  
- Hermes agentic coding capabilities  

**OPEN THREADS**  
- Operator-gated SIM‑1 live report-grid de-risk (needs opened CIMCO Edit session)  
- Live simulation run on VMC‑01 once CIMCO open  
- Final driver wiring for `U‑CIMCO‑FSM‑LIVE‑DRIVE`  
- Load machine-specific `.mcfg` files into CIMCO for collision detection  
- Create known-bad NC to exercise collision-row extraction & verify `report-header-only` handling  
- Supply per-machine production NCs (inch vs mm) validation  
- Finalize enabling simulation add-on settings (collision & limit errors, stock, machine config) and confirm reports contain collision rows  
- Implement combo-write capability (`CB_SETCURSEL`)  
- Build load-machine op (select Backplot Setup page, apply combo values, click OK, validate over-travel NC → limit row)  
- Finalize Task #5/#6 post‑processor closed-loop sweep once machine load functional  
- Generate posts for highest-selling machines after successful runs  
- Integrate Ollama and Hermes into loop workflow  
- Finalize UI driver to support all required interactions without crashes
