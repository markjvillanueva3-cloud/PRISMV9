# speed-feed session ec0368b3 (2026-06-23, 20.1MB, spine 148KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `db05d65c8f` – SFC accuracy auditor (11 M‑row corpus validated).  
- `e0003bce57` – Closed‑loop tests for physics core (`sfc-jm-fleet-closed-loop.test.ts`).  
- `a531c2ee8f` – Safety‑bug fix in `ProductEngine.sfcCalculate` (over‑power detection tightened).  
- `e865ea83a7` – Added “danger” tier (>150 % over‑power) to page test suite.  
- `05e08b4702` – Material‑aware Vc/fz, rpm clamp, ISO‑group handling, category→grade aliasing in Codex SFC page (`ProductEngine.sfcCalculate`, `ManufacturingCalculations.calculateSpeedFeed`).  
- `fa6a037974` – Validated 4 codex panels; discovered engagement‑arc doubling bug.  
- `247c5856f2` – Fixed engagement‑arc doubling (`φ` instead of `2φ`), decoupled max/avg chip formulas, updated tests.  
- `76154a3ea6` – Corrected surface‑finish Ra (per‑tooth Brammertz, removed `fz×teeth`).  
- `4ad8a0116b` – Replaced inline MATERIAL_HARDNESS with canonical constants; tool life now ~9 min (vs 2.2 min).  
- `U-SFC-OVERPOWER-SPINDLE-EFF` – Added SPINDLE_DRIVE_EFFICIENCY = 0.85, rewrote over‑power check to use spindle power (`cutting/η`).  

**DECISIONS**  
- Do not rewire Codex SFC page to orchestrator until physics model is fully validated; fix page’s own Vc/fz/rpm clamp logic first.  
- Keep variability batch running (was disabled 6 days); no immediate action.  
- Proceed with full closed‑loop testing of remaining six backend actions (`cycle_time`, `engagement`, `deflection`, `power_torque`, `surface_finish`, `tool_life`).  
- After engagement‑arc fix, re‑evaluate page→orchestrator rewrite (physics‑review gated).  
- Restart `:3100` server via supervisor kill to deploy new dist bundle.  

**OPERATOR DIRECTIVES**  
- `/goal /loop [10m] complete all remaining back end development tasks…`.  
- “DO IT ALL” – variability batch, closed‑loop tests, tool‑life investigation.  
- “use sound logic” – verify vendor parity before rewiring.  
- “approve continue” – proceed after vendor‑parity check.  
- “Restart and keep hardening the SFC calculator especially cutting result output, we need it 100 % accurate every time.”  

**FINDINGS/BUGS**  
- Variability batch disabled (state: `Disabled` since 2026‑06‑17).  
- Safety bug: Haas OM‑2 (5.6 kW) reported “safe” at 112 % spindle power; fixed by tightening safety scoring.  
- RPM not clamped in page engine – now added clamp to machine max.  
- Constant fz = 0.288 for all materials replaced with canonical milling feeds (P:0.15, M:0.12, N:0.20).  
- ISO‑group blind Vc corrected; stainless ran faster than steel.  
- Engagement arc doubled (`2φ`) → wrong chip thickness; fixed to `φ`.  
- Surface‑finish Ra inflated by `fz×teeth`; removed.  
- Tool life ~9 min (canonical constants) vs 2.2 min (inline).  
- Over‑power check now uses spindle power with efficiency 0.85; previously compared raw cutting power → ~25 % lenient.  

**DOMAIN SPECIFICS**  
- Engines: `SpeedFeedOrchestratorEngine.compute()`, `ProductEngine.sfcCalculate()`, `ManufacturingCalculations.calculateSpeedFeed`.  
- Dispatchers: `calcDispatcher` actions (`cycle_time`, `engagement`, `deflection`, `power_torque`, `surface_finish`, `tool_life`).  
- Metrics/paths: SFC nine‑axis physics, chip‑load, material‑aware feeds/speeds; accuracy auditor streams JSONL corpus under `state/shared/sfc-variability-results`.  
- Vendor parity probe script (`vendor-parity-probe.mjs`) compares page vs orchestrator vs published carbide bands.  

**TOOLS USED**  
- PRISM tooling: `/checkin-oscar`, `slot-bind-enforce.mjs`, `chat-slots.mjs`, `/compact`, `/checkin` pipeline.  
- Testing: Jest (`*.test.mjs`), TypeScript compiler (`tsc`).  
- Scripts: `scripts/lib/sfc-accuracy-audit-lib.mjs`, `scripts/sfc-accuracy-audit.mjs`.  
- Vendor adapters: G‑Wizard/HSMAdvisor data import (not present locally).  
- Physics‑reviewer agent, per‑file scrutiny gates (2 reviewers), fanout gate.  
- Build: `build:fast` (esbuild), supervisor restart (`mcp-server-supervisor.mjs`).  

**OPEN THREADS**  
1. Fix engagement‑arc bug (already addressed; re‑run tests).  
2. Full closed‑loop test suite for all six backend actions; add reference‑value tests where needed.  
3. Rewire page to orchestrator (physics‑review gated) once page physics validated.  
4. Electron/Android/iOS shell builds – pending after UI proven 100 % correct.  
5. Vendor parity validation for all materials (confirm published carbide bands).  
6. Full variability batch run (`sfc-variability-batch-run.mjs`).  
7. Orchestrator Vc over‑derate decision (operator‑controlled).
