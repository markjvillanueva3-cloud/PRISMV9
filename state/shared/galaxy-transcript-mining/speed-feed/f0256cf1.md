# speed-feed session f0256cf1 (2026-06-26, 4.9MB, spine 39KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-KSF-01` (commit 690a72ad16): OEM‑verified spindle caps for 5 JM mills, two design errors fixed, 4/4 tests passed.  
- `U-MACHDB-01` (commit 5e1529ce56): 1015‑machine completeness audit (regenerable artifact).  
- `U-MACHDB-PROGRAM` (commit df82a7e1b7): Phase‑0 deep assessment + 6‑phase program spec.  
- `U-MACHDB-02` (commit 71cef78a31): Canonical machine schema + key‑alias normalizer, with coverage ≥ 95% for strong attributes.  
- Kienzle SFC frontend build plan (`KIENZLE-SFC-FRONTEND-BUILD-PLAN-2026-06-26.md`) committed.

**DECISIONS**  
- Design’s inline material constants are incorrect; all cutting numbers must be sourced from the real backend (`sf_orchestrate`).  
- Missing mill spindle caps must be added (U‑KSF‑01).  
- Data fragmentation requires a canonical schema and normalizer before any gap‑fill or new physics engines.  
- Adopt a 6‑phase program: (1) audit, (2) canonical schema + normalizer, (3) normalize all records, (4) fill sparse attributes, (5) wire into `sf_orchestrate`, (6) add FRF, torque‑curve, taper‑stiffness, accel/jerk, gear‑range engines and build the Kienzle page.

**OPERATOR DIRECTIVES**  
- Continue calculation training/fine‑tuning; finish front‑end build of the new Kienzle design.  
- Add all missing features, ensure backend SFC wiring to all consumers.  
- Begin closed‑loop testing suite for cutting‑parameter output accuracy.  
- Reorient where left off and check Hermes CLI status.  
- Audit **ALL** machines (1000+), fill everything to 100 %, build all required physics.

**FINDINGS/BUGS**  
- Design’s material constants diverge by ~48 % (ti64 kc, 4140 Taylor C).  
- JM mills lacked spindle caps; two design errors discovered and corrected.  
- Data is highly fragmented: ~100 key variants for power, rpm, torque, etc.; consumers silently drop many machines.  
- Sparse attributes: way‑type (~6 %), accel/jerk (0–4 %), accuracy (1 %), repeatability (5 %), FRF rigidity (0 %), thermal‑comp (~1 %), surface‑finish (0 %), build‑quality (0 %).  
- Normalizer test bugs fixed; coverage now ≥ 98 % for strong attributes.

**DOMAIN SPECIFICS**  
- Front‑end: `SpeedFeedPage`, `KienzleSpeedFeedPage.tsx`, components under `components/sfc/`, hooks `useSfc`.  
- Back‑end routes: `/api/v1/speed-feed/orchestrate`, `/api/v1/sfc`, ShopConfigurationEngine, MACHINE_CATALOG_QUICK.  
- Orchestrator engine: `SpeedFeedOrchestrateEngine.compute`.  
- Machine registry: `src/registries/MachineRegistry.ts`, `machine-kinematics-catalog.ts`, `machine-torque-curves.json`, etc.  
- Constants for kc/mc/Taylor in `constants.ts`.

**TOOLS USED**  
- PRISM `/checkin` pipeline, chat‑slot helpers (`chat-slots.mjs`), slot‑bind‑enforce hook.  
- Sonnet recon workflow + synthesis plan.  
- Vitest & tsc for tests and type checking.  
- Node scripts for registry access/normalizer.  
- Git for commits.

**OPEN THREADS**  
1. `U-MACHDB-03`: add normalized accessor to the machine registry (cached, read‑time).  
2. Phase 3: normalize all 1015 records using the new schema.  
3. Phase 4: fill sparse attributes with OEM data or physics‑derived defaults (balance grade, FRF rigidity, etc.).  
4. Phase 5: wire normalized data into `sf_orchestrate` so SFC uses real per‑machine caps for all 1015 machines.  
5. Phase 6: implement new engines (FRF stability lobe, torque‑curve power limiter, taper stiffness, accel/jerk effective feed, gear‑range selector).  
6. Build the Kienzle Speed‑Feed page after normalization and engine wiring.  
7. Run closed‑loop accuracy tests for cutting‑parameter output.
