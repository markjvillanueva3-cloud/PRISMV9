# speed-feed session 396bc735 (2026-05-19, 24.3MB, spine 489KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `817c95ba72`: wired `backend-dev` as 6th domain in `tribal-by-domain-inject.mjs`; added R12 validator to `tribal-rerank.mjs`; created `retag-tribal-backend-dev.mjs`; tests 40/40 PASS.  
- `c346fe51b2`: shipped 6 high‑ROI wikis (`software-engineering`, `code-tribal`).  
- `a8f3475028`: broader retag pass (+23 entries) + 2 new wikis.  
- `d9f1b7960f`: added 5 more wikis, 3 retags; loop iter 5.  
- `d716d20a96`: merged cancelled wikis (LoRA, RL, MCP‑tool).  
- `e190747c7d`: committed 3 operator‑pain‑point wikis to `slot/lima`.  
- `dc3f2328b7`: merged `slot/lima` → `cad-fusion-live-ms0`, bringing all 22 lima wikis into main.  
- Wiki commits (slice 3):  
  - `parallel-tool-call-discipline.md` – 105 lines (`9b5a0a2c49`)  
  - `fleet-coordination-discipline.md` – 149 lines (`3d2b4df4c4`)  
  - `cron-and-scheduled-task-discipline.md` – 164 lines (`3d2b4df4c4`)  
  - `commit-message-conventions.md` – 187 lines (`5079190f45`)  
  - `prism-self-update-loop.md` – 163 lines (`318a26989c`)  
  - `recall-injection-flow.md` – 178 lines (`fcf905d1a8`)  
  - `bash-vs-powershell-on-windows.md` – 219 lines (`b290ce2b20`).  
- Slice 6: 17 new `software-engineering/` wikis (e.g., `schema-read-discipline.md`, `git-shared-index-hazards.md`, `loop-state-tracking-discipline.md`); 1 improvement (`wiki-automation-discipline.md`); 3 memory entries (`feedback_autonomous_loop_drift_discipline.md`, `reference_wiki_recall_index_stale_2026‑05‑18.md`, `feedback_scan_lived_signals_for_wiki_gaps.md`).  

**DECISIONS**  
- Place `backend-dev` last in `DOMAIN_MAP`.  
- Add R12 domain validator to `tribal-rerank.mjs`.  
- Use atomic write + try/catch for retag script.  
- Commit new wikis to `slot/lima`; merge into main after golf‑integrator approval.  
- Implement watchdog in Stop chain to regenerate stale wiki indexes/embeddings (`settings.json:527`).  
- Use `git -C <path>` for non‑chained shell commits.  
- Adopt drift‑discipline: if a verification loop consumes > 1 tick, record anomaly and return to original purpose.  
- Pathspec clamping with `git add -- <file>`.  
- Treat lockfiles size 0 + mtime > 60 s as wedged peers; patient‑retry until fleet‑reaper threshold (10 min).  
- Prefer cloud scheduling for ≥ 1 h intervals or daily phrasing.  
- Offer cloud scheduling for intervals ≥ 60 min or daily phrasing; if chosen, invoke `schedule` skill instead of `CronCreate`.  
- Adopt loop‑state tracking discipline: register, tick, end loops cleanly before cancelling cron; cancel autonomous cron jobs after final tick.

**OPERATOR DIRECTIVES**  
- `/loop [5m] continue high value wiki and obsidian usage`.  
- `/loop [10m] exhaust all backend‑dev tribal knowledge injection`.  
- `/checkin-lima` → claim `lima` slot, bind to `lima-work`, run full `/checkin` pipeline.  
- Commit new wikis to `slot/lima`; merge into main when ready.  
- “make sure everything you're building is automated or it will sit stagnant.”  
- “CRITICAL: Respond with TEXT ONLY. Do NOT call any tools.”

**FINDINGS/BUGS**  
- Bash session cwd caused `worktree-commit-route` failures; resolved with explicit `git -C`.  
- Stale wiki indexes (19 h+), stale embeddings, missing Obsidian feed stamp.  
- `karpathy‑12‑rule‑discipline.md` absent from `_leaf-index.jsonl`; recall test returned empty.  
- Watchdog initially inactive; now wired to Stop chain.  
- `index.lock` jam: 4‑minute wedged peer detected; resolved via patient retry and lock clearance.  
- Cron rounding: non‑divisible intervals rounded to nearest clean interval with user notification.  
- Pathspec clamping verified: only intended file committed despite peer scripts in index.  
- Loop executed 30 times with duplicate triggers; resolved by manual cancellation.

**DOMAIN SPECIFICS**  
- Hooks/Dispatchers: `tribal-by-domain-inject.mjs`, `wiki-precheck-inject.mjs`, `system-viz-on-commit.mjs`.  
- Scripts: `tribal-rerank.mjs`, `retag-tribal-backend-dev.mjs`, `regen-wiki-from-viz.mjs`.  
- State files: `state/shared/tribal-embed-index.json`, `_leaf-index.jsonl`, `_embeddings.jsonl`.  
- Paths: `knowledge/wiki/software-engineering/*`, `knowledge/wiki/code‑tribal/*`, `knowledge/wiki/lima/*`.  
- PRISM pipeline stages: WRITE → SYSTEM‑VIZ → WIKI INDEXES → OBSIDIAN VAULT.  
- Wiki topics created: parallel tool calls, fleet coordination, cron vs scheduled task, commit message conventions, self‑update loop, recall injection flow, Bash vs PowerShell interop, process discipline, tool discipline, knowledge surfaces, engine lifecycle, docs pipeline, persistence patterns, loop mechanics.

**TOOLS USED**  
- `CronCreate` (cron expression generation).  
- `schedule` skill (cloud scheduling).  
- `AskUserQuestion`.  
- Node helpers: `chat-slots.mjs`, `milestone-tracker.mjs`, `per-agent-handoff.mjs`.  
- Git hooks: `post‑commit`, `worktree‑commit‑route`.  
- Pathspec clamping via `git add -- <file>`.  
- Lockfile detection logic (size/mtime heuristics).  
- Test harnesses: `tribal-by-domain-inject.test.mjs`, `retag-tribal-backend-dev.test.mjs`.

**OPEN THREADS**  
- Verify `karpathy‑12‑rule-discipline.md` appears in `_leaf-index.jsonl`; run recall test again.  
- Confirm watchdog triggers regeneration of indexes and embeddings after merge.  
- Monitor for any R8 duplication warnings when adding new wikis.  
- Ensure all 40+ backend‑dev wikis auto‑injected via both `tribal-by-domain-inject` and `wiki-precheck-inject`.  
- Resolve stale recall index (currently 3 h+ stale).  
- Finalize integration of `recall-injection-flow` with self‑update loop.  
- Monitor lockfile health to avoid future wedged peers.  
- Schedule next `/loop` iteration for remaining gaps in PRISM automation coverage.
