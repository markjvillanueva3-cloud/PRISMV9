# speed-feed session 7a2dd31d (2026-06-22, 14.1MB, spine 118KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `698525d504`: Proven‑pipeline activation – engine load‑at‑init + resumable harness, 8 tests.  
- `e764ec48e3`: Scrutiny fixes + full CNC LATHE corpus mined (16 558 programs).  
- `7d2e0af436`: Mill lane added to proven store; dedup fix for lathe samples.  
- `c24f63e029`: Convergence‑diff refreshed, stale evidence corrected.  
- `ded461e4f3`: Thin‑mill coverage closed with HSM catalog seed (adds 7 samples).  
- `3298f26b31`: Wiki entry and R15 cycle documentation completed.  
- `b320db8e8f`: Self‑correction of earlier claim that focused SFC page lacked safety block.  
- `ecb2c583da`: Convergence P2 implementation, flag‑gated (`PRISM_SFC_CONVERGE` default OFF).  
- `243da34546`: Re‑mine cron script added (`install-sfc-remine-task.ps1`).  
- `4e3ed0af70`: Frontend uncertainty surfacing action wired (preview of CI95/uncertainty).  
- `3dbdad0462`: Convergence preview action (`prism_calc:sfc_convergence_preview`) – read‑only comparison.  
- `962e4e0174`: `U-SFC-OUTCOME-BUS-REAL` – fixed fake 100 % bus‑capture metric, added tests, tsc clean.  
- `8aa2ed6dfb`: `U-SFC-WIRE-EXEMPT-AUDIT` – corrected stale “not exposed” markers; identified two phantom orphans (ProvenanceWireEngine & InferenceGateWireEngine).  
- `f43071ff6d`: `U-SFC-PROVENANCE-WIRE` part (a) – wired ProvenanceWireEngine.cite() into MultiHypothesisRankerEngine, added 74 tests, tsc clean; landed on `slot/oscar`.

**DECISIONS**  
- Proven pipeline must be activated to ground orchestrator; otherwise UI shows empty store.  
- Mill lane addition closes thin coverage; no further data needed at this stage.  
- Convergence P2 is safety‑critical and flag‑gated; operator sign‑off required before enabling (`PRISM_SFC_CONVERGE=1`).  
- Frontend uncertainty surfacing requires visual verification (Playwright); cannot ship blind.  
- Re‑mine cron established to keep proven store fresh; scheduled task registration deferred until operator confirmation.  
- Convergence enable decision pending; gate to product.  
- Branch reconcile of `f43071ff6d` to main tree required.  
- Part (b) (`SFCInferenceGateWireEngine`) needs physics‑reviewer pass before wiring.  
- Product items (convergence enable, web surfacing, mobile) remain gated.

**OPERATOR DIRECTIVES**  
- “Build everything we need.”  
- “Finish the frontend build and UI for the SFC web app.”  
- “Once we prove that it works 100 % begin building the Electron, iOS and Android version of the SFC app.”  
- De‑gated quebec: oscar now owns & builds SFC frontend.  
- Update galaxy settings to reflect new ownership.  
- Full closed‑loop testing of the SFC app page suite; start with JM Die fleet machines.

**FINDINGS/BUGS**  
- Two physics engines diverge: orchestrator engine over‑derated (~‑63 %) vs. `UltimateSpeedFeedEngine` (published‑aligned).  
- Mill path initially broken due to CommonJS `require()` in ESM; fixed by commit `f10b3aec2a`.  
- Convergence diff stale – 4 turning cases already fixed (`679a27226`).  
- Safety block rendering misidentified earlier; focused page actually renders safety score/status/factors.  
- Resumable harness dedup bug: double‑count on resume; fixed by conditional dedup only for lathe lane.  
- Convergence P2 flag default OFF to avoid accidental speed changes.  
- `SpeedFeedOutcomeFeedbackBridgeEngine.tryBusCapture()` hardwired `return true`; fixed to call `captureSFC` and return real `ok`.  
- Two “WIRE‑EXEMPT” markers stale (Ranker & ParameterRefinement); two orphan engines identified (ProvenanceWireEngine no consumer, InferenceGateWireEngine orphan on slot/india).  
- Agent attempt to wire `SFCRAGWarmStartEngine` failed due to dynamic‑import `{}` issue; reverted.

**DOMAIN SPECIFICS**  
- Engines: `UltimateSpeedFeedEngine`, `SpeedFeedNineAxisOrchestratorEngine`, `SpeedFeedOrchestratorEngine`, `ProvenSpeedFeedAggregatorEngine`, `SpeedFeedOutcomeFeedbackBridgeEngine`, `SFCOutcomeCaptureWireEngine`, `SFCMultiHypothesisRankerEngine`, `SFCParameterRefinementEngine`, `SFCProvenanceWireEngine`, `SFCInferenceGateWireEngine`, `SFCRAGWarmStartEngine`.  
- Proven aggregator: `ProvenSpeedFeedAggregatorEngine` (store schema, lazy hydration).  
- Proven store path: `mcp-server/data/state/proven-speed-feed-store.json`.  
- Corpus mining harness: `scripts/extract-jm-proven-speedfeed.ts`.  
- Mill catalog seed file: `src/data/jmdie-proven-mill-programs.ts`.  
- Dispatchers: `prism_calc:sfc_convergence_preview`, `prism_calc:sfc_calculate`, `surfaces_into` (wire), etc.

**TOOLS USED**  
- PRISM check‑in wrapper (`/checkin-oscar`).  
- Chat‑slot helpers (`chat-slots.mjs`).  
- Atomic write utilities (`src/utils/atomicWrite.ts`).  
- Vitest test framework (8–15 tests; 74 tests added in `f43071ff6d`).  
- TSC with 16 GB heap for full project type‑check.  
- Cron script generator (`install-sfc-remine-task.ps1`).  
- PRISM SFC tools: `prism_calc:sfc_convergence_preview`.  
- Git utilities: partial‑path commits, `git-add-lane-guard`.  
- Sonnet agent (built provenance wire).  
- Explore agent (recon frontend state).

**OPEN THREADS**  
- Enable convergence – operator must flip `PRISM_SFC_CONVERGE=1` after reviewing `SFC-CONVERGENCE-DIFF.md`.  
- Frontend uncertainty surfacing – requires visual‑verify session (Playwright) before UI changes can be shipped.  
- Electron/iOS/Android shells – gated on confirmed frontend behavior; needs separate visual verification.  
- Visual verification of UI changes – not possible in current context; must be deferred to a dedicated session.  
- Merge `f43071ff6d` from `slot/oscar` into `cad-fusion-live-ms0`.  
- Wire `SFCInferenceGateWireEngine` (part (b)) after physics review.  
- Complete product items: convergence enable, web surfacing, mobile.  
- Resolve worktree import friction if further orphan wiring needed.  
- Execute full closed‑loop testing of the SFC app page suite per operator directive.
