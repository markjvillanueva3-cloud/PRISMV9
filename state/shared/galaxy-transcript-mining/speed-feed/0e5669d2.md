# speed-feed session 0e5669d2 (2026-06-09, 33.8MB, spine 281KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `da013c4406` – Obsidian‑vault audit + cheap‑node‑access map (wiki & galaxy brain).  
- `9e4376b3b2` – U‑VAULT‑RAG‑WIRE (keyword recall hook wired, live‑verified).  
- `168c202646` – B1 U‑VAULT‑SYNC‑RESILIENT (writeWithRetry + guarded writeFileSync; new test).  
- `ea21008f25` – B2 U‑VAULT‑INDEX‑META (in‑place frontmatter stamp, metadata‑stamp script).  
- `8c4dff660a` – B3 U‑VAULT‑MAINT‑CRON installers (promotion & rot‑sentinel scripts; disabled by default).  
- `984313825e` – C2 U‑VAULT‑LINK‑HEAL scorer recalibration + decay‑leak fix (auto‑apply reduced 14 100→2).  
- `838123429b` – C3 U‑VAULT‑TRIBAL‑COVERAGE refresh (banner cleared, cron status reported; coverage 83.7 %).  
- `45225e0cc3` – Persisted “fix‑memory” record for this session.  
- `d948b85a74` – Hardened scorer (minLen 8 + edit‑fraction guard); tests 47/47 pass.  
- `3d796dcf5c` – fleet‑task‑health‑watch: added EXPECTED_UNREGISTERED_TASKS, banner noise partitioning, WSL guard.  
- `96ed5222e2` – vault‑backlink schema & builder for reverse edge (`doc_nodes`) with 17 tests.  
- `d856173b86` – staleness flag in backlinksFor and CLI output update.  
- `0e2724871a` – regen‑viz.mjs auto‑refresh stage that rebuilds `vault‑backlinks.json` on each graph regeneration.  
- `14aba14e3a` / `4a44b5393c` – prism_session:doc_nodes dispatcher action, schema, tests (14/14 pass).  
- `f23c58b42d` – Updated vault‑access map to close reverse‑edge gap & fix stale memory‑rag‑inject claim.  
- `2d49bf0d33` – canvas‑read-lib.mjs + CLI subcommands (`canvas`, `canvas-doc`); 15/15 tests, staleness flag.  
- `47fe91554f` – Updated access map for `.canvas` row; corrected stale claim.  
- T2: `ollama-pipeline-injector` wired into `settings.json`; live‑validated (R15).  
- Audit spec `7ec4a5ea02` – lane‑assigned backlog for sierra, alpha, bravo.

**DECISIONS**  
- Use `/checkin-sierra` wrapper to force‑claim the sierra slot before canonical `/checkin`.  
- Verify gaps against live R8 code; audit with ultracode then build in dependency order.  
- Persist context to wiki entry, galaxy brain `system-viz/MEMORY.md`, two C:/H:/ memory files, handoff file.  
- Wire `memory‑rag‑inject` only when always‑on injector disabled; close P0 RAG‑wire first.  
- For B2 avoid re‑emitting entire index; use in‑place metadata stamp script.  
- Tighten scorer: exact match or Levenshtein ≤ 2 for auto‑apply; demote prefix/substring to medium; gate `autoApplyEligible` on reason set.  
- Hook‑heap cap: replace hard 4096 MB NODE_OPTIONS with `${PRISM_HOOK_HEAP_MB:-384}` (commit charge ↓ ≈ 70 %).  
- MCP maxConnections: raise from 200 → 512 (`PRISM_MCP_MAX_CONNECTIONS`).  
- Add `TRANSIENT_PRESSURE_CODES` & `isTransientPressureCode()`; modify `isLaunchFailureCode()` so ERROR_NO_SYSTEM_RESOURCES no longer flags “failing”.  
- Widen `OFFLOADABLE_PATTERNS` to include lint, classify, docstring‑generation, diff‑summary, triage, extract, rename, describe, reword, format, list.  
- Introduce `EXPECTED_UNREGISTERED_TASKS`; partition missing tasks into real vs deferred (banner noise).  
- Keep hook‑heap cap at 384 MB; raising worsens commit charge (70 % vs 92 %).  
- Reclaim WSL balloon via `wsl --shutdown` after Docker shutdown; keep migration freeze on.  
- Build reverse edge (`doc_nodes`) and canvas reader to close all cheap node‑access paths.  
- Use Ollama only for offloading creation scripts; reject router‑wide widening premise.  
- Wire regen‑viz stage for auto‑refresh of `vault‑backlinks.json`.  
- Adopt fail‑soft, staleness flag pattern across new readers.  
- Reuse existing `generateBlurb()` (fail‑soft, cacheable) for viz→wiki narrative; switch default Ollama model from `gpt‑oss:20b` to warm `qwen2.5-coder:32b`.  
- Flag‑gate post‑pass generator that enriches all 3 child generators after field‑dump; exclude `layer-stack-overview.md` (overwritten later).  
- Dry‑run must skip Ollama calls; only count enriched entries.

**OPERATOR DIRECTIVES**  
- “Fill all gaps.”  
- Reorient from recent sierra sessions, verify vault built & operational, map cheap‑token paths for each node, persist context in sierra’s domain.  
- Validate hook‑heap cap is holding (commit charge ~70 %).  
- Tidy “MISSING‑task” banner noise by marking vault crons as `EXPECTED_UNREGISTERED`.  
- Write handoff notes; recommend `/compact`.  
- After migration freeze lifts, arm deferred items: restart MCP daemon for new maxConnections, register vault promotion/rot sentinel cron tasks.  
- Continue system viz & Obsidian synergy; keep node mapping for quick searches/tool usage savings.  
- Utilize Ollama for task efficiency when viable; fully synergize within Prism OS (tool calls, /system‑viz, obsidian vault, memory, wiki, tribal knowledge).  
- Add fail‑soft Ollama “what/why” narrative generator to `scripts/regen-wiki-from-viz.mjs`.  
- Wire pipeline‑injector into `settings.json`.

**FINDINGS / BUGS**  
- `obsidian-memory-sync.mjs`: unguarded `writeFileSync` line 342; missing export `syncMemories`.  
- `WikiIndexMaintainerEngine.formatIndex`: stale frontmatter (`last_verified:2026‑05‑08`, “770 entries”) never invoked.  
- `memory-rag-inject` unwired, zero settings → keyword recall dead.  
- Scorer overconfidence: prefix/substring auto‑apply; decay path leaks >60‑char slugs (score ≥ 0.85); short Levenshtein (≤2 edits on <5‑char tokens) still eligible.  
- Stale `wiki-link-fix-candidates.json` (2026‑06‑05, `autoApplyCount:14100`).  
- Full‑corpus regen (`--batch 0`) OOM‑killed; only partial 100‑link file produced.  
- Over‑confidence bug in scorer (incorrect 2‑edit reference values).  
- False‑positive “MCP failing” alarm from transient spawn‑refusal under high commit charge.  
- Pagefile distributed across G/J/L drives; C: pagefile only 4 GB – no immediate risk.  
- GPU offload coverage low (~8 %); most offloadable work kept on Claude due to narrow pattern set.  
- Shared-index race caused merge into peer commit (content preserved, attribution lost).  
- Commit‑pressure lesson: sort by `PrivateMemorySize64`; raising hook heap worsens charge.  
- vmmemWSL balloon (95 GB) ignores `.wslconfig memory=16GB` until `wsl --shutdown`.  
- Lgit layer regex missed alphabetic suffix, miscounting canvas stats.  
- Stale memory‑rag‑inject claim in access map.  
- Amending on shared tree caused a doc line to be attributed to another commit (history contamination).  
- Falsified premise: widening router to hit 30 % offload invalid; current offloading already optimal.  
- Duplicate guard (`SemanticSimilarityGuardEngine.cosineCheck`) triggered on duplicate assets.  
- `gpt‑oss:20b` cold‑load >30 s → `generateBlurb()` returns null, skipping enrichment.  
- `layer-stack-overview.md` overwritten by its own generator; narrative lost.  
- Dry‑run incorrectly invoked Ollama, causing unnecessary latency/API usage.

**DOMAIN SPECIFICS**  
- Engines/dispatchers: `system-viz`, `WikiIndexMaintainerEngine`, `obsidian-memory-sync.mjs`, `wiki-link-fix-suggester.mjs`.  
- Actions/hooks: `memory-rag-inject`, `node-card-prefetch-inject`, `build-wiki-leaf-index.mjs`, `regen-wiki-from-viz.mjs`.  
- Portable‑node wrapper (`bash` & `.cmd`).  
- MCP server (`mcp-server/src/index.ts`) – maxConnections, hook heap metrics.  
- Fleet‑task‑health‑watch (`fleet-task-health-watch.mjs`) – health classifier, banner noise handling.  
- Host‑aware‑synthesis‑model (`host-aware-synthesis-model.mjs`) – Ollama offload resolver.  
- System‑viz‑query (`system-viz-query.mjs`) – CLI subcommands `node-card`, `doc_nodes`, `canvas`.  
- Regen‑viz (`regen-viz.mjs`) – graph regeneration, auto‑refresh of `vault‑backlinks.json`.  
- Doc_nodes dispatcher action in `prism_session`.  
- Canvas‑read‑lib (`canvas-read-lib.mjs`) – JSONCanvas parsing, staleness flag.  
- Vault‑access map document – tracks populated-node coverage.  
- SemanticSimilarityGuardEngine.cosineCheck; PRISMSelfAwarenessEngine.searchAIFeatures.  
- Paths: `scripts/lib/contextual-blurb.mjs`, `scripts/ask-ollama.mjs`, `settings.json` (pipeline injector), `state/shared/specs/OLLAMA-SYNERGY-AUDIT-2026-06-09.md`.

**TOOLS USED**  
- PRISM tools: `/checkin-sierra`, ultracode workflow, `update-config` skill, `write` tool, portable‑node wrapper, MCP server, fleet‑task‑health watcher, Ollama offload engine.  
- Git hooks: commit‑ownership‑guard, git‑add‑lane‑guard, main-tree-write-block.  
- Duplicate guard hook (`ai‑duplication-guard.mjs`).  
- Live MCP duplicate scan (`prism_dev:copilot_check_duplication`).  
- Unit tests: vitest/node:test for scorer, fleet-task health, offloader patterns (62/62 pass).  
- Scripts/hooks: dedup, memory feed hooks, handoff writer.

**OPEN THREADS**  
- Full‑corpus regen of `wiki-link-fix-candidates.json` – OOM; need streaming/chunked rewrite.  
- Reviewer‑flagged over‑confidence in short Levenshtein auto‑apply – add min‑length floor & tie‑veto.  
- Performance follow‑up on suggester and coverageComplete marker (not critical).  
- MCP daemon restart to apply `maxConnections=512`.  
- Register vault crons (`PRISM Vault Memory Promotion Cron`, `PRISM Vault Rot Sentinel Cron`) after migration freeze lifts.  
- WSL Memory Guard task remains unregistered – decide arm vs defer; monitor balloon after Docker shutdown, ensure `.wslconfig` applied.  
- Pagefile relocation to H: optional.  
- Further offload coverage tuning beyond current pattern set.  
- Wire `multi-provider-router.mjs` to use host‑aware‑synthesis‑model for Ollama injection.  
- Finalize weekly synth resolver patch (import change).  
- Build U‑VIZ‑WIKI‑NARRATIVE for wiki narrative generation (pending).  
- Final commit of T1 (U‑VIZ‑WIKI‑NARRATIVE) after fixes: exclude overview, dry‑run skip, model switch.  
- Verify no other unwired Ollama hooks remain; audit remaining 88 % surface.  
- Lane assignments: sierra → viz‑wiki narrative build; alpha → distill‑tribal unblock / wiki‑NLI‑lint / memo‑throttle / cache‑consolidate / CLAUDE.md fix; bravo → WeeklySynthesisEngine resolver.
