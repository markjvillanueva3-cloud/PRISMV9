# speed-feed session d1c0715f (2026-06-26, 17.1MB, spine 141KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `cb40bbba7b` – HSS aggressive‑Vc thermal cap (clamp Vc for hot ISO groups P/M/K/S/H)  
- `5684b03311` – Orchestrator headline tool‑material‑blindness fix (removed 3.4× over‑speed for HSS)  
- `a5790c3217` – Test correction invalidated by aggressive‑Vc cap  
- `b1b2d9fa54` – Wiki lesson documenting bug class and resolution  
- `0d95de4286` – tsx‑reexec guard no‑op under Vitest, restoring 12 SFC regression tests  
- `55dac1597c` – Triaged ~107 MILL‑HARD‑MS1 failures; routed spec to foxtrot  
- `3da3bcc600` – Fixed engagement dead‑wiring: feed_per_tooth & cutting_speed optional  
- `b8641ced34` – API‑layer guard (`assertNotBlocked`) + UX guard for blank panel when no machine selected  
- `49251eff15` – Dead slider fix: derate‑only Vc factor for `optimize_for` (cost / tool_life)  
- Force/Power overstatement fix – routed Kienzle force sites through shared `calculateKienzleCuttingForce` core  

**DECISIONS**  
- Clamp aggressive Vc only for hot ISO groups; keep N exempt.  
- Use Hermes (xAI Grok) as independent physics consensus engine.  
- Route MILL‑HARD‑MS1 failures to foxtrot; leave oscar logic untouched.  
- Make engagement schema optional for feed/speed, preserving geometry‑only contract.  
- Keep PRISM_SFC_CONVERGE operator‑only; do not ship automatically.  
- Guard architecture: single-point `assertNotBlocked` in `envelopeGuard.ts`, wired into every SFC endpoint (`sfc.ts post`).  
- UX scope: require machine selection before Calculate; align frontend with backend hard gate.  
- Optimize_for scope: derate‑only Vc factor, never raises Vc, operator‑gated for productivity raise.  
- Force/Power fix: replace inline Kienzle formula with shared core to match vendor physics; no test re‑baseline needed.  

**OPERATOR DIRECTIVES**  
- Continue calculation training and fine tuning.  
- Complete remaining queued work items (PRISM_SFC_CONVERGE, MILL‑HARD‑MS1 triage, sfcAllAxisSweep harness).  
- Do not idle; proceed on reversible/internal forks.  
- Close orphaned Claude Desktop app (`cowork-svc.exe`).  
- Build signal for force/power fix – proceed with vendor‑parity validation and physics review.  

**FINDINGS/BUGS**  
- HSS aggressive Vc over-speed (~2.5× for P/M/K/S/H); resolved by thermal cap.  
- Orchestrator headline tool‑material‑blindness: HSS published at carbide speed (≈3.4×).  
- Engagement endpoint required `feed_per_tooth` & `cutting_speed`, causing Zod failures; made optional.  
- tsx re‑exec guard prevented Vitest from running sfcAllAxisSweep tests; fixed to no‑op under Vitest.  
- ~107 MILL‑HARD‑MS1 failures due to incomplete foxtrot milestone (tool‑steel hardness classification, ai_reasoning, force fields).  
- Silent‑blank defect: backend returns `{blocked:true}` on 200 OK; frontend ignores → empty panel.  
- Dead slider: `optimize_for` declared but never consumed in `SpeedFeedOrchestratorEngine`; cost/balanced/productivity all returned identical Vc/life.  
- Over‑stated force/power (~3× too high) due to inflated `fz` usage without Martellotti mean‑chip‑thickness or engaged‑teeth duty factor.  
- Cross-path parity divergence: SfcCalculatorPage vs SpeedFeedOrchestratorEngine produced different Vc, Fc, power, life for same cut.  

**DOMAIN SPECIFICS**  
- Engines: `UltimateSpeedFeedEngine.calculate()`, `SpeedFeedOrchestratorEngine.compute()`, `SpeedFeedNineAxisOrchestratorEngine.run()`, `calculateKienzleCuttingForce`.  
- Dispatchers/paths: `/calculate`, `/cycle-time`, `/engagement`, `/deflection`, `/power-torque`, `/surface-finish`, `/tool-life`, `/orchestrate` (`sfc.ts post`), `sf_quick`, `sf_orchestrate`; page routes `SfcCalculatorPage`, `SpeedFeedPage`.  
- Metrics: Vc, fz, rpm, power, torque, MRR, kc, Fc, life, deflection, workholding.  
- Tool‑material speed overrides: `tool-material-speed-override.ts`, `getMaterialSpecificToolSpeedFactor()`, `isHssAggressiveVcThermallyCapped()`.  
- Physics models: 9‑axis SFC, Kienzle/Taylor, chip-load calculations, material-aware feeds/speeds.  

**TOOLS USED**  
- Hermes (xAI Grok) physics consensus.  
- Octopus, ultracode, system-viz, Obsidian vault for documentation.  
- Physics-reviewer & reviewer agents; 3‑of‑3 scrutiny gate.  
- tsx re‑exec guard (`planTsxReexec`).  
- Git helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`, `git-add-lane-guard`, hooks/guards.  
- Vitest unit & integration tests.  
- TypeScript compiler (`tsc`).  
- Claude‑Code CLI for session management.  

**OPEN THREADS**  
- PRISM_SFC_CONVERGE convergence delta / base-table adjustment (operator-only pending).  
- SFC frontend page-by-page closed-loop verification.  
- Live closed‑loop testing of remaining JM Die machines (other models, materials).  
- Surface finish / fz lever refinement (post‑finalization).  
- Gate narrowing for machine-completeness logic.  
- SpeedFeed API client (`speedfeed.ts`) audit confirmed clean.
