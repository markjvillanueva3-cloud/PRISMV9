# speed-feed session be5e37e8 (2026-05-19, 28MB, spine 135KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U‑FGE01 `62b5794101`: STEP corpus → evidence‑based build sequence (`cad_class_build_sequence_evidence`).  
- U‑BPA‑CONSUMER `6cbe5b1561`: offline JSONL consumer for blueprint‑accuracy events.  
- U‑FGE02 `c60f6c9396`: wired evidence ranking into Fusion360 orchestrator (`cad_class_drive_build`).  
- U‑TDP01 `5e3048ad71`: 4‑stage print→CAD→CAM training driver (stub mode).  
- U‑TDP02 `8d533c1e10`: batch harvester with idempotent registry.  
- U‑TDP03 `99288aec44`: extraction aggregator → per‑class learned templates (Welford stats).  
- U‑TDP04 `5709f19d7b`: OCR benchmark runner vs CAD‑derived GT.  
- U‑TDP05 `1bc36d949d`: CAD‑derived ground truth extractor from STEP corpus.  
- U‑TDP07 `d85573b7d4`: deterministic embedded‑text PDF dimension extractor (SolidWorks/AutoCAD/Inventor).  
- U‑PQ-MS-COMPLETE-FILTER: picker‑fix on `cad-fusion-live-ms0`; 66/66 tests, 2‑reviewer PASS.  
- U‑TDP08: paired‑tolerance‑band & Rc hardness regexes; 77/77 tests (57 existing + 20 new).

**DECISIONS**  
- Two‑tier extraction: deterministic embedded‑text for ~13 % PDFs; vision OCR for remaining 87 %.  
- Adopt `qwen2.5vl:7b` (~5 GB) via Ollama with GPU passthrough; reject `moondream:1.8b`, OOM‑prone `llama3.2‑vision:11b`.  
- Wire evidence ranking into Fusion360 orchestrator (`use_corpus_evidence`).  
- Commit all U‑TDP07 changes in slot/mike worktree; defer vision path (U‑TDP06) until GPU model ready.  
- Fix picker namespace mismatch to stop duplicate units from HOOK‑SYNERGY‑MS0.  
- Commit U‑TDP08 patterns before wiring extractor into benchmark grader.

**OPERATOR DIRECTIVES**  
- Continue training OCR print‑reading capabilities of Prism system.  
- Verify non‑interference with Mike’s work; hand off OCR entirely to BLUEPRINT‑OCR‑TRAINING‑MS1/2.  
- Add 13th chat slot and update all references (already done).

**FINDINGS / BUGS**  
- `moondream:1.8b`: hallucinated dimensions, malformed JSON → unusable for training.  
- `llama3.2‑vision:11b`: OOM on RTX 3080 (10 GB); requires 16 GB GPU.  
- Embedded‑text parser fixed OCR fragment leakage, tolerance parsing errors, split‑token dropped‑dot fabrication via regex guards.  
- Vision path not yet integrated; `U‑TDP06` pending.  
- Only ~3.5 % of JM Die PDFs contain embedded text; 95.5 % are image‑only.  
- Picker bug caused duplicate units from HOOK‑SYNERGY‑MS0 due to unit-ID vs milestone-name mismatch.  
- Zero‑dim PDFs: job sheets, OCR‑garbled scans, real drawings with unrecognized formats (paired‑tolerance bands, Rc hardness ranges).  
- U‑TDP08 added new patterns; still need full corpus delta measurement and further pattern coverage (comma‑decimal, MAX/MIN qualifiers).

**DOMAIN SPECIFICS**  
- Engines/adapters: `U-TDP01` training driver, `U-TDP02` harvester, `U-TDP03` aggregator, `U-TDP04` benchmark grader (stub), `U-TDP05` GT, `U-TDP06` CNC GT, `U‑TDP07` deterministic extractor, `U‑TDP08` regex patterns.  
- Engines: `CADClassFeatureLibraryEngine`, `BlueprintExtractionRAGEngine`, `GroundTruthRegistryEngine`.  
- Consumers/dispatchers: `blueprint‑accuracy-consumer-lib.mjs`, `blueprint‑accuracy‑consumer.mjs`; actions: `cad_class_build_sequence_evidence`, `blueprint-accuracy-consumer`, `training-driver-print-to-cam`, `harvest-pdf-text-batch`, `pdf-text-extract-lib`.  
- Metrics/state: per‑class Welford stats (`U-TDP03`), daily ledger (`U-BPA-CONSUMER`), event JSONL (`recordEvent`).  
- Path flow: PDF harvest → text extraction → training driver → record events → consumer → aggregation → benchmarking.

**TOOLS USED**  
- PRISM internal: `chat-slots.mjs`, `/checkin` pipeline, `audit-roadmap-drift.mjs`, per‑file scrutiny agents, precompact hook, milestone‑tracker, per‑agent handoff, `spatcher.ts`, `scripts/lib/training-driver-lib.mjs`, `training-driver-print-to-cam.mjs`, `U-TDP07 lib`, `U‑TDP08 tests`, `priority-queue.mjs`, `shipped-units-source-of-truth.mjs`.  
- External: Ollama API (`llama3.2‑vision`, `qwen2.5vl`), PyMuPDF (`fitz`) for PDF→PNG at 300 dpi, Node spawnSync for Python subprocesses, Node test runner (`node:test`), `tsc`, `npm run lint`.

**OPEN THREADS**  
- Build & integrate vision OCR path (`U‑TDP06`) using `qwen2.5vl:7b`.  
- Pull and test `qwen2.5vl:7b` in Ollama; confirm GPU passthrough on sample PDFs.  
- Scale harvest across full 76 K JM Die corpus; capture embedded‑text vs image‑only coverage statistics.  
- Train LoRA/fine‑tune models once OCR path functional.  
- Finalize CAD/CAM AI training pipeline after extraction accuracy proven.  
- Wire deterministic extractor into `U-TDP04` benchmark grader (replace stub).  
- Run full 600‑PDF corpus delta measurement to quantify extraction rate improvements.  
- Add comma‑decimal normalization, MAX/MIN qualifiers, slash‑paired tolerance (candidate U‑TDP09).  
- Integrate OCR training data into overall CAD/CAM training loop once both paths functional.  

The slot/mike worktree is clean; all changes satisfy R12 fail‑loud, per‑file scrutiny, and 3‑of‑3 Stop gate.
