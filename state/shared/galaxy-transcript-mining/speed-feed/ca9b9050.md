# speed-feed session ca9b9050 (2026-06-25, 112.6MB, spine 2026KB, 14 of 24 slice(s), HEAD+TAIL sampled, model gpt-oss:20b)

**SHIPPED**  
- Copied 365 project‑only slash commands + 52 skills → 751 commands & 84 skills load everywhere (H:/prism/.claude/commands).  
- Built `sync-cmds-skills-to-user.mjs` re‑sync helper; fixed 24 stale copies.  
- Added `"main":"electron/main.cjs"` to `mcp-server/web/package.json`; updated electron‑builder files mapping `../dist/web/** → dist`.  
- Updated Capacitor config `webDir: ../dist/web`; added mobile scaffolding (`android/`, `ios/`).  
- Electron desktop build produced `PRISM‑0.1.0‑x64.zip` (171 MB) via two‑step driver `scripts/electron-dist.mjs`.  
- Global fetch proxy installed in `src/lib/apiBase.ts`; API base URL configurable via `VITE_API_BASE_URL`.  
- Added `APP_ARCHITECTURE.md`, updated `.gitignore` to exclude generated artifacts.  
- E2E suite: added `axe-core`, removed broken `benchmarkTruth.spec.ts`; 157 specs collected, failures due to networkidle timeouts & semantic markup mismatches.  
- Front‑end wire‑in: `quoteThreeView()` client fn + panel on QuoteBuilderPage (commit 2d85f24782).  
- CAD/print drop‑box + auto‑redaction route `/cad/blueprint-redact` with UI panel (commit 387482bbb3).  
- Demo‑string purge identified OS‑shell demo strings; cross‑galaxy work order filed.  
- VendorUnitPriceEngine (`vendor_unit_price`) added, UI tier badges (commit 7fb49f6b18).  
- Make‑vs‑buy panel added to QuoteBuilderPage; fixed dead panels via `unwrapQuotingBody` helper (commit d526c01ede & 17a082f723).  
- Self‑compact resolver: single‑tab owning‑pid tier + live‑pid re‑resolution (self‑compact.mjs); all tests green.  
- Shared redactor extended to include `cost_breakdown`, flat keys for `process_cost`; exported `redactThroughEnvelope`.  
- Hotel‑portal routes gated with `verifyToken` & role tiers; FE token wiring added (commit 18f8da8ed9).  
- `/quote/rates` now redacts `margin_pct`/`overhead_pct` when anonymous (commit 5538ba5c38).  
- Routers (`inbox.ts`, `integrations.ts`, `learning.ts`, `docLearn.ts`, `doc.ts`) gated with `verifyToken`/`requireRole`; added missing `DOC_WRITE_ROLES` on `/migrate` (commit 943bf4259a).  
- RFQInbox assignment facet, CreditManagement reviewCredit routes, OEEDashboard deterministic engine methods (`losses()`, `trend()`); all tests pass (commits 35959c2ec8, 697d2ad6fa, 27362064d4).  
- OEE routes, dispatcher, tests added; OSHA compliance & safety‑training engines with adapters and listAllPPE/listAllRecords methods (commits 7ab40eb54b, 3213b0f0de).  
- Maintenance work‑order P2 orphan logged; auditor tool persisted (commit d01b16ae37).  

**DECISIONS**  
- Sync project‑only commands/skills into user scope to enable slash commands from any directory.  
- Keep Vite `outDir` at `../dist/web`; adjust consumer configs instead of moving output.  
- Add main field & remap electron‑builder files for SPA inclusion; use two‑step zip driver to avoid winCodeSign symlink extraction.  
- Global fetch proxy rewrites `/api/...` URLs for Electron/Capacitor; `VITE_API_BASE_URL` overrides default localhost:3100.  
- Adopt self‑compact resolver (single‑tab owning‑pid + live‑pid re‑resolution) and extend shared redactor to strip internal cost fields when anonymous.  
- Gate all hotel‑portal & ERP routes with `verifyToken`/`requireRole`; apply role tiers mirroring HR/admin hierarchy.  
- Use `unwrapEnvelope` for all `prism_business` actions to avoid dead‑panel `{type,text}` envelopes.  
- Add deterministic OEE engine methods (`losses()`, `trend()`) due to lack of persisted event store.  
- Prioritize Tier‑1 clusters (PM/Maintenance, Assets, Payroll, Audit/CAPA, NCR, Vendor, Calibration, Shipping) sequentially; defer Tier‑2.

**OPERATOR DIRECTIVES**  
- Execute both Path A (relaunch from `H:\prism`) and Path B (sync commands/skills).  
- Finish building & testing web app, Electron desktop, iOS/Android mobile apps.  
- Run `/compact` to reset context; after compaction surface `quote_what_if` panel.  
- Restart backend :3100 to load new actions live; verify all 19 quoting client functions via `/api/v1/*`.  
- Continue hardening quoting app: fix anon cost/margin leaks, dead panels, envelope mismatches.  
- After exhausting quoting work, audit hotel/ERP surface with same security hardening pass.  
- Run auto‑compaction continuously; use hermes CLI, ollama offloading, octopus, RTX 6000, PSN, obsidian vault, system‑viz optimally.

**FINDINGS / BUGS**  
- Slash commands failed due to CWD mismatch; project‑only commands hidden.  
- Drift in user‑scope copies: 24 stale files older than source.  
- Electron builder missing main field → UI shell produced.  
- Vite `outDir` mismatch caused electron‑builder & Capacitor to miss SPA.  
- E2E failures: networkidle timeouts (backend not running) and semantic markup mismatches.  
- Code‑signing step fails on Windows; non‑critical for dev builds.  
- Three panels (ThreeView, LVP, Outsource) dead due to `.result` reads from bare `/quoting` body.  
- Inconsistent response shapes across routes: bare body, wrapped `.result`, MCP envelope.  
- `InstantQuoteResult` leaks internal cost stack via `cost_breakdown`.  
- Dead panel crash: unguarded derefs on anon callers (`per_part_cost.toFixed()`).  
- Envelope mismatch: route returns `{result:…}` but FE client expects bare body → page crash.  
- Batch clamp mis‑stated; engine pre‑clamps `batch_size` with `Math.max(1,…)`.  
- `/api/v1/cost/estimate`, `/api/v1/cost/quote`, `/api/v1/pipeline/quote` leak pure cost basis (`total_cost_per_part`, `machine_cost`, etc.).  
- `/api/v1/quote/rates` leaks `margin_pct`, `overhead_pct`; redacted for anonymous.  
- Missing dispatcher actions for many categories (notify‑*, kanban‑*, a3-report, e2‑**, qbo‑**, receiving‑*).  
- API rate limit errors during work‑orders.

**DOMAIN SPECIFICS**  
- **Engines**: ThreeViewPricingEngine, VendorUnitPriceEngine, LocationAwareVendorPricingEngine, OutsourceRecommenderEngine, LeadTimePricingTierEngine, OEECalculatorEngine (losses/trend), OSHAComplianceEngine, SafetyTrainingRecordEngine, PreventiveMaintenanceEngine, CustomerManagementEngine (reviewCredit), RFQToOrderOrchestratorEngine (assignment facet).  
- **Dispatchers**: `prism_quoting` (`three_view_pricing`, `vendor_unit_price`, `location_vendor_pricing`, `quote_what_if`), `prism_cad` (`blueprint_redact`), `businessDispatcher` (`rfq_assign`, `credit_review`, `oee_losses`, `oee_trend`, etc.), `intelligenceDispatcher`, `productDispatcher`, `pipelineDispatcher`.  
- **Metrics**: headline price, optimal‑vs‑market, cost‑floor, margin confidence band, total_cost_per_part, machine_cost, tool_cost_per_part, setup_cost_per_part, cost_breakdown, loss minutes, trend array.  
- **API paths**: `/api/v1/quoting/*`, `/api/v1/cad/blueprint-redact`, `/api/v1/hotel-portal/*` (verifyToken + role tiers), `/api/v1/wedm-erp/*`, `/api/mcp/quoting`, `/api/v1/inbox/*`, `/api/v1/integrations/*`, `/api/v1/learning/document/*`, `/api/v1/doc/*`, `/api/v1/maintenance/work-orders`, `/api/v1/maintenance/refresh`, `/api/v1/osha-incidents`, `/erp/rfq-assign`, `/erp/rfq-status`, `/erp/credit-review/:id`, `/erp/credit-review-all`, `/erp/oee-trend`, `/erp/oee-losses`.  
- **Unique paths**: `H:/prism/.claude/commands`, `H:/prism/.claude/hooks`, `mcp-server/web/package.json`, `capacitor.config.json`.

**TOOLS USED**  
- PRISM MCP server dispatchers (`prism_dev:server_info`, `prism_session:health_check`).  
- Claude Code command loader & skill registry.  
- Node fs scripts for copying files, npm install.  
- Electron Builder CLI + two‑step zip driver (`scripts/electron-dist.mjs`).  
- Capacitor CLI (`cap add`, `cap sync`).  
- Playwright e2e runner; Vitest unit tests.  
- PRISM utilities: `sync-cmds-skills-to-user.mjs`, `self-compact.mjs`, `wt-tab-focus.mjs`.  
- Redactor helpers: `redactInternalMarginFields`, `redactThroughEnvelope`.  
- Global fetch proxy (`src/lib/apiBase.ts`).  
- CI workflow `.github/workflows/mobile-build.yml` (Ubuntu Android, macOS iOS).  
- Git hooks for shared‑tree attribution safety; memory & handoff scripts.

**OPEN THREADS**  
1. Wire remaining Tier‑2 clusters: `notify-*`, `kanban-*`, `a3-report`, `e2‑**`, `qbo‑**`, `receiving-*`.  
2. Resolve missing dispatcher names or create engine methods for those categories.  
3. Address API rate limiting on work‑order routes.  
4. Finalize audit of hotel/ERP surface: add missing gates/tests, ensure no anon cost/PII leaks.  
5. Complete integration of all new routes with front‑end (client API functions).  
6. Add comprehensive tests for all added routes and engine methods; 3‑of‑3 gate compliance.  
7. Ensure security hardening (`verifyToken`, role checks) across all endpoints.  
8. Conduct end‑to‑end testing of full front‑end ↔ back‑end wiring after all features are wired.
