# speed-feed session db273e77 (2026-06-10, 158.4MB, spine 1339KB, 14 of 16 slice(s), HEAD+TAIL sampled, model gpt-oss:20b)

**SHIPPED**  
- `e5ad4ea802` – HW‑retune (CPU/GPU, Docker/NIM sync).  
- `f9e953f0e8` – host‑portable fix to `enforce-eslint-after-edit.py`; added two memory docs.  
- `786bd70566` – safe edits to `business/MEMORY.md` & `ai-training/MEMORY.md`.  
- `873446a147` – claimed quality galaxy; populated CLAUDE.md §§2/5/6/7, added ownership note to MEMORY.md.  
- `1443283f8b` – registered 12 MIT‑AI textbooks in `pdf-sources/registry.json`; new `ai_textbook` category.  
- `3935f8d4ad` – added `topics/source/note` fields to `PDFSourceMetadata`.  
- `632335cec6` – goal‑clear → next‑unit fallback hook (no drift sync).  
- `c83ca9be64` – F5 autoresume staleness fix (`DEFAULT_MAX_AGE_MIN 240→720`).  
- `8cd8d615e9` – slot‑domain‑awareness inject dedup (line‑based strip, 91 % token save).  
- `eefef0359a` – psn‑leg‑state inject dedup (82 % token save).  
- `8d2b6cf8e0`, `87e96fa485`, `afe169ab0e` – dedup for foxtrot/delta/xray, whiskey/charlie‑awareness/echo‑post, charlie‑knowledge.  
- `1f295f51e6` – shared `dedupedContext()` helper (tested 7/7).  
- `636d36bf59`, `75c44d8412` – semantic memory recall with nomic‑embed & self‑refresh cache (covers all 1496 memos).  
- `HIGHVALUE-DISCOVERY-2026-06-08.md` – ultracode discovery queue (11 items).  
- `2c006fec7c` – `MEMORY_SEED` reader wired to precompact handoff.  
- `2a5f980a5d` – spec correction for `ollama-auto` (`mode:auto`).  
- `1dd17250b3` – added `embedTextBatch`; memo‑cache builder now 3× faster.  
- `826be35aa4`, `4a939fc35f`, `05e3c45196` – precompact MEMORY_SEED enrichment, seed‑status observability, test‑hermeticity fix.  
- `8c945662ac` – route‑suggest per‑session gate (fires on goal clear).  
- `a6aee37203` – tmp‑orphan sweep; removed all 6 real orphans.  
- `c28c8875ca`, `98312e8a08` – atomic write + env‑isolated rate‑file for doctrine gate.  
- `6162fd99af` – grep‑test portability fix (`process.execPath`).  
- `caabc8fea6` – 176 tribal→vault reference nodes materialized.  
- `35dc2ec4c3`, `d92b9f52f4` – subagent bundle OOM fixes (bounded reads, summarizeSystemViz bug).  
- `792beb75e8` – added `resolveObsidianMemDir`; routed `cag‑router` through it.  
- `e1b95b05a8` – split‑brain fix.  
- `8da1541c00` – excluded 9 571 `node_*` pointer stubs from memory recall (≈9.5k I/O calls saved).  
- `c7e346da99` – routed `cag‑router COLD_SOURCES` through `resolveObsidianMemDir`.  
- `d579626848` – fixed `extractWikilinks`; removed ~15 819 broken links.  
- `b45f08e732` – routed four hot hooks (`precompact-memo-emit`, `psn-leg-state-inject`, `stop-auto-capture-per-slot`, `stop-memory-size-watchdog`) through `resolveObsidianMemDir`.  
- `a9052f63b0` – added session‑gate to `backendAudit` route‑suggest (eliminates duplicate static messages).  
- `01c3b15f56` – serialized recall‑counter RMW with `withExclusiveLock`; fixed `.value` unwrap bug.  
- `U‑OLLAMA‑NAV‑ENFORCE` (`36105372ec`) – UserPromptSubmit hook injecting ollama bridge for nav intents; pins `gpt‑oss:20b`.  
- `U‑DARK‑WIKI‑RANK` (`767e8fea4c`) – library, tests, CLI ranking 32 630 dark wiki files (71 high‑value).  
- `U‑LINT‑ORPHAN‑OOM` – patch to `lint-wiki-orphans.mjs`; guards against 643 MB graph OOM; reports ~13 k orphans.  
- `U‑GRAPH‑STREAM‑DEGREE` – streaming graph pass (`graph-stream-degree.mjs`) avoids full graph materialization.  
- `U‑ADVISORY‑DECAY` – token‑savings lever with corrected metric (injections vs fires).  
- `regression-lock-audit` – fleet‑wide audit flag regressions lacking runnable test.  
- `Grep-index-first` – added `offloaded` field; measurable taken‑signal.  
- `/loop` slash command – auto‑injects 6 LOOP DISCIPLINE rules (closed‑loop default, eval gate each iter, feed‑forward, self‑correct, orchestrator/zero‑token, budget stop).  
- `/goal` slash command – enriched `goal-prereq-inject.mjs` with 4 GOAL DISCIPLINE rules.  
- `system-viz.md` – updated engine count (3697 wired / 89 unwired of 3786); added cheap‑node‑access & find‑cache resilience section.  
- `scripts/lib/ollama‑verified‑offload.mjs` – core verified‑offload wrapper with three built‑in verifiers (`enumMember`, `jsonShape`, `nonEmptyText`).  
- `scripts/ollama‑offload.mjs` – first live consumer (classify & digest) proven on real Ollama.  
- `feedback_primary_backend_builders_no_galaxy_gate.md` – forbids galaxy ownership gates from blocking primary slots.  
- `U‑VERIFIED‑OFFLOAD‑NAV` – ollama‑verified nav‑rerank CLI (`127234e940`).  
- `U‑VERIFIED‑OFFLOAD‑NAV‑WIRE` – auto‑fire PreToolUse:Bash hook (`0c641ef45a`, test `4b299e313b`).  
- `U‑COMMIT‑PRESSURE‑RELIEF` – commit‑aware memory relief scripts (`530afadcfa`).  
- `U‑NAV‑ADVISORY‑HARDEN` – parser hardening for nav‑rerank advisory.  
- `0e12da9955` – system fixes: orphan llama‑server reap, MCP daemon recovery, gitignore/allowlist update.  
- `U‑AUTOSTART‑LOOP‑GOAL` – `/startup‑<slot> /loop [10m] /goal` injected on all startup surfaces (post‑compact, full‑restart boot, launcher cold‑boot).  
- pre‑compact stub fix: slot‑scoped git resume and non‑stub re‑entry directive.  
- `U‑SUBAGENT‑GALAXY‑PACK` – spawned agents inherit parent slot’s galaxy domain pack (`CLAUDE.md`, `MEMORY.md`, paths, toolbelt, synthesis).  
- `U‑GOAL‑FORCE‑100PCT` – `/goal` now injects rule 5 enforcing 100 % completion.  
- `digest-files` CLI verb added to `scripts/lib/ollama‑verified‑offload.mjs`.  
- `9fcda446a1` – Stop hook `stop-task-boundary-compact-nudge.mjs`; wired into stop chain.  
- `61a6288d0e` – verified search rerank unit (`ollama-search-rerank.mjs`, `ask-ollama.mjs`).  
- `7dec157db0` – HS‑01 root fix: explicit `--session-id` anchor, peer‑leak removal, refuse‑on‑ambiguous.  
- `e81dec5cba` – HS‑01 env‑anchor patch (uses `CLAUDE_CODE_SESSION_ID`) and test suite.

**DECISIONS**  
- Use `/checkin-alpha` as slot‑binding wrapper; force‑take alpha slot, bind to topic `alpha-work`, then run full `/checkin`.  
- Keep fleet‑reaper under **golf** (unified fleet hygiene).  
- Commit via the **commit‑mutex (`U‑FGC‑1`) pathspec commit** to avoid absorbing peer churn.  
- Do not start Docker; leave audit‑only mode per operator hold on 47 migration tasks.  
- Leave `stop_on_failing_tests.mjs` built but unwired (fleet‑wide fail‑closed hook would block all chats).  
- Adopt detached, resilient Ollama pulls for golf’s model downloads (`ollama‑verified‑offload`).  
- Use verified‑offload pattern: code verifier → auto‑execute only on success.  
- Enforce 100 % completion in `/goal` via rule 5; four determinations added.  
- Auto‑start loop with `/loop [10m] /goal` on every startup surface (post‑compact, full restart, launcher).  
- Implement per‑slot live‑actuation lever for self‑compaction; native auto‑compact @90 % triggers `/compact`.  
- Replace materializing graph loads with streaming degree pass (`graph-stream-degree.mjs`).  
- Adopt semantic task‑boundary compact nudge as a Stop hook (`stop-task-boundary-compact-nudge.mjs`).  
- Use explicit `--session-id` anchor (HS‑01) → env variable fallback → PID‑pin fallback for stable session ID.  
- Merge duplicate rerank logic into shared lib; keep only one verified search/nav rerank unit.

**OPERATOR DIRECTIVES**  
- “golf is busy, you do the pull please.”  
- “try again” after stalled pull.  
- “read those with playwright and ensure our system is setup properly.”  
- “take over for golf to pull the models.”  
- “keep pulling then as we wait for downloads you claim the quality galaxy.”  
- “relook into the x articles I sent earlier for alpha to apply or compare to what we have.”  
- “utilize ultracode to get a full picture of our setup; actually read the files not just the titles.”  
- “prove self compaction and auto session start works so we can apply to full fleet… use ultracode to assess if there are further improvements we can make to precompaction, compaction, session handoffs and automatic session continuation using /startup‑natoname + /loop (make sure its the updated loop slash command) + /goal (updated goal slash command) + [whatever the chat slots current goal is + fallback work is domain specific tasks and units leftover in their task queue or existing roadmap]”.  
- “continue the standing /goal autonomously (/yolo): max local‑LLM utilization + token savings + obsidian/system‑viz/PSN synergy, enforce ollama for reads/searches/navigation…”.

**FINDINGS / BUGS**  
- Vitest 4 regression: `poolOptions.threads.*` ignored; migrated to top‑level `test.*`.  
- Encoding bug in soft‑config script (em‑dash mis‑parsed by PowerShell).  
- `main-tree-write-block` slot conflict: cwd `H:/prism` resolved to **papa** instead of alpha, causing intermittent write blocks.  
- Docker/NIM config Blackwell‑aligned; stale comment “7b workhorse” left in compose file.  
- Pull for `gpt‑oss:120b` / `gemma4:31b` stalls at ~93 % due to registry throughput limits; each restart re‑downloads the layer (no blob‑dedup).  
- `stop_on_failing_tests.mjs` exists but is not wired in any settings.json or bundle.  
- Enforce ESLint after edit had hard‑coded path (`C:/PRISM/mcp-server/eslint.config.js`); fixed to host‑portable `eslint.config.mjs`.  
- Missing file:line citation doctrine – added in G2.  
- “Ask‑before‑adding‑deps” doctrine present but no gate (gap).  
- 12 MIT textbooks not in corpus; ingestion queued.  
- Corrupted `gemma4` partial cleared for fresh resume.  
- Memory pressure: commit‑charge ~97 % dominated by `vmmemWSL`; mitigated by killing zombie TS servers, freeing 54 GB RAM.  
- MCP server false alarm after memory‑pressure spike; restarted supervisor.  
- OOM in subagent bundle due to unconditional large JSON reads; bounded reads fixed.  
- Duplicate rerank logic: `ollama-search-rerank.mjs` duplicates `ollama-nav-rerank.mjs`.  
- API rate‑limit errors on search/rerank arm B; pending re‑run.  
- Grep-index-first missing `offloaded`; added field, now measurable.  
- Regression‑lock audit uncovered 4 unlocked regressions (no test).  
- HS‑01 stable‑session‑id miskeyed long‑turn chats due to stale PID‑pin; fixed with explicit anchor & env variable.  
- 65 GB orphan llama‑server reap freed ~67 GB, commit % dropped from ≈97 % to ≈38 %.  
- MCP daemon down repeatedly; watchdog missing.  
- Git landmine: untracked 503 MB tribal‑embed shards; added to `.gitignore`.  
- `stop-memory-to-wiki-suggest.mjs` block had ENOENT bug (`"node"`); fixed with `process.execPath`.  
- `extractWikilinks` produced ~15 819 dangling links; greedy skill regex problematic, replaced by existence‑gate engine/dispatcher.  
- `withExclusiveLock` returned wrapper requiring `.value`; bug fixed.

**DOMAIN SPECIFICS**  
- **Engines / Actions**: `vitest.config.ts`, `MINBatchExtractorEngine.ts`, `McxBatchExtractorEngine.ts`, `PDFSourceRegistryEngine.ts`, `PDFFormulaExtractionEngine`, `CpkPredictionGateEngine`, `SPCProcessCapabilityEngine`, `memory-index-search-lib.mjs`, `dedupedContext()`, `embedTextBatch`, `route-suggest`, `stop-task-boundary-compact-nudge.mjs`, `ollama‑verified‑offload.mjs`, `ollama-nav-rerank.mjs`, `ollama-search-rerank.mjs`, `ask-ollama.mjs`, `precompact-handoff.mjs`, `session-start-auto-resume.mjs`, `stable-session-id.mjs`, `chat-slots.mjs`, `goal-prereq-inject.mjs`, `loop-state.mjs`.  
- **Dispatchers / Hooks**: `/checkin-alpha` wrapper, `chat-slots.mjs` claim/reclaim helpers, `audit-roadmap-drift.mjs`, `U‑FGC‑1` commit‑mutex, `main-tree-write-block.mjs`, `soft-config` script, `stop-goal-clear-advance.mjs`, `slot-domain-awareness-inject.mjs`, `psn-leg-state-inject.mjs`, `memory-relevance-inject.mjs`, `session-start-auto-resume.mjs`, `stop-task-boundary-compact-nudge.mjs`, `stop-memory-to-wiki-suggest.mjs`.  
- **Metrics / Paths**: settings.json env vars (`OLLAMA_MAX_LOADED_MODELS`, `PRISM_EMBED_CONCURRENCY`), Docker compose GPU config, nim‑docker‑launcher.mjs, `H:/prism/.claude/settings.json`, `mcp-server/data/pdf-sources/registry.json`, `memory-embeddings-sidecar.json`, `memo-embedding-cache.jsonl`, `tribal-embed-index.json`, `_embeddings.jsonl`.  
- **Paths**: `docker-compose.gpu.yml`, `nim-docker-launcher.mjs`, `scripts/lib/obsidian-mem-dir.mjs`, `scripts/lib/exclusive-file-lock.mjs`.

**TOOLS USED**  
- PRISM command `/checkin-alpha`; chat‑slot helpers (`claim`, `reclaim`); audit scripts (`audit-roadmap-drift.mjs`); commit‑mutex (`U‑FGC‑1`); hook `main-tree-write-block.mjs`; soft‑config script; Docker compose (`docker-compose.gpu.yml`) and nim launcher (`nim-docker-launcher.mjs`).  
- Ultracode workflows (`w7p3m3zp2`, `w2r7wnzri`, `wzlg0dikb`, `woifzfy8s`); Hermes workflow; System‑viz; Obsidian app; PSN integration.  
- CronCreate for recurring `/loop`; `cron-delete`.  
- Git tooling (commit, amend, rev-list).  
- Node scripts: `ollama-fanout.mjs`, `callOllamaOnce`, `node-card-offset.js`.  
- Test harnesses (`node:test` with hermetic async FS); scrutiny‑3way.mjs.

**OPEN THREADS**  
1. Complete stalled pulls for `gpt‑oss:120b` / `gemma4:31b` under golf’s durable infra; resume from cleared partials.  
2. Ingest 12 MIT textbooks into the academy corpus (lima/academy).  
3. Wire nav‑rerank and search rerank to auto‑fire after `/system-viz find`.  
4. Merge duplicate rerank logic into a single shared lib (`ollama-nav-rerank.mjs`).  
5. Re‑run search/rerank arm B once API limits subside; monitor rate‑limit handling.  
6. Finalize HS‑01 stable‑session‑id fix across all 24 callers (env anchor already covers).  
7. Implement per‑slot live‑actuation lever for self‑compaction; parallelize full‑fleet sweep (>2 min).  
8. Replace mtime‑based lock reclaim with PID liveness check in `zulu-orchestrator-sweep.mjs`.  
9. Automate find‑cache regeneration on every graph write (currently manual).  
10. Deploy auto‑start/loop updates fleet‑wide; validate on full fleet.  
11. Monitor and patch any residual RTK warning noise after manual prefixing changes.  
12. Verify that the backend‑builder rule (`feedback_primary_backend_builders_no_galaxy_gate.md`) is enforced across all primary slots.  
13. Continue queue: retrofit existing advisory hooks to verified execution where a verifier exists (next unit search/navigation offload).  
14. Explore further precompaction/compaction optimizations per ultracode assessment.
