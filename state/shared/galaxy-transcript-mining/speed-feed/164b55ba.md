# speed-feed session 164b55ba (2026-05-15, 4.8MB, spine 37KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `WireEdmFeasibilityPanel.tsx` wired to `wedm_assess_feasibility`; local fallback retained; P1 bug (min_achievable_mm) fixed.  
- `WireEdmCostBreakdownPanel.tsx` wired to `weCostEstimate`; all P1/P2 issues resolved, local math kept as offline fallback.  
- `turningDispatcher.ts`: added `turning_cost_estimate` enum entry, Zod schema, lazy import of `JobCostingEngine`, switch case mapping.  
- `src/routes/turning.ts`: POST `/cost` route returning `{ result }`.  
- Web API helper `turningApi.cost` in `web/src/api/turning.ts`.  
- `LatheCostPanel.tsx`: full rewrite with live wiring, `computeLocalCost()` fallback, LIVE/OFFLINE badge.  
- Dispatcher round‑trip test `turningCostEstimate.dispatcher.test.ts` written and passed legitimacy gate.

**DECISIONS**  
- **Dispatcher architecture**: use enum + Zod schema + lazy import to keep dispatcher lightweight; action names mirror engine methods (`turning_cost_estimate`).  
- **Engine mapping**: `JobCostingEngine.calculateJobCost()` returns a `CostBreakdown` with 10 categories; panel maps directly, no transformation except local fallback.  
- **Local fallback**: each panel keeps an offline calculation path to avoid API latency during development and for missing data scenarios.  
- **Testing strategy**: round‑trip dispatcher tests use algebraic invariants, variability spanning configs, and minimal/partial‑data failure modes; value‑bound assertions only (no presence‑only).  
- **Test coverage floor**: happy path + ≥3 failure modes + ≥2 adversarial inputs; variability floor ≥3 distinct material/density/batch size combos.  

**OPERATOR DIRECTIVES**  
- Create `memory/feedback_tests_plan_for_variability.md` and add a pointer to it in `MEMORY.md`.  
- Edit `CLAUDE.md` BUILD/TEST/CI section: “All tests must plan for variability and adaptability.”  
- Write `web/src/components/calculator/LatheCostPanel.test.tsx` covering the live path, local fallback, and error scenarios.  
- Optionally add a `/cost` route assertion in `src/__tests__/turning-edm-routes.test.ts`.  
- Run `tsc --noEmit` and `vitest run` on the two new tests; ensure 3‑of‑3 scrutiny passes before final `/handoff`.  

**FINDINGS/BUGS**  
- Feasibility panel: min_achievable_mm used wrong constant (0.02 vs engine’s 0.015); fixed.  
- Cost panel: LIVE hero total mismatched local “Power” tile; resolved by reconciling with engine totals.  
- Dispatcher: `overhead_pct` field removed because `JobCostingEngine` applies its own overhead rate; schema updated accordingly.  

**DOMAIN SPECIFICS**  
- **Engines**: `EDMFeasibilityEngine`, `EDMCostDocumentationEngine`, `JobCostingEngine`.  
- **Dispatchers**: `turningDispatcher.ts` (enum, Zod schema, lazy import).  
- **Routes**: `/api/v1/turning/cost` POST.  
- **Web API helpers**: `wireEdm.ts`, `turningApi.cost`.  
- **Components**: `WireEdmFeasibilityPanel.tsx`, `WireEdmCostBreakdownPanel.tsx`, `LatheCostPanel.tsx`.  
- **Tests**: dispatcher round‑trip (`turningCostEstimate.dispatcher.test.ts`), component tests (`WireEdm*Panel.test.tsx`, new `LatheCostPanel.test.tsx`).  

**TOOLS USED**  
- PRISM fleet tools: `/checkin`, per-agent handoff, precompact guard.  
- TypeScript + Zod for schema validation.  
- React/Vite + Vitest for component testing.  
- `makeStubServer` helper for dispatcher round‑trip tests.  
- TSC (`tsc --noEmit`) and Vitest CLI for build verification.  

**OPEN THREADS**  
1. Finish writing `LatheCostPanel.test.tsx`.  
2. Optional `/cost` route assertion in `turning-edm-routes.test.ts`.  
3. Run `tsc --noEmit` & `vitest run` on all tests; ensure 3‑of‑3 scrutiny passes.  
4. Commit memory file and CLAUDE.md edit.  
5. Final `/handoff` after successful build/test and scrutiny.
