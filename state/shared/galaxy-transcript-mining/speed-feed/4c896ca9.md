# speed-feed session 4c896ca9 (2026-06-22, 225.6MB, spine 342KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `1166e477db` – De‑inline HeatTreatmentAwareSpeedFeedEngine; canonical constants + NaN guard (`isPosFinite`).  
- `626481e848` – Gap #2: added `heat_treat_regime` to Vc path (single hardness derate, R15 dispatcher).  
- `c127137384` – Gap #3: fuzzy‑match warning for unknown materials.  
- `ed91a74f2c` – Hardened‑steel force caveat: H‑switch lowers Vc but keeps base kc in Kienzle force.  
- `556d2b65d3` – Gap #5b: replaced Euler–Bernoulli with Timoshenko (Cowper shear) for all tool materials.  
- `e6a23caf4a` – Gap #6: added additive `surface_integrity?` output.  
- `U‑SFC‑LIFE‑UNCERTAINTY‑FOSM` – Life‑uncertainty band now uses canonical Taylor‑sensitivity FOSM & ISO 3685 CVs.  
- `Gap #8 ball‑end effective diameter` – Extracted reusable `effectiveDiameter`; wired additive report into SFC.  
- `U‑SFC‑COOLANT‑THERMAL`, `U‑SFC‑KC‑EFFECTIVE‑ISO‑FORCE`, `U‑SFC‑KIENZLE‑FOSSIL‑TEST‑FIX`, `U‑SFC‑THERMAL‑KC‑HARDENED` – 6 backend SFC units.  
- `U‑SFC‑PROVEN‑PIPELINE‑ACTIVATE` – Proven lathe pipeline harness (13/13 tests, 3‑of‑3 scrutiny).  
- `09d605bac1` – Mill‑MCX skip accounting (`mineJMDiePrograms`).  
- `U‑SFC‑ORCH‑TURNING‑FIX` – Turning bug: rpmDiameter logic in SpeedFeedOrchestratorEngine.compute().  
- `c5fac24e43`, `26d5adbb36` – UI uncertainty surface (UncertaintyAdvisoryBanner, deriveAdvisory).  
- `3c26c7ae04` – Boring‑diameter fix (preserves bore ID for rpm/Vc).  
- `fd8df11f81`, `b359d166a5` – CAM‑strategy fidelity bug fixed; added no‑op `clearCache()`.  
- `efb0c97358` – Fixed autopilot material alias (“steel” → 1045); removed unsafe fallback.  
- `396ae501b7` – Updated Inconel test to grade‑specific 3200 kc (physics‑reviewed).  
- `efb570b720` – Added optional `base_rpm?` to MachineSpindle, `max_cutting_feed_mmmin?` to MachineAxes; updated `extractConstraints`.  

**DECISIONS**  
- Adopt canonical constants (`CANONICAL_HEAT_TREAT_REGIME`, `CANONICAL_KIENZLE`, etc.) and remove all inline magic numbers.  
- Use NaN guard (`isPosFinite`) on speed/feed calculations to avoid silent NaNs.  
- Wire new physics only as additive/report‑only unless safety‑critical; defer force‑path changes to dedicated units.  
- Commit after each fully scrutinized unit (2‑arm or 3‑arm) and checkpoint when risk/budget thresholds are reached.  
- Prioritize gaps that improve accuracy or expose latent bugs before adding new features.  
- Architecture: unify physics under `UltimateSpeedFeedEngine`; `SpeedFeedOrchestratorEngine` delegates to it (P2).  
- Scope: complete convergence P2, activate proven store load‑at‑init, run full lathe mine; optionally full mill mine.  
- Adopt two‑engine architecture: production uses `SpeedFeedOrchestratorEngine` (milling‑centric); convergence targets use `UltimateSpeedFeedEngine`.  
- Turning rpm/Vc must use workpiece diameter; boring uses bore ID.  
- CAM strategy resolution must preserve exact strategy label; generic fallback only for unknown strategies.  
- Add documented no‑op `clearCache()` to satisfy aspirational tests.  
- Defer AI‑confidence redesign (#25), missing process‑damping method (#29), cross‑galaxy normalizer (#28) to maintain quality.

**OPERATOR DIRECTIVES**  
- `/checkin-oscar` wrapper binds chat to slot `oscar`.  
- “continue” (verbatim) – resume autonomous loop after checkpoint.  
- Goal: finish all remaining backend dev tasks, prioritize oscar/SFC, complete frontend UI, prove 100 % before building electron/iOS/Android.  
- Approve convergence direction (P2) or run full lathe mine first.  
- Decide whether to extend decision artifact to turning cases or proceed with load‑at‑init of proven store.  
- “What’s next” → continue autonomous loop.  
- “Continue” → proceed with remaining backend tasks (thermal‑k derivation, proven full‑mine cron) and front‑end route‑switch for CV render (#21 pt2).

**FINDINGS / BUGS**  
- Dangling `REGIME_MODIFIERS`/`REGIME_EXPECTED_HARDNESS` refs → ReferenceError.  
- Silent NaN propagation in speed/feed due to missing guard.  
- Fuzzy‑match material resolution silently defaulted to steel (25–55 % Vc/force error).  
- Hardened‑steel force under‑predicted because H‑switch lowered Vc but kept base kc.  
- Cryo thermal regression: cryogenic coolant produced higher temp than flood due to missing direct cooling factor.  
- Stale guardian tests for Kienzle `mc` values (ISO N/S).  
- Inline magic numbers in life‑uncertainty band; material‑independent CV ignored Taylor sensitivity.  
- Duplicate test file for HeatTreatmentAwareSpeedFeedEngine.  
- Turning bug: orchestrator used tool diameter → Vc = 1.8 m/min vs engine’s 185 m/min.  
- CAM‑strategy fidelity bug collapsed strategy names to “conventional”.  
- Mastercam `.mcx‑8` binaries silently counted as zero samples; undercount in proven‑mill data.  
- Missing `clearCache()` API caused aspirational cache tests to fail.  
- Autopilot alias `"steel"` → 1018 (kc 1700) diverged from canonical 1045 (kc 1800).  
- Inconel test asserted generic S‑group kc 2800 while engine returned correct grade‑specific 3200.  
- MachineAwareSpeedFeedEngine defaulted to 15 kW power and 100 mm/min feed due to wrong field names; hardcoded maxRPM/torque ignored real specs.  
- SpeedFeedAdvancedAIEngine self‑consistency under‑estimated (fixed 10‑unit binning, double penalty on agreement, non‑deterministic `Math.random()`).  
- Chatter/Stability engines returned zeros; missing `calculateProcessDamping` method.

**DOMAIN SPECIFICS**  
- **Engines / Actions**:  
  - `HeatTreatmentAwareSpeedFeedEngine`, `UltimateSpeedFeedEngine`, `SurfaceIntegrityEngine`, `StochasticToolWearEngine`, `BallEndMillEngine`, `SpeedFeedOrchestratorEngine`, `NineAxisOrchestratorEngine`, `ProvenSpeedFeedAggregatorEngine`.  
- **Dispatchers / Schemas**: `calcDispatcher.prism_calc` (Zod schema with `.passthrough()`), `prism_calc` pipeline.  
- **Metrics / Paths**: Vc, Vf, speed, feed, force, torque, power, thermal temp, tool life, uncertainty bands (`ci_95_low`, `ci_95_high`, `cv_pct`).  
- **Key Files / Paths**:  
  - `mcp-server/src/engines/...` (engine sources).  
  - `constants.ts` (canonical tables).  
  - `calcDispatcher.md` (dispatcher schema).  
  - Test suites under `tests/` and `vitest`.  
- **Machine Specs**: `MachineSpindle`, `MachineAxes` (optional `base_rpm?`, `max_cutting_feed_mmmin?`).  

**TOOLS USED**  
- PRISM CLI helpers (`/checkin-oscar`, `chat-slots.mjs`, `commit-lane guard`).  
- Node scripts for slot reclaim/claim.  
- TypeScript compiler (`tsc --noEmit`).  
- Vitest test runner (completeness gate).  
- Physics‑reviewer, independent reviewer, safety‑physics reviewer agents.  
- Ultracode workflow engine (fanout gate).  
- `node` runtime validation and live tests.  
- 3‑of‑3 scrutiny framework (`scrutiny-3way.mjs`).  
- Explore (Sonnet) for fan‑out search.  
- RTK bash prefix, `loop-state.mjs`.  

**OPEN THREADS**  
- Gap #10 outcome‑capture sink (additive output).  
- Thermal‑kc consistency (already addressed; no further action).  
- Frontend phase‑1: deprecate orphan `SpeedFeedPage`, surface uncertainty signal, port‑3100 E2E with Quebec.  
- Electron/iOS/Android build once frontend is 100 % proven.  
- Convergence P2 implementation – operator sign‑off required.  
- Load‑at‑init of proven store into orchestrator.  
- Full lathe mine scheduled task (validate engine vs JM Die at scale).  
- Full mill mine extraction (Mastercam `.mcx` extractor) and integration.  
- Thermal‑k derivation physics cycle (#11).  
- AI‑confidence redesign (#25), missing process‑damping method (#29), cross‑galaxy normalizer (#28).  
- Remaining backend tasks: #17 proven full‑mine cron, #11 thermal‑k derivation.  
- Deploy turning fix (restart `:3100` MCP bridge).  
- Sign‑off convergence re‑baseline (#15).  
- Front‑end route‑switch for CV render (#21 pt2).
