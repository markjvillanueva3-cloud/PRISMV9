# speed-feed session 7d4a5202 (2026-06-24, 17MB, spine 178KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Backend accuracy fix (lever routing) – commit `ec0ce2ea26`.  
- Frontend test‑only commits: `U-SFC-FE-TEST-OVERSTRICT`, `U-SFC-FE-UNIT-TOGGLE-TEST`, `U-SFC-FE-LATHE-G71-ASYNC`, `U-SFC-FE-<slug>`.  
- Nine‑axis stale‑test cleanup commit.  
- 6 additional commits + ~11 test fixes (CalculatorPage smoke 17→22/24, finishView 6/6, routeContinuity 5/5).  
- Backend deflection‑Vc‑lever fix deployed; 276 K‑cell‑proven accuracy.

**DECISIONS**  
- Use `slot-bind-enforce` to deterministically bind the `oscar` slot before `/checkin`.  
- Separate engine paths: `ProductEngine.sfcCalculate` (page), `SpeedFeedOrchestratorEngine` (backend corpus).  
- Lever routing: force‑driven → `fz`, feed→`fz`, rpm clamps `Vc`, power applied last.  
- Deploy via elevated restart of the `:3100` daemon (`singleton-service-guard.mjs`, `Start-Process -Verb RunAs`).  
- Frontend built with Vite SPA; tests run with Vitest, async waits (`findByRole`, 5 s timeout).  
- Scope to safe, verified increments only – no risky edits until product decisions.  
- Product decisions: choose strict machine‑filter for holder (blocks #4/#6), autoProgramming label “open lathe wizard” vs “Launch Lathe Wizard”.

**OPERATOR DIRECTIVES**  
- Prioritize backend → frontend build → native apps.  
- Continue autonomous overnight loop; measure remaining sibling tests one file at a time, commit clean fixes.  
- If desired, override to push #3/#4 data fixes immediately – say the word.  
- Resolve 3 product decisions (~5 min each) before next session.

**FINDINGS/BUGS**  
- Deflection‑Vc lever bug collapsed `Vc` by 2–7×; now fixed.  
- Nine‑axis stale‑test failures unrelated to fix.  
- CalculatorPage: 7 failing tests – 3 over‑strict (memory, tool‑crib), 4 production gaps (missing lathe G71 package, CAT40 Big+ option, holder quick‑select label mismatch, saved preset fixture name mismatch).  
- Backend `VITEST_REPORT` stale 56 days; cannot refresh.  
- Hanging CAM test workers (`CAMX-MS21`, `CAMTribalKnowledge`, `CAMKernelDispatcher`) block fleet‑infra; full suite hangs at ~255 s.

**DOMAIN SPECIFICS**  
- Engines: `SpeedFeedOrchestratorEngine`, `ProductEngine.sfcCalculate`, `SpeedFeedNineAxisOrchestratorEngine`.  
- Dispatchers: `/api/v1/speed-feed/orchestrate`, `/api/v1/session/memory/recall`, `calculatorHolderLibrary.ts`, `holderPackageInterfaceId`, `holderPackageMatchesStyle`.  
- Metrics: `Vc`, `fz`, power, torque, deflection, tool life; 276 K‑cell accuracy proven.  
- Paths: `H:/prism/.claude/helpers/chat-slots.mjs`, `singleton-service-guard.mjs`, node scripts (`/checkin-oscar`), reference file `reference_oscar_sfc_frontend_triage_2026_06_24.md`.

**TOOLS USED**  
- PRISM check‑in pipeline, chat‑slots helper, slot-bind-enforce hook, singleton-service-guard, Vitest, `npm run build`, electron-builder, Capacitor config.  
- Node scripts: `/checkin-oscar`, `chat-slots.mjs`.  
- Hermes (xAI Grok), Ollama models, auto‑compaction hooks.

**OPEN THREADS**  
- Finish frontend gaps: #3 lathe G71 package, #4 CAT40 Big+ option, #6 holder quick‑select label, #7 saved preset fixture name.  
- Build native apps (Electron, iOS, Android) after deploy; address missing Android SDK/JDK and macOS tooling, electron symlink‑priv workaround.  
- Re‑run full e2e tests against live backend.  
- Add missing data packages for G71 and CAT40 Big+.  
- Resolve CAM catalog coverage cluster (add 80+ packages).  
- Fix hanging CAM workers; regenerate stale `VITEST_REPORT` with sharded `--pool=forks`.  
- Re‑arm cron once blockers cleared.
