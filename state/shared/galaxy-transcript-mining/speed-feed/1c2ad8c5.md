# speed-feed session 1c2ad8c5 (2026-06-04, 13.3MB, spine 111KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑OSC9‑GPU‑TAG‑CACHE` (7fb4fabb54): deterministic GPU tag cache, frozen 77 tags; expanded consensus now 12 base + 6 turning/carbide cells (no fabricated HSS).  
- `U‑OSC9‑ALL‑LOGICAL‑INPUTS` (546f00cb36): full sweep of milling + drilling + turning × 6 ISO groups × 5 diameters (180 cells); 100 % containment for milling, turning shows high‑vc bias, drilling remains broken.  
- `U‑OSC9‑DRILL‑CHIPGEOM` (81a3eb72c8): fixed Kienzle force model for drilling/tapping; real twist‑drill chip geometry, safety‑physics oracle S(x)=0.92, all 3 former red tests green, no milling regression.  
- `U‑CSFH‑01` (df68a51086): extracted combinatorial axes and physics oracles into shared modules; 2‑reviewer PASS, behavior‑preserving.  
- `b69f872681`: Open‑Cartesian comparison runner (`openCartesianCompareSweep.ts`) with 19/19 tests, 3‑of‑3 Stop PASS.  
- `7431657f68`: Safety‑critical physics fix: carbide de‑rate for hardened steel (107 m/min → 38.5 m/min), updated constants and tooling logic.

**DECISIONS**  
- Adopt GPU tag cache to make expanded consensus reproducible offline, preventing non‑deterministic Ollama runs.  
- Expand sweep to all logical inputs before adding closed‑loop calibration; ensures every regime is validated.  
- Fix drilling physics first – blocks meaningful comparison with HSMAdvisor/GWizard.  
- Defer romeo/juliett DB accessors until those slots signal readiness; focus on closed‑loop testing now.  
- Use scratch‑cwd for outcome bus to avoid EPERM lock; keep engine unchanged.  
- Adopt comprehensive closed‑loop calibration path (`CALIB-PERSIST → CALIB-APPLY-WIRE`) rather than isolated fixes.  
- Map all substrate areas first (6 parallel agents) before touching critical physics.  
- Fix hardened‑steel Vc bug before calibration loop to prevent band‑aid factor learning.  
- Gate all changes through safety‑physics oracle and 3‑of‑3 Stop scrutiny.

**OPERATOR DIRECTIVES**  
- “foxtrot isn’t running. Get a more up‑to‑date scope on what has been done past 48 h.”  
- “Run precompact to plan for closed‑loop testing if we’re ready.”  
- “We can do romeo‑juliett data when they’re ready. We should have enough to start closed‑loop tests and comparisons to the other 2 calculators.”  
- “run it on LIVE data” – produce real divergence + specialized‑calc candidates.  
- “Always go with the most comprehensive path forward.”

**FINDINGS/BUGS**  
- Broken drilling regime: `ap_mm=0` → Kienzle rejects; fixed by computing chip geometry from diameter & point angle.  
- Fabricated HSS cells in earlier GPU classification (22/22 conflicts); removed via updated script.  
- EPERM tax on outcome bus due to live ring lock; resolved by running sweep from scratch‑cwd.  
- Min‑n guard for turning baseline: 1‑row `N` cells over‑claimed; added `insufficient_baseline` flag.  
- P0/P1 bugs in exhaustive runner (silent cell drop, weak‑rule coverage gap); fixed with result‑cap warning and extended disagreement rule.  
- Infinity bug & timeout magic numbers resolved (`PHYSICS_TIMEOUT_MS`, `FAST_TIMEOUT_MS`).  
- G‑Wizard abstains on generic material×diameter combos (`gwizard_computed: 0`).  
- PRISM diverges from handbook by ~45 % median; no candidates surfaced due to `minCells=3`.  
- Safety flag: hardened steel (>45 HRC) with carbide tooling – PRISM recommends 107 m/min vs handbook’s 38.9 m/min (+175 %).  
- Physics bug: PRISM’s H‑group Vc formula lacks tool‑material term; CBN‑tabulated rows incorrectly use carbide speed.

**DOMAIN SPECIFICS**  
- Engines/Actions: `sfc-full-logical-sweep.mts`, `sfc-gpu-classify-deferred-series.mts`, `SpeedFeedTriVendorBatchComparatorEngine.run()`, `HSMAdvisorComparatorBridgeEngine.run()`, `SpeedFeedDeepLearningEngine`, `OutcomeFeedbackBridgeEngine`, `SpeedFeedExhaustiveCombinationEngine`, `jmFirstSweep`, `openCartesianCompareSweep`.  
- Dispatchers/Metrics: `InfiniteConditionCombinatorEngine` (structured sampling), `SpeedFeedBaselineComparatorEngine`, `VendorDeltaCalibrationBridge.calibrateFromLedger`, `UltimateSpeedFeedEngine.calculate` (predictSpeed/predictFeed multipliers).  
- Paths: `state/shared/sfc/prism-vs-consensus-sweep.json`, `state/shared/sfc/expanded-consensus.json`, `state/CURRENT_POSITION.md`, `open_cartesian_compare.jsonl`.  
- Constants: `TOOL_MATERIAL_VC_FACTOR[H]` in `constants.ts`.

**TOOLS USED**  
- PRISM slot helpers (`chat-slots.mjs`, `slot-bind-enforce.mjs`).  
- `/checkin-oscar` wrapper, `/precompact-pending-guard.mjs`.  
- GPU classification script (`sfc-gpu-classify-deferred-series.mts`).  
- Sweep enumeration script (`sfc-full-logical-sweep.mts`).  
- Outcome bus engine (`shopFloorOutcomeBridge.ts`, `OutcomeCaptureBusEngine.ts`).  
- Vitest for unit tests, TSX for smoke checks.  
- PRISM test harness (`vitest run --pool=forks`), 3‑of‑3 Stop gate (arms A/B/C).  
- Safety‑physics oracle, Ultracode mapping workflow (`feature-dev:code-explorer`, `safety-physics` agent), code‑explorer plugin, CI timeout adjustments.

**OPEN THREADS**  
- Build `U‑OSC9‑OPEN‑CARTESIAN‑COMPARE`: runner that batches open‑Cartesian space and feeds to vendor comparators; detect regimes where PRISM diverges.  
- Finalize closed‑loop calibration keystones: `U‑OSC9‑CALIB‑PERSIST`, `U‑OSC9‑CALIB‑APPLY‑WIRE`.  
- Integrate romeo/juliett DB accessors (`U‑CSFH‑03`) when slots ready.  
- Resolve slow variability vitest (2.5 s/call) or replace with focused TSX assertions.  
- Persist calibration factors to disk (`CALIB-PERSIST`).  
- Validate closed‑loop on all divergent regimes (including hardened‑steel).  
- Address remaining over‑speed in orchestrator’s separate “80” table.  
- Ensure full 3‑of‑3 Stop passes after all changes committed.
