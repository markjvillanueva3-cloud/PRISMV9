# speed-feed session bca3789f (2026-05-19, 30.5MB, spine 132KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**

- `baef3c361d` – dashboard offload‑rate fix & drift guard  
- `94d4d0feac` – 33× wiki recall via leaf‑scan  
- `doc-reflection` – memory file + wiki append  
- `ecbadc2862` – obsidian_lookup tool (adds 644 memories)  
- `b756569c04` – dispatcher_map tool (scans DISPATCHER_DIGEST.md)  
- `c55b68b8ea` – infra‑docker fix (production stage, port filter, launcher guard)  
- `b041693181` – reaper‑coord noise separation  
- `60dbc72116` – RTK prefix‑reminder hook adopted from kilo  
- `6d0139c0b6` – semantic_lookup (14 738 pre‑built vectors)  
- `aa58c8f3eb` – obsidian_precheck hook (memory pre‑check without altering wiki hook)  
- `53957302d4` – wiki_leafidx_failLOUD detection for silent no‑op exit‑0 in build-wiki-leaf-index  

---

**DECISIONS**

- Pivoted to backend dev tooling; expanded Ollama‑PRISM bridge with read‑only tools: obsidian_lookup, dispatcher_map, semantic_search, tribal_lookup.  
- Adopted kilo’s stranded HTML+RTK cluster; deferred root‑doc guard due to stale twin issue.  
- Enforced per‑file 2‑reviewer scrutiny gate (fail‑loud).  
- Commit to delta slot for consolidated work; used `[MAIN]` prefix when committing from shared tree.  
- Use `stable-session-id.mjs` for handoff and armed precompact guard.  
- Slot‑lima worktree (`H:/prism-slot-lima`) to avoid cross‑chat misattribution.  
- Adopt R12 fail‑loud guard on wiki leaf index generation.  
- Keep constants external (`mcp-server/src/physics/constants.ts`).  
- Never skip git hooks unless explicitly requested; commit only staged deliverables via `git commit -- <pathspec>`.  
- Disable `core.fsmonitor` during commits to prevent hangs.

---

**OPERATOR DIRECTIVES**

- Continue html and rtk upgrades, expand obsidian wiki & context management with ollama/system‑viz; keep building massive build; find high ROI obsidian/ollama usage; continue from where left off.

---

**FINDINGS/BUGS**

| Issue | Resolution |
|-------|------------|
| Offload rate miscalc (true ≈ 9.3%) | Corrected denominator, added drift guard, updated dashboard |
| L2b HTTP transport blocked (`POST /mcp`) | Pivoted to in‑process dispatching; flagged for future fix |
| Dispatcher_map schema drift | Implemented header validation guard |
| HTML companion root‑doc twin stale | Deferred guard; reimplement later with fresh twins |
| RTK prefix‑reminder main() guard missing | Added `if (require.main === module)` guard |
| Infra Docker build broken | Fixed Dockerfile, added port filter & launcher guard |
| Silent no‑op exit‑0 in `build-wiki-leaf-index.mjs` | Added fail‑loud detection (`U‑WIKI‑LEAFIDX‑FAILLOUD`) |
| Parser bug for flat‑string triggers | Resolved on main (`2ba5d4baf3`) |
| Misattribution class caused commits to be swept into other slots | Fixed class logic |
| Wedged `git commit` due to fsmonitor & temp‑space exhaustion | Retried after cleanup |
| Potential V8 string‑cap RangeError in large `lines.join("\n")` | Mitigated with streaming write guard |

---

**DOMAIN SPECIFICS**

- **Engines / Actions**  
  - EDM dispatcher: `electrode_ai_reason_full`, `advanced_analysis`, `deep_learning_analyze`, `ultimate_analyze`
- **Dispatchers / Tools**  
  - `obsidian_lookup` (22 734 leaf files)  
  - `dispatcher_map` (scans DISPATCHER_DIGEST.md)  
  - `semantic_search` (BM25 + int8‑quantized embeddings, 14 738 vectors)  
  - `tribal_lookup` (4 230 tip corpus)  
  - `obsidian_precheck` hook
- **Metrics / Paths**  
  - Offload rate: `rawOffloadRate`, `adjustedOffloadRate`  
  - Fleet‑reaper events: `fleet-reaper-coordinator suggest/keep`  
  - Dashboard paths: `/api/chat/tools`  
  - Hook paths: `unit-knowledge-pack.mjs`, `html-companion-guard.mjs`, `rtk-prefix-reminder.mjs`

---

**TOOLS USED**

| Category | Tool / Script |
|----------|---------------|
| PRISM Commands | `/checkin`, `/startup`, `/precompact`, `/compact` |
| Slot Management | `chat-slots.mjs`, `slot-bind-enforce.mjs` |
| Handoff | `per-agent-handoff.mjs`, `precompact-pending-guard.mjs` |
| Milestone Tracking | `milestone-tracker.mjs` |
| Bridge / Hooks | `ollama-prism-bridge.mjs`, `unit-knowledge-pack.mjs`, `unit-knowledge-pack-inject.mjs` |
| Backend Dev Tools | `rtk-prefix-reminder.mjs`, `html-companion-guard.mjs`, `dispatcher_map.js`, `semantic_search.js`, `tribal_lookup.js`, `obsidian_lookup.js`, `obsidian_precheck.js` |
| Testing | Node test suites (`*.test.mjs`) |
| Build Tools | Docker compose, npm scripts |

---

**OPEN THREADS**

1. Iter 2 HTML adopt – implement root‑doc guard deferral, add `checkTwin()` integration test.  
2. Iter 3 HTML‑PRIMARY follow‑ups – resolve stale‑lock, case‑fold argv, dispatcher mirror, double‑escape.  
3. RTK coverage gaps – complete hook coverage analysis and missing tests.  
4. Unit‑knowledge‑pack injection – finalize throttling, context output, ensure no cross‑chat mis‑commit.  
5. L2b MCP dispatcher integration – fully integrate in‑process dispatching; resolve HTTP transport blocker or migrate to alternative transport.  
6. U‑OFFLOAD‑AUDIT – audit remaining routing suggests and offload decisions.  
7. AGENT_CHAT visibility on non‑zero exit.  
8. Stage‑order assertion for tool execution.  
9. Export of exit‑code constants.  
10. `_stats.md` write protection.  
11. Streaming JSONL write to avoid V8 string cap.  
12. Further Obsidian/wiki context‑management enhancements (memory cache, wiki‑leaf index refresh triggers).
