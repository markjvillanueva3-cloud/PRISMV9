# speed-feed session 86373eb3 (2026-06-10, 48.8MB, spine 403KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- New 3‑window launcher `Launch‑PRISM‑Fleet‑3win.ps1`; desktop shortcuts updated (fallback 4‑window).  
- Added `capture‑accounts‑runbook.md` and guard in `nim‑autostart.mjs` to skip NIM when `LOCAL_LLM_BACKEND=ollama`.  
- Docker autostarts cleaned: removed `ollama/ollama‑nomic‑preload` from `COMPOSE_SERVICES`; dropped `ollama` from `DEFAULT_SERVICES`, added fail‑fast guard for `--services=mcp`.  
- Updated `docker-compose.yml`: removed obsolete `version:` key, set `KEEP_ALIVE=30m`, `MAX_LOADED_MODELS=4`.  
- Infra fixes: NIM disabled (`restart=no`), `LOCAL_LLM_BACKEND=ollama`, master‑index heap guard added (no OOM).  
- Reverse Obsidian mirror hook wired to PostToolUse.  
- Built & tested `vault-to-lora-dataset.mjs` – 245 Alpaca triples, 19/19 tests pass.  
- Implemented `vault-to-gnn-refpool.mjs`: extracts 4 engine→dispatcher pairs; writes `system‑graph.json` via streaming I/O (adds refs 125 → 129).  
- Fixed `seed‑ghost-from-unwired.mjs` with streaming I/O; added `writeGraphStreamingAtomic` to `scripts/lib/graph‑io.mjs`.  
- Updated all tests – 37/37 pass.  
- Mine‑galaxy‑transcripts: content‑based discovery, cache (`discoverByContent.cache.json`), per‑transcript slice cap `MAX_SLICES_PER_TX=14`, new `--next-unpopulated --backend-only` mode.  
- Sidecar rebuilds (`build-memory-index-sidecar.mjs`, `build-memory-embeddings-sidecar.mjs`) invoked after each mining run.  
- 35 galaxy synthesis memos populated; Hermes‑Obsidian self‑learning loop E2E proof executed.  
- Fleet test‑drive completed for all 13 slots.  
- Updated system map (`SYSTEM-MAP.md`), staged GEPA candidate in `state/shared/specs`.  

**DECISIONS**  
- Drop NIM containers; switch to Ollama backend (`LOCAL_LLM_BACKEND=ollama`).  
- Keep native Ollama auto‑start via scheduled task `PRISM Ollama Serve`; remove compose‑ollama from autostart.  
- Guard `nim-autostart.mjs` so never starts NIM when Ollama is active; launcher fail‑fast guard for misused `--services=mcp`.  
- Align docker‑compose: no `version:` key, keep‑alive 30 min, max loaded models 4.  
- Flip recall injectors (`MASTER_INDEX`, `MEMORY_INDEX`, `WIKI_PRECHECK`) to 1; add heap‑aware sidecar guard for master‑index.  
- Do not re‑register Brain Refresh with `--with‑viz`; leave redundant RAG hooks dead, wire only necessary ones.  
- Patch tribal installers to write durable runners (not TEMP).  
- Create synthesis cron installer; leave `find-connections` unscheduled (per‑target).  
- Reuse existing seeder for seed‑ghost-from-unwired.mjs; extend with vault‑source mode.  
- Adopt streaming I/O (`readGraphStreaming`, `writeGraphStreamingAtomic`) for graph reads/writes.  
- Keep LoRA feeder separate from GNN feeder.  
- Do not auto‑force Ollama routing fleet‑wide; raise adoption of advisory hooks instead.  
- Generalize transcript miner to single router classifying all 34 galaxies (content‑based).  
- Cache per‑transcript classification for 1000× speedup; cap slices per transcript (`MAX_SLICES_PER_TX=14`).  
- Increase synthesis timeout to 900 s for `gpt‑oss:120b`.  
- Use vault memo presence as success signal, drop misleading grep wrapper.  
- Run detached overnight‑vault‑compound.mjs pipeline (yolo mode).  
- Do not re‑add `prism-vault-fs` MCP to Hermes config; never log `PRISM_OBSIDIAN_API_KEY`; load via plugin data.json only.  
- Set `OLLAMA_CONTEXT_LENGTH=65536`, configure Hermes (`model.context_length: 65536`, `max_tokens:16384`).  

**OPERATOR DIRECTIVES**  
- Capture accounts 2–6 with `claude login` + `node scripts/capture‑claude‑credentials.mjs account‑N …`; create `ROTATION_ORDER.json`.  
- After capture, double‑click **PRISM Fleet (3‑window)** to launch; credentials swap and windows stagger automatically.  
- Run elevated PowerShell block to register scheduled tasks (install‑synthesis‑crons.ps1, install‑tribal‑consolidate-cron.ps1, etc.).  
- Commit the 9 edits or keep them live – choice.  
- Do not self‑elevate; task registration requires elevated PowerShell.  
- Never kill peer `claude.exe` (pid 120268).  
- Do not commit `.env`; never expose `PRISM_OBSIDIAN_API_KEY`.  

**FINDINGS/BUGS**  
- NIM containers exit 137 OOM (Docker capped ~16 GB, NIM needs 20–40 GB).  
- PRISM probing dead NIM on every request (`LOCAL_LLM_BACKEND=auto`) caused latency/timeouts.  
- Compose `mcp` service name mismatch fixed by guard; duplicate MCP daemons warning transient.  
- Qdrant collections 768‑dimensional match Ollama’s nomic‑embed; no reindex needed.  
- Master‑index OOM resolved with heap guard.  
- Recall injectors now enabled and firing.  
- TEMP‑reaped cron runners fixed by durable path.  
- Missing synthesis crons created installer for three runnable ones; `find-connections` left unscheduled (per‑target).  
- `seed‑ghost-from-unwired.mjs` crashed ERR_STRING_TOO_LONG on 642 MB graph – streaming I/O solved.  
- LoRA feeder CRLFs normalized; `.md` extension leak hardened in `instructionFromName`.  
- Regex for frontmatterField fixed (`[ \t]`, non‑greedy).  
- Transcript classification regex extended to recover ~250 topic‑bearing handoffs; 1,355 handoffs now classified into mineable/classify-only buckets.  
- Ollama `/v1` endpoint ignores `options.num_ctx`; caused prompt truncation and Hermes failures.  
- `node-process-janitor.mjs` killed background MCP & Hermes processes – fixed via protect regex.  
- Fleet‑reaper mistakenly terminated overnight vault pipeline twice; extended regex protects it now.  
- Missing scheduled tasks: OCR training loop, slot migration status, system awareness freshness (operator‑only).  
- Obsidian REST API fails to bind when vault picker window remains open; requires manual launch or `obsidian://open?path=H%3A%2Fprism%2Fknowledge`.  
- Hermes gateway dies on logoff; needs `hermes gateway install` as SYSTEM service.  

**DOMAIN SPECIFICS**  
- Slots & windows: W1 (alpha, bravo, golf, india, papa, sierra, romeo); W2 (charlie, hotel, oscar, echo, xray, juliett); W3 (delta, kilo, foxtrot, mike, whiskey, lima).  
- Key scripts/dispatchers: `Launch‑PRISM‑Fleet.ps1`, `slot‑tab‑boot.ps1`, `nim‑autostart.mjs`, `local‑llm‑bridge.mjs`, `docker‑intel‑autostart.mjs`, `ollama‑docker‑launcher.mjs`.  
- RAG/CAG corpus: `knowledge/.obsidian` (167 MB tribal + 118 MB wiki).  
- NN/GNN reference pool: `system‑graph.json` nodes of kind `ghost.unwired-engine`; gate requires `isValidDispatcher`, confidence ≥0.8.  
- LoRA dataset builder: Alpaca schema `{instruction, input, output}` from vault feedback memories (`feedback_*`).  
- Graph I/O: `scripts/lib/graph‑io.mjs` – `readGraphStreaming`, `writeGraphStreamingAtomic`.  
- Transcript miner: generalized router classifying all 34 galaxies; writes per‑galaxy memories.  
- Galaxy synthesis: `galaxy-synthesis-refresh.mjs` clusters vault memories into `patterns/<galaxy>_synthesis.md`; compounding into `<galaxy>/MEMORY.md`.  
- Engines: Ollama MAP (`gpt‑oss:20b`) → REDUCE → synthesis (`gpt‑oss:120b`).  

**TOOLS USED**  
- PRISM scripts: `seed-ghost-from-unwired.mjs`, `vault-to-lora-dataset.mjs`, `vault-to-gnn-refpool.mjs`, `mine-india-transcripts.mjs`, `galaxy-synthesis-refresh.mjs`, `scripts/lib/graph‑io.mjs`.  
- External services: Ollama local LLM (Qwen2.5‑Coder), ultracode, Qdrant embeddings.  
- Hooks & dispatchers: `cag-router-inject.mjs`, `ollama-unified-semantic-router.mjs`, `node-card-prefetch-inject.mjs`.  
- PowerShell installers (`install-synthesis-crons.ps1`, etc.).  
- Docker compose for services.  
- Node.js hooks (mjs).  
- Hermes cron utilities: `hermes cron create/run/tick`.  
- `overnight-vault-compound.mjs`; `node-process-janitor.mjs`, `fleet-reaper-sweep.mjs`.  

**OPEN THREADS**  
- Run elevated PowerShell block to register tasks and verify success.  
- Commit or keep live the 9 edits (7 H: files + 2 C: settings).  
- Implement & test generalized transcript miner for all 34 galaxies; ensure coverage of ~250 handoffs.  
- Finish mining remaining 21 galaxies (agent‑orchestration, discovery, dormant‑data, knowledge‑conversion, corpus‑aggregation, database‑expansion, primary PRISM app domains).  
- Run sidecar rebuild + re‑embed after each batch; schedule foreground tasks per galaxy or ≤3 with reaper protection.  
- Verify final compounding into `<galaxy>/MEMORY.md` for all galaxies; run `galaxy-synthesis-refresh.mjs`.  
- Monitor GPU usage and adjust model loading strategy (avoid contention between gpt‑oss:20b, 120b).  
- Validate weekly self‑review job on `gpt‑oss:120b` (not yet E2E verified).  
- Mine GEPA round 2 success/failure traces.  
- Operator‑elevated re‑registration of missing scheduled tasks (OCR training loop, slot migration status, system awareness).  
- Install Hermes gateway as SYSTEM service (`hermes gateway install`).  
- Confirm Obsidian REST API binding after vault launch; eliminate manual intervention.  
- Monitor future Ollama context truncation; verify `ollama ps` CONTEXT remains 65536.
