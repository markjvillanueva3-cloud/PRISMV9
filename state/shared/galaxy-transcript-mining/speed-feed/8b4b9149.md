# speed-feed session 8b4b9149 (2026-06-10, 68.2MB, spine 680KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**

- 5ae481f748 – EPERM‑leak fix in OutcomeCaptureBusEngine + orphan‑tmp cleanup  
- 86f0e3fe0c – wired three orphan SFC engines (SpeedFeedTriComparatorEngine, SpeedFeedExhaustiveCombinationEngine, SpeedFeedDownstreamSubscriberEngine) + 16 round‑trip dispatcher tests  
- a2dbfa76e1 – closed‑loop driver + rescue of 7 orphaned SF engines + consensus‑filter bug fix (exclude not‑aligned externals)  
- 43e1b8e449 – G‑Wizard alignment symmetry added, regression test for misaligned advisory  
- 891c66e728 – full sweep comparison (74 cells) with material ISO mapping fixed; missing N/H coverage added  
- 16d6eecef4 – SpeedFeedCalibrationPersistEngine + tests, wiring to prism_calc:speed_feed_calibration_persist  
- 890b2c39e7 – VC assessment spec addendum with live‑results; memory capture of closed‑loop training results  
- 3438987f0e – hardening: clamped flag fix, persist tmp cleanup, skipped_lines observability  
- f31398a1a5 – SpeedFeedGpuJudgeEngine + driver wiring for GPU‑based closed‑loop judge  
- f5d14ddb29 – GPU judge hardening (probeGpuResidency bug, limit=0 guard, silent‑exclusion note, fetch timeout)  
- 951f5ac335 – polish: isProbe constant, suppression‑on‑probe test  
- 3a1c20fca2 – full sweep mode added to exhaustive engine; runStreaming generator; tests for full mode  
- af0ac16c5a – scrutiny fixes: cells.length ReferenceError, ISO‑group collapse documented and locked with test  
- 28eca0ed84 – spec doc‑reflection of GPU judge findings  
- 54c0ae06c8 – memory for GPU judge findings  
- b847c0f1ff – scoping spec for axis‑awareness enhancement (tool material, coolant, holder, machine, etc.)  
- e9b68da865 – canonical tool‑material speed factor table added; explicit‑only guard  
- 658c8280fe – wired factor into UltimateSpeedFeedEngine; 10 unit tests  
- 585584e3ae – wired CoolantVcModifier (algo 8.5) into engine; removed duplicate table  
- 7d0affcae6 – de‑inlined rigidity factor to constants.ts  
- f998f8af71 – hoisted tool‑material, coolant and rigidity multipliers into alts path; added 4 propagating tests  
- U-OSC-VENDOR-COMPARE – comparison harness PRISM vs published baselines (Sandvik/Kennametal/CNC‑Cookbook/HSMAdvisor)  
- da41a58fd1 – full 102 060‑combination sweep (calc half)  
- c48829e331 – axis‑liveness probe (measured live/inert status of all goal axes)  
- d03458fff1 – extended probe to include tool_life; confirmed runoutImpact not applied  
- 2070c472a4 – workholding.type → force‑cap (ClampingForceEngine wiring)  
- 7dc7798033 – spindle.hp → power clamp (advisory‑to‑clamp conversion)  
- 8b191d5427 – tool_holder.type runout → tool_life (reuse of existing runoutImpact)  
- 81e37ece16 – numeric target_ra_um finish axis; 12 tests  
- a257f872bb – U‑OSC‑AXIS‑LIVENESS‑COMPLETE (all toolpath, cut‑type, mode axes live; probe bug fixed)  
- 6a3ad56545 – U‑OSC‑COMPARE‑TOOLMATERIAL‑VARIABILITY (576 tool_material sweep)  
- c78faa5a73 – U‑OSC‑COMPARE‑HSS‑BASELINE (web‑verified HSS baseline added, 54 points)  
- 835df42c74 – U‑OSC‑COMPARE‑CERAMIC‑CBN‑BASELINE (ceramic & CBN entries added, 12 points)  
- 4c544db4ae – U‑OSC‑COMPARE‑PER‑VENDOR (explicit G‑Wizard/HSMAdvisor deltas surfaced)  
- 08d7fc6d37 – U‑OSC‑ALL‑AXIS‑SWEEP (OAT×2 regimes + factorial sweep of all 25 named axes; 18/25 live, 7 inert classified)  
- 93de130f59 – U‑OSC‑ALL‑AXIS‑TOOLLIFE (tool‑life liveness added; 2 axes now live via tool‑life only, total 20 live)  
- a2ec922ca2 – U‑OSC‑CONTROLLER‑FEATURES (controller smoothing factor wired into prism_optimized; 19/25 live after sweep)  
- a8f72823cb – U‑OSC‑RUNOUT‑LIFE‑DERATE (single‑source runout TIR tool‑life derate)  
- 73b97ef25f – U‑OSC‑HOLDER‑RUNOUT‑DEDUP (fixed double‑count in orchestrator after runout derate)  
- e4d6c00fa8 – doc‑reflect (added regression note & lesson)  
- 69146aa9c1 – U‑OSC‑RADIAL‑ENGAGEMENT (radial depth honored in prism_optimized; hex_mm force‑collapse fix)  
- 32f1e6266a – U‑OSC‑NEG‑RADIAL‑GUARD (reject NaN/≤0 radial, guard added)

**DECISIONS**

- Back‑end first: fix data spine → wire orphan engines → run closed‑loop driver against live G‑Wizard & HSMAdvisor; honest output required.  
- Consensus filter must exclude `aligned:false` externals unless better data exists; misaligned advisory kept visible if sole external.  
- Adopt dedup‑first policy for physics constants (CoolantVcModifier, HPCVcBoostCalculator) and move all to constants.ts; axis multipliers applied only when user supplies value.  
- Full combinatorial sweep now possible with tool‑material, coolant, rigidity axes live; treat as “calculations” half of goal.  
- Add `full` sample mode to exhaustive engine (~69 k cells); replace 50‑cell cap with streaming generator (`runStreaming`) for all combinations.  
- Implement GPU machinist judge engine (RTX PRO 6000 Blackwell) for closed‑loop training; schedule recurring `/loop` every 10 min.  
- Keep physics changes (tool material, coolant, holder, machine, etc.) pending sign‑off; produce scoping spec instead of full implementation until review.  
- Use explicit guard on canonical tool‑material speed factor table; only apply when explicitly requested.  
- Maintain G‑Wizard crib as geometry‑only library; vendor data must be proven speeds to contribute.  
- Keep controller default feed inert (product decision); no code change pending until physics‑reviewer verdict.  
- Defer safety‑critical fixes (radial_pct, coolant clamp ceiling) to fresh sessions after token pressure & safety review.

**OPERATOR DIRECTIVES**

- Run closed‑loop driver with live G‑Wizard `toolcrib.csv` and HSMAdvisor `settings_v2.xml`; expose honest alignment flags and abstention reasons.  
- Populate G‑Wizard crib with computed S/F per tool via closed Adobe‑AIR UI; run vision‑OCR batch over vendor PDFs to generate richer baseline data.  
- Execute full comparison sweep against live G‑Wizard/HSMAdvisor once data available.  
- Continue from current state: finalize consensus bug, expose honest results, complete report.  
- Swap “hotel” references to “oscar”; all work stays in SFC/oscar lane; coordinate UI design with Quebec (front‑end).  
- Do not take out‑of‑lane units; stay within oscar lane.

**FINDINGS/BUGS**

- EPERM leak fixed; orphan temp files removed.  
- G‑Wizard tool selection defaults to first crib tool → 0 usable sfm rows; vendor cannot contribute.  
- HSMAdvisor returns constant 634.6 m/min for all materials, flagged as unaligned (advisory).  
- Consensus bug: `consensusOf(externals)` incorrectly included not‑aligned systems in median.  
- Material ISO mapping misclassified aluminum under M; missing N/H coverage fixed.  
- G‑Wizard alignment symmetry added; regression test for misaligned advisory.  
- clamped flag false‑positive; persist tmp cleanup needed; skipped_lines observability missing.  
- probeGpuResidency prefix‑match bug → false GPU residency claim; ReferenceError `cells.length` after runStreaming refactor.  
- ISO‑group collapse: 15 selectable names collapse to 6 physics profiles (materials within a group give identical Vc).  
- Tool material, coolant, holder axes inert in earlier sweeps; now live via new multipliers.  
- PRISM over‑speeds HSS cast iron by +108 % (vc) and CBN hardened steel by +49 %; fixed in U‑OSC‑CONTROLLER‑FEATURES.  
- PRISM’s fz uniformly +67–91 % higher than published sources; surfaced via per‑vendor deltas.  
- Several axes inert due to design or optimizer internalization: machine_accuracy, controller_brand, spindle_thru, target_ra, radial_pct.  
- Coolant_effectiveness clamp ceiling of 1.08 would regress existing values (cryogenic = 1.40, through_tool = 1.25); identified as spec error.  
- Radial_pct inertness: prism_optimized used static balanced ae_pct instead of engine‑resolved ae; hex_mm formula collapsed to ~0 at high immersion; negative or NaN radial_depth caused NaN forces.

**DOMAIN SPECIFICS**

- **Engines:** OutcomeCaptureBusEngine, SpeedFeedTriComparatorEngine, SpeedFeedExhaustiveCombinationEngine, SpeedFeedDownstreamSubscriberEngine, SpeedFeedCalibrationPersistEngine, SpeedFeedGpuJudgeEngine, UltimateSpeedFeedEngine, SpeedFeedNineAxisOrchestratorEngine, ClampingForceEngine, SpeedFeedBaselineComparatorEngine.  
- **Actions/Dispatcher:** `calcDispatcher.ts` (speed_feed_calibration_persist, speed_feed_gpu_judge), `sfc_nine_axis_run`, `tri-compare.ts`, `sfc-full-sweep-compare.mjs`.  
- **Metrics/Outputs:** Vc, rpm, feed_rate_mmmin, fz, AP, MRR, tool_life_min, cutting forces (tangential/radial/axial), target_ra_um, radial_depth_pct, gpu_resident, vram_used_mib, ledger_rows_skipped, matched_model.  
- **Paths:** `mcp-server/src/engines/...`, `src/data/<vendor>-speed-feed-data.ts`, `resources/MANUFACTURER_CATALOGS`, `state/outcomes/sfc-full-sweep-ledger.jsonl`, `state/outcomes/sfc-calibration-model.json`, `H:/prism/.claude/helpers/chat-slots.mjs`, `H:/prism/.claude/helpers/slot-bind-enforce.mjs`.  
- **Physics constants:** `src/physics/constants.ts` (tool‑material, coolant, rigidity multipliers).  
- **Vendor comparison harness:** U-OSC-VENDOR-COMPARE.

**TOOLS USED**

- PRISM CLI skills (`/hermes-workflow`, `/forge-audit-v2`, `ultracode`), Node.js helpers (`chat-slots.mjs`, `claim`, `reclaim`), build/test tools (`tsc`, `esbuild`, `vitest`, `Jest`), Git hooks (`slot-bind-enforce`, `bootstrap-slot-enforce`, `slot-commit‑enforce`).  
- GPU: RTX PRO 6000 Blackwell, NVMe SSD, 128 GB RAM.  
- Vision‑OCR batch with Ollama LLMs (qwen2.5‑vl) on GPU for vendor PDF extraction.  
- Scrutiny‑3way script (`.claude/scripts/scrutiny‑3way.mjs`) for physics‑reviewer + 3‑of‑3 gate.  
- CronCreate/CronDelete for recurring `/loop`.  
- Skill tool for slash commands.

**OPEN THREADS**

- Implement force‑coupled fix for `radial_pct` (honor user input, enforce workholding & spindle power clamps) – requires physics‑reviewer and 3‑of‑3 scrutiny.  
- Final UI polish remains Quebec‑led; no oscar action needed.  
- Coolant clamp ceiling adjustment (≥ 1.40) and TSC double‑count guard (FIX‑1).  
- Radial_pct duty‑cycle correction (radial_pct safety‑critical fix pending fresh context).  
- Target_ra sweep‑widening to exercise cap binding in all regimes.  
- By‑design documentation for inert axes (machine_accuracy, controller_brand, spindle_thru, target_ra, radial_pct).  
- Vision‑OCR batch over vendor catalogs to generate `mcp-server/src/data/<vendor>-speed-feed-data.ts`.  
- Calibrate `CUTTING_PARAMS` from extracted tables; apply to engine with safety review.  
- Reconcile MRR coupling (`U-OSC-MRR-RECONCILE`).  
- Implement `U-OSC-RIGIDITY-DOC` – flow rigidity/holder/spindle stiffness into stability‑lobe analysis.  
- Complete full sweep vs G‑Wizard/HSMAdvisor once live data exported.  
- Finalize remaining inert axes (workholding, holder type, controller, etc.) following dedup‑first pattern.
