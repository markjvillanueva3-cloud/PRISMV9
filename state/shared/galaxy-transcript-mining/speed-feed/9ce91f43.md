# speed-feed session 9ce91f43 (2026-05-14, 4.8MB, spine 46KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `4426e02b1` – restored 274 KEEP files (import‑referenced & entry points).  
- Peer‑absorbed batch 2 – 422 TEST + AMBIGUOUS files now tracked.  
- `6b8997ca2` – restored 91 ORPHAN files after per‑file review.  
- `47a706fc0` – audit script (`audit-untracked-refs.mjs`) + Phase 0 cross‑link docs.  
- `dcb8f21df` – system‑viz untracked‑files layer (live indexer).  

**DECISIONS**  
- Restore *all* 787 previously untracked files; do not cherry‑pick to avoid losing Codex work.  
- Treat the five distinct front‑end surfaces (`/calculator`, `/speed-feed-calc`, `/ppg`, `/ppg-lite`, `/post‑processor`) as separate products, not duplicates.  
- Wire cross‑links between these surfaces via `SurfaceCrossLink.tsx`.  
- Use system‑viz untracked‑files layer to keep the live index updated automatically.  
- Defer implementation of a new dispatcher action for `LatheCostPanel`; focus on existing panels first (Phase 1A).  

**OPERATOR DIRECTIVES**  
- Resume Phase 1A: wire the three local‑mock panels (`WireEdmFeasibilityPanel`, `WireEdmCostBreakdownPanel`, `LatheCostPanel`) to real dispatcher actions.  
- Verify backend routes and dispatcher actions before wiring.  

**FINDINGS/BUGS**  
- 787 source files existed on disk but were untracked due to a mis‑archive (`3010a9613`) and a history strip (2026‑05‑12).  
- Duplicate `/post‑processor` route detected; removed the duplicate.  
- Missing backend route/action for `LatheCostPanel`.  
- 298 tsc errors & 70 vitest failures are pre‑existing tech debt, not introduced by restoration.  
- Index contention and peer‑commit collisions during staging/commits (resolved with pathspecs).  

**DOMAIN SPECIFICS**  
- Panels: `WireEdmFeasibilityPanel`, `WireEdmCostBreakdownPanel`, `LatheCostPanel`.  
- Dispatcher actions: `wedm_assess_feasibility`, `weCostEstimate()`, *new* `turning_cost_estimate` (to be added).  
- API routes: `/api/v1/edm/feasibility`, `/api/v1/edm/cost`.  

**TOOLS USED**  
- PRISM commands: `/checkin`, `/system-viz`, `/adaptive-optimize` (attempted but blocked).  
- Scripts: `audit-untracked-refs.mjs`, `merge-augmentations.mjs`.  
- Build tools: `tsc`, `vitest`.  
- Git hooks: `commit-ownership-guard`, `worktree-commit-route`.  

**OPEN THREADS**  
1. Implement and expose `/api/v1/turning/cost` dispatcher action (`turning_cost_estimate`).  
2. Wire `LatheCostPanel` to the new action once available.  
3. Resolve remaining vitest failures & tsc errors (Phase 1D).  
4. Finalize system‑viz untracked‑files layer updates after any new commits.  
5. Optionally schedule a recurring loop for ongoing SFC checks if desired.
