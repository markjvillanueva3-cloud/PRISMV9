# blueprint-vision session 72260bce (2026-06-19, 13.3MB, spine 104KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `8cfd4da130`: OCR corpus‑train loop switched to continuous “do it all until complete” (unlimited ExecutionTimeLimit, 30 min backstop).  
- `9c7943f019`: Blueprint + GD&T tribal‑knowledge injection plan & drop‑zone.  
- `27659a5c8f`: Fast‑exit guard (`isCorpusDrained`) and R16 gap closure in runner.  
- `73582a78c0`: Added `--retry‑failed`, fixed probe dim‑key bug, confirmed transient failures; tests updated (23/23).  
- `ed8dcf451b`: Built reusable vision‑probe tool; qwen3‑vl:32b benchmark unusable → closed ladder work‑order.  
- `bfcd8256fe`: Fixed parseVisionResponse mis‑count of dims.  
- `1f16ca589c`: Updated backlog & handoff after confirming scan‑preprocessing ineffective.  
- `d820c15936`: Fixed multi‑page OCR bug in validate‑perfect‑parts; unioned all pages’ dims, recall lift 0 → 0.21.  
- Added `programGtReliability` classifier to `extractProgramGT`.  
- Updated `dimension-set-score.mjs` and metrics to use GT reliability flag.  
- Wrote 6 new unit tests for the classifier; all 18 tests pass after ASCII‑guard fixes.  
- Wired reliability flag into `validate-perfect-parts.mjs`; added skip logic for contour‑unreliable parts and transparency fields in scored report.

**DECISIONS**  
- Switched OCR training loop to continuous mode to avoid nightly scheduling & GPU contention.  
- Added `--retry‑failed` flag for reprocessing failed prints with the same ensemble.  
- Decided not to add qwen3‑vl:32b as a third VLM (unusable, slow); closed ladder work‑order.  
- Determined recall failure due to page‑0‑only OCR on multi‑page bundles; fixed by rendering all pages and unioning dims in validate‑perfect‑parts.  
- Postponed calibration of singletons & GT normalization until fresh budget; next focus live print‑reading path.  
- GT Reliability Model: classify each part’s G‑code as stepped (GT‑reliable) or contour (GT‑unreliable); aggregate recall/precision only over stepped parts to avoid metric artifacts.  
- Keep multi‑page OCR pipeline; confirm live consumer (`run-ollama-vision-extract.mjs`) is already page‑correct, no change needed.  
- Extend skip logic for contour‑unreliable parts before GPU OCR runs.

**OPERATOR DIRECTIVES**  
- `/goal [ /loop …]` – complete xray tasks, then improve blueprint/OCR interpretation via closed‑loop training.  
- “build it all” – execute recall harness comprehensively.  
- “keep pushing” – accept risk and build #1 corroboration fix surgically.  
- “do everything that will improve blueprint reading”.  
- “Complete all xray tasks, then improve blueprint-reading & OCR capabilities.”  
- “Do deep research on what else can improve blueprint reading.”  
- “Override 1: change from nightly to do it all until its complete” (OCR corpus‑training).  
- “Override 2: plan for tribal knowledge injection … run pdf‑learn and video‑learn for blueprint reading and GD&T.”

**FINDINGS/BUGS**  
- OCR loop capped by 12 h ExecutionTimeLimit; fixed to unlimited.  
- Ensemble corroboration filter (2‑of‑2 quorum) initially blamed recall collapse, later found page‑0‑only OCR on multi‑page bundles.  
- qwen3‑vl:32b produces empty output and is ~6× slower → unusable.  
- Scan preprocessing ineffective for failed prints.  
- 15.2 % ensemble‑failed were transient contention failures, recoverable via `--retry‑failed`.  
- Low recall (~0.32) partly due to GT over‑counting for contoured parts (taper/arc).  
- Program‑derived GT unreliable for radiused/contoured parts; must not be used as a metric denominator there.  
- Test failure caused by incorrect assumption about contour fraction threshold; fixed by adjusting test logic.  
- R15 wiring gap: consumer did not yet use new reliability flag—now wired.

**DOMAIN SPECIFICS**  
- OCR training loop: `blueprint‑ocr‑training‑loop.mjs`, weak‑label loop, cursor JSONL (`processed‑cursor.jsonl`).  
- Vision model selection: `vision-model-select.mjs`, `isThinkingTrap` logic.  
- PDF preprocessing: `pdf-to-png.py` with `--preprocess` & `--deskew`.  
- Probe tool: `probe-vision-model.mjs`.  
- Ground‑truth derivation: `find-perfect-parts.mjs`, `derive-ground-truth-from-cad.mjs`, `cnc-ground-truth-build.mjs`.  
- Validation harness: `validate-perfect-parts.mjs`, `ocr-closed-loop.mjs`, `CrossSourceDimensionReconciliationEngine.ts`.  
- Metrics: recall, precision, F1, dims_extracted, gt_matched, contourFraction, gtReliable.  
- Engines/Actions: `run-ollama-vision-extract.mjs`, `blueprint-ocr-training-loop.mjs`, `validate-perfect-parts.mjs`, `print_to_program_full`, `blueprint_to_quote`.  
- Paths/Preprocessing: `pdf-to-png.py` (Otsu binarize + despeckle + deskew), `page-classify.mjs`.

**TOOLS USED**  
- `/checkin-xray` wrapper (slot‑binding).  
- `chat-slots.mjs` (claim/reclaim).  
- `vision-model-select.mjs`.  
- `pdf-corpus-watcher-sweep.mjs`.  
- `probe-vision-model.mjs`.  
- `ocr-al-queue-surface.mjs`.  
- `validate-perfect-parts.mjs`.  
- `CrossSourceDimensionReconciliationEngine.ts`.  
- `scoreDimensionSet.mjs`.  
- Scripts: `blueprint-ocr-training-loop.mjs`, `run-ollama-vision-extract.mjs`, `dimension-set-score.mjs`.  
- Libraries: `lib/ocr-training-loop-lib.mjs` (RETRYABLE_FAILURE_STATUSES, parseCursorDoneSet).  
- VLM client via Ollama.

**OPEN THREADS**  
- Trace live print‑reading path (`cad_pdf_blueprint_extract`, `blueprint_to_quote`, etc.) for page‑0‑only bug.  
- Calibration of singletons & GT normalization (task #9).  
- Full exact‑dimension recall harness from STEP geometry (A2).  
- Potential addition of a third usable VLM if future models become available.  
- Full “do it all until complete” OCR corpus‑training run still pending; monitor GPU usage and iterate on synthetic calibration.  
- Tribal knowledge injection: gather additional PDF/GD&T sources, run `pdf-learn`/`video-learn`.  
- Further refine contour GT handling (e.g., detect stepped vs continuous arcs more robustly).  
- Expand metrics to include per‑part reliability scores in the overall report.  
- Validate that skip logic for contour parts does not inadvertently exclude valid callouts in future part families.
