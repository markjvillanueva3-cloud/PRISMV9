# blueprint-vision session 001bd6c3 (2026-06-10, 85.1MB, spine 782KB, 9 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Docker Desktop v29.4.3 → prism‑qdrant container up (`:6333`, 3 collections).  
- Hermes config migrated to `model.default=anthropic/claude-opus‑4‑8`, `fallback_model=custom/qwen2.5-coder:32b`; GUI front‑end finished, gateway at `:9120`.  
- Qdrant daemon cleared stale connection by restarting MCP server (`:3100`), now `isConnected=true`.  
- Commit 28b72e4dee added `scripts/fill-galaxy-memory-sections.mjs`, updated 34 galaxy `MEMORY.md` with canonical sections.  
- Updated `syncGalaxyMemories()` in `obsidian-memory-sync.mjs` to mirror enriched `MEMORY.md` into Obsidian vault.  
- U5b (`ollama-tool-agent.mjs`) 3499f5f20f; U5c (`ollama-offload-enforce.mjs`) d807d46f44; U6 (`ollama-codegen.mjs`) a59f35221e; U7 (`ollama-build-loop.mjs`) cfda0e391a; U9 (`ollama-coresidency.mjs`) 7771ca7f86; U10 (`ollama-capability-probe.mjs`) e17be56e82.  
- U1, U2, U3, U4, U‑OAB‑120B committed; Go‑live merge `slot/bravo → cad-fusion-live-ms0` 5a91ba1862; RTK config tightened; `settings.json` updated `PRISM_CODEGEN_MODEL=gpt-oss:120b`.  
- U‑OCTOPUS‑LIVE‑PRODUCER, U‑REWRITER‑LOCALHOST‑FIX, U‑OPTCTX‑LOCALHOST‑FIX, U‑OLLAMA‑FANOUT, U‑CONSENSUS‑DRAIN‑LOCAL, U‑CONSENSUS‑DRAIN‑PANEL‑FIX, U‑LOCALHOST‑HARDCODE‑GUARD, U‑GUARD‑CURL‑PRECISION, U‑RAG‑DENSE‑EMBED‑127.  
- f8b7fa6d44 added sidecar‑first read in `readChatPressure`; 098a9f7651 corrected TTL comment to 180 s.

**DECISIONS**  
- Use PRISM MCP :3100 as vault‑read surface; Hermes primary Claude Code, fallback Ollama qwen2.5-coder:32b.  
- Revert supervisor `cwd` patch; CapabilityIndexEngine uses `PRISM_ROOT`.  
- Fleet coordination via `slot_brief_write` on :3100.  
- Provider mapping: `custom` for local Ollama (OpenAI‑compatible).  
- Primary zulu model → gpt‑oss:20b (≥64 K context); fallback Claude Code.  
- Headless Hermes (`hermes -z`) eliminates GUI hang; schedule via PowerShell.  
- Adopt Claude Code CLI as sole Zulu orchestrator; Hermes desktop app no longer required.  
- Set GPT‑OSS‑120B as fleet‑wide coding default in `settings.json`.  
- `OllamaTaskOffloaderEngine.ts` handles task→model routing; use co‑residency mutex (U9) and capability probe (U10).  
- Apply `RECOMMENDED_ENV` to Ollama server after coordination (restart required).  
- OLLAMA_URL env var + write‑time guard fixes localhost bug.  
- Bounded Ollama fan‑out limits Anthropic rate‑limits.  
- Consensus drain local‑only by default; Claude optional via env flag.  
- Sidecar‑first strategy for CHO02; TTL 180 s.

**OPERATOR DIRECTIVES**  
- Enable extra‑usage billing at claude.ai/settings/usage or switch Hermes primary to local model if unavailable.  
- Start Qdrant Docker container (`docker run …`).  
- Re‑send test message in Hermes, confirm processing.  
- Verify Hermes headless: `hermes -z "check zulu status"`.  
- Confirm MCP server alive: `curl http://127.0.0.1:3100/mcp/status`.  
- Ensure Qdrant collections populated; run ingestion script if needed.  
- Verify headless zulu with gpt‑oss:20b produces final answer (max tokens sufficient).  
- Restart Hermes daemon after any Qdrant or config changes to clear stale connection state.  
- Monitor memory pressure; consider compacting idle chat sessions.  
- Confirm fleet integration via MCP (`slot_brief_list`, `prism_context`) end‑to‑end.  
- Coordinate Ollama server restart to apply RECOMMENDED_ENV (affects ~17 live peers).  
- Resolve octopus live dispatch bug (`ollama:unreachable`).  
- Finalize octopus consensus engine if not fully operational.  
- Monitor RTK config changes for safety compliance.

**FINDINGS / BUGS**  
- Qdrant service down → vector search fails; Docker Desktop must be running.  
- Hermes messages HTTP 400 from Anthropic due to missing extra‑usage billing.  
- N:\ drive bug resolved by daemon restart; no stale paths.  
- Hermes fallback to Ollama works; 429 rate‑limit observed but not blocking.  
- git pull aborted due to dirty `package-lock.json`; launcher stuck.  
- “Unknown provider 'openai'” fixed by changing to `custom`.  
- Daemon cached stale Qdrant connection; requires restart.  
- GUI front‑end must finish before gateway :9120 reachable.  
- Octopus live dispatch bug: ollama:unreachable due to URL handling in OllamaClientEngine.  
- CRLF flip on U1 fixed via amend.  
- Field mismatch `pressure.level` vs `pressureLevel` caused hook no‑op; sidecar-first fix removed false critical signal.  
- Over‑reporting byte estimate flagged every prompt as critical; sidecar-first strategy corrects it.  
- TTL comment drift corrected (60 s → 180 s).  
- gpt‑oss returned separate `reasoning` field; Hermes custom provider mis‑parsed, causing “no final response”.  
- Memory count inflated by auto‑generated `node_*`; real curated count 66.  
- Corrupted `ai-training_synthesis.md` (all‑NUL).

**DOMAIN SPECIFICS**  
- **Hermes**: `config.yaml` (model, provider, base_url); endpoints `/api/status`, `/api/chat`.  
- **PRISM MCP**: `slot_brief_write`, `self_awareness_search`, `master_index_query`; server `http://127.0.0.1:3100/mcp`.  
- **Obsidian REST API**: port 27123 (`/active/`, `/vault`).  
- **Qdrant**: REST at `:6333`; collections `prism_engines`, `prism_formulas`, `prism_skills`.  
- **Galaxy artifacts**: `MEMORY.md`, `PATHS.md`, `TOOLBELT.md`, `CLAUDE.md`; engines under `mcp-server/src/engines/<galaxy>/`.  
- **Zulu orchestrator**: `zulu-orchestrator-sweep.mjs` drives fleet via scheduled task.  
- **Obsidian mirror**: `knowledge/memories/galaxies/<galaxy>/MEMORY.md` synced by `syncGalaxyMemories()`.  
- **Local stack**: Ollama (gpt‑oss 120b 65 GB, qwen2.5-coder 32b 37 GB, gpt‑oss 20b 13 GB), Qdrant, MCP :3100, Obsidian REST API.  
- **Engines**: `OllamaClientEngine`, `MultiModelConsensusEngine`, `PromptRewriterEngine`, `OptimalContextInjectEngine`, `OctopusWithHermesRag`, `OctopusLiveProducer`, `RAGDenseEmbedEngine`.  
- **Metrics**: offload rate ~6–7 %; VRAM 96 GB; take‑rate banner honesty fix.

**TOOLS USED**  
- Docker Desktop / `docker run` / docker‑compose for Qdrant.  
- Hermes CLI (`hermes config migrate`, `hermes -z`, `hermes chat`) and GUI front‑end.  
- Node scripts: `ollama-tool-agent.mjs`, `ollama-offload-enforce.mjs`, `ollama-codegen.mjs`, `ollama-build-loop.mjs`, `ollama-coresidency.mjs`, `ollama-capability-probe.mjs`, `fill-galaxy-memory-sections.mjs`, `syncGalaxyMemories()`, `galaxy-synthesis-refresh.mjs`.  
- ultracode workflows (`wf_bb2026cd-2f4`, `wq09sxi8s`, `wzows014c`).  
- Git operations: cherry‑pick, merge, amend; slot/bravo worktree.  
- Testing framework: `node --test`, Vitest.  
- Cron loop `/loop 10m`.  
- Scripts/lib/`ollama-fanout.mjs`, `octopus-first-live-record.mjs`, `precompact-auto-trigger.mjs`, `dream-session-walk.mjs`, `stop-dream-queue-surface.mjs`, `zulu-advisory-inject.mjs`.

**OPEN THREADS**  
- Bring Qdrant up and verify semantic search.  
- Enable extra‑usage billing or switch Hermes primary to local model.  
- Confirm Hermes message flow after changes.  
- Verify headless zulu with gpt‑oss:20b final answer.  
- Restart Hermes daemon after any Qdrant or config changes.  
- Monitor memory pressure; compact idle chat sessions.  
- Confirm fleet integration via MCP end‑to‑end.  
- Coordinate Ollama server restart to apply RECOMMENDED_ENV.  
- Resolve octopus live dispatch bug (`ollama:unreachable`).  
- Finalize octopus consensus engine if not fully operational.  
- Monitor RTK config changes for safety compliance.  
- Remaining dormant features: `zulu-advisory-inject.mjs`, dream‑cycle upstream feed, orchestrator‑advisory producer, consensus drain scaling, CHO02 cache correctness.
