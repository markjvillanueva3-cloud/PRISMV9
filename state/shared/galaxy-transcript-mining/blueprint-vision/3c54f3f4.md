# blueprint-vision session 3c54f3f4 (2026-06-22, 23.1MB, spine 145KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**

- `d197fa6cd5` – Mill‑program GT extraction (hole/bore diameters), live‑validated (`9102741` recall 0.5).  
- `7e8cb4ef57` – Doctrine sync: updated `format:"json"` status, added mill‑GT note.  
- `4c0828c118` – Thread‑callout normalizer (.mjs) with P1/P2 fixes, 88/88 tests.  
- `3a2316206c` – Added `--axis` filter to validation runner for targeted recall measurement.  
- `20661dda93` – TS clone of thread normalizer wired into `BlueprintVisionOCREngine`.  
- `0440ed4f04` – Chamfer‑callout normalizer (.mjs) with tests and P1 fix.  
- `f0a08b7c02` – Pure‑core tiling extractor (clique‑partition merge, ASCII guard).  
- `d94fc110ae` – Integrated tiling into closed‑loop validator (`--tile`), live E2E validation.  
- `865c312428` – GD&T symbol normalizer (.mjs) with datum‑deficiency fix.  
- `377e99e57e` – TS clone of GD&T normalizer for `BlueprintVisionOCREngine`.  
- `c1a0498791` – Added FCF validation to second OCR engine’s `extractGDT`.  
- `463b1d8fa1` – *U‑XRAY‑PERPRINT‑UNITS*: per‑print unit propagation with corroboration gate; 121/121 tests, live‑validated on a 3‑page lathe scan.  
- `c6b6e7b14a` – *BACKLOG‑TRUTH*: P1.6 done, P2.9 dormant, GPU usable, worklist mostly single‑page.  
- `5d67236d37` – *P1.5 REGION‑ROUTING DESIGN*: pure lib + glue, grounded on page‑classifier pattern; ready for full implementation.

**DECISIONS**

- Prioritized mill‑GT over program‑selection/CAD‑model GT after R15 premise‑validation refuted them.  
- Adopt dual‑home normalizer pattern (.mjs script + TS clone) to guarantee identical behavior across OCR paths.  
- Use greedy clique‑partition instead of union‑find for tile overlap merging to avoid over‑merging distinct corner features.  
- Centralize unit authority in `forceUnits` channel; per‑print propagation is a lightweight wrapper feeding into this channel, preserving precedence rules.  
- Corroboration gate requires title‑block identity (part number or title) before counting a unit vote, preventing hallucinated bare units from anchoring an entire print.  
- Keep fusion logic recall‑first; no drop rule needed—singletons retained and routed to the AL queue.  
- Adopt ASCII‑guard compliant regexes; dropped rare minus‑sign P2s in favor of `\p{Pd}`.  
- Deferred GPU‑bound corpus sweep until core units shipped due to reaper contention.

**OPERATOR DIRECTIVES**

- Continue closed‑loop training; focus on high‑ROI non‑GPU tasks (normalizers, tiling core).  
- Validate each unit with live data before commit.  
- Use atomic `git add && git commit -F -` pattern with `[MAIN-FORCE]`.  
- Capture reference memories for every shipped unit.  
- Execute all ranked backlog items (“do it all”).  
- Keep progressing blueprint reading & training improvements after loop completion.

**FINDINGS/BUGS**

- Thread regex collided with mixed‑fraction drills → fixed with negative lookahead.  
- Unbounded bore derivation poisoned recall denominator → bounded by `BORE_MAX_IN`.  
- Tool‑steel grades (M2 STEEL) incorrectly fabricated into metric threads → added material guard.  
- Spot‑drill misidentified as hole feature → tightened exclusion regex.  
- Union‑find over‑merge due to non‑transitive tile overlap → replaced with clique partition.  
- Shape mismatch between fused ensemble dims and `extractDimension` → shape‑tolerant accessors added.  
- Baseline OCR path silently failed when all models returned empty → fixed accounting logic.  
- GD&T symbol normalizer missing for non‑canonical emissions → implemented `normalizeGdtSymbol`.  
- Tiling lift is part‑dependent: improves dense mill prints but not sparse lathe parts; now measured per part.  
- GD&T datum‑deficiency bug fixed: a TP callout without a datum was silently ignored.  
- Multi‑page training data suffered from wrong unit labels (96 % of corpus); resolved via per‑print propagation.  
- GPU contention previously blocked vision models; probe confirmed current fleet can run vision concurrently with code model.  
- Worklist largely single‑page, so per‑print fix has modest impact on that set but remains critical for genuine multi‑page scans.

**DOMAIN SPECIFICS**

- **Engines/Actions**: `BlueprintVisionOCREngine.ts` (`convertDimensions`, `convertGDT`), `BlueprintOCREngine.extractGDT`, `validate-perfect-parts.mjs`, `blueprint-ocr-training-loop.mjs`.  
- **Dispatchers**: `vision-tiling-extract.mjs` orchestrates tiling; `region-classify.mjs` (future).  
- **Metrics/Paths**: recall‑vs‑GT measured per part; tile‑vs‑baseline mean recall; unit‑propagation success rate; 9102741 recall 0.5 for mill‑program GT.  
- **Unique Files**: `scripts/lib/vision-tiling-lib.mjs`, `scripts/lib/crop-image-tiles.py`, `scripts/lib/ollama-vision-extract-lib.mjs`, `vision-ensemble-fuse.mjs`.

**TOOLS USED**

- PRISM helpers: `chat-slots.mjs`, `/checkin.md` pipeline, `/checkin-xray`, `/loop`, `/compact`.  
- Slot‑binding helpers: `slot-bind-enforce.mjs`, `ascii-guard` hook.  
- Scripts: `capture-claude-credentials.mjs`, `arm-account-switch.mjs`, `vision-tiling-lib.mjs`, `ollama-vision-extract-lib.mjs`, `vision-ensemble-fuse.mjs`.  
- Internal libs: `vision-tiling-lib.mjs`, `ollama-vision-extract-lib.mjs`, `vision-ensemble-fuse.mjs`.  
- Node scripts for slot claim and loop; Vitest + tsc (heap = 12 GB) for unit tests.  
- Git hooks: `PreToolUse`, `slot‑bind‑enforce`, `ascii-guard`.  
- Python PIL for tile cropping.

**OPEN THREADS**

- Full GPU‑bound recall lift measurement across entire corpus (mill + lathe) pending after tiling integration.  
- Refinement of region tiling heuristics for dense pages.  
- Potential addition of countersink normalizer later.  
- Monitoring model stability (`qwen3-vl:8b` vs `32b`) for consistent recall.  
- Full implementation of P1.5 REGION‑ROUTING (region classifier, view/VLM, table/parser, title‑block parser).  
- Tile‑vs‑Baseline corpus comparison to quantify tiling lift across the GT set.  
- Consumer‑wiring for dormant enrichment (thread/chamfer/GD&T output into downstream pipelines).  
- Per‑feature‑type confidence calibration (currently sample‑size limited).
