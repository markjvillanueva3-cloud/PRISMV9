# blueprint-vision session 58cb6b0a (2026-06-23, 17.6MB, spine 120KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-XRAY-CALIB-ACCUMULATE` – persistent calibration‑sample store (`calibration-sample-store.mjs`) with 17/17 tests, GPU‑free live validation (commit 5ab3c49002).  
- `U-XRAY-PROGRAM-GT-CALIB` – real program‑GT emission into same store; opt‑in flag, 11/11 tests (commit 815567da84).  
- `U-XRAY-APP-INTEGRATION-PLAN` – operator‑requested blueprint/OCR integration plan (5 phases) (commit cd108380ac).  
- `U-XRAY-CALIB-STATS` – CLI (`calibration-store-stats.mjs`) exposing corpus size, source mix, reliability (19/19 tests) (commit 198d811ef4).  
- `U-APP-REDACT-LIB` – shared TS redaction module with full 118‑customer coverage and distinctive tier logic; 25 lib tests (commit 47a37b714d).  
- `U-APP-REDACT-WIRE` – wired redactor into `prism_cad:blueprint_redact`; fixed key‑leak, 62/62 tests (commit 62c20067d1).  
- `U-XRAY-READING-KNOWLEDGE` – curated knowledge bundle (`blueprint-reading-knowledge.mjs`) with tribal + ASME Y14.5 injection; 14 lib + 45 ensemble + 121 extract‑lib tests, 2‑arm scrutiny PASS (commit b8ef51c9fe).  
- `U-XRAY-TRUNCATION-KEYCUT` – tier‑2 JSON salvage for mid‑key truncation; restores ~30 dims on dense prints, 127/127 extract‑lib & 45 ensemble tests, 2‑arm scrutiny PASS (commit fa6a037974).  
- `U-XRAY-NUM-PREDICT-TUNABLE` – env‑tunable `PRISM_OCR_NUM_PREDICT` with auto‑coupled `num_ctx`; default remains 4096 (no recall lift at higher caps) (commit e1cfab0e20).  
- `U-XRAY-READING-GUIDANCE-VALIDATE-WIRE` – `--reading-guidance` flag wired into both plain and region‑route paths of `validate-perfect-parts` (commit e1cfab0e20).  

**DECISIONS**  
- Persist calibration samples across runs to hit MIN_RELIABLE=50 without GPU.  
- Emit program‑GT only when `--emit-calibration`; keep byte‑identical behavior off by default.  
- Build single provenance‑tagged knowledge channel; inject via opt‑in flag into VLM prompt (`buildVisionPrompt`).  
- Add tier‑2 truncation salvage that trims to last complete property; fail loudly only if nothing remains.  
- Keep reading guidance disabled on full‑page runs (truncation risk); enable by default on region‑route crop path once validated.  
- Use shared TS redaction library for LoRA engine and app surface, expanding from 9 to 118 customers with distinctive tier logic.  
- Production default `num_predict = 4096`; higher caps do not lift recall and risk hallucination.  
- Default‑off for `--reading-guidance`; no precision/recall benefit observed, slight precision drop.  
- Preprocessing/deskew (`--enhance`) is behavior‑changing but does NOT recover missing GT dims; 05850 is GT‑ceiling bound.  
- Future default flips (e.g., raising `num_predict`, enabling guidance) must be gated behind validated recall gains on a fixture with legible missing dimensions.

**OPERATOR DIRECTIVES**  
- Continue improving blueprint reading capabilities; apply further enhancements.  
- Prioritize backend tasks (xray), OCR/blueprint improvements, closed‑loop training, and integration into PRISM app features.  
- Explore `num_predict` bump and region‑route default‑on guidance as next steps.

**FINDINGS / BUGS**  
- Mid-key truncation in VLM output caused dense prints to return 0 dims; fixed by tier‑2 salvage (live 0 → 28 dims).  
- Key‑leak in redaction library (customer names exposed as object keys); resolved with full‑registry mask and distinctive fallback.  
- GD&T knowledge not injected into live prompt; addressed via knowledge channel.  
- Default‑on reading guidance risked truncation on full‑page prints; gated to crop path.  
- Recall on part 05850 pinned at 3/7 across all levers (num_predict, `--reading-guidance`, `--enhance`); missing GT callouts are absent from scanned drawing (GT‑ceiling).  
- Extra dims at high num_predict (56–86) are not real; no recall lift, precision unchanged or slightly worse.  
- No hallucination inflation when raising num_predict; some runs actually decreased dims.

**DOMAIN SPECIFICS**  
- Slot: `xray` (blueprint‑vision).  
- Calibration store: `scripts/lib/calibration-sample-store.mjs`.  
- Program‑GT extraction: `scripts/lib/cnc-program-gt-lib.mjs`.  
- Prompt builder & ensemble: `buildVisionPrompt`, `vision-ensemble-fuse.mjs`, `region-classify.mjs`, `validate-perfect-parts.mjs`.  
- OCR engines: Qwen2.5VL/7B (probe), Qwen3‑VL 8B‑Instruct (production).  
- Context coupling: `num_ctx` auto‑scaled with `num_predict` to avoid overflow.  
- Env knobs: `PRISM_OCR_NUM_PREDICT`; flags `--reading-guidance`, `--enhance`.  
- PDF rasterization chokepoint: `rasterAllPages` in `pdf-to-png.py`.  
- Redaction module: `src/engines/blueprint-vision/.../blueprintRedaction.ts`.  
- Knowledge channel: `scripts/lib/blueprint-reading-knowledge.mjs`.  
- Truncation salvage: `repairTruncatedJson`, `salvageTruncatedJson`.

**TOOLS USED**  
- PRISM harnesses (`chat-slots.mjs`, `slot-bind-enforce.mjs`, `/checkin`, `/loop`, `/goal`).  
- Node.js test runner (`node:test`), syntax checker (`node --check`).  
- TypeScript compiler (`tsc`).  
- Git lane guard `[MAIN-FORCE]`.  
- GT validation harness (`validate-perfect-parts.mjs`) with JSON reporting.  
- PDF‑to‑PNG conversion script (`pdf-to-png.py`).  
- Per‑file 2‑arm scrutiny, Ultracode, Hermes agents, parallel orchestration.

**OPEN THREADS**  
1. Identify and run GT validation on fixtures where missing dims are legibly present; test if `num_predict` or `--reading-guidance` can lift recall there.  
2. After confirming real extra dims, evaluate raising production default for `num_predict`.  
3. Re‑assess `--reading-guidance` once a fixture shows potential recall improvement.  
4. Explore additional preprocessing options (deskew, contrast) on other parts to close remaining OCR gaps.  
5. Per-type calibration – volume‑gated; needs >50 samples per type.  
6. Multi-part region comparison – requires corpus with ≥2 scoreable callout parts.  
7. GPU seeding run (`--emit-calibration`) – quiet‑fleet window needed.  
8. Y14.5 corpus drop zone – operator‑gated; pending integration into knowledge channel.  
9. Region-route crop path – enable reading guidance by default once truncation risk mitigated.  
10. `num_predict` bump (to 8192) – potential next step to reduce truncation frequency.  
11. RAG engine activation – wake dormant `BlueprintExtractionRAGEngine` for live knowledge retrieval.  
12. Academy course integration (`academy-course-0c`) – feed into knowledge channel.
