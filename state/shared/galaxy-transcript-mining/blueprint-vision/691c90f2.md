# blueprint-vision session 691c90f2 (2026-06-20, 4.6MB, spine 31KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `13557d84` [U‑PRINT‑OCR‑PDF] – added PDF → multi‑page rasterization, per‑page OCR via Ollama, union of dimensions; back‑compat single‑image path unchanged.  
- `f2aa3e95` [U‑PRINT‑OCR‑PDF‑HARDEN] – sanitizer for `maxPages`, `dpi`, and injected `analyzer/rasterizer`; prevents DoS and page‑0 re‑introduction.  
- Nightly OCR grinder remains **RUNNING** (training loop continues).

**DECISIONS**  
- Extend `CADLiveBlueprintOcrAdapter` to accept PDFs, rasterize all pages (cap 12), OCR each via existing VLM pipeline, union results; no dispatcher signature change → zero‑edit deployment.  
- Add additive optional fields (`sourcePage`, `pagesOcrd`, `pagesTotal`) for richer metadata.  
- Implement option sanitizer to clamp dangerous values and strip injected components.  
- Keep single‑image path intact for backward compatibility.

**OPERATOR DIRECTIVES**  
- “Continue upgrading and training the OCR/blueprint reading feature.”  
- After shipping, user said “go” → proceed with next backlog item: **grinder scan‑preprocessing (P0)**.

**FINDINGS/BUGS**  
- Live MCP OCR path (`cad_live_blueprint_ocr` → `CADLiveBlueprintOcrAdapter.ocrPrint`) failed loudly on PDFs and only processed page 0, causing operator “missed dims.”  
- Grinder had page‑classify gate but lacked PDF support until now.  
- Hard‑coded “PDF unsupported” error in adapter; removed after audit.  
- Concurrent commit race on shared tree caused initial clobbering; resolved with atomic `git commit -- <paths>`.

**DOMAIN SPECIFICS**  
- **Engines/Actions:** `cad_live_blueprint_ocr`, `CADLiveBlueprintOcrAdapter`, `resourceExtractionDispatcher:ocr_process`, `businessDispatcher:blueprint_to_quote`.  
- **Tools:** `pdf-to-png.py` (rasterizer), Ollama vision engine, VLM ensemble (`qwen2.5vl`, etc.), page‑classify gate, OCR grinder pipeline.  
- **Metrics/Paths:** multi‑page OCR union, `pagesOcrd/pagesTotal`, sanitizer thresholds.

**TOOLS USED**  
- PRISM helpers: `/checkin-xray` wrapper, `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
- Dispatchers: `cadDispatcher`, `businessDispatcher`, `resourceExtractionDispatcher`.  
- Scripts: `pdf-to-png.py`.  
- Testing: Vitest, TypeScript (`tsc --noEmit`).  
- Build: esbuild.  
- Git: atomic pathspec commits with `[MAIN-FORCE]` prefix.

**OPEN THREADS**  
- **Grinder scan‑preprocessing (P0):** wire `--preprocess` flag into grinder’s PDF rasterization step to reduce 15.2 % scanned‑fail leak.  
- Subsequent backlog items: recall‑first ensemble fusion, live adapter page‑classify gate.
