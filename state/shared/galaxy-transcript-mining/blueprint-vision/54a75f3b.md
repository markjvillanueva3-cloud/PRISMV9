# blueprint-vision session 54a75f3b (2026-06-04, 14.4MB, spine 77KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `7a1aea6723` – multi‑VLM ensemble OCR (`vision-ensemble-fuse.mjs`, CLI, leading‑dot JSON parse fix).  
- `c8bb8a3a25` – closed‑loop OCR training engine (agreement‑fraction calibration, isotonic regression, tiering). Emits `trainset.jsonl`.  
- `4fec77e8c1` – xray→India LoRA staging seam (`trainset → BlueprintLoRABridgeEngine → staged local‑lora bundle`).  
- `b121b19f7b` – real Qwen2.5‑VL PEFT trainer (`blueprint_vl_train_lora.py`, tests).  
- `df56f6b45f` – takeover‑plan doc (next steps for India LoRA fine‑tune).

**DECISIONS**  
- Use RTX 6000 Blackwell to run ≥3 diverse VLMs concurrently; fuse via N‑way clustering + noisy‑OR.  
- Treat ensemble as teacher; corroborated dims → pseudo‑labels; calibrate P(correct|agreement fraction).  
- Only train when `n_models≥2`; single‑model runs → `no_corroboration` tier, never trained on.  
- Replace leading‑dot parse bug with string‑aware scanner (`repairLeadingDotDecimals`).  
- Skip delta until print→CAD→gcode pipeline ready; focus on OCR & LoRA training now.  
- Build real VL PEFT trainer instead of stub; defer full India LoRA fine‑tune until GPU/PEFT stack available.

**OPERATOR DIRECTIVES**  
- Take over India’s blocked blueprint‑vision LoRA fine‑tune (T3.1–T3.2).  
- Deploy `blueprint_vl_train_lora.py` via `py-subprocess-bridge → DetachedLoRARunnerEngine`.  
- Install torch ≥ 2.7/cu128, peft/datasets/trl; enable GPU preflight (`is_available()` false‑positive on sm_120).  
- Generate ≥50 calibrated samples (grow trainset, pre‑warm ≥2 VLMs).  
- Create operator‑verified eval split (Brier ≤ 0.15) before production deployment.

**FINDINGS/BUGS**  
- Leading‑dot decimals in OCR output caused entire extraction to be discarded.  
- Single‑model OCR runs silently produced “gold” tier labels; fixed by `no_corroboration` rule.  
- Ollama daemon contention caused timeouts for cold models on Blackwell.  
- GPU is sm_120 but torch 2.6/cu124 only supports ≤sm90 → false positive `cuda.is_available()`.  
- `wedm_train_lora.py` is a simulation stub; real VL trainer required.  
- Calibration under‑powered (≤28 samples); needs ≥50 for reliable Brier.

**DOMAIN SPECIFICS**  
- Galaxy: **xray** – blueprint‑vision, RTX 6000 Blackwell, VLMs: qwen3‑vl, qwen2.5‑vl, llama3.2‑vision.  
- Engines/dispatchers: `vision-ensemble-fuse`, `ocr-training-loop-lib`, `BlueprintLoRABridgeEngine`, `DetachedLoRARunnerEngine`.  
- Metrics: agreement fraction → accuracy (isotonic), tier thresholds (gold/silver/bronze/no_corroboration).  
- Paths: `/checkin-xray`, `/startup-xray`, `/compact`, per‑agent handoff, slot‑binding hooks.

**TOOLS USED**  
- PRISM commands: `/checkin-xray`, `/startup-xray`, `/compact`.  
- Helpers: `slot-bind-enforce.mjs`, `chat-slots.mjs`, `milestone-tracker.mjs`, `stable-session-id.mjs`, `precompact-pending-guard.mjs`.  
- Libraries: `vision-ensemble-fuse.mjs`, `ocr-training-loop-lib.mjs`, `blueprint_vl_train_lora.py`, `py-subprocess-bridge`, `DetachedLoRARunnerEngine`, `GpuStackHealthEngine`.  
- Testing: Node test harness (`node:test`), Python `--self-test`.

**OPEN THREADS**  
1. Finish T3.2: wire `py‑subprocess‑bridge → DetachedLoRARunnerEngine`, monitor, dispatcher.  
2. Implement T1.3: GPU preflight check for sm_120 (real matmul probe).  
3. Grow trainset to ≥50 calibrated samples (pre‑warm ≥2 VLMs).  
4. Install torch ≥ 2.7/cu128 + peft/datasets/trl; set up operator‑verified eval split.  
5. Resolve Ollama contention for cold models on Blackwell.  
6. Complete full 3‑model ensemble benchmark once daemon quiet.  
7. Finalize India LoRA fine‑tune pipeline (deploy, monitor, gate).
