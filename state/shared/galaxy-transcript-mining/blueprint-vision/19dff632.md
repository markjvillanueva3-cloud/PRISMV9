# blueprint-vision session 19dff632 (2026-06-10, 23.8MB, spine 178KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e44a3a1592` – wired 4 orphan payroll‑filing methods into `prism_business`.  
- `e649790e76` – added fifth action `remitLiability` (positional args) and updated tests.  
- `f069052772` – verified the 6 “false‑gap” audit items are shipped.  
- `9dfd621910` – committed reusable transcript miner (`scripts/mine-hotel-transcripts.mjs`).  
- `61518eb988` – fixed four miner bugs (empty‑response, noise filtering, combined‑combined glob).  
- `099e6b92bd` – final hotel forge roadmap (`state/shared/specs/HOTEL-FORGE-ROADMAP-2026-06-09.md`).  
- `U-HOTEL-PORTAL-PERSISTENCE` – migrated 4 durable Maps to SQLite WAL (`dd57b82b52`, scrutiny‑fix `17f3e0ffec`).  
- `U-HOTEL-ALLOWLIST-WRITE-ENABLE` – added per‑action manager‑role gate for workflow writes (`18f37c812e`, polish `d8d2824cf2`, doctrine spec `c84c48b453`).  
- `U1 (iOS redesign foundation)` – single‑source CSS vars, SF font, Tailwind utilities (`7cc24f0482`).  
- `U2 (primitives upgrade)` – updated WorkspacePrimitives, added ResultCard/Stepper, fixed bare‑var utilities (`ecdd33a2ea`).  
- `ecdd33a2ea` – 3‑of‑3 PASS, R12 defect caught pre‑commit.

**DECISIONS**  
- Use Ollama for bulk mechanical work; default model `qwen2.5-coder:32b`, reserve `gpt-oss:120b` for heavy synthesis.  
- Employ ultracode workflow for high‑level reasoning and roadmap synthesis.  
- Reset budget with `/compact`; immediately push portal persistence (`U-HOTEL-PORTAL-PERSISTENCE`).  
- Approve role model for write‑enable; keep financial/PII writes 403.  
- Fleet‑wide iOS redesign supersedes Calculator Studio HUD doctrine; coordinate with Quebec.  
- Adopt single‑source `:root` CSS vars exposed via Tailwind config.  
- Keep dominant WorkspacePrimitives for 106 pages; defer migration of `components/ui/*`.

**OPERATOR DIRECTIVES**  
- `/compact` (reset context).  
- Begin `U-HOTEL-PORTAL-PERSISTENCE` after compact.  
- Continue roadmap: `ALLOWLIST-WRITE-REVIEW`, `REALTIME-VERIFY`.  
- Approve role model (already granted).  
- Proceed with `/compact` then start U1/U2.  
- Explicit ask: “customization + haptics.”  
- Extend Quebec’s `data-theme` system; do not fork.

**FINDINGS/BUGS**  
- Stale audit: missing tax engine built; payroll wiring gaps resolved.  
- Mobile engines: 6 wired, only `MobileVoice` exempt (tagged).  
- Q2S DFM bug confirmed – file to Charlie.  
- False‑wire guard flagged two issues: ambiguous `marketplace_lead_get` null return and regex bug in placeholder detector; both fixed.  
- Marketplace lead param mismatch fixed.  
- Detector regex bug fixed.  
- Placeholder regex missed “Unknown business action” string – fixed.  
- `customer-portal.db`, `-wal`, `-shm` added to `.gitignore`.  
- Bare‑var Tailwind utilities (`min-h-[var(--tap-min)]`) replaced with `min-h-11`, `tracking-[-0.02em]`.  
- Haptics only on native shell; Capacitor missing.  
- R8/R7 discovery: Quebec parallel iOS theme exists; both share `prism‑theme` localStorage key; divergence in values (PRISM‑cyan accent + 18 px radii vs systemBlue + 10/14/16 px).

**DOMAIN SPECIFICS**  
- Slot: **hotel** (ERP/HR/business).  
- Key engines: `PayrollLiabilityFilingEngine.ts`, `BusinessDispatcher.ts`.  
- Constants audit covers 11 ERP data files (`payroll-tax-tables.js`, etc.).  
- Allowlist actions: 17 read‑only entries in `src/data/business-dispatch-allowlist.ts`.  
- CustomerPortalEngine uses SQLite WAL via CoordinationStoreEngine pattern.  
- Business dispatcher: 879 actions, 17 read‑only allowlist.  
- Role gate for writes: manager-tier roles (`lead`, `supervisor`, `hr_manager`, `admin`).  
- Tailwind config and CSS vars provide design tokens; SF font set in `<html>`.  
- WorkspacePrimitives component set backs 106 pages.

**TOOLS USED**  
- Ollama (`qwen2.5-coder:32b`, `gpt-oss:20b/120b`).  
- Ultracode workflow (8 agents, resumable).  
- Scripts: `scripts/mine-hotel-transcripts.mjs`, `ask-ollama.mjs`.  
- Git hooks: slot‑commit‑enforce, ASCII‑guard, 3-of‑3 PASS, R12 defect detection.  
- PRISM tools: business dispatcher, false‑wire guard, auditLog middleware.  
- SQLite via better-sqlite3 (WAL).  
- Tailwind CSS v3.4.17, PostCSS, Vite.  
- framer-motion, react-grid-layout, @dnd-kit.  
- 3-of-3 scrutiny gate (`node .claude/scripts/scrutiny-3way.mjs`).

**OPEN THREADS**  
- Build `U-HOTEL-PORTAL-PERSISTENCE` (heavy persistence of 4 in‑memory maps).  
- Resolve remaining false‑wire guard issues.  
- Finalize and execute roadmap units: `ALLOWLIST-WRITE-REVIEW`, `REALTIME-VERIFY`.  
- Implement Capacitor shell for real haptics.  
- Migrate `components/ui/*` primitives to new design system.  
- Continue UI refinements (U3–U7).  
- Coordinate full fleet rollout with Quebec.  
- Read Quebec’s iOS theme implementation before adding `useThemeTokens`.  
- Decide on brand direction: PRISM‑cyan vs authentic systemBlue.  
- Determine feasibility of U3 (customization + haptics) post‑reconciliation.
