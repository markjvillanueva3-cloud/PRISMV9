# blueprint-vision session 571d4bdd (2026-05-19, 16.8MB, spine 56KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- **U‑TDP06 – CNC‑derived ground truth** (commit `01e59ad58a`): pure G‑code → feature‑kind library, 35/35 tests, CLI smoke‑verified.  
- **U‑TDP07 – OCR cascade extractor** (commit `5270c65aea`): PyMuPDF vector‑text + raster rendering + Qwen2.5‑VL VLM; wired into `run‑ocr‑benchmark.mjs`; 46/46 tests, end‑to‑end smoke passes.

**DECISIONS**  
- Adopt a **layered OCR pipeline**:  
  1. Extract exact text from vector PDFs (PyMuPDF).  
  2. Render raster pages → feed to local VLM (Qwen2.5‑VL 7B) for dense dimension extraction.  
  3. Use presence‑only ground truth; nominal values are omitted because they cannot be reliably inferred from raw G‑code or PDF text.  
- **Local‑first**: run all heavy inference on the home RTX 4080 via Ollama; fall back to `llama3.2‑vision:11b` if memory pressure occurs.  
- Keep **benchmarking honest (R12)** by matching the GT schema (`.nominal` vs `.presence_only`) and rejecting any nominal data that cannot be proven.

**OPERATOR DIRECTIVES**  
- “Continue improving our OCR and print reading capabilities. Do deep research and find other reputable tools from good sources to plug into our OCR pipeline to improve accuracy.”  
- (Earlier, unfulfilled) “Check bus chat, golf redistributed work from today to the chats.”

**FINDINGS / BUGS**  
- `nominal_mm` field mismatched benchmark’s `.nominal`; rewritten to presence‑only.  
- Regex bug in dimension parsing (`\b` after `L.E.`); fixed.  
- Qwen2.5‑VL memory: 15.4 GB on 16 GB VRAM → added pre‑warm and extended timeout; fallback to llama3.2‑vision if needed.  
- Anti‑pattern detector false positives (token→needle rename, base64 byte naming); corrected.  
- PDF corpus is ~96 % vector, not raster as initially assumed.

**DOMAIN SPECIFICS**  
- **Blueprint OCR Pipeline**: `scripts/blueprint-extract-sidecar.py` (PyMuPDF), `scripts/lib/blueprint-extractor-lib.mjs`, `scripts/lib/blueprint-extract-io.mjs`.  
- **Metrics**: presence‑only GT score, vector PDF ratio, VLM inference latency per page, memory usage.  
- **Dispatchers**: per-file scrutiny reviewers (A & B), precompact guard, slot‑binding helpers, milestone tracker.

**TOOLS USED**  
- Internal: `chat-slots.mjs`, `per-agent-handoff.mjs`, `milestone-tracker.mjs`, `precompact-pending-guard.mjs`.  
- External: PyMuPDF (text & raster), Ollama (`qwen2.5-vl:7b`, `llama3.2-vision:11b`), eDOCr2 (research reference), PaddleOCR, Surya, docTR.

**OPEN THREADS**  
1. Run the 3‑of‑3 close‑out scrutiny for U‑TDP06 (`01e59ad58a`).  
2. Create a real PDF GT corpus to fully grade U‑TDP07 against CAD and CNC ground truth.  
3. Integrate eDOCr2 segmentation stage or fine‑tune a small head on the 76 k JM Die corpus (U‑TDP08).  
4. Finalize Qwen memory handling (pre‑warm, timeout, fallback strategy).  
5. Execute `/compact` to seal the session and hand off to next user.
