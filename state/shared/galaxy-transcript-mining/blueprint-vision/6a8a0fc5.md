# blueprint-vision session 6a8a0fc5 (2026-06-23, 50.3MB, spine 400KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5fc84e6fbf` – U‑QP‑TSX‑REEXEC‑E2E‑ENV‑ROBUST (env‑brittle E2E test fixed)  
- `0a41c90a4c` – region‑classifier‑lib (pure GPU‑free segmentation, data‑loss‑safe routing)  
- `region-glue-lib.mjs` – fraction→pixel scaling, recall‑first union (`mergeRegionResults`)  
- `region-classify.mjs` – DI‑testable orchestrator + live Ollama VLM smoke  
- `e9e338adfd` – --region-route wired into validate‑perfect‑parts.mjs  
- `111…` – Hybrid fused builder `buildRegionRoutedFused` (preserves non‑dim fields)  
- `08f77dee4e` – live VALIDATE instrumentation on part 05850  
- `7a58fb6d0a` – 3‑seed run on part 05850, recall 0.4286 stable, 9/9 pages OK  
- `baff21551d` – per‑page region‑route diagnostics (`classifyRegionPageDrop`)  
- `a3bab52e28` – fixed floor‑dim counting (finite‑positive predicate)  
- `54bf706045` – merge‑clique collapsed stat for drop attribution  
- `1cd25d757d` – cross‑seed consumer `region-drop-report` & CLI  
- `e2e469593b` – provisional/variance‑checked gating to consumer output  
- `a783df2419` – U‑XRAY‑ENSEMBLE‑NONDIM‑UNION (fuseEnsemble returns dims, gdt, notes, profiles, surface_finishes)  
- `abc63f4874` – U‑XRAY‑GDT‑LABEL‑TIER (added `gdt_labels` to trainset JSONL & LoRA builder; operator‑confirm CSV)  
- `e7fd24791b` – U‑XRAY‑P15‑REGION‑NONDIM‑RESCUE (`mergeRegionFused` preserves non‑dim fields on dense pages)  
- `3a330195d6` – U‑XRAY‑GDT‑GOLD‑VERIFY (buildGdtRecords, VERIFY‑gdt.csv)  
- `U-XRAY-OCR-ADAPTER-WIRE-EXEMPT` – added WIRE‑EXEMPT marker to BlueprintOCRAdapter.ts  
- `U-XRAY-OCR-ADAPTER-WIRE-EXEMPT-REVERT` – removed marker, added inline docstring

**DECISIONS**  
- Build pure libs → glue → live orchestrator (cores‑before‑integration).  
- Data‑loss‑safe contract: region routing may only add recall to full‑page floor.  
- Recall‑first union via `mergeTiledDimensions` (non‑transitive clique) to avoid over‑merging same values.  
- Region routing reserved for dense pages; validated by 28 dims recovered vs 0 on full‑page OCR.  
- Hybrid fused output must include all non‑dim fields before training cron wiring.  
- GD&T closed‑loop: carry → train → recover → confirm (U‑XRAY‑GDT‑LABEL‑TIER, U‑XRAY‑GDT‑GOLD‑VERIFY).  
- Wire‑exempt marker used only for BlueprintOCRAdapter; later reverted to keep unit UNWIRED.  

**OPERATOR DIRECTIVES**  
- Run autonomous `/loop` on `xray` slot (10 min iterations); prioritize OCR/blueprint reading.  
- Use Ollama (`qwen3-vl:8b-instruct`, `qwen3-coder:30b`) for VLM calls; fall back to Sonnet/Opus if needed.  
- Maintain per‑file 2‑arm scrutiny + end‑of‑task 3‑of‑3 gates.  
- Do not start new units when PC health thresholds exceeded (RAM > 88 %, GPU > 93 GB, CPU > 90 %).  
- After each unit, commit `[MAIN-FORCE]` on `cad-fusion-live-ms0`, update handoff, trigger auto‑compact if token budget YELLOW.  
- Checkpoint at YELLOW; let auto‑compact reset before new unit.  
- Stage GD&T source PDFs for next volume‑blocked unit (per‑type calibration).  

**FINDINGS/BUGS**  
- Quoting E2E test environment‑specific; fixed to assert honest JSON verdict.  
- Full‑page OCR fails on dense 3400×2200 pages (0 dims); region routing recovers 28 dims.  
- Training cron wiring blocked due to missing `summary` & non‑dim fields in hybrid fused output; resolved by `buildRegionRoutedFused`.  
- Merge bug ruled out: floor dim cannot be dropped by merge (clique logic).  
- `fuseEnsemble` silently dropped `gdt`, `notes`, `profiles`, `surface_finishes`; fixed.  
- Region routing lost GD&T/notes on dense‑page rescue; union now preserves them.  
- `BlueprintOCRAdapter` mis‑classified as unwired; marked wire‑exempt, then reverted to keep unit UNWIRED.

**DOMAIN SPECIFICS**  
- Engines: `extractWithRegionRouting`, `buildRegionRoutedFused`, `validate-perfect-parts.mjs`, `region_page_diags`, vision‑ensemble‑fuse, ocr‑training‑loop‑lib.  
- Dispatchers/Agents: Hermes (Sonnet), Octopus consensus (`prism_ai:consensus_decide`), Opus fallback.  
- Metrics: recall per page (0.4286 on part 05850), dims count (28 via region routing), pages with dims, varianceChecked flag, `n_gdt`, `n_notes`, `n_profiles`, `n_surface_finishes`.  
- Paths: `state/shared/ocr-training-loop/perfect-print-cad-program-parts.json`, `scripts/lib/region-glue-lib.mjs`, `blueprint-gdt-corpus/`, `pdf-extract-gdt-2026-06-23.jsonl`.

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `handoff`, `loop-state.mjs`.  
- Build tools: `audit-unwired-engines.mjs`, `mergeTiledDimensions`, `vision-tiling-lib.mjs`, `crop-image-tiles.py`.  
- AI models: Ollama (`qwen3-vl:8b-instruct`, `qwen3-coder:30b`, `qwen2.5-coder:32b`), Sonnet, Opus.  
- Agents: Hermes (Sonnet agents), Octopus (consensus).  
- Scripts: `node scripts/audit-unwired-engines.mjs`, `node scripts/fleet-survival-status.mjs`, `node scripts/self-compact.mjs`.  

**OPEN THREADS**  
1. Training cron wiring – integrate `rr.fused` into `buildTrainsetRow`/`classifyActiveLearning`; recompute summary counts (pending final commit).  
2. Heavy multi‑part comparison – run full‑page vs region routing vs tile on multiple parts (GPU‑heavy, queued).  
3. Per‑type GD&T calibration (P2.9) blocked by volume; await operator staging of PDFs.  
4. Continuous health monitoring & auto‑compact trigger logic.  
5. Verify no other unwired false positives after wire‑exempt marker removal.  
6. Slot routing misconfiguration causing peer‑slot directives to land on xray session – launch‑side fix needed.
