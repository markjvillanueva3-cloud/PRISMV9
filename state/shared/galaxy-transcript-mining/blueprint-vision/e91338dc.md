# blueprint-vision session e91338dc (2026-05-19, 6.5MB, spine 18KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `/build-brief.mjs` skill (≈620 L) – reads full wiki bodies, tribal tips, and regression logs to compose a deep knowledge brief for a unit or topic.  
- `build-brief.md` command file with frontmatter, triggers, and documentation.  
- 34‑test suite (node:test) fully passing; E2E tests confirm correct JSON output and markdown rendering.  
- Patch‑sibling commit applied to `CLAUDE.md`; trigger ledger regenerated so `/build-brief` is auto‑discoverable.

**DECISIONS**  
- Reuse existing `unit-knowledge-pack.mjs` plumbing; avoid duplicate logic.  
- Switch from architecture‑graph fallback to direct `_leaf-index.jsonl` lookup for wiki resolution (ensures graph‑independent deep context).  
- Exclude `code-tribal` from the boosted set to keep “deep” content in design/architecture domain.  
- Implement IDF weighting and title deduplication to surface relevant sections over generic keyword matches.  
- Rewrite `excerptBody` with provable budget math: honor `maxChars`, preserve lead section, avoid overshoot of truncation marker.  
- Add path‑containment guard on leaf‑index `path` field to prevent repo traversal.  
- Wire missing CLI flags (`--k`, `--tribal-k`, etc.) and ensure `parseArgs` matches the command signature.

**OPERATOR DIRECTIVES**  
*(none requested beyond current loop)*

**FINDINGS/BUGS**  
- Deep wiki context initially 0 due to wrong index usage; fixed by querying `_leaf-index.jsonl`.  
- Duplicate tribal entries and double‑bullet lines in output; removed via dedup logic.  
- `excerptBody` truncation marker could overshoot `maxChars`; corrected with section‑budget algorithm.  
- `parseArgs` exposed config fields not present on CLI; aligned flag set.  
- Path containment guard needed for leaf‑index `path` field; added to prevent traversal.  
- IDF weighting was ineffective when `code-tribal` boosted; removed boost and re‑weighted tokens.

**DOMAIN SPECIFICS**  
- PRISM slot model (`juliett`, session continuity, terminal‑pinning).  
- Wiki architecture: 28 520 entries in `_leaf-index.jsonl`; types include architecture, action, engine, tribal-tip, code‑tribal.  
- IDF weighting (inverse document frequency) for keyword relevance; `LEAF_MEATY_MULT = 1.25` boosts hand‑written design types.  
- Memory files (`memory/*.md`) and git commit extraction via `gitCommitsForMilestone`.  
- Architecture‑graph fallback when system‑graph.json > 200 MB (now bypassed for wiki resolution).  
- Trigger ledger integration for auto‑discovery of new skills.

**TOOLS USED**  
- Node.js scripts (`build-brief.mjs`, helpers in `unit-knowledge-pack.mjs`).  
- Master index search lib (`runMasterIndexSearch`, `runTribalSearch`, etc.).  
- `node:test` framework with two parallel reviewer agents.  
- Patch‑sibling pattern for editing `CLAUDE.md`.  
- Trigger ledger regeneration script (auto‑trigger hook).  

**OPEN THREADS**  
- Further high‑ROI skills beyond `/build-brief` (e.g., automated knowledge‑injection for new feature branches).  
- Continuous improvement of IDF weighting and relevance scoring.  
- Exploration of deeper integration with PRISM’s VLM ensemble for richer brief generation.  
- Monitoring of memory file updates and git commit extraction performance at scale.
