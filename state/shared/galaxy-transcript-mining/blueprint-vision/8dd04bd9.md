# blueprint-vision session 8dd04bd9 (2026-06-24, 24.7MB, spine 173KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-XRAY-CAD-GT-TRIANGULATE` – `cad-dimension-gt-lib.mjs`: 26/26 tests, GPU‑free.  
- `U-XRAY-EXTRACTION-CONTRACT` – `BlueprintExtractionContract.ts`: 14+ tests, tsc‑clean.  
- `U-XRAY-RECONCILE-CANDIDATES` – `reconcile-candidate-adapters.mjs`: 12/12 tests, no P0/P1.  
- `U-XRAY-RECONCILE-LIVE` – `--reconcile` mode runs engine on real part, produces consensus dims; live‑data validated.  
- `U-XRAY-DRAWING-EXTRACT-CONTRACT-NORMALIZER` – added `normalizeDrawingExtractToContract`; 28 tests, tsc‑clean.  
- `U-XRAY-EXTRACT-CONTRACT-WIRE` – dispatcher action `blueprint_extract_contract` + `/api/v1/cad/blueprint-extract-contract` route; 5 round‑trip tests, 2‑arm scrutiny PASS.

**DECISIONS**  
- Added CAD GT triangulation lib for `CrossSourceDimensionReconciliationEngine`.  
- Created drawing_extract normalizer to fix fuseEnsemble shape mismatch.  
- Wired contract via dispatcher action mirroring `blueprint_redact`.  
- Deferred GPU mill‑recall validation (87 % GPU load); schedule in quiet window.  
- OCR training crons disabled pending elevation.  
- Skipped building document extraction contract (cross‑domain, no consumer).

**OPERATOR DIRECTIVES**  
- Continue closed‑loop training (crons currently disabled).  
- Plan application of blueprint reading/OCR/document reading into PRISM app features.  
- Use ollama offloading, hermes agents, parallel agents, engineered loops, harnesses, crons.

**FINDINGS/BUGS**  
- CAD‑GT for OCR recall deprioritized; only 5/11 neutral‑STEP parts have program GT → minimal recall leverage.  
- OCR/training crons all disabled or missing (`Blueprint OCR Batch` failed, `OCR Training Loop` missing).  
- GPU mill‑recall validation would evict shared coder model; GPU heavily used.  
- Silent‑loss seam: `drawing_extract` output incompatible with fuseEnsemble normalizer – fixed by new normalizer.  
- `slimResponse` strips empty `errors[]`; test adjusted accordingly.  
- No new orphans in blueprint‑vision after audit‑unwired‑engines sweep.

**DOMAIN SPECIFICS**  
- Cross‑source dimension determination: prints OCR + CAD STEP + CNC programs → consensus via `CrossSourceDimensionReconciliationEngine`.  
- Engine consumes `DimCandidate[]` (value_mm, type, source, confidence) → `ReconciliationReport`.  
- fuseEnsemble output shape: `dimensions[]`, `gdt`, `notes`, `profiles`, `surface_finishes`, `summary`.  
- Vision extraction reps: `extractGdt`, `extractNote`, `extractProfile`, `extractSurfaceFinish`.  
- `step-dimension-extract.mjs`: radii & bbox from STEP text.  
- `cnc-program-gt-lib.mjs`: program GT extraction for mill programs.  
- `validate-perfect-parts.mjs` harness: OCR recall evaluation against program GT.  
- `BlueprintExtractionContract.ts` schema: dimensions, gdt, notes, profiles, surface_finishes, summary with confidence thresholds.  
- Dispatcher action `blueprint_extract_contract` and `/api/v1/cad/blueprint-extract-contract` route.

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `slot-bind-enforce`.  
- Canonical `/checkin` pipeline.  
- Unit tests via `node:test` + `vitest`.  
- Type checking with `tsc`.  
- Scripts: `vision-ensemble-fuse.mjs`, `step-dimension-extract.mjs`, `cnc-program-gt-lib.mjs`, `validate-perfect-parts.mjs`.  
- Contract & normalizer code (`BlueprintExtractionContract.ts`).  
- Dispatcher action implementation in `cadDispatcher`.  
- Automated nightly build: cron job‑xray-autobuild‑001 (*/10 * * * *, 7 day auto‑expire) – every 10 min, uses `ask‑ollama.mjs` & `ask‑hermes.mjs` for mechanical work.

**OPEN THREADS**  
- GPU mill‑recall validation pending scheduling (quiet fleet window).  
- OCR training crons disabled; need elevation to re‑enable or confirm intentional disable.  
- Phase‑1 upload route still unimplemented but dispatcher action present; front‑end integration pending.  
- Document extraction contract not built (cross‑domain, no consumer).  
- Blueprint OCR adapter deferred (no concrete backend yet).  
- Scheduled recurring task `job-xray-autobuild-001` created; can cancel with `CronDelete <job-id>`.
