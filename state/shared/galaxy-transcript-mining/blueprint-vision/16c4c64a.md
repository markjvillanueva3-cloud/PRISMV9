# blueprint-vision session 16c4c64a (2026-06-04, 22.6MB, spine 128KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-CGP-CONCURRENCY` – commit c0ce903f35: async worker pool for vision OCR on RTX 6000 Blackwell.  
- `U-CGP-MEASURE` – commit 8fd4193762: real‑data E2E measurement (≈ 0.189 pages/min/worker → 2× throughput).  
- `U-CGP-NUMPARALLEL-RECO` – commit ca4395947b: raise `OLLAMA_NUM_PARALLEL` from 2→3+.  
- `U-WIRE-SHOP-OUTCOME-INGEST` – commit 9b5aa4c2b6: wire ShopOutcomeIngestProcessorEngine → `prism_dev:shop_outcome_ingest`.  
- `U-WIRE-GCODE-MATERIAL-PARSE` – commit 267718a1ee: wire GCodeMaterialParserEngine → `prism_dev:gcode_material_parse`.  
- Reachability classifier + E2E test – commit b902ac2024 (110→49 genuine / 54 false).  
- `CircularDependencyEngine → prism_dev:dependency_cycle_analyze` – commit 6917ccaf3e (Tarjan SCC, 6 round‑trip tests).  
- `OperatorPreferencesEngine → prism_session` – commit 7ec7c17249 (set/get/apply, 8 round‑trip tests).  
- `PSNHealthCheckEngine → prism_dev:psn_health_check` – commit b240aa83c0 (11‑leg classifier, 4 tests).  
- `MeasurementSystemAnalysisEngine → prism_dev:msa_analyze` – commit 6620095eaf (Gage R&R ANOVA, 5 tests).  
- `SimulationStallDetectorEngine → prism_dev` – commit a3ab2bc96c (5‑action lifecycle, 7 tests).  
- `SlotSessionHistory wire` – case‑insensitive guard fix (2 tests).  

**DECISIONS**  
- Bounded async worker pool driven by GPU tier concurrency; honor `OLLAMA_NUM_PARALLEL`; effective workers = min(workers, slots).  
- Replace serial `spawnSync` with `runExtractionPool`; add fail‑soft and timeout handling.  
- Build reachability classifier detecting DIRECT_WIRED, AGGREGATOR_REACHABLE, CAPABILITY_WIRED_ELSEWHERE, PHANTOM_NAME.  
- Use path‑scoped commits to avoid slot‑enforce absorption.  
- Split genuine orphan classification into type‑(a) (wire directly) vs type‑(b) (route to domain).  
- Checkpoint strategy: finish light wires, pause, let 5 min cron fire for heavy multi‑action engines.  
- File‑backed engine guard made case‑insensitive on Windows.

**OPERATOR DIRECTIVES**  
- Wire six verified genuine orphans:  
  * `CircularDependencyEngine → prism_dev:dependency_cycle_analyze`  
  * `SemanticAssetIndexEngine → prism_dev:semantic_asset_index`  
  * `PSNHealthCheckEngine → prism_session:psn_health_check`  
  * `OperatorPreferencesEngine → prism_session:operator_preferences`  
  * `SlotSessionHistoryEngine → prism_session:slot_history`  
  * `CNCToolOffsetPersistenceEngine → prism_turning:cnc_tool_offset`  
- Continue wiring until session‑scoped Stop hook “[complete classifier aware hunt]” satisfied.  
- No user prompts.

**FINDINGS/BUGS**  
- ExpandingMandrelEngine misclassification fixed via substring capability detection (`calculateExpandingMandrel`).  
- Slot‑enforce hook absorption resolved with `[BOOTSTRAP-SLOT-ENFORCE]` + path‑scoped commit.  
- `normalizeParams` alias map bug corrected (snake_case handling).  
- Edge‑only node cycle bug in `CircularDependencyEngine` fixed.  
- `SlotSessionHistoryEngine` guard case‑sensitive → fixed to case‑insensitive on Windows.  
- `.git/index.lock` contention resolved with lock‑aware retry loop.  
- `stable-session-id.mjs` returned empty; flagged for fix.  
- 49 % false positives in unwired list routed to sierra.  
- CIMCO machine‑bind answer delivered (f1e4ade66e) with `.mcfg` source path and VMC‑01 mapping.

**DOMAIN SPECIFICS**  
Engines: CircularDependencyEngine, SemanticAssetIndexEngine, PSNHealthCheckEngine, OperatorPreferencesEngine, SlotSessionHistoryEngine, CNCToolOffsetPersistenceEngine, MeasurementSystemAnalysisEngine, SimulationStallDetectorEngine.  
Dispatchers: `prism_dev` (dependency tooling, psn_health_check, msa_analyze, sim_stall_*), `prism_session` (operator_prefs_*, slot_history), `prism_turning` (cnc_tool_offset).  
Metrics: pages/min/worker ≈ 0.189; concurrency speedup 2×; effective workers bounded by `OLLAMA_NUM_PARALLEL`; 30 round‑trip tests; tsc‑clean.  
Key paths:  
- `scripts/batch-ollama-vision-extract.mjs`  
- `scripts/lib/catalog-gpu-profile.mjs`  
- `scripts/measure-catalog-extraction-rate.mjs`  
- `scripts/classify-engine-reachability.mjs`  
- `mcp-server/src/tools/devDispatcher.ts`  
- `state/shared/cimco/romeo-machine-bind-answer.md`  
- `machine-index.json`  
- `jm-fleet-sim-map.json`.

**TOOLS USED**  
PRISM helpers (`chat-slots.mjs`, `slot-bind-enforce.mjs`), scheduling (`CronCreate`, `CronDelete`), internal scripts (batch‑ollama‑vision‑extract, catalog‑gpu‑profile, measure‑catalog‑extraction‑rate, classify‑engine‑reachability, devDispatcher, devActionSchemas), dispatcher pattern (enum + schema + case), Zod validation, Vitest tests, TypeScript compiler (`tsc`), Git commit with `[BOOTSTRAP-SLOT-ENFORCE]` and lock‑aware retry loop, AGENT_CHAT writer.

**OPEN THREADS**  
- Wire remaining genuine orphan engines (`SBOMReviewEngine`, `CNCToolOffsetPersistence`) with round‑trip tests; update dispatchers.  
- Integrate reachability classifier into system‑viz ghost.unwired detector.  
- Verify no false positives after wiring.  
- Route infra‑dependent engines to other domains (e.g., SemanticAssetIndex → juliett/india).  
- Confirm Stop hook “[complete classifier aware hunt]” satisfied and will auto‑clear.
