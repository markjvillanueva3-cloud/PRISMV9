# blueprint-vision session b0ec5c36 (2026-06-06, 12.2MB, spine 55KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `e354869c93`: keystone OCR parse fix (repairLeadingDotDecimals + repairTruncatedJson, handles leading‑dot and leading‑plus JSON errors). 61/61 tests passed, two‑reviewer scrutiny PASS.  
- T3.2 Node→GPU LoRA runner (`scripts/lib/blueprint-vl-train-runner.mjs`) shipped; 16 hermetic tests + live end‑to‑end validation on Blackwell (torch 2.11+cu128).  
- Updated `INDIA-TAKEOVER-PLAN-blueprint-lora.md` and `CLAUDE.md` regression entry; doc‑reflection commit landed.  

**DECISIONS**  
- Use a slot‑binding wrapper (`/checkin-xray`) to guarantee correct handoff before the canonical `/checkin` pipeline.  
- Gate GPU training on live `gpu_health.py` (arch‑list + matmul probe) to avoid running with incompatible torch.  
- Deploy a dedicated venv (`python-gpu`) for GPU work; bootstrap pip if missing.  
- Compose JSON repair functions in order: truncate repair → leading‑dot/plus strip, to preserve data integrity.  
- Rely on the overnight `PRISM Blueprint OCR Batch` task for corpus‑scale processing; accept load‑dependent throughput as design.  

**OPERATOR DIRECTIVES**  
- Run the GPU dependency install from an operator terminal (not harness):  
  ```
  ! H:/Tools/python-gpu/Scripts/python.exe -m ensurepip
  ! H:/Tools/python-gpu/Scripts/python.exe -m pip install trl qwen-vl-utils pillow pymupdf
  ```  
- After dependencies, execute `runBlueprintVlLoRATrain({bundlePath, outputDir})` to finish LoRA fine‑tune.  

**FINDINGS/BUGS**  
- OCR ensemble silently dropped all prints due to malformed JSON (`".86"`, `"+0.015"`).  
- GPU health check failed with torch 2.6+cu124 (no sm_120); required torch ≥ 2.7+cu128.  
- Long pip installs are killed by the harness (exit 255) regardless of foreground/background; must run in operator terminal.  
- Corpus batch task stops early under peak fleet load; design relies on repeated overnight runs.  

**DOMAIN SPECIFICS**  
- Engines/dispatchers: `gpu_health.py`, `blueprint_vl_train_lora.py`, `scripts/lib/lora-training-pipeline.mjs`, `py-subprocess-bridge.mjs`.  
- Actions: `parseVisionResponse`, `repairLeadingDotDecimals`, `repairTruncatedJson`, GPU matmul probe, LoRA training pipeline.  
- Metrics/outputs: `training_brier`, `adapter_dir`, per‑step `{event:"progress",stage:"train"}` logs.  
- Paths: OCR worklist (`blueprint-ocr-worklist-clean.txt`), checkpoint (`blueprint-ocr-checkpoint.jsonl`).  

**TOOLS USED**  
- PRISM CLI helpers: `/checkin-xray`, `chat-slots.mjs`.  
- VLM ensemble via Ollama (qwen3‑vl, qwen2.5vl, llama3.2‑vision).  
- Python GPU venv (`python-gpu`), py‑bridge for NDJSON streaming.  
- Testing harness: hermetic tests in `scripts/lib/blueprint-vl-train-runner.mjs`.  

**OPEN THREADS**  
- Operator must run pip install of missing trainer deps (trl, qwen_vl_utils, pillow, pymupdf).  
- Finish LoRA fine‑tune (T4.1) once GPU env is ready and eval split meets Brier ≤ 0.15.  
- Resolve corpus batch throughput under high fleet load; consider scheduling adjustments or task throttling.  
- Commit pending doc‑reflection when git lock clears.
