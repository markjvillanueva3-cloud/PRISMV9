# blueprint-vision session 396bc735 (2026-05-19, 24.3MB, spine 489KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `817c95ba72`: wired `backend‑dev` as 6th domain in `tribal-by-domain-inject.mjs`; added retag script, tests, validator.  
- `c346fe51b2`, `a8f3475028`, `d9f1b7960f`, `d716d20a96`: >20 high‑ROI backend‑dev wikis committed (software‑engineering + code‑tribal).  
- `dc3f2328b7`: merged `slot/lima` into `cad-fusion-live-ms0`; 22 lima wikis now visible to peer chats.  
- `f2e4e98b`: cron job created (every 5 min, session‑only) via `CronCreate`.  
- `ae7b42d1`: cron job for `/loop` at 5 min intervals (session‑only).  
- 10 new software‑engineering wikis:  
  - `parallel-tool-call-discipline.md` – 105 L, commit 9b5a0a2c49  
  - `fleet-coordination-discipline.md` – 149 L, commit 3d2b4df4c4  
  - `cron-and-scheduled-task-discipline.md` – 164 L, commit 5079190f45  
  - `commit-message-conventions.md` – 187 L, commit 318a26989c  
  - `prism-self-update-loop.md` – 163 L, commit fcf905d1a8  
  - `recall-injection-flow.md` – 178 L, commit b290ce2b20  
  - `bash-vs-powershell-on-windows.md` – 219 L, commit b290ce2b20  
- `wiki‑automation-discipline` updated.

**DECISIONS**  
- Domain order: `backend-dev` last in `DOMAIN_MAP`.  
- Retag selection: source=memory∧≥2 keywords OR source=external∧≥4 keywords; atomic write + idempotent plan/apply.  
- Cron interval 5 min chosen for session‑only scheduling (no cloud).  
- Adopt autonomous-loop drift discipline: limit investigations to one extra tick, record finding, resume loop.  
- Use `git commit -- <pathspec>` to clamp pathspec and avoid peer sweep.  
- Lock state handling: size 0 + mtime >60s → wedged peer; patient retry (do not kill below fleet‑reaper threshold).  
- `/loop` parsing: leading token or trailing “every” clause; if ≥60 min or daily phrasing, prompt for cloud schedule via `AskUserQuestion`; otherwise convert to cron expression and call `CronCreate`.  
- Cron conversion table:  
  - `Nm` (≤59) → `*/N * * * *`  
  - `Nm` (≥60) → round to whole hours (`0 */H * * *`)  
  - `Nh` (≤23) → `0 */N * * *`  
  - `Nd` → `0 0 */N * * *`  
  - `Ns` → treat as `ceil(N/60)m` and round.

**OPERATOR DIRECTIVES**  
- Continue `/loop [5m]` until high‑value wikis saturate (~61 total).  
- After merge, run `node H:/prism/scripts/regen-wiki-from-viz.mjs` to refresh `_leaf-index.jsonl` and embeddings.  
- Verify `wiki-precheck-inject.mjs` returns new wikis; check missing entries (e.g., `karpathy‑12‑rule‑discipline.md`).  
- Monitor Obsidian feed stamp (`state/shared/.obsidian-feed-stamp`) and trigger manually if absent.  
- “CRITICAL: Respond with TEXT ONLY. Do NOT call any tools.”  
- “Make sure everything you're building is automated or it will sit stagnant.”  
- “Check into lima and commit to lima work tree.”

**FINDINGS/BUGS**  
- `karpathy-12-rule-discipline.md` missing in leaf‑index; likely frontmatter/path issue.  
- Embeddings stale (~89 h) after merge; need regen pipeline.  
- Watchdog added but has not fired on a Stop hook yet.  
- Index.lock contention patterns identified (stale orphan, wedged peer, healthy).  
- Bash vs PowerShell redirect mismatch (`$null` vs `/dev/null`).  
- Git hook lane guard occasionally auto‑unstages peer scripts.

**DOMAIN SPECIFICS**  
- `backend-dev` token set: backend, hook, ollama, lora, gnn, neural, llm, embedding, kernel, slot, fleet, synergy.  
- `tribal-rerank.mjs` accepts `--domain` with R12 validator; error “unknown domain…”.  
- Retag script uses BD_KEYWORD_RE covering all R1–R12 tokens.  
- PRISM pipeline: WRITE → SYSTEM‑VIZ → WIKI INDEXES → OBSIDIAN VAULT.  
- Wiki precheck inject chain, master index precheck, memory relevance, tribal domain, chat bus, loop awareness, skill auto trigger, ollama injector.  
- SessionStart chain: awareness snapshot, build state, CLAUDE brief, auto resume, consolidated handoffs, terminal pin, git health, git sync.  
- Commit‑message conventions: `[SCOPE]/U-ID: title`; scope vocab; U‑ID rules; `[MAIN]` semantics; post‑commit hook parsing.  
- Fleet coordination: slot identity, claims architecture, chat bus doctrine, peer claim guards, cross‑tree commit collision class, index.lock diagnosis table.  
- Recall injection flow: inject blocks chain, reading rules, K‑tuning knobs, drill‑skill family.  
- Loop scheduling mechanics: interval parsing, cloud vs local decision logic, cron expression mapping, rounding strategy for non‑divisible intervals.

**TOOLS USED**  
- `.claude/hooks/tribal-by-domain-inject.mjs`  
- `.claude/scripts/tribal-rerank.mjs`  
- `scripts/retag-tribal-backend-dev.mjs`  
- `CronCreate` (jobs f2e4e98b, ae7b42d1)  
- `system-viz-on-commit.mjs`  
- `node H:/prism/scripts/regen-wiki-from-viz.mjs`  
- Watchdog script in `settings.json`  
- `schedule` skill  
- Git hooks: post‑commit, worktree‑commit‑route, lane‑guard  
- Pathspec clamping via `git commit -- <pathspec>`  
- PRISM scripts: `build-wiki-leaf-index.mjs`, `wiki-precheck-inject.mjs`, `regen-wiki-from-viz.mjs`  
- `AskUserQuestion`

**OPEN THREADS**  
- Run regen pipeline to update leaf‑index and embeddings after merge.  
- Fix indexing of `karpathy-12-rule-discipline.md`; ensure frontmatter includes `domain: backend-dev`.  
- Verify Obsidian feed stamp creation; trigger manually if missing.  
- Confirm cron job persists across sessions (currently session‑only).  
- Validate that all 61 wikis are auto‑injected via `wiki-precheck-inject.mjs` and appear in queries.  
- Verify new wikis indexed by SYSTEM‑VIZ and appear in Obsidian.  
- Monitor lock contention resolution; ensure patient retry logic holds under load.  
- Integrate `prism-self-update-loop` and `recall-injection-flow` triggers fully (test on next tick).  
- Consider adding a wiki on “parallel tool call vs parallel agent call” distinctions if gaps remain.  
- Implement `/loop` command handler applying parsing/decision logic; ensure cleanup of cron jobs when loops terminate or cancelled.  
- Validate rounding behavior for edge intervals (e.g., 7 m, 90 m).
