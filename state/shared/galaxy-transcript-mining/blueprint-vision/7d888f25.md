# blueprint-vision session 7d888f25 (2026-06-16, 5.7MB, spine 50KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `75b306e72c`: re‑armed the “PRISM OCR Training Loop” scheduled task to run daily at 02:00, bumped calibration window (8→24) and weak‑label window (120 s→5 h).  
- Commit `670329d41e`: added `keep_alive` support in the Ollama request builder (`buildOllamaRequestBody`) and corresponding tests; wrapper now sets `PRISM_OLLAMA_VISION_KEEP_ALIVE=15m`.  

**DECISIONS**  
- Use a reaper‑immune scheduled task for the 7,794‑print corpus instead of chat‑background runs.  
- Pin VLMs in VRAM with `keep_alive` to avoid cold‑loads and GPU eviction during calibration/weak‑label phases.  
- Keep the training loop resumable via a cursor; each run picks up where it left off after any reaper kill.  

**OPERATOR DIRECTIVES**  
- Verify the growing active‑learning queue (`corpus-train/active-learning-queue.jsonl`) and perform gold‑verification on those rows.  
- Once a sufficient gold set is confirmed, trigger India’s LoRA fine‑tune to reach 100 % CAD accuracy.  

**FINDINGS / BUGS**  
- Initial perception of missing VLM roster was a transient `/api/tags` misread; the full 16‑model roster is present.  
- Calibration slowdown (17 s→67 s per seed) caused by lack of `keep_alive`; models were evicted between calls, leading to cold‑loads.  
- Shared‑tree commit race: lib/test edits absorbed into a peer’s commit (`9b61cfb734`), but functional code is now in HEAD; attribution split noted.  

**DOMAIN SPECIFICS**  
- OCR pipeline stages: calibration (synthetic GT → trust function), weak‑labeling (real JM prints → silver tier, active‑learning queue).  
- Cursor file `processed-cursor.jsonl` tracks progress; trainset and active‑learning queue grow per print.  
- Scheduled task path: `H:/prism/scripts/run-ocr-training-loop-overnight.ps1`.  

**TOOLS USED**  
- PRISM helpers: `/checkin-xray`, `chat-slots.mjs`, `install-ocr-training-loop-task.ps1`.  
- Node scripts for OCR training loop, calibration, weak‑labeling.  
- Ollama HTTP API (no CLI).  
- PowerShell 5.1 wrapper for scheduled task.  

**OPEN THREADS**  
- Complete gold‑verification of the active‑learning queue and initiate LoRA fine‑tune.  
- Monitor nightly runs to ensure cursor reaches 7,794 without reaper kills; adjust `max-time-sec` if needed.  
- Verify long‑term stability of VLM `keep_alive` under fleet load (no unintended GPU starvation).
