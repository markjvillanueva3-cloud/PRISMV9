# post-processor session 9029a5d7 (2026-05-26, 21.4MB, spine 61KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e9bf140cbc` – consolidation spec + 15 WEDM dispatcher actions (consolidated 1 125 posts).  
- `efd4ccf0cc` – 15 lathe/AGI actions, WinMax driver, JM‑fleet audit.  
- `7030bfae9a` – WinMax driver (launch/verify/prove).  
- `a8f1c08033` – full dark‑engine wiring (+22 actions).  
- `840625c7eb` – R12 fail‑loud regression fix (quality_score 0 → 85, cross‑dialect leaks removed).  
- `a52d0f3472` – Vision‑to‑CAD MVP (PartMediaToCADEngine + MeasurementReconciliationEngine).  
- `PostLibraryEngine.ts` & tests committed.  
- `PostFeatureAuditEngine.ts` & dispatcher actions committed.  

**DECISIONS**  
- Master Post Processor will be built from PRISM engines; no direct Fusion/Inventor post.exe integration.  
- Build a *PostProcessorVerificationOrchestrator* that consumes any .NC file and runs PRISM analyzers.  
- Bridge all posts to the PPG page & employee portal via `post_library_*` dispatcher actions.  
- Adopt PSN + system‑viz for node tracking and visibility.  
- Treat the R12 quality_score=0 bug as a validator stub; fix it rather than re‑implementing the engine.

**OPERATOR DIRECTIVES**  
- Consolidate all posts in H drive into `CONSOLIDATED` (vanilla / work‑in‑progress) and `PRISM ENHANCED` folders, categorized by domain/brand.  
- Update JM fleet post processors for mill, lathe, wire; include advanced features per machine/controller.  
- Bridge to the post‑processor generator page & employee portal for downloads.  
- Build Master Post Processor for internal print‑to‑CNC program feature.  
- Add Vision‑to‑CAD reverse engineering capability (photo/video → parametric CAD).  
- Use Fusion/Inventor post kernel to generate G‑code and test with engines.  
- Ensure WinMax driver works for Hurco posts.

**FINDINGS / BUGS**  
- R12 fail‑loud regression: quality_score 0 caused by validator stub; fixed to 85.  
- Cross‑dialect leaks (5) from Heidenhain using Fanuc G81/G85; corrected.  
- Heidenhain/Mitsubishi asymmetry (lower quality scores); root cause regex mismatch, pending fix.  
- Hypermill posts missing; discovered .loc files are sidecars, not actual posts; scan updated.  
- Lock contention prevented committing 4 key files; awaiting clear window.  
- WinMax driver half‑built: verify mode unreliable, screenshot scope limited.

**DOMAIN SPECIFICS**  
- Engines: `PostLibraryEngine`, `PostFeatureAuditEngine`, `Vision-to-CAD` (`PartMediaToCADEngine`, `MeasurementReconciliationEngine`), `MasterPostProcessorUnifiedAGIEngine`, `PostProcessorVerificationOrchestratorEngine`.  
- File types: `.cps`, `.tcl`, `.pst`, `.psb`, `.lib`, `.loc`, `.cnc`, `.est`, `.tcp`, `.cdr`.  
- Key paths: `H:/prism/JM DIE/POST PROCESSORS/`, `state/shared/specs/`, `knowledge/wiki/`.  
- PSN nodes for post processors and system‑viz integration.

**TOOLS USED**  
- PRISM tooling: `camDispatcher.ts`, `mcp-server`, `scripts/post-processor-consolidate.mjs`, `PostLibraryEngine`, Vision‑to‑CAD engines.  
- PSN, system‑viz, Claude Vision API, ffmpeg, Node.js scripts.  
- Git hooks (`slot-enforce`, `add-lane-guard`), H8 misattribution handling.

**OPEN THREADS**  
1. Commit the 4 pending files after lock contention clears.  
2. Trace & fix Heidenhain/Mitsubishi quality asymmetry (regex, engine chain).  
3. Refine hypermill `.loc` classification; promote remaining posts to PRISM Enhanced.  
4. Build PPG page + employee portal frontend components.  
5. Create Mastercam / hyperMILL / ESPRIT PRISM‑enhanced templates.  
6. Finalize Master Post Processor integration (internal print‑to‑CNC).
