# post-processor session 321c1d3f (2026-06-03, 37.4MB, spine 386KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U‑PT‑KNOWLEDGE‑ENRICH` – 68/68 tests, live proof on okuma‑genos‑osp, okuma‑b250‑lathe, hurco‑v11‑standalone.  
- `U‑PT‑HAAS‑SPEC` – ground‑truth spec for full Haas master‑post engine (Hurco template).  
- `U‑PT‑HAAS‑ENGINE` – 27/27 tests, fixed inch‑scale P0 bug and feed/arc safety checks.  
- `U‑WINMAX‑LIVE‑PROBE‑FINDING` – live WinMax mill probe, captured button set, verified whereami resolution.  
- `U‑WINMAX‑BUTTON‑SIGNATURE` (commit a1c1efa31d) – 45/47 tests, added automationId tiebreak, DRAW_VERIFY screen with real buttons.  
- `U‑WINMAX‑NAV‑PATH‑CONTRACT` (commit dd1fcdb48c) – 68/68 +30/30 tests, fixed CLI contract, hardened mapCli.  
- `U‑PT‑HAAS‑CANNED‑CYCLES` (commit a1acfda90b) – 45/47 tests +3/3 corpus proof.  
- `U-CIMCO-WIKI-TRIBAL` – system‑wide CIMCO verification/simulation wiki +12 tribal tips.  
- `U-PATCHES-ALLOWLIST` – fleet‑wide leave‑a‑copy fix for patch‑consumption workflow.  
- `U-CIMCO-CONTROL-MAP` – catalog + .mcfg reader + sim‑report parser (19/19 tests).  
- `U-CIMCO-MACHINE-INDEX` – 86 machines indexed, 44 unit‑UNRESOLVED flagged (tests 4/4).  
- `U-CIMCO-POST-INDEX` – 25 JS posts & 44 controllers parsed into JSON (5/5 tests).  
- `U-CIMCO-PER-GALAXY-PLOTS` – dependency‑ordered build plan for 10 galaxies.  
- `U-CIMCO-TOOL-INDEX` – 366 cutters from 14 .tmlib libraries indexed (188 Metric / 178 Imperial).  
- `CimcoVerificationBridgeEngine.ts + dispatcher prism_cimco` – SPINE‑1 bridge engine, 22/22 tests, 3‑of‑3 scrutiny PASS.  
- `scripts/lib/nc-normalize.mjs` – pure NC normalizer (18/18 vitest pass).  
- `scripts/cimco-jm-machine-map.mjs → jm-fleet-sim-map.json` – 15 JM machines mapped to CIMCO sim machines with correct kinematics (9/9 tests, real‑data validation).  
- `297a3c9194` – Post‑proof readiness ledger (per‑machine golden corpus + sim machine + proof method + 246 content‑drift groups).  
- `430f735fff` – Workflow P0 fix, added Hurco .hnc coverage, fleet readiness report.  
- `d0e5df9e16` – `scripts/lib/nc-dialect-masks.mjs` with `roundTrip(golden,candidate)` classifier (byte‑identical / volatile‑header‑only / semantic‑drift).  
- `38da8cb412` – Drift audit refactor to use `roundTrip`; re‑classifies 246 drift groups → 247 semantic, 8 identical, 2 header‑only.  
- `7a14bf0def` – Fixed dialect masks (paren‑anchored patterns, fail‑closed unknown dialect).  

**DECISIONS**  
- Harness `--from‑knowledge` leg for operator travelers and playbook sequencing (condition 3).  
- Build full Haas master‑post engine from JM program; pre‑enumerate canned cycles to avoid half‑built engines.  
- Resolve field‑less graphics via button tiebreak, add DRAW_VERIFY screen; wrap CLI outputs in `{ok,keys}` envelopes; enforce single‑line JSON for whereami/navigate.  
- Adopt MCP dispatcher pattern (`prism_calc`, `prism_safety`, etc.) with `duplicationGuardEngine.mustCheckBeforeCreating()` and 3‑of‑3 scrutiny gate.  
- Enable YOLO mode: no questions, auto‑select highest priority, immediate execution, write directly, no implicit unit caps; auto‑fix protocol (MAX_ATTEMPTS = 3).  
- Reject speculative dialects (G86/G89/G76) until golden archive evidence; focus on byte‑matching real JM data.  
- Use CIMCO Machine Simulation as primary oracle; build JSON `.mcfg` reader and machine indexer; generate post inventory; defer heavy TS bridge engine until fresh tick.  
- Dedicated TS dispatcher `prism_cimco`; isolated NC normalizer module; map JM fleet machines to CIMCO sim via kinematic‑fit scorer; prioritize SPINE‑1.  
- 10‑agent workflow `cimco-post-proof-fleet`; build dialect masks offline; refactor drift audit with `roundTrip`; make unknown-dialect masks fail‑closed.

**OPERATOR DIRECTIVES**  
- Launch WinMax mill Hurco sim (completed); resume condition 1 live nav when ready.  
- Schedule recurring `/loop /yolo-mode` every 10 min; activate YOLO mode (no questions, auto‑select, immediate execution).  
- Next task: build `U‑PT‑HAAS‑CANNED‑CYCLES` and bridge engine `prism_cimco`.  
- Provide live‑app capture session for CIMCO UIA driver (SPINE‑2); confirm headless automation + 2026 REST API coverage.  
- Approve JM‑fleet post‑proof harness after bridge engine & machine map validation.  
- Use workflow `cimco-post-proof-fleet` with parallel agents; status via `status`.

**FINDINGS / BUGS**  
- Fixed Haas inch‑mode 25.4× scale bug; removed Fanuc‑only M29 before G84 for Haas.  
- Corrected G83 byte drift (removed XY, set G99); added NaN guard in approach block; fixed first‑hole coupling with divergence guard.  
- Excluded numeric‑only softkey IDs from `signatureOf`; added DRAW_VERIFY screen to resolve whereami ambiguity.  
- Fixed CLI contract: path now `{ok,keys}` envelope; hardened mapCli parsing; resolved CRLF churn via atomic commit.  
- SPINE‑1 simulation report parser fail‑open bug fixed; kinematic scorer weighted orientation & axis count; NC normalizer token assertion corrected.  
- CIMCOSimulation.exe lacks headless CLI → use UIA read; 44 machine defs unit‑UNRESOLVED flagged; Orientation enum corrected to “Mill”.  
- Server rate‑limiting blocks parallel agent dispatch; plan to re‑run leaner once throttle lifts.  
- AxisHints regex `tr\d` mis‑classifies VMC‑03 (Haas VF‑2TR); fixed.  
- 246 content‑drift groups identified; 247 semantic, 8 identical, 2 header‑only after `roundTrip` refactor.  
- Dialect masks greedy patterns fixed to paren‑anchor and fail‑closed; no remaining fail‑open issues.

**DOMAIN SPECIFICS**  
- Master‑post router (`master_post_by_machine`) dispatches per‑controller engines (Haas, Fanuc, Okuma, etc.).  
- CamDispatcher.ts orchestrates engine selection; post‑training harness (`trainPost`, `knowledgeEnrich`, playbook sequencing).  
- Prism dispatcher suite: `prism_calc`, `prism_safety`, `prism_cam`, `prism_cad`, `prism_turning`, `prism_ai`, `prism_dev`; actions include `emitCannedCycle`, `duplicationGuardEngine`, `scrutiny‑3way.mjs`.  
- CIMCO integration: `.mcfg` reader, machine indexer (`state/shared/cimco/machine-index.json`), simulation report parser (UIA read via `winmax-ui-map.mjs`), MariaDB DNC‑Max API access.  
- Dedicated TS dispatcher `prism_cimco`; bridge engine `CimcoVerificationBridgeEngine.ts`; NC normalizer module; JM‑fleet to CIMCO sim machine map (`jm-fleet-sim-map.json`).  
- Metrics: test pass counts (68/68, 45/47, 22/22), per‑galaxy plots, control‑map JSON, machine/post/tool indices.  
- Post‑proof readiness ledger, drift audit, `roundTrip` classifier, nc-dialect masks; workflow `cimco-post-proof-fleet` with 10 agents.

**TOOLS USED**  
- RTK CLI (`rtk git`, `rtk vitest run`, `rtk tsc`).  
- Ollama offload (`/ollama-*`; qwen2.5-coder:7b for summarization/error triage).  
- System‑viz queries (`system-viz-query.mjs`).  
- Obsidian 2nd‑brain searches (`/wiki-query`, `prism_memory:semantic_search`, `prism_knowledge:tribal_search`).  
- Master‑index awareness (`prism_session:master_index_query`).  
- MCP dispatchers (`prism_calc`, `prism_safety`, `prism_cam`, `prism_cad`, `prism_turning`, `prism_ai`, `prism_dev`, `prism_cimco`).  
- Duplication guard engine (`duplicationGuardEngine.mustCheckBeforeCreating()`).  
- Scrutiny framework (two‑reviewer 3‑of‑3 gate via `scrutiny‑3way.mjs`).  
- CronCreate for recurring `/loop`.  
- Loop state and handoff files (`loop-state.mjs`, `state/shared/handoffs/...`).

**OPEN THREADS**  
- Build & test `U‑PT‑HAAS‑CANNED‑CYCLES` and `prism_cimco` bridge engine.  
- Resume WinMax sim to finish condition 1 live navigation; monitor shared‑tree lock contention.  
- Complete CIMCO↔PRISM bridge TS build, dispatcher wiring, machine index & post inventory integration.  
- Finish SPINE‑2 UIA driver (CIMCO sim cycle‑time) and live testing of JM‑fleet posts.  
- Implement per‑galaxy plots for remaining slots; finalize post‑proof harness.  
- Run full dialect‑extraction workflow with 10 agents once reset; verify trunnion simulation, final proof against cleaned golden set.  
- Confirm all fail‑open issues resolved in dialect masks and drift audit; ensure 3‑of‑3 scrutiny passes.
