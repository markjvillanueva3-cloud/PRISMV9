# post-processor session 69c2d78c (2026-05-28, 8.2MB, spine 23KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Fusion 360 PRISM HTTP API Server (commits `4a1f0b0a0a`, `206c9e1783`, `80524f0e2f`) – live in `CAD‑FUSION‑LIVE‑MS0`.  
- Heidenhain/Mitsubishi dialect‑aware scoring fix shipped at iter 17 (doc‑comment 1216‑1218).  

**DECISIONS**  
- Adopt `/checkin‑echo` as slot‑locked wrapper; force‑take `echo`, bind to `echo-work`, then delegate full `/checkin` pipeline.  
- Use chat‑slot reclaim/claim via `chat-slots.mjs`; prefer `--force true --confirmRecent true`.  
- Prioritize Heidenhain/Mitsubishi asymmetry trace (P0) over other queue items; comprehensive path: diagnose, patch regex sites, add regression test, re‑validate.  
- Rebuild dist after fixing build blockers (`await‑outside‑async`, `file://` imports); use `npm run build:incremental`.  

**OPERATOR DIRECTIVES**  
- “continue” – resume Heidenhain/Mitsubishi asymmetry trace.  
- “always do the most comprehensive path forward”.  
- “do another sweep for 4 parallel agents of dormant features…”.  
- “check sessions from 5/27/2026 and previous night to regain context and tasks in queue”.  

**FINDINGS / BUGS**  
- Stale `dist/` caused Heidenhain/Mitsubishi score drop (0/40 at 50‑55); rebuild fixed.  
- Three literal‑regex sites (`detectedOps.push("hsm")`, `optimizeGCode` safe‑start, `generateImprovementSuggestions`) produce false warnings for non‑Fanuc dialects.  
- Validator routing mis‑labels controller=fanuc for all scenarios; actual emit path uses Heidenhain signals but missing `M120`.  
- Build blockers: `await‑outside‑async`, four `file://` dynamic imports.  
- ACL lock on wedged file (`wedm-pcd.md`) triggers C‑drive gate via `hasHCounterpart()` misclassifying EPERM as “missing”.  

**DOMAIN SPECIFICS**  
- `MasterPostProcessorUnifiedAGIEngine.generatePost` – core emit path; `signals.*` overrides per dialect.  
- Validator spec (`state/shared/specs/POST-PROCESSOR-PROVE-OUT‑2026‑05‑26.md`).  
- Loop state file: `state/shared/loop-state/loop-9029a5d7-d438-42fc-b03d-2652b80994fc.json`.  
- Dormant feature inventory: `PRISM_UNIVERSAL_POST_GENERATOR_V2.js`, Okuma OSP control engines, Kalman controller, AI scoring, backplot engine.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `audit-roadmap-drift.mjs`.  
- Pipeline: `/checkin.md` (steps 3‑14).  
- Build: esbuild (`npm run build:fast`), tsc (`npm run build:incremental`).  
- Testing: vitest.  
- Scripts/commands: `/checkin‑echo`, `hasHCounterpart()`.  

**OPEN THREADS**  
- Complete Heidenhain/Mitsubishi asymmetry trace → confirm root cause (A/B/C) and patch.  
- Add regression test for Heidenhain score ≥ 75; re‑run validator.  
- Port dormant features in priority order (Universal Post Generator, Okuma OSP control, Kalman controller).  
- Resolve C‑drive gate by resetting ACL on `wedm-pcd.md`.  
- Continue `/loop` directive: “close Heidenhain/Mitsubishi enhancement asymmetry”.
