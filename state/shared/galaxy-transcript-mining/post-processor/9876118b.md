# post-processor session 9876118b (2026-05-18, 48.5MB, spine 260KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `ac9cca8902` – enum‑blind `$p.Name` sanitization + 3 regression tests.  
- `da01003b61` – FLEET‑TASK‑HEALTH‑MS0 (watchdog + nudge).  
- `31cc2c84d3` – FLEET‑TASK‑HEALTH‑MS0 fix‑up (C0‑strip, hardening, tests).  
- `d3db37cc2c` – FLEET‑REAPER‑MS1 per‑PID `Stop‑Process` logic (batch‑kill bug fix).  
- `bb7d30c7cc` – slotLabel:null deep‑fix (unblocks Build B).  
- `85703afab6` – U‑CHO01 (clear/compact decision module).  
- Commit for U‑CHO02 (context‑fill monitor) – 16 new tests, 45/45 PASS.  
- `cc14791cd7` – OLLAMA‑KEEP‑ALIVE (`keep_alive="-1"`).  
- `b41b829232` – SOFT‑RELIEF‑V2 (alive RSS > 100 MB).  
- `9cfc411eb3` – OPT‑2 (critical threshold 95 → 88 %).  
- U‑FR‑CRASH‑WATCH – crash‑watch detection, snapshot persistence.  
- U‑CRIT‑NUDGE‑PATHFIX – fixed chat‑slots path for soft‑relief nudge.  
- `nim-docker-launcher.mjs` – Docker/NGC‑key launcher for NIM (16/16 tests).  
- `[NIM‑ACTIVATION‑MS0]/SPEC` – migration spec to rewrite ~12 offload hooks to `local‑llm‑bridge`.  

**DECISIONS**  
- Fleet‑reaper always on; golf slot owns it, alpha only runs `/fleet‑reaper --uninstall`.  
- Lightweight tail monitor with ≥95 % pressure filter (idempotent via `/fleet‑reaper`).  
- Commit enum‑blind fix and per‑PID kill immediately; defer slotLabel:null deep‑fix until memory pressure subsides.  
- Batch‑kill bug routed to peer (`claude‑23c10eea`); no duplicate work.  
- Memory‑pressure tuning: critical threshold lowered to 88 %; soft‑relief V2 activates on alive RSS > 100 MB; OLLAMA keep_alive set to “‑1”.  
- Crash detection uses `lastHeartbeat` freeze, snapshots crash state, advises `/clear` or `/compact`.  
- Pagefile policy switched to system‑managed auto‑grow with memory compression & page combining (requires reboot).  
- NIM integration: 3‑backend router (`local‑llm‑bridge` → Ollama fallback), provision NIM via `nim-docker-launcher.mjs`, migrate offload hooks one by one.  

**OPERATOR DIRECTIVES**  
1. Reboot after applying pagefile changes to raise commit ceiling.  
2. Start Docker Desktop, set `NGC_API_KEY`, run `node nim-docker-launcher.mjs`.  
3. If using golf for work (not hygiene), export `PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1` or disable legacy allowlist hook globally.  
4. Run `/compact` after handoff is written.  
5. If monitor dies, run `/fleet‑reaper` to re‑arm it.  
6. Monitor `PRISM_FLEET_REAPER_DISABLE=1` flag for temporary pauses.  
7. Keep an eye on Windows tasks and system performance; ensure fleet‑reaper scheduled task is active.  

**FINDINGS/BUGS**  
- `process-slot-map.mjs` `$p.Name` unsanitized → enum‑blind at high pressure (fixed).  
- Batch‑kill bug: PowerShell `Stop‑Process` batch timed out under critical pressure, leaving orphaned PIDs alive (fixed in TIER‑1/2 hardening).  
- `slotLabel:null` in fleet‑memory‑monitor prevented Build B nudge; deferred deep fix.  
- Commit memory oscillated 90–99 %; no orphans to reap at critical pressure – only `/compact` can relieve live chats.  
- Memory pressure remained 98–99 % during peak; reaper unable to kill due to batch bug.  
- Crashes caused by Windows commit‑limit exhaustion (pagefile fixed but inactive until reboot).  
- NIM server never provisioned; router orphaned; all offload hooks use inline Ollama clients.  
- Soft‑relief dead lever because stale slots reclaimed before reaper could act.  
- Docker daemon down blocks NIM provisioning.  

**DOMAIN SPECIFICS**  
- **Engines/Actions**: fleet‑reaper, fleet‑memory‑monitor, chat‑slots manager, process‑slot‑map, precompact, OLLAMA offload stack (`ollama-auto-router`, `ollama-unified-semantic-router`), local‑llm‑bridge, nim‑docker‑launcher.  
- **Metrics/Paths**: `mem.usedPct` (90–99 % → 88 % threshold), `softRelief.targets`, `gpu.freeMb`, `ollama.reachable`, `coord.commit`, `state/shared/fleet-memory-history.jsonl`, `state/shared/orchestrator-directives.json`.  
- **Dispatchers**: chat‑slots claim/reclaim, fleet‑reaper sweep, orchestrator advisory inject, U‑CHO01–06, U‑FR‑CRASH‑WATCH.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `stable-session-id.mjs`.  
- Scripts: `fleet-task-health-watch.mjs`, `critical-memory-compact-nudge.mjs`, `nim-docker-launcher.mjs`, `install-fleet-reaper-task.ps1`.  
- Hooks: `.claude/hooks/fleet-task-health-stop.mjs` (Stop), `.claude/hooks/critical-memory-compact-nudge.mjs` (UserPromptSubmit).  
- Helpers: `per-agent-handoff.mjs`, `precompact-pending-guard.mjs`, `milestone-tracker.mjs`.  
- Test framework: Node’s built‑in `node:test`.  

**OPEN THREADS**  
1. SlotLabel:null deep‑fix in fleet‑memory‑monitor & process‑slot‑map (peer‑claim coordination).  
2. NIM migration: rewrite ~12 offload hooks to use `local‑llm‑bridge` (`U‑NIM‑MIGRATE‑01…N`).  
3. Activate pagefile changes via reboot; verify commit ceiling increase.  
4. Start Docker Desktop, set `NGC_API_KEY`, run nim launcher to bring up NIM service.  
5. Further validate TIER‑1/TIER‑2 hardening under sustained pressure.  
6. Optional tuning of soft‑relief thresholds or additional crash‑watch heuristics post‑reboot and NIM live.
