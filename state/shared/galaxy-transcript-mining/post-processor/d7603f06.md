# post-processor session d7603f06 (2026-05-23, 44.2MB, spine 120KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U-BRIDGE-WIRE-TRIBAL – wired 3 tribal engines into `prism_shop_practice` (10 new actions).  
- U-CAMAGI12 – TribalKnowledgeApplicatorEngine (score‑based synthesis, 51 tests).  
- U-PB-EXPAND-CAPABILITIES – added `explainRule`, `coverageReport`, `quantitativeGuidance`.  
- U-PB-INTEGRITY-AUDIT – `auditIntegrity()` & `playbook_audit` action (28 tests).  
- U-PB-CONFLICT-DETECT – `detectConflicts()`, 5 dispatcher actions, 48 tests.  
- U-PB-CONFLICT-DETECT-CONDITIONS-ALL – folded `conditions_all` into discrete set; 11 new tests.  
- U-PB-CONFLICT-DETECT-NUL-FIX – removed stray NUL byte from conflict test file.  
- U-PB-CONFLICT-RANK – `rankConflicts()` with severity/evidence scoring (29 tests).  
- /playbook skill – 12‑action CLI, propagated fleet‑wide via c‑to‑h mirror.  
- Iter 9 – U‑PB‑SUGGEST‑RESOLUTION  
  - `6bd789d40d` – engine/dispatcher/schema + 2 test files (39/39 tests).  
  - `3de1e7a82e` – P1 fixes: scale‑collision note, `asBoundedString`, stale JSDoc.  
  - `60009927bc` – docs: wiki entry, RECENT‑SHIPMENTS, MEMORY index.  
- Iter 10 – U‑PB‑RELATED‑GRAPH  
  - `fa2ccacafe` – engine (BFS, cycle guard, unresolvedRefs, truncated flag), dispatcher action, schema, tests (35/35).  
  - `e97e33d9eb` – docs: wiki entry, RECENT‑SHIPMENTS, MEMORY index.  
- Iter 11 – playbook_validate_corpus  
  - `4f9e0845c2` – corpus audit harness, deep‑chain regression test, P1 fixes applied.  
- U-PB-WIKI-TRIO – architecture‑level wiki entry for playbook trio.  
- RECENT‑SHIPMENTS-2026-05-22.md inbox append (queued for CLAUDE.md drain).  

**DECISIONS**  
- Adopt single dispatcher surface `prism_shop_practice` with 5‑surface pattern: ACTION enum → handler map → Zod schema → ACTION_SHOP_PRACTICE_SCHEMAS.  
- Implement four‑step conflict workflow: detect → rank → resolve → related‑graph; all layers enforce R12 fail‑loud for stale IDs.  
- New engine code resides in `mcp-server/src/engines/MachiningPlaybookEngine.ts`; physics constants from `src/physics/constants.ts`.  
- Enforce COMPREHENSIVE‑BUILD: each action ≥3 failure modes, ≥2 adversarial tests; round‑trip E2E dispatcher test.  
- Use H7 BM25 sidecar; keep `MEMORY.md` <22 KB and index via `scripts/build-memory-index-sidecar.mjs`.  
- Introduce per‑file scrutiny gate (2 parallel reviewers, 2‑of‑2 strict stop gate).  
- Add `duplicationGuardEngine.mustCheckBeforeCreating()` to avoid duplicate assets.  
- BFS with cycle guard in `relatedGraph()`: expose `unresolvedRefs` array, `cycleEdges` list, `truncated` flag when depth limit exceeded.  

**OPERATOR DIRECTIVES**  
- Permission granted to modify memory & wiki; no public H:/prism output.  
- `/playbook` skill propagated fleet‑wide: old user‑global file renamed, new 186‑line version installed.  
- Loop continues with next high‑ROI foxtrot unit; status checks requested but no new work orders issued.  

**FINDINGS/BUGS**  
- `UNWIRED_ENGINES_MANIFEST.json` false positives – always grep dispatchers first.  
- Scrutiny‑3way captured wrong diff – re‑dispatch correct SHA.  
- NUL byte removed from conflict test file; prevented hidden binary classification.  
- `scrutiny-3way.mjs` replaced child_process.exec on RegExp with String.matchAll.  
- `describeOverlap()` now folds `conditions_all` into discrete set.  
- CoverageReport bug: guarded against undefined `query.tolerance_mm` (fixed iter 11).  
- Duplicate rule detection prevented by `duplicationGuardEngine.mustCheckBeforeCreating()`.  
- Schema consistency enforced between `playbook_related_graph` and `playbook_suggest_resolution` via stricter Zod shape.  
- P1 fixes applied: JSDoc accuracy, malformed‑input handling, stack‑overflow risk in deep DFS.  

**DOMAIN SPECIFICS**  
- Engines: `MachiningPlaybookEngine` (conflict workflow + relatedGraph() BFS), `TribalKnowledgeApplicatorEngine`.  
- Actions in `prism_shop_practice`: 12 playbook actions (`advise`, `lookup`, `add_rule`, `sequence`, `setup`, `antipatterns`, `explain`, `coverage`, `quantitative`, `audit`, `conflicts`, `conflicts_ranked`).  
- Dispatcher: `shopPracticeDispatcher.ts`; ACTIONS enum → handler map → Zod schema → ACTION_SHOP_PRACTICE_SCHEMAS.  
- Metrics: each action ≥3 failure modes + 2 adversarial tests; round‑trip E2E dispatcher test; conflict workflow includes detect, rank, resolve, related‑graph.  
- Paths: `mcp-server/src/engines/MachiningPlaybookEngine.ts`, `shopPracticeDispatcher.ts`, `ACTION_SHOP_PRACTICE_SCHEMAS` map, `__tests__/`.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `CronCreate`, `c-to-h-mirror`; per‑file scrutiny agents (2 parallel reviewers), 2‑of‑2 strict stop gate.  
- Testing: Vitest with Zod v4 (.describe(), strict shape), `tsc --noEmit`; utilities `asBoundedString`, `RULE_ID_MAX_LEN`.  
- Scrutiny: `.claude/scripts/scrutiny-3way.mjs`; `duplicationGuardEngine.mustCheckBeforeCreating()`.  
- Memory index: `scripts/build-memory-index-sidecar.mjs` (H7 BM25); `MEMORY.md` <22 KB.  
- Wiki & inbox: `knowledge/wiki/`, `RECENT‑SHIPMENTS-<date>.md`, CLAUDE.md hooks.  
- Loop automation: cron every 5 min.  

**OPEN THREADS**  
- Implement `suggestResolution()` / `suggestResolutions()` for conflict resolution (pending).  
- Expand coverage blind‑spots: add machining‑knowhow rules where `coverageReport` shows zero applicability.  
- Build tribal knowledge corpus: units `muS-D83..D85`, `U-GAP-TRIBAL-KNOWLEDGE-GRAPH`.  
- Finalize doc‑reflection: update CLAUDE.md once RECENT‑SHIPMENTS inbox drains.  
- `/playbook` CLI skill shipped and fleet‑wide propagated; no further action needed.
