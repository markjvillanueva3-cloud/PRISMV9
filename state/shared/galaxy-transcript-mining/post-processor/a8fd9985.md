# post-processor session a8fd9985 (2026-05-22, 28MB, spine 256KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `4d4a4900e7` – CLAUDE.md doctrine shift: golf → full work slot, removed refuse_list.  
- `9057df86f6` – golf soul rewritten to role=work.  
- `81ed0d959c` – merged cad‑fusion‑live‑ms0 into slot/golf (1192‑commit sync).  
- Guard fix (`claude-md-golf-only-guard.mjs`) identity bug resolved, 429 insertions.  
- U‑FR‑STUCK‑HUNT: 3 commits, 33/33 tests PASS; Docker & Qdrant containers restored; MCP connectivity re‑established.  
- System memory recovered from ~95 % to ~70 %.  
- Commit `U-CAD-DIST-DATA-COPY`: copies `cad-token-vocabulary.json` from src to dist.  
- False‑proof commit retained for audit; retraction + `feedback_api_success_is_not_proof.md`.

**DECISIONS**  
- Golf slot now a full work slot; fleet‑reaper ownership moved from alpha → golf.  
- Slot‑worktree model enforced: golf operates in `H:/prism-slot-golf`.  
- Wire `CADToSTEPPipelineEngine` into `cadDispatcher.ts`; use mock mode (`PRISM_CAD_MOCK=1`) for CI tests.  
- Adopt “print→CAD→new‑print + dimensional comparison table” as sole proof standard; API success ≠ proof.  
- Prioritize small JM Die roundtrip before blisk proof.  
- Upgrade fleet‑reaper with hunters for stuck bash, fsmonitor orphans, stale slot PIDs.  
- Capture open CAD/blisk goal in handoff; do not fake proof completion.

**OPERATOR DIRECTIVES (verbatim)**  
- “please make sure all fleet-hygiene monitors and memory pressure helpers are active.”  
- “Goal set: [ prioritize training cad … | prove you can draw a blisk in hypercad ] /loop [5m] /goal.”  
- “that blisk was completely false” + “the only real test is to produce a part from print, generate the CAD then generate a new‑print and compare dimensions to the original print.”  
- “can you clear any orphan nodes, bash and git tasks left over?”  
- “upgrade fleet reaper to hunt for stuck shells and everything else you just found”.

**FINDINGS/BUGS**  
- `CADToSTEPPipelineEngine` existed but was unwired; wiring edits made in `cadDispatcher.ts`.  
- No CAD→drawing emission action present – identified blocker.  
- Guard `claude-md-golf-only-guard.mjs` had identity bug (fixed).  
- Memory pressure (~95 %) caused Docker daemon crash, Qdrant unhealthy → MCP disconnects; restored after WSL2 EXT4 corruption fix.  
- Stuck bash (>5 min), git fsmonitor orphans, stale slot PIDs present; fleet‑reaper did not kill crashed slots due to stale JSON records.  
- Build script data‑asset copy issue resolved (`cad-token-vocabulary.json` now in `dist/data`).  
- Open bugs:  
  - U‑BLISK‑RECOMMEND‑BLADES‑NAN – NaNs in blade recommendation engine.  
  - U‑BLISK‑GENERATE‑SPEC‑SCHEMA‑DOC – missing schema docs for blisk generation.  
  - U‑NACA‑PARSER‑6SERIES – parser accepts only 4/5‑digit NACA, not 65‑series.  
  - U‑PART‑TEMPLATE‑CYLINDER‑DEGENERATE – cylinder template returns null radius/volume.

**DOMAIN SPECIFICS**  
- **Engines / Actions**: `CADToSTEPPipelineEngine`, `DimensionalSignatureEngine`, `cad_dimensional_signature`, `cad_feature_tree_validate/extract`, `cad_pdf_blueprint_extract`, `cad_neural_generate`, `part_template_cylinder`, `cam_hyperCADS_analyze`, `GroundTruthFeatureTreeExtractor`.  
- **Dispatchers**: `cadDispatcher.ts` (actions list, includes `cad_to_step_run/batch/extensions`), extraction (`cad_pdf_blueprint_extract`), generation (`cad_blueprint_generate`).  
- **Metrics / Paths**: PRISM_FLEET_REAPER_* env knobs, host presets (`home`, `work`), scheduled tasks list. Slot‑worktree: `H:/prism-slot-golf`. Unique paths: `H:/prism/mcp-server/src/tools/dispatchers/cadDispatcher.ts`, `scripts/lib/fleet-reaper-stuck-hunters.mjs`.

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `per-agent-handoff.mjs`, `precompact-pending-guard.mjs`, `stable-session-id.mjs`, `milestone-tracker.mjs`.  
- Dispatchers: `cadDispatcher.ts`.  
- Scripts/skills: `fleet-reaper-stuck-hunters.mjs`, `slot-bind-enforce.mjs`.  
- Hooks: `slot-bind-enforce.mjs`, `fleet-reaper-sweep.mjs`.  
- CI utilities: `node:test`, `vitest`, `node:assert/strict`.

**OPEN THREADS**  
1. Commit and fully test wiring of `CADToSTEPPipelineEngine` into `cadDispatcher.ts`.  
2. Create/run `mcp-server/src/__tests__/dispatchers/cadDispatcher.cad-to-step.test.ts`; run vitest with mock mode (`PRISM_CAD_MOCK=1`).  
3. Wire or create CAD→drawing emission action (likely via `DimensionalSignatureEngine`); enable STEP export → 2‑D blueprint extraction.  
4. Execute print→CAD roundtrip on small JM Die: `cad_pdf_blueprint_extract` → `cad_to_step_run` → `cad_dimensional_signature`; generate dimensional comparison table per `feedback_api_success_is_not_proof.md`.  
5. Verify build script copies all data assets; re‑run `npm run build:*` after commit 5.  
6. Restart MCP server to load updated vocab (`cad-token-vocabulary.json`).  
7. Document new workflow in memory file and wiki entry `reference_u_wire_cad_to_step_2026_05_21.md`.  
8. Finalize CAD training capabilities, wiki entries, AI system integration.  
9. Ensure memory pressure remains below 80 % after planned 64 GB RAM upgrade.  
10. Once proof passes, update CLAUDE.md / wiki and close goal.
