# corpus-aggregation session e6d53366 (2026-06-08, 2.6MB, spine 28KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
— No new builds/commits were made in this session (read‑only).

**DECISIONS**  
- *Scope*: Test zulu Hermes agent end‑to‑end by running a live sweep on a single slot (`romeo`) to validate decision logic, gating and SendKeys delivery.  
- *Safety*: Opt‑in slots have a 24 h dry‑run grace; the scheduled task sets `PRISM_ZULU_DRY_RUN=1` to keep all actuation off by default.  
- *Topology constraint*: Fleet runs as tabs in one Windows Terminal window → UIA cannot target non‑focused tabs, blocking real keystrokes.

**OPERATOR DIRECTIVES** (verbatim)  
- “do it” – trigger a live actuation test on a chosen slot.  

**FINDINGS/BUGS**  
1. **P2 Capability‑Map dupe** – `zulu` appears twice in `state/shared/zulu-awareness-index.json`; 27 fingerprints reported for 26 slots.  
2. **UIA no‑tab error** – live sweep on `romeo` reached the SendKeys step but failed with `uia:no-tab`. The tabbed WT topology prevents keystrokes to background tabs.  
3. **Stale heartbeat** – `juliett` shows a 22 min stale ping; likely mid‑long task, not dead.  
4. **Zero opt‑in visible in raw chat‑slots.json** – projection from `state/shared/zulu-opt-in.json` is required for live actuation.

**DOMAIN SPECIFICS**  
- *Orchestrator*: zulu slot (self‑exempt), sweeps every 5 min, reads slot pressure & awareness.  
- *Agent*: Hermes orchestrator (`scripts/lib/zulu-orchestrator-lib.mjs`) – functions `pickActionableSlots`, `planSlotAction`, `decideExecutionGate`, etc.  
- *Opt‑in store*: `state/shared/zulu-opt-in.json`; 24 h grace window after `optInAt`.  
- *SendKeys path*: `/precompact → /compact → /checkin-<slot>` dispatched via UIA to the terminal tab with caption `PRISM <slot>`.  
- *Self‑exempt slots*: `zulu`, `golf` never receive actuation.  

**TOOLS USED**  
- `chat-slots.mjs` (claim/reclaim helpers)  
- `zulu-orchestrator-lib.mjs` (decision core)  
- `zulu-opt-in.mjs` (opt‑in CLI)  
- `/checkin` pipeline (`H:/prism/.claude/commands/checkin.md`)  
- PowerShell cmdlets: `Get-Content`, `Get-ScheduledTask`, `Get-ScheduledTaskInfo`  
- Windows UI Automation (SendKeys, tab selection)

**OPEN THREADS**  
1. Surface the P2 dupe to `juliett`/`india`.  
2. Run a live actuation on a focused slot (e.g., bring a target tab into focus first) to prove keystroke delivery.  
3. Leave the agent green; no further action needed unless above threads are pursued.
