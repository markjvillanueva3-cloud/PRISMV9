# corpus-aggregation session 86373eb3 (2026-06-09, 30.8MB, spine 356KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Created `Launch‑PRISM‑Fleet‑3win.ps1` (3‑window per‑account launcher).  
- Updated shortcuts: `PRISM Fleet (24‑tab).lnk → 3‑window`; kept `PRISM Fleet.lnk` as 4‑window fallback.  
- Modified `settings.json`: `LOCAL_LLMBACKEND=ollama`, NIM URLs inert, comment added.  
- Guard in `nim‑autostart.mjs` exits when backend is `ollama`.  
- Removed `ollama` & `ollama‑nomic‑preload` from compose services (`docker‑intel‑autostart.mjs`).  
- Edited `ollama‑docker‑launcher.mjs`: dropped `ollama` from `DEFAULT_SERVICES`, fail‑fast guard for `--services=mcp`.  
- Updated `docker-compose.yml`: removed `version:`, set `KEEP_ALIVE=30m`, `MAX_LOADED_MODELS=4`.  
- Cleared stale `DOCKER_RUNTIME_STATE.json`; stopped all NIM containers (`restart=no`).  
- Reboot‑hardened native Ollama on :11434; SessionStart now only qdrant.  
- Updated 4‑window PRISM Fleet launcher to 19‑slot layout, raised resume max MB to 200.  
- Added durable cron‑runner installers for tribal tasks (fixed TEMP reaping).  
- Built `install‑synthesis‑crons.ps1` installer for weekly‑memory‑synthesis, run‑daily‑context, run‑knowledge‑distillation.  
- Implemented heap guard in `master-index-search-lib.mjs`; added regression tests.  
- Wired reverse H→C Obsidian mirror hook into `settings.json PostToolUse`.  
- Built GNN feeder (`vault-to-gnn-refpool.mjs`): +4 ghosts (636→640), refs 125→129.  
- Built LoRA feeder (`vault-to-lora-dataset.mjs`): 245 Alpaca triples.  
- Fixed `seed‑ghost-from-unwired.mjs`: streaming I/O, added `writeGraphStreamingAtomic`.  
- Added regression tests for CRLF removal, `.md` strip, non‑newline frontmatter regex.  
- Committed cache‑based transcript discovery (`discoverByContent`) and null‑deref guard.  
- Set `MAX_SLICES_PER_TX` cap; increased `SYNTH_TIMEOUT_MS` to 900 s.  
- Enabled `--next-unpopulated --backend-only` mode for ROI‑driven mining.  
- Completed agent‑orchestration task (14/34 galaxies); final backend batch launched, halted on dormant‑data due to timeout; dormant‑data mine re‑launched.

**DECISIONS**  
- Adopt 3‑window per‑account fleet launcher (accounts 1–3), keep 4‑window fallback.  
- Switch local LLM backend to Ollama; permanently disable NIM usage.  
- Eliminate compose‑Ollama service to avoid :11434 collision with native Ollama.  
- Ensure native Ollama auto‑starts via scheduled task `PRISM Ollama Serve`.  
- Guard against misused `--services=mcp` in launcher.  
- Keep recall hooks off by default; rely on CAG router for token gating; omit `--with‑viz` flag from brain‑refresh.  
- Leave redundant RAG hooks dead; use durable path (`.claude/cron-runners/`) for cron runners.  
- Do not create wrapper for `find-connections`; treat as per‑target tool.  
- Generalize India miner to all 34 galaxies via registry; keep single pipeline.  
- Do not auto‑force Ollama routing fleet‑wide; raise adoption of route‑suggests, make `node_card` default for system‑viz reads.  
- Use streaming I/O for graph writes.  
- Switch discovery from handoff‑based to content classification; mine backend galaxies first (ROI ordering).  
- Per‑galaxy foreground mining with `--limit 6`, `--max-slices 14`; avoid reaper kills.  
- Add null‑deref guard in cache lookup.  
- Adopt one galaxy per task strategy after multi‑batch failures.

**OPERATOR DIRECTIVES**  
- Capture & snapshot Claude Max accounts 2–6 (`claude login` + `node capture‑claude‑credentials.mjs`).  
- After capturing, launch new 3‑window fleet via updated shortcut; verify on reboot native Ollama starts automatically and no port conflicts.  
- Run elevated PowerShell block to register the 6 scheduled tasks (tribal & synthesis crons); confirm success.  
- Confirm `find-connections` is not a cron runner.  
- Commit all scoped files (registry, miner, feeders, tests).  
- Decide on enabling `node_card` as auto‑default; tighten route‑suggest take‑rate.  
- Register missing scheduled tasks if required.  
- Continue from where left off: resume mining for remaining backend galaxies and dormant‑data synthesis.

**FINDINGS/BUGS**  
- NIM containers OOM exit 137 due to 16 GB WSL cap; stopped with `restart=no`.  
- Dead‑end penalty from probing dead NIM removed by setting backend to Ollama.  
- Port collision on :11434 between native and compose Ollama resolved.  
- Misnamed `mcp` service in autostart launcher fixed.  
- Stale `DOCKER_RUNTIME_STATE.json` cleared.  
- Qdrant collections 768‑dim match Ollama nomic‑embed output – no reindex needed.  
- Master‑index hook caused heap OOM; fixed with sidecar size guard & regression tests.  
- Temp‑dir cron runners broken by TEMP reaping; fixed durable path.  
- Missing synthesis crons addressed via installer.  
- Brain‑refresh task missing `--with‑viz` flag would regress heavy regen; left as is.  
- `find-connections` requires target slug; not a batch cron runner.  
- `seed‑ghost-from-unwired.mjs` crashed ERR_STRING_TOO_LONG; fixed with streaming I/O.  
- CRLF leaks normalized; `.md` extension stripped; frontmatter regex corrected.  
- 1,355 handoffs unclassified due to strict regex; expanded `classifyHandoff`.  
- Fleet‑reaper kills long detached `--all` runs; mining must be foreground or reaper‑protected.  
- GPU contention stalls `nomic‑embed-text`; embeddings sidecar rebuild required after mining.  
- Large transcripts (~48 MB) cause long spine extraction; mitigated by `MAX_SLICES_PER_TX`.  
- Null‑deref risk in cache lookup addressed.  
- Batch with `--next-count 5` hit timeout on dormant‑data (288 MB) at transcript `db273e77`; synthesis incomplete.

**DOMAIN SPECIFICS**  
- Scripts: `Launch‑PRISM‑Fleet‑3win.ps1`, `slot-tab‑boot.ps1`, `docker‑intel‑autostart.mjs`, `ollama‑docker‑launcher.mjs`, `local‑llm‑bridge.mjs`, `nim‑autostart.mjs`.  
- Engines: Qdrant vector store, Ollama local backend; NIM disabled.  
- Dispatchers/Actions: `node_card`, `ollama-prism-bridge`, `rag-read-offload-advisory`, `cag-router-inject`, `galaxy-synthesis-refresh`, `connections_materialize`, `run-daily-context`, `weekly-memory-synthesis`, `run-knowledge-distillation`.  
- Metrics: reference pool 125→129 refs; ghosts 636→640; high‑conf refs 125→129; `MAX_SLICES_PER_TX` cap; `SYNTH_TIMEOUT_MS` 900 s; 2,097 handoffs processed; 33/34 galaxies mineable; 245 Alpaca triples; 4 new ghosts.  
- Key paths: `system-graph.json`, `knowledge/memories/*.md`, `wiki/*.md`, `scripts/vault-to-lora-dataset.mjs`, `scripts/vault-to-gnn-refpool.mjs`, `scripts/seed-ghost-from-unwired.mjs`, `scripts/lib/graph-io.mjs`, `discoverByContent` cache file, vault directory.

**TOOLS USED**  
- Ultracode workflow; PRISM internal modules (`local‑llm‑bridge.mjs`, `nim-autostart.mjs`, `master-index-search-lib.mjs`).  
- PowerShell (`pwsh.exe`) for launchers & task registration.  
- Docker Desktop (WSL2) for container management.  
- Windows Scheduled Tasks for native Ollama auto‑start and cron runners.  
- PRISM stop-hook scripts; memory→wiki advisories.  
- `mine-galaxy-transcripts.mjs`, `discoverByContent`, `classifyTranscriptContent`, `build-memory-index-sidecar.mjs`, `build-memory-embeddings-sidecar.mjs --resume`.  
- ObsidianVaultSyncEngine, ObsidianRestBridgeEngine, etc.

**OPEN THREADS**  
- Capture Claude Max accounts 2–6 pending before launcher can run fully.  
- Auto‑switch at 90% not implemented; would need `rate_limits.five_hour` signal or custom hook.  
- Monitor future port conflicts on :11434.  
- Remaining backend galaxies to mine: knowledge‑conversion, corpus‑aggregation, database‑expansion (and any others).  
- Full population of all 34 galaxies still pending; requires orchestrated foreground runs or reaper‑protected background jobs.  
- Embeddings sidecar rebuild after mining.  
- Verify each mined memo compounding into galaxy’s `MEMORY.md`.  
- Confirm scheduled tasks registration success.  
- Decide on wrapper for `find-connections` or use `connections_materialize`.  
- Monitor timeouts on large light galaxies; adjust per‑task timeout or split further.
