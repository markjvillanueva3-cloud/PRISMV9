# corpus-aggregation session 16c4c64a (2026-06-04, 22.6MB, spine 128KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `c0ce903f35` – U‑CGP‑CONCURRENCY: async worker pool for OCR extraction (profile‑driven).  
- `8fd4193762` – U‑CGP‑MEASURE: real‑data E2E rate 0.189 pages/min/worker → 2× on current host.  
- `ca4395947b` – U‑CGP‑NUMPARALLEL‑RECO: OLLAMA_NUM_PARALLEL advisory (golf config).  
- `9b5aa4c2b6` – ShopOutcomeIngestProcessorEngine → prism_dev:shop_outcome_ingest (round‑trip test).  
- `267718a1ee` – GCodeMaterialParserEngine → prism_dev:gcode_material_parse.  
- `b902ac2024` – Reachability classifier tool (110 candidates → 49 GENUINE / 54 FALSE) – scripts/classify-engine-reachability.mjs.  
- `6917ccaf3e` – CircularDependencyEngine → prism_dev:dependency_cycle_analyze (Tarjan SCC).  
- `7ec7c17249` – OperatorPreferencesEngine → prism_session (set/get/apply).  
- `b240aa83c0` – PSNHealthCheckEngine → prism_dev:psn_health_check (11‑leg classifier).  
- `6620095eaf` – MeasurementSystemAnalysisEngine → prism_dev:msa_analyze (Gage R&R).  
- `a3ab2bc96c` – SimulationStallDetectorEngine → prism_dev (stall watchdog actions).  
- `?` – SlotSessionHistoryEngine → prism_session readAll after case‑insensitive guard fix.  
- `f1e4ade66e` – CIMCO machine‑bind answer posted to Echo.

**DECISIONS**  
- Adopt bounded async worker pool driven by GPU profile; no serial extraction.  
- Validate speedup with real data (0.189 pages/min/worker → 2×).  
- Use profile‑driven concurrency + OLLAMA_NUM_PARALLEL for throughput reporting.  
- Build deterministic reachability classifier to filter false “ghost.unwired” labels before wiring.  
- Wire only genuine orphans verified by the classifier; skip aggregator‑reachable and capability‑wired engines.  
- Split GENUINE_ORPHAN into: (a) self‑contained → wire directly; (b) infra‑dependent → route to owning domain.  
- Adopt lock‑aware retry loop for commits to avoid `.git/index.lock` contention.  
- Guard paths case‑insensitively on Windows.

**OPERATOR DIRECTIVES**  
- Session‑scoped Stop hook active with condition: `[ complete classifier aware hunt ]`.  
- “lets wire” – initiate wiring of remaining genuine orphans.

**FINDINGS/BUGS**  
- OLLAMA_NUM_PARALLEL=1 in legacy config caused false 3× claim; fixed by reading live env and bounding to min(workers, slots).  
- 54/110 “ghost.unwired” labels were false (aggregator‑reachable or capability‑wired).  
- Classifier mis‑labelled ExpandingMandrelEngine due to capability root mismatch; fixed with substring detection.  
- Commit absorption: WIRE‑EXEMPT edits swept into xray’s commit; resolved by path‑scoped commit `[BOOTSTRAP-SLOT-ENFORCE]`.  
- `stable-session-id.mjs` returned empty → handoff helper unusable; flagged for papa/alpha.  
- `.git/index.lock` under live contention (2 git.exe) – commit retries required.  
- Path‑confine guard was case‑sensitive on Windows, causing legitimate reads to fail; fixed.

**DOMAIN SPECIFICS**  
- Engines: batch-ollama-vision-extract.mjs, catalog-gpu-profile.mjs, measure-catalog-extraction-rate.mjs, GCodeMaterialParserEngine.ts, ShopOutcomeIngestProcessorEngine.ts, CircularDependencyEngine, OperatorPreferencesEngine, PSNHealthCheckEngine, MeasurementSystemAnalysisEngine, SimulationStallDetectorEngine, SlotSessionHistoryEngine.  
- Dispatchers: devDispatcher.ts (prism_dev), intelligenceDispatcher.ts, integrationDispatcher.ts, prism_session.  
- Actions wired: dependency_cycle_analyze (Tarjan SCC), operator_prefs_set/get/apply, psn_health_check, msa_analyze, sim_stall_markProgress/scan/stats/complete, slot_session_history_readAll, shop_outcome_ingest, gcode_material_parse.  
- Metrics: pages/min/worker, effectiveWorkers, concurrencySpeedup, OLLAMA_NUM_PARALLEL.  
- Paths: H:/prism/.claude/helpers/chat-slots.mjs, H:/prism/.claude/commands/checkin.md, scripts/classify-engine-reachability.mjs.

**TOOLS USED**  
- PRISM slot‑binding hooks (`slot-bind-enforce.mjs`, `chat-slots.mjs`).  
- CronCreate/CronDelete for recurring `/loop`.  
- Skill tool for slash commands (e.g., `/goal`).  
- Jest/TS test harness for round‑trip tests.  
- PRISM dispatcher pattern (enum + schema map), Zod schemas, Vitest round‑trip tests.  
- TypeScript compiler (`tsc`).  
- Git commit with `[BOOTSTRAP-SLOT-ENFORCE]` and path‑scoped lock‑retry.

**OPEN THREADS**  
- Wire remaining genuine orphans: SemanticAssetIndexEngine (infra‑dependent), CNCToolOffsetPersistenceEngine, SlotSessionHistoryEngine (already wired).  
- Integrate reachability classifier into system‑viz ghost.unwired detector (sierra).  
- Finalize wiring of any remaining orphan engines flagged after re‑classification.  
- Future routing: sierra (system‑viz classifier fix), juliett/india (`SemanticAssetIndexEngine` + 11 AI orphans), delta/oscar/whiskey domain orphans.
