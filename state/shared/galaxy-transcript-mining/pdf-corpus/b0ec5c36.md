# pdf-corpus session b0ec5c36 (2026-06-06, 12.2MB, spine 55KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Keystone OCR parse fix (`e354869c93`) – 61/61 tests, 2‑reviewer PASS, live‑validated on Blackwell (8–9 dims per print).  
- GPU health check (T1.3) shipped & live‑validated – `torch_ready:true`, `qlora_ready:true` with torch 2.11+cu128.  
- Node→GPU LoRA runner (U‑XRAY‑VL‑TRAIN‑RUNNER) committed – 16 hermetic tests, 2‑reviewer PASS, end‑to‑end validation against real GPU stack.

**DECISIONS**  
- Parser bug fixed by reordering `repairLeadingDotDecimals` after truncation repair; added leading‑plus handling.  
- Adopted dedicated `python-gpu` venv (torch 2.11+cu128) for all GPU training; removed old torch 2.6/cu124 blocker.  
- Corpus batch throughput is load‑dependent; overnight scheduled task is the correct durable design.  
- Operator must install missing deps (`trl`, `qwen-vl-utils`, `pillow`, `pymupdf`) in `python-gpu` before fine‑tune can run.

**OPERATOR DIRECTIVES**  
```bash
# In a clean terminal (outside harness)
H:/Tools/python-gpu/Scripts/python.exe -m ensurepip
H:/Tools/python-gpu/Scripts/python.exe -m pip install trl qwen-vl-utils pillow pymupdf
```
- Verify `gpu_health.py` reports `torch_ready:true`.  
- Trigger the overnight OCR batch after building a fresh worklist; monitor checkpoint progress.

**FINDINGS / BUGS**  
- Parser silently dropped all prints due to malformed JSON (`.86`, `+0.015`) – fixed.  
- GPU health refused old torch 2.6/cu124; now passes with cu128.  
- Corpus batch stopped early under fleet load (host kill at ~1–2 min); design is overnight job, not a code bug.  
- Trainer failed with `missing_dependency` because `python-gpu` lacked `trl`, `qwen-vl-utils`, `pillow`, `pymupdf`.

**DOMAIN SPECIFICS**  
- Engines/dispatchers: `DetachedLoRARunnerEngine`, `GpuStackHealthEngine`, `parseVisionResponse`, `gpu_health.py`.  
- Metrics: `training_brier`, `adapter_dir`, structured error codes (`missing_dependency`, `gpu_unusable`).  
- Key paths: `scripts/lib/blueprint-vl-train-runner.mjs`, `batch-ollama-vision-extract.mjs`, `H:/prism/.claude/helpers/chat-slots.mjs`.

**TOOLS USED**  
- PRISM helpers: chat‑slots, checkin pipeline, py‑subprocess‑bridge.  
- Skills/dispatchers: `/checkin-xray`, `/loop`, `/goal`.  
- Scripts: `blueprint_vl_train_lora.py`, `repairLeadingDotDecimals`, `parseVisionResponse`, `gpu_health.py`.

**OPEN THREADS**  
1. Operator must install missing deps in `python-gpu` to enable LoRA fine‑tune (T4.1).  
2. Trigger durable OCR batch after worklist build; ensure checkpoint accumulates under load.  
3. Wait for git lock clearance to land remaining doc‑reflection commit.  
4. Verify full GPU training stack (all deps, eval split) before deploying LoRA fine‑tune.
