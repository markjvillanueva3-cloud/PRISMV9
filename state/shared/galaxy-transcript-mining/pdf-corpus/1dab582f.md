# pdf-corpus session 1dab582f (2026-05-25, 37MB, spine 80KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED (builds/commits)**  
- SF‑PSN‑WIRE‑MS0 – 13 units (`U‑SFPSN‑01` … `U‑SFPSN‑02D`) – commits `df730c2f3a` → `71d2ad09d9`.  
- Vendor catalog baseline – 3 commits: `32f6ce976e`, `f18dc3e6eb`, `ff6842226b`.  
- Monolith extraction ingest – `0dbd4a090e`.  
- Tool‑registry & schema – commit `13b31ae2a3` (A1–A3).  
- Merge catalog → registry – `19559bbe33`.  
- Scraping scaffolds – D2 (`7f53468e8f`), D6 (`b8f1cdf752`).  
- Speed‑feed overlay – E2 (`b02d954548`).  
- System‑viz augmentation – F1 (`e1c5d19a52`).  
- Collision envelope adapter – E3 (`ffcbb71014`).  
- Vendor STEP URL inventory – C1 (`1e6f48bbfe`).  
- PDF extraction scaffold – B1 (`d45422fd7e`).  
- Camelot wrapper & wire – B1‑CAMELOT (`f7878a95d3`).  
- Phase A dedup audits – `660f95b680` (4 units in one commit).

**DECISIONS (architecture/scope + why)**  
- SF‑PSN wiring uses lazy `require()` to avoid circular deps and a bit‑equivalent shim pattern (`REL_TOLERANCE 1e‑12`) for physics modules.  
- R12 fail‑loud doctrine: verify actual contract before any side effect; used in catalog extraction, camelot wrapper, collision envelope adapter.  
- Peer‑sweep mitigation: pathspec‑only commits (`git commit -o <files>`) and attribution loss handling via `feedback_conflict_fork_rule`.  
- Catalog ingestion split into monolith JSONs (B0) + PDF scaffolding (B1) to allow incremental extraction; camelot wrapper added only after operator installs dependencies.  
- Phase A dedup audits consolidate duplicate engines across tool‑holder, fixture, workholding, and CAM‑export domains to a single canonical engine per domain.

**OPERATOR DIRECTIVES (verbatim asks)**  
- `H:/Tools/python/python.exe -m pip install "camelot-py[cv]"` – unlocks B2–B5 mass extraction.  
- Provide credentials for Misumi, Sandvik CoroPlus, Kennametal NOVO, Iscar etool portals (D3‑D5).  
- Approve edit of `CADCorpusIngesterEngine` scan‑root to enable E1.  
- Load exported files into Fusion 360 (`Fusion360ToolExportEngine`), HyperMILL (`HyperMillACStandardToolDBEngine`), and Mastercam (`MastercamToolExportEngine`) for manual verification (E1–E3).

**FINDINGS/BUGS**  
- Peer‑sweep: 5 occurrences; resolved via pathspec commits and attribution loss documentation.  
- Camelot wrapper R12 bug: missing `camelot_installed` flag – fixed in B1‑CAMELOT.  
- Mislabeling of Tungaloy vs Sandvik GC catalogs – corrected by pattern update (`/^gc_2023-2024_/i → "tungaloy"`).  
- Missing zip parts (21 out of 78) – operator must re‑upload.  
- Unknown PDFs (8) – require manual classification or extended pattern table.

**DOMAIN SPECIFICS**  
- Engines: `UltimateSpeedFeedEngine`, `SpeedFeedOrchestratorEngine`, `ToolRegistry`, `CollisionDetectionEngine`, `CADCorpusIngesterEngine`, `Fusion360ToolExportEngine`, `HyperMillACStandardToolDBEngine`, `MastercamToolExportEngine`.  
- Dispatchers/skills: `pdf-learn`, `cad-extract`, `tool-enrich`, `acquire-models`, scraping scripts (`scrape-step-backfill.mjs`, `scrape-pts-tools.mjs`).  
- Metrics: 56 anti‑regression tests passed (SF‑PSN), ~96 tests for catalog ingestion, 280+ tests across all units.  
- Paths: `mcp-server/data/catalog-extractions/` (monolith JSONs), `scripts/vendor-catalog-audit.mjs`, `vendor-catalog-summarize.mjs`.

**TOOLS USED**  
- PRISM tools: `/checkin`, `/loop`, `/goal`, `chat-slots.mjs`, `audit-roadmap-drift.mjs`, `merge-augmentations.mjs`.  
- Scripts/skills/hooks: `vendor-catalog-audit.mjs`, `vendor-catalog-summarize.mjs`, `scrape-step-backfill.mjs`, `scrape-pts-tools.mjs`, `extract-vendor-pdf.mjs`, `catalog-storage-paths.mjs`.  
- External: `pdftotext` (MSYS), `camelot-py[cv]` (Python wrapper).

**OPEN THREADS (what is still to build)**  
1. **Phase B schemas** – ship B1 (`ToolAssembly`), B2 (`WorkholdingSetup`), B3 (`JobSetup`).  
2. **Export verification** – E1, E2, E3: load `.tools`, `.htdb`, `.tooldb` into Fusion 360, HyperMILL, Mastercam for manual validation.  
3. **CADCorpusIngesterEngine scan‑root edit** (E1) – operator approval required.  
4. **Portal scraping** – D3–D5 (Misumi, Sandvik CoroPlus, Kennametal NOVO, Iscar etool).  
5. **Mass PDF extraction** – B2–B5 once camelot is installed and credentials provided.  
6. **Missing zip parts re‑upload** – operator action to complete catalog corpus.  

These items must be addressed before the galaxy can fully ingest all tooling, holders, fixtures, and workholding into the unified database for Fusion, HyperMILL, and Mastercam exports.
