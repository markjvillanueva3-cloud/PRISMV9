# pdf-corpus session 77971357 (2026-05-17, 36.5MB, spine 184KB, 3 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U‑REGEN‑VIZ‑MERGE‑FAILLOUD – commit f9dc218d78 (regen‑viz merge‑fail‑loud fix)  
- U‑FEEDBACK‑FORCING – commit b1e599d5fc  
- U‑SOURCEHASH‑DOC‑ALIGN – commit 0bac2d4c2f  
- U‑COMPLEXITY‑FALLBACK – commit 3d416cb040  
- U‑BUG‑FINDINGS‑WIKI‑HOOK – commit bb198d9285  
- U‑BUG‑FINDINGS‑WIKI‑BACKFILL – commit 1539fbc8b2  
- CLAUDE.md updates – commits 9c5377a2a7, dabb7a76c6  
- RTK binary rtk.exe v0.40.0 installed via `/rtk‑setup`; PreToolUse Bash hook wired  
- F8 cache‑reader audit retracted – commit 17235ead06  
- `build-requests-viz-sync.mjs` (296 insertions)  
- `high-value-additions-rank.mjs` (high‑ROI wiring scan)  
- 14 `node:test` cases added; pure helpers exported, `isMain` gated  

**DECISIONS**  
- Adopt 4‑tier `resolveUnitKey` resolver in `pick-prefresh-inject`.  
- Add heap‑size flag & error handling to `regen-viz.mjs` (fix silent merge failure ~4 GB).  
- Ship above fixes before running precompact/compact.  
- Keep peer‑claimed files untouched; roll back accidental commits.  
- Keep RTK hook active; install binary via `/rtk‑setup`.  
- Do not build new cache‑reader hook—`file-read-cache.mjs` already dedups.  
- Leave slot‑worktree migration as operator‑mandated fleet change (no auto‑migration).  
- Record memory‑inject telemetry issue for future review.  
- Add injection layer: 39 SessionStart, 25 UserPromptSubmit, 1 SubagentStart hooks; define consumption rules.  
- Doctrine: consume pre‑searched injections instead of re‑deriving.  
- Implement injection‑layer anti‑patterns & tuning knobs.  
- Ship build‑request sync script only if log >20–30 entries; otherwise manual column.  
- Log but defer wiring of 82 unwired Lathe engines until validation (≈50 % false positives).  

**OPERATOR DIRECTIVES**  
- Continue loop with all current tasks (no new unit cap).  
- Rule: always update wiki for bug findings (`U‑BUG‑FINDINGS‑WIKI‑HOOK`).  
- Implement persistent build‑request log at `state/shared/USER-BUILD-REQUESTS-LOG.md`.  
- Extend tribal‑obsidian‑viz utilization protocol doc.  
- Monitor MS1 P1 backlog: `U-RIE-ADAPTER`, `U-CALIBRATION`, `U-TRANSFER`.  
- Build and search for high‑ROI node wirings in system‑viz.  

**FINDINGS/BUGS**  
- Silent merge failure due to Node heap limit (~4 GB) in `regen-viz.mjs` spawnSync.  
- Legacy nested `{plan: …}` shape still used in some tests; updated to flat plan format.  
- Peer‑claimed file `dev-tool-leverage.md` accidentally staged; rolled back.  
- Per‑file scrutiny fixed 2 P0s + 1 P1 in commit b1e599d5fc.  
- Envelope drift reduced from 175 → 11 after system‑viz refresh.  
- F8 cache‑reader claim false positive; audit retracted.  
- Memory‑inject hook lacks telemetry; hit rate unmeasured.  
- `classifyHits` regex fixed to match real system‑viz find output:  
  `L<layer>/<subgroup> <node-id> <label>` with `ghost.` prefix for ghost nodes.  
- P0+P1 regression resolved; all tests PASS.  
- Raw `NEEDS_WIRING` signal shows ~50 % false positives (729 engines flagged).  

**DOMAIN SPECIFICS**  
- RGS‑TOOL‑AUTOINVOKE‑MS1 units: feedback forcing, RIE adapter, calibration, transfer.  
- `pick-prefresh-inject` hook & resolver logic.  
- System‑viz graph 145 k nodes / 700 k edges after full regen.  
- Slot-task-claims and chat-slots JSON stores for slot management.  
- Units: `U-FEEDBACK-FORCING`, RTK, `file-read-cache.mjs`.  
- Hooks: `pick-prefresh-inject.mjs`, `error-pattern-promote.mjs`, RTK hook (Claude).  
- Memories: `reference_dev_pipeline_toolbox_2026_05_17.md`, `USER-BUILD-REQUESTS-LOG.md`.  
- Injection hooks: SessionStart, UserPromptSubmit, SubagentStart; defined consumption rules.  
- System‑viz find output format & ghost node detection.  
- Lathe domain cluster: 82 unwired engines (high‑ROI wiring target).  
- F3‑orphan-engines P0 lev 72.9; F1‑orphan-hooks P0 369 orphans /78.5% lev 61.9.  
- Build‑requests log at `state/shared/USER-BUILD-REQUESTS-LOG.md`.  
- Awareness docs: `knowledge/memories/`.  

**TOOLS USED**  
- Scripts: `regen-viz.mjs`, `pick-prefresh-inject.mjs`, `regen-viz-merge-guard.mjs`.  
- Git helpers: `chat-slots.mjs`, `slot-task-claims.mjs`.  
- Test harnesses: `__tests__/` (Jest), `node:test`.  
- PRISM commands: `/checkin`, `/loop`, `/schedule`, `CronCreate`.  
- Hook scripts: `pick-prefresh-inject.mjs`, `error-pattern-promote.mjs`, `file-read-cache.mjs`, RTK hook.  
- Helper utilities: `install-fleet-reaper-task.ps1`, `skill`.  
- PRISM system‑viz find query.  
- `build-requests-viz-sync.mjs`.  
- `high-value-additions-rank.mjs`.  
- `validate-unwired-signal.mjs`.  

**OPEN THREADS**  
- Remaining MS1 P1 backlog: `U-RIE-ADAPTER`, `U-CALIBRATION`, `U-TRANSFER`.  
- Pending precompact/compact after merge‑fail fix.  
- Slot‑worktree migration (operator‑mandated).  
- Memory‑inject telemetry counter / threshold tuning.  
- Ollama offload scope narrowing (F3) pending implementation.  
- Further efficiency gains beyond quick wins to be scoped.  
- Validate raw `NEEDS_WIRING` signal with `validate-unwired-signal.mjs` before wiring Lathe engines.  
- Wire 82 unwired Lathe engines once validated.  
- Monitor build‑request log drift; add sync script if >20–30 entries.
