# pdf-corpus session 54a75f3b (2026-06-04, 14.4MB, spine 77KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `7a1aea6723` – `vision-ensemble-fuse.mjs`, CLI & 26 tests: multi‑VLM OCR on RTX 6000 Blackwell (qwen3‑vl, qwen2.5‑vl, llama3.2‑vision) with N‑way clustering + noisy‑OR fusion.  
- `c8bb8a3a25` – `ocr-training-loop-lib.mjs` & runner: closed‑loop OCR training engine; agreement‑fraction calibration (isotonic), tiering, emits `trainset.jsonl`.  
- `4fec77e8c1` – `trainset-to-lora-pairs.mjs` + adapter to `BlueprintLoRABridgeEngine`: transforms `trainset.jsonl` into staged `local‑lora` bundle consumable by India’s LoRA stack.  
- `b121b19f7b` – `blueprint_vl_train_lora.py` & tests: real Qwen2.5‑VL PEFT trainer (handles image token masking, NDJSON protocol).  

**DECISIONS**  
- Unlock idle RTX 6000 GPU by running a 3‑model ensemble OCR; replace single‑model OCR to exploit full memory and compute.  
- Fix leading‑dot decimal parse bug in vision extraction (`repairLeadingDotDecimals`).  
- Use ensemble distillation for training: only train when ≥2 models agree (no single‑model hallucinations).  
- Calibrate `P(correct|f)` where `f = k / n_models` to make calibration invariant to varying model counts.  
- Stage LoRA bundles via existing `BlueprintLoRABridgeEngine`; defer actual fine‑tune until India’s environment is ready (torch≥2.7/cu128, PEFT, datasets, trl).  

**OPERATOR DIRECTIVES**  
- “utilize india's capabilities … use workflow to fine tune your galaxy and system for lora” → produce takeover plan (`INDIA‑TAKEOVER‑PLAN‑blueprint‑lora.md`).  
- “precompact but plan for taking over india's work since its currently busy” → handoff written with RESUME directive to start T3.2 (Node→GPU runner).  
- “continue” → proceed with T3.2 implementation and subsequent steps.  

**FINDINGS/BUGS**  
- Leading‑dot decimal in OCR output caused silent data loss; fixed by string‑aware scanner.  
- Original calibration used raw `k`, mislabeling single‑model runs; corrected to fraction `f`.  
- Torch 2.6/cu124 on sm_120 Blackwell reports `cuda.is_available()` true but lacks kernels → need torch≥2.7/cu128.  
- `wedm_train_lora.py` is a text‑only stub; real VL trainer required (implemented).  
- Ollama daemon contention prevents cold models from loading within 300 s; only two warm models survive in current run.  

**DOMAIN SPECIFICS**  
- **xray galaxy**: blueprint‑vision OCR, print→CAD pipeline, uses `vision-model-select`, `vision-ab-compare`, cross‑source dimension reconciliation (`cad_dimension_reconcile`).  
- **Ensemble OCR**: qwen3‑vl, qwen2.5‑vl, llama3.2‑vision; N‑way clustering + noisy‑OR fusion.  
- **Closed‑loop training engine**: isotonic regression, agreement‑fraction calibration, tiering (gold/silver/bronze/no_corroboration).  
- **India galaxy**: LoRA stack (`BlueprintLoRABridgeEngine`, `py-subprocess-bridge`, `DetachedLoRARunnerEngine`), PEFT trainer, operator‑verified split, Brier gate.  

**TOOLS USED**  
- PRISM tooling: `/checkin-xray` wrapper, slot‑binding hooks (`slot-bind-enforce.mjs`, `chat-slots.mjs`), `stable-session-id.mjs`, `per-agent-handoff.mjs`.  
- Vision extraction libs: `vision-ensemble-fuse.mjs`, `ollama-vision-extract-lib.mjs`.  
- Training loop lib: `ocr-training-loop-lib.mjs`.  
- Bridge engine: compiled `BlueprintLoRABridgeEngine` (`dist/`).  
- Workflow orchestrator for takeover plan.  

**OPEN THREADS**  
- **T3.2** – Implement Node→GPU runner: wire `py-subprocess-bridge → DetachedLoRARunnerEngine`, monitor, dispatcher; add E2E tests.  
- **T1.3** – Add GPU preflight (`preflight_gpu()`) to `GpuStackHealthEngine`.  
- **T2.1** – Expand trainset to ≥50 reliable samples (synthetic prints with ≥2 models).  
- Install torch≥2.7/cu128, PEFT, datasets, trl in India’s environment; create operator‑verified split and Brier gate.  
- Final deployment of LoRA fine‑tune once all blockers cleared.
