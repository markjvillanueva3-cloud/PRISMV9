# pdf-corpus session 86373eb3 (2026-06-10, 48.8MB, spine 403KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- New `Launch‑PRISM‑Fleet‑3win.ps1` (3‑window per‑account staggering).  
- Desktop shortcuts: `PRISM Fleet (24‑tab)` → 3‑window launcher; fallback `PRISM Fleet` remains 4‑window.  
- `settings.json`: `LOCAL_LLMBACKEND=ollama`; all NIM containers set to `restart=no`, stopped.  
- Guard in `nim-autostart.mjs` skips NIM start when backend is ollama.  
- `docker-compose.yml`: removed obsolete `version:` key; `KEEP_ALIVE=-1 → 30m`; `MAX_LOADED_MODELS=6 → 4`.  
- Master‑index OOM guard added, recall hooks enabled, reverse mirror hook wired.  
- Durable cron‑runner installers created for weekly, daily‑context, knowledge‑distillation.  
- GNN feeder (`vault-to-gnn-refpool.mjs`) built (9/9 tests); 4 new high‑confidence reference ghosts added to 642 MB graph.  
- LoRA feeder (`vault-to-lora-dataset.mjs`) built (15/15 tests); 245 of 247 feedback memories validated; CRLF, .md extension, regex \s bugs fixed.  
- Registry for all 34 galaxies created; generalized transcript miner recovered ~2 k handoffs across 33/34 galaxies.  
- Miner updated: content‑based discovery, `MAX_SLICES_PER_TX`, cache of assignments, `NUM_CTX` restored, synthesis timeout raised to 900 s (gpt‑oss:120b). All tests pass (27/27); commit `5203edf318`.  
- Hermes + Obsidian self‑learning loop operational; inbox sweep, morning brief, weekly review cron jobs running.  
- Obsidian REST bridge authenticated on `127.0.0.1:27123`; MCP server (`start-http.mjs`) and Hermes gateway up (PID 57264).  
- Fleet of 13 slots running; all scheduled cron jobs firing every 25 min.

**DECISIONS**  
- Adopt Ollama‑only backend; remove NIM from autostart, keep native Ollama via scheduled task.  
- Guard `nim-autostart.mjs` instead of deleting hook to preserve future re‑enableability.  
- Remove `ollama` and `ollama‑nomic‑preload` from compose services and launcher defaults.  
- Enable `MASTER_INDEX`, `MEMORY_INDEX`, `WIKI_PRECHECK_INJECT`; disable vault precheck flag.  
- Add heap‑aware sidecar guard in `master-index-search-lib.mjs`.  
- Wire `h-to-c-obsidian-mirror.mjs` as PostToolUse hook.  
- Replace TEMP‑based cron runners with durable `.claude/cron-runners/`.  
- Commit vs keep live changes pending; decide on node_card auto‑default and Ollama route‑suggest rate.  
- Set `OLLAMA_CONTEXT_LENGTH=65536`; configure Hermes model context length 65 536, max tokens 16 384, MCP timeout 300s.  
- Do not re‑add `prism-vault-fs` MCP to Hermes config; keep `PRISM_OBSIDIAN_API_KEY` vault‑only.

**OPERATOR DIRECTIVES**  
1. Capture Claude Max credentials for accounts 2–6 using `node H:\prism\scripts\capture-claude-credentials.mjs <account> <label> <pos>` and create `H:\.claude-accounts\ROTATION_ORDER.json`.  
2. Run elevated PowerShell block to register all missing/repair tasks:  
   ```powershell
   powershell -NoProfile -ExecutionPolicy Bypass -File H:/prism/.claude/helpers/install-synthesis-crons.ps1 -RunNow
   powershell -NoProfile -ExecutionPolicy Bypass -File H:/prism/.claude/helpers/install-tribal-consolidate-cron.ps1 -RunNow
   powershell -NoProfile -ExecutionPolicy Bypass -File H:/prism/.claude/helpers/install-tribal-promotion-cron.ps1 -RunNow
   powershell -NoProfile -ExecutionPolicy Bypass -File H:/prism/.claude/helpers/install-wiki-tribal-audit-task.ps1 -RunNow
   ```  
3. Re‑register the four cron installers and five unhealthy scheduled tasks via elevated PowerShell.  
4. Run `hermes gateway install` (elevated) to persist gateway on logoff.  
5. Verify all 6 scheduled tasks show `LastResult 0x0`.  
6. Decide whether to commit the 9 edits (7 H: files + 2 C: settings) or keep them live.  
7. Run bounded proofs (`--limit 6`) for each galaxy before full population; avoid `--all` background jobs.

**FINDINGS/BUGS**  
- NIM containers OOM‑crashed (exit 137) due to Docker 16 GB cap; removed from autostart.  
- PRISM probed dead NIM on every request → latency/API‑limit errors.  
- Compose ollama conflicted with native Ollama on port 11434; removed from compose services.  
- `docker-compose.yml` had obsolete keys and potential pinned‑model leak; fixed.  
- Misuse of `--services=mcp` caused “no such service: mcp” errors; added fail‑fast guard.  
- Master‑index OOM bug fixed with heap guard.  
- TEMP‑based cron runners broken by tmp‑orphan janitor; replaced with durable path.  
- `find-connections.mjs` not a batch cron; scope creep noted.  
- `seed-ghost-from-unwired.mjs` crashed on 642 MB graph (ERR_STRING_TOO_LONG); fixed via streaming I/O and atomic write.  
- GNN reference pool starved at 0; India miner grew it to 125 refs, vault feeder added 4 more.  
- LoRA feeder had CRLF \r leaks (2,260), .md extension leak, regex \s ate newlines; all corrected.  
- `classifyHandoff` regex too strict: missed `.archive.<date>`, long‑UUID IDs, Agent@HOST, slot‑keyed forms; hardened to recover ~250 handoffs.  
- `galaxy-synthesis-refresh.mjs` comments misrepresented mechanism; zero‑coverage galaxies omitted; counters conflated pre‑SINCE and missing files – all fixed.  
- Fleet‑reaper mistakenly killed overnight vault pipeline twice; extended protect regex (`PRISM_REAPER_PROTECT_EXTRA`).  
- GPU contention with gpt‑oss:120b stalls embedding sidecar rebuild; embeddings need separate run after mining.  
- Large transcript spine extraction (48 MB) dominates runtime; slice cap mitigates.  
- Exit 255 from `grep | timeout` wrapper misleads success; vault memo presence is real indicator.

**DOMAIN SPECIFICS**  
- **Engines:** Ollama MAP (`gpt‑oss:20b`), SYNTHESIS (`gpt‑oss:120b`), nomic‑embed‑text, ObsidianRestBridgeEngine, Hermes skill engines (gepa, obsidian_read/search).  
- **Actions/Dispatchers:** `vault-to-gnn-refpool.mjs`, `seed-ghost-from-unwired.mjs`, `vault-to-lora-dataset.mjs`, `mine-galaxy-transcripts.mjs`, `build-memory-index-sidecar.mjs`, `build-memory-embeddings-sidecar.mjs`, `galaxy-synthesis-refresh.mjs`, `h-to-c-obsidian-mirror.mjs`.  
- **Metrics:** 3‑window launcher, 6 Claude Max accounts, Docker memory cap 16 GB → NIM OOM; KEEP_ALIVE=30m, MAX_LOADED_MODELS=4; 642 MB system‑graph.json (125→129 refs); LoRA dataset 245/247 validated; miner processed 2,097 handoffs, 33/34 galaxies mineable.  
- **Paths:** `H:/prism/scripts`, `H:/prism/.claude/cron-runners/`, `H:/prism/knowledge`, `state/shared/articles`.

**TOOLS USED**  
- PowerShell scripts (`Launch‑PRISM‑Fleet‑3win.ps1`, shortcut edits, scheduled‑task installers).  
- Docker Desktop (WSL2) & docker‑compose.  
- Node.js modules: `nim-autostart.mjs`, `claude-account-lib.mjs`, `master-index-search-lib.mjs`, `h-to-c-obsidian-mirror.mjs`.  
- Cron‑runner utilities (`*.mjs`).  
- Playwright for article extraction; `schtasks` for scheduled‑task management.  
- PRISM scripts: `overnight‑vault‑compound.mjs`, `start-http.mjs`, `fleet-reaper-sweep.mjs`.

**OPEN THREADS**  
- Commit vs keep live changes pending (decision on 9 edits).  
- Build node_card auto‑default and tighten Ollama route‑suggest rate.  
- Re‑register missing scheduled tasks; ensure foreground per‑galaxy runs (`--limit ≤6`).  
- Verify embeddings sidecar rebuild succeeds under GPU load; schedule separately if needed.  
- Confirm all vault memos present and synthesis‑refresh marks galaxies as stale/compounded.  
- Weekly self‑review cron job still unvalidated on gpt‑oss:120b.  
- GEPA round 2 traces to mine and review.  
- Automate Obsidian vault auto‑open for REST binding or ensure manual launch.  
- Continuous monitoring of MCP server health; keep `.env` git‑ignored.
