# pdf-corpus session c7361c9f (2026-06-10, 72.9MB, spine 495KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- ascii‑guard PreToolUse block (`ascii-guard.mjs`) – diff‑aware enforcement of no smart‑punctuation.  
- MCP server tuning: heap floor 4 GB→24 GB, watchdog RSS 3 GB→18 GB; inflight‑aware restart + bounded semaphore on `/mcp`.  
- Reaper/monitor restoration – all 8 fleet‑hygiene tasks enabled (Fleet Reaper, Aggressive Killer, Singleton Guard, Slot‑Task‑Claim).  
- Docker/WSL/Ollama/Qdrant stack up; redundant NIM containers stopped.  
- Slot‑branch routing verified – 26 worktrees, 16 active slots, hooks in `H://.claude/settings.json`.  
- U-MCP-CONCURRENCY-HARDEN (committed).  
- G5 closed‑redundant janitor (`U-GOLF-G5-CLOSE`).  
- G1 closed‑premise corrected (`U-GOLF-G1-CLOSE`).  
- G10 crash‑critical auto‑reenable guard shipped & live‑validated (`U-GOLF-G10`, `-HARDEN`, `-DONE`).  
- G6 native‑Ollama health arm (`U-GOLF-G6-OLLAMA-HEALTH-ARM`).  
- U‑GOLF‑TASK‑OWNER‑MAP committed (deterministic WARN routing).  
- U‑GOLF‑CRASH‑POSTMORTEM‑DIGEST committed.  
- GPU‑VRAM admission guard hook (`gpu-vram-admission-guard.mjs`, commit f3eb0c1c15).  
- vLLM incorporation plan file (`VLLM-INCORPORATION-PLAN-2026-06-10.md`).  
- MCP-server hardening: safe‑additive fixes + P2‑1 refinement (commits `63d08b5eed`, `5181350fd7`).  
- Bravo lone‑surrogate API error fixed with surrogate‑safe truncation lib & hook (`c1a50b7c99`, `3495a1dc6a`).  
- BaseRegistry/Logger added lightweight Logger class, restored 15 registries + atomic persist & single‑flight init (`62fe49af70`).  
- Express.json body limit raised to 50 MB (env‑overridable) – silent 413 bug fixed.  
- UnhandledRejection → exit opt‑in flag added.  
- vLLM admission guard wired with `PRISM_VRAM_GUARD_MODE=block`.  
- U‑GOLF‑TASK‑OWNER‑MAP commit 5dc70e36bd; reverse‑completeness guard commit 13596741a4.  
- Production heap env‑tunable scripts (`deploy/start.sh`, `start-production.*`) – commit 52fc2bd0fe.

**DECISIONS**  
- Adopt Ollama as primary local LLM offload; NIM only when explicitly required.  
- Enforce ascii‑guard to block smart‑punctuation regressions.  
- Raise MCP heap/watchdog limits for 136 GB RAM; add inflight‑aware restart logic.  
- Clear migration‑freeze marker; re‑enable all frozen tasks post‑PC upgrade.  
- Treat fleet as ultracode: unlimited parallel agents via bounded semaphore & inflight watchdog.  
- Use bounded ≤3‑agent ultracode workflow to avoid rate limits.  
- Auto‑enforce slot binding (`slot-bind-enforce.mjs`) – no manual bash claim.  
- Skip full `/startup`; run only `/fleet-reaper`.  
- Revert drift fix that caused false “MISSING” warnings in fleet‑task‑health.  
- Keep reapers/monitors active; schedule bounded reap‑check each cycle.  
- Build #2 first to enforce GPU safety before heavy model launch; keep guard warn mode by default, block opt‑in (`PRISM_VRAM_GUARD_MODE=block`).  
- Use explicit pathspec commits to avoid shared‑index race and ownership‑guard interference.  
- Adopt surrogate‑safe truncation library for all naive `.slice()` injectors; expose as reusable skill.  
- Make MCP hardening defaults env‑overridable (body limit, unhandledRejection flag, pagination clamp, token TTL, response truncation, deep health probes).  
- Do not restart Docker daemon unless explicitly commanded.

**OPERATOR DIRECTIVES**  
- Start Qdrant/Docker/WSL/Ollama; ensure Ollama active.  
- Improve context continuously.  
- Bring fleet reapers up post‑PC upgrade; clear zombies.  
- Ensure chat‑fleet commit to own slot branches fully active; fix git tree conflicts.  
- Enforce rules, systems, skills, hooks that were advisory (focus on em‑dash, ascii, text).  
- Utilize Ollama for task efficiency when viable.  
- Improve MCP server performance; eliminate disconnects.  
- Account for fleet spawning parallel agents.  
- Keep system in ultracode mode with unlimited workflow agents.  
- `/startup-golf`: force‑take golf slot, bind to `golf-work`, run `/fleet-reaper`.  
- `/compact` after G5/G1/G10 to reset context.  
- Keep `/loop /goal keep fleet reapers running`.  
- Run hygiene check (aggressive killer, reaper health, slot‑task‑claim) each tick; do not advance golf unit unless budget allows.  
- Knock backend backlog when idle: start unwired engine wiring, vLLM migration plan, GPU‑VRAM guard build after `/precompact`.  
- Re‑register stale `Blueprint OCR Batch` (operator‑elevated PowerShell).  
- Broadcast fleet warning of upcoming vLLM POC and GPU contention.  
- Proceed with Phase‑0 vLLM POC once Docker Desktop restarted to fix image manifest issue.  
- Confirm PRISM Fleet Reaper remains enabled; no new degraded tasks.

**FINDINGS/BUGS**  
- NIM vision container crashed (GPU memory exhaustion); removed.  
- GPU utilization low (5 % idle) – routing under‑utilized but not a model issue.  
- Missing ascii‑guard caused em‑dash regressions; now enforced.  
- MCP watchdog threshold too low for new hardware; fixed with higher limits & inflight awareness.  
- Concurrency gap: each `/mcp` created fresh McpServer; semaphore + queue bounds peak memory, prevents slot leak.  
- G5 premise stale – existing decay logic handles orphan stamps.  
- G1 premise 22K untracked files genuine backlog; safe junk patterns removed locally.  
- Regression from adding synthesis‑cron names to `KNOWN_PRISM_TASKS`; reverted.  
- Index.lock contention resolved after peers committed.  
- False “MISSING” alarm due to transient system pressure (`0x800710E0`).  
- Shared‑tree commit race fixed via pathspec commits & ownership‑guard review.  
- Lone surrogate 400 errors from naive `payload.slice(0,2048)`; fixed with surrogate‑safe truncation.  
- BaseRegistry lacked Logger class; added lightweight wrapper.  
- vLLM GPU memory: 80 B fp8 ≈83 GB; guard warns by default, block mode opt‑in.  
- Docker image manifest corruption – requires Docker Desktop restart or tag pin.  
- Raw NUL byte in test file cleaned up.  
- Merge‑augmentations bug still fails at 630 MB graph despite 24 GB heap & 8 KB stack.

**DOMAIN SPECIFICS**  

*Engines/Actions* – Fleet Reaper, Aggressive Killer, Singleton Guard, Slot‑Task‑Claim, Local‑LLM Bridge (`local-llm-bridge.mjs`), Nim Docker Launcher, MCP server dispatcher (`index.ts`), Chat‑Slot claim helper (`chat-slots.mjs`), gpu‑vram-admission-guard, vLLM admission guard, Bravo API (surrogate‑safe truncation).

*Dispatchers* – MCP server dispatcher, Chat‑Slot claim helper, cron `e12cf005`, `/precompact`, ultracode workflow engine.

*Metrics/Paths* – GPU metrics (`fleet-reaper-sweep.mjs`), Ollama cost router (`ollama-cost-router.mjs`), health endpoint (inflight & peakInflight), nvidia‑smi stats, Ollama API `/api/ps`, PRISM Fleet Reaper health JSON, `PRISM_VRAM_GUARD_MODE=block`, `PRISM_VIZ_REGEN_HEAP_MB`.

**TOOLS USED**  
- PRISM scripts: `fleet-reaper-sweep.mjs`, `fleet-task-health-watch.mjs`, `06-aggressive-killer.mjs`, `slot-task-claim.mjs`, `docker-service-health-stop.mjs`.  
- Ultracode workflow engine.  
- Ollama LLM (gpt‑oss:120b, qwen2.5-coder:32b).  
- CronCreate / CronDelete for recurring tasks.  
- Git hooks: `commit-ownership-guard`, pathspec commits.  
- Node 20+ (`String.prototype.toWellFormed()` for surrogate handling).  
- Docker Compose (vLLM compose file).  
- Playwright (external article scraping).  

**OPEN THREADS**  
1. Root cause of recurring disabling of crash‑critical tasks.  
2. Merge‑augmentations bug in regen‑viz (fails at 630 MB graph).  
3. MISC‑TASKS inventory cleanup (317 orphaned incomplete items).  
4. Node heap cap adjustments for batch jobs beyond regen chain.  
5. GPU‑VRAM admission guard refinement post‑`/precompact`.  
6. Draft vLLM migration plan finalization.  
7. Decision on removing `PRISM_ALLOW_UNWIRED=1`.  
8. Finalize unwired engine wiring & 89‑engine bridge units.  
9. Re‑register stale OCR batch (operator action).
