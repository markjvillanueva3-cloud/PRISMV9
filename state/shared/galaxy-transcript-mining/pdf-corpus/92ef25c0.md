# pdf-corpus session 92ef25c0 (2026-05-27, 25.7MB, spine 88KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- 12 new course modules (44‑58) covering JM‑Die fleet, Lean/Sigma/Kaizen, post‑processors, speed/feed, tooling DB, work‑holding DB, CAD advanced, CAM complex geometry, FEA, 5‑axis mastery, mill/lathe/tooling encyclopedias, hard milling/turning.  
- Web blueprints for all 60 courses (catalog + detail pages).  
- Free local video‑extraction pipeline (Whisper + Ollama vision) – no API credits.  
- JM Die corpus catalog (82 PDFs) and full page‑by‑page extraction (8,752 notable entries).  
- Two deep‑research wiki entries (CAD 2/3D & CAM complex geometry).  
- Course‑60 “chip control mastery” with tribal JSONL.  
- Per‑employee tracks + dev‑seed picker for Justin, Chris, Mark.  
- AcademyHub drill‑down UI (domain → sub‑category → generate ordered coursework).  
- Video embed infrastructure and curated YouTube picks (27 videos).  
- Phone install stack: `dev-seed-apprentice.html`, tunnel script, `vercel.json`, Playwright smoke test, onboarding doc.

**DECISIONS**  
- Slot‑binding wrapper (`/checkin‑lima`) to isolate work in the *lima* slot.  
- Use pypdf for PDF extraction (free, no API).  
- Replace paid Whisper/Claude with local Whisper + Ollama vision; expose same schemas for downstream generators.  
- Catalog‑first approach: list all PDFs → generate wiki stubs & tribal entries → feed into knowledge‑extractor.  
- Academy curriculum driven by `CurriculumEngine.ts` (imports, RICH_MODULES, courseDefinitions).  
- PWA‑ready phone install via Cloudflare tunnel + Vite dev server; dev‑seed page for role‑based onboarding.

**OPERATOR DIRECTIVES**  
- “Continue from where you left off.” → finish remaining R12 deferrals: auto‑select tracks on login, persist user picks, add video sections to all courses, deploy permanent URL (Vercel), integrate real ERP records.  

**FINDINGS/BUGS**  
- 5 encrypted PDFs + 1 ZIP in JM Die corpus – require alternative sources or decryption.  
- Cloudflare tunnel initially pointed at Express on 127.0.0.1:3100; fixed to Vite’s 0.0.0.0:3100.  
- `dev‑seed-apprentice.html` redirect bug (`/academy` → `/learning/academy`).  
- Conflict‑fork with peer courses 35‑43 – resolved by renumbering own courses 44‑47.  
- Missing `manifest.webmanifest` and `sw.js` in *lima* branch – added via main‑tree copy.

**DOMAIN SPECIFICS**  
- **Machines:** Okuma lathes LTH‑01…07, VMC‑01…05 mills, sinker EDM, wire EDM.  
- **Tooling:** spindle connections (HSK, CAT, BT, BBT, PSC, KM, R8), holder families, end‑mill variants, indexable inserts, drill bodies, taps/threading, turret types, collet standards, hard‑milling materials (CBN/PCBN).  
- **Processes:** CAD functions, CAM toolpaths, milling/machine toolpaths, lathe toolpaths, wire EDM paths, post‑processing, speed/feed calculation, tooling database, work‑holding database.  
- **Graph nodes:** PostProcessorPipelineEngine, UltimateSpeedFeedEngine, ToolSelectionEngine, WorkholdingValidationEngine.

**TOOLS USED**  
- PRISM core: `chat-slots.mjs`, `/checkin` pipeline, `CurriculumEngine.ts`.  
- Video learn: `VideoLearningEngine.js`, `video-learn` skill.  
- PDF extraction: `pypdf`, local OCR (Tesseract).  
- LLM backends: Whisper (local), Ollama vision (`qwen2-vl`, `llava`), qwen2.5‑coder for knowledge extraction.  
- Frontend: Vite, React PWA, service worker, Playwright smoke tests.  
- Deployment helpers: Cloudflare tunnel script, `vercel.json`, `netlify.toml`.  

**OPEN THREADS**  
1. **Add video sections** to all 60 courses (≈200 insertions).  
2. **Persist user picks** and auto‑select tracks on login (`U‑ACADEMY‑TRACK‑AUTOSELECT`, `U‑ACADEMY‑PICKS‑PERSIST`).  
3. **Permanent URL** – deploy via Vercel/Netlify; keep tunnel for quick demos.  
4. **Real ERP integration** – replace dev‑seed with `/api/v1/auth/login`.  
5. **Tag‑based predicates** on `CourseBlueprint` to make sub‑category filtering robust.  
6. **Offline lesson caching** (SW update strategy).  
7. **Push notifications** for course reminders.
