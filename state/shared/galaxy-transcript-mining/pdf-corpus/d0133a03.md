# pdf-corpus session d0133a03 (2026-06-10, 54.6MB, spine 498KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit 23ad2cdf3e – CLAUDE.md honesty block, `tournament-rank.mjs`, `intake-quarantine-guard.mjs`, `save-workflow.md`, ULTRACODE‑SYNERGY‑MS0 spec, `GroupRelativeRewardNormalizerEngine` (GRPO), added `group_advantage`.  
- Orders 3/4: GRPO (`037e3ac930`,`3fa529432f`) and RULER (`46553bb74a`).  
- Fact‑checker agent `.claude/agents/fact-checker.md`.  
- Spec drift fix `accf6f247f` (orders 3–5 shipped).  
- MCP Priority Guardian registered; reaper & monitor cluster 10/10 ready.  
- Ollama keep‑alive patch `cebde4fd94` (30 min TTL, maxLoaded = 4).  
- GWizardToolCribExportEngine (2 commits), WSL memory guard (`27-wsl-memory-guard.mjs`) installer token fixed.  
- Fusion machine‑library export (`44c41ee643`,`a6d80537cf`).  
- HolderSelectionEngine core (`4ab181a78d`); Fusion, Mastercam, HyperMill tool exporters wired to real holders (`82ca289ef4`,`7783cbef3b`).  
- Tool‑catalog cap lift (hyperMILL/Mastercam) `6064ace7a4`.  
- HyperMill holder wire `4a2f84eacc`; material→type→brand tooling DB `96b7849416`; per‑tool SFC ceiling `592133f902`; holder type→brand DB `6e0c8cba8c`; Mastercam exportFromTools entrypoint `ed312a4d91`.  
- Material→type→brand tree for Mastercam & hyperMILL `149c64da28`.  
- JMCustomerVendorDatabaseEngine (`361e4710e1`) wired to `prism_business`; DocuStrataMaterialPriorEngine (`818870ea59`) wired to `prism_data`.

**DECISIONS**  
- Fleet‑reaper ownership moved alpha→golf; `/checkin-golf` non‑skippable, legacy golf‑slot write allowlist disabled.  
- Re‑enable durable PRISM Fleet Reaper task; restore 9/9 hygiene cluster.  
- Use 15‑agent workflow to analyze ultracode sources; build GRPO normalizer core RL component.  
- Add `rankTrajectories` method on `MultiModelConsensusEngine`; adopt GRPO for advantage normalization (bounded rank‑fallback, NaN handling).  
- Set `OLLAMA_KEEP_ALIVE=30m`, `OLLAMA_MAX_LOADED_MODELS=4`; register MCP Priority Guardian to prevent priority inversion.  
- Replace synthetic holder logic with shared `HolderSelectionEngine` (taper, shank‑bore fit, type); keep synthetic fallback only when no catalog match.  
- Use proven tool‑library CSV format for Fusion; prioritize Fusion → Mastercam → hyperMILL wiring.  
- Adopt real‑holder wiring across all CAM exporters; type→brand ordering for holders, material→type→brand for tooling.  
- Defer HyperMill per‑tool SFC until holder leg complete; add Mastercam subset export to mirror HyperMill’s `exportToHMT`.  
- Use `toolCatalogEngine` as sole source; drop fragile CSV adapter; ship full 13238‑tool catalog (no trimming).  
- Fix ESM loader (`__dirname → import.meta.url` + dist/data fallback); keep slimResponse contract with explicit found/count.

**OPERATOR DIRECTIVES**  
- “Make sure fleet reaper is running and monitor it often.”  
- “Finish all remaining tasks.”  
- Explicit “build” command after transcript – trigger highest‑leverage unit build (GRPO).  
- “build” – continue building next ULTRACODE order.  
- “continue” – proceed with current work.  
- “applty all fixes and look for more gap fills.”  
- “go back git tree work.”  
- `/loop [10m] /goal … max out database potential benefits…` (clear: complete all remaining romeo tasks).  
- “All tool holder, tooling, inserts and machine databases are added to fusion, hypermill, mastercam, hsmadvisor, gwizard, prism sfc, mill and lathe wizards apps and galaxies, cad and cam galaxies.”  
- User command “go” → proceed to Mastercam holder wiring (Fusion already wired).

**FINDINGS/BUGS**  
- Legacy golf‑slot allowlist blocked writes; bulk disable of 46/47 PRISM tasks restored hygiene cluster.  
- `tournament-rank.mjs` seed‑bias bug fixed; GRPO base function `ok` logic simplified.  
- `makeInput` helper dropped `group_advantage`; corrected to forward field.  
- Reaper sweep loop died on transient failure → replaced with resilient monitor.  
- Ollama keep_alive=-1 triggered memory‑pressure gate (~70 GB commit); TTL change resolved.  
- Reaper reported 11 idle/mid‑compact peer crashes; functioning correctly.  
- Stale comment `STD_FLOOR(1e‑9)` fixed; WSL commit bloat traced to Ollama, not WSL.  
- MCP server flapping mitigated by Priority Guardian (does not restart).  
- Installer for WSL guard failed due missing token – fixed.  
- MCP server crash loop between watchdog polls; current restart restores service.  
- HSMAdvisor adapter reads only first `<Tool>` element – bulk export gap.  
- HyperMILL/Mastercam exporters capped at 5k tools; cap lifted to 100 k.  
- No standalone Fusion holder‑library golden exists; only tool‑library CSVs with holder columns.  
- HyperMill export `tool_length` floor‑clamp bug fixed (real holder gauge > tool length).  
- CoatingMult substring‑shadow bug (`"altin".includes("tin")`) resolved.  
- gpt‑oss offload reasoning in `thinking`; set `num_predict≥3000`.  
- Potential slug collisions – injective‑slug guard and ISO‑segment sanitization added.  
- `catalogLoader.ts` crashed under ESM (`__dirname` undefined); fixed with `import.meta.url`.  
- `slimResponse` stripped null/empty data; tests now assert explicit found/count.  
- JMDB and DocuStrata engines had no real consumers (audit false‑positive).  
- Path resolution issue: engines used `process.cwd()` vs repo root.

**DOMAIN SPECIFICS**  
- Engines: `GroupRelativeRewardNormalizerEngine`, `MultiModelConsensusEngine` (`rankTrajectories`), `GWizardToolCribExportEngine`, `HolderSelectionEngine`, `FusionToolExportEngine`, `MastercamToolExportEngine`, `HyperMillToolExportEngine`, `UltimateSpeedFeedEngine`, `toolCatalogEngine`, `JMCustomerVendorDatabaseEngine`, `DocuStrataMaterialPriorEngine`.  
- Actions/dispatchers: `group_normalize_reward` (lazy import), `prism_ai:group_normalize_reward`, `prism_ai:rank_trajectories`, `prism_cam:fusion_export_tool_library`, `prism_cam:hsmadvisor_export_settings`, `businessDispatcher` (8 actions), `dataDispatcher` (5 actions).  
- Metrics: reaper sweep JSON (`slots["owned-by-crashed"]`, `softRelief.{…}`, `gpu.{freeMb}`); 14/14 engine tests, 7/7 dispatcher round‑trip tests; 19+ unit tests for machine export.  
- Data structures: GRPO advantage `(r_i – mean)/(std + ε)`; NCTools table (`max_spindle_speed`, `max_feedrate`), Materials table (ISO‑group Vc/fz); type→brand tree, material→type→brand tree, per‑ISO tooling data.  
- Paths: `H:/prism/.claude/helpers/chat-slots.mjs`, `scripts/fleet-reaper-sweep.mjs`, `hooks/golf-slot-write-allowlist.mjs`, `src/engines/*`, `src/dispatchers/*`, `resources/FUSION360/...`, `jm-fusion-tools/by-type-brand/<ISO>/<type>/<brand>.csv`.

**TOOLS USED**  
PRISM: skills `/checkin-golf`, `/forge7`; PDF corpus ingestion (`pdf-learn`), table extraction, source registry; dispatcher (`calcDispatcher.ts`); workflow synergy analysis; G‑Wizard exporter, HSMAdvisor exporter; camDispatcher, prism_calc.  
Node/TS: `node H:/prism/.claude/helpers/chat-slots.mjs`, `tsc`, Vitest/Jest for round‑trip and unit tests, ESLint.  
LLM offload: gpt‑oss:120b, qwen2.5-coder:32b (sequential).  
Git hooks: c-to-h-mirror, scrutiny gate, commit staging.  
Scripts/hooks: `fleet-reaper-sweep.mjs`, `05-soft-config-tweaks.ps1`, `singleton-service-guard.mjs`.  

**OPEN THREADS**  
- Operator‑scope tasks disabled: Brain Refresh, NN‑Graph Retrain, Blueprint OCR Batch, orchestrators – need approval to enable.  
- Cross‑slot india/sierra engines pending; verify reaper cluster health after next run.  
- Verify `group_advantage` persistence in ledger post‑production writes.  
- Unpushed commits backlog (~2 733).  
- Wiki track‑vs‑ignore policy for auto‑generated action pages – operator approval needed.  
- HSMAdvisor bulk export – new reader/adaptor required.  
- MCP crash‑loop root cause (memory/heap) investigation pending owner.  
- WSL guard stability monitoring; no auto‑shutdown due to live containers.  
- Wire hyperMILL holder selection into its exporter; organize tooling libraries by material→type→brand; ensure all exporters emit real holders.  
- #23 step 2 Mastercam/HyperMill tree replication – source‑fidelity decision (OPT‑1 vs OPT‑2) pending.  
- Expand `HolderSelectionEngine` to include REGOFIX/TUNGALOY catalogs once extraction pipeline ready.  
- Add per‑app SFC‑optimized tooling DB for HSMAdvisor, G‑Wizard, lathe/mill wizards.  
- Next dormant‑engine wiring: `ERPImportEngine`, `SemanticAssetIndexEngine`, `LocalEmbeddingEngine` – write tests and live validation before commit.  
- Audit comment‑mention false positives review; future audits to ignore non‑import comments.  
- Post‑compact re‑init loop with fresh context to resume unwired‑engine queue.
