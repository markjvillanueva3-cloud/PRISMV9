# pdf-corpus session 91364a5d (2026-05-27, 32.1MB, spine 151KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `3cd8a1aba4` – PDF‑extraction layer on slot/kilo (kilo attribution).  
- `01317726f5` – CAM‑knowledge index (360 nodes, 111 CAM PDFs).  
- `32a707ec22` – Absorption of extraction artifacts into oscar peer commit.  
- `d8bd95f102` – Self‑learning loop substrate (classifier, orchestrator, outcome wire).  
- `f6118295d1` – Toolpath tip retriever + end‑to‑end loop test.  
- `7957b8a48b` – 100 % catalog coverage commit (224 toolpaths, 115 catalog‑only gaps).  
- `35bd64890d` – Final closed‑loop deliverable chain; handoff to other slots.  
- `cdaa42f0e6` – YouTube tribal harvest (142 tips across 6 CAMs).  
- `67f6f4dbc1` – Mastercam + HyperMILL deep‑dive (415 YouTube tips).  
- `98d88a71be` – Pypdf TRIBAL + WIKI extraction (4 477 pages, 150 curated tips).  
- `ed02805d58` – Fleet training corpus inventory (8 sources catalogued).  
- Closed‑loop training loop 1 (75/61 outcomes, 11 promos, retrain fired).  
- Closed‑loop training loop 2 (75/75 outcomes, 4 demotions, retrain fired).  
- Closed‑loop training loop 3 (4 892 outcomes, 150 promos, 74 dems, retrain fired).  
- PDF extraction of 10 hyperMILL vendor manuals → 2 532 pages, 86 notable.  
- Video‑learn pipeline ready for the 37 OPEN MIND MP4s (local batch).

**DECISIONS**  
- Use `/checkin‑kilo` slot‑binding wrapper → isolated work on slot/kilo branch.  
- Adopt pypdf extraction for PDFs; youtube‑free‑extract for video transcripts.  
- Build exhaustive toolpath catalog (224 entries) and always‑emit generator to guarantee 100 % coverage.  
- Deploy kNN‑Jaccard classifier + FSM orchestrator → closed‑loop self‑learning.  
- Keep all training‑corpus work in slot/kilo; defer LLM inference to echo/india slots (`defer‑cam‑to‑echo`).  
- Use hyperMILL vendor manuals + OPEN MIND E‑Learning videos as primary training corpus.  
- Extract PDFs with pypdf; fallback to Tesseract OCR when text empty.  
- Replace Claude Vision with Ollama Qwen2.5‑VL (free, local).  
- Build a geometry‑verification gate before Delta emits STEP.  
- Accept Mastercam X8 as legacy seat; no free upgrade path.

**OPERATOR DIRECTIVES** *(verbatim)*  
- “link pdfs for the cad cam software in the resources folder to the cad and cam node domains.”  
- “check lima academy that might touch into cam for wiki and tribal knowledge.”  
- “run next batch of tasks before utilizing everything gathered by all chats to use in the training pipelines.”  
- “generate nodes for ALL text and content (convert to html if it would be helpful) … generate wikis and tribal knowledge if we haven’t done so already.”  
- “extract page‑by‑page of notable data that will train the system from the easiest input to complex work.”  
- “look at all machining concepts… see if there are avenues of inventions or enhancements utilizing our vast math and science resources.”  
- “do more deep research hunting for pdfs and books for cad drawing 2d and 3d, assemblies … blisks, turbines and molds.”  
- “build everything we need to get to 100 % coverage for closed‑loop self‑learning… capable of using every single tool path type with all button input for each field that pops up in each cam software.”  
- “pass them off to corresponding chat slots … finish building the closed loop learning system then run it in yolo‑mode.”  
- “run next loop”  
- “scale to 50k”  
- “build everything”  
- “deep dive and assess what else we need to do to improve print reading and cad file reading.”  
- “continue from where you left off.”  
- “since we'll be generating a tone of templates and memories, follow delta's example of using wiki for memory storage on how to do certain parts.”  
- “whats next on our task list for cam?”

**FINDINGS / BUGS**  
- `pdf-parse` v2 exit 255 crash → outer try/catch added.  
- Missing CHM extraction for Mastercam (39 .chm files).  
- FunctionIndex engines shallow; only HyperMILL deep (~1.7 MB).  
- Blueprint extraction crashed after 738 PDFs → per‑PDF error handling.  
- Ollama `/api/chat` dead (100 % timeout); NN/GNN tier‑5 AUROC ungraded.  
- Lathe domain tribal and wire‑EDM tips missing; catalog‑only gaps (115 entries).  
- OOM from hook `xmalloc` during large extraction runs.  
- Windows path‑separator bug → machine inferred as `unknown`.  
- Silent unit conversion fallback (inch→mm) caused wrong CAD generation.  
- OCR detection false positives: 90 k+ scanned PDFs flagged as text‑extractable incorrectly.  
- F3D new binary format (`Manifest.dat` + `.smb`) – SQLite parser unusable; only metadata extractable.  
- 99.1% “direct” classification artifact of defaulting `material='generic'`; real PMI needed.  
- Geometry‑verification gate missing → turbine drawings mis‑classified as generic.

**DOMAIN SPECIFICS**  
- Engines: `TemplateApplicabilityClassifierEngine`, `SelfLearningLoopOrchestratorEngine`, `OutcomeFeedbackWireEngine`, `ToolpathTipRetrieverEngine`, `VideoLearningEngine`, `BlueprintOCREngine`, `PrintToCADOrchestratorEngine`, `CADReverseTemplateEngine`, `StepImportEngine`, `TesseractBridge`, `OllamaVisionBridge`.  
- Dispatchers: `/checkin‑kilo`, `camDispatcher`, `prism_cam`.  
- Metrics: 78 561 training‑ready pages, 224 toolpaths cataloged, 115 catalog‑only gaps, 4008 PDFs classified, 4 477 extracted pages from TRIBAL + WIKI; closed‑loop outcomes (75/61→11 promos, 75/75→4 demotions, 4 892→150 promos, 74 dems).  
- Key paths:  
  - `H:/PRISM/resources/HYPERMILL/hyperMILL/31.0` & `/33.0` (E‑Learning MP4s).  
  - `H:/PRISM/JM DIE/OKUMA/hyperCAD-S and hyperMILL Online Training/PDF`.  
  - `H:/PRISM/JM DIE/FUSION CAD AND CAM FILES` (6 593 files).  
  - `H:/PRISM/resources/CAD FILES` (40 STEP/STP).

**TOOLS USED**  
- PRISM: `/checkin`, `chat-slots.mjs`, `audit-roadmap-drift.mjs`, `system-viz-query.mjs`, `build-state-snapshot.mjs`, `system-synergy-map.mjs`.  
- Extraction: `pdf-parse`, `pypdf` (Lima), `youtube-free-extract.mjs`, Tesseract OCR, `extract-cad-cam-pdf-content.mjs`, `extract-pdf-pages-curriculum.mjs`, `generate-complete-toolpath-coverage.mjs`.  
- Video: FFmpeg + Whisper + Claude Vision → replaced by Ollama Qwen2.5‑VL (`qwen2.5vl:7b`).  
- OCR bridge, TesseractBridge, OllamaVisionBridge.  
- Node/TS build system, Python scripts, Git.

**OPEN THREADS**  
1. Acquire & parse Mastercam .chm help files; complete function index depth for all primary CAMs.  
2. Finish blueprint extraction (2975 PDFs) fully crash‑free.  
3. Train NN/GNN tier‑5 model; revive Ollama LLM inference.  
4. Fill catalog‑only gaps with tips (115 entries) via targeted video searches or vendor docs.  
5. Integrate closed‑loop training run into echo/india slots; schedule periodic retraining.  
6. Resolve OOM in hook during large extraction runs.  
7. Ship F3D metadata extractor (Tier A free win).  
8. Install Tesseract OCR on the host.  
9. Pull `qwen2.5vl:7b` into Ollama and expose a lightweight bridge.  
10. Wire BlueprintOCREngine & PrintToCADOrchestratorEngine into `prism_cam`.  
11. Run full JM corpus coverage with real PMI (post‑OCR/Ollama).  
12. Implement multi‑view 3D feature reconstruction for turbine cases.  
13. Commit all uncommitted changes and tag the current state.  
14. Finalize Mastercam licensing decision (stay X8 + lean Fusion).
