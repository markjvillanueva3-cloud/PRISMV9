# pdf-corpus session 2ae65067 (2026-05-27, 31.1MB, spine 88KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED (commits & units)**  
*U‑OSC9‑08* – `SpeedFeedShopLibraryBridgeEngine` (ShopTool → MRR ranking) – 35/35 tests, 4 P1s fixed inline – commit `7c9643f7f0`.  
*U‑OSC9‑09* – `HSMAdvisorAdapterEngine` (UTF‑16/8 detector) – 31/31 tests – commit `32a707ec22`.  
*U‑OSC9‑10* – `SpeedFeedPDFCorpusBridgeEngine` (kilo/fleet/mike nodes) – 33/33 tests – commit `aefecf1676`.  
*U‑OSC9‑11* – `HSMAdvisorComparatorBridgeEngine` – 28/28 tests – commit `e6c390fc2b`.  
*U‑OSC9‑12* – G‑Wizard & HSMAdvisor library exporters – 52 tests – commit `9c99921260`.  
*U‑OSC9‑13* – `WedmTrainingPairBridgeEngine` – 26/26 tests – commit `88ff0daef9`.  
*Wire‑Fix* – dispatcher actions wired to main tree (engines only on slot/oscar) – commit `be173cf2b5`.  
*Envelope* – milestone JSON registered – commit `f32c99216a`.  
*Memory* – SFC domain map, wear taxonomy, divergence analysis, etc. – commits `8119a98333`, `7706c534a3`, `96fd10e055`.  
*Machine‑Scale Exporter* – 1066 machines with full torque curves applied to HSMAdvisor – commit `4a3e31195f`.  

**DECISIONS (architecture & scope)**  
- Slot‑binding wrapper (`/checkin-oscar`) used to guarantee handoff before `/checkin`; auto‑enforced hook prevents double‑claim.  
- All vendor adapters and exporters run in `slot/oscar` worktree to avoid main‑tree lock contention; later merged into main after wire‑fix.  
- Parity strategy: export PRISM tool catalog → G‑Wizard CSV & HSMAdvisor XML, then use same catalog for both vendors so comparison is fair.  
- Machine physics: replace generic spindle envelopes with real torque curves from `machine-torque-curves.ts` (1058 machines).  
- Divergence root‑cause investigation moved to `UltimateSpeedFeedEngine.alternatives.balanced.vc`; plan audit A→B→C sequence.  
- Wear taxonomy & diagnostic engine shipped; image classifier deferred (Tier‑3 CLIP k‑NN pending dataset).  

**OPERATOR DIRECTIVES**  
- `/goal` – resume milestone, pull all references, tasks, units for SFC.  
- `/loop [5m] /goal /yolo-mode` – schedule recurring 5 min loop with immediate execution of `/goal`.  
- Stop hook: “primary domain is speed‑feed calculator…pull up all references…” – automatically triggers until condition met; no manual clear needed.  

**FINDINGS/BUGS**  
1. HSMAdvisor XML declared UTF‑16 but stored UTF‑8 → fixed via BOM sniffing (iter 2).  
2. ShopToolLibraryBridgeEngine had `tool_diameter_mm` vs `diameter_mm` mismatch – TS gate caught and fixed (iter 1).  
3. Silent wire‑break: dispatcher actions on main referenced slot‑only engines; detected by `git ls-files` audit, fixed in wire‑fix commit.  
4. Weak_disagreement 84.6% structural due to missing SFM/IPT in aggregated catalogs – resolved with catalog joiner (iter A).  
5. Divergence analysis: PRISM’s balanced Vc ~70 % above baseline DB for N‑aluminum; root cause traced to `UltimateSpeedFeedEngine.alternatives.balanced.vc`.  

**DOMAIN SPECIFICS**  
- **Engines**: `SpeedFeedShopLibraryBridgeEngine`, `HSMAdvisorAdapterEngine`, `SpeedFeedPDFCorpusBridgeEngine`, `HSMAdvisorComparatorBridgeEngine`, `GWizardLibraryExporterEngine`, `HSMAdvisorLibraryExporterEngine`, `WedmTrainingPairBridgeEngine`, `MachineTorqueCurveExporterEngine`, `SpeedFeedCatalogJoinerEngine`, `ToolWearDiagnosticEngine`.  
- **Dispatchers**: `prism_calc:*` actions (e.g., `sfc_shop_library_rank`, `hsmadvisor_read_current_state`, `sfc_pdf_corpus_bridge`, `hsmadvisor_compare`, `gwizard_read_toolcrib`, `wedm_training_pair_lookup`, `tool_wear_diagnose`).  
- **Metrics**: Vc, MRR, chatter stability, weak_disagreement, divergent cells, gear‑ratio‑based speed envelopes.  
- **Paths**: `/checkin-oscar` wrapper → slot binding → `/checkin`; CronCreate for recurring loops; `slot/oscar` worktree path `H:/prism-slot-oscar`.  

**TOOLS USED**  
PRISM utilities: checkin wrapper, slot‑claim helper (`chat-slots.mjs`), CronCreate/Cancel, dispatcher action registry, vitest test harness, tsc compiler, Zod schema validation. External: Node.js, Git, Bash scripts for reclaim/claim, JSONL parsing, XML regex extractor, CSV writer, SQLite reader (G‑Wizard).  

**OPEN THREADS**  
- **Audit A**: trace `UltimateSpeedFeedEngine.calculate()` on worst divergent cell to confirm balanced Vc vs baseline DB.  
- **Machine‑axis sweep** in TriVendor comparator once divergence fix applied.  
- **Holder cross‑product**: export real holder geometry into vendor tools and PRISM chatter engine.  
- **Image classifier for wear** (Tier‑3 CLIP k‑NN) to auto‑detect wear mode from photos.  
- **Future units**: U‑OSC9‑14 AI training, U‑OSC9‑15 G‑Wizard SQLite extraction, U‑OSC9‑16 HSMAdvisor material‑ID→ISO mapping.  

*Cron job `44b59162` cancelled; loop ended at iter 13/20.*
