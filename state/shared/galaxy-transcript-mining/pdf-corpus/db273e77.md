# pdf-corpus session db273e77 (2026-06-10, 158.4MB, spine 1339KB, 14 of 16 slice(s), HEAD+TAIL sampled, model gpt-oss:20b)

**SHIPPED**  
- `e5ad4ea802` (U‑ALPHA‑HWSYNC‑RETUNES) – 9 files, 3/3 scrutiny PASS.  
- Background pull task `bittzospv`: gpt‑oss:120b & gemma4:31b (~3 h @6 MB/s).  
- `632335cec6` (U‑GOAL‑CLEAR‑ADVANCE) – goal‑clear → next‑unit fallback hook.  
- `c83ca9be64` (F5 autoresume staleness fix) DEFAULT_MAX_AGE_MIN 240→720.  
- `8cd8d615e9` (slot‑domain‑awareness‑inject dedup).  
- `eefef0359a` (psn‑leg‑state‑inject dedup).  
- `636d36bf59` (memory‑relevance‑inject semantic recall, F3).  
- `75c44d8412` (self‑refreshing cache for F3).  
- `1f295f51e6` (dedupedContext helper) used across 8 injectors.  
- `8d2b6cf8e0` (foxtrot/delta/xray injectors dedup).  
- `87e96fa485` (whiskey/charlie‑awareness/echo‑post dedup).  
- `afe169ab0e` (charlie‑knowledge dedup, completes queue item #1).  
- `9fcda446a1` (task‑boundary compact‑nudge stop‑hook for /compact).  
- `61a6288d0e` (verified search re‑rank unit A+C PASS, B rate‑limited).  
- `7dec157db0` (HS‑01 root‑cause fix – explicit --session-id anchor).  
- `e81dec5cba` (env‑anchor patch CLAUDE_CODE_SESSION_ID).  
- `92301e5574 + 8119ab7cb6` (commit‑msg drafter).  
- `1175a6f26b + 8c57f02d77` (line‑anchored file digest).  
- `e928ef5010 + 71c817c2a2` (loop‑narrate).  
- `0acb1dcbc9 + 31fae0eaf8` (large‑read advisory hook).  
- `127234e940 + 6980943ba8` (Ollama navigation re‑rank).  
- `60805c36c6 … 1edf02aa26` (memory→wiki promotion advisor Q11).  
- `792beb75e8 + dea23630a2` (dead foreign‑machine path fix in handoff-memory-seed.mjs & distill-session-learnings.mjs).  
- `530afadcfa` (commit‑pressure‑relief script triggers on commit %).  
- `4b299e313b` (nav‑advisory hardening parser).  
- `1dd17250b3` (batch embeddings embedTextBatch()).  
- `6162fd99af` (grep test ENOENT bug fixed – node path → process.execPath).  
- `35dc2ec4c3 + d92b9f52f4` (subagent bundle OOM fixes & regex fix).  
- `caabc8fea6` (Q1 tribal reference nodes materialized).  
- `U‑AUTOSTART‑LOOP‑GOAL`, `U‑SUBAGENT‑GALAXY‑PACK`, `U‑GOAL‑FORCE‑100PCT`, `U‑DOCREFLECT`.  
- `U‑OLLAMA‑NAV‑ENFORCE`, `U‑DARK‑WIKI‑RANK`, `U‑LINT‑ORPHAN‑OOM`, `U‑GRAPH‑STREAM‑DEGREE`.  
- `U‑VERIFIED‑OFFLOAD‑NAV`, `U‑VERIFIED‑OFFLOAD‑NAV‑WIRE`, `U‑COMMIT‑PRESSURE‑RELIEF`, `U‑NAV‑ADVISORY‑HARDEN`.

**DECISIONS**  
- Alpha slot → golf owns fleet‑reaper; 47 scheduled tasks disabled, no RESTORE‑PRISM‑TASKS.  
- Docker/NIM audit only – runtime unchanged until hold cleared.  
- Commit via pathspec to avoid absorbing peer churn; do not wire `stop_on_failing_tests.mjs` fleet‑wide.  
- Let stalled pull resume from partials; do not kill hung client.  
- Adopt ultracode workflow to verify gaps before fleet‑wide fixes; async auto‑lint-post-edit.mjs over sync eslint hook.  
- Move cross‑galaxy gate ownership of quality from golf to alpha; hand model pull to golf via durable task (`ollama-resilient-pull.ps1`).  
- Use detached `Start‑Process pwsh` for model pull; register textbooks via `registry.json`; add fleet‑fallback in loop engine; hook into Stop (goal‑clear).  
- Replace hardcoded `"node"` with `process.execPath`; introduce env override `PRISM_GOAL_CLEAR_ADVANCE_SLOTS_JSON`.  
- Keep feature commits minimal; separate drift sync from secret changes.  
- Adopt injection‑dedup pattern across all per‑prompt injectors (~5–12 K tokens/slot/session).  
- Design hooks as fail‑open (boot‑path stale hint, semantic recall).  
- Run ultracode discovery in focused 3‑lane workflow after API rate limits recover; use three‑arm scrutiny gates.  
- Adopt native auto‑compact@90% as sole compaction trigger; task‑boundary compaction as nudge via `stop-task-boundary-compact-nudge`.  
- Self‑compaction chain: auto‑compact → precompact‑handoff.mjs → session‑start‑auto‑resume.mjs.  
- Auto‑session‑start must use explicit `--session-id`; replace 24 caller edits with single helper reading `CLAUDE_CODE_SESSION_ID`.  
- Adopt ultracode‑based verified‑offload queue (`scripts/lib/ollama-verified-offload.mjs`).  
- Skip building new units this turn due to 97 % commit pressure.

**OPERATOR DIRECTIVES**  
- Finish remaining tasks, align Ollama models with new CPU/GPU, sync Docker/NIM, verify atomic‑level synergy.  
- “Golf is busy, you do the pull please” → background pull started; no further action until completion.  
- Read three X posts via Playwright and confirm system matches prescriptions.  
- Keep pulling gpt‑oss:120b & gemma4:31b; claim quality galaxy for alpha; re‑look at X article URLs.  
- Take over model pull from golf; pull models via Ollama (gpt‑oss:120b, gemma4:31b).  
- /loop [10m] /goal … schedule recurring prompt and start goal.  
- Run `/compact` now to trigger next session with correctly keyed handoff.  
- Continue standing /goal autonomously (yolo): maximize local‑LLM utilization, enforce Ollama for reads/searches/navigation, seek token savings and context retention.  
- Prove self compaction and auto session start works so we can apply to full fleet.

**FINDINGS/BUGS**  
- Vitest‑4 regression: `poolOptions.threads` ignored → migrated to top‑level `test.*`.  
- Script encoding bug (line 45) caused PowerShell parse errors; fixed with UTF‑8 flag.  
- Partial download stalled at ~93% due to flaky registry stream; partials preserved for resume.  
- `stop_on_failing_tests.mjs` unwired; would block all chats if wired.  
- GPU health instructions cu129 → corrected to cu128.  
- Docker/NIM audit: no P0, but registry stream drop noted.  
- `enforce-eslint-after-edit.py` hard‑coded C:/PRISM path; file unwired.  
- MIT textbooks missing from PRISM corpus; ingestion pending via lima/academy lane.  
- Memory pressure spike (~98 % commit) cleared by cache drop.  
- Stop‑hook regex bug (m flag) corrupted handoff; fixed by removing `m`.  
- Claim gating bug prevented false claims on freeform prose; now gated to structured sources only.  
- Settings.json drift resolved by committing only hook entry.  
- HS‑01 stable‑session‑id mismatch miskeyed handoffs; fixed with explicit `--session-id` anchor.  
- OBSIDIAN app not running, REST API dark; recall lexical‑only → semantic recall added.  
- RESUME_LOOP regex broken; replaced with line‑based strip.  
- API rate limiting blocked ultracode discovery lanes twice; resolved by focused run.  
- Stale close‑out audit prevented goal completion; refreshed it.  
- Duplicate MCP daemon detected and reaped; commit‑pressure gate fired at 97 % due to orphan llama‑server.  
- MCP daemon down repeatedly; watchdog tasks missing `NextRun`.  
- Scheduled‑task WARN false positive verified.  
- Stop advisory block caused by shard transition of tribal‑embed index.

**DOMAIN SPECIFICS**  
Engines/Actions:  
- `MINBatchExtractorEngine.ts`, `McxBatchExtractorEngine.ts` (concurrency bump), `GpuStackHealthEngine.ts`.  
- `memory-index-search-lib.mjs`, `memory-relevance-inject.mjs`, `route-suggest` hook, `subagent-start-context.mjs`.  
- `PDFSourceRegistryEngine.ts`, `registry.json`.  
- `loop-state.mjs`, `stop-goal-clear-advance.mjs`, `stop-force-loop-continue.mjs`.  
- `ollama-resilient-pull.ps1`, MCP server health endpoint `http://127.0.0.1:3100/health`.  
Dispatchers:  
- `/checkin-alpha` slot‑binding wrapper; full `/checkin` pipeline steps 3–7 & 8–14.  
- `/startup-alpha /loop [10m] /goal`; `/checkin.md` pipeline; `CronCreate` for `/loop` scheduling; `slot-galaxy-map.mjs`.  
Metrics/Paths:  
- `OLLAMA_MAX_LOADED_MODELS`, `FLASH_ATTENTION`, `PRISM_EMBED_CONCURRENCY`.  
- Docker compose GPU passthrough, `nim-docker-launcher.mjs`.  
- H:/prism/.claude/settings.json (tracked).  
- Memory‑pressure metrics; pagefile ceilings: C:192 GB+G:56 GB → ~439 GB; H:64 GB/512 GB on reboot → ~951 GB.  
- Token‑zone thresholds GREEN/YELLOW/RED; auto‑compact at 90 %.

**TOOLS USED**  
PRISM helpers: `chat-slots.mjs`, `audit-roadmap-drift.mjs`, `slot-bind-enforce.mjs`, `precompact-handoff.mjs`, `session-start-auto-resume.mjs`, `stable-session-id.mjs`, `dedupedContext` helper, `stop-task-boundary-compact-nudge.mjs`, `ask-ollama.mjs`, `ollama-nav-rerank.mjs`.  
Ultracode workflow (multi‑agent fan‑out, adversarial verify); Hermes‑workflow; System‑viz summarizer.  
Scripts: `ollama-resilient-pull.ps1`, `kill-zombie-tsservers.ps1`, `03-memory-pressure-auto-relief.ps1`, `02-kill-zombie-tsservers.ps1`.  
Git commit mutex `U-FGC-1`; scrutiny‑3way.mjs; CronCreate; test harness (Jest/TS tests); `scripts/lib/ollama-verified-offload.mjs`.

**OPEN THREADS**  
- Background pull task `bittzospv` completion pending.  
- Playwright tweet‑reading workflow verification against three X posts incomplete.  
- Ingest MIT textbooks into PRISM corpus via lima/academy lane.  
- Finalize quality galaxy ownership mapping in `CHAT-SLOT-DOMAINS`.  
- Complete model pull of gpt‑oss:120b & gemma4:31b via golf’s lane.  
- Resolve rerank arm B rate‑limiting; consolidate dedup between search and nav rerank.  
- Verify stable‑session‑id fix fleet‑wide (env‑anchor commit).  
- Implement orphan llama‑server detector in golf’s reaper.  
- Configure MCP watchdog `NextRun` to auto‑restart daemon.  
- Optimize full‑fleet sweep speed for self‑compaction; implement per‑slot live actuation override.  
- Wire `ollamaNavRerank` to auto‑fire after `/system-viz find`.  
- Finalize and git‑track `.claude/commands/yolo-mode.md`.  
- Deploy verified‑offload queue: advisory‑hook retrofit, scrutiny pre‑screen offload.  
- Monitor long‑term conversion rates of large‑read advisory suggestions.
