# pdf-corpus session be5e37e8 (2026-05-19, 28MB, spine 135KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED (builds / commits)**  
- `62b5794101` – U‑FGE01: STEP geometry → `buildSequenceForEvidence`; added dispatcher `cad_class_build_sequence_evidence`.  
- `6cbe5b1561` – U‑BPA‑CONSUMER: offline events consumer (`blueprint-accuracy-consumer-lib.mjs`, CLI).  
- `c60f6c9396` – U‑FGE02: wired evidence ranking into live `cad_class_drive_build`.  
- `5e3048ad71` – U‑TDP01: training driver (extract → CAD → CAM, stub mode, event recording).  
- `8d533c1e10` – U‑TDP02: batch harvester for print dirs, idempotent registry.  
- `99288aec44` – U‑TDP03: extraction aggregator; per‑class Welford stats.  
- `5709f19d7b` – U‑TDP04: OCR benchmark vs CAD GT (precision/recall/F1, dimensional error).  
- `1bc36d949d` – U‑TDP05: CAD‑derived ground‑truth extractor from STEP corpus.  
- `d85573b7d4` – U‑TDP07: deterministic embedded‑text PDF extractor (inline‑± parser, OCR guard).  
- `cafd0871c1` – U‑TDP08: regex for paired‑tolerance bands & Rc hardness ranges; 77/77 tests.  
- `1576134f55` – picker‑filter commit on `cad-fusion-live-ms0`: excludes completed HOOK‑SYNERGY‑MS0 units from priority queue.  

*(U‑TDP06 – vision OCR adapter pending)*  

**DECISIONS (architecture/scope + why)**  
- Use deterministic embedded‑text path for ~3.5–13 % PDFs; zero hallucinations, high confidence.  
- Vision OCR for remaining 87 % with `qwen2.5vl:7b` (fits 10 GB RTX 3080); moondream failed, llama3.2‑vision OOM.  
- Offline consumer drains JSONL events into rolling‑window state (`blueprint-accuracy-consumer`).  
- Aggregate extractions via Welford online algorithm (U‑TDP03).  
- Benchmark extraction quality vs CAD GT using precision/recall/F1 and dimensional error (U‑TDP04).  
- Modular training driver: adapters for extract, driveCAD, driveCAM, recordEvent; swap OCR or vision backends.  
- Harvester scans directories, infers `part_class`, records processed paths to avoid re‑runs (U‑TDP02).  
- Pick non‑overlapping unit from priority queue by filtering completed milestones via name, not ID.  
- Wire U‑TDP07 into live grader once vision OCR available; current grader returns null.  

**OPERATOR DIRECTIVES**  
- `/checkin-mike /goal train cad and cam ai systems…` – start with Fusion360, then CAM driver.  
- Train print reading first: generate dimension & tolerance templates before CAD drawing.  
- Verify OCR training accuracy; use Ollama if accurate enough, otherwise explore alternatives.  
- Confirm GPU/Docker setup for vision OCR.  
- “Continue” directives to keep training pipeline moving.  

**FINDINGS/BUGS**  
- Vision models: moondream hallucinated dimensions; llama3.2‑vision OOM on RTX 3080 (fits only on RTX 4080).  
- Parser regex bugs: missing dot in tolerance parsing, split‑token dropped‑dot fabrication; OCR fragment guard needed to reject garbage fragments.  
- Embedded‑text coverage ~3.5–13 % of JM Die PDFs; 87 % image‑only.  
- Event consumer activation missing – fixed by adding `blueprint-accuracy-consumer`.  
- Metric calculation bugs: incorrect confidence thresholds, geometric-only GD&T handling; corrected in U‑TDP04/05.  
- New regex patterns raised extraction rate from 13.5 % to ~21 % on a 19‑PDF subdir; zero‑dim PDFs remain high.  

**DOMAIN SPECIFICS (engines/actions/dispatchers/metrics/paths)**  
| Component | Path / Action | Purpose |
|-----------|---------------|---------|
| `CADClassFeatureLibraryEngine.buildSequenceForEvidence` | `mcp-server/src/engines/CADClassFeatureLibraryEngine.ts` | Ranks features by evidence ratio for build sequencing. |
| Dispatcher `cad_class_build_sequence_evidence` | `mcp-server/src/tools/dispatchers/cadDispatcher.ts` | Emits evidence‑based sequence data to orchestrator. |
| `BlueprintExtractionRAGEngine` | `H:/prism/.claude/...` | Parses PDFs into extraction objects (confidence, dimensions). |
| `GroundTruthRegistryEngine` | `...` | Stores CAD/G-code ground truth for benchmarking. |
| `blueprint-accuracy-consumer-lib.mjs` / CLI | `scripts/lib/blueprint-accuracy-consumer-lib.mjs`, `scripts/blueprint-accuracy-consumer.mjs` | Drains JSONL events, maintains rolling window state.json, emits xproc actions. |
| Training driver (`U‑TDP01`) | `scripts/training-driver-print-to-cam.mjs` | Orchestrates extract → CAD → CAM stages; records events. |
| Harvester (`U‑TDP02`) | `scripts/harvest-pdf-text-batch.py/.mjs` | Scans directories, infers part_class, idempotent registry. |
| Aggregator (`U‑TDP03`) | `scripts/lib/...` | Computes per‑class mean/stddev of dimensions via Welford. |
| Benchmark (`U‑TDP04`) | `...` | Calculates precision/recall/F1 against CAD GT. |
| CAD‑GT extractor (`U‑TDP05`) | `...` | Generates ground truth from STEP geometry reports. |
| Embedded‑text parser (`pdf-text-extract-lib.mjs`) | `H:/prism-slot-mike/scripts/lib/pdf-text-extract-lib.mjs` | Parses SolidWorks split‑token and AutoCAD inline‑± formats; includes OCR fragment guard. |

Metrics tracked: precision, recall, F1 per part_class; dimensional mean ± stddev, min/max, n; confidence distribution; event counts; rolling window size.

**TOOLS USED**  
- Slot‑claim helpers (`chat-slots.mjs`).  
- `/checkin` pipeline (`commands/checkin.md`).  
- Drift audit (`audit-roadmap-drift.mjs`).  
- System viz ping, CLAUDE.md staleness checks.  
- Per-file scrutiny gate (2 parallel reviewers).  
- 3‑of‑3 stop gate; precompact guard.  
- Scripts: `pdf-text-extract-lib.mjs`, `training-driver-print-to-cam.mjs`, `harvest-pdf-text-batch.py/.mjs`, `blueprint-accuracy-consumer-lib.mjs`, `blueprint-accuracy-consumer.mjs`.  
- Hooks: `stable-session-id.mjs`, `per-agent-handoff.mjs`.  
- External tools: PyMuPDF (`fitz`) for PDF→PNG, Ollama API (vision models), Docker Desktop with GPU passthrough.  
- Priority‑queue and shipped‑units source of truth dispatchers.

**OPEN THREADS**  
1. Wire U‑TDP07 into U‑TDP04 grader (`makeLiveExtractor` must return deterministic extractor).  
2. Implement vision OCR adapter (U‑TDP06) using `qwen2.5vl:7b` or similar that fits 10 GB VRAM; integrate in U‑TDP01.  
3. Full AI training loop – fine‑tune/LoRA vision model on extracted data, generate CAD drawings from prints.  
4. CAD drawing generation pipeline (U‑FGE02): convert dimensions/features into Fusion360 models.  
5. CAM program generation: feed Fusion360 outputs to CAM driver; validate CNC programs against ground truth.  
6. Scale harvest across entire JM Die corpus – run U‑TDP02 on all 76 K PDFs, update coverage metrics.  
7. Robust parser patterns – add support for Inventor, SolidEdge exports.  
8. Metric dashboards & alerts – expose precision/recall/F1 and dimensional error trends in system viz.  
9. Automated regression tests – ensure new vision model does not regress on deterministic path.  
10. Run full 600‑PDF corpus delta measurement to quantify extraction rate gains.  
11. Add comma‑decimal normalization, MAX/MIN qualifiers, slash‑paired tolerance handling (U‑TDP09).  
12. Resolve zero‑dim PDF cases: classify job sheets vs OCR‑garbled scans; fallback heuristics or manual labeling.
