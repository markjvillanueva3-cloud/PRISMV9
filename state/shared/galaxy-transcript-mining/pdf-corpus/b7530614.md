# pdf-corpus session b7530614 (2026-05-19, 21.9MB, spine 85KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-VIZ-F11-CROSS-LOCK` – 5‑file sidecar build with shared `.system-graph-write.pid` lock (commit 4022e99606).  
- `U-CLEAR-AUTO-RESUME` – auto‑resume wiring in `settings.json` (commits 5b16e56c68 & c004ad1cb8).  
- `U-MASTER-INDEX-SIDECAR` – inverted‑index sidecar for system index issue (commit 291646f427).  
- `U-ACTIVATE-BEFORE-BUILD-PRECHECK` – ensures master‑index search preflight satisfied (committed after lock sweep).

**DECISIONS**  
- Fleet‑reaper ownership moved from **alpha** to **golf**; alpha no longer owns reaper.  
- Chose sidecar over raising `PRISM_GRAPH_MAX_BYTES` due to OOM risk; sidecar size ~105 MB, load <1.5 s.  
- Implemented shared `.system‑graph-write.pid` lock across `regen-viz`, `system‑viz-on-commit`, and `system‑viz-add-node`.  
- Adopted per‑agent handoff + precompact guard; used stable‑session‑id for terminal binding.  
- Loop‑state with high‑ROI ordering, 5 min tick interval.

**OPERATOR DIRECTIVES**  
- `/goal complete all tasks in queue high roi tasks first /loop [5m] /goal`  
- “Continue work from earlier today.”  
- “Fix system index issue” (resolved via sidecar).  
- Build `U-MASTER-INDEX-SIDECAR` per spec at `state/shared/specs/UNITS/U-MASTER-INDEX-SIDECAR.md`.

**FINDINGS / BUGS**  
- `__isMain` guard in `claude-md-golf-only-guard.mjs` prevented main() from running; fixed by adjusting path comparison.  
- Raising `PRISM_GRAPH_MAX_BYTES` to 512 MB caused >138 s load and ~1.6 GB RSS → fleet OOM; reverted.  
- Git `index.lock` contention due to peer commits; resolved via `git-lock-sweeper` and pathspec commit.  
- Initial sidecar schema mismatch; fixed by aligning node shape with `searchGraphHits`.

**DOMAIN SPECIFICS**  
- Engines: `build-graph-index` (generates sidecar), `master-index-search-lib` (search engine).  
- Actions/dispatchers: `slot-task-claim`, `per-agent-handoff`, `chat-slots`, `audit-roadmap-drift`.  
- Metrics: `system-graph.json` 372 MB, `architecture-graph.json` 28 MB; sidecar ~105.6 MB; loadGraph <1.5 s after sidecar.  
- Unique paths: `H:/prism/scripts/build-graph-index.mjs`, `scripts/lib/master-index-search-lib.mjs`, `.claude/hooks/claude-md-golf-only-guard.mjs`, `system-viz-add-node.mjs`, `regen-viz.mjs`, `system-viz-on-commit.mjs`.

**TOOLS USED**  
- PRISM tools: `chat-slots.mjs`, `per-agent-handoff.mjs`, `precompact-pending-guard.mjs`, `slot-task-claim.mjs`, `audit-roadmap-drift.mjs`, `build-graph-index.mjs`, `master-index-search-lib.mjs`.  
- Hooks/skills: `claude-md-golf-only-guard.mjs`, `system-viz-add-node.mjs`, `regen-viz.mjs`, `system-viz-on-commit.mjs`, `seed-ghost-from-unwired.mjs`, `session-start-auto-resume.mjs`.  
- Scripts/tests: `build-graph-index.test.mjs`, `master-index-search-lib.test.mjs`.

**OPEN THREADS**  
- Next highest‑ROI unit pending in alpha queue (not yet started); handoff resume should target that unit.  
- Ensure precompact guard cleared and no stale git lock remains.  
- Monitor downstream consumers for regressions after sidecar integration.
