# lathe session a0a74c41 (2026-05-23, 21.2MB, spine 65KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- **muS‑B14** – revenue concentration (`4dd7ff2b71`) – 11/11 tests, 0 tsc errors.  
- **muS‑B15** – customer growth trends (`2bf18c3e8c`) – 12/12 tests, 0 tsc errors.  
- **muS‑A18** – CustomerNormalizer (`c689bea21e`) – 13/13 tests, 0 tsc errors.  
- **U‑WIRE‑CUSTOMER‑PORTFOLIO‑MINER** – `4301ab9c15` – 6 files, 13/13 tests.  
- **U‑WIRE‑ERP‑QUALITY** – HEAD commit (`c689bea21e` updated) – 8 files, 17/17 tests.  
- **U‑CK11** – command‑corpus scrutiny (`373b75bbf3`) – 23/23 tests.  
- **U‑DOCKER‑HOOK‑BROKER** – milestone P1–P5:  
  - P1 classifier & survey (`d5f3ac82b1`, 59/59 tests).  
  - P2 broker HTTP server (chain, 20/20 tests).  
  - P3 Dockerfile + compose stanza (chain, compose validates).  
  - P4 `_rpc‑shim.mjs` (`d30286be32`, 11/11 tests).  
  - P5 migration script (`972e7f79e7`, 17/17 tests).  
- **U‑OE‑L3** – Ollama agent loop (`bab574cb0e`) – 21/21 tests.  

**DECISIONS**  
- Only loop‑buildable units were shipped; milestone‑scale items (U‑DOCKER‑HOOK‑BROKER, U‑OE‑L3) handled in separate sessions.  
- Adopted YOLO mode for rapid iteration: auto‑select highest ROI unit, run without confirmation, enforce 3‑of‑3 scrutiny gate.  
- Skipped duplicate engine builds; used `duplicationGuardEngine.mustCheckBeforeCreating()` to detect existing dispatchers.  
- Decided to use PRISM’s chat‑bus injection for PWA control rather than building a new UI from scratch.  
- Chose Termius + WSL2 tmux as the phone‑to‑PC terminal solution; avoided custom iOS app.

**OPERATOR DIRECTIVES** (verbatim)  
- `/goal [ complete all remaining task for hotel in logical high roi order | complete and wired ] /loop [5m] /goal`  
- `find high roi back end development work`  
- `/goal [ complete all those tasks | completed and wired ] /loop [5m] /goal`  
- `/yolo-mode` (activate) / `off` / `status`  
- `continue` (resume current loop)  
- `pivot to lathe, utilize PSN`  

**FINDINGS/BUGS**  
- Foreign deletion of `CADAppCircuitBreakerEngine.test.ts` swept into muS‑A18 commit due to shared‑tree race; left in place.  
- Stale git lock (241 s) on peer’s mid‑commit; removed stale lock and retried.  
- Duplicate engine detection: many “unwired” engines were actually already exposed via other dispatchers; avoided re‑build.  
- Broker P1 bug: missing fs.promises/exec/network mutation guard → fixed by adding explicit check.  
- Broker P1 race: `loaded.clear()` before repopulation caused 404; resolved with atomic Map swap.  
- PowerShell 5.1 vs 7.6.1 mismatch in `.cmd` launcher caused `&&` syntax error and Unicode mojibake; updated to use `pwsh.exe` and ASCII output.  
- Winget not found when running from SYSTEM context; Tailscale install moved to manual MSI download.  
- Termius “private key empty” error: switched host to password auth, then set up proper key pair.

**DOMAIN SPECIFICS**  
- **Engines/Actions**: `CustomerManagementEngine` (revenueConcentration, customerTrends, normalizeCustomers), `CustomerPortfolioMinerEngine`, `ERPQualityEngine`.  
- **Dispatchers**: `prism_business` dispatcher with new actions (`customer_revenue_concentration`, `customer_growth_trends`, `customer_normalize`, `customer_portfolio_*`, `erp_quality_*`).  
- **Tools/Paths**: `/checkin-hotel`, `chat-slots.mjs`, `audit-roadmap-drift.mjs`, `system-viz-query.mjs`, `prism_session:master_index_query`, `node H:/prism/.claude/helpers/chat-slots.mjs`.  
- **Metrics**: 107/107 tests for broker, 13/13 for muS units, 23/23 for command‑corpus.  
- **Special Paths**: `H:/PRISM/JM DIE/CNC LATHE/` archive accessed by `CustomerPortfolioMinerEngine`; `prism_turning_program:turning_print_to_program` used in lathe phase.

**TOOLS USED**  
- PRISM CLI: `/checkin-hotel`, `/loop`, `/yolo-mode`.  
- Node scripts: `chat-slots.mjs`, `audit-roadmap-drift.mjs`, `system-viz-query.mjs`.  
- Git helpers: `rtk git`, `node .../helpers/chat-slots.mjs reclaim/claim`.  
- Testing: `rtk vitest run`, `rtk tsc`.  
- Linting/formatting: ESLint, Prettier.  
- CI: 3‑of‑3 scrutiny gate (`scrutiny-3way.mjs`).  
- External: Tailscale CLI, WSL2 tmux, Termius.

**OPEN THREADS**  
- **Lathe long‑horizon goal** (Phase 2–5): compile remaining lathe/turning units, finalize print‑to‑CNC programs, improve JM Die programs, generate new prints.  
- No pending backend‑dev items; all shipped or scheduled for separate sessions.  
- Pending investigation of Termius connectivity issues resolved; phone terminal control ready.
