# lathe session 96e6ce13 (2026-05-25, 11.4MB, spine 49KB, 1 slice(s), model gpt-oss:20b)

**Goal acknowledged:** complete the full Prism app test suite and validate full adaptability & variability coverage.  

---

### Immediate action plan

1. **Run the comprehensive test harness**  
   - Trigger `/forge7` with `--full-suite --coverage 0.98`.  
   - Capture results in `state/shared/specs/PRISM-TEST-SUITE-2026-06-13.json`.

2. **Validate coverage & adaptability metrics**  
   - Use `/system-viz-query.mjs` to pull current coverage graphs for each domain (mill, lathe, wedm, cam, post‑processor).  
   - Verify that each axis of the variability matrix (controllers, machines, cycles, materials, tools, holders, coatings, coolants, workholding) has ≥ 90 % representation in the test set.

3. **Generate missing adaptability/variability tests**  
   - Invoke `scripts/generate-adaptivity-tests.mjs` with parameters:  
     ```bash
     --controllers all \
     --machines all \
     --cycles all \
     --materials all \
     --tools all \
     --holders all \
     --coatings all \
     --coolants all \
     --workholding all
     ```
   - Merge generated tests into the existing test suite.

4. **Run PSN integration checks**  
   - Execute `/psn-integration-test` to ensure that all public APIs are exercised by at least one test case and that PSN logs are produced correctly.

5. **Verify system‑viz consistency**  
   - Run `/system-viz-validate.mjs` against the latest `ghost.launch_readiness` roost.  
   - Confirm that no orphaned nodes or missing edges exist for any of the newly added tests.

6. **Final pass & report**  
   - If all steps succeed, emit a consolidated status file:  
     `state/shared/specs/PRISM-TEST-SUITE-COMPLETION-2026-06-13.md`  
   - Include metrics: total tests run, coverage percentages, adaptability score, PSN hit rate.

---

**Execution will commence now.**
