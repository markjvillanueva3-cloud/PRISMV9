# lathe session 8b4b9149 (2026-06-09, 38.2MB, spine 431KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `5ae481f748` – fixed EPERM leak in `OutcomeCaptureBusEngine` (atomic‑append rewrite, orphan‑tmp cleanup).  
- `86f0e3fe0c` – wired three engines (`SpeedFeedTriComparatorEngine`, `SpeedFeedExhaustiveCombinationEngine`, `SpeedFeedDownstreamSubscriberEngine`) into dispatcher; added 16 round‑trip tests.  
- `a2dbfa76e1` – guard `aligned !== false` in external‑filter for consensus; regression test for median integrity.  
- `43e1b8e449` – refined consensus logic: misaligned advisories visible only when no aligned external exists.  
- `16d6eecef4` – added `SpeedFeedCalibrationPersistEngine`; unit & dispatcher tests.  
- `890b2c39e7` – VC assessment spec addendum (closed‑loop clarification).  
- `3438987f0e` – hardened calibration engine (clamped flag, skipped rows, persist tmp cleanup).  
- `f31398a1a5` – added `SpeedFeedGpuJudgeEngine`; dispatcher wiring.  
- `f5d14ddb29` – hardened GPU judge (probe‑exact match, fetch timeout, loud‑on‑zero‑judgeable).  
- `951f5ac335` – polishing commit (single‑source probe flag, suppression test).  
- `3a1c20fca2` – full 69 120‑cell sweep engine + `runStreaming` generator.  
- `af0ac16c5a` – fixed ReferenceError & ISO‑group collapse; added lock test.  
- `b847c0f1ff` – spec for axis‑awareness enhancement (tool material, coolant, holder, machine, spindle, controller, workholding, insert).  
- `28eca0ed84` – GPU judge spec addendum (`matched_model`, `vram usage`).  
- `54c0ae06c8` – memory file recording GPU‑judge evidence.  
- `658c8280fe` – canonical tool‑material speed factor table added; explicit‑only safety guard.  
- `585584e3ae` – wired existing `CoolantVcModifier`; removed duplicate table.  
- `7d0affcae6` – de‑inlined rigidity factor to `constants.ts`.  
- `f998f8af71` – hoisted tool‑material, coolant & rigidity multipliers into primary Vc path and alternatives; ensured orchestrator forwards them.  
- `e9b68da865` – initial tool‑material table commit (pre‑de‑inline).  
- `da41a58fd1` – full 102 060‑combination sweep; 733 distinct Vc values.  
- `c48829e331` – axis‑liveness probe (8 live, 4 inert).  
- `d03458fff1` – extended probe to tool_life (runout→life) – 1.03×/1.02× on life.  
- `2070c472a4` – workholding clamp‑cap wired via validated `ClampingForceEngine`.  
- `7dc7798033` – spindle‑hp → power clamp added; 1.13×/5.41× on feed/MRR.  
- `8b191d5427` – runout→life applied to tool_life (no double‑count).  
- `a257f872bb` – updated probe script with `toolpath.strategy`, `cut_type`, `mode`.  
- `0a146e22c6` – controller‑look‑ahead hygiene fix; physics‑reviewer ruled inert on default.  

**DECISIONS**  
- Adopt first‑principles physics engine (~20 models) + 9‑axis shop‑context model.  
- Exclude `aligned === false` externals from consensus median; misaligned advisories visible only when no aligned external exists (balance safety & operator insight).  
- Treat live vendor data as “un‑aligned” unless proven speeds exist; exclude un‑aligned systems from consensus calculation.  
- Wire engines into dispatcher: add actions for tri‑comparator, exhaustive combination, downstream subscriber.  
- Build two‑layer closed‑loop: CPU calibration (`SpeedFeedCalibrationPersistEngine`) + GPU judge (`SpeedFeedGpuJudgeEngine`).  
- Expand exhaustive engine to full 69 K combos; provide streaming API (`runStreaming`).  
- Add canonical constants block for axis multipliers (tool material, coolant, rigidity); apply only when explicitly specified.  
- Wire `CoolantVcModifier` into engine; de‑inline rigidity factor.  
- Apply axis factors in primary Vc path and alternatives; ensure orchestrator forwards them.  
- Keep controller inert on default per physics review; add safety‑positive MRR adjustment for non‑aggressive modes.

**OPERATOR DIRECTIVES**  
- Select auth provider (Clerk, Supabase, Auth0).  
- Decide whether to ship provider‑independent webhook signature fix immediately.  
- Populate G‑Wizard `toolcrib.csv` with computed S/F per tool (closed Adobe‑AIR UI; no API).  
- Run Blackwell `qwen2.5‑vl` OCR batch over 287 catalog PDFs for richer baseline.  
- Decide on controller philosophy: default recommendation inert vs adjusted via MRR realism factor.  
- Schedule recurring `/loop` prompt every 10 min to run calculations across all machine/spindle/controller/material/workholding/holder combinations and compare with G‑Wizard/HSMAdvisor.

**FINDINGS/BUGS**  
- EPERM leak caused 11 888 orphan tmp files in `OutcomeCaptureBusEngine`.  
- G‑Wizard toolcrib contains zero proven surface‑speed values; HSMAdvisor returns constant 634.6 m/min for all materials (unaligned).  
- Consensus calculation incorrectly included unaligned external systems, producing impossible titanium value (634 m/min).  
- Tri‑comparator defaults to first G‑Wizard tool when no selector provided.  
- Tool‑material, coolant and rigidity axes were inert; orchestrator did not forward them → under‑speeding (~25 % mean ΔVc, worst –53 % on 6061 Al).  
- G‑Wizard `toolcrib.csv` geometry‑only; HSMAdvisor data files lack pre‑computed cutting speeds.  
- Several axes (spindle.hp, workholding.type, tool_holder.type/runout, controller) inert by design; others live after probe.  
- MRR coupling partially affected by new multipliers; requires reconciliation.

**DOMAIN SPECIFICS**  
- Engines: 41 total (Kienzle, Taylor, Merchant, Johnson‑Cook, Altintas SLD, Gilbert economic speed, etc.).  
- Dispatcher actions: 158 existing + 3 new (`speed_feed_calibration_persist`, `speed_feed_gpu_judge`).  
- Live vendor data paths: G‑Wizard `toolcrib.csv` (~12 MB), HSMAdvisor `settings_v2.xml` (~9 KB).  
- Ledger: 85 MB outcome ledger with ~7 782 rows.  
- Metrics/fields: `ledger_rows_skipped`, `gpu_resident`, `matched_model`, `vram_used_mib`, Vc, rpm, feed, stability margin, DOC, MRR.  
- Unique paths: `sfc-full-sweep-compare.mjs`, `src/physics/constants.ts`, `src/engines/speed-feed/UltimateSpeedFeedEngine.ts`, `src/algorithms/CoolantVcModifier.js`, `src/data/<vendor>-speed-feed-data.ts`.

**TOOLS USED**  
- PRISM helpers (`chat-slots.mjs`, slot‑bind‑enforce hook).  
- `/hermes-workflow` (dynamic workflow planner).  
- `/forge-audit-v2` (audit‑discipline engine).  
- Ultracode multi‑agent orchestration.  
- Node scripts for git ops, slot claiming.  
- Vitest/Jest test harness.  
- `fs.appendFileSync` atomic append.  
- Ollama inference server (`qwen2.5-coder:32b`, `gpt-oss:120b`).  
- Vision‑OCR via `qwen2.5-vl`.  
- CronCreate for recurring `/loop`.  
- Git hooks (scrutiny script, commit hooks).  

**OPEN THREADS**  
1. Implement provider‑independent webhook signature fix.  
2. Finalize auth‑provider selection.  
3. Run full test suite to confirm no regressions after consensus filter change.  
4. Update documentation/comments for misaligned advisory inclusion rule.  
5. Add tool‑material axis (speed factor multiplier, tests, safety gate).  
6. Add coolant physics (thermal/speed factor).  
7. Model holder/machine/spindle/controller/workholding/inserts physics; wire to orchestrator.  
8. Resolve MRR reconciliation with new multipliers.  
9. Implement U‑OSC‑RIGIDITY‑DOC: flow rigidity/holder/spindle stiffness into stability‑lobe analysis and DOC cap.  
10. Vendor calibration: run vision‑OCR batch on Kennametal & other catalogs; generate `<vendor>-speed-feed-data.ts`; calibrate `CUTTING_PARAMS`.  
11. Full sweep vs G‑Wizard/HSMAdvisor after calibration; capture ΔVc statistics.  
12. Decide controller philosophy (default inert vs MRR realism adjustment).  
13. Ensure galaxy resources mapped to folder.  
14. Obtain populated `toolcrib.csv` with speed/feeds if possible.
