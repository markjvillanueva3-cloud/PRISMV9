# lathe session 5c91e392 (2026-06-09, 16.2MB, spine 150KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `b76b4d55ab` – Resumable OCR loop (cursor + multi‑page rasterization).  
- `265e8a6e41` – 7 794 drawing prints all‑pages OCR; two reliable VLMs selected.  
- `5a8dbfefdd` – Doc‑reflection updates (`CLAUDE.md`, wiki, memory).  
- `cdc9ec44b3` – PowerShell launcher + Task Scheduler installer for nightly closed‑loop training.  
- `01ce06d589` – Recommitted lost commit (manifest builder & multi‑page fix).  
- `250ffb8a6e` – 91‑part “perfect” list, `find-perfect-parts.mjs`, tests, JSON report, updated GT extraction.

**DECISIONS**  
- Use `/checkin-xray` slot‑binding wrapper for handoff & drift checks.  
- Replace weak‑label loop with stream‑append + resume cursor (reaper‑immune).  
- Rasterize PDFs page‑by‑page (96 % multi‑page) to avoid data loss.  
- Select only `qwen3‑vl:8b‑instruct` and `qwen2.5vl:7b`; discard unreliable models.  
- Deploy closed‑loop training as reaper‑immune scheduled task via Task Scheduler.  
- Keep GPU+CPU fully unblocked (torch 2.11+cu128, bitsandbytes 4‑bit, trl/peft/datasets).  
- Adopt 3‑of‑3 PRISM reviewer gate; fallback to single reviewer on rate‑limit.  
- Use Qwen2.5‑VL + Qwen3‑VL ensemble for OCR; ground truth from CNC feed‑move program.  
- Replace raw toolpath count with feed‑move‑aware GT and binary guard.  
- Sanitize filenames (`/`→`_`, `:`→`_`) before globbing; remove erroneous `basename()`.  
- Reclassify mill programs (G17, Y‑axis) to avoid false lathe GT.

**OPERATOR DIRECTIVES**  
- Read previous xray sessions for context.  
- Upgrade OCR & blueprint reading with new GPU/CPU; wire, test, validate then start closed‑loop training on all jm/docustra prints.  
- Use 91‑part list as true test before full corpus.  
- `/checkin-xray` – force‑take `xray` slot and run standard `/checkin`.

**FINDINGS/BUGS**  
- Page‑0 OCR bug → 76 % dimensions lost; fixed by page‑by‑page rasterization.  
- Worklist reader mis‑treated `#` comments as missing prints; now skipped.  
- GPU contention caused one VLM timeout during calibration; scheduled task runs in quieter window.  
- Cursor file had bogus “comment” entry; removed.  
- Trainset rows not idempotent on replay; added `key` field and last‑wins dedup.  
- Missing commit `01ce06d589` lost during `/compact`; re‑committed.  
- Server‑side rate limit caused reviewer failures; passed after retry.  
- GT extraction inflated denominator (121 + toolpath points).  
- Binary `.mcx‑8` files misparsed as text → garbage coordinates.  
- Print resolver failed on filenames with `/` or `:`; fixed by sanitizing first.  
- Mill program `9102741.hnc` incorrectly classified as lathe, inflating GT counts.  
- OCR recall on real scanned PDFs ≈ 0 %; precision ≈ 0 %.

**DOMAIN SPECIFICS**  
- Engines/Actions: `blueprint-ocr-training-loop.mjs`, `build-print-corpus-manifest.mjs`, `xray-trainset-to-lora.mjs`, `find-perfect-parts.mjs`, `cad-analyze-step.mjs`.  
- Dispatchers/Skills: `/checkin-xray` wrapper, `chat-slots.mjs`, `slot-bind-enforce.mjs`, scrutiny script (`--target HEAD`), ledger entries.*, stop gate (`opusReviewed/claudeReviewed/codexReviewed`).  
- Metrics: `trainable_labels`, `tier_totals`, `active_learning_queue`, `ensemble_failed`, `missing`; calibration trust floor `f=0.5`, `f=1`.  
- Paths: `H:/prism/.claude/commands/checkin.md`, `~/.claude/commands/checkin.md`, `scripts/run-ocr-batch-overnight.ps1`, `install-blueprint-ocr-batch-task.ps1`, `blueprint-program-join-full-v6.jsonl`.

**TOOLS USED**  
- PRISM slot‑binding wrapper (`/checkin-xray`).  
- Chat‑slot helpers (`chat-slots.mjs`).  
- Slot‑bind‑enforce hook (`slot-bind-enforce.mjs`).  
- OCR runner (`blueprint-ocr-training-loop.mjs`).  
- Manifest builder (`build-print-corpus-manifest.mjs`).  
- Trainset‑to‑LoRA wrapper (`xray-trainset-to-lora.mjs`).  
- PowerShell launcher & Task Scheduler installer.  
- PRISM CLI (`checkin`, `scrutiny`, `ledger`, `task scheduler`).  
- Scripts: `find-perfect-parts.mjs`, `cad-analyze-step.mjs`.  
- Test harnesses: mutation‑verified unit tests, 2‑reviewer gate.

**OPEN THREADS**  
- Step 6: Run full LoRA fine‑tune on overnight trainset (HF base model pull & quiet GPU).  
- Operator gold: Acquire real‑scan operator‑verified dimensions to close domain‑shift gate (STEP 4/7).  
- Ensemble concurrency: Explore adding third VLM once reliable.  
- Performance tuning: Optimize GPU usage under fleet contention; schedule calibration in lower‑load windows.  
- Deployment: Finalize production pipeline for continuous closed‑loop training & monitoring of trainable label yield.  
- Full “true test” on larger sample beyond 4 fixed parts.  
- Improve OCR recall for real scanned PDFs before scaling to full corpus.  
- Finalize and publish verdict report; commit remaining artifacts.
