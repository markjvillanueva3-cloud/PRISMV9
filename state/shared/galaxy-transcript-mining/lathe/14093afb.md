# lathe session 14093afb (2026-06-26, 17.2MB, spine 146KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `1567dba6f1` – U‑W2: *lathe‑closed-loop-full.mjs* (full driver over 34 993 .MIN).  
- `d291e3abf0` – U‑W5: *LatheLoRASafetyEvaluatorEngine* wired to dispatcher.  
- `c88511610c` – U‑W6: tribal classifier + advisory matcher; 694 tips extracted, advisory surfacing per part.  
- `df75ed69c` – U‑W8: Kienzle in‑page rename (brand='Kienzle').  
- `f0c768e293` – U‑W2 documentation commit (master‑plan progress log).  
- `412bd3ecab` – U‑W6 coverage run (21 STEP files, 51 non‑turned parts skipped).  
- `6d723b68f5` – FF2 hallucination guard added to tribal classifier.  
- Kienzle build (`H:/KIENZLE APP BUILD.zip`) – FE↔BE wiring verified live; G4 handoff written for Quebec.  
- U‑W9: closed‑loop Rung‑B grid regrounded in JM’s tool‑steel stock (60 programs, 0 errors, 100 % SFM envelope, 96.3 % feed).  
- U‑W10: bounded‑concurrency + in‑flight guard on `MaterialRegistry.load()`; 3989 materials load, no EMFILE/OOM at 4 GB.  
- U‑W11: in‑flight promise guard for `MaterialRegistry`; harness finishes cleanly (0 s).

**DECISIONS**  
- Adopt pure‑JS STEP geometry leg (`lathe-rungc-step-loop.mjs`) to close Rung C, avoiding GPU OCR dependency.  
- Implement fleet‑floor safety envelope (max 3800 rpm / 11 kW) as most restrictive JM lathe; guarantees never‑false‑SAFE.  
- Surface tribal knowledge only as advisory (<5 % real signals); no parametric adjustment.  
- Declared U‑W4 & U‑W5 wired; dedup‑guard prevents duplicate work.  
- Proceeded with U‑W9 after confirming grid empirical validity.  
- Fixed `MaterialRegistry.load()` by bounding concurrency and adding in‑flight guard; eliminated EMFILE/OOM.  
- Routed remaining cross‑lane Quebec FE gaps & Oscar shared‑core loader issue to owners.

**OPERATOR DIRECTIVES**  
- “max lathe tribal 57→500 via /pdf‑learn + /video‑learn + /lathe‑learn on ~80 PDFs/videos + 6 MIT courses.”  
- Stop only on genuine spiral or all units done (then loop‑state end + 3‑of‑3 scrutiny).  
- Read handoff & master‑plan files each fire.  
- Build next dependency‑ordered unit (U‑W2 → U‑W8).  
- Apply deduplication guard before any new asset.  
- Run reference‑value tests (≥3 happy, ≥2 adversarial) through dispatcher; per‑file 2‑arm scrutiny.  
- Commit each unit with `[MAIN‑FORCE] [KIENZLE‑LATHE‑WIZARD]/U‑ID` adding only own files.  
- Tick loop state (`loop-state.mjs`) after each commit.  
- Run audit‑close‑out‑candidates every ~3rd fire.  
- Route mechanical text to Ollama; reserve Claude for physics/safety/synthesis.

**FINDINGS/BUGS**  
- Fixed path‑casing double‑count in STEP enumeration.  
- Corrected `sfm_max` drift (mm vs ft/min); bounded [20, 3000] ft/min.  
- FF2 hallucination guard added to prevent false feed_factor.  
- Low tribal corpus yield (<5 %) with one hallucinated case; not wired into generation.  
- OCR leg remains GPU‑bound; pending future GPU availability.  
- EMFILE “too many open files” in `MaterialRegistry.load()` eliminated by concurrency bound & in‑flight guard.  
- Kienzle build’s FE↔BE wiring live; no dead wire.  
- U‑W9 grid accurately models JM’s real tool‑steel stock (annealed‑P turning).

**DOMAIN SPECIFICS**  
- Engines/Dispatchers: `BlueprintVisionOCREngine`, `TurningPrintIntakeEngine`, `runPipeline`, `LatheLoRASafetyEvaluatorEngine`, `turningDispatcher` (Okuma).  
- Metrics/Paths: `full_geometry_loop_closed`, `safety_basis`, `steps_scored`, `paired`, `tribal_advisory`.  
- Unique Paths: `/lathe-closed-loop-full.mjs` orchestrates Rung A, B, C; `lathe‑rungc‑step‑loop.mjs` runs STEP → profile → TurningInput → program → score.  
- Tribal corpus: `state/shared/lathe‑tribal‑corpus.jsonl`.  
- Safety envelope logic (max rpm, power limits).

**TOOLS USED**  
- PRISM helpers: `duplicationGuardEngine`, `audit-close-out-candidates.mjs`, `loop-state.mjs`.  
- Ollama for mechanical text extraction.  
- Node/TSX scripts: `lathe-closed-loop-full.mjs`, `MaterialRegistry.ts`.

**OPEN THREADS**  
- Provide video/MIT sources for remaining tribal “/video‑learn” pass (no local lathe videos).  
- G4 frontend design concept (Quebec lane) – requires external handoff.  
- OCR leg remains GPU‑bound; future work pending GPU availability.  
- Cross‑lane Quebec: 10 UI‑gap Kienzle FE surfaces (Shop‑Floor Live → Scheduling → Inventory → Blueprint/OCR/CAD → CAM/Lean → Accounting/GL/Sales).  
- Oscar/shared loader inefficiency mitigated by in‑flight guard; no further action needed.
