# lathe session 52bebb83 (2026-06-03, 5.9MB, spine 29KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Lathe adapter bound in `PipelineHarnessAdaptersEngine.ts` (commit `3e9b3e8667`).  
- Accuracy harness (`TurningPrintToProgramEngine.runFullPipeline`) shipped with 26/26 tests green, tsc clean.  
- Training loop for per‑op SFM/IPR calibration committed (same commit).  

**DECISIONS**  
- Use existing `TurningPrintToProgramEngine` as the core engine; wrap it in a lathe adapter (`makeLatheAdapter()`).  
- Replace stubbed “lathe” domain with real binding, update devDispatcher gate to use `isBound('lathe')`.  
- Add CJS shim for `__dirname` in `catalogLoader.ts` to allow raw TSX execution.  
- Expose material‑default disclosure as a lower‑bound caveat in accuracy reports.  
- Adopt closed‑loop learning: parse `.MIN → TurningInput`, generate program, diff vs master, train per‑op calibration on train split, evaluate on held‑out test.  

**OPERATOR DIRECTIVES**  
- Continue training until 100 % accuracy (closed‑loop self‑learning).  
- Use `/goal /loop /yolo` to drive iterative improvement.  

**FINDINGS/BUGS**  
- Lathe adapter was a stub → headless roundtrip impossible.  
- Over‑budget stage verdicts are only warnings; failure requires missing/out‑of‑order payload or handoff break.  
- Test failures due to unbound lathe domain and hard‑coded dispatcher gate (`domain !== "mill"`).  
- Git index.lock contention caused stale lock errors; resolved by detecting age > 30 s and clearing.  
- OOM during harness teardown (memory pressure from 193 fleet loops); fixed with graceful `process.exit`.  
- Missing material‑default disclosure misled accuracy interpretation.  
- `__dirname` shim required for TSX imports; without it engine crashes.  

**DOMAIN SPECIFICS**  
- PRISM Lathe Studio pipeline: upload → feature extraction → material identification → machine/tool selection → physics calculations → speed/feed optimization → G‑code generation → safety checks → setup sheet → backplot → download.  
- `TurningPrintToProgramEngine.runPipeline(input)` returns `TurningProgramResult` (6 stages, `tool_id` handoff).  
- Metrics: op‑coverage, SFM in‑band, IPR in‑band, mean accuracy.  
- Ground‑truth cloud: 16 558 Okuma `.MIN` programs; G96 literal SFM p50 = 200, G50 safety = 97.5 %.  

**TOOLS USED**  
- PRISM CLI wrappers (`/checkin-whiskey`, `chat-slots.mjs`).  
- Node scripts for slot reclaim/claim.  
- Git with slot‑commit‑enforce hook.  
- Lathe Studio web UI (http://localhost:3000/lathe).  
- API endpoints `/api/v1/lathe/*`.  
- Dispatcher actions (`prism_turning_program:*`).  
- TSX harness, Vitest, tsc for type checking.  
- `curl` health check, `npm start` for MCP server.  

**OPEN THREADS**  
- Close the 30‑point accuracy gap: material inference from `.MIN`, shop‑profile calibration override.  
- Reduce memory pressure during training loops (optimize data structures).  
- Validate no leakage in train/test split; formal audit of randomization.  
- Finalize documentation and dashboards (`state/shared/dashboards/lathe-roundtrip-accuracy.{json,md}`).  
- Integrate continuous loop into galaxy workflow for ongoing self‑learning.
