# lathe session 6eaa145c (2026-06-26, 7.3MB, spine 41KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `U-W6-VISION` – auto‑discovery lib + vision fallback (3 files) → 30+ live machining tips, 11/11 tests, two P1 bugs fixed.  
- `U-W8-RENAME-INPAGE` – Kienzle rename on Lathe Wizard UI (page title, pricing label, launch button).  
- `U-W-STEP-PROFILE` – STEP → rotational‑profile extraction core (4 files) → live validation on real FASTENAL OKUMA part.  
- `U-W-STEP-SEGMENT` – multi‑body segmentation (`selectBestBodyProfile`) with suspect flagging; live validation on AGRATI OP2 part.

**DECISIONS**  
- Use `/checkin-whiskey` wrapper to guarantee slot binding before standard `/checkin`.  
- Reuse existing vision lib (`ollama-vision-extract-lib.mjs`) instead of rebuilding OCR stack.  
- Implement a pure discovery lib (`lathe-tribal-corpus-discover.mjs`) to replace hard‑coded PDF list.  
- Add vision fallback for image‑heavy PDFs; flag `--vision`/`--vision-model`.  
- Exclude milling docs via `MILL_EXCLUDE` guard and word‑boundary filtering.  
- Detect Ollama config errors as retriable, not permanent skips.  
- Switch revolution axis from bbox center to centroid; add `suspect:true` when symmetry score high.  
- Add non‑finite vertex rejection in mesh conversion.  
- Introduce `pick_ambiguous` flag for ambiguous body selections.  
- Register a dedicated “PRISM Lathe Tribal Drain” task (every 17 min, `--limit 1`) to grow tribal count toward 500.

**OPERATOR DIRECTIVES** *(verbatim)*  
- “assess how much more we need to build for this feature, how to properly set up a comprehensive, exhaustive closed‑loop test of the system utilizing ALL jm die prints, jobs, cad models, cad/cam programs, g code programs, and other available data.”  
- “build all back end and front end design for lathe wizard utilizing claude design sessions from today design concept and name change to Kienzle.”

**FINDINGS/BUGS**  
- P1: filter contamination – milling docs incorrectly entered into lathe corpus. Fixed with `MILL_EXCLUDE` guard.  
- P1: silent permanent skip on Ollama config error – now retriable.  
- P1: axis center bug (bbox vs centroid) – switched to centroid, added suspect flag.  
- P2: non‑finite vertex rejection – added in mesh conversion.  
- P2: single‑body double‑count audit list – fixed `candidates_evaluated`.  
- P2: `pick_ambiguous` flag for ambiguous body selections.  
- Missing scheduled drain for lathe tribal ingestion (fixed by registering new task).  
- Design‑concept artifact missing; rename only, no full FE redesign.

**DOMAIN SPECIFICS**  
- Engines/Actions: `StepImportEngine`, `TurningCADImportEngine.importSolid()`, `TurningPrintToProgramEngine.convertBlueprint()`, `TurningInput` contract, `lathe-rungc-ocr.json`.  
- Dispatchers/Tools: `/checkin-whiskey`, `chat-slots.mjs`, `occt-import-js`, `ollama-vision-extract-lib.mjs`, `lathe-tribal-ollama-ingest.mjs`, `drain-resources-tribal.mjs`.  
- Metrics/Paths: `full_geometry_loop_closed` flag, tribal PDF count (target ≥500), `.MIN` score per part, `suspect:true` for unreliable geometry.

**TOOLS USED**  
- PRISM CLI wrappers (`/checkin-whiskey`, `/handoff`).  
- Node helpers (`chat-slots.mjs`, `reclaim`, `claim`).  
- Git tooling (`git rev-parse`).  
- Vision OCR via Ollama (`qwen2.5vl:7b`).  
- OCCT JavaScript wrapper (`occt-import-js`).  
- PDF parsing (`PyMuPDF`/`fitz`).  
- Scheduled task registration (PRISM drain tasks).  

**OPEN THREADS**  
- Wire STEP profile → `TurningInput` → Rung C driver and flip `full_geometry_loop_closed` (iter 6/7).  
- Final FE redesign using missing design‑concept artifact or new `frontend-design` session.  
- Ensure tribal drain task fully processes all lathe PDFs to reach ≥500 tips.  
- Validate body‑segmentation on a broader set of JM parts; handle multi‑body exports that are truly clean turned bodies.  
- Integrate the new `pick_ambiguous` flag into downstream gating logic.
