# lathe session 1730eb7c (2026-05-27, 127MB, spine 288KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commits: `84db861343` (lathe‑wizard bridge), `38bcc524f5` (PDFs+JSON), `5121df34cd` (vendor master index + query helper), `88c1b98e48` (8 vendor PDFs & curriculum manifest), `6fda303ee1` (per‑page PDF extractor, 186 pages → JSONL), `4d08d27a` (cron `*/5 * * * *` for `/goal /yolo-mode`).  
- Video transcripts: U‑LATHE‑VIDEO‑LIVE‑C‑AXIS (iter 40), U‑LATHE‑VIDEO‑FINISH‑PASS (41), U‑LATHE‑VIDEO‑MULTI‑TASKING (42), U‑LATHE‑VIDEO‑G76 (44), U‑LATHE‑VIDEO‑SUB‑SPINDLE (45), U‑LATHE‑VIDEO‑HARDENED‑CBN (46), U‑LATHE‑VIDEO‑ALUMINUM (47), U‑LATHE‑VIDEO‑BAR‑FEEDER (48).  
- Iteration ranges: 49‑52 (JM‑fleet controller coverage); 53‑60 (Y‑axis turret ops, parting/grooving, etc.); 61‑70 (probing, tool offset, G74/G75 cycles, G54 setup, polygon turning); 71‑80 (nose radius, materials, CAM bridges, tooling brands); 81‑90 (coolant, MQL, cycle‑time, gear cutting, IoT); 91‑100 (GD&T, reference state, AB‑locator memo, training-loop stages, validator scaffolds); 101‑107 (GD&T implementation, reference capture); 108‑114 (shop‑tool bridge, tribal-query engine, wizard vendor lookup, AB‑version locator, G76 validator, loop stage 4/5); 115‑120 (CBN expansion, vendor graph node design, media ingest pipeline, PDF download workflow, machine vendor models); 121‑127 (design memo synthesis, verification checklist, validator test scaffold, commit, real‑data validation); 128‑135 (CRLF preservation, operation inference, T‑format normalization, drilling cycles, byte‑exact passthrough, comment extraction, controller‑aware G70, A‑marker stripping); 136‑143 (P0 engine implementation, E2E wizard proof); 144‑155 (real‑data driven fixes & regression tests); 156‑167 (further real‑data validation across 4 customers, bug fixes); 168‑185 (session‑final memos, breadcrumbs, sentinel JSON, README updates); 186‑193 (plateau acknowledgments); 194‑198 (operator‑facing templates: tool‑list ingestion, PDF ingestion, MCP dispatcher action, CLI scan‑runner, TypeScript wiring); 199‑215 (meta‑README indexing, AB‑locator CLI scanner, regression tests, JM‑Die archive scan, customer‑rootfile fix, README banner syncs, empirical v2.0.0 upgrade pattern finding, Stage 4 detector added, consolidated session‑end memo); 216‑226 (score flag, empirical upgrades, new detectors, test passes); 227‑236 (consolidated memo, idle iterations); 237‑242 (idle); 243‑246 (idle); 247‑248 (idle); 249 (ACME score result); 250 (durable cron `8505e156` replaces dead session‑only cron; entry‑point memo updated); 251‑255 (AGRATI & CAMCAR scans, body‑diff investigations); 256‑260 (AB‑locator over‑pairing finding, pair_type field, metric corrections); 261‑264 (ALCOA outlier retraction, detector rationale rewrite, helper extraction commit, wiki lesson); 265‑267 (parseBlocks comment contamination bug fix, regression test); 268 (full trace appended); 269‑270 (empty_source detection added); 271 (unsatisfiable loop documented); 272‑274 (idle); 275‑277 (classifyPairType helper committed); 278‑279 (cross‑customer matrix extended to ACUMENT & SFS; SFS anomaly resolved); 280‑282 (ACME unchanged after locator fix); 283‑284 (retraction memo updated with 6‑customer matrix); 285‑286 (README banner sync, entry‑point memo updated); 287‑301 (added customers 7–20 to cross‑customer matrix; all showing 100 % A=B parity); 302 (closed‑loop training roadmap memo); 303 (PPGSFCClosedLoopOrchestratorEngine built, 6‑phase SFC → LoRA train → Brier eval → promotion); 304‑313 (customer scan loop under `/yolo-mode`); 316 (deep‑dive synthesis memo `reference_lathe_wizard_build_wire_plan_2026_05_27.md`); 317 (`prism_ai:submit_sfm_override` wired; operator override capture persists in `overrideHistory`).  

**DECISIONS**  
- Adopt non‑terminal `/yolo-mode` doctrine: cron auto‑fire (`*/5 * * * *`) triggers one concrete iteration; stop hook blocks stopping until condition holds.  
- TDD contract‑first workflow: test scaffold → implementation → integration tests.  
- Modular engine architecture: separate modules per P0 unit (G76 validator, shop‑tool bridge, tribal‑query, wizard vendor lookup, AB locator, training‑loop stages).  
- Real‑data validation primary feedback; bugs discovered via JM‑Die programs.  
- Prioritize operator‑facing templates for next session (tool‑list ingestion, PDF ingestion, MCP dispatcher action, CLI scan‑runner, TypeScript wiring).  
- Replace session‑only cron with durable `8505e156`; delete after use.  
- Treat v2.0.0 B‑versions as pure annotation pass‑through; retract ALCOA outlier claim.  
- Implement `empty_source` pair_type to filter degenerate inputs.  
- Resolve AB‑locator priority bug (`filename_has_b_suffix`) for canonical B selection.  
- Document unsatisfiable loop between `/goal /yolo-mode` and doctrine; plan escape hatch.

**OPERATOR DIRECTIVES**  
- Install stop hook for `/goal /yolo-mode`; block stopping until condition holds.  
- `/goal [extract wiki …]`, `/goal clear`.  
- Wire `prism_lathe:query_vendor_tribal` MCP dispatcher action (~50 lines).  
- Delete cron `8505e156`.  
- Build operator‑override capture CLI (`prism_ai:submit_sfm_override`) and persist adapter registry to disk.  
- Ingest real shop tool lists (iter194) and PDF ground‑truth (iter195).  
- Add to memory anything lathe machining related.

**FINDINGS/BUGS**  
- Regex `String.match()` lacked global flag → missed multi‑occurrence counts; fixed with `matchAll`.  
- R12 fail‑loud threshold raised from 1 to 2 on `controller-haas`.  
- Cron interval rounding: 90 m rounded to 1 h (0 */1 * * *).  
- CRLF preservation bug in Stage 5 round‑trip.  
- Missing G‑code operation inference for G81/G85/G87.  
- Incomplete T‑format normalization (Mazak 6‑digit ↔ Fanuc 4‑digit).  
- Byte‑exact passthrough regression when no levers applied.  
- Operator T‑block comments not used as operation hints.  
- Okuma G71 dialect misidentified → spurious missing G70; controller‑aware detection added.  
- A‑marker stripping in AB‑locator caused unpaired versions; fixed to strip both A and B markers.  
- PRISM_UPGRADED folder pattern mismatch → 0 paired → fixed (iter200).  
- Customer‑rootfile misclassification → 195→118 customers (iter203).  
- v2.0.0 upgrade pattern incorrectly claimed for ALCOA; real body unchanged (iter261).  
- `parseBlocks` matched G‑codes inside parenthesized comments → false G40/G80 detection (iter265).  
- AB‑locator over‑pairing: human‑revision `-B.nc` selected over canonical B (iter256).  
- Empty source files produced 116‑line header only; flagged as `empty_source` (iter270).  
- SFS G80 anomaly due to locator bug, not pipeline behavior (iter280).

**DOMAIN SPECIFICS**  
- Engines/modules: LatheAITrainingEngine, LatheCSSOptimizerEngine, LatheChipMechanicsEngine, LathePostProcessor, BoringBarDeflectionEngine; `lathe-g76-thread-validator.mjs`, `lathe-shop-tool-library-bridge.mjs`, `lathe-tribal-query-engine.mjs`, `lathe-wizard-vendor-lookup.mjs`, `lathe-ab-version-locator.mjs`, `stage‑4 reason engine`.  
- Dispatchers: `prism_lathe:query_vendor_tribal` (vendor lookup), `prism_lathe:query_video_topics` (topic index), MCP dispatcher action `prism_ai:submit_sfm_override`.  
- Metrics: physics deviation detection (`scorePhysicsIssues`), ALCOA empirical baseline (~57/100), G40/G80 parity, avg_delta per customer, pair counts (14 475 A/B pairs).  
- Paths: `mcp-server/data/ingestion_cache/lathe-tribal-master-index-2026-05-26.json`, `lathe-vendor-expansion-2026-05-26.json`, `state/shared/extracted-pdfs/whiskey-lathe-pages-2026-05-26.jsonl`, `knowledge/wiki/video-extract-*.md`, `mcp-server/data/state/lora-adapter-registry.json`, `jm-die-profile.ts`.

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`; scripts `extract-lathe-videos-tribal.mjs`, `query-lathe-tribal.mjs`, `youtube-free-extract.mjs`, `pdf-parse` v2.  
- Cron API: `CronCreate`, `CronDelete`; durable cron `8505e156`.  
- Test harnesses: Jest test suites for each extractor and dispatcher; `*.test.mjs` integration tests.  
- Real‑data validation scripts (batch wizard run, CRLF check).  
- Operator‑facing templates: tool‑list ingestion, PDF ingestion, MCP dispatcher action, CLI scan‑runner, TypeScript wiring.  
- Dispatcher framework: `prism_ai`, `prism_lathe`; Zod schemas for action validation.  
- LoRA training backend (Flow Nexus/Ollama) – to be wired.

**OPEN THREADS**  
1. Wire `prism_lathe:query_vendor_tribal` dispatcher action and expose tribal corpus.  
2. Persist adapter registry to disk (`mcp-server/data/state/lora-adapter-registry.json`).  
3. Implement real LoRA training backend (Flow Nexus/Ollama) and hook up Brier oracle with shop‑floor outcome feed.  
4. Build end‑to‑end orchestrator for print‑to‑program pipeline (sequence planner → setup selection → safety gate → program emit).  
5. Delete cron `8505e156` to stop noisy `/yolo-mode`.  
6. Implement remaining operator templates iter194‑198 (shop tool‑list ingestion, master‑index PDF ingestion).  
7. Integrate AB‑locator scanner into full training pipeline.  
8. Finalize closed‑loop training with real shop data (tool‑list + ERP outcomes).  
9. Resolve unsatisfiable loop: add auto‑pass escape hatch for `/yolo-mode` or skip hook installation.  
10. Add real‑time performance metrics for wizard execution and pipeline throughput.
