# knowledge-conversion session db273e77 (2026-06-09, 106.3MB, spine 888KB, 11 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e5ad4ea802` – vitest config to 16 threads, batch extractor engines concurrency 16, cu129→cu128 GPU health fixes, PRISM_EMBED_CONCURRENCY=16, Docker/NIM audit report.  
- `873446a147` – CLAUDE.md §2/5/6/7 updated with real constants; MEMORY.md ownership note added.  
- `f9e953f0e8` – enforce‑eslint‑after‑edit.py path fixed to host‑portable.  
- `1443283f8b` – 12 MIT AI textbooks registered in `pdf-sources/registry.json`.  
- `3935f8d4ad` – added `topics/source/note` to PDFSourceMetadata; updated hook logic.  
- `ee26028a48` – loop‑state cascade with `pick-unit`, `PRISM_LOOP_MAX_ROLLS`; tests added.  
- `632335cec6` – stop-goal-clear-advance hook installed.  
- `c83ca9be64` – F5 autoresume staleness fix (DEFAULT_MAX_AGE_MIN 240→720).  
- `8cd8d615e9` – slot‑domain dedup (91 % token savings).  
- `eefef0359a` – PSN‑leg‑state dedup (82 % cut).  
- `636d36bf59` – semantic memory recall hook (`memory-relevance-inject.mjs`) with nomic‑embed.  
- `75c44d8412` – self‑refreshing cache for semantic recall.  
- `1f295f51e6`, `eefef0359a`, `8d2b6cf8e0`, `87e96fa485`, `afe169ab0e` – dedup helper (`dedupedContext()`) cutting 82–95 % tokens across 8 injectors.  
- `2c006fec7c` – MEMORY_SEED reader (51/51 tests).  
- `2a5f980a5d` – #3 ollama‑auto correction (no config change).  
- `1dd17250b3` – batch embeddings (`embedTextBatch()`) 3× faster, 15/15 tests.  
- `826be35aa4`, `4a939fc35f`, `05e3c45196` – #11a precompact MEMORY_SEED enrichment & seed‑status observability.  
- `8c945662ac` – #4 route‑suggest per‑session gate (25→1 fires).  
- `a6aee37203` – #11b tmp‑orphan sweep (6 orphans removed).  
- `c28c8875ca`, `98312e8a08` – shared‑file race fixes in doctrine rate limiter.  
- `6162fd99af` – grep test portability fix.  
- `caabc8fea6` – Q1 tribal reference node promotion (176 nodes).  
- `35dc2ec4c3`, `d92b9f52f4` – subagent bundle OOM mitigation, system‑viz summary correction.  
- `e80e6e3a41` – vision‑model single‑source refactor (`VISION_FAMILY_LEADERS`).  
- `89146678bf` – embed‑progress honesty fix (PID stamp, classifier).  
- `415941b1f0` – RTK‑guard false‑positive fix.  
- `2999dac687` – synthesis report (`GOAL-DISCOVERY-SYNTHESIS-2026-06-09.md`).  
- `58688be398` – system‑viz OOM finding (find & node-card).  
- `U‑OLLAMA‑NAV‑ENFORCE` + model‑pin (`ollama-nav-enforce-inject.mjs`, pin gpt‑oss:20b).  
- `U‑DARK‑WIKI‑RANK` – ranks 32 630 dark wiki files; 71 high‑value identified.  
- `U‑LINT‑ORPHAN‑OOM` – restores orphan detector (13 055 orphans, <3 s).  
- `U‑GRAPH‑STREAM‑DEGREE` – streaming degree pass eliminates 643 MB OOM.  
- `advisory-decay` unit wired; `bumpStats()` runs on every fire; `decayDecision` gated on suggest nudge.

**DECISIONS**  
- Do not wire `stop_on_failing_tests.mjs`; leave pending operator review.  
- Keep model pull background task (`bittzospv`) running; no action until completion.  
- Retune vitest config to top‑level `test.maxThreads`.  
- Add `PRISM_EMBED_CONCURRENCY=16` to match GPU capacity.  
- Use session‑scoped Stop hook: must satisfy token savings, context retention, vault value, H‑drive wiring; no user prompts.  
- Alpha lane handles config cleanup (vision‑model single source, Ollama navigation, local‑LLM usage); Bravo owns engine routing U3–U7.  
- Disable `pre-tool-router-table-advise.mjs` (82 K token savings).  
- Session‑once gate for `node-no-rtk-wrap`.  
- Keep `isAlreadyRtk` guard to suppress RTK false positives.  
- Skip owner‑gated work (Bravo routing, Sierra graph writes) until after Stop hook satisfied.  
- Queue remaining ultracode items (#3–#6) for next cron.

**OPERATOR DIRECTIVES**  
- Finish remaining tasks; ensure Ollama models align with new CPU/GPU; synchronize Docker/NVIDIA NIM.  
- 47 scheduled tasks remain disabled until migration complete.  
- Wire Obsidian app and PSN to H drive; find high‑value token savings, context retention/expansion.  
- Max out local‑LLM utilization on PC hardware; use `/system-viz`, master graph, PSN, Obsidian vault.  
- Use Ollama for searches, reads, codebase navigation; exploit AI tied to India’s domain and Sierra’s system‑viz domain.  
- Re‑register Zombie Reaper v2 and Blueprint OCR Batch via elevated PowerShell.  
- Verify high‑value improvements with new PC specs.

**FINDINGS/BUGS**  
- Vitest 4.1.5 ignored `poolOptions.threads`; regression fixed.  
- `stop_on_failing_tests.mjs` exists but unwired; potential gap in test‑before‑done logic.  
- Model pull stalled (~6–7 MB/s); partials frozen at ~93 %.  
- GPU health operator had stale cu129; corrected to cu128.  
- Duplicate vision‑model literals fixed; `vision-model-select.mjs` committed after 5 days.  
- Embed‑progress marker “running” for ~24 h; fixed with PID stamp and classifier.  
- RTK guard false positives suppressed by new guard.  
- System‑viz OOM on `find` & `node-card`; noted for future fix.  
- Subagent bundle OOM mitigated via bounded head read.  
- Shared‑file race in doctrine rate limiter resolved (atomic write, env isolation).  
- Grep test ENOENT bug fixed; portability issue resolved.  
- Memory pressure: 96–98 % due to `vmmemWSL`; no immediate crash risk.  
- Backend audit push duplicated on every edit; session‑gated version reduces traffic by ~8 k tokens.  
- Node‑pointer stubs inflated recall corpus (~9.5k I/O calls).  
- 15 819 broken wikilinks eliminated.

**DOMAIN SPECIFICS**  
- **Engines/Actions**: vitest config, batch extractor engines, GPU health script, Docker/NIM audit, PDF ingestion (`PDFSourceRegistryEngine.ts`), loop‑state cascade, stop hooks (`stop-goal-clear-advance.mjs`, `stop-force-loop-continue.mjs`), semantic memory recall (`memory-relevance-inject.mjs`), dedup helper (`dedupedContext()`), MEMORY_SEED reader, embedTextBatch, route‑suggest gate, orphan sweep, subagent bundle builder, vision‑model refactor, embed‑progress classifier, RTK guard, Ollama navigation bridge, dark wiki ranker, lint‑orphan OOM, graph‑stream degree.  
- **Metrics**: test pass 33/33; GPU usage 39 GB of 96 GB; Ollama models 32b + gpt‑oss 20b present; token savings ~82–95 % across injectors; route success rate 13.3%; memory pressure 96–98 %.  
- **Paths**: `settings.json`, `vitest.config.ts`, `MINBatchExtractorEngine.ts`, `McxBatchExtractorEngine.ts`, `gpu_health.py`, `GpuStackHealthEngine.ts`, `state/shared/DOCKER-NIM-AUDIT-2026-06-08.md`, `pdf-sources/registry.json`, `PDFSourceMetadata` type, `loop-state.mjs`, `.claude/settings.json`, `H:/prism/.claude/helpers/chat-slots.mjs`, `H:/prism/.claude/.settings.json`, `state/shared/HIGHVALUE-DISCOVERY-2026-06-08.md`, `state/shared/GOAL-DISCOVERY-SYNTHESIS-2026-06-09.md`.

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `/checkin`, `commit-mutex`, `stop‑hooks` framework.  
- Scripts/hooks: `stop_on_failing_tests.mjs`, `audit-roadmap-drift.mjs`, `ollama-resilient-pull.ps1`, `memory-relevance-inject.mjs`, `psn-leg-state-inject.mjs`.  
- Ultracode workflow (`wgypolzah`, `w3qho9bc3`, `wp9xijq9b`), Hermes‑workflow, System‑viz graph utilities.  
- Local LLMs: Ollama (gpt‑oss:20b, qwen2.5-coder:32b), nomic‑embed‑text.  
- External APIs: fxtwitter for X extraction; Playwright for tweet fetch.  
- Git tooling: `git-commit-mutex`, pathspec scoping.

**OPEN THREADS**  
- Complete gpt‑oss and gemma4 model pulls via golf’s durable task.  
- Wire `memory-rag-inject` to use galaxy‑brain semantic recall.  
- Finish remaining ultracode queue items (#3–#6) for next cron.  
- Implement PRISM_OLLAMA_OFFLOAD_AUTOEXEC flag and vision‑model cleanup (alpha scope).  
- Resolve system‑viz OOM on `find` & `node-card`.  
- Refresh find‑cache sidecar to prevent future OOMs.  
- Await 3‑of‑3 reviewer consensus on advisory‑decay commit.  
- Integrate Obsidian app and PSN wiring to H drive; validate high‑value improvements with new PC specs.  
- Explore exploitation of AI systems tied to India’s domain and Sierra’s system‑viz domain.
