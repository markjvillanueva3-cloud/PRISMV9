# knowledge-conversion session 501bd704 (2026-06-04, 22.3MB, spine 93KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `f11efe4047` – GPU‑stack‑health foundation (`gpu_health.py`, bridge, engine).  
- `9cd5a624ef` – base‑rate normalization in `voteDispatcher`.  
- `8abba4f27c` – direct‑embed inference (loadDirectEmbeddings + classifier change).  
- `ae60024500` – honest eval JSON (`embeddingMode:"direct"`, `isDegenerate:false`).  
- `dcb2c86bb8` – source‑signal enrichment for ghost embeddings.  
- `44f4a90ccb` – calibration disabled by default (AUROC/Brier regressed).  
- `da6aedfc07` – resume‑hash fix restoring `\x1F` delimiter (~372k nodes).  
- `6f2bad4792` – fleet‑wide learning artifacts, consolidated memory, stale‑memory fixes.  
- `9bd4b22abd` – `.gitattributes eol=lf`; default stratified holdout in `runAssessment`.

**DECISIONS**  
- Use direct‑embed inference to bypass broken edgeless‑SAGE model.  
- Add base‑rate normalization to mitigate majority‑class dominance.  
- Enrich ghost embeddings with leak‑free engine source signal (docblock + public API).  
- Keep calibration off by default; optional future work.  
- Adopt stratified holdout as default; keep flat split via `--flat-holdout`.  
- Record split choice in assessment results and `NN‑EVAL.json`.  
- Preserve 2‑reviewer per‑file, 3‑of‑3 final gate; commit minimal files with explicit pathspecs.

**OPERATOR DIRECTIVES**  
- “Continue” – proceed.  
- “Keep review gates; I meant bypass gates preventing you from working in other galaxies.”  
- “Do not respond to local‑command messages unless explicitly asked.”

**FINDINGS/BUGS**  
- H2GCN wrong fix → edgeless inference nullifies message‑passing, causing constant‑vote collapse.  
- Feature starvation + 8‑d embeddings caused majority‑class dominance.  
- Leak in `ghostEmbedText` exposed truth labels; fixed by stripping `prism_*` tokens.  
- Calibration (LOO isotonic) over‑confident on holdout → AUROC/Brier regressed.  
- Resume‑hash omitted `\x1F`, causing silent re‑embeds of ~372k nodes.  
- CRLF/LF flip bug resolved with eol=lf; stale memories still warn about missing `.gitattributes`.  
- Flat split underestimates AUROC (0.737→0.808) and overestimates macroF1 (0.452→0.439); stratified split yields AUROC 0.808, macroF1 0.439, Brier 0.179.

**DOMAIN SPECIFICS**  
- Engines/Actions: `GpuStackHealthEngine.ts`, `OllamaCapabilityProbeEngine.ts`, `build-node-embeddings.mjs`, `scripts/lib/py-subprocess-bridge.mjs`.  
- Dispatchers: `devDispatcher` (MCP tool), `chat-slots` helper, `/startup-india`, `/checkin-india`, `runAssessment`, `assessHoldout`, `computeMacroF1`.  
- Metrics/Gates: AUROC ≥ 0.78, macroF1 ≥ 0.55, Brier ≤ 0.15, accuracy, `isDegenerate` flag.  
- Paths: `scripts/py/gpu_health.py`; `src/__tests__/gpu_stack_health.test.ts`; `src/GNN/*`; `state/shared/nn-graph/ghost-node-embeddings.jsonl`; `.gitattributes`; `NN‑EVAL.json`.

**TOOLS USED**  
- PRISM CLI skills (`/startup-india`, `/checkin-india`, `/loop`).  
- Node helpers: `chat-slots.mjs`, `devDispatcher`.  
- Test harnesses: Vitest, node-test, makeGraph eval tests.  
- Git hooks: `slot-commit-enforce`, `worktree-route`, index.lock handling.  
- Build tools: esbuild, tsc, npm scripts.

**OPEN THREADS**  
1. Calibration – develop Platt/temperature scaling to improve AUROC/Brier without over‑confidence.  
2. MacroF1 / Brier – enrich ghost embeddings with engine source metadata and balanced reference pool to reach macroF1 ≥ 0.55, Brier ≤ 0.15.  
3. GPU‑training gating – install torch/CUDA 13.2 on RTX 6000 (golf‑side).  
4. Deployment – enable `PRISM_NNG_DIRECT_EMBED=1` in production once gates pass.  
5. Documentation/Wiki – finalize learning artifact, update stale memories, remove `.gitattributes` warnings.  
6. Loop iterations 3–20 – monitor gate status; ensure two parallel reviewers with R12 metric‑gaming check pass.
