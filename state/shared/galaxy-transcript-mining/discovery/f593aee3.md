# discovery session f593aee3 (2026-06-09, 5.9MB, spine 34KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `state/shared/delta-context-briefing-2026‑06‑09.md` – full 26‑session synthesis (22 KB).  
- `state/shared/delta-ollama-efficiency-plan-2026‑06‑09.md` – tiered Ollama routing plan (14.8 KB).  
- `scripts/lib/transcript-digest.mjs` – reusable extractor used for the workflow.

**DECISIONS**  
- Adopt bounded concurrency (≤3) for transcript‑digest fan‑outs to avoid Anthropic rate limits.  
- Persist raw transcript context in delta’s domain (`reference_delta_transcript_context…`) so it survives session restarts.  
- Use local Ollama models (`gpt‑oss:120b`, `qwen2.5-coder:32b`, etc.) for non‑geometry tasks; keep geometry, unit, and safety logic on Claude/Blackwell.  
- Prioritize fixing the dead tag in `cad-ollama-archetype-label.mjs` (A1) as first concrete action.

**OPERATOR DIRECTIVES**  
- Confirm that the context briefing and Ollama plan files are committed to the repo.  
- Execute A1 now (replace retired `qwen2.5-coder:7b` tag with a live local model).  
- Once A1 is merged, run the live revolute‑assembly proof against Fusion bridge `:18365`.  
- Merge the `slot/delta` arc into `cad-fusion-live-ms0`.  
- Build and validate `CAD-FEATURE-RECOGNITION-MS0`.

**FINDINGS/BUGS**  
- Only ~11 of 26 delta transcripts are genuine CAD work; many mis‑labelled or infra sessions.  
- Dead tag in `cad-ollama-archetype-label.mjs` causes silent no‑op, forcing Claude to classify archetypes.  
- `slot/delta` arc (408 commits) unmerged; contains closed‑loop CAD training pipeline.  
- Revolute‑assembly live proof pending; Fusion bridge `:18365` must be running.  
- `CAD-FEATURE-RECOGNITION-MS0` missing; STEP history absent, limiting feature‑recognition.

**DOMAIN SPECIFICS**  
- **Threads:** CAD‑TRAINING‑PIPELINE (print→CAD→compare→correct loop), CAD‑FUSION‑LIVE‑MS0 (Fusion API server).  
- **Features:** closed‑loop correction, edge‑feature mechanic, assembly PoC (G7), dual‑training corpus.  
- **Metrics/paths:** `slot/delta` branch diverged from `cad-fusion-live-ms0` at 2026‑05‑18; 408 commits ahead.  
- **Engine notes:** `CADFeatureRecognitionEngine` flagged stub (`U-EFF25`) – verify before wiring.

**TOOLS USED**  
- Ultracode workflow (`wf_28042811-3c4`) for Ollama scoping.  
- Transcript digest script (`scripts/lib/transcript-digest.mjs`).  
- PRISM tools: `/checkin`, `/loop`, `/goal`, `/pick-unit`.  
- Local LLMs via Ollama API (gpt‑oss, qwen2.5-coder).  

**OPEN THREADS**  
1. Run revolute‑assembly live proof (`node scripts/cad-fusion-assembly-poc-live.mjs --port <live>`).  
2. Merge `slot/delta` arc into shared branch.  
3. Build and validate `CAD-FEATURE-RECOGNITION-MS0`.  
4. Fix dead tag in `cad-ollama-archetype-label.mjs` (A1).  
5. Ensure all geometry‑related logic remains on Claude/Blackwell; guardrails for units, GD&T, collision checks.
