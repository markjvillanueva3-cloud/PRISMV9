# discovery session 86373eb3 (2026-06-09, 29.9MB, spine 353KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Added `Launch‑PRISM‑Fleet‑3win.ps1` (3‑window per‑account) and updated legacy launcher to keep 4‑window fallback.  
- Set `LOCAL_LLM_BACKEND=ollama`; guard in `nim‑autostart.mjs` skips NIM when ollama is used.  
- Stopped all NIM containers, set restart policy to `restart=no`, removed their autostart hook (`docker‑intel‑autostart.mjs`).  
- Removed compose `ollama` service from `COMPOSE_SERVICES`; launcher’s `DEFAULT_SERVICES` trimmed accordingly.  
- Added fail‑fast guard for invalid `--services=mcp`.  
- Fixed master‑index OOM with heap guard (384 MB); recall knobs (`MASTER_INDEX`, `MEMORY_INDEX`, `WIKI_PRECHECK_INJECT`) set to `1`.  
- Wired reverse mirror hook (`h-to-c-obsidian-mirror.mjs`) into PostToolUse; edits in Obsidian flow back to canonical C: source.  
- Created durable cron‑runner directory (`H:/prism/.claude/cron-runners/`); patched three broken tribal tasks to write runners there.  
- Built `vault-to-gnn-refpool.mjs`: added 4 high‑confidence vault‑derived reference ghosts (125→129).  
- Fixed `seed-ghost-from-unwired.mjs --apply/--revert` with streaming I/O (`readGraphStreaming`, `writeGraphStreamingAtomic`).  
- Updated `scripts/lib/graph-io.mjs` to expose `writeGraphStreamingAtomic`.  
- Built `vault-to-lora-dataset.mjs`: 245 Alpaca‑style instruction‑tuning pairs extracted from feedback memories; CRLF normalization and `.md` stripping applied.  
- Generalized transcript miner (`mine-galaxy-transcripts.mjs`) with registry, hardened `classifyHandoff`, recovered ~250 topic‑bearing handoffs.  
- Miner now uses content‑based discovery, per‑transcript slice cap (max 14), 900 s timeout for gpt‑oss:120b synthesis.  
- Vault memos written for 13 galaxies (backend‑helper, cam, india, lathe, mill, speed‑feed, wedm, system‑viz, bug‑hunting, token‑optimization, tribal‑knowledge).  
- `agent-orchestration` populated (`bqr3qt2o1`, exit 0); heavy backend galaxies processed: token‑optimization, tribal‑knowledge, system‑viz, fleet‑hygiene, wiring, bug‑hunting.  

**DECISIONS**  
- Deploy separate 3‑window per‑account launcher while keeping 4‑window fallback for compatibility.  
- Switch LLM backend to ollama; permanently disable NIM containers and remove compose `ollama` service to avoid port collision with native Ollama.  
- Keep RAG/CAG fully wired; LoRA training data remains markdown→JSONL in vault, binary weights on disk.  
- Use existing GNN seeder as base, extend with vault source instead of creating new seeder.  
- Adopt streaming I/O for all graph reads/writes (`writeGraphStreamingAtomic`).  
- Keep LoRA feeder separate from GNN feeder; each targets distinct training signals.  
- Generalize India’s transcript miner to all galaxies via registry; avoid per‑galaxy cloning.  
- Switch from handoff‑gated to content‑based discovery for transcript mining; cache classification once per run.  
- Limit each galaxy to 6 newest transcripts, cap large transcripts to 14 slices; raise synthesis timeout to 900 s for gpt‑oss:120b.  
- Batch remaining light backend galaxies in one task (`--next-unpopulated --backend-only --next-count 5`) after agent‑orchestration completes.  

**OPERATOR DIRECTIVES**  
- Run elevated PowerShell block to register all pending scheduled tasks (six cron installers).  
- Verify that native Ollama starts automatically on boot and no compose service collides on port 11434.  
- After agent‑orchestration, launch final backend batch (`bv3pb1fts`).  
- Mine galaxies one by one in ROI order using `node mine-galaxy-transcripts.mjs --content --galaxy <name> --limit 6 --max-slices 14` with > 900 s timeout.  
- After each galaxy, rebuild memory index sidecar (`build-memory-index-sidecar.mjs`) and embeddings sidecar (`build-memory-embeddings-sidecar.mjs --resume`), then run `galaxy-synthesis-refresh.mjs`.  

**FINDINGS/BUGS**  
- 22 concurrent Claude processes hit same Max account → 429 burst; resolved by per‑account launcher.  
- NIM containers crash‑loop (exit 137) due to Docker Desktop RAM cap (~16 GB); removed autostart and disabled restart.  
- Compose `ollama` service on :11434 collides with native Ollama; removed from autostart list.  
- Launcher requested service `mcp`, actual compose service is `prism‑server`; added guard.  
- Duplicate MCP daemons warning resolved by cleanup.  
- Qdrant dimension mismatch cleared: all 768 d vectors match Ollama’s nomic‑embed output.  
- Master‑index OOM bug fixed with heap guard; recall knobs enabled vault recall.  
- Broken tribal tasks failed because runners written to `$env:TEMP`; patched to durable path.  
- `seed-ghost-from-unwired.mjs --apply` crashed on 642 MB graph (`ERR_STRING_TOO_LONG`); fixed with streaming I/O.  
- LoRA extractor leaked CRLFs and `.md` extensions; both fixed.  
- `classifyHandoff` regex missed archive suffixes, long‑UUID IDs, Agent@HOST forms → ~250 handoffs recovered.  
- GPU contention with gpt‑oss:120b blocks nomic‑embed‑text embeddings; increased timeout to 900 s.  
- Grep wrapper produced misleading exit 255 codes; now omitted.  

**DOMAIN SPECIFICS**  
- **Engines/Actions**: `Launch‑PRISM‑Fleet‑3win.ps1`, `nim‑autostart.mjs`, `docker‑intel‑autostart.mjs`, `seed-ghost-from-unwired.mjs`, `vault-to-gnn-refpool.mjs`, `vault-to-lora-dataset.mjs`, `mine-galaxy-transcripts.mjs`, `discoverByContent`, `build-memory-index-sidecar.mjs`, `build-memory-embeddings-sidecar.mjs`, `galaxy-synthesis-refresh.mjs`.  
- **Dispatchers**: `node_card` (system‑viz reads), `ollama-prism-bridge`, `cag-router-inject`.  
- **Metrics/Paths**: system‑graph.json 542 MB → 640 ghosts; reference pool 125→129; LoRA dataset at `state/shared/lora/vault-feedback-dataset.jsonl`; vault memos per galaxy (e.g., `backend-helper/MEMORY.md`).  

**TOOLS USED**  
- PowerShell scripts: `Launch‑PRISM‑Fleet.ps1`, `Launch‑PRISM‑Fleet‑3win.ps1`, `slot‑tab‑boot.ps1`.  
- Node.js scripts: `nim‑autostart.mjs`, `docker‑intel‑autostart.mjs`, `switch‑claude‑account.mjs`, `seed-ghost-from-unwired.mjs`, `vault-to-gnn-refpool.mjs`, `vault-to-lora-dataset.mjs`, `mine-galaxy-transcripts.mjs`.  
- Docker Compose: services `prism-server`, `qdrant`; compose `ollama` removed.  
- Native Ollama daemon (auto‑started via scheduled task).  
- Qdrant for embeddings; nomic‑embed‑text model for dense recall.  

**OPEN THREADS**  
- Verify full reboot cycle to ensure no residual NIM containers start and native Ollama is up.  
- Run elevated PowerShell block to register six scheduled tasks; confirm `LastResult 0x0`.  
- Execute generalized transcript miner for all remaining galaxies (agent‑orchestration, discovery, dormant‑data, knowledge‑conversion, corpus‑aggregation, database‑expansion).  
- After each galaxy, rebuild sidecars and run synthesis refresh.  
- Monitor GPU VRAM when loading large models to avoid exceeding 96 GB.  
- Confirm no clobbering with India’s active session; coordinate via chat bus if needed.  
- Commit new files (`vault-to-gnn-refpool.mjs`, `vault-to-lora-dataset.mjs`, registry, miner) after passing all scrutiny gates.
