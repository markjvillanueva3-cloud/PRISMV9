# frontend-app session 86373eb3 (2026-06-10, 37.2MB, spine 368KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Created `Launch‑PRISM‑Fleet‑3win.ps1`; updated shortcuts: PRISM Fleet → 4‑window fallback; PRISM Fleet (3‑window) new launcher.  
- Set env vars: `LOCAL_LLM_BACKEND=ollama`, `PRISM_RESUME_MAX_MB=200`.  
- Stopped all NIM containers, added guard in `nim-autostart.mjs` to skip when backend is ollama.  
- Added heap‑aware sidecar guard in `master-index-search-lib.mjs`; wired `h-to-c-obsidian-mirror.mjs` as PostToolUse hook.  
- Patched tribal installers (`install-tribal-consolidate-cron.ps1`, etc.) to write durable runners; created `install-synthesis-crons.ps1`.  
- Created `vault‑to‑gnn‑refpool.mjs`, `seed‑ghost‑from‑unwired.mjs` (streaming I/O), `vault‑to‑lora‑dataset.mjs`; added `writeGraphStreamingAtomic` to `graph‑io.mjs`.  
- Added `mine-galaxy-transcripts.mjs` with content‑based discovery, cache writer; fixed missing `NUM_CTX`, raised synthesis timeout to 900 s.  
- Captured full‑text of 5 X articles via `fxtwitter` API into `state/shared/articles/2026‑06‑09‑*.md`; built Zulu orchestrator verified against 8 Hermes articles; wired Hermes→Obsidian loop with `MEMORY.md`, `USER.md`, `prism-vault-loop` skill, and 3 cron jobs.  

**DECISIONS**  
- Per‑account credential swap + staggered launch to avoid 429s; each window logs into distinct Max account.  
- Raise resume size cap from 40 MB to 200 MB; drop NIM stack entirely (stop containers, set backend to ollama, remove compose‑ollama autostart).  
- Remove `mcp` service name mismatch guard in launcher; keep only `prism-server`.  
- Align compose `ollama` service: delete obsolete `version:` key, set `KEEP_ALIVE=30m`, reduce `MAX_LOADED_MODELS` to 4.  
- Keep recall off by default via CAG‑router; do not re‑register brain‑refresh with `--with-viz`; leave redundant RAG hooks dead.  
- Do not overwrite active India session; use streaming I/O for large graphs; generalize transcript‑to‑galaxy miner; harden handoff classification regex.  

**OPERATOR DIRECTIVES**  
- Apply fixes to ensure PC launches without NVIDIA NIM, Ollama, Docker issues; fully connect to Prism MCP server.  
- Run elevated PowerShell block to register six scheduled tasks (tribal + synthesis); decide commit edits vs. live changes.  
- Run `hermes gateway install` and approve UAC prompt; register Zulu orchestrator task, set `zuluOptIn` on slots G10/G12; verify end‑to‑end proof tomorrow at 6:07 AM.  
- For each remaining backend galaxy, run:  
  ```
  node mine-galaxy-transcripts.mjs --next-unpopulated --backend-only
  node build-memory-index-sidecar.mjs
  node build-memory-embeddings-sidecar.mjs --resume
  node galaxy-synthesis-refresh.mjs
  ```  
  Monitor GPU/VRAM; unload `gpt‑oss:120b` before heavy embeddings if needed.  

**FINDINGS/BUGS**  
- NIM containers OOM‑kill (exit 137) due to 16 GB WSL cap vs. 20–40 GB model size.  
- 40 MB resume guard caused 15 of 19 slots to start fresh checkin instead of resuming.  
- API limit errors from concurrent Claude processes hitting same Max account; no staggering in current launcher.  
- Missing slash command `/checkin‑zulu` because Zulu agent not running in desktop app.  
- `mcp` service name mismatch caused “no such service” errors.  
- Duplicate MCP daemons (3 reported, 2 reap) from stale state file.  
- Port collision: native Ollama on :11434 collides with compose‑ollama when autostarted.  
- Master‑index OOM bug fixed by heap guard; recall hard‑off prevented vault writes resolved.  
- Scheduled tasks missing; `find-connections.mjs` not a cron runner.  
- LoRA dataset builders pull from DB, not vault; NN/GNN reference pool starved.  
- Seed‑ghost crash (`ERR_STRING_TOO_LONG`) fixed via streaming I/O.  
- CRLF and `.md` extension leaked into LoRA output fields.  
- `frontmatterField` regex bug: consumed newlines; fixed to `[ \t]`.  
- Handoff classification regex too strict, lost 250+ handoffs; now handles `.archive.<date>`, long UUIDs, `Agent@HOST`, slot‑keyed forms.  

**DOMAIN SPECIFICS**  
- Slot resolution via `slot-tab-boot.ps1` tier system; resume guard `PRISM_RESUME_MAX_MB`.  
- Backend routing in `local-llm-bridge.mjs`: `LOCAL_LLM_BACKEND=ollama` bypasses NIM probes.  
- Docker compose services: `prism-server`, `qdrant`, `prometheus`, `postgres`, `ollama`; scheduled task `PRISM Ollama Serve` auto‑starts native Ollama on logon.  
- Engines: Ollama `gpt‑oss:20b` (MAP), `gpt‑oss:120b` (SYNTHESIS), `nomic‑embed-text` (embeddings).  
- Actions/dispatchers: `discoverByContent`, `mineGalaxyTranscripts`, `build-memory-index-sidecar`, `build-memory-embeddings-sidecar`, `galaxy-synthesis-refresh`.  
- Metrics: transcript count per galaxy, cache hits, synthesis duration.  
- Paths: transcripts in `C:/Users/.../.claude/projects/H--prism`; vault memos under `<galaxy>/reference_<galaxy>_transcript_synthesis.md`; articles at `state/shared/articles/2026‑06‑09‑*.md`.  
- Backend galaxies: token‑opt, tribal‑knowledge, agent‑orchestration, system‑viz, fleet‑hygiene, wiring, bug‑hunting.  
- App domains: cad, business, ai‑training, cad‑fusion‑live, compliance‑safety, hermes‑zulu, quality.  

**TOOLS USED**  
- Scripts/launchers: `Launch‑PRISM‑Fleet.ps1`, `slot-tab-boot.ps1`, `nim-autostart.mjs`, `local-llm-bridge.mjs`, `master-index-search-lib.mjs`, `h-to-c-obsidian-mirror.mjs`, `install-tribal-consolidate-cron.ps1`, `install-synthesis-crons.ps1`, `vault‑to‑gnn‑refpool.mjs`, `seed‑ghost‑from‑unwired.mjs`, `vault‑to‑lora‑dataset.mjs`, `graph‑io.mjs`, `mine-galaxy-transcripts.mjs`, `build-memory-index-sidecar.mjs`, `build-memory-embeddings-sidecar.mjs`, `galaxy-synthesis-refresh.mjs`.  
- Docker utilities: `docker-intel-autostart.mjs`, `ollama-docker-launcher.mjs`.  
- Config files: `settings.json`, `.claude/.credentials.json`, `H:/.claude‑accounts/...`.  
- Monitoring hooks: `fleet-reaper-sweep.mjs`, `fleet-services-watchdog.mjs`.  
- Node.js wrapper, PowerShell scripts, Windows Task Scheduler.  
- External services: Ollama local LLM, ultracode, fxtwitter API, `ollamaCall` wrapper with retry.  

**OPEN THREADS**  
- Verify launcher starts correctly after full reboot; native Ollama up before any Claude session; no port collisions.  
- Monitor API‑limit behavior once per‑account staggering is in place; adjust token‑tick wait if needed.  
- Optional: re‑enable NIM stack later with larger WSL memory or separate GPU allocation.  
- Execute elevated PowerShell block and verify all six scheduled tasks appear `0x0`.  
- Commit vs. leave edits decision pending from operator.  
- Re‑register PDF Corpus Watcher (optional).  
- Scope for cron wrapper around `find-connections.mjs` remains unaddressed.  
- Finalize generalized transcript miner for all 34 galaxies; per-file scrutiny gate; re-register elevation‑required cron jobs.  
- Complete last ~7 app‑domain galaxy memos (Task 30); execute brain‑compounding step (Task 31).  
- Verify vault memos written and compounding into `MEMORY.md` occurs.  
- Final end‑to‑end proof of Hermes‑Obsidian loop tomorrow.
