# frontend-app session 9e91d800 (2026-05-26, 19.9MB, spine 86KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `d02f713f0a`: SlotSessionHistoryEngine + 57/57 tests; sidecar hooks (`sessionstart`, `heartbeat`, `stop`) wired in settings.json.  
- `U-SR01`‑`U-SR04`: SessionStart, Heartbeat, Stop sidecar hooks committed and tested.  
- `U-FH01`: Fixed heartbeat‑keepalive timeout (8 ms → 8000 ms) in `C:\Users\wompu\.claude\settings.json`.  
- `U-MCP-CAPACITY-PLAN-PUBLISH` & `MCP-CAPACITY-MS0.md`: Wiki ghost and spec for 30‑chat MCP scaling.  
- `U-FH03`: Resolved stale scheduled‑task `LastTaskResult=2147946720`.  
- `U-OBSOITE‑REAPER‑TASKS`: Disabled obsolete reaper tasks, confirmed Fleet Reaper health.  
- `U-SR07`: Added §Resume block to `/checkin` skill.  
- `U-SR08`: E2E test for SlotSessionHistoryEngine + sidecar hooks (4/4 PASS).  
- `U-FD06`: Generated slot‑queue features for system‑viz; tests 10/10 PASS.  

**DECISIONS**  
- Adopt a *session‑history sidecar* (`state/shared/slot-sessions/<nato>.jsonl`) to persist session start, heartbeat, and end events per slot; enables crash‑inferred resume.  
- Rewrite the fleet launcher to support 26 slots across 4 terminal windows with `--resume` logic; removes legacy 15‑slot /3‑window layout.  
- Make `/checkin-golf` non‑skippable: always run Fleet Reaper sweep, bind handoff to `golf-work`, and enforce allowlist bypass via `PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1`.  
- Queue all remaining units in `slot-task-queues.json` with `dedicated:true` for golf; use `feedback_git_commit_pathspec_2026_05_20` to avoid peer‑absorption.  

**OPERATOR DIRECTIVES** (verbatim)  
- `/checkin-golf — slot‑locked /checkin + fleet‑reaper owner`.  
- “Force‑takes the **golf** slot … always runs the fleet‑reaper”.  
- “Disable or bypass the legacy allowlist hook” – choose Option A (`PRISM_GOLF_WRITE_ALLOWLIST_BYPASS=1`).  
- “Roll in all tasks that were in this session to golf”.  

**FINDINGS/BUGS**  
- `settings.json` hooks mis‑nested (missing `{matcher,hooks}` wrapper) → `/doctor` errors.  
- Line 233 path `"H:.claude\binportable-node"` corrupted; fixed by rewriting to forward slashes.  
- Reaper never killed chats due to heartbeat timeout bug (`timeout:8`).  
- MCP server disconnects after a few minutes because `StreamableHTTPServerTransport` was instantiated per request (single‑transport invariant violated).  
- Fleet‑reaper sweep reported 7 crashed slots, 9 stale slots; all reaped successfully.  

**DOMAIN SPECIFICS**  
- **Engines/Actions**: `SlotSessionHistoryEngine`, `fleet-reaper-sweep.mjs`, `chat-slots.mjs`.  
- **Dispatchers/Skills**: `/checkin-golf`, `/slot-resume`, `/queue-add`, `/checkin` (with §Resume).  
- **Metrics/Paths**: `state/shared/handoffs/HANDOFF‑golf‑slot-recovery-ms0.md`; `state/shared/slot-sessions/golf.jsonl`.  
- **Unique to this galaxy**: 26 NATO‑named slots, per‑slot worktree binding, Fleet Reaper ownership shift, allowlist bypass flag.  

**TOOLS USED**  
- Hooks: `.claude/hooks/slot-session-sidecar-{sessionstart,heartbeat,stop}.mjs`, `golf-slot-write-allowlist.mjs`.  
- Scripts: `chat-slots.mjs` (claim/reclaim), `fleet-reaper-sweep.mjs`, `Launch‑PRISM‑Fleet.ps1`.  
- Skills/Commands: `/checkin-golf`, `/slot-resume`, `/queue-add`, `/checkin`.  
- Testing: vitest (`SlotSessionHistoryEngine.test.ts`), node:test (`slot-session-sidecar.test.mjs`).  

**OPEN THREADS**  
- **U‑SR05** – Launcher rewrite (26 slots / 4 windows, `--resume` logic).  
- **U‑SR06** – `/slot-resume` skill implementation.  
- **U‑SR07** – §Resume block already added; no further work.  
- **U‑FD02** – Task queue schema update (`dedicated:true`).  
- **U‑FD03** – Generate fleet‑state features for system‑viz (pending).  
- **U‑FD04** – Generate slot‑queue features for system‑viz (pending).  
- **U‑FH02** – Restore reaper‑runs JSONL history.  
- **U‑MCP‑FACTORY‑REFACTOR** – Dedicated chat MCP factory to satisfy single‑transport invariant (P0/L).  

These items capture the current state and next steps for resuming work on this galaxy.
