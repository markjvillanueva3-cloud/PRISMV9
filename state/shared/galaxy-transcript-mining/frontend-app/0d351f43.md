# frontend-app session 0d351f43 (2026-06-09, 0.8MB, spine 15KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- None yet – no builds or commits completed.

**DECISIONS**  
- Keep core PRISM repo (≈1038 GB) + `prism-backups` (≈42 GB).  
- Drop all nested worktrees, SteamLibrary, Tools, and Claude profile copies.  
- Final payload ≈1.2 TB.  
- Use **rclone** for direct‑to‑Box streaming instead of Box Drive mount due to C: cache wall.  
- Require a Box plan with ≥1 TB free space and per‑file limits that accommodate large uploads.

**OPERATOR DIRECTIVES**  
- “copy entire p drive to box cloud storage in folder `Prism H Drive`”

**FINDINGS/BUGS**  
- C: has only ~12 GB free; Box Drive pins all staged files, never evicts.  
- robocopy fails when enumerating destination (ERROR 31).  
- 4.49 M file count in source; after dropping worktrees → 1.89 M files (~155 GB).  
- Probe shows Box does not back‑pressure; C: stays full after copy.  
- `attrib +U` cannot dehydrate staged files.  
- Unknown Box quota and per‑file limits.

**DOMAIN SPECIFICS**  
- Source: P: mirror of H:, containing PRISM repo, worktrees, SteamLibrary, Tools, Claude profiles.  
- Target: `Box\Prism H Drive`.  
- Exclude list includes `.claude`, `.venv*`, `.hf-cache`, `.uv-cache`, `node_modules`, Docker/WSL directories, credential dirs, and identified worktree folders.  
- Git‑based reconstruction of worktrees; only main repo kept.

**TOOLS USED**  
- PowerShell (`robocopy`, `attrib`)  
- Box Drive virtual filesystem (C:\Users\wompu\Box)  
- rclone (direct Box API streaming)  
- Windows Cloud Filter API (for probing cache behavior)

**OPEN THREADS**  
- Verify Box plan capacity and per‑file limits via web UI.  
- Complete rclone authorization (`rclone authorize box`).  
- Clean up probe copy on C: after confirmation.  
- Execute full rclone backup with persisted exclude set.  
- Monitor upload progress, handle resumptions if needed.
