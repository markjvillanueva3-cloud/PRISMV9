# frontend-app session 321c1d3f (2026-06-03, 37.4MB, spine 386KB, 5 slice(s), model gpt-oss:20b)

**SHIPPED (builds/commits)**
- `U-WINMAX-BUTTON-SIGNATURE` – commit a1c1efa31d: button‑tiebreak, DRAW_VERIFY screen, argv guard.
- `U-WINMAX-NAV-PATH-CONTRACT` – commit dd1fcdb48c: unified CLI envelope `{ok,…}`, single‑line JSON for `whereami`/`navigate`, hardened `mapCli`.
- `U-PT-KNOWLEDGE-ENRICH` – commit ef49daac34: `/from‑knowledge` leg, knowledge traveler & playbook checks.
- `U-PT-HAAS-SPEC / ENGINE` – commit 3dbc22e2f0: ground‑truth spec, G‑code dialect, inch scaling, feed/arc safety fixes.
- `U-PT-HAAS-CANNED‑CYCLES` – commit a1acfda90b: canned cycle output bare line, default G99, NaN guard, first‑hole coupling guard.
- `U-CIMCO-WIKI-TRIBAL` – 12 tribal tips added to architecture wiki.
- `U-PATCHES-ALLOWLIST` – fleet‑wide leave‑a‑copy patch guard.
- `U-CIMCO-CONTROL-MAP` – catalog, `.mcfg` reader, sim‑report parser (19/19 tests).
- `U-CIMCO-MACHINE-INDEX` – 86 machines indexed; 44 units‑unresolved flagged.
- `U-CIMCO-POST-INDEX` – 25 JS posts + 44 eRPost parsed (5/5 tests).
- `U-CIMCO-PER-GALAXY-PLOTS` – dependency‑ordered build plan for 10 galaxies.
- `U-CIMCO-WIKI-REFLECT / REFLECT2` – wiki updated with all shipped CIMCO units.
- `U-CIMCO-TOOL-INDEX` – 14 XML tool libs, 366 cutters (188 Metric / 178 Imperial).
- **SPINE‑1** – `CimcoVerificationBridgeEngine.ts`, dispatcher, tests 22/22 pass, 3‑of‑3 scrutiny.
- `scripts/lib/nc-normalize.mjs` – NC normalizer, 18/18 test suite green.
- `scripts/cimco-jm-machine-map.mjs` – JM‑fleet → CIMCO sim mapping (9/9 tests).
- `297a3c9194` – Post‑proof readiness ledger (golden corpus + sim machine mapping + `compareNC` drift audit, 15 machines, 9 143 programs, 246 content‑drift groups).
- `430f735fff` – Workflow P0 fix (trunnion regex), Hurco `.hnc` coverage, fleet readiness report.
- `d0e5df9e16` – `nc-dialect-masks.mjs` + `roundTrip` classifier; 10/10 tests, safety‑invariant verified on JM golden data.
- `38da8cb412` – Refactored drift audit via `roundTrip`; reclassifies 247 semantic‑drift, 8 byte‑identical, 2 volatile‑header‑only pairs.
- `7a14bf0def` – Dialect‑mask fail‑closed fix (greedy patterns corrected).

**DECISIONS (architecture/scope + why)**
- Use slot‑echo for post‑processing; keep core harness clean.
- Additive button tiebreak (`signatureOf.buttons`) instead of rewriting `whereami`.
- Standardize CLI envelope to `{ok,…}` for deterministic consumer contracts.
- Enumerate `U‑PT‑HAAS‑CANNED‑CYCLES` before engine write‑back to avoid half‑built safety engine.
- Adopt MCP dispatchers (`prism_calc`, `prism_safety`, `prism_cam`, etc.) to avoid reinvention.
- Enforce `duplicationGuardEngine.mustCheckBeforeCreating()` on every new asset; throw on duplicates.
- Require 2‑parallel reviewers + 3‑of‑3 gate (`scrutiny-3way.mjs`) before any stop.
- Reject speculative dialect units unless byte‑equivalent to golden archive.
- Fix Haas M29 bug: remove `M29` before `G84`; document Fanuc‑only.
- Standardize canned cycle output: bare line, default G99, guard for first‑hole coupling and NaN in approach block.
- Adopt dispatcher pattern (`prism_cimco`) for all new engines; keep core logic isolated.
- Build offline units (CIMCO parsing, indexing) before wiring bridge engine to avoid mid‑build spiral.
- Map kinematics by orientation + axis count rather than flat generic floor.
- Use workflow + parallel agents; defer extraction when session limit reached.
- Prioritize safety gates: P0/P1 review via adversarial agents before any commit.

**OPERATOR DIRECTIVES**
- `/loop` scheduled recurring prompt (cron job ca6b97bb).
- `/yolo-mode` activated – run autonomously, no questions (`# YOLO Mode — Maximum Velocity Development`).
- `off` – deactivate YOLO mode.
- `status` – report current state.
- Schedule recurring `/yolo-mode` every 5 min (`cron: */5 * * * *`).
- Goal clear: utilize full CIMCO suite to prove out all JM‑fleet posts at 100% working; live‑test on approval.
- Add native CIMCO sim machines where available.

**FINDINGS/BUGS**
- Softkey labels graphical → UIA cannot read; need button tiebreak.
- CLI contract mismatch: `path` emits bare array, consumer expects `{ok,…}`; pretty‑print multi‑line.
- Haas engine 25.4× inch scaling bug, non‑finite feed/arc safety issues.
- M29 before G84 caused silent tap hang on Haas.
- Cycle line drift: X/Y present and default G98; corrected to bare + G99.
- NaN guard missing in approach block; added.
- First‑hole coupling issue fixed with divergence guard.
- Fail‑open bug: `??` used instead of `||` in report parsing; fixed.
- Duplicate guard prevented accidental re‑creation during CI.
- Kinematic mapping collapsed lathes; corrected by weighting orientation & axis count.
- NC normalizer test failure due to tokenization logic; resolved.
- 44 vendor machine defs lack Unit and Orientation (units‑unresolved).
- Server rate limiting blocks parallel agent dispatch.
- Stale Git lock observed; recovered.

**DOMAIN SPECIFICS (engines/actions/dispatchers/metrics/paths)**
- Engines: `HaasNGCMillMasterPostEngine`, `Hurco` post, `Okuma` engines, `CimcoVerificationBridgeEngine`.
- Actions: `generateProgram`, `emitToolpath`, `emitCannedCycle`, `whereami`, `navigate`, `compareNC`, `roundTrip`.
- Dispatchers: `prism_shop_practice:tribal_enrich`, `master_post_by_machine`, `winmax-ui-map CLI`, `mapCli`, `prism_calc`, `prism_safety`, `prism_cam`, `prism_cad`, `prism_turning`, `prism_ai`, `prism_dev`, `prism_cimco`.
- Metrics: test pass counts (68/68, 31/31), conformance scores, safety gate passes; drift audit 247 semantic‑drift, 8 byte‑identical, 2 volatile‑header‑only pairs; 15 machines, 9 143 programs sampled.
- Paths: `/checkin‑echo`, `/loop`, `/yolo-mode`, `/goal clear`, `/status`.
- File formats: `.mcfg` JSON machine defs, `.js` posts, `.eRPost` binary controller defs, `.tmlib` XML tool libs, `.hnc` Hurco.

**TOOLS USED**
- RTK (`rtk git`, `rtk vitest run`, `rtk tsc`, `rtk npm run build`).
- Ollama offload (`/ollama-*` skills, `/claude/hooks/ollama-task-offloader.mjs`).
- `system-viz-query.mjs`.
- Obsidian 2nd‑brain queries (`/wiki-query`, `prism_memory:semantic_search`, `prism_knowledge:tribal_search`).
- `prism_session:master_index_query`.
- `duplicationGuardEngine.mustCheckBeforeCreating()`.
- Scrutiny 3‑way gate (`node H:/prism/.claude/scripts/scrutiny-3way.mjs`).
- Loop state (`loop-state.mjs`) and cronCreate for recurring loop.
- Scripts: `scripts/lib/nc-dialect-masks.mjs`, `roundTrip` classifier, `nc-normalize.mjs`, `cimco-jm-machine-map.mjs`.

**OPEN THREADS**
- Implement `U‑PT‑HAAS‑CANNED‑CYCLES` (next unit).
- Live WinMax Mill sim remains up; navigation tests pending.
- Build CIMCO↔PRISM bridge engine (TS + dispatcher wiring).
- Resume per‑galaxy plot workflow after rate limiting lifts.
- Implement live UIA driver for Simulation Report extraction.
- Post‑compact tick to run heavy tsc build for bridge engine.
- Build golden round‑trip harness and `volatileCommentMask` profiles for all dialects.
- Obtain live CIMCO sim runs and Mastercam CAM source to enable byte‑equivalence parity.
- Finalize fleet readiness roadmap (`state/shared/cimco/post-proof-readiness.md`).
- Verify no remaining safety‑gate fail‑opens; complete 3‑of‑3 scrutiny on new commits.
