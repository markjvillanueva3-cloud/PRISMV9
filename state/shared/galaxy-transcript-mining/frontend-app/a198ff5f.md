# frontend-app session a198ff5f (2026-05-29, 53.3MB, spine 280KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Updated `Launch-PRISM-Fleet.ps1` & `launch-prism-fleet.bat`: PowerShell 7, 4‑window × 6‑tab layout, alphabetical NW/NE/SW/SE groups; removed semicolon collision.  
- Patched regenerator (`regenerate-launch-fleet.mjs`) to emit `-File slot-tab-boot.ps1 -Slot X`.  
- Added desktop icon (OneDrive redirect fixed).  
- Switched Claude CLI to 2.1.153, calling `claude.exe` directly.  
- Created galaxy dirs for all 26 slots (`H:/prism-slot‑<name>`), each with CLAUDE.md, MEMORY.md, PATHS.md, TOOLBELT.md.  
- Generated 48 skill wrappers: `/galaxy-buildout-<slot>.md`, `/smart-<slot>.md`.  
- Added `stop-auto-capture-per-slot.mjs` hook (auto‑memory capture).  
- Implemented PER‑SLOT‑CLOSED‑LOOP‑INTEGRATION spec & JSON recommendations.  
- Updated `slot-tab-boot.ps1` to detect missing galaxy buildout and trigger brief.  
- Built master index (`state/shared/master-index.md`) and graph (`state/shared/master-graph.json`); regenerated system‑viz.  
- Fixed HP bar logic; MP bar redesign options prepared.  
- Added `/ready` endpoint + `waitForReady()` in mcp‑server (dist rebuilt).  
- Implemented MP‑5h quota burn UI & context‑aware HP bar (`ctxMaxEffective`).  
- Committed `outcome-bus-auto-tap.mjs` & `stop-auto-capture-per-slot.mjs` hooks.  
- Added Opus 4.8 flag to all fleet launchers (fallback 4.7).  
- Implemented galaxy‑first‑on‑resume override in `slot-tab-boot.ps1`.  
- Shipped India meta‑bus spec + recommendations JSON; added India wire to 20 slots (excluding hygiene/unallocated).  
- Registered Codex MCP plugin; updated Octopus probe – Claude, Ollama, Gemini live.  
- Added per‑slot forge command auto‑invoke directive.  
- Committed `FLEET‑EXPANSION‑PLAN‑2026‑05‑28.md` and `MASTER‑BRAIN‑TEMPLATE.md`; 10‑agent forge‑audit‑v3 synthesis.  
- Updated alpha’s `engines/token‑optimization/MEMORY.md` with master‑backpointer header.  
- Assigned COMMAND‑KERNEL‑MS0 to alpha; queued U‑CK11.  
- Removed `.mcp.json` entry for `prism_safe`; kept shared HTTP bridge (`:3100`).  
- Added Tier‑1 MCP filter map (`mcp-tool-domains.mjs`) + tests + bridge filter.  
- Created CWD‑self‑resolution tier (`PRISM_BOOT_SLOT`) and verified (inert in prod).  
- Updated per‑galaxy memory sync router (`reconcileGalaxies`); 22 galaxy dirs created, 797 synced / 93 routed.  
- Activated MCP via `df8688d1a2 U-MCP-ACTIVATE`; 24 tests PASS.  

**DECISIONS**  
- Use PowerShell 7 for all tabs; launch via `.lnk → Launch‑PRISM‑Fleet.ps1`.  
- Replace inline `-Command` with `-File` to avoid semicolon collision.  
- Call `claude.exe` directly, bypassing `claude.cmd`.  
- Keep anti‑compact env vars (`CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=99`, etc.).  
- Tiered session resume: Tier 1 live slot → Tier 2 recent JSONL → Tier 3 fresh `/checkin`.  
- Add domains romeo/uniform/victor/xray; keep others unchanged.  
- Per‑slot self‑build via brief at `state/shared/per-slot-galaxy-buildout/<slot>.md`.  
- Use STOP hook auto‑capture for important outcomes.  
- Deploy `/smart‑<slot>` skill wrappers (Haiku/Sonnet/Opus routing).  
- Master index & system‑viz are canonical search sources; update them.  
- Adopt per‑domain shard architecture (Option B) as milestone `MCP-SHARDING-MS0`; keep single master with fleet‑reaper for now.  
- Use fleet‑reaper to collapse 20 watchdogs into one; defer per‑chat stdio MCP until after launch.  
- Keep claude‑flow tools obsolete – use PRISM native equivalents.  
- Prioritize ready gate, quota UI, outcome bus before launching.  
- Adopt Tier‑1 filter over 4 servers; re‑designate papa to frontend.  
- Single‑PC mode chosen; cross‑PC sync issues removed.  

**OPERATOR DIRECTIVES**  
- Launch PRISM Fleet from desktop icon; ensure 4 windows × 6 tabs, alphabetical.  
- Continue chats from last night’s sessions.  
- Use latest Claude CLI (2.1.153).  
- Resolve OneDrive Desktop redirection.  
- Patch regenerator for semicolon collision.  
- Add new domains & galaxy buildouts.  
- Enable per‑slot self‑build instructions.  
- Update master index/graph/system‑viz; enforce use in searches.  
- Auto‑capture memories, wiki, tribal knowledge, skills, hooks per slot.  
- Ensure India domain closed‑loop integration works (`outcome-bus-auto-tap.mjs`).  
- Run Ollama/Docker/NVIDIA NIM; fix UI terminal HP/MP bars.  
- Verify fleet launch readiness (all 24 slots have complete galaxy buildouts).  
- Confirm `slot-tab-boot.ps1` triggers brief if galaxy missing.  
- Integrate parallel agents’ review feedback into final ship.  
- “Continue prior session’s India meta‑bus closed‑loop work.”  
- “Verify fleet launch readiness.”  
- “Fill all gaps and fixes before launching the fleet.”  
- “Use forge commands and slash commands, automate when needed.”  
- “Never use Claude flow tools; they are obsolete.”  
- “Assess all roadmap plans within entire H: drive and compare to task list.”  

**FINDINGS / BUGS**  
- `launch-prism-fleet.bat` had semicolon collision → 0x80070002 errors.  
- `claude.cmd` wrapper caused cmd.exe failures; switched to `claude.exe`.  
- OneDrive Desktop path mis‑pointed; desktop icon now correct.  
- Tier 1 resume missed reaped‑claim chats due to cleared `chat-slots.json`; added Tier 1.5 scan.  
- Precompact auto‑trigger ignored override env vars at 880K tokens; fixed by setting `PRECOMPACT_SOFT_TOKENS`/`HARD`.  
- Liveness check using stale PID; added JSONL mtime fallback.  
- Missing zulu slot in `SLOT_GALAXY_MAP`; added.  
- System‑viz graph stale (2.5 h); regenerated.  
- Stop hook errors: broken hooks, schema‑invalid outputs; cleaned up.  
- Cache‑miss mitigation #1 applied; #2/#3/#4 pending.  
- Ollama initially offline; now running.  
- `/ready` missing in dist → fixed with fast build.  
- `outcome-bus-auto-tap` not consumed (dead‑letter loop).  
- 33 dormant T0 hooks (e.g., golf‑slot‑write‑allowlist).  
- Codex plugin requires login; Octopus voices: Codex & Grok missing.  
- Ollama rewriter skip rate ~50/50 – needs fix.  
- NIM chat model not deployed (only embeddings available).  
- Multiple launchers lack Opus flag.  
- `/checkin‑$Slot` passed as bare slash command → “unknown skill args” error; fixed by wrapping in natural language prompt.  
- Tab titles overwritten by claude.exe; fixed via `suppressApplicationTitle`.  
- Default Windows Terminal profile is PowerShell 5.1, causing fallback to non‑PS7 and python errors (not yet resolved).  

**DOMAIN SPECIFICS**  
- Slot domains: alpha, bravo, charlie, delta, echo, foxtrot, golf, hotel, india, juliett, kilo, lima, mike, november, oscar, papa, quebec, romeo, sierra, tango, uniform, victor, whiskey, xray, zulu.  
- Galaxy dirs under `H:/prism-slot‑<name>`; each contains CLAUDE.md, MEMORY.md, PATHS.md, TOOLBELT.md.  
- `SLOT_GALAXY_MAP` maps slots to galaxy dirs.  
- Engines per galaxy: mill (`prism_mill`), lathe (`prism_lathe`), wedm (`prism_wedm`), cad (`prism_cad`), cam (`prism_cam`), quoting, business, post‑processor, etc.  
- Actions: `prism_intelligence`, `prism_calc`, `prism_ai consensus_decide`.  
- Dispatchers: mcp‑server (HTTP/stdio), octopus consensus engine, codex MCP plugin, forge commands.  
- Metrics: HP bar context tokens (`ctxMaxEffective`), MP bar 5h quota burn, outcome‑bus JSONL, NN‑GRAPH retrain, tribal capture, calibration monitor.  
- Key paths: `/ready`, `/health`, `/smart` wrappers, `slot-tab-boot.ps1`, `generate-per-slot-mcp-config.mjs`.  

**TOOLS USED**  
- PRISM core: mcp‑server, octopus consensus, codex MCP plugin, forge commands, slash commands.  
- Scripts: `Launch-PRISM-Fleet.ps1`, `slot-tab-boot.ps1`, `regenerate-launch-fleet.mjs`, `generate-per-slot-mcp-config.mjs`, `outcome-bus-auto-tap.mjs`, `stop-auto-capture-per-slot.mjs`.  
- Hooks: `stop-auto-capture-per-slot`, `outcome-bus-auto-tap`, `postToolUse` (auto‑invoke forge).  
- Skills: `forge7`, `forge-audit-v2/v3`, `dedup`, `scrutinize`, `handoff`, `octopus`, `lathe-studio`, `quote-to-ship`, `smart`, `pick-unit`.  
- Runtime: PowerShell 7, Windows Terminal (suppressApplicationTitle), Node.js scripts, Claude CLI 2.1.153, Opus 4.8, Ollama/Docker/NVIDIA NIM.  

**OPEN THREADS**  
- Ship `outcome-bus-auto-tap.mjs` for India domain (first ship).  
- Verify Ollama/Docker/NVIDIA NIM running and integrated.  
- Final UI terminal fixes: HP bar accurate; MP bar redesign chosen.  
- Update master index/graph/system‑viz to include new galaxies; regenerate system‑viz.  
- Confirm all 24 slots have complete galaxy buildouts before fleet launch.  
- Ensure `slot-tab-boot.ps1` triggers brief if galaxy missing.  
- Integrate parallel agents’ review feedback into final ship.  
- Implement per‑chat stdio MCP server/CLI (`MCP-PER-CHAT-MS0`).  
- Bridge outcome‑bus JSONL to consumers (NN‑GRAPH retrain, tribal capture) – dead‑letter fix.  
- Deploy NIM chat model for Ollama offload.  
- Resolve Ollama rewriter skip rate; unify native vs container instance.  
- Finalize codex login and enable all voices in octopus.  
- Upgrade `forge-audit` to v3 (parallel fan‑out).  
- Monitor VS Code vs PowerShell performance via hooks.  
- Define dynamic‑workflow integration strategy (research preview).  
- Retry Octopus pass on the two commits (rate limit hit).  
- Codify advisory‑gate rule into `CLAUDE.md`.  
- Fleet relaunch to apply title suppression and checkin fix.  
- Optional: change WT default profile to PowerShell 7 to avoid fallback errors.  
- Capture `/checkin‑<slot>` bug as a wiki/memory entry for future regressions.
