# frontend-app session 001bd6c3 (2026-06-10, 59.3MB, spine 532KB, 7 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commits ab2ccf42a4 & e6713584e2 – `generate-hermes-features.mjs`, system‑viz roost (P4).  
- Commit c988a21ec4 – blind edit causing Hermes boot‑loop.  
- Commit 56aa423427 – Hermes on Opus 4.8 via Claude Code OAuth.  
- Docker Desktop Qdrant up on `:6333` (healthy).  
- Hermes config migrated to v28; `model.default = gpt‑oss:20b`, `fallback_model = anthropic/claude‑opus‑4‑8`.  
- MCP server alive at `:3100/mcp`; Obsidian REST API reachable on `:27123`.  
- MEMORY.md fill (34/34) via commit 28b72e4dee & script `fill-galaxy-memory-sections.mjs`.  
- CLAUDE.md domain‑core filler commits: 7d79f345c2, 1ab785c21d, 3ea4f40192, e6eba32eec, 2579da89a4.  
- Unit commits: U5b (`ollama-tool-agent.mjs`), U5c (`ollama-offload-enforce.mjs`), U6 (`ollama-codegen.mjs`), U7 (`ollama-build-loop.mjs`), U10 (`ollama-capability-probe.mjs`), U9 (`ollama-coresidency.mjs`).  
- Offload router roster refresh (U1, commit 2340a2e699).  
- Route‑hook default to `gpt‑oss:20b` (U2, commit a2756779c2).  
- Take‑rate banner honesty fix (U3, commit a60d7ba0bf).  
- LLM compressor on RTK residue (U4, commit 612418fde7).  
- Fleet‑wide coding model set to `gpt‑oss:120b` via `PRISM_CODEGEN_MODEL` env (U-OAB‑120B, commit 3904b5e7b0).  
- Go‑live (#14) merged onto `cad‑fusion‑live‑ms0` (commit 5a91ba1862).

**DECISIONS**  
- Use PRISM MCP :3100 as Hermes read surface; drop filesystem server.  
- Link Hermes to Claude Code primary, fallback to local Ollama (`qwen2.5‑coder:32b`).  
- Revert supervisor `cwd` patch; plan PRISM_ROOT root‑fix.  
- Keep Qdrant optional for fleet coordination.  
- Replace invalid provider `openai` with `custom`/`anthropic`.  
- Set Hermes primary model to `gpt‑oss:20b`; keep Claude fallback.  
- Use headless PowerShell CLI (`hermes -z`) instead of GUI launcher.  
- Adopt canonical 11‑artifact rubric (`PER‑SLOT‑GALAXY‑BUILD‑KIT.md`).  
- Use ultracode for assessment; Ollama local stack (qwen2.5‑coder:32b, gpt‑oss:120b).  
- Ground CLAUDE.md from `PATHS.md`, `MEMORY.md`, `ENGINE_DIGEST.md`.  
- Enforce lane discipline: commit only own files; never delete.  
- Build automated LLM routing system per new goal; integrate with Claude Opus review arm.  
- Default Ollama host to `127.0.0.1`; override via `OLLAMA_HOST`.

**OPERATOR DIRECTIVES**  
- Restart Hermes after config changes to load provider/model settings.  
- Verify Qdrant container running; restart Docker Desktop if it stops.  
- Test Hermes with simple prompt; confirm response uses `gpt‑oss:20b`.  
- If 400 “extra‑usage” errors persist, enable billing or switch to Claude fallback.  
- Monitor memory pressure (commit charge ~96 %); avoid heavy parallel tasks until RAM stabilizes.  
- Bring Qdrant up and running.  
- Resolve Hermes 400 error by enabling extra‑usage billing or adjusting fallback logic for 400.  
- Implement PRISM_ROOT cwd fix for CapabilityIndexEngine vs repo‑root conflict.  
- Re‑enable disabled Hermes‑Obsidian Bridge scheduled task.  
- Regenerate `ai-training_synthesis.md` via local stack when owner available.  
- Confirm all engines use updated host logic; add regression test covering env override and default case.

**FINDINGS/BUGS**  
- Stale MCP daemon had `N:\` path; healthy after restart.  
- Qdrant vector store down (no HTTP 200); now up on `:6333`.  
- Hermes message fails with HTTP 400 from Anthropic; third‑party apps need “extra usage” billing enabled.  
- Hermes fallback triggers only on 429, not on 400 → no local model used when 400 occurs.  
- `self_awareness_search` returned 5 hits after restart; earlier empty due to stale daemon.  
- Dream‑cycle writes into `knowledge/memories/`; sync zone safe.  
- Unknown provider `openai` rejected config; fixed by using `custom`.  
- Config version mismatch (0→28) caused stale connection state; resolved with migration.  
- Hermes daemon cached “not connected” to Qdrant because started before Qdrant up; requires restart.  
- GUI launcher stuck on git‑pull due dirty `package-lock.json`; cleared and relaunched.  
- 400 errors from Claude non‑retryable; fallback never triggered.  
- gpt‑oss reasoning channel mis‑parsed by Hermes custom provider → no final response.  
- Missing token‑budget knob for Hermes output; fixed in config.  
- Claude API rate limiting prevented multi‑agent audit; switched to local stack.  
- `node_*` auto‑generated file inflation bug inflated memory counts (~20×); exclusion logic fixed.  
- Test failure due to injected mutation; corrected test and function.  
- `ai-training_synthesis.md` corrupted (all‑NUL) flagged.  
- Stale roster caused 0 % offload; fixed in U1.  
- Route‑hook default pointed to retired model; fixed in U2.  
- Compressor safety‑denylist had P0 breach; fixed and reverified.  
- Kimi cloud violates data sovereignty → not wired.

**DOMAIN SPECIFICS**  
- Hermes ↔ Obsidian bridge: `hermes-obsidian-memory-bridge.mjs`, `knowledge/hermes-brain/`.  
- PRISM MCP dispatchers: `prism_memory`, `prism_session`, `prism_knowledge`, `prism_context`.  
- Slot coordination APIs: `slot_brief_write`, `chat_post`, `slot_brief_list`.  
- Dream‑cycle: 11,211 memos → 200 connections; OOM fix applied.  
- Octopus‑RAG: `prism_knowledge:search`, `tribal_search`.  
- Qdrant vector store engine: collections `prism_engines`, `prism_formulas`, `prism_skills`.  
- Hermes agent runtime; MCP server at `:3100`; provider plugins `anthropic`, `custom`.  
- Obsidian REST API plugin on `:27123`.  
- Local Ollama endpoint `127.0.0.1:11434`; models `gpt‑oss:20b` (128 K context), `qwen2.5-coder:32b` (~37 GB), `qwen3-vl:8b-instruct` (256 K).  
- Galaxy structure: `engines/<galaxy>/MEMORY.md`, `PATHS.md`, `TOOLBELT.md`; vault mirror at `memories/galaxies/<galaxy>`.  
- MASTER‑BRAIN‑TEMPLATE sections.  
- Scripts: `syncGalaxyMemories()`, `galaxy-synthesis-refresh.mjs`, `fill-galaxy-memory-sections.mjs`.  
- Local stack: Ollama (11434), Qdrant (6333), MCP :3100.  
- Engines/dispatchers: `ModelRoutingEngine.ts`, `AISystemRouterEngine.ts`, `OllamaTaskOffloaderEngine.ts`, `local‑llm-task-router.mjs`, `ollama-cost-router.mjs`.  
- Offload hooks: `ollama-offload-enforce.mjs`, `ollama-tool-agent.mjs`, `ollama-codegen.mjs`, `ollama-build-loop.mjs`, `ollama-capability-probe.mjs`, `ollama-coresidency.mjs`.  
- U1 router roster refresh; U2 route‑config.json; U3 banner honesty fix; U4 LLM compressor; U-OAB‑120B env; U5 RTK config.toml.

**TOOLS USED**  
- `/checkin-bravo` wrapper.  
- `wire-hermes-local-backend.mjs`.  
- `mcp-server-supervisor.mjs`.  
- Slot‑brief APIs (`slot_brief_write`, `chat_post`, `slot_brief_list`).  
- `npx` (server‑filesystem).  
- Node, Docker, Qdrant container.  
- Docker Desktop, docker‑compose.  
- Hermes CLI: `hermes config migrate`, `hermes -z`.  
- PRISM MCP server (`:3100/mcp`).  
- Obsidian REST API plugin.  
- Local Ollama runtime.  
- Claude Code CLI.  
- Node.js scripts & Jest tests (`node -e`, `jest`).  
- Git for commits, diff, status.  
- Ultracode workflows (planning, verification).  
- TypeScript compiler `tsc` with targeted emit.  
- Esbuild `build:fast`.  
- Vitest / node test harnesses.  
- npm scripts (`npm run build:fast`, etc.).

**OPEN THREADS**  
- Resolve Hermes 400 error: enable extra‑usage billing or adjust fallback logic for 400.  
- Implement PRISM_ROOT cwd fix for CapabilityIndexEngine vs repo‑root conflict.  
- Re‑enable disabled Hermes‑Obsidian Bridge scheduled task.  
- Regenerate `ai-training_synthesis.md` via local stack when owner available.  
- Monitor MCP :3100 and Qdrant stability; schedule periodic syncs to prevent drift.  
- Consider updating zulu orchestrator to use `gpt‑oss:120b` once adapter issue resolved.  
- Finalize headless zulu script (`zulu-cli.ps1`).  
- Schedule periodic probe/maintenance after core units live (U11 cron).  
- Resolve vision provider choice (Claude vs local Ollama).  
- Confirm no other hardcoded `localhost` in fleet‑wide engines.  
- Ensure deployment pipeline picks up targeted emit without disrupting peers.  
- Build automated LLM routing system per new goal; integrate with Claude Opus review arm.  
- Coordinate remaining soul realignments with Papa.  
- Finalize shop‑floor & cad‑fusion‑live tribal/memory thresholds.  
- Ollama‑server‑env restart (#1) pending operator coordination.
