# frontend-app session 91364a5d (2026-05-27, 32.1MB, spine 151KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit `3cd8a1aba4` (slot/kilo): PDF extraction layer – 12 files, 1392 insertions (`U-PDF-CORPUS-FULL-LAYER`).  
- Commit `32a707ec22`: absorption of same layer into oscar’s peer‑commit; re‑committed to kilo.  
- Commit `ed02805d58` (slot/kilo): fleet training corpus inventory – `state/shared/training/fleet-training-corpus-inventory.json`.  
- Commit `d8bd95f102` (slot/kilo): self‑learning loop substrate – `TemplateApplicabilityClassifierEngine`, `SelfLearningLoopOrchestratorEngine`, `OutcomeFeedbackWireEngine`; 62/62 tests pass.  
- Commit `f6118295d1` (slot/kilo): `ToolpathTipRetrieverEngine` + E2E test; 58/58 PASS.  
- Commit `cdaa42f0e6` (slot/kilo): YouTube tribal harvest – 142 tips from 22 videos, catalog coverage updated to 224 toolpaths.  
- Commit `67f6f4dbc1` (slot/kilo): Mastercam & hyperMILL deep‑dive – additional YouTube tips (415 total).  
- Commit `98d88a71be` (slot/kilo): pypdf extraction of JM DIE TRIBAL+WIKI PDFs – 4,477 pages, 78,561 training rows.  
- Commit `35bd64890d` (slot/kilo): closed‑loop deliverable chain linking prior commits.  
- Commit `7957b8a48b` (slot/kilo): exhaustive catalog of 224 toolpaths with wiki nodes; 115 catalog‑only gaps flagged.  
- `scripts/blueprint-pmi-extract.py`: PDF PMI extraction – 12/12 tests.  
- `scripts/print-to-cad-handoff.py`: orchestrator + unit‑gate contract, 9/9 tests.  
- Wiki entry `knowledge/wiki/architecture/open-source-vision-options-for-blueprint-ocr.md`.  
- Doctrine memory `feedback_wiki_for_how_to_memory_for_pointers.md`.  
- Chat‑bus broadcast to foxtrot/whiskey/echo/mike/oscar with contract spec.  
- Truncated `reference_open_source_vision_options_2026_05_27.md` (pointer only).  

**DECISIONS**  
- Use slot‑kilo for `/checkin-kilo` wrapper; lock via `/checkin`.  
- Adopt Lima’s `pypdf` extractor + `pdf‑parse v2` crash‑safety patch.  
- Employ `youtube-free-extract.mjs` for transcript harvesting; regex match tips to software/toolpath.  
- Build self‑learning loop substrate before full training pipeline.  
- Generate exhaustive toolpath catalog (224 entries) and always‑emit wiki nodes, marking catalog‑only gaps.  
- Prioritize PDF extraction over OCR initially; defer blueprint extraction to background.  
- Use free Ollama Qwen2.5‑VL (`ollama pull qwen2.5vl:7b`) as Claude‑Vision replacement.  
- Install Tesseract OCR for scanned PDF PMI extraction.  
- Reverse engineer F3D metadata only (Manifest.dat); full feature tree requires Fusion export.  
- Build geometry‑verification gate engine to enforce part‑class match before delta emits CAD.  
- Wire existing TS engines (`BlueprintOCREngine`, `PrintToCADOrchestratorEngine`, etc.) into `prism_cam` dispatcher; no new engines needed.  
- Mastercam X8 cannot be upgraded without payment; use Fusion for new work, keep X8 for legacy tasks.

**OPERATOR DIRECTIVES**  
- `/checkin-kilo — slot‑locked /checkin`.  
- “Lima is working on prism academy that might touch into cam for wiki and tribal knowledge so check it.”  
- “Ensure we link PDFs for the CAD/CAM software in the resources folder to the CAD and CAM node domains for easy access.”  
- “Run next batch of tasks before utilizing everything gathered by all chats to use in the training pipelines.”  
- “Generate nodes for ALL text and content (convert to HTML if helpful), convert to nodes to wire into CAD and CAM, generate wikis and tribal knowledge if not already done.”  
- “Extract page‑by‑page notable data that will train the system from easiest input to complex work.”  
- “Do more deep research hunting for PDFs/books for CAD drawing 2D/3D, assemblies, complex drawings; deep dive in blisks, turbines, molds.”  
- “Build everything needed to reach 100% coverage for closed‑loop self‑learning + template generation.”  
- “Keep extracting wiki + tribal knowledge for CAM know‑how injection within cam domain node; use Lima’s method for PDF extraction on H:/PRISM/JM DIE/TRIBAL + WIKI PDFs pertaining to CAD/CAM software, look for reputable sources online for more CAM tips and tricks for each CAM software.”  
- “Extract wiki and tribal knowledge from as many videos as it takes to learn how to use every function in each primary CAM software; generate wiki and tribal knowledge nodes for every single tool path.”  
- “Find sources online with samples for both hypermill and mastercam.”  
- “Run next loop → execute full combinatorial training batch (≈50 k outcomes).”  
- “Scale to 50k – already completed.”  
- “Build everything – focus on F3D metadata extractor, vision bridge, Tesseract wrapper, geometry gate.”  
- “Deep dive and assess what else we need to do to improve print reading and CAD file reading.”

**FINDINGS/BUGS**  
- `pdf‑parse v2` emits stray async errors → fixed by outer try/catch & per‑PDF timeout.  
- Rate limiting on API calls (Ollama, external services) intermittently blocked progress.  
- Mastercam `.chm` help files missing; only PDFs extracted.  
- Esprit documentation absent in resources folder.  
- Lathe tribal knowledge completely missing; many toolpaths lack coverage.  
- 115 catalog‑only gaps flagged after exhaustive catalog generation.  
- NN/GNN tier‑5 AUROC not graded; training pipeline stalled.  
- Ollama `/api/chat` offline, preventing LLM‑based training steps.  
- Windows path separator bug caused machine inference to be `unknown`.  
- Jaccard similarity inflated by defaulted PMI fields → 99 % direct classification artifact.  
- Silent fallback on ambiguous callouts (inch→mm conversion).  
- Outcome shape mismatch (`'failure'` vs `'fail'`) caused silent drops and zero demotions.  
- F3D files are new binary `.dat/.smb`; SQLite parser unusable.  
- Tesseract not installed; OCR bridge missing.  
- Vision model absent; Ollama pull required.

**DOMAIN SPECIFICS**  
- Slot binding via `.claude/helpers/chat-slots.mjs` (claim/force takeover).  
- Drift checks: `audit-roadmap-drift.mjs`.  
- State snapshot & graph queries: `build-state-snapshot.mjs`, `system-viz-query.mjs`.  
- PDF processing engines: `PDFProcessingPipelineEngine`, `pdf‑parse v2`, `pypdf`, `youtube-free-extract.mjs`.  
- Self‑learning loop engines: `TemplateApplicabilityClassifierEngine` (kNN‑Jaccard), `SelfLearningLoopOrchestratorEngine` (FSM), `OutcomeFeedbackWireEngine`, `ToolpathTipRetrieverEngine`.  
- Function index engines per CAM software (`HyperMILLFunctionIndexEngine.ts`, `MastercamFunctionIndexEngine.ts`, etc.).  
- Catalog file: `mcp-server/data/state/cam-toolpath-catalog.json` (224 entries).  
- Scripts: `scripts/extract-cad-cam-pdf-content.mjs`, `scripts/generate-complete-toolpath-coverage.mjs`.  
- Geometry‑verification gate engine, blueprint OCR engine (`BlueprintOCREngine`), print‑to‑CAD orchestrator (`PrintToCADOrchestratorEngine`).  
- F3D metadata extractor (`extract-f3d-metadata.py`) and PDF PMI extraction script.  
- Dispatcher: `camDispatcher` (~2 475 actions).  
- State/DB: SQLite `memory.db`, HNSW vector index, JSONL traces.  
- Key resource paths:  
  - `resources/HYPERMILL/hyperMILL/{31.0,33.0}/cycLang/English/` – cycle `.LOC`.  
  - `resources/OPEN MIND/doc/{31.0,33.0}/E-Learning/Machine‑simulation/en-US/story_content/*.mp4`.  
  - `H:/PRISM/resources/JM DIE/...` – hyperMILL, Mastercam, Esprit corpora.  
  - `H:/PRISM/resources/CAD FILES/` – STEP/STP only.  
  - `JM DIE/FUSION CAD AND CAM FILES/` – .dxf, .step, .f3d.

**TOOLS USED**  
- `.claude/helpers/chat-slots.mjs`, `audit-roadmap-drift.mjs`, `build-state-snapshot.mjs`, `system-viz-query.mjs`.  
- PDF tools: `pdf‑parse v2`, `pypdf` (Lima).  
- Video/Audio: `youtube-free-extract.mjs`, FFmpeg, Whisper, Claude Vision.  
- OCR & vision: Tesseract OCR, Ollama (`qwen2.5vl:7b`).  
- Engines: `TemplateApplicabilityClassifierEngine`, `SelfLearningLoopOrchestratorEngine`, `OutcomeFeedbackWireEngine`, `ToolpathTipRetrieverEngine`, `BlueprintOCREngine`, `PrintToCADOrchestratorEngine`, `CADReverseTemplateEngine`, `StepImportEngine`, `DXFGeometryParserEngine`, `F3DSQLiteParserEngine`.  
- Node.js/TypeScript build system, npm scripts.  
- Python 3 stdlib (`zipfile`, `sqlite3`) for F3D metadata extraction.  
- Git, chat‑bus, slot‑management utilities.

**OPEN THREADS**  
1. Complete blueprint PMI extraction (`scripts/blueprint-pmi-extract.py`).  
2. Ship `extract-f3d-metadata.py` (Tier A free win).  
3. Install Tesseract and pull Qwen2.5‑VL; build OCR & vision wrappers.  
4. Wire existing TS engines into `prism_cam` dispatcher.  
5. Run full JM corpus coverage with real PMI (post‑OCR/vision).  
6. Implement geometry‑verification gate engine.  
7. Commit all uncommitted changes and finalize memory entries.  
8. Complete blueprint extraction of 2,975 JM DIE drawings (background).  
9. NN/GNN tier‑5 AUROC promotion & retrain pipeline.  
10. Ollama `/api/chat` recovery to enable LLM‑based training steps.  
11. Further training of the self‑learning loop (promote/demote thresholds, outcome ledger integration).  
12. Expand toolpath coverage for missing categories (lathe, wire EDM, specialized strategies) and acquire additional sample files for hypercad/hypermill and mastercam.  
13. Execute full training pipeline using assembled corpus and catalog.
