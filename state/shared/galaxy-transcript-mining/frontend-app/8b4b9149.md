# frontend-app session 8b4b9149 (2026-06-10, 48.9MB, spine 523KB, 6 slice(s), model gpt-oss:20b)

**SHIPPED**  
- 5ae481f748 – EPERM rename‑leak fix in `OutcomeCaptureBusEngine.ts`  
- 86f0e3fe0c – wire TriComparator, ExhaustiveCombination, DownstreamSubscriber into `calcDispatcher`; added 16 round‑trip tests  
- 890b2c39e7 – spec addendum “Closed‑Loop Training – Live Results” (closed‑loop training on live 86‑cell sweep)  
- 16d6eecef4 – `SpeedFeedCalibrationPersistEngine` + tests; persisted calibration model  
- e9b68da865 – canonical `CANONICAL_TOOL_MATERIAL_SPEED_FACTOR` table, explicit‑only guard  
- 658c8280fe – coolant axis: wired existing `CoolantVcModifier`; removed duplicate table  
- 585584e3ae – de‑inline rigidity→Vc factor to constants (no inline physics)  
- 7d0affcae6 – applied tool‑material & coolant factors to default mode alternatives  
- f998f8af71 – applied factors into PRISM‑optimized mode alternatives; exposed on orchestrator surface  
- da41a58fd1 – full 102 k‑combination sweep, 733 distinct Vc values (runStreaming)  
- c48829e331 – axis‑liveness probe: 8 live axes, 4 inert (spindle.hp, workholding.type, tool_holder.type/runout, controller)  
- d03458fff1 – extended probe to include `tool_life`; confirms runout impact is inert on life  
- 2070c472a4 – shipped workholding‑type → cutting‑force cap (`ClampingForceEngine` wiring)  
- 7dc7798033 – shipped spindle.hp → power clamp (advisory‑to‑clamp conversion)  
- 8b191d5427 – shipped runout→life (applied existing `runoutImpact.life_reduction_pct`)  
- a257f872bb – updated probe: `toolpath.strategy`, `cut_type`, `mode` live; fixed probe bug  
- 81e37ece16 – added numeric finish‑RA cap (`target_ra_um`; Brammertz Ra = fz²/(32r))  
- 6a3ad56545 – U‑OSC‑COMPARE‑TOOLMATERIAL‑VARIABILITY (576 tool_material sweeps)  
- c78faa5a73 – U‑OSC‑COMPARE‑HSS‑BASELINE (54 HSS baseline points, 6 tests)  
- 835df42c74 – U‑OSC‑COMPARE‑CERAMIC‑CBN‑BASELINE (ceramic & CBN entries added, 12 tests)  
- 4c544db4ae – U‑OSC‑COMPARE‑PER‑VENDOR (explicit G‑Wizard/CNCCookbook and HSMAdvisor deltas)  
- 08d7fc6d37 – U‑OSC‑ALL‑AXIS‑SWEEP (OAT×2 regimes + factorial, 16/25 live axes)  
- 93de130f59 – U‑OSC‑ALL‑AXIS‑TOOLLIFE (tool_life added to liveness; 18/25 live)  
- a2ec922ca2 – FIX‑2: controller_features wiring gap applied in prism_optimized mode (feed/mrr updated, 3‑of‑3 scrutiny PASS)  
- 907e74acab – fixed HSS cast‑iron over‑speed (+108%) and CBN +48% by material‑specific speed factor table  

**DECISIONS**  
- Adopt bootstrap slot‑enforce commit pattern for shared‑tree changes.  
- Replace read‑rewrite‑rename append with `fs.appendFileSync` + bounded retry to eliminate EPERM contention.  
- Wire engines following dispatcher enum/case patterns; expose only public contract (no zero‑arg getters).  
- Add comprehensive round‑trip tests covering happy path, variability, failure modes, adversarial cases.  
- Commit only changed files explicitly (`calcDispatcher.ts`, new test file).  
- Calibration derive stays CPU‑bound; GPU reserved for full sweep & judge.  
- Added `full` sample mode → ~69 k cells; introduced `runStreaming` to remove 50‑cell cap.  
- Created `SpeedFeedGpuJudgeEngine` using Ollama on RTX PRO 6000.  
- Fixed scrutiny findings: clamped flag, persist tmp cleanup, skipped_lines observability, probeGpuResidency prefix bug, ISO‑group collapse, tool‑material inertness.  
- Adopt canonical constants table for all physics multipliers; enforce `CONFIRM_CRITICAL=true` guard.  
- Dedup first: use existing `CoolantVcModifier` & `HPCVcBoostCalculator`; avoid parallel tables.  
- De‑inline hard‑coded rigidity factor to maintain “no inline physics” rule.  
- Apply factors only when explicit (safety guard); inferred defaults to 1.0.  
- Extend factors into default PRISM‑optimized mode alternatives; otherwise axes invisible on orchestrator surface.  
- Adopt a “ship‑one‑axis‑at‑a‑time” pattern with physics‑reviewer + two‑agent scrutiny.  
- Keep controller default inert (product‑philosophy decision).  
- Expand finish quality from categorical `cut_type` to tunable `target_ra_um` using Brammertz Ra = fz²/(32r) model.  
- Baseline DB is carbide‑only; added HSS, ceramic, CBN entries with cited sources.  
- Extended `BaselineSource` enum to include Tungaloy/Iscar/NTK for new materials.  
- Replaced uniform `CANONICAL_TOOL_MATERIAL_SPEED_FACTOR` with sparse override table and widened clamp (0.10–8.0).  
- Added `baseline_detail.per_source` to `TriCompareResult`.  
- Implemented two‑regime OAT sweep for all 25 named axes; added `tool_life` as separate liveness metric.  
- Applied `controller_features` smoothing factor in prism_optimized mode (feed/mrr updated).  

**OPERATOR DIRECTIVES**  
- Pick an auth provider (Clerk / Supabase / Auth0) to unblock revenue gate.  
- Decide whether to ship provider‑independent webhook‑signature fix immediately.  
- Finalize and run closed‑loop comparison driver against live G‑Wizard/HSMAdvisor data; expose alignment flags & abstentions.  
- Continue from last state; schedule recurring `/goal` to run full calculations for all possible combinations and compare to G‑Wizard/HSMAdvisor.  
- Stop hook condition: execute full SFC closed‑loop training with max variability, baseline comparison, GPU utilization.  
- Verify galaxy mapped to `H:\PRISM\resources`.  
- Investigate Charlie’s vendor‑catalog pulls; schedule Blackwell OCR batch over catalog PDFs (qwen2.5‑VL on RTX 6000).  
- Operator must populate computed speeds in G‑Wizard crib (geometry‑only data) and launch OCR batch.  
- Must not pause or ask the user; continue until condition met.  

**FINDINGS/BUGS**  
- EPERM rename failure due to Windows file‑handle contention – fixed.  
- G‑Wizard crib contains 0 tools with proven `sfm`; comparator aborts but defaults to first tool if no selector supplied.  
- HSMAdvisor returns single cached cut (634.6 m/min) for all materials; engine flags it as unaligned yet still includes in consensus median – bug: not‑aligned systems should be excluded from consensus.  
- Consensus computation currently averages misaligned external data, producing impossible numbers.  
- clamped flag false‑positive (P2).  
- persist tmp not cleaned on rename failure (P2/P3).  
- skipped_lines observability missing (P2/P3).  
- probeGpuResidency prefix‑match bug (P2).  
- `cells.length` ReferenceError after runStreaming refactor.  
- ISO‑group collapse: 15 selectable names → 6 physics profiles.  
- Tool material inertness: Vc formula lacks tool‑material term; base params carbide‑anchored.  
- Coolant, holder, machine, spindle, controller, workholding, insert axes currently inert.  
- Default mode used `sfc.alternatives.balanced.vc` ignoring tool‑material & coolant factors → axes invisible on orchestrator surface.  
- PRISM under‑speeds published baselines by ~25 % (worst –53 % on 6061 Al).  
- G‑Wizard `toolcrib.csv` geometry‑only; no speed data, so comparison contribution zero.  
- Vendor catalog PDFs font‑encoded → need vision‑OCR.  
- runoutImpact `life_reduction_pct` computed but never applied to `tool_life`.  
- Controller default inert intentionally (product‑philosophy).  
- Probe misidentified mode as inert due to testing phantom field (`optimize_for`).  
- Finish quality only had `cut_type`; numeric RA axis missing.  
- Comparison sweep hardcoded `tool_material: "carbide"` despite vendor adapters modeling HSS, ceramic, CBN.  
- PRISM over‑speeds HSS cast iron by +108% and CBN by +48%; fixed by material‑specific speed factor.  
- Ceramic under‑speed –49%; baseline now includes ceramic data.  
- Baseline DB originally only carbide; missing non‑carbide entries caused false “dead” comparisons.  
- Dead axes: `controller_features`, `spindle_thru`, partial `tool_holder_type`, `target_ra` cap not binding, `radial_pct` optimizer internalized.  

**DOMAIN SPECIFICS**  
Engines:  
- `OutcomeCaptureBusEngine`  
- `SpeedFeedCalibrationPersistEngine` (CPU)  
- `SpeedFeedGpuJudgeEngine` (GPU)  
- `SpeedFeedExhaustiveCombinationEngine` (full sweep)  
- `UltimateSpeedFeedEngine`  
- `SpeedFeedNineAxisOrchestratorEngine`  
- `SpeedFeedTriComparatorEngine`  
- `SpeedFeedBaselineComparatorEngine`  
- `CoolantVcModifier` (alg 8.5)  
- `HPCVcBoostCalculator` (alg 8.7)  
- `ClampingForceEngine`  
- `runoutImpact`  

Actions/dispatchers:  
- `calcDispatcher` (`sfc_nine_axis_run`)  
- `buildModeRecommendation`  
- `translateToUltimate`  
- `compare()` / `run()` (TriComparator, Baseline)  
- `getToolMaterialSpeedFactor()`  
- `getMultipliers({iso_group, coolant})`  
- `ClampingForceEngine.calculate`  
- `runoutImpact.life_reduction_pct`  

Metrics:  
- Vc, fz, ap, MRR, rpm, DOC (chatter‑free depth)  
- cutting_speed_mpm, feed_rate_mmmin, mrr_cm3min, tool_life_min, ae, target_ra_um  

Paths:  
- `H:/prism/state/shared/specs/SFC-VC-ASSESSMENT-2026-06-08.md`  
- `src/physics/constants.ts`  
- `algorithms/*`  
- `data/<vendor>-speed-feed-data.ts`  
- G‑Wizard crib (`settings_v2.xml`)  
- HSMAdvisor settings/machines files  
- Blackwell OCR baseline  

**TOOLS USED**  
PRISM helpers: `chat-slots.mjs`, `slot-bind-enforce` hook, `hermes-workflow-planner.mjs`.  
Audit: `/forge-audit-v2`.  
Workflow engine: Ultracode (`agent() / parallel() / pipeline()`).  
Scripts: `stable-session-id.mjs`, `update-prism-inventory.mjs`, `build-state-snapshot.mjs`, `system-synergy-map.mjs`.  
Node.js/tsx scripts, `npx tsx`, Ollama inference server (GPU LLM), CronCreate/CronDelete, Skill slash commands, scrutiny script, Git hooks, tsc, vitest.  
PRISM tools: SFC engine, axis‑liveness probe script, comparison harness, physics‑reviewer, independent reviewer, test suite.  
Dispatchers: `calcDispatcher` (`sfc_nine_axis_run`).  
CLI scripts: `/startup-oscar`, `sfc-full-sweep-compare.mjs`, `sfc-all-axis-sweep.mjs`.  
Hooks: `slot-bind-enforce`, Stop hook.  

**OPEN THREADS**  
- Finalize closed‑loop driver script; ensure honest alignment/abstention reporting.  
- Fix consensus computation to exclude unaligned external data.  
- Implement provider‑independent webhook‑signature fix.  
- Resolve auth provider choice to unlock revenue path.  
- Implement remaining axes: machine rigidity, spindle specs, controller types, workholding/fixture physics, insert geometry.  
- Add tool‑material speed‑factor multiplier to Vc formula.  
- Extend GPU judge to handle new axes and produce verdicts.  
- Finalize spec/memory updates for full sweep results; validate safety gating (S(x)≥0.98).  
- MRR reconciliation (`U‑OSC‑MRR‑RECONCILE`).  
- Rigidity DOC integration (`U‑OSC‑RIGIDITY‑DOC`).  
- Remaining inert axes: holder, spindle, controller, workholding, insert; document or refine sweep.  
- Vendor calibration extraction: schedule vision‑OCR batch for Kennametal & other high‑priority catalogs (Blackwell qwen2.5‑VL).  
- Full sweep vs live G‑Wizard/HSMAdvisor data (requires operator export of computed speeds).  
- Live G‑Wizard/HSMAdvisor closed‑app calculators remain operator‑gated; need operator to export data.  
- Remaining dead axes (`controller_brand`, `machine_accuracy`, `target_ra` cap not binding) require documentation or further sweep refinement.  
- Physics‑reviewer validation of fz force envelope for aggressive‑feed cases still pending.  
- Final integration test for controller_features wiring gap already committed but awaiting broader regression suite run.
