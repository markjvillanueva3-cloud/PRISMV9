# frontend-app session 1730eb7c (2026-05-27, 127MB, spine 288KB, 4 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `84db861343` – Academy → lathe‑wizard bridge (6 priors, 15/15 tests)  
- `38bcc524f5` – 4 local PDFs + 6 vendor research (PDF & vendor data nodes)  
- `5121df34cd` – 14‑vendor master tribal index + AI‑queryable helper (`scripts/query-lathe-tribal.mjs`)  
- `88c1b98e48` – 8 vendor PDFs downloaded, curriculum manifest created  
- `6fda303ee1` – Per‑page PDF extractor (186 lathe pages) with tests  
- U-LATHE-WIZARD-VENDOR-LOOKUP (P0) – wiring vendor JSON into wizard engine  
- U-LATHE-TRIBAL-MASTER-INDEX – 14 vendors, 87+ grades, topic buckets, query helper  
- U-LATHE-PDF-VENDOR-INGEST – 4 PDFs + 6 vendors ingested (JSONL)  
- U-LATHE-VIDEOS-TRIBAL – 200 video transcripts → wiki stubs & tribal JSON  
- U-BODY‑TOPIC‑CLASSIFIER – 34 topic buckets, body‑level classifier (`scripts/extract-lathe-videos-tribal.mjs`)  
- U-INSERT‑GEOMETRY‑EXPANSION – ANSI code list expanded to 8 codes (2→8)  
- U-WORKHOLDING‑REGEX‑EXPANSION – workholding‑chucks regex grew from 3 → 20 matches  
- `feedback_yolo_mode_nonterminal_goal_pattern.md` – doctrine on non‑terminal `/yolo-mode` loops  
- 186+ iters under non‑terminal `/yolo-mode`; cron autonomous  
- All 6 P0 engines (G76 validator, shop‑tool bridge, tribal query, wizard vendor lookup, AB‑locator, training‑loop stages 4–5) code‑complete with full test suites  
- 132/132 hermetic tests passing across wizard pipeline  
- 12 real JM‑Die programs validated (4 customers × 4 Okuma variants)  
- 8 real‑data‑driven fixes locked in by 5 regression‑test suites  
- iter199 meta‑README indexing five operator templates  
- iter200 first working AB‑locator CLI scanner (`scan-jm-die-ab-pairs.mjs`)  
- iter201 regression tests for AB‑locator scanner  
- iter202 full JM‑Die archive scan: 14 471 A/B pairs across 195 customer‑like keys (JSONL)  
- iter203 fix to `PRISM_UPGRADED` B‑folder pattern; ALCOA files now pair correctly (11/88)  
- iter204 re‑run scanner after PRISM_UPGRADED fix; pair count rises to 14 475  
- iter228 regression tests for Stage‑4 detector `detectMissingSafetyStateFlags`  
- iter257 added `--upgraded-only` flag and `pair_type` field to scanner  
- iter260,281,282 AB‑locator priority fix; SFS G80 anomaly resolved  
- iter292–301 cross‑customer scans of 20 customers confirming pure annotation pass‑through  
- iter302 training‑signal roadmap memo shipped (four‑path priority list)  
- PPGSFCClosedLoopOrchestratorEngine (U‑PPG‑SFC‑14) – 6‑phase SFC closed‑loop engine in PRISM  
- Operator‑override capture MCP action (`prism_ai:submit_sfm_override`) and history query (`prism_ai:get_override_history_size`)  
- Cron `8505e156` deleted; no more `/yolo-mode` auto‑injections  

**DECISIONS**  
- Adopt non‑terminal `/yolo-mode`: continuous autonomous iteration until goal clears; cron re‑injects every 5 min (P0 engines)  
- Tool‑batch ceiling 144 to avoid resource exhaustion  
- Design‑first, contract‑first TDD workflow  
- Real‑data validation prioritized over synthetic fixtures; bugs surfaced on real JM‑Die `.MIN` files  
- Wire vendor JSON (`wizard_query_records`) into `LatheCAMIntelligenceEngine.selectInsert`  
- Implement `prism_lathe:query_vendor_tribal` dispatcher for file‑system‑free queries  
- Closed‑loop framework uses operator overrides as training signal, not A/B pairs  
- LoRA adapters per customer × material × machine with EWC++ forgetting protection and Brier shadow‑mode promotion gate  
- Lathe wizard needs single dispatcher wire (`prism_lathe:query_vendor_tribal`) to expose tribal corpus  

**OPERATOR DIRECTIVES**  
- `/checkin-whiskey` – force‑take whiskey slot, bind to `whiskey-work`, run standard `/checkin`  
- `/goal /yolo-mode` – start autonomous loop for lathe knowledge expansion; session‑scoped Stop hook  
- `/loop [5m]` – schedule recurring cron job (`*/5 * * * *`)  
- `/goal clear` – only used manually; stop hook auto‑clears when condition met  
- “Briefly acknowledge the goal, then immediately start (or continue) working toward it — treat the condition itself as your directive and do not pause to ask the user what to do.”  
- Extract wiki & tribal knowledge from all videos via memory memo `reference_whiskey_lathe_complete_asset_map_2026_05_27.md`  
- Add to memory anything lathe machining related – memo now contains engines, actions, databases, program locations  
- Wire `prism_lathe:query_vendor_tribal` MCP dispatcher (~50 lines)  
- Persist in‑memory adapter registry to disk (`mcp-server/data/state/lora-adapter-registry.json`)  
- Hook real LoRA training backend (Flow Nexus or Ollama) and Brier oracle for shadow mode  
- Build end‑to‑end print‑to‑program orchestrator using canonical 12‑stage lathe sequence  

**FINDINGS/BUGS**  
- Regex `String.match()` lacked `/g`; fixed to `matchAll` for accurate phrase counting (topic classifier)  
- Physics deviation detection implemented: parses Vc/RPM/feed from programs, compares against academy priors; 41/41 tests passed  
- Cron interval rounding: 90 m → rounded to 1 h (`0 */1 * * *`)  
- `slot-bind-enforce.mjs` idempotent fast‑path avoids redundant reclaim/claim calls  
- CRLF → LF conversion bug (iter148)  
- G‑code operation inference missing (iter150)  
- Mazak T‑format → Fanuc 4‑digit normalization (iter151)  
- Missing drilling‑cycle G‑codes (iter153)  
- Byte‑exact round‑trip failure when no levers applied (iter154)  
- Operator‑comment extraction for T‑blocks (iter156)  
- Okuma G71 dialect misidentified as needing G70 finish‑pass (iter159)  
- A‑marker stripping in AB‑locator for source‑folder pairs (iter165)  
- ALCOA file pairing bug fixed by adding `PRISM_UPGRADED` to B‑folder patterns  
- Customer‑rootfile misclassification resolved; root files no longer treated as part names  
- AB‑locator over‑pairing: human‑revision siblings and empty source files incorrectly counted; fixed with new pair_type logic  
- `parseBlocks` erroneously matched G‑codes inside comments, causing false safety‑flag detection; corrected in iter265  
- SFS G80 anomaly due to AB‑locator priority bug; resolved in iter281  
- ITW degenerate source (`A.MIN`) produced 116‑line placeholder; classified as `empty_source`  
- Operator‑override capture path missing; override history stays empty  
- Adapter registry is in‑memory only; adapters lost on restart  
- No real LoRA training or Brier oracle – current phases are stubs/simulations  

**DOMAIN SPECIFICS**  
- Lathe domain: G‑code canned cycles (G70–G76, G96, G97, G50), insert geometry codes (CNMG/WNMG/DNMG…), ISO material groups P/M/K/N/S/H  
- Vendor data: 14 deep vendors + 25 breadth tooling & machine brands; 87+ grades indexed; vendor JSON includes tool families, grade focus, speed/feed charts  
- PDF extraction: `pdf‑parse v2` per‑page array; 186 lathe pages classified; 300+ video transcripts processed (79 lathe‑relevant)  
- Topic buckets: 34 defined (toolpath cycles, modal modes, tooling, operations, setup, process, materials, advanced secrets, controllers)  
- Engines / Actions: `lathe-g76-thread-validator`, `lathe-shop-tool-library-bridge`, `lathe-tribal-query-engine`, `lathe-wizard-vendor-lookup`, `lathe-ab-version-locator`, `lathe-training-loop-stage‑4-reason`, `lathe-training-loop-stage‑5-generate`, `PPGSFCClosedLoopOrchestratorEngine`, `TurningPrintToProgramEngine`  
- Dispatchers: `turningDispatcher`, `turningProgramDispatcher`, `threadDispatcher`, `threadingPipelineDispatcher`, `aiReasoningDispatcher.ts` (`prism_ai:ppg_sfc_closed_loop`), `turningDispatcher.ts` (orphaned lathe actions)  
- Paths: scripts (`scripts/extract-lathe-pdfs-per-page.mjs`, `scripts/query-lathe-tribal.mjs`, `scripts/extract-lathe-videos-tribal.mjs`); scanner (`scan-jm-die-ab-pairs.mjs`); archive (`jm-die-profile.ts`); PRISM_UPGRADED folder patterns; A/B file naming conventions  
- Customer scan: 14 475 A/B pairs across 195 keys, 20 additional customers scanned (HOLO‑KROME, OPTIMAS, BIRMINGHAM FASTENER, etc.)  
- Training roadmap: four‑path priority list in iter302 memo  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `checkin.md` pipeline  
- Scripts: `scripts/extract-lathe-pdfs-per-page.mjs`, `scripts/query-lathe-tribal.mjs`, `scripts/extract-lathe-videos-tribal.mjs`  
- CronCreate/cronDelete for recurring `/yolo-mode`; cron ID `8505e156` deleted, earlier `4d08d27a`  
- Git hooks: `slot-commit-enforce` for cross‑worktree commits  
- Testing frameworks: Jest (15/15, 12/12, 41/41 passes)  
- Design memo scripts (`reference_*.md`)  
- Test scaffolds (`*.test.mjs`)  
- Implementation modules (`lathe-*.mjs`, `turning-*.mjs`, `ppg-sfc-*.mjs`)  
- Memory capture hooks (`stop-obsidian-memory-feed.mjs`)  
- Zod schemas for action validation  
- Flow Nexus / Ollama for LoRA training (stubbed)  
- Brier oracle simulation module  
- Cron management (`CronDelete`)  

**OPEN THREADS**  
1. Wire `prism_lathe:query_vendor_tribal` dispatcher and test hermetic match.  
2. Persist adapter registry to disk (`mcp-server/data/state/lora-adapter-registry.json`).  
3. Hook real LoRA backend and Brier oracle into the 6‑phase engine.  
4. Build end‑to‑end print‑to‑program orchestrator (12 stages).  
5. Integrate real shop tool‑list ingestion (iter194) and PDF ground‑truth (iter195) for training data.  
6. Activate closed‑loop switch once overrides, persistence, LoRA training, and Brier oracle are in place.  
7. Complete remaining 98 JM‑Die customers scan to build comprehensive A/B corpus.  
8. Finalize operator templates (tool‑list ingestion, PDF ingestion, MCP dispatcher wiring, CLI scan‑runner, TS engine wiring).  
9. Deploy wizard pipeline into production shop floor UI/UX.
