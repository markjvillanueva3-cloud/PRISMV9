# frontend-app session 52bebb83 (2026-06-03, 5.9MB, spine 29KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Lathe adapter bound in `PipelineHarnessAdaptersEngine` (commit ≈ 3e9b3e8667).  
- Accuracy harness (`TurningPrintToProgramEngine.runFullPipeline`) committed and verified (26/26 tests, tsc clean).  
- Shared core lib extracted for parser/derive/regen/score; trainer & scorer now import it.  

**DECISIONS**  
- Replace stubbed lathe adapter with `makeLatheAdapter()` wrapping `turningPrintToProgramEngine.runPipeline()`.  
- Use `isBound('lathe')` gate in devDispatcher to expose lathe from MCP surface.  
- Add CJS shim for `__dirname` in `catalogLoader.ts` to allow raw tsx execution.  
- Adopt train/test split closed‑loop learning: learn JM shop‑profile calibration, apply to held‑out programs.  
- Persist learned profile; expose via dashboard (`state/shared/dashboards/lathe-roundtrip-accuracy.{json,md}`).  

**OPERATOR DIRECTIVES**  
- Continue the self‑learning loop until 100 % accuracy is achieved (user request).  
- Prioritize material inference from `.MIN` files and shop‑profile calibration.  

**FINDINGS / BUGS**  
- Lathe adapter was a stub → headless roundtrip impossible before binding.  
- Test failures due to old “lathe unbound” assertions; dispatcher gate hard‑coded for mill only.  
- Git index.lock contention from peer commits; stale lock cleared manually.  
- OOM during harness teardown (memory pressure from 193 fleet loops).  
- `__dirname` missing in ESM tsx → required shim.  
- Accuracy baseline: mean 41.6 % (op‑coverage 100 %, SFM 8.5 %, IPR 6.3 %).  
- After learning: mean 76 % (+30.8 pts lift on 8 held‑out programs).  

**DOMAIN SPECIFICS**  
- Lathe pipeline stages: upload → feature extraction → material identification → machine/tool selection → physics calculations → speed/feed optimization → G‑code generation → safety checks → setup sheet → backplot visualization → download package.  
- Core types: `TurningInput`, `TurningProgramResult`.  
- Engine entry: `runPipeline(input: TurningInput): TurningProgramResult`.  
- Metrics: op‑coverage, SFM in‑band, IPR in‑band, mean accuracy.  
- Data sources: Okuma `.MIN` programs (16 558), JM master cloud, PRISM physics models.  

**TOOLS USED**  
- PRISM CLI (`/checkin-whiskey`, `/lathe-studio`).  
- `PipelineHarnessAdaptersEngine.ts`, `turningPrintToProgramEngine`.  
- `devDispatcher.ts` gate logic.  
- TypeScript compiler (tsc), Vitest, tsx for harness execution.  
- Git hooks (`slot-commit-enforce`, lock‑handling).  
- Memory‑pressure mitigations (manual OOM cleanup).  
- Training script (train/test split, calibration learning).  

**OPEN THREADS**  
1. Material inference from `.MIN` to eliminate 1018/P default lower bound.  
2. Refine JM shop‑profile speed/feed calibration; target >90 % accuracy.  
3. Reduce memory pressure during harness runs (optimize fleet loops).  
4. Verify no data leakage in train/test split; enforce strict isolation.  
5. Expand safety checks for G‑code download; ensure collision, clamping, swing pass.  
6. Finalize documentation and dashboards for continuous monitoring.
