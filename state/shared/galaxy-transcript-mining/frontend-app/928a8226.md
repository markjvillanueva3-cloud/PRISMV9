# frontend-app session 928a8226 (2026-06-10, 68.1MB, spine 676KB, 8 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Hook errors cleared – parser loads 228 rules with no stderr.  
- Scheduled‑task warnings identified as intentional migration freeze (no action).  
- Unwired engines audit: 90 built but unwired flagged for owner triage.  
- Academy tribal routing clarified fallback to “general” by design.  
- Resilient Ollama pull script fixed em‑dash → hyphen; gpt‑oss synergy fix; resumed gpt‑oss:120b pull (≈94 MB/s, 96% done).  
- Hookify front‑matter & dangling hook ref fixes (`scripts/fix-hookify-frontmatter.mjs`, `scripts/remove-dangling-hook-refs.mjs`).  
- CAG-router “qdrant://prism‑memory” label corrected to real collections.  
- Embedder OOM & context-window fixes (`embed-wiki-into-tribal-index.mjs`); empty-text skip guard added.  
- System-wide gap-map artifact created.  
- Buffered tribal index loader and consumer wiring (`182788232a`) – buffer-based incremental parse, 10/10 tests.  
- Docker-service-health CLI (`fb314a6fd1`) wired into Stop hook.  
- MCP recovery: reaped duplicate daemons, started via `TRANSPORT=http`; corrected restart command in banner (`006c435ddb`).  
- Singleton-service-guard extended to reap+start logic (`e2081e0780`, `d22681f5d2`).  
- Qdrant collections verified (`prism_engines`, `prism_skills`, `prism_formulas`).  
- Migration‑freeze marker mechanism (`4141daf9d8`) replacing static list.  
- Consolidated 16 cost‑bridge hooks into single router (`cost-bridge-dispatch.mjs`).  
- Quoting awareness generator restored and made router‑aware (`generate-quoting-awareness.mjs`).  
- CostSavingsTrackerEngine test suite added (13 R9 tests, `U-QP-COST-SAVINGS-TEST`).  
- Variance test contract updated to 5 keys.  
- WEDMQuoteBridgeEngine tagged `WIRE‑EXEMPT`.  
- Ollama dispatcher defaults updated to pulled models; source‑lock extended (`no-retired-llm-refs.test.mjs`).  
- Margin‑floor guard added to QuoteEstimatorEngine (`U-QP-MARGIN-FLOOR-GATE`); shop config floor field added.  
- Dispatcher round‑trip test proving margin‑floor survives business dispatcher (`U-QP-R15-DISPATCHER-ROUNDTRIP`).  
- Frontend margin‑floor alert banner and tests (`U-QP-FRONTEND-MARGIN-BANNER`, `87d5c4bf9a`).  
- AI‑substrate routing matrix coverage expanded to 5 routes; completeness invariant added.  
- ResizeObserver polyfill inserted in web test setup; hidden frontend failures unmasked.  
- Frontend quote‑pages test repair: flat-array mock bug fixed, null guards added.  
- Dispatch map for fleet synergy goal recorded (`state/shared/SYNERGY-GOAL-STATUS-2026-06-09.md`).  
- Closed-loop provenance gate (`U-QP-CLOSED-LOOP-PROVENANCE-GATE`) – synthetic outcomes blocked from promoting calibration factors.  
- Outbound promote gate (`U-QP-OUTBOUND-PROMOTE-GATE`) – real outbound sold‑price alignment fail‑closed.  
- Calibration freshness preflight gate (`U-QP-CALIBRATION-FRESHNESS-PREFLIGHT`) – warn on >24 h stale factors, hard cutoff to raw FMV.

**DECISIONS**  
- Adopt local Ollama stack on RTX PRO 6000 Blackwell; `OLLAMA_MAX_LOADED_MODELS = 3×GPU`.  
- Use cost‑router tiers (cheap → balanced → strong → best) with explicit model mapping.  
- Replace string-based tribal index loading with buffered incremental parser to avoid V8 512 MiB cap.  
- Batch workflow agents ≤3 to stay within Anthropic rate limits.  
- Treat Stop hook as session‑scoped gate that blocks until all subsystems wired/validated.  
- Guard regex must match forward slashes; `stop_on_hook_unregistration` emits JSON `{continue:true}`.  
- MCP boot heap floor set to ≥4096 MB (`--max-old-space-size`).  
- Migration freeze marker file `state/shared/MIGRATION-FREEZE-ACTIVE.flag`.  
- Consolidate cost‑bridge hooks into single router; apply `WIRE‑EXEMPT` tags for adapters invoked via routes.  
- Make CostSavingsTrackerEngine testable by injecting storePath; keep default singleton behavior.  
- Extend source‑lock to dispatcher tree and Ollama defaults.  
- Margin‑floor gate caller‑overrideable, WARN‑flagged; default sourced from ShopConfigurationEngine.  
- Closed-loop learning gated at provenance, outbound promotion, and quote‑emission freshness.

**OPERATOR DIRECTIVES**  
- Monitor gpt‑oss:120b pull until completion; verify via `/api/tags`.  
- Run targeted test of `routeModelForTask` with full installed list to confirm selection logic.  
- Continue working toward goal without pausing; no further action required from operator at this moment.  
- “Utilize ultracode for planning, deep reasoning and orchestrating + ollama local llm for grunt work… goal clear: finish all remaining phases of units and tasks.”  
- “Synergize ollama (make sure we pulled correct models relative to gpu, cpu, nve ssd and 128 gb of ram), docker, qdrant, obsidian vault app, PSN, /system‑viz, prism galaxies, prism front end and back end build, claude.md, memories, wiki, tribal knowledge, prism awareness, gsd, tdd, skills, scripts and hooks | goal clear: everything wire, tested, validated and synergized across the entire system, node by node, galaxy by galaxy.”  
- “Continue periodic /loop iterations to address remaining galaxy‑specific gaps.”

**FINDINGS/BUGS**  
- Parse error in resilient‑pull script prevented gpt‑oss:120b download; fixed em‑dash → hyphen.  
- After fix pull resumes at ~94 MB/s, 96% done; final 2.7 GB should finish within a minute.  
- Synergy bug: routing logic prefers smaller `gpt‑oss:20b` for “search_synthesis” on Blackwell GPUs despite `120b` available.  
- Hookify front‑matter parsing failed on non‑ASCII headings; fixed by transliteration.  
- gpt‑oss synergy produced empty `.response` because model emits `thinking` channel and `ask‑ollama` omitted `num_predict`.  
- Qdrant “prism‑memory” collection missing; cosmetic label only.  
- Tribal embed index (~533 MiB) exceeded V8 512 MiB string limit → OOM on read; size guard added.  
- Embedder crashed on large files (> context window) and empty-text stubs; empty‑text skip guard added.  
- V8 hard max string length (~512 MiB) caused tribal index parse failures; default Node heap (~2 GB) OOMs when parsing >~150 MB JSON index.  
- Duplicate daemon leak caused MCP port 3100 down; reaping required.  
- Wrong restart command in connectivity banner (used stdio instead of `TRANSPORT=http`).  
- Guard miscounted MCP daemons due to regex mismatch (`mcp-server\dist\index`).  
- `stop_on_hook_unregistration` silent no‑op; JSON protocol fixed.  
- `/system-viz` find OOMs under 384 MB cap; addressed by heap floor.  
- OCR $1 noise: 65 % of ext_price rows degenerate; source‑side cleanup required.  
- Stale calibration factor ignored `is_stale`; now guarded by preflight gate.  
- Blueprint→quote adapter mismatch (OCR engine emits `gdt_frames`, bridge expects `analysis.gdt`); runtime bug resolved.  
- tsc OOM false‑green: bare `tsc` aborted before full run; 16 GB heap needed.

**DOMAIN SPECIFICS**  
- Engines: Ollama local LLM stack (`gpt‑oss`, `qwen2.5-coder`, `nomic-embed-text`); MCP server; QuoteEstimatorEngine; CostSavingsTrackerEngine; DigitalTwinEstimator; QuotingClosedLoopEngine; OutboundPriceIndexEngine; ShopConfigurationEngine.  
- Dispatchers: `ask‑ollama.mjs`, `tribal‑rerank.mjs`, `embed-wiki‑into‑tribal-index.mjs`, business dispatcher (`prism_business`), AI‑substrate routing matrix, `cost-bridge-dispatch.mjs`.  
- Metrics/paths: `/api/tags`; Qdrant collections (`prism_engines`, `prism_skills`, `prism_formulas`); tribal index file; PSN snapshot; calibration factors (`quotingActiveFactors.json`).  
- Hardware: Blackwell GPU (RTX PRO 6000), CPU, NVE SSD, 128 GB RAM.  
- State files: `state/shared/MIGRATION-FREEZE-ACTIVE.flag`; `state/shared/SYNERGY-GOAL-STATUS-2026-06-09.md`.

**TOOLS USED**  
- Node scripts (`fleet-reaper-sweep.mjs`, `chat-slots.mjs`, `load-tribal-index.mjs`, `singleton-service-guard.mjs`, `docker-service-health-check.mjs`).  
- PowerShell script `ollama-resilient-pull.ps1`.  
- Ollama API `/api/tags`; local file inspection for non‑ASCII.  
- PRISM tools: CronCreate, fleet‑reaper, hookify plugin, workflow orchestration (`/effort`, `/loop`), ultracode workflow, ollama local llm `call-ollama.mjs`.  
- Test harnesses: vitest (3-of-3 scrutiny), Jest, tsc with 16 GB heap.  
- Git hooks, CI pipelines; `ask‑ollama` CLI.

**OPEN THREADS**  
- Resolve routing logic to select `gpt‑oss:120b` over `20b` for best tier “search_synthesis”.  
- Confirm full installation of `gpt‑oss:120b`; re-run fleet‑reaper sweep after install.  
- Finalize empty-text skip guard in embedder; run full 6 k-file driver to >99 % coverage.  
- Wire buffered tribal index loader into all remaining consumers (e.g., PSN leg #5).  
- Verify Stop hook condition satisfied once all subsystems wired/validated; clear gate.  
- Monitor fleet‑reaper and monitors for orphaned processes after latest driver run.  
- Qdrant tribal migration to replace JSON index (deferred pending operator confirmation).  
- Re‑embedding lost ~29 K entries; re‑populate tribal index to >10.5 k baseline.  
- Audit raw-string readers for fail-open patterns; implement fail-loud guards where needed.  
- U-QP-ACCOUNTING-WIRE: integrate ERP actuals into quoting system (pending budget).  
- Hotel analytics tests (`QuoteAnalyticsPage`, `JMDieFleetScanStatusPanel`) unresolved.  
- Blueprint OCR bridge adapter implementation and tests pending final review.  
- Outbound OCR noise filter: source‑side cleanup required before gate can go live.  
- Use‑layer intake→quote path validation for production readiness.
