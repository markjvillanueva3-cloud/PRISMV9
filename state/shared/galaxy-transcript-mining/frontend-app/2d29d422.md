# frontend-app session 2d29d422 (2026-05-26, 55.1MB, spine 147KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `834145ad9a` – ChainOfVerificationEngine (substrate)  
- `afe76af0a2` – QuotingActiveFactorLoader + health UI, calibration bridge  
- `78e8e27a7c` – 13‑axis audit spec & estimateCalibrated wire‑up  
- `4122176561` – LeadTime, SecondaryOps, Tolerance, CrossPart synergy engines  
- `f407c6d527` – Tesseract OCR bridge + FreightCost engine  
- `7030bfae9a` – PSI‑delta bridge (NN/GNN retraining trigger)  
- `d050b3ecab` – QuotingWorkbenchPage (mobile‑first UI)  
- `3b82ce312c` – McMaster API adapter, Stress‑Test engine, Docustrata trainer stub  
- `5b370300f0` – clean customer extraction / regex extension (`POST PROCESSORS`, `_PART LIBRARY`)  
- `acee69cad3` – JSONL drift‑audit ledger (`train-cycle-history.jsonl`) + buildLedgerRow helper  
- `bd3ad1ffc7` – rolling‑window summarizer (MAPE, CoV, safe‑to‑activate)  
- `b1c6a096ff` – 3‑tier alert classifier / Drift alert detector (`detectDriftAlert`)  
- `71e08eae58` – per‑record signal variance (variance buckets: machine_class × time_bucket × material)  
- `88f6f975ae` – wiki entry for pipeline discoverability  
- `4f00ed1473` – latest‑drift‑alert.json state file  
- `15b09088ad` – bootstrap distribution probe (`--summary`)  
- `3de92ef087` – round‑trip E2E composition test (baseline → train → ledger → alert)  
- `3820f1ed4f` – Docustrata bridge shim / revenue overlay (`mergeDocustrataRevenue`)  
- `2d4e2cfa3e` – Docustrata format validator (`validateDocustrataPayload`)  
- `d9f727aa06` – synthetic revenue generator (`generateSyntheticRevenueRecords`)  
- `cb52c38aee` – Docustrata pipeline orchestrator (extractor source flag)  
- `7bc1c940e3` – Windows Scheduled Task installer (`install-quoting-pipeline-cron.ps1`)  
- `0158f14138` – sample fixture for validator contract  
- `d74521aa4c` – SessionStart alert banner formatter  
- `211ab8e1f3` – verify‑script bug fix (cron tests discovery)  
- `67e1b53aa4` – live baseline staleness finding (`baseline-records.json`)  
- `8235c3c725` – regenerated baseline + new findings  
- `848e0107ab` – regex extension for noise patterns  
- `eafec0ccb9` – JM‑Die layout audit (layout inversion fix)  
- `491ed8602c` – machine/customer filter fix (`machine_class × time_bucket`)  
- `a38d790324` – real‑customer chain evidence (`real_customer_subdir`)  
- `4f6a1c92fc` – balanced sampling, 3‑way variance (variance buckets)  
- `ae75d99e9b` – numbered‑prefix filter fix  

**DECISIONS**  
- Adopt substrate‑first architecture: ChainOfVerificationEngine powers all domain engines.  
- Ship calibration bridge (`QuotingActiveFactorLoader`) before UI to guarantee calibrated quotes.  
- Use pure‑function ledger + summarizer pipeline for observable drift, no side‑effects in training loops.  
- Windows Task Scheduler entry runs `quoting-baseline-bootstrap.mjs` → `quoting-train-cycle.mjs --json`.  
- Closed‑loop pipeline: bootstrap → train‑cycle → ledger → summarize → alert → state.  
- Variance injection via file metadata (`deriveRecordDefaults`) to avoid degenerate training data.  
- Docustrata revenue overlay with `revenue_source` flag for mode detection.  
- Single‑command health check (`quoting-pipeline-verify.mjs`) + full‑chain smoke test.  

**OPERATOR DIRECTIVES**  
- “Continue all remaining quoting units and keep training the system. Operator is asleep — yolo mode, no questions, build the next unit, commit, tick, continue.”  

**FINDINGS/BUGS**  
- Customer‑extractor over‑broad → fixed regex to exclude non‑customer subdirs (`POST PROCESSORS`, `_PART LIBRARY`).  
- Percentile calculation bug in ledger summarizer → corrected to NIST nearest‑rank.  
- Iter10: shared‑tree lock absorbed 4 hotel files → mitigated with pre‑commit empty‑index check.  
- Iter11: NIST percentile bug fixed by test.  
- Iter17: incorrect `cov_gate_fail_rate` assumption → test corrected.  
- Iter28: Math.round boundary caused stale alert mis‑detection → fixed.  
- Iter32: cron‑install tests silently excluded from verify; regex fixed.  
- Iter33: baseline‑records.json pre‑iter13 stale → regenerated needed.  
- Iter34: iter9 regex too strict (PRISM MODIFIED POST PROCESSORS, HURCO CNC PROGRAMS) and JM‑Die layout inverted → addressed in iter35–39.  

**DOMAIN SPECIFICS**  
- **Engines/Actions**: `QuotingCalibrationEngine`, `QuotingActiveFactorLoaderEngine`, `ChainOfVerificationEngine`, `McMasterCarrAPIAdapter`, `QuotingPipelineStressTestEngine`, `DocustrataHistoricalPricingTrainerEngine` (stub), `TrainCycleLedgerBuilder`, `SummarizeLedger`, `DetectDriftAlert`, `bootstrap`, `trainCycle`, `buildLedgerRow`, `summarizeLedger`, `detectDriftAlert`, `deriveRecordDefaults`, `mergeDocustrataRevenue`, `validateDocustrataPayload`, `generateSyntheticRevenueRecords`, `runDocustrataPipeline`.  
- **Dispatchers**: 27 `prism_quoting` actions (`shop_profile_get/list`, `wizard_to_quote`, `print_to_program_to_quote`, `speed_feed_to_cycle`, `secondary_ops_price_for_profile`, `cross_part_synergy_from_fleet`, `machine_invest_roi`, `dynamic_shop_rate`, `shop_electricity_cost`, `shop_utilities_cost`, `quoting_mcmaster_{quote,batch}`, `quoting_pipeline_stresstest`, `quoting_docustrata_train`, `quoting_outcome_psi_delta_{score,batch}`).  
- **Metrics**: MAPE, signed bias, CoV gate, safe‑to‑activate flag, PSI‑delta score, drift level, alert thresholds (ALERT 2108%, WARN 850%, INFO 200%), variance buckets.  
- **Paths**: `/quoting-workbench`, `/quoting-calibration-health`, `/system-viz-brain`, `/goal-20`, `state/shared/quoting/baseline-records.json`, `state/shared/quoting/latest-drift-alert.json`, `state/shared/quoting/docustrata-revenues.sample.json`.  

**TOOLS USED**  
- PRISM CLI (`/checkin-charlie`), `chat-slots.mjs`, `CronCreate`, scripts: `quoting-baseline-bootstrap.mjs`, `quoting-train-cycle.mjs`, `install-quoting-pipeline-cron.ps1`, `quoting-docustrata-pipeline.mjs`.  
- Vitest, Jest/Node test files (`*.test.mjs`), meta‑runner `quoting-pipeline-verify.mjs`.  
- TypeScript (tsc), Git hooks for slot‑worktree discipline, pre‑commit empty‑index check.  

**OPEN THREADS**  
- Docustrata extractor wire (iter42) – finalize real revenue bridge (`mergeDocustrataRevenue`).  
- Dispatcher wire – peer‑contended integration not yet addressed.  
- Validate baseline bootstrap: ensure `quoting-baseline-bootstrap.mjs` pulls realistic JM Die historical records and writes `baseline-records.json`.  
- Expose drift alert via `/system-viz-brain` or new UI panel for quick monitoring.
