# frontend-app session 68828b1a (2026-06-04, 26.7MB, spine 164KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- 06ac0f7ab8 – fixed stale bravo soul (`role: hermes‑zulu-builder`).  
- ed5f6d3cde – added `FLEET-DOMAIN-GOALS-2026-06-03.md` (17 galaxy goals).  
- 6ead9a74e9 – updated `rgs-pipeline-rules.mjs`: +30 pipeline rules, +6 agent rules.  
- a679e455c1 – added `switch‑claude‑account.mjs`; U2 swap; 12 tests passed.  
- 5cf1a88ed7 – absorbed U6 auto‑swap wiring.  
- c988a21ec4 – Hermes wiring script with auto‑rollback (`wire-hermes-local-backend.mjs`).  
- 856c417d2b – Octopus upgrade to `gpt‑oss:120b`, `qwen2.5-coder:32b`; 56/56 tests green.  
- 1bb66a1822 – Hermes “coder brain” planner + harness emitter; 37/37 tests green.  
- 23de0e7881 – Dream‑cycle OOM fix (inverted‑index blocking, DF cap <4 s).  
- f2564b81a0 – H: drive atlas generator; maps all 145 top‑level dirs.  
- 443c84d08b – Reactive 429‑fallback executor (`routeWithFallback`); 52/52 tests green.  
- `hermes-obsidian-memory-bridge.mjs` shipped, scheduled every 15 min.

**DECISIONS**  
- Use `/startup-bravo` slot‑binding wrapper with auto‑enforce for deterministic bravo claim.  
- Run Hermes on Claude Opus 4.8 (Anthropic) to avoid credit exhaustion; MCP at :3100.  
- Schedule memory bridge independent of quota; enable `cron_mode: deny` → later allow scheduled autonomy.  
- Account‑switch cycle: U1 capture, U2 swap, U4 5h populator, U6 coordinator; token‑counter gating and staggered chat restarts via `account-switch-restart-coordinator`.  
- Rate‑limit‑safe bursts ≤5 agents to avoid shared‑Claude quota saturation.  
- Adopt native 64 K context local model as primary; pull `Qwen3-Coder 30B` (256 K) instead of rope‑scaling `qwen2.5-coder:32b`.  
- Reviewer can be local `gpt‑oss:120b` or cloud Opus 4.8, switched on 429 via reactive fallback.  
- Enable Obsidian‑vault bridge, dream‑cycle synthesis, H: atlas for full vault coverage.  
- Keep Hermes planner + harness emitter to generate workflow skeletons before execution.

**OPERATOR DIRECTIVES**  
- Rotate exposed `GEMINI_API_KEY`.  
- Capture second Claude account (`claude login` → `capture‑claude‑credentials.mjs`) and run account‑switch loop test.  
- After pulling `Qwen3-Coder 30B`, wire Hermes config:  
  ```
  node scripts/wire-hermes-local-backend.mjs --model qwen3-coder:30b --apply
  ```  
- Optionally `/compact` to start fresh context before wiring.

**FINDINGS/BUGS**  
- Hermes boot‑looped due to OpenRouter credit exhaustion; fixed by switching to Anthropic Opus 4.8.  
- U4 5h‑populator missing `rate_limits.five_hour`; auto‑switch cannot fire automatically.  
- MCP :3100 down during loops; needs restart.  
- `cron_mode: deny` blocks scheduled autonomy; not yet enabled.  
- Shared‑tree `.git/index.lock` contention caused commit absorption; use pathspec commits.  
- RGS defaults reference retired Ollama models (`deepseek-r1:14b`, `qwen2.5-coder:14b`).  
- Pre‑existing consensus test failures fixed by hermetic env isolation.  
- 11 failures from unstubbed Gemini/Grok voices resolved with correct defaults.  
- Dream‑cycle OOM reduced to <4 s via inverted‑index blocking & DF cap.  
- H: drive atlas now maps all 145 top‑level directories.  
- Reactive fallback executor handles rate limits.

**DOMAIN SPECIFICS**  
- Hermes desktop app (Electron + Python backend, Web UI :9120).  
- Obsidian vault integration via MCP bridge; memory sync to `knowledge/hermes‑brain/`.  
- Zulu orchestrator task for runtime liveness.  
- Account‑switch cycle: U1 capture → U2 swap → U4 5h populator → U6 coordinator.  
- RGS pipeline rules (`rgs-pipeline-rules.mjs`) govern unit‑to‑skill mapping.  
- Octopus model roster (`MultiModelConsensusEngine.ts`).  
- Engines/dispatchers: `MultiModelConsensusEngine`, `PRISMContextInjectorEngine`, dream‑cycle engines, `multi-provider-router`.  
- Actions: account‑switch scripts (`switch‑claude‑account.mjs`), octopus model selection, Hermes workflow planner/emitter, dream‑cycle synthesis, atlas generation.  
- Metrics: test counts (56/56, 37/37, 52/52); OOM <4 s; 145 dirs mapped by atlas.  
- Paths: `/hermes-workflow-planner.mjs`, `/hermes-workflow-emitter.mjs`, `/wire-hermes-local-backend.mjs`, `dream-cycle/*.mjs`, `atlas-generator.mjs`.

**TOOLS USED**  
- `cronCreate`, `slot-slots.mjs`, `chat-slots.mjs`.  
- Skills: `/startup-bravo`, `/capture-claude-credentials`, `/switch-claude-account`.  
- Scripts: `fleet-wake-sequencer.mjs`, `account-switch-restart-coordinator.mjs`, `hermes-obsidian-memory-bridge.mjs`, `wire-hermes-local-backend.mjs`.  
- RGS rule table (`rgs-pipeline-rules.mjs`).  
- PRISM tooling: `multi-provider-router.mjs`, `ask‑ollama.mjs`, `ollama-pipeline-injector.mjs`.  
- Testing: `vitest`, `node:test`, `tsc`.  
- Git hooks: lock‑sweep, commit attribution.

**OPEN THREADS**  
- Enable Hermes `cron_mode` for scheduled autonomy.  
- Seed kanban board and configure goal for Hermes.  
- Wire mcp‑obsidian stdio bridge so Obsidian becomes true MCP server.  
- Build U4 5h populator to emit `rate_limits.five_hour`.  
- Update Octopus defaults to current powerful models (`gpt‑oss:120b`, `qwen2.5-coder:32b`).  
- Resolve shared‑tree lock contention for future commits.  
- Verify MCP :3100 uptime and Hermes connectivity before next autonomous run.  
- Finish pulling `Qwen3-Coder 30B` (~18 GB).  
- Wire Hermes config to local backend after pull completes.  
- Verify account‑switch loop with captured credentials.  
- Ensure Obsidian‑vault bridge continues functioning post‑wiring.  
- Integrate slash‑command/Ollama routing for high‑ROI commands.
