# frontend-app session 92ef25c0 (2026-05-27, 25.7MB, spine 88KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- **Courses 44‑58 + 60** – 13 new curriculum modules, fully wired in `CurriculumEngine.ts` and web blueprints (`web/src/data/academy.ts`).  
- **Course 59 (video atlas)** – video‑embed renderer, YouTube‑picks resource, `/learning/academy` route fix.  
- **Wiki & Tribal** – 20+ wiki entries, 8 946 page‑level tribal items from the JM Die corpus, 154 machine‑specific tribal items, 43 data‑bridge nodes.  
- **Free local video pipeline** (`video_learn_local.py`, `whisper_local.py`, `vision_analyze_ollama.py`, `knowledge_extract_ollama.py`).  
- **Page‑by‑page PDF extractor** – full corpus drain (74 PDFs → 8 752 notable page entries).  
- **Deep‑research wiki on chip control & morphology** (commit d3cf898f92).  
- **AcademyHub drill‑down component** (`AcademyHub.tsx`) with domain/sub‑category selector and “Generate Optimized Coursework” button.  
- **Phone install infra** – `dev-seed-apprentice.html`, `phone-tunnel.ps1`/`.sh`, Vercel/Netlify configs, Playwright smoke test.  
- **Per‑employee tracks & dev‑seed picker** (Mark, Chris, Justin).  

**DECISIONS**  
- Replace paid APIs with free local alternatives: Whisper via `openai-whisper`, Vision via Ollama (`qwen2-vl`/`llava`), knowledge extraction via `qwen2.5-coder`.  
- Adopt R7 conflict‑fork and R12 fail‑loud for safe merges and gap reporting.  
- Use pypdf for PDF extraction; no external OCR or transcription services.  
- Deploy PWA through Cloudflare tunnel (`cloudflared`) for HTTPS on local dev, with future Vercel/Netlify production URLs.  
- Keep curriculum data in `CurriculumEngine.ts` + `web/src/data/academy.ts`; new courses added via imports and `RICH_MODULES`.  

**OPERATOR DIRECTIVES** (verbatim)  
- `/checkin-lima … /loop [5m] /goal /yolo-mode` – trigger massive course expansion.  
- “Continue from where you left off.” – resume pending R12 items.  
- “Install on phones.” – set up PWA install flow.  

**FINDINGS/BUGS**  
- Video extraction failed due to missing ffmpeg and captions; fixed by free pipeline.  
- Encrypted PDFs (3) + zip/malformed files in JM Die corpus; flagged for operator triage.  
- Blisk geometry absent from corpus – R12 gap noted.  
- Cloudflare tunnel mis‑pointed to Express on 127.0.0.1:3100; corrected to Vite on 0.0.0.0:3100.  
- `/academy` redirect bug fixed to `/learning/academy`.  

**DOMAIN SPECIFICS**  
- **Engines:** `CurriculumEngine`, `VideoLearningEngine`, `PDFExtractor`, `KnowledgeExtractOllama`, `VisionAnalyzeOllama`.  
- **Actions/Dispatchers:** `chat-slots.mjs` (slot claim), `audit-roadmap-drift.mjs`, `system-viz ping`, `knowledge_extract_ollama.py`, `vision_analyze_ollama.py`.  
- **Metrics:** course completion, quiz scores, PWA install & usage stats.  
- **Unique Paths:** `/learning/academy`, `/dev‑seed‑apprentice.html`, `/video‑learn`, `/api/v1/auth/login`.  

**TOOLS USED**  
- PRISM tools: `CurriculumEngine.ts`, `web/src/data/academy.ts`, `scripts/generate-jm-fleet-wiki-tribal.mjs`, `video_learn_local.py`, `pdf-learn` scripts.  
- Dispatchers/skills: `chat-slots.mjs`, `audit-roadmap-drift.mjs`, `system-viz ping`.  
- Scripts/hooks: `phone-tunnel.ps1/.sh`, `cloudflared` install, Playwright smoke tests (`apprentice-smoke.spec.ts`).  

**OPEN THREADS**  
- Persist course‑pick selections between sessions (U‑ACADEMY‑PICKS‑PERSIST).  
- Auto‑select tracks on login based on employee role (U‑ACADEMY‑TRACK‑AUTOSELECT).  
- Add YouTube sections to all 60 courses (U‑COURSES‑VIDEO‑EMBED‑PASS).  
- Deploy permanent production URL via Vercel/Netlify.  
- Replace dev‑seed with real ERP employee records for authentication.  
- Offline lesson content caching for true PWA experience.
