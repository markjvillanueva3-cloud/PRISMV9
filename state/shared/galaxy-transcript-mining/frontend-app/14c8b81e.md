# frontend-app session 14c8b81e (2026-06-09, 5.4MB, spine 74KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- Commit 316872f7 – `scripts/call-engine.mjs` + `.cmd/.sh` wrappers + 8‑case `call-engine.test.mjs`.  Server‑free engine invoker that replicates all dispatcher functionality (Zod validation, `--list`, `--methods`, fail‑loud).  
- Commit 38af3407 – `27-wsl-memory-guard.mjs` (CLI & pure helpers), `27-wsl-memory-guard.test.mjs` (15/15 pass), and `install-wsl-memory-guard-task.ps1`.  Detects WSL memory over‑run, advises or enforces `wsl --shutdown`, installs a 15‑min watchdog.

**DECISIONS**  
- Dispatcher logic is thin Zod → engine.method routers; therefore a direct node import of the plain `.ts` engines suffices – no MCP server needed for code work.  
- Build a CLI harness (`call-engine.mjs`) to expose all engine methods, enabling local testing and scripting without the MCP daemon.  
- Root cause of “API limit” errors is host memory pressure (WSL 96 GB + Ollama models).  Fix by: (1) killing zombie tsservers; (2) unloading idle Ollama models (~65 GB); (3) enforcing WSL 16 GB cap via `wsl --shutdown` and a watchdog.  
- Guarded CLI (`27-wsl-memory-guard.mjs`) uses pure helpers for testability, exits with codes 0/1/2, and is idempotent.

**OPERATOR DIRECTIVES**  
- “commit” – triggered the two commits above.  
- “try again” – prompted re‑run of zombie‑killer; later led to Ollama unload.  
- “Continue from where you left off.” – request for a concise status summary (handled).  

**FINDINGS/BUGS**  
- API limit errors were actually `ECONNREFUSED` due to MCP server OOM caused by host memory > 98 % commit.  
- WSL 16 GB cap in `.wslconfig` was never applied (`vmmemWSL` ≈ 96 GB).  
- Three concurrent Ollama servers (~65 GB) were resident; unloading them freed ~68 GB and cleared the critical‑pressure gate.  
- `02-kill-zombie-tsservers.ps1` successfully reclaimed 3 GB of zombie tsserver memory.

**DOMAIN SPECIFICS**  
- Engines: plain `.ts` singletons in `src/engines/*.ts`.  
- Dispatchers: thin Zod‑validated routers (`prism_*`) that call engine methods.  
- Paths: `scripts/call-engine.mjs` imports engines directly; `27-wsl-memory-guard.mjs` reads `/etc/wsl.conf`, queries `vmmemWSL` via PowerShell, and checks Docker/WSL status.  

**TOOLS USED**  
- Node 22 + tsx for TS execution.  
- Bash & PowerShell for system introspection (`wsl --shutdown`, `ollama stop`).  
- Git for commits; `rtk` for status.  
- `node:test` framework for harness tests.  

**OPEN THREADS**  
1. Run `wsl --shutdown` (your terminal) to reclaim ~80 GB and enforce the 16 GB cap.  
2. Install the watchdog: `powershell -NoProfile -ExecutionPolicy Bypass -File H:/prism/.claude/helpers/install-wsl-memory-guard-task.ps1 -RunNow`.  
3. Wire the guard into the fleet‑task‑health list (optional).  
4. Execute `/compact` to finalize the session and load the handoff.
