# frontend-app session ac1150e0 (2026-05-28, 2.5MB, spine 17KB, 1 slice(s), model gpt-oss:20b)

**DECISIONS**  
- Use USB flash drive for bulk extraction of programs, tool data, parameters, and manuals from OSP (easiest, no infrastructure).  
- Wire Ethernet SMB share or Okuma DNC tools for live data; prefer MTConnect adapter for spindle/axis/alarm feeds.  
- Employ THINC‑API to pull machine capability fingerprint into `MachineCapabilityEngine` (`machine_fingerprint` action).  
- Rely on myOkuma portal and distributor service portals for PDF manuals instead of attempting Windows shell access (avoids tamper detection).  
- Skip pre‑NGC manual chase; NGC manuals cover G/M codes identically, UI differences are operator‑known.  

**OPERATOR DIRECTIVES**  
- Pull parameter dump + tool data + program library via USB stick first.  
- Then connect Ethernet and enable MTConnect for live feed (axis limits, accel, lookahead).  
- Use THINC‑API long‑term to build machine fingerprint into PRISM (`MachineCapabilityEngine`).  
- Download all PDFs from myOkuma portal; if unavailable, contact Okuma distributor or tech support.  
- USB‑extract manuals from OSP using HELP key → `C:\OSP\HELP\` (no password).  

**FINDINGS/BUGS**  
- Haas Resource Center blocks raw WebFetch with 403; requires Playwright browser automation.  
- Pre‑NGC manual URLs not discoverable; only NGC PDFs available, but sufficient for G/M code coverage.  
- myOkuma app store now hosts THINC apps only; manual library moved to separate service portal or distributor.  
- MC Machinery URLs stale (404); PDF access gated behind customer portal login.  

**DOMAIN SPECIFICS**  
- `MachineCapabilityEngine` / `machine_fingerprint` action consumes THINC‑API data.  
- `prism_machine_live` subscribes to `mtconnect_*` actions for spindle, axis, alarm streams.  
- Manual ingestion via `/pdf-learn` skill; HTML help requires custom `HurcoWinMaxHtmlScraperEngine`.  
- Paths: OSP USB extraction → `C:\OSP\HELP\`, Ethernet SMB share → `\\<osp-ip>\NCDATA`, MTConnect adapter feed → `mtconnect_*`.  

**TOOLS USED**  
- PRISM skills: `/pdf-learn`, `MachineCapabilityEngine`.  
- Dispatchers/actions: `mtconnect_*` (spindle, axis, alarm), `prism_machine_live`.  
- External tools: Playwright for web scraping, PowerShell for file downloads.  
- Data storage: `mcp-server/data/machines/`, `ingestion_cache/jm-die-fleet-manuals-manifest.json`.  

**OPEN THREADS**  
- Build and run `HurcoWinMaxHtmlScraperEngine` to mirror WinMax HTML help into PRISM ingestion pipeline.  
- Obtain Okuma manuals via distributor portal or USB extraction; feed into `/pdf-learn`.  
- Acquire Mitsubishi EDM PDFs from MC Machinery customer portal; integrate into manual tree.  
- Retrieve Roku‑Roku HC 658‑II spec and Fanuc 31i‑B5 operator manual (archive.org).  
- Finalize THINC‑API integration for live capability fingerprinting in `MachineCapabilityEngine`.
