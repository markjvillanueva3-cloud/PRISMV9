# frontend-app session 2ae65067 (2026-05-27, 31.1MB, spine 88KB, 1 slice(s), model gpt-oss:20b)

**SHIPPED**  
- U‑OSC9‑08 – `SpeedFeedShopLibraryBridgeEngine` (MRR ranking, 35/35 tests) – commit `7c9643f7f0`.  
- U‑OSC9‑09 – `HSMAdvisorAdapterEngine` (UTF‑8/16 fix, 31/31 tests) – commit `32a707ec22`.  
- U‑OSC9‑10 – `SpeedFeedPDFCorpusBridgeEngine` (kilo/fleet PDF bridge, 33/33 tests) – commit `aefecf1676`.  
- U‑OSC9‑11 – `TriVendorComparatorEngine` (HSMAdvisor baseline + G‑Wizard diff, 28/28 tests) – commit `e6c390fc2b`.  
- U‑OSC9‑12 – `GWizardLibraryExporterEngine` & `HSMAdvisorLibraryExporterEngine` (parity CSV/XML, 26/26 each test) – commit `9c99921260`.  
- U‑OSC9‑13 – `WedmTrainingPairBridgeEngine` (PDF extraction bridge, 26/26 tests) – commit `88ff0daef9`.  
- Wire‑fix (main‑tree dispatcher actions for slot‑only engines) – commit `be173cf2b5`.  
- Envelope (`OSCAR‑SFC‑9AXIS‑MS0.json`) – commit `f32c99216a`.  
- Machine‑torque‑curve scaling (1066 machines, 9 255 points) – commit `4a3e31195f`.  
- Catalog joiner for SFM/IPT enrichment – commit `7706c534a3`.  
- Tool‑wear taxonomy & diagnostic engine – commit `8119a98333`.  
- Memory index of all SFC domain nodes – file `reference_oscar_sfc_domain_map_2026_05_27.md`.

**DECISIONS**  
- Slot binding wrapper (`/checkin-oscar`) enforces deterministic slot claim via harness ID; avoids stale bindings.  
- All vendor adapters (HSMAdvisor, G‑Wizard) export tools in PRISM’s native format to enable parity.  
- Machine axis added to HSMAdvisor UI but not yet used by TriVendor comparator; plan to expose it later.  
- Adopted a “parity bridge” pattern: engines for each vendor share the same input schema and dispatcher actions (`prism_calc:*`).  
- Chose to enrich G‑Wizard data with SFM/IPT from manufacturer catalogs before running tri‑vendor smoke.  

**OPERATOR DIRECTIVES**  
- `/loop [5m] /goal` – schedule recurring goal checks every 5 min, auto‑expire after 7 days.  
- “Add power curves for ALL machines in the database” – implemented via machine‑torque‑curve catalog (1058 entries).  
- “Compare features/accuracy of SFC vs competitors” – performed post‑parity smoke; identified divergence on hard groups and structural weak‑disagreement.  
- “Insert tool wear diagnostics” – shipped taxonomy & engine.  

**FINDINGS / BUGS**  
- UTF‑16 declared but UTF‑8 stored in HSMAdvisor `settings_v2.xml` (fixed by BOM sniff).  
- Zod enum drift: `domain:"cad"` missing from allowed list; added to catalog joiner.  
- Passthrough‑clobber on `part_volume_cm3` in ShopLib bridge – fixed inline.  
- Silent wire‑break: dispatcher actions referenced slot‑only engines; resolved by moving them to main tree.  
- Weak‑disagreement (84 % of cells) due to missing SFM/IPT in aggregated tool catalogs; solved with catalog joiner.  
- Divergent cells on hard groups (M/H/K) traced to `UltimateSpeedFeedEngine.alternatives.balanced.vc` being ~70 % above baseline DB median; root‑cause audit pending.  

**DOMAIN SPECIFICS**  
- Engines: `SpeedFeedShopLibraryBridgeEngine`, `HSMAdvisorAdapterEngine`, `GWizardLibraryExporterEngine`, `TriVendorComparatorEngine`, `WedmTrainingPairBridgeEngine`, `MachineTorqueCurveExporterEngine`, `SpeedFeedCatalogJoinerEngine`, `ToolWearDiagnosticEngine`.  
- Data paths: `state/shared/cad-cam-pdf-tribal-seeds.json`, `wedm-training-corpus/`, `machine-torque-curves.ts`, `tool-wear-taxonomy.ts`.  
- Actions: `prism_calc:sfc_shop_library_rank`, `prism_calc:hsmadvisor_read_current_state`, `prism_calc:gwizard_library_export`, `prism_calc:hsmadvisor_library_export`, `prism_calc:sfc_pdf_corpus_bridge`, `prism_calc:hsmadvisor_compare`, `prism_calc:gwizard_read_toolcrib`, `prism_calc:wedm_training_pair_lookup`, `tool_wear_diagnose`.  
- Metrics: Vc, MRR, ROI popup, chatter‑stability, weak‑disagreement, divergent.  

**TOOLS USED**  
- PRISM helpers: `chat-slots.mjs`, `CronCreate`, dispatcher (`calcDispatcher.ts`), vitest test harness, Zod validation, `AtomicValue` pattern, `feedback_engine_tests_in_tests_dir`.  
- External: XML parsing via hand‑rolled UTF‑16 detector, CSV/SQLite exporters for G‑Wizard, JSONL for PDF corpus.  

**OPEN THREADS**  
1. **Machine‑axis sweep in TriVendor comparator** – expose machine ID per cell to compare spindle envelope effects.  
2. **Holder‑context cross‑product** – inject real PRISM holder data into vendor tool exports and add `tool_holder` axis to smoke.  
3. **Root‑cause audit of `UltimateSpeedFeedEngine.alternatives.balanced.vc`** – determine if PRISM’s Pareto‑knee Vc is too high relative to baseline DB; decide on re‑framing success criterion or adjusting balanced‑vc selection.  
4. **U‑OSC9‑WEAR‑VISION image classifier** – build photo dataset, train CLIP‑based k‑NN for wear mode detection.  
5. **Feature/accuracy comparison with updated machine and tool parity** – rerun tri‑vendor smoke after machine axis integration to quantify remaining divergence.  

---
