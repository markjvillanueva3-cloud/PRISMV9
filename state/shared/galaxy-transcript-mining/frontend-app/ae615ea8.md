# frontend-app session ae615ea8 (2026-06-10, 26.7MB, spine 171KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- SIM‑4 (machine‑bind gate) – 54 tests, 3/3 scrutiny passed.  
- SIM‑5 (run‑complete gate) – 63 tests, 3/3 scrutiny passed.  
- SIM‑6 (assessLiveRunClearance keystone) – 53 vitest, 3/3 scrutiny passed; added bind & run‑completeness gates.  
- SIM‑7 (fleet rollup) – 10 tests, 3/3 scrutiny passed; reports all 12 mill/lathe machines drive‑ready and 3 EDM routes.  
- U‑CIMCO‑SIM‑1A part 2 (`--op read-report`) – 67/67 vitest green, commit `dba50eb3da`.  
- U‑CIMCO‑FSM‑LIVE‑DRIVE (`driveLiveFsm`) – 69/69 tests, commit `dba50eb3da`.  
- CLEARANCE‑COMPOSE (`composeClearanceInput`) – 69/69 tests, commit `dba50eb3da`.  
- CIMCO‑CLOSED‑LOOP‑STATUS (HTML twin) – status snapshot, commit `e482189325`.  
- Single‑process C# op `invoke-read --name "Machine Simulation" --then "Simulate"` → PrismCimcoUI.exe, commit `40cf2e0d3b`.  
- Reaper‑safe PowerShell fleet sweep (`cimco-fleet-sweep.ps1`) + `--from-envelopes` – drives all 15 JM machines end‑to‑end, JSONL results, commit `a28927fc0b`.  
- Normalizer header‑only fix (`report-header-only`) and tests, commit `59fa8456f5`.  
- Echo Forge Roadmap v2 – `state/shared/specs/ECHO-FORGE-ROADMAP-2026-06-09.md`.

**DECISIONS**  
- Adopt Ollama pre‑flight + 3/3 Claude for all code reviews.  
- Use ultracode workflow with local `qwen2.5-coder:32b` to generate Echo roadmap (no external API).  
- Replace UIA with MSAA driver (`PrismCimcoUI.exe`) for reliable ribbon in cold launch.  
- Build single‑process `invoke-read` op to avoid two‑process attach and ensure collision run trigger.  
- Employ reaper‑safe PowerShell sweep instead of long Node fleet driver; per‑machine incremental flush & resume cursor for resilience.  
- Treat header‑only report as non‑clearing (`report-header-only`) rather than opaque.

**OPERATOR DIRECTIVES**  
- Open CIMCO Edit on VMC‑01 (or any JM machine).  
- Trigger live‑simulation flow: load NC → Machine Simulation invoke → Simulate run → read report → assessLiveRunClearance.  
- Repeat for all 15 machines (12 mill/lathe + 3 EDM).  
- Complete all CIMCO tasks using Ollama for searches, coding, and closed‑loop testing of post processors.

**FINDINGS / BUGS**  
- Torn‑commit on shared tree caused loss of `Program.cs`; fixed with `git commit --only`.  
- Read‑report normalizer failures due to grid‑not‑found handling & array type check; corrected.  
- SIM‑1 report‑grid MSAA readability still unverified – operator action required.  
- Orphan `PrismCimcoUI.exe` left resident, causing SIGKILL on repeated launches; fixed by `killCimco()`.  
- Two‑process invoke→read attach unreliable; solved with single‑process op.  
- Header‑only report mislabelled as opaque; corrected to distinct non‑clearing source.  
- Reaper killed long Node fleet driver after second launch; resolved by per‑machine PowerShell sweep.

**DOMAIN SPECIFICS**  
- **CIMCO Integration**: `PrismCimcoUI.exe` MSAA driver, `AccessibleObjectFromWindow`, simulation report parsing (`parseSimulationReport`).  
- **SPINE‑2 Units**: SIM‑4 bind gate, SIM‑5 run‑complete, SIM‑6 clearance keystone, SIM‑7 fleet rollup.  
- **Clearance Flow**: `assessLiveRunClearance` includes machine‑bind, units‑known, kinematics‑verified, sim‑cleared, run‑completeness.  
- **FSM‑live‑drive** (`driveLiveFsm`) chains navigation, simulation, poll‑to‑quiescence, clearance assessment; dispatcher gate `cimco_live_run_clearance` receives composed input from `composeClearanceInput`.  
- **Fleet Mapping**: `planFleet()` provides 15‑machine plan; all 12 mill/lathe machines bind cleanly, 3 EDM routed.  
- **Simulation Flow**: `navigateLive` → Machine Simulation invoke → Simulate run → report grid read (`Report` table).  
- **Fleet Sweep Script** (`cimco-fleet-sweep.ps1`) iterates over 15 JM machines, invoking C# op per machine, writing JSONL results.

**TOOLS USED**  
- PRISM CLI: `/checkin-echo`, `/startup-echo`, `/loop`, `CronCreate/CronDelete`.  
- PRISM helpers: `chat-slots.mjs`, `slot-bind-enforce.mjs`.  
- Ultracode workflow engine (parallel agents).  
- Local Ollama (`qwen2.5-coder:32b`) via `scripts/ask-ollama.mjs`.  
- Git for commit/treestate management, `git status` checks.  
- Node test harness (`node:test`).  
- Node scripts: `cimco-sim-driver.mjs`, `cimco-fleet-drive.mjs`.  
- PowerShell sweep script (`cimco-fleet-sweep.ps1`).  
- C# driver `PrismCimcoUI.exe` (invoke‑read op).

**OPEN THREADS**  
- Operator‑gated SIM‑1: validate report grid MSAA readability and run live simulation on VMC‑01, then all 15 machines.  
- U‑CIMCO‑FSM‑LIVE‑DRIVE: next step after SIM‑1; orchestrate full live‑drive flow once read‑report verified.  
- Post‑processor roadmap completion (e.g., `prism_pp` dispatcher, AGI‑tier engines).  
- Load machine envelopes via `.mcfg` per JM machine (currently using CIMCO default).  
- Create known‑bad NC to exercise collision‑row extraction after envelope load.  
- Validate inch vs mm production NCs for each machine post‑envelope load.
