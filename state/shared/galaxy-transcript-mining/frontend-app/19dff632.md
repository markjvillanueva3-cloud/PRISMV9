# frontend-app session 19dff632 (2026-06-10, 20.6MB, spine 150KB, 2 slice(s), model gpt-oss:20b)

**SHIPPED**  
- `e44a3a1592`: wired 4 orphan PayrollLiabilityFilingEngine methods (`compute940`, `generateW2`, `reconcileW2sTo941`, `contractor1099Totals`) into prism_business.  
- `e649790e76`: added `remitLiability` method + round‑trip test.  
- `f069052772`: committed Phase‑0 verify unit (`U-HOTEL-P0-VERIFY-SHIPPED`).  
- `9dfd621910`: added reusable Ollama transcript miner script `scripts/mine-hotel-transcripts.mjs` and initial digests.  
- `61518eb988`: fixed miner bugs (empty‑response, noise filtering, stale files).  
- `099e6b92bd`: committed finalized hotel forge roadmap spec `state/shared/specs/HOTEL-FORGE-ROADMAP-2026-06-09.md`.  
- `U-HOTEL-PORTAL-PERSISTENCE`: folded 4 in‑memory maps into SQLite WAL store inside `CustomerPortalEngine`; all tests green, scrutiny passed.  
- `U-HOTEL-ALLOWLIST-WRITE-ENABLE`: per‑action manager‑role gate added for handoff workflow writes; financial/PII writes remain 403; route tests passed.  
- iOS‑redesign doctrine `state/shared/specs/FLEET-IOS-REDESIGN-DOCTRINE-2026-06-09.md` committed.

**DECISIONS**  
- Use Ollama `gpt‑oss:20b` for bulk transcript summarization; use `qwen2.5‑coder:32b` as default grunt coder (reserve `gpt‑oss:120b` only for heavy multi‑file synthesis).  
- Adopt ultracode workflow (8 agents) for high‑level reasoning, roadmap construction, adversarial review.  
- Commit miner script and digests; avoid costly Claude calls.  
- Proceed with next build unit after all verify units complete.  
- Fold SQLite WAL into `CustomerPortalEngine` instead of separate store; keep `rateBuckets` in memory.  
- Reuse Quebec’s `WorkspacePrimitives`; extend for iOS‑style buttons, focus rings, haptics hook.  
- Keep all financial/PII write actions 403; expose workflow‑state writes behind manager‑role gate.  
- Require `/compact` before starting multi‑file UI foundation build (U1–U3) to avoid drift.

**OPERATOR DIRECTIVES**  
- Run `/compact` before `U-HOTEL-PORTAL-PERSISTENCE` and UI foundation builds.  
- Execute false‑wire regression guard after allowlist test harness is in place.  
- File remaining bugs (e.g., `marketplace_lead_get` null handling) to appropriate domain chat.  
- Approve role model → granted; enabled manager‑role gate for handoff writes.  
- `/startup-hotel` invoked to claim hotel slot and run standard startup pipeline.

**FINDINGS/BUGS**  
- Payroll wiring: 5 orphan methods wired; `remitLiability` required positional‑arg normalization.  
- Mobile engines: all but `MobileVoice` wired; `MobileVoice` has WIRE‑EXEMPT tag – no new tags needed.  
- DFM contract bug line 1471 caused non‑iterable `ctx.features`; filed to charlie.  
- Guard for allowlist regression caught ambiguous `marketplace_lead_get` null response and regex bug in placeholder detector; both fixed.  
- Miner script bugs (empty Ollama responses, aggressive noise filtering, stale combined digest) resolved.  
- False‑wire guard regex missed real “Unknown business action: X” string → fixed.  
- Missing `.db`, `-wal`, `-shm` gitignore entries for `customer-portal.db` added.  
- Haptics unsupported on iOS Safari; Capacitor shell required for real device haptics.  
- Design doctrine conflict: Quebec’s “Calculator Studio” HUD forbids iOS‑style springs/uppercase; new doctrine supersedes it.  
- PTO double‑apply bug and empty `approved_by` in payroll writes flagged (not fixed here).

**DOMAIN SPECIFICS**  
- Slot: hotel (ERP/HR/business).  
- Key engines/actions: `PayrollLiabilityFilingEngine`, `businessDispatcher.ts`, `MarketplaceSeedingEngine`, `CustomerPortalEngine`.  
- Routes: `/api/v1/business` (dispatch), `/realtime`, `/hotel‑portal`, `/customer-portal.db`.  
- Metrics: IRS payroll values, W‑2 box calculations, FUTA/FICA rates.  
- Unique paths: `scripts/mine-hotel-transcripts.mjs`, `state/shared/specs/HOTEL-FORGE-ROADMAP-2026-06-09.md`, `state/shared/specs/FLEET-IOS-REDESIGN-DOCTRINE-2026-06-09.md`.  
- Business dispatcher: 879 actions, read‑only allowlist of 17 actions.  
- MarketplaceSeedingEngine: leads keyed by `supplierId`.  
- CustomerPortalEngine uses CoordinationStoreEngine pattern (better-sqlite3 WAL).  
- AuditLog middleware globally mounted; role DB (`ROLE_DB`) and `requireRole/requirePermission` helpers exist.

**TOOLS USED**  
- Ollama (`gpt‑oss:20b`, `qwen2.5‑coder:32b`), `ask-ollama.mjs`.  
- Ultracode workflow (8 agents).  
- Scripts: `scripts/mine-hotel-transcripts.mjs`.  
- Business dispatcher test harness `businessDispatcher.payroll-filing-wire.test.ts`.  
- PRISM core components: `businessDispatcher`, `auditLog`, `requireRole`, role DB, false‑wire guard test suite.  
- Design primitives: `WorkspacePrimitives`, framer‑motion, react-grid‑layout, @dnd-kit.  
- SQLite WAL via better-sqlite3 in CoordinationStoreEngine.

**OPEN THREADS**  
- Implement false‑wire regression guard for 17 allowlisted actions.  
- Finalize and run remaining roadmap units after persistence build.  
- Build UI foundation U1–U3 on fresh budget after `/compact`.  
- Capacitor shell for real device haptics (phone app only).  
- Finalize role‑gate mapping for all manager roles; ensure audit trail records true `req.userId`.  
- Address flagged PTO double‑apply and empty `approved_by` bugs in future slots.
