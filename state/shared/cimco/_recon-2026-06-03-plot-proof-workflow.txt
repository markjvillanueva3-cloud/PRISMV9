{
  "summary": "Probe CIMCO blind-nav plotting gaps + sim architecture + post-proof harness design for JM fleet 100%-working-posts goal",
  "agentCount": 5,
  "logs": [],
  "result": {
    "findings": [
      "I now have complete, decisive evidence. The dash-flag cluster reveals the full picture: the `--savebundle` flag sits directly alongside `/ms` (Machine Simulation), and the exported plugin functions `IsSimulationRunning`, `GetSimulationType`, `CloseSimulation`, `ShowStats`, `GetNumberOfRunningSimulations`, `PlotFile` confirm the simulation is a live, GUI-window-hosted process (`PlotWindow`, `MachineSimulateWindow`). I have enough to give a fully-grounded verdict. No further scans needed.\n\n---\n\n# RECON FINDINGS — CIMCO headless/CLI simulation verdict\n\n## (a) VERDICT: A real CLI exists for LAUNCHING simulation, but a true HEADLESS sim-VERDICT-to-file mode DOES NOT EXIST. Confidence: HIGH.\n\nCIMCO Edit has a genuine, undocumented-in-help command-line interface (recovered verbatim from the binary's usage banner). It can **launch** Machine Simulation / Solid Simulation on an NC file from the command line, and can **save a presentation/simulation bundle to disk**. But there is **no flag that runs the sim non-interactively and emits a collision/over-travel/gouge verdict file**, and the collision report is bound to the GUI (`UI::CollisionReport`). The simulation always opens a live GUI window (`PlotWindow` / `MachineSimulateWindow`) and requires a live license.\n\n### Evidence FOR a CLI (verified — strings extracted from the binary itself):\n- **CIMCOEdit.exe** contains a complete `printf`-style usage banner at byte offset ~12892336–12893376. Verbatim entries:\n  - `Open: %s file1`\n  - `Compare: %s /c file1 file2`\n  - `Backplot: %s /plot file1`\n  - `Backplot only: %s /plotonly file1`\n  - `Minimal backplot: %s /minimalplot file1`\n  - **`Machine Simulation: %s /ms file1`**  ← the sim-launch flag\n  - `Solid simulation: %s /solid file1`\n  - `Solid only: %s /solidonly file1`\n  - `Load Setup File in read-only mode: %s /sf file1`\n  - `Read-only: %s /readonly file1`\n  - `Set language: %s /lang=XX`\n  - `Example: %s /t \"ISO Mill\" /c \"FILE 1.NC\" \"FILE 1.NC\"`  ← shows `/t` template + `/c` compare composing\n  - Header literal: `Command Line Info`\n- A parallel **dash-form** flag table also exists (offset ~12948148): `/plot`↔`--plot`, `/ms`, `/sf`↔`/setupfile`, `/plotonly`, `/solid`↔`--solid`, `/solidonly`, `--line`, `--no-bring-to-front`, `--title`, `--filter`, `/multichannel`, **`--savebundle`**, `/readonly`.\n- The help page `pending-updates.htm` line 74 independently confirms one of these: *\"New: Added `--no-bring-to-front` flag to prevent the editor from being brought to the foreground when called via command line. (S25874)\"* — proves CLI invocation is a real, supported, current feature.\n\n### Evidence AGAINST a headless verdict emitter (the keystone gap stays):\n- **The sim is a GUI window, not a headless engine.** CIMCOEdit.exe exports (offset ~12948376+): `PlotWindow`, `MachineSimulateWindow`, `IsSimulationRunning`, `GetSimulationType`, `CloseSimulation`, `ShowStats`, `GetNumberOfRunningSimulations`, `PlotFile`. These are interactive-window lifecycle calls — `/ms file1` opens a simulation **window**; it does not run-to-completion-and-exit-with-a-verdict.\n- **The collision report is UI-bound.** CIMCOSimulation.exe contains the C++ RTTI symbol `.?AVCollisionReport@UI@@` (offset 5074472) = class `UI::CollisionReport`. The verdict object lives in the **UI** namespace — there is no headless/service collision-report emitter. This corroborates `backplotsimulationreport.htm`: the Simulation Report (collisions/limits) renders in the docking-pane \"Simulation tab\" and is populated by the *\"Check collision and limit errors\"* button — an interactive action.\n- **The only file the sim writes is a presentation bundle, not a verdict.** CIMCOSimulation.exe strings (offset ~4290416): `Presentation bundle successfully created.` / `Failed to create presentation bundle.` / `Failed to save simulation file.` — confirms `--savebundle` saves the *simulation replay* (the `.MacBundle`/presentation per `presentation-viewer.htm`), which is a 3D playback artifact for human viewing, **not** a parseable collision/over-travel/gouge verdict. `presentation-viewer.htm` confirms the Presentation Viewer is a *viewer* (playback/timeline GUI), with no documented verdict export.\n- **No usage banner / no batch/headless tokens in CIMCOSimulation.exe.** It has `Failed to parse command line` + `CommandLineToArgvW`/`GetCommandLineW` (it accepts args, e.g. a file to replay) but NO usage banner and NO `headless`/`batch`/`nogui`/`/report`/`report.xml` strings. R12: I searched explicitly for these and they are **NOT FOUND**.\n- **License gate confirmed** (`launch-surface.json` + `backplot-fast.htm`): Machine Simulation features \"become available once a valid license key has been installed\" (`Sys/KeyManager.exe`). Any sim run needs the live licensed app.\n\n## (b) CLI flags found, VERBATIM\n```\n%s file1                                  (open)\n%s /c file1 file2                         (file compare — two files)\n%s /t \"template name\" file1               (apply template)\n%s /lang=XX                               (language; XX=us,de,fr,...)\n%s /plot file1        | --plot            (backplot)\n%s /plotonly file1                        (backplot only)\n%s /minimalplot file1                     (minimal backplot)\n%s /ms file1                              (MACHINE SIMULATION — opens sim window)\n%s /sf file1          | /setupfile        (load setup file, read-only)\n%s /solid file1       | --solid           (solid simulation)\n%s /solidonly file1                       (solid only)\n%s /multichannel file1 file2 ...          (multi-channel view)\n%s /readonly file1                        (read-only)\n%s /newwin                                (open new editor instance)\n--no-bring-to-front                       (suppress foreground raise — KEY for unattended runs)\n--line   --title   --filter   --savebundle   (save sim/presentation bundle)\n/nocalc /nobase /nodncmax /noplot /nonc /nomazakview /minimalsetup  (feature-disable switches)\n%s /ncbaseuser=<username> [/ncbasepass=<password>]\n%s /lockmachinegroup=\"<machinegroupname>\"\nExample: %s /t \"ISO Mill\" /c \"FILE 1.NC\" \"FILE 1.NC\"\n```\nNote: `%s` = the CIMCOEdit.exe path. CIMCOSimulation.exe accepts a file arg (replay) but exposes **NO documented flag banner** — UNVERIFIED beyond \"takes a command line.\"\n\n## (c) Most-automatable path to a sim verdict, ranked (least live-GUI first)\n\n1. **NC-NORMALIZE BYTE-EQUIV + DIALECT-MASK OFFLINE PROOF (already built, ZERO GUI, ZERO license).** For the dialect/syntax-correctness half of \"100%-working post,\" the existing offline arms (nc-normalize byte-equiv, dialect masks, dialect allowlists in `CimcoVerificationBridgeEngine`) need NO CIMCO at all. This is the only fully-blind, fully-automatable arm and should carry the maximum proof weight. It does NOT prove collision/over-travel/gouge.\n\n2. **`CIMCOEdit.exe /ms \"<nc>\" --no-bring-to-front` + EXTERNAL-COMMAND file-channel callback (semi-automatable; needs live license + 1 UIA/operator trigger).** Launch the sim window in the background (`--no-bring-to-front` keeps it from stealing focus). The verdict still requires the in-window \"Check collision and limit errors\" action and a UIA read of the Simulation tab (the SPINE-2 reader) — but `--no-bring-to-front` makes a batch-loop tolerable. NEEDS-LIVE-VERIFY: whether `/ms` alone auto-runs collision detection or requires the button click (per `backplotsimulationreport.htm`, collision check is an explicit on-demand button — so likely needs the click → UIA still required).\n\n3. **`--savebundle` presentation export → parse the bundle offline (NEEDS-LIVE-VERIFY, medium-risk).** If `/ms file1 --savebundle <out>` can be made to save a simulation bundle that internally records collision events, PRISM could parse that bundle offline. UNVERIFIED that the bundle contains a machine-readable collision verdict (the strings only prove it saves a *replay*; `UI::CollisionReport` suggests the verdict is NOT serialized into the bundle). Worth one empirical probe on the live app, but do not architect around it.\n\n4. **Full SPINE-2 UIA Simulation-Report reader (operator-driven-live, highest cost).** Unavoidable for the authoritative in-app collision/limit verdict. The keystone blocker stands.\n\n**Bottom line for architecture:** the answer is **operator-driven-live-UIA for the collision/over-travel/gouge verdict** — there is no automatable-offline path to that specific verdict. The CLI buys you blind *launch* + *focus suppression* + *file open/compare/setup-load*, which meaningfully reduces GUI interaction, but the verdict read itself remains UIA. Architect for: offline dialect/byte-equiv proof (full auto) + CLI-launched sim with `--no-bring-to-front` + SPINE-2 UIA verdict read (operator-attended).\n\n## (d) Can the External-Command hook carry the verdict back to PRISM blind? PARTIALLY — it carries PRISM's verdict OUT, not CIMCO's verdict back.\nFrom `setupexternalcommands.htm` (read fully) + `launch-surface.json.integrationHook`: the External Commands surface is a genuine **file-channel, blind-safe** hook. It runs `<program> <params>` with macro substitution (`$FILEPATH`, `$FILE`, `$FILENOEXT`, `$PATH`, `$OUTFILE`) and an optional `$OUTFILE` output path. Critically:\n- **Direction:** It lets CIMCO **call PRISM** on the currently-open NC file (PRISM reads `$FILEPATH`, runs `prism_cimco`, writes to `$OUTFILE`). That is PRISM's *own* verdict flowing through a file channel — fully blind, no UIA. Verified-safe.\n- **What it CANNOT do:** It does NOT expose CIMCO's *internal simulation collision verdict*. External Commands operate on the NC text file, not on the live simulation's collision-report object (`UI::CollisionReport`). There is no macro like `$COLLISIONREPORT` or `$SIMVERDICT`. So the External-Command hook cannot carry the *CIMCO sim verdict* back blind — that still needs the SPINE-2 UIA reader.\n\n**Net:** the External-Command file channel is the right blind transport for PRISM's offline/dialect verdict and for triggering PRISM from inside CIMCO, but it is NOT a back-channel for CIMCO's collision/gouge verdict. It complements, does not replace, SPINE-2.\n\n---\n### Incidental finds worth logging\n- CIMCO's own dev tree leaks in both exes: `C:\\Users\\djcim\\gitrepo\\cimco-simulation\\AlgoLab\\dev\\PythonScripts` and PDB paths `D:\\SIMSHAPEPRJ\\SimShape\\CIMCOSimulation.pdb` — indicates an internal Python scripting/automation surface exists *in CIMCO's dev environment*, but NO shipped Python/script entry point was found in the install (NOT FOUND in shipped binaries as a usable hook). Do not assume a scriptable API ships.\n- `AUTOSAVETIME` exists in CIMCOEdit.exe (editor autosave), unrelated to sim-verdict export.\n\n### Files read fully\n`backplotsimulationreport.htm`, `presentation-viewer.htm`, `advanced-configuration-editor.htm`, `license.htm`, `setupbackplot.htm`, `custom-machines.htm`, `setupexternalcommands.htm`, `backplot-fast.htm`, `pending-updates.htm`, `H:/prism/state/shared/cimco/launch-surface.json`, `H:/prism/state/shared/cimco/nav-map.json` (criticalPathVerdicts + synthesis). Strings scanned: `H:/prism/resources/cimco-2026/CIMCOEdit/CIMCOEdit.exe` (30.8MB) and `H:/prism/resources/cimco-2026/CIMCOEdit/Dll/CIMCOSimulation.exe` (7.0MB).",
      "I have complete, verified data. Final report follows.\n\n---\n\n# RECON FINDINGS — CIMCO help-module coverage & CNC-Calc relevance to post-proving\n\n## (a) EXACT inventory of CIMCO help modules + plotting status\n\n**Directory:** `H:/prism/resources/cimco-2026/CIMCOEdit/Help/` — there are **THREE** English (`_us`) help CHMs, not one:\n\n| CHM (US) | Size | Module | Plotted? | Decompiled? |\n|---|---|---|---|---|\n| `edit_us.chm` | 4,772,248 B | NC Editor + Backplot + Simulation + DNC-transmission + Setup + Machine Models + Tool Manager | **YES** — 511 surfaces, `generatedFrom: \"resources/cimco-2026/_extracted/edit_us (154 decompiled CHM pages, CIMCO Edit 2026.01.10)\"` | YES (154 `.htm` in `H:/prism/resources/cimco-2026/_extracted/edit_us/`) |\n| `cnc-calc_us.chm` | 10,364,664 B | CNC-Calc (CAD drawing + CAM toolpath generation) | **NO** | **NO** — only `_extracted/edit_us/` exists under cimco-2026 (verified: `_extracted/` contains exactly one subdir, `edit_us/`) |\n| `nc-base_us.chm` | 4,690,879 B | NC-Base (standalone DNC/program-datastore app) | **NO** | **NO** |\n\n(Plus non-English locale duplicates: cnc-calc {de,dk,fr,se,sp}, edit {cz,de,fr,it,jp,se,sp}, nc-base {cz,de,fr,sp} — irrelevant; English is canonical.)\n\n**Page counts for the unplotted modules:** I could **NOT** get an exact decompiled page count because neither CHM is extracted (R12: NOT VERIFIED by direct count). Proxies:\n- `cnc-calc_us.chm` is **10.4 MB — 2.2× larger than edit_us** (the biggest help module in the suite). Its image sidecar `Help/CNCCalc/` holds **63 files** (62 `.bmp` topic illustrations + a `Turning/` subfolder, **0 `.htm`**), so expect on the order of **~150–250 topic pages** when decompiled. The bmp basenames confirm content: `ClimbMilling`, `ConventionalMilling`, `HelixAngle/Pitch/Clearance`, `LeadArcRadius/Sweep`, `RampAngle/Length`, `RoughingDeviation`, `StockToLeaveXY/Z`, `FinishAtAllDepths`, pocket ordering — i.e. **CAM toolpath-parameter pages**.\n- `nc-base_us.chm` ≈ 4.7 MB (same class as edit_us → likely ~150 pages of DNC server / program-database admin).\n\n## (b) Critical-path vs orthogonal — with reasoning\n\n**CNC-Calc → ORTHOGONAL to post-proving. Do NOT plot for the proving goal.**\nDirect evidence from `H:/prism/resources/cimco-2026/CIMCOEdit/Posts/Programming CNC-Calc Post Processor.html` (body, tags stripped):\n> *\"Post processors in CNC-Calc are little programs written in JavaScript… The type field can be either `POST_TYPE_MILL` or `POST_TYPE_TURN`… expects to find a set of functions: `onInit()`, `onSpindleRotation()`, `onCompensation()`, `onAxisSubstitutionStart()`…\"*\n\nCNC-Calc is CIMCO's **CAD/CAM authoring module**: it *draws geometry, generates toolpaths, and emits NC through its own JS post processors*. That makes it a **SECOND CAM/post SOURCE that competes with PRISM's posts — not an independent verifier of them.** The operator goal is *verifying PRISM-generated posts*, and a verifier must be source-agnostic (consume an arbitrary `.nc` and report collision/over-travel/gouge). CNC-Calc would instead produce a *different* program from a *different* post engine — useful only as an alternate generator or a cross-check oracle, never as the proof surface itself. Wiring it in risks conflating \"PRISM's post is correct\" with \"CNC-Calc's post agrees,\" which is a weaker and confounded claim.\n\n**NC-Base → ORTHOGONAL (lower priority than even CNC-Calc).** It is the DNC/program-storage server (push programs to machines, version them, manage the shop datastore). Confirmed the editor already exposes the *transmission* slice that proving touches: `edit_us` already plotted a **`dnc-transmission` cluster (34 surfaces)** and 11 `dncsetup*.htm` pages live inside edit_us (`dncsetupserial.htm`, `dncsetupftp.htm`, etc.). The full NC-Base *server admin* surface is shop-floor deployment, **after** correctness is proven — not on the proving path.\n\n**The verifier surface is ALREADY in the plotted module.** I read `H:/prism/resources/cimco-2026/_extracted/edit_us/backplotsimulationreport.htm` in full — the **Simulation Report** (the exact collision/limit/gouge verdict your SPINE-2 UIA reader must scrape) is a tab in **edit_us**, not in a separate module:\n> *\"The Simulation Report lists all program errors detected within the NC program, including the line number, the type of error, a description and the action taken. The Simulation Report also provides information about collisions and limits when the Machine Simulation add-on is being used… Click 'Check collision and limit errors' to rerun collision detection.\"*\n\nThe nav-map's `synthesis.areasCovered` already includes `\"simulation\"` (a 68-surface cluster) and the `criticalPathVerdicts` already plot `open-nc-in-editor` and `load-machine-model-and-stock` as navigable. So the keystone blocker is **not** an unplotted help module — it is exactly what your brief states: the **live licensed app + the unbuilt SPINE-2 UIA reader** of `backplotsimulationreport.htm`'s report table. Plotting more CHMs does **not** unblock it.\n\n## (c) Recommendation — what to plot next, what to DEFER\n\n- **DEFER `cnc-calc_us.chm` (explicit).** Reason: it is a CAM *generator*, not a *verifier*; orthogonal to proving PRISM posts. Revisit only if the goal expands to \"use CIMCO as a second-opinion CAM/post oracle.\" Tag as `[DEFERRED — orthogonal: CAM source not verifier]`.\n- **DEFER `nc-base_us.chm` (explicit).** Reason: DNC server/datastore admin = shop-floor deployment, post-proving. The proving-relevant DNC *transmit* slice is already covered inside edit_us (`dnc-transmission` cluster, 34 surfaces). Tag `[DEFERRED — deployment-phase, transmit-slice already in edit_us]`.\n- **Plot next (highest ROI, inside the already-plotted module):** the **`window-help-misc` cluster, which the nav-map flags as FAILED extraction** — `note: \"window-help-misc cluster failed StructuredOutput — re-extractable.\"` That is a real gap in the *editor* surface you already depend on. Re-extract it before adding any new CHM.\n- **Real critical-path work is NOT a plotting task:** build **SPINE-2** = the UIA reader for the Simulation Report table in `backplotsimulationreport.htm` (line-number / error-type / description / action columns + the \"Check collision and limit errors\" trigger), against the live licensed app with the Machine Simulation add-on enabled. Everything needed to author the *machine model + stock + WCS* feed is already plotted (`load-machine-model-and-stock` verdict, `.setup` scanning sidecar, MachineCfg/.MacBundle, `setup.machine.toolpath-unit` UNITS-FIRST guard).\n\n## (d) Decompile blocker for the deferred CHMs (exact)\n\nNeither `cnc-calc_us.chm` nor `nc-base_us.chm` has an `_extracted` directory anywhere under `H:/prism/resources` (verified: `find … -ipath \"*cnc-calc*\"` / `*cnccalc*` returns only the raw `.chm`, the `Dll/CNCCalc.dll`, the `.bmp` image sidecar, and a Posts PDF/HTML — **no extracted `.htm` pages**). If/when un-deferred, the blocker is identical to the original edit_us pipeline:\n\n```\nhh.exe -decompile \"H:/prism/resources/cimco-2026/_extracted/cnc-calc_us\" \\\n       \"H:/prism/resources/cimco-2026/CIMCOEdit/Help/cnc-calc_us.chm\"\n# (Windows HTML Help) — or 7z x cnc-calc_us.chm -o<dir> as a portable fallback.\n```\nSame `7z x` / `hh.exe -decompile` step for `nc-base_us.chm`. No page count is available until that runs (R12: UNVERIFIED until decompiled).\n\n---\n**Confidence:** HIGH on the module inventory, the \"only edit_us plotted\" status, and the CNC-Calc=CAM-generator-not-verifier judgment (all directly read: dir listing, nav-map `generatedFrom`/`synthesis`/`clusters`/`criticalPathVerdicts`, the CNC-Calc Post Processor HTML, and `backplotsimulationreport.htm`). MEDIUM-only on the *estimated* page counts for the two unplotted CHMs (proxied from file size + bmp count, not a decompiled count).",
      "I have everything needed. Here is the recon report.\n\n---\n\n# SPINE-2 Simulation-Report Schema — CIMCO Edit 2026 Machine Simulation\n\n## (a) Complete enumerated list of CIMCO sim-report finding categories (VERBATIM)\n\nCIMCO's verdict surface is split across **two coupled UI surfaces**, both gated on the Machine Simulation add-on license. The verbatim category names are authoritative.\n\n### Surface 1 — Simulation Report (docking-pane tab) — `backplotsimulationreport.htm`\nThe report is a **table** with these exact columns (verbatim, line 4-5):\n> \"the **line number** the error occurs on, the **type** of error, a **description** of the error and the **action** taken... The *ACTION* column describes what action CIMCO Edit will take until the error is resolved such as ignoring a command or highlighting a collision.\"\n\nMapped columns: **LINE / TYPE / DESCRIPTION / ACTION**.\n\nThe report's two top-level severity filters (verbatim, lines 19-24): **\"Show Errors\"** and **\"Show Warning\"**. With Machine Simulation licensed it additionally \"include[s] information about **collisions and limit errors**\" (line 18, backplot-fast.htm). So the report's severity classes are: **Error · Warning · Collision · Limit error**.\n\nA rerun control exists (verbatim, line 12-13): **\"Check collision and limit errors\"** — \"Click this button to rerun collision detection... it does not run with each modification.\"\n\n### Surface 2 — Stop Conditions (the granular event taxonomy) — `menubackplottoolpath.htm` lines 108-144\nThis is the keystone. CIMCO names **8 distinct detectable stop-event categories**, VERBATIM checkbox labels + their detection description:\n\n| # | VERBATIM label | VERBATIM detection text |\n|---|---|---|\n| 1 | **Tool Change** | \"stop the simulation upon detecting a tool change\" |\n| 2 | **Collision** | \"stop the simulation upon detecting a collision\" |\n| 3 | **Rapid To Stock** | \"stop the simulation upon detecting a rapid movement that collides with the stock\" |\n| 4 | **Stopped Spindle Cuts** | \"detecting the tool cutting while the spindle is stopped\" |\n| 5 | **Holder To Stock** | \"detecting a collision between the tool holder and stock\" |\n| 6 | **Tool And Models** | \"detecting a collision between the tool and any model... such as workpiece, fixture or machine models\" |\n| 7 | **Travel Limits** | \"detecting that an axis has exceeded its travel limit\" |\n| 8 | **Optional** (program stops) | \"detecting a stop command in the NC program\" → enumerated **M00 Program Stop / M01 Optional Stop / M02 End of Program / M30 Program End and Reset** |\n\nCollision granularity (from `backplotsimulationreport.htm` Collision Settings, lines 32-69): collisions are detected **per model-pair** — e.g. \"tool/workpiece or C-axis/Z-axis\" — with modes **Find One / Find All**, and Tool/Workpiece collision sub-modes **ON / Rapid Moves / Cutting Moves / OFF**.\n\nLive panel signals (`fast-version-controls.htm`):\n- **Axis Control Panel** (lines 113-117): \"Any axis that runs over its travel limit is highlighted in **red**\" — the live travel-limit signal.\n- **Stop Conditions Panel** (lines 147-153): appears with \"information about the cause of the stop\" — example given verbatim: \"the simulation has stopped because the **Y-axis has traveled beyond its limit**.\"\n- **Stopped Spindle** behavior (`backplotsimulationreport.htm` lines 90-97): Cut / No cut / **Mark collision**.\n\n### R12 — what CIMCO does NOT name (UNVERIFIED / NOT FOUND as report categories)\n- **\"Gouge\"** — **NOT FOUND** anywhere in `edit_us`. CIMCO has no verdict called \"gouge.\" The equivalent is **Stock Compare** (`menubackplotsolid.htm` lines 60-67): a color-coded geometric over/under-machining comparison (Red = over-machined/too much removed = the gouge analog; Blue = under-machined; Green = in tolerance), and the Cross-Section \"Workpiece\" compare (Red = too much removed, Yellow = too little). This is **visual/color-coded, NOT a discrete report finding row** — there is no documented per-line \"gouge\" verdict.\n- **\"Near-miss\"** — **NOT FOUND**. CIMCO has no near-miss concept; closest is **Cutter Allowance** (penetration tolerance before a collision is flagged) and **Compare Range** tolerance banding. No proximity/clearance verdict exists.\n- **Cycle time** — is NOT in the Simulation Report. It is surfaced separately (see section d).\n\n## (b) Proposed SPINE-2 report schema (fields + types + pass/fail rule)\n\nThe schema must model the **report row contract** plus the **8 stop-event categories** as a richer `category` enum than PRISM's current 4. Proposed:\n\n```ts\ntype CimcoReportSeverity = \"error\" | \"warning\";            // Show Errors / Show Warning filters\ntype CimcoFindingCategory =                                 // the 8 Stop-Condition events + base\n  | \"collision\"            // generic Collision (model-pair)\n  | \"collision_tool_workpiece\"  // Tool And Models (tool↔workpiece/fixture/machine)\n  | \"collision_holder_stock\"    // Holder To Stock\n  | \"rapid_to_stock\"            // Rapid To Stock (rapid-through-stock)\n  | \"stopped_spindle_cut\"       // Stopped Spindle Cuts\n  | \"travel_limit\"              // Travel Limits (axis exceeded travel limit)\n  | \"program_stop\"              // Optional: M00/M01/M02/M30\n  | \"tool_change\"               // Tool Change (informational stop)\n  | \"syntax_error\"              // base NC interpreter error (non-MachineSim)\n  | \"warning\";\n\ninterface CimcoSimReportRow {\n  line: number | null;          // LINE column (null if N-prefixed or absent)\n  type: string;                 // TYPE column (verbatim CIMCO label)\n  description: string;          // DESCRIPTION column\n  action: string;               // ACTION column (\"Ignored\" / \"Highlighted collision\" / ...)\n  severity: CimcoReportSeverity;\n  category: CimcoFindingCategory;\n  axis?: \"X\"|\"Y\"|\"Z\"|\"A\"|\"B\"|\"C\"|null;  // for travel_limit (parse \"Y-axis ... beyond its limit\")\n  modelPair?: [string, string] | null;  // for collision (e.g. [\"C-axis\",\"Z-axis\"])\n}\n\ninterface CimcoSimReportVerdict {\n  pass: boolean;\n  counts: Record<CimcoFindingCategory | \"warning\", number>;\n  firstOffendingLine: number | null;\n  rows: CimcoSimReportRow[];\n  cycleTimeSeconds?: number | null;   // from Toolpath Statistics / Tool Status (NOT the report)\n  controllerVerified: false;          // sim-clean ≠ controller-verified\n  collisionCheckRan: boolean;         // did \"Check collision and limit errors\" actually run?\n  source: \"uia\" | \"fixture\";\n  summary: string;\n}\n```\n\n**Pass/fail rule (safety-critical, fail-loud):**\n- **FAIL** if any: `collision*`, `rapid_to_stock`, `stopped_spindle_cut`, `travel_limit`, `collision_holder_stock`, `syntax_error`, OR any row with empty/unclassifiable `type` (fail-safe to error).\n- **WARNING (non-failing, advisory):** `warning`, `tool_change`, `program_stop` (M00/M01/M02/M30 are intentional NC stops, not defects).\n- **HARD GATE — `collisionCheckRan` MUST be true.** Because \"Check collision and limit errors... does not run with each modification\" (line 13), a report can be **stale/empty because the check never ran**. A SPINE-2 reader that returns `pass=true` on an empty report **without confirming the collision pass executed is a fail-OPEN safety hole.** SPINE-2 must verify the rerun button was clicked and detection completed before honoring a clean verdict.\n\n## (c) Gaps: PRISM's existing parser vs the real report\n\n`scripts/cimco-control-map.mjs#parseSimulationReport` and the TS port `CimcoVerificationBridgeEngine.ts#evaluateSimulationReport` are byte-faithful parity ports of each other. Both model only **4 categories**: `error / warning / collision / limit`. Gaps vs the documented contract:\n\n1. **Collapses 6 distinct collision/over-travel events into 2 buckets.** The classifier (`.mjs` lines 241-248 / `.ts` lines 590-597) keys only on substrings `\"collision\"`, `\"limit\"/\"over-travel\"/\"overtravel\"/\"travel\"`, `\"warn\"`. It will NOT distinguish **Rapid To Stock**, **Holder To Stock**, **Stopped Spindle Cuts**, or **Tool And Models** — yet these are the exact crash modes a live-test cares about. Specifically: **\"Stopped Spindle Cuts\" and \"Rapid To Stock\" descriptions may NOT contain the word \"collision\"** → they would currently misclassify to `error` (still fails, so fail-safe holds) but lose the actionable category. **R12 risk:** a \"Rapid To Stock\" row whose TYPE is e.g. \"Rapid move into stock\" classifies as generic `error`, masking the specific rapid-through-stock crash class the operator must see.\n2. **No `tool_change` / `program_stop` handling.** A Tool Change or M00/M01/M30 stop row (if it ever surfaces as a report row) would classify to `error` and **falsely FAIL** a good program. Currently latent (these are stop-events, not report rows) but the schema should explicitly route them to non-failing.\n3. **No `collisionCheckRan` gate.** Both parsers treat `input == null` as \"clean run (sim ran, found nothing)\" (`.mjs` line 261-262 / `.ts` line 613-614). **This is the fail-open hole** — an empty report is indistinguishable from \"collision detection never executed.\" For a safety-critical live test this MUST be hardened.\n4. **No axis / model-pair extraction.** The real report identifies WHICH axis over-traveled (\"Y-axis... beyond its limit\") and WHICH model-pair collided (\"C-axis/Z-axis\"). Current parser discards this into a flat `description` string. Operators need the axis named.\n5. **No cycle-time field.** Correct per the report contract (cycle time is not in the report), but SPINE-2 should carry it from the separate surface (section d) for completeness.\n6. **Column-order heuristic is fragile.** The string-row splitter (`.mjs` lines 266-277) assumes `[LINE, TYPE, DESCRIPTION, ACTION]` from pipe/tab/multi-space splitting. UIA element-tree extraction yields **per-cell text already keyed by column**, so the SPINE-2 reader should consume structured cells (LINE/TYPE/DESCRIPTION/ACTION) directly, not re-split flattened strings — the heuristic risks mis-columning a DESCRIPTION that contains a multi-space gap.\n\n**What the parser gets RIGHT:** the `||`-not-`??` fall-through (`.ts` line 639) correctly avoids the falsy-singular-key fail-open; warnings-don't-fail is correct; typeless-row→error fail-safe is correct; `controllerVerified:false` honesty is correct.\n\n## (d) Surfacing channel — UIA-only dialog vs exportable file\n\n**The Simulation Report itself is UIA-only — there is NO documented export/save/clipboard for it. CONFIRMED.**\n\nEvidence:\n- `backplotsimulationreport.htm` describes the report as **\"located in the docking pane\"** / **\"upper section of Simulation tab\"** — an on-screen interactive list. The ONLY documented interaction is **\"Click on any program error in the list to jump to the corresponding line\"** (line 6). A grep of the page for `export|save|copy|clipboard|.txt|.csv|.html` returned **only the copyright footer — zero export affordance.** The report **\"automatically updates\"** and rows are **\"immediately removed when corrected\"** (line 7) — it is a live, ephemeral view, not a persisted artifact.\n- The Stop Conditions Panel (`fast-version-controls.htm` lines 147-153) is likewise an **on-screen panel** (\"appears on the right side of the screen with information about the cause of the stop\") — UIA-only.\n- This matches PRISM's own catalog: `cimco-control-map.mjs` `read_simulation_report` is `channel: UIA`, `confidence: LIKELY`, evidence: *\"Simulation Report is an on-screen docking-pane tab; rows are UIA-readable text (no screenshot)\"*. The header comment (lines 14-17) confirms: *\"the Simulation Report is an on-screen docking-pane tab with columns LINE / TYPE / DESCRIPTION / ACTION.\"*\n\n**=> SPINE-2 reader must walk the UIA element tree of the Simulation-tab docking pane and read the report list's row/cell text (LINE/TYPE/DESCRIPTION/ACTION). No file/CSV/clipboard path exists for the verdict report.** Confidence: **CONFIRMED from docs** that no export is documented; **LIKELY** (needs live-app verify) that UIA text-read works against the actual list control.\n\n**IMPORTANT cross-surface caveat — cycle time IS exportable, but it is a DIFFERENT surface:**\n- Cycle time / machining time is NOT in the Simulation Report. It lives in **Toolpath Statistics** (`menuncfuncinfo.htm`, NC Functions ▸ Info menu): \"range of X,Y,Z, **machining time**, tool change time, toolpath length... **Data can be exported**\" with **Export format: Standard or CSV** → \"a new document is created with the exported data\" (lines 8, 38, 47). This is a **FILE/exportable channel** — not UIA-only.\n- It is also shown live in the **Tool Position and Status Panel** (`fast-version-controls.htm` lines 64-79): **Dist** (distance traveled) + **Total** (total toolpath distance) + Feed + Spindle speed, and per `presentation-viewer.htm` line 94: \"elapsed time, and **total machining time for cycle-time validation**.\"\n\nSo SPINE-2 has a **two-channel split**: the **collision/limit verdict** is UIA-only (Simulation Report docking pane), while **cycle-time** can be pulled via the exportable Toolpath Statistics CSV (FILE channel) — the more robust path for that one metric.\n\n---\n\n### Files read fully (absolute paths)\n- `H:/prism/resources/cimco-2026/_extracted/edit_us/backplotsimulationreport.htm`\n- `H:/prism/resources/cimco-2026/_extracted/edit_us/backplot-fast.htm`\n- `H:/prism/resources/cimco-2026/_extracted/edit_us/fast-version-controls.htm`\n- `H:/prism/resources/cimco-2026/_extracted/edit_us/menubackplotsolid.htm`\n- `H:/prism/resources/cimco-2026/_extracted/edit_us/setupbackplot.htm`\n- `H:/prism/resources/cimco-2026/_extracted/edit_us/backplotvariables.htm`\n- `H:/prism/resources/cimco-2026/_extracted/edit_us/backplotnavigation.htm`\n- `H:/prism/resources/cimco-2026/_extracted/edit_us/menubackplottoolpath.htm` (the Stop Conditions keystone — 8 verbatim categories)\n- `H:/prism/resources/cimco-2026/_extracted/edit_us/menubackplotview.htm`\n- `H:/prism/resources/cimco-2026/_extracted/edit_us/menuncfuncinfo.htm` (cycle-time export channel)\n- `H:/prism/scripts/cimco-control-map.mjs` (`parseSimulationReport`)\n- `H:/prism/mcp-server/src/engines/post-processor/CimcoVerificationBridgeEngine.ts` (`evaluateSimulationReport` port)\n\n### Top SPINE-2 implementation directives (R12, safety-critical)\n1. Expand the `category` enum from 4 → the 8 verbatim Stop-Condition events; route `tool_change`/`program_stop` to NON-failing.\n2. Add the **`collisionCheckRan` gate** — refuse to return `pass=true` on an empty report unless the \"Check collision and limit errors\" pass is confirmed to have executed. This closes the current fail-open hole.\n3. Consume **structured UIA cells** (LINE/TYPE/DESCRIPTION/ACTION) directly; don't re-split flattened strings.\n4. Extract the named **axis** (travel_limit) and **model-pair** (collision) from DESCRIPTION.\n5. Pull cycle-time from the **Toolpath Statistics CSV export** (FILE channel), not the report.",
      "I have complete, verified evidence across all four investigation axes. I also note a critical finding: `parseSimulationReport` already exists in `cimco-control-map.mjs` (lines 238-311) — the orchestrator's premise that the SPINE-2 report reader is \"unbuilt\" may need updating (it parses a report, the open question is whether the live app emits one). I'll flag this. Final report follows.\n\n---\n\n# CIMCO Native Sim-Machine Recon — FINDINGS (slot echo, READ-ONLY)\n\n## (a) The .mcfg format + exactly where UNITS live + how to resolve the 44 unresolved\n\n**Format: plain JSON** (verified by reading 5 real files). Confirmed by the parser comment at `scripts/cimco-control-map.mjs:53`: *\".mcfg is plain JSON (MachineDefinition{Header,MachinePartGroups,Collision,Revolver})\"*. NOT XML/INI. Top structure:\n```\n{ \"MachineDefinition\": {\n    \"Header\": { \"DisplayName\", \"GUID\", \"Orientation\", \"Unit\", \"Version\":2.0,\n                \"MaxCuttingFeedrate\"(opt), \"Acceleration\"(opt) },\n    \"MachinePartGroups\": [ {Type:\"Base\"}, {Type:\"Head\",Axis:{...nested...}}, {Type:\"Table\",Axis} ],\n    \"Collision\": [ {GroupOne:[...],GroupTwo:[...],Name} ... ],\n    \"ToolchangePositions\": [ {Commands,Name,Position{X,Y,Z},Sequence{...}} ],\n    \"WorkpieceTransformation\": {X,Y,Z} } }\n```\n\n**WHERE UNITS LIVE — `MachineDefinition.Header.Unit`** (a string). This is the single authoritative scale field. Verified values found in the corpus: `\"Metric\"` only (e.g. `Cimco Mill 3 Axis Type C.mcfg:7` → `\"Unit\" : \"Metric\"`; lathe → same). **The help doc `configuration-editor.htm:54-56` confirms it as a `Base` property: \"Unit System: The Unit System (Metric/Imperial) used in the configuration for property values such as positions, limits and feedrates.\"** So Imperial is a legal value — the format supports it — but **ZERO .mcfg in this corpus declares Imperial** (verified: `grep \"Unit\":\"Imperial\"` → No files found).\n\n**Kinematics fields** (per axis, nested parent→child via `\"Axis\"`): `Name` (X/Y/Z/A/B/C/U/V/W), `Type` (\"Translation\" | \"Rotation\"), `Vector{X,Y,Z}` (axis direction, e.g. B = `{0,-1,0}`), `Limits{Min,Max}` (mm for linear, degrees for rotary), `MaxSpeed`, and for rotary `Center{X,Y,Z}` (centre of rotation). Lathe adds a `Clamp{Angle,ClampTime,Jaws,UnclampTime}` sub-object on the C/table axis. The nesting order IS the kinematic chain (Head carries the spindle axes; Table carries rotary stack).\n\n**WHY 44 are unresolved (root cause, verified):** the parser at `cimco-control-map.mjs:175-181` reads `Header.Unit`, lowercases it, maps `metric/mm/millimeter→\"mm\"`, `inch/imperial/in→\"inch\"`, else `\"unknown\"` and pushes a warning. **The 44 vendor-supplied .mcfg (Haas, Doosan, DMG Mori, Hermle, Hwacheon, Matsuura, Mikron, Quaser, Roeders) have NO `Unit` key in their Header at all** — verified: `Haas VF1.mcfg` Header = `{DisplayName, Version, GUID}` only; `Haas UMC-750.mcfg` identical. The 42 resolved are all the Cimco-authored generic templates that DO carry `\"Unit\":\"Metric\"`. Distribution confirmed exactly: `byUnit = {\"mm\":42, \"unknown\":44}`.\n\n**How to resolve the 44 — three findings:**\n1. **Can we read units from another field? YES, with high confidence — the AXIS LIMITS are self-evidently metric.** `Haas VF1.mcfg` limits are Z `-508`, X `±254`, Y `±203` → these are exactly **20\"/10\"/8\"** travel (VF-1 spec) expressed in **mm**. A Haas VF-1 in inches would have limits ~`20/10/8`, not `508/254/203`. So the vendor .mcfg geometry is authored in **mm** even though the field is absent. The resolution is: **the absent-`Unit` vendor configs are implicitly metric (mm)** — CIMCO's own machine library ships everything in mm. This is verifiable per-file by a magnitude heuristic (linear limit |max−min| > ~50 ⇒ mm, since an inch machine's travel in inches is rarely >50). I'd gate this as a *derived* resolution, not a blind default.\n2. **Is JM=inch the safe default per units-guard? NO — that would be the WRONG default here, and dangerously so.** The units-guard rule (`scripts/lib/units-guard.mjs`, CLAUDE.md §SAFETY RAILS) says JM *parts/programs* are inch — but the **.mcfg machine geometry is a SEPARATE unit domain**. The Haas .mcfg is mm. Defaulting the .mcfg to inch would mislabel a 508 mm travel as 508 inch → a 25.4× over-scaled machine envelope → sim either passes garbage or false-alarms. **Correct resolution: vendor .mcfg with missing `Unit` ⇒ resolve to `mm` (the CIMCO library convention), NOT inch.** The inch convention applies to the *NC program / workpiece*, which CIMCO handles via the `G20/G21` in the toolpath, independent of the machine-model unit.\n3. **The cleanest fix:** author the missing `\"Unit\":\"Metric\"` line into each of the 44 vendor Header blocks (or, in the index parser, fall back to `\"mm\"` when `Header.Unit` is absent AND axis-limit magnitudes confirm metric, emitting a `unitsInferred:true` flag rather than silent assumption — R12). Confidence the vendor configs are metric: **HIGH** (corroborated by exact mm↔inch spec arithmetic on VF-1).\n\n## (b) How to author a NEW native sim machine (.mcfg) for a JM machine the generic templates don't fit\n\nTwo authoring routes, both documented:\n- **GUI route (CIMCO Edit, Machine-Simulation add-on):** `custom-machines.htm` → \"Add\" dialog → set Machine name + Type (3-Axis / 4-Axis Head / 4-Axis Table / 5-Axis Head-Head / 5-Axis Head-Table / 5-Axis Table-Table), then the **Advanced Machine Configuration Editor** (`advanced-configuration-editor.htm`) which *\"automatically calculates the machine kinematics, adapting to the configured axes arrangement\"* — you set Travel limits (min/max), Feedrate, Position per axis + Tool-Change positions + STL models. For revolvers/spindles/collinear axes you must use the non-advanced **Machine Configuration Editor** (`configuration-editor.htm`). **Saved to `\\AppData\\Roaming\\...\\MachineCfg\\`** (`configuration-editor.htm:30-31`) — a green icon = error-free/saveable, red = errors.\n- **Direct JSON route (PRISM-native, NO GUI, the high-leverage path):** since .mcfg is plain JSON and `readMachineDef` round-trips it, **PRISM can emit a .mcfg programmatically from its own machine-kinematics model** (e.g. `ShopConfigurationEngine` / the JM 5-VMC fleet travels). Recipe per JM machine: build Header `{DisplayName, GUID:<uuid>, Orientation:\"Vertical\"|\"Horizontal\"|\"Lathe\", Unit:\"Imperial\", Version:2.0}` (use Imperial since JM travels are known in inches — OR author in mm by ×25.4 and set Metric; pick ONE and be explicit), then the MachinePartGroups Base→Head(spindle linear axes)→Table(rotary stack) chain with real JM `Limits{Min,Max}` from the machine spec, `Vector` per axis, `Center` for rotaries, plus the Collision pairs (copy the standard tool|workpiece, tool|fixture, axis|workpiece set from the nearest generic template) and a ToolchangePositions block. **CRITICAL authoring rule from `configuration-editor.htm:178-179`:** *\"the geometry (STL models) of a machine must be set in relation to the machine zero position and the origin set to the tip of the spindle nose\"* — and `WorkpieceTransformation` defines work-origin offset (e.g. `Z:-548` on the 3-axis template). Without correct STL+transform the collision sim is geometrically wrong even if axes are right.\n\n## (c) Which of the 10 generic-template JM machines MOST need a purpose-authored .mcfg\n\nAll 10 generic-template machines score only **0.45–0.50** (just over the 0.30 generic floor; far below the 0.60 native threshold) — `cimco-jm-machine-map.mjs:48` `TIER={nativeMin:0.6, genericMin:0.3}`. Priority ranking (highest authoring need = worst kinematic fidelity vs real machine):\n\n1. **VMC-02 Okuma M460V-5AX** (mill, 5-axis) → mapped to generic `Cimco Mill 5 Axis Head Head 45° AB Type A` @0.45. **HIGHEST PRIORITY** — 5-axis kinematics are the most sensitive to wrong centre-of-rotation/trunnion config; a generic 45°-AB head-head template almost certainly does NOT match the M460V's actual table-tilt kinematics. Wrong 5-axis kinematics = the exact \"clean sim is meaningless\" failure mode. Real Okuma 5AX travels are known to PRISM.\n2. **LTH-07 Okuma Multus B250II** (mill-turn) → generic `Cimco Lathe Mill-Turn BC + Sub` @0.50. Mill-turn B-axis + sub-spindle kinematics are complex; generic template won't carry the real B-axis limits/live-tooling geometry.\n3. **VMC-01 Hurco VM30i** and **VMC-05 Roku-Roku HC 658-II** (both 3-axis mills) → generic `Cimco Mill 3 Axis Type A` @0.45. 3-axis is the most forgiving, BUT both have **zero vendor .mcfg** in CIMCO and Roku-Roku is a precision jig-mill with non-standard travels — author exact travels.\n4. **LTH-01/02 Okuma GENOS L300-M / L200E-M** (live-tool lathes) → generic `Cimco Lathe 4 Axis CY + Sub` @0.45 — C+Y live tooling; verify the generic CY template matches.\n5. **LTH-03/04/05/06 Okuma plain lathes** → generic `Cimco Lathe 3 Axis C` @0.45 — lowest priority (simplest kinematics), author last.\n\nNote: **every one of the 10 also carries the metric-candidate UNITS WARNING** (mm template vs JM-inch) — so authoring an Imperial-unit .mcfg for each simultaneously fixes both the kinematic-fit AND the units gap.\n\n## (d) Honest risk + how to gate it\n\n**The risk is real and load-bearing: a wrong-units or wrong-kinematics .mcfg makes a CLEAN CIMCO sim result MEANINGLESS — actively dangerous because it gives false green-light confidence on a post JM will LIVE-RUN.** Two distinct failure axes:\n- **Wrong units (25.4×):** an inch JM machine simulated against a mm .mcfg (or vice-versa) mis-scales the entire envelope — over-travel/collision checks fire on phantom limits or miss real ones. This is the *exact* 25.4× class the units-guard exists to stop.\n- **Wrong kinematics:** generic 5-axis template ≠ real trunnion centre-of-rotation → a 5-axis collision sim that passes proves nothing about the real machine.\n\n**The codebase already encodes this honesty (verified, do not weaken it):**\n- `jm-fleet-sim-map.json` `safety` field + every entry's `mustVerifyKinematics:true`: *\"A CIMCO-sim CLEAN result is conformance-clean, NOT controller-verified, and is only valid if the .mcfg kinematics match the real machine. JM convention = INCH.\"*\n- `parseSimulationReport` (in `cimco-control-map.mjs:305-306`) **already** hard-codes the honest verdict string: a pass renders as *\"CIMCO-sim CLEAN (…) — conformance-clean, NOT controller-verified\"*. **NB for the orchestrator: a sim-report PARSER already exists at `cimco-control-map.mjs:238-311` (`parseSimulationReport`)** — the SPINE-2 gap is narrower than \"unbuilt reader\"; the open blocker is whether the live licensed `CIMCOSimulation.exe` *emits* a machine-readable report (the file comment at lines 11-13 notes it exposes \"NO discoverable public headless sim+report-export flags\" → the report must be produced via UIA/GUI, then this parser consumes it).\n\n**Recommended gate (3-of-3, fail-loud):** a CIMCO sim verdict may be promoted to \"approved for live test\" ONLY when (1) `unitsResolved === true` AND the .mcfg unit domain is reconciled with the program's G20/G21 (no 25.4× mismatch), (2) `mustVerifyKinematics` has been cleared by a human/automated check of travels + axis-config + centre-of-rotation against the real machine spec, and (3) the sim report parses CLEAN. If any of the 3 is unmet, the verdict must surface as **\"conformance-clean, UNVERIFIED kinematics — NOT cleared for live run\"**, never as a bare pass. Authoring exact-unit, exact-travel .mcfg files for the 10 generic machines (per (b)/(c)) is what turns gate-condition (1)+(2) from \"blocked\" to \"clearable.\"\n\n---\n**Confidence summary:** .mcfg format/units location — **CONFIRMED** (read 5 files + parser). 44-unresolved root cause (missing Header.Unit on vendor files) — **CONFIRMED** (read Haas headers + parser logic + distribution). Vendor files are metric — **HIGH** (mm↔inch spec arithmetic on VF-1, not a declared field). Authoring routes — **CONFIRMED** (help docs + JSON round-trip). Gap ranking — derived from the live `jm-fleet-sim-map.json` scores + scorer weights. **NOT FOUND / UNVERIFIED:** no .mcfg declares Imperial anywhere in this corpus, so an Imperial example to copy does not exist — an authored JM .mcfg would be the first Imprial config and should be validated in the GUI editor (green-icon check) before trusting it.\n\nKey files: `scripts/cimco-control-map.mjs` (parser + report parser), `scripts/cimco-jm-machine-map.mjs` (scorer), `state/shared/cimco/machine-index.json` (86 machines, byUnit mm:42/unknown:44), `state/shared/cimco/jm-fleet-sim-map.json` (15 JM, byStatus generic-template:10/native-cimco-match:2/not-applicable:3), `resources/cimco-2026/_extracted/edit_us/{custom-machines,advanced-configuration-editor,configuration-editor,setupmachinemodels}.htm`, example configs under `resources/cimco-2026/CIMCOEdit/MachineCfg/`."
    ],
    "synth": "## ARCHITECTURE VERDICT\n\n**Operator-gated (live UIA) for the safety verdict. No headless CLI path exists.** Confidence: HIGH, four-finding-converged.\n\nThe CLI is real (`/ms file1 --no-bring-to-front`) but it only *launches a GUI sim window* — it does not run-to-completion-and-emit-a-verdict. The collision/over-travel/gouge object lives in the `UI::CollisionReport` namespace (Finding 1), the report is a live UIA docking-pane tab with NO export/CSV/clipboard affordance (Finding 3), and `--savebundle` writes a 3D *replay* not a parseable verdict. The External-Command hook carries PRISM's verdict OUT, not CIMCO's verdict back (Finding 1d).\n\n**Consequence for the build plan:** split the proof into two independently-shippable arms with very different automation profiles:\n- **Arm 1 — OFFLINE-AUTO (full weight, no license, no GUI):** dialect/byte-equiv/syntax correctness + .mcfg unit+kinematics integrity. This is everything we can prove blind, and it is where this tick's work concentrates. It does NOT prove collision/gouge.\n- **Arm 2 — OPERATOR-ATTENDED-UIA:** the collision/limit verdict via SPINE-2 UIA reader against the live licensed app. We build the *parser hardening* offline now (so it's ready), but it cannot be E2E-proven without the operator. **Do NOT architect as if a clean offline run = cleared for live test** — that is the fail-OPEN safety hole (Finding 3b/4d). Every promotion must carry `controllerVerified:false` + the 3-of-3 gate.\n\nA correction that lowers the perceived gap (Finding 4): `parseSimulationReport` is NOT unbuilt — it exists at `scripts/cimco-control-map.mjs:238-311` with a TS port in `CimcoVerificationBridgeEngine.ts`. The real SPINE-2 work is *hardening that existing parser* (4→8 categories, collisionCheckRan gate) + building the UIA *element-tree reader* that feeds it. The reader is the only operator-blocked piece.\n\n## NEXT UNITS (dependency-ordered, R13 logical: core→integration)\n\n**U-CIMCO-MCFG-UNITS-INFER:** Resolve the 44 unit-unknown vendor .mcfg via magnitude-heuristic fallback (`Header.Unit` absent + linear-limit |max−min|>~50 ⇒ mm) emitting `unitsInferred:true` — never blind-default, never JM-inch (that's the 25.4× trap, Finding 4a). | files: `scripts/cimco-control-map.mjs` (the `Header.Unit` reader ~lines 175-181), `state/shared/cimco/machine-index.json` (regenerate `byUnit`), new `mcp-server/src/__tests__/CimcoMcfgUnits.test.ts` | **offline-now** | depends-on: none (FOUNDATION)\n\n**U-CIMCO-REPORT-SCHEMA-8CAT:** Expand `CimcoFindingCategory` enum 4→8 verbatim Stop-Condition events (collision / collision_tool_workpiece / collision_holder_stock / rapid_to_stock / stopped_spindle_cut / travel_limit / program_stop / tool_change) + route `tool_change`/`program_stop` to NON-failing; add axis + model-pair extraction from DESCRIPTION (Finding 3a/3b). | files: `mcp-server/src/engines/post-processor/CimcoVerificationBridgeEngine.ts` (`evaluateSimulationReport`), `scripts/cimco-control-map.mjs` (`parseSimulationReport` parity port), test file | **offline-now** (parser logic provable on fixtures) | depends-on: none (parallel to UNITS)\n\n**U-CIMCO-COLLISIONCHECK-GATE:** Add the `collisionCheckRan` hard gate — `parseSimulationReport` must NOT return `pass:true` on an empty/null report unless the \"Check collision and limit errors\" pass is confirmed executed. Closes the fail-OPEN hole where empty-report ≡ check-never-ran (Finding 3b, safety-critical). | files: `CimcoVerificationBridgeEngine.ts` (the `input==null` branch ~line 613), `scripts/cimco-control-map.mjs` (~line 261), test | **offline-now** (the gate logic + fixtures) — but the *signal* it reads needs-live-app to wire | depends-on: U-CIMCO-REPORT-SCHEMA-8CAT\n\n**U-CIMCO-PROMOTION-3OF3-GATE:** Implement the fail-loud promotion gate: a verdict reaches \"approved for live test\" ONLY when (1) `unitsResolved===true` + .mcfg-unit reconciled with program G20/G21, (2) `mustVerifyKinematics` cleared, (3) report parses CLEAN with `collisionCheckRan===true`. Any unmet → surface \"conformance-clean, UNVERIFIED kinematics — NOT cleared for live run\" (Finding 4d). | files: `CimcoVerificationBridgeEngine.ts`, `state/shared/cimco/jm-fleet-sim-map.json` (consume `mustVerifyKinematics`), dispatcher wiring `prism_cimco` in post-processor dispatcher, test | **offline-now** | depends-on: U-CIMCO-MCFG-UNITS-INFER, U-CIMCO-COLLISIONCHECK-GATE\n\n**U-CIMCO-MCFG-EMIT-JM:** Programmatic .mcfg JSON emitter from PRISM's JM machine-kinematics model — author exact-unit, exact-travel native configs for the 10 generic-template JM machines, highest-fidelity-first (VMC-02 Okuma M460V-5AX). Fixes both kinematic-fit AND units gap per machine (Finding 4b/4c). Pick ONE unit convention explicitly per file (recommend Metric ×25.4 from JM inch travels, since zero Imperial example exists to validate against — Finding 4 NOT-FOUND). | files: new `scripts/emit-jm-mcfg.mjs`, `mcp-server/src/engines/.../ShopConfigurationEngine.ts` (source travels), `state/shared/cimco/jm-fleet-sim-map.json`, output to `resources/cimco-2026/CIMCOEdit/MachineCfg/`, test | **offline-now** to emit; **needs-live-app** (GUI green-icon validation) before trust | depends-on: U-CIMCO-MCFG-UNITS-INFER\n\n**U-CIMCO-CLI-LAUNCH-WRAPPER:** Wrap the verified launch CLI (`CIMCOEdit.exe /ms \"<nc>\" --no-bring-to-front`) as a PRISM-callable launcher that opens the sim window unattended (focus-suppressed) and registers the External-Command file-channel hook so CIMCO can call PRISM's offline verdict on `$FILEPATH`→`$OUTFILE` (Finding 1c-rank2, 1d). | files: new `scripts/cimco-launch-sim.mjs`, External-Commands config under `state/shared/cimco/`, test | **needs-live-app** (license to actually launch; offline-testable as command-string construction only) | depends-on: U-CIMCO-PROMOTION-3OF3-GATE\n\n**U-CIMCO-SPINE2-UIA-READER:** Build the UIA element-tree reader that walks the Simulation-tab docking pane, reads per-cell LINE/TYPE/DESCRIPTION/ACTION directly (NOT re-split flattened strings — Finding 3c-#6), confirms the \"Check collision and limit errors\" trigger fired, and feeds the hardened `parseSimulationReport`. Pull cycle-time from the SEPARATE Toolpath Statistics CSV export (file channel), not the report (Finding 3d). | files: new `scripts/cimco-spine2-uia-reader.mjs` (or `.ts` engine), `CimcoVerificationBridgeEngine.ts` consume structured cells, test | **operator-only** (live licensed app + Machine Simulation add-on; cannot E2E without the operator driving) | depends-on: U-CIMCO-COLLISIONCHECK-GATE, U-CIMCO-REPORT-SCHEMA-8CAT, U-CIMCO-CLI-LAUNCH-WRAPPER\n\n**U-CIMCO-WINDOW-HELP-MISC-REEXTRACT:** Re-extract the `window-help-misc` cluster that failed StructuredOutput in the nav-map (a real gap in the *already-depended-on* edit_us surface) — completes editor-surface plotting before any new CHM (Finding 2c). | files: `state/shared/cimco/nav-map.json`, extraction sidecar | **offline-now** | depends-on: none (independent hygiene; low priority vs safety units)\n\n## SINGLE HIGHEST-LEVERAGE UNIT FIRST THIS TICK\n\n**U-CIMCO-MCFG-UNITS-INFER.**\n\nRationale: It is the only true foundation — every downstream safety claim depends on units being right, and it is the highest-consequence-per-line fix in the whole plan. The 44 unresolved configs are not a cosmetic gap: defaulting them to JM-inch (the naive reading of the units-guard rule) would mislabel a 508 mm Haas VF-1 travel as 508 inch = a **25.4× over-scaled envelope** that makes the collision sim either pass garbage or false-alarm — the exact failure class the units-guard exists to stop (Finding 4a/4d). It is 100% offline, needs zero license, has a verified-correct resolution (mm via magnitude heuristic, corroborated by exact VF-1 mm↔inch spec arithmetic), blocks U-CIMCO-PROMOTION-3OF3-GATE and U-CIMCO-MCFG-EMIT-JM, and ships with a clean test (the `byUnit` distribution flips 42/44 → 86/0-with-inferred-flag). Fixing units before kinematics before report-reading is the strict R13 dependency order. Build the foundation that all green-lights stand on, before any green-light can be issued.\n\n## PLOTTING COMPLETENESS VERDICT\n\n**The proving-critical surface is FULLY plotted (in edit_us) — the collision/limit verdict, machine-model, stock, setup, DNC-transmit are all present.** The keystone blocker is NOT an unplotted CHM; it is the live app + the UIA reader (Finding 2b/2c).\n\n- **Remaining to plot (do now, cheap):** re-extract the `window-help-misc` cluster that failed StructuredOutput in `nav-map.json` — the only known gap in the editor surface we already depend on (→ U-CIMCO-WINDOW-HELP-MISC-REEXTRACT).\n- **DEFER `cnc-calc_us.chm`** (10.4 MB, ~150-250 est. pages, NOT decompiled): it is a CAM *generator* with its own JS post processors — a competing post SOURCE, not a source-agnostic verifier. Wiring it conflates \"PRISM's post is correct\" with \"CNC-Calc agrees\" (weaker/confounded). Tag `[DEFERRED — orthogonal: CAM source not verifier]`. Revisit only if goal expands to a second-opinion CAM oracle.\n- **DEFER `nc-base_us.chm`** (4.7 MB, ~150 est. pages, NOT decompiled): DNC server/datastore admin = shop-floor *deployment*, after correctness is proven. The proving-relevant transmit slice is already in edit_us (`dnc-transmission`, 34 surfaces). Tag `[DEFERRED — deployment-phase, transmit-slice already in edit_us]`.\n- Decompile recipe when un-deferred: `hh.exe -decompile <out> <chm>` or `7z x <chm> -o<dir>`. Page counts UNVERIFIED until run (Finding 2d, R12).\n\nSo \"the entire cimco app\" plotted ≠ required for the goal. We plot the one failed editor cluster and explicitly defer the two CAM/DNC CHMs as out-of-path.\n\n## HONEST OPERATOR-ONLY BLOCKERS (surfaced, not hidden — R12)\n\n1. **Live CIMCO license required for ANY simulation run.** Machine Simulation features unlock only with a valid key via `Sys/KeyManager.exe` (Finding 1). No license = no collision verdict, period. Offline arm needs nothing; sim arm is hard-blocked.\n2. **The collision/over-travel verdict is operator-attended UIA — un-automatable to a file.** No headless flag, no CSV/clipboard export, the report is a live ephemeral docking-pane view, and \"Check collision and limit errors\" is an explicit on-demand button-click (Finding 1a/3d). SPINE-2 can be *built* offline but cannot be E2E-*proven* without the operator driving the live app.\n3. **`mustVerifyKinematics` cannot be auto-cleared for the 10 generic-template machines.** A clean sim against a wrong-kinematics .mcfg (esp. 5-axis VMC-02 trunnion centre-of-rotation) proves NOTHING about the real machine (Finding 4c/4d). Until U-CIMCO-MCFG-EMIT-JM ships AND each authored .mcfg is GUI-validated (green-icon) by the operator, kinematic fidelity is operator-cleared, not machine-cleared.\n4. **No Imperial .mcfg exists in the corpus to copy.** An authored JM Imperial config would be the FIRST — it must be validated in the live GUI editor before trusting (Finding 4 NOT-FOUND). This is why U-CIMCO-MCFG-EMIT-JM recommends the Metric-×25.4 path (a known-good convention) and still gates on operator green-icon check.\n5. **`--savebundle` verdict-content is UNVERIFIED** — strings prove it saves a replay, `UI::CollisionReport` suggests the verdict is NOT serialized in. One empirical probe on the live app is worth doing, but do NOT architect around it (Finding 1c-rank3).\n\nGiven the safety-critical live-test, surface blockers 1–3 to the operator NOW: the offline arm (this tick + next several) ships real value without them, but the collision green-light fundamentally requires the operator at the live licensed app."
  }
}