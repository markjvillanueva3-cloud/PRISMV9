# PRISM Fleet Launcher Update Required

When the Hermes tabbed interface is complete, update the fleet launcher to:

1. Support launching multiple Hermes windows with pre-configured tabs.
2. Default each window to subsets of the active fleet + ZULU.
3. Integrate with the Claude Account Pool for automatic account rotation per window.
4. Support the new autonomous /goal + /loop mode on launch.

This should be done after the Hermes desktop tab system is stable.
