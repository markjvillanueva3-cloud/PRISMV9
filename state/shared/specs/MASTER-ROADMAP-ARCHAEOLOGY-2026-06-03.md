# PRISM Master Roadmap Archaeology (2026-06-03)

Mined the full historical roadmap/plan corpus (339 roadmaps/plans, back to the Feb 2026 project origin) via a 16-batch parallel excavation, consolidated here from 6 batch digests. The purpose is to recover the *original* per-domain goals (what each domain set out to be before the galaxy/slot model), surface dormant builds worth reviving (pre-extracted, validated, but never wired), and carry forward the durable design ideas — to feed the master per-galaxy goal synthesis and the SFC / post-processor / quoting / ERP build-priority work. Everything below is grounded in the digests; superseded and abandoned material is explicitly fenced off so future chats don't re-mine dead ends. Counts and statuses are as-of the source plans (Feb–May 2026) — verify against `PRISM-INVENTORY-LATEST.md` + live engines before building.

---

## Historical goals by domain

**mill** — Originally "comprehensive print→validated-program milling pipeline" (collision-safety, multi-machine DB, 95K-tool selection, real-geometry toolpath gen, controller hardening — MILLING-COMPREHENSIVE 11MS/113U). Evolved into MILL-AGI ("AGI-grade cognitive milling": 5 ONNX neural engines + 7 Synergy Laws + closed feedback loop, ~800U/80MS) then collapsed to the integration-first thesis ("STOP CREATING — START INTEGRATING": wire 1,869 engines @30% via facade delegation). The throughline: build the physics core, then *wire what exists* rather than re-extract. Now the foxtrot/alpha mill galaxy.

**lathe / mill-turn** — Set out as the "most complex machine type" — full turn+mill multi-channel sync (2-4 ch), sub-spindle transfer, live tooling, Swiss guide-bushing, bar-feeder production loop (MILL-TURN-COMPREHENSIVE 12MS/138U). Evolved toward LATHE-MASTER's ambitious P0 substrate: formal G-code verification (Z3/SMT), local LoRA on JM Die's 5,297 Okuma programs, predictive 60s pre-play twin, live THINC/MTConnect telemetry. Today whiskey owns lathe (physics-first safety: G50/CSS, chuck-jaw).

**wedm** — The deepest-specced domain from origin: complete print-to-part pipeline (18-stage taxonomy, 12-engine, 15,900 LOC, physics-justified per parameter) → AGI layer (autonomous program-gen, self-optimizing params, digital twin, sensor fusion) → frontend parity (match lathe UX, DXF/photo→program). Evolved into WEDM-CONSOLIDATED master. Most production-complete of any domain. Now mike's galaxy.

**cad** — Originally CAD-draw + feature-recognition + STEP round-trip + vendor function-index parity (SolidWorks/Inventor/Mastercam). Evolved through CadQuery kernel ambitions and the 6-step studio wizard pattern (Wire EDM wizard became the clone template). Today delta owns cad (75/75 engines wired, electrode/trilobe gen).

**cam** — The master print-to-CNC spine: CAMX 14-stage MCP dispatcher chain (intake→features→DFM→machine→tool→strategy→S/F→toolpath→collision→post→sim→setup-sheet→cost→package, ~950U), every stage an MCP action so app + agent share one surface. Plus the Fusion 360 and hyperMILL integration product lines (the biggest unbuilt revenue surfaces). Superseded by PRISM-UNIFIED-v2. Now kilo's galaxy.

**post-processor** — Started as "turn 16+ existing post engines into a paid product" (machine make/model/year/controller→physics-optimized G-code, 4-tier pricing). The flagship "Post-Processor Framework" (Universal Intermediate Representation → 12+ controllers) was one of R14's four named commercial products. Lead-with-value framing (the before/after OptimizationReport). Now echo's galaxy (14-controller AGI surface).

**sfc / speed-feed** — Born as the physics core: Kienzle/Taylor/Merchant kc1.1 registry enrichment over 18,516 entries + Coolant/Coating registries. Evolved into the all-machine accuracy mandate (every one of 1,941 machine rows hydrates legal selections, validated against published data, honest limited-mode for the rest) + multi-model physics fusion (convergence solver). Now oscar's galaxy (9-axis orchestrator, vendor parity).

**quoting** — Originally cost-estimation as one of R14's four flagship products. Evolved toward the probabilistic "money shot": DrawingToQuoteMathPipeline producing P10/P50/P90 cost with confidence bands (SCIMATH stakeholder-unanimous). Now charlie's galaxy (print-to-quote + DocuStrata pricing).

**business / ERP** — Set out as "PRISM as full shop OS" (canonical job/traveler/labor/event spine, role-aware shells, ERP+accounting sync, offline replay — ULTIMATE-SHOP-OS) plus competitor-parity backend (E2/QuickBooks/Xometry/Fictiv) and an Employee/HR/Job-Tracking + Lean/Six-Sigma layer. Evolved into hotel's galaxy (355 engines, prism_business 879 actions). The largest unbuilt-frontend surface (engines exist, dashboards don't).

**academy** — Course/curriculum/MIT-OCW extraction: ~3,880 atomic skills from 206 MIT courses + 25 CNC/CAM resources, 4 tiers, SCORM export. Evolved into lima's galaxy (pypdf 8,752-page corpus, 3-leg ship contract).

**ai-training** — The original moat thesis: the Data Flywheel (every recorded job improves recommendations → personal/shop/network/global model tiers + Expert Distillation). Abandoned for lack of a user base; partially revived as the india NN-graph substrate + content/video learning pipeline ("the only post-processor that learns"). Now india's galaxy (GraphSAGE GNN tier-5, ~95 LoRA engines).

**system-viz** — Grew out of the R5 visual layer (operator-facing UI per intelligence feature) + the reverse-index/master-index awareness need. Evolved into the 548MB live graph that IS the fleet search substrate. Now sierra's galaxy.

**infra / cross** — The deepest evolutionary arc: from the v17 "Claude Code as autonomous engineering platform" (3-archetype subagents, hooks-as-hard-blocks, Task DAGs, model routing) through the Forge auto-wiring system (artifact lifecycle U0→U6, monotonic quality score), the embedding/semantic-RAG gap, and the persistence gap (PostgreSQL dead-code → in-memory fallback). These ancestors became today's slot fleet, scrutiny-3way gate, duplication-guard, master-index, and wiring-inference cascade.

---

## DORMANT BUILDS WORTH REVIVING

Prioritized by leverage (compounding/unblocking first, then revenue, then domain depth). Deduped across all 6 batches; superseded/abandoned items removed.

| Build/feature | Domain | What it was | Why revive now | Source plan |
|---|---|---|---|---|
| **PostgreSQL persistence + pgvector semantic search** | infra | `pg` is dead-code → entire DB layer silently falls back to in-memory; all auth tokens/rate-limits/business data lost on restart. pgvector to replace `String.includes` over 95K records. | Single most consequential gap — every revenue galaxy (ERP, quoting, shop-OS) depends on durable state + AS9100 audit trail. Fail-loud violation living in production. | proud-snuggling-tarjan (INFRA) |
| **Embedding infrastructure → Semantic RAG / Router / Memory** | infra/cross | In-process `@xenova/transformers` + `better-sqlite3` vector table over 3,700+ tribal tips / 95K tools / cross-session memory. | Independently surfaced by 3 plans as #1 leverage; replaces fleet-wide `String.includes()` search. NN-GRAPH GNN is only a partial descendant — the general semantic search is still missing. | AGENTIC-PATTERNS, INTEGRATION_PLAN_27, MASTER-AI-SYSTEM |
| **R13 monolith intelligence extraction (7 modules, ~27,000 lines)** | cross/mill/cam/post/tribal | rules_engine (5,500L), machining_rules (4,200L), best_practices, troubleshooting (symptom→cause→fix trees), operation_sequencer, tool_selector, constraint_engine, gcode_generator — never wired. | Largest single pool of dormant executable machining expertise. 184 of 257 extracted engines also unwired. Directly feeds the troubleshooter + process-planning products. | BRAINSTORM_R12_R14, PHASE_R13, MCP_ROUND2 |
| **5 ONNX/GNN mill neural engines** | mill/ai-training | MillStrategyNeural (Transformer), ChatterNeuralClassifier (CNN), SurfaceFinishCnn, ToolLifeGnn (GNN over tool-holder topology), GCodeUnderstandingTransformer (embed 24,545 JM programs). Specced w/ training-data sources, never trained. | NN-GRAPH/GNN infra now exists — the blocker (no inference substrate) is gone. Embedding 24,545 JM programs unlocks similarity/anomaly search. | MILL-AGI-UNIFIED |
| **Real-geometry toolpath gen + cutter-comp + multi-setup (MILL-MS4)** | mill | Pocket spiral/zigzag/morph-offset, helical/ramp entry, rest-machining, G41/G42 cutter-comp w/ lead-in/out arcs, multi-setup WCS (G54-59) + datum transfer. `PrintToProgramPipelineEngine` was a SCAFFOLD emitting hardcoded X0/Y0. | Highest-value build for *actual* print-to-G-code. Cutter-comp + multi-setup explicitly deferred to MILL-MASTER P72 — verify if ever built; "zero production value" without it. | MILLING-COMPREHENSIVE |
| **DrawingToQuoteMathPipeline + MonteCarloCostPDFEngine** | quoting/cross | 9-step probabilistic quote → P10/P50/P90 cost with confidence bands; the SCIMATH stakeholder panel's unanimous "money shot". | Directly feeds revenue + the RFQ→calculator→quote spine; quoting galaxy priority. Probabilistic quoting is a differentiator. | warm-wibbling-wreath (SCIMATH) |
| **Post-Processor Framework (UIR→12+ controllers) + OptimizationReportEngine** | post | Universal Intermediate Representation → 12 controllers (R14 flagship). OptimizationReport = "23% cycle saved, 3 tool-life risks, 47s air-cutting, 2 missing HSM codes" before/after one-pager. Plus Swiss dialects (Citizen/Star LFV, guide-bushing, sub-spindle). | Post galaxy priority build. The report is the #1 sales artifact; lead every paid milestone with it. Wire 16 existing post engines, don't rebuild. | R14, PP-MAXIMIZATION/PP-REVENUE |
| **MachineHandbookIntelligence pipeline (7 engines)** | infra/mill/lathe/wedm | Extract 910 machines' handbook PDFs → queryable registry: real spindle torque curves / axis-accel / work-envelope / thermal-comp replacing generic defaults; alarm/parts/maintenance/controller-programming engines; handbookLimitGuard hook. | 70% reuses existing DocumentLearning pipeline. Gates G-code against actual controller firmware + handbook spindle maxes — authoritative truth vs conservative tribal estimates. Handbook-coverage as SVI dimension. | piped-soaring-crab, MACHINE-HANDBOOK |
| **6 WEDM safety-guard engines + WEDM-P2P production pipeline** | wedm/shop-floor | Current-density / pulse-limit / power-density / kerf-width / wire-deflection / thin-wire-derate guards (physics-formula-backed, 10+ tests each) into WEDMSafetyEnvelopeEngine. WEDM-COMPREHENSIVE: 12 engines / 15,900L / 249 tests already near production. | A near-complete domain to harden/revive; safety-critical. Single-source limits from wire-spec-sheets.ts. | wobbly-riding-thimble, WIRE-EDM-COMPREHENSIVE |
| **Lean/Six-Sigma manufacturing-intelligence dashboards** | business/quality | OEE / SPC control charts / Kaizen board / Value-Stream-Map / Kanban / 5-Whys / Fishbone / A3 generator. Most wire to ALREADY-BUILT engines (SPCProcessCapability, NelsonSPCRules, OEECalculator, LeanSixSigma) that just lack frontends. | Low-effort/high-visibility: engines exist, only need UI + a downtime fact-table schema layer (from the data-analytics review). Business-galaxy revenue surface. | serene-meandering-prism |
| **Fusion 360 3-tier add-in product (Free/Pro/Ultimate)** | cam/cad | Free S/F panel → Pro 400+ CAM-param injection across all Fusion tabs → Ultimate internal PRISM toolpaths w/ per-block variable S/F, V-HACD collision pre-check, multi-setup tolerance stack. Plus the F360-REV keystone: reroute AutoProgram S10 through the 38-stage PostProcessorPipeline + PhysicsPrecomputeCache. | Single largest unbuilt revenue surface. F360-REV silo fix (per-block physics currently bypassed) is high-value even without the full product. | FUSION360-FULL, F360-REV |
| **Content/Video learning pipeline (6 engines)** | ai-training | ContentIngestionPipeline (text/video/doc/URL/social router) + NLP auto-tagger + KnowledgeDedup (TF-IDF cosine) + physics-validator (flags Kienzle-violating tips) + course auto-gen + fleet self-calibration. Vision-consensus over N videos w/ provenance. | "The only post-processor that learns" — captures YouTube/social CNC knowledge no competitor does. 6 engines specced, unwired. Physics-validate every ingested tip. | LEARN-PIPELINE, PHASE_CC |
| **Multi-channel + sub-spindle mill-turn pipeline (MT-MS4/MS7)** | lathe/mill-turn | Channel-sync codes (M200/M201, WAITM), sub-spindle transfer grip/thermal physics, bar-whip Timoshenko beam, guide-bushing deflection, bar-feeder M99 loop. `MillTurnSwissPipelineEngine.assembleProgram()` was empty. | Directly serves JM Die's Multus B250II. Per-time-step multi-element collision model is the correct pattern for any multi-channel machine. | MILL-TURN-COMPREHENSIVE |
| **Tool DB → 45K + Tool Explorer app** | sfc/cross | Schema-normalize+index FIRST, then fill turning inserts (16%→90%), grooving/threading (0 entries), boring (1%); 2.4GB catalog archive unprocessed. Faceted search, compare-with-verdict, inline RPM/feed calculator ("killer feature"), CAM-library export. | Largest open data gap. Index-before-expand (every search is a linear scan today). Per-material vc HARD-BLOCK table is the ancestor of units-guard. | TOOL_EXPANSION |
| **Dormant physics/AI IP catalog** | sfc/mill/cam/ai-training | 113KB cutting-thermal, 67KB thermal-expansion, 235KB MIT ODE solvers (chatter/thermal transient: RK/Adams/BDF stiff), ACO operation sequencer, Bayesian/PINN/EKF learning, 490-formula registry with 23/27 categories <10% wired. | Pre-extracted, validated, sitting unused. THERMAL/VIBRATION/SURFACE/ECONOMICS/SCHEDULING formula categories are nearly dark. | ASSET_INVENTORY, PRISM_ASSET_AUDIT_v14 |
| **WEDM sensor-fusion + digital-twin stack + EDMDischarge fusion plugin** | wedm/shop-floor | WEDMSensorFusion (Kalman over 8 streams: tension, spark-gap V @100µs, current waveform, water resistivity) + WEDMMachineState + WEDMDigitalTwin (<10ms) + material-characterization-from-spark. EDMDischarge plugin closes the gap↔MRR↔Ra coupling loop. | Real-time shop-floor infra never built. The fusion plugin would close WEDM's coupled-convergence loop in PhysicsFusionOrchestrator. | WEDM-AGI, WIRE-EDM-COMPREHENSIVE |
| **MachinePackageGeneratorEngine + 12 machine-safety hooks** | cross/sfc | Unify 920 machines into canonical machine-package (machine+controller+spindle+coolant+capability w/ provenance+confidence); per-machine legal-option matrices; spindle-limit/envelope/power-budget blocker hooks; ranked acquisition recs w/ ROI. | Scrutiny flagged machine data 0.5% complete, 0/920 have confidence metadata — a live data debt that poisons every cut-safety + part-fit decision. 5-level truth hierarchy (fail-closed in UI). | MCAT-MS0 |
| **Shop OS canonical event spine (ShopStateEngine)** | business | Canonical job/traveler/labor/event spine, role-filtered shells (employee/lead/planner/manager/admin), workflow command-center queues, E2 ERP/accounting bidirectional sync w/ offline replay, production-readiness scoring + release gates. | The connected-shop-floor OS; one source of truth for jobs/labor/approvals. Foundational for the business galaxy. | ULTIMATE-SHOP-OS, lucky-sauteeing-blum |
| **PhysicsFusionConvergenceEngine** | sfc | Nested FTW/FES/FDT feedback solver: Anderson acceleration + Broyden fallback + DFT oscillation detection + per-ISO-group α-adaptation (S=0.20 Inconel → N=0.85 Al), NaN/divergence/degenerate handling, machinist-facing convergence reporting. | Mathematical heart of multi-model physics fusion; production S/F mandates fusion_tier≥2. Verify if it shipped — if not, a major sfc dormant build. | iridescent-scribbling-haven |
| **InstantQuoteEngine + DFMPipelineEngine + FileStorage/PartsLibrary** | business/quoting | CAD→price+qty-breaks+lead-times; Cpk feasibility + cost-per-issue; SHA-256 dedup + revisions; QuickBooks OAuth2 connector; ApprovalWorkflow; JobTraveler+MachineDispatch. | Competitor-parity backend (Xometry/Fictiv instant-quote). Feeds the RFQ→quote→ship revenue spine. | lucky-sauteeing-blum |
| **ThreadCalculationEngine** | sfc/cross | ISO/Unified/British/ACME/NPT/Whitworth + tap-drill + thread-mill params + GO/NOGO gauges (12 tools). Flagged P0 "missing = people die". | Tier-0 safety-critical; threads are pervasive across mill/lathe. Verify current coverage — flagged missing at origin. | MCP_ENHANCEMENT_v2 |
| **CoolantRegistry (200+) + CoatingRegistry (100+)** | sfc | Coolants w/ full material-compat matrix; PVD/CVD coatings. Fully specced RGS units, likely never fully populated. | Closes a data gap that every S/F + tool-life calc silently approximates around. | S1-MS1-registry-enrichment |
| **MultiAxisPrintToProgramEngine (rebuild)** | mill/cam | RTCP/TCP compensation, singularity avoidance, 4 kinematic models, G68.2. Flagged STUB needing complete rebuild. | Filter to M460V indexed 3+2 first (shop reality), but the 5-axis substrate is the most-flagged stub. | FIVE-AXIS, mill-agent-10 |
| **codex-newfile-review orchestrator + AGI self-awareness engines** | infra | Deterministic `node --check`/esbuild HARD-GATE on every session-new file + advisory Codex line review + auto-viz-node + ghost-wire proposal. Plus 7 AGI self-model engines + DocPropagationEngine (0-drift across 17 doc surfaces). | The deterministic-gate + advisory-LLM split is the right R5 pattern; doc-propagation prevents the stale-claim hazard PRISM still fights. | humble-juggling-cerf, MASTER-AI-SYSTEM |

---

## USEFUL IDEAS WORTH KEEPING

Design ideas/approaches (not full builds) to carry forward into galaxy synthesis:

- **[infra] Wire-don't-rebuild + UTILIZE-BEFORE-CREATE.** Facade engines that delegate to existing physics engines; asset-utilization audit table (count × %-wired × gap) as the decision substrate. The ancestor of duplication-guard + master-index — still the operating doctrine. (MILL-AI v4, UNIFIED_v8)
- **[infra] Atomic file-created→awareness-updated transaction.** "No single transaction binds creation to registration" is the root cause of ALL duplication. Bind with PostWrite-sync + CAS lockfile + git-checkout rollback (forge-triple→forge-quint). (SCRUTINY-UNIVERSAL-HOOKS)
- **[infra] Dev-tools-first ROI.** "1 hour improving dev capability = 10+ hours saved downstream" / "fix dev infrastructure FIRST because ROI compounds across every later session." The explicit doctrine behind today's PRIORITIZE-devtools-backend rule. (PHASE_DA, UNIFIED_v8)
- **[infra] Route deterministic transforms to code, LLM only for judgment.** Don't gate on a flaky LLM for what a pure tool decides (deterministic compiler check is the hard block, Codex advisory). The R5 doctrine origin. (AGENTIC-PATTERNS, humble-juggling-cerf)
- **[infra] Hook stack as a feedback control system.** PID-controlled blocking aggression (relax when over-blocking, tighten when quality degrades) + Lyapunov stability + Bayesian-updated dedup threshold (the arbitrary 0.85 is PAC-undertrained). (MASTER-AI-SYSTEM-ADDENDUM)
- **[infra] Explicit "Retired Work" / conflict-resolution tables.** Kill redundant roadmap units rather than build duplicates (~5,500 LOC retired when Phase-0 absorbed them); authority/precedence ladder for conflicting roadmaps. (MASTER-EXECUTION-PLAN, mill-agent-10)
- **[cross] Product framing — answer the questions machinists actually ask.** "I need to machine Inconel 718 on my DMU 50 — what do I do?", "what if I bump Vc 20%?", "I can't get Inconel — substitute?" Intent-engine routing for the Dave persona (20-yr machinist, 10-sec patience). (PHASE_R3, PHASE_R8)
- **[cross] Lead every paid milestone with a demo-able before/after value artifact.** The OptimizationReport pattern; physics-backed wiring framed as revenue spine (RFQ→calculator→quote), not cosmetic parity. (PP-REVENUE, CWEDM)
- **[safety] Be better WITHOUT learning data via engineering quality.** Cold-start moat: 3,518 materials, physics not lookup, uncertainty quantification — learning is bonus. Should anchor every per-galaxy goal. (DATA_FLYWHEEL)
- **[safety] Fail-loud as a formal contract.** Every invariant needs threshold + enforcement + violation-response or it's an ASPIRATION not a CONTRACT. Safety-stability-under-load: |S(x)_loaded − S(x)_isolated| < 0.02. Limited-mode honest behavior beats fake full optimization. (SYSTEM_CONTRACT, CALC-OEM-ACCURACY)
- **[safety] Edge-case routing gate.** Detect thin walls <1mm / deep pockets L/D>5 / interrupted cuts / stainless/titanium → conservative S/F or manual review. Phased launch (simple-parts-only MVP, disable risky geometries until validated on 50→200→1000 real parts). (whimsical-hugging-fountain)
- **[cross] Mathematical governance.** Every formula declares formula_id/domain/inputs/units/constants/provenance; no inline constants anywhere. Honest formula labels (canonical/prototype/novel); NaN/Inf + FEM-vs-beam ±20% validation gate on custom formulas. (PRISM-UNIFIED-v2, SCIMATH)
- **[cross] Single canonical source per data class.** 8 competing materials/ dirs = drift hazard → pick ONE, archive the rest. The lesson the fleet relearned with vendor-catalog-db + resource-roots. (PRISM_DATABASE_AUDIT)
- **[ai-training] Multi-source consensus parameters with provenance.** Mean/min/max/stddev across N videos + confidence; "vision catches what the machinist never says aloud"; physics-validate before trusting; auto-calibration bounded ±30% of canonical constants, never exceeding safety. (PHASE_CC, LEARN-PIPELINE)
- **[mill/wedm] 4-LOOP per-unit quality protocol.** SCRUTINIZE→GAP-FILL→TIE-UP→VALIDATE, re-run review and findings MUST decrease. Per-milestone "Knowledge Sources" citation blocks. The 7 Synergy Laws (reasoning-trace-or-deny, formula-citation-required, feedback-or-it-didn't-happen). Ancestors of per-file scrutiny + AgiEngineResponse `_why` traces. (MILLING-COMPREHENSIVE, MILL-AGI)
- **[wedm/cross] Cross-galaxy UX-parity leveling.** One domain's complete UX becomes the spec for another (WEDM↔lathe). The 6-step studio wizard as a reusable clone template. (WEDM-UNIFIED, wire-edm-wizard-review)
- **[cad] Function-index pattern.** AI queries "what extrude params does X expose, units+defaults?" without grepping — replicable to any CAD/CAM vendor. (silly-coalescing-toast)
- **[infra] Trajectory evaluation.** Verify the engine-call *sequence* not just final output — catches silently-skipped safety steps. Protective hooks that BLOCK downstream emission when an upstream gate artifact is missing/stale. (AGENTIC-PATTERNS, LATHE-MASTER)
- **[cam] Pre-compute expensive physics at tool-selection, cache for the per-block hot path.** SLD 5-15ms / thermal RK4 20-80ms computed once → <2ms lookup. (F360-REV)

---

## Superseded / do-NOT-revive

Dead ends — future chats should not re-mine these:

- **Nexus generalized MCP framework + 15 DX features marketplace** (Hot Reload, Time-Travel Debugger, Agent Blueprints rev-share, HITL gates as a *product*) — speculative monetization of dev tooling with no user base; the PRISM-native fleet/hook/scrutiny stack already covers the useful parts.
- **$2.05M Yr1 ARR / 4-track business-launch timelines** (NEXUS_UNIFIED) — go-to-market projections, not buildable substrate.
- **The numbered monolithic phase ladder (P0→DA→R1–R15, v13–v25)** — superseded by the galaxy/slot model + PRISM-UNIFIED-ROADMAP-v2. Mine the *content* (R13 extraction, R14 products) not the phase numbers.
- **All version-variant snapshots** — MILL-AI-INTEGRATION v1/v2/v3/v3.1 (use v4), CAMX v17–v23 (use v24, itself now superseded by UNIFIED-v2), MILL-MASTER-RELEASE-CERT v3–v12 (current v13.1.0), PHASE_R2_SAFETY v16/v19 — re-versioned duplicates.
- **Process/recovery scaffolding** — PRISM_RECOVERY_CARD, PRISM_PROTOCOLS_*, ROADMAP_INSTRUCTIONS/TRACKER/SECTION_INDEX, ROLE_MODEL_EFFORT_MATRIX, OPUS_CONFIG_BASELINE, NEXUS placeholders ("copy the real file"), per-session agent-scratch (`*-agent-<hash>.md`), dated investigation handoffs — Feb-era execution protocols and trackers, no dormant builds.
- **PRISM-distributable-plugin / per-tenant multi-tenancy** — became the agents/skills/hooks system; one-session-per-tenant is moot for the single-test-shop (JM Die) reality.
- **Data Flywheel network-effect moat (as specced)** — the multi-shop network/global-tier model is abandoned (no user base); keep only the closed-loop job-outcome learning idea (india substrate) and the cold-start-via-quality doctrine.
- **shimmering-imagining-curry parent-aliveness orphan reaper** — too narrow; superseded by FLEET-REAPER's ancestry/confirm-after-N-ticks model. Do not retrofit into the known-good reapers.

---

## Feeds-forward

How this archaeology should shape the master per-galaxy synthesis + the SFC/post/quoting/ERP priority builds:

- **Anchor every per-galaxy goal on its ORIGINAL intent + the cold-start-via-engineering-quality doctrine.** Each galaxy's MEMORY.md goal statement should name (a) what the domain originally set out to be (this doc's §Historical goals), (b) its dormant builds worth reviving, (c) the "be better without learning data" framing. The mill/wedm 4-LOOP + 7-Synergy-Laws + formula-citation discipline should be lifted to galaxy-level build doctrine.
- **SFC priority: revive the physics-fusion convergence core + close the registry/data gaps.** Verify PhysicsFusionConvergenceEngine shipped (Anderson/Broyden); populate Coolant/Coating registries; finish the all-machine selection-hydration matrix (every machine row hydrates legal selections, validated against published data, honest limited-mode otherwise). The dormant 490-formula registry (THERMAL/VIBRATION/SURFACE at <10%) is the deepest sfc reserve.
- **Post priority: ship the OptimizationReportEngine FIRST as the value artifact, then the UIR→controller framework + Swiss dialects.** Wire the 16 existing post engines (don't rebuild); the F360-REV AutoProgram-S10→PostProcessorPipeline reroute closes a real per-block-physics silo. Lead with before/after demo value.
- **Quoting priority: build the DrawingToQuoteMathPipeline (P10/P50/P90 + MonteCarloCostPDF) on top of InstantQuote/DFM.** This is the stakeholder-unanimous money shot and the RFQ→calculator→quote revenue spine. Feed it the dormant cost-database IP (288KB TCO models).
- **ERP priority: unblock the PostgreSQL persistence gap before any business-galaxy feature, then surface the Lean/Six-Sigma dashboards over already-built engines.** The in-memory-fallback bug is a fail-loud violation and an AS9100 audit-trail blocker; durable state is a precondition for the Shop OS event spine. The OEE/SPC/Kaizen dashboards are low-effort (engines exist, only frontends + a downtime fact-table schema are missing).
- **Cross-cutting: revive embedding/semantic-RAG infra + R13 monolith extraction as the two highest-leverage non-domain builds.** Embeddings replace fleet-wide `String.includes` (3,700 tips / 95K tools / cross-session memory); R13's 7 unwired intelligence modules (~27,000 lines) are the largest pool of dormant machining expertise feeding the troubleshooter + process-planning + tool-selector across every domain galaxy.
