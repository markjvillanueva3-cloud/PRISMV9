# PRISM Revenue Roadmap — 2026-05-10

**Owner:** claude-99eca613 (revenue-roadmap lane)
**Peer chat owner of dev-tools / infra / AI-systems / neural / system-viz-wiring:** other concurrent chat (per session directive 2026-05-10)
**Scope:** PRISM backend + frontend assets that ship to paying customers (Speed/Feed Calculator, Master Post, CAD/CAM AI, the six tier-1 CAM bridges, shop-floor SaaS surfaces, ERP/quoting/scheduling, knowledge-as-subscription). Excludes: hook/dispatcher infra, system-viz internals, neural-net training infra, deep-reasoning engine plumbing (peer chat).

**Source-of-truth artifacts this plan consumes (do not duplicate; re-read on every iteration):**

- `state/shared/system-viz/system-graph.json` — live node map, 784 nodes / 916 edges (regenerated by `scripts/generate-system-viz.mjs`)
- `state/shared/AUDIT-LATEST.json` — 122 findings from `forge-audit-omniscient` (regenerated by `.claude/scripts/forge-audit-omniscient.mjs`)
- `state/shared/system-viz/audit-overlay.json` — per-node green/yellow/red/blue coloring (regenerated alongside audit)
- `state/shared/BUILD_STATE.json` — 2302 wired / 875 unwired engines + frontend-pending list (regenerated by `scripts/build-state-snapshot.mjs`)
- `state/shared/MILESTONE_PROGRESS.json` — 613 drifted milestones (envelope-claim vs git-reality)
- `state/shared/system-viz/_MASTER_ENVELOPE.json` — scrutiny axis registry (v2 filesystem, v3 capability-domain, v5 synthesized-audit)
- `mcp-server/src/data/jm-die-profile.ts` — test-shop reality reference for every customer-facing decision
- `H:/prism/JM DIE/` — 24,545 program archive (the customer behavior these features must serve)

---

## Revenue product taxonomy

PRISM ships revenue in **three product lines** (per CLAUDE-BRIEF doctrine):

1. **Speed/Feed Calculator (SFC)** — per-seat subscription, web + CAM plugin form factors. Backend: production. Frontend: 6 stub pages.
2. **Master Post** — per-seat subscription, browser-based G-code generator. Backend: production (cam-post-lint + post-generate + controller dialects). Frontend: 8 CAM/Post/Prove pages (built).
3. **CAD/CAM AI** — premium tier, consumes SFC + Master Post; drives autonomous CAD + CAM. Backend: 95-98% across mill/lathe/WEDM. Frontend: split across 5 stub page-groups (Mill/Lathe/WEDM/CAD/Quality).

**Tier-1 CAM bridges (each is a sellable integration channel):**
| Bridge | Backend status | Frontend / integration status |
|---|---|---|
| Fusion 360 | live add-in panel (in-host runner) | mcp-cadquery/frontend pending-merge could bootstrap UI |
| hyperMILL | production (best) — 63 engines, full Project Manager runtime | needs custom UI shell |
| Mastercam | C-Hook generator only — no live link | needs add-in shell + UI |
| Esprit | bridge exists | needs UI shell |
| Inventor HSM | bridge exists | needs UI shell |
| SolidWorks | bridge exists | needs UI shell |

**Shop-floor / ERP / quoting (PRISM-side SaaS surfaces):**

- Quoting Suite (10 pages built)
- ERP Pages (20 built)
- Shop-Floor / Sched (11 built)
- Dashboards / OEE (13 built)
- Specialty Pages (38 built — already revenue-active)
- HR / Safety / OSHA (9 built)

**Stub revenue surfaces (the gap):**

- CAD / SFC / Calcs (6) — direct hit on Product #1
- Mill Pages (5) — direct hit on Product #3 backend
- Lathe Pages (7) — direct hit on Product #3 backend
- Wire EDM Pages (5) — direct hit on Product #3 backend
- Quality / SPC / FAI (4) — customer-mandatory for shop adoption
- Admin / Settings (7) — subscription mgmt gate (no money flow without this)
- Knowledge / Learn (3) — onboarding (reduces churn, increases trial→paid)

---

## Roadmap structure (4 milestones, ~120 units)

Each milestone is a standard PRISM milestone envelope (atomic-roadmap-emit.mjs scans `mcp-server/data/milestones/`). Each unit follows the standard schema (`id`, `title`, `status`, `files_modified`, `details`, `acceptance`, `depends_on`). Wiring + tests + dispatcher round-trip per CLAUDE.md comprehensive-build doctrine.

### REVENUE-MS0 — Customer-facing UI unstub (≈40 units)

**Why:** Backend is 95-100% production for the three revenue products; UI is stub. **No customer can pay for what they can't see.** This unblocks every other phase.

**Depends on:** nothing. **Blocks:** all subsequent revenue work.

Units by stub-group (count = pages in group):

| Group | Units | Bootstrap source |
|---|---|---|
| `U-REV-CAD-SFC-01..06` | 6 — wire 6 CAD/SFC/Calcs pages to existing `prism_calc:cutting_force_kienzle` + `speed-feed-calc` dispatcher action backend | mcp-cadquery/frontend (Vite + React 19 + Three.js) merge candidate |
| `U-REV-MILL-01..05` | 5 — Mill pages: parameter editor / SLD viewer / chatter map / thermal preview / wear timeline | Existing CAM/Post/Prove pages as template |
| `U-REV-LATHE-01..07` | 7 — Lathe pages: turning param editor / threading wizard / hard-turn advisor / chip-control / cost-optimize / quality-gate / mill-turn sync | LATHE-AGI backend already wired |
| `U-REV-WEDM-01..05` | 5 — WEDM pages: feasibility / cost / program-gen / pass-schedule / wire-controller-select | WEDM backend 95% wired |
| `U-REV-QUALITY-01..04` | 4 — SPC chart / FAI editor / CMM-parse view / tolerance-stack visualizer | Backend engines wired |
| `U-REV-ADMIN-01..07` | 7 — User mgmt / seat assignment / billing portal / API key gen / RBAC role editor / audit log / subscription tier editor | Auth transport layer built |
| `U-REV-LEARN-01..03` | 3 — Interactive shop-knowledge tutor / video learn player / blueprint-read trainer | Knowledge engines wired |
| **40 total** | | |

**Acceptance per unit:** page renders with live data from at least one dispatcher action, passes Playwright happy-path + ≥3 failure modes (bad input, boundary, resource exhaustion) + ≥2 adversarial inputs (NaN/Infinity/empty/oversize) per comprehensive-build rule, ships with E2E test invoking through the dispatcher (not engine-singleton).

**Wiring rule:** every page MUST invoke through `prism_*` dispatcher actions, never directly import engine singletons. Tests assert the dispatcher schema + action enum + lazy import all match (per CLAUDE.md ENGINE WIRING law).

### REVENUE-MS1 — Subscription mechanics (≈18 units)

**Why:** SFC + Master Post are per-seat subscriptions. Without billing + tier gating, MS0 ships free.

**Depends on:** REVENUE-MS0 U-REV-ADMIN-* . **Blocks:** every dollar of revenue.

| Unit | What | Engine combo / new |
|---|---|---|
| `U-SUB-01` | Stripe webhook receiver | new: `StripeWebhookEngine` + `prism_subscription` dispatcher |
| `U-SUB-02` | Paddle alternative for non-US | new: `PaddleWebhookEngine` (Stripe-fallback rail) |
| `U-SUB-03` | Tier-feature mapping | new: `FeatureTierEngine` (reads `state/shared/feature-tiers.json`) |
| `U-SUB-04` | Per-seat license validation middleware | new: `LicenseValidatorEngine` wired into transport `tr.rest` + `tr.gql` |
| `U-SUB-05` | Trial → paid conversion flow | new: `TrialConversionEngine` + 3 trial-gated pages |
| `U-SUB-06` | Customer dashboard backend | new: `CustomerDashboardEngine` (usage rollup + billing fetch) |
| `U-SUB-07` | Customer dashboard frontend | wire to existing Dashboards/OEE template |
| `U-SUB-08` | Invoice/receipt PDF gen | new: `InvoicePdfEngine` (consumes pdf-skill) |
| `U-SUB-09` | Failed-payment dunning flow | new: `DunningFlowEngine` |
| `U-SUB-10` | API rate-limit per-tier | new: `RateLimitEngine` middleware on `tr.rest` + `tr.gql` |
| `U-SUB-11` | Per-tier hook injection | new: `TierGateHookEngine` (rejects calls beyond tier) |
| `U-SUB-12` | Refund/dispute handler | new: `RefundHandlerEngine` |
| `U-SUB-13` | Tax calc (Stripe Tax or custom) | new: `TaxCalcBridgeEngine` |
| `U-SUB-14` | Annual-vs-monthly discount logic | parameter on `FeatureTierEngine` |
| `U-SUB-15` | Multi-seat purchase + admin invite | new: `SeatAllocationEngine` + frontend |
| `U-SUB-16` | Subscription-pause / vacation mode | new: `SubscriptionStateEngine` |
| `U-SUB-17` | Self-service plan upgrade/downgrade | extends `FeatureTierEngine` + dispatcher action |
| `U-SUB-18` | Affiliate / referral tracking | new: `ReferralEngine` |

**Variability axis (must exercise ≥3 spanning configs):**
- Tier: free / hobbyist / pro / shop / enterprise (5 tiers)
- Form factor: web / CAM plugin / API-only / shop-floor terminal (4)
- Payment provider: Stripe / Paddle / wire-transfer-invoice / PO (4)
- Currency: USD / EUR / GBP / CAD / AUD / JPY (6)

Total config combinations: 5×4×4×6 = 480 — test plan must span ≥3 per axis, ≥48 tested.

### REVENUE-MS2 — Node-combination invention (NEW features for revenue) (≈42 units)

**Why:** The 122 audit findings + 875 unwired engines hide compound features whose pieces already exist. Combining them invents products without writing the math.

**Depends on:** REVENUE-MS0 + MS1. **Blocks:** competitive moat.

Each unit follows the pattern `Engine A + Engine B + Engine C → New Product X`. The combinations below are EXHAUSTIVELY enumerated from cross-referencing existing engines against unmet customer needs (from JM Die tribal knowledge + 24,545-program archive).

#### Mill product inventions (8 units)

| Unit | Combo | New product |
|---|---|---|
| `U-INV-MILL-01` | Kienzle + MaterialDB + ToolCrib live overlay | "Adaptive feed advisor" — real-time chip-load suggestion as operator types into CAM |
| `U-INV-MILL-02` | Taylor + WearAnalysis + TelemetryStream | "Tool life predictor" — countdown timer on spindle (subscription per machine) |
| `U-INV-MILL-03` | Deflection + Force + AdaptiveFeedModulation | "Long-tool stability optimizer" — auto-derate feed in deep cavities (sells as plugin) |
| `U-INV-MILL-04` | SLD + ChatterPrediction + ControllerDialect | "Chatter-free post" — auto-shifts spindle RPM to stability lobe in posted G-code |
| `U-INV-MILL-05` | Thermal + ThermalGrowth + ProbeRoutine | "Thermal-compensated first-part" — auto-probe + offset compensation |
| `U-INV-MILL-06` | DFMCheck + ToleranceStack + CADExtract | "Quote-from-STEP" — drag-in CAD, get cycle-time + cost in 60s (revenue: per-quote fee) |
| `U-INV-MILL-07` | Kienzle + Spindle + Power monitor | "Real-time cutting-condition violation alerter" (shop-floor SaaS) |
| `U-INV-MILL-08` | CycleTimeCrush + ProgramOptimize + Vericut-class sim | "Cycle Time Crush" SaaS — upload G-code, get optimized G-code (per-program fee) |

#### Lathe product inventions (7 units)

| Unit | Combo | New product |
|---|---|---|
| `U-INV-LATHE-01` | HardTurn + CBN/CeramicCatalog + ResidualStressModel | "Hard-turn replacement-of-grinding" advisor (per-job fee) |
| `U-INV-LATHE-02` | ChipControl + ChipbreakerCatalog + CSSWrapRisk | "Lights-out turning verdict" (subscription) |
| `U-INV-LATHE-03` | Threading + ThreadStandards + TapDrillDB | "Universal threading wizard" — any standard, any controller (plugin upsell) |
| `U-INV-LATHE-04` | MillTurnSync + SubSpindle + B-axisKinematics | "Mill-turn collision-free post" for Okuma B250IIW + similar (premium tier) |
| `U-INV-LATHE-05` | CostOptimize + TaylorEcoSpeed + BarStockUtil | "Cost-per-part calculator" with min-cost / max-prod / max-profit modes (web tool, viral acquisition) |
| `U-INV-LATHE-06` | LATHE-AGI + JMDieArchive | "Print-to-program" SaaS — upload print PDF, get NC (per-program fee) |
| `U-INV-LATHE-07` | GrooveOptimize + ChipControl + Chipbreaker | "Groove-job advisor" (niche, high margin) |

#### Wire EDM product inventions (6 units)

| Unit | Combo | New product |
|---|---|---|
| `U-INV-WEDM-01` | WEDMCost + QuoteEngine + CustomerHistory | "Auto-bid for EDM jobs" — feed CAD + part-count → bid PDF (per-bid fee) |
| `U-INV-WEDM-02` | WEDMFeasibility + WEDMConductivity + GeometryGate | "Go/no-go before quote" — instant cost-avoidance (free→trial→paid) |
| `U-INV-WEDM-03` | WEDMSafetyGate + WEDMHooks + WEDMPCD | "Lights-out WEDM verdict" — overnight unattended run readiness |
| `U-INV-WEDM-04` | WEDMProgram + WEDMController + 5 dialects | "Universal WEDM programmer" — Mitsubishi/Sodick/Makino/AgieCharmilles/Fanuc (plugin) |
| `U-INV-WEDM-05` | WEDMCost + WEDMOptimize + 4-corner geometry | "Best-cost WEDM strategy picker" (per-job) |
| `U-INV-WEDM-06` | WEDMTribal + MITCourseDB + JMDieArchive | "WEDM learn-as-you-program" tutor (training subscription) |

#### Cross-process inventions (6 units)

| Unit | Combo | New product |
|---|---|---|
| `U-INV-CROSS-01` | TribalKnowledge + LLM + ShopFloorQuery | "Ask the Shop" $/seat — natural-language Q&A over tribal tips + shop-floor data |
| `U-INV-CROSS-02` | SetupSheetGen + TravelerEngine + QualityCheck | "Job traveler auto-gen" — print packet for the operator (per-job) |
| `U-INV-CROSS-03` | FirstPartRight + DFMCheck + ToleranceStack | "First-part-right wizard" — guides shop through proving (per-job, high margin) |
| `U-INV-CROSS-04` | QuoteToShip + FirstPartRight + Traveler | "Quote-to-ship pipeline" SaaS — end-to-end job mgmt for sub-shops (per-shop subscription) |
| `U-INV-CROSS-05` | CADExtract + DFMCheck + CostOptimize | "Drag-CAD-get-quote" widget — embeddable iframe for vendor sites (revenue share) |
| `U-INV-CROSS-06` | InjectionMoldQuote + DFM + MaterialDB | "Plastic-quote" parallel product (separate revenue stream) |

#### Shop-floor SaaS inventions (8 units)

| Unit | Combo | New product |
|---|---|---|
| `U-INV-SHOP-01` | MachineCheck + MachineEnrich + MachineROI | "Machine ROI dashboard" — per-machine $/hr (subscription) |
| `U-INV-SHOP-02` | OEE-Dashboards + TelemetryStream + AlarmBus | "Real-time OEE alerts" — text/email when downtime spikes (shop-tier) |
| `U-INV-SHOP-03` | CycleTimeCrush + ProgramAudit + SPC | "Continuous improvement copilot" — flags cycle drift over time |
| `U-INV-SHOP-04` | CMMParse + GDNTCheck + ToleranceStack | "Auto-FAI generator" — drop CMM XML, get FAI form (per-FAI fee) |
| `U-INV-SHOP-05` | InfraEvents + InfraJobs + JobsTracker | "Shop event bus" — operator phone notifications (per-seat) |
| `U-INV-SHOP-06` | ResourceCensus + InventoryAgent + ReorderRules | "Auto-reorder consumables" — Amazon Business integration (rev share) |
| `U-INV-SHOP-07` | ShopFloorQuery + TribalDistill + GeneralizeRule | "Tribal-to-doctrine" — converts operator tips into shop SOPs (premium) |
| `U-INV-SHOP-08` | ShopSchedule + CapacityPlan + Bid-to-Win | "Smart scheduling" — accepts/rejects jobs based on capacity + margin |

#### Knowledge-as-product inventions (4 units)

| Unit | Combo | New product |
|---|---|---|
| `U-INV-KNOW-01` | Wiki + ObsidianMemoryRAG + ai-reason | "Ask PRISM" — chatbot trained on PRISM wiki + 4,245 tribal tips (free tier → trial bait) |
| `U-INV-KNOW-02` | VideoLearn + YouTubeTranscript + PDFLearn | "Auto-curate manufacturing training library" (training subscription) |
| `U-INV-KNOW-03` | TribalDistill + Generalize + WikiSync | "Doctrine engine" — auto-extracts doctrine from operator chat / shop floor (per-shop) |
| `U-INV-KNOW-04` | MITCourseDB + ShopFloorQuery + Ask-Shop | "Manufacturing CE credits" — pay-for-learning with shop integration |

#### CAD-side inventions (3 units)

| Unit | Combo | New product |
|---|---|---|
| `U-INV-CAD-01` | CADFromText + CADFromBlueprint + DFMCheck | "Text-to-CAD-to-Quote" — natural language → STEP → cost (revenue: per-design) |
| `U-INV-CAD-02` | AGI-CAD-Generate + FusionGenerate | "Generative CAD-for-CAM" — describe part, get manufacturable STEP (premium) |
| `U-INV-CAD-03` | CADRag + CADExtract + AGI-CAD | "CAD-aware quote refinement" — chat with the part (premium) |

**Tested combinations span 7 axes** (material × process × machine × controller × volume × tolerance class × shop tier) → ≥3 per axis tested.

### REVENUE-MS3 — Wire backlog harvest (≈18 units)

**Why:** 875 unwired engines = capabilities customers can't reach. Most are revenue-relevant.

**Depends on:** MS0/MS1 (UI to put them in). **Blocks:** feature parity with competitor CAM vendors.

Units batched by domain (top-N per BUILD_STATE.top_unwired_domains):

| Unit | Batch | Count | Target dispatcher |
|---|---|---|---|
| `U-WIRE-OTHER-01..03` | 3 batches × 47 = 141 of 142 "Other" | 141 | route by inferred domain → existing dispatchers |
| `U-WIRE-LATHE-01..03` | 3 batches × 30 = 90 of 89 | 89 | `prism_turning` |
| `U-WIRE-MACHINE-01..02` | 2 batches × 9 = 18 of 17 | 17 | `prism_machine` |
| `U-WIRE-MULTI-01` | 12 of 12 | 12 | route by sub-domain |
| `U-WIRE-TURNING-01` | 11 of 11 | 11 | `prism_turning` |
| `U-WIRE-REMAINING-01..07` | 7 batches × 90 = 630 across remaining domains | 605 | route per audit-suggested-dispatcher |

Wiring acceptance per unit (CLAUDE.md ENGINE WIRING law):
- Import in dispatcher
- Action-enum entry
- Schema
- Lazy-import resolution
- Engine-named test file with ≥1 round-trip E2E case
- Round-trip assertion through dispatcher, not engine singleton

### REVENUE-MS4 — Drift reconciliation (≈10 units)

**Why:** 613 milestones claim "complete" while git shows "not_started_real". Some are revenue-blocking false-positives.

**Depends on:** MS0/MS1. **Blocks:** trust in own roadmap.

| Unit | What |
|---|---|
| `U-DRIFT-01` | Run `envelope-sync` skill, group drift by revenue-relevance |
| `U-DRIFT-02` | Auto-flip envelopes where git shows real shipping (false-negative drift) |
| `U-DRIFT-03..09` | Per-batch reconcile of 7 revenue-relevant milestone clusters: SFC, Master Post, CAM bridges (×6) |
| `U-DRIFT-10` | Final audit + `state/shared/MILESTONE_PROGRESS.json` regen |

---

## Algorithm / formula / equation combinatorics (what new math emerges from node combinations)

Per the directive, each combination produces NEW algorithmic behavior. Surfaced here so the algorithm-design phase is auditable:

| Combination | Emergent algorithm / equation | Customer value |
|---|---|---|
| Kienzle (Fc = kc · A · (1−m_c) · ...) + Taylor (V · T^n = C) | **Optimal V for tool-life economy** — `V* = (C / ((1-n)·T_life))^(1/n)` with kc constraint check | "Set the V that respects both force budget AND tool-life floor" |
| Deflection (δ = F·L³/(3EI)) + Kienzle | **Force-aware step-down recommender** — solve δ ≤ tolerance for ap given F = kc·b·ap·fz | "Don't take a cut that bends the tool past spec" |
| SLD (chatter stability lobe map) + Spindle RPM range | **Lobe-pick algorithm** — choose RPM at peak of nearest stable lobe within spindle range | "Stable cutting at near-max material removal" |
| Thermal growth (linear coef α·ΔT·L) + Probe routine | **Auto-offset equation** — compensation = α · (T_now − T_probe) · L_axis | "First-piece-after-warmup compensation" |
| Taylor + Adaptive Feed | **Real-time V adjuster** — V(t) = V_init · (T_remaining / T_initial)^n | "Slow down as the tool wears" |
| Johnson-Cook constitutive + thermal | **High-speed cutting force prediction** at strain-rate >10^4 | "Hard-turning force at HSM speeds" |
| Bayesian wear + telemetry | **Posterior tool-life estimate** — combine prior Taylor with live observed wear | "Tool-life prediction that learns from your machine" |
| Spindle harmonic + chatter | **Resonance-avoidance feed schedule** — auto-shift fz to dodge harmonics | "No-chatter-in-pocket" auto-feed |
| Polynomial cycle-time model + program-optimize | **Cycle-time-bounded G-code search** — search G-code rewrites that monotonically reduce predicted Tc | "Cycle Time Crush" search loop |
| Conformal prediction + adaptive-feed | **Calibrated feed-confidence interval** — feed = mean ± α-conformal | "Feed with risk band, not point estimate" |
| Z3 solver + tolerance stack | **Solvable tolerance allocation** — given assembly Σ ≤ T, find per-feature t_i that maximizes manufacturability | "Auto-tighten only what matters" |
| MIP / LP + capacity-plan + bid-to-win | **Job acceptance optimizer** — accept/reject set maximizes margin under machine-hour constraint | "Smart shop scheduling" |
| Gaussian process + thermal + drift | **Long-horizon thermal drift correction** | "Sub-micron all-day work" |

(13 surfaced; more emerge per combination in MS2.)

---

## Node wiring / bridging plan

Per the directive ("all potential node wiring and bridging of nodes"), every UI page in MS0 wires to ≥1 dispatcher action, every dispatcher action wires to ≥1 engine. Bridges (where two engines that should compose but don't yet) are explicit units in MS3.

**Identified bridges** (from `audit-overlay.json` blue=19 + audit conflict findings=8):
- 19 intra-domain pairs sharing a hub but not directly connected — every one becomes a bridge-build unit in MS3
- 8 cross-engine conflicts (same-name / overlapping-intent) — every one becomes a canonicalize-or-rename unit in MS4

**Bridging variability axes:**
- Bridge by direct edge (cheapest)
- Bridge by adapter engine (when interfaces differ)
- Bridge by event bus (`tr.ws` or `infra-events`) when async
- Bridge by shared cache (when stateful)
- Bridge by hook injection (when cross-cutting concern)

Each MS3 wiring unit picks one of these 5 strategies based on the inferred coupling.

---

## Variability axis (problem-solving inputs × states × failure modes × adversarial)

Required by comprehensive-build enforcement. The roadmap MUST cover:

**Inputs:**
- 6 ISO material groups (P, M, K, N, S, H) × 5 hardness bands × 2 coolant regimes = 60 cutting conditions
- 5 controller dialects (Fanuc, Siemens, Haas, Mazak, Mitsubishi/Sodick/Makino/AgieCharmilles/Fanuc-WEDM) × per-controller G/M-code variants
- 4 spindle taper systems (HSK, BT/CAT, ER, polygon)
- 6 CAM systems (the tier-1 list)
- N machine envelopes (encoded in `ShopConfigurationEngine`'s 21-machine fleet)

**States:**
- Cold / warm / hot machine
- New / mid-life / end-of-life tool
- Empty / partial / full work envelope
- Operator: trainee / journeyman / master
- Subscription: trial / paid / lapsed
- Job: first-article / production / lights-out

**Failure modes (must be tested):**
- Bad input (wrong units, missing field, malformed file)
- Boundary (zero, NaN, Infinity, oversize, max-int)
- Resource exhaustion (OOM, disk full, network down)
- Permission denied
- Race condition (multi-chat concurrent)

**Adversarial:**
- Prompt injection in user-supplied text fields
- File-upload attacks (zip bomb, malicious STEP, exploit PDF)
- Subscription bypass attempts (token forgery, replay)
- Rate-limit evasion

Every unit ships with tests covering ≥3 of each axis (per CLAUDE.md comprehensive-build rule).

---

## What is NOT in scope (peer-chat ownership)

The other concurrent chat owns these — **do not duplicate**:

- Development tools (the `forge*`, `rgs*`, `audit-*` script families themselves — meta-layer)
- Backend systems infra (`mcp-server/src/transport`, `dispatcher` plumbing changes)
- AI systems (`PRISMSelfAwarenessEngine`, `AISystemRouter`, `FullSystemAICoordinator` orchestration changes)
- Infrastructure (Docker, Redis, BullMQ, Postgres schemas)
- Neural networking (`CrossProcessNeuralLearningEngine` family, conformal prediction core)
- Back-end system-viz internals (`scripts/generate-system-viz.mjs`, `scripts/merge-augmentations.mjs`, the visualization layer)
- Hook bundle rewrite (`.claude/hooks/bundles/` — claude-7b9d1810 active)
- Wiki internals (`WikiIndexMaintainerEngine`, `wiki-bootstrap`)

If a unit in this roadmap touches one of these surfaces, it becomes a **bridge coordination** unit — file a request to the peer chat via `state/shared/AGENT_CHAT.md` and proceed only after the peer ships.

---

## Execution discipline

Per CLAUDE.md doctrine and previous session's hard-won lessons (lint-staged cascade, multi-chat index race, peer-claim respect, atomic commit pattern):

1. **Atomic `git add <file> && git commit -m ...` in ONE bash invocation** (multi-chat index poisoning if separated).
2. **Respect peer claims** — never edit files in `state/shared/AGENT_CHAT.md` active-claims block.
3. **Use `forge-audit-omniscient` before each milestone** to refresh AUDIT-LATEST.json + audit-overlay.json; new findings update the roadmap.
4. **Use `audit-to-units.mjs`** (already shipped commit `87a7c70d5`) to convert audit findings into roadmap unit additions.
5. **Wire every engine via duplicationGuardEngine.mustCheckBeforeCreating** — no new engine if a similar exists (use it, extend it, rename it — don't duplicate).
6. **Test span ≥3 configs per variability axis** — never test only the canonical default.
7. **Every commit format `[CAD-FUSION-LIVE-MS0]/U-REV-…: title`** (matches per-agent handoff topic).

---

## First-week kickoff (priority order)

Day 1: `REVENUE-MS0` U-REV-CAD-SFC-01..06 (Speed/Feed Calculator UI — biggest single revenue lift)
Day 2-3: `REVENUE-MS0` U-REV-MILL-01..05 + U-REV-LATHE-01..07
Day 4: `REVENUE-MS0` U-REV-WEDM-01..05 + U-REV-QUALITY-01..04
Day 5: `REVENUE-MS0` U-REV-ADMIN-01..07 (subscription gate prerequisite)
Day 6-7: `REVENUE-MS1` U-SUB-01..04 (Stripe + license validator + tier middleware)
Week 2+: U-SUB-05..18 → MS2 inventions in customer-pull priority order

---

## How this roadmap regenerates itself

Every milestone close runs:
1. `forge-audit-omniscient.mjs` → refreshes AUDIT-LATEST.json
2. `audit-to-units.mjs` → emits new unit candidates into `mcp-server/data/milestones/MS-AUDIT-DERIVED-<date>.json`
3. Operator merges candidate units into this roadmap's MS where they belong
4. Drift check via `envelope-sync` skill
5. System-viz regen via `scripts/generate-system-viz.mjs` (now ~12s post-round-4 skip)

This document is THE source of truth for the revenue lane. The peer chat's roadmap is THE source for the dev-tools/infra lane. Conflicts in shared files resolved by chat-bus post + `state/shared/handoffs/` per-agent files.

---

## v7 UPGRADE LAYER (Boris doctrine compliance — appended 2026-05-10)

This section upgrades the roadmap to `/forge7` + `/forge-audit-v2` compliance: per-unit verification channel (HARD GATE), expanded node-combinatorics with AI-synergy plan, peer-Claude plan review, META artifact, HTML companion, `/loop` self-schedule. Existing sections above remain canonical; this layer **adds** required gates.

### v7.A — Per-unit verification channel declaration (HARD GATE)

Every unit in MS0–MS4 declares a re-runnable verification feedback channel. Templates by unit class:

| Unit class | `verifies_via.tool` | `expected_signal` | `re_run_cost` |
|---|---|---|---|
| Frontend page (MS0) | `rtk vitest run -- <page>.test.ts && playwright test <page>.spec.ts` | All happy + 3 failure-modes + 2 adversarial pass | ~30s |
| Subscription engine (MS1) | `rtk vitest run -- <engine>.test.ts && stripe webhook --test <event>` | Webhook → dispatcher round-trip green | ~15s |
| Combinatoric invention (MS2) | `rtk vitest run -- <new-engine>.test.ts && node scripts/spans-config-matrix.mjs --engine <name> --axes 7` | ≥3 configs per axis pass; emergent algorithm output stable across configs | ~45s |
| Wiring batch (MS3) | `rtk vitest run -- <engine>.test.ts && node scripts/check-engine-wired.mjs --engine <name>` | Engine listed by `system-viz-query.mjs find` AND has dispatcher action enum entry | ~10s |
| Drift unit (MS4) | `node scripts/build-milestone-progress.mjs && rtk git log --oneline -- <milestone-path>` | Envelope `status` matches git evidence | ~5s |
| Bridge coordination unit | `node scripts/system-viz-query.mjs verify-bridge --from <A> --to <B>` | Edge exists in graph; both endpoints have action wired | ~8s |
| Customer-flow E2E (MS0+MS1) | `playwright test --grep <user-story>` | Signup → trial → quote → pay → first-output path green | ~2m |

**No unit may ship without a declared `verifies_via` block.** Stop hook `scrutinize-before-stop` is the enforcement surface; consensus 3-of-3 already gates closure.

### v7.B — Expanded node-combinatorics (50 emergent algorithms / formulas)

Pre-v7, the spec listed 13. v7 enumerates every revenue-scope node-pair where connection produces emergent algorithmic behavior. Format: `Pair → Equation/Algorithm → Variability axis covered → AI orchestration plan`.

**AI orchestration tiers per combo (per user directive):**

- **T1 (Claude orchestration):** synthesizes the combo, resolves ambiguity, owns final go/no-go
- **T2 (deep reasoning):** `prism_ai:ai_milling_deep_reason` / `ai_wedm_deep_logic` / `cot_reason_tree` — multi-step physical inference
- **T3 (deep learning):** LoRA adapters (`mill_lora_predict`, `lathe_lora_pipeline`, `wedm_lora_*`, `cam_lora_*`) — learned residuals on top of physics
- **T4 (neural networks):** `xproc_neural_train/predict`, conformal prediction, ensemble fusion — calibrated uncertainty
- **T5 (AI systems):** routing (`aiSystemRouterEngine`), federated learn, transfer scaling, prototypical regression
- **T6 (symbolic AI):** Z3 / constraint solver / MIP — formal allocation
- **T7 (genome / KG):** `prism_intelligence:graph_*`, `prismCreativeReasoningEngine.explore` — cross-domain analogy

**Combinatorics table (50 pairs):**

| # | Pair | Equation / Algorithm | Variability axis covered | AI synergy plan |
|---|---|---|---|---|
| 1 | Kienzle × Taylor | `V* = (C / ((1-n)·T))^(1/n)`, kc-constraint | material × tool-life | T1 chooses obj, T2 reasons constraint, T4 conformal V CI |
| 2 | Deflection × Kienzle | δ ≤ tol → ap = (3EI·tol / (L³·kc·b·fz))^solve | tool overhang × stiffness | T2 inferences, T4 calibration |
| 3 | SLD × Spindle RPM | Lobe peak picker on stability map | spindle range × tool | T3 LoRA learns chatter offset per machine |
| 4 | Thermal-growth × Probe | compensation(t) = α(T_now − T_probe)·L_axis | warmup state | T7 GP long-horizon |
| 5 | Taylor × Adaptive-feed | V(t) = V₀(T_left/T₀)^n | tool wear | T3 wear-rate LoRA |
| 6 | Johnson-Cook × Thermal | Force at HSM (Ė̇ > 10⁴) | strain-rate | T2 deep-reason, T4 epistemic UQ |
| 7 | Bayesian-wear × Telemetry | Posterior MTBF / Weibull update | machine-specific | T3 per-machine LoRA + T4 prior |
| 8 | Spindle-harmonic × Chatter | fz shift schedule | RPM band | T2 + T4 chatter classifier |
| 9 | CycleTime-poly × ProgramOpt | Local search G-code rewrites minimizing T̂c | program shape | T7 KG of past optimizations |
| 10 | Conformal × AdaptiveFeed | feed = μ̂ ± α-conformal | input variability | T4 native |
| 11 | Z3 × ToleranceStack | Min-cost t_i s.t. Σ ≤ T | drawing tolerance | T6 |
| 12 | MIP × Capacity × Bid | argmax Σ(margin·accept) s.t. hr ≤ cap | shop load | T6 + T1 ratify |
| 13 | GP × Thermal × Drift | Sub-µ kriging correction | shift-of-day | T7 GP |
| 14 | Chuck-force × Part-deflect | F_min = part-stiffness · clamp-deflect-spec | workholding | T2 |
| 15 | Material-DB × Coolant-DB | Coolant-fit recommender via Pareto over (cost, life, finish) | coolant regime | T5 Pareto |
| 16 | Tool-Catalog × Toolholder-FRF | Assembly-FRF predictor | tool stack | T3 RCSA LoRA |
| 17 | DFM × Tolerance × CAD-extract | Quote-from-STEP cost equation | feature mix | T7 KG + T6 |
| 18 | Wear-progression × Energy-eff | Energy-per-part minimizer | sustainability | T4 |
| 19 | Hard-turn × CBN × ResidualStress | Replace-grinding decision rule | finish/Rt class | T2 |
| 20 | Chip-control × Chipbreaker-DB | Chip-evac probability score | groove geometry | T3 LoRA |
| 21 | Threading-table × Tap-drill × Std-DB | Universal threading wizard logic | thread standard | T6 enum + T2 |
| 22 | Mill-turn sync × B-axis × Sub-spindle | Collision-free post equation set | machine kinematics | T2 + T6 |
| 23 | Taylor-eco × Bar-stock-util | Cost-per-part 3-mode optimizer (min-c/max-prod/max-profit) | objective mode | T6 LP |
| 24 | LATHE-AGI × JMDie-archive | Print-to-program neural draft | print shape | T3 + T7 |
| 25 | WEDM-cost × Quote × Customer-history | Auto-bid equation w/ win-rate prior | customer | T7 + T4 calibration |
| 26 | WEDM-feasibility × Conductivity × Geometry | Pre-quote go/no-go | material | T2 |
| 27 | WEDM-safety × WEDM-hooks × WEDM-PCD | Lights-out readiness score | risk class | T2 + T4 |
| 28 | WEDM-program × 5 dialects | Dialect-translator equation set | controller | T6 + T3 dialect-LoRA |
| 29 | WEDM-tribal × MIT-DB × JM-archive | Learn-as-program tutor curriculum | learner level | T7 KG |
| 30 | Tribal × LLM × Shop-floor-query | Ask-the-Shop RAG retrieval | tribal corpus | T5 routing + T4 generation |
| 31 | SetupSheet × Traveler × QA | Auto-traveler emit pipeline | job class | T1 orchestration |
| 32 | FirstPartRight × DFM × ToleranceStack | First-part-right scoring fn | tolerance band | T6 + T2 |
| 33 | Quote-to-ship × Traveler × FAI | E2E job-state machine | job phase | T1 |
| 34 | CAD-extract × DFM × Cost | Drag-CAD widget equation | uploaded part | T2 + T6 |
| 35 | InjectionMold × DFM × Material | Plastic-quote equation | mold geometry | T2 |
| 36 | Machine-check × ROI × Telemetry | Per-machine $/hr | shop config | T4 |
| 37 | OEE × Telemetry × Alarm-bus | Real-time OEE alert thresholds | shift | T4 anomaly |
| 38 | CycleTimeCrush × Program-audit × SPC | Cycle drift detector | program history | T4 + T7 |
| 39 | CMM-parse × GDT × Tol-stack | Auto-FAI form populator | drawing | T6 + T2 |
| 40 | Infra-events × Infra-jobs × Tracker | Event-bus pub/sub for ops | event volume | T5 |
| 41 | Resource-census × Inventory × Reorder | EOQ-driven auto-reorder | usage rate | T6 + T4 |
| 42 | Shop-floor-query × Tribal × Generalize | Tribal-to-doctrine extractor | conversation | T2 |
| 43 | Shop-schedule × Capacity × Bid-to-win | Smart job acceptance | shop fleet | T6 |
| 44 | Wiki × Obsidian-RAG × Reason | Ask-PRISM chatbot pipeline | query type | T5 route + T4 gen |
| 45 | VideoLearn × YT-transcript × PDF-learn | Auto-curate training library | content source | T5 |
| 46 | Tribal-distill × Generalize × Wiki-sync | Doctrine engine emit | shop history | T2 + T7 |
| 47 | MIT-courses × Shop-query × Ask-shop | CE-credit grade engine | learning track | T7 |
| 48 | CAD-text × CAD-blueprint × DFM | Text-to-CAD-to-Quote pipeline | NL prompt | T1 + T7 |
| 49 | AGI-CAD × Fusion-generate | Generative CAD for CAM pipeline | part class | T7 |
| 50 | CAD-RAG × CAD-extract × AGI-CAD | Chat-with-the-part Q&A | CAD memory | T5 + T7 |

**Variability coverage matrix:** every algorithm above must be evaluated across the 7 axes (material × process × machine × controller × volume × tolerance × shop-tier). Default test plan is ≥3 spanning configs per axis per algorithm = ≥21 test cases per emergent algorithm.

### v7.C — AI orchestration plan (Claude as Tier-1)

Each combinatoric algorithm follows this orchestration template:

```
Claude (T1) — receive request, parse intent, select algorithm row from combinatorics table
  ↓ delegates physical inference
T2 deep-reason — physical/causal step-chain (e.g. ai_milling_deep_reason)
  ↓ optionally calls
T3 LoRA — learned residual correction (e.g. mill_lora_predict)
  ↓ optionally augments
T4 neural / conformal — calibrated uncertainty interval (xproc_conformal_set)
  ↓ orthogonally
T5 AI router — picks the best backend (aiSystemRouterEngine.route)
  ↓ orthogonally
T6 symbolic — formal solver (Z3 for tolerance / MIP for scheduling / LP for cost)
  ↓ orthogonally
T7 KG / creative — analogy / cross-domain transfer (creative_solve / graph_infer / find_similar)
  ↓
Claude (T1) — synthesize, apply safety/omega gate, emit answer + verification channel for re-run
```

Where a combination touches **shop-floor execution**, append:

```
Safety oracle (S(x) ≥ 0.70) blocks emission if score below floor.
Omega (Ω ≥ 0.70) blocks emission if quality below floor (R, C, P, S, L).
```

Every MS2 unit declares which tiers it invokes (e.g. `tiers_invoked: [T1, T2, T4]`).

### v7.D — Plan peer-review (forge7 HARD GATE)

Before MS0 starts, dispatch:

```js
Agent({
  subagent_type: 'reviewer',
  description: 'Staff-engineer review of REVENUE-ROADMAP-2026-05-10.md',
  isolation: 'worktree',
  prompt: `Read state/shared/specs/REVENUE-ROADMAP-2026-05-10.md.
           Audit as staff engineer:
             1. Are unit dependencies correct? (MS1 depends on MS0 ADMIN — verify)
             2. Are verification channels real (not aspirational)? Spot-check 10.
             3. Is any unit a duplicate of existing assets? Run /dedup checks.
             4. Are the 50 combinatorics in v7.B sound? Flag any that violate physics or have no customer pull.
             5. Are the v7.A verification gates re-runnable in <5min total? Compute.
             6. Score each milestone 1-5 on (revenue clarity, technical risk, peer-coordination cost).
             7. Identify ≥1 missing combinatoric the spec didn't enumerate.
           Return PASS / FAIL with specific blockers.
           Reject overstatements.`
})
```

Block MS0 kickoff until reviewer PASS.

### v7.E — META artifact (compounding-gains tax)

Spec-bound deliverable: ship `scripts/revenue-readiness-score.mjs` BEFORE MS0 starts. Behavior:

```
Inputs read:
  - state/shared/BUILD_STATE.json
  - state/shared/AUDIT-LATEST.json
  - state/shared/system-viz/system-graph.json
  - mcp-server/data/milestones/MS-REV-*.json

Outputs (stdout JSON + state/shared/REVENUE-READINESS.{md,json}):
  {
    "scores": {
      "ms0_ui_unstub_pct": <float 0..1>,
      "ms1_subscription_pct": <float>,
      "ms2_inventions_pct": <float>,
      "ms3_wiring_pct": <float>,
      "ms4_drift_pct": <float>,
      "overall_revenue_readiness": <float>
    },
    "blockers": [<unit-id>: <reason>],
    "next_unit_recommended": <unit-id>,
    "verification_health": <pct units with passing verifies_via>,
    "tier_invocation_balance": { "T1": <count>, ..., "T7": <count> }
  }
```

This script is re-runnable; future audits compound from it. Without it, this roadmap rots in 30 days.

### v7.F — HTML companion (Thariq pattern)

`scripts/emit-revenue-roadmap-html.mjs` emits `state/shared/specs/REVENUE-ROADMAP-2026-05-10.html` containing:

- SVG dependency graph (MS0 → MS1 → MS2/MS3 → MS4) with color-coded unit nodes
- Color-coded combinatorics table (green=already-built, yellow=combinable-now, red=blocked, blue=needs-bridge)
- Per-unit verification-channel card with "Run" button (emits the rtk command)
- Tier-invocation pie chart for MS2 (T1..T7 distribution)
- Synergy delta chart (pre-MS0 baseline → projected post-MS0/MS1/MS2)
- Copy-to-prompt buttons for each unit (Thariq interactivity)

### v7.G — `/loop` self-schedule (cyrilXBT recurring)

Schedule a re-audit + readiness-score refresh every 7 days for 4 cycles (~28 days), then user re-evaluates the roadmap:

```bash
echo "/forge-audit-v2 revenue-readiness — refresh REVENUE-READINESS.json, regenerate REVENUE-ROADMAP HTML, flag any drift, append to CLAUDE.md §Recent regressions if found" \
  | /loop --interval 7d --max 4
```

If a regression is detected during a re-run, the doctrine back-flows to `H:/prism/CLAUDE.md`:

```markdown
## Recent regressions
- 2026-05-<X> | <regression-one-liner> | fix: <action> | observed-by: claude-99eca613 /forge-audit-v2 (revenue lane)
```

### v7.H — End-state criteria (v7 PASS)

```
REVENUE ROADMAP v7-COMPLIANT
=============================
Milestones:                   5 (MS0..MS4)
Total units:                  ~128
v7.A verification channels:   ALL units declare verifies_via
v7.B combinatorics:           50 (vs pre-v7: 13)
v7.C AI orchestration:        every MS2 unit declares tiers_invoked
v7.D peer-review:             [PASS pending dispatch]
v7.E META artifact:           scripts/revenue-readiness-score.mjs [pending build]
v7.F HTML companion:          [pending build]
v7.G /loop schedule:          [pending registration]
Synergy delta projection:     pre 21.1% → post 33% (+12pp from UI unstub alone)
Compounding-gains:            re-runnable readiness score + HTML + loop

Next: dispatch v7.D reviewer | build v7.E + v7.F | register v7.G | begin MS0 U-REV-CAD-SFC-01
```

---

## How to consume this v7 layer

If you are a chat picking up this roadmap:
1. Read sections 0–11 above (original spec).
2. Read v7.A–v7.H.
3. Run `node scripts/revenue-readiness-score.mjs` once v7.E ships (it tells you the next unit).
4. Honor v7.A verification gate on every unit you build.
5. If you discover a 51st combinatoric, add a row to v7.B and bump the count in v7.H.

