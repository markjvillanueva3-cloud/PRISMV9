# DARK-FACADE INDIA FIXLIST — 2026-06-23

> READ-ONLY verification of the 13 india-owned dark-facade dispatcher actions
> (aiReasoningDispatcher.ts ×4 + orchestrationDispatcher.ts ×9) from
> `state/shared/specs/DARK-FACADE-AUDIT-2026-06-23.json`. Every verdict below was
> derived by reading the REAL engine body (never guessing a method name) plus its
> immediate callees. Ordered clean-first (india's ready-to-fix queue at the top).

**Counts by verdict:** clean = 4 · ambiguous = 4 · no_real_method = 4 · false_positive = 1 (total 13)

| dispatcher | action | verdict | realMethod | callStyle | crashGuardFields | note |
|---|---|---|---|---|---|---|
| aiReasoningDispatcher.ts | uncertainty_pipeline_run | clean | `propagate` | instance-singleton | `uncertain_params[]`, `stages[]` | Facade probes run/execute/process; real entry is `uncertaintyPropagationPipelineEngine.propagate(input: PipelineInput)`. 1:1 rewire `…propagate(p)`. Reads `uncertain_params[].mean/.std_dev/.name` + `stages[].model` in every path → schema must require both arrays non-empty (empty stages → `stageOutputs[len-1]` undefined deref). |
| aiReasoningDispatcher.ts | cross_domain_orchestrate | clean | `planJob` | static-class | `features[]` (each `{id,type}`) | Static-class singleton (`crossDomainOrchestratorEngine = CrossDomainOrchestratorEngine`); facade probes orchestrate/plan/execute → miss. Rewire `CrossDomainOrchestratorEngine.planJob(p)`. `planJob` Zod-parses `OrchestrationInputSchema` (`features.min(1)`, each feature `{id,type}` required) so a missing/empty `features` THROWS — schema must require it (the engine throw becomes the dispatcher error). |
| orchestrationDispatcher.ts | foresight_orchestrate | clean | `reportFor` | instance-singleton | `description` (non-empty string) | Facade probes foresee/run/analyze → miss; real async entry `foresightOrchestratorEngine.reportFor(input)`. Rewire `await …reportFor(p)`. `validate()` throws on missing/empty `description`; sub-engine calls are `safeCall`-wrapped so only `description` is load-bearing. |
| orchestrationDispatcher.ts | wet_run_pilot_orchestrate | clean | `pilotPromotionReadiness` | positional-args | `pilot_id` (string), `nowTs` (finite number) | Facade probes run/orchestrate/execute → miss. Real authoritative gate is `wetRunPilotOrchestratorEngine.pilotPromotionReadiness(pilotId, nowTs)` — POSITIONAL (not a params object). `validateString(pilot_id)` + `validateTs(nowTs)` THROW; schema must require `pilot_id` (non-empty) and a finite `nowTs` (default `Date.now()`), then call `…pilotPromotionReadiness(p.pilot_id, p.nowTs ?? Date.now())`. (Siblings `businessCostSummary`/`complianceAuditSnapshot` exist for other lanes.) |
| aiReasoningDispatcher.ts | mit_course_knowledge_query | ambiguous | `searchAlgorithms` \| `searchCourses` \| `getAlgorithmsForEngine` | instance-singleton | `query` (string) | No `query`/`search`/`getCourse` method exists. The intent "knowledge_query" maps to ≥3 real search surfaces — `searchAlgorithms(query,limit)`, `searchCourses(query,limit)`, `getAlgorithmsForEngine(name)`, `getStats()`. India must pick/route (likely a `type` discriminator: algorithm vs course) before a 1:1 rewire. All take a plain string; no deref-crash risk (defensive iteration), so the only guard is requiring a non-empty `query`. |
| orchestrationDispatcher.ts | sampling_plan_generate | ambiguous | `mil1916` \| `aoqlPlan` \| `ocCurve` | static-class | (per chosen method, see note) | `samplingPlanEngine` IS the class; facade `new SamplingPlanEngine().generate()` is doubly wrong (static methods, no `generate`). Two distinct plan generators — `SamplingPlanEngine.mil1916({lotSize,verificationLevel})` (MIL-STD-1916) vs `SamplingPlanEngine.aoqlPlan({lotSize,aoql})` (AOQL) — plus `ocCurve(n,c,N)`. India must disambiguate (e.g. `plan_type`). Each Zod-parses its own request (`lotSize` positive int required; `aoql` positive `<0.5`; `verificationLevel` enum) — those become the crash guards once a method is chosen. |
| orchestrationDispatcher.ts | roadmap_dag_build | ambiguous | `load` \| `topoSort` \| `loadFromMilestones` | instance-singleton | (none for disk `load`; `milestones[]` for in-mem) | No `build`/`compute`/`analyze`. `roadmapDAGEngine` exposes `load(opts)` (reads `mcp-server/data/roadmap-index.json` from disk — THROWS if file missing), `topoSort()` (THROWS on cycle), `stats()`, `loadFromMilestones(milestones[])`, `ancestors/descendants(id)`. "dag_build" most likely = `load()` then `stats()`/`topoSortPartial()`, but the exact deliverable is unclear → india decides. Disk `load` needs no input field; the in-memory path needs `milestones[]`. |
| orchestrationDispatcher.ts | smart_tool_select | ambiguous | `selectToolOrchestrated` | instance-singleton | `decision_point`, `material_iso_group`, `operation_type`, `machine_*`, feature dims | Facade probes select/run/recommend → miss; the ONE real method is `selectToolOrchestrated(req: OrchestratedToolRequest)`. Name-wise a clean 1:1, but flagged ambiguous because the call-tree fans into `smartToolSelectorEngine.select()` + `pipelineDecisionOrchestratorEngine.decide()` (both UNREAD here) whose required fields define the real crash surface — a strict schema needs `decision_point` + the full `SmartToolRequest` shape (`material_iso_group`, `operation_type`, machine power, feature `diameter/depth/width_mm`) verified against `SmartToolSelectorEngine.ts` before wiring. Verify those two engines, then this likely promotes to clean → `selectToolOrchestrated(p)`. |
| aiReasoningDispatcher.ts | machine_lora_base_info | no_real_method | null | n/a | — | `machineLoRABase` is NOT an engine instance — it's a plain factory object `{ buildDatasetHelper(opts), createCadence(initial,clock) }` (the real work lives in classes `BaseLoRADatasetBuilder`/`BaseLoRACadence`). No `getInfo`/`info` member exists, and there is no read-only "info" method to surface. Needs a BUILD: add a real `getInfo()`/`describe()` returning `{ DEFAULT_SPLIT, DEFAULT_CADENCE, helpers }`, OR retarget the action. Cannot be a 1:1 rewire. |
| orchestrationDispatcher.ts | operator_dashboard_orchestrate | no_real_method | null (`getStatus` is closest but contract-mismatched) | instance-singleton | `machine_id`, `current_rpm`, `current_feed`, `current_load_pct` | `operatorDashboardOrchestratorEngine` has `getStatus(input)`, `getAlerts(input)`, `getShiftSummary(input)` — NO `run`/`orchestrate`/`analyze`. "orchestrate" has no 1:1 match; `getStatus` is the closest but the action name implies a broader compose. India must decide the contract (probably route to `getStatus`) → then it becomes clean. As-is, no method named to the intent = needs a build/decision. If routed to `getStatus`, schema MUST require `machine_id` + numeric `current_rpm/current_feed/current_load_pct` (used unguarded in temp/chatter/wear math → NaN propagation otherwise). |
| orchestrationDispatcher.ts | rollback_plan_build | no_real_method | `planRollback` (positional, non-params-object) | positional-args | `unitId` (string), `steps[]` (BuildStep[]) | Real method `rollbackPlannerEngine.planRollback(unitId, steps)` exists but the facade's `(p)` single-object call is structurally wrong — it takes TWO positional args `(unitId: string, steps: BuildStep[])`, and `steps` are `BuildStep`s produced by `BuildPlannerEngine`/`AtomicStepDecomposerEngine`, not a free param object. `assertInputs` THROWS if `unitId` empty or `steps` not an array. Marked no_real_method for a clean params rewire: india must build the `{unitId, steps}`→positional adapter + decide where `steps[]` come from (likely chain off `build_plan_engine`). `planAndVerify` is the fuller entry. |
| orchestrationDispatcher.ts | print_corpus_orchestrate | no_real_method | `scan` (but ctor requires a writer + injected extractFn) | instance-singleton | `rootDir` (string), `extractFn` (function) | The facade `new PrintCorpusOrchestratorEngine()` is broken: the constructor REQUIRES a `PrintCorpusTableWriter` argument and there is NO singleton export + no zero-arg ctor → the instance is unusable. Real method async `scan(req)` needs `rootDir` + an injectable async `extractFn` (and a constructed writer). A param object cannot carry a live `extractFn`/writer, so this needs a BUILD: construct the writer, wire a default extract backend (BlueprintExtractionRAGEngine per the header), then expose `scan({rootDir,...})`. Not a 1:1 rewire. |
| orchestrationDispatcher.ts | tribal_explain | false_positive (partial) | `explainTipRelevance` | instance-singleton | `tipId` (string), `context` (object) | Heuristic probed `explain`/`run`/`generate` — none of those EXACT names exist, so the audit flagged it dark. But the engine DOES have the obviously-intended real method `tribalExplanationEngine.explainTipRelevance(tipId, context)` (plus `buildReasoningChain`, `predictUpcomingTips`, `synthesizeAgentPerspectives`). The facade is genuinely dark TODAY (probe miss) yet a real 1:1 target exists, so this is a heuristic near-miss (the `explain` prefix matched the method's INTENT not its name). Rewire `…explainTipRelevance(p.tip_id ?? p.tipId, p.context ?? {})` — POSITIONAL (tipId, context); `getTip` returns null safely for a missing tip (no crash), so the only guard is requiring `tip_id`. |

## India's ready-to-fix queue (CLEAN — 1:1 rewires)

1. **uncertainty_pipeline_run** → `uncertaintyPropagationPipelineEngine.propagate(p)` (instance-singleton) — guard `uncertain_params[]` + `stages[]` non-empty.
2. **cross_domain_orchestrate** → `CrossDomainOrchestratorEngine.planJob(p)` (static-class) — guard `features[]` (each `{id,type}`).
3. **foresight_orchestrate** → `await foresightOrchestratorEngine.reportFor(p)` (instance-singleton) — guard non-empty `description`.
4. **wet_run_pilot_orchestrate** → `wetRunPilotOrchestratorEngine.pilotPromotionReadiness(p.pilot_id, p.nowTs ?? Date.now())` (positional-args) — guard `pilot_id` + finite `nowTs`.

## Notes for the ambiguous/build tier

- **smart_tool_select** is one engine-read (SmartToolSelectorEngine.ts + PipelineDecisionOrchestratorEngine.ts) away from promoting to clean (`selectToolOrchestrated(p)`); its required-field list is the gating unknown.
- **mit_course_knowledge_query / sampling_plan_generate / roadmap_dag_build** all have real methods but the action intent maps to ≥2 surfaces → need a discriminator (`type`/`plan_type`) decision, not a build.
- **machine_lora_base_info / print_corpus_orchestrate** are genuine BUILDs (no callable info method; ctor needs an injected writer+extractFn respectively).
- **operator_dashboard_orchestrate** + **rollback_plan_build** have real methods whose CONTRACT (getStatus vs orchestrate; positional `(unitId,steps[])` from a planner) must be decided + adapted before wiring.
