# PRISM Master RGS Pipeline Roadmaps (2026-06-03)

Per-domain executable RGS roadmaps — atomic nodes/wirings/bridges + external-software training plans — synthesized from all 16 domain RGS passes, the master fleet plan, the priority-build plan, the roadmap-archaeology dormant-build catalog, the consolidated roadmap/task queue, and the per-slot session transcripts. Each domain unit names a real engine/dispatcher/bridge on disk (no invention), is ordered high-ROI-first, and carries an RGS toolchain + concrete acceptance gate. Operator priority order is preserved: **F0 foundations (sierra/india/golf) → SFC (oscar) + post (echo) → support (kilo/foxtrot/whiskey/mike/romeo) → quoting (charlie) → ERP (hotel) → JM-Die catch-up (alpha/delta/xray/lima)**. Foundations gate everything else — a stale system-viz graph blocks india's GNN holdout and fleet search; a dark MCP/Ollama substrate drops all 33 galaxies' reachability at once — so they lead. This doc is the optimal cross-domain path Hermes coordinates from.

## Fleet high-ROI execution order

Single ordered path across ALL domains. Foundations first (they gate consumers); producer-before-consumer within each tier (proven core before the integration that rides it).

| rank | slot | top unit | ROI | blocks / blocked-by |
|------|------|----------|-----|---------------------|
| 1 | sierra | U-VIZ-MERGE-STREAMING-OOM (kill exit-0xC0000409 streaming merge) | H | BLOCKS india GNN holdout + 7 FAST[] roosts + every slot's search; blocked-by: none (Tier-0 #1) |
| 2 | golf | U-MCP-RESTART-ACTUATOR (guarded MCP :3100-only auto-restart) | H | BLOCKS all 33 galaxies' prism_* reachability on a wedge; blocked-by: none (Tier-0 #2) |
| 3 | india | U-ROUTE-LADDER (purge hardcoded deepseek-r1:14b, capability-aware resolver) | H | BLOCKS MS2/MS5/MS6 routing; blocked-by: none (inference-only, no golf dep) |
| 4 | sierra | U-VIZ-FAST-REGISTER-9 (splice 9 OOM-blocked ghost-roost generators) | H | BLOCKS fleet roadmap roosts; blocked-by: #1 |
| 5 | sierra | U-VIZ-REFPOOL-SEED-EMBED (seed ref-pool + 768-d node embeddings) | H | BLOCKS india eval holdout + 8 galaxies' "proven"; blocked-by: #1 |
| 6 | india | U-GPU-EMBED-WORKER + parity assert | H | BLOCKS full-corpus embed; blocked-by: golf Python-GPU stack (degrade-fallback OK) |
| 7 | india | U-FULL-CORPUS-EMBED (resumable 302K-node sweep) | H | UNBLOCKS semantic search + GNN features; blocked-by: #5, #6 |
| 8 | golf | U-FD02..05 + U-FH02 (unified operator fleet-health dashboard) | H | feeds operator UX + india SVI; blocked-by: none |
| 9 | golf | U-REAP-TUNER (closed-loop false-positive-reap tuner) | H | drives false reaps→0; blocked-by: india outcome bus (#3 ladder) |
| 10 | oscar | U-SFC-COMBO-HARNESS (combinatorial SFC validation over calculator page) | H | gates echo post matrix (F/S per block); blocked-by: none |
| 11 | oscar | U-OSC9-CALIB-APPLY-WIRE (orphaned calib-apply keystone) | H | gates SFC calibrate moat; blocked-by: #10 |
| 12 | india | U-RABITQ-HNSW + U-RAG-ORCHESTRATOR (real semantic RAG) | H | replaces fleet String.includes; blocked-by: #7 |
| 13 | echo | U-POST-DARK-UNWIRE (14 post engines method?.() → real calls) | H | unblocks all post tiers; blocked-by: none |
| 14 | echo | U-POST-PROVE-MATRIX-5AX (5-axis-first post-proving harness) | H | saleable MasterPost proof; blocked-by: #13, oscar #10 (F/S), kilo toolpaths |
| 15 | romeo | U-WIRE-SPEED-6 (wire 6 Speed* engines → oscar SFC surface) | H | feeds oscar SFC matrix; blocked-by: none |
| 16 | foxtrot | U-P2P-CORPUS-LOADER (real .hmc/.nc corpus loader) | H | mill print-to-program e2e; blocked-by: none |
| 17 | echo | U-POST-PROVE-MATRIX-MILL-LATHE (3-axis→mill-turn→Swiss→lathe) | H | saleable bulk; blocked-by: #14, whiskey cut-physics |
| 18 | mike | U-WTW00 (fix 0-byte WEDMLoRADatasetBuilderEngine) | H | unblocks wedm_lora + self-improving half; blocked-by: none (producer-first) |
| 19 | echo | U-POST-WEDM-DIALECTS (5 WEDM dialect engines) | H | EDM pipeline 0.38→1.0; blocked-by: #13, mike physics |
| 20 | whiskey | U-LATHE-MATERIAL-INFER-FROM-MIN (break 41.6% floor) | H | gates lathe accuracy; blocked-by: none |
| 21 | kilo | U-CAMM-FUS-D1 (Fusion "Speed&Feed via PRISM" add-in) | H | Revenue Day-1; blocked-by: oscar calibrated SFC (#11) |
| 22 | romeo | U-WIRE-OTHER-21 (wire 21 "Other"-bucket engines) | H | 19% of unwiring backlog; blocked-by: none |
| 23 | mike | U-WTW-REG01 (edm-wires + edm-dielectrics registries) | H | biggest EDM reachability hole; blocked-by: #18 |
| 24 | charlie | U-QP-DATASRC-WIRE-3 (feed 3 unconsumed real data sources) | H | drives MAPE off 71.1% synth; blocked-by: hotel actuals (#27) |
| 25 | hotel | U-GAP-ERP-PERSISTENCE-PG (Postgres+pgvector durable state) | H | gates every populated ERP engine + audit trail; blocked-by: juliett DB coord |
| 26 | charlie | U-QP-QUOTE-UI (customer-facing quote app) | H | product-visible quoting; blocked-by: #24 |
| 27 | hotel | U-BRIDGE-ERP-QUOTE (cost-feedback loop → charlie) | H | unblocks charlie MAPE; blocked-by: #25 |
| 28 | alpha | U-BW-OFFLOAD-HOOK-WIRE (wire dead ollama-route-pretooluse.mjs) | H | offload 11.2%→30% fleet-wide; blocked-by: none |
| 29 | delta | U-GAP-WIRE-JMDIE-CORPUS (CAD coverage 33%→high) | H | feeds every downstream galaxy; blocked-by: none |
| 30 | xray | U-XRAY-DIM-ADAPTERS (3 real-candidate adapters) | H | dim-reconcile real-data; blocked-by: juliett index |
| 31 | lima | U-ACADEMY-SVI-SUBSYSTEM (register Courses SVI axis) | H | makes academy measurable; blocked-by: none |

**Parallelism note:** ranks 1-3 run concurrently (sierra OOM, golf MCP actuator, india route-ladder are independent foundations). Ranks 4-9 serialize behind #1 (graph) or #3 (outcome bus). Support/SFC/post (10-23) parallelize once #1 lands. Quoting/ERP (24-27) couple via the hotel→charlie actuals edge. JM-Die catch-up (28-31) is independent and can run anytime a slot is free.

## Per-domain RGS roadmaps

The 16 domain RGS passes, verbatim, in operator priority order: F0 foundations (sierra → india → golf) → P1 SFC+post (oscar → echo) → P1-support (kilo → foxtrot → whiskey → mike → romeo) → P2 quoting+token (charlie → alpha) → P3 ERP (hotel) → P4 JM-Die catch-up (delta → xray → lima).

---

## SIERRA — system-viz RGS PIPELINE ROADMAP (F0)
**North-star (1 line):** Always-fresh, always-trustworthy fleet brain map — one canonical ~244K-node graph that IS the fleet search/navigation substrate, regenerated OOM-free every time so `pendingCount=0` + `sidecarOk=true`.
**Time-allocation (from transcripts):** ON-TRACK but with one redirect: sierra's last 30d are squarely on-soul (find-cache reliability → nav-inject → cross-substrate typed edges, ~25 commits), EXCEPT it keeps absorbing romeo cross-lane orphans (`3f941f2885` JM mill handbooks) when its own queue shows `eligible 0/0`. Redirect: the merge-OOM (still failing 9h ago, exit 0xC0000409 on the `merge augmentations` stage) is Tier-0 #1 and blocks india's eval holdout + every slot's search — it must lead, not be deferred again behind cross-lane rescue work. Stop picking up romeo's DB-gen until the OOM + FAST-register-9 + ref-pool chain is closed.
**Pipeline units (HIGH-ROI ORDERED):**
1. **U-VIZ-MERGE-STREAMING-OOM: streaming/chunked merge-augmentations to kill exit-0xC0000409** | ROI=H — Tier-0 #1; the 24GB heap bump (`f87b3810ce`) only raised the ceiling, the V8 ~512MB single-string `JSON.stringify`/external-alloc cap on the 575MB graph still SIGKILLs the `merge augmentations` stage (live failure 2026-06-03T16:55); blocks india's GNN holdout + 7 FAST[] roosts + every regen | node=`merge-augmentations.mjs` (rewrite to stream-read base graph + append-splice via `graph-io.mjs` chunked writer, never materialize one 575MB string) + harden `regen-viz-merge-guard.mjs` | wiring=none (build-script; surfaced via `prism_dev` regen status) | bridge=UNBLOCKS india `nn-graph-eval.buildHoldout` (NN/GNN leg) | rgs-toolchain=`/wire-unwired`→`code-analyzer` + `wiring-review-agent` (per rgs rules: `dispatcher|wiring|orphan` → wiring-review-agent) | acceptance=10 consecutive `regen-viz` runs write `.last-successful-regen.json {ok:true, pendingCount:0, sidecarOk:true}` with peak RSS <24GB; `.last-regen-failure.json` not touched; node-count within ±1% of prior good graph.
2. **U-VIZ-FAST-REGISTER-9: splice the 9 OOM-blocked ghost-roost generators into FAST[]** | ROI=H — 9 of 49 `generate-*-features.mjs` run their generator but are absent from `regen-viz.mjs` FAST[] → their augmentation is never (re)produced → ghost data silently dropped (2/9 wired pre-OOM, 7 explicitly blocked on it); directly gated by U-1 | node=`regen-viz.mjs` FAST[] + per-generator `loadOptional("<output>.json")` verify in `merge-augmentations.mjs` | wiring=none (regen pipeline) | bridge=feeds priority-queue / bridge-synergy / domain-pipeline roosts (the fleet roadmap surface) | rgs-toolchain=`/wire-unwired`→`wiring-review-agent` | acceptance=`ls scripts/generate-*-features.mjs | grep -v test` count == FAST[] roost-stage count; one post-U1 regen materializes all 9 augmentations; node-count delta > 0 and each roost's nodes appear in `system-viz-query find`.
3. **U-VIZ-REFPOOL-SEED-EMBED: produce seed-ghost ref-pool + heterophily-ready 768-d node embeddings** | ROI=H — Tier-0 #3 lever; sierra OWNS the labeled substrate half (india owns the model). poolSize=0 is why india's deploy gate perma-DEFERS and 8 galaxies' "proven" status is blocked; only reachable AFTER U-1 (stale graph = no holdout) | node=`seed-ghost-from-unwired.mjs` (≥2 high-conf `ghost.unwired-engine` ref nodes) + `graph-node-embedding-bridge.mjs` (refresh `_node-embeddings.jsonl`, drop `.partial`) | wiring=none (regen post-merge stage; consumed by `nn-graph-eval.mjs`) | bridge=FEEDS india GNN tier-5 (`graphsage-train-pipeline.mjs`) | rgs-toolchain=`/scrutinize`→`code-analyzer` (data-quality, not physics) | acceptance=`_node-embeddings.jsonl` final (no `.partial`), ≥562 nodes matched (prior live build floor), ref-pool poolSize ≥2; india `runAssessment` produces a non-empty 62-ghost-class holdout (no longer `insufficient-reference-pool`).
4. **U-VIZ-VERIFY-AFTER-WRITE: trajectory-eval protective hooks (R-SVIZ-1) — block downstream emission on stale upstream** | ROI=H — applies the archaeology "trajectory evaluation" idea + sierra's own merge-SIGKILL-silent-continue regression class; prevents the next silent-stale-clobber outage (a sierra graph mistake = fleet-wide search outage) | node=harden `stop-graph-staleness-backstop.mjs` + `sessionstart-graph-staleness-inject.mjs` to gate on `.last-successful-regen.ts > source mtime` AND `pendingCount==0` | wiring=`prism_dev` regen-status action surfacing the gate verdict | bridge=none (protective spine) | rgs-toolchain=`/scrutinize`→`code-analyzer` | acceptance=hook BLOCKS Stop/SessionStart consumers when graph is stale or pendingCount>0; passes silently when fresh; regression test for the merge-SIGKILL-continue path.
5. **U-VIZ-LIVE-DIFF-VIEW: regen-to-regen delta/diff surface** | ROI=M — "to-build" from the galaxy card; turns the 7-min opaque regen into an auditable changelog (nodes/edges added/dropped per run), the operator-visible half of the substrate | node=`build-system-viz-livediff.mjs` (wire its output into a `/system-viz` diff roost) + FAST[] register | wiring=`prism_session:master_index_*` consumer of the diff sidecar | bridge=`documented-by` cross-substrate edge → wiki log | rgs-toolchain=`/forge-triple`→`reviewer` (UI/integration weighting) | acceptance=two consecutive regens emit a diff JSON listing exact node/edge add/drop counts; rendered in the viewer; non-zero on a known graph mutation, zero on a no-op regen.
6. **U-VIZ-OBSIDIAN-BRIDGE-HARDEN: bidirectional graph↔Obsidian vault sync (Docker-Ollama summarized)** | ROI=M — external-software training leg (Obsidian); `system-viz-obsidian-bridge-v2.mjs` exists but the `documented-by` edge type (just shipped `d254c65305`) only covers 38 galaxy→memory edges; compound as wiki/memory coverage grows | node=`system-viz-obsidian-bridge-v2.mjs` + `generate-cross-substrate-edges.mjs` (extend `documented-by` to per-engine wiki/tribal nodes) | wiring=`prism_context:claim_file` lock on wiki writes | bridge=`documented-by` graph→Obsidian/Wiki (PSN legs #1/#3) | rgs-toolchain=`/wiki-query` + `/scrutinize`→`reviewer` | acceptance=N engine nodes gain a `documented-by` edge to their wiki/memory node; NO-DANGLING invariant (test `generate-cross-substrate-edges.test.mjs`) holds; Obsidian vault link-count rises measurably.
7. **U-VIZ-CUSTOMER-DASHBOARD: operator/customer-facing graph view** | ROI=M — "to-build" from card; today `/system-viz` is a dev-fleet tool; a curated read-only roost view (job/program/quote reachability, not 244K dev nodes) is the product-visible surface | node=new curated-projection generator over `system-graph.json` + `_server.cjs` view route | wiring=`prism_session:master_index_query` filtered projection | bridge=consumes hotel/charlie roosts | rgs-toolchain=`/forge-triple`→`reviewer` (UX/integration) | acceptance=read-only operator view renders a filtered subgraph (<5K nodes) with node→repoPath nav; loads from the canonical graph (no second writer).

**External-software training plan (Obsidian, Docker):**
1. **Obsidian — bidirectional graph↔vault as the persistent brain (U-6):** drive `system-viz-obsidian-bridge-v2.mjs` to mirror every engine/roost node's `documented-by` edge into the Obsidian-indexed `knowledge/` vault, and pull the vault's `[[backlink]]` graph back as `documented-by` edges (convention C in CROSS-SUBSTRATE-SYNERGY-MS0) — so the system-viz graph and Obsidian's own graph view converge on one topology; validate by parity-checking sierra's emitted edge count against Obsidian's native unresolved/resolved-link count.
2. **Docker + Ollama (@127.0.0.1:11434) — offload graph-node summarization off the critical path:** route the per-node label/summary generation (the 244K-node describe pass that currently bloats regen) to the local `qwen2.5-coder` via the Docker batch-processor / `ask-ollama.mjs`, keeping Claude for synthesis only — replicates the regen workflow at near-zero token cost and is the natural home for the Blackwell 32B offload tier (per alpha's BLACKWELL-TOKEN routing); validate offload-rate ≥30% on `ollama-offload-stats.json`.
3. **Docker — containerized regen parity harness:** run `regen-viz.mjs` inside a memory-capped Docker container to reproduce the exact V8 external-alloc ceiling deterministically (host RAM masks it), making U-1's streaming fix provable against a fixed-memory oracle rather than an intermittent host failure.
4. **Obsidian Canvas import of the graph:** export a curated subgraph via `export-graph-cypher.mjs` → JSON Canvas so the operator-facing view (U-7) can be authored/validated against Obsidian's Canvas renderer (a real external layout tool), importing its node-grouping conventions rather than hand-rolling layout.

**Atomic gaps (nodes/wirings/bridges):**
- **Live OOM node:** `merge augmentations` stage fails exit 0xC0000409 (3221225786) on the 575MB graph (`.last-regen-failure.json` 2026-06-03T16:55, durationMs 245057) — the single highest-leverage gap; not a wiring gap, a streaming-rewrite gap.
- **Unspliced roost nodes:** 9 of 49 `generate-*-features.mjs` absent from `regen-viz.mjs` FAST[] (2/9 wired, 7 OOM-blocked) → augmentations silently never (re)produced — verified gap, real cause is FAST[] membership, NOT a named-splice gap (`merge-augmentations` loads by output-JSON name).
- **Orphan ref-pool:** `seed-ghost-from-unwired.mjs` ref-pool poolSize=0 + `_node-embeddings.jsonl.partial` left mid-flight → india's GNN holdout empty (`insufficient-reference-pool`), 8 galaxies' "proven" status blocked.
- **15.7K dead edges** found (`9560b33374` U-VIZ-DEAD-PIXEL-WIRE) — wired into the dead-pixel detector but not yet pruned in a regen pass.
- **Envelope-drift nodes:** SYSTEM-VIZ-BRAIN-MS0 (`completed`/`in_progress_real`) + MS-VIZ-ROADMAP-BIND (`completed`/`not_started_real`) — `claims_completed_but_units_pending`; reconcile envelope to reality.
- **Missing bridge:** no `embeds`/`consensus-of` cross-substrate edge types materialized yet (only `owned-by-slot` 82 + `documented-by` 38) — the CROSS-SUBSTRATE-SYNERGY decomposition ledger lists them as remaining, gated on the 24GB regen-exec.

**RGS-rule updates this domain needs:** Add a **system-viz / graph-regen rule** to `scripts/lib/rgs-pipeline-rules.mjs` — currently a unit like "streaming merge-augmentations / regen-viz OOM / ghost-roost FAST[] register / node-embedding ref-pool" only matches the generic `/wire|dispatcher|unwired|orphan/i → /wire-unwired` rule, which mis-frames a graph-infra streaming-rewrite as an engine-wiring task. Proposed: `test: /\b(regen-viz|system-viz|ghost-roost|augmentation|merge-augment|node-embedding|graph-merge)\b/i, skill: "/audit-viz-first" (or "/system-viz"), reviewAgent: "code-analyzer", why: "graph-substrate infra unit — viz-first query before any grep, streaming/IO-correctness over wiring"`. This closes the false-route where sierra's own Tier-0 OOM work gets a wiring-review-agent instead of an I/O/memory-correctness reviewer.

---

## INDIA — ai-training RGS PIPELINE ROADMAP (F0)
**North-star (1 line):** Single self-improving learning substrate every domain clones — own GNN/LoRA/RAG/outcome machinery on the 96GB Blackwell GPU; NN-GRAPH deploy gate DEFERRED (AUROC 0.388/0.5 < 0.78) → clears, and every "training" claim that silently routed through Ollama (an inference-only server) becomes a real PyTorch+CUDA backward pass.
**Time-allocation (from transcripts):** On-track, correctly pivoted. India exhausted NN/GNN tier-5 *text-feature* work (definitive R12-honest negative: features NON-SEPARABLE, cascade defers to tiers 1-4) and moved to BLACKWELL-AI-MS0 — the right call, because one GPU collapses three walls at once (GNN node-cap, serial-embed wall, empty LoRA `innerTrain`). The discipline to hold: stay on the **inference-only MS0/MS1/MS2/MS5/MS6 units india can ship NOW** and do NOT block india's green gate on golf's Python-GPU lane (P1 correction). Anti-drift watch: the fleet had ≥7 slots shadowing alpha's BLACKWELL-TOKEN units — india owns the AI-systems-that-consume-GPU half, golf owns infra (Ollama/NIM/CUDA/PyTorch/model-pulls); do not build infra. Recent good signal: 2 NN/GNN schema-read regression fixes + feature-separability close were all honest-number work, not green-washing.

**Pipeline units (HIGH-ROI ORDERED):**

1. **U-ROUTE-LADDER: capability-aware model resolver, purge every hardcoded `deepseek-r1:14b`** | ROI=H — 5 subsystems hardcode a model that *isn't even installed*; this is a live fail-silent (route picks an absent model → no-op). Pure inference, zero golf dependency, gates MS2/MS5/MS6. | node=extend `ModelRoutingEngine.ts` (R7: do NOT fork `aiSystemRouterEngine`) + wire to shipped `OllamaCapabilityProbeEngine` | wiring=`prism_ai:model_ladder_pick` (+ existing `prism_ai:capability_probe`) | bridge=token-optimization (alpha) — shares the route table; surface the cross-galaxy edge | rgs-toolchain=generic fallback `/scrutinize` + `reviewer` (NO ai-training rule exists — see RGS-rule updates) | acceptance="summarize"→7b, "physics tradeoff"→32b-when-present/7b-when-not; grep gate `Grep "deepseek-r1|:14b" src/engines/** = 0` (scoped non-test).

2. **U-GPU-EMBED-WORKER + embedding-space parity assert** | ROI=H — closes the P2 silent-divergence risk before any corpus pass; cheap pilot (1,000 nodes) that de-risks the entire RAG re-embed. | node=`scripts/py/gpu_embed_worker.py` (sentence-transformers `device=cuda`) + `scripts/lib/gpu-batch-embedder.mjs` (Ollama-parity fallback) | wiring=consumed by `prism_knowledge:rag_query_full` downstream; uses shipped `scripts/lib/py-subprocess-bridge.mjs` | bridge=system-viz (sierra) — node embeddings feed `graph-node-embedding-bridge.mjs` | rgs-toolchain=generic fallback `/scrutinize` + `reviewer` | acceptance=same 50 strings through HF-GPU and Ollama → pairwise cosine > 0.99 (else standardize on ONE path); pilot: same-dispatcher cosine > cross-dispatcher cosine before committing to full run.

3. **U-FULL-CORPUS-EMBED: resumable 302K-node + 38,710-file sweep** | ROI=H — closes the 98% wiki/tribal embedding gap (`embeddingHitCount=0` was the empirical block); the serial 200ms/doc Ollama loop has NEVER finished a full pass. Unblocks semantic search AND GNN features in one run. | node=`scripts/gpu-embed-full-corpus.mjs` (atomic checkpoint /2000, resets stale `embed-all-wiki-progress.json` state:running>5min, VRAM headroom pre-check) | wiring=writes `node-embeddings-768d.jsonl` consumed by `nn-graph-retrain-lifecycle.mjs` pre-retrain stage (already wired) | bridge=tribal-knowledge (golf) — shared tribal-embed-index keyed by doc-ID+domain | rgs-toolchain=generic fallback `/scrutinize` + `reviewer` | acceptance=full pass <10 min; `node-embeddings-768d.jsonl` 3,790 → ~302K rows; single-`dim` assertion holds (no mixed-space).

4. **U-RABITQ-HNSW + U-RAG-ORCHESTRATOR: the actual semantic RAG/router/memory** | ROI=H — the archaeology #1-leverage cross-cutting dormant build ("Embedding infra → Semantic RAG/Router/Memory", surfaced by 3 independent plans); replaces fleet-wide `String.includes`. Harvests the already-approved `mcp__claude-flow__embeddings_rabitq_*` (CLAUDE.md HARVEST tool, 32× compression). | node=`scripts/lib/rabitq-hnsw-builder.mjs` (consumes wired `embeddings_quantize`) + `RAGCorpusOrchestratorEngine.ts` (HyDE→GPU-embed→Hamming probe→float32 rerank→32b LLM rerank, `domain` filter) | wiring=`prism_knowledge:rag_query_full` | bridge=token-optimization (alpha co-owns the router half) + cad (delta) corpus | rgs-toolchain=generic fallback `/scrutinize` + `reviewer` (NEW rule should route RAG→`/wiki-query`+`/deep-search`) | acceptance=real-data, NOT `toBeDefined()` — "kc1.1 SS304"→Kienzle tribal tip; "G84 tapping"→feed-rate tip; RaBitQ recall loss <5% vs exact cosine on held-out query set.

5. **U-GNN-EDGE-PREDICT: link-prediction dead-edge surfacer (the real MS3 win, reordered ahead)** | ROI=H — P1 correction: this is **deployable with NO reference-pool dependency**, unlike the multi-class GNN that is data-blocked. Surfaces ranked `(engine,dispatcher)` dead-edge candidates → directly feeds `stop_on_unwired_assets.mjs` (the 110 unwired engines, fleet-wide). | node=`GnnEdgePredictionEngine.ts` + `scripts/py/edge_predict.py` (link-prediction head) | wiring=`prism_dev:infer_missing_wiring` → `dead-edge-candidates.jsonl` | bridge=wiring (romeo) — consumes the 110-unwired list; system-viz (sierra) ghost roost | rgs-toolchain=`/wire-unwired` (matches the existing `/wire-unwired` rule via "wiring/unwired") + `wiring-review-agent` | acceptance=top candidate for a known-wired engine matches its real dispatcher on a held-out check.

6. **U-GNN-GPU-TRAIN + U-GNN-LIFECYCLE-WIRE: break the constant-vote collapse on GPU** | ROI=M — fixes the heterophily collapse (apply `graph_heterophily_aggregate`/H2GCN + stratified neg-sampling; current 8-dim classifier collapsed to a constant `prism_turning` predictor, AUROC 0.5). Gated on golf's Python-GPU stack — schedule AFTER MS0-MS2, do not block india's green gate on it (P1). | node=`scripts/py/gnn_train.py` (PyG GATv2/H2GCN, 768→256→128→~20-class, `NeighborLoader[25,10]`, `CrossEntropyLoss(weight=inv_freq)`) + modify `nn-graph-retrain-lifecycle.mjs` (`--python-backend`/`PRISM_NNG_GPU_TRAINER=1`) | wiring=writes `graphsage-checkpoint.candidate.json`; **JS `promoteDecision()` stays sole arbiter** (auditability + Stop-hook chain) | bridge=system-viz (sierra) — gated on sierra's merge-OOM fix (stale graph = no holdout) | rgs-toolchain=`test-team` + `test-review-agent` (NEW rule should route GNN/training→`/ai-train-india`) | acceptance=standalone loss decreases AND `distinctPredictions>1` on the 62-node holdout (constant-vote signal breaks); schema-validator test in `src/__tests__/` before any production read. **Honest gate:** promotes nothing until reference-pool grows (several classes at zero) — do not read "fixed in candidate" as a fleet win.

7. **U-LORA-TRAINER-CORE + U-LORA-PROMOTE-GATE: fill the empty `innerTrain` ghost contract** | ROI=M — all ~95 LoRA engines are orchestration shells whose `innerTrain` slot was NEVER filled (every "fine-tune" was a silent no-op); only lathe+mill stacks exist. Gated on golf's PEFT/Unsloth/bitsandbytes cu128. | node=`scripts/lib/lora-trainer-blackwell.py` (Unsloth QLoRA NF4) + `LoRATrainerBridgeEngine.ts` filling the pipeline's `innerTrain`; respects `DistributedLockManager.withLock("lora-train-<domain>")` | wiring=extends `lora-training-pipeline.mjs`; hands to existing `LatheLoRAOllamaDeployerEngine` via `U-ADAPTER-DEPLOY` | bridge=speed-feed/mill/lathe/wedm/cam — per-domain adapters via `LoRAAdapterRegistryEngine` (train once→registry→all domains) | rgs-toolchain=`/scrutinize` + `reviewer`+`physics-review-agent` (G-code holdout touches safety) | acceptance=**P0-5 generative gate, NOT classification metrics** — exact-match/BLEU/pass@k on a held-out G-code set + S(x)≥0.95 safety floor + regression-vs-base delta; lathe 50-example r=8 1-epoch: loss decreases AND adapter saves; below-gate candidate refused; safety veto blocks independently.

8. **U-PER-GALAXY-SVI-EMITTER: make india's own psi visible** | ROI=M — india's own SVI is invisible (it emits everyone else's learning signal but has no self-dimension); needed so the fleet can rank ai-training work and so the LoRA-template turnkey (wedm/cam/quoting/sfc still owed) has a measured target. | node=`AiTrainingSviEmitterEngine.ts` (psi from deploy-gate state + embedding coverage + offload take-rate) | wiring=`prism_dev:svi_ranked_backlog` subsystem registration | bridge=token-optimization (alpha) SVI rollup | rgs-toolchain=`/scrutinize` + `reviewer` | acceptance=ai-training appears as a scored SVI subsystem dimension; psi reflects real deploy-gate DEFERRED→clears transition (not a hardcoded 1.0).

**External-software training plan (Ollama, Docker, NVIDIA NIM):**
- **Ollama (inference-only — the load-bearing boundary):** Ollama serves GGUF + `nomic-embed-text` embeddings + KV-cached CAG contexts and **CANNOT train** (no backward pass) — every training claim routing through Ollama is structurally a no-op. Concrete training actions that DO use it: (a) **MS2 RAG re-embed** runs `nomic-embed-text` over 302K nodes + 38,710 files to build the resident HNSW index — but with a mandatory **parity assert** (HF-GPU `nomic-embed-text-v1` vs Ollama `nomic-embed-text`, pairwise cosine > 0.99) so the GPU batch-embedder and query-time embeddings share ONE space; (b) **MS5 octopus local-voice** routes `qwen2.5-coder:32b` (~22GB, the real local heavy model — kimi2.6 is a 1T-param MoE, cloud-only, struck from every local role) as a real consensus voice via `MultiModelConsensusEngine.ask()` behind the `scripts/lib/redact-secrets.mjs` PII gate; (c) **MS6 CAG** uses Ollama KV-cache (`OLLAMA_MAX_LOADED_MODELS`/`FLASH_ATTENTION`/keep_alive) to make per-galaxy context resident — india's units degrade-and-pass if golf hasn't set these. Capability probe (`prism_ai:capability_probe`, shipped) reads `/api/tags`+`/api/ps` so route() can never pick an absent model.
- **Docker (golf-provisioned, india-consumed):** the **real LoRA/GNN training stack** runs in the Unsloth Blackwell Docker image (or Studio venv) — `torch 2.11+cu129`, bitsandbytes from the matching CUDA build, PyG/DGL for sm_120, `TORCHDYNAMO_DISABLE=1`+`UNSLOTH_COMPILE_DISABLE=1`. India's exhaustive-training action: run **QLoRA fine-tunes per-domain** (7B ~6-8GB/~10min, 32B ~24-35GB/~30-45min, time-shared via `DistributedLockManager.withLock("gpu-train-<domain>")` on the 02:00 S4U cron) against the **24,545 JM Die programs + held-out G-code corpus** — replicating each domain seat's real output (Mastercam X8 / hyperMILL v31 / Fusion / Okuma OSP / Hurco WinMax NC) as the training target, with exact-match/BLEU/pass@k parity validation vs the golden NC archive.
- **NVIDIA NIM (optional GPU-serving):** if golf stands up NIM endpoints, india routes to them automatically via the capability probe's present-backend detection (no india code change) — used to serve larger reasoning models for octopus/RAG-rerank when a NIM-hosted model beats the local 32b tier; **U-PYGPU-HEALTH** (`gpu_health.py`) is the hard gate that asserts a real bnb 4-bit dequant op runs ON GPU (`get_device_capability()==(12,0)`, `sm_120 in get_arch_list()`) — not just `torch.cuda.is_available()` — so NIM/CUDA never silently falls back to CPU.
- **Exhaustive corpus training (the "train using its real software" mandate):** embed + index every installed-seat artifact as the training corpus — Mastercam `.NCI`/posts, hyperMILL `.hmc` projects (`HMCProjectParser`), Fusion exports, the 160,582 NC + 13,790 `.cps` post corpus, Okuma `.MIN`, Hurco WinMax — so the GNN/LoRA/RAG learn from JM's actual shop output, and parity-validate generated G-code byte-against the golden NC archive (echo's `nc-normalize.mjs` classifier).

**Atomic gaps (nodes/wirings/bridges):**
- **Unwired/orphan engines (named):** `OutcomeRLBridgeEngine.ts` — exists on disk, **wiring/wiki-bucket orphaned**; must verify a live consumer before treating it as the reward substrate (the closed-loop reward is ❌ "not live" on the generalization scorecard). `GalaxyAdapterFactoryEngine` — **unbuilt** (blocks the build-once LoRA fan-out to all 34 galaxies). The **67 forked LoRA engines** need a real migration (`U-MMO-LORA-PIPELINE-COLLAPSE-MIGRATE` is NOT done) before any standardized `innerTrain` rolls out fleet-wide.
- **Missing wirings:** `prism_ai:model_ladder_pick` (MS1), `prism_knowledge:rag_query_full` (MS2), `prism_dev:infer_missing_wiring` (MS3 edge-predict), `prism_dev:gpu_stack_health` — none exist yet; the ~10 hardcoded `deepseek-r1:14b` sites (ModelRoutingEngine, MultiModelConsensusEngine, ConsensusAIBridgeEngine, OllamaContextFloorEngine, OllamaHookBridgeEngine, OllamaTaskOffloaderEngine, ConsensusAuditLogEngine, LedgerStoreEngine, WikiIngestRouterEngine, aiReasoningDispatcher) are all dead wirings pointing at an uninstalled model.
- **Missing bridges:** india→sierra GNN-input edge is **stale** (merge-OOM means no holdout — gates india's eval); india→every-domain LoRA-template edge exists for only lathe+mill (wedm/cam/quoting/sfc adapters owed); the **reference-pool seed** (≥2 high-confidence `ghost.unwired-engine` reference ghosts) is poolSize-blocked — the data-side gate that keeps the multi-class GNN DEFERRED across 8 galaxies' "proven" status.

**RGS-rule updates this domain needs:** **NOT none — this is a real coverage gap.** `scripts/lib/rgs-pipeline-rules.mjs` has explicit `RULES` for pdf/video/forge-triple/wire-unwired/mill/lathe/wedm/cam/cad/test/scrutinize/dedup, but **ZERO rule for AI-training units** — every LoRA/GNN/RAG/embed/neural/train/self-improving unit falls through to `GENERIC_FALLBACK` (`/scrutinize` + `reviewer`), the weakest routing. Add:
1. `{ test: /\b(lora|gnn|graphsage|neural net|deep learning|self[-\s]?improv|retrain|fine[-\s]?tune)\b/i, skill: "/ai-train-india", why: "AI-training unit — model/retrain pipeline", confidence: 0.8 }` (the `/ai-train-india` skill already exists and is unrouted).
2. `{ test: /\b(rag|embedding|semantic search|corpus embed|vector index|hnsw|rerank|retrieval)\b/i, skill: "/wiki-query", why: "RAG/retrieval unit — query the knowledge substrate first", confidence: 0.75 }` (+ secondary `/deep-search`).
3. Agent rule: route training/model units to `test-review-agent` (the candidate→holdout gate is a test-integrity problem) and **add a `physics-review-agent` arm for any LoRA unit emitting G-code** (generative gate touches S(x) safety, not classification AUROC — per P0-5). Word-boundary `\b` anchors throughout to avoid the documented wire-EDM/`/wire-unwired` false-match class. These also serve the cross-domain LoRA fan-out: any galaxy's "train a per-domain adapter" unit then routes to india's pipeline skill instead of a bare `/scrutinize`.

---

## GOLF — fleet-hygiene RGS PIPELINE ROADMAP (F0)
**North-star (1 line):** Keep the 26-slot fleet alive, lean, and self-healing with zero babysitting — defend the MCP :3100 / Docker / Ollama substrate every one of 33+ galaxies depends on (a wedged daemon drops all `prism_*` reachability at once).

**Time-allocation (from transcripts):** ON-TRACK, with one drift to correct. Golf has correctly migrated code-hygiene (4125→0 tsc) → MCP-daemon reliability (`U-MCP-FACTORY-REFACTOR` fresh-server-per-request `1297b0a8f5`, `U-MCP-BIND-FAILFAST`, `U-WATCHDOG-BOOTGUARD`) → context-economy → local-LLM/Blackwell infra (`U-MODEL-CATALOG-QWEN3`, `U-BLACKWELL-HOST-PRESET`). The DRIFT: the last ~18h is Blackwell GPU-catalog work that is genuinely alpha/india's BLACKWELL milestone (the trajectory spec flags "multiple slots shadowing alpha-authored BLACKWELL units" as a coordination hazard). Redirect: finish the F0 operator work order (guarded MCP auto-restart actuator + fleet-health dashboard `U-FD02..05`/`U-FH02`) before any more GPU-catalog shadowing — that is the assigned F0, and the reaper/MCP substrate is golf's non-delegable lane.

**Pipeline units (HIGH-ROI ORDERED):**

1. **U-MCP-RESTART-ACTUATOR: guarded MCP :3100-only auto-restart actuator** | ROI=H — the explicit F0 work order (PRIORITY-BUILD line 188; Top-3-next-move #1); a wedged daemon drops `prism_*` reachability for 33 galaxies simultaneously (Tier-0 #2). Auto-restart stays FORBIDDEN for Docker; this actuator restarts ONLY the MCP node process, behind a confirm gate. | node=extend `scripts/fleet-services-watchdog.mjs` + `mcp-server/scripts/ollama-docker-launcher.mjs --services=mcp` (wire, don't rebuild — both exist) | wiring=scheduled-task + Stop-hook surface (NOT a dispatcher action, per design line 80) | bridge=none (golf-internal; reads `chat-slots.json` liveness) | rgs-toolchain=`/scrutinize` + `wiring-review-agent` (destructive-action-dense; the actuator is a wiring/health unit) | acceptance=injected-wedge test (kill :3100 listener) → actuator detects via port preflight within one 5-min tick → restarts MCP only, never touches Docker containers → `prism_session:ping` recovers; assert idempotent (no restart storm), assert Docker daemon untouched.

2. **U-FD02..U-FD05 + U-FH02: unified operator fleet-health dashboard** | ROI=H — the second half of the F0 work order + Top-3 #3; today the fleet has NO single live operator view (reaper log + memory-history + task-health are 3 separate JSONLs). One glanceable surface = the "perfect hygiene" operator UX. | node=new `scripts/fleet-health-dashboard.mjs` aggregating the 4 telemetry surfaces (`fleet-reaper.log`, `fleet-memory-history.jsonl`, task-health audit, `ollama-offload-stats.json`) → render via existing `scripts/lib/html-report-render.mjs` (`mdToHtml`, AUDIT-SYNERGY-MS0) | wiring=`scripts/dashboard-serve.mjs` route (existing serve harness) | bridge=sierra/system-viz (publish a `ghost.fleet_health` roost edge so /system-viz surfaces it) + per-galaxy SVI attribution feed to india | rgs-toolchain=`/scrutinize` + `reviewer` (UI/integration-weighted per per-file gate) | acceptance=dashboard renders all 26 slots' liveness + per-tree RAM + the 8+ `PRISM *` scheduled-task states + Ollama offload-rate in one HTML page; assert it degrades gracefully (missing JSONL → honest "no data" not crash); WAI-ARIA a11y pass.

3. **U-REAP-TUNER: india-cloned closed-loop false-positive-reap threshold tuner** | ROI=H — Top-3 #2, named "the single biggest perfect-hygiene lever." The reaper's confirm-after-N-ticks (2×300s) + age-floor are hand-set constants; close the loop over the existing `.fleet-reaper-kills.jsonl` / `.fleet-reaper-actions.jsonl` / `.fleet-reaper-candidates.json` ledgers to drive false-positive reaps → 0. | node=new `scripts/fleet-reaper-threshold-tuner.mjs` cloning india's `AdaptiveThreshold`/`MetaLearningOptimizer` pattern (clone, don't reinvent — india owns the substrate) reading reaper kill/keep outcomes | wiring=`xproc_outcome_publish {slot:golf}` → india's OutcomeFeedbackBus (the per-domain self-improving-AI clone, `feedback_domains_own_ai_training_systems`) | bridge=india/ai-training (consumes the tuner outcomes; bounded ±30% of canonical confirm-window per archaeology "auto-calibration bounded" idea) | rgs-toolchain=`/scrutinize` + `reviewer` | acceptance=replay historical kill ledger → tuner proposes confirm-window/age-floor adjustments that would have prevented every recorded false-positive (the MCP-zombie `non-claude-parent` class) with ZERO increase in missed-orphan misses; fail-loud if ledger < N samples (no degenerate fit).

4. **U-DOCKER-WEDGE-DETECT: Docker-daemon-wedge auto-detection-with-alert (advise-only)** | ROI=M-H — named gap in MASTER-FLEET-PLAN line 80; a Docker wedge silently degrades the master-index to BM25-only fleet-wide (Qdrant/Postgres/Prometheus all DOWN). Detection + AGENT_CHAT alert; auto-restart stays forbidden (would kill every container). | node=extend `scripts/fleet-services-watchdog.mjs` + reuse `mcp-server/scripts/ollama-docker-launcher.mjs` health probe (wire-don't-rebuild) | wiring=writes one advisory per episode to `AGENT_CHAT.jsonl` (the existing ONE-advisory-per-episode pattern from memory-monitor) | bridge=juliett/database-expansion (Qdrant/Postgres owner consumes the wedge alert) | rgs-toolchain=`/scrutinize` + `wiring-review-agent` | acceptance=stop Docker daemon in a test → watchdog detects within one tick → emits exactly ONE advisory (no spam), names the down containers, NEVER issues a restart; assert advisory throttle holds across repeated ticks.

5. **U-MACHINE-HANDBOOK-MS0: MachineHandbookIntelligence pipeline (7 engines) — substrate-truth phase** | ROI=M-H — fleet-priority #4 (Tier-1); golf-owned as substrate-truth/data-debt owner. Machine data is 0.5% complete (0/920 confidence metadata) — this poisons every cut-safety decision fleet-wide. 70% reuses existing `DocumentLearningEngine`. | node=`MachineHandbookRegistryEngine` + `handbookLimitGuard` hook (NEW — first real .ts engines in `engines/fleet-hygiene/`; check `duplicationGuardEngine` first vs `MachineLimitGuard`/`ShopConfigurationEngine`) | wiring=`prism_calc`/`prism_safety` (gates G-code vs real firmware spindle/axis maxes) + `handbookLimitGuard` PreToolUse | bridge=foxtrot/mill + whiskey/lathe + mike/wedm (every domain's machine-limit checks read the handbook registry) | rgs-toolchain=`/pdf-learn` (handbook PDF extraction via lima's pypdf page-extractor) + `physics-reviewer` (spindle/torque-curve limits are physics) | acceptance=extract ≥1 real machine handbook (JM Haas VF-2 / Hurco VM30i) → registry returns real spindle-power/RPM-cap with provenance+confidence → `handbookLimitGuard` BLOCKS a G-code request exceeding the handbook max (was a generic default); fail-closed when a machine has no handbook (honest "unverified" not a fake limit).

6. **U-MCP-FACTORY-CLOSEOUT: close the stale MCP-CAPACITY-MS0 queue entry** | ROI=M — `U-MCP-FACTORY-REFACTOR` shipped 3d ago (`1297b0a8f5`, fresh McpServer per request) but still shows ELIGIBLE in the slot queue — a close-out-debt drift. Drain it across the 5 surfaces (envelope/roadmap-index/MILESTONE_PROGRESS/BUILD_STATE/chat-bus). | node=none (doc/close-out) | wiring=`node scripts/close-out-milestone.mjs` | bridge=none | rgs-toolchain=`/close-out-audit` → `/scrutinize` (generic fallback; pure-doc close-out) | acceptance=`U-MCP-FACTORY-REFACTOR` no longer eligible in `slot-query.mjs golf`; MILESTONE_PROGRESS reflects shipped; zero silent close-out debt.

7. **U-SR05 + U-SR06: slot-recovery phase-A hardening** | ROI=M — eligible queue (SLOT-RECOVERY-PHASE-A); post-/compact slot-reclaim robustness is core fleet-hygiene (golf reclaims crashed slots every `/checkin-golf`). | node=extend `.claude/helpers/chat-slots.mjs` (`reclaim`/`golf-liveness`) | wiring=`/checkin-golf` Step 2 contract | bridge=none | rgs-toolchain=`/scrutinize` + `reviewer` | acceptance=force a crashed-slot state (stale heartbeat) → reclaim restores binding without clobbering a live peer (PID-reuse + wedged-harness `indeterminate` cases both refuse-to-reclaim).

8. **U-SKILL-MIRROR-RECONCILE: C:↔H: skill/settings mirror drift audit** | ROI=M — eligible queue (DEV-INFRA); the `c-to-h-mirror` hook is one-way (C:→H:), so H:-side edits silently drift. Golf as fleet-config-doctor audits it. | node=`scripts/mirror-c-to-h-audit.mjs` (exists — wire/run, don't rebuild) | wiring=Stop-hook advisory | bridge=none | rgs-toolchain=`/scrutinize` (generic) + `reviewer` | acceptance=audit reports every C:/H: byte-divergent skill/settings file; assert no false-positive on intentional-H:-only files (`.bak-*`/cache excludes hold).

**External-software training plan (Docker, Ollama):**
1. **Ollama capability + latency corpus extraction (own the offload substrate):** drive `mcp-server/scripts/ollama-docker-launcher.mjs` + `scripts/ask-ollama.mjs` to enumerate every installed model at `127.0.0.1:11434/api/tags`, then build a real latency/throughput parity table per model (qwen2.5-coder:3b/7b/14b/32b, qwen3 catalog from `U-MODEL-CATALOG-QWEN3`) by replaying golf's actual hook-offload calls — feed it into `ollama-offload-stats.json` so the dashboard (U-FD02) reports true per-model take-rate, and capture the known `/api/chat` cold-load-stall regression (`reference_golf_ollama_coldload_stall` — 9.6GB mmap from H: aborts under short hook timeouts) as a measured threshold, not a guess.
2. **Docker health-parity replication (substrate truth):** run `scripts/docker-mcp.mjs` + `fleet-services-watchdog.mjs` against the live Docker stack (Qdrant/Postgres/Prometheus) to extract each container's healthy-state signature (port, healthcheck latency, restart count), persist it as the wedge-detection baseline (U-DOCKER-WEDGE-DETECT), and validate detection by injecting a real `docker stop` — replicating the daemon's own healthcheck workflow rather than re-implementing it.
3. **Ollama-route the reaper-tuner's bulk classification:** the U-REAP-TUNER ledger-replay (per-process orphan-class labelling over thousands of kill/keep rows) is a deterministic-classify task — route it to local qwen via `/ollama-classify` (R5: model only for judgment, code for the deterministic transform; CLAUDE.md §AI-SYSTEM-ROUTING) so the closed-loop tuner trains at ~zero Claude cost and golf's own offload-rate stays ≥30%.
4. **GPU-floor parity for the reaper coordinator:** validate the fleet-reaper GPU/Ollama coordinator's VRAM/host-floor presets (`fleet-reaper-host-presets.mjs`) against real `nvidia-smi` output on the live box (RTX 4080S 16GB today, RTX PRO 6000 96GB Blackwell post-swap) so the coordinator's "is the GPU busy" gate is calibrated to actual hardware, not a hardcoded floor — but keep this scoped (the Blackwell GPU *catalog* itself is alpha/india's lane, not golf's).

**Atomic gaps (nodes/wirings/bridges):**
- **No dispatcher-wired `.ts` engines in `engines/fleet-hygiene/`** — the dir is doctrine-only (CLAUDE/MEMORY/PATHS/TOOLBELT). This is BY DESIGN (reaper/monitor/task-health are scheduled-task + hook surfaces, MASTER-FLEET-PLAN line 80), so it is NOT an orphan to "wire" — but it means golf has zero presence in the engine→dispatcher graph and zero SVI attribution. GAP: golf's substrate-health has no `prism_*` query surface. Proposed bridge: a read-only `prism_dev:fleet_health_query` action backed by U-FD02's aggregator (the ONE place a dispatcher action is justified — operator/agent queryability of fleet state).
- **MCP auto-restart actuator: missing** — `ollama-docker-launcher.mjs --services=mcp` exists but there is no guarded, confirm-gated, MCP-only auto-restart wiring (U-MCP-RESTART-ACTUATOR fills it). The actuator is the single most-consequential missing node (Tier-0 #2).
- **Docker-wedge detector: missing node** — `fleet-services-watchdog.mjs` exists but does not specifically detect the silent BM25-only-degradation wedge with a throttled AGENT_CHAT alert (U-DOCKER-WEDGE-DETECT).
- **Missing bridge golf↔india** — the reaper telemetry ledgers (`.fleet-reaper-kills.jsonl`) are never fed to india's outcome/closed-loop substrate; U-REAP-TUNER + `xproc_outcome_publish {slot:golf}` is the missing edge.
- **Missing bridge golf↔sierra** — golf MEMORY.md notes "sierra (system-viz) — no galaxy dir registered yet"; the fleet-health dashboard should publish a `ghost.fleet_health` roost edge (cross-substrate ADD-only edge spine, CROSS-SUBSTRATE-SYNERGY-MS0) so /system-viz can render golf's substrate state.
- **920-machine handbook data debt** — 0.5% complete, 0/920 confidence metadata (the MachineHandbookIntelligence node, fleet-priority #4); the largest data-truth gap golf owns.

**RGS-rule updates this domain needs:** YES — `scripts/lib/rgs-pipeline-rules.mjs` has NO fleet-hygiene branch, so golf units fall to the generic `/scrutinize`@0.30 fallback (or mis-route: a "wire the MCP actuator" unit false-matches `/wire-unwired` on `\bwiring\b`). Add a fleet-hygiene PIPELINE_RULE before the generic fallback:
- `test: /\bfleet[-\s]?(reaper|health|hygiene|memory|task)\b|\borphan[-\s]?process\b|\bzombie\b|\bmcp[-\s]?(restart|watchdog|daemon|supervisor)\b|\bscheduled[-\s]?task\b|\bdocker[-\s]?wedge\b/i` → `skill: "/fleet-reaper"`, `why: "fleet-hygiene / reaper / MCP-substrate unit — routes to the golf hygiene orchestrator"`, `confidence: 0.80`. Place it ABOVE the `/wire-unwired` rule so "wire the MCP restart actuator" routes to hygiene, not generic wiring (same wire-EDM-style structural-exclusion lesson the file already learned twice).
- Add an AGENT_RULE: `test: /\bfleet[-\s]?(reaper|health|hygiene)\b|\borphan\b.*\bprocess\b|\bmcp[-\s]?daemon\b/i` → `agent: "wiring-review-agent"` (destructive-action-dense health/process units are wiring/integration reviews, not physics) — but ensure the existing `physics-reviewer` rule still wins for any U-MACHINE-HANDBOOK spindle/torque-limit unit (handbook limits ARE physics; the `force|thermal|feed|speed` physics test must precede the hygiene agent rule).

---

## OSCAR — speed-feed RGS PIPELINE ROADMAP (P1)
**North-star (1 line):** Make the SFC calculator page emit physics-correct RPM/feed/DOC/WOC with cited uncertainty for EVERY mathematically/logically valid input combination (zero silent defaults), and ship the calibration moat (`MS-SFC-CALIBRATE`) that turns physics-default into shop-calibrated — the saleable SFC subscription product.

**Time-allocation (from transcripts):** REDIRECT. Last 30 days oscar drifted across many fronts (OSCAR-SFC-9AXIS U-OSC9-08..17, then MILL-STUDIO, then bridge-wiring, then PER-SLOT-CLOSED-LOOP, then a fresh `OSCAR-SFC-3WAY-MS0/U-GWIZARD-COMPARE` 31h ago). The engine layer is BUILT (`UltimateSpeedFeedEngine` 31 models/401 asserts, `SpeedFeedNineAxisOrchestratorEngine`, both comparator bridges). Stop adding orchestrators — the remaining value is (1) a combinatorial validation harness over the calculator page, and (2) authoring the 24 empty `MS-SFC-CALIBRATE` units (envelope is a 0-unit shell, `status:not_started`). Producer-before-consumer: a proven SFC must land before echo's post matrix (the post carries oscar's F/S per block).

**Pipeline units (HIGH-ROI ORDERED):**
1. **U-SFC-COMBO-HARNESS: combinatorial SFC validation harness over the calculator page** | ROI=H — the literal operator PRIORITY-1 deliverable; one harness validates the parameter every post block carries | node=`SpeedFeedExhaustiveCombinationEngine` (exists — extend; do NOT rebuild) driving `prism_calc:sfc_nine_axis_run`/`sfc_calculate`/`ultimate_speed_feed` across the 9-axis cross-product (materials×machines×tools×holders×operations×toolpath×finish×tolerance×coolant) | wiring=`prism_calc:sfc_nine_axis_run` (wired) + new `prism_calc:sfc_combo_validate` | bridge=none (self-contained) | rgs-toolchain=`test-team` + `physics-reviewer` (matchAgents fires on "feed|speed|force") | acceptance=100% of valid cells return physics-valid OR honest limited-mode flag (zero silent default), Altintas SLD chatter-stable RPM gate fires per cell, units-clean per-line (no 25.4× scale anomaly), machine/HT-aware adjustment applied; extends the 103-case `SPEEDFEED-VARIABILITY-MS0` matrix (`1b87f98f2c`) to full coverage.
2. **U-OSC9-CALIB-APPLY-WIRE: wire the orphaned calib-apply keystone** | ROI=H — the keystone the whole CALIBRATE milestone hangs on; calibration is dead without the apply path | node=new `SpeedFeedCalibrationApplyEngine` (NO `SpeedFeedCalib*` engine exists today — confirmed absent; `StratifiedCalibrationEngine`+`AdaptiveCalibrationEngine` are generic, reuse their math) | wiring=`prism_calc:calibrate_stratified` (action exists) → apply path into `sfc_nine_axis_run` | bridge=oscar↔india (`xproc_calibration_monitor_record` records actuals → drift-canary) | rgs-toolchain=`/scrutinize` + `physics-reviewer` | acceptance=a shop-calibrated cell overrides the physics default and records the actual via `xproc_calibration_monitor_record`; round-trip E2E through `prism_calc` proves calibrated > physics-default delta is applied, not silently dropped.
3. **U-SFC-CALIB-AUTHOR: author the 24 MS-SFC-CALIBRATE units (Stacked-Bayesian-Model-Averaging, regime-routed)** | ROI=H — envelope is a literal 0-unit `not_started` shell; the saleable moat | node=`MS-SFC-CALIBRATE.json` envelope population + regime-router on a physics-prior backbone (reuse `SpeedFeedPSNDecisionPriorEngine` Bayesian prior) | wiring=`prism_calc:calibrate` family | bridge=oscar↔india (LoRA-trained per-material SFC models) | rgs-toolchain=`/forge-triple` (engine+skill+hook, fires on "engine"+"hook") then per-unit `physics-reviewer` | acceptance=24 units authored with concrete acceptance each; SBMA ensemble beats single-model on held-out JM actuals (CI, not scalar — per `feedback_mathematical_exhaustive_completeness`).
4. **U-SFC-TRI-COMPARE-WIRE: complete the tri-comparator (PRISM × baseline × G-Wizard × HSMAdvisor)** | ROI=H — `sfc_tri_compare` action enum exists but the handler only references `HSMAdvisorComparatorBridgeEngine`; the new `GWizardComparatorBridgeEngine` (shipped `2d0a2d54ea`) and `SpeedFeedTriComparatorEngine` are NOT wired into the action | node=`SpeedFeedTriComparatorEngine` + `GWizardComparatorBridgeEngine` (both exist) | wiring=`prism_calc:sfc_tri_compare` → stack both legs | bridge=none | rgs-toolchain=`/wire-unwired` + `wiring-review-agent` | acceptance=`sfc_tri_compare` returns PRISM/baseline/G-Wizard/HSMAdvisor on one matrix in PRISM-canonical metric; UNITS-FIRST 25.4× guard fires on inch-vs-mm install; MRR axis correctly dropped for the G-Wizard leg (crib has no cut depth).
5. **U-SFC-CATALOG-EXTRACT: per-vendor S/F extraction backlog → cited real data** | ROI=M-H — the matrix runs on defaults until cited tables land; compounds every downstream cell | node=`SpeedFeedPDFCorpusBridgeEngine` (exists, `sfc_pdf_corpus_bridge` wired) + `<vendor>-speed-feed-data.ts` emitters from `state/shared/quoting/catalog-sfc-extraction-manifest.json` | wiring=`prism_calc:sfc_pdf_corpus_bridge` | bridge=oscar↔romeo (romeo runs BLACKWELL-DB-GEN vision-OCR; oscar consumes) ↔ lima (pypdf page-extractor, fleet-canonical) | rgs-toolchain=`/pdf-learn` (fires on "catalog|datasheet") + `reviewer` | acceptance=each new `<vendor>-speed-feed-data.ts` cites source per row (R9), matrix cell Vc-delta recovered where toolcrib matched (`SpeedFeedCatalogJoinerEngine`), zero `toBeDefined()` stubs.
6. **U-SFC-FUSION-BUTTON: "Speed&Feed via PRISM" in-seat add-in button** | ROI=M-H — Revenue-Day-1 surface; puts the validated calculator in the operator's hand | node=`CAMAddInFrameworkEngine` → calibrated SFC (requireTier gated); shared pillar with kilo's `MS-CAM-MASTERY/U-CAMM-FUS-D1` | wiring=`prism_cam:auto_speed_feed_optimize` / `prism_calc:sfc_nine_axis_run` from the add-in | bridge=oscar↔kilo (Fusion add-in framework) | rgs-toolchain=`/cam-strategy` + `reviewer` (UI/integration weighted) | acceptance=in-seat button calls calibrated SFC, gated by subscription tier, returns cited RPM/feed to the Fusion/Mastercam session; rides on U-OSC9-CALIB-APPLY-WIRE (must land after #2).
7. **U-SFC-PROPAGATION-VERIFY: prove SFC fans into echo's post + mill/lathe/wedm losslessly** | ROI=M — the producer-before-consumer gate for the P1 post matrix; validates oscar's F/S reaches every NC block | node=`SpeedFeedPropagationBridgeEngine` (exists) | wiring=`prism_calc:speed_feed_bridge` → `cam_speed_feed_bridge` → `ToolpathBlock`→NC | bridge=oscar→echo (per-NC-block F/S), oscar→foxtrot/whiskey/mike (cut physics), oscar→charlie (MRR→cycle_min) | rgs-toolchain=`/scrutinize` + `physics-reviewer` | acceptance=E2E: a calculated cell's F/S appears byte-correct in an echo-emitted NC block for one machine per post tier (5-axis→3-axis→mill-turn→Swiss→lathe→wedm); MRR→cycle_min reaches charlie's `CycleTimeEstimatorEngine`.

**External-software training plan (G-Wizard, HSMAdvisor, Ollama):**
1. **HSMAdvisor corpus + parity (live seat applied 2026-05-27):** `HSMAdvisorAdapterEngine` reads `C:/Users/wompu/AppData/Roaming/HSMAdvisor/user_tool_lib.tooldb2.xml` (41,209 tools) + `machines.xml` (12 machines); run `HSMAdvisorComparatorBridgeEngine` across the full tool×material cross-product and assert PRISM Vc/fz/rpm/feed within tolerance of HSMAdvisor's output per cell — every divergence becomes a calibration training pair. Round-trip via `HSMAdvisorLibraryExporterEngine`/`HSMAdvisorMachineExporterEngine` (ShopTool[]→tooldb2.xml/machines.xml) to prove import↔export fidelity.
2. **G-Wizard parity leg (just shipped, now exhaust it):** `GWizardAdapterEngine` reads `GWizard.*/Local Store/toolcrib.csv` (41,209 tools); drive `GWizardComparatorBridgeEngine` over every crib tool × the material axis (G-Wizard crib stores no material — supply it) and ledger the vc/fz/rpm/feed diffs as the 3rd tri-comparator leg. Replicate G-Wizard's workflow: export PRISM ShopTool[] → `toolcrib.csv` via `GWizardLibraryExporterEngine` so a G-Wizard seat consumes PRISM-recommended tools. (The empty `GWizard.db` machines table is the deferred `U-OSC9-15` — leave deferred, crib-CSV is the live surface.)
3. **41,192-tool aggregate as the training substrate:** `PRISMToolCatalogAggregatorEngine` already deduped 24 `*-extracted.json` → 41,192 tools — use this as the canonical input set for BOTH parity runs so PRISM/HSMAdvisor/G-Wizard score the identical tool population (no apples-to-oranges).
4. **Ollama offload for the corpus + diff work:** route the per-vendor PDF S/F extraction (U-SFC-CATALOG-EXTRACT) and the comparator diff-summarization through local qwen2.5-coder via `/route-to-obsidian` / `ask-ollama.mjs` (Ollama @127.0.0.1:11434) — reserve Claude for the SBMA calibration design + physics-prior synthesis. Keeps the 41K-cell parity sweeps off the Claude token budget.

**Atomic gaps (nodes/wirings/bridges):**
- **Unwired action handler:** `sfc_tri_compare` is in the calcDispatcher action enum but its handler references only `HSMAdvisorComparatorBridgeEngine` — `SpeedFeedTriComparatorEngine` + `GWizardComparatorBridgeEngine` exist on disk but are NOT bound into the action (U-SFC-TRI-COMPARE-WIRE).
- **Missing node:** NO `SpeedFeedCalibrationApplyEngine` / calib-apply engine exists — `U-OSC9-CALIB-APPLY-WIRE` is genuinely orphaned (only generic `StratifiedCalibrationEngine`/`AdaptiveCalibrationEngine`/`PhysicsAutoCalibrationEngine` exist; none wire calibrated SFC back into `sfc_nine_axis_run`).
- **Empty envelope:** `mcp-server/data/milestones/MS-SFC-CALIBRATE.json` has `units.length === 0`, `status:not_started` — the "24-unit" milestone is an unauthored shell (U-SFC-CALIB-AUTHOR).
- **6 unwired `Speed*` engines** (romeo's BUILD_STATE batch, `Speed:6`) directly serve oscar but sit dark — coordinate with romeo's "wire 110 unwired ≤5/commit" work order rather than re-wiring in oscar's lane.
- **Bridge present but unverified end-to-end:** `SpeedFeedPropagationBridgeEngine` → echo per-NC-block F/S has no passing E2E asserting byte-correct F/S in an emitted NC block (U-SFC-PROPAGATION-VERIFY).

**RGS-rule updates this domain needs:** The current `rgs-pipeline-rules.mjs` routes SFC units reasonably via the `physics|...|feed|speed` agent rule (→`physics-reviewer`) and `/scrutinize`/`/cam-strategy`/`test-team`. Gaps: (1) **no `speed-feed`/`sfc`/`calibrat` pipeline branch** — "SFC calibration matrix" units fall to the 0.30 GENERIC_FALLBACK `/scrutinize`; add a rule `test: /\bsfc\b|speed[-\s]?feed|\bcalibrat/i → skill: /auto-speed-feed, why: "speed/feed calculation or calibration unit", confidence: 0.80`. (2) **no vendor-parity branch** — `g-wizard|hsmadvisor|toolcrib|tooldb2` should route to a parity/extraction skill (currently only `/pdf-learn` catches the "catalog" token); add `test: /g-?wizard|hsmadvisor|toolcrib/i → skill: /auto-speed-feed`. Both fit the existing keyword-cascade pattern (confidence 0.80, `\b`-bounded) and would lift SFC units off the generic fallback.

---

## ECHO — post-processor RGS PIPELINE ROADMAP (P1)
**North-star (1 line):** Single canonical CAM→controller G-code emitter; saleable MasterPost with byte-equivalence proof vs JM golden NC archive — EDM post pipeline 0.38→1.0, ~40% real machine coverage → 100%, exercised 5-axis-first down through Swiss.

**Time-allocation (from transcripts):** ON-TRACK, ONE redirect. Echo's genuine lane is CIMCO-INTEGRATION-MS0 (179 commits; offline-provable verification spine + adversarial fail-OPEN hunting). The newest precompact drifted onto an alpha BLACKWELL-TOKEN commit during a force-reclaim — **ignore that drift, do not shadow Blackwell GPU work** (alpha/india/golf own it). Spend ~70% on the operator-priority post-proving matrix (units 1-4 below) and ~30% on converting the ~14 dark post engines from `method?.()` fallbacks to real calls — that dark tail is the literal cause of "reachable-on-paper, dark-in-practice."

**Pipeline units (HIGH-ROI ORDERED):**

1. **U-POST-DARK-UNWIRE: convert ~14 stub-wired post engines from `method?.()` dark fallbacks to real executing calls** | ROI=H — a dark engine = a dark post tail; this is echo's #1 leverage move (FLEET-DOMAIN-GOALS Tier-1 #5), unblocks every downstream tier | node=`MasterPostProcessor{AGIOrchestration,Genius,UnifiedAGI}Engine` + `PostProcessorAGIContinuousLearningEngine` + `LathePostGeneratorActiveLearningEngine` + `JMDiePostProcessorLearningEngine` (the `generateAGIPost?.() ?? not-callable` pattern in PATHS.md) | wiring=`prism_cam:master_post_agi_orchestrate` / `master_post_genius_generate` (camDispatcher, make the `?.()` resolve to a real bound method) | bridge=none (intra-galaxy) | rgs-toolchain=`/wire-unwired` (matches the `\bunwired\b|\borphan\b` branch after wedm-exclusion) + agent `wiring-review-agent` | acceptance=`grep -c "method?.()" ` on these engines == 0; each converted engine returns a real UnifiedPostResult, not a fallback sentinel; 3-of-3 PASS.

2. **U-POST-PROVE-MATRIX-5AX: post-proving harness, 5-AXIS tier first** | ROI=H — operator-locked top tier; RTCP/TCPM + singularity is the highest-value, highest-risk emit | node=reuse `MasterPostProcessorUnifiedAGIEngine` (CONTROLLER_PROFILES: haas-ngc/hurco/heidenhain/fanuc/siemens-840d/okuma) + `MillKinematicsCollisionEngine.detectSingularity` gate | wiring=`prism_cimco:verify` (CimcoVerificationBridgeEngine, 6 actions) + `prism_cam` post-emit cases; reuse `scripts/lib/nc-normalize.mjs` (byte-equiv) + `nc-dialect-masks.mjs` | bridge=`consumes`→kilo (`cam_strategy_recommend`→ToolpathBlock) + `consumes`→oscar (`cam_speedfeed_compute` per-block F/S) | rgs-toolchain=`/cam-strategy` (falls through current rules) + agent `physics-review-agent` | acceptance=every JM 5-ax machine emits G43.4/G234 + G68.2, `detectSingularity` fires on a known singular pose, CIMCO sim verdict PASS (fail-CLOSED on empty report), byte-class ∈ {identical, volatile-header-only} — never semantic-drift.

3. **U-POST-PROVE-MATRIX-MILL-LATHE: extend matrix 3-axis mill → mill-turn → Swiss → standard lathe** | ROI=H — covers the JM 5-VMC + 100%-Okuma lathe fleet; the saleable bulk | node=`HaasNGCMillMasterPostEngine` (condition-1/2 proven), `HurcoV11MillMasterPostEngine` (92K WinMAX), `Fusion360MillTurnBridgeEngine`, `LathePostProcessorAIEngine` (73K, currently single-method dark), Citizen/Star LFV Swiss dialects (archaeology) | wiring=`prism_cam` canned-cycle emit (G81/82/83/73/84/85, G96/G50, G76/G92, G12.1/G13.1, M200/M201/WAITM) + `prism_cimco:verify` | bridge=`shares`→foxtrot (mill master-post), `shares`→whiskey (Okuma OSP-P300 corpus) | rgs-toolchain=`/lathe` (structural lathe-branch) + `/mill` + agent `physics-review-agent` | acceptance=each tier × each JM machine byte-matched to golden NC; coolant-before-spindle + rapid-limit + retract gates fire; Swiss guide-bushing/LFV/sub-spindle-pickoff emit cleanly; units-first (zero 25.4× anomalies).

4. **U-POST-WEDM-DIALECTS: wire the 5 WEDM dialect engines so JM wire-EDM posts exist** | ROI=H — closes the absent wire-EDM post (CLAUDE.md "Wire-EDM post absent"); EDM pipeline 0.38→1.0 | node=`WEDMPostMitsubishiEngine` (12K) + `WEDMPostSodickEngine` (10K) + `WEDMPostMakinoEngine` + `WEDMPostAgieEngine` + `WEDMPostFanucEngine` (all stub-wired today) | wiring=`prism_cam` WEDM emit cases (rough/skim/taper/no-core) + `prism_cimco:verify` | bridge=`shares`→mike (WEDM cut-physics for the posted blocks) | rgs-toolchain=`/wedm-program` (the wedm-exclusion branch in rgs rules routes here, NOT /wire-unwired) + agent `code-analyzer` | acceptance=Mitsubishi MV1200R + Sodick/Makino/Agie/Fanuc each emit a valid wire program for a JM held-out part; dialect-lint (8 rules) PASS; mike confirms physics-faithful kerf/skim sequence.

5. **U-POST-P0-MACHINE-GAPS: close the 4 P0 machine-coverage gaps** | ROI=M-H — named real gaps from the post-gen coverage audit (8b6c2c5a1) | node=`MasterPostProcessorEngine` MACHINE_FEATURE_DB + `ShopConfigurationEngine` (21 JM machines) | wiring=`prism_cam` machine-routing (register Haas PRE-NGC dialect, VMC-05 Roku-Roku post, EA sinker, fix FA10S mis-route) | bridge=`consumes`→juliett (CIMCO machine-index for clone) | rgs-toolchain=`/post-register` + `/post-validate` + agent `wiring-review-agent` | acceptance=all 4 machines emit + pass lint + CIMCO-sim; VMC-05 has a registered byte-matched post; FA10S routes to the correct dialect (regression test locks the mis-route).

6. **U-POST-ALARM-AWARE-EMIT: wire the 2,588-alarm DB into PostProcessorPipeline P5** | ROI=M — declared-but-unwired safety surface; cross-checks emitted codes against `controller-alarm-database.json` (13 controllers) | node=`PostProcessorPipelineEngine` (7-phase/38-stage) P5 + `GCodeSafetyAnalyzerEngine` (67K) | wiring=`prism_cam` post-pipeline P5 case reads `controller-alarm-database.json` | bridge=`consumes`→juliett (alarm DB index) | rgs-toolchain=`/post-harden` + `/scrutinize` + agent `code-analyzer` | acceptance=an emit that would trip a known controller alarm (e.g. Okuma G25/G26 signal family) is flagged pre-emit with the alarm id; fail-CLOSED on ambiguous codes.

7. **U-POST-OPT-REPORT: ship `OptimizationReportEngine` as the value artifact (archaeology R14 flagship)** | ROI=M — the #1 sales artifact; lead every paid milestone with the before/after one-pager ("23% cycle saved, 3 tool-life risks, 47s air-cutting, 2 missing HSM codes") | node=`OptimizationReportEngine` (revive — wire to existing `GCodeOptimizationEngine` + `GCodeRuntimePredictorEngine`, do not rebuild emit) | wiring=`prism_cam:optimization_report_generate` (new case in camDispatcher) | bridge=`emits`→charlie (cycle-time delta → quote value-add) | rgs-toolchain=`/program-optimize` + `/post-diff` + agent `reviewer` (completeness-weighted) | acceptance=given a raw vs PRISM-emitted NC pair, report emits all 8 scorecard dims with cited deltas; numbers trace to real `GCodeRuntimePredictor` output (no fabricated %).

8. **U-POST-MASTERPOST-LEGAL-GATE: surface the `U-LEGAL-13` gate for `MS-MASTERPOST` (0/44, BLOCKED)** | ROI=M — fail-loud honesty; do NOT ship copyrighted-derived dialect codes | node=`MasterPostProcessorUnifiedAGIEngine` (gate, not emit) | wiring=`prism_cam` MasterPost surface returns a NEEDS-LEGAL-CLEARANCE flag until codes re-derived from public manuals (Fanuc B-61395E, Haas 96-0284, Mitsubishi IB-1501279, Siemens 840D, Okuma OSP-P300) | bridge=none | rgs-toolchain=`/scrutinize` + agent `reviewer` | acceptance=MasterPost product surface refuses to emit a copyrighted-derived dialect code; the legal gate is visible in the ghost-roost, not silently bypassed.

**External-software training plan (CIMCO Edit, Hurco WinMax, Mastercam X8, hyperMILL):**
1. **CIMCO Edit / CIMCOSimulation — live-sim verification arm (SPINE-2).** The offline arm (`CimcoVerificationBridgeEngine`, `prism_cimco` 6 actions) is shipped; light up the live arm by driving `CIMCOSimulation.exe` headless (bundled MariaDB) against the 86 indexed `.mcfg` machines + 44 `.eRPost` posts (CIMCO inventory already mapped to the 15-machine JM fleet via `U-CIMCO-JM-MACHINE-MAP`). Every U-POST-PROVE-MATRIX tier emits → CIMCO sim → PASS/FAIL verdict captured as the post's proof-of-correctness; resolve the 246-247 same-base-name golden-drift groups by having the operator pick one baseline per part, then byte-diff re-emission vs that golden.
2. **Hurco WinMax — byte-equivalence against live MAX5/WinMAX seat.** `HurcoV11MillMasterPostEngine` (92K) is JM's lead post; re-prove condition-1/2 against the installed WinMax seat (the `U-WINMAX-LIVE-PROBE-FINDING` re-proof pattern) by importing the 12 JM `.cps` (Haas Classic / Hurco WinMAX·MAX5 / Okuma OSP-P300 / Fanuc 31i) from `JM DIE/PRISM MODIFIED POST PROCESSORS/`, emitting the same part through PRISM, and byte-diffing — drift > volatile-header = fail.
3. **Mastercam X8 — golden-NC corpus parity.** Index + parity-validate against the 52 Mastercam posts + the 160,582-NC / 13,790-.cps corpus juliett already DB-indexed: replicate Mastercam's emit for a sample part-set, classify each PRISM emit as byte-identical | volatile-header-only | semantic-drift via the golden round-trip classifier, and feed every divergence to india as a reward label (`xproc_outcome_publish {slot:echo}`).
4. **hyperMILL v31 — live-seat post replication.** `HyperMillCodeGeneratorEngine` (36K) + `HyperMillACServerConfig` (8K) + `hypermill-post-configs.json`: extract the AC-server post configs from the installed hyperMILL v31 seat, replicate its 5-axis RTCP + collision-pre-check emit through PRISM's pipeline, and validate the `Fusion360MillTurnBridgeEngine` mill-turn handoff terminates losslessly into echo's post (kilo owns the lossless `ToolpathBlock` round-trip).

**Atomic gaps (nodes/wirings/bridges):**
- **Dark nodes (stub-wired, `method?.()` not-callable):** `MasterPostProcessorAGIOrchestrationEngine`, `MasterPostProcessorGeniusEngine`, `MasterPostProcessorUnifiedAGIEngine`, `PostProcessorAGIContinuousLearningEngine`, `PostProcessorTransformerEngine`, `CrossCAMPostEngine`, `NovelPostProcessorBridgeEngine`, `HybridPostMergeEngine`, `FusionPostSyncEngine`, `MachineFingerprintEngine`, `PostProcessorTrainerEngine`, `LathePostProcessorAIEngine` (73K, single `getPostProfile` wired), `LathePostGeneratorActiveLearningEngine`, `JMDiePostProcessorLearningEngine`.
- **Unwired dialect nodes:** the 5 `WEDMPost{Mitsubishi,Sodick,Makino,Agie,Fanuc}Engine` (stub-wired).
- **Missing wirings:** `controller-alarm-database.json` (2,588 alarms) → PostProcessorPipeline P5; `OptimizationReportEngine` → `prism_cam:optimization_report_generate` (no case today); camDispatcher stub tail L19871-20022.
- **Missing/weak bridges:** echo→india reward-label publish is single-method (the 3 learning engines are dark); the SFC bridge family (`U-BRIDGE-SFC-{FUSION,HYPERMILL,MASTERCAM,ESPRIT,SOLIDWORKS,INVENTORHSM}`) + `U-BRIDGE-WIRE-{FUSION,HYPER,MASTERCAM}` sit in the eligible queue unbuilt — these are the per-block F/S and toolpath-ingest edges every post-proof rides on.
- **Data gap:** no `controller-dialects/<vendor>.ts` dir exists (only `okuma-dialect-knowledge.ts`); per-vendor dialect knowledge is single-sourced and thin.

**RGS-rule updates this domain needs:** The current `rgs-pipeline-rules.mjs` has **no post-processor branch** — post units fall through to `/cam-strategy` (the `\bcam\b` branch) or the generic `/scrutinize` fallback, which under-routes echo's real toolchain. Add a post-processor branch BEFORE the generic fallback: `test: /\bpost[-\s]?proc|\bpost[-\s]?gen|\bg[-\s]?code\b|\bdialect\b|\bcontroller\b|\bmaster[-\s]?post\b|byte[-\s]?equiv|\bcimco\b|\bwinmax\b/i` → `skill: "/post-generate"` (with `/post-validate`, `/post-harden`, `/post-diff`, `/post-register` as cascade sub-skills) + `agent: code-analyzer`. Word-boundary guard `\bpost\b` against "postpone/poster/postmortem"; ensure the WEDM-exclusion branch still routes wire-EDM-post units to `/wedm-program` first (they should NOT hit the new post branch). This closes echo's slice of the 42% generic-fallback gap and aligns the 8 units above to their real Tier-3 skills.

---

## KILO — cam RGS PIPELINE ROADMAP (P1s)
**North-star (1 line):** Universal CAM brain — any feature set → validated, collision-checked, lossless toolpath for any seat (Fusion/hyperMILL/Mastercam/Esprit), surfaced as an in-seat add-in button (MillTurn 0.92 / MultiAxis 0.91 strong; EDM 0.37 / Laser 0.37 / Grinding 0.52 weak → lift toward 1.0).
**Time-allocation (from transcripts):** REDIRECT. Kilo's freshest sessions drifted onto alpha-authored BLACKWELL-TOKEN units (attribution-shadow, 0 own-domain commits) and a stalled CAM /loop (iter 1/20). Re-anchor on the cam galaxy: P1 = the operator-priority toolpath-feeds-echo spine + Fusion/Mastercam add-in revenue surface, then lift the 3 weak CAM-adjacent pipelines. Stop borrowing precompact RESUME-lines from alpha's milestone.

**Pipeline units (HIGH-ROI ORDERED):**

1. **U-CAMM-FUS-D1: Fusion "Speed&Feed via PRISM" in-seat add-in button** | ROI=H — Revenue Day 1, single largest unbuilt revenue surface; oscar's calibrated SFC rides this same pillar D | node=`CAMAddInFrameworkEngine` (wire to `UltimateSpeedFeedEngine` via `cam_speedfeed_compute`) | wiring=`prism_cam:mastercam_addin_generate` + new Fusion D1 action on `camFunctionDispatcher` | bridge=cam↔oscar (`cam_speedfeed_compute`→ToolpathBlock) | rgs-toolchain=`/cam-strategy` + `wiring-review-agent` | acceptance=add-in button calls live `sfc_nine_axis_run`, returns physics-valid RPM/feed per selected tool in-seat, requireTier gate fires; E2E round-trip test asserts non-default F/S.

2. **U-CAMM-FUS-D2: Fusion "Auto-program via PRISM" add-in button** | ROI=H — completes the auto-programming chain (feature→strategy→toolpath) in-seat; the F360-REV per-block-physics un-bypass | node=`CAMAGIMasterOrchestratorEngine` + reroute AutoProgram S10→`PostProcessorPipelineEngine` | wiring=`prism_cam:cam_strategy_recommend`→`toolpath_generate` chained behind D2 action | bridge=cam↔echo (toolpath→post) + cam↔delta (`feature_recognize`→strategy) | rgs-toolchain=`/cam-strategy` + `code-analyzer` | acceptance=button drives features→collision-checked toolpath with per-block variable S/F (not bypassed); asserts S10 reroutes through 38-stage pipeline.

3. **U-CAMX-TOOLPATH-LOSSLESS-ECHO: prove lossless `toolpath_generate`→ToolpathBlock→echo for all 6 post tiers** | ROI=H — the PRIORITY-1 dependency; echo's post-proof matrix needs real toolpaths carrying strategy+tool-list+WCS for 5ax/3ax/mill-turn/swiss/lathe/wedm | node=`CAMKernelEngine` + `CAMCrossSystemTranslatorEngine` (`cam_cross_translate`) | wiring=`prism_toolpath:toolpath_generate` + `collision_check_full` mandatory gate before emit | bridge=cam→echo (NCI/APT/ToolpathBlock lossless) | rgs-toolchain=`/cam-strategy` + `test-team` + `wiring-review-agent` | acceptance=per-tier round-trip test: `ToolpathBlock`→echo preserves strategy/tool-list/WCS byte-for-byte; collision gate fires on every path (no ungated emit).

4. **U-CAMX-EDM-STRATEGY-WIRE: wire EDM strategy stage into `cam_strategy_recommend` (machine-domain keyed)** | ROI=H — EDM pipeline 0.37 (worst CAM-adjacent); `edmMultiPassStrategyEngine` IS wired (3 actions @ camDispatcher:7615) + `_edmAsm` declared but the strategy *recommender* isn't EDM-keyed; unblocks echo wire-post + mike's WEDM loop | node=`EDMMultiPassStrategyEngine` + `_edmAsm` (EDM tool-assembly) → route under `cam_strategy_recommend` | wiring=`prism_cam:cam_strategy_recommend` machine-domain branch `wedm` | bridge=cam↔mike (`cam_strategy_recommend` wedm-keyed) + cam↔echo (5 WEDM dialects) | rgs-toolchain=`/cam-strategy` + `wiring-review-agent` | acceptance=`cam_strategy_recommend({machine_domain:'wedm'})` returns EDM multi-pass strategy (not null/mill-default); EDM reachability 0.37→≥0.6.

5. **U-CAMX-GRIND-LASER-STRATEGY-WIRE: wire Grinding + Laser strategy stages (machine-domain keyed)** | ROI=M — lifts Grinding 0.52 / Laser 0.37; `_grindAsm`/`_laserAsm`/`_wjAsm` declared in camDispatcher but not strategy-recommender-keyed | node=`_grindAsm` + `_laserAsm` assemblies → `cam_strategy_recommend` grinding/laser branches | wiring=`prism_cam:cam_strategy_recommend` machine-domain branches `grinding`/`laser` | bridge=cam↔foxtrot (grinding cut physics) | rgs-toolchain=`/cam-strategy` + `code-analyzer` | acceptance=both domains return a real strategy keyed by machine_domain; Grinding 0.52→≥0.7, Laser 0.37→≥0.6; no silent mill-fallback.

6. **U-MASTERCAM-WIRE: wire the unwired `eng.mastercam` node (L5/unwired) + Mastercam 3-button bridge** | ROI=M — only unwired CAM node at L5; Mastercam X8 is a live JM seat; `mastercam_strategy_recommend`/`_params` actions exist (camDispatcher:1304) but the engine node sits dark | node=`mastercam` engine (wire into `prism_cam`) | wiring=`prism_cam:mastercam_addin_generate` + `mastercam_strategy_recommend` round-trip | bridge=cam↔echo (Mastercam post dialect) | rgs-toolchain=`/wire-unwired` + `wiring-review-agent` | acceptance=`eng.mastercam` shows wired in system-viz; Mastercam strategy round-trips through dispatcher; CAM-vendor wiredPct lifts.

7. **U-CAM-LOOP-CLOSE: close the CAM closed-loop /loop (learn LATHE_OP_ORDER from corpus, iter 1/20→20/20)** | ROI=M — resumes kilo's own open self-improving loop (iter1 shipped `446dc68261`, 19 iters unclosed); publishes outcomes to india's GNN | node=`CAMFeedbackLoopEngine` (closed-loop tap) + corpus pairwise-preference learner | wiring=`xproc_outcome_publish {slot:kilo, domain:cam}` (auto via `outcome-bus-auto-tap.mjs`) + `xproc_kg_project_features` | bridge=cam↔india (strategy embeddings → GNN tier-5) | rgs-toolchain=`/cam-strategy` + `test-team` | acceptance=20-iter loop closed; learned op-order beats baseline on held-out JM corpus pairwise accuracy; outcomes land in india's calibration record.

8. **U-CAMX-COLLISION-GATE-UNIVERSAL: mandatory `collision_check_full` clearance gate on every generated path feeding any post** | ROI=M — safety-critical; today the gate exists (camDispatcher:2494) but isn't asserted on every post-feeding path | node=`MillKinematicsCollisionEngine.detectSingularity` (5-ax) + `collision_check_full` | wiring=`prism_toolpath:collision_check_full` enforced pre-`post_process` | bridge=cam↔foxtrot (5-ax singularity) | rgs-toolchain=`/cam-toolpath-check` + `code-analyzer` | acceptance=every toolpath→post path asserts a passing collision verdict (fail-CLOSED on missing report); 5-ax singularity gate fires on RTCP paths.

**External-software training plan (hyperMILL v31, Mastercam X8, Esprit, Fusion 360):**
1. **Corpus extraction from installed seats** — parse the live JM `.hmc`/`.nc` (hyperMILL v31) + Mastercam X8 `.mcam`/`NCI` archives via `HMCProjectParser` + `CAMCrossSystemTranslatorEngine`, building strategy→outcome training pairs (which strategy/stepover/stepdown each seat actually chose per feature). Tag by ISO material + machine_domain; cite source file (R9, no fabrication). Feeds `ToolpathStrategyDB` (586) and U-CAM-LOOP-CLOSE pairwise learner.
2. **Parity validation against the tool's own output** — for a fixed feature set, run `cam_strategy_recommend` → `toolpath_generate` and diff the resulting NCI/cut-list against what hyperMILL v31 and Mastercam X8 emit in-seat (Esprit/Fusion 360 where seats permit). Assert PRISM's strategy is within tolerance of the seat's (stepover, engagement, retract scheme) or surface an honest divergence — this is the saleable "matches your seat" proof.
3. **Workflow replication via Fusion 360 add-in (D1/D2/D3)** — replicate Fusion's CAM/HSM dialog workflow through `CAMAddInFrameworkEngine` so the add-in button reproduces the in-seat auto-programming path (U-CAMM-FUS-B/D); validate each Fusion 2D/3D/5-ax/turn/mill-turn strategy maps to a PRISM strategy node.
4. **Library import** — import each seat's tool/holder/machine libraries (hyperMILL `.hmt`, Fusion `.machine`, Mastercam tool DB) via romeo's `cimco_toollib_export` reverse path so the CAM strategy recommender selects from the seat's *real* tool crib, not a generic catalog; round-trip-verify the export.

**Atomic gaps (nodes/wirings/bridges):**
- **Unwired node:** `eng.mastercam` (L5/unwired in system-viz — the only dark CAM engine node) → U-MASTERCAM-WIRE.
- **Declared-but-unkeyed wirings:** `_edmAsm`, `_grindAsm`, `_laserAsm`, `_wjAsm` are declared in `camDispatcher.ts:192` but `cam_strategy_recommend` is not machine-domain-keyed for EDM/Grinding/Laser/Waterjet → the 0.37-0.52 weak pipelines (U-CAMX-EDM/GRIND-LASER).
- **Missing bridges:** cam→echo lossless `ToolpathBlock` round-trip is unproven for the 6 post tiers (U-CAMX-TOOLPATH-LOSSLESS-ECHO); cam↔mike WEDM-dialect strategy edge is one-directional (echo declares kilo, mike reciprocation unverified); the Fusion D1/D2/D3 in-seat actions (`mastercam_addin_generate` + Fusion D-actions) do not yet exist on `camFunctionDispatcher`.
- **Orphan milestones (real, off-roadmap):** CAMX-MS0.5..MS11 + MS-CAM-MASTERY (3/34) + CADCAM-AGI-MS0 (24u never_started) + CADCAM-DAGI-MS1/2/3/5 (~50u never_started) + CAM-ML-CLOSEDLOOP-MS0 (15u) sit `in_progress_real`/`never_started` with shipped-but-unreconciled units — needs a roadmap-index promotion pass.

**RGS-rule updates this domain needs:** The `/cam-strategy` rule (`/\bcam(?:ming)?\b|\btoolpath\b/i`, conf 0.75) under-routes the in-seat add-in + vendor-bridge units — add a branch routing `add-in|addin|in-seat|fusion (button|pillar)|mastercam bridge|hyperMILL bridge|F360-REV` → `/cam-bridge` (the existing add-in skill) at conf 0.80, paired with `wiring-review-agent`. Add a machine-domain-keyed strategy branch so `EDM/Grinding/Laser strategy stage` units route to `/cam-strategy` rather than falling to the generic 0.3 fallback. (The wire-EDM polysemy guard already correctly excludes `/wire-unwired` false-matches — leave it.) Otherwise the current rule table is adequate for the CAMX-* toolpath block.

---

## FOXTROT — mill RGS PIPELINE ROADMAP (P1s)
**North-star (1 line):** Deepest-volume print→proven-program for JM's 5-VMC fleet, every parameter physics-defended — PrintToProgram 0.90 / MultiAxis 0.91 / MillTurn 0.92 → 1.0 (MASTER-FLEET line 67).
**Time-allocation (from transcripts):** On-track — foxtrot's genuine lane is PRINT-TO-PROGRAM-REPLICATION-MS0 + the mill self-improving knowledge backbone (PDF→video→cited-tribal→outcome-loop→replicate-from-print); among the healthiest galaxies. **Redirect note:** the newest session (claude-501bd704) drifted into bravo's HERMES orchestrator work — stay in mill lane. P1-support role means foxtrot supplies proven 3-axis + 5-axis cut physics + machine coverage for echo's 5-axis-first and 3-axis post tiers; do that BEFORE the fallback BRIDGE/tribal-graph cluster.

**Pipeline units (HIGH-ROI ORDERED):**
1. **U-P2P-CORPUS-LOADER: real `.hmc/.nc` corpus loader for replicate-from-print** | ROI=H — operator-priority Tier-2 #10; closes mill PrintToProgram 0.90→end-to-end; `replicate_from_print` runs against actual shop programs not synth | node=build `HMCProjectParser` (does NOT exist on disk) feeding existing `MillProgramReplicationEngine` over `JM DIE/HURCO CNC PROGRAMS/` (25 `.hnc`) + `JM DIE/CNC MILL HAAS/` (469 files) | wiring=`multiAxisProgramDispatcher:replicate_similarity_search` (engine already wired there) | bridge=consumes echo CIMCO machine-defs + juliett machine-index (cross-slot) | rgs-toolchain=`/cad-from-blueprint` (print-intake) + `/mill` + test-team + physics-reviewer | acceptance=`replicate_similarity_search` E2E returns ≥1 real JM program for a held-out print; 4-axis fixture path covered; units-first (no 25.4× on `.hnc` inch programs)
2. **U-VMC05-ROKU-POST: register VMC-05 Roku-Roku post** | ROI=H — the literal P0 machine gap (jm-die-profile L252 `// no post yet — engine surfaces no_post_available`); 1 of 5 VMCs has zero post coverage | node=wire existing `RokuRokuParserEngine` (today only on `dataDispatcher`) into the post/master-post path + `MillKinematicsCollisionEngine` for the Fanuc 31i-B5 5-axis kinematics | wiring=register post in `jm-die-profile.ts` VMC-05 + `multiAxisProgramDispatcher` post-emit; shared with echo `HaasNGCMillMasterPost`-class | bridge=echo (post byte-class verify via `nc-normalize.mjs`) | rgs-toolchain=`/mill` + `/cam-post-lint` + physics-reviewer (Fanuc 31i 5-ax G43.4/G68.2) | acceptance=VMC-05 emits a byte-class=identical|volatile-header-only post for a JM Roku-Roku program (no `no_post_available`); CIMCO sim verdict PASS fail-closed
3. **U-MILL-MS4-CUTTERCOMP: revive real-geometry toolpath + G41/G42 cutter-comp + multi-setup WCS** | ROI=H — archaeology #8/Tier-2; "zero production value without it"; `PrintToProgramPipelineEngine` scaffold emitted hardcoded X0/Y0 | node=`MillingPrintToProgramEngine` + `AdvancedMillingStrategiesEngine` (pocket spiral/zigzag/morph, helical/ramp entry, rest-machining, G54-59 datum transfer) | wiring=`prism_mill:print_to_program` / `cncOpsDispatcher` mill-op route | bridge=queries oscar `cam_speedfeed_compute` per pass; terminates in echo Hurco/Roku post | rgs-toolchain=`/mill` + `/program-validate` + physics-reviewer + test-team | acceptance=pocket+profile emits real cutter-comp arcs (lead-in/out, no hardcoded X0/Y0); multi-setup WCS datum-transfer correct; G41/G42 lint-clean
4. **U-HYPERMILL-SENTINEL: ship `hypermill/CLAUDE.md` sub-galaxy sentinel + wire 3 unwired `Hyper*` engines** | ROI=M — closes stop_on_unwired_assets orphans; Bibryam P1 cascade missing for the ~17+50 `Hyper*` sub-galaxy | node=write `mcp-server/src/engines/hypermill/CLAUDE.md` (confirmed absent) + identify+wire the 3 orphan `Hyper*` (audit `HyperMillDeepLearning`/`HyperMillBladeRoughing`/`HyperMillDentalBlankRouter` actual dispatcher refs) | wiring=`camDispatcher` `cam_hypermill_*` (existing surface) | bridge=kilo CAM strategy (HyperMILL is kilo-adjacent) | rgs-toolchain=`/wire-unwired` + wiring-review-agent + `/dedup` | acceptance=`hypermill/CLAUDE.md` ≤200 lines loads on edit-under-dir; 3 named engines have ≥1 dispatcher ref; stop_on_unwired_assets clean for `Hyper*`
5. **U-MILL-NEURAL-EMBED: embed 24,545 JM programs via `GCodeUnderstandingTransformer` (5 ONNX/GNN mill engines)** | ROI=M — archaeology #6/Tier-1; NN-GRAPH infra now exists (blocker gone); unlocks similarity/anomaly search feeding U-P2P retrieval | node=`MillStrategyNeuralEngine` (built) + revive `GCodeUnderstandingTransformer`/`ChatterNeuralClassifier`/`SurfaceFinishCnn`/`ToolLifeGnn` over the JM corpus | wiring=`millDispatcher` mill-neural action + feeds india GNN node-embeddings | bridge=india (`xproc_outcome_publish {slot:foxtrot}`; node-embedding-768d bridge) — gated by india deploy-gate clearance (F0) | rgs-toolchain=`/ai-train-india` template + `/mill` + physics-reviewer | acceptance=24,545 programs embedded; similarity query returns geometry-near neighbors; honest lower-bound metric (no fake AUROC), feeds U-P2P retrieval ranker
6. **U-MULTIAXIS-RTCP: harden `MultiAxisPrintToProgramEngine` 5-axis (RTCP/TCPM G43.4, singularity, G68.2)** | ROI=M — archaeology #23 most-flagged stub; filter to M460V indexed 3+2 first (shop reality) then simultaneous-5 | node=`MultiAxisPrintToProgramEngine` (exists, wired to multiAxisProgramDispatcher) + `MillKinematicsCollisionEngine.detectSingularity` gate | wiring=`multiAxisProgramDispatcher` (existing) | bridge=echo 5-axis post tier-1; oscar F/S | rgs-toolchain=`/mill` + `/cnc-simulate` + physics-reviewer + test-team | acceptance=A=0 singularity gate fires before generate; G68.2 tilted-plane + G43.4 emitted; collision-clear on M460V 3+2 fixture
7. **U-MILL-BRIDGE-CLUSTER: wire mill shop-floor BRIDGE cluster (fallback, queue-eligible)** | ROI=M — ROADMAP-CONSOLIDATED Deep-integration; only after P1-support core lands | node=bridge wirings `U-BRIDGE-WIRE-MACHINE/SHOP/SENSOR` + `U-BRIDGE-SHOPFLOOR-LEARN` + `U-BRIDGE-OPERATOR-GATES` (all in eligible queue) | wiring=`millDispatcher` + shop-floor dispatcher | bridge=shop-floor telemetry→`TribalTipOutcomeBridgeEngine` learning loop; operator-gates on autonomous output | rgs-toolchain=`/wire-unwired` + wiring-review-agent | acceptance=shop-floor signal reaches the outcome loop; operator gate blocks un-reviewed autonomous post

**External-software training plan (hyperMILL v31, Hurco WinMax, Mastercam, G-Wizard, HSMAdvisor):**
- **hyperMILL v31 (live seat):** Extract the installed seat's demo/standard tool DB + cycle catalog via existing `HyperMillDemoDbExtractor`/`HyperMillIMDbExtractor`/`HyperMillStandardToolDB` → `.hmt` parity; replicate its cycle-parameter defaults into `HyperMillCycleDefaultsEngine` and **byte-validate** PRISM-emitted hyperMILL macro output against macros the seat generates for a known JM pocket/contour job (the `HyperMillCodeGeneratorEngine` round-trip). Cite every imported parameter (R9).
- **Hurco WinMax (installed):** Parse the 25 JM `.hnc` WinMax programs (the `JM DIE/HURCO CNC PROGRAMS/` corpus) as ground-truth labels; replicate WinMax conversational→ISO behavior and **byte-match `HurcoV11MillMasterPostEngine` output** against the live WinMax sim (condition-1 spine already re-proven vs live Mill sim per `U-WINMAX-LIVE-PROBE-FINDING`) — extend to canned-cycle + 5-axis coverage.
- **G-Wizard + HSMAdvisor (installed):** Drive the existing G-Wizard/HSMAdvisor adapters (oscar's `settings_v2.xml` live-state adapter) across the mill cut-physics matrix and **parity-validate PRISM SFC RPM/feed against their outputs** for the JM tool crib per ISO group — every divergence >tolerance becomes a calibration delta, not a silent default. This is foxtrot supplying the cut-physics that oscar's SFC matrix posts.
- **Mastercam X8 (live seat):** Import Mastercam NCI/posted-NC for JM parts as additional replication corpus + ToolpathBlock parity; cross-check `cam_cross_translate` (kilo's `CAMCrossSystemTranslatorEngine`) hyperMILL↔Mastercam strategy round-trip so U-P2P retrieval is seat-agnostic.

**Atomic gaps (nodes/wirings/bridges):**
- **Missing node:** `HMCProjectParser` — named in the work order + handoffs but **does not exist on disk** (`ls *HMC*.ts` → none); U-P2P-CORPUS-LOADER must build it over `MillProgramReplicationEngine`.
- **Missing sentinel node:** `mcp-server/src/engines/hypermill/CLAUDE.md` — confirmed absent (Bibryam P1 cascade dark for the whole HyperMILL sub-galaxy).
- **Wiring gap:** `RokuRokuParserEngine` wired only to `dataDispatcher` — not on the millDispatcher/post-emit path; VMC-05 has `// no post yet` in `jm-die-profile.ts` L252.
- **Orphan nodes:** the "3 unwired `Hyper*`" + "last 1 unwired mill engine" (MASTER-FLEET L70) — audit `HyperMillDeepLearningEngine`/`HyperMillBladeRoughingEngine`/`HyperMillDentalBlankRouter` for real refs; dormant `GCodeUnderstandingTransformer`+4 ONNX/GNN mill engines (specced, never trained).
- **Missing output stage:** `PRISM_UPGRADED/` mill output pipeline (lathe v2.0.0 never run on mill — PATHS.md confirms no `PRISM_UPGRADED/` mill dir).
- **Bridge gaps:** foxtrot→india node-embedding bridge gated by india F0 deploy-gate; foxtrot→echo VMC-05 post byte-class bridge (new).

**RGS-rule updates this domain needs:** The existing `/mill` rule (`\bmill(ing|-turn)?\b`) + `/cam-strategy` + `physics-reviewer` cover most foxtrot units, but three patterns currently fall through to the generic `/scrutinize` fallback:
- **`replicate-from-print` / `replication` / `print-to-program-replication`** → no rule matches "replicate" today; add a rule routing to `/cad-from-blueprint` + `/mill` (these are the highest-value foxtrot units and they get the 0.30 generic fallback).
- **`master-post` / `post-register` / `roku-roku` / VMC machine-registration** → add a post-domain rule routing to `/cam-post-lint` + `/post-register` (post-tier work is core P1 and currently only catches via the broad `/cam` token).
- **`hypermill` / `hmt` / `cycle-catalog`** → a `/hypermill-*` skill family exists (hypermill-rough/finish/full-job/etc.) but no rgs-rule routes to it; add a `\bhypermill\b|\b\.hmt\b` → `/hypermill-full-job` (or `/cam-strategy`) rule so HyperMILL sub-galaxy units stop hitting the generic fallback.

---

## WHISKEY — lathe RGS PIPELINE ROADMAP (P1s)
**North-star (1 line):** Any turned-part print → crash-safe, controller-ready lathe program for JM's 100%-Okuma OSP fleet; Turning pipeline 0.74-0.78 → 0.92 → 1.0 (PRIORITY-BUILD-PLAN WORK ORDER → whiskey; FLEET-DOMAIN-GOALS Tier-2 #9).

**Time-allocation (from transcripts):** ON-TRACK, stay the course — whiskey rightly moved from breadth (wiring all `LatheAI*` engines + training 16,558 programs) to depth/rigor (honest 41.6% roundtrip floor vs JM Okuma .MIN ground truth). The redirect: the operator-locked PRIORITY-1 now positions whiskey as **Swiss + standard-lathe cut-physics SUPPORT for echo's post tiers 3-4**, so couple accuracy-gap closure with feeding echo's post-proof (producer-before-consumer). Do NOT start net-new AI engines — wire-don't-rebuild; the queue is empty (0/0) so run off the named MS0 line, not blind queue pickup.

**Pipeline units (HIGH-ROI ORDERED):**
1. **U-LATHE-MATERIAL-INFER-FROM-MIN: material inference from .MIN (non-speed-circular)** | ROI=H — root cause of the 41.6% floor; gates every downstream accuracy number | node=`TurningPrintToProgramEngine.deriveInput()` (in `lathe-print-to-program-roundtrip-accuracy.ts` harness) + new `LatheMaterialInferenceEngine` (infer from .MIN comments/customer/family, NOT speed) | wiring=`prism_turning:material_infer` (turningDispatcher) | bridge=none | rgs-toolchain=`/lathe` + `physics-reviewer` (per-file 2-reviewer gate, safety-relevant) | acceptance=material correctly inferred on ≥20/24 Rung-A samples without reading SFM; mean accuracy floor lifts measurably above 41.6%.
2. **U-LATHE-JM-SHOP-SFM-FEED-CALIBRATE: JM-shop SFM/feed calibration override** | ROI=H — textbook ISO-P runs 2-4× JM's conservative practice; this is the in-band fix | node=`TurningPrintToProgramEngine` (Ra-driven finish feed; calibrate to Rung-A cloud: finish .0025IPR/200SFM, rough .007IPR/250SFM) + `LatheActualFeedback` capture | wiring=`prism_turning:sf_calibrate_apply` | bridge=CONSUMES oscar CSS/IPR via `SpeedFeedPropagationBridgeEngine` (oscar→whiskey edge) | rgs-toolchain=`/auto-speed-feed-lathe` + `physics-reviewer` | acceptance=SFM err 8.5%→<3%, IPR err 6.3%→<3% on the 24-sample baseline; G50/CSS cap still passes.
3. **U-TURNING-STRATEGIES-REGISTRY-CONNECT: connect the 4th (strategies) registry into Turning** | ROI=H — the named 0.74→0.92 reachability lever, matching MillTurn | node=wire existing strategy registry into `turningDispatcher` Turning stages (today only materials/tools/machines registries reach it) | wiring=`prism_turning:strategy_recommend` (turningDispatcher) | bridge=none (intra-galaxy) | rgs-toolchain=`/lathe` + `wiring-review-agent` | acceptance=Turning reachability 0.74→0.92; strategy selected per op (OD/ID/thread/groove/part-off) in roundtrip; no `method?.()` dark fallback.
4. **U-MILLTURN-SWISS-ASSEMBLE-PROGRAM: revive `MillTurnSwissPipelineEngine.assembleProgram()` (dormant MT-MS4/MS7)** | ROI=H — archaeology #18; serves JM's Multus B250II; the empty `assembleProgram()` is the literal gap; supplies echo's tier-3/4 cut physics | node=`MillTurnSwissPipelineEngine` + existing `Swiss{GuideBushingPhysics,PartTransferSequence,ChannelFileEmitter}Engine` + `SubSpindleHandoffVerifier` (channel-sync M200/M201/WAITM, bar-feeder M99) | wiring=`prism_turning:millturn_assemble` + `prism_cam:lathe_post_process` | bridge=`Fusion360MillTurnBridgeEngine` (lathe↔mill mill-turn handoff) → FEEDS echo tiers 3-4 | rgs-toolchain=`/lathe` + `physics-reviewer` (collision/sub-spindle phase tolerance is safety-critical) | acceptance=Swiss guide-bushing/LFV/pickoff + sub-spindle transfer emits non-empty program through `lathe_partoff_safety_gate` + 0.5° phase tolerance; echo round-trips the `ToolpathBlock` lossless.
5. **U-LATHE-BRAIN-MERGE+ENVELOPE-FIX: merge unmerged `H:/prism-slot-whiskey` 6-file galaxy brain + fix LATHE-MASTER envelope drift** | ROI=M — discovery is blind until merged; `lathe-gcode-lint.mjs` (the 8-gotcha lint BRAIN) + 7 hermetic libs live only in the slot worktree | node=merge `scripts/lib/lathe-gcode-lint.mjs`, `lathe-g76-thread-validator.mjs`, `lathe-shop-tool-library-bridge.mjs` etc. to shared tree; flip `LATHE-MASTER` envelope `not_started`→`in_progress_real` (BUILD_STATE line 231 drift) | wiring=`/lathe-lint` CLI + PostToolUse `lathe-gcode-lint-guard.mjs` | bridge=none | rgs-toolchain=`/wire-unwired` + `reviewer` | acceptance=8-gotcha lint reachable from shared tree; `system-viz-query find lathe` shows the brain (not blind); envelope no longer claims `not_started` while units shipped.
6. **U-LATHE-MASTER-POST-PRODUCTIZE: lathe Master Post productization parity with mill/SFC** | ROI=M — the post-proof rides on a productized lathe post; echo owns dialect-emit, whiskey owns the cut-physics half it consumes | node=existing `lathe{MasterPost,MasterPostUnifiedOutput}ActionSchemas` + Okuma OSP-P300 emit (JM 100%-Okuma) | wiring=`prism_cam:lathe_post_process` + `lathe_sf_*` (camDispatcher) | bridge=terminates in Okuma OSP post (shared `JM DIE/POST PROCESSORS` corpus with echo) | rgs-toolchain=`/lathe-master-post` + `reviewer` | acceptance=standard OD/ID/thread/groove/part-off + Okuma OSP emit byte-class clean (byte-identical | volatile-header-only, no semantic-drift) through `nc-normalize.mjs`.
7. **U-LATHE-AUDIT-PIPELINE-GENERALIZE: generalize 3-stage audit pipeline (mill/wedm/welder template)** | ROI=M — `LatheProgramAuditPipelineEngine` ran 114,646 variants + caught the body-rescale bug; the `U-AUDIT-FINDINGS-BRIEF` flagged generalizing the template | node=`LatheProgramAuditPipelineEngine` → parametrize per-domain envelope-fit gate | wiring=`prism_turning:audit_pipeline_run` | bridge=FEEDS juliett (DB-index audit results) | rgs-toolchain=`/lathe` + `test-review-agent` | acceptance=audit pipeline runs on a second domain corpus with envelope-fit gate firing on real over-envelope programs (the 99.88% FAIL class), not a stub.
8. **U-LATHE-CLOSED-LOOP-TRAIN: train operator-override capture (30-experience threshold)** | ROI=M — `WHISKEY-LATHE-CLOSED-LOOP-MS0` captured overrides; the self-improving half is the FLEET-DOMAIN-GOALS "proven by training" status | node=`LatheContinuousLearningEngine` + override store from `U-CL1-OPERATOR-OVERRIDE-CAPTURE` | wiring=`prism_ai` (operator SFM-override) | bridge=PUBLISHES outcomes→india (`xproc_outcome_publish`) ; `LatheActualFeedback`→hotel | rgs-toolchain=`/lathe-lora` + `test-review-agent` | acceptance=≥30 captured overrides trigger a real retrain; calibrated SFM/IPR shifts toward operator behavior (not a no-op promote).

**External-software training plan (Mastercam, Fusion 360, CIMCO):**
1. **CIMCO offline byte-class proof of every lathe post** (live `CIMCOSimulation.exe` sim is blocked offline per FLEET-1MONTH §123, so use the shipped offline arm): run U-MILLTURN/U-MASTER-POST emitted Okuma OSP + standard-lathe NC through echo's `CimcoVerificationBridgeEngine` (`prism_cimco`, 6 actions) + `nc-normalize.mjs`/`nc-dialect-masks.mjs` golden classifier vs the `JM DIE/POST PROCESSORS` corpus — assert byte-identical | volatile-header-only for each cut cycle; this is the post-proof acceptance gate.
2. **Mastercam corpus extraction (Mastercam X8 live seat)** via `MastercamMillTurnBridge` + `MastercamMillTurnFunctionIndexEngine` — index every Mastercam lathe/mill-turn toolpath operation (rough/finish/groove/thread/part-off + C-axis polar) into the strategies registry (U-3) so Turning strategy recommendations are parity-validated against Mastercam's own operation taxonomy, not invented.
3. **Fusion 360 mill-turn workflow replication** via `Fusion360MillTurnBridgeEngine` — replicate the Fusion turning + sub-spindle/Multus workflow (CAD feature → turning op order → `ToolpathBlock`) and parity-diff PRISM's `assembleProgram()` output (U-4) against a Fusion-posted golden for the same JM Multus B250II part; close any op-order divergence by feeding it into the closed-loop capture (U-8).
4. **Vendor library import (already-installed seats/catalogs)**: re-validate the 14-vendor insert/holder catalogs (`turning-vendor-catalog-loader.ts` — Sandvik/Tungaloy/Kennametal/ISCAR/Mitsubishi/Korloy) against G-Wizard + HSMAdvisor `settings_v2.xml` live-state for SFM/IPR sanity, and import Okuma OSP program examples (`okuma-osp-program-examples.ts`, ~120K) as the dialect ground-truth corpus for U-1/U-2 material+calibration inference. Ollama (qwen2.5-coder @127.0.0.1:11434) offloads .MIN comment summarization for material inference.

**Atomic gaps (nodes/wirings/bridges):**
- **Empty node:** `MillTurnSwissPipelineEngine.assembleProgram()` — declared, body empty (archaeology #18, MT-MS4/MS7). The keystone dormant build.
- **Unwired/dark:** 4th (strategies) registry NOT connected into `turningDispatcher` Turning stages (today only materials/tools/machines reach it) — the named 0.74→0.92 gap.
- **Orphan brain:** `H:/prism-slot-whiskey` 6-file galaxy brain (CLAUDE/MEMORY/PATHS/TOOLBELT/GSD/KNOWLEDGE) + `lathe-gcode-lint.mjs` + 7 hermetic libs UNMERGED to shared tree — `system-viz-query find lathe` shows `eng.lathe` in `L5/unwired`; discovery blind.
- **Envelope drift:** `LATHE-MASTER not_started` (BUILD_STATE line 231) while units shipped → `claims_not_started_but_has_shipped_units`. Z3/SMT G-code verification + predictive pre-play twin substrate still dormant.
- **Missing bridge:** lathe Master Post → echo tier-3/4 post-proof edge depends on U-4 + U-6; today echo's Swiss/mill-turn tiers lack whiskey cut-physics. Swiss BUILD_STATE coverage is 4/6 wired (2 unwired, line 325).

**RGS-rule updates this domain needs:** The `/lathe` route (lines 100-107) and `physics-reviewer`/`test-review-agent` agent rules already cover this domain well. Two additions would sharpen routing: (1) add a **`/lathe-master-post` skill route** keyed on `lathe.*master.*post|lathe.*postgen|OSP|\.MIN` so post-productization units (U-6) route to the post pipeline, not the generic `/lathe` orchestrator; (2) add a **mill-turn/Swiss structural test** — `\bmill[-\s]?turn\b|\bswiss\b|\bsub[-\s]?spindle\b|\bguide[-\s]?bushing\b` → `/lathe` + `physics-reviewer` (sub-spindle phase + collision is safety-critical) so dormant-revival units like U-4 don't fall through to the generic-fallback confidence 0.3 bucket. Otherwise: none.

---

## MIKE — wedm RGS PIPELINE ROADMAP (P1s)
**North-star (1 line):** Deepest fully-closed-loop domain — print→EDM-program with self-improving discharge-physics AI; EDM pipeline 0.38 (worst in fleet) → 0.72 → 1.0.
**Time-allocation (from transcripts):** REDIRECT toward soul. The last 30 days drifted broad (COMMAND-KERNEL, BRIDGE-WIRING, CLOSE-OUT, a borrowed BLACKWELL-TOKEN precompact line) and only the final ~10 days hit wedm (TRAINING-WIZARD audit → P2P-accuracy ACU-family fix). Spend ≥80% on the in-flight WEDM-P2P-ACCURACY TASK #3 (real G-code emit vs held-out JM corpus) and the dataset/registry blockers below; treat the DB-GAP queue units (`U-GAP-DB-*`, `U-DB0x`) as fallback only. Note U-WTW-AUDIT recommends routing the *training-wizard build* to charlie (canonical wire slot) — mike owns the accuracy harness + registry/dialect wiring, coordinate to avoid double-build.

**Pipeline units (HIGH-ROI ORDERED):**

1. **U-WTW00: Fix the 0-byte `WEDMLoRADatasetBuilderEngine.ts`** | ROI=H — confirmed genuinely 0 bytes; blocks `wedm_lora` r=4 training (U-TRAIN-13) and the entire self-improving half of the galaxy; producer-first per R13 | node=`WEDMLoRADatasetBuilderEngine` (build the real engine: emit print→params training pairs from JM `WIRE EDM/` programs + `jm-die-wedm-program-patterns.ts`) | wiring=`edmDispatcher.wedm_lora_dataset_build` (+ feed `WEDMLoRAAdapterEngine`/`WEDMLoRACadenceEngine`) | bridge=wedm→ai-training/india (`xproc_kg_project_features {slot:mike}`) | rgs-toolchain=`/wedm` parent skill + `wedm-audit` sub-skill → `test-review-agent` (dataset-builder is data-correctness, not physics) | acceptance=engine emits ≥N non-empty, schema-valid training pairs from real JM programs; `wedm_lora` r=4 runs to completion (no 0-byte ENOENT); vitest green in `src/__tests__/`.

2. **U-WTW-REG01: Stand up the wire + dielectric registries and wire them into the EDM 8-stage pipeline** | ROI=H — the single biggest reachability hole (galaxy card: "EDM has NO tools/wire registry"); `edm-wires.ts`/`edm-dielectrics.ts` confirmed MISSING while `wire-spec-sheets.ts` exists unconnected | node=`src/registries/edm-wires.ts` + `src/registries/edm-dielectrics.ts` (single-source from existing `wire-spec-sheets.ts` + `edm-material-db.ts` + `wedm-published-conditions.ts`; never inline) | wiring=`edmDispatcher` wire/dielectric selection into `EDMCuttingParamFlushEngine` + `EDMMaterialMachineWireEngine` + `EDMMultiPassStrategyEngine` | bridge=wedm→oscar/speed-feed (query SFC for wire-tension/flush/power baselines — symmetric edge already declared) | rgs-toolchain=`/wedm` + `wedm-feasibility` → `physics-review-agent` (discharge-physics registry) | acceptance=cut/flush/wire-selection stages read from the registries (no inline constants — `stop_on_inlined_constants` clean); brass/coated/Mo wire + DI-water/oil dielectric resolve with provenance; round-trip test asserts a JM material→wire→params path.

3. **U-WTW-P2P-TASK3: Force real G-code emit vs a held-out JM corpus (break the regression-lock)** | ROI=H — the explicit in-flight TASK #3; current "100%" is a regression-lock NOT measured accuracy (R12 honesty); exposes the `patterns.ts` R7 gap (3 divergent E-code selectors can't emit E952/E56xx) | node=drive `WEDMPrintToProgramEngine` + `EDMPostProcessGCodeEngine` + `EDMDrawingInterpretationEngine` to emit; extend `scripts/wedm-print-to-program-accuracy.ts` with `--heldout` arg | wiring=`edmDispatcher.wedm_p2p_accuracy_run` (structural-skeleton diff vs ground truth) | bridge=wedm→echo/post-processor (NC emit) + wedm→india (publish accuracy as reward label via `xproc_calibration_monitor_record`) | rgs-toolchain=`/wedm` + `wedm-validate` → `test-review-agent` + independent `reviewer` | acceptance=`--heldout` points at JM `WIRE EDM/` programs OUTSIDE ITW/NOZE/FIOCCHI; accuracy drops below 100% and reports an honest measured number; the E952/E56xx emit gap is surfaced (not masked).

4. **U-WTW-SELECTOR-UNIFY: Collapse the 3 divergent E-code selectors to one** | ROI=H — R7 (surface-don't-average) conflict already documented in CLAUDE.md regressions (2026-06-02): `JM_DIE_ECODE_FAMILIES`/`selectECodeFamily`, `WEDMProgramOptimizerEngine`, and `WireEDMDeepAIHardeningEngine._selectECodeFamily` are N-divergent | node=single canonical `selectECodeFamily` sourced from `jm-die-wedm-tech-tables.ts` (`buildAcuFamilyFromFAS`); delete the two shadow impls | wiring=all three consumers call the one selector via `edmDispatcher` | bridge=none | rgs-toolchain=`/wedm` + `wedm-audit` → `code-analyzer` (drift/dedup) | acceptance=one selector, all 5 ACU 7-pass families (E952/E56xx incl) reachable from every consumer; `material_calibrated` fail-loud flag intact; 117/117 green; no regression of the 2026-06-02 fix.

5. **U-WTW-DIALECT-WIRE: Wire the 5 WEDM controller-post dialects (jointly with echo)** | ROI=H — Sodick/Makino/Agie engines + `MitsubishiMV1200RWireEDMMasterPostEngine` confirmed on disk but only the router is wired; Fanuc dialect is the gap; lifts EDM 0.38 and unblocks echo's tier-6 wire-EDM post + kilo EDM strategy | node=`WEDMPostSodickEngine`/`WEDMPostMakinoEngine`/`WEDMPostAgieEngine`/`MitsubishiMV1200RWireEDMMasterPostEngine` + new Fanuc-wire dialect | wiring=`edmDispatcher.wedm_post_emit {dialect}` (convert `method?.()` dark fallbacks to real executing calls — the dark-tail leverage echo flagged) | bridge=wedm→echo (shared dialect surface) + wedm→`cimcoDispatcher` sim-verify | rgs-toolchain=`/wedm` + `wedm-controller` → `wiring-review-agent` + `reviewer` | acceptance=all 5 dialects emit real NC (0 `method?.()` fallbacks), pass dialect-lint, and a CIMCO sim verdict PASS (fail-CLOSED on empty report); byte-class identical|volatile-header-only vs JM goldens.

6. **U-WTW-SAFETY-GUARDS: Revive the 6 WEDM safety-guard engines into `WEDMSafetyEnvelopeEngine`** | ROI=H — safety-critical (archaeology Tier-2 #9, WEDM-COMPREHENSIVE 12 engines/249 tests near production); a guard gap on a discharge program is a fail-loud contract violation | node=current-density / pulse-limit / power-density / kerf-width / wire-deflection / thin-wire-derate guards → `WEDMSafetyEnvelopeEngine` (physics-backed, single-source limits from `wire-spec-sheets.ts`) | wiring=`edmDispatcher.wedm_safety_envelope` + S(x) gate on every wedm emit | bridge=wedm→compliance-safety (CONSUMES S(x) gate — symmetric edge declared) | rgs-toolchain=`/wedm` + `wedm-safety-gate` → `physics-review-agent` + independent `reviewer` | acceptance=each guard has ≥10 tests + a fail-loud threshold+response; thin-wire <0.10mm derate fires; no wedm program emits without passing the envelope.

7. **U-WTW-ORPHAN-WIRE: Wire the ~100 orphan WEDM engines in ≤5/commit batches** | ROI=M — U-WTW-AUDIT found 164 WEDM engines / ~100 orphan / ~600KB dark AI-tier code; reachability is the literal SVI lever, but lower-leverage than the producer/registry/accuracy core above | node=orphan `WEDM*`/`EDM*` engines (e.g. `WEDMContinuous`/`Online`/`LearningLoop`, `WEDMTransferLearning`, `WEDMDriftDetection`) | wiring=`edmDispatcher` action per engine | bridge=hand each wired batch to uniform for silent-no-op verification (romeo pattern) | rgs-toolchain=`/wire-unwired` + `wedm-audit` → `wiring-review-agent` | acceptance=orphan count drops to 0 in `stop_on_unwired_assets`; each wired engine fires once in `EngineUtilizationAuditEngine` (not just reachable-on-paper).

8. **U-WTW-SENSORFUSION: WEDM sensor-fusion + digital-twin + EDMDischarge fusion plugin** | ROI=M — archaeology domain-depth #19; closes the gap↔MRR↔Ra coupled-convergence loop; depends on U-WTW00+REG01 being live first (proven foundation per R13) | node=`WEDMSensorFusionEngine` (Kalman over 8 streams: tension/spark-gap-V/current-waveform/water-resistivity) + `WEDMDigitalTwinEngine` (<10ms) + EDMDischarge plugin into PhysicsFusionOrchestrator | wiring=`edmDispatcher.wedm_sensor_fusion` + `wedm_digital_twin` | bridge=wedm→shop-floor (live discharge status → adaptive — symmetric edge) | rgs-toolchain=`/wedm` + `wedm-reason` → `physics-review-agent` + `reviewer` | acceptance=fusion engine ingests 8 named streams, twin updates <10ms, material-characterization-from-spark emits with uncertainty; closed-loop record feeds india drift-canary.

**External-software training plan (CIMCO, WEDM controllers):**
1. **CIMCO Edit / sim parity gate (already wired — exploit it):** route every dialect emit from U-WTW-DIALECT-WIRE through `cimcoDispatcher`/`CimcoVerificationBridgeEngine` (6 actions) for a sim verdict; fail-CLOSED on an empty report. Build a `wedm-cimco-parity` harness that emits PRISM NC for each JM `WIRE EDM/` program and asserts CIMCO sim PASS + byte-class vs the original — this is replicating CIMCO's own backplot/verify workflow as the accuracy oracle.
2. **WEDM controller corpus extraction from the JM `WIRE EDM/` seat archive (4,058-file / 99-customer archive confirmed at `JM DIE/WIRE EDM/`):** harvest real Mitsubishi `.tech`/E-code programs page-by-page via lima's pypdf extractor (canonical) + the NC text, single-source the ACU 7-pass families and E-code tables into `jm-die-wedm-tech-tables.ts` — never hand-author dialect codes (U-LEGAL-05 gate; re-derive from public Mitsubishi IB-1501279 / Sodick / Makino manuals only).
3. **Controller-dialect parity replication:** for each of the 5 dialects, replicate the controller's program structure (rough→skim→taper→no-core block order, offset/E-code conventions) and validate emitted NC against held-out real programs from that controller's machines — drives U-WTW-P2P-TASK3's honest accuracy number down from the regression-locked 100%.
4. **Wire/dielectric library import:** import wire + dielectric spec libraries (brass/coated/Mo wire diameters, DI-water/oil) into `edm-wires.ts`/`edm-dielectrics.ts` from `wire-spec-sheets.ts` + vendor spec sheets, so the registries in U-WTW-REG01 carry cited real cut-condition tables, not defaults. (No live CIMCO toollib for wire as there is for milling — source from manufacturer wire spec sheets + the published-conditions data.)

**Atomic gaps (nodes/wirings/bridges):**
- **Node — 0-byte `WEDMLoRADatasetBuilderEngine.ts`** (confirmed, Apr-20, 0 bytes): the literal blocker for `wedm_lora`.
- **Nodes — missing registries:** no `src/registries/edm-wires.ts`, no `src/registries/edm-dielectrics.ts` (CLAUDE.md §2 "verify" flags both); `wire-spec-sheets.ts` exists but is unconnected.
- **Wirings — dark dialect engines:** `WEDMPostSodickEngine`/`Makino`/`Agie` + `MitsubishiMV1200RWireEDMMasterPostEngine` on disk, only the router wired (`method?.()` dark fallbacks); Fanuc-wire dialect absent.
- **Wiring — ~100 orphan WEDM engines** (~600KB AI-tier code) in `edmDispatcher`; orphan `WEDMSafetyEnvelopeEngine` guard set.
- **Graph ghosts surfaced by pre-bash inject:** `edm:wedm_post_dialect_config` is `[L8/stub]` (unbuilt config node) while `wedm-dialect-verify` + `wedm-blackboard-post` are `[L10/built]` — the dialect config is the missing atomic node between them.
- **Bridge — wedm→oscar** declared symmetric but un-exercised: no live wire-tension/flush/power query into SFC (REG01 closes it). **Bridge — wedm→echo** dialect surface shared but dark until U-WTW-DIALECT-WIRE.

**RGS-rule updates this domain needs:** Largely **none for routing** — `rgs-pipeline-rules.mjs` already routes `\bwedm\b|\bwire[-\s]*edm\b|\bsinker[-\s]*edm\b` to `/wedm` (Tier-3) and correctly excludes wire-EDM context from the `/wire-unwired` false-match (the documented bug-class fix at lines 67-68 + 189-190). Two additive gaps: (1) add a **dataset/training sub-route** so unit text matching `lora|dataset.?builder|training.?pair|retrain` under a wedm context maps to a `wedm-train`/`test-review-agent` toolchain (today it inherits the generic `/wedm` parent only); (2) add a **dialect/post sub-route** so `dialect|controller.?post|post.?emit` in wedm context routes to `wedm-controller` + `wiring-review-agent` + a CIMCO-sim acceptance step, since post-emit accuracy needs the sim oracle, not a generic physics review.

---

## ROMEO — wiring RGS PIPELINE ROADMAP (P1sub)
**North-star (1 line):** Close the gap between "built engine on disk" and "invokable via MCP dispatcher action" — wire the 110 unwired engines (Engines 88%→100%) and populate the cited tool/material/machine catalogs that SFC + post-proof consume, so no capability sits dark. (MASTER-FLEET-PLAN: "romeo's literal mission … 184 of 257 extracted engines also unwired.")

**Time-allocation (from transcripts):** REDIRECT-with-credit. The last 30 days were a justified-but-divergent pivot into catalog DB-gen + CAM-vendor export (CIMCO `.tmlib` / Fusion `.machine` / hyperMILL `.hmt`, BLACKWELL-DB-GEN, DB-COVERAGE-GAPFILL) — valuable substrate, but the *core 110-engine unwiring* "hasn't started executing" (MEMORY.md "No sessions yet"). Spend ~60% on the wiring mission (it is Tier-1 #4, the single largest reachable-units lever fleet-wide), ~30% finishing the cited-catalog population already in flight (it directly feeds oscar's P1 SFC matrix), ~10% on ghost-action purge + R13 monolith revival.

**Pipeline units (HIGH-ROI ORDERED):**

1. **U-WIRE-SPEED-6: wire the 6 unwired Speed* engines → oscar's SFC surface** | ROI=H — these 6 directly serve oscar's P1 SFC matrix; dark Speed engines = silent SFC capability loss on the fleet's lead product | node=`SpeedFeedAdvancedAIEngine`, `SpeedFeedAtScaleHarnessEngine`, `SpeedFeedAutopilotEngine` + 3 remaining Speed* (enumerate via `audit-unwired-engines.mjs`) | wiring=`prism_calc:speedfeed_advanced` / `prism_calc:speedfeed_at_scale` (natural home; cross-check `prism_session:dispatcher_map_compact` first) | bridge=romeo→oscar (cited S/F surface) | rgs-toolchain=`/wire-unwired` + `wiring-review-agent` (rgs rule matches `\bunwired\b`) | acceptance=round-trip test in `src/__tests__/dispatcher-calc.test.ts` exercises each action via the wire (NOT direct engine call); test fails when wire broken; ≤5/commit cap

2. **U-WIRE-OTHER-21: wire the 21 "Other"-bucket unwired engines (largest single count)** | ROI=H — 21 of 110 = 19% of the entire backlog; biggest reachable-units lever | node=enumerate the 21 via `EngineUtilizationAuditEngine` + `audit-unwired-engines.mjs`, classify each to natural dispatcher | wiring=per-engine `prism_<domain>:<action>` (physics→prism_calc, CAM→prism_cam, AI→prism_ai) | bridge=hand each wired batch→uniform (bug-hunting) for silent-no-op verify | rgs-toolchain=`/wiring-batch` (cap 5/commit) + `wiring-review-agent` | acceptance=`EngineUtilizationAuditEngine` fire-count >0 post-wire per engine; `stop_on_unwired_assets` clean for the batch; per-engine round-trip test

3. **U-GHOST-ACTION-PURGE: eliminate Zod enum actions that no engine handles** | ROI=H — a ghost action is "a lie to operators" (galaxy CLAUDE.md anti-pattern); closed-loop "ghost-action eliminator" is the explicit MASTER-FLEET-PLAN to-build | node=`DispatcherRoutingEngine` (runtime route table) + new `GhostActionDetectorEngine` (or extend `AgentSDKVerifierEngine`) | wiring=`prism_dev:ghost_action_audit` | bridge=feeds sierra system-viz L7→L6 wired/unwired layer | rgs-toolchain=`/scrutinize` + `wiring-review-agent` | acceptance=scan every `prism_*` action enum, assert each maps to a live handler; report ghost count → 0 or each ghost has a follow-up wiring unit; round-trip test asserts a known ghost is flagged

4. **U-WIRE-CATALOG-CITED: finish cited tool/material catalog population (continue in-flight)** | ROI=M-H — directly feeds oscar's P1 SFC matrix "on cited real data, not defaults"; Sandvik/Helical/Sumitomo/ISCAR/Kennametal/Korloy/Guhring/OSG already filled, continue the 44-HIGH worklist | node=extend the BLACKWELL-DB-GEN catalog-gen path (`estimateExtractionPlan()` concurrent vision-OCR) | wiring=`prism_data:catalog_query` (consumes juliett's ISO-513 material axis) | bridge=romeo→oscar (cited S/F) + romeo→charlie (cost-index) | rgs-toolchain=`/pdf-learn` (rgs rule matches `catalog`) using lima's pypdf page-extractor (fleet-canonical) + `reviewer` | acceptance=per ISO group (P/M/K/N/S/H)×tool-class×vendor → assert cited cutting data present (R9, no fabrication, cite source) — no `0.5`-default leakage

5. **U-AUTOWIRE-RANKED-QUEUE: impact-ranked auto-wire queue (the needed MCP feature)** | ROI=M — MASTER-FLEET-PLAN "Needed — an impact-ranked auto-wire queue (callers × test-coverage × domain-leverage)"; turns the remaining backlog from FIFO into ROI-ordered | node=`AutoWiringEngine` (extend — proposes dispatcher-action stubs from engine signatures) scored by `/wiring-potential` formula | wiring=`prism_dev:wiring_potential_rank` | bridge=consumes tango's `audit-unwired-engines.mjs` punch list (discovery→wiring edge) | rgs-toolchain=`/wiring-potential` + `wiring-review-agent` | acceptance=ranked list of remaining unwired engines emitted with callers×coverage×leverage score; top-5 match operator-priority (Speed/Other); no invented engines in the list

6. **U-WIRE-MONOLITH-5: wire the 5 unwired Monolith* catalog/DB engines (R13 revival on-ramp)** | ROI=M — Monolith 12/17 wired (71%); the 5 unwired (e.g. `MonolithControllerDatabaseEngine`, `MonolithMachineSpecStandardEngine`, `MonolithToolTypesDatabaseEngine`) are the on-ramp to R13 monolith extraction (Tier-1 #3, ~27K lines of dormant executable expertise) | node=5 unwired Monolith* engines (enumerate exact set via auditor; wire-don't-rebuild) | wiring=`prism_data:monolith_controller_db` / `prism_calc:machine_spec_standard` (route per content) | bridge=feeds echo (machine/controller defs) | rgs-toolchain=`/wire-unwired` + `wiring-review-agent` | acceptance=each Monolith DB queryable via dispatcher round-trip; cited machine/controller data present; fire-count >0

7. **U-WIRE-EXPORT-VERIFY: round-trip-verify CIMCO/Fusion/hyperMILL exports (close the in-flight wiring)** | ROI=M — `cimco_toollib_export`→prism_data already shipped; verify Fusion `.machine` XML + hyperMILL `.hmt` round-trip so JM fleet exports are trustworthy | node=existing export engines + `AgentSDKVerifierEngine` (post-commit wire-correctness) | wiring=`prism_data:fusion_machine_export` / `prism_data:hypermill_hmt_export` round-trip | bridge=romeo→kilo (Fusion tool libs) | rgs-toolchain=`/scrutinize` + `wiring-review-agent` | acceptance=export → re-import into a parser → assert byte/field round-trip fidelity; P0 material-preset gate (fixed in `1f25831b15`) stays green

8. **U-WIRE-REMAINING-49: sweep the residual unwired (Wet 3 / Creo 3 / Hyper 3 / Swiss 2 / Tool 2 / Quoting 2 + long tail)** | ROI=M — drives Engines coverage to 100%; the tail is heterogeneous so it goes last (logical order: high-leverage clusters first) | node=residual unwired engines by domain bucket | wiring=per-engine natural dispatcher (Hyper→prism_cam, Creo→prism_cad, Wet→prism_cam wedm/waterjet) | bridge=hand each batch→uniform verify | rgs-toolchain=`/wiring-batch` (cap 5/commit) + `wiring-review-agent` | acceptance=BUILD_STATE unwired count → 0; `stop_on_unwired_assets` fleet-clean; every batch has a failing-when-broken round-trip test

**External-software training plan (internal — engine/dispatcher wiring):** Romeo's "external software" is the wire surface itself + the real shop seats whose libraries it imports/exports against. Concrete exhaustive-training actions:
1. **CIMCO Edit 2026 tool DB parity** — round-trip PRISM tools → `.tmlib` → re-open in CIMCO Edit 2026 → assert field fidelity (already-shipped `cimco_toollib_export`); extend to import CIMCO's installed tool libraries back into PRISM as cited catalog rows.
2. **Fusion 360 `.machine` + hyperMILL `.hmt` corpus extraction** — import the installed Fusion machine configs and hyperMILL v31 `.hmt` tool/holder libraries from the live seats as ground-truth, then validate PRISM's generated exports re-import cleanly into Fusion/hyperMILL (parity gate, not vague "use the software").
3. **Catalog OCR via Blackwell GPU + Ollama vision** — run `estimateExtractionPlan()` host-aware concurrent vision-OCR (qwen3-vl on the 96GB Blackwell box, Ollama @127.0.0.1:11434) over the `H:/PRISM/resources` 1,008-PDF vendor catalog index (using lima's pypdf page-extractor, fleet-canonical), training the cited tool/material catalogs the SFC matrix consumes — cite every extracted datum (R9).
4. **G-Wizard / HSMAdvisor parity validation** — cross-check the wired Speed* engine outputs against G-Wizard and HSMAdvisor (both installed) for representative ISO-group×tool-class cases as an external sanity oracle on the cutting data being wired through.

**Atomic gaps (nodes/wirings/bridges):** Real unwired engines (110 total per BUILD_STATE): **Speed (6)** — `SpeedFeedAdvancedAIEngine`, `SpeedFeedAtScaleHarnessEngine`, `SpeedFeedAutopilotEngine` + 3; **Monolith (5)** — subset of `MonolithControllerDatabaseEngine`/`MonolithMachineSpecStandardEngine`/`MonolithToolTypesDatabaseEngine`/`MonolithFixtureDatabaseEngine`/`MonolithWorkholdingDatabaseEngine` (12/17 wired); **Other (21)** — largest bucket, needs per-engine classification; plus Wet 3 / Creo 3 / Hyper 3 / Swiss 2 / Tool 2 / Quoting 2 / Onshape 2 / Grok 2 and a long single-count tail. **Missing wirings:** no `ghost_action_audit` action (ghost Zod enums uncounted); no `wiring_potential_rank` (auto-wire queue is FIFO not ROI-ranked). **Missing bridges:** romeo→uniform handoff is doctrine but not a typed edge; the discovery(tango)→wiring(romeo)→verify(uniform) chain should be a cross-substrate `owned-by-slot` + `documented-by` edge set in `cross-substrate-edges-augmentation.json`. **Dormant:** R13 monolith intelligence extraction (7 modules, ~27K lines — Tier-1 #3) is the largest dormant executable pool; the Monolith-5 wiring is its on-ramp.

**RGS-rule updates this domain needs:** The existing `/wire-unwired` + `wiring-review-agent` rules already correctly handle wiring units (the wire-EDM false-match was fixed — `\bunwired\b|\borphan\b|\bdispatcher\b|\bwiring\b` after excluding wedm context). Two additions: (1) add a **`ghost.*` / "ghost action" / "Zod enum"** unit-text pattern → route to `/scrutinize` + `wiring-review-agent` (current rules don't recognize ghost-action work as wiring). (2) add a **"catalog DB-gen" / "tmlib" / ".machine" / ".hmt" / "vendor catalog"** pattern → route to `/pdf-learn` (catalog already matches) AND a new `export-review` lens via `reviewer` for round-trip-export parity units, since romeo's divergent-but-justified catalog/export work is currently unrouted by the wiring rule. Otherwise: none.

---

## CHARLIE — quoting RGS PIPELINE ROADMAP (P2)
**North-star (1 line):** Any print → margin-correct, physics-grounded, customer-ready quote in one shot; the loop closes on quote-vs-actual — QuoteToShip 0.51 → 1.0, 85% built → 100%.
**Time-allocation (from transcripts):** On-track but idle between units — ~40 `U-QP-*` commits hardened the closed loop from "trains" to "trains honestly + self-audits" (reference-reliability, ext/outbound price calibration, under-quote/variance reporting). Stay on the quoting soul: the next dollar of leverage is no longer more self-audit instrumentation — it is (a) feeding the 3 unconsumed real data sources to drive MAPE off the 71.1% synth ceiling and (b) shipping the customer-facing quote UI. Redirect away from BLACKWELL/db-coverage terminal-drift (recent precompact RESUME lines cite other slots' commits — attribution noise, not charlie work).

**Pipeline units (HIGH-ROI ORDERED):**
1. **U-QP-DATASRC-WIRE-3 (feed cost-index + tool-purchases + curated-DocuStrata to training)** | ROI=H — closes the self-reported 2-of-5 coverage gap; the only path to drive MAPE below the 71.1% synth ceiling onto real $10M-AP cost-basis | node=wire `VendorCostIndexEngine`(cost-index, already on `cost_index_prior`) + `jm-tool-purchases.json` + `docustrata-invoices.curated.json` into `QuotingTrainingLoopEngine` train-cycle | wiring=`prism_quoting:training_status` (already shipped — re-assert coverage 40%→100%) | bridge=← hotel `ERPCostFeedbackEngine` (true actuals) | rgs-toolchain=`/scrutinize` (generic fallback today — see RGS-rule-updates) + `test-team` (data-coverage tests) | acceptance=`data_source_coverage` --json reports 5/5 consumed; MAPE measurably < 71.1% on real back-tested orders (NOT synth self-consistency); never blends grains (units-safe).
2. **U-QP-QUOTE-UI (customer-facing quote app feature + training-status frontend consumer)** | ROI=H — the operator's explicit "quoting app feature"; backend `training_status`/`cost_index_prior`/`outbound_price_*` actions exist with ZERO frontend consumer; web/src has cost.ts/erp.ts but no quote route | node=new React route under `mcp-server/web/src` consuming `prism_quoting` via `web/src/api/` + a `web/src/api/quoting.ts` client | wiring=consumes `prism_quoting:{training_status,outbound_price_calibration,cost_index_prior}` | bridge=none (pure consumer) | rgs-toolchain=`reviewer` (UI weighted: integration+UX+state) per per-file scrutiny | acceptance=route renders full cost-stack + confidence band + margin-floor gate; gate fires when quote at/below per-customer floor; `latest-training-status.json` rendered live.
3. **U-QP-MARGIN-FLOOR-GATE (FMV→true-quote margin-floor enforcement)** | ROI=H — turns advisory FMV (which charlie correctly never passes off as a quote) into a profit-guaranteed quote; the load-bearing gate behind U-QP-QUOTE-UI | node=new `QuoteMarginFloorGateEngine` (per-customer floor from `jm-die-profile.ts`, NEVER inline margin — gotcha #8) | wiring=`prism_quoting:quote_with_margin_floor` (new case on the 82-case dispatcher) | bridge=← hotel customer-terms | rgs-toolchain=`/dedup` (check `CostAlarmEngine`/`CostAwareRouterEngine` first) → `reviewer` | acceptance=at/below-floor → gate blocks + emits shortfall $; above-floor → emits quote; no inline rate/margin constant (`stop_on_inlined_constants` clean).
4. **U-QP-QUOTESHIP-STRATEGIES-WIRE (connect the 4th `strategies` registry + lift dialect coverage)** | ROI=M — QuoteToShip 0.51→1.0 is gated on it carrying only materials/tools/machines, missing `strategies`, and stuck at a single controller dialect | node=wire `strategies` registry into the QuoteToShip pipeline (parallels whiskey's identical Turning 4th-registry wire) | wiring=`prism_business:quote_to_ship_run` strategies input | bridge=↔ kilo `cam_strategy_recommend` (strategy→cost basis, physics-routed never inlined) | rgs-toolchain=`/wire-unwired` + `wiring-review-agent` | acceptance=QuoteToShip reachability 0.51→1.0; >1 controller dialect reachable; round-trip E2E asserts strategy carried into cost.
5. **U-QP-DRAW2QUOTE-MATH (DrawingToQuoteMathPipeline P10/P50/P90 core — VERIFIED unbuilt)** | ROI=H — archaeology dormant #12, the SCIMATH stakeholder-unanimous "money shot"; zero refs on disk (only generic `MonteCarloEngine.ts` exists, NOT a cost-PDF) | node=new `DrawingToQuoteMathPipelineEngine` on top of wired `InstantQuoteEngine`+`DFMPipelineEngine`, reusing `MonteCarloEngine.ts` for the PDF draw | wiring=`prism_quoting:drawing_to_quote_math` (P10/P50/P90 band) | bridge=← delta `cad_feature_recognize`+DFM (start from features, never re-parse prints); ← oscar MRR→cycle_min cost basis | rgs-toolchain=`/forge-triple` (engine+skill+hook; cited-physics + R9 reference values) + `reviewer` | acceptance=emits P10/P50/P90 with confidence band on real JM parts; band brackets the back-tested actual ≥X% of cases; degenerate/empty input → honest limited-mode, never a fabricated band.
6. **U-QP-MONTECARLO-COSTPDF (MonteCarloCostPDFEngine — the probabilistic engine behind U5)** | ROI=M — the cost-distribution sampler the P10/P50/P90 band reads; sibling unbuilt archaeology engine | node=new `MonteCarloCostPDFEngine` (cost-input distributions → cost PDF; reuse `MonteCarloEngine` sampler core, don't re-roll RNG) | wiring=consumed by `drawing_to_quote_math` (engine-to-engine, WIRE-EXEMPT if wrapped) | bridge=none | rgs-toolchain=`/forge-triple` + `physics-reviewer` (uncertainty propagation = RSS, AtomicValue schema) | acceptance=produces a sampled cost PDF with declared input distributions + provenance; deterministic under fixed seed; NaN/divergence handled (fail-loud limited-mode).
7. **U-QP-DOLLARS-LEFT-SURFACE (operator-facing under-quote / dollars-left-on-table report)** | ROI=M — `assessUnderQuotes` already computes per-customer dollars-left-on-table (gotcha #24, `aefaeaea99`); it has no operator surface | node=wire existing `assessUnderQuotes` rollup into the U-QP-QUOTE-UI dashboard | wiring=`prism_quoting:training_status` extension OR new read-only action | bridge=→ hotel (commission/flash already renders JM fleet) | rgs-toolchain=`reviewer` (advisory-honesty weighted) | acceptance=surfaces top-N worst under-quotes + by-customer Σ positive gap_usd; clearly labeled ADVISORY (high-MAPE → DIRECTIONAL), never a quote without the margin-floor gate.
8. **U-QP-CALIB-OUTCOME-PUBLISH (close the india loop on every shipped quote)** | ROI=M — galaxy CLAUDE.md mandates `xproc_outcome_publish{slot:charlie}` + `xproc_calibration_monitor_record`; today only training-cycle audits, not per-quote outcomes | node=wire `outcome-bus-auto-tap` for quote actions (verify auto-fire, else explicit) | wiring=`xproc_outcome_publish` + `xproc_calibration_monitor_record` | bridge=→ india GNN/drift-canary retrain candidacy | rgs-toolchain=`/scrutinize` + `test-team` | acceptance=every quote emits an outcome + calibration record; india drift-canary can fire retrain on quoting drift (verified via a round-trip E2E, not just the action existing).

**External-software training plan (DocuStrata pricing, ERP data):**
1. **Curated-DocuStrata invoice extraction → training corpus.** Search the DocuStrata index (`manifest.json` + `.index/` — NEVER re-OCR per CRITICAL-RESOURCE-ROOTS doctrine) for the curated invoice set (174 vendors, 20,550 bill-lines, $4.9M AP, 2014-2026); pipe `docustrata-invoices.curated.json` through `DocuStrataMaterialPriorEngine` (exists) into `QuotingTrainingLoopEngine` as a **material-price prior** — INBOUND-only, link-only financial pointers, never create discrete AR/AP records (gotcha #7, `financial_guard`).
2. **DocuStrata quote-execution variance replication.** Extend the just-shipped `U-QP-DOCUSTRATA-VARIANCE` (`87c40bdba6`) to validate PRISM's predicted price against DocuStrata's recorded quoted-vs-executed deltas per line — parity validation against the tool's real outputs (freshness-preflight, advisory, units-clean per-line).
3. **ERP actuals as ground-truth (hotel `ERPCostFeedbackEngine`).** Consume hotel's per-category variance (material/labor/machine-hr/overhead/freight) as the ONLY true outbound-revenue ground truth — the dependency MASTER-FLEET-PLAN flags. Replace the iter59 match_pct=0 per-part join with the distribution-prior approach already proven (`OutboundPriceIndexEngine`, `cd3f38be0a`), comparing predicted-FMV distribution to the real `ext_price` distribution (correct grain per gotcha #15/#16).
4. **$10M-AP cost-index library import.** Import `jm-vendor-cost-index.json` (7 category medians, already wired to `cost_index_prior` but NOT fed to training) as the feature-half cost basis — replicating how the shop actually prices off vendor-cost history rather than synthetic defaults.

**Atomic gaps (nodes/wirings/bridges):**
- **Unwired nodes:** Quoting BUILD_STATE = 13 engines / 11 wired / **2 unwired** (15%). The 2 unwired quoting engines are the priority orphan target (name them via `/orphan-inventory` filtered to quoting prefixes Cost*/Quote*/Estimat*/Pricing*); plus the **eligible queue's** `U-GAP-WIRE-JMDIE-CORPUS` + `U-WIRE-BACKLOG-WIRE` quoting-adjacent picks.
- **Genuinely-unbuilt nodes (verified zero refs on disk):** `DrawingToQuoteMathPipelineEngine`, `MonteCarloCostPDFEngine` (U5/U6) — the archaeology #12 money-shot, NOT yet scaffolded.
- **Missing wirings:** no customer-facing quote UI route in `mcp-server/web/src` (backend actions exist, zero frontend consumer — the #1 product-visibility gap); no `web/src/api/quoting.ts` client.
- **Missing/weak bridges:** quoting↔hotel `ERPCostFeedbackEngine` actuals edge is the stated blocker for outbound-revenue calibration (live but starved — hotel's gate at 61.44%, Postgres unblock pending); quoting↔delta `cad_feature_recognize` edge exists (symmetric) but quotes should START from features not re-parse prints — verify the live consume path. Ghost node `○ QUOTE · QuotingEngine` surfaced in the graph — confirm it's not a phantom dispatcher action.

**RGS-rule updates this domain needs:** **Add a quoting/cost pipeline rule** — `scripts/lib/rgs-pipeline-rules.mjs` has NO quoting/cost/pricing/quote/DFM/Monte-Carlo branch, so every charlie `U-QP-*` unit falls through to the 0.30 generic `/scrutinize` fallback (mis-routed, low confidence). Proposed PIPELINE_RULES entry: `test: /\bquot(e|ing)\b|\bcost[-\s]?(estimat|model|index)\b|\bpricing\b|\bmargin\b|\bDFM\b|\bmonte[-\s]?carlo\b|P10\/P50\/P90/i → skill: "/quote-to-ship", why: "quoting/cost-estimation unit", confidence: 0.80`. **Add an AGENT_RULES entry** routing cost/quote units to a quote/cost reviewer (today only physics/test/wiring agents exist — quoting units get the generic `code-analyzer`); proposed `test: /\bquot(e|ing)\b|\bcost\b|\bpricing\b/i → agent: "code-analyzer"` with a note to weight cost-model correctness + units-safety (the gotcha-#7 grain-blend trap). Word-boundary guard `\bcost\b` to avoid matching "Costa"/"costume"; exclude when the unit is pure ERP (route to hotel) per R7 cross-galaxy surfacing.

---

## ALPHA — token-optimization RGS PIPELINE ROADMAP (P2)
**North-star (1 line):** Make the 26-slot fleet maximally token-efficient — every prompt routes to the cheapest correct surface, sessions stay in budget, the Obsidian brain compounds context so nothing is re-derived; "free" infra that lowers all 33 other galaxies' cost (offload 11.2% → ≥30%).
**Time-allocation (from transcripts):** On-track and squarely on soul — alpha is correctly inside BLACKWELL-TOKEN-SYNERGY-MS0 (route-profile → offload-tier → best-tier-reach, last commit 8m ago). Redirect ONE notch: the Blackwell *routing brain* (`ModelRoutingEngine` already knows `home_blackwell`/32B) is built, but the take-rate is still 0.8% and offload 11.2% because the `ollama-route-pretooluse.mjs` hook is **unwired in settings.json** (grep count 0). Stop adding tiers; wire the offloader and make unambiguous routes binding. Secondary: close the long-stuck COMMAND-KERNEL-MS0 U-CK11 close-out debt and stand up the missing bidirectional Obsidian sync (HMEMV04-06) — the biggest context-retention hole.
**Pipeline units (HIGH-ROI ORDERED):**
1. **U-BW-OFFLOAD-HOOK-WIRE: wire the dead `ollama-route-pretooluse.mjs` offloader** | ROI=H — the literal #1 work-order move; 4732 fires / 0 offloads because the hook is absent from `H:/.claude/settings.json` (verified grep count 0). Wiring it turns the existing `ModelRoutingEngine` Blackwell 32B tier from theory into measured offload. | node=`ollama-route-pretooluse.mjs` (wire) → consumes `ModelRoutingEngine` | wiring=settings.json `PreToolUse` cluster + `prism_dev:offload_route_status` | bridge=alpha→india (`xproc_outcome_publish` feeds routing-classifier closed loop) | rgs-toolchain=`/wire-unwired` + `wiring-review-agent` (matches `\bunwired\b`) | acceptance=`ollama-offload-dashboard.mjs --json` offloadRate climbs off 11.2% within one fleet-day; `byHook["ollama-route-pretooluse"].offloaded > 0`.
2. **U-BW-OLLAMA-PREWARM-VERIFY: prove Ollama is reachable + 32B warm on the Blackwell host** | ROI=H — a wired router still emits 0 offloads if `127.0.0.1:11434` is cold or the host-class detector mis-reads non-Blackwell. Cheapest dependency of U1. | node=`ollama-prewarm-on-pipeline.mjs` + host-class detector in `ModelRoutingEngine` | wiring=`ollama_route_check` skill → `prism_dev` | bridge=alpha→golf (golf owns qwen3 catalog + reaper GPU floor; U-BW-CATALOG-REALIGN) | rgs-toolchain=`/ollama-route-check` + `test-review-agent` | acceptance=`/api/tags` lists `qwen2.5-coder:32b` resident; prewarm logs a warm hit; non-Blackwell hosts stay byte-identical (no false 32B route).
3. **U-BW-AUTO-ROUTE-BINDING: convert unambiguous advisory nudges to binding routes** | ROI=H — take-rate is 0.8% because every nudge is advisory and unactioned. Make the deterministic cases (Grep-over-Agent, MCP-action-over-reimpl, gist-only summarize) binding under the existing `U-BW-AUTO-ROUTE-ALLOWLIST` gate (R5: code answers deterministic routing, not the model). | node=`route-suggest-inject.mjs` (referenced in galaxy CLAUDE.md but missing on disk — build it) + allowlist gate | wiring=`prism_context:route_suggest_stats` | bridge=none | rgs-toolchain=`/route-suggest-stats` + `wiring-review-agent` | acceptance=take-rate climbs past 0.8% on the allowlisted classes; safety/judgment routes remain advisory (fail-open, never block a cloud-needed call).
4. **U-CK11-CLOSE: per-category scrutiny pass → close COMMAND-KERNEL-MS0** | ROI=M — 28/29 units done; milestone stuck `in_progress` since 2026-05-17 purely on U-CK11 close-out debt. Alpha-owned (PSK is the Obsidian-brain syscall substrate). Pure close-out, unblocks the envelope. | node=`.claude/kernel/psk.mjs` (scrutiny pass, no new code) | wiring=`prism_session:psk` (already wired) | bridge=none | rgs-toolchain=`/scrutinize` + `reviewer` (matches `scrutin`) | acceptance=`close-out-milestone.mjs COMMAND-KERNEL-MS0` flips envelope to complete across all 4 surfaces; 3-of-3 PASS.
5. **U-HMEMV04-06: bidirectional Obsidian vault sync (vault-side edits flow back to C:)** | ROI=M — flagged the biggest dormant context-retention hole (engines/CLAUDE.md §3 + SCOPE-EXPANSION §Q4 #1). Today the feed is one-way (C:→H:→Obsidian); an operator edit in the vault is lost. Closing it makes the Obsidian brain a true read/write store and feeds india's routing-classifier richer context. | node=new `ObsidianVaultSyncEngine` (check `HERMES-MEMORY-VAULT-MS0.json` for stubs first — wire-don't-rebuild) | wiring=`prism_memory:vault_sync_status` + Stop hook | bridge=alpha→bravo (Hermes `%LOCALAPPDATA%/hermes/memories/` → `knowledge/`) | rgs-toolchain=`/dedup` (mandatory pre-build) then `/forge-triple` + `code-analyzer` | acceptance=`obsidian_sync_status` reports bidirectional:true; a vault-side edit round-trips to `C:/.../memory/*.md` within one Stop cycle; conflict = last-writer-wins with provenance, never silent clobber.
6. **U-COST-DASH-FRONTEND: in-product per-job/per-quote token+$ dashboard (frontend consumer)** | ROI=M — the MCP half exists: `CostEfficiencyBridgeEngine` is already wired to `businessDispatcher:cost_efficiency_bridge_analyze` (verified). Only the frontend consumer is missing. Surfaces token+$ spend per quote into hotel's ERP — turns "free infra" into a visible product metric. | node=`CostEfficiencyBridgeEngine` (built) + new web consumer | wiring=`prism_business:cost_efficiency_bridge_analyze` (built — consume it) | bridge=alpha→hotel (ERP cost feedback) | rgs-toolchain=`/token-dashboard` + `reviewer` (UI/integration weighted) | acceptance=web surface renders per-job token+$ from the live dispatcher (no mock); matches `SessionTokenLedgerEngine` totals within rounding.
7. **U-SEMANTIC-ROUTER-CORE: embedding-based route classifier (alpha owns the router half)** | ROI=M — the #1-surfaced cross-cutting dormant build (3 plans). Replaces fleet-wide `String.includes` route-matching with 384-d ONNX embeddings (PRISM already has the ONNX+HNSW infra). Alpha is the natural token/cost owner; india co-owns the memory/RAG half. Build the core verifiably BEFORE consumers (R13 logical order). | node=new `SemanticRouteClassifierEngine` (reuse ONNX 384-d + HNSW, do NOT rebuild embeddings) | wiring=`prism_context:semantic_route_classify` | bridge=alpha→india (shared embedding infra; NN-GRAPH is only a partial descendant) | rgs-toolchain=`/dedup` then `/forge-triple` + `code-analyzer` | acceptance=classifier beats `String.includes` baseline on a held-out route-label set (precision + recall reported, not a stub `toBeDefined`); falls back to keyword match on low-confidence (fail-open).

**External-software training plan (Ollama, Obsidian):**
1. **Ollama parity/calibration harness** — replay a held-out set of real fleet offload candidates (the 666 `suggested` events in `ollama-offload-stats.json`) through `qwen2.5-coder:32b` on the Blackwell host vs the prior `cloud` answer; record agreement rate per task-class (summarize/explain/triage/classify). Promote a class to *binding* offload only when local-vs-cloud agreement ≥ a measured threshold — this is the data that makes U3's allowlist defensible rather than guessed.
2. **Ollama model-tier benchmarking** — exhaustively probe each model in `ModelRoutingEngine` (`qwen2.5-coder:32b/14b/7b/3b`) for latency + token-throughput + `keep_alive` warm-hit rate on this host, writing the real numbers back into the tier table so the host-class detector routes by measured capability, not hard-coded `runsOn` lists.
3. **Obsidian vault corpus ingestion** — point the bidirectional sync (U5) at the installed Obsidian vault, import its existing `[[wikilink]]` graph + `aliases:` frontmatter as ground-truth edges into `knowledge/`, and validate that `memory_search`/PSN-leg recall returns vault-authored notes (not just C:-fed ones). This replicates Obsidian's own backlink/graph workflow inside PRISM rather than re-deriving it.
4. **Obsidian → semantic-router training pairs** — use the vault's human-curated note→topic structure as labeled training data for U7's `SemanticRouteClassifierEngine` (a note tagged `#token-economy` is a positive route example), closing the loop: the external tool's organization teaches PRISM's router.

**Atomic gaps (nodes/wirings/bridges):**
- **Unwired hook (the keystone):** `ollama-route-pretooluse.mjs` exists on disk (23KB, edited Jun 3) but is absent from `H:/.claude/settings.json` (grep count 0) — the entire "4732 fires / 0 offloads" symptom. (U1)
- **Missing-on-disk node:** `route-suggest-inject.mjs` is referenced in `token-optimization/CLAUDE.md` but does not exist (`ls` 404) — a documented-but-unbuilt route-nudge surface. (U3)
- **Frontend orphan:** `CostEfficiencyBridgeEngine` → `businessDispatcher:cost_efficiency_bridge_analyze` is wired at MCP level but has **no web consumer** — a built backend with a dead front edge. (U6)
- **Dormant cross-cutting node:** no `SemanticRouteClassifierEngine` exists; fleet route-matching is still `String.includes`. (U7)
- **Missing bridges:** alpha→hotel cost edge (CostEfficiencyBridge → ERP) is doc-asserted but has no rendered consumer; alpha→bravo Obsidian-memory bridge (`hermes/memories/` → `knowledge/`) is unbuilt (HMEMV04-06).
- **Close-out debt:** COMMAND-KERNEL-MS0 envelope stuck `in_progress` on the single open U-CK11.

**RGS-rule updates this domain needs:** Real gap — `scripts/lib/rgs-pipeline-rules.mjs` has **zero token/cost/ollama/obsidian routing** (only physics/test/wiring/mill/lathe/wedm/cam/cad/pdf/video/scrutiny rules). Add:
- `{ test: /\bollama\b|\boffload\b|local[-\s]?llm|qwen|route.?(profile|nudge)|model[-\s]?rout/i, skill: "/ollama-route-check", why: "local-LLM offload / model-routing unit", confidence: 0.85 }`
- `{ test: /\btoken(s)?\b|\bcost\b|budget|context[-\s]?budget|efficien|cache|CAG|prompt[-\s]?cache/i, skill: "/token-dashboard", why: "token-economy / cost-efficiency unit", confidence: 0.8 }`
- `{ test: /obsidian|vault|memory[-\s]?sync|knowledge[-\s]?vault/i, skill: "/route-to-obsidian", why: "Obsidian/vault context-retention unit", confidence: 0.8 }`
- AGENT_RULES: add `{ test: /token|cost|ollama|offload|budget|cache/i, agent: "code-analyzer" }` so token-economy units get a non-physics reviewer (today they fall to the generic `/scrutinize` fallback). Keep the wiring-review-agent structural exclusion pattern (don't let "route" alone trigger wiring).

---

## HOTEL — business RGS PIPELINE ROADMAP (P3)
**North-star (1 line):** Autonomous back-office — accepted quote → costed/scheduled/shipped/invoiced job with per-category cost truth + credit/compliance gates; QuoteToShip 0.51–0.72 → 1.0, durable-state and orphan tribal wired, on a strict financial-discipline soul.
**Time-allocation (from transcripts):** On-track but mid-pivot — hotel spent the last 30d on JM-DOC-POPULATION (554,999 files, gate 0→61.44%) + ERP/HR Phase 1-3 + NETPLAT-UI de-stub. REDIRECT the front of the queue: the operator work-order makes Postgres-unblock the *precondition* (durable state gates every populated engine), then orphan-tribal wire + the 6 inline-constants extract (HARD-RULE violation living in prod), THEN resume corpus population — population value compounds only once durable+credible surfaces consume it.

**Pipeline units (HIGH-ROI ORDERED):**
1. **U-GAP-ERP-PERSISTENCE-PG: Unblock PostgreSQL + pgvector durable state** | ROI=H — `pg` is dead-code → `ERPIntegrationEngine`/auth/rate-limit/`GeneralLedgerEngine` silently fall back to in-memory; all business+auth data lost on restart (fail-loud violation + AS9100 audit-trail blocker; the archaeology #1 single-most-consequential gap). | node=`ERPIntegrationEngine` + new `PgPersistenceAdapter` (wire `pg` Pool, replace in-mem Maps; pgvector table to retire `String.includes` over business records) | wiring=`prism_business.erp_persistence_status` (+ `accounting-audit` reads durable GL) | bridge=`business→database-expansion` (juliett owns SQLite-WAL/pg discipline — coordinate, don't fork) | rgs-toolchain=`/wire-unwired` + `wiring-review-agent` (generic-default — see RGS gap below) | acceptance=restart-survival E2E: write GL journal + auth token → kill process → reload → records present + `debits===credits` invariant holds; fail-LOUD (throws) when `pg` unreachable, never silent in-mem.
2. **U-WIRE-BACKLOG-ERP-TRIBAL: Wire orphan HotelERPTribalKnowledgeEngine** | ROI=H — 0 dispatcher refs, 17 stranded tribal categories; alpha-ranked #1 dormant-capacity opportunity (0.79); `BusinessSyncEngine` (320-byte stub) wired only via `method not callable` dark fallback at businessDispatcher.ts:5774. | node=`HotelERPTribalKnowledgeEngine` (+ implement-or-archive `BusinessSyncEngine` per never-delete-only-disable) | wiring=`prism_business.erp_tribal_query` / `erp_tribal_categories` | bridge=`business→tribal-knowledge` (golf-owned tribal store) | rgs-toolchain=`/wire-unwired` + `wiring-review-agent` | acceptance=round-trip: 17 categories enumerable via dispatcher; `stop_on_unwired_assets` clean; `business_sync_stats` returns real data not the `method not callable` sentinel.
3. **U-GAP-ERP-CONSTANTS-EXTRACT: Extract 6 inline financial/HR constants families** | ROI=H — payroll-tax-tables / pto-policies / benefits-plans / customer-terms / vendor-profile / chart-of-accounts inlined in engines violate the no-inline-tax-tables HARD RULE (the financial analogue of the physics-constants rule); poisons `EmployeePayrollGrossPayEngine` + `GeneralLedgerEngine`. | node=`EmployeePayrollGrossPayEngine`/`HRComplianceEngine`/`GeneralLedgerEngine` → import from new `src/data/*` | wiring=`prism_business.payroll_constants_source` (provenance read) | bridge=none | rgs-toolchain=`/dedup` (pre-extract dup-check) + `code-analyzer` | acceptance=zero inline tax/PTO/benefits literals in engine bodies (grep-gate); FLSA gross-pay test passes against extracted `src/data/payroll-tax-tables.ts`; per-table `schemaVersion` + source citation.
4. **U-BIZ-LEAN-DASHBOARDS: Surface Lean/Six-Sigma dashboards over built engines** | ROI=H — low-effort/high-visibility revenue surface; `OEECalculator`, `SPCProcessCapability`, `NelsonSPCRules`, `LeanSixSigma` engines EXIST, only frontends + a downtime fact-table schema are missing (archaeology serene-meandering-prism). | node=`OEECalculator`+`SPCProcessCapability`+`NelsonSPCRules`+`LeanSixSigma` (+ new `DowntimeFactTableEngine` schema) | wiring=`prism_business.oee_compute` / `spc_control_chart` / `kaizen_board` / `vsm_render` | bridge=`business→quality` (golf-owned Cpk/SPC galaxy) + frontend route (`/lean-sixsigma` NETPLAT-UI) | rgs-toolchain=`/forge-triple` (engine-frontend-route triple) + `reviewer` (UX-weighted) | acceptance=OEE/SPC control-chart/Kaizen/VSM render real JM downtime data (not demo); Nelson rule-violation flags fire on a seeded out-of-control series; route live in NETPLAT-UI (6/8→8/8 pages real).
5. **U-BRIDGE-ERP-QUOTE: Close QuoteToShip cost-feedback loop into adaptive engines** | ROI=H — `ERPCostFeedbackEngine` per-category variance is the ONLY true outbound-revenue ground truth; closing it unblocks charlie's MAPE off the 71.1% synth ceiling (the cross-galaxy keystone edge). | node=`ERPCostFeedbackEngine` → publish variance to OutcomeFeedbackBus | wiring=`prism_business.erp_cost_feedback` → `quote_actual_variance` | bridge=`business→quoting` (`QuoteToOrderBridgeEngine` outbound; charlie CONSUMES actuals) + `→ai-training` (india OutcomeBus) | rgs-toolchain=`/wire-unwired` + `wiring-review-agent` | acceptance=accepted-quote→shipped-job emits per-category (material/labor/machine/overhead) variance row; charlie back-test reads real actuals; bus tap recorded in `xproc_outcome_*`.
6. **U-BRIDGE-ERP-SCHED: ShopState canonical event spine + job-shop scheduling** | ROI=M — the connected-shop-floor OS (`ShopStateEngine`: job/traveler/labor/event spine, role-filtered shells, release gates) is foundational for the galaxy; covers `U-GAP-ERP-JOBSHOP-SCHEDULING`. | node=`ShopStateEngine` (build canonical spine) + finite-capacity scheduler | wiring=`prism_business.shop_state_event` / `job_schedule_plan` | bridge=`business→shop-floor` (golf-owned live-machine status → adaptive) | rgs-toolchain=`/forge-triple` + `reviewer` | acceptance=traveler state-machine drives a real JM job through queued→running→complete with labor events; schedule respects 5-VMC fleet capacity; offline-replay deterministic. *(Depends on U-GAP-ERP-PERSISTENCE-PG — durable spine.)*
7. **U-BIZ-Q2S-VERBS: QuoteToShip frontend verbs + PII/consent gate** | ROI=M — closes reachability 0.51→1.0 by wiring the missing q2s verbs (`rfq_assign`, Kaizen/OEE actions+routes, `credit_review_all`) + adds the missing PII gate on customer-export paths (zero PII gate on untrusted intake today). | node=`quote_to_ship_run` orchestrator verbs + `CustomerPortalEngine` export path → `aidefence has_pii` | wiring=`prism_business.rfq_assign` / `credit_review_all` / `prism_inbox` PII scan | bridge=`business→intake` (PII scanner on `intake_processor_*`) | rgs-toolchain=`/forge-triple` + `reviewer` (security-weighted) | acceptance=q2s reachability 1.0 (all verbs routed); customer-export blocked when `has_pii` true unless consent-flagged; 21-stage orchestrator runs end-to-end on a JM RFQ.
8. **U-JMDOC-100: Drive JM-DOC population gate 61.44%→100%** | ROI=M — 7 financial tuples pending; read-mostly population that compounds once units 1-5 give durable+credible consuming surfaces (P4 catch-up half of hotel's order). | node=`DocumentInboxEngine.seedFinancialPointers` (close remaining tuples, link-only, never create AR/AP/GL) | wiring=`prism_inbox.inbox_population_status` (gate→100%) | bridge=`business→database-expansion` (DocuStrata `manifest.json`/`.index/` — never re-OCR) | rgs-toolchain=`/pdf-learn` (lima pypdf for any new extraction) + `reviewer` | acceptance=accountability ledger reconciles 100% with 0 silent drops; `financial_guard` invariant holds (pointers never become discrete financial records).

**External-software training plan (Docker (Postgres), ERP stack):**
1. **Stand up Postgres in the installed Docker** as the durable backing store for `ERPIntegrationEngine` + auth + GL; load the `pgvector` extension; mirror the in-mem schema to real tables with `schemaVersion` columns; run the restart-survival E2E (unit 1) against the live container — this is "training the ERP layer on its own persistence software," not a mock.
2. **ERP-vendor parity validation:** `ERPIntegrationEngine` already bridges 7 vendors (JobBOSS/Epicor/ProShop/Global Shop/SAP/Oracle/Generic). Replicate a real JM work-order round-trip through each adapter and assert field-level parity (PO#/job#/cost-codes) against the vendor's export schema — corpus = the DocuStrata QuickBooks export (174 vendors, 20,550 bill-lines, 2014-2026, $4.9M AP) as the AP ground-truth set for `acct-quickbooks-sync` + cost-feedback calibration.
3. **QuickBooks/accounting parity:** drive `AccountingHardeningEngine.acct-bank-reconcile` + `acct-wip-valuation` against the curated DocuStrata invoices (link-only pointers) and assert GL `debits===credits` + WIP valuation matches the export totals — replicating the shop's real accounting workflow, not synthetic ledgers.
4. **Offload the heavy text work:** route ERP doc-classification + invoice line-item extraction to Ollama (`ask-ollama` summarize/classify @127.0.0.1:11434, qwen2.5-coder) and, where GPU is available, NVIDIA NIM — keep Claude for the financial-invariant judgment calls (R5). Cite every extracted financial tip with its source pointer (R9, link-only discipline).

**Atomic gaps (nodes/wirings/bridges):**
- **Orphan node:** `HotelERPTribalKnowledgeEngine` (0 dispatcher refs, 17 tribal categories stranded) — unit 2.
- **Dark-fallback wiring:** `BusinessSyncEngine` wired at `businessDispatcher.ts:5774` but returns `method not callable` sentinel — 320-byte stub; implement or formally archive (unit 2).
- **Dead-code dependency:** `pg` import path dead → `ERPIntegrationEngine`/auth/`GeneralLedgerEngine` silent in-memory fallback (unit 1) — the fail-loud violation.
- **Missing nodes:** `ShopStateEngine` canonical event spine (unit 6); `DowntimeFactTableEngine` schema for OEE (unit 4) — engines around them exist, the spine/schema don't.
- **Missing bridges:** `business→quoting` cost-feedback (`ERPCostFeedbackEngine`→charlie, unit 5); `business→quality` (OEE/SPC frontends, unit 4); `business→shop-floor` (ShopState→live machines, unit 6); PII gate on `business→intake` export paths (unit 7).
- **Frontend gap:** 3 NETPLAT-UI pages still demo-data + no Lean/Six-Sigma routes — the galaxy's largest unbuilt-frontend surface (engines exist, dashboards don't).

**RGS-rule updates this domain needs:** The RGS rule table (`scripts/lib/rgs-pipeline-rules.mjs`) has **NO business/ERP/finance rule** — every hotel unit falls through to the generic `/wire-unwired`, `/forge-triple`, `/dedup`, `/scrutinize` defaults, which under-routes financial-discipline work. Add a business rule: `test = /\b(erp|quote.?to.?ship|invoice|payroll|gl|ledger|accounting|cost.?feedback|oee|spc|kaizen|six.?sigma|job.?cost|ap|ar|credit.?review)\b/i` → `skill: "/biz-health"` (or `/erp-health` / `/job-cost` — all exist as skills) with a finance-weighted review agent (`reviewer` weighted on: no-inline-tax-tables, `debits===credits` invariant, link-only financial-pointer discipline, PII/consent on export). Also add a `persistence`/`pg`/`durable-state` pattern → `/wire-unwired` + a fail-loud-on-fallback acceptance hint, since the in-memory-fallback class is a recurring archaeology gap, not just hotel's.

---

## DELTA — cad RGS PIPELINE ROADMAP (P4)
**North-star (1 line):** PRISM's geometry brain — any input (blueprint/photo/text/STEP) → validated, feature-recognized, DFM-checked, tolerance-stacked solid (STEP AP242 + live Fusion/hyperCAD-S), trusted by every downstream galaxy; drive JM corpus coverage 33% → high and make CADCAM-AGI real, not doctrine.

**Time-allocation (from transcripts):** **Redirect.** Last session auto-pinned to a HERMES-MASTER-ORCHESTRATOR topic post-`/compact` and shipped 0 delta units — re-bind topic to **cad** first. The real, high-value delta thread is the closed-loop `CAD-TRAINING-PIPELINE` /loop (iter 8/20, last commit `12f4e04a4d`) + the Fusion add-in `/new` fresh-doc fix. Spend ~60% driving the JM-corpus coverage lever (P4 operator priority) via the already-built `CADReverseCorpusCatalogEngine`/`CADReverseTemplateEngine`, ~25% finishing the Fusion add-in `/new` fix + resuming the /loop (coordinate with **kilo** for Fusion ownership), ~15% wiring orphans + the 2 PENDING_MERGE UIs. Stop building net-new CAD engines — the surface is 75/75 wired; this is a **wire + train + populate** phase.

**Pipeline units (HIGH-ROI ORDERED):**
1. **U-GAP-WIRE-JMDIE-CORPUS: drive CAD_COVERAGE_MATRIX 33% → high on the 16K JM corpus** | ROI=H (largest cad dark-data gap; the explicit P4 operator priority and the lever every downstream galaxy compounds on) | node=`CADReverseCorpusCatalogEngine` + `CADReverseTemplateEngine` + `CADCanonicalTreeAdapterEngine` (already built CAD-REVERSE-ENGINEER-MS0 — wire-don't-rebuild) | wiring=`cadDispatcher.cad_reverse_corpus_catalog` + `cad_coverage_matrix_report` | bridge=cad→india (`xproc_kg_project_features` — extraction outcomes feed GNN/LoRA corpus) | rgs-toolchain=`/cad-from-blueprint` + `wiring-review-agent` | acceptance=`cad_coverage_matrix_report` returns coverage measurably >33% over the 20,006-file reverse corpus with per-format (STEP/IPT/DXF/STL) counts; never re-OCR (search juliett's indexed corpus per UNITS-FIRST).
2. **U-GAP-CAD-FEATURE-PRIMITIVES: complete feature-primitive coverage feeding the recognizer** | ROI=H (feature_recognize is the strongest live cross-galaxy edge — cam + quoting both consume it; gaps here silently degrade every downstream quote/strategy) | node=`CADFeatureRecognitionEngine` + `CADOperationTaxonomyEngine` (wire missing primitives; use `spatial_ransac_fit` / `prism_algorithm` for robust planar-face + axis extraction) | wiring=`cadDispatcher.cad_feature_recognize` | bridge=cad→cam (`feature_recognize` → `cam_strategy_recommend`) + cad→quoting (`feature_recognize`+DFM → `BlueprintToQuoteBridgeEngine`) | rgs-toolchain=`/cad-feature-recognize` + `/cad-from-blueprint` + `wiring-review-agent` | acceptance=round-trip E2E: a JM STEP part → feature set asserted against ground-truth labels, then consumed without error by `cam_strategy_recommend` AND charlie's `BlueprintToQuoteBridgeEngine`.
3. **U-FUS-NEW-DOC-FIX: Fusion add-in `/new` fresh-doc fix + resume CAD-TRAINING-PIPELINE /loop** | ROI=H (the one open blocker on the live closed loop — build lands in a pre-existing doc, components=4=prior+this; unblocks iter 8/20→20/20 of the real print→CAD→live-build→verify pipeline) | node=`Fusion360LiveBridgeEngine` (`:18360`) + `Fusion360CADGeneratorAdapter` | wiring=`cadAutomationDispatcher.fusion_execute` (add explicit `/new` fresh-doc create+activate) | bridge=cad↔kilo (shared Fusion ownership / runCandidate) | rgs-toolchain=`/fusion-generate` + `/cad-validate` + `reviewer` (integration-weighted) | acceptance=add-in `/new` produces a fresh active doc (component count == this-build only); `CAD-TRAINING-PIPELINE` /loop resumes and a build verifies every dimension within the ≥70% E2E gate against the JM Die 12-case corpus.
4. **U-CADCAM-AGI-CONVERT: convert CADCAM-AGI-MS0 (24u never_started) — make the india-cloned closed loop real** | ROI=H (largest never_started CAD milestone; turns "CADCAM-AGI" from doctrine into a real self-improving loop, per the MASTER-FLEET-PLAN "Needed") | node=`CADSystemNeuralArchAdapterEngine` (CAD-COMPLETE-MS0, already built) + clone india's per-domain LoRA template | wiring=`cadDispatcher.cad_train` / `cad_rag` | bridge=cad→india (CAD-RAG + CAD-train embeddings; the `GCodeUnderstandingTransformer` geometry-adjacent embedding delta over 24,545 JM programs) | rgs-toolchain=`/cad-train` + `/cad-rag` + `test-team` | acceptance=one real training cycle fires end-to-end (CAD features → embedding → outcome back to OutcomeFeedbackBus), 24u envelope flips from never_started with shipped commits; tests assert real corpus (no `toBeDefined` stubs).
5. **U-CAD-FRONTEND-MERGE: ship the 2 PENDING_MERGE CAD UIs** | ROI=M (makes feature_recognize → cam/quoting consumption product-visible; closes the "unified print→model→DFM→quote operator front door" gap) | node=`cqask/ui` (NL→CadQuery via `CadQueryCodeGeneratorEngine`) + `mcp-cadquery/frontend` (Three.js viewer) | wiring=`cadDispatcher` actions via `mcp-server/web/lib/api.ts` → HTTP bridge :3100 | bridge=cad→frontend-app (quebec consumes prism_* dispatchers) | rgs-toolchain=`/forge-app-wire` + `reviewer` (UX + React-version-align weighted) | acceptance=`cqask/ui` ports to App Router OR runs as sandboxed subapp (React 19 align); NL prompt → CadQuery → 3D viewer renders a JM part round-trip with no console errors.
6. **U-WIRE-BACKLOG: reconcile cad orphan engines + bridge-wire units** | ROI=M (orphan wiring unblocks reachability; cheap, compounding substrate-hardening) | node=audit unwired cad engines (`BobCADCAMBridgeEngine`, `SolidWorksCADExecutionBridge`, `InventorCADCodeGeneratorEngine`, `HyperCADSCodeGeneratorEngine` — verify each has a dispatcher ref) | wiring=`cadDispatcher` / `cadAutomationDispatcher` per-engine | bridge=none (intra-galaxy closure) | rgs-toolchain=`/wire-unwired` + `wiring-review-agent` | acceptance=`stop_on_unwired_assets` clean for cad; every CAD vendor bridge resolves via at least one dispatcher action or carries a `// WIRE-EXEMPT:` tag.
7. **U-CAD-FUNCTION-INDEX: revive the per-CAD-vendor function-index pattern** | ROI=M (dormant build; AI queries "what extrude params does Fusion/Mastercam/FreeCAD expose, units+defaults?" without grepping — replicable across all CAD vendors, directly enables the external-software training plan below) | node=`Fusion360FunctionIndexEngine` (build per MS-CAM-MASTERY pillar B pattern) + extend to hyperCAD-S/FreeCAD/Mastercam | wiring=`cadDispatcher.cad_function_index_query` | bridge=cad→cam (shared with the MS-CAM-MASTERY Fusion function-index) | rgs-toolchain=`/cad-from-blueprint` + `reviewer` | acceptance=`cad_function_index_query "extrude"` returns the Fusion API signature + params + defaults + units from the indexed seat (not grep), with UNITS-FIRST guard on inch/mm.

**External-software training plan (Fusion 360, hyperCAD/hyperMILL, FreeCAD, Mastercam):**
1. **Corpus extraction from installed seats** — enumerate the Mastercam X8 + hyperMILL v31 + FreeCAD install roots (see `[[cad-corpus-paths]]`), extract native model files (.mcam/.hmc/.FCStd) + their exported STEP/IGES as paired (source-model ↔ neutral-format) training tuples; feed `CADReverseCorpusCatalogEngine`. Cite each extracted file path (R9) — never synthetic.
2. **Parity validation against the tool's own output** — for the 20,006-file JM reverse corpus, run delta's STEP AP242 emitter (`cad-step-ap242-emitter.mjs`) and diff against the seat's native STEP export (`U-VALIDATION-ROUNDTRIP` harness, 28/28 tests) — dim-diff every face/feature; promote only when ≥70% E2E. This is the FreeCAD/Fusion ground-truth oracle.
3. **Replicate the vendor's parametric workflow** — drive **live Fusion 360** via `Fusion360LiveBridgeEngine` (`:18360`) + **hyperCAD-S** via `U-HCS-CONNECTOR` host add-in to replay print→CAD build steps, capturing the operation taxonomy (extrude/revolve/pattern/shell) into `CADOperationTaxonomyEngine` as labeled training data; replicate FreeCAD parametric trees via `FreeCADCodeGeneratorEngine` for offline (no-license) augmentation.
4. **Import vendor libraries + build the function-index (U-CAD-FUNCTION-INDEX)** — index each seat's extrude/feature API params, default values, and units into the function-index store so the CAD-AGI queries vendor capabilities structurally; offload the heavy index summarization to Ollama (`127.0.0.1:11434`, qwen2.5-coder) and use NVIDIA NIM/GPU for the CAD-RAG embedding pass over the 129,306-file corpus.

**Atomic gaps (nodes/wirings/bridges):**
- **Never_started milestones:** CADCAM-AGI-MS0 (24u, 0/24), CADCAM-DAGI-MS1 (16u), MS2 (14u), MS3 (10u), MS5 (10u) — all `never_started`; CADCAM-DAGI-MS4 envelope drift (`claims_not_started_but_has_shipped_units`).
- **Unwired/orphan candidates:** verify dispatcher refs for `BobCADCAMBridgeEngine`, `SolidWorksCADExecutionBridge`, `InventorCADCodeGeneratorEngine`, `HyperCADSCodeGeneratorEngine`, `MastercamCADExecutionBridge` (vendor bridges — high orphan risk).
- **Bridges:** cad→quoting symmetric edge needs verification (CLAUDE.md flags "verify quoting declares cad"); cad→academy reciprocity unconfirmed ("verify academy reciprocates"). cad→cam (`feature_recognize`→`cam_strategy_recommend`) and cad→india (`xproc_kg_project_features`) are confirmed symmetric.
- **Frontend nodes:** `cqask/ui` + `mcp-cadquery/frontend` are PENDING_MERGE (unreachable from the App Router today).
- **Fusion node:** add-in `/new`-doubling is an operator/add-in-side gap, not pure-TS-fixable from the slot — needs the host-side fresh-doc create+activate.

**RGS-rule updates this domain needs:** The current `\bcad\b|\bblueprint\b|\bprint[-\s]*to[-\s]*program\b` rule routes ALL cad-intake to a single skill (`/cad-from-blueprint`) — too coarse for delta's actual unit mix. Add finer routes: (1) `\bfeature[-\s]*recogni|\bdfm\b|\btolerance[-\s]*stack\b` → `/cad-feature-recognize` (geometry-analysis units); (2) `\bfusion\b|\bhypercad\b|\blive\s*bridge\b|add-?in` → `/fusion-generate` + `reviewer` (live-host integration units); (3) `\bcorpus\b|\bcoverage[-\s]*matrix\b|reverse[-\s]*engineer` → `/cad-corpus` + `wiring-review-agent` (the dominant P4 population work — currently falls through to the generic `/scrutinize` fallback); (4) `\bcad[-\s]*train|cad[-\s]*rag|cadcam[-\s]*agi\b` → `/cad-train` + `test-team`. Without these, the high-volume corpus/feature/training units mis-route to the generic review fallback.

---

## XRAY — blueprint-vision RGS PIPELINE ROADMAP (P4)
**North-star (1 line):** Universal front-door — any unstructured input (blueprint/PDF/raster/native CAD) → clean, mm-normalized, per-field-confidence structured PRISM data; Blueprint coverage 91%→100% + a galaxy SVI row.
**Time-allocation (from transcripts):** REDIRECT — re-anchor off the romeo BLACKWELL-DB-GEN shadow (latest 2 sessions shipped 0 xray units while borrowing romeo's RESUME line — the documented attribution-drift hazard). Spend the next block on xray-owned domain work in this order: (1) source REAL dim candidates for `cad_dimension_reconcile` (started-but-unbuilt), (2) promote the A/B vision model so OCR confidence rises measurably, (3) forge the 4 missing native readers + close the 1 unwired Blueprint asset to 100%. The Blackwell GPU is a *tool* xray uses (concurrent OCR), not a milestone xray owns.

**Pipeline units (HIGH-ROI ORDERED):**
1. **U-XRAY-DIM-ADAPTERS: 3 thin real-candidate adapters for `cad_dimension_reconcile`** | ROI=H — closes the started-but-unbuilt NEXT step on the only xray-owned thread (`CrossSourceDimensionReconciliationEngine`, shipped `a57ef19c2d`); turns the engine from synthetic to real-data | node=wire 3 readers into `CrossSourceDimensionReconciliationEngine` (OCR-store adapter → search `jm-die-database/.index/*.jsonl` + Docustrata, never re-OCR; STEP-geometry adapter → `cad_step_parse_file` with INCH→mm CONVERSION_BASED_UNIT 25.4 guard; G-code coord-span adapter → toolpath bbox) | wiring=`prism_cad.cad_dimension_reconcile` (already wired — feed it real candidates) | bridge=juliett (SEARCH `manifest.json`+`.index/`, R8 never re-OCR) → ai-training (`xproc_outcome_publish slot=xray`) | rgs-toolchain=`/cad-from-blueprint` + `wiring-review-agent` (B-arm: source-adapter integrity, conflict-flag-never-average) | acceptance=survey workflow finds ≥20 JM parts with all 3 sources; reconcile emits per-candidate provenance + R12 conflict-flag (never averages divergent dims); real-data E2E on ≥1 JM part (pure-core+injected-readers MUST ship a real-data E2E per RGS-MS1 lesson)
2. **U-XRAY-VISION-AB-PROMOTE: run the A/B vision-OCR benchmark on a quiet fleet + promote winner** | ROI=H — `bench-vision-ocr-ab.mjs` + `vision-ab-compare.mjs` shipped 2026-06-03 but the empirical run *pends a quiet fleet* (vision cold-load starves under live ollama contention); promotion is the single lever that raises measured OCR confidence | node=`scripts/lib/vision-model-select.mjs` (Blackwell big-VRAM seam) — produce a real upgrade/stay verdict | wiring=feeds `BlueprintVisionOCREngine` model selection (`cad_live_blueprint_ocr`) | bridge=ai-training (india fine-tunes the vision model — defer model rollout to india's surfaces) | rgs-toolchain=`/scrutinize` + `test-review-agent` (verdict reproducibility, paired-sample integrity) | acceptance=paired A/B over a real JM print sample emits upgrade/stay with measured Δconfidence (not synthetic); winner persisted to `vision-model-select.mjs`; offload stat recorded
3. **U-XRAY-NATIVE-SAT-XT: forge native readers for SAT + X_T (Parasolid)** | ROI=M-H — both confirmed MISSING on disk (`ls` MISSING); silent-empty-parse on these formats corrupts every downstream consumer (the "trusting a CAD parser's silent success" anti-pattern) | node=`SATParserEngine.ts` + `XTParserEngine.ts` (or vendor-SDK bridge) — emit geometry + volume-vs-filesize cross-check | wiring=new `prism_cad` actions `cad_sat_parse` / `cad_xt_parse` (cadDispatcher) | bridge=cad/delta (delta CONSUMES xray geometry schema — match `cad_step_parse_file` output shape) | rgs-toolchain=`/forge-triple` (engine+skill+hook) + `wiring-review-agent` (per-format test required; STEP≠IGES unit-default discipline) | acceptance=per-format parser + per-format test; volume-vs-source-size cross-check flags implausible empty geometry; mm-normalized output; round-trips a real SAT/X_T sample
4. **U-XRAY-NATIVE-OBJ-FBX: forge native readers for OBJ + FBX** | ROI=M — completes the standing 4-reader gap (OBJ/FBX MISSING); mesh-format intake widens the front-door coverage | node=`OBJParserEngine.ts` + `FBXParserEngine.ts` (reuse `STLToVoxelGridEngine` voxel path for mesh) | wiring=`prism_cad` actions `cad_obj_parse` / `cad_fbx_analyze` | bridge=none (mesh-only; no GD&T tie) | rgs-toolchain=`/forge-triple` + `wiring-review-agent` | acceptance=magic-byte format sniff routes correctly; mesh→voxel volume computed; per-format test; flags non-watertight mesh
5. **U-XRAY-BLUEPRINT-100: close the 1 unwired Blueprint asset → 100% + register galaxy SVI row** | ROI=M — Blueprint is 91% (10/11 wired per BUILD_STATE); the single unwired engine is the reachable-units lever to 100%, and the SVI row makes progress-to-1.0 measurable not inferred | node=identify the 1 unwired Blueprint engine via `audit-unwired-engines.mjs`, wire it | wiring=its natural `prism_cad`/`qualityDispatcher` action | bridge=none | rgs-toolchain=`/wire-unwired` + `wiring-review-agent` (silent-no-op verify — hand to uniform per fleet plan) | acceptance=Blueprint 11/11=100% in regenerated BUILD_STATE; SVI row registered (blueprint-vision psi); uniform confirms non-silent fire
6. **U-XRAY-FOREIGN-OCR: Polish/Spanish annotation OCR for JM shop-floor prints** | ROI=M — JM Die operators are Polish/Spanish-primary ([[project_jm_die_shop_floor_languages]]); annotations carry foreign-language text the English-default OCR misreads → silent extraction errors | node=add multilingual model/lang-pack route to `BlueprintVisionOCREngine` (vision-LLM path) + per-field confidence on foreign tokens | wiring=`prism_cad.cad_live_blueprint_ocr` (lang param) | bridge=ai-training (outcomes feed india for label correction) | rgs-toolchain=`/pdf-learn` + `test-review-agent` | acceptance=real JM print with Polish/Spanish callouts extracts non-English annotations with confidence; CP1252-mangle guard (verify via node, not Read tool)
7. **U-XRAY-OVERNIGHT-GPU-OCR: execute the overnight GPU-OCR batch (12,321 blueprints need OCR)** | ROI=M — corpus population is the P4 catch-up work order; 12,321 blueprints flagged needing GPU-OCR in the training-readiness manifest; the overnight reaper-immune vehicle (qwen3-vl:8b) is already built | node=run `overnight OCR batch vehicle` + `-CONCURRENT-OCR` using `estimateExtractionPlan()` host-aware sizing on the Blackwell box | wiring=`resourceExtractionDispatcher.ocr_process` + ledger to `state/shared/blueprint-extraction-*-<date>.jsonl` | bridge=ai-training (`BlueprintLoRABridgeEngine.blueprint_lora_prepare_set`) → cad/delta round-trip eval | rgs-toolchain=`/extract-xray` (multi-print-aware macro) + `reviewer` | acceptance=source-SHA dedup (never re-OCR), one result per print (multi-print split first), per-field confidence ≥0.70 floor; morning digest (`-REVIEW`); extractions land in juliett's ingestion path

**External-software training plan (GPU-OCR/NVIDIA NIM, Docker):**
1. **Concurrent vision-OCR on the Blackwell GPU (qwen3-vl:8b @ Ollama 127.0.0.1:11434):** run `estimateExtractionPlan()` to size host-aware concurrency, then drive the overnight reaper-immune batch over the 12,321 flagged blueprints — replacing the baked-in 16GB overnight-only path with concurrent extraction the Blackwell 96GB unlocks. This *trains the pipeline on the real JM corpus* (8,154→36,638 split prints), not synthetic dims.
2. **A/B parity validation against vendor CAD outputs:** for STEP/Fusion/FreeCAD native parsers, extract geometry then cross-validate dimensions against the installed-seat ground truth — Fusion 360 live drive-and-replay (delta's bridge), FreeCAD `.fcstd` via `FCStdNativeParserEngine`, Mastercam X8 STEP imports — feeding `cad_dimension_reconcile` a STEP-geometry candidate that must agree with OCR + G-code spans (R12 conflict-flag on disagreement).
3. **NVIDIA NIM GPU-OCR for high-throughput raster extraction:** route the raster-scan path (deskewed JM viewer-archive scans, 85,345 files) through NIM containerized vision when local Ollama cold-load starves under fleet contention — the quiet-fleet blocker the A/B benchmark hit. Dockerized NIM gives a reproducible, isolated OCR worker that survives the fleet-reaper.
4. **Corpus replication / library import (search-never-re-OCR):** import juliett's consolidated DocuStrata + JM-file DB (`jm-die-database/.index/*.jsonl`, 257,992 files) + Qdrant tribal/wiki collections as the *prior-print store* — every extraction first searches `manifest.json`+`.index/` for a previously-extracted SHA before spending GPU, replicating the paid-for corpus into the dim-reconcile OCR-store adapter rather than re-OCRing it (R8).

**Atomic gaps (nodes/wirings/bridges):**
- **Unwired node (1):** 1 of 11 Blueprint engines unwired (BUILD_STATE Blueprint 91%) — name via `audit-unwired-engines.mjs`, wire to its `prism_cad`/quality action (U-XRAY-BLUEPRINT-100).
- **Missing nodes (4):** `SATParserEngine`, `OBJParserEngine`, `FBXParserEngine`, `XTParserEngine` (Parasolid) — all confirmed MISSING on disk; standing native-reader gap → silent-empty-parse paths (U-XRAY-NATIVE-SAT-XT, U-XRAY-NATIVE-OBJ-FBX).
- **Incomplete wiring:** `CrossSourceDimensionReconciliationEngine` is wired to `cad_dimension_reconcile` but fed only synthetic candidates — the 3 real-source adapters (OCR-store/STEP/G-code) are the missing edge (U-XRAY-DIM-ADAPTERS).
- **Missing bridge (asymmetric edge):** juliett's CLAUDE.md lists charlie/echo/hotel as consumers but **NOT xray** — xray reads juliett's DB but the edge is undocumented juliett-side; ping juliett to add the xray consumer edge (advisory, per STEP 10a).
- **Pre-existing (not xray-introduced):** 7 tsc errors in `cadDispatcher.ts` (lines 3262/4086-4091/4708) — left untouched, peer-owned baseline.
- **SVI gap:** no galaxy SVI row for blueprint-vision psi (FLEET-DOMAIN-GOALS Tier-3 #17, ORPHAN) — register in U-XRAY-BLUEPRINT-100.

**RGS-rule updates this domain needs:** The current `rgs-pipeline-rules.mjs` routes `\bcad\b|\bblueprint\b|\bprint-to-program\b` → `/cad-from-blueprint` and `pdf|document|catalog` → `/pdf-learn`, which covers most xray units. Add: (1) a rule for `ocr|vision[-\s]?ocr|dimension reconcil|cross-source dim` → route to a vision-extraction skill (`/extract-xray` / `/cad-extract`) — today OCR/dim-reconcile units only match the broad `/cad-from-blueprint` rule and miss the multi-print-split + per-field-confidence discipline; (2) a rule for `gd&t|gdt|fcf|tolerance stack|datum` → `/cad-tolerance-check` so GD&T units get the datum-schema-tie review weighting; (3) a `native (cad )?(reader|parser)|sat|parasolid|x_t|step|iges|fcstd|f3d` → `/forge-triple` + `wiring-review-agent` with a per-format-test acceptance note (the volume-vs-size silent-empty-parse guard). Otherwise none — the structural `/forge-triple` and `/wire-unwired` rules already cover the build/wire units correctly.

---

## LIMA — academy RGS PIPELINE ROADMAP (P4)
**North-star (1 line):** A self-compounding course factory — harvest the real corpus (MIT-OCW + 8,752-page pypdf JM-Die) into citation-disciplined courses, then close course → certification → shop-floor qualification so learning is load-bearing (an operator can't run a machine until certified), with course-efficacy outcomes feeding india.
**Time-allocation (from transcripts):** On-track but drifting — lima's last 33h spent shadow-borrowing alpha's BLACKWELL-TOKEN + PSN-synergy RESUME lines (precompact padding), not academy. Redirect: close the PRISM-ACADEMY-MOBILE/FEATURES-MS0 in-flight tail (`U-ACADEMY-TAG-METADATA`, ProgressTracker/Assessment active-path, phone E2E) FIRST, then the operator-priority MIT-OCW harvest + SVI registration. Keep shared-infra rescue (MCP ESM fixes) as opportunistic, not the lane.

**Pipeline units (HIGH-ROI ORDERED):**
1. **U-ACADEMY-SVI-SUBSYSTEM: register academy as an SVI "Courses/Curriculum" reachability axis** | ROI=H — operator lever #1; academy progress is invisible today (no SVI dimension), so every other unit is unmeasurable | node=wire `CurriculumEngine` + `LearningProgressionEngine` into `SVIEnhancedCalculatorEngine` (new subsystem descriptor; do NOT build a new SVI engine) | wiring=`prism_dev:svi_ranked_backlog` + `prism_session:master_index_node_status` surfaces the Courses axis | bridge=academy→system-viz (`documented-by` cross-substrate edge, courses roost) | rgs-toolchain=`/scrutinize` + `code-analyzer` (generic fallback fires — see rule-updates) | acceptance=`svi_ranked_backlog` returns a non-null Courses dimension with 29-catalog/37-wired count; vitest asserts subsystem registered
2. **U-GAP-ACADEMY-MIT-OCW-INGEST: harvest MIT-OCW at scale into the EMPTY extracted dir** | ROI=H — lima's original mandate; `mcp-server/data/extracted-knowledge/mit-courses/` is literally 0 files, starving the already-wired mcfi_/mcdl_ dispatchers | node=`MitCourseIndexEngine` (indexes 200+ OCW) → `MITCourseDeepLearningEngine` (mcdl_extract_algorithm) — wire-don't-rebuild, both exist | wiring=`prism_dev:mcfi_query` + `prism_dev:mcdl_find_relevant_courses` return real harvested data (not stubs) | bridge=academy→knowledge-conversion (3-lane router, juliett-adjacent) | rgs-toolchain=NEW `/learn-corpus` + `reviewer` (see rule-updates) | acceptance=`mcdl_get_all_course_ids` returns ≥50 real OCW course ids; every extracted algorithm carries an ISO/Kienzle/Taylor citation (R9)
3. **U-GAP-ACADEMY-COURSES-FROM-CORPUS: auto-build courses from the 8,752-page pypdf JM-Die corpus** | ROI=H — corpus already extracted (`jm-die-corpus-pages.jsonl`, 17.5MB / 73 PDFs); the populated app becomes teachable | node=`CourseBuilderEngine` + `KnowledgeCurriculumBridgeEngine` (corpus→problem-sets) | wiring=`prism_knowledge:learn_course_from_source` + `course_build_*` | bridge=academy→pdf-corpus (uses lima's fleet-canonical pypdf extractor) | rgs-toolchain=`/pdf-learn` (confidence 0.80) + `reviewer` | acceptance=≥5 new `course-*.ts` generated from corpus pages with cited source-page provenance; `learn-course-autogen.test.ts` green
4. **U-ACADEMY-CATALOG-DRIFT-FIX: resolve 37-wired-vs-29-catalog drift (complete-by-construction)** | ROI=M — content backbone is silently incomplete; 37 wired courses but only 29 `course-*.ts` files in integration tree (8 phantom or slot-worktree-stranded) | node=`CurriculumEngine` catalog reconciliation + `scripts/audit-academy-prereq-chain.mjs` (6 problem classes) | wiring=`prism_knowledge:academy_course_list` count == on-disk count | bridge=none | rgs-toolchain=`/scrutinize` + `wiring-review-agent` (catalog↔dispatcher coverage) | acceptance=`audit-course-dispatcher-citations.mjs` reports 0 wired-without-file; merge slot/lima 35..60 expansion into integration tree
5. **U-ACADEMY-CERT-SCHEDULER-LOOP: close course → certification → shop-floor qualification gate** | ROI=H — the load-bearing academy feature; makes learning enforce-able (operator blocked from a machine until certified) | node=`CertificationTrackingEngine` → `EmployeeRoleAcademyInjectionEngine` → `AutomatedJobSchedulerEngine` qualification gate (all 3 exist — wire the chain) | wiring=`prism_business:learning_assess` + scheduler reads cert state before machine-assign | bridge=academy→business/HR (`EmployeeMachineDomainAcademyEngine`) + academy→shop-floor (scheduler) | rgs-toolchain=`/scrutinize` + `reviewer` (integration + safety-gate weighted) | acceptance=E2E: uncertified operator → scheduler refuses VMC-01 assignment; `CertificateEngine` emits valid-for window; india receives course-efficacy outcome
6. **U-ACADEMY-MS0-TAIL: finish PRISM-ACADEMY-MOBILE/FEATURES-MS0 in-flight tail** | ROI=M — open since AcademyHub overhaul (`cbaaeea215`); `U-ACADEMY-TAG-METADATA` (Course.tags[] migration), ProgressTracker/Assessment active-path, storage-degraded toast, 44pt touch-target fix | node=`LearningProgressionEngine` active-path + web `LearningLayout`/ContinueLearningWidget | wiring=`prism_operating_system:course_progress` reflects active-path | bridge=academy→frontend-app (hotel wires ContinueLearningWidget into DashboardPage hero, already handed off via AGENT_CHAT.jsonl) | rgs-toolchain=`/scrutinize` + `reviewer` (UX + state-management weighted, .tsx) | acceptance=phone E2E (Justin verify) passes; ProgressTracker shows active learning path; PWA service-worker offline-loads a course
7. **U-BRIDGE-WIRE-VIDEO / U-BRIDGE-LEARN-CAM: wire video-learning + CAM-learning leg into AI substrate** | ROI=M — academy is the "only post-processor that learns" leg; captures CNC YouTube/CAM-vendor tutorials no competitor does | node=`VideoELearningAIEngine` + `VideoLearningEngine` + `AIResourceLearningEngine` (corpus→AI capability) | wiring=`aiReasoning:video_elearning_*` + physics-validator flags Kienzle-violating tips | bridge=academy→ai-training (india) `U-BRIDGE-AI-TIER1-TIER2` | rgs-toolchain=`/video-learn` (confidence 0.80) + `physics-reviewer` (validate every ingested tip) | acceptance=1 real CAM-vendor video → cited course module; physics-validator rejects an injected speed/feed tip that violates `constants.ts`

**External-software training plan (PDF corpus, MIT-OCW):**
1. **MIT-OCW exhaustive harvest** — run `MitCourseIndexEngine` against the 206-course OCW catalog (target the 25 CNC/CAM-relevant: 2.008 Design&Manufacturing, 2.810 Manufacturing Processes, 2.830 Control of Manufacturing); `MITCourseDeepLearningEngine.mcdl_extract_algorithm` extracts the ~3,880 atomic skills into `extracted-knowledge/mit-courses/`; each algorithm links to `physics/constants.ts` via `mcdl_link_to_physics_constants` and cites the source lecture (R9 — `mcdl_cite_sources`).
2. **JM-Die pypdf corpus replication** — re-run the canonical `scripts/extract-jm-die-corpus-page-by-page.py` (76× deeper than pdf-parse) over `H:/PRISM/JM DIE/{TRIBAL,WIKI}` (80 PDFs / 1.1GB) + `H:/PRISM/resources` 1,008-PDF vendor catalogs (Sandvik/Iscar/Kennametal); feed pages into `CourseBuilderEngine` to auto-generate cited courses — replicating the real shop's training material into the app.
3. **CAM/CAD seat workflow replication** — `VideoELearningAIEngine` ingests the installed Mastercam X8 + hyperMILL v31 + Fusion E-Learning corpora (paths in `reference_cam_corpus_locations.md`); extract each vendor's documented strategy workflow into a course module, parity-validating against the seat's own documented output (R9 — assert the extracted feed/speed matches the seat's posted value, not a hardcoded stub).
4. **Ollama-offload the summarize/tag leg** — route course-content summarization + auto-tagging + lint to local qwen2.5-coder via `/ask-local` / `learn-corpus` when Ollama @127.0.0.1:11434 is up (it was offline session-wide — gate behind a liveness probe, fall back to honest `confidence:draft` tips); reserve Claude for synthesis + citation-validation.

**Atomic gaps (nodes/wirings/bridges):**
- **EMPTY data node (critical):** `mcp-server/data/extracted-knowledge/mit-courses/` = 0 files — mcfi_/mcdl_ dispatchers are wired but data-starved (the dormant mandate).
- **Catalog drift:** 37 wired courses vs 29 `course-*.ts` on disk — 8 phantom/stranded (slot/lima 35..60 expansion not merged to integration tree).
- **Missing bridge — cert→scheduler:** `CertificationTrackingEngine`/`EmployeeRoleAcademyInjectionEngine` exist but are NOT wired into `AutomatedJobSchedulerEngine`'s qualification gate — the load-bearing loop is open.
- **Missing SVI axis:** no academy/Courses subsystem in `SVIEnhancedCalculatorEngine` — flagged `[ORPHAN — cross-cutting, no envelope]` in trajectory Tier-3 #17.
- **Orphan cross-substrate node:** academy galaxy has a `documented-by` edge to `memory_patterns.academy_synthesis` but no `owned-by-slot` lima edge materialized in courses roost.
- **Outcome bridge unverified:** `U-BRIDGE-WIRE-OUTCOME` (course-efficacy → india) listed eligible but never shipped — the self-improvement loop is unclosed.

**RGS-rule updates this domain needs:** Two real gaps in `scripts/lib/rgs-pipeline-rules.mjs` — academy units currently fall through to the 0.30 `/scrutinize` generic fallback (no academy branch):
1. **Add a course/curriculum/learning pipeline rule** — `test: /\bcourse\b|\bcurriculum\b|\blesson\b|\bquiz\b|\bcertif|\bMIT[-\s]?OCW\b|learning[-\s]path|academy/i` → `skill: "/learn-corpus"` (or `/learn-pipeline`), `why: "academy course/curriculum/MIT-OCW build"`, `confidence: 0.80`. This captures `U-GAP-ACADEMY-*` + `U-PAM-MODEXPAND-*` which today match nothing. Polysemy guard: exclude `\bof course\b` (regex `(?<!\bof\s)course`) to avoid the conversational false-fire.
2. **Add a `reviewer`-weighted-on-citation-integrity agent rule** in `matchAgents` for the same academy keywords — academy's R9 invariant is "every extracted tip cites a real standard (ISO 6983/3685/8688, Kienzle 1952, Taylor 1907)", which `physics-reviewer` doesn't check and `code-analyzer` misses; route academy units to `reviewer` weighted on citation/provenance completeness. (`/pdf-learn` + `/video-learn` rules already exist and correctly route the intake leg — no change there.)

## RGS-SYSTEM UPDATE recommendations

Consolidated punch list of every new rule/pattern the 16 domains need in `scripts/lib/rgs-pipeline-rules.mjs`. Today, ~42% of fleet units fall to the `GENERIC_FALLBACK` (`/scrutinize` @0.30) because the rule table has no branch for them. These rules close that gap so RGS routes the new external-software + new-feature units to the right skill + reviewer instead of the weakest default. **30 rule changes** across PIPELINE_RULES + AGENT_RULES, ordered so structural-exclusion (wedm/route polysemy) guards stay correct.

**A. NEW PIPELINE_RULES (skill routes) — add these BEFORE the generic fallback, with word-boundary `\b` anchors:**

| # | domain | `test` regex | `skill` | confidence | note |
|---|--------|--------------|---------|-----------|------|
| 1 | sierra | `/\b(regen-viz\|system-viz\|ghost-roost\|augmentation\|merge-augment\|node-embedding\|graph-merge)\b/i` | `/audit-viz-first` (or `/system-viz`) | 0.80 | graph-infra streaming-rewrite — NOT engine wiring; reviewer=`code-analyzer` |
| 2 | india | `/\b(lora\|gnn\|graphsage\|neural net\|deep learning\|self[-\s]?improv\|retrain\|fine[-\s]?tune)\b/i` | `/ai-train-india` | 0.80 | AI-training units (skill exists, currently unrouted) |
| 3 | india | `/\b(rag\|embedding\|semantic search\|corpus embed\|vector index\|hnsw\|rerank\|retrieval)\b/i` | `/wiki-query` (+`/deep-search`) | 0.75 | RAG/retrieval — query substrate first |
| 4 | golf | `/\bfleet[-\s]?(reaper\|health\|hygiene\|memory\|task)\b\|\borphan[-\s]?process\b\|\bzombie\b\|\bmcp[-\s]?(restart\|watchdog\|daemon\|supervisor)\b\|\bscheduled[-\s]?task\b\|\bdocker[-\s]?wedge\b/i` | `/fleet-reaper` | 0.80 | place ABOVE `/wire-unwired` so "wire the MCP actuator" → hygiene not generic wiring |
| 5 | oscar | `/\bsfc\b\|speed[-\s]?feed\|\bcalibrat/i` | `/auto-speed-feed` | 0.80 | SFC calibration units off the 0.30 fallback |
| 6 | oscar | `/g-?wizard\|hsmadvisor\|toolcrib\|tooldb2/i` | `/auto-speed-feed` | 0.80 | vendor-parity branch |
| 7 | echo | `/\bpost[-\s]?proc\|\bpost[-\s]?gen\|\bg[-\s]?code\b\|\bdialect\b\|\bcontroller\b\|\bmaster[-\s]?post\b\|byte[-\s]?equiv\|\bcimco\b\|\bwinmax\b/i` | `/post-generate` (cascade: `/post-validate` `/post-harden` `/post-diff` `/post-register`) | 0.80 | guard `\bpost\b` vs postpone/poster; WEDM-post units must hit `/wedm-program` FIRST |
| 8 | kilo | `/add-?in\|in-seat\|fusion (button\|pillar)\|mastercam bridge\|hyperMILL bridge\|F360-REV/i` | `/cam-bridge` | 0.80 | in-seat add-in + vendor-bridge units |
| 9 | foxtrot | `/replicate[-\s]?from[-\s]?print\|replication\|print[-\s]?to[-\s]?program[-\s]?replication/i` | `/cad-from-blueprint` (+`/mill`) | 0.80 | "replicate" matches nothing today |
| 10 | foxtrot | `/\bhypermill\b\|\b\.hmt\b\|cycle[-\s]?catalog/i` | `/hypermill-full-job` (or `/cam-strategy`) | 0.80 | HyperMILL sub-galaxy → its skill family |
| 11 | whiskey | `/lathe.*master.*post\|lathe.*postgen\|\bOSP\b\|\.MIN/i` | `/lathe-master-post` | 0.80 | post-productization units (vs generic `/lathe`) |
| 12 | whiskey/echo | `/\bmill[-\s]?turn\b\|\bswiss\b\|\bsub[-\s]?spindle\b\|\bguide[-\s]?bushing\b/i` | `/lathe` (+`physics-reviewer`) | 0.80 | safety-critical Swiss/sub-spindle dormant-revival |
| 13 | mike | `/(lora\|dataset.?builder\|training.?pair\|retrain).*wedm\|wedm.*(lora\|dataset)/i` | `/wedm` → `wedm-train` | 0.75 | wedm dataset/training sub-route (inherits parent today) |
| 14 | mike | `/(dialect\|controller.?post\|post.?emit).*wedm\|wedm.*(dialect\|post)/i` | `/wedm-controller` (+CIMCO-sim acceptance) | 0.75 | wedm dialect/post sub-route |
| 15 | romeo | `/ghost[-\s]?action\|zod enum\|ghost\.\*/i` | `/scrutinize` (+`wiring-review-agent`) | 0.75 | ghost-action work recognized as wiring |
| 16 | romeo/oscar | `/catalog db-?gen\|tmlib\|\.machine\b\|\.hmt\b\|vendor catalog/i` | `/pdf-learn` (+`reviewer` export-parity lens) | 0.75 | catalog/export round-trip parity |
| 17 | charlie | `/\bquot(e\|ing)\b\|\bcost[-\s]?(estimat\|model\|index)\b\|\bpricing\b\|\bmargin\b\|\bDFM\b\|\bmonte[-\s]?carlo\b\|P10\/P50\/P90/i` | `/quote-to-ship` | 0.80 | quoting/cost — guard `\bcost\b` vs Costa/costume |
| 18 | alpha | `/\bollama\b\|\boffload\b\|local[-\s]?llm\|qwen\|route.?(profile\|nudge)\|model[-\s]?rout/i` | `/ollama-route-check` | 0.85 | local-LLM offload / model-routing |
| 19 | alpha | `/\btoken(s)?\b\|\bcost\b\|budget\|context[-\s]?budget\|efficien\|cache\|CAG\|prompt[-\s]?cache/i` | `/token-dashboard` | 0.80 | token-economy / cost-efficiency |
| 20 | alpha/sierra | `/obsidian\|vault\|memory[-\s]?sync\|knowledge[-\s]?vault/i` | `/route-to-obsidian` | 0.80 | Obsidian/vault context-retention |
| 21 | hotel | `/\b(erp\|quote.?to.?ship\|invoice\|payroll\|gl\|ledger\|accounting\|cost.?feedback\|oee\|spc\|kaizen\|six.?sigma\|job.?cost\|ap\|ar\|credit.?review)\b/i` | `/biz-health` (or `/erp-health` `/job-cost`) | 0.80 | business/ERP/finance — entirely unrouted today |
| 22 | hotel/general | `/persistence\|\bpg\b\|durable[-\s]?state/i` | `/wire-unwired` (+fail-loud-on-fallback acceptance hint) | 0.75 | in-memory-fallback class is a recurring archaeology gap |
| 23 | delta | `/\bfeature[-\s]?recogni\|\bdfm\b\|\btolerance[-\s]?stack\b/i` | `/cad-feature-recognize` | 0.80 | geometry-analysis units |
| 24 | delta/xray | `/\bfusion\b\|\bhypercad\b\|\blive\s*bridge\b\|add-?in/i` | `/fusion-generate` | 0.80 | live-host integration |
| 25 | delta | `/\bcorpus\b\|\bcoverage[-\s]?matrix\b\|reverse[-\s]?engineer/i` | `/cad-corpus` | 0.80 | dominant P4 CAD population work |
| 26 | delta | `/\bcad[-\s]?train\|cad[-\s]?rag\|cadcam[-\s]?agi\b/i` | `/cad-train` | 0.80 | CAD self-improving loop |
| 27 | xray | `/ocr\|vision[-\s]?ocr\|dimension reconcil\|cross-source dim/i` | `/extract-xray` (or `/cad-extract`) | 0.80 | OCR/dim-reconcile (multi-print-split discipline) |
| 28 | xray | `/gd&t\|gdt\|fcf\|tolerance stack\|datum/i` | `/cad-tolerance-check` | 0.80 | GD&T datum-schema-tie |
| 29 | xray | `/native (cad )?(reader\|parser)\|\bsat\b\|parasolid\|x_t\|\bstep\b\|iges\|fcstd\|f3d/i` | `/forge-triple` (+per-format-test acceptance) | 0.80 | native-reader silent-empty-parse guard |
| 30 | lima | `/(?<!\bof\s)course\b\|\bcurriculum\b\|\blesson\b\|\bquiz\b\|\bcertif\|\bMIT[-\s]?OCW\b\|learning[-\s]?path\|academy/i` | `/learn-corpus` (or `/learn-pipeline`) | 0.80 | academy — exclude `of course` polysemy |

**B. NEW AGENT_RULES (reviewer routing) — `matchAgents` additions:**
- **india:** training/model units → `test-review-agent` (candidate→holdout is a test-integrity problem); LoRA units emitting G-code → ADD a `physics-review-agent` arm (generative gate touches S(x), not classification AUROC).
- **golf:** `/\bfleet[-\s]?(reaper\|health\|hygiene)\b\|\borphan\b.*\bprocess\b\|\bmcp[-\s]?daemon\b/i` → `wiring-review-agent` — BUT the existing `force\|thermal\|feed\|speed` physics test must precede it so U-MACHINE-HANDBOOK spindle/torque limits still get `physics-reviewer`.
- **charlie:** `/\bquot(e\|ing)\b\|\bcost\b\|\bpricing\b/i` → `code-analyzer`, weighted on cost-model correctness + units-safety (the gotcha-#7 grain-blend trap); exclude pure-ERP (route to hotel per R7).
- **alpha:** `/token\|cost\|ollama\|offload\|budget\|cache/i` → `code-analyzer` (non-physics reviewer for token-economy units).
- **hotel:** business/ERP units → `reviewer` weighted on no-inline-tax-tables + `debits===credits` invariant + link-only financial-pointer discipline + PII/consent-on-export.
- **lima:** academy units → `reviewer` weighted on citation/provenance completeness (R9 — every tip cites ISO 6983/3685/8688 / Kienzle 1952 / Taylor 1907; physics-reviewer doesn't check this).

**C. STRUCTURAL-EXCLUSION guards to preserve (do NOT regress):**
- The wire-EDM `/wire-unwired` false-match exclusion (`rgs-pipeline-rules.mjs` lines 67-68 + 189-190) — already learned twice; the new golf rule #4 + romeo ghost-action rule #15 must sit so `wedm` context still excludes from `/wire-unwired`.
- The echo `/post-generate` rule #7 must NOT swallow WEDM-post units — `/wedm-program` matches first.
- The lima rule #30 `of course` negative-lookbehind, the charlie `\bcost\b` boundary, and the echo `\bpost\b` boundary are all anti-polysemy guards — keep them.

## External-software training matrix

Every installed tool with its owning slot(s) and an exhaustive-utilization training action. The pattern fleet-wide: extract the seat's real corpus → parity-validate PRISM output against the seat's own output → replicate its workflow → import its libraries; cite every datum (R9), offload heavy text to Ollama (R5).

| software | owning slot(s) | training action |
|----------|----------------|-----------------|
| **Fusion 360** | delta (CAD), kilo (CAM), oscar (SFC button), whiskey (mill-turn), xray (STEP import) | Drive `Fusion360LiveBridgeEngine` (`:18360`) to replay print→CAD build steps + replicate CAM/HSM dialog workflow via `CAMAddInFrameworkEngine`; capture op-taxonomy (extrude/revolve/pattern) into `CADOperationTaxonomyEngine`; parity-diff PRISM STEP AP242 emit vs Fusion's native export (≥70% E2E gate); import `.machine` configs as ground-truth (romeo round-trip-verify). Fix the add-in `/new` fresh-doc bug first (delta U-FUS-NEW-DOC-FIX). |
| **hyperMILL v31** | kilo (CAM), foxtrot (mill), echo (post), romeo (`.hmt` lib) | Extract demo/standard tool DB + cycle catalog via `HyperMillDemoDbExtractor`/`HyperMillIMDbExtractor`/`HyperMillStandardToolDB` → `.hmt` parity; replicate 5-axis RTCP + collision-pre-check emit; byte-validate `HyperMillCodeGeneratorEngine` macro output vs seat-generated macros for a known JM pocket/contour; import `.hmt` tool/holder libs (romeo round-trip). |
| **Mastercam X8** | kilo (CAM), echo (post), whiskey (mill-turn), foxtrot (mill), oscar/romeo (tool DB), lima (E-learning) | Index 52 Mastercam posts + 160,582-NC/13,790-.cps corpus (juliett DB-indexed); replicate emit for a sample part-set, classify each PRISM emit byte-identical\|volatile-header-only\|semantic-drift via golden round-trip classifier; index lathe/mill-turn op taxonomy into the strategies registry; feed divergences to india as reward labels (`xproc_outcome_publish`). |
| **Esprit** | kilo (CAM) | Parity-validate `cam_strategy_recommend`→`toolpath_generate` against Esprit in-seat output where the seat permits (stepover/engagement/retract); surface honest divergence as the "matches your seat" proof. |
| **CIMCO Edit / CIMCOSimulation** | echo (post-verify spine), mike (wedm sim), whiskey (lathe byte-class), romeo (`.tmlib` export) | Offline arm shipped (`CimcoVerificationBridgeEngine`, 6 actions); light up live `CIMCOSimulation.exe` headless (bundled MariaDB) against 86 `.mcfg` + 44 `.eRPost` (mapped to 15-machine JM fleet); every post-proof tier emits→CIMCO sim→PASS/FAIL captured as proof-of-correctness; resolve 246-247 golden-drift groups (operator picks one baseline/part). Round-trip PRISM tools→`.tmlib`→re-open in CIMCO Edit 2026 (romeo field-fidelity gate). |
| **Hurco WinMax** | foxtrot (mill), echo (post) | Parse 25 JM `.hnc` WinMax programs as ground-truth labels; replicate conversational→ISO behavior; byte-match `HurcoV11MillMasterPostEngine` (92K) vs live WinMax sim (condition-1 spine re-proven per `U-WINMAX-LIVE-PROBE-FINDING`); extend to canned-cycle + 5-axis. |
| **G-Wizard** | oscar (SFC parity), foxtrot (mill cut-physics) | `GWizardAdapterEngine` reads `toolcrib.csv` (41,209 tools); drive `GWizardComparatorBridgeEngine` over every crib tool × material axis (crib has no material — supply it); ledger vc/fz/rpm/feed diffs as the 3rd tri-comparator leg; export PRISM ShopTool[]→`toolcrib.csv` so a G-Wizard seat consumes PRISM tools. (Leave the empty `GWizard.db` machines table deferred per U-OSC9-15.) |
| **HSMAdvisor** | oscar (SFC parity), foxtrot (mill cut-physics), whiskey (lathe SFM sanity) | `HSMAdvisorAdapterEngine` reads `user_tool_lib.tooldb2.xml` (41,209 tools) + `machines.xml` (12 machines); run `HSMAdvisorComparatorBridgeEngine` across tool×material cross-product, assert PRISM Vc/fz/rpm/feed within tolerance — each divergence → calibration training pair; round-trip export ShopTool[]→tooldb2.xml/machines.xml (import↔export fidelity). |
| **Ollama** (@127.0.0.1:11434) | golf (substrate owner), india (consumer), alpha (router), all (offload) | Golf enumerates every installed model (`/api/tags`), builds per-model latency/throughput parity table, captures cold-load-stall threshold; alpha replays 666 `suggested` offload candidates through `qwen2.5-coder:32b` vs cloud, promotes a class to binding only when agreement ≥ measured threshold; india uses it INFERENCE-ONLY (cannot train — every training claim through Ollama is a no-op) for `nomic-embed-text` RAG re-embed (with HF-GPU parity assert >0.99) + octopus 32B local-voice + CAG KV-cache. Offload-rate target ≥30% (`ollama-offload-stats.json`). |
| **Docker** | golf (substrate health), hotel (Postgres), juliett (Qdrant/Postgres), sierra (regen parity harness), india (Unsloth train image) | Golf extracts each container's healthy-state signature (Qdrant/Postgres/Prometheus) as wedge-detection baseline, validates via real `docker stop` (auto-restart FORBIDDEN — would kill all containers); hotel stands up Postgres+pgvector as durable ERP/auth/GL backing store (restart-survival E2E); sierra runs `regen-viz` in a memory-capped container to reproduce the V8 external-alloc ceiling deterministically; india runs the real LoRA/GNN stack in the Unsloth Blackwell image (`torch 2.11+cu129`, bitsandbytes, PyG sm_120). |
| **NVIDIA NIM** | india (GPU-serving), xray (high-throughput OCR), hotel (doc extraction) | If golf stands up NIM endpoints, india routes automatically via the capability probe's present-backend detection (no code change) — serves larger reasoning models for octopus/RAG-rerank when NIM beats local 32b; xray routes the raster-scan path (85,345 deskewed JM scans) through NIM containerized vision when local Ollama cold-load starves under fleet contention (reproducible reaper-immune OCR worker); `gpu_health.py` hard-gates that a real bnb 4-bit dequant runs ON GPU (`get_device_capability()==(12,0)`), never silent CPU fallback. |

**Cross-cutting corpus mandate:** every domain embeds + indexes its installed-seat artifacts as the training corpus (Mastercam `.NCI`/posts, hyperMILL `.hmc`, Fusion exports, 160,582 NC + 13,790 `.cps`, Okuma `.MIN`, Hurco WinMax, the 1,008-PDF vendor catalogs, the JM `WIRE EDM/` 4,058-file archive) and parity-validates generated G-code byte-against the golden NC archive (echo's `nc-normalize.mjs` classifier). Lima's pypdf page-extractor is the fleet-canonical PDF tool; DocuStrata is search-only (never re-OCR — `manifest.json`+`.index/`).

## Hermes coordination — the optimal cross-domain path

Hermes routes from the rank table above, honoring three hard dependencies and the producer-before-consumer rule. The path:

**WAVE 0 — Foundations (run in parallel, gate everything downstream):**
- **sierra** → U-VIZ-MERGE-STREAMING-OOM (rank 1). This is the single highest-leverage unit: a stale/un-regenerable graph blocks india's GNN holdout, 7 FAST[] roosts, and fleet-wide search. Until it lands, no slot's `/system-viz find` is trustworthy.
- **golf** → U-MCP-RESTART-ACTUATOR (rank 2). Independent of sierra; defends the MCP :3100 substrate all 33 galaxies' `prism_*` reachability depends on.
- **india** → U-ROUTE-LADDER (rank 3). Independent (inference-only, no golf-GPU dep); purges the hardcoded-absent-model fail-silent that gates MS2/MS5/MS6 routing.
These three have NO cross-dependency — Hermes dispatches all three slot-briefs simultaneously.

**WAVE 1 — Foundation completion (serialize behind WAVE 0):**
- sierra U-VIZ-FAST-REGISTER-9 + U-VIZ-REFPOOL-SEED-EMBED (ranks 4-5) — both blocked-by the OOM fix (stale graph = no holdout/roost).
- india U-GPU-EMBED-WORKER → U-FULL-CORPUS-EMBED → U-RABITQ-HNSW (ranks 6,7,12) — the embed sweep blocked-by sierra's ref-pool seed; golf's Python-GPU stack is a soft dep (india degrades-and-passes if golf hasn't provisioned it — do NOT block india's green gate on golf).
- golf U-FD02..05 dashboard + U-REAP-TUNER (ranks 8-9) — the tuner consumes india's outcome bus (WAVE 0 ladder).

**WAVE 2 — SFC + Post + Support (parallel once the graph is fresh):** These run concurrently across many slots; the only intra-wave coupling is producer-before-consumer:
- **oscar** U-SFC-COMBO-HARNESS → U-OSC9-CALIB-APPLY-WIRE (ranks 10-11) MUST land before **echo**'s post-prove matrix (the post carries oscar's F/S per block) and before **kilo**'s Fusion SFC button (rank 21, rides calibrated SFC).
- **romeo** U-WIRE-SPEED-6 (rank 15) feeds oscar's SFC surface — run early-in-wave alongside oscar.
- **echo** U-POST-DARK-UNWIRE → U-POST-PROVE-MATRIX-5AX (ranks 13-14) — the dark-unwire is independent; the 5-axis matrix waits on oscar F/S + kilo toolpaths.
- **kilo** U-CAMX-TOOLPATH-LOSSLESS-ECHO feeds echo; **foxtrot** U-P2P-CORPUS-LOADER + U-VMC05-ROKU-POST supply mill cut-physics + machine coverage for echo's 3-axis tier; **whiskey** U-MILLTURN-SWISS-ASSEMBLE-PROGRAM supplies Swiss/mill-turn cut-physics for echo tiers 3-4; **mike** U-WTW00 (producer-first, 0-byte fix) → U-WTW-REG01 → U-WTW-DIALECT-WIRE feeds echo's WEDM dialect tier.
- **romeo** U-WIRE-OTHER-21 (rank 22) runs anytime — independent, biggest single unwiring lever.
- Hermes sequences echo's wedm-dialect unit (rank 19) AFTER mike's registry (rank 23) so the post emits real cut-physics.

**WAVE 3 — Quoting + ERP (couple via the hotel→charlie actuals edge):**
- **hotel** U-GAP-ERP-PERSISTENCE-PG (rank 25) is the precondition — durable state gates every populated ERP engine. Then U-BRIDGE-ERP-QUOTE (rank 27) publishes per-category variance.
- **charlie** U-QP-DATASRC-WIRE-3 (rank 24) CONSUMES hotel's actuals to drive MAPE off the 71.1% synth ceiling — so charlie's data-wire waits on hotel's cost-feedback bridge. U-QP-QUOTE-UI (rank 26) is the product-visible payoff.
Hermes routes hotel's persistence + cost-feedback FIRST, then charlie's consume; they are not parallel.

**WAVE 4 — JM-Die catch-up (fully independent, fills any free slot):**
- **alpha** U-BW-OFFLOAD-HOOK-WIRE (rank 28) — wiring the dead offloader lowers ALL galaxies' token cost; high-value but not blocking, so it slots wherever alpha is free.
- **delta** U-GAP-WIRE-JMDIE-CORPUS (rank 29), **xray** U-XRAY-DIM-ADAPTERS (rank 30), **lima** U-ACADEMY-SVI-SUBSYSTEM (rank 31) — each independent; delta/xray consume juliett's index (read-only, no contention).

**What Hermes routes to each slot NEXT (immediate slot-brief targets):** sierra=OOM-streaming-rewrite · golf=MCP-restart-actuator · india=route-ladder · oscar=SFC-combo-harness · echo=post-dark-unwire · kilo=Fusion-D1-button · foxtrot=P2P-corpus-loader · whiskey=material-infer-from-MIN · mike=fix-0-byte-LoRA-dataset · romeo=wire-Speed-6 · charlie=datasrc-wire-3 (after hotel cost-feedback) · alpha=offload-hook-wire · hotel=Postgres-persistence · delta=JM-corpus-coverage · xray=dim-adapters · lima=academy-SVI-axis.

**Parallel vs wait summary:** Parallel-safe — WAVE 0 (3 foundations), WAVE 2 support slots (kilo/foxtrot/whiskey/mike/romeo all build their producer cores at once), WAVE 4 (alpha/delta/xray/lima). Must-wait — india embeds wait on sierra ref-pool; echo post-matrix waits on oscar F/S + kilo toolpaths; echo wedm-tier waits on mike registry; charlie data-wire waits on hotel cost-feedback; everything waits on sierra's graph being fresh enough to trust `/system-viz find` for discovery. Hermes' job is to hold the WAVE-0 gate, then fan WAVE 2 wide, and keep the two producer-consumer couples (oscar→echo, hotel→charlie) in order.

