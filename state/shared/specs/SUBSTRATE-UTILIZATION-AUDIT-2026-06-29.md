# Substrate-Utilization Audit — 2026-06-29 (slot:sierra)

> Operator goal: *"ensure the Obsidian vault, system-viz graphs, and master indexes are used efficiently, effectively and optimally for all tool calls that the Claude Code CLI, Ollama offloading, and Hermes would use for all tasks."*
>
> Produced by the `substrate-utilization-audit` Workflow (8 parallel MAP agents + opus synthesis, run `wf_ea6ce1df-a18`). This spec is the **verified** record — two cross-report conflicts were resolved by live code-reads (see §Conflict corrections), which **flip** two of the synthesis's gap premises. Ship order below supersedes the raw synthesis.

## Matrix verdict (channel × substrate)

| Channel | Vault | Graphs | Master-index |
|---|---|---|---|
| **Claude-CLI** | OK (sidecar recall; vault-precheck filename-only) | OK (cheap seekCard; pre-*-graph wired via bundles) | **GOOD now** (was POOR: degraded ~20K subset → fixed to daemon-first 110K, `U-SIERRA-MASTER-INDEX-DAEMON-FIRST` 24d8b0e1fe) |
| **Ollama-offload** | POOR (ask-ollama ask/triage/summarize/explain/codegen run blind, no vault grounding) | POOR (cold graph load instead of find-cache) | POOR (no server-free fallback) |
| **Hermes** | NONE (zero inference-side vault access) | POOR (expensive loadGraph) | NONE (no path in any mode) |

The diagonal degrades sharply: the CLI main loop is broadly OK; **Ollama is half-blind; Hermes is almost entirely blind despite being the "stronger" managed lane.**

## SHIPPED this session
- **Gap 1 — `U-SIERRA-MASTER-INDEX-DAEMON-FIRST` (commit 24d8b0e1fe).** master-index-precheck-inject now queries the warm :3101 daemon (full 110K) via async `masterIndexSearch`, fail-soft to in-process. LIVE-validated: daemon-win 33%→80% after tuning `daemonTimeoutMs` 250→400ms (the daemon answers ~255ms, straddling the old 250ms default — the synthesis's "~120ms" assumption was wrong; without this tuning the fix was a no-op 2/3 of the time). 2-arm scrutiny PASS.

## Conflict corrections (verified by live code-read — these flip the synthesis)
1. **`cag-consume.mjs` EXISTS** at `.claude/helpers/cag-consume.mjs` (+ test). The synthesis's cag-efficiency/inject-redundancy agents grepped only `hooks/`+`scripts/lib/` and declared it "absent → dead producer / consumer never shipped." **Wrong.** Gap 3 must be re-scoped: the question is whether the 3 retrieval injectors (master-index-precheck, memory-index-precheck, tribal-by-domain) actually IMPORT+CALL `cag-consume` to honor the CAG skip flags — NOT "build a missing consumer." Verify wiring first.
2. **`pre-{read,grep,bash}-graph-inject` ARE wired via bundles** (bash-bundle.mjs:58, grep-glob-bundle.mjs:32, read-bundle.mjs:21; 0 direct settings.json refs). The synthesis's Gap 5 "4 orphaned graph hooks" is mostly wrong — only `wiki-precheck-inject` (per-prompt wiki retrieval) is in question. Confirm wiki-precheck wiring before any settings.json change.

## Remaining gaps — dependency-ordered ship queue

### QUICK WINS (1–2 files, mechanical, safe; each: per-file scrutiny + test)
- **Gap 4 — OllamaHookBridge dispatcher grounding. SHIPPED 2026-06-29 (slot:sierra): `U-SIERRA-GAP4-MCP-ROUTE-GROUNDING`.** New cycle-free leaf `scripts/lib/dispatcher-hints.mjs` (parse DISPATCHER_DIGEST.md table -> stopword-filter the prompt -> whole-word-domain + substring-name score -> top-N manifest; reuses `vault-hints.tokenizeQuery`, pure + fail-soft) wired into `.claude/hooks/lib/ollama-hook-bridge.mjs` via an exported pure `resolveSystemPrompt(hookType, prompt, options)` that appends the manifest to the `mcp_route` system prompt ONLY (gated; opt-out `groundDispatchers:false`; respects explicit `systemPrompt`). 14/14 tests; 2-arm per-file scrutiny PASS (after a FAIL->fix round). **SURFACE CORRECTION (9th synthesis claim flipped by live code-read, R12):** the spec named the `.ts` `OllamaHookBridgeEngine` -- but the LIVE `mcp_route` hook lane runs through this SEPARATE `.mjs` bridge (`.claude/hooks/mcp-route-suggest.mjs:388` imports `queryOllama` from `./lib/ollama-hook-bridge.mjs`, calls it `hookType:'mcp_route'` with no `systemPrompt` override). Enriching the `.ts` engine would have been an orphan (no live hook consumer); `ai_feature` has NO live consumer at all, so that arm was dropped. **Two defects the per-file LIVE-digest validation caught (the fixture masked both):** (a) generic tokens (`action`/`prism`/`mcp`/template boilerplate) polluted matching -> added STOPWORDS; (b) substring matching false-positived (`force` in en**force**ment, `cutting` in cross-**cutting**) and offered the explicitly-UNWIRED `unwiredBridgeDispatcher` as a route under "choose ONLY from these" -> added whole-word-domain matching + `EXCLUDE_RE` (unwired/deprecated never routable) + a real-digest regression test. Documented P2 follow-ups: calc's terse digest domain (`prism_calc -- calc`) lacks speed/feed/cutting keywords so a physics query surfaces `vibrationPhysicsDispatcher` (a legit cutting-physics dispatcher) not calc -- a digest-data recall limit, not an algo bug; and `mcp-route-suggest.mjs`'s prompt embeds a static dispatcher-name template that mildly biases selection. **P2 SHIPPED 2026-06-29 `U-SIERRA-GAP4-P2-GROUNDQUERY`:** added a backward-compatible `groundingQuery` option to `resolveSystemPrompt` (defaults to `prompt`); the consumer now passes the RAW bash command, so the manifest is selected by the actual task, not the boilerplate template. LIVE-PROVEN: a cam/toolpath bash command grounds to camDispatcher/safetyDispatcher/cplDispatcher/toolpathDispatcher (was polluted with calcDispatcher/devDispatcher from the template's `prism_dev/prism_calc` list). 15/15 tests. (The terse-calc-domain recall limit remains a digest-data note, not an algo bug.)
- **Gap 6 — Ollama+Hermes vault grounding. ✅ SHIPPED 2026-06-29 (slot:sierra): `U-SIERRA-GAP6-VAULT-GROUNDING` 5d58ea7ec3 + `U-SIERRA-GAP6-HELP` 68fef85420.** Cycle-free leaf `scripts/lib/vault-hints.mjs` (getVaultHints → top-N memory excerpts + formatVaultHintsBlock; 8/8 tests, pure+fail-soft) wired into BOTH `ask-ollama` (ask/codegen/file branches, 63/63) and `ask-hermes` (buildChatBody user content + PRISM-identity system prompt, 71/71) behind opt-in `--with-context` (default-off = byte-identical). 2-arm scrutiny PASS (no P0/P1). DEFERRED P2s (handoff): (a) bridge re-export to retire the leaf's focused list/score copies (the OBSIDIAN_*-6-site refactor — own unit); (b) ask-hermes `getVaultHints({})` → pass `{root}` for symmetry; (c) ask-hermes file modes tokenize full content (relevance noise, not a break) — consider keying off basename; (d) tighten the leaf test's readImpl to key on the joined path. The original design-finding (below) is retained for history:
  - **(historical) DESIGN VALIDATED + ARCHITECTURAL BLOCKER FOUND (2026-06-29 attempt, slot:sierra).** A `getVaultHints(query, opts)` helper was built + live-tested: query "okuma lathe threading dialect" → top-3 memory files with frontmatter-stripped content EXCERPTS (the dialect map + echo-okuma ref) — genuine grounding for the ONE-SHOT `callModel` path (filenames alone give a topic hint but no facts; excerpts ground it). Empty/short query → `[]`. PLUS a shared `formatVaultHintsBlock(hints)` renderer (R7 single path).
  - **BLOCKER (verified by code-read, R12):** the helper canNOT live in `ollama-prism-bridge.mjs`. The bridge **statically imports `ask-ollama.mjs`** (bridge.mjs:60-66 `loadGraph,searchGraph,renderHits,readFileCapped,truncate`), so `ask-ollama` importing the bridge = a real cycle (bridge.mjs:69 comment forbids it). And `ask-hermes` importing the heavy bridge just for 2 fns is a per-call CLI load smell. (My single-line grep first missed the multiline import — verify multiline imports.)
  - **PROPER FOUNDATION (own unit, do FIRST):** extract `scoreLeafFilenames` + `listObsidianMemoryFiles` + their constants (`OBSIDIAN_*`, `WIKI_MIN_TOKEN_LEN`) + `getVaultHints` + `formatVaultHintsBlock` into a cycle-free leaf `scripts/lib/vault-hints.mjs`; have the bridge IMPORT+RE-EXPORT them byte-identically (the established `mcp-streamable-client.mjs` pattern at bridge.mjs:67-75 — keeps the bridge's `obsidian_lookup`/`wiki_lookup` consumers + tests green). THEN `ask-ollama` (`--with-context` flag in parseArgs:233; inject in runRequest ask/codegen/file branches) AND `ask-hermes` (flag in parseArgs:263; inject in buildChatBody:129 user content + PRISM-identity in systemPromptFor:110) both import from the leaf (cycle-free, lightweight). Confidence 0.75. The reverted helper body is preserved in this session's transcript; re-place it in the leaf verbatim.
- **Gap 9 (dedup subset). ⚠ MOSTLY A NON-GAP — verified 2026-06-29 (slot:sierra), premise corrected.** The synthesis claimed "wrap 6 emitters in `dedupeOrMarker` for ~30-50% reduction" — but verification shows **4 of the 6 already dedup**: `slot-soul-inject` (7 refs), `slot-context-bundle-inject` (4), `psn-leg-state-inject` (6), `slot-brief-inject` (1) — their `dedup -- block unchanged` lines appear live in-session. `fleet-work-digest` is NOT an inject hook (wrong name). Only **`cag-router-inject` (0 dedup refs)** genuinely lacks it, and its output is a per-prompt CAG-route classification (a few lines) — deduping only helps on identical /loop re-submits = marginal. So the ~30-50% claim is unfounded; the real residual is one small optional wrap (`cag-router-inject`), low priority. `dedupeOrMarker` lives at `scripts/lib/injection-dedup-fs.mjs:37`. (The broader "52 always-firing hooks / 27 silently-failing" inject-redundancy audit may still hold — but the named 6-emitter dedup quick-win does not.)
- **node-card-prefetch TRIGGER_PREFIXES** — add `memory_feedback`/`memory_reference` (offset index self-verifies; 2-line). Trivial.

### MEDIUM (own commit)
- **Gap 2 — cheap graph for offload channels. ~NON-GAP (verified 2026-06-29 slot:sierra, 10th synthesis correction).** The premise (a 370MB cold `loadGraph` in the 3 offload channels -> swap to find-cache + `readCards`) is FALSE on every count: (1) all three channels share ONE `loadGraph` -- `ask-hermes.mjs:65` and `ollama-prism-bridge.mjs:61` both IMPORT it from `ask-ollama.mjs`; (2) that `loadGraph` reads the compact **27MB `architecture-graph.json`** FIRST and REFUSES the 370MB `system-graph.json` (`ask-ollama.mjs:116,553`), parsed-once-and-cached per run (`ollama-prism-bridge.mjs:1256`); (3) `find-cache.json` is **55MB -- LARGER** than the 27MB already used; (4) `readCards`/`seekCard` are id-SEEK surfaces, but `viz_search`/`rerank` need a full keyword SCAN -- different access pattern. So the "swap" would REGRESS load size and not even fit the use-case. The offload graph channel is ALREADY optimal (smallest scannable corpus, shared + cached). No action.
- **Gap 3 — CAG consumer wiring. ✅ RESOLVED 2026-06-29 (`U-SIERRA-GAP3-MEMORY-CAG-SKIP` 78edc4b4d8).** Conflict 1 confirmed: `cag-consume.mjs` exists in `.claude/helpers/` and `shouldSkip`/`skipAdvisory` are real. 2 of 3 retrieval injectors already consumed it (`master-index-precheck`:193, `tribal-by-domain`:340); the ONLY gap was `memory-index-precheck-inject` (0 refs) — now wired with the same `shouldSkip("memoryRelevanceInject")` guard (verified: skip fires on flag=true, fail-soft otherwise). All 3 retrieval injectors now honor CAG cold-tier skip. NOTE: `memory-index-precheck` is currently ENV-disabled fleet-wide (`PRISM_MEMORY_INDEX_INJECT=0`) — a separate lever; the parity fix is correct for when it's enabled. The synthesis's "dead producer / consumer never shipped" framing was wrong (the producer's 5,259 sidecars ARE consumed by 2-now-3 injectors).
- **Gap 8 — vault body recall. Slice (b) SHIPPED 2026-06-29 (slot:sierra) `U-SIERRA-GAP8-SCANDIRS`; slice (a) QUEUED.** Two parts: **(b) SCAN_DIRS fix [DONE]** -- verified 3 of 11 SCAN_DIRS missing under `knowledge/` (MIT-OCW, PRISM-System-Map [a `.canvas` FILE not a dir], specs [real specs live at `state/shared/specs`, a different ROOT]); they scanned nothing AND the auto-generated `_Scanned:` footer falsely claimed them (R12). Removed the 3 dead, added 6 real content-bearing non-master-index-covered dirs (sessions/roadmap/observations/jm-corpus/h-drive-atlas/hermes-brain); live-validated (footer now truthful, hits returned). **(a) body-excerpt sidecar [SHIPPED 2026-06-29 `U-SIERRA-GAP8-BODY-RECALL`]:** new shared leaf `scripts/lib/vault-precheck-lib.mjs` (single-sources SCAN_DIRS + tokenizer + scoreEntry + loadSidecar + sidecarStaleness; 7/7 tests) + generator `scripts/build-vault-precheck-sidecar.mjs` (179 entries w/ first-250-char excerpts -> tracked `state/shared/vault-precheck-sidecar.json`, mirrors memory-index-sidecar) + rewired `obsidian-vault-precheck-inject.mjs` to score basename(strong)+excerpt(weak, body recall) with a live basename-only FALLBACK (never worse than today) and a self-maintaining throttled/detached regen (`maybeTriggerRegen` -- no new settings.json Stop entry). Deliberate recall gain: basename now splits on `_` so snake_case parts match. LIVE-PROVEN: a body-only token surfaces a file the legacy basename-only path scored 0 on. 2-arm scrutiny PASS (P2s: in-place-edit staleness lag + a cosmetic BOM-in-regex, both matching the accepted memory-index sibling pattern). **Cross-root enhancement SHIPPED 2026-06-29 `U-SIERRA-GAP8-CROSSROOT`:** the lib moved to a multi-root `SCAN_TARGETS` model with REPO-relative entry paths, so the precheck now ALSO scans `state/shared/specs` (925 design/audit/roadmap specs that had ZERO precheck coverage). Sidecar regenerated to 1179 entries (1000 specs + 179 knowledge) and committed ATOMICALLY with the repo-relative hook (the new `${h.path}` link requires the repo-relative sidecar -- shipping them together avoids a broken-link window; per-file scrutiny P1). LIVE-PROVEN: "substrate utilization audit" now surfaces the audit spec #1; knowledge/ recall unregressed. Cap raised 150->1000 (per-dir; pre-built sidecar so per-prompt cost = score 1179 entries, cheap).

### LARGER (own milestone)
- **Gap 5 — re-wire `wiki-precheck-inject`. NON-GAP (verified 2026-06-29 slot:sierra, 11th synthesis correction).** The premise ("per-prompt wiki retrieval is dead") is FALSE: `wiki-precheck-inject.mjs` IS wired in an ACTIVE UserPromptSubmit block at `H:/prism/.claude/settings.json:822-826` (5000ms timeout, between archived-skill-suggest + chat-bus-inject) AND was observed FIRING this session (injected `## 📚 Wiki precheck -- relevant entries already known`). The synthesis likely grepped the user-global settings.json (where it is absent) -- but the PROJECT settings.json hooks fire. No settings.json change needed (the fleet-risk change is AVOIDED). Conflict-correction #2 above (which hedged "confirm wiki-precheck wiring first") is hereby resolved: WIRED + FIRING.
- **Gap 7 — search-first ENFORCEMENT. MEASURED 2026-06-29 (slot:sierra) -- proposed tuning is RISK>REWARD, DON'T BUILD as specified.** Re-measured live: `grep-index-first` = `{fired:1819, suggested:308, offloaded:1}` -> take-rate **0.05%** even AFTER the taken-signal correlator (U-GREP-TAKEN-SIGNAL) shipped. The 0% premise holds, but the analysis flips the FIX: (1) the advisory-decay system is CORRECTLY suppressing a ~0%-adoption nudge -- "gate decay-mute on graphHits.length===0" would un-mute hit-bearing nudges that are STILL ignored 99.95% of the time = more injected-token noise, fighting the decay system (net-negative). (2) "extend force-deny to fuzzy >=0.85" makes a HARD BLOCK on Grep more aggressive on a FUZZY name match -- a 0.85-similar name is NOT the same asset, so it would false-deny a legit Grep + misroute the Read to the wrong file (`decideForceGraphRead` is exact-match-only TODAY by design, `grep-index-first.mjs:205-225`, gated by `exists()` + `CLEAN_IDENTIFIER` + exact `CODE_SYSTEM_INDEX` resolve). The safe deterministic lever (exact-match force-deny) ALREADY works and covers 4180 catalogued assets. RECOMMENDATION: leave the decay-suppression (it is correct for a 0%-take advisory); do NOT extend force-deny to fuzzy without a measured false-deny analysis. The real lever for adoption is the model following the advisory, not a riskier block. (Verify-before-build: measuring the live take-rate is what flipped this.)
- **Gap 3-Hermes — `hermes-prism-bridge.mjs`** (clone `ollama-prism-bridge.mjs`, R11 clone-don't-fork) so Hermes gets the same read-only substrate tools. Depends on Gap 2's cheap-graph path.
- **Daemon concurrency (P2 from Gap-1 scrutiny).** The :3101 daemon runs search synchronously on a single event loop → under 26-slot bursts the tail exceeds the 400ms client timeout → graceful fallback to the slow in-process path. **MEASURE-FIRST telemetry SHIPPED 2026-06-29 `U-SIERRA-DAEMON-LATENCY-TELEMETRY`:** `scripts/lib/daemon-latency-stats.mjs` (pure, 6/6 tests) wired into the daemon's `/health` -> now exposes `maxSearchMs` + `slowSearchCount` (searches ≥350ms, the timeout-risk threshold), so an operator can SEE whether the ceiling is actually biting in production (verified-by-code-read: `handleSearch` is synchronous, so genuine concurrency needs a worker-thread refactor). **The refactor itself remains DEFERRED to a focused session** — it is live fleet-search infra whose concurrency win can only be validated by a 26-slot load test (worker threads + a shared/per-worker 262MB index); not a YELLOW-context build. Telemetry first (the Gap-7 discipline), refactor when the data + a load-test window warrant it.

## Completeness-critic follow-ups (from the synthesis, still open)
- No channel measured end-to-end with a real task — gains are structural inference (except Gap 1, now live-measured, and Gap 7's 0% take-rate).
- MCP-server-UP path not traced (reports assumed MCP-down fallbacks).
- Embeddings/semantic path (Qdrant 14,738 vectors) lightly covered — is BM25-everywhere leaving semantic recall on the table?
- No write/regen-side audit — the sidecars these cheap reads depend on (system-graph-index, memory-index-sidecar, find-cache, node-card-offsets) have their own freshness/regen story that determines whether the cheap paths return CORRECT results. Next audit.

_Wiki: companion lesson pending. Memory: reference_substrate_utilization_audit_2026_06_29 (to write)._
