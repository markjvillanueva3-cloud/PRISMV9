# Blueprint-Vision OCR — Pre-Test Upgrade Roadmap (VERIFIED)
> slot:xray · 2026-05-30 · source: xray-ocr-upgrade-hunt workflow (7 investigators + 16 adversarial verifiers + synthesis)
> baseline commit bb4eae6aec (OCR gateway unblocked). counts: {"investigators":7,"candidates":44,"unique":44,"verified":16,"confirmed_or_partial":14,"already_built":1,"refuted":1,"pre_test_blockers":8}

## Executive summary
The single most important upgrade is the resumable batch runner WITH its folded-in GPU-claim mode (Tier-1 #6): there is no batch path on disk today (only a credential stub + a useless text-only pdftotext stub), the GPU contention is LIVE RIGHT NOW (qwen2.5-coder:7b is resident at 10.1GB, so loading the 15.3GB VL on top silently offloads to CPU and triggers the 5-minute-abort failure mode), and a 7.8-10.6-day single-stream run with no checkpoint wastes the whole wall-clock on any crash. We are NOT ready to run the batch test. Five blocking gaps must close first, all in_session except one operator pip: (1) multi-page fix — the runner drops ~76% of all drawing pages silently today; (2) scan grayscale/binarize preprocessing so a low confidence is attributable to the model, not skew; (3) the rich multi-zone schema port so we extract material/title-block/GD&T/profiles, not just bare dimensions; (4) code-side unit detection (the VLM already ignores the in-prompt inch→mm instruction — a present output-unit bug); (5) an empirical model bake-off against the 37 STEP/DXF-parseable triples, because the qwen2.5vl:7b choice rests on a single anecdote and the accuracy ledger holds 2 failed events. Build them in dependency order (schema before units before batch), then run. The GroundTruth conformal scorer already exists and is wired — do not rebuild it; it just needs scalar-labeled pairs the batch itself will start generating.

## Pre-test blocker checklist (build in dependency order)
1. Fix multi-page / multi-print blindness — render & extract EVERY page, one extraction object per print
2. Add scan preprocessing — grayscale (in-session) + Otsu binarization + deskew (operator pip)
3. Port the rich multi-zone schema (title-block + GD&T + datum + notes + profiles + surface-finish + units + raw_text) into the local-Ollama prompt AND parser
4. Add explicit unit-system detection + code-side (R5) inch→mm conversion
5. Build the empirical model bake-off harness scored against the 37 STEP/DXF-parseable triples (qwen2.5vl:7b vs llama3.2-vision:11b)
6. Build the resumable BATCH vision-OCR runner with checkpoint/SHA-dedup AND folded-in GPU-claim mode (unload coder, warm VL, suppress fleet-reaper coordinator)

---
## PRISM Blueprint-Vision OCR Upgrade Roadmap (slot:xray)
_Verified on disk + live GPU state, 2026-05-30. Goal: print-to-data round-trip. Operator ask: "find upgrades before testing."_

The shipped pipeline (commit `bb4eae6aec`) works on ONE page of ONE print at a time and extracts ONLY `{confidence, dimensions}`. Before a batch test over the 12,321 needs-OCR scans is meaningful, six gaps must close. Five are correctness/measurement blockers; without them the run silently drops ~76% of pages, learns from un-validatable inputs, and you can't say whether a single extracted dimension is right.

---

### TIER 1 — DO BEFORE THE BATCH TEST (blocking set, dependency-ordered)

These gate a *meaningful* run. Every one is `in_session` except scan-preprocessing's binarize/deskew tier (operator pip). Build in this order — later items depend on earlier ones being in the runner.

| # | Title | Impact | Effort | Concrete in-session build | Verified status |
|---|-------|--------|--------|---------------------------|-----------------|
| 1 | **Multi-page / multi-print blindness fix** | high | in_session | In `run-ollama-vision-extract.mjs`: read page count, loop pages, pass `--page N` (the `.py` ALREADY supports it, line 32), emit ONE extraction object per page. Add `--pages`/`--all-pages` flag. No Python-core change. | CONFIRMED. Runner renders page 0 only (line 129, no loop). 97% of corpus is multi-page (avg 4.1, max 31) → ~76% of pages dropped SILENTLY today (R12 violation + doctrine "split before OCR"). |
| 2 | **Scan preprocessing — grayscale tier (in-session), binarize+deskew tier (operator)** | high | operator | In-session NOW: `pdf-to-png.py` → `get_pixmap(colorspace=fitz.csGRAY)` (pure PyMuPDF, csGRAY confirmed available). Operator-gated: `pip install opencv-python-headless numpy` → Otsu binarize + deskew, **degrade-to-grayscale-when-cv2-absent (R12, never silent-skip)**. | CONFIRMED + NOT BUILT (0 grep matches). 12,824 literal "Scanned Document" files. cv2/numpy/PIL MISSING on H:/Tools/python. Lets you attribute low confidence to model ceiling vs skew artifact — without it the batch baseline is un-interpretable. |
| 3 | **Rich multi-zone schema port (title-block + GD&T + datum + notes + profiles + surface-finish + units + raw_text)** | high | in_session | Widen `buildVisionPrompt` AND `parseVisionResponse` in lockstep using the verbatim contract at `BlueprintVisionOCREngine.ts:201-278`. Single-shot or 4-pass (multi-pass warranted — `num_predict:2048` already truncates dense lists, `repairTruncatedJson` exists because of it). | PARTIAL-VERDICT but DO IT. Reframe rationale: the rich schema is the WIRED production Claude-path contract (cadDispatcher:2552/2558), NOT orphaned dead code — the local runner regressed it. Material→quoting, title-block→identity, profiles→CAD reconstruction are all goal-blocking. |
| 4 | **Explicit unit-system detection + code-side (R5) inch→mm conversion** | high | in_session | Add `units:'inch'\|'mm'\|'mixed'` + per-dim `raw_text` to the schema (depends on #3's parser widening). DELETE the in-prompt "Convert inches to mm" line. Convert in CODE keyed on detected/declared units; JM=inch corpus prior as low-confidence fallback. | CONFIRMED. STRONGER than proposed: the session's own proof values (1.234, 0.876, 0.3575) are raw INCH — the VLM already IGNORES the in-prompt conversion. This is a PRESENT non-deterministic output-unit bug, not a latent risk. |
| 5 | **Empirical model bake-off harness (CORRECTED candidate set + ground-truth scope)** | high | in_session | `scripts/blueprint-model-bakeoff.mjs`: sample from the **37** STEP/DXF-parseable triples (NOT 426 — see below) + optionally 187 text-program triples as coordinate secondary key; candidates = **qwen2.5vl:7b vs llama3.2-vision:11b** (the only deployable vision models pulled); score per-dim numeric match within tol; one model at a time, coder unloaded. Emit JSON. | PARTIAL-VERDICT, two corrections applied: (a) only 37/426 triples carry .stp/.step/.dxf — dominant CAD is .ipt (Inventor binary, NO parser exists); (b) qwen3-vl:8b NOT pulled (live `/api/tags` = qwen2.5vl/moondream/llama3.2-vision only). Accuracy ledger = 2 events, both `failed`. Model choice rests on ONE anecdote — measure before a 12,321-print run. |
| 6 | **Resumable BATCH runner + GPU-claim mode (MERGE these two — same script)** | high | in_session | `scripts/batch-ollama-vision-extract.mjs`: ONE long-lived node process iterating the 12,321 work-list, reusing the pure lib + `pdf-to-png.py` in-process; checkpoint sidecar `blueprint-ocr-checkpoint.jsonl` keyed on **source PDF SHA-256** (resume + dedup — same print lives at multiple paths); emit outcome events. **Fold in GPU-claim** (the lighter path, not a new hint vocabulary): `keep_alive:0` POST to evict `qwen2.5-coder:7b` → warm VL with `keep_alive >= batch` → `PRISM_FLEET_REAPER_OLLAMA_COORD_DISABLE=1` for the window → confirm `size_vram>0` via exported `readGpuState`/`readOllamaState` → release on exit/SIGINT. | CONFIRMED. Batch runner + checkpoint = NOT BUILT. Only "batch" paths are a credential-gate STUB (`harvest-prints-to-training.mjs:150`) + pdftotext text-only stub (useless for scans). GPU contention LIVE NOW: coder 10.1GB resident; coder+VL=25.4GB >> 16GB → silent CPU offload (the documented 2-min-load/5-min-abort mode). Backlog = 7.8-10.6 days single-stream; crash with no checkpoint = restart from zero. Honest correction: the "amortize node cold-starts" framing is overstated (~3% of inference) — the load-bearing value is **checkpoint + GPU scheduling**, not process model. |

**Dependency order is real:** #3 (rich schema) must land before #4 (units field lives in that schema) before #5/#6 consume the widened output. #1 + #2 are independent and can land first/parallel. #6 should be the LAST before-test build (it operationalizes everything above) and SHOULD reuse the existing I/O layer (`runSidecar`/`callOllamaVlm`/`prewarmVlm` from `blueprint-extract-io.mjs`) for free multi-page coverage + cold-load handling — see Tier-3 note.

---

### TIER 2 — HIGH-ROI AFTER (do soon; big accuracy/throughput/training gain, but not gating the first batch)

| Title | Impact | Effort | What / Why | Verdict note |
|-------|--------|--------|-----------|--------------|
| **Route VLM GD&T text → GDTCalloutParserEngine + FCFSyntaxValidatorEngine** | high | in_session | After #3 emits a `gdt[]` channel of FCF-tokenized strings, pipe each through `cad_gdt_callout_parse` → structured `{symbol,tolerance_mm,datums[],errors[]}` → `cad_fcf_validate`; surface datum-3-2-1 deficiency as low-confidence flags (R5: VLM reads, engine validates grammar). | PARTIAL — engines + dispatcher REAL (GDTCalloutParserEngine.ts:188 datum-deficiency error, cadDispatcher:265/297). NOT the trivial "runner never calls them": the VLM doesn't emit FCF-tokenized text yet — REQUIRES #3 first (prompt must request `"POS 0.02 A B"` form; the parser won't parse freeform prose). Non-blocking; builds on #3. |
| **NxM clip-tiling + overlap-band dedup for large-format (C/D-size) prints** | high | in_session | Add `--tile NxM --overlap` to `pdf-to-png.py` via `get_pixmap(clip=fitz.Rect)` (pure PyMuPDF, no new dep). Trigger when page ≥ ~14in or px > model budget. **The real work is the merge dedup, not the clip.** | PARTIAL — pixel math confirmed (17×11in @300dpi = 16.8M px ≈ 16.8× Ollama default ~1.0M-px cap). TWO traps the proposal soft-pedals: (1) current `parseVisionResponse` BLINDLY concats dimension lists (zero dedup) — tiling WITHOUT new dedup REGRESSES via double-counting; (2) no per-dim bbox exists, so band-scoped dedup isn't achievable — global `(kind,nominal)` dedup is the fallback and CAN drop two legit same-nominal holes. Citation fix: the ~1.0M cap is Ollama/Transformers default, not a Qwen constant. |
| **VLM-SFT JSONL emitter (image+messages) — the corpus fine-tune format** | medium | in_session | `scripts/build-vlm-sft-corpus.mjs`: join pairs → rendered PNG + target-label JSON in Unsloth/LLaMA-Factory `messages` shape; reuse `buildVisionPrompt` as the user turn so train==inference. **Scope first cut to the 498 cad-labeled pairs** where a real target can be produced. | PARTIAL — genuinely missing (only text-LoRA bridge + text-tuple splitter exist). BUT two unbuilt deps the proposal hand-waves: (a) `doc_id`→single-print resolution + container split (9,563 distinct doc_ids back 37,744 refs — heavy multi-print reuse); (b) the assistant-turn LABEL doesn't exist (depends on the next row). Impact medium until labels exist. |
| **Pair→target-label materializer (CAD/program → answer key)** | high | in_session* | `scripts/extract-pair-target-labels.mjs`: resolve `.ipt→STEP` via GroundTruthBatchExtractor+DimensionalSignatureEngine; normalize to `{confidence,dimensions:[...]}`; write `blueprint-target-labels.jsonl`. The 4,245 pairs are JOIN records with ZERO materialized dims today. | PARTIAL — *effort under-tiered: the g-code path (`GCodeReverseCADEngine.reconstruct`) needs ParsedBlock[] (no .min tokenizer exists), a tool-catalog Map, and a stock box; `classifyOp` is private. AND it's a WEAK proxy (machining features ≠ print GD&T/datums). **Scope to the 72-498 CAD-labeled pairs via DimensionalSignature (SI-metres → mm) first**; defer the 3,747 program-labeled majority. |
| **Wire local Ollama VLM as a `BlueprintAnalyzer` into CADLiveBlueprintOcrAdapter** | high | in_session | Build `LocalOllamaBlueprintAnalyzer` satisfying `BlueprintAnalyzer.analyzeBlueprint` → maps `{confidence,dimensions}` into `BlueprintVisionResult`; lets `cad_live_blueprint_ocr` run fully offline (drops ANTHROPIC_API_KEY dep for JM-shop deployment) + drives the SAME dispatcher path. | PARTIAL — seam REAL + injectable (`CADLiveBlueprintOcrAdapter.ts:54/66/183`, dispatcher passes full opts). Two flags: (1) NOT "thin" — must synthesize id/DimensionType/unit/raw_text AND decide the per-field-confidence policy (the local lib has only OVERALL confidence, which COLLIDES with doctrine "per-field confidence MANDATORY" + the 0.70 floor — operator decision needed); (2) round-trip payoff gated on 4 OTHER unwired deps (intentBuilder/cadDrawer/cadDimensionExtractor/printGenerator). |

---

### TIER 3 — USE WHAT EXISTS (route to the already-built asset; do NOT rebuild)

| Title | What exists | Action |
|-------|-------------|--------|
| **GroundTruth extraction SCORER + conformal calibration + 4-tier trust** | `GroundTruthValidationEngine.ts:551-904` IS the BLUEPRINT-OCR-TRAINING-MS1 scorer — `validateExtractionBackend` (565), `computeConformalCoverage` (882), `pickGroundTruthScalar` operator_correction>erp_actual>macro_vc_var>ocr_inferred (848). Dispatcher-wired: `gt_validate_backend`/`gt_compare_backends`/`gt_snapshot_baseline`/`gt_regression_gate` (cadDispatcher:235, 2996+). The "scorer is dead until built" claim was a 44-of-904-line misread. | DO NOT build a new scorer. The genuine thin gap: (a) MCP path needs `precomputedExtractions[]` (one VLM extraction per pair — runtime fns can't cross MCP boundary, per the dispatcher's own error message at :3005); (b) NO production producer emits `operator_correction` (only a test) — add an operator-review emitter that appends operator_correction into the events JSONL. **CRITICAL PREREQUISITE the conformal-wiring proposal missed:** `validateExtractionBackend` consumes pairs with scalar `groundTruthValues` — I confirmed all 76,205 rows in `blueprint-training-pairs.jsonl` carry ZERO `groundTruthValues` (label_source none=71,960). So conformal calibration is BLOCKED behind the Tier-2 label materializer + scoping to CAD-labeled pairs. Run the batch FIRST — it generates the operator_correction-tier data this needs. (corrected: conformal-wire = medium impact / cross_slot, NOT in_session — it depends on india/kilo program+CAD dim parsing.) |
| **Sidecar I/O layer for the batch runner (R8)** | `blueprint-extract-io.mjs` (`runSidecar` multi-page max_pages:4 + vector-aware `skip_raster_when_vector`, `callOllamaVlm`, `prewarmVlm`) + `blueprint-extractor-lib.mjs`. | REUSE the I/O LAYER in #6's batch runner for free multi-page + cold-load handling — but KEEP the new value-extraction prompt+parser. **Do NOT do the proposed full prompt/parser swap:** the existing `buildVlmPrompt` is PRESENCE-ONLY (`{present:[kinds]}`, its own comment "we never ask for nominal — footgun"), which CANNOT rebuild a CAD file. Swapping would DESTROY nominal/tolerance extraction — a goal-critical regression, not a dedup. |

---

### TIER 4 — DROPPED (refuted on disk)

| Title | Why dropped |
|-------|-------------|
| **Program-parse scoring harness on GCodeReverseCADEngine** | Mechanism fundamentally broken, verified: (1) SEMANTIC INCOMPATIBILITY — VLM emits print-space `{drawing-feature-kind, print-callout nominal}`; the engine emits machining-space where a hole's `primary_dim_mm` = TOOL DIAMETER and pocket dims = TOOLPATH XY EXTENTS, not part dims → kind+nominal match scores ~0 even on a perfect part. (2) Engine is MILL-ONLY (classifies on Cartesian extents + G81/G83 + endmill tools); corpus is 94% LATHE (.MIN 8,279 lathe vs 546 mill) — the proposal's own cited `9007405.MIN` is a G18/G96 lathe program → engine extracts ZERO features. (3) Missing prereqs: no .MIN tokenizer exists, no tool-catalog Map, no stock box. Real applicable surface ≈ ≤575 mill files, not 4,173 (~7× overstated). The honest truth signal is CAD geometry via DXF/STEP parsers in print-space — captured by the Tier-2 CAD-labeled materializer. |

---

### Honest deltas applied to the verified set
- **#6 = merge of the batch-runner + GPU-claim upgrades** (same script; GPU-claim is the lighter `COORD_DISABLE=1` + `keep_alive` path, not a new routing-hint vocabulary as originally over-scoped).
- **#5 bake-off ground-truth corrected 426 → 37** parseable triples; **candidate set corrected** — qwen3-vl:8b is NOT pulled (live-verified), so the deployable comparison is qwen2.5vl:7b vs llama3.2-vision:11b.
- **Conformal-calibration wiring downgraded** to Tier-3-blocked (high→medium, in_session→cross_slot) — the pairs carry no scalar ground-truth; it must FOLLOW the batch, not precede it.
