# OLLAMA-SYNERGY-AUDIT — evidence-ranked offload backlog (2026-06-09, slot:sierra)

**Goal (operator):** "utilize Ollama for better task efficiency when viable; keep improving system synergy for higher token savings; make sure the Ollama LLM is fully synergized across tool-calls, /system-viz, Obsidian vault, memory, wiki, and tribal-knowledge creation + injection; find where else Ollama helps."

**Method:** 6-surface parallel audit Workflow (`wf_7f974777-bf2`, 7 agents, all claims verified at HEAD), pre-armed with the falsified-premise finding + alpha punch list so dead premises could not be re-proposed. ROI-ranked KEEP set with file:line, model, lane.

**Live state:** Ollama daemon healthy — 10 models loaded on the idle 96GB RTX PRO 6000 Blackwell (qwen2.5-coder:1.5b/7b/32b, gpt-oss:20b/120b, qwen3-vl:8b, qwen2.5vl:7b, moondream:1.8b, llama3.2-vision:11b, nomic-embed-text). The ~5% offload rate is **session shape (orchestration-heavy), not a bug** — the offloader correctly keeps judgment work on Claude (R5).

## SHIPPED (sierra; cross-chat permission granted 2026-06-09 unblocked the alpha/router-lane wires)
- **#2 U-TRIBAL-QA-LLM-UNBLOCK ✅ commit `6fb278a2ee`** — wired an Ollama-gated LLM Q-A path into `scripts/distill-tribal.mjs` (was heuristic-only on the dead "Ollama not loaded 2026-05-08" premise). Reuses `callOllama` + `resolveSynthesisModel`+`fetchInstalledModels` (no forked call path); clustering stays deterministic TF-IDF; per-cluster fail-soft to heuristic (down / model-fail / unusable text); import-safe `isMain` guard + injectable indexPath/outputDir for tests; audit schema 1->2 (`qaModel`/`qaCounts`/`qa_via`). 19/19 tests, **3-of-3 A/B/C PASS**. LIVE: daemon resolved gpt-oss:120b [blackwell-best], 7/7 clean retrieval questions, 0 fallback. ~70K Claude-tok/full re-distill saved. [[reference_tribal_qa_llm_unblock_2026_06_09]].
  - **gpt-oss RECONCILIATION (supersedes the rows below + #8's note):** the earlier "gpt-oss returns empty .response (harmony)" rejection was a SYMPTOM of num_predict starvation (128 starves the `thinking` channel) and/or the `generateBlurb` 30s cold-load timeout — NOT a harmony incompatibility. Through `callOllama` (num_predict>=1024 + 180s timeout) gpt-oss:120b is fully viable and IS this host's `search_synthesis` best tier. Rule: size num_predict>=1024 for gpt-oss; do NOT blanket-reject it for /api/generate text.
- **#1 T1 — U-VIZ-WIKI-NARRATIVE ✅ commit `cd54edb940`** — flag-gated $0-Claude qwen2.5-coder:32b narrative for viz->wiki entries. 15 tests, 3-of-3 PASS. (gpt-oss:20b rejected live via the `generateBlurb` path — empty .response + 38s cold-load > its 30s timeout; that path's num_predict/timeout, NOT gpt-oss itself, was the issue — see #2 reconciliation. The viz-wiki generator stays on 32b; revisit gpt-oss there only with raised num_predict+timeout.)
- **#8 U-OLLAMA-PREWARM-WIRE ✅ LIVE.** Wired the orphan `ollama-prewarm-on-pipeline.mjs` into settings.json UserPromptSubmit right after the injector (timeout 8000, mirrored C->H). Verified-safe before wiring: advisory/never-block, warmup is a detached+unref'd `curl -m 30` (self-terminates, NOT a fleet-reaper orphan), keyword-gated (/forge*,/rgs,/scrutinize,/dedup,/deep-search,/pdf-learn,/close-out,/precompact), 10-min per-model cooldown, ollama-down + already-warm guards, kill `PRISM_OLLAMA_PREWARM_DISABLE=1`. Its PIPELINE_MODELS already map to qwen2.5-coder:32b/nomic-embed-text — the audit's "align to gpt-oss:120b" is REJECTED (gpt-oss harmony format returns empty .response; 32b is the validated workhorse). Validated live: /rgs -> clean suppress + qwen2.5-coder_32b.iso cooldown stamp; plain prompt silent. Pairs with T2: injector surfaces routes, prewarm hides the cold-load. NOT a git artifact (settings out-of-repo).
- **T2 — U-OLLAMA-PIPELINE-INJECTOR-WIRE ✅ LIVE.** `ollama-pipeline-injector.mjs` was an orphan (built+tested, 0 refs in settings). Wired into `C:/Users/wompu/.claude/settings.json` UserPromptSubmit (after `master-index-precheck-inject`, timeout 8000, mirrored C:→H:). Verified live: `/rgs`→route block (634ch), `summarize the diff`→verb route, plain prompt→suppressed. Advisory/fail-open/kill-switch `PRISM_OLLAMA_PIPELINE_INJECT=0`. Surfaces concrete Ollama routes (RGS→gpt-oss:120b, scrutinize→32b reviewer-D, pdf-learn→vision, summarize/explain/classify→`prism_dev:ollama_hook_query`) for already-eligible pipelines — the BEHAVIORAL lever the falsified-finding identified, NOT a classifier widen.
- **#5 U-MEMO-EXTRACT-THROTTLE + U-MEMO-EXTRACT-USER-TYPE-FIX ✅ commits `f1b69db664` + `1ecf50b6be`** — fixed 3 fleet-wide defects in `stop-obsidian-memory-extract.mjs` (PSN leg #1, the only live memo-creator): (1) fleet-GLOBAL rate file -> per-session (`sessionRateFile(session_id)` + 24h prune); (2) transcript picked by file SIZE / stdin never read -> read the Stop-hook `transcript_path` (canonical `readFileSync(0)`), fallback by mtime; (3) user turns dropped (`type:"human"`-only) -> accept `type:"user"` (live shape) + array content. Auto-fix bonus (scrutiny-surfaced): the `execSync` curl-string injection + `localhost` -> canonical `callOllama` (fetch 127.0.0.1, 15s timeout, no shell). 23/23 tests, 3-of-3 A/B/C PASS, live-validated clean (no vault writes). [[reference_memo_extract_fixes_2026_06_09]].
- **Also (U-NAV-ACCEL-GAP-A-FALSIFIED, commits `f1b69db664`+`2c766b9308`):** the sibling CODEBASE-NAV-ACCEL spec Gap A premise ("~3,300 source files have no node") was EMPIRICALLY FALSE — the live offset index has 5,931 `eng.*` (5,819 atomic via `generate-engine-saturate.mjs`) + 2,270 `schema.*` + `alg.*`/`script.*`/`fs.*` all seekCard-able. Avoided a ~3,300-node duplicate generator; spec corrected. [[reference_nav_accel_gap_a_already_built_2026_06_09]].
- **#3 U-WEEKLY-SYNTH-RESOLVER + U-WEEKLY-SYNTH-NUMPREDICT ✅ commits `b5d249f4f5` + `9697a9135a` + `71a818b49c`** — `WeeklySynthesisEngine.ts:defaultOllamaSummarizer` (Sunday-cron retro) now resolves its model host-aware via `resolveSynthesisModel` instead of hardcoding 32B: env pin (`PRISM_WEEKLY_SYNTHESIS_OLLAMA_MODEL`) wins -> route `search_synthesis` to host 'best' tier -> fail-soft 32B const. Also: explicit `OLLAMA_NUM_PREDICT=-1` (closes the harmony empty-output risk on the now-default 120b), `OLLAMA_TIMEOUT_MS` 90s->180s, injectable `resolveModel`+`fetchImpl` deps, and a STALE RED test fixed (`DEFAULT_OLLAMA_MODEL` asserted `:7b` but code was `:32b` since the 2026-06-04 migration), plus `@ts-expect-error` on both `scripts/lib` `.mjs` imports (nets -2 tsc errors). 62/62 vitest, tsc-clean for both files, **3-of-3 A/B/C PASS** (×2 rounds). **LIVE-VALIDATED end-to-end on home_blackwell:** resolver -> gpt-oss:120b [blackwell-best]; raw `/api/generate` with `num_predict:-1` returned an 804-char, **4/4-section** retro in 68s (http 200). This extends #2's `callOllama` reconciliation to the raw `/api/generate` path: gpt-oss:120b empty-`.response` is a num_predict artifact, NOT harmony incompat. ~10-15K Claude-tok/week saved. [[reference_weekly_synth_resolver_2026_06_09]].

## REJECTED (verified dead — do NOT build)
| # | Premise | Why dead |
|---|---|---|
| R1 | Widen `local-llm-task-router` WORK_CLASS_PATTERNS to chase 30% offload | The 44 keeps are correct judgment tasks (orch/directive/safety/reasoning, R5). Widening = regression. |
| R2 | Wire LLM pass onto `session-end-goal-synthesis` | Dormant (0 settings refs) + reads month-stale data |
| R3 | "synthesis scripts hardcode 7b" / "multi-provider-router.mjs:214 inlines 7b" | FALSE at HEAD — every `:7b` is a comment/JSDoc; all resolve to 32b via `resolveSynthesisModel`. Punch-list #5 SHIPPED 5/6. |
| R4 | LLM-rerank `findInGraph` (system-viz `find`) | Pure substring filter, ~1060×/day — LLM = R5 + latency regression |
| R5 | LLM-rerank tribal/memory INJECTION hot path | Per-prompt 2.5s-timeout hot path — model call architecturally wrong |
| R6 | Bigger embedding model | `nomic-embed-text` is the smallest dedicated embedder; keep |
| R7 | LLM-summarize `memory-compact.mjs` | Byte-truncation is deterministic; LLM risks silent content loss (R12) |

## KEEP — ROI-ranked backlog (lane-assigned for fleet execution)
Lane: **sierra**=system-viz · **alpha**=vault/memory/wiki/tribal/router · **bravo-adj**=synthesis `.ts`. Migration-freeze gates any scheduled-task registration.

| # | Unit | file:line | Model | Saving / gain | Lane | Sketch |
|---|---|---|---|---|---|---|
| 1 | **U-VIZ-WIKI-NARRATIVE** (sierra's top) | child generators `generate-{layer,domain,dispatcher}-wiki.mjs` (bodies are 100% template field-dumps) | gpt-oss:20b | ~135 entries × ~150-tok never-Claude "what/why" paragraph; lifts viz-wiki search recall | **sierra** | REUSE `generateBlurb()` from `scripts/lib/contextual-blurb.mjs` (fail-soft, mtime-cache) per entry; FLAG-GATE off the every-commit hot path (`PRISM_VIZ_WIKI_NARRATIVE=1` or separate pass); keep counts/Mermaid procedural |
| 2 | U-TRIBAL-QA-LLM-UNBLOCK ✅ **SHIPPED** (`6fb278a2ee`) | `scripts/distill-tribal.mjs` | host-aware (gpt-oss:120b live) | ~70K Claude tok/full re-distill | **sierra** (cross-chat) | DONE — ollama-gated LLM Q-A via callOllama+resolveSynthesisModel, per-cluster fail-soft to heuristic, TF-IDF clustering kept procedural. 19/19 + 3-of-3 + live gpt-oss:120b 7/7. Model is host-resolved (NOT the audit's 7b guess); --model / PRISM_DISTILL_TRIBAL_MODEL override. |
| 3 | U-WEEKLY-SYNTH-RESOLVER ✅ **SHIPPED** (`b5d249f4f5`+`9697a9135a`+`71a818b49c`) | `mcp-server/src/engines/WeeklySynthesisEngine.ts` | host-aware (gpt-oss:120b live) | ~10-15K Claude tok/week | **sierra** (cross-chat) | DONE — see SHIPPED section. resolveSynthesisModel + num_predict=-1 + stale-7b-test fix. 62/62 + 3-of-3 + live 804ch/4-sections/68s. |
| 4 | U-WIKI-NLI-LINT (no equivalent today) | live linter `lint-wiki-orphans.mjs` (0 Ollama) | gpt-oss:20b | Periodic wiki contradiction lint — catches what nothing else does | **alpha** | Pairwise-NLI pass over page pairs via 20b → advisory report; do NOT resurrect dormant `prism_wiki` |
| 5 | U-MEMO-EXTRACT-THROTTLE ✅ **SHIPPED** (`f1b69db664`+`1ecf50b6be`) | `.claude/hooks/stop-obsidian-memory-extract.mjs` | keep 32b (via callOllama) | per-session throttle + stdin transcript_path + type:user capture + execSync->callOllama | **sierra** (cross-chat) | DONE — see SHIPPED section. 23/23 + 3-of-3 + live. |
| 6 | U-MEMO-CACHE-CONSOLIDATE ⚠ **PREMISE REFINED** (sierra 2026-06-09) | `build-memo-embedding-cache.mjs` (JSONL) vs `build-memory-embeddings-sidecar.mjs` (int8) | keep nomic-embed | ~2× embed cost (overlap only) | **needs-scoping** | NOT a safe dedup — these are DISTINCT caches: JSONL feeds the MCP-INDEPENDENT hot-path recall hook (memory-relevance-inject, +34 galaxy brains, full-precision), int8 feeds search-lib hybrid retrieval (BM25-sidecar records). "Retire JSONL builder" BREAKS MCP-independent recall. Re-scope: (a) accept-both, or (b) share embed-compute only (emit both formats from one embed pass). See [[reference_memo_cache_consolidate_premise_verified_2026_06_09]]. |
| 7 | U-MEMORY-SYNTH-SCHEDULE | `weekly-memory-synthesis.mjs:154,120` (LLM theme pass default-OFF + unscheduled) | qwen2.5-coder:7b | $0-Claude weekly memo synthesis | **alpha** | ⚠ GATED by migration freeze (scheduled-task registration frozen) — register weekly/monthly + flip `PRISM_WEEKLY_LLM_SYNTH=1` when freeze lifts |
| 8 | U-OLLAMA-PREWARM-WIRE ✅ **SHIPPED** (LIVE) | `.claude/hooks/ollama-prewarm-on-pipeline.mjs` | warms 32b/nomic | Hides cold-load latency on pipeline prompts | **sierra** (cross-chat) | DONE — wired after the injector in settings.json. NOTE: kept `PIPELINE_MODELS` at 32b (did NOT align to gpt-oss:120b — gpt-oss harmony format returns empty .response, live-rejected). |
| 9 | U-CLAUDEMD-7B-DOCFIX (doc-honesty R12) | `H:/prism/CLAUDE.md` + `~/.claude/CLAUDE.md` §AI SYSTEM ROUTING + §TOKEN ECONOMY say `qwen2.5-coder:7b`; all code is 32b | n/a (doc) | Kills the recurring "7b hardcode" false premise at the source | **alpha** | Bump both to 32b floor; note gpt-oss:120b for synthesis/reasoning |

**Low-ROI deferrals (noted once):** ghost-roost `info:` LLM gloss (must stay procedural); Obsidian canvas node relabel (cosmetic); `generate-cad-cam-pdf-tribal-seeds.mjs:9` stale "Ollama offline" (confirm-read first, sibling of #2).

## Provenance
Audit workflow `wf_7f974777-bf2` (full transcript in session subagents dir). Evidence: [[reference_obsidian_tokensavings_premise_falsified_2026_06_09]], [[reference_alpha_forge_punchlist_2026_06_04]], [[feedback_ollama_token_routing]]. Memory: [[reference_ollama_synergy_audit_2026_06_09]].
