# PRISM Master Per-Galaxy Plan (2026-06-03)

This is the master per-galaxy plan — a single decision surface synthesizing the three same-day foundation
artifacts: **FLEET-DOMAIN-GOALS** (north-star goal + synergy matrix + SVI-to-perfect roadmap per galaxy),
**FLEET-1MONTH-TRAJECTORY** (each chat's 30-day arc + fallback work mapped to roadmaps), and
**MASTER-ROADMAP-ARCHAEOLOGY** (original per-domain intent + 25 dormant builds worth reviving + durable ideas).
Each of the 17 galaxy cards below fuses those into one actionable view: goal, trajectory health, app + MCP +
backend-dev features (built and to-build), the dormant builds that belong to that domain, the durable ideas to
apply, and the top-3 synergy-aware next moves. The fleet meta-goal is a perfect fleet-wide SVI (psi=1.0) reached
through three disciplines — clear per-domain goals, india-cloned self-improving training, and synergy-first
building. Everything is grounded in the three docs; nothing is invented. (BUILD_STATE: 3664/3774 engines wired
= 97%, 110 unwired, 3025 units pending across 112 active milestones, 192 envelope-drift milestones.)

---

## ALPHA — token-optimization
**Goal:** Make the 26-slot fleet maximally token-efficient — every prompt routes to the cheapest correct surface, every session stays in budget, the Obsidian brain compounds context so nothing is re-derived; "free" infrastructure that lowers all 33 other galaxies' cost. Offload 11.2% → ≥30%.
**Trajectory status:** On-track — squarely on its soul; moved from PSN-synergy graph honesty into hardware-aware Blackwell 32B/14B local-LLM offload routing (BLACKWELL-TOKEN-SYNERGY-MS0).
**App features (built + to-build):** Built — token-zone state engine (GREEN/YELLOW/RED), budget gate, session ledger, diff-cost estimator, RTK bash wrapper, Ollama offload pipeline, CAG prompt-cache anchoring, MASTER-BRAIN-TEMPLATE exemplar. To-build — in-product cost dashboard surfacing per-job/per-quote token+$ spend (CostEfficiencyBridgeEngine → hotel ERP); binding auto-route enforcement (take-rate is 0.8%).
**MCP server features:** Built — 10 wired engines across `prism_context`/`prism_session`/`prism_dev` (TokenAwareness, TokenBudgetAllocator, TokenEconomy/Tracker, TokenAccounting, SessionTokenLedger, DiffTokenEstimator, HookEfficiency, CADTokenRepresentation, CostEfficiencyBridge). Needed — repair the dead `ollama-route-pretooluse` path (4732 fires, 0 offloads).
**Backend dev tools:** `/token-dashboard` `/token-ledger` `/context-budget` `/slim` skills; RTK (60-99% bash reduction); 5 Ollama offload hooks; per-hook efficiency profiles. BLACKWELL host-class detector unlocking 14B/32B tiers.
**Dormant builds to revive:** Embedding infrastructure → Semantic RAG / Router / Memory (the #1-surfaced cross-cutting build — replaces fleet-wide `String.includes`; alpha is the natural token/cost owner of the router half); codex-newfile-review deterministic-gate + advisory-LLM split (R5 pattern alpha already lives).
**Useful ideas to apply:** Route deterministic transforms to code, LLM only for judgment (R5 origin); dev-tools-first ROI ("1 hour dev capability = 10+ hours saved"); hook stack as a feedback control system (PID-controlled blocking aggression).
**Top 3 next moves:** (1) Lift offload 11%→30% by fixing the dead `ollama-route-pretooluse` 0-offload hook + warming Ollama; (2) convert advisory route-nudges to binding for unambiguous cases (Grep-over-Agent, MCP-over-reimpl) so take-rate climbs off 0.8%; (3) close `U-CK11` to ship COMMAND-KERNEL-MS0 + stand up bidirectional Obsidian vault sync (HMEMV04-06, the biggest context-retention hole) — feeds india's routing-classifier closed loop.

## BRAVO — hermes-zulu
**Goal:** Live, slot-less master conductor — fans 25 workers across 34 galaxies with fail-closed authority, teaches/learns overnight against the Obsidian brain, keeps every shipped asset stub-free. Move from readiness NO-GO (0 slots zuluOptIn) to live.
**Trajectory status:** On-track but dark — converging on Hermes-as-ZULU (slot-brief channel → fleet-orchestrate → auto-fanout → 17-galaxy domain-goals); 60+ units shipped, but the orchestrator is live-but-dark (0 zuluOptIn).
**App features (built + to-build):** Built — Hermes desktop app wired to PRISM MCP `:3100` exposing all ~103 dispatchers as Hermes tools; slot-brief consume-once channel; dream-scan→proposals. To-build — `zuluOptIn` opt-in path (stops the no-op); enforcement promotion (governor/auction are ledger-record only); system-viz `ghost.hermes_app` roost.
**MCP server features:** Built — `prism_session:zulu_authority_check`, `zulu_task_auction`, `hermes_fanout_plan`/`file_scope_partition`/`budget_estimate`/`verdict_aggregate`/`self_correct`, `opus_assess_complexity`, `prism_context:slot_brief_{write,list}`. Needed — `U-OPUS-EXECUTE-WIRE` (live Anthropic client behind `OpusCapabilityEngine.execute()`).
**Backend dev tools:** Parallel-agent fan-out planner, model-tier router (`model_attribution_*`), slot-brief inject hook; `fleet-task-health-watch` (now 39 tasks, CRASH_CRITICAL on the orchestrator).
**Dormant builds to revive:** codex-newfile-review orchestrator + AGI self-awareness engines + DocPropagationEngine (0-drift across 17 doc surfaces — directly addresses the stale-claim hazard Hermes governance fights); the v17 "Claude Code as autonomous engineering platform" ancestor (3-archetype subagents, Task DAGs, model routing) is bravo's literal lineage.
**Useful ideas to apply:** Explicit "Retired Work"/conflict-resolution tables (authority/precedence ladder for conflicting roadmaps — orchestrator-native); trajectory evaluation (verify engine-call sequence, not just final output).
**Top 3 next moves:** (1) Close the `zuluOptIn` gap so live orchestration moves work (the central NO-GO); (2) wire `OpusCapabilityEngine.execute()` to a live client so the model-tier router is real; (3) bridge Hermes's `%LOCALAPPDATA%/hermes/memories/` → `knowledge/` so overnight learning surfaces in the Obsidian-indexed vault — defers all retrain to india.

## CHARLIE — quoting
**Goal:** Any print → margin-correct, physics-grounded, customer-ready quote in one shot; loop closes on quote-vs-actual. QuoteToShip pipeline 0.51 → 1.0; 85% built → 100%.
**Trajectory status:** On-track (idle between units) — steadily hardened the closed loop from "trains" to "trains honestly + self-audits"; ~40 commits of reference-reliability + price-calibration + variance reporting.
**App features (built + to-build):** Built — `InstantQuoteEngine` (qty breaks, lead-time tiers, share-token), `BlueprintToQuoteBridgeEngine` print-to-quote, per-process quotes (Additive/Casting/InjectionMold/SheetMetal), `JobCostingEngine`, drift-alert + cost-alarm. To-build — customer-facing quote UI with full cost stack + confidence band + per-customer margin-floor gate (turns advisory FMV into a true quote); the 2 unbuilt Quoting units.
**MCP server features:** Built — `fair_market_value`, `inflation_adjust`, `QuoteAnalyticsEngine` conversion/accuracy via `prism_business` + `prism_quoting`. Needed — `prism_quoting:training_status` action + frontend consumer for `latest-training-status.json`.
**Backend dev tools:** Closed-loop self-report of data-source coverage (2-of-5 honestly surfaced as gap); degenerate-baseline-refusal guard; outbound-calibration reference-health ledger.
**Dormant builds to revive:** **DrawingToQuoteMathPipeline + MonteCarloCostPDFEngine** (the SCIMATH stakeholder-unanimous "money shot" — P10/P50/P90 cost with confidence bands; charlie's flagship revival); InstantQuoteEngine + DFMPipelineEngine + FileStorage/PartsLibrary (competitor-parity instant-quote backend).
**Useful ideas to apply:** Lead every paid milestone with a demo-able before/after value artifact (the OptimizationReport pattern, applied to quotes); single canonical source per data class (no blending grains — charlie already enforces units-safety).
**Top 3 next moves:** (1) Feed the 3 unconsumed sources (cost-index $10M AP, tool-purchases, curated DocuStrata invoices) to training to drive MAPE off the 71.1% synth ceiling; (2) connect QuoteToShip to the `strategies` registry it lacks + lift dialect coverage past 1 (0.51→1.0); (3) build the DrawingToQuoteMathPipeline P10/P50/P90 layer on top of InstantQuote/DFM — blocked on hotel's `ERPCostFeedback` actuals for true ground truth.

## DELTA — cad
**Goal:** PRISM's geometry brain — any input → validated, feature-recognized, DFM-checked, tolerance-stacked solid (STEP AP242, live Fusion); every downstream galaxy consumes trustworthy geometry. JM corpus coverage 33% → high; CADCAM-AGI never_started → built.
**Trajectory status:** On-track but drifting — heavy sustained CAD output (CAD-FUSION-LIVE host bridges, DRAW-MAX round-trip, COMPLETE engines, REVERSE-ENGINEER), but the live CAD-TRAINING-PIPELINE /loop auto-pinned to a Hermes orchestrator topic post-/compact (re-bind to CAD before resuming).
**App features (built + to-build):** Built — blueprint→CAD / photo→CAD / text→CAD (CadQuery codegen), tolerance check/stack + GD&T, collision/clearance, stock-removal sim, parametric electrode/trilobe/blisk gen, live Fusion 360 drive-and-replay. To-build — ship the 2 PENDING_MERGE CAD UIs (`cqask/ui` NL→CadQuery, `mcp-cadquery/frontend` Three.js viewer); a unified print→model→DFM→quote operator front door.
**MCP server features:** Built — `cadDispatcher` (564 actions) + `cadAutomationDispatcher` (367) + `cadDrawingKnowledgeDispatcher` (11) + `cadRegressionDispatcher` (37); STEP AP242 round-trip, `Fusion360LiveBridgeEngine` (`:18360`). Needed — convert CADCAM-AGI-MS0 (24u never_started) so the india-cloned closed loop is real, not just doctrine.
**Backend dev tools:** `cad-step-ap242-emitter.mjs`, 70-file synth round-trip harness, CAD-RAG over 129,306-file corpus + JM STEP/IPT/DXF; `cad-step-lint` guard.
**Dormant builds to revive:** Function-index pattern (AI queries vendor extrude params without grepping — replicable across CAD vendors); 5 ONNX/GNN mill neural engines include `GCodeUnderstandingTransformer` over 24,545 JM programs (geometry-adjacent embedding delta can feed).
**Useful ideas to apply:** Be better WITHOUT learning data via engineering quality (cold-start moat: physics not lookup, uncertainty quantification); single canonical source per data class.
**Top 3 next moves:** (1) Drive `CAD_COVERAGE_MATRIX` 33% → high on the 16K JM corpus (largest cad dark-data gap); (2) finish the Fusion add-in `/new` fresh-doc fix + resume CAD-TRAINING-PIPELINE /loop (coordinates with kilo for Fusion ownership); (3) convert CADCAM-AGI-MS0 + merge the two CAD frontends so feature_recognize → cam/quoting consumption is reachable end-to-end and product-visible.

## ECHO — post-processor
**Goal:** Single canonical CAM→controller G-code emitter; saleable MasterPost with byte-equivalence proof vs JM golden NC. EDM post pipeline 0.38 → 1.0; ~40% real coverage → 100%.
**Trajectory status:** On-track — building the offline-provable verification spine (NC-normalize → dialect golden round-trip → CIMCO inventory/machine-map → `prism_cimco` sim-bridge); 179 commits; signature is adversarial fail-OPEN hunting (3 gate holes closed in CIMCO alone).
**App features (built + to-build):** Built — 7-phase post pipeline (physics→force/thermal/wear→CI95→safety+tribal→emit), G-code safety gate, cross-controller transpiler, HurcoV11 WinMAX production post. To-build — live MasterPost product surface (single canonical emit + 8-dim scorecard + provenance + byte-equiv CI, blocked by U-LEGAL-13); per-customer post config surface (`PostLibraryConfiguratorEngine`).
**MCP server features:** Built — camDispatcher ~155 post cases + productDispatcher 24 ppg + `prism_cimco` (6 actions). Needed — wire the 5 WEDM stub dialects (Mitsubishi/Sodick/Makino/Agie/Fanuc) so JM wire-EDM posts exist; alarm-aware emit (2,588-alarm DB → P5).
**Backend dev tools:** `post-nc-dialect-lint.mjs` (8 rules), non-circular `post-gen-reward.mjs`, `nc-normalize.mjs` byte-equivalence comparator, golden round-trip classifier.
**Dormant builds to revive:** **Post-Processor Framework (UIR→12+ controllers) + OptimizationReportEngine** (R14 flagship — the OptimizationReport is the #1 sales artifact; Swiss dialects Citizen/Star LFV); echo's top revival. The F360-REV AutoProgram-S10→PostProcessorPipeline reroute (closes a real per-block-physics silo).
**Useful ideas to apply:** Lead every paid milestone with a before/after value artifact (OptimizationReport pattern — ship it FIRST per archaeology priority); wire-don't-rebuild (16 existing post engines, don't re-extract).
**Top 3 next moves:** (1) Convert the ~14 stub-wired engines from `method?.()` dark fallbacks to real executing calls (highest-leverage — the literal cause of reachable-on-paper/dark-in-practice; blocks mill/lathe/wedm tails); (2) close the 4 P0 machine gaps (Haas PRE-NGC, Roku-Roku, EA sinker, FA10S) + wire the 5 WEDM dialects to lift EDM 0.38; (3) clear U-LEGAL-13 → ship MS-MASTERPOST (0/44) with byte-equivalence + OptimizationReport leading.

## FOXTROT — mill
**Goal:** Deepest-volume print→proven-program for JM's 5-VMC fleet, every parameter defended by physics. PrintToProgram 0.90 / MultiAxis 0.91 / MillTurn 0.92 → 1.0.
**Trajectory status:** On-track (one session drifted into Hermes work) — building the self-improving mill knowledge backbone (PDF→video→cited tribal → outcome loop → print-to-program replication); among the healthiest galaxies.
**App features (built + to-build):** Built — `MillingPrintToProgramEngine`, `AdvancedMillingStrategiesEngine` (HSM/trochoidal/adaptive registry), `MillProgramOptimizerEngine`, `HurcoV11MillMasterPostEngine`, `MillStrategyNeuralEngine`. To-build — unified mill-archive locator (programs split across Haas + Hurco dirs); `PRISM_UPGRADED/` mill output pipeline (lathe v2.0.0 never run on mill).
**MCP server features:** Built — `prism_mill` (49 actions, 49 Zod schemas); +3 multiAxisProgramDispatcher actions (replicate_from_print). Needed — register VMC-05 Roku-Roku post; close the 1 unwired mill engine + the 3 unwired `Hyper*` engines.
**Backend dev tools:** Full Mill-LoRA stack at lathe parity (Cadence/Deployment/ExperimentTracker/Ensemble/Monitoring/MasterOrchestrator); `TribalTipOutcomeBridgeEngine` (Laplace-smoothed effectiveness); video/PDF corpus extractors (~309 cited tips).
**Dormant builds to revive:** **Real-geometry toolpath gen + cutter-comp + multi-setup (MILL-MS4)** — pocket spiral/zigzag/morph, helical/ramp entry, rest-machining, G41/G42 cutter-comp, multi-setup WCS (the highest-value build for ACTUAL print-to-G-code; the scaffold emitted hardcoded X0/Y0); **5 ONNX/GNN mill neural engines** (MillStrategyNeural/ChatterNeuralClassifier/SurfaceFinishCnn/ToolLifeGnn/GCodeUnderstandingTransformer — NN-GRAPH infra now exists, blocker gone); MultiAxisPrintToProgramEngine rebuild (RTCP/singularity/G68.2).
**Useful ideas to apply:** 4-LOOP per-unit quality protocol (SCRUTINIZE→GAP-FILL→TIE-UP→VALIDATE) + 7 Synergy Laws (formula-citation-required, feedback-or-it-didn't-happen); pre-compute expensive physics at tool-selection, cache for per-block hot path.
**Top 3 next moves:** (1) Wire the iter2 REAL corpus loader (`HMCProjectParser` over JM .hmc/.nc) so `replicate_from_print` runs against actual shop programs + close the last unwired mill engine; (2) revive MILL-MS4 cutter-comp + multi-setup (zero production value without it); (3) ship `hypermill/CLAUDE.md` sentinel + register VMC-05 Roku-Roku post so all 5 VMCs reach MultiAxis parity — every cut queries oscar SFC, never inlines.

## GOLF — fleet-hygiene
**Goal:** Keep the 26-slot fleet alive, lean, self-healing with zero babysitting; protect the MCP/Docker/Ollama substrate all galaxies depend on. Fleet psi 1.0 but "shrinking" — defend reachability.
**Trajectory status:** On-track — migrated from code hygiene (4125→0 tsc) into deep MCP-daemon reliability hardening, then context-economy, now local-LLM/Blackwell GPU infra (qwen3 catalog + reaper host presets for the RTX PRO 6000 96GB swap).
**App features (built + to-build):** Built — ancestry-confirmed slot-aware orphan reaper, per-tree RAM attribution naming the ONE chat to /compact, GPU/Ollama coordinator. To-build — operator-facing fleet-health dashboard (one live view); guarded MCP :3100-only auto-restart actuator.
**MCP server features:** Reaper/memory/task-health are scheduled-task + hook surfaces (not dispatcher actions by design); golf QUERIES the system-graph for orphan classification. Needed — Docker-wedge auto-detection-with-alert (auto-restart stays forbidden).
**Backend dev tools:** `fleet-reaper-sweep.mjs`, `fleet-memory-monitor.mjs`, `fleet-task-health-watch.mjs`, `critical-memory-compact-nudge.mjs`, `cleanup-orchestrator.mjs`, tmp-orphan-janitor; Blackwell host presets + enum-cache sidecar; the MCP factory-refactor fix (fresh McpServer per /mcp request).
**Dormant builds to revive:** **MachineHandbookIntelligence pipeline (7 engines)** — extract 910 machines' handbook PDFs into a queryable registry with handbookLimitGuard hook (70% reuses existing DocumentLearning; gates G-code against actual firmware/spindle maxes — golf-owned as the substrate-truth/data-debt owner alongside MCAT); MachinePackageGeneratorEngine + 12 machine-safety hooks (machine data 0.5% complete, 0/920 confidence metadata).
**Useful ideas to apply:** Atomic file-created→awareness-updated transaction (the root-cause-of-all-duplication binding); dev-tools-first ROI; fail-loud as a formal contract (threshold + enforcement + violation-response).
**Top 3 next moves:** (1) Hold the MCP :3100 / Docker substrate with a guarded MCP-only auto-restart actuator (a wedged daemon drops all `prism_*` reachability for 33 galaxies at once — Tier-0 #2); (2) wire the india-cloned closed-loop threshold-tuner over the existing reaper JSONL ledgers (the single biggest "perfect hygiene" lever — drive false-positive reaps to zero); (3) ship the unified operator fleet-health dashboard (`U-FD02..05`, `U-FH02`) + per-galaxy SVI attribution.

## HOTEL — business
**Goal:** Autonomous back-office — accepted quote → costed/scheduled/shipped/invoiced job; per-category cost truth with credit/compliance gates. QuoteToShip 0.51-0.72 → 1.0; orphan tribal engine → wired.
**Trajectory status:** On-track — moving from building ERP/business engines to populating them with JM's real corpus (554,999 files reconciled, gate 0→61.44%) under a strict financial-discipline soul; ERP/HR app Phase 1-3 shipped.
**App features (built + to-build):** Built — `quote_to_ship_run` orchestrator (21 stages), `CustomerPortalEngine`, `BillingEngine` (Stripe), `EmployeeMachineDomainAcademyEngine` Cpk-gated promotion, commission/daily-flash, real JM Die fleet across 5/8 NETPLAT-UI pages. To-build — the q2s frontend verbs (`rfq_assign`, Kaizen/OEE actions + routes, `credit_review_all`); PII/consent gate on customer-export paths.
**MCP server features:** Built — `prism_business` (879 action cases / 16 buckets), `GeneralLedgerEngine` (debits=credits invariant), `ERPCostFeedbackEngine` per-category variance, 7-vendor ERP round-trip, `prism_inbox:inbox_population_status`. Needed — wire orphan `HotelERPTribalKnowledgeEngine` (0 refs, 17 tribal cats stranded).
**Backend dev tools:** JM-DOC accountability ledger/gate (all 554,999 files reconciled, 0 silent drops); 4 archive seed-bridges (109,558 docs / 85,345 scans / 104,587 DocuStrata pointers / 34,452 link-only financial pointers — financial_guard invariant).
**Dormant builds to revive:** **Lean/Six-Sigma manufacturing-intelligence dashboards** (OEE/SPC/Kaizen/VSM/Kanban/5-Whys/Fishbone/A3 — engines exist, only need UI + downtime fact-table; low-effort/high-visibility revenue surface); **Shop OS canonical event spine (ShopStateEngine)** (role-filtered shells, E2 ERP bidirectional sync, release gates — foundational for the business galaxy).
**Useful ideas to apply:** Single canonical source per data class; fail-loud as formal contract (financial-discipline soul: never create AR/AP/GL from indexed-only pointers); mathematical governance (no inline tax tables).
**Top 3 next moves:** (1) Wire the orphan `HotelERPTribalKnowledgeEngine` (Tier-1 #6, the #1 ranked dormant-capacity opportunity 0.79) + extract the 6 inline financial/HR constants families; (2) raise QuoteToShip reachability (connect 4th strategies registry + close cost-feedback loop into adaptive engines) — this directly unblocks charlie's outbound-revenue calibration; (3) surface the Lean/Six-Sigma dashboards over already-built engines (OEE/SPC/Kaizen low-effort frontends).

## INDIA — ai-training
**Goal:** Single self-improving learning substrate every domain clones; own GNN/LoRA/RAG/outcome machinery. NN-GRAPH deploy gate DEFERRED (AUROC 0.388 < 0.78) → clears.
**Trajectory status:** On-track (pivoting) — exhausted NN/GNN tier-5 text-feature work (definitive negative result: NON-SEPARABLE, cascade defers to tiers 1-4) and pivoted to Blackwell GPU-leverage AI infra; 2 NN/GNN schema-read regression fixes this period.
**App features (built + to-build):** Built — GraphSAGE GNN tier-5 wiring-inference, LoRA stacks for lathe + mill, RAG corpora (blueprint/cad/tribal/mit/pdf), OutcomeFeedbackBus + auto-tap hook. To-build — a GNN that clears the deploy gate; a turnkey per-domain LoRA template the other galaxies clone (only lathe+mill exist).
**MCP server features:** Built — `xproc_outcome_*`, `xproc_kg_project_features`, `xproc_calibration_monitor_*`, `xproc_conformal_*`, `prism_ai:capability_probe` (OllamaCapabilityProbe). Needed — per-galaxy SVI emitter for ai-training (own psi is invisible).
**Backend dev tools:** `graphsage-trainer/-predictor/-pipeline/-checkpoint/-model.mjs`, `nn-graph-retrain-lifecycle.mjs` (6h S4U), node-embedding bridge, conformal/calibration monitor, `MetaLearningOptimizer`/`EvolutionaryReward`/`AdaptiveThreshold` engines.
**Dormant builds to revive:** **Embedding infrastructure → Semantic RAG / Router / Memory** (#1-leverage cross-cutting build — NN-GRAPH is only a partial descendant; the general semantic search is still missing, india co-owns with alpha); **Content/Video learning pipeline (6 engines)** ("the only post-processor that learns" — ContentIngestion + NLP tagger + KnowledgeDedup + physics-validator + course auto-gen + self-calibration); **Data-Flywheel closed-loop job-outcome learning** (keep ONLY the loop, not the network-effect moat).
**Useful ideas to apply:** Multi-source consensus parameters with provenance (mean/min/max/stddev across N videos + physics-validate, auto-calibration bounded ±30%); be better WITHOUT learning data via engineering quality (cold-start moat).
**Top 3 next moves:** (1) Seed ≥2 high-confidence reference ghosts (poolSize 0 → real holdout) so the deploy gate can score — Tier-0 #3 gating 8 galaxies' "proven" status; (2) apply `graph_heterophily_aggregate` (H2GCN) + stratified neg-sampling to break the heterophily collapse + run fresh `runAssessment` to clearance; (3) stand up the per-galaxy SVI emitter + a turnkey LoRA template (wedm/cam/quoting/sfc still owed) — blocked by sierra's merge OOM (stale graph = no holdout).

## KILO — cam
**Goal:** Universal CAM brain — feature set → validated, collision-checked strategy for any seat; live in-seat add-in button. MillTurn 0.92 / MultiAxis 0.91 strong; EDM 0.38 / Laser 0.37 weak.
**Trajectory status:** On-track but shadowing — own line is CAM closed-loop self-improvement (learn LATHE_OP_ORDER from corpus, /loop iter 1/20 open), but the freshest session pivoted to alpha-authored BLACKWELL-TOKEN units (coordinate ownership to avoid double-work).
**App features (built + to-build):** Built — physics-aware strategy pick, toolpath generation, mandatory collision clearance gate, 5-axis swarf/contour with singularity check, cross-vendor translation. To-build — the MS-CAM-MASTERY add-in buttons (Fusion Speed&Feed / Auto-program / Post via PRISM, Revenue Day 1) + Mastercam 3-button bridge.
**MCP server features:** Built — `prism_cam` + `camFunctionDispatcher` + `prism_toolpath`: `cam_strategy_recommend`, `toolpath_generate`, `collision_check_full`, `cam_multiaxis_recommend`, `cam_cross_translate`. Needed — `mastercam_addin_generate` + the Fusion D1/D2/D3 in-seat actions.
**Backend dev tools:** `CAMAGIMasterOrchestratorEngine` + `CAMKernelEngine` (DXF/SVG/NL→strategy), `CAMCrossSystemTranslatorEngine`, `CAMFeedbackLoopEngine` (closed-loop tap), `ToolpathStrategyDB` (586) + `CAMSystemDB` (61), `/p2p-intake-check` pre-flight (~99% token savings on incomplete inputs).
**Dormant builds to revive:** **Fusion 360 3-tier add-in product (Free/Pro/Ultimate)** — the single largest unbuilt revenue surface (Free S/F panel → Pro 400+ CAM-param injection → Ultimate internal PRISM toolpaths + V-HACD collision pre-check); the F360-REV per-block-physics silo fix; CAMX 14-stage MCP dispatcher chain (kilo's original spine).
**Useful ideas to apply:** Pre-compute expensive physics at tool-selection, cache for the per-block hot path (F360-REV origin); product framing — answer the questions machinists actually ask (intent-engine routing).
**Top 3 next moves:** (1) Ship the MS-CAM-MASTERY in-seat add-in buttons (Fusion D1/D2/D3 + Mastercam — the Revenue-Day-1 surface; oscar's "Speed&Feed via PRISM" rides the same pillar); (2) wire EDM/Grinding/Laser strategy stages into `cam_strategy_recommend` keyed by machine-domain (lifts 0.37-0.52 weak pipelines); (3) close the CAM /loop iter 1/20 (learn LATHE_OP_ORDER) — consume cad features, produce lossless toolpaths into echo.

## LIMA — academy
**Goal:** Self-compounding learning factory — auto-build verified, prereq-correct, cited courses + certification paths. Register a Courses SVI subsystem; close the course loop.
**Trajectory status:** On-track — primary arc is PRISM Academy buildout (citation-discipline across 9 courses, ContinueLearningWidget + AcademyHub PWA), recently broadened into shared backend infra (MCP connectivity fixes, AI-core wiring).
**App features (built + to-build):** Built — course catalog + lesson/quiz rendering, auto-course-build from corpus, learning-path/prereq graph, adaptive next-lesson, instructor LMS, web learner surface (`/learning/academy`), `/pdf-learn` `/video-learn` skills. To-build — course→certification→role-qualification round-trip into the shop-floor scheduler (operator can't run a machine until certified); mobile/phone learner surface; resolve 37-wired-vs-29-catalog drift.
**MCP server features:** Built — academy actions across 5 dispatchers (`prism_knowledge` academy_/course_/learn_course_*, `prism_dev` mcfi_/mcdl_, `aiReasoning` video_elearning_, `business` learning_, `operatingSystem` course_). Needed — register academy as an SVI subsystem ("Courses/Curriculum" dimension).
**Backend dev tools:** `CourseBuilderEngine`+`learn_course_from_source`, `Training*` cluster (DatasetSnapshot/ExampleAssembler/Ledger/TemplateContinuousLearning) assembling operator-outcome→LoRA/RAG pairs; lima's pypdf page-extractor (fleet-canonical, 76× deeper).
**Dormant builds to revive:** Course/curriculum/MIT-OCW extraction (~3,880 atomic skills from 206 MIT courses + 25 CNC/CAM resources, 4 tiers, SCORM export — lima's original mandate, MIT-OCW dir still EMPTY/harvest-on-demand); Content/Video learning pipeline (shared with india — course auto-gen leg).
**Useful ideas to apply:** Be better WITHOUT learning data via engineering quality; multi-source consensus with provenance (course efficacy as the reward signal); product framing (Dave persona, 10-sec patience).
**Top 3 next moves:** (1) Register academy as an SVI subsystem (lever #1 — progress is invisible without a Courses reachability axis); (2) harvest MIT-OCW at scale + resolve the 37-vs-29 catalog drift so the content backbone is complete-by-construction; (3) close course → certification → shop-floor qualification (`EmployeeRoleAcademyInjectionEngine` → scheduler gate) so learning is load-bearing — course-efficacy outcomes feed india.

## MIKE — wedm
**Goal:** Deepest fully-closed-loop domain — print→EDM-program with self-improving discharge-physics AI. EDM pipeline 0.38 (worst in fleet) → 0.72 → 1.0.
**Trajectory status:** Stalled-on-soul / mixed — shifted over the month from broad fleet-hygiene (COMMAND-KERNEL, BRIDGE-WIRING) toward its wedm soul (training-wizard audit → P2P accuracy harness); the "100%" is a regression-lock, NOT measured accuracy (honestly flagged).
**App features (built + to-build):** Built — print-to-program (`EDMDrawingInterpretationEngine`), feasibility gate, multi-pass rough+skim, start-hole setup, cut+flush params, quality/recast/HAZ monitoring, Mitsubishi MV1200R master post, taper/corner/slug handling. To-build — a saleable WEDM Master Post product mirroring SFC/Master Post; broaden dialects beyond 6 (Sodick/Makino/AgieCharmilles parity).
**MCP server features:** Built — `edmDispatcher` (~50 actions). Needed — connect the wire/electrode + dielectric registries into the 8-stage pipeline (EDM has NO tools/wire registry — the single biggest reachability hole).
**Backend dev tools:** Self-improving cluster on disk (`WEDMNeuralTraining`, `WEDMContinuous/Online/LearningLoop`, `WEDMLoRAAdapter`+`Cadence`+`DatasetBuilder`, `WEDMTransferLearning`, `WEDMDriftDetection`); `scripts/wedm-print-to-program-accuracy.ts` harness. BLOCKER: `WEDMLoRADatasetBuilderEngine.ts` is 0 bytes.
**Dormant builds to revive:** **6 WEDM safety-guard engines + WEDM-P2P production pipeline** (current-density/pulse-limit/power-density/kerf-width/wire-deflection/thin-wire-derate guards, physics-backed; WEDM-COMPREHENSIVE 12 engines/15,900L/249 tests near production); **WEDM sensor-fusion + digital-twin stack + EDMDischarge fusion plugin** (Kalman over 8 streams, <10ms twin, closes the gap↔MRR↔Ra coupling loop).
**Useful ideas to apply:** 4-LOOP per-unit quality protocol + 7 Synergy Laws (wedm/mill origin); cross-galaxy UX-parity leveling (WEDM↔lathe, the 6-step studio wizard clone template); single-source limits from `wire-spec-sheets.ts`.
**Top 3 next moves:** (1) Fix the 0-byte `WEDMLoRADatasetBuilderEngine` (`U-WTW00/00a/00b`) + wire the wire/electrode + dielectric registries into the EDM 8-stage pipeline (Tier-2 #7 — 0.38→0.72, also caps kilo EDM + echo wire-post); (2) WEDM-P2P-ACCURACY TASK #3 — force real G-code emit vs a held-out JM corpus so accuracy can drop below 100% and expose the `patterns.ts` R7 gap; (3) fill the empty wedm-soul doctrine (§5 gotchas / §6 tribal) with first-hand discharge physics — note U-WTW-AUDIT recommends routing the WEDM-TRAINING-WIZARD build to charlie (canonical wire slot).

## OSCAR — speed-feed
**Goal:** Ship SFC as a calibrated, vendor-beating saleable product that self-corrects on shop actuals. Engines wiredPct 88 → 100; MS-SFC-CALIBRATE never_started → built.
**Trajectory status:** On-track — moved from "wire dormant SFC engines" to proving SFC accuracy against HSMAdvisor + G-Wizard on real JM tooling (3-way vendor-fairness comparator), now heading into the cited-baseline data-population grind.
**App features (built + to-build):** Built — 9-axis SFC recommendation, single-cell physics, Altintas SLD chatter-stable-RPM gate, tri-vendor parity diff, machine/heat-treat-aware adjustment, G-Wizard/HSMAdvisor library+machine export, lathe CSS/G96 facade, MS-CAM-MASTERY Fusion pillar D ("Speed&Feed via PRISM"). To-build — closed-loop calibration product (`MS-SFC-CALIBRATE`, 24u never_started — the saleable moat).
**MCP server features:** Built — `prism_calc:sfc_nine_axis_run`, `sfc_calculate`, `ultimate_speed_feed`, `sfc_pdf_corpus_bridge`. Needed — wire `U-OSC9-CALIB-APPLY-WIRE` keystone (orphaned/flag-gated).
**Backend dev tools:** `UltimateSpeedFeedEngine` (31 models, 401-assertion gauntlet), `SpeedFeedOrchestratorEngine` (2,851 LOC), `SpeedFeedNineAxisOrchestratorEngine`, HSMAdvisor `settings_v2.xml` live-state adapter, `SpeedFeedPropagationBridgeEngine` (fans to mill/lathe/wedm + print_to_program).
**Dormant builds to revive:** **PhysicsFusionConvergenceEngine** (Anderson accel + Broyden fallback + DFT oscillation detection + per-ISO-group α-adaptation — the mathematical heart of multi-model fusion; production S/F mandates fusion_tier≥2); **Tool DB → 45K + Tool Explorer app** (schema-normalize-FIRST, inline RPM/feed "killer feature"); **CoolantRegistry (200+) + CoatingRegistry (100+)**; **ThreadCalculationEngine** (P0 "missing = people die"); **Dormant physics/AI IP catalog** (490-formula registry, THERMAL/VIBRATION/SURFACE <10% wired).
**Useful ideas to apply:** Mathematical governance (formula_id/units/provenance, no inline constants); edge-case routing gate (thin walls / deep pockets / stainless-titanium → conservative or manual review); single canonical source per data class.
**Top 3 next moves:** (1) Build `MS-SFC-CALIBRATE` (24u Stacked-Bayesian regime-routed ensemble — the saleable differentiator; turns physics-default into shop-calibrated) + wire the orphaned `U-OSC9-CALIB-APPLY-WIRE`; (2) verify/revive PhysicsFusionConvergenceEngine + populate Coolant/Coating registries (every S/F + tool-life calc silently approximates around these); (3) lift Engines wiredPct 88→100 + ingest the 44 outstanding vendor catalogs — oscar's SFC improvement lifts mill/lathe/wedm/echo simultaneously (the SFC spine touches 5 galaxies).

## ROMEO — wiring
**Goal:** Drive every built engine/algorithm/formula/tribal-tip to invokable-via-dispatcher; 97% → sustained 100%. 110 engines unwired.
**Trajectory status:** Divergent (justified) — the wiring galaxy pivoted hard into DB/tooling-catalog POPULATION + CAM-vendor export wiring (filling empty Sandvik/ISCAR/Kennametal catalogs, CIMCO/Fusion/hyperMILL export) rather than the core 110-engine unwiring mission, which "hasn't started executing" (MEMORY.md "No sessions yet" for the wiring SVI).
**App features (built + to-build):** Romeo is infra, not a product surface. Built — catalog DB-gen out to real shop tools (CIMCO Edit 2026 .tmlib, Fusion .machine XML, hyperMILL .hmt). To-build — a closed-loop "ghost-action eliminator" (detect Zod enum actions no engine handles).
**MCP server features:** Built — `AutoWiringEngine`, `DispatcherRoutingEngine` (runtime route table), `cimco_toollib_export`→prism_data round-trip. Needed — an impact-ranked auto-wire queue (callers × test-coverage × domain-leverage).
**Backend dev tools:** `EngineUtilizationAuditEngine` (fire-count after wiring), `AgentSDKVerifierEngine` (post-commit wire-correctness), `audit-unwired-engines.mjs`, `/wire-unwired` `/wiring-batch` `/wiring-potential` `/utilization-dashboard`; `estimateExtractionPlan()` (host-aware concurrent OCR sizing).
**Dormant builds to revive:** **R13 monolith intelligence extraction (7 modules, ~27,000 lines)** — rules_engine, machining_rules, troubleshooting trees, operation_sequencer, tool_selector, constraint_engine, gcode_generator (the largest single pool of dormant executable expertise; 184 of 257 extracted engines also unwired — romeo's literal mission); the wire-don't-rebuild facade-delegation pattern (the integration-first thesis).
**Useful ideas to apply:** Wire-don't-rebuild + UTILIZE-BEFORE-CREATE (asset-utilization audit table as the decision substrate — romeo's operating doctrine); explicit "Retired Work" tables (kill redundant units, don't build duplicates).
**Top 3 next moves:** (1) Wire the 110 unwired engines in ≤5/commit batches (Other:21, Speed:6, Monolith:5 lead) — the single largest reachable-units lever fleet-wide (Engines 88%→100%, Tier-1 #4); (2) wire dormant Algorithms (85%) + Tribal Tips (80%, rank-1 opportunityScore 0.79) + purge ghost Zod actions; (3) execute the BLACKWELL-DB-GEN concurrent vision-OCR catalog extraction (real cited catalogs feed SFC/quoting/CAM) — hand each wired batch to uniform for silent-no-op verification.

## SIERRA — system-viz
**Goal:** Always-fresh, always-trustworthy fleet brain map — one canonical graph that IS the fleet search substrate. Merge OOM → 0.
**Trajectory status:** On-track — steadily converting the 548MB graph into a fleet search/navigation substrate (find-cache reliability → nav-inject → cross-substrate typed edges); absorbing romeo cross-lane orphans when its own queue is empty.
**App features (built + to-build):** Built — `/system-viz` 3D 10-layer + 21-roost map, `/master-index` + `/utilization-dashboard` + `/orphan-inventory` + `/deep-search`, pre-bash/grep/read/write graph-inject nav hints, node→repoPath template. To-build — a customer/operator dashboard view of the graph (today it's a dev-fleet tool); live diff/delta view between regens.
**MCP server features:** Built — `MasterIndexEngine`, `GraphImportanceEngine` (personalized PageRank), `prism_session` ranked-hybrid-graph-search, hybrid_search MCP wiring. Needed — OOM-proof streaming merge so all 49 roosts splice reliably.
**Backend dev tools:** `regen-viz.mjs` (the ONE canonical writer, ~7 min/run), ~48 `generate-*-features.mjs` ghost-roost generators, `merge-augmentations.mjs` ADD-only splice, cross-substrate-edge-schema, system-viz-dead-pixel-detector; heap bumped 16→24GB.
**Dormant builds to revive:** Embedding/semantic-RAG infra (sierra produces the labeled substrate — 768-d node embeddings + ref-pool — the consumer half india owns); the reverse-index/master-index awareness ancestor (sierra's lineage).
**Useful ideas to apply:** Trajectory evaluation (verify engine-call sequence — protective hooks that BLOCK downstream emission when an upstream artifact is stale; R-SVIZ-1 verify-after-write); single canonical source per data class (one canonical writer).
**Top 3 next moves:** (1) Kill the merge-augmentations exit-134 OOM with a streaming/chunked merge so `pendingCount=0` + `sidecarOk=true` every regen — Tier-0 #1, blocks india's eval holdout + every slot's search; (2) close the dual-registration gap (9 of 49 generators in FAST[] but unspliced → ghost data silently dropped, 7 blocked on the OOM); (3) produce the seed-ghost ref-pool + heterophily-ready embeddings so india's GNN AUROC becomes load-bearing — fixing the OOM directly unblocks india's retrain.

## WHISKEY — lathe
**Goal:** Any turned-part print → crash-safe, controller-ready lathe program for JM's 100%-Okuma fleet. Turning pipeline 0.74-0.78 → 0.92+ → 1.0.
**Trajectory status:** On-track — moved from breadth (wiring every lathe AI engine + full-corpus training of 16,558 programs) to depth/rigor: proving real print-to-program accuracy vs JM Okuma .MIN ground truth with R12-honest lower bounds (41.6% mean — a floor).
**App features (built + to-build):** Built — print/CAD → lathe program, 3 pre-emit safety gates (predicate/part-off/jaw-select), G96/G50 CSS-cap + G76/G92 threading + G75 peck-groove, `/lathe-lint` (8 gotchas), JM Die program upgrader v2, 14-vendor insert/holder catalogs, hard-turn + Swiss + diamond-turn. To-build — lathe Master Post productization parity with mill/SFC.
**MCP server features:** Built — `turningDispatcher` (373 actions) + 3 sub-dispatchers (turningProgram 14 / thread 17 / threadingPipeline 3); ~39 AI-tier actions on `prism_turning` (Bayesian opt, active learning, RL select, LoRA training). Needed — connect the 4th (strategies) registry into Turning stages.
**Backend dev tools:** `LatheAI*` stack (Orchestration/Reasoning/Ultra/ActiveLearning/Attention/Anomaly/Bayesian + ContinuousLearning), 3-stage `LatheProgramAuditPipelineEngine` (114,646 variants), `lathe-print-to-program-roundtrip-accuracy.ts` harness, operator SFM-override capture.
**Dormant builds to revive:** **Multi-channel + sub-spindle mill-turn pipeline (MT-MS4/MS7)** — channel-sync codes (M200/M201/WAITM), sub-spindle transfer grip/thermal, bar-whip Timoshenko beam, guide-bushing deflection, bar-feeder M99 loop (serves JM's Multus B250II; `assembleProgram()` was empty); the LATHE-MASTER P0 substrate (Z3/SMT G-code verification, predictive pre-play twin).
**Useful ideas to apply:** Trajectory evaluation (verify the engine-call sequence — catches silently-skipped safety steps); be better WITHOUT learning data via engineering quality; cross-galaxy UX-parity leveling (the 6-step studio wizard).
**Top 3 next moves:** (1) Connect the 4th (strategies) registry into Turning (0.74→0.92, matching MillTurn) + wire dormant lathe engines/algorithms; (2) close the iter-3 accuracy gap (material inference from .MIN + JM-shop SFM/feed calibration — textbook ISO-P runs 2-4× JM's conservative practice, the root of the 41.6% floor); (3) merge the unmerged `H:/prism-slot-whiskey` 6-file galaxy brain + fix `LATHE-MASTER` envelope drift so discovery isn't blind.

## XRAY — blueprint-vision
**Goal:** Universal front-door — any unstructured input → clean, mm-normalized, per-field-confidence structured data. Blueprint 91% wired → 100%; register a galaxy SVI row.
**Trajectory status:** On-track (latest session diverged) — steady march from galaxy scaffolding → OCR closed-loop + overnight batch → supervised print-to-CAD/program training data → multi-source dimension fusion; newest session pivoted onto romeo's BLACKWELL-DB-GEN (0 xray commits).
**App features (built + to-build):** Built — multi-print PDF split (8,154 containers → 36,638 prints), GD&T callout parse + FCF datum-tie validate, tolerance stackup/IT-grade/fit, per-format native CAD parse (DXF/SVG/STEP/STL/FreeCAD/Fusion), `blueprint_to_quote` + `print_to_program_full` bridges. To-build — native readers for SAT/OBJ/FBX/X_T (Parasolid); Polish/Spanish annotation OCR (JM shop-floor languages).
**MCP server features:** Built — `cadDispatcher` (~40 actions): `cad_pdf_blueprint_extract`, `cad_pdf_pattern_rescue_extract`, `cad_gdt_callout_parse`, `cad_fcf_validate`, `cad_dimension_reconcile` (CrossSourceDimensionReconciliation, noisy-OR confidence-weighted). Needed — close the 1 pending Blueprint asset → 100%.
**Backend dev tools:** `extract-jm-die-corpus-page-by-page.py`, `BlueprintLoRABridgeEngine` (`blueprint_lora_prepare_set/export`), `BlueprintExtractionRAGEngine`, `BlueprintCoverageAuditEngine`, `ocr-closed-loop.mjs`, `bench-vision-ocr-ab.mjs` (built 2026-06-03), overnight reaper-immune OCR batch vehicle (qwen3-vl:8b).
**Dormant builds to revive:** Function-index pattern (per-CAD-vendor param query, shared with delta); xray feeds the CAD→CAM→post front-end (OCR→features) and is the front of the print-to-program spine — its revival leverage is in finishing native readers + the A/B vision model promotion.
**Useful ideas to apply:** Mathematical governance / conflict-flag-never-average (R12 — already core to `cad_dimension_reconcile`); UNITS FIRST (INCH→mm CONVERSION_BASED_UNIT 25.4 guard); single canonical source per data class (SEARCH juliett's indexed corpus, never re-OCR).
**Top 3 next moves:** (1) Source REAL dim candidates for `cad_dimension_reconcile` via 3 thin adapters (print-OCR store / CAD STEP geometry / CNC G-code coord-span) — the started-but-unbuilt NEXT step; (2) run the vision-OCR A/B benchmark on a quiet fleet + promote the winner so OCR confidence rises measurably; (3) forge the 4 missing native readers (SAT/OBJ/FBX/X_T) closing silent-empty-parse paths + wire the 1 pending Blueprint asset to 100% — extraction outcomes feed india's GNN/LoRA.

---

## Fleet-wide dormant-build revival priority

The 25 dormant builds from the archaeology, ranked by leverage (compounding/unblocking first, then revenue, then domain depth — the archaeology's own ordering), each with the owning slot/galaxy. Tier-0/1 builds unblock multiple galaxies and should precede single-domain depth.

| # | Dormant build | Owning slot (galaxy) | Leverage class |
|---|---------------|----------------------|----------------|
| 1 | **PostgreSQL persistence + pgvector semantic search** | golf/juliett (infra/database) | Tier-0 — durable state + AS9100 audit; every revenue galaxy depends on it; live fail-loud violation |
| 2 | **Embedding infra → Semantic RAG / Router / Memory** | india + alpha (ai-training/token) | Tier-0 — #1-surfaced by 3 plans; replaces fleet-wide `String.includes`; NN-GRAPH is only partial |
| 3 | **R13 monolith intelligence extraction (7 modules, ~27,000 lines)** | romeo (wiring) | Tier-1 — largest pool of dormant executable expertise; feeds troubleshooter + process-planning + tool-selector across every domain |
| 4 | **MachineHandbookIntelligence pipeline (7 engines)** | golf (fleet-hygiene/substrate-truth) | Tier-1 — gates G-code vs real firmware/spindle maxes; 70% reuses DocumentLearning; handbook coverage = SVI dimension |
| 5 | **MachinePackageGeneratorEngine + 12 machine-safety hooks** | golf/romeo (machine-data debt) | Tier-1 — machine data 0.5% complete, 0/920 confidence; poisons every cut-safety decision |
| 6 | **5 ONNX/GNN mill neural engines** | foxtrot + india (mill/ai-training) | Tier-1 — NN-GRAPH infra now exists; embed 24,545 JM programs for similarity/anomaly |
| 7 | **Content/Video learning pipeline (6 engines)** | india + lima (ai-training/academy) | Tier-1 — "the only post-processor that learns"; physics-validate every ingested tip |
| 8 | **Real-geometry toolpath gen + cutter-comp + multi-setup (MILL-MS4)** | foxtrot (mill) | Tier-2 — highest-value for ACTUAL print-to-G-code; "zero production value" without it |
| 9 | **6 WEDM safety-guard engines + WEDM-P2P pipeline** | mike (wedm) | Tier-2 — safety-critical; WEDM-COMPREHENSIVE 12 engines near production |
| 10 | **PhysicsFusionConvergenceEngine** | oscar (speed-feed) | Tier-2 — mathematical heart of multi-model fusion; production S/F mandates fusion_tier≥2 |
| 11 | **Post-Processor Framework (UIR→12+ controllers) + OptimizationReportEngine** | echo (post-processor) | Tier-2/revenue — the OptimizationReport is the #1 sales artifact; lead every paid milestone |
| 12 | **DrawingToQuoteMathPipeline + MonteCarloCostPDFEngine** | charlie (quoting) | Revenue — the stakeholder-unanimous "money shot" (P10/P50/P90); RFQ→quote spine |
| 13 | **Fusion 360 3-tier add-in product (Free/Pro/Ultimate)** | kilo + cad (cam/cad) | Revenue — single largest unbuilt revenue surface; F360-REV silo fix high-value alone |
| 14 | **Tool DB → 45K + Tool Explorer app** | oscar/romeo (speed-feed/data) | Revenue/data — largest open data gap; index-before-expand; inline RPM/feed "killer feature" |
| 15 | **Lean/Six-Sigma manufacturing-intelligence dashboards** | hotel (business) | Revenue/low-effort — engines exist, only need UI + downtime fact-table |
| 16 | **InstantQuoteEngine + DFMPipeline + FileStorage/PartsLibrary** | charlie/hotel (quoting/business) | Revenue — competitor-parity (Xometry/Fictiv) instant-quote backend |
| 17 | **Shop OS canonical event spine (ShopStateEngine)** | hotel (business) | Foundational — one source of truth for jobs/labor/approvals; the connected-shop-floor OS |
| 18 | **Multi-channel + sub-spindle mill-turn pipeline (MT-MS4/MS7)** | whiskey (lathe) | Domain-depth — serves JM's Multus B250II; `assembleProgram()` was empty |
| 19 | **WEDM sensor-fusion + digital-twin + EDMDischarge fusion plugin** | mike (wedm) | Domain-depth — real-time shop-floor infra; closes WEDM's coupled-convergence loop |
| 20 | **Dormant physics/AI IP catalog (490-formula registry)** | oscar (speed-feed) | Domain-depth — THERMAL/VIBRATION/SURFACE/ECONOMICS nearly dark; pre-extracted, unused |
| 21 | **ThreadCalculationEngine** | oscar (speed-feed) | Safety-critical — P0 "missing = people die"; threads pervasive across mill/lathe |
| 22 | **CoolantRegistry (200+) + CoatingRegistry (100+)** | oscar (speed-feed) | Data — every S/F + tool-life calc silently approximates around these |
| 23 | **MultiAxisPrintToProgramEngine (rebuild)** | foxtrot/kilo (mill/cam) | Domain-depth — most-flagged stub; filter to M460V indexed 3+2 first |
| 24 | **codex-newfile-review orchestrator + AGI self-awareness + DocPropagation** | bravo (hermes-zulu) | Infra — deterministic-gate + advisory-LLM split (R5); doc-propagation kills stale-claim hazard |
| 25 | **WEDMLoRADatasetBuilder (0-byte) + WEDM training-wizard** | mike (wedm) | Domain-depth — blocks wedm_lora; ~100 orphan WEDM engines / ~600KB AI-tier code |

**Top-3 highest-leverage revivals (fleet-wide):** (1) PostgreSQL persistence + pgvector — the single most consequential gap (durable state, AS9100, every revenue galaxy depends on it); (2) Embedding → Semantic RAG/Router/Memory — independently surfaced by 3 plans as #1, replaces fleet-wide `String.includes`; (3) R13 monolith intelligence extraction (~27,000 lines, 7 modules) — the largest dormant pool of executable machining expertise feeding the troubleshooter/process-planning/tool-selector across every domain.
