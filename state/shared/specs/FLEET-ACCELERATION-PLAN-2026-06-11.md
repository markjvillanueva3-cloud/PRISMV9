# FLEET ACCELERATION PLAN -- 2026-06-11 (Obsidian 2nd-brain + Hermes autonomous orchestration)
> Generated by zulu via ultracode discovery Workflow. Operator directive: "accelerate obsidian learning + 2nd brain + hermes self autonomous orchestration of the fleet." Advisory, human-verify.

## Headline
- **The single biggest move is one env-gate removal: take ZULU out of dry-run.** The entire actuator stack (UIA tab-focus, SendKeys, /precompact+/compact+/checkin sequencing) is built, tested, and sweeping every 5 min -- but `PRISM_ZULU_DRY_RUN=1` in the scheduled-task env blocks every actuation (`zulu-orchestrator-log.jsonl` shows all 26 slots returning `gate=dry-run, gateReason=zulu-dry-run-env`). Reinstall without `-DryRun` and the fleet becomes self-orchestrating in minutes.
- **The acceleration is ~70% knob-flips, ~30% real builds.** Both axes are gated more by disabled switches than by missing engineering. Obsidian's galaxy-brain mirror, live-brain read, PSN checklist, leg-coverage gauge, memory-RAG, token-awareness surface, and octopus weekly feed are all built-and-dark behind explicit `=1`/`=0` env gates in `C:/Users/wompu/.claude/settings.json`.
- **The compounding center of gravity is the auto-feed loop.** Wiring `galaxy-synthesis-refresh.mjs --no-cascade` into the Stop hook makes the 2nd-brain self-compound every session end with zero operator action -- today it never fires (0 refs in either settings.json). That one wire turns synthesis from manual-only into continuous.
- **Obsidian's #1 throughput bottleneck is non-incremental sync** (`obsidian-memory-sync.mjs` rewrites the entire 641-memo vault on every run, no mtime/SHA skip) -- an 80-90% I/O cut for a single guard before the writeFileSync loop.
- **Hermes' self-learning tier is wired-but-starved:** `ragPolicyDecision` returns null every sweep because `rerank=null` is hardcoded (`sweep.mjs:381`) and no historical corpus is fed -- the decision history already sits in `zulu-orchestrator-log.jsonl`, it just needs a corpus builder + `prism_memory:semantic_rerank` wire.
- **The recall path leaks tokens on every cold-hit prompt:** `cache_control:ephemeral` is catalogued in the CAG anchor sidecar but never applied (`cag-cold-cache-anchor.mjs:22-23`), so ~120KB of static doctrine re-injects as plain text each cold query.
- **One move compounds most across BOTH axes: lift `PRISM_ALLOW_UNWIRED=1`** (`settings.json:48`). It re-arms R15 orphan-pressure, which is exactly the forcing function that drives GNN tier-5 (AUROC 0.808, deploy-ready-selective) to auto-classify the 66 truly-dormant engines into dispatchers -- feeding both the brain (more wired surface to learn) and the orchestrator (graph-driven routing). Requires false-positive re-verification first.

## RANKED ACCELERATION QUEUE (cross-facet, by ROI)
1. **[B] Remove `PRISM_ZULU_DRY_RUN` from the ZULU scheduled task** -- reinstall via `install-zulu-orchestrator-task.ps1` with no `-DryRun` flag (`install-zulu-orchestrator-task.ps1:15-16`); proven actuator stack goes live for all 26 opted-in slots -- **QUICK-WIN** -- owner: **zulu**
2. **[A] Wire `galaxy-synthesis-refresh.mjs --no-cascade` into the Stop hook** -- brain auto-compounds every session end (today 0 refs, never fires) -- ~~QUICK-WIN~~ **BUILD** (downgraded 2026-06-11, zulu deep-read R8/R12) -- owner: **golf**
   - **CORRECTION (do NOT wire as a blocking Stop entry):** the script is generation-heavy, not a fast advisory. `--no-cascade` only skips the L2 re-run; the L1 regen still calls Ollama generation for every stale galaxy (its own header: "~20 min of generation" worst case; exit-code contract `0=done 1=fail 3=deferred`). A blocking Stop entry would hang session end for minutes and contend with any active GPU job (e.g. the in-flight 7B QLoRA training). Correct wiring = a **throttled-detached spawn wrapper** mirroring `stop-obsidian-memory-feed.mjs` (fire-and-forget, ≤1×/Nmin fleet-wide stamp, never blocks Stop), built when the GPU is idle. Detect-only freshness can be polled safely anytime (hash-compare uses `/api/embeddings`, light); the regen is the GPU-heavy part to gate.
3. **[A/PSN] Re-enable PSN checklist inject** -- delete `"PRISM_PSN_CHECKLIST_INJECT_DISABLE": "1"` (`settings.json:65`); restores "which leg owns this?" forcing function on every prompt -- **QUICK-WIN** -- owner: **golf**
4. **[A] Re-enable memory-RAG** -- `"PRISM_MEMORY_RAG_DISABLED": "0"` (`settings.json:59`); cross-session retrieval feeds every slot startup -- **QUICK-WIN** -- owner: **golf**
5. **[A] Enable galaxy MEMORY.md Obsidian mirror permanently** -- `PRISM_GALAXY_MEMORY_OBSIDIAN_MIRROR=1` (gate at `obsidian-memory-sync.mjs:47`, default OFF); 34 galaxy brains become visible to the Obsidian graph + bridge backlinks -- **QUICK-WIN** -- owner: **alpha**
6. **[A] Incremental sync in `obsidian-memory-sync.mjs`** -- add mtime/SHA skip before the writeFileSync loop (`PRISM_OBSIDIAN_SYNC_INCREMENTAL=1` default on); 80-90% I/O reduction on the 641-memo vault -- **BUILD** -- owner: **alpha**
7. **[B] Lower ZULU actuation threshold `critical` -> `warn`** -- extend `decideClearOrCompact` (`chat-orchestrator-decisions.mjs:68-70`) with a `warn` arm guarded by `hasUnresolvedHandoff || slotQueueLength>0`; preempts the high-watermark forced-compact cliff -- **BUILD** -- owner: **bravo**
8. **[A/recall] Tribal embed `keep_alive`** -- add `keep_alive: process.env.OLLAMA_KEEP_ALIVE || "30m"` to the `/api/embeddings` fetch body in `tribal-rerank.mjs:58-62` (currently no keep_alive field); kills ~200-400ms cold-reload between hits -- **QUICK-WIN** -- owner: **sierra/india**
9. **[A/B] Lift `PRISM_ALLOW_UNWIRED=1` + arm `stop_on_unwired_assets.mjs`** -- `settings.json:48` -> `"0"` and add the hook to the Stop block (wiring test 4/4 proves it works); re-arms R15 orphan-pressure -> GNN tier-5 auto-classifies 66 dormant engines. Requires false-positive recheck -- **BUILD** -- owner: **india/sierra**
10. **[PSN] Regenerate cross-substrate edges** -- run `node scripts/generate-cross-substrate-edges.mjs` (augmentation file is currently **0 bytes / 0 edges**); materializes `consensus-of` (line 621) + `documented-by` so GNN + Obsidian brain are graph-traversable; then cron it -- **BUILD** -- owner: **sierra**
11. **[A] Install weekly-synthesis cron + `PRISM_WEEKLY_SYNTHESIS_OCTOPUS=1`** -- `pwsh -File scripts/cron/weekly-synthesis-cron.ps1` (exists, uninstalled); folds 26 unused octopus consensus runs into the Sunday retro -- **QUICK-WIN** -- owner: **golf**
12. **[B] RAG-policy corpus builder for ZULU** -- `scripts/build-zulu-historical-corpus.mjs` parses `zulu-orchestrator-log.jsonl` into `{fingerprint, decision, outcome}`, wire `prism_memory:semantic_rerank` to replace hardcoded `rerank=null` (`sweep.mjs:381`); closes the self-learning loop -- **BUILD** -- owner: **bravo**

## QUICK-WINS (flip an existing switch -- do first)
- **Take ZULU out of dry-run** -- reinstall `install-zulu-orchestrator-task.ps1` with no `-DryRun`; live SendKeys actuation for all 26 slots (`zulu-orchestrator-log.jsonl` currently all `zulu-dry-run-env`).
- **`PRISM_MEMORY_RAG_DISABLED` -> `"0"`** (`settings.json:59`) -- restores cross-session RAG retrieval, dead since the knob was set.
- **Remove `PRISM_PSN_CHECKLIST_INJECT_DISABLE=1`** (`settings.json:65`) -- restores 11-leg "consult the right leg first" directive on every prompt.
- **`PRISM_PSN_LEG_COVERAGE_GAUGE=1`** (consumed at `psn-leg-state-inject.mjs:80`, currently unset) -- live N/11 coverage dial in every inject.
- **`PRISM_OBSIDIAN_LIVE=1`** -- enables per-prompt `prism_session:obsidian_search` (`slot-context-bundle-inject.mjs:36,237`, 1.5s timeout + TTL cache); wires leg #1 into every decision.
- **`PRISM_GALAXY_MEMORY_OBSIDIAN_MIRROR=1`** (`obsidian-memory-sync.mjs:47`, default OFF) -- 34 galaxy brains reach the Obsidian graph.
- **Flip the 3 token-awareness knobs to `"0"`** -- `PRISM_TOKEN_AWARENESS_INJECT_DISABLE` (`settings.json:75`), `PRISM_META_INJECT_DISABLE` (`:82`), `PRISM_TOKEN_AWARE_STOP_DISABLE` (`:84`); restores budget/spiral visibility.
- **`PRISM_WEEKLY_SYNTHESIS_OCTOPUS=1`** (in cron wrapper + settings env) -- folds 26 octopus runs from `state/shared/octopus-runs.jsonl` into the weekly retro.
- **`PRISM_ROUTER_TABLE_ADVISE_DISABLE` -> `"0"`** (`settings.json:11`) -- re-enables the model-routing advisor.
- **`tribal-rerank.mjs` keep_alive** -- pass `OLLAMA_KEEP_ALIVE` (already exported fleet-wide, commit `e5f29a5df`) in the embed fetch body (`tribal-rerank.mjs:58`); prevents nomic-embed-text eviction in the 60s throttle window.
- **`PRISM_CAG_ROUTER_MIN_CONF=0.3`** (settings env; default undefined/0) -- low-evidence HYBRID falls back to HOT-only, cutting dual-layer injection noise (`cag-router-inject.mjs:155-157`).
- **`PRISM_5H_SWITCH_GATE_ENABLE=1`** (task env) -- arms the shipped denominator-free 5h account-switch keystone (2026-06-11); autonomous account cycling.
- **`PRISM_NODECARD_PREFETCH_K` 3 -> 5 + add `ghost.*` to whitelist** (`node-card-prefetch-inject.mjs:38`) -- surfaces ghost-roost soul/slot on every relevant prompt, zero build.
- **`PRISM_ZULU_COMPACT_WAIT_MS=60000`** (from 90s default, `sweep.mjs:34`) -- recovers 30s/compact slot, cuts sweep wall-time.
- **Run the sidecar rebuild** -- `node scripts/build-memory-index-sidecar.mjs && node scripts/build-memory-embeddings-sidecar.mjs` (`PRISM_MEMORY_SIDECAR_FORCE=1` if needed); un-stales the hash oracle so synthesis staleness detection (`galaxy-synthesis-refresh.mjs:89-98`) stops misreading STALE as FRESH.

## BUILDS (real engineering, owner-routed)

**alpha (Obsidian brain / leg #1, galaxy federation, memory-embedding surface):**
- Incremental mtime/SHA skip in `obsidian-memory-sync.mjs` before the writeFileSync loop (80-90% I/O cut on 641 memos).
- Nightly `install-vault-feeder-task.ps1` running `vault-to-gnn-refpool.mjs --apply` + `vault-to-lora-dataset.mjs --out` at 24h cadence (mirror `install-brain-refresh-task.ps1`); closes the vault->AI-feeder gap (both feeders are manual today, no cron/hook wiring).
- Auto-embed Stop hook (T5, ~10 min throttle) wiring `embed-missing-wiki-batch.mjs` so session-written wiki pages embed without manual `/wiki-ingest` (`embed-all-wiki.mjs` / `embed-wiki-into-tribal-index.mjs` have no scheduled trigger today).

**golf (fleet hygiene -- cron installs + settings.json wiring):**
- Add `galaxy-synthesis-refresh.mjs --no-cascade` as a Stop hook entry (currently 0 refs in either settings.json; referenced only as a manual advisory at `slot-context-bundle-inject.mjs:127`).
- Install the weekly-synthesis cron (`scripts/cron/weekly-synthesis-cron.ps1` exists, `schtasks` shows no "PRISM Weekly Synthesis") with `PRISM_WEEKLY_SYNTHESIS_OCTOPUS=1`.
- Execute the settings.json knob-flip batch above (hygiene duty) + fleet-wide validation after.

**bravo (builds hermes-zulu; `SOUL.md:19-20`):**
- `scripts/build-zulu-historical-corpus.mjs`: parse `zulu-orchestrator-log.jsonl` -> `{fingerprint, decision, outcome}` tuples; wire `prism_memory:semantic_rerank` as the `rerank` fn to replace hardcoded `null` (`sweep.mjs:381`). Turns RAG-policy tier from dead weight into a learned compact/clear predictor.
- Warn-level preemptive /compact arm in `decideClearOrCompact` (`chat-orchestrator-decisions.mjs:68-70`) guarded by `hasUnresolvedHandoff || slotQueueLength>0`; prevents the high-watermark cliff (today only `critical` actuates, `decisions.mjs:62`).
- Tabbed-fleet multi-pane fallback: when `hwnd:tabbed-fleet-occluded` (`sweep.mjs:321,437`), broadcast the directive via `AGENT_CHAT.jsonl` instead of SendKeys; closes the last unreachable-window class.

**sierra (system-viz / recall substrate; authored CHEAP-NODE-ACCESS-MS0 + cross-substrate edges):**
- Run + commit + cron `scripts/generate-cross-substrate-edges.mjs` (augmentation is 0 bytes/0 edges today; `consensus-of` impl at lines 554-729, line 621) so edges don't decay back to 0.
- Tribal shard seek-offset index -- mirror `scripts/lib/node-card-offset-lib.mjs` to build `tribal-embed-index-offsets.json`; replaces streaming both 503MB shards sequentially (`load-tribal-index.mjs` buffered walker) with seek retrieval (~98% I/O cut, same gain as node-card).
- Graph-router hook (`U-GRAPHROUTER-HOOK`): UserPromptSubmit hook reads find-cache domain hits, `seekCard(ghost.galaxy.*)` via `node-card-read.mjs:seekCard`, extracts soul-slot + top-3 wiki/mem pointers, injects routing context. ~150 lines, 0 graph reads -> autonomous galaxy->slot routing replacing static CLAUDE.md lookup.
- Add `sourceHash` frontmatter to `galaxy-meta-synthesis.mjs` so L2 `_meta_synthesis.md` self-detects staleness (0 sourceHash hits today, cascade can't detect regen need).
- Cross-chat sidecar rebuild coalescing (`galaxy-synthesis-refresh.mjs:38`): debounced single-flight claim so N parallel chats don't each rebuild the full sidecar.
- Arm `stop_on_unwired_assets.mjs` into the Stop block after false-positive re-verification (per CLAUDE.md §ENGINE WIRING; known false-positive history exists).

**india (GNN tier-5 / neural path):**
- Wire `gradeSelectiveDeploy` output from `nn-graph-eval.mjs` into the wiring-inference dispatcher as the tier-5 selective path (AUROC 0.808 / Brier 0.041 at `minConf=0.7`, validated CLAUDE.md §NN-GRAPH 2026-06-06); pairs with lifting `PRISM_ALLOW_UNWIRED`.
- PageRank-driven `/pick-unit` (`U-PAGERANK-PICK`): wire `algorithmDispatcher:graph_pagerank` (`algorithmDispatcher.ts:585`, wired but zero non-test callers) seeded on pending ghost nodes to rank candidate units by centrality -- a topology second-signal alongside SVI.

**Cross-cutting build (PSN traceability):**
- Stop hook that auto-appends the session's dominant PSN legs consulted (from `psn-leg-state-inject` counters via `feature-counter.mjs`) to the per-chat handoff -- closes the brain->decision->handoff traceability gap.

## Axis A -- Obsidian learning / 2nd-brain: current state + plan
**Current state.** The brain has a working spine: `stop-obsidian-memory-feed.mjs` (Stop T4) detached-spawns `obsidian-memory-sync.mjs --quiet` at most once/3min fleet-wide (stamp `.claude/cache/obsidian-memory-feed-last.json`), rewriting the vault at `H:/prism/knowledge/memories/<type>/` from the 641-file auto-memory dir; `build-memo-embedding-cache.mjs` rebuilds the semantic-recall JSONL incrementally (hash-reuse, batch 64). 35 galaxy `_synthesis.md` files live at `knowledge/memories/patterns/`, built by `galaxy-reflection-synthesis.mjs` (blunt, ~20min) and refreshed surgically by `galaxy-synthesis-refresh.mjs` (hash-diff, `:86-98`). Recall is five layers wired in settings.json: CAG cold-anchor (`:374`), CAG router-inject (`:1342`), tribal-by-domain (`:1387` -> `tribal-rerank.mjs`, nomic-embed over 2x 503MB shards), memory-index precheck (`:1437`, BM25), and the node-card prefetch (`:1372`).

**The gap is that the compounding loops are dark or manual.** `galaxy-synthesis-refresh.mjs` has 0 settings.json refs (never auto-fires); the vault->AI feeders (`vault-to-gnn-refpool.mjs`, `vault-to-lora-dataset.mjs`) and wiki-embed pipeline have no cron/hook; the galaxy MEMORY.md mirror is default-OFF; `cache_control:ephemeral` is documented but unapplied (`cag-cold-cache-anchor.mjs:22-23`); `PRISM_MEMORY_RAG_DISABLED=1` kills cross-session retrieval; and the memory-index sidecar is stale fleet-wide (20+ stale warnings per dry-run), so synthesis staleness detection itself is unreliable. The sync is also non-incremental (641 writes per run regardless of delta).

**Plan.** Flip the dark knobs first (memory-RAG on, galaxy mirror on, live-brain on, PSN checklist + coverage gauge on, weekly-octopus on), rebuild the sidecar to un-stale the hash oracle, then make the loop self-compounding: Stop-hook the synthesis refresh, cron the vault->AI feeders and wiki-embed, and make `obsidian-memory-sync.mjs` incremental. Net: the 2nd-brain ingests, synthesizes, embeds, and feeds the neural/recall layers continuously with zero operator action -- roughly two-thirds quick-win, one-third build.

## Axis B -- Hermes self-autonomous orchestration: current state + plan
**Current state.** The ZULU orchestrator is fully built and running: `zulu-orchestrator-sweep.mjs` + `zulu-orchestrator-lib.mjs` run every 5min (+420s phase, `install-zulu-orchestrator-task.ps1:8-11`). Each sweep reads per-slot pressure (`readChatPressure`/CHO02), decides clear/compact/noop (`decideClearOrCompact`, `chat-orchestrator-decisions.mjs:57-84`), resolves the window via UIA tab-focus then title-fallback, and actuates via `send-keys-to-window.ps1` (CHO04) with /precompact+/compact|clear+/checkin sequencing. All 26 slots are opted in (`zulu-opt-in.json`). The graph substrate is cheap to read -- 548MB master graph via seekable offset index (`node-card-offset-lib.mjs`, ~0.3ms/card), ghost roosts for all 34 galaxies carrying soul-slot + wiki/mem pointers, `prism_session:node_card` (`sessionDispatcher.ts:1883`) and `graph_pagerank` (`algorithmDispatcher.ts:585`) wired.

**The gap is that the live loop is gated off and the learning tier is starved.** The fleet is stuck in dry-run -- `PRISM_ZULU_DRY_RUN=1` makes every sweep classify+decide but never actuate (`zulu-orchestrator-log.jsonl`: all `gate=dry-run`). The RAG-policy tier returns null every sweep (`rerank=null` hardcoded, `sweep.mjs:381`; `ZULU_HISTORICAL_DECISIONS` unset) despite the decision history sitting in the log. `decideClearOrCompact` only actuates at `critical` (`:62`), so slots at 60-80% for hours never get preemptive compaction. The 5h account-switch gate is shipped-but-inert. And `PRISM_ALLOW_UNWIRED=1` + `graph_pagerank`'s zero non-test callers mean the graph is a passive map, not a routing driver -- galaxy->slot mapping is static doctrine, not topology-driven.

**Plan.** First, take ZULU out of dry-run (the single highest-leverage action -- proven stack, one env gate). Then lower the actuation threshold to `warn` (guarded), arm the 5h gate, and feed the self-learning loop: build the historical corpus from `zulu-orchestrator-log.jsonl` and wire `prism_memory:semantic_rerank` into the dead `rerank` slot. Finally, make the graph a routing driver: the graph-router hook (`seekCard(ghost.galaxy.*)` -> slot advice), lift `PRISM_ALLOW_UNWIRED` to re-arm GNN tier-5 dispatch-classification of the 66 dormant engines, and wire `graph_pagerank` into `/pick-unit` for topology-ranked work selection. Net: ZULU moves from sweeping-but-silent to live, self-learning, and graph-aware.
