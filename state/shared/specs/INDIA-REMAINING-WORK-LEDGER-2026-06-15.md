# INDIA REMAINING-WORK LEDGER — ROI-ranked (2026-06-15)

> Compiled by slot:india (session 046765e9) from ALL india work across all previous sessions:
> 235 commits, 24 handoffs + 22 consolidated open-threads, 141 memories (43 C: + 98 vault),
> the full ai-training galaxy (CLAUDE/MEMORY/PATHS/TOOLBELT/SOUL/AWARENESS), 8 ledger specs.
> Method: 3 parallel sonnet miners (1 completed full inventory; 2 reaped mid-work under host load,
> gaps filled inline) + Opus synthesis + dedup + ROI re-rank against india's metrics discipline.
> MCP bridge was down this session — all data from git/file/grep (no mcp__prism__*).
>
> **ROI = impact / effort, adjusted for risk + dependency-readiness + correcting for measured ceilings.**

## CRITICAL CORRECTIONS applied during synthesis (R12 — honest re-rank)
1. **Full-coverage GNN lift (ref-pool growth + H2GCN) is MEASURED-CEILING-BOUNDED, not high-ROI.**
   `U-NN-FEATURE-SEPARABILITY-CLOSE` (44702e0cac, thread 5) is a definitive negative result: tier-5
   GNN cannot recover the structural dispatcher label from text features (LOO 0.339 < 0.5 baseline;
   intra/inter cosine gap 0.0017 → non-separable). Ghosts lack dispatcher edges by definition
   (cold-start). So pouring effort into full-coverage lift is LOW ROI. The cascade correctly defers
   to tiers 1-4. tier-5's selective-deploy @ τ=0.7 was VALIDATED on the Jun-6 62-node pool (AUROC 0.808
   emitted-set) but is **POOL-GATED, not unconditional**: the 2026-06-15 multi-seed probe shows the pool
   collapsed 62→13 and there is now **NO deployable operating point** (0/5 seeds) — so selective-deploy
   is currently OFF pending pool growth (see Phase-C #1 below). AUROC stays seed-stable ~0.83 but macroF1
   ~0.10 (one-class). Calibration for the Brier gate is ALSO a measured dead-end (Murphy reliability
   0.0197 of 0.179). Do NOT re-run the hop-sweep or chase 0.78 full-coverage; DO fix the pool collapse.
2. **The highest-ROI GNN work is VERIFICATION, not lift** — confirm which checkpoint is actually live
   (an 8-dim AUROC 0.096 model may be serving while the eval reports the 768d direct-embed AUROC 0.808);
   if the old one serves, the selective-deploy guarantee is silently violated. Low effort, high impact,
   pure india metrics discipline.
3. **Stale threads excluded:** post-processor work (threads 11/12/13/15/21) is echo's domain since the
   2026-06-09 india→ai-training migration. Recurring `L8-P0/P1/P2-MS2` "Next:" pointers are stale
   roadmap echoes, not live india units.

---

## TIER-1 — DO NOW (high ROI · ready · autonomous · no operator/GPU dependency)

| Rank | Item | Source(s) | Effort | Impact | ROI | Next step |
|------|------|-----------|--------|--------|-----|-----------|
| 1 | **GNN checkpoint-consistency verification** — confirm whether the LIVE inference checkpoint is the 8-dim AUROC 0.096 (2026-05-16) model or the 768d direct-embed AUROC 0.808 eval; confirm `seed-ghost-gnn-classify.mjs` uses `GNN_DEFAULTS.minConf=0.7` and defers below τ. If the old checkpoint serves, selective-deploy guarantees are void. | galaxy 2.2/3.1 · `NN-EVAL.json` vs `--status` | S | 4 | 5.0 | grep GNN_DEFAULTS + diff checkpoint files + reconcile `--status` vs `nn-eval-refresh`; if 768d passed all gates promote via `.candidate.json` |
| 2 | **Octopus null-probe: omit ollama voice instead of seating non-runnable tag** — change the `?? DEFAULT_OLLAMA_MODEL` terminal fallback (`MultiModelConsensusEngine.ts:245/551`) to `?? null` + skip the panel seat when null. | galaxy 6.1 · `MEMORY.md §5` | S | 3 | 4.0 | edit + test; cloud voices carry consensus alone when no local model runnable |
| 3 | **Outcome-bus emission audit + MetaLearning threshold check** — count `state/shared/outcome-bus.jsonl` + `dev-outcomes.jsonl` rows by emitter galaxy; check vs `MetaLearningOptimizerEngine` 2848 threshold; if above + not firing, the meta-learning loop is silently broken. | galaxy 9.1/9.2 · `CLAUDE.md §7` | S→M | 4 | 4.0 | row-count audit script; surface zero-emitter galaxies |
| 4 | **Correct stale `SFCInferenceGateWireEngine` wiring claim** — grep main-tree dispatchers; if zero hits, mark `reference_sfc_inference_gate_wire_la1_2026_06_01.md` unverified. | galaxy 7.2 · `MEMORY.md §NEW AXIS` | S | 2 | 3.0 | grep + memory correction (R12) |
| 5 | **OutcomeDriftCalibrationBridgeEngine live-wiring verify** — read the engine; confirm wired to `prism_outcome` + signals reach the retrain trigger, not a stub. | galaxy 9.3 · `PATHS.md` | S | 3 | 3.0 | inspect + flag if stub/unwired |

## TIER-2 — NEXT (medium · mostly autonomous · loop/cron candidates)

| Rank | Item | Source(s) | Effort | Impact | Notes |
|------|------|-----------|--------|--------|-------|
| 6 | **Wire 6 WIRE_SAFE_DATA engines to dispatchers** (IntentClassifier, PolicyExperienceLedger, TransferLearning, TemporalReasoning, RealTimeAnomalyDetection, KnowledgeIngestion) — DATA/stats/provenance actions ONLY, never NN inference. | galaxy 7.1 · `INDIA-AI-ORPHAN-WIRE-QUEUE-2026-06-11.md` | M×6 | 3 | round-trip E2E test per engine through the dispatcher + 3-of-3. One-per-loop-iter. |
| 7 | **Selective-deploy: evaluate τ=0.5 max-coverage point** — re-eval shows τ=0.5 = 46.77% coverage, 3 classes, Brier 0.101, macro-F1 0.587 (both gates clear). Could become the production gate (broader coverage, still passing). Do NOT go below τ=0.5. | galaxy 3.2 · `NN-EVAL.json §selective.curve` | S | 3 | re-eval only; honest coverage expansion 32%→47% |
| 8 | **LoRA 512-pair coverage audit across 34 galaxies** — confirm `assemble-fleet-lora-corpus.mjs` produces ≥512 pairs/galaxy; trigger `galaxy-synthesis-refresh.mjs` for sparse galaxies. | galaxy 4.2 · `ai-training_synthesis.md §Open threads` | M | 3 | balances training signal |
| 9 | **Conformal coverage audit** — run `ConformalCalibrationMonitorEngine` over the 62-ghost holdout + live log; verify marginal coverage ≥ 1−α. | galaxy 8.2 | M | 2 | validates tier-5 reliability guarantee |
| 10 | **Drift threshold vs graph-growth tuning** — 168h baseline may be too long given fleet graph growth; verify `PRISM_NN_RETRAIN_*` knobs + document recommended window. | galaxy 8.3 · `--status` | S | 2 | avoid stale-model inference after rapid growth |
| 11 | **ConsensusModelPerformanceEngine** — implement real consensus telemetry (per-model AUROC/latency/cost over the octopus outcome ledger) OR formally mark deferred (it's a throwing build-unblock stub). | galaxy 4.3 · `MEMORY.md §12` | M / S | 3 | currently a stub — must not be wired until real |
| 12 | **RAG: Qdrant dense arm of `hybrid-retrieval.mjs`** — wire the sharded tribal embed index as the dense arm of the 4-substrate RRF. | galaxy 5.1 · `INDIA-CONTEXT-LEDGER §2` | M | 3 | deferred low-ROI vs other levers; do after Tier-1 |
| 13 | **RAG cache fingerprint-invalidation audit** + **MIT-OCW corpus completeness** (`extracted/mit-ocw/` missing per PATHS). | galaxy 5.2/5.3 | M | 3 | prevents stale-retrieval; materialize MIT corpus |

## TIER-3 — LATER / BLOCKED / NEEDS-OPERATOR or GPU or CLEAN-SESSION

| Item | Source | Blocker | Impact |
|------|--------|---------|--------|
| **LAND INDIA-MERGE-PLAN-MS0 — 6 built+3of3 features onto main** (GOAL-FORMALIZER, EMBEDDING-SSOT, RAG-HYBRID v1+v2, rag_eval, embedding-honesty) | `INDIA-MERGE-PLAN-MS0.md` | **Needs a clean main-based worktree** — slot/india has UNRELATED history with main (1695 ahead/874 behind); landing = cherry-pick `cd4195d40a..HEAD` (25 commits; 11 new files clean, 7 additive-conflict commits) + verify build on main. Delicate; the plan itself says "execution deferred to a clean session." Tsc-staleness caveat (slot/india 1278 errors are pre-existing). | **5** (6 features go fleet-live) |
| Operator-label active-learning worklist (`active-label-worklist-proposed.md`) → seed ref-pool | galaxy 1.1 · INDIA-CONTEXT-LEDGER §2 | **NEEDS OPERATOR** to resolve 31/31 LLM↔GNN CONFLICTs (class-collapse: GNN predicts prism_cam for all). Even then, full-coverage is ceiling-bounded (see correction 1). | 3 (selective-deploy already covers the useful band) |
| H2GCN heterophily encoder in production trainer | galaxy 2.1 · thread 4 #9 | **GPU + ceiling-bounded** (correction 1 — likely won't clear full-coverage gate). LOW ROI. | 2 |
| rsLoRA r=32-64 GPU training runs | galaxy 4.1 | **GPU-gated** (Blackwell); pipeline ready (`vault-to-lora-dataset` + `assemble-fleet-lora-corpus` ship). | 4 (when GPU available) |
| `xproc_outcome_publish` emission across 19 consumer galaxies | galaxy 9.1 · `CLAUDE.md §7` | **Cross-galaxy integration push** (L). This is the compounding learning engine — but spans other slots' galaxies. | 4 (long-term) |
| AI-functionality-in-Obsidian + H-drive sandbox; surface NN/GNN/LoRA/RAG state as vault notes; wire ask-ollama RAG | thread 4 #11 | needs :3100 MCP rebuild+restart (fleet-coordinated) | 3 |
| Brain-refresh scheduled task on all hosts · 20× corpus-count audit · AMP-CONSUME consumer verify | galaxy 10.1/10.2/10.3 | hygiene; multi-host | 2 |

---

## Execution plan (this session → ongoing)
- **This session (Phase C):** execute TIER-1 #1, #2, #4 (autonomous, S-effort, high-ROI, eval-gated). #3/#5 are audits — run if budget remains.
- **Loops/crons:** TIER-2 is a per-iteration `/loop` queue (one item/iter, eval-gated). The 6 WIRE_SAFE_DATA engines (#6) are ideal one-per-iter loop work.
- **Operator-gated:** the merge-plan landing (TIER-3 top) is HIGH impact but needs a clean session + operator awareness of the unrelated-history cherry-pick. Flag for a dedicated pass.
- **Do NOT pursue:** full-coverage GNN lift / hop-sweep / Brier calibration (all measured dead-ends — correction 1).

---

## Phase C execution log — 2026-06-15 (session 046765e9)

**TIER-1 #1 — GNN checkpoint consistency: VERIFIED, finding sharpened → defined action.**
Live inference loads `graphsage-checkpoint.json` = **dim 8** (AUROC ~0.096, 2026-05-16). The **768d candidate** (`graphsage-checkpoint.candidate.json`, 2026-06-14) is **deploy-ready-selective** (AUROC 0.808, selective @ τ=0.7 passes) but **unpromoted** — purely because `PRISM_NN_SELECTIVE_PROMOTE` is default-OFF, so the lifecycle only promotes on the full-coverage gate (which the candidate correctly fails: macro-F1 0.439, Brier 0.179). `minConf=0.7` gate IS wired (`seed-ghost-gnn-classify.mjs:63`). **ACTION (gate-protected, NOT an india-refuse): set `PRISM_NN_SELECTIVE_PROMOTE=1` on the reaper-immune `PRISM Nn Graph Retrain` scheduled task** → next run selective-promotes the 768d candidate if it clears the selective gate. Heavy/reaper-prone → must run as the scheduled task, not a session bash. Memory: `reference_gnn_checkpoint_selective_promote_gap_2026_06_15`.

**TIER-1 #4 — stale SFCInferenceGateWire claim: DONE (corrected).**
Verified on the main tree: engine + schema + test EXIST, but `calcDispatcher.ts` has **zero** `applyToSFCResult`/`sfcGate` refs, and commit `3d470ac75f` is **NOT an ancestor of main HEAD**. So the wiring is **stranded on slot/india, unmerged** — a 7th item for the merge-landing queue alongside the 6 INDIA-MERGE-PLAN features. Corrected memory `reference_sfc_inference_gate_wire_la1_2026_06_01` with a STATUS CORRECTION block.

**TIER-1 #3 — outcome-bus emission audit: DONE (major finding).**
`state/shared/outcome-bus.jsonl` = **74,538 rows** (39 MB), far above the MetaLearningOptimizer 2848 threshold (so meta-learning is volume-eligible). BUT **99.97% (74,513 rows) come from ONE emitter `outcome-bus-auto-tap`** — only **3 distinct emitters** (the other two: cad-course-closed-loop 18, domain-closed-loop-train 6). The `CLAUDE.md §7` goal "drive `xproc_outcome_publish` across all 19 consumer galaxies" is **UNMET** — the bus is a monoculture, not diverse per-galaxy labeled signal. This is consistent with (and a root cause of) the GNN full-coverage ceiling: 74K near-identical auto-tap rows carry little separable label signal. **The real lever for the compounding loop is emission DIVERSITY (19-galaxy `xproc_outcome_publish` integration), not volume** — promotes TIER-3 item 9.1 to a genuine high-impact cross-galaxy push. (`dev-outcomes.jsonl` = exactly 2848 rows — verify it is not threshold-capped/truncated.)

**Merge-landing queue (TIER-3 top) now = 7 stranded-on-slot/india items:** the 6 INDIA-MERGE-PLAN-MS0 features + the SFC inference-gate dispatcher wiring (`3d470ac75f`). All need the same vehicle: a clean main-based worktree + cherry-pick (unrelated-history; never `git merge slot/india`).

**Remaining TIER-1 (autonomous, not yet executed this session):** #2 octopus null-probe omit-voice (`MultiModelConsensusEngine.ts` .ts edit + build), #3 outcome-bus emission audit + MetaLearning threshold, #5 OutcomeDriftCalibrationBridge wiring verify. These + all of TIER-2 are the `/loop` execution queue (one item/iter, eval-gated).

**Efficiency (Phase A, this session, shipped):** FORK-STORM-CONSOLIDATION bundles (Bash −6/cmd, Stop −11/turn, UPS −8/prompt, PostToolUse dedup −2/Bash) + Phase-3 `batch-self-nice` self-throttle on the 3 heavy india batch jobs (1a40c35a69). See `reference_forkstorm_consolidation_2026_06_14`.

### Phase C continued — 2026-06-15 (TIER-1 #1 activated + TIER-1/2 sweep)
- **TIER-1 #1 — GNN selective-deploy: TRIALED → REVERTED → MULTI-SEED-CHARACTERIZED (root cause corrected)** (`7a72455577`, `b36894cc54`, then the variance probe): the first-pass "single-seed outlier / 0.38 swing" framing was **WRONG** and is corrected here. Built `scripts/nn-graph-holdout-variance.mjs` (+test 15/15) and ran 5 holdout seeds at a FIXED model on the current 763MB graph. **Findings:** (a) direct-embed AUROC is **seed-STABLE** — mean 0.829, range [0.762, 0.881], stdev 0.041 — so the Jun-6 0.808 REPRODUCES, NOT a fluke; (b) the earlier 0.4286 was a **different classifier** (the trained checkpoint, model mode), not a reseed (the trainer is mulberry32 seed-deterministic); (c) the deploy gate fails **stably** on **macroF1 ~0.10** (a near-constant `prism_turning` predictor — AUROC ranks but cannot assign classes) + Brier ~0.22; (d) the selective path is now **NON-DEPLOYABLE (0/5 "no-deployable-operating-point")** because the reference pool **COLLAPSED 62→13** since Jun-6. **Refreshed NN-EVAL.json** (direct-embed, current graph) → `classifyGnn` now returns `selectiveDeployReady:false, verdict:"shipped-research-only"` (VERIFIED) so the PSN leg #10 no longer injects the stale "deploy-ready-selective ✓" banner fleet-wide. Auto-promote stays **OFF** (correct — no classifier clears any gate). **Real lever = reference-pool GROWTH.** The 62→13 collapse: the high-conf reference band collapsed 124→31 (streamed the live graph: 208 ghosts stable, only **31 at conf≥0.8, 129 (62%) bunched in 0.6-0.8 just under the refMinConf=0.8 gate**; architectural fragility: refMinConf==confidenceCap=0.8 gates the pool at the exact ceiling, so candidate-set growth starves it). Engines-wired-out REFUTED as sole cause (count stable 208); seed-loss ruled out (`reference-pool-seed-2026-05-23.json` advisory-only, never merged — the "regen-viz seed stage" guess was WRONG). Mechanism INFERRED not proven (scrutiny arm-B): most-likely confidence-deflation and/or Jun-4 galaxy-feature composition-change — the Jun-6 per-node histogram was not captured, so the two cannot be cleanly separated from one snapshot. **Fix crossroad (surfaced, not unilaterally taken):** (a) operator-label the 31-entry `active-label-worklist-proposed.json` (best, gated, 93% GNN↔Ollama conflict); (b) refMinConf below the cap — **EMPIRICALLY REFUTED**: a 0.8/0.7/0.6 sweep (3 seeds each) grows holdout 13→41→72 but AUROC DROPS 0.80→0.65 (0.6-0.8 ghosts are noisier refs), macroF1 stays ~0.10 at all thresholds (non-separability ceiling invariant), selective stays non-deployable 0/3 at all thresholds → a red herring, NOT a fix; (c) autonomous `vault-to-gnn-refpool.mjs` vault-confirmed ground-truth labels ([[reference_gnn_refpool_vault_grow_2026_06_10]]). Only ground-truth growth (a or c) helps the pool; even that likely lifts at most the SELECTIVE path. CAVEAT (scrutiny arm-B): macroF1 ~0.10 invariant to POOL SIZE does NOT prove a non-separability ceiling — feature-quality (H2GCN/GPU) + class-weighting are SEPARATE untested axes that could move full-coverage. NOT retrains-as-tried/calibration/refMinConf/auto-promote. Memories `reference_gnn_selective_promote_disproven_2026_06_15` + `reference_gnn_pool_collapse_confidence_deflation_2026_06_15`.
- **TIER-1 #5 — VERIFIED HEALTHY:** `OutcomeDriftCalibrationBridgeEngine` (458 LOC, 0 stubs, 2 dispatcher refs). Not a gap.
- **TIER-2 #11 — VERIFIED correctly-deferred:** `ConsensusModelPerformanceEngine` is a throwing stub + NOT wired (R12 must-not-wire honored). No action.
- **TIER-2 #10 — RESOLVED:** drift window IS tunable (`PRISM_NN_RETRAIN_MAX_AGE_HOURS`=168 + NODE/EDGE/GHOST_DELTA_PCT bands). Recommend lowering MAX_AGE_HOURS if graph grows >5%/168h.
- **TIER-2 #13 — gap CONFIRMED:** MIT-OCW corpus NOT materialized (`extracted/mit-ocw` absent) — MIT engines have no corpus; materialize = M (extraction pipeline).
- **TIER-2 #8 — infra present** (assemble-fleet-lora-corpus + vault-to-lora-dataset + many state/shared/lora datasets); full per-galaxy 512-pair count needs an assembler run.

**Genuinely-gated remainder (cannot complete from a slot/india / MCP-down / reaper-active session):** TS-engine items #2 octopus + #6 (6 orphan-wire engines) need the **merge-landing** (clean main-worktree; slot/india too stale to build-verify). Heavy GNN runs (#7 τ=0.5 re-eval, #9 conformal) run via the **SYSTEM scheduled task**. rsLoRA/H2GCN need **GPU**. #3-fix (19-galaxy emission) is a cross-galaxy L. MIT-OCW materialization is M. **Operator unlocks:** (1) authorize a dedicated merge-landing session (unblocks the whole TS queue + 7 stranded features). NOTE: selective-deploy is NOT an instant flag-flip — the multi-seed probe (above) shows no classifier clears any gate on the current pool; the real prerequisite is **reference-pool growth / fixing the 62→13 pool collapse**, then re-run `nn-graph-holdout-variance.mjs` and only enable `PRISM_NN_SELECTIVE_PROMOTE` if the selective gate clears across seeds.

### Phase C-2 execution log — 2026-06-15 (push-through batch, session 046765e9)
- **#16 vault-to-gnn-refpool: APPLIED** — `--apply` merged the 9 vault-confirmed ground-truth refs (1 new node + 8 ghosts updated to conf 0.85; graph now 345175 nodes). The only non-refuted ref-pool lever. Durable re-apply (survive regen rebuilds) = follow-up (wire into regen-viz / scheduled task).
- **#8 LoRA 512-pair audit: DONE** — `assemble-fleet-lora-corpus` → `trainingReady: true` (1372 rows, floor 1000, 34/34 covered) BUT **every galaxy is <512 pairs** (academy:10, ai-training:16…) — per-galaxy SPARSE. Fleet floor met; per-galaxy growth via `galaxy-synthesis-refresh` is the queued follow-up.
- **#11 ConsensusModelPerformanceEngine: VERIFIED ALREADY-DONE (stale ledger claim corrected, R12)** — NOT a throwing stub: real impl by echo (`GRAPH-OCTOPUS-AUTOWIRE-MS0/U-GO-C6`, 2026-05-22), correctly `// WIRE-EXEMPT` (consumed by `MultiModelConsensusEngine` lines 222-227, no dispatcher action by design). No work needed; the "throwing build-unblock stub" framing was stale.
- **#17 τ=0.5 re-eval: NEGATIVE (DONE)** — on the current 13-holdout pool the selective curve is flat-degenerate (τ=0.4/0.45/0.5 all 84.6% cov, Brier 0.2485 > gate, macroF1 0.141); deployPoint.found=false. The ledger's "τ=0.5 = 46.77%/Brier 0.101" was the Jun-6 62-pool, now collapsed. τ=0.5 is NOT a fix on the current pool (same root cause).
- **#23 rsLoRA: ENABLED + GPU-validated** — added `use_rslora` to `fleet_lora_train.py` (config-dict + `--rslora` flag + threaded 3 call sites; +3 tests, 14/14 pass; `--dry-run --rslora --rank 32` builds `{"r":32,…,"use_rslora":true}`, 1372 rows). rsLoRA scales α/√r (Kalajdzievski 2023) — better at r≥32. Venv `H:/Tools/blackwell-gpu-venv` (py3.13, torch 2.11+cu128, peft 0.19.1) + GPU (95GB free) confirmed. Full r=32 production run is READY but best AFTER corpus growth (#8 sparse).
- **#2 octopus null-probe: RESOLVED — verified NOT-A-BUG, the proposed fix would REGRESS (R7/R8/R12).** Traced the full path: when a non-runnable model is seated via `?? DEFAULT_OLLAMA_MODEL` (`MultiModelConsensusEngine.ts:541/570`), `callOllama` (@906) returns `ok:false` gracefully (no crash), and that failed response is FILTERED before consensus (`if (!r.ok || r.answer.length===0) continue` @626; `responses.filter(r=>r.ok)` @634/@751) → cloud voices carry consensus alone, which IS the ledger's desired outcome. The keep-the-name design is DELIBERATE (comments @392-394: "Honoring [] literally would silence the local voice on every GPU-less host; … callOllama's real load attempt is the final authority") to avoid silencing a working local voice on a probe false-negative (the "96GB idle card reporting [] runnable" case). The omit-the-voice change would trade that robustness for one fast-failing call attempt — a regression. NO edit; ledger item closed as based on a stale misread.
- **#9 conformal: BLOCKED this turn** — `ConformalCalibrationMonitorEngine` is engine-only (no CLI runner) + MCP bridge down → no invocation path. Queue (needs MCP up or a CLI wrapper).
- **#6 SCOPED (ready for a fresh focused turn) — `INDIA-AI-ORPHAN-WIRE-QUEUE-2026-06-11.md`:** 6 remaining WIRE_SAFE_DATA engines (IntentClassifier, PolicyExperienceLedger, TransferLearning, TemporalReasoning, RealTimeAnomalyDetection, KnowledgeIngestion). PROVEN pattern (bravo shipped #1 KnowledgeLineage `f7ae1ac016` + #2 LocalEmbedding `894be27d1f`): add action(s) to `INDIA_AI_ORPHAN_ACTIONS` + `INDIA_AI_ORPHAN_SCHEMAS` (z.record) + a `case` before default in `aiReasoningDispatcher` (prism_ai), result `{success:true, data: slimResponse(result)}`; test via `ai-dispatcher-ledger-wire.test.ts` (concrete-value asserts, monkeypatch disk persist). R12 INVARIANT: DATA/stats/provenance ONLY, never NN inference (R8-verify each export+signature first — agent specs UNVERIFIED). COORDINATION: bravo has all-galaxies access + posted to chat-bus on this queue — CHECK chat-bus/file-claims on `aiReasoningDispatcher.ts` before editing (do NOT collide). Lane: build-verify on the main tree (baseline 648 tsc); per-file 2-arm scrutiny per engine.

**#6 IN PROGRESS — IntentClassifier WIRED (1/5; papa already shipped #5 TransferLearning via XFER_LEARN, so 5 remain not 6):** added `classify_intent` + `quick_classify_intent` + `extract_intent_entities` to `aiReasoningDispatcher` INDIA_AI_ORPHAN (R8-verified exports, non-empty-string guards, R12 DATA-only). 44 insertions, **0 deletions** (purely additive); tsc clean (639≤648, 0 in dispatcher); **5/5 new round-trip tests pass** in `ai-dispatcher-ledger-wire.test.ts`. NOTE the dispatcher wraps every result as `{success:true, data: slimResponse(caseResult)}` (line 4806) so tests read `r.data.*`. Source-verified; goes LIVE on next MCP build/restart. On the main-tree disk (git-sync lands it on cad-fusion-live-ms0). **DISCOVERED pre-existing bug (separate unit):** the 14 `ledger_*`/`ledger_drift_*` tests in that file are RED on clean HEAD — they assert top-level `r.config`/`r.entries` but the `{success:true,data}` wrapper nests them under `r.data` (proven: fail with my changes stashed). The data-wrapper was added after those tests; fix = update them to `r.data.*` (NOT done here — not the IntentClassifier unit). **Remaining #6 (4):** PolicyExperienceLedger · TemporalReasoning · RealTimeAnomalyDetection · KnowledgeIngestion (same pattern).

**Still queued:** #24 (19-galaxy `xproc_outcome_publish` — L, cross-galaxy) · MIT-OCW materialization (M) · galaxy-synthesis corpus growth (per-galaxy LoRA pairs toward 512) · #9 conformal (needs MCP up or a CLI wrapper) · full rsLoRA r=32 run: in-session bash launch was **REAPED at step 54/400** (the fleet-reaper kills long session-orphan GPU python under load — the "session child is safe" assumption is FALSE). `checkpoint-50/` salvaged. Re-launched via scheduled task `PRISM rsLoRA R32 Train` — reached **371/400** then hard-killed (no training error, no OOM [35GB free], a peer WPy64 GPU job appeared; kill cause UNCONFIRMED). So the scheduled task DELAYED but did NOT prevent the kill (371 vs 54 in-session) — the "reaper-immune" claim was over-optimistic (R12 corrected). **DELIVERABLE ACHIEVED:** a REAL rsLoRA r=32 adapter exists at `state/shared/lora/adapters/fleet-rslora-r32/checkpoint-350/` (`r=32, alpha=32, use_rslora=true`, `adapter_model.safetensors`) — 87.5%-trained, fully loadable. The last 50 steps are marginal (sparse corpus, GPU contended) — not re-fought. #23 substantively DONE (rsLoRA enabled + validated + a real adapter trained). TRUE reaper-immunity for long GPU jobs needs a reaper protect-list / golf coordination / low-contention window, not just a scheduled task.

### Phase C-3 execution log — 2026-06-15 (push-through batch 2, session 046765e9)
- **#6 engines 2-5: DONE** — wired the last 4 WIRE_SAFE_DATA engines as INDIA_AI_ORPHAN units 4-7 (8 actions, DATA-only R12): PolicyExperienceLedger (`policy_experience_stats/query`), TemporalReasoning (`temporal_snapshots/project/forecast`), RealTimeAnomalyDetection (`detect_cutting_anomalies` + finite/positive guards + 250k DoS cap), KnowledgeIngestion (`knowledge_ingestion_stats/pending`). R8-verified every export+signature first; 17 round-trip tests (happy + ≥3 failure + ≥2 adversarial each). **#6 is now COMPLETE (5/5: Intent + these 4; TransferLearning is papa's XFER group).**
- **CMCCL ledger RELAND (new, the big find): DONE** — the 17 RED `ledger_*`/`ledger_drift_*` failures in `ai-dispatcher-ledger-wire.test.ts` were NOT a `r.data` wrapper bug (prior-session mis-diagnosis, R12-corrected) — the 10 U-CMCCL09/10 actions had **ZERO dispatcher refs**: MasterAITrainingLedger + LoRADriftCoordinator engines were complete but NEVER wired on cad-fusion-live-ms0 (test was @ts-nocheck'd until U-EFF16 stripped it). Wired a new CAM_ML_LEDGER group (10 actions) + fixed ~13 success assertions to `r.data.*`. 39->41 green.
- **BUG FIXED (caught wiring): LoRADriftCoordinator.setConfig mutate-then-validate pollution** — a rejected `ledger_drift_config{set}` left the singleton partial-applied (threshold:0 -> master-retrain fires on every drift). Fixed to validate-before-assign + 2 regression tests (engine + dispatcher); reviewer D empirically proved the test fails on revert (R9-valid).
- **VALIDATION:** 88/88 tests across the 3 affected files; tsc 615 baseline, **0 errors in the 4 changed files** (the transient "0 total" was an incremental-cache artifact). 3-arm scrutiny, 0 P0/P1.
- Memory `reference_cmccl_ledger_reland_2026_06_15` · wiki `code-tribal/learnings/cmccl-ledger-reland-setconfig-pollution`.
- **Net:** 18 new DATA-only prism_ai actions across 6 india AI engines + 1 real engine bug fix. Still queued (unchanged): #9 conformal (CLI wrapper), #24 19-galaxy xproc emission (L), MIT-OCW materialization (M), galaxy-synthesis corpus growth, GPU/operator-gated items.

### Phase C-4 execution log -- 2026-06-16 (#9 conformal-audit TOOL, session 046765e9 continued)
- **#9 TOOL DONE (result still pool-gated).** Shipped `scripts/nn-graph-conformal-audit.mjs` (+ `.test.mjs` **13/13**) -- a tested orchestrator of two existing engines (R8: zero reinvention of LAC/coverage math) wiring `CrossProcessConformalClassificationEngine.calibrate/predictionSet` + `ConformalCalibrationMonitorEngine.configure/record/status` end-to-end. India discipline encoded IN CODE: (a) **REFUSE-GATE** n_test < MIN_MEANINGFUL_N=20 -> `{ok:false,refused:true}` + CLI exit 2 (matches the monitor's own MIN_WINDOW_SIZE -- defense-in-depth); (b) **trustworthy flag** `fullSetRate>0.5` -> trustworthy:false + warning (a trivially-inflated empirical=1.0 is NOT a real signal); (c) `--strict` CI gate fails closed on UNTRUSTWORTHY (exit 2), not just on guarantee-unmet. CLI live-proven: 80-pair fixture -> exit 0 with coverage report; 30-pair fixture -> "REFUSED: insufficient holdout: n_test=15 < MIN=20" exit 2.
- **3 reviewer-A findings fixed inline (operator fix-inline doctrine):** P1 false-green CI gate (`--strict + ok + !trustworthy -> exit 2`, not 0 -- the silent CI green on a known-bad result was the failure the audit exists to prevent); P2 silent miscount on out-of-range test label (bound-check against numClasses before the loop -> clean ok:false); P3 silent NaN on bad CLI flag (`parseArgs` throws "must be a finite number"). **Each fix pinned by a regression test** (incl. a child_process.spawnSync test that asserts the real CLI exit code -- otherwise the gate could be silently regressed in main()'s exit branch).
- **What the tool does NOT solve (R12 honest scope):** the audit RESULT is still pool-gated -- producing a real `{probs,label}` holdout JSONL needs a full per-class softmax `graphsage-predictor` pass over the live 62-ghost holdout, AND the holdout is collapsed to n=13 < 20, so even a full predictor pass would correctly trigger the refuse-gate. Same ref-pool-growth root cause as the tier-5 deploy gate. Fix path unchanged: grow the ref-pool (vault-to-gnn-refpool + operator-label worklist) -> full-softmax predictor pass -> run this audit -> trustworthy coverage number.
- Per-file 2-arm scrutiny: arm A (code-analyzer) PASS 3 findings (all fixed); arm B (test-review-agent) PASS 0 P0/P1 with hand-math verified for every test (LAC, qHat, threshold, coverage). Memory `reference_conformal_audit_tool_2026_06_16` · wiki `code-tribal/learnings/conformal-audit-false-green-strict-gate.md`.
- **Still queued (unchanged):** #24 19-galaxy `xproc_outcome_publish` (L, cross-galaxy) · MIT-OCW materialization (M) · galaxy-synthesis corpus growth -> full rsLoRA r=32 · operator-gated: merge-landing, label worklist, GPU items. **Once the ref-pool grows to n_test>=20:** generate the holdout JSONL with the predictor and run `node scripts/nn-graph-conformal-audit.mjs --predictions <file> [--strict]` -> the FIRST trustworthy conformal coverage number for the tier-5 GNN.

### Phase C-5 execution log -- 2026-06-16 (#24 MEASUREMENT substrate, session 046765e9)
- **#24 measurement DONE (remediation remains cross-galaxy L).** Shipped `scripts/outcome-bus-diversity-audit.mjs` (+ `.test.mjs` **14/14**) -- the reproducible, statistically-rigorous gating diagnostic for outcome-bus diversity. Pure read over `state/shared/outcome-bus.jsonl`; reports per-emitter/per-slot/per-domain histograms with Wilson 95% CIs on the dominant emitter + normalized Shannon entropies. Helpers exported for downstream composition.
- **LIVE production bus measurement (the substantive Phase-C-3 follow-up):** 78,999 rows / 42MB, dominant source `outcome-bus-auto-tap` = **99.97% share [Wilson 95% CI 99.95-99.98] -- MONOCULTURE statistically confirmed**, sourceShannonNormalized=0.0027 (~ 0), distinctSources=3, distinctSlots=25, distinctDomains=26. **Slot/domain diversity is FINE (0.88 / 0.89 entropy) because auto-tap fans out across them -- SOURCE diversity is the broken axis.** 1 malformed line surfaced (no silent drop). Persisted at `state/shared/specs/OUTCOME-BUS-DIVERSITY-2026-06-16.json`.
- **India discipline encoded:** REFUSE-GATE on n<200 (binomial CI too wide), Wilson-CI-lower-bound monoculture (not point estimate -> no false positives on real-world wobble), `--strict` CI gate exit 1 on monoculture (live-validated: production bus -> exit 1). Same pattern as the #9 conformal audit.
- **CORRECTION (R12) to Phase C-3:** the prior "dev-outcomes.jsonl = exactly 2848 rows" finding is now invalid -- the file is GONE from disk (verified). Only `state/shared/outcome-bus.jsonl` exists as a live bus artifact.
- **What this UNBLOCKS for #24 remediation:** every consumer galaxy emitting `xproc_outcome_publish` can score itself against the dated baseline JSON; once a slot adds even 100-1000 emissions, the score moves and is provably measurable. The "volume is not the lever, source diversity is" framing now has a SCORE, not just an anecdote.
- **Cross-galaxy remediation (#24 emission across 19 galaxies) is STILL gated**: peer-owned files, 3 peers online + 9 foreign claims at write time -- proper coordination required. This unit is the substrate they will all gate against, NOT the cross-galaxy push itself.
- Memory `reference_outcome_bus_diversity_2026_06_16` (Obsidian auto-feed on Stop).
- **Still queued (unchanged):** the 19-galaxy emission push (L, cross-galaxy, gated on peer coordination) · MIT-OCW materialization (M) · galaxy-synthesis corpus growth · operator/GPU items.

### Phase C-6 execution log -- 2026-06-16 (vault->GNN ref-pool extraction broadened, session a803c8fa)
- **U-VAULT-REFPOOL-BROADEN: SHIPPED** (`07506609fa`, cad-fusion-live-ms0). The ONLY non-refuted lever for the india-owned PSN leg #10 (pool collapsed 62->13), grown CODE-SIDE ($0 GPU/MCP). `vault-to-gnn-refpool.mjs::extractConfirmedWirings` caught only **10 of 51** vault confirmed-wiring lines (fixed `<Engine>{0,40}wired...prism_X`, `[A-Z]`-anchored). Rewrote to anchor-on-assertion + nearest-preceding-Engine walk-back. **Live: 10 -> 16 (+6, +60%), 0 conflicts, all 16 manually validated correct.** 2 false-label P1s caught by per-file 2-arm scrutiny (arm-A FAILed then both PASS): clause cross-pair (`;`-bound when the clause has its own subject) + parenthetical helper (mask `(...)` unless bare-engine appositive; recovered the real PayrollLiability label) + a P2 nested-paren leak. 20 tests (was 9), all R9 fail-on-revert. Memory `reference_vault_refpool_broaden_2026_06_16`.
- **U-VAULT-REFPOOL-IDEMPOTENT: SHIPPED** (`e804997662`, cad-fusion-live-ms0) — the durability PREREQUISITE. `--apply` REPLACED existing ghost nodes (re-stamping volatile `proposed_at`) + ALWAYS wrote the 542MB graph → a periodic/post-regen re-apply would churn the graph AND the retrain drift fingerprint → spurious GPU retrains. Extracted pure `mergeVaultGhosts`/`nodeContentEqual` (proposed_at-insensitive content-equality); `--apply` now SKIPS the write when `!changed` ("UP-TO-DATE"). 8 new tests (28 total), 2-arm scrutiny PASS. Now a re-apply is a cheap no-op when current → any re-apply trigger is safe.
- **U-VAULT-REFPOOL-DURABLE: SHIPPED** (`6d962b37d3`) — the actual durability. Added a **pre-fingerprint stage 1a to `nn-graph-retrain-lifecycle.mjs`** (the `PRISM Nn Graph Retrain` cron) running `vault-to-gnn-refpool --apply` every retrain. PRE-fingerprint is correct (COUNT-based fingerprint + idempotent apply → 0 nodes in steady state → no spurious drift; POST would trip the ghost-delta gate). Clones the 2b/2c fail-soft pattern (opts.applyVaultRefpool||default, opt-out `PRISM_NN_RETRAIN_VAULT_REFPOOL_DISABLE`, telemetry, heap-bumped spawnSync). 6 tests (ordering lock + opt-out + fail-soft + non-zero + null-status/timeout R12), full suite 73/73, 2-arm scrutiny PASS (P2 null-status fix inline). **SCOPE (R12):** durable for the retrain/eval consumer only; other graph consumers in the post-regen/pre-tick window still see absent refs (graph-wide = a regen-viz-level re-apply, cross-lane). **The vault→GNN lever arc is COMPLETE: broaden → idempotent → durable.**
- **R15 SIBLING (scrutiny arm-B P2):** `scripts/ghost-wire-outcomes-to-refpool.mjs` (the clone-don't-fork twin, `ghost.outcome-wired.*` conf 0.8) STILL always-replaces/always-writes. Build-once fix: extract `mergeVaultGhosts`/`nodeContentEqual` to a shared `scripts/lib/refpool-merge.mjs`, both feeders import (NOT a 2nd clone-fork — R7/R8).
- **Still queued (unchanged):** durable vault-refpool re-apply (lifecycle stage, designed above) · R15 sibling idempotency (shared lib) · MIT-OCW materialization (M, source at `resources/MIT COURSES/`) · 19-galaxy `xproc_outcome_publish` (L, cross-galaxy) · galaxy-synthesis corpus growth (synthesis-refresh is fresh — needs more raw memories, not a re-run) · rsLoRA r=32 final (GPU, checkpoint-350 done) · operator/GPU items.
