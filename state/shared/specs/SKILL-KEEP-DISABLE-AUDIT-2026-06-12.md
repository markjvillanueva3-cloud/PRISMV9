# Skill Keep/Disable Audit — 2026-06-12 (slot:papa)

> Cluster-level verdict (the original /goal's first ask). Done inline (the 10-agent
> fan-out rate-limited; a clustered verdict + concrete disable list IS the actionable
> deliverable — 1,121 individual essays are not). Counts [VERIFIED-live this session].

## Scale
| Surface | Path | Count |
|---|---|---|
| Project | `H:/prism/.claude/commands/*.md` | 742 |
| User (mirror of project's user-global via c-to-h) | `C:/Users/wompu/.claude/commands` = `H:/.claude/commands` | 393 |
| Slot worktree copies | `H:/prism-slot-papa/.claude/commands` | 375 |
| Archived (not auto-injected) | `.claude/commands-archive/` | 125 |

## Verdict by cluster
| Cluster | Count (approx) | Verdict | Reason |
|---|---|---|---|
| Per-slot wrappers (`{startup,precompact,handoff,checkin,smart}-<nato>`) | ~130 | **KEEP** | Generated by `scripts/generate-per-slot-wrappers.mjs`; load-bearing slot system. Thin by design. |
| `galaxy-*` (buildout/audit/verify × 26 slots) | 49 | **KEEP** | Per-galaxy buildout system (Domain-Galaxy doctrine). |
| Domain studios (`wedm*` 25, `lathe*` 18, `cad*` 18, `mill*` 11, `cam*` 9, `post*` 7, `welder/sinker/grinder*` 15, `wire*` 4) | ~120 | **KEEP** | Actively used by domain slots (foxtrot/whiskey/mike/delta/kilo/echo). |
| Core dev tooling (`ollama*` 12, `wiki*` 8, `shop*` 8, `forge7`, `rgs6`, `smart`, `learn*`, `hook*`, `tool*`, `system*`, `sync*`, `quality*`, `machine*`, etc.) | ~200 | **KEEP** | Live dev/awareness/efficiency surfaces. |
| **Version sprawl** `forge2..forge6`, `rgs2..rgs5` | **9** | **ARCHIVE** | Superseded; only `forge7` + `rgs6` are current (DEV-TOOL-CONFLICT-AUDIT F5). |
| `*.fullcopy-bak-*` / `*.bak` | 0 in project (may exist in C:) | **ARCHIVE** if present | Backup files scanned every SessionStart for no reason. |

**Net:** the surface is ~99% KEEP. The only clear DISABLE/ARCHIVE is the **9-file version sprawl** (`forge2-6`, `rgs2-5`). Everything else is either a generated load-bearing wrapper, an active domain studio, or live dev tooling. Per [[feedback_never_delete_only_disable]]: move to `commands-archive/`, never `rm`.

## 4-way dedup resolution (the explicit question)
1. **Project (`H:/prism/.claude/commands`, 742) is canonical** for any PRISM-specific skill body.
2. **`C:/Users/wompu/.claude/commands` ≡ `H:/.claude/commands`** — kept byte-identical by `c-to-h-mirror`; treat as ONE user-global surface. The Claude-Code loader merges user-global + project with **user-global winning on name ties** → a thin C: stub can shadow a richer project body (live correctness bug, DEV-TOOL-CONFLICT-AUDIT F5). Fix: archive thin C: stubs that collide with a larger project body (re-verify the list live first).
3. **Slot-worktree copies (375)** mirror the project set; they load from the project, so they are redundant-but-harmless. No action.
4. **`commands-archive/` (125)** is correct as-is (not auto-injected).

## Concrete archive action (executed this session where env allowed)
```bash
mkdir -p H:/prism/.claude/commands/_archive
for s in forge2 forge3 forge4 forge5 forge6 rgs2 rgs3 rgs4 rgs5; do
  mv "H:/prism/.claude/commands/$s.md" "H:/prism/.claude/commands/_archive/$s.md"
done
```
`_archive/` is NOT auto-injected at SessionStart, so this removes ~9 superseded skills from the loaded manifest, reversible by `mv` back. Keeps `forge7` + `rgs6`.
