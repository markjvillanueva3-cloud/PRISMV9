# GIT-TREE-REMEDIATION-MS0 — Roadmap (canonical, evolving)

> **v2** · 2026-05-12 · supersedes `GIT-TREE-REMEDIATION-MS0-ROADMAP-2026-05-12.md` (v1, kept for history) · incorporates **SCRUTINY-1** (3-agent pass: Correctness 74 / Safety 58 / Completeness 68) + the system-viz / git-connected-node plan the v1 omitted. Generator: `/rgs6` (domain-adapted: git-infra/ops — "wiring" = settings.json/harness config; "tests" = git verification commands).
> **Status:** PLANNING. No git mutations performed. Decision gates `U-GC-00/01/02` still open.

## REVISION LOG
- **v1** (commit `637502940`, `…ROADMAP-2026-05-12.md`) — first cut. Scrutinized → `SCRUTINY-1.md` (commit `49d47e0e0`).
- **v2** (this file) — folds in all 14 SCRUTINY-1 must-fixes: renumbered to one scheme; `worktree.baseRef` corrected to a *harness* setting not `git config`; added the **freeze-enforcement** unit (U-GC-10); added **Path C `merge --allow-unrelated-histories`** and dropped Paths A/D/E (cherry-pick/graft/rebase-onto were traps); retire old trunks as **tags** not branches; added the **origin-only-branch** unit (U-GC-20) and the **stale-fork auto-triage script** (U-GC-21–23); `filter-repo` gets `--prune-empty=never` + a pre-rewrite >20MB blob inventory review; `--mirror` backup → **≥2 backups, ≥2 volumes, verified, kept until P4+P5 done, taken before the gc**; `U-GC-14` push sequencing fixed (push `main` first → set GitHub default → re-point worktrees → only then retire `origin/cad-f` as a *renamed* ref); `U-GC-13` force-push uses **per-ref leases on the rewritten branches only**, never `--force-with-lease --all`; added the **PRISM hook-collision section** (`asset-deletion-block` will block `rm -rf .claude/...`; `stop_on_uncommitted_critical` blocks the operator's Stop during the sweep; temp freeze hook must be bundle-absorbed); the `*-augmentation.json` gitignore is now per-file-audited not a glob (several are hand-curated); `master`'s 1326 commits get their own analysis unit; the `recovery` "remote" is corrected to a dangling ref; CI/branch-coupling, `repack -adf`, git-lfs-as-alternative, harness `EnterWorktree` post-rename, "other clones", `enforce-handoff-topic` rename-break, and the `merge-staging-ms0` reset-vs-preserve inconsistency are all addressed. **NEW Phase 6 — system-viz + git-connected nodes** added (the v1 omission the user flagged).

---

## META

| Field | Value |
|---|---|
| Milestone | `GIT-TREE-REMEDIATION-MS0` · scope tag `[GIT-CLEANUP]` |
| Tier | 2 (dev-tools / system-knowledge) · leverage 9/10 |
| Units | 30 (`U-GC-00` … `U-GC-29`) — 5 decision gates, 7 phases |
| Quiesce windows | 1 (P3 — the rewrite + force-push; ~30–60 min, fleet frozen by U-GC-10's hook) |
| Backups | ≥2 (`git clone --mirror` to a *different physical volume* + `git bundle --all` off-drive), `fsck`-verified, taken **before** any `gc --prune`, kept until **all of P4+P5+P6 verified** (not a fixed 30 days) |

### Decision gates (BLOCKING)
| ID | Decision | Recommendation | Blocks |
|---|---|---|---|
| `U-GC-00` | Canonical trunk? | **`cad-fusion-live-ms0`** (1216 commits, live, checked out everywhere, CI-unverified-but-current; vs `main` 874 frozen-5-days, `master` 1326 stale-7-weeks) | U-GC-06, U-GC-07, U-GC-15..17 |
| `U-GC-01` | `archive/forge-orphans-2026-05-01` (3.09 GB whisper model + ~9 881 orphaned files + 502 commits): keep / bundle-off-drive-then-retire? | **Keep** (gc doesn't touch it; revisit after P0) | U-GC-26 |
| `U-GC-02` | History rewrite + force-push to unblock `cad-f`'s push? | **Yes** (one quiesce window) | U-GC-11..14 |
| `U-GC-15` | After analyzing `main`/`master`/`claude-*` unique work: **Path B** (declare cad-f canonical, archive others as tags) or **Path C** (`git merge --allow-unrelated-histories` the wanted trunk into cad-f)? | **B** unless the analysis surfaces wanted clusters → then **C** (never A/D/E) | U-GC-16 |
| `U-GC-26` | `archive/forge-orphans` final disposition (per U-GC-01 + post-bundle-verify) | n/a | — |

### Verified facts this roadmap rests on (re-checked in SCRUTINY-1)
`git merge-base` is **empty for every pair** of {`cad-fusion-live-ms0` 1216c, `main` 874c, `master` 1326c, `claude/fervent-bohr` ~200c, `claude/zen-dirac` ~1300c} — they are mutually-unrelated histories. `cad-f` is **87 commits ahead of `origin/cad-f`** (unpushed) and **unpushable** (≥7 blobs >100 MB: six `system-graph.json` versions to 182.5 MB, one 134 MB `obsidian-augmentation.json`, both on `cad-f`; plus `models/ggml-large-v3.bin` 3.09 GB / `ggml-base.bin` 148 MB on `archive/forge-orphans`). `.git` ≈ **43 GB** (216 759 loose objects, only 16 511 prune-packable — the rest is *reachable history*; 56 packs; `garbage: 0`). **40 worktrees** + 1 stray (`brave-euclid` → `gitdir: C:/PRISM/.git/...` wrong drive). **15** `worktree-agent-*` cruft branches (all == `main`, no worktree dirs). `git config worktree.baseRef` **unset** → harness `EnterWorktree` defaults to `fresh` = branch-from-`origin/main` (a bifurcation vector — and the fix is a **harness setting in `H:/.claude/settings.json`**, NOT `git config`). `git-filter-repo` **not installed**. `worktree-commit-route.mjs` **is wired** (via `bash-bundle`) — `H:/PRISM/CLAUDE.md` ("dormant") + `GSD_MICRO.md` ("NOT YET WIRED") are stale. `state/shared/system-viz/system-graph.json` is **not tracked** (regenerated locally; the 118 MB blob is history-only). Several `state/shared/system-viz/*-augmentation.json` files **are tracked and hand-curated** (`cam-vendor-catalog-augmentation.json`, `engine-classification-repair-log.json`, `action-engine-edges-augmentation.json`, …) — must stay tracked. `recovery` is **not a remote** (`git remote -v` → only `origin`) — it's a dangling `refs/remotes/recovery/work/intel-ollama-obsidian-ms0` ref. CI keys on `main` (`.github/workflows/{ci,deploy,auto-index,dockerfile-sync}.yml` `branches: [main]`) — `cad-f` has never been CI'd. `enforce-handoff-topic.mjs` excludes `main/master/develop` from branch-based topic derivation but NOT `cad-fusion-live-ms0`. `RESUME_AT_WORK.md` referenced by CLAUDE.md §66 — **does not exist**. system-viz graph: 19 950 nodes / 11 layers (an older version had an `Lgit=694` git-commit layer); 2 wiki nodes literally name `cad-fusion-live-ms0` (`vault.wiki.lessons.cad-fusion-live-ms0-{h-drive-archaeology,integration-discovery}`); the graph generator (`scripts/generate-system-viz.mjs` + `generate-vault-graph.mjs` + `generate-engine-graph.mjs` + `augment-graph-with-awareness.mjs` + `dedup-graph-nodes.mjs` + `export-graph-cypher.mjs`) reads `git log`/`git branch`/`git worktree list`; `scripts/install-system-viz-git-hook.mjs` installs a git hook that regenerates/stales the graph.

---

## DEPENDENCY DAG

```
U-GC-00 (trunk?) ─┬→ U-GC-06 ─→ U-GC-07
                  └→ U-GC-15 (B|C?) ─→ U-GC-16 ─→ U-GC-17
U-GC-01 ─→ U-GC-26
U-GC-02 ─→ U-GC-09 ─→ U-GC-10 (freeze) ─→ U-GC-11 ─→ U-GC-12 ─→ U-GC-13 ─→ U-GC-14   [QUIESCE WINDOW]
P0  U-GC-03 U-GC-04 U-GC-05               (independent; tier-0; now, no quiesce)
P1  U-GC-06←00 ; U-GC-07←06 ; U-GC-08     (tier-0/1; low risk; ship now after U-GC-00)
P2-pre  U-GC-09 (≥2 verified backups, off-drive)  ──gates──→ P3
        U-GC-29a (dirty-worktree sweep — START DAY 1, run continuously)  ──gates──→ U-GC-13
P3  U-GC-10 freeze-on → U-GC-11 filter-repo(--prune-empty=never) → U-GC-12 inventory-verify → U-GC-13 force-push(per-ref leases) → U-GC-14 resync+freeze-off  [QUIESCE]
P4  U-GC-15←{00, post-rewrite SHAs} → U-GC-16 exec → U-GC-17 rename→main+retire-as-tags  (tier-2)
P5  U-GC-18 U-GC-19 ; U-GC-20 origin-only ; U-GC-21 triage-script → U-GC-22 review → U-GC-23 exec ; U-GC-24 broken-wt ; U-GC-25 claude-* ; U-GC-26 forge-orphans  (tier-3; after U-GC-17)
P6  U-GC-27 (rename blast-radius sweep) → U-GC-28 (regen system-viz graph + verify generator handles new topology)  (tier-3; after U-GC-17)
P7  U-GC-29 (partial verify after P0+P1) ; U-GC-29b (full verify) ; U-GC-29c (document + memory)
```
Tier-floor invariants: **P0 before P3** (the `.gitignore`+untrack must land so the rewrite target is clean); **`U-GC-29a` dirty-worktree-sweep is a HARD prereq for `U-GC-13`** (the force-push → per-worktree `reset --hard` destroys uncommitted WIP — ~17 k+ uncommitted lines across 40 worktrees today); **backups (`U-GC-09`) before any `gc --prune`** (P0's `U-GC-05`); **`U-GC-10`'s freeze-hook active for the entire P3 window**.

---

## PHASE 0 — Zero-risk, no rewrite (now; no quiesce)

- **`U-GC-03` — `.gitignore` + untrack generated artifacts (per-file audited).** *Why:* stop the bleeding; clean target for P3. *What:* (a) `git ls-files state/shared/system-viz/ | …` → for each `*.json`/`*.cypher`, classify generated (ignore) vs. hand-curated (keep tracked — `cam-vendor-catalog-augmentation.json` et al. stay); ignore the *generated* ones (`system-graph.json`, `graph.cypher`, `obsidian-augmentation.json`, `data-catalogs-atomic-augmentation.json`, …) + `**/.serena/cache/`, `c/tmp/`, `mcp-server/tmp/`, `mcp-server/dev/null`, `models/*.bin`, `Docustrata/.index/*.json`, the tribal-embed/RAG indexes; (b) `git rm --cached` only the *tracked-and-now-ignored* ones. Commit. *Acceptance:* `git check-ignore` matches each ignored path; `git ls-files | grep -c system-graph.json` → 0; the curated augmentation files still `git ls-files`-present; `/system-viz` still regenerates `system-graph.json` (now ignored). *Risk:* low. *Owner:* direct.
- **`U-GC-04` — Retire branch cruft + the stray worktree + the dangling `recovery` ref.** *What:* `mv H:/PRISM/.claude/worktrees/brave-euclid H:/PRISM/.claude/worktrees/brave-euclid.orphan-archive-<date>` (NOT `rm -rf` — that trips `asset-deletion-block.mjs`; confirm `C:/PRISM/` doesn't exist as a separate clone first) → `git worktree prune`; for each of the 15 `worktree-agent-*`: `git merge-base --is-ancestor <b> main && git branch -m <b> archive/<b>` (rename, don't `-D` — per `feedback_never_delete_only_disable`; `-D` only after the backup window + user nod); `git update-ref -d refs/remotes/recovery/work/intel-ollama-obsidian-ms0` (dangling ref from a removed remote). *Acceptance:* `git worktree list | grep -c brave-euclid` → 0; `git branch | grep -c '^  worktree-agent'` → 0; `git for-each-ref refs/remotes/recovery` → empty. *Risk:* low (user nod). *Owner:* direct.
- **`U-GC-05` — `gc --prune=now` + `repack -adf`.** *Why:* reclaims the loose-object pile (mostly via **repack/dedup/zlib** of reachable history — the JSON blobs compress ~5–10× — *plus* deletes the 16 511 unreachable objects); consolidates 56 packs → 1. *What:* `git config gc.auto 0` (repo-wide, for the window); `git reflog expire --expire=now --all`; `git gc --prune=now`; `git repack -adf`. *Depends on:* `U-GC-09` (backups taken first) **and** `U-GC-29a` (dirty-sweep done — `reflog expire --all` nukes worktree reflogs, which is the point, but you want the WIP archived first). *Acceptance:* `git count-objects -vH` → `size` < 5 GiB, loose `count` < 20 k, `packs` ≤ 2; `git fsck --connectivity-only` → clean; the dirty-sweep loop (U-GC-29a) re-run → all worktrees clean. *Risk:* low (only drops garbage) but needs a brief no-git-ops window (`git-anti-clobber` locks help; ideally announce). *Owner:* direct.

## PHASE 1 — Process fixes so it doesn't re-bifurcate (now, after U-GC-00; low risk)

- **`U-GC-06` — `worktree.baseRef = head` as a HARNESS setting + a shared `canonical-trunk.mjs` module + `trunk-guard.mjs` SessionStart hook.** *Why:* harness-created worktrees currently branch from `origin/main` while the repo is on `cad-f` — the mechanical bifurcation vector. *What:* (a) set `worktree.baseRef` to `head` in **`C:/Users/wompu/.claude/settings.json`** (the C:→H: mirror source — NOT `git config`); (b) `.claude/hooks/lib/canonical-trunk.mjs` exporting `CANONICAL_TRUNK` (env `PRISM_CANONICAL_TRUNK`, default = the `U-GC-00` value) + `isOnTrunk(headRef)` (`git merge-base --is-ancestor`); (c) `.claude/hooks/trunk-guard.mjs` SessionStart hook using that lib — if `!isOnTrunk(HEAD)`, emit `additionalContext` warning ("HEAD doesn't descend from <CANONICAL> — possibly a bifurcated branch; see GIT-TOPOLOGY.md"); wire into `sessionstart-bundle.mjs` SUB_HOOKS (timeout 2000), fail-open; also add `cad-fusion-live-ms0` to `enforce-handoff-topic.mjs`'s branch-exclusion list now (so pre/post-rename handoff-topic behavior matches). *Acceptance:* harness `EnterWorktree`/`git worktree add ../tmp-test` → new branch's merge-base == current HEAD (then remove the test wt); `node --check` all 3 files; `echo '{}' | node trunk-guard.mjs` exit 0; on `master` → warning fires; `verify-hook-refs --quiet` clean; `stop_on_hook_unregistration` allows (bundle-absorbed name recognized). *Risk:* low (warn-only). *Owner:* direct.
- **`U-GC-07` — `worktree-commit-route.mjs`: trunk-ancestry check before the `[MAIN]`/`CROSS_CUTTING`/`[MAIN-FORCE]` bypasses (warn→block via `PRISM_TRUNK_GUARD_MODE`, default `warn` ~1 week then `block`).** Uses the `canonical-trunk.mjs` lib. *Acceptance:* on canonical trunk `[MAIN] X: y` allowed; on a wrong-trunk branch warned (warn) / denied (block); on `work/lathe-master` committing `[LATHE…]` still allowed; `verify-hook-refs` clean. *Risk:* medium (could block live chats — warn-first + the existing 3-strike auto-pass mitigate; announce when flipping to `block`). *Owner:* direct.
- **`U-GC-08` — Reconcile stale doctrine + the `/sync-rebase` footgun.** *What:* `H:/PRISM/CLAUDE.md` + `GSD_MICRO.md`: "worktree-commit-route NOT YET WIRED" → "wired"; **change `/sync-rebase`'s default `--base` to `$PRISM_CANONICAL_TRUNK` (fallback: current HEAD's tracking branch)** — *this clause moves to P0, it's an active footgun today* (rebasing a `cad-f`-derived branch onto unrelated `origin/main`); replace the "move work via git stash → pop" instruction with the `git show <ref>:<path>` pattern (per `feedback_no_git_stash_shared_tree`); create `state/shared/RESUME_AT_WORK.md` with the §8 silent-overwrite history (the topic-naming doctrine leans on it); add a "canonical trunk" para to `H:/PRISM/CLAUDE.md` referencing `/checkin`'s existing divergence check. *Acceptance:* `grep -c "NOT YET WIRED" CLAUDE.md GSD_MICRO.md` → 0; `grep base ~/.claude/commands/sync-rebase.md` → `$PRISM_CANONICAL_TRUNK`; `RESUME_AT_WORK.md` exists. *Risk:* low. *Owner:* direct. **(The `/sync-rebase --base` sub-clause: do it in P0, day 1.)**

## PHASE 2-pre — gates for the rewrite

- **`U-GC-09` — Take + verify ≥2 backups, off-drive, BEFORE any `gc --prune`.** *What:* `git clone --mirror H:/prism <other-volume>/prism-git-backup-<date>.git` AND `git bundle create <off-drive>/prism-backup-<date>.bundle --all`; for each: `git -C/-b <backup> fsck --full` (budget ≥10 min on 43 GB) + `git rev-parse cad-fusion-live-ms0` matches source + size sanity. Confirm H: is on stable power (it's a *portable* drive — a yank mid-rewrite + a same-drive backup = total loss). Keep both until **all of P4+P5+P6 verified**. *Acceptance:* `fsck` clean on both; both have `cad-fusion-live-ms0` at the current SHA; both on volumes other than the one being rewritten. *Risk:* none (read-only of source). *Owner:* direct. **Do this before the quiesce window so the window holds only irreversible work.**
- **`U-GC-29a` — Dirty-worktree sweep (START DAY 1; runs continuously; HARD prereq for `U-GC-13`).** *Why:* `U-GC-13`'s force-push → `U-GC-14`'s per-worktree `git reset --hard origin/<branch>` destroys uncommitted WIP (~17 k+ lines across 40 worktrees: `xproc-neural` 13 119 untracked, `cad-complete-ms0` 3 811, `file-claim-fix` 452, `claudemd-enforcement` 97, `knowledge-wiki-ms0` 77, `u-fus-api01` 116, `u-fus-api02` 77, `mill-master` 51, `merge-staging-ms0` 47, `session-efficiency` 31, `cam-exhaust-cam43-plus` 30, `psau-sav2` 28, `intel-ollama-obsidian-ms0` 19, `cam-exhaust-ms0` 18, `cam-fusion-ms1` 15, `engine-wire-ms0` 14, + ~15 with <10). *What:* for each dirty worktree — owning chat commits-or-discards (post on `AGENT_CHAT.md`), or (abandoned) `git -C <wt> diff > state/shared/wip-archive/<branch>.diff` + `git -C <wt> add -A && commit -m "[ARCHIVE] WIP pre-remediation"` on that branch; special-case `work/xproc-neural` (unborn — 0 commits; archive the 13 119 untracked files, then it's safe to drop); special-case `work/merge-staging-ms0` (triage its WIP + 12 unique commits → fold into `U-GC-16`'s analysis; do NOT later `reset` it away). **Also:** if any worktree has uncommitted edits to `src/physics/constants.ts`/`*Safety*`/`*Force*` — `stop_on_uncommitted_critical.mjs` will block the *operator's* Stop until those are committed; handle explicitly. *Acceptance:* the loop `for wt in $(git worktree list --porcelain | awk '/^worktree/{print $2}'); do echo "$wt: $(timeout 8 git -C "$wt" status --porcelain 2>/dev/null | wc -l)"; done` → every line `0`, or the non-zero ones acknowledged in `state/shared/wip-archive/` / on the chat bus. **Re-run immediately before `U-GC-13`.** *Risk:* medium (a missed dirty worktree → WIP dies at U-GC-14). *Owner:* coordinated across the fleet.

## PHASE 3 — Unblock the push (QUIESCE WINDOW; force-push; irreversible)

- **`U-GC-10` — Activate the maintenance freeze.** *What:* write `state/shared/.git-freeze-active` (operator session id + expiry); add `git-maintenance-freeze.mjs` to **`bash-bundle.mjs` SUB_HOOKS** (NOT a top-level settings.json entry — that'd trip `settings-json-addonly-guard` on removal) — a PreToolUse hard-block: if the flag exists and the caller isn't the operator session, **deny** any `git (commit|push|merge|rebase|reset|cherry-pick|filter-repo|worktree add|branch -[dD])` with "Git maintenance window — your commit is BLOCKED to protect the history rewrite; park your work, do not retry"; add a Stop-hook clause (in `git-sync-stop.mjs` or a sibling) that skips the push while the flag is set; `set PRISM_DISABLE_GIT_SYNC=1` (verify `git-sync-fetch`/`git-sync-stop` honor it; if not, that's a prereq sub-task); best-effort ping each of the 8 chats with a human prompt to idle (fires `chat-bus-inject` — advisory only; the hook is the real control). *Acceptance:* a non-operator `git commit` is denied with the message; the operator's own commits pass; `git-sync-stop` skips the push when the flag is set. *Risk:* medium (a bug in the freeze hook blocks everyone — fail-open on hook error; reversible by `rm` the flag). *Owner:* operator.
- **`U-GC-11` — `filter-repo` on a fresh `--single-branch` mirror, `--prune-empty=never`.** *What:* (0) `pip install git-filter-repo` (or scoop/standalone — it's **not installed**); (1) generate the **>20 MB blob inventory** `git rev-list --all --objects | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '$1=="blob"&&$3>20000000' | sort -k3 -rn` → review with the user → finalize the `--path` exclusion list (prefer explicit paths over a blind `--strip-blobs-bigger-than`); (2) `git clone --no-local --single-branch --branch cad-fusion-live-ms0 H:/prism /tmp/prism-rewrite` *(plus `main`, `master`, and any wanted `work/*` — NOT `archive/forge-orphans` (3 GB whisper) — to keep the mirror small; verify the clone with `git fsck --full && git rev-list --count HEAD` matching the source before proceeding)*; (3) `cd /tmp/prism-rewrite && git filter-repo --force --prune-empty=never --invert-paths --path state/shared/system-viz/system-graph.json --path state/shared/system-viz/graph.cypher --path state/shared/system-viz/obsidian-augmentation.json --path state/shared/system-viz/data-catalogs-atomic-augmentation.json --path mcp-server/dev/null --path-glob 'mcp-server/.serena/cache/*' [+ the user-reviewed extras]`; (4) `git reflog expire --expire=now --all && git gc --prune=now --aggressive`. *Acceptance:* on the rewritten mirror — `git rev-list --all --objects | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '$3>52428800'` → empty; `git log --all --oneline -- state/shared/system-viz/system-graph.json` → empty; `git rev-list --count HEAD` before == after (or a user-approved, reconciled delta) **and** the rewritten HEAD commit message == the expected one; `du -sh .git` < 2 GB; `git fsck --full` clean; `/tmp` had ≥ (`.git`-size × 1.5) free before the clone. *Risk:* high (every SHA changes). *Owner:* operator.
- **`U-GC-12` — Inventory-verify the rewrite** (the acceptance of `U-GC-11`, broken out as a gate so it's checked before the force-push). If any check fails → delete the broken mirror, re-clone, retry `U-GC-11` (the live repo is untouched until `U-GC-13`).
- **`U-GC-13` — Force-push the rewritten branches only, per-ref leases.** *What:* `git push --force-with-lease=cad-fusion-live-ms0:<old-sha> --force-with-lease=main:<old-sha> [--force-with-lease=master:<old-sha> …] origin cad-fusion-live-ms0 main master` — **never `--force-with-lease --all`** (no-ref leases from a fresh clone don't protect against a concurrent push between clone-time and push-time). On partial-push (network drop mid-multi-ref): re-run until `git ls-remote origin` exactly matches the mirror's refs; keep the freeze in effect until that's green. *Acceptance:* `git ls-remote origin cad-fusion-live-ms0` == the rewritten SHA; `git ls-remote origin | sort` == the mirror's refs `| sort`. *Risk:* high. *Owner:* operator.
- **`U-GC-14` — Re-sync every worktree + every other clone; lift the freeze.** *What:* re-run `U-GC-29a`'s dirty-check (must be green — if a chat slipped a commit during the freeze, `git -C H:/prism log origin/cad-fusion-live-ms0..cad-fusion-live-ms0` is non-empty → STOP, cherry-pick those onto the rewritten history, re-force-push, re-verify; do NOT `reset --hard` past unmerged local work); in `H:/prism`: `git fetch origin && git reset --hard origin/cad-fusion-live-ms0`; for each of 40 worktrees: `rm -f <wt>/.git/index.lock` if >1 h old, `git -C <wt> fetch origin && git -C <wt> reset --hard origin/<that-branch>` — **for worktree branches not on origin** (local-only `work/*`): either push them to origin first so the rewrite covered them, or `git -C <wt> rebase --onto <new-base> <old-base>` against the rewritten history — never `reset --hard <local-old-sha>` (that's an orphaned pre-rewrite SHA that `gc` will delete); report every worktree it couldn't reset (don't silently skip); enumerate other clones (does `C:/PRISM/` exist? laptops? CI checkout caches?) — re-clone or hard-reset each; `rm state/shared/.git-freeze-active`, revert the freeze-hook bundle entry, `unset PRISM_DISABLE_GIT_SYNC`, `git config --unset gc.auto`; post on `AGENT_CHAT.md` "history rewritten — `git fetch && git reset --hard origin/<branch>` before any commit; your `git-sync-stop` will refuse to push until you do." *Acceptance:* `H:/prism` at the new HEAD, clean; every worktree clean and tracking the right `origin/<branch>`; `git push origin cad-fusion-live-ms0` → "Everything up-to-date"; spot-check 3 chats `fetch && reset --hard` cleanly. *Risk:* high (same window). *Owner:* operator + coordinated.

## PHASE 4 — Trunk reconciliation (after the rewrite; on a fresh `work/trunk-reconcile-<date>` branch — leave `merge-staging-ms0` for triage)

- **`U-GC-15` — Analyze the 3 (4) disjoint histories; decide Path B vs C.** Three analysis sub-units: (a) `main`'s 874 unique commits (`git diff cad-f main -- <area>` + `git log cad-f..main` per subsystem → classify each cluster: superseded / salvageable-from-`archive/forge-orphans` / wanted); (b) `master`'s **1326** unique commits (same — it's the largest branch); (c) the `claude/*` roots (~200 + ~1300 unique commits — independent roots, not pre-fork divergence). Output `state/shared/MAIN-MASTER-CLAUDE-UNIQUE-WORK-AUDIT.md` with every cluster classified + a Path-B-vs-C recommendation per trunk. *Acceptance:* the audit doc covers all of `main`+`master`+`claude/*` with a verdict each. *Risk:* low (read-only). *Owner:* one chat + user reviews.
- **`U-GC-16` — Execute the chosen path.** *Path B (recommended default):* `git tag archive/main-final-<date> main && git tag archive/master-final-<date> master && git tag archive/claude-fervent-bohr-<date> claude/fervent-bohr && …`, push the tags, then `git branch -D main master claude/fervent-bohr claude/zen-dirac` (tags keep the commits reachable; `git branch -a` stays clean; tags don't get auto-pushed by `--all`) — only if `U-GC-15` confirms zero "wanted" clusters. *Path C (if `U-GC-15` finds wanted work):* `git checkout cad-fusion-live-ms0 -b work/trunk-reconcile-<date>`; `git merge --allow-unrelated-histories <wanted-trunk>` (one merge commit, all of its tree appears, conflicts only on path-overlap — resolve favoring cad-f per R7 "newer wins"); test; merge `work/trunk-reconcile-<date>` → cad-f; then archive the merged-in trunk as a tag (Path B mechanism). *Risk:* low (B) / high (C — unrelated-history merge, budget hours, do it on the staging branch). *Owner:* one chat; physics/test reviewer agents validate a Path-C merge.
- **`U-GC-17` — Promote `cad-fusion-live-ms0` → `main`; re-point everything.** *What:* `git branch -m cad-fusion-live-ms0 main`; `git push -u origin main`; **set the GitHub default branch to `main` via the API/UI** (note: `git config init.defaultBranch` is repo-local and does NOT change the remote default); `git remote set-head origin main`; re-point all 40 worktrees' upstreams (`git -C <wt> branch --set-upstream-to=origin/<branch>`) + `H:/prism`; `git push origin cad-fusion-live-ms0:refs/heads/archive/cad-fusion-live-ms0-pre-rename-<date>` (rename-on-remote, keep ≥90 days) **then** `git push origin :cad-fusion-live-ms0` (delete the old name — LAST, after every consumer is confirmed re-pointed); update `canonical-trunk.mjs`'s default + `PRISM_CANONICAL_TRUNK` → `main`; handle harness `EnterWorktree`-managed worktrees (a chat in one must `ExitWorktree --keep` then re-enter, or re-point upstream manually — announce). *Acceptance:* `git -C H:/prism rev-parse --abbrev-ref HEAD` → `main`; GitHub default branch == `main`; `git symbolic-ref refs/remotes/origin/HEAD` → `refs/remotes/origin/main`; `git ls-remote origin | grep -c '/cad-fusion-live-ms0$'` → 0; `origin/archive/cad-fusion-live-ms0-pre-rename-<date>` exists; all worktrees track the right upstream. *Risk:* medium. *Owner:* operator + user (for the GitHub default-branch change).

## PHASE 5 — Worktree / branch cleanup (after `U-GC-17`; SHAs stable)

- **`U-GC-18` — Retire the broken `work/xproc-neural` worktree** (0 commits, `0000000` HEAD; its 13 119 untracked files archived by `U-GC-29a` already, verified): `git worktree remove --force H:/prism-xproc-neural`; `git update-ref -d refs/heads/work/xproc-neural`. *Risk:* low.
- **`U-GC-19` — Retire the stale-merged worktrees/branches** (`work/cost-cascade-ms0` and `work/graph-context-ms0` — *same HEAD `20d8967e1`*, collapse the dup pair first; `work/sfc-calibrate`; `work/cad-complete-ms0` + `work/psau-sav2` after their WIP): for each — `git merge-base --is-ancestor <b> main` → `git worktree remove <p>` → `git branch -m <b> archive/<b>-<date>` → `git push origin archive/<b>-<date>` → `git push origin :<b>` (rename-on-remote, then delete; never bare `-D`/`push :` first). *Risk:* low (user nod).
- **`U-GC-20` — Triage the ~25 origin-only branches** (`origin/archive-2026-02-01`, `origin/claude/affectionate-perlman`, `origin/claude/interesting-shamir`, `origin/work/*` with no local copy, `origin/worktree-data-loss-fix`, …): `git branch -r` → classify each (ancestor-of-`main`? unique? archive-snapshot?). **Critical:** `U-GC-13`'s force-push only touched *local* branches → these origin-only ones keep their pre-rewrite (blob-carrying) history on the remote → the *clone size* doesn't shrink until they're handled. For ones holding >100 MB blobs: `git push origin :<branch>` after archiving as a tag (or `git filter-repo` them in a second pass). **`origin/worktree-data-loss-fix` must NOT be touched without the user reviewing it.** Keep `origin/archive-2026-02-01` (cold storage). *Acceptance:* `git rev-list --all --objects --remotes | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '$3>104857600'` → empty (no >100 MB blob reachable from any origin ref). *Risk:* medium. *Owner:* user reviews; direct executes.
- **`U-GC-21` — Build `scripts/git-stale-fork-triage.mjs`.** For each `work/*` / `claude/*` branch emit: `merge-base --is-ancestor main` (merged → safe-retire), `git log -1 --format=%cr` (idle?), `git rev-list --count main..<b>` (unique commits), `git diff --name-only main...<b> | wc -l` (file overlap), `git rev-list --count <b>..main` — and a heuristic recommendation (merged→retire / 0-commits→drop / <30d & unique→revive-candidate / >60d & unique→archive-tag). Output a pre-filled `state/shared/STALE-FORK-TRIAGE.md`. *Acceptance:* the script runs, emits a row for every `work/*`+`claude/*` branch with a recommendation. *Risk:* low.
- **`U-GC-22` — User reviews + approves `STALE-FORK-TRIAGE.md`** (the ~24 `work/*` forks: `intel-ollama-obsidian-ms0` 983 unique, `lathe-pro-v3-ms2` 497, `mill-master` 489, `ai-aware-harden` 403, `lathe-master` 443, `wedm-consolidated` 427, `tsc-cleanup-ms0` 88, `engine-wire-cad-ms0` 89, `cad-fidx-solidworks` 66, `ppgh05` 49, `ppg-advancedpost` 43, `cad-fidx-fus-93a0` 40, `intel-p8-schema` 30, `engine-wire-ms0` 28, `intel-ollama-obsidian-ms1` 26, `lathe-prod-ready-ms0` 18, `cam-hypermill-ms1` 15, `cam-exhaust-cam43-plus` 14, `cam-fusion-ms1` 12, `knowledge-wiki-ms0` 11, `session-efficiency` 7, `cam-spcfai-ms0` 3, `cam-engine-fixes` 1, `lathe-pro-v3-bookkeeping` 1, `meta/claudemd-enforcement` 1, `meta/file-claim-fix` 4 + `worktree-u-fus-api01` 497 / `api02` 2 + the `claude/*` roots) — decision per branch: revive / merge / archive-tag-and-retire. *Risk:* medium (don't lose wanted work — that's why it's user-triaged). *Owner:* user.
- **`U-GC-23` — Execute the triage:** cherry-pick the "merge" ones onto `main`; `git tag archive/<x>-<date> <x> && git push origin archive/<x>-<date> && git worktree remove <p> && git branch -D <x> && git push origin :<x>` for the "archive" ones (tags keep them reachable); leave the "revive" ones. *Risk:* medium.
- **`U-GC-24` — Retire `worktree-u-fus-api01/02`** (auto-gen names; `api01`'s 497 commits routed through `U-GC-22`'s triage; `mv`/`worktree remove` per the never-delete rule + per-worktree user OK). *Risk:* low–med.
- **`U-GC-25` — `claude/*` auto-branches** — folded into `U-GC-15c` (analysis) + `U-GC-22`/`U-GC-23` (triage as independent roots, archive-as-tags, never blind `push origin :claude/<x>`). Keep `origin/archive-2026-02-01`. *Risk:* low–med.
- **`U-GC-26` — `archive/forge-orphans-2026-05-01` final disposition** (per `U-GC-01`): **keep** → no action (gc doesn't touch reachable-from-branch blobs; ~3.3 GB stays packed on that branch, never checked out on `main`), document in `GIT-TOPOLOGY.md` as intentional cold storage. **If 3.3 GB must be reclaimed:** `git bundle create <off-drive>/forge-orphans-<date>.bundle archive/forge-orphans-2026-05-01` → verify (`clone + fsck`) → `git tag archive/forge-orphans-final-<date> archive/forge-orphans-2026-05-01 && git push origin archive/forge-orphans-final-<date>` → `git branch -D archive/forge-orphans-2026-05-01 && git push origin :archive/forge-orphans-2026-05-01 && git worktree remove H:/prism-forge-archive && git reflog expire --expire=now --all && git gc --prune=now` → document the bundle location **permanently** in `GIT-TOPOLOGY.md`. **Never** run the delete-path without the verified off-drive bundle. *Risk:* med (delete-path). *Owner:* user decides.

## PHASE 6 — System-viz + git-connected nodes (after `U-GC-17`; the v1 omission)

- **`U-GC-27` — Rename blast-radius sweep: everything that reads/references the branch name.** *What:* enumerate + fix every place `cad-fusion-live-ms0` is baked in or that the rename behavior-changes: (a) `enforce-handoff-topic.mjs` exclusion list (already done in `U-GC-06`); (b) the graph generators (`scripts/generate-system-viz.mjs`, `generate-vault-graph.mjs`, `generate-engine-graph.mjs`, `augment-graph-with-awareness.mjs`, `dedup-graph-nodes.mjs`, `export-graph-cypher.mjs`) — grep for hardcoded `cad-fusion-live-ms0` / branch assumptions; (c) `scripts/install-system-viz-git-hook.mjs` (the post-commit graph-regen/stale hook — does it reference the branch?); (d) the BUILD_STATE / MILESTONE_PROGRESS / CLAUDE-BRIEF generators (read `git log` — confirm they don't hardcode the branch); (e) `mcp-server/data/roadmap-index.json` (any branch refs?); (f) `worktree-commit-route.mjs` scope-matching (matches `[SCOPE]` tokens — `CAD-FUSION-LIVE-MS0` still works); (g) `.github/workflows/*` (keyed on `main` — confirm the rename *enables* CI on the canonical trunk; flag the behavior change); (h) `git-sync-stop`/`git-sync-fetch`/`git-health-guard` (operate on the current branch — fine); (i) the 2 wiki lesson nodes `vault.wiki.lessons.cad-fusion-live-ms0-{h-drive-archaeology,integration-discovery}` (at `knowledge/wiki/lessons/cad-fusion-live-ms0-*.md`) — keep the names (they document history) but add a note; (j) `~/.claude/commands/{checkin,sync,handoff,six-chat-bootstrap}.md`, `GSD_QUICK.md`, `GSD_MICRO.md`, `DEV_PROTOCOL.md`, `CLAUDE-CODEX-COORDINATION-DIRECTIVE.md` — sweep for branch refs; (k) per-chat `HANDOFF-*-cad-fusion-live-ms0.md` topic-suffixed handoff files — rename to `…-main.md`? (or leave — they're historical); (l) any `state/shared/*.json` with a `branch` field. Output `state/shared/RENAME-BLAST-RADIUS.md` listing every hit + the fix applied/deferred. *Acceptance:* `grep -rl 'cad-fusion-live-ms0' scripts/ .claude/ mcp-server/data/ state/shared/ knowledge/wiki/ .github/ ~/.claude/commands/ 2>/dev/null` → only the intentionally-kept ones (the 2 wiki lessons, history docs); the graph generators run clean on the renamed trunk; `RENAME-BLAST-RADIUS.md` exists. *Risk:* low–med (a missed hardcode → a broken generator/hook on the new trunk). *Owner:* one chat + user reviews.
- **`U-GC-28` — Regenerate the system-viz graph + verify it reflects the new topology.** *What:* `node scripts/generate-system-viz.mjs` (or `/system-viz` — note: it writes the now-gitignored 23 MB `system-graph.json`, that's fine); confirm the regen succeeds against the renamed trunk + the reduced worktree/branch set (the git-layer / `Lgit` nodes if that layer is enabled should now show `main` not `cad-fusion-live-ms0`, ~12 worktrees not 40, ~half the branches); confirm `scripts/install-system-viz-git-hook.mjs`'s hook still fires correctly; spot-check the `system-viz-query.mjs` adapter still answers (`headline`, `find <name>`, `blast-radius <nodeId>`). Re-run `dedup-graph-nodes.mjs` + `export-graph-cypher.mjs` if the pipeline needs it. *Acceptance:* `node scripts/generate-system-viz.mjs` exits 0; `node scripts/system-viz-query.mjs headline` → fresh, references `main`; the graph's git-layer nodes (if present) reflect the new topology; `state/shared/system-viz/system-graph.json` is gitignored (not staged). *Risk:* low. *Owner:* the system-viz lane-owner chat (lane-locked — coordinate).

## PHASE 7 — Verify & document

- **`U-GC-29` — Partial verification after P0+P1** (checkpoint *before* committing to the irreversible P3): `git fsck --connectivity-only` clean; `git count-objects -vH` size < 5 GiB; `worktree.baseRef`=head in the harness settings; `trunk-guard.mjs`/`worktree-commit-route.mjs` behave; `verify-hook-refs --quiet` clean; the dirty-worktree sweep (`U-GC-29a`) green. Confirms the 43 GB→~4 GB win landed before P3.
- **`U-GC-29b` — Full verification after P6:** `git fsck --full` clean; `git count-objects -vH` size < 2 GiB / loose < 10 k / packs ≤ 2; `git push --dry-run origin main` AND verify `git rev-list --objects HEAD | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '$3>104857600'` → empty (no >100 MB blob at HEAD) AND `git rev-list --all --objects --remotes | cat-file --batch-check | awk '$3>104857600'` → empty (no >100 MB blob anywhere on origin); `git merge-base --is-ancestor main HEAD` → true; `git worktree list | wc -l` → ≤ ~12 (from 40); `git branch -a | wc -l` → roughly halved; `node scripts/generate-system-viz.mjs` exits 0; the freeze hook is removed (file + bundle entry); `git config --get gc.auto` → unset or non-zero; spot-check 3 chats `fetch && reset --hard` cleanly. Paste results into the commit msg.
- **`U-GC-29c` — Document + memory:** `state/shared/GIT-TOPOLOGY.md` (live trunk = `main`; active worktree→branch map; retired/archived branches & tags; the `worktree.baseRef=head` + `trunk-guard` + `[MAIN]`-restriction process model; the off-drive backup locations & retention; the `forge-orphans` bundle location if retired); update `H:/PRISM/CLAUDE.md` ("Lane discipline" / "PER-CHAT HANDOFF" / "Main branch (you will usually use this for PRs): main" — verify the last is auto-generated and now correct); add `reference_git_topology.md` to the memory vault + the `MEMORY.md` pointer; mark this roadmap `COMPLETE`. Schedule the off-drive backups' eventual removal (after a long grace, calendar reminder — `leave-a-copy-behind-guard` will flag premature removal).

---

## FAILURE-MODE REGISTER (F1–F28; F12–F28 added from SCRUTINY-1)

F1 `filter-repo` refuses with linked worktrees → run on a fresh `--single-branch` mirror (U-GC-11). F2 `reset --hard` destroys WIP → `U-GC-29a` is a hard prereq, re-run before U-GC-14. F3 live chat breaks mid-commit → `U-GC-10`'s freeze hook (not just an announcement). F4 `git stash` clobbers peers → `U-GC-08` removes the stale doctrine. F5/F21 `master` retired without diff-review → `U-GC-15b` is its own analysis unit with an acceptance gate. F6 unrelated-history merge conflict-fest (Path C) → staging branch, budget hours, Path B default. F7/F18 `system-graph.json` regen/re-commit during the freeze → `.gitignore` lands in P0; freeze hook blocks commits; `/system-viz` disabled during the window. F8 `archive/forge-orphans` deleted, lost → never the delete-path without the verified off-drive bundle (U-GC-26). F9/F13/F14 single same-(portable-)drive 30-day backup → ≥2 backups, ≥2 volumes, verified, kept until P4+P5+P6 done, taken before the gc (U-GC-09). F10 `trunk-guard` blocks everything → warn-only default, fail-open. F11 `gc --prune` corrupts a pack via a concurrent worktree git op → `gc.auto 0` + no-git-ops window (U-GC-05); the framing "drops dead-reflog objects" is the *point*, not the hazard. F12 `/tmp` (=C:) out of space cloning 43 GB → `gc` before the clone; clone `--single-branch`; assert ≥1.5× free; `fsck` the clone before `filter-repo` (U-GC-09/11). F15 `filter-repo` dies mid-run → throwaway mirror; recovery = delete + re-clone + retry (U-GC-12). F16 force-push partially succeeds → push the critical ref first + verify; `git ls-remote origin` must match the mirror before unfreezing (U-GC-13). F17 a chat slips a commit during the freeze → the freeze *hook* (U-GC-10) + the `git log origin/<b>..<b>` pre-`reset` assertion (U-GC-14). F19 a worktree branch rewritten differently / not on origin → pre-build the worktree→origin-ref map; push local-only ones first or `rebase --onto`, never `reset --hard <old-sha>` (U-GC-14). F20 concurrent git op during `U-GC-05`'s gc → `gc.auto 0` + window. F22 blind `--strip-blobs-bigger-than 20M` → inventory + user review first (U-GC-11). F23 `git push origin :cad-fusion-live-ms0` deletes the live-trunk remote ref before consumers re-pointed → push `main` first → re-point all → keep `origin/archive/cad-fusion-live-ms0-pre-rename` → delete last (U-GC-17). F24 `.py` scripts >20 MB stripped → folded into F22's inventory. F25 `filter-repo --invert-paths` prunes commits that were only `system-graph.json` (incl. HEAD `02720fd64`) → `--prune-empty=never` + `rev-list --count HEAD` before==after (U-GC-11). F26 stale `index.lock` in a worktree blocks the reset loop → `rm -f` >1h-old locks + report failures (U-GC-14). F27 the roadmap file's SHA moves under the operator post-rewrite → keep a flat copy outside `.git`. F28 SessionStart/Stop hooks fire during the rewrite (`git-sync-stop` pushes; `git-sync-fetch` pulls a half-rewritten origin) → `PRISM_DISABLE_GIT_SYNC=1` + the freeze hook + no chat starts/stops during the window (U-GC-10). **F29 (NEW) — system-viz graph generator hardcodes `cad-fusion-live-ms0`** → it crashes/produces-wrong-output post-rename → `U-GC-27` greps + fixes the generators; `U-GC-28` verifies a clean regen. **F30 (NEW) — `install-system-viz-git-hook.mjs`'s git hook references the old branch** → silently stops regenerating the graph post-rename → `U-GC-27` checks it.

## PRISM HOOK-COLLISION SECTION
- `asset-deletion-block.mjs` (PreToolUse HARD BLOCK): `rm -rf` of anything under `/.claude/` → use `mv … …archive-<date>` (U-GC-04). `git worktree remove --force` of worktrees under `.claude/worktrees/` may trip it — and per `feedback_never_delete_only_disable` needs explicit per-worktree user OK anyway.
- `stop_on_uncommitted_critical.mjs` (Stop HARD BLOCK): blocks the *operator's* Stop if any worktree has dirty `constants.ts`/`*Safety*`/`*Force*` → `U-GC-29a` handles it.
- `stop_on_hook_unregistration.mjs` + `settings-json-addonly-guard.mjs`: the temp freeze hook (U-GC-10) and `trunk-guard.mjs` (U-GC-06) must be added to *bundle SUB_HOOKS* (not top-level settings.json) so they're recognized via `bundleAbsorbedHookNames()` and removing the freeze hook later isn't a settings.json deletion. When retiring the freeze hook, remove it from the file *and* the bundle SUB_HOOKS in the same commit.
- `git-anti-clobber.mjs.disabled`: exists, worktree-aware (per-worktree + remote locks) but disabled — `U-GC-08` should note why and whether reviving it is in scope; the 40-worktree reset loop (U-GC-14) serializes through it if revived, else just run it from one operator session.
- `git-sync-stop.mjs` (Stop, supervised push): "if diverged → DO NOT push" — after the rewrite, every chat's `git-sync-stop` correctly refuses; `U-GC-14` must announce so chats `fetch && reset --hard` first.
- `leave-a-copy-behind-guard.mjs` (Stop): may flag the eventual backup deletion (`U-GC-29c`) — aligns with "keep the backups long".
- `comprehensive-build-enforce` / `scrutinize-before-stop`: the *commits* this roadmap produces (U-GC-03..08, U-GC-29c) pass through the normal Stop hooks — `stop_on_failing_tests` needs a fresh green test report (now fast, doesn't run vitest — see U-CLI-PERF-04); `stop_on_unwired_assets` sees `trunk-guard.mjs` as a new hook → it must be settings-registered in the same commit (U-GC-06 does this via the bundle).

## ROLLBACK PLAN
P0: `git revert` the commits; `U-GC-05`'s gc/repack only drops garbage (but `git bundle --all` *before* it, just in case — that bundle is also one of the U-GC-09 backups). P1: `git revert` the hook/doc commits (also revert the bundle SUB_HOOKS entries — `stop_on_hook_unregistration` blocks a half-revert); `unset` the harness `worktree.baseRef`. P3: **the irreversibility line** — recover via the ≥2 off-drive backups (`git -C <backup> push --mirror origin` re-force-pushes everything; then every worktree+clone `fetch && reset --hard` — scripted, not hand-done; assert `git ls-remote origin` == backup's refs before declaring recovery; if a chat already pulled the bad state, its post-pull uncommitted work is gone — the freeze hook prevents this). P4: old `main`/`master`/`claude*`/`cad-f` archived as **tags** (`archive/*-final-<date>`) and the old remote names kept as `origin/archive/*-pre-rename-<date>` for ≥90 days — pre-state always reachable; rollback the rename = `git branch -m main cad-fusion-live-ms0; git push origin <archived-sha>:refs/heads/cad-fusion-live-ms0; git remote set-head origin cad-fusion-live-ms0`. P5/P6: every "delete" was a rename-to-`archive/` / tag-then-delete with the off-drive backups still live — recoverable.

## SEQUENCING
1. **Day 1, no quiesce:** P0 (`U-GC-03` gitignore+untrack, `U-GC-04` cruft+stray+recovery-ref, the `/sync-rebase --base` clause of `U-GC-08`) + start `U-GC-29a` (dirty-worktree sweep — runs continuously, fleet coordination). Then `U-GC-09` (backups — before `U-GC-05`'s gc) → `U-GC-05` (gc+repack). Then `U-GC-29` (partial verify — confirm 43→~4 GB).
2. **After `U-GC-00` (trunk decision):** P1 (`U-GC-06` baseRef+canonical-trunk-lib+trunk-guard, `U-GC-07` worktree-route trunk-check warn-mode, `U-GC-08` doc reconcile).
3. **Schedule a quiesce window** (after `U-GC-29a` is green): P3 — `U-GC-10` freeze-on → `U-GC-11` filter-repo → `U-GC-12` verify → `U-GC-13` force-push → `U-GC-14` resync+freeze-off. `cad-f` pushes after this.
4. **After the rewrite:** P4 — `U-GC-15` analyze → `U-GC-16` Path B/C → `U-GC-17` rename→`main`+retire-as-tags.
5. **After the rename:** P6 — `U-GC-27` rename-blast-radius sweep → `U-GC-28` regen system-viz graph + verify generators. (P6 before P5's branch deletions so the generators are confirmed working on the new topology first.)
6. **Incrementally over weeks:** P5 — `U-GC-18` broken-wt, `U-GC-19` stale-merged, `U-GC-20` origin-only-branches (gates "clone size actually shrank"), `U-GC-21`→`22`→`23` triage-script→review→exec the ~24 forks, `U-GC-24` u-fus-api, `U-GC-26` forge-orphans.
7. **End:** P7 — `U-GC-29b` full verify, `U-GC-29c` document+memory.

## OPEN DECISIONS BLOCKING START
`U-GC-00` canonical trunk (rec: `cad-fusion-live-ms0`) · `U-GC-01`/`U-GC-26` `archive/forge-orphans` keep-vs-bundle-then-retire (rec: keep) · `U-GC-02` do the rewrite+force-push (rec: yes) · `U-GC-15` Path B vs C (rec: B, decided after the analysis) · when can the fleet quiesce ~30–60 min (P3).

*(RGS v6 discipline: atomic-first, dependency-ordered, tier-floors, every unit has why/depends/blocks/acceptance/risk/owner, full failure-mode register, rollback per phase, hook-collision section, decision gates factored as their own units. Engine-centric S2.5/S2.6 (ai-priority-rank, atomic-roadmap-emit, per-domain subagent fan-out) deliberately skipped — N/A for git-infra ops.)*
