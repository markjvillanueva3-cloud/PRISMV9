# GIT-TREE-REMEDIATION-MS0 — Roadmap (canonical, evolving)

> **v3** · 2026-05-12 · folds in **SCRUTINY-2** (3-agent pass on v2: Correctness 74→88, Safety 58→74, Completeness 68→81 — issues found were polish + one real omission, not structural; convergence near). Supersedes v2 (commit `4051469bc`) and v1 (`…ROADMAP-2026-05-12.md`), both kept for history. Generator: `/rgs6`, domain-adapted (git-infra/ops — "wiring" = settings.json/harness config; "tests" = git verification commands).
> **Status:** PLANNING. No git mutations performed. Decision gates open: `U-GC-00` (trunk), `U-GC-01` (forge-orphans), `U-GC-02` (rewrite?), `U-GC-15` (Path B vs C — decided after the analysis), + the quiesce-window scheduling.

## REVISION LOG
- **v1** (`…ROADMAP-2026-05-12.md`, commit `637502940`) → **SCRUTINY-1** (`…SCRUTINY-1.md`, `49d47e0e0`): 14 must-fixes, Correctness 74 / Safety 58 / Completeness 68.
- **v2** (commit `4051469bc`) — folded SCRUTINY-1: one numbering scheme; `worktree.baseRef` = HARNESS setting not git config; freeze-enforcement unit; Path C `merge --allow-unrelated-histories` (dropped A/D/E); retire trunks as tags; origin-only-branch unit; stale-fork auto-triage script; `filter-repo --prune-empty=never` + blob-inventory review; ≥2 verified off-drive backups; per-ref-lease force-push; `*-augmentation.json` per-file-audited; `master`'s 1326c first-class; `recovery` corrected to a dangling ref; hook-collision section; **NEW Phase 6** (rename-blast-radius + system-viz regen).
- **v3** (this file) — folds **SCRUTINY-2**: (correctness) fixed the false "graph generators read git log/branch/worktree" claim — only `generate-git-tree.mjs` reads git (`git log --all`, no hardcode); reconciled the META unit count (35 IDs incl. sub-numbered, not 30); pinned `U-GC-29a` to P0. (safety) freeze-hook operator-identity = an **env var** `PRISM_GIT_FREEZE_OPERATOR=1` (not session-id — survives compact, can't be impersonated) + the hook's first line is `if (!existsSync(FLAG)) return continue` before any parsing + the hook enforces its own expiry (dead operator → auto-no-op) + flag-absent/parse-error → continue; the **U-GC-14 slip-check is now snapshot-all-branch-tips-at-freeze-on / diff-at-freeze-off** (the old `git log origin/<b>..<b>` was vacuous for local-only branches); freeze deny-list adds `pull|fetch` (it's the operator's window); added the **`scripts/git-emergency-restore.mjs`** unit (write + dry-run-test the P3 rollback script *before* P3); per-worktree `git stash create` snapshot before each U-GC-14 `reset --hard`; Path C conflict-resolution is **per-cluster reviewed against U-GC-15's audit** (not blanket "favor cad-f" — which contradicted picking Path C) + an abort threshold (>~200 conflicted paths → fall back to per-cluster cherry-pick) + per-cluster semantic acceptance (not just vitest-green) + history-doubling documented; `install-system-viz-git-hook.mjs` checked for **auto-committing the graph** (a P0 fix if it does — instant blob-back otherwise); U-GC-28 verifies every worktree has the P0 `.gitignore` commit before regen; new hook collisions: **`file-claim-guard`** on the bundle-file edits (likely day-1 blocker), `stop_on_content_deletion` on the `git rm --cached`, `chat-state-isolator`, the fork-budget; U-GC-04's `recovery`-ref deletion is tag-first + reachability-checked. (completeness) **NEW: `generate-git-tree.mjs` + the tracked `git-tree-augmentation.json` (763 KB, 695 `Lgit` nodes naming `cad-fusion-live-ms0` ×600 + 600 commit SHAs) are now first-class** — U-GC-03 classifies the file, U-GC-28 regenerates it via `node regen-viz.mjs --full`; the `Lgit` layer is currently *not merged* into the live `system-graph.json` (pre-existing break — U-GC-28 either fixes the merge or documents it absent); added `scripts/{system-viz-on-commit,regen-viz,merge-augmentations}.mjs` to the rename-blast-radius; added `.claude/hooks/{neural-roadmap-resume-detect:94,session-start-claim-slot}.mjs` (more branch-name-derived-topic / hardcoded-fallback spots); enumerated the 6 git-connected state files (`AGENT_WORKBOARD.json`, `chat-slots.json`, `predicted-collisions.json`, `tribal-embed-index.json`, `wiki-orphans.json`, `git-health-state.json`); `.git/info/exclude` (3 local-only ignores — NOT committed → lost in backups/re-clones → `JM DIE/` 24 545 files at risk in U-GC-29a's `git add -A` path) → replicate into every clone or move to `.gitignore`; husky/lint-staged is a no-op (stated); `core.autocrlf` unset (decide after the rewrite); no CODEOWNERS; `.github/workflows/{web,validate-cam-enrichment}.yml` trigger on any-branch `push:` → CI burst expected after U-GC-13; `prism_dev`/`prism_session` git-aware-action check; remote is `markjvillanueva3-cloud/PRISMV9.git` (personal repo → likely no `main` branch-protection — confirm). **U-GC-27 split into 27a (generators+git-hook — code that breaks), 27b (the git-tree-augmentation regen-or-gitignore decision), 27c (docs/handoff/state — cosmetic); the generator-hardening AUDIT moved to P1 (before U-GC-17, so rename-safety is confirmed before the rename lands).**
- **v3 → SCRUTINY-3** (next): SCRUTINY-2's agents recommend loop 3 be a *time-boxed* polish pass — the v2→v3 delta was one real omission + sequencing/atomicity fixes + ~8 "state it" items, none dangerous-if-missed. Then ship.

---

## META

| Field | Value |
|---|---|
| Milestone | `GIT-TREE-REMEDIATION-MS0` · scope `[GIT-CLEANUP]` |
| Tier | 2 (dev-tools / system-knowledge) · leverage 9/10 |
| Units | **38** — `U-GC-00`…`U-GC-29` flat + sub-units `U-GC-15{a,b,c}`, `U-GC-27{a,b,c}`, `U-GC-29{a,b,c}` + `U-GC-EMERGENCY-RESTORE` + the 5 decision gates (`U-GC-00/01/02/15(decide)/26`) |
| Quiesce windows | 1 (P3 — rewrite + force-push; ~30–60 min, fleet frozen by U-GC-10's env-gated hook) |
| Backups | ≥2 (`git clone --mirror` to a *different physical volume* + `git bundle --all` off-drive), `git fsck --full`-verified, taken **before** any `gc --prune`, kept until **all of P4+P5+P6 verified** |

### Decision gates (BLOCKING)
| ID | Decision | Recommendation | Blocks |
|---|---|---|---|
| `U-GC-00` | Canonical trunk? | **`cad-fusion-live-ms0`** (1216c, live, checked out everywhere; vs `main` 874c frozen-5d / `master` 1326c stale-7wk — all three mutually-unrelated histories) | U-GC-06, U-GC-07, U-GC-15..17 |
| `U-GC-01` | `archive/forge-orphans-2026-05-01` (3.09 GB whisper + ~9 881 files + 502c): keep / bundle-off-drive-then-retire? | **Keep** (gc doesn't touch reachable-from-branch blobs); revisit after P0 | U-GC-26 |
| `U-GC-02` | Do the history rewrite + force-push? | **Yes** (one quiesce window) | U-GC-09..14 |
| `U-GC-15(decide)` | After analyzing `main`/`master`/`claude-*` unique work: Path B (declare cad-f canonical, archive others as tags) or Path C (`merge --allow-unrelated-histories` the wanted trunk in, per-cluster conflict resolution)? | **B** unless `U-GC-15a/b/c` find wanted clusters → then **C** (never A/D/E; if Path-C conflict set >~200 paths → abort to per-cluster cherry-pick) | U-GC-16 |
| `U-GC-26` | `archive/forge-orphans` final disposition (post-bundle-verify) | n/a | — |

### Verified facts (re-checked SCRUTINY-1 + SCRUTINY-2)
`git merge-base` empty for every pair of {`cad-fusion-live-ms0` 1216c, `main` 874c, `master` 1326c, `claude/fervent-bohr` ~200c, `claude/zen-dirac` ~1300c} — mutually-unrelated histories. `cad-f` is 87 commits ahead of `origin/cad-f` (unpushed) and **unpushable** (≥7 blobs >100 MB: six `system-graph.json` versions to 182.5 MB, one 134 MB `obsidian-augmentation.json`, both on `cad-f`; + `models/ggml-large-v3.bin` 3.09 GB / `ggml-base.bin` 148 MB on `archive/forge-orphans`). `.git` ≈ 43 GB (216 759 loose objects, only 16 511 prune-packable — the rest is *reachable history*; 56 packs; `garbage: 0`). 40 worktrees + 1 stray (`brave-euclid` → `gitdir: C:/PRISM/.git/...` wrong drive). 15 `worktree-agent-*` cruft branches (all == `main`, no worktree dirs). `git config worktree.baseRef` **unset** → harness `EnterWorktree` defaults to `fresh` = branch-from-`origin/main` (a bifurcation vector — fix is a **harness setting in `C:/Users/wompu/.claude/settings.json`**, the C:→H: mirror source, NOT `git config`). `git-filter-repo` **not installed**. `worktree-commit-route.mjs` **is wired** (via `bash-bundle`) — `H:/PRISM/CLAUDE.md`/`GSD_MICRO.md` ("dormant"/"NOT YET WIRED") are stale. `system-graph.json` is **not tracked** (regenerated; the 118 MB blob is history-only). **`state/shared/system-viz/git-tree-augmentation.json` IS tracked** (763 KB, 695 `Lgit`-layer nodes — `git.history` hub, `git.branch.<name>`, `git.commit.<sha7>`×600, `--touched--> eng.X` edges — bakes in `cad-fusion-live-ms0` ×600+ and 600 commit SHAs the P3 rewrite invalidates; the `Lgit` layer is currently NOT in the merged live `system-graph.json` — pre-existing wiring break). Several `state/shared/system-viz/*-augmentation.json` are tracked + hand-curated (`cam-vendor-catalog-augmentation.json`, `engine-classification-repair-log.json`, `action-engine-edges-augmentation.json`, …) — keep tracked. The **graph pipeline does NOT read the branch name** — only `scripts/generate-git-tree.mjs` runs git (`git log --all`, `for-each-ref refs/heads refs/remotes`, `git rev-parse --abbrev-ref HEAD` — no hardcode) → the rename is mechanically safe; U-GC-27a's grep is belt-and-suspenders. Pipeline entry points: `scripts/regen-viz.mjs` (FAST list includes `generate-git-tree.mjs`), `scripts/system-viz-on-commit.mjs` (fires on every commit + hourly cron + `/system-viz`), `scripts/merge-augmentations.mjs` (folds the git-tree nodes in at line 1268). `recovery` is **not a remote** (`git remote -v` → only `origin` = `https://github.com/markjvillanueva3-cloud/PRISMV9.git` — a *personal* repo, so likely no `main` branch-protection; confirm via `gh api repos/.../branches/main/protection`) — it's a dangling `refs/remotes/recovery/work/intel-ollama-obsidian-ms0` ref pointing at a *real* commit `12fe39f2…` (check reachability from local `intel-ollama-obsidian-ms0` before deleting). CI: `.github/workflows/{ci,deploy,auto-index,dockerfile-sync}.yml` key on `branches: [main]` (`cad-f` never CI'd — an argument FOR the rename); `web.yml`+`validate-cam-enrichment.yml` trigger on any-branch `push:` (only `paths:` filters) → expect a CI burst after U-GC-13; `claude-review.yml` on `pull_request`. No `.github/CODEOWNERS`. `husky`/`lint-staged` is a no-op (`.husky/pre-commit` skips lint-staged when no config — there's none). `core.autocrlf` unset (CRLF warnings every commit). `.git/info/exclude` has 3 local-only ignores (`resources/`, `JM DIE/`, `recovery-logs/`) — NOT committed (lost in `--mirror` backups + re-clones → a `git add -A` in U-GC-29a's abandoned-worktree path would stage `JM DIE/`'s 24 545 files). `enforce-handoff-topic.mjs:99` excludes `main/master/develop` from branch-topic derivation but NOT `cad-fusion-live-ms0`; `session-start-claim-slot.mjs` also derives the chat-slot topic from the branch name; `neural-roadmap-resume-detect.mjs:94` has a literal `?? "cad-fusion-live-ms0"` fallback. 6 git-connected state files name the branch: `AGENT_WORKBOARD.json`, `chat-slots.json`, `predicted-collisions.json`, `tribal-embed-index.json`, `wiki-orphans.json`, `mcp-server/data/state/git-health-state.json` (the last is cached + consumed by `git-health-guard.mjs`). `RESUME_AT_WORK.md` (referenced by CLAUDE.md §66) — does not exist. 2 wiki lesson nodes literally name `cad-fusion-live-ms0` (`vault.wiki.lessons.cad-fusion-live-ms0-{h-drive-archaeology,integration-discovery}` at `knowledge/wiki/lessons/cad-fusion-live-ms0-*.md` — keep the names; they document history).

---

## DEPENDENCY DAG

```
U-GC-00 (trunk?) ─┬→ U-GC-06 ─→ U-GC-07 ; U-GC-27a-AUDIT (P1, read-only generator grep)
                  └→ U-GC-15a/b/c → U-GC-15(decide B|C) → U-GC-16 → U-GC-17
U-GC-01 ─→ U-GC-26
U-GC-02 ─→ U-GC-EMERGENCY-RESTORE (write+dry-run the P3 rollback script) ─→ U-GC-09 (≥2 verified off-drive backups) ─→ U-GC-10 (freeze on) → U-GC-11 (filter-repo --prune-empty=never) → U-GC-12 (verify) → U-GC-13 (force-push per-ref leases) → U-GC-14 (resync + freeze off)   [QUIESCE WINDOW]
P0  U-GC-03 (gitignore+untrack — incl. classify git-tree-augmentation.json; CHECK install-system-viz-git-hook for auto-commit-of-graph → if so, fix HERE)  U-GC-04 (branch-cruft + stray-wt mv + recovery-ref tag-then-delete + reachability check)  U-GC-08-clause (/sync-rebase --base fix — day 1)  U-GC-29a (dirty-worktree sweep — START DAY 1, run continuously; HARD prereq for U-GC-13)  U-GC-09 (backups, before the gc)  U-GC-05 (gc --prune + repack -adf)  →  U-GC-29 (partial verify)
P1  U-GC-06 (worktree.baseRef-as-harness-setting + canonical-trunk.mjs lib + trunk-guard.mjs + enforce-handoff-topic & session-start-claim-slot exclusion-list updates) ; U-GC-07 (worktree-commit-route trunk-check, warn→block) ; U-GC-08 (doc reconcile) ; U-GC-27a-AUDIT (read-only grep of all generators/hooks/state for hardcoded branch refs → confirm rename-safe)   [after U-GC-00; low risk]
P3  U-GC-EMERGENCY-RESTORE → U-GC-09 → U-GC-10 → U-GC-11 → U-GC-12 → U-GC-13 → U-GC-14   [QUIESCE; irreversible at U-GC-13]
P4  U-GC-15a/b/c (analyze main/master/claude-*) → U-GC-15(decide) → U-GC-16 (Path B|C exec) → U-GC-17 (rename→main + retire-as-tags + GitHub default-branch + re-point all)
P5  U-GC-18 (broken xproc-neural wt) ; U-GC-19 (stale-merged) ; U-GC-20 (origin-only branches — gates "clone size actually shrank") ; U-GC-21 (build git-stale-fork-triage.mjs) → U-GC-22 (user review STALE-FORK-TRIAGE.md) → U-GC-23 (exec triage) ; U-GC-24 (u-fus-api01/02) ; U-GC-25 (claude-* — folded into 15c+22+23) ; U-GC-26 (forge-orphans disposition)
P6  U-GC-27b (the git-tree-augmentation.json regen-or-gitignore decision + run generate-git-tree.mjs) ; U-GC-27c (docs + handoff files + the 6 state files — cosmetic) ; U-GC-28 (regen the whole viz graph via `node regen-viz.mjs --full` + verify the Lgit layer + verify the build-state/milestone-progress/claude-brief generators run clean against `main`)   [after U-GC-17; the U-GC-27a AUDIT half already ran in P1]
P7  U-GC-29 (partial verify after P0+P1) ; U-GC-29b (full verify after P6) ; U-GC-29c (document + memory + GIT-TOPOLOGY.md)
```
Tier-floor invariants: **P0 before P3** (gitignore+untrack must land first); **`U-GC-29a` dirty-sweep is a HARD prereq for `U-GC-13`** + re-run immediately before it; **backups (`U-GC-09`) + the emergency-restore script (`U-GC-EMERGENCY-RESTORE`) before any `gc --prune` and before P3**; **`U-GC-27a-AUDIT` (generator rename-safety grep) runs in P1, before `U-GC-17`**; **`U-GC-10`'s env-gated freeze active for the entire P3 window**; **`install-system-viz-git-hook` auto-commit-of-graph check is a P0 gate** (if it auto-commits the 23 MB graph, that's a P0 fix or the P3 rewrite is permanently defeated by the next commit).

---

## PHASES (deltas from v2 in **bold**; unchanged units summarized)

### PHASE 0 — Zero-risk, no rewrite (now; no quiesce)
- **`U-GC-03`** — `.gitignore` + untrack generated artifacts, **per-file audited (incl. an explicit verdict for `state/shared/system-viz/git-tree-augmentation.json` — it's generated by `generate-git-tree.mjs` → gitignore it + `git rm --cached`)**; keep the hand-curated `*-augmentation.json` tracked. **ALSO in P0: read `scripts/install-system-viz-git-hook.mjs` and the post-commit hook it installs — confirm it does NOT `git add`/`git commit` `system-graph.json` or any now-ignored artifact; if it does, fix it HERE (a post-rename auto-commit of the 23 MB graph permanently defeats the P3 rewrite).** Acceptance: `git check-ignore` matches each ignored path incl. `git-tree-augmentation.json`; the curated ones still `git ls-files`-present; the system-viz git hook does not stage/commit ignored artifacts; `/system-viz` still regenerates. *Owner:* direct.
- **`U-GC-04`** — branch cruft + stray worktree + the dangling `recovery` ref: `mv …/brave-euclid …/brave-euclid.orphan-archive-<date>` (NOT `rm -rf` — `asset-deletion-block` blocks that; confirm `C:/PRISM/` isn't a separate clone first) → `git worktree prune`; for the 15 `worktree-agent-*`: `git merge-base --is-ancestor <b> main` → if yes `git branch -m <b> archive/<b>`, **if no → `git tag archive/<b>-<date> <b>` and route it to `U-GC-22`'s triage (don't rename-and-forget)**; **`git tag archive/recovery-ref-<date> 12fe39f2… 2>/dev/null || true` (best-effort — check reachability from local `intel-ollama-obsidian-ms0` first) then `git update-ref -d refs/remotes/recovery/work/intel-ollama-obsidian-ms0`**, note in GIT-TOPOLOGY.md. *Owner:* direct.
- **`U-GC-05`** — `gc --prune=now` + `repack -adf`: `git config gc.auto 0` (window) → **announce: any chat needing reflog-based recovery must do it before this window** → `git reflog expire --expire=now --all` → `git gc --prune=now` → `git repack -adf`. *Depends on:* `U-GC-09` (backups) **and** `U-GC-29a` (dirty-sweep done). Acceptance: `git count-objects -vH` size < 5 GiB, loose < 20 k, packs ≤ 2; `git fsck --connectivity-only` clean; the dirty-sweep loop re-run → all worktrees clean. Restore `gc.auto` after (or leave 0 — decide). *Owner:* direct.
- **`U-GC-29a`** *(P0 — not "P2-pre"; pinned per SCRUTINY-2)* — dirty-worktree sweep, START DAY 1, runs continuously, HARD prereq for `U-GC-13`. For each dirty worktree: owning chat commits-or-discards (post on `AGENT_CHAT.md`), or (abandoned) `git -C <wt> diff > state/shared/wip-archive/<branch>.diff` + commit on that branch — **but: do NOT `git add -A` blindly in an abandoned worktree without first replicating `.git/info/exclude` into it (else 24 545 `JM DIE/` files get staged)**. Special-case `work/xproc-neural` (unborn — archive the 13 119 untracked files, then safe to drop), `work/merge-staging-ms0` (triage WIP + 12 unique commits → `U-GC-15a`; don't `reset` it away). If any worktree has dirty `constants.ts`/`*Safety*`/`*Force*` → `stop_on_uncommitted_critical` blocks the operator's Stop — handle explicitly. Acceptance: `for wt in $(git worktree list --porcelain | awk '/^worktree/{print $2}'); do echo "$wt: $(timeout 8 git -C "$wt" status --porcelain 2>/dev/null | wc -l)"; done` → every line `0` or acknowledged. Re-run immediately before `U-GC-13`. *Owner:* coordinated.

### PHASE 1 — Process fixes (now, after U-GC-00; low risk)
- **`U-GC-06`** — `worktree.baseRef = head` in `C:/Users/wompu/.claude/settings.json` (HARNESS setting, not `git config`); `.claude/hooks/lib/canonical-trunk.mjs` (exports `CANONICAL_TRUNK` from env `PRISM_CANONICAL_TRUNK` default = `U-GC-00`'s value + `isOnTrunk()`); `.claude/hooks/trunk-guard.mjs` SessionStart hook → wired into `sessionstart-bundle.mjs` SUB_HOOKS, fail-open; **add `cad-fusion-live-ms0` to the branch-exclusion list in BOTH `enforce-handoff-topic.mjs:99` AND `session-start-claim-slot.mjs`; fix `neural-roadmap-resume-detect.mjs:94`'s `?? "cad-fusion-live-ms0"` fallback to `?? CANONICAL_TRUNK`**. Acceptance: harness `EnterWorktree` → new branch's merge-base == current HEAD; `node --check` all; `trunk-guard` exit 0 on `{}`; warning fires on `master`; `verify-hook-refs --quiet` clean; `stop_on_hook_unregistration` allows. *Owner:* direct.
- **`U-GC-07`** — `worktree-commit-route.mjs` trunk-ancestry check before the `[MAIN]`/`CROSS_CUTTING`/`[MAIN-FORCE]` bypasses (warn→block via `PRISM_TRUNK_GUARD_MODE`, default `warn` ~1 wk then `block`), using `canonical-trunk.mjs`. *Owner:* direct.
- **`U-GC-08`** — doc reconcile: "worktree-commit-route NOT YET WIRED" → "wired" (`CLAUDE.md`, `GSD_MICRO.md`, `GSD_QUICK.md`, `DEV_PROTOCOL.md`); **`/sync-rebase --base` → `$PRISM_CANONICAL_TRUNK` (do this clause in P0, day 1 — active footgun)**; replace "move work via git stash → pop" with `git show <ref>:<path>`; create `state/shared/RESUME_AT_WORK.md` (§8 silent-overwrite history); add a "canonical trunk" para to `CLAUDE.md` referencing `/checkin`'s divergence check; note in `CLAUDE.md` that `git-anti-clobber.mjs.disabled` exists and why (and whether reviving it is in scope); **state: husky/lint-staged is a no-op; no CODEOWNERS; `core.autocrlf` decision deferred to post-rewrite**. *Owner:* direct.
- **`U-GC-27a-AUDIT`** *(P1 — moved earlier per SCRUTINY-2; the *fix* half stays in P6)* — read-only: `grep -rln 'cad-fusion-live-ms0\|getCurrentBranch\|rev-parse --abbrev-ref' scripts/generate-* scripts/regen-viz.mjs scripts/system-viz-on-commit.mjs scripts/merge-augmentations.mjs scripts/install-system-viz-git-hook.mjs .claude/hooks/ .claude/helpers/ .claude/scripts/` → confirm no hardcoded branch ref will break a generator/hook post-rename (the audit so far says: only `generate-git-tree.mjs` reads git, no hardcode; `neural-roadmap-resume-detect.mjs:94` has a fallback — fixed in U-GC-06). Output a checklist. Acceptance: every generator/hook either has no branch hardcode OR is on the fix list (U-GC-06/27c). *Owner:* one chat.

### PHASE 2-pre — gates for the rewrite
- **`U-GC-EMERGENCY-RESTORE`** *(NEW per SCRUTINY-2)* — write + dry-run-test `scripts/git-emergency-restore.mjs <backup.git>`: does `git -C <backup.git> push --mirror origin`, then asserts `git ls-remote origin | sort` == `<backup>` refs `| sort`, then prints (does NOT run) the per-worktree `fetch && reset --hard` loop. Written *before* P3 (writing a recovery script during the emergency is the failure mode). Acceptance: `node --check`; a dry-run against one of the U-GC-09 backups prints a plausible plan; the ls-remote-match assertion logic is correct. *Owner:* direct.
- **`U-GC-09`** — ≥2 backups, off-drive, BEFORE any `gc --prune`: `git clone --mirror H:/prism <other-volume>/prism-git-backup-<date>.git` + `git bundle create <off-drive>/prism-backup-<date>.bundle --all`; each: `git fsck --full` (budget ≥10 min) + `rev-parse cad-fusion-live-ms0` matches source + size sanity; **also copy `.git/info/exclude` alongside each backup (it's per-clone, not committed)**; confirm H: is on stable power (portable drive). Keep both until all of P4+P5+P6 verified. *Owner:* direct.

### PHASE 3 — Unblock the push (QUIESCE WINDOW; irreversible at U-GC-13)
- **`U-GC-10`** — Activate the maintenance freeze. *What:* write `state/shared/.git-freeze-active` (operator session id + `expiry > now+15min`); operator sets `PRISM_GIT_FREEZE_OPERATOR=1` in their own shell; add `git-maintenance-freeze.mjs` to **`bash-bundle.mjs` SUB_HOOKS** — hook logic: line 1 `try { if (!existsSync(FLAG)) return emit(continue) } catch { return emit(continue) }`; parse the flag — parse-error → treat as **active** (safe for a freeze); if `Date.now() > expiry` → auto-no-op (a dead operator doesn't brick the fleet); if `process.env.PRISM_GIT_FREEZE_OPERATOR === '1'` → allow; else **deny** any `git (commit|push|merge|rebase|reset|cherry-pick|filter-repo|worktree add|branch -[dD]|pull|fetch)` (yes, `pull|fetch` too — it's the operator's window; `git-sync-fetch` is correctly blocked); fail-open on any unexpected throw. Add a Stop-hook clause skipping `git-sync-stop`'s push while the flag is set; `set PRISM_DISABLE_GIT_SYNC=1`. **Snapshot every local branch tip SHA to `state/shared/.git-freeze-branch-tips.json` at freeze-on** (for U-GC-14's slip check). *Acceptance:* `node --check bash-bundle.mjs`; positive control — a non-operator `git commit` denied; negative control — operator `git status` allowed (and a non-operator `git status` allowed — only the mutating verbs are blocked); `rm` the flag → non-operator `git commit` passes again. **Prereq check: no chat holds a file-claim on `bundles/*.mjs` (`grep -rl bundles mcp-server/data/claims/`) — coordinate the release on `AGENT_CHAT.md` if so (most likely day-1 blocker per SCRUTINY-2).** *Risk:* medium (fail-open + env-gated + self-expiring + bundle-syntax-checked mitigate). *Owner:* operator.
- **`U-GC-11`** — `filter-repo` on a fresh `--single-branch` mirror, `--prune-empty=never`: (0) `pip install git-filter-repo` (not installed); (1) `git rev-list --all --objects | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '$1=="blob"&&$3>20000000' | sort -k3 -rn` → review with the user → finalize the `--path` exclusion list (prefer explicit paths over a blind cutoff); (2) `git clone --no-local --single-branch --branch cad-fusion-live-ms0 H:/prism /tmp/prism-rewrite` (+ `main`, `master`, wanted `work/*` — NOT `archive/forge-orphans`; assert `/tmp` has ≥1.5× `.git`-size free; `git fsck --full && git rev-list --count HEAD` matching source before proceeding); (3) `cd /tmp/prism-rewrite && git filter-repo --force --prune-empty=never --invert-paths --path state/shared/system-viz/system-graph.json --path …/graph.cypher --path …/obsidian-augmentation.json --path …/data-catalogs-atomic-augmentation.json --path mcp-server/dev/null --path-glob 'mcp-server/.serena/cache/*' [+ user-reviewed extras]`; (4) `git reflog expire --expire=now --all && git gc --prune=now --aggressive`. Acceptance: rewritten mirror — no blob >50 MB (`awk '$3>52428800'` empty); `git log --all -- system-graph.json` empty; `git rev-list --count HEAD` before == after (or a user-approved delta) AND rewritten HEAD commit message == expected; `du -sh .git` < 2 GB; `git fsck --full` clean. *Owner:* operator.
- **`U-GC-12`** — Inventory-verify the rewrite (the acceptance of U-GC-11, broken out as a gate). Fail → delete the broken mirror, re-clone, retry (the live repo is untouched until U-GC-13).
- **`U-GC-13`** — Force-push the rewritten branches only, per-ref leases: `git push --force-with-lease=cad-fusion-live-ms0:<old-sha> --force-with-lease=main:<old-sha> [--force-with-lease=master:<old-sha> …] origin cad-fusion-live-ms0 main master` — **never `--force-with-lease --all`**. On partial-push: re-run until `git ls-remote origin | sort` == the mirror's refs `| sort`; keep the freeze in effect until green. (Personal-account repo → likely no `main` branch-protection blocking the force-push — confirm `gh api repos/markjvillanueva3-cloud/PRISMV9/branches/main/protection` first.) *Owner:* operator.
- **`U-GC-14`** — Re-sync everything + lift the freeze. **Slip-check:** diff every local branch tip vs `state/shared/.git-freeze-branch-tips.json` (snapshotted at freeze-on) — anything new on ANY branch (incl. local-only `work/*`) → STOP, cherry-pick those commits onto the rewritten history, re-force-push, re-verify; do NOT `reset --hard` past unmerged local work. Then: in `H:/prism` `git fetch && git reset --hard origin/cad-fusion-live-ms0`; for each of 40 worktrees — `rm -f <wt>/.git/index.lock` if >1 h old; **`S=$(git -C <wt> stash create); [ -n "$S" ] && echo "<wt> $S" >> state/shared/wip-archive/u-gc-14-prereset-stashes.log`** (a pre-reset working-tree snapshot — `stash create` doesn't push to `refs/stash`, so it's safe per `feedback_no_git_stash_shared_tree`; recoverable via `git -C <wt> stash apply $S`); then `git -C <wt> fetch && git -C <wt> reset --hard origin/<branch>` — for worktree branches NOT on origin: push them first or `git -C <wt> rebase --onto <new-base> <old-base>`, never `reset --hard <old-sha>`; report every worktree it couldn't reset; replicate `.git/info/exclude` into each re-clone; enumerate other clones (does `C:/PRISM/` exist? laptops? CI caches?) — re-clone/hard-reset each; `rm state/shared/.git-freeze-active state/shared/.git-freeze-branch-tips.json`, **remove `git-maintenance-freeze.mjs` from the file AND `bash-bundle.mjs` SUB_HOOKS in the same commit**, `unset PRISM_DISABLE_GIT_SYNC PRISM_GIT_FREEZE_OPERATOR`, `git config --unset gc.auto`; post on `AGENT_CHAT.md` "history rewritten — `git fetch && git reset --hard origin/<branch>` before any commit; your `git-sync-stop` will refuse to push until you do." *Acceptance:* `H:/prism` at the new HEAD, clean; every worktree clean + correct upstream; `git push origin cad-fusion-live-ms0` → "up-to-date"; `node --check bash-bundle.mjs` after the freeze-hook removal; **the operator's Stop succeeds after the removal commit** (if `stop_on_hook_unregistration` blocks it, the documented escape is the 3-strike auto-pass); spot-check 3 chats `fetch && reset --hard` cleanly. *Owner:* operator + coordinated.

### PHASE 4 — Trunk reconciliation (after the rewrite; on a fresh `work/trunk-reconcile-<date>` branch)
- **`U-GC-15a/b/c`** — analyze the 3 (4) disjoint histories: (a) `main` 874c, (b) `master` **1326c** (largest), (c) `claude/*` roots (~200c + ~1300c). For each: per-subsystem `git diff cad-f <other>` + `git log cad-f..<other>` → classify each cluster: superseded / salvageable-from-`archive/forge-orphans` / **wanted (with a concrete "feature X reachable / behaves" acceptance for each wanted cluster — not just "vitest green")**. Output `state/shared/MAIN-MASTER-CLAUDE-UNIQUE-WORK-AUDIT.md`. *Owner:* one chat + user reviews.
- **`U-GC-15(decide)`** — Path B (zero wanted clusters) or Path C (any wanted).
- **`U-GC-16`** — execute. *Path B:* `git tag archive/{main,master,claude-fervent-bohr,claude-zen-dirac}-final-<date> <branch>`, push the tags, then `git branch -D` them (tags keep the commits reachable; `git branch -a` stays clean; tags aren't auto-pushed by `--all`). *Path C:* `git checkout cad-fusion-live-ms0 -b work/trunk-reconcile-<date>`; `git merge --allow-unrelated-histories <wanted-trunk>`; **if `git merge` reports >~200 conflicted paths → abort, fall back to per-cluster cherry-pick of the `U-GC-15`-identified wanted commits onto a fresh branch**; resolve conflicts **per-cluster, reviewed against `U-GC-15`'s audit** (NOT a blanket "favor cad-f" — that would drop the wanted clusters; "newer-wins" is the wrong rule when cad-f never had the feature); the "test" gate = **each wanted cluster's concrete acceptance from `U-GC-15` passes**, plus vitest-green + physics/test reviewer agents; then merge `work/trunk-reconcile-<date>` → cad-f → push (cad-f is pushable post-P3); archive the merged-in trunk as a tag; **document in GIT-TOPOLOGY.md: "history doubled on <date> via Path-C merge of <trunk> — intentional; the count-consuming generators (BUILD_STATE, MILESTONE_PROGRESS, system-viz Lgit) now see ~2× commits"**. *Owner:* one chat; reviewer agents validate.
- **`U-GC-17`** — promote `cad-fusion-live-ms0` → `main`; re-point everything: `git branch -m cad-fusion-live-ms0 main`; `git push -u origin main`; **set the GitHub default branch to `main` via API/UI** (`git config init.defaultBranch` is repo-local, does NOT change the remote default); `git remote set-head origin main`; re-point all 40 worktrees' upstreams + `H:/prism`; `git push origin cad-fusion-live-ms0:refs/heads/archive/cad-fusion-live-ms0-pre-rename-<date>` (rename-on-remote, keep ≥90 days) **then** `git push origin :cad-fusion-live-ms0` (delete the old name — LAST, after every consumer confirmed re-pointed); update `canonical-trunk.mjs`'s default + `PRISM_CANONICAL_TRUNK` → `main`; handle harness `EnterWorktree`-managed worktrees (`ExitWorktree --keep` then re-enter, or re-point upstream — announce). *Owner:* operator + user (for the GitHub default-branch change).

### PHASE 5 — Worktree / branch cleanup (after U-GC-17; SHAs stable)
- `U-GC-18` retire broken `work/xproc-neural` wt (0c; 13 119 untracked archived by U-GC-29a, verified). · `U-GC-19` retire stale-merged (`cost-cascade-ms0`+`graph-context-ms0` *same HEAD* — collapse the dup; `sfc-calibrate`; `cad-complete-ms0`+`psau-sav2` after WIP) — rename-on-remote then delete, user nod. · `U-GC-20` triage the ~25 origin-only branches — **`U-GC-13`'s force-push only touched LOCAL branches → origin-only ones keep the >100 MB blobs → clone size doesn't shrink until handled**; for blob-carrying ones `git filter-repo` a 2nd pass or delete after tag; **`origin/worktree-data-loss-fix` must NOT be touched without user review**; keep `origin/archive-2026-02-01`. Acceptance: `git rev-list --all --objects --remotes | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '$3>104857600'` → empty. · `U-GC-21` build `scripts/git-stale-fork-triage.mjs` (per-branch: ancestor-of-main? last-commit, unique-commit count, file-overlap → heuristic recommendation → pre-filled `STALE-FORK-TRIAGE.md`). · `U-GC-22` user reviews/approves (~24 forks + `claude/*` roots: `intel-ollama-obsidian-ms0` 983u, `lathe-pro-v3-ms2` 497, `mill-master` 489, `ai-aware-harden` 403, `lathe-master` 443, `wedm-consolidated` 427, … decision per branch: revive/merge/archive-tag). · `U-GC-23` exec triage (cherry-pick the "merge" ones onto `main`; tag-then-delete the "archive" ones). · `U-GC-24` retire `worktree-u-fus-api01/02` (api01's 497c via U-GC-22; `mv`/`worktree remove` + per-wt user OK). · `U-GC-25` `claude/*` — folded into `U-GC-15c`+`22`+`23`. · `U-GC-26` `archive/forge-orphans` disposition (keep → document as cold storage; or `git bundle create <off-drive>/forge-orphans-<date>.bundle …` → verify (clone+fsck) → tag → `branch -D` + `push :` + `worktree remove` + `gc --prune` → document the bundle location *permanently* — never the delete-path without the verified bundle).

### PHASE 6 — System-viz + git-connected nodes (after U-GC-17; the U-GC-27a AUDIT half already ran in P1)
- **`U-GC-27b`** — the `git-tree-augmentation.json` decision + run: if U-GC-03 classified it "generated → gitignore" (recommended — it's emitted by `generate-git-tree.mjs`), then it's already ignored & untracked; run `node scripts/generate-git-tree.mjs` (or via `regen-viz.mjs`) → it now emits `git.branch.main` + the rewritten commit SHAs, ~94 branches → ~12, 40 worktrees → ~12. If kept-tracked instead: regenerate-and-commit it. *Owner:* the system-viz lane-owner chat (lane-locked — coordinate).
- **`U-GC-27c`** — cosmetic rename-staleness sweep: the 6 git-connected state files (`AGENT_WORKBOARD.json`, `chat-slots.json`, `predicted-collisions.json`, `tribal-embed-index.json`, `wiki-orphans.json`, `mcp-server/data/state/git-health-state.json`) — classify each self-heals-on-next-run vs. needs-a-one-time-fix (`git-health-state.json` caches a divergence verdict against the old branch — clear it); per-chat `HANDOFF-*-cad-fusion-live-ms0.md` files (rename to `…-main.md`? or leave — historical); `~/.claude/commands/{checkin,sync,handoff,six-chat-bootstrap}.md`, `CLAUDE-CODEX-COORDINATION-DIRECTIVE.md` — sweep for branch refs; the 2 wiki lesson nodes `vault.wiki.lessons.cad-fusion-live-ms0-*` — annotate (keep names); `prism_dev`/`prism_session` MCP dispatchers — `grep -ln git mcp-server/src/tools/dispatchers/{devDispatcher,sessionDispatcher}.ts` and fix any branch-aware action. Output `state/shared/RENAME-BLAST-RADIUS.md`. *Owner:* one chat + user reviews.
- **`U-GC-28`** — regenerate the whole viz graph + verify: `node scripts/regen-viz.mjs --full` (the real orchestrator — NOT the 6 individual scripts) → confirms `generate-git-tree.mjs` + `merge-augmentations.mjs` run clean against the renamed trunk; verify the `Lgit` layer — **either it's now merged into `system-graph.json` (`node -e "const g=require('./state/shared/system-viz/system-graph.json'); console.log(g.nodes.filter(n=>n.layer==='Lgit').length, g.nodes.some(n=>n.id==='git.branch.main'), g.nodes.some(n=>n.id==='git.branch.cad-fusion-live-ms0'))"` → ~695, true, false) OR (if the pre-existing merge break isn't in scope to fix here) U-GC-29c's GIT-TOPOLOGY.md documents "the Lgit layer is generated by `generate-git-tree.mjs` but not currently merged into the live graph — known break, tracked separately"; confirm `install-system-viz-git-hook`'s hook still fires + doesn't auto-commit the (now-ignored) graph; **verify `node scripts/build-state-snapshot.mjs && node scripts/build-milestone-progress.mjs && node mcp-server/scripts/generate-claude-brief.mjs` all exit 0 and reference `main`** (these are NOT viz scripts — a bad U-GC-27/U-GC-06 edit to them isn't caught by the viz regen); `node scripts/system-viz-query.mjs headline` → fresh, references `main`; `state/shared/system-viz/system-graph.json` gitignored (not staged) **AND `git -C <every-worktree> check-ignore state/shared/system-viz/system-graph.json` passes** (every worktree has the P0 commit — gate this before the regen). *Owner:* the system-viz lane-owner chat.

### PHASE 7 — Verify & document
- **`U-GC-29`** — partial verify after P0+P1 (checkpoint before the irreversible P3): `git fsck --connectivity-only` clean; `git count-objects -vH` size < 5 GiB; `worktree.baseRef`=head in the harness settings; `trunk-guard`/`worktree-commit-route` behave; `verify-hook-refs --quiet` clean; `U-GC-29a` green; `U-GC-EMERGENCY-RESTORE` script written + dry-run-passes; `install-system-viz-git-hook` confirmed not auto-committing ignored artifacts. Confirms 43→~4 GB landed.
- **`U-GC-29b`** — full verify after P6: `git fsck --full` clean; `git count-objects -vH` size < 2 GiB / loose < 10 k / packs ≤ 2; verify no >100 MB blob at HEAD (`git rev-list --objects HEAD | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '$3>104857600'` empty) AND none on any origin ref (`… --remotes …` empty); `git merge-base --is-ancestor main HEAD` true; `git worktree list | wc -l` ≤ ~12; `git branch -a | wc -l` ~halved; `node scripts/regen-viz.mjs --full` exit 0; the freeze hook removed (file + bundle entry); `git config --get gc.auto` (decided value); spot-check 3 chats `fetch && reset --hard` cleanly. (NOTE: `git push --dry-run` does NOT trigger GH001 — verify the blob is *gone*, not that dry-run "succeeds".)
- **`U-GC-29c`** — document + memory: `state/shared/GIT-TOPOLOGY.md` (trunk=`main`; worktree→branch map; retired branches & tags; the `worktree.baseRef=head`+`trunk-guard`+`[MAIN]`-restriction model; off-drive backup locations & retention; the `forge-orphans` bundle location if retired; if Path C: the history-doubling note; the Lgit-merge-break note if not fixed; `core.autocrlf` decision); update `H:/PRISM/CLAUDE.md` ("Lane discipline"/"PER-CHAT HANDOFF"/"Main branch … main" — verify the last is auto-generated and now correct); `reference_git_topology.md` memory + `MEMORY.md` pointer; mark this roadmap `COMPLETE`; schedule the off-drive backups' eventual removal after a long grace (`leave-a-copy-behind-guard` flags premature removal).

---

## FAILURE-MODE REGISTER (F1–F32)
F1 filter-repo refuses w/ linked worktrees → fresh `--single-branch` mirror. F2 `reset --hard` destroys WIP → `U-GC-29a` hard prereq + re-run before U-GC-14 + per-worktree `stash create` snapshot in U-GC-14. F3 live chat breaks mid-commit → `U-GC-10`'s env-gated freeze hook (not an announcement). F4 `git stash` clobbers peers → `U-GC-08` removes the stale doctrine; U-GC-14 uses `stash create` (no `refs/stash` push). F5/F21 `master` retired without diff-review → `U-GC-15b` its own analysis unit. F6 unrelated-history merge conflict-fest → staging branch, abort-threshold (>200 paths → cherry-pick fallback), Path B default. F7/F18 graph regen/re-commit during the freeze → `.gitignore` lands P0; freeze hook blocks commits; `install-system-viz-git-hook` confirmed not auto-committing (P0); U-GC-28 gates "every worktree has the P0 commit". F8 forge-orphans deleted lost → never the delete-path without the verified off-drive bundle (U-GC-26). F9/F13/F14 single same-portable-drive backup → ≥2, ≥2 volumes, verified, kept until P4+P5+P6 done, before the gc + `.git/info/exclude` copied alongside. F10 trunk-guard blocks everything → warn-only, fail-open. F11 gc corrupts a pack via concurrent worktree op → `gc.auto 0` + window + announce-reflog-recovery-first. F12 `/tmp`(=C:) out of space cloning 43 GB → gc before the clone; `--single-branch`; assert ≥1.5× free; fsck the clone. F15 filter-repo dies mid-run → throwaway mirror; recovery = delete+re-clone+retry. F16 force-push partial → push critical ref first + verify; `git ls-remote origin` must match the mirror before unfreezing. F17 chat slips a commit during the freeze → the env-gated freeze hook + the **branch-tip-snapshot diff** slip-check in U-GC-14 (covers local-only branches the old `origin/<b>..<b>` check missed). F19 worktree branch rewritten differently / not on origin → pre-build the worktree→origin-ref map; push local-only first or `rebase --onto`, never `reset --hard <old-sha>`. F20 concurrent git op during U-GC-05's gc → `gc.auto 0` + window. F22 blind `--strip-blobs-bigger-than` → inventory + user review first. F23 `push origin :cad-f` before consumers re-pointed → push `main` first → re-point all → keep `origin/archive/cad-f-pre-rename` → delete last. F24 `.py` >20 MB stripped → folded into F22. F25 filter-repo prunes commits that were only `system-graph.json` (incl. HEAD) → `--prune-empty=never` + `rev-list --count HEAD` before==after. F26 stale `index.lock` blocks the reset loop → `rm -f` >1h-old + report failures. F27 the roadmap file's SHA moves under the operator post-rewrite → keep a flat copy outside `.git`. F28 SessionStart/Stop hooks fire during the rewrite → `PRISM_DISABLE_GIT_SYNC=1` + the freeze hook (which now also blocks `pull|fetch`) + no chat starts/stops during the window. F29 a generator hardcodes `cad-fusion-live-ms0` → `U-GC-27a-AUDIT` (P1) greps + the fix list (U-GC-06/27c) + `U-GC-28` verifies a clean regen. F30 `install-system-viz-git-hook` auto-commits the graph → permanent blob-back → checked in **P0** (a P0 fix if true). **F31 (NEW) — `.git/info/exclude` lost in a re-clone → `git add -A` in U-GC-29a's abandoned-worktree path stages `JM DIE/`'s 24 545 files** → replicate `.git/info/exclude` into every clone (U-GC-09/14) or move those 3 lines to `.gitignore`. **F32 (NEW) — Path-C "favor cad-f" silently drops the wanted clusters** (it contradicts picking Path C) → per-cluster conflict resolution reviewed against `U-GC-15`'s audit; per-cluster semantic acceptance. **F33 (NEW) — `file-claim-guard` blocks the operator editing `bundles/*.mjs` on day 1** → check `mcp-server/data/claims/` for a claim on `bundles/`; coordinate the release first. **F34 (NEW) — bundle-load failure → silent inactive freeze** (the whole Bash matcher no-ops, freeze + commit-ownership-guard + worktree-route all off) → `node --check bash-bundle.mjs` + positive+negative controls in U-GC-10's acceptance, and again on removal in U-GC-14. **F35 (NEW) — freeze-flag expiry already past / unparseable** → write asserts `expiry > now+15min`; parse-error → active (safe for a freeze); hook enforces the expiry (dead operator → auto-no-op). **F36 (NEW) — operator locked out by a session-id rotation** → operator identity is `PRISM_GIT_FREEZE_OPERATOR=1` env (survives `/compact`, can't be impersonated by other processes), not a session-id match.

## PRISM HOOK-COLLISION SECTION (F-flagged ones above + these)
`asset-deletion-block.mjs` HARD-BLOCK: `rm -rf` under `/.claude/` → use `mv …archive-<date>` (U-GC-04). `stop_on_uncommitted_critical.mjs` HARD-BLOCK: blocks the operator's Stop if any worktree has dirty `constants.ts`/`*Safety*`/`*Force*` → U-GC-29a handles. `stop_on_hook_unregistration.mjs` + `settings-json-addonly-guard.mjs`: the freeze hook + `trunk-guard` go in *bundle SUB_HOOKS* (not top-level settings.json); remove the freeze hook from file + bundle entry in one commit; verify the operator's Stop succeeds after that commit (else 3-strike auto-pass). **`file-claim-guard.mjs`: blocks the operator editing `bundles/*.mjs` if a chat holds a claim → check `mcp-server/data/claims/` first (most likely day-1 blocker).** **`stop_on_content_deletion.mjs`: may block U-GC-03's `git rm --cached` of tracked generated artifacts → know this before P0; workaround = `_disabled_by:`/exemption or per-commit override.** **`chat-state-isolator.mjs`: if it rewrites the bash payload, the freeze hook's identity read must use the env var, not a payload session-id field (it does).** `git-anti-clobber.mjs.disabled`: exists, worktree-aware — `U-GC-08` notes why/scope; the 40-worktree reset loop serializes through it if revived else run from one operator session. `git-sync-stop.mjs`: "if diverged → DO NOT push" — post-rewrite every chat correctly refuses; U-GC-14 announces. `leave-a-copy-behind-guard.mjs`: flags premature backup deletion (U-GC-29c). `comprehensive-build-enforce`/`scrutinize-before-stop`: the roadmap's commits pass through Stop hooks — `stop_on_failing_tests` needs a fresh green report (fast now — see U-CLI-PERF-04); `stop_on_unwired_assets` sees `trunk-guard.mjs`/`git-maintenance-freeze.mjs` as new hooks → settings-register them in the same commit (via the bundle). **Fork-budget: `bash-bundle.mjs` is at 8 sub-hooks; adding `git-maintenance-freeze` → 9 (on the edge per `reference_harness_hang_prevention` — remove it promptly post-P3, which is U-GC-14's job); `trunk-guard` in `sessionstart-bundle` is permanent (one more).**

## ROLLBACK PLAN
P0: `git revert` the commits; `git bundle --all` before `U-GC-05`'s gc (that bundle is also one of the U-GC-09 backups). P1: `git revert` the hook/doc commits (also revert the bundle SUB_HOOKS entries — `stop_on_hook_unregistration` blocks a half-revert); `unset` the harness `worktree.baseRef`. P3: **the irreversibility line** — run `scripts/git-emergency-restore.mjs <backup.git>` (= `git -C <backup> push --mirror origin` + the ls-remote-match assertion + the per-worktree reset loop), written + dry-run-tested in `U-GC-EMERGENCY-RESTORE` *before* P3; the freeze hook (now blocking `pull|fetch` too) plus the U-GC-14 branch-tip-snapshot slip-check minimize the "a chat already pulled the bad state" window — but it's not zero, so U-GC-14's per-worktree `stash create` snapshot is the belt; if a chat's post-pull uncommitted work is still lost, that's the residual (the freeze hook does NOT block a chat's working-tree edits, only its git mutations). P4: old `main`/`master`/`claude*`/`cad-f` archived as **tags** + `origin/archive/*-pre-rename-<date>` kept ≥90 days — rollback the rename = `git branch -m main cad-fusion-live-ms0; git push origin <archived-sha>:refs/heads/cad-fusion-live-ms0; git remote set-head origin cad-fusion-live-ms0`. P5/P6: every "delete" was a rename-to-`archive/`/tag-then-delete; the off-drive backups are still live.

## SEQUENCING
1. **Day 1, no quiesce:** P0 (`U-GC-03` gitignore+untrack incl. `git-tree-augmentation.json` + the `install-system-viz-git-hook` auto-commit check; `U-GC-04` cruft+stray+recovery-ref; the `/sync-rebase --base` clause) + start `U-GC-29a` (dirty-sweep, runs continuously). Then `U-GC-09` (backups — before the gc) + `U-GC-EMERGENCY-RESTORE` → `U-GC-05` (gc+repack). Then `U-GC-29` (partial verify — confirm 43→~4 GB; emergency-restore script ready).
2. **After `U-GC-00`:** P1 (`U-GC-06` baseRef+canonical-trunk-lib+trunk-guard+exclusion-list-updates; `U-GC-07` worktree-route trunk-check warn-mode; `U-GC-08` doc reconcile; `U-GC-27a-AUDIT` generator rename-safety grep — before the rename).
3. **Schedule a quiesce window** (after `U-GC-29a` green + `U-GC-09` backups + `U-GC-EMERGENCY-RESTORE` ready): P3 — `U-GC-10` freeze-on → `U-GC-11` filter-repo → `U-GC-12` verify → `U-GC-13` force-push → `U-GC-14` resync+freeze-off. `cad-f` pushes after this.
4. **After the rewrite:** P4 — `U-GC-15a/b/c` analyze → `U-GC-15(decide)` → `U-GC-16` Path B/C → `U-GC-17` rename→`main`+retire-as-tags.
5. **After the rename:** P6 — `U-GC-27b` git-tree regen → `U-GC-27c` cosmetic sweep → `U-GC-28` regen the whole graph + verify generators (incl. the non-viz BUILD_STATE/MILESTONE_PROGRESS/CLAUDE-BRIEF ones). (P6 before P5's branch deletions so the generators are confirmed working on the new topology.)
6. **Incrementally over weeks:** P5 — `U-GC-18` broken-wt, `U-GC-19` stale-merged, `U-GC-20` origin-only (gates "clone size shrank"), `U-GC-21`→`22`→`23` triage, `U-GC-24` u-fus-api, `U-GC-26` forge-orphans.
7. **End:** P7 — `U-GC-29b` full verify, `U-GC-29c` document+memory.

## OPEN DECISIONS BLOCKING START
`U-GC-00` canonical trunk (rec: `cad-fusion-live-ms0`) · `U-GC-01`/`U-GC-26` forge-orphans keep-vs-bundle (rec: keep) · `U-GC-02` do the rewrite+force-push (rec: yes) · `U-GC-15(decide)` Path B vs C (rec: B, post-analysis) · when can the fleet quiesce ~30–60 min (P3) · whether `master` (1326c) and the `claude/*` roots hold anything worth Path C (answered by `U-GC-15a/b/c`).

*(RGS v6 discipline: atomic-first, dependency-ordered, tier-floors, every unit has why/depends/blocks/acceptance/risk/owner, full failure-mode register (F1–F36), rollback per phase, hook-collision section, decision gates as their own units. Engine-centric S2.5/S2.6 deliberately skipped — N/A for git-infra ops. Scrutiny scores: v1 [74/58/68] → v2 [88/74/81] → v3 [this — SCRUTINY-2 polish folded]. SCRUTINY-2 agents recommend loop 3 be a time-boxed final pass, then ship.)*
