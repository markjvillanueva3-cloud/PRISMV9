# GIT-TREE-REMEDIATION-MS0 — Roadmap (canonical, evolving)

> **v4** · 2026-05-12 · folds in **SCRUTINY-3** (loop-2 3-agent pass on v3: Correctness 88, Safety 86, Completeness 91 — one real correctness defect + three "phantom-state-on-every-clone" completeness gaps + safety/sequencing polish; no structural issues). Supersedes v3 (`23aeb0602`), v2 (`4051469bc`), v1 (`…ROADMAP-2026-05-12.md`) — all kept for history. Generator: `/rgs6`, domain-adapted (git-infra/ops — "wiring" = settings.json/harness config; "tests" = git verification commands).
> **Status:** PLANNING. No git mutations performed. Decision gates open: `U-GC-00` (trunk), `U-GC-01` (forge-orphans), `U-GC-02` (rewrite? — now with a fallback ladder), `U-GC-15` (Path B vs C — decided after the analysis), + the quiesce-window scheduling.

## REVISION LOG
- **v1** (`…ROADMAP-2026-05-12.md`, commit `637502940`) → **SCRUTINY-1** (`…SCRUTINY-1.md`, `49d47e0e0`): 14 must-fixes, Correctness 74 / Safety 58 / Completeness 68.
- **v2** (commit `4051469bc`) — folded SCRUTINY-1: one numbering scheme; `worktree.baseRef` = HARNESS setting not git config; freeze-enforcement unit; Path C `merge --allow-unrelated-histories` (dropped A/D/E); retire trunks as tags; origin-only-branch unit; stale-fork auto-triage script; `filter-repo --prune-empty=never` + blob-inventory review; ≥2 verified off-drive backups; per-ref-lease force-push; `*-augmentation.json` per-file-audited; `master`'s 1326c first-class; `recovery` corrected to a dangling ref; hook-collision section; **NEW Phase 6** (rename-blast-radius + system-viz regen).
- **v3** (commit `23aeb0602`) — folded **SCRUTINY-2**: fixed the false "graph generators read git log/branch/worktree" claim (only `generate-git-tree.mjs` reads git); reconciled the META unit count; pinned `U-GC-29a` to P0; freeze-hook operator-identity = env var `PRISM_GIT_FREEZE_OPERATOR=1`; U-GC-14 slip-check = branch-tip-snapshot diff; freeze deny-list adds `pull|fetch`; added `U-GC-EMERGENCY-RESTORE`; per-worktree `git stash create` snapshot in U-GC-14; Path C conflict-resolution per-cluster (not "favor cad-f"); `install-system-viz-git-hook` auto-commit check is P0; new hook collisions (`file-claim-guard`, `stop_on_content_deletion`, `chat-state-isolator`, fork-budget); `generate-git-tree.mjs` + `git-tree-augmentation.json` first-class; `.git/info/exclude` handling; husky no-op stated; `core.autocrlf` deferred; no CODEOWNERS; `web.yml`/`validate-cam-enrichment.yml` unfiltered push triggers; `prism_dev`/`prism_session` git-aware check; remote = `markjvillanueva3-cloud/PRISMV9.git` (personal); U-GC-27 split into 27a/27b/27c; generator-hardening AUDIT moved to P1.
- **v4** (this file) — folds **SCRUTINY-3**: **(correctness)** ① `state/shared/system-viz/system-graph.json` **IS tracked** (118.76 MB at HEAD — itself one of the >100 MB blobs, NOT history-only) → U-GC-03 must `git rm --cached` it + `.gitignore` line (the body said only `git-tree-augmentation.json`); ② commit counts refreshed: `cad-fusion-live-ms0` = **1226c** (not 1216), **97 ahead** of `origin/cad-f` (not 87); `main` 874 ✓ `master` 1326 ✓; ③ the `recovery`-ref commit `12fe39f2…` is **already reachable** from `refs/heads/main` AND `work/intel-ollama-obsidian-ms0` (the no-`work/`-prefix branch name in v3 doesn't exist) — `git for-each-ref --contains` confirms — deleting the `recovery/*` ref is provably safe now; ④ `git-tree-augmentation.json` literal `cad-fusion-live-ms0` count is ~177, not ×600 (×600 is the *commit-SHA* count); `merge-augmentations.mjs` folds it at line **121**, not 1268; loose-object count ~216 888. **(completeness — 3 MUST-ADDs, all "phantom state on every clone"):** ⑤ **`git notes` / `refs/replace/*` are silently dropped by a `--single-branch` `filter-repo`** → U-GC-11 enumerates `refs/notes refs/replace` first (include them in the rewrite via a `--mirror` clone + `--refs`, or document as intentionally dropped); U-GC-29b asserts the rewritten remote matches; ⑥ **`origin/HEAD` (the symbolic-ref) on *other* clones is stale after the rename** (`C:/PRISM/`, laptops, CI caches) → U-GC-14's "enumerate other clones" clause adds `git -C <clone> remote set-head origin --auto` + the AGENT_CHAT.md post-rewrite message says so; ⑦ **`.git/objects/info/commit-graph` + `multi-pack-index` reference dead SHAs after the rewrite/gc** → U-GC-05 ends with `git commit-graph write --reachable && git multi-pack-index write` (or `git maintenance run --task=commit-graph`); U-GC-14's per-worktree loop adds `rm -f <wt>/.git/objects/info/commit-graph` (lazy rebuild). **(completeness — SHOULDs):** ⑧ **F37 (NEW): the `git maintenance` builtin** — if a Windows Scheduled Task / `[maintenance]` config block is registered against this repo it runs `gc`/`commit-graph`/`prefetch` *during the freeze* (it's not a Claude-shell `git` invocation, so the bash hook can't see it) → U-GC-04 checks `git config --get-regexp '^maintenance\.'` + `schtasks /query | findstr -i git` → `git maintenance unregister` for P3, re-decide in U-GC-14; ⑨ `.git/hooks/` local hooks (distinct from Claude hooks) + `core.hooksPath` audited in U-GC-08; they're per-clone (same class as `.git/info/exclude`) — mentioned alongside the F31 mitigation; ⑩ U-GC-06 acceptance now asserts the **`c-to-h-mirror` actually fired** (`diff <(jq .worktree C:/Users/wompu/.claude/settings.json) <(jq .worktree H:/.claude/settings.json)` identical); `gh repo set-default` + IDE git-state self-heal noted in U-GC-27c; ⑪ **U-GC-10 gains an explicit window-START announcement protocol** (operator posts to AGENT_CHAT.md + AGENT_WORKBOARD.md *before* flipping the flag; names the operator on the workboard coordination-status line; uses `/broadcast`; states "subagents will hit the `git`-deny — expected, not a bug") — v3 only had the *end* announcement; ⑫ **U-GC-29b gains the only outside-validation check that exists**: `git clone --depth=1 <url> /tmp/prism-fresh && du -sh` < ~500 MB shallow / full re-fetch < 2 GB + a trivial round-trip push test (whitespace commit to a throwaway branch → push → no GH001 → delete); ⑬ **U-GC-02's decision-gate row gains a fallback ladder** for "NO, don't `filter-repo`": (1) `git lfs migrate import --above=100MB` (still rewrites history + needs the window, but no `pip` dep), (2) BFG Repo-Cleaner (`java -jar bfg.jar --strip-blobs-bigger-than 100M` — no `pip`), (3) abandon the push, document `cad-f` as local-only-canonical, create `origin/main-pushable` as a squashed snapshot — P4 rename targets that; **U-GC-00's row gains** "if ≠ `cad-f` → swap the rename direction in U-GC-17 + the `canonical-trunk.mjs` default in U-GC-06; everything else is symmetric"; ⑭ U-GC-11 preserves `/tmp/prism-rewrite/.git/filter-repo/commit-map` → `state/shared/git-rewrite-<date>/commit-map` (old-SHA → new-SHA — needed because `git-tree-augmentation.json` + Linear links + wiki nodes + old handoffs bake in 600 commit SHAs); U-GC-27b/29c reference that map instead of regenerating blind. **(safety — SHOULDs):** ⑮ **`U-GC-EMERGENCY-RESTORE` REFUSES a single-branch mirror** (`push --mirror` from `/tmp/prism-rewrite` would DELETE every origin ref not in it) — the script asserts `<backup.git>` is the U-GC-09 full `--mirror` clone (refs-count ≥ live-origin refs-count) *before* the push; the ls-remote-match is post-push (already too late) so the pre-check is the real guard; the script header states it's P3-scoped, not a general tool; ⑯ **the "a chat pulled the bad state mid-window" residual is now actioned**: U-GC-14, before any worktree `reset --hard`, archives the diff of any worktree whose `@{upstream}` is not an ancestor of the OLD pre-rewrite tip (it pulled mid-window) to `wip-archive/` + names the chat on AGENT_CHAT.md (the `stash create` snapshot already covers it; this makes the diverged subset explicit); ⑰ **decided F31's "or": move `JM DIE/`, `recovery-logs/`, `resources/` from `.git/info/exclude` into committed `.gitignore`** in U-GC-03 (eliminates the 24 545-file staging risk permanently); the replicate-into-clones step stays only as belt for pre-`.gitignore` backups; ⑱ **document-before-destroy**: U-GC-26 appends the forge-orphans bundle path to GIT-TOPOLOGY.md *and commits that* before `branch -D`; U-GC-09 writes the backup locations to GIT-TOPOLOGY.md/handoff *as the backups are made* (not deferred to U-GC-29c); ⑲ U-GC-14's `index.lock` removal: a *fresh* (<1 h) lock on a worktree whose owning chat is confirmed-dead on AGENT_CHAT.md (no heartbeat >5 min) → remove it too, log which; ⑳ backup-retention floor anchored to the **`U-GC-29b` sign-off artifact**, not "phase completion" (P5 runs over weeks). **(nice-to-have, folded cheap):** `git config feature.manyFiles true; core.untrackedCache true` post-repack (U-GC-05) — root cause of the 8 s `git status` timeout the 40-worktree loop works around; `.mailmap` noted in U-GC-08 as out-of-scope-but-cheap-now.
- **v4 → SCRUTINY-4** (next, final): loop 3 is a *time-boxed* polish pass — the v3→v4 delta was three real omissions of the file's own bug class + ~14 "state it" / sequencing items, none dangerous-if-missed. After loop 3, ship.

---

## META

| Field | Value |
|---|---|
| Milestone | `GIT-TREE-REMEDIATION-MS0` · scope `[GIT-CLEANUP]` |
| Tier | 2 (dev-tools / system-knowledge) · leverage 9/10 |
| Units | **38** — `U-GC-00`…`U-GC-29` flat + sub-units `U-GC-15{a,b,c}`, `U-GC-27{a,b,c}`, `U-GC-29{a,b,c}` + `U-GC-EMERGENCY-RESTORE` + the 5 decision gates (`U-GC-00/01/02/15(decide)/26`) |
| Quiesce windows | 1 (P3 — rewrite + force-push; ~30–60 min, fleet frozen by U-GC-10's env-gated hook) |
| Backups | ≥2 (`git clone --mirror` to a *different physical volume* + `git bundle --all` off-drive), `git fsck --full`-verified, taken **before** any `gc --prune`, locations written to GIT-TOPOLOGY.md as they're made, `.git/info/exclude` + `.git/hooks/` copied alongside, kept until **the `U-GC-29b` full-verify sign-off** |

### Decision gates (BLOCKING)
| ID | Decision | Recommendation | Blocks |
|---|---|---|---|
| `U-GC-00` | Canonical trunk? | **`cad-fusion-live-ms0`** (1226c, live, checked out everywhere; vs `main` 874c frozen-5d / `master` 1326c stale-7wk — all three mutually-unrelated histories). **If ≠ `cad-f`** → swap the rename direction in `U-GC-17` + the `canonical-trunk.mjs` default in `U-GC-06`; everything else is symmetric. | U-GC-06, U-GC-07, U-GC-15..17 |
| `U-GC-01` | `archive/forge-orphans-2026-05-01` (3.09 GB whisper + ~9 881 files + 502c): keep / bundle-off-drive-then-retire? | **Keep** (gc doesn't touch reachable-from-branch blobs); revisit after P0 | U-GC-26 |
| `U-GC-02` | Do the history rewrite + force-push? | **Yes** (one quiesce window). **If NO** → fallback ladder: (1) `git lfs migrate import --above=100MB` (still rewrites + needs the window, no `pip` dep), (2) BFG (`java -jar bfg.jar --strip-blobs-bigger-than 100M` — no `pip`, simpler), (3) abandon the push, document `cad-f` as local-only-canonical, create `origin/main-pushable` as a squashed snapshot → P4 rename targets that. | U-GC-09..14 |
| `U-GC-15(decide)` | After analyzing `main`/`master`/`claude-*` unique work: Path B (declare cad-f canonical, archive others as tags) or Path C (`merge --allow-unrelated-histories` the wanted trunk in, per-cluster conflict resolution)? | **B** unless `U-GC-15a/b/c` find wanted clusters → then **C** (never A/D/E; if Path-C conflict set >~200 paths → abort to per-cluster cherry-pick) | U-GC-16 |
| `U-GC-26` | `archive/forge-orphans` final disposition (post-bundle-verify) | n/a | — |

### Verified facts (re-checked SCRUTINY-1 → -3, live as of 2026-05-12)
`git merge-base` empty for every pair of {`cad-fusion-live-ms0` **1226c**, `main` 874c, `master` 1326c, `claude/fervent-bohr` ~200c, `claude/zen-dirac` ~1300c} — mutually-unrelated histories. `cad-f` is **97 commits ahead** of `origin/cad-f` (unpushed) and **unpushable** (≥7 blobs >100 MB: `state/shared/system-viz/system-graph.json` — which **IS tracked, 118.76 MB at HEAD** — plus ≥4 more `system-graph.json` history versions to 182.5 MB, one 134 MB `obsidian-augmentation.json`, all on `cad-f`; + `models/ggml-large-v3.bin` 3.09 GB / `ggml-base.bin` 148 MB on `archive/forge-orphans`). `.git` ≈ 42 GB (≈216 888 loose objects, only 16 511 prune-packable — the rest is *reachable history*; 56 packs; `garbage: 0`). 40 worktrees + 1 stray (`brave-euclid` → `gitdir: C:/PRISM/.git/...` wrong drive). 15 `worktree-agent-*` cruft branches (all == `main`, no worktree dirs). `git config worktree.baseRef` **unset** → harness `EnterWorktree` defaults to `fresh` = branch-from-`origin/main` (a bifurcation vector — fix is a **harness setting in `C:/Users/wompu/.claude/settings.json`**, the C:→H: mirror source, NOT `git config`). `git-filter-repo` **not installed** (no binary, no python module). `worktree-commit-route.mjs` **is wired** (via `bash-bundle` — 8 sub-hooks) — `H:/PRISM/CLAUDE.md`/`GSD_MICRO.md` ("dormant"/"NOT YET WIRED") are stale. **`state/shared/system-viz/system-graph.json` IS tracked** (118.76 MB at HEAD — itself one of the >100 MB blobs; U-GC-03 must `git rm --cached` it + add to `.gitignore`). **`state/shared/system-viz/git-tree-augmentation.json` IS tracked** (763 KB, `newNodes`=695 = the `Lgit` layer — `git.history` hub, `git.branch.<name>`×94, `git.commit.<sha7>`×600, `--touched--> eng.X` edges — bakes in `cad-fusion-live-ms0` ×~177 + 600 commit SHAs the P3 rewrite invalidates; the `Lgit` layer is currently NOT in the merged live `system-graph.json` — pre-existing wiring break). Several other `state/shared/system-viz/*-augmentation.json` are tracked + hand-curated (`cam-vendor-catalog-augmentation.json`, `engine-classification-repair-log.json`, `action-engine-edges-augmentation.json`, …) — keep tracked. The **graph pipeline does NOT read the branch name** — only `scripts/generate-git-tree.mjs` runs git (`git log --all`, `for-each-ref refs/heads refs/remotes`, `git rev-parse --abbrev-ref HEAD`, `git log --name-only` — no hardcode) → the rename is mechanically safe; U-GC-27a's grep is belt-and-suspenders. Pipeline entry points: `scripts/regen-viz.mjs` (FAST list includes `generate-git-tree.mjs`), `scripts/system-viz-on-commit.mjs` (fires on every commit + hourly cron + `/system-viz`), `scripts/merge-augmentations.mjs` (folds the git-tree nodes in at line **121** via `loadOptional("git-tree-augmentation.json")`), `scripts/install-system-viz-git-hook.mjs` (installs a post-commit hook — P0: confirm it does NOT `git add`/`commit` the now-ignored graph). `recovery` is **not a remote** (`git remote -v` → only `origin` = `https://github.com/markjvillanueva3-cloud/PRISMV9.git` — a *personal* repo, so likely no `main` branch-protection; confirm via `gh api repos/markjvillanueva3-cloud/PRISMV9/branches/main/protection`) — it's a dangling `refs/remotes/recovery/work/intel-ollama-obsidian-ms0` ref pointing at commit `12fe39f23665e2c9ef49d9fc44b0da2ba0b77e3d`, which **`git for-each-ref --contains 12fe39f2…` shows is already reachable from `refs/heads/main` AND `work/intel-ollama-obsidian-ms0`** (+ ~15 `worktree-agent-*`) → deleting the `recovery/*` ref loses nothing. CI: `.github/workflows/{ci,deploy,auto-index,dockerfile-sync}.yml` key on `branches: [main]` (`cad-f` never CI'd — an argument FOR the rename); `web.yml`+`validate-cam-enrichment.yml` trigger on any-branch `push:` (only `paths:` filters) → expect a CI burst after U-GC-13; `claude-review.yml` on `pull_request`. 7 workflow files total, all accounted for. No `.github/CODEOWNERS`. `husky`/`lint-staged` is a no-op (`.husky/pre-commit` skips lint-staged when no config — there's none). `core.autocrlf` unset (CRLF warnings every commit) — decide after the rewrite. `.git/info/exclude` has exactly 3 local-only ignores (`resources/`, `JM DIE/`, `recovery-logs/`) — NOT committed (lost in `--mirror` backups + re-clones → a `git add -A` in U-GC-29a's abandoned-worktree path would stage `JM DIE/`'s 24 545 files) → U-GC-03 moves them to committed `.gitignore`. `.git/hooks/` (local git hooks, distinct from Claude hooks) + `core.hooksPath` — not yet audited (U-GC-08), also per-clone. `enforce-handoff-topic.mjs:99` excludes `main/master/develop` from branch-topic derivation but NOT `cad-fusion-live-ms0`; `session-start-claim-slot.mjs` also derives the chat-slot topic from the branch name; `neural-roadmap-resume-detect.mjs:94` has a literal `?? "cad-fusion-live-ms0"` fallback. 6 git-connected state files name the branch: `AGENT_WORKBOARD.json`, `chat-slots.json`, `predicted-collisions.json`, `tribal-embed-index.json`, `wiki-orphans.json`, `mcp-server/data/state/git-health-state.json` (the last is cached + consumed by `git-health-guard.mjs`). `RESUME_AT_WORK.md` (referenced by CLAUDE.md §66) — does not exist. 2 wiki lesson nodes literally name `cad-fusion-live-ms0` (`vault.wiki.lessons.cad-fusion-live-ms0-{h-drive-archaeology,integration-discovery}` at `knowledge/wiki/lessons/cad-fusion-live-ms0-*.md` — keep the names; they document history). No `refs/notes/*` / `refs/replace/*` assumed but **not yet verified** (U-GC-11 verifies). No `git maintenance` task assumed but **not yet verified** (U-GC-04 verifies). `.gitmodules` / submodules: none. `commit-graph` file present (`.git/objects/info/commit-graph`) — stale-after-rewrite.

---

## DEPENDENCY DAG

```
U-GC-00 (trunk?) ─┬→ U-GC-06 ─→ U-GC-07 ; U-GC-27a-AUDIT (P1, read-only generator grep)
                  └→ U-GC-15a/b/c → U-GC-15(decide B|C) → U-GC-16 → U-GC-17
U-GC-01 ─→ U-GC-26
U-GC-02 ─→ U-GC-EMERGENCY-RESTORE (write+dry-run the P3 rollback script) ─→ U-GC-09 (≥2 verified off-drive backups) ─→ U-GC-10 (freeze on + window announce) → U-GC-11 (filter-repo --prune-empty=never; preserve commit-map; handle notes/replace) → U-GC-12 (verify) → U-GC-13 (force-push per-ref leases) → U-GC-14 (resync + commit-graph rebuild + freeze off)   [QUIESCE WINDOW]
P0  U-GC-03 (gitignore+untrack — incl. system-graph.json AND git-tree-augmentation.json; move .git/info/exclude→.gitignore; CHECK install-system-viz-git-hook for auto-commit-of-graph → if so, fix HERE)  U-GC-04 (branch-cruft + stray-wt mv + recovery-ref tag-then-delete + git-maintenance-task check)  U-GC-08-clause (/sync-rebase --base fix — day 1)  U-GC-29a (dirty-worktree sweep — START DAY 1, run continuously; HARD prereq for U-GC-13)  U-GC-09 (backups, before the gc)  U-GC-05 (gc --prune + repack -adf + commit-graph write + feature.manyFiles)  →  U-GC-29 (partial verify)
P1  U-GC-06 (worktree.baseRef-as-harness-setting + mirror-fired check + canonical-trunk.mjs lib + trunk-guard.mjs + enforce-handoff-topic & session-start-claim-slot & neural-roadmap-resume-detect fixes) ; U-GC-07 (worktree-commit-route trunk-check, warn→block) ; U-GC-08 (doc reconcile + .git/hooks audit) ; U-GC-27a-AUDIT (read-only grep of all generators/hooks/state for hardcoded branch refs → confirm rename-safe)   [after U-GC-00; low risk]
P3  U-GC-EMERGENCY-RESTORE → U-GC-09 → U-GC-10 → U-GC-11 → U-GC-12 → U-GC-13 → U-GC-14   [QUIESCE; irreversible at U-GC-13]
P4  U-GC-15a/b/c (analyze main/master/claude-*) → U-GC-15(decide) → U-GC-16 (Path B|C exec) → U-GC-17 (rename→main + retire-as-tags + GitHub default-branch + re-point all + remote set-head on every clone)
P5  U-GC-18 (broken xproc-neural wt) ; U-GC-19 (stale-merged) ; U-GC-20 (origin-only branches — gates "clone size actually shrank") ; U-GC-21 (build git-stale-fork-triage.mjs) → U-GC-22 (user review STALE-FORK-TRIAGE.md) → U-GC-23 (exec triage) ; U-GC-24 (u-fus-api01/02) ; U-GC-25 (claude-* — folded into 15c+22+23) ; U-GC-26 (forge-orphans disposition — document-before-destroy)
P6  U-GC-27b (the git-tree-augmentation.json regen-or-gitignore decision + run generate-git-tree.mjs; reference commit-map) ; U-GC-27c (docs + handoff files + the 6 state files + gh default-repo + IDE-state notes — cosmetic) ; U-GC-28 (regen the whole viz graph via `node regen-viz.mjs --full` + verify the Lgit layer + verify the build-state/milestone-progress/claude-brief generators run clean against `main`)   [after U-GC-17; the U-GC-27a AUDIT half already ran in P1]
P7  U-GC-29 (partial verify after P0+P1) ; U-GC-29b (full verify after P6 — incl. the fresh-clone-is-small + round-trip-push end-to-end test) ; U-GC-29c (document + memory + GIT-TOPOLOGY.md)
```
Tier-floor invariants: **P0 before P3** (gitignore+untrack must land first — incl. `system-graph.json`); **`U-GC-29a` dirty-sweep is a HARD prereq for `U-GC-13`** + re-run immediately before it; **backups (`U-GC-09`) + the emergency-restore script (`U-GC-EMERGENCY-RESTORE`) before any `gc --prune` and before P3**; **`U-GC-27a-AUDIT` (generator rename-safety grep) runs in P1, before `U-GC-17`**; **`U-GC-10`'s env-gated freeze active for the entire P3 window**; **`install-system-viz-git-hook` auto-commit-of-graph check + the `git maintenance`-task check are P0 gates** (either, if true, permanently re-introduces the blob/garbage after the rewrite); **`U-GC-29b`'s fresh-clone test is the only outside-validation that the whole exercise worked**.

---

## PHASES (deltas from v3 in **bold**; unchanged units summarized)

### PHASE 0 — Zero-risk, no rewrite (now; no quiesce)
- **`U-GC-03`** — `.gitignore` + untrack generated artifacts, **per-file audited — explicitly INCLUDING `git rm --cached state/shared/system-viz/system-graph.json` + a `.gitignore` line (it's tracked at 118.76 MB at HEAD — itself one of the >100 MB blobs) AND `git-tree-augmentation.json` (generated by `generate-git-tree.mjs`)**; keep the hand-curated `*-augmentation.json` tracked. **ALSO move the 3 `.git/info/exclude` lines (`JM DIE/`, `recovery-logs/`, `resources/`) into the committed `.gitignore`** (eliminates the 24 545-file staging risk permanently — the replicate-into-clones step in U-GC-09/14 then only matters for backups taken before this commit). **ALSO in P0: read `scripts/install-system-viz-git-hook.mjs` and the post-commit hook it installs — confirm it does NOT `git add`/`git commit` `system-graph.json` or any now-ignored artifact; if it does, fix it HERE (a post-rename auto-commit of the 23 MB graph permanently defeats the P3 rewrite).** Acceptance: `git check-ignore` matches each ignored path incl. `system-graph.json` + `git-tree-augmentation.json` + `JM DIE/`; `git ls-files | grep -E 'system-graph\.json|JM DIE/'` empty; the curated `*-augmentation.json` still `git ls-files`-present; the system-viz git hook does not stage/commit ignored artifacts; `/system-viz` still regenerates. *Owner:* direct.
- **`U-GC-04`** — branch cruft + stray worktree + the dangling `recovery` ref + **the `git maintenance` builtin**: `mv …/brave-euclid …/brave-euclid.orphan-archive-<date>` (NOT `rm -rf` — `asset-deletion-block` blocks that; confirm `C:/PRISM/` isn't a separate clone first) → `git worktree prune`; for the 15 `worktree-agent-*`: `git merge-base --is-ancestor <b> main` → if yes `git branch -m <b> archive/<b>`, if no → `git tag archive/<b>-<date> <b>` and route it to `U-GC-22`'s triage (don't rename-and-forget); **the recovery ref: `git tag archive/recovery-ref-<date> 12fe39f2…` (the commit is provably reachable from `main` + `work/intel-ollama-obsidian-ms0` per `git for-each-ref --contains` — the tag is belt only) then `git update-ref -d refs/remotes/recovery/work/intel-ollama-obsidian-ms0`**, note in GIT-TOPOLOGY.md; **the `git maintenance` builtin: `git config --get-regexp '^maintenance\.'` + `schtasks /query 2>$null | findstr -i git` → if a maintenance task / `[maintenance]` block exists, `git maintenance unregister` (it runs `gc`/`commit-graph`/`prefetch` outside the bash-hook's reach — would fire during the P3 freeze) → re-decide whether to re-register in U-GC-14; record either way.** *Owner:* direct.
- **`U-GC-05`** — `gc --prune=now` + `repack -adf` + **commit-graph rebuild + manyFiles tuning**: `git config gc.auto 0` (window) → **announce: any chat needing reflog-based recovery must do it before this window** → `git reflog expire --expire=now --all` → `git gc --prune=now` → `git repack -adf` → **`git commit-graph write --reachable && git multi-pack-index write` (so `git log` doesn't reference dead packs) → `git config feature.manyFiles true && git config core.untrackedCache true && git update-index --untracked-cache` (root cause of the 8 s `git status` timeout the 40-worktree loop works around — cheap, reversible)**. *Depends on:* `U-GC-09` (backups) **and** `U-GC-29a` (dirty-sweep done). Acceptance: `git count-objects -vH` size < 5 GiB, loose < 20 k, packs ≤ 2; `git fsck --connectivity-only` clean; `git commit-graph verify` clean; the dirty-sweep loop re-run → all worktrees clean. Restore `gc.auto` after (or leave 0 — decide in U-GC-29c). *Owner:* direct.
- **`U-GC-29a`** *(P0 — pinned per SCRUTINY-2)* — dirty-worktree sweep, START DAY 1, runs continuously, HARD prereq for `U-GC-13`. For each dirty worktree: owning chat commits-or-discards (post on `AGENT_CHAT.md`), or (abandoned) `git -C <wt> diff > state/shared/wip-archive/<branch>.diff` + commit on that branch — **but: do NOT `git add -A` blindly in an abandoned worktree without first confirming the committed `.gitignore` covers `JM DIE/` (U-GC-03 moved it there) — pre-U-GC-03 worktrees still need `.git/info/exclude` replicated in**. Special-case `work/xproc-neural` (unborn — archive the 13 119 untracked files, then safe to drop), `work/merge-staging-ms0` (triage WIP + 12 unique commits → `U-GC-15a`; don't `reset` it away). If any worktree has dirty `constants.ts`/`*Safety*`/`*Force*` → `stop_on_uncommitted_critical` blocks the operator's Stop — handle explicitly. Acceptance: `for wt in $(git worktree list --porcelain | awk '/^worktree/{print $2}'); do echo "$wt: $(timeout 8 git -C "$wt" status --porcelain 2>/dev/null | wc -l)"; done` → every line `0` or acknowledged. Re-run immediately before `U-GC-13`. *Owner:* coordinated.

### PHASE 1 — Process fixes (now, after U-GC-00; low risk)
- **`U-GC-06`** — `worktree.baseRef = head` in `C:/Users/wompu/.claude/settings.json` (HARNESS setting, not `git config`); `.claude/hooks/lib/canonical-trunk.mjs` (exports `CANONICAL_TRUNK` from env `PRISM_CANONICAL_TRUNK` default = `U-GC-00`'s value + `isOnTrunk()`); `.claude/hooks/trunk-guard.mjs` SessionStart hook → wired into `sessionstart-bundle.mjs` SUB_HOOKS, fail-open; **add `cad-fusion-live-ms0` to the branch-exclusion list in BOTH `enforce-handoff-topic.mjs:99` AND `session-start-claim-slot.mjs`; fix `neural-roadmap-resume-detect.mjs:94`'s `?? "cad-fusion-live-ms0"` fallback to `?? CANONICAL_TRUNK`**. Acceptance: harness `EnterWorktree` → new branch's merge-base == current HEAD; **`diff <(jq .worktree "C:/Users/wompu/.claude/settings.json") <(jq .worktree "H:/.claude/settings.json")` → identical (the `c-to-h-mirror` hook fired)**; `node --check` all; `trunk-guard` exit 0 on `{}`; warning fires on `master`; `verify-hook-refs --quiet` clean; `stop_on_hook_unregistration` allows. *Owner:* direct.
- **`U-GC-07`** — `worktree-commit-route.mjs` trunk-ancestry check before the `[MAIN]`/`CROSS_CUTTING`/`[MAIN-FORCE]` bypasses (warn→block via `PRISM_TRUNK_GUARD_MODE`, default `warn` ~1 wk then `block`), using `canonical-trunk.mjs`. *Owner:* direct.
- **`U-GC-08`** — doc reconcile + **`.git/hooks/` audit**: "worktree-commit-route NOT YET WIRED" → "wired" (`CLAUDE.md`, `GSD_MICRO.md`, `GSD_QUICK.md`, `DEV_PROTOCOL.md`); **`/sync-rebase --base` → `$PRISM_CANONICAL_TRUNK` (do this clause in P0, day 1 — active footgun)**; replace "move work via git stash → pop" with `git show <ref>:<path>`; create `state/shared/RESUME_AT_WORK.md` (§8 silent-overwrite history); add a "canonical trunk" para to `CLAUDE.md` referencing `/checkin`'s divergence check; note in `CLAUDE.md` that `git-anti-clobber.mjs.disabled` exists and why (and whether reviving it is in scope); **state: husky/lint-staged is a no-op; no CODEOWNERS; `core.autocrlf` decision deferred to post-rewrite; `.mailmap` is out of scope but the rewrite is the cheap moment if ever wanted**; **audit `ls .git/hooks/ | grep -v '\.sample$'` + `git config --get core.hooksPath` → enumerate any active local hook, decide if it interferes with U-GC-13/14, document in GIT-TOPOLOGY.md; note `.git/hooks/` is per-clone (lost in re-clones — same class as `.git/info/exclude`)**. *Owner:* direct.
- **`U-GC-27a-AUDIT`** *(P1 — the *fix* half stays in P6)* — read-only: `grep -rln 'cad-fusion-live-ms0\|getCurrentBranch\|rev-parse --abbrev-ref' scripts/generate-* scripts/regen-viz.mjs scripts/system-viz-on-commit.mjs scripts/merge-augmentations.mjs scripts/install-system-viz-git-hook.mjs .claude/hooks/ .claude/helpers/ .claude/scripts/` → confirm no hardcoded branch ref will break a generator/hook post-rename (the audit so far says: only `generate-git-tree.mjs` reads git, no hardcode; `neural-roadmap-resume-detect.mjs:94` has a fallback — fixed in U-GC-06). Output a checklist. Acceptance: every generator/hook either has no branch hardcode OR is on the fix list (U-GC-06/27c). *Owner:* one chat.

### PHASE 2-pre — gates for the rewrite
- **`U-GC-EMERGENCY-RESTORE`** — write + dry-run-test `scripts/git-emergency-restore.mjs <backup.git>`: **first asserts `<backup.git>` is the U-GC-09 *full* `--mirror` clone (`git -C <backup.git> for-each-ref | wc -l` ≥ `git ls-remote origin | wc -l`) and REFUSES a `--single-branch` mirror (`push --mirror` from `/tmp/prism-rewrite` would DELETE every origin ref not in it)**; then `git -C <backup.git> push --mirror origin`; then asserts `git ls-remote origin | sort` == `<backup>` refs `| sort` (post-push — too late if the arg was wrong, which is why the pre-check is the real guard); then prints (does NOT run) the per-worktree `fetch && reset --hard` loop. Script header: "P3-SCOPED EMERGENCY ONLY — not a general tool; `push --mirror` deletes any ref created after the backup, acceptable only inside the P3 window before U-GC-17." Written *before* P3 (writing a recovery script during the emergency is the failure mode). Acceptance: `node --check`; a dry-run against one of the U-GC-09 backups prints a plausible plan; the single-branch-refusal positive-control trips; the ls-remote-match assertion logic is correct. *Owner:* direct.
- **`U-GC-09`** — ≥2 backups, off-drive, BEFORE any `gc --prune`: `git clone --mirror H:/prism <other-volume>/prism-git-backup-<date>.git` + `git bundle create <off-drive>/prism-backup-<date>.bundle --all`; each: `git fsck --full` (budget ≥10 min) + `rev-parse cad-fusion-live-ms0` matches source + size sanity; **also copy `.git/info/exclude` AND `.git/hooks/` (non-`.sample`) alongside each backup (both per-clone, not committed)**; confirm H: is on stable power (portable drive); **write each backup's path + date to `state/shared/GIT-TOPOLOGY.md` and this chat's handoff NOW** (not deferred to U-GC-29c — document-before-rely). Keep both until the `U-GC-29b` full-verify sign-off. *Owner:* direct.

### PHASE 3 — Unblock the push (QUIESCE WINDOW; irreversible at U-GC-13)
- **`U-GC-10`** — Activate the maintenance freeze **+ announce the window**. *Window announce (BEFORE flipping the flag):* operator posts to `AGENT_CHAT.md` + `AGENT_WORKBOARD.md` "GIT FREEZE <start>–<end>, operator=chat-<id>, all chats: finish your current commit and idle; subagents will see `git`-mutation-deny — that's expected, not a bug; window ends with a 'history rewritten — `git fetch && reset --hard`' post"; name the operator in the workboard coordination-status line; fire `/broadcast`. *What:* write `state/shared/.git-freeze-active` (operator session id + `expiry > now+15min`); operator sets `PRISM_GIT_FREEZE_OPERATOR=1` in their own shell; add `git-maintenance-freeze.mjs` to **`bash-bundle.mjs` SUB_HOOKS** — hook logic: line 1 `try { if (!existsSync(FLAG)) return emit(continue) } catch { return emit(continue) }`; parse the flag — parse-error → treat as **active** (safe for a freeze) AND log to `state/shared/.git-freeze-parse-error.log` (diagnosability); if `Date.now() > expiry` → auto-no-op (a dead operator doesn't brick the fleet); if `process.env.PRISM_GIT_FREEZE_OPERATOR === '1'` → allow (incl. the operator's own `git reset --hard origin/<branch>` in U-GC-14); else **deny** any `git (commit|push|merge|rebase|reset|cherry-pick|filter-repo|worktree add|branch -[dD]|pull|fetch)` (yes, `pull|fetch` too — it's the operator's window; `git-sync-fetch` is correctly blocked); fail-open on any unexpected throw. Add a Stop-hook clause skipping `git-sync-stop`'s push while the flag is set; `set PRISM_DISABLE_GIT_SYNC=1`. **Snapshot every local branch tip SHA to `state/shared/.git-freeze-branch-tips.json` at freeze-on** (for U-GC-14's slip check; also snapshot each tracking-branch SHA so U-GC-14 can detect a worktree that pulled mid-window). *Acceptance:* `node --check bash-bundle.mjs`; positive control — a non-operator `git commit` denied; negative controls — operator `git status` allowed, operator `git reset --hard` allowed, a non-operator `git status` allowed (only mutating verbs blocked); `rm` the flag → non-operator `git commit` passes again; the parse-error path logs + treats-active. **Prereq check: no chat holds a file-claim on `bundles/*.mjs` (`grep -rl bundles mcp-server/data/claims/`) — coordinate the release on `AGENT_CHAT.md` if so (most likely day-1 blocker).** *Risk:* medium (fail-open + env-gated + self-expiring + bundle-syntax-checked mitigate). *Owner:* operator.
- **`U-GC-11`** — `filter-repo` on a fresh `--single-branch` mirror, `--prune-empty=never`: (0) `pip install git-filter-repo` (not installed); **(0.5) `git for-each-ref refs/notes refs/replace` → if any exist: do NOT use `--single-branch` (it drops them) — `git clone --mirror` then `filter-repo --refs <branch-list + the notes/replace refs>`, OR document them as intentionally dropped in GIT-TOPOLOGY.md**; (1) `git rev-list --all --objects | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '$1=="blob"&&$3>20000000' | sort -k3 -rn` → review with the user → finalize the `--path` exclusion list (prefer explicit paths over a blind cutoff); (2) `git clone --no-local --single-branch --branch cad-fusion-live-ms0 H:/prism /tmp/prism-rewrite` (+ `main`, `master`, wanted `work/*` — NOT `archive/forge-orphans`; assert `/tmp` has ≥1.5× `.git`-size free; `git fsck --full && git rev-list --count HEAD` matching source before proceeding); (3) `cd /tmp/prism-rewrite && git filter-repo --force --prune-empty=never --invert-paths --path state/shared/system-viz/system-graph.json --path …/graph.cypher --path …/obsidian-augmentation.json --path …/data-catalogs-atomic-augmentation.json --path mcp-server/dev/null --path-glob 'mcp-server/.serena/cache/*' [+ user-reviewed extras]`; **(3.5) copy `/tmp/prism-rewrite/.git/filter-repo/commit-map` → `H:/prism/state/shared/git-rewrite-<date>/commit-map` and commit it (old-SHA → new-SHA — needed by U-GC-27b/29c; `git-tree-augmentation.json` + Linear links + wiki nodes + old handoffs bake in 600 commit SHAs)**; (4) `git reflog expire --expire=now --all && git gc --prune=now --aggressive`. Acceptance: rewritten mirror — no blob >50 MB (`awk '$3>52428800'` empty); `git log --all -- system-graph.json` empty; `git rev-list --count HEAD` before == after (or a user-approved delta) AND rewritten HEAD commit message == expected; `du -sh .git` < 2 GB; `git fsck --full` clean; `commit-map` exists + line count == commit count. *Owner:* operator.
- **`U-GC-12`** — Inventory-verify the rewrite (the acceptance of U-GC-11, broken out as a gate). Fail → delete the broken mirror, re-clone, retry (the live repo is untouched until U-GC-13).
- **`U-GC-13`** — Force-push the rewritten branches only, per-ref leases: `git push --force-with-lease=cad-fusion-live-ms0:<old-sha> --force-with-lease=main:<old-sha> [--force-with-lease=master:<old-sha> …] origin cad-fusion-live-ms0 main master` — **never `--force-with-lease --all`**. On partial-push: re-run until `git ls-remote origin | sort` == the mirror's refs `| sort`; keep the freeze in effect until green. (Personal-account repo → likely no `main` branch-protection blocking the force-push — confirm `gh api repos/markjvillanueva3-cloud/PRISMV9/branches/main/protection` first.) *Owner:* operator.
- **`U-GC-14`** — Re-sync everything + lift the freeze. **Slip-check (covers commits):** diff every local branch tip vs `state/shared/.git-freeze-branch-tips.json` (snapshotted at freeze-on) — anything new on ANY branch (incl. local-only `work/*`) → STOP, cherry-pick those commits onto the rewritten history, re-force-push, re-verify; do NOT `reset --hard` past unmerged local work. **Diverged-WIP check (covers the "pulled mid-window" residual):** any worktree whose `@{upstream}` SHA is NOT an ancestor of the OLD pre-rewrite tip (i.e. it `git fetch`ed/`pull`ed mid-window before the freeze, or slipped past it) → `git -C <wt> diff > state/shared/wip-archive/<branch>-diverged.diff` + name the chat on `AGENT_CHAT.md` BEFORE its `reset --hard`. Then: in `H:/prism` `git fetch && git reset --hard origin/cad-fusion-live-ms0`; for each of 40 worktrees — `rm -f <wt>/.git/index.lock` if >1 h old OR (if <1 h) the owning chat is confirmed-dead on `AGENT_CHAT.md` (no heartbeat >5 min), logging which; **`S=$(git -C <wt> stash create); [ -n "$S" ] && echo "<wt> $S" >> state/shared/wip-archive/u-gc-14-prereset-stashes.log`** (a pre-reset working-tree snapshot — `stash create` doesn't push to `refs/stash`, so it's safe per `feedback_no_git_stash_shared_tree`; recoverable via `git -C <wt> stash apply $S`); then `git -C <wt> fetch && git -C <wt> reset --hard origin/<branch>` — for worktree branches NOT on origin: push them first or `git -C <wt> rebase --onto <new-base> <old-base>`, never `reset --hard <old-sha>`; **`rm -f <wt>/.git/objects/info/commit-graph` (lazy rebuild — the old one references dead SHAs)**; report every worktree it couldn't reset; replicate `.git/info/exclude` + `.git/hooks/` into each re-clone (only matters for pre-U-GC-03 backups; the committed `.gitignore` covers `JM DIE/` going forward); **enumerate other clones** — does `C:/PRISM/` exist? laptops? CI checkout caches? — re-clone/hard-reset each AND `git -C <clone> remote set-head origin --auto` (the symbolic-ref `origin/HEAD` still points at `cad-fusion-live-ms0` which is fine until U-GC-17 renames it, but `--auto` makes it self-correct); `rm state/shared/.git-freeze-active state/shared/.git-freeze-branch-tips.json`, **remove `git-maintenance-freeze.mjs` from the file AND `bash-bundle.mjs` SUB_HOOKS in the same commit**, `unset PRISM_DISABLE_GIT_SYNC PRISM_GIT_FREEZE_OPERATOR`, `git config --unset gc.auto`, **re-register `git maintenance` if U-GC-04 decided to (or leave unregistered)**; post on `AGENT_CHAT.md` "history rewritten — `git fetch && git reset --hard origin/<branch>` before any commit; `git remote set-head origin --auto`; your `git-sync-stop` will refuse to push until you do." *Acceptance:* `H:/prism` at the new HEAD, clean; every worktree clean + correct upstream + no stale commit-graph; `git push origin cad-fusion-live-ms0` → "up-to-date"; `node --check bash-bundle.mjs` after the freeze-hook removal; **the operator's Stop succeeds after the removal commit** (if `stop_on_hook_unregistration` blocks it, the documented escape is the 3-strike auto-pass); spot-check 3 chats `fetch && reset --hard` cleanly. *Owner:* operator + coordinated.

### PHASE 4 — Trunk reconciliation (after the rewrite; on a fresh `work/trunk-reconcile-<date>` branch)
- **`U-GC-15a/b/c`** — analyze the 3 (4) disjoint histories: (a) `main` 874c, (b) `master` **1326c** (largest), (c) `claude/*` roots (~200c + ~1300c). For each: per-subsystem `git diff cad-f <other>` + `git log cad-f..<other>` → classify each cluster: superseded / salvageable-from-`archive/forge-orphans` / **wanted (with a concrete "feature X reachable / behaves" acceptance for each wanted cluster — not just "vitest green")**. Output `state/shared/MAIN-MASTER-CLAUDE-UNIQUE-WORK-AUDIT.md`. *Owner:* one chat + user reviews.
- **`U-GC-15(decide)`** — Path B (zero wanted clusters) or Path C (any wanted).
- **`U-GC-16`** — execute. *Path B:* `git tag archive/{main,master,claude-fervent-bohr,claude-zen-dirac}-final-<date> <branch>`, push the tags, then `git branch -D` them (tags keep the commits reachable; `git branch -a` stays clean; tags aren't auto-pushed by `--all`). *Path C:* `git checkout cad-fusion-live-ms0 -b work/trunk-reconcile-<date>`; `git merge --allow-unrelated-histories <wanted-trunk>`; **if `git merge` reports >~200 conflicted paths → abort, fall back to per-cluster cherry-pick of the `U-GC-15`-identified wanted commits onto a fresh branch**; resolve conflicts **per-cluster, reviewed against `U-GC-15`'s audit** (NOT a blanket "favor cad-f" — that would drop the wanted clusters; "newer-wins" is the wrong rule when cad-f never had the feature); the "test" gate = **each wanted cluster's concrete acceptance from `U-GC-15` passes**, plus vitest-green + physics/test reviewer agents; then merge `work/trunk-reconcile-<date>` → cad-f → push (cad-f is pushable post-P3); archive the merged-in trunk as a tag; **document in GIT-TOPOLOGY.md: "history doubled on <date> via Path-C merge of <trunk> — intentional; the count-consuming generators (BUILD_STATE, MILESTONE_PROGRESS, system-viz Lgit) now see ~2× commits"**. *Owner:* one chat; reviewer agents validate.
- **`U-GC-17`** — promote `cad-fusion-live-ms0` → `main` (**if `U-GC-00` ≠ `cad-f`, swap: rename `cad-f`→`archive/cad-f-<date>` and keep `main` as-is — the rest of this unit is symmetric**); re-point everything: `git branch -m cad-fusion-live-ms0 main`; `git push -u origin main`; **set the GitHub default branch to `main` via API/UI** (`git config init.defaultBranch` is repo-local, does NOT change the remote default); `git remote set-head origin main`; re-point all 40 worktrees' upstreams + `H:/prism` + **`git -C <every-other-clone> remote set-head origin --auto`** (the `C:/PRISM/`/laptop/CI clones from U-GC-14's enumeration); `git push origin cad-fusion-live-ms0:refs/heads/archive/cad-fusion-live-ms0-pre-rename-<date>` (rename-on-remote, keep ≥90 days) **then** `git push origin :cad-fusion-live-ms0` (delete the old name — LAST, after every consumer confirmed re-pointed); update `canonical-trunk.mjs`'s default + `PRISM_CANONICAL_TRUNK` → `main`; handle harness `EnterWorktree`-managed worktrees (`ExitWorktree --keep` then re-enter, or re-point upstream — announce). *Owner:* operator + user (for the GitHub default-branch change).

### PHASE 5 — Worktree / branch cleanup (after U-GC-17; SHAs stable)
- `U-GC-18` retire broken `work/xproc-neural` wt (0c; 13 119 untracked archived by U-GC-29a, verified). · `U-GC-19` retire stale-merged (`cost-cascade-ms0`+`graph-context-ms0` *same HEAD* — collapse the dup; `sfc-calibrate`; `cad-complete-ms0`+`psau-sav2` after WIP) — rename-on-remote then delete, user nod. · `U-GC-20` triage the ~25 origin-only branches — **`U-GC-13`'s force-push only touched LOCAL branches → origin-only ones keep the >100 MB blobs → clone size doesn't shrink until handled**; for blob-carrying ones `git filter-repo` a 2nd pass or delete after tag; **`origin/worktree-data-loss-fix` must NOT be touched without user review**; keep `origin/archive-2026-02-01`. Acceptance: `git rev-list --all --objects --remotes | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '$3>104857600'` → empty. · `U-GC-21` build `scripts/git-stale-fork-triage.mjs` (per-branch: ancestor-of-main? last-commit, unique-commit count, file-overlap → heuristic recommendation → pre-filled `STALE-FORK-TRIAGE.md`). · `U-GC-22` user reviews/approves (~24 forks + `claude/*` roots: `intel-ollama-obsidian-ms0` 983u, `lathe-pro-v3-ms2` 497, `mill-master` 489, `ai-aware-harden` 403, `lathe-master` 443, `wedm-consolidated` 427, … decision per branch: revive/merge/archive-tag). · `U-GC-23` exec triage (cherry-pick the "merge" ones onto `main`; tag-then-delete the "archive" ones). · `U-GC-24` retire `worktree-u-fus-api01/02` (api01's 497c via U-GC-22; `mv`/`worktree remove` + per-wt user OK). · `U-GC-25` `claude/*` — folded into `U-GC-15c`+`22`+`23`. · `U-GC-26` `archive/forge-orphans` disposition (keep → document as cold storage; or `git bundle create <off-drive>/forge-orphans-<date>.bundle …` → verify (clone+fsck) → **append the bundle path to `GIT-TOPOLOGY.md` and commit that NOW (document-before-destroy)** → tag → `branch -D` → `push :` → `worktree remove` → `gc --prune` → the location is already documented).

### PHASE 6 — System-viz + git-connected nodes (after U-GC-17; the U-GC-27a AUDIT half already ran in P1)
- **`U-GC-27b`** — the `git-tree-augmentation.json` decision + run: if U-GC-03 classified it "generated → gitignore" (recommended — it's emitted by `generate-git-tree.mjs`), then it's already ignored & untracked; run `node scripts/generate-git-tree.mjs` (or via `regen-viz.mjs`) → it now emits `git.branch.main` + the rewritten commit SHAs (~94 branches → ~12, 40 worktrees → ~12). If kept-tracked instead: regenerate-and-commit it. **Use the `state/shared/git-rewrite-<date>/commit-map` to spot-check that the regenerated commit SHAs are the new ones (no stale `12fe39f2…`-style refs).** *Owner:* the system-viz lane-owner chat (lane-locked — coordinate).
- **`U-GC-27c`** — cosmetic rename-staleness sweep: the 6 git-connected state files (`AGENT_WORKBOARD.json`, `chat-slots.json`, `predicted-collisions.json`, `tribal-embed-index.json`, `wiki-orphans.json`, `mcp-server/data/state/git-health-state.json`) — classify each self-heals-on-next-run vs. needs-a-one-time-fix (`git-health-state.json` caches a divergence verdict against the old branch — clear it); per-chat `HANDOFF-*-cad-fusion-live-ms0.md` files (rename to `…-main.md`? or leave — historical); `~/.claude/commands/{checkin,sync,handoff,six-chat-bootstrap}.md`, `CLAUDE-CODEX-COORDINATION-DIRECTIVE.md` — sweep for branch refs; the 2 wiki lesson nodes `vault.wiki.lessons.cad-fusion-live-ms0-*` — annotate (keep names); `prism_dev`/`prism_session` MCP dispatchers — `grep -ln git mcp-server/src/tools/dispatchers/{devDispatcher,sessionDispatcher}.ts` and fix any branch-aware action; **`gh repo set-default --view` (confirm it's `markjvillanueva3-cloud/PRISMV9` and that `gh pr` now defaults to `main`); `.vscode/`/`.idea/` git state self-heals on reopen — no action, just note it**. Output `state/shared/RENAME-BLAST-RADIUS.md`. *Owner:* one chat + user reviews.
- **`U-GC-28`** — regenerate the whole viz graph + verify: `node scripts/regen-viz.mjs --full` (the real orchestrator — NOT the 6 individual scripts) → confirms `generate-git-tree.mjs` + `merge-augmentations.mjs` run clean against the renamed trunk; verify the `Lgit` layer — **either it's now merged into `system-graph.json` (`node -e "const g=require('./state/shared/system-viz/system-graph.json'); console.log(g.nodes.filter(n=>n.layer==='Lgit').length, g.nodes.some(n=>n.id==='git.branch.main'), g.nodes.some(n=>n.id==='git.branch.cad-fusion-live-ms0'))"` → ~695, true, false) OR (if the pre-existing merge break isn't in scope to fix here) U-GC-29c's GIT-TOPOLOGY.md documents "the Lgit layer is generated by `generate-git-tree.mjs` but not currently merged into the live graph — known break, tracked separately"; confirm `install-system-viz-git-hook`'s hook still fires + doesn't auto-commit the (now-ignored) graph; **verify `node scripts/build-state-snapshot.mjs && node scripts/build-milestone-progress.mjs && node mcp-server/scripts/generate-claude-brief.mjs` all exit 0 and reference `main`** (these are NOT viz scripts — a bad U-GC-27/U-GC-06 edit to them isn't caught by the viz regen); `node scripts/system-viz-query.mjs headline` → fresh, references `main`; `state/shared/system-viz/system-graph.json` gitignored (not staged) **AND `git -C <every-worktree> check-ignore state/shared/system-viz/system-graph.json` passes** (every worktree has the P0 commit — gate this before the regen). *Owner:* the system-viz lane-owner chat.

### PHASE 7 — Verify & document
- **`U-GC-29`** — partial verify after P0+P1 (checkpoint before the irreversible P3): `git fsck --connectivity-only` clean; `git count-objects -vH` size < 5 GiB; `git commit-graph verify` clean; `worktree.baseRef`=head in the harness settings (+ mirrored to H:); `trunk-guard`/`worktree-commit-route` behave; `verify-hook-refs --quiet` clean; `U-GC-29a` green; `U-GC-EMERGENCY-RESTORE` script written + dry-run-passes + the single-branch-refusal control trips; `install-system-viz-git-hook` confirmed not auto-committing ignored artifacts; `git maintenance` task checked (unregistered or recorded). Confirms 42→~4 GB landed.
- **`U-GC-29b`** — full verify after P6: `git fsck --full` clean; `git count-objects -vH` size < 2 GiB / loose < 10 k / packs ≤ 2; verify no >100 MB blob at HEAD (`git rev-list --objects HEAD | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '$3>104857600'` empty) AND none on any origin ref (`… --remotes …` empty); `git merge-base --is-ancestor main HEAD` true; `git for-each-ref refs/notes refs/replace` on the rewritten remote matches expectation; `git worktree list | wc -l` ≤ ~12; `git branch -a | wc -l` ~halved; `node scripts/regen-viz.mjs --full` exit 0; the freeze hook removed (file + bundle entry); `git config --get gc.auto` (decided value); spot-check 3 chats `fetch && reset --hard` cleanly. **THE OUTSIDE-VALIDATION CHECK (the only one that proves the whole exercise worked):** `git clone --depth=1 https://github.com/markjvillanueva3-cloud/PRISMV9.git /tmp/prism-fresh && du -sh /tmp/prism-fresh/.git` → < ~500 MB shallow; `cd /tmp/prism-fresh && git fetch --unshallow && du -sh .git` → < 2 GB; then a trivial round-trip: `git checkout -b _gc-roundtrip-test && echo "" >> README.md && git commit -am "test" && git push origin _gc-roundtrip-test` → no GH001 → `git push origin :_gc-roundtrip-test` (delete); `rm -rf /tmp/prism-fresh`. (NOTE: `git push --dry-run` does NOT trigger GH001 — this real push is the proof.)
- **`U-GC-29c`** — document + memory: `state/shared/GIT-TOPOLOGY.md` (trunk=`main`; worktree→branch map; retired branches & tags; the `worktree.baseRef=head`+`trunk-guard`+`[MAIN]`-restriction model; off-drive backup locations & retention floor (= the U-GC-29b sign-off); the `forge-orphans` bundle location if retired; the `commit-map` location; if Path C: the history-doubling note; the Lgit-merge-break note if not fixed; the `git maintenance` decision; the `.git/hooks` audit result; `core.autocrlf` decision); update `H:/PRISM/CLAUDE.md` ("Lane discipline"/"PER-CHAT HANDOFF"/"Main branch … main" — verify the last is auto-generated and now correct); `reference_git_topology.md` memory + `MEMORY.md` pointer; mark this roadmap `COMPLETE`; schedule the off-drive backups' eventual removal *after* the U-GC-29b sign-off + a long grace (`leave-a-copy-behind-guard` flags premature removal).

---

## FAILURE-MODE REGISTER (F1–F37)
F1 filter-repo refuses w/ linked worktrees → fresh `--single-branch` mirror. F2 `reset --hard` destroys WIP → `U-GC-29a` hard prereq + re-run before U-GC-14 + per-worktree `stash create` snapshot in U-GC-14. F3 live chat breaks mid-commit → `U-GC-10`'s env-gated freeze hook + the window announce. F4 `git stash` clobbers peers → `U-GC-08` removes the stale doctrine; U-GC-14 uses `stash create` (no `refs/stash` push). F5/F21 `master` retired without diff-review → `U-GC-15b` its own analysis unit. F6 unrelated-history merge conflict-fest → staging branch, abort-threshold (>200 paths → cherry-pick fallback), Path B default. F7/F18 graph regen/re-commit during the freeze → `.gitignore` lands P0 (incl. `system-graph.json`); freeze hook blocks commits; `install-system-viz-git-hook` confirmed not auto-committing (P0); U-GC-28 gates "every worktree has the P0 commit". F8 forge-orphans deleted lost → never the delete-path without the verified off-drive bundle whose location is committed first (U-GC-26). F9/F13/F14 single same-portable-drive backup → ≥2, ≥2 volumes, verified, kept until the U-GC-29b sign-off, before the gc + `.git/info/exclude` + `.git/hooks/` copied alongside + locations committed as made. F10 trunk-guard blocks everything → warn-only, fail-open. F11 gc corrupts a pack via concurrent worktree op → `gc.auto 0` + window + announce-reflog-recovery-first. F12 `/tmp`(=C:) out of space cloning 42 GB → gc before the clone; `--single-branch`; assert ≥1.5× free; fsck the clone. F15 filter-repo dies mid-run → throwaway mirror; recovery = delete+re-clone+retry. F16 force-push partial → push critical ref first + verify; `git ls-remote origin` must match the mirror before unfreezing. F17 chat slips a commit during the freeze → the env-gated freeze hook + the **branch-tip-snapshot diff** slip-check in U-GC-14 (covers local-only branches the old `origin/<b>..<b>` check missed). F19 worktree branch rewritten differently / not on origin → pre-build the worktree→origin-ref map; push local-only first or `rebase --onto`, never `reset --hard <old-sha>`. F20 concurrent git op during U-GC-05's gc → `gc.auto 0` + window. F22 blind `--strip-blobs-bigger-than` → inventory + user review first. F23 `push origin :cad-f` before consumers re-pointed → push `main` first → re-point all → keep `origin/archive/cad-f-pre-rename` → delete last. F24 `.py` >20 MB stripped → folded into F22. F25 filter-repo prunes commits that were only `system-graph.json` (incl. HEAD) → `--prune-empty=never` + `rev-list --count HEAD` before==after. F26 stale `index.lock` blocks the reset loop → `rm -f` >1h-old (or dead-chat <1h) + report failures. F27 the roadmap file's SHA moves under the operator post-rewrite → keep a flat copy outside `.git`. F28 SessionStart/Stop hooks fire during the rewrite → `PRISM_DISABLE_GIT_SYNC=1` + the freeze hook (which now also blocks `pull|fetch`) + no chat starts/stops during the window + the window announce so chats idle. F29 a generator hardcodes `cad-fusion-live-ms0` → `U-GC-27a-AUDIT` (P1) greps + the fix list (U-GC-06/27c) + `U-GC-28` verifies a clean regen. F30 `install-system-viz-git-hook` auto-commits the graph → permanent blob-back → checked in **P0** (a P0 fix if true). F31 `.git/info/exclude` lost in a re-clone → `git add -A` in U-GC-29a's abandoned-worktree path stages `JM DIE/`'s 24 545 files → **U-GC-03 moves the 3 lines into committed `.gitignore`** (eliminates it); replicate-into-clones stays as belt for pre-U-GC-03 backups. F32 Path-C "favor cad-f" silently drops the wanted clusters → per-cluster conflict resolution reviewed against `U-GC-15`'s audit; per-cluster semantic acceptance. F33 `file-claim-guard` blocks the operator editing `bundles/*.mjs` on day 1 → check `mcp-server/data/claims/` for a claim on `bundles/`; coordinate the release first. F34 bundle-load failure → silent inactive freeze (the whole Bash matcher no-ops, freeze + commit-ownership-guard + worktree-route all off) → `node --check bash-bundle.mjs` + positive+negative controls in U-GC-10's acceptance, and again on removal in U-GC-14. F35 freeze-flag expiry already past / unparseable → write asserts `expiry > now+15min`; parse-error → active (safe for a freeze) + logged to `.git-freeze-parse-error.log`; hook enforces the expiry (dead operator → auto-no-op). F36 operator locked out by a session-id rotation → operator identity is `PRISM_GIT_FREEZE_OPERATOR=1` env (survives `/compact`, can't be impersonated), not a session-id match. **F37 (NEW) — the `git maintenance` builtin / a Windows Scheduled Task runs `gc`/`commit-graph`/`prefetch` during the P3 freeze** (it's not a Claude-shell `git` invocation → the bash hook can't see it) → **U-GC-04 checks `git config --get-regexp '^maintenance\.'` + `schtasks /query | findstr -i git` → `git maintenance unregister` for the duration of P3, re-decide in U-GC-14.** **F38 (NEW) — `git notes`/`refs/replace/*` silently dropped by a `--single-branch` `filter-repo`** → U-GC-11 enumerates them first; if any exist, `--mirror` clone + explicit `--refs`, or document as intentionally dropped; U-GC-29b asserts. **F39 (NEW) — stale `commit-graph`/`multi-pack-index` on every clone references dead SHAs after the rewrite → `git log` crashes / shows phantoms** → U-GC-05 ends with `commit-graph write --reachable`; U-GC-14's per-worktree loop `rm -f <wt>/.git/objects/info/commit-graph`. **F40 (NEW) — `origin/HEAD` symbolic-ref on *other* clones (`C:/PRISM/`, laptops, CI caches) points at the renamed-away `cad-fusion-live-ms0`** → U-GC-14/17 `git -C <clone> remote set-head origin --auto` on each + the AGENT_CHAT.md post says so. **F41 (NEW) — `git-emergency-restore.mjs` invoked with the `--single-branch` `/tmp/prism-rewrite` mirror → `push --mirror` DELETES every origin ref not in it** → the script asserts `<backup.git>` is the full `--mirror` clone (refs-count ≥ live-origin) and REFUSES otherwise, *before* the push.

## PRISM HOOK-COLLISION SECTION (F-flagged ones above + these)
`asset-deletion-block.mjs` HARD-BLOCK: `rm -rf` under `/.claude/` → use `mv …archive-<date>` (U-GC-04). `stop_on_uncommitted_critical.mjs` HARD-BLOCK: blocks the operator's Stop if any worktree has dirty `constants.ts`/`*Safety*`/`*Force*` → U-GC-29a handles. `stop_on_hook_unregistration.mjs` + `settings-json-addonly-guard.mjs`: the freeze hook + `trunk-guard` go in *bundle SUB_HOOKS* (not top-level settings.json); remove the freeze hook from file + bundle entry in one commit; verify the operator's Stop succeeds after that commit (else 3-strike auto-pass). `file-claim-guard.mjs`: blocks the operator editing `bundles/*.mjs` if a chat holds a claim → check `mcp-server/data/claims/` first (most likely day-1 blocker). `stop_on_content_deletion.mjs`: may block U-GC-03's `git rm --cached` of tracked generated artifacts (esp. `system-graph.json`) → know this before P0; workaround = `_disabled_by:`/exemption or per-commit override. `chat-state-isolator.mjs`: if it rewrites the bash payload, the freeze hook's identity read must use the env var, not a payload session-id field (it does). `git-anti-clobber.mjs.disabled`: exists, worktree-aware — `U-GC-08` notes why/scope; the 40-worktree reset loop serializes through it if revived else run from one operator session. `git-sync-stop.mjs`: "if diverged → DO NOT push" — post-rewrite every chat correctly refuses; U-GC-14 announces. `git-health-guard.mjs`: reads the cached `git-health-state.json` divergence verdict — U-GC-27c clears it post-rename. `leave-a-copy-behind-guard.mjs`: flags premature backup deletion (U-GC-29c — anchored to the U-GC-29b sign-off). `comprehensive-build-enforce`/`scrutinize-before-stop`: the roadmap's commits pass through Stop hooks — `stop_on_failing_tests` needs a fresh green report (fast now — see U-CLI-PERF-04); `stop_on_unwired_assets` sees `trunk-guard.mjs`/`git-maintenance-freeze.mjs` as new hooks → settings-register them in the same commit (via the bundle). Fork-budget: `bash-bundle.mjs` is at 8 sub-hooks; adding `git-maintenance-freeze` → 9 (on the edge per `reference_harness_hang_prevention` — remove it promptly post-P3, which is U-GC-14's job); `trunk-guard` in `sessionstart-bundle` is permanent (one more).

## ROLLBACK PLAN
P0: `git revert` the commits; `git bundle --all` before `U-GC-05`'s gc (that bundle is also one of the U-GC-09 backups). P1: `git revert` the hook/doc commits (also revert the bundle SUB_HOOKS entries — `stop_on_hook_unregistration` blocks a half-revert); `unset` the harness `worktree.baseRef`. P3: **the irreversibility line** — run `scripts/git-emergency-restore.mjs <FULL-mirror-backup.git>` (= the full-mirror-assertion + `git -C <backup> push --mirror origin` + the ls-remote-match assertion + the per-worktree reset loop), written + dry-run-tested in `U-GC-EMERGENCY-RESTORE` *before* P3 (the script REFUSES a single-branch mirror — F41); the freeze hook (now blocking `pull|fetch` too) plus the U-GC-14 branch-tip-snapshot slip-check + the diverged-WIP archive minimize the "a chat already pulled the bad state" window — but it's not zero, so U-GC-14's per-worktree `stash create` snapshot is the belt; if a chat's post-pull uncommitted work is still lost, that's the residual (the freeze hook does NOT block a chat's working-tree edits, only its git mutations). P4: old `main`/`master`/`claude*`/`cad-f` archived as **tags** + `origin/archive/*-pre-rename-<date>` kept ≥90 days — rollback the rename = `git branch -m main cad-fusion-live-ms0; git push origin <archived-sha>:refs/heads/cad-fusion-live-ms0; git remote set-head origin cad-fusion-live-ms0` (+ `remote set-head --auto` on other clones). P5/P6: every "delete" was a rename-to-`archive/`/tag-then-delete; the off-drive backups are still live.

## SEQUENCING
1. **Day 1, no quiesce:** P0 (`U-GC-03` gitignore+untrack incl. `system-graph.json` + `git-tree-augmentation.json` + the `.git/info/exclude`→`.gitignore` move + the `install-system-viz-git-hook` auto-commit check; `U-GC-04` cruft+stray+recovery-ref+`git maintenance` check; the `/sync-rebase --base` clause) + start `U-GC-29a` (dirty-sweep, runs continuously). Then `U-GC-09` (backups — before the gc, locations committed) + `U-GC-EMERGENCY-RESTORE` → `U-GC-05` (gc+repack+commit-graph+manyFiles). Then `U-GC-29` (partial verify — confirm 42→~4 GB; emergency-restore script ready).
2. **After `U-GC-00`:** P1 (`U-GC-06` baseRef+mirror-check+canonical-trunk-lib+trunk-guard+exclusion-list-updates; `U-GC-07` worktree-route trunk-check warn-mode; `U-GC-08` doc reconcile + `.git/hooks` audit; `U-GC-27a-AUDIT` generator rename-safety grep — before the rename).
3. **Schedule a quiesce window** (after `U-GC-29a` green + `U-GC-09` backups + `U-GC-EMERGENCY-RESTORE` ready): P3 — `U-GC-10` window-announce + freeze-on → `U-GC-11` filter-repo (handle notes/replace; preserve commit-map) → `U-GC-12` verify → `U-GC-13` force-push → `U-GC-14` resync+commit-graph-rebuild+freeze-off. `cad-f` pushes after this.
4. **After the rewrite:** P4 — `U-GC-15a/b/c` analyze → `U-GC-15(decide)` → `U-GC-16` Path B/C → `U-GC-17` rename→`main`+retire-as-tags+remote-set-head-everywhere.
5. **After the rename:** P6 — `U-GC-27b` git-tree regen (vs commit-map) → `U-GC-27c` cosmetic sweep → `U-GC-28` regen the whole graph + verify generators (incl. the non-viz BUILD_STATE/MILESTONE_PROGRESS/CLAUDE-BRIEF ones). (P6 before P5's branch deletions so the generators are confirmed working on the new topology.)
6. **Incrementally over weeks:** P5 — `U-GC-18` broken-wt, `U-GC-19` stale-merged, `U-GC-20` origin-only (gates "clone size shrank"), `U-GC-21`→`22`→`23` triage, `U-GC-24` u-fus-api, `U-GC-26` forge-orphans (document-before-destroy).
7. **End:** P7 — `U-GC-29b` full verify + the fresh-clone-is-small + round-trip-push end-to-end test, `U-GC-29c` document+memory.

## OPEN DECISIONS BLOCKING START
`U-GC-00` canonical trunk (rec: `cad-fusion-live-ms0`; if not → swap rename direction) · `U-GC-01`/`U-GC-26` forge-orphans keep-vs-bundle (rec: keep) · `U-GC-02` do the rewrite+force-push (rec: yes; if no → the `lfs migrate` / BFG / squash-snapshot fallback ladder) · `U-GC-15(decide)` Path B vs C (rec: B, post-analysis) · when can the fleet quiesce ~30–60 min (P3) · whether `master` (1326c) and the `claude/*` roots hold anything worth Path C (answered by `U-GC-15a/b/c`).

*(RGS v6 discipline: atomic-first, dependency-ordered, tier-floors, every unit has why/depends/blocks/acceptance/risk/owner, full failure-mode register (F1–F41), rollback per phase, hook-collision section, decision gates as their own units. Engine-centric S2.5/S2.6 deliberately skipped — N/A for git-infra ops. Scrutiny scores: v1 [74/58/68] → v2 [88/74/81] → v3 [SCRUTINY-2 polish] → v4 [this — SCRUTINY-3: 88/86/91, three "phantom-state-on-every-clone" MUST-ADDs + the system-graph.json-is-tracked correctness fix + safety/sequencing polish]. SCRUTINY-3 agents recommend loop 4 be a time-boxed final pass, then ship.)*
