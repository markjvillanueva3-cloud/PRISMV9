# ALPHA /goal — improve AI systems + synergize across all galaxies (2026-06-11)

> Closed-loop bounding of the operator's open /goal (yolo). Survives compaction (R10).

## GOAL
Improve the fleet AI stack (NN/GNN/LoRA/CAG/RAG/hybrids) AND keep it synergized into every
galaxy's Obsidian/PSN/awareness/CLAUDE.md/souls/memories/wikis.

## EVAL gate (how each increment is "done")
1. `state/shared/specs/AI-SYNERGY-AUDIT.md` per-galaxy score (disc/owns/vault/xsub/aware) -- regen `node scripts/audit-ai-synergy.mjs`.
2. Real fail-pre/pass-post tests on any code touched (R9).
3. Live-data numbers (retrieval quality / cache hit / coverage), never "looks fine" (R12).

## STOP per increment
One fleet-wide lift, WIRE->TEST->VALIDATE->APPLY-TO-ALL, committed, then checkpoint. Iterate.

## STATE (grounding, 2026-06-11)
- Synergy-WIRING half SATURATED: audit = strong 34 / partial 0 / weak 0, all dims 100%.
  -> The lever is now SUBSTANTIVE AI quality, not wiring.
- The fleet-wide AI reasoning substrate every galaxy uses: `scripts/lib/galaxy-reasoning-bridge.mjs`
  + RAG `galaxy-context-retrieval.mjs` + hybrid `galaxy-dense-rerank.mjs` + CAG `galaxy-cag-cache.mjs`
  + LoRA `galaxy-lora-emit.mjs`. Improving THIS = improving AI for ALL 34 galaxies at once (R15).
- One concerning AI leg: NN/GNN (#10) AUROC 0.808, full-holdout below gate, SELECTIVE-DEPLOY -> owner india (GPU ref-pool).

## INCREMENT QUEUE
1. [in-progress] CAG+RAG+hybrid retrieval-quality lift in the fleet reasoning bridge (delegated; alpha lane).
2. CAG cache effectiveness / invalidation correctness (token-economy = alpha lane).
3. LoRA feeder coverage/quality (vault->LoRA dataset) across galaxies.
4. NN/GNN ref-pool growth -> coordinate with india (advisory, not alpha-owned).

## DOCTRINE
Coordinator routes (R5), subagents do narrow work; checkpoint at YELLOW (R6/R10); commit gated by
3-of-3 scrutiny; bootstrap-slot-enforce tag for shared-tree commits.
