# Sierra high-leverage node combinations / generation / wiring / bridging — 2026-05-29

Sierra (system-viz) using the graph to direct where the fleet should invest integration effort. Ranked by **leverage = impact / effort**. Graph landscape (live query): 3,273 engines (729 unwired = 78% wired), 97 dispatchers / 8,168 actions, 175 envelope drift, ~9,314 orphan nodes / 980 ghosts. MCP :3100 down → queried via `system-viz-query.mjs`.

## ★ Headline insight (the highest-leverage move sierra is uniquely positioned to make)
**Leverage-rank the 729 unwired engines by graph importance.** `GraphImportanceEngine` (personalized PageRank) already scores node importance; the unwired set is known. The product **`importance(node) × unwired`** = a *leverage-ranked wiring queue* — wire the high-PageRank-unwired engines FIRST (each wire unblocks the most downstream). No one else combines these two signals; it converts a flat 729-item backlog into a leverage-ordered one. **Action:** a small script joining `GraphImportanceEngine` output × the unwired set → ranked queue feeding `/pick-unit`/dispatcher-wirer. Owner: sierra. Effort: LOW (both inputs exist). Impact: HIGH (re-prioritizes the entire fleet wiring backlog).

## WIRING
- **W1 — 9 feature-generators orphaned from regen-viz FAST[]** (they never run → their roost data never reaches the graph). **2 of 9 WIRED + VERIFIED:** `generate-quoting-pipeline-features.mjs` (FAST[]-add alone — runner+splice pre-existed; commit b6fa43575a) and `run-hotel-domain-features.mjs` (the RUNNER, not the pure-lib `generate-*`; fixed a producer/consumer path mismatch — runner wrote to `staging/` but merge `loadOptional` reads VIZ_DIR root; 381 nodes/3 roosts verified; commit 87816bf19c). **7 BLOCKED (U-VIZ-FAST-REGISTER-9):** galaxy, hermes-zebra-ops, milling-tribal-tip-bridge, psn-health, sfc-variability, svi-component, vendor-catalog — NONE has a runner, NONE is in merge's `loadOptional` list, each writes to a non-root path → each needs a runner-to-VIZ_DIR-root **plus a NEW merge splice block**. **Blocker:** merge-augmentations OOMs (exit 134) on this 548MB-graph host → a new splice can't be end-to-end verified here; adding 7 unverifiable graph-mutating splices = half-build (R12). Needs a higher-memory host or a sharded/testable merge. Leverage HIGH / effort now MED-HIGH per-gen (runner+splice, not a one-liner). Owner: sierra + content peers.
- **W2 — 729 unwired engines** → feed the ★ leverage-ranked queue. Owner: domain slots (dispatcher-wirer agent).
- **W3 — 175 envelope drift** (claimed vs derived status) → `/envelope-sync` reconcile. Leverage MED / effort LOW.

## BRIDGING (cross-galaxy / cross-domain — the "synergize the galaxy" value)
- **B1 — Asymmetric PSN edge: india ↔ system-viz.** Sierra's CLAUDE.md declares "india CONSUMES sierra's seed-ghost ref-pool + embeddings"; india's CLAUDE.md should declare system-viz back (verify + reciprocate). Leverage MED / effort LOW. Owner: sierra+india (chat-bus ping).
- **B2 — bridge-synergy roost (16 deep-integration + 26 wiring bridges)** already curated: SFC→6 CAM bridges, CAD↔CAM AI, 3-tier AI hierarchy, closed-loop learning, ERP, operator gates. These are the highest-impact cross-domain wires. Owner: respective domain pairs; sierra keeps the roost current.
- **B3 — Cross-dispatcher bridges** (manufacturing[29] ↔ ai_intel[22]): e.g. `toolpath`→`ml`, `safety`→`omega`, `cam`→`aiReasoning`. Surface candidate bridges where a manufacturing dispatcher's output feeds an AI dispatcher's input.

## GENERATION
- **G1 — materialize the 8 pending generators** (overlaps W1) → +8 domains' roost nodes once verified+wired.
- **G2 — leverage-queue as a roost** (optional): emit the ★ ranked wiring queue as a `ghost.leverage_queue` roost so it renders color-coded in `/system-viz`. Requires the dual-registration discipline (FAST[]+merge); defer until the queue script exists.

## NODE COMBINATIONS
- **N1 — the system-viz engine cluster as a unified capability. ✅ SHIPPED (commit 1e11fa0642).** `RankedHybridGraphSearchEngine` (new) composes `MasterIndexEngine.query` + `HybridIndexEngine.fuse` into **ranked-hybrid-graph-search**: each master-index hit carries `confidence` (lexical) AND `utilization` (structural-importance proxy); RRF-fuse the two rankings so a structurally-important hub out-ranks a high-confidence isolated match. Wired to `prism_session:master_index_ranked_hybrid` (distinct axis from `hybrid_search`, which fuses across the 4 SOURCES). OOM-safe — reuses MasterIndex's cached index; `GraphImportanceEngine.rankByTask` (true PageRank) is the documented upgrade but OOMs on the 548MB graph, so `utilization` is its proxy. 13 tests + 2-of-2 reviewer PASS. **Follow-up N1.1:** swap in GraphImportance PageRank for list B once a higher-mem host or sharded-graph reader exists.
- **N2 — hub × orphan pairs. ⛔ MERGE-OOM-BLOCKED (verified 2026-05-29, reverted).** Built+tested a correct orphan→hub pairer (10/10 synthetic tests) but the OOM-safe `architecture-graph.json` is the WRONG substrate: its `subgroup` is a TYPE bucket (`dispatcher`/`registry`/`fs`), not a domain cluster, so "same-subgroup anchor" = nonsensical (dispatcher→dispatcher); and the documented orphans (AISubsystem etc.) + per-engine call-edges live only in the 548MB merged graph (OOM exit-134). Meaningful anchoring needs domain-clustered subgroups + engine↔dispatcher edges = merged graph. **Same keystone blocker as W1.** The `needsTarget` orphans are GNN tier-5's input (india owns). Unblocks-both fix: streaming/sharded merge or a domain-clustered OOM-safe export. Memory: [[reference_sierra_n2_wrong_substrate_2026_05_29]].

## Done this session
- Wired `generate-quoting-pipeline-features.mjs` into regen-viz FAST[] (syntax-checked; materializes 24 nodes on next regen).
- This report = the FIND deliverable (ranked opportunity map). The ★ leverage-ranked wiring queue (N1/W2 join) is the single highest-leverage follow-up — small script, sierra-owned, re-prioritizes the whole fleet backlog.

_Cross-refs: [[reference_sierra_regen_fast_registration_gap_2026_05_29]] · `SIERRA-GALAXY-COMPLETENESS-ASSESSMENT-2026-05-29.md` · bridge-synergy roost (`scripts/generate-bridge-synergy-features.mjs`)._
