# TSC Domain-Fix Campaign — live state (2026-06-15, slot:papa)

> Operator directive: "do all the domain fixes for the other domains, bypass all gates and blocks."
> Interpretation: bypass OWNERSHIP/lane/claim/galaxy gates (papa is elevated past them). KEEP the universal
> safety rails — NO fabricated physics/material/feed/cost/numeric values, NO `any`/`@ts-ignore`/silencing,
> behavior-neutral on shop-floor, affected-tests, scrutiny. A wrong machine value is worse than a tsc error.

## Goal
Drive the fleet tsc baseline (638 at campaign start, see TSC-BASELINE-REMEDIATION-MAP) to 0 by fixing
DOMAIN-owned engines across all galaxies via TYPE-CORRECT RECONCILIATION only.

## Method (per galaxy, sequential — NOT parallel galaxies)
1. Units staged at `state/shared/tsc-units-<galaxy>.json` (from `tsc-campaign-buckets.json`).
2. Fan-out workflow: reuse script `C:/Users/wompu/.claude/projects/H--prism-slot-papa/2ac3eecf-3fc4-4cd2-90ac-8c73ec7f4bde/workflows/scripts/tsc-fix-hotel-wf_d5a52ec4-62d.js` via `{scriptPath, args:<units>}` + description `[SCOPED] --force-fanout` (fanout-gate override is operator-sanctioned). Agents: `model:'sonnet'`, one per file, strict no-fabrication rules + structured `{file,skipped,fabricatedValue,fixedCount,deferred,notes}`.
3. **BATCH SIZE <= ~12 files** — 32 concurrent tripped BOTH the fork-storm ceiling (799 bash) AND server-side API rate-limiting (24/32 agents failed). 16-concurrent still rate-limits under fleet load; prefer 8-12.
4. tsc gate: `cd mcp-server && NODE_OPTIONS=--max-old-space-size=16384 npx tsc --noEmit` (raw OOMs at 4GB). Authoritative count = `grep -c 'error TS'`. The PreToolUse "0->574" hook is a STALE-TRACKER ARTIFACT — ignore it.
5. **REVIEW GATE (essential — fan-out is reliable but not perfect):** scan reports for `fabricatedValue` (must be 0); spot-check every value/semantic field mapping by reading the diff (caught cogs->total_expenses = net-vs-gross margin bug; caught CADCapabilityMatrix systemic drift). Commit ONLY files tsc confirms 0 + no new. Defer the rest.
6. Anti-sweep before commit (`git status --porcelain`; skip `??`/peer-` M`; hunk-verify). Tight-retry `[MAIN-FORCE] [BUILD-QUALITY-PAPA]/U-TSC-DOMAIN-<GALAXY>` (index.lock storm).

## Progress
- **615 -> 606**: hotel ✅ commit `864a5f5987` (OSHA300Log, ERPIntegration).
- **606 -> 555**: delta batch 1 ✅ commit `0c94d1ce25` (7 files: CADAccuracyValidator, CADLiveBlueprintOcr, CADReasoningChain, CADTokenRepresentation, ImpellerCAD, NeuralCADGeneration, SolidWorksAutomationBridge).
- **555 -> ~521**: delta batch 2 ✅ commit `8dae6bde4f` (11 files: cadDispatcher, CATIA recommend()-API, ElectrodeAdvanced param-typing, HyperCADSLiveBridge, TextToCAD, CADFileClassifier/CADParameterPredictor structural Record keys, +log.info/Dirent/HookCategory). **DELTA GALAXY DONE: 18 files committed; 11 DEFERRED** (HyperCADSCodeGenerator/SolidWorks/InventorCAD = systemic CADCapabilityMatrix interface drift; CadQuery/CADGeometryComparison = invented-logic / no-op executeImpl stub; ElectrodeAIReasoning/TrilobeElectrode = sinker-EDM physics-constant gap; CADAdapterRegistry/CADArchiveJoin/CADPartArchetype/CADRegenerationTest/CADTrialError = cascade follow-ons needing per-file work).
- **521 -> 482**: india ✅ commit `26c2b875b8` (20 files). DEFER: FiveAxisToolpath/FiveAxisDeepLearning (MaterialProps density/specific_heat physics->india), ReasoningChainSharing (:any+invented method), ControllerKnowledge (dialect-field cascade).
- **482 -> 457**: kilo+tango ✅ commit `f13cf93c08` (13 files incl ICADCodeGenerator.ts +4 optional excess-fields = CADCapabilityMatrix systemic enabler). DEFER: Fusion360/MastercamCodeGenerator (CADCapabilityMatrix missing-required CAD-host metadata->delta), IntelligentSequencingAdapter (SequencingResult result-drift: tool_changes_saved count vs tool_change_savings_pct pct + rationale-not-in-type->kilo).
- **457 -> 438 -> 427**: mike/WEDM ✅ commits `358c6bbce7`(batch1 5f) + `2cf953c99e`(batch2 8f). 13 files / 30 errors. DEFER->mike (8 files): WEDMSetupSheet (SEMANTIC BUG caught — agent swapped hasTaper taper->skim; + wire_consumption=0/per_pass=[]/passes_per_profile->num_passes display decisions), WireEDMMachineTechData (method required->optional weakening), WEDMJobCreator+WEDMNeuralTraining (incomplete), WEDMProductionReadiness+WEDMCalculatorAI+WEDMNeuralTraining (AIReasoningDomain wedm_* enum behind DOMAIN_PROMPTS Record exhaustiveness), WEDMSafetyEnvelope (axis envelope-limit VALUES), WireEDMNeuralOrchestration (wire_type/diameter shop-floor values vs 8 ambiguous WireType unions).

### Cross-cutting deferred theme: CADCapabilityMatrix systemic drift
ICADCodeGenerator.ts:297 requires nativeLengthUnit/nativeAngleUnit/requiresSubprocess/typicalLatencyMs but ~10 generators (HyperCADS, SolidWorks, Inventor, Fusion360, Esprit, NX, FreeCAD…) declare notes/maxComplexity/supportsUndo/supportsParametric and omit the required 4. ONE delta/kilo architectural pass (reconcile interface↔generators↔consumers) clears this whole family. Also: many generators' runScriptBody/execute paths call non-existent bridge methods (executeVBA, generateCadQueryCode) — they need real execution wiring, not invented logic.

## ⚠️ CLEAN-BUILD TRUTH (2026-06-16): incremental tsc UNDER-REPORTS — always verify with `--incremental false`
The `.tsbuildinfo` cache reported **3** errors when the real count (clean `npx tsc --noEmit --incremental false`) was **329**. Per-galaxy "X→Y" numbers in earlier commit messages were incremental-based and are approximate; the clean build is the only authority. **Always run `--incremental false` for the campaign gate count.** Per-file `0` on a *dirty* file is still trustworthy (dirty files are re-checked), so committed per-file work is sound — only global trajectory numbers were soft.

## TRUE STATE (clean build): 638 -> 329 -> **269** (2026-06-16 session: infra batches 1-3, -60). See §SESSION 2026-06-16 at the bottom. ALL domain galaxies done: hotel, delta, india, kilo, tango, mike, whiskey, oscar, foxtrot.
oscar+foxtrot DONE (commit c516a27aa6): 10 files. DEFER->oscar: SpeedFeedExhaustiveCombination (combo-space: 'climb' is a cut-direction not a strategy; er32/hsk63 vs ToolHolderType). NEW PIPELINE BUILT: `tsc-fix-verify-wf.js` (fix->adversarial-verify, caught AdvancedAI missing-import + whiskey shank_size swap + thermal-veto weakening).

## REMAINING (329, clean build) = infra bucket + documented owner-defers + 1 peer breakage
- **infra bucket** (re-extract FRESH from `/tmp/tsc-clean.log` — the old tsc-units-infra.json is stale vs current): ManufacturingHooks(7), index.ts(5), ToolCatalogAdaptive(5), cycleSchedulingBridge(5), Waterjet(4), ShopMachineOverlay(4), PostProcessorAISelfAwareness(4), + tail. Run through `tsc-fix-verify-wf.js`.
- **owner-defers (already routed, NOT papa to guess)**: WEDMSetupSheet(48)+WEDMJobCreator(13)+WEDMNeuralTraining(5)->mike; LatheQualityGate(7)+LatheSafetyHooks(4)+LathePostGeneratorDialect+TurningStochasticPlan->whiskey; SolidWorks(7)+HyperCADS+Inventor+Fusion360/Mastercam CADCapabilityMatrix->delta; ProcessIntelligenceRouter(6)->hotel; Electrode/Trilobe sinker-physics->physics-reviewer; FiveAxis MaterialProps->india.
- **PEER BREAKAGE (NOT papa)**: `hermesDispatcher.ts:90` TS1005 ',' expected x3 — fleet build-breaker, flag owner.
**Resume next: oscar+foxtrot** then infra. Use the NEW fix->verify pipeline `tsc-fix-verify-wf.js` (stage-2 adversarial skeptic per file — caught whiskey's shank_size->tool_diameter swap + T_melt_C thermal-veto weakening). Flow: split <=10-12/batch -> Workflow scriptPath -> tsc per-file -> commit verify-PASS+tsc-0 files / revert FAIL+defer.

### whiskey/Lathe DONE (commits 033c1749ad + 1a0790fb89): 29 files, 427->366 (61 cleared)
DEFER->whiskey: LatheQualityGate (semantic-swap shank_size_mm->tool_diameter_mm + T_melt_C req->opt thermal-veto weakening), LatheSafetyHooks (quality->? category ambiguous for safety hooks), LathePostGeneratorDialect (peer mid-edit), TurningStochasticPlan (insertChangeSchedule/wearAccumulation invented methods). NOTE: LatheTribalIntegrationEngine is a peer untracked file.

## DEFERRED — need the owning slot's domain/architecture decision (papa will NOT guess)
- **CADCapabilityMatrix systemic drift** (HyperCADSCodeGenerator, SolidWorksCodeGenerator, +~8 generators): interface at `ICADCodeGenerator.ts:297` requires nativeLengthUnit/nativeAngleUnit/requiresSubprocess/typicalLatencyMs but generators declare notes/maxComplexity/supportsUndo/supportsParametric. Reconcile interface<->generators<->consumers in ONE pass. -> delta/kilo.
- **sinker-EDM physics constants** (ElectrodeAIReasoning, TrilobeElectrode): `EDM_PHYSICS.sinker_spark_gap`/`sinker_duty_cycle` absent from constants.ts (wire-EDM only). -> physics-reviewer-gated unit with cited sources.
- **ProcessIntelligenceRouter** (hotel): imports 3 nonexistent CrossProcess{SpeedFeed,Post,Feature}Bridge modules whose types are in public interfaces. -> create the bridges or redesign the API.
- **RealTimeFinancialSnapshot** (hotel): UNTRACKED orphan; grossMarginPct needs a COGS account classification (IncomeStatement has no cogs field). Left uncommitted.
- **missing-Record-key domain data** (CADFeatureClassifier/CADFileClassifier/CADParameterPredictor): FeatureType/FormatProfile records missing keys (thread_internal/pocket_complex/slot_dovetail/slot_t_shaped; .prt/.dwg/.dxf/.sat/.3dm) — need the real profile/format values. -> delta.

### Shop-floor review-gate catches (why EVERY batch needs human diff review — agents are sonnet)
- **mike WEDMSetupSheet**: sonnet agent changed `hasTaper p.type==="taper"` -> `p.pass_type==="skim"` to force compile. taper≠skim — silently corrupts operator setup-sheet tip selection (skim passes are on nearly every job). Caught by reading the DIFF, NOT the structured `fabricatedValue` flag (which was false). LESSON: semantic-value swaps that satisfy tsc evade the no-fabrication flag; only diff review catches them. Same class earlier: cogs->total_expenses (net-vs-gross margin).
- **AIReasoningDomain / exhaustiveness trap**: adding a union member to AIReasoningDomain breaks `DOMAIN_PROMPTS: Record<AIReasoningDomain,string>` (PRISMIntelligenceLayer.ts:106) — forces authoring prompt content OR semantic domain remap (behavior change). Same class as CADCapabilityMatrix missing-required. **Enum extension behind a Record/satisfies guard is NEVER additive-safe -> defer to owner.**
- **required->optional weakening** keeps recurring (WireEDMMachineTechData.method, FiveAxis density). It is on the papa refuse_list — defer unless it provably reflects reality AND a discriminated union is the real fix (owner's call).

## Resume
Read this file + `tsc-campaign-buckets.json`. For the next galaxy: `cat state/shared/tsc-units-<g>.json` -> Workflow{scriptPath above, args} -> tsc -> review -> commit. Companion: TSC-BASELINE-REMEDIATION-MAP + TSC-PER-SLOT-PUNCHLIST. Memory: [[reference_papa_tsc_generic_seam_2026_06_15]].

---

## SESSION 2026-06-16 (slot:papa, claude-a59e4a3e) — infra campaign 329 -> 269 (60 cleared, 3 MAIN-FORCE commits)
Clean-build baseline reconfirmed 329 at session start (MCP bridge dead all session -> direct node fallbacks). Method: HookExecutor.ts shared-seam errors fixed ROOT-CAUSE by papa (1 edit clears many; avoids per-file agent races on the shared producer); independent infra files via `tsc-fix-verify-wf.js` harness (sonnet fix -> sonnet adversarial-verify); Opus diff-review gate on every result; per-batch clean tsc `--incremental false` + new-error-file regression diff before each commit.

### HookExecutor.ts authored-but-undeclared seam (root-cause; ~9 hook files fully cleared)
`src/engines/HookExecutor.ts` HookDefinition/HookCategory lagged what hook authors wrote. Added (ALL additive; verified no `Record<HookCategory>` / exhaustive switch / satisfies guard exists -> safe; each verified genuinely authored):
- HookCategory union: + `"quality"` (b1) + `"data-quality"` (b3, asserted by resourceIntegrityHook.test).
- HookDefinition: + `condition?:(ctx)=>boolean` (b1) + `timeoutMs?:number` (b3). Both are authored gating/timeout config currently UNCONSUMED by HookExecutor (advisory; LATENT: hooks that set `condition` still fire unconditionally -> wiring it in = separate behavior-changing unit for the hook owner).
- Fully cleared by the seam: ManufacturingHooks, SafetyQualityHooks, RecoveryHooks, ControllerHooks, SchemaHooks, NLHookEngine, resourceIntegrityHook.

### Commits
- `df56fd140c` BATCH1 (329->290): HookExecutor seam + index.ts (SDK McpServer->Server stub-boundary `as unknown as` casts + zero-arg producer alignment), cycleSchedulingBridge (event-bus payload casts + dead `getSlots?.()->[]`), ToolCatalogAdaptive (`unknown`->typeof guards; **papa TIGHTENED** with `&& truthiness` to preserve the boost score - blocked the agent's 0/empty edge-case behavior shift), PostProcessorAISelfAwareness (`?? null` + additive `dependencies?` on ReasoningStep).
- `b7f00bae5f` BATCH2 (290->276): calculatorProgrammingCatalog (drop NodeNext `with{type:json}` import-attr), ContentIngestionPipeline (null-guard), AgentWorkflow (typeof-narrow improvement), ConnectionMaterializer (catch `instanceof Error` narrow), SessionStability (`number[]` annot), AgenticLoop (`confidence`->`current_confidence` same-semantic field).
- `9e9028b031` BATCH3 (276->269): HookExecutor seam2 + AutomatedTaskDelegator (`?? null`), HookCreationGuard (`validateInput`->`validate` abstract impl, no broken callers), OutcomeFeedbackOverrideStore (real `unsubscribe` closure -> feedbackBusEngine.unsubscribe), ProgramEquivalentIndex (drop phantom `ProgramToPrintLink`), PeerCommitAuditor (NEW `.claude/helpers/git-log-tail.d.mts` ambient decl).

### Adversarial verify CAUGHT 6 BAD FIXES (reverted, NOT shipped) -- KEEP THIS GATE HARD:
1. **authHttp.ts** -- agent GUTTED 288 lines of working OAuth (authorize/token/revoke/introspect) -> 33-line stub + fabricated SERVER_NAME literals.
2. **SyncCodeVerificationEngine.ts** -- agent restructured a boolean ternary, silently INVERTING signal-detection for the dominant `waitMatch===undefined` path.
3. **aiDispatcher.ts** (UNTRACKED pre-existing stub) -- fabricated model IDs (`claude-fable-5`, `qwen3-coder:32b` != canonical `qwen2.5-coder:32b`) + ModelRouterEngine bypass. FLAG owner (prism_ai routing).
4. **HookDAGValidatorEngine.ts** -- renamed public `validate`->`validateDAG` breaking `hookDispatcher.ts:301` (`.events` on null TypeError); agent's "zero callers" claim was FALSE.
5. **hooks/index.ts** -- dup-export fix SILENTLY DOWNGRADED `preMachineControllerCompatibility` from blocking/critical (MachineValidationHooks) to warning (CrossReferenceHooks) = SAFETY REGRESSION.
6. **AutomatedResourceHarvestingPipeline.ts** -- noop + CRLF full-file noise; `callDocumentAction` not exported by documentLearningDispatcher.

### REMAINING 269 by owner (deterministic route -- PAPA-INFRA is papa's target)
- **PAPA-INFRA** ~31 files / ~50 errs. **batch4 STAGED** (10f/15e: FormulaValidation, ImageOCRPipeline, InfiniteConditionCombinator, MacroFillOrchestrator, SafetyExplanation, MINBatchExtractor, MonolithStockPositionsDatabase, RoadmapIntelligence, RollbackPlanner, SkillExecutor). Then: PDFHandbookBatch(3), UnifiedProgramParser(3), ManufacturerCatalogAI(3 - WATCH catalog values), ShopMachineOverlay(4)+Waterjet(4) [SHOP-FLOOR -> behavior-neutral only], routes/operating-system(1), routes/python-api(1), dataDispatcher(1), PostEmitSafetyGate(1).
- **PAPA-FIXABLE-WITH-CARE (Opus not sonnet -- these need coordinated/careful edits the harness got wrong):** HookDAGValidator (rename `validate`->`validateDAG` in BOTH engine AND `hookDispatcher.ts:301`); hooks/index (give the TWO `preMachineControllerCompatibility` distinct names, keep BOTH); authHttp (add `authorizationUrl`/`tokenUrl`/`scopes` to OAuthConfig + populate in getAuthConfig, OR derive from `config.issuer` like the original); SyncCodeVerification (null-assert preserving the ORIGINAL ternary grouping `(signalMatch && waitMatch!==null)?false:signalMatch`).
- **OWNER-DEFERS:** mike(WEDM 9f/77e), kilo/echo(CAM-post 19f/34e), delta(CAD 16f/30e incl CADCapabilityMatrix systemic), whiskey(lathe 7f/22e), india(AI 6f/14e), hotel(biz 6f/13e), oscar(SFC 6f/9e), physics-reviewer(Electrode/Trilobe/SpecificCuttingEnergy 3f/10e), lima(academy 5f/7e), foxtrot(mill 2f/3e), charlie(quote 2f/2e). AdaptiveSystemIntegration(2): getMill/LatheIntegration arg-count needs ISO-group/flutes/leadAngle PHYSICS values -> physics owner (cannot fabricate).

### Resume next (papa): batch4 (staged) via harness -> careful-papa items -> iterate to PAPA-INFRA=0. GATE EVERY BATCH: clean tsc `--incremental false` + new-error-file diff before commit. Memory: [[reference_papa_tsc_infra_campaign_2026_06_16]].

## SESSION 2026-06-16b (slot:papa, claude-a59e4a3e cont.) — batch4a 269 -> 262
Clean baseline RECONFIRMED 269 at session resume (caught a false-zero from a `2>` redirect — tsc writes diagnostics to STDOUT not stderr; always `> log 2>&1` or capture stdout). MCP dead all session -> node fallbacks.
- **269 -> 262**: batch4a ✅ commit `9d63fba1e7` (5 files, 7 errs, 0 new): FormulaValidationEngine (removed spurious 5th/4th args to kienzleForce/taylorLife + dropped an as-any), InfiniteConditionCombinatorEngine (vectorStringFields() projection helper replacing illegal casts), minFileSchema (ROOT-CAUSE z.infer->z.input clears MINBatchExtractor:460 call-site), MonolithStockPositionsDatabaseEngine (keyof StockBounds narrowing), RollbackPlannerEngine (step.description->step.summary same-meaning). All 5 PASS sonnet adversarial-verify + Opus diff-review; normalized-signature regression diff EMPTY.
- **harness concurrency lesson REAFFIRMED**: 10-file fanout -> ALL 10 failed instantly (22s) on server-side rate-limit ("not your usage limit") under fleet load (8 active slots). 5-file fanout succeeded (288s). Under live fleet load, cap at ~5 concurrent, not 8-12.
- **262 -> 255**: batch4b ✅ commit `58575a3f46` (4 files, 7 errs, 0 new): ImageOCRPipeline (log.info zero-arg->msg), MacroFillOrchestrator (description?-optional + idUndercut maps REAL existing length_in/depthBeyondFinish_in->unsuffixed via the function's established diameter_in->diameter convention, no arithmetic/no units risk), SafetyExplanation (SAFETY behavior-neutral: key as keyof VetoParams narrowing), SkillExecutor (additive metadata? on SkillLoadResult). **RoadmapIntelligence REVERTED** -- adversarial-verify FAIL: agent used `"milestone" as unknown as DecisionCategory` (= as-any double-assert; milestone absent from DecisionCategory union). Proper fix = add "milestone" to DecisionCategory union in DecisionReasoningEngine.ts (careful-papa/owner item).
- **BATCH4 COMPLETE**: 10 files attempted (4a 5 + 4b 5), 9 fixed (269->255, -14), 1 deferred. The adversarial-verify gate caught 1/10 bad fix (RoadmapIntelligence) -- gate is load-bearing, keep it.
- **HARNESS CONCURRENCY (reaffirmed)**: 10-concurrent fanout = instant fleet-wide rate-limit under load; 5-concurrent succeeds. Cap at 5 while fleet is busy.
### Resume next (clean baseline NOW 255): batch5 infra (PDFHandbookBatch/UnifiedProgramParser/ManufacturerCatalogAI[WATCH catalog values]/ShopMachineOverlay+Waterjet[SHOP-FLOOR behavior-neutral only]/routes/operating-system/routes/python-api/dataDispatcher/PostEmitSafetyGate) via the 5-file harness -> then PAPA-FIXABLE-WITH-CARE Opus hand-edits (HookDAGValidator validate->validateDAG in engine+hookDispatcher:301; hooks/index dup preMachineControllerCompatibility distinct-name; authHttp OAuthConfig fields; SyncCodeVerification ternary; RoadmapIntelligence DecisionCategory union +milestone). GATE EVERY BATCH: clean tsc `--incremental false` (capture STDOUT not stderr) + normalized-signature regression diff before commit.

## OVERNIGHT PUSH 2026-06-16c (slot:papa, claude-a59e4a3e) — operator: "run autonomously tonight to fill all remaining tsc errors"
Worklist: `state/shared/tsc-worklist.json` (94 fixable / 6 physics-value-defer). Stager: `/tmp/stage.mjs` (pulls per-file errs by basename). Loop = ≤5-file harness → Opus diff-review → clean-tsc gate (per-file isolation, NOT global count — shared tree has live peer drift HyperCADS/erp/cad-validation/etc.) → anti-sweep commit (my files only) → revert FAIL/defer.
- **255 -> 252**: W1 `827abe1eae` (ShopFloorCheckInEngine recordTaskEvent test-spec'd method, dataDispatcher MaterialProperties keys, ContentBrief catch-guard). REVERT python-api (category->operation OPERATION_MODIFIERS physics-pipeline semantic-swap). SKIP PostEmitSafetyGate (peer untracked).
- **252 -> 246**: W2 `098f92dc23` (DocumentControl/E2Shop/MultiERP/LocalLearning/StockSize — all undefined-guards/closure-narrow/unknown-narrow, behavior-neutral fall-through).
- **246 -> 242**: W3 `4889692bab` (MeasureSummary Zod-v4 z.record 2-arg, Runbook closure-narrow, MotionControllerInjection +Injection.type discriminant, WorkSurfaceScaffold parse-capture). REVERT JMDieProgramAnalyzer -- harness fix MOVED the error (cleared TS2339 but introduced TS7022 self-ref implicit-any); **sonnet verify is diff-only (no compile) so it misses harness-INTRODUCED tsc errors -- the per-batch clean-tsc gate is the load-bearing catch**. JMDie defer: needs `const prevMaxRpm: number|null` explicit annot (careful-Opus).
- **LESSON**: verify catches semantic-swaps/weakening (read-the-diff); the clean-tsc gate catches new-error-introduction (compile). BOTH required. ~2/19 harness fixes bad so far (python-api swap, JMDie new-error) — gate held.
- **242 -> 236**: W4 `156f938945` (ToleranceAware/CADGeometry/DFMAware TS2720 implements->extends BaseEngine: value-import + constructor super(info) + executeImpl delegating to existing public method; MillDeviation dead-guard). DFMAware import hand-fixed by papa (harness left `import type`). REVERT ChatterStabilityLobe (removing `new` on StabilityLobeDiagram singleton un-masked 19-err API mismatch -- .lobes/.sweet_spots/.unconditional_limit/.chatter_frequency absent from real StabilityLobeOutput).
- **236 -> 233**: W5 `59941d8aff` (CADArchiveJoinAugmenter Array->Set coercion; CADTrialErrorLearning index-sig widen + CategoryAggregate annot). REVERT 3 net-zero (un-masking): CADAdapterRegistry (L97 fix surfaced L48 TS2769), CADPartArchetypeRegistry (L37 Zod fix surfaced L53 TS2322), CADRegenerationTest (L318 casing fix un-masked L341 maxRetries-not-in-EmbeddingBackend).
- **UN-MASKING PATTERN** (key overnight lesson): on cascade files an error-typed `any`/`never` shadows downstream errors; clearing the first SURFACES the 2nd -> single-fix-per-file nets zero. These need ALL-errors-at-once or careful multi-fix. The clean-tsc gate catches it (verify cannot -- diff-only). Recurring high-yield CLEAN classes: Zod-v4 z.record(z.string(),...), closure-narrowing (const capture before callback), resolveMaterial null-guards, casing renames to real exports, TS2720 implements->extends.
- **230 -> 222**: W7 `dc3bfd7a94` (TS2352 validated-boundary `as unknown as` double-casts -- CrossProcessAIBridge x3 orchestration, CrossProcess Mediation/Counterfactual/DoCalculus CausalDAG at Zod passthrough, HSMAdvisorComparatorBridge introspection).
- **222 -> 212**: W8 `cd9618af8d` (PPJobScenarioAdvisor optional scenario field clears PPAGIReport; PPInlineCornerBreak number|null annots; PPSinkerEDMPost orbit_radius_mm rename; PPControllerAdaptation +tapping clears PPEndToEnd). REVERT PPGDialectRanker (controller_family fix un-masked L233 operation_types).
- **ANTI-SWEEP LESSON (shared tree)**: the index can carry CONCURRENTLY peer-pre-staged files; `git add <mine>` does NOT unstage them, so `commit` sweeps them. W8 swept 3 peer files (.gitignore + 2 vault-lessons-to-lora scripts), caught + fixed via soft-reset+unstage+recommit (`cd9618af8d`). GOING FORWARD: `git reset -q` (clear index) BEFORE `git add <mine>` each commit; verify `git diff --cached --name-only` == exactly my files before commit.
### Overnight resume (baseline 212, 43 cleared 255->212 over 8 waves): continue 5-file harness waves over `tsc-worklist.json` fixable[]; SKIP cascade/un-masking files; big domain (WEDMSetupSheet 48, WEDMJobCreator 13) per-file not batch; physics-value-defer[6] stay deferred (Electrode/Trilobe/FiveAxis/AdaptiveSystemIntegration/PPValidatorAGIWiring/QuotingMaterialBridge -> physics-reviewer). CAREFUL-OPUS PILE (need 2nd coordinated fix or semantic decision): ChatterStabilityLobe (SLD API map), CADAdapterRegistry+CADPartArchetypeRegistry+CADRegenerationTest (downstream 2nd err), PPGDialectRanker (operation_types+controller_family together), JMDie (const prevMaxRpm:number|null), python-api (category->keywords[]), LectureNoteExtraction (real executeImpl dispatch not courseId2), RLCAMFeedback+OfflineRL (contract/enum), HookDAGValidator, hooks/index, authHttp, SyncCodeVerification, RoadmapIntelligence DecisionCategory union, CADCapabilityMatrix systemic (SolidWorks/Inventor/Fusion360/Mastercam/HyperCADS/NX). KNOWN HIGH-YIELD CLEAN classes (prioritize): Zod-v4 z.record(z.string(),...), closure-narrow const-capture, resolveMaterial null-guards, TS2561 did-you-mean SAME-meaning renames, TS2720 implements->extends (value-import+constructor+executeImpl-delegate), TS2783 dup-property removal, additive producer fields, validated-boundary `as unknown as`.

## RESUMED SESSION 2026-06-17 (slot:papa, after 2:40am session-limit reset) — AUTHORITATIVE baseline 209
W9 `37719888b7` (6 files): SprutCAMBridge (TS2783 dup-key spread-reorder) + **W7 RECOVERY** -- commit `dc3bfd7a94` (W7 CrossProcess x4 + HSMAdvisor TS2352 double-casts) was DROPPED from history during the session-limit-gap branch reset; its verified edits survived in the working tree and were re-committed here. All 6 re-gated 0-error.
- **GIT-STATE LESSONS (resume):** (1) a multi-hour session-limit gap = peer branch resets can DROP your uncommitted-but-thought-committed work -- re-verify `git log` for your commits on resume; working-tree edits survive even when the commit doesn't. (2) `git-add-lane-guard` ARMS on resume (re-binds papa->slot/papa) and blocks `git add` in the h:/prism main tree -- the working pattern is **`git commit <pathspec> -m ...`** (commits exact paths from working tree, bypasses git add, anti-sweep inherent). This is the pinned main-tree-pathspec decision.
- **DEFERRED this session (need careful Opus / domain owner):** WireEDMMachineTechData (method?: refuse-list weakening -> discriminated union), ControllerKnowledge (deep GCodeDialect cascade -- 12+ undeclared controller-specific G-code fields surface ~4/round; needs ONE-PASS field audit: grep all dialect-literal keys, add all missing as optional), FusionAIOrchestration+SolidCAMAIOrchestration (untouched, agents died at limit; semantic-enum + missing-method).
### AUTHORITATIVE RESIDUAL 209 (clean tsc, 2026-06-17) -- top files: WEDMSetupSheet(48), WEDMJobCreator(13), SolidWorks(7), LatheQualityGate(7), ProcessIntelligenceRouter(6 missing-bridge-modules), WEDMNeuralTraining(5), Waterjet/ShopMachineOverlay/LathePostGenDialect/InventorCAD/ElectrodeAIReasoning/ControllerKnowledge(4 each). NET this campaign 638->...->209. The harness-fixable single-file bulk is CLEARED; residual is the HARD set: (a) big multi-error domain files (WEDM 66 total) = per-file careful, semantic-swap risk; (b) CADCapabilityMatrix systemic (SolidWorks/Inventor/Fusion/Mastercam/HyperCADS/NX) = ONE interface-reconciliation pass -> delta; (c) physics-value defers (Electrode/Trilobe/FiveAxis/SpecificCuttingEnergy/AdaptiveSystemIntegration) -> physics-reviewer NEVER fabricate; (d) cascade files (ControllerKnowledge) = field audit; (e) ProcessIntelligenceRouter missing CrossProcess{SpeedFeed,Post,Feature}Bridge modules = build-the-bridges feature, not a type fix. RECOMMEND: route (a)->mike, (b)->delta, (c)->physics-reviewer, (d)+(e)->papa careful per-file passes (one file per dedicated harness call feeding ALL its errors + verify, NOT 5-file batches).
- **W10 `30d39a44e1`: 209->197 (12 cleared, 5 files):** WaterjetProgramAssembler (additive part_name?/part_number? on WaterjetBaseProfile, ledger-only), LegalGate (null->undefined + class-field-narrowing const), PDFHandbookBatchProcessor+PDFTableExtraction (re-export ExtractedTable + source.name->source.title), PostProcessorDeepAIHardening (fanuc_31i->fanuc / siemens_840d->siemens_sinumerik model->family canonicalization). REVERT UnifiedProgramParser (probe-enum add clean+no-cascade but ParsedOperation[] literal-assignment L1204 shape still mismatches -> careful full-shape reconcile defer).
- **ControllerKnowledge static-audit finding:** the GCodeDialect cascade is ENTANGLED -- a static key-extract found 109 undeclared string-keys but most (name/manufacturer/category/description/family/version/path/type/notes/gCode) belong to a SIBLING controller-metadata structure in the same file, NOT GCodeDialect. Bulk-add would pollute. Precise fix = iterative-tsc (add only tsc-flagged GCodeDialect-literal excess) over ~5-8 rounds, OR read the file structure to separate the GCodeDialect literals first. Genuinely careful; stays deferred.
### CURRENT AUTHORITATIVE baseline 197 (2026-06-17, after W10). Overnight+morning push total: 255->197 (58 cleared + recovered the lost W7 commit). The single-file harness-fixable bulk is EXHAUSTED; W10 already needed 1 revert (UnifiedParser), W11 in flight on the last small clean-class files. Beyond this the residual is WEDM(66)/CAD-systemic/lathe/physics-defer/cascade -- careful per-file or domain-owner. See top-files list above (re-derive: `node /tmp/tsc-categorize.mjs H:/prism/state/shared/tsc-clean-w10.log`).

## CHECKPOINT 2026-06-17 (slot:papa, claude-a59e4a3e) — baseline 184, papa-safe tier EXHAUSTED
- **W11 `e2cb44b60c`: 197->191 (6 cleared):** CAMRecommend (PartAnalysis.quantity schema-default count), MillJobProfitabilityAnalytics (Partial<MillPortfolioQueryInput> param — .parse() applies defaults next line), AgenticLoop (mapIntentToDomain return string->ManufacturingDomain; invalid "business"/"general"/"cam" -> valid "cost"/"machining" — net CORRECTNESS fix, old invalid strings never matched downstream), CAMAGIMaster (3: GeometryType narrow on mapFeatureToHyperMillGeometry [harness only HALF-applied — added import, left the `: string` sig; papa completed it], drop non-existent TribalTip.operations, MillOrchestrationRequest type->request_type with the fabricated flat physics props [cutting_speed_m_min:100/feed_per_tooth_mm:0.1/...] removed = excess-prop dead literals, behavior-neutral + removes fabricated physics). REVERT NXCodeGenerator (maxOpsPerScript->limits cleared TS2353 but UN-MASKED TS2739 missing 4 required CADCapabilityMatrix fields nativeLengthUnit/nativeAngleUnit/requiresSubprocess/typicalLatencyMs — needs NX domain facts, NEVER fabricate -> delta).
- **W12 `09c9c68c8f`: 191->184 (7 cleared):** LatheSafetyHooks (priority "medium"->"normal" — HookPriority is critical|high|normal|low|background, no "medium"), ManufacturerCatalogAI (HolderReasoningStep union +"observation" — legit step-1 trace type, grep confirms NO exhaustive switch/case on .type so additive-safe), ReasoningChain (hoist query.min_confidence to `const minConfidence` so narrowing survives the .filter closure — clears 1 of 3; the other 2 [captureKnowledge / EventHandler] are method-missing, DEFERRED), SyncCodeVerification (collapse the no-op ternary `signalMatch && waitMatch!==null ? false : signalMatch` to `if (signalMatch)` — provably equivalent since waitMatch is always null post-`continue`; narrows non-null, clears 2x TS18047, removes a real smell).
- **CAMPAIGN TOTAL: 638 -> 184.** Overnight+morning push 255->184 (71 cleared + recovered the lost W7 commit). The single-file harness-fixable + small-clean-class bulk is now EXHAUSTED.

### AUTHORITATIVE RESIDUAL 184 ROUTING (clean tsc `tsc-clean-w12.log`, 2026-06-17) — ~133 of 184 are DOMAIN-OWNER/PHYSICS-GATED, NOT papa-safe to force:
- **-> mike (WEDM, ~77):** WEDMSetupSheet(48), WEDMJobCreator(13), WEDMNeuralTraining(5), WEDMProductionReadiness(3), WireEDMMachineTechData(3), WireEDMNeuralOrchestration(2), WEDMSafetyEnvelope(1), WEDMCalculatorAI(1), WireBreakAutoRethread(1). **WEDMSetupSheet root diagnosed (papa, do-not-fix):** the consumer destructures sheet/cycleTime/confidence from a WEDMGenerateResult and reads a DIFFERENT data model than declared — `cycleTime.cutting_time_min`/`per_pass` (real CycleTimeBreakdown has rough_cut_min/skim_passes_min/thread_wire_min/setup_min/total_time_min), `sheet.num_passes`/`controller`/`program_number`/`submerged`/`num_profiles` (not on SetupSheet), `result.wire_consumption_m`, `confidence.summary`. Mix of nullability (32x TS18048) + shape-mismatch (11x TS2339) + cascade. Fixing requires deciding the canonical WEDM setup-sheet/cycle-time data model — wrong field mapping = wrong controller/pass-count/submerged/flush values shown to a shop-floor operator (refuse-list). mike owns this.
- **-> whiskey (lathe, ~17):** LatheQualityGate(7), LathePostGeneratorDialect(4), LatheMasterOrchestratorFacade(3), TurningStochasticPlan(2), TurretLayout(1).
- **-> delta (CAD-capability-matrix family, ~18):** SolidWorks(7), Inventor(4), Fusion360(2), Mastercam(2), CadQuery(2), NXGen(1). **SYSTEMIC:** Fusion360+Mastercam+NX all OMIT the 4 required CADCapabilityMatrix fields (nativeLengthUnit/nativeAngleUnit/requiresSubprocess/typicalLatencyMs); only SolidWorks (via ...SOLIDWORKS_CAPABILITY_DETAILS spread) is complete. ONE interface-reconciliation pass with per-CAD domain facts. NEVER fabricate nativeLengthUnit (unit-scale error class).
- **-> physics-reviewer (physics-value defers, ~20, NEVER fabricate):** ElectrodeAIReasoning(4), TrilobeElectrodeGeometry(3), SpecificCuttingEnergy(3), FiveAxisDeepLearning(3), AdaptiveSystemIntegration(2), SpeedFeedExhaustiveCombination(2), ChatterStabilityLobe(2), SpeedFeedNineAxis(1).
- **-> papa careful per-file / domain-confirm (~30, semantic-swap risk):** method-missing class [FusionAIOrchestration getPhysicsProfile (engine has getCuttingRecommendation — semantic guess -> echo/delta), SolidCAMAIOrchestration+PowerMillAIOrchestration selectStrategy (NO such method exists -> kilo/CAM gap), python-api .search->.query (arg-shape; W1 already reverted this file once), ReasoningChain captureKnowledge->captureFromLLMReasoning (LLMReasoningTrace arg-shape)], ShopMachineOverlay(4 machine-spec shape), ControllerKnowledge(4 entangled GCodeDialect cascade — iterative-tsc field audit), UnifiedProgramParser(3 ParsedOperation[] full-shape reconcile), RoadmapIntelligence(1 — category "milestone" is NOT a manufacturing DecisionCategory[machine/tool/holder/fixture/material/operation/strategy/supplier/investment]; DESIGN mismatch, not enum-add), authHttp(3 — reads authorizationUrl/tokenUrl/scopes that exist on NO type in src; auth-security, careful), ReinforcementLearningCAMFeedback(2 TS2554 missing-5th-arg), HookDAGValidator, hooks/index(dup preMachineControllerCompatibility — verify true-dup-vs-reexport first).
- **peer-churn (not papa):** UnifiedProgramParser line-shifts + transient peer commits in shared cad-fusion-live-ms0 tree.
- **HONEST STATUS (R12):** papa CANNOT drive global tsc to 0 solo — ~133/184 is domain-owner/physics work. papa cleared every genuinely-clean type-hardening error (nullability, module-resolution, enum-to-valid-member, schema-default, validated-boundary cast, behavior-neutral excess-prop removal) without weakening a single type or fabricating a single physics/material/machine value. Reverts this session: NXGen (un-masked CAD-systemic). Re-derive residual: `node /tmp/tsc-categorize.mjs H:/prism/state/shared/tsc-clean-w12.log`.

## CHECKPOINT 2026-06-17b (slot:papa, claude-a59e4a3e) — baseline 179
- **W13 `821dabd8b9`: 184->183** — ReasoningChain captureKnowledge->capture (verified call obj == Omit<KnowledgeTip,id|created_at|usage_count>) + coordinated null-guard + result.tip.id->result.id x4. EventHandler subscribe-arity DEFERRED.
- **U-TSC-INFRA-CONTROLLERKNOWLEDGE `2acbe334cc`: 183->179 (4 cleared) -- the twice-deferred entangled GCodeDialect cascade, RESOLVED.** Method that worked: scoped property extraction to `gCodeDialect:{}` blocks ONLY (awk depth-counter), which EXCLUDES the sibling-metadata contaminants (gCode/notes/letter) that broke the earlier bulk attempt. Verified all 85 undeclared dialect props are string-valued G/M codes (no non-string in any dialect block). Declared all 85 optional on GCodeDialect + added precise `modeSpecificBehavior?: Record<string,{...}>` (single Hurco BNC/ISNC usage) on ControllerProfile. ALL additive optional fields -- no value fabricated, no type weakened. 4 errors -> 0; zero regressions elsewhere. LESSON: an "entangled cascade" deferral can be cracked by SCOPING the bulk-add to the correct container (gCodeDialect blocks) instead of a flat file-wide key grep -- the contamination was the only real blocker, and structural scoping removes it.
- **CAMPAIGN TOTAL: 638 -> 179.** This-session continuation: 197->179 (18 cleared).
### REMAINING papa-careful tier (each needs investigation / carries revert risk -- do one-at-a-time, read contract first, defer if unverifiable):
ShopMachineOverlay(4: CanonicalMachinePackage shape + spindle_taper + y_mm on a 2-axis {x_mm,z_mm} travel = machine-geometry question), ReinforcementLearningCAMFeedback(2 TS2554 missing-5th-arg -- need signature + arg semantics), UnifiedProgramParser(3 ParsedOperation[] full-shape reconcile), RoadmapIntelligence(1 -- "milestone" not a manufacturing DecisionCategory = DESIGN mismatch), authHttp(3 -- OAuth authorizationUrl/tokenUrl/scopes exist on NO type = auth-security), ReasoningChain EventHandler(1 -- eventBus subscribe contract), python-api .search(1 -- contract mismatch, W1-reverted once). Method-missing -> domain: FusionAIOrchestration getPhysicsProfile (echo/delta), SolidCAM/PowerMill selectStrategy (kilo). DOMAIN CLUSTERS UNCHANGED: mike WEDM ~77 / whiskey lathe ~17 / delta CAD-cap-matrix ~18 / physics-reviewer ~20.

## CHECKPOINT 2026-06-17c (slot:papa) — baseline 176
- **U-TSC-INFRA-SHOPMACHINE `6ca3d5d8a2`: 179->176 (3 cleared)** -- ShopMachine type-completion (ShopConfigurationEngine.ts): +spindle_taper?:string (taper id, not physics) + work_envelope.y_mm?:number (mill XYZ vs lathe x/z). Additive optional, 11 importers non-breaking. ShopMachineOverlay 396 DEFERRED (lightweight builder vs heavy CanonicalMachinePackage = design mismatch / ~15 fabricated machine-spec fields).
- **CAREFUL-TIER DEFER FINDINGS (read-contract-first confirmed these are NOT papa-safe):**
  - RLCAMFeedback (2 TS2554): NOT "add a reward arg" -- step() wants outcome:{mrr,tool_life_factor,surface_ra,safety_margin}, caller has only scalar reward. Constructing outcome = fabricating 4 physics components. -> india/kilo (RL contract).
  - authHttp (3 TS2339): discovery doc reads config.authorizationUrl/tokenUrl/scopes; OAuthConfig has none + routes not registered = incomplete OAuth scaffolding. Fixing = fabricating OAuth endpoint URLs (breaks auth). -> auth-security owner.
  - ShopMachineOverlay 396, RoadmapIntelligence (milestone vs DecisionCategory design), UnifiedParser (ParsedOperation[] shape), python-api/.search, ReasoningChain EventHandler (eventBus contract) = same class.
- **CAMPAIGN TOTAL: 638 -> 176.** This-session continuation: 197->176 (21 cleared incl. the twice-deferred ControllerKnowledge cascade). papa-safe + safe-careful tiers EXHAUSTED. Residual ~176 = domain-owner (mike WEDM ~77 / whiskey lathe ~17 / delta CAD ~18 / physics ~20) + ~28 design/security/contract defers. papa cannot drive global->0 solo (R12).

## CHECKPOINT 2026-06-17d (slot:papa) — baseline 110 — OPERATOR DIRECTIVE: "do all fixing for all domains"
This session pivoted from papa-lane-only to ALL-DOMAINS (operator override; papa elevated access). Inviolable rail kept: NEVER fabricate physics/material/feed/machine/wire/safety values -- derive from real fields or use the file's own empty-sentinel (0/[]/"" commented), defer genuine value gaps.
- **WEDMSetupSheet `164085bbd3`: 176->128 (48 cleared)** -- guard 4 optional subsystem outputs + reconcile to REAL producer types (cutting=rough+skim, num_passes=passes.length, controller=result.controller, num_profiles=result.profiles_cut, confidence.summary derived) + file-sentinel for unemitted (wire/per-pass/program_number/submerged).
- **WEDMJobCreator `89179da416`: 128->115 (13 cleared)** -- PassDetail .type->.pass_type / .predicted_ra_um->.expected_ra_um / drop e_pack_code; ??-guard optional program fields; wire 'n/a'/0 sentinel.
- **AIReasoningDomain `25d2dd5654`: 115->110 (5 cleared)** -- +wedm_calculator/wedm_neural_optimization/wedm_calibration union members; un-masked DOMAIN_PROMPTS Record -> authored 3 LLM prompt strings (text, no values).
- **CAMPAIGN TOTAL: 638 -> 110 (528 cleared).** Entire WEDM core (66) done this session.
### REMAINING 110 triage (for continuation -- baseline log state/shared/tsc-aidomain2.log):
- **Lathe cluster ~17 (COMPLEX, investigate per-file):** LatheMasterOrchestratorFacade(3 arity TS2554 -- need method sigs), LathePostGeneratorDialect(4 -- arg objects missing line_number_start/increment [post formatting params] + parameters subset), LatheQualityGate(7 -- OperationInput.type excess / ValidationContext.material absent / QualityGateMachine.max_power_kw absent / findLastIndex needs es2023 -> manual reverse-find), TurningStochasticPlan(2 method-missing insertChangeSchedule/wearAccumulation -> verify TurningInsertLifeEngine API or defer), TurretLayout(1 TS2352 -> as-unknown-as at validated boundary).
- **CAD capability-matrix ~18 (needs VERIFIED per-CAD nativeLengthUnit -- NEVER guess; Fusion=cm is a confirmed memory fact, others verify from each generator's emitted-units code):** SolidWorks(7)/Inventor(4)/Fusion360(2)/Mastercam(2)/CadQuery(2)/NXGen(1).
- **Physics-value ~20 (NEVER fabricate; defer to physics-reviewer unless purely type-shape):** Electrode(4)/Trilobe(3)/SpecificCuttingEnergy(3)/FiveAxis(3)/ChatterStability(2)/SpeedFeedExhaustive(2)/SpeedFeedNineAxis(1)/Adaptive(2).
- **WEDM remainder ~10:** WEDMSafetyEnvelope(1 -- DEFER: needs X/Y/Z axis-travel safety LIMITS {min,max,soft_band}, machine safety values, NEVER fabricate -> OR make the Record Partial if only electrical params are monitored, verify consumers first), WEDMNeuralTraining(3 -- 570 const-compare bug + 1934/1945 .body on KB-literal), WireEDMMachineTechData(3 -- TechLookupResult.method missing on not-found result + never.toLowerCase), WireEDMNeuralOrchestration(2 -- WireType/WireDiameter union value mismatch), WireBreakAutoRethread(1 -- operatorSkill use-before-assign).
- **Method-missing/orchestration ~10:** Fusion/SolidCAM/PowerMill/NXCAM getPhysicsProfile/selectStrategy (domain method intent -> verify real method or route CAM), ProcessIntelligenceRouter(6 -- missing CrossProcessBridge MODULES = build-the-bridges feature, not a type fix).
- **Misc ~15:** UnifiedParser(3 ParsedOperation[] shape), authHttp(3 OAuth fields on no type -- security defer), RoadmapIntelligence(1 milestone-vs-DecisionCategory design), LectureNote(2), Quoting(2), python-api(1 contract), ShopMachineOverlay(1 CanonicalMachinePackage design), ReasoningChain(1 eventBus arity), hooks/index(1).
