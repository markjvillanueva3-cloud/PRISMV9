# WHISKEY CLOSED-LOOP LATHE TESTING — build plan + asset map (2026-05-31)

**Goal (operator /goal /loop /yolo):** build everything for *fully closed-loop lathe testing* — train for every possibility to generate a lathe part: use all existing CAD/CAM/prints/programs to generate **toolpath templates for every op type with variable params + cutting conditions** (JM-fleet focus, 100% Okuma OSP LTH-01..07); **assess the "enhanced" JM lathe programs** (did we really generate proper programs?) via a workflow; derive a **tool inventory from JM order docs** (charlie/hotel) → what tools were used vs purchased.

## ✅ SHIPPED 2026-06-01 — all 4 goal legs delivered (slot/whiskey, tested + committed)
| Leg | Unit | Artifact | Validation |
|---|---|---|---|
| **Assess enhanced programs** | U-CL1 / U-CL1b | `scripts/lathe-program-assessor.mjs` (8-gotcha PROPER + A/B machining-delta + `--pairs-jsonl`) | 11/11 node:test; ran 113 enhanced pairs (ALCOA/ACME/ITW) = 100% PROPER, 0 improved, ITW 98.8% passthrough, defect=feed-mode-undeclared |
| **Toolpath templates (every type)** | U-CL2 | `mcp-server/src/engines/LatheToolpathTemplateEngine.ts` (cycle + vc/fn/ap bands from constants.ts + safety gates per op type; extends `TurningStrategyCatalog`) | 14/14 vitest + 3/3 dispatcher round-trip; wired `prism_turning` `turning_template_build`/`turning_template_list_all` |
| **Tool inventory from order docs** | U-CL3 | `scripts/lib/lathe-tool-inventory-crossref.mjs` (op→tool-type demand vs JM purchase corpus) | 7/7 node:test + real demo on `jm-tool-purchases.json` ($4.91M) |
| **Closed-loop testing substrate** | U-CL4 | `scripts/lathe-closed-loop-test.mjs` (composes U-CL1 safety + U-CL3 tooling → per-program PASS/FAIL) | 6/6 node:test + real ALCOA demo (PASS) |
| **GENERATE side (closes the loop)** | U-CL5 | `scripts/lib/lathe-template-emitter.mjs` (U-CL2 template → Okuma program, PROPER by construction: G50 cap + G95 baked in) | 5/5 incl. CLOSED-LOOP proof: template→emit→assess=PROPER→U-CL4=PASS |
| **EVERY-TYPE coverage proof** | U-CL6 | `mcp-server/src/__tests__/lathe-closed-loop-coverage.integration.test.ts` (drives the REAL engine `buildAllTemplates()`→emit→assess/test, no fixtures) | 12/12 vitest: all 8 categories (rough/finish/groove/thread/bore/drill/contour/specialty) emit PROPER + PASS; negative control (G96 w/o G50) FAILs (non-vacuous); exercises the cannedCycle/cssMode field seam through the real engine |
| **PRODUCTION-EMIT closure (machine-ready .MIN)** | U-CL7 | `mcp-server/src/__tests__/lathe-production-emit-closed-loop.integration.test.ts` (drives the REAL move-level post `lathePrintProgramEmitterEngine.emit()`→assess/test) | 4/4 vitest: machine-ready Okuma-OSP gcode bakes G50 cap BEFORE G96, is PROPER (0 ERROR, no css-no-rpm-cap), PASSES with tools on hand, carries a non-stub sign-off dossier; out-of-envelope program is HARD-BLOCKED by emit() (ISO 16090-1 gate). Closes the emit-half future-wire. |
| **FLEET assessment via WORKFLOW** ("use workflow") | U-CL-WF | `Workflow jm-lathe-enhanced-assessment` (run `wf_fbb1a317-df6`, 17 agents) + report `state/shared/specs/WHISKEY-JM-ENHANCED-FLEET-ASSESSMENT-2026-06-01.md` | **5,341 A/B pairs / 118 customers**: 99.0% PROPER (54 ERROR-carrying → gate), 80.6% annotation-passthrough, **19.4% genuinely re-machined** (corrects the 113-pair "0% improved"; improvement is cohort-split — early-alphabet customers re-machined, BRICO-onward annotation-only ⇒ two upgrade runs). Dominant defect `feed-mode-undeclared` fleet-wide — the exact defect U-CL5/U-CL7 emitters fix by construction. |
| **Tools USED vs PURCHASED reconciliation** (goal component 3) | U-CL8 | `scripts/lib/lathe-fleet-tool-reconcile.mjs` (+ `.test.mjs`, 5/5) — composes U-CL4 op-inference + U-CL3 crossref; report `state/shared/specs/WHISKEY-JM-TOOL-RECONCILE-2026-06-01.md` | 1,500 programs vs $4.91M purchase corpus: **100% tool-type coverage, zero lathe procurement gap**; lathe spend dominated by carbide-blank $4.34M (JM grinds its own form tools); ~$497K is non-lathe tooling (reamer/end-mill/grind/misc). R12 caveat: op-inference omits G84 tap/ream so tap/reamer demand understated. |

**Key data-backed finding (the goal's "did we really generate proper programs?") — FLEET-SCALE via workflow (U-CL-WF, 5,341 pairs / 118 customers), supersedes the 113-pair sample:** enhanced JM lathe programs are **99.0% PROPER/safe** (54 carry residual ERROR gotchas → gate, do not run unreviewed) and **only 19.4% genuinely re-machined** — **80.6% annotation-passthrough**. The improvement is cohort-split (early-alphabet customers re-machined, BRICO-onward annotation-only ⇒ two upgrade runs). The dominant defect fleet-wide is `feed-mode-undeclared`; the upgrader rarely declares G95/G94 or caps G96 with G50 — the exact two defects PRISM's U-CL5/U-CL7 emitters fix by construction. Memory: [[reference_whiskey_jm_enhanced_program_assessment_2026_06_01]]. The FULL closed loop is now runnable AND proven for **every toolpath type**: GENERATE (U-CL2 template → U-CL5 emit, proper-by-construction) → TEST (U-CL4 = U-CL1 safety + U-CL3 tooling) → verdict, proven PROPER→PASS end-to-end across all 8 turning categories through the real engine (U-CL6, with a negative control proving the proof is non-vacuous). U-CL5 is the lightweight template→canned-cycle emitter for the test loop; **U-CL7 closes the heavy path** — the move-level production post (`LathePrintProgramEmitterEngine`, with envelope hard-block + sign-off dossier) is now proven to emit machine-ready Okuma-OSP `.MIN` that passes the same closed loop (PROPER + PASS) and to hard-block out-of-envelope programs. The one remaining un-proven seam is the GENERATOR (`LathePrintToolpathGeneratorEngine.generateProgram()` building a `ToolpathProgram` from a `SequencePlan`) — a deeper planner→generator chain (needs a `SequencePlan` fixture); the emitter it feeds is proven (U-CL7), so this is the natural next unit. Cross-tree note: `vendor-catalog-db` + the enhanced-program archive live in MAIN (read cross-tree; not yet synced into slot/whiskey).

## Recon — EXISTING infra (REUSE, do NOT rebuild — R8/dedup)
| Need | Existing asset | Path |
|---|---|---|
| Quality scoring | `aggregateQualityScore` (10-pt rubric EXPERT≥80..POOR<20) | `scripts/lathe-quality-pipeline.mjs` |
| Corpus aggregation | `analyzeRecords` (quality dist, per-op freq, insert-code coverage, G-code dist, issue clusters); reads training-loop JSONL (one rec per `.MIN`) | `scripts/lathe-baseline-analyzer.mjs` |
| A/B (orig↔"enhanced") pairing | `lathe-ab-version-locator.mjs` (`.MIN` Okuma; B in `PRISM_UPGRADED` folders; pairAB/classifyPairType) | `scripts/lib/lathe-ab-version-locator.mjs` |
| Physics/safety lint | 8-gotcha PASS/FAIL (`lathe-gcode-lint.mjs`) + G76 thread validator | `scripts/lathe-program-lint.mjs`, `scripts/lib/lathe-{gcode-lint,g76-thread-validator}.mjs` |
| Program analysis engine | `JMDieProgramAnalyzerEngine`, `LatheQualityGateEngine` | `mcp-server/src/engines/` |
| Part-family templates | `LathePartFamilyTemplateExtractorEngine`, `TurningPrintToProgramEngine` | `mcp-server/src/engines/` |
| Tooling/vendor | `lathe-shop-tool-library-bridge.mjs`, `lathe-wizard-vendor-lookup.mjs` | `scripts/lib/` |
| Training loop | `lathe-training-loop.mjs` + stage-4-reason + stage-5-generate | `scripts/`, `scripts/lib/` |
| Cutting conditions | Kienzle/Taylor/turning-speeds (READ-ONLY, never inline) + registries | `mcp-server/src/physics/constants.ts`, `mcp-server/src/registries/` |
| JM program corpus | 118 customers / 14,475 A/B `.MIN` pairs | `H:/PRISM/JM DIE/CNC LATHE/<customer>/` |

**KNOWN FINDING (R12, iter261):** `JMDieLatheProgramUpgraderV2` B-versions are **PURE annotation pass-through — NOT machining-improving** ([[reference_iter218_alcoa_outlier_retraction_2026_05_27]] + [[feedback_jm_die_b_versions_are_ai_not_human_upgrade]]). The goal's "did we really generate proper programs?" is exactly this question at scale.

## 🚨 CRITICAL RECON FINDING (this session — the real gap)
Ran `lathe-baseline-analyzer` on the latest training-loop output (`state/shared/lathe-training-loop/iter-8-*.jsonl`): **11 programs** scored (tiny sample of 14,475), mean **55 (MEDIOCRE)**, **0% insert coverage**. BUT inspecting a record exposes the truth — **the closed loop is SCAFFOLDED, not real:**
- `lathe-training-loop.mjs` has 11 stages; **stages 4–11 are `"status":"skeleton"` TODOs**: 4 REASON (`PRISMCreativeReasoningEngine.explore`), 5 GENERATE (`LathePrintProgramEmitterEngine` emit corrected `.MIN`), **6 DIFF (`A-to-C` and `B-to-C` deltas — the A/B assessment)**, 7 OPERATOR_REVIEW (UI TODO), 8 LEARN, 9 EMBED, 10 WIKI_PROMOTE, 11 SYSTEM_VIZ_TICK.
- **Stage 3 VALIDATE is placeholder:** `physics_count:0`, every quality sub-score defaults to `50`, `ops:[]` empty. The "mean 55" is default-50 noise, **not** real physics assessment — even though `lathe-gcode-lint.mjs` (8-gotcha PASS/FAIL) + `aggregateQualityScore` (10-pt rubric) EXIST and are NOT wired into the loop.
- **Implication:** an A-vs-B quality delta built on these scores would falsely "confirm" the iter261 pass-through finding for the WRONG reason (both ~50 because the scorer doesn't differentiate). Must make scoring REAL first (R9/R12).

**Revised first build (U-CL0, prerequisite to U-CL1):** wire the EXISTING `lathe-gcode-lint` (8 gotchas) + `aggregateQualityScore` into the training-loop **stage-3 VALIDATE** so quality reflects real physics/safety violations (not placeholder 50). Then U-CL1 (A-vs-B delta) and the skeleton stages 5/6 (GENERATE/DIFF) produce meaningful results. This is the genuine "build everything for closed-loop testing" core: implement the 8 skeleton stages + real validation.

## Goal decomposition + prioritized sequence
- **U-CL1 — A-vs-B assessment report (FIRST, iter-1):** run locator→quality-pipeline over JM A/B pairs → quantify whether B (enhanced) programs are PROPER (lint-clean + rubric ≥ tier) and IMPROVED over A (quality delta). Confirms/refutes the iter261 pass-through finding at scale. Pure reuse; deterministic (offline — MCP/server down doesn't block). _Note: goal says "use workflow"; the host kills long workflows (recon hung 31 min), so the deterministic script pipeline IS the assessment — more reliable here (R7)._
- **U-CL2 — toolpath template library:** per-op-TYPE (OD turn/face/bore/thread/groove/part/drill/knurl/taper/contour) templates with variable param ranges + cutting conditions, sourced from constants.ts + registries + observed JM op frequencies (from U-CL1's baseline). Extends `LathePartFamilyTemplateExtractorEngine` (part-family) → per-op-type.
- **U-CL3 — tool inventory from order docs:** cross-ref charlie/hotel's sorted JM purchase orders ↔ tools referenced in programs (via lathe-shop-tool-library-bridge) → "purchased vs used" reconciliation.
- **U-CL4 — closed-loop wire:** print → template (U-CL2) → program → assess (U-CL1) → tool-check (U-CL3) → outcome → learn (training-loop). Runnable end-to-end harness.

## Blockers / honesty (R12)
- MCP server (3100) + Ollama DOWN → engine dispatcher calls fail; use the offline SCRIPTS (locator/quality-pipeline/baseline-analyzer/lint all run via `node`).
- Long agent Workflows hang on this host (24k-file archive + contention + repo corruption) — prefer deterministic scripts over agent fan-out for the assessment.
- U-CL3 depends on charlie/hotel order-doc artifacts being present in-slot — verify before building (recon agent for that class did not return before the workflow was killed).
