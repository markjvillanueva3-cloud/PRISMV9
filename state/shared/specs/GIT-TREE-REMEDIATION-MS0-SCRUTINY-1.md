# GIT-TREE-REMEDIATION-MS0 — Scrutiny Pass 1

> **Date:** 2026-05-12 · **Method:** 3 parallel review agents (correctness/feasibility · risk/safety/rollback · completeness/alternatives/doctrine), each with the PRISM SPAWNED-AGENT CONTEXT auto-injected (now incl. the `## Doctrine & memory` section) + explicit instruction to use CLAUDE.md, the memory vault, the wiki, and `system-viz-query`. Target: `GIT-TREE-REMEDIATION-MS0-ROADMAP-2026-05-12.md` @ commit `637502940`.
> **Verdict:** the *spine* is sound (zero-risk hygiene → anti-bifurcation hooks → quiesced rewrite → trunk reconcile → cleanup → doc; tier-floor invariant; `--mirror` backup as the irreversibility line; warn-first hooks; full failure-mode register). But it has **factual errors about the current repo**, **~14 omissions**, **a non-functional git operation**, **risky push sequencing**, **no enforcement on the commit freeze** (the most likely real-world data-loss event), and **three conflicting unit-numbering schemes**. Scores: **Correctness 74/100 · Safety 58/100 · Completeness 68/100.**

---

## A. FACTUAL CORRECTIONS (the roadmap states these wrong — fix before execution)

1. **THREE (four) mutually-unrelated histories, not two.** `git merge-base` returns nothing for *every* pair: `cad-fusion-live-ms0` ⟂ `main` ⟂ `master` ⟂ `claude/fervent-bohr`. Commit counts: `cad-f` **1216** (roadmap says 1211), `main` 874, **`master` 1326** (the *most* commits of any branch — the roadmap treats it as a parenthetical "diff-review"). The reconciliation problem is bigger than stated: `master` needs its own `U-GC-11`-grade analysis; the `claude/*` branches are independent roots (200–1300 unique commits each), not "pre-fork divergence".
2. **`recovery` is NOT a remote.** `git remote -v` → only `origin`. What exists is a dangling remote-tracking ref `refs/remotes/recovery/work/intel-ollama-obsidian-ms0` (→ `12fe39f23`) left over from a removed remote. So `U-GC-16`'s `git push <remote> :<branch>` on it would fail — correct cleanup is `git update-ref -d refs/remotes/recovery/work/intel-ollama-obsidian-ms0` (a P0 housekeeping step).
3. **`state/shared/system-viz/system-graph.json` is NOT currently tracked** (`git ls-files` doesn't list it; `git check-ignore` exit 1 — not ignored either; it's regenerated locally). The 118 MB blob lives **only in history** → only `U-GC-09`'s `filter-repo` can strip it. `U-GC-04`'s claim "removes the 118 MB blob from the next tree" is wrong; what `git rm --cached` actually removes is the *tracked* `*-augmentation.json` files + `mcp-server/dev/null`. **And: several `*-augmentation.json` are hand-curated overlays** (`cam-vendor-catalog-augmentation.json`, `engine-classification-repair-log.json`, `action-engine-edges-augmentation.json` — all in the live `git status -M` list) — the blanket `*-augmentation.json` gitignore pattern would *lose curated data on a fresh checkout*. U-GC-03/04 need a per-file audit (generated → ignore; curated → keep tracked), not a glob.
4. **>100 MB blob inventory is incomplete: 7 blobs, not "5 commits all system-graph.json".** Six `system-graph.json` versions (incl. a **182.5 MB** one the roadmap never lists), one **134 MB `obsidian-augmentation.json`**, plus `models/ggml-large-v3.bin` (**3.09 GB**) / `models/ggml-base.bin` (148 MB) on `archive/forge-orphans`. The `filter-repo --path` list happens to cover `obsidian-augmentation.json`, but the prose will mislead an operator.
5. **`git-filter-repo` is NOT installed** (`which git-filter-repo` fails; no python module). `U-GC-09` step 0 must be "install it (pip / scoop / standalone script)". F1 only worries about linked worktrees.
6. **`git config worktree.baseRef head` does NOTHING.** `worktree.baseRef` is not a git config key — git stores it as inert text and never reads it (verified). It's a **Claude-Code harness setting** (per the `EnterWorktree` tool doc: `fresh`=branch-from-`origin/<default>`, `head`=branch-from-current-HEAD). It must be set in **`H:/.claude/settings.json`** (the C:→H: mirror source), under the key the harness reads — *not* `.git/config`. As written, the bifurcation vector stays open and the unit's acceptance ("EnterWorktree branches off current HEAD") fails.
7. **`.git` is ~43 GB and the loose pile is mostly *reachable history*, not garbage** (`prune-packable: 16511` of 216 759; `garbage: 0`). `git gc --prune=now` shrinks `.git` by **repack/delta/zlib** of all reachable objects (the JSON blobs compress ~5–10×) and *deletes* only the unreachable 16 K. The "42 GB → 3–5 GB via pruning" framing mislabels reachable history as "garbage" and the estimate is coincidental (it'll mostly come from repacking). 56 packs → also call out a one-time `git repack -adf`.
8. **`git push --dry-run … → no GH001` is a false-confidence acceptance check** — `--dry-run` doesn't transfer objects, so the server never sees the 118 MB blob and never raises GH001 (verified: `git push --dry-run origin cad-fusion-live-ms0` succeeds *right now* on the un-fixed repo). Replace with: verify the blob is *gone* (`git rev-list --objects HEAD | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '$3>104857600'` → empty), or an actual `git push`.
9. **`git fsck --full` on a 43 GB repo takes minutes-to-tens-of-minutes** — it appears in 3 units' acceptance with no time budget; use `--connectivity-only` / `--no-dangling` or budget ≥10 min.
10. **`enforce-handoff-topic.mjs` excludes `main`/`master`/`develop` from branch-based topic derivation — but NOT `cad-fusion-live-ms0`.** Today a chat on `cad-f` gets topic `"cad-fusion-live-ms0"`; post-rename it's on `main` → derivation skips the branch step → handoff filenames change form (the "topicless handoff" failure class CLAUDE.md warns about). Add `cad-fusion-live-ms0` to that exclusion list *now* so pre/post-rename behavior matches.
11. **CI is keyed on `main`.** `.github/workflows/{ci,deploy,auto-index,dockerfile-sync}.yml` all have `branches: [main]` (`ci.yml` twice). CI has *never* run against `cad-fusion-live-ms0` → the live trunk is CI-unverified. This is an *argument FOR the rename* — and post-rename CI starts firing on the new `main` (a behavior change to flag, not a surprise). No workflow hardcodes `cad-fusion-live-ms0` (checked).
12. **`work/merge-staging-ms0` is internally inconsistent in the roadmap:** `U-GC-12` says "reset merge-staging to cad-f HEAD" while `U-GC-11`'s breakdown counts "MERGE-STAGING (12)" as unique work to triage and `U-GC-14` lists it with 47 dirty lines. You can't both reset it and preserve its 12 commits. Fix: triage merge-staging's WIP + 12 commits in `U-GC-14`/`U-GC-17` first; for reconciliation use a *fresh* `work/trunk-reconcile-<date>` branch, leave `merge-staging` for triage.
13. **`brave-euclid` worktree:** its `.git` file is `gitdir: C:/PRISM/.git/worktrees/brave-euclid` — points at the *wrong drive*. Confirm `C:/PRISM/` doesn't exist as a separate clone (if it does, it's a clone that needs the `U-GC-10` re-sync treatment) before removing the stray dir.

---

## B. CONSOLIDATED MUST-FIX LIST (prioritized; merges the 3 agents' top-5s)

**P0-blocking (do these before any execution):**

1. **Renumber the roadmap once, cleanly.** It openly has 3 conflicting schemes (`U-GC-00…22` in META; `U-GC-03/04/04b/05/05cfg/06/06b` in phase bodies; the DAG's own) and says "the DAG is authoritative … wait — keep the canonical numbering." For an execution artifact that's a defect. Suggested: P0 = `U-GC-01..05` (gitignore+untrack curated-audited, branch-cruft+recovery-ref, `worktree.baseRef` *as a harness setting*, `gc`+`repack -adf`, `/sync-rebase --base` fix), P1 = `U-GC-06..09` (canonical-trunk shared module, `trunk-guard.mjs` SessionStart, `worktree-commit-route` trunk-check, doc reconcile), P2 = `U-GC-10..14` (verified backup ×2 off-drive, **freeze-hook**, dirty-worktree sweep [start day 1], filter-repo+`--prune-empty=never`, force-push-rewritten-refs-only+resync), P3 = `U-GC-15..18` (`main`/`master`/`claude*` analysis ×3, B-vs-C decision gate, execute reconcile, rename→`main`+retire-as-tags), P4 = `U-GC-19..24` (origin-only-branch triage, stale-fork auto-triage script + review + exec, broken-worktree removal, `archive/forge-orphans` decision), P5 = `U-GC-25..27` (partial verify after P0+P1, full verify, document+memory).

2. **`worktree.baseRef`: set it as a HARNESS setting in `H:/.claude/settings.json`, not `git config`** (correction A.6). The whole point of the unit fails otherwise.

3. **Add a freeze-enforcement unit (`U-GC-FREEZE`, executes immediately before the rewrite, removed after the resync verifies).** The roadmap names "commit freeze" as the mitigation for F3/F17 but has *no mechanism* — 8 autonomous YOLO chats that auto-commit after each unit will land a commit on doomed history during the 30–60 min window → silently `reset --hard`'d away at force-push time. **The fix:** a temporary **bundle-absorbed PreToolUse hard-block hook** `git-maintenance-freeze.mjs` (denies all non-operator `git (commit|push|merge|rebase|reset|cherry-pick|filter-repo|worktree add)` while `state/shared/.git-freeze-active` exists), wired into `bash-bundle.mjs` SUB_HOOKS so `stop_on_hook_unregistration` + `settings-json-addonly-guard` don't trip; a Stop-hook clause that skips `git-sync-stop`'s push while the flag is set; `PRISM_DISABLE_GIT_SYNC=1` (or the real off-switch — verify one exists) for the window; and a **pre-`reset` assertion in the force-push unit**: `git log origin/<branch>..<branch>` must be empty — else cherry-pick those commits onto the rewritten history first, never `reset --hard` past unmerged local work. Fully reversible (`rm` the flag + revert the bundle entry) → "never delete only disable" compliant.

4. **Backup posture: ≥2 independent backups, ≥2 physical volumes, verified, kept until P4+P5 done.** The roadmap's single `git clone --mirror` to `H:/prism-git-backup-*.git` is on the *same (per memory, portable) drive* as the repo and is deleted on a fixed 30-day clock while P4 deletions "run incrementally over weeks". Fix: `git clone --mirror` to a *different* volume **and** `git bundle create … --all` off-drive; `git -C <backup> fsck --full` + `rev-parse cad-fusion-live-ms0` + size-check *each*; take them **before** `U-GC-05`'s `gc --prune=now` (not at U-GC-09); keep until all of P4+P5 complete and verified. (The `leave-a-copy-behind-guard.mjs` Stop hook may flag a premature backup deletion — aligns with this.)

5. **`filter-repo` will prune commits that contained only `system-graph.json` — possibly including HEAD.** `--invert-paths` on a path that was a commit's *only* change makes that commit empty, and `filter-repo` prunes empty commits by default; `02720fd64` (the HEAD-adjacent 118.76 MB `system-graph.json` commit) is exactly such a commit. Fix: run with `--prune-empty=never`; acceptance must assert `git rev-list --count HEAD` before == after (modulo a user-approved, reconciled delta) and the rewritten HEAD commit message matches the expected one.

6. **Blind `--strip-blobs-bigger-than 20M` with no inventory review = "delete history without disable".** Generate `git rev-list --all --objects | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '$1=="blob"&&$3>20000000' | sort -k3 -rn` first, review with the user, prefer explicit `--path` exclusions for the known-bad ones over the blind cutoff.

7. **`U-GC-13` push sequencing leaves a window with no remote trunk + conflates "GitHub default branch" with `git config init.defaultBranch`.** Fix order: push `main` first → set the GitHub default branch (API/UI) and `git remote set-head origin main` → re-point all 40 worktrees' upstreams + verify → *only then* retire `origin/cad-fusion-live-ms0` (and as a **rename-on-remote** `origin/archive/cad-fusion-live-ms0-pre-rename-<date>`, kept ≥90 days, not a hard `push origin :`). Drop the redundant raw `git symbolic-ref` (keep `git remote set-head`). Note `git config init.defaultBranch` is repo-local and does NOT change GitHub's default.

8. **`U-GC-10` force-push: never `--force-with-lease --all` from a fresh clone** — the no-ref-arg lease checks against *this clone's* remote-tracking refs (= origin at clone-time), so a push another chat lands between clone and force-push is exactly the unprotected window. Push only the rewritten branches explicitly with per-ref leases: `git push --force-with-lease=cad-fusion-live-ms0:<old-sha> --force-with-lease=main:<sha> origin cad-fusion-live-ms0 main`. Also: handle partial-push (`git ls-remote origin` must exactly match the mirror's refs before unfreezing) and stale `.git/worktrees/agent-*/index.lock` in the per-worktree reset loop (`rm -f` >1h-old locks; *report* any worktree it couldn't reset, never silently skip → that's how a worktree ends up orphaned on pre-rewrite SHAs that `gc --prune` then deletes).

**Correctness / completeness must-adds:**

9. **Add "Path C — `git merge --allow-unrelated-histories <other-trunk>` into cad-f"** (one merge commit, all of the other trunk's tree appears, conflicts only on path-overlap, the other history preserved as a 2nd parent). **Drop Path A (cherry-pick across unrelated histories — fails for most commits), Path D (`replace --graft` — rewrites parentage not trees, wrong here), Path E (`rebase --onto` — 874 sequential conflicts).** Make `U-GC-12` a *decision gate* (B-vs-C, fed by the analysis units) + a separate execution unit. **Default to Path B** (declare cad-f canonical) — the fleet has been on cad-f for weeks, `main` is frozen 5 days, `master` 7 weeks stale — fall back to Path C only if the analysis surprises. **Retire the old trunks as TAGS (`archive/main-final-<date>`, `archive/master-final-<date>`), not branches** — tags don't show in `git branch`, don't get auto-pushed by `--all`, signal "frozen" unambiguously.

10. **Add an origin-only-branch unit.** `git push --force-with-lease --all` only pushes *local* branches → the ~25 `origin/*`-only branches (`origin/archive-2026-02-01`, `origin/claude/affectionate-perlman`, …) keep their **blob-carrying history on the remote forever** → the clone size doesn't actually shrink. Enumerate `git branch -r`, classify each (ancestor-of-trunk? unique? archive?), and delete/rewrite the ones holding the >100 MB blobs.

11. **Add a stale-fork auto-triage script (`scripts/git-stale-fork-triage.mjs`)** — for each `work/*` / `claude/*` branch emit: `merge-base --is-ancestor <trunk>` (merged → safe-retire), last-commit (`%cr`), unique-commit count (`rev-list --count <trunk>..<b>`), file-overlap (`diff --name-only <trunk>...<b> | wc -l`), and a heuristic recommendation. The user approves a pre-filled `STALE-FORK-TRIAGE.md` instead of triaging 24+ branches from blank — that's what makes P4 actually complete. Split `U-GC-17` into script / review / exec-merge / exec-archive units.

12. **Operationalize `feedback_never_delete_only_disable` across P3/P4.** Every `branch -D` / `git push origin :<x>` defaults to `git branch -m <x> archive/<x>-<date>; git push origin archive/<x>-<date>` (rename, push the renamed ref) — hard-delete only after the (extended) backup window AND explicit user approval. **Never run `U-GC-20`'s delete-path** (the only copy of a 3.09 GB whisper model + 9 881 orphaned files + 502 unique commits, on a 30-day-backup hope) — if 3.3 GB must be reclaimed: `git bundle create forge-orphans-<date>.bundle archive/forge-orphans-2026-05-01` off-drive → verify (`clone + fsck`) → rename-not-delete → document the bundle location in `GIT-TOPOLOGY.md` *permanently*. **`origin/worktree-data-loss-fix` must NOT be deleted without review** — a branch literally named "data-loss-fix" may hold the fix for the very class of bug this roadmap risks.

13. **Hook-collision section.** As written: `rm -rf H:/PRISM/.claude/worktrees/brave-euclid` is HARD-BLOCKED by `asset-deletion-block.mjs` (`/.claude/` + `rm -rf`) — use `mv … brave-euclid.orphan-archive-<date>` instead (also matches the never-delete rule). `git worktree remove --force` of worktrees under `.claude/worktrees/` (`worktree-u-fus-api01/02`) may trip the same hook — and per the memory `worktree remove --force` of a dir with untracked work needs explicit per-worktree user OK. `stop_on_uncommitted_critical.mjs` will block the *operator's* Stop if any worktree has dirty `constants.ts`/`*Safety*`/`*Force*` files during `U-GC-14` — the sweep must handle that case. The temp freeze hook must be a *bundle SUB_HOOK*, not a top-level `settings.json` entry, or removing it post-window trips `settings-json-addonly-guard`. The 40-worktree `reset --hard` loop serializes through `git-anti-clobber`'s locks — run it from one operator session, not parallel.

14. **Other must-adds:** (a) audit `.github/workflows/` for branch refs + flag the "CI starts on the renamed trunk" behavior change; (b) consider **git-lfs** as an alternative for *non-regenerable* large binaries (`models/*.bin`) — keeps them retrievable vs. nuking; (c) `git repack -adf` after `gc` and again after `filter-repo` (56 packs → 1); (d) backup-verify gate (`git -C <backup> fsck --full`) *before* the quiesce window; (e) handle harness `EnterWorktree`-managed worktrees post-rename (a chat in one must `ExitWorktree --keep` then re-enter, or re-point upstream); (f) extend `U-GC-22`'s doc sweep to `GSD_QUICK.md`, `GSD_MICRO.md`, `DEV_PROTOCOL.md`, `CLAUDE-CODEX-COORDINATION-DIRECTIVE.md`, `~/.claude/commands/{checkin,sync,handoff,six-chat-*}.md`; (g) enumerate "other clones" (does `C:/PRISM/` exist? laptops? CI checkout caches?) for the post-force-push re-sync; (h) pick the *create* path for `RESUME_AT_WORK.md` (the §8 silent-overwrite history is load-bearing context the topic-naming doctrine leans on) — don't leave it "operator's choice".

**Existing-tooling-leverage (don't reinvent):**

15. `trunk-guard.mjs` (new SessionStart hook) and the `worktree-commit-route` trunk-check should share a `.claude/hooks/lib/canonical-trunk.mjs` module (one `git merge-base --is-ancestor` impl + the `PRISM_CANONICAL_TRUNK` constant), not duplicate the logic. `/checkin` already does the divergence-from-origin check — `trunk-guard` *extends* it with a trunk-ancestry assertion; say so, and have U-GC-08's doc-reconcile mention `/checkin`. `git-anti-clobber.mjs.disabled` exists and is worktree-aware — the roadmap should explain why it's disabled and whether reviving it is in scope. `/sync-rebase`'s `--base=main` is an *active footgun today* (rebasing a cad-f branch onto unrelated `main`) → move that one-line fix to **P0**, ahead of the trunk decision.

---

## C. SEQUENCING REVISIONS (consolidated)

1. **P0 first move: the `/sync-rebase --base` fix** — one-line doc edit, zero risk, prevents an unrelated-history rebase any chat could trigger right now.
2. **`U-GC-14` (dirty-worktree sweep) starts day 1**, run its acceptance check continuously, so it's *already green* when the quiesce window opens — it's a fleet-coordination task with latency, not a serial blocker discovered at window-open.
3. **Move branch-cruft cleanup (`worktree-agent-*` ×15), the literal-duplicate `cost-cascade`/`graph-context` pair, the `recovery`-ref cleanup, and the `gc`+`repack -adf` into P0** (zero-risk, modulo rename-first) — so the P2 `filter-repo` has the smallest possible ref-set to rewrite.
4. **Take + verify the ≥2 backups before the quiesce window** — the window then contains only irreversible work (filter-repo + gc-prune + force-push + resync), not the slow `--mirror` clone.
5. **Ship `trunk-guard.mjs` in P1, parameterized** (`PRISM_CANONICAL_TRUNK` env, default `cad-fusion-live-ms0`) — its value is highest *during* the migration churn; flip the default to `main` at the rename (one-line change in `canonical-trunk.mjs`). Don't wait for P4.
6. **Two-pass verification:** a partial pass after P0+P1 (`git fsck`, `count-objects`, hook checks — confirm the 43 GB → ~4 GB win actually landed) *before* committing to the irreversible P2; the full suite after P5.
7. **Don't run a second `gc`** — do all P0 ref-deletions + the dirty-sweep first, then one `gc --prune=now` + `repack -adf` pass.

---

## D. THE 3 SCORES & WHAT THEY MEAN

| Lens | Score | One-line |
|---|---|---|
| **Correctness / feasibility** | **74/100** | Structural diagnosis fully verified; loses points on one non-functional op (`git config worktree.baseRef`), incomplete blob inventory, risky `U-GC-13`/`U-GC-10` push sequencing, mislabeled `gc` mechanism, backwards F11 reasoning, `filter-repo` not installed, 3 conflicting numbering schemes. |
| **Risk / safety / rollback** | **58/100** | Unusually well-structured for git surgery, BUT: no enforcement on the commit freeze (F17 = near-certain data loss), single same-drive 30-day backup (F9/F13/F14), `filter-repo` empty-commit pruning unguarded (F25), blind 20 MB blob cutoff (F22), `U-GC-20` delete-path catastrophic, hook-naive (`rm -rf .claude/...` hard-blocked as written), 40-worktree reset is a manual chore with no "did anyone slip through?" gate. |
| **Completeness / alternatives / doctrine** | **68/100** | Spine good (phase order, tier-floor, dirty-sweep prereq, `--mirror` insurance, warn-first hooks, decision gates). Missing: CI coupling, `master`'s 1326 commits under-atomized, origin-only branches not handled (clone size won't shrink), no auto-triage script, no LFS consideration, no `repack -adf`, handoff-topic break on rename, harness `EnterWorktree` post-rename, "other clones", the `merge --allow-unrelated-histories` option (best one, absent), the `merge-staging` internal inconsistency. Most gaps are *additive*, not structural. |

**Net read:** the roadmap is a solid v1 — keep its spine, apply the 14 P0-blocking + completeness fixes above to produce v2, and it's executable. The single biggest gap is **F17 (freeze enforcement)** — without the temp hard-block hook, the quiesce-window plan is a hope, and the force-push will eat someone's commit. The single biggest correctness fix is **`worktree.baseRef` is a harness setting, not git config**. The single biggest completeness fix is **handle the origin-only branches and `master` as first-class** (the rewrite as designed leaves the remote bloated and `master`'s 1326 commits un-analyzed).

---

## E. NEXT ACTION

Either: (a) I produce **v2 of the roadmap** folding all of the above in (renumbered, with the freeze unit, the corrected `worktree.baseRef` step, Path C, the origin-only-branch + auto-triage-script units, the rename-as-tags mechanism, the hook-collision section) — ~1 pass; or (b) the user picks the trunk (`U-GC-00`) and the archive decision (`U-GC-01`/`U-GC-20`) first, and I produce v2 with those baked in. Recommend (a) then (b).
