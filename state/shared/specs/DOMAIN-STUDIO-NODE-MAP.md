# Domain Studio Node Map — what each `/lathe` / `/mill` / `/wedm` mega-command must wire in

**Generated:** 2026-05-12 by claude-c47ec810 (slot charlie) from the live `state/shared/system-viz/system-graph.json` (19,950 nodes / 75,511 edges, regen'd this session).
**Purpose:** the source-of-truth list of system-viz nodes / engines / AI / NN / tribal / formulas / dispatchers that the next session must combine into one comprehensive slash command per machining domain (per the user's 2026-05-12 request: "make a slash command for each domain machining type to combine engines, skills, tribal knowledge, ai systems, neural network paths, /system-viz … utilize the max amount of logic, deep learning, deep reasoning, all engines/formulas/algorithms (invent new ones for max variability if needed), speed/feed, physics engines, tooling … to generate perfected, hard-coded, machine-labeled programs; coding must match controller + machine capabilities").
**Status (updated 2026-05-12 by claude-8f2683e8 / alpha):**
- ✅ ENUMERATION DONE (this file).
- ✅ **3 mega-commands BUILT** — `H:/prism/.claude/commands/{lathe,mill,wedm}.md` (208 / 232 / 250+ lines each — `/lathe` has §9 macro-program detail + §10 training/learning; `/mill` and `/wedm` have §9 training/learning; `/wedm` §9a heavily references the taptite-electrode Excel macro). Slash commands surface in the project skill list.
- ✅ **`MacroLibraryEngine` BUILT** — `mcp-server/src/engines/MacroLibraryEngine.ts` cataloging the 4 Okuma OSP macros + priority-cascade family matcher + template-placement. Wired to `prism_cad` AND `prism_turning` (4 actions each: `macro_library_list`, `macro_match_family`, `macro_place_template`, `macro_fanout_dry_run`). 31/31 tests pass.
- ✅ **MACRO-PROGRAM-PIPELINE-MS0 SCOPED** — `state/shared/specs/MACRO-PROGRAM-PIPELINE-MS0-2026-05-12.md` (7 units; macro-fill → S(x)-gated emit → per-machine native-dialect labelled programs).
- ✅ **TRAINING-LEARNING-MS0 SCOPED** — `state/shared/specs/TRAINING-LEARNING-MS0-2026-05-12.md` (7 units; per-domain template-extractor engines + electrode-coverage audit + taptite-Excel-macro bridge + closed-loop continuous learning).
- ⏳ **MS0 unit execution = next sessions** — the program-generation half is safety-critical (operator-in-the-loop, S(x) ≥ 0.70 hard block, no bulk-auto-emit).

---

## Per-domain nodes (from system-viz)

| layer | LATHE / TURNING | MILLING | WIRE-EDM |
|---|---|---|---|
| L1 frontend | `fe.pages.lathe` (7 React pages) | `fe.pages.mill` (5) | `fe.pages.wedm` (5) |
| L3 Tier-3 AI | `ai.t3.lathe` — **LatheAGIOrchestrator + Knowledge Graph** | `ai.t3.mill` — **MillingAGIMasterEngine** | `ai.t3.wedm` — **WEDMAGIOrchestrator** |
| L4 dispatchers (domain) | `prism_turning` (`turningDispatcher`), `turningProgramDispatcher` | `prism_mill` (`millDispatcher`), `holePatternDispatcher` | `prism_edm` (`edmDispatcher`) |
| L5 engine clusters | `eng.lathe` (~89 engines), `eng.turning` | `eng.mill` (~240 engines) | `eng.wedm` (62 WEDM engines — see `WEDM_DIGEST.json`) |
| L8/L9/L10 state·registries·tribal | lathe state files, materials/tools/machines/strategies/formulas registries (domain-filtered), lathe wiki + 3,700+ tribal tips + playbook rules filtered to turning | mill state, registries (mill-filtered), mill wiki + tribal + playbook | WEDM state (`WEDM_*.json/jsonl`), registries (5 controller dialects: Mitsubishi/Sodick/Makino/AgieCharmilles/Fanuc), 46 WEDM tribal tips + 14 MIT-cited formulas |

## Cross-domain nodes EVERY studio command must also wire in
- **Physics / formulas / algorithms (L5+L6):** Kienzle force model, Taylor tool-life, tool-deflection, part-deflection, boring-bar deflection (lathe), cutting-temperature + thermal-wear-coupling (RK4 ODE), chatter stability lobe + regenerative-chatter, surface-finish predictor + surface-integrity + residual-stress, advanced-wear-physics, constitutive (Johnson-Cook) — plus the stochastic variants. Canonical constants ONLY from `mcp-server/src/physics/constants.ts` (kc1.1 P=1800 M=2100 K=1100 N=700 S=2800 H=3200). **"Invent new ones for max variability if needed" → route through `prismCreativeReasoningEngine.explore(problem,"optimal")` (15 sci domains, 120+ formulas) + `CrossDisciplinaryDeepLearningEngine`; any new formula must cite its source + be added to constants.ts / the formula registry, never inlined.**
- **Speed/feed:** `SpeedFeedOrchestrator` (central hub, ~2,851 LOC), `UltimateSpeedFeed`, `AutoSpeedFeed`, chip-thinning, `prism_calc` actions; lathe bridge for turning S/F, mill bridge for milling. `/auto-speed-feed`, `/auto-speed-feed-lathe`.
- **Tooling:** tool registry + crib + life-monitoring + standardization; `/tool-select`, `/tool-enrich`, `/tool-crib-guide`; tool-wear progression + stochastic-tool-life (Weibull).
- **Post-processing / controller match:** `PostProcessorPipeline` (38 stages), `LathePostProcessor`, `FiveAxisPost`, the 18-CAM-system bridges; **controller dialect resolution** — Okuma OSP (the `Macro programs/` macros are OSP `VC`-variable), Fanuc, Siemens, Haas, Mazak, Mitsubishi/Sodick/Makino/AgieCharmilles for WEDM, HNC. `/post-generate`, `/post-validate`, `/ppg-quick-start`, `prism_dev` post actions. **Per-machine programs require: ShopConfigurationEngine (21 JM Die machines) → each machine's controller + travel limits + spindle/turret/magazine capabilities → emit a program labelled `<part>__<MACHINE-ID>.MIN` whose codes match THAT machine's controller + envelope.**
- **Safety (UNCONDITIONAL):** `prism_safety:validate_physics`, `S(x)` scoring (hard block < 0.70), Omega gates, `CollisionPreventionEngine`, `machineLimitGuard`, `crossFieldPhysics`, `materialSanity`, the SafetyHooks validators. Every generated program is gated; operator-in-the-loop review is unconditional. `/safety-validation-guide`, `/forge-safety`.
- **AI hierarchy:** Tier-1 Claude (orchestrate) → Tier-2 `FullSystemAICoordinator` (route) → the domain Tier-3 AGI above; `aiSystemRouterEngine.route(task)`; deep reasoning via `ManufacturingReasoningEngine` / `MultiPathReasoningEngine` / `prismCreativeReasoningEngine`. `prism_ai`, `prism_intelligence`, `aiReasoningDispatcher`.
- **Neural-network / deep-learning paths:** `mlDispatcher` (`prism_ml`), `CrossDisciplinaryDeepLearningEngine`, `SurfaceFinishCnnEngine`, the LoRA fine-tuning paths (`/lathe-lora`, `/train-lora`), `SonaLearningOptimizer`, the `nnDeepLearn`/`cnn` engines. Closed-loop learning: every generated/run program → outcome → feeds `feedback_loop_record` + the domain AGI's KG so the system learns from it.
- **CAM kernel + bridges:** `ai.t3.cam` (CAM kernel + 6 tier-1 bridges: Fusion 360, hyperMILL, Mastercam, Esprit, Inventor HSM, SolidWorks), `camDispatcher`, `camFunctionDispatcher`. Print→program: `BlueprintProgramJoinEngine`, `BlueprintOCREngine`, `PrintToProgram` pipeline, the new `PartFolderOrganizerEngine` (`prism_cad:create_part_folder` etc.).
- **/system-viz:** each studio command should query `scripts/system-viz-query.mjs` / `state/shared/system-viz/system-graph.json` to self-report its own coverage (which domain nodes it touched) + flag unwired domain engines.
- **Macro library:** `H:/PRISM/JM DIE/Macro programs/` (4 Okuma-OSP lathe macros: BASE WAFER INSERT MACRO `O1001`, BASIC-CASING, BASIC CASING WITH SINGLE COUNTERBORE, BASIC TOP HAT CASING WITH SINGLE COUNTERBORE — `VC1xx` parametric). ✅ **BUILT:** `MacroLibraryEngine` catalogs these + priority-cascade family matcher + template-placement; wired to `prism_cad` + `prism_turning` with 4 actions each; 31/31 tests pass. ⏳ The safety-gated `MacroFillOrchestratorEngine` (print-dims → VC vars → emit) is **MACRO-PROGRAM-PIPELINE-MS0** U2-U7.
- **Training corpus / starting templates (NEW 2026-05-12 per user — every print, program, CAD/CAM file is training data):** ⏳ **TRAINING-LEARNING-MS0** (7 units) — `LathePartFamilyTemplateExtractorEngine` + `MillPartFamilyTemplateExtractorEngine` + `WEDMPartFamilyTemplateExtractorEngine` + their matchers, `ElectrodeCoverageAuditEngine`, `TaptiteElectrodeMacroBridgeEngine` (READ-ONLY adapter for `H:/PRISM/JM DIE/Automated Program_Corrected 5-25.xlsm` — 5.6 MB, the canonical electrode-program generator for copper-taptite-electrode → sinker-EDM-into-carbide-insert jobs; 73 electrode files + 22 taptite files in corpus to audit), `TrainingTemplateContinuousLearningEngine`, `/learn-corpus` skill. Corpus join via `H:/PRISM/Docustrata/.index/jm-die-index-v2.json` (10.3 MB, rebuilt 2026-05-09). Box-drive S/F values are NOT ground truth.

## Existing skills to subsume / replace
`/lathe-studio` `/lathe-ai` `/lathe-agi-explain` `/lathe-lora` `/lathe-optimize` `/lathe-postgen` `/lathe-print-to-program` `/lathe-thread` `/lathe-validate` `/lathe-masterpost` `/lathe-erp` `/lathe-groove` `/lathe-harden` `/lathe-learn` · `/mill-studio` `/mill-master` `/mill-agi` `/mill-awareness` `/mill-harden` `/mill-learn` `/mill-optimize` `/mill-validate` · `/wire-edm-studio` `/wedm-program` `/wedm-ai-advisor` `/wedm-optimize` `/wedm-validate` `/wedm-harden` `/wedm-learn` `/wedm-reason` (+ ~20 more `/wedm-*`). The new `/lathe` `/mill` `/wedm` mega-commands should be the single entry point that orchestrates the right subset of these + all the nodes above.

## Build order (recommendation)
1. `/forge` + `/rgs` scoping run → milestone for the macro→program pipeline (the safety-critical part). Units: macro-library engine + tests; `MacroProgramFillEngine` (print-dims → VC vars, S(x)-gated) + tests; per-machine post-resolution; the bulk fan-out into part folders; operator-review workflow.
2. The 3 mega-commands (`/lathe` `/mill` `/wedm`) — each ~300-500 line skill wiring everything above. Bounded, non-safety-critical, can build directly.
3. Per-lathe-machine program generation — only after #1's safety pipeline exists. NEVER bulk-auto-emit G-code without S(x) ≥ 0.70 + sim + operator sign-off.
</content>
