# Generated per-brand CAM tool libraries -- large + deterministically regenerable via
# `node scripts/emit-brand-tool-libraries.mjs`. Track the MANIFEST (proof + counts), not the bulk.
*.tools
*.hmt
*.hmt.sql
*.mcam-holders
*.tooldb
PRISM_*.csv
CRON-LOG.jsonl
!MANIFEST.json
!HARNESS-REPORT.json
!.gitignore
