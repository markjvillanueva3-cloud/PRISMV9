# Round-6 Agent 4 — Legal / IP Clearance Gap — **HARD BLOCKER for Master Post**

**Verdict:** Real BLOCKER for Master Post; NEEDS-REVIEW for SFC. The roadmap has ZERO unit for source-data IP clearance, ToS, EULA, or data-licensing (MS1's PCI/GDPR/SOC2 is *customer*-data — the wrong axis). Two load-bearing data sources have file-level evidence of being derived from copyrighted manufacturer/vendor material.

**IP risk by source:**
| Source | Risk | Evidence |
|---|---|---|
| **Controller alarm DBs** (`extracted/controllers/alarms/`) | **LIKELY-PROBLEM** | `MASTER_ALARM_DATABASE.json` `sources[]` names manufacturer **service manuals by part number**: `FANUC_Series0_Maintenance_Manual_B-61395E`, `Haas_Maintenance_Manual_96-0284`, `Mitsubishi M800/M80/E80 Series Alarm/Parameter Manual IB-1501279`, `Okuma OSP-P300 Alarm Manual`, `Siemens 840D Diagnostics Manual`, `SINAMICS S120 Fault Codes Manual`. Schema: `message_text` = *"Exact message displayed on controller screen"*; `MASTER_ALARM_DATABASE_ACCURATE.json` claims `"all_from_documentation": true`. → option (a): **reproduced verbatim** (alarm number is free; descriptive prose / fix tables are the manufacturer's protected expression). Mixed with forum scrapes (`Practical_Machinist_Forums`, `CNCCookbook`, `HelmanCNC`, `MRO Electric blog` — also third-party copyrighted). `dataDispatcher.ts` surfaces `alarm_decode` to end users → the verbatim text is IN the shipping product. ~2,500 alarms. |
| **Post-processor configs** (`PRISM_VERIFIED_POST_DATABASE_V2.js`, `PRISM_FUSION_POST_DATABASE.js`) | **LIKELY-PROBLEM** | Header comments: `// Codes extracted from actual production posts` then `// Source: HAAS_VF2_-Ai-Enhanced__iMachining_.cps`, `// Source: OKUMA-M460V-5AX-Ai_Enhanced-_iMachining_.cps`, `// Source: HURCO_VM30i_Ai-Enhanced__iMachining_.cps`. A `.cps` = an **Autodesk Fusion 360 / HSMWorks post-processor source file** (Autodesk copyrighted JS); "iMachining" = **SolidCAM**. These configs were reverse-engineered from CAM-vendor posts — modification for own use is licensed; **redistribution/resale as a competing product is not**. `PRISM_FUSION_POST_DATABASE.js` has `version:'8.9.400'` + a manufacturers→vendor-URL map. Selling Master Post ($299/controller) as-is = selling a derivative of Autodesk + SolidCAM IP. (The PRISM-authored physics layers `G_FORCE_PHYSICS`/accel tables in `PRISM_POST_PROCESSOR_DATABASE_V2.js` are fine; the controller dialect configs ride on vendor posts. G-code commands themselves — G187, CYCLE800, TRAORI — are the builders' public command sets, not protectable; the *curated config derived from a specific .cps* is the gray zone.) |
| **JM-Die customer programs** (`JM DIE/`, 24,545 files) | **NEEDS-REVIEW (leaning problem)** | `jm-die-profile.ts` has **zero NDA/consent/license language** — only `JM_DIE_CUSTOMERS` (118 names: ITW, Alcoa, Arconic, Holo-Krome, Optimas, Fastenal, Cherry...). Programs are owned by JM Die's customers (or JM Die under contract). Engines consume them as a training/analysis corpus: `JMDieMillProgramHarvestEngine`, `JMDIEPatternAnalyzer`, `CustomerPortfolioMinerEngine`, `JMDieProgramAnalyzerEngine` (+ `ppDispatcher` refs jm-die-profile). Two questions: (1) *learn statistical patterns?* — defensible if anonymized (speeds/feeds aggregates + tool-sequence frequencies are facts); (2) *surface/display raw programs?* — **No** without JM Die + customer consent (a customer program for an ITW fastener die is ITW's confidential IP; showing it to another PRISM customer = misappropriation). The "byte-equivalence acceptance against noisy archive" QA criterion implies *reproduction* — the dangerous mode. Need: a written data-use agreement with JM Die (and JM Die must have the right to grant it — check their customer-PO confidentiality clauses). |
| **MIT course material** (`PRISM_*_MIT.js`) | **CLEAN-mostly** | Algorithm implementations (Bézier, NURBS, ODE solvers, linear algebra, control systems) from MIT OCW courses. Math + algorithms are **not copyrightable** — only specific code/text expression. MIT OCW is CC-BY-NC-SA → forbids commercial use of MIT's *verbatim text/slides*, but PRISM uses the underlying math (Kalman 1960, PID, LQR, marching cubes — public). Caveat: if any file pastes MIT lecture-note prose, that's a CC-BY-NC violation. Low-effort clean: cite the math by inventor, strip pasted prose. |
| **Vendor tool catalogs** (`emuge-tool-catalog.ts`, `guhring-tool-catalog.ts`, Sandvik/Kennametal/ISCAR refs) | **NEEDS-REVIEW** | Headers: `// Extracted from ZK12023_DEGB RevA EMUGE Katalog 160.pdf`, `// Source: Guhring Full Catalog, extracted via scripts/extract-guhring-tools.py`. 13,715 EMUGE tools + Guhring full catalog scraped from vendor PDFs. Part numbers + dimensions are facts; *bulk extraction of a vendor's structured catalog* can run afoul of database-extraction doctrine + the PDF's copyright/ToS. Moderate risk. Sandvik/ISCAR/Kennametal/Walter publish **ISO 13399 data feeds** for exactly this — switching to the official feed eliminates the risk. |
| **Material cutting data** (Kienzle kc1.1, Taylor C/n, Johnson-Cook in `physics/constants.ts`) | **CLEAN** | Published empirical constants from the scientific literature (ISO 3685, Sandvik handbooks, peer-reviewed papers). Physical constants + measured properties = facts, not copyrightable. Canonical kc1.1 (P=1800, M=2100, ...) is textbook. **The one load-bearing source that's genuinely clean.** |

**Load-bearing for revenue:** Master Post **cannot ship** without the post configs AND alarm DB (alarm-decode is a marketed MP feature) — **both LIKELY-PROBLEM. This product is the highest IP exposure in the roadmap.** SFC **cannot ship** without material cutting data — **CLEAN**; it *uses* vendor catalogs for tool recs — NEEDS-REVIEW but **degradable** (SFC works with generic tool geometry). Quote-to-Ship leans on JM-Die priors + alarm DB — degradable (works with anonymized aggregates).
**Net: SFC is shippable after a vendor-catalog review. Master Post is NOT shippable until post configs + alarm DB are re-derived or licensed. The roadmap treats Master Post as ready-to-monetize — that's the gap.**

**Paths to clean:** Alarm DB → re-derive from machine-builder *operator* manuals (free with machines) + rewrite every cause/fix in PRISM's own words (functional, paraphrasable); strip forum scrapes; keep only number + PRISM-authored description + PRISM-authored fix; ~2-3 weeks LLM-rewrite + spot-check → CLEAN. Post configs → re-author from machine-builder *programming* manuals (full G/M-code refs); delete + rebuild every `// Source: *.cps` config (flagged: Haas VF2, Okuma M460V/LB3000, Hurco VM30i — audit all); keep PRISM's physics/dynamics layers; OR partner with the builders (Haas/Okuma ISV programs — a "Haas-certified post" is *more* sellable); ~3-4 weeks. JM-Die corpus → anonymize hard (strip names/part-numbers/identifying comments; keep statistical features); written data-use agreement; **never surface raw programs in the product** (kill byte-equivalence reproduction for customer-facing features; OK for PRISM's internal regression testing only); treat JM Die as a design partner. Vendor catalogs → switch to official ISO 13399 feeds; request EMUGE/Guhring catalog-data permission (routine).

**Proposed `MS-LEGAL` (~13 units; [BLOCKER] = gates revenue; critical path = `U-LEGAL-02/03/04/10/13`):** `U-LEGAL-01` IP-source inventory & risk register (`IP_SOURCE_REGISTER.json`) · `U-LEGAL-02` engage IP counsel — opinion on verbatim alarm text / .cps-derived posts / JM-Die learn-vs-display / bulk catalog extraction [BLOCKER] · `U-LEGAL-03` re-derive alarm DB from public operator manuals + PRISM-authored prose; delete forum/verbatim entries [BLOCKER — Master Post] · `U-LEGAL-04` re-author post configs from programming manuals; quarantine+rebuild every `// Source: *.cps`; tag PRISM-original physics `// PRISM-AUTHORED` [BLOCKER — Master Post] · `U-LEGAL-05` vendor-catalog migration to ISO 13399 feeds / written permissions [BLOCKER — SFC tool-rec; SFC core ships without] · `U-LEGAL-06` JM-Die data-use agreement (PRISM may learn-not-redistribute; JM Die warrants right to grant) [BLOCKER — JM-Die-surfacing features] · `U-LEGAL-07` JM-Die anonymization pass (kill byte-equivalence reproduction in customer surfaces) · `U-LEGAL-08` MIT/academic audit (math only; re-cite by inventor; strip pasted OCW prose) · `U-LEGAL-09` ToS [BLOCKER — public launch] · `U-LEGAL-10` EULA / subscription license (license-not-sale, per-controller/seat, no-redistribution of generated posts, audit rights; reconcile with `PRISM_SUBSCRIPTION_SYSTEM.js`) [BLOCKER — Master Post sale] · `U-LEGAL-11` privacy policy + DPA [BLOCKER — EU customers] · `U-LEGAL-12` output-IP + indemnification policy (PRISM doesn't own customer G-code output; indemnifies on PRISM-supplied data — viable only after `U-LEGAL-03/04`) · `U-LEGAL-13` **"legally clean to sell" sign-off gate — NO Master Post or SFC revenue unit closes until this passes.**

**Note on `feedback_no_public_h_drive`:** that rule bars *public publishing* of H: code/data; a SaaS (customer uses it over the web, doesn't receive code/data) is arguably neither "public publishing" nor purely "internal" — and a **desktop app distributes the code+data** (the v7.3 directive), which DOES conflict. Get explicit per-artifact user clearance before launch; the desktop build must contain only `U-LEGAL-13`-cleared data.

**Key files:** `extracted/controllers/alarms/{FANUC,MASTER}_ALARM_DATABASE*.json`, `extracted/controllers/ALARM_SCHEMA.json` (`message_text` = "Exact message displayed"), `extracted_modules/complete_extraction/PRISM_VERIFIED_POST_DATABASE_V2.js` (×4 `.cps` source comments), `extracted_modules/databases/PRISM_FUSION_POST_DATABASE.js`, `extracted/engines/post_processor/PRISM_POST_PROCESSOR_DATABASE_V2.js`, `mcp-server/src/data/jm-die-profile.ts` (118 names, zero consent), `mcp-server/src/data/{emuge,guhring}-tool-catalog.ts` (PDF-extracted), `mcp-server/src/tools/dispatchers/dataDispatcher.ts` (`alarm_decode`), `mcp-server/src/engines/{JMDieMillProgramHarvest,JMDIEPatternAnalyzer,CustomerPortfolioMiner,JMDieProgramAnalyzer}Engine.ts`, `mcp-server/src/physics/constants.ts` (the clean source).
