# Round-6 Agent 5 — "Make It Work" Engine Critical-Path Gap

**Verdict:** v7.2's "MS3 wiring = background lane, OFF the GA critical path" is **mostly right for SFC, dangerously wrong for Master Post, and the round-3.5 '5 net-new engines' claim is EMPIRICALLY FALSE** (all 5 exist as TS; 4 of 5 already wired). The genuinely-missing-engine count is **near zero for SFC core**.

**SFC — what the product needs:**
| Engine | Status |
|---|---|
| `KienzleForceModelEngine` (828ln, 89 math) | wired-OK (`prism_calc:kienzle_force` → real Fc) |
| `CuttingForceEngine` (246ln) | wired-OK (`cutting_force` → real Kc = kc1.1·h^-mc) |
| `SpeedFeedOrchestratorEngine` (3,570ln, 171 math) | wired-OK (`sf_orchestrate/sf_quick/sf_resolve_*/sf_compare/sf_optimize` — full machine/tool/material/holder/coolant resolver, real) |
| `UltimateSpeedFeedEngine` (3,068ln) | wired-OK (`sfc_calculate` routes) |
| `ManufacturingCalculations.calculateSpeedFeed` (1,348ln) | wired-OK (`speed_feed`) |
| Taylor tool-life (CANONICAL_TAYLOR + Bayesian/Monte-Carlo engines) | wired-OK |
| `SpindleTorqueCurveEngine` (751ln) + `SpindlePowerCheckEngine` | **wired-OK** — round-3.5 said "MISSING (SpindleCharacteristicEngine)" → FALSE |
| `ToolCatalogEngine` (2,667ln, 72 math) + `ToolCatalogAdaptiveEngine` + `ToolDatabaseBridgeEngine` | **wired-OK** — round-3.5 said "MISSING (ToolCatalogEngine)" → FALSE |
| `MaterialDatabaseEngine` (1,037ln) + `MaterialDatabaseBridgeEngine` | **wired-OK** — round-3.5 said "MISSING (MaterialBandResolverEngine)" → FALSE as a build; real gap = *data coverage* (11/60 hardness×coolant cells), not a missing engine |
| `SurfaceRoughnessEngine` (248ln) | **wired-OK** — round-3.5 said "MISSING (SurfaceRoughnessPredictor)" → FALSE; thin → needs Brammertz-model enrichment, not a new engine |
| `DeflectionOverlayEngine` + `ToolDeflectionPredictionEngine` + `TimoshenkoDeflectionEngine` + `PartDeflectionEngine` | **wired-OK** — round-3.5 said "MISSING (DeflectionCalculateEngine)" → partially FALSE; 4 deflection engines wired. Real gap: no discrete `prism_calc:deflection_calculate` action returning numeric δ + ap-derate → **action-surfacing, not engine-build** |
| `GilbertEconomicSpeedEngine` (254ln, 13 math) | **exists-but-UNWIRED** — round-3.5 said "DOES NOT EXIST" → FALSE; has Gilbert/Taylor V* math; needs wiring + `prism_calc:lathe_cost_optimize` action, not a build |
| `mill_chatter_predict` / `mill_force_calculate` | **is-a-stub** — `MillingForceEngine.ts` = 15-line `// WIRE-EXEMPT` stub returning `{ok:false,stub:true}`; millDispatcher routes here |
| `mill_scientific_analyze`/`_optimize`/`mill_uncertainty_quantify` | **is-a-stub** — `MillScientificPipelineEngine.ts` = 14-line stub (the real one is `MillingPhysicsKernelEngine` 1,924ln — `mill_physics_force` already routes to it) |
| `ChatterStabilityLobeEngine` (35KB), `CoolantOptimizationPhysicsEngine` (25KB), `RoughnessConversionEngine`, `MachineAwareSpeedFeedEngine` (16KB), `ProvenSpeedFeedAggregatorEngine` (16KB), `SpeedFeedDeepLearning/AdvancedAI/UltimateAI`, `SpeedFeedResourceIntegrationEngine` (59KB), `ToolDatabaseDeepLearningEngine`, `ToolLifeGnnEngine`, `SpindleTorqueGateEngine` | **unwired-needs-wiring** — all on the 875 list; v7.2 `U-REV-MS0-HOIST-SFC-01` enumerates 8 |

**SFC genuinely-missing-engine count: 0-1** (the only arguably-greenfield item is `ThermalFieldEngine` — v7.2 `U-REV-MS0-HOIST-MILL-02-NEW` correctly flags "no equivalent in unwired pool"). Everything else = **wire 8-12 engines + surface 3 actions (`deflection_calculate`, `lathe_cost_optimize`, `material_resolve`) + de-stub 6 mill actions + enrich SurfaceRoughness/MaterialDB data ≈ 6-9 dev-days** — NOT round-3.5's 14 days.

**Master Post — what the product needs:**
| Engine | Status |
|---|---|
| `PostProcessorEngine` (480ln) | wired-OK — real `process()`, DIALECTS table, canned-cycle emission. `prism_cam:post_process` → here via `getEngine("post")` |
| `MasterPostProcessorEngine` (902ln) | wired-OK — real `process(segments, config)`: header/HSM-init/per-segment G-code, MACHINE_FEATURE_DB. `master_post_process` → here |
| `GCodeTranspilerEngine` (403ln) | wired-OK — real `transpile()`: dialect mapping, cycle conversion, safe-start. `gcode_transpile` → here |
| `MasterPostGeneratorEngine` (2,275ln), `LathePostProcessorEngine`, `LathePostProcessorDialectValidatorEngine`, `MasterPostProcessorGeniusEngine`, `...AGIOrchestrationEngine` | wired-OK |
| **`post_process` dispatch fallback** | **STUB-RISK** — `camDispatcher.ts:~2199`: `result = engine.process?.(...) ?? engine.compute?.(params) ?? { post_processed: true, controller }` — if a post engine lacks `.process`, callers silently get `{post_processed:true}` (false-green). Not currently triggered but unguarded → replace with `callOrThrow` |
| `MasterPostProcessorUnifiedAGIEngine` (58KB), `MasterPostFineTuningEngine` (35KB), `PostProcessorUnificationEngine` (4KB), `LathePostProcessorAIEngine` (73KB), `LatheMasterPostRouterEngine`, `...UnifiedOutputEngine`, `...SelfAwarenessEngine`, `...DeepReasoningEngine`, `...EnsembleCrossCheckEngine`, `...APIEngine`, `...RegressionMatrixEngine` (~10-12 lathe-post engines) | **unwired-needs-wiring** — these are the Master-Post *product-line differentiators* (router, ensemble cross-check, deep-reasoning), **on the Master-Post-GA critical path, NOT background** |
| `GCodeTemplateEngine` (58KB), `DNCGenerateEngine`, `SoftJawBoringGCodeEngine`, `OkumaMacroConverterBridgeEngine`, `OkumaMachineStepIngesterEngine`, `OkumaManualTipExtractorEngine`, `OkumaGosigerTranscriptMinerEngine`, `JmDieMachineConfigEngine`, `WEDMPostFanucEngine` | **unwired** (mostly background — controller-knowledge mining) |
| Monolith ports available: `PRISM_POST_PROCESSOR_DATABASE_V2.js`, `PRISM_POST_PROCESSOR_GENERATOR.js`, `PRISM_GUARANTEED_POST_PROCESSOR.js`, `PRISM_CONTROLLER_DATABASE.js`, `PRISM_CONTROLLER_OUTPUT.js`, `PRISM_OKUMA_OSP_CONTROL_ENGINE.js`, `PRISM_EXPANDED_POST_PROCESSORS.js`, `PRISM_RL_POST_PROCESSOR.js`, `PRISM_TOOLPATH_GCODE_BRIDGE.js`, `PRISM_SIEMENS_5AXIS_CAM_ENGINE.js` | exists-in-monolith — useful for controller-coverage expansion; core post path already functions |

**Master Post genuinely-missing-engine count: 0** (core path real). BUT the product line (44 units, per-controller productionization) needs ~10-12 lathe-post engines wired + the `post_process` stub fallback removed.

**Round-3.5 "5 P0 net-new" reality (0 of 5 genuinely net-new):** `SpindleCharacteristicEngine` "MISSING" → `SpindleTorqueCurve`+`SpindlePowerCheck` exist & wired → surface action, ~0d · `ToolCatalogEngine` "MISSING" → 2,667ln exists & wired → surface `tool_catalog_resolve`, ~0.5d · `DeflectionCalculateEngine` "MISSING" → 4 deflection engines wired → add discrete action + ap-derate solver, ~1d · `GilbertEconomicSpeedEngine` "DOES NOT EXIST" → 254ln exists, UNWIRED → wire + `lathe_cost_optimize`, ~0.5d · `MaterialBandResolverEngine` "MISSING" → `MaterialDatabaseEngine`+Bridge exist & wired → add `material_resolve` interpolator + expand 60-cell data, ~2d (mostly data). So "5 net-new, 14 days" → **~4-5 days of wiring + action-surfacing + data**. Relabel `U-REV-MS0-ENG-*` units from "Build" → "Wire+surface" (Gilbert) / "Surface+enrich" (rest).

**Product-critical of 875 unwired + ~1,029 monolith:** ~37 keyword-match; **GA-blocking ≈ 8 SFC** (`MachineAwareSpeedFeed`, `ProvenSpeedFeedAggregator`, `ChatterStabilityLobe`, `CoolantOptimizationPhysics`, `SpindleTorqueGate`, +3 AI-variant SpeedFeed) + **~12 Master Post** (the lathe-post cluster + `GCodeTemplateEngine` + `MasterPostProcessorUnifiedAGIEngine`) = **≈20 of 1,900 (~2.3%)**. The other ~855 unwired + ~1,029 monolith are genuinely background (context engines, AI plumbing, per-CAM-vendor mining, learning loops); no SFC/MP *page fails to render* without a monolith port.

**Recommendation: carve `MS0-CRITWIRE` (~20 units, ~3 dev-days, 2 chats) out of MS3, hoisted to MS0-P0:** SFC-critical (8 engines → `prism_calc`/`prism_safety` + 3 actions + 60-cell data expansion + de-stub `mill_chatter_predict`/`mill_scientific_analyze` — **SFC-blocking, not MS2; reroute `MillingForceEngine`→`MillingPhysicsKernelEngine`, `MillScientificPipelineEngine`→`prism_calc` chain**); Master-Post-critical (~10-12 lathe-post engines → `prism_cam` + remove the `post_process` `?? {post_processed:true}` stub fallback). What does NOT need carving: the ~855 other unwired, all ~1,029 monolith, MS3.5 (415), ML-plumbing (289) — v7.2 is right those stay background.

**Date impact:** SFC GA ~May-27 = **realistic** if MS0-CRITWIRE SFC subset (~4-5 dev-days, parallelizable → ~3 calendar days) lands Week 0-1 AND the 60-cell material-data work isn't underestimated AND `U-REV-CI-00` (the CI non-stub gate — round-5 a9 found ZERO CI enforcement of non-stub returns) lands FIRST (else GA ships on false-green tests). Master Post GA ~June-24 = **AT RISK** — if the ~10-12 lathe-post engines stay buried in the 875-backlog "continuous lane," MP slips ~3-4 weeks (can emit Fanuc/Haas but the differentiators won't be reachable); if hoisted into a Master-Post-P0 set + MS3 lathe-first batches genuinely run in parallel Week 3-7, June-24 holds.

**Key files:** `state/shared/UNWIRED-ENGINE-AUDIT-2026-05-07.json` (THE real 875 list — `BUILD_STATE.json` has only a misleading 25-engine sample), `mcp-server/src/engines/MillingForceEngine.ts` + `MillScientificPipelineEngine.ts` (the 14-15-line stubs), `mcp-server/src/tools/dispatchers/camDispatcher.ts:~2199` (`post_process` `?? {post_processed:true}` fallback), `state/shared/specs/REVENUE-ROADMAP-v7.2.md:~265-281` (the `U-REV-MS0-ENG-*` units to relabel), `state/shared/audit-findings/revenue-roadmap/round5/09-ci-audit.json` (R5A9-002/003: no CI enforcement of non-stub returns).
