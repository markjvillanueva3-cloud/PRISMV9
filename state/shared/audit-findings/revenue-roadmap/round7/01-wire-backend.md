# Round-7 Agent 1 — Backend Wiring Topology (full detail folded into v7.4 §R8.1)

**Source of truth:** `state/shared/UNWIRED-ENGINE-AUDIT-2026-05-07.json` (3,177 canonical engines · 2,139 wired-direct · **875 unwired** · 81 wire-exempt · 56 via-orch / 15 via-route / 10 via-hook / 1 via-singleton — each unwired record carries a `suggestedDispatcher`). NOT `BUILD_STATE.json` (25-engine sample only). Dispatchers: `H:/prism/data/docs/DISPATCHER_DIGEST.md` = 78 dispatchers / 4,130 actions (`mcp-server/data/docs/` copy doesn't exist). Monolith: `extracted_modules/EXTRACTION_PRIORITY_LIST.json` = 932 to TS-port (priority AI_ML 94 → PHYSICS 54 → GEOMETRY 85 → DATABASES 127 → SYSTEM 79 → OTHER 437); realistic net-new yield after `duplicationGuardEngine` dedup ≈ 250-350. No `check-engine-wired.mjs` / `wiring-batch.mjs` exist yet (deliverables). Existing gates: `stop-auto-wire.mjs` (advisory), `stop_on_unwired_assets.mjs` (HARD BLOCK on new orphans), `stop_on_orphan_engine.mjs`, `tribal-autowire.mjs`. Audit generator: `scripts/audit-unwired-engines.mjs`.

**Wiring rule matrix (28 categories → logical targets, weighted avg ≈ 2.05 targets/engine ⇒ ~1,550-1,800 new dispatcher action surfaces):** see v7.4 §R8.1 for the full table. Key rules: physics→`prism_calc`+`prism_safety`; cam→`prism_cam`+(turning∥5axis∥cad); ai_reasoning→`prism_ai`+`prism_intelligence`; memory→`prism_memory`+specialized-consumer; safety→`prism_safety`+`prism_calc`+domain-collision; lathe→`prism_turning`+`prism_cam`+(calc∥business); the 397 "UNKNOWN" bucket → per-engine triage (~60%→`prism_dev`/`prism_context`, ~25%→real domain, ~15% WIRE-EXEMPT).

**Unwired backlog by category (sums to 875):** other/dev-tooling/mis-bucketed-mechanical 397 (the P0 triage target) · lathe 105 (REV) · wedm 60 (REV) · cam 38 (REV) · machine 29 · infra 26 · mill 22 (REV) · ai_reasoning 21 · cad 20 (REV) · ai_ml_opt 20 · business 20 (REV) · session 18 · tool 17 · fluid_thermal 13 · neural 13 · knowledge 13 · hook 7 (many exempt) · skill_script 6 · physics 6 (REV) · safety 5 (CRITICAL) · material 4 (REV) · nontrad 4 · fixture 3 (SAFETY) · quality 3 (REV) · memory 3 · mechanical 2.

**Milestone `MS-WIRE-BACKEND`:** P0 (~4 batches, ~1.5 dev-wk) = audit-script upgrade + `wiring-batch.mjs` + `check-engine-wired.mjs` + the ~29 product-critical wirings (= §R7.2 `MS-CRITWIRE`). P0b (~10 fast batches, ~2 dev-wk) = the 397-UNKNOWN post-triage. P1 (~37 batches @ 9.5, ~7.5 dev-wk) = ~345 revenue-domain unwired (lathe 11 → wedm 7 → cam 4 → cad 2 → mill 3 → machine 3 → business 2 → tool 2 → +3). P2 (~9 batches, ~2 dev-wk) = ~88 ai/neural/opt/knowledge/session/memory. P3 (~6 batches, ~1.5 dev-wk) = ~54 infra/hook/skill_script/fluid_thermal/mechanical. **Foreground total ≈ 66 batches ≈ 14.5 dev-wk.** Background lane (off critical path) = monolith ports M1-M6 ≈ ~50 batches @ 6 modules ≈ ~10 dev-wk parallel; pull M1 (AI/ML, ~45 net-new) forward (revenue-adjacent). **Grand total all-875-wired + 932-modules-dispositioned ≈ ~116 batches ≈ ~24.5 single-dev dev-wk / ~10 calendar wk @ 3 parallel.** Burndown today ≈ ~62% (Σ reached/Σ expected); target ≥95%.

**Verification gate:** `check-engine-wired.mjs` (NEW) reads `state/shared/wiring-rule-matrix.json` (the §R8.1 table as data) → per engine: classify category, look up logical targets, grep dispatchers for actual wiring, compute reached/expected. `--ci` fails the GitHub job if UNWIRED-or-PARTIAL count increased vs `BASELINE_INVENTORY.json` (anti-regression); `--stop` blocks Stop if this session's diff introduced a new orphan/partial; `--report` writes `state/shared/WIRING-COVERAGE.md`. Each batch unit acceptance: per wired engine, an `<EngineName>.test.ts` `it()` calling EVERY wired action, asserting a concrete non-stub result. Milestone "done" = `UNWIRED==0 && WIRED-PARTIAL<50` (residuals → `state/shared/WIRING-EXCEPTIONS.md` ledger). WIRE-EXEMPT-count growth gated (no "tag-everything-exempt" gaming).
