# Round-7 Agent 4 — SFC Calibration + Force/Algorithm/Formula Combinatorics (full detail folded into v7.4 §R8.4)

**`MS-SFC-CALIBRATE` = 24 units, ~10 dev-wk, 3 phases + ongoing.** Already exists: a live HSMAdvisor UIA-comparison harness (`H:/prism/output/hsmadvisor-live-current-case-prism-compare.json` shows **0.79% RPM delta, 4.49% feed delta vs HSMAdvisor** on the spot case), a 2,676-scenario variability sweep (`calculator-live-variability-sweep-*-hsm-*`), `calculator-live-audit.ts`, ~120 force/formula `prism_calc` actions across ~80 engines, the v7.B combinatorics table (~50 rows in v7.1). **No HSMAdvisor *install* found on H:** — only the prior scrape output + Autodesk HSMWorks/Inventor-HSM CAM (NOT Gerfanov's calculator). So the HSMAdvisor matrix-runner (P0-02) assumes the app is available to re-scrape, else falls back to manual-export + the existing `hsmBaseline` snapshot.

**The 12 benchmarks:** B1 HSMAdvisor (UIA scrape — *the* "my HSMAdvisor") · B2 G-Wizard (manual CSV, ~200 cells) · B3 FSWizard (manual, ~100, floor sanity) · B4 Harvey/Helical "Machining Advisor Pro" (Playwright form-fill, ~300 tools — best for end-mill-geometry effects) · B5 Sandvik CoroPlus/Machining-Calculator (scriptable — *the Kienzle anchor*; PRISM's kc1.1/mc should reproduce Sandvik's kc to <2% by construction) · B6 Kennametal NOVO (login-gated; proxy via the already-extracted Kennametal cutting tables) · B7 Walter Machining Calculator (Playwright) · B8 ISCAR/Seco/Tungaloy (~50 cells each, vendor-spread bracketing) · B9 internal `extracted/materials*` cutting-data DBs (Sandvik/Kennametal-calibrated, 1,047 materials — `prism_data:cutting_data_recommend`; the *primary always-available* benchmark) · B10 internal JM Die proven programs (24,545 files — `SpeedFeedMinerEngine`+`ProvenSpeedFeedAggregatorEngine`; the *ground-truth* — vendor calcs advisory, proven empirical) · B11 internal tribal tips (~3,700 — operator multipliers like "Vc×1.00, fz×0.90 conf=58%"; a prior adjustment) · B12 reference ISO 3685 / Machinery's Handbook 31st / ASM Vol.16 (Taylor C/n, baseline Vc — physics-first sanity bound for exotics). Per cell: `residual = (prism − benchmark)/benchmark` for {rpm, vf, fz, ap_max, mrr, power}; aggregated P50/P90/P95 per {benchmark × ISO × op × tool}.

**The force/algorithm/formula inventory (~120 actions, ~80 engines — see v7.4 §R8.4 for the full enumeration):** force/shear — Kienzle (`kienzle_force`/`kienzle_milling`/`kienzle_size_effect`), Merchant (`merchant_analysis`), Oxley/Lee-Shaffer (`sci_oxley`/`sci_oblique` — for Ti/Inconel where Kienzle under-predicts 15-40%), Zorev (`zorev_stress_distribution`), Piispanen/thick-shear-zone, helix-decomposition, mechanistic milling forces (`milling_forces`), turning force, drilling thrust+torque, specific cutting energy (`specific_cutting_energy`), Johnson-Cook flow stress, stochastic force. Tool-life — basic+extended Taylor, Usui crater, combined wear, Coffin-Manson, Bayesian/Monte-Carlo, Gilbert economic speed, Archard, thermal-wear-coupling. Chip — `chip_thinning`/`chip_thinning_compensation` (full Sandvik `h_ex = fz·sin(κ)·sin(φ_ex)`, `φ_ex = arccos(1−2ae/D)` — the v7.B Row-5 arcsin fix), ball-nose/round-insert, built-up-edge, Brammertz. Deflection — `tool_deflection_predict`, `timoshenko_deflect`/`_multi_section`/`_compare`/`_max_ld`, `tool_assembly_deflection`, part/workpiece/boring-bar, the iterative max-ap-under-deflection loop (v7.B Row 2, zero new engines), stochastic. Thermal — Loewen-Shaw/Trigger/Peclet/damage-number/expansion/growth/compensation/wear-coupling/sim, stochastic. Chatter — Altintas SLD (with the `Re[Φ(ωc)]≥0` guard — v7.B Row-3 fix), regenerative, multi-frequency, neural-classify, variable-helix, MDOF, process damping, `StabilityRPMRewriterEngine`, stochastic. Surface finish — `surface_finish`/`_roughness_calc`, Brammertz, scallop, `SurfaceFinishDatabaseEngine`, `surface_finish_cnn`, stochastic. MRR/power/torque/dynamics — `power_torque`, `spindle_torque_curve`/`_check`/`_available`, `mrr`, Colding, corner dynamics + `arc_feed_correction`, trochoidal/HSM/`adaptive_feedrate`. Recommendation layer — `SpeedFeedOrchestratorEngine` (`sf_orchestrate`/`sf_quick`/`sf_compare`/`sf_optimize`/`sf_resolve_*`/`sf_stochastic`), `UltimateSpeedFeedEngine`, `MachineAwareSpeedFeedEngine`, `ProvenSpeedFeedAggregatorEngine`, `SpeedFeedMinerEngine`, `SpeedFeed{DeepLearning,AdvancedAI,UltimateAI,Autopilot}Engine`, `SFC*` engines, `Lathe SpeedFeed*` (4), `PPGSFCClosedLoopOrchestratorEngine`. Optimization layer — `bayesian_optimize`/`pso_optimize`/`aco_optimize`/`sa_optimize`/`moo_nsga2`/GP-surrogate/`de_optimize`/`ga_optimize`/`gradient_optimize`/`bfgs_optimize`/`trust_region_minimize`/`pareto_optimize` (for "max MRR s.t. power≤limit ∧ deflection≤tol ∧ chatter-free ∧ tool-life≥target"). Calibration/uncertainty infra (the milestone WIRES it, doesn't build it) — `bayesian_calibrate`, `physics_calibrate_submit`/`_predict`/`_state`/`_reset`, `calibration_model_select`/`calibration_kienzle`/`calibration_taylor`/`calibration_surface_bias`/`calibration_drift`/`calibration_thermal`, `monte_carlo_simulate`/`monte_carlo_process`, `uncertainty_pipeline`/`_chain`, `doe_taguchi`, `lhs_sample`, `consistency_check`, + `AdaptiveCalibrationEngine`/`PredictionCalibrationEngine`/`StratifiedCalibrationEngine`/`ConformalCalibrationMonitorEngine`/`OutcomeDriftCalibrationBridgeEngine`/`MultiControllerCalibrationEngine`/`CrossProcessCalibrationAuditorEngine`.

**The synthesis methodology — Stacked Bayesian Model Averaging over a regime-routed ensemble, with a physics-prior backbone:** (1) physics-first backbone (never a black box — the SFC sells on "here's the math"): material → ISO-group kc1.1/mc (or J-C→Merchant for ISO S/H + HSM) → Kienzle force → power/torque → **clamp to spindle curve** → chip-thinning feed → **clamp to deflection-limited ap** (iterative) → **snap RPM to stable SLD lobe** → Taylor/Gilbert economic-speed check → Brammertz finish check; each clamp = a hard physical constraint, not a learned weight. (2) regime routing (`calibration_model_select`): route to the model *family* with the lowest validated residual for that {ISO × op × tool-class × hardness-state} cell. (3) Bayesian model averaging within the regime: final = Σ wᵢ·predᵢ, wᵢ ∝ exp(−residualᵢ²/2σ²), online-updated; stack {Kienzle-physics, J-C-Merchant, vendor-table-lookup (B9), proven-program-aggregate (B10), DL-advisor}; vendor + proven get high prior weight, physics gets high weight when residual small, DL gets weight only where validated; tribal multipliers (B11) as post-hoc Bayesian adjustment weighted ∝ tip confidence. (4) Monte-Carlo confidence band (`monte_carlo_simulate` + stochastic-force/wear/deflection): output = `{rpm,vf,fz}` ± P10-P90 band + a `confidence` score (currently `null` in the HSMAdvisor compare — this milestone fills it). The calibration loop: out-of-tolerance cell → `bayesian_calibrate`/`physics_calibrate_submit` (update wᵢ) + `calibration_kienzle`/`calibration_taylor` (nudge within physical bounds: kc1.1 ±15%, n ±0.05) → re-synthesize → re-measure → `SFC_CALIBRATION_LEDGER.json`; `SFCDriftCanaryEngine` watches: >5% drift → re-trigger. JM Die proven-program ingest = outcome-capture loop (`SpeedFeedMinerEngine` → B10 re-weight via `SFCOutcomeCaptureWireEngine`).

**Variability matrix + sampling:** axes — material (1,047 → ~6 ISO × ~4 hardness/condition ≈ 24 classes for the grid; `SFCFewShotNewMaterialEngine` handles new) × operation (~23) × tool class (~12) × machine (21 → ~6 capability classes) × condition (~10). Full Cartesian ≈ 400K → sampling: Tier-0 = ~300 cells JM Die actually runs (tuples ≥10× in the 24,545 proven programs — revenue-critical; validate vs B10+B9+B1) · Tier-1 = Taguchi orthogonal array (`doe_taguchi`, ~50-80 — every main effect + 2-way; validate vs all benchmarks) · Tier-2 = LHS fill (`lhs_sample`, ~200 — smoothness/no-cliff; the 2,676-cell mill sweep covers a chunk) · Tier-3 = adversarial (~50 — Inconel low-ae HSM, hardened-D2 hard-turning, thin-wall 6061, deep-hole peck-drill Ti, full-slot 316) · Tier-4 = drift watch (rotating 5% weekly). First-pass ≈ 600-700 cells, not 400K.

**Accuracy target (DoD):** Tier-0 — within ±8% of median benchmark on {rpm,vf,fz,ap_max} for ≥90% of cells; within ±5% of the JM Die proven value for ≥80%; power ±15%. Tier-1/2 — within ±12% of HSMAdvisor on {rpm,vf,fz} for ≥85%. Tier-3 — sits *inside* the vendor-calculator cloud (min/max of {B1,B4,B5,B7}) for ≥75%, and NEVER violates a hard physics constraint (power/torque/deflection/chatter) — 100% gate, zero tolerance. Every recommendation ships with a non-null `confidence` + P10-P90 band.

**Unit structure:** P0 (~2 dev-wk, 6 units) — `U-SFCC-P0-01` `SFCBenchmarkMatrixEngine` + `SFC_BENCHMARK_MATRIX.json` · `02` HSMAdvisor matrix runner (parametrize the UIA scrape; cache `hsm_baseline.json` for offline CI) · `03` vendor Playwright scrapers (B4/B5/B7) · `04` internal-benchmark adapters (B9/B10/B11/B12) · `05` `SFCResidualHarnessEngine` → `SFC_RESIDUAL_REPORT.json` (extends `calculator-live-audit.ts`) · `06` manual-export importer (B2/B3/B6/B8) + CI assertion. P1 (~3 dev-wk, 7 units) — `U-SFCC-P1-01` `SFCSynthesisEngine` (the physics-backbone chain, deterministic, logged) · `02` regime router · `03` `SFCBayesianAveragingEngine` · `04` tribal post-adjust + Monte-Carlo confidence band · `05` `SFCCalibrationLoopEngine` → `SFC_CALIBRATION_LEDGER.json` · `06` proven-program outcome-capture loop · `07` customer-facing SFC result API/page returns `{rec, band, confidence, contributing_models[], benchmark_deltas[], formula_trace[]}` — the "here's the math + vs HSMAdvisor/Sandvik" panel. P2 (~2.5 dev-wk, 5 units) — `U-SFCC-P2-01` Tier-0 validation (~300 cells) · `02` Tier-1 Taguchi OA · `03` Tier-2 LHS fill · `04` Tier-3 adversarial · `05` DoD scorecard (`SFC_ACCURACY_SCORECARD.json` + HTML). Ongoing — `U-SFCC-ONG-01` `SFCDriftCanaryEngine` cron. Net-new engines ≈ 8; the rest is WIRING the ~120 existing actions + existing calibration infra into one calibrated output. P0→P1→P2 strictly serial; ONG after P2.

**Open item for the user:** no HSMAdvisor install found on H: — only the prior scrape output (`output/hsmadvisor-*`, `tmp-hsmadvisor-window-map.csv`) + Autodesk HSMWorks/Inventor-HSM (which is NOT Gerfanov's calculator). P0-02 assumes the desktop app is available to re-scrape against the new matrix; fallback = manual-export + the existing `hsmBaseline` snapshot in the 2,676-cell sweep.
