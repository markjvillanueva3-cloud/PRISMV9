## corpus-aggregation — fleet-managed

### Current state
**Size:** 8,569 bytes / 82 lines.
**Quality grade: PARTIAL**

Issues found:
- The TOOLBELT.md has a blank dispatcher section: "_(owning slot lists the domain's prism_* dispatcher actions here)_" — the dispatchers ARE known and verifiable but were never filled in.
- PATHS.md §Domain dispatchers states "_(none cited in CLAUDE.md — owning slot adds the domain's prism_* actions)_" — same gap.
- "Key engines" block in CLAUDE.md (lines 21–32) lists only 12 engines; PATHS.md shows 46 matched engines. The 12 listed were auto-generated by Ollama distillation and are not hand-verified — some (e.g. `CrossProcessFedAvgAggregatorEngine`, `FiveAxisAggregatorEngine`, `HermesParallelVerdictAggregatorEngine`) are clearly non-core to corpus-aggregation and may be false positives from name-matching.
- "High-ROI domain memories" block (lines 35–41) cites opaque bracketed references like `[reference/node_formula_formula_adjusted_caddispatcher_action_cad_corpus_ingest]` — these are graph-node ID strings, not human-readable doctrine; they are advisory/advisory-only per the `<!-- GALAXY-CLAUDEMD-FILL -->` block header and should not be treated as load-bearing.
- No explicit dispatcher action table: the three relevant dispatchers (`prism_resource_harvester` / `prism_resource_harvesting` / `prism_knowledge` corpus actions / `prism_cad_automation` corpus actions) are not listed with their verified action names anywhere in the galaxy CLAUDE.md.
- Canonical slot listed as "kilo (de-facto)" but the galaxy is fleet/golf-managed with no dedicated slot — the kilo label is accurate as de-facto but should be explicit about the no-dedicated-slot status.
- The cross-cutting methodology block (lines 57–83) is verbatim-identical across all 34 galaxies — pure boilerplate duplication that belongs in the universal-core pointer, not repeated here.
- The AI-systems fleet-state block (lines 69–76, also repeated in MEMORY.md) is a pointer-only stanza that is identical across all galaxies — zero corpus-aggregation-specific content.
- The "Critic + keep-working contract" stanza is also fleet-wide boilerplate with no corpus-specific content.
- No "what NOT to do" list anywhere in the file.
- No explicit data-pipeline sequencing (scan → classify → ingest → aggregate → validate → serve) documented.

### KEEP
- §Scope (lines 5–6): accurate 1-paragraph domain summary — load-bearing orientation.
- §Cross-galaxy edges (lines 8–9) and §Related galaxies (lines 11–12): the 5-edge PSN map (pdf-corpus / mit-curriculum / tribal-knowledge / academy / ai-training) is accurate and load-bearing for dependency ordering.
- The two verified engine names that ARE core: `ContentIngestionPipelineEngine.ts`, `CADTrainingCorpusOrchestratorEngine.ts`, `TribalCorpusOrchestratorEngine.ts`, `PrintCorpusOrchestratorEngine.ts`, `JMDieTrainingCorpusEngine.ts`, `SourceCatalogAggregator.ts` (verified by Grep as existing under `mcp-server/src/engines/`).
- The "Loops" sentence in the cross-cutting block is corpus-aggregation-relevant: "corpus ingest /loop (zero-drop streaming); coverage-audit /loop" — keep this, strip the rest.
- §Domain knowledge warning "⚠ Ollama-distilled … advisory; verify engine names against PATHS.md" — accurate caveat, keep as a note.
- The critical-resource-roots block in PATHS.md (auto-generated, wired) with the domain-relevant subset annotation: `resources/PDF`, `resources/RESOURCE PDFS`, `JM DIE/TRIBAL + WIKI`, `Docustrata/.index` — this lives in PATHS.md and should be referenced, not duplicated.
- Standing patterns / invariants in MEMORY.md (lines 66–71): these are domain-critical and NOT currently in CLAUDE.md — promote them.
- The PDF corpus canonical extractor pointer: "USE lima's pypdf page-by-page extractor — `scripts/extract-jm-die-corpus-page-by-page.py`" (verified exists).

### DROP
- Lines 57–76 (entire cross-cutting methodology block): 100% boilerplate, identical across all 34 galaxies. Replace with a single universal-core pointer line.
- Lines 78–83 (Critic + keep-working contract stanza): fleet-wide boilerplate, zero corpus-specific content.
- Lines 69–76 in the AI-systems fleet state block: pointer-only, identical across all galaxies, no corpus-aggregation specifics.
- The "High-ROI domain memories" block (lines 35–41): the opaque graph-node ID references are not actionable doctrine; a chat can't use `[reference/node_formula_formula_adjusted_caddispatcher_action_cad_corpus_ingest]` without resolving it first. Replace with the actual dispatcher action names.
- "Key engines" list (lines 21–32): the 12-engine subset is incomplete and some entries are likely name-match noise. Replace with the 8 verified core engines from MEMORY.md + the PATHS.md 46-engine pointer.
- §Cross-refs footer (lines 54–55): the relative path `../../../../state/shared/specs/DOMAIN-GALAXY-DOCTRINE-2026-05-26.md` adds no operational value to a running chat.

### ADD (domain-specific — the heart of this assessment)

**1. Pipeline sequencing (the operational contract — currently absent)**
The corpus-aggregation pipeline has a defined stage order that every chat working in this domain must follow:
```
SCAN → CLASSIFY → INGEST → AGGREGATE → VALIDATE → SERVE
```
- SCAN: `prism_resource_harvester:scan_folder` (verified: `resourceHarvesterDispatcher.ts:92`) or `prism_resource_harvesting:harvest_scan` (verified: `resourceHarvestingDispatcher.ts:98`)
- CLASSIFY: `prism_resource_harvester:classify_file` (verified: `resourceHarvesterDispatcher.ts:101`) or `prism_cad_automation:cad_corpus_classify` (verified: `cadAutomationDispatcher.ts:331`)
- INGEST: `prism_resource_harvester:start_harvest` → `harvest_status` → `harvest_resume` (verified: `resourceHarvesterDispatcher.ts:38–43`); `prism_cad_automation:cad_corpus_ingest` (verified: `cadAutomationDispatcher.ts:332`); `prism_knowledge:corpus_harvest_mit` / `corpus_harvest_vendor` / `corpus_harvest_online` (verified: `knowledgeDispatcher.ts:163–165`)
- AGGREGATE: `SourceCatalogAggregator.ts` — "Unified query interface for all 28 engine SOURCE_FILE_CATALOG exports" (verified exists)
- VALIDATE: never persist cutting data without reference oracle (see §What NOT to do)
- SERVE: `prism_data:database_search` / `database_list` / `globalSearch`

**2. Verified dispatcher action table (currently entirely absent from CLAUDE.md)**
| Dispatcher | Key corpus-aggregation actions | Source file (verified) |
|---|---|---|
| `prism_resource_harvester` | `scan_folder`, `classify_file`, `start_harvest`, `harvest_status`, `harvest_resume`, `get_index` | `resourceHarvesterDispatcher.ts:38–43` |
| `prism_resource_harvesting` | `harvest_scan`, `harvest_start`, `harvest_status` | `resourceHarvestingDispatcher.ts:31–33` |
| `prism_knowledge` | `corpus_harvest_mit`, `corpus_harvest_vendor`, `corpus_harvest_online` | `knowledgeDispatcher.ts:163–165` |
| `prism_cad` | `cad_corpus_scan`, `cad_corpus_ingest`, `cad_corpus_orchestrate`, `cad_corpus_status`, `cad_corpus_find_by_class`, `cad_corpus_load_manifest` | `cadDispatcher.ts:195,226` |
| `prism_cad_automation` | `cad_corpus_classify`, `cad_corpus_ingest`, `cad_corpus_scan_only` | `cadAutomationDispatcher.ts:331–332,455` |
| `prism_data` | `database_search`, `database_list`, `globalSearch` | runtime; KnowledgeDB registered |
| `prism_memory` | `semantic_search` (recall before re-deriving) | standard fleet |

**3. Verified core engine list (8 confirmed, replacing the 12-entry unverified Ollama list)**
All verified by file existence under `mcp-server/src/engines/`:
- `HarvestPipelineEngine.ts` — orchestrates per-type harvesting from scanned resources (dep of `resourceHarvesterDispatcher`)
- `IngestionOrchestratorEngine.ts` — routes scanned files to domain-specific ingesters
- `KnowledgeIngestionOrchestratorEngine.ts` — knowledge-ingestion orchestrator
- `ContentIngestionPipelineEngine.ts` — unified knowledge ingestion pipeline
- `SourceCatalogAggregator.ts` — unified query interface for all 28 SOURCE_FILE_CATALOG exports
- `TribalCorpusOrchestratorEngine.ts` — tribal corpus orchestration
- `PrintCorpusOrchestratorEngine.ts` — print (blueprint) corpus orchestration + `PrintCorpusTableWriter.ts`
- `JMDieTrainingCorpusEngine.ts` — JM Die training corpus (U-LEARN-03)
- `JMDieDocustrataIngestEngine.ts` — Docustrata document ingest (verified)
- `JMDieFleetWideIngestEngine.ts` — fleet-wide JM Die ingest (verified)
- `CADTrainingCorpusOrchestratorEngine.ts` — CAD training corpus (CAD-COMPLETE-MS0/U-CADC17)
- `CAMTrainingExtractionAggregatorEngine.ts` — CAM extraction aggregator
- `ProvenSpeedFeedAggregatorEngine.ts` — speed/feed data aggregator with statistical analysis

Per-process tribal corpus engines (verified): `GrindingTribalCorpusEngine.ts`, `SinkerEDMTribalCorpusEngine.ts`, `LaserCuttingTribalCorpusEngine.ts`, `WaterjetCuttingTribalCorpusEngine.ts`, `WeldingTribalCorpusEngine.ts`, `AdditiveManufacturingTribalCorpusEngine.ts`

**4. What NOT to do in this domain (currently absent)**
- **Never persist extracted cutting data without a reference oracle.** `scripts/extract-generic-catalog.py` mis-parses/crashes on the vendor corpus — silently poisoning `cutting_data` stores is a safety violation. Validate every `[tool, material_iso, vc/fz]` tuple against a known reference before persist. Source: `reference_catalog_extraction_pipeline_gap_2026_05_31.md` (MEMORY.md standing patterns).
- **Never re-OCR Docustrata.** It is already indexed at `H:/PRISM/Docustrata/.index/*.jsonl` + `manifest.json`. Use `prism_data:database_search` or the index directly.
- **Never re-scan resource folders from scratch** when `prism_resource_harvesting:harvest_scan` or `prism_resource_harvester:get_index` can return the cached manifest.
- **Never inline physics/cutting constants** — import from `mcp-server/src/physics/constants.ts`. Aggregators carry provenance (vendor+pdf+page), never hardcode values.
- **Never use pdf-parse for PDF corpus harvest.** Use `scripts/extract-jm-die-corpus-page-by-page.py` (lima pypdf canonical — 76× deeper, domain-tagged). Source: `feedback_use_lima_pypdf_page_extractor.md`.
- **Never fabricate engine names or dispatcher actions** — the PATHS.md 46-engine list includes name-match noise (e.g. `FiveAxisAggregatorEngine`, `HermesParallelVerdictAggregatorEngine` — these aggregate other things, not this domain's corpus). Verify by reading the engine file header before citing.
- **After PDF corpus exhaustion, cover JM-related machines next** — do not pivot to web/synthetic data; ground truth is in the JM Die archive first. Source: `feedback_jm_machine_extraction_after_pdf_exhaustion.md`.

**5. Corpus data source priority (currently absent)**
Ordered by trust/ground-truth value for this domain:
1. `H:/PRISM/JM DIE/` — real-shop ground truth (programs, tribal, wiki; 38,251 indexed files)
2. `H:/PRISM/resources/PDF` + `resources/RESOURCE PDFS` — vendor manuals, catalogs, MIT-OCW
3. `H:/PRISM/resources/MANUFACTURER_CATALOGS/uploaded/` — 242 vendor catalog PDFs (extraction pipeline has known gap — do not auto-persist cutting data)
4. `H:/PRISM/Docustrata/.index/` — business/order corpus (already indexed; do not re-OCR)
5. Online: `corpus_harvest_online` via `knowledgeDispatcher` (lowest trust; requires human validation gate)

**6. Key wiki / tribal pointers (currently partially cited)**
- `knowledge/wiki/corpus-aggregation/` — 4 entries (query before re-deriving)
- `knowledge/wiki/training/cad-corpus-index.md` — CAD corpus index
- `knowledge/wiki/training/cam-corpus-index.md` — CAM corpus index
- `knowledge/wiki/code-tribal/post-processor-cross-controller-corpus.md` — tribal (cited in current file, keep)
- `knowledge/wiki/reference/tribal-knowledge-access---jm-die-test-shop---3-700--machinist-tips.md` — 3,700+ tip access
- `state/shared/specs/DATA-EXTRACTION-UTILIZATION-MASTERPLAN.md` — master extraction plan
- Tribal tip store: `[[tribal-corpus-index]]` (3,920 tips / 73 categories) via `prism_memory:semantic_search`

**7. No-dedicated-slot operating pattern (currently understated)**
This galaxy has NO dedicated slot — it is fleet/golf-managed. Any slot doing corpus work should:
- Check `prism_resource_harvester:harvest_status` before starting a new harvest (avoid duplicate runs)
- Use `/loop` with `loop-state tick` for ingest loops to survive `/compact`
- Write findings to `knowledge/memories/patterns/corpus-aggregation_synthesis.md` after any significant aggregation pass

### IDEAL SECTION OUTLINE
1. **Domain identity** — scope (multi-source corpus → academy/NN/GNN substrate), no-dedicated-slot note, cross-galaxy PSN edges (5 edges)
2. **Universal-core pointer** — one line referencing H:/PRISM/CLAUDE.md for R1-R15, scrutiny gate, handoff, commit format, units-first, no-stub rules
3. **Pipeline contract** — SCAN → CLASSIFY → INGEST → AGGREGATE → VALIDATE → SERVE with dispatcher action per stage
4. **Dispatcher action table** — 7 dispatchers, verified action names, source file:line
5. **Core engines** — 13 verified engines with 1-line role each
6. **Data source priority** — ordered list (JM Die → resources PDF → vendor catalogs → Docustrata → online)
7. **What NOT to do** — 6 explicit prohibitions (cutting-data persist, re-OCR, re-scan, inline constants, pdf-parse, noise engines)
8. **PDF extraction canonical** — `scripts/extract-jm-die-corpus-page-by-page.py` (lima pypdf, 76× deeper)
9. **Key wiki / tribal pointers** — 7 entries with paths
10. **Loop patterns** — corpus ingest loop (zero-drop streaming) + coverage-audit loop patterns
11. **AI-systems fleet state** — single pointer line to `knowledge/memories/patterns/ai-systems-fleet-state.md`

### UNIVERSAL-CORE POINTER
The following rules must remain accessible but must NOT be duplicated into this galaxy file — reference as:
> "Universal doctrine: H:/PRISM/CLAUDE.md — §R1-R15, §SCRUTINY GATE (3-of-3), §PER-CHAT HANDOFF, §COMMIT FORMAT (`[SCOPE]/U-ID: title`), §SAFETY RAILS (units-first / no-stub / no-inline-constants), §TOKEN ECONOMY (RTK / Ollama offload ladder), §KARPATHY DISCIPLINE."

Specifically: R1-R15 rules, 3-of-3 scrutiny gate protocol, per-chat handoff write/read commands, commit format, units-first safety rail, no-stub-engine enforcement, RTK bash prefix, Ollama fallback ladder (Ollama → Sonnet subagent → Opus), duplicationGuardEngine check-before-creating, all-means-all quantifier rule, existence-not-correctness rule, deep-search-before-claiming-absence rule.
