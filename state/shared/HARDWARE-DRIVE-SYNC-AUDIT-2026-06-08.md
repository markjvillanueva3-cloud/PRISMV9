# PRISM Hardware + Drive-Swap Sync Audit

> **Generated 2026-06-08 (slot:alpha, session ef62ba0b)** by a 6-surface parallel workflow audit (19 agents, adversarially verified). Context: operator swapped drive letters — the new 4TB NVMe is now `H:` (live tree), the old drive is now `P:` (kept as cold backup, untouched per operator decision). Hardware upgraded to Ryzen 9 9950X3D2 (16C/32T) · 127 GiB RAM · RTX PRO 6000 Blackwell 96 GB.
>
> ## Applied this session (in-scope correctness)
> - **[FIXED] 3.1 — dangling CPU-python path.** Recreated the directory junction `H:\Tools\python → H:\Tools\python-3.14` (the swap left `H:\Tools\python` empty). This restores all **34 hardcoded `H:/Tools/python/python.exe` references + the `PRISM_PYTHON_PATH` env var + the `portability-guards.test.ts` assertion** in one move (more comprehensive than repointing the env var alone). Proven live: `python\python.exe` → Python 3.14.5, exit 0. **No settings.json edit needed.**
> - **[FIXED] §4 — host-preset CPU descriptor.** `fleet-reaper-host-presets.json` DESKTOP-N7MI1VB description updated `Ryzen 7 7800X3D` → `Ryzen 9 9950X3D2 (16C/32T)` + swap event noted. (Cosmetic; nothing routes on it.)
>
> ## Pending YOUR approval (report-only retunes — §3 + §4 below)
> All other items are unchanged on disk. Nothing was retuned. See §6 for the dependency-ordered apply list.
>
> ## ⚠ Scheduled tasks: DELIBERATELY DISABLED — do NOT re-enable (operator decision 2026-06-08)
> All **47 `PRISM *` scheduled tasks are Disabled** (last ran successfully 2026-06-06 ~22:50, then batch-disabled — i.e. killed via `KILL-PRISM-TASKS.bat` before the drive-letter swap). The Stop-hook alarm "scheduled-task safety net CRITICAL — 0/47 healthy" is therefore **EXPECTED, not a fault.** Operator is **still mid-migration** and chose to **keep them disabled**. **Do NOT run `RESTORE-PRISM-TASKS.bat` / `Enable-ScheduledTask` until the operator says migration is done.** When restoring: `PRISM Fleet Task Health` is NOT-REGISTERED and `PRISM MCP Server` needs re-register (lastResult 267014 = disabled) — both need an ELEVATED shell + their `install-*-task.ps1`.

## 1. Verdict

The live system is **correctly synced to the drive swap** and **correctly recognizes the new GPU**. The drive-swap is clean: a 6-surface read-only sweep found **zero live P: references** in any settings.json, helper, hook, script, scheduled-task installer, `.mcp.json`, docker-compose, or state JSON — every live path resolves to H:, and the old `P:\prism`/`P:\Tools` backup is correctly left untouched and unreferenced. The torch/CUDA/bitsandbytes GPU stack is provisioned for sm_120 and reproduces GREEN live (torch 2.11.0+cu128, `cuda.is_available()=True`, NF4 ok), and every GPU-model/VRAM-pinned routing surface (host-class, ModelRouting, catalog-gpu-profile, soft-config-tweaks, OllamaCapabilityProbe) resolves this card to the `blackwell`/`home_blackwell` tier via hostname/`nvidia-smi`, never a hardcoded 16GB cap. **There is no P0.** The real gaps are second-order: (a) one path-fix — `PRISM_PYTHON_PATH` points at a now-empty dir that survived the swap only as a symlink target (the GPU python fallback masks it, so it is bounded); (b) a cluster of CPU/thread/heap *defaults* that were never revisited after the **CPU upgrade (7800X3D 8c/16t → 9950X3D2 16c/32t)** — these leave the upgraded machine underused but break nothing and are all report-only retunes needing operator approval; and (c) one live Ollama env value (`OLLAMA_MAX_LOADED_MODELS=4` vs the script's blackwell `6`) that is a revision stale. Everything else is CPU-model-string doc-drift, which is functionally inert because nothing routes on the CPU string.

## 2. P0 — correctness (breaks/misroutes live system NOW)

**None.** Zero P0 findings. The drive swap is clean (0 live P: references), the GPU stack is live-GREEN for sm_120, and no CPU-model string is a routing key (`host-class.mjs` keys off the preset `label`, `ModelRoutingEngine` keys off the GPU profile name). The live system is not misrouted by either the swap or the hardware upgrade.

## 3. P1 — on old-hardware defaults (upgraded machine underused or silently wrong)

These are the genuine "upgraded machine left on old defaults" items. All are **report-only retunes requiring operator approval** except the python path-fix (3.1), which is an in-scope correctness path-repair. Note: several reviewer arms downgraded the perf items below to P2 (override knobs exist / not in a live hot path) — they are grouped here as P1-class "underused-or-wrong" per the section's own definition, with the downgrade rationale carried in the notes.

| # | Finding | File:line | Current | Recommended | Notes |
|---|---------|-----------|---------|-------------|-------|
| 3.1 | CPU-python path dangles after swap (symlink not recreated) | `.claude/settings.json:7` (via `C:/Users/wompu/.claude/settings.json:7`, mirrored to H:) | `PRISM_PYTHON_PATH = "H:/Tools/python/python.exe"` (dir is empty) | `PRISM_PYTHON_PATH = "H:/Tools/python-3.14/python.exe"` (verified present) OR recreate `H:/Tools/python → python-3.14` symlink | Bounded: GPU path (`PRISM_PYTHON_GPU_PATH=H:/Tools/python-gpu/Scripts/python.exe`) resolves FIRST in both resolvers, so all GPU work is fine. But `constants.ts:82` `PATHS.PYTHON = env.PRISM_PYTHON_PATH \|\| "python"` has no GPU fallback and no existence-check, and ~9 non-GPU CPU spawn sites (guard/devx3/context/shopPractice/session dispatchers, `CadBridge.ts:152`, `CadQueryCodeGeneratorEngine.ts:231`) now hit ENOENT — they worked pre-swap via the symlink. **This is the one swap-induced regression.** Edit through C: settings.json. |
| 3.2 | vitest worker pool pinned to OLD 8-core CPU | `mcp-server/vitest.config.ts:12,35,38,49` | `maxThreads = …\|\| 8`; `minThreads …\|\| 4`; `maxConcurrency: 8`; header "Ryzen 7 7800X3D — 8 physical cores" | `maxThreads` default **8→16** (one per physical core of the 16-core 9950X3D2); `maxConcurrency` **8→16**; update header to "9950X3D2 — 16 physical cores / 32 threads" | ~3400-case suite runs at ~50% parallelism by default; `PRISM_VITEST_MAX_THREADS` override already exists. Reviewer-downgraded to P2 (dev-only test wall-time). **Caveat:** 9950X3D2 is dual-CCD with stacked V-Cache on only ONE 8-core CCD — the "X3D L3 makes workers cheap" rationale only covers cores 1–8; raising to 16 is still right (8 idle cores is real waste) but drop/qualify the blanket cache justification for cores 9–16. Two tests do NOT assert this cap (the asserting tests are the batch-extractor ones in 3.3). |
| 3.3 | Batch extractor pools hard-capped at 8 on a 32-thread box | `MINBatchExtractorEngine.ts:122-126` + `McxBatchExtractorEngine.ts:152-156` | `Math.max(1, Math.min(cpus - 1, 8))` → yields 8 (was 8 on old box too, clamp pins it) | Raise ceiling to `Math.min(cpus - 1, 16)`; prefer `os.availableParallelism()` over `os.cpus().length`; apply to BOTH (clone-don't-fork) | Reviewer-downgraded to P2: occasional batch corpus-ingestion utility (not a live hot path), operators can already override to `.max(64)` via `cadAutomationActionSchemas.ts:751`. **Fix is incomplete as written:** two tests hard-assert `defaultConcurrency() ∈ [1,8]` (`MINBatchExtractorEngine.test.ts:101-105`, `McxBatchExtractorEngine.test.ts:115`) — must update both assertions or `stop_on_failing_tests` blocks. |
| 3.4 | GPU embedding default concurrency = 1 despite measured 15.3× on this exact card | `scripts/lib/tribal-graph-embedding.mjs:29`; `scripts/build-wiki-embeddings.mjs:201` | `DEFAULT_CONCURRENCY = 1`; wiki embed defaults `PRISM_EMBED_CONCURRENCY \|\| "1"` | Keep lib `DEFAULT_CONCURRENCY=1` for test determinism; set `PRISM_EMBED_CONCURRENCY=16` as a blackwell host env in settings.json | Vectors byte-identical at any concurrency (verified, min cosine 1.0); ~51 vs ~3.3 embeds/s = 26k-file backlog 2.2h→8.5min. No host env sets it today (verified absent in both settings.json). Reviewer-downgraded to P2 (infrequent operator-initiated batch). |
| 3.5 | VL LoRA trainer pinned to 7B / batch=1 / grad_accum=8 (16GB-era) | `scripts/lib/blueprint-vl-train-runner.mjs:58-70,67-68` + `mcp-server/scripts/blueprint_vl_train_lora.py:73,527-528` | `baseModel: Qwen2.5-VL-7B`, `batchSize:1`, `gradAccum:8`, `dpi:150/200` | Raise per-device batch to ~8–16, lower grad_accum to ~1–2 (keep effective batch); optionally promote to a 32B-class VL base now that it fits 96GB; make host-aware via `detectGpuTier()` so weak-host path is preserved | Reviewer-downgraded to P2: trainer is **not yet wired** (only its own tests reference it) and the staged corpus is **8 pairs from 1 print** (`<<50-sample floor`, "proves the wiring, not a trainable corpus"). Also note: no `max_pixels` cap + no gradient checkpointing means VL activation memory (not the 14GB weights) dominates, so "batch 8–16" is a guess — **validate against a live VRAM probe before raising.** Real lever, dormant path. |
| 3.6 | Live `OLLAMA_MAX_LOADED_MODELS=4` is a revision behind the blackwell tier's `6` | `scripts/system-health/05-soft-config-tweaks.ps1:35` vs live `HKCU\Environment` | live env = 4; current script blackwell tier sets 6 | Re-run `05-soft-config-tweaks.ps1` (sets 6) then **restart Ollama** to pick it up | Confirmed P1 by reviewer (not downgraded): the other 3 blackwell fields are current (NUM_PARALLEL=4, KEEP_ALIVE=-1, KV_CACHE_TYPE=f16) — only MAX_LOADED is stale, proving the env was last written by an older script revision. The multi-VLM ensemble OCR pattern (shipped this fleet) wants ≥3 VLM families + coder + embed co-resident (5–6 models, ~50GB, fits 96GB easily) — capping at 4 forces unnecessary eviction. **Active perf ceiling.** Ties to 3.7. |
| 3.7 | `05-soft-config-tweaks.ps1` has no scheduled-task registrar (manual one-shot) | `scripts/system-health/05-soft-config-tweaks.ps1` (0 registrars repo-wide) | run manually; no "PRISM Soft Config" task; whole `00–33` system-health sequence is unscheduled | Register an AtStartup+daily task OR document as a required post-swap manual step in a host-swap runbook | Reviewer-downgraded to P2 (live env is functional today; survives reboot via User scope, not a profile reset). **Caveat that weakens the obvious fix:** every "PRISM \*" scheduled task is currently **Disabled** on this host (deliberate manual-ops state) — a newly registered task would be born Disabled and not self-heal unless separately enabled. Root cause of 3.6's staleness. |
| 3.8 | MCP supervisor heap pinned at 4GB on a 127GiB box | `scripts/mcp-server-supervisor.mjs:206` | `heapFlag = "--max-old-space-size=4096"` | Raise to 8192 (RAM unchanged by swap, so optional); reconcile with the 16GB autostart constant | Reviewer-downgraded to P2 with an **important correction:** the recycle horizon is gated on **RSS** (`mcp-server-watchdog.mjs` `PRISM_MCP_WATCHDOG_RSS_THRESHOLD_MB=3072`), co-designed to trip *before* the 4GB heap. Raising heap ALONE changes nothing — you must also raise the RSS threshold. Real fix is the documented `error_ledger_recall_similar` ref-retention leak. Not swap-induced (RAM unchanged). |

## 4. P2 — doc-drift

- **Host preset still names the OLD CPU.** `state/shared/dashboards/fleet-reaper-host-presets.json` (line 19 / line 23 depending on copy) DESKTOP-N7MI1VB `description` reads "…Ryzen 7 7800X3D · 127GB RAM…". GPU+RAM correct; CPU stale. `host-class.mjs` keys off the `label` ("blackwell"), never the description — cosmetic, but it is the operator's primary source-of-truth string for this host. Recommend "Ryzen 9 9950X3D2 (16C/32T)". `_updatedAt: 2026-06-03` confirms the CPU upgrade post-dated the GPU-swap edit.
- **`gpu_health.py` + `GpuStackHealthEngine` operator-fix messages say `cu129` but the installed/correct toolkit is `cu128`.** `scripts/py/gpu_health.py:143,187,239` (+ comments 16-17,46,53,324) and `GpuStackHealthEngine.ts:17,60`. Gate LOGIC is hardware-agnostic and correct (compares device sm_120 tag vs `torch.cuda.get_arch_list()`), so it does NOT break — but if it ever fails, it tells the operator to install the wrong CUDA wheel. Reconcile all 5 sites to `cu128` (sm_120-supported, live-GREEN).
- **Upgrade-plan + provisioning docs reference `cu129`.** `state/shared/specs/BLACKWELL-AI-UPGRADE-PLAN-2026-06-03.md:43,335` ("pin torch 2.11+cu129"); contrast `GPU-STACK-PROVISIONED.md:14` + `GPU-STACK-HEALTH.json` which correctly record cu128. Reconcile to "cu128 (sm_120-supported, verified)".
- **Vision-OCR `num_ctx` default 8192 + its rationale comment are written for the old 16GB card.** `scripts/lib/ollama-vision-extract-lib.mjs:415-423` ("…on a 16GB card with the chat fleet running"). On 96GB, dense multi-zone drawings can be context-clipped unnecessarily. Optional retune to 16384–32768 + update the comment (overridable via `opts.modelOptions`; host-gate to keep weak-host at 8192). Borders on P1-perf but flagged P2 since `repairTruncatedJson` is the truncation safety net.
- **`mcp-priority-guardian.ps1:41-42` affinity-mask example uses the old 16-thread layout.** Comment example `0xFFF0` covered threads 4–15 on the 7800X3D; on the 32-thread 9950X3D2 it strands threads 16–31. Low risk (affinity is opt-in, defaults to mask `0` = no pin), but a stale functional footgun if copied verbatim. Re-cut to a 32-thread split (e.g. `0xFFFFFFF0`).
- **Blackwell reference memory says "verified live … Ryzen 7 7800X3D".** `knowledge/memories/reference/reference_blackwell_ollama_utilization_optimize_2026_06_03.md:13`. High-visibility because labeled "(verified live)". Fix the **C: source** memory file so the H: mirror doesn't re-drift.
- **`GPU-STACK-PROVISIONED.md:5` records "driver CUDA 13.2" vs live driver 596.59 / compute cap 12.0.** Driver CUDA (13.x) and torch runtime CUDA (cu128) are independent and both valid (forward-compatible). Status-doc label only; optional refresh to note both.
- **`fleet-reaper-host-presets.mjs:39` BUILTIN_PRESETS.home + 3 fleet-reaper-home wiki/skill copies name "7800X3D + 64GB".** `knowledge/wiki/os/commands/fleet-reaper-home.md:8,13` + `knowledge/wiki/architecture/skills/project/fleet-reaper-home.md:16`. These describe the generic 16GB/64GB **"home" reference tier**, NOT this blackwell box — **leave as-is IF the home reference PC is a distinct machine; only stale if "home" historically meant this box pre-swap.** Operator judgment call; N/A for the blackwell host itself.
- **`defender-exclusion-bootstrap.ps1:14` parenthetical "(~1-2% of total CPU on a 7800X3D)".** Pure illustrative prose; nothing keys off it. Lowest priority — optionally genericize.

## 5. Rejected / spurious findings

No findings were rejected outright (0 in the rejected set). However, several flagged items were **verified-and-confirmed as correct (recorded so they are not re-flagged)** — the operator should treat these as "checked, no action":

- **Drive-swap routing is clean.** 0 live P: references anywhere; the only repo-wide `P:[\\/]` regex matches are the tail of "MCP:" in `scripts/core/gsd_sync.py:243,261`. Stale `C:\PRISM` / `Box\PRISM` strings in audit-snapshot JSONs are non-executing data artifacts, not routing config.
- **GPU model/VRAM routing is fully Blackwell-tuned.** `ModelRoutingEngine` (`home_blackwell` profile), `05-soft-config-tweaks.ps1` (≥48GB→blackwell tier, fixed 2026-06-04 to stop clobbering back to 16GB), `catalog-gpu-profile.mjs` (DESKTOP-N7MI1VB=96), `OllamaCapabilityProbeEngine` (home_blackwell=80000 MiB + name-match), `host-class.mjs` (hostname→blackwell) all resolve correctly off live `nvidia-smi`/hostname.
- **CPU model string is NOT a routing key** — confirmed by inspecting `host-class.mjs:34-67` (keys off preset `label`) and `ModelRoutingEngine.ts:44,146-451` (keys off GPU profile). All the 7800X3D doc-drift is therefore safe/cosmetic, never a live misroute.
- **`regen-viz.mjs:208-211` heap (24GB) is already correctly sized for the 127GiB box** (comment cites the verified 136GB host RAM) — the one heap site already revisited for the new RAM.
- **No 64GB RAM pin exists anywhere** — `git grep '64GB|65536|67108864'` returned zero RAM-pin hits; build/test/CI=16GB, retrain/graph scripts=8GB, all safe on 127GiB.
- **The torch/CUDA/bnb core is correctly provisioned** — live probe reproduces the BLACKWELL-AI-MS0 "torch 2.11+cu128 qlora-ready, PROVISIONED GREEN" claim (sm_120 in arch_list, real matmul + NF4 op ok). The GraphSAGE GNN trainer is pure JS/CPU (no `device='cuda'`), correctly out of GPU-pin scope.
- **`wedm_train_lora.py` is a simulation placeholder** (no torch, hardcoded loss, fake "completed" adapter) — real R12-shaped stub, but **untracked, zero live callers** (only an `fs.existsSync` test reference), already documented and superseded by the real `blueprint_vl_train_lora.py`. Archive/rebuild when convenient; NOT a hardware-pin defect and NOT live.
- **`DetachedLoRARunnerEngine` concurrency=1 (cap 16)** and **`UV_THREADPOOL_SIZE` unset (libuv default 4)** — genuine headroom opportunities but correctly P2: single-GPU LoRA is often deliberately serialized to avoid VRAM thrash, and UV_THREADPOOL gains only the I/O-bound batch scripts. Measure before changing.
- **Two MCP launchers disagree on heap (4GB vs 16GB)** — confirmed textually but the 16GB endpoint (`prism-http-autostart.mjs:51`) is a **self-documented unwired legacy orphan**; the actually-wired launcher is the supervisor (4GB) and the wired session daemon (`mcp-daemon-autostart.mjs`) sets no heap flag at all. Cosmetic consistency item, not a P1 operability bug.
- **Several task/cron scripts bake a dead node path** (`portable-node/node.exe` dir-form, `portable-node.exe`) — **pre-existing, NOT swap-induced** (they point at H:, not P:); each has a `Get-Command node` PATH fallback that resolves, so tasks register and run. Robustness cleanup only.

## 6. Recommended apply order

Dependency-ordered. `[in-scope path-fix]` = correctness repair safe to apply now; `[operator-approval-needed]` = retune/perf/doc, report-only.

1. **`[in-scope path-fix]` Repoint `PRISM_PYTHON_PATH`** to `H:/Tools/python-3.14/python.exe` (or recreate the `H:/Tools/python` symlink) — edit via `C:/Users/wompu/.claude/settings.json` (c-to-h-mirror replicates to H:). *Fixes the one swap-induced CPU-python ENOENT regression; do first, no deps.* (3.1)
2. **`[operator-approval-needed]` Re-run `05-soft-config-tweaks.ps1` then restart Ollama** — brings live `OLLAMA_MAX_LOADED_MODELS` 4→6 to match the current blackwell tier. *Independent; immediate active-perf win for the multi-VLM ensemble.* (3.6)
3. **`[operator-approval-needed]` Schedule `05-soft-config-tweaks.ps1`** (AtStartup+daily) OR add a host-swap runbook note — *depends on #2 having validated the script's current output; note the "all PRISM tasks Disabled" caveat so a new task is enabled, not born dormant.* (3.7)
4. **`[operator-approval-needed]` Set `PRISM_EMBED_CONCURRENCY=16`** as a blackwell host env in settings.json (keep lib `DEFAULT_CONCURRENCY=1`) — *realizes the documented 15.3× embed speedup; env-only, no code change.* (3.4)
5. **`[operator-approval-needed]` Optionally set `UV_THREADPOOL_SIZE=16/32`** in host env for I/O-bound batch scripts — *measure first; group with #4 since both are settings.json env additions.* (§5 / P2)
6. **`[operator-approval-needed]` Bump vitest defaults 8→16** (`vitest.config.ts`) + update header to 9950X3D2 + drop/qualify the X3D-cache rationale for cores 9–16. *Code+comment; no test fallout for this file.* (3.2)
7. **`[operator-approval-needed]` Raise batch-extractor ceiling 8→16** in BOTH extractors + switch to `os.availableParallelism()` **and update the two `[1,8]` test assertions** (`MINBatchExtractorEngine.test.ts:101-105`, `McxBatchExtractorEngine.test.ts:115`) in the same commit — *the test edits are mandatory or `stop_on_failing_tests` blocks Stop.* (3.3)
8. **`[operator-approval-needed]` VL-LoRA retune** — run a live VRAM probe at dpi=150/200 first (activation memory, not weights, dominates), THEN raise batch / lower grad_accum and make it host-aware via `detectGpuTier()`. *Gated on a probe; the trainer is dormant/unwired so lowest urgency.* (3.5)
9. **`[operator-approval-needed]` Supervisor heap** — only meaningful if ALSO raising `PRISM_MCP_WATCHDOG_RSS_THRESHOLD_MB` (heap alone is inert vs the RSS recycle gate); true fix is the `error_ledger_recall_similar` leak. *Defer behind the leak task.* (3.8)
10. **`[operator-approval-needed]` Doc-drift batch (§4)** — single hygiene commit: host-preset CPU string, all `cu129`→`cu128` sites (5 in gpu_health/engine + 2 in upgrade-plan), num_ctx comment, affinity-mask example, GPU-STACK-PROVISIONED driver label; fix the **C: source** of the blackwell reference memory so the H: mirror doesn't re-drift. *No deps; can run any time.* (§4)
11. **`[operator-approval-needed, judgment]` "home" reference-tier descriptors** — decide whether the generic 16GB/64GB "home" tier (`fleet-reaper-host-presets.mjs:39` + 3 wiki copies) ever meant this box pre-swap; leave as-is if it is a distinct machine. (§4)