# 05 — Pipeline-Gen Audit (slot india, 2026-05-26)

Tight retry scope. R12 fail-loud findings.

## Q1 — 18-stage definition: WHERE IS IT?

**Status:** Defined in WIKI only, NOT in code or canonical spec.

- `knowledge/wiki/architecture/print-to-program-pipeline-canonical.md` — full 18-stage table with engine attribution per stage (intake -> format parse -> feature recog -> material -> GD&T -> DFM -> process plan -> setup -> tool select -> SF compute -> strategy -> toolpath gen -> simulate -> post -> safety -> setup sheet -> quality plan -> cost). Authored 2026-05-21 by hotel (`U-WIKI-ARCH-P2P-CANONICAL`).
- `knowledge/wiki/architecture/specs/spec-domain-pipeline-ms0-design.md` — GHOST entry pointing at `state/shared/specs/DOMAIN-PIPELINE-MS0-DESIGN.md`. **R12 FAIL-LOUD:** that spec_file path was NOT found in this audit pass (the wiki ghost is auto-generated by `generate-misc-l8-wiki.mjs`; existence of the pointer != existence of the target).
- No `STAGES = [...]` constant exists in `mcp-server/src/` — the 18 stages live as a markdown table only.

**Gap:** Pipeline definition is doctrine, not code. A canonical machine-readable `pipelineStages.ts` would let RGS/router/orchestrator key off the same vocab.

## Q2 — Per-domain rules (mill/lathe/wedm/cam/cad)

Glob `scripts/lib/rgs-pipeline-rules*.mjs` returns:
- `scripts/lib/rgs-pipeline-rules.mjs` (single file, all domains)
- `scripts/lib/rgs-pipeline-rules.test.mjs`

**R12 FAIL-LOUD:** Per-domain split files (`rgs-pipeline-rules-mill.mjs`, `-lathe.mjs`, `-wedm.mjs`, `-cam.mjs`, `-cad.mjs`) referenced in CLAUDE.md §RGS-TOOL-AUTOINVOKE **do not exist**. All rules live in one monolithic `rgs-pipeline-rules.mjs` (deep-frozen `RULES[]` regex table). The CLAUDE.md "5-file" framing overstates structural separation — verify against disk, not the index.

## Q3 — RGS sidecar (`roadmap-tool-plans.json`)

```
size_bytes=945,410 (~923 KB)
age_hours=31.6
top_keys=schemaVersion,generatedAt,generator,degraded,plans
plans (object) subkeys=363
```

**R12 FAIL-LOUD:** **363 plans, NOT 4,404.** CLAUDE.md §RGS-TOOL-AUTOINVOKE asserts "every open roadmap unit (4,404 units)" — sidecar is at **8.2% coverage**. Age 31.6h is just over the nightly replan cadence (24h). Two possible causes: replan task degraded (`degraded` key present in top_keys), or replan reads a stale `roadmap-index` that lost units in a regen. India should investigate before relying on plans for pickup.

## Q4 — `recordOutcome` callsites (top 5 in engines)

20+ matches. Top 5 by relevance to pipeline replan:

1. `CAMFeedbackLoopEngine.ts:350` — `static recordOutcome(input: RecordOutcomeInput)` — confirms AI picks, drives ranking.
2. `CAMConfidenceCalibrationEngine.ts:189` — outcome -> calibration delta.
3. `CAMAIValidationEngine.ts:402, 569, 599, 689` — multi-site routing into both CAMFeedbackLoop + Calibration.
4. `CrossProcessOutcomeStore.ts:290` — `recordOutcome(id, outcome)` — durable append store.
5. `BackendRouterEngine.ts:340` + `AdaptiveShopRateEngine.ts:128` + `EmployeePerMachineSFAdaptiveEngine.ts:168` + `ConsensusModelPerformanceEngine.ts:187` — non-CAM consumers (router, shop-rate, SF adaptive, consensus).

**Gap:** None of these callsites publish back to the RGS sidecar. CAM outcomes -> CAMFeedbackLoop is closed; **pipeline-stage outcome -> RGS replan is open.** No engine writes a record keyed on `roadmap-unit + stage` that the next nightly replan would consume.

## Q5 — Article-incorporation: 3 units for india's queue

Pipeline outcome -> consolidator -> replan loop (closes measurable gap):

1. **U-IND-PIPELINE-STAGES-CONST** — Extract the 18-stage table from the canonical wiki into `mcp-server/src/pipeline/STAGES.ts` (typed enum + per-stage engine attribution). Closes Q1 R12 gap; unblocks programmatic stage-aware routing.
2. **U-IND-RGS-OUTCOME-FEEDBACK** — New `RGSOutcomeBusEngine` consuming `CrossProcessOutcomeStore` + `CAMFeedbackLoopEngine` events keyed on `roadmap-unit-id`. Emits sidecar deltas the nightly `replan` consumes. Closes Q4 gap (publish-back missing) + Q3 staleness symptom.
3. **U-IND-PIPELINE-SIDECAR-COVERAGE-AUDIT** — Script `audit-pipeline-sidecar-coverage.mjs` that compares `roadmap-tool-plans.json plans` keys vs open roadmap-index unit-IDs; fails loud at <90% coverage; surfaces to `/system-viz`. Closes Q3 R12 (363/4404).

**Layer-4 dreaming hook:** RGSOutcomeBus events would be the natural training signal for the GraphSAGE tier-5 NN (CLAUDE.md §NN-GRAPH) — outcome-labeled edges are the missing supervised signal that AUROC=0.096 needs.
