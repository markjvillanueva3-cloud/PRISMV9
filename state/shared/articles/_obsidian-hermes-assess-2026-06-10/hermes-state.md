I have a complete, verified picture. The morning brief trace confirms a clean LOCAL gpt-oss:120b run with `## Response` (no error/spiral), the GEPA candidate exists with real failure-pattern output, the gateway is live (PID 57264, python, started 7:25am), and all 3 cron jobs report `last_status: ok`. Let me write the memo.

## Hermes capability state + accelerations

Hermes/ZULU is the local Nous-Research agent installed at `C:/Users/wompu/AppData/Local/hermes/`, running as a gateway daemon that orchestrates the PRISM fleet via the `prism` MCP (`http://127.0.0.1:3100/mcp`). Verified against live files 2026-06-10.

### What exists + works today (verified)

- **Gateway daemon — LIVE.** `gateway_state.json` = `running`, PID 57264 (python, started 2026-06-10 07:25:55, confirmed alive via Get-Process). Lock/pid files current. `config.yaml:495` strict=false, trust_recent_files=true.
- **Local-model E2E loop — CLOSED (the headline).** `config.yaml:1-7` default = `gpt-oss:20b` @ `http://127.0.0.1:11434/v1`, `ollama_num_ctx: 65536`, `context_length: 65536`. The num_ctx fix is real and applied: `OLLAMA_CONTEXT_LENGTH=65536` is set as a User env var on the serve task (verified), and `context_length_cache.yaml` shows gpt-oss:120b/20b resolving to 131072. Latest morning-brief trace (`cron/output/044ec1701ace/2026-06-10_09-22-17.md`) ends in `## Response` (clean final), NOT `## Error` / continuation spiral. All 3 cron jobs `last_status: ok`.
- **Cron scheduler + job chaining — WORKING.** `cron/jobs.json` has 3 enabled jobs: inbox sweep (`23 20 * * *`, gpt-oss:20b, 6 completions), morning brief (`7 6 * * *`, gpt-oss:120b, 7 completions, `context_from: [inbox-sweep]`), weekly self-review (`11 19 * * 0`, gpt-oss:120b, `context_from: [morning-brief, inbox-sweep]`, 0 runs yet — not due). `context_from` chaining is configured and live. Real outputs land at `knowledge/hermes-outputs/notes/2026-06-10-{morning-brief,inbox-sweep}.md`.
- **Skills (procedural memory) — 2 PRISM skills live.** `skills/prism/prism-vault-loop/SKILL.md` (v1.0.0, the active loop contract, READ→ACT→WRITE-BACK, use_count 14) and `skills/prism/prism-daily-brief-loop/SKILL.md` (use_count 0 — see dormant). Plus ~28 bundled/hub skill categories (`skills/.hub/`, `.bundled_manifest` = airtable/arxiv/etc.). Progressive disclosure + skill-hub infra present.
- **3-tier memory — Tier-1 + Tier-2 confirmed.** Tier-1: `memories/MEMORY.md` (2653B, under the 2200-char *content* cap per `config.yaml:361` memory_char_limit) + `memories/USER.md` (958B). Tier-2: `state.db` (3.3MB SQLite FTS + WAL). `config.yaml:358-364` memory_enabled + user_profile_enabled = true. Tier-3 external providers: `memory.provider: ''` (none active — by design, PRISM vault is the external brain via MCP).
- **SOUL.md identity slot — present + PRISM-specific.** `SOUL.md` (5243B) = ZULU orchestrator persona, fleet map, hard refuses (no gate exemption, no stub directives), write-lane = `hermes-outputs/` only.
- **GEPA-lite — BUILT + RAN once.** `H:/prism/scripts/hermes-skill-gepa.mjs` (10.5KB, trace-driven, dry-run-default, never touches live skill, operator-PR-gated). One real candidate staged: `state/shared/specs/SKILL-CANDIDATE-GEPA-20260610-prism-vault-loop.md` (6.6KB, NEEDS-REVIEW, traces_used 4, model qwen2.5-coder:32b) with genuine failure-pattern output (caught the qwen 32K-ctx-too-small failure). Verdict logged to `state/shared/skill-loop-verdicts.jsonl`.
- **MCP wiring — confirmed.** `config.yaml:568-574` `mcp_servers.prism` → `:3100/mcp`. `toolsets: [hermes-cli]`. `delegation.orchestrator_enabled: true`, max 5 concurrent children, depth 1.

### Dormant / disabled / half-built (with activation cost)

- **Curator — SEEDED but NEVER RUN (`run_count: 0`).** `config.yaml:390-399` enabled=true, interval 168h, stale@30d, archive@90d. `.curator_state` = "deferred first run — curator seeded, will run after one interval." So the stale/archive lifecycle daemon is configured but has produced 0 reports. Activation cost: S — `hermes curator run --dry-run` (one command) to force first pass; near-zero risk (worst case = recoverable archive).
- **GEPA round 2 — STALLED at one candidate.** The staged candidate from 2026-06-09 23:38 is still `NEEDS-REVIEW` — never reviewed/applied, so the loop hasn't compounded. No re-run since. The script's candidate is idempotent-per-day (won't overwrite), so it's effectively paused pending operator/reviewer action. Activate-dormant.
- **`prism-daily-brief-loop` skill — DUPLICATE / unused.** `use_count: 0`, created by agent 2026-06-10, overlaps `prism-vault-loop` (which the SKILL.md itself says it "is used by"). It's an agent-authored near-dup that no cron job references (`jobs.json` all point at `prism-vault-loop`). This is exactly what Curator would consolidate. Activate-dormant (let Curator merge, or pin one + archive the other).
- **`fallback_model` = claude-opus-4-8 — needs Anthropic credits.** `config.yaml:575-577`. Per MEMORY.md it "needs Anthropic extra-usage credits," so unattended fallback to Claude is not free/guaranteed. Local is now primary so this is lower-priority, but the fallback path is unproven under load.
- **Checkpoints — DISABLED.** `config.yaml:116` `checkpoints.enabled: false`. No state snapshots for cron-session recovery.
- **Kanban auto-decompose — enabled but unobserved.** `config.yaml:462-473` dispatch_in_gateway=true, auto_decompose=true. `kanban.db` exists (114KB) but no evidence it's driving fleet work; unverified whether ZULU is decomposing/dispatching kanban tasks vs. just running the 3 vault crons.
- **Weekly self-review — 0 runs.** Not due until 2026-06-14; the deepest GEPA/self-improvement signal source hasn't fired yet, so the self-evolution flywheel is unproven end-to-end.

### Top accelerations (each: WHAT | concrete FILES | activate-dormant-or-net-new | rough effort S/M/L | est ROI)

1. **Activate the Curator first pass + auto-consolidate the duplicate skill** | `config.yaml:390-399`, `skills/.curator_state`, `skills/prism/prism-daily-brief-loop/SKILL.md` (the dup), `skills/.usage.json` | **activate-dormant** | **S** | **High** — one `hermes curator run --dry-run` proves the never-run lifecycle daemon, and the dup `prism-daily-brief-loop` (use_count 0) is the obvious first consolidation/archive target. Closes the article's verified gap #6 ("PRISM: NO equivalent skill-lifecycle daemon"). Zero downside (worst case = recoverable archive).

2. **Close GEPA round 2: review→apply the staged candidate, then re-run + schedule a cron** | `state/shared/specs/SKILL-CANDIDATE-GEPA-20260610-prism-vault-loop.md`, `scripts/hermes-skill-gepa.mjs`, `state/shared/skill-loop-verdicts.jsonl`, `cron/jobs.json` | **activate-dormant (review) + net-new (cron job)** | **M** | **High** — the candidate already diagnosed a real failure (qwen 32K ctx). Dispatch a reviewer-subagent to grade it, apply if PASS, then add a recurring cron (e.g. weekly, after self-review) so the trace→optimize loop compounds instead of sitting at one candidate. This is the self-evolution flywheel that's currently 1-and-done.

3. **Per-job model-routing by cost/depth** | `cron/jobs.json` (already per-job `model`), `config.yaml:1-7` (default), `context_length_cache.yaml` | **activate-dormant (infra exists, underused)** | **S** | **Medium-High** — jobs already carry per-job models (sweep=20b, brief/review=120b). Extend: route trivial/deterministic sweeps to `gpt-oss:20b` always, reserve `gpt-oss:120b` for synthesis (brief/weekly), and use qwen2.5-coder:32b only where its 32K ctx fits (GEPA already learned 32K is too small for the 65K-prompt jobs — encode that as a guard). Low effort since the field is already there; ROI = fewer ctx-overflow failures + faster cheap jobs.

4. **Ollama prewarm before cron ticks (eliminate cold-load latency)** | `config.yaml:1-7` (base_url), the `PRISM Ollama Serve` task, a new `scripts/hermes-cron-prewarm.mjs` | **net-new** | **S/M** | **Medium** — morning brief (gpt-oss:120b) cold-loads ~60GB before the 06:07 tick. A detached `/api/generate` warm-up keyed to the next cron `next_run_at` (readable from `cron/jobs.json`) removes first-token stall and shortens the chain. Mirrors PRISM's existing `ollama-prewarm-on-pipeline.mjs` pattern — clone-don't-fork.

5. **Add high-value PRISM skills to ZULU's catalog + enable checkpoints for cron recovery** | `skills/prism/` (new SKILL.md files), `config.yaml:116` (`checkpoints.enabled: false`→true) | **net-new (skills) + activate-dormant (checkpoints)** | **M** | **Medium** — ZULU has only 2 PRISM skills, both vault-loop variants. Net-new orchestration skills (e.g. `prism-fleet-wake` wrapping `scripts/fleet-wake-sequencer.mjs`, `prism-slot-brief-dispatch` wrapping `slot_brief_write`, `prism-octopus-consume` for the loop the morning brief keeps flagging as #1 priority) turn ZULU from a 3-cron reporter into an active conductor. Pair with flipping `checkpoints.enabled: true` so a reaper-killed cron session can resume rather than restart (the same non-resumable-burn failure class PRISM already fixed elsewhere).

**Honest caveats (R12):** The weekly self-review (0 runs, due 2026-06-14) and Curator (0 runs) mean the *self-improvement* half of Hermes is configured but not yet proven end-to-end — accelerations 1 and 2 are what convert "configured" to "proven." The kanban auto-decompose path (`config.yaml:462`, `kanban.db` present) is enabled but I found no evidence it's driving fleet dispatch — unverified, worth a separate probe before building on it.