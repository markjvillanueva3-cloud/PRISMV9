Confirmed. The live Hermes vault-loop covers morning-brief, inbox, weekly-synthesis, self-improvement — but NOT thinking-partner, connection-finder (PRISM-side only, advisory), project-health, or research-converter as Hermes-scheduled jobs. I now have a fully grounded picture. Writing the memo.

## Under-applied loop-engineering techniques (Obsidian+Hermes)

The Hermes↔Obsidian closed loop is genuinely live on this box — not a paper claim. The under-application is concentrated in five techniques where the *machinery exists* but the *higher-leverage half of the pattern is dormant*. Verified against live files; absences marked.

### What exists + works today (verified)
- **Hermes cron loop is LIVE.** `C:/Users/wompu/AppData/Local/hermes/cron/jobs.json` defines 3 jobs (morning vault brief / inbox sweep / weekly self-review), all run as ZULU calling the `prism-vault-loop` skill (`C:/Users/wompu/AppData/Local/hermes/skills/prism/prism-vault-loop/`). Cron output is *fresh* — `cron/output/` has runs at 2026-06-10 09:22, 09:05, 08:52. Result code 0.
- **`context_from` job-chaining IS wired** — but only in the real Hermes home `jobs.json` (morning brief chains from evening inbox sweep; weekly review chains from both). It does NOT exist anywhere in the PRISM repo (`Grep context_from H:\prism\scripts` = 0 hits). The AGENTIC doc's "wired in jobs.json" is true only off-repo.
- **Dream cycle + self-reflect scheduled tasks = Ready and running.** `PRISM Hermes Dream-Cycle Synth` last ran 2026-06-10 03:17, LastTaskResult 0. Scripts real: `scripts/hermes-dream-cycle-synth.mjs`, `hermes-self-reflect-populater.mjs`, `obsidian-learning-revival.mjs` (all + tests).
- **GEPA-lite is real and operator-gated** — `scripts/hermes-skill-gepa.mjs` parses live Hermes cron traces (`cron/output/<job>/*.md` Response/Error tails) → Ollama critique → STAGED `SKILL-CANDIDATE-GEPA-*.md` (NEEDS-REVIEW, never touches live skill). Correct GEPA shape.
- **CAG engine + cyril skills partially ported.** `PromptCachingEngine.buildCachedSystem()` is wired to `prism_dev:pc_build_cached_system` (devDispatcher.ts:3955, 28 tests). `connection-finder.md` and `wiki-morning.md` + `weekly-synthesis.md` skills exist PRISM-side.

### Dormant / disabled / half-built (with activation cost)
- **`PRISM Hermes-Obsidian Bridge` scheduled task = DISABLED** (verified `Get-ScheduledTask` State). The other two Hermes tasks are Ready; this one — the write-back bridge — is off. Activation: S (re-enable + verify, but check *why* it was disabled first — R7).
- **CAG cold-cache hook is NOT cache-wired (the doc's gap #2, confirmed).** `Grep buildCachedSystem H:\prism\.claude\hooks` = **0 files**. The ~8 per-turn injectors re-emit static doctrine every turn; `buildCachedSystem` is reachable only via an MCP dispatcher action, never the injection layer. `cag-cold-cache-anchor.mjs` has no `increment/coldHit/stats` telemetry — you cannot currently *measure* the cold-hit rate. This is the single biggest measured cost lever, and it's pure wiring (R8). Activation: M (route 8 injectors' static blocks through `buildCachedSystem()`, move static→SessionStart, add skip telemetry).
- **5 of cyril's 7 skills NOT ported / NOT Hermes-scheduled.** Verified missing as `.claude/commands/`: `inbox-processor`, `project-health`, `research-converter`, `thinking-partner`. The live `prism-vault-loop` covers morning/inbox/weekly/self-improve but NOT thinking-partner, project-health, or research-converter. **thinking-partner is the highest-leverage miss** — it's the only skill that *acts on* the vault (surfaces tensions/contradictions/open-questions across `knowledge/memories/`) rather than summarizing it. Activation: S–M per skill.
- **HMEMV04-06 bidirectional vault = `not_started`** (verified `HERMES-MEMORY-VAULT-MS0.json` all units `not_started`). Obsidian-side edits do NOT flow back to C: — the C:→H: mirror + `stop-obsidian-memory-feed.mjs` are one-way. The engines/CLAUDE.md flags this as "biggest dormant-X-article miss." A reverse mirror script does NOT exist (only `bootstrap-c-mirror.mjs` for backlog). Activation: M–L.
- **GEPA candidates never auto-applied + no `context_from` trace→skill feedback edge.** GEPA stages candidates but there's no recurring cron that runs it against the *just-completed* dream/reflect traces. The trace→GEPA→skill-revision loop is manual. Activation: S (add a `context_from`-chained GEPA job after weekly self-review in the Hermes `jobs.json`).

### Top accelerations

| # | WHAT | concrete FILES | activate-or-net-new | effort | est ROI |
|---|------|---------------|--------------------|--------|---------|
| 1 | **CAG cache-wire the per-turn injectors** — route the ~8 static-doctrine injectors through `buildCachedSystem()` + add cold-hit telemetry so the rate is *measurable*. Targets Claude Code's cited 92% prompt-cache hit-rate. | `mcp-server/src/engines/PromptCachingEngine.ts:119`, `.claude/hooks/cag-cold-cache-anchor.mjs`, `.claude/hooks/cag-router-inject.mjs`, the 8 injectors | **activate-dormant** (engine built+tested, injection layer un-wired) | **M** | **Highest** — every turn across 26 slots pays static-re-emit cost today; biggest measured token/cost lever, pure wiring |
| 2 | **Port `thinking-partner` as a Hermes-scheduled job** — the one cyril skill that *reasons against* the vault (tensions/underdeveloped-ideas/open-questions over `knowledge/memories/`), chained via `context_from` after weekly synthesis. | new `.claude/commands/thinking-partner.md` + new job in `C:/Users/wompu/AppData/Local/hermes/cron/jobs.json` + `prism-vault-loop` skill | **net-new** (skill absent, verified) | **S–M** | **High** — converts the vault from passive recall to active contradiction-surfacing; compounds with the 11K-memo brain |
| 3 | **Re-enable the disabled `PRISM Hermes-Obsidian Bridge` task** (after diagnosing why it was disabled — R7). The write-back arm of the loop is currently off. | `Get-ScheduledTask 'PRISM Hermes-Obsidian Bridge'` (State=Disabled), `scripts/system-viz-obsidian-bridge-v2.mjs` | **activate-dormant** | **S** | **High** — a disabled bridge silently breaks the "WRITE-back" half of the closed loop |
| 4 | **Close the GEPA feedback edge** — add a `context_from`-chained `hermes-skill-gepa.mjs --apply`-staging job that fires after each weekly self-review, so trace→critique→candidate runs on a cadence instead of manually. Keep operator-gated ship. | `scripts/hermes-skill-gepa.mjs`, Hermes `cron/jobs.json` (add 4th job), `state/shared/skill-loop-verdicts.jsonl` | **activate-dormant** (GEPA built; loop edge missing) | **S** | **Med-High** — self-improving skill loop with zero new infra; the GEPA self-congratulation-bias fix only pays off if it runs unattended |
| 5 | **Build HMEMV04-06 reverse vault mirror** — Obsidian-side edits flow H:→C: so a human editing a memory in Obsidian isn't silently overwritten by the one-way C:→H: mirror. | `mcp-server/data/milestones/HERMES-MEMORY-VAULT-MS0.json` (HMEMV04-06 `not_started`), pattern after `scripts/bootstrap-c-mirror.mjs` | **net-new** | **M–L** | **Med** — unlocks true bidirectionality; today human vault edits are at risk; flagged as "biggest dormant miss" in engines/CLAUDE.md |

**Honest caveats (R12):** The Hermes loop is more alive than the AGENTIC doc's "memo-asserted" hedging implies — I verified fresh cron output and successful task runs this morning. The `hermes-zulu` *galaxy directory* has only doc files (CLAUDE/MEMORY/PATHS/TOOLBELT), no `.ts` engines — the orchestration parallel-execution engines named in the AGENTIC doc (`HermesFileScopePartitionerEngine` etc.) were NOT found under that path and remain **unverified-here**. The accelerations above are deliberately the wiring/activation gaps (S/M effort, dormant-built assets) over net-new builds, per R13 logical-order: cache-wire (#1) and re-enable-bridge (#3) sit on proven foundations and should precede the net-new thinking-partner/HMEMV work.