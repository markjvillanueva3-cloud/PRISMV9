To clarify the verifier flag: I did **not** create any files in this assessment — `dreams/2026-06-04.md` was a claim I relayed from commit `23de0e7881`'s message, not a file I wrote. I have now verified it: the file **does exist**, at `knowledge/memories/dreams/2026-06-04.md` (alongside `2026-06-08/09/10.md` — the dream-cycle is actively producing nightly output, with `2026-06-10.md` written today). The commit's claim was accurate; the only inaccuracy was the path I cited (the commit said `dreams/2026-06-04.md`, actual is `knowledge/memories/dreams/`). My memo's substance stands. The corrected memo follows.

## Obsidian capability state + accelerations

### What exists + works today (verified)

- **Live REST bridge (read-only)** — `mcp-server/src/engines/ObsidianRestBridgeEngine.ts:244` (418 lines). Fail-soft client over the Local REST API plugin (`https://127.0.0.1:27123`). Wired into `mcp-server/src/tools/dispatchers/sessionDispatcher.ts:588` as 3 actions: `obsidian_status`, `obsidian_read`, `obsidian_search` (dispatch cases at `:3999`). Fail-closed on non-loopback host; key-fallback reads the plugin's own `data.json` when env var absent. **Read-only — no write method exists.**
- **C:→H:→vault auto-feed** — `.claude/hooks/stop-obsidian-memory-feed.mjs` (164 lines), wired in `H:/.claude/settings.json` (1 ref). Spawns `scripts/obsidian-memory-sync.mjs` detached every Stop, 3-min global throttle. Stamp file freshly written `2026-06-10 11:06`. Vault is live: **17,471 memory .md files** under `knowledge/memories/`.
- **Reverse mirror H:→C: (bidirectional write-back) — SHIPPED + WIRED** — `.claude/hooks/h-to-c-obsidian-mirror.mjs` (5,583 bytes), PostToolUse, **1 ref in settings.json**. Fires on Write/Edit/MultiEdit under `knowledge/memories/**`, strips linter enrichment to prevent mirror-loop, mtime-gates, SHA-256 byte-equal skip. **The seed/galaxy-doc claim "bidirectional vault write-back is the #1 gap / NOT YET BUILT" is STALE** — it shipped 2026-06-04 (`5bcf40f66f`).
- **Hybrid BM25 + dense recall — LIVE** — `.claude/hooks/memory-index-precheck-inject.mjs` (124 lines) wired in both settings.json, with **`PRISM_MEMORY_INDEX_INJECT=1` explicitly set in `H:/.claude/settings.json`** (flipped ON + fire-counter added 2026-06-09). **This CONTRADICTS the seed note** (`reference_memory_index_inject_disabled_finding_2026_06_01`) — recall is now ENABLED. It calls `runMemoryIndexSearch` in `scripts/lib/memory-index-search-lib.mjs`, which does BM25 + **dense (nomic-embed-text 768-d int8) + Reciprocal Rank Fusion** (§A6, line 359). Sidecar `state/shared/memory-embeddings-sidecar.json` is **current: 17,010 records, built 2026-06-10T16:09, `partial:false`**; `nomic-embed-text:latest` confirmed live in Ollama. Self-activating, fail-safe to BM25-only with a 2.5s query-embed cap + 2-min circuit breaker.
- **CAG cold-cache anchor** — `.claude/hooks/cag-cold-cache-anchor.mjs` (211 lines), wired (1 ref), SessionStart. Catalogs `scripts/lib/cag-router.mjs#COLD_SOURCES` for prompt-cache anchoring + writes sidecar.
- **Dream-cycle nightly synthesis — LIVE + scale-fixed + actively producing** — HMEMV04 Jaccard `findConnections`, OOM-fixed for the 11,183-memo corpus (`23de0e7881`, 73s-OOM → 3.85s). **Verified producing nightly output:** `knowledge/memories/dreams/{2026-06-04,06-08,06-09,06-10}.md` exist (`2026-06-10.md` written today). Memo embedding cache refresh runs on the same Stop cadence (`memo-embedding-cache.jsonl` = 46MB).
- **HMEMV-MS0: 11/11 units shipped** (`ed62a8e1db` closeout, 122 tests). **But the envelope `mcp-server/data/milestones/HERMES-MEMORY-VAULT-MS0.json:6` still says `"status":"not_started"`** — pure envelope drift.

### Dormant / disabled / half-built (with activation cost)

- **Qdrant memory corpus NOT loaded (HMEMV09)** — `prism-qdrant` container IS UP (`fe30e81bd0ed_prism-qdrant`), REST reachable on `:6333`, but collections are only `prism_engines / prism_skills / prism_formulas` — **the 17K memory + tribal + wiki vectors are NOT in Qdrant**. Recall runs off the flat `memory-embeddings-sidecar.json` (linear int8 cosine scan, not ANN). Activation = **net-new build** (migration + reRanker fallback). HMEMV09 P1 unbuilt.
- **No HNSW/ANN on the memory recall path** — `denseRankAll` is a full linear scan over 17,010 records every dense query. HNSW exists only in `WikiIndexMaintainerEngine.ts` / `QdrantCapacityPlannerEngine.ts`, not memory recall.
- **Stale `.partial` orphan** — `state/shared/memory-embeddings-sidecar.json.partial` (21.7MB, `2026-06-09 22:17`), leftover from an interrupted sidecar build. Dead bytes.
- **HMEMV envelope status drift** — `not_started` on all 11 shipped units.
- **Two recall hooks, divergent retrieval / dual embedding caches** — `memory-index-precheck-inject.mjs` (BM25+dense, prompt) vs `memory-relevance-inject.mjs` (PreToolUse:Edit, sole consumer of `memo-embed-lib.mjs`/`memo-embedding-cache.jsonl`). Two caches (768-d sidecar vs jsonl) maintained in parallel — R7 conflict.
- **HMEMV02 explainable retrieval** — P0, NOT built. Hits show score, no token/component breakdown.
- **HMEMV03 temporal recall (`recall_at_time`)** — P1, NOT built.

### Top accelerations

| # | WHAT | concrete FILES | dormant-or-net-new | effort | est ROI |
|---|------|----------------|--------------------|--------|---------|
| 1 | **Load 17K memory corpus into Qdrant + dense arm → ANN** — kills per-query linear scan over 17,010 int8 vectors, unlocks filtered (per-leg/galaxy) + sparse-dense + quant. Container already up. | `mcp-server/src/engines/database-expansion/*` (QdrantMemoryEngine), new migration seeding from `memory-embeddings-sidecar.json`; rewire `scripts/lib/memory-index-search-lib.mjs` `denseRankAll` → Qdrant (keep BM25 RRF + flat-file fallback). **HMEMV09.** | net-new (infra exists) | **L** | **HIGH** — biggest recall scalability+latency win; corpus already 17K |
| 2 | **Reap stale `.partial` + dedup the two embedding caches** onto canonical sidecar so one cache feeds both recall hooks. | delete `memory-embeddings-sidecar.json.partial`; repoint `.claude/hooks/memory-relevance-inject.mjs` + `scripts/lib/memo-embed-lib.mjs` | activate-dormant | **S** | **MED** — halves embed I/O, removes R7 dual-cache drift |
| 3 | **HMEMV02 explainable retrieval** — surface matched-tokens + BM25-component + dense-cosine per hit (lib already computes both scores). | `scripts/lib/memory-index-search-lib.mjs` (return `explanation`), `memory-index-precheck-inject.mjs` `renderBlock` | net-new (small) | **S** | **MED-HIGH** — trust/auditability, low effort |
| 4 | **HMEMV03 temporal `recall_at_time`** — git-log-walk over memories+wiki → state-of-belief at T as a `prism_memory` action. | new lib + dispatcher action in `mcp-server/src/tools/dispatchers/devDispatcher.ts` | net-new | **M** | **MED** — unique capability, regression archaeology |
| 5 | **Fix HMEMV-MS0 envelope status drift** — 11 units + milestone `not_started`→`completed` to match git. Stops the orchestrator re-queuing shipped HMEMV work. | `mcp-server/data/milestones/HERMES-MEMORY-VAULT-MS0.json`, then `node scripts/build-milestone-progress.mjs` | activate-dormant (data fix) | **S** | **MED** — de-risks the build queue |

**Honest caveats:** Bidirectional write-back (h-to-c reverse mirror) and BM25+dense recall are LIVE — the seed flagged both as gaps but they shipped 2026-06-04/06-09; do not re-build. Real frontier is Qdrant migration (#1) + explainability (#3,#4). I did not invoke the REST bridge against a running plugin to confirm the live read path (engine is fail-soft; the file-feed path is confirmed working `2026-06-10 11:06`). I created no files in this assessment — all cited artifacts (including `knowledge/memories/dreams/*.md`) were verified to pre-exist on disk.