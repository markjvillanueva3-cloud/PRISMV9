## Dormant/disabled Obsidian+Hermes assets (fastest ROI)

**Verdict up front (R12 honesty):** The classic "BUILT-but-unwired hook" lane for this domain is now **nearly empty** — my sweep found **0 unwired** obsidian/hermes/memory/recall/vault/tribal hooks in `C:/Users/wompu/.claude/settings.json` (every such `.mjs` on disk is wired). The 2026-06-06 spec's top-4 levers all **shipped since** it was written. The remaining dormancy is **(a) one disabled scheduled task carrying 2499 stranded memories, (b) env-gated-OFF context injectors, and (c) one disabled diagnostic task.** Activating dormant still beats net-new — but the dormant pool is small and well-understood.

### What exists + works today (verified)

- **memory-index per-prompt recall is LIVE again.** The 2026-06-01 memo (`reference_memory_index_inject_disabled_finding_2026_06_01.md`) said `PRISM_MEMORY_INDEX_INJECT=0` was a deliberate context-economy cluster. **That cluster has since been flipped ON** — `C:/Users/wompu/.claude/settings.json:48-50` now reads `PRISM_MASTER_INDEX_INJECT=1`, `PRISM_MEMORY_INDEX_INJECT=1`, `PRISM_WIKI_PRECHECK_INJECT=1`. The earlier memo is stale on this point.
- **Live recall pipeline, all wired (1 settings ref each):** `memory-rag-inject.mjs`, `recall-first-advisory.mjs`, `tribal-by-domain-inject.mjs`, `memory-index-precheck-inject.mjs`, `obsidian-vault-precheck-inject.mjs` (`PRISM_OBSIDIAN_VAULT_PRECHECK_DISABLE=0`).
- **Hermes offline-compounding tasks are RUNNING again.** Per `PowerShell Get-ScheduledTask`: `PRISM Hermes Dream-Cycle Synth` = **Ready**, `PRISM Hermes Self-Reflect Weekly` = **Ready** (the 2026-06-08 memo found both *Disabled*; they were re-enabled). Output proves it: `knowledge/memories/dreams/2026-06-10.md` written today 3:17 AM; `weekly-hermes-reflection-2026-06-07.md` present. The fail-soft floor (`scripts/obsidian-learning-revival.mjs` + `obsidian-learning-revival-sessionstart.mjs`) is wired in `H:/.claude/settings.json:334`.
- **Spec lever #1 (L5 source-chain propagation) SHIPPED + wired** — `scripts/lib/source-chain-lib.mjs` imported into `.claude/hooks/master-index-precheck-inject.mjs:43` (`decorateHits`/`renderHitProvenance`), stamps `[src: <type>:<id>]` on hits.
- **Spec lever #2 (PSN-attribution ledger) SHIPPED + LIVE-WRITING** — `scripts/lib/psn-attribution-lib.mjs` + `state/shared/psn-attribution.jsonl` (429 lines, last written **today 11:08 AM**). Spec marked this "(planned)/absent."
- **Spec lever #3 (compaction memo emitter) SHIPPED + wired** — `.claude/hooks/precompact-memo-emit.mjs` wired at `C:/.../settings.json:401` (PreCompact).
- **`zulu-advisory-inject.mjs` field-fix done + now WIRED** — confirmed 1 ref in `H:/.claude/settings.json` (the 06-09 memo's gate was lifted; CHO02 accuracy fixed).
- **Other healthy scheduled tasks:** `PRISM Brain Refresh`, `PRISM Tribal Promotion Cron`, `PRISM Tribal Consolidate Weekly`, `PRISM Wiki Link Healer Apply/Suggest`, `PRISM Wiki-Tribal Audit Regen`, `PRISM Fleet Memory Monitor` — all **Ready**.

### Dormant / disabled / half-built (with activation cost)

1. **`PRISM Hermes-Obsidian Bridge` scheduled task = Disabled** (the only disabled Hermes pipeline element). Task action: `node H:/prism/scripts/hermes-obsidian-memory-bridge.mjs`. **What it does:** copies the Hermes *desktop* agent's own siloed memory store (`C:/Users/wompu/AppData/Local/hermes/**/*.md`) into `H:/prism/knowledge/hermes-brain/` with Obsidian frontmatter, making Hermes's notes first-class vault graph nodes. SHA-256 source-body dedup, idempotent, distinct from `obsidian-memory-sync.mjs` (no clobber). **Why it's off:** rationale recorded as "unknown — not touched without operator" (`OBSIDIAN-HERMES-CONTEXT-LEARNING-ACCEL-2026-06-06.md:22`). **The ROI bomb:** the source dir holds **2499 `.md` memory files**; the target `H:/prism/knowledge/hermes-brain/` is **ABSENT (never bridged)** — i.e. 2499 Hermes learnings are currently invisible to the vault/Obsidian graph and to every recall/RAG path. **Activation:** one elevated run — `Enable-ScheduledTask -TaskName 'PRISM Hermes-Obsidian Bridge'` (or a one-shot `node H:/prism/scripts/hermes-obsidian-memory-bridge.mjs` with no elevation needed for the manual run). **Risk:** LOW — idempotent, dedup-by-SHA, writes to a fresh dedicated dir; main risk is a one-time bulk-add of 2499 nodes to the vault graph (size/regen pressure) and unverified content quality of Hermes notes. Recommend a manual run first to inspect, then enable the cron.

2. **3 SessionStart health-injectors: WIRED but env-gated OFF** (context-economy cluster at `settings.json:58-60`, same pattern as the earlier memory-index cluster):
   - `knowledge-link-audit-inject.mjs` — `PRISM_KNOWLEDGE_LINK_AUDIT_INJECT=0`. Surfaces wiki↔memory broken-link drift from `state/shared/.knowledge-link-audit.json` (**1.4 MB, fresh 6/9**). Smoke-tested: emits 2 bytes (silent) when off.
   - `substrate-health-inject.mjs` — `PRISM_SUBSTRATE_HEALTH_INJECT=0`. Per-chat PSN substrate-health gate; cache `state/shared/.cache/substrate-health-last.json` (**stale — 5/26**).
   - `nn-graph-health-inject.mjs` — `PRISM_NN_GRAPH_INJECT=0`. Surfaces GraphSAGE GNN tier-5 health from `state/shared/nn-graph/NN-EVAL.json` (fresh 6/6). **Activation:** flip the knob `0→1` in `C:/.../settings.json`. **Risk:** LOW mechanically, but these are **deliberately off for per-prompt context budget** — re-enabling adds SessionStart context the operator chose to suppress. Do **not** auto-flip; activate only on explicit operator request (same lesson as the 06-01 false-gap). Of the three, `knowledge-link-audit` has the freshest/most-actionable producer data.

3. **2 injector knobs set to 0 whose hooks DON'T EXIST on disk** (`PRISM_AI_MEMO_INJECT=0` at :56, `PRISM_GOAL_SYNERGY_INJECT=0` at :57, `PRISM_PSN_CHECKLIST_INJECT_DISABLE=1` at :55): `ai-memo-inject.mjs`, `goal-synergy-inject.mjs`, `psn-checklist-inject.mjs` — **0 settings refs, 0 files matched by Glob.** These are dead knobs / never-built or removed hooks, not dormant assets. **No action** (they're not "off built things," they're orphan env keys).

4. **`RunFullMemoryDiagnostic` task = Disabled** — Windows memory *diagnostic* (RAM, not PRISM memory-vault). Out of scope for context-learning ROI; leave as-is.

### Top accelerations

| # | WHAT | concrete FILES | activate-dormant vs net-new | effort | est ROI |
|---|------|----------------|------------------------------|--------|---------|
| 1 | **Bridge the 2499 stranded Hermes desktop memories into the Obsidian vault** — the single biggest dormant corpus; currently 0 of 2499 are vault-visible | task `PRISM Hermes-Obsidian Bridge` → `scripts/hermes-obsidian-memory-bridge.mjs`; source `C:/Users/wompu/AppData/Local/hermes/*.md` (2499 files); target `H:/prism/knowledge/hermes-brain/` (empty) | **activate-dormant** (manual run first, then `Enable-ScheduledTask`) | **S** | **HIGH** — 2499 new recall/RAG-reachable nodes for one command; idempotent, low risk |
| 2 | **Re-enable `knowledge-link-audit` SessionStart inject** (freshest producer of the 3 gated injectors) — surfaces wiki↔memory broken-link drift fleet-wide | `.claude/hooks/knowledge-link-audit-inject.mjs`; knob `PRISM_KNOWLEDGE_LINK_AUDIT_INJECT` at `settings.json:58`; data `state/shared/.knowledge-link-audit.json` (1.4MB, 6/9) | **activate-dormant** (flip 0→1) — **operator-gated** (context-economy) | **S** | **MED** — improves link hygiene; but reverses a deliberate budget choice, so operator-confirm |
| 3 | **Refresh + re-enable `substrate-health-inject`** — its producer cache is stale (5/26); regen then flip | `.claude/hooks/substrate-health-inject.mjs`; `state/shared/.cache/substrate-health-last.json` (stale); knob `:60` | **activate-dormant + light net-new** (stale-producer refresh) | **S-M** | **MED** — per-chat PSN health visibility; needs producer refresh first or it shows stale data |
| 4 | **Wire `psn-attribution` read action into a surface** — ledger is live-writing (429 rows today) but has **0 settings refs** as a consumer; the "which leg got consulted" signal isn't yet surfaced/queried | `scripts/lib/psn-attribution-lib.mjs`, `state/shared/psn-attribution.jsonl`; spec lever #2 named a `prism_session:psn_attribution` read action | **net-new (thin)** — a read/render layer over a live producer | **S-M** | **MED** — closes the "did knowledge get used?" blindness; data already accumulating, just unsurfaced |
| 5 | **Clean up 3 dead env knobs** (`PRISM_AI_MEMO_INJECT`, `PRISM_GOAL_SYNERGY_INJECT`, `PRISM_PSN_CHECKLIST_INJECT_DISABLE`) pointing at non-existent hooks | `settings.json:55-57` | **net-new (hygiene)** — delete or build the 3 missing hooks | S | LOW — not acceleration, just removes confusing dead knobs |

**Honest bottom line for the zulu queue:** #1 (Hermes-Obsidian bridge → 2499 memories) is the standout cheap win and the only true dormant-corpus activation left. #2–#4 are smaller and #2/#3 are operator-gated context-economy decisions — do not auto-flip. The previously-rich "dormant Obsidian/Hermes" lane has largely been *worked off* over the last week (dream/reflect tasks re-enabled, spec levers 1-3 shipped, recall injectors re-enabled), so this lane's remaining ROI is concentrated almost entirely in #1.