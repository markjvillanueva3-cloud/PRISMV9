# DB-GAP-LIST — Prioritized Action List for H: Database Pathways

**Generated:** 2026-06-04 · **Owner:** slot juliett (database-expansion — ingestion / schema / atomicity / pathways)
**Source:** synthesis of 10 db-census scout inventories. Companion to `H-DRIVE-DB-CATALOG.md` (same dir).

> ## ⚠️ VERIFY-BEFORE-ACTION (R12 — machine-generated; diagnoses are NOT ground truth)
> This gap-list was synthesized by a multi-agent census Workflow. The **store enumeration** (paths/sizes/counts) is from real `find/du/wc` and is reliable. The **gap DIAGNOSES + actions are advisory** — each MUST be verified against live code/config before execution. A blind execution can REGRESS a currently-correct surface.
>
> **Already verified FALSE (juliett, 2026-06-04):**
> - **A2 (the listed #1 P0) is WRONG.** It claims galaxy `PATHS.md` point at a non-existent `mcp-server/data/databases/DB_MANIFEST.json`. **Reality:** all 34 galaxy `PATHS.md` already reference the CORRECT existing path `data/databases/DB_MANIFEST.json` (verified, 22 KB, v2.0.0) and wire via `scripts/wire-db-stores-to-consumers.mjs`. **There is no broken pointer — do NOT "fix" it** (that would break the correct pointers). DB_MANIFEST discoverability is already fleet-wide.
> - **D1 (a #1 P0) is WRONG on size AND status.** It claims `_node-embeddings.jsonl.partial` is "556 MB, stalled since 2026-05-22". **Reality (verified 2026-06-04 12:01):** it is 252 MB and ACTIVELY GROWING (235→247→252 MB across minutes, mtime current) — sierra/system-viz restarted the nomic-embed pass on the Blackwell TODAY (live loop `16c4c64a`). **It is a live peer write — do NOT resume/reclaim/touch it** (multi-writer corruption). Wait for sierra to finalize `.partial`→canonical; C4 consolidation is gated on that.
> - **Bonus reclaim (juliett, 2026-06-04):** removed `state/shared/tribal-embed-index.json.timingtest1` — a 508 MB **untracked** byte-size-identical throwaway dup of the live index (NOT the canonical 507 MB float twin C4 targets; that one is still read by `tribal-rerank.mjs` and stays until the int8 migration). 508 MB dead disk reclaimed.
>
> Treat every remaining item as a HYPOTHESIS to confirm (does the store exist? is it really unwired — grep src? is the "duplicate" actually a partition?) before doing the work.

**Ranking key:** `fleet-search-value × effort`. P0 = high value, low/medium effort (do first). P1 = high value, higher effort. P2 = medium value. P3 = low value / cleanup. Each item names the **owner slot** that should action it (juliett owns ingestion/schema/atomicity/pathways; domain stores route to the domain slot).

---

## (a) UNPATHED databases — the fleet literally cannot find them via PATHS.md / shared-index / critical-roots

| # | Pri | Store | Why it matters | Action | Owner |
|---|---|---|---|---|---|
| A1 | **P0** | `quoting/jm-vendor-ap-ledger.jsonl` ($10.08M A/P) + `jm-sold-orders.json` (outbound) + `jm-tool-purchases.json` ($4.91M) | The headline JM financial stores — wired to quote engines but invisible to discovery surfaces | Add a `## Financial stores` block to `quoting/PATHS.md` + `PRISM_SHARED_INDEX_SURFACES.md`; register in `DB_MANIFEST.json` | juliett + charlie |
| A2 | **P0** | `data/databases/DB_MANIFEST.json` path is **wrong fleet-wide** — galaxy PATHS.md point at `mcp-server/data/databases/DB_MANIFEST.json` (absent; verified 2026-06-04) | The central 27-DB registry is unreachable from any galaxy resolving relative to `mcp-server/` | Fix the path token in `wire-galaxies-to-resource-roots.mjs` (or PATHS template) → re-materialize all 34 PATHS.md | juliett |
| A3 | ~~P0~~ **PATHED 2026-06-08 (juliett)** | `databases/jm-file-inventory.jsonl` (108 MB, 554,999 lines) + `scan-tracking/jm-die-scan-ledger.jsonl` (87 MB, 301,948 lines) + `system-viz/h-drive-files.jsonl` (187 MB, 1,275,776 lines) | Largest JM/H: inventories; the substrate behind every corpus join, but UNPATHED | ✅ All 3 pathed into `database-expansion/PATHS.md` §"Large file/scan inventories" (with verified live consumers — they were WIRED, only discovery-invisible). `JMFileInventory` + `JMScanLedger` registered in DB_MANIFEST (now 36); `h-drive-files` left as a sierra/system-viz PATHS pointer (peer-owned, no DB_MANIFEST overstep). The "move scattered ones" sub-action deferred — moving 382 MB of live-consumed JSONL would break the wired readers' hardcoded paths (would need a coordinated consumer-path migration, not a P0 quick-win). | juliett ✓ |
| A4 | **P1** | `tribal-embed-index.json` (507 MB) source corpus `mcp-server/data/tribal/*.jsonl` (×7, incl `jm-die-corpus-pages.jsonl` 17.5 MB) UNPATHED in tribal-knowledge baseline | The tribal RAG substrate is invisible to its own galaxy atlas | De-baseline `tribal-knowledge/PATHS.md` — enumerate the 7 owned stores (model on wedm's 47-store atlas) | mike (tribal-knowledge) |
| A5 | **P1** | 16 of 34 galaxy PATHS.md are **auto-derived baseline** (store-blind): corpus-aggregation, pdf-corpus, pdf-corpus-mill, quality, shop-floor, compliance-safety, mit-curriculum, knowledge-conversion, agent-orchestration, cad-fusion-live, dormant-data + others | These galaxies own real stores (e.g. quality owns `data/quality/*`, pdf-corpus owns 30 ingestion_cache jsonl) but their atlas only repeats the 3-root boilerplate | Per-galaxy de-baseline pass: enumerate owned stores into each PATHS.md (wedm = gold standard template) | per-domain slot, coordinated by juliett |
| A6 | **P2** | Docustrata `.index/training-triples-v4.jsonl` (print→program training triples) + `phase20-verified-prints.jsonl` (44,012 pages) | High value for india AI-training + blueprint accuracy, but only root-pathed | Path the named `.index/*.jsonl` derived DBs explicitly in ai-training + blueprint-vision PATHS.md | india + xray |
| A7 | **P2** | `quoting/quoting-data-index.json` (46-entry self-index) is itself UNPATHED+UNWIRED | A ready-made self-index of the whole quoting root that nothing points at | Promote it to a pathed surface in `quoting/PATHS.md` + link from shared-index | charlie |
| A8 | **P3** | `system-viz/h-drive-census.json` (126 MB) + `UNLINKED-MENTIONS.json` (96.8 MB) + `jm-sim/jm-job-catalog.json` (19.3 MB) | Large orphans, overlap canonical census/JM stores | Path-or-prune decision (see C2/C5) | sierra + juliett |

## (b) UNWIRED databases — no dispatcher/engine consumes them (dead data)

| # | Pri | Store | Why it matters | Action | Owner |
|---|---|---|---|---|---|
| B1 | ~~P0~~ **DONE 2026-06-04 (juliett)** | `jm-die-database/tables/documents.jsonl` (59.7 MB, 111,745 docs) + Docustrata `blueprint-program-join-full-v6.jsonl` (76,205 PNs) | **VERIFY-FIRST CORRECTION:** the v6 join was ALREADY wired (`prism_data:program_print_link_lookup`/`_coverage` + a `prism_dev` mirror + uppl tests) — the "no consumer" claim was HALF-FALSE. Only `documents.jsonl` was the real gap. | ✅ `documents.jsonl` wired: new `JMDieDocIndexEngine` + `prism_data:jm_die_doc_lookup` (14 tests, live-validated on 111k docs) — commit `0674947b2c`. v6 needs no action. | juliett ✓ |
| B2 | ~~P0~~ **INGEST DONE 2026-06-08 (juliett)** | `H:/PRISM/JM DIE/Prism JM Die/**/part.json` — **30,890 orphaned extraction sidecars** (20,858 `_UNASSIGNED`, 24 malformed) | The print-to-program extraction substrate, un-databased & 67% unassigned — pure dead PRISM-produced data | ✅ Ingested → `state/shared/databases/jm-part-library.jsonl` (30,890 rows, zero-drop reconciliation invariant) via `scripts/build-jm-part-library.mjs`; wired `prism_data:jm_die_part_lookup` (JMDiePartLibraryEngine, 19 round-trip tests, live-validated: 10,008 assigned / 5,086 program-linked / 2,402 exact+program). **Remaining:** the GPU customer/feature *classify* of the 20,858 `_UNASSIGNED` (the xray half) is still open. | juliett ✓ (ingest) · xray (classify) |
| B3 | **P1** | `memory-embeddings-sidecar.json` (15 MB) + `memory-index-sidecar.json` (6.7 MB, 11,315 records) | Built embedding sidecars with **no engine consumer found under src** — paid-for vectors going unused | Wire to a memory-retrieval consumer (or fold into the canonical vector store, D-consolidation) | india |
| B4 | **P1** (verify-first 2026-06-08 juliett: claim partly false) | `mcp-server/data/state/jm-die-full-program-index-v2.json` (14 MB) + v1 (12 MB) | The "v2 UNWIRED + v1 pure-dup" claim is HALF-FALSE: BOTH have a live consumer — `scripts/docustrata/phase16-blueprint-program-join-v6.py` reads v1+v2, and `scripts/build-jm-die-program-index.mjs` is v1's builder. Neither is "nothing reads it". **VERIFIED the supersede hypothesis is TRUE:** `MillProgramCorpusEngine` already loads the canonical `jm-die-database/tables/files.jsonl` (NOT these state files) — so the right action is ARCHIVE BOTH (superseded), NOT wire to MillProgramCorpus. **Blocker:** archiving breaks the phase16 join script if re-run → needs the phase16 producer migrated to `files.jsonl` first (a coordinated foxtrot+juliett unit, not a quick archive). Deferred with verified premise. | foxtrot + juliett |
| B5 | **P1** | `resources/` orphans: CIMCO machine-kinematics configs (71), Fusion tool CSVs (7), MACHINING-KNOWLEDGE formula `.js` (3, 320 KB), CAD-model corpus (411 STEP/1172 STL/1433 DXF — no index) | Real domain data sitting unwired in the resources install-tree; the formula `.js` belongs in `src/data/` feeding creative-reasoning | Move formula `.js` → `mcp-server/src/data/` + wire; build a non-PDF CAD/NC index for the "FOR LEARNING ENGINE" dirs; wire CIMCO configs to 5-axis kinematics | delta + echo |
| B6 | **P2** | `mcp-server/data/quality/{audit-log.jsonl,job-history.json,parameter-priors.json}` — Cpk/SPC stores with 0 PATHS pointers (wired but invisible) | Quality gate data discoverable by no atlas | Path into `quality/PATHS.md` (de-baseline) | quality slot |
| B7 | **P2** | `training/wiki-canonical-pairs.jsonl` (282 pairs) — no consumer under src/scripts | Possibly orphaned training output | Verify producer intent; wire to training loop or archive | india |
| B8 | ~~P3~~ **ARCHIVED 2026-06-08 (juliett)** | `knowledge/{data-index/DATA_TAXONOMY,sessions/SESSION_KNOWLEDGE_INDEX}.json` (Feb-17, 8 entries, fully UNWIRED+STALE) | Dormant dead indexes superseded by live inventory/CODE_SYSTEM_INDEX | ✅ Verified zero live consumers (only `.claude/worktrees/` stale-peer hits + the merged system-graph index — no `src/`/`scripts/` reader) → renamed to `*.archive.2026-06-08.json` (never-delete-only-disable; untracked files, in-place rename). | juliett ✓ |

## (c) Un-consolidated / DUPLICATE stores that should be merged

| # | Pri | Duplicate set | Cost | Action | Owner |
|---|---|---|---|---|---|
| C1 | ~~P0~~ **VERIFY-FALSE — already single-sourced (juliett 2026-06-08)** | Vendor stores "quadruple-overlap" | The "uncoordinated overlap, deprecate 3" premise is WRONG — verified a coherent producer→DB pipeline: `quoting/vendor-directory.jsonl` (482) is the **SOURCE** (built by charlie's `build-vendor-directory.mjs` from `vendor-sources/` + JM-AP), and `vendor-catalog-db/tables/vendors.jsonl` is its **byte-identical DB MIRROR** (juliett's `build-vendor-catalog-db.mjs` READS vendor-directory.jsonl as input — `abs.vendors`). Deprecating the "standalone" directory would break the DB builder (it IS the canonical source, not a copy). The other two are NOT dups: `databases/jm-vendors.jsonl` (12 rows) is a distinct **spend-aggregated view** (vendor_key/total_spend/grades — real $22K Griggs data, consumed by `JMCustomerVendorDatabaseEngine`); `vendor-catalog-manifest.json` is the **catalog-extraction manifest** (consumed by `VendorCatalogManifestEngine` + the db builder). No safe deprecation — every store has live consumers + distinct schema/purpose. **(no-op; the pipeline is already correct)** | juliett + charlie |
| C2 | **P1** | 3 FS-census overlap: `system-viz/h-drive-census.json` (126 MB) + `h-drive-dir-index.json` (21 MB) + canonical `RESOURCE_CENSUS.json` (dead 0-row) | 147 MB + a dead canonical | Fix/re-run the canonical census scanner (see D5), fold the two viz censuses into it, then evict | sierra + juliett |
| C3 | **P1** | `baseline-records-corpus.json` + `-with-real` + `-with-synth` (16.8 + 18.7 + 18.7 = **54 MB**, 3 near-identical) | 54 MB of overlapping records | Merge to base + `{real,synth}` delta flags | charlie + india |
| C4 | **P1** | Embedding-sidecar sprawl: `tribal-embed-index.json` (507 MB float) ≡ `_embeddings.jsonl` (106 MB int8, SAME corpus) + `_node-embeddings.jsonl.partial` (LIVE — sierra building it now, ~252 MB & growing 2026-06-04, NOT 556 MB; C4 consolidation must wait for its finalize) + `nn-graph/768d` (7.2 MB) + memory sidecars (22 MB) = **~1.2 GB, 6 stores, no shared HNSW** | 401 MB redundant (float vs int8 of identical corpus) + no unified index | Single canonical int8 dim-768 vector store + one finalize/rotate path; retire the 507 MB float twin (keep 106 MB int8) | india + juliett |
| C5 | **P2** | `jm-sim/jm-job-catalog.json` (19.3 MB) overlaps `databases/jm-file-inventory.jsonl` + `jm-die-database/manifest.json` | 19 MB | Consolidate JM corpus view into jm-die-database manifest | juliett |
| C6 | **P2** | Docustrata `.index/` version chains: join v0–v6 (~131 MB, only v6 live); documents-classified v0/v2/v3; text-extracted v0/v2/v3; jm-die-index v1/v2; role-to-docs v0/v2/v3 (~250 MB+ superseded) | ~250+ MB superseded JSONL/JSON | Archive all `-v{<latest}` (NEVER delete the `.index/` — it is the never-re-OCR substrate; only archive superseded versions) | juliett |
| C7 | **P2** | `print-corpus-tables/rows.jsonl` (58 MB) vs `by-customer/_PART_LIBRARY.jsonl` (58 MB) — partition or copy? | 58 MB if copy | Verify partition-vs-copy; if copy, keep one + a view | xray |
| C8 | **P3** | Rollback/bak pairs: `system-graph.previous.json` (495 MB), `HOOK_REGISTRY.previous.json`, `STOP_HOOK_REGISTRY.previous.json`, `slot-task-queues.bak-pre-lima-*`, `.tribal-embed-index.bak.json`, `cad-corpus-manifest{,-recovered}` pair, `test-model-batch1-*`, `find-cache.json` | ~1 GB+ rollback/cache/debris | Cap rollbacks to N=1; evict regenerable caches + test-model debris | fleet-hygiene (golf) |
| C9 | **P2** | `zebra-orchestrator-log.jsonl` (10 MB, DEAD) superseded by `zulu-orchestrator-log.jsonl` (Zebra→Zulu rename) | 10 MB dead dup | Archive zebra log | hermes-zulu (bravo) |

## (d) GPU-gen acceleration opportunities — embedding/LLM/OCR passes that should run on the Blackwell (RTX PRO 6000, 96 GB)

| # | Pri | Job | Value | Action | Owner |
|---|---|---|---|---|---|
| D1 | ~~P0~~ **LIVE — DO NOT TOUCH** | `system-viz/_node-embeddings.jsonl.partial` — **ACTIVE peer write** by sierra/system-viz on the Blackwell (loop `16c4c64a` iter4). **VERIFIED 2026-06-04 12:01 (juliett):** 252 MB and GROWING (235→247→252 MB observed across minutes, mtime current). The "556 MB / stalled since 2026-05-22" figure was STALE — the pass was restarted today. | Unblocks GNN tier-5 once sierra's run finalizes `.partial`→canonical | Hands-off until sierra's loop completes; juliett/india must NOT resume or reclaim — live multi-writer corruption risk | sierra (owns live run) |
| D2 | **P0** | Architecture-leaf embed tail: `_embeddings.jsonl` covers 9,680/35,096 architecture .md (27.6%) | The only true wiki coverage gap; high-value memory/tribal/code-tribal already ~100% | Hash-gated incremental Ollama `/api/embed` GPU batch to close 27.6%→100% (rows already carry `h:` hash) | india |
| D3 | **P1** | `jm-sold-orders.json` revenue OCR: 7,247 low + 1,333 no-confidence of the $47K-confirmed (vastly understated) outbound side | Closes the quote-accuracy gap (revenue side is 99% un-extracted; baseline corpus uses cost×1.4 STUB → MAPE 71%) | GPU/xray OCR pass over sold-orders + Docustrata invoices → replace the stub revenue label → retrain | xray + charlie |
| D4 | **P1** | Upgrade `CAM_TRIBAL_RAG_INDEX.json` (TF/vocab) + `SEMANTIC_SIGNATURES.json` (term-bag) from sparse → dense embeddings | Two "vector" stores that are actually term-presence bags — real embeddings would lift retrieval quality | Re-generate as dense nomic-embed-text vectors on GPU; keep the same wired consumers | kilo + india |
| D5 | **P2** | 241 unindexed `MANUFACTURER_CATALOGS` PDFs (287 on-disk, 46 in pdf-index) + 513 RESOURCE PDFS + 196 MIT PDFs | Direct tool-parameter / training extraction value | GPU-OCR + embed pass via lima pypdf extractor → extend `cad-cam-resources-pdf-index.json` | oscar + lima |
| D6 | **P2** | `prism-reference-db` 15% extraction loss (`storesFailed:1066` + `emptyDropped:2832`) | ~15% of structured reference data never landed | LLM-assisted re-pass over the failed source files | delta + juliett |
| D7 | **P3** | LoRA fine-tune targets: `quoting-lora-smoke-out/{train,val,test}.jsonl` + PSN training text legs | Direct GPU fine-tune datasets | Schedule GPU LoRA train when quoting revenue labels are real (post-D3) | india |

---

## Top 5 prioritized actions (fleet-search-value × effort)

1. ~~**[A2/P0] Fix the fleet-wide `DB_MANIFEST.json` broken pointer**~~ → **VERIFY-FALSE (no action) — confirmed 2026-06-08 (juliett):** `mcp-server/data/databases/DB_MANIFEST.json` is genuinely ABSENT, but the galaxy PATHS.md already reference the CORRECT existing path `data/databases/DB_MANIFEST.json` (verified present, 36 DBs). There is no broken pointer — "fixing" it would break the correct ones. **(no-op)**
2. ~~**[A1/P0] Path the 3 JM financial stores**~~ → **DONE 2026-06-04 (juliett, commit `8300622f39`):** the A/P ledger + sold-orders + $4.91M tool-spend are pathed into `quoting/PATHS.md` + registered in DB_MANIFEST (auto-propagated to quoting+business PATHS.md). Verified present 2026-06-08. **(juliett ✓ + charlie)**
3. ~~**[B1/P0] Wire the dead JM derived DBs**~~ → **DONE 2026-06-04 (juliett):** `documents.jsonl` wired via `prism_data:jm_die_doc_lookup` (JMDieDocIndexEngine, commit `0674947b2c`, 14 tests). The `blueprint-program-join-v6` half was ALREADY wired (`program_print_link_lookup`) — verify-first caught the half-false claim. **(juliett ✓)**
4. ~~**[D1/P0] Resume the 556 MB stalled node-embedding pass**~~ → **CORRECTED 2026-06-04 (juliett): NOT stalled — sierra/system-viz is actively building it on the Blackwell right now** (252 MB & growing, loop `16c4c64a`). No action for juliett/india — wait for sierra to finalize `.partial`→canonical, THEN fold C4 consolidation. **(sierra owns the live run; do NOT touch the active write)**
5. ~~**[B2/P0] Ingest the 30,896 orphaned `part.json` extraction sidecars**~~ → **INGEST DONE 2026-06-08 (juliett):** consolidated 30,890 sidecars → `state/shared/databases/jm-part-library.jsonl` (zero-drop invariant; 10,008 assigned / 20,858 `_UNASSIGNED` / 24 malformed) + wired `prism_data:jm_die_part_lookup` (JMDiePartLibraryEngine, 19 tests, live-validated). The GPU customer/feature *classify* of the 20,858 `_UNASSIGNED` (the xray half) remains open. **(juliett ✓ ingest · xray classify)**
