# HANDOFF — claude-99eca613 — cad-fusion-live-ms0

**Generated:** 2026-05-11 (post §R9 MS-CAM-MASTERY + v7.5 build/commit)
**Branch:** cad-fusion-live-ms0
**v7.5 commits:** original 3 files landed in `2ddb3ec76` (swept into a peer's commit — content permanent, message cosmetic). Then **`2440e79e4`** = §R9.8 (system-viz node-coverage confirmation).

**v7.6 commit (`cf774b5e9`)** = `[CAD-FUSION-LIVE-MS0]/U-REVENUE-V7.6-RESOURCE-PRINT-PROGRAM` — added **§R10** (`round9/00-v7.6-resource-print-program.md`, 56.7 KB): produced by a 4-agent forge-style survey of `Resources/` + `JM DIE/` + `Docustrata/` + the print→program engine surface. §R10.1 bridges the orphaned `RES-ROADMAP.json` (28 milestones, only RES-MS0 + RES-MS18 done) into the revenue roadmap — promotes 10 RES-MS to revenue milestones (`MS-RES-TOOLDB-IMPORT`=RES-MS14, `MS-RES-NC-MINE`=RES-MS11, `MS-RES-XLSM-ENGINE`=RES-MS16-core, `MS-RES-CADCAM-DOCS`=RES-MS5/6 [8 U-RES-DOC units], `MS-RES-MATERIAL-ENRICH`=RES-MS22, `MS-RES-FIXTURE-CATALOGS`=RES-MS3/19, `MS-RES-POST-CYCLE-LIB`=RES-MS2/20/25, `MS-RES-HYPERMILL-SDK`=RES-MS7/15/23, `MS-RES-FORMULA-ALGO`=RES-MS1/9, `MS-RES-MACHINE-MODELS`=RES-MS4), folds the rest (RES-MS6/10/12/13/17/21/24/27) into existing milestones, full 28-row status table. §R10.2 = the CAD/CAM-tutorial inventory by system feeding §R9 (hyperMILL best-documented; 21 InventorCAM PDFs ✅; Esprit has NO source — `U-RES-DOC-08` acquires it; HSMWorks CHM unprocessed; SolidWorks Planchard book unextracted). §R10.3 accounts for the LIVE Docustrata extraction (phase 15 deep-OCR running NOW, ~47% done, ETA ~8 hrs) — adds `MS-DOCU-FINISH` (3u: finish phase15 → full phase-8 classifier pass → v5 rejoin) + `MS-DOCU-INGEST` (2u: de-stub `PairedPrintProgramBundleEngine` + feed it the 55 verified triples; back-annotate the archive) + 3 corrections to §R8.3 (v4→v5, no numeric ≥0.85 confidence — categorical exact/loose/ambiguous, drop MinerU). §R10.4 = `MS-PRINT-PROGRAM-LOOP` (~22 units, 4 tracks: A per-part-family templates [`U-MIN-FINGERPRINT`=RES-MS26 + `MachineDomainTemplateLibraryEngine` + `MillPartClassifierEngine` + `DieCavityBatchProgramEngine` + the `.xlsm` VBA decode], B re-optimization [`ProgramReoptimizationOrchestratorEngine` routing to the existing `LatheProgramOptimizerEngine`/`MillProgramOptimizerEngine`/`ProgramPhysicsOptimizerEngine`/`GCodeSafetyAnalyzerEngine` + `ArchiveReoptimizationBatchEngine` → the re-optimized archive becomes the CLEAN training set, raw = mistake-exemplar pool], C lathe-knowledge-direct [`LatheKnowledgeDirectProgrammerEngine` + lathe-tuned NL parser + `U-NC-MINING-CALIBRATE`=RES-MS11 + the Okuma `.MIN`-dialect post + customer→material map + per-machine knowledge profile — built on the existing no-CAD `TurningProgramAssemblerEngine` + `prism_machining_kb` ~30 lathe actions], D back-annotation [`ProgramPrintLinkIndexEngine` extending `BlueprintProgramJoinEngine` + PN normalizer + program-side seed + print-pointer fields on `ProgramMemoryEngine`/`LatheProgramCatalogEngine` + `ArchiveToPartsCatalogIngesterEngine` + rebuild the sparse CAD `master-index.json` + **`U-MCX-BINARY-PARSER` — the highest-leverage unit, unlocks all the mill arms since the `.mcx-8` are unreadable binaries today (= why `MILL_AI_TRAINING_REPORT` is cold at 27 programs)**]). `U-INV-LATHE-06` (the MS2 combinatoric print-to-program-SaaS unit) rescopes to reference `MS-PRINT-PROGRAM-LOOP`. §R10.5 LOCKS the lathe-bypass-CAD principle (for turned parts — the dominant class, 16,558 `.MIN` → ~8-14 archetypes — the knowledge-direct path IS the primary product, no CAD step; mill stays CAD/CAM-bound; `U-PPL-C4` lathe-direct is Revenue-Day-1-eligible). §R10.7 = GA-critical slice (`MS-RES-NC-MINE` + `MS-RES-TOOLDB-IMPORT` feed §R8.4 SFC-calibrate; `MS-PRINT-PROGRAM-LOOP` Track C the lathe-direct product; `MS-RES-CADCAM-DOCS` U-RES-DOC-01 gates §R9 Fusion add-in) vs the post-ship background lanes (Track A templates, Track B archive re-opt, Track D `.mcx` parser, the catalog/material/SDK extractions). v7.6.md = 2,854 lines / 371 KB. **Anti-dup (extend/compose, never fork):** `ProvenPartRecipeEngine`, `GCodeIntelligencePipelineEngine`, `TurningProgramAssemblerEngine`, `BlueprintProgramJoinEngine`, `ProgramMemoryEngine`, `PartLibraryEngine`, `LatheProgramOptimizerEngine`, `MillProgramOptimizerEngine`, `LathePartClassifierEngine` — run `duplicationGuardEngine.checkBeforeCreating()` on every new engine. **NOT YET BUILT — §R10 is the enumeration; the ~50+ engines/units are an execution job, check in with the user before starting.**

---

## RESUME DIRECTIVE (single most important field)

**Roadmap-generation is COMPLETE through v7.6 + the RGS pipeline has been RUN over it (commit `69d9d961a`).** What landed: (1) the prose spec — `state/shared/specs/REVENUE-ROADMAP-v7.6.md` (2,854 lines, commit `cf774b5e9`); (2) the structured form — `scripts/register-revenue-roadmap-envelopes.mjs` materializes the v7.6 §R-layers into **35 milestone envelopes (585 units)** in `mcp-server/data/milestones/MS-*.json` (local derived state — that dir is untracked) + adds them to the tracked `mcp-server/data/roadmap-index.json` (728 total milestones) with real-namespace `viz_node_id`s + tier 0/1/2 + `blocks`/`blockedBy` + `doc_propagation`; dep-graph verified acyclic, no contradictions, no tier violations (14 tier-0 / 15 tier-1 / 6 tier-2 milestones); the v7.6 corrections are baked into the envelopes (v4→v5 join, categorical-confidence, clean lathe corpus for U-TRAIN-P2P-06, U-INV-LATHE-06 rescope, MS-INFRA scaling-floor); (3) the atomized bundle — ran `atomic-roadmap-emit.mjs` → `ai-priority-rank` → `conflict-predict` → `roadmap-pass-record`: `state/shared/atomic-roadmap.json` (3572 units, 351 tier-0 — up from 1137/40 because I made 2 minimal additive fixes to `atomic-roadmap-emit.mjs`: `inferTier` now honors explicit `env.tier`/`unit.tier`, `loadCandidates` now reads `env.phases[].units` — the ACP-style envelope schema was collapsing every such milestone to a single candidate), `atomic-roadmap-chat-1..6.md` (the 6 lane splits), `atomic-roadmap-summary.md`, `predicted-collisions.json` (1209 collisions, verdict RED — fork affected chats to sibling worktrees before Phase 1), `roadmap-pass-history.jsonl` (a pass record appended — the closed loop's first revenue-roadmap entry; the embed-index over it failed 7 records because Ollama wasn't reachable for nomic-embed — re-run when local compute is up). The atomic-roadmap output + the 35 envelopes are LOCAL DERIVED STATE (regen: `node scripts/register-revenue-roadmap-envelopes.mjs && node .claude/scripts/atomic-roadmap-emit.mjs && node .claude/scripts/conflict-predict.mjs && node .claude/scripts/roadmap-pass-record.mjs`). Coordination posted to the chat bus: (A) the revenue milestones merge INTO the single `atomic-roadmap.json` with `track:"revenue"`, not a third atomized doc; (B) real-namespace `viz_node_id`s so the peer's `audit-roadmap-viz-bindings.mjs` binds cleanly. The peer (MarkV-35820 / `Claude@MARKV/pid-33388`, the backend-devtools-roadmap chat) was also editing `atomic-roadmap-emit.mjs` — my edits are at `inferTier`(~95) + `loadCandidates`(~154), away from their `enrichWithEvidence`/XML-output work; both committed cleanly. **Outstanding (the pipeline UPGRADE, not the pipeline itself):** wiring the 4 dormant data sources (MILESTONE_PROGRESS shipped-fraction, tribal-density verdict, system-viz blast-radius, envelope drift) more deeply into `atomic-roadmap-emit.mjs` per `stateless-weaving-beacon.md` Track-A — partly done (`enrichWithEvidence` exists, `roadmap-pass-history.jsonl` exists), the peer is on it; re-run `/rgs6` when it lands, the bundle improves + the closed-loop telemetry logs the delta.

**Below is the v7.5 RESUME (still accurate — the execution sequence is unchanged; v7.6 §R10 added the resource/print-program/Docustrata layer, see the v7.6 commit note further down):**

**The roadmap is now v7.5 — `state/shared/specs/REVENUE-ROADMAP-v7.5.md` (committed, 2630 lines / 311KB).** Lineage: v7.1 (round-4 merge) + §R5 (round-5 consensus) + §R6 (monolith-harvest) + §R7 (round-6 launch-closure + desktop-app final phase) + §R8 (round-7 wire-everything / deep-training / SFC-calibration / doc-backflow / viz-binding) + **§R9 MS-CAM-MASTERY (PRISM as expert CAD/CAM operator + bridge add-ins)**. **Next phase = EXECUTION.** No more roadmap rounds unless the user asks.

**Read these first, in this order:**
1. `round8/00-v7.5-cam-mastery.md` (§R9) — MS-CAM-MASTERY: per-system (Fusion → hyperCAD/hyperMILL → Mastercam → Inventor → SolidWorks → Esprit, in that order; ~19 others idle until ship) 5 training pillars: A "how to CAD" (every sketch/feature/assembly/PMI/drawing tool at click level — `*CADFunctionIndexEngine` exhaustive + vendor docs + JM-Die CAD corpus); B "how to use CAM" (every 2D/3D-rough/3D-finish/5-axis/turning/mill-turn/probing strategy + every dialog input + every button — `*FunctionIndexEngine` + `*DeepLearningEngine` + per-system strategy catalogs); C function-index completeness + per-system LoRA adapter (`CAMLoRAAdapterTrainerEngine`); D the PRISM bridge add-in (`CAMAddInFrameworkEngine` + `BatchCAMAddInGenerators` + `cam-bridge` skill): "Speed&Feed via PRISM" / "Auto-program via PRISM" / "Post via PRISM" (subscription post), gated by `requireTier`; E the Claude orchestration glue. AI backbone (tribal/DL/deep-reasoning/algorithms/formulas/SFC/mech-eng/neural/AI-systems/Claude-orch/knowledge+memory) is the SUBSTRATE — all exists; §R9 wires the CAM function-indexes into it. Revenue tie: MS-CAM-MASTERY ← MS-MASTERPOST (verified posts) ← MS-PAY (tier gate) ← U-LEGAL-13. Fusion add-in Speed&Feed + Auto-program ship at Revenue Day 1 (~Wk 6-8); Post-via-PRISM at Master Post GA (~Wk 9-10).
2. `round7/00-v7.4-wiring-training-closure.md` §R8.7 (updated sequencing) — the Week-0 cluster: `U-REV-CI-00` + `U-WIRE-BE-P0-01` (audit-script upgrade + `wiring-batch.mjs` + `check-engine-wired.mjs`) + `U-WF-CI-00` (`audit-page-wiring.mjs`) + `U-DOCFLOW`'s `audit-doc-backflow.mjs`, all fold into one CI-gate cluster. Co-design the system-viz roadmap-binding schema with the peer chat (`MS-VIZ-ROADMAP-BIND` is shared lane).
3. `round6/00-v7.3-closure.md` §R7.4 (Revenue Day 1 Definition of Done) + §R7.5 (blockers).

**First units to build, in order:**
1. **`U-REV-CI-00`** (§R5.1 B5 / §R7.5 blocker #2) — CI non-stub gate, BEFORE any MS0 page is marked complete. **LANE CAVEAT:** touches `scripts/*.mjs` + `.github/workflows/ci.yml` = peer's dev-tools/infra lane → post to chat bus first; if the peer claims it, fall back to a revenue-lane unit (`U-REV-XPOST-01` cross-controller G-code transpiler page, or `U-REV-ARCHIVE-01` customer/program-history search page). Build spec: `scripts/check-engine-wired.mjs` (import+call+action-enum+Zod+round-trip-E2E per engine named in a unit), `scripts/audit-test-assertion-density.mjs` (flags `toBeDefined()`-only files), `scripts/audit-stub-engine-returns.mjs` (greps dispatched outputs for `{ok:false,stub:true}`/placeholders), `scripts/spans-config-matrix.mjs` (≥3 spanning configs per N-config domain), `expectNotStub(result)` test helper, `revenue-gates` job in `.github/workflows/ci.yml`. Also build `scripts/audit-doc-backflow.mjs` (§R8.5 U-DOCFLOW) + `scripts/audit-page-wiring.mjs` (§R8.2 U-WF-CI-00) + `scripts/check-engine-wired.mjs --ci` upgrade in the same cluster.
2. **`MS-CRITWIRE` SFC subset** (§R7.2 / round6/05-critwire.md) — wire 8 SFC engines (`MachineAwareSpeedFeed`, `ProvenSpeedFeedAggregator`, `ChatterStabilityLobe`, `CoolantOptimizationPhysics`, `SpindleTorqueGate` + 3 AI-variant) to `prism_calc`/`prism_safety`; surface 3 actions (`deflection_calculate`, `lathe_cost_optimize`, `material_resolve`); de-stub `mill_chatter_predict`/`mill_scientific_analyze` (`MillingForceEngine.ts`/`MillScientificPipelineEngine.ts` = 14-15-line stubs; real engine is `MillingPhysicsKernelEngine` 1,924ln); remove the `post_process` `?? {post_processed:true}` fallback in `camDispatcher.ts:~2199` (make it `callOrThrow`). Real 875-unwired list = `state/shared/UNWIRED-ENGINE-AUDIT-2026-05-07.json` (with `suggestedDispatcher`), NOT `BUILD_STATE.json` (25-engine sample). Also: `U-MONO-MAT-REPOINT` 1-line fix at `mcp-server/src/constants.ts:61` (PATHS.MATERIALS_DB → `extracted/materials_v9_complete/` not 3-file `data/materials/`) — gives SFC the 1,047-material vendor DB.
3. **`MS-PAY`** (§R7.2 / round6/03-pay.md) — `U-PAY-01` Stripe Checkout + verified webhook → tier flag (~70% exists; wire `routes/billing.ts /webhook` → `BillingEngine.handleWebhook` THEN `userStore.setPlan`; flip `STRIPE_TEST_MODE=false`); `U-PAY-02` fix the live-route sig verification — currently a *comment* not code [SECURITY: anyone can self-grant a paid tier; the real HMAC-SHA256 + 5-min replay + idempotency impl exists in `BillingEngine.handleWebhook`, just not called from the route]; `U-PAY-03` mount `requireTier` on SFC/MP/CAD-CAM routes; `U-PAY-04` refresh `user.plan` per-request; `U-PAY-05` Billing Portal route; `U-PAY-06` one `state/shared/pricing-config.json` + create `state/shared/feature-tiers.json` (referenced as canonical but DOESN'T EXIST; pricing currently hardcoded in 4 disagreeing places).
4. Then: MS0-EXTENSION pull units (§R5.1 B2/B3/B4) → MS-FRONTEND P0 pages → MS-INFRA P0 (licensing backend live, AuthEngine on Postgres — `mcp-server/src/db/schema.sql` has the unused PG schema; AuthEngine currently in-memory Maps) → MS-GTM P0 → ★ **Revenue Day 1** (~Week 6-8: free SFC + $39 Pro tier = first dollar) → Master Post line (gated on `U-LEGAL-13`, Hurco-WinMAX-first controller priority) → MS1 billing → MS-WIRE-BACKEND/FRONTEND batches (off critical path) → MS-TRAIN-DEEP (needs one CUDA GPU ≥16GB overnight for mistral-7B QLoRA; the detached-GPU runner needs a real HF/peft backend — the one stub all training fronts share) → MS-SFC-CALIBRATE → MS-CAM-MASTERY (Fusion add-in ships at Revenue Day 1) → MS-PILOT (real JM-Die 15-machine fleet, Haas TM-1P first) → ★ **MS-DESKTOP** (Electron capstone, ~Week 12-18).

**1 HARD BLOCKER (`U-LEGAL-13`):** alarm DBs reproduced verbatim from manufacturer service manuals (Fanuc B-61395E, Haas 96-0284, Mitsubishi IB-1501279, Siemens 840D, Okuma OSP-P300); post configs carry `// Source: HAAS_VF2_...iMachining_.cps` (derived from Autodesk Fusion/HSMWorks + SolidCAM). Master Post + the bridge "Post via PRISM" + the desktop build CANNOT SHIP until `U-LEGAL-03/04` re-derive from public manuals. SFC is close to clean (Kienzle/Taylor = textbook empirical facts). Engage IP counsel Week 0 — long-lead. Also a tension flagged for the user: the desktop app distributes code+data — conflicts with the `feedback_no_public_h_drive` "for now" rule.

**Compounding-gains artifact to build first (in my lane):** `scripts/revenue-day1-checklist.mjs` — re-runnable green/red board for the §R7.4 Definition-of-Done conditions. Also `scripts/reconcile-roadmap-vs-viz.mjs` (§R8.6, but co-design schema with peer first).

---

## CURRENT STATE (1-line)

REVENUE-ROADMAP v7.5 DONE & in git history (content in `2ddb3ec76`). 5 rounds of scrutiny + monolith survey + 2 launch-completeness gap-hunts + §R9 CAM-mastery, all folded. Verdict CONDITIONAL-GO. Roadmap-generation phase complete. Next: EXECUTION, starting with the Week-0 CI-gate cluster (coordinate lane with peer first).

---

## SESSION ACCOMPLISHMENTS (this session — roadmap v7.2 → v7.5)

- v7.2: re-dispatched 4 round-5 agents that didn't survive a restart → all 10 round-5 consensus agents done → aggregated → wrote `round5/00-v7.2-resolution.md` (§R5: 6 BLOCKER folds, 8 MAJOR folds, sequencing, cost/revenue tables) → merged v7.1+§R5 → committed `6a0852779` (72 files).
- v7.2 §R6: monolith survey of `extracted/` (91MB/895 files) + `extracted_modules/` (149MB/~1048 files) — the decomposed PRISM v8.89 986K-line HTML build; ~8-12% reachable as live MCP; ~1,350 orphaned `.js` modules = a SEPARATE pool from the 875 unwired TS engines. → wrote MS-MONOLITH-HARVEST (3 quick wins: `U-MONO-MAT-REPOINT`/`U-MONO-CATALOG-WIRE`/`U-MONO-ALGO-SURFACE` + the port backlog) + `knowledge/wiki/entities/prism-v8-89-monolith-extraction.md` + `reference_monolith_extraction.md` memory → committed `3b0fb5a08`.
- v7.3 §R7: 6-agent launch-completeness gap-hunt → 6 new milestones (MS-CRITWIRE ~20u, MS-PAY ~6u, MS-FRONTEND ~14u, MS-INFRA ~12u, MS-GTM ~16u, MS-LEGAL ~13u) + MS-DESKTOP ~18u (Electron, THE FINAL PHASE per user) + desktop distribution reconciliation (Tier A hosted licensing/funnel backend; Tier B PRISM Studio Electron — bundles MCP+React+Node, local 127.0.0.1, license-key validation reusing `PRISM_SUBSCRIPTION_SYSTEM.js`, code-signed, auto-update) + Revenue Day 1 DoD → committed `a4d43cca7`.
- v7.4 §R8: 4-agent wire/train/calibrate gap-hunt → MS-WIRE-BACKEND (~756 engines wired + ~932 monolith modules dispositioned; 28-category wiring rule matrix; `check-engine-wired.mjs --ci` gate), MS-WIRE-FRONTEND (~95 page-wirings; ~8 new route modules; ~12 new api-clients; `audit-page-wiring.mjs` gate), MS-TRAIN-DEEP (26u, 3 fronts: print→CNC-program, print→CAD/CAM, SFC-accuracy-calibration-loop), MS-SFC-CALIBRATE (24u: SFC = Stacked Bayesian Model Averaging over a regime-routed ensemble with a physics-prior backbone, calibrated against HSMAdvisor/G-Wizard/FSWizard/Sandvik-CoroPlus/Kennametal-NOVO/the-1,047-material-DBs/the-24,545-JM-Die-programs/~3,700-tribal-tips/ISO-3685; sampling = Tier-0 ~300 cells → Taguchi OA → LHS → adversarial, ~600-700 not ~400K), U-DOCFLOW (cross-cutting "update CLAUDE.md/skills/GSD/dev-protocols/Obsidian/system-viz/memories as you build" rule + `audit-doc-backflow.mjs`), MS-VIZ-ROADMAP-BIND (~10u SHARED-w/-peer; `roadmap-to-viz-nodes.mjs` + ghost-node schema + `reconcile-roadmap-vs-viz.mjs`) → committed `0b3f80dec` (+ 4 condensed round7 agent reports).
- v7.5 §R9: wrote `round8/00-v7.5-cam-mastery.md` (MS-CAM-MASTERY, §R9.0–§R9.7) → built `REVENUE-ROADMAP-v7.5.md` (2630 lines) via `node --input-type=module -e '...'` (the `node -e "..."` form fails on apostrophe escaping under bash double-quoting) → emitted `REVENUE-ROADMAP-v7.5.html` (28968 bytes) → content landed in git via `2ddb3ec76` (a peer's commit swept the staged files; `commit-ownership-guard` + multi-chat ref races prevented a clean own-commit, but disk == HEAD so the content is preserved).

---

## KEY ARTIFACTS

| Artifact | Path |
|----------|------|
| **v7.5 spec (FINAL)** | `state/shared/specs/REVENUE-ROADMAP-v7.5.md` |
| v7.5 HTML companion | `state/shared/specs/REVENUE-ROADMAP-v7.5.html` |
| §R9 MS-CAM-MASTERY | `state/shared/audit-findings/revenue-roadmap/round8/00-v7.5-cam-mastery.md` |
| §R8 wire/train/calibrate/docflow/viz | `state/shared/audit-findings/revenue-roadmap/round7/00-v7.4-wiring-training-closure.md` (+ `01-wire-backend`, `02-wire-frontend`, `03-train-deep`, `04-sfc-calibrate`) |
| §R7 launch-closure + desktop | `state/shared/audit-findings/revenue-roadmap/round6/00-v7.3-closure.md` (+ `01-frontend`..`06-gtm`) |
| §R5 round-5 consensus + §R6 monolith | `state/shared/audit-findings/revenue-roadmap/round5/00-v7.2-resolution.md` |
| earlier specs (kept) | `state/shared/specs/REVENUE-ROADMAP-v7.{1,2,3,4}.md` |
| monolith wiki entry | `knowledge/wiki/entities/prism-v8-89-monolith-extraction.md` |
| Round finding JSONs | `state/shared/audit-findings/revenue-roadmap/round{2,3,3-5,4,5}/` |
| Readiness scorer | `scripts/revenue-readiness-score.mjs --spec=<path>` |
| HTML emitter | `scripts/emit-revenue-roadmap-html.mjs --spec=<path> --out=<path>` |
| Round aggregator | `scripts/audit-round-aggregate.mjs --round N --scope revenue-roadmap` |
| Section merger | `scripts/merge-roadmap-sections.mjs --round N --version X` |

---

## CRITICAL CONTEXT (would be lost to compaction — NOT derivable from code)

1. **v7.5 footprint:** v7.1 ~215u/9 milestones → v7.5 adds §R7 (6 milestones + MS-DESKTOP ~99u) + §R8 (MS-WIRE-BACKEND ~756 engines + MS-WIRE-FRONTEND ~95 + MS-TRAIN-DEEP 26 + MS-SFC-CALIBRATE 24 + MS-VIZ-ROADMAP-BIND ~10) + §R9 (MS-CAM-MASTERY ~per-system pillars). The "215 units" number is now the pre-§R7 core; total is much larger but most of §R8 (wiring batches, training) is OFF the critical path.
2. **Revenue Day 1 DoD (§R7.4):** `U-LEGAL-13` ✓ + MS-INFRA P0 ✓ + MS-PAY ✓ + MS-CRITWIRE SFC subset + 6 mill stubs de-stubbed + `U-REV-CI-00` green ✓ + Master Post emits clean G-code for ≥2 controllers (Hurco WinMAX + Haas) on cleared data ✓ + MS-FRONTEND P0 pages ✓ + MS-GTM P0 ✓ + trial→paid KPI in MS0-close. ≈ Week 6-8 (SFC-led). Desktop GA ≈ Week 12-18.
3. **Revenue projection (round-5 a7):** Pessimistic $36K/$125K · **Realistic $359K/$1.64M** · Optimistic $1.06M/$5.54M (12mo/36mo ARR). **Master Post = biggest ARR-per-eng-week unlock — ship FIRST** (independent surface, backend ~70% built, 4-week MVP, Hurco-WinMAX-first). MS-PILOT = credibility lever (pessimistic→realistic). MS-TRAIN-DEEP = pricing-power lever (Omega 0.75→1.0). MS-CAM-MASTERY = the Fusion add-in's Speed&Feed + Auto-program is a Revenue-Day-1 differentiator (consume SFC inside the CAD/CAM the customer already uses).
4. **The legal BLOCKER `U-LEGAL-13` gates ALL paid-tier revenue** — see RESUME DIRECTIVE. Long-lead; engage counsel Week 0.
5. **The security hole `U-PAY-02`** — the live Stripe `/webhook` route's signature verification is a comment, not code; `StripeBillingEngine.handleWebhookEvent`'s `constructEvent` is a no-op in live mode. The verifying impl exists in `BillingEngine.handleWebhook` — just wire the route to it.
6. **Round-3.5's "5 net-new SFC engines, 14 days" was empirically FALSE** — all 5 exist as TS, 4 of 5 already wired (`GilbertEconomicSpeedEngine` 254ln is unwired). SFC's real critical path ≈ 5 days of wiring + 3 action surfaces + 60-cell material expansion + de-stubbing 6 mill actions. NOT MS2.
7. **The LoRA cluster:** ~80 LoRA engines (Lathe most complete; per-process cadence engines; cross-cutting `MachineLoRABaseEngine`/`LoRAAdapterRegistryEngine`/`LoRACompositionEngine`/`LoRAMoEGatingEngine`/`DetachedLoRARunnerEngine`/`CAMLoRAAdapterTrainerEngine`). **Every checkpoint = `status:"not_trained", samples:0`** — plumbing wired, nothing trained. Base = mistral-7B-v0.1, QLoRA, one CUDA GPU ≥16GB overnight. The detached-GPU runner needs a real HF/peft backend (the one stub all training fronts share).
8. **MS-CAM-MASTERY confirmed on disk:** `Fusion360FunctionIndexEngine`, `Fusion360CADFunctionIndexEngine`, `HyperMillFunctionIndexEngine`, `HyperCADCADFunctionIndexEngine`, `MastercamFunctionIndexEngine`, `MastercamCADFunctionIndexEngine`, `InventorHSMFunctionIndexEngine`, `InventorCADFunctionIndexEngine`, `InventorCAMFunctionIndexEngine`, `EspritFunctionIndexEngine`, `FusionDeepLearningEngine`, `MastercamDeepLearningEngine`, `CAMAddInFrameworkEngine`, `BatchCAMAddInGenerators`, `CAMLoRAAdapterTrainerEngine`, `CAMLoRAEngine`; **SolidWorks-CAM function-index may need building**. Dispatcher actions: `fusion360_function_index_*`, `inventor_hsm_function_index_*`, `esprit_function_index_*`, `mastercam_function_index_*`, `hypermill_function_index_*`, `cam_fusion_build_*`, `cam_mastercam_build_*`, `cam_hypermill_build_*`, `cam_inventor_hsm_build_*`, `mastercam_addin_generate`, `solidcam_addin_generate`, `nx_addin_generate`, `hypermill_addin_generate`, `powermill_addin_generate`, `catia_addin_generate`, `cam_addin_generate`.
8a. **AWARENESS BACKBONE** (source of truth, auto-injected, on disk): `state/shared/CLAUDE-BRIEF.md` (what we have — CAM tier-1 priority Fusion > hyperMILL > Mastercam > Esprit), `state/shared/PRISM-BUILD-CONTEXT.md` (what we're building), `state/shared/PRISM-BUILD-VISION.md` (max-value vision). Top gap: "Pillar telemetry rot". Build doctrine: before any new engine/script/hook — check those 3 + `duplicationGuardEngine.mustCheckBeforeCreating()`; new features not in the vision spec → add to `mcp-server/scripts/build-vision-spec.json` first.
9. **Lane discipline:** This chat (99eca613) owns customer-facing UI / billing / MS2 inventions / MS3 wiring / MS4 drift / Master Post / monolith harvest. Peer chat owns dev-tools / infra / AI-systems / neural / system-viz internals / hooks / wiki internals. **`U-REV-CI-00` + `MS-VIZ-ROADMAP-BIND` straddle the boundary — coordinate via chat bus before building** (`node H:/prism/.claude/helpers/agent-coordination.mjs post --agent claude-99eca613 --status proposing --current "..."`). **As of 2026-05-11 the peer RGS6/backend-devtools chat (`Claude@MARKV/pid-33776`) is ALREADY building the viz-binding surface** — `scripts/audit-roadmap-viz-bindings.mjs` (EXIT-FAILS=103), a `ROADMAP-VIZ-BINDING` unit, and a `doc_propagation` field on roadmap units (overlaps my §R8.6 MS-VIZ-ROADMAP-BIND + §R8.5 U-DOCFLOW). **Do NOT build a parallel binding script** — consume theirs; my contribution is the *revenue-roadmap unit envelopes* getting `viz_node_id` + `doc_propagation` fields, nothing more.
9a. **§R9 EXECUTION CONSTRAINT (from `claude-d9860be8` viz-obsidian-brain, chat bus 2026-05-11 09:37):** MS-CAM-MASTERY must EXTEND the existing wiki surface, not fork one. `knowledge/wiki/architecture/_leaf-index.jsonl` already has per-engine/action/skill entries for every `*FunctionIndexEngine` (Fusion360, HyperMill, Mastercam, InventorHSM, Esprit, …). Query it by system name; add the pillar-A/B/C/D/E coverage notes to those entries. Related infra already shipped (commits `ada1a171e`, `ac864aaf9`): `GRAPH-AS-LLM-CONTEXT-MS0` ~70%, U-GAC02 GraphRAG done, `build-wiki-embeddings` done, `generate-misc-l8-wiki` done (266 entries), `fingerprint-gate` done — the AI substrate for §R9 pillar E (Claude-orchestration via knowledge+memory) is mostly there.
10. **Multi-chat git hygiene:** stale `.git/index.lock` (rm before each op); peers commit on top → ref races (reset index, re-stage only mine with `git add -- <files>`, verify with `git diff --cached --name-only`); `commit-ownership-guard` auto-unstages peer-owned files; **`--no-verify` blast-dampener blocks after 3 fires in 10min — commit WITHOUT it**. If `commit-ownership-guard` mangles a commit repeatedly, fall back to the conflict-fork rule (`git worktree add ../prism-<scope> -b work/<scope>`). Handoff files are gitignored — don't commit them.
11. **Subagent gotchas:** `isolation:worktree` failed 9/10 in round 2 on Windows + 17k-file tree × 9-parallel. Read-only audits use NO isolation. Background async agents do NOT survive a session restart (re-dispatch). `ingestion-cache-root-guard` redirects some agent Writes to `mcp-server/data/ingestion_cache/<scope>/` — copy back before commit.
12. **Known cosmetic:** `revenue-readiness-score.mjs` reads v7_compliance flags A/D/G as `false` against v7.x — v7.x is the milestone-execution detail spec, not the original A-H meta-layer doc. Unit enumeration is the load-bearing output.

---

## OPEN TASKS

- ✅ Roadmap-generation phase (v7.2 → v7.3 → v7.4 → v7.5, all committed/in-history) — DONE
- 🔲 **EXECUTION: Week-0 CI-gate cluster** — `U-REV-CI-00` (4 audit scripts + `expectNotStub` helper + `revenue-gates` CI job) + `audit-doc-backflow.mjs` + `audit-page-wiring.mjs` + `check-engine-wired.mjs --ci`. COORDINATE LANE FIRST (peer's dev-tools/infra). Fallback: `U-REV-XPOST-01` or `U-REV-ARCHIVE-01` page.
- 🔲 EXECUTION: `MS-CRITWIRE` SFC subset (8 engines + 3 actions + de-stub 6 mill actions + `post_process` callOrThrow + `U-MONO-MAT-REPOINT` 1-line fix)
- 🔲 EXECUTION: `MS-PAY` (`U-PAY-01..06`)
- 🔲 EXECUTION: `scripts/revenue-day1-checklist.mjs` (my lane, compounding-gains)
- 🔲 EXECUTION: MS0-EXTENSION pull units → MS-FRONTEND P0 → MS-INFRA P0 → MS-GTM P0 → ★ Revenue Day 1 → Master Post line (gated on `U-LEGAL-13`) → MS1 → MS-WIRE batches → MS-TRAIN-DEEP → MS-SFC-CALIBRATE → MS-CAM-MASTERY → MS-PILOT → ★ MS-DESKTOP
- 🔲 (deferred, v7.2 §R5.5) 9 residual MAJORS — Weibull RUL incomplete-gamma, MATBAND citations, MS2-collapse cross-tab, frontend-merge owner, test-import gate, MIPSolver homing, LEARN trial→paid KPI, fix the 16 false-green tests once enumerated

---

## TOPIC

`cad-fusion-live-ms0` (matches commit-scope `[CAD-FUSION-LIVE-MS0]`)
