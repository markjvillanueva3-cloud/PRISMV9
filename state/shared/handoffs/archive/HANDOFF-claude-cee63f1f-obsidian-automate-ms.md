# HANDOFF: claude-cee63f1f
Updated: 2026-05-09T05:01:09.175Z
Family: Claude | Machine: DESKTOP-N7MI1VB | Session: claude-cee63f1f

## STATE
Hit 1M hard cap on session resume before any tool calls landed. Tasks 1+2 already complete pre-restart. Tasks 3-7 specced with literal edit anchors.

## RESUME
Execute 5 utilization-gap edits identified by Docker/Gemini/Codex audits. ALL FILE CONTENTS WERE READ PRE-COMPACT — do NOT re-read whole files (that blew context); use Edit/Write with the anchors below. Tasks 1+2 done (Codex MCP already in ~/.codex/config.toml [mcp_servers.prism]; Gemini CLI OAuth verified — refresh_token valid for maysoncarsonbryanna3@gmail.com). REMAINING 5 EDITS: (1) DOCKER SCOUT in H:/prism/.github/workflows/ci.yml — append new job 'docker-scout' AFTER security-audit job (ends ~line 279). Job: 'name: Docker Scout — base image CVE scan', runs-on ubuntu-latest, steps: checkout@v6, docker/login-action@v3 with secrets DOCKER_HUB_USERNAME+DOCKER_HUB_TOKEN and continue-on-error true, docker/scout-action@v1 with command cves on Dockerfile, sarif-file output, upload-sarif to Security tab. Whole job continue-on-error true so missing secrets don't break PR CI. (2) GEMINI LONG-CONTEXT ARM in H:/prism/.claude/scripts/scrutiny-3way.mjs: at line 83-85 change GEMINI_ARGS default from ['--no-install','gemini'] to ['--no-install','gemini','-m','gemini-2.5-pro']. After line 112 (end of MAX_DIFF_BYTES IIFE) add: const GEMINI_MAX_DIFF_BYTES = (() => { const env = process.env.PRISM_SCRUTINY_GEMINI_MAX_DIFF_BYTES; if (!env) return 800_000; const n = Number.parseInt(env, 10); return Number.isFinite(n) && n > 0 ? n : 800_000; })(); Modify captureDiff(target) at line 164 to accept second arg maxBytes (default MAX_DIFF_BYTES) and use it instead of constant. In dispatcher (later in file ~line 450 — find where captureDiff is called), call once for codex/ollama with default and separately for gemini with GEMINI_MAX_DIFF_BYTES. (3) AISystemRouter PROBE in H:/prism/mcp-server/src/engines/AISystemRouterEngine.ts lines 206-217 — current code only checks 'docker ps'. Replace probe block for docker-physics-agent and docker-batch-processor cases: after 'execSync("docker ps"...)' succeeds, run 'execSync("docker image inspect prism-physics-agent:latest")' (or prism-batch-processor) and on failure set reachable=false detail='daemon up but image prism-{x} not built'. Honest fallthrough to claude-opus. (4) DOCKERFILE SYNC GUARD — H:/prism/Dockerfile and H:/prism/mcp-server/Dockerfile are byte-identical (57 lines, multi-stage Node 22-alpine). Don't delete. Add line 1 to BOTH: '# CANONICAL: H:/prism/Dockerfile — mcp-server/Dockerfile must stay byte-identical (CI-enforced).' Then create H:/prism/.github/workflows/dockerfile-sync.yml: 'name: dockerfile-sync\non: [push, pull_request]\njobs:\n  cmp:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v6\n      - run: cmp Dockerfile mcp-server/Dockerfile'. (5) GEMINI BLUEPRINT TIER 3 — Create H:/prism/Docustrata/.index/phase8-tier3-gemini-vision.py (~150 lines). Imports: google.generativeai, fitz, PIL.Image, json, os, sys, pathlib.Path. Read GEMINI_API_KEY from env, exit gracefully with verdict 'skipped' if absent. Pattern from sibling phase8-tiered-blueprint-classifier.py: INDEX=Path(r'H:\PRISM\Docustrata\.index'); reads phase8-classified-pages.jsonl; filters pages where 0.3<=tier1.drawing_score<=0.6 OR (tier2.strong_indicators>=1 AND not is_drawing_likely); renders page at fitz.Matrix(2.4,2.4); sends to gemini-2.5-pro with prompt 'Is this a manufacturing engineering drawing? Return ONLY JSON: {is_drawing: bool, confidence: 0-1, part_number: str|null, materials: [str], notes: str}'; parses JSON response; appends to phase8-tier3-gemini-results.jsonl. main() takes sys.argv[1] as sample size (default 100). Resume-from-jsonl pattern same as parent. AFTER ALL 5: cd H:/prism/mcp-server && npx tsc --noEmit 2>&1 | grep -E 'AISystemRouter|scrutiny' must be empty. Commit '[MAIN] [OBSIDIAN-AUTOMATE-MS3]/U-UTILIZATION-GAPS: Docker Scout + Gemini long-ctx arm + router probe honesty + Dockerfile sync + Tier3 vision pilot'. Then return to original Fusion bridge work in HANDOFF-claude-cee63f1f-obsidian-automate-ms.md (4 PRISMBridge.py endpoints). Reading the handoff is fine; do NOT re-read whole large files.

## CONTEXT
I had read pre-compact: ci.yml 1-279 in full, both Dockerfiles in full (byte-identical 57-line copies), scrutiny-3way.mjs 1-400, AISystemRouterEngine.ts complete (283 lines), phase8-tiered-blueprint-classifier.py 1-200. Helper stable-session-id resolved to wrong peer ID claude-d9860be8 on first attempt — that handoff exists but is mine in disguise; this one is the canonical one for claude-cee63f1f. No peer chats currently claim my target files. Audit verdicts: Docker=fine-Scout-only-gap, Gemini=blueprint-OCR-biggest-win, Codex=well-utilized-only-MCP-gap-and-that-was-already-done. The 90-min Fusion bridge work (4 PRISMBridge.py endpoints) is STILL deferred per the prior handoff — finish utilization gaps first. The d9860be8 stale handoff should be deleted since it's mine but mis-attributed: rm state/shared/handoffs/HANDOFF-claude-d9860be8-obsidian-automate-ms.md after confirming peer claude-d9860be8 (the real one working on system-health/01-pagefile-relocate.ps1) doesn't write their own there.
