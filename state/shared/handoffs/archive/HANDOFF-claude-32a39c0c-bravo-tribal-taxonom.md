---
session: claude-32a39c0c
topic: bravo-tribal-taxonomy-graph
slot: foxtrot
written_at: 2026-05-16T00:35:26.671Z
machine: DESKTOP-N7MI1VB
family: Claude
session_key: claude-32a39c0c
status: active
---

# HANDOFF: claude-32a39c0c
Updated: 2026-05-16T00:35:26.671Z
Family: Claude | Machine: DESKTOP-N7MI1VB | Session: claude-32a39c0c

## STATE
Cluster lib + 50-case test written. 48/50 PASS, 2 FAIL (one known: schoolChain unknown-school fallback bug; one unknown — re-run tests with grep 'not ok' to identify). Magic numbers extracted to named consts (L1/L2 thresholds, topK rep, title items, DJB2 seed). Dead Z_uncategorized ternary removed. Doc-comments trimmed per Karpathy doctrine. Slot bravo, chat claude-41db1b82, topic bravo-tribal-taxonomy-graph.

## RESUME
RESUME: Fix 2 failing tests in scripts/lib/tribal-graph-clusters.test.mjs. Step 1: cd H:/prism && node --test scripts/lib/tribal-graph-clusters.test.mjs 2>&1 | grep -A3 'not ok' to see BOTH failures (only 1 visible pre-compact: schoolChain unknown-school test — fix is in scripts/lib/tribal-graph-clusters.mjs schoolChain function — replace 'L5:${schoolCode}' with 'const effective = SCHOOL_TAXONOMY[schoolCode] ? schoolCode : "Z_uncategorized"; return [`L5:${effective}`, ...]'). Step 2: re-run tests, must be 50/50. Step 3: dispatch 2 parallel scrutiny reviewers (per-file gate doctrine) on tribal-graph-clusters.{mjs,test.mjs} — agent A = test-review-agent, agent B = code-analyzer weighted on hostile-input safety + dead-code detection. Step 4: write scripts/lib/tribal-graph-embedding.mjs (Ollama nomic-embed-text 768d, batch with concurrency limit, checkpoint to disk every N items, fail-soft when Ollama unreachable). Step 5: write paired .test.mjs with mocked Ollama fetch. Step 6: dispatch reviewers on embedding files. Step 7: write scripts/tribal-graph-build.mjs orchestrator (reads TRIBAL_TIP_INDEX.json + CAM_TRIBAL_RAG_INDEX.json 5.5MB + per-domain JSONs + mcp-server/dist/data/*-tips compiled — NOT .ts source — normalizes via normalizeTip, dedupes, clusterByJaccard L0->L1, aggregateLevel L1->L2->L3, classifies L4 domain + L5 school + schoolChain L6/L7/L8, computes lateral wires via cosine > 0.7, emits state/shared/dashboards/TRIBAL-GRAPH-V1.{json,md} + per-node Obsidian markdown under knowledge/wiki/tribal-graph/L{0..8}/. Step 8: dispatch reviewers on builder. Step 9: run builder against real 11586-tip store, validate output samples. Step 10: 3-of-3 Stop scrutiny gate. THEN enter Phase 2 — open-ended /loop over H:/prism/resources/MIT COURSES (1645 files, 13 MIT class zips incl 1.060/10.34/16.410/16.852j/16.885j/2.003/2.14/2.43/6.046j/6.837/9.40) + RESOURCE PDFS (3916 files) + acquire OpenStax/MIT-OCW/Open-Textbook-Library/gov-PDFs/Internet-Archive/author-copies. SLOT: bravo (claude-41db1b82). USER SPEC HIGHLIGHTS: graph-of-graphs not orthogonal-grid; algorithm (C) Jaccard backbone + cosine wires confirmed; CRITICAL variability/inference layer required — each L0 atom has required_inputs/optional_inputs/output_variables for partial-input queries (extractSignature already in lib); 10 wire types (relates-to/prerequisite/instance-of/contradicts/extends/analogous-to/formula-of/safety-of/tribal-of/case-study-of). DUPLICATION GUARD checked — no existing tribal-graph script/engine (engines that exist: CAMTribal*/MillTribal*/LatheTribal*/PostProcessorTribal* per-domain, none unify; kg_schema is empty 0-nodes). Per-file scrutiny gate ACTIVE (CLAUDE.md doctrine): dispatch 2 reviewers between each file pair. End-of-task 3-of-3 still required at Stop.

## CONTEXT
User pivoted from 'multi-axis orthogonal taxonomy' to 'graph-of-graphs with lateral wires across solar-systems/galaxies' mid-session. Then added critical requirement: variability/inference layer for partial-user-inputs (formulas adjust + tribal usage adjusts when user lacks all SFC inputs). Algorithm choice (C) = Jaccard for aggregation backbone (predictable hierarchy) + Ollama nomic-embed-text cosine for lateral wires (semantic surprise connections). Slot 'kilo' attempted but chat-slots.mjs SILENTLY fell back to bravo (kilo not in SLOT_NAMES — only 7 slots defined: alpha-foxtrot+golf; 10-chat expansion via feedback_fleet_design_10_chats is designed not yet wired). prism_context:claim_file FAILED 7x with sessionId/pcName validation error — file claims for tribal-graph-* skipped; mitigated by all 7 paths being non-conflicting NEW files not in any peer's claim list (checked chat-bus). Tests use node:test (vitest broken per reference_ollama_cost_routing). Source-of-truth tribal data lives in mcp-server/src/data/*-tips.ts files (auto-ingested-tips/bobcad-cam-tips/etc) + JSON state stores (TRIBAL_TIP_INDEX 133KB hyperMILL extracted + CAM_TRIBAL_RAG_INDEX 5.5MB + per-domain). 11586 total tip count from tribal_stats. 200+ flat categories incl garbage 13-pipe-composites motivated user's 'categorize first' demand. Existing tribal_search returns rich schema (tk-001 sample). tribal_export_bulk requires output_dir param. kg_schema is empty/not-populated. PRISM has stochastic/uncertainty/Bayes machinery in prism_calc + prism_intelligence + prism_adaptive_control + prism_validate dispatchers — variability layer ROUTES to these, does not reimplement. NO new engine yet — script-first per Karpathy R3 (surgical changes). Promote to TribalGraphInferenceEngine.ts only after script proves taxonomy fits real data.
