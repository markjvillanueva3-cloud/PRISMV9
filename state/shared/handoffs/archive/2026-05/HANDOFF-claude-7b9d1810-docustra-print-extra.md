---
session: claude-7b9d1810
topic: docustra-print-extra
written_at: 2026-05-12T13:46:00.000Z
machine: MarkV
family: Claude
session_key: claude-7b9d1810
status: active
---

# HANDOFF: docustra-print-extra (lane handoff)
Updated: 2026-05-12T13:46Z  ·  Last live session: **claude-c47ec810** (slot **charlie**).  ·  Prior: claude-520e3809 (bravo).
(File named for the original `claude-7b9d1810` session — `stable-session-id.mjs` mis-resolves this chat, so update this file BY PATH, not via `per-agent-handoff.mjs read --terminal`.)

## ⚡ FIRST ACTION FOR THE NEW SESSION
Two things run in the background, both self-managed by 10-min scheduled tasks (`schtasks /query | findstr PRISM`), both resumable, both survive session-end:
1. **`jm-part-library`** (PID 23992 @ 13:43Z) — the part-library bulk materializer (see §PART LIBRARY). `node H:/prism/scripts/daemon-supervisor.mjs status jm-part-library` + `tail H:/prism/state/shared/daemons/jm-part-library.log` + `node H:/prism/scripts/daemon-supervisor.mjs list`. Done when `H:/PRISM/JM DIE/_PART LIBRARY/_BUILD_SUMMARY.md` exists (~1–1.5 h). Reviver: `"PRISM JM Part Library Reviver"`.
2. **`phase15-huge`** (PID 3812 @ 13:43Z, restarted) — the 779-deferred-big-container-PDF OCR pass. `node H:/prism/scripts/daemon-supervisor.mjs status phase15-huge` + `wc -l < H:/prism/Docustrata/.index/phase15-deep-rescan-parallel.jsonl` should climb (140,441 @ ~13:00Z). Done when `Docustrata/.index/phase15-huge-chunked-summary.md` exists (~13–15 h). Reviver: `"PRISM Docustra Phase15-Huge Reviver"`. **⚠ This daemon keeps dying every ~15–20 min** (a reaper or crash — root cause unconfirmed) and the reviver has a **PID-reuse hole**: `daemon-supervisor`'s liveness check is `process.kill(pid,0)`, which is fooled if Windows recycles the old PID to an unrelated process — then the reviver thinks it's still alive and doesn't restart it. If you find it `alive:false` and the jsonl frozen, manually: `node H:/prism/scripts/daemon-supervisor.mjs stop phase15-huge` then `... start phase15-huge -- "H:/Tools/python/python.exe" H:/prism/Docustrata/.index/phase15-deep-rescan-huge-chunked.py 0 1`. (Hardening the reviver to confirm "a python running THIS script exists" before deciding is a TODO.)

Then `node .claude/helpers/chat-slots.mjs claim --chatId "claude-<8hex>" --branch "$(git -C H:/prism rev-parse --abbrev-ref HEAD)" --topic docustra-print-extra` for a fleet slot.

## ⚡ PART LIBRARY — primary deliverable this session (claude-c47ec810, slot charlie, 2026-05-12)
User asked: "match prints to programs / cad models / cad-cam programs; start a new folder in the JM die folder; organize by customer folder → part number folder; within that folder = the print + all related docs, then a folder for the cnc program and a folder for the cad/cam file; this is the template for all incoming orders/jobs/parts." User chose **copy everything in** (not manifest/hardlink) and **every non-garbage print PN (~24,768)**.

**Built (all type-checks clean against the project — the 1356 pre-existing tsc errors are NOT mine; my 3 files add 0):**
- `mcp-server/src/engines/PartFolderOrganizerEngine.ts` — the on-demand interface. `createPartFolder({partNumber, customer?, prints[], cncPrograms[], cadCam[], programs[], copyMode, overwrite, ...})` → creates `<root>/<CUSTOMER>/<PART NUMBER>/{print+docs at root, part.json manifest, CNC PROGRAM/, CAD-CAM/}`; idempotent (skips a complete folder); PN+customer sanitization + path-traversal guard; PDF page isolation via fitz (whole-PDF copy fallback if fitz unavailable); also `getPartFolder`, `partLibraryStats`, `populateFromJoinTable` (small-default-limit in-engine bulk path). Static class + `partFolderOrganizerEngine` singleton.
- Wired to **`prism_cad`** (NOT `prism_dev` — that's peer-owned): actions `create_part_folder` / `get_part_folder` / `part_library_stats` / `part_library_populate` — added to `ACTIONS` enum + switch cases in `cadDispatcher.ts` + zod schemas in `cadActionSchemas.ts` (`createPartFolderSchema` etc. + registered in `ACTION_CAD_SCHEMAS`). Dispatcher case maps snake↔camel; engine also tolerates both + filters junk refs.
- `mcp-server/src/__tests__/PartFolderOrganizerEngine.test.ts` — **44 tests, all pass**: sanitization (incl. traversal/NUL/oversize/reserved-names), classifyFile, customer resolution, happy path (prints+CNC+CAD), spanning configs (prints-only / prints+CNC / manifest-mode), failure modes (missing source / illegal chars / empty PN / traversal / missing PDF), idempotency/overwrite, getPartFolder, partLibraryStats, populateFromJoinTable (dry-run + real + limit/offset + missing-table), PDF page extraction, **+ a real round-trip through the prism_cad dispatcher handler** (captures the registered handler, invokes `create_part_folder`/`get_part_folder`/`part_library_stats`/`part_library_populate`).
- `mcp-server/data/state/part-library-layout.json` — single source of truth for the layout (libraryRoot, subdir names `CNC PROGRAM`/`CAD-CAM`, sanitize rules, file-classification ext lists, customer-resolution priority+reject-patterns, manifest field list). Read by BOTH the TS engine and the python backfill.
- `Docustrata/.index/phase18-build-part-library.py` — the **bulk backfill** (fitz in-process, no subprocess churn → ~1–1.5 h for all 25 028 eligible rows vs ~4 h for the TS path). Reads `blueprint-program-join-full-v5.jsonl` + `phase7-drawing-candidates.jsonl`; per non-garbage row: resolve customer (program-path > print-OCR > `_UNASSIGNED`), mkdir folder + subdirs, extract each drawing page → single-page PDF in the folder root, copy programs → `CNC PROGRAM/` (kind3==nc_program) or `CAD-CAM/` (else), write `part.json`. Resumable (skips complete `part.json`); disk-guarded (`sys.exit(2)` if H: < 20 GB, does NOT write the summary so it's seen as not-done); writes `_PART LIBRARY/_BUILD_SUMMARY.md` on clean completion. CLI `python phase18-build-part-library.py [LIMIT] [DRY_RUN]` (`0 0` = all, real).
- `H:/PRISM/JM DIE/_PART LIBRARY/README.md` (the convention doc — folder layout, manifest schema, how new orders get a folder via `prism_cad:create_part_folder` or by hand, how it was built, v1 limitations) + `_PART LIBRARY/_TEMPLATE/PART-NUMBER-HERE/{part.template.json, _PUT THE PRINT...txt, CNC PROGRAM/.txt, CAD-CAM/.txt}` (copy-rename-fill skeleton).
- esbuild built (`npm run build:fast`, 4.2 s) — `PartFolderOrganizerEngine` + `create_part_folder` are in `dist/index.js`. (Restart the MCP server to pick it up.)
- **Bulk run LAUNCHED** as the `jm-part-library` daemon @ 13:43Z (see FIRST ACTION). When `_BUILD_SUMMARY.md` exists: `prism_cad:part_library_stats {by_customer:true, with_disk:true}` for the count; spot-check a few `<CUSTOMER>/<PN>/part.json`; show the user the summary. Then `schtasks /delete /tn "PRISM JM Part Library Reviver" /f` + delete `H:/prism/scripts/jm-part-library-reviver.cmd`. Re-run `phase18-build-part-library.py` after the phase15 huge pass + a fresh phase16 re-join to fold in the new prints (resumable). The whole structure RE-POPULATES then — current build uses the 129K-row-era join data.

**Caveats surfaced to the user:** prints = drawing-likely pages only (non-drawing related docs like POs/routers not yet keyed by PN — needs a full-PDF OCR pass); match confidence varies (`exact`≈filename-token match, ~half internal-corroborated; `loose` weaker; `ambiguous`; `miss`=print only); `.ipt` files may be part model OR toolpath doc (can't tell without opening).

**BULK BUILD DONE** (`phase18` finished 09:13 local / ~14:13Z, 26.2 min): 25,028 part folders, 42,407 print pages copied, 10,678 program/CAD files copied. `_PART LIBRARY/_BUILD_SUMMARY.md` written. The `jm-part-library` daemon exited cleanly → delete its reviver task + .cmd (`schtasks /delete /tn "PRISM JM Part Library Reviver" /f` + rm `H:/prism/scripts/jm-part-library-reviver.cmd`).

## ⚡ CUSTOMER CONSOLIDATION — phase19 (2026-05-12, after the bulk build)
User: "the part library customer folders have lots of duplicates for the same company (Birmingham, etc.); consolidate them; do deep research on the real company names." Found ~1,024 customer folders, but most are OCR garbage (form labels `PURCHASE ORDER`/`SHIP TO`/`PART NUMBER`, title-block fields `DRAWN DATE`→OCR'd `BRAWN DATE`/`RAW DATE`, hardness specs `HRA845MIN…`, die-detail line names `…INSERT…`/`…PUNCH…`, address fragments `299 BEELINE DR`/`4300 S RACINE AVE`/`CITY OF INDUSTRY`, part numbers `B-0182`…) — only ~35 are real companies with multiple OCR-garble variants.
- **`mcp-server/data/state/part-library-layout.json` `customerResolution`** now carries: `aliases` (35 canonical companies ← 241 variant names — Birmingham Fastener, Allfast Fastening Systems [HQ City of Industry CA — confirmed by web search], Agrati [Park Forest IL + Medina OH plants folded into one], Fastenal, Holo-Krome [incl. the HRA*/RC*/HARDNESS*HOLOKR spec-line variants], ITW Shakeproof / ITW, Precision Form, Multitec Industries, SFS Group USA, Barnes Industries, Grandeur Fasteners [note "GRANDUER" was the OCR typo], Accurate Threaded Fasteners, Wrentham Tool, Fontana Fasteners [+ LEP brand], Forgo Fasteners, OMG Inc, Hi-Performance Fastening Systems, Acument Global Technologies, Howmet Aerospace, Arconic, Alcoa, JM Die, Midwest Fabricating, TFI Aerospace, Jebco, Crescent Manufacturing, Reed & Prince, Lake Erie Screw, Mead Industries, Topura America, Continental Midland, Valley Fastener Group, Brainard Rivet, Clendenin Brothers); `noisePrefixes` (233 form-label/title-block/die-detail/address prefixes); `noiseRegexes` (17, incl. part-number patterns; the `^[A-Z]{5,}$` all-consonant one is only used with `--aggressive`).
- **`Docustrata/.index/phase19-consolidate-customers.py`** — the one-shot consolidator. Reads the layout config; for each customer folder, `canonicalize()` (strip leading digits → alias lookup → noise check) → if it changes, MOVES the part folders into the canonical folder (on a same-PN collision the incoming one is renamed `<PN>__from__<oldfolder>` — none happened, the join table dedupes by PN), updates each moved `part.json`'s `customer`/`customerSource`/`notes`/`lastUpdatedAt`, removes the emptied folder; writes `_PART LIBRARY/_CONSOLIDATION_LOG.md`. Idempotent + `--dry-run`/`--force`/`--aggressive`. **RAN it:** 1,024 → **511 customer folders** (526 garble folders folded away), 7,784 part folders moved to canonical companies, 4,283 noise parts routed to `_UNASSIGNED` (now 18,124 there), 0 collisions. Top: Allfast 929 · Agrati 666 · Fastenal 360 · Holo-Krome 273 · Fontana 195 · OMG 106 · Precision Form 104 · Grandeur 104 · Multitec 91 · JM Die 90 · Barnes 89 · Accurate Threaded 88 · SFS 69 · Midwest Fabricating 56 · Valley 51 · Wrentham 46 · ITW Shakeproof 36 · Hi-Performance 35 · Birmingham Fastener 31 · …
- **`phase18-build-part-library.py` `resolve_customer` is now alias-aware** (`canonicalize_customer()` added) — so a re-run of phase18 (after the huge pass + re-join) lands parts on the canonical names directly; phase19 stays useful for re-applying after the alias map grows.
- **FOLLOW-UPS:** (1) wire `PartFolderOrganizerEngine.resolveCustomer` (TS) — and the explicit-`customer` path in `createPartFolder` — to apply the same `aliases`/`noisePrefixes` from the layout config, then `npm run build:fast` (so `prism_cad:create_part_folder` from new orders also canonicalizes). (2) The ~476 long-tail customer folders still un-consolidated are mostly gibberish + a few real single-folder customers (STALCOP, OPTIMAS, ARCHER, COBRA, CHERRY, GESIPA, EJOT, SEMBLEX, ATF, HOLLY OPERATIONS, FALL RIVER, SIG SAUER, FIOCCHI, HORNADY, CHOCTAW DEFENSE, AIR INDUSTRIES, AJ MANUFACTURING, ANDERSON MFG, MACLEAN-FOGG, WHITESELL, SPS TECHNOLOGIES, UNITED STEEL, etc.) — add more `aliases`/noise entries + re-run phase19. (3) The deeper issue: **18,124 / 25,028 parts are `_UNASSIGNED`** — that's the OCR + join data quality (a `miss` part with `print_customers:[]`, or a print that's a PO whose "customer" line is an address). The real fix is upstream in the OCR customer-extraction + the join's customer logic, not the folder layer.

**Uncommitted (disk only):** the engine/dispatcher/schemas/test (3 source files + 1 test), `mcp-server/data/state/part-library-layout.json`, `Docustrata/.index/phase18-build-part-library.py`, `H:/PRISM/JM DIE/_PART LIBRARY/README.md` + `_TEMPLATE/*`, `H:/prism/scripts/{phase15-huge-reviver.cmd,jm-part-library-reviver.cmd}`, `dist/*` (esbuild output). Plus all the earlier docustra `.py` scripts. Committing in the shared `H:/prism` tree fights `commit-ownership-guard` → fork a worktree if you want them committed. **Scrutiny gate:** session has a substantial uncommitted diff; run `node .claude/scripts/scrutiny-3way.mjs --session-id <id>` + the Opus reviewer agent + `--mark-opus` per CLAUDE.md §SCRUTINY GATE before declaring done (or it auto-passes after 3 block attempts).

## ⚡ NEW REQUEST (2026-05-12, late session — ENUMERATED, NOT BUILT — needs a fresh full-budget session)
User wants: (1) `H:/PRISM/JM DIE/Macro programs/` (NEW folder, 4 Okuma-OSP `VC`-variable lathe macros: `BASE WAFER INSERT MACRO.min`=O1001, `BASIC-CASING.MIN`, `BASIC CASING WITH SINGLE COUNTERBORE.min`, `BASIC TOP HAT CASING WITH SINGLE COUNTERBORE.min` — wafer-insert / casing / top-hat-casing part families) → fan the matching macro into every `_PART LIBRARY/<...>/` part folder that's one of those families; for each such part WITH an existing program, generate a NEW version that calls the macro (fill `VC1xx` vars from print dims). (2) Make a program **per lathe machine**, labelled per machine, codes matching that machine's controller + envelope capabilities. (3) Use the FULL PRISM stack — deep-learning, deep-reasoning, all engines/formulas/algorithms (invent new for max variability if needed, via `prismCreativeReasoningEngine`/`CrossDisciplinaryDeepLearningEngine` — but cite + register, never inline), speed/feed, physics engines, tooling, `/system-viz`. (4) Make ONE comprehensive slash command per machining domain (`/lathe`, `/mill`, `/wedm`) combining all engines + skills + tribal + AI + NN paths + system-viz. (5) "If we need to generate a forge slash-command run and rgs pipeline, do so."
- **DONE this session:** the system-viz enumeration → `state/shared/specs/DOMAIN-STUDIO-NODE-MAP.md` — the source-of-truth list of which system-viz nodes / engine clusters (`eng.lathe` ~89, `eng.mill` ~240, `eng.wedm` 62) / Tier-3 AGIs (`LatheAGIOrchestrator`, `MillingAGIMasterEngine`, `WEDMAGIOrchestrator`) / dispatchers (`prism_turning`+`turningProgramDispatcher`, `prism_mill`+`holePatternDispatcher`, `prism_edm`) / cross-domain physics+speedfeed+tooling+post+safety+AI+NN+tribal nodes each `/lathe` `/mill` `/wedm` command must wire in, + the build order.
- **NOT done (next session, full budget):** the 3 mega-commands; the macro-library engine + safety-gated `MacroProgramFillEngine` + per-machine post-resolution + the bulk fan-out; the `/forge`+`/rgs` scoping run for the program-generation pipeline. **⚠ The program-generation half is SAFETY-CRITICAL** (auto-emitting G-code from OCR'd print dims → wrong VC var = scrapped part / spindle crash) — must go through `S(x) ≥ 0.70` + sim/back-plot + operator-in-the-loop, NOT freehand bulk emit. That's why it needs `/rgs` → a milestone with the gates built in. Recommended order: (B) `/forge`+`/rgs` scoping run first → (A) the 3 mega-commands → (C) per-machine program gen only after the safety pipeline exists. Started: NOTHING beyond the node-map spec — checked in with the user for the green light + which piece first.

## STATE
Docustra Print Extraction — **Phase-15 deep-rescan daemon DONE; huge-chunked pass RUNNING and now bulletproof (self-reviving scheduled task).** Remaining: finish huge pass (~7–13 h) → re-run join pipeline → ingest (needs MCP) → final report + hand the spot-check sheet to the user.
- **`phase15-memsafe` daemon: DONE.** Kept HANGING on pathological PDFs (mupdf/tesseract spinning in C — the per-page try/except can't catch a C-level hang): 4w/1.8/30 → 2w/1.4/20 → 1w/1.2/12 all eventually froze (last at `[25/4803]` ~82 min). **FIXED: a hard per-doc timeout** in `phase15-deep-rescan-parallel-memsafe.py` — the 1-worker driver runs each doc via `pool.apply_async(...).get(timeout=PER_DOC_TIMEOUT)` (=120 s); on `MPTimeoutError` it writes a sentinel `{"doc_id":…,"page_index":-1,"error":"timeout: …auto-skipped"}` (resume won't retry it), `pool.terminate()`+`join()`s the stuck worker, respawns `Pool(1, maxtasksperchild=25)`, continues. Final run processed all 4,772 pending docs in ~145 min, **0 timeouts, 0 OOM**. jsonl was **129,352 rows** at memsafe-done.
- **`phase15-huge` pass: RUNNING (PID 2588 since 12:30Z, orphan-surviving).** `phase15-deep-rescan-huge-chunked.py` hardened — RENDER_SCALE 1.8→1.2, `PER_DOC_TIMEOUT=180 s` + the same apply_async-with-timeout-and-respawn driver, `maxtasksperchild=10`, CHUNK_SIZE 10. Processing the **779 huge docs** (>12..>30-candidate-page container PDFs in `phase15-skipped-huge.jsonl` — the multi-print packets). **These are very print-rich:** the first 240 huge docs yielded **1,115 drawing-likely pages** (~4.6/doc vs ~2.3% for normal docs), `pn=4948`, `cust=3021` — jsonl jumped 129,352 → 139,708 by doc 240; **140,006 @ 12:43Z**, climbing. **537 huge docs pending** (242 done; restart resumes by skipping doc_ids in OUT). These are SLOW — >78 s/doc observed (many chunked pages each), so real ETA ~7–13 h. Appends to `phase15-deep-rescan-parallel.jsonl`. 0 timeouts/OOM so far.
- **🔒 Self-reviving scheduled task (NEW 2026-05-12 12:45Z, claude-c47ec810):** `schtasks /query /tn "PRISM Docustra Phase15-Huge Reviver"` — fires every 10 min, runs `H:/prism/scripts/phase15-huge-reviver.cmd` which: `if exist phase15-huge-chunked-summary.md → exit` else `daemon-supervisor start phase15-huge -- python …huge-chunked.py 0 1` (idempotent: no-op if PID alive; resumes from OUT if dead). Survives session-end AND reboot. **DELETE both the task and the .cmd once the join pipeline has been re-run on the final jsonl** (`schtasks /delete /tn "PRISM Docustra Phase15-Huge Reviver" /f` + rm the .cmd). Manual restart still works too: `node H:/prism/scripts/daemon-supervisor.mjs start phase15-huge -- "H:/Tools/python/python.exe" H:/prism/Docustrata/.index/phase15-deep-rescan-huge-chunked.py 0 1` (**always 1 worker** — timeout path only activates for n_workers==1, and 1 worker is the only config that doesn't thrash this box). memsafe restart (if ever needed): same with `phase15-deep-rescan-parallel-memsafe.py`.
- Live check: `wc -l < H:/prism/Docustrata/.index/phase15-deep-rescan-parallel.jsonl` + `grep -oE '\[[0-9]+/[0-9]+\] pages=[0-9]+ draw=[0-9]+ pn=[0-9]+ .*ETA=[0-9]+min' H:/prism/state/shared/daemons/phase15-huge.log | tail -3` + `node H:/prism/scripts/daemon-supervisor.mjs status phase15-huge`. `grep -c 'error.*timeout: process' …jsonl` = docs auto-skipped on timeout (a few is fine). Note: the huge run prints `[n/537]` (it re-derives PENDING fresh on restart), not `[n/779]`.
- Watcher: background bash poll-loop. Pattern (re-arm if none active & you'll be in-session a while): per poll `ping -n 181 127.0.0.1` (~3 min) → DONE if `phase15-huge-chunked-summary.md` exists; STALLED if jsonl line-count frozen ~12 polls (~36 min) while alive (C-level hang — the 10-min reviver handles a *dead* daemon, but a *hung* one needs a kill so the reviver can respawn it). The `ping -n` sleep DOES work in the bg context. Watchers this session: …b8lh1prub (memsafe-done), brdg1bqy2/bqrajsffs (prior, died w/ their sessions), **brs8ezm3t** (current, armed 12:46Z, 4 h budget — dies w/ this session but the scheduled task is the real safety net now).
- Join pipeline last re-run this session on the **129,352-row** jsonl (results below) — **MUST re-run after the huge pass finishes** (it'll have added ~10K+ more rows / hundreds more drawing pages). Nothing committed (python scripts in `Docustrata/.index/` untracked; `mcp-server/data/state/jm-die-full-program-index-v2.json` untracked; the new `scripts/phase15-huge-reviver.cmd` untracked — all on disk only).

## RESUME
**0. (FIRST ACTION above — confirm daemon alive + jsonl climbing, claim a fleet slot; optionally re-arm a watcher. No restart needed unless `status` says `alive:false` AND no summary AND the scheduled task somehow isn't reviving it — then run the manual restart cmd above.)**

**1. WHEN THE HUGE PASS FINISHES** (`Docustrata/.index/phase15-huge-chunked-summary.md` exists — the scheduled task auto-stops at that point): re-run the join pipeline against the FULL jsonl, in order — `cd H:/prism/Docustrata/.index && "H:/Tools/python/python.exe" phase16-blueprint-program-join-v5.py && "H:/Tools/python/python.exe" phase16b-miss-analysis.py && "H:/Tools/python/python.exe" phase16c-validate-matches.py && "H:/Tools/python/python.exe" phase16d-spot-check-sheet.py`. Then **(1b)** clean up the daemon scaffolding: `schtasks /delete /tn "PRISM Docustra Phase15-Huge Reviver" /f` + delete `H:/prism/scripts/phase15-huge-reviver.cmd`. Then **(2)** the ingest — `prism_dev:blueprint_ingest_phase15 {jsonl_path:'H:/prism/Docustrata/.index/phase15-deep-rescan-parallel.jsonl'}` (NEEDS a session with the PRISM MCP server connected — the Claude Code sessions this lane has run in do NOT have the `prism_*` dispatcher tools; if no MCP, the engine is `BlueprintOCREngine.ingestPhase15JSONL` in `mcp-server/dist/` — could be driven via a `node -e` against the built dist, but easiest is just to run from the MCP-connected session). Then **(3)** report final yield to the user: drawing-likely pages, distinct prints, PN coverage, customer attribution, match counts (Print→CNC corpus target 228 → ~5,400+ verified prints). Then **(4)** hand `phase16d-spot-check-sheet.md` to the user to mark ✓/✗/? — first human ground-truth set (task #9).

**2. Task 8 — `BlueprintProgramJoinEngine` reader/server refactor — BLOCKED on user approval** (brainstorming-skill HARD GATE). Design posted to the user, awaiting their OK before writing the spec to `state/shared/specs/`. The design: python `phase16-…-v5.py` stays the batch *producer*; the TS engine becomes the *query layer* over `blueprint-program-join-full-v5.jsonl` (methods: `loadJoinTable`/`getProgramsForPart`/`getPartsForProgram`/`getPrintsForCustomer`/`joinStats`/`searchParts`; lazy-load+cache the join jsonl + `jm-die-full-program-index-v2.json`; mtime invalidation); `prism_dev:print_program_join` gains `mode:"serve"`; new `BlueprintProgramJoinEngine.serve.test.ts`; OUT of scope = no re-implementing the matcher in TS. When the user approves → write spec → user reviews spec → invoke `superpowers:writing-plans` → implement (full test floor: happy + ≥3 failure modes + ≥2 adversarial + dispatcher round-trip).

**3. Maybe-commit:** the `Docustrata/.index/*.py` pipeline (`phase15-*`, `phase16-v5`, `phase16b/c/d`, `phase17-*`) + `mcp-server/data/state/jm-die-full-program-index-v2.json` are on disk, uncommitted. Committing in the main `H:/prism` tree fights `commit-ownership-guard` (7,000+ other-chat changes) — fork a worktree (`git worktree add ../prism-docustra -b work/docustra-print-extra`) if you want them committed.

## CURRENT JOIN RESULTS (re-run on the 129,352-row jsonl this session — pre-huge-pass)
- `blueprint-program-join-full-v5.jsonl`: **29,532 normalized print PNs** → exact **903**, loose **1,185**, ambiguous 178, garbage 4,504, miss 22,762. Matched (exact+loose) **2,088 (7.1%)**.
- Trustworthy core: of 903 exact, **~427 are internal-name-corroborated** (phase16c sample: 50% of exact tier; loose tier only 4%). The other ~476 exact matched a filename token only — mix of real-but-not-written-inside and coincidental.
- Customer cross-check (after the `program_customer_from_path()` fix — real customer from `…\CUSTOMERS\X\` path, not the USB-dump folder name): **213 customer-corroborated**, 19 ambiguous-blobs narrowed (was 72/15 before the fix → ~3×). 11,569 records OCR'd a usable print customer; many `customer_match=no` are *truthful* (these are POs/packets where the OCR'd "customer" is a street address / requisitioner, not the company).
- Miss bucket (`phase16b`): ~27% likely-real-customer-PN-JM-never-machined / ~42% ambiguous / ~31% OCR noise — so the *plausibly-real* match rate is ~10–16%, not 7%.
- CAD triage (`phase17`, `jm-die-full-program-index-v2.json`): kind3 = cam_project 15,401 · nc_program 17,078 · drawing_2d 1,966 · geometry_model 341; `has_cad_model` = 15,742 files carry 3D geometry. `.ipt` part-vs-toolpath split needs the files opened (separate job — noted in `phase17-type-summary.md`).
- New artifact `phase16d-spot-check-sheet.md` (38 KB): 105 sampled matches across 5 strata (40 exact+internal-corroborated, 20 exact+filename-only, 20 loose, 19 customer-rescued, 10 ambiguous), each with the source-PDF path+page + matched-program `source_path` + a `✓/✗/?` column + a tally table. The human-validation seed — phase16c only has the structural proxy.

## CONTEXT — pipeline scripts (all in `H:/prism/Docustrata/.index/`, none committed)
- `phase16-blueprint-program-join-v5.py` — the v5 join. Normalizes print PNs (strip L-/M-/G- op-prefix, -OPn, material codes, trailing rev letter — loop to fixed point), indexes JM program filenames by *delimited* digit-runs ≥3 + `<d>-<d>` compounds only (NOT bare `digits_only` — that caused `1225`↔`A12EWUP-25T` false positives), + an internal-name index (`program-internal-names.json`, renamed-file recovery), + `program_customer_from_path()` (anchor-segment rule: dir after `CUSTOMERS`/`CNC LATHE`/`WIRE EDM`/… ; else deepest non-dump segment). Confidences: miss / garbage (date/phone/year/short-numeric/range regexes) / ambiguous (>25 files or >8 non-exact) / exact / loose; **bare ≤4-digit PNs can never be "exact"**. Customer cross-check narrows would-be-ambiguous blobs to the customer-corroborated set when ≤8. Reads `jm-die-full-program-index-v2.json`. Writes `blueprint-program-join-full-v5.jsonl` + `phase16-v5-summary.md`.
- `phase16b-miss-analysis.py` — buckets the miss pile by regex → likely-real / ambiguous / OCR-noise. Writes `phase16b-miss-analysis.md`.
- `phase16c-validate-matches.py` — structural precision proxy: a match is "internal-corroborated" if the PN (or a raw OCR variant) appears INSIDE the program file. Samples 400/tier, seed 42. Writes `phase16c-validation-summary.md`. (Ignore the `multipn_page` / "any-corroboration 87%" columns — `multipn_page` fires for ~every record; the internal-name % is the real metric.)
- `phase16d-spot-check-sheet.py` — generates the human spot-check sheet (above). Reads v5 jsonl + `program-internal-names.json` + `phase7-drawing-candidates.jsonl` (for doc_id→PDF path). Seed 42 (stable).
- `phase17-type-program-index.py` — types `mcp-server/data/state/jm-die-full-program-index.json` → `…-v2.json` (adds `kind3`/`relation`/`kind3_confidence`/`has_cad_model`) + `phase17-type-summary.md`.
- `phase15-deep-rescan-parallel-memsafe.py` (hardened) — RENDER_SCALE 1.2, MAX_CAND_PAGES 12, PER_DOC_TIMEOUT 120; 1-worker driver = apply_async-with-timeout-and-respawn (n_workers>1 keeps the old `imap_unordered` path, no timeout — but always run 1 worker); self-sets OPENBLAS/OMP/MKL threads=1; skip-set = doc_ids in OUT jsonl + P15_PRIOR + p8_ids; >MAX_CAND_PAGES cand pages → `phase15-skipped-huge.jsonl`.
- `phase15-deep-rescan-huge-chunked.py` (hardened) — same shape, RENDER_SCALE 1.2, CHUNK_SIZE 10 (close+reopen fitz every 10 cand pages), PER_DOC_TIMEOUT 180, `maxtasksperchild=10`; reads `phase15-skipped-huge.jsonl` (backfills candidate_pages from `phase7-drawing-candidates.jsonl`), resumes by skipping doc_ids in OUT.

PRISM side (the Task-8 targets, untouched): `mcp-server/src/engines/BlueprintProgramJoinEngine.ts` (static-method class + `blueprintProgramJoinEngine` singleton; currently joins against the 100-file `program-labels.json` stub + sparse `master-index.json`; has `normalizePartNumber`, `extractPartNumberCandidates`, `joinBlueprintsToPrograms`), `mcp-server/src/engines/BlueprintOCREngine.ts` (`ingestPhase15JSONL` → byPartNumber/byCustomer), dispatcher `mcp-server/src/tools/dispatchers/devDispatcher.ts` actions `blueprint_ingest_phase15` + `print_program_join` (both take `jsonl_path`; `print_program_join` accepts a `program_labels_path` override). devDispatcher.ts is also touched by @charlie/claude-58e6d5d4 + @alpha's SKILLS-UTILIZATION work — coordinate before editing it.

Self-grade this session: ~A− → A. Daemon now bulletproof (per-doc timeout = robust against all pathological PDFs, not whack-a-mole); customer cross-check ~3×; human-validation seed produced; honest about the no-ground-truth and .ipt-split caveats. Big token spend on the daemon-wrangling, though.
