# HANDOFF — claude-15ba78da — xproc-neural-connect-ms0

**Generated:** 2026-05-11 (post CN09 + CN10 + earlier HARNESS-AUDIT pass) — written by live chat at /precompact (per-agent-handoff helper was hung on Cygwin fork exhaustion, so written directly via Write)
**Branch:** cad-fusion-live-ms0 (main tree, `H:/prism`)
**Source:** live-chat

---

## RESUME DIRECTIVE (single most important field)

**Continue XPROC-NEURAL-CONNECT-MS0.** CN09 + CN10 are DONE & committed:
- `b899d3825` `[XPROC-NEURAL-CONNECT-MS0]/U-CN09` — **XProcNeuralAutoFireEngine**: one idempotent `activate()` ignites the closed-loop learning system that CN02–CN08 built but never turned on (NN auto-train + 4 fan-out bridges CN04/06/07/08). Boot-wired in `mcp-server/src/index.ts` behind `PRISM_XPROC_AUTOFIRE` (default on; `=0` to stay dormant) + `prism_ai` actions `xproc_autofire_{activate,deactivate,status}`. Fixups: `1bf456ac4` (dead ternary), `4bb8cb401` (`catch (… : unknown)` not `any`).
- `e704a39a0` `[XPROC-NEURAL-CONNECT-MS0]/U-CN10` — **experience-replay mixing** in the NN auto-train flush: `CrossProcessNeuralLearningEngine.enableAutoTrain({replayMixRatio, replayMaxRecords})` — each retrain also pulls up to `ceil(buffer*ratio)` historical terminal `OutcomeRecord`s from the singleton `crossProcessOutcomeStore` (stratified by process mill/lathe/wedm round-robin + global top-up, deduped by id, capped), concats them into the training batch. `neural.train.tick` gains `replayMixed`; `autoTrainStatus()` gains `replayMixRatio`/`replayMaxRecords`. `replayMixRatio=0` (default for direct callers) = zero behavior change; `XProcNeuralAutoFireEngine` boot path opts into `0.5`. Also fixed 2 pre-existing `TS2341 'totalSamplesSeen is private'` errors. Fixup `44379abd7` (exact assertions in tests). Opus scrutiny PASS on both CN09 & CN10; codex nits addressed; Gemini CLI keeps timing out at 360s (infra, not code).

All tests pass: 56/56 in the touched suites (`XProcNeuralAutoFireEngine.test.ts` 33, `CrossProcessNeuralLearningEngine.replaymix.test.ts` 13, `CrossProcessNeuralLearningEngine.autotrain.test.ts` 10) + CN06/07/08 regression 124/124. tsc-clean on all touched files.

**NEXT — the user was asked to pick one (check the last user message in the new session before assuming):**
1. **CN11 — EWC consolidation in the retrain** (highest value, riskiest): after each `train()`, anchor old-task weights via `CrossProcessEWCMemoryPreservationEngine` (Fisher-information penalty). Needs restructuring `CrossProcessNeuralLearningEngine.train()` to expose per-sample gradients — a non-trivial change to a HEAVILY-WIRED engine (in=48 out=54). Treat as its own unit, not a tail of CN10.
2. **CN12 — RL bridge**: `outcome.completed` → extract (state=request features, action=chosen params, reward=outcome quality) → `CrossProcessRewardShaperEngine` → `CrossProcessQLearningTabularEngine` / `CrossProcessPolicyGradientEngine` / `CrossProcessMultiArmedBanditEngine` (all built-but-isolated, `in=3 out=1`). New `OutcomeRLBridgeEngine`, same pattern as CN06/07/08.
3. **CN13 — wire the ~18 isolated `CrossProcess*` ML kernels** (MAML-lite, prototypical-net, deep-ensemble, Bayesian-MLP, novelty-detector, EWC, the conformal-classification suite APS/RAPS/Mondrian, the federated-agg suite FedAvg/secure-agg/client-selection) to `prism_ai`/`prism_intelligence` so they're callable. Breadth wiring, lower risk.

**Pattern for any new CN unit:** new engine in `mcp-server/src/engines/<Name>Engine.ts` (static methods, `<name>Dispatch(action, params)` wrapper, `__testHandle(payload)` test seam, failure isolation per downstream call) → wire to `mcp-server/src/tools/dispatchers/aiReasoningDispatcher.ts` (add 3 entries to `XPROC_ROUTES` lazy-import map + add `case "xproc_<name>_*":` lines to the `executeAIReasoningAction` switch — all xproc cases fall through to one body `result = await routeXprocAction(action, params)`) → add the action names to `AI_REASONING_ACTIONS` array AND matching Zod entries to `ACTION_AI_REASONING_SCHEMAS` in `mcp-server/src/schemas/aiReasoningActionSchemas.ts` (the `Record<AIReasoningAction, z.ZodTypeAny>` type enforces they stay in sync) → companion `*.test.ts` in `mcp-server/src/__tests__/` → run `node --max-old-space-size=16384 node_modules/typescript/bin/tsc --noEmit 2>&1 | grep <yourfiles>` to confirm tsc-clean (codebase has ~30 pre-existing errors elsewhere — ignore those) → run the affected vitest files → `node .claude/scripts/scrutiny-3way.mjs --session-id <slug> --target HEAD` in background + dispatch an `Agent(subagent_type:'reviewer', ...)` Opus reviewer in parallel → `--mark-opus pass --session-id <slug>`.

**Multi-chat hazard:** the main tree (`cad-fusion-live-ms0`) is being hammered by ~4–6 peer chats. `git commit -- <pathspec>` to avoid sweeping up peer-staged files. `git index.lock` reappears constantly — `rm -f .git/index.lock` then commit immediately. The `blast-dampener` hook BLOCKS after 3× `--no-verify` in 10 min — use plain `git commit` (the lint-staged pre-commit hook is fast / no-op for `.ts` files). Bash itself periodically hangs (Cygwin fork exhaustion) — if a `node ...` command via Bash hangs >60s, kill it and use the Write/Edit tools or `"C:/Program Files/nodejs/node.exe" ...` directly.

---

## CURRENT STATE (1-line)

Shipped XPROC-NEURAL-CONNECT CN09 (ignite the loop, boot-wired) + CN10 (experience-replay mixing in the auto-train flush), plus an earlier HARNESS-AUDIT pass (U-TIER3e/f/g/h: mcp-posttool-tracker scoped to MCP matcher; consensus-queue bounded at 200 + drainer copied to main tree; --max validation). Fixed 2 pre-existing TS2341 errors. All Opus-scrutiny PASS, tests green, tsc-clean on touched files. Next: pick CN11 / CN12 / CN13.

---

## CONTEXT TO PRESERVE (not derivable from the code)

- **XPROC-NEURAL-CONNECT-MS0 has NO formal milestone envelope** — it's an ad-hoc `U-CN01..U-CN10` commit sequence on `cad-fusion-live-ms0` / main tree. CN01=outcome publish adapter, CN02=SpeedFeedOrchestrator NN consumer (gated emit), CN03=Omega+NN.confidence 7th dim, CN04=TribalKnowledge ← outcome.recorded bridge, CN05=KG semantic-search → NN feature projector, CN06=drift+calibration outcome bridge, CN07=replay/sampler outcome bridge, CN08=episodic memory outcome bridge, CN09=ignite, CN10=replay mixing.
- `CrossProcessNeuralLearningEngine`: model is `INPUT_DIM → HIDDEN_DIM=16 → OUTPUT_DIM=3` (classes: success/failure/operator_override; `recordToLabel` returns null for `pending`). `enableAutoTrain`/`disableAutoTrain`/`autoTrainStatus`/`flushAutoTrainBuffer` are added via `.prototype` with an ambient `declare module` block (U-NN-LOOP03). `autoTrainTotalTicks` is **singleton-cumulative and NOT reset by `disableAutoTrain`** → tests that assert tick counts must use `ticksBefore + 1` deltas, not absolutes. `train(records, opts: TrainOpts)` → `TrainResult {epochsRun, samplesUsed, samplesSkipped, finalLoss, trainAccuracy, initialLoss}`. The two `feedbackBusEngine.publish("neural.train.tick", {...})` sites need `// @ts-expect-error private` before `totalSamplesSeen: this.totalSamplesSeen` (they're private accesses from a prototype-method context).
- `CrossProcessOutcomeStore` is a **singleton** (`crossProcessOutcomeStore`) — `.query(filter: {bridge?, process?, material?, customer?, outcome_kind?, since?, until?, limit?})` returns `OutcomeRecord[]` newest-first; `.clear()` is test-only; `.record(input)` returns the generated id AND **publishes `outcome.recorded` to the shared bus** — so in tests, seed the store BEFORE `enableAutoTrain()` (otherwise the seed events land in the auto-train buffer). `OutcomeRecord` is defined in `CrossProcessOutcomeStore.ts` (schemaVersion, id, ts, bridge, process, request_summary, response_summary, outcome?:{kind,...}, operator?).
- The 4 fan-out bridges — `TribalKnowledgeOutcomeBridgeEngine` (CN04, subscribes `outcome.recorded`), `OutcomeDriftCalibrationBridgeEngine` (CN06), `OutcomeReplayBufferBridgeEngine` (CN07), `OutcomeEpisodicMemoryBridgeEngine` (CN08) — all `outcome.completed`, all have static `subscribeToOutcomes(): {ok:true, alreadySubscribed:boolean}` / `unsubscribeFromOutcomes(): {ok:true, wasSubscribed:boolean}` / `isSubscribedToOutcomes(): boolean` / `reset()`.
- `feedbackBusEngine` is an **instance** (`export const feedbackBusEngine = new FeedbackBusEngine()`) — `.reset()` / `.subscriberCount(topic)` / `.publish(topic, payload)` / `.subscribe(topic, cb): SubscriptionHandle` / `.unsubscribe(handle)` / `.stats()`. Delivery is via `queueMicrotask` → in tests, `await Promise.resolve()` ×4 to drain. `CrossProcessOutcomeStore.record()` publishes `outcome.recorded` at line ~251 and `outcome.completed` at line ~289 (only on actual kind transition).
- `aiReasoningDispatcher.ts`: `xproc_*` actions route via `routeXprocAction(action, params)` → `XPROC_TIER1_HANDLERS[action]` (per-action handlers) else `XPROC_ROUTES[action]()` (lazy-import map, cached in `_xprocCache`) then `wrapper(action, params)`. The `executeAIReasoningAction(action: AIReasoningAction, ...)` switch has all `case "xproc_..."` lines falling through to one body. `AI_REASONING_ACTIONS` is the `as const` array (lines 17–434) that derives `AIReasoningAction`; `ACTION_AI_REASONING_SCHEMAS: Record<AIReasoningAction, z.ZodTypeAny>` (line ~1108) forces array↔schema-map key sync at compile time.
- Build: `npm run build` = `node --max-old-space-size=16384 node_modules/typescript/bin/tsc --noEmit && node esbuild.config.mjs`. `npx` is NOT on PATH in the bash env — use `"C:/Program Files/nodejs/node.exe" node_modules/vitest/vitest.mjs run <files>` for tests, and `node --max-old-space-size=16384 node_modules/typescript/bin/tsc --noEmit` for type checks (default 4GB heap OOMs; the codebase needs 16GB). Codebase currently has ~30 pre-existing tsc errors in unrelated files (KienzleForceModel, BarStockCutPlanEngine, index.ts:729+, aiReasoningDispatcher.ts:628+, etc.) — these are NOT a regression gate for new work; just confirm YOUR touched files add zero new errors.
- Scrutiny gate (CLAUDE.md): 3-of-3 (Codex + Gemini + Opus). Codex usually finishes in ~70s; **Gemini's CLI has been consistently timing out at 360000ms all session** (infra issue, not code). The Opus reviewer agent (`Agent(subagent_type:'reviewer', ...)`) is the load-bearing arm. Run `scrutiny-3way.mjs --session-id <slug> --target HEAD` in background + Opus reviewer in parallel; record `--mark-opus pass --session-id <slug>` when the agent returns PASS.

---

## DEFERRED ITEMS

None half-built. CN09 and CN10 are both COMPLETE (engine + wiring + tests + scrutiny). CN11/CN12/CN13 are fresh units, not deferred fragments.
