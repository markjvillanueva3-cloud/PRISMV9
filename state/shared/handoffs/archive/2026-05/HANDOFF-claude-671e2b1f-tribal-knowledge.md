---
session: claude-671e2b1f
topic: tribal-knowledge
written_at: 2026-05-11T17:05:00.000Z
machine: MARKV
family: Claude
session_key: claude-671e2b1f
status: active
---

# HANDOFF: claude-671e2b1f
Updated: 2026-05-11T17:05:00.000Z
Family: Claude | Machine: MARKV | Session: claude-671e2b1f

## STATE
**Tribal F-track delivered + 8 tribal-card bugs fixed + 4 harness chat-stall bugs fixed.** Worktree `H:/prism-tribal-binder` on `work/tribal-node-binder`; harness fixes committed from `H:/prism` on `cad-fusion-live-ms0`.

### This session's commits
- **★ THE REAL FIX (live in settings.json now; new .mjs files uncommitted on disk)** — **PostToolUse hook-bundle consolidation.** Root cause of "chats stall + commits hang": every tool call cold-starts a fan-out of PostToolUse hooks (`Edit/Write` → ~22, `Bash` → ~18, `Read` → ~21), ×6 chats ≈ ~110-130 simultaneous `node.exe` → Windows process table thrashes → every cold-start balloons. Fix (mirrors the existing PreToolUse `edit-bundle`/`bash-bundle`/`read-bundle`): the 19 `Edit|Write|MultiEdit` PostToolUse entries + the 15 `Bash|Read` entries are now `exit 0` shell no-ops (~20ms each, NO node spawn), and TWO new bundle hooks run them at concurrency 6: `H:/prism/.claude/hooks/bundles/posttool-edit-bundle.mjs` (19 sub-hooks) + `posttool-bash-read-bundle.mjs` (15). Bundles are fail-safe (`emit{continue:true}` on error) AND block-propagating (forward `continue:false`/`decision:block` if a bundled guard fires — physics-canonical-constants / jm-die-provenance / ingestion-cache-root do). Now an Edit/Bash/Read tool call cold-starts ~10-13 node procs instead of ~18-22; ×6 chats ~60-78 vs ~110-130 — ~2x less peak fork pressure. **settings.json: 140→142 hooks** (add-only — `settings-json-addonly-guard` permits since count went UP; edits applied to `C:/Users/Mark Villanueva/.claude/settings.json` via `node scripts/migrate-posttool-group-bundle.mjs`, then `cp`'d to `H:/.claude/settings.json`). Migration tooling (idempotent, backs up, restores-on-failure): `H:/prism/.claude/scripts/migrate-posttool-edit-bundle.mjs` (Edit-specific, already run) + `migrate-posttool-group-bundle.mjs "<matcher>"` (general — use it to also bundle the remaining `Read` (3), `*` (3), small groups if wanted). **Backups:** `C:/Users/Mark Villanueva/.claude/settings.json.bak-posttoolbundle-2026-05-11T1950` (Edit) + `.bak-posttoolbundle-bash-read-2026-05-11T1954` (Bash|Read) — revert via `node scripts/migrate-posttool-*-bundle.mjs --revert "<bak>"`. All bundles syntax-checked + smoke-tested (`{"continue":true}` in ~0.85-1.0s, 15-19 sub-hooks at conc 6). NOTE: there's also a `PRISM_HOOK_PROFILE` runtime gate (`.claude/helpers/hook-profile.mjs` → `shouldSkipHook()`) — a `minimal` profile would cut hook *logic* time (not cold-start) for the roadmap sessions; orthogonal lever, worth flipping.
- `<uncommitted, live on disk>` **[MAIN] [HARNESS-AUDIT]/U-PERF-AUTOLINT-DIRECT-INVOKE** — `auto-lint-post-edit.mjs` (PostToolUse on Edit/Write of `.ts` in `mcp-server/src/`) was invoking `npx eslint ... {shell:true}` — `npx` binary re-resolution + Windows `.cmd` shim + a shell, ~0.3-1s of pure overhead per .ts edit. Now invokes ESLint's entry script directly with the running node (`node node_modules/eslint/bin/eslint.js`, `shell:false`), with an `npx` fallback if the local install is missing; internal timeout 15000→7000ms (< the hook's 8000ms settings.json budget, so ESLint self-aborts cleanly). NOTE: the *bulk* of `auto-lint-post-edit`'s cost is ESLint's own config-load (~3-6s for this big TS project) — the npx-skip only shaves the wrapper overhead; this hook remains a meaningful per-.ts-edit tax for the roadmap chats and is a candidate to disable or rethink (CI + `comprehensive-build-enforce` already cover lint). Syntax-checked + early-exit paths smoke-tested; full eslint-run measurement was contaminated by an accidental `git stash` (see ⚠️ below).
- `<pending bg b87tltkpr>` **[MAIN] [HARNESS-AUDIT]/U-PERF-WIKI-RECALL-TARGETED-LOOKUP** — `wiki-recall-on-read.mjs` (PostToolUse on Read — fires on EVERY Read) stopped JSON-parsing the ~5.5MB `_leaf-index.jsonl` into a name→entry map (+ a 5.5MB tmpdir cache it re-parsed each Read) just to look up one key. New `lookupLeafEntry()`: `readFileSync` the index once, `String.indexOf` the literal `"name":"<slug>"` token, parse ONLY that line, confirm `parsed.name === name`. No full parse, no map, no cache file. Behaviour identical (first-match-wins, fail-safe). Smoke-tested (match / no-mapping / mapped-but-not-in-wiki). **Fix is LIVE on disk** (runtime hook); commit pending due to shared-branch `index.lock` contention — if a peer commit sweeps the staged file, re-attribute or leave it; if not, commit it from `H:/prism` on `cad-fusion-live-ms0` with that message/lane.
- `72e6b28e9` **[MAIN] [HARNESS-AUDIT]/U-STALL-FIX-RUNHOOK-STDIN** — 4 chat-stall vectors in the hook hot path, FIXED + tested:
  1. `hook-runner.runHook` — **orphaned-grandchild HARD HANG** (no upper bound): on timeout the sub-hook is SIGKILL'd but `child.on("close")` only fires when the child's stdout/stderr pipes have no remaining writers; a grandchild that inherited those pipes (or a slow Windows reap) pinned the Promise → `runBundle` hung → the Edit/Bash/Read tool call hung → chat stalled. Fix: on timeout also `child.stdout/stderr.destroy()` + a +1s hard-fallback `finish()` (idempotent settle guard); `spawn()` wrapped so a *synchronous* fork-storm throw degrades to a no-op result; timers `.unref()`'d.
  2. `hook-runner.readStdin` — 100ms race truncated the payload on multi-chat load → bundle ran against `{}` → safety sub-hooks silently no-op'd (gate bypass). Fix: synchronous `readFileSync(0)` to EOF (Claude Code closes the hook's stdin after writing — reliable) + `isatty(0)` short-circuit for manual invocation (and `isatty(0)` instead of `process.stdin.isTTY` so we never construct the stdin Stream — a referenced pipe socket that keeps the loop alive = another stall vector).
  3. `hook-runner.emit` — `process.exit()` right after `process.stdout.write()`; on Windows a hook's stdout pipe write is async → truncated JSON → Claude Code saw empty output → treated a *blocking* bundle as no-op. Fix: exit from the write callback (+ 2s unref'd fallback).
  4. `per-agent-handoff.readStdinSessionId` — `process.stdin.isTTY` lazily builds a referenced `net.Socket` over fd 0 (pipe) → process hangs on "cleanup" after the handoff is already written (**observed this session: `/handoff` Bash call hung >45s, killed manually**). Fix: `isatty(0)` — same answer, no Stream.
  Also hardened `tribal-spike.mjs` (recent PreToolUse hot-path addition): replaced `for await (const chunk of process.stdin)` with the sync `isatty/readFileSync(0)` pattern.
  Verified: `read-bundle.mjs` emits `{"continue":true}` exit 0 (no truncation); `per-agent-handoff read/list` return in ~0.4s for /dev/null AND piped-JSON stdin (was >45s hang); standalone `runHook` test ALL PASS (hanging hook ± inherited-stdout grandchild resolves at ~timeout+ε; spawn/module fail resolves fast; well-behaved hook unaffected).
- `<sha>` **[CAD-FUSION-LIVE-MS0]/U-TRIBAL-FIX-STORE-HASH-STALE** — 3 `EnrichedNodeContextStore` bugs (also `work/tribal-node-binder`... actually committed on `work/tr...` — check `git log`): #6 `hashCard` used a string-array `JSON.stringify` replacer (= recursive key-allowlist → tip content dropped from the dedup hash → re-baked card w/ changed tips silently kept stale) → replaced with recursive `stableStringify`; #7 `isStale` returned "fresh forever" on a malformed `baked_at` (`NaN > TTL` is false) → fail-toward-stale; #8 `flushMirror` used a fixed `.tmp` (concurrent flush could rename a half-written file over the mirror; failed rename left litter) → per-process tmp name + `unlinkSync` on failure. +6 tests; 71/71 across 5 store test files.

### Earlier this session (already in prior handoff): tribal F-track + git-topology + 5 tribal-card bugs
- `6034df152` F2-F4 (TribalCardServer + bin + viewer.html + CONTRACT.md), `a42e6f85b` F5-F6 (regen/query CLIs), `d92e470c4` F7 (spec §14), `efe4364b9` F8 (.gitignore), `2d0859a31` git-topology audit, `bb50d3063` git-tree map in /system-viz, `af8a715b5` GITTREE-VIZ-MS0 roadmap.
- Bug fixes (committed): `042b595c3` #1 TribalCardServer crash (remote DoS — no top-level try/catch + raw decodeURIComponent), `203410e3d` #2 slug collision (slugify not injective → silent .md/.html/index overwrite) + #3 index.json lost-update (no lock), `c076dd949` #4 markdown injection (Obsidian renders raw HTML in .md) + #5 `javascript:` href in obsidianCanonical.

## RESUME
Both directives delivered. **Default next:** pick a fresh roadmap unit (`PRISM-UNIFIED-ROADMAP-v2.md` / `mcp-server/data/roadmap-index.json`), claim it, fork a worktree if scope differs. **If asked to keep hunting node/hook stalls — the NEXT BIG WIN (not done, deliberately scoped out):** **PostToolUse has 44 wired hooks** (incl. dups `recall-counter-track.mjs` ×2 + `mcp-posttool-tracker.mjs` ×2 — can't dedup via Edit, `settings-json-addonly-guard` blocks array-shrink). On this machine `node` cold-start is ~400-700ms under multi-chat load, so 44 PostToolUse + ~11 individual PreToolUse ≈ ~55 node spawns *per tool call* × 6 chats ≈ ~330 simultaneous node.exe → OS process table thrashes → cold-starts balloon → "stalls". Real fix: a `posttool-bundle.mjs` mirroring `edit-bundle`/`bash-bundle`/`read-bundle` (one entry, internal `runPool` at concurrency 6, dispatch sub-hooks by `tool_name`) — collapses the 44-wide blast into a controlled trickle. Big + risky (must get each sub-hook's matcher right or a tracking/safety hook silently stops firing) — a dedicated session. Also: git commits are taking >90s under the same load (slow pre-commit hook chain holds `.git/index.lock` — blocks all 6 chats' git ops); separate problem, but related to the same machine-saturation root cause. Lower-risk leftover: ~30+ hooks still use `process.stdin.on("data"/"end")` + `setTimeout` fallback (bounded by each hook's settings.json timeout, so a slowdown not a hard hang — a refactor). **Tribal lane** items 1+2 (system-viz-on-commit chain append + main-tree UI wiring) are CROSS-LANE (CONTRACT.md §3-§8, §7) — hand to the viz-scripts lane. Items 4+5 (live-fallback, cross-session cooldown) are telemetry-gated. Item 3 (Phase-0 `tribal-spike.log` `is_subagent` inspection) is on Mark.

## CONTEXT
Harness fixes committed on `cad-fusion-live-ms0` from `H:/prism` (precedent: `[MAIN] [HARNESS-AUDIT]/U-FORK-STORM-FIX` etc.) — the live files are fixed regardless of git state since hooks run from `H:/prism/.claude/`. `git-sync-stop` will push. Disable knobs unchanged: `PRISM_HOOK_BUNDLE_CONCURRENCY` (default 6), `PRISM_HANDOFF_STDIN_AUTH=0` (legacy handoff identity). GOTCHA (now FIXED): `per-agent-handoff.mjs write` used to hang >45s on cleanup — the `isatty(0)` fix in `72e6b28e9` resolves it; if it still hangs, write `HANDOFF-<id>-<topic>.md` directly (frontmatter shape above). Tests on Windows: `H:/Tools/nodejs/npx.cmd vitest run ...` (not `rtk npx`). Tribal-track suite: 209/209 across 10 files (pre-store-fix count) → +6 store tests = re-verify via the verification cmd in the prior handoff.
