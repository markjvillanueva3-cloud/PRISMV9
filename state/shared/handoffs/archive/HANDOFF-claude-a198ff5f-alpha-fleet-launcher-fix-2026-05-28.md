# HANDOFF — alpha (claude-a198ff5f) — Fleet launcher fix + cache-miss audit — 2026-05-28

## RESUME directive (what the next alpha should do FIRST)

You're picking up alpha from session `a198ff5f` after a fleet relaunch. **Two work streams are live:**

1. **Last night's alpha session** (`168624b9-fb46-49a2-aff2-ef1e1c8f854a`, 6 hours / 13 MB, ran 5/27 18:58 → 5/28 00:54) — ended on operator directive: **"complete goal. then assess how each claude.md, soul file, memories and wikis should be synergized specifically for each chat slot and their designated domain"** (PER-SLOT-GALAXY-BUILDOUT continuation). That session lives in `H:\prism-slot-alpha\` and is the substantive work to resume.
   - Manual resume: `cd H:\prism-slot-alpha; claude --dangerously-skip-permissions --resume 168624b9-fb46-49a2-aff2-ef1e1c8f854a`
   - Or use: `& 'H:\Tools\prism-fleet\resume-specific.ps1' -Slot alpha -SessionId 168624b9-fb46-49a2-aff2-ef1e1c8f854a -Cwd 'H:\prism-slot-alpha'`

2. **Cache-miss mitigation series** (this chat) — operator wants to work around the [Anthropic prompt cache 125% miss premium](https://www.reddit.com/r/ClaudeCode/comments/1tlzpcc/). **1 of 4 done; 3 pending — see Pending Work below.**

## What was DONE this session (commit-ready)

### 1. PRISM Fleet Launcher — full rewrite (operator: "fix the launch-prism-fleet.bat on my desktop")

**Files modified:**
- `H:\Tools\prism-fleet\Launch-PRISM-Fleet.ps1` — 2×2 quadrant grid for 24 slots (NW alpha-foxtrot, NE golf-lima, SW mike-romeo, SE sierra-zulu, drops xray+yankee). Uses `claude.exe` direct (bypasses broken `claude.cmd`).
- `H:\Tools\prism-fleet\slot-tab-boot.ps1` — 4-tier session resolution: T1 chat-slots.json chatId, T1.5 shared-dir scan for `/checkin-<slot>` first-msg match, T2 slot-keyed project dir scan, T3 fresh /checkin (with domain-bootstrap for romeo/uniform/victor). **PID + JSONL-mtime liveness guard** (skip live slots). **Anti-compact env vars** (`CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=99`, `PRECOMPACT_SOFT/HARD_TOKENS=99M`). NEW PARAMS: `-ResumeSession <uuid>` and `-ForceCheckin` switches let the .bat regenerator override tier resolution with its size+age guards.
- `H:\Tools\prism-fleet\resume-specific.ps1` — one-off resume helper for historical sessions (used to open last night's alpha).
- `H:\Tools\prism-fleet\probe-claude-launch.ps1` — diagnostic probe tab.

### 2. Desktop shortcut

- `C:\Users\wompu\Desktop\PRISM Fleet (24-tab).lnk` — new icon pointing at pwsh 7 + `Launch-PRISM-Fleet.ps1` (created 08:23). Coexists with the legacy `PRISM Fleet.lnk`.
- **CRITICAL — see [[reference_this_pc_onedrive_desktop_2026_05_28]]**: Windows shows `C:\Users\wompu\OneDrive\Desktop` as Desktop on THIS PC. The local `C:\Users\wompu\Desktop` is INVISIBLE. **The actual fleet launcher the operator uses is `C:\Users\wompu\OneDrive\Desktop\LAUNCH-PRISM-FLEET.bat`** — regenerated by `node H:/prism/scripts/regenerate-launch-fleet.mjs`.

### 3. The `;` collision bug (root cause of "0x80070002")

`LAUNCH-PRISM-FLEET.bat` was emitting per-tab: `"%PWSH%" -NoExit -Command "$env:PRISM_BOOT_SLOT='alpha'; %CLAUDE% --dangerously-skip-permissions '/checkin-alpha'"`. Windows Terminal's `;` action-separator parser does NOT respect pwsh `-Command` quote scope — it split mid-tab, tried to launch `%CLAUDE% --dangerously-skip-permissions '/checkin-X'` as a NEW tab's commandline → CreateProcess `FILE_NOT_FOUND 0x80070002` on every tab.

**Fix applied (TWO files):**
- `C:\Users\wompu\OneDrive\Desktop\LAUNCH-PRISM-FLEET.bat` — manually rewritten to use `pwsh -File slot-tab-boot.ps1 -Slot X` per tab (zero `;` in tab commandline).
- `H:\prism\scripts\regenerate-launch-fleet.mjs` — patched so regens emit the new pattern. Replaced `claudeCmdForSlot()` → `bootArgsForSlot()` returning `-File "%BOOT%" -Slot <name> [-ResumeSession <uuid> | -ForceCheckin]`. Updated bat prologue: `%CLAUDE%` env var removed, `%BOOT%` env var added pointing at `H:\Tools\prism-fleet\slot-tab-boot.ps1`. Existence check updated.

**Verified working:** test charlie tab spawned successfully, operator confirmed "it launched". Regenerator dry-run produced clean `.bat` with 2 `-ResumeSession` + 5 `-ForceCheckin` invocations across 10 active slots (8 staleness-demoted because chat-slots.json bindings >135min old).

### 4. Claude Code update (operator: "update claude cli if there's an update")

- `@anthropic-ai/claude-code` 2.1.152 → **2.1.153** (npm install -g succeeded with locked-file cleanup warning — stale `.claude-code-kibYjIgd` temp dir frees on reboot, 224 MB).
- All 4 flags my launcher depends on confirmed present in 2.1.153: `--resume`, `--continue`, `--fork-session`, `--dangerously-skip-permissions`.
- Path unchanged: `H:\Tools\nodejs\node_modules\@anthropic-ai\claude-code\bin\claude.exe`.

### 5. CHAT-SLOT-DOMAINS.md updated

`H:\CHAT-SLOT-DOMAINS.md` — added three new domain entries operator requested:
- **ROMEO** = WIRING UNWIRED ENGINES (593 unwired per /awareness-snapshot)
- **UNIFORM** = BUG HUNTING (silent failures, R12 violations, regressions)
- **VICTOR** = DORMANT DATA EXCAVATION (strict order: `H:\PRISM\extracted` → `extracted_modules` → codebase folder-by-folder)
- **ZULU** (corrected — was "ZEBRA" in doc, canonical schema is zulu per `chat-slots.mjs` SLOT_NAMES)

### 6. Cache-miss mitigation #1 — noisy hooks disabled

`H:\.claude\settings.json` env section added:
- `PRISM_REWRITER_HEALTH_WARN_DISABLE=1` — kills the ollama-broken banner whose `(36/50 → 35/50 → ...)` counter was changing per turn, invalidating cache every prompt
- `PRISM_CAG_ROUTER_INJECT_DISABLE=1` — kills CAG-route sidecar hash injection
- `PRISM_PSN_LEG_STATE_INJECT_DISABLE=1` — kills the NN/GNN AUROC-not-finite advisory

### 7. Memory saved

`C:\Users\wompu\.claude\projects\H--prism\memory\reference_this_pc_onedrive_desktop_2026_05_28.md` — DESKTOP-N7MI1VB OneDrive Desktop redirect + fleet launcher canonical location + the `;` collision bug rationale. Indexed in `MEMORY.md` under "This PC — DESKTOP-N7MI1VB config (2026-05-28)".

## Pending Work — cache-miss mitigation #2, #3, #4

Per my 4-item triage list earlier in this session:

| # | Action | Where | Effort |
|---|--------|-------|--------|
| **2** | **CAG cold-cache anchor missing `cache_control: ephemeral` markers** on static prefixes. Anchor catalogs CLAUDE.md / MEMORY.md / engine-digest / dispatcher-digest / physics-constants / wiki-index / tribal-tips but doesn't mark them. Patch `H:\prism\scripts\lib\cag-router.mjs` (search for `COLD_SOURCES`) to emit cache breakpoints. **~80-120K tokens saved per cold start.** | `scripts/lib/cag-router.mjs` | Medium |
| **3** | **Fleet tab `claude --resume <uuid>` re-injects massive JSONLs** with no keep-alive — if >5 min between prompt and operator interaction, the whole JSONL misses cache (alpha's 53MB ≈ 10M tokens × 12.5x miss premium = ouch). Mitigations: (a) prewarm with a synthetic short prompt right after resume, (b) cap auto-launched chats per minute, (c) batch operator's typical "post-launch" prompts and submit them as warm-up sequence. | `slot-tab-boot.ps1` + new prewarmer | High |
| **4** | **Slow subagent dispatches exceed 5-min TTL** — earlier 3-parallel-reviewers hit ECONNRESET after ~6 min and busted the cache when their callback returned. Cap subagent budget at 4 min OR run them in background and accept no return value. | Agent dispatch wrapper | Low |

**Operator preference noted:** "do one then generate handoff to continue" → did #1 (lowest difficulty, highest hit-rate, immediately effective). #2 is the highest ROI for a fresh-budget chat. #3 directly affects fleet-launcher behavior. #4 is a coding-discipline rule.

## State snapshot

- **Branch:** `cad-fusion-live-ms0` (1884 ahead AND 1 behind origin — diverged; needs merge before push — see `git-sync` SessionStart warning).
- **Slot worktrees:** all 26 NATO worktrees exist on disk under `H:\prism-slot-<name>` (verified). Most slots in chat-slots.json have been demoted to /checkin by the regenerator's staleness-guard (>90min JSONL age).
- **Live fleet from last night (chat-slots.json snapshot at session start):** alpha (this chat), echo, golf, hotel, india, juliett, mike, oscar, sierra all had active claims with PIDs.
- **Memory pressure:** session at YELLOW (66% ctx, ~26M cache-read tokens cumulative). Operator's spend visible in cumulative metrics.

## How to continue (specific recommendation)

**Operator's intent** ("as soon as we finish current tasks and I launch the prism fleet, we can pick right back up from last nights session"):

1. **Operator closes this chat** + launches the fleet via OneDrive Desktop's `LAUNCH-PRISM-FLEET.bat` (now fixed).
2. **Alpha tab in the fleet** picks tier 1 resume from chat-slots.json on session `a198ff5f` — that's THIS chat's session-id. But the chatId 13656 (stale pid) means liveness guard passes → resume.
3. Alternative: alpha tab can be told to resume last night's `168624b9` via the regenerator's override (set `MCP_RESUME_MAX_AGE_MIN=999 node scripts/regenerate-launch-fleet.mjs` to disable the staleness guard for alpha, OR manually run resume-specific.ps1 against 168624b9).
4. **First prompt to fresh alpha after relaunch:** "resume per-slot galaxy synergy assessment from session 168624b9; cache-miss mitigations 2-4 still pending (see this handoff §Pending Work)"

## Cross-references

- [[reference_this_pc_onedrive_desktop_2026_05_28]] — OneDrive desktop redirect + regenerator `;`-bug rationale
- [[feedback_conflict_fork_rule]] — when worktree migrations get blocked
- [[reference_session_continuity_stack_2026_05_15]] — PRISM_BOOT_SLOT mechanism
- `state/shared/specs/SWARM-LAUNCHER-MS0.md` — fleet launcher spec
- CLAUDE.md §PER-CHAT HANDOFF + §SESSION CONTINUITY STACK

## Operator history this session

1. "please fix the launch-prism-fleet.bat on my desktop" — DONE
2. "i need original alpha context from last night for alpha" — DONE (resume-specific.ps1 helper + identified session 168624b9)
3. "the prism fleet launcher is still broken" — chased semicolon-collision through 2 wrong hypotheses (node update? claude.cmd?) before finding it
4. "update claude cli if theres an update" — DONE (2.1.152→2.1.153)
5. "not seeing it, i think onedrive is messing things up" — KEY DISCOVERY (OneDrive Desktop redirect)
6. "patch it" (the regenerator) — DONE
7. "synergize system to work around these" (Reddit cache miss article) — #1 of 4 done, handoff covering 2-4

**End of handoff.**
