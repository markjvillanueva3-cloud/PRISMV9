# CLAUDE.md Regression Archive

Older regression bullets evicted from CLAUDE.md by scripts/compress-claude-md.mjs. Newest-first per archival batch.

---

## Archived 2026-05-20 (compress-claude-md, 108 bullets evicted from CLAUDE.md by golf slot)

- 2026-05-18 | **Local git repo had 7 corrupt loose objects from a crashed disk-write event (~2026-04-22, same window as the 26-day-old `maintenance.lock`)** — only `e36809bbd238…` was previously known. Discovered via long-running `git fsck --no-dangling`: 6 empty 0-byte stub files (`425838…`, `6911ae…`, `6ba702…`, `875d61…`, `90aa08…`, plus the known `e36809…`) and 1 with inflate-error garbage (`f41beba8…`, 10282 bytes). Every `git gc` errored on the bad tree, blocking automatic repo maintenance for ~26 days. | partial fix: (this session, slot bravo) — **4 of 7 recovered** by deleting the empty stubs for `425838 / 6911ae / 6ba702 / 90aa08` after `verify-pack -v` confirmed each lives in `pack-b1687ff69fe8b7b7924ccf3129e6059c20060c06.idx`. Git auto-falls-back to the pack copy once the corrupt loose is gone; `git cat-file -t` now returns `tree`/`blob` correctly for all four. | remaining: `875d61482ac89327d3845a5c0737433b6b5659b7` (empty stub, NOT in any pack), `f41beba824ed8131bc420e3e345ac07d2ec5dc55` (corrupt 10KB loose, NOT in any pack), `e36809bbd238e2894fff1e89620be0846c9a1923` (totally absent). All three return 404 on origin (`gh api repos/markjvillanueva3-cloud/PRISMV9/git/{trees,blobs}/<sha>` → Not Found) — truly-lost data the crashed write killed before push. Current snapshot doesn't reference them (`git ls-tree -r HEAD` walks clean); reachable only via historical commits / reflog, so daily flow is unaffected. Only `git gc` still fails on `e36809bb…`; not in any hot path. | next steps if full repo rebuild is wanted: (a) `git fsck --no-reflogs` to confirm whether the 3 are reachable from any current ref (probable: only reflog → `git reflog expire --expire=now --all && git gc --prune=now` clears it — destructive, drops 3297 reflog entries) OR (b) `git filter-repo --strip-blobs-with-ids` if reachable from history, OR (c) fresh `git clone --bare` from origin + `git format-patch` 180 local-only commits + `git am` into the fresh clone. | observed-by: claude-9033b60c slot bravo, "did you check the entire h drive? utilize /system-viz to search. can you determine what is missing now?" /goal. | verify: `git -C H:/prism cat-file -t 425838415319e07ee2ce40d6bce22c4a4d80efae` → `tree` (was: corrupt); `git -C H:/prism gc --prune=now 2>&1 | head -2` → still fails only on `e36809bbd238…` (not on the 4 fixed).
- 2026-05-18 | **`git-lock-sweeper.mjs` `TOP_LOCKS` array missed `.git/objects/info/commit-graphs/commit-graph-chain.lock`** — a crashed `git fetch` (commit-graph rebuild stage) leaves this lock at a deep path the sweeper never visited. On the work PC the lock had been present since 2026-05-18 09:58 — every subsequent `git fetch` and `git maintenance run --task=commit-graph` failed with `fatal: Unable to create '…/commit-graph-chain.lock': File exists`, while the sweeper kept reporting "cleared 1 stale lock(s) — .git/index.lock" (it WAS clearing index.lock, just not the commit-graph one). Same session also surfaced an ancient `.git/objects/maintenance.lock` from **2026-04-22** (~26 days old, 0 bytes) blocking `git maintenance run` — that lock was created by a crashed maintenance task and never gc'd because the sweeper didn't know about it either. | fix: (this session, slot bravo) — `git-lock-sweeper.mjs` `TOP_LOCKS` extended with `.git/objects/info/commit-graphs/commit-graph-chain.lock`. Functional smoke-test: `touch -t 202605180900 <lock> && echo '{...PreToolUse git...}' | node git-lock-sweeper.mjs` → `cleared 1 stale lock(s)`, file unlinked. Same 5-min / 30s PreToolUse age thresholds apply (lock has same atomic-write lifetime as index.lock). Ancient `maintenance.lock` removed manually this session (single one-off; future stale-maintenance-locks could also be added to `TOP_LOCKS` if recurring, but `git maintenance` has been disabled fleet-wide in this repo due to corrupt-object `e36809bbd238e2894fff1e89620be0846c9a1923` that blocks all gc). | observed-by: claude-9033b60c slot bravo, "fix any other git tree commit issues" /goal. | verify: `node --check H:/prism/.claude/hooks/git-lock-sweeper.mjs` → OK; `grep -c "commit-graph-chain.lock" H:/prism/.claude/hooks/git-lock-sweeper.mjs` → 1.
- 2026-05-18 | **`U-VIZ-FIND-CACHE` test suite deleted the live `state/shared/system-viz/system-graph.json` (~370 MB)** because its naive setup/teardown pair tracked the per-test backup via `process.pid`-suffixed filename but never restored it process-wide. Bug surfaced on test 4 (`graph vanished + sidecar present`) — it called `teardownStubGraph()` to "ensure no graph" WITHOUT a matching `setupStubGraph()` first, so the helper unlinked the real file then found no backup to restore. Subsequent tests then ran against a missing original — each setup saw "no real graph, nothing to back up" and each teardown left no graph behind. Net: live `system-graph.json` gone after `node --test` first run. Tests still PASS-with-bug because the hermetic stubs don't depend on the real graph. | fix: U-VIZ-FIND-CACHE (commit `4f481252f1`, slot bravo) — `setupStubGraph` now backs up the original ONCE on first call (gated by `_backupCreatedThisProcess`), `teardownStubGraph` only removes the stub (does NOT restore), and a `process.on("beforeExit")` handler restores the original ONCE at suite exit. Test 4 rewritten to use `setupStubGraph` + immediate `unlinkSync(REAL_GRAPH)` instead of bypassing setup. 11/11 PASS post-fix. Recovery: `node --max-old-space-size=16384 --stack-size=8192 scripts/regen-viz.mjs` (in progress this session). | observed-by: claude-9033b60c slot bravo, `/goal improve overall system performance` /loop iter 5. | lesson: hermetic test backup/restore must be SUITE-LIFETIME, not PER-TEST. Per-test asymmetric setup/teardown is a footgun on real-file fixtures. | verify: `node --test H:/prism/scripts/lib/__tests__/system-viz-find-cache.test.mjs` → 11/11; `ls H:/prism/state/shared/system-viz/system-graph.json` → file present, size ~370 MB after regen completes.
- 2026-05-18 | **The `PRISM Fleet Reaper` scheduled task ran as S4U (current user) → `Stop-Process` returned "Access is denied" on elevated / cross-security-context node processes, so those orphans piled up** (operator-reported: a PowerShell window flashing access-denied as the reaper tried to kill nodes). S4U can terminate only the installing user's same-or-lower-integrity processes — an elevated chat's `node` children, or a SYSTEM-spawned helper, are unkillable by it. Separately surfaced this session: `fleet-reaper-tier.test.mjs` had silently gone stale — it hard-coded `DEFAULT_MEM_CRITICAL_PCT=95`, but OPT-2 (2026-05-17) lowered that constant to 88 (`88 < warn 90` → `tierFromPressure` collapses the warn band by design); 7 tier assertions failed at HEAD, unrelated to this session, caught when the new `--hunt` test exercised the file family. | fix: U-FR-ADMIN-HUNT (commit `f73d74af1d`, slot hotel) — `install-fleet-reaper-task.ps1`: **SYSTEM is now the DEFAULT scheduled-task principal** (`NT AUTHORITY\SYSTEM` terminates any process regardless of owner/integrity, needs no UAC, runs session 0 = no window); `-AsCurrentUser` is the conservative S4U opt-out, `-AsSystem` a back-compat no-op alias. `fleet-reaper-sweep.mjs`: pure `classifyKillError()` (names an access-denied kill failure explicitly — `reapProcesses` tags every result with `errorClass`) + a `--hunt` CLI mode (`buildHuntReport()` — Task-Manager view of every node/bash/git target + reap verdict, heaviest-RSS first, for Claude Code to invoke). +26 `node:test` cases. U-FR-TIER-TEST-DRIFT (commit `813974b15b`) realigns the stale tier test — production-default assertions use the real imported constants, generic warn-band logic uses an explicit non-degenerate 90/95 banding. 103/103 fleet-reaper tests green; 3-of-3 PASS. | observed-by: claude-cb728a14 slot hotel, `/goal` reaper-admin-mode. | apply: re-register the task elevated — `! powershell -NoProfile -ExecutionPolicy Bypass -File H:/prism/.claude/helpers/install-fleet-reaper-task.ps1 -RunNow` (SYSTEM is now the default; no extra flag needed). | verify: `Get-ScheduledTask 'PRISM Fleet Reaper'` → `Principal.UserId` = `SYSTEM`; `node H:/prism/scripts/fleet-reaper-sweep.mjs --hunt --dry-run`; `node --test H:/prism/scripts/__tests__/fleet-reaper-hunt.test.mjs` → 26/26.
- 2026-05-18 | **`seed-ghost-from-unwired.mjs` pretty-printed the ~390 MB merged `system-graph.json` (`JSON.stringify(g, null, 2)`) at both write sites (`--apply` + `--revert`).** Once the graph crossed the size threshold the indented string exceeded V8's ~512 MB max-string-length cap → `RangeError: Invalid string length`, crashing the NN-GRAPH reference-pool seed stage out of every `regen-viz --full` (observed: `failed=1`; 681 `ghost.unwired-engine` reference nodes never seeded → `nn-graph-eval` pool stuck at `poolSize:0`, GNN tier-5 dormant-by-data). Same V8 string-cap class as the documented `merge-augmentations` fix — `seed-ghost` was the last script still pretty-printing the full merged graph. | fix: U-SEED-GHOST-COMPACT (commit `0160a1521d`, slot hotel) — compact `JSON.stringify(g)` at both sites, matching the sibling convention `merge-augmentations.mjs:1490`; +2 structural source-guard regression tests in `seed-ghost-from-unwired.test.mjs` (29/29 across both seed-ghost test files). Re-ran `seed-ghost --apply`: 681 ghosts + 576 edges seeded; drift gate clean; graph 244,020 nodes, all 15 layers (L0-L13 + L4a + Lgit). | observed-by: claude-cb728a14 slot hotel, `/checkin-hotel /loop` system-viz update. | verify: `grep -c "JSON.stringify(g, null, 2)" H:/prism/scripts/seed-ghost-from-unwired.mjs` → 0; `node --test H:/prism/scripts/seed-ghost-from-unwired.test.mjs` → 24/24.
- 2026-05-18 | **`/checkin-<nato>` did NOT reliably force the slot claim — peer chat stole a unit out from under the slot owner.** The NATO wrapper markdown made the *model* hand-copy `STABLE="claude-<8hex-from-Chat-Isolation-line>"` and run a bash slot-claim. Post-`/compact` the model copies the STALE id from the conversation summary, not the live `**Chat Isolation:**` line: observed this session — a hotel chat ran as `claude-93351de7` (a dead prior session in its lineage) instead of live `claude-2d30710b`; the claim was rejected and peer **bravo** grabbed `COMMAND-KERNEL-MS0::U-CK09`. `stable-session-id.mjs` cannot rescue the wrapper bash (post-`/compact` Bash context → PID-pin miss → "most-recently-touched cached session" fallback returns a PEER's id, repro: `claude-9876118b`). Structural: a markdown wrapper that needs the model to hand-copy an id across `/compact` is unreliable by construction. | fix: U-SLOT-BIND-ENFORCE (commit `679feae088`, slot hotel) — `.claude/hooks/slot-bind-enforce.mjs` UserPromptSubmit hook reads the AUTHORITATIVE stdin `session_id` (same source as `chat-state-isolator`'s Chat Isolation line), derives `claude-${sid.slice(0,8)}` (NO case-fold — a `.toLowerCase()` divergence was a scrutiny-caught P0 that re-creates the bug), force-claims the slot named in `/checkin|startup|precompact|handoff-<nato>` / `--preferSlot`. Idempotent fast-path (no `/loop` evict-thrash), fail-safe never-guess, R12 honest failure (failed claim never emits ✅). Wired UserPromptSubmit after `session-id-pin` (C:+H:). 33 tests incl. 8 `main()` subprocess oracles; 4 reviewers ×2 rounds PASS/SHIP. Lesson: pure-core + injected-deps MUST ship a real subprocess integration oracle — the P1 fixes lived in `main()` which the first test pass left uncovered (caught by scrutiny, not the suite). | observed-by: claude-2d30710b slot hotel (user-reported). | verify: `grep -c slot-bind-enforce H:/.claude/settings.json` → 1; `echo '{"prompt":"/checkin-hotel","session_id":"DEADBEEF-x"}' | "H:/.claude/bin/portable-node" .claude/hooks/slot-bind-enforce.mjs` → emits a deterministic bind/advisory (never silent on a slot-locked prompt with a usable sid).
- 2026-05-17 | **harden WIKI_INDEX_REL regression oracle (3-of-3 arm-B P2)** | observed-in: 36f7bb42d | fix: see commit | verify: `git -C H:/prism show 36f7bb42d`
- 2026-05-17 | **realign tier test to OPT-2 crit=88** | observed-in: 813974b15 | fix: see commit | verify: `git -C H:/prism show 813974b15`
- 2026-05-17 | **strip NUL byte from UNDEF_SENTINEL literal** | observed-in: 8672514f1 | fix: see commit | verify: `git -C H:/prism show 8672514f1`
- 2026-05-17 | **fix patch-sibling verify command (3-of-3 arm-B P1)** | observed-in: a53af4ac7 | fix: see commit | verify: `git -C H:/prism show a53af4ac7`
- 2026-05-17 | **NVIDIALLMCAMEngine resolves PRISM-canonical NIM_URL + /v1-doubling fix** | observed-in: c03ffbe4c | fix: see commit | verify: `git -C H:/prism show c03ffbe4c`
- 2026-05-17 | **source (c) — recover bridge-unit completion from git subjects** | observed-in: e11e681f8 | fix: see commit | verify: `git -C H:/prism show e11e681f8`
- 2026-05-17 | **4-surface doc-reflection for picker→bridge arc** | observed-in: 382f32069 | fix: see commit | verify: `git -C H:/prism show 382f32069`
- 2026-05-17 | **slot-domain filter + SSOT extraction** | observed-in: a9f1df580 | fix: see commit | verify: `git -C H:/prism show a9f1df580`
- 2026-05-17 | **U-ID gate + mtime cache + fail-on-revert oracle** | observed-in: 9cdc2db2e | fix: see commit | verify: `git -C H:/prism show 9cdc2db2e`
- 2026-05-17 | **union git+envelope as shipped-units source of truth** | observed-in: c84a0c7cb | fix: see commit | verify: `git -C H:/prism show c84a0c7cb`
- 2026-05-17 | **operator close-out runbook (Docker-restore path)** | observed-in: a8df7ca9d | fix: see commit | verify: `git -C H:/prism show a8df7ca9d`
- 2026-05-17 | **replace 'typeof this.X' with ClassName['X'] in 6 engines (-6 expected)** | observed-in: 1af3c577a | fix: see commit | verify: `git -C H:/prism show 1af3c577a`
- 2026-05-17 | **claude-md-collapse-milestones tool (live apply deferred — CLAUDE.md claimed by claude-88486e9e)** | observed-in: e484539c0 | fix: see commit | verify: `git -C H:/prism show e484539c0`
- 2026-05-17 | **align FiveAxis enum drift + bridge strategy-entry contract (-8 tsc)** | observed-in: 19945c606 | fix: see commit | verify: `git -C H:/prism show 19945c606`
- 2026-05-17 | **MaterialNodeProps/OperationNodeProps/ToolNodeProps Record bridge (-7)** | observed-in: f4f7df0ff | fix: see commit | verify: `git -C H:/prism show f4f7df0ff`
- 2026-05-17 | **save durable bravo task queue plan** | observed-in: f3c413e18 | fix: see commit | verify: `git -C H:/prism show f3c413e18`
- 2026-05-17 | **wiki entry + patch-sibling for CLAUDE.md/MEMORY.md (peer-locked)** | observed-in: 8391f3d28 | fix: see commit | verify: `git -C H:/prism show 8391f3d28`
- 2026-05-17 | **wire consolidated open-threads into post-/compact resume-read path** | observed-in: 182df1aa3 | fix: see commit | verify: `git -C H:/prism show 182df1aa3`
- 2026-05-17 | **MaterialEntry->context adapter + 3 canonical ISO tables (-13)** | observed-in: 4eb6ce33b | fix: see commit | verify: `git -C H:/prism show 4eb6ce33b`
- 2026-05-17 | **4-surface reflection + P0 regression log** | observed-in: a6abf2704 | fix: see commit | verify: `git -C H:/prism show a6abf2704`
- 2026-05-17 | **bump CANONICAL_MATERIAL_DB count test 13->15 (scrutiny arm-C blocker)** | observed-in: f24d9a3c0 | fix: see commit | verify: `git -C H:/prism show f24d9a3c0`
- 2026-05-17 | **correct cutover spec (3-of-3 arm-A R12 fix)** | observed-in: 3177ae823 | fix: see commit | verify: `git -C H:/prism show 3177ae823`
- 2026-05-17 | **WireEDMSettingsEngine wiring-gate test + 2 real bug fixes** | observed-in: 56f90ae99 | fix: see commit | verify: `git -C H:/prism show 56f90ae99`
- 2026-05-17 | **doc-reflection for U-SDF19 heartbeat-cache wire** | observed-in: 16096eabe | fix: see commit | verify: `git -C H:/prism show 16096eabe`
- 2026-05-17 | **extend C0-strip to $p.Name (closes enum-blind regression class)** | observed-in: ac9cca890 | fix: see commit | verify: `git -C H:/prism show ac9cca890`
- 2026-05-17 | **`system-graph.json` has THREE independent writers, not two** (peer-reviewer-found during DEV-TOOL-CONFLICT-AUDIT /forge-audit-v2 echo). Existing CLAUDE.md entry names `generate-system-viz.mjs` vs `regen-viz.mjs`; the third writer is `scripts/system-viz-add-node.mjs` whose own docstring (lines 113-124, 407-450) documents the race: add-node respects generate-system-viz's PID lock, but generate-system-viz does NOT respect add-node's lock — one-way fence. Per F1+F11 of the audit, the canonical fix (give `generate-system-viz.mjs` its own `OUT_FILE`) must also cross-lock `regen-viz.mjs` and `system-viz-add-node.mjs` against each other. **Current live state** (verified this session): graph is `schemaVersion 2.1.0 / 20702 nodes / fsCoverage:false` = `generate-system-viz` last-wrote, the merged 372K-node `regen-viz` product is wiped (CLAUDE-BRIEF headline "372,731 nodes" stale). | fix: pending — unit `U-VIZ-SPLIT-OUT-FILE` queued (Track J). Interim: `node scripts/regen-viz.mjs` restores the canonical merged graph. | observed-by: claude-dacc6809 slot echo `/forge-audit-v2` peer-reviewer agent abd240de561947257. Audit: `state/shared/specs/DEV-TOOL-CONFLICT-AUDIT-2026-05-17.md`. META tool: `scripts/dev-tool-conflict-detector.mjs` (baseline=6, currently=13). | verify: `grep -lE "writeFileSync.*system-graph\.json" scripts/*.mjs` → 3 files; `node -e "const g=require('./state/shared/system-viz/system-graph.json');console.log(g.schemaVersion,(g.nodes||[]).length,!!g.fsCoverage)"` → currently `2.1.0 20702 false` (BUG state).
- 2026-05-17 | **`roadmap-index.json` has 5 independent writers, 3 NON-atomic** (DEV-TOOL-CONFLICT-AUDIT F4). `reconcile-roadmap-drift.mjs` (atomic) + `close-out-milestone.mjs` (atomic) + `reconcile-milestones.mjs` (NON-atomic) + `register-devtools-roadmap-envelopes.mjs` (NON-atomic) + `register-revenue-roadmap-envelopes.mjs` (NON-atomic). A reader hitting mid-write of any non-atomic writer sees truncated JSON; a `register-*` run after `close-out-milestone` re-introduces stale `pending` status (silent close-out debt class). | fix: pending — unit `U-ROADMAP-INDEX-WRITER-CONSOLIDATE` queued. Route all 5 through atomic `atomicWriteJson` helper; long-term collapse to single mutation library. | observed-by: claude-dacc6809 slot echo `/forge-audit-v2`. | verify: `node scripts/dev-tool-conflict-detector.mjs --paths-only \| grep roadmap-index` returns the path; `grep -L "atomicWriteJson" scripts/{reconcile-milestones,register-devtools-roadmap-envelopes,register-revenue-roadmap-envelopes}.mjs` returns all 3.
- 2026-05-17 | **HOOK-VS-HOOK race latents (`error-memory.json` + `skill-usage-stats.json`)** — DOWNGRADED from HIGH live race to LOW latent by peer-reviewer (DEV-TOOL-CONFLICT-AUDIT F2+F3). `error-pattern-memory.mjs`, `skill-usage-tracker.mjs`, `smart-skill-suggest.mjs` are ALL ORPHAN hooks (not in settings.json + not in any bundle); only `error-learner-hook.mjs` is wired (via `posttool-bash-read-bundle.mjs`). So no live race today — BUT all four hooks DO contain `writeFileSync` calls targeting the same path. The instant any of the 3 orphans gets wired the race materializes. | fix: pending — unit `U-ERROR-MEMORY-CANONICAL-WRITER` + `U-SKILL-USAGE-CANONICAL-WRITER`. Designate one canonical writer per path; the others become reader-only via in-process engine API; OR convert both target files to append-only `.jsonl`. Wire-first-without-fix would resurrect the race. | observed-by: same audit + peer reviewer abd240de561947257. | verify: `grep -lE "writeFileSync.*error-memory" .claude/hooks/*.mjs` → 2 (both writers exist); `grep -c error-pattern-memory H:/.claude/settings.json` → 0 (orphan confirmed).
- 2026-05-17 | **U-VIZ-SPLIT-OUT-FILE F1 leg SHIPPED** (commit `dd735c1871`, echo, /loop). `scripts/generate-system-viz.mjs` `OUT_FILE` renamed → `architecture-graph.json`; architecture-only producer no longer clobbers regen-viz merged graph. Live-verified separate files (mtimes 21:28 fresh vs 20:38 untouched). Per-file scrutiny PASS (`code-analyzer` agent af4118d7bf1933aea, 0 P0/P1). Also patched `scripts/dev-tool-conflict-detector.mjs` to resolve `path.join(BASE, "x")` constants (was blind; conflict count corrected 13→16; roadmap-index.json correctly shows 7 writers now). F11 leg (cross-lock `regen-viz.mjs` subprocess write + `system-viz-add-node.mjs` atomic-rename) DEFERRED — neither caught by source-grep detectors; needs shared PID-lock convention. | verify: `grep -lE "writeFileSync.*system-graph\.json" scripts/generate-system-viz.mjs` → 0; `git -C H:/prism show dd735c1871`.
- 2026-05-17 | **Skill version sprawl + backup files in active skill dir** (DEV-TOOL-CONFLICT-AUDIT F5). 6 superseded forge versions (`forge`..`forge6`, ~106KB) + 5 superseded rgs versions (`rgs`..`rgs5`, ~133KB) + 2 `.fullcopy-bak-20260512-*` files all in `H:/.claude/commands/` — scanned by SessionStart every session, ~250KB of skill text the LLM never needs. Zero references in CLAUDE.md or hooks point to anything before `forge7` / `rgs6`. Plus 64 H:/ vs H:/prism/ skill mirrors with large size deltas (`startup.md` 629B vs 22.2KB; `forge-triple.md` 722B vs 9.8KB) — operators don't know which loads first; Claude-Code skill-loader merges both with user-global winning on ties, so PRISM-specific bodies may be silently shadowed. | fix: pending — unit `U-SKILL-ARCHIVE-FORGE-RGS-BAK` (move to `H:/.claude/commands/_archive/`, retire from SessionStart skill scan; archive subdir is not auto-injected) per [[feedback_never_delete_only_disable]]. Plus `U-SKILL-MIRROR-RECONCILE` for the 64-mirror set. | observed-by: same audit + skill-sprawl hunter agent a9263c11212d4b12f. | verify: `ls H:/.claude/commands/forge[0-9]*.md H:/.claude/commands/rgs[0-9]*.md H:/.claude/commands/*fullcopy-bak* \| wc -l` → currently 13; target after archive: 2 (forge7 + rgs6).
- 2026-05-17 | **backfill CLAUDE.md regression block for U-SDF13 + U-SDF15** | observed-in: 1904c4cf7 | fix: see commit | verify: `git -C H:/prism show 1904c4cf7`
- 2026-05-17 | **sticky chatId→slot history cache (closes /compact slot-drift regression)** | observed-in: 590b565fb | fix: see commit | verify: `git -C H:/prism show 590b565fb`
- 2026-05-17 | **derive slot-identity-cache default dir from PRISM_ROOT env var** | observed-in: 72e768371 | fix: see commit | verify: `git -C H:/prism show 72e768371`
- 2026-05-17 | **doc-reflection — wiki entry for slot-identity-cache (+ Obsidian memory file)** | observed-in: ce0d9f994 | fix: see commit | verify: `git -C H:/prism show ce0d9f994`
- 2026-05-17 | **cross-reference CLOSE-OUT-DEFERRED in /goal pre-flight injector** | observed-in: bc11938c6 | fix: see commit | verify: `git -C H:/prism show bc11938c6`
- 2026-05-17 | **fail-loud stderr log on slot-identity-cache persist failure** | observed-in: 9ea2f9dcf | fix: see commit | verify: `git -C H:/prism show 9ea2f9dcf`
- 2026-05-17 | **wire slot-identity cache into heartbeat() — pre-SDF13/heartbeat-only chats never got a cache file (3/8 live peers drifting)** | observed-in: 9f47f18ca9 | fix: see commit | verify: `git -C H:/prism show 9f47f18ca9`
- 2026-05-17 | **restore EDM_PHYSICS.kunieda volumetric-efficiency block (-10 TS2339)** | observed-in: 36671c740 | fix: see commit | verify: `git -C H:/prism show 36671c740`
- 2026-05-17 | **unknown-bridge for 10 WEDM safety-gate engine calls (-10 TS2345)** | observed-in: 86a06e8e3 | fix: see commit | verify: `git -C H:/prism show 86a06e8e3`
- 2026-05-17 | **v1.2 same-day fix detection** | observed-in: 2e5dd1397 | fix: see commit | verify: `git -C H:/prism show 2e5dd1397`
- 2026-05-17 | **align mlDispatcher with engine input schemas (-7 errors)** | observed-in: 944aa77a3 | fix: see commit | verify: `git -C H:/prism show 944aa77a3`
- 2026-05-17 | **rate-limit doctrine reminder (~50 fires/session -> 1)** | observed-in: 6409714df | fix: see commit | verify: `git -C H:/prism show 6409714df`
- 2026-05-17 | **strip comments+strings+dates before magic-number scan** | observed-in: 5d02ecb50 | fix: see commit | verify: `git -C H:/prism show 5d02ecb50`
- 2026-05-17 | **lower inject threshold + rate-limit** | observed-in: b459870a2 | fix: see commit | verify: `git -C H:/prism show b459870a2`
- 2026-05-17 | **META-tool schema-read-blindness, again — `high-roi-skill-rank.mjs` (shipped this session) read `j.totals.{offloaded,keptOnClaude}` against an `ollama-offload-stats.json` schema v2.0.0 that emits those fields top-level.** Result: META reported `offloaded=0, kept=0, ratio=n/a` against a working route (real: `offloaded=65, kept=744, ratio=8.0%, tokensSaved=14580`). The audit's F1 finding was published as "dead-route CRITICAL" with a 2M-tokens/month ROI claim — both refuted by the peer reviewer (Phase 4B `/forge-audit-v2`) in the SAME session before the audit closed. 4 poisoned history entries were also propagated to `state/shared/high-roi-skill-history.jsonl` before the catch. **Same class as 2026-05-16 juliett's "3 META-tool calculation bugs ... assumed a schema without reading the file first"** — recurring class, 1 day apart. Schema-read-first must be a reflex, not a discipline. The /forge-audit-v2 peer-reviewer pattern (Boris Phase 4B in worktree isolation) IS what caught it — keep that pattern hard. | fix: U-HRSR-SCHEMA-V2 (this session, slot lima, in `state/shared/specs/HIGH-ROI-SKILL-ROUTING-AUDIT-2026-05-17.md`) — `readOllamaStats` now schema-probes via `j.schemaVersion` + `"totals" in j` before assuming shape; falls through both v1 (`totals.*`) and v2 (top-level) paths; reports `schemaV` in output for visual confirmation. Poisoned history pruned. Audit F1 reframed from "dead-route CRITICAL" to "under-tuned WARN" with corrected baseline (8.0% vs 30% target). | observed-by: claude-88486e9e slot lima `/forge-audit-v2 high roi skills + obsidian/ollama routing` — peer reviewer agent `a6be0f8474b62402b` caught it. | verify: `node -e "const j=require('./mcp-server/data/state/ollama-offload-stats.json');console.log(j.offloaded,j.keptOnClaude,'totals?', 'totals' in j)"` → `65 744 totals? false`; `node scripts/high-roi-skill-rank.mjs --json | jq '.ollama.ratio'` → ≈ 0.08 (not null).
- 2026-05-17 | **`generate-system-viz.mjs` and `regen-viz.mjs` both write `state/shared/system-viz/system-graph.json` — last-writer-wins clobber.** They are two INDEPENDENT generators of two DIFFERENT products: `generate-system-viz.mjs` emits the ~20K-node 10-layer architecture graph (`schemaVersion:"2.1.0"`, carries `meta.coverage`); `regen-viz.mjs` emits the ~372K-node merged filesystem-coverage graph (carries `fsCoverage`, no `schemaVersion:"2.1.0"`). `regen-viz.mjs` does NOT invoke `generate-system-viz.mjs` (verified — 0 references). Whichever ran last owns the path. Consequence: running `generate-system-viz.mjs` standalone — which VIZ-COVERAGE-MS0's own acceptance criteria REQUIRE ("Regenerated viz reports...") — silently wipes the regen-viz graph, so `detect-system-viz-drift.mjs` then reports "no fsCoverage entries in graph" and the `/system-viz` 372K map is degraded to the 20K base until the next regen-viz cron. The CLAUDE-BRIEF "372,731 nodes" headline and the awareness tooling all expect the regen-viz product. | fix: pending — a follow-up unit should give `generate-system-viz.mjs` its own distinct OUT_FILE (e.g. `architecture-graph.json`) OR determine one generator is legacy and retire it. Interim: after running `generate-system-viz.mjs`, re-run `node scripts/regen-viz.mjs` to restore the canonical graph. | observed-by: claude-58bd7f4e slot echo, /loop VIZ-COVERAGE-MS0 verification. `viz-domain-coverage.test.mjs` test 18 was hardened to skip-loud when the shared path holds the non-2.1.0 product. | verify: `node -e "const g=require('./state/shared/system-viz/system-graph.json');console.log(g.schemaVersion, (g.nodes||[]).length, !!g.fsCoverage)"` — schemaVersion "2.1.0" + ~20K + no fsCoverage means generate-system-viz won the path.
- 2026-05-17 | **RESOLVED — system-viz L5 engine-domain coverage was hardcoded, not single-source.** `scripts/generate-system-viz.mjs` carried a hand-edited `domainsBuiltIn` array (24 domains, hardcoded engine counts) + a separate `unwiredDomains`/`otherCount` residual catchall. Those counts drifted from `BUILD_STATE.json` — the viz headline and BUILD_STATE reported different wired-engine figures for the same metric (two surfaces, one metric, two answers). | fix: VIZ-COVERAGE-MS0/U-VIZ-COVERAGE-FIX (this session, slot echo) — new pure lib `scripts/lib/viz-domain-coverage.mjs` (`computeDomainCoverage`) derives L5 straight from `BUILD_STATE.COVERAGE_BY_DOMAIN.rows` (canonical table from `build-state-snapshot.mjs::computeCoverageByDomain`). L5 now renders top-40 domains by engine count + 1 aggregated `eng.miscdomains` rest node; every L5 node sums back to the BUILD_STATE total exactly (verified: `meta.coverage`={total:3274,wired:2545,unwired:729,pct:78} === raw row sum, 41 L5 nodes, 0 dup ids). New `meta.coverage` field carries the single-source figure. The legacy hand-rolled `eng.other` catchall was deleted (it would collide with BUILD_STATE's literal "Other" domain → `eng.other`). 18-case `node:test` suite (`scripts/lib/viz-domain-coverage.test.mjs`) — conservation invariant + real-BUILD_STATE + generated-graph integration. 3-of-3 per-file scrutiny: PASS×2 round 3. | known follow-up (P2, envelope): the `dispatcherToDomains` L4→L5 edge heuristic still emits old semantic tokens (cad/cam/wedm/safety/...) — 9 now have no L5 node; the loop WARNS them loudly (R12) rather than silently dropping. Re-mapping the heuristic to the prefix taxonomy is deferred. | verify: `node --test H:/prism/scripts/lib/viz-domain-coverage.test.mjs` → 18/18; `node -e "const g=require('./state/shared/system-viz/system-graph.json');console.log(JSON.stringify(g.meta.coverage))"`.
- 2026-05-17 | **`readDockerHealth` (FLEET-REAPER-MS1.1) only mirrored `parsed.services.*` but the real `ollama-docker-health.mjs` probe emits `docker` + `ollama` as TOP-LEVEL keys (only `{qdrant,postgres,prometheus}` under `services`).** Net: `services.docker` was NEVER populated for any real payload → `dockerHealth.available` permanently `false` (→ spurious "docker down but ollama reachable" caveat on every real run), and the new Tier-2 `serviceRestartAction` daemon-down safety guard (`svc.docker && svc.docker.up===false`) was DEAD in production — with `PRISM_FLEET_REAPER_SERVICE_RESTART=1` it would `docker restart` against a dead daemon, the exact destructive action the invariant forbids. Hermetic Tier-2 tests all passed because their `HEALTH()` fixture fabricated `docker` *inside* `services` — a shape the producer never emits (the "hermetic fakes don't prove production wiring" class, same as RGS-TOOL-AUTOINVOKE-MS1). Caught by per-file scrutiny Reviewer A (code-analyzer) before ship. | fix: U-FR-TIER2-SERVICE-RESTART (commit this session, slot alpha) — `readDockerHealth` now folds top-level `parsed.docker`/`parsed.ollama` into the normalized `services` map (back-compat: an explicit `parsed.services.docker` is NOT overwritten); `available` derives correctly; added 3 real-producer-shape E2E tests (`REAL_PROBE()` → `readDockerHealth → serviceRestartAction → restartWedgedServices`) — the fail-on-revert regression oracle the hermetic suite lacked. Reviewer B independently verified end-to-end PASS. Side-effect: fixes the latent MS1.1 spurious-caveat bug too. | observed-by: claude-23c10eea slot alpha per-file scrutiny. Lesson: a pure-core + injected-readers design MUST ship ≥1 real-producer-shape E2E. | verify: `node --test H:/prism/scripts/__tests__/fleet-reaper-service-restart.test.mjs` → 19/19; `node -e "import('./scripts/fleet-reaper-sweep.mjs').then(m=>{const d=m.readDockerHealth({runHealthProbe:()=>JSON.stringify({docker:{up:false},services:{qdrant:{up:false}}})});console.log(d.services.docker.up===false)})"` → `true`.
- 2026-05-17 | **`scripts/regen-viz.mjs --full` silently corrupted downstream artifacts when the merge subprocess SIGKILLed under host memory pressure.** Parent emitted `[regen-viz] ✗ merge failed` with ZERO captured stderr (signal-kill leaves no V8 message; `stdio: "inherit"` was already set but the kernel reaped the child before V8 could print), then *continued* through 7 post-merge stages (`repair-graph-engine-classification` → `dedup-graph-nodes` → `reparent-viz-categories` → `add-parent-contains-edges` → `system-viz-obsidian-bridge-v2` → `generate-executive-briefing` → `generate-wiki-debt-worklist` → drift-gate), ALL reading the *stale* pre-merge `system-graph.json` (~99K nodes instead of expected ~145K). `EXECUTIVE-BRIEFING.md` / `WIKI-DEBT-WORKLIST.md` / `obsidian-augmentation.json` published with stale headline metrics; drift-gate falsely certified "clean" because stale ≠ truncated. Script exited 1 (already fail-loud on `failed > 0`) — cron caught the signal but artifacts were *already* corrupted. Karpathy R12 — silent corruption masquerading as a recoverable failure. Reproduced 2026-05-17 lima session 77971357 (`.tmp-regen-viz-lima-full.log`). | fix: U-REGEN-VIZ-MERGE-FAILLOUD (commit `f9dc218d78`, 2026-05-17 lima) — new pure helper `scripts/lib/regen-viz-merge-guard.mjs` `decideMergePostState({mergeStatus, mergeSignal, preMergeNodeCount, postMergeNodeCount, augTotalBytes})` returning `{abort, exitCode, reason, message}`. Four paths: merge-fail (exit 2), silent-no-op (exit 3, defined as `augBytes ≥ 1MB && pre > 0 && post ≤ pre`), continue, default. Orchestrator snapshots pre+post node count, calls guard, `process.exit(guard.exitCode)` on abort — post-merge stages never run against a stale graph. 19/19 tests PASS via `node:test`. Per-file 2-reviewer gate (arm A: code-analyzer, arm B: independent reviewer): both PASS, 0 P0/P1. **Spec lesson**: the prior-session spec at `state/shared/specs/U-REGEN-VIZ-MERGE-FAILLOUD-FIX-PLAN-2026-05-17.md` claimed 4 fixes; 3 were ALREADY in place (heap arg `--max-old-space-size=16384`, `stdio: "inherit"`, fail-loud exit). Re-reading the actual code at HEAD invalidated 3 of 4 spec claims — handoff RESUMEs are working hypotheses, not ground truth. | observed-by: claude-77971357 slot lima `/checkin-lima /loop all lima work`. P2 follow-ups for separate units: streaming node-count reader (avoid 2× JSON.parse on 153MB graph), atomic-write in `merge-augmentations.mjs:1430`, extending fail-loud to repair/dedup/reparent/parent-edges/executive-briefing stages. Memory: [[reference_u_regen_viz_merge_faillod_2026_05_17]]. | verify: `node --test H:/prism/scripts/lib/regen-viz-merge-guard.test.mjs` → 19/19 PASS; `grep -c decideMergePostState H:/prism/scripts/regen-viz.mjs` ≥ 1.
- 2026-05-16 | **handoff-driven slot pinning (closes bravo->delta drift)** | observed-in: d7631452b | fix: see commit | verify: `git -C H:/prism show d7631452b`
- 2026-05-16 | **prototype-form ReturnType + generateProgram rename (-6)** | observed-in: 623022ca5 | fix: see commit | verify: `git -C H:/prism show 623022ca5`
- 2026-05-16 | **z.input<> for generate+predictCount param types (-8)** | observed-in: 97edb4179 | fix: see commit | verify: `git -C H:/prism show 97edb4179`
- 2026-05-16 | **[MAIN] [TSC-FIX]/routes/milling: return next(e) consistency + merge enrichSpeedFeed 3rd arg (-7)** | observed-in: 683a255d5 | fix: see commit | verify: `git -C H:/prism show 683a255d5`
- 2026-05-16 | **3 wiring-gate test suites (36/36 PASS)** | observed-in: 964454db9 | fix: see commit | verify: `git -C H:/prism show 964454db9`
- 2026-05-16 | **1199: taylorLife 3-arg + preserve coating multiplier (-1)** | observed-in: 711713029 | fix: see commit | verify: `git -C H:/prism show 711713029`
- 2026-05-16 | **2802: direct import of CostSavingsTrackerEngine (-1)** | observed-in: 33245533f | fix: see commit | verify: `git -C H:/prism show 33245533f`
- 2026-05-16 | **2721: drop stale 2nd arg to slimResponse (-1)** | observed-in: 6b768b29f | fix: see commit | verify: `git -C H:/prism show 6b768b29f`
- 2026-05-16 | **4 discriminated-union narrowings (-5)** | observed-in: ce873f7e2 | fix: see commit | verify: `git -C H:/prism show ce873f7e2`
- 2026-05-16 | **824: explicit discriminant narrowing on broadcast result (-2)** | observed-in: f1681107c | fix: see commit | verify: `git -C H:/prism show f1681107c`
- 2026-05-16 | **1180: unknown-bridge for LatheOptimizationConstraints (-1)** | observed-in: f28fce374 | fix: see commit | verify: `git -C H:/prism show f28fce374`
- 2026-05-16 | **[MAIN] [TSC-FIX]/MachiningPlaybook+PlaybookRules: add getAllRules() canonical API (-1)** | observed-in: 12f4cd0d4 | fix: see commit | verify: `git -C H:/prism show 12f4cd0d4`
- 2026-05-16 | **canonical field renames + drop non-existent keys (-2)** | observed-in: fc864822f | fix: see commit | verify: `git -C H:/prism show fc864822f`
- 2026-05-16 | **WIRE-EXEMPT + 15-case test + 2 latent-bug fixes** | observed-in: f7fd9b29b | fix: see commit | verify: `git -C H:/prism show f7fd9b29b`
- 2026-05-16 | **revive auto-router for /-prefixed prompts** | observed-in: 66aa07afa | fix: see commit | verify: `git -C H:/prism show 66aa07afa`
- 2026-05-16 | **353: search() returns array, not envelope (-1)** | observed-in: 96df2c778 | fix: see commit | verify: `git -C H:/prism show 96df2c778`
- 2026-05-16 | **420: stats().total in place of getAllRules() (-1)** | observed-in: 6755e7f9d | fix: see commit | verify: `git -C H:/prism show 6755e7f9d`
- 2026-05-16 | **the permanent fix — bump transcript freshness 5min → 4h** | observed-in: b3d7693bd | fix: see commit | verify: `git -C H:/prism show b3d7693bd`
- 2026-05-16 | **PlaybookRule shape + RuleCategory literal (-2)** | observed-in: 1131b3713 | fix: see commit | verify: `git -C H:/prism show 1131b3713`
- 2026-05-16 | **3264: unknown-bridge for OutcomeRecord (-1)** | observed-in: 5a560716a | fix: see commit | verify: `git -C H:/prism show 5a560716a`
- 2026-05-16 | **14-case behavioral suite clears wiring gate** | observed-in: ef7235297 | fix: see commit | verify: `git -C H:/prism show ef7235297`
- 2026-05-16 | **selectOptimalStrategy 3-arg + featuretype/kinematics rename (-1 net)** | observed-in: 9562a197d | fix: see commit | verify: `git -C H:/prism show 9562a197d`
- 2026-05-16 | **realign 5 engine adapter calls (-5)** | observed-in: fc8c96cb7 | fix: see commit | verify: `git -C H:/prism show fc8c96cb7`
- 2026-05-16 | **rename primary_strategy→selected_strategy (-4)** | observed-in: a0228c8db | fix: see commit | verify: `git -C H:/prism show a0228c8db`
- 2026-05-16 | **window-PID liveness gate — slot stays locked as long as the terminal window is open** | observed-in: f2156e582 | fix: see commit | verify: `git -C H:/prism show f2156e582`
- 2026-05-16 | **[MAIN] [OBSIDIAN-INTELLIGENCE-MS3]/E1+E4-ENVELOPE-DRIFT-FIX: flip status=completed (re-verified 49/49 tests PASS)** | observed-in: 28ac3ff50 | fix: see commit | verify: `git -C H:/prism show 28ac3ff50`
- 2026-05-16 | **ToolGeometry + TaskCategoryT casts via unknown bridge (914->908, -6)** | observed-in: e62ebadbe | fix: see commit | verify: `git -C H:/prism show e62ebadbe`
- 2026-05-16 | **wire tribal corpus into MillingAGIMasterEngine (audit finding #3)** | observed-in: cdad09490 | fix: see commit | verify: `git -C H:/prism show cdad09490`
- 2026-05-16 | **rotation_order 2-char rotary pairs + MaterialProps literal (total 941->932)** | observed-in: 9eda303f5 | fix: see commit | verify: `git -C H:/prism show 9eda303f5`
- 2026-05-16 | **complete 11 MaterialProps literals (total 952->941)** | observed-in: 99286ba87 | fix: see commit | verify: `git -C H:/prism show 99286ba87`
- 2026-05-16 | **live wired-engine resolver (CLAUDE-BRIEF #1 gap)** | observed-in: 2513098c8 | fix: see commit | verify: `git -C H:/prism show 2513098c8`
- 2026-05-16 | **eval-harness honesty fix + reproducible U4 checkpoint** | observed-in: 51b4c66bf | fix: see commit | verify: `git -C H:/prism show 51b4c66bf`
- 2026-05-16 | **kc1_1->kc11_mpa 7 of 15 (total 959->952)** | observed-in: 872ad577d | fix: see commit | verify: `git -C H:/prism show 872ad577d`
- 2026-05-16 | **mark 2 FiveAxis engines as intentional internal-layer** | observed-in: a95ac0286 | fix: see commit | verify: `git -C H:/prism show a95ac0286`
- 2026-05-16 | **[MAIN] [TSC-FIX]/FiveAxis MaterialProps + kinematics field-rename (1015->1003 net)** | observed-in: 3591c991d | fix: see commit | verify: `git -C H:/prism show 3591c991d`
- 2026-05-16 | **activate the per-slot branch system — fix juliett/lima drift + /checkin Step 2c cutover** | observed-in: b8dfbf208 | fix: see commit | verify: `git -C H:/prism show b8dfbf208`
- 2026-05-16 | **10 clean fixes (15->5 errors)** | observed-in: 832400d48 | fix: see commit | verify: `git -C H:/prism show 832400d48`
- 2026-05-16 | **post-ship audit punch-list (10 P0 integration bugs)** | observed-in: e7e2dbf1b | fix: see commit | verify: `git -C H:/prism show e7e2dbf1b`
- 2026-05-16 | **mark 2 internal-layer engines WIRE-EXEMPT** | observed-in: fa003c123 | fix: see commit | verify: `git -C H:/prism show fa003c123`
- 2026-05-16 | **extractConstraints reads real MachineSpindle fields** | observed-in: 64ab700dc | fix: see commit | verify: `git -C H:/prism show 64ab700dc`
- 2026-05-16 | **fix 4 pre-existing devDispatcher.ts tsc errors** | observed-in: 1f1fec299 | fix: see commit | verify: `git -C H:/prism show 1f1fec299`
- 2026-05-16 | **MEMORY.md crossed the 24,576-byte truncation ceiling — 24,688 B / 100.5% / status=critical**. The U-MEMORY-COMPRESS one-shot fix without a durable watchdog allowed silent re-growth past 22 KB → past 24 KB in ~0 days; the `memory-size-watch.mjs` shipped earlier today exists but ran only on-demand, never gated/scheduled. Fleet-wide recall now actively truncating; freshest memory entries unreachable across all sessions until compressed. | fix: re-run U-MEMORY-COMPRESS protocol (pointer-only index, ≤200 chars/entry) AND wire `scripts/memory-size-watch.mjs` into a Stop hook OR `/loop --interval 1d` for durability. Watchdog gap is the root cause; the one-shot is not enough. | observed-by: claude-416be9ac slot bravo `/forge-audit-v2 look for nodes that need updating reflecting whats now built and optimize their usage` peer-reviewed BLOCK→FIX→SHIP. Audit: `state/shared/specs/STALE-NODES-AUDIT-2026-05-16.md`. META tool: `scripts/node-staleness-rank.mjs` (baseline appended to `state/shared/node-staleness-history.jsonl`). | verify: `node scripts/node-staleness-rank.mjs --json | jq '.memory.bytes,.memory.status'` → bytes < 22000 AND status == "fresh".
- 2026-05-15 | **atomically swap 10 stop_on_* gates → stop-regression-bundle** | observed-in: 62587fb9f | fix: see commit | verify: `git -C H:/prism show 62587fb9f`
- 2026-05-15 | **restore 4 session-continuity hooks (slot-resume was broken fleet-wide)** | observed-in: 5bd70167f | fix: see commit | verify: `git -C H:/prism show 5bd70167f`
- 2026-05-15 | **restore 3 more wiped MINIMAL_ALLOWLIST gates** | observed-in: 4984c918b | fix: see commit | verify: `git -C H:/prism show 4984c918b`
- 2026-05-15 | **restore scrutinize-before-stop (3-of-3 gate was OFF fleet-wide)** | observed-in: 58e89c46e | fix: see commit | verify: `git -C H:/prism show 58e89c46e`
- 2026-05-15 | **spawn writer via process.execPath, not bare "node"** | observed-in: 940f95e43 | fix: see commit | verify: `git -C H:/prism show 940f95e43`
- 2026-05-15 | **stop-regression-bundle (BUILT + tested, NOT wired)** | observed-in: 7e29bcea0 | fix: see commit | verify: `git -C H:/prism show 7e29bcea0`
- 2026-05-15 | **[HOOKS-AUTOMATION-V2]/P0.3-B: fix error-pattern-promote grouping (never promoted)** | observed-in: 049eb81c4 | fix: see commit | verify: `git -C H:/prism show 049eb81c4`
- 2026-05-15 | **[HOOKS-AUTOMATION-V2]/P0.1: fix memory-relevance-inject 0% fleet-wide recall** | observed-in: 2a5b60cfd | fix: see commit | verify: `git -C H:/prism show 2a5b60cfd`
- 2026-05-15 | **classifier category accuracy + Unicode-bypass safety pre-gate** | observed-in: 2bbf12654 | fix: see commit | verify: `git -C H:/prism show 2bbf12654`
- 2026-05-15 | **[MAIN] [OBSIDIAN-INTELLIGENCE-MS3]/D2-FIXUP — close 3-of-3 Arm B+C blockers (parity test + WIRE-EXEMPT + unterminated-fm audit-log fix)** | observed-in: 7c87d2161 | fix: see commit | verify: `git -C H:/prism show 7c87d2161`
- 2026-05-15 | **regression-auto-write.mjs Stop hook + 22 tests** | observed-in: 8ba9a84d7 | fix: see commit | verify: `git -C H:/prism show 8ba9a84d7`

## Back-flowed historical regressions (patch-sibling B1 / U-LIMA-B1, integrated 2026-05-20 by slot lima)

> Source: `state/shared/dashboards/patches/CLAUDE-MD-PATCH-token-savings-audit.md` (authored 2026-05-17 by claude-77971357 when CLAUDE.md was peer-locked). The patch's CLAUDE.md splice never landed; CLAUDE.md is now golf-only (U-OBF-GOLF). All three findings below are resolved-historical — recorded here for the audit trail. The patch's doctrine portion (`scripts/token-savings-rank.mjs` META tool) is wiki-captured at [[audit-token-savings-2026-05-17]].

- 2026-05-17 | **token-savings layer is mostly write-only across the fleet (2 P0 / 5 P1 audit)** | observed-in: state/shared/specs/AUDIT-TOKEN-SAVINGS-2026-05-17.md | fix: leverage-ranked action list — F2 (MEMORY.md trim) + F3 (Ollama threshold 0.90->0.80) + F1 (rtk init -g). META: `node scripts/token-savings-rank.mjs --json --history` | status: RESOLVED — token-savings audit shipped; doctrine wiki-captured.
- 2026-05-17 | **MEMORY.md re-crossed truncation ceiling — 24,603B / -27B headroom** (2nd time in 2 days) | fix: U-MEMORY-COMPRESS-V2 (durable watchdog-auto-compact actuator, not just advisory) | status: RESOLVED — U-MEMORY-COMPRESS-V2 shipped by golf `3798922e49` 2026-05-19.
- 2026-05-17 | **cache reader-path systemically dead — 0 hits across all 3 caches** (96 file-read keys / 0 hits) | fix: trace which hook should PreToolUse:Read consult `.claude/cache/file-read-cache.json` before re-loading | observed-by: peer-reviewer agent a0310b5d6 | status: HISTORICAL — see U-MEMORY-INDEX-SIDECAR / read-optimize work in later sessions.
