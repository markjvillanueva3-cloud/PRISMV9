# AI-SYSTEMS IMPROVEMENT -- ROUTED WORK QUEUE (2026-06-11, slot:zulu orchestrator)

> Operationalizes `DOMAIN-MASTERY-ASSESSMENT-2026-06-11.md` into per-owner, eval-gated units. The /goal "improve AI systems (NN/GNN/LoRA/CAG+RAG/hybrids) across ALL galaxies + synergize with vault/Hermes/PSN/awareness/CLAUDE.md/souls/memories/wikis" is a **fleet-scale, multi-session, domain-owned** effort -- not a single-chat deliverable. Zulu (orchestrator) ROUTES; the specialist slots EXECUTE + eval-gate each unit (R15 WIRE->TEST->VALIDATE).
>
> **Bound loss function (already MET at the structural/data tier):** `node scripts/ai-systems-synergy-goal-gate.mjs` exits 0 -- synergy 34/34 (gaps=0), LoRA 1138 rows/34 galaxies trainingReady, GNN AUROC 0.8084 selective-deploy. This ledger tracks the FRONTIER lift beyond that bound.

## P0 -- safety-truth + binding unblocks (do first)

| # | Owner | Unit | First step | Eval gate (done-signal) |
|---|---|---|---|---|
| 1 | **india** | GNN full-coverage = data, not epochs | Instrument `xproc_outcome_publish` across the 19 consumer galaxies that never emit to OutcomeFeedbackBus | per-galaxy publish count >0; macro-F1 trending 0.439->0.55 (NOTE: india actively looping `8347ba23` -- coordinate, do NOT double-build) |
| 2 | **oscar** | speed-feed `tryBusCapture()` hardwired `return true` (R12) | Wire real OutcomeCaptureBus capture | `speedfeed_outcome_stats.bus_capture_success_rate_pct` truthful + R9 regression test (red pre-fix) |
| 3 | **golf/compliance** | S(x) <0.70 hard-block unverified | E2E dispatcher round-trip test vs live JM G-code samples | test proves block fires at S(x)<0.70 through `prism_safety` |
| 4 | **delta** | cad `CADFeatureRecognitionEngine` stub (gates cad->cam->quoting->india) | Restore real implementation | `feature_recognize`->`cam_strategy_recommend` returns real features through dispatcher |
| 5 | **india** | 6 WIRE_SAFE_DATA dispatcher-dark engines | Wire IntentClassifier/PolicyExperienceLedger/TransferLearning/TemporalReasoning/RealTimeAnomalyDetection/KnowledgeIngestion into `prism_ai` | 6 deterministic DATA actions round-trip (no NN inference) |
| 6 | **juliett** | boot-guard dormant + `PRISM_ROOT`/`import.meta.url` cwd conflict | Activate boot-guard; fix resolver | no silent restart; path-resolution test green |
| 7 | **hotel** | 6 business constants unverified/possibly-inlined | Extract+verify payroll-tax/pto/benefits/customer-terms/vendor-profile/chart-of-accounts | `stop_on_inlined_constants` clears on business engines |

## P1 -- wiring closure (R15 no-orphans) + branch-drift reconcile

| # | Owner | Unit | Eval gate |
|---|---|---|---|
| 8 | **kilo** | wire `eng.mastercam` (L5/unwired) + ship 6 SFC->CAM bridges | mastercam round-trip; 6 bridges connect oscar speeds/feeds into CAM cycle params |
| 9 | **whiskey** | merge slot/whiskey 6-file brain + wire lathe BATCH12-15 (38 engines) | shared-tree brain canonical; 38 engines in `prism_turning` |
| 10 | **echo** | reconcile slot/echo 12 commits + wire 8 dark post engines + JM wire-EDM .cps | byte-equivalence vs golden NC archive |
| 11 | **hotel** | wire `HotelERPTribalKnowledgeEngine` (confirmed orphan) + seed real JM customers | 17 tribal categories reachable; placeholder customer fields replaced |
| 12 | **foxtrot** | close 7 unwired mill engines (U-BRIDGE-WIRE-MILL) + fresh mill program corpus | ghost-roost unwired count -7; mislabeled programs replaced |
| 13 | **xray** | `format:"json"` constrained decode for qwen2.5vl OCR | A/B recovers ~30% excluded prints (3-of-3, safety-relevant shared lib) |
| 14 | **sierra/tango** | wire ~10 untracked domains into ROADMAP-CONSOLIDATED; reconcile 4 drifted worktrees | ghost-roost + SVI backlog cover all 34; 0 unmerged canonical brains |

## Zulu's own-lane unit (hermes-zulu, deferred to post-rate-limit)
- **A-06 galaxy-brain-read consumer API** -- injectors read galaxy-LOCAL synthesis, not the MASTER brain; build a `galaxy-brain-read` dispatcher action for cross-galaxy compound recall. Blocked NOW: needs the 3-of-3 scrutiny gate (reviewer agents fail on the active session limit). Resume after 7:50pm CT.

## Resume / completion
- 8 unassessed domains (incl **wedm**): `Workflow({scriptPath:".../domain-mastery-assessment-wf_9c28241d-daa.js", resumeFromRunId:"wf_9c28241d-daa"})` after 7:50pm CT.
- This queue is the orchestrator's executable output; each owning slot picks up its row via `/checkin-<slot>` and eval-gates per R15.
