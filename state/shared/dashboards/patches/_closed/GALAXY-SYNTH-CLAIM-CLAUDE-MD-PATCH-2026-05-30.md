> **✅ ALREADY-DONE (triage `wq31b7vsz`, 2026-06-02).** Self-gated on a 'BRAIN COMPOUNDING' CLAUDE.md section that was never created; full record already in wiki [[galaxy-synthesis-claim-ledger]] + memory; KNOWLEDGE-VAULT path keeps a rank-6 sub-unit wiki-only. CLOSED.

# PATCH-SIBLING — CLAUDE.md pointer for galaxy-synthesis-claim ledger (rank 6)

**For:** golf / integrator (CLAUDE.md is a high-contention shared-tree surface; alpha writes the
patch-sibling per the patch-sibling convention rather than racing the edit).
**By:** claude-da9aacf5 slot alpha · 2026-05-30 · commit (see git log for `U-GALAXY-SYNTH-CLAIM`).
**Advisory** — apply when convenient; nothing breaks if it lands late (the wiki + Obsidian memory
already carry the full record).

## Where
The brain-compounding stack (amplifiers #1 L2 / #2 incremental-refresh / brain-refresh) is currently
documented in wiki + Obsidian memory, NOT in CLAUDE.md. If a consolidated CLAUDE.md "BRAIN
COMPOUNDING" pointer section is ever created, add this one-liner; otherwise this can stay
wiki-only (a rank-6 sub-unit does not independently warrant its own CLAUDE.md section per the
KNOWLEDGE VAULT promotion path — memory→wiki, CLAUDE.md only for load-bearing doctrine).

## Suggested one-line pointer (if/when a brain-compounding section exists)

```markdown
- **Galaxy synthesis-claim ledger (rank 6, 2026-05-30 alpha)** — fleet mutex for parallel galaxy
  synthesis. `scripts/galaxy-synthesis-claim.mjs` (clone of slot-task-claim atomic-RMW; store
  `state/shared/galaxy-synthesis-claims.json` keyed `galaxy@sourceHash`); wired fail-open into
  `galaxy-synthesis-refresh.mjs` (`PRISM_GALAXY_SYNTH_CLAIM_DISABLE=1` reverts byte-identical).
  Precondition for amplifier #3 (20-chat distributed synthesis) — generation-coordination only;
  sidecar-rebuild coalescing is a separate un-built lever. Wiki: [[galaxy-synthesis-claim-ledger]].
  Memory: [[reference_alpha_galaxy_synthesis_claim_2026_05_30]].
```
