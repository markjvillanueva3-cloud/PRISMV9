> **✅ ALREADY-DONE (triage `wq31b7vsz`, 2026-06-02).** Doctrine live via primary channel — feedback_plot_path_capture_working_path.md auto-feeds Obsidian + path-ledger.mjs exists; the MEMORY.md pins were always optional 'DURABLE reinforcement', and C: MEMORY.md is 21931B (near the 24576B truncation ceiling). CLOSED — do not re-pin.

# MEMORY-MD-PATCH — WORKING-PATH-CAPTURE-MS0 (U-WPC-PROPAGATE + U-WPC-MASTER-BRAIN + U-WPC-INDIA-COORD)

> PATCH-SIBLING for the next editor(s) of the master `MEMORY.md` and the per-galaxy `MEMORY.md`
> files (all peer-locked / write-blocked from the alpha slot worktree). Alpha wrote this 2026-05-31.
> The PRIMARY propagation is already live: the feedback memory `feedback_plot_path_capture_working_path.md`
> auto-feeds Obsidian via `stop-obsidian-memory-feed.mjs` → all galaxies recall it on their next prompt
> via `master-index-precheck-inject` / `memory-relevance-inject`. These MEMORY.md pins are the DURABLE
> per-surface reinforcement — splice when each surface is free.

---

## 1. Master `MEMORY.md` (C:/Users/wompu/.claude/projects/H--prism/memory/MEMORY.md) — master-brain pointer

Splice ONE line under "## Standing doctrine (feedback_*)" (master-brain discovery edge for the rule):

```
- [plot path → capture working-path → compound-learn](feedback_plot_path_capture_working_path.md) — fleet rule: track movements; capture proven path; replay (autonomous CAD/CAM) + feed india's bus; compound across galaxies. Mechanism scripts/lib/path-ledger.mjs. Wiki [[working-path-capture]].
```

## 2. Every galaxy `MEMORY.md` (mcp-server/src/engines/<galaxy>/MEMORY.md) — per-galaxy Standing-focus pin

Each galaxy owner splices this one-liner into their `## Standing focus` (or `## Standing doctrine`):

```
- **Working-path capture (fleet rule [[feedback_plot_path_capture_working_path]]):** plot your path; on a proven goal, `path-ledger.captureWorkingPath` + `emitLearningRow` (→ india's outcome-bus) + replay via `toExecutionPlan`. This galaxy's proven paths compound the fleet learner.
```

## 3. india galaxy `MEMORY.md` (mcp-server/src/engines/ai-training/MEMORY.md) — U-WPC-INDIA-COORD

Splice under india's "## Cross-galaxy bridges" (coordination — WorkingPath rows now flow into india's bus):

```
- `scripts/lib/path-ledger.mjs` (alpha, WORKING-PATH-CAPTURE-MS0) — `emitLearningRow` writes WorkingPath-completion rows to your OutcomeFeedbackBus (`state/shared/outcome-bus.jsonl`, same schema as `outcome-bus-auto-tap.mjs`; `source:"path-ledger"`, `tool:"WorkingPath"`, additive `working_path_id`/`steps_count`/`score`). These are proven goal-trajectories per (domain,goalType) — high-value RL/GNN training rows. INDIA OWNS the ingestion: confirm/extend the retrain consumer (`nn-graph-retrain-lifecycle.mjs` corpus) to pick up `source:"path-ledger"` rows so the path-SELECTION model self-improves (acceleration plan Lever 3, `state/shared/specs/PATHING-ACCELERATION-PLAN-2026-05-31.md`). Alpha emits; india ingests — per your "learning signal goes through india" doctrine.
```

---

## Recent regressions (none — additive doctrine + new pure-core lib; no behavior changed)
