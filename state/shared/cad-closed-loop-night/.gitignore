# Raw H:-drive enumeration manifests (regenerable via filter-canonical.mjs + the enumeration sweep;
# ~90MB total) and per-night runtime artifacts stay out of git. Counts live in the committed JSONs.
manifest-*.txt
logs/
night-summary.jsonl
