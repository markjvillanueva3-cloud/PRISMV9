warning: in the working copy of 'mcp-server/data/docs/COMPACTION_SNAPSHOT.md', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/data/docs/DISPATCHER_DIGEST.md', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/data/docs/ENGINE_DIGEST.md', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/data/docs/sandvik-gc-turning-sample.txt', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/data/extracted-knowledge/debug/sandvik-cutting-lines.txt', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/data/extracted-knowledge/debug/sandvik-sample.txt', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/data/state/QA-MS13/typescript-error-audit.json', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/src/__tests__/kar-ms1-hooks.test.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/src/__tests__/ppg-haas-hurco-validation.test.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/src/__tests__/stochastic-edm.test.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/src/algorithms/SurfaceFinishPredictor.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/src/data/wedm-published-machines.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/src/engines/ChainFailureRecoveryEngine.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/src/engines/MillPatternMinerEngine.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/src/engines/SpeedFeedOrchestratorEngine.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/src/engines/TribalKnowledgeAdvisorEngine.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/src/engines/index.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/src/schemas/businessActionSchemas.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/web/scripts/calculator-live-variability-sweep.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/web/src/__tests__/AuditManagerPage.test.tsx', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/web/src/__tests__/LatheResultsPage.test.tsx', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/web/src/__tests__/LatheUploadPage.test.tsx', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/web/src/__tests__/ShopFloorLivePage.test.tsx', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/web/src/__tests__/WireEdmWizardPage.test.tsx', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/web/src/api/scheduling.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/web/src/api/scientificMath.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/web/src/data/calculatorWorkspace.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/web/src/features/machine-workspace/programmingAuthorityContract.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/web/src/pages/MaintenanceWorkOrderPage.tsx', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'mcp-server/web/src/pages/ShippingPackingPage.tsx', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'state/SESSION_DIGEST.md', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'state/SESSION_STATE.md', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'state/pre_build_snapshot.json', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'state/shared/CLAUDE-CODEX-TASK-QUEUE-DIRECTIVE.md', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'state/shared/SYSTEM-CAPABILITIES.md', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'state/todo.md', LF will be replaced by CRLF the next time Git touches it
diff --git a/mcp-server/data/docs/COMPACTION_SNAPSHOT.md b/mcp-server/data/docs/COMPACTION_SNAPSHOT.md
index b61508b10..87164ee75 100644
Binary files a/mcp-server/data/docs/COMPACTION_SNAPSHOT.md and b/mcp-server/data/docs/COMPACTION_SNAPSHOT.md differ
diff --git a/mcp-server/data/docs/DISPATCHER_DIGEST.md b/mcp-server/data/docs/DISPATCHER_DIGEST.md
index 48bc4e684..c192d82ac 100644
Binary files a/mcp-server/data/docs/DISPATCHER_DIGEST.md and b/mcp-server/data/docs/DISPATCHER_DIGEST.md differ
diff --git a/mcp-server/data/docs/ENGINE_DIGEST.md b/mcp-server/data/docs/ENGINE_DIGEST.md
index e59c66a05..f3ed8370c 100644
Binary files a/mcp-server/data/docs/ENGINE_DIGEST.md and b/mcp-server/data/docs/ENGINE_DIGEST.md differ
diff --git a/mcp-server/data/state/QA-MS13/typescript-error-audit.json b/mcp-server/data/state/QA-MS13/typescript-error-audit.json
index 2e1d8b147..3f36722c4 100644
Binary files a/mcp-server/data/state/QA-MS13/typescript-error-audit.json and b/mcp-server/data/state/QA-MS13/typescript-error-audit.json differ
diff --git a/mcp-server/src/__tests__/kar-ms1-hooks.test.ts b/mcp-server/src/__tests__/kar-ms1-hooks.test.ts
index 4a7c83b99..f5cef5015 100644
Binary files a/mcp-server/src/__tests__/kar-ms1-hooks.test.ts and b/mcp-server/src/__tests__/kar-ms1-hooks.test.ts differ
diff --git a/mcp-server/src/__tests__/ppg-haas-hurco-validation.test.ts b/mcp-server/src/__tests__/ppg-haas-hurco-validation.test.ts
index a41d33601..1864d6142 100644
Binary files a/mcp-server/src/__tests__/ppg-haas-hurco-validation.test.ts and b/mcp-server/src/__tests__/ppg-haas-hurco-validation.test.ts differ
diff --git a/mcp-server/src/__tests__/stochastic-edm.test.ts b/mcp-server/src/__tests__/stochastic-edm.test.ts
index df3c69a8e..6e2802b6c 100644
Binary files a/mcp-server/src/__tests__/stochastic-edm.test.ts and b/mcp-server/src/__tests__/stochastic-edm.test.ts differ
diff --git a/mcp-server/src/algorithms/SurfaceFinishPredictor.ts b/mcp-server/src/algorithms/SurfaceFinishPredictor.ts
index 34f82aaf4..873fceaf7 100644
--- a/mcp-server/src/algorithms/SurfaceFinishPredictor.ts
+++ b/mcp-server/src/algorithms/SurfaceFinishPredictor.ts
@@ -39,7 +39,7 @@ import {
 
 import type { ISOGroup } from "../physics/constants.js";
 
-// ─── Input Interface ─────────────────────────────────���─────────────
+// ─── Input Interface ─────────────────────────────────────────────────
 
 /**
  * Surface finish predictor input parameters
@@ -91,7 +91,7 @@ export interface SurfaceFinishOutput extends AlgorithmOutput {
   Ra_composite: AtomicValue<number>;
 }
 
-// ──��� Material Correction Factors ───────────────────────────────────
+// ───── Material Correction Factors ───────────────────────────────────
 
 /**
  * Material correction factors for surface finish.
@@ -126,7 +126,7 @@ const BUE_SPEED_RANGE: Record<ISOGroup, { min: number; max: number }> = {
   H: { min: 50, max: 120 },  // Hardened: minimal BUE with CBN
 };
 
-// ─── Valid Ranges ─────────────────────────────────────────────���────
+// ─── Valid Ranges ────────────────────────────────────────────────────
 
 const VALID_RANGES = {
   feed_mm: { min: 0.01, max: 2.0, unit: "mm" },
diff --git a/mcp-server/src/data/wedm-published-machines.ts b/mcp-server/src/data/wedm-published-machines.ts
index 59136a7ba..d33532f4b 100644
--- a/mcp-server/src/data/wedm-published-machines.ts
+++ b/mcp-server/src/data/wedm-published-machines.ts
@@ -53,7 +53,7 @@ export interface WEDMMachineSpec {
 export const WEDM_MACHINE_SPECS: WEDMMachineSpec[] = [
   // ═══════════════════════════════════════════════════════════════════════════
   // MITSUBISHI
-  // ════════════════════════════════════��══════════════════════════════════════
+  // ════════════════════════════════════════════════════════════════════════════
   {
     manufacturer: "Mitsubishi",
     model: "MV1200-S Advance M800",
@@ -94,9 +94,9 @@ export const WEDM_MACHINE_SPECS: WEDMMachineSpec[] = [
     source: "https://www.mcmachinery.com/product/mv2400-r-advance-plus-m800-wire-edm/",
   },
 
-  // ���══════════════════════════════════════════════════════════════════════════
+  // ═════════════════════════════════════════════════════════════════════════════
   // SODICK
-  // ════��═══════════════════════��══════════════════════════════════════════���═══
+  // ═══════════════════════════════════════════════════════════════════════════════
   {
     manufacturer: "Sodick",
     model: "VL400Q",
@@ -136,9 +136,9 @@ export const WEDM_MACHINE_SPECS: WEDMMachineSpec[] = [
     source: "https://sodick.com VL series brochure",
   },
 
-  // ═���════════════════════════════════════════════════��════════════════════════
+  // ══════════════════════════════════════════════════════════════════════════════
   // MAKINO
-  // ═══════════���═══════════════���═════════════════════════════════��═════════════
+  // ════════════════════════════════════════════════════════════════════════════════
   {
     manufacturer: "Makino",
     model: "U6",
@@ -194,9 +194,9 @@ export const WEDM_MACHINE_SPECS: WEDMMachineSpec[] = [
     source: "https://www.makino.com/en-us/machine-technology/machines/wire-edm/u86",
   },
 
-  // ══════��════════════════════════════════════════════════════════════════════
+  // ════════════════════════════════════════════════════════════════════════════
   // AGIECHARMILLES (GF MACHINING)
-  // ═════════════���═════════════════════════════════════════════════════════════
+  // ═════════════════════════════════════════════════════════════════════════════
   {
     manufacturer: "AgieCharmilles",
     model: "CUT P 350",
@@ -237,9 +237,9 @@ export const WEDM_MACHINE_SPECS: WEDMMachineSpec[] = [
   },
 ];
 
-// ════���════════════════════════════════════════════════════��═════════════════
+// ══════════════════════════════════════════════════════════════════════════════
 // PUBLISHED CUTTING CONDITION DATA
-// ════════════════════════════════════════════════════��══════════════════════
+// ════════════════════════════════════════════════════════════════════════════
 
 /**
  * Published wire EDM cutting speeds by material and thickness.
@@ -382,7 +382,7 @@ export const PUBLISHED_CUTTING_CONDITIONS: PublishedCuttingCondition[] = [
 
 // ═══════════════════════════════════════════════════════════════════════════
 // PUBLISHED SPARK GAP AND OFFSET DATA
-// ═══════════════��═══════════════════════════════════════════════════════════
+// ════════════════════════════════════════════════════════════════════════════
 
 /**
  * Published spark gap (overcut) values.
diff --git a/mcp-server/src/engines/ChainFailureRecoveryEngine.ts b/mcp-server/src/engines/ChainFailureRecoveryEngine.ts
index e0d1a9c3c..7e0985be8 100644
--- a/mcp-server/src/engines/ChainFailureRecoveryEngine.ts
+++ b/mcp-server/src/engines/ChainFailureRecoveryEngine.ts
@@ -285,7 +285,7 @@ export class ChainFailureRecoveryEngine {
     return capped;
   }
 
-  // ── User Notification Generation ─────���─────────────────────
+  // ── User Notification Generation ─────────────────────────────
 
   /**
    * Generate a structured user notification for a chain failure.
diff --git a/mcp-server/src/engines/MillPatternMinerEngine.ts b/mcp-server/src/engines/MillPatternMinerEngine.ts
index 271ccdfe6..d51bbe22a 100644
--- a/mcp-server/src/engines/MillPatternMinerEngine.ts
+++ b/mcp-server/src/engines/MillPatternMinerEngine.ts
@@ -245,7 +245,7 @@ export class MillPatternMinerEngine {
 
   // ────────────────────────────────────────────────────────────────
   // PRIVATE: Initialization
-  // ─────────────────────���──────────────────────────────────────────
+  // ──────────────────────────────────────────────────────────────────
 
   private _initResult(): MillMineResult {
     return {
@@ -265,7 +265,7 @@ export class MillPatternMinerEngine {
 
   // ────────────────────────────────────────────────────────────────
   // PRIVATE: Haas extraction
-  // ───��──────────────────��─────────────────────────────���───────────
+  // ────────────────────────────────────────────────────────────────────
 
   private _extractHaasChipLoads(filename: string, section: HaasToolSection, result: MillMineResult): void {
     if (!section.spindle_rpm || section.spindle_rpm <= 0) return;
@@ -322,9 +322,9 @@ export class MillPatternMinerEngine {
     }
   }
 
-  // ──────────────────────────────��─────────────────────────────────
+  // ─────────────────────────────────────────────────────────────────
   // PRIVATE: Hurco extraction
-  // ───────────���────────────────────────────────���───────────────────
+  // ────────────────────────────────────────────────────────────────────
 
   private _extractHurcoChipLoads(filename: string, section: HurcoToolSection, result: MillMineResult): void {
     if (!section.spindle_rpm || section.spindle_rpm <= 0) return;
@@ -366,9 +366,9 @@ export class MillPatternMinerEngine {
     }
   }
 
-  // ──────────────────────────────��─────────────────────────────────
+  // ─────────────────────────────────────────────────────────────────
   // PRIVATE: Roku-Roku extraction
-  // ──��─────────────────────────────────────────────────────────────
+  // ─────────────────────────────────────────────────────────────────
 
   private _extractRokuRokuChipLoads(filename: string, section: RokuRokuToolSection, result: MillMineResult): void {
     if (!section.spindle_rpm || section.spindle_rpm <= 0) return;
@@ -427,7 +427,7 @@ export class MillPatternMinerEngine {
     }
   }
 
-  // ─���──────────────────────────────────────────────────────────────
+  // ──────────────────────────────────────────────────────────────────
   // PRIVATE: Generic pattern extraction
   // ────────────────────────────────────────────────────────────────
 
@@ -499,7 +499,7 @@ export class MillPatternMinerEngine {
     }
   }
 
-  // ─────────────────────────��──────────────────────────────────────
+  // ─────────────────────────────────────────────────────────────────
   // PRIVATE: Accumulator helpers
   // ────────────────────────────────────────────────────────────────
 
@@ -548,9 +548,9 @@ export class MillPatternMinerEngine {
     }
   }
 
-  // ────────���────────────────────────────���──────────────────────────
+  // ────────────────────────────────────────────────────────────────────
   // PRIVATE: Post-processing
-  // ──────────���─────────────────────────────────────────────────────
+  // ──────────────────────────────────────────────────────────────────
 
   private _computeTopPatterns(result: MillMineResult): void {
     const patterns: MillPattern[] = [];
diff --git a/mcp-server/src/engines/SpeedFeedOrchestratorEngine.ts b/mcp-server/src/engines/SpeedFeedOrchestratorEngine.ts
index 39127ba28..7eadaa523 100644
--- a/mcp-server/src/engines/SpeedFeedOrchestratorEngine.ts
+++ b/mcp-server/src/engines/SpeedFeedOrchestratorEngine.ts
@@ -46,6 +46,13 @@ function getMachiningPlaybookEngine(): any {
   return machiningPlaybookEngine as any;
 }
 
+const MAX_REPORTED_TOOL_LIFE_MIN = 480;
+
+function capReportedToolLifeMinutes(value: number) {
+  if (!Number.isFinite(value)) return 1;
+  return Math.max(1, Math.min(MAX_REPORTED_TOOL_LIFE_MIN, value));
+}
+
 function derivePlaybookCategories(input: OrchestratorInput): RuleCategory[] | undefined {
   const operation = input.operation?.toLowerCase();
   if (!operation) return undefined;
@@ -714,7 +721,7 @@ const MATERIAL_DB: Record<string, MaterialRecord> = {
     mc: 0.25,
     k_thermal: 28,
     machinability_factor: 0.55,
-    vc_base: { roughing: 100, finishing: 160 },  // Conservative for tool steels
+    vc_base: { roughing: 110, finishing: 170 },  // Annealed tool steels; HSM/light radial cuts lift from here
     aliases: ["d2 annealed", "a2 annealed", "s7 annealed", "m2 annealed", "tool steel annealed"],
   },
   // Tool steels in HARDENED state (45-62 HRC) — post heat-treatment
@@ -796,6 +803,8 @@ const CAM_STRATEGY_DB: Record<string, Record<string, CAMStrategyRecord>> = {
     "conventional": { ae_pct: 50, speed_multiplier: 1.0, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
     "high speed": { ae_pct: 25, speed_multiplier: 1.3, feed_multiplier: 1.1, is_adaptive: false, ae_variable: false },
     "peel mill": { ae_pct: 5, speed_multiplier: 2.2, feed_multiplier: 1.0, is_adaptive: true, ae_variable: false },
+    "drill": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "drilling": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
   },
   fusion360: {
     "adaptive clearing": { ae_pct: 10, speed_multiplier: 2.0, feed_multiplier: 1.0, is_adaptive: true, ae_variable: true },
@@ -810,12 +819,20 @@ const CAM_STRATEGY_DB: Record<string, Record<string, CAMStrategyRecord>> = {
     "maxx machining": { ae_pct: 8, speed_multiplier: 2.0, feed_multiplier: 1.0, is_adaptive: true, ae_variable: true },
     "5x tangent plane": { ae_pct: 30, speed_multiplier: 1.0, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
     "conventional": { ae_pct: 50, speed_multiplier: 1.0, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "hm drilling": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "hypermill drilling": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "drilling package": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "drill": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "drilling": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "hole making": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
   },
   nx: {
     "adaptive milling": { ae_pct: 10, speed_multiplier: 1.8, feed_multiplier: 1.0, is_adaptive: true, ae_variable: true },
     "cavity milling": { ae_pct: 50, speed_multiplier: 1.0, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
     "contour profile": { ae_pct: 100, speed_multiplier: 0.85, feed_multiplier: 0.9, is_adaptive: false, ae_variable: false },
     "zlevel": { ae_pct: 40, speed_multiplier: 1.0, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "hole making": { ae_pct: 100, speed_multiplier: 0.92, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "drilling": { ae_pct: 100, speed_multiplier: 0.92, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
   },
   solidcam: {
     "imachining": { ae_pct: 10, speed_multiplier: 1.5, feed_multiplier: 1.0, is_adaptive: true, ae_variable: true },
@@ -823,6 +840,15 @@ const CAM_STRATEGY_DB: Record<string, Record<string, CAMStrategyRecord>> = {
     "hss": { ae_pct: 25, speed_multiplier: 1.3, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
     "profile": { ae_pct: 100, speed_multiplier: 0.85, feed_multiplier: 0.9, is_adaptive: false, ae_variable: false },
     "pocket": { ae_pct: 50, speed_multiplier: 1.0, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "drilling": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "hole making": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+  },
+  hsmadvisor: {
+    "hsm": { ae_pct: 15, speed_multiplier: 1.55, feed_multiplier: 1.1, is_adaptive: false, ae_variable: false },
+    "adaptive": { ae_pct: 10, speed_multiplier: 1.8, feed_multiplier: 1.05, is_adaptive: true, ae_variable: true },
+    "trochoidal": { ae_pct: 8, speed_multiplier: 2.0, feed_multiplier: 1.0, is_adaptive: true, ae_variable: false },
+    "conventional": { ae_pct: 50, speed_multiplier: 1.0, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "drilling": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
   },
   generic: {
     "adaptive": { ae_pct: 10, speed_multiplier: 2.0, feed_multiplier: 1.0, is_adaptive: true, ae_variable: true },
@@ -830,6 +856,20 @@ const CAM_STRATEGY_DB: Record<string, Record<string, CAMStrategyRecord>> = {
     "hsm": { ae_pct: 15, speed_multiplier: 1.3, feed_multiplier: 1.1, is_adaptive: false, ae_variable: false },
     "conventional": { ae_pct: 50, speed_multiplier: 1.0, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
     "full slot": { ae_pct: 100, speed_multiplier: 0.75, feed_multiplier: 0.85, is_adaptive: false, ae_variable: false },
+    "drill": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "hole making": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "drilling": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "bolt circle drill": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "drill cluster sequencer": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "prism drill cluster sequencer": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "canned drilling cycle": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "g81 g83 g84 cycle block": { ae_pct: 100, speed_multiplier: 0.9, feed_multiplier: 1.0, is_adaptive: false, ae_variable: false },
+    "engrave / mark": { ae_pct: 15, speed_multiplier: 0.8, feed_multiplier: 0.65, is_adaptive: false, ae_variable: false },
+    "engraving": { ae_pct: 15, speed_multiplier: 0.8, feed_multiplier: 0.65, is_adaptive: false, ae_variable: false },
+    "engrave": { ae_pct: 15, speed_multiplier: 0.8, feed_multiplier: 0.65, is_adaptive: false, ae_variable: false },
+    "marking": { ae_pct: 15, speed_multiplier: 0.8, feed_multiplier: 0.65, is_adaptive: false, ae_variable: false },
+    "chamfer mill": { ae_pct: 20, speed_multiplier: 0.85, feed_multiplier: 0.75, is_adaptive: false, ae_variable: false },
+    "chamfer": { ae_pct: 20, speed_multiplier: 0.85, feed_multiplier: 0.75, is_adaptive: false, ae_variable: false },
   },
 };
 
@@ -922,6 +962,8 @@ const MACHINE_CATALOG_QUICK: Record<string, { power_kw: number; max_rpm: number;
   'dmg mori dmf 260':      { power_kw: 28,   max_rpm: 12000, torque_Nm: 200, taper: 'SK40',    rigidity: 'high',   type: 'vertical_mill',   guideway: 'box',         nat_freq_hz: 650,  accel_m_s2: 5.0,   jerk_m_s3: 12 },
   'dmg mori nlx 2500':     { power_kw: 26,   max_rpm: 3500,  torque_Nm: 576, taper: 'A2-8',    rigidity: 'high',   type: 'lathe',           guideway: 'box',         nat_freq_hz: 500,  accel_m_s2: 7.0,   jerk_m_s3: 20 },
   // ── Haas ──  (standard VMC: accel 0.3-0.5G, jerk 5-15 m/s³)
+  'haas vf-2ss':           { power_kw: 22.4, max_rpm: 12000, torque_Nm: 122, taper: 'BT40',    rigidity: 'medium', type: 'vertical_mill',   guideway: 'linear',      nat_freq_hz: 750,  accel_m_s2: 8.0,   jerk_m_s3: 20 },
+  'haas vf-2ssyt':         { power_kw: 22.4, max_rpm: 12000, torque_Nm: 122, taper: 'BT40',    rigidity: 'medium', type: 'vertical_mill',   guideway: 'linear',      nat_freq_hz: 750,  accel_m_s2: 8.0,   jerk_m_s3: 20 },
   'haas vf-2':             { power_kw: 22.4, max_rpm: 8100,  torque_Nm: 122, taper: 'BT40',    rigidity: 'medium', type: 'vertical_mill',   guideway: 'box',         nat_freq_hz: 600,  accel_m_s2: 3.5,   jerk_m_s3: 8 },
   'haas vf-4':             { power_kw: 22.4, max_rpm: 8100,  torque_Nm: 122, taper: 'BT40',    rigidity: 'medium', type: 'vertical_mill',   guideway: 'box',         nat_freq_hz: 600,  accel_m_s2: 3.5,   jerk_m_s3: 8 },
   'haas vf-6':             { power_kw: 22.4, max_rpm: 8100,  torque_Nm: 122, taper: 'BT40',    rigidity: 'medium', type: 'vertical_mill',   guideway: 'box',         nat_freq_hz: 550,  accel_m_s2: 3.0,   jerk_m_s3: 7 },
@@ -1139,7 +1181,7 @@ function fuzzyMatchMaterial(raw: string): string | undefined {
  * Normalize a CAM strategy name for matching.
  */
 function normalizeStrategyName(raw: string): string {
-  return raw.toLowerCase().replace(/[\s\-_]/g, "");
+  return raw.toLowerCase().replace(/[^a-z0-9]/g, "");
 }
 
 /**
@@ -1155,9 +1197,15 @@ function normalizeCAMSystem(raw: string): string {
     hypermill: "hypermill",
     hyper: "hypermill",
     nx: "nx",
+    nxcam: "nx",
     siemens: "nx",
+    siemensnx: "nx",
+    siemensnxcam: "nx",
     solidcam: "solidcam",
     solid: "solidcam",
+    hsmadvisor: "hsmadvisor",
+    hsm: "hsmadvisor",
+    fswizard: "hsmadvisor",
   };
   return map[norm] ?? "generic";
 }
@@ -1888,6 +1936,14 @@ export class SpeedFeedOrchestratorEngine {
       }
     }
 
+    if (stratRec === undefined && input.operation === "drilling") {
+      const genericStrats = CAM_STRATEGY_DB["generic"];
+      stratRec = genericStrats["drilling"];
+      stratName = "drilling";
+      stratConf = lookupConf * 0.9;
+      stratSrc = "mapped_from_operation:drilling";
+    }
+
     // Map from input.strategy (generic type) if no CAM strategy specified
     if (stratRec === undefined && input.strategy !== undefined) {
       const stratMap: Record<string, string> = {
@@ -1931,6 +1987,36 @@ export class SpeedFeedOrchestratorEngine {
   // resolveGeometry
   // ────────────────────────────────────────────
 
+  private defaultFeatureToleranceMm(input: OrchestratorInput): { value: number; source: string } {
+    const operation = (input.operation ?? "milling").toLowerCase();
+    const cutType = (input.cut_type ?? "roughing").toLowerCase();
+    const strategyText = [
+      input.strategy,
+      input.cam_strategy,
+      input.cam_system,
+    ].filter(Boolean).join(" ").toLowerCase();
+    const radialPct = input.radial_depth_pct
+      ?? (input.radial_depth_mm !== undefined && input.tool_diameter_mm
+        ? (input.radial_depth_mm / input.tool_diameter_mm) * 100
+        : undefined);
+    const lightRadialHsm = /hsm|adaptive|dynamic|trochoidal|peel|hpc|optimized rough/i.test(strategyText)
+      && (radialPct === undefined || radialPct <= 25);
+
+    if (operation === "drilling" || operation === "reaming" || operation === "thread_milling") {
+      return { value: 0.05, source: "default_holemaking_0.05mm" };
+    }
+    if (cutType === "finishing") {
+      return { value: 0.03, source: "default_finishing_0.03mm" };
+    }
+    if (cutType === "semi_finishing") {
+      return { value: 0.075, source: "default_semi_finishing_0.075mm" };
+    }
+    if (lightRadialHsm) {
+      return { value: 0.20, source: "default_hsm_roughing_0.20mm" };
+    }
+    return { value: 0.15, source: "default_roughing_0.15mm" };
+  }
+
   private resolveGeometry(input: OrchestratorInput): ResolvedGeometry {
     const userConf = 0.9;
     const defaultConf = 0.3;
@@ -1940,7 +2026,8 @@ export class SpeedFeedOrchestratorEngine {
     const height = input.workpiece_height_mm ?? 50;
     const diameter = input.workpiece_diameter_mm ?? 0;
     const wallThickness = input.wall_thickness_mm ?? 999;
-    const tolerance = input.feature_tolerance_mm ?? 0.05;
+    const defaultTolerance = this.defaultFeatureToleranceMm(input);
+    const tolerance = input.feature_tolerance_mm ?? defaultTolerance.value;
 
     // Overhang ratio: if user provides, use it; otherwise infer from tool stickout / diameter
     const toolDia = input.tool_diameter_mm ?? 10;
@@ -1990,7 +2077,7 @@ export class SpeedFeedOrchestratorEngine {
       ),
       feature_tolerance_mm: av(
         tolerance, hasTol ? userConf : defaultConf,
-        hasTol ? "user_input" : "default_0.05mm"
+        hasTol ? "user_input" : defaultTolerance.source
       ),
       is_thin_wall: av(
         isThinWall, hasWall ? 0.8 : 0.3,
@@ -2421,6 +2508,14 @@ export class SpeedFeedOrchestratorEngine {
     const formulas_used: string[] = [];
     const dn_warnings: string[] = [];  // DN bearing speed limit warnings (merged into playbook_warnings later)
     const engines_called: string[] = ["SpeedFeedOrchestratorEngine"];
+    const safeModifier = (value: unknown, fallback = 1): number => {
+      const parsed = typeof value === "number"
+        ? value
+        : typeof value === "string" && value.trim() !== ""
+          ? Number(value)
+          : NaN;
+      return Number.isFinite(parsed) && parsed > 0 ? parsed : fallback;
+    };
 
     // ── Step 1.5: Query Proven Program Parameters (KAR-MS2 U-KAR14) ──
     t0 = Date.now();
@@ -2433,6 +2528,10 @@ export class SpeedFeedOrchestratorEngine {
 
     // ── Step 1.6: Query Tribal Knowledge Modifiers (TK-MS3 U-TK17) ──
     t0 = Date.now();
+    const tribalRadialPct = input.radial_depth_pct
+      ?? (input.radial_depth_mm !== undefined && tool.diameter_mm.value > 0
+        ? (input.radial_depth_mm / tool.diameter_mm.value) * 100
+        : camStrat.ae_pct.value);
     const tribalMods = tribalKnowledgeAdvisorEngine.getModifiers({
       material: input.material,
       iso_group: material.iso_group.value,
@@ -2440,6 +2539,12 @@ export class SpeedFeedOrchestratorEngine {
       machine_id: input.machine_name,
       tool_type: input.tool_material,
       tool_diameter_mm: input.tool_diameter_mm,
+      hardness_hb: input.hardness_hb ?? material.hardness_hb.value,
+      hardness_hrc: input.hardness_hrc,
+      cut_type: input.cut_type,
+      strategy: input.strategy,
+      cam_strategy: input.cam_strategy,
+      radial_depth_pct: tribalRadialPct,
       workholding: input.workholding_type,
     });
     if (tribalMods.evidence_count > 0) {
@@ -2451,8 +2556,11 @@ export class SpeedFeedOrchestratorEngine {
     // ── Step 2: Core Speed/Feed Physics ──
     const D = tool.diameter_mm.value;
     const z = tool.flutes.value;
+    const operation = input.operation ?? "milling";
     const cutType = input.cut_type ?? "roughing";
     const isRoughing = cutType === "roughing" || cutType === "semi_finishing";
+    const isAxialHolemaking = operation === "drilling" || operation === "reaming";
+    const isFullDiameterHoleCut = operation === "drilling";
 
     // Base cutting speed from material DB
     const vcBase = isRoughing
@@ -2464,6 +2572,21 @@ export class SpeedFeedOrchestratorEngine {
     const coatingRec = COATING_DB[coatingKey] ?? COATING_DB["TiAlN"];
     const coatingSpeedFactor = coatingRec.speed_multiplier;
 
+    // Tool substrate speed factor. Material DB speeds are carbide-oriented; HSS
+    // must not inherit carbide surface speeds just because the same geometry is used.
+    const toolMaterialKey = String(tool.material.value ?? "carbide").toLowerCase();
+    const isoKey = material.iso_group.value;
+    const TOOL_MATERIAL_SPEED_FACTORS: Record<string, Partial<Record<string, number>> & { default: number }> = {
+      carbide: { default: 1.0 },
+      hss: { P: 0.28, M: 0.22, K: 0.21, N: 0.45, S: 0.28, H: 0.18, default: 0.25 },
+      cermet: { P: 1.05, M: 0.85, K: 1.15, N: 0.75, S: 0.55, H: 0.70, default: 0.95 },
+      ceramic: { P: 1.25, M: 0.70, K: 1.80, N: 0.60, S: 1.15, H: 1.30, default: 1.0 },
+      cbn: { P: 0.95, M: 0.65, K: 1.25, N: 0.50, S: 0.75, H: 1.80, default: 1.0 },
+      pcd: { P: 0.60, M: 0.45, K: 0.70, N: 2.20, S: 0.35, H: 0.30, default: 0.75 },
+    };
+    const materialSpeedMap = TOOL_MATERIAL_SPEED_FACTORS[toolMaterialKey] ?? TOOL_MATERIAL_SPEED_FACTORS.carbide;
+    const toolMaterialSpeedFactor = materialSpeedMap[isoKey] ?? materialSpeedMap.default;
+
     // Insert grade speed factor
     const GRADE_SPEED_FACTORS: Record<string, number> = {
       'GC4325': 1.10, 'GC4315': 1.15, 'GC4335': 1.05,  // Sandvik
@@ -2502,10 +2625,11 @@ export class SpeedFeedOrchestratorEngine {
 
     // Effective cutting speed
     // TK-MS3 U-TK17: Apply tribal Vc modifier (Tier 2)
-    const tribalVcMod = tribalMods.vc_modifier;
-    let Vc = vcBase * coatingSpeedFactor * insertGradeFactor * coolantSpeedFactor * camSpeedMult
+    const tribalVcMod = safeModifier(tribalMods.vc_modifier);
+    let Vc = vcBase * toolMaterialSpeedFactor * coatingSpeedFactor * insertGradeFactor * coolantSpeedFactor * camSpeedMult
            * geomDerating * gradeFactor * calVcFactor * tribalVcMod;
-    formulas_used.push("Vc = Vc_base × coating_factor × insert_grade_factor × coolant_factor × cam_multiplier × geom_derating × grade_factor" +
+    formulas_used.push("Vc = Vc_base × tool_material_factor × coating_factor × insert_grade_factor × coolant_factor × cam_multiplier × geom_derating × grade_factor" +
+      (toolMaterialSpeedFactor !== 1.0 ? ` × ${toolMaterialKey}(${toolMaterialSpeedFactor.toFixed(2)})` : "") +
       (calVcFactor !== 1.0 ? ` × cal_vc(${calVcFactor})` : "") +
       (tribalVcMod !== 1.0 ? ` × tribal(${tribalVcMod.toFixed(2)})` : ""));
     if (insertGradeFactor !== 1.0) {
@@ -2627,7 +2751,7 @@ export class SpeedFeedOrchestratorEngine {
     fz *= (1.0 - tirPenalty);
 
     // TK-MS3 U-TK17: Apply tribal fz modifier (Tier 2)
-    const tribalFzMod = tribalMods.fz_modifier;
+    const tribalFzMod = safeModifier(tribalMods.fz_modifier);
     fz *= tribalFzMod;
     formulas_used.push("fz = base_fz × cam_feed_mult × (1 - TIR_penalty)" +
       (tribalFzMod !== 1.0 ? ` × tribal(${tribalFzMod.toFixed(2)})` : ""));
@@ -2638,17 +2762,34 @@ export class SpeedFeedOrchestratorEngine {
 
     // Axial depth of cut
     const isAdaptive = camStrat.is_adaptive.value;
+    const positiveFiniteInput = (value: unknown): number | undefined => {
+      if (typeof value === "number") {
+        return Number.isFinite(value) && value > 0 ? value : undefined;
+      }
+      if (typeof value === "string" && value.trim() !== "") {
+        const parsed = Number(value);
+        return Number.isFinite(parsed) && parsed > 0 ? parsed : undefined;
+      }
+      return undefined;
+    };
+    const fallbackAp = isRoughing ? D * 1.0 : D * 0.1;
+    const requestedAp = positiveFiniteInput(input.axial_depth_mm);
     let ap: number;
-    if (input.axial_depth_mm !== undefined) {
-      ap = input.axial_depth_mm;
-    } else if (isRoughing) {
-      ap = D * 1.0;
+    if (requestedAp !== undefined) {
+      ap = requestedAp;
     } else {
-      ap = D * 0.1;
+      ap = fallbackAp;
+      if (input.axial_depth_mm !== undefined) {
+        formulas_used.push(`Ignored invalid axial depth input: ${JSON.stringify(input.axial_depth_mm)}; using ${ap.toFixed(3)} mm fallback`);
+      }
     }
     // TK-MS3 U-TK17: Apply tribal ap modifier (Tier 2)
-    const tribalApMod = tribalMods.ap_modifier;
+    const tribalApMod = safeModifier(tribalMods.ap_modifier);
     ap *= tribalApMod;
+    if (!Number.isFinite(ap) || ap <= 0) {
+      ap = fallbackAp;
+      formulas_used.push(`Axial depth sanitized to ${ap.toFixed(3)} mm after modifier validation`);
+    }
     // Clamp by geometry (workpiece height if known, thin wall)
     if (geometry.workpiece_height_mm.value > 0 && ap > geometry.workpiece_height_mm.value) {
       ap = geometry.workpiece_height_mm.value;
@@ -2658,29 +2799,42 @@ export class SpeedFeedOrchestratorEngine {
     }
 
     // Radial depth of cut
+    const fallbackAe = D * (camStrat.ae_pct.value / 100);
+    const requestedAe = positiveFiniteInput(input.radial_depth_mm);
+    const requestedAePct = positiveFiniteInput(input.radial_depth_pct);
     let ae: number;
-    if (input.radial_depth_mm !== undefined) {
-      ae = input.radial_depth_mm;
-    } else if (input.radial_depth_pct !== undefined) {
-      ae = D * (input.radial_depth_pct / 100);
+    if (requestedAe !== undefined) {
+      ae = requestedAe;
+    } else if (requestedAePct !== undefined) {
+      ae = D * (requestedAePct / 100);
     } else {
-      const aePct = camStrat.ae_pct.value;
-      ae = D * (aePct / 100);
+      ae = fallbackAe;
+      if (input.radial_depth_mm !== undefined || input.radial_depth_pct !== undefined) {
+        formulas_used.push(
+          `Ignored invalid radial depth input: ae=${JSON.stringify(input.radial_depth_mm)}, pct=${JSON.stringify(input.radial_depth_pct)}; using ${ae.toFixed(3)} mm strategy fallback`,
+        );
+      }
     }
     // TK-MS3 U-TK17: Apply tribal ae modifier (Tier 2)
-    const tribalAeMod = tribalMods.ae_modifier;
+    const tribalAeMod = safeModifier(tribalMods.ae_modifier);
     ae *= tribalAeMod;
     // Apply thin_wall / pocket derating
     if (geometry.is_thin_wall.value) {
       ae = Math.min(ae, D * 0.15);
     }
+    if (!Number.isFinite(ae) || ae <= 0) {
+      ae = fallbackAe;
+      formulas_used.push(`Radial depth sanitized to ${ae.toFixed(3)} mm after modifier validation`);
+    }
 
     // ── Chip Thinning Correction ──
     // When ae < 50% of D, actual chip thickness < programmed fz
     // fz_eff = fz × D / (2 × sqrt(ae × (D - ae)))   [Sandvik]
+    let chipThinFactorApplied = 1.0;
     if (ae < D * 0.5 && ae > 0 && D > ae) {
       const chipThinFactor = D / (2 * Math.sqrt(ae * (D - ae)));
       const clampedFactor = Math.min(3.0, chipThinFactor);
+      chipThinFactorApplied = clampedFactor;
       fz *= clampedFactor;
       Vf = fz * z * rpm;
       formulas_used.push(
@@ -2690,21 +2844,85 @@ export class SpeedFeedOrchestratorEngine {
 
     // ── Step 3: Derived Calculations ──
 
-    // MRR = ap * ae * Vf / 1000 (cm³/min)
-    const mrr = (ap * ae * Vf) / 1000;
-    formulas_used.push("MRR = ap × ae × Vf / 1000 [cm³/min]");
-
     // Kienzle cutting force: Fc = kc1.1 × ap × fz^(1-mc)
     // INFRA-5-1 U-CAL1: Apply calibration factor to kc1.1
     const calKcFactor = input.calibration_overrides?.kc1_1_factor ?? 1.0;
     const kc1_1 = material.kc1_1.value * calKcFactor;
     const mc = material.mc.value;
-    const Fc = kc1_1 * ap * Math.pow(Math.max(fz, 0.001), 1 - mc);
-    formulas_used.push("Fc = kc1.1 × ap × fz^(1-mc) [Kienzle]" + (calKcFactor !== 1.0 ? ` (kc1.1 cal: ×${calKcFactor})` : ""));
+    const computeMrr = (currentAp: number, currentAe: number, currentVf: number): number => {
+      if (isFullDiameterHoleCut) {
+        const holeAreaMm2 = (Math.PI * D * D) / 4;
+        return (holeAreaMm2 * currentVf) / 1000;
+      }
+      return (currentAp * currentAe * currentVf) / 1000;
+    };
+    // Kienzle-style drilling uses feed/rev and cutting lip width instead of hole depth as side-milling ap.
+    const computeCuttingForce = (currentFz: number, currentAp: number): number => {
+      if (isAxialHolemaking) {
+        const feedPerRev = Math.max(currentFz * z, 0.001);
+        const chipThickness = Math.max(feedPerRev / 2, 0.001);
+        const lipWidth = Math.max(D / 2, 0.001);
+        const operationFactor = operation === "reaming" ? 0.35 : 1.0;
+        return kc1_1 * lipWidth * Math.pow(chipThickness, 1 - mc) * operationFactor;
+      }
+      return kc1_1 * currentAp * Math.pow(Math.max(currentFz, 0.001), 1 - mc);
+    };
+    const computeMeanSpecificCuttingForce = (currentFz: number): number => {
+      const chipThickness = Math.max(currentFz / chipThinFactorApplied, 0.01);
+      const chipEnergyFactor = Math.max(0.85, Math.min(1.25, Math.pow(chipThickness / 0.10, -mc * 0.30)));
+      return kc1_1 * chipEnergyFactor;
+    };
+    const spindleLoadFactor = isAxialHolemaking ? 1.0 : 1.25;
+    const computePowerKW = (
+      currentFz: number,
+      currentAp: number,
+      currentAe: number,
+      currentVf: number,
+      currentVc: number,
+    ): number => {
+      if (isAxialHolemaking) {
+        const force = computeCuttingForce(currentFz, currentAp);
+        return (force * currentVc) / (60 * 1000);
+      }
+      const currentMrr = computeMrr(currentAp, currentAe, currentVf);
+      return (currentMrr * computeMeanSpecificCuttingForce(currentFz) * spindleLoadFactor) / 60000;
+    };
+    const computeTangentialForce = (
+      currentFz: number,
+      currentAp: number,
+      currentAe: number,
+      currentVf: number,
+      currentVc: number,
+    ): number => {
+      if (isAxialHolemaking) {
+        return computeCuttingForce(currentFz, currentAp);
+      }
+      const power = computePowerKW(currentFz, currentAp, currentAe, currentVf, currentVc);
+      return currentVc > 0 ? (power * 60 * 1000) / currentVc : computeCuttingForce(currentFz, currentAp);
+    };
+
+    // MRR = ap * ae * Vf / 1000 for milling; drilling uses hole area * feed.
+    const mrr = computeMrr(ap, ae, Vf);
+    formulas_used.push(
+      isFullDiameterHoleCut
+        ? "MRR = (π × D² / 4) × Vf / 1000 [drilling cm³/min]"
+        : "MRR = ap × ae × Vf / 1000 [cm³/min]",
+    );
 
-    // Power = Fc * Vc / (60 * 1000) [kW]
-    const powerKW = (Fc * Vc) / (60 * 1000);
-    formulas_used.push("P = Fc × Vc / 60000 [kW]");
+    const Fc = computeTangentialForce(fz, ap, ae, Vf, Vc);
+    formulas_used.push(
+      isAxialHolemaking
+        ? "Fc = kc1.1 × (D/2) × (feed_per_rev/2)^(1-mc) [axial holemaking Kienzle]"
+        : "Fc = 60000 × P / Vc [MRR-calibrated tangential force]"
+        + (calKcFactor !== 1.0 ? ` (kc1.1 cal: ×${calKcFactor})` : ""),
+    );
+
+    const powerKW = computePowerKW(fz, ap, ae, Vf, Vc);
+    formulas_used.push(
+      isAxialHolemaking
+        ? "P = Fc × Vc / 60000 [kW]"
+        : `P = MRR × mean_kc × spindle_load(${spindleLoadFactor.toFixed(2)}) / 60000 [kW]`,
+    );
 
     // Torque = Power * 30000 / (π * RPM) [Nm]
     const torqueNm = rpm > 0 ? (powerKW * 30000) / (Math.PI * rpm) : 0;
@@ -2723,10 +2941,10 @@ export class SpeedFeedOrchestratorEngine {
     // Apply coating life factor
     toolLifeMin *= coatingRec.life_multiplier;
     // TK-MS3 U-TK17: Apply tribal tool life modifier (Tier 2)
-    const tribalTlMod = tribalMods.tool_life_modifier;
+    const tribalTlMod = safeModifier(tribalMods.tool_life_modifier);
     toolLifeMin *= tribalTlMod;
-    // Clamp to reasonable range [1, 9999]
-    toolLifeMin = Math.max(1, Math.min(9999, toolLifeMin));
+    // Report a finite planning horizon instead of leaking the old 9999 sentinel.
+    toolLifeMin = capReportedToolLifeMinutes(toolLifeMin);
     formulas_used.push("T_life = (C/Vc)^(1/n) × coolant_life × coating_life" +
       (tribalTlMod !== 1.0 ? ` × tribal(${tribalTlMod.toFixed(2)})` : "") + " [Taylor]");
 
@@ -2739,16 +2957,62 @@ export class SpeedFeedOrchestratorEngine {
 
     // Tool deflection: δ = Fc × L³ / (3 × E × I)
     // Use canonical E modulus for resolved tool material (not always carbide)
-    const toolMat = (tool as any).material ?? "carbide";
+    const rawToolMaterial = (tool as any).material;
+    const toolMat = typeof rawToolMaterial === "string"
+      ? rawToolMaterial
+      : typeof rawToolMaterial?.value === "string"
+        ? rawToolMaterial.value
+        : "carbide";
     const E_tool = (CANONICAL_TOOL_MODULUS as any)[toolMat] ?? CANONICAL_TOOL_MODULUS.carbide;
     const stickout = tool.stickout_mm.value;
-    const d_shank = D;  // approximate shank ≈ tool diameter
+    const d_shank = D;  // shank diameter is approximately the nominal cutting diameter for end mills
     const I_moment = (Math.PI * Math.pow(d_shank, 4)) / 64;
-    const deflection_mm = I_moment > 0
-      ? (Fc * Math.pow(stickout, 3)) / (3 * E_tool * I_moment)
+    const fluteLengthForFlex = Math.min(Math.max(tool.flute_length_mm.value, 0), stickout);
+    const flutedSectionStart = Math.max(0, stickout - fluteLengthForFlex);
+    const fluteCoreRatio = isAxialHolemaking
+      ? 1.0
+      : tool.flutes.value >= 5 ? 0.92
+        : tool.flutes.value >= 4 ? 0.88
+          : tool.flutes.value >= 3 ? 0.84 : 0.78;
+    const I_fluted = I_moment * Math.pow(fluteCoreRatio, 4);
+    const beamIntegral = (fromMm: number, toMm: number, inertia: number): number => {
+      if (toMm <= fromMm || inertia <= 0) return 0;
+      return (Math.pow(stickout - fromMm, 3) - Math.pow(stickout - toMm, 3)) / (3 * inertia);
+    };
+    const fullCylinderFlexIntegral = I_moment > 0 ? Math.pow(stickout, 3) / (3 * I_moment) : 0;
+    const compositeFlexIntegral = fullCylinderFlexIntegral > 0
+      ? beamIntegral(0, flutedSectionStart, I_moment)
+        + beamIntegral(flutedSectionStart, stickout, I_fluted)
       : 0;
+    const fluteComplianceFactor = fullCylinderFlexIntegral > 0
+      ? Math.max(1, Math.min(2.5, compositeFlexIntegral / fullCylinderFlexIntegral))
+      : 1;
+    const machineCompliance = machine.rigidity.value === "high"
+      ? 1.25
+      : machine.rigidity.value === "low" ? 2.60 : 1.80;
+    const holderCompliance = 1 + Math.max(0, 1 - holder.stiffness_factor.value) * 2;
+    const workholdingCompliance = workhold.rigidity_factor.value >= 0.95
+      ? 1.0
+      : workhold.rigidity_factor.value >= 0.75 ? 1.25 : 1.65;
+    const systemComplianceFactor = Math.min(3.5, machineCompliance * holderCompliance * workholdingCompliance);
+    const lateralHolemakingFraction = isAxialHolemaking
+      ? Math.min(0.18, Math.max(0.04, 0.045 + (holder.tir_mm.value / Math.max(D, 1)) * 8))
+      : 1.0;
+    const computeDeflectionMm = (forceN: number): number => {
+      const lateralForceN = forceN * lateralHolemakingFraction;
+      return I_moment > 0
+        ? ((lateralForceN * Math.pow(stickout, 3)) / (3 * E_tool * I_moment)) * systemComplianceFactor * fluteComplianceFactor
+        : 0;
+    };
+    const forceForWorkholding = (forceN: number): number =>
+      isAxialHolemaking ? forceN * 0.45 : forceN;
+    const deflection_mm = computeDeflectionMm(Fc);
     const deflection_um = deflection_mm * 1000;
-    formulas_used.push(`δ = Fc × L³ / (3 × E × I), E=${E_tool/1000}GPa (${toolMat}), I=π×d⁴/64`);
+    formulas_used.push(
+      isAxialHolemaking
+        ? `δ = (Fc × lateral_imbalance ${lateralHolemakingFraction.toFixed(3)}) × L³ / (3 × E × I), E=${E_tool/1000}GPa (${toolMat}), I=π×d⁴/64`
+        : `δ = Fc × L³ / (3 × E × I) × system_compliance(${systemComplianceFactor.toFixed(2)}) × flute_compliance(${fluteComplianceFactor.toFixed(2)}, core≈${(fluteCoreRatio * 100).toFixed(0)}%D), E=${E_tool/1000}GPa (${toolMat}), I=π×d⁴/64`,
+    );
 
     // ── Step 4: Safety Checks ──
     const safetyChecks: SafetyCheck[] = [];
@@ -2854,7 +3118,7 @@ export class SpeedFeedOrchestratorEngine {
     });
 
     // Deflection check
-    const tolLimit = (input.feature_tolerance_mm ?? 0.05) / 3;
+    const tolLimit = geometry.feature_tolerance_mm.value / 3;
     const deflUtil = tolLimit > 0 ? (deflection_mm / tolLimit) * 100 : 0;
     const deflPass = deflection_mm <= tolLimit;
     safetyChecks.push({
@@ -2896,15 +3160,16 @@ export class SpeedFeedOrchestratorEngine {
     // Workholding force check: Fc < clamping_force * 0.7
     const clampForceN = workhold.clamping_force_kN.value * 1000;
     const whLimit = clampForceN * 0.7;
-    const whUtil = whLimit > 0 ? (Fc / whLimit) * 100 : 0;
-    const whPass = Fc <= whLimit;
+    const workholdingForceN = forceForWorkholding(Fc);
+    const whUtil = whLimit > 0 ? (workholdingForceN / whLimit) * 100 : 0;
+    const whPass = workholdingForceN <= whLimit;
     safetyChecks.push({
       name: "workholding",
       passed: whPass,
       message: whPass
-        ? `Cutting force ${Fc.toFixed(0)} N within workholding limit ${whLimit.toFixed(0)} N (70%)`
-        : `Cutting force ${Fc.toFixed(0)} N EXCEEDS workholding limit ${whLimit.toFixed(0)} N`,
-      value: Fc,
+        ? `Process force ${workholdingForceN.toFixed(0)} N within workholding limit ${whLimit.toFixed(0)} N (70%)`
+        : `Process force ${workholdingForceN.toFixed(0)} N EXCEEDS workholding limit ${whLimit.toFixed(0)} N`,
+      value: workholdingForceN,
       limit: whLimit,
     });
     limitingFactors.push({
@@ -2914,91 +3179,128 @@ export class SpeedFeedOrchestratorEngine {
       severity: whUtil > 100 ? "critical" : whUtil > 80 ? "warning" : "info",
     });
 
-    // ── Apply proportional reduction if any check fails ──
+    // ── Apply bounded iterative reduction if any check fails ──
     let reductionFactor = 1.0;
     const failedChecks = safetyChecks.filter((c) => !c.passed);
     if (failedChecks.length > 0) {
-      for (const check of failedChecks) {
-        if (check.value !== undefined && check.limit !== undefined && check.value > 0) {
-          const ratio = check.limit / check.value;
-          reductionFactor = Math.min(reductionFactor, ratio);
+      let remainingFailedChecks = failedChecks;
+
+      for (let deratePass = 0; deratePass < 4 && remainingFailedChecks.length > 0; deratePass += 1) {
+        let passReductionFactor = 1.0;
+        for (const check of remainingFailedChecks) {
+          if (check.value !== undefined && check.limit !== undefined && check.value > 0) {
+            const ratio = check.limit / check.value;
+            passReductionFactor = Math.min(passReductionFactor, ratio);
+          }
         }
-      }
-      // Apply reduction proportionally
-      reductionFactor = Math.max(0.1, reductionFactor * 0.95); // 5% extra margin
-      Vc *= reductionFactor;
-      rpm = Math.round((1000 * Vc) / (Math.PI * D));
-      if (rpm > maxRPM) rpm = maxRPM;
-      fz *= Math.sqrt(reductionFactor); // reduce fz less aggressively than speed
-      Vf = fz * z * rpm;
 
-      // Recompute derived values after reduction
-      const FcAdj = kc1_1 * ap * Math.pow(Math.max(fz, 0.001), 1 - mc);
-      const powerAdj = (FcAdj * Vc) / (60 * 1000);
-      const torqueAdj = rpm > 0 ? (powerAdj * 30000) / (Math.PI * rpm) : 0;
-      const deflAdj_mm = I_moment > 0
-        ? (FcAdj * Math.pow(stickout, 3)) / (3 * E_tool * I_moment)
-        : 0;
-      const lifeAdj = Math.pow(taylorC / Math.max(Vc, 1), 1 / taylorN)
-        * coolant.life_factor.value * coatingRec.life_multiplier;
-
-      // Update safety checks to reflect post-adjustment values
-      for (const sc of safetyChecks) {
-        if (sc.name === "power") {
-          sc.value = powerAdj;
-          sc.passed = powerAdj <= powerLimit;
-          sc.message = `Power adjusted to ${powerAdj.toFixed(2)} kW (was over limit, reduced by ${((1 - reductionFactor) * 100).toFixed(0)}%)`;
-        } else if (sc.name === "torque") {
-          sc.value = torqueAdj;
-          sc.passed = torqueAdj <= torqueLimit;
-          sc.message = `Torque adjusted to ${torqueAdj.toFixed(2)} Nm`;
-        } else if (sc.name === "deflection") {
-          sc.value = deflAdj_mm;
-          sc.passed = deflAdj_mm <= tolLimit;
-          sc.message = `Deflection adjusted to ${(deflAdj_mm * 1000).toFixed(1)} µm`;
-        } else if (sc.name === "feed_rate") {
-          sc.value = Vf;
-          sc.passed = Vf <= vfLimit;
-          sc.message = `Feed rate adjusted to ${Vf.toFixed(0)} mm/min`;
-        } else if (sc.name === "workholding") {
-          sc.value = FcAdj;
-          sc.passed = FcAdj <= whLimit;
-          sc.message = `Cutting force adjusted to ${FcAdj.toFixed(0)} N`;
+        // Keep a margin and permit deep cuts to derate below the old 10% floor when physics requires it.
+        passReductionFactor = Math.max(0.02, passReductionFactor * (deratePass === 0 ? 0.95 : 0.90));
+        reductionFactor *= passReductionFactor;
+
+        const forceLimited = remainingFailedChecks.some((check) =>
+          check.name === "torque" || check.name === "deflection" || check.name === "workholding"
+        );
+        const speedLimited = remainingFailedChecks.some((check) =>
+          check.name === "power" || check.name === "rpm" || check.name === "feed_rate"
+        );
+        let fzReductionFactor = Math.sqrt(passReductionFactor);
+        let apReductionFactor = 1.0;
+        if (forceLimited) {
+          const forceExponent = Math.max(0.1, 1 - mc);
+          const depthShare = isAxialHolemaking ? 0 : remainingFailedChecks.some((check) => check.name === "deflection") ? 0.62 : 0.5;
+          apReductionFactor = depthShare > 0
+            ? Math.max(0.08, Math.min(1, Math.pow(passReductionFactor, depthShare)))
+            : 1.0;
+          fzReductionFactor = Math.max(
+            0.03,
+            Math.min(1, Math.pow(Math.max(0.001, passReductionFactor / apReductionFactor), 1 / forceExponent)),
+          );
+          ap *= apReductionFactor;
+          formulas_used.push(
+            `Force-limited safety derating pass ${deratePass + 1}: ap×${apReductionFactor.toFixed(2)}, fz×${fzReductionFactor.toFixed(2)} for torque/deflection/workholding envelope`,
+          );
+        }
+        if (speedLimited || !forceLimited) {
+          Vc *= passReductionFactor;
+        }
+        rpm = Math.round((1000 * Vc) / (Math.PI * D));
+        rpmClamped = rpm > maxRPM;
+        if (rpm > maxRPM) rpm = maxRPM;
+        fz *= fzReductionFactor;
+        Vf = fz * z * rpm;
+
+        // Recompute derived values after this reduction pass.
+        const powerAdj = computePowerKW(fz, ap, ae, Vf, Vc);
+        const FcAdj = computeTangentialForce(fz, ap, ae, Vf, Vc);
+        const torqueAdj = rpm > 0 ? (powerAdj * 30000) / (Math.PI * rpm) : 0;
+        const deflAdj_mm = computeDeflectionMm(FcAdj);
+        const workholdingAdjN = forceForWorkholding(FcAdj);
+
+        // Update safety checks to reflect post-adjustment values.
+        for (const sc of safetyChecks) {
+          if (sc.name === "power") {
+            sc.value = powerAdj;
+            sc.passed = powerAdj <= powerLimit;
+            sc.message = `Power adjusted to ${powerAdj.toFixed(2)} kW (cumulative derate ${((1 - reductionFactor) * 100).toFixed(0)}%)`;
+          } else if (sc.name === "torque") {
+            sc.value = torqueAdj;
+            sc.passed = torqueAdj <= torqueLimit;
+            sc.message = `Torque adjusted to ${torqueAdj.toFixed(2)} Nm`;
+          } else if (sc.name === "rpm") {
+            sc.value = rpm;
+            sc.passed = rpm <= maxRPM;
+            sc.message = rpm <= maxRPM
+              ? `RPM adjusted to ${rpm} within max ${maxRPM}`
+              : `RPM adjusted to ${rpm} EXCEEDS max ${maxRPM}`;
+          } else if (sc.name === "deflection") {
+            sc.value = deflAdj_mm;
+            sc.passed = deflAdj_mm <= tolLimit;
+            sc.message = `Deflection adjusted to ${(deflAdj_mm * 1000).toFixed(1)} µm`;
+          } else if (sc.name === "feed_rate") {
+            sc.value = Vf;
+            sc.passed = Vf <= vfLimit;
+            sc.message = `Feed rate adjusted to ${Vf.toFixed(0)} mm/min`;
+          } else if (sc.name === "workholding") {
+            sc.value = workholdingAdjN;
+            sc.passed = workholdingAdjN <= whLimit;
+            sc.message = `Process force adjusted to ${workholdingAdjN.toFixed(0)} N`;
+          }
         }
-      }
 
-      // Recalculate limiting factor utilizations
-      for (const lf of limitingFactors) {
-        if (lf.parameter === "power_kw") {
-          lf.utilization_pct = powerLimit > 0 ? (powerAdj / powerLimit) * 100 : 0;
-        } else if (lf.parameter === "torque_Nm") {
-          lf.utilization_pct = torqueLimit > 0 ? (torqueAdj / torqueLimit) * 100 : 0;
-        } else if (lf.parameter === "spindle_rpm") {
-          lf.utilization_pct = maxRPM > 0 ? (rpm / maxRPM) * 100 : 0;
-        } else if (lf.parameter === "deflection_mm") {
-          lf.utilization_pct = tolLimit > 0 ? (deflAdj_mm / tolLimit) * 100 : 0;
-        } else if (lf.parameter === "feed_rate_mmmin") {
-          lf.utilization_pct = (Vf / vfLimit) * 100;
-        } else if (lf.parameter === "workholding_force") {
-          lf.utilization_pct = whLimit > 0 ? (FcAdj / whLimit) * 100 : 0;
+        // Recalculate limiting factor utilizations.
+        for (const lf of limitingFactors) {
+          if (lf.parameter === "power_kw") {
+            lf.utilization_pct = powerLimit > 0 ? (powerAdj / powerLimit) * 100 : 0;
+          } else if (lf.parameter === "torque_Nm") {
+            lf.utilization_pct = torqueLimit > 0 ? (torqueAdj / torqueLimit) * 100 : 0;
+          } else if (lf.parameter === "spindle_rpm") {
+            lf.utilization_pct = maxRPM > 0 ? (rpm / maxRPM) * 100 : 0;
+          } else if (lf.parameter === "deflection_mm") {
+            lf.utilization_pct = tolLimit > 0 ? (deflAdj_mm / tolLimit) * 100 : 0;
+          } else if (lf.parameter === "feed_rate_mmmin") {
+            lf.utilization_pct = (Vf / vfLimit) * 100;
+          } else if (lf.parameter === "workholding_force") {
+            lf.utilization_pct = whLimit > 0 ? (workholdingAdjN / whLimit) * 100 : 0;
+          }
+          lf.severity = lf.utilization_pct > 100 ? "critical"
+            : lf.utilization_pct > 80 ? "warning" : "info";
         }
-        lf.severity = lf.utilization_pct > 100 ? "critical"
-          : lf.utilization_pct > 80 ? "warning" : "info";
+
+        remainingFailedChecks = safetyChecks.filter((c) => !c.passed);
       }
     }
 
     // Recompute final derived values (after possible adjustment)
-    const finalFc = kc1_1 * ap * Math.pow(Math.max(fz, 0.001), 1 - mc);
-    const finalPower = (finalFc * Vc) / (60 * 1000);
+    const finalPower = computePowerKW(fz, ap, ae, Vf, Vc);
+    const finalFc = computeTangentialForce(fz, ap, ae, Vf, Vc);
     const finalTorque = rpm > 0 ? (finalPower * 30000) / (Math.PI * rpm) : 0;
-    const finalMRR = (ap * ae * Vf) / 1000;
+    const finalMRR = computeMrr(ap, ae, Vf);
     const finalRa = ((fz * fz * 1000) / (32 * rCorner)) * calRaFactor;
-    const finalDefl_mm = I_moment > 0
-      ? (finalFc * Math.pow(stickout, 3)) / (3 * E_tool * I_moment)
-      : 0;
-    const finalLife = Math.max(1, Math.min(9999,
+    const finalDefl_mm = computeDeflectionMm(finalFc);
+    const finalLife = capReportedToolLifeMinutes(
       Math.pow(taylorC / Math.max(Vc, 1), 1 / taylorN)
-      * coolant.life_factor.value * coatingRec.life_multiplier));
+      * coolant.life_factor.value * coatingRec.life_multiplier * tribalTlMod);
 
     // ── Step 5: Limiting Factor ──
     // Sort by utilization descending
@@ -3149,9 +3451,9 @@ export class SpeedFeedOrchestratorEngine {
       const altRpm = Math.min(Math.round((1000 * altVc) / (Math.PI * D)), maxRPM);
       const altVf = altFz * z * altRpm;
       const altMRR = (ap * ae * altVf) / 1000;
-      const altLife = Math.max(1, Math.min(9999,
+      const altLife = capReportedToolLifeMinutes(
         Math.pow(taylorC / Math.max(altVc, 1), 1 / taylorN)
-        * coolant.life_factor.value * coatingRec.life_multiplier));
+        * coolant.life_factor.value * coatingRec.life_multiplier);
       return {
         label,
         cutting_speed_mpm: Math.round(altVc * 10) / 10,
@@ -3214,9 +3516,9 @@ export class SpeedFeedOrchestratorEngine {
         "Hardened steel (ISO H): use CBN or ceramic inserts, minimize interrupted cuts, ensure rigid setup",
       );
     }
-    if (deflection_um > 20) {
+    if (finalDefl_mm * 1000 > 20) {
       playbook_warnings.push(
-        `High deflection (${deflection_um.toFixed(0)} µm): consider shorter stickout, larger shank, or reduced ap/ae`,
+        `High deflection (${(finalDefl_mm * 1000).toFixed(0)} µm): consider shorter stickout, larger shank, or reduced ap/ae`,
       );
     }
 
@@ -3305,20 +3607,29 @@ export class SpeedFeedOrchestratorEngine {
     }
 
     // ── Step 10: Build and return OrchestratorResult ──
+    const roundTo = (value: number, digits: number) => {
+      const factor = 10 ** digits;
+      return Math.round(value * factor) / factor;
+    };
+    const roundPositive = (value: number, digits: number, floor: number) => {
+      if (!Number.isFinite(value) || value <= 0) return 0;
+      return Math.max(floor, roundTo(value, digits));
+    };
+
     const result: OrchestratorResult = {
       cutting_speed_mpm: Math.round(Vc * 10) / 10,
       spindle_rpm: rpm,
       feed_per_tooth_mm: Math.round(fz * 10000) / 10000,
-      feed_rate_mmmin: Math.round(Vf),
+      feed_rate_mmmin: roundPositive(Vf, 1, 0.1),
       axial_depth_mm: Math.round(ap * 1000) / 1000,
       radial_depth_mm: Math.round(ae * 1000) / 1000,
 
-      mrr_cm3min: Math.round(finalMRR * 100) / 100,
-      power_kw: Math.round(finalPower * 100) / 100,
-      torque_Nm: Math.round(finalTorque * 100) / 100,
-      tangential_force_N: Math.round(finalFc),
+      mrr_cm3min: roundPositive(finalMRR, 3, 0.001),
+      power_kw: roundPositive(finalPower, 3, 0.001),
+      torque_Nm: roundPositive(finalTorque, 3, 0.001),
+      tangential_force_N: roundPositive(finalFc, 1, 0.1),
       tool_life_min: Math.round(finalLife),
-      surface_finish_Ra_um: Math.round(finalRa * 100) / 100,
+      surface_finish_Ra_um: roundPositive(finalRa, 3, 0.001),
       deflection_um: Math.round(finalDefl_mm * 1000 * 10) / 10,
 
       overall_confidence: Math.round(overallConfidence * 1000) / 1000,
@@ -4177,7 +4488,7 @@ function optimizeFn(engine: SpeedFeedOrchestratorEngine, input: OrchestratorInpu
     const noseR = input.corner_radius_mm ?? 0.4;
     const Ra = (fz * fz) / (32 * noseR) * 1000;
 
-    return [mrr, Math.min(toolLife, 999), -Ra]; // all maximize
+    return [mrr, capReportedToolLifeMinutes(toolLife), -Ra]; // all maximize
   };
 
   // Dominance: a dominates b if a[i] >= b[i] for all i, strictly > for at least one
diff --git a/mcp-server/src/engines/TribalKnowledgeAdvisorEngine.ts b/mcp-server/src/engines/TribalKnowledgeAdvisorEngine.ts
index 2606002a7..97129b515 100644
--- a/mcp-server/src/engines/TribalKnowledgeAdvisorEngine.ts
+++ b/mcp-server/src/engines/TribalKnowledgeAdvisorEngine.ts
@@ -44,8 +44,20 @@ export interface TribalQueryContext {
   keywords?: string[];
   /** Customer name for customer-specific tips */
   customer?: string;
+  /** Hardness value in HB */
+  hardness_hb?: number;
   /** Hardness value in HRC */
   hardness_hrc?: number;
+  /** Cut type from calculator/orchestrator */
+  cut_type?: string;
+  /** High-level strategy, e.g. hsm/adaptive/trochoidal */
+  strategy?: string;
+  /** CAM-specific strategy label, e.g. Dynamic Milling */
+  cam_strategy?: string;
+  /** Radial engagement as percent of tool diameter */
+  radial_depth_pct?: number;
+  /** Workholding type, used for evidence search */
+  workholding?: string;
   /** Surface finish target Ra in um */
   target_ra_um?: number;
 }
@@ -62,6 +74,8 @@ export interface TribalModifiers {
   fz_modifier: number;
   /** Depth of cut modifier */
   ap_modifier: number;
+  /** Radial engagement / width of cut modifier */
+  ae_modifier: number;
   /** Tool life modifier (>1 = extended, <1 = reduced) */
   tool_life_modifier: number;
   /** Coolant recommendation */
@@ -74,6 +88,10 @@ export interface TribalModifiers {
   tip_ids: string[];
   /** Overall confidence (0-1) */
   confidence: number;
+  /** Number of direct rule/tip evidence items used */
+  evidence_count: number;
+  /** Whether modifiers are approved for automatic application */
+  auto_apply_approved: boolean;
 }
 
 /** Constraints on cutting parameters */
@@ -181,6 +199,7 @@ export class TribalKnowledgeAdvisorEngine {
     let vc_modifier = 1.0;
     let fz_modifier = 1.0;
     let ap_modifier = 1.0;
+    let ae_modifier = 1.0;
     let tool_life_modifier = 1.0;
     let coolant_strategy: TribalModifiers["coolant_strategy"];
     let chip_break_strategy: TribalModifiers["chip_break_strategy"];
@@ -188,7 +207,7 @@ export class TribalKnowledgeAdvisorEngine {
     // 1. Apply material modifiers
     if (context.material) {
       const matKey = this._normalizeMaterial(context.material);
-      const matMod = MATERIAL_MODIFIERS[matKey];
+      const matMod = this._contextualMaterialModifier(matKey, context);
       if (matMod) {
         if (matMod.vc_modifier) {
           vc_modifier *= matMod.vc_modifier;
@@ -222,6 +241,7 @@ export class TribalKnowledgeAdvisorEngine {
         if (opMod.vc_modifier) vc_modifier *= opMod.vc_modifier;
         if (opMod.fz_modifier) fz_modifier *= opMod.fz_modifier;
         if (opMod.ap_modifier) ap_modifier *= opMod.ap_modifier;
+        if (opMod.ae_modifier) ae_modifier *= opMod.ae_modifier;
         if (opMod.coolant_strategy) coolant_strategy = opMod.coolant_strategy;
         if (opMod.chip_break_strategy) chip_break_strategy = opMod.chip_break_strategy;
         notes.push(`Operation ${context.operation}: modifiers applied`);
@@ -241,18 +261,22 @@ export class TribalKnowledgeAdvisorEngine {
     }
 
     // Calculate confidence based on matching depth
-    const confidence = Math.min(0.95, 0.5 + tip_ids.length * 0.08);
+    const evidence_count = tip_ids.length;
+    const confidence = Math.min(0.95, 0.5 + evidence_count * 0.08);
 
     return {
       vc_modifier: Math.round(vc_modifier * 100) / 100,
       fz_modifier: Math.round(fz_modifier * 100) / 100,
       ap_modifier: Math.round(ap_modifier * 100) / 100,
+      ae_modifier: Math.round(ae_modifier * 100) / 100,
       tool_life_modifier: Math.round(tool_life_modifier * 100) / 100,
       coolant_strategy,
       chip_break_strategy,
       notes,
       tip_ids,
       confidence,
+      evidence_count,
+      auto_apply_approved: evidence_count > 0 && confidence >= 0.66,
     };
   }
 
@@ -404,6 +428,48 @@ export class TribalKnowledgeAdvisorEngine {
     return m.replace(/[^a-z0-9]/g, "");
   }
 
+  private _contextualMaterialModifier(
+    matKey: string,
+    context: TribalQueryContext,
+  ): Partial<TribalModifiers> | undefined {
+    const base = MATERIAL_MODIFIERS[matKey];
+    if (!base) return undefined;
+
+    // Annealed A2 at low radial HSM engagement behaves like a productive ISO-P
+    // tool steel cut; do not apply hardened/abusive roughing derates here.
+    if (matKey === "a2" && this._isAnnealedLightRadialHsm(context)) {
+      return {
+        ...base,
+        vc_modifier: 1.0,
+        fz_modifier: 0.90,
+        tool_life_modifier: 0.95,
+        coolant_strategy: "flood",
+      };
+    }
+
+    return base;
+  }
+
+  private _isAnnealedLightRadialHsm(context: TribalQueryContext): boolean {
+    const hrc = context.hardness_hrc;
+    const hb = context.hardness_hb;
+    const annealed = (hrc !== undefined && hrc <= 32)
+      || (hb !== undefined && hb <= 300)
+      || (hrc === undefined && hb === undefined && /anneal|200|230/i.test(context.material ?? ""));
+
+    const strategyText = [
+      context.strategy,
+      context.cam_strategy,
+      ...(context.keywords ?? []),
+    ].filter(Boolean).join(" ").toLowerCase();
+
+    const lightRadial = context.radial_depth_pct === undefined || context.radial_depth_pct <= 25;
+    const hsmLike = /hsm|adaptive|dynamic|trochoidal|peel|hpc|optimized rough/i.test(strategyText);
+    const milling = !context.operation || /mill/i.test(context.operation);
+
+    return annealed && lightRadial && hsmLike && milling;
+  }
+
   private _queryTribalKnowledge(context: TribalQueryContext): KnowledgeTip[] {
     try {
       // Build search keywords from context
@@ -423,7 +489,7 @@ export class TribalKnowledgeAdvisorEngine {
         min_confidence: 50,
       });
 
-      return result.tips || [];
+      return Array.isArray(result) ? result : result.tips || [];
     } catch (err) {
       log.warn("TribalKnowledgeAdvisorEngine: Failed to query tribal knowledge", { error: err });
       return [];
diff --git a/mcp-server/src/engines/index.ts b/mcp-server/src/engines/index.ts
index d9105b2ea..869ae2612 100644
--- a/mcp-server/src/engines/index.ts
+++ b/mcp-server/src/engines/index.ts
@@ -3027,13 +3027,13 @@ export { bashCommandClassifierEngine, BashCommandClassifierEngine } from "./Bash
 // Context Integrity — guards against quality degradation from token optimization
 export { contextIntegrityEngine, ContextIntegrityEngine } from "./ContextIntegrityEngine.js";
 
-// Belt Drive Engine � V-belt & timing belt selection, tension, life
+// Belt Drive Engine  V-belt & timing belt selection, tension, life
 export { beltDriveEngine, BeltDriveEngine } from "./BeltDriveEngine.js";
 
-// Spring Calc Engine � helical compression spring rate, stress, fatigue
+// Spring Calc Engine  helical compression spring rate, stress, fatigue
 export { springCalcEngine, SpringCalcEngine } from "./SpringCalcEngine.js";
 
-// Weld Strength Engine � fillet/butt weld stress, AWS D1.1 allowables
+// Weld Strength Engine  fillet/butt weld stress, AWS D1.1 allowables
 export { weldStrengthEngine, WeldStrengthEngine } from "./WeldStrengthEngine.js";
 
 // Output Budget Enforcer — per-tool output token budgets with truncation
@@ -3056,13 +3056,13 @@ export {
   type OfflineConfig,
 } from "./PerformanceBudgetEngine.js";
 
-// Gear Train Engine � spur/helical gear pair, Lewis bending, AGMA contact stress
+// Gear Train Engine  spur/helical gear pair, Lewis bending, AGMA contact stress
 export { gearTrainEngine, GearTrainEngine } from "./GearTrainEngine.js";
 
-// Hydraulic Cylinder Engine � force, flow, buckling, cycle time
+// Hydraulic Cylinder Engine  force, flow, buckling, cycle time
 export { hydraulicCylinderEngine, HydraulicCylinderEngine } from "./HydraulicCylinderEngine.js";
 
-// Pneumatic Cylinder Engine � air cylinder sizing, consumption, cushioning
+// Pneumatic Cylinder Engine  air cylinder sizing, consumption, cushioning
 export { pneumaticCylinderEngine, PneumaticCylinderEngine } from "./PneumaticCylinderEngine.js";
 
 // Session Token Ledger — real-time token cost accounting per tool call
@@ -3071,7 +3071,7 @@ export { sessionTokenLedgerEngine, SessionTokenLedgerEngine } from "./SessionTok
 // Smart Prefetch — predicts needed files from imports and co-access patterns
 export { smartPrefetchEngine, SmartPrefetchEngine } from "./SmartPrefetchEngine.js";
 
-// Bolt Torque Engine � torque-tension, clamp load, VDI 2230
+// Bolt Torque Engine  torque-tension, clamp load, VDI 2230
 export { boltTorqueEngine, BoltTorqueEngine } from "./BoltTorqueEngine.js";
 
 // Tool Axis Optimization Engine — 5-axis orientation optimization [CAMK-MS0/U05]
@@ -3089,10 +3089,10 @@ export { algorithmSelectorEngine, type NovelAlgorithmId, type AlgorithmSelection
 // Feature To Zone Engine — geometric feature → machining zone mapper [CAMK-MS0/U01]
 export { featureToZoneEngine, type FeatureInput as ZoneFeatureInput, type MachiningZone, type FeatureToZoneResult } from "./FeatureToZoneEngine.js";
 
-// Fatigue Life Engine � S-N curve, Goodman/Gerber, Miner damage
+// Fatigue Life Engine  S-N curve, Goodman/Gerber, Miner damage
 export { fatigueLifeEngine, FatigueLifeEngine } from "./FatigueLifeEngine.js";
 
-// Heat Treatment Engine � hardening, tempering, carburizing, annealing
+// Heat Treatment Engine  hardening, tempering, carburizing, annealing
 export { heatTreatmentEngine, HeatTreatmentEngine } from "./HeatTreatmentEngine.js";
 
 // Tool Call Pipeline — declarative tool call chains with dry-run costing
@@ -3101,13 +3101,13 @@ export { toolCallPipelineEngine, ToolCallPipelineEngine } from "./ToolCallPipeli
 // Compaction Strategy — intelligent keep/compress/drop decisions for context
 export { compactionStrategyEngine, CompactionStrategyEngine } from "./CompactionStrategyEngine.js";
 
-// Cam Profile Engine � disc cam mechanism, pressure angle, contact stress
+// Cam Profile Engine  disc cam mechanism, pressure angle, contact stress
 export { camProfileEngine, CamProfileEngine } from "./CamProfileEngine.js";
 
-// Surface Treatment Engine � anodize, plate, nitride, shot peen
+// Surface Treatment Engine  anodize, plate, nitride, shot peen
 export { surfaceTreatmentEngine, SurfaceTreatmentEngine } from "./SurfaceTreatmentEngine.js";
 
-// Electric Motor Engine � motor sizing, efficiency, VFD, energy cost
+// Electric Motor Engine  motor sizing, efficiency, VFD, energy cost
 export { electricMotorEngine, ElectricMotorEngine } from "./ElectricMotorEngine.js";
 
 // Tool Output Fingerprinter — detects recurring duplicate outputs via hashing
@@ -3122,13 +3122,13 @@ export { toolCallThrottleEngine, ToolCallThrottleEngine } from "./ToolCallThrott
 // Edit Planner — minimizes edit context for token savings, batch planning
 export { editPlannerEngine, EditPlannerEngine } from "./EditPlannerEngine.js";
 
-// Bearing Selection Engine � ISO 281 L10 life, ndm, lubrication
+// Bearing Selection Engine  ISO 281 L10 life, ndm, lubrication
 export { bearingSelectionEngine, BearingSelectionEngine } from "./BearingSelectionEngine.js";
 
-// Shrink Fit Engine � interference fit, Lame pressure, assembly temp
+// Shrink Fit Engine  interference fit, Lame pressure, assembly temp
 export { shrinkFitEngine, ShrinkFitEngine } from "./ShrinkFitEngine.js";
 
-// Spline Stress Engine � involute spline shear/bearing, fretting risk
+// Spline Stress Engine  involute spline shear/bearing, fretting risk
 export { splineStressEngine, SplineStressEngine } from "./SplineStressEngine.js";
 
 // Tool Selection Advisor — intent-based tool recommendation for cheapest option
@@ -3137,60 +3137,60 @@ export { toolSelectionAdvisorEngine, ToolSelectionAdvisorEngine } from "./ToolSe
 // Grep Optimizer — optimizes Grep params for minimal token cost
 export { grepOptimizerEngine, GrepOptimizerEngine } from "./GrepOptimizerEngine.js";
 
-// Crane Load Engine � sling tension, wire rope SF, dynamic factors
+// Crane Load Engine  sling tension, wire rope SF, dynamic factors
 export { craneLoadEngine, CraneLoadEngine } from "./CraneLoadEngine.js";
 
-// Conveyor Design Engine � belt conveyor capacity, power, tension
+// Conveyor Design Engine  belt conveyor capacity, power, tension
 export { conveyorDesignEngine, ConveyorDesignEngine } from "./ConveyorDesignEngine.js";
 
-// Piping Pressure Engine � Darcy-Weisbach, Reynolds, minor losses
+// Piping Pressure Engine  Darcy-Weisbach, Reynolds, minor losses
 export { pipingPressureEngine, PipingPressureEngine } from "./PipingPressureEngine.js";
 
-// EDM Parameter Engine � wire/sinker EDM pulse settings, MRR, wear
+// EDM Parameter Engine  wire/sinker EDM pulse settings, MRR, wear
 export { edmParameterEngine, EDMParameterEngine } from "./EDMParameterEngine.js";
 
-// Plasma Arc Engine � plasma cutting current, speed, gas, cost
+// Plasma Arc Engine  plasma cutting current, speed, gas, cost
 export { plasmaArcEngine, PlasmaArcEngine } from "./PlasmaArcEngine.js";
 
-// Electrochemical Engine � ECM/ECD Faraday MRR, current density
+// Electrochemical Engine  ECM/ECD Faraday MRR, current density
 export { electrochemicalEngine, ElectrochemicalEngine } from "./ElectrochemicalEngine.js";
 
-// Batch 36 � Flywheel, Clutch/Brake, Coupling
+// Batch 36  Flywheel, Clutch/Brake, Coupling
 export { FlywheelEnergyEngine, flywheelEnergyEngine } from './FlywheelEnergyEngine.js';
 export { ClutchBrakeEngine, clutchBrakeEngine } from './ClutchBrakeEngine.js';
 export { CouplingSelectionEngine, couplingSelectionEngine } from './CouplingSelectionEngine.js';
 
-// Batch 37 � Corrosion, Creep, Fracture
+// Batch 37  Corrosion, Creep, Fracture
 export { CorrosionRateEngine, corrosionRateEngine } from './CorrosionRateEngine.js';
 export { CreepLifeEngine, creepLifeEngine } from './CreepLifeEngine.js';
 export { FractureToughnessEngine, fractureToughnessEngine } from './FractureToughnessEngine.js';
 
-// Batch 38 � DynamicBalance, NoiseLevel, SealSelection
+// Batch 38  DynamicBalance, NoiseLevel, SealSelection
 export { DynamicBalanceEngine, dynamicBalanceEngine } from './DynamicBalanceEngine.js';
 export { NoiseLevelEngine, noiseLevelEngine } from './NoiseLevelEngine.js';
 export { SealSelectionEngine, sealSelectionEngine } from './SealSelectionEngine.js';
 
-// Batch 39 � ChainDrive, LinearMotion, ValveSizing
+// Batch 39  ChainDrive, LinearMotion, ValveSizing
 export { ChainDriveEngine, chainDriveEngine } from './ChainDriveEngine.js';
 export { LinearMotionEngine, linearMotionEngine } from './LinearMotionEngine.js';
 export { ValveSizingEngine, valveSizingEngine } from './ValveSizingEngine.js';
 
-// Batch 40 � HeatExchanger, PumpSelection, FanSelection
+// Batch 40  HeatExchanger, PumpSelection, FanSelection
 export { HeatExchangerEngine, heatExchangerEngine } from './HeatExchangerEngine.js';
 export { PumpSelectionEngine, pumpSelectionEngine } from './PumpSelectionEngine.js';
 export { FanSelectionEngine, fanSelectionEngine } from './FanSelectionEngine.js';
 
-// Batch 41 � WeldDistortion, BroachDesign, PressBrake
+// Batch 41  WeldDistortion, BroachDesign, PressBrake
 export { WeldDistortionEngine, weldDistortionEngine } from './WeldDistortionEngine.js';
 export { BroachDesignEngine, broachDesignEngine } from './BroachDesignEngine.js';
 export { PressBrakeEngine, pressBrakeEngine } from './PressBrakeEngine.js';
 
-// Batch 42 � RollingContact, ElectroPlating, CastingDefect
+// Batch 42  RollingContact, ElectroPlating, CastingDefect
 export { RollingContactEngine, rollingContactEngine } from './RollingContactEngine.js';
 export { ElectroplatingEngine, electroplatingEngine } from './ElectroPlatingEngine.js';
 export { CastingDefectEngine, castingDefectEngine } from './CastingDefectEngine.js';
 
-// Batch 43 � TorsionBar, ColumnBuckling, FlatPattern
+// Batch 43  TorsionBar, ColumnBuckling, FlatPattern
 export { TorsionBarEngine, torsionBarEngine } from './TorsionBarEngine.js';
 export { ColumnBucklingEngine, columnBucklingEngine } from './ColumnBucklingEngine.js';
 export { FlatPatternEngine, flatPatternEngine } from './FlatPatternEngine.js';
@@ -3218,7 +3218,7 @@ export {
 export { ultimateSpeedFeedEngine, UltimateSpeedFeedEngine } from "./UltimateSpeedFeedEngine.js";
 export { postProcessorGeneratorEngine, PostProcessorGeneratorEngine } from "./PostProcessorGeneratorEngine.js";
 
-// Batch 44 � ThreadGage, SurfaceIntegrity, MachineVibration
+// Batch 44  ThreadGage, SurfaceIntegrity, MachineVibration
 export { ThreadGageEngine, threadGageEngine } from './ThreadGageEngine.js';
 export { SurfaceIntegrityEngine, surfaceIntegrityEngine } from './SurfaceIntegrityEngine.js';
 export { MachineVibrationEngine, machineVibrationEngine } from './MachineVibrationEngine.js';
@@ -3717,7 +3717,7 @@ export { signalProcessingToolkitEngine } from './SignalProcessingToolkitEngine.j
 export { timeSeriesCompletionEngine } from './TimeSeriesCompletionEngine.js';
 export { varianceReductionEngine } from './VarianceReductionEngine.js';
 
-// Batch 9 � final unexported engines (2026-03-14)
+// Batch 9  final unexported engines (2026-03-14)
 export { assemblyEngine } from './AssemblyEngine.js';
 export { multiProcessCAMBridgeEngine, MultiProcessCAMBridgeEngine } from './MultiProcessCAMBridgeEngine.js';
 
@@ -3888,7 +3888,7 @@ export { laserInterferometerCompensationEngine, LaserInterferometerCompensationE
 // ProvenPartRecipeEngine — Proven manufacturing recipe storage (FAI-passed parts), ~/.prism/proven-recipes/ persistence
 export { provenPartRecipeEngine, ProvenPartRecipeEngine } from "./ProvenPartRecipeEngine.js";
 
-// PartSimilarityEngine � Multi-dimensional part similarity scoring with weighted normalized distance
+// PartSimilarityEngine  Multi-dimensional part similarity scoring with weighted normalized distance
 export { partSimilarityEngine, PartSimilarityEngine } from "./PartSimilarityEngine.js";
 
 // AdaptivePipelineGeneratorEngine — Adapts proven recipes to new parts with Kienzle/Taylor physics scaling
@@ -3912,7 +3912,7 @@ export { InstructorDashboardEngine, instructorDashboardEngine } from "./Instruct
 // RealTimeMachineIntelligenceEngine — TECH-MS2: spindle monitor, chatter detect, thermal comp, tool life countdown, time series
 export { realTimeMachineIntelligenceEngine, RealTimeMachineIntelligenceEngine } from "./RealTimeMachineIntelligenceEngine.js";
 
-// VisualLabEngine � 3D scene descriptions for PRISM Academy machining visualization
+// VisualLabEngine  3D scene descriptions for PRISM Academy machining visualization
 export { VisualLabEngine, visualLabEngine } from "./VisualLabEngine.js";
 
 // CostSavingsTrackerEngine — VAL-MS0: ROI proof, persistent savings log
diff --git a/mcp-server/src/schemas/businessActionSchemas.ts b/mcp-server/src/schemas/businessActionSchemas.ts
index e82186465..2fa2ca3ff 100644
--- a/mcp-server/src/schemas/businessActionSchemas.ts
+++ b/mcp-server/src/schemas/businessActionSchemas.ts
@@ -2340,7 +2340,7 @@ export const ACTION_BUSINESS_SCHEMAS: ActionSchemaMap = {
     complexity: z.enum(["simple", "moderate", "complex", "extreme"]).describe("Part complexity"),
     urgency: z.enum(["standard", "rush", "emergency"]).describe("Order urgency"),
   }).passthrough(),
-  // �                                ──
+  // � ──
   tool_inv_check_availability: z.object({
     operations: z.array(z.object({
       type: z.string().describe("Operation type (face_mill, pocket, drill, turn, thread, etc.)"),
diff --git a/mcp-server/src/state/shared/SELF_IMPROVEMENT_PATTERNS.json b/mcp-server/src/state/shared/SELF_IMPROVEMENT_PATTERNS.json
index 8f685e8d7..e43fc649b 100644
--- a/mcp-server/src/state/shared/SELF_IMPROVEMENT_PATTERNS.json
+++ b/mcp-server/src/state/shared/SELF_IMPROVEMENT_PATTERNS.json
@@ -1,87 +1,13 @@
 {
-  "timestamp": "2026-04-16T00:50:44.995Z",
-  "total_patterns": 5,
-  "patterns": [
-    {
-      "id": "SIP-001",
-      "category": "unresolved_error",
-      "description": "uncategorized in calculations: test error message",
-      "frequency": 5,
-      "severity": "medium",
-      "automation_level": 0,
-      "priority": 2.5,
-      "suggested_fix": "hook",
-      "fix_description": "Add input validation hook for prism_calc:test_action to reject invalid parameters",
-      "source_file": "failure_patterns.jsonl",
-      "domain": "calculations",
-      "last_seen": "2026-04-16T00:50:04.227Z"
-    },
-    {
-      "id": "SIP-002",
-      "category": "unresolved_error",
-      "description": "validation-failure in file-operations: Invalid params for doc_upload",
-      "frequency": 1,
-      "severity": "medium",
-      "automation_level": 0,
-      "priority": 0.5,
-      "suggested_fix": "hook",
-      "fix_description": "Add automated check for validation-failure in file-operations",
-      "source_file": "failure_patterns.jsonl",
-      "domain": "file-operations",
-      "last_seen": "2026-04-12T22:49:37.300Z"
-    },
-    {
-      "id": "SIP-003",
-      "category": "unresolved_error",
-      "description": "uncategorized in file-operations: Command failed: python -c \nimport sys, json\nsys.path.insert(0, \"H:\\\\cad-engine\")\nfrom src.document_extract import extrac",
-      "frequency": 1,
-      "severity": "medium",
-      "automation_level": 0,
-      "priority": 0.5,
-      "suggested_fix": "hook",
-      "fix_description": "Add automated check for uncategorized in file-operations",
-      "source_file": "failure_patterns.jsonl",
-      "domain": "file-operations",
-      "last_seen": "2026-04-12T22:49:55.797Z"
-    },
-    {
-      "id": "SIP-004",
-      "category": "unresolved_error",
-      "description": "stale-reference in other: LearningAdaptationEngine.getStats is not a function",
-      "frequency": 1,
-      "severity": "medium",
-      "automation_level": 0,
-      "priority": 0.5,
-      "suggested_fix": "hook",
-      "fix_description": "Add pre-flight check hook for other to validate references before use",
-      "source_file": "failure_patterns.jsonl",
-      "domain": "other",
-      "last_seen": "2026-04-15T01:46:40.624Z"
-    },
-    {
-      "id": "SIP-005",
-      "category": "unresolved_error",
-      "description": "uncategorized in other: Missing required params: task_description, requirements (array)",
-      "frequency": 1,
-      "severity": "medium",
-      "automation_level": 0,
-      "priority": 0.5,
-      "suggested_fix": "hook",
-      "fix_description": "Add automated check for uncategorized in other",
-      "source_file": "failure_patterns.jsonl",
-      "domain": "other",
-      "last_seen": "2026-04-15T01:46:52.479Z"
-    }
-  ],
-  "aggregate_priority": 4.5,
-  "system_improvement_rate": 0,
-  "sources_read": [
-    "failure_patterns.jsonl",
-    "QUALITY_SCORES.json",
-    "FORMULA_ACCURACY.json",
-    "ERROR_LOG.jsonl",
-    "LEARNING_LOG.jsonl",
-    "session-edit-counter.json"
-  ],
-  "warnings": []
+  "timestamp": "2026-04-24T20:04:46.365Z",
+  "total_patterns": 0,
+  "patterns": [],
+  "aggregate_priority": 0,
+  "system_improvement_rate": 1,
+  "sources_read": [],
+  "warnings": [
+    "failure_patterns.jsonl not found — skipping failure analysis",
+    "QUALITY_SCORES.json not found — skipping quality analysis",
+    "FORMULA_ACCURACY.json not found — skipping formula accuracy analysis"
+  ]
 }
\ No newline at end of file
diff --git a/mcp-server/web/scripts/calculator-live-variability-sweep.ts b/mcp-server/web/scripts/calculator-live-variability-sweep.ts
index e82c12a6b..348d798b5 100644
--- a/mcp-server/web/scripts/calculator-live-variability-sweep.ts
+++ b/mcp-server/web/scripts/calculator-live-variability-sweep.ts
@@ -1,4 +1,4 @@
-import { mkdirSync, writeFileSync } from 'node:fs';
+import { existsSync, mkdirSync, readFileSync, writeFileSync } from 'node:fs';
 import path from 'node:path';
 
 import {
@@ -26,6 +26,23 @@ import {
 import { buildCalculatorSpeedFeedParams } from '../src/utils/calculatorSpeedFeedContract.ts';
 import { deriveToolReachDefaults } from '../src/utils/calculatorParameterOptimization.ts';
 
+const DEFAULT_ENDPOINT_BASE = 'http://127.0.0.1:3000/api/v1/speed-feed';
+const ENDPOINT_BASE = process.env.PRISM_SPEEDFEED_URL ?? DEFAULT_ENDPOINT_BASE;
+const OUTPUT_DIR = path.resolve('H:/PRISM/output');
+const OUTPUT_BASENAME = process.env.PRISM_OUTPUT_BASENAME ?? 'calculator-live-variability-sweep';
+const MODE_FILTER = (process.env.PRISM_SWEEP_MODE ?? 'all').toLowerCase();
+const SCENARIO_START = Math.max(0, Number(process.env.PRISM_SCENARIO_START ?? '0') || 0);
+const SCENARIO_LIMIT = Math.max(0, Number(process.env.PRISM_SCENARIO_LIMIT ?? '0') || 0);
+const OPTIMIZE_INTERVAL = Math.max(1, Number(process.env.PRISM_OPTIMIZE_INTERVAL ?? '3') || 3);
+const PROGRESS_INTERVAL = Math.max(1, Number(process.env.PRISM_PROGRESS_INTERVAL ?? '50') || 50);
+const HSM_BASELINE_ENABLED = process.env.PRISM_HSM_BASELINE === '1';
+const INCLUDE_RECORDS = process.env.PRISM_INCLUDE_RECORDS === '1';
+const INCLUDE_PARAMS = process.env.PRISM_INCLUDE_PARAMS === '1';
+const HSM_MACHINE_DB_PATHS = [
+  'C:/Users/Mark Villanueva/AppData/Roaming/HSMAdvisor/machines.xml',
+  'C:/Program Files (x86)/HSMAdvisor/machines/default_machines.machine.xml',
+];
+
 type ConventionalMode = Extract<MachineMode, 'mill' | 'lathe'>;
 
 type SweepScenario = {
@@ -49,10 +66,36 @@ type SweepCheck = {
   isAdaptive?: boolean;
   confidence?: number;
   cycleTimeSec?: number | null;
+  spindleRpm?: number | null;
+  feedRateMmMin?: number | null;
+  feedPerToothMm?: number | null;
+  cuttingSpeedMpm?: number | null;
+  surfaceFinishRaUm?: number | null;
+  toolLifeMin?: number | null;
+  axialDepthMm?: number | null;
+  radialDepthMm?: number | null;
+  mrrCm3Min?: number | null;
+  powerKw?: number | null;
+  deflectionUm?: number | null;
+  hsmBaselineChecks?: number;
+  hsmMachineName?: string | null;
+  failedSafetyChecks?: string[];
   anomalies: string[];
 };
 
+type HsmAdvisorMachine = {
+  name: string;
+  maxHp: number | null;
+  minRpm: number | null;
+  maxRpm: number | null;
+  maxFeed: number | null;
+  units: number | null;
+};
+
+const HSM_ADVISOR_MACHINES = HSM_BASELINE_ENABLED ? loadHsmAdvisorMachines() : [];
+
 type SweepRecord = {
+  scenarioIndex: number;
   scenario: {
     mode: ConventionalMode;
     programmingId: string;
@@ -62,15 +105,321 @@ type SweepRecord = {
     materialId: string;
     toolId: string;
   };
+  request?: Record<string, unknown>;
   checks: SweepCheck[];
 };
 
+const NUMERIC_SUMMARY_FIELDS = [
+  'confidence',
+  'cycleTimeSec',
+  'spindleRpm',
+  'feedRateMmMin',
+  'feedPerToothMm',
+  'cuttingSpeedMpm',
+  'axialDepthMm',
+  'radialDepthMm',
+  'mrrCm3Min',
+  'surfaceFinishRaUm',
+  'toolLifeMin',
+  'powerKw',
+  'deflectionUm',
+  'hsmBaselineChecks',
+] as const;
+
+type NumericSweepField = (typeof NUMERIC_SUMMARY_FIELDS)[number];
+
 type CatalogBundle = {
   machines: Record<ConventionalMode, MachineCatalogItem[]>;
   materials: MaterialCatalogItem[];
   tools: Record<ConventionalMode, ToolCatalogItem[]>;
 };
 
+function normalizeBaselineText(value: unknown) {
+  return String(value ?? '').toLowerCase().replace(/[^a-z0-9]/g, '');
+}
+
+function parseXmlNumber(section: string, tag: string): number | null {
+  const match = section.match(new RegExp(`<${tag}>([^<]*)</${tag}>`, 'i'));
+  if (!match) return null;
+  const value = Number(match[1]);
+  return Number.isFinite(value) ? value : null;
+}
+
+function parseXmlText(section: string, tag: string): string | null {
+  const match = section.match(new RegExp(`<${tag}>([^<]*)</${tag}>`, 'i'));
+  return match?.[1]?.trim() || null;
+}
+
+function loadHsmAdvisorMachines(): HsmAdvisorMachine[] {
+  const dbPath = HSM_MACHINE_DB_PATHS.find((candidate) => existsSync(candidate));
+  if (!dbPath) return [];
+
+  const xml = readFileSync(dbPath, 'utf8');
+  const machineBlocks = xml.match(/<skv_fswizard_machine>[\s\S]*?<\/skv_fswizard_machine>/gi) ?? [];
+  return machineBlocks
+    .map((block) => ({
+      name: parseXmlText(block, 'name') ?? '',
+      maxHp: parseXmlNumber(block, 'Max_HP'),
+      minRpm: parseXmlNumber(block, 'Min_RPM'),
+      maxRpm: parseXmlNumber(block, 'Max_RPM'),
+      maxFeed: parseXmlNumber(block, 'Max_Feed'),
+      units: parseXmlNumber(block, 'units'),
+    }))
+    .filter((machine) => machine.name.length > 0);
+}
+
+function hsmAdvisorMachineDbPath() {
+  return HSM_MACHINE_DB_PATHS.find((candidate) => existsSync(candidate)) ?? null;
+}
+
+function machineMaxRpmHint(machine: MachineCatalogItem): number | null {
+  const rawMachine = machine as any;
+  const direct = Number(rawMachine.maxRpm ?? rawMachine.spindleMaxRpm ?? rawMachine.max_spindle_rpm);
+  if (Number.isFinite(direct) && direct > 0) return direct;
+
+  const spindleText = [
+    ...(Array.isArray(machine.spindleOptions) ? machine.spindleOptions : []),
+  ].map((option: any) => `${option?.label ?? ''} ${option?.detail ?? ''}`).join(' ');
+  const rpmValues = [...spindleText.matchAll(/(\d{4,5})\s*rpm/gi)]
+    .map((match) => Number(match[1]))
+    .filter((value) => Number.isFinite(value) && value > 0);
+  return rpmValues.length > 0 ? Math.max(...rpmValues) : null;
+}
+
+function chooseClosestHsmMachine(candidates: HsmAdvisorMachine[], rpmHint: number | null) {
+  if (candidates.length === 0) return null;
+  if (rpmHint == null) return candidates[0];
+  return [...candidates].sort((a, b) => {
+    const aDelta = typeof a.maxRpm === 'number' ? Math.abs(a.maxRpm - rpmHint) : Number.POSITIVE_INFINITY;
+    const bDelta = typeof b.maxRpm === 'number' ? Math.abs(b.maxRpm - rpmHint) : Number.POSITIVE_INFINITY;
+    return aDelta - bDelta;
+  })[0];
+}
+
+function findHsmAdvisorMachine(machine: MachineCatalogItem): HsmAdvisorMachine | null {
+  if (!HSM_BASELINE_ENABLED || HSM_ADVISOR_MACHINES.length === 0) return null;
+
+  const rawMachine = machine as any;
+  const signature = normalizeBaselineText([
+    machine.id,
+    rawMachine.label,
+    rawMachine.name,
+    machine.machineTypeLabel,
+  ].join(' '));
+  const rpmHint = machineMaxRpmHint(machine);
+  const exactRules: Array<[RegExp, RegExp]> = [
+    [/haas.*vf2|vf2ss|vf2/, /haas.*(vf2|vfseries)/],
+    [/haas.*umc|umc750/, /haas.*umc750|haas.*vfseries/],
+    [/okuma.*genos|m560/, /okuma.*genos.*m560/],
+    [/okuma.*b300|lb300|millturn/, /okuma.*b300|okuma.*millturn/],
+    [/hurco|vmx30/, /hurcovmx30ui/],
+  ];
+
+  for (const [sourcePattern, hsmPattern] of exactRules) {
+    if (!sourcePattern.test(signature)) continue;
+    const match = chooseClosestHsmMachine(
+      HSM_ADVISOR_MACHINES.filter((candidate) => hsmPattern.test(normalizeBaselineText(candidate.name))),
+      rpmHint,
+    );
+    if (match) return match;
+  }
+
+  const machineTokens = signature.split(/(?=haas|okuma|hurco|vf|umc|genos)/g).filter(Boolean);
+  const fuzzy = HSM_ADVISOR_MACHINES.find((candidate) => {
+    const candidateName = normalizeBaselineText(candidate.name);
+    return machineTokens.some((token) => token.length >= 5 && candidateName.includes(token));
+  });
+  if (fuzzy) return fuzzy;
+
+  if (/generic|vmc/.test(signature)) {
+    return HSM_ADVISOR_MACHINES.find((candidate) => normalizeBaselineText(candidate.name) === 'genericvmc') ?? null;
+  }
+  return null;
+}
+
+function relativeDelta(actual: number, expected: number) {
+  if (!Number.isFinite(actual) || !Number.isFinite(expected) || Math.abs(expected) < 1e-9) return Infinity;
+  return Math.abs(actual - expected) / Math.abs(expected);
+}
+
+function roundAuditValue(value: number | null) {
+  return typeof value === 'number' && Number.isFinite(value)
+    ? Number(value.toFixed(6))
+    : null;
+}
+
+function summarizeValues(values: number[]) {
+  if (values.length === 0) {
+    return { count: 0, min: null, max: null, avg: null };
+  }
+
+  const min = Math.min(...values);
+  const max = Math.max(...values);
+  const avg = values.reduce((sum, value) => sum + value, 0) / values.length;
+  return {
+    count: values.length,
+    min: roundAuditValue(min),
+    max: roundAuditValue(max),
+    avg: roundAuditValue(avg),
+  };
+}
+
+function incrementCounter(counter: Record<string, number>, key: string | null | undefined) {
+  if (!key) return;
+  counter[key] = (counter[key] ?? 0) + 1;
+}
+
+function summarizeSweepRecords(records: SweepRecord[]) {
+  const routeSummaries: Record<string, unknown> = {};
+
+  for (const route of ['quick', 'orchestrate', 'optimize'] as const) {
+    const checks = records.flatMap((record) => record.checks.filter((check) => check.route === route));
+    const metricValues: Record<NumericSweepField, number[]> = Object.fromEntries(
+      NUMERIC_SUMMARY_FIELDS.map((field) => [field, []]),
+    ) as Record<NumericSweepField, number[]>;
+    const strategySources: Record<string, number> = {};
+    const hsmMachines: Record<string, number> = {};
+    let failedCheckCount = 0;
+    let anomalyItemCount = 0;
+    let hsmCheckedCount = 0;
+
+    for (const check of checks) {
+      if (!check.ok) failedCheckCount += 1;
+      anomalyItemCount += check.anomalies.length;
+      if ((check.hsmBaselineChecks ?? 0) > 0) hsmCheckedCount += 1;
+      incrementCounter(strategySources, check.strategySource);
+      incrementCounter(hsmMachines, check.hsmMachineName);
+
+      for (const field of NUMERIC_SUMMARY_FIELDS) {
+        const value = check[field];
+        if (typeof value === 'number' && Number.isFinite(value)) {
+          metricValues[field].push(value);
+        }
+      }
+    }
+
+    routeSummaries[route] = {
+      checkCount: checks.length,
+      failedCheckCount,
+      anomalyItemCount,
+      hsmCheckedCount,
+      strategySources,
+      hsmMachines,
+      metrics: Object.fromEntries(
+        NUMERIC_SUMMARY_FIELDS.map((field) => [field, summarizeValues(metricValues[field])]),
+      ),
+    };
+  }
+
+  return routeSummaries;
+}
+
+function toolFluteCount(tool: ToolCatalogItem, toolpath: ProgrammingEnvironmentOption['toolpaths'][number]) {
+  const rawTool = tool as any;
+  const explicit = Number(
+    rawTool.flutes
+    ?? rawTool.defaultFlutes
+    ?? rawTool.fluteCount
+    ?? rawTool.numberOfFlutes,
+  );
+  if (Number.isFinite(explicit) && explicit > 0) return explicit;
+  const signature = `${rawTool.operation ?? ''} ${rawTool.geometryClass ?? ''} ${toolpath.operationId ?? ''} ${toolpath.label ?? ''}`.toLowerCase();
+  if (/drill|ream|spot/.test(signature)) return 2;
+  return 4;
+}
+
+function applyHsmBaselineChecks(
+  scenario: SweepScenario,
+  anomalies: string[],
+  result: {
+    spindleRpm: number | null | undefined;
+    feedRateMmMin: number | null | undefined;
+    feedPerToothMm: number | null | undefined;
+    cuttingSpeedMpm: number | null | undefined;
+    axialDepthMm: number | null | undefined;
+    radialDepthMm: number | null | undefined;
+    mrrCm3Min: number | null | undefined;
+    powerKw: number | null | undefined;
+  },
+) {
+  if (!HSM_BASELINE_ENABLED) {
+    return { checkCount: 0, machineName: null as string | null };
+  }
+
+  let checkCount = 0;
+  const diameterMm = Number((scenario.tool as any).defaultDiameter ?? 0);
+  const flutes = toolFluteCount(scenario.tool, scenario.toolpath);
+
+  if (
+    diameterMm > 0
+    && typeof result.cuttingSpeedMpm === 'number'
+    && typeof result.spindleRpm === 'number'
+  ) {
+    checkCount += 1;
+    const expectedRpm = (1000 * result.cuttingSpeedMpm) / (Math.PI * diameterMm);
+    const rpmTolerance = Math.max(25, expectedRpm * 0.025);
+    if (Math.abs(result.spindleRpm - expectedRpm) > rpmTolerance) {
+      anomalies.push(`hsm_baseline_rpm_formula:${result.spindleRpm.toFixed(1)}!=${expectedRpm.toFixed(1)}`);
+    }
+  }
+
+  if (
+    typeof result.spindleRpm === 'number'
+    && typeof result.feedPerToothMm === 'number'
+    && typeof result.feedRateMmMin === 'number'
+  ) {
+    checkCount += 1;
+    const expectedFeed = result.spindleRpm * result.feedPerToothMm * flutes;
+    const feedTolerance = Math.max(1, expectedFeed * 0.03);
+    if (Math.abs(result.feedRateMmMin - expectedFeed) > feedTolerance) {
+      anomalies.push(`hsm_baseline_feed_formula:${result.feedRateMmMin.toFixed(2)}!=${expectedFeed.toFixed(2)}`);
+    }
+  }
+
+  if (
+    diameterMm > 0
+    && typeof result.feedRateMmMin === 'number'
+    && typeof result.axialDepthMm === 'number'
+    && typeof result.radialDepthMm === 'number'
+    && typeof result.mrrCm3Min === 'number'
+  ) {
+    checkCount += 1;
+    const operation = scenario.toolpath.operationId;
+    const expectedMrr = operation === 'drilling'
+      ? ((Math.PI * diameterMm * diameterMm) / 4) * result.feedRateMmMin / 1000
+      : result.axialDepthMm * result.radialDepthMm * result.feedRateMmMin / 1000;
+    if (relativeDelta(result.mrrCm3Min, expectedMrr) > 0.035 && Math.abs(result.mrrCm3Min - expectedMrr) > 0.01) {
+      anomalies.push(`hsm_baseline_mrr_formula:${result.mrrCm3Min.toFixed(3)}!=${expectedMrr.toFixed(3)}`);
+    }
+  }
+
+  const hsmMachine = findHsmAdvisorMachine(scenario.machine);
+  if (hsmMachine) {
+    if (typeof result.spindleRpm === 'number' && typeof hsmMachine.maxRpm === 'number' && hsmMachine.maxRpm > 0) {
+      checkCount += 1;
+      if (result.spindleRpm > hsmMachine.maxRpm * 1.02) {
+        anomalies.push(`hsm_machine_rpm_limit:${result.spindleRpm.toFixed(0)}>${hsmMachine.maxRpm.toFixed(0)}:${hsmMachine.name}`);
+      }
+    }
+    if (typeof result.feedRateMmMin === 'number' && typeof hsmMachine.maxFeed === 'number' && hsmMachine.maxFeed > 0) {
+      checkCount += 1;
+      const feedIpm = result.feedRateMmMin / 25.4;
+      if (feedIpm > hsmMachine.maxFeed * 1.05) {
+        anomalies.push(`hsm_machine_feed_limit:${feedIpm.toFixed(1)}ipm>${hsmMachine.maxFeed.toFixed(1)}ipm:${hsmMachine.name}`);
+      }
+    }
+    if (typeof result.powerKw === 'number' && typeof hsmMachine.maxHp === 'number' && hsmMachine.maxHp > 0) {
+      checkCount += 1;
+      const hsmPowerKw = hsmMachine.maxHp * 0.7457;
+      if (result.powerKw > hsmPowerKw * 1.08) {
+        anomalies.push(`hsm_machine_power_limit:${result.powerKw.toFixed(2)}kW>${hsmPowerKw.toFixed(2)}kW:${hsmMachine.name}`);
+      }
+    }
+  }
+
+  return { checkCount, machineName: hsmMachine?.name ?? null };
+}
+
 function machineSupportsToolpath(
   mode: ConventionalMode,
   machine: MachineCatalogItem,
@@ -274,7 +623,7 @@ async function buildRequest(
 }
 
 async function callRoute(route: 'quick' | 'orchestrate' | 'optimize', body: Record<string, unknown>) {
-  const response = await fetch(`http://127.0.0.1:3001/api/v1/speed-feed/${route}`, {
+  const response = await fetch(`${ENDPOINT_BASE}/${route}`, {
     method: 'POST',
     headers: { 'Content-Type': 'application/json' },
     body: JSON.stringify(body),
@@ -302,18 +651,35 @@ function analyzeResponse(
     summarizeValue(resultRoot?.cycle_time_sec)
     ?? summarizeValue(resultRoot?.cycle_time_seconds)
     ?? summarizeValue(resultRoot?.runtime_sec);
+  const spindleRpm = summarizeValue(resultRoot?.spindle_rpm);
+  const feedRateMmMin = summarizeValue(resultRoot?.feed_rate_mmmin);
+  const feedPerToothMm = summarizeValue(resultRoot?.feed_per_tooth_mm);
+  const cuttingSpeedMpm = summarizeValue(resultRoot?.cutting_speed_mpm);
+  const axialDepthMm = summarizeValue(resultRoot?.axial_depth_mm);
+  const radialDepthMm = summarizeValue(resultRoot?.radial_depth_mm);
+  const mrrCm3Min = summarizeValue(resultRoot?.mrr_cm3min);
+  const surfaceFinishRaUm = summarizeValue(resultRoot?.surface_finish_Ra_um);
+  const toolLifeMin = summarizeValue(resultRoot?.tool_life_min);
+  const powerKw = summarizeValue(resultRoot?.power_kw);
+  const deflectionUm = summarizeValue(resultRoot?.deflection_um);
+  const failedSafetyChecks = Array.isArray(resultRoot?.safety_checks)
+    ? resultRoot.safety_checks
+      .filter((check: any) => check?.passed === false)
+      .map((check: any) => String(check?.name ?? 'unknown'))
+    : [];
 
   if (payload.status !== 200) {
     anomalies.push(`http_${payload.status}`);
   }
 
   if (route !== 'optimize') {
-    const rpm = summarizeValue(resultRoot?.spindle_rpm);
-    const feed = summarizeValue(resultRoot?.feed_rate_mmmin);
-    const ra = summarizeValue(resultRoot?.surface_finish_Ra_um);
-    if (!(typeof rpm === 'number' && Number.isFinite(rpm) && rpm > 0)) anomalies.push('invalid_rpm');
-    if (!(typeof feed === 'number' && Number.isFinite(feed) && feed > 0)) anomalies.push('invalid_feed');
-    if (!(typeof ra === 'number' && Number.isFinite(ra) && ra > 0)) anomalies.push('invalid_surface_finish');
+    if (!(typeof spindleRpm === 'number' && Number.isFinite(spindleRpm) && spindleRpm > 0)) anomalies.push('invalid_rpm');
+    if (!(typeof feedRateMmMin === 'number' && Number.isFinite(feedRateMmMin) && feedRateMmMin > 0)) anomalies.push('invalid_feed');
+    if (!(typeof axialDepthMm === 'number' && Number.isFinite(axialDepthMm) && axialDepthMm > 0)) anomalies.push('invalid_axial_depth');
+    if (!(typeof radialDepthMm === 'number' && Number.isFinite(radialDepthMm) && radialDepthMm > 0)) anomalies.push('invalid_radial_depth');
+    if (!(typeof mrrCm3Min === 'number' && Number.isFinite(mrrCm3Min) && mrrCm3Min > 0)) anomalies.push('invalid_mrr');
+    if (!(typeof surfaceFinishRaUm === 'number' && Number.isFinite(surfaceFinishRaUm) && surfaceFinishRaUm > 0)) anomalies.push('invalid_surface_finish');
+    if (failedSafetyChecks.length > 0) anomalies.push(`failed_safety:${failedSafetyChecks.join(',')}`);
   }
 
   if (route !== 'optimize' && expectedAdaptive(scenario.toolpath) && isAdaptive !== true) {
@@ -328,6 +694,27 @@ function analyzeResponse(
     anomalies.push('invalid_confidence_range');
   }
 
+  if (typeof confidence === 'number' && confidence < 0.55) {
+    anomalies.push(`low_confidence:${confidence.toFixed(3)}`);
+  }
+
+  if (typeof deflectionUm === 'number' && deflectionUm > 250) {
+    anomalies.push(`high_deflection:${deflectionUm.toFixed(1)}`);
+  }
+
+  const hsmBaseline = route !== 'optimize'
+    ? applyHsmBaselineChecks(scenario, anomalies, {
+      spindleRpm,
+      feedRateMmMin,
+      feedPerToothMm,
+      cuttingSpeedMpm,
+      axialDepthMm,
+      radialDepthMm,
+      mrrCm3Min,
+      powerKw,
+    })
+    : { checkCount: 0, machineName: null as string | null };
+
   return {
     route,
     ok: anomalies.length === 0,
@@ -340,6 +727,20 @@ function analyzeResponse(
     isAdaptive: typeof isAdaptive === 'boolean' ? isAdaptive : undefined,
     confidence: typeof confidence === 'number' ? confidence : undefined,
     cycleTimeSec: typeof cycleTimeSec === 'number' ? cycleTimeSec : null,
+    spindleRpm: typeof spindleRpm === 'number' ? spindleRpm : null,
+    feedRateMmMin: typeof feedRateMmMin === 'number' ? feedRateMmMin : null,
+    feedPerToothMm: typeof feedPerToothMm === 'number' ? feedPerToothMm : null,
+    cuttingSpeedMpm: typeof cuttingSpeedMpm === 'number' ? cuttingSpeedMpm : null,
+    axialDepthMm: typeof axialDepthMm === 'number' ? axialDepthMm : null,
+    radialDepthMm: typeof radialDepthMm === 'number' ? radialDepthMm : null,
+    mrrCm3Min: typeof mrrCm3Min === 'number' ? mrrCm3Min : null,
+    surfaceFinishRaUm: typeof surfaceFinishRaUm === 'number' ? surfaceFinishRaUm : null,
+    toolLifeMin: typeof toolLifeMin === 'number' ? toolLifeMin : null,
+    powerKw: typeof powerKw === 'number' ? powerKw : null,
+    deflectionUm: typeof deflectionUm === 'number' ? deflectionUm : null,
+    hsmBaselineChecks: hsmBaseline.checkCount,
+    hsmMachineName: hsmBaseline.machineName,
+    failedSafetyChecks,
     anomalies,
   } satisfies SweepCheck;
 }
@@ -368,14 +769,18 @@ async function loadBundle(): Promise<CatalogBundle> {
 
 async function main() {
   const bundle = await loadBundle();
-  const scenarios = buildRepresentativeScenarios(bundle);
+  const allScenarios = buildRepresentativeScenarios(bundle)
+    .filter((scenario) => MODE_FILTER === 'all' || scenario.mode === MODE_FILTER);
+  const scenarios = SCENARIO_LIMIT > 0
+    ? allScenarios.slice(SCENARIO_START, SCENARIO_START + SCENARIO_LIMIT)
+    : allScenarios.slice(SCENARIO_START);
   const holderCache = new Map<string, Awaited<ReturnType<typeof fetchToolHolderCatalog>>>();
   const records: SweepRecord[] = [];
   let quickCalls = 0;
   let orchestrateCalls = 0;
   let optimizeCalls = 0;
 
-  for (const scenarioWithLicense of scenarios) {
+  for (const [scenarioOffset, scenarioWithLicense] of scenarios.entries()) {
     const { licenseId, ...scenario } = scenarioWithLicense;
     const params = await buildRequest(scenario, holderCache);
     const checks: SweepCheck[] = [];
@@ -388,7 +793,7 @@ async function main() {
     orchestrateCalls += 1;
     checks.push(analyzeResponse('orchestrate', scenario, orchestrate));
 
-    if (quickCalls % 3 === 0) {
+    if (quickCalls % OPTIMIZE_INTERVAL === 0) {
       const optimize = await callRoute('optimize', {
         ...params,
         objectives: ['productivity', 'surface_finish', 'tool_life'],
@@ -398,6 +803,7 @@ async function main() {
     }
 
     records.push({
+      scenarioIndex: SCENARIO_START + scenarioOffset,
       scenario: {
         mode: scenario.mode,
         programmingId: scenario.programming.id,
@@ -407,12 +813,14 @@ async function main() {
         materialId: scenario.material.id,
         toolId: scenario.tool.id,
       },
+      request: INCLUDE_PARAMS ? params as Record<string, unknown> : undefined,
       checks,
     });
 
-    if (records.length % 50 === 0) {
+    if (records.length % PROGRESS_INTERVAL === 0) {
       console.log(JSON.stringify({
         progress: `${records.length}/${scenarios.length}`,
+        scenarioIndex: SCENARIO_START + scenarioOffset,
         quickCalls,
         orchestrateCalls,
         optimizeCalls,
@@ -424,29 +832,81 @@ async function main() {
     record.checks
       .filter((check) => check.anomalies.length > 0)
       .map((check) => ({
+        scenarioIndex: record.scenarioIndex,
         ...record.scenario,
         route: check.route,
         anomalies: check.anomalies,
         strategyName: check.strategyName ?? null,
         strategySource: check.strategySource ?? null,
+        confidence: check.confidence ?? null,
+        spindleRpm: check.spindleRpm ?? null,
+        feedRateMmMin: check.feedRateMmMin ?? null,
+        feedPerToothMm: check.feedPerToothMm ?? null,
+        cuttingSpeedMpm: check.cuttingSpeedMpm ?? null,
+        axialDepthMm: check.axialDepthMm ?? null,
+        radialDepthMm: check.radialDepthMm ?? null,
+        mrrCm3Min: check.mrrCm3Min ?? null,
+        surfaceFinishRaUm: check.surfaceFinishRaUm ?? null,
+        toolLifeMin: check.toolLifeMin ?? null,
+        powerKw: check.powerKw ?? null,
+        deflectionUm: check.deflectionUm ?? null,
+        hsmMachineName: check.hsmMachineName ?? null,
+        failedSafetyChecks: check.failedSafetyChecks ?? [],
       })),
   );
 
+  const lowConfidenceCount = anomalies.filter((item) =>
+    item.anomalies.some((anomaly) => anomaly.startsWith('low_confidence:')),
+  ).length;
+  const highDeflectionCount = anomalies.filter((item) =>
+    item.anomalies.some((anomaly) => anomaly.startsWith('high_deflection:')),
+  ).length;
+  const hsmBaselineCheckCount = records.reduce(
+    (sum, record) => sum + record.checks.reduce((inner, check) => inner + (check.hsmBaselineChecks ?? 0), 0),
+    0,
+  );
+  const hsmBaselineViolationCount = anomalies.filter((item) =>
+    item.anomalies.some((anomaly) => anomaly.startsWith('hsm_')),
+  ).length;
+  const mathAudit = {
+    routeSummaries: summarizeSweepRecords(records),
+    hsmBaselineMethod: 'installed_hsmadvisor_machine_database_plus_rpm_feed_mrr_power_envelope_identities',
+    directHsmAdvisorCalculator: 'blocked_by_hsmadvisor_component_license_outside_hsmadvisor_process',
+  };
+
   const report = {
     generatedAt: new Date().toISOString(),
-    endpointBase: 'http://127.0.0.1:3001/api/v1/speed-feed',
+    endpointBase: ENDPOINT_BASE,
+    modeFilter: MODE_FILTER,
+    totalAvailableScenarios: allScenarios.length,
+    scenarioStart: SCENARIO_START,
+    scenarioLimit: SCENARIO_LIMIT || null,
     scenarioCount: records.length,
     quickCalls,
     orchestrateCalls,
     optimizeCalls,
     totalRouteCalls: quickCalls + orchestrateCalls + optimizeCalls,
     anomalyCount: anomalies.length,
+    lowConfidenceCount,
+    highDeflectionCount,
+    hsmBaseline: {
+      enabled: HSM_BASELINE_ENABLED,
+      machineDatabasePath: hsmAdvisorMachineDbPath(),
+      machineCount: HSM_ADVISOR_MACHINES.length,
+      checkCount: hsmBaselineCheckCount,
+      violationCount: hsmBaselineViolationCount,
+      directCalculator: 'blocked_by_hsmadvisor_component_license_outside_hsmadvisor_process',
+    },
+    mathAudit,
     anomalies,
+    records: INCLUDE_RECORDS ? records : undefined,
   };
 
-  const outputDir = path.resolve('H:/PRISM/output');
-  mkdirSync(outputDir, { recursive: true });
-  const outputPath = path.join(outputDir, 'calculator-live-variability-sweep-2026-04-08.json');
+  mkdirSync(OUTPUT_DIR, { recursive: true });
+  const outputPath = path.join(
+    OUTPUT_DIR,
+    `${OUTPUT_BASENAME}-${MODE_FILTER}-${SCENARIO_START}-${records.length}.json`,
+  );
   writeFileSync(outputPath, `${JSON.stringify(report, null, 2)}\n`, 'utf8');
 
   console.log(JSON.stringify({ outputPath, ...report }, null, 2));
diff --git a/mcp-server/web/src/__tests__/AuditManagerPage.test.tsx b/mcp-server/web/src/__tests__/AuditManagerPage.test.tsx
index 8aad967c7..e8fe0e77e 100644
Binary files a/mcp-server/web/src/__tests__/AuditManagerPage.test.tsx and b/mcp-server/web/src/__tests__/AuditManagerPage.test.tsx differ
diff --git a/mcp-server/web/src/__tests__/LatheResultsPage.test.tsx b/mcp-server/web/src/__tests__/LatheResultsPage.test.tsx
index 8cd5f5847..6ffa1b3ba 100644
Binary files a/mcp-server/web/src/__tests__/LatheResultsPage.test.tsx and b/mcp-server/web/src/__tests__/LatheResultsPage.test.tsx differ
diff --git a/mcp-server/web/src/__tests__/LatheUploadPage.test.tsx b/mcp-server/web/src/__tests__/LatheUploadPage.test.tsx
index 6a0d8f904..7c60f0a6d 100644
Binary files a/mcp-server/web/src/__tests__/LatheUploadPage.test.tsx and b/mcp-server/web/src/__tests__/LatheUploadPage.test.tsx differ
diff --git a/mcp-server/web/src/__tests__/ShopFloorLivePage.test.tsx b/mcp-server/web/src/__tests__/ShopFloorLivePage.test.tsx
index c7f02046f..0c5cac937 100644
Binary files a/mcp-server/web/src/__tests__/ShopFloorLivePage.test.tsx and b/mcp-server/web/src/__tests__/ShopFloorLivePage.test.tsx differ
diff --git a/mcp-server/web/src/__tests__/WireEdmWizardPage.test.tsx b/mcp-server/web/src/__tests__/WireEdmWizardPage.test.tsx
index bce719fcc..921a294ca 100644
Binary files a/mcp-server/web/src/__tests__/WireEdmWizardPage.test.tsx and b/mcp-server/web/src/__tests__/WireEdmWizardPage.test.tsx differ
diff --git a/mcp-server/web/src/api/scheduling.ts b/mcp-server/web/src/api/scheduling.ts
index 074a9707e..cd48c8eaf 100644
Binary files a/mcp-server/web/src/api/scheduling.ts and b/mcp-server/web/src/api/scheduling.ts differ
diff --git a/mcp-server/web/src/api/scientificMath.ts b/mcp-server/web/src/api/scientificMath.ts
index b47d17f37..bb1b9dce7 100644
Binary files a/mcp-server/web/src/api/scientificMath.ts and b/mcp-server/web/src/api/scientificMath.ts differ
diff --git a/mcp-server/web/src/data/calculatorWorkspace.ts b/mcp-server/web/src/data/calculatorWorkspace.ts
index 98314af92..0d543cd9e 100644
--- a/mcp-server/web/src/data/calculatorWorkspace.ts
+++ b/mcp-server/web/src/data/calculatorWorkspace.ts
@@ -2035,19 +2035,23 @@ export function toolSupportsToolpath(
 ) {
   const supportedOperations = tool.supportedOperations ?? [tool.operation];
   const matchesOperation = supportedOperations.includes(toolpath.operationId);
+  if (!matchesOperation) {
+    return false;
+  }
+
   const keywords = tool.toolpathKeywords?.map((keyword) => keyword.toLowerCase()) ?? [];
   if (keywords.length === 0) {
-    return matchesOperation;
+    return true;
   }
 
   const signature = `${toolpath.label} ${toolpath.path}`.toLowerCase();
   const matchesKeyword = keywords.some((keyword) => signature.includes(keyword));
 
   if (toolpath.operationId === 'finishing' || toolpath.operationId === 'turning_finish') {
-    return matchesOperation && matchesKeyword;
+    return matchesKeyword;
   }
 
-  return matchesOperation || matchesKeyword;
+  return true;
 }
 
 export function coolantOptionsForMode(mode: MachineMode): SelectionOption[] {
diff --git a/mcp-server/web/src/features/machine-workspace/programmingAuthorityContract.ts b/mcp-server/web/src/features/machine-workspace/programmingAuthorityContract.ts
index 4e3d385c8..532eaa8bf 100644
--- a/mcp-server/web/src/features/machine-workspace/programmingAuthorityContract.ts
+++ b/mcp-server/web/src/features/machine-workspace/programmingAuthorityContract.ts
@@ -562,7 +562,7 @@ export function getToolpathDefaults(
       return { docMm: 0.3, wocMm: 0.20, isAbsolute: false, entryStyle: 'balanced', finishTarget: 'tight-finish' };
     }
     if (toolpath.operationId === 'face_milling') {
-      return { docMm: 0.3, wocMm: 0.80, isAbsolute: false, entryStyle: 'balanced', finishTarget: 'general' };
+      return { docMm: 0.02, wocMm: 0.80, isAbsolute: false, entryStyle: 'balanced', finishTarget: 'general' };
     }
     if (toolpath.operationId === 'drilling') {
       return { docMm: 1.0, wocMm: 1.0, isAbsolute: false, entryStyle: 'balanced', finishTarget: 'general' };
diff --git a/mcp-server/web/src/pages/CalculatorPage.tsx b/mcp-server/web/src/pages/CalculatorPage.tsx
index 2c33850f6..1299a2987 100644
--- a/mcp-server/web/src/pages/CalculatorPage.tsx
+++ b/mcp-server/web/src/pages/CalculatorPage.tsx
@@ -1729,6 +1729,15 @@ function toolingInterfaceAliases(value?: string) {
   switch (normalized) {
     case 'cat40-big-plus':
       return ['cat40-big-plus', 'cat40'];
+    case 'cat50':
+    case 'cat-50':
+      return ['cat50', 'cat-50'];
+    case 'bt30':
+    case 'bt-30':
+      return ['bt30', 'bt-30'];
+    case 'bt40':
+    case 'bt-40':
+      return ['bt40', 'bt-40'];
     case 'hsk-a63':
       return ['hsk-a63', 'hsk_a63'];
     case 'bmt45':
@@ -1767,6 +1776,12 @@ function toolingInterfaceLabel(value?: string) {
       return 'CAT 40 Big+';
     case 'cat40':
       return 'CAT 40';
+    case 'cat50':
+      return 'CAT 50';
+    case 'bt30':
+      return 'BT30';
+    case 'bt40':
+      return 'BT40';
     case 'bt50':
       return 'BT50';
     case 'hsk-a63':
@@ -1880,6 +1895,9 @@ function normalizeToolingInterfaceId(value: string, fallback: string): string {
   if (!normalized) return fallback;
   if (/(cat\s*40).*?(big\+|big plus)|(big\+|big plus).*?(cat\s*40)/i.test(normalized)) return 'cat40-big-plus';
   if (/cat\s*40/i.test(normalized)) return 'cat40';
+  if (/cat\s*50/i.test(normalized)) return 'cat50';
+  if (/bt\s*30/i.test(normalized)) return 'bt30';
+  if (/bt\s*40/i.test(normalized)) return 'bt40';
   if (/bt\s*50/i.test(normalized)) return 'bt50';
   if (/hsk[\s-]*a\s*63/i.test(normalized)) return 'hsk-a63';
   if (/capto\s*c\s*6|coromant\s*capto\s*c\s*6/i.test(normalized)) return 'capto-c6';
@@ -1990,7 +2008,13 @@ function defaultHolderPackageForMachine(
   }
 
   if (machineMode === 'mill') {
+    const generatedMillFallbackId = buildGeneratedMillHolderFallbacks(machine, tool)[0]?.id ?? '';
     switch (profile.spindleConnectionTypeId) {
+      case 'cat40':
+        if (toolId === 'face-mill') return 'sandvik-shell-arbor';
+        if (toolId === 'finisher') return 'haimer-shrink-mill';
+        if (toolId === 'adaptive-endmill') return 'haimer-hydraulic-rough';
+        return 'regofix-er-collet';
       case 'bt50':
         return toolId === 'face-mill' ? 'sandvik-bt50-shell-arbor' : 'regofix-er-collet';
       case 'hsk-a63':
@@ -2000,6 +2024,7 @@ function defaultHolderPackageForMachine(
         if (toolId === 'adaptive-endmill') return 'haimer-hydraulic-rough';
         return 'haimer-bigplus-finishing';
       default:
+        if (generatedMillFallbackId) return generatedMillFallbackId;
         if (toolId === 'face-mill') return 'sandvik-shell-arbor';
         if (toolId === 'finisher') return 'haimer-shrink-mill';
         if (toolId === 'adaptive-endmill') return 'haimer-hydraulic-rough';
@@ -2035,6 +2060,102 @@ function defaultHolderPackageForMachine(
   return '';
 }
 
+function isMillFaceFallbackCandidate(tool?: ToolCatalogItem | null) {
+  const geometry = String(tool?.geometryClass ?? '').toLowerCase();
+  const signature = `${tool?.id ?? ''} ${tool?.label ?? ''}`.toLowerCase();
+  return geometry === 'face-mill' || signature.includes('face');
+}
+
+function isMillDrillFallbackCandidate(tool?: ToolCatalogItem | null) {
+  const geometry = String(tool?.geometryClass ?? '').toLowerCase();
+  const signature = `${tool?.id ?? ''} ${tool?.label ?? ''} ${tool?.operation ?? ''}`.toLowerCase();
+  return geometry === 'drill' || /drill|tap|threadmill|thread mill/.test(signature);
+}
+
+function isMillBoringFallbackCandidate(tool?: ToolCatalogItem | null) {
+  const geometry = String(tool?.geometryClass ?? '').toLowerCase();
+  const signature = `${tool?.id ?? ''} ${tool?.label ?? ''} ${tool?.family ?? ''}`.toLowerCase();
+  return geometry === 'boring' || /boring/.test(signature);
+}
+
+function buildGeneratedMillHolderFallbacks(
+  machine?: MachineCatalogItem | null,
+  tool?: ToolCatalogItem | null,
+): HolderPackageOption[] {
+  const profile = deriveMachineToolingCapability(machine);
+  if (!profile || profile.kind !== 'magazine') return [];
+
+  const spindleConnectionTypeId = normalizeToolingInterfaceId(
+    profile.spindleConnectionTypeId ?? profile.interfaceLabel ?? '',
+    '',
+  );
+  if (!spindleConnectionTypeId) return [];
+
+  const interfaceLabel =
+    profile.spindleConnectionLabel
+    ?? profile.interfaceLabel
+    ?? toolingInterfaceLabel(spindleConnectionTypeId);
+  const basePackage = {
+    mode: 'mill' as const,
+    brandId: 'generic',
+    brandLabel: 'Generic',
+    compatibleLayoutKinds: ['magazine'] as const,
+    compatibleSpindleConnectionTypeIds: [spindleConnectionTypeId],
+    source: 'fallback' as const,
+  };
+
+  if (isMillFaceFallbackCandidate(tool)) {
+    return [{
+      ...basePackage,
+      id: `generic-${spindleConnectionTypeId}-face-arbor`,
+      label: `${interfaceLabel} shell mill arbor`,
+      detail: `Fallback face-mill arbor package for ${interfaceLabel} magazines when the live holder registry omits the interface family.`,
+      holderStyleId: 'machine-standard',
+      holderStyleIds: ['machine-standard'],
+      holderType: 'Shell mill arbor',
+      toolId: 'face-mill',
+      spindleInterface: interfaceLabel,
+    }];
+  }
+
+  if (isMillDrillFallbackCandidate(tool)) {
+    return [{
+      ...basePackage,
+      id: `generic-${spindleConnectionTypeId}-drill-chuck`,
+      label: `${interfaceLabel} hydraulic drill chuck`,
+      detail: `Fallback drill and tap holder package for ${interfaceLabel} spindle interfaces when the live holder registry omits the interface family.`,
+      holderStyleId: 'hydraulic',
+      holderStyleIds: ['machine-standard', 'hydraulic'],
+      holderType: 'Hydraulic drill chuck',
+      spindleInterface: interfaceLabel,
+    }];
+  }
+
+  if (isMillBoringFallbackCandidate(tool)) {
+    return [{
+      ...basePackage,
+      id: `generic-${spindleConnectionTypeId}-boring`,
+      label: `${interfaceLabel} boring head adapter`,
+      detail: `Fallback boring package for ${interfaceLabel} spindle interfaces when the live holder registry omits the interface family.`,
+      holderStyleId: 'machine-standard',
+      holderStyleIds: ['machine-standard'],
+      holderType: 'Boring head adapter',
+      spindleInterface: interfaceLabel,
+    }];
+  }
+
+  return [{
+    ...basePackage,
+    id: `generic-${spindleConnectionTypeId}-endmill`,
+    label: `${interfaceLabel} hydraulic endmill chuck`,
+    detail: `Fallback endmill holder package for ${interfaceLabel} spindle interfaces when the live holder registry omits the interface family.`,
+    holderStyleId: 'hydraulic',
+    holderStyleIds: ['machine-standard', 'hydraulic'],
+    holderType: 'Hydraulic chuck',
+    spindleInterface: interfaceLabel,
+  }];
+}
+
 function toolingCapabilityBadges(machine?: MachineCatalogItem | null) {
   const profile = deriveMachineToolingCapability(machine);
   if (!profile) return [];
@@ -3141,18 +3262,33 @@ export function CalculatorPage() {
       }
       return true;
     });
+  const holderFallbackTool =
+    baseToolsForMode.find((item) => item.id === toolId)
+    ?? liveTools.find((item) => item.id === toolId && item.mode === machineMode)
+    ?? baseToolsForMode[0]
+    ?? liveTools.find((item) => item.mode === machineMode)
+    ?? liveTools[0];
 
   const holderCompatibilityMachine = effectiveSelectedMachine ?? selectedMachine;
   const fallbackCompatibleHolderPackages = HOLDER_PACKAGE_LIBRARY.filter(
     (item) => item.mode === machineMode && holderPackageMatchesMachine(item, holderCompatibilityMachine),
   );
+  const generatedFallbackHolderPackages = machineMode === 'mill' && fallbackCompatibleHolderPackages.length === 0
+    ? buildGeneratedMillHolderFallbacks(holderCompatibilityMachine, holderFallbackTool)
+    : [];
   const catalogCompatibleHolderPackages = liveHolderPackages.filter(
     (item) => item.mode === machineMode && holderPackageMatchesMachine(item, holderCompatibilityMachine),
   );
+  const knownCompatibleHolderPackages = [
+    ...fallbackCompatibleHolderPackages,
+    ...generatedFallbackHolderPackages.filter(
+      (item) => !fallbackCompatibleHolderPackages.some((existing) => existing.id === item.id),
+    ),
+  ];
   const compatibleHolderPackagesBase = catalogCompatibleHolderPackages.length
     ? catalogCompatibleHolderPackages
-    : fallbackCompatibleHolderPackages;
-  const legacySelectedHolderPackage = HOLDER_PACKAGE_LIBRARY.find(
+    : knownCompatibleHolderPackages;
+  const legacySelectedHolderPackage = [...HOLDER_PACKAGE_LIBRARY, ...generatedFallbackHolderPackages].find(
     (item) => item.id === holderPackageId && item.mode === machineMode && holderPackageMatchesMachine(item, holderCompatibilityMachine),
   );
   const compatibleHolderPackages =
@@ -3358,6 +3494,13 @@ export function CalculatorPage() {
           : compatibleToolsForMode.length
             ? compatibleToolsForMode
             : baseToolsForMode;
+  const selectedTool =
+    toolsForMode.find((item) => item.id === toolId)
+    ?? compatibleToolsForMode.find((item) => item.id === toolId)
+    ?? toolsForMode[0]
+    ?? compatibleToolsForMode[0]
+    ?? baseToolsForMode[0]
+    ?? liveTools[0];
   const liveToolsForMode = toolsForMode.filter((item) => item.source === 'database');
   const toolCatalogBadge = toolCatalogStatus.source === 'live'
     ? (toolCatalogStatus.sampled ? 'Sampled live tool catalog' : 'Live tool database')
@@ -3440,13 +3583,6 @@ export function CalculatorPage() {
     : effectiveCatalogSource(materialCatalogStatus) === 'hybrid'
       ? calculatorCopy(interfaceLanguage, 'material.sourceHybrid', 'sampled live + curated')
       : calculatorCopy(interfaceLanguage, 'material.sourceFallback', 'PRISM baseline while the live registry recovers');
-  const selectedTool =
-    toolsForMode.find((item) => item.id === toolId)
-    ?? compatibleToolsForMode.find((item) => item.id === toolId)
-    ?? toolsForMode[0]
-    ?? compatibleToolsForMode[0]
-    ?? baseToolsForMode[0]
-    ?? liveTools[0];
   const selectedToolBodyType = selectedTool ? inferToolBodyType(selectedTool) : 'solid';
   const selectedToolConstructionLabel = selectedToolBodyType === 'indexable' ? 'Indexable body' : 'Solid tool body';
   const toolSelectionLabel = selectedToolBodyType === 'indexable' || toolBodyFilter === 'indexable' ? 'Tool body' : 'Tool family';
@@ -4168,19 +4304,31 @@ export function CalculatorPage() {
     selectedTool?.geometryClass,
   ]);
 
+  const effectiveSelectedToolpath = useMemo(
+    () => (
+      selectedToolpath
+      ?? filteredToolpathOptions.find((item) => item.id === toolpathId)
+      ?? licensedToolpathOptions.find((item) => item.id === toolpathId)
+      ?? toolpathOptions.find((item) => item.id === toolpathId)
+      ?? null
+    ),
+    [filteredToolpathOptions, licensedToolpathOptions, selectedToolpath, toolpathId, toolpathOptions],
+  );
+
   useEffect(() => {
     if (applyingSnapshotRef.current) return;
-    const defaults = getToolpathDefaults(selectedToolpath, machineMode);
+    const defaults = getToolpathDefaults(effectiveSelectedToolpath ?? undefined, machineMode);
     if (!defaults) return;
     if (machineMode === 'mill' || machineMode === 'lathe') {
-      if (defaults.isAbsolute) {
-        setDoc(defaults.docMm);
-        setWoc(defaults.wocMm);
-      } else {
-        const dia = toolDiameter || 12;
-        setDoc(Math.round(dia * defaults.docMm * 100) / 100);
-        setWoc(Math.round(dia * defaults.wocMm * 100) / 100);
-      }
+      const nextDoc = defaults.isAbsolute
+        ? defaults.docMm
+        : Math.round((toolDiameter || 12) * defaults.docMm * 100) / 100;
+      const nextWoc = defaults.isAbsolute
+        ? defaults.wocMm
+        : Math.round((toolDiameter || 12) * defaults.wocMm * 100) / 100;
+
+      setDoc((current) => (Math.abs(current - nextDoc) < 0.0001 ? current : nextDoc));
+      setWoc((current) => (Math.abs(current - nextWoc) < 0.0001 ? current : nextWoc));
     }
     const entryOpts = ENTRY_STYLE_OPTIONS[machineMode];
     if (entryOpts?.some((item) => item.id === defaults.entryStyle)) {
@@ -4190,7 +4338,7 @@ export function CalculatorPage() {
     if (finishControlModeRef.current !== 'manual') {
       setDesiredRaUm(desiredRaForFinishTarget(defaults.finishTarget));
     }
-  }, [selectedToolpath?.id, machineMode]);
+  }, [effectiveSelectedToolpath?.id, machineMode, selectedTool?.id, toolDiameter]);
 
   useEffect(() => {
     const defaults = defaultSelectedFeatureIds(machineMode, selectedMachine);
@@ -4270,7 +4418,7 @@ export function CalculatorPage() {
     }
     if (machineMode === 'lathe') {
     const nextHolderPackageId = defaultHolderPackageForMachine(machineMode, holderCompatibilityMachine, selectedTool);
-      const nextHolderPackage = HOLDER_PACKAGE_LIBRARY.find((item) => item.id === nextHolderPackageId);
+      const nextHolderPackage = [...HOLDER_PACKAGE_LIBRARY, ...generatedFallbackHolderPackages].find((item) => item.id === nextHolderPackageId);
       setCoolant(holderCompatibilityMachine?.toolingLayout?.liveTooling ? 'tsc' : 'flood');
       setWorkholding('collet-chuck');
       setWorkholdingCategory('all');
@@ -4293,7 +4441,7 @@ export function CalculatorPage() {
       setWoc(0.6);
     } else if (machineMode === 'mill') {
       const nextHolderPackageId = defaultHolderPackageForMachine(machineMode, holderCompatibilityMachine, selectedTool);
-      const nextHolderPackage = HOLDER_PACKAGE_LIBRARY.find((item) => item.id === nextHolderPackageId);
+      const nextHolderPackage = [...HOLDER_PACKAGE_LIBRARY, ...generatedFallbackHolderPackages].find((item) => item.id === nextHolderPackageId);
       const millSetupDefaults = defaultMillSetupForMachine(holderCompatibilityMachine);
       setCoolant('flood');
       setWorkholding(millSetupDefaults.workholdingId);
@@ -4312,11 +4460,9 @@ export function CalculatorPage() {
       setHolderPackageId(nextHolderPackageId || 'sandvik-shell-arbor');
       setHolderStyle(holderPackagePrimaryStyle(nextHolderPackage));
       setStabilityId(millSetupDefaults.stabilityId);
-      setDoc(2.5);
-      setWoc(38);
     } else if (machineMode === 'wire_edm') {
       const nextHolderPackageId = defaultHolderPackageForMachine(machineMode, holderCompatibilityMachine, selectedTool);
-      const nextHolderPackage = HOLDER_PACKAGE_LIBRARY.find((item) => item.id === nextHolderPackageId);
+      const nextHolderPackage = [...HOLDER_PACKAGE_LIBRARY, ...generatedFallbackHolderPackages].find((item) => item.id === nextHolderPackageId);
       setCoolant('dielectric');
       setWorkholding('wire-fixture');
       setWorkholdingCategory('all');
@@ -4338,7 +4484,7 @@ export function CalculatorPage() {
       setWoc(0.25);
     } else if (machineMode === 'edm') {
       const nextHolderPackageId = defaultHolderPackageForMachine(machineMode, holderCompatibilityMachine, selectedTool);
-      const nextHolderPackage = HOLDER_PACKAGE_LIBRARY.find((item) => item.id === nextHolderPackageId);
+      const nextHolderPackage = [...HOLDER_PACKAGE_LIBRARY, ...generatedFallbackHolderPackages].find((item) => item.id === nextHolderPackageId);
       setCoolant('dielectric');
       setWorkholding('wire-fixture');
       setWorkholdingCategory('all');
@@ -4394,7 +4540,7 @@ export function CalculatorPage() {
 
     const nextHolderPackage =
       compatibleHolderPackages.find((item) => item.id === nextHolderPackageId)
-      ?? HOLDER_PACKAGE_LIBRARY.find((item) => item.id === nextHolderPackageId);
+      ?? [...HOLDER_PACKAGE_LIBRARY, ...generatedFallbackHolderPackages].find((item) => item.id === nextHolderPackageId);
 
     if (!nextHolderPackage) {
       return;
diff --git a/mcp-server/web/src/pages/MaintenanceWorkOrderPage.tsx b/mcp-server/web/src/pages/MaintenanceWorkOrderPage.tsx
index 0417ba3ae..8a4162d93 100644
--- a/mcp-server/web/src/pages/MaintenanceWorkOrderPage.tsx
+++ b/mcp-server/web/src/pages/MaintenanceWorkOrderPage.tsx
@@ -292,4 +292,4 @@ function dot3(a, b) {
     return a.x * b.x + a.y * b.y + a.z * b.z;
 }
 export const nurbsEngine = new NURBSEngineImpl();
-//# sourceMappingURL=NURBSEngine.js.map                                                                                                                                                                                                                                                                                                                  AAC,GAAG,KAAK,GAAG,KAAK,CAAC,CAAC,CAAC,CAAC,CAAC,GAAG,OAAO,GAAG,CAAC,GAAG,CAAC,GAAG,CAAC,GAAG,CAAC,GAAG,CAAC,CAAC,GAAG,CAAC,CAAC,GAAG,KAAK,CAAC,CAAC,CAAC,CAAC,CAAC,CAAC;QAE9E,MAAM,IAAI,GAAG,IAAI,CAAC,IAAI,CAAC,IAAI,CAAC,GAAG,CAAC,CAAC,GAAG,CAAC,GAAG,CAAC,EAAE,CAAC,CAAC,CAAC,CAAC;QAC/C,OAAO,EAAE,EAAE,EAAE,CAAC,GAAG,IAAI,EAAE,EAAE,EAAE,CAAC,GAAG,IAAI,EAAE,QAAQ,EAAE,CAAC,EAAE,IAAI,EAAE,CAAC,EAAE,CAAC;IAC9D,CAAC;IAED,6CAA6C;IAC7C,iBAAiB,CAAC,OAAqB,EAAE,KAAK,GAAG,EAAE,EAAE,KAAK,GAAG,EAAE;QAC7D,MAAM,QAAQ,GAAc,EAAE,CAAC;QAC/B,MAAM,OAAO,GAAc,EAAE,CAAC;QAC9B,MAAM,GAAG,GAAoC,EAAE,CAAC;QAChD,MAAM,OAAO,GAAa,EAAE,CAAC;QAE7B,KAAK,IAAI,CAAC,GAAG,CAAC,EAAE,CAAC,IAAI,KAAK,EAAE,CAAC,EAAE,EAAE,CAAC;YAChC,MAAM,CAAC,GAAG,CAAC,GAAG,KAAK,CAAC;YACpB,KAAK,IAAI,CAAC,GAAG,CAAC,EAAE,CAAC,IAAI,KAAK,EAAE,CAAC,EAAE,EAAE,CAAC;gBAChC,MAAM,CAAC,GAAG,CAAC,GAAG,KAAK,CAAC;gBACpB,QAAQ,CAAC,IAAI,CAAC,IAAI,CAAC,eAAe,CAAC,OAAO,EAAE,CAAC,EAAE,CAAC,CAAC,CAAC,CAAC;gBACnD,OAAO,CAAC,IAAI,CAAC,IAAI,CAAC,aAAa,CAAC,OAAO,EAAE,CAAC,EAAE,CAAC,CAAC,CAAC,CAAC;gBAChD,GAAG,CAAC,IAAI,CAAC,EAAE,CAAC,EAAE,CAAC,EAAE,CAAC,CAAC;YACrB,CAAC;QACH,CAAC;QACD,KAAK,IAAI,CAAC,GAAG,CAAC,EAAE,CAAC,GAAG,KAAK,EAAE,CAAC,EAAE,EAAE,CAAC;YAC/B,KAAK,IAAI,CAAC,GAAG,CAAC,EAAE,CAAC,GAAG,KAAK,EAAE,CAAC,EAAE,EAAE,CAAC;gBAC/B,MAAM,GAAG,GAAG,CAAC,GAAG,CAAC,KAAK,GAAG,CAAC,CAAC,GAAG,CAAC,CAAC;gBAChC,OAAO,CAAC,IAAI,CAAC,GAAG,EAAE,GAAG,GAAG,CAAC,EAAE,GAAG,GAAG,KAAK,GAAG,CAAC,CAAC,CAAC;gBAC5C,OAAO,CAAC,IAAI,CAAC,GAAG,GAAG,CAAC,EAAE,GAAG,GAAG,KAAK,GAAG,CAAC,EAAE,GAAG,GAAG,KAAK,GAAG,CAAC,CAAC,CAAC;YAC1D,CAAC;QACH,CAAC;QACD,OAAO,EAAE,QAAQ,EAAE,OAAO,EAAE,GAAG,EAAE,OAAO,EAAE,CAAC;IAC7C,CAAC;IAED,yEAAyE;IACzE,mBAAmB,CAAC,OAAqB,EAAE,KAAc,EAAE,SAAS,GAAG,MAAM,EAAE,OAAO,GAAG,GAAG;QAC1F,IAAI,CAAC,GAAG,GAAG,EAAE,CAAC,GAAG,GAAG,CAAC;QACrB,KAAK,IAAI,IAAI,GAAG,CAAC,EAAE,IAAI,GAAG,OAAO,EAAE,IAAI,EAAE,EAAE,CAAC;YAC1C,MAAM,CAAC,GAAG,IAAI,CAAC,eAAe,CAAC,OAAO,EAAE,CAAC,EAAE,CAAC,CAAC,CAAC;YAC9C,MAAM,GA
\ No newline at end of file
+//# sourceMappingURL=NURBSEngine.js.mapAAC,GAAG,KAAK,GAAG,KAAK,CAAC,CAAC,CAAC,CAAC,CAAC,GAAG,OAAO,GAAG,CAAC,GAAG,CAAC,GAAG,CAAC,GAAG,CAAC,GAAG,CAAC,CAAC,GAAG,CAAC,CAAC,GAAG,KAAK,CAAC,CAAC,CAAC,CAAC,CAAC,CAAC;QAE9E,MAAM,IAAI,GAAG,IAAI,CAAC,IAAI,CAAC,IAAI,CAAC,GAAG,CAAC,CAAC,GAAG,CAAC,GAAG,CAAC,EAAE,CAAC,CAAC,CAAC,CAAC;QAC/C,OAAO,EAAE,EAAE,EAAE,CAAC,GAAG,IAAI,EAAE,EAAE,EAAE,CAAC,GAAG,IAAI,EAAE,QAAQ,EAAE,CAAC,EAAE,IAAI,EAAE,CAAC,EAAE,CAAC;IAC9D,CAAC;IAED,6CAA6C;IAC7C,iBAAiB,CAAC,OAAqB,EAAE,KAAK,GAAG,EAAE,EAAE,KAAK,GAAG,EAAE;QAC7D,MAAM,QAAQ,GAAc,EAAE,CAAC;QAC/B,MAAM,OAAO,GAAc,EAAE,CAAC;QAC9B,MAAM,GAAG,GAAoC,EAAE,CAAC;QAChD,MAAM,OAAO,GAAa,EAAE,CAAC;QAE7B,KAAK,IAAI,CAAC,GAAG,CAAC,EAAE,CAAC,IAAI,KAAK,EAAE,CAAC,EAAE,EAAE,CAAC;YAChC,MAAM,CAAC,GAAG,CAAC,GAAG,KAAK,CAAC;YACpB,KAAK,IAAI,CAAC,GAAG,CAAC,EAAE,CAAC,IAAI,KAAK,EAAE,CAAC,EAAE,EAAE,CAAC;gBAChC,MAAM,CAAC,GAAG,CAAC,GAAG,KAAK,CAAC;gBACpB,QAAQ,CAAC,IAAI,CAAC,IAAI,CAAC,eAAe,CAAC,OAAO,EAAE,CAAC,EAAE,CAAC,CAAC,CAAC,CAAC;gBACnD,OAAO,CAAC,IAAI,CAAC,IAAI,CAAC,aAAa,CAAC,OAAO,EAAE,CAAC,EAAE,CAAC,CAAC,CAAC,CAAC;gBAChD,GAAG,CAAC,IAAI,CAAC,EAAE,CAAC,EAAE,CAAC,EAAE,CAAC,CAAC;YACrB,CAAC;QACH,CAAC;QACD,KAAK,IAAI,CAAC,GAAG,CAAC,EAAE,CAAC,GAAG,KAAK,EAAE,CAAC,EAAE,EAAE,CAAC;YAC/B,KAAK,IAAI,CAAC,GAAG,CAAC,EAAE,CAAC,GAAG,KAAK,EAAE,CAAC,EAAE,EAAE,CAAC;gBAC/B,MAAM,GAAG,GAAG,CAAC,GAAG,CAAC,KAAK,GAAG,CAAC,CAAC,GAAG,CAAC,CAAC;gBAChC,OAAO,CAAC,IAAI,CAAC,GAAG,EAAE,GAAG,GAAG,CAAC,EAAE,GAAG,GAAG,KAAK,GAAG,CAAC,CAAC,CAAC;gBAC5C,OAAO,CAAC,IAAI,CAAC,GAAG,GAAG,CAAC,EAAE,GAAG,GAAG,KAAK,GAAG,CAAC,EAAE,GAAG,GAAG,KAAK,GAAG,CAAC,CAAC,CAAC;YAC1D,CAAC;QACH,CAAC;QACD,OAAO,EAAE,QAAQ,EAAE,OAAO,EAAE,GAAG,EAAE,OAAO,EAAE,CAAC;IAC7C,CAAC;IAED,yEAAyE;IACzE,mBAAmB,CAAC,OAAqB,EAAE,KAAc,EAAE,SAAS,GAAG,MAAM,EAAE,OAAO,GAAG,GAAG;QAC1F,IAAI,CAAC,GAAG,GAAG,EAAE,CAAC,GAAG,GAAG,CAAC;QACrB,KAAK,IAAI,IAAI,GAAG,CAAC,EAAE,IAAI,GAAG,OAAO,EAAE,IAAI,EAAE,EAAE,CAAC;YAC1C,MAAM,CAAC,GAAG,IAAI,CAAC,eAAe,CAAC,OAAO,EAAE,CAAC,EAAE,CAAC,CAAC,CAAC;YAC9C,MAAM,GA
\ No newline at end of file
diff --git a/mcp-server/web/src/pages/ShippingPackingPage.tsx b/mcp-server/web/src/pages/ShippingPackingPage.tsx
index a4be3cdab..32c07cecc 100644
Binary files a/mcp-server/web/src/pages/ShippingPackingPage.tsx and b/mcp-server/web/src/pages/ShippingPackingPage.tsx differ
diff --git a/mcp-server/web/src/utils/calculatorSpeedFeedContract.ts b/mcp-server/web/src/utils/calculatorSpeedFeedContract.ts
index c5ec60f19..7de95bc8b 100644
--- a/mcp-server/web/src/utils/calculatorSpeedFeedContract.ts
+++ b/mcp-server/web/src/utils/calculatorSpeedFeedContract.ts
@@ -150,6 +150,12 @@ function hasFiniteNumber(value: unknown): value is number {
   return typeof value === 'number' && Number.isFinite(value);
 }
 
+function normalizeRadialDepthMm(toolDiameterMm: number, wocMm: number) {
+  if (!(toolDiameterMm > 0) || !(wocMm > 0)) return wocMm;
+  const ratio = wocMm / toolDiameterMm;
+  return ratio > 1 && ratio < 1.005 ? toolDiameterMm : wocMm;
+}
+
 const CRITICAL_SAFETY_PATTERN =
   /unsafe|critical|fail|failure|collision|crash|catastrophic|exceed|exceeds|overload|not supported|breakage|tool break|pull.?out|interference|limit exceeded|thermal shock/i;
 
@@ -778,9 +784,10 @@ export function buildCalculatorSpeedFeedParams(
   const cutType = inferCutType(input);
   const strategy = inferStrategy(input);
   const holderType = inferHolderType(input);
+  const normalizedWocMm = normalizeRadialDepthMm(input.toolDiameterMm, input.wocMm);
   const radialDepthPct =
-    input.toolDiameterMm > 0 && input.wocMm > 0
-      ? roundMetric((input.wocMm / input.toolDiameterMm) * 100, 3)
+    input.toolDiameterMm > 0 && normalizedWocMm > 0
+      ? roundMetric((normalizedWocMm / input.toolDiameterMm) * 100, 3)
       : undefined;
   const toolStickoutMm = input.toolStickoutMm ?? undefined;
   const fluteLengthMm = input.fluteLengthMm ?? undefined;
@@ -829,8 +836,8 @@ export function buildCalculatorSpeedFeedParams(
     toolpath_strategy: input.toolpath?.label || input.toolpath?.id || undefined,
     axial_depth_mm: roundMetric(input.docMm, 3),
     doc_mm: roundMetric(input.docMm, 3),
-    radial_depth_mm: roundMetric(input.wocMm, 3),
-    woc_mm: roundMetric(input.wocMm, 3),
+    radial_depth_mm: roundMetric(normalizedWocMm, 3),
+    woc_mm: roundMetric(normalizedWocMm, 3),
     radial_depth_pct: radialDepthPct,
     workholding_type: inferWorkholdingType(input),
     workholding_stiffness: inferWorkholdingStiffness(input.stabilityId),
diff --git a/mcp-server/web/test-results/e2e-report/index.html b/mcp-server/web/test-results/e2e-report/index.html
index 55de92859..659b46709 100644
--- a/mcp-server/web/test-results/e2e-report/index.html
+++ b/mcp-server/web/test-results/e2e-report/index.html
@@ -87,4 +87,4 @@ Error generating stack: `+l.message+`
     <div id='root'></div>
   </body>
 </html>
-<template id="playwrightReportBase64">data:application/zip;base64,</template>
\ No newline at end of file
+<template id="playwrightReportBase64">data:application/zip;base64,</template>
\ No newline at end of file
diff --git a/mcp-server/web/test-results/e2e/.last-run.json b/mcp-server/web/test-results/e2e/.last-run.json
index cbcc1fbac..660a74c02 100644
--- a/mcp-server/web/test-results/e2e/.last-run.json
+++ b/mcp-server/web/test-results/e2e/.last-run.json
@@ -1,4 +1,7 @@
 {
-  "status": "passed",
-  "failedTests": []
+  "status": "failed",
+  "failedTests": [
+    "4c731f9118a009bd5eca-932a518cedfb5d92ffe9",
+    "4c731f9118a009bd5eca-ead3c19904ab1f65e412"
+  ]
 }
\ No newline at end of file
diff --git a/state/SESSION_DIGEST.md b/state/SESSION_DIGEST.md
index f5a87328a..f5cee4c09 100644
Binary files a/state/SESSION_DIGEST.md and b/state/SESSION_DIGEST.md differ
diff --git a/state/SESSION_STATE.md b/state/SESSION_STATE.md
index 92b91e6ab..f8beb7ade 100644
Binary files a/state/SESSION_STATE.md and b/state/SESSION_STATE.md differ
diff --git a/state/pre_build_snapshot.json b/state/pre_build_snapshot.json
index f0ff0fe6d..60a6df94e 100644
Binary files a/state/pre_build_snapshot.json and b/state/pre_build_snapshot.json differ
diff --git a/state/shared/CLAUDE-CODEX-TASK-QUEUE-DIRECTIVE.md b/state/shared/CLAUDE-CODEX-TASK-QUEUE-DIRECTIVE.md
index 82cd9e1c7..c8af5155b 100644
Binary files a/state/shared/CLAUDE-CODEX-TASK-QUEUE-DIRECTIVE.md and b/state/shared/CLAUDE-CODEX-TASK-QUEUE-DIRECTIVE.md differ
diff --git a/state/shared/SYSTEM-CAPABILITIES.md b/state/shared/SYSTEM-CAPABILITIES.md
index d35d90924..696dcf9c3 100644
Binary files a/state/shared/SYSTEM-CAPABILITIES.md and b/state/shared/SYSTEM-CAPABILITIES.md differ
diff --git a/state/todo.md b/state/todo.md
index 89d2d4d8f..808977326 100644
Binary files a/state/todo.md and b/state/todo.md differ
