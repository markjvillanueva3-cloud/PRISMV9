L28: path.join(PATHS.STATE_DIR, "material-registry.json"),
L78: if (files.some(f => f.name.endsWith(".json") && f.name !== "index.json")) {
L101: const jsonFiles = files.filter(f => f.name.endsWith(".json") && f.name !== "index.json");
L108: const fileName = file.name.replace('.json', '');
L149: const jsonFiles = files.filter(f => f.name.endsWith(".json"));
L491: const filePath = path.join(layerPath, `${id}.json`);
