// Count materials per ISO group in the running registry
const groups = ['P', 'M', 'K', 'N', 'S', 'H', 'X'];
// We can't run this directly - need to check via MCP calls
console.log('Need to check via prism_data searches per group');
