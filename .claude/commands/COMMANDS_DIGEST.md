# .claude/commands/ — COMMANDS_DIGEST.md

_Auto-generated by `scripts/generate-commands-digest.mjs` (U-MWO16, slot:bravo 2026-05-26)._

**316 skills** indexed across 1 categories.  Invoke any via `/<name>` in chat or via Skill tool.

## (top-level) (316)

| Skill | Description |
|-------|-------------|
| `//approvals` | Query and manage approval workflows for PRISM entities (quotes, POs, invoices, payroll, NCRs, jobs, change orders, cred… |
| `//awareness-snapshot` | One-shot PRISM awareness snapshot — built/wired/utilized/drifted in 60 lines. Regenerates state/shared/AWARENESS-SNAPSH… |
| `//batch-optimize` | Run Kienzle/Taylor/Brammertz physics optimization on ALL programs in a Box folder. Generates a fleet-wide report showin… |
| `//brain-recall` | Query the unified Obsidian-vault + system-graph + wiki BM25 brain |
| `//build-brief` | Compose a deep pre-build knowledge brief for a unit-id, milestone, or topic. Reads FULL wiki bodies + memory bodies + t… |
| `//cad-corpus` | Manage the JM Die CAD training corpus for neural CAD generation. |
| `//cad-dfm` | Comprehensive DFM analysis to optimize designs for manufacturing efficiency and cost. |
| `//cad-dfm-generate` | Generate CAD models with DFM (Design for Manufacturability) filtering and auto-fix. |
| `//cad-explain` | Query chain-of-thought reasoning for CAD generation decisions. |
| `//cad-extract` | Extract machining features, dimensions, and tolerances from CAD models for CAM programming. |
| `//cad-feature-recognize` | Automatically recognize machining features from CAD geometry for CAM programming. |
| `//cad-from-photo` | Vision-to-CAD reverse engineering — picture/video of a physical part → starting-bad parametric CAD template → caliper-r… |
| `//cad-from-text` | Generate CAD models from natural language descriptions using TextToCADGenerationEngine. |
| `//cad-review` | Review CAD model for manufacturability, quality issues, and design best practices. |
| `//cad-tolerance` | Generate CAD models with automatic GD&T (Geometric Dimensioning and Tolerancing) application. |
| `//cad-tolerance-check` | Analyze and validate geometric tolerances and dimensions in CAD models per GD&T standards. |
| `//cad-validate` | Multi-layer validation ensuring CAD models are 100% accurate before CAM handoff. |
| `//cam-strategy` | Select optimal machining strategies based on geometry, material, and ADAPTIVE cutting conditions. |
| `//capacity-plan` | Plan shop capacity with variability-aware cycle time estimates. |
| `//capture-claude-credentials` | Snapshot ~/.claude/.credentials.json into a per-account vault (ZEBRA-ACCOUNT-CYCLE-MS0/U1). One-time per account; reusa… |
| `//checkin-alpha` | Force-claim slot ALPHA + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot alpha --forc… |
| `//checkin-bravo` | Force-claim slot BRAVO + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot bravo --forc… |
| `//checkin-charlie` | Force-claim slot CHARLIE + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot charlie --… |
| `//checkin-delta` | Force-claim slot DELTA + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot delta --forc… |
| `//checkin-echo` | Force-claim slot ECHO + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot echo --force`. |
| `//checkin-foxtrot` | Force-claim slot FOXTROT + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot foxtrot --… |
| `//checkin-golf` | Force-claim slot GOLF + run the full /checkin pipeline. GOLF OWNS THE FLEET-REAPER (doctrine moved from alpha 2026-05-1… |
| `//checkin-hotel` | Force-claim slot HOTEL + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot hotel --forc… |
| `//checkin-india` | Force-claim slot INDIA + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot india --forc… |
| `//checkin-juliett` | Force-claim slot JULIETT + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot juliett --… |
| `//checkin-kilo` | Force-claim slot KILO + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot kilo --force`… |
| `//checkin-lima` | Force-claim slot LIMA + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot lima --force`… |
| `//checkin-mike` | Force-claim slot MIKE + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot mike --force`… |
| `//checkin-november` | Force-claim slot NOVEMBER + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot november … |
| `//checkin-oscar` | Force-claim slot OSCAR + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot oscar --forc… |
| `//checkin-papa` | Force-claim slot PAPA + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot papa --force`. |
| `//checkin-quebec` | Force-claim slot QUEBEC + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot quebec --fo… |
| `//checkin-romeo` | Force-claim slot ROMEO + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot romeo --forc… |
| `//checkin-sierra` | Force-claim slot SIERRA + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot sierra --fo… |
| `//checkin-tango` | Force-claim slot TANGO + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot tango --forc… |
| `//checkin-uniform` | Force-claim slot UNIFORM + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot uniform --… |
| `//checkin-victor` | Force-claim slot VICTOR + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot victor --fo… |
| `//checkin-whiskey` | Force-claim slot WHISKEY + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot whiskey --… |
| `//checkin-xray` | Force-claim slot XRAY + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot xray --force`. |
| `//checkin-yankee` | Force-claim slot YANKEE + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot yankee --fo… |
| `//checkin-zebra` | Force-claim slot ZEBRA + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot zebra --forc… |
| `//checkin-zulu` | Force-claim slot ZULU + run the full /checkin pipeline. NATO-phonetic shortcut for `/checkin --preferSlot zulu --force`. |
| `//cmm-parse` | Parse and analyze CMM measurement reports for quality insights. |
| `//cpk-calc` | Calculate Cp, Cpk, Pp, Ppk with variability-aware confidence intervals. |
| `//deep-search` | Search-first then deep-reason fallback. Runs master_index_query first; if top hits all score below the confidence floor… |
| `//desk-search` | Query desk payloads, saved views, pinned entities, recents, and global cross-entity search. |
| `//dev-tool-leverage` | Run the dev-tool leverage aggregator — one command, 4 META audits in a unified ranked dashboard. Surfaces the highest-l… |
| `//diagnose-fix &lt;artifact&gt;` | Operator front-end for forensic-classify → suggest → apply → test → trace on a broken artifact (engine / test / hook / … |
| `//emerging-thesis` | TF-IDF synthesis over recent vault activity — surfaces the dominant concept emerging from knowledge/memories in the las… |
| `//erp-sync` | Synchronize PRISM data with ERP systems for bidirectional updates. |
| `//extract-dark-content` | Discover and extract knowledge from unindexed, undocumented, or hidden content sources. |
| `//extracted-query` | Query the 1788-module extraction stockpile by status / type / dispatcher / name. Returns top-K rows from the classified… |
| `//fix-hook-schemas` | Runs the two-stage hook audit-and-fix pipeline that keeps the `.claude/hooks/` and `.claude/helpers/` scripts emitting … |
| `//foresight` | Surface the foresight stack's go/caution/no_go verdict for a proposed |
| `//forge-supervised` | Supervised /forge-triple wrapper — runs /forge-triple (engine+skill+hook trio) then /scrutiny-gate end-of-task automati… |
| `//gdnt-check` | Validate geometric dimensioning and tolerancing compliance per ASME Y14.5/ISO 1101. |
| `//graphiti` | Query and append to the PRISM graphiti-lite episode store (PSN leg 12 candidate). Temporal validity + provenance traceb… |
| `//handoff-alpha` | Force-claim slot ALPHA + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-bravo` | Force-claim slot BRAVO + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-charlie` | Force-claim slot CHARLIE + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-delta` | Force-claim slot DELTA + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-echo` | Force-claim slot ECHO + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-foxtrot` | Force-claim slot FOXTROT + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-golf` | Force-claim slot GOLF + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-hotel` | Force-claim slot HOTEL + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-india` | Force-claim slot INDIA + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-juliett` | Force-claim slot JULIETT + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-kilo` | Force-claim slot KILO + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-lima` | Force-claim slot LIMA + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-mike` | Force-claim slot MIKE + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-november` | Force-claim slot NOVEMBER + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-oscar` | Force-claim slot OSCAR + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-papa` | Force-claim slot PAPA + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-quebec` | Force-claim slot QUEBEC + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-romeo` | Force-claim slot ROMEO + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-sierra` | Force-claim slot SIERRA + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-tango` | Force-claim slot TANGO + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-uniform` | Force-claim slot UNIFORM + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-victor` | Force-claim slot VICTOR + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-whiskey` | Force-claim slot WHISKEY + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-xray` | Force-claim slot XRAY + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-yankee` | Force-claim slot YANKEE + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-zebra` | Force-claim slot ZEBRA + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//handoff-zulu` | Force-claim slot ZULU + run the full /handoff pipeline. NATO-phonetic shortcut for slot-bound session handoff. |
| `//hybrid` | One query across all 4 PSN retrieval substrates (memory + master + episode + Qdrant), fused by RRF |
| `//ingest` | Scan JM Die source roots and ingest files into PRISM. Scans for new/changed CNC programs, prints, CAD files, spreadshee… |
| `//knowledge-query` | Unified knowledge query — ONE MCP call replaces N Grep/Glob/Agent searches. Hits prism_session:master_index_query (syst… |
| `//lathe-lora` | Process a LatheLoRA model response through the physics-augmentation pipeline. Validates LLM-suggested turning parameter… |
| `//lathe-studio` | Launch the Lathe Studio wizard — full pipeline from part geometry to optimized turning G-code with tribal knowledge inj… |
| `//learn` | Route learning requests to the appropriate specialized learning skill based on source type. |
| `//learn-batch` | Run the 4 long-term learning hooks (meta-learning, error-learner, error-recovery, efficiency-monitor) as a batch via ag… |
| `//learn-pipeline &lt;src&gt;` | Operator front-end for the canonical knowledge-ingest pipeline. detect <src> → extract → dedup → tribal → wiki → memory… |
| `//macro-convert` | Convert any hardcoded Okuma program to a parametric macro with V variables for stock size, drill sizes, boring bar size… |
| `//master-index` | Unified master search across system-viz graph + Obsidian vault + capability index + BUILD_STATE. Use INSTEAD OF Grep/Gl… |
| `//material-stock` | View and manage material inventory — stock levels, reorder alerts, ISO group lookup, and JM Die seeding. |
| `//mcp-broadcast` | Fire a fleet-wide MCP reconnect broadcast — surfaces a `/mcp` nudge in every chat's next prompt |
| `//mill-studio` | Launch the Mill Studio wizard — full pipeline from part geometry to optimized milling G-code with toolpath optimization… |
| `//my-presets` | Manage speed/feed presets, toolpath presets, fixture setups, and course-based learning progression. |
| `//my-shop` | View My Shop dashboard — data completeness, domain counts, and recommended actions to populate your shop profile. |
| `//observability` | Query PRISM's observability state — unified view over scrutiny ledger + error-pattern + token-economy + route-savings +… |
| `//order-status` | Track customer orders through 14 milestones, manage portal access tokens, quality documents, and customer messaging. |
| `//orphan-inventory` | Generate the built-but-unwired audit punch list. Reads system-graph orphans (low in/out-degree but documented) + groups… |
| `//p2p-intake-check` | Fast pre-flight check of a blueprint / drawing input for the print-to-program pipeline. Surfaces missing fields, ambigu… |
| `//pick-dev` | Deterministic next-unit picker LOCKED to the backend-devtools roadmap (roadmap_priority=0). Same picker engine as /pick… |
| `//pick-unit` | Deterministic next-unit picker (devtools-first, revenue-second) via the psk kernel. Subtracts shipped units, returns to… |
| `//pipeline &lt;subcommand&gt;` | Operator meta-command over the COMMAND-KERNEL-MS0 pipeline registry. `list` shows registered pipelines, `dry-run <slug>… |
| `//post-diff` | Compare NC code from different posts or versions for validation. |
| `//post-generate` | Generate production-ready NC code with adaptive parameter injection. |
| `//post-harden` | Customize and harden post-processor for specific machine characteristics. |
| `//post-register` | Register and configure post-processors in PRISM for machine compatibility. |
| `//post-validate` | Validate G-code output against machine capabilities and safety requirements. |
| `//pp-resolve` | Resolve machine, material, and tool context from PRISM's registries and show the resolved parameters that PostProcessor… |
| `//pre-review` | Manual invocation of the **pre-Claude review pattern** (sibling of automatic |
| `//precompact` | Writes a structured handoff through psk's `handoff` syscall so the post-`/compact` |
| `//precompact-alpha` | Force-claim slot ALPHA + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-bravo` | Force-claim slot BRAVO + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-charlie` | Force-claim slot CHARLIE + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-delta` | Force-claim slot DELTA + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-echo` | Force-claim slot ECHO + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-foxtrot` | Force-claim slot FOXTROT + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-golf` | Force-claim slot GOLF + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-hotel` | Force-claim slot HOTEL + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-india` | Force-claim slot INDIA + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-juliett` | Force-claim slot JULIETT + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-kilo` | Force-claim slot KILO + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-lima` | Force-claim slot LIMA + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-mike` | Force-claim slot MIKE + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-november` | Force-claim slot NOVEMBER + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-oscar` | Force-claim slot OSCAR + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-papa` | Force-claim slot PAPA + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-quebec` | Force-claim slot QUEBEC + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-romeo` | Force-claim slot ROMEO + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-sierra` | Force-claim slot SIERRA + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-tango` | Force-claim slot TANGO + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-uniform` | Force-claim slot UNIFORM + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-victor` | Force-claim slot VICTOR + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-whiskey` | Force-claim slot WHISKEY + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-xray` | Force-claim slot XRAY + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-yankee` | Force-claim slot YANKEE + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-zebra` | Force-claim slot ZEBRA + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//precompact-zulu` | Force-claim slot ZULU + run the full /precompact pipeline. NATO-phonetic shortcut for slot-bound precompact handoff. |
| `//prints` | Manage engineering prints/drawings, search by part number or customer, view revision history, and track print library s… |
| `//process-docs` | Extract structured data from POs, invoices, and RFQs. Auto-classify documents, review pending extractions, and approve … |
| `//program-optimize` | Re-run per-block physics optimization on existing G-code. Returns optimized program with variable S/F per block. |
| `//program-perfect &lt;part&gt;` | Operator front-end for the canonical part-program perfection chain. Generate → optimize → validate → loop until converg… |
| `//program-simulate` | Simulate CNC programs across all machine types with physics-based validation, collision detection, and cycle time predi… |
| `//psn-automate` | Generate per-unit implementation plans for PSN-INCORPORATION-MS0 deep-learning, machine-learning, and deep-reasoning un… |
| `//psn-autonomy-cycle` | PSN Autonomy Cycle — fire all 5 self-learning primitives in sequence (P1 ingest → P2 score → P3 trainer manifest → P4 s… |
| `//qdrant-revive` | Diagnose and revive the local Qdrant vector-DB container. Run when you see the "Qdrant OFFLINE" banner in /checkin or f… |
| `//quote` | Generate quotes for manufacturing jobs with accurate cost estimation, cycle time prediction, and competitive pricing. |
| `//r12-audit` | Run the fleet-wide R12 audit — find hooks that reference fake MCP actions in nudge text. Reports Tier A (real dispatche… |
| `//reap-zombies` | HOOK COVERAGE (added 2026-04-30):** This action runs automatically on every SessionStart via `.claude/hooks/stale-claim… |
| `//refresh-awareness` | Regenerate all auto-injected awareness artifacts — build context, build vision, Claude brief — so the next prompt has f… |
| `//research &lt;token-or-unit-id&gt;` | One-shot research brief for a roadmap unit-id OR a free token. Fuses master-index + wiki-query + memory recall + system… |
| `//schedule` | Schedule manufacturing jobs with capacity planning, due date optimization, and resource allocation. |
| `//scrutiny-gate` | Operator front-end for the canonical PRISM scrutiny gates — per-file 2-reviewer in BUILD stage, then end-of-task 3-of-3… |
| `//session-cycle` | Composed lifecycle pipeline — runs the canonical chat session as a single chain: /checkin → /pick-unit → /research → bu… |
| `//ship-confirm` | Confirm shipment readiness and generate shipping documentation. |
| `//shop-floor-query` | Query live shop floor data with natural language and get actionable insights. |
| `//shop-knowledge` | Query the knowledge mined from 2000+ Box drive programs. Get shop patterns for speeds/feeds, tool station layouts, oper… |
| `//shop-live-status` | Inspect canonical shop contracts, active jobs, labor sessions, and WebSocket room health. |
| `//shop-quote` | Generate rapid quotes for shop floor requests with variability-aware time estimates. |
| `//shop-safety-check` | Comprehensive safety validation for operations, programs, and setups. |
| `//slot-query` | Pull tasks/sessions/commits/handoffs for a chat slot by name + recency. Use whenever the user asks "what is slot X doin… |
| `//spc` | Real-time SPC monitoring with adaptive control limits and variability awareness. |
| `//startup` | Full session bootstrap via psk — 4 syscalls (checkin composite + position + handoff read + pick). Heavyweight; use /boo… |
| `//startup-alpha` | Force-claim slot ALPHA + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-bravo` | Force-claim slot BRAVO + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-charlie` | Force-claim slot CHARLIE + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-delta` | Force-claim slot DELTA + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-echo` | Force-claim slot ECHO + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-foxtrot` | Force-claim slot FOXTROT + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-golf` | Force-claim slot GOLF + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-hotel` | Force-claim slot HOTEL + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-india` | Force-claim slot INDIA + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-juliett` | Force-claim slot JULIETT + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-kilo` | Force-claim slot KILO + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-lima` | Force-claim slot LIMA + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-mike` | Force-claim slot MIKE + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-november` | Force-claim slot NOVEMBER + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-oscar` | Force-claim slot OSCAR + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-papa` | Force-claim slot PAPA + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-quebec` | Force-claim slot QUEBEC + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-romeo` | Force-claim slot ROMEO + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-sierra` | Force-claim slot SIERRA + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-tango` | Force-claim slot TANGO + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-uniform` | Force-claim slot UNIFORM + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-victor` | Force-claim slot VICTOR + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-whiskey` | Force-claim slot WHISKEY + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-xray` | Force-claim slot XRAY + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-yankee` | Force-claim slot YANKEE + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-zebra` | Force-claim slot ZEBRA + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//startup-zulu` | Force-claim slot ZULU + run the full /startup pipeline. NATO-phonetic shortcut for slot-bound session start. |
| `//sync-terminals` | Synchronize awareness and state across multiple Claude Code terminal sessions. |
| `//timeline` | View the activity timeline and manage comments for any PRISM entity (jobs, quotes, POs, invoices, NCRs, etc.). |
| `//tolerance-stack` | Perform tolerance stack-up analysis with variability-aware worst-case and statistical methods. |
| `//tooling` | Manage tool holder inventory, search by taper/machine/brand, check stock levels, and get reorder alerts. |
| `//traveler` | Manage E2-style job travelers with ordered routing steps, dual time tracking (setup + cycle), QR scan, and machine disp… |
| `//utilization-dashboard` | Graph-wide utilization classifier — buckets every PRISM node into hub/sink/source/orphan/ghost. Use to answer "what's a… |
| `//vendor` | Manage vendors, view scorecards, map supply chain, track brand preferences, and assess supply risk for JM Die. |
| `//wedm-program` | Generate a complete, production-ready Wire EDM NC program with physics-derived parameters, per-pass optimization, and f… |
| `//weekly-synthesis` | 15-minute Monday ritual — synthesize last 7 days of vault activity into thesis + contradictions + gaps + 1 action. |
| `//wire-edm-analyze` | Run full surface integrity, recast layer, fatigue, and compliance analysis on a Wire EDM job. |
| `//wire-edm-studio` | Launch the Wire EDM Studio wizard — full pipeline from DXF/contour to optimized G-code with per-pass discharge energy, … |
| `//wire-pipeline` | Operator front-end for the canonical orphan→wired pipeline. orphan-inventory → /dedup → /research → wire dispatcher act… |
| `//wiring-audit` | Audit harness hook wiring — orphan hooks (on disk, unwired), dangling refs (wired, no file), and C:↔H: settings mirror … |
| `/`/playbook`` | Query, audit, and analyze the **~296-rule machining-knowhow corpus** maintained in `MachiningPlaybookEngine` (`mcp-serv… |
| `/── Auto-trigger frontmatter (forward-compat for Phase D orchestrator) ──` | triggers: |
| `/── Auto-trigger frontmatter (forward-compat for Phase D orchestrator) ──` | triggers: |
| `/── Auto-trigger frontmatter (forward-compat for Phase D orchestrator) ──` | triggers: |
| `/── Auto-trigger frontmatter (forward-compat for Phase D orchestrator) ──` | triggers: |
| `/── Auto-trigger frontmatter (forward-compat for Phase D orchestrator) ──` | triggers: |
| `/── Auto-trigger frontmatter (forward-compat for Phase D orchestrator) ──` | triggers: |
| `/── Auto-trigger frontmatter (forward-compat for Phase D orchestrator) ──` | triggers: |
| `/── Auto-trigger frontmatter (forward-compat for Phase D orchestrator) ──` | triggers: |
| `/── PRISM auto-trigger frontmatter ──` | triggers: |
| `/── PRISM auto-trigger frontmatter ──` | triggers: |
| `/advisor-strategy` | Claude Advisor strategy router |
| `/ask-local` | Offload token-heavy work to a LOCAL Ollama model so it never enters the Claude context window. Use BEFORE reading a lar… |
| `/audit-viz-first` | Audit / discovery query that uses /system-viz as the primary search surface BEFORE Grep/Glob/Agent. Auto-routes the nou… |
| `/Blueprint to CAD Generation Studio` | Skill:** `/cad-from-blueprint` |
| `/build-state` | Print the current PRISM BUILD_STATE — built vs unwired vs pending vs frontend-merge — and offer drill-down |
| `/Business Wiring Health Check` | Check the health of cross-engine business wiring chains. |
| `/CAD Neural Training Studio` | Skill:** `/cad-train` |
| `/CAD Retrieval-Augmented Generation Studio` | Skill:** `/cad-rag` |
| `/CAD Similarity Search Studio` | Skill:** `/cad-search` |
| `/checkin` | Slot-claim + drift + commit-hygiene + position via the psk `checkin` composite syscall. Args after /checkin are the pri… |
| `/claude-flow-help` | Show Claude-Flow commands and usage |
| `/claude-flow-memory` | Interact with Claude-Flow memory system |
| `/claude-flow-swarm` | Coordinate multi-agent swarms for complex tasks |
| `/close-out` | Close out a completed milestone across every roadmap surface — roadmap-index.json, MILESTONE_PROGRESS, BUILD_STATE, and… |
| `/close-out-audit` | Audit roadmap envelopes for units whose deliverables exist on disk but whose status is still "pending" (silent close-ou… |
| `/college-extract` | Execute a per-course AUTOGEN-SPEC from state/shared/college-course-specs/ — drives live extraction (Playwright/WebFetch… |
| `/connection-finder` | cyrilXBT Connection-Finder over PRISM vault — for a target memory/wiki slug, rank OTHER notes by TF-IDF similarity that… |
| `/Context Audit` | Audit the current context window to identify token waste and suggest pruning. This is a read-only diagnostic — it doesn… |
| `/continue-roadmap` | (no description) |
| `/De-Sloppify` | A focused cleanup pass that runs AFTER code generation and review. Catches mechanical issues that creative/review agent… |
| `/distill-tribal` | Cluster near-duplicate tribal tips by TF-IDF cosine and emit canonical IdeaBlocks (Q-A markdown) to knowledge/wiki/code… |
| `/dream-review` | List staged DREAM-RECEIPT-MS0 receipt-bundle artifacts in state/shared/dream-artifacts/, render each artifact's REPORT.… |
| `/E2 Setup` | Connect PRISM to your E2 Shop System instance for bidirectional work order sync, tool inventory import, and time tracki… |
| `/erp-health` | ERP persistence health check |
| `/error-learn-review` | Review captured hook-block / tool-error patterns from the error-learn ledger. Inspect what's been blocked, see frequenc… |
| `/find` | Single-shot symbol/file resolver via /system-viz graph. Wraps master-index BM25 + node-status into one tool call. Repla… |
| `/fleet-reaper` | (no description) |
| `/fleet-reaper-home` | Configure THIS PC as a "home" fleet-reaper host (RTX 4080 SUPER class GPU 16GB + Ryzen 7 7800X3D + 64GB RAM tier — qwen… |
| `/fleet-reaper-work` | Configure THIS PC as a "work" fleet-reaper host (smaller GPU ~8GB free at idle + tighter RAM tier — qwen2.5-coder:3b re… |
| `/frontend-merge-plan` | Compare codex frontend builds (cqask/ui, mcp-cadquery/frontend) against main mcp-server/web; output port-vs-sandbox dec… |
| `/generate-roadmap` | (no description) |
| `/go` | Composite gate — fast-build + tsc + affected tests + hook coverage + lint. Run before committing a non-trivial change. … |
| `/golf-bootstrap` | One-shot setup for the golf-slot hygiene chat. Reads state/shared/golf-cron-registry.json and schedules each enabled en… |
| `/harness-security-audit` | Scan the Claude Code harness (settings.json, hooks, .mcp.json, CLAUDE.md) for security misconfigurations — leaked secre… |
| `/hook-profile-set` | View or change the PRISM_HOOK_PROFILE runtime gate (minimal \| standard \| strict). Lets you cut hook overhead 60-90% for… |
| `/integrate` | Slot-Worktree Integrator — the "8th chat" role. Surveys all slot/<name> branches across H:/prism-slot-*/, ff-merges wor… |
| `/iterate-retrieve` | Progressive context refinement for codebase search. Runs DISPATCH→EVALUATE→REFINE→LOOP (max 3 cycles) and returns ranke… |
| `/Job Cost Estimator` | Estimate complete job costs using PRISM's JobCostingEngine with shop-configured rates. |
| `/karpathy` | Reaffirm the Karpathy 4 coding rules in the current chat. Use when starting a complex implementation or after a compact… |
| `/lathe` | Lathe / Turning Domain Studio — the single entry point that orchestrates every PRISM lathe engine, the Lathe Tier-3 AGI… |
| `/lathe-master-post` | Unified lathe post-processing pipeline — route, emit, validate, explain, and audit G-code for any lathe controller |
| `/lathe-postgen` | Full wizard for lathe post-processor generation — ingest spec, generate skeleton, validate, test, and register |
| `/Local Ask` | Route a prompt through PRISM's local LLM orchestration so Ollama models (with optional LoRA adapter) handle it before a… |
| `/local-health` | Check Ollama/Docker stack health and token savings potential |
| `/Machine Harden` | Audit, enrich, and validate the 910-machine database. |
| `/macro-program` | Macro-program pipeline — the operator-facing front door for MACRO-PROGRAM-PIPELINE-MS0. Catalog the 4 JM Okuma-OSP macr… |
| `/Memory Search` | Semantic search over PRISM's long-term memory: past programs, outcomes, tribal tips, formulas, rules, playbooks, operat… |
| `/mill` | Milling Domain Studio — the single entry point that orchestrates every PRISM milling engine (~240 in the eng.mill clust… |
| `/MILL-MASTER` | Envelope:** `MILL-MASTER` v4.0.0 |
| `/moc-gaps` | cyrilXBT Gap-Finder over a PRISM MOC (Map of Content) — classifies [[slug]] children into well-covered, fragile, missin… |
| `/Model Status` | Report health of the local LLM stack: Ollama daemon reachability, installed models, warm-up state, active LoRA adapters… |
| `/Neural CAD Generation Studio` | Skill:** `/agi-cad-generate` |
| `/octopus` | Octopus multi-LLM consensus — provider fleet status, setup guidance, and how to run a cross-vendor consensus / review p… |
| `/offload-stats` | Show Ollama task offloading statistics and token savings |
| `/ollama-bridge` | Run a multi-step PRISM codebase investigation on a LOCAL Ollama model — the model autonomously chains read-only graph/w… |
| `/Ollama-Powered PRISM Architecture Plan` | - 106 hooks inject `additionalContext` → 3-10K tokens/prompt overhead |
| `/ollama-route-check` | One-line health check of the Ollama offload route — current ratio vs ≥30% target, hook decision rate, top keep-on-Claud… |
| `/Outcome` | Record the real-world result of a PRISM-produced CNC program. This is the truthful signal that feeds U-LLM6 LoRA traini… |
| `/page-sweep` | (no description) |
| `/PDF Learn` | Extract and learn from PDF documents using deep AI reasoning. Converts PDFs into categorized tribal knowledge, formulas… |
| `/PDF Process` | Classify, extract, and normalize manufacturing knowledge from PDFs across PRISM locations. |
| `/peer-audit` | READ-ONLY operator query — surface recent activity by the golf hygiene chat across the bug-attribution ledger, peer-aud… |
| `/pick-build-close` | Macro loop — pick a unit, build it, close it out. Chains /pick-unit → build (whatever the chat does) → /close-out-audit… |
| `/program-audit` | Full CNC program audit (safety + physics + collisions) |
| `/psn-state` | Print the PRISM Synergy Network (PSN) per-leg health digest on-demand. Shows concerning legs only (silent when healthy)… |
| `/Quote-to-Ship Pipeline` | Run the 21-stage QuoteToShip pipeline from blueprint intake through shipping. |
| `/regression-audit` | Audit CLAUDE.md "## Recent regressions" entries for staleness — surface entries whose code was already fixed but doc ne… |
| `/Resource Census` | Scan and categorize all manufacturing resources (PDFs, videos, CAD models, catalogs, data files) across 3 filesystem lo… |
| `/route` | Classify a task prompt and get a recommended AI provider chain (Claude / Gemini / GPT-4.1 / DeepSeek-R1 / Ollama). Opti… |
| `/route-suggest-stats` | Report mcp-route-suggest fire telemetry — total TOKEN-SAVE fires this fleet, breakdown by tool (Grep/Bash/Read/Write/Gl… |
| `/route-to-obsidian` | Summarize an Obsidian wiki entry via local Ollama (qwen2.5-coder) instead of reading it into Claude context. Use for an… |
| `/scrutinize-mark` | Record completion of scrutiny (self review + parallel reviewer agent) so the Stop hook lets you finish. Run after /scru… |
| `/self-improve` | Self-improvement pattern scanner |
| `/Shared Agent Chat` | Post a shared note that both Claude and Codex can see through the PRISM coordination surfaces. |
| `/Shared RGS Sync` | Use this command when Claude or Codex needs to align on roadmap sequencing, execution gating, or the readiness to begin… |
| `/Shop Schedule` | Schedule jobs across machines using PRISM's ShopSchedulerEngine with registry-backed machine capabilities. |
| `/Shop Setup Wizard` | Configure shop-wide rates, machines, and overhead for accurate costing across all PRISM engines. |
| `/skill-lint` | Run the static skill-quality linter over PRISM's skill library — flags lazy hand-wave language, bodies past the 500-lin… |
| `/skill-test` | Run the @eng_khairallah1 three-scenario production-grade test on a PRISM skill — invoke it against its happy / edge / s… |
| `/skill-trigger-coverage` | (no description) |
| `/smart` | (no description) |
| `/Sync Memory` | Dump PRISM's Qdrant-backed memory to a JSON bundle on the portable H: drive (or import one into a fresh PC). Designed f… |
| `/synergy-recall` | Cross-surface recall — fan out a query across all 5 PRISM knowledge surfaces (master-index, tribal, memory, wiki, skill… |
| `/system-coordinate` | Tier-1 → Tier-2 entry — dispatch a DomainAGIIntent through FullSystemAICoordinatorEngine. Routes mfg slice (mill/lathe/… |
| `/system-viz` | Open the PRISM Live System Map — PSN's render substrate. 3D 10-layer + 21 roost overlays of the entire codebase + all 1… |
| `/system-viz-drift` | Surface the latest system-viz drift report inline. Tells you which namespaces have drifted from the graph snapshot (sta… |
| `/Train LoRA` | Kick off a LoRA fine-tune of the local base model using recent shop outcomes as training signal. Queries `OutcomeTracki… |
| `/unlinked-mentions` | Scan memory + wiki vault for unlinked mentions of known note-slug names. Emits state/shared/UNLINKED-MENTIONS.{json,md}… |
| `/Video Learn` | Extract and learn from machining/manufacturing videos using deep AI. Converts video content into tribal knowledge, proc… |
| `/wedm` | Wire-EDM Domain Studio — the single entry point that orchestrates every PRISM wire-EDM engine (62 WEDM + 181 EDM-family… |
| `/wedm-audit` | Audit WEDM asset counts and dispatcher wiring status |
| `/wedm-safety-gate` | Evaluate WEDM program composite S(x) safety score before emit |
| `/wiring-batch` | Propose a wire-batch plan from the unwired-engine backlog — pick a domain, get top-N engines + suggested dispatcher + a… |
| `/wiring-potential` | Rank candidate dispatchers for an orphan engine (built but unwired). Returns top-K targets with rationale + numeric tra… |
