Run the full PRISM test suite:
1. Run `cd mcp-server && npx vitest run --reporter=verbose 2>&1` — capture all test results
2. If any tests fail, list the failing test names and their error messages
3. Report: X/Y tests passing, any failures with details, test duration
