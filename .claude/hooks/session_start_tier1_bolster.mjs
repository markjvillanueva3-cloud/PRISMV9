#!/usr/bin/env node
/**
 * session_start_tier1_bolster.mjs — SessionStart Hook (stub)
 *
 * Placeholder for Tier 1 context bolstering. The real implementation was
 * referenced in settings.json but never landed on disk.
 *
 * This stub exits cleanly so SessionStart doesn't spam the transcript
 * with MODULE_NOT_FOUND errors. Replace the body if/when a real bolster
 * routine is needed — for now tier1-context-pack.mjs handles loading.
 */
process.exit(0);
