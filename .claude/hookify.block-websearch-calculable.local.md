---
name: block-websearch-calculable
enabled: true
event: all
action: block
---
TOKEN SAVE: PRISM has engines for speed/feed calculations. Use /calc, /defaults, or /process-calc instead of web searching. Saves ~1500 tokens per blocked search.
