# PRISM MCP Server - CI/CD Pipeline
# R6 Production Hardening - Safety-critical manufacturing intelligence
name: PRISM CI/CD

on:
  push:
    branches: [master, main, 'release/*']
  pull_request:
    branches: [master, main]

env:
  NODE_VERSION: '20'
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # Stage 1: Build + Unit Tests
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - run: npm ci
      - run: npm run build
      - name: Unit Tests (vitest)
        run: npx vitest run --reporter=verbose
      - name: R4-R11 Standalone Tests
        run: npx tsx scripts/run-all-tests.ts --phase vitest-skip 2>&1 || true
        env:
          NODE_OPTIONS: '--max-old-space-size=4096'
      - name: R4-R11 Phase Tests (quick)
        run: |
          for phase in r2 r3 r4 r5 r7 r8 r9 r10 r11; do
            if ls tests/$phase/*-tests.ts 1>/dev/null 2>&1; then
              echo "=== $phase ==="
              first_file=$(ls tests/$phase/*-tests.ts 2>/dev/null | head -1)
              if [ -n "$first_file" ]; then
                timeout 60 npx tsx "$first_file" 2>&1 | tail -5 || echo "⚠️ $phase: timeout or error"
              fi
            fi
          done
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: prism-build
          path: dist/
          retention-days: 7

  # Stage 2: Security Scan
  security:
    runs-on: ubuntu-latest
    needs: build-and-test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      - run: npm ci
      - name: Audit dependencies
        run: npm audit --production --audit-level=high
        continue-on-error: true
      - name: Check for secrets
        run: |
          grep -rn "password\|secret\|api_key\|token" src/ --include="*.ts" | grep -v "test" | grep -v "\.d\.ts" || echo "No secrets found"

  # Stage 3: Docker Build + Push
  docker:
    runs-on: ubuntu-latest
    needs: [build-and-test, security]
    if: github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/heads/release/')
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha
            type=ref,event=branch
            type=semver,pattern={{version}}
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          target: production

  # Stage 4: Deploy (manual approval for production)
  deploy:
    runs-on: ubuntu-latest
    needs: docker
    if: github.ref == 'refs/heads/master'
    environment: production
    steps:
      - name: Deploy notification
        run: echo "🚀 PRISM ${{ github.sha }} ready for deployment. Docker image pushed."
      - name: Health check after deploy
        run: |
          echo "Post-deploy health check would run here"
          echo "curl -sf http://PRODUCTION_HOST:3000/health || exit 1"
