/**
 * postUltActionSchemas — Zod validation schemas (stub for dispatcher import)
 */
import { z } from "zod";

export const ACTION_POST_ULT_SCHEMAS: Record<string, z.ZodType> = {};

// Backward-compat alias (esbuild fix 2026-04-25)
export const POST_ULT_ACTION_SCHEMAS = ACTION_POST_ULT_SCHEMAS;

