/**
 * solidcamCodeGeneratorActionSchemas — Zod validation schemas (stub for dispatcher import)
 */
import { z } from "zod";

export const ACTION_SOLIDCAM_CODE_GENERATOR_SCHEMAS: Record<string, z.ZodType> = {};
