---
source: ultracode Workflow wf_033276f6-cf8 (5 agents, 1.31M tokens)
built: 2026-06-10 slot:zulu
purpose: ROI-ranked build queue to accelerate Obsidian + Hermes
---

The verification confirms the key contradictions. Now I have the grounded picture:

- **`knowledge/hermes-brain/` does NOT exist** — confirms the 2499-file bridge is unbridged (memo 3's standout win is real).
- **HMEMV envelope: all 11 units `not_started`** — confirms envelope drift (memos 1+4 both flagged; the units shipped per git but envelope lies).
- **`h-to-c-obsidian-mirror.mjs` exists + 1 settings ref** — confirms reverse mirror IS wired (memo 1 correct; memo 4's "reverse mirror does NOT exist" is STALE — the H→C *memory* mirror exists, though full HMEMV04-06 bidirectional vault edit-flow-back is a separate, broader claim).
- **`.partial` orphan: 21.7MB, Jun 9** + live sidecar 22MB Jun 10 — confirms dead bytes.

I have enough to produce the queue.

## Obsidian + Hermes Acceleration Build Queue (zulu, 2026-06-10)

### TIER 0 -- activate-dormant (cheap, do first)

- **[T0-1] Bridge the 2499 stranded Hermes desktop memories into the vault** | unlocks 2499 Hermes learnings as recall/RAG/Obsidian-graph nodes (currently 0 are vault-visible) | `scripts/hermes-obsidian-memory-bridge.mjs` (manual run first) → then `Enable-ScheduledTask -TaskName 'PRISM Hermes-Obsidian Bridge'`; target `knowledge/hermes-brain/` (VERIFIED ABSENT — never bridged); source `C:/Users/wompu/AppData/Local/hermes/*.md` | risk LOW (idempotent, SHA-dedup, fresh dir) — one-time bulk-add of 2499 nodes = graph regen pressure; inspect content quality on the dry run | **zulu-can-do-inline** (manual run) → confirm → enable cron
- **[T0-2] Fix HMEMV-MS0 envelope status drift** | stops the orchestrator (i.e. zulu's own queue) re-queuing 11 shipped units | `mcp-server/data/milestones/HERMES-MEMORY-VAULT-MS0.json` (VERIFIED all 11 + milestone `not_started`) — set shipped units to `completed`, then `node scripts/build-milestone-progress.mjs`. **R12 caveat: HMEMV02/03 are NOT shipped per memos 1+4 — do NOT mark those `completed`; only mark units with a matching git commit (HMEMV01/04-11 closeout `ed62a8e1db`). Verify each against git-log before flipping.** | risk LOW (data fix) — over-marking HMEMV02/03 would hide real gaps | **zulu-can-do-inline**
- **[T0-3] Reap stale `.partial` orphan + dedup the two embedding caches** | removes 21.7MB dead bytes + halves embed I/O + kills R7 dual-cache drift | delete `state/shared/memory-embeddings-sidecar.json.partial` (VERIFIED 21.7MB, Jun-9, orphan vs live 22MB Jun-10 sidecar); repoint `memory-relevance-inject.mjs` + `scripts/lib/memo-embed-lib.mjs` off `memo-embedding-cache.jsonl` onto the canonical 768-d sidecar | risk LOW-MED — repoint changes a live recall hook's source; validate both hooks still fire post-repoint | **zulu** (delete .partial inline) / **route:bravo** (cache-repoint — touches recall retrieval, domain build)
- **[T0-4] Activate Hermes Curator first pass + consolidate the duplicate skill** | proves the never-run (`run_count:0`) skill-lifecycle daemon; archives `prism-daily-brief-loop` (use_count 0, agent-authored near-dup of `prism-vault-loop`) | `hermes curator run --dry-run`; `config.yaml:390-399`; `skills/.curator_state`; `skills/prism/prism-daily-brief-loop/SKILL.md` | risk LOW (worst case = recoverable archive) | **zulu-can-do-inline** (it's a Hermes CLI command + dup cleanup, orchestration-adjacent)

### TIER 1 -- high-ROI builds (S/M effort)

- **[T1-1] CAG cache-wire the per-turn injectors + add cold-hit telemetry** | every turn across 26 slots re-emits static doctrine today — biggest *measured* token/cost lever; makes cold-hit rate measurable | route the ~8 static injectors through `PromptCachingEngine.buildCachedSystem()` (built+wired to `prism_dev:pc_build_cached_system`, devDispatcher.ts:3955, 28 tests — but `Grep buildCachedSystem .claude/hooks` = 0, so injection layer is UN-wired); add `increment/coldHit/stats` telemetry to `cag-cold-cache-anchor.mjs` | risk MED — moving static→SessionStart changes per-turn context shape across all slots; stage + measure before fleet-wide | **route:bravo** (recall/injection-layer build) — dep: none (engine proven) | **highest ROI in T1**
- **[T1-2] HMEMV02 explainable retrieval** | surfaces matched-tokens + BM25-component + dense-cosine per hit (lib already computes both scores) — trust/auditability, low effort | `scripts/lib/memory-index-search-lib.mjs` (return `explanation`), `memory-index-precheck-inject.mjs` `renderBlock` | risk LOW | **route:bravo** — dep: T0-3 (single canonical cache first, so explanation reflects the live path)
- **[T1-3] Wire `psn-attribution` read/render action** | closes "did knowledge actually get used?" blindness — ledger is LIVE-writing (429 rows, written today 11:08) but has 0 consumer refs | `scripts/lib/psn-attribution-lib.mjs`, `state/shared/psn-attribution.jsonl`; add `prism_session:psn_attribution` read action (spec lever #2 named it) | risk LOW (thin read layer over a live producer) | **route:papa** (backend dispatcher action) — dep: none
- **[T1-4] Port `thinking-partner` as a Hermes-scheduled job** | the ONE cyril skill that *reasons against* the vault (surfaces tensions/contradictions/open-questions over `knowledge/memories/`) vs summarizing it — converts vault from passive recall to active contradiction-surfacing | new `.claude/commands/thinking-partner.md` (VERIFIED absent) + new `context_from`-chained job in `C:/Users/wompu/AppData/Local/hermes/cron/jobs.json` after weekly synthesis | risk LOW-MED — adds a recurring local-model job; gate on Curator (T0-4) so it isn't another orphan dup | **route:bravo** (skill+job build) — dep: T0-4 (Curator live to catch dups), T0-1 (richer vault to reason over) | **best net-new ROI**
- **[T1-5] Close the GEPA feedback edge** | self-improving skill loop with zero new infra — GEPA stages candidates but no recurring cron runs it on fresh traces (stalled at 1 NEEDS-REVIEW candidate since 2026-06-09) | review→apply the staged `SKILL-CANDIDATE-GEPA-20260610-prism-vault-loop.md` (real failure-pattern output, caught qwen 32K-ctx-too-small), then add a `context_from`-chained `hermes-skill-gepa.mjs` job after weekly self-review in `cron/jobs.json` | risk LOW (dry-run default, operator-PR-gated ship — never touches live skill) | **route:bravo** (skill loop) + **zulu** (dispatch reviewer-subagent to grade the candidate) — dep: T0-4 (Curator), weekly-self-review proven (not due until 2026-06-14, so candidate-grade can proceed now, cron-edge after)
- **[T1-6] Per-job Hermes model-routing by cost/ctx** | fewer ctx-overflow failures + faster cheap jobs — infra exists (per-job `model` field), underused | `cron/jobs.json` (per-job model), `config.yaml:1-7`, `context_length_cache.yaml`; encode the GEPA-learned guard (qwen 32K too small for 65K-prompt jobs → never route 65K jobs to qwen) | risk LOW | **zulu-can-do-inline** (knob/config flip on Hermes side) — dep: none

### TIER 2 -- deeper builds (L effort)

- **[T2-1] Load 17K memory corpus into Qdrant + dense arm → ANN (HMEMV09)** | kills the per-query linear scan over 17,010 int8 vectors (`denseRankAll` full scan every dense query); unlocks filtered per-leg/galaxy + sparse-dense + quant; biggest recall scalability+latency win | `prism-qdrant` container already UP on `:6333` (collections currently only engines/skills/formulas — memory/tribal/wiki NOT loaded); new migration seeding from `memory-embeddings-sidecar.json`; rewire `scripts/lib/memory-index-search-lib.mjs` `denseRankAll`→Qdrant, KEEP BM25 RRF + flat-file fallback; engines under `mcp-server/src/engines/database-expansion/*` (QdrantMemoryEngine) | risk MED-L — recall path rewrite; the flat-file fallback is the safety net, validate it triggers on Qdrant-down | **route:india** (RAG/ANN/embedding migration) — dep: T0-3 (canonical single sidecar = clean migration source), T0-2 (envelope honest so HMEMV09 isn't double-queued)
- **[T2-2] HMEMV03 temporal `recall_at_time`** | unique capability — git-log-walk over memories+wiki → state-of-belief at time T (regression archaeology) | new lib + `prism_memory`/`devDispatcher.ts` action | risk LOW-MED (read-only git walk) | **route:papa** (backend dispatcher) — dep: T0-2 (HMEMV03 must stay `not_started` until built)
- **[T2-3] HMEMV04-06 full bidirectional vault edit-flow-back** | Obsidian-side human edits flow H→C so a vault edit isn't silently overwritten by the one-way C→H mirror | `HERMES-MEMORY-VAULT-MS0.json` (HMEMV04-06 `not_started`), pattern after `scripts/bootstrap-c-mirror.mjs`. **R12 / R7 conflict to resolve FIRST: memo 1 says the H→C *memory* reverse mirror (`h-to-c-obsidian-mirror.mjs`) IS shipped+wired (VERIFIED 1 settings ref); memo 4 says "reverse mirror does NOT exist." These are reconcilable — the *memory-file* mirror exists, but HMEMV04-06's broader bidirectional-vault contract (conflict-resolution, edit-flow-back for non-memory vault notes) may be the genuine gap. DO NOT build until you diff `h-to-c-obsidian-mirror.mjs`'s scope against the HMEMV04-06 unit specs — you may be rebuilding a shipped asset.** | risk MED (mirror-loop, clobber — same class PRISM already hit) | **route:bravo** — dep: T2-3-diff (resolve the shipped-vs-gap conflict), T0-2

### Recommended first 3 actions (concrete, sequenced)

1. **[T0-1] Dry-run the Hermes-Obsidian bridge — the single biggest cheap win (2499 stranded memories, target dir verified absent).** First command: `node H:/prism/scripts/hermes-obsidian-memory-bridge.mjs` (manual, no elevation needed). Inspect a sample of the new `knowledge/hermes-brain/*.md` for content quality, THEN `Enable-ScheduledTask -TaskName 'PRISM Hermes-Obsidian Bridge'`. Do this first — it's pure activation, low risk, and enriches the vault that every downstream item (T1-4 thinking-partner, T2-1 Qdrant) reasons over.

2. **[T0-2] Fix the HMEMV envelope drift so you stop re-queuing shipped work — but honestly (R12).** First edit: open `mcp-server/data/milestones/HERMES-MEMORY-VAULT-MS0.json`, and for EACH of the 11 units run `git log --oneline --all | grep -i hmemv0N` to confirm a real ship commit before flipping `not_started`→`completed`. Mark only the verified-shipped units (closeout `ed62a8e1db` covers the MS0 batch); leave HMEMV02/03 `not_started` (both memos confirm unbuilt). Then `node scripts/build-milestone-progress.mjs`.

3. **[T0-3] Reap the verified `.partial` orphan inline.** First command: `rm state/shared/memory-embeddings-sidecar.json.partial` (VERIFIED 21.7MB Jun-9 dead orphan; live sidecar is the Jun-10 22MB file). Then write the cache-repoint (the R7 dual-cache dedup) as a routed work order to **bravo** — that half touches a live recall hook and is a domain build, not a zulu inline flip.