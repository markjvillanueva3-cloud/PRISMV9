{
  "schemaVersion": 4,
  "id": "MACRO-PROGRAM-PIPELINE-MS0",
  "title": "Macro-program pipeline — catalog + safety-gated fill + per-machine emit + bulk fan-out",
  "track": "MACRO-PROGRAM-PIPELINE",
  "priority": "P0",
  "owner": "claude-a7ea87ab",
  "created_at": "2026-05-12T18:55:00.000Z",
  "updated_at": "2026-05-12T18:55:00.000Z",
  "status": "in_progress",
  "total_units": 7,
  "completed_units": 5,
  "updated_at": "2026-05-13T22:00:00.000Z",
  "dependencies": [],
  "rationale": "User 2026-05-12: 'add the corresponding macro program to each file system that has a part that could use one of the programs; check existing programs for each part then make a version of each program utilizing one of the macro programs; make a program for each lathe machine and label it for each machine; coding must match controller and machine capabilities'. Spec was scoped by claude-8f2683e8 (slot charlie) per the forge methodology with the live system-viz graph as dependency oracle. Key finding: ~80% of this pipeline already exists (OkumaParametricProgramEngine, MacroProgramIntelligenceEngine, OkumaMacroHeaderGeneratorEngine, LatheProofCarryingEmitEngine, LathePrintProgramEmitterEngine, ShopConfigurationEngine, LathePartClassifierEngine, PartFolderOrganizerEngine, etc). Milestone is mostly WIRING + a thin orchestrator + the 2 missing macro families + the bulk fan-out — NOT a from-scratch build. U2-U6 are SAFETY-CRITICAL (auto-emitting G-code from print dims) and gated by S(x) >= 0.70 + envelope check + sim + operator sign-off. NEVER bulk-auto-emit G-code. U1 is non-safety-critical (file copying + part classification) and ships first.",
  "knowledge_sources": {
    "engines": [
      "MacroLibraryEngine (new in U1 — catalog the 4 OSP macros, family match, template placement)",
      "MacroProgramIntelligenceEngine (parse → AST, validate, expand)",
      "OkumaParametricProgramEngine (extend in U3 with generateWaferInsert + generateTopHatCasing)",
      "OkumaMacroHeaderGeneratorEngine (VC100=... header block)",
      "LatheProofCarryingEmitEngine (S(x) >= 0.70 gate)",
      "LathePrintProgramEmitterEngine (envelope-aware emit)",
      "LathePrintProgramSignoffEngine / LatheFirstPieceApprovalEngine / LatheProgramSignoffDossierEngine (operator-in-the-loop)",
      "LathePartClassifierEngine + LathePartFamilyPlanningEngine (part → family)",
      "PartFolderOrganizerEngine (_PART LIBRARY/ + part.json)",
      "ShopConfigurationEngine (21 JM machines)",
      "PostProcessorPipelineEngine + LathePostProcessorEngine + LathePostProcessorDialectValidatorEngine",
      "PPMacroFlowValidatorEngine + PPMacroVariableValidatorEngine",
      "LatheReplayFrameCompilerEngine + LatheEnvelopeBreachReplayEngine"
    ],
    "tribal_knowledge": "JM Die Macro programs/ (4 Okuma OSP macros: BASE WAFER INSERT MACRO, BASIC-CASING, BASIC CASING WITH SINGLE COUNTERBORE, BASIC TOP HAT CASING WITH SINGLE COUNTERBORE); project_okuma_controller_limits (P300L/P300LA program-memory + char-count ceilings on large parametric programs)",
    "formulas": [
      "VC130 = (VC111 * 3.82) / VC110 (Okuma RPM from SFM and dia — must remain expression, never pre-computed)",
      "Okuma ID-taper angle in 250-270 band (270 = 0 deg convention)",
      "S(x) >= 0.70 HARD BLOCK (LatheProofCarryingEmitEngine + prism_safety:validate_physics)"
    ],
    "reference": [
      "state/shared/specs/MACRO-PROGRAM-PIPELINE-MS0-2026-05-12.md (full spec, 31 KB, 7 units)",
      "H:/PRISM/JM DIE/Macro programs/ (the 4 OSP macros)",
      "H:/PRISM/JM DIE/_PART LIBRARY/ (25,028 part folders, 483 customer folders — built via phase18 + phase19)",
      "src/data/jm-die-profile.ts (21 lathe-capable machines)"
    ]
  },
  "units": [
    {
      "id": "MS0-U1",
      "title": "MacroLibraryEngine + dual-wiring to prism_cad + prism_turning + part-folder template placement (NON-safety-critical)",
      "status": "completed",
      "shipped": [
        "MacroLibraryEngine (catalog, matchFamily, placeMacroTemplate, fanoutDryRun)",
        "MacroLibraryEngine.test.ts (36 tests passing — 4 round-trip cases through prism_cad + 4 through prism_turning)",
        "prism_cad dispatcher: 4 macro_* action cases (already in HEAD)",
        "prism_turning dispatcher: 4 macro_* action cases (this pickup) + ACTIONS enum",
        "cadActionSchemas: macroLibraryListSchema + macroMatchFamilySchema + macroPlaceTemplateSchema + macroFanoutDryRunSchema",
        "turningActionSchemas: re-export of the 4 schemas from cadActionSchemas (same engine, identical validation)"
      ],
      "completed_at": "2026-05-12T18:55:00.000Z",
      "completed_by": "claude-a7ea87ab (picked up fork commit 0e2457a80 from claude-8f2683e8)"
    },
    {
      "id": "MS0-U2",
      "title": "MacroFillOrchestratorEngine — fill VC vars from print dims (SAFETY-CRITICAL)",
      "status": "completed",
      "completed_at": "2026-05-13T18:40:00.000Z",
      "completed_by": "claude-48e5d403 (slot alpha)",
      "shipped": [
        "MacroFillOrchestratorEngine.ts (NEW, ~420 LOC) — thin orchestrator composing U3 generators",
        "PartPrintFeatures input shape (decoupled from any specific print intake engine — orchestrator caller builds it from OCR/CAD/operator-fill)",
        "MacroFillCandidate output shape — carries program, filledVars (with provenance source field), calculatedVars (expressions preserved), validation, needsGate:true, filledAt timestamp",
        "fillCandidate(features, targetMachine) — REJECTS missing required VCs per family (no guessing), REJECTS family=casing/cbore (handled by V1-V199 path), propagates underlying validator rejections, asserts calculated-VC-preservation invariant defense-in-depth",
        "prism_turning action: macro_fill_candidate (ACTIONS enum + case handler with explicit features + target_machine validation)",
        "prism_cam action: macro_fill_candidate (co-located with okuma_generate_* since it COMPOSES them)",
        "MacroFillOrchestratorEngine.test.ts (NEW — 20/20 passing in 762ms): happy + 3 negatives + 2 adversarial per family + dispatcher wiring + HARD RULE expression preservation assertions"
      ],
      "tests_run": "20/20 passing",
      "hard_rules_asserted": [
        "candidate.calculatedVars.VC130.formula === '[VC111 * 3.82] / VC110' (wafer-insert)",
        "candidate.calculatedVars.VC150.formula === '[VC121 * 3.82] / VC120' (top-hat)",
        "typeof formula === 'string' (never a pre-computed number)",
        "needsGate: true on every candidate (type-level reminder for U4)",
        "filledVars carry provenance source string (audit trail)",
        "Missing required VCs throw — orchestrator does NOT guess defaults",
        "Output has NO file/path/emitted properties (candidate, not a file)"
      ]
    },
    {
      "id": "MS0-U3",
      "title": "OkumaParametricProgramEngine: + generateWaferInsert + generateTopHatCasing",
      "status": "completed",
      "completed_at": "2026-05-13T18:30:00.000Z",
      "completed_by": "claude-48e5d403 (slot alpha)",
      "shipped": [
        "OkumaParametricProgramEngine.generateWaferInsert (modeled on BASE WAFER INSERT MACRO O1001, VC100+ form, 8 calculated VCs as expressions)",
        "OkumaParametricProgramEngine.generateTopHatCasing (modeled on BASIC TOP HAT CASING WITH SINGLE COUNTERBORE, VC100+ form, 17 calculated VCs as expressions, optional ID undercut + counterbore gated by IF/GOTO)",
        "OkumaParametricProgramEngine.getWaferInsertDefaults (per-material defaults: aluminum/stainless/brass/titanium/steel)",
        "OkumaParametricProgramEngine.getTopHatCasingDefaults (per-material defaults: aluminum/stainless/brass/steel)",
        "Wafer/TopHat config interfaces (geometry, OD chamfer/radius, drills, clearance, optional undercut, counterbore) — fully type-safe inputs",
        "Wafer/TopHat validators (reject negative length, finishOD>stock, ID>=OD, step OD<=small OD, step drill order, counterbore<=step drill 2, counterbore>step OD)",
        "prism_cam action: okuma_generate_wafer_insert",
        "prism_cam action: okuma_generate_top_hat",
        "prism_cam action: okuma_defaults extended with family routing (casing | cbore | waferinsert | tophat)",
        "OkumaParametricProgramEngine.test.ts (NEW — 33 tests passing, covers happy + 3 negatives + 2 adversarial per family + dispatcher wiring + HARD RULE assertions that VC130/VC150 stay as expressions matching /VC111.*VC110/ and /VC121.*VC120/)",
        "/okuma-macro skill updated with waferinsert + tophat args + family parameter + SAFETY-CRITICAL invariants block"
      ],
      "tests_run": "33/33 passing (1.07s)",
      "hard_rules_asserted": [
        "VC130 wafer-insert spot RPM stays as [VC111 * 3.82] / VC110 — never pre-computed",
        "VC150 top-hat spot RPM stays as [VC121 * 3.82] / VC120 — never pre-computed",
        "All 8 wafer-insert calculated VCs (VC130/131/132/133/134/135/143/144) emitted as expressions in gcode AND metadata",
        "All 12+ top-hat calculated VCs (VC150-153 RPMs, VC154-155 point depths, VC145 cb depth, VC156-167 derived) emitted as expressions",
        "Round-trip through MacroProgramIntelligenceEngine.parseOkumaMacro produces 6 toolDefs for wafer, 8 for top-hat, plus all auto-calcs",
        "No code path emits a final program file — output is a candidate object that must pass MS0-U4 gate"
      ]
    },
    {
      "id": "MS0-U4",
      "title": "Safety gate around the candidate — S(x) >= 0.70 + envelope + sim + operator (SAFETY-CRITICAL load-bearing)",
      "status": "completed",
      "completed_at": "2026-05-13T18:50:00.000Z",
      "completed_by": "claude-48e5d403 (slot alpha)",
      "shipped": [
        "MacroCandidateGateEngine.ts (NEW, ~370 LOC) — composes envelope + materialSanity + spindleSpeed + drillRatio + calculatedVcExpression gates",
        "S(x) ≥ 0.70 HARD BLOCK threshold (constant SX_HARD_BLOCK_THRESHOLD) — no override in this pipeline",
        "GateSignature schema with weighted contribution to S(x) (sum=1.0): envelope 0.25, materialSanity 0.25, spindleSpeed 0.20, drillRatio 0.15, calculatedVcExpression 0.15",
        "Machine limits catalog for JM Die fleet (OKUMA_LB-3000-EX / LB-4000-MY / LU-15 + GENERIC_LATHE fallback)",
        "SafetyRecord (versioned 1.0.0) with signatures + envelope + sanity + spindle results",
        "SignoffDossier — built ONLY on pass; carries candidate + safetyRecord + needsOperatorReview:true + setupSheetSummary + physicsReport + 7-item firstPieceChecklist",
        "GateResult type: dossier=null on fail, NO file/path/emitted/outputFile property — emit NOTHING on FAIL asserted at type level",
        "prism_turning action: macro_gate_candidate (passes/fails the candidate, never emits)",
        "prism_cam action: macro_gate_candidate (co-located with macro_fill_candidate)",
        "MacroCandidateGateEngine.test.ts (NEW — 14/14 passing in 345ms): happy + 5 blocker scenarios + S(x) boundary check + weighted-sum math + dispatcher wiring + end-to-end U2→U4 flow"
      ],
      "tests_run": "14/14 passing (with full milestone test trifecta: 33 U3 + 20 U2 + 14 U4 = 67 tests all green)",
      "hard_rules_asserted": [
        "S(x) ≥ 0.70 HARD BLOCK — passed = sxScore >= 0.70 AND zero BLOCKED signatures",
        "ANY blocked signature ⇒ passed=false (independent of S(x) score — defense-in-depth)",
        "passed=false ⇒ dossier=null AND NO file/emit/path properties on the result",
        "needsOperatorReview:true on EVERY dossier (unconditional operator gate)",
        "Calculated VC expressions carried through dossier UNCHANGED (gate does NOT resolve them)",
        "Tainted candidate (pre-computed VC130='993.2' string) ⇒ calculatedVcExpression BLOCK",
        "Envelope failure (part length > machine Z travel) ⇒ envelope BLOCK + dossier=null",
        "Negative dim VC ⇒ materialSanity BLOCK (with allowlist for enable flags VC113/116/124/129/134/139/140 and VC142 ID-center default)",
        "Program max RPM > machine ceiling ⇒ spindleSpeedRange BLOCK",
        "candidate.needsGate must be true (REJECTS hand-crafted non-U2 candidates)"
      ]
    },
    {
      "id": "MS0-U5",
      "title": "Per-machine post-resolution + labelled .MIN emit (parametric or expanded by controller memory)",
      "status": "completed",
      "completed_at": "2026-05-13T22:00:00.000Z",
      "completed_by": "claude-af644f3f (slot bravo)",
      "shipped": [
        "MacroPerMachineEmitterEngine.ts (NEW, ~620 LOC) — composes U2 fill + U4 gate + per-machine .MIN emit; module-load drift-guard between U4's JM_DIE_MACHINE_LIMITS and U5's FLEET_METADATA",
        "FLEET_METADATA: 3 Okuma machines (LB-3000-EX, LB-4000-MY, LU-15) + GENERIC_LATHE fallback; controller routing by dialect (okuma_osp/fanuc/haas/mazak/siemens/mitsubishi/generic) with per-controller memory limits",
        "MacroCandidateGateEngine.JM_DIE_MACHINE_LIMITS export (single source of truth for machine IDs across U4↔U5)",
        "Zod input schema (PartRef + dossier + targetMachines) with shape validation + bespoke invariants (passed=true required, needsGate must be true)",
        "Path-traversal sandbox: customerName/partNumber sanitized; explicit cncProgramDir/partJsonPath asserted under libraryRoot via path.resolve",
        "Gate-contract assertion (passed=true but dossier=null throws — structural bug never silenced)",
        "Per-machine independence: one machine's gate throw / disk failure / dialect-pending NEVER aborts the loop — error becomes a typed PerMachineResult entry, loop continues",
        "structuredClone-based candidate cloning (preserves Date/Map/etc — replaces JSON.parse(JSON.stringify) footgun)",
        "Defense-in-depth candidate integrity re-check on input (typeof formula === 'string' + reject bare-numeric strings)",
        "Atomic file writes via safeWriteSync (temp + rename); corrupt part.json preserved as <path>.corrupt-<iso> NOT silently overwritten",
        "Comment-injection defense: Okuma OSP unmatched-paren risk neutralized by replacing ( and ) with [ and ] in header strings",
        "prism_turning action: macro_emit_per_machine (ACTIONS enum + case handler + Zod schema)",
        "prism_cam action: macro_emit_per_machine (co-located with okuma_generate_* + macro_fill_candidate + macro_gate_candidate)",
        "prism_turning + prism_cam: macro_gate_candidate (U4 dispatcher close-out — previously uncommitted)",
        "macroEmitPerMachineSchema in cadActionSchemas.ts with .describe() on every field per schemas.md convention",
        "MacroPerMachineEmitterEngine.test.ts (NEW — 34/34 passing): happy + 4 negatives + 5 adversarial + 6 path-traversal + 5 candidate-integrity + per-machine independence + non-Okuma dialect + comment-injection + corrupt-JSON preservation + structuredClone isolation + catalog parity drift guard + summary counters",
        "macroEmitPerMachine.dispatcher.test.ts (NEW — 7/7 passing): prism_turning round-trip + prism_cam ACTIONS enum + schema validation + anti-regression count"
      ],
      "tests_run": "41/41 passing (34 engine + 7 dispatcher; 67+41 = 108 total green across MS0-U2/U3/U4/U5)",
      "hard_rules_asserted": [
        "HARD RULE: no machine ever gets a file unless THAT machine's gate passed at S(x) >= 0.70 AND zero BLOCKED signatures — asserted per-machine in 'every emitted machine's sxScore is >= 0.70' test",
        "Calculated VC expressions (VC130, VC150) carried through to emitted .MIN file UNCHANGED — asserted by regex match on /VC130\\s*=\\s*\\[VC111\\s*\\*\\s*3\\.82\\]\\s*\\/\\s*VC110/",
        "Non-Okuma controllers exit with form: 'dialect-translation-pending' and file=null + dialectTranslationPending=true — asserted on GENERIC_LATHE",
        "targetMachines=[] returns ZERO machines (NOT a fallback to fleet) — asserted directly",
        "Path traversal via customerName/partNumber ('..', '/', '\\', Windows-reserved) throws — 4 dedicated tests",
        "Explicit cncProgramDir/partJsonPath outside libraryRoot throws — 2 dedicated tests",
        "Tainted candidate (pre-computed VC130='993.2' or formula=999 number) rejected at input — 2 dedicated tests",
        "Corrupt part.json preserved as .corrupt-<iso> rather than overwritten — explicit test verifies backup file exists with original content",
        "Per-machine independence: dossier built for LB-3000 re-gates each fleet machine independently — every machine's safetyRecord.targetMachine matches the machine being gated",
        "Catalog parity: module-load drift-guard throws if JM_DIE_MACHINE_LIMITS keys != FLEET_METADATA keys — asserted by successful import (a drift would prevent the test file from loading)",
        "Operator-review unconditional: needsOperatorReview:true at output level + part.json.lastMacroEmit + part.json.perMachinePrograms[i] + .MIN file header banner"
      ]
    },
    {
      "id": "MS0-U6",
      "title": "Bulk fan-out into _PART LIBRARY/ (gated, operator-confirmed, NEVER auto) + macro-bulk-emit-guard Stop hook",
      "status": "not_started"
    },
    {
      "id": "MS0-U7",
      "title": "/macro-program skill + close-out (digest regen + envelope close + 3-way scrutiny)",
      "status": "not_started"
    }
  ],
  "exit_gate": "All 7 units shipped; macro_* actions wired to prism_turning + prism_cad (+ prism_safety for the gate, prism_dev for the post) and round-tripped in tests; macro-bulk-emit-guard Stop hook in MINIMAL_ALLOWLIST and test passes; OkumaParametricProgramEngine has generateWaferInsert + generateTopHatCasing (4 families generatable); NO code path emits a final program file at S(x) < 0.70 or without an operator-review flag (asserted in U2/U4/U5/U6 tests); ENGINE_DIGEST/DISPATCHER_DIGEST regenerated; DOMAIN-STUDIO-NODE-MAP.md updated; milestone close-out written; 3-way scrutiny PASS; omega_floor 1.0 met.",
  "omega_floor": 1.0,
  "resources_utilized": [
    "OkumaParametricProgramEngine",
    "MacroProgramIntelligenceEngine",
    "OkumaMacroHeaderGeneratorEngine",
    "LatheProofCarryingEmitEngine",
    "LathePrintProgramEmitterEngine",
    "LathePrintProgramSignoffEngine",
    "LatheFirstPieceApprovalEngine",
    "LatheProgramSignoffDossierEngine",
    "ShopConfigurationEngine",
    "LathePartClassifierEngine",
    "LathePartFamilyPlanningEngine",
    "PartFolderOrganizerEngine",
    "PostProcessorPipelineEngine",
    "LathePostProcessorEngine",
    "PPOkumaTurningPostEngine",
    "OkumaB250LatheMasterPostEngine",
    "LathePostProcessorDialectValidatorEngine",
    "LatheReplayFrameCompilerEngine",
    "LatheEnvelopeBreachReplayEngine",
    "PPMacroFlowValidatorEngine",
    "PPMacroVariableValidatorEngine",
    "prism_safety:validate_physics",
    "CollisionPreventionEngine",
    "machineLimitGuard"
  ]
}
