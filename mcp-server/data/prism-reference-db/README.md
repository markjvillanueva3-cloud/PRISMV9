# PRISM Reference DB (extracted from the monolith)

Generated by `scripts/extract-monolith-databases.mjs`. 1859 data stores across 17 categories (13,920 records, 25.8MB), written as one `<category>.json` bundle per category.

Each bundle is `{ category, count, stores: { <NAME>: <data>, … } }`. Look up a store by name inside its category bundle; `MANIFEST.json` maps every store name → category + bundle + recordCount.

**Store names are case-sensitive.** `MACHINES`, `Machines`, and `machines` are THREE distinct stores (different monolith definitions) and coexist as distinct keys. Consumers (ERP / quoting / post) must key by the exact name — do NOT lowercase-normalize, or you will collide distinct datasets.

Sources: the 2 non-modular monolith HTMLs + `extracted_modules/` + `extracted/` + `data/materials_complete/` on `H:/PRISM`.

This is **separate and cross-referenced** — tooling/holders live in `../jm-die-database/`; nothing in `mcp-server/src/data/` was overwritten. See `MANIFEST.json` for the per-store index, `failed[]` for fail-loud extraction misses, and `crossRef[]` for src/data overlaps.

## Categories

- **other** (`other.json`) — 1047 stores, 6,236 records
- **materials** (`materials.json`) — 150 stores, 1,980 records
- **machines** (`machines.json`) — 131 stores, 1,209 records
- **process** (`process.json`) — 147 stores, 1,141 records
- **tools** (`tools.json`) — 37 stores, 956 records
- **cad** (`cad.json`) — 164 stores, 874 records
- **controllers** (`controllers.json`) — 43 stores, 346 records
- **workholding** (`workholding.json`) — 30 stores, 291 records
- **coolants** (`coolants.json`) — 19 stores, 183 records
- **holders** (`holders.json`) — 20 stores, 180 records
- **safety** (`safety.json`) — 11 stores, 129 records
- **post** (`post.json`) — 21 stores, 124 records
- **coatings** (`coatings.json`) — 9 stores, 102 records
- **physics** (`physics.json`) — 18 stores, 90 records
- **inserts** (`inserts.json`) — 5 stores, 45 records
- **cost** (`cost.json`) — 6 stores, 29 records
- **abrasives** (`abrasives.json`) — 1 stores, 5 records
