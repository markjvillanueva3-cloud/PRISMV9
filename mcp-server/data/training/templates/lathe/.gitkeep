# LathePartFamilyTemplateExtractorEngine output directory
# Tracked so the engine's default output path exists in fresh clones —
# templates themselves are .gitignored (regen-able from corpus snapshot).
# Owner: TRAINING-LEARNING-MS0/U1
