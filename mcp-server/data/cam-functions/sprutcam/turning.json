{
  "schemaVersion": 1,
  "system_id": "sprutcam",
  "section_key": "turning",
  "extraction_source": "SprutCAM 17 Turn Reference / training docs",
  "extraction_date": "2026-04-27",
  "milestone": "CAM-EXHAUST-MS0/U-CAM-FIDX-18",
  "summary": {
    "total_operations": 4,
    "total_parameters": 56,
    "categories": ["roughing", "finishing", "grooving", "threading"]
  },
  "operations": {
    "rough_turn": {
      "display_name": "Rough Turn",
      "category": "roughing",
      "description": "OD/ID rough turning with G71/G72 cycles. Supports profile detection, undercut handling, and pattern repeats.",
      "dialog_tabs": ["Profile", "Tool", "Stepover", "Cycle"],
      "parameter_count": 14,
      "parameters": {
        "profile": {
          "startZ": { "type": "number", "unit": "mm", "tab": "Profile", "required": true },
          "endZ": { "type": "number", "unit": "mm", "tab": "Profile", "required": true },
          "maxDiameter": { "type": "number", "unit": "mm", "tab": "Profile", "required": true },
          "radialStock": { "type": "number", "unit": "mm", "default": 0.3, "tab": "Profile" },
          "axialStock": { "type": "number", "unit": "mm", "default": 0.1, "tab": "Profile" }
        },
        "stepover": {
          "depthOfCut": { "type": "number", "unit": "mm", "default": 2.0, "range": [0.1, 20], "tab": "Stepover", "required": true },
          "passes": { "type": "integer", "default": 0, "description": "0 = compute from depth", "tab": "Stepover" },
          "direction": { "type": "enum", "values": ["axial", "radial", "contour"], "default": "axial", "tab": "Stepover" }
        },
        "cycle": {
          "cycleType": { "type": "enum", "values": ["G71", "G72", "G73", "longForm"], "default": "G71", "tab": "Cycle" },
          "leadInDistance": { "type": "number", "unit": "mm", "default": 2.0, "tab": "Cycle" },
          "retractMode": { "type": "enum", "values": ["clearance", "stock", "shortest"], "default": "clearance", "tab": "Cycle" },
          "tipRadiusComp": { "type": "boolean", "default": true, "tab": "Cycle" },
          "feedPerRev": { "type": "boolean", "default": true, "tab": "Cycle" },
          "ccsMode": { "type": "boolean", "default": true, "tab": "Cycle" }
        }
      }
    },
    "finish_turn": {
      "display_name": "Finish Turn",
      "category": "finishing",
      "description": "G70 finish turning with optional spring pass. Supports separate finish-tool selection from rough.",
      "dialog_tabs": ["Profile", "Strategy", "Tool", "Output"],
      "parameter_count": 12,
      "parameters": {
        "profile": {
          "startZ": { "type": "number", "unit": "mm", "tab": "Profile", "required": true },
          "endZ": { "type": "number", "unit": "mm", "tab": "Profile", "required": true },
          "stockToLeave": { "type": "number", "unit": "mm", "default": 0, "tab": "Profile" }
        },
        "strategy": {
          "direction": { "type": "enum", "values": ["rightToLeft", "leftToRight"], "default": "rightToLeft", "tab": "Strategy" },
          "leadInDistance": { "type": "number", "unit": "mm", "default": 1.0, "tab": "Strategy" },
          "leadOutDistance": { "type": "number", "unit": "mm", "default": 1.0, "tab": "Strategy" },
          "springPass": { "type": "boolean", "default": false, "tab": "Strategy" },
          "tipRadiusComp": { "type": "boolean", "default": true, "tab": "Strategy" },
          "cycleType": { "type": "enum", "values": ["G70", "longForm"], "default": "G70", "tab": "Strategy" }
        },
        "output": {
          "ccsMode": { "type": "boolean", "default": true, "tab": "Output" },
          "maxRPM": { "type": "number", "unit": "rpm", "default": 4500, "tab": "Output" },
          "feedPerRev": { "type": "boolean", "default": true, "tab": "Output" }
        }
      }
    },
    "groove_turn": {
      "display_name": "Groove Turn",
      "category": "grooving",
      "description": "Plunge grooving with optional finish pass. Supports G75 cycle output and peck strategy.",
      "dialog_tabs": ["Profile", "Strategy", "Tool", "Output"],
      "parameter_count": 14,
      "parameters": {
        "profile": {
          "centerZ": { "type": "number", "unit": "mm", "tab": "Profile", "required": true },
          "outerDiameter": { "type": "number", "unit": "mm", "tab": "Profile", "required": true },
          "rootDiameter": { "type": "number", "unit": "mm", "tab": "Profile", "required": true },
          "width": { "type": "number", "unit": "mm", "tab": "Profile", "required": true },
          "leftSideStock": { "type": "number", "unit": "mm", "default": 0.05, "tab": "Profile" },
          "rightSideStock": { "type": "number", "unit": "mm", "default": 0.05, "tab": "Profile" },
          "rootStock": { "type": "number", "unit": "mm", "default": 0.1, "tab": "Profile" }
        },
        "strategy": {
          "plungeOverlap": { "type": "number", "unit": "percent", "default": 80, "range": [10, 100], "tab": "Strategy" },
          "depthIncrement": { "type": "number", "unit": "mm", "default": 1.0, "tab": "Strategy" },
          "peck": { "type": "boolean", "default": true, "tab": "Strategy" },
          "peckRetract": { "type": "number", "unit": "mm", "default": 0.5, "tab": "Strategy" },
          "dwellAtRoot": { "type": "number", "unit": "s", "default": 0.2, "tab": "Strategy" },
          "groovePattern": { "type": "enum", "values": ["plunge", "step", "spiral"], "default": "step", "tab": "Strategy" },
          "finishPass": { "type": "boolean", "default": true, "tab": "Strategy" }
        }
      }
    },
    "thread_turn": {
      "display_name": "Thread Turn",
      "category": "threading",
      "description": "G76 single-point threading. Modified-flank infeed default. Multi-start support via Z-offset.",
      "dialog_tabs": ["Thread", "Passes", "Tool", "Output"],
      "parameter_count": 16,
      "parameters": {
        "thread": {
          "startZ": { "type": "number", "unit": "mm", "tab": "Thread", "required": true },
          "endZ": { "type": "number", "unit": "mm", "tab": "Thread", "required": true },
          "majorDiameter": { "type": "number", "unit": "mm", "tab": "Thread", "required": true },
          "pitch": { "type": "number", "unit": "mm", "tab": "Thread", "required": true },
          "starts": { "type": "integer", "default": 1, "range": [1, 8], "tab": "Thread" },
          "internal": { "type": "boolean", "default": false, "tab": "Thread" },
          "threadStandard": { "type": "enum", "values": ["metric", "UN", "UNF", "BSP", "NPT", "ACME", "trapezoidal"], "default": "metric", "tab": "Thread" }
        },
        "passes": {
          "infeedType": { "type": "enum", "values": ["radial", "modifiedFlank", "alternating", "fullFlank"], "default": "modifiedFlank", "tab": "Passes" },
          "infeedAngle": { "type": "number", "unit": "deg", "default": 29.5, "range": [0, 30], "tab": "Passes" },
          "firstCutDepth": { "type": "number", "unit": "mm", "default": 0.3, "tab": "Passes" },
          "minCutDepth": { "type": "number", "unit": "mm", "default": 0.05, "tab": "Passes" },
          "passes": { "type": "integer", "default": 0, "tab": "Passes" },
          "springPasses": { "type": "integer", "default": 1, "range": [0, 5], "tab": "Passes" },
          "cycleType": { "type": "enum", "values": ["G76", "G33", "G92", "longForm"], "default": "G76", "tab": "Passes" },
          "leadInLength": { "type": "number", "unit": "mm", "default": 3.0, "tab": "Passes" },
          "ccsMode": { "type": "boolean", "default": false, "description": "G97 fixed RPM is required for threading", "tab": "Passes" }
        }
      }
    }
  },
  "training_topics": [
    {
      "topic": "SprutCAM turning discipline",
      "key_concepts": ["G71/G72 cycle selection", "modified-flank infeed", "G97 vs G96 for threading"],
      "best_practices": [
        "G71 for monotonic Z OD profiles; G72 for face-direction; G73 for pattern repeats with offset",
        "Modified-flank is the default for 60-degree threads — radial loads only the tip; full-flank loads both sides",
        "G97 (fixed RPM) is mandatory for threading — surface-speed mode causes lead drift on tapered threads"
      ]
    }
  ]
}
