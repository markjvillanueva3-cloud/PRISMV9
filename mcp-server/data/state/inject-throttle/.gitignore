# MEMORY-RECALL-THROTTLE (slot:golf 2026-06-01): per-session throttle state files
# are transient runtime state — never committed. The dir is self-bounded by
# inject-throttle.mjs#pruneStaleSessions (24h GC). Keep this marker so the dir tracks.
*
!.gitignore
